# 報告（0018 適用と V-20 の実装 — 権限の全列挙で、さらに重い穴が出ました）— 2026-08-20

**宛先**: レビュー側／オーナー
**発行**: 開発側
**対象**: `REVIEW_ANON_EXPOSURE_VERDICT_20260820.md` の §6-1 / §6-2 / §6-3
**結果**: **`0018` を staging → 本番に適用済み。V-20 は両環境で合格。933 件 PASS / typecheck クリーン**

---

## 0. 🔴 権限を全列挙したら、裁定より重い穴が出ました

裁定の指示どおり「叩いて確かめる」から「**何が付与されているかを列挙する**」に道具を変えたところ、**HTTP で見えていたより悪い姿**が出ました。

### 適用前（staging・本番ともほぼ同一）

```
付与 30 行 / うち書き込み権限つき 24 行
```

| 発見 | 内容 |
|---|---|
| **裁定が指摘した `users` の書き込み** | `anon` / `authenticated` とも **DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE**。裁定のとおり |
| **★裁定に無かった: `ep_ledger` / `pp_ledger` の `TRUNCATE`** | `0002` は **`insert, update, delete` だけを revoke** しており、**`TRUNCATE` が残っていました** |
| **公開ビューにも書き込み権限** | `races_public` などにも `INSERT/UPDATE/DELETE` が付いていた |

### ★`TRUNCATE` がなぜ重いか

> **`TRUNCATE` は RLS の対象外です。ポリシーがあっても止まりません。**

`ep_ledger` / `pp_ledger` は **RLS 有効＋「自分の行だけ」のポリシー**があり、**読み取りは正しく守られていました**。しかし `TRUNCATE` はポリシーを通らないので、

> **公開鍵（anon）で、ポイント台帳を全消しできる状態でした。**

**★これは「守りが1枚ある」ことが「守られている」に見えていた形**です。私の前便は RLS の有無を見て `ep_ledger` を「保護されて0行」に分類しました。**その分類は読み取りについては正しく、破壊については何も言っていませんでした。**

**裁定の「測る軸が足りない」という指摘が、もう一段深いところにも当たっていました。**

### 適用後（両環境）

```
付与 13 行 / うち書き込み権限つき 0 行
```

---

## 1. `0018_lock_public_grants.sql` — 「既定で閉じる」形に反転

**個別に閉じる形をやめました。** `0002` が個別 revoke だったため、**後から足したテーブルには何の守りも付きませんでした**（`0017` に RLS が入ったのはレビュー側が要求したからで、構造が要求したからではない、という指摘のとおりです）。

| # | やったこと |
|---|---|
| ① | `revoke all on all tables in schema public from anon, authenticated`（**`TRUNCATE` / `REFERENCES` / `TRIGGER` も含めて落とす**） |
| ② | **`alter default privileges in schema public revoke all on tables from anon, authenticated`** — ★**将来のテーブルにも付かない**ようにする。これが無いと次のマイグレーションで穴が再生します |
| ③ | 公開ビュー4つに `select` のみ戻す |
| ④ | **`users`: RLS 有効 ＋ `using (id = auth.uid())` の SELECT ポリシー ＋ `grant select` のみ**（★`revoke all` にしない — S-2 と同型の事故を避ける） |
| ⑤ | 本人スコープの台帳4つに `authenticated` の `select` のみ戻す（**anon には戻さない**） |
| ⑥ | 実体テーブル9つの RLS を有効化（③で grant していないので既に読めないが、**二重に持つ**） |

### ★⑥について — 今回の教訓は「両方要る」です

- **RLS だけ**だと `TRUNCATE` が素通りする（今回の穴）
- **grant だけ**だと、1つ間違えた瞬間に開く

**どちらか一方では足りない**ことが実測で分かったので、両方掛けています。

### 当てる前の確認（裁定 §2 の必須項目）

| 確認 | 結果 |
|---|---|
| Web が anon で読んでいるもの | **`races_public` / `race_entries_public` / `race_odds_public` の3つだけ**（`grep "\.from('" apps/web/src` で全数）。いずれも ③ で戻している |
| ワーカー・`migrate.mjs` | `DATABASE_URL`（postgres）直結＝**anon/authenticated の権限を使わない** |
| RPC | `security definer`。**関数の権限はテーブル権限と別**なので影響なし |

### 適用と確認

- **staging** → V-20 合格 → **本番** → V-20 合格
- **★出力は切らずに全部残しました**（R-27。前回 `| head -4` で SIGPIPE を起こした反省）。本番の適用は **`0018` の1件のみ**で、位置引数の修正が効いていることも確認できました
- **本番ワーカーは健全**（適用後、直近20分で2レース生成・最新レースは現在時刻の21分後）

---

## 2. V-20 を実装しました（登録簿方式）

| 実装 | |
|---|---|
| `tools/lib/exposure-registry.mjs` | **public スキーマの全オブジェクトの「期待される姿」**（`public_view` / `owner_scoped` / `closed`）。21件を登録 |
| `tools/verify-anon-exposure.mjs` | 走査型の判定器（READONLY）。**付与の全列挙 → 登録簿と突合 → 振る舞いの確認** |
| `apps/cli/test/exposure-registry.test.ts` | 登録簿の論理を DB 無しで固定（10件） |

### 判定結果（本番）

```
✅ ★③ 登録簿に無いオブジェクトが無い（新しいテーブルを黙って足せない）
✅ 登録簿に、DB に存在しないものが残っていない
✅ ★① anon / authenticated に insert / update / delete / truncate が無い
✅ ★② 公開ビュー以外は anon から0行
✅ 公開ビューは anon から読める（塞ぎすぎていない）
✅ ★races.server_seed が anon から読めない（§8.6 Provably Fair）
✅ ★horses.potential が anon から読めない（§12.4）
✅ ★horses.genotype が anon から読めない（§5.5）
=== V-20 合格（8 件中・環境 production） ===
```

**V-19 #15（`user_identities` が anon から0行）は、登録簿の `closed` として吸収されました。**

### ★「塞ぎすぎていない」も測っています

**守りのゲートは、閉じる方向に倒れると気づきにくい**（画面が壊れて初めて分かる）ので、**公開ビューが読めることも合格条件に入れています。** 片側だけのゲートにしません（V-15 / V-12a で両側化した形と同じ）。

---

## 3. ★メタテストが私のツールを捕まえました（そして直し方を選びました）

`tool-guard.test.ts` の「読取専用に分類したものは書き込み文を持たない」が **`verify-anon-exposure.mjs` を落としました。**

**原因**: 検出パターンが `truncate` を**語**として見ており、**「anon に truncate 権限が付いていないか」を検査するツール**が、**書き込むツールに見えた**からです。

**★直し方を2つ考えて、後者を採りました**:

| | |
|---|---|
| (a) コメントの語を言い換える | **却下。** これは検出器を黙らせるだけで、**次に同じ検査を書く人がまた引っかかります** |
| **(b) `truncate <識別子>` という文の形で見るように直す** | **採用。** 他の3パターン（`insert into` / `delete from` / `update X set`）は元から文の形で、`truncate` だけが語で見られていました |

**そして、直したときに検出器が鈍っていないことを同じ試験で固定しました**（R-14）:

```ts
for (const sample of ['insert into t values (1)', 'delete from t where x',
                      'update horses set stats = 1', 'truncate t',
                      'truncate table t', 'alter table t add column c int']) {
  expect(write.test(sample), `★検出器がこれを見逃します: ${sample}`).toBe(true);
}
```

**★検出器の精度を上げる変更は、感度が落ちていないことを同時に示さなければ、ただの目潰しです。**

---

## 4. 環境間の差分（裁定 §3 の要求）

**適用前・適用後とも、staging と本番の付与に差はありませんでした。**

| | 適用前 | 適用後 |
|---|---|---|
| staging | 30 行 / 書込 24 | 13 行 / 書込 **0** |
| 本番 | 30 行 / 書込 24 | 13 行 / 書込 **0** |

**同一だったこと自体が情報**です — 穴は片方の環境の事故ではなく、**Supabase の既定をそのまま使っていたことに由来する構造的なもの**でした。

---

## 5. 残っているボール

| # | | 誰 |
|---|---|---|
| 1 | `QUESTIONS_SETUP_RPC_20260820.md` の裁定（**唯一やり残している照会**） | レビュー側 |
| 2 | 正典への反映（D-076〜078／V-19／**V-20**／R-25〜R-28／**0018 の経緯**） | レビュー側 |
| 3 | **VPS で `ls -l /opt/star-current`**（稼働コミットの確定）→ 配備の二択 | オーナー |
| **🔴 4** | **本番の service_role キー・DB パスワードのローテーション** | **オーナー** |

**★4 について、今回の発見で意味が変わりました。** `anon` キーで台帳を全消しできる状態だったということは、**この案件の権限設計が「既定で開いている」側にあった**ということです。`0018` で anon 側は閉じましたが、**service_role キーは RLS も grant も全部迂回します。** 平文でディスクに置かれていることの重みは、今回いっそう大きくなりました。
