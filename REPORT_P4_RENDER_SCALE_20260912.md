# REPORT_P4_RENDER_SCALE_20260912 — ★滲みを直した ／ ★先に測り方を作った記録

**日付** 2026-09-12 ／ **枝** `p4/race-30sec-cuts`
**着手前 HEAD** `f9b8e14` ／ **指示** `HANDOVER_P4_RENDER_SCALE_20260912.md` §0〜§3（★オーナー「進めてください」＝§6 A の許可）

★そのまま見る: **`http://localhost:3210/race?dev=1`** ／ ★戻し口: **`http://localhost:3210/race?dev=1&dpr=1`**

---

## 0. ★この便でしたこと

| # | 何 | 状態 |
|---|---|---|
| ⓪ | ★**壊していないことの測り方**（★引継ぎ書 §3）を先に作った | ★`tools/measure-render-scale.mjs` |
| ① | ★地面の走査線を**物理画素の行数**で回す | ★入れました |
| ② | ★画布を **`1280 × dpr`**（上限 2）で持つ | ★入れました |
| ③ | ★馬の焼きコマ 560 → 1120px | ★**手を付けていません**（★引継ぎ書どおり①②の後に別判断） |

★**引き伸ばしが無くなりました。**

| | 画布 | 画面（★1152 CSS px × dpr 1.5） | |
|---|---|---|---|
| ★直す前 | 1280×720 | 1728×973 | ⚠️ ★**1.35 倍に引き伸ばし** |
| ★直した後 | **1920×1080** | 1728×973 | ★**0.9 倍に縮小**（★縮小は滲みません） |

---

## 1. ⓪ ★先に作った測り方 — `tools/measure-render-scale.mjs`

★引継ぎ書 §3 の 4 項目をそのまま測ります。★**オーナーの画面と同じ条件**で開きます
（★頁の上で 1152 CSS px・dpr 1.5。★窓の幅は**測って合わせます**。★手置きしません）。

| 何を | どう測るか |
|---|---|
| ① 細かさ | ★**芝・馬・文字**を 3 つの秒で切り出し、★輝度のラプラシアンの絶対値の平均 |
| ② 同じ絵 | ★画布の中身を 1280 に揃えて平均の差。★**差の地図も出す** |
| ③ 検定 | ★`npm test` / `npm run typecheck`（★道具の外） |
| ④ 速度 | ★再生中の実コマ数 |

★秒とカットは `out/race-through/cuts.md`（★通し 86 秒・16 カット）から選びました。

| 秒 | カット | 地面の描き方 |
|---|---|---|
| 30.0 | `side-drive` | ★`parallax-plate.ts` の**1 枚貼り**（★引継ぎ書 F-4） |
| 38.0 | `fourth-corner-far` | ★`world-textured.ts` の**走査線**（★F-3・★①が効くのはここ） |
| 51.0 | `straight-contest` | ★真横（★オーナー「とりあえず OK」の画） |

⚠️ ★**枠は下見（`--probe`）で全景に目盛りを焼いて、目で見て決めました。**
★毎回 `crop-*.png` を出すので、★**測った所が後から確かめられます**（★中身が変わったら取り直すこと）。

### 1-1. ⚠️ ★作っている途中で踏んだこと（★同じ所を踏まないように）

| # | 踏んだこと | 直し方 |
|---|---|---|
| M-1 | ★秒を送っても **0 に戻された**。★`/race` は ★**素材を読み終えた時点で `auditSec`（＝0）をもう一度当てます** | ★送ったあと ★**秒と `__raceDiag` の両方**を確かめ、★駄目なら送り直す |
| M-2 | ⚠️ ★**「画布 1280x720 → 1280x720」と嘘を印字した。** ★開いた直後の画布は ★**JSX が焼いた値のまま**で、★実際の大きさは描かれた後に決まる | ★**描かれた後に読む**。★全ての秒で同じでなければ止める |
| M-3 | ★速度が ★**31 / 56 / 49.7 / 53.3 コマ/秒**と振れた。★別々に起こしたブラウザを比べると ★**相手の機嫌**を測ることになる | ★**同じセッションの中で、画布の大きさだけ**を `?dpr=1` で切り替えて交互に測る |

---

## 2. ①② 何を変えたか

★**倍率の出どころは 1 か所**です（★`packages/render/src/pixel-scale.ts`・★R-31）。
★画布の大きさ・描画の変換・地面の走査線の 3 か所が、★すべてここから引きます。

### 2-1. ② 画布（`apps/web/src/app/race/page.tsx` の `render`）

- ★`cv.width = 1280 × 倍率`（★上限 2・★下限 1）。★`ctx.setTransform(倍率, …)` で拡大
- ★**描画コードは 1 行も変わりません**（★座標は 1280×720 のまま）
- ⚠️ ★`canvas.width` への代入は**文脈の状態を消す**ので、★大きさが変わったときだけ代入します
- ⚠️ ★JSX の `width` 属性では変えられません（★SSR の値に戻される）。★**描くたびに**設定します

### 2-2. ① 地面の走査線（`packages/render/src/world-textured.ts`）

- ★地面（`:296`）と ★地平線のかすみ、★**引継ぎ書が指した 2 つのループだけ**
- ★投影の計算は**論理座標のまま**。★貼る先の刻みだけを物理画素に
- ⚠️ ★開始行を ★**物理画素の格子へ丸めます**。★半画素ずれたまま貼ると、★かえって滲みます
- ★`mow-stripes.ts`（多角形塗り）と `parallax-plate.ts`（1 枚貼り）は ★**自動で追随**（★F-4 のとおり）

### 2-3. ★戻し口

★**`/race?dpr=1`** … ★引き伸ばしていた頃の画素数に戻ります。

---

## 3. ★結果

### 3-1. ① 細かさ（★同じ表示の大きさで比較・★芝が下がったら不合格）

| 秒 | 芝 | 馬 | 文字 |
|---|---|---|---|
| 30（真横・1 枚貼り） | 4.99 → 5.89（**1.18 倍**） | 6.83 → 11.03（**1.62 倍**） | 10.88 → 13.88（**1.28 倍**） |
| 38（4 角・走査線） | 5.42 → 7.51（**1.39 倍**） | 7.46 → 12.12（**1.63 倍**） | 9.73 → 12.51（**1.29 倍**） |
| 51（直線） | 5.21 → 6.13（**1.18 倍**） | 9.70 → 14.04（**1.45 倍**） | 9.99 → 12.53（**1.25 倍**） |

→ ★**9 か所すべてで上がりました。★下がった所はありません。**
⚠️ ★前回（`?render=2x`）は ★**文字だけ 2.72 倍で芝が破綻**していました。★今回は芝も上がっています。
★1:1 の見比べ（★拡大なし・PNG）: `out/render-scale/ab-38-horse-turf.png` ／ `ab-38-text.png`

### 3-2. ② 同じ絵であること

| 秒 | 平均の差 | 判定 |
|---|---|---|
| 30 | **1.39 階調** | OK（★基準 2 未満） |
| 38 | **1.29 階調** | OK |
| 51 | **1.04 階調** | OK |

⚠️ ★数字だけで「縁の滑らかさです」と言い切らないため、★**差の地図**を出しました
（`out/render-scale/after/diff-*.png`）。★光っているのは ★**輪郭線だけ**で、
★馬・HUD・コース図・馬名プレートは ★**1 つも動いていません**（★動いていれば面で光ります）。

### 3-3. ★戻し口が本当に元へ戻るか（★検定②より強い証拠）

★`?dpr=1` で撮って「直す前」と突き合わせました。

| 秒 | 差 |
|---|---|
| 30 | ★**最大 0 階調** |
| 38 | ★**最大 0 階調** |
| 51 | ★**最大 0 階調** |

→ ★**1 画素も違いません。** ★「倍率 1 では何も変えていない」を実画面で言えます。

### 3-4. ③ 検定

- ★`npm test` **150 ファイル・1572 件 全緑**（★着手前 149 / 1563 ＝ ★**＋1 ファイル・＋9 件**）
- ★`npm run typecheck` 緑
- ★新しい道具は `tools/lib/classification.mjs` へ登録済み（★R-24）

★新しい検定 `packages/render/test/pixel-scale.test.ts`（9 件）:
⚠️ ★定数を読むだけにしていません。★**`drawTexturedWorld` が実際に何行貼るか**を数えます
（★倍率だけ上げて呼び出し側が渡し忘れても緑になる形を避けるため・★R-16 の家族）。

①★上限 2 ②★下限 1 ③★戻し口 `?dpr=1` ④★壊れた URL は端末の値へ
⑤★**倍率 1 なら貼る指示が指定なしと完全一致** ⑥★倍率 1.5 で**行数 1.5 倍・貼る高さ 1/1.5**
⑦★**貼る先が物理画素の整数行に乗る**（★半画素ずれで滲ませない）

---

## 4. ⚠️ ④ 速度 — ★**下がりました。ご判断をお願いします**

★同じセッションの中で、★画布の大きさだけを切り替えて 3 回ずつ測った中央値です。

| カット | ★画布 1280（★直す前と同じ画素数） | ★画布 1920（★直した後） | |
|---|---|---|---|
| 30 秒 `side-drive` | 55.0 コマ/秒 | **41.5** | ★−25% |
| 38 秒 `fourth-corner-far` | 21.5 コマ/秒 | ⚠️ **16.0** | ★−26% |

- ★測ったブラウザは ★**実 GPU**で動いていました（`ANGLE (Intel Iris Xe, D3D11)`）。
  ★ソフトウェア描画の数字ではありません
- ⚠️ ★**4 コーナーは、直す前から既に 21.5 コマ/秒**でした。★元から重いカットです
- ⚠️ ★これは ★**この機械 1 台の数字**です。★オーナーの環境での見え方は別に確かめる要があります

★**止める必要があれば `?dpr=1` で即戻せます。**

---

## 5. ⚠️ ★途中で 1 度、私の変更でない差を「差」として報告しかけました

★`?dpr=1` と「直す前」を比べたとき、★38 秒だけ ★**最大 2 階調・3278 画素（0.36%）**の差が出ました。
★形は ★**遠くのスタンド帯に沿った斑点**でした。

★そのまま「走査線の副作用です」と書かず、★無害な説明を潰しました（★台帳「交絡を潰してから印と呼ぶ」）:

| 潰した順 | 確かめたこと | 結果 |
|---|---|---|
| 1 | ★同じ条件で 2 回撮ったら差が出るか | ★**差 0**（★描画は再現する） |
| 2 | ★旧 `world-textured.ts` を取り出して**描画命令を全部突き合わせ** | ★**39,408 命令が完全一致**（★倍率 1 では変えていない） |
| 3 | ★1 コマ描き終えた時点の変換行列が単位行列か | ★**単位行列**（★変換の漏れは無い） |
| 4 | ★**ビルドし直しただけ**で変わるか | ⚠️ ★**これでした。★差が 0 になりました** |

→ ★**開発サーバの古いビルドが残っていただけ**でした。★同じ理由で `after` も取り直しています。
★もし 1〜3 を省いて出していたら、★**在りもしない副作用**をご報告していました。

---

## 6. ★手を付けていないもの

| # | 残件 | 状態 |
|---|---|---|
| R-a | ★③ 馬の焼きコマ 560 → 1120px（★64MB → 約 256MB） | ★**未着手**（★引継ぎ書どおり①②の後に別判断・★下の B） |
| R-b | ★`perspective-draw.ts:356 / :367` の空のグラデーションも ★**論理座標の 1px 帯**で回っている | ★**触っていません**（★引継ぎ書が指した 2 ループの外）。★今より悪くはなりません |
| R-c | ★デフォルメの俯瞰素材（★4 角で「上から見た絵」が要る） | ★2026-09-11 朝から動いていません |
| R-d | ★芝の目①の見え方（★カメラの高さ・位置） | ★未着手 |
| R-e | ★通し 86 秒・16 カットの不合格箇所 | ★**ご指摘待ち**（★秒で言っていただければカットが分かります） |

---

## 7. ★オーナー判断待ち

| # | 論点 |
|---|---|
| **A** | ⚠️ ★**速度**。★4 コーナーが 21.5 → 16.0 コマ/秒。★このまま進めるか、★`?dpr=1` に戻すか、★上限を下げるか |
| **B** | ★③ 馬の焼き直し（★64MB → 約 256MB）。★①②の後の今、★**馬だけ気になるか**をご覧いただきたい |
| **C** | ★通しの中身（★86 秒・16 カット）の不合格箇所。★滲みと別に、★秒でご指摘ください |

---

## 8. ★動かし方

```bash
npm run dev:web                                  # ★http://localhost:3210
# ★そのまま見る    http://localhost:3210/race?dev=1
# ★戻し口          http://localhost:3210/race?dev=1&dpr=1

node tools/measure-render-scale.mjs --probe          # ★下見（★枠を決める全景・目盛りつき）
node tools/measure-render-scale.mjs --label before   # ★基準値
node tools/measure-render-scale.mjs --label after    # ★直した後（★before と突き合わせて合否）
node tools/measure-render-scale.mjs --revert         # ★戻し口が元の絵に戻るかの照合
npm test && npm run typecheck
```
