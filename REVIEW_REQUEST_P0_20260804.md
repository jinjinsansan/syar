# P0 レビュー依頼 — 開発側 → レビュー側

**発行**: 2026-08-04（開発側 Claude）
**対象**: 指示書 `DEV_INSTRUCTIONS_P0_20260804.md`（第1号）
**成果物**: `https://github.com/jinjinsansan/syar`（private）/ branch `main` / commit `b237360`

---

## 0. まず結論

| 検証 | 目標 | 実測（seed 42） | 判定 |
|---|---|---|---|
| V-1 | potential SD が平均の 12〜18% | 13.93% | **PASS** |
| V-2 | 100世代後の平均能力が初期比 +50% 以内 | **+64.96%** | **FAIL** |
| V-3 | 隔世遺伝/大物覚醒/大突然変異が設定値 ±0.5pt | 乖離 最大 0.022pt | **PASS** |
| テスト | 全ユニットテスト PASS | 42件 PASS | **PASS** |
| 決定論 | seed 42 の出力が毎回一致 | MD5 一致を確認 | **PASS** |

**V-2 は定数調整では達成できません。**正典 §6 の遺伝機構に「選抜停滞（selection plateau）」を表す要素が存在しないことが原因で、指示書 §6 に従い `QUESTIONS_P0_20260804.md` **Q1** に照会を出しています。**指示書 §4 のとおり目標値には一切触れていません。**

---

## 1. 独立再実行の手順（コピペで動きます）

環境: Node 22 / npm 10 で動作確認済み。ネットワーク接続・DB は不要です。

```bash
git clone https://github.com/jinjinsansan/syar.git
cd syar
npm install

# ① ユニットテスト（42件）
npm test

# ② 型チェック（strict / any 使用ゼロ）
npm run typecheck

# ③ 100世代シミュレータ（出荷既定）
npm run simulate -- --generations 100 --seed 42
```

> **③ の exit code は 1 です。**これは異常終了ではなく、V-2 が FAIL のときに 1 を返す仕様です（V-1/V-2/V-3 すべて PASS で 0）。

```bash
# ④ 指示書 §3.4 の文字どおりの既定を再現する（後述 §3 の逸脱の確認用）
npm run simulate -- --generations 100 --seed 42 --selection-h2 1

# ⑤ QUESTIONS Q1 の提案A（三検証すべて PASS する設定）
npm run simulate -- --generations 100 --seed 42 \
  --set-balance REGRESSION_RATE=0.2 --set-genetics MUTATION_SD=90

# ⑥ 多シード比較（QUESTIONS Q1 の表を再現。4シード × 6設定）
npx tsx apps/cli/src/sweep.ts

# 決定論の確認（2回実行して同一になること）
npm run simulate -- --generations 100 --seed 42 --json out/a.json --quiet
npm run simulate -- --generations 100 --seed 42 --json out/b.json --quiet
md5sum out/a.json out/b.json

# 全オプション
npm run simulate -- --help
```

---

## 2. 読む順番の推奨

1. **本書** — 全体像と再実行手順
2. **`REPORT_P0_20260804.md`** — 指示書 §5 の様式に沿った完了報告。**§5 に仕様からの逸脱・解釈で埋めた箇所を20件すべて列挙**しています
3. **`QUESTIONS_P0_20260804.md`** — 判断が必要な照会4件（Q1 が最重要）
4. コード — `packages/sim-engine/src/` が正典 §6 の実装本体

---

## 3. 既定を1つ変更しています（要承認・REPORT §5-1）

指示書 §3.4 の「種牡馬は能力合計上位20%からランダム選択」を素直に実装すると、**プレイヤーにも見せないはずの真の `potential`（正典 §5.5）で種牡馬を選ぶ**ことになります。現実にはあり得ない完全情報選抜であり、選抜応答を最大化します。

そこで「観測能力 = 真の能力 + 個体固有ノイズ」を導入し、その精度を遺伝率 h² で表現しました。**既定 `selectionH2 = 0.3`**（競走能力の実測遺伝率）です。

- **指示書どおりの再現**: `--selection-h2 1`（実行時にもこの旨を出力します）
- 上位20%・ランダム選択・年間種付上限といった選抜構造そのものは指示書のままです
- h²=1 では V-1 も FAIL します（8.44%）。既定 0.3 なら V-1/V-3 が PASS し、争点が V-2 一つに絞れます

これ以外に**正典 §13.1 の定数は1つも変更していません。**

---

## 4. 特に検算していただきたい3点

### 4-1. V-2 が構造的に達成不能である、という主張の是非【最重要】

主張は3つです。いずれもコードを読むか再実行すれば検証できます。

1. **正典 §13.3 の「抑制の三本柱」は平均の上昇を止めない。** 上限1000は創始平均450から見て +122% の位置にある壁で +50% を守れません。平均0の対称な突然変異は、方向性選抜と組むと毎世代その正の裾が選ばれるため**抑制ではなく燃料**として働きます（`--set-genetics MUTATION_SD=0,BIG_MUTATION_SD=0` で +64.96% → +43.98%）。headroom は current 側の機構で potential には作用しません（正典 §7.3）。
2. **V-1 と V-2 は同じ「ばらつき ÷ 平均」に支配される表裏の関係にある。** V-1 を 12% 以上に保つ分散があると、14世代の選抜で平均は必然的に +50% を超えます。実際 `--set-founders ABILITY_MEAN=700` で V-2 は通りますが、上限飽和で **V-1 が 4.90% に崩壊**します。
3. **seed 42 は比較的甘いシードだった。** 4シードで ±10pt 動きます（⑥ で確認可）。単一シードでの合否判定は危険で、これは正典 §13.3「たまたま通ったを許さない」への実地の回答でもあります。

解決案（平均回帰）は実装済みですが **`REGRESSION_RATE = 0`（無効）が既定**です。正典外の機構なので承認前に有効化しません。⑤ で挙動を確認できます。

### 4-2. 3×4 クロスの近交係数（QUESTIONS Q2）

指示書 §3.5-2 の「3×4クロス F=0.0703125」は、Wright の式から 3×4 単独では導出できません。実装とテストは **`F = (1/2)^(2+3+1) = 1/64 = 0.015625`**（サラブレッド生産で通用する「3×4 = 1.56%」と一致）を正としています。0.0703125 = 9/128 は複数の共通祖先の寄与の和としてなら厳密に作れるため、その例も別テストに入れました。

→ `packages/sim-engine/test/inbreeding.test.ts`（導出コメント付き・9ケース）

なお同ファイルでは **Wright の排他条件**（父側経路と母側経路は共通祖先以外を共有してはならない）も検証しています。これを省くと親子交配などで F が過大になります。

### 4-3. 突然変異の適用単位（QUESTIONS Q3）

正典 §6.4「各形質に平均0の正規分布ノイズを常時加える」を、**アレルごとに独立**と解釈しました。形質ごと（両アレルに同じノイズ）にすると V-1 の分散が約1.4倍になります。

---

## 5. リポジトリに入っていないもの（意図的）

clone しても以下は含まれません。

| 除外したもの | 理由 |
|---|---|
| `git.txt` | 認証情報を含む（指示書 §1-3）。読んでいません・使っていません |
| `files.zip` | 同上の指示による |
| `SPEC.md` / `CLAUDE.md` / `AI_PROMPT.md` | **廃棄済み文書。実在馬名・他社固有名称を計27件含むため**（憲法 §0.1）。ローカルには残してあります |
| `GORILLA_STABLE_SPEC_v1.2_infra.md` | 別プロジェクトの参考資料であり正典ではないため |
| `image-cockpit-for-codex-workflows/` | 独自の git リポジトリを持つ別プロジェクト |

---

## 6. レビュー側への申し送り1件（P0 の範囲外）

**`CLAUDE.md` が廃棄済みのまま生きています。**このファイルは Claude Code がセッション開始時に自動で最優先読み込みするため、現状では次に誰かがこのディレクトリでセッションを開いた瞬間、「SEGA スターホースのブラウザ再現・種牡馬に実在の名馬を使う」という**憲法違反の指示が有効になります**。

git 追跡からは外しましたが、ローカルのファイルは生きているので効果は残ります。正典を指す STAR 用の内容への差し替えが必要です。正典管理はレビュー側の担当と理解しているため、開発側の独断では書き換えていません。起草の要否をご指示ください。

---

## 7. 憲法（指示書 §1）の遵守状況

1. **命名** — 他社製品由来の語はコード・コメント・出力のいずれにも使用していません（`git grep` で検証可）。P0 にポイントは登場しません
2. **実在馬名** — 使用していません。テスト用識別子は `HORSE_000001` / `A` / `SS` 等の無機質なものです
3. **`git.txt`** — 読まず・使わず・コミットせず。`.gitignore` 登録済み。外部ネットワーク接続は一切ありません
4. **決定論** — エンジン・CLI ともに `Date.now()` / `Math.random()` 不使用（`git grep` で検証可）。乱数はすべて注入で、同一シードの出力バイト一致を自動テスト化しています
