# 報告 — ゴール前の接戦（P4）: スコア→タイム写像の形（γ）と比較映像

作成日: 2026-08-25 / ブランチ `p4/race-30sec-cuts` / 基点 `aa52b70`
指示書: `DEV_INSTRUCTIONS_P4_FINISH_CONTEST_20260825.md`
★**採否は書きません**（オーナー判定事項）。

---

## 0. ★指示から外れた判断（R-8: 逸脱の自己申告）

| # | 逸脱 | 理由 |
|---|---|---|
| **D-1** | 指示書の表題は「**比較映像2本**」ですが、本文 §2 は **γ=1.0/1.3/1.6 の3本**を必須としています。★**3本**で出しました | 本文の指定を優先。現行（1.0）が並んでいないと差が読めないため |
| **D-2** | 3-5 で **staging / 本番の DB には接続していません**。コードとマイグレーションの読み取りだけです | 外部サービスへの接続は指示書の範囲外で、オーナー承認が要ると判断しました。★接続した読み取り調査が必要ならご指示ください（`readonly` の道具で可能です） |
| **D-3** | 3-2 の N は指示の「1000 程度」どおり **1000** ですが、3-3 は **200 レース**です | 3-3 は位置モデルを毎回組み直すため実行時間が伸びます。★比が γ 間で一定であることの確認には十分と判断しました |
| **D-4** | 検証中に **`tools/lib/race-audit-build.mjs` に `balance` の差し替え口**を足しました（未追跡の道具） | I-5「道具側で balance を差し替える」を満たすため。★製品コードではありません |

---

## 1. I-1〜I-6 の実施状況

| | 内容 | 実施 |
|---|---|---|
| **I-1** | `TIME_GAP_SHAPE_GAMMA` を `balance.ts` に新設・既定 1.0 | ✅ `packages/race-engine/src/balance.ts:421-449`（型）/ `:540-542`（既定値） |
| **I-2** | ★**γ===1 は現行の式へ短絡** | ✅ `packages/race-engine/src/race.ts`（下記 §2） |
| **I-3** | `r_last <= 0` は全員 0 秒 | ✅ `gapOf()` の 2 行目 `if (!(ratioLast > 0)) return 0;` |
| **I-4** | ★着差ラベルは**確定した `timeGap` の隣接差**から | ✅ `timeGaps` を先に確定 → `marginLabel(timeGapSec - prevGap)` の `prevGap` は `timeGaps[i-1]` |
| **I-5** | 比較映像は**道具側で balance を差し替え** | ✅ `tools/verify-time-gap-shape.mjs` / `tools/lib/race-audit-build.mjs`（本番既定は無改変） |
| **I-6** | 実画面用の URL クエリ | ✅ `/race?contest=1.3`。★既定・不正値・範囲外はすべて **`DEFAULT_RACE_BALANCE.TIME_GAP_SHAPE_GAMMA`（=1.0）へフォールバック**。デモ `/race` の `build()` 限定で、サーバー確定経路には入れていません |

### ★I-2 の短絡を「どう保証したか」

実装は 2 段です。

```ts
const linearGapOf = (score) => winner.finalScore > 0
  ? ((winner.finalScore - score) / winner.finalScore) * baseTimeSec * balance.TIME_GAP_FACTOR
  : 0;                                   // ★現行の式そのもの（掛ける順序も同じ）
const gapOf = (score) => {
  if (gamma === 1) return linearGapOf(score);     // ★短絡
  ...
};
```

★**保証は「同値のはず」ではなく実測です。** `tools/verify-time-gap-shape.mjs` が
**現行の式を道具の中に写し**、製品の出力と `Object.is` で突き合わせます
（`===` は -0 と 0 を区別しないため `Object.is`）。

```
1000 レース / 23976 個の数値を Object.is で比較 … ★不一致 0 個
```

---

## 2. 3-1 既定が動かないこと

| 検査 | 結果 |
|---|---|
| `timeGapSec` / `timeSec` が現行の式と**ビット一致** | ★**23976 個中 0 個の不一致**（1000 レース） |
| `npm test` | ★**115 ファイル / 1130 件パス**（終了コード 0） |
| `npm run typecheck` | エラーなし |

⚠️ ★**バイト比較はしていません**（指示 3-1 の注記どおり）。決定論の数値で比べています。

### ★縮退ケースで 1 つだけ挙動が変わります（正直に）

旧 `prevGap` は分母に `(winner.finalScore || 1)` を使っており、
★**勝者のスコアが 0 以下のとき**だけ `timeGapSec`（0 になる）と食い違う値を出していました。
新実装ではラベルも `timeGaps` から取るので、この場合ラベルは 0 差になります。
**実レースでこの状態が起きるかは確認していません**（全馬のスコアが 0 以下のときのみ）。

---

## 3. 3-2 ゲートが動かないこと

```
1000 レース × γ(1.3 / 1.6) … ★着順列の不一致 0 レース
```

着順が一致するので、V-4 / V-5 / V-6 は**定義上**不変です
（レビュー側が `verify-race.ts` に `timeSec` 参照ゼロを確認済み）。10万レースは回していません。

### ★1000 本中 1 本、既存の不具合で計測から外したレースがあります

seed **1295** は `D-059`（位置モデルの最終順 ≠ 確定着順）で落ちます。
★**この便の変更とは無関係**です — 変更を `git stash` で退避した HEAD でも同じシードが落ちました。
道具は**飛ばした本数を必ず出す**ようにしてあります（無言の間引きをしない）。

⚠️ これは**このデモ画面が落ちる**ことを意味します（`page.tsx:707` の同じゲート）。
★本報告の範囲外なので直していません。**別件として扱ってください。**

---

## 4. 3-3 解析値と位置モデルの一致

| γ | 解析値（1着-2着） | 位置モデル | 比（解析÷モデル） |
|---:|---:|---:|---:|
| 1.0 | 2.65 馬身 | 2.82 馬身 | **0.938** |
| 1.3 | 1.50 | 1.62 | **0.937** |
| 1.6 | 0.89 | 0.96 | **0.936** |

★**比は γ を変えてもほぼ一定**（0.936〜0.938）。解析値は位置モデルを一定の割合で
過小評価しますが、**γ による歪みは持ち込みません**。

⚠️ ★**レビュー側の「γ=1.0 では 1.03 倍で一致」とは食い違います**（こちらは 1/0.938 = 1.066 倍）。
測り方の違いだと思われます（当方は「勝馬がゴールした瞬間の 2 着との差」を位置モデルから取り、
解析値は `timeGap × 距離÷baseTime ÷ 2.4`）。★**どちらが正しいかは判定していません。**

---

## 5. レビュー側の表の再現

| γ | 1着-2着 | 4頭が5馬身内 | 独走(3馬身+) | 総差 p10/p50/p90 |
|---:|---:|---:|---:|---|
| 1.0 | 2.61 馬身（レビュー 2.65） | 32%（31%） | 44%（44%） | 13.64/17.61/25.63 |
| 1.3 | 1.47（1.47） | 55%（54%） | 26%（26%） | 13.64/17.61/25.63 |
| 1.6 | 0.82（0.86） | 70%（69%） | 18%（19%） | 13.64/17.61/25.63 |

★**総差の列が γ 間で完全に同一**であることを独立に再現しました（V-17② に触れていない証拠）。

⚠️ ★**総差の絶対値だけが食い違います。** レビュー側は 2.15/2.58/3.73、当方は 13.64/17.61/25.63 で
**約 6.7 倍**です。★**不変であることは一致しますが、量が一致しません。**
当方の定義は「(最下位の timeGap − 1着の timeGap) × 速度 ÷ 2.4」です。
★**定義の擦り合わせをお願いします**（どちらかが誤っている可能性があります）。

---

## 6. 検査

| | 結果 |
|---|---|
| `npm test`（γ 既定 1.0） | ★**115 ファイル / 1130 件パス**・**終了コード 0**（新テスト 6 件を含む） |
| `npm run typecheck` | エラーなし |
| `/race`（既定 = γ 1.0） | 200 |
| `/race?contest=1.6` | 200 |
| `/race?cinematography=v4` | 200 |

⚠️ ★`npm test | tail` の形で受けると**失敗しても終了コードが 0 になります**（パイプの終了コード）。
   ファイルへ落として `$?` を見ています。

---

## 7. 3-4 密集の副作用（★今回いちばん危ないところ）

道具: `tools/audit-contest-overlap.mjs`（seed 42・表示 34.0〜46.2s の決着部分・30fps・中央値）

| γ | 画面内の頭数 | 重なっている組 | いちばん近い 2 頭の隔たり | HUD の裏に入る頭数 |
|---:|---:|---:|---:|---:|
| 1.0 | 2.0 頭 | 0.0 組 | 2.50 馬体分 | 1.0 頭 |
| **1.3** | **3.0 頭** | **0.0 組** | 0.59 馬体分 | 1.0 頭 |
| **1.6** | **6.0 頭** | **1.0 組** | 0.46 馬体分 | ★**4.0 頭** |

★**γ=1.3 は重なりを作らずに頭数だけ増えます**（2 頭 → 3 頭）。
★**γ=1.6 は画面に 6 頭入る代わりに、重なりが出ます。**

実画面（`out/2d-overhead-stride/g16/f0225.jpg`・残り 39m）で確認した具体的な症状:

- 先頭 2 頭が**大きく重なり**、後ろの馬の**鞍布の馬番が前の馬に隠れます**
- 追走集団が**画面の左端**に寄り、★**左下の COURSE 表示の裏に入ります**（中央値 4 頭）

⚠️ ★「HUD の裏」の判定は**実測した矩形ではなく、絵から読んだおおよその矩形**です（目分量）。
   頭数の傾向を見るための目安として読んでください。

### ★もう 1 つ、映像で見えたこと（数値ではなく事実の報告）

γ=1.6 では**ゴール直前で先頭が入れ替わります**（順位表の 1 着が一時的に別馬になり、差 0.0）。
★**最終着順は 1000 レースで完全一致を確認済み**なので、これは
「決着直前に**抜きつ抜かれつが起きる**」という写像の効果です。実況の文言もそれに追随しています
（「ゴール前、アオバハヤテとミライノツバサ、並んでゴールへ！」）。

★これを「ドラマチック」と見るか「勝ち馬が分かりにくい」と見るかは**オーナー判定**です。

---

## 8. 3-5 記録の整合（★接続はしていません・コードの読み取りのみ）

| 事実 | 場所 |
|---|---|
| 走破タイムは `race_entries.finish_time numeric(7,3)` に保存 | `db/migrations/0001_init.sql:196` |
| 確定時に worker が書き込む | `apps/worker/src/pg-store.ts:357-360` |
| 公開ビューにも出る | `db/migrations/0006_public_views.sql:27` |
| 画面はその値を読むだけ | `apps/web/src/app/races/[id]/page.tsx:128` |
| ★**確定は `DEFAULT_RACE_BALANCE` で `resolveRace` する** | `apps/worker/src/settle.ts:47-52` |

★**ここが要点です。** 既定の γ を将来動かすと:

1. **過去のレコードは古い写像のまま**、以後のレースは新しい写像になり、**新旧が混在**します
   （`finish_time` は保存値であって再計算ではないため）
2. ★**Provably Fair の再現検証**（§8.6 / §8.8）で、第三者が公開された `server_seed` から
   レースを再計算すると、**着順は一致するが走破タイムは一致しません**。
   `settleRace` が既定の balance を使うためです
3. 着差ラベル（`marginLabel`）も同様に、過去の記録と再計算で食い違います

⚠️ ★**現状では「新旧混在をどう扱うか」の決めがありません。** 選択肢としては
（a）レースごとに使った γ を記録する（b）過去分を再計算して置き換える（c）表示だけ新写像にする
などが考えられますが、★**開発側では決めません。** 正典 §8.6 に関わるためレビュー側の判断が要ります。

★**staging / 本番に既存のレース記録があるか**は、接続していないので**わかりません**（上記 D-2）。

---

## 9. 出力した映像（seed 42・台本 v5・同じ表示秒 34.0〜46.2s・367 コマ / 30fps）

| 中身 | パス |
|---|---|
| γ=1.0（現行） | `out/2d-finish-contest/finish-contest-g10.mp4` |
| γ=1.3 | `out/2d-finish-contest/finish-contest-g13.mp4` |
| γ=1.6 | `out/2d-finish-contest/finish-contest-g16.mp4` |
| ★3 つを並べたもの | `out/2d-finish-contest/finish-contest-compare.mp4` / `.gif` |

- 撮り方: 通常 `/race` を**本物のブラウザで開き**、`?contest=` だけを変えて同じ表示秒でシーク
  （`tools/capture-overhead-stride.mjs`）。★台本・カメラ・seed・素材は同一です
- ⚠️ ★**コマのバイト比較で判定しないでください**（同じ条件で 2 回撮ると境目付近で食い違います）。
  ショット・画角の同一性は決定論の `auditSceneAt` で確認しています

### 余力分（seed 332 / 474 / 14）

★**撮っていません。** 1 本あたり約 5 分 × 3 シード × 3 γ = 45 分かかるため、
まず seed 42 の 3 本をお出しします。必要ならご指示ください（道具はそのまま使えます）。

---

## 10. ★この便で足したもの（指示書の文面を超えた分・R-8）

`packages/race-engine/test/time-gap-shape.test.ts`（6 件）を足しました。
指示書の範囲は「映像の出力と報告まで」ですが、**写像そのものは製品コードに入る**ため、
無防備なまま置くのは危ないと判断しました。守っているのは次の 5 点です。

1. 既定が 1.0 であること
2. ★γ=1.0 が現行の式と**ビット一致**すること（`Object.is`）
3. γ=1.3 / 1.6 / 2.2 で**着順が変わらない**こと
4. γ を変えても**総差が変わらない**こと（V-17②）
5. ★γ を上げると上位が締まること（R-14: **効いていない実装でも通る検査**にしない）
