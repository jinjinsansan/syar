# 第4便へのレビュー回答 — 技術レビュー合格

レビュー側: Codex。対象HEAD: `5da948e`。実装修正: `cb38b5b`。比較元: `01ef45e`。
依頼書: `REVIEW_REQUEST_P4_TRAFFIC_TYPES_FIX3_20260909.md`。

## 判定

**今回の修正範囲は合格。F-3を解消とし、前便で解消したF-4・F-5も含め、このレビューでの技術的な差し戻し事項をクローズする。**

画面と監査の接続を守る検査、独立した直線長の期待値、飽和と達成を区別する説明・境界検査を確認した。映像の美観や許容尺についての承認ではなく、対象コミットの技術レビューの判定である。

## F-3の確認

`race-clock-wiring.test.ts` は、前便で未検査だった時計の引数、方針条件、直線長の式を節点の種類と値で判定する形になった。現在の画面・監査にある時計の宣言と直線長の入力に適用されることを確認した。

レビュー側でも、提出された `wiringOf` / `violationsOf` をそのまま取り出し、メモリ上のソースコピーに前回の4変異を適用した。

| 変異 | 今回の結果 |
|---|---|
| `!LEGACY_MOTION` による対応逆転 | 条件の節点で検出 |
| `(void RACE_PACE_POLICY, 'legacy')` | 第3引数の節点で検出 |
| 時計に渡す距離を800へ固定 | 第2引数の節点で検出 |
| `(homeStretchMetersOf(course), 400)` | 直線長の式で検出 |

正常版は画面・監査とも違反0件。方針式の引用符を二重引用符へ変え、改行を入れた版も違反0件だった。書式と意味を区別できている。提出された検定には従来4件を含む8件の変異検査があり、正常版2件と合わせて10件になっている。

この検査は現コードの接続と指定した故障を対象とする。任意の書換えすべての安全性を証明する汎用解析としての合格ではない。

## 直線長と時計の照合

比較側が監査モデルの出力を読まず、試験入力の `hs` を使う修正を確認した。レビュー側でも、実際の `auditClock` と、指定した直線長から組んだ `raceClockFor` を照合した。

| 指定直線長 | 正常な本編尺 | 21点の時刻対応の最大差 | 監査モデルの直線長だけ400へ置換した本編尺 |
|---|---:|---:|---:|
| 290m | 66.4447349298秒 | 0秒 | 65.8140214306秒（不一致を検出） |
| 400m | 65.8157968604秒 | 0秒 | 65.8157968604秒（一致） |
| 620m | 60.9152184386秒 | 0秒 | 65.8190666067秒（不一致を検出） |

seed 42・1600mの3走路で確認。異常版はメモリ上のモデルコピーを渡したもので、製品ファイルは変更していない。400mだけ一致するのは期待どおり。他2条件では、比較側まで400へ引きずられる前便の穴が解消している。

## F-5の説明と境界

- 「構造上、目標に届かない」という旧コメントが撤回内容に更新された。
- `saturation` は倍率が端にいることだけを表し、目標達成は `achieved` で判定する説明になった。
- 最短端と最長端をそれぞれ目標にしたとき、saturationあり・achieved=trueが両立する検査が追加された。

F-4の計算を含む製品の実行ロジックは、この便では変更されていない。`time-warp.ts` の製品差分は注記の修正のみであり、前便のF-4/F-5解消判定を維持する。

## 再実行結果

- 型チェック: `TYPECHECK_EXIT=0`。
- 全体テスト: 144ファイル・1466件すべて成功、`TEST_EXIT=0`。23:13:09開始、221.57秒。対象4ファイルは10/16/6/4の計36件で、前便から5件増。
- 9条件の交通流監査: `AUDIT_EXIT=0`。新版は接近ペア・フレーム数0、最大横速度約1.30m/s、旧新版とも最終着順一致。既存の本編尺も前回と一致。
- レビュー側の反例・直線長プローブ: `PROBE_EXIT=0`。

証拠ファイル:

- `tmp/review-traffic-fix3-typecheck.log`
- `tmp/review-traffic-fix3-tests.log`
- `tmp/review-traffic-fix3-audit.log`
- `tmp/review-traffic-fix3-probes.mjs` / `.json`

コマンド直後の `$LASTEXITCODE` を保存し、表示したうえでそのコードで終了させた。ログの末尾だけから成功を判断していない。

開始時の追跡済み差分はなし。未追跡 `.claude/` は存在し、そのまま保持した。レビューで追加したのは本回答書と検証用プローブ・ログ。監査の出力JSONは再生成した。製品コード・既存テスト・正典は変更せず、コミット・push・配備も行っていない。

## 次の判断

開発側への追加の差し戻しはない。オーナー判断として残るのは、今回提示された範囲では次の2件。

1. 現行のレース映像・尺を採用するか。2400mは本編約93.1〜94.6秒、総尺約113.3〜114.8秒。採用する許容尺が決まった後、資料・正典・CIへ反映する。
2. 型Cの素材を描き直すか。

この判定は、全会場・全シード、実レース全条件の達成領域、フレーム単位の同一性、ブラウザ再撮影、本番配備の確認を含まない。3直線長の検査と既定走路9条件の監査を組み合わせて27条件を検査したという意味でもない。これらの未検証範囲を、今回の技術的差し戻しの残件とは混同しない。
