# 引継ぎ書 — レース映像の品質（オーナー指摘 9 件を直した便）

作成: 2026-08-28 / ブランチ `p4/race-30sec-cuts` / タグ `p4-race-quality-20260828`
起点: `2b2aa3d` → HEAD `051832c`（20 コミット）
前の引継ぎ書: `HANDOVER_P4_GAMMA_DECISION_20260827.md`
先行裁定: `REVIEW_P4_CUT_SEAM_VERDICT_20260827.md`

---

## ★0. この便でいちばん大事なこと — 方針が変わりました

★**「直す」を「減らす」でやっていました。** 途中でオーナーに止められています。

> どんどんカットしていけばレース演出としての品質が下がります。
> 開発がうまくいくことを最優先したところで得るものは何もありません。
> 結局のところユーザーがきません。お客様がこない。そのまま電子ゴミです。

★私がやっていたこと:

| 症状 | 私の処理 | 失ったもの |
|---|---|---|
| 4 角の境目で走行方向が反転 | ★**4 角の正面カットを外した**（案 B） | ★4 角の見せ場 |
| 発走直後がインベーダーに見える | 正面カットを 240m → 100m に**詰めた** | （これは妥当・飛び出しだけ残した） |
| 正面追従が遅く見える | ★**尺を 4.7 秒 → 2.4 秒に詰めた** | ★差し・追い込みを見せる時間 |

★**数字は良くなります。検査も緑になります。しかし中継としては貧しくなります。**

★**撤回して、両方とも「カットを良くする」で直しました**（§3）。

⚠️ ★**次のセッションへ**: 症状が出たときに **「そのカットを消す/縮める」が最初に浮かんだら、
　それは危険信号**です。★**先に「このカットが弱いのはなぜか」を測ってください。**
　この便では 2 回とも、★**弱さの原因は別にあり、直せるものでした**（自動ズーム／追従距離）。

★これをテストで固定しています。★**戻ったら落ちます。**

```
script-v5.test.ts  ★4 角の正面カットを台本から外さない（減らして直さない）
script-v6.test.ts  ★②の正面カットは尺を削らず、カメラが詰まることで速さを出す
                     （窓 0.05 以上・closeIn を外さない）
```

---

## 1. オーナー指摘と、その結果（9 件）

### 第 1 便（5 件）

| # | 指摘 | 原因 | 直し方 |
|---|---|---|---|
| ① | ゲートで 1 枠が白いガードに被る | ★ラチの**横木だけ**が深さに関係なく固定太さ（支柱は縮んでいた）。420m 先の横木が手前と同じ 3.2px | 横木も実寸 0.09m ×`pxPerM`。1px 未満は薄さで。空気遠近もラチに掛ける |
| ② | インベーダーみたいな動き | ★**エンジンは滑らか**（同期性 0%・内向き 59%・進路角 中央 0.23°）。★正面カットが**横の収束の全過程**を映していた（`start-front` 0〜240m と `SETTLE_M` 250m がほぼ一致） | 飛び出しだけ正面（0〜100m）、以降は斜め前が受ける |
| ③ | 別のレースに見える | ★**境目で走行方向が反転**（→83 → ←26 px/m）。★**カットの境目は一度も測られていなかった** | §2 参照（外す → 戻す → 迫る絵にする） |
| ④ | 騎手の色が同じ | 仕様（色は枠）。★**一度「現状維持」と決めたが実画面で不合格** | 帽子＝枠色 / 上着＝馬ごと（実際の JRA の形） |
| ⑤ | ゴール前を巨大化してリプレイ | 新規 | 本編の後ろに 4 秒・半速 |

### 第 2 便（4 件）

| # | 指摘 | 原因 | 直し方 |
|---|---|---|---|
| ① | 相変わらず騎手の色が同じ | ★上着まで枠色だった | `silk-1〜18` から**離れた 12 色**を選ぶ（最小色距離 27.6 → **38.8**） |
| ② | 18 秒で大きくなり、後退する | ★**別々の 2 つ**。(a) `frameContenders` が馬群に追従してズームが揺れる (b) ★**時間が 5 倍速のまま寄りのカットに入っていた** | (a) 画角固定 (b) 寄りは実時間に戻る 1200m から |
| ③ | リプレイと分からない | 表示が無い | 「リプレイ」を上に出す |
| ④ | 結果がリプレイ前 | 順序が逆 | 本編 → 勝馬 → リプレイ → 着順ボード |

### 第 3 便（2 件）

| # | 指摘 | 原因 | 直し方 |
|---|---|---|---|
| ⑤ | ゴールをリプレイしていない | ★**残り 8.3m で終わっていた。** 正体は `broadcastV2StartLagM` の発走の遅れ（`15.6×1.6÷3`）。位置モデルは 1600m 頭打ちなので**表示位置は 1600m に永久に届かない** | ★レース秒でなく**表示秒**を巻き戻す。基準は★**勝馬の通過**（本編の終わり＝最後の 1 頭とは 2.83 秒違う） |
| ⑥ | 急に遅くなる／迫力がなくなる | ★見た目の速さ＝**画面上で地面がどれだけ流れるか**。正面追従 113px/s（寄りは 2279px/s） | §3 参照 |

---

## 2. ③ の経緯（★裁定の前提が 2 つ崩れています・レビュー側へ要差し戻し）

1. **裁定は案 A（4 角を反対側から撮る）を選んだ** → ★**成立しませんでした。**
   掃引 32 通り（内馬場 w=−8〜−2m を含む）★**すべて向きは ←** のまま。しかも内馬場側は悪化（←14.8 → 24.9px/m）。
   ★向きを決めているのは「カメラがどちら側か」ではなく、**カメラを直線の先に置いて
   コーナーを回る馬を正面から受けている配置そのもの**。

2. **裁定は「`8c7203d` の revert 理由の解明」を着手条件にした** → ★**理由は記録されていました。**
   ★コミットにも報告書にも無く、★**`tools/audit-corner-turn.mjs` のコメントの中**にありました:
   > カット後半で本来と逆を向くため「全員斜めになりながら曲がっている・前回より酷い」
   > ★**跳びより「向きが逆のまま」のほうが悪い**

   ⚠️ ★**申し送り**: ★**「記録に無い」と言う前に、コードとコメントも探すこと。**
   　この案件は経緯をコメントに書く文化なので、★**文書と git だけを探すのは探索範囲の誤り**です。

3. → 案 B（外す）を採り、★**その後オーナー判断で撤回して戻しました**（§0）。

---

## 3. 「カットを良くする」で直した 2 件（★この便の中心）

### 3-1. 第 4 コーナー — 自動ズームが「迫ってくる」を打ち消していた

★固定カメラなのに `broadcastV2FixedFov` が**距離に関わらず馬を画面の 22% に保つ**ため、
★**近づいても大きくなりませんでした。** 掃引表の「馬高比 **21% → 21%**」がそれで、
★**私はそれを異常と思わずに読んでいました。**

→ `fixedCamera.approach` を新設（距離に応じて狙いの大きさを変える）。

```
カット内の馬  21% で一定 → ★18.7% → 29.4%
入りの跳び    1023 → 911px ／ 画角比 0.31× → 0.42× ／ 向きの反転量 ←26 → ←19
素材の入替 0 回 ／ カット内の左右反転 0 回
```

⚠️ ★**範囲の設定を一度外しました。** 150m→55m にしたらカットが終わるまでに伸び切らず
19.7% 止まり。★**実際の距離（190m→90m）を測って合わせました。**

### 3-2. 直線の正面追従 — カメラが抜かれないから速さが出ない

★正面追従はカメラが馬群の 32m 前を**同じ速さで走り続ける**ので、
★地面が横へ流れず、馬も大きくなりません（**113px/s**・真横の寄りは 2279px/s）。
★**実際の中継の正面カメラは馬に抜かれます。** だから馬が迫り、それが横の流れの代わりをします。

→ `closeIn` を新設（残り距離に応じて前方距離を 32m → 10m へ詰める）。

```
地面の流れ  113 → ★505 px/s
カット内の馬 39px/m 一定 → ★69.9 → 84.5px/m
実画面の馬高比 18.6% → 25.2% →（次のカットで）41.6% ＝ 連続した寄り
出口の跳び  1073 → ★303px（大きさ比 2.34× → 1.23×）
```

★どちらも**位置だけの関数**なので決定論は保たれます（憲法 4）。

---

## ★4. 測定器の是正（R-30 系・この便だけで 5 件）

★**通算 9 件目まで来ました。** 毎回「測定器が画面と違うものを読んでいた」形です。

| # | 何が違っていたか | 影響 |
|---|---|---|
| 1 | γ（道具 1.0 / 画面 1.3） | 前便の看板だった数字が別の写像のもの |
| 2 | `shot-race-at.mjs` が **`script` を一度も渡していない**（既定 v4） | 画面に無い 4 角を出し続け、案 B の確認で発覚 |
| 3 | `auditSceneAt` の `climax` 既定が画面と逆 | v6 の跳び px が演出入りの数字だった |
| 4 | `noContenderFrameShots` が**呼ぶ側任せ** | ゴール前の画角を 26.0° と報告（画面は 22°） |
| 5 | `audit-corner-turn.mjs` が **`'v5'` 固定** | v6 にしか無いカットを一度も見ていない |

★**共通の形**: ★**「渡さなければ画面と違う側に落ちる」**。R-27（既定は狭い側・安全な側へ）の
測定器版です。→ ★**2〜5 はすべて「画面の既定から引く」形に直しました。**

⚠️ ★**根治は未了**: γ は `audit-cut-seam.mjs` が `SCREEN_GAMMA = 1.3` を**自前で持っています**。
★同じ値を 2 か所で持つ形で、裁定 §9 が「共通土台に触るので別便」としたままです。

---

## 5. ★次にやること

### 5-1. レビュー側へ差し戻す（★未了）

★裁定の前提が 2 つ崩れています（§2）。★**このまま次の指示書を待つと、崩れた前提の上に積みます。**

- 案 A が成立しないこと（掃引 32 通りの実測）
- revert 理由が道具のコメントにあったこと、および「**向きが逆 ＞ 跳び**」という優先順位
- ★**宿題4（残った跳びの分布）は提出できる状態**です（20 seed・境界別）

### 5-2. 4 角の境目の反転（★残っています）

20 seed / 180 境目のうち **40 箇所**（入口・出口 ×20）。

- **出口**（4 角 → 真横）は **←7px/m**（ほぼ正面から迫る画）からの反転なので**弱い**
- **入口**（真横 → 4 角）は **→82 → ←19** で**実在する**

★次の一手は**閃光トランジション**（`broadcastV2FlashAt` が既にある）。
★**反転を消すのではなく、「角度が変わった」と読ませる**編集の定石です。★未着手。

### 5-3. γ の確定（★オーナー）

★**主軸 1.6 のまま確定していません。** 確定後、跳び px と共通頭数を測り直す必要があります（裁定 §9）。

### 5-4. v6 を既定にするか（★オーナー）

★**既定は v5 のままです。** この便で見ていただいた画は全部 `?cinematography=v6` です。
⚠️ ★ただし 4 角・勝負服・リプレイ・ラチは **v5 にも入っています**（台本に依らない直しのため）。

---

## 6. 残件（技術・前便から継続）

1. ★測定器と画面が γ を **1 か所から引く**根治（§4・共通土台）
2. ★`audit-contest-focus.mjs` が **0 コマのとき FAIL に倒れない**（0.00 / NaN% で「異常なし」に見える）
3. ★**能力順プール（D-018 準拠）で測り直していない**
4. ★「画面に入る走路 9.2m」の出どころが追えない（実測 5〜6m）
5. ★13 頭以上のとき勝負服の最小色距離が **27.5**（目安 30 未満）。その頭数を出すなら選び直しが要る
6. ★リプレイに COURSE ミニマップと ORDER 表が被る（馬に重なる）

---

## 7. 再現コマンド

```powershell
# 見る（dev サーバー: cd apps/web ; npm run dev）
#   ⚠️ contest= を省くと 1.3 / cinematography=v6 を落とすと v5 に黙って落ちます
http://localhost:3210/race?seed=42&cinematography=v6&contest=1.6

# 境目（走行方向の反転・跳び px・境界ごとの分布）
npx tsx tools/audit-cut-seam.mjs --seeds 42 --script v6 --gamma 1.6
npx tsx tools/audit-cut-seam.mjs --seeds 1,2,...,20 --script v6 --gamma 1.6

# 4 角の素材切替・左右反転（★--script を必ず付ける）
npx tsx tools/audit-corner-turn.mjs --script v6

# 見た目の速さ（画面上で地面が何 px/s 流れるか）
npx tsx tools/_groundflow.mjs --from 12 --to 45

# 静止画（★--script を必ず付ける。既定は画面の既定 v5）
npx tsx tools/shot-race-at.mjs --script v6 17.5 18.0 18.6

# リプレイ区間（ゴールを含んでいるか）
npx tsx tools/_replayspan.mjs
```

★`tools/_*.mjs` は `.gitignore` 対象・追跡外です（登録簿には載せます）。

---

## 8. 検証

```
npm test        → 120 ファイル / 1192 件 PASS（exit 0）
npm run typecheck → クリーン（exit 0）
```

★**未 push**（CLAUDE.md・オーナー指示待ち）。
⚠️ ★`apps/web/package.json`（ポート 3210）は**オーナーの既存作業**。コミットに混ぜていません。

---

## ★9. 申し送り

1. ★**症状を「カットを消す/縮める」で処理しない。** 先に「このカットが弱いのはなぜか」を測る（§0）
2. ★**「記録に無い」と言う前に、コードとコメントも探す**（§2-2）
3. ★**測定器は「渡さなければ画面と違う側に落ちる」形を作らない**（§4）
4. ★**時間を持つものは端まで見る。** リプレイを実装して「できた」とし、
   ★**終わりを一度も見ずに**報告しました。オーナーに「ゴールをリプレイしていますか？」と
   問われて初めて気づいています
5. ★**一度「現状維持」と決めたことも、実画面で不合格なら覆る**（勝負服の色）
