# P0 レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-04（レビュー側 Claude）
**対象**: `REVIEW_REQUEST_P0_20260804.md` / `REPORT_P0_20260804.md` / `QUESTIONS_P0_20260804.md`（commit `4da5890`）
**判定**: **PASS（条件付き→オーナー決定により解消）** — 実装は正典に忠実で品質が高い。Q1 はオーナー決定により確定（§5・D-008）。**残タスクは P0-fix 3件**（§8）

---

## 0. 結論（先に）

| 項目 | 判定 |
|---|---|
| 報告値の再現性 | **CONFIRMED** — V-1 13.93% / V-2 +64.96% / V-3 全3項目、レビュー側環境で完全一致 |
| ユニットテスト42件・typecheck | **CONFIRMED** — レビュー側で独立実行し全PASS |
| 憲法遵守4項目 | **CONFIRMED** — レビュー側の独立 grep で確認（詳細 §2） |
| 「V-2 は構造的に達成不能」の主張 | **CONFIRMED** — 3根拠すべて再実行で実証（§3） |
| 逸脱 5-1（選抜 h²=0.3） | **承認**（§4-1） |
| Q2（3×4 = 0.015625） | **開発側が正しい。指示書が誤り＝レビュー側の非**（§4-2） |
| Q3（突然変異はアレル単位） | **承認**（§4-3） |
| Q4（相加的発現・距離値域） | **承認**（§4-4） |
| Q1（V-2 の扱い） | **オーナー判断事項**。レビュー側の推奨は §5 |
| 申し送り（CLAUDE.md） | **対応済み** — レビュー側が STAR 用に差し替えた（§6） |

**開発側の報告に誇張・隠蔽はなかった。** 数値はすべて再現し、逸脱20件も実際に列挙されていた。V-2 FAIL を定数調整で取り繕わず照会に上げた判断は正しい。

---

## 1. 再現手順（レビュー側が実際に実行したもの）

```bash
npm test                                          # 42件 PASS
npm run typecheck                                 # exit 0・any ゼロ
npm run simulate -- --generations 100 --seed 42   # V-1 13.93% / V-2 +64.96% / V-3 PASS
npm run simulate -- --generations 100 --seed 42 --set-genetics MUTATION_SD=0,BIG_MUTATION_SD=0
npm run simulate -- --generations 100 --seed 42 --set-founders ABILITY_MEAN=700
npm run simulate -- --generations 100 --seed 42 --set-balance REGRESSION_RATE=0.2 --set-genetics MUTATION_SD=90
npx tsx apps/cli/src/sweep.ts                     # 6設定 × 4シードの表を完全再現
# ★レビュー側の追加検証（指示書にも報告にもない）
npm run simulate -- --generations 300 --seed 42
npm run simulate -- --generations 300 --seed 42 --set-balance REGRESSION_RATE=0.2 --set-genetics MUTATION_SD=90
```

exit code 1 は V-2 FAIL による仕様どおりの挙動であることを確認（異常終了ではない）。

---

## 2. 憲法遵守の独立検証

レビュー側が `git grep` で確認（開発側の報告を鵜呑みにしない）:

| 項目 | 結果 |
|---|---|
| `Date.now` / `Math.random` | 実コード0件（ヒットは規約を明記したコメント2件のみ） |
| 他社製品由来の語 | 0件（grep ヒットは変数名 `gp`＝grandparent の誤検出のみ） |
| 実在馬名 | 0件 |
| `any` | 0件 |
| `git.txt` / `files.zip` / 廃棄済み3文書 | いずれも git 未追跡（追跡34ファイル） |

実行時出力の冒頭に `MUTATION_SD=45  REGRESSION_RATE=0（正典どおり=無効）` と h²=0.3 の注意書きが常時表示される設計は良い。**逸脱を実行のたびに自己申告する**形になっており、レビューの取りこぼしを防ぐ。以後の指示書でもこの様式を標準とする。

---

## 3. Q1 の検算 — 3根拠すべて CONFIRMED、かつ新事実あり

### 3-1. 開発側の3根拠（すべて再現）

| 主張 | レビュー側の実測（seed 42） | 判定 |
|---|---|---|
| 突然変異は抑制でなくインフレの燃料 | 変異ゼロで +64.96% → **+43.98%**。ただし **V-1 が 8.92% に崩壊** | CONFIRMED |
| V-1 と V-2 は表裏（片方を通すと他方が壊れる） | 創始平均700で V-2 +32.65% PASS だが **V-1 7.40% FAIL** | CONFIRMED |
| seed 42 は甘いシード・単一シード判定は危険 | sweep で ②出荷既定 4シード平均 +64.71%／最悪 +73.8% を再現 | CONFIRMED |

「正典 §13.3 の抑制三本柱は平均の上昇を止めない」も理論・実測とも支持する。上限1000は創始平均450から見て +122% の位置にあり +50% の番人にならない。headroom は `current` 側の機構で `potential` に作用しない（正典 §7.3 のとおり）。**開発側の分析は正しい。**

### 3-2. ★レビュー側の追加検証 — 「本当に壊れるもの」が判明した

指示書も報告も**100世代（=100ゲーム内年）までしか見ていない**。サービス寿命を考えれば 1ゲーム内年 ≒ 実時間8.7日（1週=4時間・D-007）なので、100年 ≒ 実時間2.4年、**300年 ≒ 実時間7年**。そこでレビュー側が 300世代を回した:

| 設定 | 300世代 V-2 | 300世代 V-1 | 親子相関 | 意味 |
|---|---|---|---|---|
| **出荷既定** | +110.12% | **3.24%（崩壊）** | 0.302 | 平均950/上限1000 = **天井飽和**。全馬がほぼ同じ能力になり、レースが実力勝負でなくなる |
| **提案A** | +32.11%（100世代の+29.54%からほぼ横ばい＝**真の平衡**） | 14.91%（維持） | 0.288 | 平均598/上限1000 = 天井余裕60%。個体差が保たれる |

**これが V-2 が本当は守ろうとしていた失敗モードである。** 「平均が上がること」自体は害ではなく、**上がり続けて天井に張り付き、個体差が消えること**が害。そしてそれを検出するのは V-1（と長期horizon）であって、+50% という比率ではない。

### 3-3. 開発側の評価に1点の訂正

QUESTIONS Q1 の「案Aの親子相関 0.365 は①指示書どおりの 0.348 より高い。つまり案Aは血統の意味を薄めていない」は、**比較対象が不適切**である。①は開発側自身が却下した設定（h²=1・上限飽和で相関が落ちている）。正しい比較対象は②出荷既定の **0.433** であり、案Aの 0.365 は **16%の低下**。「薄めない」は不正確で、正確には「**短期の血統伝達をやや薄めるが、長期の破綻を防ぐ**」。

ただし 300世代では②も 0.302 まで落ちる（天井飽和のため）ので、**長期における案Aの優位は揺るがない**。結論は変わらないが、オーナーが判断する材料としては正確な比較を示すべきだった。

---

## 4. 照会への回答

### 4-1. 逸脱 5-1（選抜 h²=0.3）— **承認**

指示書 §3.4 を文字どおり実装すると「**プレイヤーにも見せない真の potential で種牡馬を選ぶ**」ことになり、正典 §5.5（素質値は本人にも非公開）と矛盾する。指示書の書き方が現実の選抜を表現できていなかった。h²=0.3 は競走能力の実測遺伝率として妥当。`--selection-h2 1` で指示書どおりを再現できる設計も適切。

**条件**: P1 でレースエンジンが入ったら、この近似は**実際のレース結果に基づく選抜**へ置き換えること（開発側の認識どおり）。P1 指示書に明記する。

### 4-2. Q2（3×4クロスの近交係数）— **開発側が正しい。指示書が誤り**

Wright の式で、3×4（共通祖先が父側3代・母側4代）は父・母からの経路長が n1=2, n2=3。

```
F = (1/2)^(n1+n2+1) = (1/2)^6 = 1/64 = 0.015625
```

サラブレッド生産で通用する「3×4 = 1.56%」と一致する。**指示書 §3.5-2 に書いた 0.0703125 (=9/128) は、レビュー側が導出せずに書いた誤った数値**である。開発側が導出コメント付きで正した対応は正しい。0.0703125 を「多重クロス（2×3 + 4×4）の例」として別テストに残した判断も妥当。

> **教訓（Seven Days の同型ミスの再来）**: 指示書に数値を書くときは必ず自分で導出する。「それらしい数値」を検証せずに正典・指示書へ入れない。§7 に記録。

### 4-3. Q3（突然変異はアレル単位）— **承認**

変異は個々の遺伝子に起きるのが自然であり、片アレルだけが変異して次世代に伝わる方が正典 §6.2 の「隠れた名血」の設計とも整合する。正典 §6.4 に「アレルごとに独立適用」を明記する。

### 4-4. Q4 — **承認**

1. **素質値の発現＝相加的（2アレルの平均）**: 承認。正典 §6.2 が約束する「表のステータスが平凡でも片方のアレルに900が眠る」は相加的で成立する（900+300→600 と平均的だが、900は子に伝わる）。優性/劣性の導入は v1 のスコープ外とし、v1.1 の深み追加の候補として保留
2. **距離適性 center 1000〜3600m / range 200〜1200**: 承認。正典 §10.3 の番組表（短距離〜長距離）と矛盾しない

### 4-5. その他の逸脱18件 — **すべて承認**

特に妥当と判断したもの:

- **#20 Wright の経路排他条件**: 正典に書いていなかったが、これがないと親子交配で F が過大になる。**正典の欠落を埋めた正しい実装**
- **#19 V-1 の判定＝能力別CVの平均**: 「毎回違う馬が出る」という目標の意図に照らして、5能力合計のCV（独立成分の合成で必ず小さく出る・約6%）より能力別CVが適切。コードを読んで測定方法も確認（親個体を複製して本体状態を汚さない・シード導出も健全）
- **#12 スキル遺伝子の2%新規獲得**: 正典に継承規則がなく、これがないと世代を経て遺伝子プールが枯渇する。正典 §5.3 に追記する
- **#17「世代」の定義**: 指示書 §3.4 が「年1産」と定めた以上、1世代=1年の解釈は正しい。ただし**用語が紛らわしい**ため、正典・以後の指示書では「**100ゲーム内年（血統深度13.9世代・実時間約2.4年相当）**」と書く

---

## 5. Q1 への推奨 — オーナー判断事項

正典 §13.3 により**目標値の変更はオーナー承認＋正典改訂が必要**。レビュー側の推奨は **Q1-a と Q1-b の併用**:

### 5-1. 機構: 平均回帰（案A）を採用し、正典 §6.4 に追記する

300世代で真の平衡（+32%で横ばい・V-1 14.91%維持）を実証した唯一の案。他の手段（定数調整・創始平均の変更）はすべて V-1 を壊すことが実測で否定された。

**ただしオーナーに認識していただきたいトレードオフ**: 平均回帰は「血統の優位が世代を経ると平均へ引き戻される」機構である。正典 §1.1/§1.4-3 は「世代を重ねるほど自分の牧場の血統が濃くなり強くなる」蓄積感を中核価値と定めており、**強すぎる回帰はこの中核価値を削る**。案Aの回帰0.20は1世代あたり偏差の20%を引き戻す（半減期約3世代＝実時間約1ヶ月）。プレイヤー体験としては「**良い配合を続けている限り上位にいられるが、放置すると平均に戻る**」形になる。新規参入者が追いつけない断絶を防ぐ効果もあり、レビュー側は許容範囲と判断するが、**中核価値に触る変更なのでオーナーの明示的な承認が要る**。

### 5-2. 指標: V-2 を「比率」から「平坦化＋天井余裕」へ再定義する

現行の V-2（+50%以内）には2つの問題がある:

1. **正典 §1.1/§1.4-3 と直接対立する**。「世代を重ねるほど強くなる」ことが中核価値なら、集団平均は上がる。上限を絶対比率で縛ると中核価値を否定することになる
2. **このゲームでは絶対能力値に意味がない**。レースは出走馬同士の相対比較（正典 §8.2）、NPC はプレイヤー上位30%×0.92 に追従（§10.5）、オッズも相対スコアから算出（§9.2）。平均が +30% でも +65% でも体験は変わらない。**壊れるのは天井に張り付いて個体差が消えたときだけ**

改訂案:

| 新基準 | 内容 | 根拠 |
|---|---|---|
| **V-2a 平坦化** | 最終20世代の平均能力の傾きが **0.5%/世代未満** | 失敗モードは「上がり続けること」＝傾き。§13.3 の文言「強くなり続けると崩壊する」に忠実 |
| **V-2b 天井余裕** | 集団平均がアレル上限の **80% を超えない** | 天井飽和＝個体差の消滅の早期警報。出荷既定は300世代で95%に達し V-1 崩壊、案Aは60%で安定 |
| **V-2c 長期健全性** | **300ゲーム内年**（実時間約7年相当）でも V-1 が 12〜18% に留まる | 100年horizonでは出荷既定も V-1 PASS してしまい、破綻を検出できなかった |

（従来の「+50%以内」は廃止。案Aはこの3基準をすべて満たす）

### 5-3. Q1-c（選抜圧を下げる）は不採用

開発側の判断に同意。「強い種牡馬を狙う」楽しさは正典 §1.4 の中核であり、そこを削って数値を通すのは本末転倒。

---

## 6. 申し送りへの対応 — `CLAUDE.md` は差し替え済み

指摘は正しく、深刻だった。旧 `CLAUDE.md` は「他社製品のブラウザ再現・種牡馬に実在の名馬を使う」という**憲法違反の指示**を含み、Claude Code がセッション開始時に自動で最優先読み込みするため、次に誰かがこのディレクトリでセッションを開いた瞬間に有効化される状態だった。git 追跡から外しても効果が残るという指摘も正確。

**レビュー側が STAR 用の内容に差し替えた**（正典へのポインタ・憲法要約・廃棄済み文書リスト・三者体制・よく使うコマンド）。開発側が独断で書き換えず照会に上げた判断は正しい。

---

## 7. 教訓の記録

1. **指示書に書く数値は必ず自分で導出する**（Q2 の 0.0703125）。Seven Days で2回起きた「レビュー側が未検証の前提を正典に入れる」ミスの3回目。以後、指示書のレビュー時に「この数値の出どころは？」を自問する
2. **検証horizonを目的から逆算する**。100世代という数字を指示書に書いたが、それがサービス寿命の何年に当たるかを検算していなかった。300世代を回して初めて本当の失敗モードが見えた
3. **比較対象の選び方で結論の見え方が変わる**（§3-3）。優劣を主張するときは「自分が却下した設定」ではなく「現行の既定」と比べる
4. 実行時に逸脱を自己申告する出力設計は有効。以後の指示書で標準様式とする

---

## 8. ★レビュー側コード監査の結果（並列サブエージェント・file:line 検証）

### 8-1. 平均押し上げバグ — **なし（断定）**

最重要の懸念だった「potential 補正の累積」は否定された。`phenotype.ts:66-79` の boost・血の濃縮・ニックスは **potential にのみ適用され genotype に書き戻されない**（`breeding.ts:125,169`）。したがって世代を跨いで複利で積み上がらない。選抜も二重にかかっていない（牝馬は全頭配合・プール補充は無圧・選抜は種牡馬上位20%の1段のみ）。clamp は上下対称で、上限1000 の切り捨てはむしろ平均を**抑制**する側に働く。

正典との一致も file:line で確認: genotype スキーマ（`types.ts:51-65`）・配合フロー1〜11（`breeding.ts:99-157`）・アレル継承（`genetics.ts:34-36`）・隔世遺伝と大物覚醒（`genetics.ts:82-167`）・Wright式と排他条件（`inbreeding.ts:153-165`）・インブリード補正の順序と係数（`phenotype.ts:54-109`）・§13.1 定数（`balance.ts:32-60`・数値ズレなし）。

`REGRESSION_RATE` は既定0で `!== 0` ガードの内側にあり、**分岐内で乱数を消費しない**ため、既定実行は当該機構が存在しないビルドとビット単位で同一。既定結果への影響ゼロと断定できる。

### 8-2. アブレーション分析（レビュー側が独立実行・原因の定量分解）

| 条件（seed 42・100世代） | V-2 |
|---|---|
| 既定 | **+64.96%** |
| **種牡馬選抜を無効化** (`--stallion-top 1.0`) | **+10.87%** |
| 大物覚醒オフ (`BIG_ATAVISM_RATE=0`) | +50.52% |
| 突然変異オフ | +43.99% |
| インブリード補正オフ | +43.17% |
| 完全情報選抜 (`--selection-h2 1`) | +96.98% |

**選抜を外すと +10.9% まで落ちる。** 正典の各機構が単独で生む上昇は合計でも約+11%（予算内）であり、残り約+54pt は**方向性選抜への応答そのもの**。正典に選抜応答を打ち消す機構が存在しない以上、「V-2 FAIL は構造的問題」という開発側の主張は**定量的にも支持される**。

### 8-3. ★開発側が自己申告していなかった発見3件

| # | 発見 | 影響 | 対応 |
|---|---|---|---|
| **A-1** | **大物覚醒が単調上昇ラチェット**（`genetics.ts:159-163`）。祖先の最大値は常に子のアレル対以上なので、低い方を置換する実装は**必ず上げ方向にしか働かない**。これ単独で血統インフレの**約14pt**を生んでいた | 開発側は「低い方を置換」という選択自体は 5-3-6 で自己申告したが、**それが一方向ラチェットであることと寄与量は報告していない** | 正典 §6.3 に警告として明記済み。回帰0.20が相殺する前提で機構は維持。**回帰を弱める場合は本機構も見直す** |
| **A-2** | **距離適性が世代とともに拡散**（`genetics.ts:43-46`+`balance.ts:99-106`）。変異SDを値域比で機械的に縮尺した結果、`distance_center` は SD=117・clamp±390 になり、実測20世代で SD 369→591。**§5.2 が意図するマイラー／ステイヤーの分化が消える** | ゲーム性に直結する（距離適性が全馬フラットに近づくと出走選択の意味が薄れる） | 正典 §5.2 に警告を明記。**P0-fix 対象**（距離系の変異幅は値域比でなく絶対値で小さく固定） |
| **A-3** | **V-2 指標自体に約14%の構造的上げ底**。創始馬は F=0・ニックス1.0 で生成される（`founders.ts:101`）ため、無補正の創始世代と補正済みの産駒を比べる比率は、遺伝的向上でない分を含む | 旧 V-2 の「+50%」という数値の精度そのものを毀損していた | V-2 再定義（§5-2）の根拠に追加。正典 §13.3 に記載済み |

補足: `DOMINANT_WEIGHT` は 0.5 なら完全中立だが、**0.5 超に設定すると系統的な上げ要因**になる（`balance.ts:211`）。将来のチューニング時の注意点として正典 §13.1 にコメントを残した。

---

## 9. オーナー決定（2026-08-04）— D-008

| 論点 | 決定 |
|---|---|
| 平均回帰 | **案A（REGRESSION_RATE=0.20・MUTATION_SD 45→90）を採用**。正典 §6.4 に追記済み |
| V-2 | **再定義を承認**。V-2a 平坦化（最終20世代の傾き<0.5%/世代）/ V-2b 天井余裕（平均≤上限の80%）/ V-2c 長期健全性（300ゲーム内年でも V-1 が12〜18%）。旧「+50%以内」は廃止 |
| トレードオフの承認 | 血統の優位は「一度築けば永続」ではなく「**選び続ける腕前が上位を保つ**」形になる（偏差の20%/世代を引き戻す・半減期約3世代≒実時間1ヶ月）ことをオーナーが承認 |

正典 §5.2 / §5.3 / §6.3 / §6.4 / §6.5 / §13.1 / §13.2 / §13.3 / §0.4 を改訂済み。

---

## 10. 開発側へのボール — P0-fix（指示書 `DEV_INSTRUCTIONS_P0FIX_20260804.md`）

1. **F-1** 平均回帰を既定で有効化（`REGRESSION_RATE=0.20` / `MUTATION_SD=90`）
2. **F-2** V-2 を新3基準（V-2a/V-2b/V-2c）に置き換え、CLI の判定と出力を改訂
3. **F-3** 距離系形質の変異幅を値域比でなく絶対値で固定し、世代を経ても距離適性の分化が保たれることを検証（A-2）

完了後、レビュー側が再度すべて独立再実行して照合する。
