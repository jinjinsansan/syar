# P0-fix5 レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-05（レビュー側 Claude）
**対象**: `REVIEW_REQUEST_P0FIX5_20260805.md` / `REPORT_P0FIX5_20260805.md`（commit `f01a3bf`）
**判定**: **PASS — P0（遺伝エンジン）完了。P1 へ進む**

---

## 0. 結論

| 項目 | 判定 |
|---|---|
| **既定経路が P0-fix4 から変わっていないこと**（合格条件の核心） | **CONFIRMED** — `npm run verify` の出力が**レビュー側の P0-fix4 実行結果と一行一句まで同一** |
| ユニットテスト98件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| J-1 切断係数が clamp比に追随 | **CONFIRMED** — `--set-genetics MUTATION_SD=45` で丈夫さ sd 66 → **60**・clamp 200（レビュー側の独立実測） |
| J-2 `basis` 3分岐の固定 | **CONFIRMED** |
| J-3 r=0 の例外 | **CONFIRMED** — 停止し、**理由と代替手段を示すメッセージ**を出す |
| 自己申告（`basis` の到達不能分岐） | **承認・高く評価**（§2） |
| 未解決の質問 | なし |

---

## 1. レビュー側が実際に実行したもの

```bash
npm test              # 98件 PASS
npm run typecheck     # exit 0
npm run verify        # P0-fix4 の実行結果と完全一致（所要272.3秒はマシン負荷差）
npm run simulate -- --generations 5 --seed 42 --quiet --json out/j1.json --set-genetics MUTATION_SD=45
    # → durability sd=60 clamp=200（既定は 66/109）
npm run simulate -- --generations 5 --seed 42 --set-balance REGRESSION_RATE=0
    # → 例外で停止
```

J-3 のエラーメッセージが良い:

> `buildTraitMutation: 回帰率は正の数である必要があります (0)。r=0 では平衡式が定義されず、非能力形質の変異が無言で停止します。回帰を使わない比較をしたい場合は sd 上書きなしの traitMutation を明示的に構築してください。`

**何が起きたか・なぜ駄目か・どうすればよいか**の3点が揃っている。以後のエラーメッセージの標準とする。

---

## 2. J-2 の自己申告 — 「経路を固定せよ」が2度目の成果を出した

`basis` の切替条件が `Math.abs(founderMean) > 1e-9` だったため、**`sd` 分岐に実質到達できなかった**（平均1.01・SD18.6 のようなほぼゼロ中心の分布でも `ratio` 判定になり、ほぼ0で割った不安定な乖離が出る）。`|平均| > 創始SD` へ是正。

**これは I-3 で指摘した「単体を固定するのと経路を固定するのは別物」が、今度は開発側の実装の欠陥を炙り出した例である。** 分岐関数を単体でテストしていれば3分岐とも「動く」ので気づけなかった。**テストを本番経路で書くこと自体が設計のレビューになっている。**

既定6形質はいずれも平均がSDの3.8〜9.1倍あり `ratio` のままなので、既定の挙動は不変。レビュー側でも確認した。

### 2-1. 軽微な観察（対応不要・記録のみ）

`|平均| > 創始SD` という切替条件は、境界付近で判定の厳しさが不連続になる。平均 = SD ちょうどの形質では、`ratio` 基準の ±10% が SD 単位では ±0.1SD という非常に厳しい帯になる一方、`sd` 基準はそれより緩い帯になるはずである（`sd` 基準の閾値は正典に明記されていない）。

現行6形質は最小でも平均/SD = 3.8 なので境界から遠く、**実害はない**。将来ゼロ中心に近い形質を追加するときは、①`sd` 基準の閾値を正典に明記し ②境界での連続性を確認すること。**P1 の宿題ではない**が、形質追加時のチェック項目として記録する。

---

## 3. J-1 の選択（(a) 解析式）— 妥当

(b) の fail-closed は「静かに間違わない」を満たすが、`sweep.ts` のように **clamp比を意図的に振る用途が実在する**以上、拒否すると使えなくなる — この判断は正しい。「比を変えたら再計算せよ」という自分のコメントを**コード自身が満たせる形にする**という筋の通し方も良い。

既定 k=150/90 で係数 0.915604 となり、導出される sd/clamp が正典 §6.4 の表と完全一致するため、**出荷既定の挙動は変わらない**。レビュー側の verify 出力が P0-fix4 と1文字も違わないことがその証拠である。

`config.test.ts` の是正（`clamp/sd ≈ ratio` しか見ないテスト → sd の実値まで固定）も適切。**あのテストは欠陥を固定していた**という認識を持てている点が重要。

---

## 4. P0 完了の宣言

**P0（遺伝エンジン + 100世代シミュレータ）を完了とする。**

### 到達した状態

- **遺伝機構**: 2アレル継承・隔世遺伝6%・大物覚醒1.5%（能力5種のみ）・突然変異＋平均回帰・Wright近交係数（経路排他条件つき）・インブリード補正・近交弱勢・ニックス・生産上限
- **定数管理**: すべて `FOUNDERS` と平衡式から導出。**CLI 上書き時も再導出され、clamp比を変えれば切断係数も追随する**。導出をバイパスする経路は残っていない
- **検証**: V-1 / V-2a〜e / V-3 の7系統を 4シード × 100/300ゲーム内年・実運用規模800頭でゲート。テスト98件
- **決定論**: シード固定で完全再現。乱数・時刻はすべて注入

### 6便の記録 — 誰が何を見つけたか

| 便 | 問題 | 発見者 |
|---|---|---|
| P0 | V-2 が構造的に達成不能 | 開発側 |
| P0 | 300世代で天井飽和し V-1 が 3.24% に崩壊 | レビュー側 |
| P0 | 大物覚醒が単調上昇ラチェット（インフレの約14pt） | レビュー側監査 |
| P0 | 指示書の 3×4クロス期待値が誤り | 開発側 |
| P0-fix | 丈夫さが 650→450 へ恒久移動 | レビュー側監査 |
| P0-fix2 | 距離系の回帰率 0.01 は誤結論だった | **開発側（自己訂正）** |
| P0-fix2 | clamp 位置の実装ずれ | **開発側（自己発見）** |
| P0-fix2 | 芝適性のSDが 0.71倍に収縮（どのゲートにも掛からず） | レビュー側監査 |
| P0-fix2 | clamp の一次回答が誤り（平衡式の前提を壊す） | **レビュー側（自己訂正）** |
| P0-fix3 | clamp のテストが純関数内部しか守っていない | レビュー側監査 |
| P0-fix3 | CLI 上書きが sd 導出をバイパス | レビュー側監査 |
| P0-fix4 | 正典の丈夫さ sd が丸めミス | 開発側 |
| P0-fix4 | 切断係数が clamp比に追随しない | レビュー側監査 |
| P0-fix5 | `basis` の `sd` 分岐が到達不能 | **開発側（自己発見）** |

**開発側が3度、レビュー側が2度、自分の結論を訂正した。** 三者のいずれか一方だけでは、この半分も見つからなかった。

### 確立した恒久ルール（P1 以降に適用）

| # | ルール |
|---|---|
| R-1 | 単体を固定するだけでなく**経路**を固定する |
| R-2 | 境界テストは**両側**を押さえる |
| R-3 | 「判定不能」は PASS でなく **FAIL** に倒す |
| R-4 | **導出をバイパスする経路を数える** |
| R-5 | 平均のゲートだけでは**分散の破壊**を捕まえられない |
| R-6 | 一方向にしか働かない機構は、**適用先ごとに意味が通るか**確認する |
| R-7 | **較正した条件を明記する** |
| R-8 | 実行時に**逸脱を自己申告する出力** |

---

## 5. 次便 — P1（レース判定エンジン + モンテカルロ検証）

指示書 `DEV_INSTRUCTIONS_P1_20260805.md` を発行済み。要点:

- **スコープ**: レース判定（§8.2〜8.5, 8.7）/ Provably Fair（§8.6, 8.8）/ 介入モデル（§8b・スコア側のみ・UI なし）/ **選抜を実レース結果ベースへ置換** / モンテカルロ検証ハーネス
- **合格基準**: V-4（1番人気勝率30〜34%）・V-5（複勝率60〜65%）・V-6（最低人気0.5〜2%）・V-8（AI代行が手動最適の93〜97%）・V-9（interventionMult 分布）・**V-12（平均F と表現型乖離の監視・新設）** ＋ **P0 ゲートが実レース選抜下でも成立すること**
- **正典を2点是正**: V-7（故障率）は調教で発生するため **P1 → P3 へ移動**。V-12 を新設
- **★最大の注意点**: 育成ループが P3 のため、P1 では**素質開放率をプレースホルダで置く**。`RACE_RANDOM_K = 0.12` の較正はこの仮定に依存するので、**P3 で再較正が要る**ことを明記させる（R-7 の適用）

---

## 6. 教訓の記録

1. **良いエラーメッセージは「何が起きたか・なぜ駄目か・どうすればよいか」の3点を持つ**（J-3）。以後の標準とする
2. **テストを本番経路で書くこと自体が設計のレビューになる**。J-2 は「テストを書いたら実装の欠陥が出た」2度目の例（1度目は I-3 の指摘）
3. **「既定の挙動は変わらないはず」という予測を、出力の完全一致で検証する**やり方は有効。J-1〜J-3 のように「既定以外の経路」を直す変更では、これが最も強い回帰の証拠になる

---

## 7. 【追補】コード監査の結果 — J-1 が新しい穴を1つ開けていた

監査は J-1/J-2/J-3 をすべて追認した（解析式の独立検算で誤差 4e-8〜6e-7・変異テストで「係数をリテラルに戻すと落ちる」ことを確認・`git worktree` で aef42b0 と実行比較して**既定経路の差分ゼロ**を確認）。切替条件 `|平均| > 創始SD` についても「**境界で不連続にならない**」と評価された（|mean|=sd のとき ratio 乖離と sd 乖離の絶対値が一致するため。旧 `>1e-9` こそが「ほぼ0で割る」不連続だった）。§2-1 に書いた私の懸念は**杞憂**である。

### 7-1. 🔴 MEDIUM: `--set-genetics MUTATION_SD=0` で導出 sd が NaN になる（P0-fix5 の退行）

レビュー側でも再現した:

```
MUTATION_SD=0 → durability {"sd":null,"clamp":null}  ※JSON の null は NaN
                temper / surface / distance すべて同じ
```

機序: `clampRatio = MUTATION_CLAMP / MUTATION_SD = 150/0 = Infinity` → ガード `!(clampRatio > 0)` は **Infinity を通す** → `clampTruncationFactor(∞)` が `2·∞·φ(∞) = ∞·0 = NaN`。

**P0-fix4 では同じ上書きが `{sd: 66, clamp: Infinity}`（有限の sd）だったので、これは J-1 が新たに開けた fail-open である。** NaN の sd は `mutateAllele` 経由で **genotype に静かに流れ込む**。

**そして最も重要な点**: `regression.test.ts` には「`MUTATION_SD=0` でも NaN を返さない（ゼロ除算の防御）」という**同名のテストが既に存在するのに、balance を直接組んでいて `resolveRuntimeConfig` を通らないため捕まらない**。

> **I-3・I-4 とまったく同じ「純関数は通るが呼び出し側が壊れている」構造が、3度目に出た。** R-1（単体でなく経路を固定する）を恒久ルールに入れた直後の便で、同じ形の穴が生まれている。ルールを書くだけでは足りず、**既存テストが本番経路を通っているかの棚卸し**が要る。

対処: `Number.isFinite(k)` でなければ **1 を返す**（clamp/sd → ∞ は「切断なし」なので解析的に正しい）。この場合 `{sd: 60, clamp: Infinity}` となり、clamp が実質無効という意味も通る。

### 7-2. 🟡 LOW: `CLAMP_TRUNCATION_FACTOR = 0.9156` が本番から参照されなくなり二重管理が残った

現在の参照元は `regression.test.ts` のみで、**テストが古いリテラルから期待 sd を計算している**。将来 `MUTATION_CLAMP`/`MUTATION_SD` を動かすとコードと期待値が食い違う。`balance.ts` の「比を変えたら再計算すること」というコメントも、コードが自動計算する今となっては**誤誘導**である。

**G-0 / I-2a で二度潰したのと同じクラス**なので、定数を削除するか「`clampTruncationFactor(MUTATION_CLAMP_RATIO)` と一致すること」だけを固定する形にする。

### 7-3. 🟡 LOW: `v2d.worstDeviation` が NaN に塗り潰される

`reduce` の初期値が先頭要素で、以降は `Math.abs(t.deviation) > NaN` が常に false。`V2D_TRAITS` の先頭 `surface.turf` は J-2 の `none` フィクスチャそのものなので、実測で `worstKey=surface.turf / worstDeviation=NaN` となり**同時に存在する実数の乖離が隠れる**。

合否は `every()` で別計算なのでゲートは正しいが、**最も診断が必要な場面で verify 表に NaN が出て真の最悪形質が見えない**。`--json` でも `null` になる（型宣言は `number`）。NaN を最後尾に送るだけで解消する。

### 7-4. 憲法の周辺（P1 前に処理）

`AI_PROMPT.md` に実在馬名10頭が残っている。**廃棄済み文書**であり本便の差分外だが、**P1 で命名・アセット作業に入る前に**スクラブするか「参考であり実装に持ち込まない」旨の注記を入れる。

### 7-5. 判定への影響と処理方針

3件とも**出荷既定の挙動には影響しない**（既定は `MUTATION_SD=90`・`worstDeviation` の NaN は `none` フィクスチャでのみ発生）。よって **P0-fix5 の PASS は維持し、P0 完了とする**。

3件はいずれも小さいので、**6便目を立てず P1 指示書の冒頭に「先に片付ける積み残し」として同梱**する。

### 7-6. 監査作業中の事故（申告あり・復旧確認済み）

監査側が比較用 `git worktree` を撤去する際、npm workspace のシンボリックリンクを辿って本体ソースが削除された。`git checkout -- apps packages` で復元済みとの申告があり、**レビュー側で独立に確認した**: `git status` は私の正典編集（3行）と未追跡2ファイルのみ、テスト98件 PASS、typecheck exit 0。**リポジトリは健全である。**

> 教訓: npm workspaces のシンボリックリンクがある環境で `git worktree remove` や再帰削除を行うと、リンクを辿って本体を消しうる。以後の監査では worktree を**リポジトリ外**に作る。
