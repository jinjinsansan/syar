# HANDOVER_P4_RENDER_SCALE_20260912 — ★次のセッションへ

**枝** `p4/race-30sec-cuts` ／ **終了時 HEAD** `f9b8e14` ／ **開始時 HEAD** `7f07f1f`
**未コミットの製品コード** なし（★未追跡は `side-v9b` の画像 8 枚のみ＝★研究用・本線に入れない）

---

## 0. ★次にやること（★これだけ読めば始められます）

> ★**レース画面の滲みを直す。** ★原因は「1280 の絵を 1728 物理画素に引き伸ばして表示している」こと。
> ★撮り方では直りません。★**描画をデバイス解像度に合わせる**作業です。

★**着手前に、測り方を先に作ること**（★2026-09-11〜12 に、測らずに直して 3 回壊しました）。

---

## 1. ★確かめた事実（★推測ではありません）

| # | 事実 | どう確かめたか |
|---|---|---|
| F-1 | ★アプリは **1280×720** を描き、★画面は **1152 CSS px × dpr 1.5 = 1728 物理 px** で出す（★**1.35 倍に引き伸ばし**） | ★実測（`getBoundingClientRect` と `devicePixelRatio`） |
| F-2 | ★レース描画に `devicePixelRatio` を見る箇所が **1 つも無い** | ★`grep`（★`rig-lab` の three.js だけが見ている） |
| F-3 | ★地面は **`world-textured.ts:296` の走査線ループ**で、★**論理座標の 720 行**を貼って描く | ★コード |
| F-4 | ★横視点の地面（`parallax-plate.ts`）は ★**1 枚貼り**で走査線ループを持たない | ★コード |
| F-5 | ★画布を 2 倍にすると ★**文字は 2.72 倍細かくなるが、芝とスタンドは破綻**する | ★実測＋オーナー評「壊したのですか？」 |
| F-6 | ★馬は `/art/baked/`（★**64MB・65 ファイル**）の ★**560px で焼いたコマ**から描く。★2 倍にしても ★**1.12 倍しか細かくならない** | ★実測・`tools/bake-race-frames.mjs` の註記 |

---

## 2. ★手順（★①②が本体・★③は別判断）

### ① `world-textured.ts` の走査線を**物理画素の行数**で回す
- ★対象は ★**2 つのループだけ**（★地面 `:296` ／ ★地平線のかすみ）
- ★投影の計算は**論理座標のまま**。★貼る先だけ物理画素にする
- ★`mow-stripes.ts`（多角形塗り）と `parallax-plate.ts`（1 枚貼り）は ★**自動で追随する**（F-4）

### ② 画布を `1280 × dpr` で持つ
- ★`dpr` は ★**上限 2**（★4K 機で重くならないように）
- ★描くコードは **1280×720 の座標のまま**（`ctx` を dpr 倍する）
- ⚠️ ★**JSX の `width` 属性では変えられません**。★SSR が焼いた値にハイドレーションで戻されます
  （★2026-09-12 に踏みました）。★**描画のたびに設定すること**

### ③ 馬の焼きコマ 560px → 1120px（★容量 64MB → **約 256MB**）
- ★①②の後、★**馬だけ気になるか**を見てから判断
- ⚠️ ★台帳に前例あり（★「side-v9 は初期化 +31% で本線に入れない」）。★容量は一度却下された論点

---

## 3. ⚠️ ★着手前に用意する「壊していないことの測り方」

★**これを先に作ること。** ★2026-09-11〜12 は、これが無いまま直して 3 回壊しました。

1. ★**細かさ**を 3 か所（★芝・馬・文字）で、★**同じ表示サイズに揃えて**測る。
   ★①②の前後で比較し、★**芝が下がったら不合格**（★前回はここが下がったのに気づかず出しました）
2. ★**同じ絵であること** — ★高解像度を 1280 に縮めて元と比べ、★差が ★**平均 2 階調未満**
   （★＝縁の滑らかさだけ。★版面・カット・馬の位置が動いていないこと）
3. ★`npm test`（★1563 件）と `npm run typecheck`
4. ★**速度** — ★画素数が 2.25 倍になる。★コマ落ちしないかを実測
5. ★上の 4 つが通ってから ★**はじめてオーナーにお見せする**

---

## 4. ⚠️ ★今夜踏んだ罠（★同じ所を踏まないこと）

| # | 罠 | 対処 |
|---|---|---|
| T-1 | ★**取り下げ済みの実験が、映像だけ古いまま残っていた** | ★比べ物は `tools/capture-tilt-compare.mjs` で作る（★出す前に ①カット ②素材 ③秒 ④効いているか を自分で検査して止まる） |
| T-2 | ★2560px 幅を頁に貼り、★**43% に縮んで潰れた** | ★頁幅（1100px）と同じ大きさで作る。★拡大は**最近傍**。★PNG で渡す |
| T-3 | ★**1 倍の画布 1280 を 1920 で撮っていた**（★自分で引き伸ばしていた） | ★`capture-race-through.mjs` は ★**画布と 1:1** が既定。★超えると止まる |
| T-4 | ★長い撮影は ★**必ず詰まる**（★実測 1133 / 707 / 15 コマ目・★位置は毎回違う） | ★`--start-sec` / `--end-sec` / `--keep` で ★**10 秒ずつ起こし直す** |
| T-5 | ★2 倍の絵を `toDataURL` で運ぶと ★**CDP がタイムアウト** | ★絵は `Page.captureScreenshot`、★JS からは秒と診断だけ |
| T-6 | ★`clip.scale` に ★**dpr が掛かる**（★前半 1920px・後半 2880px のコマが混ざった） | ★dpr で割る ＋ ★**1 コマ目の大きさを必ず検査** |
| T-7 | ⚠️ ★**道具がオーナーの画面にエッジの窓を開き続けていた**（★ずっと前から。★オーナーは毎回縮小して作業していた） | ★`tools/lib/cdp.mjs` の ★**既定をヘッドレス**に（`d8e5fff`）。★窓が要るときだけ `--show-browser` |

★**T-7 は「撮れた絵」しか見ていなかったために、★申告されるまで気づけませんでした。**
★道具が ★**相手の環境に何をしているか**も、成果物の一部として見ること。

---

## 5. ★今夜入れたもの（★コミット済み）

| コミット | 中身 |
|---|---|
| `064273a` | ★4 角で ★**馬の絵を走路の接線へ回す**のを既定に（★オーナー判定「回したがまだマシ」）。★交差角 52° → 80.5°。★戻し口 `?tilt=off`。★検定 4 件 |
| `1e879f0` | ★**通し撮りの道具** `tools/capture-race-through.mjs` ＋ 報告書 `REPORT_P4_CORNER_TILT_20260912.md` |
| `d144ee0` | ★暗い場面の h264 潰れを直す（★crf 15・tune animation・1 秒ごと鍵コマ） |
| `f791f8a` | ⚠️ ★**2 倍で描く（後で全部取り下げ）** |
| `d8e5fff` | ★**ブラウザをヘッドレスに** ／ ★`?render=2x` を ★**製品コードから削除**（★`git diff d144ee0` は空） |
| `f9b8e14` | ★通しをヘッドレスで撮り直し（★1280×720・★16 カット・★86 秒・★17MB） |

★**芝の目の調査**（★オーナーの見立ては当たり・★案 A は効かず）は `REPORT_P4_CORNER_TILT_20260912.md` §3。

---

## 6. ★オーナー判断待ち

| # | 論点 |
|---|---|
| A | ★①②（★レース描画の中心部に触る）を進めてよいか |
| C | ★③ 馬の焼き直し（★64MB → 約 256MB）。★①②の後に判断でよいか |
| D | ★**通しの中身**（★86 秒・16 カット）の不合格箇所。★滲みと別に、★秒で指摘いただく |

---

## 7. ★動かし方

```bash
npm run dev:web                      # ★http://localhost:3210
# ★見比べ台        http://localhost:3210/gait-review
# ★そのまま見る    http://localhost:3210/race?dev=1
# ★別端末から      http://192.168.128.44:3210/...   ★Wi-Fi の IP（★起動ログの Network: は Tailscale なので使わない）

node tools/capture-race-through.mjs --start-sec 0 --end-sec 10 --keep   # ★通しを 10 秒ずつ
npx tsx tools/measure-turf-grain.ts                                     # ★芝の目と馬の向きの角度
npm test && npm run typecheck
```
