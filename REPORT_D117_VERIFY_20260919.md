# REPORT — D-117 の後の V（前 → 後 と、その差）

- 日付: 2026-09-19
- 発行: 開発側
- 裁定: `REVIEW_VERIFY_COST_VERDICT_20260919.md` **CC-8'** / `REVIEW_MUSTINCLUDE_VERDICT_20260919.md` **MI-2**
- 基準値: `REPORT_D117_BASELINE_20260919.md`（HEAD `4b70652`・D-117 に手を付ける**前**）

---

## 1. ★予測（★**流す前**に書きます・MI-2）

> **MI-2** ✅ 取り直しは 1 回でよい。★そして「一致するはず」という予測を**先に書いてから**流してください
> （★**外れたら、それが発見**です）

### ★★予測: **完全一致します。** 小数点以下も含めて 1 ビットも動きません。

| # | 基準値 | ★予測 |
|---|---|---|
| **V-4**（1番人気 勝率） | **31.09%** | **31.09%**（★完全一致） |
| **V-5**（1番人気 複勝率） | **62.15%** | **62.15%**（★完全一致） |
| **V-6**（下位3ランク 平均勝率） | **1.20%** | **1.20%**（★完全一致） |

シード別も同じです（42: 31.40/62.63/1.19・7: 30.34/61.25/1.26・2026: 31.77/62.94/1.15）。

### ★なぜ「帯の中」ではなく「完全一致」と言えるのか

**D-117 で触ったのは 3 か所だけで、そのどれもこの経路の乱数の並びを動かしません。**

**① 馬場状態の抽選を `drawRacePrefix()` に切り出した**
- ★引く順（頭数 → 距離 → 馬場 → 馬場状態）も、★引く回数（各 1 回）も**同じ**です。
- ★`Rng.int`/`pick`/`float` はどれも `float()` を**ちょうど 1 回**消費します
  （`int` は `min + floor(float() * span)`。★**棄却サンプリングではない** — ★読んで確かめました）。
- → ★**依存するのは値であって消費数ではありません。**

**② `programme.trackCondition` の上書き口を足した**
- ★`verify-race.ts` は `programme` に **`surface` / `distance` / `courseShape` しか渡しません**
  （`apps/cli/src/verify-race.ts:302-309` で確認）。
- → ★`trackCondition` は `undefined` なので、★**抽選の結果がそのまま使われます**（従来どおり）。

**③ `mustInclude`（DS-2）を足した**
- ★`verify-race.ts` は**渡しません**。
- → ★`forced = []` なので、`candidateCount` も `chosen` も `spare` も床の走査も**従来と同じ式**になります。
- ✔ ★これは**機械が既に言っています**: `must-include-bit-identical.test.ts` が
  ★`generateRace` の返り値を**丸ごと JSON で 300 レース**比較し、★一致を確かめています
  （★対照 2 つつき: 中身がある／1 頭渡すと変わる）。

### 🔴 外れたら何を意味するか

- ★**上の 3 か所以外に、私が気づいていない変更がある**ということです。
- ★「帯の中だからよい」とは**しません**。★SE は V-4 で **0.189pp** なので、
  ★**0.5pp の動きは 2.6 SE** で見えます。★**動いたなら、動いた理由を特定するまで進みません。**

---

## 2. 実測

```
npx tsx apps/cli/src/verify-race.ts
```

- HEAD: `76f87b8`（★予測をコミットした直後・D-117 第 3〜5 段が入った状態）
- 既定のまま: `VERIFY_RACES = 60,000` / `VERIFY_SEEDS = [42, 7, 2026, 31337]` / 母集団 400 頭
- **所要 721.8 秒（12.0 分）**・総合 **PASS**

### ★★予測は当たりました — **完全一致**です

| # | 合格域 | 前（`4b70652`） | **後（`76f87b8`）** | **差** | SE |
|---|---|---|---|---|---|
| **V-4** | 30〜34% | 31.09% | **31.09%** | ★**0.00pp** | 0.189pp |
| **V-5** | 60〜65% | 62.15% | **62.15%** | ★**0.00pp** | 0.198pp |
| **V-6** | 0.5〜2% | 1.20% | **1.20%** | ★**0.00pp** | 0.0253pp |

★**SE も余裕も 1 桁目まで同じ**です（V-4: 1.09pp / 5.8 SE ・ 2.91pp / 15.4 SE。
V-5: 2.15pp / 10.9 SE ・ 2.85pp / 14.4 SE。V-6: 0.7pp / 27.5 SE ・ 0.8pp / 31.8 SE）。
★シード間のばらつき（SD 0.54%）も同じです。

★**0.00pp は「帯の中に収まった」ではありません。「1 ビットも動いていない」**です。
★V-4 の SE は 0.189pp なので、★もし 0.5pp 動いていれば **2.6 SE** で見えました。

### ★これで言えること

★いま**プレイヤーは 0 人**なので、★すべてのレースが「プレイヤー馬のいないレース」です。
★案 **(b)**（登録馬を先に入れ、残りは従来どおりの帯から引く）を採ったので、
★**その種のレースは 1 ビットも変わらない** — ★それが実測で確かめられました。

→ ★★**D-117 の作り直しは、既存のレースの作り方を何も変えていません。**
  ★プレイヤー馬が入った日に V が動いたら、★**その原因はプレイヤー馬 1 つだけ**です（★切り分けられます）。

⚠️ ★これは (a)(c) を採っていたら**言えませんでした**（★動いた理由が 2 つになる）。

---

## 3. ⚠️ 一致していないもの（★所要時間）

| | 前 | 後 | 差 |
|---|---|---|---|
| 所要 | **641 秒**（10 分 41 秒） | **722 秒**（12 分 2 秒） | ★**+81 秒（+12.6%）** |

🔴 ★**これはコードの遅れではありません**（★結果が 1 ビットも同じなので、計算量は同じです）。
★同じ機械で ★**ユニットテスト（2,347 件）と staging への問い合わせを並行して流していました**。
→ ★**測り直していないので「12.6% 遅い」を性能の数字として使わないでください**（★交絡あり）。

---

## 4. ⚠️ この報告が説明していないこと

- **母集団 400 頭の合成集団**です（PO-5）。配備は 4,389 頭中 3,000 頭で、**別の集団**です。
- ★`--pool` で本番の集団を食わせた取り直しは**していません**。
  ★D-117 は生成の経路を変えましたが、★**オッズが自馬を含むのは配備の経路だけ**なので、
  ★プレイヤーが 1 人でも馬を持った日に、★そちらで取り直す価値があります。
- **MI-4**（レビュー側の指摘）: ★オッズは締切の**後**に計算されるので、
  ★強い自馬は**人気になり配当は下がります**（D-117 ③ の効果）。
  ★§9.5 の 5,000 EP の上限と合わせて、★**自馬で荒稼ぎする経路は塞がれたまま**です。
  ⚠️ ★ただし ★**この実行はそれを測っていません**（★プレイヤー馬 0 頭の経路です）。
  ★塞がっていることを数で言うには、★自馬を入れた `--pool` の実行が要ります。

---

## 5. CC-10（登録簿）

★**登録しません。** ★CC-10 は「取り直しが済むまで」の短い期限で載せる約束でした。
→ ★**取り直しは済み、完全一致でした**ので、★載せるものがありません
  （★緑に戻ったのに登録簿に残っているのも落ちる形にしてあります・RD-2）。
