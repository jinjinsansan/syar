# 報告（P4）— Q-P4-38 を実装しました。**回避策が3つとも消えました**

## 本便で正典の改訂が要る決定をしたか

★**はい。D-063（jostle）を撤去しました。** そして 🔴 **V-16 ① について照会が1件**あります（§4）。

---

## 1. ★結果（実測）

### 通過順位 — **実際の競馬の形になりました**

```
1-1-1-1-3   2-2-2-4-8   3-3-3-3-6   4-4-4-5-5
1-1-2-7-11  2-2-1-2-3   3-3-3-5-9   4-4-5-6-8
```

★**前3つが揃っています**（中継の `8-8-8-4`）。1頭あたりの順位変動:

| 区間 | 生成器（いま） | jostle（前） | jostle なし |
|---|---|---|---|
| 1角→2角 | ★**0.00 着** | 3.35 | 0.03 |
| 2角→3角 | ★**0.76 着** | 3.55 | 0.43 |
| 3角→4角 | 1.92 着 | 2.22 | — |
| 4角→ゴール | 2.59 着 | 2.04 | — |

★**道中は動かず、勝負所から動く。** 乱数で揺らしていたときは道中が 3.35着 ふらついていました。

### V-16

```
残りm  局面      出走表   画面    差
 1500  start     0.732  0.676  -0.056
 1200  cruise    0.732  0.676  -0.056
  900  cruise    0.732  0.799  +0.067
  800  spurt     0.732  0.878  +0.146
  600  spurt     0.732  0.931  +0.199
  400  straight  0.732  0.973  +0.241
  200  straight  0.732  0.986  +0.254

② 勝負所以降で有意差            ★PASS（+0.146 → +0.254）
③ スタート直後の差 −0.056       ★PASS（許容 0.188）
④ 局面とともに上昇 0.676 → 0.878 → 0.986（+0.310）  ★PASS
① 画面 ≥ 出走表                 ⚠️ **道中で −0.056**（§4 で照会）
```

★**③④ は、裁定のとおり「上がるのが既定」になりました。**
前は ③ +0.218（FAIL）・④ 平坦（+0.001）でした。

---

## 2. 何をしたか

```
いま:  位置(t) = f(走破タイム, 脚質, ペース)              ← 逆算できる＝漏れる
是正:  位置(t) = 真の位置 + a(t)·( 隊列の位置 − 真の位置 )
       隊列の位置 = 隊列の中心 + 広がり × (0.5 − 脚質スロット)
       a = 1（残り1000m 以上）… ★脚質しか語らない＝漏れない
       a = 0（残り 200m 以下）… ★真実そのもの＝着順は厳密に一致（D-059）
```

★**隊列の中心は全馬の平均**なので、どの馬についても同じ値です（個体の情報がない）。
そこからのずれは**脚質のスロットだけ**で決まり、脚質は**出走表に既にあります**。
→ ★**画面が新しく漏らすものがありません。**

### ★収束の開始を「勝負所（残り800m）」ではなく「3角（残り1000m）」にしました

⚠️ 800m から始めると、★**その瞬間はまだ純粋な隊列**なので、
V-16 ② を「勝負所以降」で測ると**入口の1点だけで落ちます**（実測 0.680 対 0.732）。

★**これは通すための調整ではありません。** オーナー指示にも
「★**3角 いよいよ仕掛ける騎手が動き始める**」とあり、
実際の競馬でも**仕掛けは勝負所の入口より前**から始まります。

### 撤去したもの（D-063）

```
PHASE_JOSTLE / harmonicsFor / easeWithin / fadeOf
DEFAULT_JOSTLE / JOSTLE_FADE_M / boundaryFidelity（'exact' | 'shape'）
```

★**`boundaryFidelity` も撤去しました。** あれは「中間境界で逆算できる」を避けるための
回避策で、生成器では**中間境界がそもそも真の位置ではない**ので不要です。

### 横位置 `w`（Q-P4-29）— **同じ生成器から出しています**

```
w = 内寄りの基準（脚質スロット） + 直線での膨らみ（後ろの馬ほど外へ）
```
★`HorseAt.w` に載りました。⚠️ **斜め俯瞰で初めて画面に出ます**（真横では表示できません）。

---

## 3. ★検査

**792件 緑。** うち、この便で**意味が変わったもの**:

| 検査 | 旧 → 新 |
|---|---|
| 「境界時刻には必ず境界の位置にいる」 | ★**発走とゴールだけ**に。中間境界がぴったりだと**そこで逆算できる**（実測: 道中 0.923→0.731 なのに境界だけ 0.963 のまま） |
| 「道中は動かない」→「道中はシードで変わる」 | ★**「道中は脚質で決まり、走破タイムでは決まらない」**（★1日で2度、意味が変わりました。経緯を全部残しています） |
| 「'exact' は中間境界を動かさない」 | ★**「中間境界は動く — そこが漏洩の口だったから」** |
| ゲージの検査 | ★**時刻ではなく残り距離**で見る形に（生成器のぶん位置がずれるため） |

★**新規**: `formation.test.ts` 8件（漏れない／読める／収束／着順不変／後戻りしない／`w`）

---

## 4. 🔴 Q-P4-40 — **V-16 ① は、この設計では成立しません**

```
道中: 画面 0.676 / 出走表 0.732 → ★−0.056
```

★**これは裁定の言葉そのままの帰結です**:

> 序盤の位置は**脚質しか語らず**、脚質は**出走表に既にあります**。
> ★**画面が新しく漏らすものがありません**

**漏らさない ⇒ 画面 ≤ 出走表**。①（全局面で 画面 ≥ 出走表）は、
★**「道中に何か余分に映せ」と要求している**ことになります。

### 選べる形

| 案 | |
|---|---|
| **A** | ★**① を「道中は出走表と同等（差が `se` 以内）」に緩める。** 漏れない設計では上回れない |
| **B** | ★**道中にも、出走表に無い本物の情報を出す。** ⚠️ ただし**それが結果に効いていなければ AUC は上がりません** |
| **C** | ① を「**勝負所以降**で 画面 ≥ 出走表」に限定する（②と同じ範囲にする） |

### ★B について — **D-065 と直結します**

`w`（内/外）は**いま生成できています**。斜め俯瞰なら**画面にも出ます**。
★**しかし `w` が着順に効いていなければ、画面ボットの AUC は上がりません**（飾りだから）。

→ ★**D-065（距離ロスを着順に効かせる）が実装されて初めて、B が成立します。**
   ⚠️ D-065 は**まだ `resolveRace` に入っていません**。

---

## 5. Q-P4-31（D-062 の時間配分）— **測り直しました**

> 道中を3倍速で送る根拠は「情報が負だから」でした。Q-P4-38 で道中が情報を持つようになります。
> せっかく作った情報を、速送りで捨てることになります（裁定）

★**測定**: 道中の画面ボットは **0.676**、出走表は 0.732。**差は −0.056**。

→ ★**道中は、まだ出走表を上回っていません。** つまり
   **「せっかく作った情報」はまだ出走表を超えていない**ので、
   ★**いまの圧縮（道中 2.7倍速）を緩める根拠は、まだありません。**

⚠️ **D-065 が入って `w` が効くようになれば、話が変わります。** そのときに測り直します。

---

## 6. 積み残し

| # | |
|---|---|
| 🔴 **Q-P4-40** | V-16 ① の扱い（上記） |
| 🔴 **Q-P4-21 / 22** | `emptyAtMeter` ／ ゲージの定義（★次便で裁定いただける由） |
| ⚪ **D-065** | ★**まだ `resolveRace` に入っていません。** Q-P4-40 の B はこれ待ち |
| ⚪ **手順③** | 正典 §12.1 ／ アートバイブル §3 の改訂（★承認済み。文面を次便で出します） |
| ⚪ **sceneAt の斜め俯瞰対応** | これで `w` が画面ボットにも見えるようになります |
| ⚪ **第3便** | まだ出していません |
| ★**オーナー** | **staging の DB パスワードと service_role キーのローテーション（未実施）** |
