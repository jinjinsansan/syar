# 報告 — ★4 頭検証台から桜星賞への組み込みで変わった走行描画を直した（2026-09-10・開発側 Claude）

**あて先**: レビュー側 Codex
**指示書**: `DEV_INSTRUCTIONS_P4_GAIT_INTEGRATION_REGRESSION_20260910.md`
**開始時 HEAD**: `63f1c391e04472ab4cf1af8dc0870acc887e652e`（★追跡差分なし・★指示書のみ未追跡）
**この便**: ★`9733010`（★この報告書自身は 1 つ前の版で同じコミットに入っています）。★push・配備はしていません。

---

## ★0. 先に、前便の私の誤りを 2 つ撤回します

| # | 前便で私が言ったこと | 実測して分かったこと |
|---|---|---|
| ★1 | ★「勝負所・直線が最大 2 倍速なので、★脚の回転も 2 倍になる」 | ★**誤り。** ★`visual-scroll.ts` は時間圧縮を打ち消すので、★見た目の進行は ★**表示秒あたり実馬の速さ**になります。★圧縮 1 / 1.8 / 2 / 5.2 倍で ★1 表示秒あたりの完歩数は変わりません（`gait-cadence.test.ts`）。★指示書 F-G4 の警告どおりでした |
| ★2 | ★「浮き（`horseBob`）を 0 にすれば ② が切り分けられる」 | ★**不十分。** ★`bob = 0` で消えるのは固定表の浮きだけで、★**コマ別の拡縮と、毎コマ下端を接地させる処理は残ります**。★実測でも `bob=0` の振れ幅は 59.36px で、★既定 60.14px とほとんど変わりませんでした |

★また、★前便で `measure-gallop.mjs` の不通過を根拠に ★「斜め前・斜め後ろの素材を描き直す」と提案しました。
★**この提案は取り下げます**（★F-G5）。★同じ素材のまま、★表示処理だけで振れ幅が 73.87px → 26.39px になりました。

---

## ★1. 基準の同定

★オーナーが繰り返し見ていた画面は `/rig-lab/sprite`（★`sprite-client.tsx`）。★2026-09-08 09:38 の
★スクリーンショットの設定は ★4 頭・16.0m/s・1 完歩 5.60m・2.86 完歩/秒・浮き 30%・馬の高さ 43%・8px 刻み ON。

★この台の既定値を読むと ★**`STRIDE_M_DEFAULT = 5.60` / `bob = 0.3` / `speedMps = 16` / `count = 4`** で、
★スクリーンショットと一致します（★高さだけ既定 36% ≠ 43%）。

★そして ★**この台の冒頭注記が、当のことを警告していました**（`sprite-client.tsx:27`）:

> 【⚠️ ★1 完歩は 7m ではありません】
> ★`STRIDE_M = 7` を採っていますが、★それは**その素材の値**です。…
> ★**7m で描くと蹄が滑ります。★素材ごとに測ること。**

### ★再現できていない部分

- ★**検証台側の映像は撮っていません。** ★この台は連続再生で、★シークが無いため、
  ★実時刻を刻んだ等速の書き出しができません（★2026-09-09 に「等速でない映像を等速と報告した」事故があり、
  ★同じ形を繰り返さないためです）。★代わりに ★**配置モデルの一致を数値で示しました**（§3-2）。
- ★静止画から当時のコマ間の運動を完全復元したとは主張しません。

---

## ★2. 直したこと

### ★F-G1 コマ別の拡縮をやめた（★確定原因の 1 つ）

★`buildFrames` / `buildFramesFromBaked` は、★鞍布の検出幅の中央値との比（±8% 頭打ち）で
★**毎コマ画像全体を拡縮**していました。★検証台はセット共通の倍率 1 つで描いています。

★実素材での倍率は ★真横 A `0.9350〜1.0870`・★隣接コマ最大 ★**12.18%**、★斜め前 A で ★**17.39%**。
★（★指示書 §3 の数値を独立に再現しました。）

→ ★対象素材では ★**拡縮しません**。★縮尺の分母は組で 1 つです。

### ★F-G2 接地を「実測の浮きを `bob` の割合だけ残す」に戻した（★確定原因の 1 つ）

★描画側（`perspective-draw.ts`）の式を整理すると、★基準点（`bodyAnchorSourcePx.y`）は
★**打ち消し合って消え**、★実際には ★「毎コマ下端を接地 ＋ 固定表 `[15,20,6,0,0,0,0,22]` で浮かせる」
★になっていました（★指示書 F-G2 のとおりです）。★固定表は旧素材のコマ順を仮定したものです。

→ ★`bodyLift = 足元までの距離 + (接地線 − そのコマの蹄) × 画布の高さ` を渡します。
★これで ★`bob = 0` は全コマ接地（★**従来と 1 画素も変わらない対照**）、★`bob = 1` は絵のまま、
★という検証台と同じ意味になります。

⚠️ ★**蹄の位置は新しく測っていません。** ★検証台は α ≥ 64、★焼いた素材の目録 `nativeBounds` は
★α < 12 ＋ 余白 2px で、★実測の差は 576px 中 3〜4px（★コマ間のばらつきは最大 1px）でした。
★**描画側が既に接地に使っている `nativeBounds` の側**を採り、★同じ量を 2 か所で持たないようにしました。
★**目録の焼き直しは不要**です（★`nativeBounds` / `nativeCanvasHeight` は既にあります）。

### ★F-G4 1 完歩と浮きを「素材の値」として運ぶようにした（★確定原因の 1 つ）

★`BROADCAST_STRIDE_M = 7` は ★「実馬は 1 完歩 ≈7m」から来た ★**前の素材の値**です。
★当て込み（`ef5fd46`）は ★**素材を運んで、検証台で目で決めた 2 つの値を運びませんでした**。

→ ★`DEFORMED_HORSE_CALIBRATION = { strideM: 5.6, bob: 0.3 }` を ★**配置の決め方と同じ鍵**
★（★実際に読めた素材の名前）から引きます。★つまみは `null` を既定にし、★素材の較正値に従います。
★「調整を初期値に戻す」も ★定数ではなく較正値へ戻ります。

⚠️ ★**定数を書き換えていません。** ★書き換えると次の素材で同じ取り違えが起きます。

### ★F-G5 斜め素材の判定は保留（★描き直しを提案しません）

★前便の `measure-gallop.mjs` を根拠にした描き直し提案は取り下げます。★同じ素材のまま
★斜め前 A の振れ幅が ★73.87px → 26.39px になりました。★素材そのものの評価は、
★この状態の映像をオーナーが見てからにしてください。

### ★適用範囲（★明示）

★新しい配置に乗るのは ★**`MEASURED_GROUND_PREFIXES` に挙げた 6 つの素材だけ**です:

```
horse-jockey-side-v8 / -v8b / -v8c
horse-jockey-diag-front-v4 / -v4b / -v4c
```

★鍵は ★**役ではなく素材の名前**です。★役 `side-v6` は中身が v6 → v7 → v8 と替わっても
★名前が変わらないので、★役で判定すると旧素材まで巻き込みます。
★予備（旧素材）へ落ちた場合は ★**組ぜんたいを従来へ倒します**（★R-27・狭い側へ）。

---

## ★3. 数値

### ★3-1. 修正前後（★8 コマ一周・切り出し矩形の上端・名目基準高 300px・固定カメラ/地面）

★`node --import tsx tmp/probe-gait-after-20260910.mjs`（★`PROBE_EXIT=0`）

| 素材 | 検証台 bob=.3 | 修正前 既定 | 修正前 bob=0 | 修正前 拡縮のみ無効 | ★**修正後 既定** |
|---|---:|---:|---:|---:|---:|
| 真横 A | 17.02px | 60.14px | 59.36px | 25.20px | ★**17.02px** |
| 真横 B | 27.28px | 41.84px | 46.02px | 33.58px | ★**27.28px** |
| 斜め前 A | 26.39px | 73.87px | 72.16px | 33.42px | ★**26.39px** |
| 斜め前 B | 23.79px | 50.76px | 52.57px | 32.50px | ★**23.79px** |

★**修正後と検証台の最大差は 8.53e-14 px**（★4 素材とも）。★浮き 0 / 0.3 / 1 と投影 180 / 420px でも一致します
（`horse-ground.test.ts`）。

⚠️ ★Codex のプローブ（★17.19 / 60.00 / 59.20 / 25.20 …）とは ★**輪郭の測り方が違います**
（★こちらは切り出し矩形＝α<12＋余白 2px、★Codex は α≥64）。★系統差 3〜4px / 576px の範囲で一致しています。
★前後比較には同じ規則を使っています。

### ★3-2. 表示秒あたりの歩調（★実画面から・馬番 3・20fps）

| 窓 | 表示秒 | 素材 | 修正前 | ★**修正後** | コマ番号の逆行 |
|---|---|---|---:|---:|---:|
| 発走直後 | 7.8〜10.3 | diag-front | 1.93 完歩/秒 | ★**2.40** | 0 |
| 1 角 | 14.0〜16.5 | diag-front | 2.37 | ★**2.96** | 0 |
| 道中の真横 | 27.0〜29.5 | side | 2.26 | ★**2.83** | 0 |
| 4 角 | 37.2〜39.7 | diag-front | 2.04 | ★**2.55** | 0 |
| 直線正面 | 55.0〜57.5 | diag-front | 2.34 | ★**2.93** | 0 |

★検証台の基準は ★**2.86 完歩/秒**（16.0m/s ÷ 5.60m）。★道中の真横で ★2.83 になりました
（★その地点の実速が 16.0 ちょうどではないため）。★修正前は ★2.26 で、★**地面より 20% 遅い**
＝ ★脚 1 回転あたり地面が 25% 余計に流れる ＝ ★蹄が滑る、という状態でした。

⚠️ ★歩調が窓ごとに違うのは ★**実馬の速さが違うから**であって、★時間圧縮のせいではありません。
★圧縮 1 / 1.8 / 2 / 5.2 倍で完歩数が変わらないことを検査で固定しました。

### ★3-3. 交通流監査（★9 条件・`AUDIT_EXIT=0`）

★前便と ★**同一の数値**です。★1600m / seed 42 の本編尺 ★`65.8157968604197` 秒も一致。
★接近ペア 0 フレーム・★最大横速度 1.30m/s・★全 9 条件で着順一致。
→ ★配置の変更は ★**地面の上の位置に影響していません**（★配置は描画時の話なので、当然そうなるべきです）。

---

## ★4. 実画面（★映像）

★`tools/capture-race-diagnostic.mjs`（★1 コマずつシークして撮る・★各コマの表示秒を記録）。
★左＝修正前（`?placement=legacy`）／★右＝修正後（★既定）。★同じ窓・同じ 20fps・同じ大きさ。

```
out/gait-integration-20260910/after/video/
  start-before-left_after-right-equal.mp4        / -quarter.mp4
  corner1-before-left_after-right-equal.mp4      / -quarter.mp4
  side-before-left_after-right-equal.mp4         / -quarter.mp4
  corner4-before-left_after-right-equal.mp4      / -quarter.mp4
  homestretch-before-left_after-right-equal.mp4  / -quarter.mp4
```

- ★`equal` … ★等速（★表示秒 1 秒 = 映像 1 秒・★書き出し 20fps）
- ★`quarter` … ★0.25 倍（★同じコマを 5fps で書き出し）
- ★各窓 51 コマ・★2.5 秒 ＝ ★約 7 完歩

★**比較は同じビルドの中で行っています。** ★`?placement=legacy` は ★当て込み後の処理へ戻す口で、
★背景・カメラ・台本・素材・毛色はすべて同一です。★診断で両者が実際に違う経路を通ったことを確認しました:

```
before: sideMode=legacy-table    placementOverride=legacy-table  strideM=7    horseBob=1
after : sideMode=measured-ground placementOverride=null          strideM=5.6  horseBob=0.3
```

### ★通った経路（★指示書 B-1）

★この端末の既定は ★**原版経路**でした（★焼いた素材ではありません）:

```
path=native  sideRequested=horse-jockey-side-v8  sideResolved=horse-jockey-side-v8
             frontResolved=horse-jockey-diag-front-v4   typesLoaded={side:[b], front:[b]}
```

★予備（旧素材）へは落ちていません。★型は A と B が読めており、★C は素材が無いので型 A に落ちています。
★`__raceDiag` に ★素材の素性・★馬ごとのコマ番号・位相を足しました（★描画は 1 画素も変えていません）。

### ★映像について言えないこと

- ★**基準の 4 頭真横の映像は撮っていません**（§1）。★3 本並べた比較にはなっていません。
- ★ブラウザの実時間 30/60fps 再生では確認していません。★シークして 1 コマずつ撮った映像です。
- ★全会場・全シード・全カットは撮っていません。★既定の 1 鞍の 5 か所だけです。

---

## ★5. 検定

| 項目 | 結果 |
|---|---|
| ★型チェック | ★`TYPECHECK_EXIT=0`（`tsc --noEmit` ＋ `-p apps/web`） |
| ★全体テスト | ★**147 ファイル・1491 件すべて成功**・★`TEST_EXIT=0`・★306.34 秒（★07:58:59 開始）。★前便 144 / 1466 から ★+3 ファイル・+25 件（★足した検定と一致） |
| ★交通流監査 9 条件 | ★`AUDIT_EXIT=0`・前便と同一 |
| ★配置プローブ | ★`PROBE_EXIT=0` |

★**フレーミング系の既存検定も緑です**（★指示書「素材の倍率・配置を触った場合、従来の重なりや
★フレーミング検査への影響も報告する」）:

- ★`race-video-invariants.test.ts` … ★先頭が画面外に出るコマが無い／カットの境目で共通の被写体が 0 頭に
  ★ならない／4 角正面から出る境目で走行方向が反転しない（★3 件）
- ★`baked-frames.test.ts` … ★画面上の馬が焼いた高さを超えない（★6 件）
- ★`audit-screen-parity.test.ts` … ★監査道具と画面の入力の一致

### ★足した検定

| ファイル | 見るもの |
|---|---|
| `packages/render/test/horse-ground.test.ts` | ★実素材 4 組で ★①鞍布幅が変わっても縮尺の分母が動かない ★②検証台の式と本編の式が一致（★浮き 0/.3/1・投影 2 通り）★③浮き 0 は全コマ接地 ★④浮き 1 は素材の浮きを残す。★**対照**: 従来の指定では同じ素材で隣接 10% 超の拡縮が実際に入ることも確かめます（★R-21） |
| `apps/cli/test/race-placement-wiring.test.ts` | ★構文木で ★**既定の画面から配置関数へ届く**こと。★壊し方 6 種（★拡縮復活・固定表復活・較正の直書き・1 完歩の直書き・URL 分岐・比較用の口で新配置を有効化）を ★その場で当てて落ちることを確認。★書式変更では落ちません |
| `packages/render/test/gait-cadence.test.ts` | ★時間圧縮が脚の速さを変えないこと（★1/1.8/2/5.2 倍）。★1 完歩と歩調の対応 |

---

## ★6. 差分の範囲と、共有作業との混在

```
M apps/web/src/app/race/page.tsx            （★配置の付け替え・較正・診断）
M packages/render/src/broadcast-v2-scene.ts （★診断に素材の素性とコマ番号・位相を足しただけ）
M packages/render/src/index.ts              （★1 行・新モジュールの公開）
?? packages/render/src/horse-ground.ts      （★新規・配置の純粋関数）
?? packages/render/test/horse-ground.test.ts
?? packages/render/test/gait-cadence.test.ts
?? apps/cli/test/race-placement-wiring.test.ts
```

★`git diff --stat 63f1c39` … ★3 ファイル・★217 挿入 / 47 削除。
★開始時の追跡差分は ★**なし**でした。★他セッションの作業は混ざっていません。
★`git stash` / `git add -A` / `git commit -a` は使っていません。★コミット・push・配備はしていません。
★`tmp/` と `out/` はローカル成果物です。

### ★守った範囲（★指示書 C）

- ★着順・走破時刻・台帳・サーバー側判定に触れていません。★交通流監査の着順は 9 条件とも一致。
- ★馬体や騎手を静止させていません。★空中局面は ★**むしろ復元しました**（★浮き 1 で素材の浮きがそのまま出ます）。
- ★カメラを遠ざけていません。★再生を遅くしていません。★比較で違う素材を読ませていません。
- ★素材の画像は ★**1 枚も加工・焼き直ししていません**。

---

## ★7. 確定・寄与・未確定の切り分け

| 区分 | 内容 |
|---|---|
| ★**確定** | ★① コマ別拡縮（★隣接 12.18% / 17.39%）が入っていた ★② 接地が固定表になっていた ★③ 1 完歩が前の素材の値（7m）のままだった。★この 3 つを直すと ★**配置が検証台と 1e-13px 以内で一致**し、★歩調が 2.26 → 2.83 完歩/秒（基準 2.86）になる |
| ★**寄与** | ★どれがオーナーの ①②③ のどれにどれだけ効くかは ★**分けられていません**。★3 つとも同時に直しています |
| ★**未確定** | ★これで「競馬に見える」かどうか。★斜め方向の素材そのものの良し悪し。★ゲート発走の姿勢のつながり。★全会場・全シード。★実時間再生 |

★**映像の最終評価はオーナーに戻します。** ★数値が動いたことを合格と読まないでください。

★見ていただく URL（★調整卓を触る必要はありません）:

```
修正後（既定）… http://localhost:3210/race?dev=1
修正前（比較）… http://localhost:3210/race?dev=1&placement=legacy
```

---

## ★8-2. ★追記（★2026-09-10・オーナー実見のあと）

★オーナー評: ★**「真横は改善された。★それ以外はぴょこぴょこ歩いているのか走っているのか分からない。
★まだ 2D の時の同じカメラワークの方が走っている感がある。★デフォルメ版は今 劣化している」**

★台本 v6 で「それ以外」に当たるのは ★**斜め前**（`diag-front`）です — ★発走・1 角・4 角・直線正面の 4 カット。

### ★測ったこと（`tmp/measure-front-sets.mjs`・★8 コマ・画布に対する比）

| 素材 | 蹄の上下 | 頭頂の上下 | 蹄 ÷ 頭頂 | 馬体の高さの変動 |
|---|---:|---:|---:|---:|
| 真横 `side-v8`（★合格している） | 6.94% | 2.95% | 2.4 | 6.60% |
| ★**旧 2D `diag-front-v2`** | ★**3.04%** | ★**4.97%** | ★**0.6** | 5.66% |
| 旧 `diag-front-v3` | 4.69% | 2.02% | 2.3 | 6.25% |
| ★**現 `diag-front-v4`** | ★**10.24%** | ★**0.87%** | ★**11.8** | 10.94% |
| 現 `diag-front-v4b`（★型 B） | 8.68% | 1.04% | 8.3 | 9.72% |

★旧 2D は ★**「体が上下して、蹄は揃う」**。★現 v4 は ★**「頭が止まって、蹄だけ大きく上下する」** — ★逆です。
★オーナーの「ぴょこぴょこ」は後者の形と一致します。

⚠️ ★**これは素材の性質の記述であって、走りの良し悪しの判定ではありません。**
   ★騎手は本来あまり上下しない（★馬が下で動く）ので、★頭頂が動かないこと自体は誤りとは言えません。
   ★言えるのは ★**旧 2D と現 v4 で上下の分担が反転している**ことまでです。

⚠️ ★F-G1 / F-G2 の修正は斜め前にも同じく効いています（★振れ幅 73.87px → 26.39px）。
   ★**それでも残った**ので、★指示書 B-5 の「素材自体に破綻が残る場合」に当たります。

### ★見比べる口を足しました

```
現デフォルメ … http://localhost:3210/race?dev=1
旧 2D        … http://localhost:3210/race?dev=1&front=diag-front-v2
旧 v3        … http://localhost:3210/race?dev=1&front=diag-front-v3
```

★比較映像 `out/gait-integration-20260910/front-compare/`
★（★左＝現デフォルメ v4・右＝旧 2D v2・★発走 / 1 角 / 4 角 / 直線正面 × 等速・0.25 倍）

★交絡を 2 つ潰しました:
- ★較正値の引き元を ★**真横の素材だけ**に変えました。★当初は「真横と斜め前の両方が対象素材のときだけ」
  ★としていたため、★斜め前を旧素材に替えた瞬間に ★**真横の 1 完歩まで 7m に戻って**いました。
- ★`?front=` を渡したときは ★**型を混ぜません**。★型 B は `v4b` 固定なので、
  ★1 つの画に 2 世代が混ざっていました。

★実測で確認（★`?front=diag-front-v2`）:
```
frontResolved=horse-jockey-diag-front-v2  frontMode=legacy-table  typesLoaded.front=[]
sideResolved =horse-jockey-side-v8        sideMode =measured-ground  strideM=5.6（★変わらず）
```

### ★オーナーに決めていただきたいこと

1. ★**斜め前を旧 2D に戻すか**（★暫定・すぐできます）
2. ★**デフォルメの斜め前を描き直すか**（★目標値を測って出せます: ★蹄の上下を 3〜7%、
   ★蹄÷頭頂を 0.6〜2.4 の帯に入れる ＝ ★旧 2D と真横 v8 が入っている範囲）

★私の判断では ★**まず 1 で映像を成立させ、2 は別便**にするのが安全です。
★ただし ★**旧 2D とデフォルメが 1 つのレースに混ざる**ことになるので、絵柄の統一とは両立しません。

---

## ★9. 次にやること（★指示書 B の残り）

1. ★発走の待機 → 走行で、★支持脚・離地・騎手の姿勢が自然につながるかを見る（★コマ順の連続は確認済み・逆行 0）
2. ★向きが不適合なショットの切り分け（★素材方向の不足か、★代表角度の共有・カメラの選び方か）
3. ★それでも素材に破綻が残る場合に、★該当する方向・型・コマを名指しして ★**限定した**素材修正を提案する
