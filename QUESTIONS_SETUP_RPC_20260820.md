# 照会（セットアップ RPC — D-074 の初期馬付与が実装できるところまで決まっていない）— 2026-08-20

**宛先**: レビュー側
**発行**: 開発側
**関連**: D-074 ／ D-075 ／ V-19 #14 ／ Q-WEB-04 ② ／ §10.5 ／ §4.2
**なぜ今か**: `REVIEW_AUTH_IDENTITY_VERDICT_20260820.md` の「次の一歩 4（セットアップ RPC は upsert 禁止 → #14 を固定）」に着手したところ、**RPC 自体が書けませんでした**
**未裁定**: Q-SETUP-01 〜 Q-SETUP-04

---

## 0. 決まっていること・いないこと

**RPC が行うべき3つ**（Q-WEB-04 ②）は明確です — `users` 行の作成／初期 EP の付与（`ep_ledger` 記帳）／初期馬の付与を**1トランザクション**で。

**[CODE] スキーマ側も揃っています**:

| | |
|---|---|
| `horses.owner_id` が null＝NPC・`npc_stable_id` と**排他制約**（`0001_init.sql:143`） | → NPC 馬の付与は `owner_id` を入れて `npc_stable_id` を null にする形で表せる |
| `ep_ledger.reason` の許容値に **`inflow`** がある | → 初期 EP はこれで記帳できる（**照会不要**） |
| `horses.retired_at_week is null` ＝ 現役（`0010_training_state.sql:27,73-75`） | → 「現役馬が0頭」は表せる（**照会不要**） |
| staging の NPC プール **7,370 頭**（所有馬 0） | → 母数はある |

**書けないのは「どの馬を、どう選ぶか」です。**

---

## 1. 🔴 Q-SETUP-01 — 「素質は `user_id` から決定的に導く」が、文字どおりには実装できません

### D-074 の文

> **★素質は `user_id` から決定的に導く** — 引退させて引き直しても**同じ馬が出る**ので、振り直しが成立しない

**目的は明確です**（振り直しの否定）。**しかし「同じ馬が出る」は成立しません。**

| # | なぜ |
|---|---|
| 1 | **再付与の発火条件は「現役馬が0頭」**（D-074）。つまり**その馬は引退済み**で、引退した馬を現役に戻すことはできません（`retired_at_week` は消せない・戻せば繁殖の系譜と矛盾します） |
| 2 | **プールは有限で共有**です。`user_id` から特定の1頭を決定的に選ぶと、**2人が同じ馬に当たったとき、後の1人に渡せません**。そこで「次の候補」に落とすと、**もう決定的ではなくなります**（誰が先に登録したかで変わる） |

### 選択肢

| | 案 | 評価 |
|---|---|---|
| (a) | **特定の1頭**を `user_id` から決定的に選ぶ | **★破綻します**（上記1・2） |
| (b) | **素質の水準**を `user_id` から決定的に導き、**その水準に合う馬をプールから取る** | 目的は満たす。ただし**「ハッシュ運で初期条件が変わる」という新しい不公平**が生まれる |
| (c) | **水準を全員共通の固定帯にする** | 決定性は**自明に**満たされ（誰が何回引いても同じ）、**ハッシュ運の不公平も生じない**。個体差は無くなる |

**★開発側の意見: (c) を推します。**

**D-074 の目的は「振り直しを成立させない」ことであって、「人によって初期馬を変える」ことではありません。** (b) は目的を満たすために `user_id` のハッシュを持ち込みますが、**その副作用として「登録した瞬間に、自分では動かせない優劣がつく」**という、D-074 が避けようとしたものと同じ性質の不満を作ります。

**(c) なら「全員が同じ条件で始まる」と言い切れます。** これは §10.5 の「同じ土俵にいることが公正性の担保」とも、憲法 §0.3 の「無償で始められる」とも素直に噛み合います。**個体差は、そのあとの配合と育成で作るものだと考えます。**

**ただし体験としては (b) の狭い帯のほうが「自分の馬」感が出る可能性はあります。裁定をお願いします。**

---

## 2. 🔴 Q-SETUP-02 — 「低めだが外れではない」の具体値

> **素質は低めだが外れではない範囲に下限を置く**（初手が絶望的だと離脱する）

**下限も上限も、測る量も決まっていません。** 決めていただきたいのは3つです。

1. **何で測るか** — `potential` の合計か、能力5種の平均か、`unlock_rate` を掛けた現在値か、★表示（§12.4 の1〜5）か
2. **帯の位置** — NPC 集団の分布に対してどこか（例: 下位25〜40パーセンタイル）
3. **★較正定数として扱うか** — D-075 が初期 EP を較正定数としたのと同じく、**値そのものをゲートにしない**扱いでよいか（V-14 の「係数ではなく結果をゲートにする」と同型）

**開発側の意見**: **パーセンタイルで指定するのが安全**です。絶対値で書くと、**世界が成熟して集団の水準が動いたときに帯が意味を失います**（D-053 で「較正条件は定数の意味の一部」と確認したのと同じ形）。

---

## 3. 🟡 Q-SETUP-03 — 「同格帯」の候補集合と、枯渇したときの扱い

### ① 候補集合

**「同格帯・低素質」の「同格帯」が何を指すか**が決まっていません。**新馬・未勝利相当（＝まだデビューしていない若い馬）**という理解でよいですか。

その場合、**NPC プールの中でそれをどう表すか**も決めてください（`birth_year` と現在の週から年齢を出す／`generation` の最新層を使う／レース戦績が0件、など）。

### ② 枯渇したとき

**条件に合う NPC 馬が無い場合**の扱いが決まっていません。**D-074 は「専用の生成経路を作らない」**としているので、**その場で1頭作る**のは採れないはずです。

| | 案 |
|---|---|
| (a) | 帯を広げて必ず1頭出す（決定性は保つ） |
| (b) | セットアップを失敗させて再試行を促す（★新規登録が止まるので離脱に直結） |
| (c) | NPC 繁殖のバッチが供給する前提で、**在庫の下限を監視する**（V 系の監視項目にする） |

**開発側の意見**: **(a) を既定にしつつ (c) を併せて**。**(b) は「最初の1頭が来ない」＝ D-074 が避けようとした離脱そのもの**です。

### ③ NPC を1頭抜くことの影響

§10.5 は NPC の役割に **①出走頭数の充填 ②ベット対象の供給 ③血統プールの供給**を挙げています。**付与はこの母数を減らします。**

7,370 頭に対し1人1頭なら当面は軽微ですが、**「減らしてよい」と明示しておきたい**です（後で「いつの間にか NPC が痩せていた」を避けるため）。

---

## 4. 🟡 Q-SETUP-04 — 未セットアップ状態の判定を、どこに置くか

前回裁定 §1 で —

> **「`users` を参照する RPC が自然に落ちる」ことに任せないでください** — 落ち方が RPC ごとに違うと、どこかで「0 で通る」ものが出ます。**未セットアップを明示的に判定して拒否**すること

**同意します。置き場所だけ決めさせてください。**

| | 案 |
|---|---|
| (a) | **共通の判定関数**（例: `assert_setup_complete()`）を作り、**各書き込み RPC の先頭で呼ぶ** |
| (b) | 各 RPC が個別に `users` の存在を確認する |

**開発側の意見**: **(a)**。(b) は**書き忘れが必ず出ます**し、書き忘れは「通ってしまう」側に倒れます。**(a) なら「呼んでいない RPC がある」ことをメタテストで検出できます**（`tools/lib/classification.mjs` の登録漏れを `tool-guard.test.ts` が塞いでいるのと同じ形）。

---

## 5. 決まれば、すぐ書けるもの

裁定が出れば **V-19 #14（セットアップ RPC の二重実行 → 口座・初期 EP・初期馬が1つだけ）は短時間で固定できます。** `users.id` が PK なので**素の `insert` なら2回目は落ちます**。要求は「**upsert を書かない**」「**2回目は全ロールバック**」で明快なので、**RPC の設計さえ決まれば、試験は既存の `verify-v19-db.mjs` に足すだけ**です。
