# 照会: 自分の繁殖牝馬で配合する経路（`foal_requests.kind = 'breed'`・相手は NPC の種牡馬だけ）— 2026-09-22

> 開発側 → レビュー側。裁定 `REVIEW_I1_STEP3_ROLE_REQUEST_VERDICT_20260922.md` §5「次の順番」1。
> コードはまだ書いていません。本番には触れていません。開始時の HEAD `18b0ebb`。
> 他の利用者の種牡馬に付ける経路（種付料の 80% が持ち主へ）は、**この便に入れません**（I-4 と弁護士ゲートの便）。

## §0 一行

初回の配合（`breed_initial`）の確定の作りを、そのまま `kind = 'breed'` にも使います。違いは 3 つです。
- 母が**自分の繁殖牝馬**であること
- 種付料を**確定のときに EP から引く**こと（NPC 種牡馬なので全額焼却）
- 業務上の一意の鍵が**母と年**であること

決めてほしいのは、**種付料が依頼と確定の間に変わったときの扱い（B-4）**と、**画面に出す見積もりの置き場所（B-6）**の 2 つです。

## §1 事実

| # | 事実 | 箇所 |
|---|---|---|
| F-1 | `foal_requests.kind` は `'breed'` を既に許している。形の制約（父母あり・下書きなし）も `breed_initial` と同じ組に入っている。**受け付ける RPC が無い** | `0061:46-56`・`0061:20` |
| F-2 | 初回の配合の確定（`confirmInitialBreeding`）の流れは次のとおり。種付料は**免除**で、額を要求の行に書き、台帳には増減 0 の `stud_fee` 行を置く<br>・錠の順は要求 → 母 → 種牡馬<br>・`canMate`、今年の産駒の数え（馬＋下書き）、所有上限（現役＋名前の無い下書き）を見る<br>・`breed()` の種は `player-foal|<seed_key>` から作る | `player-breeding.ts:150-330` |
| F-3 | 台帳の語 `stud_fee` は既にある（`0001`〜）。種付料の式は TS の `npcStudFee(G1 勝利数, 総獲得賞金)`（§10.5） | `packages/scheduler/src/stud-fee.ts:43` |
| F-4 | 調教の EP は、ワーカーが週送りの中で `spend_training_ep` を呼んで引く。足りなければ ST00x で投げ、ワーカーは休養に落とす | `training-runner.ts:368`・`0021` |
| F-5 | 段 1 以降、持ち主の繁殖牝馬は NPC の配合に使われない。段 3 で持ち主が繁殖入りさせられるようになった。**繁殖入りさせても、今は何も起きない** | `breeding-runner.ts`・`0070` |
| F-6 | 種牡馬の年の種付上限（`20 + G1 × 10`）は `canMate` が `coverings_this_year` で見る。NPC の牝馬とプレイヤーの母が同じ種牡馬の枠を取り合う（初回の配合と同じ） | §6.7 |

## §2 案

### 受付の RPC `request_breeding(p_request_id, p_dam_id, p_sire_id, p_max_fee_ep)`
- 利用者の行を最初にロックする（`0070` と同じ作法）。
- 安い確かめだけ SQL で行う。
  - 母が自分の馬で、`retirement_role = 'broodmare'` であること
  - 父が持ち主の居ない NPC の `stallion` であること
  - `p_max_fee_ep > 0` であること
- 年齢・今年の産駒・種付上限・近交は、**確定のときにワーカーが TS で見る**（`canMate` を SQL に写さない・D-052）。
- 冪等は 2 段にする。
  - 要求 ID の主キー。同じ ID で別の依頼が来たら ST040。
  - **業務上の一意 `(dam_id, breed_year) where kind = 'breed' and status <> 'failed'`**。`breed_year` は受付の時点の年（`world_state.game_week` から出す。読めなければ ST041 で受け付けない）。失敗した依頼は枠を使わないので、同じ年にやり直せる。
- 列を 2 つ足す: `breed_year int`、`max_fee_ep bigint`。形の制約は `kind = 'breed'` のとき非 null とする。

### 確定（ワーカー・`runPlayerBreeding` の中）
- `breed_initial` と同じ関数に通し、違いは引数で渡す（母の条件・種付料を引くかどうか）。**経路を 2 本にしない。**
- 種付料は**確定のときの額**を `npcStudFee` で出す（初回の配合の免除と同じ数え方）。
  - 額が `max_fee_ep` を超えたら `failed / fee_above_max`（B-4）。
  - EP を引く: `users.entry_points` を条件付きで減らし（残高 ≥ 額）、台帳に `stud_fee`・`-額` を書く（`dedupe_key = stud_fee:<要求 ID>`）。足りなければ `failed / ep_short`（B-2）。
  - **同じ取引の中で**下書き・種牡馬の数え・要求の完了を書く。1 つでも落ちれば全部戻る。
- NPC の種牡馬なので**全額焼却**（§3.3・§10.5）。利用者の間で EP は動かない。

## §3 決めてほしいこと

- **B-1 いつ引くか**: 確定のとき（ワーカー）。裁定の第一候補どおり。受付では引かない（受付で引くと、確定で断ったときに返す経路が要る＝EP が戻る経路が増える）。→ これでよいか。
- **B-2 足りないとき**: `failed / ep_short` にして、**待たない・やり直さない**。母はその年のうちに、もう一度依頼できる（失敗は枠を使わない）。→ これでよいか。
- **B-3 冪等の鍵**: 要求 ID ＋ `(母, 受付の年)`。受付の年と確定の年がずれた場合（ワーカーが年をまたいで遅れたとき）は、確定の年の規則（`canMate`・今年の産駒）で判定する。同じ母が 1 年に 2 頭産むことは、`canMate` と「今年の産駒の数え」の 2 か所で止まる。→ これでよいか。
- **B-4 🔴 額が依頼と確定の間に変わったら**: 種牡馬が間に G1 を勝つと、式の額が上がる。
  - (a) **推し**: 依頼に「払ってよい上限」（`max_fee_ep`、画面が見積もりをそのまま入れる）を持たせる。確定の額がそれを超えたら断る。払うのは常に確定の額（**上限は断る理由にしか使わない**。利用者の申告で額が下がることは無い・憲法 3）。
  - (b) 確定の額を黙って引く（利用者から見ると、見積もりより多く引かれることがある）。
  - (c) 受付の時点の額で固定する。受付の RPC は SQL で、式は TS にしかないので、**式を SQL に写すことになる**（D-052）。採らない。
- **B-5 種牡馬の枠の取り合い**: NPC の牝馬とプレイヤーの母が同じ種牡馬の年の枠を取り合う（F-6）。先に確定した方が取る（ワーカーが種牡馬の行をロックする）。**プレイヤーを優先する仕組みは作らない**（NPC の供給と同じ土俵・§10.5）。→ これでよいか。
- **B-6 画面に出す見積もり**: 画面は種牡馬ごとの見積もり額を出す必要がある。式は `@star/scheduler` の `npcStudFee`（純粋関数）。
  - (a) **推し**: 読むだけの RPC で「持ち主の居ない種牡馬の G1 勝利数・総獲得賞金・今年の残り枠」を返す。額は画面側で `npcStudFee` を呼んで出す（表示だけ。**確定の額はワーカーが出す**・B-4 (a) の上限で食い違いを受け止める）。
  - (b) 見積もり専用の RPC（SQL に式を写す）。採らない。
  - ⚠️ (a) では、**画面が業務の式を呼ぶ**ことになる。ただし `packages/` の関数を読んで表示するだけで、Route Handler に業務の処理を書くのではない（CLAUDE.md の禁止に当たらないと見ている）。この読みでよいか。
- **B-7 画面**: 裁定 Q-E のとおり、この便で「繁殖入り」と一緒にデザイナーへ依頼する。依頼の文は照会の答えが出てから書く。

## §4 着手の順（答えをもらった後）

1. 移行（列 2 つ・形の制約・部分一意・受付の RPC・読むだけの RPC）→ 試験（最後の定義から読む作法で）
2. ワーカー: 確定の関数を `kind` で分岐させる（経路は 1 本のまま）・種付料を引く・`fee_above_max`／`ep_short`
3. staging の予行（必ず rollback）: 正常・再送・同じ ID で別の依頼・母が自分の繁殖牝馬でない・父が持ち主のいる馬・今年 2 頭目・上限超え・EP 不足・額の上昇・台帳の行と残高の一致
4. 報告。取引の境界を変える場合は、呼び出す道具と偽の DB を並べる（裁定 I-1 §6）
