# REPORT_P3_Q32_20260812 — Q-P3-32 の是正と、指定4項目の検証

## 本便で正典の改訂が要る決定をしたか

**していません。** ただし**測り方について1つ、裁定をお願いしたい**ことがあります（§4 の V-10）。

---

## 1. 結果一覧

| 指定項目 | 結果 | 証拠 |
|---|---|---|
| ① `diag-conditions` が 7/7 一致 | **PASS**（0/7 → 7/7） | `tools/diag-conditions.mjs` |
| ② `track_condition` の分布が good 固定でない | **PASS**（稍重が実際に発生） | `tools/verify-conditions-db.mjs` |
| ③ V-10 をワーカー＋DB の経路で測る | **★実現払戻率は原理的に判定不能。**代わりの測定で **PASS** | `tools/verify-v10-db.mjs` |
| ④ 道悪で `heavy_aptitude` が効く | **PASS**（★観察ではなく実験で） | `tools/verify-heavy.mjs` |
| テスト | **602件 緑** | `30fa5f6` |

---

## 2. 是正の内容

**レース条件の出所を1つにしました。**

| | 是正前 | 是正後 |
|---|---|---|
| DB に保存 | `conditionsOf(idx)` | **`buildRace` が返した条件** |
| オッズの前提 | `generateRace` が自分で引いた値 | 同上（同じ値） |
| `track_condition` | **`'good'` 直書き** | `generateRace` が §10.4 の分布から引いた値 |

- `generateRace` に `programme`（番組表の距離・馬場）を渡せるようにしました
- `buildRace` は**オッズを計算したときの条件をそのまま返します**
- `cycle-runner` は `build` の返り値ひとつを保存します

### ★乱数の並びを変えていません

`rng.pick(DISTANCES)` を**飛ばさず、引いてから上書き**しています。
飛ばすと乱数の並びがずれ、**頭数も馬場状態も出走馬も変わります**。
P1 のゲート（V-4/V-5/V-6）は `programme` を渡さない経路で測っているので、
この書き方なら**1ビットも動きません**（段1で P1 の再測定が不要という裁定に沿います）。

---

## 3. ①② の実測

```
## ① 保存された条件 vs 番組表（cycle 581 以降の8本）
  cycle 588  DB turf/1200m/good      番組表 turf/1200m   一致
  cycle 587  DB dirt/3000m/yielding  番組表 dirt/3000m   一致
  cycle 585  DB turf/2000m/yielding  番組表 turf/2000m   一致
  cycle 582  DB dirt/1400m/yielding  番組表 dirt/1400m   一致
  （他4本も一致）
  ✓ ① 8/8

## ② 馬場状態
  good 21件(84.0%) / yielding 4件(16.0%) / soft 0 / bad 0
  ✓ ② 良馬場以外のレースが存在する（4/25）
  ⚠️ 25本しかないので**割合の一致は判定していません**
```

生成側の分布（2000サイクル）は **良 75.3% / 稍重 14.4% / 重 6.8% / 不良 3.5%** で、
§10.4 の CDF（75/15/7/3）と一致します。

### ★是正前のレースが16本残っています

staging には「オッズは A の条件・DB は B の条件」のままのレースが残っています。
**黙って除くと「直った」と読めてしまう**ので、`--from-cycle` で判定範囲を明示し、
**除いた本数と理由を必ず出す**ようにしました。

---

## 4. ③ V-10 を本番経路で — ★実現払戻率は原理的に測れません

指定どおり、**本番が作ったオッズ（`race_odds`）と本番が出した着順（`race_entries`）**だけで測りました。

```
  券種              払戻率     目標      差       SE
  win             292.97%    82%  210.97pt  178.48pt
  trifecta        853.55%    77%  776.55pt  862.79pt
  （他5券種も同様に大きく外れ、SE も同程度）

  【±1% を 3 SE で判定するのに要るレース数】
    win        2,866,895 本
    trifecta  66,996,567 本
★V-10（本番経路）: 判定不能 — 本数が足りません。合否を出しません
```

**1レース＝1回の確定**なので、分散が桁違いに大きくなります。
1本の生成に約100秒かかるので、**287万本は約9年、6,700万本は約212年**です。
→ **この経路で実現払戻率を ±1% で判定することは原理的にできません。**

`verify-pmin.ts` が「1レースにつき確定を何度も引く」（D-036）のは、まさにこのためでした。

### ★代わりに、分散ゼロで測れる量を足しました

`race_odds` には**確率とオッズが両方**入っています。
§9.4 の設計では `odds = (1 − margin) / p` なので、**`p × odds` は買い目ごとに `1 − margin`** のはずです。
**確定を1回も引かずに**、オッズ表が正しい控除率を含むか測れます。

```
  券種              p×odds     目標      差        上限に当たった目
  win             81.999%    82%  -0.001pt         なし
  place           82.068%    82%  +0.068pt         なし
  quinella_place  80.012%    80%  +0.012pt         なし
  quinella        79.990%    80%  -0.010pt         なし
  exacta          79.981%    80%  -0.019pt         なし
  trio            76.957%    77%  -0.043pt         なし
  trifecta        76.645%    77%  -0.355pt         なし
```

**全券種が ±0.36pt 以内**で、**上限に当たった目は 0**（D-035 の `p_min` が効いています）。
→ **本番経路のオッズ表そのものは正しい控除率を含んでいます。**

### ★裁定をお願いします

「本番の払戻率を測る」の**測り方**として、
**`p × odds`（オッズ表が含む控除率）を V-10 の本番経路版とする**のでよいでしょうか。

- 実現払戻率は**原理的に測れない**（上記）
- `p × odds` は**確定を引かないので、オッズ表が壊れていれば少数レースでも即座に出ます**
- ただし「**着順の分布が p と一致しているか**」は別の話で、これは測れていません
  （それを測るには結局、多数の確定が要ります）

---

## 5. ④ 道悪の効き — ★観察では逆符号が出ました

まず観察で測りました。**道悪レースでの `heavy_aptitude` と着順の相関**:

```
  道悪（稍重）  3本  相関 +0.208  SE 0.079   ← ★逆符号（適性が高いほど着順が悪い）
  良馬場       15本  相関 +0.095  SE 0.066
```

★**良馬場でも同じ向き**でした。これは「道悪で効いていない」ではなく、
**`heavy_aptitude` が能力と負に相関している**（交絡）と読むのが自然です。
**出走馬の能力が違うまま相関を取っても、適性の効果は取り出せません。**

### ★そこで観察をやめ、実験にしました

**同じ出走馬・同じ乱数で、馬場だけ `good` → `soft` に変えて確定**します。
能力も枠も脚質も同じなので、**差はすべて馬場によるもの**です。

```
  標本 178頭ぶん（12レースを good と soft で1回ずつ）
  道悪適性と「soft で上がった順位」の相関: +0.253
  適性60以上（96頭）: 平均 +0.16 着ぶん上昇
  適性40以下（19頭）: 平均 −0.47 着ぶん下降
  ✓ ★道悪適性の高い馬が soft で順位を上げている
```

**判定は実験で出し、観察は参考値として併記**します。

---

## 6. 追加したもの

| ツール | 役割 |
|---|---|
| `tools/diag-conditions.mjs` | 生成の2経路を突き合わせる（読むだけ） |
| `tools/verify-conditions-db.mjs` | **保存された行**が番組表どおりか（`--from-cycle`） |
| `tools/verify-v10-db.mjs` | 本番経路の払戻率と、オッズ表が含む控除率 |
| `tools/verify-heavy.mjs` | 道悪の効き（観察＋実験） |
| `tools/settle-races.mjs` | staging で時間を進める代わり。★着順は本番と同じ `settleRace` を通す |
| `tools/diag-insert.mjs` | 一括投入の同値性と速度（前便） |

---

## 7. 照会

| # | 内容 |
|---|---|
| **Q-P3-34（新規）** | **V-10 の本番経路版の測り方**（§4）。`p × odds` を採るか |
| **Q-P3-33（新規）** | 馬場状態の**割合**の一致は何本で判定するか（いま25本では判定していません） |
| **Q-P3-31** | V-11 に「機構が動いていること」の条件（★正典に追加済みとのこと。実装は次便） |
| Q-P3-27〜30 | 未回答 |
| Q-P3-20〜26 | 未回答 |

---

## 8. 次便（段2〜4）

裁定どおり、**変更は1回・測定は3回**で進めます:

```
① 現状（B-6 配線あり・PLACEHOLDER_UNLOCK あり）
② ＋PLACEHOLDER_UNLOCK 廃止
③ ＋condition/fatigue 配線
```

★帯を出たら **K の再較正が要るので、動かす前に報告**します。
併せて **V-11 の②（発行量と消費量がともに実質ゼロでない）** を実装します。

---

## 9. 再現コマンド

```bash
npx tsx tools/diag-conditions.mjs                                    # ①
npx tsx tools/seed-races.mjs --env staging --races 8 --ahead 60
npx tsx tools/settle-races.mjs --env staging --from-cycle 581
npx tsx tools/verify-conditions-db.mjs --env staging --from-cycle 581  # ①②
npx tsx tools/verify-v10-db.mjs --env staging                          # ③
npx tsx tools/verify-heavy.mjs --env staging                           # ④
```

---

## 10. ★オーナーへ（再掲）

**staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん）。
