# 初回の馬づくり：実装前調査と変更案（2026-09-22）

状態: **提案・未実装**。オーナーが希望した「父を選ぶ → 母を選ぶ → 仔馬誕生の演出 → 命名 → 最初の調教」を、既存システムに接続するための調査。Claude Code / Codex は最初に `CLAUDE.md` と `STAR_SPEC_v2.0.md` を読むこと。この文書だけで正典を上書きしない。

## 1. 結論

- `packages/sim-engine/src/breeding.ts` の `canMate()` / `breed()` は父母・シード・祖先・ニックスから仔馬を決定する純粋な計算として再利用できる。遺伝式を画面側や SQL に書き直す必要はない。
- `apps/worker/src/breeding-runner.ts` は NPC 世界の週次配合用。プレイヤーが選んだ父母を受け付ける機能ではない。親の選択、排他、EP、本人の仔馬として保存、命名の経路は新設が必要。
- `db/migrations/0037_create_account_picks_horse.sql` の `create_account()` は口座・初期 EP・**既存 NPC 馬の所有権移転**を１トランザクションで行う。父母から新しい馬を作らない。`apps/web/src/lib/setup.ts` もその結果を読むだけ。
- **最大の仕様衝突:** `packages/scheduler/src/week.ts` は生後 78 週から調教可、104 週から出走可と定める。真の新生馬を現在週に誕生させると、命名直後に調教はできない。最初の馬の年齢と時間の扱いはオーナー決定が必要。
- `STAR_SPEC_v2.0.md` の D-074 / D-079 は既存 NPC 馬の無償付与を前提とする。新規ユーザーの入口を変えるなら、旧決定を履歴として残しつつ現行決定を明示して改訂する必要がある。既存所有馬を一括変換しない。

## 2. 実際に使える部品

| 部品 | 再利用 | 制約 |
|---|---|---|
| `breed()` | 遺伝子、近交、血統、適性、仔馬の性別等の計算 | サーバーで呼ぶ。クライアントに `genotype` / `potential` / `stats` を渡さない |
| `canMate()` | 性別、年齢、母の産駒上限、当年配合、父の種付上限 | DB の最新行をロックしてから再検査する。UI の候補表示だけで成立を保証しない |
| `buildPedigreeCache()` 等 | 血統・近交の計算 | 本番の `pedigree_cache` には過去の鍵不整合がある。修復と移行の順番を守る |
| `normalizeName()` | 表記揺れを吸収した馬名比較 | これだけでは NG 名検査にならない。実在名・重複・文字数をサーバー側で検査する |
| `RaceStrip compact` | 調教画面の常設表示の枠 | 現在は確定後の「結果から再現した録画」。生中継位置データではない |
| `training_orders` / `set_training_order` | 調教指示を保存する DB 側の下地 | 現行 `/train` は保存ボタンを無効化。ワーカー側の指示読込は本番未配備 |

## 3. 推奨する画面フロー

```
ログイン
  → 表示名・厩舎名・勝負服（本人の基本設定）
  → 初回配合の説明（最初の１頭は無償）
  → 父馬の候補一覧・選択
  → 母馬の候補一覧・選択
  → 選んだ２頭と血統の確認、確定
  → サーバーの生産完了後に誕生演出
  → 生まれた馬の性別・親・名前入力
  → 命名確定
  → 初回の調教／育成画面
```

選択・演出の順番と入力状態はデザイナーの新規ハンドオフが必要。既存 `レースが数分おきに開催.zip` 内の `RACE_NOTICE_HANDOFF.md` は、ログイン・登録・初回設定では RaceStrip を非表示、調教のフォーム操作中は極小 22×16px とする。初回調教に到達した時点で小窓が見えることを受入条件にする。走行中の実映像については `DESIGN_LIVE_RACE_DATA_CONTRACT_20260921.md` の位置データ契約が別途必要。録画を生中継と表示しない。

## 4. サーバー側の責務と状態

1. **候補の公開:** 新規アカウントでも読める、初回配合専用の父・母候補ビューを作る。公開項目は表示名、血統、性別、合法性の説明などに限定。非公開の遺伝子、素質、現在能力、内部スコアは出さない。候補の在庫、年齢、所有権、配合回数を最新時点で検証する。
2. **初回権利:** 認証済みの本人に初回無料配合を１回だけ認める。`auth.uid()`、メール確認、口座状態、所有馬状態をサーバー側で確認する。連打・再送・別タブ・通信断でも仔馬や初期 EP が増えない冪等キーを持つ。
3. **確定処理:** 父母の DB 行をロックし、`canMate()` 相当の条件を再確認し、サーバーが乱数シードと仔馬 ID を生成・記録する。`breed()` の出力、父母の使用回数、本人の所有権、繁殖記録、初期 EP 台帳を矛盾なく確定する。SQL だけで TypeScript の遺伝計算を別実装しない。既存 Next.js Route Handler にビジネスロジックを置かない。
4. **命名:** 仔馬生成後は暫定状態として管理し、本人だけが一度名前を確定できるようにする。`horses.name` は `NOT NULL` なので、名前未設定のまま通常の `horses` 行に入れる設計はできない。非公開の下書きレコードで確定済みの父母・シード・計算結果を保持し、命名後に通常の馬として公開する案が必要。重複、文字数、禁止名、実在馬名の検査をサーバー側で行う。画面を閉じても命名画面から再開できる。
5. **完了判定:** `users` 行の有無だけで「初回設定完了」とみなさない。`profile_created` / `parents_chosen` / `foal_created` / `named` / `ready` のような永続状態を持ち、再ログイン時に正しい段階へ戻す。既存の `login`、`home`、`useStableView` もそれに合わせて更新する。

上記は必要な責務であり、実装方式は未確定。現行プロセスは Web、CLI、VPS ワーカーだけで、TypeScript `breed()` をトランザクション内で安全に実行する公開サービスがない。サーバー側の配置、DB接続、ジョブ処理の方式を先に決め、staging で失敗時の再実行を検証する必要がある。

## 5. 決定が必要な仕様

| 論点 | 推奨案 | 理由・未決事項 |
|---|---|---|
| 誕生から最初の調教まで | 初回だけ時間の扱いを明示した導入体験を作る。年齢を黙って偽装しない | 現行 `canTrain()` は生後78週から。即時調教は正典改訂と育成・出走較正の再検証が必要 |
| 初回の親馬 | NPC 世界から、サーバーが適格と判定した候補を提示 | 現在の馬の遺伝と世界を使う。候補選択で隠し素質を漏らさない |
| 初回の費用 | １頭目のみ無償。父母の回数上限と在庫は通常どおり扱う案 | 無償参加を維持。再試行による無料再抽選を防ぐ |
| 既存プレイヤー | 現在の馬・台帳を保持し、新規アカウントから新フロー | 本番資産の一括書換え・再付与を避ける |
| 命名期限 | 完了まで初回フローを再開可能にする | 通信断で名前のない馬が永久に残るのを防ぐ |
| レース小窓 | 初回設定中は非表示、調教画面から表示 | デザイナーの確定仕様どおり。生中継データは別工程 |

## 6. 検証と配備の条件

- 純粋な `breed()` の既存テストを保持し、入口の変更で遺伝・近交・分布・決定論が変わらないことを確かめる。父母の組合せを固定した再実行で同じ仔馬になること。
- 同じ操作の二重送信・別タブ・中断・再ログイン・親馬が選択中に他者に使われる競合を検証する。EP や仔馬は一度だけ、失敗は途中状態から回復できること。
- 初回配合の候補と命名 RPC の RLS / 権限を検査し、他人の親馬・仔馬を操作できないこと、非公開能力値を読めないこと。
- `birth_week`、父母の ID、血統キャッシュ、繁殖回数、命名、所有者、週次育成を読み書きの両側で検査する。新生馬の年齢・調教可能時期を特に確認する。
- 既存アカウントの所有馬とポイントはそのまま。新規アカウントの導入体験は staging で実際に完走し、PC・スマートフォンで確認する。
- **本番 DB・ワーカーにはまだ適用しない。** 既存の本番血統修復 → 移行 `0060` → ワーカー配備には先行順序とオーナー許可がある。初回配合用の新移行を重ねる場合も、その順序との整合を確認する。

## 7. 次の実装単位

1. 正典の変更案と、初回年齢・親候補・無料権利を確定する。
2. デザイナーへ父母選択、誕生、命名、初回調教の画面・動き・中断再開を依頼する。
3. staging で候補の読み取り契約とサーバー側の初回生産処理を実装・検証する。
4. 画面を接続し、初回調教で RaceStrip が見えることを確認する。実レース小窓の生中継化は別のデータ配信工程として追跡する。
