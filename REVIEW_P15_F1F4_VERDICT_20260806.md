# F-1〜F-4 の裁定 — 近交回避案の撤回・合格基準3 の PASS・出荷構成の確認要請

**発行**: 2026-08-06（レビュー側 Claude）
**対象**: `233d471`（F-1〜F-4）
**判定**: **F-1 撤回 / 合格基準3 PASS / 合格基準1 は測定構成の確認待ち / V-2e は据え置き**

---

## 1. F-1 を撤回します — 近交回避は系統多様性の対抗力になりません

**penalty=3.0 で y50 有効系統数 6.02 → 2.09。** これで確定です。

理由も明白で、**近交回避は「自分の血からの反発」であって「多様性への誘引」ではありません。** 40厩舎すべてが自分の血から反発し、同時に最高能力に引かれるので、**全員が「自分と血が遠い最強系統」という同じ一点に集まります。**

しかも現実的です。実際の生産者も近交を避けつつ流行の種牡馬に殺到し、それが単一系統化を進めました。レビュー側が「現実の生産者は近交を避けるから単一系統にならない」と書いたのは、**現実の因果を逆に読んでいました。**

**そして V-12a は penalty=0 で PASS（平均F 0.0519 / 0.0884 / 0.0508 ≤ 0.10）。** 通っているゲートを壊してまで採る理由がありません。

→ **R-15 に従って機構を入れたまま penalty=0 で無効化した判断が正解**です。正典に **D-027** として記録しました。**ライブ運用でプレイヤーが意図的に近交を進めた場合の予備手段**として保持します。

> **有効化する場合の申し送り**: 25倍のコストは 1配合ごとに全種牡馬と F を計算しているため。**系統単位でキャッシュすれば桁で落ちます**。今は不要。

---

## 2. 合格基準3 — **PASS で閉じます**

**y50 で 6.02 / 5.25 / 5.03、3シードとも ≥5。** 判定線を通りました。y100 の減少は**ライブ運用の監視項目**へ降ろします（D-026）。

### ただし確認測定を1つ（ブロッカーにはしません）

**5.03 はレビュー側が発明した閾値に対する余裕ゼロの通過**です。掃引の裁定で「余裕ゼロの解は採らない」と書いた以上、指標の値だけで閉じるのは筋が通りません。**指標ではなく機能を測ってください:**

1. サービス開始時点の集団で、**実際に成立している `sire_line × bms_line` の組の数**（N-4 の「ニックスが意味を持つ程度」の実体）
2. **任意の配合で高Fになる割合**（アウトブリードの選択肢が実際に残っているか）

**これが問題なければ P1.5 の系統の議論は完全に閉じます。**

---

## 3. ★合格基準1 — V-2e の FAIL は出荷構成の値でない疑いがあります [CODE]

`apps/cli/src/preseed.ts:198` の既定は **`generations: 50`** で正典 §10.5 と一致しています。ところが:

| 出所 | seed2026 の V-2e |
|---|---|
| 合格基準1 の報告 | **0.771 FAIL** |
| F-4 の分解（**深さ50**） | 最悪 `distance_center` **0.842 PASS** |
| 深さ依存の測定 | 深さ100 = **0.771** |

**0.771 は深さ100 の値と完全に一致します。** 合格基準1 を `--generations 100` で回していませんか。**だとすれば出荷構成（50世代）では3シードとも PASS です。**

→ **50世代での3シードを出してください。**

### これは R-12 の裏返しです

今まで「**緩い**構成で測って通す」を警戒してきましたが、**「厳しい構成で測って落とす」も同じ誤り**です。どちらも出荷構成で測っていない。**規律は「出荷構成で測る」であって「厳しく測る」ではありません。**

---

## 4. F-4 — 予測は支持されず。**V-2e の下限 0.8 は据え置き**

| 形質 | SD比 | 選抜 |
|---|---|---|
| distance_center | 0.842 | あり |
| surface.turf | 0.881 | あり |
| heavy_aptitude | 0.915 | あり |
| **durability** | **0.938** | **なし** |
| surface.dirt | 0.938 | あり |
| temper | 0.951 | あり |
| distance_range | 0.953 | あり |

**選抜なしの durability が中位で、選抜ありの surface.dirt と同値。** 差 0.025 は 1.7 SE で分割の根拠になりません。そもそも選抜なしは1形質だけなので、平均の比較として成立していません。

**レビュー側の予測は棄却します。閾値は動かしません。** 深さ50 で全形質 0.842 以上なら、**そもそも動かす必要がありません。**

---

## 5. 自己申告2件について

### ① 変異試験の出力を読まずにコミット

「grep の終了コードで素通り」と同型で、通算3度目の**「実行したことと、結果を見たことの混同」**です。ただし**変異試験自体は正しく検出していました。壊れていたのは手順で、仕組みではありません。**

### ② ハーネスの SIGTERM 復元漏れ — 一般化に同意。R-18 として正典に追加

> **R-18: 検証ツールが作業ツリーを改変するなら、異常終了しても復元できなければならない。** `finally` は SIGTERM では走らない。**後始末まで含めて検証ツールである。**

補足すると、**この誤判定を可視化したのは P1 で必須化したベースライン緑の事前確認**です。無ければ「テストが既に落ちている」状態で**全変異が「防御済み」と誤報告**されていました。**②は事故ではなく、事前確認が仕事をした事例**です。

---

## 6. コードに撤回済みの根拠が残っています [CODE]

`apps/cli/src/preseed.ts:158-162` の `linesPerStable` のドキュメントコメント:

> 配合を制限して守るのではなく**創始系統を増やして**浮動に耐えさせる。配合を制限しないので近交は増えない（むしろアウトブリードの選択肢が増える）。

**両方とも実測で否定されました**（前者は D-026、後者は D-027）。**次に読む人が根拠として使わないよう、D-026/D-027 を指す記述に差し替えてください。**

---

## 7. P1.5 の残り

| # | 状態 |
|---|---|
| 合格基準1 | **50世代での3シードを再提出**（§3） |
| 合格基準2（5代血統表） | 済 |
| 合格基準3 | **PASS**。確認測定のみ（§2・ブロッカーでない） |
| 合格基準4（再現性） | 済 |
| 合格基準5（NG リスト） | 済 |
| 合格基準6（V-13） | 済 |
| 合格基準7（テスト・変異試験） | **再実行中の結果待ち** |

**これらが揃えば P1.5 クローズ、P2（`DEV_INSTRUCTIONS_P2_20260806.md`）に着手できます。**

---

## 8. レビュー側からの申し送り

**系統集中について、レビュー側の仮説は浮動・創始系統・近交回避と三度続けて外れました。**

ただし三度とも、**指示が仮説として出て、開発側が測って、正典に入る前に潰れています。** そして三度とも、**開発側が「指示どおりにやったら悪化した」ことを数字で示して止めました**（R-15 で有効化しなかったのが効いています）。

**順序が保たれている限りこれは機能している状態です。** 指示が仮説の形で来たときは、今回と同じように**測ってから採否を決めてください。**
