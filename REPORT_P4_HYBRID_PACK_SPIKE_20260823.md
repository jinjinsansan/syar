# REPORT — P4「2D世界＋3D馬群 ハイブリッド8秒検証」（2026-08-23）

対象指示書: `DEV_INSTRUCTIONS_P4_HYBRID_PACK_SPIKE_20260823.md`
前提: `REVIEW_P4_3D_PACK_SPIKE_VERDICT_20260823.md`（CONDITIONAL PASS）
実施範囲: 指示書の範囲のみ。通常 `/race`・レース結果・DB・worker・認証・ポイント・投票には触れていません。
競馬場の 3D 化も、通常レースへの組み込みも行っていません。commit・push もしていません。

> ★**この報告書は採否を決めません。** 測った値と、撮れた絵と、その場で起きた失敗を並べます。
> 判定はレビュー側の仕事です。
> ★**モバイル実機だけ未測定**です。数字を作らず `QUESTIONS_P4_HYBRID_PACK_SPIKE_20260823.md` で照会しています。

---

## 1. 変更ファイル一覧

### 新規

| ファイル | 行 | 役割 |
|---|---:|---|
| `apps/web/src/app/3d-hybrid-lab/page.tsx` | 28 | ハイブリッド検証ページ（本番では 404） |
| `apps/web/src/app/3d-hybrid-lab/hybrid-lab-client.tsx` | 885 | 3 層の合成・カメラ整合・測定の入口 |
| `apps/web/src/lib/spike-3d.ts` | 586 | ★3D 馬群の組み立て（3D 単体ページと**共通**） |
| `packages/render/test/scene-layers.test.ts` | 166 | 層分けが通常経路を変えないことの検証 |
| `packages/render-3d/test/hybrid.test.ts` | 223 | 指示書 §12 の自動テスト |
| `tools/lib/cdp.mjs` | 144 | DevTools プロトコルの共通部品 |
| `tools/diagnose-foot-artifact.mjs` | 288 | 足元の破片の原因をメッシュ単位で特定 |
| `tools/write-artifact-findings.mjs` | 174 | 上の生データ 2 つを突き合わせて結論を作る |
| `tools/capture-hybrid-lab.mjs` | 595 | 撮影・投影・遮蔽・影・資源・色調・性能・決定論 |
| `tools/render-hybrid-compare.mjs` | 101 | 参考・2D V3・3D C2・H3 を 1 枚に並べる |
| `tools/measure-hybrid-spike.mjs` | 161 | 測定 JSON を 1 つにまとめる |

### 既存の変更（最小限）

| ファイル | 変更 |
|---|---|
| `packages/render/src/parallax-plate.ts` | 層に `front?: boolean`、`drawParallaxPlate` に `pass` 引数（既定は従来どおり） |
| `packages/render/src/broadcast-v2-scene.ts` | `layer?: 'all' \| 'background' \| 'foreground'`（既定は従来どおり） |
| `apps/web/src/app/3d-pack-lab/pack-lab-client.tsx` | 872 → 502 行。共通部品 `lib/spike-3d.ts` を使う形へ |
| `tools/lib/classification.mjs` | 新規ツール 5 本を READONLY へ登録（R-24） |

### ★触っていないもの

`packages/race-engine` / `apps/web/src/app/race` / `apps/worker` / `packages/betting` / `packages/auth` /
DB スキーマ / マイグレーション。

---

## 2. 未コミット作業との境界

作業ツリーには**3 つの層**が同居しています。混ぜていません。

| 由来 | 内容 |
|---|---|
| ★着手前からあったもの | `apps/web/package.json` の `dev` ポート `3100 → 3210` |
| 前回の 3D スパイク | `packages/render-3d/` / `3d-pack-lab` / `api/spike-3d` / `tools/capture-3d-pack-lab.mjs` ほか |
| 今回のハイブリッド | 上の §1 |

commit も stash も reset も revert もしていません。push もしていません。
コミットする場合は、ポート変更を今回の変更へ混ぜないよう部分ステージングが要ります
（前回のレビュー §「現作業ツリーの扱い」と同じ）。

---

## 3. 足元アーティファクトの原因と証拠（実装前ゲート §2）

### 症状

C2 相当・4.0 秒・12 頭の画面で、芝の上に馬体から切り離された黒い板・破片。
★どこが破片かも目で決めていません。**暗い画素の連結成分**のうち、
いちばん大きい成分（＝馬群）の下半分にある小さく孤立した塊を破片としました。

- 破片 **15 個 / 662 画素**（外接矩形は `artifact-findings.json` の `symptom.fragmentBoxes`）

### 影か本体か

影を切っても 662 画素のうち変わるのは **119 画素**だけ。→ 大半は**実体のあるメッシュ**。

### 原因

27 メッシュを 1 つずつ消し、破片の画素がどれだけ動くかを数えました。

| メッシュ | 破片画素の変化 | 画面全体の変化 |
|---|---:|---:|
| `SM_Horseshoes_R_F` | 361 | 734 |
| `SM_Horseshoes_L_B` | 131 | 445 |
| `SM_Horseshoes_R_B` | 60 | 534 |
| `SM_Horseshoes_L_F` | 46 | 721 |

**蹄鉄 4 個で 598 / 662 画素（90%）**。分類は **Skinning**。

裏づけ（`tools/inspect-glb.mjs` で GLB を直接読んだ実測）:

```text
SM_Horseshoes_L_B  skin=なし  SM_Horseshoes_L_B < Armature
SM_Horseshoes_L_F  skin=なし  SM_Horseshoes_L_F < Armature
SM_Horseshoes_R_B  skin=なし  SM_Horseshoes_R_B < Armature
SM_Horseshoes_R_F  skin=なし  SM_Horseshoes_R_F < Armature
```

蹄の骨ではなく `Armature` の直下にあり、走行アニメが骨を動かしても追従しません。

- Root Motion ではありません（水平成分は読み込み時に止めてあり、馬体は走路どおりに出ています）
- Z-fighting でもありません（面の重なりではなく、芝の上に単独で浮いた塊です）

### 採用した処置

**蹄鉄 4 個を表示しない。** ★背景・芝・ブラー・モーションブラーでは隠していません。

### ★先に試して、測って取り下げた案

蹄の骨（`DEF-f_hoof.L/R`・`DEF-r_hoof.L/R`）へ**付け直す**案を先に実装しました。
付け直し自体は成功しています（`reparentedParts: 48` / `reparentMissed: 0`）。
しかし **静止姿勢の時点で、蹄鉄の形の中心が蹄の骨から 0.44〜1.20m 離れて**いました
（`capture-3d-pack-lab.mjs --hoof` の実測）。骨に付けると、その距離を半径にして振り回されます。

| | 付け直し前 | 付け直し後 |
|---|---:|---:|
| 破片 | 15 個 / 662 画素 | **20 個 / 4,300 画素** |
| 接地の持ち上げ | 0.008m | **0.943m**（馬が 1m 近く浮く） |

→ 取り下げました。

### 結果

| | 前 | 後 |
|---|---:|---:|
| 破片 | 15 個 / 662 画素 | **1 個 / 45 画素**（−93%） |
| 残りの正体 | — | 影を切ると 45/45 が消える＝**正しい影** |
| 副産物: 描画呼び出し | 764 | 668 |
| 副産物: 三角形 | 1,032,746 | 1,019,498 |

### ★失うもの

蹄鉄 4 個（1 頭あたり）。C2 の画角では 1m ≒ 151px なので、14cm の蹄鉄は約 21px。
★ただし出していたときは蹄から 0.44〜1.20m ずれた位置にあり、
「蹄の下の縁」としては**機能していませんでした**。
正しく直すには、蹄鉄が蹄の骨に skin されているモデルが要ります（§16 へ）。

### ★この過程で見つけた、前回報告書の誤り

名前の解決で**二度**外しました。どちらも例外を投げません。

1. `getObjectByName('DEF-f_hoof.L')` → 静かに `undefined`（`reparentedParts: 0`）
2. `.` を `_` に置換して再挑戦 → こんども全滅（`reparentMissed: 48` ＝ 12 頭 × 4 個）

three の GLTFLoader は節名を `PropertyBinding.sanitizeNodeName` に通し、
`.` を **`_` に置き換えるのではなく削除**します（`DEF-f_hoof.L` → `DEF-f_hoofL`）。

★**同じ理由で首の骨 `DEF-neck.003` も取れていませんでした**（`neckMissed: 12`）。
`REPORT_P4_3D_PACK_SPIKE_20260823.md` §6 に

> 首 … 位相を 0.12 遅らせた正弦（`neckAngleOf`）／脚と完全同期しない

と書きましたが、**実際には効いていませんでした。訂正します。**
今回の修正で効くようになりました（`neckMissed: 0`）。
成功数と失敗数の両方を数えるようにしたので、今後は黙って素通りしません。

---

## 4. 2D背景・3D馬・2D前景の構造

```text
最前面: 2D 前景（手前ラチ）        drawBroadcastV2Scene(..., layer: 'foreground')   z-index 3
中央  : 3D 馬群（背景が透明の WebGL）                                              z-index 2
背面  : 2D 背景（芝・奥ラチ・スタンド・観客・空） layer: 'background'               z-index 1
```

- ★世界の描画を**ツール側へ写していません**。既存の `drawBroadcastV2Scene` を
  層を指定して 2 回呼ぶだけです。ここにあるのは**素材の読み込みだけ**です。
- 背景と前景は 8 秒間**毎コマ描き直します**。1 枚のスクリーンショットで固定していません。
- 3D は `alpha: true` ＋ クリア透明度 0。

### `packages/render` への最小分離

| 追加 | 既定 |
|---|---|
| `ParallaxLayer.front?: boolean` | 未指定。★既定の描画では順序も見た目も変わらない |
| `drawParallaxPlate(..., pass)` | `'all'`（従来どおり） |
| `drawBroadcastV2Scene(..., { layer })` | `'all'`（従来どおり） |

★通常経路が 1 呼び出しも変わらないことを、**描画の呼び出しを 1 つずつ記録して**確かめています
（`packages/render/test/scene-layers.test.ts` ★①）。

⚠️ ★透視ワールド（`texturedWorld`）を使うカットでは層分けに対応していません。
前景だけを描こうとすると世界まで塗ってしまうため、**その組み合わせは例外で止めます**。
今回の側面カットはパララックス経路なので影響ありません。

### 手前に置いた層

`front-rail` のみ。★芝（`turf-near`）は**背景のまま**です。
馬は芝の上に立つので、手前の芝を馬の前へ出すと**足が芝に隠れます**。

---

## 5. カメラ投影の合わせ方と誤差

### 合わせ方

★見た目で合わせていません。**3D のカメラを 2D の `scene.camera` から作ります。**

```text
2D: eye/target/fovY（x,y が地面・z が上）
      ↓ 読み替えるだけ
3D: position/lookAt/fov（x,z が地面・y が上）   z = −y
```

### 実測（`projection-proof.json`）

同じ (s, w) を 2D の `project()` と 3D のカメラで画面へ落とし、px で比べました。

| | 実測 | 合格値 |
|---|---:|---|
| 基準点の数 | 8 点 × 3 時刻 | 8 点以上 |
| 中央誤差 | **0.000px** | 3px 以下 |
| 最大誤差 | **0.000px** | 8px 以下 |
| 地平線の縦ずれ | **0.000px** | 5px 以下 |
| 蹄の接地点のずれ | **0.000px** | 3px 以下 |

★近似ではなく**厳密一致**です。両者が同じピンホールのモデルで、同じカメラの値を使うためです。
時刻は 0 秒・4.0 秒・7.9 秒（最終フレーム）。

### ★途中で外したこと

**① 手の向きの取り違え。** 2D の y をそのまま 3D の z に入れたら**鏡像**になりました。
実測: 縦は完全一致（623.98 = 623.98）なのに、横だけが画面中央 960px を軸に反転
（919.37 ↔ 1000.63 / 1448.41 ↔ 471.59）。★**中央誤差 977px・最大 11,229px。**
目で見れば「なんとなく合っていない」で済んだところです。`z = −y` に直しました。

**② 地平線の測り方。** 3D 側の地平線を「走路を 4000m 先へ進んだ点」で取り、**109px ずれ**ました。
走路は楕円なので、4000m 先は正面ではなく回り込んだ場所です。地平線ではありません。
カメラの視線の**水平成分**の無限遠へ直しました。

---

## 6. 前景遮蔽の証拠

### 測り方

★撮った絵の差ではなく、**3D の画布のアルファと前景の画布のアルファの重なり**を数えます。

⚠️ ★はじめは「前景あり／なしで撮った 2 枚の差」を数え、**7,999 画素が「隠れた」**と出ました。
色分けして見ると、その正体は**馬の輪郭のアンチエイリアスの差**でした
（ブラウザが画布を 1 枚増やすと、下の層の合成がわずかに変わる）。測り方を変えました。

### そのままのカット — ★遮蔽は起きません

| | 実測 |
|---|---:|
| 隠された馬の画素 | **0**（32 標本すべて） |
| 馬の画素 | 513,664 |

理由（測って確かめたこと）:
パララックスの層の縦位置は**プレートの切り取り**（`zoom` / `verticalAnchor`）で決まり、
**カメラの高さと独立**です。手前ラチは画面下端（y ≒ 846〜1080）に出て、
馬（足元 y ≒ 700）に届きません。カメラを 1m・2m・3m 下げても 0px のままでした。

### 層の前後そのもの — ★効いています

実際の素材・実際の前景の描画経路のまま、**位置だけを 260px 上へずらした測定専用の設定**:

| | 実測 |
|---|---:|
| 隠された馬の画素 | **101,421 / 516,151（19.6%）** |
| 外接矩形 | `foreground-proof.json` の `orderingProbe.occludedBox` |

- ラチより奥の脚は隠れ、ラチより手前へ出るべき部分は消えていません
- マスクの縁に白・黒のハローはありません（`foreground-mask-shifted.png`）
- 0 秒・最も隠れる時刻・最終フレームの 3 時点で整合します（`atThreeTimes`）

★このカットを提出物のまま進めてよいか、
「手前ラチが馬をまたぐカット」を別に用意すべきかは、レビュー側の判断だと考えます（照会に記載）。

---

## 7. 影方式 S0 / S1 / S2 の比較

| | S0 影なし | S1 3D リアルタイム影 | S2 軽量影 |
|---|---|---|---|
| 作り | 技術対照 | `ShadowMaterial` の面に影だけ合成 | 接地点の楕円・走行位相で少し潰れる |
| 描画呼び出し | 249 | 262 | 261 |
| 三角形 | 510,288 | 630,602 | 510,312 |
| 影を落とすメッシュ | 0 | 12 | 0 |
| 60 コマの平均 fps | 61.9 | 61.9 | 61.3 |
| p95 | 17.0ms | 17.0ms | 16.9ms |
| 8 秒 240 コマの WebGL 時間 | 5.88ms | **11.34ms** | 未測定 |

- ★このカットでは**値段の差が fps に出ません**（どれも 60fps 上限に張り付く）。
  差が出るのは **WebGL の 1 コマ時間**で、S1 は S0 の約 1.9 倍です。
- 見た目は `shadow-comparison.png`。★このカットは馬体が地面をほぼ隠すため、影の差は小さく出ます。

**H3 では S1 を採用しました。** 形に追従する影が得られ、デスクトップでは費用が
p95 に現れないためです。★ただし採否はレビュー側の判断で、S2 へ替える余地は残しています
（S2 は三角形が S0 と同じで、モバイルの逃げ道になります）。

---

## 8. 資源共有と最適化前後

### 何を測ったか

名前ではなく **UUID の実体**で数えました（`resource-sharing.json`）。

| | 前 | 後 | 目標 |
|---|---:|---:|---|
| 描画呼び出し | 674 | **262** | −30% 以上 → **−61%** |
| 三角形（影あり） | 1,032,746 | **630,602** | −25% 以上 → **−38%** |
| テクスチャ（GPU） | 206 | **26** | −30% 以上 → **−87%** |
| ジオメトリ（GPU） | 102 | **21** | — |
| 形の実体 | 102 種 | 21 種（1 つを 11.5 か所で使用） | — |
| 材質の実体 | 101 種 | 25 種（1 つを 9.6 か所で使用） | — |
| 絵の実体 | 7 種 | 7 種（1 つを 75 か所で使用） | — |
| 影を落とすメッシュ | 336 | **12** | — |

### 何が起きていたか（内訳を数えて分かったこと）

1. **騎手を 1 頭ごとに `new` していた。** カプセル 11 個 × 12 頭で、形も材質も別物。
   → カプセルを 3 つに束ね（勝負服の色・肌・黒）、形は module に 1 つ、材質は色ごとに 1 つ。
2. **全 337 メッシュが影を落としていた。** 描画呼び出し 674 ＝ 337 × 2 パス。
   → 影を落とすのは**馬体だけ**に限定（`SHADOW_CASTERS`）。
3. **★テクスチャ 206 の内訳が分からなかった。** 材質が持つ絵は 7 種しかない。
   数え直すと ★**192 枚が骨の姿勢テクスチャ**でした（12 頭 × 16 スキン付きメッシュ）。
   `SkeletonUtils.clone` はメッシュごとに骨組みを作るため、同じ骨を指していても別々に確保されます。
   → 同じ骨の並びを使うメッシュで**骨組みを共有**。スキン付きメッシュ 192 に対し、骨テクスチャは 192 → **12 枚**。
4. 毛色・たてがみの材質を頭数ぶん複製していた → **色ごとに 1 つ**（毛色は 5 種類しかない）。

### 読み込み時間

| | 実測 |
|---|---:|
| 全体 | 7.8〜9.7 秒 |
| うちパララックス素材 | 4.2〜6.0 秒 |
| うち GLB（取得・Draco 展開・12 頭複製） | 1.8〜3.3 秒 |

★**目標の 4 秒以下に届いていません。** 支配しているのは 3D ではなく**2D の素材読み込み**です
（開発サーバーが毎回 PNG を配るため）。本番では配信と圧縮が変わるので、
この数字のまま本番を語れません。★測り直しが要る項目として残します。

---

## 9. デスクトップ性能

### 環境

| 項目 | 値 |
|---|---|
| ブラウザ | Microsoft Edge 151.0.4129.101 |
| GPU | `ANGLE (Intel, Intel(R) Iris(R) Xe Graphics (0x000046A8) Direct3D11 vs_5_0 ps_5_0, D3D11)` |
| WebGL2 | はい |
| 解像度 | 1920×1080 / `devicePixelRatio` 固定 1 |
| 実行 | 各条件 3 回。表は**中央値**。生のコマ時間は各 JSON の `rawFrameTimesMs` |

⚠️ ★ヘッドレスで測っていません。ヘッドレスだと WebGL が CPU 実装へ落ちることがあり、
実機より大幅に遅い数字を「測定結果」として出してしまいます。

### 中央値

| 条件 | 平均 fps | p50 | p95 | 最大 | 30fps 未満 | 2D 背景 | WebGL | 2D 前景 | ヒープ |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| **H3 12 頭・影 S1** | **58.6** | 16.7ms | **19.4ms** | 21.4ms | **0.0%** | 1.56ms | 11.34ms | 0.16ms | 115MB |
| H3 12 頭・影なし | 60.1 | 16.7ms | 17.0ms | 17.4ms | 0.0% | 1.08ms | 5.88ms | 0.08ms | 114MB |
| H3 24 頭・影 S1 | 59.5 | 16.7ms | 19.4ms | 23.8ms | 0.0% | 1.13ms | 13.77ms | 0.14ms | 121MB |
| 2D 背景だけ | 60.0 | 16.7ms | 16.9ms | 17.3ms | 0.0% | 0.63ms | 0.54ms | 0.01ms | 85MB |
| 3D 馬だけ（H0） | 60.1 | 16.7ms | 16.9ms | 17.3ms | 0.0% | 0.03ms | 3.55ms | 0.02ms | 87MB |

### 合格基準との対照

| 基準 | 実測 |
|---|---|
| 12 頭・採用影で平均 30fps 以上 | **58.6fps** |
| p95 50ms 未満 | **19.4ms** |
| 30fps 未満 5% 未満 | **0.0%** |

★合成時間は単独で測れません（ブラウザが 3 枚の画布を合成するため）。
代わりに**層ごとの描画時間**と**コマ全体の時間**を並べています。
コマ全体の p50 16.7ms は `requestAnimationFrame` の 60fps 上限に張り付いた値です。

★24 頭でも 12 頭とほぼ同じなのは、この構成では**上限に張り付いたまま**だからです。
差は WebGL の 1 コマ時間に出ています（11.34ms → 13.77ms）。

---

## 10. モバイル性能

★**未測定です。数字を作っていません。**

指示書 §15-5 の停止条件に当たるため、`QUESTIONS_P4_HYBRID_PACK_SPIKE_20260823.md` で照会しています。
他の成果物はすべて完成させたうえで、この 1 点だけを保留にしています。

### 実機の代わりにはならない参考値

`throttled-cpu4x-844x390@2.json`

| 条件 | 実測 |
|---|---|
| CPU 1/4・844×390・dPR 2（実描画 1688×780） | 平均 **18.8fps** / p95 **111.4ms** / 30fps 未満 **97.1%** |

⚠️ ★**これは実機の測定ではありません。** デスクトップの Chromium で CPU を絞り、
画面を電話の大きさにしただけです。**GPU は Intel Iris Xe のまま**で、
発熱・電池・メモリ制約は再現していません。
「これでモバイルは無理」とも「大丈夫」とも言えません。実機の数字が要ります。

---

## 11. 決定論とテスト

### 決定論（`determinism-proof.json`）

同じ時刻を 2 回描き、間に別の時刻を挟んで、**画素の SHA-256** を比べました。

| 時刻 | 結果 |
|---|---|
| 0 秒 | 一致 |
| 4.0 秒 | 一致 |
| 7.9 秒 | 一致 |
| 対照: 0 秒 と 4.0 秒 | ★違う（「そもそも動いていない」ではない） |

### ★ここで見つけた不具合

最初の測定では **0 秒だけ一致しませんでした**（違う画素 22,442・4.0 秒でも 9,649）。
差分を絵にすると、★**馬の頭と首に集中**していました。

原因は `neck.rotation.z += neckAngleOf(...)`。
`mixer.update(0)` がこの骨を毎回書き戻すとは限らず、**足した角が積み上がって**いました。
→ 前のコマで足したぶんを**戻してから**足す形に直しました。

⚠️ ★これは §3 の名前解決の修正で**首が効くようになった直後に生まれた**不具合です。
前回まで首は動いていなかったので、積み上がりも起きませんでした。
★**動くようにしたら別の壊れ方をした**という順序を、そのまま記録します。
不具合を含んだまま撮っていた 4 本の動画は撮り直しました。

### テスト（`packages/render-3d/test/hybrid.test.ts` ほか）

```text
npm test          … Test Files 113 passed (113) / Tests 1128 passed (1128)
npm run typecheck … エラーなし（ルート + apps/web）
npm run build:web … 成功
```

⚠️ ★`npm run build:web` は動いている開発サーバーの `.next` を上書きします。
   検証中に流すと**開発サーバーが 500 や無応答になります**（この作業中に 2 回起こしました）。
   撮影のあとに流すか、流したら開発サーバーを再起動してください。

指示書 §12 の 14 項目:

| # | 内容 | 置き場所 |
|---|---|---|
| ① | 同一 seed・時刻で 3 層とも 1 画素も変わらない | `hybrid.test.ts` ★① |
| ② | 2D と 3D が同じ `s/w`・同じ注視時刻を使う | `hybrid.test.ts` ★② |
| ③ | 投影の中央 3px 以下・最大 8px 以下 | `hybrid.test.ts` ★③ |
| ④ | 地平線ずれ 5px 以下 | `hybrid.test.ts` ★④ |
| ⑤ | 蹄接地点ずれ 3px 以下 | `hybrid.test.ts` ★⑤ |
| ⑥ | 前景マスクが馬より前に合成される | `hybrid.test.ts` ★⑥（重ね順と実測の 2 本） |
| ⑦ | 層の順序を壊すとテストが落ちる | 下の R-16 で確認 |
| ⑧ | 通常 `/race` からハイブリッドが参照されない | `spike-guard.test.ts` ★② |
| ⑨ | 本番で検証ページとモデル API が 404 | `hybrid.test.ts` ★⑨ |
| ⑩ | `Date.now()` / `Math.random()` 非依存 | `hybrid.test.ts` ★⑩（2 本） |
| ⑪ | 原因メッシュの処置が全頭へ適用される | `hybrid.test.ts` ★⑪ |
| ⑫ | 非表示メッシュが影を落とさない | `hybrid.test.ts` ★⑫ |
| ⑬ | 形・絵・材質の共有数が測定 JSON と一致 | `hybrid.test.ts` ★⑬ |
| ⑭ | H0 が 3D 単体の対照になっている | `hybrid.test.ts` ★⑭ |

### ★R-16: 壊したときに落ちることの確認

| 壊した箇所 | 結果 |
|---|---|
| 層の重ね順（前景の z-index を 3 → 0） | ★⑥ が落ちた |
| 影を落とすメッシュの限定を外す | ★⑫ が落ちた |
| `front` 印の層を背景でも描く | `scene-layers.test.ts` ★②★③ が落ちた |
| 背景の層でも馬を描く | `scene-layers.test.ts` の 3 件が落ちた |
| `/race` から検証ページを参照する | `spike-guard.test.ts` ★② が落ちた |
| `phaseOffset` を 0 に潰す | `race-scene.test.ts` ★③ の 2 件が落ちた |

すべて確認後に復元しています。

⚠️ ★JSON を読む判定は、**ファイルが無ければ落とします**（飛ばしません）。
「測っていない」を「合格」と読み替えないためです。

---

## 12. 成果物と再生成方法

`out/hybrid-pack-spike/`（詳細は同ディレクトリの `README.txt`）

| ファイル | 内容 |
|---|---|
| `h0-3d-baseline.mp4` | H0: 3D 馬群のみ（影 S0）。回帰対照 |
| `h1-background.mp4` | H1: 2D 背景 ＋ 3D 馬（影 S0） |
| `h2-foreground.mp4` | H2: H1 ＋ 2D 前景（影 S0） |
| `h3-optimized.mp4` | H3: H2 ＋ 採用した影 S1。提出候補 |
| `h0..h3-contact-sheet.png` | 各変種 1 秒ごと 8 枚 |
| `compare-reference-2d-3d-hybrid.png` | REFERENCE / 2D V3 / 3D C2 / HYBRID H3 × 5 時点 |
| `shadow-comparison.png` / `.json` | 影 3 案 |
| `projection-proof.png` / `.json` | 投影のずれ |
| `foreground-*.png` / `foreground-proof.json` | 前景の遮蔽（そのまま／ずらし） |
| `resource-sharing.json` | 資源の共有 |
| `tone-stats.json` | 色調 |
| `determinism-proof.json` | 決定論 |
| `performance-*.json` | デスクトップ性能（3 回の中央値・生データつき） |
| `throttled-cpu4x-*.json` | ★実機ではない参考値 |
| `scene-measurements.json` | 上をまとめたもの |
| `diagnostics/` | 足元アーティファクトの原因調査 |

再生成の手順は `README.txt` に全コマンドを書いています。

---

## 13. 観察できた事実

★測った値と、撮れた絵に写っていたものだけです。

1. **2D 世界の中に 3D 馬群が、投影誤差 0.000px で入る。**
   3D のカメラを 2D のカメラから作るので、近似ではなく厳密一致です。
2. **接地が合う。** 蹄の接地点と 2D の地面のずれは 0.000px。
   走行アニメを 1 周ぶん動かして最も低くなる瞬間で高さを決めています。
3. **層の前後は効く。** 前景を 260px ずらした測定で、馬の 19.6% が正しく隠れました。
   ★ただし**そのままのカットには遮蔽の瞬間がありません**（構造上・実測 0px）。
4. **足元の破片は原因まで特定して消えた。** 15 個 662 画素 → 1 個 45 画素。
   残る 1 個は影を切ると全部消えるので、正しい影です。
5. **資源は目標を超えて減った。** 描画呼び出し −61% / 三角形 −38% / テクスチャ −87%。
   ★内訳を数えるまで、テクスチャ 206 のうち 192 が骨の姿勢テクスチャだと分かりませんでした。
6. **デスクトップは基準を満たす。** 12 頭・影ありで 58.6fps / p95 19.4ms / 30fps 未満 0.0%。
   24 頭でも p95 は同じで、差は WebGL の 1 コマ時間（11.34 → 13.77ms）に出ます。
7. **層の値段が分かれた。** 2D 背景 1.1〜1.6ms / WebGL 5.9〜13.8ms / 2D 前景 0.1〜0.2ms。
   ★重いのは 3D で、2D 世界の描画は 1 コマ 1.6ms 以下です。
8. **★読み込みは 2D 素材が支配している。** 全体 7.8〜9.7 秒のうち、
   パララックス素材が 4.2〜6.0 秒、GLB が 1.8〜3.3 秒。目標の 4 秒に届いていません。
9. **色調は既に合っている。** 世界ぜんたいの明るさ分位 p05 0 / p50 40 / p95 109 に対し、
   馬は p05 4 / p50 19 / **p95 110**。いちばん明るいところが一致しています。
10. **★比較シートで見ると、2D V3 の馬は H3 の馬より大きく写ります**（同じカメラなのに）。
    ★これは絵を見ての記述で、数値では詰めていません。
    関連する実測として、購入モデルの馬体は**き甲 2.06m**です（実馬の目安 1.6m 前後）。
    3D 側がむしろ大きいので、2D のスプライトはさらに大きく描かれていることになります。
11. **参考映像は「寄り」である。** 参考の 50〜58 秒は 2〜3 頭しか画面に入っていません。
    H3 は 12 頭が入る画角で、参考より引いています。

---

## 14. 開発側の推測（★事実ではありません）

⚠️ ここから下は**測っていないこと**です。事実と混ぜないでください。

- §13-7 から、**この方式の重さは 3D 側にほぼ全部ある**のではないか。
  2D 世界を足しても 1 コマ 1.6ms しか増えないので、
  「世界を 2D のまま使う」判断は性能面では有利に働いているように見えます。
- §13-8 から、**本番の読み込み時間は開発サーバーの値とかなり違う**のではないか。
  素材の配信・圧縮・キャッシュが変われば 4 秒は届く可能性があります。ただし未測定です。
- §13-10 から、**2D と 3D の馬の大きさが合っていない**可能性があります。
  合っていないなら、カットを切り替えたときに馬の大きさが跳びます。
  ★次段階で数値化する価値があると思いますが、今回は測っていません。
- §6 から、**手前ラチが馬をまたぐカットは、いまの側面カットとは別に要る**のではないか。
  4 角や直線入口のように、ラチが画面を横切るカットなら自然に起きます。
- モバイルについては、`throttled` の値（18.8fps）は CPU だけを絞った下限なので、
  実機の GPU 次第で大きく変わるはずです。**推測の域を出ません。**

---

## 15. 本実装へ進む場合の段階案

★見積もりであって約束ではありません。

| 段 | 内容 | 前提 | 概算 |
|---|---|---|---|
| A | モバイル実機で H3 を測る | 実機（照会中） | 0.5 日 |
| B | 馬の大きさを 2D と数値で突き合わせる | — | 1 日 |
| C | 手前ラチがまたぐカット（4 角・直線入口）でハイブリッドを検証 | 透視ワールドの層分け対応が要る | 3〜4 日 |
| D | 台本の各カットを 3D カメラへ移す | C の結果 | 2〜3 日 |
| E | 発走・追い出し・減速・勝馬のアニメーション | ★素材次第（§16） | 不明 |
| F | 騎手の差し替え | ★素材次第（§16） | 不明 |
| G | 通常 `/race` への段階導入 | A〜F | 3〜5 日 |

★そのまま残せるもの: レースエンジン、コース幾何、時間伸縮、実況・ナレーター、HUD、枠色の規則。

---

## 16. 騎手と追加アニメーションの要件

★今回は**要件をまとめるだけ**にし、制作・購入へは広げていません（指示書 §14）。

### 騎手

いまは**カプセルと球の簡易騎手**です（購入モデルに騎手が入っていないため）。
今回は形を 3 つに束ねて資源を共有させましたが、**完成品質の判定には使えません**。

必須:

1. 商用利用可・改変可のライセンスが**書面で確認できる**こと
2. 馬モデルと**寸法・鞍位置が合う**こと（鞍の上端に腰が来る）
3. 前傾・追い出し・減速の 3 姿勢に対応できるリグ
4. 勝負服の色を差し替えられること（枠色 8 色）
5. 三角形数 **5 千以下**が望ましい

### 追加アニメーション

いまは `Gallop_RM` **1 本だけ**です。

必須: start / acceleration・gallop cruise・drive（追い出し）・deceleration・walk または winner follow。
継ぎ目が合っていること。

### 馬モデル（今回の実測から追加した項目）

1. ★**蹄鉄が蹄の骨に skin されている**こと。今回のモデルは `skin=なし` で `Armature` 直下にあり、
   静止姿勢でも蹄から 0.44〜1.20m ずれていました。**表示できません。**
2. ★**手綱・たてがみが走行アニメの骨に付いている**こと（同じ理由で出せていません）。
3. ★**マテリアル名が全メッシュに付いている**こと（空だと既定の白になり、`Braids` が白い編み目になりました）。
4. 単位はメートル、原点は蹄の接地点（今回はセンチで、原点が蹄から 30cm ずれ）。
5. 三角形数 1 頭 **2 万以下**が望ましい（今は 57,219）。
6. ★**節名に `.` を使わない**こと。three の GLTFLoader が削除するため、名前で引けなくなります。
   今回はこれで**二度**外しました。

---

## 17. スコープ外で発見した問題

★指示書の範囲外なので**直していません**。報告だけします。

1. **`/race` とハイブリッド検証で、パララックス素材の読み込みが二重にあります。**
   `apps/web/src/app/race/page.tsx` の中にインラインで書かれた読み込みと、
   `3d-hybrid-lab/hybrid-lab-client.tsx` の `loadPlate()` です。
   ★世界の**描画**は共有していますが（`drawBroadcastV2Scene`）、**素材の読み込み**は別々です。
   `/race` を変更しない制約のため、今回は統合していません。

2. **透視ワールド（`texturedWorld`）のカットは層分けに対応していません。**
   前景だけを描こうとすると世界まで塗るため、その組み合わせは例外で止めています。
   4 角のカットでハイブリッドを試すには、ここの対応が先に要ります。

3. **本番ビルドの一覧に `/3d-hybrid-lab` と `/3d-pack-lab` が行として出ます。**
   実行時は 404 ですが、経路の存在とチャンクは残ります。
   前回のレビューでも指摘済みで、スパイク終了時に削除する方針のままです。

4. **`@star/render-3d` が `node_modules/@star/` に貼られていません。**
   `npm install` を流していないためです。テストとツールは相対パスで読んでいます。

5. **読み込み時間の目標（4 秒）に届いていません。**
   支配しているのは 2D のパララックス素材で、開発サーバーの配信条件に強く依存します。
   本番相当の条件で測り直す必要があります。

---

## 完了条件の確認

| 指示書の要求 | 状態 |
|---|---|
| §2 足元アーティファクトの原因確定と除去 | 完了（隠していません） |
| §3 2D 背景・3D 馬・2D 前景の 3 層 | 完了 |
| §4 投影誤差の測定 | 完了（すべて 0.000px） |
| §5 影 S0/S1/S2 の比較 | 完了 |
| §6 資源共有と最適化 | 完了（3 項目とも目標超過） |
| §8 H0〜H3 の 4 変種 | 完了 |
| §9 前景遮蔽の証拠 | 完了（そのままは 0px という事実つき） |
| §10 デスクトップ性能 | 完了（基準を満たす） |
| §10 モバイル実機 | ★**未測定**。照会中 |
| §11 成果物 | `out/hybrid-pack-spike/` |
| §12 自動テスト | 完了（壊したら落ちることも確認） |
| §16 報告書 | 本書 |
| 通常 `/race`・DB・worker・認証に触れない | 触れていません |
| 既存の未コミット変更を保持 | 保持しています |
| commit・push しない | していません |
| 採否の判定 | ★していません |
