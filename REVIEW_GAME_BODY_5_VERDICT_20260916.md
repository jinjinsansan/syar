# 裁定 — 照会 `QUESTIONS_GAME_BODY_5_20260916.md`（Q-GB5-1・Q-GB4-1・Q-GB4-2）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側・オーナー
**対象**: 照会 `QUESTIONS_GAME_BODY_5_20260916.md` ／ 報告 `REPORT_GAME_BODY_4_20260916.md`・`REPORT_GAME_BODY_5_20260916.md` ／ 開発側 HEAD `5c248c2`（push 済み）
**関連**: 正典 D-055・D-056・D-037・§9.1・§10.4・§15.3・D-103・D-109・D-110・§18

> 凡例（R-13）: **✔** ＝ レビュー側がコードを読んで確かめた ／ **△** ＝ 未確認（報告の記載に基づく）
> 本書は正典を編集しません（★D-103 の**文面の訂正**だけは本日レビュー側が正典に入れます・§5）。

---

## 0. 判定

| # | 事項 | 判定 |
|---|---|---|
| **Q-GB5-1** 出走登録したレースが確定できない | 🔴 **事実を確認。★①も②も採らず、第三の形（③）で直す**（§1） |
| **Q-GB4-1** 先天個性を genotype の行にするか | ✅ **いまの形（既存の発現値から導く）のままでよい**（§2） |
| **Q-GB4-2** 暴走を率で引くか期待値か | ✅ **率で引く。ただし乱数の入れ方を 3 つ縛る**（§3） |
| 自分で見つけた誤り 3 件 | ✅ **すべて受理。報告に残した判断が正しい**（§4。★条件 1 件を追加） |
| 第 4 便・第 5 便の中身（着順に効かない） | ✅ **受理**（△ レビュー側は再実行していません） |

★**直さずに照会したのは正しい判断です。** ①と②のどちらを採るかで置き場所が変わり、間違えると D-055／D-056 で閉じた穴を開け直します。

---

## 1. 🔴 Q-GB5-1 — 出走登録したレースが確定できない

### 1-1. 事実（✔ レビュー側もコードで確認）

| # | 事実 | 出どころ |
|---|---|---|
| 1 | `enter_race` は `(race_id, horse_id, gate, weight, strategy, jockey_frozen)` だけを入れる。★**`entrant_snapshot` が列の並びに無い** | ✔ `db/migrations/0024_entry_rpc_and_story.sql` の `insert into race_entries …` |
| 2 | レース生成側は凍結を書いている | ✔ `apps/worker/src/pg-store.ts:212`（`entrant_snapshot` を含む一括挿入） |
| 3 | 確定側は**1 頭でも凍結が無ければ rollback して `UnfrozenRaceError`** → D-056 の中止・返還へ | ✔ `pg-store.ts:379-386` |
| 4 | staging の cycle 5682 が実際に確定できなかった（2/20 頭） | △ 報告 §4d-1 |
| 5 | 本番には `0024` を当てていない | △ 報告（★本番に実害が無いことは受理） |

→ ★**「利用者が 1 人でも出走登録すれば、そのレースが中止になり、他の客の馬券まで全額返還される」**という読みは正しいです。**公開後は毎日起きます。**

### 1-2. ★なぜ ① を採らないか

`enter_race`（SQL）の中で凍結を組むと、**凍結の作り方が TS（`pg-store.ts`）と SQL の 2 か所**になります。
- **D-052「条件は 1 か所で引く」・R-30 に反します。** `RaceEntrant` の 13 項目の形が変わるたびに両方を直すことになり、**いずれ必ず離れます**（この案件で 5 回起きた「同じ量を 2 か所で持って離れた」形）
- 開発側の見立て（「二重帳簿に近づく」）に同意します

### 1-3. ★なぜ ② だけでも足りないか

「発走の直前にワーカーが埋める」は**出どころが 1 か所**に保たれる点で正しいのですが、**埋め忘れ・埋める前に確定が走った場合、いまと同じ壊れ方（レースごと中止）に戻ります**。窓を別に担保する、という開発側の懸念のとおりです。

### 1-4. ★採る形（③）

| # | 中身 |
|---|---|
| 1 | **`enter_race` は「受付」までにする** — 枠の確保・料金の記帳・**凍結待ちの状態**で行を作る（状態列を足す）。★料金の記帳と行の作成は**同じトランザクション**のまま（§14.4） |
| 2 | ★**凍結はワーカーが、生成と同じ TS の関数で書く**（§15.3 の「複雑な処理はワーカー、クライアントは積むだけ」の形）。**出どころは 1 か所のまま** |
| 3 | ★**発走前に「凍結の無い出走馬」が残っていたら、その馬だけを取消（除外）にし、レースは止めない** |
| 4 | **D-056 はそのまま残す**（発走後・確定の時点で凍結が無ければ中止）。③は**その前段**を足すだけで、閉じた穴は開けない |
| 5 | **利用者への表示**: 取消になったら**理由を出す**（黙って消さない）。登録料と騎手の料金は**返す**（§9.1 の返還と同じ考え方。額の扱いは §6 の起票候補） |

★**3 の根拠**: **§9.1 に「取消・除外馬を含む馬券は全額返還（EP）」があり、実装も既にあります**（✔ `apps/worker/src/payout.ts:37-43・79-81`・`packages/betting/src/settle.ts:85`）。
→ **1 頭の不備で他の客の馬券まで消す（レースごと中止）のは、釣り合いが取れていません。** 取消なら**他の客の馬券は生きたまま**です。

### 1-5. 検査（この便の受け入れ条件）

| # | 内容 |
|---|---|
| 1 | ★**登録 → 凍結 → 確定まで通す**（入口だけ見ない）。★**これが今回の抜けの本体**です（実演が「登録が通る」までしか見ていなかった） |
| 2 | **変異**: ワーカーが凍結を書かない形にすると、**その馬だけ取消・レースは確定**になること（レースごと中止にならないこと） |
| 3 | **変異**: 発走前の取消の処理を外すと、確定が D-056 で止まること（安全網が生きていること） |
| 4 | 取消になった馬の**登録料・騎手の料金が返っている**こと（台帳で確認） |
| 5 | staging で **実際に登録 → 発走 → 確定**を通すこと（cycle 5682 の再現と、直った後の 1 本） |

---

## 2. Q-GB4-1 — 先天個性を genotype（§5.4）の行にするか

**✅ いまの形（既存の発現値から決定論で導く）のままでよい。**

| # | 理由 |
|---|---|
| 1 | **V-1・V-2a〜V-2e を取り直さずに済む**（新しい形質を遺伝の系に足さないため） |
| 2 | **既存の形質（道悪適性・気性・根性・賢さ）を通して自然に遺伝する** — 「泥巧者の仔が泥巧者になりやすい」は成立します |
| 3 | ★**独立の行にするのは「その特性だけを狙って配合したい」という遊びを入れると決めたとき**でよい。そのときは **§5.2 と §5.4 の両方**を更新する（片方だけにした D-015 の再発防止）＋ V-1・V-2 系を取り直す |

⚠️ **条件**: 導出の式は **1 か所**に置き、**画面・物語・レースがすべてそこから引く**こと（D-052）。★「泥巧者」の判定を画面側でもう一度書かないこと。

---

## 3. Q-GB4-2 — 「暴走」を率で引くか、期待値で載せるか

**✅ 率で引く。** D-110 ② の文面（「失敗の率を下げる」）どおりで、**§18 の物語・実況に「暴走した」という出来事が出せる**ことが決め手です。期待値だけでは出来事が作れません。

★**ただし乱数の入れ方を 3 つ縛ります。**

| # | 条件 |
|---|---|
| 1 | ★**シードは既存の系列から導く**（`deriveRng` の別ストリーム）。**新しい乱数源を作らない**（憲法 4・§8.6。`Math.random()` も時計も読まない） |
| 2 | ★**引きの結果を介入ログに残す**（§8.8「seed ＋ 介入ログ → 同じ着順」が保たれること）。残さないと再現の証明が崩れます |
| 3 | ★**V-13 の測り方を先に改める** — 同じ入力でも散るので、**多数回の平均で「仕掛けの巧拙が結果に出る」を測る**形にする。★測り方を決めてから効かせる便に入ること（測れないものを効かせない） |

---

## 4. 自分で見つけた誤り 3 件

| # | 中身 | 判定 |
|---|---|---|
| ① | 「格は天井を変えない」は誤りで、**故障の恒久ダメージ（§7.5・D-044）で天井は下がりえる**。言えるのは「**上げない**」だけ | ✅ **正しい訂正**。★**D-103 の文面をレビュー側が直します**（§5） |
| ② | `stableGradeOf` が値の無い格で**毎頭に警報**を出していた | ✅ 受理。**全体の検査で別ファイルが落ちて気づいた**という経緯も良い記録です |
| ③ | `tools/settle-races.mjs` が `epochMs` を渡しておらず、**確定しても物語が 1 行も書かれなかった**。純関数と偽 DB の検査は全部緑だった | ✅ 受理。★**この案件で繰り返している形**です（「実装した」と「経路に繋がっている」は別・D-037／R-28 の家族） |

### ★条件 GC-3（追加）

③ を受けて、**この便から次を守ってください**:

> ★**`tools/*.mjs` の「手で回す道具」も、引数の渡し忘れを検査で固定する。**
> 純関数と偽 DB の検査が緑でも、**道具が引数を渡していなければ何も起きません**。
> ★**構文木か実行で「道具が必要な引数を渡していること」を見る検査**を、物語の書き込み（§18）と確定の道具に置くこと。

---

## 5. 正典の訂正（本日レビュー側が入れます）

| 箇所 | 訂正 |
|---|---|
| **D-103 ①** | 「素質の上限（`potential`）を**変えない**」→ ★「**上げない**」。⚠️ **故障の恒久ダメージで天井は下がりえる**（§7.5・D-044）ので、格について言えるのは「上げない」だけである（開発側の自己申告・2026-09-16） |

---

## 6. 正典への起票候補（オーナーの承認後）

| 候補 | 中身 |
|---|---|
| **D 候補: 出走登録の凍結** | ①`enter_race` は受付まで（枠・料金・凍結待ちの状態）②**凍結はワーカーが生成と同じ関数で書く**（出どころは 1 か所）③**発走前に凍結の無い馬は取消（除外）にし、レースは止めない**（§9.1 の返還の経路に載せる）④D-056 は最後の安全網として残す ⑤取消の理由を利用者に出し、登録料・騎手の料金を返す |
| **D 候補: 暴走の乱数** | 既存の系列から導く・介入ログに残す・V-13 の測り方を多数回の平均に改める（§3） |
| D-109 の註記 | 先天個性は**既存の発現値から導く**形を既定とし、独立の遺伝の行にするのは「狙って配合する遊び」を入れるときだけ（§2） |
| §18 の註記（LR-9 候補） | ★**物語の書き込みは「手で回す道具」からも通ること**を検査で固定する（GC-3） |

---

## 7. 次の順番

| 順 | 中身 |
|---|---|
| **1** | 🔴 **Q-GB5-1 の直し（③）** — 利用者に害が出る唯一の項目。★**本番へ `0024` を当てる前に**必ず |
| 2 | **GC-1・GC-2**（第 1 便の報告の直し・まだ残っています）＋ **GC-3**（手で回す道具の検査） |
| 3 | **V-10（80 レース）の判定**（★較正定数は動かさない・Q-V10-3 の裁定を待つ） |
| 4 | 第 2 便の残り（§18 の物語・Discovery の見せ方） |
| 5 | 個性・騎手を**効かせる**便（V-4〜V-6・V-17・V-18・V-13・V-9a・V-8 を取り直す。★V-13 の測り方を先に決める） |

---

---

# 追記（2026-09-16 夜）— D-111 の実装（`38c234a`・`35b68e7`）の判定と、Q-GB5-2・Q-V10-3

**対象**: 開発側 HEAD `35b68e7`（push 済み）／ 報告 `REPORT_GAME_BODY_4/5/6_20260916.md`

## 8. 判定 — 🔴 **D-111 はまだ受理できません（条件 GE-1〜GE-3）**

開発側は「受け入れ条件 5 件すべて満たした」と報告していますが、★**そのうち 3 件は名目上緑で、実際の経路を通っていません。**

### 8-1. GE-1 🔴 確定側のクエリが取消の行を外していない

| | |
|---|---|
| 事実 | ✔ HEAD でも `apps/worker/src/pg-store.ts:354-356` は `select … from race_entries e where e.race_id = $1 order by e.gate` のまま。★**`scratched_at` の条件が無い** |
| 何が起きるか | 取消の行は `entrant_snapshot` が null のままなので、**`unfrozen > 0` が立ち、D-056 でレースごと中止**になる。★**直す前と同じ結末**（D-111 ③ が成立しない） |
| ⚠️ 紛らわしい点 | `:466-467` に `scratched_at is not null` を引くクエリがあるが、**これは払戻に渡すためのもので、確定の判断より後**。**確定の入口は素通し** |
| 直し | ★**`where e.race_id = $1 and e.scratched_at is null`** にする。あわせて**枠順・`fieldSize`・出走頭数が取消を除いた数**になることを確かめる |

### 8-2. GE-2 🔴 検査が `settleRace` を通っていない

- `apps/cli/test/entry-to-settle.test.ts` は `freezePendingEntries` を `fakeDb` で呼ぶだけ（✔ `:106`・`:118`）。★**`settleRace` を一度も呼びません**
- だから「★② 取消 2 頭でもレースは確定できる」が、**確定の SQL を通らずに緑**になっています
- 直し: ★**取消 1 頭を含むレースが `settled` になること**を、**実際の `settleRace`**（偽の DB でよいが、**あの SQL を通る形**）で見る

### 8-3. GE-3 🔴 staging の実演が、肝心の経路を通っていない

- 報告 §4e-1 の行 ③ は ★**「凍結 2 頭・取消 0 頭」**。→ ★**取消が一度も起きていません**
- 通ったのは **②→④（凍結の前は中止・後は確定）**＝受け入れ条件の **① と ③**。★**条件 ②（取消 1 頭でもレースは確定）は未実演**
- 直し: ★**取消を 1 頭作って通す**（例: 馬の行を一時的に引けない状態にする）。**取消 1 頭・レースは `settled`** を実演する

### 8-4. ★2 回続けて同じ形です（記録）

今日の Q-GB5-1 と、本日の GE-1〜GE-3 は**同じ形**です — ★**入口は確かめ、出口を確かめていない**。
★**「受け入れ条件を満たした」と書けてしまう構造**そのものが問題なので、**次からは「条件ごとに、どの検査のどの行が・どの経路を通ったか」を報告に書いてください**（R-13 の具体化）。

### 8-5. 受理するもの

| # | |
|---|---|
| 1 | ★**2 つ目の穴（`settleRace` が取消の枠を `settlePayouts` に渡していなかった）の自己申告と修正は正しい**（✔ `pg-store.ts:466-470`）。渡さなければ**取消馬を含む馬券が「外れ」になり、客の EP が返らない** |
| 2 | 凍結が**生成と同じ `toEntrant`** を通る（✔ `entry-freeze.ts:111`）・**乱数は既存の系列から**（`deriveRng(cycle, FREEZE_STREAM, gate)`）・**時計を読まない** |
| 3 | ★**`main.ts:145` で確定より前に呼ばれている**（「実装したが呼ばれていない」ではない・D-037／R-28 の家族を回避） |
| 4 | `0028` は**列 2 つと CHECK（理由の無い取消を作れない）だけ**・RPC を再定義していない・返金は `dedupe_key` で冪等 |
| 5 | GC-1・GC-2（`5c5b1e8`）・GC-3／LR-9（`tool-wiring.test.ts`）の適用 |
| 6 | ★**誤り 10 件の自己申告**。とくに**検査自身の誤り 4 件を分けて書いた**こと。4 件目（自分のコメント「Math.random も読まない」が禁止語に引っかかった）は **D-108 ③ の家族**そのもので、記録として価値があります |

---

## 9. Q-GB5-2 — V-13 の σ の線（裁定）

| # | 裁定 |
|---|---|
| 1 | ★**σ の線は 3σ で確定**。V-13 は「巧拙が結果に出るか」という**存在の確認**なので、2σ では偶然で通る回が出ます |
| 2 | ★**効果量 0.03 は据え置き。両方を課す**（3σ **かつ** 差 0.03 以上）。SE だけだと**標本を増やせばどんな小さな差でも有意**になります（開発側の註記のとおり） |
| 3 | **`V13_MEASUREMENT` の置き場は `measurement.ts` のままでよい**。★ただし**正典 §13.2 に「3σ かつ 0.03」を書き、コードはその写し**とする（測定条件は正典に固定して値照合で守る・§16.3・R-12）。**起票はレビュー側が行います** |
| 4 | ✔ `standardError` が**不偏 SD** から出ていることを確認（`apps/cli/src/stats.ts:35-38`）。★既存の母標準偏差を流用していれば **SE を過小に報告**していたので、この分離は正しい直しです |
| 5 | ★**対標本（同じ馬・同じ条件で最適と早仕掛けを比べる）**にしたことも正しい。別々の馬の平均どうしを引くと**馬の差が混ざります** |

---

## 10. Q-V10-3 — `place` の −1.26pt（裁定: 据え置き。ただし理由が違います）

★**これは製品の払い不足ではなく、道具の測り方の問題です。**

| 事実 | 出どころ |
|---|---|
| `st.stake += table.size` — **売った目だけ**を賭け金に数える（D-096 のとおり） | ✔ `apps/cli/src/verify-pmin.ts:108` |
| `if (odds === undefined) st.unsold += 1` — ★**売っていない目が当たったとき、払戻を 1 円も足さない** | ✔ 同 `:111` |

→ ★**取りこぼした当たりの分だけ、払戻率が下がります。** `place` は **1 レースの売り目が 13 しかなく**、未発売が **3.32%**（他は 0.1〜2.2%）なので、最も大きく出ます。ワイドは未発売 2.17% でも売り目 63 で −0.35pt に収まっています。
★**D-096 の下でその目は売られないので、客は買えません** — 買えない目の当たりを払戻から引くのは、**製品ではなく測定の誤り**です。

**指示**: ★**測り方を直して測り直す**（未発売の的中を判定から外すか、賭け金の側と揃える）。**較正定数（`margin`・`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`）は動かさない**。★**AF-2・BF-6 はそれまで開いたまま**です。

---

## 11. 次の順番（更新）

| 順 | 中身 |
|---|---|
| **1** | 🔴 **GE-1・GE-2・GE-3**（確定側のクエリ 1 行・検査を `settleRace` まで・staging で取消 1 頭を実演） |
| 2 | **V-10 の測り方の訂正**（§10）→ `place` を測り直す |
| 3 | ★**本番へ `0024`・`0028` を当てるのは 1 が通った後**（`0021`・`0022`・`0023` と新しいワーカーは先に進めてよい） |
| 4 | 第 6 便（個性と騎手を効かせる）— ★**§13.2 に 3σ を起票した後**に着手 |

---

---

# 追記 2（2026-09-16 夜）— GE-1〜GE-3 の受理と、引退の分岐の発見

**対象**: `08faed2`（GE の直し）・`1b956fa`（デザイナーの回答 3 件と D13-2）／ リモート HEAD `1b956fa`

## 12. 判定 — ✅ **GE-1〜GE-3 を受理**（3 回目で出口まで通りました）

| 条件 | 判定 | レビュー側が確かめたこと |
|---|---|---|
| **GE-1** | ✅ | ✔ `apps/worker/src/pg-store.ts:365` に **`and e.scratched_at is null`** |
| **GE-2** | ✅ | ✔ 検査が**本物の `settleRace` を通る**（`entry-to-settle.test.ts:23` で `createPgStore`）。★③ は「**取消 1 頭でも `settled`・確定は 18 頭・取消馬に着順なし**」／★④ は偽 DB に `honorScratch: false` を渡して **D-056 の中止が戻る**＝**製品から 1 行消したときに落ちる錨**／⑤ は**発行された SQL の文面**に条件があることを見る |
| **GE-3** | ✅ | ✔ `entry-freeze.test.ts`（引退の分岐）＋ staging cycle 5708（凍結 1・取消 1・理由つき・400 EP 返金 → `settled`・取消馬に着順なし・11/12 頭・取り残し 0） |
| ★レビュー側の再実行 | ✔ | `entry-to-settle`・`entry-freeze`・`entry-scratch-migration`・`tool-wiring` の **4 ファイル・20 件すべて合格** |

## 12-1. ★引退の分岐は、レビュー側の条件に入っていませんでした（発見の記録）

- ★**GE-3 は「取消を 1 頭作れ」としか書いておらず、作り方を指定していませんでした。** 開発側が最初に採った「`genotype` を null にする」は **`not null` で弾かれ**（★制約が正しく働いた）、**そもそも本番では起きない作り方**でした
- → 開発側が **「登録の後・発走の前に引退する」**（週送りで 260 週に達する／致命的な故障・§7.1・§7.5）という**実際に起こる経路**に差し替え、★**その過程で「引退した馬がそのまま走る」という別の穴**を見つけました
- ★**もし人工的な作り方のまま通していたら、本番では起きない経路で緑になり、引退の穴は残ったままでした。** これは開発側の手柄として記録します
- ★**レビュー側の反省**: **「変異を作れ」と指示するときは、「本番で起こる経路で作ること」まで書く。** 作り方を指定しない指示は、**人工的な作り方で緑になる余地**を残します（R-16 の家族）
- → **D-111 に ⑥ として起票しました**（本日・「登録の後・発走の前に引退した馬も取消の対象」）

## 12-2. 検査側で踏んだ 3 件（受理）

偽 DB が賞金の経路を知らず落ちた（★**確定が賞金まで通っている証拠**）／その分岐が 0 行を返した／`includes` の見分けが `left join` を含む別のクエリまで吸った。→ **SQL の先頭で見分ける形に統一**。★**実装を疑う前に検査自身を疑う**形で分けて記録したのは正しい作法です。

## 12-3. デザイナー便（`1b956fa`・D13-2 履歴書型の馬詳細）✅ 問題なし

| # | 確かめたこと |
|---|---|
| 1 | ★**`potential` は画面に届いていない**。実装側の 4 か所（`training/page.tsx:59`・`game-demo.ts:65`・`horse-resume-demo.ts:9`・`view.ts:9,11,39`）は**すべて「渡さない」と書いたコメント**で、`horse-resume.tsx` は 0 件（§5.5・§12.4・D-101） |
| 2 | ★**`discoveryStageOf(relevantRuns)` は回数だけを引数に取り、素質の値を受け取れない**（✔ `packages/sim-engine/src/discovery.ts:35`）。★**D-108 ③「入力の形で書く」を、禁止語ではなく引数の形で満たしている** — 本件で 2 回踏んだ罠を避けています |
| 3 | 段階ごとの文字は `unknown`・`hint`・`narrow` では**評価を返さない** |
| 4 | 個性は画面に直書きせず導出（D-109）／親密度の言葉は騎手の画面と共有（D-052）／★**着順に効くものは無い** |

## 12-4. 🔴 残っているのは V-10 の測り方だけです

`apps/cli/src/verify-pmin.ts:108-113` は**未修正**です（✔ HEAD で確認）。★**§10 は「裁定待ち」ではなく指示**です。`place` の −1.26pt が 80 レースで再現するのは、**同じ測り方で同じ偏りが出ている**だけで、製品の払い不足の証拠ではありません。

## 12-5. 本番への適用（更新）

★**`0024`・`0028` を当ててよい状態になりました。** 順番は `0021` → `0022` → V-20 → `0023` → **新しいワーカーの配備** → `0024` → `0028`。**配備の後の数周で「1 レースを作る周」が 1 周の 60% 以内**であることを確かめてください（ES-6 裁定 §3 の 5）。

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
