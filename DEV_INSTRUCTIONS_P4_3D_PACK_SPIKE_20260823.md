# DEV INSTRUCTIONS — P4「3D 馬群描画 8 秒技術検証」

日付: 2026-08-23  
発行: レビュー側 Codex  
対象ブランチ: `p4/race-30sec-cuts`  
前提判定: `REVIEW_P4_2D_LIMIT_TEST_VERDICT_20260823.md`  
優先度: 最優先

---

## 0. 結論と目的

2D限界テストの判定は **FAIL**。現行2Dスプライト方式を追加調整して参考映像の完全再現を目指す作業は終了する。

次に行うのは完成版3Dレースの実装ではない。既存レースの位置データを、ブラウザ上の小さな3Dシーンへ接続し、次の4点だけを判断できる証拠を作る。

1. 12頭の馬が同じ3D空間内に存在し、自然な遮蔽と奥行きを作れるか
2. 馬ごとの走行位相をずらして、同期した複製物に見える問題を軽減できるか
3. 1920×1080・30fps相当でWeb実装として成立する性能があるか
4. 既存のレースエンジン、コース座標、演出台本を維持したまま、描画層だけを置換できるか

**このスパイクの目的は「3Dなら必ず参考と同じになる」と証明することではない。3D方式へ本投資する価値と接続可能性を判定することである。**

---

## 1. 厳守事項

1. `STAR_SPEC_v2.0.md`、`CLAUDE.md`、前提レビューを最初から最後まで読む。
2. 実在の馬名、競馬場名、レース名、人物名、他社固有名称を新規コード、変数、コメント、画面、成果物へ転記しない。
3. 参考動画は構図、動き、カメラ、密集感の観察にだけ用いる。
4. DB、Supabase、worker、認証、ポイント、投票、公正性、レース結果を変更しない。
5. `.env`、`secrets.*.env`、`git.txt`を読まない。
6. push、デプロイ、外部公開をしない。
7. 既存の未コミット変更を保持し、コミットへ混ぜない。
8. 通常の `/race` へ3Dを組み込まない。検証経路を完全に隔離する。
9. 現行2Dレースの削除、置換、リファクタリングを行わない。
10. 外部素材を無断取得しない。ライセンスが曖昧な3Dモデルを使用しない。

---

## 2. 作業開始前の調査

最初に読み取り専用で次を調べ、報告書へ記録する。

### 2.1 既存環境

- `package.json` / `package-lock.json` にThree.js、React Three Fiber、Playwright、Puppeteerなどが存在するか
- ローカルに利用可能なChromium系ブラウザと自動撮影手段があるか
- WebGL2が利用可能か
- リポジトリ内に `.glb`、`.gltf`、`.fbx`、`.obj`、`.blend` があるか
- 既存ライセンス文書に3D素材利用条件があるか

### 2.2 接続点

- 既存リプレイモデルから時刻`t`における各馬の`gate / s / w`を得る経路
- `course.ts`の`posOf`が返す位置・接線・法線
- 現行カメラの注視点、FOV、追従方式
- 1920×1080描画時の既存2Dのフレーム時間

### 2.3 調査結果による分岐

#### 利用可能な合法3D馬モデルと走行アニメーションがリポジトリ内にある場合

ライセンス、出典、再配布条件を明記して使用してよい。

#### ★オーナー確認済みの購入資産（2026-08-23追記・この分岐を採用）

オーナーから、次のモデルは**本人が商用利用目的で直接購入した資産であり、Seven Days Derbyのチャンピオンページでも実際に使用している**との確認を得た。今回のSTAR 3Dスパイクで使用してよい。

```text
V:\dev\Cusor\sevendays\archive\champions-keiba\models\holsteiner.glb
```

確認済みの技術情報:

- ファイルサイズ: 11,661,920 bytes
- SHA-256: `A0CD9BA2C48CF125D8D30247BACEDFA57BB4E56A247DDFB00462B40620761BC8`
- glTF 2.0 / Draco圧縮
- メッシュ25、スキン2
- 馬リグ131 joints、手綱リグ15 joints
- アニメーション1本: `Gallop_RM`（393 channels）
- 鞍、頭絡、手綱を含む

旧実装とテクスチャ資料:

```text
V:\dev\Cusor\sevendays\archive\champions-keiba\renderer.js
V:\dev\Cusor\sevendays\champion-3d-handoff\
V:\dev\Cusor\sevendays\CHAMPION_3D_ART_PROMPT.md
```

取り扱い:

1. 元ファイルを直接編集、移動、削除しない。
2. まず元の絶対パスから読み取り専用でロード確認する。
3. STAR内へコピーする場合は、通常のWeb公開ディレクトリへ無条件に置かず、スパイク専用の場所へ置く。
4. コピー後にSHA-256を再計算し、上記と一致することを確認する。
5. 報告書へ「オーナー購入済み商用資産」「元の絶対パス」「コピー先」「SHA-256」を記録する。
6. モデル単体を成果物として配布、外部公開、別サービスへアップロードしない。
7. 旧レンダラーは参考にしてよいが、CDNからThree.jsを実行時ロードする方式は持ち込まない。
8. `Gallop_RM`を最初に使用し、プロシージャル馬型の新規作成は不要とする。モデルが技術的にロードできない場合のみ停止して事実を報告する。

#### 3D馬素材がない場合

外部素材を勝手にダウンロードしない。以下の**検証用プロシージャル馬型**をコードで組む。

- 胴、胸、首、頭を単純な楕円体または箱で表現
- 前後左右4本の脚を上脚・下脚に分ける
- 騎手は胴、頭、上腕、前腕、大腿、下腿の簡易リグ
- 実在馬や既存製品の造形を模倣しない
- 見た目の完成度ではなく、関節運動、方向、遮蔽、影、性能を検証できる形にする

この場合、最終判定は「3Dパイプラインと動きの成立性」に限定し、馬モデルの美術品質は判定しない。

#### 3Dライブラリがない場合

第一候補は`three`のみを追加する。React Three Fiber、物理エンジン、ゲームエンジンなどを同時導入しない。

依存追加にネットワーク接続や承認が必要なら、迂回せず承認を求める。CDNから実行時ロードしない。

---

## 3. 実装方式

### 3.1 推奨構成

```text
packages/render-3d/
  package.json
  src/
    index.ts
    horse-rig.ts
    horse-motion.ts
    race-scene.ts
    camera.ts
    types.ts
  test/
    horse-motion.test.ts
    race-scene.test.ts

apps/web/src/app/3d-pack-lab/
  page.tsx
  pack-lab-client.tsx

tools/
  capture-3d-pack-lab.mjs
  build-3d-pack-compare.mjs
```

実際の構成は既存規約に合わせて変更してよい。ただし以下を守る。

- 3Dの座標変換、位相、カメラ計算はReactコンポーネントから分離する
- 時刻`t`を明示入力すれば同じシーンになる
- UIの`requestAnimationFrame`経過時間をゲーム状態の正本にしない
- `/3d-pack-lab?t=2.0&seed=42`のように、指定時刻を静止表示できる
- 通常のナビゲーションから検証ページへリンクしない
- 本番ビルドで誤って公開される点が問題なら、開発環境ガードを置く

### 3.2 Three.jsを使う場合

- `three`本体を直接利用する
- 初回スパイクではReact Three Fiberを追加しない
- WebGLRenderer、Scene、PerspectiveCamera、DirectionalLight、AmbientLight程度に限定する
- ポストプロセス、物理エンジン、GPUパーティクルは後回し
- 影は12頭すべてで試し、重ければ品質段階を比較する
- renderer、geometry、material、textureを毎フレーム作り直さない
- 馬ごとに同じGeometry/Materialを可能な限り共有する

---

## 4. 固定検証条件

- 出力: 1920×1080
- 長さ: 8.0秒
- 目標: 30fps以上
- 馬数: 12頭
- シード: 42
- 路面: 単純な芝平面
- コース区間: 直線
- カメラ: 真横追従、カット切替なし
- 上位8頭の前後スパン: 5〜7m
- 横方向: 2〜4列
- HUD、実況、タイトル、発馬機、ゴール、観客、スタンド: なし
- 音声: なし
- 色差: 最低3種類
- 勝負服: 最低4色。ただし簡易マテリアルでよい
- 乱数: すべて固定シードから導出

2D限界テストと比較するため、可能な限り同じレースシード、同じ8秒区間、同じ密集表示座標を使用する。

`packDensify`を再利用してよいが、検証コードを本番レースへ接続しない。

---

## 5. 馬と騎手の最低要件

### 5.1 3Dとして必須

- カメラ角度に応じて馬体の見える形が連続的に変わる
- 左右の脚が別々の奥行きを持つ
- 手前の馬が奥の馬を深度バッファで隠す
- 脚、胴、首、頭、騎手が同じ板ではなく、別の3D部位として動く
- 接地影が地面上に出る
- 馬の向きはコース接線に従う
- コースが曲がった場合も、素材切替ではなくモデル自体が回転する

### 5.2 走行アニメーション

最低でも1完歩を連続周期として表現する。

- 前脚と後脚を同一角度で往復させない
- 脚の接地期と空中期を分ける
- 胴体の上下動は控えめにする
- 首は脚と完全同期させず、小さな遅れを持たせる
- 騎手の腰は馬体に追従し、上体は揺れを一部吸収する
- 12頭の位相は固定ハッシュでずらす
- 完歩周波数または完歩長へ±4%以内の個体差を持たせる
- 平均速度、位置、着順は変更しない

プロシージャル馬型の場合、関節角の数値は報告書へ記録する。

### 5.3 個体差

最初のスパイクでは別モデル12体を作らない。

- 全頭で基本メッシュを共有
- 大きさを±3%以内で固定シード変化
- 首の基準角を小さく変える
- 毛色と勝負服を変える
- 位相と完歩差を変える

個体差のために着順や当たり判定を変更しない。

---

## 6. カメラ

最低2種類を同じ8秒で生成する。

### C1 — 2D比較用真横

- 2D限界テストV3に近い真横構図
- 12頭全体を画面へ入れる
- 目的は2Dと3Dの方式差を比較すること

### C2 — 参考寄り真横追従

- 先頭または注目群3〜5頭へ寄る
- 望遠寄りのFOV
- 馬体高が画面高のおおむね30〜40%になる
- 馬群全体を常に見せる必要はない
- 注目群の平均位置を滑らかに追う
- カメラの急な左右移動やズームを避ける

余力があればC3として10〜15度だけ斜め前へ回したカメラを静止画で作る。これは2D方式では弱かった「角度に対する連続性」の確認用であり、8秒動画は不要。

---

## 7. 性能測定

主観だけで採否を決めない。開発環境で最低限次を測る。

- ブラウザ名とバージョン
- GPU/WebGL renderer文字列（取得できる範囲）
- 1920×1080で8秒動かした平均FPS
- p50 / p95 / 最大フレーム時間
- 30fps未満のフレーム割合
- draw calls
- triangles
- geometries
- textures
- JavaScript heap（取得可能なら）
- 初回ロード時間
- 12頭を24頭へ増やした負荷試験の平均FPS

測定時は開発者ツールを開いた状態と閉じた状態を混ぜない。同一条件で3回実行し、中央値を採用する。

ブラウザ自動化が利用できない場合、手動測定値を自動測定のように書かない。取得できなかった項目と理由を明記する。

---

## 8. 決定論とフレーム撮影

アニメーションは次の形にする。

```text
scene = sceneAt(seed, raceTimeSec)
```

同じ`seed`と`raceTimeSec`なら、以下が一致すること。

- 各馬の位置
- 各馬の向き
- 関節角
- カメラ位置と注視点
- ライト
- 色と個体差

動画撮影は実時間の`requestAnimationFrame`任せにせず、可能なら1/30秒ずつ時刻を固定して240フレームを生成する。

ブラウザからの自動フレーム生成が環境上困難な場合は、まず指定時刻の静止画5枚と実時間8秒動画を提出してよい。ただし、その制約を報告し、完成版の決定論的録画方法を別課題として明記する。

---

## 9. 成果物

すべて`out/3d-pack-spike/`へ出す。

```text
out/3d-pack-spike/
  c1-wide.mp4
  c2-reference-close.mp4
  c1-contact-sheet.png
  c2-contact-sheet.png
  c3-oblique.png                 # 実装した場合
  compare-2d-3d-reference.png
  performance.json
  scene-measurements.json
  README.txt
```

### コンタクトシート

- 0.0 / 2.0 / 4.0 / 6.0 / 最終フレーム
- 同一解像度と配置
- 名前と時刻以外の評価誘導文を書かない

### 比較シート

行:

1. REFERENCE
2. 2D V3
3. 3D C1
4. 3D C2

列は5時点。参考は同一時刻ではなく、対応する構図・運動段階を選び、採用した元動画時刻をJSONへ記録する。

### `scene-measurements.json`

- seed
- 時刻
- 各馬の`gate / s / w / world xyz / yaw / animation phase / stride scale / size scale`
- カメラ位置、注視点、FOV
- ライト設定
- 上位8頭の前後スパン
- 横幅と列数
- 使用モデルとライセンス情報
- 生成コマンド

### `performance.json`

§7の値と測定条件、3回分の生データ、中央値を記録する。

### `README.txt`

依存導入、開発ページ起動、静止画、動画、比較シート、テストを第三者が再生成できるコマンドを書く。

---

## 10. 自動テスト

最低限、次を実装する。

1. 同一シード・時刻で全馬の位置、向き、位相、関節角が一致する
2. 時刻を進めると位相が進む
3. 12頭の位相がすべて同一でない
4. 馬の前後順序が入力レースデータと一致する
5. 3D化が`@star/race-engine`の結果を変更しない
6. 3D検証コードが通常の`/race`から参照されない
7. C1/C2のカメラが有限値を返し、馬群を視錐台内へ入れる
8. コース接線が変わると馬のyawも連続して変わる
9. 3Dシーンの時刻計算が`Date.now()`や`Math.random()`へ依存しない
10. Webページを追加した場合、型検査とビルドが通る

実行:

```powershell
npm test
npm run typecheck
npm run build:web
```

依存追加後はlockfileを更新し、不要な依存が増えていないことを確認する。

---

## 11. 合否判定

開発側はPASS/FAILを決めない。事実、成果物、性能を提出し、レビュー側が判定する。

### 技術PASS

- 12頭が3D空間内で自然に遮蔽される
- カメラを斜めへ振っても素材切替による破綻がない
- 位相差が見た目に現れ、全頭同期に見えない
- C2で3〜5頭へ寄った構図が成立する
- 1920×1080で平均30fps以上
- p95フレーム時間が50ms未満
- 通常レースとレース結果へ影響しない
- 同一時刻の再現性がある

### 条件付きPASS

見た目と接続は成立するが、影または高品質設定で30fpsを割る。品質段階、LOD、影対象の制限で改善可能な測定根拠がある場合。

### FAIL

- プロシージャル馬でも関節運動や向きが成立しない
- 3D化しても全頭が同期した複製物に見える
- 12頭で平均30fpsを大きく下回る
- 既存座標と接続するためにレース結果側の変更が必要
- 決定論的な時刻指定ができない
- WebGL環境が対象ブラウザで利用できない
- 合法かつ実用的な馬モデル・アニメーションの調達見込みがない

美術品質だけを理由に、プロシージャル馬型のスパイクをFAILにしない。美術品質と技術成立性を分けて報告する。

---

## 12. この段階で行わないこと

- 完成版馬モデルの制作・購入
- フル競馬場の3D化
- 観客、掲示板、発馬機、ゴール演出
- HUDの3D化
- 音声や実況の変更
- レース全カットの3D対応
- 現行2Dコードの削除
- 通常`/race`への切替スイッチ追加
- モバイル最適化の完遂
- 物理シミュレーションによる着順計算
- 馬同士の衝突で位置や結果を変更
- Three.js以外の大型依存を同時導入

---

## 13. 停止して質問する条件

以下の場合は`QUESTIONS_P4_3D_PACK_SPIKE_20260823.md`を作り、推測で進めない。

1. 外部3D素材の購入、ダウンロード、利用許諾が必要
2. `three`の導入にユーザー承認が必要で取得できない
3. 対象ブラウザまたはGPU要件が不明で方式選択が変わる
4. 既存の位置データだけでは馬の方向を決められない
5. 通常レースコードを変更しなければ検証できない
6. 既存ユーザー変更と衝突する
7. WebGL2が利用できず、WebGPU等への方式変更が必要

素材がないこと自体は停止理由ではない。まずプロシージャル馬型で技術部分を検証する。

---

## 14. 報告書

完了時に次を作る。

```text
REPORT_P4_3D_PACK_SPIKE_20260823.md
```

必須内容:

1. 変更ファイル一覧
2. 依存追加と理由
3. 3D素材の出典・ライセンス、またはプロシージャル馬型の構成
4. 既存レース座標との接続方法
5. 決定論の実装
6. 馬と騎手の関節運動
7. C1/C2/C3の違い
8. 性能測定の環境、生データ、中央値
9. テスト結果
10. 成果物一覧と再生成方法
11. 観察できた事実
12. 開発側の推測（事実と分離）
13. 本実装へ進む場合の置換対象と概算工程
14. 必要になる3D馬モデルの要件
15. スコープ外で発見した問題

「3Dになった」「動いた」だけで完了としない。2D V3および参考との同一シート、性能測定、再現性テストが必要。

---

## 15. Claude Codeへ渡す開始メッセージ

> `DEV_INSTRUCTIONS_P4_3D_PACK_SPIKE_20260823.md`を最初から最後まで読み、その範囲だけを実施してください。これは完成版3Dレースではなく、8秒・12頭・真横1カットの技術検証です。通常の`/race`、レース結果、DB、worker、認証、ポイントには触れないでください。現行2Dの追加調整も行わないでください。最初に既存3D環境と素材を調査し、合法な馬モデルがなければプロシージャル馬型で進めてください。外部素材の取得や購入が必要なら停止して質問してください。成果物、性能測定、比較シート、再生成手順、自動テスト、報告書が揃うまで完了扱いにしないでください。既存の未コミット変更を保持し、pushしないでください。
