# REPORT — P4 3D 馬群スパイク（2026-08-23）

対象指示書: `DEV_INSTRUCTIONS_P4_3D_PACK_SPIKE_20260823.md`
実施範囲: 指示書の §1〜§14 のみ。通常の `/race`・レース結果・DB・worker・認証・ポイントには触れていません。
現行 2D の追加調整も行っていません。push もしていません。

> ★**この報告書は採否を決めません。** 「3D になった」「動いた」は結論ではないので書きません。
> 測った値と、撮れた絵と、その場で起きた失敗を並べます。判定はレビュー側の仕事です。

---

## 1. 変更ファイル一覧

### 新規（3D スパイク）

| ファイル | 行 | 役割 |
|---|---:|---|
| `packages/render-3d/package.json` | 13 | 依存ゼロの純 TypeScript パッケージ |
| `packages/render-3d/src/types.ts` | 84 | 場面・カメラ・馬の型 |
| `packages/render-3d/src/horse-motion.ts` | 130 | 個体差（毛色・勝負服・大きさ・完歩）と走行位相 |
| `packages/render-3d/src/camera.ts` | 127 | C1/C2/C3 のカメラ計算 |
| `packages/render-3d/src/race-scene.ts` | 96 | 時刻 → 場面（位置・向き・位相・カメラ・光） |
| `packages/render-3d/src/index.ts` | 11 | 公開口 |
| `packages/render-3d/test/race-scene.test.ts` | 329 | §10 の自動テスト（17 件） |
| `packages/render-3d/test/spike-guard.test.ts` | 81 | 隔離とモデルの同一性（4 件） |
| `apps/web/src/app/3d-pack-lab/page.tsx` | 26 | 検証ページ（本番では 404） |
| `apps/web/src/app/3d-pack-lab/pack-lab-client.tsx` | 682 | three.js の実体 |
| `apps/web/src/app/api/spike-3d/model/route.ts` | 47 | 開発時だけ GLB を返す（本番では 404） |
| `apps/web/public/draco/*` | — | Draco 展開器（three.js 同梱物の複製） |
| `tools/capture-3d-pack-lab.mjs` | 255 | 時刻指定のコマ撮り／性能測定 |
| `tools/render-3d-pack-compare.mjs` | 105 | 参考・2D・3D の比較シート |
| `tools/measure-3d-pack-spike.mjs` | 195 | 測定値の集計 |
| `tools/inspect-glb.mjs` | 33 | GLB の中身を数える（読むだけ） |

### 既存の変更（最小限）

| ファイル | 変更 |
|---|---|
| `package.json` | `@types/three` を devDependencies へ |
| `apps/web/package.json` | `three@^0.185.1` を dependencies へ |
| `tsconfig.json` / `apps/web/tsconfig.json` | `@star/render-3d` のパス解決 |
| `.gitignore` | `/assets-spike/3d/*.glb` を除外（モデルをリポジトリに入れない） |
| `tools/lib/classification.mjs` | 新規ツール 4 本を READONLY へ登録（R-24） |

### ★触っていないもの

`packages/race-engine` / `packages/render` / `apps/web/src/app/race` / `apps/worker` / `packages/betting` /
`packages/auth` / DB スキーマ / マイグレーション。

⚠️ `apps/web/package.json` の `dev` ポート `3100 → 3210` は**着手前からあった未コミット変更**です。
そのまま残しています。混ぜていません。

---

## 2. 依存追加と理由

| 追加 | 版 | 理由 |
|---|---|---|
| `three` | 0.185.1 | WebGL の描画。指示書 §12 が「Three.js 以外の大型依存を同時導入しない」と定めているので、これ 1 本だけ |
| `@types/three` | 0.185.0 | `any` 禁止のため型が要る |

★**追加していないもの**（意図的）:

- Puppeteer / Playwright … コマ撮りは Node 内蔵の `WebSocket` で DevTools プロトコルを直接叩いています
- react-three-fiber / drei … React の層を挟まず three.js を直に使っています
- 物理エンジン・後処理ライブラリ … スパイクの問いに要りません

---

## 3. 3D 素材の出典・ライセンス

- ファイル: `holsteiner.glb`
- 出典: **オーナー本人が商用利用目的で直接購入**した資産。Seven Days Derby でも使用中。
  元ファイルは `V:\dev\Cusor\sevendays\archive\champions-keiba\models\holsteiner.glb`。
- 取り扱い: スパイク専用に `assets-spike/3d/holsteiner.glb` へ**複製**。元ファイルは読むだけで変更していません。

| 項目 | 実測値 |
|---|---|
| SHA-256（複製） | `a0cd9ba2c48cf125d8d30247bacedfa57bb4e56a247ddfb00462b40620761bc8` |
| SHA-256（元ファイル） | 同上（**1 バイトも違わない**） |
| サイズ | 11,661,920 バイト |
| メッシュ | 25 |
| スキン | 2（joints 131 / 15） |
| ノード | 173、うちアニメーションが動かすのは 131 |
| マテリアル | 4（`M_Hair` / `M_Tack` / `M_Holsteiner` / `M_Eyes`） |
| テクスチャ / 画像 | 7 / 7 |
| 拡張 | `KHR_draco_mesh_compression`, `KHR_materials_specular`, `KHR_materials_ior` |
| アニメーション | `Gallop_RM` 1 本のみ。393 チャンネル / 0.800 秒 |
| 三角形（申告値） | 57,219 |
| 単位 | **センチメートル**（馬体メッシュ 62.7 × 206.2 × 264.4） |
| 騎手 | **含まれていません** |

★同一性の確認は `packages/render-3d/test/spike-guard.test.ts` ★③ が機械で行います。
★モデル単体を配布用の場所（`apps/web/public/`）へは置いていません（同 ★④）。
★リポジトリにも入れていません（`.gitignore`）。

### プロシージャル部分（騎手）

モデルに騎手がないため、騎手だけ手続き的に組んでいます（`buildRider`）。
カプセルと球で胴・頭・兜・腕 2 本・脚 2 本。勝負服の色は枠色から。
★これは**間に合わせ**です。§14 に要件として書きます。

---

## 4. 既存レース座標との接続方法

★**位置はレースエンジンが決めた値が正本**です（憲法 3）。3D 側は読むだけです。

```
@star/race-engine の model.at(秒)  →  { gate, meters(s), w }
        ↓ （素通し。並べ替えも補正もしない）
@star/render/course.ts の posOf(course, s, w)  →  平面座標
        ↓
{ x, y: 0, z }  … 2D の (x, y) を 3D の (x, z) に読み替えるだけ
        ↓
向き yaw = atan2( posOf(s+1) − posOf(s) )  … コースの接線
```

- ★奥行きの前後関係は**深度バッファ**に任せます。2D のような描画順の細工をしていません。
- ★`sceneAt` は入力の並びをそのまま返します（`race-scene.test.ts` ★④・★⑤ が確認）。
- ★2D 限界テストと同じ `packDensify(PACK_LIMIT_TEST)` を通し、**同じ密度の馬群**で撮っています。
  比べる土台を変えないためです（R-30）。

---

## 5. 決定論の実装

- `sceneAt({ seed, raceTimeSec, ... })` は **seed と時刻だけ**で決まります。
- `Date.now()` / `Math.random()` / 引数なしの `new Date()` を使っていません
  （`race-scene.test.ts` ★⑨ が注釈を落としたうえでソースを検査）。
- 個体差はすべて `hash01(馬番, seed + 定数)`。乱数ではありません。
- アニメーションは `mixer.update(dt)` に任せず、**`action.time` を直接指定**して進めます。
  コマ落ちや実時間のゆらぎが絵に入りません。
- コマ撮りは `requestAnimationFrame` 任せにせず、`__packLabSeek(t)` で 1/30 秒ずつ時刻を送ります。
  ★同じ時刻なら何度撮っても同じ絵です。

---

## 6. 馬と騎手の関節運動

| 要素 | 実装 | 実測の効き |
|---|---|---|
| 脚 | `Gallop_RM`（131 ボーン）をそのまま。`action.time` を位相から決める | 馬ごとに位相が違う |
| 前進 | ★`Root.position` の**水平成分だけ**を固定。上下動は歩様なので残す | 馬が勝手に前へ行かない |
| 完歩長 | `STRIDE_M = 7`（2D の `BROADCAST_STRIDE_M` と同値）× 個体差 ±4% | 0.9627〜1.0385 |
| 体格 | ±3% | 0.9728〜1.0263 |
| 首 | 位相を 0.12 遅らせた正弦（`neckAngleOf`） | 脚と完全同期しない |
| 騎手 | 腰は馬体に追従、上体は揺れの 28% だけ受ける（`RIDER_ABSORB = 0.72`） | 上体が跳ねない |
| 毛色 | 5 色。★素材の絵を**明るさだけ**にして色を載せる | 12 頭で 5 色すべて使用 |
| 勝負服 | 枠色 8 色（D-060: 色は枠・数字は個体） | 12 頭で 8 色すべて使用 |

### ★位相を位置から切り離したこと

2D は位相を「進んだ距離 ÷ 完歩長 ＋ 馬番 × 定数」で作っていました。
3D では**オフセットをハッシュにし、位置と無関係**にしています。
効きを、馬群を詰めた形で測りました（完歩を 12 等分して「何区画に散るか」を数える。最大 12）:

| 馬群の形 | 3D（位相を切り離す） | 対照: 位置由来だけ |
|---|---:|---:|
| 発走直後・完全な横並び（前後 0m） | **8** | **1** |
| 発走直後・団子（前後 1m） | **8** | **3** |
| 前後 6m（参考映像に近い） | 8 | 12 |
| 前後 27m（2D V3 の実測に近い） | 8 | 11 |

⚠️ ★**この対照値は、進んだ距離が小さいところでしか意味がありません。**
数百 m 走ったあとは、距離の差が完歩長を何周も跨ぐので、位置由来でも見かけ上は散ります。
効くのは上 2 行だけです。ここを取り違えないよう `scene-measurements.json` の `howToRead` にも書きました。

---

## 7. C1 / C2 / C3 の違い

| | C1 | C2 | C3 |
|---|---|---|---|
| ねらい | 12 頭ぜんぶを入れる（2D 比較用） | 参考に近い寄り | 角度の連続性の確認 |
| 横へ | 46m | 34m | 34m |
| 高さ | 3.4m | 2.6m | 2.8m |
| 真横から | 0° | 0° | 12° |
| 注視点 | 全頭の前後の中点 | 上位 4 頭の平均 | 上位 4 頭の平均 |
| 画角の決め方 | 前後の広がりから逆算 | 馬体高が画面の 35% になる値 | 同左 |
| 実測 画角 | 11.82° | 11.99° | 11.99° |
| 実測 注視距離 | 46.04m | 34.01m | 34.02m |
| 実測 見える幅 | 16.94m | 12.70m | 12.70m |
| 実測 馬体の画面占有率 | 0.262 | **0.350** | 0.350 |

★C2 は指示書 §6 の「30〜40%」の中央に入っています。

⚠️ ★判定の物差しを一度まちがえました。
最初は「C1 の画角 > C2 の画角」で引き／寄りを判定しようとして、**テストが落ちました**（9.5° < 12.0°）。
馬群が 6m しかないと、C1 は全頭を入れるためにむしろ寄れてしまいます。
画角ではなく**注視距離での見える幅**と**馬体の画面占有率**で測る形に直しました。

---

## 8. 性能測定

### 環境

| 項目 | 値 |
|---|---|
| ブラウザ | Microsoft Edge 151.0.4129.101 |
| GPU（WebGL renderer） | `ANGLE (Intel, Intel(R) Iris(R) Xe Graphics (0x000046A8) Direct3D11 vs_5_0 ps_5_0, D3D11)` |
| WebGL2 | はい |
| 解像度 | 1920×1080 / `devicePixelRatio` 固定 1 |
| コマ数 | 240（8 秒ぶん） |
| 実行 | 各条件 3 回。表は**中央値**。生のコマ時間は各 JSON の `rawFrameTimesMs` |

⚠️ ★**ヘッドレスで測っていません。** ヘッドレスだと WebGL が SwiftShader（CPU）へ落ちることがあり、
そのまま測ると**実機より大幅に遅い数字**を「測定結果」として出してしまいます。
画面の外に置いた実ウィンドウで、実際の GPU で測っています。renderer 文字列を上に載せたのはそのためです。

### 中央値

| 条件 | 平均 fps | p50 | p95 | 最大 | 30fps 未満の割合 | ヒープ | 三角形 | 描画呼び出し |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 12 頭・影あり | **55.4** | 16.7ms | 25.5ms | 33.9ms | 0.4% | 133MB | 1,032,746 | 764 |
| 12 頭・影なし | **58.3** | 16.7ms | 19.8ms | 23.6ms | 0.0% | 95MB | 516,350 | 380 |
| 24 頭・影あり | **41.6** | 23.0ms | 31.0ms | 54.4ms | 2.9% | 133MB | 2,052,950 | 1,414 |
| 24 頭・影なし | **54.5** | 17.3ms | 24.6ms | 31.2ms | 0.0% | 129MB | 1,036,250 | 793 |

- ジオメトリ 106 / テクスチャ 206（12 頭時・`_page-info.json`）
- モデル読み込み（GLB 取得 → Draco 展開 → 12 頭ぶん複製 → 初回描画）: **3.8〜6.2 秒**
- ★上限は `requestAnimationFrame` の 60fps です。55 は「時々落ちる」を意味します。
- ★影が支配的です。12 頭で影を切ると三角形が半分（影マップの描画ぶん）になり、p95 が 25.5 → 19.8ms へ。

---

## 9. テスト結果

```
npm test        …  Test Files 111 passed (111) / Tests 1107 passed (1107)
npm run typecheck …  エラーなし（ルート + apps/web）
npm run build:web …  成功
```

新規 21 件の内訳（`packages/render-3d/test/`）:

| # | 内容 | 結果 |
|---|---|---|
| ① | 同一 seed・時刻で完全一致（時刻を変えれば変わる対照つき） | 通過 |
| ② | 時刻を進めると位相が進む（0.2 秒で完歩の 0.3〜0.6 周） | 通過 |
| ③ | 12 頭の位相が同一でない／横並び・団子でも潰れない | 通過 |
| ④ | 馬の前後順序が入力と一致 | 通過 |
| ⑤ | `s` / `w` を素通し（レースの位置を変えない） | 通過 |
| ⑥ | 3D コードが `packages/render` と `/race` から参照されていない | 通過 |
| ⑦ | C1/C2/C3 のカメラが有限で、全馬が視錐台の前方 | 通過 |
| ⑧ | コーナーで yaw が連続（1m あたり 0.02rad 未満、合計 0.3rad 以上回る） | 通過 |
| ⑨ | `Date.now` / `Math.random` / `new Date(` を使っていない（注釈を除いて検査） | 通過 |
| — | 個体差が ±3% / ±4% に収まる・毛色 3 種以上・勝負服 4 色以上 | 通過 |
| — | C1 は全頭が入る／C2 は馬体高 30〜40%／伸びた馬群では C1 が広い | 通過 |
| 隔離① | 検証ページと配信 API に本番ガードがある | 通過 |
| 隔離② | どの画面からもリンクされていない | 通過 |
| 隔離③ | 複製が元ファイルと SHA-256 一致 | 通過 |
| 隔離④ | 公開ディレクトリに 3D モデルがない | 通過 |

### ★テストが「壊したときに落ちる」ことの確認（R-16）

書いたテストが本当に効くか、**わざと壊して**確かめました（すべて確認後に復元）:

| 壊した箇所 | 結果 |
|---|---|
| `packages/render/src/course.ts` に `@star/render-3d` の文字列を足す | ⑥ が落ちた |
| `wideFovDeg` の戻り値を 6 に固定 | C1 の 2 件が落ちた |
| 検証ページから本番ガードを消す | 隔離① が落ちた |
| `/race` から検証ページを参照する | 隔離② が落ちた |
| `phaseOffset` を 0 に潰す | ③ の 2 件が落ちた |

⚠️ ★最後の 1 件は**最初は落ちませんでした**。
`s = 300` で見ていたため、完歩長の ±4% だけで位相が何周もずれ、オフセットを潰しても散って見えたからです。
発走直後（`s ≒ 2m`）で見る形に直して、初めて落ちるようになりました。
★「通った」ではなく「壊したら落ちた」まで確かめないと、この種のテストは意味を持ちません。

---

## 10. 成果物一覧と再生成方法

`out/3d-pack-spike/`（詳細は同ディレクトリの `README.txt`）

| ファイル | 内容 |
|---|---|
| `c1-wide.mp4` | C1・240 コマ・8 秒・1920×1080 |
| `c2-reference-close.mp4` | C2・同上 |
| `c3-oblique.png` | C3・4.0 秒の静止画 |
| `c1-contact-sheet.png` / `c2-contact-sheet.png` | 1 秒ごと 8 枚（4 列 × 2 段） |
| `compare-2d-3d-reference.png` | REFERENCE / 2D V3 / 3D C1 / 3D C2 × 5 時刻 |
| `scene-measurements.json` | モデル・カメラ・馬群・位相。読み方は `howToRead` |
| `performance-{12,24}h-{shadow,noshadow}.json` | 3 回ぶんの生データと中央値 |

再生成（開発サーバーが `:3210` で動いていること）:

```bash
npx tsx tools/capture-3d-pack-lab.mjs --cam c1 --out c1-wide
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --out c2-reference-close
npx tsx tools/capture-3d-pack-lab.mjs --cam c3 --still --t 4 --out c3-oblique
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --perf --horses 12 --shadows 1
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --perf --horses 12 --shadows 0
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --perf --horses 24 --shadows 1
npx tsx tools/capture-3d-pack-lab.mjs --cam c1 --perf --horses 24 --shadows 0
npx tsx tools/render-3d-pack-compare.mjs
npx tsx tools/measure-3d-pack-spike.mjs
```

診断用（絵の不具合の原因を目で当てないための道具）:

```bash
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --bounds --t 4      # メッシュが世界のどこにあるか
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --still --hide 名前  # 個別に消して切り分ける
node tools/inspect-glb.mjs assets-spike/3d/holsteiner.glb          # GLB の中身を数える
```

---

## 11. 観察できた事実

★ここは**測った値と、撮れた絵に写っていたもの**だけです。

1. **12 頭が 1 つの 3D 空間に立ち、深度バッファで前後が解決している。**
   `compare-2d-3d-reference.png` の 3D C2 行で、手前の馬が奥の馬を実際に隠しています。
   描画順の細工はしていません（`sceneAt` は入力順のまま返します）。

2. **馬ごとに脚の位相が違う。** 完歩を 12 等分した区画で、C2 の 12 頭は 6 区画に散っています。
   横並び・団子に詰めた最悪の形でも 8 区画。位置由来だけなら同じ形で 1 区画・3 区画です。

3. **12 頭に毛色 5 種すべて（栗・鹿・黒鹿・芦・青）と勝負服 8 色すべてが出ている。**
   `scene-measurements.json` の `individuality` に、実際に使われた 5 色 / 8 色が並びます。

   ⚠️ ★ここは**一度まちがえて書きました。** 「2D V3 は 12 頭とも同じ毛色・同じ勝負服」と
   書きかけましたが、実装を読むと違いました。2D 側も `tools/render-2d-pack-limit.mjs:139` の
   `COAT_BY_GATE` で 5 種類（bay / chestnut / dark-bay / grey / blue-black）を配っています。
   ★**仕様としては 2D も色を分けています。**

   そのうえで、比較シートを見たままを書くと（★これは測った値ではなく、絵を見ての記述です）:
   2D V3 の行では 12 頭の毛色の差が判別しづらく、騎手は全頭が同じ白い勝負服に見えます。
   3D C2 の行では毛色の 5 段と勝負服の 8 色が離れて見えます。
   ★差が「仕様の差」ではなく「見え方の差」である以上、
   2D 側の毛色の焼き込み量を上げれば縮まる可能性があります（★未測定）。

4. **コーナーでモデルそのものが回る。** s = 560→700m で yaw の総回転 0.3rad 以上、
   1m あたりの変化は 0.02rad 未満。素材の差し替えではありません。

5. **性能（Intel Iris Xe・1920×1080）**
   12 頭・影あり 55.4fps（30fps 未満 0.4%）／24 頭・影あり 41.6fps（同 2.9%）。
   影を切ると 24 頭でも 54.5fps・30fps 未満 0%。

6. **★3D の場面には世界が無い。** 地面・空・光だけです（指示書 §4 の指定どおり）。
   比較シートで並べると、スタンド・観客・ラチ・距離標・芝目まで作り込んである 2D V3 のほうが
   **画面としては密度が高く見えます**。3D 側が現時点で持っているのは馬群だけです。

7. **★参考映像は「寄り」である。** 参考の 50〜58 秒は 2〜3 頭しか画面に入っていません。
   C2（馬体高 35%）はこれに近く、C1（26%）は参考より引いています。

8. **購入モデルには走行アニメが 1 本しかない。** `Gallop_RM` のみ。
   歩き・キャンター・ゴール後の減速・立ち上がりに使えるものはありません。

9. **購入モデルの一部が、この使い方では壊れる。**（すべて実測で特定）
   - `headgear_Reins`（手綱）… 走行アニメの骨に付いておらず、★**地面に細い棒として落ちる**
     （実測 minY 0.00 / maxY 0.25 / 長さ 0.92m）。芝の上に黒い棒が散らばって写っていました。
   - `Mane_Short` / `Mane_ShortCovered` … 同じ理由で潰れる（高さ 0.31〜0.32m・水平の広がり 0.01m）
   - `Braids` … マテリアル名が空で、★**白い編み目がファスナーのように首すじへ**並ぶ
   - `Tail_Base` / `HorseBodyMesh_2`（尻尾）… ★**蛍光の緑**に出る

   → 表示時に前 3 種を出さず、たてがみ・尻尾は毛色を暗くした無地へ差し替えています。
   ★**モデルのファイルは変更していません。**

10. **★測り方のせいで一度まちがえた（R-30）。**
    キャンバスは画面上 1156px 幅で表示されていました。そこを 1920px へ引き伸ばして撮っていたので、
    最初の絵は輪郭が甘く出ていました。★このまま出していれば「3D は輪郭が甘い」という
    **測り方のせいの結論**を報告するところでした。原寸表示（`?full=1`）に直し、
    1920 でなければ撮影を止めるようにしています。

11. **★成果物に開発環境の飾りが写り込んでいた。** 左下に Next.js のバッジ（丸い「N」）。
    撮影時に隠すようにしました。

---

## 12. 開発側の推測（★事実ではありません）

⚠️ ここから下は**測っていないこと**です。事実と混ぜないでください。

- §11-6 から、**3D と 2D の差の大半は「馬」ではなく「世界」にある**のではないか。
  3D 側に 2D と同じスタンド・ラチ・距離標・芝目を入れたら、印象は逆転するかもしれません。
  ただし作り込みの工数は測っていません。
- §11-8 から、**アニメーションが 1 本しかないことが、本実装の最大の障害**ではないか。
  発走・追い出し・ゴール後の減速・勝利は、いま作れません。
- 性能について、Intel Iris Xe で 24 頭・影あり 41.6fps という値は、
  **影を切るか、影を落とすのを近い数頭に限れば** 18 頭級でも 60fps に収まりそうに見えます。
  ただし他の GPU（特にスマートフォン）では測っていません。
- 騎手を手続き的なカプセルのままにすると、寄れば寄るほど粗が見えます。
  参考映像と同じ寄り（C2）では既に目立ちます。

---

## 13. 本実装へ進む場合の置換対象と概算工程

★これは**見積もりであって、約束ではありません**。

| 段 | 置換対象 | 内容 | 概算 |
|---|---|---|---|
| A | `apps/web/src/app/race/page.tsx` の描画層 | 2D の `drawScene` を 3D の場面へ差し替え。台本・時間伸縮・実況・HUD はそのまま流用 | 3〜5 日 |
| B | 世界（§11-6） | スタンド・観客・内外ラチ・距離標・芝目・発馬機を 3D 化。2D の実測値をそのまま持ち込める | 5〜8 日 |
| C | アニメーション（§11-8） | 発走・追い出し・減速・勝利。購入か外注か手付け | ★不明（素材次第） |
| D | カメラ台本 | `SCRIPT_V4` の各カットを 3D のカメラ数値へ移す | 2〜3 日 |
| E | 性能 | 影の間引き・LOD・遠景のインスタンス化 | 2〜4 日 |
| F | 端末対応 | スマートフォン・低性能 GPU での実測と落とし所 | ★不明（実機が要る） |

★**そのまま残せるもの**: レースエンジン、コース幾何（`course.ts`）、時間伸縮（`time-warp.ts`）、
実況・ナレーター、HUD、枠色の規則（D-060）。3D 化はいずれにも触れません。

---

## 14. 必要になる 3D 馬モデルの要件

いま持っているモデルで足りなかったところを、要件の形に直しました。

**必須**

1. アニメーション: **駈歩に加えて**、発走の立ち上がり／追い出し（鞭）／ゴール後の減速／常歩。
   最低でも 4 本。ループの継ぎ目が合っていること。
2. 手綱・たてがみ・尻尾が**走行アニメの骨に付いている**こと。
   ★今のモデルは手綱が骨に付いておらず、地面に落ちます（§11-9）。
3. マテリアル名が全メッシュに付いていること。空だと既定の白になります（§11-9 の `Braids`）。
4. 毛色を差し替えられること。**素材の絵に色が焼き込まれていない**か、
   焼き込まれていても明度だけ使って染められる作りであること。
5. 単位と原点: メートル、原点は蹄の接地点。
   ★今のモデルはセンチで、原点が蹄から 30cm ずれています。
6. 三角形数: 1 頭 **2 万以下**が望ましい。今は 57,219。18 頭で 100 万を超えます。

**あるとよい**

7. 騎手が同梱で、鞍の上に座った状態のリグを持つこと（★今は手続き的なカプセルです）。
8. LOD（遠景用の軽い版）。
9. 毛色・体格の異なる 3〜4 体（今は 1 体を色と大きさで散らしています）。
10. 商用利用可・改変可のライセンスが**書面で確認できる**こと。

---

## 15. スコープ外で発見した問題

★指示書の範囲外なので**直していません**。報告だけします。

1. **本番ビルドの一覧に `/3d-pack-lab` と `/api/spike-3d/model` が行として出ます。**
   どちらも実行時に 404 を返すので機能はしませんが、**経路の存在は隠せていません**。
   経路ごと消したいなら、ビルド時に除外する仕組み（環境変数によるページ除外）が要ります。
   現状は「出るが応答しない」を隔離テスト①で、「どこからも押せない」を②で担保しています。

2. **`@star/render-3d` が `node_modules/@star/` に貼られていません。**
   `npm install` を流していないためです。テストは相対パスで読んでいるので通りますが、
   `tools/measure-3d-pack-spike.mjs` も相対パスにする必要がありました。
   本実装へ進むなら `npm install` の実行が要ります。

3. **`out/` の直下に、過去の使い捨て出力が 100 件以上たまっています。**
   `.json` / `.txt` / `.png` が混在していて、どれが何の証拠か追えません。
   `out/<作業名>/` へまとめる規則が要ると思います（今回は `out/3d-pack-spike/` に閉じています）。

4. **`tools/` に `_` 始まりの使い捨てツールが 30 本以上あります。**
   `tools/lib/classification.mjs` に登録はされていますが、
   いつの何の調査だったかは登録簿からは読めません。

---

## 完了条件の確認

| 指示書の要求 | 状態 |
|---|---|
| 成果物（動画 2・静止画 3・比較シート） | `out/3d-pack-spike/` に有り |
| 性能測定（3 回・中央値・生データ・24 頭負荷） | 4 条件ぶん有り |
| 比較シート（参考 / 2D V3 / 3D C1 / 3D C2） | 有り |
| 再生成手順 | §10 と `README.txt` |
| 自動テスト | 21 件。★壊したら落ちることも確認済み |
| 報告書 | 本書 |
| 通常の `/race`・DB・worker・認証・ポイントに触れない | 触れていません |
| 既存の未コミット変更を保持 | `apps/web/package.json` のポートを保持 |
| push しない | していません |
| 採否の判定 | ★していません（レビュー側の仕事） |
