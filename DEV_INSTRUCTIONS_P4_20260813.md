# 開発指示書 P4 — レトロ描画 + 観戦 + 介入UI

**発行**: 2026-08-13（レビュー側 Claude）
**正典**: `STAR_SPEC_v2.0.md`（§8b 介入・§12 UI・§15 アプリ化・`.claude/docs/technical-preferences.md`）
**完了条件**（§16.1）: **遊んで面白いと感じられる**

---

## 0. ★このフェーズは評価軸が変わります

```
P0/P1 : 計算が正しいか      → 数値で判定
P2    : 止まらないか        → 稼働で判定
P3    : 一生を通せるか      → 通しで判定
P4    : ★面白いか           → ★測れない
```

**「面白い」は測れません。そして測れないからといって、判定を諦めるわけではありません。**

### 役割分担を先に決めます

| | 誰が | 何を |
|---|---|---|
| **面白いかどうか** | **オーナー** | 実際に遊んで判断する |
| **面白さが成立する前提条件** | **レビュー側** | 測れるものはすべてゲートにする |
| **オーナーが判断できる状態を作る** | **開発側** | ★これが P4 の主たる仕事 |

**★「オーナーに見せて『どうですか』と聞く」のは、判断できる状態ではありません。** **何を見て、何を触って、どこで判断するのかを、そちらが用意してください。**

---

## 1. スコープ

### U-1【必須】レース観戦の描画（§12）

- **レトロ調の2D描画**。§7.6 が「レトロUIのテキストボックスと相性が良く実装コストも低い」と書いているとおり、**軽さが設計の一部**です
- **★描画はレース結果を再生するもので、結果を決めるものではない。** 着順は §8 で確定済みの値を、そのまま見せます
- **★同じ seed から同じ映像が出ること**（§8.6 の延長。観戦のリプレイが成立する条件）

### U-2【必須】介入UI（§8b）

**P1 で作った介入モデルに、人間の手を繋ぎます。**

- 全操作は**クリック/タップ1種類で完結**（`technical-preferences.md`。マウスホバーに依存しない）
- スタミナゲージの表示。**初期値は §7.4 の調子・疲労から決まる**（P3 で配線済み）
- **仕掛けの操作**（§8b）

### U-3【必須】馬券購入と結果表示のUI

- **`odds_at_purchase` が購入時に固定される**ことが画面から分かること（§9）
- **自馬出走レースの制限**（§9.5）を、買えなくなってから知るのではなく**買う前に分かる**形で

### U-4【必須】§8.6 の検証手順を公開する

**P2 で「Vercel の画面の値だけで第三者検証できた」ことを確認しました。** **利用者が自分で検証できなければ、機構があっても信頼の役に立ちません。**

- **seed_commit / seed_reveal と、再計算の手順を画面から辿れるように**
- §17 の公開前チェックリストにも入れます

### U-5【必須】アセット配信（Cloudflare R2）

- **初回ロード 5MB 以下**（`technical-preferences.md`）

---

## 2. 合格基準

### 測れるもの（レビュー側が判定します）

| # | 基準 | 出典 |
|---|---|---|
| **C-1** | **60fps 維持 / フレーム予算 16.6ms** | technical-preferences |
| **C-2** | **ドローコール 500以下** | 同上 |
| **C-3** | **初回ロード 5MB 以下**（WASM + アセット） | 同上 |
| **C-4** | **メモリ 256MB 以下**（モバイルタブ想定） | 同上 |
| **C-5** | **同じ seed から同じ映像**（観戦のリプレイ） | §8.6 |
| **C-6** | **★人間が UI 経由で操作したときの `interventionMult` が、ハーネスの「最適な仕掛け」と「早すぎる仕掛け」の範囲を再現する** | §3 |
| **C-7** | **§9.5 の制限が、買う前に画面から分かる** | §9.5 |
| **C-8** | **第三者が画面の情報だけで §8.6 を再計算できる**（手順つき） | §8.6 |
| **C-9** | P0〜P3 の全ゲート回帰（**R-23: SHA 併記**） | §16.2 |
| **C-10** | テスト全PASS・typecheck・`any` ゼロ・変異試験 | — |

### 測れないもの（オーナーが判定します）

**「遊んで面白いと感じられる」** — **そちらの仕事は、オーナーがこれを判断できる状態を用意することです**（§5）。

---

## 3. ★C-6 が P4 で最も重要な基準です

**V-13 は「仕掛けの巧拙が結果に出ること」を保証しています。** ハーネスでは**最適な仕掛けと早すぎる仕掛けで倍率に 0.20 の差**が出ました。

**しかし、それは「理論上、正しく操作すれば差が出る」という話です。**

**★入力遅延・ゲージの見づらさ・押しどころの分かりにくさがあれば、人間には最適な仕掛けができません。** そのとき:

- **V-13 は通り続けます**（ハーネスは人間を通らないので）
- **プレイヤーにとっては「仕掛けても何も変わらない」ゲームになります**
- **そして数字のどこにも現れません**

**これは P3 で繰り返し出た「機構は存在するが働いていない」の、UI 版です。**

→ **人間が操作した `interventionMult` の分布を記録し、ハーネスの最適/早すぎるの範囲と突き合わせてください。**

---

## 4. 引き継ぐもの

**P0〜P3 で確立した規律は全部そのままです。特に P4 で効くもの:**

| # | 内容 |
|---|---|
| **R-16 の検査法** | **「この基準を最も安易に満たす方法は何か」** — 答えが「機構を止めること」なら反対側が要る |
| **R-21** | **「出なかった」「落ちた」を理由を読まずに結果にしない** |
| **R-22** | **照合は壊れ方と同じ粒度で**（平均は個体ごとのずれを見ない） |
| **R-23** | 合格に SHA を併記。**「その SHA 以降コードが変わっていないか」で判定** |
| **R-24 の三分割** | うっかりは不可能に / 意図的は拒否で足りる / **迂回ですらないものが最も危険** |
| **D-054** | **「効いているか」と「ゲートが捕まえるか」は別の問い** |
| **標本の規律** | **SD を測ってから減らす。そして `|乖離| + kSE < 帯` で判定する** |

**★そして P3 の最大の教訓:**

> **「PASS と表示されているが、何も検証していない」が、P3 だけで6件出ました。**
> **すべて、外から見て健全でした。**

**P4 は画面がつくフェーズなので、「動いているように見える」ことが今まで以上に簡単です。**

---

## 5. ★オーナーが判断できる状態とは

**次の3つを用意してください:**

| # | 内容 |
|---|---|
| 1 | **触れる状態**（URL を開けば遊べる。手順書ではなく) |
| 2 | **何を見てほしいかの一覧**（「この画面のここを触って、こう感じるか」を具体的に。★「全体的にどうですか」ではなく） |
| 3 | **★判断が分かれたときに何を変えられるかの選択肢**（「面白くない」と言われたときに、何を動かせるのかを先に用意しておく） |

**3が無いと、「面白くない」という判定が行き止まりになります。**

---

## 6. 報告様式

**冒頭に必須:**

```
## 本便で正典の改訂が要る決定をしたか
- あり / なし  （ありの場合: 何を、どこに書いたか）
```

そのうえで: 実装したもの / 再現手順 / **C-1〜C-10 の実測（SHA 併記）** / 定数の3分類 / 仕様からの逸脱（全件）/ 未解決の質問。

---

## 7. 着手順

**U-1（描画）→ U-2（介入UI）→ U-3（馬券UI）→ U-4（検証手順）→ U-5（配信）**

**C-6 は U-2 の完了条件**なので、介入UI を作ったら、そのまま人間の操作を記録して測ってください。

**★描画を作る前に、C-3（初回ロード5MB）と C-1（60fps）の予算をどう配分するかを先に決めてください。** 作ってから削るのは高くつきます。
