# 報告 — ES-5（案 (f) 同じ和の分解）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ 開発側 → レビュー側・オーナー
**対象**: 回答 `REVIEW_ENGINE_LANE_SPEED_ES3_ANSWER_20260916.md` §4（★オーナー承認 F-1 の後に着手）
**ブランチ**: `p4/race-30sec-cuts` ／ **開始 HEAD** `0050119` ／ ES-5 は本報告と同じコミット

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **△** ＝ 未確認・推定
> ⚠️ **F-1** はオーナーの「はい」（2026-09-16）で承認と受け取りました。**F-2（`0021`・`0022` の本番適用）・F-3（push）は回答をいただいていない**ので、どちらもしていません。
> ⚠️ 本番には接続していません。staging は読み取りだけです。

---

## 0. 結論

1. **分解の式を入れました。** 結果の差は丸めだけです。距離ロスの差は最大 **3.7×10⁻¹³ m**、スコアの相対差は最大 **5.2×10⁻¹⁶**、走破タイムの差は最大 **2.8×10⁻¹⁴ 秒** で、着順は 182 組 × シード 20 のすべてで完全一致しました ✔
2. **合格線（1.3 倍以下）には届きませんでした。** `97364c5^` の **約 1.4〜1.6 倍** です（ES-3 は 3.1〜7.2 倍）。指示どおり、ここで止めて報告します ✔
3. 残りの差のほとんどは、**1 レースに 1 回の `lanePlanOf`（4〜7µs）** です。馬ごとの距離ロスは 18 頭の合計で約 1µs まで下がりました ✔ → 次の手は §5 に書きました
4. **staging には D-071 以後に確定したレースがありません**（確定済み 30 本はすべて 2026-08-11〜13 の生成）。検査 5 は照合する行が 0 本で、中身のある確認になっていません ✔（§3）

---

## 1. 実装 ✔（`packages/race-engine/src/lane.ts` だけ・`race.ts` は触っていない）

| 追加・変更 | 中身 |
|---|---|
| `LaneSumPlan`・`laneSumPlanOf(plan, stepM, revealFullRun, laneModel)` | 1 レース 1 回で ΣC・A・B・`maxReveal` を作ります。刻みの `run`・`reveal`・`settle` は `laneAtOnHorse` と同じ式・同じ順番で作ります（`metersLeft = distance − s` を経由）。**legacy の走り方、またはコーナーの刻みで `settle ≠ 1`（前提 P-1 が崩れる）なら `undefined`** を返します |
| `LaneRacePlan.sum?` | `lanePlanOf` が既定（`stepM` 10・`LANE_REVEAL_FULL_RUN`・`LANE_MODEL`）で作って持ちます。既定でない引数で呼んだときや、手で組んだ `plan` のときは、その場で作ります |
| `laneExtraMOnPlan` | 分解の式を使ってよい馬は `(home − centre)·ΣC + wobbleM·S·(sinφ·A + cosφ·B)` で計算し、**そうでない馬はループ**で計算します |
| 端の判定（1 頭 1 回） | `home − wobbleM·S·maxReveal > 0.8` かつ `home + wobbleM·S·maxReveal < widthM − 0.8`（`|sin| ≤ 1` として保守的に判定） |
| `laneExtraMOnPlanLoop` | ES-3 のループをそのまま残しました（**比較の基準と縮退先**）。式は変えていません |
| `laneExtraPathOf` | その馬が `'sum'` と `'loop'` のどちらを通るかを返す純粋関数です（状態を持ちません。検査はこれで経路を見ます） |
| 変えていない | `laneAt`・`laneExtraM` の署名と値、`stepM`、`LANE_MODEL`、`LANE_REVEAL_FULL_RUN`、乱数の消費、指紋の期待値ファイル、較正定数 |

- ES-4 の上限（1 レースで `lanePlanOf` 1 回・`laneExtraM` 0 回）は ES-5 の後も合格しています ✔。分解の 1 レース分は `lanePlanOf` の中で作るので、呼び出し回数は増えません

## 2. 検査 ✔

### 2-1. 検査 1 — 指紋の検査を許容差つきに書き直した（`lane-fingerprint.test.ts`）

期待値ファイル（`884019c`）は**指紋（ハッシュ）しか持っていない**ので、数の差を直接は測れません。そこで 2 段で比べます（期待値ファイルは作り直していません）。

| 段 | 中身 | 結果 |
|---|---|---|
| ① | **ループの経路**（`resolveRace` の中も `laneExtraM` の直接呼び出しも `laneExtraMOnPlanLoop`）で回した指紋が、期待値と **1 ビットも同じ** | ✔ 182 組すべて一致 → ループの経路は `884019c` の結果そのもの |
| ② | **本番の経路**（分解の式）の数を、①のループの経路の数と**許容差つき**で比べる。馬・着順・着差の言葉は完全一致、距離ロス（`order` と直接呼び出し）の差 ≤ 1×10⁻⁹ m、スコアの相対差 ≤ 1×10⁻¹²、走破タイムと着差の秒の差 ≤ 1×10⁻⁹ 秒、`laneAt` は完全一致 | ✔ 超えた件数 0。最大の差は §0 のとおり。**丸めで距離ロスが動いた馬 41,648**（＝本番の経路が本当に分解の式を通っていることの対照） |

- ループの経路を `resolveRace` の中まで通すため、**検査の中だけ** `vi.mock('../src/lane.js')` で `laneExtraMOnPlan` を切り替えます（製品のコードに切替口は足していません）
- 対照: 本番の経路の差が 1 つも無ければ②を落とします（分解の式を通っていないのに合格する形を防ぐため）

### 2-2. 検査 2〜4（`lane-sum.test.ts`・新規）

| # | 内容 | 結果 |
|---|---|---|
| 2 | 10 場・既定・半径 4 つ違い × 7 距離 × 8・18 頭 × シード 30 で、分解の式を使う馬は差 ≤ 1×10⁻⁹ m、ループに落ちた馬は 1 ビットも同じ | ✔ 分解 65,269 頭・ループ 251 頭。**最大の差 3.41×10⁻¹³ m**（回答 §2-3 の実験と同じ値） |
| 3a | 大河原 1200m で、端で止められうる馬がループに落ちる | ✔ 780 頭中 44 頭がループに落ちる（そのうち **26 頭は実際に端で止められている**） |
| 3b | 前提 P-1 が崩れる人工の形（発走直後からコーナー・`withRunUp` を通さない区間）で、全馬がループに落ちる | ✔ `laneSumPlanOf` が `undefined`、18 頭 × 30 シードすべてがループ |
| 3c | legacy の走り方はループに落ちる | ✔ |
| 4 の的 | 幅 8m の人工の走路（実際に端で止められる馬を含む）でも、分解の式はループと一致する | ✔ |

- 本番の形でループに落ちる馬は **大河原 1200m だけ**です（`out/gen/es/es5-loop-breakdown.mts`・10 場と既定 × 7 距離 × 8・18 頭 × シード 30）。検査 2 のループ 251 頭のうち、残りの 207 頭は半径 4 つ違いの形です（1200m 109 頭・1400m 98 頭。どちらも実際に端で止められる馬を含み、64 頭・61 頭）✔

### 2-3. 変異 ✔（どれも戻して、`git diff -- packages/race-engine/src` に変異が残っていないことを確かめました）

| 変異 | 落ちた検査 |
|---|---|
| ① 端の判定を外す（常に分解の式） | 検査 2（151 件）・**3a（大河原 1200m の 26 件＝本番の形で実際に端で止められる馬）**・4 の的（176 件）・指紋 ②（253 件） |
| ② `A` と `B` を入れ替える | 検査 2（65,269 件）・3a（736 件）・4 の的（904 件）・指紋 ②（87,136 件） |
| ③ `reveal` を掛け忘れる（`A`・`B` から外す） | 検査 2（35,880 件）・4 の的（540 件）・指紋 ②（47,840 件） |

- 変異 ① は、回答 §4-2 検査 4 の「本番の組に無ければ人工の形で」には当たりませんでした。**本番の組（大河原 1200m）に、実際に端で止められる馬がいます**。人工の走路（幅 8m）は、それとは別に的として足しました

## 3. 検査 5 — staging での再計算 ✔（読み取りのみ）

`npx tsx tools/verify-entrant-freeze.mjs --env staging --recompute-settled`（`--recompute-settled` の枝は `select` だけです）

```
確定済み 30 本 / 一致 5 / ★食い違い 2 / 出走馬の凍結が無く飛ばした 23
★走路の凍結が無い（0023 より前）ので DEFAULT_OVAL で再計算した行: 7 本
食い違った行: cycle=686（2026-08-12 生成）・cycle=868（2026-08-13 生成）
```

- 食い違いは既知の cycle 686・868 の 2 本だけで、増えていません
- ⚠️ **ただし、staging の確定済み 30 本は全部 D-071（`97364c5`・2026-08-16）より前の生成です**（`out/gen/es/es5-staging-rows.mjs`・読み取り専用・`app_environment = staging` を確かめてから 1 回）。**D-071 以後に確定したレースは 0 本**なので、「D-071 以後の着順が全件一致」は**照合する行が無く、成り立っているとは言えません**
- ES-5 が着順を変えないことの根拠は、§2-1 の指紋（182 組 × シード 20・着順は完全一致）です
- staging で中身のある照合をするには、D-071 以後のエンジンで staging のレースを作って確定させる必要があります（**DB への書き込みになるので、していません** → §6）

## 4. 検査 6 — 速さ ✔

報告 `REPORT_ENGINE_LANE_SPEED_20260915.md` §2-1 と同じ道具（`bench-es0.mts`・`watch-pool.json` の先頭の馬・芝・良・シード 0〜N−1・N=2000・3000m は N/2）を使い、`97364c5^` と ES-5 を**同じ時間帯に交互に 2 回ずつ**測りました。値は 2 回の範囲です。

| 条件 | `97364c5^`（D-071 の前） | ES-3（前回の報告） | **ES-5** | **ES-5 ÷ D-071 の前** |
|---|---|---|---|---|
| 16 頭 1600m 既定 | 26 | 81〜83 | **37〜39** | 約 1.4〜1.5 倍 |
| 16 頭 1600m 最長の周 | 17〜18 | 72〜85 | **25〜26** | 約 1.4〜1.5 倍 |
| 16 頭 3000m 既定 | 26〜27 | — | **40〜42** | 約 1.5〜1.6 倍 |
| 16 頭 3000m 最長の周 | 15 | — | **22** | 約 1.5 倍 |
| 18 頭 1600m 既定 | 17〜18 | — | **23** | 約 1.3〜1.35 倍 |
| 18 頭 1600m 最長の周 | 16〜17 | — | **22〜24** | 約 1.3〜1.5 倍 |
| 18 頭 3000m 既定 | 16〜18 | 128〜131 | **27〜29** | 約 1.5〜1.8 倍 |
| 18 頭 3000m 最長の周 | 16〜17 | 109〜111 | **22〜24** | 約 1.3〜1.5 倍 |

- 各条件の最初の行（16 頭 1600m 既定）は、どの版でも他より遅く出ます（予熱が 5 回と短いため）。比は同じ道具どうしで取っています
- **8 条件のうち 1.3 倍以下に収まった条件はありません**（18 頭 1600m の下端が 1.3 倍ちょうど）

### 4-1. 内訳 ✔（`bench-es5-split.mts`・予熱 300 回・N=2000・2 回）

| 項目 | `97364c5^` | ES-3 | **ES-5** |
|---|---|---|---|
| 16 頭 1600m **直線**（距離ロスを通らない） | 15.8〜16.7 | 19.2〜19.3 | 14.9〜19.4 |
| 16 頭 1600m 楕円 | 17.5〜18.6 | 51.7〜54.1 | **25.4〜26.7** |
| 18 頭 3000m 直線 | 17.1〜17.5 | 18.8〜21.3 | 17.0〜17.8 |
| 18 頭 3000m 楕円 | 17.0〜18.2 | 100.8〜106.7 | **25.8〜27.6** |
| `lanePlanOf` 1 回（1600m ／ 3000m） | — | 2.1 ／ 2.2〜2.7 | **4.1〜4.5 ／ 7.1〜7.2** |
| `laneExtraMOnPlan` × 全馬（16 頭 1600m ／ 18 頭 3000m） | — | 29〜32 ／ 79〜82 | **1.0 ／ 1.1** |

- **直線は `97364c5^` と同じ水準**です → 残りの差は距離ロスの計算から来ています
- 楕円 − 直線 ＝ 約 8〜10µs。そのうち **`lanePlanOf` が 4〜7µs**、馬ごとが約 1µs です
- `lanePlanOf` の中身: `ovalSegments` を **3 回**（区間そのもの・`swingScale` の中の `totalTurn(distance)`・`totalTurn(1600)`）と、分解の和のループ

## 5. 次の手（開発側の案・判断はレビュー側）

リポジトリの外（スクラッチに写したエンジン）で 2 つ試しました。**リポジトリのコードには入れていません。** 数は `bench-es0.mts` を `97364c5^`・ES-5・試した版の順に交互に流し、2 回の範囲を取ったものです。

| 案 | 中身 | 結果（`97364c5^` との比） |
|---|---|---|
| (f-2) `lanePlanOf` の中の重複を 1 回にする | `ovalSegments` を 3 回 → 2 回（`totalTurn(distance)` を作った区間から数える。値は 1 ビットも同じ） | ✔ **効きませんでした**（ES-5 とばらつきの範囲で同じ）。`lanePlanOf` の時間は区間作りではなく、分解の和のループにかかっています → **進めません** |
| **(g) オッズの試行の間で `lanePlanOf` を使い回す** | ワーカーの `build-race.ts` は `ODDS_MC_TRIALS` 回の試行すべてに**同じ `oddsConditions`** を渡しています。`lanePlanOf` はシードによらないので、1 レースのオッズ計算で 1 回で足ります。スクラッチでは `race.ts` に条件のオブジェクトを鍵にした `WeakMap` を置いて測りました | △ **約 1.2〜1.4 倍**（条件ごと: 16 頭 1600m 既定 1.23 ／ 16 頭 3000m 既定 1.17 ／ 18 頭 1600m 既定 1.17 ／ 18 頭 3000m 既定 1.05〜1.5 ／ 最長の周 1.2〜1.44）。**8 条件すべてが 1.3 倍以下とは言えません**（ばらつきが大きい） |
| (d) ワーカーでの並列化 | 回答 §3 のとおり | 未着手・本番機のコア数は未確認 △ |

- (g) を本物にするときの形は、レビュー側に決めていただきたいです。**`WeakMap` はモジュールに状態を持つ**ので、回答 §Q-2「状態を持たない」に反します。代わりに、`resolveRace` が作り済みの `LaneRacePlan` を任意で受け取る形（ワーカーが試行の前に 1 回作って渡す）が考えられます。この形は `apps/worker` を触るので、ES-5 の「触ってよいもの」の外です
- (g) でも `resolveRace` 1 回あたりの差は 1〜8µs 残ります。これが距離ロス以外のばらつきなのかは、切り分けていません △
- 合格線は `resolveRace` 1 回の比で決めています。サイクルの時間には試行ごとの `winningKeys` の集計なども入り、その部分は版によって変わりません。なので**サイクル全体の比は `resolveRace` の比より小さく出る**はずです △（測っていません）。合格線をサイクル全体で測り直すかどうかも、レビュー側の判断です

## 6. 触ったもの

| ファイル | 中身 |
|---|---|
| `packages/race-engine/src/lane.ts` | §1 |
| `packages/race-engine/test/lane-fingerprint.cases.ts` | 指紋の中身を数のまま返す `fingerprintRowsOf`・`digestOfRows` を足しました（`fingerprintOf` の文字の並びは同じです。§2-1 ① が期待値と 1 ビット一致することで確かめています） |
| `packages/race-engine/test/lane-fingerprint.test.ts` | §2-1（許容差つきへの書き直し・理由は註記に書きました） |
| `packages/race-engine/test/lane-sum.test.ts` | §2-2（新規） |
| 触っていない | `race.ts`・指紋の期待値ファイル・較正定数・`stepM`・`ODDS_MC_TRIALS`・`LANE_MODEL`・`LANE_REVEAL_FULL_RUN`・`packages/render`・`apps/web`・正典・DB への書き込み・ワーカーの配備 |
| git の対象外 | `out/gen/es/es5-loop-breakdown.mts`・`out/gen/es/es5-staging-rows.mjs`（読み取り専用）、スクラッチの `bench-es5-split.mts` |

## 7. 検査の全体 ✔

- `npm run typecheck` … 通過
- `npx vitest run --maxWorkers=2 --minWorkers=1` … `Test Files 1 failed | 174 passed (175)` ／ `Tests 1 failed | 1780 passed (1781)`・終了コード 1。落ちたのは変更前からの `edit-grammar-audit` ⑨（独走代表 expected 'contest' to be 'solo'）で、1 件から増えていません（ES-4 の時点は 1775 件で、今回の検査で 6 件増えました）
- ES-5 の 3 ファイル（`lane-sum`・`lane-fingerprint`・`lane-plan-count`）だけを流すと 10 件合格・約 2 秒です（指紋の検査は ES-3 の時点より速くなりました。中で回す `resolveRace` 自体が速くなったため）

## 8. オーナー・レビュー側に決めていただくこと

| # | 判断 |
|---|---|
| E-5 | §5 のどれに進むか（開発側だけでは進めません） |
| E-6 | staging で D-071 以後のレースを作って確定させ、検査 5 を中身のある照合にするか（staging への書き込みになります） |
| F-2 | `0021`・`0022` を本番に先に当てるか（前回の報告 §2 のまま・未回答） |
| F-3 | push（未回答） |
