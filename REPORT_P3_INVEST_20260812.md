# REPORT_P3_INVEST_20260812 — ②③ の投入 / Q-P3-40 の3分類 / V-10 を実際の馬券で

## 本便で正典の改訂が要る決定をしたか

**していません。ただし正典 §13.1 の記述の更新が要ります**（§2 の `RACE_RANDOM_K`・Q-P3-41）。

---

## 1. ②③ を投入しました

裁定どおり「まとめて1回で」入れました。

| 場所 | 内容 |
|---|---|
| `build-race.ts` | `abilityOf: (h) => h.stats` を渡す（**`PLACEHOLDER_UNLOCK` を本番経路から廃止**） |
| `main.ts` | `loadTrainingStates` を毎周読み直して `buildRace` に渡す |
| `pg-store.settleRace` | `condition` / `fatigue` を DB から読む |

★**生成側と確定側で同じ値を使う**ことが要点です。片側だけ実データにすると
「オッズを計算した馬と実際に走る馬が違う」が再発します（Q-P3-32 と同じ型）。

### ★効いていることを実測しました

`tools/diag-b6.mjs`（staging・本番と同じ呼び方）:

| | 投入前 | **投入後** |
|---|---|---|
| オッズ計算時 ÷ 確定時 の能力比 | **2.2754倍**（SD 0.3149） | **1.0000**（SD 0.0000） |
| 比が 1.00 ±1% に入る馬 | **0 / 254 頭** | **254 / 254 頭** |
| 生成時の調子・疲労 | 調子 2.98 / 疲労 **0.00** | 調子 0.99 / 疲労 **66.38**（DB は 1.07 / 65.88） |

**Q-P3-29 の乖離は完全に消えました。**

テスト 622件緑 / `tsc --noEmit` / 原本とバンドルの自己検査が**バイト単位で一致**。

---

## 2. Q-P3-40 — 3分類を当てました

`docs/MUTATION_TRIAGE.md` に全件の根拠つきで置きました。
★**分類の根拠はすべてソース中のコメント**で、こちらの判断で振り分けていません。

| 分類 | 件数 | 防御の形 | 定数 |
|---|---|---|---|
| **正典の写し** | **5** | 値照合が正しい | `PRIZE_TABLE` / `CANCEL_AFTER_START_MS` / `ODDS_CAP` / `FIELD_SIZE` / `CALIBRATED_RACE_RANDOM_K` |
| **測定条件** | **1** | 値照合が正しい | `DEFAULT_POPULARITY_TRIALS`（R-12「測定の自由変数」） |
| **★較正定数** | **15** | **振る舞いテストが要る** | `LAMBDA_STAR` ほか（下記） |

```
較正定数 15件:
  LAMBDA_STAR / NAME_TAIL_RATE / TAIL_MIX_P_DEFAULT / TAIL_MIX_M_DEFAULT /
  DEFAULT_CLASS_BAND / CLASS_PRIZE_TOP_MULT / OFF_SURFACE_ENTRY_RATE /
  TRACK_CONDITION_CDF / DISTANCE_SUIT_MIN / OFF_DISTANCE_ENTRY_RATE /
  FIELD_STRENGTH_FLOOR / FLOOR_FIELD_SIZE_SLOPE / OVERSAMPLE_RATIO /
  FLOOR_REDRAW_PASSES / PLACEHOLDER_UNLOCK
```

★`PLACEHOLDER_UNLOCK` は**本便で本番経路から廃止**したので、実質 **14件**です。
★`TRACK_CONDITION_CDF` は**ソースが分類を保留**しています（「どちらに分類するかは実測してから決める」）。較正定数の側に数えていますが、§10.4 の分布の写しなら写し側です。

### ★分類の作業中に見つけたこと（Q-P3-41）

`CALIBRATED_RACE_RANDOM_K` のソースにこう書いてあります:

> ⚠️ **較正条件（正典 §13.1 に明記）**: **素質開放率プレースホルダ(0.55〜0.85)**・
> クラス分け番組・18頭立て。

**その素質開放率プレースホルダを、本便で本番経路から廃止しました。**
つまり **K の記録された較正条件は、もう現在の実装を指していません。**
（`TAIL_MIX_*` にも「P3 で本物の育成ループが入ったら**必ず再較正すること**（R-7）」とあります。）

★**ただし再較正は要らないと見ています。** 本番の実分布で測って帯の中だからです:

```
V-4 32.32%（帯 30〜34%）/ V-5 63.26%（帯 60〜65%）/ V-6 1.00%（帯 0.5〜2%）
```

→ **動かすべきは値ではなく、記録された較正条件のほう**です。
「0.55〜0.85 のプレースホルダの上で較正した」→
「**開放率 平均71.3% / SD12.6pt の実分布の上で確認した**」。
★正典 §13.1 の記述なので**こちらでは書き換えていません**。

---

## 3. V-10 を実際の馬券で測りました（★訂正を含みます）

### ★まず訂正します

> 測れなかった理由は「本番に馬券が1枚もない」ことでした。

**そうではありません。** 前便で「原理的に測れない」と書いたのは
**実現払戻率の統計的精度**についてで、馬券の有無とは別の理由です。

1レース＝1回の確定なので分散が大きく、±1% を 3 SE で判定するには
**win で約287万レース**（1本約100秒＝**約9年**）要ります。
★**馬券を入れてもこの分散は変わりません。**

実測でも、3レース・43枚で実現払戻率は **63.72%**（設計値 82%・差 −18.28pt）でした。
**これは実装の誤りではなく、標本が3レースしかないことの現れです。**

### ★ただし、馬券を入れると分散ゼロで測れるものが増えます

**払戻の機械そのものは、券ごとに厳密に検査できます。**

```
★V-10（実際の馬券・本番経路）: PASS — 6項目すべて成立（43枚）
  ✓ ① 買い手は馬を1頭も持たない（§9.5 に当たらない）      0 頭
  ✓ ② 買った枚数がすべて記録されている                   43 / 43 枚
  ✓ ③ 的中が1枚以上ある（★0枚なら払戻を検査できていない）   3 枚
  ✓ ④ ★的中券の払戻が「購入額 × 購入時オッズ」と厳密に一致  3/3 枚
  ✓ ⑤ 外れ券の払戻は 0                                  違反 0 枚
  ✓ ⑥ 払戻の総額と PP 台帳の payout が一致               2,740 / 2,740 PP
```

★**馬を1頭も持たない買い手**を使いました。§9.5「自馬出走レースでは自馬絡みの馬券のみ」が
あるので、馬を持つ合成プレイヤーでは全通り買えません（前便で実際に `place_bet` に弾かれました）。

### ★本番経路の V-10 は、2つの分散ゼロの測定で閉じています

| # | 測るもの | 実測 | ツール |
|---|---|---|---|
| 1 | **オッズ表が正しい控除率を含むか**（`p × odds` = 1−margin） | 全券種 **±0.36pt 以内**・上限に当たった目 0 | `verify-v10-db.mjs` |
| 2 | **払戻が表どおりか**（券ごとに `amount × odds`） | **3/3 枚が厳密一致**・台帳とも一致 | `verify-v10-bets.mjs` |

→ **1 と 2 が成り立てば、実現率は定義から従います。**
実現率そのものを直接測るには数百万レースが要り、それは本番経路では取れません。

★**この測り方を V-10 の本番経路版としてよいか**、裁定をお願いします（Q-P3-34 の再掲）。

---

## 4. 照会

| # | 内容 |
|---|---|
| **Q-P3-41（新規）** | **§13.1 の `RACE_RANDOM_K` の較正条件の記述**。「0.55〜0.85 のプレースホルダの上で較正」が現在の実装を指していません（§2） |
| **Q-P3-40** | **較正定数 15件（実質14件）**に振る舞いテストを足すか。B-9 の判定はこの扱い次第です |
| **Q-P3-34** | V-10 の本番経路版の測り方（§3 の2つの分散ゼロの測定で閉じる形でよいか） |
| **Q-P3-37** | VPS に配備するか（★②③ を入れたので、配備すれば本番も整合します） |
| Q-P3-33 / 20〜31 | 未回答 |

---

## 5. 再現コマンド

```bash
npx tsx tools/diag-b6.mjs --env staging          # 乖離が消えたことの確認
npx tsx tools/verify-v10-db.mjs --env staging    # オッズ表の控除率
npx tsx tools/seed-races.mjs --env staging --races 3 --ahead 40
npx tsx tools/verify-v10-bets.mjs --env staging --races 3   # 払戻の機械
npm run build:worker && node dist/worker.cjs --selfcheck
```

---

## 6. ★オーナーへ（再掲）

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・未実施）
2. **Q-P3-37**: VPS への配備。★②③ を入れたので、配備すれば本番も
   「オッズを計算した馬 ＝ 実際に走る馬」になります。ご指示があるまで行いません
