# 調査 — 画面が 2 世代 並存し、メニューが古いほうを指している（2026-09-21）

> オーナーの指摘: **「TOP からログインを押すと、古いデザインが出る」**
> 開発側の調査。**実装していません。見た目も変えていません。**

## 1. 🔴 何が起きているか（1 行）

**外枠（`layout.tsx` の `StoryShell`）は新しく、中身は古い。そしてメニューが古いほうを指しています。**

```
apps/web/src/components/nav.tsx:16-22
  href: '/home'     ラベル ホーム        ← 旧
  href: '/vote'     ラベル レース        ← 旧
  href: '/mypage'   ラベル わたしの馬    ← 旧   🔴 /stable ではない
  href: '/train'    ラベル 育成          ← 旧   🔴 /training ではない
  href: '/exchange' ラベル 交換          ← 旧
```

⚠️ **`match` の側は、新しい道を知っています**（`/stable` `/entry` `/records` `/prizes` `/races` `/odds`）。
→ **切り替えるつもりで、切り替えていません。**
→ 🔴 **2026-09-21 に本物のデータへ繋いだ `/stable` `/training` は、URL を打たないと辿り着けません。**

## 2. 面の全数（`apps/web/src/app` の実測）

| 世代 | 判定 | 面 |
|---|---|---|
| **旧**（`components/uma/*` を使う） | 10 面 | `home` `vote` `mypage` `train` `exchange` `earn` `howto` `login` `signup` `watch-race` |
| **新**（`components/ui` を使う） | 8 面 | `entry` `odds` `prizes` `race` `races` `setup` `stable` `training` |
| どちらでもない | — | `/`（LP）`records`（17 行の薄い面）`watch` `race-next` ほか実験用 |

## 3. 🔴 旧 → 新 の対応（**ここが本題**）

| 旧 | 行数 | 新の相手 | 行数 | 判定 |
|---|---|---|---|---|
| `/mypage` | 201 | **`/stable`** | 306 | ✅ 在る（今日 本物のデータに繋いだ） |
| `/train` | 208 | **`/training`** | 622 | ✅ 在る（今日 本物のデータに繋いだ） |
| `/vote` | 266 | **`/races` ＋ `/odds`** | 119 + 58 | △ **投票の面が無い**（`/races` は一覧・`/odds` はオッズ） |
| `/exchange` | 162 | **`/prizes`** | 222 | ✅ 在る |
| `/home` | 193 | — | — | 🔴 **無い** |
| `/login` | 162 | — | — | 🔴 **無い** |
| `/signup` | 213 | — | — | 🔴 **無い** |
| `/earn` | 145 | — | — | 🔴 **無い** |
| `/howto` | 132 | — | — | 🔴 **無い** |
| `/watch-race` | 98 | `/race`（6,263 行） | | △ **入口が 5 本**（下） |

→ 🔴 **「新しい相手が無い」が 5 面**（`home` `login` `signup` `earn` `howto`）。
→ **これが切り替えを止めている理由**です。メニューを新しい道に向けると、**5 面が迷子**になります。

## 4. いくらで済むか（★選ぶのはオーナー／デザイナー）

| 案 | 中身 | 見た目の変更 | 私の見立て |
|---|---|---|---|
| **A** | メニューの `href` だけ新しい道へ（`/mypage`→`/stable` など **4 本**） | **無し**（リンク先だけ） | ⚠️ `/home` `/login` の行き先が**旧のまま**残る。**中途半端が見える** |
| **B** | A ＋ 旧 5 面を新しい外枠に**載せ替える**（中身は同じ・見た目だけ） | **有り** | 🔴 **デザイナー領域**。私は触りません |
| **C** | 何もしない（URL 直打ちで確認） | 無し | ⚠️ **オーナーが遊べません**。指摘の答えになりません |

⚠️ **A は 4 行の変更**ですが、**「動く」と「遊べる」の差**はこの 5 面に残ります。

## 5. レースの演出が横画面固定

```
apps/web/src/app/race/page.tsx:323   const W = 1280（720 と対）＝ 16:9
apps/web/src/app/watch-race/page.tsx:79   aspectRatio: '16 / 9'
race/page.tsx（HUD）                 ★1280 幅の絶対座標
```

オーナー: **「横画面にしないと見れないなら、数分おきのレースが流れ続ける UX にならない」**
→ **そのとおりです。** 縦持ちの携帯では細い帯になります。

| 案 | 中身 | 費用の見立て（★測っていません・**見込み**） |
|---|---|---|
| ① | 16:9 のまま縮める | **小**。ただし**細い帯のまま**＝指摘の答えにならない |
| ② | 縦向きの画角を作る | 🔴 **大**。HUD の**絶対座標が全部**効く（6,263 行の面） |
| ③ | 動かさず、結果と物語で見せる | **中**。ただし**別の体験**＝デザイナーとオーナーの判断 |

⚠️ **①②③ とも、私の見立てです。測っていません。**
🔴 **②を選ぶなら、デザイナーの画角が先**です（`design-always-via-designer`）。

## 6. 観戦の入口が 5 本

```
/race        6,263 行  ← ★本体（新・components/ui）
/races         119 行  ← 一覧
/watch-race     98 行  ← 旧（components/uma）
/watch                 ← 実験
/race-next             ← 実験
```
→ **どれが本物かを 1 つに決める**必要があります。**私では決めません。**

## 7. 私がやっていないこと

- **1 行も直していません。** 見た目も変えていません。
- 費用の見立て（§4・§5）は **測っていません**。**見込み**です。
- `/login` `/signup` を新しい系統で書けるかは **中身を読んでいません**（外枠の判定だけ）。
