# QUESTIONS_P3_GROWTH — 成長式に、正典が数値を与えていない箇所が5つあります

**開発側 → レビュー側** / 2026-08-10
**対象**: G-1（週進行）実装済み / G-2（調教・成長式）実装済み・**未確定値を含む**

---

## 0. 先に — ★実装前の解析で、係数の桁が式全体を決めると分かりました

指示書 §5-1「**掃引する前に、解析で決まるものを先に決めてください**」に従い、
実装前に桁を確かめました。

```
正典の BASE_GAIN=12・headroom 指数 0.7・他の係数がすべて 1 のとき
（平均的な馬・potential 480・初期 stats 151＝素質開放率 31.5%）

  調教 26週（104週齢＝デビュー）  到達率 70.2%
  調教 52週（130週齢）            90.2%
  調教 78週（156週齢＝3歳）       98.2%
  ★調教104週（182週齢）          100.0%   ← 以降78週は伸び 0.00
```

**調教できるのは 78→260週の182週**なので、**後半の約78週は調教しても一切伸びません。**

★§7.1 は「**調教指示を出さない週は軽め調整扱いで成長が鈍る — これがデイリー来訪の動機**」
  と書いていますが、**3歳以降はその動機が消えます。**

→ **他の係数の典型値が 0.3〜0.5 程度でなければ、キャリア後半の成長が消えます。**
  正典が数値を与えている2つ（**軽め調整 0.3 / 追い切り 1.6**）は、ちょうどその範囲を挟んでいます。
  **この見立てを前提に暫定値を置きました。掃引はしていません。**

---

## 1. Q-P3-1【最重要】主効果の係数が正典にありません

§7.2 の表は「坂路: **SP+, PW+**」のように**どの能力に効くか**は書いていますが、
**大きさが書かれていません。** 数値があるのは `追い切り 1.6` と `軽め調整 0.3` の2つだけです。

```ts
export const MAIN_EFFECT_COEF = 0.45;  // ★暫定（較正定数として登録）
export const SIDE_EFFECT_COEF = 0.08;  // ★暫定
```

- **主効果 0.45**: §0 の解析から「0.3〜0.5」の中央。追い切り(1.6)と軽め(0.3)の間に収まる
- **副効果 0.08**: ★**0 にしませんでした。**0 だと「坂路だけ続けた馬は ST/GT/IQ が初期値のまま」になり、
  **1形質だけ極端に伸びた馬**が量産されます。正典に規定はありません

**この2つでよいか、それとも別の値をお決めになりますか。**

---

## 2. Q-P3-2 気性難／温順の閾値がありません

§7.3: 「気性難は不安定 `rand(0.5,1.3)` / 温順 `rand(0.9,1.1)`」
`temper` は **0..100（高いほど気性難）・創始水準 50**（D-009）ですが、**境界が未定義**です。

```ts
export const TEMPER_DIFFICULT_AT = 50;  // ★暫定（創始水準を境界に置いた）
```

★**中間帯を作りませんでした**（正典が2分類しか書いていないため）。
  結果として **temper 49 と 50 で成長のばらつきが不連続に変わります**。
  これを許容するか、中間帯を設けるかの判断をお願いします。

---

## 3. Q-P3-3 conditionCoef の対応がありません

§7.3 は幅（**0.7〜1.3**）だけを与えており、`condition` 0..5 との対応がありません。

```ts
conditionCoef(c) = 0.7 + 0.6 * c / 5   // ★線形（暫定）
```

---

## 4. Q-P3-4 成長曲線の補間と104週未満

§7.3 の表は **104 / 156 / 208 / 260週の4点**のみです。

- **点間**: ★**線形補間**にしました。階段にすると156週で係数が跳ね、
  **同じ調教で伸びが不連続に変わります**
- **104週未満**: ★**104週の値を使い、外挿しません。**
  外挿すると `late_bloomer` は 78週で**負**になり、**調教すると能力が下がります**

調教は78週から可能（§7.1）なので、**78〜104週の26週間の扱い**を確定させてください。

---

## 5. ★Q-P3-5【重要】故障で potential が下がったとき、current をどうしますか

§7.5 は「**恒久ダメージは potential を削る**」（中度 SP −3% / 重度 全体 −8%）と定め、
§7.3 は「**current が potential を超えることは絶対にない**」と定めています。

★**両方を満たすには current を下げるしかありませんが、そうとは書かれていません。**

実測（いまの実装）:

```
故障前 current = 500 / 重度故障で potential 500 → 460
  次の週進行で ★current が 500 → 460 に落ちる（40 減）
```

★**しかもこれは「成長関数の副作用」として起きます**（故障処理の中ではなく）。
  **私が黙って決めた仕様**なので、確定をお願いします。候補:

| 案 | 挙動 | 含み |
|---|---|---|
| **A（現状）** | current を新しい potential まで即座に切り下げる | 不変条件は常に成立。**故障の瞬間に実力が落ちる**（体感は分かりやすい） |
| **B** | current は据え置き、`current > potential` を一時的に許す | **不変条件が破れる**。B-4 の意味が変わる |
| **C** | current は据え置き、成長だけ止める（上限に達した扱い） | 不変条件は「成長で超えない」に弱まる。**故障しても実力は落ちない** |

★**A は「重度故障で能力が 8% 即減」を意味します。** 正典は「**理不尽な全損を作らない**」と
  書いているので、**8% 即減が意図どおりかの確認**が要ると考えます。

---

## 6. 実装済みのもの（B-4 の防御を含む）

### G-1 週進行（`packages/scheduler/src/week.ts`）

- **★週番号は時刻から決まる関数**（P2 の `cycle_index` と同じ設計）
- **★1週 = 4時間 = 10分サイクル24本ちょうど** → `CYCLE_MS` から導出し、
  **週境界が必ずサイクル境界に一致する**ことをテストで固定
- `weeksToProcess()`: 止まっていた間の週を**すべて**返し、処理済みは**二度と**返さない
  - ★飛ばす → 馬が育たない（§7.1 の「放置でも週は進む」が崩れる）
  - ★重なる → **二重に故障判定を引く**（プレイヤーの資産を毀損する）
- ★**初回（未処理）は直前の1週だけ**。起動直後に260週ぶん処理すると**馬が一瞬で引退します**

### G-2 調教8種と成長式（`packages/training/`）

- §7.2 の表（疲労・EP・主効果の対象・気性への影響）と §7.5 の menuIntensity は**正典の写し**
- **★B-4 の不変条件を実装で閉じ、変異試験で確認しました**

```
不変条件を外す（clamp を消す）→ ★3件のテストが落ちる
```

★**そしてもう1件、登録しただけで防御できていないものを見つけました。**

  `TEMPER_DIFFICULT_AT` を **200（値域外＝全馬が「温順」）** に変異させても、
  **20件すべて通りました。** 正典の `rand(0.5,1.3)` が一度も使われなくなるのにです。

  原因は、ばらつきを比べるテストが**閾値そのものを使って両側を作っていた**ことでした。
  **閾値を動かすとテストも一緒に動きます**（D-018 で潰した自己検出の形）。
  → 値域のリテラル（0 と 100）で両端を置き直し、**2件が落ちる**ようになりました。

★**捕まる理由も特定しました。** `headroom` が上限近くで伸びを潰すので、
  **clamp が無くても大きくは超えません**。clamp の実際の役割は
  **「上限をぴったりにする」**ことでした。**最初は1件しか落ちず**、
  「総当たり」テストが**伸びの小さい地点から始めていて discriminate していない**と分かったので、
  厳密等価で見るテストを足しました。

---

## 7. 進め方

**Q-P3-1〜5 の回答を待たずに G-3（疲労と調子）・G-4（故障）・G-5（イベント）を進めます。**
暫定値は較正定数として登録済みで、**回答が来たら差し替えるだけ**です。

★ただし **Q-P3-5 だけは挙動の分岐が大きい**ので、**G-4（故障）の実装前に**回答をいただけると
手戻りがありません。
