# REPORT — 参考映像との突合

日付: 2026-08-22 ／ 開発側 ／ ブランチ `p4/race-30sec-cuts`

## 何をしたか

`（参考映像・リポジトリ内の素材ディレクトリ）`（2:17.49 / 1920×1080 / 実機筐体のスマホ撮影）を
小間切れにし、**我々の現行 30 秒版を同じ間隔で撮って並べました。**

- 全体構造: `out/judge/ref-grid.png`（4 秒刻み 34 枚）
- レース本体: `out/judge/ref-race1.png` / `ref-race2.png`（38〜124s を 2 秒刻み）
- 寄りカット: `out/judge/ref-size.png`（62s / 90s / 104s）
- 引き・俯瞰: `out/judge/ref-high.png`（67s / 69s / 74s / 85s / 87s）
- 我々: `out/judge/ours-race.png`（8〜42s を 2 秒刻み 18 枚）

⚠️ 参考は**実機の画面をスマホで斜めから撮ったもの**です。色・シャープさ・台形歪みは
撮影由来なので、そこを比較の材料にしていません。**構図・画面に映っているもの・情報表示**を見ています。

---

## 1. 骨格は合っています

参考のレース本体（38〜124s ＝ 86 秒）の並びと、我々の 30 秒版の並びは**同じ順序**です。

| | 参考 | 我々 |
|---|---|---|
| ゲート正面 | 38〜44s | 8〜12s |
| 1 コーナー | 45〜50s | 14s |
| 向正面（横） | 50〜64s | 16〜18s |
| 3〜4 コーナー（引き） | 66〜88s | 20s |
| 直線（横） | 90〜110s | 22〜26s |
| 正面・ゴール | 94〜112s | 28〜42s |
| 勝馬 | 114〜122s | あり |

★内埒を馬の手前に描く（参考 90s で白い柱が馬を隠す）——**実装済みで一致**しています。

---

## 2. 再現できていないもの（重い順）

### ① 画面が芝で埋まっている ← ★最重要

画面に占める**芝の緑**の割合（額縁を外して実測）:

```
参考   3.5%  /  23.8%  /  33.7%      （平均 約 20%）
我々  44.3% 〜 85.0%                  （21 枚平均 約 60%）
```

**3 倍**です。参考の画面には常に
**ダートコース・内埒の生垣・外埒・スタンド・観客・木立・照明塔・ゴール板**が入っています。
我々は**芝と生垣と暗い帯**しかありません。

> ★これが、俯瞰・後方カットが 5/5 で不合格になった本当の理由だと考えます。
> 参考の 67s / 85s は**馬が画面の 10% ほどしかない**のに絵として成立しています。
> 成立させているのは馬ではなく**背景の情報量**です。
> つまり不合格の原因は**画角ではなく、引いたときに映すものが世界に無いこと**でした。
> （`world-textured.ts` の「引くと世界が空になる」問題は、俯瞰カットを落としたことで
> 棚上げになっていましたが、**棚上げにできる問題ではありませんでした**。）

### ② 馬が半分の大きさ

寄りカットでの馬体の高さ（画面の高さに対する割合。参考は画面上で当てた実測なので ±3pt 程度）:

```
参考  62s 約 43%  /  104s 約 55%
我々  fourth-corner-front 21.6%  /  homestretch-side 25.1%  /  front-close 27.4%
```

**およそ 2 倍**の差です。参考の寄りは 1 頭が画面の半分を占めます。

### ③ 馬名プレートが無い

参考は画面下部の黒帯に `4 コマノインパルス` `5 ダンビュライト` `7 ランガディア` を常時出しています。
x 位置は 62s と 104s で**同一＝固定席**です。筐体の**着席プレイヤーごとの枠**と読めます。
我々は右上の順位表だけで、**画面の中の馬と名前が結びついていません**。

### ④ 上部の走行順ストリップが無い

枠色の丸バッジ（枠番入り）が画面上部に並びます。**着順に並んでおり、67s と 69s で並びが変わります**。
＝ 生きた順位表示。我々にはありません。

### ⑤ モーションブラーが無い

参考 104s は馬体が完全に流れる一方、**埒は止まっています**。カメラぶれではなく**被写体ブラー**です。
我々は 0 です。速度感の差の大きな部分がここにあります。

### ⑥ ブランド転換演出が無い

参考は 74s に**全画面のロゴワイプ**（オレンジ地に馬頭の線画）を挟み、向正面から直線へ切り替えます。
我々はハードカットのみです。

### ⑦ 芝の縞刈りが無い ／ ⑧ スタンドが暗い帯で済んでいる ／ ⑨ ダートコースが無い

---

## 3. 数え方の但し書き（R-30）

- 緑の割合は `g > r*1.05 && g > b*1.05 && g > 40` で数えました。参考は額縁を外すため
  内側 88%×72% に、我々は上下 10% を外して測っています。**同じ判定式**です。
- 馬体の高さは、参考については**画面上で当てた実測**です。自動計測していません（撮影由来の
  ぼけで輪郭が出ないため）。★数値の精度はそこまでです。
- カット検出は 5fps のフレーム差で試みましたが、**撮影のぶれで中央値 16.2** となり
  カットとぶれを分離できませんでした。カットの位置は**目視**です。

---

## 4. 判断を仰ぎたいこと

①（世界に物が無い）は**背景の作り直し**で、量が大きく、②〜⑥のどれよりも効きます。
順番の指示をください。既に出している**コース曲率の 600m 不連続（A 幾何／B カメラ）**も未裁定です。
