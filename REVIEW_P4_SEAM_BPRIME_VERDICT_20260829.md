# 裁定 — (b′) の既定切り替え／129 種の一覧

**案件**: ★**STAR**（ポイ活型オンライン競馬育成・`V:\dev\Cusor\star`）
**発行**: 2026-08-29 ／ レビュー側 → 開発側・オーナー
**対象**: `REPORT_P4_SEAM_BPRIME_20260829.md`（255 行・`bdc5b32`）／`KANJI_SINGLETONS_20260829.md`
**前の裁定**: `REVIEW_P4_A1_DIRT_CANON_VERDICT_20260829.md`（§7 Q-A2-2・§8 Q-K-2）
**ブランチ**: `p4/race-30sec-cuts`（`origin/p4/race-30sec-cuts` より ★**10 コミット先行**）

---

## 0. 判定

| # | 論点 | ★判定 |
|---|---|---|
| **①** | ★**(b′) の既定を `true` にしてよいか** | ★**採用を承認する。ただし切り替えは条件 2 つを満たしてから**（§3・§4） |
| **②** | 測定 1 が捕まえた実装バグの扱い | ★**適切。処理も守りも正しい**（§5） |
| **③** | 129 種の一覧（別字 0 件） | ★**受理。§6-1 の限界の自己申告を正典級として扱う**（§6） |
| **④** | 恒久の別字対策（Q-K-3）に着手していないこと | ★**可**（§7） |

★**総合: PASS。** ★実装・測定・報告のいずれにも差し戻す点はありません。
★①は「**やってよい／ただし D-085 が要求する形を先に整える**」という条件付き承認です。

---

## 1. 独立に再実行しました — ★5 本すべて一致

| # | 再実行 | 期待値 | ★レビュー側の実測 | |
|---|---|---|---|---|
| 1 | `_lanefocus.mjs` | 往復のずれ**なし**／注視点のずれ 最大 **−0.25m**（直線 0.00） | ★**完全一致**（最大 −0.25m・17.3s・`fourth-corner-front`・走線 4.0） | ✅ |
| 2 | `_seamslip.mjs`【4】 | 対象 **120 通り**／現行の最大 **15.7px**／(b′) **0.0px** | ★**完全一致**（現行 0.2221m=15.7px → (b′) 0.0001m=0.0px） | ✅ |
| 3 | `lane-arc-length.test.ts` | 6 件 PASS | ★**6 passed** | ✅ |
| 4 | `npm test` | 1229 件 PASS・EXIT=0 | ★**125 files / 1229 passed / EXIT=0**（127.69s） | ✅ |
| 5 | ★**既定 ON での実測** | 不変条件 3 件 PASS／反転 4（入口のみ）／共通 0 頭 0 箇所 | ★**完全一致**（下表） | ✅ |

### 1-2. ★既定 ON でこちらが測った値（報告 §4 と突き合わせ）

★`LANE_ALIGNED_FOCUS_DEFAULT` を `true` にして測り、★**測り終えたあと `false` へ戻しました**
（`git status` で `broadcast-v2.ts` に差分が無いことを確認済み）。

```
不変条件（ON）      … 3 passed（先頭が画面外 0 ／ 共通 0 頭 0 箇所 ／ 4 角出口の反転 0）
audit-cut-seam（ON） … 反転 4 箇所（★全て入口）／ 共通の馬が 0 頭 … ★0 箇所
```

| 境界 | 報告の ON | ★こちらの ON | |
|---|---|---|---|
| side-drive → fourth-corner-front | 930 | ★**930** | ✅ |
| fourth-corner-front → side-drive | 763 | ★**763** | ✅ |
| first-corner-front → side-drive | 635 | ★**635** | ✅ |
| straight-contest → homestretch-front | 473 | ★**473** | ✅ |
| side-drive → straight-contest | 295 | ★**295** | ✅ |
| homestretch-front → straight-contest | 290 | ★**290** | ✅ |
| straight-contest → finish-line | 221 | ★**221** | ✅ |
| finish-line → winner-follow | 94 | ★**94** | ✅ |
| start-front → first-corner-front | 58 | ★**58** | ✅ |
| ★全体（中央 / 最大） | 303 / 982 | ★**303 / 982** | ✅ |

★**新しい跳びは出ていません。** ★全体の中央値は 303 で不変、最大は 994 → 982 と**わずかに減っています**。

---

## 2. ★憲法 3 の照合 — 成立します

★本判定の核なので、差分をコードで読みました。

```diff
-    camera: cameraAt(focusS),
+    camera: cameraAt(cameraFocusS),
```

| 確認したこと | |
|---|---|
| 変わったのは | ★**`cameraAt()` に渡す値の 1 か所だけ** |
| `focusS` | ★**そのまま返っている**（「何 m 地点を見ているか」の意味の値は動かしていない） |
| `laneExtraMeters`（D-071・着順に効く・エンジンが引く） | ★**触れていない。** (b′) が足したのは `laneArcLengthAt` / `sAtLaneArcLength` という**描画側の別関数** |
| 比の出どころ | ★`posOf` が使う半径から導いている ＝ ★**同じ規則を 2 か所で持っていない**（R-30） |
| 縮退 | ★`Number.isFinite(aligned) ? aligned : focusS` — ★**壊れたら現行の挙動へ落ちる**（R-27） |
| 既定の引き方 | ★`options.laneAlignedFocus ?? LANE_ALIGNED_FOCUS_DEFAULT` — ★**道具に `true`/`false` を直書きしていない**（R-31） |

★**着順・タイム・着差・払戻・位置のいずれにも触れていません。**

★また、未検証 #2（`contestFocusWithLeadInFrame` が `s` で書かれている件）が割れなかった理由も
★**数字で説明が付いています**: ★注視点のずれは最大 **0.25m**、D-088 が守る上限は**画面の半幅（数十 m）**。
★**桁が 2 つ違うので破れません。** ★「通ったから大丈夫」ではなく「★**なぜ通ったか**」が書かれているのが正しい形です。

---

## 3. ⚠️ ★条件 1 — 画面からの戻し口がありません（★D-085 違反になります）

★**これが唯一の実質的な指摘です。**

★正典 **D-085** は、★**この案件自身の失敗から**次を要求しています:

> ★**既定を変えるときは「戻せる口」を同時に置き、戻せること自体を検査で固定する** —
> `PREVIOUS_RACE_SCRIPT = 'v5'` が無いと `?cinematography=v5` が**黙って既定へ落ちる**（R-27）

★[CODE] で確認したところ、★**`laneAlignedFocus` は関数の引数としてしか存在しません**:

```
packages/render/src/broadcast-v2-scene.ts:175    readonly laneAlignedFocus?: boolean;
packages/render/src/broadcast-v2-scene.ts:407    options.laneAlignedFocus ?? LANE_ALIGNED_FOCUS_DEFAULT
tools/lib/race-audit-build.mjs:240               laneAlignedFocus: opts.laneAlignedFocus ?? …
                                                 ★apps/web からの経路は 0 件
```

★**画面（`race/page.tsx`）から (b′) を切る手段がありません。**
★この案件には既に `?cinematography=v4` / `?climax=on` / `?infield=turf` / `?surface=dirt` という
★**確立した作法**があり、★`resolveBroadcastV2Scene` は `page.tsx` の 3 か所から呼ばれているので、
★**同じ形で通せます。**

### ★求めるもの

| # | |
|---|---|
| 1 | ★**画面からの戻し口を置く**（例 `/race?laneFocus=off`。★名前は開発側の裁量） |
| 2 | ★**戻せること自体を検査で固定する**（D-085 の後半。★「口を置いた」だけでは前回と同じ穴です） |
| 3 | ★**既定の切り替えは、この 2 つと同じ便で**行う |

⚠️ ★**なぜここにこだわるか。** ★(b′) は**カメラの向け先を全カットで変えます**。
★オーナーが実画面で「前のほうが良かった」と感じたとき、★**その場で比べられなければ、判断そのものができません。**
★γ/v6 のときに `PREVIOUS_RACE_SCRIPT` が効いたのは、まさにこの場面でした。

---

## 4. ★条件 2 — 切り替えと同じ便で片付けること

| # | |
|---|---|
| 1 | ★**残件台帳 A-2 を消し、報告書へ移す**（台帳の使い方 1）。★「受け入れた」ではなく「**直した**」として |
| 2 | ★正典に **D-089** として起票する。★**番号はレビュー側で確定させた**ので、そのまま使ってください（§4-1） |
| 3 | ★**既定 OFF を前提に書かれた検査・道具が無いか**を一度走査する（★D-085 の系: 既定が変わると、直書きした検査は別のものを測り始める） |

### 4-1. ★D-089（レビュー側で番号を確定・正典へ記載済み）

> **D-089** … ★**走路の折れ目でカメラと馬が別のコマで境目を越える件（残件 A-2）は、(b′)
> ＝「注視点を走線に沿った長さで置く」で直す。** ★(a)（走路に緩和曲線）は `laneExtraM` に触れるため**却下**。
> ★(b)（平滑化）は遅れが出るため**採らない**。★変えるのは `cameraAt()` に渡す値だけで、`focusS` は動かさない。

---

## 5. ② 実装バグの扱い — ★適切です

★測定 1 が `laneArcLengthAt` の `out + (s - acc)` を捕まえ、★**注視点が −410m ずれていた**ことが分かりました。

★処理として正しい点:

- ★**直したうえで、直したことを隠していない**（§2 に「私の幾何の実装が壊れていました」と明記）
- ★**往復の検査を置いた** — ★片道では正常に見え、往復で 100m のずれが出る種類の壊れ方でした
- ★**検出器が鈍っていないことを固定した**（元の壊れ方を再現した式が落ちること・★差が 100m であることまで）— ★R-21 の要求そのものです
- ★**`posOf` を刻んで足した長さと一致することも検査した** — ★実装と `posOf` が離れないことの固定（R-30）

★**裁定 §7 の順序指定（「①を先に測れ。ここが割れたら他は無意味」）が効いた**という報告は、
★こちらの意図どおりです。★**順序を指定した理由は、まさにこれでした。**

★申し送り §10-2 を支持します:

> ★**往復を見る。片道は「それらしい数字」を返す。**

---

## 6. ③ 129 種の一覧 — ★受理。ただし §6-1 が本体です

★形式は裁定 §8 Q-K-2 の指定どおりです（★字・コードポイント・出現ファイル・**字の周辺**・✅/☆ 判定）。
★**別字 0 件・直したもの無し**、★**「この語のこの字が変」と言えたものだけを直す**という指示も守られています。
★行頭 110 字で切ると肝心の字が見えない行が 10 件あったのを**自分で直している**のも正しい。

⚠️ ★**しかし本書の価値は §6-1 にあります。**

> ★**U+82DD（芝）は 38 箇所あり、この一覧には一度も出てこなかったはずです。
> この検査は、今回の実害を一件も見つけられなかったでしょう。**

★**そのとおりです。そしてこれは、私が Q-K-2 を指示したときに見落としていた点です。**

★私は「1 回だけの字は別字の温床」という開発側の読みを是として指示を出しました。★その読みは正しい。
★しかし ★**「温床である」ことと「実害を捕まえられる」ことは別**でした。
★**実害だった芝は 38 箇所あり、単発の網には最初から掛かりません。**

→ ★**正典 R-22 の適用として記録します**:

> ★**数え方が、探しているものの形と合っているかを先に問う。**
> ★「1 回だけ」を数えても、38 箇所あるものは見つからない。

★**本書の「0 件」は「別字が無い」ではなく「1 回だけの字の中には無かった」以上のことを言いません。**
★この一文が報告書に自分で書かれていることが、★**この一覧を安全に使える唯一の理由**です。

⚠️ ★**この一覧を登録簿に昇格させないこと**（前裁定 Q-K-1 の理由がそのまま効きます）を再度確認します。

---

## 7. ④ Q-K-3 — ★着手していないのは可

★恒久の別字対策は**共通土台**なので**単独の便**、という前裁定の指定どおりです。
★この便で着手していないことに問題はありません。

⚠️ ★ただし §6-1 が示したとおり、★**恒久策の設計時には「何を数えるか」を先に決めてください。**
★単発を数える形にすると、★**38 箇所ある次の別字をまた逃します。**

---

## 8. ★申し送り（★正典に上げる候補）

1. ★**往復を見る。片道は「それらしい数字」を返す。**（§5）
2. ★**数え方が、探しているものの形と合っているかを先に問う。**（§6）
   ★「1 回だけ」を数えても、38 箇所あるものは見つかりません
3. ★**新しい機構は「入っているが使われない」で一度出す。**（R-15）
   ★既定を切らずに測れるので ★**対照が取れます**。★この便はその形で出ており、実際に対照が効きました
4. ★**既定を変える提案には、戻し口の有無を必ず添える。**（§3）
   ★D-085 は「戻せる口を同時に置き、戻せること自体を検査で固定する」まで要求しています。
   ★**口を置いただけでは、前回と同じ穴です**

---

## 9. ★次の便でやること

| # | | 誰 |
|---|---|---|
| 1 | ★**画面からの戻し口 ＋ その検査 ＋ 既定の切り替え**（§3・★同じ便で） | 開発側 |
| 2 | ★台帳 A-2 を消して報告へ移す／D-089 を正典へ（★番号は確定済み）／既定 OFF 前提の検査の走査（§4） | 開発側 |
| 3 | ★**切り替え後の実画面**（(b′) が入った絵を見て、前と比べる） | ★**オーナー [EYES]** |
| 4 | ★前裁定 §11 の [EYES] 3 件（入口の ←11px/m ／ 砂煙の濃さ ／ 17.90s の軸跨ぎ） | ★**オーナー [EYES]** |
| 5 | ★決定ログの二重帳簿／push（ブランチ基準 10 コミット） | ★**オーナー判断** |
| 6 | ★恒久の別字対策（Q-K-3・単独の便） | 開発側・保留 |

---

## 10. ★レビュー側がこの便で書き込んだファイル

| ファイル | 内容 |
|---|---|
| `REVIEW_P4_SEAM_BPRIME_VERDICT_20260829.md` | ★本書 |
| `STAR_SPEC_v2.0.md` | ★**D-089 の起票**（番号の確定は正典側の作業） |

★**コミット・push はしていません**（CLAUDE.md・オーナー指示待ち）。
★`apps/web/package.json`（ポート 3210）には触れていません。
★★**既定 ON の実測で `broadcast-v2.ts` を一時的に書き換えましたが、`false` へ復元済みです**
（`git diff` で差分が無いことを確認しました）。
