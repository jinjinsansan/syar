/**
 * 測定条件（正典 §13.2 に明記された、検証の実行条件）
 *
 * 【較正定数との違い】
 *   - **較正定数**（`calibration.ts`）: ゲームの挙動を決める値。
 *     振る舞いのテストで守る（R-14）。値照合だけでは「守られている」と言えない。
 *   - **測定条件**（このファイル）: **どう測るか**を決める値。
 *     正典に固定し、**コードと正典の一致を値照合で守る**。
 *     R-14 は較正定数についての規則であって、文書化された測定条件の照合を禁じない。
 *
 * 【なぜ「免除」ではいけないか】
 *   これらは**判定を通すために動かせる値**である:
 *     - `LONGSHOT_RANKS` を 3 → 5 にすれば、より強い馬が平均に入って V-6 が上がる
 *     - `POPULARITY_TRIALS` を減らすと推定が粗くなり V-4/V-6 が動く（実測で約1pp）
 *     - 出走頭数分布を狭めると1頭あたりの勝率が機械的に上がる（Q-4 で実際に起きた）
 *   「理由付きの免除」にすると、変えても何も落ちない。**正典との一致を照合する。**
 */

/** 正典 §13.2（D-022）: V-6 が対象にする下位ランク数 */
export const LONGSHOT_RANKS = 3;

/** 正典 §13.2: 人気推定の試行数（本番確定とは別系列・§9.2） */
export const POPULARITY_TRIALS = 500;

/**
 * ★**V ゲートが見ている集団と、配備されるプールの関係**（★**PO-5**・2026-09-19）
 *   ★裁定 `REVIEW_POOL_SIZE_VERDICT_20260919.md` PO-5「1 行で書いてください」
 *
 * ★**1 行**: ★**V-4/V-5/V-6 は 400 頭の合成集団でエンジンを測り、配備は 4,389 頭中 3,000 頭を走らせる。
 *   ★同じ集団ではない。★突き合わせは `--pool <file>` で本番の集団を食わせたときだけ成立する。**
 *
 * ✔ ★数（2026-09-19・staging で実測）:
 *   ★V ゲートの既定 … ★**400 頭**（★`POOL_MARES` × `POOL_GENERATIONS` で育てた合成集団）
 *   ★配備のプール   … ★**4,389 頭**が条件に合い、★**3,000 頭**だけ読まれる（★`RACEABLE_POOL_LIMIT`・PO-2）
 *   ★市場の在庫     … ★**7,333 頭**（★世代の条件が無い・★PO-4。★うち買える候補は **732 頭**・MK-1）
 *
 * ⚠️ ★**同じである必要はありません。** ★V ゲートが測るのは ★**エンジンの振る舞い**
 *    （★1 番人気の勝率など）で、★**配備の母数の正しさではありません**。
 * 🔴 ★**ただし「違うと分かった上で使う」こと。** ★合成集団の能力の散らばりが本番と違えば、
 *    ★V-4 の値も違います。★**橋は `--pool`**（Q-P3-39・`verify-race.ts:107`）で、
 *    ★本番の集団をそのままハーネスに食わせられます。
 *   ✔ ★最後に渡したのは **2026-09-16**（★D-099: 「staging の 3,000 頭をそのままハーネスに食わせて
 *      ★V-4 32.32% / V-5 63.26% / V-6 1.00% … 全 PASS」）。
 *
 * 🔴 ⚠️ ★**この 32.32% を、今日の値と引き算しないこと**（★2026-09-19 に危うくやりました）。
 *   ✔ ★実際の実行は `docs/ACCEPTANCE_P3.md:31`:
 *     `npm run verify:race --pool docs/pool-staging.json ★--real-ability ★--b6-wired`
 *   → ★**旗が 2 つ立っています**（★能力を実データに・★調子と疲労を実データに）。★既定はどちらもオフです。
 *   → ★母集団も ★**`docs/pool-staging.json`（★2026-08-12 の書き出し）**で、★2026-09-16 の集団ではありません。
 *     ★開放率も **71.3% / SD 12.6pt**（★2026-09-19 の書き出しは **78.1% / SD 10.8pt**）。
 *   → ★★**日付・旗・母集団の 3 つが違うので、差を「この 3 日で動いた」と読めません。**
 *
 * ✔ ★**2026-09-19・AL-11 の A**（★既定の旗・★その日の staging 3,000 頭）:
 *   ★**V-4 30.21% / V-5 61.26% / V-6 1.22%**（★全 PASS。★ただし V-4 は下限まで **1.1 SE**）。
 *   → ★**以後、`--pool` の値を比べるときは「日付・旗・母集団」を 3 つとも併記すること。**
 *
 * → ★**エンジンを触った便は既定の 400 頭で、★母数を触った便は `--pool` で**測ってください。
 */

/** 正典 §13.2: 検証で使う血統プールの世代数 */
export const POOL_GENERATIONS = 40;

/** 正典 §13.2: 検証で使う繁殖牝馬プールの頭数 */
export const POOL_MARES = 400;

/** 正典 §13.3: 受け入れ判定に使うシード列 */
export const VERIFY_SEEDS: readonly number[] = [42, 7, 2026, 31337];

/**
 * 正典 §13.2: 受け入れ判定のレース数
 *
 * 🔴 ⚠️ ★**これは 4 シードの合計です**（`verify-race.ts`: `racesPerSeed = TOTAL_RACES / SEEDS.length`）。
 *   ★1 シードあたり **15,000 レース**で、★**全体で 60,000** です。
 *
 * 🔴 ★**正典 §13.2 の表（1599 行）は「1番人気の勝率（10万レース）」と書いています**（★**VP-4**・2026-09-19）。
 *   → ★★**ゲートは、正典が求めた標本の 60% で判定されています。**
 *   ⚠️ ★D-028（2026-08-07）は「レースゲートは 4 シード × 60,000 レースで最大 0.22pt しか動かず」と
 *      ★**60,000 を前提に書かれて**おり、★**正典の中で 2 つの数が並んでいます**。
 *
 * ★**余裕があるうちは効きませんでした。** ★合成 400 頭では下限まで **5.8 SE** あります。
 * 🔴 ★**しかし配備の集団では 1.1 SE です**（★AL-11 の A・2026-09-19）:
 *
 *   | | SE | 下限（30%）までの余裕 |
 *   |---|---|---|
 *   | 60,000（実装） | 0.1874pp | ★**1.12 SE** |
 *   | 100,000（正典） | 0.1452pp | 1.45 SE |
 *
 * ⚠️ ★**値は変えていません。** ★10 万にすると所要が **約 18 分**になります（★12 分 × 100/60）。
 *    ★どちらを正とするかは ★**正典側の食い違いの解消**が要ります（★レビュー側がオーナーに上げています）。
 */
export const VERIFY_RACES = 60_000;

/**
 * ★V-7 の測定条件（正典 D-049 で分割・P3 指示書 §4）
 *
 *   指示書:「分母が故障で動くので、**定義が判定を作ります**」
 *   → 測る前に確定させ、ここに登録します。**測定条件は較正定数ではありません**
 *     （`measurement.ts` に置くのは §16.3 の3分類のうち「測定条件」だからです）。
 *
 * 【★決めた経緯】
 *   旧 V-7 は「1キャリアあたりの故障発生率 25〜35%」で、**軽度（3週で治る挫石・60%）から
 *   致命的（強制引退・1%）まで全部を足していました**。割合が同じでも中身が正反対に
 *   なりえます — 全部軽度なら無害、全部重度なら破滅的。**壊れた状態が通るゲート**でした（R-16）。
 */
/**
 * ★**V-13 の測定条件**（★2026-09-16・正典 **D-112 ③**・裁定 `REVIEW_GAME_BODY_5_VERDICT_20260916.md` §3）
 *
 * 【★なぜ改めるか】
 *   ★D-112 で「暴走」を ★**率で引く**ことになりました。★同じ入力でも ★**引きの結果で散ります**。
 *   ★いままでの V-13 は ★**1 本の差（最適の平均 − 早仕掛けの平均）が 0.03 以上か**だけを見ており、
 *   ★**散り具合を持っていませんでした**。★散る世界でこの形のまま測ると、
 *   ★「たまたま差が出た／出なかった」で合否が動きます。
 *   → ★**多数回の平均で測り、差が 0 からどれだけ離れているかを SE で言う**形にします。
 *
 * ⚠️ ★**測定条件であって較正定数ではありません**（§16.3 の 3 分類）。
 *    ★通すために動かせる値ではなく、★動かすなら測り直して報告することになります。
 */
export const V13_MEASUREMENT = {
  /**
   * ★「早すぎる仕掛け」を `EARLY_SPURT_METER` の何倍地点とするか。
   * ⚠️ ★`verify-race.ts` の `GATES.V13_EARLY_FACTOR` と**同じ値**でなければなりません
   *    （★`verify-race.test.ts` 相当の検査が突き合わせます）。★ここは測定条件としての写しです。
   */
  earlyFactor: 1.6,
  /**
   * ★**標本の単位はレース**（★1 レース 1 標本）。
   *   ★1 レースの中で「同じ馬に最適と早仕掛けを当てた差」を 1 つ取ります。
   * ⚠️ ★**対標本**です（★同じ馬・同じ距離・同じ調子で比べる）。
   *    ★別々の馬の平均どうしを引くと、★馬の差が差に混ざります。
   */
  sampleUnit: 'race_paired',
  /**
   * ★**合否は「差が 0 から何 SE 離れているか」で見る**（★「有意な差」の実装）。
   * ⚠️ ★**この 3 は正典に無い値です** — ★§13.2 は「有意な差」としか書いていません。
   *    ★照会中（`QUESTIONS_GAME_BODY_5_20260916.md` に追記）。★裁定が出たら差し替えます。
   */
  minSigma: 3,
  /**
   * ★**差そのものの下限**（★効果量。★SE だけだと、標本を増やせばどんな小さな差でも有意になります）。
   * ★従来の `V13_MIN_GAP`（0.03）を効果量の下限として引き継ぎます。
   */
  minGap: 0.03,
} as const;

export const V7_MEASUREMENT = {
  /** ★キャリアの範囲: 調教開始（78週）〜引退。故障は調教で起きる（§7.5）ので78週から数える */
  fromWeeks: 78,
  toWeeks: 260,
  /**
   * ★基準となる育成方針。**指示書に無かった5つ目の条件**で、
   *   方針で 20.6%〜50.9% と2倍以上動きます。V-14 の錨と揃えて「バランス型」。
   *   放置・追い切り偏重は**参考値**として併記します。
   */
  policy: 'balanced',
  /**
   * ★率の定義: 「**そのキャリアで1回でも負ったか**」の割合（頭数ベース）。
   *   件数ベースだと 1.3件/頭 = 130%相当になり、帯（%）の書き方と合いません。
   */
  rate: 'share_of_careers',
  /**
   * ★故障確率の式に使う丈夫さ: キャリア開始時の値（D-049）。
   *   ⚠️ 現在値を使う場合との差は**実測 0.00pt** でした。
   *      重度故障は 0.11件/頭しか起きず、起きた後の残り週数も短いため、
   *      `durability −100` が将来の確率に効く機会がほぼありません。
   */
  durabilityBasis: 'career_start',
  /**
   * ★必要頭数（R-20: 1頭の一生は単一実現）。
   *   帯の端まで 3 SE を確保する頭数を逆算した値。
   *   ⚠️ 1シードの母集団は `POOL_MARES`（400頭）しかないので、**シードを積んで集めます**。
   *      以前 1,800頭を要求して黙って400頭で測っており、
   *      **SE が変わらないのに「増やした」と報告するところ**でした（R-21）。
   */
  horses: 1800,
} as const;
