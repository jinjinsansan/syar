/**
 * 測定条件（正典 §13.2 に明記された、検証の実行条件）
 *
 * 【較正定数との違い】
 *   - **較正定数**（`calibration.ts`）: ゲームの挙動を決める値。
 *     振る舞いのテストで守る（R-14）。値照合だけでは「守られている」と言えない。
 *   - **測定条件**（このファイル）: **どう測るか**を決める値。
 *     正典に固定し、**コードと正典の一致を値照合で守る**。
 *     R-14 は較正定数についての規則であって、文書化された測定条件の照合を禁じない。
 *
 * 【なぜ「免除」ではいけないか】
 *   これらは**判定を通すために動かせる値**である:
 *     - `LONGSHOT_RANKS` を 3 → 5 にすれば、より強い馬が平均に入って V-6 が上がる
 *     - `POPULARITY_TRIALS` を減らすと推定が粗くなり V-4/V-6 が動く（実測で約1pp）
 *     - 出走頭数分布を狭めると1頭あたりの勝率が機械的に上がる（Q-4 で実際に起きた）
 *   「理由付きの免除」にすると、変えても何も落ちない。**正典との一致を照合する。**
 */

/** 正典 §13.2（D-022）: V-6 が対象にする下位ランク数 */
export const LONGSHOT_RANKS = 3;

/** 正典 §13.2: 人気推定の試行数（本番確定とは別系列・§9.2） */
export const POPULARITY_TRIALS = 500;

/** 正典 §13.2: 検証で使う血統プールの世代数 */
export const POOL_GENERATIONS = 40;

/** 正典 §13.2: 検証で使う繁殖牝馬プールの頭数 */
export const POOL_MARES = 400;

/** 正典 §13.3: 受け入れ判定に使うシード列 */
export const VERIFY_SEEDS: readonly number[] = [42, 7, 2026, 31337];

/** 正典 §13.2: 受け入れ判定のレース数 */
export const VERIFY_RACES = 60_000;

/**
 * ★V-7 の測定条件（正典 D-049 で分割・P3 指示書 §4）
 *
 *   指示書:「分母が故障で動くので、**定義が判定を作ります**」
 *   → 測る前に確定させ、ここに登録します。**測定条件は較正定数ではありません**
 *     （`measurement.ts` に置くのは §16.3 の3分類のうち「測定条件」だからです）。
 *
 * 【★決めた経緯】
 *   旧 V-7 は「1キャリアあたりの故障発生率 25〜35%」で、**軽度（3週で治る挫石・60%）から
 *   致命的（強制引退・1%）まで全部を足していました**。割合が同じでも中身が正反対に
 *   なりえます — 全部軽度なら無害、全部重度なら破滅的。**壊れた状態が通るゲート**でした（R-16）。
 */
export const V7_MEASUREMENT = {
  /** ★キャリアの範囲: 調教開始（78週）〜引退。故障は調教で起きる（§7.5）ので78週から数える */
  fromWeeks: 78,
  toWeeks: 260,
  /**
   * ★基準となる育成方針。**指示書に無かった5つ目の条件**で、
   *   方針で 20.6%〜50.9% と2倍以上動きます。V-14 の錨と揃えて「バランス型」。
   *   放置・追い切り偏重は**参考値**として併記します。
   */
  policy: 'balanced',
  /**
   * ★率の定義: 「**そのキャリアで1回でも負ったか**」の割合（頭数ベース）。
   *   件数ベースだと 1.3件/頭 = 130%相当になり、帯（%）の書き方と合いません。
   */
  rate: 'share_of_careers',
  /**
   * ★故障確率の式に使う丈夫さ: キャリア開始時の値（D-049）。
   *   ⚠️ 現在値を使う場合との差は**実測 0.00pt** でした。
   *      重度故障は 0.11件/頭しか起きず、起きた後の残り週数も短いため、
   *      `durability −100` が将来の確率に効く機会がほぼありません。
   */
  durabilityBasis: 'career_start',
  /**
   * ★必要頭数（R-20: 1頭の一生は単一実現）。
   *   帯の端まで 3 SE を確保する頭数を逆算した値。
   *   ⚠️ 1シードの母集団は `POOL_MARES`（400頭）しかないので、**シードを積んで集めます**。
   *      以前 1,800頭を要求して黙って400頭で測っており、
   *      **SE が変わらないのに「増やした」と報告するところ**でした（R-21）。
   */
  horses: 1800,
} as const;
