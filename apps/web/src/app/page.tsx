/**
 * ★**TOP（`/`）＝「馬物語」の LP**（★2026-09-13・オーナー指示「★調べて移動してください」）
 *
 * 【★なぜ移したか】
 *   ★内装（牧場・調教・番組表・オッズ・投票・記録・景品・ログイン…）は、★別セッションが
 *   ★この LP に合わせて作り直し、★既に本番へ入っています（★`fcba4f2`）。
 *   ⚠️ ★ところが ★**TOP だけが入れ替わっていませんでした**。★作った側の記録にも
 *      ★「★実際の TOP `/` の切り替えは行っていない」と明記されています
 *      （`docs/horse-story-interior-redesign-20260913.md`）。
 *   → ★本番の `/` が古いまま＝「直したはずのものが見えない」状態でした。
 *
 * 【★何を残したか】
 *   ★以前の TOP は ★**消さずに `/lp-arcade` へ退避**しました。★あちらだけが
 *   ★次のレース・最新の確定レースを ★**サーバーから読んで**導線に出します。
 *   ⚠️ ★この LP にその口はありません。★戻す・足すはオーナー判断です。
 *
 * ⚠️ ★本文は `app/lp-preview/story.tsx` を ★**そのまま**使います（★写しません）。
 *    ★2 か所に置くと、★片方だけ直る形になります（★R-30）。
 *    ★`/lp-preview` は作った側の復元点なので、★残してあります。
 */
import HorseStory from './lp-preview/story';

export const metadata = {
  title: '馬物語｜この一頭から、物語がはじまる。',
  description: '育てる日々も、駆け抜ける一瞬も。あなたの馬と、世代をつなぐ競馬育成ゲーム。',
};

export default function Page() {
  return <HorseStory />;
}
