# 引継ぎ書 — レースの音 ／ TOP の入れ替え ／ 鞍布の縦割れ（2026-09-13）

> ★このセッションで何をして、何が未決かをそのまま書きます。
> ★報告は鵜呑みにされない前提です（CLAUDE.md）。数字はすべて実測です。

## 0. 位置（同一作業ツリーの作法・CLAUDE.md）

| | |
|---|---|
| ブランチ | `p4/race-30sec-cuts` |
| 開始 HEAD | `8de6d8a` 通し: ★携帯で映像が出なくなっていたのを直す ／ ★全画面の口と「メニュー」の戻り先 |
| 終了 HEAD | `4519741` 通し: ★鞍布の縦割れを直す（★8 コマ全部測ってから窓を決める） |
| タグ | `p4-race-sound-and-top-20260913` → `903a41e`（★音と TOP まで。★その後の 2 便は含みません） |
| 本番 | `main` を `4519741` へ早送り済み。`https://star-two-chi.vercel.app/` |
| 差分 | `git diff --numstat 8de6d8a..4519741` ＝ **18 ファイル ／ +1150 行 ／ −278 行**（★mp3 5 本は行数に出ません） |

### 担当範囲（このセッションが触ったファイル）

```
  6/-1   apps/cli/test/source-integrity.test.ts
   bin   apps/web/public/audio/{fanfare,gate-open,whinny,gallop,crowd}.mp3   ★新規
266/0    apps/web/src/app/lp-arcade/page.tsx                                 ★旧 TOP の退避先
  2/-2   apps/web/src/app/lp-preview/story.tsx
 23/-253 apps/web/src/app/page.tsx                                           ★TOP の中身が入れ替わった
240/-10  apps/web/src/app/race/page.tsx
206/0    apps/web/src/app/race/race-audio.ts                                 ★新規
 17/-3   apps/web/src/components/story-shell.tsx
 15/-9   apps/web/test/mobile-css-anchors.test.ts
  1/0    packages/render/src/index.ts
 45/0    packages/render/src/silks-pixel.ts                                  ★新規
141/0    tools/check-race-sound-dev.mjs                                      ★新規
 20/0    tools/lib/classification.mjs
168/0    tools/measure-silks-window.mjs                                      ★新規
```

⚠️ **触っていない未追跡ファイル**（別セッションのもの。そのまま残しています）

- `apps/web/public/art/horse-jockey-side-v9b-pose0{1..8}.png`
- `apps/web/public/art/horse-jockey-high-diag-deformed-v1-pose01.png`
- `design/art/prompts/deformed-8frames-high-diag.txt`
- リポジトリ直下の `ファンファーレ.wav` `馬が走る音.wav` `ヒひーん音.wav`
  （★オーナー支給の原本。★変換した mp3 だけコミットしています）

## 1. 不採用になったもの — 実況の読み上げ

**オーナー評「今聞きましたが、全くダメですね。すいません。実況中継は諦めます。」**

やったこと・分かったことだけ残します。**コードは削除済み**です（`race-voice.ts`、試作の道具 2 本）。

- ブラウザ内蔵の読み上げ（`speechSynthesis`）で実況を喋らせるところまで作り、開発サーバーで動くことを実測で確認した。
- **文の側に致命的な問題があった。** 実況をそのまま声にすると、1600m・seed 42 で
  **9 本中 5 本が次の文と重なる**（最悪 4.57 秒）。重なりを外して並べ直すと **46.3 秒**で、
  本編 38.7 秒に入らない。1 文 31 字＝約 4.6 秒あったため。
- 文法を書き直せば入ることは確認した（15 字・10 本・重なり 0・4 seed）。
  **その書き直しはオーナー判断で元に戻しています。**
- **残す判断**: 中身が毎回変わるもの（馬名は生成物）を端末任せの読み上げに乗せると、声も読みも選べない。
  音を入れるなら **中身が固定のもの**に限る、というのが今回の結論です。

## 2. 入ったもの

### 2-1. レースの音（`bcdc3fe`）

オーナー支給の音源を移植しました。`apps/web/public/audio/` に mp3（計 2.5MB）。

| 場面 | 音 | 実測の発火秒（開発サーバー） |
|---|---|---|
| イントロ | `fanfare.mp3`（16.8 秒） | 10.62 |
| ゲートが開く | `gate-open.mp3`（1.8 秒）＋ `gallop.mp3`（39.6 秒） | 15.69 |
| その直後 | `whinny.mp3`（2.1 秒） | 16.09（ゲートから +0.40） |
| 勝ち馬がゴール | 走行音を絞り `crowd.mp3`（100.6 秒）へ | 46.27 |

- 原本は直下の 3 本と `dev/Cusor/sevendays/音声ファイル/` から。
  直下の 3 本は sevendays 側と**同じ尺**でした（同一音源と判断）。
- **既定は切**。`?sound=1` か画面の「音」ボタン。
  ⚠️ ブラウザは人の操作なしに音を出さないので、押下が解錠を兼ねます。
- 音源が無ければ**黙って鳴らしません**（404 で画面を壊さない）。
  例外は外へ出しません（毎コマの描画の輪から呼ぶため）。
- ⚠️ **ファンファーレは 16.8 秒、イントロは 4.4 秒。頭の 5 秒で絞ります。**
  オーナー了承済み（「今のままでＯＫです」）。全部聴かせるならイントロを伸ばす（全体 45 → 57 秒）。
- 道具: `tools/check-race-sound-dev.mjs`（`AudioContext` を差し替えて発火秒を数える。合否は出さない）

⚠️ **使っていない支給音源**が sevendays にあります:
`フィナーレ`（着順確定）／`馬が来る音`・`馬が去る音`（カットイン）／`レース演出ループ音`。

### 2-2. TOP を「馬物語」の LP に（`3e8c5d1`）

**オーナー「codex に TOP を修正してもらったものが本番で確認できません　なぜ？」への答え:
codex は TOP を触っていませんでした。**

実測（本番）で分かったこと:

```
/                      旧アーケード LP のまま（story の印なし）
/lp-preview            「馬物語」の LP はここに置かれていた
/training /stable /races /records /prizes /login   内装の塗り替えは既に本番に入っていた
/design-preview/odds   404 ← 正常（codex が意図的に本番で 404。架空オッズを実データと誤認させないため）
```

作った側の記録にも明記されています — 「実際の TOP `/` の切り替えは行っていない」
（`docs/horse-story-interior-redesign-20260913.md`）。

やったこと:

1. `/` → 「馬物語」の LP。**本文は `app/lp-preview/story.tsx` をそのまま使います**（写していません・R-30）。
2. **旧 TOP は消さず `/lp-arcade` へ退避。**
   ⚠️ **あちらだけが「次のレース・最新の確定レース」をサーバーから読んで導線に出します。
   新 LP にその口はありません。**
3. `/` と `/lp-preview` からアーケードの「STAR」帯を外した
   （`/lp-preview` には**帯が二重に載っていました**）。
4. 内装ヘッダーと LP のブランドの行き先を `/lp-preview` → `/` に。

#### ⚠️ 「他のページが以前のまま」について（未決）

オーナー指摘を受けて本番を計測しました。**塗り替えは効いています**:

```
/stable /races /records /training
  body の背景 = rgb(248,247,239)（馬物語のアイボリー）／ .story-shell あり
```

変わっていないのは**構造**です。codex が触ったページファイルは **5 つ**:

| ページ | 変更の中身 |
|---|---|
| `races/[id]/odds` | **本体を作り直し**（198 行を `components/odds-board.tsx` へ切り出して再構成） |
| `races/[id]/bet` | `className="story-tab"` を足す（1 行） |
| `stable/[horseId]` | 馬名を `h1.story-horse-name` に（1 行） |
| `training` | `className="tr-horse-heading"` を足す（1 行） |
| `design-preview/odds` | 新規（開発環境限定の見本。本番は 404） |

`story-theme.css` が `.a-panel` `.a-btn` `.a-row` `.a-band` `.a-chip` `.a-num` など
**旧アーケードの部品を軒並み上書き**する「塗り替え」方式です。
**牧場・番組表・記録・景品・ログインの構造を作り直すかは未決です。**

### 2-3. 中継の見え方（`0a464c9` / `903a41e` / `d9cd70f`）

オーナー評への対応です。

| 指摘 | やったこと |
|---|---|
| 道具ボタンが邪魔で見にくい | 触れてから **2.5 秒**で薄れて消える。触れば戻る。⚠️ **消しません**（消すと全画面にも音にも入れなくなる） |
| ブラウザの URL バーが残る | **ステージに 1 度触れた時点で全画面へ入る**。⚠️ 全画面は人の操作からしか入れない（自動開始では入れない）。⚠️ **iOS Safari にはこの口が無く、そこだけはバーが残ります** |
| ファンファーレが鳴ったり鳴らなかったり | **原因**: 音源を「音」を押した時に取りに行っていた。出番はイントロの **4.4 秒**しかなく、読み込みが間に合わない回は一度も鳴らない。→ **画面を開いた時点で取りに行く**。イントロが過ぎてから音を入れた回は札だけ立てて鳴らさない |
| 中継を押すと青くて STAR と出る（モバイル） | **旧アーケードのヘッダー帯**でした。`/race` を `OWN_HEADER` に入れて外した。地色を玄関と同じアイボリーに（⚠️ 開発卓 `?dev=1` は**暗いまま**。映像の色を見る面なので） |
| （同上）20 秒ほど白紙 | 「馬物語 ／ レースの用意をしています…」と戻る口を出す |
| 音を鳴らす道具がない | **私の入れ忘れ。** 「音」は開発卓と携帯の全画面にしか無く、机の画面では `?sound=1` を手で付けるしか鳴らせなかった。→ 画布の上に「♪ 音」「全画面」「もう一度」を置いた |

### 2-4. 鞍布の縦割れ（`4519741`）

**オーナー評「本番環境で騎手の服がまた縦縞模様です」。原因は私の 2026-09-12 の修正です。**

- 「馬の目が赤色になる」を直すため、鞍布の窓の右端を 0.95 → **0.40** に詰めた。
- ⚠️ **そのとき測ったのは 1 コマ（pose01）だけ。**
  鞍布は 1 つの塊ではなく、**騎手の脚で左右 2 つに分かれて**写る。測ったのは左側だけで、
  右側（**nx 0.30〜0.45・800〜1100 画素**）は窓の外だった。
  → 右側が元の白のまま残り、縦に割れて見える。

今度は 8 コマ全部測りました（`tools/measure-silks-window.mjs`・`--paint` で絵も出ます）:

```
鞍布 … nx 0.20〜0.45 ／ ny 0.28〜0.48（8 コマの合併・200 画素以上の塊）
目   … nx 0.61〜0.69 ／ ny 0.31〜0.40（pose05・pose07 で観測）
→ SILKS_LAYOUT_FRONT.saddlecloth = [0.05, 0.48, 0.30, 0.50]
確かめ: 新しい窓で新たに塗られる塊 41 個はすべて鞍布とその縁。
        目の大きさの塊（nx 0.61〜）は 1 つも入らない
```

**同じ失敗を繰り返さないための手当て**: 塗ってよい画素かの判定が `page.tsx` に直書きで、
道具は**別の式**で測れてしまう形でした。`packages/render/src/silks-pixel.ts` の
`silksPaintable` に寄せ、**画面も道具も同じ関数**を通します（R-30）。

真横（`side-v8` ＋ `SILKS_LAYOUT_CROUCH`）も同じ道具で測り、鞍布は窓の中でした（**変更なし**）。

## 3. 検証結果

```
npm run typecheck   通過
npx vitest run      1 failed | 1600 passed (1601)
```

**唯一の赤**: `packages/render/test/edit-grammar-audit.test.ts` ⑨「seed 分類を現 HEAD で再確認している」
→ `独走代表: expected 'contest' to be 'solo'`（seed 474 の分類が変わったまま）。
**このセッションより前からの赤**で、**代表 seed を採り直すかのオーナー判断待ち**です。

このセッション中に**自分で割って直した赤**（記録として残します）:

| 赤 | 原因 | 直し方 |
|---|---|---|
| `source-integrity`「NUL バイトが無い」 | **音源 mp3 を入れたとき、バイナリの拡張子に書き足すのを忘れた**（crowd.mp3 だけで NUL 20,196 個） | `BINARY_EXT` に mp3 / wav / m4a / ogg を追加 |
| `mobile-css-anchors` | TOP を `/lp-arcade` へ移したので、照合先のファイル名が消えた | `SCOPE` を `app/lp-arcade/page.tsx` に更新 |
| `tool-guard`（★別セッション由来） | `check-lp-preview.mjs` / `check-story-interiors.mjs` が分類簿に未登録 | 実測で DB に触れないことを確認し登録 |

## 4. 事故と、その原因

引継ぎ先が同じ穴に落ちないように残します。

1. **開発サーバーを 2 本立てて、両方壊した。**
   3210（オーナー）と 3211（私）が**同じ `apps/web/.next` を共有**し、互いのビルド出力を壊し合いました。
   3210 の `main-app.js` が 404 になり React が起動しない／3211 の `/race` が 404、はどちらもこれです。
   → **同じプロジェクトで `next dev` は 1 本だけ。**
   両方止めて `.next` を消し、3210 に 1 本だけ立て直しました。
2. **計測側の偽物で「画面が壊れている」と誤報しかけた。**
   `speechSynthesis` を差し替えるとき、**本物の `SpeechSynthesisUtterance` を残したまま偽の声を代入**して
   TypeError を出し、「1 度も喋らせていません」と報告しかけました。画面は正しく動いていました。
   → 差し替えるなら**関連する型ごと**差し替えること。
3. **`/race` はローカルでは既定で開発卓になる。**
   `process.env.NODE_ENV === 'development' && params.get('dev') !== '0'` で `devMode` が入ります。
   遊ぶ人の面を見たいときは **`?dev=0`** を付けてください。付けずに測って 2 回混乱しました。

## 5. 未決（オーナー判断）

1. **内装 5 ページ（牧場・番組表・記録・景品・ログイン）の構造を作り直すか。** いまは塗り替えのみ。
2. **新 LP に「次のレース」への導線を足すか。** いまは `/lp-arcade` にしかありません。
3. **`edit-grammar-audit` ⑨ の代表 seed を採り直すか。** 唯一の赤。
4. **ファンファーレを全部聴かせるためにイントロを伸ばすか。** いまは頭の 5 秒（オーナー了承済み）。
5. **未使用の支給音源を足すか**（フィナーレ／馬が来る・去る／ループ音）。
6. ⚠️ **デモ馬名 `ハンシンドリーム` は実在競馬場名を含みます**（憲法 1）。
   `apps/web/src/app/race/page.tsx` の `HORSE_NAMES`。差し替えをご検討ください。

## 6. すぐ使える道具

```bash
node tools/check-race-sound-dev.mjs    --base http://localhost:3210 --sec 42      # 音の発火秒
npx tsx tools/measure-silks-window.mjs --set horse-jockey-diag-front-v4 --paint   # 勝負服の窓（絵も出る）
npx tsx tools/measure-silks-window.mjs --now "0.05,0.48,0.30,0.50"                # 窓を変える前に必ず
```

⚠️ どれも**合否を出しません**。数字を出すだけです（閾値はオーナー判断・R-16）。
