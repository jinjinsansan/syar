# 照会 — Q-UI-1: 初期馬を誰がどう選ぶのか（★web からは候補を列挙できません）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**きっかけ**: UI-1（★画面を RPC に繋ぐ便）の `/setup` を配線しようとして止まりました
**関連**: **D-074**（初期馬の付与）／ **D-079**（帯・候補集合）／ **D-114**（素質を見せない）／ `0031_setup_account.sql`

> ★**配線だけを進める前提でしたが、繋ぎ先が成立していません。★推測で埋めずに出します。**

---

## 0. 🔴 何が起きているか

★`create_account` は ★**初期馬の id を引数で受け取ります**。

```sql
create or replace function create_account(
  p_display_name text, p_stable_name text, p_silk_color text, p_silk_sleeve text,
  -- ★呼ぶ側（ワーカー）が★で選んだ馬。★SQL で★を再計算しない
  p_horse_id uuid,
  p_client_token uuid
)
```

★**しかし、画面（web）はその id を得られません。**

| # | 経路 | なぜ使えないか |
|---|---|---|
| 1 | `horses` を読んで候補を探す | ✔ ★**`horses` は登録簿で `CLOSED`**（★`potential`/`genotype` を持つため）。★画面からは 1 行も読めません |
| 2 | `my_horses`（`0034`）を読む | ✔ ★**`owner_id = auth.uid()` で絞っています** — ★**まだ持っていない馬は入りません** |
| 3 | `is_initial_horse_candidate(id)` を呼ぶ | ✔ ★**「その id が候補か」を検証するだけ**です。★**列挙できません**（★id を知らないと呼べない） |

→ ★**いまの形では、`/setup` から `create_account` を呼べません。**

---

## 1. ★そして「呼ぶ側が選ぶ」は、2 つの決定と噛み合っていません

| # | |
|---|---|
| 1 | ★**D-074**: 「★**初期馬は user_id から決定的に導く — 画面で抽選しない。付与はサーバー側**」。★**画面が選ぶ形は、この決定に反します** |
| 2 | ★**D-114**（2026-09-18）: ★**素質を見せない**。★`0031` の註記は「★**呼ぶ側が★で選んだ馬**」と書いていますが、★**★はもう外に出ません**。★「★で絞る」は**サーバーの内部の話**になりました |

⚠️ ★`0031` を書いた時点では「呼ぶ側＝ワーカー」を想定していました（★註記にそう書いてあります）。
★**ワーカーが先に付与する形なら成立しますが、★その経路は実装されていません**（✔ 確認しました）。

---

## 2. ★選べる形（★開発側では決めません）

| 案 | 中身 | 良い点 / 気になる点 |
|---|---|---|
| **A** | ★**`create_account` が自分で選ぶ**（★`p_horse_id` を省略可にし、★null なら **`auth.uid()` から決定的に**候補の中から選ぶ） | ✅ ★**D-074 の「サーバー側で決定的に」そのもの**。★画面は 1 本呼ぶだけ ／ ⚠️ ★**新しい移行**が要る（`0031` は書き換えない）。★第三の登録簿の 3 条件を保つこと |
| **B** | ★**候補を 1 頭返す RPC を足す**（`pick_initial_horse()` → uuid）。★画面はそれを `create_account` に渡す | ⚠️ ★**画面が 2 本呼ぶ**ことになり、★**間に割り込まれると別の馬になりえます**（★冪等の穴） |
| **C** | ★**ワーカーが先に付与する**（★登録を検知して割り当てる） | ⚠️ ★**登録から馬が付くまでに間**が空きます（★「最初の 1 頭が来ない」は D-074 が避けたかったこと） |

★**開発側の見立ては A** です — ★**画面は「何を選ぶか」を知らなくてよい**形になり、★D-074 とも D-114 とも噛み合います。
★**選び方（どの候補を引くか）は正典 D-079 の帯の話**なので、★**規則そのものはレビュー側・オーナーに決めていただく必要があります**
（★いまは「未出走の NPC 馬」まで決まっており〔CL-5'〕、★**その中からどう 1 頭に絞るか**が空いています）。

---

## 3. ★この照会が塞いでいるもの

| # | |
|---|---|
| 1 | 🔴 ★**`/setup` の配線**（★登録 → セットアップの経路そのもの） |
| 2 | ★`my_horses` が「自分の馬だけを返すこと」の**実測**（★口座と所有馬が要るため・`REPORT_UI_WIRING_ESTIMATE` §6 の △） |
| 3 | ★`enter_race` の**資格判定の実測**（★同上） |

★**塞がっていないもの**: ★`/entry` の**レース一覧と登録の配線**（★公開ビューと `enter_race` の呼び出し自体）。★**そちらは先に進めます。**

---

## 4. ★開発側が待つ間にやること

★**`/entry` の配線**（★レース一覧を `races_public` から・★登録を `enter_race` に・★`ST001`/`ST002` の文言）。
⚠️ ★**見た目は触りません**（★2026-09-15 のオーナー指示・デザインはデザイナー経由）。★**デモデータを本番データに差し替えるだけ**です。
