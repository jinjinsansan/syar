# 【STAR】裁定: 自分の繁殖牝馬で配合する経路（`kind = 'breed'`・相手は NPC の種牡馬だけ）— 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_BREED_OWN_MARE_20260922.md`（開発側 `star-aa`・`9676f17`）
- 運用: オーナー指示「承認不要で自立的に進める」

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| 骨子 | 確定は `breed_initial` と同じ関数（経路 1 本）・受付は安い確かめだけ・`canMate` を SQL に写さない | ✅ |
| B-1 | 確定のときに引く | ✅ |
| B-2 | 足りなければ `failed / ep_short`・やり直さない | ✅ |
| B-3 | 要求 ID ＋ `(母, 受付の年)` | ✅ |
| B-4 | 額が上がったとき | ✅ **(a) 払ってよい上限**。条件 1 つ（§1） |
| B-5 | 種牡馬の枠の取り合い・プレイヤーを優先しない | ✅ |
| B-6 | 画面の見積もり | ✅ **(a)**。★CLAUDE.md の禁止には当たらない。🔴 ただし**残り枠と総獲得賞金を SQL で計算しない**（§2・照会に無かった点） |
| B-7 | 画面は「繁殖入り」と一緒にデザイナーへ | ✅ |
| 🔴 追加 | 「総獲得賞金」の数え方が SQL に既に 2 か所ある | 🔴 **1 か所にまとめてから使う**（§3） |
| 追加 | EP の引き方 | ⚠️ 既存の引き落としの作法に揃える（§4） |

---

## 1. B-4 — ✅ (a) 払ってよい上限

- ★払うのは常に**確定のときの額**（ワーカーが `npcStudFee` で出す）。上限は**断る理由にしか使わない**。★利用者の申告で額が下がる経路が無い（憲法 3）→ 正しい
- (c) の受付の時点で固定する案は、式を SQL に写すことになるので採らない（照会どおり）
- **条件**: 画面は見積もりの額を**そのまま**上限に入れる（上乗せしない）。★額が上がったら `fee_above_max` で落ち、利用者は新しい見積もりで出し直す。★落ちた理由を画面に出し、「もう一度お試しください」（`internal_error`）と区別する（裁定 `322d603` §9 と同じ）

## 2. B-6 — ✅ (a)。CLAUDE.md の禁止に当たらない。★ただし SQL で計算しない

- ✔ CLAUDE.md の禁止は「**Next.js の Route Handler にビジネスロジックを書かない**」（アプリ化で書き直しになる）。★画面が `packages/` の純粋な関数を呼んで**表示するだけ**なのは、それに当たらない。★`packages/` はアプリ化でもそのまま共有される（§15.4）ので、むしろ書き直しが要らない形。★確定の額はワーカーが出し、画面の額は目安 → 食い違いは B-4 の上限で受け止める
- 🔴 **ただし読む口の RPC に計算を持たせない**。照会は「今年の残り枠」を RPC が返す案になっている。★残り枠は `20 + G1 勝利数 × 10 − 今年の種付数` で、この式は TS の `stallionCoveringLimit`（`packages/sim-engine/src/breeding.ts:45-46`・`STALLION_BASE_COVERINGS`・`STALLION_COVERINGS_PER_G1`）にある。★RPC で引き算すると、**式の写しが SQL にできる**（D-052）
- **決定**: 読む口の RPC は**事実だけ**を返す（`g1_wins`・`coverings_this_year`・総獲得賞金の元になる値）。★残り枠も額も、画面が `packages/` の関数で出す（0067 の「時間の計算は SQL に入れない」と同じ作法）

## 3. 🔴 「総獲得賞金」の数え方が SQL に既に 2 か所

✔ [CODE]:
- `apps/worker/src/market-flow.ts:94-95` … `coalesce((select sum(e.prize_pp) from race_entries e where e.horse_id = h.id and e.prize_pp is not null), 0)`
- `apps/worker/src/player-breeding.ts:306` … `select coalesce(sum(prize_pp), 0) from race_entries where horse_id = $1 and prize_pp is not null`
- 照会の読む口の RPC が 3 か所目になる

→ ★今は同じ形だが、★**どれか 1 つを直したとき（例: 取消・失格の扱い）、他が古いまま残る**（D-052 の族）。★種付料（EP の焼却の額）と馬の値段（D-102）が、同じ馬について違う「総獲得賞金」から出る状態になりうる

**決定**: この便で、**総獲得賞金を返す SQL の関数（またはビュー）を 1 つ作り**、上の 2 か所と新しい読む口の RPC がそれを呼ぶ形にする。★試験は「`race_entries` の `prize_pp` を足している箇所が、その関数の外に無い」ことを構文で見る

## 4. ⚠️ EP の引き方を既存の作法に揃える

- 照会の案は「`users.entry_points` を条件つきで減らし、台帳に `stud_fee`・`-額` を書く」。★調教の EP は、ワーカー専用の関数 `spend_training_ep`（`0021`・D-095「ワーカー専用関数は D-080 の対象外」）が残高と台帳を 1 か所で書いている
- ★**EP を減らす SQL を 2 か所に書かない**。★`spend_training_ep` を一般化するか、同じ作りのワーカー専用の関数（例 `spend_stud_fee_ep`）を作り、**台帳の `balance_after` と残高が必ず一致する**形にする。★利用者（`authenticated`）には実行させない（D-095 と同じ）
- 試験: 台帳の最後の行の `balance_after` と `users.entry_points` が一致すること（照会の予行の「台帳の行と残高の一致」をそのまま入れる）

## 5. B-1〜B-3・B-5・B-7 — ✅（照会どおり）

- B-1: 受付で引くと、確定で断ったときに返す経路が要る（EP が戻る経路が増える）→ 確定で引くのが正しい
- B-2: 失敗は枠を使わない（部分一意 `status <> 'failed'`）ので、同じ年にやり直せる
- B-3: 受付の年と確定の年がずれたら、確定の年の規則で判定する。★1 年に 2 頭は `canMate` と「今年の産駒の数え（馬＋下書き）」の 2 か所で止まる
- B-5: 先に確定した方が取る。★プレイヤーを優先しない（§10.5 同じ土俵）
- B-7: 画面は「繁殖入り」と同じ便でデザイナーへ（裁定 `f2ff871` Q-E）

---

## 6. 追記（2026-09-22）— 実装（`c7d5bc8`）と不快な語の一覧（`f1a5ebe`）✅・次の順番

### 照合 ✅
- ✔ §2: `npc_stallion_facts` は事実の 6 列だけを返し、残り枠と額を計算しない（予行 ⑤ で列を数えた）
- ✔ §3: `horse_total_prize_pp` を作り、`market-flow`・`player-breeding`・読む口が呼ぶ。★構文の試験で、コードに `sum(prize_pp)` が 0 か所・移行では関数の中の 1 か所だけ。★元の SQL を戻す変異で赤
- ✔ §4: `spend_stud_fee_ep` は `spend_training_ep` と同じ作り。✔ `revoke all … from public, anon, authenticated`（`0071:107`）＝ワーカー専用。予行で台帳の最後の `balance_after` と残高が一致
- ✔ 確定は `confirmBreeding` の 1 本（`kind` で分ける）。ロックは要求 → 利用者 → 母 → 父（役割の変更・購入と同じ「利用者 → 馬」の順）。初回の配合の予行 37 件も合格のまま
- ✔ 予行で判定できなかった ⑥（staging のレース・出走が 0 件で、総獲得賞金の値が常に 0）を**合格に数えていない**（CK-14）
- ✔ 一覧: `loadNameChecks` は実在馬名（必須）・不快な語の完全一致・「含む」を持つ。⚠️ 「前の組で合格した行を、新しい組で拾い直す」経路は、staging の行がすべて未検査のため予行で通っていない → ★**単体の試験で通すこと**（偽の行で「版 A で合格 → 版 B で当たり」を作る）。★NG 名リストが届いた日に初めて通る経路にしない

### 次の順番
1. **デザイナーへの依頼文を 1 本にまとめ直す**（`DESIGN_REQUEST_FIRST_HORSE_ONBOARDING_20260922.md` の改訂版）。★今日の裁定で画面に効く決めが多数あり、デザイナーの手元の依頼文より新しい。★入れるもの（出どころの裁定つき）:
   - 父母の選択・誕生・命名・初回の育成（元の依頼）＋ ★母の候補切れ（「次の年まであと○日」・`8a32840` §5）
   - 待ち（最大 60 秒）・失敗の見せ分け（規則による失敗 ／ `internal_error`「もう一度」／ `fee_above_max` ／ `ep_short` ／ `no_candidate`）
   - 命名の規則（カタカナ・ー・・／2〜9 文字・D-120）と、78 週を過ぎた命名の調教費の案内（`ff7028c` §5）
   - 「あとで」ボタンは無いが、ナビで抜けられる（`8a32840` §4）
   - 繁殖入り・功労馬へ戻す・自分の母で配合（見積もりと上限・`c653606`）＋ ★「繁殖入り」の画面は配合と同じ便（`f2ff871` Q-E）
   - ★数値の素質を出さない（D-114・D-116）・代を重ねた伸びは言葉と記録で（D-121）
   - ★他社製品の固有名・画面・文言を写さない（§0.1）
2. 運営が名前を仮の名前に戻す道具（提案 `PROPOSAL_PLAYER_NAME_MODERATION_20260922.md` §4）
