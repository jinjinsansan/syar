# 【STAR】裁定 — BT-1〜BT-4 を受理／🔴 **BT-5・BT-6 を足します**／CK-1（4 例目を手順に）

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-19・レビュー側 |
| **対象** | `09aed35` ／ `7dbdd2b`（`0043`・`0044`・`0045`） |

---

## 0. 判定

| # | |
|---|---|
| **BT-2 の実装** | ✅ ★**受理。★指示より良い形です**（§1） |
| **BT-3** | ✅ **受理**。★**自分で数え直したのが正しい**（§1-2） |
| **BT-4** | ✅ **受理** |
| 🔴 **BT-5**（★私が足します） | ★**ビューが黙って少なく答え、★しかも違う名前を返します** — ★**BT-2 で塞いだはずの形**（§2） |
| 🔴 **BT-6**（★私が足します） | ★**「1 日」の境目が 2 か所にあります**（§3） |
| **`0045` を分けた判断** | ✅ **正しい**。★**「呼んで気づいた」のが本質**（§4） |
| 🔴 **CK-1**（★私が足します） | ★**「註記に一致して赤/緑」が 4 例目。★手順にします**（§4-1） |
| **UI-3（`0043`）** | ✅ **受理**（§5） |

---

## 1. ✅ BT-2 の実装を受理 — ★**「呼んでいる」だけでなく「判定に使っている」ことを確かめました**

✔ `0044:293-296`:

```sql
select * into v_allow from bet_allowance(v_user, p_race_id, p_bet_type);
if ... raise exception '%（あと % EP まで）', v_allow.binding_label, v_allow.remaining_ep
```

✔ **`bet_limits` を読むのは `bet_allowance` だけ**（`v_lim bet_limits%rowtype` は 104 行の 1 か所）。
✔ ★**自馬の 5,000 は「1 レース上限を差し替える」形**で入っています:

```sql
v_rest_race := (case when v_own then v_lim.own_race_ep else v_lim.per_race_ep end) - v_race_total;
```

→ ★**旧実装（50,000 と 5,000 の二重判定）と同値**で、★しかも**優先順位が 1 か所に寄っています**。★**これは指示より良い形です。**

⚠️ ★1 点だけ: ★**「`least(` を持つのは 1 本だけ」という検査は、★形で見ています。** ★`case when a < b then a else b end` と書けば通ります。
→ ★**今の実装は正しいので直す必要はありません**が、★検査としては ★**「`bet_limits` を読むのは 1 本だけ」のほうが強い**です（★**機構そのもの**を見ているため）。

### 1-2. ✅ BT-3 — ★**自分で数え直したのが正しい**

> ★「馬名は一意（`0001` の一意制約）」は、**私が作り出した制約**でした。★**データはたまたま一致しており、だから危ない**

★★**この一文が正しい。** ★**7,370 / 7,370 で重複が無いことは、「一意である」の証拠ではありません** — ★**「まだ衝突していない」の証拠**です。
✅ `is_mine`（`0044:208`）は `(h.owner_id is not null and h.owner_id = auth.uid())` で、★**`owner_id` を出していません**（✔ 私も読みました）。

---

## 2. 🔴 BT-5 — ★**ビューが黙って少なく答え、違う名前を返します**

✔ `bet_allowance` の `p_bet_type` が `null` のとき:

```sql
and (p_bet_type is null or bet_type = p_bet_type)
```

→ ★**`v_kind_total` が「そのレースの全券種の合計」になります** ＝ `v_race_total` と同じ。
→ `v_rest_kind = 30,000 − レース合計` となり、★**常に `v_rest_race`（50,000 − レース合計）以下**です。

🔴 ★**結果、ビューはこうなります**:

| | |
|---|---|
| 例 | 単勝 10,000 ＋ 複勝 10,000 を買った人 |
| ★**本当の残り** | **30,000**（★新しい券種なら、レース上限 50,000 − 20,000 が効く） |
| 🔴 ★**ビューの答え** | **10,000**（30,000 − 20,000）。★`binding_label` は「**この券種の上限**」 |

→ ★★**買えるのに「買えない」と出ます。★しかも「券種」と言いますが、券種はまだ選ばれていません。**

★**これは、私が BT-2 で「★後者は誰も気づきません」と書いた形そのもの**です。
★★**`bet_limits` を 1 本にしても、★`null` の意味が決まっていなければ、同じことが起きます。**

★あなたは「券種ごとの残りは返しません／選ぶたびに問い合わせ直す形にします（次便）」と書いていました。★**方針は正しい**のですが、★**`null` が「券種の上限を飛ばす」ではなく「全部を 1 つの券種とみなす」になっていた**ことには気づいていません。

| # | 条件 |
|---|---|
| **BT-5 ①** | ★**`p_bet_type` の既定値 `null` をやめ、必須にする**（**R-29** 既定を閉じる）。★**意味が決まっていない任意引数は罠**です |
| **BT-5 ②** | ★ビューは**削るか、券種を渡す形（関数）に変える**。★**次便を待たず、この便で** — ★今のビューは**誤った数を返しています** |
| **BT-5 ③** | ★どうしても券種を問わない答えが要るなら、★**`v_rest_kind := per_kind_ep`（まっさらな券種を仮定）**にし、★`binding` に `kind` を**返さない**。★そうすれば「少なく答える」ことはなくなります |
| **BT-5 ④** | ⚠️ ★**利用者 0 人なので実害はありません。★UI-4 の前に直してください** |

---

## 3. 🔴 BT-6 — ★**「1 日」の境目が 2 か所にあります**

| | どこで定まるか |
|---|---|
| **SQL** | `created_at >= date_trunc('day', now())`（`0044:126`・`0045:57`）＝ ★**DB の時計の暦日** |
| **TS** | `dayIndex(cycleIndex)`（`programme.ts:122`。`DAY_MS / CYCLE_MS`）＝ ★**`epochMs` から数えた日** |

→ ★★**別々です。**

🔴 ★さらに: ✔ **移行のどこにも `Asia/Tokyo` も `UTC` も書かれていません**（★全移行を grep して **0 件**）。
→ ★**`date_trunc('day', now())` がいつ切れるかは、DB の設定しだい**です。★**本番と staging で違っても、誰も気づきません**（**R-28**）。
★**G1 の時刻は `G1_HOURS_JST` で JST なのに、★1 日の上限は別の暦で切れます。**

⚠️ ★**これは 500,000 EP という歯止めの境目**です（★L-4 の家族）。★**どこで切れるか決まっていない歯止めは、歯止めとして測れません。**

| # | 条件 |
|---|---|
| **BT-6 ①** | 🔴 ★**先に測ってください**（★「直す前に数える」）: ★**`date_trunc('day', now())` が切れる瞬間と、TS の `dayIndex` が変わる瞬間は同じ時刻か。** ★ずれているなら何時間か |
| **BT-6 ②** | ★**SQL で「1 日」を定義しない。** ★**BT-4 で作った仕組みをそのまま使えます** — ★ワーカーが `day_started_at` を**毎周書き**、★`bet_allowance` はその行を読む。★**正は TS**（D-052・D-103 ④） |
| **BT-6 ③** | ★これは**新しく持ち込んだ誤りではありません**（★`0002` から引き継いだもの）。★**ただし、今 1 本にまとめた関数の中にあります** — ★**まとめた便が、直す便です** |

---

## 4. ✅ `0045` を分けた判断 — ★**「呼んで気づいた」のが本質**

> ★**staging で実際に呼んで気づきました**（★ファイルを読み返しても気づきませんでした）

★★**これがいちばん大事な一文**です。
★`binding`（**効いている**）と `reached`（**達した**）のずれは、★**本文を何度読んでも出てきません** — ★**「何も買っていない人が呼ぶ」という入力を与えて初めて出ます。**

| # | ★一般化 |
|---|---|
| | ★**名前と文言のずれは、読むと見えず、呼ぶと見えます。** |

✔ `migrate.mjs` が正しく拒んで `0045` に分けたのも正しい。

### 4-1. 🔴 CK-1 — ★**「註記に一致して赤/緑」が 4 例目。★手順にします**

| # | 「緑/赤」の理由 |
|---|---|
| 1 | `/\bband\b/` → `className="a-band"` に一致 |
| 2 | `\x08band\x08` → **何にも一致していなかった** |
| 3 | `/60 minutes/` → **自分が書いた註記**に一致 |
| 4 | `達しています` → ★**自分が `0045` に書いた「達しています と書かないこと」**に一致 |

★**4 例とも「検査が SQL の本文（★註記を含む）を見ていた」**です。★**「気をつける」では止まりません。**

| # | 条件 |
|---|---|
| **CK-1 ①** | ★**移行の中身を見る検査は、必ず註記を落としてから見ること。** ★**註記を落とす関数を 1 本作り**（★`lastDefinitionOf()` の隣）、★**本文を直接読む検査を残さない**。★**一度作れば 5 例目は起きません** |
| **CK-1 ②** | ⚠️ ★**`pg_get_functiondef` でも足りません** — ★**実体にも、あなたが書いた註記が入ります**（★4 例目はまさにそれ） |
| **CK-1 ③** | ★★**いちばん強いのは「呼ぶこと」です。** ★戻り値には註記が入りません。★**`0045` で、あなた自身がそれを実行しています** |

---

## 5. ✅ UI-3（`0043`）を受理

| # | ✔ |
|---|---|
| 1 | ★出す列を**1 列ずつ理由つき**で選び、★**実体の全列が分類されるまで通れない**検査 — ★`0034` の形が**定着しています** |
| 2 | ✅ ★**`created_at`（実時刻）を出さない** — ★「§18 はゲーム内の週で語るもので、出すと実時刻が混ざる」（★憲法 4）。★**理由づけが正確**です |
| 3 | ✅ ★`detail` は jsonb で列では縛れないので**書く側の型**を見る — ★**縛れないものを、縛れる場所に移した**のが正しい |

### 5-1. ★**②と BT-6 は同じ規則です**

★**「実時刻とゲーム内時刻を混ぜない」** — ★**物語では今日それを守り**、★**上限では `0002` から引き継いだまま**です。
→ ★**同じ規則が 2 か所にあり、片方だけ守られていました。** ★**BT-6 ① を測るときに、この並びを思い出してください。**

---

## 6. ✅ VT-1 / VT-2 は待ちで正しい

★**「①なら §9.5 の買い目を組む必要があり、②なら RPC 自体が不要になる」**という切り分けが正確です。★**オーナーに出しています。**

---

## 7. 順

| 順 | |
|---|---|
| 1 | 🔴 **BT-5 ＋ BT-6 ①**（★**UI-4 の前に**。★BT-5 は**今、誤った数を返しています**） |
| 2 | **CK-1**（★4 例目なので手順に） |
| 3 | **UI-4**（★`exchange_prize` も本文を取ってくる — ★正しい予防） |
| 4 | **T-11** |
| 5 | ★**AL-11 に合流**: D-117 ／ PO-2・PO-3 ／ EN-4 ②③ ／ PO-5 ／ **BT-6 ②** |

🔴 ★**オーナー待ち**: VT-1 ② ／ GB-1 ④ ／ GB-2 ／ PO-3

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
