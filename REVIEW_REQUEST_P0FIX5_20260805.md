# P0-fix5 レビュー依頼 — 開発側 → レビュー側（P0 の締め）

**発行**: 2026-08-05（開発側 Claude）
**対象**: `DEV_INSTRUCTIONS_P0FIX5_20260805.md`（J-1〜J-3）
**成果物**: `https://github.com/jinjinsansan/syar`（private）/ branch `main` / commit `6225839`

---

## 0. 結論 — 合格条件を満たしています

**`npm run verify` の出力が P0-fix4 と完全一致**（所要時間の行を除き差分ゼロ）です。

| 合格基準 | 実測 | 判定 |
|---|---|---|
| **18. clamp比 → 係数 → sd が追随** | k=3.333→0.9992→丈夫さsd **60** / k=2.0→0.9594→**63** / k=1.5→0.8823→**68**（指示書 J-1 の表と一致） | PASS |
| **19. `basis` 3分岐の固定・`none` で FAIL** | `runSimulation()` 経由で3分岐とも固定。`none` は `pass:false` かつ総合も FAIL | PASS |
| **20. `derive()` が r=0 を静かに受け入れない** | 例外を送出 | PASS |
| **既存17項目の維持** | verify が P0-fix4 と同一。テスト **98件**・typecheck exit 0・`any` ゼロ・決定論 | PASS |

---

## 1. J-1 は (a) 解析式を選びました

**理由**: (b) の fail-closed でも「静かに間違わない」は満たせますが、**`sweep.ts` のように clamp比を意図的に振る用途が実際に存在する**ため、拒否すると使えなくなります。加えて、`balance.ts` に自分で書いた「比を変えたら再計算せよ」という警告を**コード自身が満たせる形**にしておくのが筋だと判断しました。正規分布の CDF/PDF は外部依存なしで書ける範囲です（誤差関数は Abramowitz & Stegun 7.1.26、絶対誤差 1.5e-7）。

既定 k=150/90 での係数は **0.915604** で、正典の 0.9156 と**導出 sd/clamp が完全一致**します（66/±109・10/±16・13/±22・341/±568・114/±189）。だから既定の挙動が変わりません。

`config.test.ts` の「`clamp/sd ≈ ratio` しか見ない」テストも是正しました。**sd の実値まで固定**し、係数がリテラルのままなら落ちます（`MUTATION_SD=45` で 60 になること・66 でないことの両方）。ご指摘のとおり、あのテストは欠陥を固定していました。

---

## 2. 【自己申告】J-2 のテストを書いたら、自分の実装に到達不能な分岐がありました

3分岐を本番経路で固定する作業中に、**I-2b の実装では `sd` 分岐に実質到達できない**ことが判明しました。

切替条件を `Math.abs(founderMean) > 1e-9` にしていたため、創始平均 **1.01・創始SD 18.6** というほぼゼロ中心の分布でも `ratio` 判定になり、**ほぼ0の値で割った不安定な乖離**が出ます。「平均が厳密に0か」を見ていたのが誤りで、正しくは「**平均が散らばりに対して十分大きいか**」でした。

```ts
// 修正前: Math.abs(founderMean) > 1e-9   ← sd 分岐へ実質到達しない
// 修正後: Math.abs(founderMean) > founderSd
```

既定6形質はすべて平均がSDより十分大きい（丈夫さ 649 vs 71、気性 50.3 vs 10.7、適性 54.9 vs 14.3、距離 2090 vs 363 / 701 vs 122）ので `ratio` のままで、**既定の挙動は不変**です。

**これは I-3 の「経路を固定せよ」がそのまま効いた例です。** 分岐関数を単体で書いていたら気づけませんでした。J-2 を指示に入れていただいたことで表に出ました。

---

## 3. J-3 は例外にしました

`buildTraitMutation()` は回帰率 ≤ 0 で例外を投げます。`sweep.ts` の回帰率0構成（①②③）は **D-008 以前の状態を再現するもの**なので、当時の挙動＝sd 上書きなし＋品種中心だけを `legacyCentersOnly()` として明示的に組みました。「回帰なしの比較がしたい」という意図がコードに残ります。

実地確認:

| 確認 | コマンド | 結果 |
|---|---|---|
| J-1 | `--set-genetics MUTATION_SD=45` | 丈夫さ **sd 60 / clamp 200**（従来は 66 のまま） |
| J-3 | `--set-balance REGRESSION_RATE=0` | **例外で停止**（従来は非能力6形質が無言で凍結したまま完走） |

---

## 4. §3（表現型の気性）の訂正について

**ご指摘のとおりで、私の P0-fix4 報告 §1 の書き方が不正確でした。** 「平均F が小さいので無害」と書いたのは 800頭/300年（F=0.011）だけを根拠にしており、既定の 200頭/100年（F=0.0404）では成り立ちません。

そのうえで「これは欠陥ではない」という整理に同意します。§6.5 の `temper += F×120` がそのまま出たもので、**集団が小さければ近交が進み気性が荒くなるのは設計どおり**です。V-2d を genotype 判定のまま据え置く判断にも同意します（表現型を混ぜると、遺伝的な水準ずれと近交の影響が分離できなくなります）。

P1 では **平均F と「表現型 − genotype」の乖離**を監視項目に加えます。

---

## 5. 逸脱・解釈（3件・報告書 §4）

| # | 内容 |
|---|---|
| 1 | J-1 で (a) 解析式を選択（理由は §1） |
| 2 | `basis` の切替条件を `!== 0` → `|平均| > 創始SD` へ是正（§2） |
| 3 | `sweep.ts` の r=0 構成に `legacyCentersOnly()` を用意（§3） |

正典の規定からの逸脱はありません。

---

## 6. 未解決の質問

**なし。** P0 として残る宿題はありません。P1（レース判定エンジン + モンテカルロ検証）の指示書起草に進んでいただいて問題ない状態です。

引き継ぎ6点はすべて認識しています。基準線については、本便の verify 出力が P0-fix4 と同一なので **P0-fix4/5 のどちらの数値を使っても同じ**です。
