# 引継ぎ書 — 次のセッションはここから（2026-08-30）

作成: 2026-08-30 / ブランチ `p4/race-30sec-cuts` / HEAD `47681bc`
起点: `0ecdd94` → **9 コミット**（★未 push）
前の引継ぎ書: `HANDOVER_P4_SEAM_DIRT_CANON_20260829.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**
> ★この案件は R-25 で 9 日間の事故を 1 度出しています。

---

## ★0. まず 3 分でやること

```powershell
cd V:\dev\Cusor\star
git log --oneline -9                                     # ★この便の 9 本
git log --oneline origin/p4/race-30sec-cuts..HEAD | wc -l  # ★未 push の数（直書きしない）
git status --short                                       # ★` M apps/web/package.json` だけのはず
npm test                                                 # ★1267 件 PASS / EXIT=0 のはず
```

⚠️ ★**`apps/web/package.json`（ポート 3210）は触らないこと。** ★オーナーの既存作業で、
　★この便でも 9 コミットとも混ぜていません。★`git add -A` には必ず `':!apps/web/package.json'` を付けます。

---

## ★1. 次の一手 — **馬場の調整から**（オーナー指示）

★**オーナーの指示は「馬場の調整から開始」です。**

### ★1-1. いまの較正と、実画面での実測

```ts
// packages/render/src/world-textured.ts
export function trackWetnessAlpha(condition) {
  case 'yielding': return 0.22;   // 稍重
  case 'soft':     return 0.42;   // 重
  case 'bad':      return 0.55;   // 不良
  default:         return 0;      // ★良は必ず 0（1 ビットも動かさない）
}
```

★**本物のブラウザの画面**で測った走路の明るさ（良を 100%）:

| | 良 | 稍重 | 重 |
|---|---|---|---|
| 芝 | 100% | **89.1%** | **81.8%** |
| ダート | 100% | **88.5%** | **80.4%** |

### ★1-2. ★オーナーが最後に見た状態と、私の評価

★**オーナーはこの数字の版をまだ見ていません。** ★見ていただいたのは**1 つ前の弱い版**（重 87.4%）で、
★評は「★**何も変わっていません**」でした。★そのあと較正を 1.75 倍にしています。

★私が実画面を目で見た評価:

| | |
|---|---|
| 芝 | ★良（明るい黄緑・刈り目がはっきり）→ 重（深く沈んだ緑）。★**はっきり違う** |
| ダート | ★良（暖かい明るい褐色・砂埃あり）→ 重（暗い褐色・砂埃が減る）。★違うが…… |
| ⚠️ | ★**ダートの重は「暗くなった」とは見えても「濡れている」とは見えません。** ★影が落ちたように見えます |

### ★1-3. ★次に打てる手（★オーナー判断待ち）

| # | 手 | 状態 |
|---|---|---|
| ① | ★**照り**（濡れた面が光を返す） | ★未実装。★**「濡れている」に見えない**のはこれが無いため |
| ② | ★較正をさらに上げる | ★`trackWetnessAlpha` の 3 つの数だけ。★防壁は `bad < 0.7` |
| ③ | ★泥の塊の色 | ★砂埃は減らしました（`airborneDustFactor`）が、★**塊は今も乾いた土の色**です |

⚠️ ★**地面の模様を平らにする方向（濡れて締まった質感）には触れないこと。**
　★やると**地面の流れが読めなくなり**、★**「遅く感じる」を再発させます** — ★この便で 1 度失敗しています（§3-1）。

---

## ★2. この便でやったこと（9 コミット）

```
d8697be  ダート: 地面の明るさを下げる — 「走り方が芝と違う」の 3 回目の原因は明るさだった
eb19d7e  ダート: 浴びた砂で馬体・勝負服が汚れる（報告 §10-2 の残り）
c7edec1  ダート: 内側の帯を芝にする（Q-3 の B 案）を既定にする
f0e1098  ダート: 馬場状態を地面に出す — 良でも不良でも同じ地面だった
7181dc6  報告と残件台帳: ダート磨き上げ 4 件（★A-5・A-6 を新設）
b27144f  ダート: 砂煙が段で跳んでいた — 「遅く感じる／ぎこちない」の真因
7440962  訂正: 報告書の「真因は地面だった」は誤り — 真因は砂煙だった
297d295  馬場別を作る（芝・ダート × 稍重・重・不良）— 横視点にも入れた
47681bc  馬場別の濃さを上げる — 「何も変わっていません」は私の較正が弱すぎたため
```

### 2-1. ★製品で振る舞いが変わったもの

| # | 何を | どこ |
|---|---|---|
| ① | ★ダートの地面を 1 段暗い側へ（役割 `dirt-3`→`dirt-2`→`dirt-0`） | `bake-dirt-tile.mjs` / `bake-dirt-plates.mjs`（★素材を焼き直し・`ASSET_VERSION` 58） |
| ② | ★**砂煙の位相を、進んだ距離から連続で取る** | `dustPlumePhaseAt`（`dust-exposure.ts`）★**オーナー確認「改善されました」** |
| ③ | ★浴びた砂で馬体・勝負服が汚れる | `dustExposureCurve`（`dust-exposure.ts`）＋ `perspective-draw.ts` |
| ④ | ★内側の帯を芝に（既定） | `INFIELD_REVERSED_DEFAULT`（`broadcast-v2.ts`）★戻し口 `?infield=dirt` |
| ⑤ | ★**馬場状態が地面に出る**（正面も横も） | `trackWetnessAlpha` / `trackWetnessColor`（`world-textured.ts`）＋ `parallax-plate.ts` |
| ⑥ | ★濡れると砂埃が減る（塊は増える） | `airborneDustFactor`（`perspective-draw.ts`） |
| ⑦ | ★**`?surface=` と `?condition=` が効くようになった** | `page.tsx`（★それまで**効いていませんでした**） |

---

## ★3. 次のセッションが踏みやすい穴（★ここが本題です）

### ★3-1. ★**道具の描画で合否を決めない。本物のブラウザから撮る**

★**この便で最大の失敗です。**

★私は `shot-race-at.mjs`（道具の描画）で測って「馬場別ができました」と報告しました。
★オーナー評は「★**何も変わっていません**」。

★本物のブラウザから撮って測ったら、★**機構は効いており**（重 87.4%）、★**URL も届いていました**。
→ ★つまり「12% では気づけない」という意味で、★**私の較正が弱すぎた**のでした。

⚠️ ★**この案件が 9 回再発させている形（R-30）を、私が 10 回目としてやりました。**

→ ★`tools/_realshot.mjs` を新設しました。★**映像の合否は必ずこれで確かめること。**

```powershell
# ★dev サーバーを立ててから
npx tsx tools/_realshot.mjs --out dirt-soft --query "surface=dirt&condition=soft" --sec 32
```

⚠️ ★`auditSec` を付けないと canvas が描かれず、READY 判定が通りません（道具の中で付けています）。

### ★3-2. ★「検査が通った」は「見える」を意味しない

★私は「★稍重と重が目で分かる差になっているか」という検査を書き、★**通していました**。
★それでも実画面では見えませんでした。

→ ★**閾値は、実画面で確かめた値から引くこと。** ★頭で置いた値の周りに囲いを引かない。

### ★3-3. ★落ちた防壁を、意図を確かめずに緩めない

★較正を上げたら、★**私自身が書いた `bad < 0.5` に当たりました。**
★線を 0.7 へ動かしましたが、★**動かした理由を検査のコメントに書き残しています**:

- ★この線の**意図**は「走路が**黒い帯**にならないこと」
- ★0.55 でも**明るさは 60% 残り、模様も全部残る**（黒い帯ではない）
- ★0.5 という数は ★**オーナーが却下した較正**の周りに引いた囲いだった

→ ★**意図に照らして判断し、判断を残す。** ★黙って緩めない。

### ★3-4. ★同じ苦情が続いても、前回直した所が原因とは限らない

★「ダートの走り方が芝と違う」は**3 回目**でした。★原因は毎回違いました。

| 回 | 原因 |
|---|---|
| 1 回目 | 正面の地面タイル（整地の筋 0.385m がちらつき） |
| 2 回目 | 横の板（輝度を 0.78 倍に圧縮していた） |
| ★3 回目 | ★**砂煙の位相が 8 段に量子化されていた** |

⚠️ ★私は 3 回目に「地面の明るさが真因」と報告し、★**誤りでした**。
　★潰した容疑を**数字で並べてから**次を探すこと（今回は 4 つ並べて 5 つ目に当たりました）:

```
地面の流れ(px/s)      ダートは芝の 97〜98%   ← シロ
地面のコントラスト     95%                  ← シロ
コマ間の画素の変化     96%                  ← シロ
馬の動き              完全に同一            ← シロ
★砂煙の位相          8→75→80→12 px と暴れる ← ★犯人
```

### ★3-5. ★オーナーが画面で確認する前に「真因」と書かない

★測って言えるのは「★**この量は芝並みに戻した**」までです。
★私は §3-4 の 3 回目で「真因を特定して直した」と書き、★報告書と台帳を**訂正する羽目になりました**（`7440962`）。

### ★3-6. ★註釈は嘘をつくことがある

★砂煙の位相にはこう書いてありました:

> 位相はコマ番号（＝進んだ距離）から決まり、★**途切れずに続きます**

★**8 段に量子化されているので、続いていませんでした。**
→ ★註釈を根拠にしない。★測る。

### ★3-7. ★新しいフラグは `VALUELESS` に登録する

★`shot-race-at.mjs` に `--no-soil` を足して登録を忘れ、★**次の引数（秒数 44）を食われました。**
⚠️ ★その 2 行上に「★ここへ入れ忘れると**次の秒数が食われます**」と書いてありました。
→ ★**註記は読まれなければ効きません。**

### ★3-8. ★測定の窓を決め打ちにしない

★`y470〜600` の帯で測り、★**正面 83% 対 横 91%** と出て「揃っていない」と読みかけました。
★窓の下半分が**手前のラチ**（`front-rail` は plateY 762〜941・走路ではない）でした。
→ ★**良と比べて実際に沈んだ行だけ**を走路と見なす形に直しました（自分で校正する）。

### ★3-9. ⚠️ ★**道具はずっと芝のレースを測っていました**

★`tools/lib/race-audit-build.mjs` が **`surface: 'turf'` を直書き**していました。
→ ★`_groundflow` も `_soil` も `_gaitocclude` も、★**ダートについて聞いているのに芝を測っていました。**
⚠️ ★**渡し忘れではなく、渡せませんでした**（R-31 の家族で、より悪い形）。
★直しました（`surface` / `trackCondition` を受け取る・既定は画面と同じ turf / good）。

### ★3-10. ★`?surface=dirt` は効いていませんでした

★画面に URL の読み取りが**ありませんでした**（`useState('turf')` のみ・右下の選択だけ）。
⚠️ ★**引継ぎ書にも報告書にも書かれており、私もその URL をオーナーに渡しました。**
★`?surface=` / `?condition=` を足しました（★既定は画面の既定・R-31）。

### ★3-11. ★提出物を足したら、もう一度 `npm test`

★報告書・照会文書は**追跡ファイル**で、★`source-integrity.test.ts` の走査対象です。
→ ★**`git add` してから回すこと。** ★この便では守っています。

⚠️ ★別字（U+82DD/697A/780A/868A/9451）は、書いたあとに走査してから `git add` しています。

---

## 4. 残件台帳（`RESIDUALS_P4_RACE_VIDEO.md`）

| # | | 状態 |
|---|---|---|
| **A-1** | 4 角の入口の反転 | ★半分解決（入口は残る・[EYES] 待ち） |
| **A-3** | 直線の 2 本の境目に閃光がない（Q-5） | 当面入れない |
| **A-4** | 13 頭以上の勝負服の最小色距離 27.5 | 12 頭立てでは出ない |
| **A-5** | ★**芝の縞刈りがダートの上にも引かれている** | ★**未解決。正面のカットが芝の 61〜65% で止まる原因** |
| ~~A-6~~ | ~~横視点の板が馬場状態に反応しない~~ | ★**この便で解決**（`297d295`・台帳から消すこと） |
| **B-1** | `audit-race-broadcast.mjs` が発走の遅れを引いていない | 据え置き |
| **C-1** | 正典 D-090 以降の起票 | ★**この便の決定が未起票**（§8） |
| **C-2** | 未 push | ★数を直書きしない（コマンドで数える） |
| **C-3** | ダート | ★**残るは濃さの [EYES] と素材の承認** |

⚠️ ★**A-6 は解決しましたが、台帳をまだ更新していません。** ★次の便で消してください。
⚠️ ★**台帳の番号は詰めません**（過去の便が参照している番号が動くほうが害が大きい）。

---

## 5. この便で増えた検査（★次に触るとき落ちたら、これを読むこと）

| 検査 | 何を守っているか |
|---|---|
| `packages/render/test/dust-exposure.test.ts`（16 件） | ★前の馬は汚れない／溜まる／★**積分の刻みを変えても結果が変わらない**／芝では 1 粒も塗らない／★**砂煙の位相が段で跳ばない**（★8 段の式に戻すと 3 件落ちます） |
| `packages/render/test/infield-reversed.test.ts`（4 件） | ★既定が芝／`?infield=dirt` で戻せる／★**戻すと実際に色の列が変わる** |
| `packages/render/test/track-wetness.test.ts`（10 件） | ★良は必ず 0／悪くなるほど濃い／★**稍重と重が目で分かる差か**／濡れると砂埃は減り塊は増える |

---

## 6. 再実行（★すべて読取専用）

> ⚠️⚠️ ★**2026-08-31 追記 — ★この手順の `--sec 32` を、そのまま信じないでください**（★台帳 **B-2**）。
> ★R-25 のとおり ★**当時の手順は消しません**が、★下の一文を先に読んでください。
>
> ★**当時（2026-08-30）は、同じ URL を 2 枚撮ると帯の平均輝度が最大 20% 動きました。**
> ★ここに出ている「芝 100/89.1/81.8」は ★**2 回とも同じ側に落ちたから再現した**だけです。
>
> ★**2026-08-31 に測り直すと、いまは再現します**（★別プロセスで 2 回撮って **1 バイトまで一致**）。
> ⚠️ ★ただし ★**余裕がありません**: ★32 秒は ★**残り 231m ＝ 1369m 地点**で、
> ★`homestretch-front`（1312〜1392m）の ★**終端まで 23m** しかありません。
> ★何かが少しずれれば、★また境目に戻ります。★**原因は特定できていないので B-2 は閉じません。**
>
> → ★**撮る前に、その秒が安全かを測ってください**（★覚えるのをやめる）:
>
> ```powershell
> npx tsx tools/verify-shot-stability.mjs --query "surface=dirt&condition=soft" --secs 14,32
> ```

```powershell
# ★★映像の合否は「本物のブラウザ」で（★道具の描画で決めない・§3-1）
cd apps/web ; npm run dev            # ★別の窓で立てておく
npx tsx tools/_realshot.mjs --out dirt-good --query "surface=dirt&condition=good" --sec 32
npx tsx tools/_realshot.mjs --out dirt-soft --query "surface=dirt&condition=soft" --sec 32
# ★撮ったものを out/_condcmp/<馬場>-<状態>-<カット>.png に置いてから
npx tsx tools/_condcmp.mjs           # ★芝 100/89.1/81.8 ／ ダート 100/88.5/80.4

# ★砂煙
npx tsx tools/_duststep.mjs          # ★1 コマの動き 中央値 3.95px（直す前は 12.88px）
npx tsx tools/_soil.mjs              # ★きれい 3 頭 / はっきり汚れる 6 頭

# ★地面
npx tsx tools/_groundflow.mjs --surface dirt   # ★芝の 97〜98%
npx tsx tools/_interframe.mjs                  # ★コマ間の変化 芝の 96%

# ★全体
npm test > log 2>&1 ; echo "EXIT=$?"           # ★129 ファイル / 1267 件 / EXIT=0
npm run typecheck
```

⚠️ ★`tools/_*.mjs` は `.gitignore` 対象・**追跡外**です。★ただし**登録簿には載せます**
（`tools/lib/classification.mjs`）。★この便で **11 本**足し、11 本とも登録済みです。

---

## 7. 発行物（この便）

| 種別 | ファイル |
|---|---|
| 報告 | `REPORT_P4_DIRT_POLISH_20260829.md`（★§10 に訂正と真因） |
| 台帳 | `RESIDUALS_P4_RACE_VIDEO.md`（★A-5 / A-6 を新設・A-6 は解決済みなので次便で消す） |

⚠️ ★**馬場別（`297d295` / `47681bc`）はまだ報告書に書いていません。** ★次の便で書いてください。

---

## ★8. オーナー判断待ち

| # | | |
|---|---|---|
| ① | ★**馬場の濃さ** | ★**次の一手はここ**（§1）。★オーナーは 1 つ前の弱い版しか見ていません |
| ② | ★汚れの濃さ | ★[EYES]。★砂の色が馬体に近く、寄りで薄く出ます |
| ③ | ★ダート素材の承認 | ★未取得（裁定 §6-4 条件 2）。★**焼き直した後のもの**が対象 |
| ④ | ★A-5（刈り目） | ★直すとダートの見た目が変わるので判断待ち |
| ⑤ | ★push | ★指示待ち（★数は `git log --oneline origin/p4/race-30sec-cuts..HEAD \| wc -l`） |
| ⑥ | ★決定ログの二重帳簿 | ★未決（前便からの持ち越し） |

★**それ以外に開いている照会はありません。**
