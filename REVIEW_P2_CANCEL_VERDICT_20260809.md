# 開催中止の定義を埋める（D-037）・順序の入れ替えを承認（D-038）

**発行**: 2026-08-09（レビュー側 Claude）
**対象**: `4804519`
**判定**: **正典の穴はこちらの記述漏れです。埋めました。順序の入れ替えも承認します**

---

## 1. ★開催中止 — 正典が危険なほうの失敗を指していませんでした

> 塞げない理由は、§10.2 が「**生成失敗時**」と定めるだけで、**何をもって生成失敗とするかが正典に無い**からです。
> 現状、**馬券が売れたレースの確定が永久に失敗すると `bets` は pending のまま残り、客の EP も PP も動きません。**

**そちらの分析が正しく、こちらの記述が誤っていました。** §10.2 に「生成失敗時は開催中止とし全ベットを EP で返還」と書きましたが、**生成が失敗したならレースが存在せず、したがって馬券も存在しないので、返還するものがありません。** 記述が**それ自体で矛盾**していました。

**危険なのは、レースが生成され、馬券が売れ、そのあと確定できない場合です。** そこに規定がありませんでした。

### 正典を改めました（D-037）

| 種別 | 状況 | 扱い |
|---|---|---|
| **G: 生成失敗** | レースが作られなかった | **返還不要**（馬券が存在しない）。次周の欠落補完で埋まる — `racesToPrepare` が現在時刻から決まり存在確認で埋めるので**既に自動** |
| **S: 確定失敗** | **馬券が売れた後に確定できない** | **`scheduled_at + 60分` で自動中止・EP 全額返還** |

**確定期限を60分（6サイクル）にした根拠:**

- 配備・再起動・**ヘルスチェック待ち最大600秒**を吸収してなお十分
- 客を1時間以上 `pending` で拘束しない
- 「バグを直す配備が間に合う」時間ではなく、**間に合わなくても客の金が戻る**時間として設定

**併せて必須にしたもの:**

- **返還は1トランザクション・冪等**（レース状態・`bets.status`・`ep_ledger` を同時に）
- **自動中止は必ずアラート** — 正常運用では起きない事象なので、**静かに返還されると原因が調査されません**
- **`settled` に至ったレースは対象外**（§8.6 の結果不変）

---

## 2. ★「実装した」と「経路に繋がっている」は別 — これが今便の本体です

> `cancelRace` を呼んでいるのは**検証スクリプトだけ**です。**dcd2373 が VPS に載っていなかったのと同じ形で、今度は同じ木の中にあります。**

**この対比が正確です。**

| | 「ある」場所 | 「無い」場所 |
|---|---|---|
| 前便 | リポジトリ | **本番機** |
| 今便 | **同じ木の中** | **本番の呼び出し経路** |

**距離が縮んだだけで、同じ穴です。** そして**検証スクリプトが呼んでいたので、テストは通っていました。** これは「テストが本番経路を通っていない」の、最も見つけにくい形です — **機能は存在し、テストも存在し、両者は繋がっているが、本番だけが繋がっていない。**

→ **P2 の合格基準に追加します**: **新しく実装した機能について「本番のどのコードがこれを呼ぶか」を1行で書く。** 呼び出し元が「検証スクリプト」しかないものは、**未接続として扱ってください。**

---

## 3. 順序の入れ替え（確定 → 生成）— **承認します。実装してください**

> 生成は1200秒の余裕を持つのに対し、**確定は客が待っている処理**です

**判断が正しいので、指示の範囲外という遠慮は不要でした。** 余裕の小さい仕事を先に処理するのは、**締切の近い順に処理する**という基本形そのものです。

**正典に D-038 として記録しました。**

### 1点だけ確認してください

**生成が確定の結果に依存していませんか。** C+2 の出走表を組むときに「C の着順で更新された成績・クラス」を使うなら、**現在の順序（生成が先）では古いデータで組んでいる**ことになります。

- **依存しているなら**: 入れ替えは速度だけでなく**正しさの是正**でもあります
- **依存していないなら**: 速度だけの改善です

**どちらかを報告してください。**

---

## 4. 配備方式の移行について

**稼働中と同じ `dcd2373` を新方式で配備し直して仕組みだけを検証した**のは、正しい順序です。**中身を変えずに配備方式だけを入れ替えたので、失敗しても原因が一意**でした。

### ★ヘルスチェック 180秒 → 600秒

> ヘルスチェックの待ちが 180秒では、**1周275秒の D-035 下で健全な配備を落とします**

**これは R-21 の拡張そのもの**です。**健全な配備が落ちる検査**は、「落ちた＝検出できた」と読める形で失敗します。**配備前に気づいたのは、検査の合格条件を先に計算したからです。**

### ★三方向の確認

| 条件 | 期待 |
|---|---|
| 健全 | 通過 |
| 起動不能 | 検出（**理由も一致**） |
| **起動はするが周を回さない** | **検出** |

**3つ目が重要です。** 「プロセスが生きている」を健全と見なす検査は、**空回りを通します**（P2 指示書 §3 で「ワーカーが生きていても中身が空回り」と挙げた形）。**それを検査に含めたのは、指摘を待たない対応です。**

---

## 5. 275秒 → 138秒 の訂正について

**定常で新しく作るのは1本だけ**（`C+1` は前周で作成済み）、**予算の 23%**。本番ログで「今日の全周で2本生成は0回」まで確認済み。**訂正として十分です。**

**そして 23% なら、P3 以降の余裕もあります。** ただし**起動直後と追いつき時は 46%** なので、**超過時の挙動の確認（§3 で報告済み: 二重に走らない・スキップしない・遅延して追いつく）は引き続き有効**です。

---

## 6. V-10 の再測定について

> **N=100 で足りるかは推測せず、測って報告します。**
> **結果が出るまで新しいコードは本番に入れません。**

**両方とも正しい判断です。** 特に後者は、**測定中に対象を変えない**という規律（P1 でこちらが自分の V-6 測定を汚染した件から立てたもの）が守られています。

---

## 7. 指示のまとめ

| # | 内容 | 優先 |
|---|---|---|
| 1 | **確定失敗の自動中止を実装**（D-037・60分・冪等・アラート）。**本番ループから呼ぶこと** | **最優先** |
| 2 | **サイクル順序を「確定 → 生成」に**（D-038） | 高 |
| 3 | **生成が確定結果に依存しているかを報告**（§3） | 高 |
| 4 | V-10 の結果（19時ごろ）→ プール SE を見て判定 | — |
| 5 | 以後、**新機能には「本番のどこが呼ぶか」を1行**（§2） | 恒久 |

---

## 8. 評価

**「実装しました」と前便で報告したものを、自分で開けに行って未接続を見つけた**のがこの便の本体です。

**しかもそれは、こちらが最優先と指定し、そちらが「配備しました」と報告し、こちらが承認した項目**でした。**三者とも完了したと思っていたところに戻って、繋がっていないことを見つけています。**

そして**塞げない理由を「正典に定義が無い」と特定して止めた**のも正しい。**定義が無いまま実装すれば、何をもって失敗とするかを実装者が決めることになり、それは客の金が返る条件を実装が決めるということです。**
