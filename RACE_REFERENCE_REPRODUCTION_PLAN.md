# STAR レース演出・参考映像再構築計画

**作成日**: 2026-08-16  
**対象**: `ダービースタリオン/ダービースタリオン2025の２D参考用.mp4`  
**実装先**: `packages/render` / `apps/web/src/app/race` / `tools`

## 1. 最終目標

参考映像の固有素材・名称・UIを複製せず、競馬中継として一般的な映像文法をSTAR独自の素材とゲーム結果で再構築する。

完成時には、同じシードから常に同じ映像が生成され、次を満たすこと。

1. 発走、隊列形成、道中、コーナー、勝負所、直線、ゴール、ゴール後を、それぞれ目的の異なるショットで見せる。
2. 馬群が常に画面の主役であり、背景や消失点だけが目立たない。
3. カメラの切替、画角、馬群占有率、余白、重なり、進行方向がショット仕様に一致する。
4. 映像上の着順とレースエンジンの確定着順・タイム・進路が一致する。
5. 横、斜め前、斜め後ろ、後方など、カメラ方向に適した馬・騎手素材を使う。
6. 実況、順位、スタミナなどのUIは映像を隠さず、STAR独自の意匠で表示する。
7. 1200mから3000m、異なる頭数・展開・着差でも破綻しない。

「完全再現」は画素コピーではなく、参考映像と同じ役割・タイミング・視認性・速度感を持つ映像体験を、STAR独自表現で再現することと定義する。

## 2. 非目標と禁止事項

- 商用作品の画像、音声、ロゴ、文章、UI、固有名称を取り込まない。
- 参考映像の競走馬や背景を切り抜いて製品素材として使わない。
- 映像都合でレース結果、タイム、進路、スタミナを変更しない。
- `Math.random()`、ゲーム判断のための`Date.now()`を使わない。
- Webページ内にレース計算やカメラ規則を重複実装しない。
- 目視だけで「似た」と判定しない。

## 3. 現状からの主要変更

現在のWeb版はレース全編を単一の`chaseCamera()`で描いている。これを、局面と画面状態から決定論的にショットを選ぶ`ShotSequencer`へ置き換える。

```text
PositionModel + Course + display time
                │
                ▼
          ShotSequencer
      ┌─────────┼─────────┐
      ▼         ▼         ▼
 camera pose  asset view  framing target
      └─────────┼─────────┘
                ▼
       Perspective Renderer
                ▼
          UI Overlay
```

## 4. 実装工程

### Phase 0 — 証拠と基準の固定

- 参考動画の映像領域を特定する。
- 全カットについて開始・終了時刻、局面、方向、画角、馬群の矩形、注視対象を台帳化する。
- SNS UIや端末UIは測定対象から除外する。
- STARの同じ局面を比較する固定シードを決める。

**成果物**:

- `docs/race-reference-shot-catalog.md`
- `tools/race-reference-shots.json`
- 比較用コンタクトシート（`out/`、非コミット）

**合格条件**:

- 参考動画のゲーム映像区間に未分類時間がない。
- 各ショットに開始・終了、カメラ族、注視対象が記録されている。

### Phase 1 — ショットシーケンサー基盤

- `packages/render`に純粋なショット選択モデルを追加する。
- 距離だけでなく表示時間、局面、先頭、馬群、上位争いを入力にする。
- ハードカットと短い遷移を区別する。
- カメラ状態と素材方向を同じショット定義から返す。
- Webと動画生成ツールが同じAPIを使う。

**合格条件**:

- 同じ入力から同じショット列になる。
- 全編が単一カメラにならない。
- 存在しないコース区間のショットを作らない。
- ショット境界でNaN、画面外全滅、カメラ反転がない。

### Phase 2 — 発走シーケンス

- ゲート入り、静止、開扉、飛び出し、隊列形成を独立状態にする。
- ゲート用の正面・斜め後方素材を用意する。
- 馬体が房からはみ出さない専用静止姿勢を作る。
- 開扉から走行周期への遷移を作る。

**合格条件**:

- 馬、騎手、ゲートの接触関係が自然。
- 全馬が房から連続的に出る。
- 発走直後に中央へ瞬間移動しない。

### Phase 3 — 道中とコーナー

- 引き、横追い、斜め追走、コーナー俯瞰を実装する。
- 馬群の大きさと重心をショットごとに制御する。
- コース曲率とカメラ方位から素材方向を選ぶ。
- ラチ、刈り目、遠景、樹木、スタンドを撮影地点別の背景セットにする。

**合格条件**:

- 馬群矩形が各ショットの安全領域内に入る。
- 画面端で馬が切れない。
- コーナーで走路と馬の進行方向が一致する。
- 道中の映像から最終着順が不当に漏れない。

### Phase 4 — 勝負所・直線・ゴール

- 先頭固定ではなく上位争いを収めるフレーミングを導入する。
- 寄り素材、追う騎手、鞭、脚周期を局面別にする。
- ゴール板、決勝線、距離標を背景セットへ統合する。
- ゴール後は勝ち馬を追い、後続を自然に流す。

**合格条件**:

- 最後の400mで上位争いが画面外へ出ない。
- 確定着順と画面上のゴール順が完全一致する。
- ゴールで全馬が一点へ集まらない。

### Phase 5 — UI・実況・音響

- 実況帯、順位、ゲージの情報優先度と表示時刻を整理する。
- 参考作品の文言や版面は使わず、STAR独自UIにする。
- 実況は状態変化だけを通知し、同文反復を防ぐ。
- 必要なら権利確認済みの独自SEと環境音を追加する。

### Phase 6 — 素材制作

- Image Cockpitを素材ジョブ管理に使用する。
- `imagegen`で方向別の基準画を作り、人手・整列ツールでフレームを統一する。
- 各シートにフレーム数、セル寸法、接地点、向き、用途、LODを記録する。
- 自動生成物を直接採用せず、輪郭、透明境界、接地点、周期を検査する。

進捗（2026-08-16）:

- `diag-front`の1コマ基準画を生成し、クロマキー除去と白背景合成で輪郭を確認済み。
- 基準画から gather / push / flight / landing の4位相を制作し、勝負服重心で縮尺・位置を機械整列済み。
- 4コマ×枠色8行の`horse-diag-front-v1.png`を生成し、`diag-front`ショットへ実装済み。
- 4位相版は実働可能な第1版。最終品質ゲートでは中間位相を加えた8コマ版へ更新する。
- `high-diag`も専用のgather / push / flight / landingを制作し、4コマ×8枠色の実働シートとしてcorner-wideへ接続済み。
- high-diagの生成4枚は勝負服幅による縮尺差が約1%以内。勝負服重心で機械整列済み。
- sideの既存8コマは再生成せず、セル境界へ漏れた隣接コマ断片を連結成分クリーナーで除去し、版付きv3へ移行済み。
- diag-rearを専用4コマ×8枠色で制作し、rearとの素材共有を解消。発走・隊列形成・コーナー追走へ接続済み。
- 素材契約は`apps/web/public/art/horse-diag-front-master.json`に記録。
- 関連テスト9件、TypeScript型検査、Next.js本番ビルドを通過済み。

必要な最小素材族:

- gate-idle-diag-rear
- run-side-wide / run-side-close
- run-diag-front-wide / close
- run-diag-rear-wide / close
- run-rear-wide / close
- drive-side / drive-diag
- celebrate-rear / celebrate-side

### Phase 7 — 自動比較と回帰防止

- 固定時刻の参照フレームとSTARフレームを並べる。
- 馬群矩形、馬の代表幅、重心、走路占有率、地平線、切れ頭数を測る。
- ショット列と切替時刻をスナップショット化する。
- 着順、進路、決定論の既存テストを維持する。

進捗（2026-08-16）:

- seed 42・1600mについて、8ショット族の代表フレーム、接触判定値、コンタクトシートを自動生成する`tools/audit-race-broadcast.mjs`を追加。
- 初回監査でcorner-chaseの6頭切れ、straight-wideの3頭切れを検出し、再測定で全ショット12頭表示・切れ0へ修正。
- 透視投影外の黒い未塗装領域を解消。
- 内馬場、管理路、二段ラチ、刈り筋を中継画面向けに再構成し、背景全面初期化の回帰テストを追加。
- 樹林・距離標識・照明塔をコース座標へ実装。近景で巨大化する樹冠は画面上限を設けて除外。
- 注視地点から4種の背景区間を決定し、直線の観客席、向正面の設備棟、コーナーの樹林密度を切替。全周建築による誤投影は注視地点前後へ限定して解消。
- 測定結果と残課題は`docs/race-broadcast-audit-seed42.md`に固定。
- 監査を距離・頭数・seed・刻み幅指定へ拡張し、0.25秒刻みと厳密な終了時刻（winnerを含む）を連続検査。
- 終盤カメラを距離適応化。12頭5距離と6頭・18頭の距離端ケース、合計4,016フレームで全頭表示・切れ0を確認。

### Phase 8 — 距離・頭数・端ケース展開

- 1200 / 1600 / 2000 / 2400 / 3000mを検証する。
- 少頭数、18頭、逃げ切り、大差、団子、追込を検証する。
- 右回り・左回り、芝・ダート、馬場状態を検証する。

進捗（2026-08-16）:

- 1200 / 1600 / 2000 / 2400 / 3000m、各12頭・seed 42の全編連続Framing gateに合格。
- 6頭・18頭の距離端ケース、展開別seed、左右回り、芝・ダート、馬場状態の組合せ監査まで合格。
- high / middle / slow各80、合計240レース・318,825位置状態でTruth / Motion gateに合格。逃げ・先行・差し・追込の全勝利ケースを含む。
- ゴール後の全馬一点固定を描画用ランアウトで解消。確定時刻差を保ったまま決勝線の先へ流し、winnerは確定1着馬を横位置込みで追う専用カットへ変更。
- 芝・ダート×良/稍重/重/不良の8条件をレース計算と背景描画へ接続。追加240レース・329,674位置状態のTruth / Motion gateと、不良芝・良ダート・不良ダートの全編Framing gateに合格。
- レース画面に左回り・右回りを接続。右回り5距離12頭と1600m・3000mの18頭端ケースを全編監査し、長距離corner-chaseの下端切れを距離適応プリセットで修正後、失敗0を確認。
- 足元座標と表示高のフレーム間変化を測る画面座標Motion gateを追加。左右両回り×5距離で同一ショット内の瞬間移動・急拡縮0。未実装だったblend契約を撤去し、全切替を明示的なcutへ統一。
- 馬場別の決定的なキックアップを実装。良芝は無粒子、荒れ芝は芝片、ダートは土煙〜泥跳ねへ4段階で変化し、不良ダート全編でFraming / Motion退行0。

### Phase 9 — 最終受け入れ

技術ゲートをすべて通した後、オーナーがショット単位で判定する。一度合格したショットは、比較画像と測定値を固定し、別修正による退行を禁止する。

## 5. 測定項目

各ショットで最低限、次を記録する。

| 項目 | 内容 |
|---|---|
| shot family | start / wide / chase / corner / close / goal / winner |
| duration | 表示秒数 |
| view | side / diag-front / diag-rear / rear |
| target | gate / pack / leader / contenders / winner |
| horse ratio | 代表馬幅 ÷ 映像幅 |
| pack bounds | 馬群全体の矩形 |
| track ratio | 走路高 ÷ 映像高 |
| horizon | 地平線または奥側ラチのY位置 |
| safe margin | 馬群から画面端までの最小余白 |
| transition | cut / blend と時間 |

## 6. 品質ゲート

1. **Truth gate**: 確定着順・タイム・進路と映像が一致。
2. **Determinism gate**: 同じ入力のフレームハッシュが一致。
3. **Framing gate**: 馬の画面外切れが0。
4. **Shot gate**: 必須ショット族が距離に応じて出現。
5. **Asset gate**: 指定方向と実素材方向が一致。
6. **Motion gate**: 接地点の跳ね、脚周期の飛び、瞬間移動が基準内。
7. **Reference gate**: 参照値との差が許容帯内。
8. **Owner gate**: 技術ゲート通過後の映像確認。

## 7. 実装順序の原則

- 一度に全編を直さず、発走→道中→コーナー→直線→ゴールの順で完成させる。
- 各段階で「基盤」「素材」「比較」「受け入れ」を揃えてから次へ進む。
- コメントで正当化せず、測定値とテストで成立を示す。
- Webとオフライン動画で描画規則を共有する。
- 旧平行投影と新透視投影を無計画に混在させない。

## 8. 最初の実装単位

最初のコミット対象は以下に限定する。

1. 参考ショット台帳の初版。
2. 純粋な`ShotSequencer`とテスト。
3. 透視カメラのショット別プリセット。
4. Web画面を全編単一`chaseCamera()`からシーケンサー利用へ変更。
5. 固定シード42・1600mの比較レンダー。

方向別素材の本制作は、この基盤で必要な方向と表示寸法が確定してから開始する。
# 承認済みビジュアル品質基準

2026-08-17以降、発馬背景`apps/web/public/art/starting-gate-side-v1.png`の画風・密度・質感をレース映像全体の基準とする。馬、騎手、コース、設備の刷新では`docs/race-visual-quality-standard.md`を必ず参照し、1カットずつユーザー目視承認を得る。
