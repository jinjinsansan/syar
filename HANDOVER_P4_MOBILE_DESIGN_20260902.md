# 引継ぎ書 — 次のセッションはここから（2026-09-02）

作成: 2026-09-02 / ブランチ `p4/race-30sec-cuts`
起点: `9fe7660`（前の引継ぎ書）→ ★**21 コミット**（★すべて push 済み・★未 push 0 本）
前の引継ぎ書: `HANDOVER_P4_MOBILE_RACE_20260901.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**

---

## ★0. ★まず知っておくこと — ★**`main` が動きました**

★**`main` は 2026-08-20（`110ff31`）で 13 日間止まっていました。**
★本番 `https://star-two-chi.vercel.app` はそこから作られていたので、
★P4 の作業は ★**1 つも本番に出ていませんでした。**

★2026-09-02、オーナー判断で ★**`main` をブランチへ早送り**しました（★211 コミット / 595 ファイル）。
★以後、★`main` とブランチは同じ場所にあります。★push は両方へ出しています。

⚠️ ★**この事故は「ナレーターの絵が古い」ことに気づかなければ、誰も気づけませんでした。**
★オーナーが「プルダウンが無い・ナレーターが古い」と報告し、★3 つの症状の原因が
★**13 日前のビルドを見ていた**という 1 つに集約されました（正典 R-28 の家族）。
→ ★**稼働中のビルドを外から見る口が、いまも在りません。**（★§5 の判断待ち）

---

## ★1. ★この便でやったこと（★21 コミット・★3 つの塊）

### ★1-1. ★引継ぎ書 §0-4 を解いた → ★**画面の馬は最大 512px**

★前の便の着手条件でした。★**2 度失敗していた測り方を捨て**、
★**描画コードの式をそのまま読む**道具を作りました（`tools/audit-draw-scale.mjs`・読取専用）。

```
npx tsx tools/audit-draw-scale.mjs --seeds 42,332,474,14   ← 10 場 50 鞍 × 4 シード
  中央 234px ／ p90 340px ／ p99 418px ／ ★最大 512px
```

⚠️ ★**前の引継ぎ書の見込みは 2 つ外れていました**:
　★① 画面の馬は「200〜300px」ではなく ★**最大 512px**
　★② 素材は「1536px」ではなく ★**組ごとに 314〜1446px**（★1536 は画布であって馬ではない）
　→ ★**斜め後ろ（432px）と俯瞰（314px）は、いまも 1.19 倍・1.63 倍に引き伸ばして描いています**（★台帳 A-11）

### ★1-2. ★毛色を事前に焼いた（★オーナー決定 **560px**）

| | |
|---|---|
| 道具 | `tools/bake-race-frames.mjs`（★新規・★原版は 1 枚も変えません）|
| 出力 | `apps/web/public/art/baked/` … ★可逆 WebP 42 枚 / 50MB |
| 切替 | ★携帯は ★**既定で焼いた素材**。★PC は従来のまま（★`?baked=1` / `?baked=0`）|
| 検定 | `apps/cli/test/baked-frames.test.ts`（★画面の馬が焼いた高さを超えたら落ちる・★変異で落ちることを確認済み）|

★あわせて ★**描画に到達しない素材 216MB を読むのをやめました**（★絵は 1px も変わりません）。
★内訳: 代替素材 4 組 108MB ／ 中間コマ 48MB ／ `winner-v1` 6MB ／ 勝馬 8 コマ 48MB ／ 勝馬・後方 48MB。

### ★1-3. ★モバイルの版面を作り直した（★デザイン第4〜6便）

★デザイナーへ 3 通出し、★3 通とも回答をいただいて実装しました。

| 便 | 中身 | 状態 |
|---|---|---|
| ★第4便 | モバイル TOP（LP）| ★**実装済み** |
| ★第5便 | ヒーロー画（縦構図 860×1000）| ★**こちらで生成して完了**（★§3-2）|
| ★第6便 | レース演出の入口と全画面 | ★**実装済み** |

★あわせて ★**デザイナーが作っていたモバイル 11 画面**（R-6）を全部実装しました。

---

## ★2. ★いまの状態（★数字）

| | |
|---|---|
| `npm test` | ★**136 ファイル / 1379 件 PASS** |
| `npm run typecheck` | ★**終了状態 0** |
| ★横あふれ | ★**0 / 23 ページ**（★実ブラウザ・360px） |
| ★指で押せない操作 | ★**全 23 ページで 0**（★研究用の画面も含む）|

★測り方:

```powershell
npm run dev:web                                    # ★別の窓（★3210 番）
MSYS_NO_PATHCONV=1 npx tsx tools/verify-mobile-layout.mjs --widths 360,390,430 --rid <レースid> --hid h1
```

⚠️ ★**`--path /` は MSYS にパス変換されます。** ★`MSYS_NO_PATHCONV=1` を付けないと
★`/` が `C:/Program Files/Git` になり、★**1 ページも選ばれず「0 / 0」と出ます。**
★これは「異常なし」ではありません（R-21）。

⚠️ ★**実在の id はローカルの一覧から拾います**（★直書きしない）:

```bash
curl -s http://localhost:3210/races | grep -o 'href="/races/[a-z0-9-]\{8,\}"' | head -1
```

---

## ★3. ★申し送り

### ★3-1. ⚠️ ★**数字が「異常なし」と言い続けた崩れが 5 件ありました**

★**全部、絵を見て分かりました。** ★横あふれ・44px・最小文字の 3 つは、★どれも緑のままでした。

| 画面 | 何が起きていたか |
|---|---|
| ★牧場ホーム | ★`order` を一部にだけ与え、★疲労と次走が ★**馬名の隣**に回っていた |
| ★調教 | ★8 メニューが 1 列になり、★ページが ★**6,444px**。★選んで比べる画面が比べられない |
| ★馬詳細 | ★血統表 5 代が 1 列 78px に潰れ、★名前が ★**ほぼ全部「…」で切れていた** |
| ★レース詳細 | ★公正性の 3 枚が横並びのまま潰れ、★**ハッシュが 1 文字ずつ縦に割れていた** |
| ★TOP | ★ヒーローのロゴが肩の帯と ★**重なっていた**（★絶対配置の入れ子）|

⚠️ ★レース詳細の件は、★**`globals.css` の注記が自分で警告している失敗の 2 例目**です:
　★「`min-width: 0` で潰すのではなく、★**列を減らす**のが直し。★潰すと『はみ出し 0』にはなるが、★**読めない**」

### ★3-2. ★ヒーロー画は ★**こちらで作りました**（★3 回まわしました）

★デザイナーから「★絵の生成は Claude Code / codex ライン側の作業」と回答があり、
★`tools/codex-imagegen.mjs` で生成しました。★プロンプトは `design/art/prompts/lp-hero-mobile.txt`。

| 回 | 結果 |
|---|---|
| ★1 | ★馬は大きいが鼻先が右端に接触・勝負服が白灰で地味 |
| ★2 | ★余白と勝負服は直ったが ★**馬が小さく中央寄り**になり文字の裏に埋没 |
| ★3 | ★四肢が伸びきった瞬間・鮮やかな勝負服。★**下の余った芝を切り詰めて**上へ大きく（★採用）|

⚠️ ★道具は ★**「生成物が複数あったら選ばずに止まる」**作りで、★2 回それに当たりました（★2 枚・3 枚）。
★取り違えを防ぐ正しい振る舞いなので、★**中身を見て選びました。**

⚠️ ★**私は前言を訂正しました。** ★「ゲーム内の絵は 2D の様式画なので、実写のヒーローとは落差が出る」
★と報告しましたが、★参照素材と中継の実物を見たところ ★**ゲーム内の馬も写真調**でした。★誤りです。

### ★3-3. ⚠️ ★**`?dev=1` が、コースを見比べる唯一の道を塞いでいました**

★開発卓（レース選択・シード・走路・馬場・回り・自馬）を `?dev=1` の中へ入れたところ、
★**10 場 44 通りの走路を見る道が 1 本も無くなりました**:
　★① プルダウンは `?dev=1` の中
　★② 番組表は ★**競馬場を出していません**（`races_public` に `course_id` はあるが画面に無い）
　★③ `/race` を直接開くと既定の 1 鞍に固定

⚠️ ★**デザイナーの入口の設計も「番組表から決めて来る」前提**で、★選び直す口がありませんでした。
★そのまま実装したので、★**設計にも実装にも穴が無いように見えていました。**
★気づいたのは ★**オーナーの「他のユーザーが見比べたい時は？」**です。

→ ★入口に ★**「ほかのコースを観る」**を足しました（★A 案・オーナー決定）。
★競馬場ごとに ★**回り・1周・直線の長さ**を出しているので、★選ぶ前に違いが分かります。

### ★3-4. ⚠️ ★**シェルの罠を 2 つ踏みました**

★① ★**バッククォートが bash に食われました。** ★`python -c "..."` の中に `` `order` `` と書いたところ、
　★コマンド置換として実行され、★**注記からその語だけが消えました**（★CSS 本体は無事）。
　★→ ★**日本語やバッククォートを含む編集は、★`.py` ファイルに書いてから実行すること。**
　★（★記憶にある罠で、★それでも踏みました）

★② ★**パッチを作っただけで実行し忘れました。** ★型もテストも通り、キャプチャも撮れたのに、
　★画面には古い内容が出ていて気づきました。★**絵を見ていなければ「入れた」と報告していました。**

### ★3-5. ⚠️ ★**dev サーバーが 2 回、応答しなくなりました**

★長く回していると ★**1.4GB まで膨れて 200 を返さなくなります**。★`CDP がタイムアウト` はこれです。
★→ ★ポートを持っている PID を落として立て直す:

```bash
netstat -ano | grep LISTENING | grep ":3210"   # PID を拾う
taskkill //PID <PID> //F
npm run dev:web
```

### ★3-6. ⚠️ ★**テストの完了を待たずに push しました（2 回）**

★1 回目は「待ちます」と言った直後、★2 回目はオーナーの指示に従ってです。
★どちらも結果は緑でしたが、★**順序としては先に確認すべきでした。**
★3 回目以降は待ってから出しています。

---

## ★4. ★次の一手

| ★順 | ★何を | ★なぜ |
|---|---|---|
| ★1 | ★**実機で見てもらう**（★本番）| ★下の全部がそこで決まります |
| ★2 | ★**勝負服と接地影も焼く**（★台帳 A-12）| ★`?baked=1` のキャンバス 152MB のうち ★勝負服 123MB / 影 28MB。★毛色は 0 になりました |
| ★3 | ★**接地影の口を作る**（`shadow` に切り出し位置を足す）| ★足せば影も焼けます。★いまの型はアトラスを渡せません |
| ★4 | ★**斜め後ろ・俯瞰の素材**（★台帳 A-11）| ★1.19 倍・1.63 倍で引き伸ばし中。★**焼いても直りません**（★素材を作り直す話）|
| ★5 | ★**番組表に競馬場名**（★B 案）| ★`course_id` はある。★どの競馬場か分からないのは番組表として不足 |
| ★6 | ★**稼働中のビルドを外から見る口**（★R-28）| ★§0 の事故を二度と起こさないため |

---

## ★5. ★オーナー判断待ち

| # | | |
|---|---|---|
| ★① | ★**実機の確認**（TOP の馬・入口・コース選択・全画面）| ★[EYES] でしか決まりません |
| ★② | ★**番組表に競馬場名を出すか**（★§4-5）| |
| ★③ | ★**`/race` をサーバー確定の再生に移すか**（正典 §0.2-4）| ★いまはブラウザ内でエンジンを回す開発用のデモ。★前便からの持ち越し |
| ★④ | ★**ヒーロー画を作り直すか** | ★3 回目を採用しましたが、★オーナーの目で見て違えば作り直します |
| ★⑤ | ★前便からの持ち越し（★A-1 / A-3 / A-4 / A-5 / A-8 / A-9・★race の撮影用シーク）| |

---

## ★6. ★覚えておくこと（★毎回引っかかる）

| | |
|---|---|
| ★開発卓 | ★`/race?dev=1` を付けないと出ません。★消してはいません |
| ★鞍の切替 | ★`?surface=` が外れます（★「ダートの鞍なのに芝」を防ぐため）|
| ★本番の URL | ★`https://star-two-chi.vercel.app`。⚠️ ★前の引継ぎ書の `star-igvzeiaa6-...` は ★**前回のプレビュー**でした |
| ★`/race` の測定 | ★ローカルでは素材を読みません。★数字は本番でしか出ません |
| ★`--path /` | ★`MSYS_NO_PATHCONV=1` が要ります（★§2）|
| ★push | ★オーナー指示があるまで行いません。★**テストの緑を確認してから**出します |
