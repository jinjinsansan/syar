# REPORT_P2_DEPLOY — 配備方式を移行／★前便の数字の訂正と、繋がっていない経路の申告

**開発側 → レビュー側** / 2026-08-09
**コミット**: `3f8f1af`（push 済み・443 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**★あり（§4）。** §10.2 の開催中止を**何が引き金にするか**が正典にありません。

---

## 1. ★前便の数字を訂正します — 定常は 275秒ではなく 138秒です

前便で「1周2レース＝275秒＝予算の46%」と報告しました。**startup と追いつきの値です。**

`racesToPrepare` は `[C+1, C+2]` を返しますが、**`C+1` は前の周で作成済み**なので、
定常状態で新しく作るのは**1本だけ**です。本番ログでも確認しました。

```
[worker] cycle=162 ... 生成=[164] 既存=1 確定=[161]
[worker] cycle=163 ... 生成=[165] 既存=1 確定=[162]
（今日の全周のうち 生成=[] が663回、1本生成が各1回。2本生成は0回）
```

```
  定常                1本 × 138秒 = 138秒   予算の 23%
  起動直後・追いつき   2本 × 138秒 = 275秒   予算の 46%
```

**余裕は報告した倍あります。**

---

## 2. サイクル超過時の挙動（ご指示の確認）

| 問い | 答え | 根拠 |
|---|---|---|
| 二重に走るか | **走りません** | 単一プロセスの逐次ループ。加えて `pg_try_advisory_lock` で二重起動は空振り |
| 次周をスキップするか | **しません** | `racesToPrepare` は**現在時刻から**決まるので、長い周のあと次周は新しい現在から作り直す。存在確認で欠落を埋める |
| 遅延して追いつくか | **追いつきます** | 2周先まで作るので **1200秒の余裕**があります |

`TICK_MS=60秒` を超える周は `setTimeout` の下限 1秒で連続実行になります。
**138秒 vs 1200秒の余裕**なので、8倍以上の余裕があります。

### ★ただし確定が生成のぶん遅れます（提案・実装していません）

`runCycle` は **① 生成 → ② 確定**の順です。確定の期限はサイクル境界ちょうどで、
**生成の引き金も同じ境界**なので、**確定が毎回 138秒遅れます**（今は生成が0.4秒なので見えません）。

```
現在: 境界 → 生成138秒 → 確定        客が結果を見るまで +138秒
提案: 境界 → 確定 → 生成138秒        客が結果を見るまで +0秒
```

生成は2周＝1200秒の余裕を持つのに対し、**確定は客が待っている処理**です。
両方とも冪等なので順序を入れ替えても A-2 は壊れません。

★**指示の範囲外なので実装していません。** 採否をご指示ください。

---

## 3. 配備方式を移行しました（条件②）

```
/opt/star-releases/<sha>/   ← 木を丸ごと作る（git archive + npm ci）
/opt/star-current -> ...    ← ここを張り替えて restart
```

ご指摘のとおり、前案（同じ木を置き換えてから検証）では**穴が残ります** —
Node は必要になるまで require しないので、**後から読んだときに新旧が混ざります**。

### ★自分の設計の穴を1つ潰しました

ヘルスチェックの待ちを **180秒**にしていました。D-035 で1周が最大275秒になるので、
**健全な配備を落とします**。600秒にしました。
「時間内に出なかった」を「壊れている」と読む誤りで、**R-21 の拡張そのもの**です。

### ヘルスチェックを本番機で三方向確認しました

```
① 健全なサービス            → 通過（偽陰性なし）
② 起動できないサービス       → 検出。理由も "Unable to locate executable" で一致
   （★2026-08-09 の7分停止と同じモード）
③ 起動はするが周を回さない   → 検出（★プロセス生存では見ていない証拠）
```

テスト用ユニットで実施し、後片付けも確認済み（残存0件）。**本番には触れていません。**

### 本番を新方式に移行しました（★挙動は変えていません）

**稼働中と同じ `dcd2373` を配備し直しました。**新方式そのものを本番で検証するためです。

```
プロセスが見ている木: /opt/star-releases/dcd23735...
停止時間 89秒（★周の切れ目まで待つ正常な停止。SIGKILL ではありません）
deploy.sh の冪等経路も確認（既配備なら健全性だけ見る）
```

`tools/star-worker.service` を版管理下に置き、`TimeoutStopSec` を 90 → **330秒**にしました
（周の長さを下回ると、毎回 SIGKILL で落とすことになります）。

---

## 4. ★申告 — §10.2 の開催中止が、ゲームループから一度も呼ばれていません

前便で「§10.2 開催中止・§9.1 返還を**実装**しました」と報告しました。**関数はあります。**
**しかし呼び出しているのは検証スクリプトだけです。**

```
$ grep -rn cancelRace --include=*.ts --include=*.mjs .
  apps/worker/src/cancel.ts:34   （定義）
  tools/verify-cancel.mjs:8,36,42（検証スクリプト）
  ★ゲームループからの呼び出し: なし
```

**「実装した」と「本番の経路に繋がっている」は別**でした。
`dcd2373` が VPS に載っていなかったのと**同じ形**で、今度は同じ木の中にあります。

### なぜ塞げないか — 引き金が正典にありません

§10.2 は「**生成失敗時**はそのラウンドを開催中止とし全ベットを EP で返還」と定めますが、
**何をもって「生成失敗」とするか**が書かれていません。私が決める話ではないので照会します。

```
候補① 発売開始時刻を過ぎてもレースが存在しない
       → ただし存在しないレースには馬券が無いので、返還する対象がありません
候補② 確定が N 回連続で失敗した（★馬券は既に売れている＝返還が要る唯一の経路）
       → N と、確定が永久に失敗し続ける場合の扱いを決める必要があります
候補③ 生成が例外で落ちた
       → 現状 runCycle は例外を握って次周で再試行します。
         何周までなら再試行で、何周からが中止か
```

★**現状、馬券が売れたレースの確定が永久に失敗すると、bets は pending のまま残ります。**
  客の EP も PP も動きません。頻度は低いですが、**時間切れの規定が無い**のは穴です。

---

## 5. 条件① の V-10（実行中）

`verify-pmin` を100レースに変更し、**レース間SD と SE** を出すようにしました。
ご指摘のとおり、1つの出走表を10万回引いても**出走表間のばらつきは1標本のまま**です。
K はレース内、RACES はレース間の分散を潰すので**両方要ります**。

★**N=100 で足りるかは推測せず、測った SE で報告します。**

実測で1レース約4分かかっており、完了は本日19時ごろの見込みです。
**結果が出るまで新しいコードは本番に入れません。**

---

## 6. 本番の状態

| | |
|---|---|
| 稼働コード | `dcd2373`（返還の修正まで。★D-013 と D-035 は未配備） |
| 配備方式 | **★リリースディレクトリ＋symlink に移行済み** |
| ユニット | `/opt/star-current` を指すよう差し替え済み・`TimeoutStopSec=330` |

---

## 7. A-1

```
生成した周 94+ / 周の欠落なし / メモリ 69MB / FD 26 / DB接続 1
★本日の再起動: 2回（7分停止の復旧 / 配備方式の移行 89秒）
```

★§1 のとおり **M 変更後の再計測が要ります**。そのときに §2 の超過挙動も実測で確かめます。
