# 指示書 — ★走路の模型を広げる 段階①「器」（2026-08-31）

**案件**: ★**STAR**（ポイ活型オンライン競馬育成・`V:\dev\Cusor\star`）
**発行**: 2026-08-31 ／ レビュー側 → 開発側
**出どころ**: ★`REVIEW_P4_COURSE_MODEL_VERDICT_20260831.md`（★裁定）／★**オーナー決定 2026-08-31**
**ブランチ**: `p4/race-30sec-cuts`（起点 `425d47b`）

> ★**この便は「器」だけです。** ★**10 場の数値を決めないでください**（★§3 で理由を書きます）。

---

## ★0. オーナー決定（★この 2 件は決まりました）

| # | ★決定 |
|---|---|
| ★**1** | ★**段階①は「器」だけ先行してよい。** ★幾何の設計と検査の 44 通り化までを進め、★**10 場の数値の確定は帯の確定後** |
| ★**2** | ★**帯は「案 1」** — ★**②a（4〜12 馬身・★上限 12 も含めて）を残し、②b（枠順由来 ≤ 1 馬身）を足す。** ★**見直し時期: 段階①が終わった時点で上限 12 を改めて見直す** |

★**正典に起票済みです**（★2026-08-31）:

```
  V-18 の行   ★②a / ②b の 2 本立てに改訂
  D-090       ★②b を新設し、②a は上限も含めて残す（★8/30 裁定の「撤去」から差し替え）
  D-091       ★走路の寸法は「機能的な作法」の側であり採ってよい
  D-092       ★幾何（段階①）と勾配（段階②）を別の便に分ける
  D-093       ★公開前の法務確認に「権利（§0.1）」の項目を足す
  R-32        ★正典のゲートは検定（CI）の中に置く
  R-33        ★検査を 1 つの既定で書いたら、それは既定しか守らない
```

⚠️ ★**8/30 の裁定書 `REVIEW_P4_V18_CEILING_VERDICT_20260830.md` は、冒頭に失効の告知を付けました。**
★**§5-1（上限 12 を撤去）と §9（起票案）は実行しないでください。** ★§4 は生きています。

---

## ★1. この便でやること（★順番どおりに）

### ★1-0. ⚠️⚠️ ★**最初にこれ** — ★**A-10（新設）: 発走のカメラで、ゲートと地面が別々に描かれて交差している**

★**出どころ: オーナー [EYES]（2026-08-31）**

> ★**ゲートがある地面が別の絵と交差しています**

★**レビュー側が実画面で確認し、切り分けまで済ませました**（★`out/_real/gate_aone_10.png` / `gate_ousei_10.png`）。

| | ★青嶺記念 2200m（★A-8 の破綻鞍） | ★桜星賞 1600m（★A-8 は合っている鞍） |
|---|---|---|
| ★ゲートの足元を芝の境界線が斜めに横切る | ★**出る** | ★**出る** |
| ★右の白柵がゲートの右端と重なり、そのまま手前へ抜ける | ★**出る** | ★**出る** |
| ★ゲートの奥にもう 1 本の柵が水平に走る | ★**出る** | ★**出る** |
| ★（参考）カットのラベルが発走直後に「最後の直線」 | ★出る（sec 11・残り 2102m） | ★**出ない**（正しく「スタート後」） |

→ ★**A-8 とは独立です。** ★桜星賞は `_cutgeom.mjs` で「合っている 16 鞍」の 1 つ（早すぎ 1m）なのに、★**同じ交差が出ます。**
→ ★**鞍にも走路の形にも依存していません。** ★**だから模型を広げる前に直せます。**

★**求めるもの**:

1. ★**原因の特定** — ★どの層とどの層が別々に描かれているのか。⚠️ ★「見えにくくした」で済ませないこと（★台帳の作法）
2. ★直し
3. ★**検査** — ★**機構を戻すと落ちること**まで確かめる（R-14 の形）
4. ★**再現手順を報告に書く**（★鞍と秒。★`--sec 10` / `race=g2-aone`・`race=g1-ousei` で再現します）

★**判定はレビュー側が静止画で行います。** ★★**この破綻は静止画に出るので、オーナーの目を使いません。**
★（★これまで「絵の判定は必ずオーナーへ」としてきましたが、★それは**動きと「感じ」**の話です。
　★**2 枚が重なっているという幾何の破綻は、レビュー側が撮って見て判定できます。**）

### ★1-1. ⚠️ ★**着手条件** — ★`lane-geometry.test.ts` を 44 通りに広げる（★A-10 の次に）

★いまの突合は ★**`DEFAULT_OVAL` の 1 場だけ**を見ています:

```ts
// packages/race-engine/test/lane-geometry.test.ts:27-40
const mine   = ovalSegments(d);          // ★spec を渡していない
const theirs = ovalCourse(d).segments;   // ★同上
```

→ ★**`OvalSpec` を広げたとき、10 場のどこかで二重実装（`ovalSegments` / `ovalCourse`）が離れても緑のままです。**

★**求めるもの**:

1. ★**10 場 × 実距離 = 44 通り**で突き合わせる（★`raceSetupById` から spec を引くこと。★自前で組まない）
2. ★**片方だけ直した状態で落ちること**を確かめる（R-14 の形）
3. ★走路の幅・区間の長さと種類・半径・距離ロスの 4 点すべてを 44 通りで見る

⚠️ ★**これが通るまで `OvalSpec` に触らないでください。** ★正典 R-33 です。

### ★1-2. ★②b を検査に入れる（★正典 D-090）

★`lane-reveal.test.ts` に ★**②b（枠順由来の内外差 ≤ 1 馬身）**を足してください。

| | |
|---|---|
| ★定義 | ★**多数シードで均した枠間の平均差**（★1 レースごとに出して平均しないこと・台帳 B-5） |
| ★実測（44 通り） | ★**0.242〜0.503 馬身**（★1 馬身超え 0/44） |
| ⚠️ ★**必須** | ★**旧形（`LANE_MODEL_LEGACY`）に戻すとこの検査が落ちること**を同じ便で確かめる |

### ★1-3. ★V-18 を `npm test` に載せる（★正典 R-32）

★`tools/verify-v18.mjs` は ★**手で回す道具**で、CI にありません。
★**そのせいで 0.18 が ② を PASS→FAIL させてから 9 日間、検定は緑のままでした。**

⚠️ ★**そのまま CI に載せると重すぎます**（★44 通り × 2000 レースは分単位）。
→ ★**軽い版を検定に置き、重い版を道具に残す**形にしてください。
　★軽い版でも ★**「機構を止めたら落ちる」ことを確かめる**こと（R-16）。★通るだけの検査を置かない。

### ★1-4. ★`OvalSpec` を広げる（★★設計と器だけ）

★広げる先（★裁定 §4・§5）:

| | |
|---|---|
| ★**コーナーごとの半径** | ★4 本を別々に持てるようにする |
| ★**内回り／外回り** | ★同じ場が別のレースになる |
| ★**引き込み線** | ★★**§2 を読んでから設計すること**（★これは飽き対策ではありません） |

⚠️ ★**勾配（段階②）はこの便に入れないでください**（★正典 D-092）。

---

## ★2. ⚠️ ★引き込み線について — ★いまの模型は在り得ない形を作っています

★`withRunUp` は、発走から `SETTLE_M = 250m` の直線が足りないとき
★**コーナーの先頭側を直線に置き換えています**（`lane.ts:66-79` / `course.ts:135-148`）。

★レビュー側の実測（`tools/_terrain.mjs`）:

```
  新星賞（月見丘 1600m）    発走188→発走63→2角175→向正面350→3角238→4角238→直線350
                           ↑ ★1角が丸ごと消え、2角も 63m 削られている
  白露賞（白砂 1600m）      発走180→発走70→2角210→…      ★同じ
  陽炎カップ（陽光台 1600m） 発走73→発走178→2角45→…      ★2角が 45m しか残っていない
```

★**同じ競馬場なのに、距離ごとにコーナーが消えます。** ★1600m は 10 場中 9 場でこの形です。

★実際の競馬場はこうしません。★**コーナーは物理的に在り続け、発走地点のほうを外へ出します**（★引き込み線）。
→ ★**引き込み線は「表現できないものの 1 つ」ではなく、★いまの歪みの解です。**

⚠️ ★`withRunUp` が入った理由は正当です（★コーナー発走だと V-18 ① が 0.539 まで上がる・`lane.ts:44-47`）。
★**問題は解き方です。** ★「コーナーを消す」ではなく「発走地点を直線側へ出す」形に置き換えてください。

⚠️ ★**ただし [EYES] が先です。** ★「1 角が消えていることが実画面で分かるか」は
★**オーナーにしか判定できません**（★私は画面を見られません）。★**判定を待ってから作り込むこと。**

---

## ★3. ⚠️ ★この便で ★**やらないこと**

| ★やらない | ★なぜ |
|---|---|
| ★**10 場の数値を決める**（どの場を半径いくつにするか） | ★**上限 12 の見直しが段階①の完了時に控えています。** ★いま数値を決めると、見直しのあとで選び直しになります |
| ★**勾配（段階②）** | ★正典 D-092。★混ぜると V-18 が動いた原因が分からなくなります |
| ★**A-8（カットの割り当て 34/50）を直す** | ★オーナー判断「B」。★**模型を広げたあとに 1 回だけ**。★先に直すと同じ場所を 2 回書きます。⚠️ ★**ただし A-10（ゲートの地面）は別物で、★§1-0 のとおり先に直します** — ★走路の形に依存しないことを実画面で確認済み |
| ★`apps/web/package.json` に触る | ★オーナーの既存作業（ポート 3210） |
| ★`RACE_RANDOM_K` / `laneExtraMeters` / 較正定数 | ★**着順に効きます**（憲法） |

---

## ★4. ⚠️ ★受け入れ基準（★ここが前便と違います）

### ★4-1. ★**V-18 の ① と ② を両方取り直すこと**

★照会には「段階①のエンジンへの影響は `laneExtraM` の経路 1 本」とありました。
★**経路が 1 本であることは正しい**（`race.ts:141` のみ）のですが、★**その 1 本の中で半径は 2 か所に効きます**:

```
コーナーごとの半径を持たせる
   ├→ ★積分            extra += (w − centre) × (len / seg.radius)      （lane.ts:418）
   └→ ★totalTurn → swingScale → ★w そのもの                            （lane.ts:357）
                                  ↑ ★見落とされていた側
```

★`laneAt` は通り道の幅に `swingScale(distanceMeter, spec)` を掛けています。
→ ★**半径を変えれば全馬の横位置の分布が動き、① にも効きます。**

⚠️ ★**2026-08-31 に踏んだ「通り道の幅に `swingScale` を掛け忘れ、長距離が 12.4 / 15.7 馬身に悪化」した事故は、
★まさにこの系統です。** ★同じ地形をもう一度歩きます。

### ★4-2. ★**先に「半径だけを動かしたときの地図」を作ること**

★模型を広げる**前に**、★既存の 10 場で ★**半径だけを動かして ① ② がどう動くか**を測ってください。

★これが無いと、★44 通りが落ちたときに ★**「幾何のせいか `swingScale` のせいか」が分かりません。**

### ★4-3. ★基準値（★形を触る前・★レビュー側が独立に測ったもの）

```
  ★verify-v18.mjs --venues  … ★44 通り全 PASS ／ ② 7.8〜11.2 馬身 ／ ① 最大 +0.070

  ★上限 12 に近い場（★形を触ると最初に破れます）
    月見丘  11.0  11.2  10.9  10.9    ★余裕 0.8〜1.1
    白砂    10.9  10.9  10.9  10.9    ★余裕 1.1
```

---

## ★5. ★差し戻し（★前便から・★8 件）

| # | | 出どころ |
|---|---|---|
| 1 | ★**`venues.ts` の月見丘の註記を「10.9〜11.2 / 余裕 0.8」に直す** | ★裁定 §2-1。★実測より甘い数字がコードの中にあります |
| 2 | ★**「注意が要るのは月見丘だけ」を「月見丘と白砂の 8 鞍」に直す**（照会 Q-4・台帳） | ★裁定 §2-2 |
| 3 | ★**段階①の受け入れ基準に ① を加える**／★`swingScale` の地図を先に作る | ★裁定 §4-2・§6-2（＝本書 §4-1・§4-2） |
| 4 | ★**`lane-geometry.test.ts` を 44 通りへ**（★着手条件） | ★裁定 §6-1（＝本書 §1-1） |
| 5 | ★**`types.ts:71` の「この 11 形質」を 12 に直す** | ★裁定 §7-3。★`heavy_aptitude` が足されたときコメントが追随していません |
| 6 | ★**`_cutgeom.mjs` の合否を両側にする**（★「早すぎ」が負の鞍＝直線を使えていない側も数える） | ★裁定 §1-4 |
| 7 | ★**「レビュー側の作業ツリーに `tools/_*.mjs` は無い」という記述を削る** | ★裁定 §1-5。★**実在します。本裁定では実際に回しました** |
| 8 | ★台帳に ★**A-9（走路の形が距離ごとに変わる／コーナーが消える）**を新設 | ★裁定 §5（＝本書 §2） |

⚠️ ★**4 を最初にやること。** ★これが無いまま `OvalSpec` を広げると、二重実装が離れても緑のままです。

---

## ★6. ★報告様式（★この形を続けてください）

★前便から付くようになった ★**§0-2「提出物・コミット・変更範囲」** と ★**§8「期待値つきの再実行表」** は、
★**こちらの照合時間を桁で縮めました。** ★**続けてください。**

★加えてこの便では:

| | |
|---|---|
| ★**分母をコマンドで数える** | ★「48 通り」と書いて差し戻された件（★正しくは 44）。★自分の出力を数えれば分かります |
| ★**5 seed の道具で判定しない** | ★`_venuelane.mjs` は約 2 割低く出ます（台帳 B-4）。★判定は `verify-v18.mjs`（2000 レース）か `_venueverify.mjs`（200 seed）で |
| ★**相関はプールしてから 1 回だけ取る** | ★1 レースごとに ρ を出して平均すると、n=12 の偶然（\|ρ\| の期待値 ≈ 0.25）が残ります（台帳 B-5） |
| ★**カットの境目の直後は測定に使わない** | ★閃光 3・重ねる 3・切り替え 2 の**全部の直後**が危ういです（台帳 B-2）。★使えた秒は 14 秒・18 秒 |

---

## ★7. ★憲法（★この便で触れるもの）

| | |
|---|---|
| ★**着順に効く** | ★**段階①は `conditions.course` を通じて着順に効きます。** ★`RACE_RANDOM_K` / `laneExtraMeters` / 較正定数には触らないこと |
| ★演出 3 | ★着順・タイム・着差・払戻・位置に触れない |
| ★演出 4 | ★`Date.now()` / `Math.random()` を使わない |
| ★権利 | ★**D-091（新規）**: ★寸法の**型**は採ってよい。★**特定の 1 場の寸法の組をそのまま写さない。** ★数値を決めた便は、その決め方を `venues.ts` の註記に残すこと |
| ★法務 | ★**D-093（新規）**: ★§0.3 の弁護士ゲート（S-1〜S-5）は**経済構造だけ**。★権利は別項目。★**S-1〜S-5 の表に権利を混ぜないこと** |

---

## ★8. ★オーナーの [EYES] 待ち（★私は画面を見られません）

| # | | 状態 |
|---|---|---|
| ~~1~~ | ~~50 鞍が画面に出るか（動きで）~~ | ★**済み（2026-08-31）** — ★オーナー評「★**コースの違いが目で分かる**」。★B案 ④ は効いています |
| ~~2~~ | ~~ゲート周りの絵~~ | ★**済み（2026-08-31）** — ★オーナー評「★**ゲートがある地面が別の絵と交差しています**」→ ★**A-10 として §1-0 へ** |
| ★**3** | ★**1600m の鞍で「1 角が消えている」ことが分かるか**（§2） | ⚠️ ★**保留。** ★いまは**絵の破綻のほうが目立って**おり、コーナーの数まで見える状態ではありません。★**A-10 を直してから、改めて見ていただきます** |
| 4 | ★レース選択のプルダウン | ★未コミットのまま作業ツリーにあります |
| 5 | ★ダート素材の承認 ／ A-5（刈り目） | ★持ち越し |

⚠️ ★**作業ツリーに未コミットの変更が 2 本あります**（★`race-setup.ts` / `page.tsx`・2026-08-31 12:26 頃）。
★引継ぎ書（08:21）に載っていません。★**この便を始める前に、扱いを決めてください。**
