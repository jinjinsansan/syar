# 報告 — 「かくかく曲がる」の正体は、カット途中の左右反転だった

作成日: 2026-08-25 / ブランチ `p4/race-30sec-cuts` / 基点 `e009b34`
発端: オーナー評「**馬の曲がり方走り方が滑らかに曲がっていない。かくかく曲がっている**」（第4コーナー）

---

## 1. 何が起きていたか

馬の絵は板（ビルボード）なので、**向きは素材の選び分けと左右反転でしか変わりません。**
`broadcast-v2-scene.ts` は注視点の進行方向とカメラの向きの角度 `viewDeg` を 1 つ求め、

- `viewDeg < 60` → 後ろ向き素材 / `> 120` → 正面素材 / それ以外 → 真横素材（★3 段の閾値）
- 進行方向の投影が `forwardDx < 0` なら**左右反転**

としていました。この反転を**毎コマ**決めていたため、コーナーを回る途中で符号が変わり、
★**馬だけが 1 コマで裏返って**いました。

**実測**（`tools/audit-corner-turn.mjs`・30fps）:

| ショット | 向きの角度 | 1コマ最大の跳び | 素材の入替 | 左右反転 |
|---|---|---:|---:|---:|
| `start-front` | 168° 一定 | 0.00°/コマ | 0 | 0 |
| `first-corner-front` | 158° 一定 | 0.00°/コマ | 0 | 0 |
| `side-drive` | 87〜89° | 0.47°/コマ | 0 | 0 |
| **`fourth-corner-front`** | **141〜180°** | 1.89°/コマ | 0 | ★**1 回（表示 19.20s）** |
| `homestretch-side` | 86〜88° | 0.24°/コマ | 0 | 0 |
| `finish-line` | 87° 一定 | 0.00°/コマ | 0 | 0 |

★**シード 42 / 49 / 91 / 140 / 217 / 333 の 6 本すべてで 1 回**起きていました。
seed 特有ではなく、**毎レース起きる**現象です。

実画面のコマでも確認しました（`out/2d-overhead-stride/v5after/f0089.jpg` と `f0091.jpg`）。
**背景は反転せず、馬の絵だけ**が左右反転しています。

### ★これはこの案件で 3 度目の同じ形です

| | 症状 | 原因 |
|---|---|---|
| ① | 順位が入れ替わると注視点の横位置が 237px 飛ぶ | カットの途中で真偽が切り替わる |
| ② | 先頭差が境目をまたぐと画角が 13°→6° に跳ぶ | 同上（2026-08-25 に修正） |
| ③ | ★**コーナーの途中で馬だけ裏返る** | 同上（今回） |

この案件の基準は「**カットの途中で跳ぶのは不具合／カットの境目なら許容**」です。

---

## 2. 直し方

反転の判定を**そのカットが始まった地点で 1 回だけ**行い、カット中は変えません。

| ファイル | 変更 |
|---|---|
| `packages/render/src/broadcast-v2.ts` | `broadcastV2CutStartMeters()` を新設（★カット境界は**台本の表から引く**。作り直さない・R-30） |
| `packages/render/src/broadcast-v2-scene.ts` | `BroadcastV2Scene.faceFlip` を新設。描画側は自分で決め直さずこれを読む |
| `packages/render/test/face-flip.test.ts` | 新規テスト 3 件 |

- ゴール後（`allFinished`）は台本の表の外なので、これまでどおり現在地で決めます。
- ディゾルブで直前のショットを描くとき（`forceShotId`）は、**そのショットのカット**の始点で決めます。
  今のカットの始点で決めると、重ねている 0.28 秒だけ裏返ります。

★**着順・馬の位置・カメラ定義値・台本・素材には触れていません**（憲法 3）。
★時刻も乱数も使いません（憲法 4）。

### 副作用（正直に）

反転を固定するので、カットの後半は**本来の向きと逆に映る時間**があります。
今回の箇所では反転が起きるのが `viewDeg` 170〜180°（＝ほぼ真正面）の区間なので、
**左右の差がいちばん小さいところ**です。それでも「向きが厳密でない」ことは事実です。

---

## 3. 検査

| | 結果 |
|---|---|
| `npm test` | ★**114 ファイル / 1124 件パス**（新規 3 件を含む・終了コード 0） |
| `npm run typecheck` | エラーなし |
| 反転の回数（6 シード） | ★**直す前 各 1 回 → 直した後 0 回** |
| `/race`・`/race?cinematography=v4` | 200 |

新テスト `packages/render/test/face-flip.test.ts`:

1. 台本 v5 で、反転が変わるのは**カットの境目だけ**（先頭 1m 刻みで全区間）
2. 台本 v4 でも同じ
3. ★**旧方式なら第4コーナーのカットの中で反転していた**ことを同じ入力で確認
   （R-14: 直っていない状態なら落ちる検査になっているか）

---

## 4. ★まだ残っている「かくかく」の芯

★**これで曲がりが「滑らか」になったわけではありません。**
板の絵である以上、向きは **3 段の素材 ＋ 左右反転**しか表現できません。
第4コーナーでは向きが 141°→180° と 39° 変わりますが、**絵は 1 度も回りません**。
今回消したのは「途中で裏返る」という**跳び**だけです。

連続的に曲がって見せるには、次のどれかが要ります（★どれも未着手・要判断）:

1. 方向別素材を**もっと細かい角度**で用意する（素材制作）
2. 板を**わずかに回転**させて残りの角度を吸収する（新しい機構・見え方は未検証）
3. コーナーの画角を、向きの変化が小さい位置に変える（カメラ定義値の変更）

---

## 5. 残っていること

1. ★**実画面でご確認ください**（3210 は稼働中）。裏返りが消えたかどうかです。
2. 本コミットの push 可否。
3. §4 の「芯」（板は回らない）にどう手を入れるか。★開発側では決めません。
4. #3-a / #4（せめぎ合い）は `QUESTIONS_P4_FINISH_CONTEST_20260825.md` で照会を出しました。

---

## 6. ⚠️ 検査のやり方で 1 つ踏みました

`npm test | tail -6` のように**パイプで受けると、失敗しても終了コードが 0 になります**。
実際にこれで「1 件失敗」を一度見落としかけました（新しい道具の分類登録漏れ・R-24 のメタテスト）。
→ ★**要約行を読んで判定する**か、ファイルに落としてから終了コードを見ること。
