# 裁定 — ES-6（距離ロスの下ごしらえをオッズの試行の間で使い回す）（報告 `REPORT_ENGINE_LANE_SPEED_ES6_20260916.md`）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側・オーナー
**対象**: コミット `ccfb684`（ES-6）／ ブランチ `p4/race-30sec-cuts`・HEAD `ccfb684`（origin より 15 先行・push は未実施）
**前の文書**: 回答 `REVIEW_ENGINE_LANE_SPEED_ES5_ANSWER_20260916.md` §2-1・§2-2

> 凡例（R-13）: **✔** ＝ レビュー側がコードを読んで確かめた、または手元で実行した ／ **△** ＝ 未確認（報告の記載に基づく） ／ **計算** ＝ 実測値と定数から出した値
> 本書は正典を編集しません。
> ⚠️ **オーナーの確認 G-1〜G-4 は、本書の発行時点でまだレビュー側に届いていません**（§4）。

---

## 0. 判定

| 範囲 | 判定 |
|---|---|
| ES-6 の実装と検査 | ✅ **受理**（条件なし） |
| 速さ | **G-2 の確認を待って合格とする**。試行 1 回の比は最大 **1.28 倍**で、**最初に引いた線 1.3 倍の内側**（§2） |
| ES 便全体（ES-0〜ES-6） | G-1・G-2 の確認が済めば**閉じる** |

---

## 1. 照合と再実行

| 事項 | 照合 |
|---|---|
| 下ごしらえを任意で受け取る | ✔ `packages/race-engine/src/race.ts:74`（`lanePlan?`）・`:156-157`（渡されたら確かめ、無ければ中で作る） |
| 作り方は 1 か所 | ✔ `race.ts:82-84` `lanePlanForRace`（`resolveRace` の中とワーカーが同じ関数を通る・R-30） |
| 食い違えば投げる | ✔ `race.ts:87-103` `assertLanePlanMatches`: `courseShape` が楕円か・距離・`lapM`・`homeStretchM`・`widthM`・`cornerRadiiM`（有無と 4 値）を値で比べる。`course` の無いレースは `DEFAULT_OVAL` と比べる |
| モジュールに状態を持たない | ✔ 覚える仕組み（`WeakMap` 等）は無い |
| ワーカー | ✔ `apps/worker/src/build-race.ts:119`（試行の前に 1 回）・`:127`（全試行に渡す） |
| 変更したファイル | ✔ `git show --stat ccfb684`: `race.ts`・`build-race.ts`・測定器 5 つ（`verify-payout`・`verify-pmin`・`verify-race`・`bench-mc`・`diag-mneed`）・検査 3 ファイル・報告。`lane.ts`・較正定数・試行回数は触っていない |
| ★レビュー側の再実行 | ✔ HEAD `ccfb684` で `lane-plan-param`・`lane-plan-count`・`lane-fingerprint`・`lane-sum`・`build-race-lane-plan`・`course-frozen-wiring` の **6 ファイル・21 件すべて合格**（4.2 秒）。指紋の最大の差 **距離ロス 3.69×10⁻¹³ m ／ スコア相対 5.18×10⁻¹⁶ ／ タイム 2.84×10⁻¹⁴ 秒**（報告 ES-5 の値と一致）。使い回す版と作り直す版で `buildRace` のオッズの行が 1 ビット一致 |
| 全体の検査 | △ 報告: `Tests 1 failed | 1786 passed (1787)`・赤は既存の `edit-grammar-audit` ⑨ だけ（レビュー側は全体を流していない） |
| 変異 | △ 報告: 一致の確かめを外す → 検査 3 が落ちる ／ ワーカーで渡し忘れる → 検査 4 が落ちる |

---

## 2. 速さの判定

### 2-1. 報告の値（`97364c5^` との比・同じ回どうし・3 回の範囲）

| 比べ方 | 8 条件の最大 | 線との関係 |
|---|---|---|
| ① `resolveRace` 1 回 | **1.47 倍** | 1.3 倍を超える回がある（16 頭 1600m 既定 1.47・1.35 ／ 18 頭 1600m 既定 1.31） |
| ② 試行 1 回（`resolveRace` ＋ ワーカーと同じ的中目の集計・集計のコードは両版とも HEAD） | **1.28 倍** | ★**8 条件すべてで 1.3 倍の内側** |

### 2-2. レビュー側の読み

- ★**線を 1.6 倍に緩める必要はなくなりました。** 回答 ES-5 §2-2 で提案した「1.6 倍」は取り下げ、**線は最初の 1.3 倍のまま**とします。オーナーに確かめていただくのは**測る単位**だけです（G-2）
- **推奨は ② 試行 1 回**。サイクルの時間を決めているのは試行 1 回の中身全体で、的中目の集計は版によって変わらないからです
- ① が 1.3 倍を超える最大の回（1.47）は、報告自身が「どの回も高めに出る」と書いている**最初の条件**（予熱が他より短い）です。その他の条件の ① は 1.31 以下
- **本番機での目安**（計算）: 本番の実測 1 レース 110.8 秒（`70d9982`・2026-09-15）× 1.28 ＝ **約 142 秒**。10 分の **約 24%**、起動直後の 2 本でも **約 47%** で、予算（1 周 60%）に収まる
- △ ばらつきは同じ版でも 2 割前後あります。**本番機での最終の確認は、新しいワーカーを配備した後の周の実測**（ワーカーの記録の「1 レースを作る周」）で行います（§3 の 5）

---

## 3. このあとの順番

| # | 誰 | 作業 | 前提 |
|---|---|---|---|
| 1 | オーナー | G-1・G-2 の確認 | — |
| 2 | オーナー | staging で `npx tsx tools/seed-races.mjs --env staging --races 26 --trials 10000` → `npx tsx tools/settle-races.mjs --env staging` | G-2 |
| 3 | 開発側 | 生成したレースの走路の凍結（10 場・全件楕円・`venues.ts` と一致）と、再計算で着順が全件一致・`--ignore-course-frozen` で食い違い・凍結の無い行の通報の件数（回答 ES-5 §3） | 2 |
| 4 | レビュー側 | 10 場配線の最終裁定（`REVIEW_VENUE_WIRING_VERDICT_20260915.md` の VC-1〜VC-4。V-4〜V-6 と V-18 の残りの計測を含む） | 3 |
| 5 | オーナー | 本番へ `0023` と新しいワーカーの配備。**配備後の最初の数周で、ワーカーの記録の「1 レースを作る周」の秒数を読み取り、1 周 60% に収まることを確かめる** | 4 |
| — | オーナー・開発側 | `0021`・`0022`: 本番 DB の読み取り確認 → 当てる（回答 ES-5 §4） | G-3（配備とは独立） |

---

## 4. オーナーに確かめていただくこと（前回から残っているもの）

| # | 確認 | レビュー側の推奨 |
|---|---|---|
| **G-1** | F-1（ES-5 で結果を丸めの差だけ許す）を承認したか。開発側は「はい」と受け取っているが、レビュー側は直接受け取っていない | 承認済みなら、その旨を開発側・レビュー側の両方に |
| **G-2** | 速さの合格を **② 試行 1 回の比で 1.3 倍以下**で判定してよいか（**線は緩めない**。単位だけ） | **はい** → ES 便は合格 |
| **G-3** | 本番 DB の読み取り確認を許可するか（問題なければ `0021` → `0022`） | 確認を許可 |
| **G-4** | push | オーナーの判断 |

---

## 5. 正典について（参考・オーナーの承認後にレビュー側が起票）

| 候補 | 中身 |
|---|---|
| 距離ロスの計算 | 同じ 10m の和を分解して求めてよい（丸めの差 ≤ 1×10⁻⁹ m を許し、着順の完全一致で守る。前提が崩れる馬は刻みの計算に戻る）— G-1 の後 |
| D-036 の註記 | オッズ計算の所要は**試行 1 回の比**で測る。本番の実測（`70d9982`・1 レース 98〜111 秒）と、測ったコミットを併記する — G-2 の後 |

---

## 🔴 §0・§2 の「1.28 倍」の撤回（2026-09-18 追記・オーナー承認済み）

★**§0 の「試行 1 回の比は最大 1.28 倍で、最初に引いた線 1.3 倍の内側」と、§2-2 の「本番機での目安 約 142 秒」を撤回します。**

✔ **2026-09-18 の実測**（**AL-6**・`REVIEW_AL6_ODDS_COST_VERDICT_20260918.md`）:

| 距離 | ★現エンジン | D-071 前（`97364c5^`） | 比（旧÷現） |
|---|---|---|---|
| 1200m | 24.89 μs | 27.72 μs | 1.11 |
| 2000m | 21.60 μs | 28.10 μs | 1.30 |
| 3200m | 22.58 μs | 31.25 μs | ★**1.38** |

→ ★★**向きが逆でした。現エンジンのほうが 1.11〜1.38 倍速い**（★開発側の測定と、★レビュー側の独立再実行が一致）。
→ ★**「1.3 倍の内側」どころか、比は 1 を下回ります。** ★**したがって「約 142 秒」も根拠を失います**（★正しい本番機換算は **70〜98 秒**）。

### 🔴 なぜ間違えたか — ★**自分で書いた警告を、自分で無視しました**

★**本書 §2-2 に、私はこう書いていました** — 「△ **ばらつきは同じ版でも 2 割前後あります。**」
★**2 割の振れがある測定で、1.28 倍の差を「差」と読みました。** ★**1.28 は、私自身が認めた振れ幅の中にあります。**

✔ 2026-09-18 に振れの正体が判明: ★**ハイブリッド CPU（P コア＋E コア）のどちらに載るかで、同じ道具の 2 回が 25.97 と 42.22 μs に割れる**（★**1.63 倍**）。★**本書の測定がコアを固定していたかは、報告に書かれていません。**

### ★教訓

| # | |
|---|---|
| 1 | ★★**測定のばらつきより小さい差を「差」と読まない** |
| 2 | ★**「線の内側だから合格」は、線と測定の精度を比べてから言う**（★1.3 倍の線に振れ 2 割なら、**1.28 と 1.3 は区別できません**） |
| 3 | ★**測定の条件（どのコアで・何回・最小値か平均か）を報告の必須項目にする** |

★**この教訓は `tools/bench-odds-cost.mjs` の冒頭にも埋め込まれています** — ★**裁定書は読まれないことがありますが、道具は使うときに必ず開かれます。**

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
