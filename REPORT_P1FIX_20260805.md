# REPORT_P1FIX — 較正層の防御と §8.8 の完成（2026-08-05）

**指示書**: `DEV_INSTRUCTIONS_P1FIX_20260805.md`（O-1〜O-7）
**正典**: D-014〜D-019 の改訂を反映して作業しました

> **今回は指示書の閉じに従い、「作ったもの」より先に「作ったものが効いているか」を確認する順序で進めました。**
> 最初に変異試験ハーネスを作り、レビュー側の指摘（5件すべて未防御）を**自分の側で再現**してから着手しています。

---

## 1. O-1〜O-7 の対応

| # | 内容 | 状態 |
|---|---|---|
| **O-1** | §8.8「介入ログ → 同じ着順」を完成 | 完了・変異3件で防御を実証 |
| **O-2** | V-6 の測定を正し、V-4 と両立するか確認 | 完了・**両立した**（正典の K 変更は不要） |
| **O-3** | `resolveRace` の入口で `interventionMult` をクランプ | 完了・変異1件で防御を実証 |
| **O-4** | K-4 の是正と較正定数をテストで固定 | 完了・変異5件すべて防御 |
| **O-5** | 選抜指標に `prize/starts` を加えて再選定 | 完了・**`prizePerStart` へ変更** |
| **O-6** | `INTERPRETATIONS` の網羅範囲を広げる | 完了・12件 → **23件** |
| **O-7** | クランプの非対称と NaN・`--json` | 完了・変異2件で防御を実証 |

### O-1 §8.8 の完成

`packages/race-engine/src/replay.ts` を新設しました。

- `InterventionInput[]`（記録用）→ `InterventionPlan`（計算用）の変換（`planFromInputs`）
- `seed + 介入ログ → 着順` の再計算（`replayRace`）
- ハッシュ連鎖の検証と着順の再計算を1つの経路で行う `auditRaceFailures`

設計上の判断を3つ書きます:

1. **判定はサーバー受信時刻のみを使う**（§8b.6）。クライアント申告時刻を使うと、
   時刻を詐称して有利な判定を得られてしまいます。変異試験 `O1-3` で固定しました。
2. **ログの並び順に依存しない**。種別ごとに「最も早い1件」を採り、連打だけ件数を数えます。
   順序依存にすると DB から取り出す順が変わっただけで再現が壊れます。
3. **`replayRace` は `resolveRace` を呼ぶ**。別実装で「同じはず」の計算をすると、
   本番と再現がずれても気づけません（それでは監査資料になりません）。

### O-3 憲法 ±10% キャップを本番経路で守る

```ts
const interventionMult = clampInterventionMult(rawInterventionMult, balance);
if (interventionMult !== rawInterventionMult) capViolations.push({ horseId, received, applied });
```

- 非有限値（NaN / Infinity）は **1 に倒します**。NaN を素通しすると `finalScore` が NaN になり、
  ソート比較がすべて false になって**着順が入力順のまま**という静かな破綻を生みます（R-3）
- クランプは**黙って行わず `capViolations` に記録**します。範囲外の入力は不正の兆候かもしれないためです（§8b.7/§8b.8）
- `RaceBalance` にも `INTERVENTION_CAP` を持たせ、**スコア確定点が介入モジュールに依存せず自力でクランプ**できるようにしました。両者が食い違わないことはテストで固定しています

### O-6 解釈の登録範囲

走査範囲を `packages/race-engine/src` のみ → **`packages/race-engine/src` + `packages/sim-engine/src` + `apps/cli/src`** に広げました。
指摘の4件（`I-ENTROPY-SORT` / `I-SEED-FOLD` / `I-SCORE-FLOOR` / `I-MARGIN-BASIS`）に加え、
今回の実装で生じた2件（`I-REPLAY-POSITION` / `I-DRIVE-WINDOW`）も登録し、**12件 → 23件**になりました。

> **「全件」の範囲**（R-9 の一般形）: この23件は上記3ディレクトリの `*.ts` を対象にした全件です。
> `tools/` とテストコードは対象外です。範囲外に解釈が残っている可能性は排除できていません。

---

## 2. 再実行コマンド

```bash
npm install && npm run typecheck && npm test     # 237 passed / exit 0

# ★O-4 の中核: 壊すとテストが落ちることの実証
npm run mutation                                  # 12件すべて「守られている」

# K-5 モンテカルロ（10万レース・4シード）
npm run verify:race -- --races 100000 --seeds 42,7,2026,31337

# P0 ゲート（実レース選抜下 / 既定 proxy）
npm run verify -- --selection race
npm run verify

# O-2 の根拠: 試行回数を振って V-6 が動かないことを見る
for t in 30 60 150 300 600 1200; do npm run verify:race -- --races 3000 --seeds 42 \
  --pool-generations 20 --pool-mares 400 --popularity-trials $t; done

# O-5 の根拠: 指標が「強さ」と「在籍年数」のどちらを測っているか
npx tsx apps/cli/src/metric-correlation.ts
npx tsx apps/cli/src/selection-compare.ts --generations 60 --population 400 --seeds 42,7

npm run inventory
npx tsx apps/cli/src/print-interpretations.ts     # 23件
```

---

## 3. O-2 の結論

### 3-1. 試行回数が判定に効かなくなったこと（R-12）

**原因はタイブレークでした。** 下位の馬は試行を増やしても大半が0勝で並びます。
これを**入力順（＝枠順）**で割っていたため、「最低人気」が実際には
**「0勝で並んだ中で最も外枠の馬」**になっていました。

タイブレークを**平均着順**（同じモンテカルロから得られる）に変更しました。

| 試行回数 | 変更前 V-6 | 変更後 V-6 |
|---|---|---|
| 30 | — | 0.23% |
| 60 | **1.00%** | 0.33% |
| 150 | 0.50% | 0.23% |
| 300 | 0.47% | 0.30% |
| 600 | **0.33%** | 0.23% |
| 1200 | **0.30%** | 0.27% |

変更前は**単調に下がり続け**（＝収束していない＝PASS が推定ノイズの産物）、
変更後は 30〜1200試行で **0.23〜0.33% と傾向なく安定**します。
既定を **200試行**（安定域の中央付近）にしました。

順位付けは `apps/cli/src/popularity.ts` に切り出し、経路テスト8本を掛けています
（`verify-race.ts` に埋め込んだままではテストが1本も掛かりませんでした）。

### 3-2. ★V-4 と V-6 は両立しました（正典の K 変更は不要）

**正しい測定のもとでは、素の状態では両立しませんでした**（V-6 0.27%）。
指示書の助言どおり **K を上げるのではなく出走表の能力幅を締める**方向で対処しました。

| 対処 | 根拠 |
|---|---|
| **距離が向かないレースには出走しない**（`DISTANCE_SUIT_MIN=55` / 例外率 12%） | §10.4「自馬の出走レースは自分で時刻を選んでエントリー」＝オーナーは自馬に向いたレースを選ぶ |
| **1レース内の能力レンジに下限**（`FIELD_STRENGTH_FLOOR = 0.78`） | 指示書の助言「1レース内の能力レンジに上限を設ける」。実際の番組では「勝ち目のない馬」は出走してこない |

**10万レース・4シードの実測（K は正典 §13.1 の 0.26 のまま）**:

| # | 目標 | 実測 | 判定 |
|---|---|---|---|
| V-4 | 30〜34% | **33.30%** | PASS |
| V-5 | 60〜65% | **63.19%** | PASS |
| V-6 | 0.5〜2% | **0.87%** | PASS |
| V-8 | 93〜97% | **94.79%** | PASS |
| V-9a | 全サンプル 0.90〜1.10 | **0.90〜1.10** | PASS |
| V-9b（監視・目標値なし） | — | 介入馬の平均 **1.0427** | 報告 |
| V-12a / V-12b | ≤0.10 / ≥−8% | **0.0184 / −1.02%** | PASS |

感度データとして K=0.28 でも測りました（V-4 31.99% / V-5 61.33% / V-6 1.03%）。
**0.26 でも 0.28 でも通るので、正典の定数を動かす必要はありません。**

### 3-3. ★測定の是正が V-4 自体も動かしていたこと

見落としていたら誤った較正になっていた点なので明記します。
**タイブレークの是正は V-6 だけでなく V-4 も真の値へ押し上げました**（32.5% → 34.2%）。
推定器が粗いと「1番人気」が真の最強馬でないことがあり、その分 V-4 が**過小**に出ていたためです。

つまり **D-016 で正典化された K=0.26 は、偏った推定器の下で求めた値**でした。
結果的に正しい測定でも 0.26 で通りましたが、**通ったのは偶然ではなく能力幅を締めた効果**です。
K だけを据え置いて測定だけ直すと V-4 は 34.1〜34.5% で上限に張り付きます（16000レース・2シードの実測）。

---

## 4. O-4 の変異試験結果

`npm run mutation`（`tools/mutation/run.mjs`）。**改変を入れるとテストが落ちること**を機械的に実証します。

| # | 改変 | 対応前 | 対応後 |
|---|---|---|---|
| O4-1 | クラス係数を適用経路から外す | ★全緑 | **落ちた** |
| O4-2 | 開放率の幅 0.55–0.85 → 0.30–0.95 | ★全緑 | **落ちた** |
| O4-3 | クラス分けを撤廃（幅 6% → 100%） | ★全緑 | **落ちた** |
| O4-4 | K を 0.26 → 0.12 | ★全緑 | **落ちた** |
| O4-5 | 母集団を能力順に並べない | ★全緑 | **落ちた** |
| O3-1 | `resolveRace` の入口クランプを外す | （未実装） | **落ちた** |
| O1-1 | 介入ログ→プラン変換を無視 | （未実装） | **落ちた** |
| O1-2 | 監査経路が着順を再計算しない | （未実装） | **落ちた** |
| O1-3 | 判定にクライアント申告時刻を使う | （未実装） | **落ちた** |
| O2-1 | 人気のタイブレークを枠順に戻す | （未実装） | **落ちた** |
| O7-1 | 疲労係数のクランプを外す | ★全緑 | **落ちた** |
| O7-2 | `clamp` が NaN を素通しする | ★全緑 | **落ちた** |

**12件すべて防御。** 「対応前」の列は私が自分で再現した結果で、レビュー側の指摘と一致しました。

### ★ハーネス自身の事後条件（R-11）

前便で「空振りしたスクリプトが『改変した』と出力し、誤った結論を出した」ので、
本ツールは次を全部 assert し、1つでも破れたら非ゼロ終了します:

1. アンカーがちょうど1件
2. 置換後に文字列が実際に変化した
3. 書き込み後、**ディスク上の内容**が原本と異なる
4. 復元後に md5 が原本と一致
5. **改変なしの状態でテストが全緑**（後述）

---

## 5. O-5 の指標比較と再選定

### 5-1. 「強さ」と「在籍年数」のどちらを測っているか（自分で実測）

`npx tsx apps/cli/src/metric-correlation.ts` — 種牡馬プールと同じく**在籍年数を散らして**測定:

| 指標 | r(指標, 能力合計) | r(指標, 在籍年数) | |
|---|---|---|---|
| `prize`（累計） | 0.529 | **0.365** | ← 在籍年数を測っている |
| **`prizePerStart`** | **0.639** | **−0.007** | ← 強さだけを測る |
| `winRate` | **−0.003** | −0.037 | ← 能力とほぼ無相関 |
| `composite` | 0.432 | 0.246 | ← 在籍年数を測っている |

監査の指摘どおりでした。**`prizePerStart` は能力との相関が最も高く（0.639）、在籍年数とは無相関（−0.007）**です。

`winRate` が能力と**ほぼ無相関（−0.003）**なのは新しい発見です。クラス分けを入れた結果、
1レース内の能力差が小さく、勝つかどうかがほぼ運になるためです。
P1 で `winRate` が V-1 を割ったのはこれが理由でした。

### 5-2. 選抜結果への影響（60ゲーム内年・繁殖牝馬400頭・シード42,7）

| 指標 | V-1（12〜18%） | surface.turf | surface.dirt | distance_range |
|---|---|---|---|---|
| proxy（P0 の近似） | 15.54% | −1.90% | +0.40% | +2.05% |
| `prize` | 15.78% | +4.75% | +4.24% | +3.09% |
| **`prizePerStart`** | **14.95%** | +5.24% | +5.89% | +4.72% |
| `winRate` | 17.16% | +6.22% | **+10.44% ✗** | +2.80% |
| `composite` | 15.37% | +7.84% | +5.17% | +2.47% |

**`prizePerStart` を既定にしました。** V-1 が最も低い（＝選抜が最も強く効いている）ことも整合します。

---

## 6. 仕様からの逸脱・解釈（全件・**範囲を明記**）

**23件**。`npx tsx apps/cli/src/print-interpretations.ts --md` で再生成できます。

**「全件」の範囲**: `packages/race-engine/src` / `packages/sim-engine/src` / `apps/cli/src` の `*.ts`。
`tools/` とテストコードは走査していません。

今回の追加分（P1 提出時の12件に対し +11件）:

| ID | 正典 | 埋めたこと |
|---|---|---|
| `I-ENTROPY-SORT` | §8.6 | 出走馬IDをソートしてから結合（順序非依存の集合ハッシュ） |
| `I-SEED-FOLD` | §8.6 | 256bit を 32bit ごとに XOR で畳む |
| `I-SCORE-FLOOR` | §8.7 | `Math.max(0, …)` で下限0 |
| `I-MARGIN-BASIS` | §8.7 | 着差ラベルは前の馬との差で出す |
| `I-REPLAY-POSITION` | §8b.4/§8.8 | 道中のポジションはログから復元できないため中団固定 |
| `I-DRIVE-WINDOW` | §8b.3/§8.8 | 連打の集計区間を24秒（直線400m ÷ 平均速度） |

さらに、**今回のプレースホルダ**（正典に無い自前の値。すべて較正対象）:

| 値 | 現在値 | 導入理由 |
|---|---|---|
| `DISTANCE_SUIT_MIN` / `OFF_DISTANCE_ENTRY_RATE` | 55 / 12% | O-2。距離が向かないレースに出さない |
| `FIELD_STRENGTH_FLOOR` | 0.78 | O-2。1レース内の能力レンジの下限 |
| `POPULARITY_TRIALS` | 200 | O-2。安定域の中央（判定に効かないことを実測済み） |
| `DRIVE_WINDOW_SEC` | 24 | O-1。連打の集計区間 |

---

## 7. 未解決の質問

### Q7【要判断】D-015（`heavy_aptitude` の genotype 追加）は未実装です

正典 §5.2/§5.4 が改訂され `condition_aptitude` → `heavy_aptitude`（genotype に追加）になりましたが、
**O-1〜O-7 のスコープ外**と理解し、実装していません。現状は `conditionAptitude`（3値・中立50固定）のままです。

実装すると数値形質が 11 → 12 に増え、§6.4 の変異パラメータ表・V-2d/V-2f の対象・P0 の全ゲートに波及します。
**次便のスコープに入れてよいでしょうか。**

### Q8【要判断】D-017（スタミナ消費のレース時間比例化）も未実装です

`毎秒消費 = (基準消費量 / レース秒数) × …` に改訂されましたが、**`基準消費量` の値が正典にありません**。
現状は P1 の暫定解釈（`I-STAMINA-WINDOW`: 勝負所以降のみ消費）のままです。
どちらも「レース長に依存せず総消費が一定」という同じ効果を持ちますが、式は違います。

`基準消費量` の値を指定いただくか、こちらで較正して提案するかを指示してください。

### Q9【報告】D-019（V-2d/V-2f の分割）を `verify.ts` に反映していません

現在の `verify.ts` は**改訂前の厳しいゲート**（非能力形質すべてに ±10%）を適用しています。
D-019 では丈夫さのみ ±10%、他は V-2f（平坦化）になりました。
**現状は正典より厳しい基準で測って PASS している**ので結果を過大に報告してはいませんが、
ラベルは古いままです。次便で V-2f を実装してよいでしょうか。

### Q10【報告】V-9b の監視値

`interventionMult`（介入した馬のみ）の平均は **1.0427** です。D-014 で目標値は置かないことになったので、
これは報告値です。時系列監視の初期値としてお使いください。

---

## 8. 今回の作業で私が踏んだ失敗

| # | 失敗 | 検出方法 |
|---|---|---|
| 1 | **変異試験の結果を一度、無効な状態で読んだ。** ベースラインが既に赤（K を 0.26 にしたのに古いテストが 0.12 を固定したまま）で、「落ちた＝守られている」が成立していなかった | 全体テストを別途走らせて気づいた。ハーネスに**ベースライン全緑の事前検査**を追加（R-11 の拡張） |
| 2 | `python3 - <<PY` と `\|\|` を組み合わせた結果、**置換スクリプトが実行されないまま次へ進んだ**（python3 が成功して短絡した） | 直後に `rg` で反映を確認して気づいた。以後、置換の後は必ず反映確認を挟んでいる |
| 3 | 棚卸しプローブの挿入で、O-1 用の変異アンカーが実コードと食い違っていた | ツールの事後条件(1)が検出して停止した（R-11 が機能した例） |
| 4 | `neutralPlan` に `...(ctx.distance > 0 ? {} : {})` という無意味な式を残した（`ctx` を使っているように見せていた） | 自己点検。引数名を `_ctx` にして「使っていない」ことを明示 |

**1 が今回いちばん危なかった**ものです。変異試験は「壊すと落ちる」ことを示す道具ですが、
**最初から落ちていれば何も示していません**。R-11 の「検証ツールにも事後条件を」は、
出力の正しさだけでなく**前提条件**にも要る、というのが今回の学びです。

---

## 9. いま残っている弱点

1. **`FIELD_STRENGTH_FLOOR = 0.78` は自前のプレースホルダ**です。V-4/V-6 の両立はこの値に依存します。
   正典 §10.3（番組表）が確定したら、そちらの条件分けに置き換わるべきものです（→ 正典への追記依頼）
2. **V-6 は 0.87% で下限 0.5% に近い**です。P3 で育成が入って能力幅が変われば割る可能性があります
3. **`I-REPLAY-POSITION`**: 介入ログにポジション指示の**内容**が含まれていないため、
   道中の指示は再現できません。§8b.4 のログ形式の拡張が要ります
4. **`G_GATE`（ゲート巧者）は効果0**のまま（出遅れモデルが P1 に無い）
