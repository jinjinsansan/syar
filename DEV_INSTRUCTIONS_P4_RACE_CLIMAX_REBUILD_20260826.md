# 指示書 — P4 レース終盤の再構築（第4コーナー→5頭の攻防→勝馬クローズアップ）

発行: 2026-08-26 / オーナー要求を受けたレビュー側 → 開発側 Claude Code  
対象ブランチ: `p4/race-30sec-cuts`  
確認時HEAD: `8c7203d`  
参考映像: `ダービースタリオン/signal-2026-08-25-01-36-50-55.mp4`  
過去の寄りの基準: tag `p4-2d-complete-20260823`

---

## 0. オーナーの要求（解釈を変えないこと）

今回の要求は、次の3項目を1つのクライマックスとして成立させることです。

1. **第4コーナーで馬が滑らかに向きを変え、直線へ出てくる**
2. **最後の直線で上位4〜5頭が並び、進出・差し・差し返し・脱落が読める攻防を見せる**
3. **決着後に勝馬を大きく寄って映す**

★要求は「4〜5頭をカメラに収める」ではありません。  
★要求は「**4〜5頭が競馬の主役群となり、前後関係が変化する攻防を見せる**」です。

---

## 1. 以前の前提を撤回する（★最重要）

以前の指示・報告にある次の前提は、**今回のオーナー判断により撤回**します。

> 「在る競り合いしか映さない」  
> 「カラー・時計上の着差をそのまま忠実に表示する」  
> 「実際に5頭が密集していなければ、5頭の攻防は作らない」

今回は、**レース判定と確定結果に触れない「表示専用の直線演出」を許可**します。

- 着順、勝率、払戻、ポイント、DBに保存する結果は変えない
- 確定タイムと着差ラベルは変えない
- 表示中の最後の直線に限り、上位5頭の**見た目の前後位置**に決定論的な演出オフセットを加えてよい
- 残り40〜60mまでに演出オフセットを0へ戻し、**確定した着順と着差でゴール**させる
- `Date.now()` / `Math.random()` は使わず、seed・馬番・進行率だけから完全再現可能にする

これは結果の改ざんではなく、リプレイの映像文法です。エンジンの確定経路に入れてはいけません。

---

## 2. 着手前の必須作業ツリー確認

作業ツリーには、すでに未コミットの実験が残っています。

- `broadcastV2TurnFacing` / `broadcastV2TurnSqueezeX`
- 第4コーナーの素材切替と横縮小
- `frameContenders` の `withinM: 16, maxFovDeg: 22`
- `/race` デモの `TIME_GAP_SHAPE_GAMMA = 1.3`

これらはオーナー要求に対する最終解ではありません。

必ず最初に:

1. `git status --short` と対象ファイルの `git diff` を記録する
2. 既存変更を自分の作業と混同しない
3. `git reset --hard` / `git checkout --` / 一括削除は使わない
4. 上記実験を外す場合も、対象行だけを明示的に編集する

`apps/web/package.json` のポート3210変更はユーザー既存作業です。触らず、コミットに混ぜないこと。

---

## 3. 第4コーナーの修正

### 3-1. 現在の失敗

現在の「真横素材を横に縮め、正面付近で斜め前素材に切り替える」方式は不合格です。

- 横縮小は回転ではない
- 素材切替の1コマで絵の中身が跳ぶ
- 左右反転も正面付近で発生する
- 結果として「斜めを向いたまま曲がる」「カクカクする」が残る

★数式上の横幅連続だけでPASSにしないこと。  
★「切替前後の幅差が小さい」も、絵の内容が跳ぶのでPASS根拠になりません。

### 3-2. 今回の方針

現在の方向素材だけで真正面まで連続回転を表現するのは不可能です。従って、**編集で不自然な角度を見せない**方針にします。

1. 第4コーナーカットは、馬が現行素材で自然に見える角度で終える
2. 真正面へ向く前に、最後の直線の真横追従カットへハードカットする
3. 切替後は全馬が直線の進行方向と一致して見えること
4. 異なる方向素材間のディゾルブは使わない（二重馬になるため）

新しい方向素材の作成は今回の範囲外です。まず編集で正常化してください。

### 3-3. 合格条件

- 第4コーナーの間に、同一馬が1コマで別角度の絵へ切り替わらない
- 同一馬の左右がカット内で反転しない
- 斜めの馬が横移動のみでカーブを曲がったように見えない
- 直線カットへの変化は1コマのハードカットとして意図的に見える
- seed 42の30fps連番と実時間動画の両方で確認する

---

## 4. 最後の直線の「4〜5頭の攻防」

### 4-1. 禁止する代替案

次の方法だけで完了報告を出してはいけません。

- FOVを広げてカメラを引く
- `withinM` を広げて後方馬まで映す
- 着差係数だけを圧縮する
- 4〜5頭が同時に画面内に入ったことを「せめぎ合い」と数える
- 馬群が小さくなるまで引き、全体を映す

★「画面内の頭数」ではなく、**上位5頭の前後関係が時間とともに変わること**が合格条件です。

### 4-2. 実装する表示文法

上位5頭は**確定着順から決定論的に選び**ます。直線の表示位置だけに次の演出を加えます。

| 残り距離 | 見せること |
|---:|---|
| 320〜260m | 上位5頭を主役群として画面中央へ集める。一列にはしない |
| 260〜180m | 3〜5着予定馬のうち1〜2頭が進出し、4〜5頭が攻防圏に入る |
| 180〜100m | 2頭以上が一度並ぶ。別の1頭が差す、または差し返す |
| 100〜60m | 勝馬と連対馬が抜け始め、3〜5着予定馬は脱落し始める |
| 60〜0m | すべての表示オフセットを滑らかに0へ戻し、確定着順・確定着差でゴール |

必須要件:

- 演出は `packages/render` 内の純粋関数として置く
- 入力は、確定着順、馬番、本来の位置、残り距離、seedのみ
- 出力は表示用の位置、または本来位置へ加えるオフセットのみ
- 元のリプレイモデルとレースエンジンの出力は改変しない
- 表示順位が瞬間移動しないよう、区間の境界は連続関数でつなぐ
- 一時的に並ぶ場面は作ってよいが、残り60m以内で確定着順と矛盾させない
- 上位5頭を同じ縦位置に重ねない。レーン差と微小な前後差を保つ
- 自馬は演出上の優遇をしない。確定着順で上位5頭に入った場合だけ主役群に入る

### 4-3. 攻防と呼べる定量条件

同一seedの30fps連番を用い、上位5頭の表示位置について次を測定してください。

- 残り260〜60mの間に、上位5頭のうち4頭以上が同時に画面内に入る時間が2.0秒以上ある
- 少なくとも2頭で、他馬に対する前後関係の変化がある
- 少なくとも1組で、並ぶまたは一時的な前後入替わりがある
- 演出のフレームが跳ばず、1フレームの移動量が前後フレームから突発的に増えない
- ゴール時の着順と着差は演出なしの結果と一致する

上記をテストで測るだけでなく、最終判定は動画で行います。

### 4-4. カメラの役割

攻防演出を作った後に、その主役群にカメラを合わせます。

- 基準は4〜5頭を画面の中央60〜75%に置く真横追従
- 先頭2頭だけを大きく映す構図にしない
- 後方馬まで全頭入れるほど引かない
- カメラの注視点は先頭1頭ではなく、主役5頭の加重中心とする
- 終盤に主役群が広がっても、画角が1フレームで跳ばないように連続化する

---

## 5. 勝馬クローズアップの復活

tag `p4-2d-complete-20260823` の `winner-follow` には、勝馬だけを寄って映す意図が明記されています。現在も定義は残っていますが、実映像上のクライマックスとして機能していません。

必須仕様:

1. 勝馬が決勝線を通過した後、0.0〜0.4秒以内に専用カットへ移る
2. 対象は勝馬1頭。必要な場合のみ2着馬を画面端に残してよい
3. 勝馬の馬体は画面高の35〜45%を占める
4. 寄りを1.5〜2.5秒維持する
5. 直線の攻防から勝馬の寄りへはハードカットにする
6. 勝馬の足元、頭、騎手を画面外に切らない
7. 勝利ポーズ用素材がガクガクする場合は、走行素材のまま寄る。ポーズより滑らかさを優先する
8. その後に結果表示へ移行する

★「定義が残っている」だけで完了としないこと。  
★ブラウザで実際に発火し、動画に1.5秒以上収録されたことを証拠にしてください。

---

## 6. 実装の境界

### 変更してよい場所

- `packages/render/src/` の台本、カメラ、表示用リプレイ演出
- `packages/render/test/` の専用テスト
- `apps/web/src/app/race/page.tsx` の接続部
- 比較・監査用の `tools/`

### 変更禁止

- `packages/race-engine` の着順判定、スコア、タイム算出
- `DEFAULT_RACE_BALANCE`
- Workerのレース確定・保存経路
- DBマイグレーション
- 払戻・人気・勝率計算
- 新しい馬素材の作成
- レース全体の時間圧縮の再設計
- 起動中のdevサーバーに対する無断build

必要なデータを `race-engine` から受け取るのは構いませんが、エンジンの結果を変更する関数は追加しないこと。

---

## 7. 必須テスト

### 7-1. 結果不変

演出のON/OFFで次が完全一致すること。

- 着順列
- 各馬の確定タイム
- 着差ラベル
- 払戻
- ゴール時の位置順

少なくとも seed 42 / 14 / 332 / 474 で比較すること。

### 7-2. 決定論

- 同一seedの表示用位置は毎回同一
- ブラウザのフレームレートによって攻防の順序が変わらない
- 30fps収録と60fps表示で同じ時刻の位置が一致する

### 7-3. 連続性

- 第4コーナー内の素材切替・左右反転を0回にする
- 主役5頭の画面位置に1フレームの跳びがない
- カラーの注視点とFOVが連続
- 残り60mの演出解除でスナップしない

### 7-4. 通常検証

```powershell
npm test
npm run typecheck
```

buildが必要な場合は、先にdevサーバーを停止してから行うこと。devとbuildを同時に `apps/web/.next` へ実行しないこと。

---

## 8. 必須の比較映像

同一条件で次を出力してください。

### A. 全体版

- seed 42
- 通常 `/race`
- 第4コーナー手前から勝馬クローズアップ終了まで
- 30fps

### B. 直線の新旧並列比較

- 左: 修正前
- 右: 修正後
- 同一seed、同一時刻、同一尺
- 第4コーナーからゴール後2.5秒まで

### C. seed違い

- seed 14 / 332 / 474
- 直線からゴールまで
- 少なくとも3本

★静止画シートだけで合格にしないこと。  
★カクつき、進出、差し返しは時系列でしか判定できないため、動画が必須です。

---

## 9. オーナー判定用の合格条件

開発側は、次の全項目を満たすまで「完了」と書いてはいけません。

- [ ] 第4コーナーで斜めの馬がそのまま滑って曲がらない
- [ ] 第4コーナーのカット内で馬素材が跳ばない
- [ ] 直線で2頭だけでなく、4〜5頭が主役群に見える
- [ ] その4〜5頭の前後関係が途中で変化する
- [ ] 進出、並び、差し、差し返し、脱落のうち少なくとも3種類が映像から読める
- [ ] カメラを引いただけの映像になっていない
- [ ] ゴール時は本来の着順・着差と一致する
- [ ] ゴール後に勝馬が画面高35〜45%の大きさで1.5秒以上映る
- [ ] 上記全項目を通常の開発サーバー `/race` で確認できる

最終の見た目のPASS/FAILはオーナーが判定します。テストのPASSは見た目のPASSを代用しません。

---

## 10. 報告様式

`REPORT_P4_RACE_CLIMAX_REBUILD_20260826.md` を作成し、次を記載してください。

1. 変更ファイルと `file:line`
2. 既存の未コミット実験をどう扱ったか
3. 第4コーナーのカット時刻と、素材切替・反転回数
4. 攻防演出の数式、入力、出力、連続性の保証
5. seed 42 / 14 / 332 / 474 の測定結果
6. 演出ON/OFFで着順・タイム・着差・払戻が一致する証拠
7. 勝馬クローズアップの発火時刻、継続時間、馬高比
8. `npm test` / `npm run typecheck` の結果
9. 出力動画のパス
10. 指示から逸脱した判断があれば、その内容と理由

動画と報告を出した後、オーナーの確認を待ってください。オーナー確認前に「完全解決」「採用確定」としないこと。

