# 設計案 — 定常運転の供給（`POOL-SUPPLY`）

> 2026-09-20・開発側。**案です。実装していません。裁定を待ちます。**
> 🔴 **実装は「本番の作り直しが済んで、⑤ の実測が出た後」**（レビュー側の指示）。
> 理由: いま入れると、作り直しの結果が **B-3 が効いたのか供給が効いたのか分かりません。**

## 0. 何を作るのか

```
毎週 15〜16 頭 が生まれ、★毎週 15〜16 頭 が引退する世界
```

いまは **引退だけが動き、生まれません**。`insert into horses` は**製品コードに 0 件**です。

---

## 🔴 先に — **この設計には、まだ穴が 3 つ あります**

設計を書く前に現物を数えました。**塞がないと動きません。**

| # | 穴 | 実測 |
|---|---|---|
| **G-1** | ~~ニックス表が DB に無い~~ → ✅ **解けました**（下記） | 🔴 **私の誤りでした。空の表でも `breed()` は呼べます** |
| **G-2** | **配合 AI が製品側に無い** | `mateScore` / 厩舎方針 / `SIRE_CHOICE_TOP_K` は **`apps/cli/src/preseed.ts` の中**だけ |
| **G-3** | **年次カウンタを誰も書かない** | `bred_this_year` / `coverings_this_year` は列が在るのに、**SQL が 1 件も書かない**（→ **DB-1** で機械化） |

- **G-3 が効く先**: `canMate` はこの 2 つを見ます。**書かないと 2 年目から判定が壊れます**
  （全繁殖牝馬が「今年まだ配合していない」ままになり、**年に何度でも産めます**）
- **G-2 は「移す」**（コピーしない・**D-052**）。§10.5 の「専用の簡易ロジックを作らない」にも直結します
### ✅ G-1 は解けました — 🔴 **私が現物を読まずに「呼べない」と書いていました**

レビュー側の指摘で `packages/sim-engine/src/nicks.ts:23` を読みました。

```ts
export function getNicksMultiplier(table, sireLine, damSireLine): number {
  if (damSireLine === null) return 1;
  return table.get(nicksKey(sireLine, damSireLine)) ?? 1;   // ★見つからなければ 1
}
```

→ **空の表なら全組み合わせが 1.00。§6.6 の帯（1.00〜1.15）の下端で、正当な値です。**
→ **`breed()` は呼べます。**「無いと実装に入れません」は成り立ちませんでした。

⚠️ **それでも空で始めません**（レビュー側の裁定）。
プリシードは `preseedNicks(SEED, NPC_STABLES)` の表で 50 世代 配合しています。
空で始めると **世界の「過去」にはニックスが在り、「未来」には無い**ことになります。

→ ✅ **プリシードが使った表を、そのまま初期値として DB へ転記します**（案 A と同じ原則・**発明ではなく転記**）。
→ 🔴 **生成に使った種（`SEED = 20260833`）を、表と一緒に記録します**（再現できること）。
→ §6.6 の「運営が編集可能」はそのまま生きます（オーナーが後から編集できます）。

---

## ① 誰が産むか

**資格**（すべて既存の規則。新しい条件を発明しません）

```sql
retirement_role = 'broodmare'
and (現在週 - birth_week) / 52 >= MIN_BREEDING_AGE_YEARS   -- 6 歳（balance.ts）
and foal_count < MARE_LIFETIME_FOALS                       -- 8 頭（balance.ts）
and not bred_this_year                                     -- 🔴 G-3 が要る
```

**その週に産むのは誰か** — 🔴 **B-3 と同じ層化**にします。

```
繁殖週(mare) = floor(52 × rank(mare) / 繁殖牝馬の頭数)    -- rank は id から（決定論）
その週に配合するのは 繁殖週 == 現在週 mod 52 の牝馬
→ 800 頭 ÷ 52 週 = ★毎週 15〜16 頭
```

**なぜ同じ形か**: 初期投入（B-3）と定常運転が**同じ規則で散る**なら、
**作り直した初日の世界と、1 年後の世界が同じ形**になります。較正がそのまま通ります。

**相手（種牡馬）の選び方** — **発明しません。** 既にある
`mateScoreWithInbreeding` ＋ 厩舎方針 ＋ `SIRE_CHOICE_TOP_K` の順送りを**そのまま使います**
（**D-025** / **D-026**: 配合の決定経路が血統を参照していないと系統が 1 本に収束する）。
→ **G-2 の移動が前提**です。

## ② いつ走るか

| 週 | 何が起きるか |
|---|---|
| `w` | 配合 → **`birth_week = w` で `insert`** |
| `w + 78` | 調教開始可（§7.1） |
| `w + 104` | 出走可（§7.1・`raceableFrom`） |
| `w + 260` | 強制引退 → 役割付け（`retirement.ts` が既にやっています） |

⚠️ **妊娠期間を入れていません**（配合の週 ＝ 生まれた週）。
**正典に妊娠期間の記述がありません。** プリシード（`runPreseed`）も
`breed()` の `birthYear = year` で**同じ年に生まれた扱い**です。
→ **既存のエンジンと揃えました。発明していません。** ただし → 照会 **Q-2**

## ③ どこに書くか

```
packages/breeding/src/           ★純粋。★「誰と誰を配合するか」を決めるだけ
  └ 相手選び（G-2 をここへ移す）・繁殖週の割り当て・資格の判定
apps/worker/src/breeding-runner.ts   ★SQL。★★ここが insert into horses を持つ
  └ main.ts から、advanceTrainingWeeks の★後に呼ぶ
```

- 🔴 **`tools/` には置きません**（`POOL-DRAIN` の条件②「`birth_week` を製品コードが書く」）
- 🔴 **Route Handler にも置きません**（CLAUDE.md）
- ★`breed()` は `@star/sim-engine` のものを**そのまま**呼びます（§10.5 の公正性）

## ④ 止まったら気づくか

🔴 **`PROD-NEVER-AGED` と同じ形で fail-closed にします。**

```ts
// ★資格のある繁殖牝馬が居るのに、★その週 1 頭も生まれなかったら
if (eligibleMares > 0 && produced === 0) throw new Error('★供給が止まっています（POOL-SUPPLY）');
```

- ⚠️ **`onAlert` では済ませません。** 3 日・6,066 レースが「警報は読まれない」を証明しました
- ✅ **在庫の下限監視は既にあります**（**D-079 ⑧** / **D-102 ⑤**・`unlock_daily.horses`）。
  いままで**鳴っても やることが無い**状態でした（R-27 の逆）。**これで鳴る意味ができます**
- ✅ **下限の数は書きません** — `pool-size.ts` の `requiredActivePool(平均出走頭数)` から**導きます**

## ⑤ 決定論（憲法 §1-4）

| 何 | どうするか |
|---|---|
| **仔の id** | 🔴 `randomUUID()` を**使いません**。`sha256(sire_id + dam_id + birth_week)` から組みます |
| **`breed()` の種** | 同じ鍵から `deriveRng`。`Math.random()` も `Date.now()` も呼びません |
| **いまの週** | Postgres の `now()` だけ（§14） |
| **二重に産まない** | 🔴 **`unique (dam_id, birth_week)`** を張ります |

🔴 **冪等が要ります。** ワーカーは落ちて再起動します（A-1）。
`advanceTrainingWeeks` が `last_processed_week` で冪等なのと同じで、
**同じ週を二度処理しても 2 頭 生まれてはいけません。**
→ 一意制約が**最後の砦**です（アプリ側の判定だけに頼らない）。

## ⑥ 🔴 釣り合い — **ここが合否です**

> 「経路が在る」ではなく **「入る数と出る数が釣り合っている」** こと。

**算術**（実測値から）

```
入る: 繁殖牝馬 800 頭 × 年 1 頭            = ★800 頭/年 = 15.38 頭/週
出る: 現役 2,400 頭 ÷ 現役 3 年（156 週）  = ★800 頭/年 = 15.38 頭/週
                                              → ★釣り合う
```

**下支えも足ります**

| | 要る | 在る | 余裕 |
|---|---|---|---|
| 種牡馬の種付け能力 | 800/年 | 200 頭 × 20 回 = **4,000** | 5.0 倍 |
| 繁殖牝馬の補充 | 800 ÷ 8（生涯上限）= **100 頭/年** | 引退する牝馬 約 400 頭/年 | 4.0 倍 |

## 🔴 合否は、ここに書きません

> **合否の正は `tools/verify-pool-supply.mjs` です。この文書は数も式も持ちません。**

⚠️ **2026-09-21 に、ここから判定の 4 項目を消しました。** 理由:

- ここに書いた「判定 ③ `birth_week` の種類が **156**」に対し、実装した検査は
  `birth_week % 52` を数えて「52 / 52」と出していました。**数は 4 対 4 で合っていたのに、
  中身が別の量**でした（突き合わせて初めて出ました）。
- **書き写しが 2 つ在れば、いつかずれます**（D-052）。→ **1 つにします。**
- ⚠️ ただし **1 つにできない境界**が在ります（**CK-18**）: SQL ↔ TypeScript、DB ↔ 画面、
  repo ↔ 配備先。**そこは両側の原文を読む検査で縛る**しかありません
  （例: `apps/cli/test/training-order-key.test.ts`）。

**何を見ているか**（言葉だけ。数は検査が持ちます）:

- 週ごとに「出走年齢に達した数」「引退した数」「現役の頭数」を出し、**入と出が釣り合うか**
- **現役が、要る最小を下回らないか**（要る最小は `requiredActivePool` が導きます）
- **生まれた週が散っているか**（B-3 が保たれているか）
- 🔴 **有効系統数**（D-026）

⚠️ **目標の頭数は正典 §10.5 が宣言しています**（現役 2,500 / 種牡馬 200 / 繁殖牝馬 800）。
導出値（`requiredActivePool` / `requiredBroodmares`）は **下限**であって目標ではありません。
🔴 2026-09-21 に私はそこを取り違え、**世界を 800 → 676 に縮める実装**をしていました
（→ `POOL-SIZE-THREE-SOURCES`）。

🔴 **④ を忘れないこと。** 供給を入れるということは
**配合を毎週 回すということ**で、**系統の集中が動きます**（D-025/D-026 の本体）。
「頭数が釣り合った」だけで通すと、**5 年後に系統が 1 本になった世界**ができます。

---

## 照会（★私では決められないもの）

| # | 問い | なぜ |
|---|---|---|
| **Q-1** | ~~ニックス表~~ | ✅ **解けた**（空でも動く／プリシードの表を転記・種も記録） |
| **Q-2** | 妊娠期間を入れるか | ⏸ **オーナーへ。ただし止まりません**。既存エンジンに揃えて進めます |
| **Q-3** | 年の境目 | ✅ `floor(週 / 52)`。🔴 **条件つき**（下記） |
| **Q-4** | プレイヤーの馬も自動配合するか | ✅ **NPC だけ**。🔴 原則（下記） |

### ✅ Q-3 の裁定 — **年を 2 か所に書かない**

`floor(週 / 52)` でよい。**ただし `birth_year` と `bred_this_year` の「年」は、同じ 1 つの関数から引くこと**（**D-052**）。
→ `packages/scheduler/src/game-year.ts` のような 1 か所に置き、**両方がそこを呼ぶ**。
⚠️ 別々に書くと、**片方を直した日にもう片方が黙って古びます**（今日 4 回 見た形）。

### ✅ Q-4 の裁定 — **人の持ち物を、本人の同意なしに自動で配合しない**

遊びの調整ではなく**原則**。→ いまの案（NPC だけ）で正しい。
⚠️ ただし **「プレイヤーが自分で配合する」経路は別に要ります**（§1.1「血をつなぐ」が中核価値）。
→ **`PLAYER-BREEDING` として別項に起票しました。`POOL-SUPPLY` に混ぜません**
（あちらは「世界が尽きないこと」、こちらは「中核価値が遊べること」で、**合否の基準が別**です）。

### ⏸ Q-2 — 後から足すとき何が動くか

- **供給が遅れます**（配合の週 + 妊娠期間 = 生まれた週 → 最初の 1 年だけ穴が空きます）
- **配合に計画性が生まれます**（「この時期に付ければ、この時期に走る」）
- ⚠️ **釣り合いの算術（⑥）は変わりません**（定常状態では遅れは相殺されます）

## していないこと

- **実装していません。** 1 行も書いていません
- **配備・本番/staging への書き込み・作り直し・VPS**、いずれもしていません
- **G-1 / G-2 / G-3 を直していません**（→ 塞ぐ順も裁定を仰ぎます）
- **有効系統数への影響を測っていません**（判定 ④ は「測る」と書いただけ）


---

## 追記 — **DB-1: G-3 の一般形を機械で数えました**（2026-09-20）

`tools/diag-write-never.mjs`（**静的・DB に繋ぎません。CI でも走ります**）

```
horses の 45 列 / リポジトリ 647 ファイル を走査

🔴 読む側は在るのに、書く側が 1 件も無い列 … ★2 本
     bred_this_year / coverings_this_year      ← ★G-3 そのもの

⚠️ 書く側が tools/ にしか無い列 … ★23 本
     genotype / potential / sire_id / sire_line / pedigree_cache …
```

🔴 **23 本は「23 件の欠陥」ではありません。`POOL-SUPPLY` 1 件の姿です。**
数えると、**`horses` の 45 列のうち 23 列が「道具を手で流さないと入らない」**と分かります。
→ これが「製品に馬を作る経路が無い」ということの、いちばん具体的な言い方です。

⚠️ 🔴 **網を作るとき、私は一度 "候補 0 件" と出しました。**
読む側が TS で `bredThisYear` と camelCase で書いており、`bred_this_year` では当たらなかったためです。
**探していたものが、網の外にありました**（**R-21**）。両方の綴りを見るよう直してあります。

⏸ **門にはまだ入れていません** — `horses` 以外の表で精度を測っていないので
（測らずにラチェットを掛けると、門のほうが壊れます・今日 3 回 学びました）。
