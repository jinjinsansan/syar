# 報告 — (b′) の未検証 3 点を測りました／129 種の一覧

作成: 2026-08-29 / ブランチ `p4/race-30sec-cuts`
受けた裁定: `REVIEW_P4_A1_DIRT_CANON_VERDICT_20260829.md` §7（Q-A2-2）・§8（Q-K-2）
前の報告: `REPORT_P4_A1_DIRT_CANON_20260829.md`

> ★`Date.now()` / `Math.random()` を使っていません。★着順・タイム・着差・払戻・位置に触れていません（憲法 3/4）。
> ★**(b′) は「入っているが使われない」状態です**（正典 R-15）。★既定は `false` のまま。
> ★提出物を足したあとに `npm test` を回し直しました（前便の申し送り 6）。

---

## 0. この便の要点

| # | 内容 | 結果 |
|---|---|---|
| 1 | ★**(b′) の測定 1**（不変条件 3 つが 0 のままか） | ★**通過**（★ただし 1 回落ちた・§2） |
| 2 | ★**(b′) の測定 2**（新しい跳びが出ていないか） | ★**通過** |
| 3 | ★**(b′) の測定 3**（全 7 距離・全 w で 0 になるか） | ★**通過**（120 通りで 15.7px → **0.0px**） |
| 4 | ★**測定 1 が、私の実装のバグを捕まえました** | ★自己申告（§2） |
| 5 | ★**129 種の一覧**（Q-K-2） | ★**別字 0 件**・直したもの無し |

★**裁定 §7 が「①を先に測れ（ここが割れたら他は無意味）」と順序を指定していたことが、そのまま効きました。**

---

## 1. (b′) をどう入れたか（★既定では効きません）

### 1-1. 幾何（`packages/render/src/course.ts`）

```ts
laneArcLengthAt(course, s, w)     // ★s までに走線 w が実際に進む長さ
sAtLaneArcLength(course, len, w)  // ★その逆
```

★比は `posOf` が使う半径 `r = R + off·sgn` から導きます。★**同じ規則を 2 か所で持ちません**（R-30）。
⚠️ ★これは**描画がカメラを置くための量**で、★着順に効く `laneExtraMeters`（D-071・エンジンが引く）とは**別物**です。

### 1-2. 接続（`broadcast-v2-scene.ts`）

★**カメラの向け先だけ**を、先頭との差を「走線に沿った長さ」で保った点へ動かします。
★返す `focusS` は変えません（あちらは「何 m 地点を見ているか」という意味の値です）。

### 1-3. ★既定は `false`（R-15）

```ts
export const LANE_ALIGNED_FOCUS_DEFAULT = false;   // broadcast-v2.ts
```

★裁定 Q-A2-2「★**測ってから実装**」に従い、★**既定は切り替えていません。**
★道具はこの定数から引きます（R-31）。★`true`/`false` を道具に直書きしていません。

---

## 2. ⚠️ ★測定 1 が、私の実装のバグを捕まえました

★**最初、測定 1 は割れました。**

```
× ★先頭が画面の外に出るコマが 1 つも無い     → seed 42 で 329 コマ
× ★カットの境目で共通の被写体が 0 頭になる箇所が無い → 4 箇所
✓ ★4 角正面から出る境目で走行方向が反転しない
```

★**(b′) の性質ではなく、私の幾何の実装が壊れていました。**

| | |
|---|---|
| 壊れ方 | `laneArcLengthAt` の末尾が **`out + (s - acc)`**。★走路の終わりから先の延長を足すつもりが、<br>★**`s` が走路の内側で終わったときにも実行され、負の値を足していました** |
| 症状 | 1600m の走路で `s=1500` が ★**1400 相当**（100m 短く）返る |
| 実害 | ★注視点が **−410m** ずれ、★カメラが別の場所を向いた |
| 直し | `out + Math.max(0, s - acc)`（`sAtLaneArcLength` の末尾も同型） |

★**片道だけを見ていたら気づけませんでした。** ★往復（`s → 長さ → s`）を見て初めて 100m のずれが出ます。
★これは R-22 の家族です（★片側だけを見る指標は、抜けているものを見ない）。

### ★守り（`packages/render/test/lane-arc-length.test.ts`・6 件）

| 検査 | |
|---|---|
| ★**往復が一致する** | ★全 7 距離 × 走線 6 本 × 走路の内外 9 点。★**これ 1 本で上の壊れ方は全部落ちます** |
| 中心線では長さ＝`s` | 曲がっても伸び縮みしない |
| 内は短く外は長い | コーナーを含む区間で |
| 直線では走線によらず同じ | |
| ★`posOf` を刻んで足した長さと一致 | ★**実装と `posOf` が離れていないこと**（R-30） |
| ★検出器が鈍っていないこと | ★**元の壊れ方を再現した式が落ちること**（R-14）。★差がちょうど 100m であることまで固定 |

---

## 3. 測定 1 — 不変条件（★裁定が「先に」と指定した項目）

★`LANE_ALIGNED_FOCUS_DEFAULT` を `true` にして `race-video-invariants.test.ts` を回します。

| 不変条件 | (b′) OFF | ★(b′) ON |
|---|---|---|
| ★先頭が画面の外に出るコマ | 0 | ★**0** |
| ★カットの境目で共通の被写体 0 頭 | 0 箇所 | ★**0 箇所** |
| ★4 角正面から出る境目の反転 | 0 | ★**0** |

★**3 つとも 0 のままです。**

★未検証 #2（`contestFocusWithLeadInFrame` が `s` で書かれている件）は、★**注視点のずれが小さいため成立しています**:

```
★(b′) が注視点をずらす量（seed 42・全区間）
  直線               0.00m
  side-drive        −0.14m / −0.11m
  ★最大            −0.25m（17.3s・fourth-corner-front・遅れ 7.8m）
```

★D-088 が守っている「先頭を枠に残す」上限は**画面の半幅（数十 m）**なので、
★**0.25m の移動では破れません。** ★これが割れなかった理由です。

---

## 4. 測定 2 — 新しい跳びが出ていないか（全境目・4 seed）

| 境界 | OFF 中央 | ★ON 中央 |
|---|---|---|
| side-drive → fourth-corner-front | 937 | **930** |
| fourth-corner-front → side-drive | 767 | **763** |
| first-corner-front → side-drive | 635 | 635 |
| straight-contest → homestretch-front | 473 | 473 |
| side-drive → straight-contest | 288 | **295** |
| homestretch-front → straight-contest | 290 | 290 |
| straight-contest → finish-line | 221 | 221 |
| finish-line → winner-follow | 94 | 94 |
| start-front → first-corner-front | 58 | 58 |
| ★全体（中央 / 最大） | 303 / 994 | ★**303 / 982** |
| ★走行方向が反転した境目 | 4（入口のみ） | ★**4（入口のみ）** |
| ★共通の馬が 0 頭 | 0 箇所 | ★**0 箇所** |

★**新しい跳びは出ていません。** ★2 つの境目でわずかに減り、1 つで +7px。★全体の中央値は不変です。

---

## 5. 測定 3 — 全 7 距離・全 w（★1 箇所で「直った」と言わない）

★折れ目の前後 ±12m で、カメラと馬の**世界での間隔**がどれだけ振れるかを見ます。

```
  ★対象 120 通り（距離 7 × 折れ目 × 走線 6）
  ★現行の最大  0.2221m = 15.7px
  ★(b′) の最大 0.0001m =  0.0px
```

★**120 通りすべてで 0 になります。** 1600m の内訳（抜粋）:

| 距離 | 折れ目 | 走線 | 現行 | ★(b′) |
|---|---|---|---|---|
| 1600m | 600 | 0 | 15.7px | ★**0.0px** |
| 1600m | 600 | 2.2 | 12.2px | ★**0.0px** |
| 1600m | 600 | 5 | 7.9px | ★**0.0px** |
| 1600m | 1200 | 2.2 | 12.2px | ★**0.0px** |

⚠️ ★完全な 0 でないのは**弦で測っているため**です（曲線では弧より僅かに短い）。
★幾何どおりの残差（0.0001m）で、★**折れ目とは無関係に出ます**。

---

## 6. 129 種の一覧（Q-K-2）— `KANJI_SINGLETONS_20260829.md`

★裁定の指定どおり **字・コードポイント・出現ファイル・その 1 行**を並べました。

| | |
|---|---|
| 走査対象 | 追跡中の `.ts`/`.tsx`/`.mjs` **496 ファイル** |
| 異なる漢字 | 1,308 種 |
| ★1 回だけ | ★**129 種** |
| ★**別字** | ★**0 件** |
| 直したもの | ★**無し**（「この語のこの字が変」と言えたものが無いため） |
| 印を付けたもの | 2 件（★**字は正しいが語の選びが珍しい**・直していません） |

★内訳: 騎手名 16 字 ／ その他 113 字。

⚠️ ★**行頭から 110 字で切ると、肝心の字が見えない行が 10 件**ありました。
★**字の周辺**を出す形に直しました（一度そう作って外しています）。

### ★6-1. この一覧の限界（★これが本題かもしれません）

★**「1 回だけの字」は、別字が隠れやすい場所であって、別字が居る場所の全部ではありません。**

⚠️ ★今回見つかった 6 種のうち ★**U+82DD（芝）は 38 箇所**あり、
★**この一覧には一度も出てこなかった**はずです（単発ではないため）。
→ ★**この検査は、今回の実害を一件も見つけられなかったでしょう。**

★正典 R-22 のとおりです。
★**本書の 0 件は「別字が無い」ではなく、「1 回だけの字の中には無かった」以上のことを言いません。**

---

## 7. 検証

```
$ npm test
      Tests  1229 passed (1229)
npm test EXIT=0

$ npm run typecheck
typecheck EXIT=0
```

★**提出物（この報告書・一覧）を追跡に入れてから回しました**（前便の申し送り 6）。
⚠️ ★その過程で **`_lanefocus.mjs` の登録漏れ**を R-24 の番人が捕まえています。★登録しました。

---

## 8. ★独立に再実行して突き合わせてください

| # | コマンド | ★出るはずの数字 |
|---|---|---|
| 1 | `npx tsx tools/_lanefocus.mjs` | ★往復のずれ **なし**／注視点のずれ 最大 **−0.25m**（直線は 0.00） |
| 2 | `npx tsx tools/_seamslip.mjs` | ★【4】対象 **120 通り**／現行の最大 **15.7px**／★**(b′) 0.0px** |
| 3 | `npx vitest run packages/render/test/lane-arc-length.test.ts` | ★**6 件 PASS** |
| 4 | `npm test` | ★**1229 件 PASS・EXIT=0** |

### ★(b′) を ON にして測り直す手順

```
# packages/render/src/broadcast-v2.ts
- export const LANE_ALIGNED_FOCUS_DEFAULT = false;
+ export const LANE_ALIGNED_FOCUS_DEFAULT = true;

npx vitest run apps/cli/test/race-video-invariants.test.ts      # ★3 件とも PASS
npx tsx tools/audit-cut-seam.mjs --seeds 42,253,90,2 --script v6  # ★反転 4（入口のみ）/ 共通 0 頭 0 箇所
```

★**戻すのを忘れないこと。** ★既定は `false` です。

---

## 9. ★判定していただきたいこと

| # | 論点 | こちらの立場 |
|---|---|---|
| ① | ★**(b′) の既定を `true` にしてよいか** | ★測定 3 つとも通りました。★**次便で切り替えます**（この便では切り替えていません） |
| ② | ★測定 1 が捕まえた実装バグの扱い | ★直し済み・検査で固定済み。★**裁定の順序指定が効いた**と記録しました |
| ③ | ★129 種の一覧（別字 0 件） | ★**「1 回だけの字の中には無かった」以上のことを言いません**（§6-1） |
| ④ | ★恒久の別字対策（Q-K-3） | ★単独の便で、という裁定どおり**着手していません** |

---

## ★10. 申し送り

1. ★**測る順序の指定に、実際に助けられました。**（§2）
   ★裁定が「①の不変条件を**先に**測れ（ここが割れたら他は無意味）」としていなければ、
   ★**−410m ずれたまま「跳びは 0 になりました」と報告していた**可能性があります
2. ★**往復を見る。片道は「それらしい数字」を返す。**（§2）
   ★`laneArcLengthAt` は片道では正常に見えました。★往復で 100m のずれが出ました
3. ★**新しい機構は「入っているが使われない」で一度出す。**（R-15・§1-3）
   ★既定を切らずに測れるので、★**対照が取れます**
4. ★**「1 回だけ」を数えても、38 箇所あるものは見つかりません。**（§6-1）
   ★数え方が、探しているものの形と合っているかを先に問うこと（R-22）
5. ★**スクリプトはファイルに書いてから実行する。**
   ★この便でも、日本語混じりのヒアドキュメントを 1 回壊しました（前便の R-26 と同じ家族）
