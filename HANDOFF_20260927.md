# 引継ぎ書 2026-09-27（開発側 `star-aa` → 次のセッション）

> ★**最初にこの 3 つだけ読めば動けます**: §1（いまどこ）・§2（次の一手）・§7（私が今日 5 回 踏んだ罠）

- 前の HEAD: **`67b759a`**（セッション開始時・レビュー側の裁定コミット）
- 今の HEAD: **`d1ecfc9`**
- 差分（実測）: **142 ファイル / +11,300 / −991 行**（★`git diff --stat 67b759a..HEAD`）
- 私のコミット: **26 本**（★裁定書を除く。★レビュー側の裁定書も同じ枝に乗っています）

---

## 1. いまどこ

### 検証（★全部 通っています）

```
npm test        3,118 件 / 赤 3 件 … ★赤は全部 KNOWN_RED に登録済み
                node tools/verify-known-red.mjs → ✅ 合格
npm run typecheck   exit 0（root / apps/web / tools の 3 つ）
npm run build:web   exit 0（追跡ファイルの汚れ 0）
```

### 🔴 本番と手元が 1 本ずれています

| | 版 |
|---|---|
| 本番 DB | **`0089`**（89/89・未適用 0） |
| 本番の画面（Vercel） | **`b583ce2`** |
| 本番のワーカー（VPS） | **`b583ce2`**（active・失敗 0・周の重さ 0.3〜0.7%） |
| 手元 HEAD | **`d1ecfc9`** ← ★**未 push 1 本**（★段 2 B） |

→ ★`d1ecfc9` は ★**振る舞いを変えない変更**（★`built.*` への置き換え）なので、★急いで出す必要はありません。
★出すなら ★`git push origin HEAD:refs/heads/main` と ★`bash tools/deploy.sh d1ecfc9`。

### ⚠️ 未コミットは ★**オーナーへ回る分だけ**（★触らないこと）

```
?? apps/web/public/art/**  … 51 件（★画像生成の産物）
 D apps/web/public/art/uma/title-eye.{png,webp}
 M design/art/prompts/train-body-idle.txt
?? design/art/prompts/deformed-8frames-high-diag.txt
```
★私は 1 つも触っていません。★レビュー側がオーナーに回しています。

---

## 2. 🔴 次の一手（★この順で）

### ① 段 2 C — ★残り 4 行（★小さい・すぐ終わる）

★`apps/web/src/app/race/page.tsx` の、★`render` の null 判定（`:3811`）より ★**外側**に残っている分:

| 何 | 件数 | どこ | どう直すか |
|---|---|---|---|
| ★`DIST` | 1 | ★`:6066` 付近の JSX（★`built` が null でも描く） | ★引数で渡すか、★その JSX を null 判定の内側へ |
| ★`RACE_TURN` | 3 | ★判定より前 | ★同上 |

⚠️ ★**網が守っています**: ★`apps/cli/test/built-course-fields.test.ts` が
★「★内側は `built.*` を読む（30 件超）／★**外側は 0 件**」を見ています。
★C を入れたら ★**この網も書き換える**こと（★消さない。★B のときもそうしました）。

### ② 9 色が画面に出たことの確認（★段 2 の (3)）

★焼きは 2026-09-24 に ★9 色 91 枚で済んでいます（★`apps/web/public/art/baked/`）。
🔴 ★`/race` は まだ ★**枠番から毛色を引いています**（★`COAT_BY_GATE`・★月毛と白毛を含まない）。
★これを ★`coatOfHorseId(horse_id)` に替えるのが ★**段 2 の本体**で、★`0089` で ★`horse_id` は公開済みです。
⚠️ ★網 `apps/cli/test/coat-single-source.test.ts` の ★`COAT_LIST_EXEMPT` に
★**消す条件つき**で `COAT_BY_GATE` が登録してあります。★替えたら ★その行も消すこと。

### ③ 段 2 D — ★実レースを繋ぐ（★ここが本番）

★継ぎ目は ★**1 か所**です（★現物を読んで確定済み）:
```
/race の build() … resolveRace → replayOf → finalOrderMatches → replayPositionModel
★replayOf 以降は ★result しか見ていない（horseId を★馬番として読む・D-056）
→ ★やることは ★result をサーバーの行から作るだけ:
  { order: [{ horseId: String(gate), finishPosition: finish_pos, timeSec: finish_time }, …] }
```
✅ ★**`finalOrderMatches` が そのまま D-059 の番人**になります（★映像の着順 ≠ 確定着順 なら投げる）。
🔴 ★**裁定の追加条件**: ★実レースで それが鳴いたら ★**捕まえて「この録画は出せません」と止め、
★見本に落とさず、★簿に 1 行 残す**（★黙って飲み込まない）。

★読む層は ★**もう在ります**: ★`apps/web/src/lib/race-real.ts`（★確定済みだけ・`settled` 以外は投げる）。
★走路は ★`raceSetupFor()`（★`packages/scheduler/src/race-setup.ts`・★場と距離から組む）。

### ④ 🔴 そちら（レビュー側）待ちで止まっているもの

- ★**D-053 の測り直し** … ★開放率の基準は ★**staging の 2026-08-12 スナップショット**
  （★`docs/pool-staging.json`）で、★本番とは別の集団。★詳細は §5。
- ★**R-17**（★市場の見た目）・★**R-16**（★旧世代 15 枚）… ★デザイナー待ち
- ★**EP の収支** … ★オーナーが ★「もっと後回し」と明言（2026-09-26）

---

## 3. 今日 済んだこと（★大きい順）

### ① 本番配備（★DB・画面・ワーカーの 3 つ）

★移行 ★**11 本**（`0079`〜`0089`）を本番へ。★画面と ★ワーカーも入れ替え。
✔ ★本番で読み取り確認: ★`claim_daily_ep` 等 ★7/7 在る ／ ★額は正典と一致（2,000 / 200 / 10,000）／
★騎手名簿 6 人 ／ ★**古い `enter_race(jsonb)` は落ちている**（★憲法 3 の穴が本番から消えた）。

🔴 ★**配備の前に `0082` の破壊的変更に気づいて一度 止めました。**
★本番の画面が ★古い署名（`p_jockey_frozen`）を呼んでいたためです。
★経緯と噛み合わせ（★`0082`↔`/entry`・`0089`↔常設帯）は ★`DECISION_PROD_SHIP_20260926.md`。

⚠️ ★**VPS には入れます**（★私は一度「入れない」と誤報しました）:
```
ssh -i ~/.ssh/pax_vps root@162.43.29.102
bash tools/deploy.sh <sha>        # ★git push では入れ替わらない
journalctl -u star-worker -n 20 --no-pager -o cat
```
★配備後に見る 5 点は ★`RUNBOOK_PROD_DEPLOY_20260922.md` ⑥。

### ② デイリー EP を本番で受け取れるようにした（★公開を止めていた欠陥）

★D-075 は 2026-08-20 に「デイリー 200 EP」と決めていたのに ★**渡す実装が 1 つも無く**、
★1 か月以上 誰も気づかず、★オーナーの口座は残高 0 でした。★`0080` で塞ぎ、★本番に出ています。

### ③ ループ 8 段が全部 繋がった

★現物で測りました（★簿を読まず・★8 段 × 3 点）:
```
配合 ✅ 命名 ✅ 調教の指示 ✅ 出走登録 ✅ 馬券 ✅ 引退の役割 ✅ デイリー EP ✅
馬の売買 … ★2026-09-26 に繋いだ（★最後の 1 つ）
```
★`/stable/market` を ★実データへ（★`market-screen.ts` → `horse_market_listing_public` / `buy_horse`）。

### ④ 未ログインの嘘表示を 4 枚 直した

★`/entry`・`/records`・`/stable`・`/training`。★前 2 つは ★**DB の生の文が画面に出ていた**、
★後 2 つは ★**見本の馬を「あなたの厩舎」として見せていた**。
★網 ★`apps/cli/test/owner-scoped-needs-session.test.ts` が ★これを見ています。

### ⑤ 網を 8 本 新設・4 本 修理

★新設: ★`owner-scoped-needs-session` / `coat-single-source` / `url-param-one-meaning` /
★`replay-gauge` / `built-course-fields` / `race-setup-for` / `canon-amounts-implemented`（拡張）/
★`race-replay-strip`（`horse_id` 必須）
★修理（★綴りの釘付け → 要求の水準）: ★`jockey-market-wiring` / `first-pass-time-of-day` ⑧ /
★`screen-generations` / `screen-rebuild-pins`

---

## 4. 🔴 簿（`tools/lib/open-findings.mjs`）に足した項目

| id | 中身 | 消す条件 |
|---|---|---|
| `NEXT-BUILD-REWRITES-TRACKED-FILES` | ★`next build` が追跡ファイル 2 つを書き換える（★2 回目） | ★3 回目が出たら ★部品でなく ★網にする |
| `TOOLS-UNCLASSIFIED-WHILE-UNTRACKED` | 🔴 ★未追跡の道具は ★分類簿の網に映らない | ★未追跡も走査に入れるか作法にするか（★owner: review） |
| `FELLBACK-STOP-CHECK-IS-RACE-ONLY` | ★「止めているか」の検査が ★`/race` 固定 | ★`fellBack` を読む画面が ★2 つ目になった日 |
| `REPLAY-GAUGE-ABSENT-FOR-REAL-RACE` | ★録画でゲージを出さない（★**暫定**） | ★段 3 で ★自分の馬のときだけサーバーが返す口が入った日 |
| `RACE-POSITION-FORMULA-DUPLICATED` | ★位置の式が 2 か所 | ★台本 v9 の作り替え便で 1 部品へ |

⚠️ ★**消した項目が 1 つ**: ★`TRAINING-INSTRUCTION-NOT-READ`。★`0057`〜`0059` で塞がれていたのに
★**6 日 残り**、★私がそれを読んで ★**オーナーに誤報しました**。★詳細は §7 ③。

---

## 5. 🔴 開放率のずれ（★レビュー側の判断待ち・★数字は揃っています）

★ワーカーが ★「開放率が P1 のゲートを測ったときの分布からずれています」と警告します。

### 測った結果（★本番・`unlock_daily` 全 7 行）

```
日付     mean    sd     p10     p50     p90    週齢平均
09-22   70.69  14.60  47.75  73.46  87.44   188.48  ← 山
09-25   68.48  17.78  34.88  73.24  87.32   183.22  🔴 p10 −9.7pt（★段差はここ）
09-26   66.67  19.76  32.14  73.24  87.30   179.00     −2.7pt（継続）
```
→ ★**p50 と p90 は 4 日 不動**。★動いたのは ★下側だけ（★SD 14.6 → 19.8）。
→ ★★**全体が下がったのではなく ★下の裾が伸びた**形（★若い馬が入る → `unlock-flow.ts` の註記どおり）。
→ ★**私の配備（09-26）は原因ではありません**（★段差は前日）。

### 🔴 基準は ★staging の 8/12 スナップショットです

★D-053 の ★**2026-09-20 訂正**（★正典の長い 1 行の ★末尾）:
> ★ここでいう「本番の実分布」は ★**staging の実測分布である。production ではない。**
> ★母集団は ★`docs/pool-staging.json`（★2026-08-12 の書き出し・7.0MB）

→ ★ワーカーは ★**本番の生きた分布 ↔ staging の 1 か月半前のスナップショット**を比べています。
→ ★**同じ土俵ではありません。** ★pt 差を ★そのまま「悪化」と読まないこと。
✔ ★2026-09-26 に ★`UNLOCK_BASELINE` の註記と ★警告文に ★**この出どころを書き足しました**（`b583ce2`）。

### 次にやるなら

★D-053 の原則は ★「★①値を導き直す か ★②その条件でも成立を示す」。
★②の道具は在ります: ★`tools/export-pool.mjs` → ★`npm run verify:race --pool <それ> --real-ability --b6-wired`。
🔴 ★**独断で走らせないこと** — ★較正の合否に触ります（★簿 `star-measure-with-the-canon-tool` /
★`star-heavy-runs-owner-terminal`）。★規模を先に測って、★重ければオーナーの別ターミナルへ。

---

## 6. ★環境の実務（★次のセッションが最初に困る所）

```bash
# ★本番（★読むのも書くのも 私から 通ります。★--env は必ず明示）
node tools/list-unapplied-migrations.mjs --env production
npx tsx tools/migrate.mjs --env production --yes-production
# ⚠️ loadEnv() の既定は★本番。--env を省くと本番に繋ぐ

# ★本番の画面の版
curl -s https://star-two-chi.vercel.app/api/healthz

# ★ワーカー（VPS）
ssh -i ~/.ssh/pax_vps root@162.43.29.102

# ★下書きの道（★分類簿と型検査の対象外）
tools/_なにか.mjs      # ★.gitignore 済み（tools/_*）

# ★重い計算はオーナーの別ターミナル（★verify:payout / verify:pmin）
```

⚠️ ★`npm run build:web` は ★道具経由（`tools/build-web.mjs`）になりました。
★追跡ファイルを ★`finally` で戻します。★素の `next build` は ★`build:web:raw`。

---

## 7. 🔴 私が今日 踏んだ罠（★次のセッションは ここから読むと速い）

### ① ★「無い」「できない」と ★5 回 言って ★5 回 外した

| 言ったこと | 実際 |
|---|---|
| ★「生の `error.message` の網は作れない」 | ★作ったら ★`/records` の ★生きた欠陥が出た |
| ★「`?race=` を張っているリンクは 0 件」 | ★網が ★引継ぎ書の ★**5 本**を捕まえた（★手の grep では 0） |
| ★「調教の指示が繋がっていない」 | ★簿が古く、★現物は ★繋がっていた |
| ★「VPS には入れない」 | ★入れた（★`ssh -i` の形を探していなかった） |
| ★「(b) は成り立たない」→ ★これは ★**当たっていた**（★測って示した） | — |

→ ★★**作法: ★「無い」と書く前に ★探し方を 2 通り試す。** ★網で 1 回・★現物で 1 回。

### ② ★簿を信じて ★現物を見なかった

★`TRAINING-INSTRUCTION-NOT-READ` を読んで ★オーナーに ★「遊びの中心が繋がっていない」と誤報。
★現物（`training-runner.ts:355`）は ★`ordered ?? defaultMenu(...)` で ★**読んでいました**。
→ ★簿の頭に作法を書きました: ★**塞がり方が機械で分かるなら ★`stillOpen` を必ず付ける**（★NT-4）。

### ③ ★直した所の ★古い註記を消さなかった（★同じ日に 2 回）

★`/training` の冒頭と 200 行下で ★**矛盾する註記が同居**していました（★私は上だけ読んだ）。
★`Built.gauge` でも ★置換漏れの古い 1 行が残っていました。
→ ★★**直した便で、直した所の古い註記を消す。** ★註記は ★**現物より長生きします**。

### ④ ★網が ★「緑」でも ★噛んでいないことが ★3 回

| 網 | 何が悪かったか |
|---|---|
| ★見本落ちの網 | ★`catch` から ★**400 字**で見ていた → ★註記を足したら外れた |
| ★`fellBack` の網 | ★「後ろに `fellBack` の語が在るか」→ ★`{ …: fn(x).欄, fellBack: false }` を通した |
| ★口の集め方 | ★`export function` の 1 種だけ → ★`export const … =>` を 1 件も拾わなかった |

→ ★★**距離・語・1 種の構文で書かない。★構文の境目で切る。**
→ ★★**対照は「流した」ではなく「何本ぶん名指しで落ちたか」を数える**（★ループは最初の失敗で止まります）。

### ⑤ ★日本語ソースを Python で機械編集して ★3 種類 壊した（★今日の最後）

★① ヒアドキュメントの ★`\n` が ★実際の改行になり ★文字列が切れた
★② ★`\b` が ★**バックスペース(0x08)** になり ★正規表現が別物に化けた
　→ ★網が 0 件を返し、★**「置換が効いていない」と誤読しかけた**（★網が壊れていた）
★③ ★上の 2 つで ★Edit の一致が ★CRLF で崩れた

→ ★★**日本語ソースの文字列リテラルは ★Edit で書く。★Python / heredoc で書かない。**
→ ★★**網が 0 件を返したら、まず ★網自身の壊れを疑う。**

---

## 8. ★共有ツリーの作法（★守ったこと）

- ★`git stash` を ★使っていません ／ ★`git add -A` / `commit -a` も ★**使っていません**（★全部 パス名指し）
- ★作者は ★**差分の中身**で確かめました（★`git user` は全員 同じなので名前では分かりません）
- ★push は ★`git push origin HEAD:refs/heads/<枝>` の形（★`push origin main` は ★HEAD でなく ★ローカル main を送る）
- ★push のたびに ★**遠隔の sha を読んで**手元と一致を確認（★exit 0 を信用しない）
- ★レビュー側（`claude-code-game-studios-42`）の裁定書も ★同じ枝に乗っています（★私の push で一緒に出ます）
