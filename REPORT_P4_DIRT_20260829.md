# 報告 — ダート演出（地面・板・砂煙）と、裁定の宿題 1〜5

作成: 2026-08-29 / ブランチ `p4/race-30sec-cuts`
起点: `d18c0fd` → HEAD（`5238dbd` 以降）
受けた裁定: `REVIEW_P4_GAMMA_V6_DIRT_VERDICT_20260828.md`

> ★`Date.now()` / `Math.random()` を使っていません。着順・タイム・着差・払戻に触れていません（憲法 3/4）。
> ⚠️ ★`apps/web/package.json`（ポート 3210）はオーナーの既存作業。コミットに混ぜていません。

---

## 0. この便の要点

| # | 内容 | 種別 |
|---|---|---|
| 1 | ★**裁定の宿題 1〜5 を消化**（不変条件の検査・残件台帳） | 宿題 |
| 2 | ★**ダートに着手**（オーナー指示。裁定 §6-1 の「P4 クローズ後」より優先） | 決定 |
| 3 | ★**地面・横の板・砂煙をダートにした** | 実装 |
| 4 | ★**Q-3 の [EYES] 材料を実画面で提出**（内側の帯の反転） | 提出 |
| 5 | ★**同じ間違いを 2 回した**（地面の流れを潰した） | ★自己申告 |

---

## 1. 裁定の宿題 1〜5

### 宿題 1・2 — 不変条件を検査で固定（`apps/cli/test/race-video-invariants.test.ts`）

★裁定 §4 のとおり**閾値は引きません**。跳び px の許容量は演出の裁定事項です（R-16）。
★守るのは「**0 であること**」だけです。

| 守るもの | 内容 |
|---|---|
| ① | ★カットの境目で**共通の被写体が 0 頭にならない** |
| ② | ★**先頭（自馬）が画面の外に出ない** |

★台本も γ も**引数で渡しません**。渡さなければ画面の既定に落ちます（R-31）。

★**検査が本当に捕まえることを確かめました**（R-16）。
`contestFocusWithLeadInFrame` を無効にすると ★**seed 14 で 93 コマ・32.20s から**検出して落ち、戻すと通ります。
★空回り防止も入れてあります（境目を 1 つも拾えなければ落ちる・R-3 / R-21）。

⚠️ ★`tools/lib/race-audit-build.d.mts` を新設しました（`any` 禁止のため）。
★検査から使う分だけです。**足りないものは本体を見てから足すこと。**

### 宿題 3・4 — 残件台帳（`RESIDUALS_P4_RACE_VIDEO.md`）

★残件が報告書の §7 に散っていたのを 1 か所へ集めました。
★**「閃光で解決」と書かない**。見えにくくしただけのものは、そう書いています（A-1）。

### 宿題 5 — 提出前に `git status` を見る

★前便で報告書を「未追跡」と申告しましたが、**実際は追跡済み**でした。私の申告ミスです。
★本便は提出前に確認しました。

---

## 2. ダートに着手した経緯

★裁定 §6-1 は「**P4 をクローズしてから P4.5**」を推奨しています。
★一方 CLAUDE.md では**最終判断はオーナー**です。

⚠️ ★**オーナーから「いったいいつになればダートを作ってくれるのですか」という指摘**を受けました。
★P4 のクローズを止めているものを数えると、★**私の作業ではなくオーナー判断待ちの 4 件**でした。
→ ★オーナー指示として着手しています。★裁定より優先しました。

★**走路の幾何には 1mm も触れていません**（裁定 §6-3）。
★ダート戦でも馬は同じ走路（w = 0〜20）を走り、内側の帯は風景のままです。

---

## 3. 何が起きていたか（実装前の状態）

★`surface: 'dirt'` を選んでも★**地面は芝のまま**でした。二重でした。

1. `world-textured.ts` が **`assets.turf` の芝タイル 1 枚だけ**を敷き、馬場を見ていなかった
2. ★さらに `trackBand` が走路を **`#9ccb55`（芝の緑）で直書き**して上塗りしていた

★**1 枚目の試作はタイルを差し替えてもオリーブ色**になりました。2 つ目に気づいたのはその絵を見たからです。

★横からの画（`parallaxPlate`）は焼き込み済みの板で、★芝が絵に入っていました。

---

## 4. 直したもの（★すべて描画だけ）

| | 内容 |
|---|---|
| 地面タイル | `TexturedWorldAssets.dirt` を追加。★無ければ芝に落ちる（R-27） |
| 上塗りの色 | ★馬場で分ける（`SHADE` / `TRACK_TINT`）。★芝の緑の直書きをやめた |
| 横の板 | ★`manifest.dirtLayers`（層名 → 差し替え先）。★**層は増やしていない** |
| 砂煙 | ★**尾を引く砂煙**を新設（ダートのときだけ・14 粒） |
| 内側の帯 | ★`infieldReversed`（芝に反転）。★**既定は従来のまま** |

### 素材（裁定 §6-4 の条件 4 つ）

| 条件 | 対応 |
|---|---|
| ① 承認済みの芝素材を差し替えない | ★`world-turf.*` / `turf-*.png` は**読むだけ**。書くのは新規 |
| ② ダート素材は新たに承認を取る | ★**これから**（芝の承認は継承していません） |
| ③ 実在競馬場の写真を模写しない | ★**写真を一切読んでいません**。色は `palette.json`、形は種を固定した線形合同法だけ |
| ④ 登録簿に載せる | ★`bake-dirt-tile.mjs` / `bake-dirt-plates.mjs` を登録済み |

★継ぎ目も自分で測って出しています（測らずに「継ぎ目なし」と言わない・R-3）:
左右のつなぎ目 **18.0** ／ タイル内部のふつうの隣同士 **16.9**。

---

## 5. ★同じ間違いを 2 回しました — 地面の流れを潰した

★オーナー評「**走り方が芝の時と違ってぎこちない**」。★**2 回とも私が作った地面が原因**でした。

★**見た目の速さは「画面上の地面の流れ」で決まります**（既知・過去に 113 px/s 対 2279 px/s で実証）。
★地面が平坦だと**脚だけ動いて進んでいないように見えます**。

### 1 回目 — 地面タイル（正面・コーナー）

| | 進行方向のばらつき | 最も強い周期 |
|---|---|---|
| 芝 | sd **4.24** | 2.311m ごと |
| ★ダート（旧） | sd **1.68** | ★**0.385m ごと** |
| ★ダート（新） | sd **4.08** | 2.311m ごと |

⚠️ ★0.385m の周期は**私が入れた整地の筋**です。16m/s では**毎秒 41 本**流れ、
★模様ではなく**ちらつき**になっていました。

### 2 回目 — 横の板（オーナーが見ていたのはこちら）

★1 回目を直したあとも「ぎこちないまま」でした。
★**お送りいただいた画面は `winner-follow`＝横からの画**で、そこは板です。

| 板 | 芝 | ★ダート（前） | ★ダート（今） |
|---|---|---|---|
| near | 3.31 | **1.88** | ★**3.96** |
| mid | 2.80 | 1.71 | ★3.52 |
| far | 3.33 | 1.80 | ★3.89 |

⚠️ ★原因が 2 つ重なっていました。
・輝度を **0.78 倍に圧縮**していた
・色の範囲が `dirt-2`〜`dirt-0`（輝度 87〜153）と**狭かった**

### ★申し送り

★**地面を作ったら、進行方向のコントラストを芝と突き合わせる。**
★色が正しくても、**流れが読めなければ走りが壊れます。**

---

## 6. ★砂煙 — 2 回外しました

★オーナー要求「**ダートでは砂煙が必要**」。

### 1 回目 — ★何も見えなかった

機構は前からありましたが、★**良ダートで強さ 0.22・蹴り出しの 5 コマだけ**でした。
尾を引く砂煙を足しましたが、★**画面には何も出ませんでした**。

⚠️ ★原因: 砂煙を `kickupColor`（**`#796047` ＝ 地面と同じ茶**）で描いていました。
★**地面に溶けていた**のです。★不透明度を 1 にしたら巨大な塗りつぶしが出たので、描いてはいました。
→ ★`dustColor`（`#cdb494`）を新設。★**空中の砂ぼこりは地面より明るい**。

### 2 回目 — ★「丸いポンポン」に見えた

★オーナー評「**砂煙が丸いポンポンしている感じで砂煙っぽくない**」。

⚠️ ★原因: **均一な塗りで楕円を塗ると、縁が硬くて丸に見えます。★砂煙に縁はありません。**
→ ★`Ctx2D` に `createRadialGradient`（任意）を足し、中心から外へ 0 に落ちる**放射状の濃淡**で塗る。
→ ★粒は**真円**にする（楕円で切ると**上下だけ縁が残る**）。平たさは**粒ごとの高さをちらして**出す。
→ ★粒を 9 → 14 に増やして重ねる。★濃淡にすると実効の濃さが落ちるので不透明度を戻した。

★`winner-follow` でも出ていることを差分で確認しました
（範囲 x0-600 / y294-465・最大差 119・目に見える画素 6563）。
★**静止画では薄く見えますが、描かれていないのではありません。**

---

## 7. ★芝は 1 画素も変えていません

★同じ場面（seed 42・24s）を撮り直して、★**完全に同一**であることを確認しています。

---

## 8. ★Q-3 の [EYES] — 実画面を提出しました

★裁定 §6-3 の懸念は**そのとおり**でした。

- **A（いまの案）** … 走路と内側の帯が**同じ褐色に溶けて**、どこを走っているか読めません
- **B（反転案）** … 内側が緑なので、**走路が一目で分かります**

★**開発側の推奨は B** です。幾何ゼロで、引きの画の可読性だけが上がります。
★いまは `/race?infield=turf` の明示指定で、★**既定は従来のまま**です（R-27）。★採否はオーナー判断。

---

## 9. ★Q-5 について — 裁定の材料に無かった 1 点

裁定 §5-4 は「跳びは中央 301px で 4 角の 1/3」を根拠に「両方入れない」を推奨しています。
★実測でもう 1 点あります。

```
① 1312m  横(馬 41.1%・画面内 4頭) → 正面(馬 18.2%・画面内 11頭)   ★馬が 2.3 分の 1
② 1392m  正面(馬 37.9%・6頭) → 横(馬 41.3%・5頭)                  大きさはほぼ同じ
```

★**①は跳び px には出ない種類の変化**（大きさが半分以下・頭数が 4→11）です。
★判断はオーナーに委ねますが、**この 1 点は裁定の材料に無かった**ので申し添えます。

---

## 10. ★まだできていないこと

1. ★**後続が前の馬の砂を被らない**。いまは各馬が自分の砂を出すだけです。★ダートの見せ場はここ
2. ★勝負服・馬体が汚れない
3. ★ダート素材の**承認が未取得**（裁定 §6-4 条件 2）
4. ★残件台帳（`RESIDUALS_P4_RACE_VIDEO.md`）の A-1〜C-3 は変わらず

---

## 11. 検証

```
npm test        → 123 ファイル / 1215 件 PASS（exit 0）
npm run typecheck → クリーン（exit 0）
```

★`ASSET_VERSION` は 57 です。⚠️ ★素材を足したり焼き直したら**必ず上げること**。
★据え置くとブラウザが**古い manifest を返して素材を読みません**（実際に一度踏みました）。

---

## 12. 再現コマンド

```powershell
# 見る（dev サーバー: cd apps/web ; npm run dev）
http://localhost:3210/race?seed=42                  # 画面右下で馬場を切り替える
http://localhost:3210/race?seed=42&infield=turf     # ★内側の帯を芝に反転（Q-3 の B 案）

# 素材を焼き直す
npx tsx tools/bake-dirt-tile.mjs      # 地面タイル（継ぎ目も測って出す）
npx tsx tools/bake-dirt-plates.mjs    # 横の板

# 静止画
npx tsx tools/shot-race-at.mjs --seed 42 --surface dirt 14 24
npx tsx tools/shot-race-at.mjs --seed 42 --surface dirt --infield-reversed --fov 38 14
```

---

## ★13. 申し送り

1. ★**地面を作ったら、進行方向のコントラストを芝と突き合わせる。**（§5）
   ★色が正しくても、流れが読めなければ**走りが壊れます**。★同じ間違いを 2 回しました
2. ★**「見えない」と「描いていない」は別。**（§6）
   ★不透明度を 1 にして確かめるまで、私は「描けていない」と思い込んでいました
3. ★**均一な塗りには縁がある。**（§6）★煙・霧・埃に縁はありません
4. ★**素材を足したら `ASSET_VERSION` を上げる。**（§11）★古い manifest がキャッシュから返ります
5. ★**オーナーが見ている画がどれかを、先に確かめる。**（§5）
   ★私は正面の地面を直して「直った」と報告しましたが、★オーナーが見ていたのは横の画でした
