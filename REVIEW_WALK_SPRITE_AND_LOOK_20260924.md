# 【STAR】裁定: 歩きのスプライトの独立確認 ／ オーナー決定 D-1〜D-5 の受理 ／ 報告 5 件 — 2026-09-24

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa` の報告（歩きの `steps()`・`/train` の馬・表情・`/setup` の失敗表示ほか）と、オーナー決定 `DECISIONS_HORSE_LOOK_20260924.md`

## 1. オーナー決定 D-1〜D-5 — ✅ 受理。★裁定 §4 の決め残しは閉じた

- D-1 / D-2 毛色 **9 種**・白毛 3% ／ D-3 隣接 1.90 組を許す ／ D-4 表情は「まぶた・眉だけの透過レイヤー、頭部は描き直さない」／ D-5 白斑 4 種は見送り（簿 `LOOK-MARKINGS-DEFERRED`）
- → ★裁定 `b7a2514` §4 の ④⑤ が閉じたので、★**9 毛色への焼き直しに進んでよい**
- ★D-3 の理由づけ（「古い規則は**同じ馬の毛色がレースごとに変わる**ので戻せない」）は、★私の §1 の判断と同じ。★**戻せない側の欠点が重い**という読みで一致
- ★**正典（アートバイブル）の「毛色 5 種程度」を 9 種に直すのはレビュー側の仕事**。★決定が閉じたので、次の便で直す

## 2. 🔴 歩きのスプライト — ★独立に確かめた（ただし**ブラウザでは測っていない**）

**結論: 開発側の読みは正しい。`jump-none` が要る。**

✔ レビュー側が確かめたこと（[CODE]・算術）:
- `background-position-x` の百分率は「**はみ出し量に対する割合**」なので、**8 コマ・`background-size: 800%`** ならコマの境目は **k/7**（0・14.29・28.57 … 100%）
- 既定の `steps(8)`（`jump-end`）が止まるのは **k/8**（0・12.5・25 … 87.5%）→ ★**7 コマで 2 コマが半分ずつ映る**。開発側の実測（k/8 の位置で平均差 25〜35、k/7 の位置で 1.01〜1.04）と**向きも大きさも合う**
- `steps(8, jump-none)` は**両端を含む 8 点**で止まる → k/7 と一致

⚠️ ★**私はブラウザで測っていない**。★この PC の開発サーバーは**本番の DB に向いている**（`apps/web/.env.local` が production・`4e9592e` の帯で確認できる状態）ので、★**測るためだけに本番へ繋ぐ開発サーバーを立てない**。★私が確かめたのは**算術と原文**であって、★**画面ではない**。★開発側の実測を否定する材料は無い

### ★ついでに見つけたもの（同じ族・報告に無かった点）
✔ `apps/web/src/components/uma/uma-theme.css:179-186` に、★**同じ問題を別の解き方で直している箇所**がある:
- 走り（6 コマ・`background-size: 600%`）は `steps(6)`（既定）のまま、★**キーフレームの終点を 120%** にして k/5（0・20・40・60・80・100%）に合わせている
- → ★**同じ「コマの境目 = k/(N-1)」を、片方は `jump-none`、片方は終点 120% で解いている**。★どちらも正しいが、★**2 通りある**ことを知らないと、次に触る人がどちらかを壊す
- **条件**: ①`train-walk-sprite.test.ts` と同じ形の網を、★**走り（6 コマ・120%）にも**かける ②両方の註記に「★もう 1 つの解き方がここに在る」と相互に書く（★次に触る人が両方を見る）

## 3. 報告 1・3・5 — ✅

- **1**（`/train` の馬をレースの馬そのものに）: ★**焼き直していない**（パドック歩き 8 コマから騎手・鞍・頭絡・手綱を消して揃え直した）→ ★裁定 `b7a2514` §1「`side-v8` に揃える」を、★**新しく焼かずに満たした**。★`verify-pose-set` の接地 1.7% / 被写体高 2.6%（合格済みの `side-v7` は 9.6% / 9.5%）＝ ★**対照つき**
- **3**（表情 3 種が作れていない）: ★2 回試して 2 回とも別キャラになった、と**書いている**。★簿 `LOOK-FACE-MOODS-MISSING` に登録済み。★D-4 の部品方式（まぶた・眉だけの透過レイヤー・受け入れ条件「画素の 90% 以上が透明」）で焼き直し中 → ★**受け入れ条件を先に数で決めている**のが正しい
- **5**（`/setup` の失敗表示が「最後の枝が既定」で嘘の理由を出していた）: ★`Record<SetupError, string>` にして**種類を足したら型検査が落ちる**形へ。★試験も `SetupError` の宣言から種類を読む（★書き写さない）→ ★**同じ形（写しが古びる）を機械で止めた**

## 4. 報告 4（`own-header-coverage` の網の拡張）— ⚠️ 未着手でよい

★9 毛色の焼き直しと表情が先。★**簿に期限つきで載っていれば、順番は開発側が決めてよい**。

## 5. レビュー側の宿題

- ★アートバイブルの「毛色 5 種程度」→ **9 種**（D-1）。★併せて、★`側 v8 に揃える`（`b7a2514` §1）も正典に書く

---

## 6. 追記（2026-09-24）— `u-gallop` の削除と網 2 本 ✅ ／ 正典を直した

### ✅ 受理
- ✔ ★**`u-gallop` は使う所が 0 件だった**（`@keyframes` 13 件のうち 1 件だけ）。2026-09-17 に差し戻された TOP の馬の残り → ★**「2 通りが同じファイルに在る」状態そのものを消した**のは、私の条件 ② よりも良い直し方
- ✔ ★知識は捨てず、廃止の註記に「終点を N/(N-1)×100% にする解き方も在る／復活させるなら網も一緒に持ってくること」。★`u-walk` 側にも相互参照
- ✔ 網 2 本（`uma-theme-keyframes.test.ts`）: ①**宣言した `@keyframes` がすべてどこかで使われている** ②`background-position-x` を百分率で動かす `@keyframes` の終点は 100%。★`u-gallop` を戻す変異で 2 本とも落ちる
  - ★①が効く理由の言い方が良い —「**使われない規則は、間違っていても誰も気づかないまま『別の作法』として次の人に読まれる**」。★廃止の跡が 4 つあり、毎回「grep で確かめた」と註記していた＝**人の記憶に頼っていた作法**を、機械の外に出した
- ✔ 表情の部品（D-4）: ★生成物が位置を持たないので、**頭部の絵から目の外接を測って合わせる**（白目だけでは 1/4 になるので塗り広げる）。★画面は `inset: 0` で重ねるだけ＝**座標を持たない**。★**片方だけ置かない**（1 つでも条件を外したら 1 枚も置かない）

### ⚠️ `sharp` の `composite` と `resize` の順（開発側の指摘）
- ★`sharp(a).composite(b).resize(3)` は、★**鎖に書いた順ではなく `resize` の後に `composite`** が当たる。★確認用の絵が嘘をつく
- ✔ **レビュー側には画像を作る道具が無い**（この案件で私が書いたのは文書と、`git`・`curl`・`python` の読み取りだけ）。★該当なし
- ★ただし**過去に開発側が出した「確認用の絵」を私が判断材料にした場面**は在る（毛色の見本）。★**絵で確かめたと書いたものは、作り方も一緒に読む**ことを、こちらの作法に足す

### ✅ レビュー側の宿題を済ませた（`3ce2f56`）
`design/art/art-bible.md` を直した:
- 毛色 **9 種**（★重みは `coat.ts` の `COAT_WEIGHTS` が唯一の出どころ・**正典に数を書かない**）／★実在の割合そのままではないこと／★白毛を入れる（以前の「入れない」は D-2 で覆った）
- 毛色は **馬 ID から**（枠番からではない）
- 勝負服は**持ち主が居る馬だけ本人の色**・NPC は枠から／★**たてがみの色替えは採らない**
- ★**ゲームの中で「その馬」を見せる画面は `side-v8` に揃える**。TOP の看板は当面別扱い（簿 `LOOK-CHIBI-VS-SIDE-V8`）

### 歩きの実測の道具
★`tools/measure-walk-sprite-steps.mjs` は **staging に向いた dev（`:3211`）が前提**と分かった。★オーナーが staging の一周を選んだ日に、**そのついでに私が流す**（★本番に向けた dev は立てない）。

## 7. 追記（2026-09-24）— 9 毛色の焼き直し（`09e3876`）✅ ／ 🔴 落ちてくる量を測る

### ✅ 受理（★本番に出ていた不具合を 1 件 見つけた）
- ✔ `isDeformedHorseAsset` が名前の完全一致で、★**型 B の素材（`side-v8b` / `diag-front-v4b`）が抜けていた** → 同じデフォルメの絵が写真寄りの変換で焼かれていた。★**足す前から落ちていた**（配布中の素材に入っている）
- ✔ 実測（`measure-coat-spread.mjs`・合格線 20 超）: 型 B の 2 役が **19 / 17** → 修正後 **22 / 21**
  - 🔴 ★**2026-09-24 訂正（開発側の申し出）**: ★私はここに「**足す前から本番に出ていた不具合**」と書いたが、★**誤り**。✔ `HORSE_TYPE_BY_GATE` は**全枠が型 A**（2026-09-11 オーナー指示「足が白ブチの馬を一旦使わないでください」）で、★**型 B の素材は 1 度も読まれていない**。★焼いた絵が間違っていたのは本当だが、★**画面には出ていない**。★正しい言い方は「**型 B を戻す日に正しくなる**」。★開発側が自分の誤りの形も書いている —「**素材が焼かれていることと、画面がそれを引くことを、確かめずに同じものとして扱った**」。★私も、開発側の報告の「本番でいま線を割っている」をそのまま書き写した（★**引く側を自分で確かめなかった**）
- ✔ ★**なぜ気づけなかったかを 2 つ書いている**: ①`coated()` は表に無い毛色を「素材そのまま」にするだけで**何も言わない**（静かに通る）②測る道具自身が 7 色を**書き写して**おり、既定の役が `side-v6` だけで**型 B を一度も測っていなかった** → ★どちらも**この案件で何度も出ている形**（写しが古びる・片側しか見ない）。★目録から読む形に直したのが正しい
- ✔ 直し方（末尾の型 `b`/`c` は許し、**版（`v9` 等）は許さない**）は妥当。★「新しい版が黙って当たると絵が急に変わる」という理由も正しい。★網 4 本＋対照
- ✔ 新しい網 `baked-coats.test.ts`（目録の毛色が表と一致／全役に全毛色のファイルが在る）。★「表に 1 行足すのは 5 秒、焼くのは 6 分なので、離れている限りまたずれる」＝**ずれる理由を先に言っている**
- ✔ 残りの赤（写真寄り 2 役で栗毛 ↔ 月毛が 20 / 19）は、★**月毛がまだ画面に出ない**ので実害なし。★簿に「手で動かさず `tune-coat-spread.mjs` を使う・写真寄りの馬の色が変わるのでオーナーの目が要る」と条件つきで残した

### 🔴 足す条件 — 落ちてくる量を測る（照会に無かった点）
✔ レビュー側が数えた: `apps/web/public/art/baked/` は **91 ファイル・96.5 MB**（★開発側の報告と一致）。★1 枚が **1〜2 MB**（`side-walk-palomino` 2.12 MB ほか）。

- ★**毛色を増やすと、1 つの画面で落ちてくる量が増える**。★レースは**出走馬ごとに「役 × 毛色」の表**を引くので、★**1 レースで何 MB 落ちるかは、出走馬の毛色の散り方で決まる**。★9 種になると散る（★それが狙い）ので、★**落ちてくる量は増える方向**
- ✔ 正典に**容量・初回ロードの線は無い**（`STAR_SPEC_v2.0.md` を「初回ロード」「転送量」「MB」で引いて 0 件）。★線が無いまま素材だけ増えている
- **条件**（★9 毛色を**本番のレースに出す前**に）:
  1. ★**実測する**: 12 頭立て・18 頭立てのレースを 1 回見たときに**実際に落ちてくるバイト数**（★ファイルの数と合計。★repo の大きさではなく**その画面が引く量**）
  2. ★**携帯の回線での見え方**（読み込み待ちの長さ）は [EYES]。★オーナーに見てもらう
  3. ★数が出たら、★**線（目安）を決めて正典に書く**。★線が無いと、次に誰かが素材を足すときに「増えた」としか言えない
- ★**急がない**: 月毛・白毛はまだレースに出ない（`COAT_BY_GATE` に無い）。★**結線の便で一緒に測る**

## 8. 追記（2026-09-24）— 🔴 落ちてくる量の実測（`5da70be`）— ★「9 毛色で重くなる」ではなく「**もともと重い**」

### 実測（開発側・読むだけの道具 `measure-race-asset-bytes.mjs`）
| | いま（枠番から） | 馬 ID から（9 毛色） |
|---|---|---|
| 12 頭立て | 12 ファイル **13.5 MB** | 平均 **15.8 MB** ／ p95 19.4 ／ 最大 **24.0 MB**（1.77 倍） |
| 18 頭立て | 16 ファイル **18.4 MB** | 平均 18.3 ／ p95 21.8 ／ 最大 24.0（1.30 倍） |
★焼いてあるのは 9 役 × 9 毛色 = 91 ファイル 96.5 MB。★**実際に引く役は 2 件だけ**（`side-v6` / `diag-front-v2`）。

### 🔴 レビュー側の判断: ★**線を決める前に、方式を問い直す**
- ★開発側の読み「**9 毛色にすると重くなる、より、もともと重いが実態**」は**正しい**。★桁は変わらない（13.5〜18.4 → 最大 24.0 MB）
- ★**1 レースを見るのに 13〜24 MB は、携帯の回線には重い**。★このゲームは「数分おきのレースを何度も見る」設計（D-007）なので、★**初回だけの話にならない**（★キャッシュが効く範囲は未測定）
- ★**線（目安）をいま決めると、いまの重さを追認することになる**。→ ★**先に「減らせるか」を見てから線を決める**

**結線の便で見てほしい材料（★どれも私の思いつきなので、開発側が実測で潰すこと）**:
1. ★**1 役 1 枚 ＋ 実行時の色変換**（★`COAT_TRANSFORMS` は既に CSS の filter と同じ意味の係数を持ち、★`/train` は 1 枚に filter を掛けている）。★2026-09-02 に焼いたのは「端末で焼くとキャンバス 992 枚・2,398 MB」だったが、★**それは記憶量の問題**。★描く側を変えれば解けるかもしれない
2. ★**大きさそのもの**（1 枚 1〜2 MB）。★寸法・コマ数・形式（AVIF 等）で落ちるか
3. ★**要るものから落とす**（自分の馬・先頭の数頭を先に、残りを後で）
4. ★**引かない役が 7 件**ある件（`side-walk` がパドックの歩きに繋がっていない可能性）— ★**繋がっていないなら、その分の焼きは要らない**

### 条件
- ★**線（正典に書く数）は、上の 1〜4 を見た後で決める**。★それまで「線が無い」を簿に載せておく（`ASSET-BYTES-NO-BUDGET`・戻る条件は「結線の便で方式を決めたとき」）
- ★**携帯の回線での待ちの長さ**は [EYES]（オーナー）。★数だけで決めない

## 9. 追記（2026-09-24）— 🔴 実測の訂正（`5da70be` → `0fade0b`）: **19.4 / 24.3 MB**

| | いま（枠番から） | 馬 ID から（9 毛色） |
|---|---|---|
| 12 頭立て | 16 ファイル **19.4 MB**（うち発走前の紹介 5.9 MB） | 平均 21.3 ／ p95 26.9 ／ 最大 **31.6 MB** |
| 18 頭立て | 20 ファイル **24.3 MB**（同 5.9 MB） | 平均 23.8 ／ p95 29.1 ／ 最大 **31.9 MB** |

- ✔ ★**私の材料 4 への答え**: `side-walk`（パドックの歩き）は**繋がっていた**。★台本の一覧ではなく `race/page.tsx` の `bakedWalk` が**別の口**で読んでいる。★**「一覧に無い ＝ 使っていない」ではなかった**
- ✔ 引かない役は **6 件**（型 B の 2 役・写真寄りの 2 役・勝ち馬の 2 役）
- → ★**結論は変わらず、むしろ強まる**: 「9 毛色で重くなる」より「**もともと重い**」。★**1 レースを見るのに 19〜24 MB**（9 毛色にすると最大 32 MB）

### ★開発側が自分でまとめた誤りの形（★レビュー側も同じ形を踏んだ）
> 「**素材が焼かれていること**」と「**画面がそれを引くこと**」を、★**引く側を確かめずに同じものとして扱った」**（今日 2 回: 型 B の件・`side-walk` の件）。★どちらも「**一覧に載っているか**」で判断し、★**一覧の外の経路を見ていない**

★これは `R-29′`（渡された一覧を自分の母集団にしない）の**画面の版**。★**作法として足す**:
> 🔴 ★**「使っている／使っていない」を、一覧・台本・目録だけで決めない。★読む側の原文を引く**（★別の口から読んでいることがある）。
★私も §8 で「`side-walk` が繋がっていないなら焼きは要らない」と、★**一覧に載っていないことを根拠に書いた**。★同じ形。

### 条件は変えない
★線は先に決めない（簿 `ASSET-BYTES-NO-BUDGET`・`0fade0b`）。★結線の便で方式（材料 1〜4）を実測してから。★材料 1（1 役 1 枚 ＋ 実行時の色変換）は開発側も有望と見ている。★2026-09-02 の記憶量の問題は**別の測り方が要る**（CSS の filter は GPU 側）と書いているのも正しい。

## 10. 追記（2026-09-24）— 🔴 線を通った実量 ≒ **10.5 MB**／音声が次に効く

### ✅ 受理
- ✔ push（`e407050..dc3ff4c`・移行 0・新ルート 0）。★裁定書 4 通も一緒に乗った
- ✔ **q90（非可逆）はオーナー決定**。★根拠が残っている（画素の平均差 1.41/255・188px で可逆/q90/q82 を並べて「見分けられない」）。★毛色の見分けも合格線の上（24→23・22→21・26→25）。★**`--lossless` を残して戻せる**のが良い
- ✔ ★**`?baked=1` を付ければこの機械でも測れる**（既定は端末判定で PC の headless は従来経路に落ちていた）→ ★「ローカルでは測れない」は**誤りだった**と自分で訂正している

### 🔴 数の置き換え（★§8・§9 は古くなった）
★**線を通った実量**（本番ビルド換算・12 頭立て）: ★**約 10.5 MB**
| 中身 | 量 |
|---|---|
| 焼いた馬 | **4.19 MB**（★§9 の 19.4 MB から落ちた。★q90 と「実際に引く分だけ」で） |
| その他の絵（`/art/`） | 3.83 MB |
| 音声 | **2.44 MB** |
| ページの JS | 約 0.4 MB（★開発サーバーの 4.94 MB は**圧縮前で当てにならない**と自分で訂正） |

→ ★**§8・§9 の「19〜24 MB」は、焼いたファイルの大きさを足したもので、線を通った量ではなかった**。★**この §10 の数を正とする**。
→ ★開発側の読み「**馬だけ直しても終わらない。馬は 4 割**」は正しい。★`crowd.mp3` 単品 1.54 MB が、馬のアトラス 1 枚（0.42 MB）の 3.7 倍

### 🔴 音声 — ★レビュー側が [CODE] で見たこと（次に効く所）
✔ `apps/web/src/app/race/race-audio.ts:127-141`: ★**作った時点で 5 つ全部を取りに行く**（`preload()`）。★音を出さない人にも 2.44 MB 落ちる
- ✔ そうした理由も書いてある（2026-09-13・ファンファーレの出番は**イントロの 4.4 秒**しかなく、押してから取りに行くと間に合わない）→ ★**この理由は正しい。消さない**
- ★**分けられる**: `fanfare.mp3` は **0.26 MB**。★間に合わせる必要があるのはこれだけ。★`crowd.mp3`（1.54 MB）と `gallop.mp3`（0.61 MB）は**発走以降**の音で、★開いてから走り出すまで **32.4 秒**ある
- **決定（案として開発側へ）**: ★**先に取るのはファンファーレ（と短い `gate-open`・`whinny`）だけにし、`crowd` と `gallop` はパドックの間に取る**。★2026-09-13 の教訓（出番の短い音は先に取る）は**守られる**。★**2.15 MB を初回から外せる**見込み
- ★それでも残るなら、★**音源そのものを軽くする**（長さ・ビットレート・モノラル）。★音の良し悪しは [EYES]

### ③ 要る順に落とす（R-27 に触る件）— ★レビュー側の見立て
★開発側の懸念「`boot()` の組み替えは R-27『欠けたまま走らせない』に触る」は正しい。★**外し方を決めておく**:
- 🔴 ★**関門を消さない。★場面ごとに移す**。★「レースを開く前に全部揃っているか」→ ★**「その場面に入る前に、その場面に要るものが揃っているか」**（パドックに入る前 = `side-walk`／発走に入る前 = 走行のコマと音）
- ★**揃っていなければ、その場面に入らない**（★いまと同じく待つ）。★**欠けたまま先へ進めない**という R-27 の意図は、★場面ごとの関門でも保たれる
- ★試験は「★**関門を外すと、欠けたまま場面に入れてしまう**」を変異で見る

### 次
★**オーナーが携帯の回線で実機を見る**（[EYES]）。★その結果で次の手を決める、で正しい。★**数字だけで決めない**

## 11. 追記（2026-09-24）— 本番の実量 **10.78 MB** ／ ★私の指示が古かった

- 🔴 ★**私の誤り**: 「音声の分割の下調べに入ってよい」と勧めたが、★**既に実装され本番に入っていた**（`d65b3a7`・`60d071d`）。★**相手の直近のコミットを見ずに、自分の裁定の一覧から次の仕事を出した**。★手が空いた相手に仕事を出すときは、★**まず相手のしたことを読む**
- ✔ 本番の実量（`?baked=1`・12 頭立て）: 全部 **10.78 MB**（馬 4.19／その他の絵 3.82／音声 2.47／**JS・CSS 0.29**）
  - ✔ 簿の ⑥（JS を本番で測り直す）が片付いた。★開発サーバーの 4.94 MB は**当てにならなかった**（本番は 17 分の 1）
  - ✔ ⑤（音声）は分割済み。★**5 つとも落ちてくる**ことを確かめた（★後回しにしたものが一度も鳴らない、になっていない）
- ✔ ★**出せない数を出していない**: 「描き始めるまでに落ちてくる量」は 2 回で 1.73 / 3.93 MB と割れたので、★**1 回の値を代表にしない**と書いた。★確かなのは「crowd と gallop の 2.15 MB は必ず後」まで
- ✔ ★**危うく 5 度目を踏むところだった**件も書いている（窓が 8 秒では「一度も落ちてこない」と出た。40 秒に広げたら来た）→ ★**窓を決める前に「待つべき対象が何秒後に来るか」を言えるか確かめる**を自分の作法に足した

### ③ 要る順に落とす（`boot()` の組み替え）— ★着手前の条件
★§10 の「場面ごとの関門」を、受け入れ条件の形にする:
1. ★**関門を消さない**。★場面ごとに「その場面に要るものが揃っているか」を見る。★揃っていなければ**その場面に入らない**（★いまと同じく待つ）
2. ★**場面と、その場面が要るものの対応を 1 か所に書く**（★`boot()` の中に散らさない）。★次に素材が増えたとき、★**対応表に 1 行足すだけ**で済む形
3. ★試験: ①**関門を外すと、欠けたまま場面に入れる**（変異で赤）②**対応表に無い素材を足しても、場面の関門が黙って通らない**
4. ★**発走の音（gallop）は発走の場面の関門に入れる**（★2026-09-13 の教訓＝出番の短い音は先に取る、を壊さない）

### ④ 引かない 6 役を配らない — ★条件 1 つ
★**配らない役の一覧を、手で書かない**。★**画面が実際に引く役を原文から拾い、その補集合を配らない**（★今日の `side-walk` の件＝「台本の一覧に無い＝使っていない」ではない、を繰り返さない）。★合わなければ止める。
