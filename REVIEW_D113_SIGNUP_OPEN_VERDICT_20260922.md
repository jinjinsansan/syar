# 【STAR】裁定: 本番の一般登録が開いている（D-113 ⑦ 不合格）— 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 報告: 開発側 `star-aa`（`tools/read-auth-settings.mjs --env production`・READONLY）。🔴 `disable_signup=false`（あるべき `true`）
- 運用: オーナー指示「承認不要で自立的に進める」。★直す操作（Supabase の管理画面）はオーナーのもの

## 判定: 🔴 本番配備の段 ④（`0061`〜`0066`）の前に、一般登録を閉じる

### 何が起きているか
- 正典 D-113 ⑦「**開発期間中の登録は招待制または許可リストに限る。判定はサーバー側で行う**」が、本番で守られていない。公開 URL から誰でも口座を作れる
- ★**これは今日の機能より前から在る穴**。今の本番でも、口座 1 つにつき**付与馬 1 頭**（NPC の在庫・D-079 ⑧）と**初期 EP**（D-075・D-077 ①の総発行量）が、外から誰にでも渡る

### 🔴 画面が無くても、初回の配合は外から呼べる
- 初回の配合の画面はまだ無い。★しかし `request_initial_breeding` は `authenticated` に `execute` が付いた RPC（`0061:183-184`・`0062:93-94`）。★**口座さえあれば、公開されているキーで画面を通さずに直接呼べる**
- → `0061`〜`0063` とワーカーを本番に入れた時点で、★**誰でも口座を作り、無償の配合（種付料 3,000 EP 以上の免除）を 1 回ずつ取れる**。NPC の功労馬と種牡馬の年の枠を、外から削れる（D-120 ② が前提にした形）
- ★「画面が無いから公開されていない」とは言えない（D-119 の裏返し。★書く口が在れば、読む画面が無くても使われる）

### 決定
1. **オーナーが本番の一般登録を閉じる**（Supabase の管理画面 → Authentication → Sign In / Providers →「新規登録を許可」を切る）
2. 本番配備の段 ①〜③（血統修復・`0060`・ワーカー）は、登録が開いていても進めてよい（新しい口を作らない）
3. ★**段 ④（`0061`〜`0066`）の直前に、`read-auth-settings.mjs --env production` を流し直し、`disable_signup=true` を確かめる。★`false` なら段 ④ に進まずに止まる**（手順書に書く・関門にする）
4. 振る舞いでしか測れない項目（E-1〜E-5・E-7）は、段 ④ の前に測る（読むだけ・試しの登録は staging で）

### ⚠️ オーナー自身の口座について
- 登録を閉じると、オーナーも公開の登録画面から新しい口座を作れなくなる
- 裁定 `8a32840` §3 のとおり、移行の後に新しい流れ（父母 → 誕生 → 命名）を試すには新しい口座が要る
- → ★**Supabase の管理画面の「ユーザーを招待（Invite user）」で、自分のメールアドレスを招待すれば作れる**（登録を閉じていても使える）。★順番を気にせず、先に閉じてよい
