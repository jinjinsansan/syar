# 報告: ゲーム本体 第 5 便 — 残りの経路（(a)）

- 日付: 2026-09-16
- branch: `p4/race-30sec-cuts` ／ 開始時 HEAD: `9fce864`（第 4 便）
- オーナー指示: 2026-09-16「a で」（＝第 5 便（効かせる）より先に、**残りの経路**を通す）
- 根拠: 正典 **D-102**（馬の購入・手放す）・**D-103**（厩舎の格）・**§18 LR-1〜LR-8**（生涯の記録）

---

## 0. 一行でいうと

★**「入っているが繋がっていなかった 4 本の経路」を通しました。**
★**着順に効くものは 1 つもありません**（★格はブロンズ既定で 1 ビット不変・物語は読み取り専用）。

| # | 経路 | コミット |
|---|---|---|
| 1 | 厩舎の格を**週送り**に配線 | `9dd2f6f` |
| 2 | **出品を作る**（ワーカーが★と価格を書く） | `ea02511` |
| 3 | **手放す**（移行 `0026`） | `7b4d4f5` |
| 4 | **生涯の記録を書く**（確定の中） | 直近 |

---

## 1. 厩舎の格を週送りに配線（`9dd2f6f`）

- `grow()` に `gainMult`（★既定 1）、`advanceWeek()` に `grade`（★既定 `bronze`）。
- ★**伸びと費用に同じ倍率**を掛けます（`gradeGainMult` / `gradeEpCost`）。★片方だけに掛けると
  「同じ EP で上の格が強い」形になり、**D-103 ② と V-14 ③ が落ちます**。
- ワーカーは `horses.stable_grade` を読んで渡し、`MENUS[menu].epCost` の直読みをやめました（★二重帳簿）。
- ★知らない格は**黙って既定にせず**警報を出します（★列の CHECK を外した日に備える）。

**示し方**: ★ブロンズは格を渡さない場合と **1 ビット同じ**（52 週・状態も週の記録も文字にして比較）。
⚠️ ★検査 ③ で最初「天井は**変わらない**」と書いて落としました。**故障の恒久ダメージ（§7.5）で天井は下がりえます。**
格について言えるのは「**上げない**」だけで、そちらに直しました。

## 2. 出品を作る経路（`ea02511`）

- `planListings()`（純関数・DB を知らない）＋ `refreshMarketListings()`（ワーカー・日次）。
- ★★は `starsOfPotential`、価格は `priceOfStars` が出して**行に書きます**。★SQL に式を書きません（D-052）。
- ★**下ろす**: ①プールから消えた馬（買われた・引退した）②★が変わった馬
  （★故障で素質が下がると★も下がる。★**見た目と価格がずれたまま売らない**）。
- ★在庫は `count(*)` で数えます（★走査の上限で切った数で判定すると、在庫が常に足りて見えます）。
- ★下限を割ったら**警報だけ**。★帯は広げません（D-102 ⑤・D-079 ⑦）。

## 3. 手放す経路（`7b4d4f5`・移行 `0026`・★どの DB にも当てていません）

★**EP が増える経路**なので、抜けはそのまま「EP の蛇口」になります。4 つで閉じました。

| # | 閉じ方 |
|---|---|
| ① | ★**迎えた記録のある馬だけ**（`ep_ledger` の `horse_purchase`）。★配合で生まれた馬から EP が湧かない |
| ② | ★**実際に払った額と突き合わせ**、戻り ≥ 支払いなら拒否（★RPC と**列の CHECK**の二重） |
| ③ | ★**引退馬は手放せない**（★記録であって所有ではない・§18 LR-2） |
| ④ | ★**冪等**（`dedupe_key`） |

- ★戻る割合（`SELL_BACK_RATE`）は **SQL に書かず**、出品を作るときに `sellBackEP(priceEP)` を行に書きます。
- ★台帳の語 `horse_sale` は `inflow` と**分けて**持ちます（★§11.2 の発行量の監視で混ぜないため）。
- NPC 世界へ戻すとき `npc_stables` の最小 id を入れます（★`0001` の所有 XOR NPC の CHECK を保つ）。

### 3b. ★staging に当てて実演しました（オーナー指示・2026-09-16）

★`npx tsx tools/migrate.mjs --env staging`（未適用 1 件・OK・全 26 件）。★**本番には当てていません。**

| 見たこと | 結果 |
|---|---|
| 列・CHECK・語・権限（`check-0026.mjs`・読み取りのみ） | ✔ 既定値なし／`sell_back_ep < price_ep` の CHECK／`horse_sale` の語／`anon` に EXECUTE なし／D-080 の 1 行 |
| ★3 を 6,000 EP で迎え、手放す | ✔ 戻り **1,200 EP**（★TS の `sellBackEP` と一致・残高 94,000 → 95,200） |
| ★迎えた記録のない馬 | ✔ 拒否「迎えた記録のない馬は手放せません（配合で生まれた馬は手放せません・D-102 ③）」 |
| ★引退した馬 | ✔ 拒否「引退した馬は手放せません（記録として残ります・§18）」 |
| ★二度目（同じ冪等キー） | ✔ 台帳 1 行のまま |
| 台帳 | `horse_purchase -6000(1)` / `horse_sale 1200(1)` ／ ★`pp_ledger` 0 行 |
| NPC 世界への復帰 | ✔ `owner=null stable=1`（★所有と厩舎の排他を保つ） |
| 後片付け（`cleanup-check.mjs`） | ✔ 合成口座 0・台帳 0・**浮いた馬 0**・**引退の列が半端な馬 0** |

⚠️ ★**自分で見つけた誤り**: 最初の実演台本は `retired_at_week` だけを立てており、
`horses_retirement_all_or_none`（引退の 3 列は揃って入るか揃って空か）で落ちました。
★**制約が正しく、私の台本が誤り**でした（3 列を揃えて立て、後片付けでも 3 列を戻すよう直しました）。

## 4. 生涯の記録を書く経路（`b1b2018`）

- `raceStoryEvents()`（純関数）＋ `writeRaceStory()`（ワーカー）。**確定と同じトランザクション**。
- ★**記録の失敗で確定を巻き戻しません**（§18 LR-5）。★物語が 1 行欠けるより、確定と払戻が止まるほうが害が大きい。
- ★残すのは**種類と値**だけ。★文は `storyLineOf` が組み立てます（★文言を直した日に過去の行だけ古くなるのを防ぐ）。
- ★週は **Postgres の `now()`** から（★ワーカーの時計を使わない）。★`epochMs` が無ければ**書きません**。
- ★騎手は**凍結**から読みます（★名簿を引き直さない・D-105 ④）。

---

## 4b. 厩舎の格を EP で上げる経路（移行 `0027`・★どの DB にも当てていません）

- `packages/training/src/grade.ts`: `GRADE_UNLOCK_EP`（シルバー 20,000・ゴールド 60,000）・`nextGrade`・`unlockPriceEP`。
- ★**値段は SQL に書かず**、ワーカー（`grade-flow.ts`）が `stable_grade_price` の行に書きます（★`0025`・`0026` と同じ形）。
- `unlock_stable_grade(uuid,uuid)`: ★自分の現役馬のみ／★**1 回に 1 段だけ**（★飛び級させない — 合計額が変わる）／
  ★**EP だけ**（★`pp_ledger` に触れない・D-103 ④）／★動かすのは `stable_grade` 列だけ（★`potential` に触らない）／★冪等。
- ★**買えるのは強さではなく時間**です（★伸びと費用に同じ倍率＝`gainPerEpRatio` が 1.0）。

⚠️ ★**金銭で格を買える経路は作っていません**（★憲法 2。EP は遊んで貯める点で、金銭では買えません）。

## 5. 検査

| 対象 | 結果 |
|---|---|
| `npm run typecheck` | ✔ エラー 0（strict・`apps/web` 込み） |
| 新規 | `grade-week-wiring` 6 ／ `listing-plan` 7 ／ `market-flow` 7 ／ `horse-sale-migration` 10 ／ `race-story` 7 ／ `story-flow` 7 ＝ **44 件緑** |
| 既存の関係箇所 | `packages/training` 134 ／ 確定まわり 38 ／ `rpc-guard` 14 ／ 較正の登録簿 16 — すべて緑 |
| 残る赤 | **既知の 1 件のみ**（`edit-grammar-audit` ⑨・映像の話・本便の前から赤） |

★**意図して付け替えた錨 1 件**: `horse-market-migration.test.ts` ⑧ の「`0025` が最後の移行」。
★後続の移行を足すたびに落ちる形は錨になりません。「番号が連番で、購入の移行に別のことを混ぜていない」に直しました。

---

## 6. ★オーナーにお願いすること

| # | 中身 | 状態 |
|---|---|---|
| 1 | `! git push origin p4/race-30sec-cuts` | ⏳ お願いします（4 コミット） |
| 2 | **staging に `0026` を当ててよいか**（`npx tsx tools/migrate.mjs --env staging`） | ⏳ ご指示ください |
| 3 | 本番への適用 | ⏳ **していません**（`0023` と配備が先） |

## 7. ★この便で**していない**こと

| # | 中身 | いつ |
|---|---|---|
| 1 | ★**格を EP で上げる経路**（解放の RPC） | 次。★価格を SQL に書かない形（★出品と同じく「サーバーが書いた行」を読む）にします |
| 2 | ★画面（馬市場・馬物語帳・厩舎の格） | ★デザイナーのカード待ち（R-12・R-13） |
| 3 | ★`0026` の staging での実演 | ★オーナーのご指示待ち（§6 の 2） |
| 4 | ★LR-8（行の増え方の実測） | ★staging に物語が溜まってから |
| 5 | ★効かせること（第 4 便の効果 0 を 0 でない値に） | ★Q-GB4-1・Q-GB4-2 の裁定の後 |
