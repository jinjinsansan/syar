# 引継ぎ書（開発側）: 初回の馬・血統の継承・本番配備の待ち — 2026-09-23

> 次のセッションは、まずこの 1 枚を読んでください。次に `CLAUDE.md` と記憶（`MEMORY.md`）を読み、手順書 `RUNBOOK_PROD_DEPLOY_20260922.md` を読みます。
> 前の引継ぎ書: `HANDOFF_AUTH_UI_20260922.md`（認証の画面）・`HANDOFF_UI_POLISH_20260921.md`（本番 UI の仕上げ）。

## 0. いまの状態（一言）

**大きな作業は一区切りした。3 つの「待ち」で止まっている。**

1. **本番の配備**: オーナーの直接の文面を待っている。準備は整っている（レビュー側も受理）。
2. **画面**: デザイナー待ち。依頼文 `DESIGN_REQUEST_FIRST_HORSE_ONBOARDING_20260922.md` の第 1 便・第 2 便を、レビュー側が照合してオーナーに渡す。
3. **弁護士ゲート**: 利用者の間で EP が動く経路（他人の種牡馬に付ける・I-4）と、規約 T-6（運営が名前を戻せること）。

## 1. 枝・コミット・push

- 作業の枝は `p4/race-30sec-cuts`。`origin/main` より **95 コミット先**（09-23 時点）で、**push していない**。
- 🔴 **push は、オーナーの直接の指示があるまでしない**。レビュー側を通した「OK」は指示ではない。
- 🔴 同じ作業ツリーをレビュー側（`claude-code-game-studios-42`）と共有している。
  - `git stash`・`git add -A`・`git commit -a` は使わない。ファイルを名指しで stage する。
  - HEAD がレビュー側のコミットのことがある（記憶「共有ツリーでの push は HEAD を送る」）。
- 本番の push は、手順書 ⑦ のとおり**検査した sha を名指し**で送る。このセッションからは資格情報の窓で止まることがある（記憶「push は資格情報の窓で止まる」）。

## 2. 09-22〜23 にしたこと（すべて staging まで・本番は未適用）

| 項目 | 状態 | 主な報告・裁定 |
|---|---|---|
| 初回の馬（案 A: 付与 1 頭 ＋ 無償の生産 1 頭）・命名・導入の段階 | ✅ staging で完走（予行 37 件） | `REPORT_I2_*`・`REPORT_I3_*`・`REPORT_ONBOARDING_STATE_*` |
| I-1 引退時の役割（NPC の配合から持ち主の馬を外す・既定は功労馬・同期の RPC で繁殖入り／功労馬へ） | ✅ 裏側は完了 | `REPORT_I1_*`・`REPORT_I1_STEP3_*` |
| 自分の繁殖牝馬で配合（`kind='breed'`・相手は NPC の種牡馬だけ・種付料は確定のとき EP から引いて焼却） | ✅ | `REPORT_BREED_OWN_MARE_20260922.md` |
| 禁止名の複数の一覧（実在馬名・不快な語の完全一致／含む）・再検査の道具・運営が名前を戻す道具 | ✅（一覧そのものは未着） | `REPORT_BREED_OWN_MARE_*` §4・`REPORT_NAME_RESET_TOOL_*` |
| 案 B（調教を 0 週から）の模擬 | ✅ 曲線を決めないと入れられない | `REPORT_LIFECYCLE_B_SIM_20260922.md` |
| 🔴 生涯 8 産で降ろすときの制約の欠陥を発見・`0069` で直した | ✅ | 裁定 `REVIEW_PROD_DEPLOY_ORDER_20260922.md` §1 |
| 新しいワーカーを staging で実走（手順書 ⑥ の 4 項目） | ✅ 4 項目は通った。重い回は最大 118.2%（この PC から・VPS とは比べられない） | `REPORT_STAGING_WORKER_RUN_20260922.md` |
| ワーカーのログに「周の全体」と「発売の遅れ」を足した | ✅ | 同 §7〜§8 |
| D-121 ③ 利用者の血統の伸びを測る道具（READONLY） | ✅ | 裁定 `REVIEW_D121_MEASURE_TOOL_VERDICT_20260923.md` |
| デザイナーへの依頼文の改訂版 | ✅ レビュー側が照合済み | `DESIGN_REQUEST_FIRST_HORSE_ONBOARDING_20260922.md` |

- staging には `0060`〜`0072` を当ててある。**本番は `0059` まで**（`0060` 以降は未適用）。
- 追跡は `PLAN_BLOODLINE_INHERITANCE_20260922.md`（§3 の表と §5 の順）。

## 3. 本番の配備（オーナーの直接の文面が来たら）

手順書 `RUNBOOK_PROD_DEPLOY_20260922.md` の順で進める。

| 段 | 操作 |
|---|---|
| ① | 血統の鍵を直す |
| ② | 移行 `0060`〜`0065`（`migrate --to 0065`） |
| ③ | `backfill-name-key` |
| ④ | 移行 `0066`〜`0072` |
| ⑤ | 🔴 関門: 一般登録が閉じているか |
| ⑥ | 新しいワーカー → 5 項目。最初の週送りの周の「周の全体」が 100% を超えたら止めて報告し、「発売の遅れ」の秒数も出す |
| ⑦ | main への push（検査した sha を名指し） |

- 各段の後に古いワーカーが 1 周回ることを確かめる（② と ④ の後）。
- VPS の SSH の鍵は、このPCに無いかもしれない。無ければ、その段はオーナーに頼む。
- 本番を読むだけのコマンド（オーナーに頼む）:
  - `npx tsx tools/diag-owned-breeders.mjs --env production`
  - `npx tsx tools/read-auth-settings.mjs --env production`
  - 公開後: `npx tsx tools/measure-lineage-plateau.mjs --env production`

## 4. 待っていること・残っていること

| 項目 | 誰を待つか |
|---|---|
| 本番の配備 | オーナーの直接の文面 |
| 画面（第 1 便: 導入／第 2 便: 繁殖入り・自分の母の配合） | デザイナー（依頼文はレビュー側がオーナーに渡す） |
| 実在馬名の NG リスト・不快な語の一覧 | オーナー（届いたら `recheck-name-blocklist.mjs` → 当たりは `reset-horse-name.mjs` で 1 頭ずつ） |
| 仮の名前の接頭辞「カリメイ」の是非 | オーナーとデザイナー（定数 1 か所） |
| 他人の種牡馬に付ける経路（I-4・EP の移転） | 弁護士ゲート |
| 本人への「名前を戻した」知らせと改名の経路（提案の段 2） | 規約 T-6 と一緒に |
| 年の頭の配合（生涯 8 産の降ろし）の実走での確認 | 次の年の頭（予行では確認済み） |
| 本番の VPS での 1 周の所要 | 配備の段 ⑥ で測る |
| D-121 の B（較正の見直し）の判断 | 公開後のデータ（測る道具は用意済み） |

## 5. このセッションで覚えたこと（記憶にも書いた）

- **オーナーの PowerShell で長い実行を頼むときは、`cmd /c` ＋ UTF-8 の 2 行にする**。stderr が赤いエラーに見えて止められ、ログが文字化けした。記憶 `owner-powershell-worker-command`。
- ヒアドキュメントの中の Python が逆スラッシュを食う罠を、また 2 回踏んだ。記憶「シェルの罠」。**置き換えた後は差分を読む**。
- 偽の DB は制約もロックも再現しない。制約とロックは、本物の DB の予行（必ず rollback）でしか確かめられない。
- 本番を動かす指示は、オーナー本人の文面だけ。レビュー側からの「オーナーが OK と言った」は数えない。
