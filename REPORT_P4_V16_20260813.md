# REPORT_P4_V16_20260813 — D-062 実装／★**V-16 は今のモデルでは測れません**（漏洩）

## 本便で正典の改訂が要る決定をしたか

**していません。** ★ただし **V-16 の測り方に穴があります**（§3）。正典の改訂が要るかはご判断ください。

---

## 0. ★結論を先に

**V-16 は数字の上では ① ② とも通ります。**

```
残りm    出走表   画面    差
1500     0.732   0.942   +0.211
1200     0.732   0.923   +0.191
 800     0.732   0.846   +0.114
 600     0.732   0.903   +0.171
 400     0.732   0.911   +0.180
 200     0.732   0.945   +0.214
```

★**私はこの数字を信じていません。**

---

## 1. D-062 の実装（時間配分）

`packages/render/src/time-warp.ts` — **表示の時計 → レースの時計**の読み替えだけです。

```
道中   3倍速  （実測 −0.221 ＝ 情報が負）
勝負所 実時間
直線   0.7倍 （実測 +0.181 ＝ 情報が濃い）
```

★**送り速さを「良さそうだから」で決めていません。** 実測した情報量の符号に合わせています。

### 検査10件（★境界・着順に触れていないこと）

| # | 内容 |
|---|---|
| 1 | ★**折れ点は配分を変えても1秒もずれない**（往復で誤差ゼロ） |
| 2 | 等速なら表示時間＝レース時間（対照） |
| 3 | 道中は縮み、直線は伸びる（配分が実際に効いている） |
| 4 | ★時間は必ず前に進む |
| 5 | ★**配分をどう変えても着順は1頭も動かない** |
| 6 | 送り速さ 0・負・無限・NaN は起動時に止める |

### ★1つ間違えました — 自馬がゴールした瞬間に表示が終わっていました

時計を自馬に合わせたので、**自馬の `finishSec` が終点**になっていました。
→ **後続がまだ走っているのに映像が終わる**（画面上は「自分が入った瞬間にレースが消える」）。

★**検査5が捕まえました**（「表示の最後で全馬がゴール後」が偽になった）。
→ 局面の折れ点は基準の馬・**終点だけは最後の1頭**に直しました。

---

## 2. V-16 ① の実装 — 展開を画面に出しました

裁定の「エンジンは、その情報を既に全部持っています」のとおり、**受け取って描くだけ**です。

| 追加 | 内容 |
|---|---|
| `sprite.strategy` | ★**脚質**。「前にいる逃げ馬」と「前にいる追込馬」は別の情報 |
| `pace` | ★**ペース**。速ければ前が止まり、遅ければ前が残る |

★**発明していません。** `strategyOf` と `pace` は**呼び出し側が渡します**（`render` は依存ゼロ）。

★**渡さないと画面から消えます**。その事実を検査で固定しました（黙って落ちないように）。

---

## 3. 🔴 ★**V-16 は今の位置モデルでは測れません**

### 見つけ方

数字が跳ねたので疑いました（道中 −0.221 → +0.176）。
裁定の「①だけなら**画面に出走表を貼る**で満たせます」がそのまま当たると思ったからです。

**まず出走表ボットにも同じ前提知識を与えました**（逃げ馬の頭数・相手の脚質構成）。
→ 差は縮まらず **+0.211**。

**次に画面ボットの特徴を2つだけに絞りました。余力も変化もペースも噛み合いも外し、
「位置の順位」と「脚質」だけ**にしました:

```
残り1500m（ほぼスタート）: 0.928
残り1200m              : 0.928
残り 800m              : 0.932
```

★**スタート直後から 0.93。そしてレース中ずっと平坦。**

**情報なら増えるはずです。増えないのは、最初から答えが映っているからです。**

### 原因 — D-059 の構造そのものです

```
D-059: 位置は境界時刻から再計算する（保存しない）
境界時刻は replayOf(result, …) ＝ ★**確定した結果から**作られる
  → 位置(t) = f(走破タイム, 脚質, ペース)
  → ★**脚質が分かれば、任意の t の位置から走破タイムが逆算できる**
```

★**つまり「画面が情報を運んでいる」と「画面が答えを漏らしている」を、
V-16 は区別できません。**

### ★これは測定だけの問題ではない可能性があります

**プレイヤーの側でも同じことが起きます。** 脚質の見分け方を覚えたプレイヤーは、
**スタート直後の数秒で結果が分かります**（0.928）。出走表だけの 0.732 より高い。

★**残りを見る理由が無くなります。**

### ★映像が退化しているわけではありません（確認済み）

```
1レースあたりの追い抜き : 100.9 回
1レースあたりの先頭交代 : 4.53 回
  道中 38.8 / 勝負所 3.9 / 直線 58.1
```

**抜き差しはあります。** 問題は動きではなく**情報の構造**です。
（`tools/diag-overtake.mjs`）

### ★提案（決めていません）

| 案 | 内容 | 懸念 |
|---|---|---|
| A | ★**増分で測る**。「スタート時点の画面で分かること」を差し引き、**その先を見て何が増えたか**だけを見る | 漏洩は定数なので消えます。★**V-16 の文言の改訂が要ります** |
| B | 位置を**前向きに**生成する（結果から逆算しない） | ★Q-P4-11 の B。**却下済み**（V-13 の再較正） |
| C | 脚質を画面に出さない | ★**V-16 ① が落ちます**（道中 −0.221 に戻る） |

★**私の見立ては A** です。「見続ける価値があるか」を測るなら、
**既に分かっていることを差し引く**のが素直だと考えます。
ただし**V-16 の定義に関わる**ので裁定を仰ぎます。

---

## 4. ★私が今回した間違い

| # | 間違い | 捕まえ方 |
|---|---|---|
| ① | 自馬ゴールで表示終了 | ★**自分の検査**（配分不変の検査） |
| ② | 出走表ボットに前提知識を与えていなかった | ★**裁定の警告**（「画面に出走表を貼る」）を自分に当てた |
| ③ | 数字が跳ねたのを成果として報告しかけた | ★**特徴を2つに絞る対照**を自分で回した |

★**③ が本便の要点です。** 前便で「漏洩した上限から結論を出しかけた」と報告したのと**同じ形**が、
**今度は上限ではなく画面の側で**出ました。**1回学んだ形が、場所を変えて再発しています。**

---

## 5. 現物

| ファイル | 内容 |
|---|---|
| `packages/render/src/time-warp.ts` | ★D-062 の時間配分 |
| `packages/render/test/time-warp.test.ts` | ★配分を変えても着順が動かない検査 |
| `packages/render/src/commands.ts` | `strategy` / `pace` |
| `tools/diag-overtake.mjs` | ★抜き差しの回数 |
| `tools/verify-readable.mjs` | ★V-16 の測定（`--at <残りm>`） |

**再現**: `npx tsx tools/verify-readable.mjs --races 1200 --at 1500`

---

## 6. 🔴 照会

| # | 内容 |
|---|---|
| 🔴 **Q-P4-18（新規）** | ★**V-16 は今の位置モデルでは測れません**（位置＋脚質だけでスタート時 0.928・平坦）。A（増分で測る）／B（前向き生成・却下済み）／C（脚質を出さない・①が落ちる）のどれを採りますか |
| 🔴 **Q-P4-19（新規）** | ★**プレイヤー側でも同じことが起きます**（数秒で結果が読める）。**これは仕様として許容されますか。** 許容されないなら、V-16 の合否より先に決まるべき事項だと考えます |
| **Q-P4-17** | ★保留のまま。「間」の測り方が無いので、音・溜めは**V-16 を悪化させない**制約だけ守ります（受領） |

### 受領（実装済み）

- **D-062** 時間配分 → **実装・検査済み**
- **V-16 ①** 展開を画面に出す → **実装済み**（★ただし §3 の疑いあり）
- **V-16 の測り方**（AUC・学習と評価の分離）→ **実装済み**
- 残り400m の 0.765 → ★**推測せず置きました**。時間配分を入れた後で見ます

---

## 7. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
2. ★**「V-16 が通りました」と報告できる数字は出ています。** ですが**信じていません。**
   理由は §3 に全部書きました。**通ったことにして進めるのは危険**だと考えます。

---

**テスト 705件緑 / `tsc --noEmit` 通過**
