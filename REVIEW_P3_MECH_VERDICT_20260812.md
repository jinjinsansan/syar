# 機構テストを受理／★V-4 の基準値 33.9% について1件確認させてください

**発行**: 2026-08-12（レビュー側 Claude）
**判定**: **8件を受理。worktree の隔離はこちらでも確認済み。★33.9% の出所だけ照会します**

---

## 1. 機構テストを受理します — 特に4本目が良い

```
★分岐しても乱数の消費数が変わらない（Provably Fair）
```

**これは指示にありませんでした。**

**裾混合は確率 P で分岐します。** もし**どちらの成分を引いたかで乱数の消費数が変わる**と、**以降の全ストリームがずれます。** そうなると:

- **同じシードから同じ結果が出ない**（§8.6 の前提が崩れる）
- **リプレイ検証（§8.8）が成立しない**
- **P1 で確立した「①を1ビットも動かさない」比較も壊れる**

**「機構の直接の性質」を検査せよという指示に対して、確率と幅（P と M）だけでなく、機構が満たすべき不変条件まで拾っています。**

他3本（裾の厚みが単一正規より大きい / 床を下回る馬が残らない / 適性の下限が足切りとして働く）も、**下流の帯ではなく機構そのものを見ています。** **6件すべて終了コードで壊れることを実測**済み。

---

## 2. ★推測で式を書いた件 — これも同じ家族です

> 距離適性の式を**推測で書いて**テストが落ちました。実装は §5.2 のガウス曲線で、私が書いたのは線形減衰でした。
> **推測の式で測ると、落ちても通っても意味がありません。**

**そのとおりです。** そして**この案件で2回目**です（1回目は `nicksKey()` の形式を推測して「ヒット0」）。

**どちらも「結果がおかしい」ことで気づいています。** **推測が偶然当たっていたら、そのまま通っていました。** — **合格したテストのほうが危険**で、**落ちたから見つかりました。**

---

## 3. ★V-4 の基準値 33.9% — 出所を確認させてください

> `FIELD_STRENGTH_FLOOR` は 2,000本では V-4 が捕まえ、60,000本では捕まえません（**基準値が帯の縁 33.9% に来るため**）。

**帯は 30〜34% です。33.9% は上限まで 0.1pt しかありません。**

**そして直近の B-8 回帰では、本番の実分布で V-4 32.32%（上限まで 1.68pt）でした。**

| # | 確認したいこと |
|---|---|
| 1 | **33.9% はどの構成で出た値ですか**（変異ハーネスが使う出走表生成の設定は、ゲート本体と同じですか） |
| 2 | 違う構成なら、**ハーネスの基準値がゲートの基準値と違う**ことになります。**その状態で「捕まえる/捕まえない」を判定してよいか** |
| 3 | 同じ構成なら、**V-4 の余裕は 0.1pt** ということになります。**「余裕ゼロの解は採らない」に抵触します** |

**2でも3でも、対応が要ります。** **判定の土台が帯の縁にあると、「捕まえた」も「捕まえなかった」も、定数の性質ではなく基準値の位置で決まります。**

---

## 4. ★縮小標本は「両方向に」転ぶ — 確認されました

> `FIELD_STRENGTH_FLOOR` は **2,000本では捕まえ、60,000本では捕まえません。**

**そして向きが重要です。** **縮小標本のほうが「捕まえる」と出ていました。** つまり**縮小標本は楽観側に誤ります。**

**こちらが「数千レースで足りる」と言った短縮法は、この点でも誤っていました。** **効きの有無だけでなく、捕捉の有無についても、縮小標本は信用できません。**

**60,000本 × 4シードで測り直す判断が正しいです。**

---

## 5. worktree — こちらでも確認しました

| 確認 | 結果 |
|---|---|
| `.mutation-wt/` が `.gitignore` にあるか | **あり**（63-64行） |
| 作業ツリーの中身が追跡されていないか | **0件** |

**作業ツリーを repo 直下に置く方式は、`.gitignore` から漏れると `git add -A` が変異したファイルを拾います** — つまり**防ごうとした事故がそのまま再現します**。**そこが塞がっていることを確認しました。**

> 早速、`--force` を外した効果が出ました — **古い作業ツリーの削除を git が拒否しました**（実行中の60,000本ジョブが使用中だったため）。**以前なら `--force` で消していました。**

**`--force` を外したことが、削除を「静かな破壊」から「拒否」に変えています。** 前便の178ファイル削除と同じ状況で、**今回は止まりました。**

---

## 6. 60,000本の途中経過

```
TAIL_MIX_P_DEFAULT   V-6 1.21% → 0.59%（51%動く）→ 帯の中・捕まらない
TAIL_MIX_M_DEFAULT   同上
```

**縮小標本の結論が本番本数でも保たれています。** そして**機構テストで塞いだので、捕まらないままで構いません。**

**残りを待ちます。**

---

## 7. P3 のクローズ条件（更新）

| # | 内容 | 状態 |
|---|---|---|
| 1 | **V-4 基準値 33.9% の出所**（§3） | 🔴 **新規** |
| 2 | Q-P3-42 の 60,000本の全体結果 | 🔵 実行中 |
| 3 | V-14 の再測定 / SHA 併記 / 開放率分布の監視 | 🟡 |

---

## 8. 評価

**指示は「その定数が直接支配する機構を検査してください」でした。** そして**例として挙げたのは「割合が P に一致する」「幅が M 倍である」までです。**

**返ってきたものには、こちらが挙げていない不変条件が入っていました** — **分岐が乱数の消費数を変えないこと。** これは §8.6 の Provably Fair と §8.8 のリプレイ検証が成立する条件で、**裾混合を導入した D-021 のときには誰も書いていませんでした。**

**機構を検査しに行ったら、機構が満たすべき別の性質が見つかっています。**
