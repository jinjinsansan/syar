# QUESTIONS_P1 — 開発側からの照会（2026-08-05）

**宛先**: レビュー側 Claude / オーナー
**対象**: 指示書 `DEV_INSTRUCTIONS_P1_20260805.md`（K-1〜K-5）
**方針**: 正典を推測で上書きしないため、実装は暫定解釈で進め、ここに全件を出します。
正典側の判断が出たら、暫定解釈は破棄して従います。

すべて `packages/race-engine/src/balance.ts` の `INTERPRETATIONS` に登録済みで、
`npm test` が「ソースの参照と登録簿の一致」を機械的に検査しています
（`apps/cli/test/interpretations.test.ts`）。

---

## Q1【要判断・正典の内部矛盾】`condition_aptitude` が genotype にない

- 正典 **§5.2** は馬のパラメータとして `condition_aptitude: { good, yielding, soft }` を定義している
- しかし **§5.4 の genotype スキーマにこの項目がない**。§6 のアレル継承・突然変異の対象は
  §5.4 のスキーマなので、**現状の仕様では馬場状態適性は遺伝しない**

§8.3 の `trackConditionCoef` はこの値を使うため、レース判定に直接効きます。

**暫定対応**: `RaceEntrant` の入力として外から受け取り、K-5 のモンテカルロでは中立値(50)を置きました。
genotype を勝手に拡張すると P0 の全ゲート（特に V-2d/V-2e は形質ごとに測る）に波及するため、
実装は保留しています。

**照会**: ①genotype に追加する（＝P0 の形質数が 11→12 以上に増え、§6.4 の変異パラメータ表と
V-2d/V-2e の対象も増える）／②遺伝させず馬ごとの固定属性とする／③§5.2 から削除する
— のどれでしょうか。①なら回帰中心と変異SDの導出も必要になります。

---

## Q2【要判断・正典の数値が成立しない】§8b.2 のスタミナゲージ

正典 §8b.2:

```
初期値 = clamp( 50 + ST/20 + (condition-3)*4 - max(0, fatigue-50)/2, 20, 100 )
毎秒消費 = 1.0 * ポジション係数 * ペース係数   // 先頭付近1.3 / 中団1.0 / 後方0.85
仕掛け中は消費3倍
```

2000m のレースは約120秒です。消費は最小でも `0.85 × 120 = 102`、仕掛けを含めると 300 を超えます。
**ゲージの上限は 100 なので、どんな馬でも必ず 0 になります。**

その結果 §8b.3 の「ゴール前にゲージ0なら `interventionMult` −0.15」が**全馬に常時適用**され、
介入の巧拙が結果に出ません（実測でも AI 代行の平均倍率が 0.968 まで落ち、V-9 を外しました）。

**暫定対応**（2つの解釈を置きました。どちらも正典の定数自体は変えていません）:

| ID | 解釈 |
|---|---|
| `I-STAMINA-WINDOW` | ゲージが減るのは**勝負所（残り800m）以降**とする。ゲージは「余力」を表す |
| `I-STAMINA-EMPTY` | 「バテ」= **直線（残り400m）に入る前に**尽きた場合。ゴール直前に使い切る最適プレイは罰しない |

**照会**: ①この解釈で正典を追記する／②消費率の絶対値を下げる／③ゲージの上限を上げる
／④「ゴール前にゲージ0」の罰則を早仕掛け時限定と明記する — のどれでしょうか。

なお ②③は §13.1 の定数変更になるため、こちらでは動かしていません。

---

## Q3【要判断・較正結果】`RACE_RANDOM_K` を 0.12 → 0.26 に改訂すべきか

正典 §13.1 は `RACE_RANDOM_K: 0.12`、§8.7 は「**0.12 は「1番人気の勝率が30%前後」に落ち着く値**」
と述べています。同時に §8.7 は「**実装後に必ずモンテカルロ10万レースで人気別勝率を較正する**」
とも定めており、後者に従って較正しました。

**実測（10万レース・4シード）**:

| K | 1番人気 勝率 | 1番人気 複勝率 | 最低人気 勝率 |
|---|---|---|---|
| **0.12（正典値）** | **51.6%** | 81.6% | 0.38% |
| 0.20 | 38.0% | 69.0% | 0.50% |
| 0.24 | 34.2% | 64.0% | 0.85% |
| **0.26（較正値）** | **32.4%** | 61.3% | 0.81% |
| 0.32 | 30.6% | 55.4% | 1.50% |

原因は「出走馬同士の実力差」が K に対して大きいことです。要因分解
（`npx tsx apps/cli/src/race-diagnostics.ts`）:

```
中立化した要素                      レース内スコアCV   基準比
（中立化なし・基準）                     17.18%      100%
素質開放率（開放率の差を消す）             12.33%     71.8%
距離適性（全馬をレース距離ぴったりに）        15.51%     90.2%
馬場適性（全馬 50）                    16.43%     95.6%
```

最大の寄与は**素質開放率のプレースホルダ**（指示書 §3 が例示した 0.55〜0.85）でした。
ただし**開放率の幅をほぼ0（0.695〜0.705）まで潰しても 1番人気の勝率は 38% までしか下がらず**、
残りは K を動かすほかありませんでした。

**暫定対応**: `DEFAULT_RACE_BALANCE.RACE_RANDOM_K` は**正典の写しとして 0.12 のまま**にし、
較正値は `CALIBRATED_RACE_RANDOM_K = 0.26` として分離しました（`verify:race` の既定はこちら）。
P0 の D-008 と同じで、**正典は推測で上書きしません**。

**照会**: ①§13.1 を 0.26 に改訂する（あわせて §8.7 の「0.12 は30%前後」という記述も訂正が要ります）
／②0.12 のままとし、実力差の側（開放率・クラス分け）でさらに詰める
／③P3 の育成ループが入るまで判断を保留する — のどれでしょうか。

⚠️ **この較正は素質開放率プレースホルダに依存します（R-7）。P3 で本物の育成ループが入ったら
必ず再較正が要ります。** その旨を正典 §13.3 に追記していただけると、条件を忘れずに済みます。

---

## Q4【要判断・検証基準の解釈】V-8 と V-9 が同時に成立する読み方

正典 §13.2:

- **V-8**: AI代行 vs 手動介入の**期待値比** = 手動最適プレイの 93〜97%
- **V-9**: `interventionMult` の分布 = 平均 **0.99〜1.01**・全サンプルが 0.90〜1.10 内

「期待値」を `interventionMult` の期待値と読むと、手動最適は §8b.3 の各上限から
`1 + 0.03 + 0.09 + 0.10×(GT/1000)` となりキャップ 1.10 に張り付きます。
すると AI 代行は 0.93〜0.97 × 1.10 = **1.023〜1.067** でなければならず、
**V-9 の「平均 0.99〜1.01」と両立しません**。

**暫定対応**: V-9 の母集団を **全出走馬**（介入できるのは自馬のオーナーのみ・§8b.1 なので、
大半の馬は 1.00）と読みました。§8b.8 の「実測分布を日次で記録」という運用監視の文脈にも合います。
これなら AI 代行が 1.04 前後でも全体平均は 1.003 に収まり、両立します。

実測（10万レース）: V-8 = 94.68%（PASS）／V-9 平均 = 1.0032（PASS）。
参考値として「介入対象馬だけの平均 1.0415」「勝率で見た AI÷手動 = 78.74%」も併記しています。

**照会**: ①この読み（V-9 は全出走馬・V-8 は倍率の期待値比）で正典に明記する
／②V-9 は介入対象馬だけの分布とし、目標値を見直す
／③V-8 は勝率（＝実際の期待値）比とし、目標値を見直す — のどれでしょうか。
③を採る場合、現状の実測は 78.74% で目標を大きく外れるため、AI 代行の精度定数の再較正が要ります。

---

## Q5【承認依頼】賞金表のプレースホルダ（正典 §11 未執筆）

K-4 の選抜指標に賞金を使うため、着順ごとの配点が要ります。§11 は未執筆なので置きました:

```
1着 100 / 2着 40 / 3着 25 / 4着 15 / 5着 10（6着以下 0）
```

**照会**: この形（上位に厚く5着まで）で進めてよいでしょうか。§11 が確定したら差し替えます。

---

## Q6【提案】出走表の「クラス分け」を正典に明文化したい

血統プール全体から無作為に出走馬を選ぶと、最強馬と最弱馬の能力差が実際の番組より遥かに広くなり、
**1番人気の勝率が約51%**まで上がります（K=0.12 の実測）。実装では能力帯（クラス）から
出走馬を引くようにしました（クラス幅 = 母集団の6%）。

これは §10.3 の番組表・§10.5 の「NPC はプレイヤー上位30%×0.92 に追従」という
「**同格が集まる**」設計と整合しますが、**正典に明示の記述がありません**。

**照会**: クラス分け（同格帯で番組を組む）を §10.3 に明記していただけますか。
明記がないと、P2 の開催スケジューラを別の人／別のセッションが実装したときに
無作為抽選になり、V-4 が静かに壊れます。

---

## 補足: 今回こちらが**独断で決めなかった**こと

- 正典の定数（`RACE_RANDOM_K` / `STAMINA_*` / `INTERVENTION_CAP`）は1つも書き換えていません
- genotype スキーマ（§5.4）は変更していません
- 検証基準（V-4〜V-12 の目標値）は変更していません

変更したのは**自分たちが置いたプレースホルダ**（素質開放率・クラス幅・賞金表・AI代行の精度定数）
だけです。調整履歴は `REPORT_P1_20260805.md` §6 にあります。
