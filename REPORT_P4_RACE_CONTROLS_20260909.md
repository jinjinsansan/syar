# レース調整卓と発走の連続性（2026-09-09）

オーナーからの指摘：新しい馬でも旧2D時代と同じ違和感があり、真横の確認画面にあった調整つまみを現在のレースにも必要とする。発走、発走後の位置取り、コーナーの曲がり方が不自然。

**今回、調整卓を実装し、発走直後の脚の位相の飛びを修正した。位置取り・カーブ全体の自然さを解決したという報告ではない。** 前回の左右反転防止は、これらの動きの設計を直す変更ではなかった。

## 使い方

- http://localhost:3210/race?dev=1 を開く。開発モードでは `/race` だけでも操作卓を表示する。`?dev=0` で通常画面に戻せる。
- 「馬の大きさ」「上下動・浮き」「1完歩」「再生速度」「発走の加速時間」「発走時のカメラ揺れ」の6つを調整できる。
- 上下動は 0 で各コマの蹄を接地、1 で従来の浮き、2 で浮きを倍にする。影の地面の位置は動かさない。
- 1完歩は脚の回転周期、再生速度は時計・背景・馬を含む映像全体に作用する。
- 停止した場面でも即座に描き直す。再生中の変更は現在の時刻から続ける。
- 「ゲート発走へ」「発走後の位置取りへ」「最初のコーナーへ」「4コーナーへ」で停止して確認できる。コーナーの時刻は選択中のレースのカメラ切替表から取る。
- 「調整を初期値に戻す」で6項目を戻せる。初期値は順に 1 / 1 / 7m / 1倍 / 1.6秒 / 1倍。値はページ内の確認用で、再読み込みすると戻る。

## 直した処理

発走前は全馬共通の位相 `3.5/8` で静止していたが、発走後の式には馬番による位相差が即座に入っていた。この切替によって、ほとんど進んでいなくても別の脚のコマへ飛び得た。

現在は静止位相から走行距離に沿って進み、最初の1完歩で個体ごとの位相差を滑らかに開く。8コマ・16コマとも同じ位相を使う。背景補正距離の定数オフセットを発走時刻で差し引き、開始位置を0にそろえる。

また、従来の再描画用effectが `Number(null) = 0` を時刻として扱っていた。つまみの変更を再描画へ接続すると巻き戻しが発生するため、`auditSec` は明示された値だけを読み込み完了後に一度適用するよう修正した。

発走加速時間を変えた場合は、カメラ切替表と背景の補正距離を同じ設定で再計算する。レースの結果は再生成しない。

描画共通層：`packages/render/src/race-motion.ts`、`perspective-draw.ts`、`broadcast-v2-scene.ts`。
操作・時計の接続：`apps/web/src/app/race/page.tsx`。

## 調査で確認した残件

1. **位置取りの経路**：`packages/race-engine/src/lane.ts` の `laneAt` は、各馬の枠位置から内側の到達位置まで、共通の250m区間のsmoothstepで寄せる。馬ごとの到達位置や揺れはあるが、この関数には周囲の馬の位置を見て空いた場所を取る入力がない。今回、この経路は変更していない。横位置は距離ロスを通じてレース結果にも効くため、描画だけ別の位置へ移すと整合が崩れる。
2. **コーナーの向き**：描画は少数の方向別画像を選ぶ仕組みを継続している。全馬で馬群中心の向きを共有し、4コーナーの据え置きカメラでは左右の向きを入口の接線から保持する。反転は抑止できても、一頭ずつの実際の進路に沿って馬体が連続的に旋回する処理にはなっていない。
3. **完歩と素材の較正**：真横の確認画面には3.818mという実測値の記録がある一方、製品の既定は従来の7mである。現在のレースの素材・縮尺に対してどの値が自然かは、今回のつまみで比較できる。既定の完歩値を見た目の合格として選定したわけではない。

次の修正対象は、周囲の馬との前後関係を考慮する位置取りと、進路・馬体方向・カメラをそろえるコーナーの描画。つまみ追加や単一コマの反転防止だけで、この2点を解決した扱いにしない。

## 検証

- 全体テスト：140ファイル / 1,428件通過。
- 最終調整後の対象テスト：13件通過（脚の連続性・描画・ブラー）。
- 最終 `npm.cmd run typecheck` 通過。`git diff --check` 通過。
- 実ブラウザのCanvasで、大きさ・上下動・完歩・加速時間・カメラ揺れの変更による画像差を確認。
- 停止中の調整でシーク時刻が維持されることを確認。
- 18.00秒から0.5倍再生を約1.2秒実行し、18.55秒へ進行。途中からの再生継続を確認。
- 6項目の初期値リセット、4つの場面送りボタンが有効であることを確認。
- ブラウザ検証はheadless Chrome。操作・描画の反映の確認であり、通常ブラウザでの動画の自然さに対するオーナー判定を代替しない。

記録：`tmp/race-controls-20260909/result.json`。
調整卓の画像：[controls.png](tmp/race-controls-20260909/controls.png)。
発走と4コーナーの静止画、各つまみの比較画像も同じディレクトリ。

既存の `tmp/race-diagnostic/` は削除・上書きしていない。コミットは未実施。
