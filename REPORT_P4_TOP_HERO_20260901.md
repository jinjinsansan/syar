# 報告 — ★TOP ページのモバイル（★ヒーローが切り落とされていた）2026-09-01

作成: 2026-09-01 / ブランチ `p4/race-30sec-cuts` / 起点 `a119190`
指示: ★オーナー ★**「まず TOP ページから崩れていますね」「まずは TOP ページを完璧に仕上げてください」**
関連: `REPORT_P4_MOBILE_CHECK_20260901.md`（★同日の点検。★**そこで「TOP は崩れていない」と誤って報告しました**）

---

## ★1. 結論

| | |
|---|---|
| ★オーナーの指摘 | ★**正しい**。★TOP のヒーローは ★**中身が下から切り落とされていました** |
| ★量 | ★**390px で 96px ／ 360px で 158px**（★消えていたのは「登録は 1 分。クレジットカードは要りません」と CTA の下端）|
| ★私の点検 | ★**3 つの数すべてで「異常なし」と出していました**（★§4）|
| ★直した範囲 | ★`globals.css` の ★**`@media (max-width: 720px)` の中だけ**。★`page.tsx` は ★**1 行も触っていません** |
| ★デスクトップ | ★**無傷**（★1280px の実測がベースラインと完全一致・★§6）|

---

## ★2. ★ご質問への回答 — ★「TOP を馬のヒーローにするハンドオフ」

★**在りました。** ★ただし ★**「馬のイラストにする」という指示ではありません**。★正確には次のとおりです。

| 出典 | 何が書いてあるか |
|---|---|
| `docs/lp-reference-research-20260819.md` §2 | ★**参照作品**のヒーローが「大群の馬と騎手が正面から迫る写実絵」 |
| ★同 §5（学び）| ★**「ヒーローは絵が主役、コピーは 2 行＋要約 1 行」** |
| ★同 §6（STAR への当てはめ）| ★STAR のヒーロー背景は ★**「中継 HUD の 1 コマ（`/race` の静止画）」** |
| `design/hud-ds/REQUESTS.md:82`（★**R-3**）| ★同上。★「背景は中継 HUD の 1 コマを薄い青のグラデで覆う」。★オーナー判定 ★**合格・実装済み** |

★そして ★**実装されている `/lp/hero.jpg` は、その中継 HUD の 1 コマで、★馬は写っています**（★12 番・3 番・4 番の 3 頭が芝を走る画）。

⚠️ ★**つまり「馬のヒーロー」は在るのに、モバイルでは見えていませんでした。** ★理由は 2 つです:

1. ★**96px のロゴと 46px の見出し**（★PC の値）が画面を覆い、★絵が背景に沈んでいた
2. ★**下 96px が切り落とされ**、★CTA と注記が消えていた

→ ★直した後は ★**馬が見える形に戻りました**（★§5 の絵）。★§5 の学び「絵が主役」に近づきます。

---

## ★3. ★何が壊れていたか（★実測）

```
  /  390px  CLIP  <section.lp-bleed> box 390x560  下に 96px はみ出して切られている
  /  360px  CLIP  <section.lp-bleed> box 360x560  下に 158px
```

★仕組みはこうです:

| | |
|---|---|
| ★節 | ★`height: 560px` ＋ ★`overflow: hidden`（★`page.tsx:87`）|
| ★中身 | ★`position: absolute; top: 56px`（★`page.tsx:89`）|

→ ★**絶対配置の中身は、節の高さに 1px も寄与しません。**
→ ★モバイルでは中身のほうが高くなり、★**はみ出した分がそのまま切り落とされます。**

---

## ★4. ⚠️ ★**なぜ私の点検はこれを「異常なし」と言ったのか**

★`tools/verify-mobile-layout.mjs` が測るのは 5 つの量です。★**そのどれにも出ません**:

| ★測っている量 | ★なぜ出ないか |
|---|---|
| ★横あふれ（`scrollWidth − innerWidth`）| ★切られたのは ★**縦**。★横は 0 |
| ★右端を越えた要素の数 | ★切られた要素は ★**画面の右端を越えていない** |
| ★中身が箱より高い要素 | ★この検出は ★**`overflow: hidden` を持たない要素**を見ています。★節は持っています |
| ★44px 未満の操作要素 | ★切られたボタンは ★**寸法としては 56px のまま** |
| ★キャンバス倍率 | ★無関係 |

⚠️ ★**これは前便が引継ぎ書 §2-1 ① に書いた罠の「縦版」です。**
　★あちらは `html` の `overflow-x: hidden` に横のはみ出しが隠されていました。
　★今回は ★**節自身の `overflow: hidden`** に縦の切り落としが隠されていました。
　★道具は `html / body / main / header` のクリップしか解いていません。

★**教訓（★同じ形で 2 回目）**: ★`overflow: hidden` は ★**壊れを直すのではなく、測定器から隠します。**
　★「はみ出し 0」は ★**「収まっている」と「切り落とした」を区別しません。**
　→ ★見つけたのは ★**オーナーの目**でした。★数字だけ見ていたら、私は「正常」と言い続けていました。

⚠️ ★**道具にこの検出を足すかどうかは、まだ足していません**（★§8-③）。
　★今回は使い捨ての測定器（★スクラッチ領域）で測りました。★`tools/` に置くと
　★分類簿への登録が要り、★それは道具を増やす判断だからです（★R-24）。

---

## ★5. ★直したもの（★`globals.css` の `@media (max-width: 720px)` の中だけ・★5 件）

| # | ★規則 | ★何のため |
|---|---|---|
| ★1 | ★ヒーロー 2 つを `height: auto` ＋ 余白 | ★**切るものを無くす**（★高さで抑えない）|
| ★2 | ★中身を `position: relative` ＋ `top: 0` ＋ `z-index: 1` | ★通常の流れに入れて ★**節に高さを作らせる** |
| ★3 | ★ロゴ 96→54px ／ 56→40px、★見出し 46→22px、★締めの 1 行 26→17px | ★PC の文字寸法は 390px に入りません |
| ★4 | ★CTA の行を `justify-content: center` | ★折り返すと ★**左寄せ**になり、他が中央なので列が崩れて見えます |
| ★5 | ★「はじめの 5 ステップ」の中央列に `flex: 1 1 0` ＋ `min-width: 0` | ★**2 行目だけ 3 段に割れていました**（★説明文が長いため）|

### ★5-1. ⚠️ ★**`position: static` にして 1 度失敗しました**（★数字は「直った」と言いました）

★最初 ★**②を `static`** にしました。★結果:

| | |
|---|---|
| ★測定 | ★**切り落とし 0・はみ出し 0**（★「直った」）|
| ★絵 | ★**ロゴも見出しも CTA も丸ごと消えた** |

★ヒーローの背景（画像と覆い）は ★**絶対配置**で、★中身より**先**に在ります。
★中身を `static` にすると ★**位置指定のある覆いのほうが上に描かれ**、★中身が裏に回ります。

→ ★`relative` にすれば ★**流れに入りつつ覆いより上**に来ます。
⚠️ ★`top: 56px` の inline は `relative` では ★**ずらす指示**になるので `top: 0` で打ち消します。

★**これも「絵と数字を突き合わせる」でしか捕まりませんでした**（★引継ぎ書 §2-1）。

---

## ★6. ★デスクトップが無傷であること

| ★根拠 | |
|---|---|
| ★構造 | ★足した規則は ★**すべて `@media (max-width: 720px)` の中**。★1280px には当たりません |
| ★実測 | ★1280px（★`mobile: false`）の測定が ★**直す前と完全一致**（★7 件・★ロゴの箱 `278x96` も同じ）|
| ★絵 | ★1280px で撮り直し、★ヒーローが従来どおりであることを確認 |

---

## ★7. ★検査 — ⚠️ ★**偽の緑を 1 つ見つけて塞ぎました**

★新しい規則が「黙って効かなくなる」のを防ぐため、★`mobile-css-anchors.test.ts` を
★`height:` / `top:` / `font-size:` にも広げました。★**そこで変異試験が嘘を暴きました**:

| | |
|---|---|
| ★変異 | ★`page.tsx` の ★`height: 560` → `561`（★アンカーを殺す）|
| ★期待 | ★検査が落ちる |
| ★実際 | ★**通ってしまった** |
| ★原因 | ★`apps/web/src/app/camera/page.tsx:25` の ★**`const VP = { width: 1180, height: 560 }`**<br>★（★LP と何の関係もない定数）が ★**身代わり**になっていた |

★検査は全 `.tsx` を 1 本に繋いで探す作りでした。→ ★**別ページの無関係な値でも一致します。**

★**直し**: ★アンカーごとに ★**どのファイルに在るはずか**を `SCOPE` に書き、★そのファイルの中だけで探す形にしました。

| | |
|---|---|
| ★変異あり | ★**落ちる**（★`height:560px: expected 'height:560px' to be ''`）|
| ★変異なし | ★**3 件 PASS** |

⚠️ ★前便も同じ形で焼かれています（★同じ行の `width: 250,` が身代わりになった・★引継ぎ書 §2-3）。
　★**「数字が一致した」は「その数字が目的の場所に在る」を意味しません。**

---

## ★8. ★残っているもの／オーナー判断

| # | | |
|---|---|---|
| ★① | ★**ヒーロー画像の切り抜き** | ★モバイルでは画像の**縦は全部**入り、★下端の HUD の帯（★「実況中」「余力」）も写ります。★馬をもっと大きく見せるなら**切り抜き位置**の指定が要ります＝ ★**意匠の判断** |
| ★② | ★**4 つのたのしみ／登録前に見られる画面の画面写真** | ★1280px の画面を縮めて貼っているので、★モバイルでは**中身は読めません**（★デスクトップでも同じ）。★「画として見せる」ままでよいか |
| ★③ | ★**道具に「縦の切り落とし」の検出を足すか** | ★§4。★足すなら `tools/` ＋ 分類簿への登録（★R-24）。★**今回は足していません** |
| ★④ | ★**`/lp/hero.jpg` の出どころ** | ★中継 HUD の 1 コマ＝自作画で、★権利上の問題は見当たりません。★ただし ★**D-093（公開前の権利確認）の対象**として記録しておきます |

---

## ★9. ★再実行（★期待値つき）

```powershell
cd V:\dev\Cusor\star\apps\web ; npm run dev      # ★ポート 3210（★別の窓）
cd V:\dev\Cusor\star

npm test                                # ★期待: ★135 ファイル / ★1374 件 PASS（★実測: 終了状態 0）
npm run typecheck ; echo "終了状態: $?"   # ★期待: ★0

npx vitest run apps/web/test/mobile-css-anchors.test.ts   # ★3 件
npx tsx tools/verify-mobile-layout.mjs --widths 360,390 --rid <レースid> --hid h1
#   ★期待: ★はみ出し 1 / 46（★残るのは /race の 360px のみ）／ ★縦の重なり 0
```

★**変異試験**（★検査が生きていることの確認・★終わったら必ず戻すこと）:

```powershell
# ★page.tsx:87 の `height: 560` を `561` にして
npx vitest run apps/web/test/mobile-css-anchors.test.ts   # ★落ちるのが正しい
```

---

## ★10. ★作業ツリー

| | |
|---|---|
| ★変更 | ★`apps/web/src/app/globals.css`（★@media の中に 5 件）<br>★`apps/web/test/mobile-css-anchors.test.ts`（★アンカーの種類を拡張＋`SCOPE` を新設）|
| ★**変更していない** | ★`apps/web/src/app/page.tsx`（★`git diff` で差分ゼロを確認）|
| ★既存の差分 | ★`apps/web/package.json`（★ポート 3210・★オーナーの既存作業・★触っていません）|
