# REPORT — P4「2D 馬群描画の限界テスト」

日付: 2026-08-22 ／ 開発側 ／ ブランチ `p4/race-30sec-cuts`
指示書: `DEV_INSTRUCTIONS_P4_2D_LIMIT_TEST_20260822.md`

⚠️ **開発側は PASS/FAIL を判定していません。** 事実と成果物のみを提出します（指示書 §10）。

---

## 1. 変更ファイル一覧

| ファイル | 内容 |
|---|---|
| `tools/render-2d-pack-limit.mjs` | **新規**。V0〜V3 の生成（動画・コンタクトシート・測定） |
| `tools/render-2d-pack-compare.mjs` | **新規**。参考映像と並べる比較シートの生成 |
| `packages/render/src/pack-limit.ts` | **新規**。密集化・位相・完歩長の**純粋関数**（検証専用） |
| `packages/render/test/pack-limit.test.ts` | **新規**。指示書 §9 の自動テスト |
| `packages/render/src/index.ts` | `pack-limit.js` の再輸出を 1 行追加 |
| `tools/lib/classification.mjs` | 新しい道具 2 本を READONLY に登録（R-24 のメタテストが検出） |

★**通常レースの既定値・台本・着順・進行は変更していません。**
★`apps/web/package.json` の未コミット変更（ポート 3210）は**触れていません**（`git status` で保持を確認）。

---

## 2. 実装した検証経路

`tools/render-2d-pack-limit.mjs` が独立した入口です。既存の描画関数
（`resolveBroadcastV2Scene` / `drawBroadcastV2Scene` / `drawPerspectiveHorses`）を**そのまま**呼びます。

密集化は `packages/render/src/pack-limit.ts` の**純粋関数**で行い、
★**入力はその時刻の位置、出力はその時刻の描画用位置**だけです。
`resolveRace` の結果・着順・`laneExtraM` には一切触れていません。

固定条件（指示書 §5）はすべて満たしています。加えて:

- **時間は実時間**（時間圧縮なし）。脚は実物の速さで回ります
- 区間は**向正面の直線**（走路 200〜600m のうち、先頭 300m から 8 秒）。
  コーナーだと真横カメラが回り込み、馬群の見え方以外が混ざるため

---

## 3. §7 の確認事項（コード位置つき）

★既存コメントは転載していません。実際に呼ばれる経路を追った結果です。

| # | 確認事項 | 事実 | コード位置 |
|---|---|---|---|
| 1 | 馬素材の方向数 | **4 方向**（真横 / 斜め前 / 斜め後ろ / 俯瞰）＋勝馬 2 種 | `apps/web/public/art/horse-jockey-{side-v7, diag-front-v3, diag-rear-v5, high-diag-v4}-pose01..08.png` |
| 2 | 各方向のコマ数 | **8 コマ**（全方向） | 同上（`pose01`〜`pose08`） |
| 3 | 方向の切り替え | 注視点の**カメラ相対角**で 1 カットにつき 1 回だけ決める。`<60°` → 斜め後ろ、`>120°` → 斜め前、それ以外 → **真横**。★**俯瞰素材（`high-diag`）はこの分岐に出てこないため、V2 経路では一度も使われません**（読み込まれてはいる） | `broadcast-v2-scene.ts:453-455` |
| 4 | `phaseOf` / `frameOf` の入力 | `((進んだ距離 + visualDelta) ÷ 完歩長 7m) + 馬番 × 0.37` の小数部。`frameOf` は同じ値を 8 倍して切り捨て | `apps/web/src/app/race/page.tsx:1689-1693`、`BROADCAST_STRIDE_M` は `broadcast-v2.ts` |
| 5 | 位相差は維持されるか | **維持されます**（馬番の項は定数）。★ただし**支配的なのは位置の項**で、**7m 離れた 2 頭は位相が完全に一致**します | 同上 |
| 6 | 描画順 | **カメラからの奥行き**で毎コマ決定（遠い順に描く＝手前が上） | `perspective-draw.ts:757` |
| 7 | 接地点と影の基準 | **一致しています**。影は馬と同じ接地投影点 `d.p` を原点に、`bodyAnchorSourcePx` で位置合わせ | `perspective-draw.ts:790-812` |
| 8 | カメラの画角 | `side-low` = **7.6°**（backM 44 / upM 3.5 / sideM 9）。解決後も 7.6°（`measurements.json` で確認） | `broadcast-v2.ts` の `SIDE_LOW` |
| 9 | 馬群を追う基準 | **先頭でも全頭平均でもありません。**「全頭の前後の中点」と「先頭を画面の `leadFraction` に置く点」の**なめらかな max** | `broadcast-v2-scene.ts:233-238`、`broadcastV2LeadFrameFocusMeters` |
| 10 | 被写体ブラーが何を反映しているか | ★**現在は既定で無効**（`MOTION_BLUR_ENABLED = false`）。有効時の速度は**地面に対する実走速度**で、**カメラ相対ではありません** | `broadcast-v2.ts` の `MOTION_BLUR_ENABLED` / `perspective-draw.ts` の `motionBlur` |

---

## 4. V0〜V3 の差（測定値）

`out/2d-pack-limit/measurements.json` より。0 秒時点の値です。

| | 上位8頭の前後 | 全12頭の前後 | 横の広がり | 横の列数 | 位相 | 完歩長の個体差 | ブラー |
|---|---:|---:|---:|---:|---|---|---|
| **V0** 現状 | **13.84m** | 20.20m | 12.72m | 4 | 馬番 × 0.37（等間隔） | なし | なし |
| **V1** 密集 | **6.07m** | 8.75m | 4.36m | 4 | 同上 | なし | なし |
| **V2** 密集＋位相＋遮蔽 | 6.07m | 8.75m | 4.36m | 4 | ハッシュ（不等間隔） | ±4% | なし |
| **V3** 統合処理込み | 6.07m | 8.75m | 4.36m | 4 | 同上 | ±4% | 馬 1.6px / 背景 3.2px |

### ★12 頭が実際に使っているコマの種類（8 コマ中）

★これが今回いちばん重要な測定です。

| 時刻 | V0（現状） | V1（密集だけ） | V2（密集＋位相） |
|---|---:|---:|---:|
| 0s | **7** 種類 | **4** 種類 | **6** 種類 |
| 2s | 6 | **4** | 4 |
| 4s | 6 | **4** | 6 |
| 6s | 5 | **4** | 6 |

★**密集させると、コマの種類が 5〜7 → 4 に減ります。**
位相が**位置から作られている**（§3 の #4/#5）ため、前後に詰めると位相差もいっしょに潰れるからです。
V2 の位相を位置から切り離す処理で 4〜6 に戻りますが、**V0 を明確に上回ってはいません**。

---

## 5. 生成コマンド

```
npx tsx tools/render-2d-pack-limit.mjs      （V0〜V3・約13分）
npx tsx tools/render-2d-pack-compare.mjs    （参考との比較シート）
```

`out/2d-pack-limit/README.txt` に部分再生成の手順も書いています。

---

## 6. テストコマンドと結果

```
npx vitest run packages/render/test/pack-limit.test.ts   → 8 tests passed
npm run typecheck                                        → エラーなし
npm test                                                 → 109 files / 1086 tests passed
```

指示書 §9 の必須 6 項目はすべて `packages/render/test/pack-limit.test.ts` にあります。

★`npm test` は**最初 1 件落ちました**。R-24（道具の分類簿）のメタテストが、
新しい道具 2 本の登録漏れを検出したものです。`tools/lib/classification.mjs` へ登録して解消しました。
**検査が意図どおり働いた例**で、既存不具合ではありません。

---

## 7. 成果物一覧

```
out/2d-pack-limit/
  v0-current.mp4          v1-dense.mp4          v2-pack.mp4          v3-integrated.mp4
  v0-contact-sheet.png    v1-contact-sheet.png  v2-contact-sheet.png v3-contact-sheet.png
  compare-contact-sheet.png
  reference-contact-sheet.png
  measurements.json
  README.txt
```

参考映像の切り出しは **50.0 / 52.0 / 54.0 / 56.0 / 58.0 秒**（調整なし）。
比較シートの列は **0 / 2 / 4 / 6 / 7.9 秒**です。
★最後が 8.0 でなく **7.9** なのは、30fps・240 コマの最終コマが 7.967 秒で、
**8.0 秒ちょうどのコマが存在しない**ためです（8.0 を指定すると抽出が黙って失敗しました）。

---

## 8. 観察できた事実

★ここは**測定と、画像に写っていること**だけです。解釈は §9 に分けています。

1. **密集化は効きます。** V0 は馬が縦に並んだ列に見え、V1 以降は重なり合った塊になります（`compare-contact-sheet.png` の 2 行目と 3 行目）。
2. **密集化はコマの種類を減らします。** 5〜7 → 4（§4 の表）。位相が位置から作られているためです。
3. **V2 の位相処理はコマの種類を 4〜6 に戻しますが、V0（5〜7）を上回りません。**
4. **12 頭の見た目の差は「毛色 5 種類」だけです。** 首・胴・脚・騎手の姿勢は**同じ 8 枚**を共有しており、位相だけが違います。
5. **12 頭の大きさがほぼ同じです。** 横の広がり 4.36m に対しカメラまで 45m なので、奥行き差による大きさの違いは **±5% 程度**です。
6. **参考（56.0s）は 3〜4 頭が重なる寄りの画で、馬体は画面高の 35〜40%、鞍の番号（6・9）が読めます。** 逆光で馬体は暗く落ち、脚と地面に強い横流れがあります。
7. **我々の 4 バリアントはいずれも順光で、馬体が背景より明るく出ています。**
8. **V3 の統合処理は輪郭と背景を柔らかくしますが、馬体の判別は保てています**（`v3-contact-sheet.png`）。
9. **描画順は奥行きで決まっており、手前の馬が奥の馬を隠しています**（§3 #6・テスト⑤で機械的にも確認）。
10. ★**読みやすさのための横ずらし・ラベル回避の処理は、コード上に存在しませんでした**（探した結果として）。

### ⚠️ この検証の限界（判定の前に読んでください）

- ★**勝負服（枠色）をこの道具では焼いていません。** 画面（`/race`）では枠色が付きます。
  したがって**このテストの絵は、実画面より「12 頭が同じ馬に見えやすい」側に振れています**。
  毛色（5 種類）は画面と同じ規則で焼いています。
- V3 の馬層は「全体 − 背景」の差分でマスクを作っています。**厳密なアルファではありません。**
- 参考は実機をスマートフォンで撮った映像です。**色・鮮鋭度・台形歪みは比較材料にしていません。**

---

## 9. 開発側の推測（★事実ではありません）

1. 「切り抜きを並べた印象」の主因は**密度ではなく、12 頭が同じ 8 枚を共有していること**だと考えます。V1 で密度を参考と同等（6m）にしても、印象が大きく変わったようには見えないためです。
2. 位相をいくら散らしても、**取り得る姿勢は 8 通りしかありません**。12 頭いれば必ず同じ姿勢の馬が複数同時に存在します。
3. 参考の馬は**首の角度・体の傾き・騎手の姿勢**が馬ごとに違って見えます。これは位相ではなく**別々の造形**で、2D で再現するにはコマ数ではなく**個体数ぶんの素材**が要ります。
4. 逆光で馬体を暗く落とすと、細部の差が問題になりにくくなります。参考が成立している一因かもしれません（未検証）。

---

## 10. 現行 2D を続ける場合の追加素材の概算

現状: **4 方向 × 8 コマ = 32 枚**（全馬で共有、毛色 5 種を焼き分け）。

| 目的 | 追加が要るもの | 枚数の目安 |
|---|---|---|
| 旋回で向きが破綻しないようにする | 方向を 4 → **8〜12 方向** | +32〜64 枚 |
| 脚の刻みを細かくする | 8 → **12〜16 コマ** | 上記の 1.5〜2 倍 |
| ★**個体差を出す**（§9 の推測が当たっている場合） | **体型・首の角度違いを 3〜4 種** | 上記の **3〜4 倍** |

★**合計の幅は 64 枚（方向だけ）〜 768 枚（方向 12 × 16 コマ × 個体 4 種）**です。
生成・整列・鞍布基準の合わせ込みが 1 枚ごとに要るため、**枚数がそのまま工数に効きます**。
どこまで要るかは、レビュー側が V1〜V3 を見て「密度と位相で足りるか／個体差が要るか」を
判断してからでなければ、こちらでは絞れません。

---

## 11. 3D へ移行する場合

### そのまま使えるもの

| 層 | 具体 |
|---|---|
| レース | `@star/race-engine` 一式（着順・公正性・介入）。**描画とは無関係** |
| コース幾何 | `course.ts`（`posOf` / 区間 / 曲率） |
| カメラ | `perspective.ts`（投影）・`broadcastCamera`・固定カメラ・自動ズーム |
| 演出 | `broadcast-v2.ts` の台本・カット選択・尺（`time-warp.ts`）・`visual-scroll.ts` |
| HUD | `hud-kit` / `oblique-ui` / `reference-hud` / `minimap` / `entry-board` / 実況 |
| 世界の配置 | `infield.ts` / `distance-poles.ts` / `finish-post.ts` / `mow-stripes.ts` / `crowd.ts`（**配置の規則**は流用可） |

### 置き換えるもの

| 層 | 具体 |
|---|---|
| 馬の描画 | `perspective-draw.ts` の `drawPerspectiveHorses`（スプライト貼り） |
| 素材 | 方向別 8 コマ一式・`coat.ts` の焼き込み・勝負服オーバーレイ・`silks-skin.ts` |
| 方向選択 | `broadcast-v2-scene.ts` の `frameSetOf`（3D なら不要） |
| 影 | シルエットを潰す方式（3D なら実影） |

★**レースの正しさ（着順・公正性）には一切影響しません。** 置き換えは描画層に閉じています。

---

## 12. スコープ外で見つけた問題（実装していません）

1. **俯瞰素材（`high-diag-v4`・8 枚）が読み込まれているのに一度も使われていません**（§3 #3）。
   起動時の読み込みが無駄になっています。
2. `tools/render-2d-pack-limit.mjs` は `tools/shot-race-at.mjs` の**素材読み込みを写して**います。
   R-30 の観点では離れる余地があります（今回は 1 回限りの検証なので許容しました）。
3. `tools/_shotsize.mjs` が**カメラ定義の写しを持って**おり、実装と離れています
   （`front-close` が 20°、実装は 16° など）。分類簿にも載っていません。

---

## 13. 提出物の確認

`out/2d-pack-limit/` に指示書 §8 の 11 ファイルがすべて存在することを確認しました
（`v3-integrated.mp4` 15.0MB ／ `compare-contact-sheet.png` ほか）。
★報告だけで完了とせず、生成物の存在と中身（コンタクトシート）を目視しています。
