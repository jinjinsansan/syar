# 報告 — (b′) を既定にしました（残件 A-2 を閉じます）

作成: 2026-08-29 / ブランチ `p4/race-30sec-cuts`
受けた裁定: `REVIEW_P4_SEAM_BPRIME_VERDICT_20260829.md` / 正典 **D-089**
前の報告: `REPORT_P4_SEAM_BPRIME_20260829.md`

> ★`Date.now()` / `Math.random()` を使っていません。
> ★**着順・タイム・着差・払戻・馬の位置に触れていません**（憲法 3/4）。動かしたのは**カメラの向け先だけ**です。
> ★提出物を追跡に入れてから `npm test` を回しました。
> ⚠️ ★`apps/web/package.json`（ポート 3210）はオーナーの既存作業。★コミットに混ぜていません。

---

## 0. この便の要点

| # | 内容 |
|---|---|
| 1 | ★**(b′) を既定にしました**（`LANE_ALIGNED_FOCUS_DEFAULT = true`） |
| 2 | ★**戻し口を置きました** — `/race?laneFocus=off`（D-089 の条件 1） |
| 3 | ★**戻せること自体を検査で固定**（8 件）。★「口を置いた」だけにしていません |
| 4 | ★**既定 OFF を前提にした検査・道具は 0 件**でした（走査済み・§4） |
| 5 | ★**残件台帳 A-2 を消しました**（「受け入れた」ではなく **直した**） |

★**D-089 が要求した 3 つの条件は、すべて同じ便で片付けています。**

---

## 1. 何を変えたか（★製品の実質差分）

### 1-1. 既定の切り替え（`packages/render/src/broadcast-v2.ts`）

```diff
- export const LANE_ALIGNED_FOCUS_DEFAULT = false;
+ export const LANE_ALIGNED_FOCUS_DEFAULT = true;
```

★併せて**註記を実装に合わせました**。★「いまは `false` です」のまま残すと、
★**コメントが実装より古い**状態になります（この案件が繰り返し踏んでいる形・R-25 の家族）。

### 1-2. 戻し口（★D-089 の条件 1）

```ts
/** ★`/race?laneFocus=off` で切る。★`?cinematography=v4` などと同じ作法 */
export function laneAlignedFocusFromSearch(search: string): boolean {
  const v = new URLSearchParams(search).get('laneFocus');
  if (v === 'off') return false;
  if (v === 'on') return true;
  return LANE_ALIGNED_FOCUS_DEFAULT;
}
```

★画面（`race/page.tsx`）の **`resolveBroadcastV2Scene` を呼ぶ 3 か所すべて**に通しました。

⚠️ ★**`off` / `on` 以外はすべて既定へ落ちます**（R-27: 縮退は狭い側・安全な側へ）。
　★綴りを間違えたときに「**切ったつもりで切れていない**」より、
　★「**切れなかった**」ほうが画で気づけます。

### 1-3. ★変えていないもの

★`focusS` / `focusW` は **ON/OFF で 1 ビットも変わりません**（D-089 の条文どおり）。
★動かすのは `cameraAt()` に渡す値だけです。★検査で固定しました。

---

## 2. ★「口を置いた」だけにしない（D-089 の条件 2）

★裁定 §3 の求めるもの 2:

> ★**戻せること自体を検査で固定する**（★「口を置いた」だけでは前回と同じ穴です）

`packages/render/test/lane-aligned-focus.test.ts`（★8 件）:

| # | 検査 | なぜ要るか |
|---|---|---|
| 1 | 既定が `true` | ★切り替えたこと自体を固定 |
| 2 | `?laneFocus=off` で切れる | |
| 3 | `?laneFocus=on` で明示できる | |
| 4 | ★**省略・綴り違い・空はすべて既定へ落ちる** | R-27。★7 通りの入力で確認 |
| 5 | ★**切ると、カメラの向け先が実際に変わる** | ⚠️ ★**URL の解釈だけを見ると、呼び出し側が渡し忘れていても緑になります**（R-31 の形） |
| 6 | ★渡さなければ既定と同じ画になる | |
| 7 | ★`focusS` / `focusW` は ON/OFF で変わらない | D-089 の条文（動かすのはカメラだけ） |
| 8 | ★**直線では ON/OFF で差が出ない** | ⚠️ ★ここで差が出るなら実装が誤り（幾何どおりでない） |

⚠️ ★**#5 が本体です。** ★#2〜#4 は「URL を読めているか」しか見ておらず、
　★**画面が渡し忘れていても通ります**。★実際にカメラが動くことまで見て、初めて「戻せる」と言えます。

⚠️ ★**#8 も必要です。** ★コーナーで比べないと差が 0 なので、
　★**検査が何も守らない状態**になりえます（R-3 / R-21）。★試験馬はコーナーの中に置いています。

---

## 3. 出荷状態の実測（★製品経路・既定 ON）

| | (b′) OFF | ★**出荷（ON）** |
|---|---|---|
| ★走路の折れ目の跳び（全 7 距離 × 折れ目 × 走線 120 通り） | 最大 **15.7px** | ★**0.0px** |
| カットの境目の跳び（中央 / 最大・4 seed） | 303 / 994px | ★**303 / 982px** |
| ★走行方向が反転した境目 | 4（入口のみ） | ★**4（入口のみ）** |
| ★共通の馬が 0 頭の境目 | 0 箇所 | ★**0 箇所** |
| ★先頭が画面の外に出るコマ | 0 | ★**0** |
| 注視点のずれ（(b′) − 現行） | — | 最大 **0.25m**（直線は 0.00） |

★**折れ目の跳びが消え、他は動いていません。**

⚠️ ★入口の反転（A-1）は**この件とは別**で、★**残っています**（台帳 A-1 のまま）。

---

## 4. ★既定 OFF を前提にした検査・道具の走査（D-089 の条件 2-3）

★裁定 §4 の求めるもの 3:

> ★**既定 OFF を前提に書かれた検査・道具が無いか**を一度走査する
> （D-085 の系: 既定が変わると、直書きした検査は別のものを測り始める）

### 4-1. 結果 — ★0 件

| 走査 | 結果 |
|---|---|
| `laneAlignedFocus` / `laneFocus` の直書き | ★**0 件**（型定義と、既定から引く 1 行のみ） |
| `resolveBroadcastV2Scene` を直接呼ぶ道具 | ★**20 本以上あるが、全部が自動で新しい既定に乗ります** |
| `npm test` の全数 | ★**1237 件 PASS**。★旧挙動に固定された検査は 1 件も無し |

### 4-2. ★なぜ道具が自動で乗るのか（★構造で満たしています）

```ts
// broadcast-v2-scene.ts
const laneAligned = options.laneAlignedFocus ?? LANE_ALIGNED_FOCUS_DEFAULT;
```

★既定は **`resolveBroadcastV2Scene` の中**にあります。
→ ★**渡さなければ画面と同じ側へ落ちます**（R-31）。★道具ごとに書き足す必要がありません。

⚠️ ★これは **R-31 が言う「渡さなければ画面と違う側に落ちる形を作らない」** を、
　★**手順ではなく構造で**満たした形です。★9 件の再発は、既定が呼ぶ側にあったために起きていました。

---

## 5. ★残件台帳 A-2 を消しました

★台帳の使い方 1:「★直したら**この台帳から消し**、報告書に移す」。

| | |
|---|---|
| 状態 | ★**直しました**（「受け入れた」ではありません） |
| 実測 | 全 7 距離 × 折れ目 × 走線 **120 通りで 15.7px → 0.0px** |
| 出どころ | 正典 **D-089** / 本書 |

⚠️ ★**番号は詰めていません**（A-1 / A-3 / A-4 のまま）。
　★**過去の便が参照している番号が動くほうが害が大きい**ためです。★台帳の冒頭に理由を書きました。

★併せて台帳の古い記述を直しました:
最終更新日（8-28 → 8-29）／ C-2 の未 push 数（49 → **138**）／ C-3（ダートは**着手済み**）。

---

## 6. 検証

```
$ npm test
 Test Files  126 passed (126)
      Tests  1237 passed (1237)
npm test EXIT=0

$ npm run typecheck
typecheck EXIT=0
```

★素材を足していないので `ASSET_VERSION` は据え置き（57）です。

---

## 7. ★独立に再実行して突き合わせてください

| # | コマンド | ★出るはずの数字 |
|---|---|---|
| 1 | `npx vitest run packages/render/test/lane-aligned-focus.test.ts` | ★**8 件 PASS** |
| 2 | `npx tsx tools/_seamslip.mjs` | ★【4】120 通り／現行 **15.7px** ／ ★**(b′) 0.0px** |
| 3 | `npx tsx tools/audit-cut-seam.mjs --seeds 42,253,90,2 --script v6` | ★反転 **4 箇所（全て入口）**／共通 0 頭 **0 箇所**／中央 **303px**・最大 **982px** |
| 4 | `npm test` | ★**1237 件 PASS・EXIT=0** |

### 実画面（★比べられます）

```powershell
cd apps/web ; npm run dev
# ★出荷（(b′) 入り）
http://localhost:3210/race?seed=42&cinematography=v6&contest=1.6
# ★切って比べる
http://localhost:3210/race?seed=42&cinematography=v6&contest=1.6&laneFocus=off
```

---

## 8. ★判定していただきたいこと

| # | 論点 | こちらの立場 |
|---|---|---|
| ① | ★D-089 の条件 3 つを満たしているか | ★戻し口・戻せることの検査・往復の検査、すべて同じ便で置きました |
| ② | ★戻し口の名前（`laneFocus`） | ★裁定は「名前は開発側の裁量」。★既存の `cinematography` / `climax` / `infield` / `surface` と同じ作法 |
| ③ | ★台帳 A-2 を消したこと | ★「直した」として消し、番号は詰めていません |
| ④ | ★実画面の見え方 | ★**[EYES]・オーナー**（`?laneFocus=off` と並べて比べられます） |

---

## ★9. 申し送り

1. ★**既定を切り替えたら、その定数の註記も同じ便で直す。**（§1-1）
   ★「いまは `false` です」が残っていました。★**コメントが実装より古いのは事故のもと**です
2. ★**戻し口の検査は「URL を読めたか」で終わらせない。**（§2）
   ★呼び出し側が渡し忘れていても、URL の解釈だけなら緑になります。
   ★**実際に画が変わること**まで見て、初めて「戻せる」と言えます
3. ★**差が出ない条件で検査を書かない。**（§2 #8）
   ★直線で比べると (b′) の差は 0 です。★試験馬はコーナーに置く必要がありました
4. ★**既定は「呼ばれる側」に置く。**（§4-2）
   ★呼ぶ側に置くと、道具ごとに書き足すことになり、★**書き忘れが画面と違う側へ落ちます**。
   ★R-31 の再発 9 件は、すべてこの形でした
