# 【STAR】裁定: 旧世代の画面の始末 ／ 🔴 私の §3 の指示が事故を招いた ／ 下見画面 18 枚 — 2026-09-25

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa` の報告（`/training` → `/train` の転送で**調教の指示を出す口を消しかけた**・`screen-generations` の 52 枚の始末・下見画面 18 枚）

## 1. 🔴 まず私の誤り

★裁定 `fc3f872` §3 で私はこう書いた — 「**新版が在るもの（`/training`・`/prizes`）は、まず新版へ送る**」。
- ✔ 事実: `/train` は **見た目だけ**（`rpc(` も Repo も呼ばない）。★`/training` が **`rpc('set_training_order')` を呼ぶ唯一の画面**
- → ★**私の指示どおりに送ると、調教の指示を出す口が消え、育成のループが止まる**
- 🔴 ★**私は「新版が在る」を、名前と存在だけで判断した**。★**繋がっているかを確かめなかった**。★これは私自身が 09-24 に作法として書いた「**一覧・台本・目録で『使っている／いない』を決めない。読む側の原文を引く**」の**そのままの違反**
- ★開発側が**自分で気づいて戻した**。★気づかせたのは ★**消した画面を見張っていた検査 3 本が「落ちる」ではなく「読めない」で止まった**こと。★**「読めない」で止まったら、消した物の役割を疑う**を作法に足す

## 2. ✅ 入った網（★私の指示より強い）
- ✔ `screen-generations.test.ts` の「★**転送の送り先が、サーバーを呼んでいる**（見た目だけの画面へ送らない）」。★対照で、転送を入れ直すと落ちることを確かめた
- → ★**私の「新版へ送る」という指示は、この網が在って初めて安全に出せる**。★**指示を機械に翻訳したのは開発側**

## 3. ✅ 52 枚の始末（`screen-generations.ts`）
★1 枚ずつ記録（new 15 / keep-as-is 2 / redirected 1 / rebuild 15 / dev-only 19）。★網 9 本（簿と実物が 1 対 1・旧世代は 37 枚以下・`redirected` はファイルが無く転送が在る・送り先が繋がっている）→ ★**「要らない」と「要るが未着手」を混ぜない**（D-122 ③）を満たす。

### ★同じ便で出た 4 つの波及（★記録として重い）
①リンクを付け替えて `/training` をどこからも辿れなくした（`screen-reachable` が拾った）②釘の数を引き算で 2 度 間違えた（★**数は数えて入れる。引き算で入れない**）③消した `/prizes` が唯一の使い手だった CSS 規則が 2 つ死んだ（`mobile-css-anchors` が拾った）④見張りを `/exchange` に向けたら**変数名の違いだけで 3 件 落ちた** → ★**形で見るよう直した**（★性質を見る網が、書き方を見ていた）
→ ★**1 枚 消すと 3 方向に波及する**。★`rebuild` の 15 枚も、★**1 枚ずつ・網を先に**進めること

## 4. 🔴 下見画面 18 枚が本番で護りなしに開く — ★オーナー判断
- ✔ `/art-lab` `/camera` `/gait-review` `/rig-lab/*` `/still` `/race-quality-lab` `/race-world-lab` `/lp-preview` `/lp-arcade` `/design-check` ほか。★護りが在るのは `/design-preview/odds` だけ（`NODE_ENV !== 'development'`）
- ★**オーナーの「白い古いデザインが残っている」の経路として最有力**（★09-20 の報告にも「開発用ページ 7 件が公開で 200・noindex もログインの壁も無い」と在る。★**5 日 経って増えている**）
- ★開発側が**塞がずにオーナーに聞いた**のは正しい（★オーナーが素材を見る経路でもあるため）

**レビュー側の案（オーナーが選ぶ）**:
| 案 | 中身 | 向き |
|---|---|---|
| **A（推奨）** | ★**既定で全部 護る**（`NODE_ENV !== 'production'` でだけ開く）。★**ただしオーナーが見るものだけ残す**（★いま実際に使っているのは `/design-check`）。残す分は `noindex` を付ける | ★いちばん簡単・戻せる |
| B | Vercel の Deployment Protection で丸ごと囲う（★09-20 の「オーナー待ち 4 件」の 1 つ。★bypass を決めてから入れる） | 画面を変えずに済むが、★**bypass を決めないと `DP-1` が死ぬ** |
| C | いまのまま（護らない） | ★公開の前には採れない |

★**件数は釘付け済み**なので、★**新しい下見画面が護りなしで増えることはもう無い**（★開発側の手当て）。

## 5. 追記（2026-09-25）— 作り直す前の釘 ✅ と、新しい発見 2 件

### ✅ 受理
- ✔ `screen-deps.ts` が **相対 import を 2 段 辿る**（★`/entry` の実体は `lib/entry-screen.ts`・`/records` は `records-view.tsx` に渡すだけ）。★**`page.tsx` だけ見ると分からない**＝ 今日の誤報（`entry-repo.ts` を直して「直した」）の原因そのもの。★**浅さの限界（共有の部品は辿らない）も明記**している
- ✔ `screen-rebuild-pins.ts` が rebuild 15 枚の **いま呼んでいる RPC と lib を釘付け**。★**実測の出力から写した**（★手で数えず・引き算もせず。★今日 2 度 間違えた反省が形になっている）
- ✔ 網 3 本追加（計 12 本）・測り方は 1 か所（道具も同じ関数を読む）
- ✔ ★**対照**: `set_training_order` の呼び出しを壊すと「★**唯一の画面**。落とすと育成のループが止まります」と出る → ★**今日の転送は押す前に止まった**

### 🔴 新しい発見 2 件
1. ★**`/stable/retired` は見本のデータを読んでいる**（`horse-story-demo`）。★本物に繋がっているのは `/stable/roles`。→ ★**引退馬の一覧は繋がっていない**ので、★**綺麗にしても中身は空**。★釘に「作り直しの前に本物へ繋ぐ」と書いたのは正しい
   - ⚠️ ★**デザイナーの第 2 便で「生涯の記録の色」を見るのはこの画面**。★**見本のデータを見て色を決めることになる**（★色を決めるには足りるが、★**中身が本物だと思わせない**こと）
2. `/stable/market` も見本だけ（既知・D-122 ④）。★「見本だけ」の件数を釘付けした

### ⚠️ オーナーの手が要るもの（2 つ・レビュー側から伝える）
- ★**デザイナーへの依頼（DesignSync）は開発側から通せない**（`/design-login` の認可）。★オーナーの操作が要る。★依頼の中身は釘の表がそのまま使える
- ★下見画面 18 枚: ★A（既定で護り `/design-check` だけ残す）を推すが、★**本番でしか見えない素材を確かめる手段**（`/still`・`/gait-review`）を残すかは**オーナーの使い方次第**

## 6. 追記（2026-09-25）— 手順を 5 組に流した結果 ✅

- ✔ `/races`→`/vote` 転送・`/races/[id]/odds`→`/odds/[id]` 転送・`/odds`（一覧）と `/odds/[id]` は**役割が違うので両方 残す**（★1 行で書けた）・`/races/[id]` は**新版が無いので残して作り直す**
- ✔ ★**手順どおり簿に書いてから着手**した。★釘を 37 → **35 に下げた**（★下げないと次から緩む）
- ✔ ★**気づき**: 新世代の `race-strip` が `/races/${id}` を指している ＝ ★**新しい画面から 1 歩で旧い画面に落ちる** → ★`/races/[id]` の作り直しの優先度が高い、と簿に書いたのは正しい
- ✔ 網の誤りを 3 つ自分で直した。★①転送の `source` を `[id]` で探していた（Next は `:id`）→ ★**動的な転送を入れた瞬間に「転送が無い」と嘘をつく形**②送り先の判定で階層を決め打ち → ★測り方を `screen-deps.ts` の 1 か所に寄せた ③註記の中で JSDoc を閉じてしまった
  - ★①②は「★**網が自分の書き方に依存していた**」形。★**測り方を 1 か所に寄せる**のが効いた（★この案件で繰り返し効いている手）
- ✔ ★「画面を 1 枚消すと 4 方向に波及する」を簿の見出しに（リンク／見張りが"読み込めない"で止まる／その画面だけの CSS／生成物の types）。★生成物の types は **2 度**踏んだ（`next build` は通るのに型検査だけ落ちる）

### 🔴 `/races/[id]/bet` — ★オーナーへ（レビュー側の推し）
- ✔ 事実: 売っているレースは**常に 1 本**（サイクル 6 分・D-007）。★「今以外のレースに買う」は起きない
- ★**開発側の推し（`/vote` に寄せて捨てる）に同意**。★理由を 1 つ足す: ★**投票の上限の見せ方（§9.4）を 2 か所に持つと、片方だけ直る**（★今日 何度も出た形）
- ★ただし「**先のレースを眺めて、発売を待って買う**」遊び方を想定しているなら、★**`/races/[id]`（出走表）は残す**ので、★そこから「発売が始まったら `/vote` へ」と送る形でよい（★買う口を 2 つにしない）
- → ★**オーナーの一言待ち**。★それまで `/races/[id]/bet` は触らない
