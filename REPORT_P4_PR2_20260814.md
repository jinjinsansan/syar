# REPORT_P4_PR2_20260814 — PR2（カメラ）完了／D-065 手順1（距離ロスの大きさ）を実測

## 本便で正典の改訂が要る決定をしたか

**していません。**

---

## 1. D-065 手順1 — ★実装前の見積もり（裁定のご指示どおり）

```
内ラチから  余計に走る            距離に対する割合
     2m    −33.5m（−14.0馬身）    −2.09%
    17m    +29.3m（+12.2馬身）    +1.83%
★内外差 62.8m ＝ 26.2馬身 ＝ 距離の 3.93%
乱数の幅 RACE_RANDOM_K = 0.22 ＝ 22%
```

★**レビュー側の見積もり（3.9% / 22%）と一致しました。**

### 着順への影響（300レース・スコアを距離割合ぶん目減りさせる当て木）

| 目減り | 1頭あたりの着順変動 | 勝ち馬が替わる割合 |
|---|---|---|
| **一律 0〜10%** | **0.00 着** | **0%** | ★対照。**一律なら動かない**（当て木が正しいことの検算） |
| ★**実際の距離ロス相当（×1）** | **0.18 着** | **2%** |
| ×2 | 0.36 着 | 8% |
| ×3 | 0.51 着 | 10% |

★**影響は小さいです。** 勝ち馬が替わるのは **2%**。**V-4 は動かない見込み**で、
レビュー側の「K の再較正までは要らない」という読みを支持します。

### ★ただし、1つ申し添えます

この当て木は**距離ロスをレース全体に均した**ものです。**実際は違います**:

- 距離ロスは**コーナーに集中**します（直線では 0）
- そして★**横位置 w は「選ぶ」もの**です（4角で追込が外へ持ち出す、等）

★**つまり本当の影響は「w をどう決めるか」（Layer B）に依存します。**
2% という数字は**下限の目安**で、「外に出す判断」が入ると**もっと効きます**。
★**w の決め方はエンジンの領域**なので、**設計を出してから実装**すべきだと考えます（Q-P4-29）。

---

## 2. PR2（カメラ）— 完了

`packages/render/src/camera.ts`。**純粋関数・依存ゼロ・着順に触れません。**

### ★D-058 との衝突を先に処理しました

仕様書は連続ズーム、正典 D-058 は「表示は整数倍のみ」。**分けました**:

```
zoom（px/m）  … ★連続。走路の縮尺。**位置の変換にだけ使う**
spriteScale   … ★整数のみ（1 or 2）。**スプライトの拡大に使う**
```

★補間のときも `spriteScale` は**切り替え**ます（検査で固定）。

### 実装

| | |
|---|---|
| `worldToScreen` | ①注視点を原点に ②★**進行方向を画面の右向きに回す** ③縦を `tilt` で圧縮 ④横を望遠圧縮 |
| `cutsFor(course)` | 発走→隊列形成→向正面→3角→4角→直線→追い比べ |
| `blendCamera` | 切り替え／補間 |
| `focusOf` | 馬群の重心・先頭・自馬・**叩き合い**・ゲート |

### 検査19件

- ★注視点は必ず画面の中央
- ★**コーナーでも走路が横に流れる**（縦や斜めに流れない）
- tilt=0 で潰れ、tilt=1 で真俯瞰
- ★**スプライトの拡大率を補間しない**（D-058）
- ★**叩き合いに該当馬が無ければ先頭に落とす**（画面が空にならない）
- ★**カメラをどう変えてもコース幾何の出力は1ビットも変わらない**

★**デバッグ画面**: `http://localhost:3100/camera`

### ★また同じ間違いをしました（2度目）

カットの切り替え位置を**手で書いて**いたので、**カットの名前と実際の区間がずれました**
（「向正面」のカットで実際は 3角）。★**俯瞰図のラベルでも同じ間違いをしています。**

→ **区間の境目はコースが知っているので、読むように**しました。
さらに**存在しない区間のカットが残る**不具合（1000m に向正面は無い）も検査が捕まえました。

---

## 3. Q-P4-28 — 係数を2つに分けます（裁定どおり）

> つまみが1つで目標が2つです。**係数を2つに分けてください。**

★**次便で実装します。** 「隊列の広がり（位置の散らばり）」と「速度の振れ（時間の変動）」を
別の係数にし、**両方を同時に満たせることを実測で示します。**

---

## 4. 🔴 照会

| # | 内容 |
|---|---|
| 🔴 **Q-P4-29（新規）** | ★**横位置 w の決め方**（脚質ごとの目標 w／詰まったら外へ／4角で外に出す判断）。**これが決まらないと距離ロスの本当の影響が測れません** |
| **Q-P4-21 / 22 / 20 / 25 / 26** | 未裁定のまま |

★**D-062（時間配分）と D-063（揺らぎ）の見直し**は、**距離ロスを入れてから V-16 を測り直して**判断します（裁定のご指示どおり「無くせるなら無くす」方向で）。

---

**テスト 763件緑 / `tsc --noEmit` 通過**
