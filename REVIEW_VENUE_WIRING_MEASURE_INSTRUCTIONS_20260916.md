# 指示書 — 10 場配線の残りの測定（VC-1）と、条件 VC-2〜VC-4 の片付け

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側
**根拠**: 中間裁定 `REVIEW_VENUE_WIRING_VERDICT_20260915.md` §2（条件 VC-1〜VC-5）／ 指示書 `DEV_INSTRUCTIONS_VENUE_WIRING_20260915.md` §7（VW-5）・§2（VW-0）／ 裁定 `REVIEW_ENGINE_LANE_SPEED_ES6_VERDICT_20260916.md`（ES 便は合格）／ 報告 `REPORT_STAGING_DEMO_20260916.md`（VW-6 の 3〜7 は完了）
**前提**: ブランチ `p4/race-30sec-cuts`・HEAD `37aa0a6`（origin より 18 先行・push は未実施）

> 凡例（R-13）: 報告では「**再現した事実**」と「**未検証の主張**」を分けてください。
> ⚠️ **本番 DB への接続・移行の適用・ワーカーの配備はしません。** 本便は測定と、報告・依存宣言・検査の片付けだけです。
> ⚠️ **帯を外れても較正定数を動かさないでください**（指示書 §7-2 と同じ）。値を報告し、照会に回してください。

---

## 0. なぜ今できるか

VC-1 が止まっていたのはエンジンが遅かったからで、**ES 便（ES-0〜ES-6）で解消しました**（試行 1 回の比 1.28 倍）。以前は数時間〜数十時間の見積りだった測定が、**V-10 以外は分の単位**になった見込みです（△ 見積りはレビュー側の計算。実測で置き換えてください）。

**VW-6（staging の実演）は完了**として受理しました。残りは下の 4 つです。

---

## 1. VC-1 — ゲートの測定

### 順番と見積り

| # | 測定 | コマンド | レビュー側の見積り（△） |
|---|---|---|---|
| **M-1** | **V-18 の本番の 70 組** | `npx tsx tools/verify-v18.mjs`（既定＝本番の条件） | 数分 |
| **M-2** | **V-4・V-5・V-6** | `npm run verify:race` と `npm run verify:race -- --legacy-conditions` | 合わせて 20〜30 分 |
| **M-3** | **VW-0 の大きさ** | 指示書 `DEV_INSTRUCTIONS_VENUE_WIRING_20260915.md` §2-2 の手順 | 数分 |
| **M-4** | **V-10** | `npm run verify:payout -- --races 500 --seeds 42` | **数時間**（オーナーの別のターミナル） |

★**M-1 → M-2 → M-3 を先に**出してください。**本番への `0023` とワーカーの配備の可否は、この 3 つで判断します**（§7）。M-4 はその後で構いません。

### M-1 V-18（70 組）

- 帯（正典 §13.2・D-090）: **① 枠順と着順の順位相関 |ρ| ≤ 0.10** ／ **②a 最内と最外の走行距離差 4〜12 馬身** ／ **②b 枠順由来の内外差 ≤ 1 馬身**
- **月見丘・白砂を必ず含める**（`venues.ts` の註記「天井まで 0.8〜1.1 馬身」）。前回は 23 組で中断し、この 2 場が未計測でした
- 報告は**場 × 距離の表**で、①・②a・②b を並べる
- ②b は `gateBiasV18`。組の数が多いので、②b を全 70 組で回すか場の軸で選ぶかは、所要を測ってから決めて註記してください（R-33）
- 測定条件（頭数・レース数）は道具の既定のままにし、**報告に明記**（R-12: 測定の自由変数は固定して書く）

### M-2 V-4・V-5・V-6

- 帯: **V-4 1 番人気の勝率 30〜34%** ／ **V-5 1 番人気の複勝率 60〜65%** ／ **V-6 下位 3 ランクの平均勝率 0.5〜2%**
- ★**本番の条件と旧来の条件（`--legacy-conditions`）の両方**を出し、**変わった量**を並べる（指示書 §7-2）
- **V-6 は出走頭数別にも分解**して報告（§13.2 の註記。プール平均は大頭数で裾が死んでいることを隠す）
- 変わった量の読み: 10 場になって走路の形が場ごとに変わるので、**距離ロスの分布が変わり、V-4〜V-6 が動きえます**。動いた場合も定数は触らず、値を報告してください

### M-3 VW-0 の大きさ

- 指示書 §2-2: staging の確定済みの 1400m 以下のレースから数本、**凍結された出走馬（`entrant_snapshot`）で、`courseShape` を `'oval'` と `'straight'` の 2 通り**に振ってモンテカルロを回す
- 出すもの: **券種ごとの 1 着確率の差の平均と最大** ／ **「直線でオッズを付けて楕円で確定した」ときの払戻率**（切り捨て前・D-094）
- これは**直す前に起きていた食い違いの大きさ**の記録です（AF-0 と同じ形）。staging の馬券は 0 枚なので、実害の算定ではありません

### M-4 V-10

- `npm run verify:payout -- --races 500 --seeds 42`（**オーナーの別のターミナル**・数時間）
- 判定: **切り捨て前の払戻率で 設定 margin ±1%**（D-094）。**出走表間 SD と プール SE ≤ 0.25pt** を報告（D-036・§13.2 の SE の式）
- 切り捨て後の払戻率と、切り捨てで減った分を**券種ごとに並べる**
- ★**この 1 本で、監査 第 1 便の AF-2 と 第 2 便の BF-6 も閉じます**
- 売らない目（D-035 の下限・D-096 の 1.0 倍未満）を**賭け金から外していること**を確かめてください（D-096 の註記）

---

## 2. VC-2 — CI の V-18 の対照の変異

`apps/cli/test/v18-gate.test.ts` の対照（「凍結を読めば場ごとに ②a が違い、凍結を渡さない値とも違う」）について、**壊すと落ちること**を実演してください。

- 変異の例: `measureV18` に `frozen` を渡さない形に戻す（＝`DEFAULT_OVAL` に落ちる）
- 期待: 対照が落ちる（白砂と大河原の差が 0 になる）
- 戻して合格を確かめ、`git diff` に変異が残っていないことを確認
- ⚠️ 初版が `expect(at()).toBe(at())`（同じ値を自分と比べる）だったので、**この実演が無いと対照が効いている根拠がありません**（R-16）

## 3. VC-3 — `apps/cli` の依存宣言

`apps/cli/package.json` の `dependencies` に **`@star/scheduler`** を足してください（`verify-race.ts`・`verify-payout.ts`・検査が引いています）。いまはワークスペースのリンクで解決されているだけで、配る物の自己検査（D-043）や依存の整理で黙って外れます。

## 4. VC-4 — 報告の穴を埋める

`REPORT_VENUE_WIRING_20260915.md` を次のとおり直してください（追記で構いません）。

| # | 箇所 | 中身 |
|---|---|---|
| 1 | §1-1 の末尾 | `git diff --numstat`（TBD のまま） |
| 2 | §0-2 | VW-0 の大きさ（M-3 の結果） |
| 3 | §6 の冒頭 | 中断の原因。**照会書 §3-1 は「Windows Update の自動再起動」**と書いており、報告の「メモリ不足」と食い違っています。どちらが正しいかを書く |
| 4 | §7 の 3 | 所要の見積り（「1 本 80〜96 秒」は 08-11 の註記で D-071 の前）。**実演の実測（1 本 約 2 秒・`--trials 10000`）**に置き換え、フルの試行回数での見積りも書く |
| 5 | §7 の表 | VW-6 の 3〜7 を**完了**にし、`REPORT_STAGING_DEMO_20260916.md` を参照 |
| 6 | §6-1・§9 の 7 | V-4〜V-6 の TBD を M-2 の結果に |

---

## 5. 報告

`REPORT_VENUE_WIRING_MEASURE_<日付>.md` を作ってください。

- **M-1〜M-3 の結果を先に**（M-4 は出たら追記）
- **測ったコミットを併記**（R-23・R-28。ゲートの合格は「どのビルドで測ったか」とセットでのみ意味を持つ）
- 期待値つきの再実行表
- 帯を外れた項目があれば、**値と、外れ方の向き**だけを書く（定数は動かさない）
- VC-2 の変異の実演・VC-3・VC-4 の完了

---

## 6. 触ってよいもの

| 可 | 不可 |
|---|---|
| `apps/cli/package.json`（依存の宣言）・`apps/cli/test/v18-gate.test.ts`（変異の実演の後に戻す）・`REPORT_VENUE_WIRING_20260915.md`（追記）・新しい報告 | 較正定数の値（`RACE_RANDOM_K`・`TAIL_MIX_*`・`LAMBDA_STAR`・`ODDS_CAP`・`stepM` ほか）・`packages/race-engine/src`・`packages/scheduler/src`・`apps/worker/src`・正典・本番 DB・ワーカーの配備・push |

---

## 7. 配備の線（レビュー側の判断）

| # | 条件 |
|---|---|
| 1 | **M-1・M-2・M-3 が出て、帯の内側**（または外れた理由がオーナーの判断で受理されている）ことを、最終裁定で確かめてから **`0023` と新しいワーカーを本番へ配備** |
| 2 | 配備の後、**ワーカーの記録の「1 レースを作る周」の秒数**を読み、**1 周がサイクルの 60% に収まる**ことを確かめる（ES-6 裁定 §3 の 5） |
| 3 | **M-4（V-10）は、実際の利用者が入る前**に必ず済ませる。配備の後でよい |
| 4 | `0021`・`0022` の適用は本便と独立（`REPORT_G3_PROD_DB_READONLY_20260916.md` §4 の手順・オーナーの指示で） |

---

## 8. オーナーにお願いすること

| # | 中身 |
|---|---|
| 1 | **M-4（V-10）を別のターミナルで流す**か、開発側に流させるか |
| 2 | `0021`・`0022` の本番への適用（レビュー側の推奨: **当てる**。手順は確認済みで、`--yes-production 0021` で当たるのは `0021` だけ） |
| 3 | push（レビュー側の推奨: **する**） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
