# 報告 — CF-5（1 周の読み込みの所要）／ CF-6（上限 3,000 の根拠）（2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**指示**: 裁定 `REVIEW_CF1_STEADY_STATE_VERDICT_20260918.md` **CF-5 / CF-6**
**道具**: `tools/measure-cycle-load.mjs --env staging`（★READONLY・新設）

> 凡例（R-13）: **✔** ＝ 実測 ／ **△** ＝ 未確認 ／ **🔴** ＝ 判断待ち・欠陥

---

## 0. 結論

| # | |
|---|---|
| **CF-5** | ✅ ★**読み込みの側は、母数 7,000 でも 1 周の重さになりません**（★DB 側 **69 ms** ＋ 調子疲労 **13.6 ms**）。★1 周 180 秒に対して 0.05%。→ ★**A（母数を増やす）は、読み込みの観点では通ります** |
| **CF-5 の残り** | 🔴 ★**週送りの書き込みは測れていません**（★状態を変えるので R-24）。★**D-104 が警告していた本体はここ**です |
| **CF-6** | 🔴 ★**3,000 に文書化された根拠はありません**（★導入コミットにも註記なし）。★ただし ★**正典 §10.5「現役 2,500 頭」に対する余裕**と読むのが自然です → ★**較正定数ではなく「世界の規模の写し」**。★**A は §10.3 だけでなく §10.5 の改訂を伴います** |

---

## 1. ✔ CF-5 — 測り方を先に直しました

🔴 ★**初めの測り方は間違っていました。**

| # | 何が起きたか | 直し |
|---|---|---|
| 1 | ★**2 回の実行で 1 頭あたり 0.97 ms と 0.22 ms**（★4 倍の開き）。★**網の往復が支配的**でした | ★**`explain analyze` で DB 側の実行時間だけを取る**形に。★**同じ DB を VPS も読む**ので、★この量はそのまま効きます |
| 2 | ★調子・疲労の SQL を**表の名前で推測**し、「表が無い」と出した | ★`loadTrainingStates` と**同じ SQL** に直した（★実体は `horses` の列。★R-21: エラーを「該当なし」と読まない） |

⚠️ ★**網こみの値を「ワーカーの所要」として報告しません**（★ES-6 の「1.28 倍」と同じ轍・R-30）。

### ✔ 測定（staging・★最小値 / 3 回）

```
       上限       読めた頭数          網こみ        ★DB 側       1頭あたり
      500         500       430 ms      12.1 ms     24.2 μs
     1000        1000       517 ms      13.3 ms     13.3 μs
     2000        2000       885 ms      37.9 ms     18.9 μs
     3000        3000      1190 ms      39.3 ms     13.1 μs
     5000        4389      1467 ms      41.2 ms      9.4 μs

  ★伸び（DB 側）: 1 頭あたり 8.24 μs ＋ 固定 10.8 ms
  ★7,000 頭に換算（DB 側）: ★**69 ms**
  調子・疲労（loadTrainingStates と同じ SQL）: ★DB 側 13.6 ms / 7,370 頭
```

→ ★**1 周 180 秒に対して、読み込みは 0.1 秒未満**です。

### 🔴 ★ついでに見つけたもの 2 件

| # | |
|---|---|
| 1 | ★**`loadTrainingStates` には `where` がありません** — ★引退馬も繁殖馬も含む**全頭**（7,370）を毎周読んでいます。★いまは DB 側 13.6 ms なので実害はありませんが、★母数を増やすと素直に伸びます |
| 2 | ★**勝利数の集計（`loadWinsByHorse`・この便で私が足したもの）は `race_entries` の行数に比例します。** ★行は**増え続けます**（★1 日 480R × 13 頭 ＝ 6,240 行/日 ＝ **年 228 万行**）。★指示書 CL-1 の「★重ければ列にする」は、★**いずれ必ず来ます**（★§18 の LR-8 と同じ形） |

### 🔴 ★測れていないもの（★正直に書きます）

★**週送りの書き込み**（`training-runner` の UPDATE・★7,000 頭ぶん・★80 周に 1 回）。
★**状態を変えるので、読み取り専用の道具では測れません**（R-24）。
⚠️ ★**D-104 が「週進行のバッチの所要は頭数と間隔の掛け算で増える。D-100 の間隔を決める測定と一緒に測る」と書いていた、その測定です。**
→ ★**配備後に `journalctl` の「周=」で読むのが唯一の方法**です（R-28）。★`main.ts` の使用率の直しが効きます。

---

## 2. 🔴 CF-6 — 上限 3,000 の根拠

| # | 調べたこと | 結果 |
|---|---|---|
| 1 | ★導入コミット | ✔ `4f8039c`（2026-08-08・P2「DB の馬を HorseRecord に戻す層」）。★**`limit = 3000` に註記はありません** |
| 2 | ★文書 | ✔ ★**3,000 の根拠を述べた文書はありません**（★報告書・裁定・正典を走査） |
| 3 | ★正典に対応する値 | ✔ ★**§10.5（1330 行）「プールサイズ: 現役 2,500頭 / 種牡馬 200 / 繁殖牝馬 800」** ／ ✔ §14（1717 行）「NPC 2,500頭の週進行バッチ」 |

→ ★**3,000 は「正典の現役 2,500 頭に少し余裕を持たせた値」と読むのが自然**です（△ ★書いた人の意図は確認できません）。
→ ★**較正定数ではありません**（★着順にも経済にも効きません）。★**「世界の規模の写し」**です。

### ★したがって A（母数を増やす）が伴うもの

| # | |
|---|---|
| 1 | 🔴 ★**正典 §10.5 の改訂**（★現役 **2,500 → 7,000**）。★**§14 の「NPC 2,500頭の週進行バッチ」も同じ数**です |
| 2 | ★`loadRaceablePool` の上限（3,000 → 母数に合わせる）。★**写しなので、正典を直せば従います** |
| 3 | ★**プリシードの規模**（`DEFAULT_PRESEED_OPTIONS`: 種牡馬 200 / 繁殖牝馬 800）。★現役 7,000 を毎年供給できるか（△ 未確認） |
| 4 | ★週送りの所要（★上の CF-5 の残り） |

⚠️ ★**①は正典の改訂なので、開発側では書きません。**

---

## 3. ★次

| # | |
|---|---|
| 1 | 🔴 ★**§10.5 の改訂（現役 2,500 → 7,000）の是非**。★これが決まれば A に入れます |
| 2 | ★**CF-2**（定常の判定基準）は (c) に入る直前に書きます |
| 3 | ★週送りの所要は ★**配備後の周の実測**で閉じます（R-28・AL-6 と同じ形） |
