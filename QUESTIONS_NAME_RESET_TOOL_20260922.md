# 照会: 運営が馬名を仮の名前に戻す道具 — 2026-09-22

> 開発側 → レビュー側。提案 `PROPOSAL_PLAYER_NAME_MODERATION_20260922.md` §2 段 2・§4、裁定 `REVIEW_BREED_OWN_MARE_VERDICT_20260922.md` §6「次の順番」2。
> コードはまだ書いていません。本番には触れていません。開始時の HEAD `1a3e4fe`。

## §0 一行

道具の骨組みは、既存の書く道具と同じにします（既定は下見・`--apply` で書く・本番は関門）。
決めてほしいのは次の 3 つです。
- **仮の名前の形**（P-1）
- **理由をどこに残すか**（P-2）
- **元の名前を残すか**（P-3）

## §1 事実

| # | 事実 | 箇所 |
|---|---|---|
| F-1 | NPC の仔の予備の名前は「厩舎の接頭辞 ＋ ID の先頭 6 文字」。**英数字（16 進）を含む**ので、利用者の命名の規則（カタカナ・ー・・／2〜9 文字・D-120）に合わない | `breeding-runner.ts:700` |
| F-2 | 馬名は `name_key`（正規化）で一意（`0066`）。検査の版は `name_checked_with`（null／版／`hit:<版>`） | `0064`・`0066`・`f1a5ebe` |
| F-3 | 戦績・血統・生涯の記録は馬の ID に付いている。名前を変えても消えない（§18） | `horse_story_event.horse_id` など |
| F-4 | 利用者が自分で改名する経路は無い（命名は仔の下書きにだけ付けられる・`kind = 'name'`） | `0065` |
| F-5 | 本番で書く道具は、旗 2 つ ＋ 写せない数 1 つの関門を持つ（`backfill-name-key`・`recheck-name-blocklist`） | `tools/lib/args.mjs` |

## §2 案

`tools/reset-horse-name.mjs --env <環境> --horse <ID> --reason <語>`（STATE_CHANGING）

- 既定は**下見**。いまの名前は出さず、ID と、仮の名前と、当たっている一覧の有無だけを出す。
  - 実在馬名や不快な語の可能性があるため、元の名前を出力に書かない（憲法 §0.1）。
- `--apply` で書く。書くものは次の 3 つ。
  - `horses.name`・`name_key`（仮の名前）
  - `name_checked_with`（今の一覧の組で検査した版。当たれば別の仮の名前を引き直す）
  - 記録（P-2）
- 本番は関門を 3 つ置く: `--yes-production`、`--reset-name`、`--expect-horse <ID を 2 回目に写す>`。
  - 最後の 1 つは、写し間違いの 1 回で別の馬を書き換えないため。1 頭ずつ、ID を 2 回渡す。
- 同じ取引の中で書いてから読み直し、合わなければ戻す（他の書く道具と同じ）。

## §3 決めてほしいこと

- **P-1 仮の名前の形**
  - (a) **推し**: 「**カリメイ**」＋ ID から決まるカタカナ 5 文字（計 9 文字）。規則（カタカナ・2〜9 文字）に合い、一目で仮の名前と分かる。同じ ID なら同じ名前（決定論）。重なったら、決まった手順で次の候補へ進む。
  - (b) NPC と同じ `generateHorseName`（普通の名前に見える）。仮と分からず、本人がそのまま使い続けることになりうる。
  - (c) NPC の予備の名前（F-1）をそのまま使う。規則に合わない。
  - ⚠️ 「カリメイ」という語そのものの是非（画面に出る語）はオーナーとデザイナーの判断。語は定数 1 か所に置く。
- **P-2 理由をどこに残すか**
  - (a) **推し**: 新しい表 `horse_name_resets`。列は次のとおり。
    - `horse_id`
    - `reason`（`real_horse` / `offensive` / `person` / `trademark` / `other`）
    - `note`（運営の短い書き込み）
    - `old_name_hash`
    - `new_name`
    - `created_at`
    - 書くのは道具だけ（revoke all）。移行が 1 つ要る。
  - (b) 生涯の記録（`horse_story_event`）に書く。他人も読める表なので、**理由（人名・商標など）が公開される**。採らない。
- **P-3 元の名前を残すか**
  - **推し**: 平文では残さない。`hashNormalizedName(元の name_key)` だけを残す（実在馬名の一覧と同じ扱い）。
  - 残すのは「同じ名前をまた付けさせない」ためで、判定は一覧で行う。
  - 平文が要る場面（異議の申し立てなど）があるなら、オーナーの判断で別途。
- **P-4 本人に知らせるか**: 知らせる経路（通知・生涯の記録）は、提案 §2 段 2 の「改名の依頼」の流れと一緒に作る（この道具では作らない）。→ これでよいか。
- **P-5 この便の範囲**: 道具 ＋ 記録の表 ＋ 移行 ＋ staging の予行（必ず戻す）まで。本人の改名の経路（F-4）は作らない。→ これでよいか。
