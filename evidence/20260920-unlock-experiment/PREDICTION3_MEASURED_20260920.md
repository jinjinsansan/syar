# 予言 3 の判定 — ★**記録した量で測り直しました**（2026-09-20・staging 演習 5 回目の世界）

## 🔴 なぜ測り直したか

レビュー側の指摘（`AU-21`: **同じもので測ったか**）。
「前」と「後」で**別の統計量**を比べていました。

| | 量 | 値 |
|---|---|---|
| 「前」（正典 D-053 註記・production 実測） | **`mean(stats ÷ potential)`・能力値 1 つ 1 票**（n=36,775） | **0.3149**（範囲 0.2790〜0.3509） |
| 「後」（こちらが最初に出した） | **馬ごとの `sum(stats)/sum(potential)` の平均**（n=2,388 頭） | 0.6849 |

⚠️ **投入直後は両者が一致します**（`stats = potential × unlock_rate` が厳密なので、どちらも平均 unlock に落ちる）。
→ **だから 0.3149 が両方で同じに見え、違いが隠れていました。** 育った後は必ずずれます。

## ✅ 測り直し（staging・演習 5 回目の世界・読むだけ）

| 量 | 母集団 | 値 | 範囲 | n |
|---|---|---|---|---|
| 🔴 **`mean(stats÷potential)`・能力値ごと**（★記録した量） | **全馬**（★本番の 36,775 と同じ取り方） | **0.4367** | 0.2789〜1.0000 | 36,850 |
| 🔴 **`mean(stats÷potential)`・能力値ごと** | **現役のみ** | **0.6902** | 0.3079〜1.0000 | 11,940 |
| 馬ごとの `sum/sum` の平均（★併記・捨てない） | 現役のみ | 0.6849 | 0.3308〜0.9623 | 2,388 |
| 集団の `sum ÷ sum` | 現役のみ | 0.6823 | — | 2,388 |

## ✅ 判定 — **向きは当たり**

**0.3149 → 0.4367**（同じ母集団の取り方）。**0.71 の方向へ動きました。**

⚠️ **ただし 3 つ、書いておきます。**

1. **全馬で測ると 0.4367 にしかなりません。** 引退した 4,970 頭は**意図的に追いつかせていない**（`breed()` は
   親の `stats` を読まないため）ので、生まれたときの値のまま残ります。
   → 🔴 **本番を作り直した後も、全馬で測れば ~0.44 です。0.71 にはなりません。**
     **0.713 と比べてよいのは「走る馬」のほう**（現役のみ **0.6902**）。
2. **これは staging です。** 予言 3 の的は **production** です。本番で同じ操作をした後に測り直すこと。
3. **年齢が揃っていません**（現役の週齢 104〜259）。若い馬はまだ育っていないので、
   0.713 に一致させるべき数ではありません（向きだけを見る・**AU-7′**）。

## ⚠️ 次に測る人へ

- **`horses.unlock_rate` という列を測らないこと。** 育成では書き換わりません（遺伝の定数・
  追いつき後も平均 0.3148・範囲 0.28〜0.35）。`training-runner.ts` の一括 update に列がありません。
- **測るのは `mean(stats ÷ potential)`、能力値 1 つ 1 票**（`jsonb_each_text` で展開して `avg`）。
  馬ごとの平均でも集団の和の比でもありません。値が 0.005 ずれます。
