# 素性 — ★**M-9 の後の測定**（★流す前に書いています・2026-09-19）

> ⚠️ 🔴 ★**この 1 枚は、★測る前に書きました。**
> ★**後から書くと、★出た数に合わせて素性が動きます。** ★先に書いて、★出た数を下に足します。

---

## 0. ★状態

| ★測定 | 🔴 ★**未実行** |
|---|---|
| ★理由 | ★**オーナーの判断待ち**（★「この 35 分をどこで流すか」） |
| ★流す条件 | ★① オーナーの判断 ★② レビュー側の「流してよい」の合図。★**両方**が要ります |

---

## 1. ★流すコマンド（★確定）

```
npx tsx apps/cli/src/verify-race.ts \
  --pool out/al11/pool-D-active-3000.json \
  --races 120000 --seeds 42,7,2026,31337,1,99,12345,65537 --b6-wired \
  > evidence/20260919-condition-fatigue/v4-m9-8seed-20260919.txt 2>&1
```

⚠️ ★**`grep` で絞らないこと**（★**MD-2'''''**）。★1 回目（14:55 の便）は ★`grep` のバッファのせいで
★**途中経過が読めず**、★harness に止められたと誤認しました。★**絞らずファイルへ。**

⚠️ ★**`timeout` を掛けないこと**（★**MD-5**）。★この道具は状態を変えませんが、
★**締切は「待つ側」に付ける**のが作法です。

---

## 2. ★素性（★VP-8）

| ★母集団 | `out/al11/pool-D-active-3000.json` ★sha256 **`df10399b5c44`**（★**凍結**・★書き出した時点のもの） |
|---|---|
| ★旗 | `--b6-wired`（★**実物の調子と疲労**。★オーナー決定 ①） |
| ★シード | 8 本（42 / 7 / 2026 / 31337 / 1 / 99 / 12345 / 65537）★**明示**する |
| ★標本 | 8 × 15,000 ＝ **120,000 レース** |
| ★前回の所要 | **2,103.4 秒（35 分）** |

---

## 3. 🔴 ★**比べる相手と、その前提**

★比べるのは ★**同じ素性で測った既存の値**です（★`v4-4-b6wired-8seed-20260919.txt`）:

```
V-4 33.79%（2/8 が帯の外）／ V-5 64.54%（1/8）／ V-6 1.07%（0/8）
```

★**前後を同じ実行で測りません**（★35 分 × 2 にしないため）。★プール・旗・シードが**すべて同じ**だからです。

### ⚠️ 🔴 ★**その前提の、正確な言い方**

> ✅ ★**正**: ★測定時の HEAD（`cbaef7f`）から ★**製品のコードは 4 行 変わっている**が、
> ★★**この実行の経路で効くのは `CONDITION_MIN` だけ**。
>
> ❌ ★**誤**: 「製品のコードは `CONDITION_MIN` の 1 行だけ」

⚠️ ★**「1 行だけ」と書いてはいけません。** ★次の人が差分を見ると ★**4 行 出ます**。★**素性が信じられなくなります。**

### ✔ ★4 行の内訳と、★効かない理由（★言い分でなく数）

| ★`balance.ts` | `CONDITION_MIN: 1 → 0` | 🔴 ★**これが効く（M-9）** |
|---|---|---|
| ★`race-field.ts` ×2 | `?? 3` → `?? CONDITION_BASE` ／ `rng.int(2,4)` → `nextCondition(0, rng)` | ✔ ★**発火しない**（★下） |
| ★`measurement.ts` | `VERIFY_SEEDS` 4 → 8 ／ `entriesPerSegment` を新規 export | ✔ ★**読まれない**（★下） |

**✔ ★`race-field.ts` が発火しない根拠**（★プールを数えた）:
```
out/al11/pool-D-active-3000.json   頭数 3000
  __training が無い : 0
  condition が無い  : 0   ← ★★これが 0 なので `?? nextCondition(0, rng)` は一度も呼ばれない
  fatigue が無い    : 0
```
→ ★`trained?.condition ?? …` は ★**左辺が必ず定義済み → `??` が短絡** → ★**乱数も 1 つも引かない**。
→ ★`toEntrant` の `?? CONDITION_BASE` も同じ（★呼ぶ側が必ず渡す）。★そして ★**`CONDITION_BASE === 3`**（`condition.ts:43`）。

**✔ ★`measurement.ts` が読まれない根拠**:
```
apps/cli/src/verify-race.ts:80   const SEEDS = parseList('--seeds', [...MC.VERIFY_SEEDS]);
```
→ ★**`--seeds` を明示するので、既定は読まれません。** ★`entriesPerSegment` は `verify-race.ts` から import されていません。

**✔ ★残り 4 ファイル**（`verify-initial-band.ts`・`apps/worker/src/*`）は ★**`verify-race.ts` から import されていません**（★別の入口・ワーカー）。

> ★**AU-16** ★「他は変えていない」は、★**変えた数**ではなく ★**効く経路に入るか**で言う。
> ★**「1 行だけ」は、たいてい「私が意図した 1 行だけ」の意味になっている。**

---

## 4. ★何を見るか（★出る前に決めておく）

| ★① | ★**V-4 / V-5 / V-6 の平均**（★オーナー決定 ②: ★平均で合否） |
|---|---|
| ★② | 🔴 ★**帯の外の本数**（★決定 ②: ★**必ず併記**。★0 でなければ合格でも余裕を足す） |
| ★③ | ★M-9 の前後の差（★前 = V-4 33.79% / V-5 64.54% / V-6 1.07%） |

### ⚠️ ★**出る前に書いておく予想**

★`M-9` は ★**①の腕を縮め、④の腕を広げる**（★レビュー側の計算）。★この実行は ★**④の腕だけ**なので、
→ ★★**V-4 は上がる向き**と予想します（★**大きさは予想しません**。★数えていないので）。

🔴 ★**外れたら**: ★**私が見落とした経路がある**ということです（★`bandStart` のときと同じ形）。
★**「帯の中だからよい」としません。**

---

## 5. ⚠️ ★この測定に付いていない不確かさ（★変わっていません）

| ★乱数 | ★8 シードで SE **0.093pp**（★シード間 SD 0.26pp）。★**減らせます** |
|---|---|
| 🔴 ★**位相** | ★プールは ★**全頭 同じ `birth_week`（−160）**。★★**シードを増やしても減りません** |
| 🔴 ★**母集団** | ★プールは**凍結**なので再現できますが、★**staging のいまとは別物**です（★`REPORT_STAGING_TOOL_DAMAGE_20260919.md`） |

---

## 6. ★結果（★出たら、ここに足します）

🔴 ★**まだ空です。**
