# ③ の結果 — ★予想と突き合わせ（2026-09-20・本番）

## ✅ ② の確定

```
npx tsx tools/migrate.mjs --env production --plan
→ ★未適用のものはありません（53 件はすべて適用済み）
```
🔴 **これで初めて「33 件 入った」と言えます。** それまでに在ったのは `migrate.mjs` の「OK が 33 個」＝
**道具がそう言った**という事実だけでした。

## ✅ 予想の答え合わせ — **6 項目とも当たり**

| # | 予想 | 実測（本番） |
|---|---|---|
| ① | 53 件 | ✅ 53 件 / 最後 `0053` |
| ②③ | ビュー 2 本・列 4 本が在る | ✅ すべて在る |
| ④ | 対照: 公開ビューが anon から まだ読める | ✅ `races_public` / `race_entries_public` とも true |
| ⑤ | anon/authenticated の書き権限 0 件 | ✅ 0 件（**2026-08-20 の `users` UPDATE の穴は塞がっている**） |
| ⑥ | anon の EXECUTE すべて false | ✅ 3 本とも false |
| 形 | ビュー 10 / 表 24 / 関数 14 | ✅ **staging と完全一致**（構成のずれは無し） |

## 🔴 予想していなかったもの — **1 件**（V-20 が落ちた）

```
🔴 ★③ 登録簿に無いオブジェクトが無い
     ★未登録: daily_run_log → tools/lib/exposure-registry.mjs に姿を書くこと
=== 🔴 V-20 不合格 1 件（11 件中・環境 production） ===
```

- ✅ **網が働いた形です。** `0052` で入った新しい表が、露出の登録簿に無いのを捕まえました。
- ✔ **露出はしていません**: 付与の全列挙 23 件に `daily_run_log` は**出ません**
  （＝ anon / authenticated に 1 つも付いていない）。**未登録だっただけ**です。
- ✅ 直し: 登録簿に `daily_run_log: CLOSED` を足し、staging で **11/11 合格**を確認。
- ⚠️ 🔴 **この穴は staging にも在りました。** `0052` は staging に先に当たっていたのに、
  **誰も `verify-anon-exposure` を流していませんでした**（①′ の演習にも入っていない）。
  → `STALENESS` の `prod-exposure` が `pending` のままであることと同じ穴です（**DP-1**）。

## 🔴 H-3 の見込みを、下方修正します

予想の段では「`0021` が入ったので、持ち馬が全頭休養に落ちていた件は次の周から直るはず」と書きました。

**実測: 本番の持ち馬は 0 頭です。**（`owner_id is not null` … **0**）

→ **H-3 が壊していた対象は、いま 0 頭です。** `0021` の値打ちは「これから人が入ったときに壊れない」
ことであって、**いま何かが直るわけではありません**。
⚠️ 「直るはず」と書いたのは**対象の数を数える前**でした。予想としては外していませんが、
**意味を過大に書いていました**。

## 次

- ⚠️ 本番で `verify-anon-exposure` を**もう 1 回**流して **11/11** を確認すること（登録簿を直した後）。
- ③′（ワーカー停止）／③″（控え）は **④ の直前**。忘れないこと。
