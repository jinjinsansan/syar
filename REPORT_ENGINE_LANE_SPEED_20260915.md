# 報告 — 楕円の走路でレース判定が遅い件（ES 便・ES-0〜ES-3）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-15 ／ 開発側 → レビュー側・オーナー
**対象**: 回答 `REVIEW_QUESTIONS_ENGINE_LANE_SPEED_ANSWER_20260915.md` §5（ES 便の指示）・§4（本番の確認）
**ブランチ**: `p4/race-30sec-cuts` ／ **開始 HEAD** `884019c` ／ **コミット** `1f58cde`（ES-1）→ `c240d95`（ES-2）→ ES-3（本報告と同じコミット）

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **計算** ＝ 実測値と定数から出した値 ／ **△** ＝ 未確認・推定
> ⚠️ 本番に対してしたのは**読み取りだけ**です（ssh での状態の確認・DB は `transaction read only` を宣言してから数えた）。書き込み・再起動・配備・ロールバックはしていません。

---

## 0. 結論

1. ★**ES-3 まで直しても、合格線（D-071 の前との比 1.3 倍以下）に届きません。** 本番の条件で **約 3.1〜7.2 倍**です（§2）。回答 §Q-2 の指示「届かなければ、そこで止めて報告」に従い、**ES-3 で止めました。ES-4（回数の上限の検査）は未着手**です
2. 結果は **1 ビットも変わっていません**（指紋 182 組・ES-1〜ES-3 のすべてで一致）。変異 ①②③ はすべて落ちます（§3）
3. ★**本番のワーカーは D-071 より前の版 `70d9982`（2026-08-12）で動いています**（§1）。だから本番の 10 分サイクルは正常です。**いまのコードをそのまま配備すると、1 レースのオッズ計算が 10 分に入りません**（計算: 約 7〜17 分・§2-2）
4. 残りの重さの大半は、**コーナーを 10m 刻みで積む計算そのもの**と読んでいます（△・§2-3）。これ以上は `stepM`・試行回数・並列化など、**回答 §Q-2 条件 4 で開発側が選んではいけない手**になります → §4 の判断をお願いします

---

## 1. ES-0 本番の確認（回答 §4・読み取り専用・1 回）✔

**10 分サイクルで生成できているか: はい**（ただし D-071 の前のエンジンで）

| # | 確かめたこと | 結果 |
|---|---|---|
| 1 | 稼働中のワーカーの版 | `/opt/star-current` → `/opt/star-releases/70d9982cc84090b3e658dfbd05644b400e8665a9` ／ `ActiveState=active` ／ `ActiveEnterTimestamp=2026-09-10 06:35:39 JST` ／ `NRestarts=0` |
| 2 | その版に D-071（`97364c5`）が入っているか | ★**入っていない**（`git merge-base --is-ancestor 97364c5 70d9982` が偽）。`70d9982` は 2026-08-12「配備の防御機構が配備者の接続に依存していた件を直す」 |
| 3 | 直近 50 本の生成の間隔 | 最小 532 秒 ／ 中央 607 秒 ／ 最大 666 秒 ／ 600±60 秒の外 5 件 |
| 4 | 直近 48 時間の抜けたサイクル | 発走枠（`scheduled_at` を 600 秒で割った枠）288 ／ 範囲 288 ／ **欠番 0** ／ 同じ発走時刻の重複 0 |
| 5 | 直近 48 時間の状態 | `settled` 288 ／ `scheduled` 3 ／ 中止 0 |
| 6 | ワーカーの記録 | 48 時間で「時間切れ・中止・再起動・失敗・error」の行 **0**。1 レースを作る周は **98.2 秒（16.4%）・110.8 秒（18.5%）**（2026-09-15 23:12・23:22 の記録） |

- DB は `app_environment = 'production'` と `transaction_read_only = on` を確かめてから数えました（スクリプトは `out/gen/es/prod-readonly.mjs`・git の対象外）
- ⚠️ `races` にサイクル番号の列が無いので、#4 は発走時刻を 10 分で割って数えました

---

## 2. 所要（同じ機械・同じ実行方法）✔

### 2-1. `resolveRace` 1 回（µs）

エンジンを `git archive`（または作業ツリーの写し）でスクラッチに取り出し、照会書 付録 B の `bench-engine.mts` を本番の条件に広げたもので測りました。`watch-pool.json` の先頭の馬・芝・良・シード 0〜N−1。**既定** ＝ `DEFAULT_OVAL`、**最長の周** ＝ 大河原の形（1 周 2400・直線 540・幅 20）。値は 2 回の実行の範囲。

| 条件 | `97364c5^`（D-071 の前・N=2000） | `cfc3ad1`（直す前・N=400） | ES-2（N=400） | **ES-3（N=2000）** | **ES-3 ÷ D-071 の前** |
|---|---|---|---|---|---|
| 16 頭 1600m 既定 | 25〜28 | 2,842〜3,097 | 128〜129 | **81〜83** | **約 3.1 倍** |
| 16 頭 1600m 最長の周 | 19 | 3,020〜3,099 | 79〜85 | **72〜85** | **約 4.1 倍** |
| 16 頭 3000m 既定 | 24〜25 | 7,963〜8,437 | 163〜169 | **135〜136** | **約 5.5 倍** |
| 16 頭 3000m 最長の周 | 16〜18 | 6,603〜6,620 | 117〜127 | **93〜101** | **約 5.7 倍** |
| 18 頭 1600m 既定 | 17 | 3,269〜3,280 | 72〜77 | **67〜70** | **約 4.0 倍** |
| 18 頭 1600m 最長の周 | 15〜16 | 3,423〜3,550 | 70〜78 | **74** | **約 4.8 倍** |
| 18 頭 3000m 既定 | 18 | 9,073〜9,235 | 146〜161 | **128〜131** | **約 7.2 倍** |
| 18 頭 3000m 最長の周 | 16 | 7,370〜7,557 | 115〜135 | **109〜111** | **約 6.9 倍** |

- ⚠️ **回数で値が変わります**: `97364c5^` を N=40 で測ると 45〜113µs でした（JIT の立ち上がりが混ざる）。比は **同じ N=2000 の `97364c5^` と ES-3** で取りました
- `97364c5^` は `course` を読まない版なので、「既定」と「最長の周」は同じ計算です（差はばらつき）
- **直す前（`cfc3ad1`）から ES-3 までで 約 35〜70 倍**速くなりました

### 2-2. 本番機への換算（計算）

回答 §2-2 と同じく、本番機の D-071 の前の 1 レース **138 秒**（正典 D-036）に比を掛けます。

| エンジン | D-071 の前との比 | 本番機の 1 レース | 10 分サイクル |
|---|---|---|---|
| D-071 の前（いまの本番） | 1 | 138 秒 | 23%（★実測の周は 98〜111 秒・§1 #6） |
| 直す前（`cfc3ad1`） | 約 120〜500 倍 | 数時間 | 入らない |
| **ES-3** | **約 3.1〜7.2 倍** | **約 7〜17 分** | **入らない** |
| 合格線 | 1.3 倍 | 約 180 秒 | 30%（1 周 2 本で 60%） |

### 2-3. 残りの重さ（△ 読み）

- 距離ロスはコーナーを `stepM` = 10m で積みます。3000m のコーナーは約 1,900m なので、**1 頭 約 190 刻み × 18 頭 ≒ 3,400 回**の刻みの計算（`Math.sin`・smoothstep・`Math.max/min`）が 1 レースに入ります
- D-071 の前はこの計算自体が無かったので、**この計算を消さないかぎり 1.3 倍には原理的に届きにくい**と読んでいます（プロファイルは取っていません・△）
- 刻みごとの計算の中で、まだ前計算できる形は **掛ける順番を変える**もの（§Q-2 条件 3 で不可）か、**刻みの数を変える**もの（`stepM`・条件 4 で不可）です

---

## 3. 指紋と変異（ES-1〜ES-3）✔

### 3-1. ES-1 指紋（`packages/race-engine/test/lane-fingerprint.*`）

- **範囲 182 組**: 10 場の凍結した形（`frozenCourseOf` → `conditionsFromFrozen`）・`DEFAULT_OVAL`・半径が 4 つとも違う形（`ovalSpecFromCornerRadii(400, [70, 85, 100, 120], 20)`）・直線 × `DISTANCE_MENU` の 7 距離 × 8 頭・18 頭 × シード 20
- **中身**: `resolveRace` の結果（馬・着順・スコア・距離ロス・走破タイム・着差の秒と言葉。数は `toString()` で丸めない）＋ `laneExtraM` を直接呼んだ値 ＋ `laneAt` を 50m 刻みで直接呼んだ値（画面の経路・シード 0）
- **期待値**: `lane-fingerprint.gen.ts` が 1 回だけ書いた `lane-fingerprint.expected.json`。書いたときの HEAD は `884019c` で、**エンジン・遺伝・番組の `src` は `cfc3ad1` から差分なし**（`git diff` 空）＝回答の「直す前のコミット」と同じコード。道具はエンジンのコードに未コミットの変更があれば止まります
- **ES-1 のコミットで先に入れ、直す前のコードで合格**を確かめました（R-15 の形）
- 検査の所要: 直す前 **15.1 秒** → ES-2 **0.7 秒** → ES-3 **0.85 秒**

### 3-2. 変異の実演

| # | 変異 | 結果 |
|---|---|---|
| 対照（ES-1） | `TURN_REF_M` を 1600 → 1601 | ✔ **落ちる**（84 組で変わった）。84 組にとどまるのは、1601m でも最後の 1m が直線に掛かる形では総旋回角が同じになるため（△ 読み） |
| **①**（ES-2） | 1 レース 1 回の値を**距離だけ**で作る（`lanePlanOf(conditions.distance)`） | ✔ **落ちる**（140 組）。140 ＝ 既定の楕円と同じ形のスターパーク以外の 9 場 × 14 ＋ 半径 4 つ違い × 14（計算で一致） |
| **②**（ES-3） | 馬ごとの定数を**別の枠**で作る（`laneHorseOf(gate + 1, …)`） | ✔ **落ちる**（168 組 ＝ 直線を除くすべて） |
| **③**（ES-3） | 掛ける順番を 1 か所入れ替える（`… * laneModel.wobbleM * reveal * swing` → `… * (laneModel.wobbleM * swing) * reveal`） | ✔ **落ちる**（118 組）。落ちなかった 50 組は、`swing` がちょうど 1 になる形（基準距離 1600m など）で積の丸めが変わらない組と読んでいます（△） |

いずれも戻して合格を確かめてからコミットしました。

---

## 4. 判断をお願いしたいこと（オーナー・レビュー側）

| # | 判断 | 開発側の見立て |
|---|---|---|
| E-1 | **合格線 1.3 倍に届かない**。この先の手をどれにするか | 開発側だけでは選びません（§Q-2 条件 4）。候補（どれも結果・較正・D-035/D-036 に効く）: (a) `stepM` を大きくする（結果が変わる → V 系の取り直し）／ (b) 距離ロスを解析的に積む形に変える（同）／ (c) オッズの試行回数 `ODDS_MC_TRIALS` の見直し（D-035 の偏り）／ (d) ワーカーでの並列化（本番機のコア数 △）／ (e) 合格線そのものの見直し（1 周の余裕率） |
| E-2 | **本番への配備の順番** | ★いまのコードを配備すると 10 分サイクルが壊れる（§2-2）。10 場の配線（`80c4eb3`）・`0021`〜`0023` の配備は **E-1 の後**（中間裁定 `REVIEW_VENUE_WIRING_VERDICT_20260915.md` のとおり） |
| E-3 | ES-3 の状態で ES-4（回数の上限の検査）を入れるか | 退行を捕まえる検査としては今でも意味がある（区間を作る回数は 1 レース 1 回になった）。指示は「止めて報告」なので未着手 |
| E-4 | サイクルの短縮（ゲーム本体の回答 `REVIEW_CONSULT_GAME_DESIGN_TWO_MODES_ANSWER_20260915.md` §4-3 (a)） | E-1 の結論が下限を決める。いまの ES-3 のままなら 10 分でも入らない |

---

## 5. 触ったもの

| ファイル | 中身 |
|---|---|
| `packages/race-engine/src/lane.ts` | ES-2: `laneAt` → `laneAtWithSwing`（`swingScale` を 1 回）・`lanePlanOf`・`laneExtraMOnPlan` ／ ES-3: `laneHorseOf`（馬ごとの 4 値）・`laneAtOnHorse`（刻みごとだけ）。`laneAt`・`laneExtraM` の署名と値は不変 |
| `packages/race-engine/src/race.ts` | ES-2: `lanePlanOf(distance, course)` を 1 レース 1 回・馬ごとに `laneExtraMOnPlan` |
| `packages/race-engine/test/lane-fingerprint.cases.ts`・`.gen.ts`・`.test.ts`・`.expected.json` | ES-1（新規） |
| 触っていない | 較正定数の値・`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`・`stepM`・`LANE_MODEL`・`LANE_REVEAL_FULL_RUN`・乱数の消費・`packages/betting`・`packages/render`・`apps/web`・`tools`・正典・本番 DB・ワーカーの配備 |

## 6. 検査 ✔

- `npm run typecheck` … 通過
- `npx vitest run --maxWorkers=2 --minWorkers=1` … **ES-1・ES-2・ES-3 のいずれも `Test Files 1 failed | 172 passed (173)` ／ `Tests 1 failed | 1772 passed (1773)`**・終了コード 1。落ちたのは変更前からの `packages/render/test/edit-grammar-audit.test.ts` ⑨（独走代表 expected 'contest' to be 'solo'）で、**1 件から増えていません**
- 変異 ②③ を戻したことは、`lane.ts` に `laneHorseOf(gate, …)` と `* reveal * swing;` が元の形で残っていることで確かめました
