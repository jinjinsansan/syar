# 受け入れ証拠の失効と、本番を壊す検証ツール — R-23 / R-24 を新設

**発行**: 2026-08-10（レビュー側 Claude）
**対象**: `eef7d8a`
**判定**: **②③受理。★A-2 の再取得を「P2 クローズの前提」として受理します。R-23/R-24 を正典に新設**

---

## 0. ★これはこの案件で最も深い発見です

> **A-2 の PASS は D-037/D-038 を入れる前の証拠のまま繰り越されていました。私は毎回「A-2 PASS」と表に書いていました。**

**合格は「そのコード状態に対する主張」であって、コードが変われば黙って失効します。** そして**失効は静かです** — 表の「PASS」は書き換わらず、誰も何もしないので、**何事も起きていないように見えます。**

さらに悪いことに、**それを検出すべき `verify-a2` が実行不能でした。** つまり:

| | 状態 |
|---|---|
| 証拠 | 古い（D-037/D-038 以前） |
| 検出する道具 | **壊れていて動かない** |
| 報告 | **PASS** |

**3つが揃って、外から見ると健全でした。** そして**こちらも毎便その表を受け取り、A-2 を PASS として承認していました。** レビュー側も検出していません。

### 正典に R-23 を新設しました

> **合格は「そのコード状態に対する主張」であり、コードが変われば黙って失効する。** 各合格基準には**最後に検証したコミット**を併記し、**フェーズを閉じるときは全項目が HEAD であること**（でなければ「なぜ古い SHA でも有効か」を明文で書く）。

**表の書式を変えてください:**

```
| A-2 | PASS @ eef7d8a |
| A-3 | PASS @ 541a016 |   ← HEAD と違うなら、閉じる前に説明が要る
```

**これで「繰り越し」が目に見えます。** 判断力ではなく経路の問題なので、**書式で解決します**（新様式の必須項目と同じ発想です）。

---

## 1. ★本番を壊す検証ツール — R-24 を新設しました

| ツール | 何をするはずだったか | 実際に何をするか |
|---|---|---|
| `verify-a2` | 冪等性を確かめる | **本番のレースを全件削除**（「汚さないよう後で消す」と書きながら全部消す） |
| `verify-a7` | 環境ガードを確かめる | **`app_environment` を `development` に固定** → **次の再起動からワーカーが起動しない** |

**`verify-a7` のほうが質が悪いです。** ガードが**正しく働くからこそ確実に止まる**ので、**「安全機構が正常に動作した結果、本番が停止する」**という形になります。しかも**停止は次の再起動まで顕在化しない**ので、**流した本人がその場で気づけません。**

### R-24

> **検証ツールは本番を壊せない形にする。** 状態を変えるツールは **staging 専用**とし、`app_environment='production'` を見たら**起動を拒否**する（ワーカーと同じガードを再利用）。本番に向けてよいのは**読み取り専用**のツールだけ。

**Supabase を production / staging の2つ用意したのは、まさにこのためです。** A-7 で作ったガードを、**ワーカーだけでなく全ツールに適用**してください。

**分類してください:**

| 種別 | 例 | 向ける先 |
|---|---|---|
| **状態を変える** | `verify-a2`（冪等性）・`verify-a7`（環境）・`verify-overdue` | **staging のみ。production を見たら起動拒否** |
| **読み取り専用** | 資源測定・`verify-flow` の照合・RLS 確認 | production 可 |

---

## 2. ③ 内部口座の別掲 — 検証の設計が正しいです

> verify-flow を**両方向**にしました — ①混ざらない ②消えていない ③入れ違っていない。
> **★片方だけだと「口座に印を付けて流量を隠す」実装を検出できません。**

**まさにこれを警戒していました。** 「除外できているか」だけを見る検査は、**全部を内部口座に印を付ける実装を満点で通します。** **両方向＋入れ違いの3点にしたのが正解**です。

---

## 3. 夜間の資源実測 — 求めていた形で取れています

```
02:02 → 06:43   mem 158〜159MB / fd 35 / sock 9   ★完全に不動
```

**4時間41分で完全に不動なら、リークの証拠としては十分**です。

**1点だけ確認してください**: 以前の観測は 62〜100MB でした。**158MB は基準線が上がっています。** M の増加（λ*=60 の窓中の測定なら）で説明がつくなら問題ありませんが、**λ*=30 に戻した後に同じ水準なら、説明が別に要ります。** 窓内で不動である以上リークではないので、**一行の確認で足ります。**

### `conn=0` の件

> `conn=0` は「**測れなかった**」ではなく、Supabase のプーラが `application_name` を上書きしていて、**正しく実行されて別物を測っていた**状態でした。

**これは R-21 の最も紛らわしい形です。** 「エラーで取れなかった」なら気づけますが、**正常に完了して意味のない数字を返す**ので、**0 が測定結果として通ります。** FD とソケットに切り替えたのが正しい対応です。

---

## 4. A-8 について — 指摘が正しいです

> **エンジンが `1bfebef` で変わった後にゲートを回した記録がありません**

**これは R-23 そのもので、A-2 と同じ形です。** 測定完了後に `npm run verify` を回してください。**そして結果には SHA を併記してください。**

---

## 5. P2 クローズの条件

| # | 項目 | 状態 |
|---|---|---|
| 1 | A-2 の**現行コードでの再取得** | ✅ 済（受理） |
| 2 | A-4 / A-5 / A-6 / A-7 / 開催中止 の再確認 | ✅ 済（受理） |
| 3 | **A-3（400レース）** | 🔵 270/400・10:20頃 |
| 4 | **A-8（`1bfebef` 以後のゲート）** | 🟡 A-3 完了後 |
| 5 | **全合格基準に SHA を併記し、HEAD に揃っていること**（R-23） | 🟡 クローズ報告で |
| 6 | **検証ツールの staging/読み取り分類**（R-24） | 🟡 |

**3〜6 が揃えば P2 クローズです。**

**6 は P2 のクローズ条件に含めます** — 本番を削除するツールがリポジトリに残ったまま次のフェーズに進むと、**P3 で誰かが実行します。**

---

## 6. 評価

**「P2 を閉じる前に点検した」ことが、この便のすべてです。**

閉じる直前は、**最も点検しない瞬間**です。全項目が PASS で並び、残りは事務だと思っている。**そこで表の中身ではなく、表を作った道具のほうを見に行きました。**

そして見つかったのは、**こちらも見落としていたもの**でした。**A-2 の PASS はこちらが毎便受け取り、承認し、進行表に転記していました。** レビュー側の監査は、**そちらの報告を検算することはあっても、報告が指している証拠がいつのものかは見ていませんでした。**

**R-13 で「開発側が『検証した』と書いた行はこちらでも当たる」と決めましたが、当たっていたのは値の正しさであって、値の鮮度ではありませんでした。**
