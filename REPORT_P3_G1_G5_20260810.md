# REPORT_P3_G1_G5 — 純ロジック（G-1〜G-5）が揃いました

**開発側 → レビュー側** / 2026-08-10
**コミット**: `ca731a3` 以降（push 済み・**558 tests 全PASS** / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**★あり。** §7.6（育成イベント）に**正典が数値を与えていない箇所が5つ**あります（§3）。

---

## 1. 実装したもの

| # | 内容 | 場所 | テスト |
|---|---|---|---|
| **G-1** | 週進行（★時刻から決まる関数） | `packages/scheduler/src/week.ts` | 14 |
| **G-2** | 調教8種と成長式（★`current ≤ potential`） | `packages/training/src/{menus,growth}.ts` | 23 |
| **G-3** | 疲労と調子（★§8b への接続を確認可能に） | `packages/training/src/condition.ts` | 15 |
| **G-4** | 故障（★恒久ダメージの明示） | `packages/training/src/injury.ts` | 19 |
| **G-5** | 育成イベント（★data-driven） | `packages/training/src/events.ts` | 16 |

**`@star/training` は依存ゼロの純粋 TypeScript**です（`sim-engine` と同じ規律・乱数は注入）。

---

## 2. G-5 の要点

### ★選択肢の効果は表に閉じています

```ts
EVENTS = [
  { id: 'condition_up', text: '厩務員が言うには…', effect: { condition: 1 } },
  { id: 'temper_rough', text: '気性が荒くなっている',
    choices: [ { id:'turnout', effect:{ forceRest:true } },
               { id:'push_through', effect:{ currentMult:{iq:…}, temper:+10 } } ],
    defaultChoice: 'turnout' },
  { id: 'awakening',    text: '素質が開花した！', effect: { potentialMultOneRandom: 1.05 } },
]
```

**増やすときは表に1行足すだけ**で、コードは書きません。

### ★§7.4 との順序を、テストで固定しました

§7.4 は**毎週 condition を再判定**します。イベントの「調子+1」を
**再判定より前に**適用すると、**再判定が上書きして効果が消えます**。

```
再判定の「後」に適用 → 効く（テスト①）
再判定の「前」に適用 → ★効果が消える（テスト②で明示）
```

**「イベントが出たのに何も起きない」が静かに成立する**形なので、
**繋ぐ順序そのものをテストにしました。**

### ★放置の既定を「強行」にしていません

`temper_rough` の既定を「強行」にすると、**放置しているだけで気性が悪化し続けます**。
「放牧」を既定にし、**既定が気性を悪化させないことをテストで固定**しました。

---

## 3. ★照会（§7.6 に数値が無いもの）

| # | 内容 | 暫定値 |
|---|---|---|
| **Q-P3-12** | 「低確率」の値。★数値があるのは**素質開花の1%だけ** | `COMMON_EVENT_PROB = 0.02` |
| **Q-P3-13** | 「強行」の **IQ+ の量**（気性+10 は明記） | `PUSH_THROUGH_IQ_MULT = 1.03` |
| **Q-P3-14** | 「放牧」を選んだときの効果 | ★休養と同じ扱い（`forceRest`） |
| **Q-P3-15** | 素質開花が**どの形質か** | ★5能力から無作為に1つ |
| **Q-P3-16** | **選択肢を選ばなかった場合**（NPC・放置）の既定 | ★`defaultChoice`（放牧） |

★Q-P3-12・13 は**較正定数として登録し、変異が捕まることを確認済み**です。

```
COMMON_EVENT_PROB = 0.5     → 1件が落ちる（毎週起きたら特別感が消える）
PUSH_THROUGH_IQ_MULT = 1.0  → 1件が落ちる（強行の利得が消え、選ぶ理由が無くなる）
```

---

## 4. 合格基準の現状

| # | 基準 | 状態 |
|---|---|---|
| B-1 | 1頭を誕生から引退まで通せる | **★staging 待ち**（実 DB での通し） |
| B-2 | V-7 故障率 25〜35% | **未測定**（★測定条件の確定が先・指示書 §4） |
| B-3 | V-11 の構造報告 | **staging 待ち** |
| **B-4** | `current ≤ potential` が全経路 | **★PASS**（成長・故障・イベントの3経路すべてで固定） |
| **B-5** | 週番号が時刻から決まる | **★PASS**（14件・欠落も重複もしない） |
| **B-6** | 調子・疲労が §8b に接続 | **★ロジックは PASS**・配線は staging 待ち |
| B-7 | 引退馬が NPC と同一の遺伝エンジン | **未着手**（G-8） |
| B-8 | P0/P1/P2 の全ゲート回帰 | **未測定**（P3 完了時に） |
| **B-9** | テスト・型・変異試験 | **★558 tests / typecheck 0** |

★**V-14 は ①② PASS / ③は EP 効率で PASS・時間軸で FAIL** です（前便のとおり・`menuIntensity` には触っていません）。

---

## 5. 次にできること

**staging が来るまでにできるのは、B-2（V-7）の測定条件の確定です**（指示書 §4）。
分母が故障で動くので、**定義が判定を作ります**。**定義案を出して照会に上げる**形で進めますか。

それとも **G-8（引退と繁殖入り）**の純ロジック部分を先に進めますか。
