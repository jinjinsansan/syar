# 裁定 — 既存部分の全体監査（`REPORT_AUDIT_20260914.md`）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-14 ／ レビュー側 → 開発側
**対象**: `REPORT_AUDIT_20260914.md`
**対象 HEAD**: `620e2c1` ／ ブランチ `p4/race-30sec-cuts`
**判定**: **受理**（監査報告として正確。重い指摘は再現した）。修正は指示書 `DEV_INSTRUCTIONS_AUDIT_FIX_20260914.md`（第 1 便）で出します。

> 凡例（R-13）: **✔ 再現** ＝ レビュー側がコードを読むか計算して確かめたもの ／ **△ 未再現** ＝ 開発側の主張のまま ／ **✖ 不成立**

---

## 0. 一行で

報告は正確です。**H-1・H-3・H-6 はレビュー側でも再現**しました。H-2・H-4・M-1・M-3 は**コードの経路まで確認**しています。そのうえで、報告に書かれていない点を 1 つ加えます。**H-4 の穴が実際に使われていた場合、1 回の呼び出しで他人の EP を全額消せました**（§3-1）。

---

## 1. レビュー側の再実行

| 検査 | 開発側の報告 | レビュー側 |
|---|---|---|
| `npm run typecheck` | 通過 | **通過**（EXIT=0・HEAD `620e2c1`） |
| `npx vitest run` | 1 failed ／ 1600 passed | **一致**: `Test Files 1 failed \| 152 passed (153)` ／ `Tests 1 failed \| 1600 passed (1601)` ／ EXIT=1。赤は `edit-grammar-audit.test.ts` ⑨ だけ（01:03〜01:11。実行の前後で HEAD は `620e2c1` のまま・追跡ファイルの変更 0 件。未追跡が 13 → 16 件に増えたのは、監査報告と本裁定・指示書の 3 本） |
| `npm run verify` | 総合 PASS | **未再実行**（遺伝エンジンは今回の修正範囲外のため） |

---

## 2. 指摘ごとの照合

| # | 判定 | レビュー側が確かめたこと |
|---|---|---|
| **H-1** 払戻が 1 PP 少ない | **✔ 件数まで一致** | 独立に総当たりした。購入額 100〜10,000 × オッズ 1.0〜999.9 の **999,000 通りのうち 31,577 通り（3.16%）で少なく、多くなるのは 0 件**。同着で 2 等分なら同じ 3.16%、3 等分なら 16,278 通り（1.63%）。`100 * 2.3 = 229.99999999999997` → 229 |
| **H-2** オッズを四捨五入して保存する | **✔ 経路 ／ △ 大きさ** | `oddsFromProbability`（`balance.ts:182-186`）は丸めない値を返す。`odds.ts:107` → `pg-store.ts:192` の `unnest(... numeric[])` を経て、`numeric(9,1)` の列に入る時点で DB が四捨五入する。**「最大 +5.24%」は再現していない** |
| **H-3** 週送りで持ち馬が全頭休養に落ちる | **✔** | `0020:197` の `assert_setup_complete()` は NPC 馬の分岐（`:206`）より前にあり、無条件。`0019` は `auth.uid()` が null なら `'未認証'` を投げる。ワーカーは `pg.Client` で DB に直接つなぐ（`main.ts:43-45`）。`training-runner.ts:200-205` はどんな例外も `epShort += 1; menu = 'rest'` にする |
| **H-4** 関数の実行権限が anon に残っている | **✔ 移行ファイルの記述 ／ △ DB の実際の状態** | `spend_training_ep` の取り消しは `public` と `authenticated` だけで、**`anon` が抜けている**（`0013:98,100`・`0014:79-80`）。一方 `0019:69` は `assert_setup_complete` について **`anon` を明示的に外している**。同じ案件の中で、anon を別に外す必要があることは知られていた。Supabase が新しい関数に anon の EXECUTE を付けるかどうかは、DB を測るまで確定しない |
| **H-5** 権利方針 | **✔ 一部** | `race/page.tsx` の `HORSE_NAMES` は 09-13 の引継ぎ書にも書かれている。それ以外の行は未照合 |
| **H-6** 本番で開発卓が開く | **✔** | `race/page.tsx:2114-2115` は `params.get('dev') === '1' \|\| (NODE_ENV === 'development' && …)`。**`?dev=1` を付ければ実行環境に関係なく開く** |
| **M-1** 締切が守られない | **✔ 一部** | `0020:112` は `scheduled_at <= now()` で締め切る。`scheduled_at` は発走時刻（`pg-store.ts:226` の「発走後 60 分で中止」の境界がこの列）。正典 §9.6 は**発走 30 秒前**、`cycle.ts` の `salesClose` も 9:30 で、DB だけが食い違っている。発売開始の判定も無い。レース行をロックしない件は △ |
| **M-3** 自馬制限 | **✔** | 正典 §9.5-3 は「自馬が複数いれば**全頭を含む**組合せのみ」。`0020:151-156` は `exists`（1 頭含めば通る） |
| M-2・M-4〜M-10・§4 | △ | 今回は照合していない。第 2 便以降で指示書に載せる前に再現する |

---

## 3. 報告に無かったもの（レビュー側の発見）

### 3-1. ★H-4 の穴の大きさは「週ごとに少しずつ」ではなく「1 回で全額」

`spend_training_ep(p_horse_id, p_week, p_amount)` は、**`p_amount` を呼ぶ側が決め**、**呼んでいるのが誰かを見ていません**（`0014:17-76`。0020 で入るまでは `auth.uid()` を 1 度も参照しない）。

→ anon に EXECUTE があった場合、**残高と同じ額を 1 回渡せば、その馬の持ち主の EP が 0 になります**。報告 §2 の「週を変えれば毎回別物」は、複数回に分けて削れるという話でした。実際は 1 回で足ります。

**実害の見込み**: 0013〜0019 が当たっていた期間は、利用者も持ち馬もいなかったはずです。ただしこれは推測です（R-25）。**指示書 AF-0 で本番を 1 回だけ読んで閉じます**。

### 3-2. 既存テストは払戻の上側しか見ていなかった（R-2）

`settle.test.ts:261-267`「払戻が購入額×オッズを超えない」は `toBeLessThanOrEqual` だけです。**少なく払う誤りは原理的に捕まりません。** H-1 が 3.16% の組合せで起きていたのに緑だった理由はこれです。

さらに、そこで使っているオッズ `3.33`・`99.99`（`:258`・`:263`）は、**`numeric(9,1)` の列からは出てこない値**です。**本番では起こりえない入力だけを試し、本番が実際に渡す入力は試していない**、という形でした（R-30 の家族）。

### 3-3. 報告の直し方の案には、同着の割り算が抜けている

報告 H-1 の案 `Math.floor(stake * Math.round(odds * 10) / 10)` には `/ mult` がありません。**`Math.floor(stake * tenths / (10 * mult))`** とすれば、999,000 通り × mult 1/2/3 のすべてで整数の正解と一致しました（誤差 0 件）。

### 3-4. `Number(b.odds_at_purchase)` を通すこと自体が誤差の入口

`pg` は `numeric` を文字列 `"2.3"` で返します。`payout.ts:70` がそれを `Number()` で浮動小数に変え、`settle` の中で 10 倍に戻しています。**十進の文字列から整数（0.1 単位）へ直接変換すれば、浮動小数を 1 度も経由しません。**

### 3-5. 正典にオッズの丸め方の規則が無い

§9.2〜§9.4 には、式・控除率・上限は書かれていますが、**表示と払戻に使うオッズを何桁で、どちら向きに丸めるか**が書かれていません。H-2 は実装の誤りというより、**決まっていなかったところを DB の列型が勝手に決めていた**ものです。→ §4-1。

### 3-6. D-080 は「ワーカーだけが呼ぶ関数」を想定していなかった

D-080 は「すべての書き込み RPC の先頭で `assert_setup_complete()` を呼ぶ」です。これは**利用者が呼ぶ RPC** を前提にした決定です。`spend_training_ep` は**ワーカーしか呼びません**（Web からの rpc 呼び出しは 0 件・報告 §5）。

走査型のメタテスト（`rpc-guard.test.ts:71-82`）がこの関数にも判定を課し、それを満たすために入れた 1 行が H-3 を生みました。**検査を満たすための変更が副作用の経路になった例です（R-26）**。→ §4-2。

### 3-7. 切り捨てるとオッズが 1.0 を下回る目がありうる

確率が高い目では、控除率を引いたオッズがもともと 1.0 近くになります。0.1 単位で切り捨てると、**買った額を下回る配当（例: 0.9 倍）**が今より出やすくなります。正典に最低オッズ（いわゆる元返し）の規則はありません。**実際にどれだけ出るかを AF-0 で測ってから、オーナーに諮ります**（測る前に最低値を決めない）。

---

## 4. 決定

### 4-1. ★オーナー決定（2026-09-14）: オッズは 0.1 単位で切り捨てる

- 表示・保存・払戻のすべてで、**同じ 0.1 単位の切り捨て値**を使う。DB の列型に丸めを任せない
- 払戻は**整数だけで計算**する（浮動小数を経由しない）
- → **正典への起票候補 D-094**（§9.2 に追記）

### 4-2. レビュー側の裁定: `spend_training_ep` はワーカー専用として閉じる

| | |
|---|---|
| 採る | **anon・authenticated・public から EXECUTE を剥がす**。ワーカー専用関数の登録簿を作り、「利用者のロールに実行権限が無いこと」を**移行ファイルの走査と実際の DB の両方**で確かめる。`assert_setup_complete()` はこの関数からは外す |
| 採らない | 関数の中で「ワーカーからの呼び出しなら検査を飛ばす」と分岐する形。接続ロールの名前はプーラ（Supavisor）を挟むと変わりうる（`main.ts:46-49` の註記どおり）。**判定を誤ると、広く通る側に倒れる**（R-27） |
| 順序 | **H-3 と H-4 は同じ便で**（報告 §2 の指摘どおり）。検査を外すのは権限を剥がすのと同じ移行ファイルに限る |

→ **正典への起票候補 D-095**（D-080 の適用範囲を「利用者が呼ぶ RPC」と明記し、ワーカー専用関数の扱いを追加）

⚠️ D-094・D-095 は、**正典への書き込みをオーナーの承認後に行います**。番号は確保済みです。

### 4-3. 着手の順番

報告 §6 の推奨順を採ります。**第 1 便 = H-1・H-2 → H-3＋H-4**。H-5・H-6・M-1〜M-3・M-6・M-7 は第 2 便以降とし、△ の項目は**再現してから**指示書に載せます。

P4（レース演出）とは触るファイルが重なりません（betting・worker・db・cli のテストだけ）。**どちらを先に進めるかは、オーナーが開発側へ投入するときに決めてください。**

---

## 5. 報告について

- **再現した事実と未検証の主張を分けて書いてあったので、照合がとても速く済みました。** 次の便以降もこの形を続けてください
- 監査の間、製品コードに触れず、DB にも本番にもつながなかったのは正しい判断です
- 権利方針に当たる文字列を本書にも書かず、「ファイル:行」だけで示したのも正しい判断です
- 直してほしい点は 1 つだけです: **直し方の案（H-1）にも再実行の裏付けを付けてください**（§3-3 のように、案そのものに穴がありました）
