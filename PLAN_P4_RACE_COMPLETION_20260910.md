# レース演出を完成させるための計画書

作成: 2026-09-10 / 担当: Codex

**目的: 今のデフォルメ馬を使い、発走から結果まで、参考映像のようにレースの流れと競り合いを自然に見続けられる完成版を作る。**

**今回の直線修正に対するオーナー判定は「とりあえずはOK」。この画を今後の基準にし、残りの場面を仕上げる。** 本書は計画であり、この便では製品コードを変更しない。

## 1. 完成形と、今回固定する方針

完成形は、ゲートで待ち、発走し、位置を取り、向正面を走り、コーナーを経て、直線の競り合い・ゴール・勝馬紹介・リプレイ・結果へ進む一本のレースである。

- 走行の主役は合格済みの真横のデフォルメ馬。今のキャラクター、走行コマ、毛色・勝負服を使う。
- 見せる内容に応じて真横の引きと寄りを使い分ける。競り合う数頭が読み取れる大きさを基準にする。
- 発走の踏み出しや曲がる姿勢を、足りない素材のまま長時間見せない。短い挿入画面と既存の走行で接続する。
- カット数は減らさない。既存の境界と尺を維持して中身を置換する。必要な内部カットの追加は可。
- 着順・走破時刻・馬の位置と速度のモデル・乱数・台帳・サーバー判定を変更しない。接戦でないレースに接戦を作らない。
- 参考から採るのは、前後関係の読みやすさ、画角の使い分け、切替の明瞭さ、レースの流れ。具体的な映像・音・画面意匠は転用しない。他社固有名称は文書・コード・表示に書かない。
- 新たな後方・正面・俯瞰の走行素材、馬の全面描き直し、3D化、別レンダラーへの移行は、この完成工程の前提にしない。

**最初に既定のレースを一本通して完成させる。その後、同じ演出が他の展開・コース・端末でも成立するところまでを完成範囲とする。** 公開・配備は、完成候補の確認後の別作業とする。

## 2. 現在地

基準は HEAD `b9922b6` に、今回オーナーが見た未コミットの直線修正を加えた作業ツリー。HEADだけに戻すと、今回のOKの画まで失うため、その状態を比較元にしてはいけない。

| 場面 | 現在の扱い | 完成までにすること |
|---|---|---|
| ① ゲート | 閉・開の画がある。単独の合否は未取得 | 現在の待機画を使い、②との連続再生で仕上げる |
| ② 発走 | 待機から全速の走行へ跳ぶ。踏み出しコマがない | 発走の短い挿入画面から、真横の走行へ繋ぐ |
| ③ 最初の位置取り | 長い斜め前の区間が残る | 真横の走行と短い隊列表示へ置換する |
| ④ 向正面などの真横 | 合格済み | 維持。③⑤との接続と表示物の重なりだけ確認する |
| ⑤ コーナー | 既定経路にコース図カットインが実装済み。合否は未確定 | 新規に作り直さず、位置・説明・出入りを仕上げる |
| ⑥ 最後の直線 | 今回の修正を「とりあえずOK」 | 今後の見え方の基準として保持する |
| ⑦ ゴール | オーナー合格済み | 入線と⑥からの接続を保持する |
| ⑧ 勝馬紹介 | 横向きの追走と勝馬帯が稼働し、合格済み | 保持。重い後方アニメは再開しない |
| ⑨ リプレイ | 稼働し、合格済み | 保持。リプレイと本編の区別を確認する |
| 着順ボード | ゴール後の録画として確認済み | 確定結果の表示と次の画面への接続を保持する |

今回の直線比較は [比較ページ](http://localhost:3210/contest-review/index.html)、実装・検証の記録は [直線修正報告](docs/contest-direct-review-20260910.md) を参照する。

過去の「残件21秒」は③13秒＋⑤3秒＋旧直線の斜め前5秒であり、②を含まない。その5秒は今回置換済み。「21秒」を残件の合計として引き継がない。

また、過去の「全体92秒」「勝馬5秒」などは録画標本の区間表であり、現在の定数・イベント境界と同一と決めつけない。以下の13秒・3秒は構成を説明する目安。実装では現在のレースが持つ切替地点・発走・入線イベントから区間を取る。

## 3. 残る場面の作り方

### 3.1 ③ 最初の位置取りを、最優先で直す

最も長く不合格の走行を見せている区間を先に処理する。基本構成は次の一案に絞る。

| 13秒区間の目安 | 見せるもの | 観客に分かってほしいこと |
|---|---|---|
| 最初の5秒 | 真横の引き。先頭と直後の集団 | 誰が前へ出て、誰が追っているか |
| 中央の2秒 | 馬番付きの簡潔な隊列表示 | 先頭・中団・後続の位置関係 |
| 最後の6秒 | 対象・画角を変えた真横 | 隊列がどう落ち着き、向正面へ続くか |

コースごとの区間が13秒でない場合は既存の尺の中で配分する。隊列表示は原則1〜2秒、長い区間でも3秒を超えない。前後の走行へ残りを配分し、区間を延長しない。

実装上の条件:

- 真横用のカメラ・注視点・背景を一組として設定する。正面カメラのまま馬画像だけを横向きへ交換しない。
- 先頭と追走集団が読めれば成立とする。全12頭を常に大きく映すことは要求しない。
- 隊列表示は、その時点で画面に使っている位置から作る。馬番・枠色・自馬の印を既存UIと揃える。
- 内外の位置まで表示できるデータがある場合だけ示す。無い場合は前後関係の図として表示し、進路争いを作り足さない。
- カメラの切替前後で走行位相・馬番・毛色を保ち、表示時間を進めたまま接続する。
- 実況者だけを13秒映す構成にはしない。図も13秒連続で表示しない。

曲率のある区間で横の追従が不自然になる場合は、まず画角と注視点を直す。なお成立しない場合の代替は、実際の位置を読む隊列図を走行画に併記し、走路の内外を細かく描写しない構図。新たな方向素材の生成ループには戻らない。

### 3.2 ② 発走は、短い編集で成立させる

基本構成は **閉じたゲートで待機 → 発走の挿入画面 → 発走後の真横の馬群**。

- 閉じたゲートとカウントダウンは既存のものを使う。
- 既存の発走イベントに合わせ、0.6〜1.0秒を初期値とする不透明な挿入画面を出す。既存の実況者・帯・発走文言で構成する。
- 待機から全速への姿勢の跳びを、その挿入画面の間に画面外で済ませる。
- 挿入画面の後は、その時点の真横の走行へ戻す。ゲートから馬が飛び出す正面画へ戻さない。
- 発走カットの終わりまで真横の馬群を見せ、③へ繋ぐ。6〜13秒などの固定値で別のタイマーを作らない。
- 扉の開放を2秒へ引き伸ばすことや、新しい踏み出し8コマの制作を完成条件にしない。

実況者の画で発走感が弱い場合の代替は、手持ちの開いたゲートの寄りと「スタート」の短い帯を使う。馬の姿勢の切替を見せない原則は共通。開閉の中間アニメを追うより、発走の出来事と次の走行が一続きに伝わることを優先する。

### 3.3 ⑤ コーナーは、既存のコース図を完成させる

現在の `raceCutInFor` / `drawCourseMapCutIn` を利用する。目安3秒の区間を維持し、次を整える。

- コースのどの部分を通り、どちらへ進んでいるかがすぐ分かる。
- 現在位置・区間名・実況を、実際に表示しているレース状態と揃える。
- 「4コーナーを回って直線へ」という固定文言が、実際の位置に合うか確認する。合わない場合は状態に合う説明に直す。カメラ区間名だけを根拠に断定しない。
- 不合格の走行を透かさない。小さいコース図との二重表示を避ける。
- 図を読める時間を確保し、区間の最後でその時点の真横走行へ戻る。
- 高所のカメラ・後方素材の新規採用は完成を妨げない追加案として後回しにする。

### 3.4 ①④⑥⑦⑧⑨は、接続と画面全体を整える

既に成立している走行を起点に、前後の場面を繋ぐ。通常の修正対象はカットの継ぎ目、HUDの配置、字幕の出入りであり、承認された馬体や走行周期を毎回変更しない。

- 寄り・引きで見せる対象が分かり、同じ馬を追える。
- 同じ真横素材でも、切替位置で画角や対象が変わり、別カットとして見える。
- ゴール線、勝馬帯、リプレイ見出し、結果の順序を保持する。
- 本編中の競り合いに、不要な全画面の図や実況者を挟まない。
- 馬が密集したときに、重要な馬の顔・馬番をHUDが長時間隠さない。外接矩形の交差だけで合否を決めず、実画面も見る。

## 4. 実装順と、各工程の成果物

| 順序 | 作業 | その工程で残すもの | 次へ進む条件 |
|---|---|---|---|
| 0 | 今回OKになった状態を基準として保存 | 対象差分・素材・レース条件・通常の再生経路・通し尺の記録 | 別セッションでも同じ画を開ける。共有作業の変更を巻き込まない |
| 1 | ③の真横＋隊列表示を実装 | 前後を含む序盤の動画。既存画面に組み込んだ状態 | 位置関係が読め、斜め前の走行が残らない |
| 2 | ①②の待機→発走→真横を実装 | イントロから③までの連続動画 | 姿勢の跳びを露出せず、発走したことが伝わる |
| 3 | ⑤の図と全カットの接続を仕上げる | **発走前から結果までの通し第1版** | 全場面が埋まり、同じ入口から最後まで再生できる |
| 4 | 通しでHUD・実況・音・開始終了操作を仕上げる | **完成候補の通し映像と実画面** | 途中で違和感や説明の矛盾がなく、通常再生が止まらない |
| 5 | 他の展開・コース・端末で確認し、共通の問題を修正 | 再現条件付きの確認結果と完成候補 | 下記の完成条件を満たす。未確認の対応環境を合格扱いにしない |

今回の直線が既にOKで、⑤も実装済みになったため、以前の「⑤から一つずつ新規実装」には戻らない。**次の実装着手は③。工程3までを優先し、早く一本のレースとして見られる状態にする。**

オーナーに見ていただく節目は、原則「通し第1版」と「完成候補」の2回。局所の確認・録画・再現・テストはCodex側で行い、毎回ユーザーへ測定作業を戻さない。大きな見せ方の変更が必要なときだけ、具体的な動画を添えて相談する。

## 5. 画面と音の仕上げ

### 5.1 画面

大きさの数字を参考映像へ機械的に合わせるのではなく、今回OKとなった直線を目視の基準にする。序盤は隊列を見渡せる引き、直線は攻防が読める寄り、勝馬紹介は一頭を見せる構図とする。

背景の継ぎ目、馬体の二重写し、突然の反転、カメラ内での不自然な拡縮、四角い影など、通常再生で目立つ問題は工程4で対処する。孤立した1コマの数値だけを改善して、通しの見え方を悪化させない。

### 5.2 実況と音

現在確認できているのは、字幕・実況者の立ち絵・口パク。これを音声実況が完成済みである根拠にはしない。工程0で実際に音が出る経路と既存素材を確認する。

- 字幕は現在の位置と出来事に同期し、コーナーの説明が直線へ残り続けるなどの矛盾を除く。
- 完成版の音は、発走音、蹄音、場内の環境音、ゴールの強調を基本構成にする。利用可能な自作・適切な利用条件の素材を使う。
- 蹄音や歓声を場面ごとに不自然に切らず、画面の切替をまたいで繋ぐ。音の重なり過ぎや音割れを避ける。
- 開始操作後に音が有効になり、停止・ミュート・再開に追従する。
- 全馬名の音声読み上げや、新しい実況音声システムの開発は別工程。これを待って映像の完成を止めない。

音素材が未整備なら、工程4の新規作業として残件に明示する。無音の比較動画だけで「音を含む完成」とはしない。

## 6. 同じところを回り続けない進め方

1. **直す目的を一つにする。** 例: 「発走時の姿勢の跳びを見せない」。関係のない脚の太さ・毛色・走行周期を同時に調整しない。
2. **一案を実装し、実画面で確認する。** 大量の候補を並べ、選定だけで数日使わない。
3. **同じ箇所の見た目調整は、初回＋修正1回を目安にする。** なお成立しない場合は§3の代替構成へ移る。同じ仮説の微調整を続けない。機能不具合や結果不整合をこの回数で放置することはしない。
4. **OKの区間を基準として残す。** 共通部品の変更が波及する場合は、その区間の動画・画像も確認する。「念のため」だけの作り直しはしない。
5. **撮影条件を読み返す。** 正しいシーク、倍率1、使用素材、レース条件を確認する。全コマ同じ画像、時刻が動いていない動画、未読込の黒画面は提出しない。
6. **通常再生とコマ送りを分ける。** 等速に書き出したコマ列は構図比較用。再生の滑らかさ・停止・読み込みは通常再生で確認する。
7. **提出先を一つにする。** 比較用の既存URLを残し、完成確認用のページでは最新の通しと実画面へ迷わず入れるようにする。古い実験動画を最新として案内しない。

## 7. 検証の範囲

### 7.1 最初の一本

既定コース・seed 42を基準に、今回OKの直線と同じ設定で作る。seed 99は別展開の確認に使う。見栄えのよい接戦だけを選んで完成を判断しない。

毎工程で対象区間と前後を確認し、工程3で一度通し、工程4以降は通常再生で発走前から結果まで通す。

### 7.2 展開・コース

| 観点 | 確認するもの |
|---|---|
| 展開 | 接戦、先行馬の粘り、後方からの追い上げ、単独先頭・縦長。実際のレースから選び条件を保存する |
| 走路 | 左右回り、芝・ダート、短い直線・長い直線、短距離・長距離を代表例に含める |
| 全収録レース | 現在のレース一覧全件で、カットの欠落・逆順・区間長0・コース外の表示・描画例外を機械的に確認する |
| 外見 | 読み込まれる馬体タイプ・毛色・勝負服を確認。タイプ切替で旧素材へ戻らない |

コース別の手置き秒数や、seedごとの見栄え補正を増やさない。問題が出た場合は、区間・カメラ・図の規則で解決する。全件の自動確認と、代表例の目視確認は区別して報告する。

### 7.3 端末と再生経路

- PCの通常入口と、携帯の通常入口で確認する。開発用URL・シーク操作だけで成立していても完成にしない。
- PCでの通常素材経路と携帯での事前加工済み素材経路が、同じキャラクター・構成・位相を使うことを確認する。
- 代表となるPC、携帯の各対象ブラウザで、初回読み込みから結果まで、停止・再開・全画面・戻る操作を通す。
- 連続3レースでメモリが増え続けないこと、素材切替時の停止やタブ落ちがないことを確認する。新しい全馬分の加工キャンバスを無制限に増やさない。
- 滑らかさは通常再生で30fpsを目標とする。平均値だけで判断せず、500ms以上の描画停止や頻繁なカクつきがないかを確認する。未達なら端末・区間・原因を明記し、比較動画で隠さない。
- 実機をCodex側で利用できない場合だけ、完成候補のURLと最小限の確認操作をオーナーへ依頼する。机上の計算で実機合格を宣言しない。

### 7.4 不変条件

各修正でカットの本数・順序・境界・総尺、確定着順・タイムが比較元と一致することを確認する。変更したショットIDは旧区間との対応を明記する。純粋な描画修正でエンジン・台帳・サーバーへ差分が出た場合は修正範囲を見直す。

型チェックと変更箇所に関係する既存テストを実行する。旧い構成そのものを固定したテストは目的を確認し、新しい構成で守るべき条件へ更新する。テストを通すために却下済みの画へ戻さない。

## 8. 完成の判定

次の条件をすべて満たしたものを完成候補として提出する。

| 条件 | 判定方法 |
|---|---|
| 9点と結果表示が一本のレースとして揃う | 開始から結果まで通常再生する |
| 発走の姿勢の跳び、長い不合格の斜め前走行が残っていない | 該当区間とカットの継ぎ目を確認する |
| 先頭・自馬・追走する馬を見失わず、実際の攻防が読める | 接戦と縦長の両方を目視する |
| カットを削って問題を隠していない | 区間一覧と通し尺を比較する |
| 今回OKの直線と、合格済みのゴール以降を壊していない | 基準動画と接続を比較する |
| 画・字幕・効果音が同じ出来事を伝える | 音ありの通常再生を確認する |
| 通常入口・対応コース・対象端末で再生できる | §7の結果を残す |
| 着順・タイム・台帳・サーバー判定が変わっていない | 差分と不変条件の確認結果を残す |

**技術的な検査に通ったことと、見え方の完成判定は分ける。最後はオーナーが通しを見て「これでよい」と判断した状態を完成とする。** 「とりあえずOK」は作業を先へ進める基準であり、レース全体の最終承認には置き換えない。

## 9. 今回の計画で後回しにするもの

全方向の走行素材、新しい踏み出しモーション、後方の勝馬アニメ、3D化、特殊なカメラの追加、全馬名の音声実況は、現在の素材による完成後の拡張とする。

ただし、通常再生の停止、明らかな姿勢・向きの破綻、HUDによる主役の長時間の遮蔽、結果の不整合、対応端末での再生不能は、完成前に直す。拡張機能と未解決の不具合を混同しない。

本計画の実装が始まったら、工程ごとの結果をこの計画に対応づけて記録する。新しい計画書を次々に増やすより、一本の通し映像を更新して完成へ進める。

## 実施記録

2026-09-10 に工程1〜3の画面実装を開始した。

- ③は既存の100〜528m区間の終端と尺を保ち、横走行・実位置から描く隊列図・別の横走行へ分割した。カットは削減せず、2つ増やしている。
- ②は発走直後0.8秒を不透明な発走帯で覆い、踏み出し姿勢のない全速走行への切替を露出しないようにした。
- ⑤は既存のコース図を継続利用し、ショット名だけで通過地点を断定する文言を現在位置を示す文言へ変更した。
- 型検査、描画パッケージ全体のテスト、ならびにカットイン・切替・台本の関連テストを実行した。実画面の連続録画は、既存の自動撮影ブラウザ接続が応答しないため、接続復旧後に工程4の通し確認として行う。

同日、実画面の確認を追加で実施した。

- seed 42: 発走から位置取り、コーナー図、直線、勝馬紹介、リプレイ、確定結果までを区間ごとに撮影した。結果画面まで到達し、撮影中の描画例外は0件だった。
- seed 99: 別展開でも発走挿入、隊列図、ゴール、勝馬紹介、リプレイ、確定結果を確認した。撮影中の描画例外は0件だった。
- 通常再生の初回計測では、操作部を毎フレーム更新する負荷で表示時計が遅れた。操作部の更新を10fpsへ間引いた後、発走から位置取りまでの通常再生は7.75秒から15.85秒まで8.1秒進んだ。
- 総尺は条件により約85〜86秒であることを実測した。過去の92秒という録画標本を現在の終端としては扱わない。
