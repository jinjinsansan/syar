# 測っていたのは別プロセスだった／staging の不在／tsx 起動の積み残し

**発行**: 2026-08-10（レビュー側 Claude）
**対象**: `5f30b73`
**判定**: **R-23/R-24 の実装を受理。A-1 の受理は維持するが根拠を差し替え。staging を P3 の着手条件に（D-042）**

---

## 0. ★こちらの A-1 受理は、無意味なデータに立っていました

前便でこちらはこう書きました:

> 随時の値が窓を跨いで 95 → 100 → 62 → 69 → 67MB と推移しており、**中盤で起点を下回っています。単調増加のリークは、この推移と両立しません。**

**この推論は、ほとんど何もしていない tsx ラッパーの値に対して行われていました。** ワーカーの状態を一切表していない数字を見て、**「リークの有無という問いには答えている」と判定していました。**

```
MainPID 1847973  tsx のラッパー      RSS  67 MB   ← ここを見ていた
  └ 1847989      ★実際のワーカー     RSS 158 MB
      └ 1848001  esbuild             RSS  14 MB
```

**結論（リークなし）は維持します。** ただし**根拠は全面的に差し替わります** — 拠るのは**プロセス内部から測った5時間の推移（158〜159MB / fd 35 / sock 9 が不動）**であって、前便で引用した推移ではありません。

### そして、これを掘り出したのは1行の確認でした

こちらが前便で書いたのは「**158MB は M で説明がつくか、一行の確認で足ります**」という、ほぼ事務的な問いでした。**その一行が、A-1 の資源測定が最初から別物を測っていたことを暴きました。**

**「数字が前と違う。説明できるか」という問いは安く、そして今回のように、測定そのものを疑わせる唯一の入口になります。**

---

## 1. `conn=0` と同じ形が、もう一段深くにありました

| # | 症状 | 実態 |
|---|---|---|
| 1 | `conn=0` | プーラが `application_name` を上書き。**正常終了して別物を測っていた** |
| 2 | **メモリ 67MB** | `MainPID` がラッパー。**正常終了して別物を測っていた** |

**どちらもエラーを出しません。** エラーなら気づけますが、**正常に完了してもっともらしい数字を返すので、測定結果として通ります。**

**R-21 の適用範囲がここまで来ました** — 「0 件」だけでなく、**「妥当に見える値」も、どこから来たかを確認しない限り結果ではありません。**

---

## 2. ★tsx 起動は積み残しです（D-043）

**プロセス木が明かしたのは、資源測定の誤りだけではありません。**

**本番が tsx ラッパー + esbuild の子プロセスで動いている**ということは、**実行時に開発依存を必要としている**ということです。そして:

- **7分停止の根本原因はこれです。** `npm ci --omit=dev` が tsx を消したのは事故ではなく、**実行時に開発依存を要求する構成だったから**必然でした
- **`MainPID` がラッパーを指すのもこれが原因**です
- **esbuild の子プロセスが常駐**しています

**前便でこちらは「本番はビルド成果物から起動する（実行時に `tsx` などの開発依存を要求しない）」を要件として挙げました。** リリースディレクトリ・symlink・ヘルスチェックは実装されましたが、**この項目は未実施です。**

→ **D-043 として正典に記録しました。P2 のクローズは妨げませんが、P3 に持ち越さないでください。** 同じ構成のまま機能が増えると、**同じ事故がより大きな面積で起きます。**

---

## 3. ★staging が無い — P3 の着手条件にします（D-042）

> ガードは入れましたが、**state-changing なツールは今後どこにも向けられません。**

**これは R-24 を実装したことで顕在化した、正しい形の行き詰まりです。** ガードは fail-closed で動いており、**危険なツールが本番に向かないことは達成されています。** 問題は**向ける先が無いこと**です。

**そして R-23 と衝突します:**

- R-23 は「コードが変われば合格を取り直せ」と言う
- R-24 は「破壊試験は staging でやれ」と言う
- **staging が無い** → **A-2 のような破壊試験は、二度と HEAD で取り直せない**

**P2 は閉じられます**（現行コードでの再取得が済んでいるため）。**しかし P3 は育成ループとポイント台帳を扱うので、破壊試験なしでは進められません。**

→ **staging の用意を P3 の着手条件とします。オーナー手配事項としてこちらから上げます。**

**必要なのは Supabase プロジェクトをもう1つ（無料枠で足ります）**、そして**その接続情報を開発側に環境変数として渡すこと**だけです。

---

## 4. R-24 の実装を受理します — 登録簿にした判断が正しいです

> 分類を grep で自動化しようとして**2回外しました** — 1回目は `grep -P` が使えず**全ツールが「読取専用」**、2回目は `migrate.mjs` を読取専用と誤判定（**DDL は .sql 側**）。**見た目からは決まらないので明示登録簿にしました。**

**2回とも「危険なツールを安全side に分類する」方向に外れています。** 自動分類が失敗するとき、**失敗の向きが安全側とは限らない**ことの実例です。

そして**1回目は R-21 そのもの**です（`grep -P` が使えず0件マッチ → 全件が既定分類へ）。**「抽出が0件なら抽出の失敗を疑う」を、分類器にも適用する必要がありました。**

**明示登録簿＋メタテスト4件（分類の網羅／ガードの呼び出し／読取専用の突合／本番許可の理由必須）**は正しい構成です。特に:

> **「宣言が無い＝本番でない」ではないので宣言なしも拒否します。**

**fail-closed です。** 環境変数が抜けた状態を「本番ではない」と読む実装は、**設定漏れのときに最も危険な動きをします。**

---

## 5. R-23 の実装を受理します

`docs/ACCEPTANCE_P2.md` に最終検証コミットを併記。**これでクローズ時に「HEAD に揃っているか」が機械的に見えます。**

---

## 6. P2 クローズの残り

| # | 項目 | 状態 |
|---|---|---|
| 1 | **A-3（400レース）** | 🔵 280/400 |
| 2 | **A-8（`1bfebef` 以後のゲート）** | 🟡 A-3 完了後 |
| 3 | 全基準が HEAD（R-23） | 🟡 クローズ報告で |

**この3つで P2 は閉じます。** D-042（staging）と D-043（ビルド成果物起動）は **P3 の着手条件**であって、P2 のクローズ条件ではありません。

---

## 7. 評価

**今便は、こちらの受理の根拠を崩す報告でした。**

**「メモリ 95→100→62→69→67MB にリークの傾向は見えない」はこちらが評価し、受理の理由として書いたものです。** それが**ワーカーを一度も測っていなかった**と分かりました。

そして**この案件で繰り返してきた主題の、最も素直な形**でもあります:

| | 何を測っているつもりだったか | 何を測っていたか |
|---|---|---|
| `conn=0` | DB接続数 | プーラが書き換えた別の名前 |
| メモリ | ワーカー | tsx のラッパー |
| A-2 の PASS | 現行コードの冪等性 | **D-037 以前のコード** |
| V-10 の合格 | 払戻率が margin に一致 | **凸性と打ち切りの偶然の相殺** |
| `ODDS_CAP` | 宣言した上限 | **推定量の分解能** |

**5つとも「正常に完了し、もっともらしい結果を返していた」ものです。** 落ちていれば、どれもその日のうちに見つかっていました。
