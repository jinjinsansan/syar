# REVIEW VERDICT — P4「2D 馬群描画の限界テスト」

日付: 2026-08-23  
レビュー: Codex  
対象コミット: `9d6cab69f11ce76e8e3e2e16364d287782d49fa9`  
指示書: `DEV_INSTRUCTIONS_P4_2D_LIMIT_TEST_20260822.md`  
報告書: `REPORT_P4_2D_LIMIT_TEST_20260822.md`

## 判定

**FAIL — 現行の2Dスプライト方式による参考映像の完全再現は終了し、3D馬群描画の技術検証へ移行する。**

これは提出物の失敗ではない。検証タスク自体は完了しており、「調整不足ではなく描画方式が主な制約である」と判断できる証拠が得られた。

## 確認したもの

- コミット `9d6cab6` の全変更
- `out/2d-pack-limit/` の指定成果物11点
- `compare-contact-sheet.png`
- V0〜V3の各コンタクトシート
- `measurements.json`
- 再生成手順
- 検証用純粋関数とテスト
- `npx vitest run packages/render/test/pack-limit.test.ts`: 8件PASS
- `npm run typecheck`: PASS

開発側報告では全体テスト1086件PASS。レビュー側では今回、関連テストと型検査を独立に再実行した。

## 判定理由

### 1. 密集化は改善だが、方式上の問題を解消しない

V0の上位8頭13.84mをV1〜V3で6.07mへ圧縮した結果、縦に離れた列から密集した塊へ改善した。したがって馬群密度は実在する差分であり、無関係ではない。

しかしV3でも、同じ側面素材を横方向へ重ねた印象が強い。参考にある「同じ3D空間を走る複数の馬」には見えず、「同系統の切り抜き画像を前後へ配置した画」の範囲を出ていない。

### 2. 12頭が同じ8姿勢を共有する制約が明瞭に残る

位相を位置から切り離し、完歩長へ±4%の差を付けても、同時に使われる姿勢は4〜6種類だった。12頭が同じ8枚を共有するため、同型の首、胴、脚、騎手姿勢が繰り返し現れる。

勝負服を焼けば個体識別は改善するが、身体の造形と動作の反復は解消しない。したがって「勝負服未適用だから不利」という但し書きは判定を覆さない。

### 3. 遮蔽は正しいが、立体感にはならない

描画順はカメラ奥行きに従っており、手前の馬が奥の馬を隠す。実装とテストの両方で確認した。

それでも重なりが立体に見えないのは、各馬の向き、輪郭、照明、身体の立体変形がカメラに応じて連続変化しないためである。正しい描画順だけでは、板同士の重なりを3D馬群へ変換できない。

### 4. V3の映像統合処理でも主因は残る

背景ぼけ、馬のぼかし、色調統一によってV3はV2より馴染んだ。ただし、輪郭を柔らかくしただけで、素材の方向不足と姿勢反復は残った。

さらに強いブラーで欠点を隠す方法は、指示書のFAIL条件に該当する。馬体の可読性を失わずに方式上の問題を消す余地は小さい。

### 5. 真横の最も有利な条件でも未達

今回のテストは、2Dスプライトに最も有利な真横固定カメラである。それでもA「切り抜き感」、B「馬群感」、C「個体ごとの運動」の合格水準へ届いていない。

正面、斜め前、斜め後ろ、旋回では方向素材の切替が加わるため、完全再現の難度はさらに上がる。真横で未達の方式を、フルレースのカメラ調整で救済する合理性はない。

## 提出実装へのレビュー所見

検証経路は通常レースから隔離され、DB、worker、認証、レース結果には触れていない。既存の未コミット変更 `apps/web/package.json` も保持されている。

テストには1点弱さがある。`V0 の対照性`テストはコピーした配列の等価性を確認しているだけで、実際の `/race` 経路とV0の描画入力が一致することまでは証明していない。ただし、ツールが既存の `resolveBroadcastV2Scene` と `drawBroadcastV2Scene` を使用していること、およびV0〜V3の相対比較から、今回の方式判定を無効にする問題ではない。

`measurements.json` の `output_times_sec` は実データでは7.9秒を使っている一方、一部表示に8が残る箇所がある。最終コマが7.967秒である説明は妥当で、判定への影響はない。

## 今後の決定

### 停止する作業

- 現行2D馬素材の追加調整による完全再現
- 芝占有率を目的としたカメラ調整
- 2D馬群を救済するためのブラー強化
- 現行方式のまま方向素材を64〜768枚へ増やす作業
- 俯瞰、正面、後方カットを2D素材で順次調整する作業

### 維持するもの

- `@star/race-engine` の結果、着順、公正性、介入
- コース座標とレーン情報
- レース時間とリプレイモデル
- カット台本の概念
- HUD、実況、隊列バー、馬名表示
- 世界オブジェクトの配置規則
- Web、DB、workerの既存構成

### 次に行うもの

完成版3Dレースの実装へ直行しない。まず独立した3D技術検証を行う。

最初の検証は以下へ限定する。

- 8秒
- 12頭
- 真横追従1カット
- 既存レースの位置データを読むだけ
- 1種類の仮馬モデルと1種類の走行アニメーション
- 馬ごとのアニメーション位相差
- 3Dの遮蔽、透視投影、照明、接地影
- HUD、実況、背景作り込みなし
- 現行V3および参考フレームと同一シートで比較

この3Dスパイクで、Webでの性能、見た目、素材調達方法、決定論、既存座標系との接続を確認してから本実装を裁定する。

## 最終結論

4日以上改善しなかった主因は、作業量不足でもカメラ値の選択ミスでもない。参考が3D馬群を撮影した映像であるのに対し、現在は限られた方向・姿勢の2D素材を並べる方式であり、その差を背景、HUD、密度、ブラーで埋めようとしていたことにある。

密集化と位相分離は、将来の3D馬群にも有用な知見として残す。ただし現行2D描画を完成版の基盤として継続しない。
