# 回答 — 楕円の走路でレース判定が遅い件（照会 `QUESTIONS_ENGINE_LANE_SPEED_20260915.md` へのレビュー側の回答）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-15 ／ レビュー側 → 開発側・オーナー
**対象**: `QUESTIONS_ENGINE_LANE_SPEED_20260915.md`（Q-1〜Q-6）／ ブランチ `p4/race-30sec-cuts` ／ HEAD `cfc3ad1`
**根拠**: 正典 **D-035・D-036**（試行回数の導出と所要）・**D-071**（距離ロスを着順に）・§10.1（10 分サイクル）・作業規律 **R-15・R-16・R-21・R-23・R-26・R-27・R-28・R-30・R-32・R-33**

> 凡例（R-13）: **✔** ＝ レビュー側がコードを読んで確かめた、または手元で実行した ／ **計算** ＝ 実測値と定数から出した値 ／ **△** ＝ 開発側の実測でレビュー側は再現していない、または未確認
> ★**本書の §5・§6 を、エンジンの速さを直す便（以下「ES 便」）の指示とします。** 別の指示書は出しません。
> 本書は正典を編集しません。§3 Q-6 の起票候補は、オーナーの承認のあとにレビュー側が起票します。

---

## 0. 結論

1. 照会の診断は**コードと一致**します。レビュー側の独立計測も開発側の値と合いました（§1）
2. ★**ただし `swingScale` を 1 回にするだけでは足りません。** 本番機の予算（D-036）に戻すには、**馬ごとに一定の値**と**区間**も 1 回にする必要があります（§2）
3. Q-1 **別の便（ES 便）を最優先**で。VW 便の残りの計測と、本番への `0023`・新しいワーカーの配備は ES 便の後
4. Q-2 **(b)＋(c)＋馬ごとの定数**。(a)（モジュールに状態を持つ）は採りません
5. Q-3 指紋は**直す前のコミットで取った値を定数ファイル**に置き、**画面の経路（`laneAt`）も**含める
6. Q-4 回数の上限で CI に置く（案のとおり）
7. Q-5 ★**オーナーが許可しました（2026-09-15）。** 読み取り専用で 1 回、§4 の内容を確かめてください
8. Q-6 回数の検査で足ります。加えて、**D-036 の「46%」が 08-16 から黙って失効していた**ことを正典に残す候補を出します

---

## 1. 照会の事実の照合

### 1-1. 原因（コード）

| 照会の記述 | 照合 |
|---|---|
| `resolveRace` は楕円のとき馬ごとに `laneExtraM` を 1 回呼ぶ | ✔ `packages/race-engine/src/race.ts:136-142` |
| `laneExtraM` は呼ばれるたびに `ovalSegments` を作り、コーナーを 10m ごとに `laneAt` を呼ぶ | ✔ `lane.ts:467`・`:471-483` |
| `laneAt` の中で `swingScale` を 2 回呼ぶ（`home` と `wob`） | ✔ `lane.ts:416-420`・`:442-445`（旧来の経路は `:447-448` で 1 回） |
| `swingScale` は `totalTurn` を 2 回、`totalTurn` は毎回 `ovalSegments` を作る | ✔ `lane.ts:197-201`・`:175-181` |
| `ovalSegments` は `ovalCornerPlan`（配列を作る）・`ring`・`backward` の複製・`withRunUp` を毎回作る | ✔ `lane.ts:143-169`・`:76-96`・`:125-140` |
| `swingScale` は距離と走路の形（`lapM`・`homeStretchM`・`cornerRadiiM`）だけで決まり、`widthM` を読まない | ✔ 上の 3 関数の本文。`laneAt` は毎回 `{ ...spec, widthM }` を作って渡すが、`widthM` は読まれない |
| `97364c5`（D-071）・`97b6d05`・`0a6e577` が `lane.ts` を変えた便 | ✔ `git log -- packages/race-engine/src/lane.ts` |

### 1-2. 所要（レビュー側の独立計測）✔

HEAD `cfc3ad1`（作業ツリーは未追跡の絵と音だけ）・`watch-pool.json` の先頭の馬・芝・良・`DEFAULT_OVAL`・2026-09-15。スクリプトはレビュー側のスクラッチ（リポジトリに書いていない）。

| 条件 | レビュー側 | 照会 §1-1（`57adf77`） |
|---|---|---|
| 楕円 1600m・12 頭 | **2,094µs** | 2,497µs |
| 楕円 3000m・12 頭 | **7,270µs** | 6,745µs |
| 楕円 1600m・8 頭 | 2,276µs | — |
| 楕円 1600m・18 頭 | **5,003µs** | — |
| 直線 1200m・12 頭 | 16µs | 16µs |

- 桁が合っています。△ **D-071 の前（`97364c5^`）の 61µs・コミットごとの段差・試作の 537µs と指紋の一致は、レビュー側は再現していません**（再現の実行は今回は見送った）
- ⚠️ 正典の出走頭数は **8〜18 頭**（§13.2）。**本番で最も重いのは 18 頭・長距離**です

---

## 2. ★見立ての追加 — `swingScale` を 1 回にするだけでは予算に戻らない（計算）

### 2-1. 予算

- 本番機（VPS・16 頭）で **1 レース 約 138 秒・1 周 2 レースで 約 275 秒＝10 分サイクルの 46%**（✔ 正典 D-036・`apps/worker/src/odds.ts:39`。`tools/deploy.sh` の註記は「約 116 秒」）
- ★**この値はすべて D-071 の前のエンジンで測ったもの**です。08-16 から誰も測り直していません（R-23: 合格はコードが変われば黙って失効する）

### 2-2. 機械によらない見積り（同じ機械での比）

照会 §1-1 の値（△ 開発側の実測）で、D-071 の前との比を取ります。

| エンジン | D-071 の前との比 | 本番機の 1 レース（138 秒 × 比） | 1 周（2 レース） |
|---|---|---|---|
| D-071 の前 | 1 | 138 秒 | 275 秒（46%） |
| いま | 約 41 倍 | **約 1.6 時間** | サイクルに入らない |
| 試作（`swingScale` を覚える） | 約 8.8 倍 | **約 20 分** | ★**約 40 分 ＝ サイクルの 4 倍** |

→ ★**Q-2 の (b) だけでは本番に出せません。**

### 2-3. 残りの重さの出どころ（コード）✔

| 何を | どこで | 1 レースで変わるか |
|---|---|---|
| `ovalSegments` を馬ごとに作る | `lane.ts:467` | 変わらない（距離と形だけ） |
| `laneAtStart(gate, fieldSize, widthM)` を刻みごと | `lane.ts:384` | **馬ごとに一定** |
| `stream(seed, gate, salt)` を刻みごとに 3 本 | `lane.ts:419`・`:424`・`:425` | **馬ごとに一定**（刻みを引数に取らない） |
| `{ ...spec, widthM }` のオブジェクトを刻みごとに 2 つ | `lane.ts:420`・`:444` | 変わらない |

刻みごとに本当に変わるのは `ranM`・`run`・`settle`・`reveal`・`sin(...)` だけです。

---

## 3. 照会への回答

### Q-1 別の便にするか → **別の便（ES 便）・最優先**

開発側の既定のとおりです。加えて、順番を固定します。

1. ES 便（本書 §5）
2. VW 便の残りの計測（VW-0 の大きさ・V-4〜V-6・V-18 の残り 47 組・VW-6 の 3〜7）
3. V-10（オーナーの別ターミナル）
4. 本番へ `0021` → `0022` → `0023` → 新しいワーカーの配備（`DEV_INSTRUCTIONS_VENUE_WIRING_20260915.md` §10 の前に **ES 便** が入る）

★**オーナー決定（2026-09-15）**: `seed-races`・V-10・V-18 の残りは ES 便の後に、時間のあるときに流す。

### Q-2 直し方 → **(b)＋(c)＋馬ごとの定数。(a) は採らない**

**(a) を採らない理由**
- `lane.ts` は純粋関数の集まりとして書かれ、測定の道具（`tools/` の約 50 本）が同じ関数を掃引で呼ぶ。キーの数に上限が無く、道具ごとに Map が育つ
- 状態を持つ安全策は、それ自体が新しい経路になる（R-26）。持たずに同じ効果が出るなら持たない

**作るもの**
- **1 レース 1 回**: `ovalSegments(distance, spec)` と `swingScale(distance, spec)`
- **1 頭 1 回**: `laneAtStart`・`stream` の 3 本（`u`・`drift`・`phase`）
- 刻みの計算は、上の値を受け取る**内部の関数 1 つ**にする

**条件**
1. ★**`laneAt` の署名と値を変えない。** 画面（`apps/web/src/app/race/page.tsx`・`race-next/page.tsx`・`rig-lab/sprite/sprite-client.tsx` の 3 か所）と道具（`tools/` 約 50 本）が呼んでいます
2. ★**`laneAt` と `laneExtraM` は同じ内部の関数を通す。** 式を 2 か所に書かない（画面の `w` とエンジンの `w` が離れる形を作らない・R-30）
3. ★**浮動小数の計算の順番を変えない。** 特に次の 3 式は、左から掛ける順番のまま前計算すること
   - `RAIL_W + homeSpreadM * (u * u) * swingScale`（`:418-420`）
   - `Math.sin(phase + run * Math.PI * 3) * wobbleM * reveal * swingScale`（`:443-444`）
   - `Math.max(0, drift * 0.85 + wave) * reveal * (widthM * 0.62) * swingScale`（`:447-448`）
   - 例: `wobbleM * swingScale` を先に掛けて持つのは**順番が変わる**ので不可
4. ★**速さのために次を動かさない**: `ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`（D-035 のバイアス）／ `stepM`（結果が変わる）／ `LANE_MODEL`・`LANE_REVEAL_FULL_RUN`
5. `race.ts` の乱数の消費（`deriveRng` のストリーム）に触らない

**合格線（同じ機械・同じ実行での比）**
- 本番の条件（**16 頭・18 頭 × 1600m・3000m**・10 場のうち最も長い周の場を含む）で、**`97364c5^` との所要の比が 1.3 倍以下**
- 根拠（計算）: 138 秒 × 1.3 ＝ 約 180 秒 ／ 1 周 約 360 秒 ＝ サイクルの **60%**
- ★**届かなければ、そこで止めて報告してください。** 並列化・試行回数などの手を開発側だけで選ばない（D-035・D-036 に効くのでオーナーとレビュー側の判断）

### Q-3 「結果が 1 ビットも変わらない」の固定 → **案のとおり、次を足す**

1. ★**期待値は直す前のコミット（`cfc3ad1`）で取り、定数ファイルとしてコミットする。** 検査の中で同じコードから作らない（自分を自分と比べる形・R-16）
2. **範囲**
   - 10 場の凍結した形（`frozenCourseOf`）× `DISTANCE_MENU` の 7 距離 × **8 頭・18 頭** × シード 20 以上
   - `DEFAULT_OVAL`
   - `ovalSpecFromCornerRadii` で作った**半径が 4 つとも違う形** 1 つ（どの場もまだ持たないが器はある）
   - 直線（`laneExtraM` を通らない対照）
3. **中身**: `resolveRace` の結果（照会の付録 B の 5 項目）＋ **`laneExtraM` の値** ＋ **`laneAt` を刻みで直接呼んだ値**（画面の経路）
4. **変異の実演**（報告に書く）
   - ① 1 レース 1 回の値を**距離だけ**で決める（走路の形を無視する）→ 落ちる
   - ② 馬ごとの定数を**別の枠**で作る → 落ちる
   - ③ §Q-2 条件 3 の掛ける順番を 1 か所入れ替える → **落ちるか落ちないかを記録する**。落ちなければそのことを書く（R-21: 落ちなかった理由を読む）
5. ★**この検査を先に 1 コミットで入れる**（直す前のコードで合格することを見せてから直す・R-15 の形）

### Q-4 速さの退行を CI で → **回数の上限で置く（案のとおり）**

- `resolveRace` 1 回あたりの `ovalSegments`（または区間を作る関数）の呼び出し回数に上限を置く。★**頭数によらない定数**になるはず（1 レース 1 回にしたので）
- 条件
  - 数える仕組みが結果を変えないこと（Q-3 の指紋で見る）
  - **変異**: ES 便の変更を戻すと落ちる
  - 上限の値は直した後の実測で決め、理由を註記
- 時間そのものは CI に置かない（機械で揺れる）。**報告には §Q-2 の比を毎回書く**

### Q-5 本番のワーカーの確認 → ★**許可済み（オーナー・2026-09-15）**

§4 の内容で、読み取り専用で 1 回。

### Q-6 R-15 の対照に速さを入れるか → **Q-4 で足りる。ただし失効の記録を正典に残す**

- 「器だけ」の便（`0a6e577`）は、Q-4 の回数の検査が CI にあれば捕まります。R-15 の文面は変えなくてよい
- ★ただし、今回の本当の穴は **D-036 の「46%」が、結果を変える便（D-071）の後も合格として残り続けたこと**です（R-23・R-32 の形）
- **正典への起票候補**（番号は起票時・オーナーの承認後）:
  > オッズのモンテカルロの所要（D-036）は、エンジンを変える便の後に失効する。エンジンを変える便は、**本番の条件での所要の比（変更前のコミットとの比）**を報告に必ず書き、CI には区間を作る回数の上限を置く。D-036 の値には**測ったコミット**を併記する
- `odds.ts:39`・`tools/deploy.sh` の所要の註記も、ES 便の後に**測ったコミット付き**で書き直す

---

## 4. Q-5 本番の確認（読み取り専用・1 回）

★**書き込み・再起動・配備・ロールバックはしません。** 異常が見えても直さず、すぐにオーナーへ報告してください。

| # | 確かめること | 方法（例） |
|---|---|---|
| 1 | 稼働中のワーカーの版（R-28） | `readlink /opt/star-current`（`/opt/star-releases/<sha>`）・`systemctl show star-worker -p ActiveEnterTimestamp,NRestarts` |
| 2 | その版に D-071（`97364c5`）が入っているか | 手元で `git merge-base --is-ancestor 97364c5 <sha>` |
| 3 | 直近のレースの生成の間隔 | `races` の直近 50 本の `created_at` の差（`set session characteristics as transaction read only`・`app_environment = 'production'` を先に確認） |
| 4 | 抜けたサイクル | 直近 48 時間の `cycle_index` の欠番の数 |
| 5 | 直近 48 時間の状態 | `status` ごとの件数（確定・発売中・中止） |
| 6 | ワーカーの記録 | `journalctl -u star-worker --since "48 hours ago"` の末尾（時間切れ・中止・再起動の行） |

**報告の形**: 表 1 枚。★「10 分サイクルで生成できているか」を**はい／いいえ**で先頭に。

---

## 5. ES 便（指示）

| 工程 | 中身 | 合格 |
|---|---|---|
| **ES-0** | 測る（読み取りのみ）: Q-5（§4）／ 同じ機械で `97364c5^` と `cfc3ad1` の所要（§Q-2 の条件） | 表 |
| **ES-1** | 指紋の検査を入れる（§Q-3）。**コードは変えない** | `cfc3ad1` のコードで合格 |
| **ES-2** | `swingScale` と `ovalSegments` を 1 レース 1 回に（§Q-2） | 指紋一致・変異 ①・比を報告 |
| **ES-3** | 馬ごとの定数を 1 頭 1 回に（§Q-2） | 指紋一致・変異 ②③・比が 1.3 倍以下 |
| **ES-4** | 回数の上限の検査（§Q-4） | 変異で落ちる |

- 各工程を**別のコミット**に。工程ごとに比を測る（どれが効いたかを残す）
- `npx vitest run --maxWorkers=2 --minWorkers=1` の `Test Files`・`Tests` の行と終了コード。既存の赤（`edit-grammar-audit` ⑨）が 1 件から増えていないこと
- 報告 `REPORT_ENGINE_LANE_SPEED_<日付>.md`（再現した事実と未検証の主張を分ける・R-13）

## 6. 触ってよいもの

| 可 | 不可 |
|---|---|
| `packages/race-engine/src/lane.ts`・（(c) で要るときだけ）`packages/race-engine/src/race.ts`・`packages/race-engine/test/` の新しいファイル・指紋の定数ファイル・（新しい定数を足すときだけ）`apps/cli/src/calibration.ts` | 較正定数の値・`packages/betting/`（試行回数）・`packages/render/`・`apps/web/`・`tools/` の道具・正典・本番 DB・ワーカーの配備 |

同じ作業ツリーの作法はこれまでと同じです（`git stash`・`git add -A`・`git commit -a` を使わず、ファイルを名指し）。

---

## 7. ついでに照合したこと

- `REPORT_VENUE_WIRING_20260915.md` §7 の「26 本で約 35 分〜7 時間」は、照会 §3-1 のとおり D-071 の前の註記に基づく見積りで、**いまのエンジンでは成り立ちません**（計算: 18 頭で 1 レース数時間）
- 照会 §3-1「04:14 のセッションの終了はメモリではなく Windows Update の再起動」— 報告 §6 の冒頭の記述（同時に流していて落ちた）と食い違うので、VW 便の報告を直すときに揃えてください

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
