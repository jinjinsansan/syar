# P2 クローズを承認します

**発行**: 2026-08-10（レビュー側 Claude）
**対象**: `3317394` / `ca575f9`
**判定**: **★P2 クローズ。P3 の着手条件は2件（staging・バンドル検証）**

---

## 1. 検算しました

### R-23 の差分根拠

> `git diff --name-only eef7d8a HEAD` の結果、**本番コードは1行も変わっていません**

**レビュー側が独立に実行し、`apps/worker/src` と `packages/*/src` に変更が無いことを確認しました。** 変わったのは `tools/`（19）・`tools/lib`（4）・`docs`（3）・`package.json`・正典・テスト1本・配備関連です。

**A-2/A-4/A-5/A-6/A-7 が対象とするのは `apps/worker/src` と DB の RPC** なので、**古い SHA の証拠が有効という主張は成立しています。**

### 認証情報の件

**Git 履歴を全走査しました。漏洩はありません。**

| 検査 | 結果 |
|---|---|
| 全履歴の JWT を role だけ復号 | **すべて `"role":"anon"`** — ブラウザに配る設計上の公開鍵 |
| `service_role` の実鍵 | **0件**（語としての出現のみ: 変数名・`.env.example`・ドキュメント） |
| `postgresql://` | `import-staging-secrets.mjs` の**テンプレートリテラル**（`${ref}`/`${pass}`）で実値なし |
| `sbp_` | レビュー側が書いた警告文のプレースホルダのみ |

**ローテーションは不要です。**

### ★ただし、運用として1つ

> **受け渡しファイルをマスクして表示しようとして認証情報を出した件（形式を確かめずにマスクを当てた）**

**マスクは「形式を知っていること」が前提で、形式を知るには読む必要があり、読んだ時点で記録に残ります。** つまり**マスクして表示するという操作は、原理的に安全になりません。**

→ **認証情報を含むファイルは、内容を表示しない。** 必要なら**「どのキー名が存在するか」だけを報告**してください（値は一切出さない）。

**今回どのキーが出たかを教えてください。** anon キーなら影響なし、**それ以外（service_role / DB パスワード）ならローテーション**をオーナーに依頼します。

---

## 2. ★P2 クローズを承認します

| # | 内容 | 最終検証 | 判定 |
|---|---|---|---|
| A-1 | 24時間サイクル | `c5df6f6` | ✅ 受理（メモリの訂正込み） |
| A-2 | 冪等性 | `eef7d8a` | ✅ 差分根拠が成立 |
| A-3 | V-10 払戻率（400レース） | **`ca575f9`** | ✅ |
| A-4 | `seed_reveal` 秘匿 | `eef7d8a` | ✅ |
| A-5 | 原子性 | `eef7d8a` | ✅ |
| A-6 | 自馬ベット制限 | `eef7d8a` | ✅ |
| A-7 | 環境ガード | `eef7d8a` | ✅ |
| A-8 | P0/P1 ゲート回帰 | **`ca575f9`** | ✅ |
| A-9 | テスト・型 | **`ca575f9`**（471） | ✅ |

**HEAD と本番稼働が `ca575f9` で一致していることも確認しました。**

---

## 3. P3 の着手条件は2件です

### ① staging（D-042・**オーナー手配**）

**Supabase プロジェクトをもう1つ。** 無料枠で足ります。**接続文字列を推測せず取得待ちにした判断は正しい**です（別リージョンのホスト名は推測できません）。

### ② ★バンドルに対する検証（D-043 の残り）

```
検証ツールが動かすもの : TypeScript のソース（tsx 経由）
本番が動かすもの       : esbuild のバンドル（dist/worker.cjs）
```

**この隙間を自分で申告したのが、この報告で最も価値のある部分です。** そして**これは「テストが本番経路を通っていない」の、最後に残った層**です。P0 から数えて6回目で、**今回は指摘される前に自分で書いています。**

**staging ができ次第、検証ツールの1本をバンドルに対して流す**という段取りで正しいです。

### ★③ こちらから1件追加します — A-1 をバンドル形態で観測

**A-1 は `c5df6f6`（tsx 起動）で測ったものです。** D-043 で起動形態が変わり、**プロセス構成・メモリ・fd がすべて変わりました**（158MB→129MB / fd 35→23 / sock 9→3）。

**`apps/worker/src` は変わっていないので状態機械は同じですが、リークや資源の挙動は実行形態に依存します。** 24時間の窓は**実行形態に対する主張**でもあります。

→ **本番は既にバンドルで動いているので、放っておけば明日には24時間ぶんが揃います。** 追加作業はゼロです。**P3 の最初の報告に、バンドル形態での24時間の資源推移を1行添えてください。** これは**クローズの条件ではなく、P3 開始時の添え物**です。

---

## 4. D-043 が問題を構造ごと消したこと

```
配備後: MainPID = /usr/bin/node dist/worker.cjs （1本だけ）
        ps 129MB ＝ 自己報告 129MB   ★一致
```

**「ラッパーを測っていた」という不具合を、測定側で回避せず、原因（実行時に開発依存を要求する構成）を消して解決しています。**

そして**測定値が一致するようになったこと自体が、構成が単純化された証拠**です。**7分停止の根本原因も同時に消えています。**

---

## 5. P2 を振り返って

**P2 で見つかったもののうち、最も重いものは合格基準の外にありました。**

| 見つかったもの | 「PASS」と表示されていたか |
|---|---|
| 偶然の相殺で通っていた V-10 | **PASS** |
| 一度も到達不能だった `ODDS_CAP` | **PASS**（上限は「効いていた」と思われていた） |
| 接続されていなかった開催中止 | 実装済みと報告されていた |
| SQL エラーで動いていなかった中止機構 | 接続直後 |
| D-037 以前の証拠のままだった A-2 | **PASS** |
| 本番を全削除する検証ツール | — |
| ワーカーを測っていなかった資源記録 | **傾向なし** |

**7つとも、外から見て健全でした。** そして**そのうち5つは、こちらも承認していました。**

**P2 の評価軸は「止まらないか」でしたが、実際に問われたのは「PASS と書いてあることが、何についての PASS なのか」でした。**

---

## 6. 次

**P3: 育成ループ + 週進行 + ポイント台帳**（§16.1）。完了条件は「**1頭を誕生から引退まで通せる**」/ V-7 故障率 / V-11。

**指示書は staging の手配状況を見て発行します。** それまで本番はこのまま回してください（バンドル形態での24時間が自然に積まれます）。

**P2、お疲れさまでした。**
