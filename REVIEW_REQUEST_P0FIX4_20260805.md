# P0-fix4 レビュー依頼 — 開発側 → レビュー側（P0 最終便）

**発行**: 2026-08-05（開発側 Claude）
**対象**: `DEV_INSTRUCTIONS_P0FIX4_20260804.md`（I-1〜I-5）
**成果物**: `https://github.com/jinjinsansan/syar`（private）/ branch `main` / commit `07c0188`

---

## 0. 結論

**要確認1件を除き、すべて PASS です。**

| 合格基準 | 実測（4シード × 100/300年・800頭） | 判定 |
|---|---|---|
| 1〜8（P0-fix3 から維持） | V-1 100年 15.46〜16.76% / 300年 14.88〜16.23%、V-2a〜c・V-3 全 PASS、テスト**93件**、決定論 OK | PASS |
| **13. V-2e が 0.9〜1.1倍** | **0.92〜1.09倍**（P0-fix3 は 0.84〜0.91倍） | PASS |
| **14. 補正係数の固定・表と一致** | 0.9156 を固定。**丈夫さ sd のみ 66 vs 表65** | **要確認（§3）** |
| **15. I-2a/b/c** | 反映済み | PASS |
| **16. 本番経路で clamp を固定** | `breed()` の観測境界で判別 | PASS |
| **17. 上書きで再導出** | 回帰0.05 で sd 66→34、創始SD200 で 66→131 | PASS |

総合 PASS（`npm run verify` 163.4秒）。

---

## 1. I-3 の指摘について

**「単体を固定することと経路を固定することは別物」** — これが今回いちばん効いた指摘でした。

前便で「式を固定した」と報告しましたが、固定できていたのは `mutateAllele()` の**内部だけ**でした。私は「純関数に切り出したから安全になった」と考え、**その純関数が実際に使われているかを確かめていませんでした**。呼び出し側に書き戻せば82件通る、という監査結果はそのとおりです。

今回は `breed()` を通した観測境界で判別します。両親のアレルを900（引き戻し −90）に固定すると:

```
正しい式  child = 900 + clamp(X, ±150) − 90   → 値域 [660, 960]
誤った式  child = 900 + clamp(X − 90, ±150)   → 値域 [750, 1050]
```

400回×2アレルを `breed()` で生成し、**上限960以下**（誤式なら1050まで出る）と**下限750未満の観測**（誤式では到達不能）の両方を固定しました。純関数の中身を直しても、呼び出し側を書き換えても落ちます。

---

## 2. I-4 は実地で確認しています

`--generations 5` での実測（`balanceDigest.traitMutation` を JSON で確認）:

| 指定 | 回帰率 | 創始durSD | durability sd | distance_center sd |
|---|---|---|---|---|
| 既定 | 0.2 | 100 | 66 | 341 |
| `--set-balance REGRESSION_RATE=0.05` | 0.05 | 100 | **34** | **177** |
| `--set-founders DURABILITY_SD=200` | 0.2 | 200 | **131** | 341（不変） |

上書きの適用と再導出は `apps/cli/src/config.ts` の `resolveRuntimeConfig()` に集約し、テストで固定しました（`apps/cli/test/config.test.ts`）。`sweep.ts` / `sweep-distance.ts` も同様に直しています。**後者は距離2形質だけを手で組んでいたため、D-012 以降は丈夫さ・気性・適性が置き去りになる状態でした。**

---

## 3. 【要確認】正典 §6.4 の表「丈夫さ sd = 65」は 66 ではないでしょうか

導出式 `sd = 創始アレルSD × √(2r − r²) ÷ 0.9156` に丈夫さの創始アレルSD 100 を入れると **65.531**、四捨五入すると **66** です。他の4形質は同じ計算で表と一致します。

さらに、表の clamp は5形質とも「**丸める前の sd** × 1.667」と一致します。丈夫さの ±109 も 65.531×1.667 = 109.2 由来で、**sd を 65 とすると 65×1.667 = 108.4 → 108 になり表と合いません**。表の sd 65 だけが丸めのずれと見ています。

実装は導出式に忠実な 66 にしてあります。表を 66 に直すか、65 を強制するかご指示ください（挙動への影響は 1.5% 程度で、V-2e は下記のとおり合格域内です）。

なお解析係数は独立に検算し、**0.915604** を確認しました（`(2Φ(k)−1) − 2kφ(k) + 2k²(1−Φ(k))` に k=150/90 を入れて 0.838331、その平方根）。指示書の 0.9156 と一致します。

---

## 4. V-2e が創始水準ちょうどに寄りました

| 形質 | P0-fix3 | **P0-fix4** |
|---|---|---|
| surface.turf | 0.92倍 | **0.99倍** |
| surface.dirt | 0.87倍 | **0.95倍** |
| distance_center | 0.93倍 | **1.01倍** |
| distance_range | 0.96倍 | **1.05倍** |
| temper | 0.84倍 | **0.92倍** |
| durability | 0.94倍 | **1.03倍** |

4シード × 100/300年の最悪値でも **0.92〜1.09倍**。下限0.8までの余裕が 0.04 → 0.12 に広がりました。指示書の見込み（0.92〜1.05程度）とも整合します。

**Q1 の診断が「浮動ではなく clamp 切断」だという訂正は正しかった**と実測で確認できました。ゲート幅で吸収せず導出式で打ち消す、という判断も結果的に正解でした（浮動が原因なら補正しても改善しないはずでした）。

---

## 5. I-2c の併記が早速効いています

seed 42・100年・800頭:

| 形質 | genotype | 表現型 | 差 |
|---|---|---|---|
| 気性 | 51.6 | **56.5** | +4.9 |
| 丈夫さ | 650.4 | **636.3** | -14.1 |

インブリードの代償（§6.5 の `temper += F×120` / `durability −= F×350`）がそのまま出ています。**故障率の分母は表現型の 636** であって genotype の 650 ではありません。P1 で近交が進むほど開くので、`INJURY_BASE` の較正時にはこちらを見てください。

---

## 6. 逸脱・解釈（3件・報告書 §4 に全件）

| # | 内容 |
|---|---|
| 1 | 丈夫さ sd が表と1違う（→ §3） |
| 2 | clamp を「丸める前の sd」から算出（丸めた sd からだと気性の clamp が17になり表と合わない） |
| 3 | V-2d の 0中心形質は創始SD基準へ自動切替。**判定不能なら `pass: false`**（指示書の二択のうち「対象外宣言」だと将来の形質追加時に宣言し忘れる余地が残るため、落とす側を選びました） |

---

## 7. P1 引き継ぎの確認

指示書 §5 の5点はすべて認識しています。1点だけ念のため — **「選抜下での分化の維持」の基準線は本報告（P0-fix4）の数値を使ってください。** I-1 の切断補正で V-2e が系統的に動いているため、P0-fix3 の値と比べると差分の解釈を誤ります。
