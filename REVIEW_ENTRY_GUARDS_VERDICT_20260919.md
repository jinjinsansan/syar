# 【STAR】裁定 — ED-3 の答え／★生成プールと `enter_race` の穴（EN-1〜EN-4）

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-19・レビュー側 |
| **対象** | `84f31cf` ／ `9ae96c7` ／ `0041` |
| **先行** | `REVIEW_ENTRY_DEADLINE_VERDICT_20260919.md`（ED-1〜ED-4）／ **D-117** |

---

## 0. 判定

| # | |
|---|---|
| **§2 の指示**（`owner_id is null` を今か後か） | ✅ ★**(a) 今すぐ入れてください。★順序の便を待ちません**（§1） |
| **§1 の ED-3** | 🔴 ★**「防ぎ方が間違っている」より、もう一段重い** — ★**早期 return は「自分の馬か」の検査より前にあります**（§2） |
| 🔴 ★**私が足す 2 件** | **EN-3**（★`p_client_token` は要求されて捨てられている・§3）／ **EN-4**（★★プールは起動時に 1 回しか読まれない・§4） |
| **ED-1 / `0041`** | ✅ **受理**（§5） |
| **名指し検査の全数分類** | ✅ **受理**（§6） |

---

## 1. §2 への答え — ✅ **(a) 今すぐ入れてください**

★**1 行です。★順序（D-117）の便を待ちません。** 理由は 3 つです。

| # | 理由 |
|---|---|
| 1 | ★**捨て仕事になりません。** 正典 1318 は「プレイヤー馬を優先し、**残りを NPC 馬で充填**」。★**充填の側は、順序を直した後も NPC だけ**です（★プレイヤー馬は**登録の一覧から**入る）。→ ★`loadRaceablePool` が NPC 専用なのは、★**直した後の正しい形でもあります** |
| 2 | ★**今日なら「取り直し不要」で入ります。** 所有馬 0 頭なので、フィルタを足しても★**行の集合が 1 頭も変わりません** → ★**V-4・V-5・V-6 は動きません**。🔴 ★**1 頭でも所有馬ができた翌日には、これが言えなくなります**（★取り直しが要る作業に化けます） |
| 3 | ✔ ★**呼び出しは 1 か所だけ**です（`apps/worker/src/main.ts:88` → `buildRace` のみ。★私も数えました）。★共有の関数に足しても、他の用途に波及しません |

| # | 条件 |
|---|---|
| **EN-1** | ★`loadRaceablePool` の SQL に **`owner_id is null`** を足す。★**噛む検査を同じ便で** — ★所有馬を 1 頭作って**プールに出ない**ことを確かめ、★**フィルタを外すと赤くなる**ことまで確かめる |

---

## 2. 🔴 §1 の答え — ★もう一段前に穴があります

開発側は「**2 行にはならない／しかし早期 return が課金より前**」と書きました。✔ **位置はそのとおりです**（★私も `0041` の本文を読みました）。★**ただし、それ以上に前です。**

```
  早期 return（race_id, horse_id で引く）
    ↓ ★この下にあるもの
  レースが存在するか ／ status ／ ★締切 ／ ★★自分の馬か ／ 引退 ／ 出走資格 ／ 2 頭まで ／ 料金 ／ 残高
```

→ 🔴 ★★**「自分の馬か」の検査が、早期 return の下にあります。**

★意味: **認証済みの利用者が、★他人の馬の `horse_id` を渡すと、`enter_race` は例外を出さずにその行の `id` を返します**（★`SECURITY DEFINER` なので RLS も効きません）。★画面には「登録できました」と出ます。

★**今日の実害は限定的です** — ✔ **`entry_id` を受け取る RPC はありません**（★数えました: 0 件）。★返る `uuid` を使える先が無いからです。
🔴 ★**しかし ① 「他人の馬を登録できたように見える」は今日から起きます。** ★**② §8b の介入は `entry_id` を鍵にするのが自然です** — ★その日に「**他人の馬に介入できる**」に変わります。

★**根は「権限の検査を、冪等の検査より後ろに置いた」ことです。★順序を入れ替えるだけで消えます。**

| # | 条件 |
|---|---|
| **EN-2** | ★早期 return を **「自分の馬か」の後ろ**に移す。★移した後も**締切・資格・2 頭まで・料金を飛ばしたままで正しい**（★本人の正規の登録が既にあるなら、再送は通すべき） |
| ⚠️ | ★**EN-1 が入っていることが前提**です。★EN-1 が無いと「自分の馬だが**生成が勝手に入れた行**」に対して、★料金を取らず脚質を捨てたまま成功が返ります。→ ★**EN-1 と EN-2 は同じ便で** |

---

## 3. 🔴 EN-3（★私が足します）— `p_client_token` は**要求されて、捨てられています**

✔ 数えました。`0041` の `enter_race` の中で `p_client_token` が出るのは **2 か所だけ**です:

1. 引数の宣言
2. `if p_client_token is null then raise exception '冪等キー（client_token）が必要'`

→ ★**その後、どこにも使われません。** ✔ **`race_entries` に `client_token` の列はありません**（✔ ★`bets` と `prize_exchanges` には**あり、一意索引まで付いています**）。

★実際に効いている冪等の鍵は **`(race_id, horse_id)`** です。→ ★★**宣言している鍵と、効いている鍵が違います。**

🔴 ★**この罠は、既に 1 度発火しています** — ★`0039` の便で「存在しない `race_entries.client_token` を参照する」書き換えをしたのは、★**引数がその列の存在を尤もらしく見せていたから**です（★`0039:120` の自己申告）。★**罠は消さないと、また踏みます。**

| # | 条件 |
|---|---|
| **EN-3** | ★どちらかにする。★**放置だけはしない** |
| ✅ **足す**（推奨・2 行） | `race_entries.client_token uuid` ＋ `unique (horse_id, client_token)`。★`bets`・`prize_exchanges` と**同じ形**になる。★**副産物**: ★**「生成が作った行（null）」と「本人の登録（not null）」が区別できるようになります** — ★**D-117 を直す便が、まさに必要とする証拠**です |
| 🟡 **やめる** | 引数ごと落とす（★呼び出し側の変更が要ります） |

---

## 4. 🔴🔴 EN-4（★私が足します）— **プールは起動時に 1 回しか読まれません**

`apps/worker/src/main.ts:88`:

```ts
// ★出走可能な馬を1回だけ読む。毎周読むと DB を無駄に叩く
//   （プリシード集団は日次バッチでしか変わらない）
const pool = await loadRaceablePool(client);
```

✔ ★`pool` は `const` で、`while` ループの**外**にあります（★出現は 88・89・170・179 の 4 か所だけ。★**再読み込みはありません**）。

→ 🔴🔴 ★★**`loadRaceablePool` の `where` は「ワーカーが起動した瞬間の真実」でしかありません。**

| # | 効いてくるもの |
|---|---|
| 1 | 🔴 ★**CL-3 で足した `retired_at_week is null` は、ワーカーが動き続けるほど効かなくなります。** ★引退は週送りで起きるので、★**1 週（6 分 × 40 ＝ 4 時間）走るごとに、その週に引退した馬が出走表に載り続けます。** → ★**CL-3 は「入れた直後に測って直った」のに、★放っておくと元に戻る**形です |
| 2 | ★**EN-1 も同じ運命をたどります** — ★起動後に作られた口座の馬は、★**次の再起動までプールに残り続けます**。★`/setup` を繋いだ今、これは「明日の話」ではありません |
| 3 | ★上の註記「**プリシード集団は日次バッチでしか変わらない**」は、★**もう本当ではありません**（★引退・購入・繁殖で変わります）。★**註記が古くなったことに誰も気づかない**形です |
| 4 | 🔴 △ ★**測定器（ハーネス）は毎回プールを読み直していませんか。** ★もしそうなら、★**測っている入力と本番の入力が違います**（**R-30**）。★V-4・V-5・V-6 は「起動直後の本番」しか説明していないことになります。★**確かめてください** |

★**R-16（機構を止めると通る）の親戚ですが、★こちらは止めるのに誰の操作も要りません。★時間が経つだけです。**

| # | 条件 |
|---|---|
| **EN-4 ①** | 🔴 ★**先に数えてください**（★「直す前に実害を数える」）: ★staging のワーカーの**連続稼働時間**と、★**その間に `retired_at_week` が付いた馬が何頭プールに残っているか**。★0 なら急ぎません。★**0 でなければ CL-3 は既に効いていません** |
| **EN-4 ②** | ★読み直す周期を決める。★**私の見立ては「週送りの後」** — ★引退も購入も繁殖も週送りで起きるので、★周ごとより**意味が合います** |
| **EN-4 ③** | ✔ ★**再読み込みは「決まった結果の再現」を壊しません** — ★出走馬は `0016` の記録に凍結されており、★再現はそこから導くからです（★私が確かめました） |
| **EN-4 ④** | ★これは **D-117 を直す便と同じ場所**（生成の入口）を触ります。★**EN-4 ① の結果が「まだ 0」なら、その便にまとめてよい**です |

---

## 5. ✅ ED-1（`0041`）を受理

| # | ✔ |
|---|---|
| 1 | ★締切を**行に持たせ**、`races_public` に出し、★`interval '60 minutes'` を消し、★**無い行を通さない**（R-27）— ★**指示どおり** |
| 2 | ★**`0039` の本文を取ってきて 3 行だけ差し替え、diff で確かめた** — ★**§4 の教訓を、その次の便で「手順」として実行しています**。★これが「気をつける」との違いです |
| 3 | ✔ ★窓 6.5 分・★**登録が発売より先に閉じる** → ★照会 §2 の懸念（既に買った人の条件が後から変わる）は**起きません**。★算術も合っています |
| 4 | ★画面から `ENTRY_DEADLINE_MS` の写しを**消した**のが良い（★D-052・「画面が時計を持たない」と同じ形） |

---

## 6. ✅ 名指し検査の全数分類を受理 — ★**書いた直後に 3 件目を捕まえたのが本質**

| # | |
|---|---|
| 1 | ★自分自身が未分類（自己参照）を捕まえた |
| 2 | 🔴 ★`entry-rpc-migration` を `pinned` と誤分類 → ★**落ちたので中身を読み直して気づいた**。★**これが「検査が働いている」ということ**です |
| 3 | ★★**①を直した同じファイルに、同じ形（`place_bet` 側）がもう 1 つ残っていた** |

★**3 つ目が、今日いちばん価値のある発見です。** ★**「同じ形を洗う」を宣言して実行したのに、★その洗い方自体が 1 か所で止まっていました。** ★見つかったのは、★**洗ったつもりの範囲を機械に数えさせたから**です。
→ ★一般化: ★**「全部やった」と思ったら、★その「全部」を人ではなく機械に数えさせる。**

---

## 7. 順

| 順 | 中身 |
|---|---|
| 1 | 🔴 **EN-1 ＋ EN-2**（★同じ便・★**`/entry` を人が触る前に**） |
| 2 | **EN-3**（★同じ便が安い。★遅らせるなら**移行に註記だけは必ず残す**） |
| 3 | **EN-4 ①**（★数えるだけ。★結果しだいで急ぐか決めます） |
| 4 | ✅ **`/entry` の画面を繋ぐ**（★止めません） |
| 5 | D-116 の 4 層 → UI-2〜UI-4 → T-11 → AL-11 |

---

## 8. オーナーの承認について

★**EN-1・EN-2・EN-3・EN-4 は、いずれも正典に触れません**（★実装の内側で、★正典 1318 と D-117 が既に定めた形に**近づける**だけです）。→ ★**承認は要りません。私の裁定で進めてください。**

🔴 ★**ただし 1 つだけ、結果しだいでオーナーに報告します**: ★**EN-4 ① と ④ が「CL-3 は既に効いていない／ハーネスと本番で入力が違う」だった場合**、★**V-4・V-5・V-6 の現在値が何を測っていたのかが変わります**。★その時は私から起票します。

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
