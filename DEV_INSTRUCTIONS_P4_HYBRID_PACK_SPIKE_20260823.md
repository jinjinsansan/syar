# DEV INSTRUCTIONS — P4「2D世界＋3D馬群 ハイブリッド8秒検証」

日付: 2026-08-23  
発行: レビュー側 Codex  
対象ブランチ: `p4/race-30sec-cuts`  
前提: `REVIEW_P4_3D_PACK_SPIKE_VERDICT_20260823.md`  
判定状態: 3DスパイクはCONDITIONAL PASS  
優先度: 最優先

---

## 0. 目的

3D馬群は、2D馬方式で解消できなかった立体的な遮蔽、角度追従、個別位相を成立させた。一方、3Dスパイクには地面と空しかなく、既存2Dレースが持つスタンド、観客、ラチ、芝、距離標などの世界情報がない。

このタスクでは、競馬場全体を3D化しない。

**既存2D世界を維持し、その中へ3D馬群を合成した1カットが映像として成立するかを8秒で判定する。**

検証する問いは次の5つだけ。

1. 2D世界と3D馬群のカメラ・地平線・接地点を一致させられるか
2. ラチなど2D前景物が3D馬を正しく隠せるか
3. 3D馬の影を2D地面へ自然に統合できるか
4. 背景込みでも性能基準を満たせるか
5. 2D V3より参考映像へ近づいたと判断できるか

完成版レースの実装、通常`/race`への導入、競馬場全体の3D化は行わない。

---

## 1. 厳守事項

1. `STAR_SPEC_v2.0.md`、`CLAUDE.md`、前提レビューを読む。
2. 現在の未コミット3Dスパイク作業を保持する。勝手にコミット、stash、reset、revertしない。
3. push、デプロイ、外部公開をしない。
4. 通常`/race`、race-engine、DB、worker、認証、ポイント、投票を変更しない。
5. 現行2D馬方式の改善へ戻らない。
6. 競馬場の新規3Dモデルを作らない。
7. 購入済みGLBをpublicへ置かず、リポジトリにも追加しない。
8. `.env`、`secrets.*.env`、`git.txt`を読まない。
9. 実在固有名、他社固有名を新規コード、コメント、成果物へ書かない。
10. 既存の`apps/web/package.json`ポート変更を保持し、今回の変更として扱わない。

---

## 2. 実装前ゲート — 足元アーティファクトの原因確定

ハイブリッド背景で隠す前に、C2/C3の足元に残る細い黒線・破片状描画の原因を確定する。

既存の診断手段を使う。

```powershell
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --bounds --t 4
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --still --shadows 0
npx tsx tools/capture-3d-pack-lab.mjs --cam c2 --still --hide <mesh-name>
```

最低限、次の比較画像を`out/hybrid-pack-spike/diagnostics/`へ残す。

```text
artifact-shadow-on.png
artifact-shadow-off.png
artifact-candidate-01-hidden.png
artifact-candidate-02-hidden.png
artifact-resolved.png
artifact-findings.json
```

`artifact-findings.json`に以下を書く。

- 症状が出る時刻と画面座標
- 影ON/OFFで消えるか
- 原因メッシュ名
- 原因が本体、影、Skinning、Root Motion、Z-fightingのどれか
- 採用した修正
- 非表示にした場合、その部品を失う影響

禁止:

- 背景、芝、ブラー、モーションブラーで隠す
- 原因不明のまま複数メッシュをまとめて消す
- 元GLBを書き換える

原因が確定できなければ、ハイブリッド実装へ進まず質問書を出す。

---

## 3. アーキテクチャ

### 3.1 検証経路

通常画面とは別に、開発専用の隔離ページを作る。

推奨:

```text
apps/web/src/app/3d-hybrid-lab/
  page.tsx
  hybrid-lab-client.tsx
```

既存`3d-pack-lab`を共通化してよいが、通常`/race`から参照しない。

本番では404、ナビゲーションからリンクなし、モデルAPIも開発限定を維持する。

### 3.2 描画レイヤー

1画面を最低3層に分ける。

```text
最前面: 2D foreground（手前ラチ、必要な前景物）
中央:   3D horses（透明背景のWebGL canvas）
背面:   2D background（芝、奥ラチ、スタンド、観客、空、内馬場）
```

HUDは今回の主判定には不要。任意のH3でのみ最前面に載せてよい。

背景と前景を単なる1枚のスクリーンショットへ固定しない。少なくとも8秒間、既存カメラ追従に対応して更新する。

### 3.3 既存2D描画の再利用

可能な限り既存の以下を再利用する。

- `resolveBroadcastV2Scene`
- `drawBroadcastV2Scene`
- `drawPerspectiveWorld`相当の世界描画
- parallax plate
- mow stripes
- crowd / infield / distance poles / finish post
- foreground rail

既存関数が「世界＋馬＋前景」を一括描画する場合、通常経路を壊さず、検証用オプションまたは純粋なレイヤー関数へ最小分離する。

同じ世界描画をツール側へコピーしない。コピーが避けられない場合は理由と差分検査方法を報告する。

---

## 4. カメラ整合

見た目で合わせない。2Dと3Dの世界座標から画面座標への投影を測る。

### 4.1 基準点

同じ時刻に、コース上の最低8点を2Dと3Dの両方で投影する。

- 注視点付近の内ラチ接地点2点
- 外ラチまたは奥側境界2点
- 馬群先頭位置
- 馬群末尾位置
- 横位置の内側端
- 横位置の外側端

各点について以下をJSONへ記録する。

```text
world s/w
2D pixel x/y
3D pixel x/y
error px
```

### 4.2 合格値

- 8点以上
- 中央誤差: 3px以下
- p95または最大誤差: 8px以下
- 地平線の縦ずれ: 5px以下
- 馬の蹄接地点と地面のずれ: 3px以下

8秒中、最低0秒・4秒・最終フレームで測る。

### 4.3 禁止

- 馬だけを目視で上下移動して合わせる
- 時刻ごとの手書きオフセット
- 1フレームだけ合う固定スクリーンショット
- 2Dと3Dで別々の注視対象や別々の時間を使う

---

## 5. 影と接地

以下の3案を比較する。

### S0 — 影なし

技術対照。接地感と性能の基準。

### S1 — 3Dリアルタイム影

3D地面は画面上では透明または2D芝に馴染ませるが、ShadowMaterial等で影だけを合成する。

### S2 — 軽量影

楕円または簡易投影影。各馬の接地点と走行位相に連動する。単なる固定黒丸にしない。

比較するもの:

- 接地感
- 足元の破綻
- 12頭のFPS / p95
- draw calls / triangles
- 影のちらつき

影が支配的だったため、S1を無条件採用しない。見た目と性能の両方で選ぶ。

---

## 6. 描画負荷の最適化ゲート

現3Dスパイクの12頭・影あり:

```text
約1,032,746 triangles
755〜764 draw calls
106 geometries
206 textures
初回ロード 3.8〜6.2秒
```

次を調査・実施する。

1. GeometryがSkeletonUtils.clone後も共有されているか、UUID単位で数える
2. Textureの実体が共有されているか、UUID単位で数える
3. 毛色・勝負服Materialを馬ごとに無制限複製していないか
4. 非表示メッシュが資源・影パスに残っていないか
5. 不要な装具、たてがみ、手綱のメッシュをロード後に外せるか
6. 影対象を馬体と主要脚へ限定できるか
7. rendererのPixelRatioを1で固定する
8. GLBロード・Draco展開・複製の各時間を分けて測る

### 目標

画質を明らかに落とさず、12頭時に以下を目標とする。

- draw calls: 現状比30%以上削減
- textures: 現状比30%以上削減
- 影ありtriangles: 現状比25%以上削減
- 初回準備: 4秒以下を目標

技術上達成できない項目は、原因を測定して報告する。数値を合わせるために馬を減らさない。

---

## 7. 固定条件

- 1920×1080
- 8.0秒 / 30fps / 240フレーム
- seed 42
- 12頭
- 2D限界テスト・3Dスパイクと同じ区間
- 上位8頭5〜7m
- 横2〜4列
- C2相当の真横追従
- 馬体画面高30〜40%
- HUDなしを基本
- 音声なし
- カット切替なし
- 参考映像の50〜58秒相当と比較

---

## 8. バリアント

最低4バリアントを作る。

### H0 — 3D C2再現

既存3DスパイクC2。回帰対照。

### H1 — 2D背景＋3D馬

2D背景と3D馬を整合させる。前景遮蔽なし、影S0またはS1。

### H2 — 前景遮蔽込み

H1に手前ラチなど2D foregroundを追加。ラチが馬体・脚を正しい位置で隠すこと。

### H3 — 最適化＋採用影

アーティファクト除去、資源共有、採用した影方式、色調統合を含む提出候補。

色調統合では画面全体を一様にぼかさない。2D世界と3D馬の光方向、明度、彩度、黒レベルを合わせる。

---

## 9. 前景遮蔽テスト

目視だけでなく、少なくとも1頭がラチを横切る時刻で証拠を作る。

```text
foreground-off.png
foreground-on.png
foreground-mask.png
foreground-proof.json
```

確認項目:

- ラチより奥の脚は隠れる
- ラチより手前へ出るべき部分まで消さない
- マスクの縁に白・黒のハローがない
- カメラ追従中にマスクがずれない
- 0秒・4秒・最終フレームで整合する

---

## 10. 性能測定

デスクトップは既存3Dスパイクと同じ環境・方法で3回測り、中央値を採る。

最低条件:

- H3、12頭、採用影、1920×1080
- H3、12頭、影なし
- H3、24頭、採用影
- 2D背景だけ
- 3D馬だけ

記録:

- avg FPS
- p50 / p95 / max frame ms
- 30fps未満割合
- draw calls / triangles / geometries / textures
- heap
- 初回ロードの内訳
- Canvas2D描画時間
- WebGL描画時間
- 合成時間

### デスクトップ合格基準

- 12頭採用影: 平均30fps以上
- p95 50ms未満
- 30fps未満5%未満

### モバイル

最低1台の実機で測る。機種、OS、ブラウザ、解像度、devicePixelRatio、発熱の有無を記録する。

実機へアクセスできない場合は数値を捏造せず、質問書を出す。その場合、デスクトップ判定まで進め、最終採用判定は保留とする。

モバイルでは表示解像度を端末CSSサイズ×上限PixelRatioで測り、1920×1080を無理に要求しない。

---

## 11. 成果物

すべて`out/hybrid-pack-spike/`へ置く。

```text
out/hybrid-pack-spike/
  diagnostics/
    artifact-shadow-on.png
    artifact-shadow-off.png
    artifact-candidate-01-hidden.png
    artifact-candidate-02-hidden.png
    artifact-resolved.png
    artifact-findings.json
  h0-3d-baseline.mp4
  h1-background.mp4
  h2-foreground.mp4
  h3-optimized.mp4
  h0-contact-sheet.png
  h1-contact-sheet.png
  h2-contact-sheet.png
  h3-contact-sheet.png
  compare-reference-2d-3d-hybrid.png
  projection-proof.json
  projection-proof.png
  foreground-off.png
  foreground-on.png
  foreground-mask.png
  foreground-proof.json
  shadow-comparison.png
  performance.json
  mobile-performance.json          # 実機測定できた場合
  resource-sharing.json
  scene-measurements.json
  README.txt
```

比較シートの行:

1. REFERENCE
2. 2D V3
3. 3D C2
4. HYBRID H3

列は5時点。同一サイズ・同一配置。評価文は画像内に書かない。

---

## 12. 自動テスト

最低限、次を追加する。

1. 同一seed・時刻で2D背景、3D馬、前景マスクの入力が一致する
2. 2Dと3Dが同じ`s / w`と注視時刻を使う
3. 投影基準点の中央値3px以下、最大8px以下
4. 地平線ずれ5px以下
5. 蹄接地点ずれ3px以下
6. foregroundマスクが馬より前に合成される
7. 背景、3D、前景の順序を壊すとテストが落ちる
8. 通常`/race`からハイブリッドコードが参照されない
9. 本番環境で検証ページとモデルAPIが404
10. `Date.now()` / `Math.random()`非依存
11. アーティファクト原因メッシュの処置が全12頭へ適用される
12. 非表示メッシュが影を落とさない
13. Geometry/Texture共有数が測定JSONと一致する
14. H0で既存C2のカメラ・馬位置が回帰しない

R-16として、投影ずれ、レイヤー順、アーティファクト処置、通常画面隔離を意図的に壊し、テストが落ちることを確認して復元する。

完了時:

```powershell
npm test
npm run typecheck
npm run build:web
```

---

## 13. 判定基準

開発側は採否を決めない。

### PASS

- 参考、2D V3、3D C2と比べ、H3が馬群の立体感と世界の情報量を両立する
- 投影誤差が基準内
- ラチ遮蔽が3時点で成立
- 足元アーティファクトが原因特定され、除去済み
- 接地影が自然
- デスクトップ性能基準を満たす
- モバイル実機でも運用可能な見込みがある
- 通常レースに影響しない

### CONDITIONAL PASS

映像とデスクトップ性能は成立するが、モバイル、初回ロード、影品質のいずれかが未達で、具体的な最適化経路が測定で示されている。

### FAIL

- 2D背景と3D馬の投影を継続的に合わせられない
- 前景遮蔽がカメラ移動でずれる
- 3D馬が背景から浮き、H3が2D V3より不自然
- アーティファクトを隠す処理が必要
- 背景込みで平均30fpsを下回る
- 通常`/race`を変更しなければ成立しない

---

## 14. 今回行わないこと

- 通常`/race`への組み込み
- 全カット対応
- 競馬場全体の3D化
- 完成版騎手の制作・購入
- 追加アニメーションの制作・購入
- 発走、ゴール、勝馬演出
- HUD改修
- 音声改修
- DBやworker変更
- モデルの外部配布
- 検証ルートを完成版として残す判断

騎手と追加アニメーションは課題として要件をまとめるだけにし、このタスクでは広げない。

---

## 15. 停止条件

以下では質問書`QUESTIONS_P4_HYBRID_PACK_SPIKE_20260823.md`を作り停止する。

1. 足元アーティファクトの原因を特定できない
2. 2D世界を背景・前景へ分離するため通常`/race`の挙動変更が必要
3. 投影誤差を基準内へ入れるため時刻別の手調整が必要
4. 購入モデルの追加配布・変換サービスが必要
5. モバイル実機測定にユーザー操作が必要
6. 既存の未コミット変更と衝突する

モバイル実機だけが不足する場合は、他の成果物を完成させたうえで質問する。

---

## 16. 報告書

完了時:

```text
REPORT_P4_HYBRID_PACK_SPIKE_20260823.md
```

必須内容:

1. 変更ファイル一覧
2. 未コミット作業との境界
3. 足元アーティファクトの原因と証拠
4. 2D背景・3D馬・2D前景の構造
5. カメラ投影の合わせ方と誤差
6. 前景遮蔽の証拠
7. 影方式S0/S1/S2の比較
8. 資源共有と最適化前後
9. デスクトップ性能
10. モバイル性能または未測定理由
11. 決定論とテスト
12. 成果物と再生成方法
13. 観察できた事実
14. 開発側の推測（事実と分離）
15. 本実装へ進む場合の段階案
16. 騎手と追加アニメーションの要件
17. スコープ外の問題

---

## 17. Claude Codeへ渡す開始メッセージ

> `DEV_INSTRUCTIONS_P4_HYBRID_PACK_SPIKE_20260823.md`を最初から最後まで読み、その範囲だけを実施してください。3D方式を継続しますが、通常`/race`への組み込みと競馬場全体の3D化は禁止です。最初に足元の破片状アーティファクトを既存診断ツールで原因特定し、隠さず除去してください。その後、既存2D世界を背景・前景へ分け、3D馬群と同じカメラ・時刻で合成したC2相当の8秒1カットを作ってください。投影誤差、ラチ遮蔽、影、資源共有、デスクトップ性能、可能ならモバイル実機性能を測定し、指定成果物・テスト・報告書が揃うまで完了扱いにしないでください。現在の未コミット作業と既存ポート変更を保持し、commit・pushしないでください。
