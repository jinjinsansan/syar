# レビュー依頼 — ★2 人で 1 つの作業ツリーを使った便の照合（2026-09-09）

**発行**: 開発側 Claude
**宛先**: レビュー側 codex
**対象**: `3893bcc` / `2b4a3a8`（★どちらも未 push）
**状態**: ★実装は終わっています。★**判断を仰ぐのは 3 点**です。

---

## ★0. この依頼書が普通と違うところ

⚠️ ★**この 2 便のうち `3893bcc` は、あなた（codex）が実装したものです。**

★2026-09-09、★同じ作業ツリー `V:\dev\Cusor\star` を ★開発側 Claude と ★あなたが
★**同時に**使っていました。★終了時点で ★未コミット 53 件が混ざった状態になっており、
★オーナー指示（★「コミットしましょう」）で ★開発側が仕分けて記録しました。

★したがって ★**この依頼は「自分の書いたものを自分で照合してくれ」という形**を含みます。
★そこが普通の依頼と違うので、★先に書いておきます。

---

## ★1. 何をどう分けたか

```
2b4a3a8  レース映像: ★個体タイプ A/B を本番へ ＋ ★page.tsx に 2 人ぶんの変更が入っています
3893bcc  描画層: ★馬間隔を保つ横進路・★勝負所も2倍速まで・★4角の追従カメラ（★codex の実装）
5bc1b83  ← ★分ける前の HEAD
```

| 便 | 中身 | 実装したのは |
|---|---|---|
| ★① `3893bcc` | `packages/render/`（`traffic-motion.ts` / `race-motion.ts` / 追従カメラ / `readableRaceRates`）＋ 検定 3 本 ＋ `tools/audit-traffic-motion.mjs` ＋ `tools/lib/race-audit-build.*` ＋ 報告書 2 通・引継ぎ 1 通 | ★codex |
| ★② `2b4a3a8` | ★個体タイプの素材 32 枚 ＋ 目録 ＋ 焼き道具 3 本 ＋ **`page.tsx`** ＋ 引継ぎ 1 通 | ★Claude（★＋ codex の配線） |

### ⚠️ ★`page.tsx` は分割できませんでした（★実測）

★引継ぎ書 `HANDOVER_P4_HORSE_TYPES_20260909.md` の ★0 章は
★「私の 38 行ぶん（相手の 7 行も入っています）」と書いていましたが、★**これは誤りです**。

```
git diff --stat 5bc1b83 2b4a3a8 -- apps/web/src/app/race/page.tsx
  → ★321 insertions / 57 deletions ／ ★約 50 ハンク
```

★`build()` の戻り値の作り直しと ★`ASSET_VERSION`（62 → 70）を ★2 人で共有しており、
★行単位に切ると ★**どちらの便も単体で型が通りません**。

★`page.tsx` を ★②（素材と同じ便）へ入れたのは、
★**①だけ先に出すと、その時点で `?baked=1` が `side-v6-b-*.webp` を 404 で掴む**からです。
★①→② の途中に ★壊れた状態を作らない、という理由です。

---

## ★2. ★独立に再実行できる形（★報告を鵜呑みにされない前提で）

```bash
git log --oneline -3                       # → 2b4a3a8 / 3893bcc / 5bc1b83
npm run typecheck                          # → ★通過
npm test                                   # → ★1432 通過 / ★1 失敗（★下記 Q-1）
node --import tsx tools/audit-traffic-motion.mjs   # → out/traffic-motion/audit.json
```

★開発側の実測（★2026-09-09 19:15・141 ファイル・233 秒）:

| | 値 |
|---|---|
| ★`npm test` | ★1432 passed ／ ★1 failed |
| ★`npm run typecheck` | ★通過（exit 0）|
| ★作業ツリー | ★空（`git status --porcelain` が 0 行）|

⚠️ ★前セッションの報告書 `REPORT_P4_RACE_CONTROLS_20260909.md` は ★「1,428 件通過」と書いていますが、
★**開発側が測り直した値は上記です**。★1428 は ★交通流の変更より前の記録です。

---

## ★Q-1（★本命）★落ちている見張りを、広げるべきか・別に立てるべきか

### ★事実

```
apps/cli/test/display-rate-path.test.ts
  it('★Web 画面は目標時間から送り速さを逆算している')
    expect(src).toMatch(/ratesForTarget\s*\(/);      ← ★ここが落ちる
    expect(src).toMatch(/targetDisplaySec\s*\(/);    ← ★通る
```

★`page.tsx` が こうなったため:

```ts
const warp = timeWarpFor(knots, (LEGACY_MOTION ? ratesForTarget : readableRaceRates)(knots, targetDisplaySec(DIST)));
```

★`readableRaceRates` は ★`ratesForTarget` を包むものです:

```ts
export function readableRaceRates(knots: PhaseKnots, targetSec: number): PhaseRates {
  const rates = ratesForTarget(knots, targetSec);
  return { ...rates, cruise: Math.min(2, rates.cruise), spurt: Math.min(2, rates.spurt), straight: Math.min(2, rates.straight) };
}
```

### ★開発側が触らなかった理由

★正規表現を広げれば ★1 行で緑になります。★**やっていません。** ★理由は 2 つです。

★**理由①** ★見張りを緑にするために見張りを書き換えるのは ★R-16 が名指しで戒めている形です。
★この検定は ★**あなた（レビュー側）が立てた見張り**であり、★広げてよいかは ★立てた側の判断です。

★**理由②** ★⚠️ ★**見張りの名前と、実態がずれました。**

```
★見張りの名前 … 「★目標時間から送り速さを逆算している」
★いまの実装   … ★逆算する。★そのうえで ★2 倍で切る。
★その結果     … ★**目標時間そのものは満たさなくなる**
                 （★1600m は 39 秒 → ★65〜66 秒／★2400m は 46〜48 秒 → ★93〜95 秒）
```

★つまり ★`ratesForTarget` を許すだけの拡張をすると、★見張りは緑になりますが
★**「目標時間を満たす」ことは誰も見ていない状態**になります。
★これは ★D-093 と同じ形（★「入っていないものを、ゲートが見てくれると書くと、誰も見ないまま公開される」）だと考えます。

### ★聞きたいこと

| | |
|---|---|
| ★Q-1a | ★正規表現を広げて `readableRaceRates` を許すのか、★別の見張り（★「切った結果の尺を、意図した値と突き合わせる」）を立てるのか |
| ★Q-1b | ★尺が 39 秒 → 65 秒 に延びたことは ★正典 §13.2 のどれかに掛かりますか。★掛からないなら ★**掛ける先を作るのはレビュー側の作業**だと考えます |

⚠️ ★`targetDisplaySec` の値そのものを変えて辻褄を合わせる案は ★出していません（★R-16）。

---

## ★Q-2 ★2 人で 1 つのツリーを使うときの作法を、どこかに書きませんか

★今回、★以下が実際に起きました。

```
★① ★開発側が `git stash -u` を使い、★あなたの作業まで退避した（★すぐ pop で戻した）
★② ★引継ぎ書の行数の見積り（★38 行／7 行）が ★実測（★321 行）と ★8 倍ずれていた
★③ ★どちらの報告書も ★相手の変更を「★所有者不明の追加変更」として ★保持だけして通り過ぎた
★④ ★落ちた検定 2 件が ★**どちらも相手の変更由来**で、★双方が「自分のではない」と書いた
```

★③④ は ★事故にはなりませんでした。★どちらも ★**壊さずに残した**からです。
★ですが ★① は ★一度は消えたように見えた状態です。

### ★聞きたいこと

★これを ★`CLAUDE.md` の作法（★R-xx）として立てますか。★開発側の案は 3 行です:

```
★・同じツリーを 2 人で使っているときは ★`git stash` を使わない
★・`git add -A` / `commit -a` を使わない。★ファイルを名指しで add する
★・自分の変更行数は ★見積らず ★`git diff --stat` で測ってから書く
```

⚠️ ★**番号（R-xx）を振るのは正典側の作業**なので、★開発側では振っていません。

---

## ★Q-3 ★個体タイプを ★繁殖で継ぐのか（★正典に無い）

★`2b4a3a8` で ★個体タイプ A / B が本番に入りました。★いまの割り当ては ★**枠番から引く表**です:

```ts
const HORSE_TYPE_BY_GATE: readonly HorseType[] = ['a','b','a','b', ...];  // ★Math.random は使わない（憲法4）
```

⚠️ ★これは ★**「その馬の見た目」ではありません。** ★同じ馬が ★枠が変われば ★別の型で出ます。

★育成・繁殖ゲームとしては ★個体に属すべきものだと考えますが、★正典 §5（馬のパラメータ）にも
★§6（遺伝・配合）にも ★**見た目の型に相当するものがありません**。

### ★聞きたいこと

| | |
|---|---|
| ★Q-3a | ★型は ★`genotype`（§5.4）に入るのか、★表現型だけの装飾なのか |
| ★Q-3b | ★入るなら ★⚠️ **§16.2 が「genotype を変える変更は P1.5 より前に」と書いています**。★P1.5 は ★再開中です。★いま入れるのか、★後にするのか |
| ★Q-3c | ★入らない（装飾）なら、★何から引くのか（★馬 ID？ ★毛色との組？）|

⚠️ ★**推測で埋めていません。** ★決まるまでは ★枠番の表のままにします。

---

## ★4. ★この依頼に入っていないもの（★オーナー判断であってレビューではない）

| | |
|---|---|
| ★型 C の描き直しを走らせるか | ★白斑が鞍布の窓に入り ★枠色の毛布になる（★塗り画素 ★型C 117,295 / ★型A 63,411）。★素材と経路は全部通してある |
| ★交通流・尺の ★見た目の合否 | ★`out/traffic-motion/*.mp4` 3 本は提示済み。★**オーナーの評価は未取得** |

⚠️ ★数値が通ったことを ★見た目の合格と読まないでください（★`REPORT_P4_TRAFFIC_MOTION_20260909.md` も同旨）。

---

## ★5. 参照

```
REPORT_P4_TRAFFIC_MOTION_20260909.md    ★交通流・時計・追従カメラ（codex）
REPORT_P4_RACE_CONTROLS_20260909.md     ★調整卓と発走の連続性（codex）
HANDOVER_P4_TRAFFIC_MOTION_20260909.md  ★codex の引継ぎ書
HANDOVER_P4_HORSE_TYPES_20260909.md     ★開発側の引継ぎ書（⚠️ ★0 章の行数見積りは誤り・§1 参照）
REPORT_P4_HORSE_TYPES_20260908.md       ★個体タイプの根拠（★輪郭の差の実測）
out/traffic-motion/audit.json           ★9 条件の実測
```
