# 報告 — #1「俯瞰で馬がぴょんぴょんする」の判断材料

作成日: 2026-08-25 / ブランチ `p4/race-30sec-cuts` / HEAD `ee1a884`
対象: `HANDOVER_P4_2D_MOTION_20260825.md` §3（オーナー指摘 #1）

> ★**これは判断材料です。採否は決めていません。製品コードは 1 行も変えていません。**
> 仮の完歩・代替カメラは監査道具の中だけで組んでいます。カメラ定義・台本・素材・
> 着順・馬の位置・レース結果には触れていません（憲法 3）。乱数・時刻も使いません（憲法 4）。

道具（読取専用・`tools/lib/classification.mjs` の `READONLY` に登録済み）:
`tools/audit-overhead-stride.mjs` / `tools/audit-overhead-stride2.mjs`

---

## 1. 先に結論

1. **引き継ぎ書が使っていた指標（画面上の px/完歩）は、この現象を説明できません。**
   その指標だと `homestretch-side` が 3px/完歩 で**最悪**になりますが、
   オーナーはそこを問題にしていません。
2. 追従カメラでは馬は画面の中で動かないので、走って見えるかを決めているのは
   **足元の地面に対してどれだけ進むか**です。これで測ると順位が実感と合います。
3. **選択肢 1（見た目の完歩を長くする）のほうが、選択肢 2（カメラ）より効きます。**
   カメラをどれだけ動かしても 86% 止まりですが、完歩 11m なら 91% に届きます。
4. ★**選択肢 1 の唯一の懸念だった「カットをまたぐと脚の速さが変わる」は、
   実際には両側とも「ハードカット」でした。**（§5）

---

## 2. 測り方を変えた理由

引き継ぎ書 §3 は「1 完歩で画面上を何 px 進むか」で測っていました。しかしこの 6 ショットは
すべて**馬群を追う（`target: 'pack'`）カメラ**です。カメラが馬を追う以上、馬は画面の中で
ほとんど動きません。この量は「カメラが追えているか」を測っており、
**走って見えるかは測っていません。**

代わりに測る量:

> ★**1 完歩（7m）進んだとき、馬は足元の地面に対して自分の体の何個ぶん動いて見えるか**

その瞬間のカメラで「馬の位置」と「7m 先の地面」を投影し、画面上の隔たりを
馬体長（`HORSE_LENGTH_M` 2.4m ×`pxPerM`）で割ります。馬の絵は板（ビルボード）なので
`pxPerM` に比例した大きさで描かれますが、**走路方向の動きだけが見込み角で圧縮されます。**
この比が小さいほど「脚は回るが進まない」＝足踏みに見えます。

実馬の基準は **7m ÷ 2.4m = 2.92 馬身/完歩**。

---

## 3. 実測（seed 42・台本 v5・30fps・先頭馬・中央値）

| ショット | 馬身/完歩 | 実馬比 | オーナー評 |
|---|---:|---:|---|
| `start-front` | 1.47 | **50%** | 指摘なし ⚠️（§6） |
| `first-corner-front` | 2.30 | 79% | 指摘なし |
| `side-drive` | 2.73 | 94% | 指摘なし |
| **`fourth-corner-high`** | **1.82** | **62%** | ★**「ぴょんぴょんする」** |
| `homestretch-side` | 2.88 | 99% | 指摘なし（旧指標では 3px で最悪） |
| `finish-line` | 2.89 | 99% | 指摘なし |

走行中のコマだけ（先頭 8m/秒 超）に絞っても値は変わりませんでした。

★`homestretch-side` が 99% になることが、この指標の妥当性の主な裏づけです。
旧指標では最悪だった箇所が、実感どおり「問題なし」側に来ます。

---

## 4. 選択肢 1 — その俯瞰だけ見た目の完歩を長くする

`fourth-corner-high` のカットは **3.3 秒**、そのときの真の走速は **14.56 m/秒**。

| 見た目の完歩 | 馬身/完歩 | 実馬比 | 完歩/秒 | 脚の速さ |
|---:|---:|---:|---:|---|
| **7 m** | 1.82 | 62% | 2.08 | ← 現行（全カット共通） |
| 9 m | 2.25 | 77% | 1.62 | 0.78 倍 |
| **11 m** | **2.65** | **91%** | 1.32 | 0.64 倍 |
| 14 m | 3.19 | 109% | 1.04 | 0.50 倍 |
| 18 m | 3.82 | 131% | 0.81 | 0.39 倍 |

- **9m** … 前例のある値（「ウサギ」指摘への対処で一度 7→9 にした経緯がコメントに残る）。
  `first-corner-front`（79%・指摘なし）と同じ水準まで来ます。脚は 0.78 倍。
- **11m** … `side-drive`（94%）に並びます。脚は 0.64 倍。

---

## 5. ★選択肢 1 の懸念は、実際には小さい

引き継ぎ書は「⚠️ カットをまたぐと脚の速さが変わる」を選択肢 1 の欠点として挙げていました。
**この俯瞰の前後は、両方ともディゾルブを掛けないハードカットでした。**

| 境目 | 視点の系統 | `sameFamily` | `FLASH_INTO` | 実際の繋ぎ |
|---|---|---|---|---|
| `side-drive` → `fourth-corner-high` | side → high-diag | false | 対象外 | ★**ハードカット** |
| `fourth-corner-high` → `homestretch-side` | high-diag → side | false | 対象外 | ★**ハードカット** |

`page.tsx` のディゾルブ条件は `FLASH_INTO.has(c.to) || sameFamily(c.from, c.to)` で、
`FLASH_INTO` は `side-drive` のみ。したがってこの 2 か所は重ねずに切り替わります。

★**画角も向きも素材も一斉に変わる瞬間**なので、脚の速さが変わっても同じ切替に紛れます。
脚の速さの変化が目立つのは同じ画のまま変わるときで、ここはそれに当たりません。

---

## 6. ⚠️ この指標の限界（正直に）

`start-front` は **50%** と最も低いのに、オーナーは指摘していません。
つまりこの指標は**必要条件であって十分条件ではありません。**

考えられる違い（★測っていません・推測です）:
- `start-front` は正面（`diag-front`）で、馬が**こちらへ向かって大きくなる**。
  大きさの変化が近づく手掛かりになり、横の移動量の少なさを補っている可能性
- 発走直後は 0.7 秒のカメラ揺れが入る
- 待機から解き放たれる瞬間で、そもそも「進んでいない」ことが自然

→ **`fourth-corner-high` を 62% から上げれば直る、とまでは言い切れません。**
言えるのは「実感と順序が一致する唯一の指標で、この俯瞰は下から 2 番目」までです。

---

## 7. 選択肢 2 — 俯瞰の位置を変える

`high-diag` の組み立ては `along = -backM×0.45` / `across = max(sideM, backM×0.55)` / `up = upM`。
現行 `backM 40 / upM 6.5 / sideM 13` は **後ろ 18.0m・横 22.0m・高さ 6.5m ＝ 仰角 12.9°** です。

★**「俯瞰」という名前ですが、実際には仰角 12.9° のほぼ真横後ろです。**
走路方向の動きを潰しているのは高さ不足ではなく、**後ろ成分 18m** です。

| backM | upM | sideM | 後ろ | 横 | 仰角 | 馬身/完歩 | 実馬比 | 画面内の頭数 | ねらい |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 40 | 6.5 | 13 | −18.0 | 22.0 | 12.9° | 1.82 | 62% | 7.0 | ← 現行 |
| 40 | 26 | 13 | −18.0 | 22.0 | 42.4° | 2.19 | 75% | 9.0 | 高さだけ上げる |
| 24 | 6.5 | 13 | −10.8 | 13.2 | 20.9° | 1.73 | **59%** | 3.0 | 後ろを減らす（★悪化） |
| 14 | 6.5 | 13 | −6.3 | 13.0 | 24.2° | 2.05 | 70% | 2.0 | さらに減らす |
| 14 | 6.5 | 22 | −6.3 | 22.0 | 15.9° | 2.41 | 83% | 3.0 | 後ろを減らし横へ離す |
| **14** | **18** | **22** | −6.3 | 22.0 | 38.2° | **2.52** | **86%** | 6.0 | 後ろを減らし＋高く |
| 24 | 18 | 22 | −10.8 | 22.0 | 36.3° | 2.32 | 80% | 6.0 | 折衷 |

⚠️ `backM` を下げると `across` も一緒に下がる（`max(sideM, backM×0.55)`）ため、
`sideM` を上げないと**画面内の頭数が 7 → 2〜3 頭に落ちます**。単独では使えません。

**選択肢 2 の要点**: 最も効く組み合わせでも **86%** で、選択肢 1 の 11m（91%）に届きません。
しかも仰角 12.9° → 38.2°、カメラ距離も大きく変わるので、★**別の画になります。**
背景の 1 枚絵（`raceCornerHigh`）はこの仰角に合わせて描かれているため、
整合が取れるかは**測っていません**（絵を見る必要があります）。

---

## 8. まとめ（採否はオーナー判断）

| | 効果 | 副作用 | 触る範囲 |
|---|---|---|---|
| **選択肢 1（完歩 9m）** | 62% → **77%** | 脚 0.78 倍。前後はハードカット | この俯瞰だけの見た目の完歩 |
| **選択肢 1（完歩 11m）** | 62% → **91%** | 脚 0.64 倍。前後はハードカット | 同上 |
| **選択肢 2（14/18/22）** | 62% → **86%** | 仰角 12.9°→38.2°・**別の画**・背景 1 枚絵との整合が未確認・頭数 7→6 | カメラ定義値 |
| **選択肢 3（そのまま）** | — | 指摘が残る | — |

★開発側の所見としては、**効果が大きく・触る範囲が狭く・唯一の懸念（カット跨ぎ）が
ハードカットで薄まる選択肢 1 が有利**に見えます。ただし §6 の限界があるため、
**どの値でも最後は実画面で見ていただく必要があります。**

## 9. 再現手順

```powershell
npx tsx tools/audit-overhead-stride.mjs     # §3 の表と選択肢 1・2 の概観
npx tsx tools/audit-overhead-stride2.mjs    # 走行中のみの測り直しと §7 の梃子の切り分け
```
