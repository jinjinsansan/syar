# 報告: 案 B（全馬で調教を 0 週から）の模擬 — 2026-09-22

> 開発側 → レビュー側（→ オーナー）。PLAN Q-1・`QUESTIONS_FOAL_LIFECYCLE_20260922.md` §2 の案 B。
> 規則は変えていません。DB に繋がない模擬です。本番には触れていません。

## §0 一行

今の成長曲線を前へ平らに延ばして調教を 0 週から許すと、次のようになる。
- V-14 ② の差がほぼ線まで縮む（15.8 → 12.3pt、線 12pt）
- キャリア後半の伸びが約 4 割減り、放置のほうが伸びるように逆転する（17.4 → 10.7pt、放置 13.3pt）
- 1 頭あたりの EP が約 43% 増える（53,003 → 75,883）

**案 B は、成長曲線と較正を決め直さないと入れられない。** 案 A（今の規則）を続ける判断を支持する材料です。

## §1 数（400 頭・seed 42 ／ 確認に seed 7。± は SE）

| 調教の開始 | V-14 ① 適切な育成（線 88%） | V-14 ② 差（線 12pt） | V-14 ③ 比 | 後半の伸び 適切な育成 | 後半の伸び 放置 | EP/頭 適切な育成 | 故障/頭 |
|---|---|---|---|---|---|---|---|
| **78 週（今・案 A）** | 90.3% ±0.24 | 15.8 ±0.35 | 0.679 | **17.37** ±0.42 | 16.87 | **53,003** | 0.91 |
| 52 週 | 92.3% | 14.7 ±0.37 | 0.672 | 14.57 | 15.60 | 60,685 | 1.10 |
| 26 週 | 93.7% | 13.5 ±0.39 | 0.665 | 12.52 | 14.41 | 68,088 | 1.20 |
| **0 週（案 B）** | 94.8% ±0.22 | **12.3** ±0.41 | 0.662 | **10.74** ±0.42 | **13.32** | **75,883** | 1.37 |
| 確認 seed 7: 78 → 0 | 90.1 → 94.4 | 16.1 → 12.7 | 0.681 → 0.662 | 17.75 → 11.30 | 16.82 → 13.44 | 52,973 → 75,806 | — |

- 78 週の行は、本物の `verify-v14`（`--horses 400 --seed 42`）の出力と一致した（90.3%・15.8pt・0.68・EP 53,003／17,803／83,614）。**同じ経路で測れている。**
- 後半の伸び: 182〜260 週齢で伸びた分を素質に対する % で平均したもの（D-044 の「キャリア後半 78 週」）。A と B で**同じ週齢の窓**を比べている。

## §2 読み方

1. **V-14 ②（差 12pt）がほぼ線上に来る**。seed 42 で余裕 0.3pt ＝ 約 0.7 SE、seed 7 で 0.7pt。早く始めるほど放置の馬も長く伸び、差（＝デイリー来訪の動機・D-048）が縮む。
2. **キャリア後半の伸びが約 4 割減り、放置が適切な育成を上回る**。適切な育成の馬は素質の上限に早く近づき、後半に伸びる余地が無くなる。D-044 が防ごうとした「後半の伸びが消える」の方向そのもの。
3. **EP の消費は約 43% 増える**（放置でも増える。放置 ＝ 既定の軽め調整にも費用がかかる）。§3.4 の収支と V-11 系の取り直しが要る。
4. 故障は 1 頭あたり 0.91 → 1.37 回。
5. V-14 ① と ③ は通る（① は上がり、③ はわずかに下がる）。

## §3 仮定と限界

- **B-1** 104 週未満の成長の係数は 104 週の値のまま（`growthCoef` は外挿しない）。0〜78 週の成長曲線は**決まっていない**。若い時期の係数を小さくすれば ② と後半の伸びは戻るが、それは較正の仕事（AL-11 の族）。
- **B-2** 出走・引退の週齢、育成方針は変えていない。
- 母集団は V-14 と同じ（`runSimulation` の最終世代・400 頭）。seed は 2 つ。
- ワーカーの週送り・NPC の調教 AI・EP 経済の世界全体は動かしていない。見たのは 1 頭の一生だけ。

## §4 変えたもの

| ファイル | 変更 |
|---|---|
| `packages/training/src/week.ts` | 任意の入力 `trainableFromForSimulation` を追加（模擬専用）。省けば `canTrain`（§7.1）のままで、振る舞いは変わらない |
| `apps/cli/src/training-career.ts` | `runCareer` に任意の `startWeek` を追加。既定の 78 なら上の入力を渡さない（較正した経路と 1 ビット同じ） |
| `apps/cli/src/lifecycle-b-sim.ts`（新規） | 模擬の道具（`--starts 78,52,26,0`・`--json`） |
| `apps/cli/test/lifecycle-b-hook.test.ts`（新規） | ① 製品のソース（worker・web・packages）が渡していない ② 渡せば 78 週より前でも調教する ③ 渡さなければ §7.1 のまま（境界の両側） |

- 🔴 最初の版は**4 通りとも一字一句同じ数**を出した。開始週だけを早めても、エンジンの `canTrain` が 78 週未満の調教を止めていたため。そこで模擬の口をエンジンに足し、「口が効く」ことを試験 ② で固定した。
- 変異: ワーカーに `trainableFromForSimulation: 0` を書くと試験 ① が赤。戻して緑。
- 取引の境界は変えていない。

## §5 判断に要ること（オーナー）

- 案 A を続けるなら、この便はここで終わり（道具と試験は残す。曲線を決めた日に測り直せる）。
- 案 B を採るなら、先に **0〜78 週の成長曲線**を決め、V-14 ②・後半の伸び・EP の収支を線の上に戻す較正が要る。
