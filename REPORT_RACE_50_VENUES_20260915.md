# 報告 — レース演出を 10 場・50 鞍に広げる（第 1〜5 便）と、発走前の流れ

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-15 ／ 開発側 → レビュー側・オーナー
**対象**: 相談 `CONSULT_RACE_50_VENUES_20260915.md` ／ 回答 `REVIEW_CONSULT_RACE_50_VENUES_ANSWER_20260915.md`（オーナー決定 §8-1）／ 計画 `PLAN_RACE_50_VENUES_IDEAS_20260915.md`
**ブランチ**: `p4/race-30sec-cuts` ／ **開始 HEAD** `57adf77` ／ **終了 HEAD** `2e5ff53`（＋未コミットの発走前の流れ・§5）
**コミット**: `3646e69`（レビュー側の発行物の受領）→ `80c4eb3`（本番の 10 場配線・別報告 `REPORT_VENUE_WIRING_20260915.md`）→ `2e5ff53`（本報告の第 1〜5 便）
**タグ**: `p4-race-50-venues-20260915`（注釈付き・`2e5ff53`）／ **push**: していません

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **計算** ＝ コードの定数から出した値 ／ **△** ＝ 未確認・推定 ／ **[EYES]** ＝ オーナーの目で決めること
> ⚠️ **指示書の外での実装です。** 根拠はオーナーの直接の指示（§1 に原文）と、回答書の §7 の順番表です。**回答書 §7 の順番どおりではありません**（§6-1）。
> ⚠️ 色・秒・長さの数値は、特に書いていないものは**開発側の仮置き**です（オーナー判断「デザイナーの回答を待たずに作る」）。
> ⚠️ 本番 DB への接続・書き込み・配備はしていません。正典は編集していません。

---

## 0. レビュー側へ — 照合していただきたいこと

| # | 照合していただきたいこと | 場所 | 確かめ方 |
|---|---|---|---|
| R1 | **長距離 4 鞍で跳びが 2 か所**になり、検査を鞍ごとの期待値にした（回答 §2 条件 1）。4 鞍の選び方が回答 §0-1 の計算と一致するか | `course.ts` `firstPassStraightsMOf`・`race-elision.ts` `sideOnlyShownMetersOf`・`side-only-script.test.ts` ② | `npx vitest run apps/cli/test/side-only-script.test.ts apps/cli/test/first-pass-time-of-day.test.ts` |
| R2 | **発走前の人気馬の紹介・出馬表が結果を読まない**（D-098） | `page.tsx` の `paddock`・`grade`・`entry` の分岐 | `npx vitest run apps/cli/test/race-preshow-wiring.test.ts`（①：分岐の中に `result`・`winner`・`finishSec` 等が無い） |
| R3 | **時間帯の決め方**（オーナー決定 2 の条件: 日本時間固定・対応表 1 か所・デモの口と既定の検査） | `packages/scheduler/src/time-of-day.ts`・`packages/render/src/time-of-day-look.ts` | `first-pass-time-of-day.test.ts` ⑤⑥⑦ |
| R4 | **重ね塗りの視認性の条件**（回答 §4-3）のうち、**満たしていないもの**がある（§4 の表） | §4 | — |
| R5 | **監査道具の鞍の受け方**（回答 §6-1・§6-2）は**未対応**（§6-2） | `tools/lib/race-audit-build.mjs` | — |
| R6 | **馬の速さの訂正**（`straightMetersLeft`）は画面と監査道具の**両方**を直した。旧来は直線の長い場で馬が速すぎた | `page.tsx`・`race-audit-build.mjs`・`race-clock-wiring.test.ts` ⑨ | §2-1 |

---

## 1. オーナーの指示（原文・時系列）

1. 「この50のレース演出を作ってください　エンジンなどは後回しがいいのです」
2. （確認）「軽いものから／待たずに作る／残したまま進める」
3. 「今流星大賞典見ていますが　これ30秒あります？　あまりにも短くないですか？　桜星賞で1600m　で３０秒で作ってるので　2000mならばもう少し長くなりませんか？　また馬の走るのを倍速にしていませんか？　有り得ないくらいに足が早い」→（確認）「直す／距離に比例して伸ばす」
4. 「なんかスピードが一瞬緩む、ような場面があります。」→（確認）写真判定は「止めない」
5. 「進めてください。そして、次から馬の数を８頭にしてください。騎手の色を全て完全に変えてください。」
6. 「冬の雪はいいですが　春の桜と　雨の落ち葉みたいな？のもおかしいです。　雨は雨だけにしてください。」
7. 「５便も進めてください。音源は後回しでいいです」
8. 「ＯＫです一旦ここでコミットしましょう　git tagもつけてください」
9. （競合機の動画を見て）「動画は「人気馬の紹介 → 格とレース名 → 出馬表 → ゲート　動画の通りにします」
10. 「パドックの絵を焼きましょう　歩きの絵も焼いてください。」

---

## 2. 直したもの（オーナー指摘から）

### 2-1. 馬が速すぎた ✔

- **原因**: 位置モデルの `straightMetersLeft` は「**境界時刻 `straightSec` が指す地点**」で、エンジンの `boundaryTimesOf` が**残り `PHASE_METERS.STRAIGHT`（400m）固定**で出す値でした。画面と監査道具はそこに**走路の直線の長さ**（290〜620m）を渡しており、400m 分の時間でその長さを走らせていました
- **実測**（先頭の直線の速さ）: 天河 620m **28.9 m/s**・潮風 310m **12.2 m/s**（桜星賞 400m は 17.0 m/s）
- **直し**: 画面・監査道具とも `PHASE_METERS.STRAIGHT` を渡す（`race-clock-wiring.test.ts` ⑨ が構文木で固定・変異つき）
- **直した後** ✔: 50 鞍すべてで最後の直線の速さ **16.3〜17.1 m/s**
- ⚠️ 着順・走破タイムには触れていません（表示の位置モデルだけ）

### 2-2. 尺を距離に比例 ✔

- `sideOnlyShownMetersOf`: 発走を `min(発走から続く直線, 200 × 距離/1600)`、最後の直線を `min(最後の直線, 250 × 距離/1600)` 見せる
- 以前は発走の長さを `course.segments[0].length` で取り、引き込み線が直線を割る鞍で **20〜500m**（流星大賞典 40m）でした
- 本編の尺 ✔: **1000m 約 18 秒 〜 3600m 約 45 秒**。**桜星賞は 29.4 秒のまま**（200m・250m は不変・検査 ⑥）

### 2-3. 写真判定で止めない・発走前後のコマ落ち ✔

- 写真判定の止め絵をやめ、見出し（1.4 秒）だけ（`PHOTO_FINISH_BADGE_SEC`）
- `posOf` を毎回全区間なめていたのを、区間の始点表（WeakMap・二分探索）に。**旧実装 `posOfWalk` とビット一致**を検査（`course-pos-cache.test.ts`）
- 勝ち馬の通過の表示秒を毎コマ計算していたのを、組み立て時に 1 回（`Built.finishCrossD`）
- 本番ビルドで発走前後 **19〜31 fps**（開発サーバーは 7〜18）✔ △ 端末 1 台・1 回の目視計測

### 2-4. 8 頭立て・勝負服 ✔

- `FIELD = 8`（画面）・`RACE_DEFAULTS.field = 8`（監査道具・R-31）
- 上着は 8 頭立て専用の 8 色 `jacket8-1..8`（`palette.json`）。**上着どうし ΔLab 最小 42.2・上着と帽子（枠色）最小 40.8**（`silks-distinct.test.ts`）

---

## 3. 入れたもの（計画書の番号）

| 便 | 番号 | 中身 | 出どころ（1 か所） | 検査 |
|---|---|---|---|---|
| 1〜2 | V-1〜V-9 | ゲートの色と屋根・ゴールの目印（板／円盤／門）・距離標・芝の刈り模様・観客の差し色・コース脇の旗（その回の風）・実況者・コース図の色・場の紹介 1 行 | `packages/render/src/venue-look.ts` `VENUE_LOOKS`（10 場・**名前の文字列から引かない**） | `venue-look.test.ts`（17）・`venue-flags-sound.test.ts`（7） |
| 2 | R-1・R-8 | 格の雰囲気（観客の空席率・勝ち馬の帯の縁・イントロの英字・ファンファーレの強さと余韻） | `GRADE_LOOKS` | 同上 |
| 2 | R-2・R-3・R-9 | 条件のチップ（2 歳・牝馬限定）・三冠の第 n 戦（`GradedRace.series` を**明示のフィールド**で追加）・リプレイの見出し | `graded-races.ts` | 同上 |
| 3 | R-6・V-12 | 季節の色味（月から 5 段・地面と景色で別の色）・白砂のダートの砂色 | `season-look.ts` `SEASON_LOOKS` | `season-atmosphere.test.ts`（7） |
| 3 | R-7・C-2 | **舞うものは冬（1〜2 月）の雪だけ**・**雨の日（重・不良）は雨だけ**（オーナー指示 6） | `seasonParticlesFor`・`rainDropsOf` | 同上 ③ |
| 4 | R-5 | 長距離 4 鞍の「発走 → 1 周目のスタンド前 200m → 最後の直線」 | `firstPassStraightsMOf`・`FIRST_PASS_SHOWN_M` | `first-pass-time-of-day.test.ts` ①〜④・`side-only-script.test.ts` ② |
| 4 | C-1 | 時間帯（朝・昼・夕・夜） | `@star/scheduler` `TIME_OF_DAY_BANDS`／`@star/render` `TIME_OF_DAY_LOOKS` | 同上 ⑤〜⑧ |
| 5 | V-13・V-15 | 場の遠景（図形）・場の紋をタイトルカードに・霧の場はレース中も霧 | `venue-scenery.ts`（`VenueLook.scenery` の明示の欄） | `venue-scenery.test.ts`（6） |

### 3-1. 長距離の 3 幕（回答 §2 の条件）

- **4 鞍** ✔: 銀河賞 `g1-ginga`・北極星カップ `g3-hokkyokusei`・大河原記念 `g2-ookawara`・白光記念 `g3-hakko`。`firstPassStraightsMOf` が全 50 鞍でこの 4 鞍だけを返す（検査 ①）。区間の終わりは**距離 − 1 周**、長さは**直線の長さ**（回答 §0-1 の「残り [1 周, 1 周 ＋ 直線]」と一致）
- **条件 1（跳びの数）** ✔: `side-only-script.test.ts` ② を鞍ごとの期待値に（4 鞍は 2・他は 1）。「見せる区間がコーナーに掛からない」は全鞍のまま
- **条件 2（勢いのバー・展開の見出しを出さない）**: 勢いは残り `MOMENTUM_FROM_M`（200m）からなので、残り 2100m より手前の 1 周目には**式の上で出ません**（計算）。展開の見出しは △ 未確認
- **条件 3（長く見せない）**: 200m（約 12 秒）[EYES]
- **区間名の直し** ✔: `broadcastV2SectionLabel` は `直線` の区間をすべて「最後の直線」と出していた。発走が直線の中にある鞍（天穹賞など）で**スタート直後に「最後の直線」**、4 鞍の 1 周目でも同じ。→ 発走の直線は「スタート後」、1 周目は「スタンド前」
- 実画面 ✔: 銀河賞・白光記念で「スタンド前へ」のコース図 → 見出し「スタンド前」→「最後の直線へ」

### 3-2. 時間帯（オーナー決定 2 の条件）

- 入力は発走の時刻（UTC ミリ秒）→ **日本時間 +540 分で固定**（`timeOfDayOfScheduledAt`）。端末の時計は読みません
- 対応表は `TIME_OF_DAY_BANDS` の 1 か所（00:00 夜／05:00 朝／10:00 昼／16:30 夕／18:30 夜・仮置き）。G1 の枠 54・78・120 は朝・昼・夜（検査 ⑤）
- デモの口 `?tod=morning|day|dusk|night` と開発卓の「時間帯」。**省くと昼**・知らない値は昼へ落として `fellBack: true`（検査 ⑥）
- **昼は何も重ねない**＝既定の鞍の画は変わらない（検査 ⑦）
- ⚠️ **本番の画面にはまだ繋いでいません**（`/race` はデモ）。本番のレース画面が発走の時刻を持つようになったら、`timeOfDayOfScheduledAt` を通すこと
- 較正定数の登録簿に `JST_OFFSET_MINUTES`・`TIME_OF_DAY_BANDS` を免除として理由付きで登録（`calibration.ts`）

### 3-3. 場の景色（第 5 便）— 絵ではなく図形にした理由 ✔

- **測った**: レース中の真横の板で空と遠景が見えるのは画面の上 **約 9%** の帯で、その大半は木の頭（板 941 行のうち木の層は 0〜188 行・窓は zoom 1.14 で 116 行目から）
- → 回答 §3・計画書 V-13 の「遠景 1 層を 10 枚」はレース中にほぼ映らないので、**景色が大きく映るタイトルカードの背景**に場の主題を**図形で**描き足しました（絵の生成・承認・登録簿 D-087 が要らない）
- 霧の場（霧ヶ原）はレース中も画面の上 45% に霧（馬より先に描く・上限 0.4）
- ⚠️ **回答 §3-2 の担保 2「オーナーが実在の場と照らして確認した旨と日付を註記に残す」はまだです**（`venue-look.ts` の註記に「確認を経て日付を残すこと」とだけ書いてあります）

---

## 4. 回答 §4-3 の視認性の条件 — 満たしたもの・満たしていないもの

| 条件 | 状態 |
|---|---|
| 1. 濃さに上限（定数・註記） | ✔ 季節 `ATMOSPHERE_TINT_MAX` 0.12・砂色はその 2 倍まで・時間帯 `TIME_OF_DAY_TINT_MAX` **0.5**（夜は景色を暗くしないと夜に見えないため）・霧 `VENUE_FOG_ALPHA_MAX` 0.4。すべて検査で固定 |
| 2. 自馬・スタミナゲージ・HUD・勝負服・枠番に掛けない | ✔ 色は**背景の板の層の矩形だけ**に重ねる（`parallax-plate.ts`）。雪・雨・霧・旗は**馬より先**に描く（`broadcast-v2-scene.ts`・検査は描画順の文字列位置）。⚠️ **透視ワールドのカット**は層に分けられないので、地面の色を**画面全体に 1 回**重ねています（馬より先なので馬・HUD には掛からない） |
| 3. 介入の局面（残り 900m〜）で弱める | △ **時間帯だけ**弱めています（`TIME_OF_DAY_INTERVENE_FACTOR` 0.5）。**季節の色味・雨・雪・霧は弱めていません** |
| 4. 画素のコントラストを測る道具 | ✗ **未着手** |

---

## 5. 発走前の流れ（未コミット・オーナー指示 9・10）

オーナーが競合機のレース前後を撮った動画（`CONSULT_GAME_DESIGN_TWO_MODES_20260915.md` §1-14）の順番で作りました。**レース本編は変えていません。**

| 秒（仮置き） | 場面 | 関数 |
|---|---|---|
| 0〜18 | 人気馬の紹介（3 → 2 → 1 番人気・各 6 秒） | `drawPaddockIntro`・`paddockPicksOf` |
| 18〜19.6 | 空撮 | 既存 |
| 19.6〜22.2 | 格の紹介（「GRADE I」→「GI」→ 閃光） | `drawGradeIntro`（英字は `GRADE_LOOKS.roman`） |
| 22.2〜25.2 | レース名 | 既存 `drawRaceTitleCard` |
| 25.2〜31.2 | 出馬表（全画面・背景は競馬場） | 既存 `drawEntryBoard` に暗幕と板の濃さの口を追加（省くと従来） |
| 31.2〜32.4 | ゲート待機 | 既存 |
| 32.4〜 | 発走 | — |

- **発走の表示秒 `RACE_INTRO_RACE_START_SEC` は 4.4 → 32.4 秒。** 2026-09-13 のオーナー指示「詰めましょう」（7.8 → 4.4）を今回の決定で**上書き**しています（R-7）
- **監査道具が発走の秒 4.4 を自前で持っていた**ので、`@star/render` の定数を読む形に直しました（`race-preshow-wiring.test.ts` ③）。⚠️ `tools/audit-race-motion.mjs` など、`RACE_INTRO_RACE_START_SEC` を import している道具は自動で新しい値になります。**過去の撮影・監査の秒指定はずれます**
- **人気とオッズ**: デモの `/race` にはオッズが無いので、馬名と同じくデモの値 `DEMO_WIN_ODDS` を 1 か所に置き、オッズの低い順に人気を付けるだけ（`popularityRanksOf`）。**結果・能力からは作っていません**。本番はサーバーのオッズを渡す
- **ファンファーレ**は人気馬の紹介の間は鳴らさず、空撮から（音源 16.8 秒・発走まで約 14 秒）
- **パドックの絵** ✔: 背景 `paddock-bg-v1`（Codex 生成・雛形 `design/art/prompts/paddock-bg-v1.txt`）を配線済み。**歩きのコマ**は型 A の 8 コマ `horse-jockey-side-walk-v1`（雛形 `deformed-8frames-walk.txt`・`tools/gen-pose-set.mjs`）。仕上げは `tools/publish-walk-frames.mjs`（緑抜き → 胴基準の位置合わせ → 勝負服の無彩色化）。**PC の原版経路だけ**読み、揃わなければ走りのコマに戻る。⚠️ **携帯の焼いた経路は走りのコマのまま**
  - 揃い ✔（`verify-pose-set.mjs`）: 接地のばらつき **0.9%**・高さ **1.2%**（レースの走りのコマ `side-v8` は 8.3%・7.9%）。⚠️ 道具の合否線「隣のコマの変化 20% 以上」は **6→7 コマ目 15.9% で不合格**。この線は駈歩の脚の動きを前提にしたもので、常歩は変化が小さいのが自然と読んでいます △
  - ⚠️ **型 B の歩き 8 コマも生成しましたが使っていません。** オーナー指示（「足が白ブチの馬を一旦使わないでください」）でレースは全枠が型 A（`page.tsx` `HORSE_TYPE_BY_GATE`）で、型 B の絵は四肢が白いため。`out/gen/unused/` に退避し、コミットしていません（生成の前に表を確かめなかった開発側の見落とし）
- ⚠️ Codex が生成のたびに `apps/web/public/art/` に作業ファイル（`paddock-introduction-v1.*`・`horse-jockey-type-[ab]-walk-frame01.*`）を書きます。`publish-walk-frames.mjs` が `out/gen/codex-strays/` へ退避します。**コミットに混ぜません**
- **デザイナー側**にこの流れの見た目（人気馬の紹介の帯・G1/G2/G3 で違う格の紹介・全画面の出馬表・ゴール後）を依頼しました（Claude Design `requests/R-11-race-preshow.md`）

---

## 6. 回答書と違うこと・やっていないこと

### 6-1. 回答 §7 の順番どおりではない

- 回答 §7 の **2（① 止め絵を放すときの跳び・③ 右回りの検査）** より先に、**3〜5（10 場の軽い案・長距離・景色／季節／時間帯／天気）** を入れています。オーナーの指示 1「この50のレース演出を作ってください」によります
- 写真判定の止め絵は**オーナー判断で止めない形**にしたので、① の「止め絵を放すときの跳び」は対象が無くなった △（止め絵そのものを消した）
- **③ 右回りの検査・② 写真判定の検査・④ 服の塊の計測・⑤ 実況の着差の言葉・⑥ 勢いの入力**は未着手

### 6-2. 監査道具（回答 §6）

- **§6-1 鞍を受ける** ✗: `buildAuditRace` は `spec`・`turn` を受けるまま。場の見た目・季節・時間帯を監査道具は**読みません**
- **§6-2 場ごとの素材の解決関数** △: 場の見た目は `venueLookOf` の 1 か所から画面と検査が引きますが、**監査道具は引きません**。場ごとの**絵の素材**は作らなかった（§3-3）ので、`?venueArt=shared` の口も作っていません
- **§6-3 重い映像の不変条件を 10 場 × 最短最長で** ✗: 未着手
- **以前からの差**（本便で見つけた・触っていない）: 監査道具 `auditClock` は `raceClockFor(knots, DIST, policy)` で、画面と違って**見せない区間（elisions）を渡していません**

### 6-3. その他の気づき（触っていない）

- 跳びを覆うコース図の画面の間、右下の残り距離が覆いの見出しの残り距離とずれて見える（白光記念で見出し 1667m・右下 2402m）✔ 目視
- タイトルカードの背景の絵（`race-title-spring-v1`）に**桜が描かれていて全 50 鞍で出ます**。冬や夜の鞍でも出る [EYES]
- **デモの馬名に実在の地名を含むもの**: 3 番「ハンシンドリーム」（既定の自馬）・7 番「ナニワスピリット」（`page.tsx` `HORSE_NAMES`）。憲法 §0.1 に照らして差し替えるか **オーナー判断待ち**

---

## 7. 検査 ✔

- `npm run typecheck`（ルート・画面）… エラー 0
- `npx vitest run --maxWorkers=2 --minWorkers=1`（2026-09-15・`2e5ff53` ＋ 未コミットの発走前の流れと歩きのコマの配線）… **1771 件中 1770 件通過**
  - 落ちている 1 件は `packages/render/test/edit-grammar-audit.test.ts` ⑨「seed 分類を現 HEAD で再確認している」（独走代表 expected 'contest' to be 'solo'）。**本便の前から落ちています**（8 頭立て以前から）。△ 8 頭立てにしたことで分類がさらに動いているかは確かめていません
- 新規の検査ファイル: `venue-look`・`venue-flags-sound`・`course-pos-cache`・`season-atmosphere`・`first-pass-time-of-day`・`venue-scenery`・`race-preshow-wiring`
- 実画面（開発サーバー 3210・ヘッドレス）✔: 8 頭・旗・雪（寒椿賞）・雨だけ（流星大賞典 不良）・花びらが出ない（桜星賞）・スタンド前（銀河賞・白光記念）・朝夕夜（桜星賞）・雪山／海／星空と川の夜／霧のタイトル・発走前の 6 場面・パドックの背景
- ⚠️ **撮影の途中でメモリ不足により開発サーバーの見張りと撮影が 1 度強制終了**しました（空き 2.9GB）。直した後の版を撮れていない場: 白砂・月見丘・陽光台・大河原・青嶺のタイトル

---

## 8. オーナーに確認していただくこと

| # | 確認 |
|---|---|
| O-1 | デモの馬名「ハンシンドリーム」「ナニワスピリット」を差し替えるか（§6-3） |
| O-2 | 場の見た目（景色の主題・ゲート・ゴールの目印）が実在の場を指させないことの確認と日付（回答 §3-2） |
| O-3 | 発走前の各場面の秒（紹介 6 秒 × 3・格 2.6 秒・レース名 3 秒・出馬表 6 秒）[EYES] |
| O-4 | 夜の暗さ（上限 0.5）と、介入の局面で季節・雨・霧も弱めるか（§4 条件 3） |
| O-5 | タイトル背景の桜を季節で差し替えるか（§6-3） |

## 9. このあと

1. 歩きのコマ（型 A）は置いて実画面で確認済み ✔（6 番・8 番の紹介で歩きのコマ・勝負服と毛色の塗り分け）→ 発走前の流れと一緒にコミット
2. レビュー側の照合（§0）
3. 並行で残っているもの: **V-10**（オーナーの別ターミナル・`npm run verify:payout -- --races 500 --seeds 42`）／ 照会 `QUESTIONS_ENGINE_LANE_SPEED_20260915.md` ／ 相談 `CONSULT_GAME_DESIGN_TWO_MODES_20260915.md` ／ **push**（オーナーの指示待ち）
