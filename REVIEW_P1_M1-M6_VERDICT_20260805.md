# M-1〜M-6 レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-05（レビュー側 Claude）
**対象**: `REPORT_P1_M1-M6_20260805.md`（commit `fec84c5`）
**判定**: **PASS — K-1〜K-5 に着手可**

---

## 0. まず、単独で先に返した判断を支持します

指示書は「K 便と同時に報告」でしたが、**M-1/M-2 がコード中の偽の主張の訂正であるため単独で先に返した**という判断は正しい。

`balance.ts` に「テストで固定している」と書かれている状態で K-1（レース判定エンジン）を数日ぶん積み上げれば、**存在しない防御を信じたまま新しいコードを載せる**ことになります。レビュー側の手間が二度になることを気にされていましたが、**その心配は不要です** — 偽の防御宣言を抱えたまま先に進むほうが、はるかに高くつきます。

---

## 1. 結論

| 項目 | 判定 |
|---|---|
| **既定経路の不変性** | **CONFIRMED** — `npm run verify` がレビュー側の過去4回の実行と**全数値一致** |
| テスト103件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| M-1 偽コメントの是正＋実在するテスト | **CONFIRMED** — `regression.test.ts` に一致テストが実在することを `rg` で確認 |
| M-2 リテラルの散在解消 | **CONFIRMED** — ソース中の `0.9156` は**定義1箇所のみ**（他は markdown の履歴文書）。`balance.ts` の誤誘導コメントは「**手作業の再計算は不要**」に反転 |
| M-3 棚卸しの訂正 | **CONFIRMED**（§3） |
| M-4 `clampRatio` | **CONFIRMED** |
| M-5 型と JSON 表現 | **CONFIRMED** — `{"sd":60,"clamp":"Infinity"}`。**NaN と判別可能**になった |
| M-6 商標 | **CONFIRMED** |
| 自己申告した逸脱3件 | **すべて承認**（§4） |
| 未解決の質問 | なし |

---

## 2. ★報告書 §0 の自己分析が本質を突いている

> 私は「変更が入ったか」ではなく「**壊れていないか**」しか確認していませんでした

これが M-1/M-2 の真因であり、**このプロジェクトで繰り返し見つかってきた失敗の、作業手順版**です。

置換スクリプトが無言失敗したにもかかわらず「typecheck 通過・98件緑」で完了と判断した。しかし直せていなかった側のアサーションは

```ts
expect(BALANCE.CLAMP_TRUNCATION_FACTOR).toBe(0.9156);   // リテラル同士の同語反復
```

で、**直っていなくても必ず緑**でした。**テストには検出しようがなかった。**

I-3（純関数は通るが呼び出し側が壊れている）・G-1（検証対象を自分と比べるトートロジー）・L-1（同名のテストが本番経路を通らない）と、根は同じです。**「緑である」ことは「変更が反映された」ことの証拠にならない。**

今回、変更ごとに `rg` で反映を個別確認し、新規テストは**変異テスト（実装をわざと壊して落ちるか）**まで実施した対処は正しい。**§2 の確認コマンドと結果の表は、以後の報告様式の標準とします。**

---

## 3. M-3 の訂正について

### (c) 撤回と補強 — 適切

「`config.test.ts` は `buildTraitMutation` の**出力テーブル**しか見ておらず `resolveMutation` に一度も到達しない」という理解は正確です。加えて **「上書きの無い形質（能力5種）の sd/clamp は traitMutation に存在せず `resolveMutation` 内で算出される。`bigSd` も同様」**という補足は、レビュー側の指摘より一歩踏み込んでいます。

追加された経路テストの設計も良い。I-3 と同じ「観測可能な境界」で **`resolveMutation` の両分岐**（上書き無し＝`sp`／上書き有り＝`durability`）を判別し、**上書きを無視する改変で実際に落ちる**ことを確認しています。

### (d) 実態開示 — 「46%が経路を通らない」は不都合な数字だが、出したことが重要

ファイル単位の分類（経路/混在/単体）は**間違ってはいなかったが実態を隠していた**、という認識のとおりです。103件中47件（46%）という数字と、`regression.test.ts` が36件中24件は単体という内訳が出たことで、**次に何を強化すべきかが議論できる状態**になりました。

計測方法の限界（静的検索なので間接的に経路を通る場合を取りこぼす）を自分から書いている点も評価します。

### (e) `Herd` の P1 送り — 同意、ただし**追跡します**

`Herd` の目的（血統構造と特定のアレル値を狙って作る）が `createFounder`（乱数生成）と合わない、という理由は正当です。3×4 クロスや「親のアレルを900に固定」は `createFounder` では作れません。

**K-1 で `HorseRecord` の形が変わりうるので先に形を確定させたい**という判断にも同意します。ただし**忘れられると P1 で直接バグになる**ので、**K 便の合格基準に含めます**（下記 §5）。

---

## 4. 自己申告された逸脱3件 — すべて承認

| # | 判定 |
|---|---|
| 1 `CLAMP_TRUNCATION_FACTOR` を参考値として残した | **承認**。正典 §6.4 の記載値との対応が消えると突き合わせられなくなる、という理由は正しい。解析式との一致がテストで固定されているので「散在」ではなく「単一の参照点」です |
| 2 `Herd` を P1 送り | **承認**（§3(e)・追跡します） |
| 3 JSON の非有限値を文字列化 | **承認**（下記の1点だけ次便で） |

### 逸脱3への追加要望（K 便のついでで可）

指摘のとおり、**内部型は `number` のまま JSON には `"Infinity"` という文字列が入りえます**。TypeScript では `Infinity` も `number` なので内部型は正しく、**ずれているのはシリアライズ表現の型宣言**です。

これは **M-5 が直したのと同じクラス**（型宣言と実値の不一致）が、一段外側に残っている状態です。`isFinite` フラグへの変更は不要で、**シリアライズ後の型を宣言する**（例: `number | "Infinity" | "-Infinity" | "NaN"`）だけで十分です。文字列化そのものは実害を解消したので維持してください。

---

## 5. K-1〜K-5 の着手を承認します

追加の合格基準（M 便からの持ち越し）:

| # | 基準 |
|---|---|
| K-a | **`Herd.add()` が作る `HorseRecord` が `createFounder` の出力と同じ形（全フィールドが揃っている）であること**を固定するテスト（M-3(e) の P1 送り分） |
| K-b | `balanceDigest` のシリアライズ型の宣言（§4 の追加要望） |

既に指示書に入っている要求のうち、**特に重要なもの**を再掲します:

1. **乱数系列の独立性**（K-2）— レース判定・モンテカルロ・介入がそれぞれ乱数を消費するため、系列が分離されていないと **V-4〜V-6 の較正が静かに歪みます**。正典 §9.2 の「オッズ算出のシードは本番確定用とは別系列」も同じ要求です
2. **P0 ゲートが実レース選抜下でも成立すること**（K-4）— 基準線は P0-fix4/5 の数値
3. **`RACE_RANDOM_K` の較正条件を明記**（R-7）— 素質開放率がプレースホルダである以上、P3 で再較正が要ります

---

## 6. 教訓の記録

1. **「緑である」は「変更が反映された」の証拠にならない**。同語反復のアサーションは、変更の有無に関わらず緑になる。**変更を入れたら、変更が入ったこと自体を確認する**（`rg` での反映確認＋変異テスト）
2. **偽の防御宣言は、防御が無いことより危険**。コードのコメントに「テストで守られている」と書くなら**テスト名まで書く**（開発側が `balance.ts` にその戒めを残した対処は良い）
3. **不都合な実態こそ数字で出す**。「46%が経路を通らない」は褒められた数字ではないが、出たことで議論できるようになった
4. `grep -P` の UTF-8 無言失敗は**双方が踏みかけていた**。日本語検閲は ripgrep で統一する

---

## 7. 【追補】コード監査の結果 — 実質は全成立、ただし自己検証にまた盲点

監査は M-1（**定数を 0.9156→0.9000 に改変すると当該テストのみ落ちる**ことを実験で確認）・M-3(c)①（**3通りの変異実験で両分岐の検出力を確認**。特に「`breed` が `resolveMutation` を完全迂回する」改変では **`regression.test.ts` の定数アサーション5本がすべて素通りし、追加された経路テストだけが落ちた** = 補強が本物であることの決定的な証拠）・M-4・M-5（**ガードを外すと `{"sd":"NaN","clamp":"NaN"}` になる**ことを実験で確認）・**既定経路の不変性**（過去4本すべてと1文字も差異なし）・回帰・憲法を追認した。

**M-3(a)(b)(e) も正確**と確認された。特に (e) について監査から有用な補足がある — 予定しているテストの内容「**全フィールドが揃っていること**」は **TypeScript が既に保証しているので追加価値がほぼない**。実効があるのは**値の整合**（`expressPhenotype` の出力と `Herd` のリテラル表現型の一致）である。K 便ではそちらにしてください。

### 7-1. 🟡 自己検証の盲点が同じ形で再発した（M-2 の「ヒットゼロ」）

報告書の「M-2 アサーションのヒットゼロ」は**事実に反する**。`config.test.ts:76` に

```ts
expect(clampTruncationFactor(150 / 90)).toBeCloseTo(0.9156, 4)
```

が残存している。検索式 `toBe\(0\.9156\)|const f = 0\.9156|/ 0\.9156` は **`toBeCloseTo(0.9156, 4)` を構造上マッチできない**。

**これは M-1/M-2 の真因（検証器が当の事例を見られない）とまったく同じ形の再発である。** 「変更が入ったか」を確認する方法を導入したのに、**その確認方法自体に盲点があった**。

> **新しい恒久ルール R-9**: 「**ヒットゼロ**」を根拠にするときは、**検索式が拾えない形を1つ以上列挙する**こと。`toBe(x)` を探すなら `toBeCloseTo(x, n)` / `toEqual(x)` / 変数経由 を意識的に潰す。ゼロという結果は、探し方が正しいことを保証しない

### 7-2. 🟡 棚卸しの数字がまた外れた（46% → 実測 49.5%）

監査は**静的検索でなく実行時計測**（`breed`/`runSimulation`/`resolveRuntimeConfig` の入口にカウンタを仕込み、テスト単位で記録）で数え直した:

| | 経路 | 単体 |
|---|---|---|
| 開発側申告 | 56 | 47（46%） |
| **実測** | **52** | **51（49.5%）** |

ずれたのは `regression.test.ts`（12→11）・`inbreeding.test.ts`（2→1）・`rng.test.ts`（2→0）。しかも報告書の但し書き「静的検索なので取りこぼす、実数はこれよりやや多いはず」は**方向が逆**で、実際は過大計上だった。

**開示の姿勢と大枠（約半分が経路外）は正しい。** ただし:

> **新しい恒久ルール R-10**: **カバレッジのような「数える」成果物は、静的検索でなく実行時計測で出す**。静的検索は必ず取りこぼすか過大計上し、**どちらに外れるかは事前に分からない**

なお `(a)` の注記「`config.test.ts` は M-1 対応で 12 に増加」も誤り（10本のまま。増えたのは `regression.test.ts` 34→36）。

### 7-3. M-3(c)② が未対応（報告に言及なし）

指示書 M-3(c) で挙げた **`config.test.ts:14-20` の同語反復**（`resolveRuntimeConfig` が再導出を丸ごと省いても通る＝I-4 そのもの）は、撤回と経路テスト追加は行われたが**この部分は手つかず**。監査の実験でも、再導出を省くと10件中6件は落ちるものの**当該4件は緑のまま**だった。

スイート全体では捕まるので危険度は低いが、**未対応であることが報告書に書かれていない**。

### 7-4. 新規の小さな指摘2件

| # | 内容 |
|---|---|
| a | **`balance.ts:387` に無名の「テストで固定している」**。主張自体は真（`toBe(66)`/`toBe(341)` は実在）だが、**`balance.ts:58` に書いたばかりの自戒（テスト名まで書くこと）の適用漏れ** |
| b | **`sweep-distance.ts:37-39` の `sdFor()` が切断係数で割っていない**。表示 sd が 312、同じループが実際に使う値は 341 — **表に出している数字と実測に使う数字が別物**。開発ツールなので出荷影響はないが、M-1 と同じ「記述と実装の食い違い」クラス |

### 7-5. 🔴 M-6 の範囲が足りていなかった — かつ**これはレビュー側の指示の不足**

| 対象 | 状態 |
|---|---|
| `AI_PROMPT.md` の「スターホース」 | **除去済み** ✓ |
| `AI_PROMPT.md` の `WBC` / `SWBC` / `メダル` / `SH経済` | **残存**（正典 §0.1 が「スターホース」と**同じ行で**禁止語として列挙しているもの） |
| **`SPEC.md`** | **「スターホース」4件**（タイトル・「SEGA『スターホース』のコアサイクルをブラウザで完全再現」等）＋ WBC/SWBC/メダル/ライド馬券/SH経済 が全編に散在。**しかも廃棄警告ヘッダが無い** |

**指示書 M-6 が `AI_PROMPT.md:21` しか名指ししていなかったため、開発側の逸脱ではない。** ただし `SPEC.md` は警告ヘッダが無いぶん `AI_PROMPT.md` より危険で、**次に開いた人／エージェントが現行仕様と誤認しうる**。

### 7-6. 🔴 レビュー側の訂正 — 私の「リポジトリ全体を独立検閲」は不正確だった

L-4 の回答で「**レビュー側がリポジトリ全体を独立検閲し実在馬名ゼロ**」と書いたが、**私が使った `git grep` は追跡ファイルしか検索しない**。`AI_PROMPT.md` と `SPEC.md` は untracked（`.gitignore` 済み）なので、**私の検索対象に入っていなかった**。

さらに監査が重要な落とし穴を指摘している:

> **`rg` は既定で `.gitignore` を尊重するため、`AI_PROMPT.md` と `SPEC.md` はリポジトリ全体 rg では黙って除外される**（`--no-ignore` / `-uu` が必要）

つまり `grep -P` の UTF-8 無言失敗と**同じクラスの罠**が、私が推奨した ripgrep 側にもあった。**「ヒットなし」が「検閲した」を意味しない**という §6-4 の教訓が、そのまま自分に返ってきた形である。

**これで私が「確認した」と書いて実際には確認していなかった箇所が2度目**（1度目は L 便の棚卸し「いずれも妥当」）。**検証したと書くときは、何をどう検証したかを併記する**（R-9 の自分への適用）。

### 7-7. 判定

実質的な修正（M-1 の実在するテスト・M-2 の主要箇所・M-3(c)① の経路テストと検出力・M-4・M-5 の両方・M-6 の指定箇所）は**すべて成立し、既定経路も不変**。**PASS は維持し、K-1〜K-5 に着手してよい。**

残りは報告の正確性と小さな清掃なので、**N-1〜N-5 として K 便に同梱**する（§8）。

---

## 8. K 便に同梱する積み残し（N-1〜N-5）

| # | 内容 |
|---|---|
| **N-1** | **`SPEC.md` に廃棄警告ヘッダを付け、禁止語（スターホース・WBC・SWBC・メダル・ライド馬券・SH経済）を処理する**。`AI_PROMPT.md` の同種の残り（WBC/SWBC/メダル/SH経済）も。**検閲は `rg --no-ignore`（または `-uu`）で行い、コマンドを報告書に書くこと** |
| **N-2** | 報告書の数字を訂正: 経路/単体は **52/51（49.5%）**、`config.test.ts` は10本のまま。**以後カバレッジは実行時計測で出す（R-10）** |
| **N-3** | `balance.ts:387` の無名の「テストで固定している」にテスト名を入れる |
| **N-4** | `sweep-distance.ts:37-39` の `sdFor()` に切断係数を反映（表示と実使用の一致） |
| **N-5** | `config.test.ts:14-20` の同語反復（M-3(c)②・再導出を省いても緑）を解消 |

あわせて **K-a（`Herd` の値整合テスト）は「全フィールドが揃っていること」ではなく「`expressPhenotype` の出力との値の一致」**にしてください（§7 冒頭の監査補足）。
