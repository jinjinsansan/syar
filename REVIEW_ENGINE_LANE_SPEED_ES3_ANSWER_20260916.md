# 回答 — ES 便の報告（ES-0〜ES-3）と、合格線に届かなかった後の手（`REPORT_ENGINE_LANE_SPEED_20260915.md` への回答）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側・オーナー
**対象**: 報告 `REPORT_ENGINE_LANE_SPEED_20260915.md` ／ コミット `1f58cde`（ES-1）・`c240d95`（ES-2）・`b956116`（ES-3）／ ブランチ `p4/race-30sec-cuts`・HEAD `b956116`（origin より 10 先行・push は未実施）
**前の文書**: 回答 `REVIEW_QUESTIONS_ENGINE_LANE_SPEED_ANSWER_20260915.md`（ES 便の指示）

> 凡例（R-13）: **✔** ＝ レビュー側がコードを読んで確かめた、または手元で実行した ／ **△** ＝ 未確認（報告の記載に基づく） ／ **計算** ＝ 実測値と定数から出した値
> 本書は正典を編集しません。
> ⚠️ **§4 の ES-5 は、オーナーが F-1（§9）を承認するまで着手しないでください。** 承認が要らない ES-4（§6）と互換の確認（§5-2）は先に進めてかまいません。

---

## 0. 判定と結論

| 範囲 | 判定 |
|---|---|
| ES-0（本番の読み取り確認） | **受理**。本番のワーカーは `70d9982`（2026-08-12）で、D-071 を含まない |
| ES-1〜ES-3 | **受理**（指紋 182 組が 1 ビット一致・変異 ①②③ が落ちる・全体の検査 1772/1773）。△ **レビュー側は検査も速さも再実行していません**。変更ファイルが `lane.ts`・`race.ts`・検査だけであることは ✔ `git show --stat` で確認 |
| 合格線 1.3 倍に届かず止めたこと | **正しい**（回答 §Q-2 の指示どおり） |

**結論**
1. ★**開発側の候補 (a)〜(e) に無い手 (f) を、レビュー側の実験で見つけました**（§2）。**コーナーを 10m 刻みで積む和は、刻みを変えずに「1 レース 3 つの和 ＋ 1 頭ごとの短い式」に分解できます。** HEAD の `laneExtraM` と 98,800 頭で突き合わせて **最大の差 3.4×10⁻¹³ m**（丸めだけ）
2. **E-1 は (f) を第 1 に推奨します。** ただし「1 ビットも変えない」（回答 §Q-2 条件 3）が「丸めの差だけ」に変わるので、**オーナーの承認（F-1）が要ります**
3. **E-2 に同意**。今のコードでワーカーを配備しない。加えて、本番に入っていない DB の権限の修正（`0021`・`0022`）は**古いワーカーとの互換を確かめたうえで**先に当てるかをオーナーが判断（§5）
4. **E-3: ES-4 を今入れてください**（§6）
5. **E-4: そのとおり**。(f) の後に測って下限を決める

---

## 1. 照合

| 事項 | 照合 |
|---|---|
| 変更の範囲 | ✔ `1f58cde` は検査 4 ファイルだけ ／ `c240d95` は `lane.ts`・`race.ts` ／ `b956116` は `lane.ts` と報告 |
| 本番の走り方の設定 | ✔ `LANE_MODEL` ＝ `homeSpreadM 7.0`・`wobbleM 0.9`（`lane.ts:306`）。`RAIL_W 2.2`・`SETTLE_M 250`・`RUN_UP_M = SETTLE_M`（`lane.ts:20・22・113`）・`REVEAL_START_RUN 0.03`・`LANE_REVEAL_FULL_RUN 0.18` |
| 本番の実測の所要 | 報告 §1 #6: 1 レースの周 **98.2〜110.8 秒**（D-071 の前のエンジン・16〜18 頭の本番の出走表）。正典 D-036 の 138 秒より短い |
| 本番機への換算 | 計算: ES-3 の比 3.1〜7.2 倍 × 実測 98〜111 秒 ＝ **約 5〜13 分**。報告 §2-2 の 7〜17 分（138 秒で換算）より短いが、**やはり 10 分に入らない** |

---

## 2. ★レビュー側の実験 — 同じ和の分解（案 (f)）

### 2-1. 前提（コードで確かめたこと）

| # | 前提 | 出どころ |
|---|---|---|
| P-1 | **最初のコーナーの前に、必ず `RUN_UP_M`（＝`SETTLE_M` ＝ 250m）の直線がある** | ✔ `lane.ts:125-140` `withRunUp` |
| P-2 | → **コーナーの刻みではどの馬も `ranM ≥ 250`** なので `settle = 1`・`settled = 1`・通り道の土台 ＝ `home` | ✔ `laneAt` の `settle = clamp(ranM / SETTLE_M)`。実験でも 77 プランの全刻みで `ranM < SETTLE_M` は **0 回** |
| P-3 | 本番の `LANE_MODEL`（legacy でない）は `base + wob` を返し、`drift`・`wave` を使わない | ✔ `laneAt` の `laneModel.legacy !== true` の枝 |
| P-4 | `home = RAIL_W + homeSpreadM × u² × S` は馬ごとに一定、`wob = sin(φ + 3π·run) × wobbleM × reveal × S` の `run`・`reveal` はレースごとに一定（馬によらない） | ✔ 同上。`u`・`φ` はシードと枠だけから（`stream`） |
| P-5 | 端の `clamp(0.8, widthM − 0.8)` が掛からなければ、和は線形 | ✔ 同上 |

### 2-2. 分解の式

コーナーの刻み `k` について `c_k = len_k ÷ R_k`・`θ_k = 3π·run_k`・`r_k = reveal_k` とすると、端の clamp が掛からないとき:

```
laneExtraM = Σ_k (home + sin(φ + θ_k)·wobbleM·r_k·S − centre) · c_k
           = (home − centre) · ΣC  +  wobbleM · S · ( sin φ · A  +  cos φ · B )

  1 レース 1 回:  ΣC = Σ c_k   A = Σ cos θ_k · r_k · c_k   B = Σ sin θ_k · r_k · c_k
  1 頭 1 回:      u = stream(seed, gate, 0x51ed2701)   φ = stream(seed, gate, 0x2f5c1d3b) · 2π
                  home = RAIL_W + homeSpreadM · (u · u) · S
```

**刻みは 10m のまま・同じ和を並べ替えただけ**なので、近似ではありません。違いは浮動小数の足す順番による丸めだけです。

### 2-3. 結果（HEAD `b956116`・レビュー側のスクラッチで実行・リポジトリには書いていない）✔

| 項目 | 値 |
|---|---|
| 範囲 | `DEFAULT_OVAL` と 10 場の凍結した形（`frozenCourseOf` → `conditionsFromFrozen`）× `DISTANCE_MENU` の 7 距離 ＝ **77 プラン** × **8 頭・18 頭** × 全枠 × **シード 50** |
| 比べた馬 | **98,800** |
| 最大の差 | **3.411×10⁻¹³ m**（最大の相対差 1.7×10⁻¹³） |
| 端の clamp が掛かりうるプラン（保守的な判定: `reveal` を 1 と見る） | **1 / 77**: **大河原 1200m**（S = 1.6098・下端の見積り 0.751 < 0.8・幅 23m）。このプランは比べていない |
| 所要（計測の手間込み） | `laneExtraM` 543ms → 分解の式 **18.5ms**（約 30 倍） |

⚠️ △ **レース全体（`resolveRace`）がどこまで速くなるかは測っていません。** 距離ロス以外の計算の割合によります。

---

## 3. E-1 への回答

| 案 | 結果への影響 | 判定 |
|---|---|---|
| **(f) 同じ和の分解**（本書 §2） | **丸めの差だけ**（実測 ≤ 3.4×10⁻¹³ m）。刻み・試行回数・較正に触れない | ★**第 1 に推奨**（F-1 の承認後） |
| (a) 刻みを粗くする | 値が変わる → V-18・V-4〜V-6 などの取り直し | 推奨しない（(f) で要らない） |
| (b) 積分を解析的に解く | 10m の和とは値が変わる（連続の積分になる） | 推奨しない（(f) で要らない） |
| (c) オッズの試行回数 | 設計式 `M ≥ λ* × ODDS_CAP ÷ (1 − margin)`（D-035）から、実質は**三連単の配当上限を下げる**ことになる＝製品の中身が変わる | 最後の手段・オーナー判断 |
| (d) ワーカーでの並列化 | 試行のシードを先に順番どおり引いて配れば、**結果は変えずにできる** | (f) で足りなかったときの次の手。本番機のコア数の確認が先（△） |
| (e) 合格線の見直し | 線はサイクルの時間の予算から来ている。緩めても 5〜13 分は入らない | 採らない |

- **本番は D-071 のエンジンを一度も動かしていない**（ES-0）ので、(f) の丸めの差は**本番のどのレースの再現にも影響しません**。影響しうるのは staging の D-071 以後のレースで、再計算は**着順**で突き合わせるので、同点でない限り一致します（ES-5 の検査 5 で確かめる）

---

## 4. ES-5（(f) の実装・★F-1 の承認後に着手）

### 4-1. 作るもの

- **1 レース 1 回**（プラン: 距離・走路の形・`revealFullRun`・`laneModel` の組）: `ΣC`・`A`・`B`・`S`・`maxReveal`・**前提 P-1 が成り立つか**（最初のコーナーの始まりが `SETTLE_M` 以上か）
- **1 頭 1 回**: `u`・`φ`・`home` と、**その馬で端の clamp が掛かりえないか**:
  `home − wobbleM·S·maxReveal > 0.8` かつ `home + wobbleM·S·maxReveal < widthM − 0.8`
- **どれか 1 つでも成り立たなければ、いまのループ（ES-3 の刻みの計算）で計算する**（R-27: 分からないときは安全な側へ）
  - 前提 P-1 が崩れるプラン ／ legacy の `laneModel` ／ 端の clamp が掛かりうる馬（大河原 1200m の `u` の小さい馬など）
- **`laneAt`（画面が呼ぶ）の署名と値は変えない。** `laneExtraM` の署名も変えない
- 式は 1 か所。ループの経路は**比較の基準と、縮退先**として残す

### 4-2. 検査

| # | 内容 |
|---|---|
| 1 | ★**指紋の検査を「許容差つき」に書き直す**。期待値ファイル（`884019c` で取ったもの）は**作り直さない**（新しいコードで期待値を作ると、自分を自分と比べる・R-16）。判定は、182 組すべてで **馬・着順・着差の言葉が完全一致**、**`laneExtraM` の差 ≤ 1×10⁻⁹ m**、**スコアの相対差 ≤ 1×10⁻¹²**、**走破タイムの差 ≤ 1×10⁻⁹ 秒**。書き直す理由を検査の註記に書く |
| 2 | **2 つの経路の一致**: 分解の式を使える馬で、分解の式とループの値の差 ≤ 1×10⁻⁹ m（10 場 × 7 距離 × 8・18 頭 × シード数十） |
| 3 | **縮退の検査**: 大河原 1200m で、端の条件を満たさない馬が**ループに落ちている**こと（経路を記録して見る）。前提 P-1 が崩れる形（最初の直線が 250m 未満になる人工の形）でもループに落ちること |
| 4 | **変異**: ① 端の条件の判定を外す（常に分解の式）→ **実際に端で止められる馬を含む組で落ちる**（そういう馬が本番の組に無ければ、人工の形で作り、その旨を書く）② `A` と `B` を入れ替える → 落ちる ③ `reveal` を掛け忘れる → 落ちる |
| 5 | **staging での再計算**: `tools/verify-entrant-freeze.mjs --env staging --recompute-settled` で、D-071 以後に確定したレースの**着順が全件一致**（既知の食い違い cycle 686・868 は走路の凍結の無い古い行で、本件と別。報告に分けて書く） |
| 6 | **速さ**: 報告 §2-1 と同じ表（16・18 頭 × 1600・3000m × 既定・最長の周・`97364c5^` と同じ N）。**比が 1.3 倍以下なら合格**。届かなければ止めて報告（次は (d)） |
| 7 | `npx vitest run --maxWorkers=2 --minWorkers=1` の行と終了コード。既存の赤（`edit-grammar-audit` ⑨）が 1 件から増えていないこと |

### 4-3. 触ってよいもの

| 可 | 不可 |
|---|---|
| `packages/race-engine/src/lane.ts`・（要るときだけ）`packages/race-engine/src/race.ts`・`packages/race-engine/test/`（指紋の検査の書き直しを含む） | 指紋の期待値ファイル（`lane-fingerprint.expected.json`）・較正定数の値・`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`・`stepM`・`LANE_MODEL`・`LANE_REVEAL_FULL_RUN`・乱数の消費・`packages/render`・`apps/web`・正典・本番 DB・ワーカーの配備 |

---

## 5. E-2 — 本番への配備

### 5-1. 順番（変更）

1. ES-4（§6）→ ES-5（§4・F-1 の後）
2. 10 場の配線の最終裁定（`REVIEW_VENUE_WIRING_VERDICT_20260915.md` の VC-1〜VC-4。計測は ES-5 の後）
3. 本番へ `0023`・**新しいワーカー**の配備（オーナーの指示で）

★**それまで、今のコードでワーカーを配備しないでください**（10 分サイクルが壊れる）。

### 5-2. ★本番は 8 月 12 日の版 — 入っていないもの

本番のワーカー `70d9982` には、8 月 12 日より後の決定と修正が**すべて入っていません**（R-28 の実例）。とくに:

- **監査の修正**（2026-09-14）: 払戻の整数計算・オッズの 0.1 切り捨て（D-094）・1.0 倍未満を売らない（D-096）
- **DB の権限の修正** `0021`・`0022`: 利用者のロールから関数の実行権を剥がす（D-095。監査 H-4「anon が 1 回呼ぶだけで他人の EP を消せる」）。**本番の利用者 0 なので実害はない**（正典 D-095 の記載・2026-09-13 実測）

`0021`・`0022` は**DB 側の権限だけ**でエンジンと無関係なので、**新しいワーカーを待たずに先に当てられる可能性があります**。ただし古いワーカーがそれらの関数を呼んでいる場合に止まらないかは未確認（△）。

**開発側への依頼（読み取りのみ・承認不要）**: `70d9982` のワーカーが呼ぶ DB 関数と使う権限を列挙し、`0021`・`0022` を当てたときに**古いワーカーが止まらないか**を、コードと staging（古い版のワーカーを使える場合）で確かめて報告する。結果を見て、オーナーが先に当てるかを決める（F-2）。

---

## 6. E-3 — ES-4 を今入れる

- `resolveRace` 1 回あたりの**区間を作る回数**（`ovalSegments` またはプランを作る関数の呼び出し）に上限を置く。ES-2 で**頭数によらず 1 レース 1 回**になっているはずなので、その値で固定する
- **変異**: ES-2 を戻す（馬ごとに区間を作る）と落ちる
- 数える仕組みが結果を変えないこと（指紋で見る）
- ES-5 の後も同じ上限が成り立つこと（ES-5 の検査に含める）

---

## 7. E-4 — サイクルの短縮

そのとおりです。ゲーム本体の回答 `REVIEW_CONSULT_GAME_DESIGN_TWO_MODES_ANSWER_20260915.md` §4-3 (a) の下限は、**ES-5（と要れば (d)）の後の本番の条件の実測**で決めます。

---

## 8. 正典について（参考・オーナーの承認後にレビュー側が起票）

| 候補 | 中身 |
|---|---|
| 距離ロスの計算 | 距離ロスは同じ 10m の和を分解して求めてよい。**丸めの差（≤ 1×10⁻⁹ m）を許し、着順の完全一致で守る**。前提（最初のコーナーの前に `SETTLE_M` の直線・端の clamp が掛からない）が崩れる場合は刻みの計算に戻る |
| D-036 の註記 | 所要は D-071 の前のエンジンで測った値。本番の実測（2026-09-15・`70d9982`）は 1 レース 98〜111 秒。**所要には測ったコミットを併記する** |

---

## 9. オーナーに決めていただくこと

| # | 判断 | レビュー側の推奨 |
|---|---|---|
| **F-1** | ★**ES-5（案 (f)）で、結果を「1 ビットも変えない」から「丸めの差だけ（≤ 1×10⁻⁹ m・着順は完全一致）」に緩めてよいか** | **許す**（本番は D-071 を動かしておらず、再現に影響するレースが本番に無い） |
| F-2 | `0021`・`0022` を新しいワーカーより先に本番へ当てるか | §5-2 の互換の確認の後に判断 |
| F-3 | push | オーナーの指示で |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
