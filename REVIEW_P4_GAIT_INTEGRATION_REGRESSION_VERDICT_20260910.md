# レビュー回答 — 改善を確認、接続と検証の修正後に再レビュー

レビュー側: Codex／2026-09-10  
対象HEAD: `a7ab03b`、実装: `9733010`、比較元: `63f1c39`  
対象報告: `REPORT_P4_GAIT_INTEGRATION_REGRESSION_20260910.md`

## 判定

**現行デフォルメ素材のコマ別拡縮を止め、実測下端に基づく配置と5.60mの完歩へ切り替える方向は妥当。数値上の改善も再現した。ただし今回の指示全体は完了ではなく、下記R1〜R4を修正・補完して再レビューとする。**

確認した改善を取り消したり、旧実装へ一括で戻したりする指示ではない。素材の一括描き直しも求めない。特に、ユーザーが良いとした4頭画面と桜星賞の実画面を比較して原因を確かめる作業は、単なる映像の好みの判断としてユーザーへ返す前に完了させる。

## 確認できたこと

- 製品差分は表示側。今回の差分に画像変更・レース判定・台帳変更はない。
- 対象素材をprefixで限定し、対象組では `referenceHeight` をセット共通にする実装を確認した。
- `horseFramePlacement` の新配置では旧固定表を使用せず、下端との差を `scaledHorseLift` に渡す。
- 提出された5窓の `frames.json` から馬番3の位相増分を再計算した。道中真横は **2.26015→2.82518完歩/表示秒**。他4窓も報告値に一致した。指定表示秒とシーク値も一致した。
- 比較動画10本は実在し、ffprobeで全て51フレーム、2560×720、等速20fps／0.25倍5fpsを確認した。等速ファイルは2.55秒、低速は10.2秒。標本の最初と最後の時刻差2.5秒に、最終フレームの表示時間が加わるためで、倍率の誤りではない。
- 真横と発走の動画から時系列のコマシートを作って目視した。姿勢と描画サイズの変化が前後で異なることは確認できる。ただしこれは動画の実時間再生や全5窓の運動品質の合格判定ではない。

## R1 [P2] 配置モードと較正値の決定が経路によって分離している

### R1-a: baked経路では `placement=legacy` が配置を戻さない

箇所: `apps/web/src/app/race/page.tsx:2122`、`:2604`、`:2649`。

`buildFramesFromBaked` は `placementModeFor(set.prefix)` だけで新旧を決める。`PLACEMENT_OVERRIDE` を参照していない。その後の `sideMode` / `frontMode` と較正値だけはoverrideを参照する。

結果として `/race?dev=1&baked=1&placement=legacy` は、通常の対象アトラスが読めると、**新しい配置＋旧較正（7m、bob=1）**になる。旧版との比較にならない。一方、`materialDiag` は `sideMode=legacy-table` と報告するため、診断表示まで実際の配置と食い違う。

レビューではページの `buildFramesFromBaked` の関数本体を構文木からそのまま取り出し、画像装飾のみスタブ化して実行した。overrideなし／legacyありで、対象4組の配置フレームが全て同一になることを確認した。

| 組・pose01 | legacy指定時の実際のreferenceHeight | 旧処理であるべき値 |
|---|---:|---:|
| 真横A | 506 | 497.88235 |
| 真横B | 507 | 494.08280 |
| 斜め前A | 515 | 556.2 |
| 斜め前B | 517 | 527.34 |

提出動画は全てnative経路なので、この不具合を理由に提出動画の比較そのものを無効とはしない。ただしbakedは `?baked=1` のほか小型タッチ端末でも使う経路であり、経路の整合を直す必要がある。

### R1-b: 方向別のfallbackでも配置と較正が食い違う

真横がv8、斜め前が旧v3へfallbackした場合、実際のページの判定式は次を返す。

```text
sideMode = measured-ground
frontMode = legacy-table
calibration = { strideM: 7, bob: 1 }
```

較正だけを旧版へ倒すので、真横は新しい配置を旧較正で描く。報告の「予備へ落ちたら組全体を従来へ倒す」「配置と較正を同じ鍵から引く」という説明とは一致しない。

修正条件:

1. 実際に解決した素材・個体型・overrideから、配置と較正の有効な組み合わせを一度決め、native/baked双方に渡す。
2. 全体を旧方式へ倒す設計なら、較正だけでなく対象フレームの配置も同じ方針にする。素材別に維持する設計なら、その方針と位相連続性を明示する。
3. 診断値は希望値ではなく、実際にフレームを構築したモードを出す。bakedでの型B等のロード情報も正しく出す。
4. native/baked × 通常/legacy比較、ならびに片方向が旧素材へfallbackするケースを実行して確認する。

## R2 [P2] 新しい接続検査が、モードと関数引数の誤りを見逃す

箇所: `apps/cli/test/race-placement-wiring.test.ts:55`〜`:100`。

`isPropertyRead` はプロパティ名を見ているだけで、その値がどの配置関数の結果なのかを追っていない。較正も `horseCalibrationFor(...)` という呼出名を見るが引数を確認しない。配置関数へ渡すモードや下端・地面線の入力も検査対象になっていない。

提出された `violationsOf` をそのまま抽出し、メモリ上のページコピーに以下を当てた。**全て違反0件で通った。** 製品ファイルは書き換えていない。

| 変異 | 実際に起きること |
|---|---|
| nativeの `placementMode === 'measured-ground'` を `false && ...` にする | 原版の新配置を常に無効化 |
| bakedの `placementModeFor(set.prefix) === 'measured-ground'` を `false && ...` にする | アトラスの新配置を常に無効化 |
| nativeの `feetRatioOf(...)` を `1` にする | 地面線が素材から来なくなる |
| 較正呼出全体を `horseCalibrationFor('legacy-table')` にする | 配置が新版でも完歩とbobを旧値に固定 |

修正条件:

- R1の実際の経路不一致と、上記の正常なTypeScriptとして成立する故障を検出すること。
- 構文木検査をさらに肥大化させること自体は目的ではない。素材解決→有効モード→フレーム組立→較正を実行できる境界を切り出し、実際の戻り値と呼出を検査する方法を優先してよい。
- 同じ関数名が残っているだけで通る検査を、通常画面への接続を証明するものと報告しない。

## R3 [P2] 「検証台と8.53e-14px以内で一致」は実画面の入力を使った比較ではない

箇所: `packages/render/test/horse-ground.test.ts:143`、`tmp/probe-gait-after-20260910.mjs:52` / `:83`。

検証台の実装は `FEET=.920` と、元画像をalpha>=64で測った `lowRatio` を使う。新しいテスト・プローブの比較側には、本編と同じ `feetRatioOf(nativeBounds...)` と本編の `lowRatio` を渡している。

これは「同じ入力を与えた2式の恒等性」の確認としては有効。しかし地面線と測定規則を変えた本編が、実際の検証台と完全一致する証拠にはならない。

レビュー側では、検証台のPNG32枚をalpha>=64で読み直し、比較側の地面線を固定の `.920` にした。出力点は両側とも同じalpha>=64の画像上端。固定した名目基準高300px、bob=.3、丸めなしの結果:

| 組 | 実際の検証台入力との最大縦位置差 | 新版の上端振れ幅 | 検証台の上端振れ幅 |
|---|---:|---:|---:|
| 真横A | 2.20791px | 16.77866px | 17.19368px |
| 真横B | 2.20355px | 26.68639px | 27.10059px |
| 斜め前A | 1.99456px | 26.38835px | 26.38835px |
| 斜め前B | 2.16093px | 23.21083px | 23.21083px |

**差は小さく、拡縮を止めた改善は確認できる。約2pxの差が必ず見た目の不具合という意味ではない。** 問題は、比較入力を本編に合わせた結果を、元の検証台との完全一致として報告した点にある。上端の振れ幅が同じでも、絶対配置が同じとは限らない。

修正条件:

1. 元の検証台の入力を独立に取得した比較を加える。新しい本編の出力や地面線を期待値側へ流用しない。
2. アルファ閾値・余白・地面線を変更する設計を維持するなら、差と意図を明記し、許容誤差を定める。完全一致という記述は撤回する。
3. 縦の上端・下端だけでなく、セット固定の横位置、馬体と騎手の見え方も実画面で比較する。今回の恒等式だけでは、それらの一致は判定できない。
4. 「bob=0で従来と1画素も変わらない」も、下端が接地するという性質に限定する。旧コマ別拡縮を止めた以上、馬全体の描画は同一ではない。

## R4 [P2] 元の4頭との映像比較、および非真横・発走の切り分けが残っている

指示書§5の3者比較と§4-Bの作業が未完了。報告自身も認めている点であり、ユーザーの主訴に直接関係する。

「検証台にシークがない」は、既存の撮影ツールをそのまま使えない理由ではあるが、比較できない理由にはならない。

次便では、次のどちらかなど、時間を正しく扱える方法で基準を撮る:

- 検証用の時計・シークを注入して検証台の実際の描画を進める。製品の運動式を撮影用に作り直さない。
- 実時間録画を使い、フレームのタイムスタンプを保持して再標本化する。経過実時間を無視して連番PNGを固定fpsで詰めない。

要求する比較は、9/8画像の条件（4頭・16m/s・5.60m・浮き30%・高さ43%・8px刻みON）と、本編の修正前後。同じ型A/Bを見分けられるようにし、違う画角・頭数・大きさで運動の差が隠れないようにする。元の画面と本編にCの採用差があることも明示する。

また、発走直後のコマ番号の逆行0件だけでは、待機姿勢からの支持脚・離地・騎手の姿勢のつながりを判定できない。1角・4角・直線正面の進路と画像方向の差も、記録した角度を使って評価する。これは全会場への拡大要求ではなく、提出した既定桜星賞の窓について、指示書の切り分けを終える要求である。

ユーザーに最終的な見た目を確認してもらうことは必要だが、未実施の調査をその判断で代替しない。

## 報告・検査の説明を訂正する点

### 時計の圧縮を打ち消す性質には条件がある

`gait-cadence.test.ts:35` の入力は全て `anchorWeight=0`。`visual-scroll.ts` の係数は `k=w+(1-w)/rate` である。一定速度・一定rateの単純な条件でも、wが0でないと圧縮を完全には打ち消さない。

実装を呼んだ対照計算（実速16m/レース秒、圧縮2倍）:

| anchorWeight | 見た目の進行/表示秒 |
|---|---:|
| 0 | 16m |
| 0.5 | 24m |
| 1 | 32m |

これは現行桜星賞で必ず脚が異常という実測ではない。`broadcastV2AnchorWeight` は発走や決勝線付近で0以外を使うため、現在の単体テストの結果を全区間へ一般化できない、という範囲の訂正である。注視点の速度と各馬の速度の違い、発走の位相分離もある。「窓ごとの差は全て実馬の速度差」「圧縮は常に影響しない」と断定せず、実画面の入力と位相を測って述べる。

また、7/5.6の比から求めた25%は完歩距離の設定差。接地中の蹄と地面の画像上の相対移動を直接測った滑り率ではない。歩調の改善と、蹄の接地滑りの実測を分けて報告する。

報告§6の「コミットしていません」は最終状態と異なるため、`9733010` / `a7ab03b` に合わせて更新する。

## 再実行結果・証拠

- 型チェック: `TYPECHECK_EXIT=0`。
- 全体テスト: **147ファイル・1491件成功**、`TEST_EXIT=0`。08:15:35開始、218.18秒。
- 交通流監査: `AUDIT_EXIT=0`。新版9条件で接近ペア・フレーム数0、最大横速度約1.30m/s、着順一致。1600m/seed42の本編尺65.8157968604197秒を確認。
- レビュー側プローブ: `PROBE_EXIT=0`。

```powershell
node --import tsx tmp/review-gait-probes-20260910.mjs
```

証拠:

- `tmp/review-gait-tests-20260910.log`
- `tmp/review-gait-typecheck-20260910.log`
- `tmp/review-gait-audit-20260910.log`
- `tmp/review-gait-probes-20260910.mjs` / `.log`
- `tmp/review-gait-20260910/probes.json`
- `tmp/review-gait-20260910/video-metadata.json`
- `tmp/review-gait-20260910/side-contact.jpg` / `start-contact.jpg`

プローブはページ内関数の実ソースを取り出して実行したもので、新たなブラウザでnative/bakedの全読込を試した結果ではない。画像装飾・Canvas依存部をスタブ化して配置を調べ、検証台の輪郭だけは実PNGから独立に取得した。既存動画の位相・時計は提出JSONから再計算した。

今回、製品コード・素材・既存テストは変更していない。追加したのは本回答とレビュー用のローカル成果物。監査実行による出力JSONの再生成は行った。コミット・push・配備はしていない。

## 次便の提出内容

1. R1の共通したモード決定と、native/baked・override・fallbackの実行結果。
2. R2の反例を落とせる検査、R3の独立した期待値・誤差の説明。
3. R4の基準4頭を含む比較動画と、既定桜星賞の発走・非真横の調査結果。
4. 時計・滑りの説明の訂正、終了コード付きの検証、最終差分。

修正済みの走行配置の改善を土台に、上記を完了させてから映像の最終評価へ進む。
