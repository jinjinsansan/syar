# REPORT_P2_INFRA — インフラ構築と合格基準の実測

**開発側 → レビュー側** / 2026-08-08
**コミット**: push 済み / 380 tests 全PASS・typecheck 0・変異試験 35-35

---

## 0. 到達状況 — 9件中8件が実測 PASS

| # | 基準 | 状態 |
|---|---|---|
| **A-1** | 10分サイクルが無人で24時間 | **稼働中**・24時間後に実測 |
| **A-2** | 強制終了→再起動で二重生成・二重払戻なし | **PASS**（本番構成・SIGKILL×3） |
| **A-3** | V-10 払戻率 ±1% | **収束確認**（+0.62pt） |
| **A-4** | seed_reveal が確定前に取れない | **PASS**（anon で実際に叩いた） |
| **A-5** | EP減算と発券が原子的 | **PASS**（壊して確認） |
| **A-6** | 自馬ベット制限 | **PASS** |
| **A-7** | staging→production で起動失敗 | **PASS** |
| **A-8** | P0/P1 の回帰 | **PASS** |
| **A-9** | テスト・変異試験 | **PASS**（380 / 35-35 / typecheck 0） |

---

## 1. インフラ構成（VPS を採用）

正典 §14.2 は「常時稼働ワーカー（**Render Background Worker 等**）」で実装先を固定しておらず、
要求は **常時稼働・任意の実行時間・状態が前進すること**の3点です。
名指しで禁止されているのは **Vercel Cron と pg_cron** だけなので、VPS を採りました。

| レイヤー | 採用 |
|---|---|
| DB / 認証 / RLS | Supabase（適用済み・0001〜0004） |
| ゲームループ | **XServer VPS**（Ubuntu 26.04 / 4vCPU / 4GB）+ systemd |
| フロント | 未着手（Vercel・§14.3 の「読み取り系だけ」を守る） |

★このサーバーは別プロジェクト（pax）で使っていたものを転用しました。
  pax のコンテナは `docker compose down` で停止・削除済み（再起動しても上がりません）。

---

## 2. ★本番構成で初めて出た問題が4件

**ローカルでは一度も出ませんでした。**P2 指示書 §3 が予告した
「ローカルでは動くが本番構成では動かない」そのものです。

| # | 内容 | 危険度 |
|---|---|---|
| ① | `node --experimental-strip-types` が TS 構文を扱えず `ERR_NO_TYPESCRIPT` | 起動しないのですぐ分かる |
| ② | **`StartLimit*` を `[Service]` に書き、systemd に無視されていた** | ★★ |
| ③ | 起点を未来に設定し、サイクル番号が負になった | 中 |
| ④ | **周ごとの記録が無く、A-1 の測定が成立しなかった** | ★★ |

### ②が危険な理由

`Restart=always` **だけ**が効いている状態は、**壊れたまま無限に再起動し続けます**。
プロセスは常に `active` に見えるので、**A-1 の「24時間回る」を見かけ上満たしてしまいます**。
`systemctl show` で systemd が実際に認識している値を確認して発見しました。

### ④が危険な理由

生成も確定も無い周を無出力にしていたため、2分間ログが出ませんでした。
これでは **プロセスが生きているだけの空回りと、実際に処理している状態を区別できません**。
指示書 §3 の「ワーカーが生きていても中身が空回り」そのもので、
**測定そのものが成立していませんでした**。毎周かならず出力する形に変更しています。

---

## 3. A-2 を本番構成で「壊して確かめた」

```
SIGKILL ×3（周の途中を狙う）→ 自動復帰して稼働継続

レース          31〜37 の7本・重複0件（n = uniq = 7）
advisory lock   残留 0件
```

### ★`SIGKILL` で確かめた意味

`SIGKILL` は **`finally` を走らせません**。つまり「必ずロックを解放する」コードは
実行されずに殺されています。それでも**残留ロックが0件**でした。

Postgres の advisory lock がセッション単位で、接続が切れると自動解放されるためです。
設計時に意図した挙動ですが、**実際に殺してみるまで確証はありませんでした**。
ロックが残れば次の周が永久に待ち、A-1 が壊れます。
`SIGTERM` では綺麗に止まるのでこの経路は通りません。

---

## 4. A-4 / A-5 / A-6 / A-7 の実測

**A-4**（anon で実際に叩いた）

```
races_public  scheduled → seed_reveal=null / seed_commit は見える
              settled   → seed_reveal 見える
races（実体）  permission denied
```

さらに CHECK 制約で、確定前の行に `seed_reveal` を**書き込むこと自体ができません**。
露出経路とデータ混入経路の両方が塞がっています。

★**S-1 の指摘が無ければ**、`security_invoker=true` と `revoke all` が衝突して
**レース一覧が誰にも見えない**状態でした。実際に叩くまで分からない類です。

**A-5**（壊して確認）

```
① 失敗する購入 → 例外・EP も馬券も動かない（原子性）
② 再送        → 同じ馬券ID・1回しか引かれない（冪等性）
```

★**S-3 の指摘が無ければ** ② は FAIL でした。

**A-6** — ①自馬絡み以外は拒否 ②自馬絡みは購入可 ③5,000EP 超は拒否 ④他レースは購入可。
★②④を入れたのが要点で、①③だけだと「全部拒否する実装」でも PASS します（R-2）。

**A-7** — ①DB に宣言なし→起動失敗 ②不一致→起動失敗 ③一致→OK。
★①が要点で、「宣言が無ければ既定値」にすると設定漏れが黙って通ります。
**不明なら安全側とは「production でないとみなす」ではなく「止める」**です。

---

## 5. A-3 の顛末

```
M と races を比を保って同時に上げる（races = M × 8）
  M=1000  races=8000   89.67% (+7.67pt)
  M=2000  races=16000  84.61% (+2.61pt)
  M=4000  races=32000  82.62% (+0.62pt)  ← ±1% の内側
```

★**7回の仮説はすべて、`races ≪ M` という条件違反が作った見かけ**でした。
凸性は M で消え、分子は races で満たされます。**両方を同時に**上げる必要があり、
私は M だけを動かして races を固定していました。

「単勝なら恒等式で 80% を下回れない」というご指摘が無ければ、
測定条件を疑うところに辿り着いていません。

---

## 6. 未了・次便

| | |
|---|---|
| **A-1** | 24時間後に `journalctl \| grep -c "生成="` で実測（プロセス生存では見ない） |
| `seed_commit` の DB 保存 | 導出のみ実装。**プロセス再起動で秘密が変わる制約**が残っています |
| 番組表のワーカー接続 | 現在は仮のレース条件で生成 |
| A-3 の複数シード | seed 42 のみ |
| 世界候補の確定 | 40シード探索中 |
| フロント（Vercel） | 未着手 |

## 7. 照会（未回答）

Q-5（§10.3 の解釈2件）/ Q-6（近交ペナルティ 0.3 の採用理由）

---

## 8. 追記（2026-08-08 夕）— §8.6 が本番構成で成立

```
cycle=43  sha256(server_seed) == seed_commit  一致
cycle=44  一致
★§8.6: PASS
```

修正前は **Provably Fair が成立していませんでした**。`server_seed` をプロセスの秘密から
毎回導出していたため、**再起動で秘密が変わり**、コミット済みレースの `seed_reveal` を
後から出せませんでした。commit は公開済みなのに reveal が出せない状態です。
A-2 で SIGKILL を3回かけた直後だったので、実際に起こりうる話でした。

→ 生成時に `server_seed` を DB へ保存し、確定時に `seed_reveal` として公開します。
  `races_public` に `server_seed` を含めていないことも実測で確認しました。

### ★自己申告 — 30分ほど誤った前提で調査していました

**「ローカルのワーカーを止めた」と報告しましたが、止まっていませんでした。**
`pkill -f` は Windows では効かず、**古いコードのまま同じ本番 DB に書き続けて**いました。
VPS 側は正しく動いていたのに、ローカルが先に `UNIMPLEMENTED` のレースを作るので、
VPS は「既存」として飛ばしていました。

しかも **「ローカルのワーカー停止（残: 7プロセス）」というログを見ながら、
7プロセスの中身を確認していません**。停止したつもりで先に進み、その後
「VPS のコードが古い」という誤った前提で調べ続けました。
**A-2 の検証を濁らせないために止める**と自分で書いておきながら、
止まったことを確かめていませんでした。

これは A-3 の7仮説と同じ型です — **確認していない前提の上に推論を積む**。
今回は「プロセスを止めた」が未確認の前提でした。

→ 以後、プロセスを止めたら**残数ではなく対象が消えたこと**を確認します。
