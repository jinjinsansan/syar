# 照会（`user_identities` の参照先と、DB 制約を固定する手段）— 2026-08-20

**宛先**: レビュー側
**発行**: 開発側
**関連**: `REVIEW_AUTH_LINE_VERDICT_20260820.md` §5（D-078）／`REVIEW_WEB_ONBOARDING_VERDICT_20260820.md` §4 ②（セットアップ RPC）／V-19 #5・#10 ／ D-074 ／ D-075 ／ D-077
**未裁定**: Q-AUTH-06 / Q-AUTH-07

---

## 1. 🔴 Q-AUTH-06 — `user_identities.user_id` は `users` を参照できません

### 裁定の DDL

```sql
create table user_identities (
  provider text not null,
  subject  text not null,
  user_id  uuid not null references users(id) on delete cascade,   -- ★ここ
  ...
  primary key (provider, subject)
);
```

### 事実の突合（[CODE]）

| # | 事実 | 出典 |
|---|---|---|
| 1 | `users.id` は **`auth.users(id)` を参照する**（＝ゲーム口座の id は Supabase Auth のユーザー id そのもの） | `db/migrations/0001_init.sql:31` |
| 2 | **`users` 行が作られるのはセットアップ RPC の中**（表示名・牧場名・勝負服を集める必要があるため） | Q-WEB-04 ② の裁定 |
| 3 | **`sub` → 口座の対応が要るのは、その前のログイン時点**（戻ってきた利用者を「同じ人」と判定するため） | Q-AUTH-02 の設計案 |

**→ 2 と 3 の順序が逆です。** ログイン時点では `users` 行がまだ存在しないので、**`references users(id)` では identity 行を書けません。**

### ★これが起こす事故（具体的な筋）

```
① 利用者が LINE でログイン    → auth ユーザーができ、セッションが出る
② /setup の途中で離脱        → users 行は無い。identity 行も書けていない
③ 後日また LINE でログイン    → sub を引いても見つからない（②で書けていないため）
                              → **別の auth ユーザーが作られる**
④ 今度はセットアップを完走    → users 行 ＋ 初期 EP 2,000 ＋ 初期馬 1 頭（D-074/D-075）
⑤ ①の auth ユーザーでも完走可 → **同じ LINE アカウントから 2 口座・4,000 EP・馬 2 頭**
```

**★これは D-077 が「出口で担保する」と決めた複垢問題が、努力ではなくスキーマの穴から入ってくる形です。**
そして **V-19 #5（同一 `sub` で2回 → ユーザーは1つ）は、まさにこれを捕まえるための項目**ですが、
**`primary key (provider, subject)` は行が書けて初めて効く**ので、**書けない期間があるとゲートは素通りします。**

**★「制約は存在するが働いていない」— R-16 の型です。**

### 開発側の提案

```sql
  user_id uuid not null references auth.users(id) on delete cascade,
```

**`auth.users` を参照する。** 理由:

1. **auth ユーザーは①の時点で存在する**ので、**ログイン時に identity 行を書けます**（＝上の③で必ず見つかる）
2. `users.id` は `auth.users(id)` と同じ uuid なので、**ゲーム口座を引くのに支障はありません**
3. `users` を参照しないので、**セットアップ未完了の利用者も一意性の対象になります**（ここが要点）

### 併せて裁定をお願いしたいこと

- **identity 行を書くのはログイン時**で、**セットアップ RPC の1トランザクション（Q-WEB-04 ②）とは別トランザクション**になります。この分離でよいか
- 分離するなら、**「auth ユーザーはあるが users 行が無い」状態が正常な中間状態**として存在します。この状態の利用者に対する扱い（`/setup` へ誘導する・それ以外の RPC は拒否する）を明文化してよいか
- ⚠️ **開発側は `0017` をまだ DB に当てていません。** 参照先が変わるなら**ファイルを直してから当てます**（適用済みを後から書き換えると `tools/migrate.mjs` のチェックサムで落ちます）

---

## 2. 🟡 Q-AUTH-07 — V-19 #5・#6・#10 を固定する手段がありません

裁定 §4 は「`sub` の一意制約・二重作成（#5・#6・#10）は**ローカル DB（Supabase CLI）で可**」としていますが、**手段がありません。**

| 確認したこと | 結果 |
|---|---|
| Docker | **未インストール**（`docker: command not found`） |
| Supabase CLI | 導入可（2.115.0）。ただし **`supabase start` は Docker に依存**する |
| `psql` | 未インストール |
| この案件の「local」 | `secrets.local.env` は **remote の development プロジェクト**（`STAR_ENV=development`）。ローカルの Postgres ではない |

**→ 制約を「壊して落ちること」で確かめられる器が、今の作業環境にありません。**

**裁定をお願いしたいこと**: 次のどれで進めるか

| | 案 | 備考 |
|---|---|---|
| **(a)** | **development プロジェクトに `0017` を当てて、そこで #5・#6・#10 を固定する** | 最短。ただし**共有環境の状態を変える**ので、開発側の独断では行いません |
| (b) | オーナーに Docker を入れてもらい、ローカルで完結させる | 環境構築の手間。以後の DB 試験すべてが楽になる |
| (c) | staging の着手条件が揃うのを待ち、#4・#9 とまとめて叩く | 待ちが長い。ただし**どうせ staging で通す項目と一緒にできる** |

**開発側の意見**: **(a) が妥当**だと考えます。`0017` は `create table` のみで既存に触れず、development は本番でも staging でもないため。ただし **R-24（状態を変えるツールの向き先）に関わるので、指示をいただいてから実行します。**

---

## 3. 参考 — 既に報告済みで、裁定待ちのもの

`REPORT_AUTH_V19_20260820.md` §0 で1件照会しています（重複を避けるため再掲のみ）:

> **LINE は web ログイン＝HS256／ネイティブ SDK＝ES256** と用途で署名方式が変わる。
> **Expo（§15.4）で ES256 の第2経路が増える**ので、**V-19 を正典に書くときは「経路ごとに」課すことを明記していただけますか。**
