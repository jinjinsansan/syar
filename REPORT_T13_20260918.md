# 報告 — T-13: サイクルを 3 分にしました（D-007 改訂・2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**根拠**: ★**正典 `b929056`**（D-007 改訂・§10.3 を 480R へ）／ 裁定 `REVIEW_CONSULT_ARCADE_LOOP_ANSWER_20260918.md` §6 の順「2. T-13 の実装（正典の改訂の後）」
**前提**: **AL-6**（所要の裏付け・`08aa1e4`）／ **AL-9**（`RACES_PER_DAY` の導出・`97d3323`）

> 凡例（R-13）: **✔** ＝ 実測 ／ **△** ＝ 未確認 ／ **🔴** ＝ 欠陥・判断待ち

---

## 0. ✅ 実測（★不変であるべきものが不変であること）

```
CYCLE_MS        = 3 min
CYCLES_PER_WEEK = 80 / WEEK_MS = 4 h / WEEKS_PER_DAY = 6
RACES_PER_DAY   = 480 / class sum = 480 MATCH
G1_SLOTS        = 180,260,400 -> 09:00,13:00,20:00
PHASE(s)        = settle=0 publish=20 salesOpen=30 salesClose=150 parade=150 start=180
90 weeks        = 15 days
```

★**積み上げる実時間は 1 日も動いていません**（★1 週 4 時間・1 日 6 週・現役 90 週 ＝ 15 日）。
★**動いたのはレースの密度だけ**（144R → 480R）。★これが (B) を選んだ意味です。

---

## 1. 触った所（★D-007 改訂の条件どおり）

| # | 場所 | 前 → 後 |
|---|---|---|
| 1 | `cycle.ts` `CYCLE_MS` | 10 分 → ★**3 分** |
| 2 | `cycle.ts` `PHASE_OFFSET_MS` | 0 / 0:30 / 3:00 / 9:30 / 10:00 → ★**0 / 0:20 / 0:30 / 2:30 / 3:00**（★発売 2 分・条件 ①） |
| 3 | `week.ts` `CYCLES_PER_WEEK` | `= 24` の直書き → ★**4 時間 ÷ `CYCLE_MS` から導出**（★3 分なら 80） |
| 4 | `programme.ts` `RACES_BY_CLASS` | 42/36/24/18/15/9（計 144）→ ★**140/120/80/60/50/30（計 480）**（★正典 §10.3 の写し・条件 ③） |
| 5 | `programme.ts` `G1_SLOTS` | `[54, 78, 120]` の直書き → ★**時刻（09/13/20 時）から導出**（条件 ④） |
| 6 | `programme.ts` `G1_DAYS` | 枠番号の直書き → ★`G1_SLOTS` を参照 |
| — | `GRADED_PER_WEEK` | ★**据え置き**（★正典に明記。G1 は週 3 回固定・G2/G3 は比としてのみ使用） |
| — | `RACES_PER_DAY` | ★**AL-9 で導出済み**（144 → ★自動で 480） |

### ★③ について — `CYCLES_PER_WEEK` を「80 の直書き」にしませんでした

★正典は「24 → 80 にする」と書いていますが、★**4 時間 ÷ `CYCLE_MS` から導きました**。
★理由: ★`week.ts` の冒頭が「**数値を別々に書くと、片方だけ動かしたときに静かにずれる**」と
★**書きながら `= 24` の直書きだった**からです（★AL-9・`G1_SLOTS` と同じ形）。
→ ★**週境界とサイクル境界の一致が構造で保たれます**（★割り切れなければ投げる・R-27）。
⚠️ ★**字面からの逸脱なので報告します。** ★直書きに戻すべきなら戻します。

### 註記（AL-10・★実体と離さない）

★`cycle.ts` の D-037（60 分で自動中止）の根拠を「**6 サイクル → 20 サイクル**」に。
⚠️ ★**60 分という時間そのものは変えていません** — ★これは「客の金が戻るまでの時間」で書かれた値です。
★ほか `week.ts`・`programme.ts`・`main.ts`・`odds.ts`（★AL-6 の実測に差し替え）の註記を直しました。

---

## 2. 🔴 ★この便で見つけた欠陥（★洗い出しに入っていなかったもの）

### 2-1. ★**レビュー側の指摘**: `G1_SLOTS` が私の一覧から抜けていました

★`[54, 78, 120]` は「1 枠 = 10 分」が前提の値で、★**3 分にすると枠 54 は 09:00 ではなく 02:42**。
★**値は動かないので誰も気づきません。** → ★時刻から導出する形に直しました（✔ 09:00/13:00/20:00 を実測）。

### 2-2. 🔴 ★**ワーカーのログが「周の使用率」を 10 分基準で出していました**（★私が見つけたもの）

```ts
// apps/worker/src/main.ts:202（★直す前）
`周=${(cycleMs / 1000).toFixed(1)}s(${((cycleMs / 600000) * 100).toFixed(1)}%)`
```

★**`600000` の直書き**でした。★3 分サイクルでは分母が 3.3 倍大きいままなので、
★**使用率が実際の 1/3.3 に見えます**。

⚠️ ★**これは配備後に読む数字そのものです** — ★R-28 の最後の条件（配備後の周の実測）と、
★AL-6 の換算（39〜54%）を**本番で確かめる根拠**が、★**この行の出力**です。
★**気づかずに配備していたら、「余裕がある」と誤って読むところでした。**
→ ★`CYCLE_MS` から取る形に直しました。

---

## 3. ✅ 検査を 3 分の世界に書き直しました（★緩めていません）

★**10 ファイルが落ちました**（★私の洗い出しの予告は 7 ファイル）。★内訳と直し方:

| ファイル | 何が落ちたか | どう直したか |
|---|---|---|
| `scheduler/test/cycle.test.ts`（8 件） | 10 分・旧フェーズ・境界 | ★**新しい表をリテラルで置き直す**（★正典の写しなので定数から取らない）。★**「発売は 2 分ある」という検査を新設** |
| `scheduler/test/week.test.ts` | 「1 週はサイクル 24 本」 | ★80 本へ。★**1 週 4 時間の検査はそのまま緑** |
| `scheduler/test/programme.test.ts`（3 件） | 144R・クラス別・枠の循環 | ★480R・新しい表・0〜479 へ |
| `scheduler/test/prize.test.ts` | §9.3（賞金が主な稼ぎ口） | ★本数を `RACES_PER_DAY` から引く形に。★**新馬の本数 42 の直書きを「最も本数が多い」という性質**に置き換え（★同じ数を 2 か所で持たない・D-052） |
| `worker/test/cycle-runner.test.ts`（3 件） | `EPOCH + 4 * 60_000` が**サイクル 1 に入った** | ★**`CYCLE_MS × 0.4` から取る**形に（★10 分なら 4:00・3 分なら 1:12。★どちらも発売中の相） |
| `worker/test/training-runner.test.ts`（5 件） | `const WEEK = CYCLE_MS * 24` | ★`CYCLE_MS * CYCLES_PER_WEEK` へ |
| `worker/test/selfcheck.test.ts` | ★**配る物（`dist/worker.cjs`）が古い** | ★`npm run build:worker` で再ビルド（★検査が「配る物が原本と違う」を正しく捕まえた形） |
| `cli/test/first-pass-time-of-day.test.ts` | `s * 10`（1 枠 10 分の直書き） | ★`s * MINUTES_PER_SLOT` へ |
| `cli/test/calibration-registry.test.ts` | ★新しい定数 3 つの登録漏れ | ★`MINUTES_PER_SLOT`・`G1_HOURS_JST`・`WEEK_REAL_MS` を理由付きで登録 |
| `render/test/edit-grammar-audit.test.ts` | ★**既知の赤**（この便と無関係） | ★触っていません |

⚠️ ★**通すために緩めた検査はありません。** ★どれも「3 分の世界で成り立つべき性質」に書き直しています。

---

## 4. ✅ 検証

| # | |
|---|---|
| 1 | 型検査 ✔ **終了コード 0** |
| 2 | 検査一式 ✔ **2,079 本中 2,078 本が緑**（★終了コード 1）。🔴 1 本は**既知の赤**（`edit-grammar-audit` ⑨・✔ `packages/render` は触っていません） |
| 3 | 配る物 ✔ `npm run build:worker` **終了コード 0**・✔ selfcheck が原本と一致 |
| 4 | 全走査 ✔ `600_000` / `144` / `10分` の直書きを**リポジトリ全体で洗い直し**、★残りは**過去の経緯を述べた註記だけ**であることを確認（★`main.ts:202` はこの走査で見つけました） |

---

## 5. 🔴 残り（★この便に混ぜていません）

| # | |
|---|---|
| 1 | ★**較正の取り直し**（**AL-11**・D-007 改訂 ⑥）— V-4・V-5・V-6・V-14・§3.4 の収支・帯の下のゲート。★**GB-6 と 1 回にまとめる**（★二度測らない）。⚠️ ★**1 日の賞金総額は 3.3 倍になりました**（★`prize.test.ts` の註記に残しました） |
| 2 | ★**配備後の周の実測**（R-28 の最後の条件）。★`main.ts` の 1 行を直したので、★**使用率が正しく出ます**。★AL-6 の換算（39〜54%）と突き合わせてください |
| 3 | ★**T-10（D-114 素質を見せない）・T-11・T-14（D-115）は別便**（★裁定 §6 の順・★着順に効くものと効かないものを混ぜない） |
