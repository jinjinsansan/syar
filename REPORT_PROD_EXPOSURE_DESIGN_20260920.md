# 設計案 — 公開中のサイトの露出を減らす（①検索避け ②開発用ページ）

> 2026-09-20・開発側。**案です。実装していません。**
> 🔴 **`main` に押すと公開サイトが即 入れ替わります。外に出る操作です。**

## 0. 自分で確かめた（実測・読むだけ）

| # | 事実 | 確かめ方 |
|---|---|---|
| 1 | **`noindex` がどのページにも無い** | `/` `/stable` `/art-lab` の配信 HTML に **0 回** |
| 2 | **開発用ページが公開で開く** | `/art-lab` `/design-check` `/race-quality-lab` … **HTTP 200** |
| 3 | **`/stable` に「素質」の列が在る** | 配信 HTML に「素質」3 回・「素質の天井（★）は変わりません」 |
| 4 | **その直しは `main` に無い** | `b3a89b9`（T-10・D-114）… p4 に在り / **main に無い** |
| 5 | **枝の差** | `main..p4` = **306** ／ `p4..main` = **0**（= きれいに早送りできる） |

⚠️ **3 は「本物の素質が漏れている」ではありません。** いまはデモデータです。
**設計として出ている**（列と★の説明が在る）という話で、`D-114` の趣旨に反します。

---

## 1. 🔴 いちばん大事な制約

```
main に commit を 1 つ置くと、306 コミットの「きれいな早送り」が崩れます
（p4 が 1 つ 遅れ、以後 merge か rebase が要る）
```

**そして ①② を `main` に届けるには、`main` に触るしかありません** — **コードで解くなら。**

---

## 2. ✅ 推す案 — **コードを触らずに、いま塞ぐ**

> **Vercel の Deployment Protection（プロジェクト設定）を有効にする。**

| | |
|---|---|
| **誰が** | **オーナー**（Vercel の管理画面。私はアクセスできません） |
| **コミット** | **0**（→ 306 の早送りが**そのまま残る**） |
| **戻せるか** | ✅ **1 クリックで戻せます**（コミットと違い、痕跡も残りません） |
| **①検索避け** | ✅ 解決（**認証が要るページは索引されません**） |
| **②開発用ページ** | ✅ 解決（**サイト全部**が公開から外れます） |

**なぜこれで足りるか**: **いま公開されているのは全部デモデータで、利用者は 0 人**です
（本番 `users 0 / bets 0 / 台帳 0`・実測）。**閉じて困る人が居ません。**

🔴 **コードで解くより強い理由**:
- `noindex` は**お願い**です。従わない巡回もあります。**認証は拒否**です
- 開発用ページを 1 つずつ塞ぐ必要がありません（**塞ぎ漏れが原理的に起きない**）
- **`main` を触らない** → 306 の追いつきが壊れません（条件④が自動で満たされます）

⚠️ **副作用（先に書きます）**:
- **`/api/healthz` も閉じます** → **`O-6` の確認が通らなくなります**
  → Vercel の **Protection Bypass トークン**を使うか、`/api/healthz` だけ除外設定にする。
  **どちらにするか決めてから**有効化してください（でないと `DP-1` の記録が止まります）
- 私の読むだけの確認（HTTP 200 の類）も通らなくなります。**それは構いません**

---

## 3. コードで解く場合（案 2）— **推しませんが、設計は出します**

### 中身（最小・他を混ぜない）

```
① noindex … apps/web/src/app/layout.tsx の metadata に 1 か所だけ
   export const metadata = { robots: { index: false, follow: false } }
   🔴 ページごとに書かない（D-052）

② 開発用ページ … 消さずに「本番で出さない」
   各 lab ページの先頭で VERCEL_ENV === 'production' なら notFound()
   🔴 人が設定する環境変数を使わない（VERCEL_ENV は Vercel が入れる・O-6 の教訓）
   ⚠️ ただし「各ページに 1 行」は D-052 に触れます
      → 共通の軽い部品（assertNotProductionPage()）を 1 つ作り、各ページはそれを呼ぶ
```

### 🔴 どう `main` へ持っていくか（**ここが本題**）

| 案 | 中身 | 早送り | 判定 |
|---|---|---|---|
| **2-a** | p4 を main に早送りする | ✅ 保たれる | 🔴 **不可**。306 コミットが一度に本番へ出ます（**最小の逆**） |
| **2-b** | main に直接 1 commit | 🔴 崩れる | ⚠️ 可。あとで p4 に main を merge（**merge コミットが 1 つ増える**） |
| **2-c** | p4 で作り、main に cherry-pick | 🔴 崩れる | ⚠️ 2-b と同じ（SHA が別物になる） |
| **2-d** | main から枝を切り、main へ merge | 🔴 崩れる | ⚠️ 2-b と同じ |

→ **コードで解く限り、早送りは必ず崩れます。** 条件④（追いつきを壊さない）と両立しません。
→ **だから案 2 ではなく案 1（設定）を推します。**

⚠️ どうしても案 2 なら **2-b**（いちばん単純）。崩れる代償を承知のうえで。

---

## 4. 検査（条件⑤）— **`CK-14` を守る**

```
★本番で /art-lab が 404 であること      … 塞げたか
★対照: /home が 200 であること          … 🔴 塞ぎすぎていないか
```
🔴 **対照が無いと、「サイトごと落ちている」が満点に見えます。**

⚠️ 🔴 **案 1 を採ると、この検査は書けません**（**両方 401 になる**ため）。
→ 案 1 の場合の検査は**別物**になります:
```
★/art-lab が 401（認証が要る）  ＋  ★対照: bypass トークン付きなら 200
```
→ **どちらの案を採るかで検査が変わります。** 先に案を決めてください。

---

## 5. 私の判断

```
✅ ①② … 案 1（Vercel の設定）を推します。★コミット 0・1 クリックで戻せる・塞ぎ漏れ無し
⏸ D-114 の直し（b3a89b9）… ★急ぎません。★案 1 で外から見えなくなります
🔴 ③（移行 33 件）… ★変わらずオーナーの直接の指示待ち。★①② とは別の話です
```

⚠️ **案 1 は私には実行できません**（Vercel の管理画面に入れません）。**オーナーの操作です。**
⚠️ **案 2 を採るなら、`main` に押すのは外に出る操作**です。**直接の指示をいただいてから**にします。
