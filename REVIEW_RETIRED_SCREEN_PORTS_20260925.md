# 【STAR】裁定: `/stable/retired` を本物へ繋いだ ／ 足りない口 2 つ（(a) 判明した能力・(b) 公開の引退馬一覧）— 2026-09-25

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa` の報告（`/stable/retired` の結線・繋げなかった 2 つ・網を 1 つ緩めた件）

## 1. ✅ 結線を受理
- ✔ 一覧は `my_retired_horses()`（`0075`）・生涯の記録は公開の view（`horse_story_event_public`・`0043:90`）。★summary は**戦績の事実だけ**（★素質・能力を出さない・D-114）
- ✔ ★**4 つの状態**（読み込み中・未ログイン・0 頭・記録 0 件）を足した → ★**空を「無い」と言い切らない**（CK-14 の画面版）
- ✔ ★繋げなかった 2 つを**画面に書いた**（「見本」の札・「口がまだ無い」と明記）→ ★**本物に見せない**。★デザイナーへの 1 行にそのまま使える

## 2. ✅ 網を 1 つ緩めた件 — 認める
- ★`horse-story-wiring` ⑤（LR-6）は `stableName` が画面に在ることを要求していた。★他人の馬を出せなくなったので落ちた → ★**「他人の馬を出しているときだけ必須」**の分岐にした
- ✔ ★**対照で確かめている**（「いまは自分の馬だけ出ます」の文を消すと必須に戻って落ちる）。✔ ★**負の側（`displayName` / `ownerName` を出さない）は 1 つも緩めていない**
- → ★**緩め方として正しい**（★要求を消したのではなく、★**要求が効く条件を書いた**）。★黙って緩めなかったことも正しい

## 3. (a) 条件別の出走回数を返す口 — ✅ **開発側が作ってよい**
- ★発見度（D-108）は「表示の層だけに持ち、内部値に触らない」。★回数は**戦績**＝公開の事実
- **条件**:
  1. ★**返すのは回数だけ**（★条件ごとの出走数・勝数）。★素質・能力・現在値・段そのものを返さない
  2. ★**段（「得意かも」等）は TS の 1 か所で導く**（D-108 ②「表示と同じ量から導く」）。★**SQL で段を計算しない**
  3. ★**自分の馬だけ**（★他人の馬の発見度は出さない）
  4. ★口が出来たら、★**釘（`horse-story-demo` が残る理由）から外す**（★開発側が自分で註記した「外し忘れると『見本のまま』が釘で守られる」は正しい）

## 4. (b) 公開の引退馬の一覧 — ✅ **作ってよい。★出す列をここで決める**
✔ 正典 **LR-6**（`:2265`）: 「**他人の馬の物語も見える**（§1.4-4 共通世界の社会性）。ただし**持ち主の個人情報は出さない**（★**表示名・牧場名まで**）」。★既に `horse_story_event_public` が誰でも読める。→ ★**範囲は正典に在る**。照会に回さず、ここで決める。

**出してよい列**: 馬の名前／性別／生年（または年齢）／**戦績の事実**（出走数・勝数・重賞勝ち数）／父と母の名前（血統）／**牧場名**（LR-6 の上限）／生涯の記録（既存の公開 view）
**出さない列**: 素質・能力・現在値・遺伝子・発見度の段（D-114）／利用者の表示名より細かい情報・メール・ID の類（LR-6）
**条件**: ①並び順と件数は**決定論**（★同じ入力で同じ結果・件数の上限を持つ）②`anon` にも開けてよい（★`horse_story_event_public` と同じ扱い）③★**見本の行を混ぜない**（開発側の判断どおり）④★口が出来たら釘と画面の「準備しています」を**同じ便で**外す

## 5. ★レビュー側の見立て（順番）
★(a) より **(b) が先**。★理由: (b) は ★**LR-6 という正典の項目が未実装**（＝「決めたのに渡す側が無い」族）で、★(a) は **表示の親切さ**。★ただし両方とも読むだけの口なので、★開発側が続けて作ってよい。
