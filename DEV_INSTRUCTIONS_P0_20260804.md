# 開発指示書 P0 — 遺伝エンジン + 100世代シミュレータ

**発行**: 2026-08-04（レビュー側 Claude）
**宛先**: 開発側 Claude（このディレクトリ `V:\dev\Cusor\star` で起動したセッション)
**承認**: オーナーがこの指示書をあなたに渡した時点で「コード開発OK」とみなす

---

## 0. あなたの役割と体制

あなたは「STAR」（ポイ活型オンライン競馬育成ゲーム・開発コード）の**開発側 Claude**である。三者体制で動く:

| 役 | 担当 |
|---|---|
| オーナー | 最終判断。日本語話者 |
| **開発側（あなた）** | 実装・テスト・報告書の作成 |
| レビュー側（別セッションの Claude） | 仕様の正典管理・あなたの成果物を**独立に再実行して照合**する |

- **正典は `STAR_SPEC_v2.0.md`**。本指示書と矛盾したら正典が勝ち、矛盾を報告書に書くこと
- 旧文書（`SPEC.md` / `CLAUDE.md` / `AI_PROMPT.md`）は**廃棄済み・読まない・従わない**。`GORILLA_STABLE_SPEC_*` は別プロジェクトの参考資料であり正典ではない
- あなたの報告は鵜呑みにされない。レビュー側がコードを直読し、検証コマンドを再実行する。**動かないもの・盛った数字は必ず露見する**。正直に書くこと

## 1. 憲法（違反 = 即差し戻し）

1. **命名**: コード内の変数名・テーブル名・コメントに他社製品由来の語を使わない（「スターホース」「メダル」「GP」「WBC」「ライド」等）。ポイントは `entry_points`(EP) / `prize_points`(PP)。ただし **P0 にポイントは登場しない**（下記スコープ）
2. **実在馬名を辞書・テストデータに入れない**。テスト用馬名は無機質でよい（`HORSE_001` 等）
3. `git.txt` に認証情報が含まれる。**読まない・使わない・コミットしない**。外部ネットワーク接続（Supabase 含む）は P0 では一切不要
4. 決定論: `Date.now()` や `Math.random()` を直接使わない。**乱数と時刻は必ず注入**する（シード固定で完全再現できること）

## 2. スコープ — P0 で作るもの / 作らないもの

**作る**（すべてローカル・UIなし・サーバーなし）:

1. **`packages/sim-engine/`** — 純粋 TypeScript の遺伝エンジン（依存ゼロ。Node にも React にも依存しない。`crypto`/`fs` 直接使用禁止・必要なら注入）
2. **100世代シミュレータ CLI** — 遺伝エンジンを使って世代を回し、§13 の検証 V-1/V-2/V-3 を実行する
3. **ユニットテスト**（Vitest）

**作らない**（スコープ逸脱 = 差し戻し）:

- レースエンジン（P1）/ オッズ・ベッティング（P2）/ 育成ループ（P3）/ UI / DB / API / Supabase 接続 / デプロイ設定

## 3. 実装仕様

### 3.1 プロジェクト構成

```
V:\dev\Cusor\star\
├── package.json            # npm workspaces
├── tsconfig.json           # strict: true, any禁止
├── packages/
│   └── sim-engine/
│       ├── src/
│       │   ├── types.ts        # Genotype, HorseRecord, BalanceConfig 等
│       │   ├── rng.ts          # シード可能PRNG (xoshiro128** 等)
│       │   ├── genetics.ts     # アレル継承・隔世遺伝・突然変異 (SPEC §6.2-6.4)
│       │   ├── inbreeding.ts   # Wright近交係数・5代血統キャッシュ (SPEC §6.5)
│       │   ├── nicks.ts        # ニックス判定 (テーブルは注入・SPEC §6.6)
│       │   ├── breeding.ts     # 配合フロー統合 (SPEC §6.1)
│       │   ├── founders.ts     # 創始世代の生成 (§3.3)
│       │   └── balance.ts      # SPEC §13.1 の BALANCE 定数 (外出し)
│       └── test/
└── apps/
    └── cli/
        └── simulate.ts         # 100世代シミュレータ
```

- git 管理: リポジトリ未初期化なら `git init`。意味のある単位でコミット。**リモートへの push はオーナー指示があるまで禁止**
- `.gitignore` に `git.txt` と `files.zip` を必ず入れる

### 3.2 遺伝エンジン（SPEC §6 を実装）

- 2アレル継承（§6.2）/ 隔世遺伝 6%・大物覚醒 1.5%（§6.3）/ 突然変異 N(0,45) clamp±150・大突然変異 0.8%（§6.4）
- Wright 近交係数: 5代までの共通祖先探索。`pedigree_cache`（5代分の祖先ID展開）を馬ごとに持ち O(1) 参照（§6.5）
- インブリード補正・近交弱勢・ニックス倍率・potential 確定・初期 stats（開放率 0.28〜0.35）— §6.1 のフロー通り
- genotype スキーマは SPEC §5.4 に完全準拠
- **全定数は `balance.ts` から参照**。マジックナンバー禁止

### 3.3 創始世代（founders）の生成 — 本指示書で新規定義

正典 §6 は「親がいる」前提のため、初代馬の生成規則をここで定める（§10 NPC プリシードでも再利用する。チューニング対象):

```ts
export const FOUNDERS = {
  ABILITY_MEAN: 450, ABILITY_SD: 110,   // 各能力アレル: clamp(N(450,110), 100, 850)
  TEMPER_MEAN: 50,  TEMPER_SD: 15,      // clamp 5..95
  DURABILITY_MEAN: 650, DURABILITY_SD: 100,  // clamp 300..950
  DISTANCE_CENTER_RANGE: [1200, 3000],  // 一様分布
  DISTANCE_RANGE_RANGE: [400, 1000],    // 一様分布
  SKILL_GENE_COUNT: [0, 2],             // 0〜2個を等確率
};
```

- 性別は牡50%/牝50%。サイアーライン/母父系ラインは創始馬に `LINE_A`〜`LINE_T`（20系統）をランダム割当し、以後は父系/母父系から継承
- ニックステーブル: シミュレータ用に 20×20 のうち5%のセルに 1.05〜1.15 の倍率をシード固定で生成（本番テーブルは運営が後日定義）

### 3.4 100世代シミュレータ CLI

```bash
npm run simulate -- --generations 100 --seed 42 [--population 500] [--json out.json]
```

- 各世代: 繁殖牝馬プール（既定200頭）× 年1産、種牡馬は「能力合計上位20%からランダム選択」既定。世代パラメータは CLI 引数で変更可能
- 生涯上限（牝8産・種牡馬の年間上限 §6.7）を守る
- 出力（JSON + 人間可読の表を両方）:
  - 世代ごと: 平均能力(5種別+合計)・最大/最小・アレル値分布・平均近交係数F・隔世遺伝/大物覚醒/大突然変異の発生数
  - 最終サマリー: **V-1**(同一配合100回の potential SD/平均)・**V-2**(100世代後の平均能力が初期比何%か)・**V-3**(隔世遺伝6%±0.5pt / 大物覚醒1.5%±0.5pt の実測)
  - PASS / FAIL 判定を明示

### 3.5 ユニットテスト（最低限）

1. **決定論**: 同一シード → 同一の子・同一のシミュ結果（全出力バイト一致）
2. **近交係数の既知ケース**: 親子交配 F=0.25 / 全兄妹 F=0.25 / 3×4クロス F=0.0703125（=(1/2)^7 × 1 + (1/2)^... 正確な期待値をテスト内で導出コメント付きで示す）/ 無関係 F=0
3. **potential 上限**: いかなる補正の組み合わせでもアレル値・potential が上限を超えない
4. **近交弱勢**: F>0.25 で虚弱フラグが約25%発生（シード固定の統計テスト）
5. **境界**: 生涯8産を超えない・種付上限を超えない

## 4. 合格基準（SPEC §13.2）

| 検証 | 目標 |
|---|---|
| V-1 | 同一配合100回の potential SD が平均の 12〜18% |
| V-2 | 100世代後の平均能力が初期比 **+50% 以内**（最重要・血統インフレ） |
| V-3 | 隔世遺伝・大物覚醒の実発生率が設定値 ±0.5pt |
| テスト | 全ユニットテスト PASS |
| 決定論 | `--seed 42` の出力が実行のたびに完全一致 |

目標から外れたら `balance.ts` の定数を調整して再検証してよい（調整履歴を報告書に記録）。**目標値そのものは変更禁止**（変更はオーナー承認事項）。

## 5. 報告様式

完了したら `REPORT_P0_[日付].md` をこのディレクトリに作成:

1. 実装したもの・ファイル一覧
2. 実行方法（コマンドをコピペで再現できる形で）
3. 検証結果（V-1/V-2/V-3 の実測値・使用シード・PASS/FAIL）
4. 定数の調整履歴（初期値から変えたものと理由）
5. 仕様からの逸脱・解釈で埋めた箇所（**必ず全部列挙**。ゼロなら「なし」と明記）
6. 未解決の質問

→ レビュー側が `npm test` と `npm run simulate` を**独立に再実行**して照合する。再現しない報告は差し戻しになる。

## 6. 質問がある場合

実装を止めるほどの仕様の穴があれば、報告書を待たず `QUESTIONS_P0_[日付].md` に書いてオーナー経由でレビュー側に回すこと。推測で正典を上書きしない。
