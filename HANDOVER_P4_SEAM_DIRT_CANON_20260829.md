# 引継ぎ書 — 次のセッションはここから（2026-08-29）

作成: 2026-08-29 / ブランチ `p4/race-30sec-cuts` / HEAD `f41e718`
起点: `9c8f9ed` → **12 コミット**（★未 push）
前の引継ぎ書: `HANDOVER_P4_RACE_QUALITY_20260828.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**
> ★この案件は R-25 で 9 日間の事故を 1 度出しています。

---

## ★0. まず 3 分でやること

```powershell
cd V:\dev\Cusor\star
git log --oneline -12                                    # ★この便の 12 本
git log --oneline origin/main..HEAD | wc -l              # ★未 push の数（直書きしない）
git status --short                                       # ★` M apps/web/package.json` だけのはず
npm test                                                 # ★1237 件 PASS / EXIT=0 のはず
```

⚠️ ★**`apps/web/package.json`（ポート 3210）は触らないこと。** ★オーナーの既存作業で、
　★この便でも一度もコミットに混ぜていません。★`git add -A` するときは必ず `':!apps/web/package.json'` を付けます。

---

## ★1. いま止まっているもの（★次の一手はここ）

### ★1-1. オーナーの目待ち [EYES] — ★**4 件**

★**これが最大の待ち行列です。** ★開発側からは出せません。

| # | 見るもの | URL |
|---|---|---|
| ① | ★**(b′) が入った絵**（前と比べる） | `?laneFocus=off` を付けると切れます（下） |
| ② | ★4 角**入口**の反転 ←11px/m（★出口は消しました） | 17.3s あたり |
| ③ | ★砂煙の濃さ（後続が被る量） | `?surface=dirt` |
| ④ | ★17.90s の**軸跨ぎ**（左右反転が 1 回入っている） | 4 角のカット内 |

```powershell
cd apps/web ; npm run dev
# ★出荷の絵
http://localhost:3210/race?seed=42&cinematography=v6&contest=1.6
# ★(b′) を切って比べる
http://localhost:3210/race?seed=42&cinematography=v6&contest=1.6&laneFocus=off
# ★ダート
http://localhost:3210/race?seed=42&surface=dirt
```

### ★1-2. オーナー判断待ち — 2 件

| # | |
|---|---|
| ⑤ | ★**push**（★数は `git log --oneline origin/main..HEAD \| wc -l` で数えること） |
| ⑥ | ★**決定ログの二重帳簿** — 埋めるか、廃止して §0.4 に一本化するか |

### ★1-3. 開発側で着手できるもの — 1 件

| # | | 状態 |
|---|---|---|
| ⑦ | ★**恒久の別字対策**（Q-K-3） | ★**単独の便で**、と裁定が定めています。★未着手 |

⚠️ ★**⑦ は [EYES] を待つ必要がありません。** ★①〜④ が返るまでの間に進められる唯一のものです。

---

## 2. この便でやったこと（12 コミット）

```
4e13146  照会: 走路の折れ目の跳び（残件 A-2）の判断を出す — 上限は 12px でなく 33.5px だった
2fcbf13  正典: D-081〜D-087 と R-31 を起票する（8 日分・正典は 8/21 で時が止まっていた）
fa65a83  註釈に別字が 47 箇所混ざっていた（grep 芝 が芝の註記を 38 件取り逃していた）
d84841a  ダート: 後続が前の馬の砂を被る（報告 §10-1「ダートの見せ場」）
f459d35  4 角: 出口の走行方向の反転を消す（測っていない量を最適化していた）
e0ef6df  報告: 4 角の出口の反転 / ダートの砂被り / 正典の起票 / 照会 2 件
de20958  報告書に照合の材料を足す（期待値つきの再実行表・判定してほしいこと）
4017d77  記録: 裁定と、正典の D-088・R-22 修復（レビュー側の発行物）
595bc7b  差し戻し 1 件を直す: 照会文書自身が検査を赤にしていた
bdc5b32  (b′) の未検証 3 点を測る — 測定 1 が私の実装バグを捕まえた
42d12d4  記録: (b′) の裁定と、正典の D-089 起票（レビュー側の発行物）
f41e718  (b′) を既定にする — 残件 A-2 を閉じる（戻し口と、戻せることの検査を同じ便で）
```

### 2-1. ★製品で振る舞いが変わったのは 3 つだけ

| # | 何を | どこ |
|---|---|---|
| ① | ★4 角の固定カメラ **90m → 30m** | `broadcast-v2.ts` の `fourth-corner-front.fixedCamera`（2 行） |
| ② | ★砂を描く順（後方の砂を全馬のあとへ） | `perspective-draw.ts`（★塗り方は 1 文字も変えていない） |
| ③ | ★**(b′) を既定に**（注視点を走線に沿った長さで置く） | `broadcast-v2.ts` の `LANE_ALIGNED_FOCUS_DEFAULT = true` |

★残りは**註釈の別字直し・検査・道具・文書**です。

### 2-2. 正典に入ったもの（★下流に流れる）

| | |
|---|---|
| **D-081〜D-087** | 開発側が起票（8 日分・裁定 §8 の 7 件に一対一） |
| **R-31** | 測定器の既定は「画面と同じ側」に倒す |
| **D-088** | 注視点の上限は画面の実寸から引く（★レビュー側が追番） |
| **D-089** | 走路の折れ目は (b′) で直す（★レビュー側が追番・切り替えの条件つき） |
| ★**R-22 の修復** | ★`6dbbfce` で改行が落ち、**R-22 の行が R-29 に飲み込まれていた**（9 日間 `grep` 0 件） |

---

## ★3. 次のセッションが踏みやすい穴（★ここが本題です）

### ★3-1. 提出物を足したら、もう一度 `npm test` を回す

★**この便で実際にやらかしました。** ★最後の**コード**変更のあとに緑を見て、
★**そのあと照会文書を足し、回さずに「1223 件 PASS」と報告**しました。★レビュー側から差し戻されています。

⚠️ ★**報告書も照会文書も追跡ファイルで、この案件では検査の対象です**
（`source-integrity.test.ts` が `git ls-files` の全追跡ファイルを走査します）。

→ ★**`git add` してから `npm test` を回すこと。**

### ★3-2. 緑を報告するときは、実行ログと終了コードを貼る

```powershell
npm test > log 2>&1 ; echo "EXIT=$?"
```

⚠️ ★**パイプの先から終了コードを読まないこと**（R-27 の系）。★「PASS しました」だけにしない（R-3）。

### ★3-3. `python -c "..."` に日本語＋バッククォートを渡さない

★**この便で正典に壊れた行を書き込みました。** ★バッククォートがコマンド置換になり、
★`` `script` `` `` `DEFAULT_RACE_SCRIPT` `` などが**消えた行**が入ります。★正典 R-26 の実例と同じ形です。

→ ★**スクリプトはファイルに書いてから実行する。** ★書いたあとは**内容を照合**して確かめる。
⚠️ ★併せて、この端末は **cp932** です。★Python から日本語や ⚠️★ を print すると落ちます。
　★`PYTHONIOENCODING=utf-8` を付けること。★付け忘れると**壊れていないものが壊れて見えます**
　（一度これでソース破損を誤検知しかけました）。

### ★3-4. 手当ては、手当てを書いた場所にしか効かない

★別字の検査を書いたとき、★**検査ファイル自身が引っかかって落ちました**。★直しました。
★**その直後、走査される側の `.md` で同じ穴を踏みました**（同じ日に 2 回）。

→ ★「直した」と思ったら、★**同じ形が他のどこにあるかを一度探す。**

### ★3-5. 何を最適化しているかを、苦情の言葉と突き合わせる

★4 角のカメラが 90m だったのは「★**カット内**の反転を消す」ためでした。
★しかしオーナーの苦情は「★**境目**の反転」（「別のレースに見える」）でした。
★掃引表は**中央のコマ**と**カット内の反転回数**しか出しておらず、
★**境目で効く「入口の 1 コマ目」「出口の最終コマ」を一度も出していませんでした。**

→ ★**測っていない量を最適化していた。** ★列を足したら答えが出ました（D-082 と同型）。

### ★3-6. 却下されたものを、名前で扱わない

★「カット内の反転」で却下されたのは ★**素材の反転をカット開始時に固定する機構**であって、
★**向きが軸で入れ替わること自体ではありません。** ★前便はこの 2 つを同じ名前で呼び、選択肢を 1 つ失っていました。

★同じ形が残件 A-2 でも起きています — ★台帳の「(b) カメラの進みを均す」に
★**「平滑化」と「幾何の置き換え」の 2 つが混ざっており**、★分けたら片方（(b′)）は欠点を持ちませんでした。

### ★3-7. 片道ではなく往復を見る

★(b′) の幾何の実装が壊れていました（`laneArcLengthAt` の末尾が負値を足す）。
★**片道では「それらしい数字」を返します。** ★往復（`s → 長さ → s`）で初めて **100m** のずれが出ました。

⚠️ ★裁定が「★**①の不変条件を先に測れ（ここが割れたら他は無意味）**」と順序を指定していなければ、
　★**注視点が −410m ずれたまま「跳びは 0 になりました」と報告していた**と思います。

### ★3-8. 数え方が、探しているものの形と合っているかを先に問う

★「1 回しか出ない漢字 129 種」を洗いました。★**別字は 0 件**でした。
⚠️ ★ただし今回の実害 **U+82DD（芝）は 38 箇所**あり、★**この一覧には出てきません**。
　→ ★**この検査は、今回の実害を一件も見つけられなかったでしょう**（正典 R-22）。

### ★3-9. 既定は「呼ばれる側」に置く

★(b′) の既定は `resolveBroadcastV2Scene` の**中**にあります。
→ ★道具が渡さなくても**画面と同じ側**へ落ちます（R-31）。★道具ごとに書き足す必要がありません。

⚠️ ★**R-31 の再発 9 件は、すべて既定が「呼ぶ側」にあったために起きていました。**

---

## 4. 残件台帳（`RESIDUALS_P4_RACE_VIDEO.md`）

| # | | 状態 |
|---|---|---|
| **A-1** | 4 角の境目の反転 | ★**半分解決**（出口は消えた／入口は残る・[EYES] 待ち） |
| ~~A-2~~ | ~~走路の折れ目の跳び~~ | ★**直しました**（D-089・台帳から削除済み） |
| **A-3** | 直線の 2 本の境目に閃光がない（Q-5） | 当面入れない（機構改修は単独の便） |
| **A-4** | 13 頭以上の勝負服の最小色距離 27.5 | 12 頭立てでは出ない |
| **B-1** | `audit-race-broadcast.mjs` が発走の遅れを引いていない | 据え置き |
| **C-1** | 正典 D-081 以降 | ★**起票済み**（D-088 / D-089 もレビュー側が追番） |
| **C-2** | 未 push | ★**数を直書きしない**ようにしました（コマンドで数える） |
| **C-3** | ダート | ★**オーナー指示で着手済み**。★残るは Q-3 の [EYES] |

⚠️ ★**台帳の番号は詰めません。** ★過去の便が参照している番号が動くほうが害が大きいためです。

---

## 5. この便で増えた検査（★次に触るとき落ちたら、これを読むこと）

| 検査 | 何を守っているか |
|---|---|
| `packages/render/test/dirt-dust-order.test.ts`（5 件） | ★砂の**描く順**（後続が被れる／自分の砂で曇らない／芝では 1 粒も描かない） |
| `packages/render/test/lane-arc-length.test.ts`（6 件） | ★走線の長さの**往復が一致**（★元の壊れ方を再現した式が落ちることまで） |
| `packages/render/test/lane-aligned-focus.test.ts`（8 件） | ★(b′) の既定と**戻せること**（★URL だけでなく**実際に画が変わる**ことまで） |
| `apps/cli/test/race-video-invariants.test.ts`（+1 件） | ★4 角**正面から出る**境目で走行方向が反転しない |
| `apps/cli/test/source-integrity.test.ts`（+2 件） | ★別字の**目印**（★防壁ではありません。列挙は必ず漏れます） |

---

## 6. 再実行（★すべて読取専用）

```powershell
# ★4 角
npx tsx tools/audit-cut-seam.mjs --seeds 42,253,90,2 --script v6   # 反転 4（入口のみ）/ 共通 0 頭 0 / 中央 303px
npx tsx tools/audit-corner-camera-sweep.mjs                        # ★入口は全 72 通りで ←
npx tsx tools/audit-corner-turn.mjs --script v6                    # 左右反転 1 回（18.03s）
npx tsx tools/_camdist.mjs                                         # 軸跨ぎ 17.90s・179.6°

# ★走路の折れ目（(b′)）
npx tsx tools/_seamslip.mjs                                        # 120 通りで 15.7px → 0.0px
npx tsx tools/_lanefocus.mjs                                       # 往復の一致／注視点のずれ 最大 0.25m

# ★ダート
npx tsx tools/_dustcover.mjs                                       # 他馬の砂の中 平均 5.5 → 7.5 頭

# ★全体
npm test > log 2>&1 ; echo "EXIT=$?"                               # 1237 件 PASS / EXIT=0
npm run typecheck
```

⚠️ ★`tools/_*.mjs` は `.gitignore` 対象・**追跡外**です。★ただし**登録簿には載せます**
（`tools/lib/classification.mjs`）。★載せ忘れると `tool-guard.test.ts` が落ちます
（★この便でも 1 度落としました）。

---

## 7. 発行物（この便）

| 種別 | ファイル |
|---|---|
| 報告 | `REPORT_P4_A1_DIRT_CANON_20260829.md` / `REPORT_P4_SEAM_BPRIME_20260829.md` / `REPORT_P4_SEAM_ADOPT_20260829.md` |
| 照会 | `QUESTIONS_P4_SEAM_SLIP_20260829.md` / `QUESTIONS_P4_SOURCE_KANJI_20260829.md` |
| 一覧 | `KANJI_SINGLETONS_20260829.md` |
| 裁定（レビュー側） | `REVIEW_P4_A1_DIRT_CANON_VERDICT_20260829.md` / `REVIEW_P4_SEAM_BPRIME_VERDICT_20260829.md` |
| 差し戻し（レビュー側） | `DEV_INSTRUCTIONS_P4_TEST_RED_20260829.md` |

⚠️ ★レビュー側は commit しません。★**開発側が「レビュー側の発行物」として 1 本にまとめて追跡に入れます**
（`4017d77` / `42d12d4` がその形）。★`apps/web/package.json` を巻き込まないこと。

---

## ★8. 未回答の照会

| Q | 内容 | 状態 |
|---|---|---|
| Q-K-1 | 使ってよい漢字の登録簿にするか | ★**今はやらない**（裁定済み） |
| Q-K-2 | 129 種を目で通す | ★**済み**（別字 0 件） |
| Q-K-3 | 恒久の別字対策 | ★**単独の便で**（★未着手・⑦） |

★**それ以外に開いている照会はありません。** ★A-2 は D-089 で閉じました。
