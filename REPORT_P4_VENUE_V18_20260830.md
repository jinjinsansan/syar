# 報告 — ★B案 ① に着手し、★測って止めました（2026-08-30・第3便）

ブランチ `p4/race-30sec-cuts` / 起点 `a83b2dc`
指示: ★オーナー「B案から」→ ★① 競馬場 10 場を帯の内側で作り直す

> ★**結論を先に**: ★① は**着手できません**。★競馬場を 1 つも触っていない状態で、
> ★**本番の走路（`DEFAULT_OVAL`）が既に V-18 ② の天井を越えています**（1200m 12.0 / 1600m 12.2 馬身）。
> ★据え置きのスターパークが帯の外なので、★残り 9 場をどう選んでも 50 鞍は帯に入りません。
> → ★照会 `QUESTIONS_P4_V18_CEILING_20260830.md`。

---

## 1. ★入ったもの

### 1-1. ★`laneAt` が走路の形を受け取るようになりました（★オーナー承認のうえで実施）

⚠️ ★**見つけた食い違い**:

```
laneExtraM(..., spec)   → ovalSegments(distance, spec)          ★venue の走路で積分する
  └ laneAt(..., spec.widthM)                                    ⚠️ ★spec を渡せない（引数が幅だけ）
       └ swingScale(distanceMeter, { ...DEFAULT_OVAL, widthM })  ⚠️ ★1周2000m 固定
```

★`swingScale` は **spec を引数に取る設計**なのに、★`laneAt` の署名に spec が無いため
★**「venue の走路で積分するのに、積分される `w` は 1周2000m 前提」**という形でした。

★ずれの大きさ（`tools/_lanespecgap.mjs`）: ★**0.872 〜 1.490 倍**（月見丘 1200m で 1.490）。

★**今日壊れていなかった理由**: ★`race.ts` は spec を渡していないので `spec === DEFAULT_OVAL`。
⚠️ ★**B案 ② で spec を通した瞬間に食い違いになります。** → ★先に直しました。

| ★確認 | |
|---|---|
| ★既定は `DEFAULT_OVAL` | ★spec を渡さない呼び出しは**従来と完全に同一** |
| ★`_venuelane.mjs` の `DEFAULT_OVAL` 行 | ★5 距離とも**変更前と 1 桁まで一致**（10.60 / 9.98 / 11.54 / 11.78 / 10.73） |
| ★`npm test` | ★**133 ファイル / 1336 件 PASS / EXIT=0** |
| ★`npm run typecheck` | ★EXIT=0 |

★**効いたこと**: ★`swingScale` が本来の仕事（距離によらず一定）をするようになり、
★競馬場ごとの**距離間のばらつきが縮みました**:

| | 直す前 | 直した後 |
|---|---|---|
| 天河 | 7.10〜11.95（幅 **4.85**） | 10.58〜12.60（幅 **2.02**） |
| 大河原 | 7.26〜12.11（4.85） | 11.33〜13.34（**2.01**） |
| 月見丘 | 9.28〜16.17（6.89） | 13.56〜16.38（**2.82**） |

→ ★**水準が競馬場の形でほぼ決まる**ようになりました（★①の設計が扱える形になった）。

⚠️ ★**ただし水準は上がりました**（帯の外 15 → 22 鞍・5 seed 基準）。
★私は事前に「直せば帯に入れやすくなる見込み」と書きました。★**それは外れました。**

### 1-2. ★競馬場の形を通したときの守り（`apps/cli/test/venue-course.test.ts` に 2 件追加）

⚠️ ★`packages/race-engine/test/lane-geometry.test.ts` は ★**`DEFAULT_OVAL` しか突き合わせていません。**
★B案 ② で形を通した瞬間、★**そこに守りが 1 つも無くなります**（走路の幾何は 2 か所にあります）。

| 検査 | 何を守るか |
|---|---|
| ★10 場 × 実際の距離で区間が一致する | ★`ovalSegments` と `ovalCourse`（引き込み線 250m も含む） |
| ★10 場で距離ロスが一致する | ★`laneExtraM` と `laneExtraMeters` |

★**変異試験で効くことを確認**: ★`laneExtraM` から `spec` を外すと**落ちます**（1 件 FAIL / 5 件 PASS）。
★戻したあと、★バックアップとの `diff` で**内容が完全一致**することを確かめています。

---

## 2. ★止めた理由（★測定）

### 2-1. ★正典の検定を回したら、本番が落ちていました

★`tools/verify-v18.mjs` は**既にあった正典の検定**です（★`resolveRace` で実着順を出し、2000 レースをプール）。

```
  距離   ①枠順と着順の相関   ②内外差（馬身）        判定
  1200m      +0.069  ○       12.0 馬身（28.8m） ★×     ★FAIL
  1600m      +0.049  ○       12.2 馬身（29.4m） ★×     ★FAIL
  2000m      +0.042  ○       11.8 馬身（28.4m） ○     PASS
  2400m      +0.015  ○       11.9 馬身（28.6m） ○     PASS
```

★**① は通っています。② だけが天井を越えています。** ★2000/2400 も天井まで 0.1〜0.2 馬身です。

⚠️ ★**私の変更が原因ではありません** — ★`verify-v18.mjs` は `laneExtraM(gate, FIELD, dist, seed)` を
★**spec 無し**で呼ぶので `DEFAULT_OVAL`、★そこは 1-1 の表のとおり変更前と一致しています。

### 2-2. ★原因は `LANE_REVEAL_FULL_RUN`（★製品コードを触らずに測りました）

★`laneExtraM` は掃引用に `revealFullRun` を公開しているので、★引数を振るだけで測れます。

```
  reveal   1200m   1600m   2000m   2400m
  1.00      4.77    6.99    5.93    5.91  ○   ← ★2026-08-21 以前
  0.50     10.10   12.00    9.24    9.38  ○
  0.30     11.81   12.23   10.74   11.30  ★×
  0.18     12.01   12.23   11.84   11.91  ★×  ← ★本番
```

⚠️ ★`lane.ts` は 0.18 を入れたとき **「+74% 増えるため V-4・V-17 は動きます」**と書き残しました。
★**V-18 はその一覧に入っていません。** ★挙げられなかったので、★**測られないまま越えました。**
★D-082 と同じ形です（★出していない列で落ちる）。

⚠️ ★**「0.18 を戻せ」とは言いません。** ★0.18 は中盤の横の広がり（2.13m→8.19m）を作っており、
★裁定が「V-17 が外れても 0.18 を否定する理由にならない」と閉じています。★選択肢は照会に出しました。

### 2-3. ⚠️ ★測り方が間違っていました（★2 か所）

#### ★① 5 seed では足りません

| 1600m・`DEFAULT_OVAL` | |
|---|---|
| ★`_venuelane.mjs`（5 seed・前便の道具） | **9.98 馬身** |
| ★`verify-v18.mjs`（2000 レース） | **12.23 馬身** |

→ ★**約 2 割低く出ます。** ★前便の「15 鞍が帯の外」も、★私が途中まで使っていた数字も、
★すべてこの標本の上に乗っていました。★200 seed で取り直すと ★**43 鞍**でした。

#### ★② 1 レースごとに相関を出して平均してはいけません

★私は最初、★12 頭ぶんの ρ を 1 レースずつ出して平均し、★**50/50 鞍が「①違反」**という結果を出しました。
⚠️ ★12 点では**偶然の相関**（|ρ| の期待値がおよそ 0.25）がそのまま残ります。
★**測っていたのはエンジンではなく、測り方でした。**

→ ★`verify-v18.mjs` に**既にある** `spearman` と同じ形（★全レースぶんをプールしてから 1 回）に直しました。
★直したあとは ★**|ρ| 最大 0.071 / 50 鞍とも許容内**で、★正典の検定（+0.015〜+0.069）と噛み合いました。

⚠️ ★`verify-v18.mjs` は**同じ罠を 2 回踏んだ記録**を自分の中に残しています
（★「道具が作った相関でした」「★測っていたのは私のシャッフルでした」）。★**3 回目でした。**

---

## 3. ★測って分かったこと（★①が再開できるときのために）

| | |
|---|---|
| ★**帯の判定は「実際に組まれている距離」で行うこと** | ★5 本の掃引列（1200/1600/2000/2400/3000）では、★銀嶺が**偽陽性**になり（`!` の付いた 2400m は銀嶺に無い距離）、★逆に 1800/1900/2500 を**見落とします** |
| ★**半径は帯の内外を決めません** | ★引継ぎ書の「半径 165〜215m」は 1.30 倍。⚠️ ★`venue-course.test.ts` は**半径 1.5 倍以上**を要求するので、★**その帯では検査に落ちます** |
| ★**スターパークが 191m で固定** | → ★1.5 倍を満たすには **127m 未満**（深い＝ロス増）か **287m 超**（大きい＝ロス減）。★上へ伸ばすのが素直です |
| ★**形の探索は動いています** | ★`tools/_venuepick.mjs`。★5 seed の下見では、★検査を満たしたまま 10 場を帯に入れる組み合わせが出ました（⚠️ ★5 seed なので**採用していません**） |

---

## 4. ★この便で足した道具（★すべて読取専用・登録簿に登録済み）

| 道具 | |
|---|---|
| `_venuefit.mjs` | ★**実際に組まれている距離**で、帯に入る形を掃引する |
| `_venuepick.mjs` | ★10 場を、V-18 ② と散らばりの検査の**両方**を満たす形へ選ぶ |
| `_venueverify.mjs` | ★いまの形で 50 鞍を検定（★**200 seed・相関はプール**） |
| `_lanespecgap.mjs` | ★`laneAt` が spec を受け取れないことで生じる倍率のずれ |
| `_revealspread.mjs` | ★② が越えた原因が `LANE_REVEAL_FULL_RUN` にあることを測る |
| `_lanebench.mjs` | ★`laneExtraM` の呼び出し速度（★総当りで間に合うかの見積り） |

---

## 5. ★再実行（★すべて読取専用）

```powershell
cd V:\dev\Cusor\star
npx tsx tools/verify-v18.mjs          # ★正典の検定。★いまは ★FAIL（1200m/1600m）
npx tsx tools/_revealspread.mjs       # ★原因（reveal を振るだけ・製品コードは触らない）
npx tsx tools/_venueverify.mjs        # ★50 鞍の検定（200 seed）
npm test ; npm run typecheck          # ★133 ファイル / 1336 件 / EXIT=0
```

---

## 6. ★開いている照会

★`QUESTIONS_P4_V18_CEILING_20260830.md` — ★Q-1（★帯を越えたまま進めるか・A〜D）が**最優先**。
★それが決まるまで ① は動かせません。
