# P0-fix3 レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-04（レビュー側 Claude）
**対象**: `REVIEW_REQUEST_P0FIX3_20260804.md` / `REPORT_P0FIX3_20260804.md`（commit `c885ecf`）
**判定**: **PASS** — 合格基準12項目すべて成立。**Q1 は (b) 採用**（切断補正を導出式へ）で P0 を締める

---

## 0. 結論

| 項目 | 判定 |
|---|---|
| `npm run verify` の出力 | **CONFIRMED** — 報告書 §3 と全数値一致（所要292秒 / 報告170.8秒はマシン差） |
| V-2e の形質別表 | **CONFIRMED** — 独立実行で §3-2 と一致 |
| ユニットテスト82件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| H-1 大物覚醒の限定 | **CONFIRMED** |
| H-2 clamp の復帰＋式の固定 | **CONFIRMED** |
| H-3 非能力形質の sd 導出 | **CONFIRMED** |
| H-4 V-2e の一般化 | **CONFIRMED** |
| 自己申告（`silenceMutation()`） | **承認**（§3） |
| **Q1 V-2e が下限寄り** | **(b) を採用 — ただし開発側の診断は一部誤り**（§2） |

**P0 の遺伝エンジンは、Q1 の補正1件を残して完成した。**

---

## 1. レビュー側が実際に実行したもの

```bash
npm test              # 82件 PASS
npm run typecheck     # exit 0
npm run verify        # 報告書 §3 と全数値一致
npm run simulate -- --generations 300 --seed 42 --population 800 --stallion-pool 240   # §3-2 と一致
```

加えて clamp 切断効果を解析的に計算し（§2）、並列サブエージェントで `git diff 2350953 9f4f970` を file:line 監査した。

---

## 2. Q1 — (b) を採用。ただし「原因は遺伝的浮動」という診断は正しくない

### レビュー側の独立計算

clamp は 1.67σ での**打ち切り**（境界に張り付ける）なので、クランプ後のノイズ分散は解析的に求まる。X ~ N(0,σ²)、Y = clamp(X, ±a)、a = 1.67σ のとき:

```
E[Y²]/σ² = [ (2Φ(a')−1) − 2a'φ(a') ] + a'² · 2(1−Φ(a'))   （a' = 1.67）
         = 0.575 + 0.265 = 0.840
∴ 実効SD = 0.9156 × sd
```

**したがって平衡SDは創始の 0.916倍**になる。これは正典 §6.4 が既に記載していた「約0.92倍」と一致し、**実測の平均0.91ともほぼ一致する**。

| 形質 | 実測（300年 seed42） |
|---|---|
| 距離幅 0.96 / 丈夫さ 0.94 / 距離中心 0.93 / 芝 0.92 / ダート 0.87 / **気性 0.84** | 平均 **0.91** |

つまり **主因は clamp の切断であり、遺伝的浮動ではない**。浮動の寄与は桁違いに小さい（Ne≈738 で1世代あたり分散の 0.068% に対し、回帰は毎世代 36% を除去する）。気性の 0.84 は理論値 0.916 からの残差で、形質間のばらつき（±0.06）の範囲内にある。

### 判断: (b) 採用 — 切断補正を導出式に折り込む

正典 §5.2/§6.4 が掲げる目標は「**創始水準への平衡**」である。切断効果は既知で定量可能なのだから、**それを見込んだうえで 1.0倍を狙うのが筋**であり、0.92倍を目標にする理由がない。

```
sd = 創始アレルSD × √(2r − r²) ÷ 0.916
```

導出される新しい値（clamp は sd の 1.67倍のまま）:

| 形質 | 現行 sd / clamp | **補正後 sd / clamp** |
|---|---|---|
| 丈夫さ | 60 / ±100 | **65 / ±109** |
| 気性 | 9 / ±15 | **10 / ±16** |
| 芝/ダート適性 | 12 / ±20 | **13 / ±22** |
| distance_center | 312 / ±520 | **341 / ±568** |
| distance_range | 104 / ±173 | **114 / ±189** |

これにより V-2e の実測は 0.84〜0.96 から **0.92〜1.05 程度**へ移り、下限0.8までの余裕が 0.04 → 0.12以上に増える。**P1 で選抜圧が加わる前に余裕を確保しておく**意味がある。

(c)（ゲートの下限を見直す）は採らない。**実測に合わせて基準を緩めるのは、基準を持つ意味を失わせる**（正典 §13.3）。

---

## 3. H-1〜H-4 と自己申告への回答

| 項目 | 判定 |
|---|---|
| **H-1** 大物覚醒の限定 | **承認**。「発動率100%でも非能力形質は0件」「同条件で通常の隔世遺伝は11形質すべてで発動」という**対照テスト**の作り方が良い。V-3 の分母が 5/11 に縮む件も正しく記録されている |
| **H-2** clamp の復帰 | **承認**。撤回の根拠（平衡式が AR(1) を前提とする以上、合計 clamp は導出根拠を壊す）を正しく受け止めている。`mutateAllele()` を純関数に切り出し、**両形式が異なる値を返す入力**（値900・ノイズ+200 → 960 vs 1010 等）で固定した対処は的確。「ノイズが clamp 内なら同値」を別テストで明示し、**なぜ従来のテストで検出できなかったか**をコードに残した点は特に良い |
| **H-3** sd の導出 | **承認**。`FOUNDERS` からの導出で創始定義に自動追随する形になっている |
| **H-4** V-2e の一般化 | **承認**。芝適性 0.71倍 → 0.92倍 は指示の狙いどおり |
| **自己申告** `silenceMutation()` | **承認**。「`MUTATION_SD=0` が非能力形質を止めなくなった」= **前便で指摘したトートロジーの裏返し**であり、自分でそう認識して申告した点を評価する。ヘルパへ集約し docstring に理由を残した対処も妥当 |

### V-2d の改善（合格基準10）

ラチェットを断ったことで**乖離の向きが上方から中立へ変わった**のが本質的な成果である。P0-fix2 では6形質すべてが上方向にずれていた（ラチェットの署名）が、P0-fix3 では ±符号が混在し絶対値も縮小した:

| 形質 | P0-fix2 | P0-fix3 |
|---|---|---|
| surface.turf | +7.22% | **-4.26%** |
| temper | +3.88% | **-1.53%** |
| distance_center | +2.18% | **-0.78%** |
| distance_range | +2.17% | **+1.52%** |

**押し返すのでなく発生源を断つ**という D-011 の狙いが数値で確認できた。

---

## 4. Q2・Q3 への回答

- **Q2（大物覚醒の演出が能力覚醒時のみになる）**: 認識のとおりで問題ない。P4（UI）への申し送りとして正典に残す。演出文言は「（能力名）の血が目覚めた！」のように能力を明示する方向が自然
- **Q3（P1 引き継ぎ）**: 本報告の実測値（`distance_range` +1.52% / V-2e 0.96倍）を新しい基準線とする点に同意。**ただし Q1(b) 適用後の値が最終的な基準線**になるので、P0-fix4 完了後の数値を使うこと

---

## 5. 教訓の記録

1. **既知の系統誤差は「見込む」だけでなく「補正する」**。切断効果0.916倍は正典に書いてあったが、ゲート幅で吸収する形にしていた。定量できる誤差は導出式で打ち消すほうが、余裕を他のリスクに回せる
2. **原因の診断は数式で裏を取る**。開発側は 0.84 を遺伝的浮動と見立てたが、浮動の寄与は回帰の 1/500 で説明にならない。切断効果を解析的に計算して初めて「理論0.916 vs 実測平均0.91」の一致が見えた
3. 対照テスト（「限定したものは発動しない」＋「限定していないものは発動する」）は、片側だけのテストより退行検出力が高い。H-1 のテスト設計を以後の標準とする

---

## 6. 【追補】コード監査の結果

差分監査（`git diff 2350953 9f4f970`）で H-1〜H-4 の実装をすべて追認した（定数外出し・ゲート位置・テストのリテラル固定・V-3 分母の移動・`silenceMutation()` がテストを無力化していないこと）。`npm run verify` も監査側の環境で報告書と行単位一致。

### 6-1. 🔴 H-2 の残穴 — 固定範囲が純関数の内部だけだった（I-3）

監査で実際にコードを書き換えて2つの実験を行った（いずれも復元済み）:

| 実験 | 結果 |
|---|---|
| A: `mutateAllele()` 本体を旧「合計 clamp」形に書き換え | `regression.test.ts:291` が FAIL — **式を固定するテストは機能している** |
| B: `mutateAllele()` は正しいまま、**呼び出し側**（`genetics.ts:238-241`）に旧形式をインラインで書き戻し | **82件すべて PASS** |

つまり「旧形式に戻すと確実に落ちる」のは `mutateAllele` を編集した場合に限られ、**本番経路が純関数を正しく使っているかは固定されていない**。合格基準11 は満たしているが、P0-fix2 で見つけた「テストが実質何も守っていない」構造が**もう一段外側に残っている**。I-3 で `breed()` 経由の固定を指示した。

### 6-2. 🔴 CLI の上書き経路が sd 導出をバイパスする（I-4）

`TRAIT_MUTATION` はモジュール読み込み時に `REGRESSION_RATE`・`FOUNDERS` から1度だけ構築されるが、`--set-balance REGRESSION_RATE=…` / `--set-founders …_SD=…` / `sweep.ts` はいずれも再構築しない。

**これは P0-fix の誤結論（「回帰0.20 では距離SDが0.4倍に収縮する」）を生んだのとまったく同じ罠である。** 既定値と `verify` は正しいので P0 の合否には影響しないが、**P1 で回帰率や創始定義を触れば必ず再発する**。I-4 で是正を指示した。

### 6-3. 🔴 正典側の編集残り3件 — レビュー側の宿題（是正済み）

監査が指摘したとおり、私の編集に不整合があった:

- §6.4 に**撤回済みの「合計に clamp」の式が残ったまま**、直後に訂正ブロックが続く矛盾状態だった
- §6.4 の「他形質は値域比で縮尺する」が D-012 と矛盾していた
- **§13.1 の `TRAIT_MUTATION` で durability/temper/surface の4キーが重複定義**されていた（旧 center のみの行を消し忘れ）

いずれも**是正済み**。TypeScript なら後勝ちでコードと一致するが、正典の定数ブロックとしては危険な状態だった。

### 6-4. 軽微

- 導出テストの一部（`regression.test.ts:190-193`）が本番と同型の式での比較＝単独ではトートロジー。ただし同ファイルにリテラル固定があるため実害なし。**ただし `FOUNDERS.*_SD` を変えたら sd が追随することの固定は存在しない**（center 側にはある）→ I-5
- `verify.ts` の `distRatios` は各シードの最悪SD比を集めた配列だが、変数名が旧 `dist`（距離）のままで平均行が何の平均か読み取りにくい

### 6-5. 教訓（追加）

4. **「テストが守っている範囲」は、テストが触る層で決まる**。純関数を固定しても、呼び出し側が純関数を使わなくなれば素通りする。単体固定と経路固定は別物
5. **導出をやめる経路がないか探す**。定数を導出で守っても、上書きAPIが導出を回避すれば同じ事故が起きる（I-4）。「この値はどこから来るか」を経路ごとに数える
