# レビュー回答 — 交通流・表示時間・個体タイプ（2026-09-09）

対象: `3893bcc` / `2b4a3a8`。依頼: `REVIEW_REQUEST_P4_TRAFFIC_TYPES_20260909.md`。
レビュー側: Codex。製品コード・既存テスト・正典は変更していない。

## 1. 判定

**修正後に再レビュー。数値監査の再現は確認したが、現状を全体合格とはしない。**

今回の交通流実装には過去のCodex実装が含まれる。実行はこのレビューでやり直したが、作者から独立した第三者レビューと同義ではない。

- Q-1: 正規表現だけの拡張では不足。経路一致と表示時間ポリシーの振る舞いを別に検査する。旧目標尺への一致を新版の保証として扱わない。
- Q-2: 共有ツリーの作法を明文化する案を採用。ただし提示の3行に、共有ファイルと失敗テストの扱いを追加する。
- Q-3: 今回はgenotypeに追加しない。枠番のA/B割当は映像デモとして限定し、個体に固定する装飾方式を次の実装案として推奨する。繁殖による継承は別の仕様判断にする。

## 2. 実コードで確認した問題

### F-1: 目標尺のテストが新版の既定を検査していない（修正必須）

`apps/web/src/app/race/page.tsx:1254` は既定で `readableRaceRates` を使う。`packages/render/test/time-target.test.ts:39` の `displayOf` は `ratesForTarget` を直接使う。従って「どの距離でも、目標の表示時間にほぼ収まる」が成功しても、新版の既定に関する証拠にならない。

`apps/cli/test/display-rate-path.test.ts:57` の正規表現失敗自体は呼出構文の変化だが、上記の検査範囲のずれも実在する。関数名を許可リストへ足すだけでは解消しない。また旧メタテストはソース内の文字列の存在を見るだけで、元から目標秒数の達成を証明してはいない。

### F-2: 監査と画面の直線長が会場によって一致しない（経路一致の修正対象）

`tools/lib/race-audit-build.mjs:120` は `straightMetersLeft: 400`。画面は `homeStretchMetersOf(course)` を渡す。既存の引継ぎにも記載された制限であり、今回新たに発生した問題とは扱わない。

今回の9条件は既定走路に限るので数値の再現は有効。ただし、共通の時計関数を呼ぶだけでは会場ごとの同一経路の証明にならない。時計に入るknots・直線長・距離・自馬枠・モードも一致させる必要がある。全会場への合格は出さない。

## 3. Q-1への回答と開発側への修正条件

### Q-1a: 経路と振る舞いを分けて検査する

1. 画面と監査で使う表示時間ポリシーの選択を共有関数へ寄せる。既定は可読性優先、`motion=legacy` は従来方式。メタテストは両方の利用箇所がこの経路に接続することを確認する。コメントや未使用の関数呼出を足すだけで成功する検査にはしない。
2. 旧 `ratesForTarget` の目標尺テストは削除せず、従来方式を検証する名前・説明にする。
3. 新版には、実際のknotsを使って時間写像が有限・単調、終点が最終ゴール時刻、各区間の速度上限が2倍、発走・ゴールの等速区間を維持することを検査する。既存の交通流テストにある速度上限の検査は活用する。
4. 目標が実現可能な場合は目標変更が時計に反映されること、不可能な場合は上限を超えて圧縮せず、目標と実尺の差を監査結果に残すことを検査する。常時2倍を返す実装や、目標引数を無視する実装を見逃さない。
5. 旧／新の本編尺、目標、超過秒、速度上限による制限の有無を出力する。イントロ・勝馬紹介・結果表示・リプレイ込みの総尺も、本編とは別に測る。再生速度の調整卓を使った時間とも区別する。
6. F-2の直線長を画面と揃え、直線長の異なる会場を含めて入力・時計の一致を確認する。旧方式に戻せることも検査する。

実測値をそのまま期待値へコピーするだけでは、意図の検査にならない。今回の65〜66秒などは現状の測定値であり、承認済みの許容帯ではない。

### Q-1b: 正典の適用先

§13.2のV-17はエンジンの走破タイムと着差であり、画面上の65秒を93〜101秒と比較するものではない。V-18も描画尺の判定ではない。V-16は可読性に関係するが、最大表示時間や離脱しにくさを直接測ってはいない。

従って、**今回の尺延長を直接判定する既存の数値ゲートは確認できない**。ただし既存の時間配分資料とコードには目標尺の意図があるので、「V番号が無いから延ばして合格」ともしない。`RACE_PRESENTATION_BASICS.md` §4と `targetDisplaySec` の注記には異なる時点の目標値が残る。古い45秒や30秒を現在の確定値として再採用することもしない。

レビュー側の判定項目として、次を置く。

- 技術条件: 上記の経路一致・時間写像・速度制限・終点維持をCIで検査する。
- 商品としての条件: 距離別の本編尺と総尺を提示し、速度上限と長い尺の組合せについてオーナーが見た目を判定する。
- 許容尺が決まった後、その値・測定範囲・優先順位を時間配分資料と必要な正典箇所に反映し、既定経路を使うCIへ入れる。

今回は数値の許容帯やV番号を捏造しない。全編2倍以下という条件と短い固定尺が両立しない場合、テスト変更では解けない。演出構成か尺の方針を選ぶ必要がある。見た目の評価は依頼書どおり未取得として残す。

## 4. Q-2への回答

`CLAUDE.md` に共有ツリーの作法を置くことには賛成。正典のR番号は必須ではない。今回はレビュー文書に採用文案を確定し、既存の作業ファイルへは書き込まない。

> 同一作業ツリーを複数セッションで使う場合、`git stash`、他作業を巻き込む復元・破棄操作をしない。`git add -A` / `git commit -a` を使わず、対象ファイルを名指しする。共有ファイルはファイル指定だけでは混入を防げないので、差分を確認し、混在したまま記録する場合はその範囲を明示する。行数は比較元・比較先を付けた `git diff --stat` / `--numstat` で測り、その数字だけで作者別の行数と断定しない。開始・終了時のHEAD、差分、担当範囲、検証結果を記録する。失敗したテストは作者を理由に対象外にせず、引継ぎ先と再確認条件を決める。

今回の分割は最終の2コミットを組としてレビューする。中間コミット単独の型チェック・動作検証は今回行っていないため、「途中も壊れていない」ことまで承認したとは書かない。

## 5. Q-3への回答

### Q-3a / Q-3b

現時点ではA/Bは描画素材の選択であり、遺伝する形質という承認済み仕様は見つからない。今回の描画修正を理由にgenotypeへ追加することは認めない。P1.5が再開中でも、既存の血統データ、プリシード、乱数列への影響を無視して変更できる意味にはならない。

繁殖で見た目を継ぐ機能を望む場合は、継承規則、既存馬への割当、データ移行、乱数ストリーム分離、再プリシードと再検証の必要性を別途設計する。このレビューだけで遺伝機能の採否を決めない。

### Q-3c

**推奨は安定した馬IDを入力にした、バージョン付きの装飾割当。毛色や枠番は入力にしない。** 同じ馬が別の枠・別の端末・別のレースでも同じ型になることを優先する。

例として、専用の名前空間と固定したハッシュ・対応表から `appearance-v1` のA/Bを導ける。ハッシュ方式と対応表は仕様化し、能力や配合に使うRNGを消費しない。将来Cを追加するときに単純な `% 型数` の変更で既存馬の型が一斉に変わらないよう、既存バージョンを維持するか、割当を保存する。

注意: 現在の `/race` の `build()` は `horseId: String(i + 1)` を生成しており、この値も枠位置と連動する。ここをハッシュするだけでは個体固定にならない。実データ側の永続ID、またはデモプール内で安定したIDが必要。

この方式は実装案としての推奨であり、既に正典で確定した仕様とは扱わない。現状の枠番表は映像デモとして保持できるが、「馬ごとに固定された外見」「繁殖で継ぐ個性」が完成したとは報告しない。型Cの描き直しも今回の対象外。

## 6. 再実行した検証

対象HEAD: `2b4a3a8`。開始時の追跡済みファイルに差分なし。未追跡は `.claude/` と依頼書であり、依頼書の測定時点の「ツリーが空」と現在は区別する。

- `npm.cmd run typecheck`: exit 0。
- `npm.cmd test`: exit 1。141ファイル中140成功・1失敗、1433件中1432成功・1失敗。19:37:55開始、264.45秒。失敗は `apps/cli/test/display-rate-path.test.ts` の「Web画面は目標時間から送り速さを逆算している」で、依頼書と一致。
- `node --import tsx tools/audit-traffic-motion.mjs`: exit 0。9条件×旧新版の18行を再生成。
- `git diff --check`: exit 0。

| 距離 | 旧本編尺（秒） | 新本編尺（秒） |
|---|---:|---:|
| 1200m | 35.2678〜35.8255 | 52.2715〜52.9492 |
| 1600m | 38.7289〜39.5095 | 65.4414〜66.3937 |
| 2400m | 46.1482〜47.9699 | 93.0680〜94.6161 |

seedは42/99/14。新版の接近ペア・フレーム数は全9条件で0、最大横速度は約1.30m/s、旧新版とも最終着順一致。浮動小数点の誤差で最大値は1.300000000000603。

接近判定は前後2.4m未満かつ横1m未満の地面上の位置。馬体の画面上の遮蔽、見た目、端末でのfps、全シード・全会場は保証しない。表示横位置の補正とエンジンの距離ロス積分が完全一致しない制限も残る。エンジン着順が一致することだけで、この制限が解消したとはしない。

成果物: `out/traffic-motion/audit.json`、`tmp/review-traffic-types-tests.log`。ブラウザの再撮影・本番接続・デプロイは実施していない。「本番へ」は依頼書の表現であり、リモート稼働版への配備を確認した意味ではない。
