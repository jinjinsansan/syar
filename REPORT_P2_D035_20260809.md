# REPORT_P2_D035 — D-035 で V-10 が全7券種 PASS（本番コード経路で実証）

**開発側 → レビュー側** / 2026-08-09
**コミット**: `9f5d88d` 以降（push 済み・443 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**★あり（§2・§4）。どちらも私は決めていません。本番へも入れていません。**

---

## 1. ★D-035 は設計どおりです。二通りで確かめました

### ① 二項分布の厳密計算（乱数なし）

```
M = 3,896,104（設計式 λ*×ODDS_CAP/(1−margin) の最大値）

  win -0.00  place -0.00  ワイド -0.00  馬連 -0.01
  馬単 -0.01  三連複 -0.03  三連単 -0.24        すべて PASS
```

設計式も裏が取れました。**M=389,610 では三連複 −0.28 PASS に対し三連単 −2.22 FAIL** で、
**三連単が律速**というご指摘のとおりです。

### ★② 本番のコード経路（`apps/worker/src/odds.ts` の `buildOddsRows`）

**計算だけでは「式が正しい」までしか言えません。**実装が同じことをしているかは別です。

```
seed=42 / 8レース / 各レースで確定を 100,000回 / M=3,896,104

  win            82.24%  +0.24pt  PASS      馬単     80.16%  +0.16pt  PASS
  place          82.08%  +0.08pt  PASS      三連複   77.16%  +0.16pt  PASS
  ワイド         80.15%  +0.15pt  PASS      三連単   77.05%  +0.05pt  PASS
  馬連           80.22%  +0.22pt  PASS

  ★上限に当たった売り目: 0 件（D-035 が効いている証拠）
  ★V-10 総合: PASS
```

**解析予測（−0.00〜−0.24pt）と実測（+0.05〜+0.24pt）が ±0.25pt 以内で一致**します。
モデルが実コードで裏付けられました。

### ★なぜ「10万レース」で測っていないのか

**M=3,896,104 では §13.2 の10万レース測定が 3.9×10¹¹ 回のレース解決になり、
本番機で約44日かかります。レース数を増やす方向では回りません。**

代わりに**1レースにつきオッズを1回作り、独立系列から確定を10万回引きました**。
測っている量（オッズ推定の誤差）は変わらず、**分散だけが下がります**。
**§13.2 の「10万レース」は D-035 の下では実行不能なので、測定条件の書き換えが要ります。**

---

## 2. ★判断① — §9.2 の M=10,000 では足りません

必要な試行数は**上限から決まる**ので、実装には**数値リテラルを置きませんでした**。

```ts
export const ODDS_MC_TRIALS = requiredOddsTrials();  // = 3,896,104
```

上限だけ動かしたときに静かに不足するのを防ぐためです。
**§9.2 の 10,000 は改訂が要ります**（そのままだと三連単 −20.90pt）。

`LAMBDA_STAR = 30` は較正定数として登録簿に入れ、変異試験の対象にしました。

---

## 3. 実行時間 — ★本番機で測りました。収まります

```
                        resolveRace のみ   全7券種の簿記こみ
  開発機                    13.4 μs            22.4 μs
  ★本番機（VPS 4vCPU）      19.5 μs            35.4 μs   ← こちらで判断

  M=3,896,104:  1レース 137.7秒   1周2レース 275.4秒   10分サイクルの 46%
```

★ご想定の 2μs より **17.7倍**重い値でしたが、**それでも収まります**（余裕は 8秒ではなく 325秒）。
**上限の引き下げをオーナー判断に上げる必要はありません。**

確認したこと:
- ★**モンテカルロは DB トランザクションの外**で走ります（`raceExists` で先に弾き、
  `build(idx)` は `begin` の前に評価される）。137秒のトランザクションにはなりません
- ロックは `pg_try_advisory_lock` なので、二重起動しても**待たずに空振り**します

★申し送り: 1周の所要が **1秒未満 → 最大275秒**に変わります。`TICK_MS=60秒` を超えるので、
周は連続実行になります（`setTimeout` の下限で保護済み）。**A-1 の再計測が要ります。**

---

## 4. ★判断② — 買えなくなる目が出ます（製品仕様の変更）

```
  券種        参照で観測   売られる   売られない
  win           11.0       11.0       0.0%
  place         11.0       10.9       1.1%
  ワイド        59.4       50.8      14.4%
  馬連          59.4       53.3      10.3%
  馬単         118.8      106.5      10.3%
  三連複       210.3      176.3      16.2%
  三連単      1253.5     1101.9      12.1%
```

★**単勝は1点も減りません。**影響は目の多い券種の 10〜16% です。

そちらの整理どおり、**現状は買えて、当たっても黙って切り詰められる状態**でした。
**払えない配当を売らないほうが客に有利**という判断に同意します。
ただし**製品の見え方が変わる**ので、オーナー承認をいただく事項だと考えます。

---

## 5. 7分停止の再発防止（★本番の配備ではまだ一度も実行していません）

`tools/deploy.sh` を書きました。要点は**順序**です。

★**稼働中の Node は既にコードを読み終えている**ので、ファイルを置き換えても
  走っているプロセスは壊れません。**壊れるのは restart した瞬間だけ**です。
  → 「置き換える → 検証する → 通ったら restart」にすれば、
    検証で落ちたときは **restart しないので停止時間がゼロ**になります。

検証は**本番機で両側**を確かめました（R-2）:

```
  健全な木  → 終了コード 0
  壊れた木  → 終了コード 1  理由 "Cannot find module './does-not-exist.js'"
```

### ★私の検査が2回、間違った理由で「成功」に見えていました

最初 `tsx -e "await import(...)"` と書きました。**`-e` は CJS 評価なので
top-level await が使えず、健全な木でも必ず落ちます**（偽陰性）。
そして「壊れた木なら落ちる」ことを確かめたつもりの検査**も同じ理由で落ちていた**ので、
**検査が効いているように見えていました。**

2度目は、ローカル（Windows）で `/tmp/...` が `V:\tmp\...` に変換され、
**対象ファイルが存在しないから**落ちていました。これも「検出できた」と読みかけました。

★**「落ちた」は「検出できた」ではありません。理由を読むまでは同じに見えます。**

### 既知の弱点（隠しません）

`npm ci` は node_modules を作り直すので、**稼働中のプロセスが遅延読み込みするものがあると
壊れえます**。完全に外すにはリリースごとの別ディレクトリ＋symlink 張り替えが要ります。
§14 にはそちらを書いていただくのが正しく、これは**その前段の暫定**です。

---

## 6. 本番に入れていないもの

| | 状態 |
|---|---|
| §10.2 開催中止・§9.1 返還 | **配備済み**（`dcd2373`） |
| D-013 のバイアス補正 | **未配備** |
| D-035 の発売下限・M=3,896,104 | **未配備** |
| `tools/deploy.sh` | **未設置** |

★**§4 のオーナー承認と、§2 の §9.2 改訂をいただいてから配備します。**

---

## 7. A-1（判定は本日 20:53 以降）

```
生成した周 90 / 確定した周 86 / 周の欠落なし
メモリ 62MB（変化なし）/ FD 26（変化なし）/ DB接続 1
```

★ただし §3 のとおり、**M を上げると1周の所要が 275秒になります**。
  A-1 は現行の M=10,000 で測った値なので、**配備後に測り直す必要があります。**

---

## 8. 合格基準

| # | 状態 |
|---|---|
| A-1 | **計測中**（★M 変更後に再計測が要る） |
| A-2 | **PASS** |
| **A-3** | **★実装は PASS**（全7券種・本番コード経路）。配備は §2/§4 の判断待ち |
| A-4〜A-9 | **PASS**（443 tests / typecheck 0） |
