# 【STAR】裁定: 移行 `0077`（初回の母の候補を読む口）と、画面と DB の出す順 — 2026-09-24

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa` の事前連絡（★裁定 `REVIEW_HORSE_IDENTITY_VERDICT_20260923.md` §5 ③ の「当てる前に 1 行」が**守られた**）

## 1. `0077` — ✅ 当ててよい。条件 2 つ

- ✔ 中身は**読む関数 1 つ ＋ `grant`** だけ（既存の表・関数・データを変えない）。★staging の実演 11/11（読むだけ）
- ✔ ★**事前に知らせてきた**（§5 ③ が働いた・初回）

**条件**:
1. 🔴 **同じ述語が 3 か所にある**。★`apps/worker/src/player-breeding.ts:163-169`（ワーカー）と、★`0077` の**中に 2 回**（数える側 `:74-77` と一覧を返す側 `:82-85`）
   - ★まず ★**`0077` の中の 2 回を 1 つにする**（CTE か副問い合わせ）。★同じファイルの中の写しは、★**次に条件が増えた日にどちらかだけ直る**
   - ★ワーカー側との重複は、SQL と TS なので 1 つにできない。→ ★**実演で「RPC の一覧の件数」と「ワーカーの述語で数えた件数」が一致することを見る**（★staging の 11 件に入っていなければ足す）。★両方の註記に相互参照を書く
2. ★当てた後の読むだけの確認（関数が在るか・候補の数）を報告する（照会どおり）

## 2. 🔴 画面を先に、DB の口を後に出した — ★順の決めを足す

- ✔ 起きたこと: `/stable/foal` を `main` に出した（`60d071d`）が、★**`0077` が当たっていないので本番で「候補を読み込めませんでした」**になる。★リンクはどこにも張っていないので、URL を直接打った人しか到達しない
- ★**害は小さい**が、★**順が逆**。★配備の手順書は「移行 → ワーカー → push」の順だったのに、★**画面だけ先に出る経路**（`main` への push）があった
- **決定（手順書に足す）**: ★**画面を本番に出す前に、その画面が呼ぶ DB の口（関数・ビュー）が本番に在るかを確かめる**。★無ければ、①移行を先に当てる ②または**その画面を出さない**
  - ★確かめ方は機械で: ★画面が呼ぶ RPC 名を原文から拾い、★本番の `pg_proc` に在るかを読むだけで突き合わせる道具（★`verify-deployed-build` の隣）。★**次の push の便で作る**
  - ★それまでは、手順書に**人の確認**として 1 行置く

## 3. staging のワーカーが 40 時間 止まっていた — ⚠️ 記録と、その先
- ✔ `world_state` の最終更新が 2026-09-22。★オーナーが `/stable/foal` で出した依頼は **DB に届いていた**（`breed_initial` / `pending` / 父母とも記録）が、★確定する側が動いていないので `pending` のまま
- ★**本番のワーカーは生きている**（13 秒前・週 284）→ ★一周の確認を**本番で行う**というオーナーの選択は妥当
- ⚠️ ★**本番で一周すると、本番に本物のデータ（仔・EP の記帳）が残る**。★それでよい（オーナーの口座・オーナーの判断）が、★**残ることを記録**しておく
- ★staging を止めたまま放置しない: ★**止まっていたことに誰も気づかなかった**（★`WORKER-STALL-0916` と同じ形）。★staging の生死を見る仕組みが本番にしか無いなら、★**簿に 1 行**（★直すかどうかは別便）

## 4. `own-header-coverage` の網 — ✅
★穴 2 つ（①正規表現が 1 段の道しか拾わない ②走査が `app` の直下だけ・`uma-parts` を使う面だけ）を自分で見つけて直した。★**入れ子を 1 つも拾えていないなら落ちる**対照を両側に置いたのが良い（★網が「何も見ていない」状態で緑にならない）。

---

## 5. 追記（2026-09-24）— `0078` と本番の確認 ✅

- ✔ ★条件 ①: `0078` が `with candidates as (…)` で**述語を 1 回だけ**書き、総数も行もそこから取る（✔ レビュー側も原文を確認: `sex = 'female'` の出現は**ファイル内 1 回**）。★`0077` は staging 適用済みなので書き換えず `create or replace` で重ねた（作法どおり）
- ✔ ★条件 ②: 本番の読むだけの確認（`begin read only`）。★関数の署名・`stable`・`security definer`・権限（`authenticated` に EXECUTE）・返す列（★**素質・能力は無い**）・RPC の総数 **1,897** ＝ ★**直に数えた 1,897 と一致**（★述語がワーカーと同じことの実証）。★世界の週 284・最終更新 10 秒前
- ✔ 手順書に「画面を本番に出す前に、その画面が呼ぶ DB の口が本番に在るか」を ⑦ の前に追加。★機械の突き合わせ（`verify-screen-rpcs-live`）は次の便
- ✔ 簿 `STAGING-WORKER-LIVENESS-UNSEEN`（★「ワーカーを常駐させる案は採らない — オーナーの端末を占有する」と、★**採らない案とその理由まで書いてある**）
- ✔ 本番に本物のデータが残ることを、★オーナーに 2 回 明記して伝えた。★一周はまだ始まっていない
- ✔ `/stable/foal` への入口（リンク）は**まだ張っていない**（★一周を見てから決める）→ ★順として正しい

★staging 1,896 / 本番 1,897 の 1 頭差は、★**staging で予行に使った分**と読める（★確かめていないので、そう書かない）。★次に触ったときに理由が分かれば記録する。

## 6. 追記（2026-09-24）— 1 頭差の理由 ✅ ／ 🔴 年齢の条件が「いま効いていない」

### ✅ 1 頭差
- ✔ 原因は **`grant-retired-horses-staging.mjs` で staging のオーナーに牝 1・牡 1 を配った**こと。★配った牝 1 頭が `owner_id` を持ち、候補（`owner_id is null`）から外れた
- ★私の読み「予行で使った分」は**半分当たり**。★正確には「**配った分**」（消費ではなく譲渡）。★本番には持ち主の居る功労馬が 0 頭なので、この差は入っていない
- ★開発側の申し送り（「2 つの環境の数を突き合わせるときは、まず**自分が何を配ったか**を見る」）は正しい。★**まとめて配っていたら「述語が違う」と誤診していた**という見立ても正しい

### 🔴 年齢の条件が、いまどちらの環境でも効いていない（★照会に無かった点）
- ✔ 開発側の実測: 「年齢を見ないと」の数が候補の数と**同じ**＝ ★**年齢で落ちている馬が 1 頭も居ない**
- → ★**年齢の項（`v_week - h.birth_week >= p_min_breeding_age_weeks`）は、いま一度も働いていない**。★働いていない条件は、★**間違っていても誰も気づかない**（★この案件で何度も出ている形・`u-gallop` と同じ）
- **条件**: ★**止める側で試す**。★staging の予行（取引の中・必ず戻す）に、★**年齢が足りない牝馬を 1 頭 作り、候補に出ないことと、総数が 1 減ることを見る**判定を足す。★対照として、★年齢の項を外すと**その馬が出てくる**ことも見る
- ★これは `0078` の変更ではなく**予行の追加**なので、★本番には触れない

## 7. 追記（2026-09-24）— 年齢の項の予行（`d10da5b`）✅

- ✔ 実演 15/15。★年齢が足りなくなった牝馬は候補から外れる（1896 → 1895）／一覧にも出ない／★**対照**（年齢の下限を 0 にすると戻る）／★取引は戻った（途中の `commit` 無し）
- ✔ ★**作り方を途中で変えた判断が良い**: 「新しく 1 頭 作る」形は必須列・制約に 3 回 落ち、★**検査が「馬の作り方」に依存する**（`horses` に列が増えるたびに落ちる）。→ ★**既に候補に居る 1 頭の列を動かす**形にした。★「変えたものが 1 つ」と言い切れ、★「総数が 1 減る」とも合う
- ✔ ★**正直な但し書き**: 週の列を 3 つ 一緒に動かしている（`horses_processed_after_birth` が縛るため）。★動かす列のうち**述語が見るのは `birth_week` だけ**で、★**対照がそれを示している**。★「1 変数と言い切れない所」を註記に書いた → ★**言い切れないことを言い切らない**形
- ✔ 分類を READONLY → STATE_CHANGING に移し、`tool-aftermath` に `restores`（`sandboxTx`）で登録

### ★申し送りを規律に上げる
> 🔴 **「数が一致した」は「全部の項が正しい」ではない。** ★一致は、★**効いていない項があっても成り立つ**（今日の年齢の項がそれ）。
★これは今日 2 回目の同じ形（★分布は合っていたがハッシュが壊れていた件・`10ba07e` §10）。★**指標が 1 つ増えるたびに「その指標が 0 を返していないか」を見る**（CK-14 の家族）。
