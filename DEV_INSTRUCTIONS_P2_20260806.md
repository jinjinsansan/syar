# 開発指示書 P2 — 開催スケジューラ + 馬券 + オッズ

**発行**: 2026-08-06（レビュー側 Claude）
**正典**: `STAR_SPEC_v2.0.md`（§4 データモデル・§9 ベッティング・§10 開催スケジューラ・§14 インフラ）
**着手条件**: **P1.5 完了後**。ここで初めてサーバーを立てます（§16）

---

## 0. このフェーズの完了条件

> **10分サイクルが無人で24時間回り続けること**（§16.1）

P0/P1 は「計算が正しいか」でしたが、**P2 は「止まらないか」**が主題です。評価軸が変わります。

---

## 1. スコープ

### 1-1. インフラを立てる（§14）

| レイヤー | 採用 |
|---|---|
| DB / 認証 / Realtime | **Supabase** |
| ゲームループ | **常時稼働ワーカー**（Render Background Worker 等） |
| フロント | Vercel（**読み取り系だけ**。ビジネスロジックを置かない・§15.2） |

**★守ること**:

- **ゲーム内時刻の真実は Postgres の `now()` のみ**。ワーカーやクライアントの時計を信用しない
- **advisory lock で二重実行を防ぐ**
- **すべての処理を冪等に**。「このレースはもう生成済みか」「この馬券はもう払い戻したか」を必ず確認してから動く。**再起動やデプロイ中の重複実行で二重払戻が起きたら経済が壊れる**
- **staging のワーカーを production DB に絶対に繋がない**。起動時に DB 側の `environment` フラグと照合し、**不一致なら起動失敗**させる（§14.6）
- **`seed_reveal` を確定前に返さない**。RLS で `status != 'scheduled'` の行のみ露出（**漏れたら Provably Fair が無意味になる**）

### 1-2. データモデルの実装（§4）

正典 §4 のスキーマをそのまま実装してください。**特に注意する2点**:

1. **二種ポイントは別カラム・別台帳**（`entry_points`/`prize_points`、`ep_ledger`/`pp_ledger`）。1つのカラムに種別フラグを持たせない — **混同できない構造にすることが分離の担保**（憲法 §0.2-3）
2. **`pp_ledger.reason` に EP からの変換を表す値を作らない。PP→EP の reason も存在させない**（構造 S-5・§0.3）

### 1-3. 開催スケジューラ（§10）

- **10分サイクル**（D-007）。タイムテーブルは §10.2
- **生成は2レース先まで先行実行**（障害時バッファ）
- **★出走馬は同格帯（クラス）から組む**（D-018）。**無作為抽選にすると1番人気の勝率が51%になり V-4 が壊れます**
- 番組表は §10.3。エントリーは**完全抽選**（賞金上位優先にしない — 新規が弾かれると離脱する）
- **生成失敗時は「開催中止」として全ベットを EP で返還。結果の事後差し替えは絶対にしない**（§10.2・§8.6）

### 1-4. オッズと馬券（§9）

- **固定オッズ**。レース生成時にモンテカルロで理論勝率を算出（§9.2）
- **オッズ算出のシードは本番確定用とは別系列**（§9.2）。**P1 で系列の独立性を実証済みなので、その仕組みをそのまま使うこと**
- **`odds_at_purchase` を購入時に固定**（後からオッズ式を直しても既存馬券の配当は変わらない）
- 7券種・控除率・配当上限・ベット上限は §9.1〜9.4
- **自馬ベット制限**（§9.5）: 自馬が出るレースでは自馬絡みの馬券のみ・1レース5,000 EP まで
- **★EP で買い PP で払い戻す**（§9）

### 1-5. ポイント増減の原子性（§14.4）

**EP の減算と馬券の発行、PP の加算と払戻の記録は、必ず同一トランザクションに収める。** 分かれているとネットワーク断で「EP だけ減って馬券が無い」事故が起きます。

→ **Supabase の RPC（Postgres 関数）を主 API とする**（§15.3）。`security definer` で、発売時間内チェック・上限チェック・自馬ルール・残高ゲートを1関数内で行い、`bets` の挿入と `ep_ledger` の更新を同一トランザクションにする。

### 1-6. 監視（§4.6・§11.2）

`point_flow_daily` を実装し、**PP の純発行量を毎日確認できる**状態にする。`margin_actual` が理論値から3%以上乖離した日はアラート。

---

## 2. 合格基準

| # | 基準 | 出典 |
|---|---|---|
| **A-1** | **10分サイクルが無人で24時間回り続ける**（実測。ワーカーを止めずに1日回す） | §16 |
| **A-2** | **ワーカーを強制終了して再起動しても、二重生成・二重払戻が起きない**（冪等性の実証） | §14.3 |
| **A-3** | **V-10 払戻率**が券種別に設定 margin ±1%（10万レース） | §13.2 |
| **A-4** | **`seed_reveal` が確定前に取得できない**（RLS の実証。**実際に叩いて確認**） | §8.6 |
| **A-5** | **EP 減算と馬券発行が原子的**（トランザクション途中で落として片方だけ残らないことの実証） | §14.4 |
| **A-6** | **自馬ベット制限が効く**（自馬絡み以外の馬券が買えないこと・上限5,000 EP） | §9.5 |
| **A-7** | **staging ワーカーが production DB に繋がると起動失敗する** | §14.6 |
| **A-8** | **P0/P1 の全ゲートが本番構成でも通る**（回帰） | §16.2 |
| **A-9** | テスト全PASS・typecheck・`any` ゼロ・変異試験すべて防御 | — |

**★A-2 と A-5 は「壊して確かめる」タイプの検証です。** P1 の変異試験と同じ発想で、**異常系を実際に起こして確認**してください。正常系が動くことは A-1 で分かります。

---

## 3. このフェーズで特に警戒すること

**P0/P1 で繰り返し出た失敗が、P2 では別の形で現れます:**

| P0/P1 での形 | P2 での形 |
|---|---|
| テストが本番経路を通っていない | **ローカルでは動くが本番構成では動かない**（RLS・トランザクション・タイムゾーン） |
| 測定の自由変数が判定を作っていた | **テスト環境の時計やレート制限が、本番と違う挙動を作る** |
| 比率と範囲が一様な歪みを検出できない（R-16） | **「動いている」ことの確認が、実は何も確認していない**（例: ワーカーが生きていても中身が空回り） |

→ **A-1 の「24時間回る」は、レースが実際に生成・確定・払戻されたことまで確認**してください。プロセスが生きていることではありません。

---

## 4. 報告様式

`REPORT_P2_[日付].md` に:

1. 実装したもの・インフラ構成
2. 再実行/再現手順（**本番構成での確認手順**を含む）
3. **合格基準 A-1〜A-9 の実測**（特に A-2/A-5 の異常系の起こし方と結果）
4. 定数の調整履歴（較正定数／測定条件の別を明記）
5. 仕様からの逸脱・解釈（**全件・範囲を明記**）
6. 未解決の質問

---

## 5. 恒久ルール（§16.3）

R-1〜R-16 は全フェーズに適用されます。**P2 で特に効くのは**:

- **R-3**（判定不能は FAIL に倒す）— 分散システムでは「不明」が増えます
- **R-8**（実行時に逸脱を自己申告）— 本番の設定ずれを早期に出す
- **R-16**（壊れた状態がこのゲートを通らないか問う）— A-1 の設計そのもの
