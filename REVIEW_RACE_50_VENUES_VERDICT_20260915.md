# 裁定 — レース演出を 10 場・50 鞍に広げる（第 1〜5 便）と発走前の流れ（報告 `REPORT_RACE_50_VENUES_20260915.md`）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-15 ／ レビュー側 → 開発側・オーナー
**対象**: 報告 `REPORT_RACE_50_VENUES_20260915.md` ／ コミット `2e5ff53`（第 1〜5 便）・`cfc3ad1`（発走前の流れ）／ ブランチ `p4/race-30sec-cuts`・HEAD `cfc3ad1`
**物差し**: 回答 `REVIEW_CONSULT_RACE_50_VENUES_ANSWER_20260915.md`（§2〜§7・§8-1 のオーナー決定と条件）／ 裁定 `REVIEW_RACE_SIDE_ONLY_VERDICT_20260915.md` §3（v9 の条件）

> 凡例（R-13）: **✔** ＝ レビュー側がコードを読んで確かめた、または手元で実行した ／ **△** ＝ 未確認 ／ **[EYES]** ＝ オーナーの目で決めること
> 本書は正典を編集しません。

---

## 0. 判定 — **CONDITIONAL PASS**

| 範囲 | 判定 |
|---|---|
| 指示書の外での実装（オーナーの直接の指示・報告 §1 に原文） | **受理** |
| 本番の経路 | ✔ **エンジン・ワーカー・DB は変わっていない**（変更はデモの `/race`・`packages/render`・監査道具・`packages/scheduler` のデータと時間帯の関数だけ）。着順・走破タイムに触れていない |
| 回答 §7 の順番 | ⚠️ 順番 2（v9 裁定の条件）を飛ばして 3〜5 を先に入れた → **RC-6 で戻す** |
| 条件 | **RC-1〜RC-8**（§3） |

---

## 1. レビュー側の再実行 ✔

HEAD `cfc3ad1`。報告に挙がった検査 12 ファイル（`race-preshow-wiring`・`side-only-script`・`first-pass-time-of-day`・`race-clock-wiring`・`venue-look`・`venue-flags-sound`・`season-atmosphere`・`venue-scenery`・`course-pos-cache`・`silks-distinct`・`race-intro`・`audit-screen-parity`）: **12 ファイル・101 件すべて合格**（8.6 秒）。

---

## 2. 照合（報告 §0 の R1〜R6 ほか）

| # | 報告の主張 | 照合 |
|---|---|---|
| R1 | 長距離の 4 鞍で跳びが 2 か所・検査を鞍ごとの期待値に | ✔ 4 鞍（銀河賞・北極星カップ・大河原記念・白光記念）は回答 §0-1 の計算と一致。`side-only-script.test.ts` 合格 |
| R2 | 発走前の人気馬の紹介・格・出馬表が結果を読まない（D-098） | ✔ **いまのコードは読んでいない**: `apps/web/src/app/race/page.tsx:3684-3764` の 3 つの分岐が読むのは `DEMO_WIN_ODDS`・`HORSE_NAMES`・`JOCKEY_NAMES`・`GRADE_LOOK`・`RACE_META`・走路の形だけ。⚠️ **ただし検査の書き方に穴** → RC-1 |
| R3 | 時間帯は日本時間で固定・対応表 1 か所・デモの口 | ✔ `packages/scheduler/src/time-of-day.ts`: 入力は発走の時刻（UTC ミリ秒）＋`JST_OFFSET_MINUTES`・端末の時計を読まない・対応表は `TIME_OF_DAY_BANDS` だけ・`?tod` を省くと昼・知らない値は昼へ落として `fellBack: true`。オーナー決定 2 の条件を満たす。本番の画面にはまだ繋いでいない（報告に明記） |
| R4 | 重ね塗りの視認性の条件のうち未達がある | ✔ 報告のとおり。条件 1・2 は満たす（透視ワールドのカットで地面の色を画面全体に重ねるのは、馬・HUD より先に描くので受理）。**条件 3 は時間帯だけ・条件 4 は未着手** → RC-4 |
| R5 | 監査道具の鞍の受け方は未対応 | ✔ 報告のとおり → RC-5 |
| R6 | 馬の速さの訂正を画面と監査道具の両方に | ✔ `page.tsx:1719`・`tools/lib/race-audit-build.mjs:153` とも `straightMetersLeft: PHASE_METERS.STRAIGHT`。`audit-screen-parity.test.ts` に対照（直線の長さを渡すと外れる）あり。表示の位置模型だけで、エンジンは不変 |
| — | 写真判定で止めない | ✔ `packages/render/src/race-climax-hud.ts:190`（`PHOTO_FINISH_BADGE_SEC = 1.4`・止め絵なし）→ **v9 裁定の条件 ①（止め絵を放すときの跳び）は対象がなくなった** |
| — | 全画面の出馬表 | ✔ 賞金の額を渡していない（`page.tsx:3749-3761`）。回答 §5 条件 1（賞金ポイントの額を演出で強調しない）に合う |
| — | 場の見た目を名前から引かない | ✔ `venue-look.ts`・`venue-scenery.ts`・`season-look.ts` に名前（`.name`）からの分岐なし。回答 §3-1 に合う |
| — | 8 頭立て | ✔ 画面 `page.tsx:169` `FIELD = 8`・監査道具 `race-audit-build.mjs:58` `field: 8`。★**変更前はどちらも 12**（`57adf77`）→ RC-2 |
| — | デモの馬名 | ✔ `page.tsx:832` に「ハンシンドリーム」「ナニワスピリット」→ RC-7 |

---

## 3. 条件

### RC-1 ★D-098 の検査を「入力」で書き直す（最優先の 1 つ）

- `apps/cli/test/race-preshow-wiring.test.ts` ① は、分岐の文字列に**禁止語**（`result`・`settled`・`winner`・`finishSec`・`finishPos`・`marginLabel`・`development`）が無いことを見ています
- ★**禁止語に当たらない、結果を読む値が同じ画面にあります**: `built.finishStyle`・`built.finishSpeeds`・`built.finishCrossD`・`built.finishChaseAt`（✔ `page.tsx:3864`・`:3940-3944`・`:4125`）。これを紹介の分岐に書いても検査は落ちません
- さらに、分岐の範囲を「最初の `return;` まで」で切っているので、分岐の中に早く抜ける `return;` が入ると残りを見ません
- これは 09-14 に確かめた教訓「**漏れの判定は、何を入力に取っているかで書く**（名前や媒体で書かない）」と同じ形です
- **直し方（どちらか。報告に選んだ理由を書く）**
  - (a) 発走前の 3 つの分岐が、**解いたレース（`built`）を一切参照しない**ことを見る
  - (b) 描く関数（`paddockPicksOf`・`popularityRanksOf`・`drawPaddockIntro`・`drawGradeIntro`・`drawEntryBoard`）の**引数の型に結果を持つ型が入らない**ことを見る
- **変異の実演**: 紹介の分岐に `built.finishStyle` を 1 行足すと落ちる

### RC-2 ★8 頭立てにしたことで、過去の合格が黙って失効している

- 監査道具の既定が 12 → 8 になったので、**v8・v9 までに合格とした映像の不変条件は 12 頭で測った値**です（R-23）
- (a) v9 裁定までの重い映像の不変条件（先頭が画面の外・共通の被写体 0 頭 など）を **8 頭で取り直す**
- (b) ★**本番の出走頭数は 8〜18**（正典 §13.2）。勝負服も 8 頭を超えると別の色の組に切り替わります（✔ `packages/render/src/bracket.ts:86`）。デモの既定は 8 でよいが、**監査は 8 頭と、8 頭を超える組（16 または 18）の両方**を回す（R-33）
- (c) `edit-grammar-audit` ⑨（seed の分類）が 8 頭で動いたかを記録する（報告 §7 の △）

### RC-3 発走の秒 4.4 → 32.4 のずれ

- `RACE_INTRO_RACE_START_SEC` を import する道具（✔ `tools/audit-race-motion.mjs`・`tools/shot-race-at.mjs`・`tools/lib/race-audit-build.mjs`）は自動で新しい値になるが、**過去の報告・裁定・撮影の秒の指定は合わなくなった**
- **秒を直書きしている道具と引数を列挙**し、監査の秒を**発走からの相対**で指定する形に揃えるか、揃えない道具を報告に書く
- 映像の不変条件の測定窓が、発走前の 32 秒を含んで**平均が薄まっていない**ことを確かめる（R-16）

### RC-4 視認性の条件の残り（回答 §4-3）

- 条件 3: 介入の局面（残り 900m〜）で、**季節の色味・雨・雪・霧も**弱める（いまは時間帯だけ `TIME_OF_DAY_INTERVENE_FACTOR`）。弱めないならその理由を報告に書いてオーナー判断（報告 O-4）
- 条件 4: 画素のコントラストを測る道具（報告用・合否の線は引かない）。★V-16 のボットは重ね塗りを見られない（D-067）ので、これが唯一の測り方です

### RC-5 監査道具を画面と同じ入力に（回答 §6・R-30）

- §6-1 `buildAuditRace` が**鞍の id を受け**、画面と同じ `raceSetupFromParam` から走路の形・回り・場の見た目・季節・時間帯を引く。省いたときは画面の既定の鞍（R-31）
- ★報告 §6-2 で開発側が見つけた食い違い: **監査道具の `auditClock` は見せない区間（elisions）を渡していない**。画面と同じ時計にする
- §6-3 重い映像の不変条件を、**10 場 × 各場の最短と最長の鞍**など場の軸で選び、**右回りの場を必ず含める**

### RC-6 v9 裁定の条件の持ち越し（回答 §7 の順番 2 に戻す）

| v9 の条件 | 状態 | この裁定での扱い |
|---|---|---|
| ① 止め絵を放すときの跳び | 止め絵ごと消した | **閉じる** |
| ③ 右回りの誤りを捕まえる検査 | 未着手 | ★**最優先**。右回りの 4 場が本番に入った（`80c4eb3`）ので、左回りだけの検査のままにしない |
| ② 写真判定の判定と言葉が別の入力 | 未着手 | ★**まだ別の入力です**: 札を出すかは**描いている位置の差**で決め（✔ `race-climax-hud.ts:202-213`）、札の言葉は**着順ボードの値**（✔ 同 `:226-227`）。どちらか一方に揃える |
| ④ 塊を 1 色に揃える処理で上着が枠色に塗られる | 未着手 | 8 色の上着（`jacket8-*`）になったので、**8 頭と 8 頭を超える組の両方**で計測 |
| ⑤ 実況の着差の言葉を `marginLabel` へ | 未着手 | そのまま |
| ⑥ 勢いの 2 つの入力を同じ関数から | 未着手 | 急がない |

### RC-7 権利（§0.1・§17.1 L-7）

| # | 事項 | 扱い |
|---|---|---|
| (a) | デモの馬名「ハンシンドリーム」「ナニワスピリット」（`page.tsx:832`）は実在の地名に由来 | **レビュー側は差し替えを推奨**（報告 O-1・オーナー判断） |
| (b) | 場の見た目・景色の主題が実在の場を一対一で指させないこと | 回答 §3-2 の担保 2: **オーナーが実在の場と照らして確認した旨と日付**を `venue-look.ts` の註記に残す（報告 O-2）。機械では検査できない |
| (c) | 新しく生成した絵（`paddock-bg-v1`・`horse-jockey-side-walk-v1`） | 正典 D-087 の素材の条件（**新規は承認を取る・実在の競馬場を模写しない・登録簿に載せる**）に照らし、**承認と登録簿への記載**を報告で示す。△ レビュー側は登録簿の置き場所を確かめていない。パドックの背景が実在のパドックを写していないことは [EYES] |

### RC-8 [EYES]（オーナーの目で）

- タイトルカードの背景の桜が**全 50 鞍に出る**（冬・夜の鞍でも）→ 季節で差し替えるか（報告 O-5）。オーナー指示 6「冬の雪はいいが、春の桜と雨の落ち葉はおかしい」と同じ系統の違和感になりうる
- 発走前の各場面の秒（報告 O-3）・夜の暗さ（上限 0.5・報告 O-4）
- 長距離の 1 周目のスタンド前 200m（約 12 秒）の長さ（回答 §2 条件 3）

---

## 4. 受理（指摘しない）

| 事項 | 理由 |
|---|---|
| 景色を「遠景の絵 10 枚」でなく**図形**でタイトルカードに描いた | レース中に空と遠景が見えるのは画面の上 約 9% だと**先に測った**（報告 §3-3）。高い発注の前に安い実験を置いた形 |
| 型 B の歩きのコマを使わず退避した | オーナー指示（足が白い馬を使わない）に合わせた。見落としを報告に書いている |
| 携帯の焼いた経路は走りのコマのまま | 既知の差として明記。揃えるのは次の便でよい |
| 歩きのコマの「隣のコマの変化 20%」の不合格（6→7 コマ目 15.9%） | 道具の線は駈歩が前提。常歩は変化が小さいという読みを受理（[EYES] で確認） |
| `graded-races.ts` に `series`（三冠の第 n 戦）を明示の欄で足した | 名前から引かない形（回答 §3-1）に合う |

---

## 5. オーナー判断

| # | 判断 | レビュー側の推奨 |
|---|---|---|
| O-1 | デモの馬名 2 つを差し替えるか | **差し替える** |
| O-2 | 場の見た目の確認と日付 | オーナーが確認して日付を残す |
| O-3 | 発走前の各場面の秒 | [EYES] |
| O-4 | 夜の暗さ・介入の局面で季節／雨／霧も弱めるか | **弱める**（回答 §4-3 条件 3） |
| O-5 | タイトル背景の桜を季節で差し替えるか | [EYES] |

---

## 6. 次の便の順番（レビュー側の推奨）

1. **ES 便**（エンジンの速さ・`REVIEW_QUESTIONS_ENGINE_LANE_SPEED_ANSWER_20260915.md`）— 最優先。触るファイル（`packages/race-engine`）は本裁定の条件と重ならない
2. **RC-1**（D-098 の検査）・**RC-6 ③**（右回りの検査）・**RC-2**（8 頭で失効した合格の取り直し）
3. RC-5（監査道具）・RC-6 ②④⑤・RC-4・RC-3
4. RC-7（権利）は**公開の前に必ず**（§17.1 L-7）

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
