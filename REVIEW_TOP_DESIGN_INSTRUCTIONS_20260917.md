# 指示 — TOP（`/`）のデザイン改訂・第 1 便：R-14 を起票するための「確かめた事実」

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-17 ／ レビュー側 → 開発側・オーナー
**対象**: `apps/web/src/app/page.tsx` → `apps/web/src/app/lp-preview/story.tsx` ／ `story.css`
**関連**: オーナー決定 **O-8**（デザインはデザイナー経由）・憲法 §0.1（実在名）・§12（UI/UX）・**D-052**（1 つの値に 1 つの出どころ）・**R-30**（2 か所で持たない）

> 凡例（R-13）: **✔** ＝ レビュー側がコードで確かめた ／ **△** ＝ 未確認 ／ **🔴** ＝ 先に潰すこと

---

## 0. 本書は依頼カードではありません

★**依頼カード（R-14）を書くのは開発側です**（O-8）。本書はその**材料**です。

| 本書に入っているもの | 本書に入っていないもの |
|---|---|
| ★コードで確かめた**現物の姿**（§2） | ★**見た目の良否・センスの裁定**（★レビュー側はコードで確かめられないものを裁定しません） |
| ★**機械で確認できる合否条件**（§4） | ★具体的な配色・構図・文言の案 |

---

## 1. オーナー決定（2026-09-17）

| # | 決定 |
|---|---|
| 1 | ★**まず TOP（`/`）だけ**変える |
| 2 | ★路線は **(A) 今の「馬物語」を磨く**（★アーケード筐体風に戻すのではない） |
| 3 | ★**新しい Claude Design のチャット**で描かせる |
| 4 | ★**重要**: ★**センスが崩れたチャットと、開発側が同期していたチャットは別物**です。→ ★**`design/hud-ds` の 30 枚のカード・`styles.css`・`github.md` の同期記録は、そのチャットの産物ではなく、正本として有効**です |

---

## 2. 現物の姿（✔ すべてコードで確認・2026-09-17）

| # | 事実 |
|---|---|
| 1 | `/` → `apps/web/src/app/page.tsx` → ★**`app/lp-preview/story.tsx` をそのまま描画**（`import HorseStory from './lp-preview/story'`）。★2026-09-13 にオーナー指示で差し替わったもの |
| 2 | ★**触る場所は 2 ファイルだけ**: `story.tsx`（**131 行**）・`story.css`（**11 行**） |
| 3 | ★**絵は 0 枚**。空・雲・丘・スタンド・柵・馬場・速度線は**すべて CSS の図形**（`ms-sun`・`ms-cloud`・`ms-hills`・`ms-stand`・`ms-field`・`ms-fence`・`ms-track`・`ms-speed`） |
| 4 | ★**`RunningHorse` は `story.tsx:14` のローカル定義**。★**レース描画（`packages/render`）と共有していません** → ★**触っても 2 か所問題は起きません**（R-30 の心配なし） |
| 5 | 節は **5 つ**: ヒーロー（★毛色プレビュー・★「Ⅱ 動きを止める」ボタン付き）→ `01 — EVERY DAY COUNTS`（育てる／挑む）→ `02 — THE STORY GOES ON`（次の世代へ）→ 締め → フッター |
| 6 | ★**スマホ縦は「未着手」ではありません**。`@media` が **4 本**: `min-width:1500px` ／ `max-width:900px` ／ ★**`max-width:600px`（作り込み済み）** ／ ★**`prefers-reduced-motion:reduce`** |
| 7 | ★`max-600` の現在値（抜粋）: ヒーロー `min-height:820px`・`h1 38px/line-height 1.55`・`ms-lead 12px`・`ms-runner{top:420px;width:106%}`・`ms-hills-far top:480px`・`ms-track top:660px`・`ms-chapters grid-template-columns:1fr`・`ms-coats small{display:none}`・`ms-nav-links{display:none}` |
| 8 | ★**導線は 5 本すべて実在** ✔ `/signup`・`/login`・`/race`・`/lp-arcade`（フッター「以前のTOPを見る」）・`/lp-preview`（作った側の復元点） |
| 9 | ★**憲法の禁止語は 0 件** ✔（`購入`・`チャージ`・`換金`・`円`・`課金` を `story.tsx`・`story.css`・`page.tsx` で抜き取り、いずれも 0 件） |

---

## 3. 🔴 先に潰す落とし穴 4 つ

### 3-1. 🔴 ★没案を新しいチャットに渡さないこと

★「何を言ってもセンスが悪い」のは、★**そのチャットが自分の過去の出力に引きずられている**形が疑われます。★**新しいチャットに旧チャットの没案を「参考」として見せると、同じ方向に再び錨を降ろします。**

→ ★渡すのは **① 依頼カード ② 本書 §2 の現在値 ③ 憲法** の 3 点だけ。★**没案は渡さない。**

### 3-2. 🔴 ★納品先を、始める前に決めること

★新しいチャットは、開発側が同期していたプロジェクトとは**別**です。★**開発側の同期はそこに届きません。**

| 案 | 中身 | 評価 |
|---|---|---|
| **(a)** | 新しいチャットにも**同じデザインシステムのプロジェクト**へ納品させる | ★**推奨**。開発側の同期がそのまま効く |
| **(b)** | オーナーが HTML を手で `design/hud-ds/components/<名前>/index.html` に置く | 動くが、★**置き忘れると開発側に永久に届かない** |

⚠️ ★どちらでも「**`design/hud-ds` が正本**」は動かさないこと（デザイナー自身の README の警告・R-30・D-052）。

### 3-3. 🔴 ★既存の `landing` カードを上書きさせないこと

| | |
|---|---|
| 既存 ✔ | `components/landing/` ＝ 1 行目 `@dsCard group="Web" name="TOP（LP）［アーケード］" viewport="1280x3770"` → ★**これは退避した `/lp-arcade` の記録**（R-3） |
| 新規 | ★**別フォルダ・別名**で。例: `components/landing-story/` ／ `name="TOP（LP）［馬物語］"` |

⚠️ ★`landing` を上書きすると、★**旧 TOP の実装表という史料が消えます。**

### 3-4. 🔴 ★1 行目の `@dsCard` コメントが無いと索引に出ません

✔ 確認: ★**`design/hud-ds/_ds_manifest.json` は存在しません** → 索引は**各 HTML の 1 行目のコメントから組まれます**。

```
<!-- @dsCard group="Web" name="TOP（LP）［馬物語］" subtitle="…" viewport="390x…" -->
```

★この 1 行を落とすと、**描けていても誰の目にも入りません**。

---

## 4. ★機械で確認できる合否条件（センスを議論せずに確認できるもの）

| # | 条件 | 確認のしかた |
|---|---|---|
| **CH-1** | ★**禁止語 0 件を維持**（`購入`・`チャージ`・`換金`・`円`・`課金`） | 納品 HTML と改訂後の `story.tsx`／`story.css` を抜き取り。★**今 0 件**なので、増えたら退行 |
| **CH-2** | ★**実在の馬名・騎手名・競馬場名・他社製品名を書かない**（憲法 §0.1） | 同上。★**参考にする実機の名前もカードに書かない**（特徴で指定する） |
| **CH-3** | ★**導線 5 本を消さない**（`/signup`・`/login`・`/race`・`/lp-arcade`・`/lp-preview`） | 納品 HTML に 5 本あるか |
| **CH-4** | ★**`prefers-reduced-motion:reduce` の枝も一緒に納品**する | ★今は `animation:none!important` で全部止まる。★**動きを増やす提案は「止めたときの見え方」も決めること**。片方だけ直すと、止めた人には壊れて見える |
| **CH-5** | ★**ヒーローの「Ⅱ 動きを止める」ボタン（`story.tsx:112`）を消さない** | ★止める手段を画面から奪わない |
| **CH-6** | ★**値は「セレクタ＋値の表」で返す**（1 行 CSS に直接パッチを当てない） | ★`story.css` は **11 行**に全部詰まっている。★差分を直接書かせると事故る |
| **CH-7** | ★**2〜3 案を同時に出させる**（1 案だけにしない） | ★「これは違う」ではなく「**この 3 つならこれ**」で決める。★言葉でセンスを直そうとするのが最も失敗する形 |
| **CH-8** | ★**§2 の現在値を渡した上で描かせる** | ★渡さないと、★**既にある `max-600` の作り込みを描き直させる** |

---

## 5. 開発側の宿題

| # | やること |
|---|---|
| 1 | ★**R-14 として `design/hud-ds/requests/` に起票**（★対象は **`/` の 1 画面のみ**・**スマホ縦優先（390 幅）**・本書 §2 と §4 を写す） |
| 2 | 🔴 ★**`DESIGN_HANDOFF_REQUEST_MOBILE_TOP_20260902.md` の冒頭に「失効」の註記を入れる** — ★あの依頼書は対象を `page.tsx` ＋ `public/lp/hero.jpg` ＋ `globals.css` の `@media` と書いているが、★**09-13 に中身が入れ替わり、今の TOP は絵を 1 枚も使わず、CSS も `story.css`**。★**残すと必ず誤読される** |
| 3 | ★**`components/landing` は今の TOP ではない**（`/lp-arcade` の記録である）ことをカードに明記 |
| 4 | ★`requests/_INDEX.md` に R-14 の行を足す（★状態・置いた日・1 行の中身） |

---

## 6. △ レビュー側が確認していないこと（正直に残します）

| # | 未確認 |
|---|---|
| 1 | △ ★**本番 `https://star-two-chi.vercel.app/` に今の TOP が入っているか**。★オーナーが開いて「**この一頭から、物語がはじまる。**」が出れば入っている。★アーケード風の「STAR」が出たら**旧 TOP のまま**＝デザイナーに見せる前に配備が必要 |
| 2 | △ ★**`story.tsx` の 1〜94 行は本書では読んでいません**（読んだのは 95〜131 行と全体の grep）。★`story.css` が `.ms-nav`・`.ms-nav-links`・`.ms-nav-start` を持つので**ヘッダーがあるはず**だが、★**markup は未確認**。★カードに書く前に開発側が確かめること |
| 3 | ★**見た目の良否は裁定しません**（コードで確かめられないため）。★出せるのは §4 の機械的な条件だけ |

---

## 7. 次の順番（変わりません）

| 順 | 中身 |
|---|---|
| 1 | 🔴 **V-10 の測り方の訂正**（`apps/cli/src/verify-pmin.ts:108-113`・裁定 `REVIEW_GAME_BODY_5_VERDICT_20260916.md` §10）。★**今いちばん古い宿題**です |
| 2 | **LR-10 の実装**（日次 1 本・別の表・閾値なし） |
| 3 | **物語の 10 種を繋ぐ便**（冒頭で 15 種すべての起点を洗い出してから） |
| 4 | ★**本便（TOP の R-14）はデザイナー待ちなので、上の 3 つと並行で構いません** |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
