# デザイナーへの回答: 第 2 便の確認事項 8 点 — 2026-09-23

> 以下の「---」から下を、そのままデザイナーへ貼り付けられます。
> 出どころ: 第 2 便 `design_handoff_breed_v2/README.md` §8。開発側がコードと DB を読んで確かめました。
> ⚠️ **7 は、こちらの依頼文の誤りの訂正**です（第 1 便の回答 §4・第 2 便 §2 C の「血が近すぎる」を取り消します）。

---

第 2 便をありがとうございます。**「役割の変更」の色は入れました**（`#dcf1ef` / `#0e7a73`）。
確認事項 8 点に答えます。決まっていないことは、決まっていないと書きます。

## 1. 種牡馬入りした後にできること — いまは一覧で見るだけです

そのとおりです。自分の種牡馬は、**自分でも他の利用者でも、付けられません**（父は NPC の種牡馬だけ）。
種牡馬入りですることは、次の 3 つだけです。

- 生涯の記録に 1 行残る（「種牡馬入りしました」）
- 役割の札が付く
- 5 頭の枠を 1 つ使う（「功労馬に戻す」で空きます）

利用者の種牡馬を使えるようにする話は別にありますが、**時期は決まっていません**。
**画面に「いずれ使えるようになります」と書かないでください。**

## 2. 今年もう産んだ母 — 薄く出して選べなくする（いまの案）でお願いします

データはあります。母ごとに「今年もう産んだか」を返せます。

一覧から外さない理由は、**年が変わると戻る**からです。消えると「馬が居なくなった」に見えます。
「いつ戻るか」を添えるなら、日数・時間と同じ扱いで**枠だけ**にしてください（中身は開発側の関数で入れます）。

## 3. 種付料が上がったときの新しい見積もり — 応答には入っていませんが、**B-3 で出し直せます**

失敗の応答が返すのは「どの失敗か」だけで、**新しい額は入っていません**。
ただし、**B-3 に戻して取り直す形で足ります**。見積もりは種牡馬の事実（G1 勝利数・総獲得賞金・今年の残り枠）から
**開発側の関数がその場で出している**ので、画面がもう一度呼べば、そのときの額が出ます。

応答に額を足すこともできます（DB の工事 1 本）。ただし**確定のときの額はその後また変わり得る**ので、
応答に入れた額も「そのとき限りの見積もり」でしかありません。**出し直して取り直す方が正確です。**

## 4. 参加ポイントが足りないとき — 残高は出せます。「確定のときの額」は出していません

- **いまの参加ポイント**: 出せます。画面が自分の残高を読む口はすでにあり、他の画面でも使っています。
- **確定したときの種付料**: **返していません**。3 と同じで、見積もりの関数をもう一度呼べば「いま必要な額」は出せます。

「いま必要な額」と「残高」の 2 つで画面が作れるなら、工事は要りません。
「断られた、そのときの額」をどうしても出したい場合だけ、応答に足す工事が要ります（DB の工事 1 本）。
**どちらが要るか、決めてください。**

## 5. 自動で功労馬に戻ったことの告知 — 「見た」を記録する仕組みは**ありません**

ご指摘のとおりです。いまあるのは**生涯の記録の 1 行だけ**で、これは何度でも見えます。
「次に開いたとき 1 回だけ」にするには、作りが要ります。案は 2 つです。

- **(a) 「見た」を保存する** … ご要望どおり 1 回だけ出ます。開発側の工事が要ります。
- **(b) 保存しない** … その年に戻った馬が居るあいだ、厩舎を開くたびに枠を出す（画面を閉じるまで）。工事は要りません。

**どちらがよいか、画面の側から決めてください。**(a) なら工事に入れます。

## 6. 「何頭目の仔か」 — 出して構いません

母ごとの産駒の数は持っています（生涯 8 頭まで）。素質の数値ではありません。

文言は「**この母の n 頭目の仔です**」のように、**母に紐づけて**ください。
利用者全体や牧場全体の順番ではないので、「n 頭目」だけだと別の意味に読まれます。

## 7. 🔴 近すぎる血 — **選べなくなることはありません。こちらの依頼文が誤りでした**

**血の近さで組合せを断ることはありません。** 近い血は**仔の出来に効くだけ**です。
したがって、**一覧で薄く出す対象からも外してください**。

第 1 便の回答 §4 の表と、第 2 便 §2 C の「血が近すぎる」の記載を**取り消します**。申し訳ありません。

**「この組合せは選べない」になるのは、次の 5 つだけ**です（いずれも一覧の時点で分かるので、薄く出せます）。

| 断る条件 | 一覧で先に出せるか |
|---|---|
| 父か母が 6 歳未満 | 出せます（年齢） |
| 母が今年もう産んだ | 出せます（2 のとおり） |
| 母が生涯 8 頭を産み終えた | 出せます（産駒の数） |
| 父の今年の種付枠が尽きた | 出せます（残り枠） |
| 父と母が同じ馬 | 起きません（父は NPC・母は自分の馬） |

## 8. 役割の変更の取り消し — 回数の制限は**ありません**

見ているのは**頭数の上限だけ**です（繁殖牝馬 10 頭・種牡馬 5 頭）。
繁殖入り → 功労馬に戻す、は**何度でもできます**。

ただし、**変えるたびに生涯の記録に 1 行増えます**（自動で戻った分も含めて）。
行き来を繰り返すと、その馬の記録が「役割の変更」で埋まります。

画面の側で歯止めを付けるなら、「戻しますか」の確認を 1 枚挟む形をお願いします。
**開発側で回数を制限するかどうかは、オーナーの判断が要ります**（いまは無制限のままです）。

---

## 開発側の控え（デザイナーには渡しません）

- 1: 父の候補は `npc_stallion_facts()`（`owner_id is null and sex='male' and retirement_role='stallion'`）。
  初回配合の親も `isInitialParent`（`owner_id === null`）。利用者の種牡馬を開くのは **I-4**（弁護士ゲート・時期未定）。
- 2: `horses.bred_this_year`。年明けに戻す経路は `breeding-runner.ts`。
- 3: 見積もりは `npcStudFee(g1Wins, totalEarningsPP)`（`packages/scheduler/src/stud-fee.ts:43`・§10.5 の式
  `3,000 + G1 × 8,000 + 総獲得PP / 20`）。素は `npc_stallion_facts()` が両方返す → **画面から再計算できる**。
  確定時は `player-breeding.ts:309` で同じ関数。`my_foal_request`（`0073`）が返すのは
  `request_id, kind, status, failure_reason, result_id, created_at` のみ。額を返すなら列 + 移行 1 本。
- 4: 残高は `users.entry_points` を画面が直接読んでいる（`apps/web/src/lib/bet-screen.ts:133`・`entry-screen.ts:200`）。
- 5: `seen_at` / `acknowledged` に当たる列は DB に**無い**（`grep db/migrations` で 0 件）。
- 6: `horses.foal_count`（`MARE_LIFETIME_FOALS = 8`）。
- 7: `canMate`（`packages/sim-engine/src/breeding.ts:49`）の `MateRejection` は
  `same_horse / sire_not_male / dam_not_female / sire_too_young / dam_too_young /
  dam_lifetime_foals_exceeded / dam_already_bred_this_year / sire_coverings_exceeded` の 8 つ。
  **近交は 1 つも含まれない**。`calcInbreedCoefficient` は仔の能力に効くだけ（`INBREED_BOOST_*` /
  `INBREED_DEPRESSION_THRESHOLD`）。`MIN_BREEDING_AGE_YEARS = 6`。
  ⚠️ **誤りの出どころ**: `DESIGN_REQUEST_SECOND_BATCH_20260923.md:60` と
  `DESIGN_ANSWERS_INTRO_V1_20260923.md:34` に開発側が「血が近すぎる」と書いた。**コードを見ずに書いた**。
- 8: `request_breeding_role`（`0070`）の拒否理由は
  `not_owner / not_retired / sex_mismatch / same_role / lifetime_foals_reached / owner_limit`。
  回数の制限は無い。記録は `horse_story_event` に毎回 1 行（自動降格も同じ種類）。
