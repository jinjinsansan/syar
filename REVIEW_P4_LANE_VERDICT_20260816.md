# Q-P4-41 / 42 の裁定 ＋ 基礎文書を読んだうえでの追加3件

**発行**: 2026-08-16（レビュー側 Claude）
**対象**: `QUESTIONS_P4_LANE_20260816.md` / `RACE_PRESENTATION_BASICS.md`

---

## 1. Q-P4-41 — **A（エンジンが引く）。近い判断ではありません**

**`w` は着順に効く（D-065）以上、レースの結果の一部です。** 描画層が引くのは責務が逆で、**2か所で引けば必ず離れます**（jostle が判定0.06／製品0.25 で離れていたのと同じ形）。

**そして Provably Fair の観点でも決まります。結果に効くものは、シードから結果を作る鎖の中に無ければなりません。** 描画層で引いた乱数は、その鎖の外です。

### 帯は「馬身」ではなく「枠順と着順の相関」で縛ります（**V-18 新設**）

| | |
|---|---|
| ① | **枠順と着順の順位相関 \|ρ\| ≤ 0.10**（枠で決まるゲームにしない） |
| ② | **最内と最外の走行距離差が 4〜12 馬身** |

**②の根拠は幾何**（半径4m 外を2コーナーで約25m ≒ 10馬身）で、**そちらの2回目（4m外・12.7馬身）とほぼ一致**します。

**②に下限があるのは R-16 の検査を通したからです。** 上限だけなら **`w` を全部ゼロにするのが最大余裕**になり、**D-065 が何もしていない状態を通します。**

---

## 2. Q-P4-42 — **エンジンが境界でスタミナを出す。近似は作らない**

> 一度やって向きが逆になっていました（残り200m で順位相関 **−0.653** ＝ **勝つ馬ほどバテて見えていた**）

**これは「精度が足りない」ではなく「符号が逆」です。** そのゲージで仕掛けを判断すれば、**プレイヤーは体系的に間違えます。** そして **C-6 のボットも同じく間違えるので、UI が悪いのかゲージが嘘なのか区別できません。**

→ **位置と同じ扱い**（D-072）:

- **エンジンが `intervention.ts` の内部状態を境界で露出**
- **自馬のみでよい**（§12.6 が「自馬にのみ表示」なので量は増えない）
- **境界の間は描画層が補間**（D-059 と同じ分担）
- **出すのは D-070 の「状態」＝残量と減り方**。`emptyAtMeter` ではない

---

## 3. ★D-062 の再測定について — **結論に同意します**

> 道中の画面ボットは **0.676**、出走表は **0.732**。
> **まだ出走表を上回っていません。圧縮を緩める根拠は、いまはありません。**

**こちらは「道中が情報を持つようになったら圧縮を緩めてください」と書きました。** **そちらは測って「まだ持っていない」と結論しています。**

**裁定の条件節を、条件が成立していないという理由で実行しなかった** — **正しい読み方です。** そして **`w` が入れば数字が動くはずなので、そのときにもう一度測ってください。**

---

## 4. ★カットの画角について — **カットごとに元スプライトを分けてください**

基礎文書 §2 の指摘:

```
参考（3D）  引き 2.7% → 寄り 14.2%   画角の幅 約5倍
こちら      読める最小は 110px 付近（1280 幅で 8.6%）
```

**ピクセルアートの下限（8.6%）が、参考の引き（2.7%）より上にあります。** **D-058 の整数倍（1×/2×）だけでは 2倍しか取れず、5倍には届きません。**

→ **そちらが既に検討している「引き 64/96px・寄り 180px」で正しいです。**

```
64px → 180px = 2.8倍   ＋ 2× ズーム → 最大 5.6倍   ★参考の5倍に届く
```

**元スプライトを2〜3種類持つのは、頭数ぶんではないのでアセット量は増えません**（毛色5種×勝負服の色替えは共通のまま）。**カットごとの元サイズは、シート契約に含めて1頭目で確定してください。**

---

## 5. ★天気（基礎文書 §7）— **作りません**

> **エンジンに天気の入力がありません。正典に無いので勝手に作りません**（照会が要ります）

**照会に回したのが正しく、そして答えは「作らない」です。**

**エンジンは既に `trackCondition`（good / yielding / soft / bad）を持っており、それが走りに効いています。** **天気を別の入力として足すと、`trackCondition` と二重に持つことになります**（P3 で繰り返した「2か所で別々に持つ」の型）。

→ **馬場状態を、画面で「なぜそうなっているか」が分かる形に描いてください。**

- **soft / bad** → 走路が暗い・泥はね・馬体が汚れる（＝雨が降ったのだと分かる）
- **光・空の色は演出として自由**（アートバイブル §2 の状態別ムード）。**ただしエンジンの入力にはしない**

**「雨が降っている絵」は描いてよく、「雨」というデータは持たない、という線引きです。**

---

## 6. 基礎文書について

**受け取りました。この文書は、こちらが持っているべきだったものです。**

特に §2（カット3系統）と §5（動きの作り方）は、**こちらが「演出は自由」と言って手を離していた領域**で、**そこが空白だったから「何を足しても面白くならない」が続いていました。**

**そして付録の #6 は、こちらの線引きの誤りです:**

> 他社製品の参考を憲法違反だと読んだ → **カメラの角度は業界共通の作法**（D-060）。**線を引き間違えたのはこちら**

**D-060 で線を引き直すまで、参考にできるものを参考にできない状態を作っていました。**

---

## 7. 進めてよいもの

| # | |
|---|---|
| 1 | **3カットの静止画**（承認済み・裁定と独立） |
| 2 | **距離別の時間配分**（実装済みとのこと。§4 の表を確認しました） |
| 3 | **枠色8色＋馬番**（承認済み） |
| 4 | **芝の縞を斜めに／横位置の微細な揺れ／カメラのゆっくりした寄り**（§5 の 3・4・5。演出なので自由。**V-16 を悪化させないことだけ**） |

**18頭での確認（§6）は、静止画の段階で一度やってください。** **12頭で決めた大きさが、18頭で重なる可能性があります。**
