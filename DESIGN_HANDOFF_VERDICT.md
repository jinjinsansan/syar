# デザイン・ハンドオフの採否判定

**受領**: `リポジトリの確認事項確認 (1).zip`（33ファイル）
**判定**: ★**ほぼ全面採用。不採用は1件、判断を仰ぐものが3件**

---

## 0. ★結論

**指定した形どおりに届いており、しかも中身が正しいです。**

- 依頼した成果物（`final.png` / `palette.json` / `layers.json` / スプライト / UI仕様 / NOTES）が**すべて揃っている**
- ★**数値で来ています**（「もっと渋く」ではなく `turf-0〜7` の8階調、速度比 0.00〜1.30 など）
- ★**なぜそうしたかが全項目に書かれています**
- ★**アートバイブルと正典の決定（D-057 / D-058）を、こちらより正確に守っています**

### ★互換性（いちばん心配していた点）

```
sprites/horse-gallop.png  1320×140 = 220×140 × 6コマ  ★契約どおり・背景透明
mockup/final.png          1280×720                    ★そのまま基準にできる
palette.json              役割名で 100件超（silk-1〜18 / coat / air / backlight）
layers.json               viewport / horizon / speedBase / layers / horsePlan
```

★**差し替えるだけで反映できます。** コードの構造変更は要りません。

---

## 1. ★向こうが、こちらの違反を見つけています

> `/still` の現行実装は `horseScale * (0.62 + depth * 0.55)` で**非整数倍に縮小していました。**

★**そのとおりです。D-058 違反です。** 「表示は整数倍のみ」と自分で検査まで書いておきながら、
`/still` では**連続値のスライダ**で縮小していました。

★**「馬の大きさ」スライダそのものが、正典違反の入口でした。** これは私の欠陥です。

---

## 2. ★採用するもの（全面）

| # | 内容 | なぜ採るか |
|---|---|---|
| 1 | **8層構成**（空／スタンド／生垣／奥の柵／奥の芝／走路／手前のラチ／ラチ内の芝） | 「のっぺり」の原因を**彩度ではなく層の数**と読み替えたのが正しい。彩度を上げるとアートバイブル P-1 に当たります |
| 2 | **霞**（`$air.alpha` 0.16 → 0.05 → 0） | 線遠近を使わずに奥行きを作る唯一の手。§3 に適合 |
| 3 | ★**倍率は 1× と 2× の2択のみ**（奥2段 1×／手前1段 2×） | **D-058 を守ったうえで厚みを出す**方法。こちらは守れていませんでした |
| 4 | **段ごとに x をずらす**（櫛の歯にしない） | 団子に見せる正解 |
| 5 | **逆光**（馬体を 0.74 倍＋縁に光） | §2「直線は逆光」。★**彩度を上げずにコントラストを出す**折り合い |
| 6 | ★**ゼッケンは絵に描かず、レンダラが固定位置に描く** | 布の大きさがコマごとに 77% ばらつく実測に対する正しい答え。**2桁が必ず収まる** |
| 7 | **速度比 0.00〜1.30**（★ラチ内の芝が 1.30） | 「馬より手前のものが馬より速く流れて初めて、内側にいることが伝わる」— 理由が正しい |
| 8 | **ゴール板をラチの高さに吊る** | 面に置くと馬に隠れる、という指摘が正確 |
| 9 | **実況帯を全幅にしない**（792px） | 「最前面をUIで塗るのは自分で壊す行為」— そのとおり |
| 10 | **ゲージ・合図を左上に集める** | §9・C-6。寄り（2×）で馬が下から入るので下は不可 |

---

## 3. ★不採用（1件）

### 順位表示の「枡」→ **丸に戻す**

向こうは §3「UI は紙。**角丸を使わない**」を根拠に**枡**にしました。**根拠は正しいです。**

★**ただしオーナーの指示は「〇で数字」でした。** そして中継でも丸が使われます。

→ **丸で実装します。** 向こうも「丸のほうがよければ戻せる。そのときは §3 を改訂してください」
と書いているので、★**アートバイブル §3 に「順位表示の丸は例外」を追記する照会**を出します。

**枡の寸法（34px・余白4px・墨の罫線・先頭は金＋冠・自馬は下線）はそのまま使えます。**

---

## 4. 🔴 オーナーの判断を仰ぐもの（3件）

### ① 毛色を入れるか

★**向こうは毛色5種を使っています**（`coat-kage` `kurokage` `kuri` `ashi` `ao`）。

⚠️ **私が毛色を入れて3回壊し、オーナーの指示で戻したばかり**です。
★**ただし向こうのやり方は違います** — **後から画素を塗り替える**のではなく、
**パレットに毛色の階調を持たせている**ので、私が壊した原因（肌・ゼッケンの巻き込み）が起きません。

**入れますか、外しますか。**

### ② 平時の彩度を上げるか

向こうの唯一の調整点:

> **もし平時の彩度も上げてよいなら、`turf-2〜5` を1段上げるだけで済みます**

★アートバイブル P-1（平時に高彩度を使わない）に当たるので、**上げるならアートバイブルの改訂**が要ります。

### ③ 馬の頭数と段数

`final.png` は **12頭・3段**（奥5・中4・手前3）です。**この構成でよいですか。**

---

## 5. 実装の順序（採用分）

1. `palette.json` を役割色として取り込む（`canvas-renderer.ts` の直書きを置換）
2. `layers.json` を背景の層に反映（速度比・高さ比率・霞）
3. スプライトを差し替え（★220×140×6・契約どおりなので `bake-sprites` はそのまま）
4. ゼッケンを**レンダラ側の固定位置描画**に変更（`dress.mjs` の布探索をやめる）
5. UI（順位表示・実況帯・ゲージ・ゴール板）を `ui/spec.md` の寸法で
6. ★**`/still` の「馬の大きさ」スライダを撤去**（D-058 違反の入口）

★**1〜3 だけで見た目の大半が変わります。** そこまでやってから、また見ていただくのが早いと考えます。
