# カットの境目の方向反転と、発走直後の横滑り — 裁定

**発行**: 2026-08-27（レビュー側 Claude）
**対象**: `QUESTIONS_P4_CUT_SEAM_20260827.md` / ブランチ `p4/race-30sec-cuts` / HEAD `2b2aa3d`
**先行裁定**: `REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md` / `REVIEW_LANE_GATES_VERDICT_20260821.md`

**判定**: ★**Q-1 は案 A。ただし `8c7203d` の revert 理由の解明が着手の前提条件です。案 C は 8/21 裁定 §7-② で既に却下されています。Q-4 の凍結は、Q-1 に関しては最初から対象外でした（反転は γ に依存しないことを実測で確認）。Q-5 は承認。Q-2 / Q-3 はオーナー判断。**

---

## 1. 照合（R-13） — 引用は全件一致

★**開発側が「検証した」と書いた行は、こちらでも当たりました。**

| 照会文の主張 | 実ファイル | 判定 |
|---|---|---|
| `verify-camera-continuity.mjs:58` が同一カット同士だけを判定 | `prev.id === cur.id`（58 行目） | ✅ |
| 「注視点が 2.51m 跳んで画面が 237px 飛んだのを見逃した」 | 同 70-84 行のコメント | ✅ |
| `SETTLE_M = 250` | `lane.ts:22` | ✅ |
| `LANE_REVEAL_FULL_RUN = 0.18` | `lane.ts:188` | ✅ |
| `DEMO_CONTEST_GAMMA = 1.3` | `page.tsx:392` | ✅ |
| v2 では `drawStartingGate` を通らない | `page.tsx:1528`（`renderer !== 'v2'` 条件） | ✅ |
| `startHorseVisualAt` の `laneY = [452, 482, 512]` | `race-intro.ts:78` | ✅ |
| ラチを馬より手前か奥かで区間ごとに振り分ける機構 | `world-textured.ts:298〜`（点ごとに判定） | ✅ |
| 枠色 緑 `#148c46` / ピンク `#f596be` | `apps/web/public/art/palette.json:286,288` | ✅ |
| 新道具に合否閾値が無い（事実 2 つのみ） | `audit-cut-seam.mjs:218-236` | ✅ |

### ★§0 の自己訂正について

★**適切です。** 申し送り「**ソースを読んだ結果を『実測』と呼ばないこと**」は、この案件の規律として正しく、R-30（測定器が画面と同じ入力を読んでいるか）の**手前にある一段**を埋めるものです。

★**私も今回これに服しました。** `reference-hud.test.ts:21` の枠色が `palette.json` と全 8 色ずれているのを見つけ、「同じ値を 2 か所で持って離れた」の 6 件目として報告しかけましたが、**確認したところテストの入力パラメータでした**（描画呼び出しの順序を見る検査で、色は任意の入力）。**報告しません。**

---

## 2. ★発見1 — 未申告の変更が 1 件・その中に前便の訂正が埋もれています

照会文は「★**製品コードは 1 行も変更していません。追加したのは読取専用の道具 1 本（`tools/audit-cut-seam.mjs`）だけです**」としていますが、作業ツリーの実態は:

```
 M tools/lib/classification.mjs      ← ★申告されていない
?? tools/audit-cut-seam.mjs
```

★**変更内容そのものに問題はありません。** 登録簿への追記であり、R-23 が要求している当のものです。製品コードでない点も正しい。

⚠️ ★**問題は、その diff の中に、照会文に書かれていない訂正が入っていることです。**

> `_gammaprobe.mjs` は 2026-08-27 の前便で作られ、**登録されていませんでした**。
> そのためテストは赤のままで、**前便の「1181 件 PASS」は作成時点で失効していました**（R-23）。

★**これは前便（γ 決定の便）の検証看板が失効していたという訂正です。** レビュー側はその便の数字を受け取る立場なので、**コードコメントではなく照会文の §3 に置かれるべきものでした。**

→ ★**§3 に 4 件目として追記してください。** 併せて、`classification.mjs` の変更も申告に含めてください。

★**一般則**: **申告は「製品コードか否か」ではなく「作業ツリーの `git status` に出るもの全部」で書くこと。** 判断の分岐を申告者側に置くと、今回のように**判断そのものが漏れます**。

---

## 3. ★発見2 — Q-2 の前提は使ってよい。ただし理由が違います

`lane.ts:188` の直上に、照会文が引いていない但し書きがあります。

> ⚠️ ★**2026-08-21 現在、この値はまだ検証が通っていません（暫定）。**
> **帯を出たら 0.35 / 0.50 / 0.70 に落とすか、1.0（変更前）へ戻します。**

★**このコメントは、同じ日に出た裁定に反しています。** `REVIEW_LANE_GATES_VERDICT_20260821.md` §6:

> **V-17 が外れても、それは 0.18 を否定する理由になりません。写像を直せば済みます。**

★**0.18 が 0.35 や 1.0 へ差し戻される筋は、裁定上すでに閉じています。**
→ ★**Q-2 の前提（200m 地点で 7.6〜9.3m）は、そのまま使って構いません。**

### ⚠️ 付随して 2 点

1. ★**裁定 §6 の条件（V-17 を縮小条件で測る）を実施した記録が見当たりません。** 正典の決定ログにも 0.18 の確定は入っていません（D-080 まで確認）。`lane.ts` は `7cff0ad` 以降変更されていません。
   → ★**P4 クローズ時の全量実行に含めてください**（裁定 §2 の指示どおり。この変更専用の実行は要りません）。
2. ★**コメントが裁定より悲観的なまま残っているのが危険です。** 将来のセッションがこれを読んで 0.18 を差し戻せば、**そのとき組んだカメラ設計ごと崩れます。**
   ★**R-25 と同型の事故**（文書がその時点の裁定より遅れており、読んだ側が古い方針で動く）。
   → ★**コメントの是正を Q-1 の実装便に同梱してください。** 内容は「裁定 2026-08-21 §6 により、V-17 の結果は 0.18 を否定しない。帯を出た場合は写像（D-064）で吸収する」。

---

## 4. ★発見3 — 反転の機構は、ソース内の既存実測で説明がつきます

★**「境目は設計上一度も測られていない」は、道具の話としては正しい**（`verify-camera-continuity.mjs` は境目のコマを捨てる）**が、角度としては既に記録されています。**

`broadcast-v2.ts` の SCRIPT_V5 コメント:

| カット | 画面上の向き | 出典 |
|---|---|---|
| `side-drive` | ★**87〜89°** で一定 | `broadcast-v2.ts:837` |
| `fourth-corner-front` | ★**156.8°〜178.7°** | 同 :828-833 の掃引表 |

★**90° をまたぐので、画面上の進行方向が反転します。これが機構です。**

→ ★**新道具の発見と既存の実測は矛盾せず、互いに裏を取り合っています。** これは良い状態です。
→ ★**台本 v5 でも同じ 2 箇所で反転するのは構造的な必然**です（v5 も 87-89° → 157-179° → side 系を通る）。**v6 が持ち込んだ問題ではない**という開発側の判断は正しい。

---

## 5. 裁定 — Q-1（4 角の方向反転をどう直すか）

### ★案 C は却下します。再照会は不要です

`REVIEW_LANE_GATES_VERDICT_20260821.md` §7-② が既に答えを出しています。

> **★「後方・俯瞰は 5 戦 5 敗」。5 連敗は偶然ではありません。**
> **→ 6 回目を試す前に、その画角を落とす判断をオーナーに諮ってください。**
> **足りないぶんを、負け続けている画角で埋めようとしないこと。**

★中立カットを俯瞰で作る案は、この裁定に正面から当たります。**採りません。**

### ★案 D は根治ではありません／案 B は見せ場を失います

いずれも開発側の自己評価に同意します。**D は反転が残り、B は 4 角の絵が消えます。**

### ★案 A を採ります — ただし前提条件付き

★**反転そのものが消え、`e009b34` で得た「正面の絵」を失わない**という開発側の推奨理由は妥当です。

⚠️ ★**しかし、開発側が特定できていないリスクがあります。**

`SHOTS['fourth-corner-front']` の掃引表（`broadcast-v2.ts:216〜`）を読むと:

```
  カメラ   168°からのずれ   反転   馬高比(始→終)
   30m(旧)     17.7°       ★1 回    21% → 26%
   60m         12.0°       ★1 回    21% → 21%
  ★90m(新)   ★11.1°        なし     21% → 21%
   120m         14.4°        なし     21% → 21%
```

★**いまカット内で反転が起きていないのは、カメラを 90m 先 / 横 22m に据えたからです。** そして —

- `aa52b70`（**反転判定をカット開始時に 1 回だけ行い、カット中は変えない**）は
  ★**`8c7203d` で revert されており、revert の理由がコミットメッセージに一切書かれていません**
  （本文は revert の定型 1 行のみ。`face-flip.test.ts` 77 行も一緒に消えています）
- ⇒ ★**現在の木は、反転を毎コマ判定しています。**
  カット内で裏返らないのは、**カメラ幾何がたまたまそれを起こさない位置にあるから**です
- ⇒ ★**案 A でカメラを反対側へ移すと、その幾何が崩れ、`aa52b70` が消したはずの
  「背景はそのままで馬の絵だけが 1 コマで裏返る」が戻りえます。**
  ★オーナー評「**滑らかに曲がっていない。かくかく曲がっている**」の再発です

### ★着手の前提条件

★**`8c7203d` が `aa52b70` を revert した理由を明らかにしてください。**

- **カメラの据え直し（90m/22m）で不要になったから revert した** のであれば、
  ★**案 A はその前提を壊すので、`aa52b70` の復活が案 A の一部になります**
- **別の理由（不具合・副作用）があった** のなら、★**その内容を報告してください。**
  そのときは案 A の実装形を変える必要があります

★**理由が判明するまで、案 A の実装に着手しないでください。**
★**この案件では「消えたはずの跳びが戻る」形が既に 3 回起きています**（注視点 237px / 画角 13°→6° / 今回の反転）。
理由の分からない revert の上に同じ領域の変更を積むのは、4 回目を作る形です。

---

## 6. 裁定 — Q-1 の閾値依頼（跳び px の許容量）

★**今回は定めません。**

★**依頼そのものは正しい**（R-16 を踏まえ、測る側が線を引くことを避けている）。**しかし、いま線を引くべきではありません。**

- ★**反転という「符号の事実」で、既に結論が出ています。** 閾値は要りませんでした
- ★いま px の許容量を決めると、道具が「**反転を消したあとの残差**」を測る器具に変質します。
  ★**まだ見ていない分布に対して先に線を引く形**であり、
  `LANE_REVEAL` の掃引で「**先に値を決めて後から正当化しない**」と釘を刺したのと同じ誤りになります

### ★順序

```
① 反転を消す（Q-1 の案 A）
      ↓
② 残った跳びの分布を測る（seed を増やして・最終 γ で）
      ↓
③ ★その分布を持ってくる。そこでレビュー側が線を引くか、引かないかを判断する
```

★**②の分布を提出してください。** 中央値・分位・最大と、**どのカット境界で出ているか**を添えること。

---

## 7. 裁定 — Q-2（発走直後の収束を正面で 12 秒見せる）

★**「エンジンには触らない」は正しい切り分けです。** §0 の実測（同期性 0%・内向き 59%・進路角 中央 0.23°）を受け入れます。
★**`RACE_RANDOM_K` にも `laneAt` にも触れません。この照会は較正を一切開いていません。**

### ★旧注記（序盤に真横を使わない）は失効しています

`SCRIPT_V3` の注記「12 頭の横の広がりが 0.85m しかなく、オーナー評『競艇のボートみたいな姿』」は
★**`LANE_REVEAL_FULL_RUN` を 1.0 → 0.18 にする前の実測**です。
★**いまは 200m 地点で 7.6〜9.3m。旧注記を理由に序盤の横・斜めを禁じません。**（前提の妥当性は §3 のとおり）

⚠️ ★**ただし 8/21 裁定 §7-② が退けているのは「俯瞰」と「後方」です。**
★**真横（案 A の切り先）と斜め後方（案 B）はその対象外**なので、**案 B は禁じられていません。**

### ★案の選択はオーナー判断

★**Q-1 と同時に判断する**という開発側の整理は妥当です（A / B はいずれも境目を増やすか動かす）。
★**レビュー側からの制約は 1 つだけ**: ★**新設・移動した境目は、必ず `audit-cut-seam.mjs` に掛けて
反転と共通被写体 0 頭を確認してから提出すること。** 直したものが別の境目を作る形を残さない。

---

## 8. 裁定 — Q-3（ゴール前リプレイの新設）

★**範囲の判断はオーナーです。** レビュー側からは [CODE] で言えることだけ置きます。

- ★**本編の後に付けるリプレイは、着順・走破タイム・着差・払戻に触れません。** 本編の時間軸を変えないため
- ★`climax-choreography` が不合格なのは、**馬そのものを動かして見かけ速度が +13.3% / −14.8% ずれた**ため。
  ★**リプレイは本編の外にあるので、同じ形にはなりません**（同じ轍を踏まないという開発側の意図は正しい）

⚠️ ★**併せて片付けるべきこと**: ★**不合格の `climax-choreography` が、いまも v5 の既定に入ったままです**
（`HANDOVER_P4_GAMMA_DECISION_20260827.md` §5-4）。★**「不合格の状態が既定」は R-27 に反します。**
★**Q-3 の可否とは独立に、既定から外すかどうかをオーナーに諮ってください。** いつでも実施できます。

---

## 9. 裁定 — Q-4（カメラ凍結は解除されたか）

★**Q-1 に関しては、凍結は最初から対象外でした。** これは実測で決着します。

`balance.ts:435-440` の写像:

```
r_i = (S1 - Si)/S1
total     = r_last × baseTime × TIME_GAP_FACTOR
timeGap_i = total × ( r_i / r_last )^γ
```

★**1 着では `r = 0` なので `timeGap = 0`。γ によらず先頭馬のタイムは動きません。**
★カット境界は台本の**進捗率**（`until: 0.540` ＝ 864m）で定義され、**先頭馬がその距離に達した時刻**で切り替わります。
→ ★**境目の実時刻は γ に依存しません。反転は純粋なカメラ幾何です。**

★**したがって Q-1 は、オーナーの「主軸 1.6」を待たずに着手してよいものでした。**

### ★凍結が本当に掛かっていた量

★**Q-4 の理由（γ でカット境界の最適位置が動く）が当たるのは、次の 2 つです。**

| 量 | γ 依存 | 扱い |
|---|---|---|
| 走行方向の反転 | ★**しない** | ★凍結の対象外。いま直してよい |
| 共通被写体の跳び px | ★**する**（後続馬の位置が動くため） | ★**γ 確定後に測り直す** |
| 共通被写体の頭数 | ★**する**（同上） | ★**γ 確定後に測り直す** |

★**この 2 つは、γ が確定してから測り直してください。**

### ★`SCREEN_GAMMA = 1.3` の二重管理（自己申告）について

★**今回の結論は汚れていません**（反転は γ 非依存）。★**自己申告は適切です。**

⚠️ ただし上の 2 量には効きます。
→ ★**根治までの運用**: **`--gamma` を常に明示すること**（省略時の暗黙値に依存しない）。
→ ★**根治そのものは共通土台（`tools/lib/race-audit-build.mjs`）に触るので別便とし、この便に混ぜないこと。**
　★他の全監査の数字が動きうるため、**単独の便で、前後の差分を出して提出**してください。

---

## 10. 裁定 — Q-5（① を描画層で直してよいか）

★**承認します。他の裁定を待たず着手して構いません。**

- ★① は発走 6.5s の `start-front`、案 A が触るのは `fourth-corner-front` で、★**別カットなので独立です**
- ★描画層に閉じ、着順・位置・払戻に触れません
- ★`world-textured.ts:298〜` の「点ごとに前後を振り分ける」機構は既にあり、**構図の調整で閉じる**という診断に同意します

⚠️ ★**1 点だけ**: ★**直したあと、他のカットのラチの見え方が変わっていないことを確認してください。**
あの機構は `s` ごとに判定するので、**構図を変えると別の区間で前後が入れ替わりえます**。

---

## 11. ④（騎手の色）について

★**照会対象外という整理に同意します。** オーナー決定（現状維持）を尊重します。
★**「実際の JRA は枠色が付くのは帽子だけ」という差を伝えたうえで決めていただいた**のは正しい進め方です。
★D-060（枠番の色分けは機能的な作法・意匠は採らない）とも整合しています。

---

## 12. 判定のまとめ

| # | 論点 | 判定 |
|---|---|---|
| **Q-1** | 4 角の方向反転 | ★**案 A。C は却下（8/21 §7-② 既決）。★`8c7203d` の revert 理由の解明が着手の前提条件** |
| **Q-1'** | 跳び px の閾値 | ★**今回は定めない。** 反転を消した後の残差分布を提出すること |
| **Q-2** | 序盤の画角 | ★**旧注記は失効。案 B（斜め後方）も可。** 案の選択は**オーナー判断**。新設した境目は必ず `audit-cut-seam.mjs` に掛けること |
| **Q-3** | ゴール前リプレイ | ★**オーナー判断。** 本編に触れない点は [CODE] で確認。併せて `climax-choreography` の既定除外を諮ること |
| **Q-4** | 凍結の解除 | ★**Q-1 は最初から対象外**（反転は γ 非依存・実測）。**跳び px と共通頭数は γ 確定後に測り直す** |
| **Q-5** | ① の描画層修正 | ★**承認。独立に着手可** |

### ★開発側の宿題

1. ★**`8c7203d` の revert 理由を報告する**（Q-1 着手の前提条件）
2. ★**照会文 §3 に 4 件目を追記**（前便の「1181 件 PASS」失効・`classification.mjs` の申告漏れ）
3. ★**`lane.ts:188` のコメントを裁定に合わせて是正**（Q-1 の実装便に同梱）
4. ★**反転を消した後、残った跳びの分布を提出**（最終 γ で・カット境界別に）
5. ★**V-17 の縮小条件測定を P4 クローズの全量実行に含める**（専用実行は不要）

### ★オーナー判断を仰ぐこと

1. ★**Q-1: 案 A で進めてよいか**（レビュー側推奨 = A・前提条件付き）
2. ★**Q-2: 序盤の画角を A / B / C のどれにするか**
3. ★**Q-3: ゴール前リプレイの新設を P4 の範囲に入れるか**
4. ★**`climax-choreography` を v5 の既定から外すか**（Q-3 と独立・いつでも可）

---

## 13. 申し送り

1. ★**申告は「製品コードか否か」ではなく `git status` に出るもの全部で書く**（§2）。
   ★判断の分岐を申告者側に置くと、判断そのものが漏れる。
2. ★**理由の書かれていない revert の上に、同じ領域の変更を積まない**（§5）。
   ★この案件では「消えたはずの跳びが戻る」形が既に 3 回起きている。
3. ★**符号（事実）で結論が出るときに、閾値を作らない**（§6）。
   ★閾値は「事実で決着しなかった残りの部分」に対してだけ要る。
4. ★**コードコメントが裁定より古いまま残ることは、引継ぎ書が古いのと同じ事故を起こす**（§3・R-25）。
   ★**裁定が前提を変えたら、その前提を持っているコメントも同じ便で直す。**
5. ★**ソースを読んだ結果を「実測」と呼ばない**（開発側 §0 の申し送り）。
   ★**レビュー側も同じ規律に服する。** 今回、テストの入力パラメータを二重管理と読みかけ、確認して取り下げた（§1）。
