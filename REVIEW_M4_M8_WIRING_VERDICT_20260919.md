# 裁定 — M-4 取り下げ／M-8 は「直した」ではない（呼び手 0）／AU-1・NT-4

**日付**: 2026-09-19（夜）
**対象**: 監査 M-4・M-8'／NT-3（期限つき簿）／GB-1 ④ の配線
**発行**: レビュー側

---

## 1. 🔴 M-4 を取り下げます — ★私の見立てが機構と合っていませんでした

監査簿の M-4 は「`STAR_SEED_SECRET` が無くても警告だけで起動する（R-27）」。
★私はこれを写して「直すべき」と書きました。★機構を確かめていませんでした。

✔ 確かめたこと:

| 見たもの | 事実 |
|---|---|
| `apps/worker/src/pg-store.ts:189-196` | ★`seed_commit` と `server_seed` は **同じ 1 本の insert**。原子的 |
| `packages/race-engine/src/fairness.ts:102` | 検証は `sha256(reveal) === commit`。★**両方とも行から読む。秘密を使わない** |
| `secrets.production.env` | 8 変数あり。★`STAR_SEED_SECRET` **のみ無し** |
| `secrets.staging.env` | ★**こちらにも無し**（＝今までずっと起動ごとの乱数） |

→ ★**秘密が毎回変わっても、公正性の検証は通ります。**
→ ★註記が書いている「保存前に落ちたレースは reveal を出せない」は、
  ★**保存前に落ちたレースは存在しない**（同じ insert）。★**起きない害**でした。

**残る価値は弱い**: 秘密を固定する利点は「★DB の行を信じずに seed を再導出できる監査経路」だけ。
★「壊れている」ではなく「あると良い」。

**🔴 そして直す順が逆でした。** fail-closed にすると止まるのは★**本番ではなく staging**
（staging にも変数が無い）。★私は「本番が止まる」を心配して、★**止まる側を間違えていました**。

**裁定**: M-4 は 🔴 → ⚠️ **後回し**へ格下げ。★オーナー期限（2026-09-26）を**外す**。
直すなら順は ①`.env.example`・`deploy/README.md`・両 secrets に変数を足す → ②その後 fail-closed。★逆にしない。

> 🔴 ⚠️ ★**2026-09-19 夜 訂正 — 上の ①「`.env.example` に足す」は不要でした。★既に在ります。**
> ✔ `.env.example:43` に `STAR_SEED_SECRET=` が**在る**（★註記つき）。★無いのは `deploy/README.md` だけ（0 件）。
> ⚠️ 🔴 ★**私は監査の「両方に無い」を、★数え直さずに裁定へ写しました**（**AU-6**）。★**裁定は、簿より強く読まれます。**
> ★開発側も同じ文を簿へ写しており、★**2 人で同じ誤りを運びました。** ✔ ★最後に数えたのは開発側です。
> ⚠️ ★**そして「fail-closed にしない」という結論は変わりません** — ★必須にすると**止まるのは本番ではなく staging**（★どちらの secrets にも無いため）。

⚠️ ★**「本番は動いていない」と結論しないこと。** `pg-store.ts:214` の註記に
「本番の 443 件すべてが good」とある。★PR-3 の前提①（やり直すのか）は**生きたまま**。

---

## 2. 🔴 M-8' — ★直しは良い。★しかし「直した」ではありません

### ✅ 直しそのものは受理

✔ 私も読んで確かめました（`packages/betting/src/point-flow.ts:143-174`）:

- `ppNetHealth()` は `ppIssued <= 0` で `'unmeasured'` を返す ✅
- `isPpNetNotUnhealthy()` は `!== 'unhealthy'` ✅
- `isPpNetHealthy()` は `isPpNetNotUnhealthy()` への**恒等**（172-174 行）
  → ★「真偽値を 1 ビットも変えていない」は ✔ **本当**。★私が読んで確かめました

✅ ★**元の検査の狙い（ゼロ除算しない）を別の検査として残した**のが正しい。★狙いごと捨てない。
✅ ★`@deprecated` の文面が「この名前は『健全か』と読めるが実際は『不健全ではないか』」と
  ★**読み違いそのものを書いている**のが良い。

### 🔴 ただし「直した」という言葉が合っていません

✔ **呼び手を数えました**（`grep` ではなく全件を目で分類）:

| 呼んでいる場所 | 件数 |
|---|---|
| 製品コード（worker / packages） | ★**0** |
| 門・検査ツール（`tools/*.mjs`） | ★**0** |
| 自分の検査（`point-flow.test.ts`） | 8 |

→ ★★**この 3 つの関数は、どの門も製品コードも呼んでいません。**
→ ★だから「真偽値を変えていない」のは当然で、★**変えても何も起きません**。

---

## 3. ✅ 本物の V-11 には穴がありません

✔ `tools/verify-v11-synthetic.mjs:353`:

```js
check(ppIssued > 0 && ppConsumed > 0, ...)
```

→ ★**発行 0 を明示的に落としています。** ★V-11 の実体は最初から fail-closed。

→ ★**M-8 の実害は元から無かった。** 監査は★**誰も読んでいない関数**を指していました。

**裁定**: 簿から M-8 を外すのは **結果としては正しい。★理由が違う。**
- ❌ 「直したから外す」
- ✅ 「★**測っている物が別だったから外す**（本物の V-11 は `verify-v11-synthetic.mjs:353` で塞がっている）」

🔴 ★理由を書き換えること。★次の人が「M-8 は `ppNetHealth` で直った」と読むと、
  ★V-11 を `isPpNetHealthy` に配線しかねない。★**そのとき初めて穴が開きます。**

### 🔴 M-9（新規）— 第二の帳簿

`point-flow.ts` は★どの門も読んでいない「健全」の第二の帳簿（**D-052**）。
★`@deprecated` は良い防具。★簿に載せる形は:

> **M-9** `point-flow.ts` の健全判定は門に配線されていない。
> ★配線するなら `ppNetHealth()`（3 状態）を使う。★`isPpNetHealthy` を**新規に呼ばない**。
> 機械の検査: ★`isPpNetHealthy` の呼び手が `point-flow.test.ts` の外に現れたら落ちる。

---

## 4. 🔴 AU-1 — ★今日 2 度目なので規則にします

**M-4 も M-8 も、★実物に配線されていないコードを指していました。**

> **AU-1** ★監査簿の行を直す前に、★**その関数・その変数を誰が呼んでいるかを数える**。
> ★呼び手が 0 なら、直す対象は**関数ではなく配線**。
> ★「直した」と書く前に、★**何の挙動が変わったかを 1 行で言えること**。言えなければ直っていない。

🔴 ★**残り 12 件の監査指摘にも、先にこれを当ててください。** ★作業を始める前に。
★M-4・M-8 の 2 件が同じ形だったということは、★同じ簿の他の行も同じ可能性があります。
★数えるのは安く、直すのは高い。

---

## 5. ✅ NT-3 を受理 — 🔴 ただし 2 点

✅ 期限で門が落ちる形にしたのが正しい。★「読まれなければ消える文書」でなくなりました。
✅ CI-5 を検査で固定した（物の理由は載せてよく、機械の理由は載せてはいけない）— ★そのとおり。
✅ 落ちる側も試した（未来日／「調査中」／担当なし／id 重複）— ★R-16。

🔴 **① M-4 の行を格下げ**（オーナー期限 2026-09-26 を外す・理由は §1）。
🔴 **② M-8 の行の理由を書き換えて外す**（§2）・★**M-9 を足す**（§3）。

### 🔴 NT-4 — ★あなたが挙げた弱点への答え

> 「`known-red` と違い、**「直ったか」を機械が判定できません**」

★それは **行ごとに「生きている証拠」を機械が見られる形で持たせれば**塞がります。

| 指摘 | 機械が見られる「まだ生きている」条件 |
|---|---|
| M-4 | `secrets.*.env` に `STAR_SEED_SECRET` が無い |
| M-9 | `isPpNetHealthy` の呼び手が検査の外に在る |
| AUDIT-SERVICE-ROLE | 当該の鍵名が当該ファイルに在る |

> **NT-4** 簿の各行に、任意で **`stillOpen`**（機械が確かめられる述語）を持たせる。
> ★在る行は、★**条件が偽になったら「消し忘れ」として落とす**。
> ⚠️ ★**全行に強制しない。** 書けない指摘のほうが多い（★書けないものを無理に書かせると、
>   ★通るだけの述語が置かれる — ★今日の「緑の理由が違う」の作り方そのもの）。

---

## 6. ✅ GB-1 ④ の配線を受理

✔ 前提を確かめました: **`horses.stats` を書く場所は 1 か所だけ**
（`apps/worker/src/training-runner.ts:406`・週送り）。
★SQL 側で書かないことは既に検査済み（`stable-grade-unlock-migration.test.ts:64`）。
→ ★**GB-1 ⑥（初回の基準＝取得時の値）の前提は成立**しています。

✅ GB-1 ⑤ の「言わなかった週は読んだ値をそのまま戻す」を**枝の検査**で見ているのが良い。
✅ 3 つの RPC に書かない＋全移行で見張る（D-052）✅ 片方だけの状態を作れない CHECK
✅ 公開ビューに出さない（D-114）✅ 標準出力に能力名を出さない（符号列を作らない）

🔴 ★**`unnest($14::jsonb[], $15::bigint[])` の並びの危うさを自分で挙げたのが良い。**
★`fillRace` の `$1..$19` と同じ形です。★型検査でも偽の DB でも出ません。
→ ★実 DB で 2 頭を一度に書いて混ざらないことを見た、というのが**唯一の守り**です。
  ★それを**検査として残す**こと（★一度 通っただけでは、次に列を足したとき黙って崩れます）。

---

## 7. ✅ 道具に 8〜10 回 止められたこと

★これは良い数字です。★そのうち 2 回が「★あなたが今日 足した検査」に止められたもの。
★道具が作者を止めるのは、★道具が作者より広く見ている証拠。

---

## 順

1. 🔴 **AU-1 を残り 12 件に当てる**（★直す前に・数えるのは安い）
2. 🔴 **簿の 3 点**（M-4 格下げ／M-8 の理由書き換え／M-9 追加）
3. **NT-4**（`stillOpen`・任意）
4. **GB-1 ④ の `unnest` 並びを検査に**
5. CI-8 → `SERVICE_ROLE_KEY` / `RACE_RANDOM_K` の写し → `.tmp-*`

🔴 **オーナー待ち**: CI-1 ／ CC-1 ＋ PO-2 ／ WK-5 ／ VP-4' ／ PR-3 ／ 撮り直し 2 本 ／ 1.68 GB
⚠️ ★**M-4 はオーナー待ちから外しました。**
