# 報告 — 馬場の調整（照りの追加 / 濃さの確定）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-08-30 ／ 開発側 → レビュー側・オーナー
**指示**: オーナー「★**馬場の調整を開始してください**」（引継ぎ書 `HANDOVER_P4_TRACK_CONDITION_20260830.md` §1）
**ブランチ**: `p4/race-30sec-cuts` ／ **起点** `309f2a1` → **HEAD** `309d326`（★3 コミット）

> ★この報告は「★**再現した事実**」と「★**未検証の主張**」を分けて書きます（R-13）。
> ★映像の合否はオーナーの目だけが決めます。★測って言えることには「実測」と書きます。

---

## ★0. 結論（3 行）

| | |
|---|---|
| ★**濡れた馬場の濃さ** | ★**現行（0.22 / 0.42 / 0.55）で確定。** ★**オーナーが開発サーバーの実画面で確認**（§2） |
| ★**照り**（濡れた面が空を映す） | ★入れました。★ただし ★**正面のカットでは目に見えません**（残件 **A-7**・§5） |
| ★**併せて見つかったもの** | ⚠️ ★**`--sec 32` では測れません**（カットの境目・残件 **B-2**・§6） |

---

## ★1. この便のコミット（3 本）

```
875aaaf  馬場: 濡れた走路が空を映す（照り）を入れる — 暗くするだけでは「濡れて」見えない
eaa2e83  台帳: A-6 を外し、A-7（照りが正面では見えない）と B-2（32 秒は測れない）を立てる
309d326  道具: 比較シート（_condsheet.mjs）を登録簿へ
```

⚠️ ★`apps/web/package.json`（ポート 3210）は ★**3 コミットとも混ぜていません**（オーナーの既存作業）。

---

## ★2. ★オーナー判断（★この便でいちばん重要な行）

★引継ぎ書 §8 ① は「★**馬場の濃さ** … ★オーナーは 1 つ前の弱い版しか見ていない」でした。

| | |
|---|---|
| ★見ていただいたもの | ★**開発サーバーの実画面**（`http://localhost:3210/race?surface=…&condition=…`）<br>★較正 **0.22 / 0.42 / 0.55**（良は 0）＋ ★照り 0.18 |
| ★併せてお渡ししたもの | ★`out/_sheet/dirt-side-by-side.png`（★左＝現行 / 右＝濃い案 0.30 / 0.55 / 0.66） |
| ★**ご判断** | ★**「これで OK です」** |
| ★したがって確定したこと | ★**濡れた馬場の濃さは現行のまま。** ★濃い案は**入れません**（撮影後に戻し、`git status` はきれいです） |

⚠️ ★**「濡れて見えるようになった」というご判断はいただいていません。** ★確定したのは**濃さ**だけです。
　★A-7（濡れの読み）は**開いたまま**です。★この 2 つを混ぜて記録しないこと。

---

## ★3. 何が足りなかったか（照りを入れた理由）

★濡れた馬場は「★**暗くする**」だけでした（`trackWetnessAlpha`）。
★暗いだけの地面は ★**「日が翳った」ようにしか見えません。** ★濡れた面が濡れて見えるのは
★**面が空を映すから**で、★暗さとは**別の量**です。

---

## ★4. 入れたもの（`875aaaf`）

| # | 何を | どこ |
|---|---|---|
| ① | ★`trackGlossAlpha`（★Schlick 近似・水の R0 ≈ 0.02） | `world-textured.ts`。★**式は我々で作っていません** |
| ② | ★正面（テクスチャ世界）に照りの層 | `drawTexturedWorld`（`trackBand` を深さ関数対応に） |
| ③ | ★横（焼き込みの板）にも**同じ関数から** | `parallax-plate.ts` の `glossAt` ／ `broadcast-v2-scene.ts` |
| ④ | ★戻し口 `/race?gloss=0` | `TRACK_GLOSS_DEFAULT` / `trackGlossFromSearch`（D-085） |
| ⑤ | ★`HORIZON_SKY_COLOR` | ★霞（空気遠近）と地面が映す空の色を**1 か所**に（D-052） |

★向きは「★**浅く当たるほど強い**」なので、★透視図では**奥ほど光り、手前は暗いまま**になります。

### ⚠️ 4-1. ★危うくもう一度「何も変わっていません」を言われるところでした

★係数を最初 **0.55** と置いて実画面で測りました:

| 良 → 重（走路が写る帯） | |
|---|---|
| ★照りなし | **−23.1%** |
| ★照り 0.55 | **−6.6%** |

⚠️ → ★**照りが、濡れの暗さをほとんど打ち消していました。**
★そのまま出せば、★オーナーが 1 度言われた「★**何も変わっていません**」を**もう一度**言われる形です。

→ ★較正条件を「★**濡れの暗さを 8 割方は残す**」に置き直し、★**0.18** にしました（R-7）。

| | |
|---|---|
| ★良 → 重 | **−18.2%**（★照りなし −23.4% の **78%** を維持） |
| ★照りが足す明るさ | ★奥の走路 **+6.8%** ／ ★手前 **+1.6%**（＝奥ほど光る・約 4 倍の差） |

★この関係は検査で固定しました（`track-gloss.test.ts`「★照りが濡れの暗さを打ち消さない」）。
⚠️ ★**係数を見張るのではなく、量どうしの比**で見ています（フレネルの形を変えて同じことが起きても捕まります）。
★**0.55 に戻すと落ちること**を確認済みです。

---

## ★5. ⚠️ ★解決していないもの — 残件 **A-7**（濡れて見えない）

★照りは入りましたが、★**正面のカットでは目で分かりません。**

| ★照りが足す明るさ | 奥 | 中（走路） | 手前 |
|---|---|---|---|
| ★**横**のカット（35s `straight-contest`） | +2.8% | **+6.8%** | +1.6% |
| ⚠️ ★**正面**のカット（14s `first-corner-front`） | +1.5% | **+1.3%** | +0.8% |

★正面の 2 枚（`out/_real/f14a.png` / `f14on.png`）を開発側の目で見比べましたが、★**差が分かりません。**

### ★なぜ強くできないか

★**暗さと照りは取り合いです**（§4-1）。★係数を上げれば見えますが、★濡れの暗さが消えます。

### ★なぜ一様になるのか（★機構の限界・推測ではありません）

★フレネルは「浅く当たるほど強い」ですが、★**画面に写る走路は 30m 先で既に十分浅い**（入射角の余弦 0.16）ため、
★奥と手前で **2 倍程度**しか変わりません。★**遠近で光り方が分かれる形になりません。**

→ ★**一様な層では「濡れ」になりません。** ★次に効きそうなのは ★**局所的な手がかり**
（★水たまり・跳ね返り・轍の照り）です。⚠️ ★**演出の裁定事項**なので、★オーナー判断待ちです。

---

## ★6. ⚠️ ★測定器の話 — 残件 **B-2**（`--sec 32` では測れません）

★較正の途中で見つけました。★**機構ではなく、測る秒の選び方の問題**です。

| | |
|---|---|
| ★実測 | ★`--sec 32` で**同じ URL を 2 枚**撮ると、★帯の平均輝度が **−6.5 / −16.3 / −12.5**（★最大 **20%**）動く |
| ★行ごと | ★「変わった行 **682/720**・最大 **+50**」。⚠️ ★**照りの入/切（700/720・最大 +51）と見分けがつきません** |
| ★なぜ | ★`homestretch-front` は **1312〜1392m の 80m しかありません**（`tools/_cuts.mjs`）。★32 秒はその**境目** |
| ★安全な秒 | ★**14 秒**（`first-corner-front`・100〜528m）と ★**35 秒**（`straight-contest`）は ★**2 枚が 1 バイトまで一致** |

⚠️ ★引継ぎ書 §6 の再実行手順が `--sec 32` です。★あの表（芝 100/89.1/81.8）が再現したのは
★**2 回とも同じ側に落ちたから**であって、★保証はありません。

⚠️ ★**併せて**: ★`_condcmp.mjs` の「良と比べて沈んだ行だけ」という見方は、
★**照りを見ることができません**（照りは沈んでいない行を明るくするため・R-22）。
→ ★`tools/_glosscmp.mjs` を足しました（★帯の平均で見る・★**先に揺らぎを測ってから信号を読む**）。

---

## ★7. ★訂正したもの（引継ぎ書 §3-6「註釈は嘘をつく」）

★`trackWetnessAlpha` の註釈に、こう書いてありました:

> 【★狙い（★本物のブラウザの画面での**実測値**）】★稍重 ≈ 83% ／ 重 ≈ 68% ／ 不良 ≈ 59%

⚠️ ★**実測値ではありません。** ★実測（`--sec 32`・`_condcmp`）はこうです:

| | 良 | 稍重 | 重 | 不良 |
|---|---|---|---|---|
| 芝 | 100% | 89.1% | 81.8% | 77.2% |
| ダート | 100% | 88.5% | 80.4% | 74.8% |

★層の濃さ（0.42）から素朴に期待する 68% に届かないのは、★上に**縞刈り・砂煙・馬**が乗るためです
（★正面のカットで縞刈りが芝の緑のままなのは ★**残件 A-5**）。★註釈を直しました。

---

## ★8. ⚠️ ★開発側が外したこと（★同じ穴に落ちないための記録）

### 8-1. ★全画面を並べて、明るさの順番を読み違えました

★比較シートの 1 枚目は**全画面を 4 枚横に並べた**ものでした。★それを見て
★**「重が稍重より明るい」と読みかけました。** ★実測は単調です（§9）。

★原因は 2 つで、★**どちらもシート側の欠陥**でした:

| | |
|---|---|
| ① | ★画面の大半が **HUD・観客・空**で、★地面はその一部でしかない |
| ② | ⚠️ ★**4 枚は同じ瞬間ではありません。** ★馬場状態は**レースの結果を変える**ので、<br>★同じ秒で撮っても**残り距離が違います**（★実測 **1121 / 1129 / 1141 / 1152m**） |

→ ★`--strip` で ★**走路だけを切り出し、隙間なく縦に積む**形にしました。

⚠️ ★**②は `_condcmp.mjs` の数字にも効いています。** ★良と重を同じ秒で比べるとき、
★**厳密には別の瞬間を比べています。** ★走路の帯の平均という見方では支配的ではありませんが、
★**奥の帯では順序が乱れます**（実測 −2.8% / −17.0% / −12.9%）。★奥の帯だけで判定しないこと。

### 8-2. ★撮影中に製品コードを触りました

★変異試験（係数を 0.55 に戻して検査が落ちるか）を、★**背景で撮影が走っている最中に**実行しました。
★dev サーバーは即座に再構成するので、★**撮影に混ざりえます**（R-18 の家族）。
→ ★その場で気づき、★**同じ URL を撮り直して 1 バイト一致を確認**しました（混ざっていませんでした）。
→ ★**撮影中は製品コードを触らないこと。**

### 8-3. ★和字が豆腐（□）になりました

★`@napi-rs/canvas` は、★書体を登録しないと和字が出ません。★見出しが全部 □ になりました。
→ ★環境の和字書体を探して登録し、★**無ければ英字に落とす**ようにしました（黙って豆腐にしない）。

### 8-4. ★`perl -0pi -e` が識別子を食いました

★`${FONT}` を含む行を perl のワンライナーで書き換えたところ、★**変数が消えた行**が入りました
（`ctx.font = 'bold 22px ';`）。★この案件が正典に壊れた行を書き込んだのと**同じ形**です。
→ ★**日本語・識別子を含む行の機械編集はエディタで行うこと。**

---

## ★9. 実測（★再現できます・すべて読取専用）

★dev サーバーを立ててから:

```powershell
cd apps/web ; npm run dev            # ★別の窓（ポート 3210）

# ★撮る（⚠️ ★秒はカットの内側に。32 秒は不可・§6）
npx tsx tools/_realshot.mjs --out now-dirt-good --query "surface=dirt&condition=good" --sec 14
npx tsx tools/_realshot.mjs --out now-dirt-soft --query "surface=dirt&condition=soft" --sec 14

# ★揺らぎ → 信号 の順で読む
npx tsx tools/_glosscmp.mjs --off out/_real/now-dirt-good.png --on out/_real/now-dirt-soft.png

# ★並べて見る
npx tsx tools/_condsheet.mjs --prefixes now --labels 現行 --out cond-strip --strip
```

### 9-1. ★馬場状態の濃さ（★走路が写る帯・良を基準に何 % 暗いか・14 秒）

| | 稍重 | 重 | 不良 |
|---|---|---|---|
| ★**現行 0.22 / 0.42 / 0.55**（★確定） | **−10.2%** | **−22.0%** | **−29.1%** |
| ★濃い案 0.30 / 0.55 / 0.66（★入れていません） | −14.2% | −28.2% | −34.6% |
| ★芝・重（参考） | — | ★現行 **−19.8%** ／ 濃い案 −26.1% | — |

### 9-2. ★照り（35 秒・横のカット）

| | 良 → 重 |
|---|---|
| ★照りなし（`?gloss=0`） | −23.4% |
| ★**照り 0.18**（★既定） | **−18.2%** |
| ★照り 0.55（★却下） | −6.6% |

### 9-3. ★測定器の揺らぎ

| 秒 | ★同じ URL を 2 枚撮ったときの差 |
|---|---|
| ★14 秒 | **+0.00 / +0.00 / +0.00**（★1 バイト一致） |
| ★35 秒 | **+0.00 / +0.00 / +0.00**（★1 バイト一致） |
| ⚠️ ★32 秒 | ★**−6.5 / −16.3 / −12.5** |

### 9-4. ★全数

| | |
|---|---|
| `npm test` | ★**130 ファイル / 1281 件 PASS / EXIT=0**（★前 129 / 1267） |
| `npm run typecheck` | ★通過 |
| ★良の絵 | ★**1 ビットも動きません**（`track-gloss.test.ts` で固定） |

---

## ★10. この便で増えた検査

| 検査 | 何を守っているか |
|---|---|
| `packages/render/test/track-gloss.test.ts`（14 件） | ★良は必ず 0／濡れるほど強い／★**奥ほど強い**／★**照りが濡れの暗さを打ち消さない**（★0.55 に戻すと落ちる）／白飛びしない／既定と戻し口／★**入れると実際に画が変わる（正面・横の両方）**／★良は入切で完全に同じ |

⚠️ ★「入れると実際に画が変わる」は ★**機構を外すと落ちること**を確認済みです（R-16 対策）。

---

## ★11. 追加した道具（★どちらも読取専用・登録簿に登録済み）

| 道具 | 何をするか |
|---|---|
| `tools/_glosscmp.mjs` | ★照りの入/切で走路のどの帯がどれだけ明るくなるか。★**先に揺らぎを測る**手順を註記に持つ |
| `tools/_condsheet.mjs` | ★馬場状態を 1 枚に並べる。★`--strip` で走路だけを縦に積む |

⚠️ ★`tools/_*.mjs` は `.gitignore` 対象（追跡外）ですが、★**登録簿には載せます**（`tools/lib/classification.mjs`）。

---

## ★12. ★オーナー判断待ち（★この便のあと）

| # | | |
|---|---|---|
| ① | ★**A-7**（濡れて見えるための局所的な手がかり） | ★**次の一手**。★水たまり・跳ね返り・轍の照り。★演出の裁定事項 |
| ② | ★**A-5**（芝の縞刈りがダートの上にも引かれている） | ★据え置き。★直すとダートの見た目が変わる |
| ③ | ★汚れの濃さ | ★[EYES]（前便からの持ち越し） |
| ④ | ★ダート素材の承認 | ★未取得（裁定 §6-4 条件 2） |
| ⑤ | ★push | ★指示待ち |
| ⑥ | ★決定ログの二重帳簿 | ★未決（前便からの持ち越し） |
| ⑦ | ★正典への起票 | ★この便の決定（★照りの既定・戻し口・較正条件）が**未起票** |

---

## ★13. ⚠️ ★報告に含めていない欠陥（★別便で判断してください）

★`dirt-near` / `dirt-mid` / `dirt-far` の ★**`.webp` が存在しません**（`?v=58` で毎回 404 → `.png` に落ちる）。
★2026-08-29 の焼き直しで `tools/build-art-webp.mjs` を通していないためです。

| | |
|---|---|
| ★表示 | ★**正しく出ます**（`.png` へ落ちる仕組みがある） |
| ★実害 | ★ダートの板だけ ★**約 7 倍重く**読んでいる（★1 回の読み込みで 3 本の 404） |
| ★なぜ直していないか | ★素材を作り直す作業で、★**「ダート素材の承認」（§12 ④）の対象が変わります。** ★勝手に触りません |
