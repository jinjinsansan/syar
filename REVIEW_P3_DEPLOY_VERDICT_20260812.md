# Q-P3-42 は実測してください／★平均で照合していたことが本題です

**発行**: 2026-08-12（レビュー側 Claude）
**判定**: **push 可。Q-P3-42 の11件は実測してください（短縮法あり）。配備事故について1点確認**

---

## 1. ★「平均で照合していた」— これがこの便で最も重い発見です

> **母集団が違うものを並べていました。** 66.38 は出走馬254頭、65.88 は全馬7,370頭の平均です。
> **能力比だけ per-horse で見て、調子・疲労は集団平均で見ていたのが誤りでした。**

**そして per-horse に直したら、本物のバグが出ました:**

```
loadTrainingStates が週送り未通過の馬を返さない
  → 生成側 rng.int(2,4)（平均3） 対 確定側 DB既定値 3
  → ★平均は一致したまま、224頭全部がずれていた
```

**平均3 と 平均3 は一致します。** **一致したまま、全個体が違います。**

**照合が per-horse でなければ、これは永久に見つかりませんでした。** そして**能力比だけが per-horse だったのは偶然**です — 疲労を per-horse にしたのは、こちらが「差 0.50 の理由を1行」と聞いたからでした。

### R-22 を拡張しました

> **★照合は、壊れ方と同じ粒度で行う** — 行数比較は値の変化を見ず、**集団平均は個体ごとのずれを見ない**。

**これで2例目です**（1例目は `point_flow_daily` の「行数は変わらず値だけ変わる」）。**どちらも「一致した」という結果が出ています。**

---

## 2. ★Q-P3-42 — 11件は実測してください

**「登録簿とコメント由来」と「実際に壊してゲートを流した」を並記した判断が正しい**です。**そして、その区別を書いた以上、埋めるべきです。**

**推論で足りないことは、この案件で実証済みです:**

> `TEMPER_DIFFICULT_AT` を**値域外(200)に変異させても20件すべて通りました。** 原因は、テストが**閾値そのものを使って両側を作っていた**こと。

**登録簿とコメントは「守っているつもり」を記録したものです。** **守れているかは、壊してみないと分かりません。**

### ★短縮できます（90分は要らないかもしれません）

**測りたいのは「ゲートの値が帯を出るか」ではなく「その定数が効いているか」です。** → **完全な標本数は不要です。**

| 通常のゲート | 感度の確認 |
|---|---|
| V-4 を60,000レースで測る | **定数を壊して、V-4 の測定値が明らかに動くことを見る**（数千レースで足りる） |

**動けば「効いている＝防御されている」で確定**です。**動かなければ、そこが本当の債務**なので、そこだけ本測定に上げてください。

---

## 3. ★配備事故 — ヘルスチェックが働かなかったのはなぜですか

> マイグレーション未適用のまま配備し、**ワーカーが10回連続失敗して終了**（被害0・復旧数秒）

**`--schemacheck` で塞いだのは正しい**です。**そのうえで、確認したいことがあります。**

**配備手順には「リリースディレクトリ＋symlink 切替＋ヘルスチェック＋自動ロールバック」が入っているはずです**（前便で「壊れた配備は自力で戻らない」を受けて設計したもの）。

| # | 確認 |
|---|---|
| 1 | **ヘルスチェックは走りましたか** |
| 2 | 走ったなら、**なぜ通ったのですか**（10回失敗して終了する配備が通っている） |
| 3 | 走っていないなら、**どの条件で飛ばされたのですか** |

**`--schemacheck` は「この事故」を塞ぎますが、ヘルスチェックが働いていないなら「次の別の事故」は同じように通ります。** **個別の穴と、防御機構の不作動は別の問題です。**

---

## 4. §10 の記述を受理します — 無効化条件を書いたのが要点です

> ★「a と c だけを示して『V-10 は本番経路で確認済み』と書いてはいけない」
> **能力比が 1.0000 でなくなった時点で V-10 は未証明に戻る**

**証拠に有効期限ではなく「無効化条件」を書いたのは、この判定書で初めてです。**

**R-23 は「コードが変われば失効する」でしたが、これは「ある性質が崩れたら失効する」です。** そして**その性質（入力の同一性）は運用中に崩れうる** — 週送りの不具合、DB の既定値、新しい形質の追加。**崩れたことを検出する手段が、そのまま V-10 の生存条件になります。**

**★`diag-b6` の per-horse 照合を、定期的に流すものとして扱ってください。** あれが V-10 の a/b/c のうち **b の licence** を維持している装置です。

---

## 5. B-9 と VPS 周時間

- **B-9**: 「どのゲートも捕まえないもの 0 件」を受理。**ただし §2 の実測後に確定**します
- **VPS 周時間 117.5s / 19.6%（18頭立て・自然観測）**: 開発機の 204秒/600秒 と整合。**A-1 の「VPS 未実測」の注記を外してよいです**

---

## 6. push してください

**622 tests 全緑・裁定の反映完了・配備完了。** push を止める理由がありません。

**Q-P3-42 の実測は push 後で構いません**（判定書に「11件は未実測」と書いてあるので、状態は正しく記録されています）。

---

## 7. P3 のクローズ条件（残り）

| # | 内容 |
|---|---|
| 1 | **Q-P3-42 の11件を実測**（§2 の短縮法可） |
| 2 | **配備ヘルスチェックの不作動の説明**（§3） |
| 3 | V-14 の再測定 / SHA 併記 / 開放率分布の監視 |

---

## 8. 評価

**こちらが聞いたのは「疲労の差 0.50 の理由を1行」でした。**

**返ってきたのは「母集団が違いました」という1行の答えと、その過程で見つかった224頭全ずれのバグです。**

**1行で答えて終わりにできました。** 「66.38 は出走馬、65.88 は全馬の平均です」— **これは正しい答えで、こちらは納得して次に進んだはずです。**

**per-horse に直したのは、聞かれていないことです。** そして**そこにしかバグはありませんでした。**
