# REPORT_P2_A3_POOLED — A-3 がプール推定でも FAIL

**開発側 → レビュー側** / 2026-08-09
**コミット**: `ba74bbf`（push 済み・435 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**なし。** ただし **A-3 について判断をお願いしたい**件があります（§1）。

---

## 1. ★A-3 がプール推定でも FAIL しました

判定方法をご指摘どおり**プール推定**に直し、10シードで測りました。

```
M=4000 / races=32000（races = M × 8）/ 総 320,000 レース

seed 42     82.62%  +0.62pt      seed 101    85.14%  +3.14pt
seed 7      82.26%  +0.26pt      seed 202    84.16%  +2.16pt
seed 2026   81.97%  -0.03pt      seed 303    83.06%  +1.06pt
seed 31337  83.73%  +1.73pt      seed 404    82.40%  +0.40pt
seed 1231   83.67%  +1.67pt      seed 505    84.55%  +2.55pt

★プール推定  83.36%   乖離 +1.36pt
   SD 1.06   SE 0.34   → 0 から 4.0 SE
★V-10（プールで ±1%）: FAIL
```

### ★前便の報告を訂正します

4シードのとき「平均 +0.64pt は SE 0.39 に対し 1.6 SE で、**系統的なずれとは言い切れない**」と
報告しました。**10シードにすると +1.36pt / 4.0 SE で、系統的です。**

サンプルを増やしたら結論が変わりました。前便の「言い切れない」は正しかった
（判定できる精度が無かった）ものの、**そこで止めずに増やしたのが正解**でした。

### 追加6シードが軒並み高いことについて

```
最初の4シード（P0 の検証シード列 42/7/2026/31337）平均 82.64%
追加した6シード（1231/101/202/303/404/505）    平均 83.83%
差 1.19pt（SE 0.56）→ 2.1 SE
```

**シードの大きさとの相関は r=0.146** で、構造は見つかりません。
2.1 SE は示唆的ですが決定的ではなく、**偶然の分割の可能性が残ります**。

★**原因は推測しません。** A-3 では機構を推測しては否定される順序を7回繰り返し、
  推測を止めたことで判定方法の誤りが見つかりました。ここで8回目を出しません。

---

## 2. 判断をお願いしたいこと

事実として言えるのは次の3点だけです。

| | |
|---|---|
| ① | プール推定 +1.36pt は **4.0 SE** で、**系統的**（偶然では説明できない） |
| ② | **全10シードが目標より上**（−0.03 〜 +3.14pt）。下振れが1つも無い |
| ③ | シードの大きさに構造は無い（r=0.146） |

②の「全部上」は、**払戻率が構造的に高く出る**ことを示唆します。
ただしそれが凸性なのか別の要因なのかは、**測らずには言えません**。

考えられる進め方（こちらでは選びません）:

- **A**: M を上げて凸性の寄与を測る（`races ≫ M` を保つため計算量が跳ねる）
- **B**: 判定域の議論（SD 1.06 のばらつきに対し ±1% が達成可能か）
- **C**: 別の切り分け（例: 券種別に見る。単勝以外で同じ向きに出るか）

**C は計算量が増えず、②の「全部上」がどの券種でも起きるかを見られます。**
ご指示があれば C から測ります。

---

## 3. A-1 は順調です

```
12時間経過時点で 生成した周 69 / 稼働中
メモリ 99.8 MB（起動時 95 MB）/ DB 接続 1本 / FD 26個 / 周の間隔 61〜70秒
```

★リーク系の指標は**24時間を待たずに確認済み**です。接続もFDも増えず、
  周の間隔も伸びていません。24時間の残りの価値は
  **番組表の一巡（144レース = 1日ぶん）**にあります。

判定は本日 20:53 以降です。

---

## 4. 待ちの間に実装したもの

### ★§10.2 の開催中止と §9.1 の返還が未実装でした（経済の穴）

```
実測: 購入 3点×1,000EP → EP 97,000
      ① 中止  返還3枚/3,000EP → EP 100,000（全額戻った）
      ② 二重中止 何もしない  → EP 100,000（増えない）
      馬券 refunded 3枚 / EP台帳 refund 3000, bet -3000 / PP 0 / status cancelled
```

★あわせて `payout.ts` の実装漏れを直しました。
  `settle()` は `hit` / `lost` / `refunded` の3状態を返すのに、呼び出し側が
  `if (s.hit) ... else ...` としか書いておらず **`refunded` を無視していました**。
  取消馬を含む馬券が「外れ」になり、客の EP が返りませんでした。**型では捕まらない漏れ**です。

★返還は EP です。PP で返すと EP→PP の変換経路ができ、憲法 §0.2 が壊れます。

### ワーカーのユニットテストを追加（17件）

`payout` / `prize-award` / `horse-repo` は**実 DB の検証スクリプトでしか確かめておらず、
回帰を検出できません**でした。

特に `horse-repo` の「**数値が文字列で返っても数値になる**」は重要で、
`pg` は `bigint` / `numeric` を文字列で返すため、ここが壊れると**能力が NaN になります**。

---

## 5. 合格基準

| # | 状態 |
|---|---|
| A-1 | **計測中**（本日 20:53 判定・リーク系は確認済み） |
| A-2 | **PASS** |
| **A-3** | **★FAIL**（プール +1.36pt / 4.0 SE・§1） |
| A-4〜A-9 | **PASS** |
