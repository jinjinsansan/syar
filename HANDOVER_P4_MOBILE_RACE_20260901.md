# 引継ぎ書 — 次のセッションはここから（2026-09-01・第 2 便）

作成: 2026-09-01 / ブランチ `p4/race-30sec-cuts`
起点: `a119190`（前の引継ぎ書）→ ★**6 コミット**（★すべて push 済み・★未 push 0 本）
前の引継ぎ書: `HANDOVER_P4_MOBILE_20260901.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**

---

## ★0. ★**次の一手は「レース映像を、事前に焼いた素材で配る」です**（★オーナー承認済み）

### ★0-1. ★何が起きているか（★1 行）

★`/race` は ★**開くたびに、その人のブラウザの中で毛色を焼き直しています。**
★そのため起動時に ★**キャンバス 992 枚・2,398MB** を確保し、★**スマホでは開けません。**

### ★0-2. ★いまは「蓋」がしてあります（★直っていません）

★2026-09-01、オーナー決定で ★**触れるがマウスの無い端末（短辺 900px 以下）では
★重い初期化を始めず、案内を出す**ようにしました（`d42225a`）。

| | ★前 | ★後（★実測・本番） |
|---|---|---|
| ★スマホ幅のキャンバス | ★992 枚 / 2,398MB | ★**0 枚 / 0MB** |
| ★スマホ幅の画像 | ★151 枚 / 403MB | ★**0 枚** |
| ★実機（iOS） | ★「このページを開けません」 | ★**「この画面は、いまはパソコン向けです」**（★オーナー確認済み）|

⚠️ ★**蓋であって直しではありません。** ★本筋が済んだら外します。

### ★0-3. ★本筋（★オーナーに説明済み・合意済み）

★**素材は 1 枚も作り直しません。** ★原版はそのまま。★変えるのは ★**毛色を塗る場所**だけです。

| | ★いま | ★将来 |
|---|---|---|
| ★原版（`/art/horse-*.png`）| ★ある | ★**そのまま** |
| ★毛色の塗り分け | ★**利用者の端末で毎回** | ★**こちらで 1 回だけ、事前に** |
| ★端末がすること | ★992 枚焼く | ★出来上がりを読むだけ |

★**この方式はすでにこのリポジトリにあります**: `tools/bake-sprites.mjs`。
★勝負服とゼッケンは ★**既にその方式へ移してあります**（★道具の冒頭に「ブラウザで書き直すと
★必ずズレます。同じ処理で PNG にしてから配ります」と書かれています）。
→ ★**毛色だけがブラウザ側に残っています。** ★新方式の発明ではなく、★通した道の適用です。

### ★0-4. ⚠️ ★**着手前に必ず解くこと** — ★「馬は何 px で描かれているか」

★焼く解像度を下げてよいかは、★**画面での描画寸法**で決まります。★素材は 1536px、
★画面の馬は 200〜300px と見込んでいますが、★**確定していません。**

★2 回測って 2 回とも失敗しました（★§2-1）。★**ここを解かずに解像度を下げないこと。**

---

## ★1. この便で何をしたか（★6 コミット）

| コミット | 中身 |
|---|---|
| `3a60575` | ★**`package-lock.json` の同期** — ★Vercel のデプロイが**1 日以上失敗していた原因**（`npm ci` が `@star/auth` の欠落で落ちていた）|
| `6037774` | ★**TOP のヒーローの切り落とし**を直す（★390px で 96px・360px で 158px 消えていた）＋ 横向き案内 |
| `125cb03` | ★報告書 2 本 |
| `ad47b45` | ★描き得ない素材 **96MB** を読むのをやめる（★`USE_SEPARATED_COMPOSITE = false`）|
| `0bbf032` | ★毛色の焼き込みの一時バッファを 1/16 に（★帯処理）|
| `d42225a` | ★**スマホ・タブレットでは重い初期化を始めない**（★蓋）|

★発行物: `REPORT_P4_MOBILE_CHECK_20260901.md` ／ `REPORT_P4_TOP_HERO_20260901.md`

---

## ★2. ★**最重要の申し送り** — ★この便、測定に 5 回騙されました

### ★2-1. ⚠️⚠️ ★**`/race` はローカルでは検証できません**（★3 回引っかかりました）

★ローカル dev は `/race` の素材を ★**0 枚**しか読みません（★7 requests / 4.1MB）。
★**旧コードでも同じ**ことを対照で確認済みです。★コードの差ではなく環境の差です。

★そのため:
  ・★読み込み量・キャンバス量・端末判定の分岐は ★**すべて本番でしか測れません**
  ・★この領域の直しは ★**デプロイしないと効果を確認できません**（★push はオーナー実行）

★測るときは必ず ★`AUDIT_BASE=<本番URL>` を付けること。

⚠️ ★**「ローカルで動かない」を「自分の編集で壊した」と読み違えました。**
　★比べていたのが「本番の旧コード」と「ローカルの新コード」で、★環境とコードが同時に違っていました。
　→ ★**対照を取ってから原因を言うこと。**

### ★2-2. ⚠️ ★**測定器が、自分の測る対象の内部を測っていました**

★「馬は最大 1536px で描かれる」と出ました。★実際は ★**`bakeCoat` の中の 1:1 転写**を
★数えていただけで、★画面への描画ではありませんでした。
→ ★`this.canvas.isConnected` で ★**画面のキャンバスだけ**に絞ったところ、
　★今度は ★**馬が 1 度も描かれない**という結果になり、★実装（9 引数 `drawImage` で
　★焼いたキャンバスから直接引いている）と食い違ったままです。★**未解決です。**

### ★2-3. ⚠️ ★**参照を握るとゴミ収集が止まります**

★キャンバスを配列に貯めて数えたら、★**その配列が全部を生かし続けて**いました。
→ ★**枚数と面積だけ集計し、要素は握らない**形で測り直し、★同じ数字であることを確認しました。

### ★2-4. ⚠️ ★**アンカー検査が身代わりで緑になっていました**

★`height: 560` を `561` に変える変異試験で ★**落ちませんでした。**
★`apps/web/src/app/camera/page.tsx` の ★`const VP = { width: 1180, height: 560 }`
★（★LP と無関係な定数）に当たっていたためです。
→ ★アンカーごとに ★**対象ファイルを書く `SCOPE`** を新設。★変異で落ちることを確認済み。

### ★2-5. ⚠️ ★**Chrome では「タッチ＋マウス」の PC を作れません**

★`Emulation.setTouchEmulationEnabled` は必ず ★**`hover: none`（タッチ専用）**になります。
→ ★蓋がタッチ対応 PC を誤って止めないかは ★**実機でしか確かめられません。**
　★判定は `(hover: none) and (pointer: coarse)` ＋ 短辺 900px 以下 です。

### ★2-6. ⚠️ ★**`npm run dev` の `Network:` 行を実機に渡さないこと**

★あの行は ★**Tailscale の口**（`100.64.0.0/10`）で、★同じ Wi-Fi のスマホからは届きません。
★Wi-Fi の口は別インターフェイスです。★DHCP なので毎回引き直すこと:

```powershell
Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.InterfaceAlias -eq 'Wi-Fi' }
```

⚠️ ★前の引継ぎ書 §0-2 も同じ誤りです（★`100.110.117.112:3215` を例示）。

---

## ★3. ★デプロイ（★この便で復旧しました）

| | |
|---|---|
| ★経路 | ★**GitHub 連携**。★`p4/race-30sec-cuts` に push すると自動でビルド |
| ★現在の URL | ★`https://star-igvzeiaa6-goldbenchan-9860s-projects.vercel.app` |
| ★ログイン壁 | ★**外してあります**（★オーナー判断・`vercel project protection disable --sso`）|
| ★戻すとき | ★`npx vercel project protection enable --sso` |

⚠️ ★**CLI からのデプロイは現実的ではありません。** ★`apps/web/public/art` が
　★**398MB・486 ファイルが git 管理下**（★`/race` に必要）で、★CLI は 15,000 ファイル上限に当たります。
　→ ★**push で GitHub 経由**が正しい経路です。

⚠️ ★`.vercelignore` を一度作りましたが ★**撤去しました**。★次のデプロイの変数を
　★「ロック修正 1 つ」に絞るためです。★CLI デプロイを使うなら改めて入れてください。

---

## ★4. ★数字（★すべて本番ビルド・実ブラウザ）

| | |
|---|---|
| ★`/race` のキャンバス（PC 幅）| ★**992 枚 / 628.6 メガピクセル / 2,398MB** |
| ★内訳 | ★1536x1024 が 127 枚 ／ 1024x1536 が 112 枚（★1 枚 6MB）|
| ★`/race` の画像 | ★**151 枚 / 復号後 403MB**（★着手前は 172 枚 / 529MB）|
| ★モバイル全ページ | ★はみ出し **1 / 46** ／ 縦の重なり **0** ／ 44px 未満 **27 / 351**（390px 列）|
| ★`/race` の縮尺 | ★縦 26% ／ 横 **64%** ／ PC 90% |

---

## ★5. ★オーナー判断待ち

| # | | |
|---|---|---|
| ★① | ★**焼く解像度**（★§0-4 を解いてから）| ★絵に影響する可能性。★解ければ影響なしで済むかもしれません |
| ★② | ★**`/race` の撮影用シーク** | ★遊ぶ人に要るか。★要らないなら隠せます |
| ★③ | ★**`/records`・`/odds` の文字リンク** | ★高さ 16〜25px。★タブに見えるので押したくなります |
| ★④ | ★**ヒーロー画像の切り抜き** | ★モバイルでは下端の HUD の帯も写ります |
| ★⑤ | ★**中継をサーバー確定の再生に切り替える** | ★正典 §0.2-4（サーバー権威）。★いまの `/race` は開発用のデモで、★ブラウザ内でエンジンを回しています |
| ★⑥ | ★前便からの持ち越し（★A-10 / 上限 12 / 引き込み線 / 勾配 / レース名 50 個 / ダート素材）| |

---

## ★6. ★再実行（★期待値つき）

```powershell
cd V:\dev\Cusor\star
npm test                                # ★期待: ★135 ファイル / ★1374 件 PASS
npm run typecheck ; echo "終了状態: $?"   # ★期待: ★0（⚠️ パイプに繋がない）

# ★モバイル（★本番へ向ける。★ローカルでは /race を測れません・§2-1）
$env:AUDIT_BASE = "https://star-igvzeiaa6-goldbenchan-9860s-projects.vercel.app"
npx tsx tools/verify-mobile-layout.mjs --widths 360,390 --rid <レースid> --hid h1
#   ★期待: ★はみ出し 1 / 46 ／ 縦の重なり 0
```

★この便で使った探針は ★**スクラッチ領域**にあります（★`tools/` に置くと
★`tool-guard.test.ts` が落ちます。★分類簿への登録が要るため・★R-24）:

| 探針 | 何を測るか |
|---|---|
| `canvasmem.mjs` | ★キャンバスの枚数・面積（★参照を握らない形）|
| `raceload.mjs` | ★読み込む画像の枚数・量・ページの例外 |
| `guard.mjs` | ★蓋が正しい端末で出るか（★5 通り）|
| `boxfit.mjs` | ★箱に収まらない中身（★CLIP と SPILL）|
| `drawsize.mjs` | ★描画寸法（⚠️ ★**結果が実装と食い違ったまま**・§2-2）|

⚠️ ★スクラッチ領域はセッションごとに消えます。★次の便で要るなら作り直すか、
　★`tools/` に置いて ★**分類簿に登録**してください。
