# QUESTIONS — UI-4（景品交換 `/prizes`・記録 `/records`）

- 日付: 2026-09-19
- 発行: 開発側
- 便: UI-4（裁定 `REVIEW_UI4_PREP_VERDICT_20260919.md`）
- 対象の移行: `0046`（`races.game_week`・`my_runs`）／`0048`（`world_state.week_started_at`）

> ★**推測で埋めていません。** ★どれも「デモにあるが、サーバーに源が無い」ものです。
> ★UI1-8 の「毛色」「脚質」、`/entry` の「今週の指示」と同じ形です。

---

## Q-UI4-1 🔴 景品の「分類」「期限」「残りわずか」を出すか

### 事実（実物で確かめました）

| デモが持つもの | `prize_catalog` の列 | `prize_catalog_public` が返す列 |
|---|---|---|
| `category`（雑貨／体験／…） | **無い** | — |
| `until`（8/31 まで） | **無い** | — |
| `tag`（期間限定／残りわずか） | **無い** | — |
| `stock`（在庫 12） | **有る** | ★**返していない**（`0007:53`） |

★`prize_catalog` の列は `id, name, cost_pp, stock, active, created_at` だけです。

### いま出しているもの

★**名前と必要 PP だけ**です。★分類タブも期限も印も出していません。

### 照会

1. ★**分類**を設ける場合、★分類の一覧は ★**誰が決める**のでしょうか（★正典 §11 に分類の定義がありません）。
   ★列を足すなら移行が要ります。
2. ★**期限**（掲載の締切）は ★正典に規定がありますか。★`active` を落とす運用で足りるなら、★列は不要です。
3. 🔴 ★**在庫の数を出すか。**
   ★`0007:20` の註記と §11.3 は ★**「在庫切れは交換画面に出さない」**＝ ★**選ばせてから断らない**と定めており、
   ★ビューが `stock > 0` で絞っています。→ ★**出ている ＝ 交換できる**。
   ★ここに「残り 3」を足すのは ★**急かす仕掛け**で、★設計の判断だと考えます。★開発側では決めません。

---

## Q-UI4-2 🔴 記録の期間に「今月」を置くか

### 事実

★デモの期間は ★**今週／今月／全期間**の 3 つです。

🔴 ★**正典にゲーム内の「月」がありません。** ★1 週 ＝ 4 時間（D-007）で、★月に相当する単位は未定義です。

★実時刻の暦月で切ると、★**BT-6 とまったく同じ問題**になります —
★`date_trunc('month', now())` は ★**セッションの TimeZone** にしたがい、★どの暦で切るか宣言した場所がありません。

### いま出しているもの

★**今週／全期間**の 2 つだけです。
★「今週」は ★`world_state.week_started_at`（★`0048`・ワーカーが `weekStartMs()` で毎周書く実時刻）と
★台帳の `created_at` を比べています。★画面は epoch も 1 週の長さも持ちません。

### 照会

1. ★「今月」を ★**やめる**か、
2. ★**N 週**（例: 直近 10 週）に読み替えるか、
3. ★**実時刻の暦月**にするか（★その場合 ★**どの暦か**を正典に書く必要があります）。

---

## Q-UI4-3 台帳の「内容」欄に何を書くか

### 事実

★デモの台帳には ★**「内容」列**があります（例: 「11R 桜星賞　1着　サクラブリーズ」）。

★`ep_ledger` / `pp_ledger` が持つのは `reason` と `ref_id` だけで、
★**`ref_id` が指す先は理由ごとに違います**:

| reason | `ref_id` |
|---|---|
| `prize` / `payout` | `race_id`（`payout.ts` / `prize-award.ts`） |
| `prize_exchange` | **null**（`0020:57`） |
| `entry_fee` / `training` ほか | ★確かめていません（★この便の担当外） |

→ ★**1 本の文にできる形になっていません。**

### いま出しているもの

★**「—」**です。★推測で組み立てていません。

### 照会

★`ref_id` の指す先を理由ごとに決めて、★**画面が 1 本の文を作れる形**にしますか。
★（★別案: ワーカーが `detail` を台帳に書く。★ただし §18 の `horse_story_event.detail` と
★**同じ情報を 2 か所**に持つことになります・D-052）

---

## Q-UI4-4 `my_runs` は「馬の戦績」であって「私の戦績」ではない

### 事実

★`my_runs`（`0046`）は `my_horses`（`0034`）と同じく ★**`h.owner_id = auth.uid()`** で絞ります。
→ ★**売った馬の過去の走りは消え、★買った馬の前の持ち主の走りは見えます。**

★`my_horses.wins` / `.starts` も同じ数え方なので、★**一貫はしています**。

### 照会

★これで意図どおりですか。★「私が持っていた間の走り」にするなら、
★`race_entries` に ★**そのときの持ち主**を凍結する列が要ります（★`0025`/`0026` の売買と合わせて設計が要ります）。

---

## 参考 — この便で出さなかったもの（★照会ではなく報告）

- 🔴 ★**1 走あたりの賞金 PP** … ★裁定 §5（**PR-1**）で「一次資料が無い」と整理されました。
  ★`race_entries` に書く形で別便にします。★`/records` には ★**合計だけ**（`pp_ledger` の `reason='prize'` の和）を出しています。
