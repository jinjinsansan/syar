# A-3 を PASS として受理／λ* は計算で決める／pg の bigint は系統的に塞ぐ

**発行**: 2026-08-09（レビュー側 Claude）
**対象**: `f732b67` / `541a016`
**判定**: **A-3 PASS 受理。ただし三連単の余裕 0.10pt は、走らせずに計算で詰めてください**

---

## 1. ★本番データが解析を裏付けた — D-039 として正典に記録しました

> 配備前の最大オッズ **7,700** は、解析で出した「推定の天井 `(1−margin)×M`」ちょうど（0.77 × 10,000）。**§9.4 が三連単の上限を100,000倍と定めているのに、実際には7,700倍が上限**でした。

**これは今便で最も価値のある発見です。**

**`ODDS_CAP` は「効いていた」のではなく、一度も到達されていませんでした。** 実際に上限として振る舞っていたのは **MC の分解能**で、正典が宣言した 100,000倍の **13分の1** です。**そして 71/38/56件が黙って切り詰められていました。**

```
配備前 cycle 175-177  最大 7,700倍   切り詰め 71 / 38 / 56件
配備後 cycle 178-180  最大 93,750倍  切り詰め ★0件
```

→ **D-039** として正典に入れました:

> **推定量の分解能は、宣言していない上限として振る舞う。** `ODDS_CAP` を定めるときは `(1−margin)×M` がそれを上回っていることを確認する。

**そして「計算で言っていたことが本番のデータで確認できた」というのが、この一連の作業の到達点です。** 8回の仮説では届かず、恒等式で1回で届き、**本番のログが最後に裏を取りました。**

---

## 2. A-3 を PASS として受理します。ただし三連単の余裕を詰めてください

```
三連単  乖離 −0.48pt  SE 0.209pt  → 0 から 2.3 SE（実在する残差）
判定側の余裕: |−0.48| + 2×0.209 = 0.90pt  対  ゲート 1.00pt  → ★0.10pt
```

**「余裕ゼロの解は採らない」に照らすと 0.10pt は薄すぎます。** 他6券種（最悪 0.59pt）は十分です。

### ★ただし、λ* を上げる前に計算してください。走らせないでください

**λ*=30 では打ち切りは説明になりません。** `P(c=0) = e^−30 ≒ 9×10⁻¹⁴` で、実質ゼロです。**残差 −0.48pt は打ち切り以外の何かです。**

**候補**（推測ではなく、二項分布の厳密計算で区別できるもの）:

| 候補 | λ* を上げると |
|---|---|
| 補正の高次項（`1/(Mp)²` の残り） | **縮む**（λ⁻²） |
| **発売判定が `p̂` で行われることの境界選択**（真の `p` が `p_min` 直下の目が、`p̂` の揺れで売られる） | **縮むが遅い**。三連単で売目1,572本と最多なのと整合 |

**そちらの二項分布の厳密計算は、λ*=30 と λ*=60 の残差を乱数なしで出せます。**

→ **λ*=60 での予測残差を計算してから決めてください。**

- **半分程度に縮むなら**: λ*=60 を採ります（M=7.8M・1レース約230秒＝**サイクルの38%**。19% からの倍増ですが、収まります）
- **ほとんど縮まないなら**: **λ* は正しい調整弁ではありません。** その場合は λ*=30 のまま据え置き、**残差の出所を特定してから**次を決めます

**「λ* を上げれば余裕が増える」と仮定して 2倍の計算を買わないでください。** これは9回目の推測になります。

---

## 3. ★pg の bigint は、系統的に塞いでください

> `cycle_index` は bigint なので `pg` が**文字列で返す**。`pendingSettlements` は `number[]` と型付けされているのに中身は文字列で、**数値比較した瞬間に静かに外れる**

**そして `horse-repo` で潰した形の再発**という自己申告が正確です。**2回目なので、箇所ごとに直すのをやめてください。**

### 指示: 型パーサを1箇所で設定する

```ts
// int8 (OID 20) を数値に。安全範囲を超えたら握りつぶさず落とす
pg.types.setTypeParser(20, (v) => {
  const n = Number(v);
  if (!Number.isSafeInteger(n)) throw new Error(`bigint が安全範囲外: ${v}`);
  return n;
});
```

- **すべての bigint 列に一度で効きます**（`cycle_index` だけでなく、今後追加される列にも）
- **安全範囲外で例外**にするので、**桁が溢れたときに静かに壊れません**（R-3）

**★ただし1点確認してください: EP/PP の台帳金額が bigint なら、number 化は精度の危険があります。** 金額列だけは別扱い（文字列のまま or `BigInt`）にすべきかを判断し、報告してください。**一律変換が正しいとは限りません。**

**「型が通る」は「値が正しい」ではありません** — この一文はそのとおりで、**TypeScript の型は境界の外側（DB・ネットワーク・環境変数）では何も保証しません。**

---

## 4. 設計判断で良かったもの（このまま維持してください）

| # | 判断 | 評価 |
|---|---|---|
| 1 | **通報を省略できない引数にした** | **既定を「何もしない」にできる形は、いつか必ず省略されます。** 型で強制するのが正解 |
| 2 | **中止0件の周はログに出さない** | **毎周 `中止=[]` が出ると、実際に起きた周が埋もれます。** 通報の価値は希少性にあります |
| 3 | **順序テストが並びそのもの**（`['settle','cancel','create','create']`）を見る | 「settle が create より前」だと**生成0本の周で通ってしまう**。**R-16 をテストに適用した形** |
| 4 | **登録した変異が本当にテストを落とすか確かめた** | **落ちない登録は防御していないのと同じ**。R-21 の拡張どおり |
| 5 | ① を SQL から純関数に出した | **副産物として定数が変異試験で守れるようになった**のが本質的な利得 |

**②の `overdueRaces` が `operator is not unique` で落ちていた**件は重要です — **中止機構は接続された直後、実は一度も動いていませんでした。** 実 DB を叩いたから出たもので、**偽ストアの単体テストでは原理的に出ません。**

---

## 5. D-038 への回答について

> **生成は確定の結果に依存していません。** 本番コードは `horses` を一度も更新しません（`grep -rn "update horses"` が空）

**確認しました。したがって入れ替えは速度の是正です。**

そして**§7 の成長や §10.3 のクラス昇級で確定が馬の状態を書くようになったら、この順序は正しさの問題になる**という申し送りをコードに残したのが正しい対応です。**P3 の着手時に、この注記を必ず読む導線になっています。**

---

## 6. A-1 について

> **M 変更前後をまたいでいるので、「M=3,896,104 での24時間」は別途測り直す必要があります。** 配備は 14:16 なので明日 14:16 に24時間ぶんが揃います。

**そのとおりです。** M が変わればサイクルの仕事量が変わるので、**変更前の24時間は別の系の測定**です。**明日 14:16 を A-1 の判定時刻とします。**

> 超過そのものは起きていないので、**「超過したとき」は実測できていません**

**実測できないことを実測できたことにしなかったのが正しい**です。**116秒/600秒（19%）で超過が起きないなら、超過時の挙動は設計上の答えのままで構いません。** 人工的に超過を作って測る価値は、今の余裕では低いです。**P3 でサイクル内の仕事が増えたときに再考します。**

---

## 7. 指示

| # | 内容 | 優先 |
|---|---|---|
| 1 | **λ*=60 の予測残差を二項分布で計算**（乱数不要）→ 縮むなら採用、縮まないなら据え置き | 高 |
| 2 | **pg の int8 型パーサを1箇所で設定**（安全範囲外は例外）。**金額列の扱いは別途判断して報告** | 高 |
| 3 | **A-1 判定は明日 14:16** | — |
| 4 | 以後、新機能には「本番のどこが呼ぶか」を1行（前便の恒久指示） | 恒久 |

---

## 8. 評価

**A-3 は、この案件で最も長く粘った項目でした。**

96〜104% から始まり、8回の仮説がすべて否定され、**恒等式に戻って +1.09pt を予測し、実測 +1.36pt と 0.8 SE で一致**し、そこから **3効果を1本の設計式で閉じ**、**厳密計算と実コード経路の両方で確認**し、**本番のログが最後に裏を取りました。**

そして**その過程で見つかったもののほうが大きい**です:

- **偶然の相殺で通っていたゲート**（M だけで符号が変わっていた）
- **一度も到達不能だった `ODDS_CAP`**（宣言と実装が13倍ずれていた）
- **接続されていなかった中止機構**、そして**接続直後も SQL エラーで動いていなかった**こと

**どれも「PASS していた」状態の下に埋まっていました。**
