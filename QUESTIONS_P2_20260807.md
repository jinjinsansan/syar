# QUESTIONS_P2 — 照会（2026-08-07）

**開発側 → レビュー側**
**コミット**: `e8858e8`（緑・push 済み）
**性質**: **手は止めていません。**§4 スキーマ・§14.4 RPC・§10 スケジューラはこの回答に依存しないので並行して進めます。

---

## Q-1【要判断】A-3 の「10万レース」は現在の定義では実行できません

### 何が起きているか

`packages/betting` と `npm run verify:payout` を実装し、seed 42 で測りました。

| 実行条件 | win | place |
|---|---|---|
| races=60 | 52.43% | 74.10% |
| races=400 | **97.09%** | **83.06%** |
| 目標 | 82% | 82% |

**分散が支配的**です。win は 60→400 レースで 52% → 97% と振れます。
一方 **place（低配当）は 83.06% で目標 82% に近い** ので、**オッズ推定そのものは壊れていません**。
振れているのは推定ではなく、1本の高配当が全体を動かす**測り方**のほうです。

### なぜレース数を増やす方向では解けないか

- A-3 は **10万レース**（§13.2）
- §9.2 は **オッズ算出に1レースあたりモンテカルロ1万回**

素直に掛けると **10⁹ 回のレース解決**になります。現在の実測で1レースあたり
（MC 2,000回で）約1.3秒なので、10⁹ 回は現実的な時間に収まりません。

### 提案（採否をお願いします）

**1レースにつき確定結果を1つ引くのではなく、独立系列から多数の確定結果を引いて
「そのレースの実現払戻率」を出す。**

- 測っている対象は変わりません。全組合せに1点ずつ買うと
  `Σ pᵢ × oddsᵢ = Σ pᵢ × (1/pᵢ)(1−margin) = (1−margin)`
  なので、**払戻率のずれ ＝ モンテカルロ推定の誤差**です。確定結果の引き方を増やしても
  この関係は変わらず、**分散だけが下がります**
- 「10万レース」を **「10万回の確定抽選」** と読み替えることになります。
  レース条件の多様性は別途レース数で担保します（例: 1,000レース × 100抽選）

**★これは判定を通しやすくする変更なので、自分では決めません。**
「10万レース」が①レース条件の多様性を要求しているのか、②払戻率推定の精度を
要求しているのかで読み方が変わります。②なら上の提案でよく、①なら
レース数側を確保したうえで MC 試行数を下げる（推定誤差は増える）判断が要ります。

### 補足: 配当上限（§9.4）の効き方

上限は**胴元の取り分が増える方向にだけ**効きます。三連単 cap 100,000 に張り付く目が
どれだけ売れるかで払戻率が下振れするので、判定域 ±1% の中に
「cap による構造的な下振れ」が含まれてよいのかも、あわせて確認したいです。

---

## Q-2【確認】インフラの認証情報

A-1 / A-2 / A-4 / A-5 / A-7 は実際の Supabase と常時稼働ワーカーがないと実証できません。
認証情報はまだ受け取っていないため、**外部接続は一切行っていません**（憲法どおり `git.txt` も見ていません）。

いただけるまでは次の順で進めます（いずれもローカル完結）:

1. §4 スキーマを SQL で書く（適用はしない）
2. §14.4 の RPC（`security definer`・EP 減算と発券を同一トランザクション）を SQL で書く
3. §10 スケジューラのロジックを純 TS で切り出す（時刻は注入・`now()` は呼ばない）

**②の SQL はレビューしていただきたいです。** A-5 の原子性は SQL の書き方そのものが根拠になるので、
実行前にコードとして見ておいてもらうほうが安全だと考えます。

---

## Q-3【要判断】A-4 は「行を隠す」では実装できません

指示書は **「RLS で `status != 'scheduled'` の行のみ露出」** と書いていますが、
**発走前のレースが見えないと馬券が買えません。**

隠したいのは行ではなく `seed_reveal` という**列**で、Postgres の RLS は行単位なので
列を隠せません。そこで:

- 実体テーブル `races` への select を `anon`/`authenticated` から剥奪
- **列をマスクしたビュー `races_public`** を公開経路にする
  （`seed_commit` は発走前から見える／`seed_reveal` は `settled`/`cancelled` のときだけ）
- 併せて `races` に RLS も張るが、**A-4 の実効はビュー側**

さらに DB 側の CHECK でも閉じました（`races_reveal_only_after_close`）。
確定前に `seed_reveal` が**入ること自体**を禁止するので、
「ビューを迂回しても漏れない」状態になります。

**この読み替えでよいか確認をお願いします。** A-4 の検証は
「`races_public` を実際に叩いて scheduled 行の `seed_reveal` が null であること」で行う想定です。

---

## 報告済みの自己申告（再掲・§REPORT 相当）

今便で自分で見つけて塞いだものが3件あります。いずれも**過去に潰したはずの型の再発**です。

| 塞いだもの | 過去の同型 |
|---|---|
| 新パッケージが `CALIBRATION_SCAN_DIRS` に無く、走査外だった | S-4（走査境界の手書き） |
| `TICKET_ORDERED` が `settle` と同じ事実の2つ目の置き場だった | L-2（二重管理） |
| `MARGIN`/`ODDS_CAP` を複数行で書き、行単位の変異がファイルを壊して「未防御」と報告された | `TRACK_CONDITION_CDF` |

3つ目は**変異試験が自分で報告してくれた**ので気づけました。
1つ目は、追加した直後に「この 32/32 は走査していない範囲について何も言っていない」と
気づいて自分で塞いだもので、**外部から指摘される前に見つかった初めての例**です。
