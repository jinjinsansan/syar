# 報告 — レース演出「真横の直線だけ」を仕上げる（台本 v9）

**案件**: STAR（ポイ活型オンライン競馬育成）
**日付**: 2026-09-14（夜）
**出す側**: 開発側 → オーナー・レビュー側
**根拠**:
- オーナー判断（2026-09-14 夜・原文）: 「O-1 見せ方を変えます　O-7 よい　いずれにしても　コーナーを全てカットです。ただしコース表ではコーナーを曲がるのは見せます　今回の３者の意見をまとめて　真横カメラワークの直線のみだが、ワクワクするレース演出に仕上げてください。」
- 3 者のまとめ `SYNTHESIS_RACE_SIDE_ONLY_20260914.md`（① `REVIEW_CONSULT_RACE_SIDE_ONLY_ANSWER_20260914.md`・`REVIEW_CONSULT_RACE_SIDE_ONLY_FOLLOWUP_ANSWER_20260914.md` ／ ② Claude Design `components/race-side-only-storyboard/index.html` ／ ③ 相談書 §5）

**ブランチ**: `p4/race-30sec-cuts` ／ **開始 HEAD** `319bae0` ／ **終了 HEAD** `57adf77`
**コミット**: `99f3774`（真横の直線だけ・ゴール前のカメラ・服の縞と鞍布・切り替えボタン）→ `57adf77`（右回りの版）
**タグ**: `p4-race-side-only-right-turn-20260915`（注釈付き・`57adf77`）／ **push**: 済み（2026-09-15・オーナー指示・`319bae0..57adf77`）

> 凡例: **✔** ＝ テスト・型チェックで確かめた ／ **[EYES]** ＝ 画面でしか判断できない（`/race` はローカルに素材が無く、本番のプレビューでしか見え方を測れない）
> ⚠️ **指示書の外での実装です。** オーナーの直接の指示「3 者の意見をまとめて仕上げてください」を根拠にしています。レビュー側の指示書（予告された 5 項目・再照会の回答 §4）より先に入れた部分があります（§5）。

---

## 0. レビュー側へ — 照合していただきたいこと

> ⚠️ **指示書の外での実装です。** 根拠はオーナーの直接の指示（§6b〜§6d に原文）と、3 者のまとめ `SYNTHESIS_RACE_SIDE_ONLY_20260914.md` です。
> 再照会の回答 §4 の予告 5 項目のうち 1〜4 を、指示書より先に入れています（5 は未着手）。

| # | 照合していただきたいこと | 場所 | 再現・確かめ方 |
|---|---|---|---|
| R1 | **ゴール前のカメラが結果を読んでいない**（Q-R7 の線）。展開の札 → その時刻の「追ってくる深さ」、画に収める相手 → 描いている位置の上位 5 頭。監査道具も同じ入力 | `broadcast-v2.ts` `finishChaseOf`／`finishChaseTable`、`page.tsx` `stateLeadGates`、`tools/lib/race-audit-build.mjs` | `npx vitest run packages/render/test/finish-chase.test.ts apps/cli/test/finish-chase-wiring.test.ts`（反実仮想・変異 3） |
| R2 | **`leadGates` の置き換えで画角が跳ばないか**（5 番手と 6 番手が入れ替わる瞬間）。未計測 | `broadcast-v2-scene.ts` `frameContenders` | 未計測。§5-2 の読み |
| R3 | **見せる区間がどの鞍でもコーナーに掛からない**／介入する人の見せ方で残り 900m から飛ばさない | `race-elision.ts`、`broadcast-v2.ts` `SCRIPT_V9` | `npx vitest run apps/cli/test/side-only-script.test.ts`（全 50 鞍） |
| R4 | **写真判定の止め方**（0.4 秒止めた後、実際の表示秒へ戻る＝止め絵の後に切り替わる）と、判定を (c) にしたこと | `race-climax-hud.ts` `photoFinishOf`、`page.tsx` `photoHold` | `npx vitest run packages/render/test/race-climax-hud.test.ts` ／ 画面は [EYES] |
| R5 | **勢いの入力**（いま＝描いている位置、0.5 秒前＝位置モデル。既定では同じ系だが同じ関数ではない） | `page.tsx` `momentumLevels` の呼び出し | §5-4 |
| R6 | **右回りの走路の直し**（`posOf` の直線とコーナーで `w` が逆向きだった）。**右回りの 4 場の既定の画が変わる**。エンジンの距離ロスと同じ意味にそろえた | `course.ts` `ovalCourse`／`posOf`／`laneRatioOf` | `npx vitest run packages/render/test/course.test.ts packages/race-engine/test/lane-geometry.test.ts` ／ §6d の計算値 |
| R7 | **服の塗り方の変更**（柄を無地に・塗る塊を 1 色に揃える・鞍布の窓では肌の判定を使わない） | `bracket.ts`、`silks-pixel.ts`、`page.tsx` `silksOverlays` | `npx vitest run packages/render/test/silks-distinct.test.ts packages/render/test/silks-pixel-saddle.test.ts` |
| R8 | **意図して書き換えた検査**（既定 v8 → v9 に伴う 6 本・柄 → 上着の色の 1 本） | §4-2・§6b | 書き換えの理由を各ファイルの註記に残してある |
| R9 | **全体のテストを最後に通していない**。変更後の全体の実行（1677 件中 3 失敗）の後に 2 件を直し、その後は関係するファイルだけを再実行した | — | `npx vitest run`（全体）。変更前から落ちている `edit-grammar-audit.test.ts` ⑨ は保存済みデータの検査 |

**オーナーの判断で決まったこと**（記録のため）: O-1 見せ方を分ける ／ O-7 「追い込み馬がある時は引く」は状態として読む ／ コーナーは全部カット・コース図では曲がるのを見せる ／ 服の柄をやめる ／ 見比べはボタンで切り替える ／ 右回りで左へ走る版を作る

**レビュー側の判断を待つもの**: 実況の着差の言葉を着順ボードに揃えるか（O-9・予告 §4-5）／ 正典への D-097 などの起票（O-1・O-7 の記録）／ 計画書 §1・§8 の「カット数は減らさない」の改訂（O-2）

---

## 1. 何が変わるか（見る人の目で）

`/race`（既定）は、次の流れになります。

| 場面 | 見えるもの | 新しい点 |
|---|---|---|
| 発走 | ゲート → 真横の追従（発走の直線だけ） | — |
| **跳び**（発走の直線の終わり → 最後の直線） | 全画面の「コース図」。**点の馬群がコーナーを曲がって**直線へ入る（2.4 秒）。右に「あなたの馬 ◯番手／先頭との差 ◯馬身／脚質」と横 1 本の隊列バー | コーナーの真横・斜めの画は **1 コマも出ない** |
| 最後の直線（残り 250m〜） | 真横の寄り → ゴール板 | カメラの引き方を**その時点で後ろから詰めている馬**で決める（勝ち馬から決めない） |
| 残り 250→200m | 左のコース図が 0.4 まで薄くなる | HUD の数は減らさず、濃さで下げる |
| 残り 200m〜 | 画面上の中央に **「ゴールまで 86m」の大きな数字**／馬名プレートの下に **勢いのバー**（4 段） | 数字は先頭の残り距離。勢いは段階だけで m/s は出さない |
| 残り 100m〜 | 画面の**周辺がわずかに暗くなる**（中央は暗くしない） | 最大 0.32 |
| ゴール直前 | **音を 0.15 秒抜く「溜め」** → 通過で歓声を一気に上げる（音を入れたとき） | 直線では走行音を厚くし、歓声を積み上げる |
| ゴール（接戦のときだけ） | **写真判定の止め絵 0.4 秒**（白い幕を一瞬・決勝線の金の縦線・「写真判定」「ハナ差」など） | 着差の言葉は着順ボードと同じ値 |
| その後 | 勝ち馬の追従 → リプレイ → 着順ボード | — |

### 比べるための口

| URL | 何が見えるか |
|---|---|
| `/race` | 新しい既定（台本 v9・観戦だけの人の見せ方） |
| `/race?cinematography=v8` | 直前の既定（最後のコーナーを 1 つ見せる形）。**比べる相手** |
| `/race?view=intervene` | **自馬で介入する人の見せ方**（O-1）。残り 900m からゴールまで時計を飛ばさず、その間のコーナーは**実時間でコース図の画面**で覆う |
| `/race?sound=1` | 音あり（「溜め」と歓声の積み上げを聞く） |
| `/race?seed=99` | 接戦の展開（写真判定が出るか・勢いが動くかを見る。seed 42 は「せめぎ合いの無い」展開） |

---

## 2. 実装したもの（まとめの比較表の番号）

| # | 案 | 中身 | 出どころ |
|---|---|---|---|
| 1 | **B**: コーナーを時計から取り除く | 新しい台本 **v9**（ゲート → 真横の追従 → 最後の直線 → ゴール板）。コーナーのカットを 1 本も持たない。既定を v8 → v9 | `broadcast-v2.ts` `SCRIPT_V9`・`v9BoundariesM`・`DEFAULT_RACE_SCRIPT` |
| — | 見せる直線を最後の直線より長くしない | 直線の短い鞍で 4 角の出口を真横で映さない | `race-elision.ts` `homeStretchM` |
| 4 | 跳びの「見立て」＋**コース図で曲がる** | 覆う窓の入口と出口のレース秒の間で、コース図の点だけを進める（脚は描かない＝倍速にならない）。脚質と隊列バーを足した | `race-elision.ts` `raceEditSweepRaceSec`・`race-cutin.ts` `RACE_COURSE_SWEEP_LEAD_SEC`・`drawToStraightCutIn` の `ownStrategyLabel`/`formationBar` |
| O-1 | 介入する人の見せ方 | 残り 900m（`EARLY_SPURT_METER`）→ ゴールを飛ばさない時計と、コーナーをコース図で覆う画面。`?view=intervene` のときだけ | `race-elision.ts` `keepFromMetersLeft`・`page.tsx` `INTERVENE_VIEW` |
| 24 | **ゴール前のカメラを状態から決める**（O-7） | 「追ってくる深さ」＝ 過去 1 秒で先頭との差を詰めている馬が、どれだけ後ろにいるか（0〜1）。表はレースにつき 1 回作り、未来の位置を読まない | `broadcast-v2.ts` `finishChaseOf`・`finishChaseTable`・`FINISH_CAMERA_BY_CHASE` |
| 24 | **画に収める相手も状態から** | 直線とゴール板のカメラが「確定着順の上位 5 頭」を画に収めていたのを、「その時刻に描いている位置の上位 5 頭」に置き換えた（レビュー側の線「未来の値を入力に取らない」に当たっていたため・照合で発見） | `page.tsx` `stateLeadGates` |
| 25 | 註記の表を定数に合わせる | 11.5°/15°/19° → 11.0°/11.5°/12.0° など | `broadcast-v2.ts` |
| 5 | カウントダウン | 残り 200m から。先頭が決勝線を越えたら消える | `race-climax-hud.ts` `drawGoalCountdown` |
| 8 | 周辺の減光 | 残り 100m から、最大 0.32、中央は 0 | `drawClimaxVignette` |
| 16 | HUD を濃さで下げる | コース図を残り 250→200m で 0.4 へ | `climaxHudFade` |
| 10 | 勢い | 残り 200m から（Q-R8）。過去 0.5 秒の速さを全馬の中央と比べて 4 段。**長さが主、色は最上段だけ彩度を抑えた金**（O-6）。`?climax=on` では出さない | `momentumLevels`・`reference-hud.ts` の `momentum` |
| 9 | 写真判定の止め絵 | 判定は (c) 先頭が決勝線を通る瞬間の描いている位置で 1〜2 着の差（Q-R9）→ `marginLabel` がクビ以内なら 0.4 秒止める。表示の言葉は着順ボードの値 | `photoFinishOf`・`drawPhotoFinishOverlay`・`page.tsx` の `photoHold` |
| 14 | 音 | 直線で走行音 ×1.45・歓声 0.2→1.0、ゴール 0.15 秒前に 0 へ、通過で歓声 ×1.2 | `race-audio.ts` `level`・`page.tsx` |

**v8（切り戻しの道）について**: カウントダウン・減光・勢い・写真判定・音の組み立て・コース図の脚質と隊列バーは **v9 のときだけ**入れました。ただし **#24（カメラを状態から決める）は v8 にも効きます**。v8 のまま結果から構図を決める形を残すと、レビュー側の線に反したままになるためです。

---

## 3. 入れていないもの（理由）

| # | 案 | 理由・次の扱い |
|---|---|---|
| 2 | 直線を残り 400m から見せる | O-3 は [EYES]。今回は 250m のまま。比べる口はまだ作っていない |
| 3 | 1 本の連続カメラ（枠取りを全区間へ） | 過去に 2 回外した記録がある（発走の団子で数頭しか映らない・被写体が端に付く）。試作が要る |
| 7 | 観客を直線だけに出す | 観客は起動時にスタンドへ焼き込んでいる。分けるには起動時のメモリが増える（side-v9 の +31% と同じ種類）。見積もりが先 |
| 13 | 展開の見出し | ゴールの後に出す形にする必要がある。今回は入れていない |
| 15 | 未使用の音源（馬が来る音・フィナーレ） | STAR のリポジトリに無い（sevendays 側）。O-8 はオーナーから渡していただく必要がある |
| 21 | 展開ごとに第 2 幕全体を変える | 状態から決める形の設計が要る。今回はカメラの引き方（#24）だけ |
| 22 | リプレイを最後の 200m だけ | [EYES]。今回は触っていない |
| 26 | 実況の着差の言葉を着順ボードに揃える（O-9） | 実況は馬身、ボードは秒差で入力の単位が違う。レビュー側の予告 §4-5 に入っているので指示書を待つ |
| 20 | 介入の「押す場所」の画 | 別の便（サーバー側と一緒に） |

---

## 4. 検査

### 4-1. 新しく足した検査

| ファイル | 守るもの |
|---|---|
| `apps/cli/test/side-only-script.test.ts` | ① 既定は v9 で、**全 50 鞍**でコーナーのカットを持たない ／ ② 観戦の見せ方で、見せる区間が**どのコーナーにも掛からない**（全鞍・R-33）／ ③ 跳びの見出しにコーナーの名前が出ない ／ ④ 介入する人の見せ方で、飛ばす区間 ∩［残り 900m, ゴール］＝ 空 ／ ⑤ v8 はコーナーのカットを持ったまま ／ コース図で進める秒は窓の入口と出口で世界と一致する |
| `packages/render/test/finish-chase.test.ts` | **反実仮想**: 分かれる時刻まで全馬の位置が同じで 1 着だけ違う 2 つのレースで、分かれる前の「追ってくる深さ」が 1 つ残らず同じ・その時刻のカメラが同じ ／ **変異**: 展開の札で構図を決めると同じ位置でもカメラが違う ／ 先頭が入れ替わる瞬間に値が跳ばない ／ 両端は展開の表の両端と同じ |
| `apps/cli/test/finish-chase-wiring.test.ts` | 画面の `resolveBroadcastV2Scene` 2 か所が `development` を渡さず、`finishChase` を渡し、`leadGates` を確定着順から作っていない（構文木）／ 変異 3 つ（札に戻す・上位 5 頭を確定着順に戻す・流れの表だけ確定着順を渡す）を捕まえる |
| `packages/render/test/race-climax-hud.test.ts` | カウントダウン（200m より手前で出さない・越えたら消える）／ 減光（100m より手前 0・最大 0.32）／ HUD の濃さ（0 にしない）／ 勢い（出し始めは `CONVERGE_END_M` と同じ・段は 0〜3・全馬が同じだけ減速しても「落ちている」と出さない）／ 写真判定（クビ以内だけ・位置で 1〜2 着を決める） |

### 4-2. 意図して書き換えた検査

| ファイル | 変えたこと | 理由 |
|---|---|---|
| `script-v5.test.ts` ① | 既定の期待を `'v8'` → `'v9'`、v8 へ戻せることを足した | オーナー判断 |
| `script-v5.test.ts` ⑥ | 4 角の正面カットは v8 で見る。既定（v9）の同じ地点は `side-drive` | v9 はコーナーのカットを持たない |
| `cut-transition.test.ts` | 台本ごとの「画角の系統が変わる切替」の表に `v9: 0` | v9 は全部 `view: 'side'` |
| `fixed-camera.test.ts` | 対象の台本を `[既定, v5, v4]` → `[v8, v5, v4]` | v9 に 4 角の正面カットが無い（守る対象が無い） |
| `finish-post.test.ts` | 4 角の正面カットを強制する場面に `script: 'v8'` を明示 | 台本を渡さないと既定（v9）の据え位置へ落ち、ゴール板が画面に入らなかった。見たいのはゴール板の描き方 |

| `apps/cli/test/race-video-invariants.test.ts` ③ | 4 角正面の出口の検査を `'v8'` 明示で測る。対照として「既定（v9）では 4 角正面が一度も選ばれない」を足した | 既定では出口が 0 件になり「拾えていない」で落ちた（R-3 の見張りが正しく働いた）。守る対象は 4 角正面を持つ台本 |

### 4-3. 監査道具を画面に揃えた（R-30）

`tools/lib/race-audit-build.mjs`（＋型 `race-audit-build.d.mts`）:
- `leadGates` を確定着順の上位 5 頭 → **描いている位置の上位 5 頭**（画面の `stateLeadGates` と同じ）
- `auditClock` に `finishChaseAt`（`finishChaseTable`・画面と同じ入力）を足し、`auditSceneAt` が `finishChase` を渡す
- ゴール板の「引く」枠取りを外す条件を `v6` だけ → `v6`・`v8`・`v9`（台本を省いたときは画面の既定 `DEFAULT_RACE_SCRIPT` で判定）。**以前から既定（v8）で道具だけが引く枠取りで測っていた食い違い**も一緒に解消

⚠️ 監査道具は時計を `readable`（時計を取り除かない）で組んでおり、画面の `short` とは違います。これは今回より前からの差で、今回は触っていません。

### 4-4. 結果

| 実行 | 結果 |
|---|---|
| ✔ `npx tsc --noEmit`（ルート） | エラー 0 |
| ✔ `npx tsc --noEmit -p apps/web` | エラー 0 |
| 変更前（`319bae0`）の `npx vitest run` | 158 ファイル中 1 失敗（`edit-grammar-audit.test.ts` ⑨） |
| 変更後の `npx vitest run`（全体） | 162 ファイル・1677 件中 **3 失敗** → 内訳は下 |
| 　└ `edit-grammar-audit.test.ts` ⑨ | **変更前から落ちている**（保存済みの測定データ `out/` の seed 分類「独走代表」が `contest`。コードではなくデータの検査） |
| 　└ `finish-chase-wiring.test.ts` | 検査の誤検出（`visual → finishSec`）。検査を直した後に単独で再実行 → **4 件すべて通過** |
| 　└ `race-video-invariants.test.ts` ③ | 既定に 4 角正面が無いため（§4-2）。直した後に再実行 → 通過 |
| 監査道具を読むテスト 10 ファイルの再実行（揃えた後） | 64 件中 63 通過・1 失敗（上の ⑨ のみ） |

⚠️ 全体の再実行は、上の 2 件を直した後にはしていません（直した 2 ファイルと、監査道具を読む 8 ファイルを個別に再実行）。

---

## 5. レビュー側に確認していただきたいこと

1. **指示書より先に入れた範囲**: 再照会の回答 §4 の予告 5 項目のうち、1（`FINISH_CAMERA_BY_DEVELOPMENT` の置き換え）・2（註記の表）・3（勢いは 200m から・過去の窓）・4（写真判定は (c)・言葉は `marginLabel`）を入れました。5（実況の着差の言葉）は入れていません。
2. **`leadGates` の置き換え**（照合で見つけた点）: 直線とゴール板のカメラが確定着順の上位 5 頭を画に収めていました（`page.tsx`・監査道具 `tools/lib/race-audit-build.mjs` の両方）。画面は「その時刻に描いている位置の上位 5 頭」にしました。5 番手と 6 番手が入れ替わる瞬間は 2 頭が同じ位置にいるので、枠取り（集合の中のいちばん後ろだけを使う）は跳ばない、という読みです（未計測）。
3. **写真判定の止め方**: 止めている 0.4 秒のあいだ表示秒を止め、そのあと**実際の表示秒へ戻ります**（0.4 秒ぶん先へ飛ぶ＝止め絵のあとに切り替わる）。着順・走破タイムには触れていません。
4. **勢いの入力**: いまの位置は描いている位置（`easedAt`）、0.5 秒前は位置モデル（`built.model.at`）です。既定（`?climax=on` でない・ゴール前）では 2 つは同じ系ですが、厳密には同じ関数を通していません。
5. **介入する人の見せ方**（`?view=intervene`）: 残り 900m からゴールまでに 3 角・4 角がある鞍では、**約 30 秒のあいだ全画面のコース図**になります（1600m で約 500m）。O-1 と「コーナーは全部カット」を両立させるとこうなる、という形です。見え方はオーナーの判断が要ります。

---

## 6. 本番のプレビューで見ていただくこと [EYES]

- 跳びのコース図（2.4 秒）で、**点がコーナーを曲がるのが読めるか**。短すぎる／長すぎるか
- カウントダウンの数字の位置と大きさ（67% 表示・モバイル縦）
- 周辺の減光の強さ（脚さばきが隠れないか）
- 勢いのバーが読めるか・うるさくないか
- 写真判定（seed 99 などの接戦で出るか・0.4 秒の止めの後の切り替わりが不自然でないか）
- 音の「溜め」（`?sound=1`）
- `?view=intervene` の 30 秒のコース図
- v8 と見比べて「真横の直線だけ」で飽きないか

⚠️ ローカルでは素材が無く、画面の見え方を測れていません。**push とプレビューのデプロイはオーナーの指示待ち**です。

---

## 6b. 追加: 騎手の服の縞を直した（2026-09-15・オーナー指摘と判断）

- **オーナー評**: 「また騎手の服に縦縞模様出ています」 → **判断**: 「柄をやめて全部直す」
- **切り分け**:
  - 今回の差分（台本 v9 ほか）は服の塗りのコードに 1 行も触れていない（`git diff` で確認）。前からの崩れが、真横の画だけになって目立ったもの
  - 開発サーバーの `/race` をヘッドレスで拡大して撮影し、崩れを 3 種と確認した
    1. 上着の真ん中の帯（一本輪の柄）が、真横のデフォルメ馬で背中と腕を横切り、2〜3 本の縞に見える（4・8・10・12 番）
    2. ヘルメットの後ろの角に、上着の窓に入った所だけ四角い別の色が出る
    3. 上着の下の端に、鞍布の窓に入った所だけ枠色の帯が出る（3・12 番）
  - 窓の実測（`tools/measure-silks-window.mjs`・side-v8／v8b・8 コマ）で、真横の窓 `SILKS_LAYOUT_CROUCH`（side-v6 用）の縁が、ヘルメットと上着の塊を途中で切っていた
- **直したこと**:
  - `packages/render/src/bracket.ts`: 柄を無地だけに（`SILK_PATTERNS = ['plain']`）。同じ枠の 2 頭は上着の色が必ず違う（既存の検査）
  - `apps/web/src/app/race/page.tsx` の `silksOverlays`: 塗る画素をつながった塊に分け、塊ごとに多い方の色 1 色に揃える。窓の数字は動かしていない（詰め直すやり方は過去 3 回、別のコマ・別の絵ではみ出した）
- **確かめたこと**:
  - 直した後に同じ 3 か所を撮り直し、帯・四角・下の帯が消えたことを目で確認
  - 型チェック（ルート・画面）エラー 0。`packages/render` のテスト 731 件中 730 通過（落ちたのは変更前からの `edit-grammar-audit` ⑨ だけ）
  - `silks-distinct.test.ts`: 「12 頭立てまでは同じ枠の柄が必ず違う」を「上着の色が必ず違う」に置き換え（意図した変更）。「塊を 1 色に揃える」「柄は無地だけ」を足した
- ~~残っているもの: 鞍布（ゼッケンの布）に枠色と白がまだらに残る~~ → 下の「鞍布のまだら」で直した

### 鞍布のまだら（2026-09-15・オーナー指示「治してください」）

- **原因**: 鞍布の生地の陰が赤みがかったクリーム色の灰（実測 (170,155,140)・(202,188,179) など）で、R>G>B のため**肌と判定されて塗られていなかった**。side-v8 の 2 コマで、鞍布の窓の明るい画素のうち 4,521 画素（`silksPaintable` と `isSkinTone` で分類・色分けした絵で、鞍布の下と右の縁に並ぶことを確認）
- **直したこと**:
  - `packages/render/src/silks-pixel.ts`: `silksPaintable` に `checkSkin`（既定 `true`）を足した。省略時は従来どおり（`tools/measure-silks-window.mjs` の数字は変わらない）
  - `apps/web/src/app/race/page.tsx`: 鞍布だけの窓（上着・兜の窓に入らない所）では `checkSkin: false`。上着・兜の窓では従来どおり肌を外す（首すじ・手）
- **確かめたこと**: 型チェック（ルート・画面）エラー 0 ／ 新しい `silks-pixel-saddle.test.ts`（鞍布の窓では実測の陰を塗る・上着の窓では従来どおり塗らない・馬の体は鞍布の窓でも塗らない）と `silks-distinct.test.ts` の 15 件通過 ／ 開発サーバーの同じ 3 場面を撮り直し、鞍布が 1 色になったことを目で確認
- **残っているもの**: 4 番の青い鞍布の左下と 2 番の黒い鞍布に小さな白い擦れ、3 番の赤い鞍布の下の縁に白い線。素材に描かれた汚しや縁取りと見ているが、確かめていない

## 6c. 追加: 見比べを道具のボタンで切り替える（2026-09-15・オーナー指示）

- **オーナー指示**: 「URL を分けずに道具でボタン分けしてください　複雑です」
- **入れたもの**（`apps/web/src/app/race/page.tsx` の `toggleViewParam`）: 押すと URL の口を書き換えて読み直す（既存の `pickRace` と同じ作法。URL を読む経路は増やしていない）
  - 「演出：新 ⇄ 前」… `?cinematography=v8`
  - 「観戦 ⇄ 介入」… `?view=intervene`
  - 「展開：通常 ⇄ 接戦」… `?seed=99`
- **置いた場所**: 開発卓の操作の列（開発サーバーは `?dev=0` を付けない限り常に開発卓・携帯のメニューもこの列）／遊ぶ人の画面の列／携帯の全画面の道具の列
- **確かめたこと**（ヘッドレス）: 3 つのボタンが出る ／ 「演出：新」を押すと `?cinematography=v8` で読み直され「演出：前」になる ／ 携帯（タッチ端末として開く）で「演出開始」→全画面の道具の列に 3 つ出る ／ 型チェック（画面）エラー 0
- **HTTPS の警告**: 開発サーバーは http のため、Chrome の「常に安全な接続を使用する」が有効だと開くたびに警告が出る。オーナーには「サイトに移動」か設定のオフを案内した（コードでは避けられない）

## 6d. 追加: 右回りで馬が左へ走ってゴールする版（2026-09-15・オーナー指示・コミット `57adf77`）

- **オーナー指示**: 「今は左回りで馬が右に向かって走りますが、今度は真逆の右回りで馬が左に走りゴールするバージョンのレース演出を作ってください」
- **切り分け**（右回りの鞍 `g2-shiokaze` を開発サーバーで撮影 ＋ 計算）:
  - 右回りの鞍でも**馬は右へ走っていた**。原因は描画の走路（`packages/render/src/course.ts`）で、**右回りの `w`（内ラチからの距離）が逆向き**だった
    - 直線の区間に回りの向きが無く、`posOf` が直線を常に左回りとして扱っていた
    - コーナーの半径が `R + off × sgn` で、右回りだけ `w` が大きいほど中心に近かった
    - 両方とも同じ向きに間違っていたので、つなぎ目は合っていたが、「外側」に置くはずの真横のカメラが**芝の中**に置かれていた
  - エンジンの距離ロス（`laneExtraMeters`・`@star/race-engine`）は元から「`w` が大きいほど外」で、回りの向きを見ない。**着順・走破タイムには効かない**
- **直したこと**:
  - `course.ts`: 直線（発走の引き込み線を含む）に回りの向きを持たせる ／ コーナーの半径を `R + off`（`laneRatioOf` も同じ規則）／ 走路の手前と先の延長も同じ側
  - 計算: 右回り 1600m の最後の直線で、内ラチ y 372（中心側）・外ラチ y 392・カメラ y 426（外側）・**馬の進む向き 画面 −812.5（左）**。左回りは直す前と同じ値
  - `broadcast-v2-scene.ts`: 勝馬のカットにも鏡像の口を渡す（右回りで勝馬だけ右を向いていた）
  - 隊列バー 3 か所（上の隊列バー・下のテロップ・跳びの覆い）に `leftward`。馬が画面の左へ進むとき先頭を左端に（判定は馬を描くのと同じカメラで注視点の 1m 先を投影）
  - ゼッケンの番号: 右回りでは重ね絵ごと鏡像になり「10」が「01」に裏返っていた → 右回りのときだけ番号を先に裏返して書く（`silksOverlays` の `mirrorNumber`・原版と焼いた絵の両方の経路）。左回りは同じ重ね絵のまま
  - `page.tsx`: `?turn=left|right`（描画だけ・`COURSE_OPTS` と `turn` の初期値を同じ値から）／ 道具のボタン「回り」（開発卓の列・遊ぶ人の列・全画面の道具）／ 開発卓の「回り」の選択も読み直す形に（番号の向きは読み込み時に焼くため）
- **確かめたこと**:
  - 開発サーバー（ヘッドレス）で右回りを撮影: ゲート・最後の直線・ゴール前・ゴール通過・リプレイ・勝馬の追従で馬が左向き・左へ走る ／ 跳びの覆いのコース図は右回りの形・隊列バーの先頭が左 ／ 上の隊列バーの先頭（4 番）が左端 ／ 番号が正しい向き（4・12・3・10・8・6、勝馬 10）
  - 対照の左回り: 隊列バーの先頭は右端・勝馬は右向き・番号（5・10・4）はこれまでどおり
  - テスト: `course.test.ts` に「つなぎ目で同じ `w` の点が跳ばない（左右とも）」「直線の内ラチはコーナーの中心側（左右とも）」を足した ／ 描画部品と画面側の配線テストで 757 件中 756 通過（落ちたのは変更前からの `edit-grammar-audit` ⑨）／ `lane-geometry.test.ts`（エンジンとの突き合わせ）13 件通過 ／ 型チェック（ルート・画面）エラー 0
- **気をつける点**:
  - 右回りの鞍は、これまでも**実際には左回りと同じ向きで描かれていた**。今回の直しで、右回りの 4 場（青嶺・潮風・月見丘・霧ヶ原）の既定の画が変わる
  - 背景の板（スタンド・生け垣）は左右反転していない（流れる向きだけ逆）。見え方は [EYES]
  - `?turn=` は描画だけの見比べ用で、鞍の回り（エンジンの走路）は変えない

## 7. 変更の範囲

`git diff --numstat 319bae0`（未追跡の新規ファイルは含まない）:

| ファイル | 追加 | 削除 |
|---|---|---|
| `apps/web/src/app/race/page.tsx` | 252 | 33 |
| `apps/web/src/app/race/race-audio.ts` | 16 | 0 |
| `packages/render/src/broadcast-v2.ts` | 197 | 8 |
| `packages/render/src/broadcast-v2-scene.ts` | 14 | 4 |
| `packages/render/src/race-cutin.ts` | 66 | 0 |
| `packages/render/src/race-elision.ts` | 42 | 3 |
| `packages/render/src/reference-hud.ts` | 17 | 0 |
| `packages/render/src/index.ts` | 1 | 0 |
| テスト 4 本（書き換え・`packages/render/test`） | 35 | 8 |
| `tools/lib/race-audit-build.mjs` | 23 | 9 |
| `tools/lib/race-audit-build.d.mts` | 2 | 0 |
| `apps/cli/test/race-video-invariants.test.ts` | 17 | 1 |

新規: `packages/render/src/race-climax-hud.ts`、テスト 4 本（§4-1）、`SYNTHESIS_RACE_SIDE_ONLY_20260914.md`、`CONSULT_RACE_SIDE_ONLY_FOLLOWUP_20260914.md`、本書。

触っていないもの: 正典 `STAR_SPEC_v2.0.md`（レビュー側の未コミットの変更がある）、レビュー側のファイル、`race-engine`（着順・走破タイム・位置モデル）、DB。
