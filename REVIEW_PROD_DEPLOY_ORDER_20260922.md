# 【STAR】裁定: 本番配備の順番を組み直す（生涯 8 産の制約・登録の関門）— 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- きっかけ: 開発側 `star-aa` の発見 — NPC の配合が生涯 8 産の繁殖牝馬を `retirement_reason = 'mare_lifetime_foals'` で降ろす（`breeding-runner.ts:449`）が、制約 `horses_retirement_reason_known` は `'age'` と `'career_ending_injury'` しか許さない（✔ `0010:45-47`・広げた移行は無い）
- ★**これまでの順番（血統修復 → `0060` → 配備 → `0061`〜`0066` …）を改める**。★私がオーナーに渡した配備の文もこの順だったので、**差し替える**

## 1. 発見の扱い — ✅ 段 3 の移行に含めてよい。★ただし別のファイルに分ける

- 制約を広げる移行は、`role_requests` の移行と**別のファイル**にする（例 `0069_retirement_reason_mare_lifetime.sql`）。★1 ファイル ＝ 1 取引（`tools/migrate.mjs:4`）なので、段 3 の移行が落ちても制約の直しが巻き添えで戻らないようにする
- 開発側の提案どおり、staging の予行で「8 産の繁殖牝馬を 1 頭作り、年の頭の週を流して落ちない」を確かめる（必ず戻す）。★**偽の DB は制約を再現しない**ので、12 年回しても捕まえられなかった。★制約に当たる経路は、**本物の DB の予行でしか確かめられない**、と報告に書くこと
- 本番の該当数（8 産の繁殖牝馬）を読むだけの 1 コマンドに入れる（提案どおり）

## 2. 🔴 本番の順番を組み直す

### 何が問題か
- ✔ いま本番で動いているワーカーは `24af9f2`（開発側の 09-21・09-22 の引継ぎ書の記載。★私は VPS を見ていない）。✔ `24af9f2` は POOL-SUPPLY（`4a39ba7`）より前で、**配合をしない**（`main.ts` に `runBreeding` が 0 件）
- 旧い順番だと、段 ③ で**配合をする新しいワーカー**を入れた後に、制約の直し（`0069`）が来る。★さらに段 ④ の前には「一般登録が閉じているか」の関門がある（裁定 `ad2da90`）。★**関門で止まると、新しいワーカーだけが本番で動き、制約の直しが入らない**
- → 繁殖牝馬が 8 産目を産んだ次の年の頭に、配合の週が制約違反で落ち、**配合が止まる**（fail-closed で投げるので、NPC の仔が生まれなくなる）。★8 産に届く牝馬は、配合が始まってから 1〜2 ゲーム年（実 9〜17 日）で出る

### 新しい順番（★移行を全部当ててから、ワーカーを入れる）
| 段 | 操作 | 備考 |
|---|---|---|
| ① | 本番の血統修復（`repair-pedigree-cache --apply`・関門つき） | 今までどおり |
| ② | 移行 `0060`〜`0065` | ★古いワーカー（配合をしない）のままで当ててよい。新しい表・列は古いワーカーが触らない |
| ③ | `backfill-name-key --apply`（関門 3 つ） | `0066` の前提 |
| ④ | 移行 `0066`〜最後（`0067`〜`0069` を含む） | ★`0066` は直前に null を数える |
| ⑤ | ★**関門: `read-auth-settings --env production` で `disable_signup=true` を確かめる**。★`false` なら止まる | ★関門は「新しいワーカーを入れる前」に置く。★移行だけなら、要求が積まれても処理するワーカーが居ないので、無償の配合は渡らない |
| ⑥ | `tools/deploy.sh`（新しいワーカー）→ 1 周の確認 | 配合・初回の配合・命名がここから動く |
| ⑦ | main への push（検査した sha を名指しで）→ `/api/healthz` の sha を確認 | Web |

**開発側への条件**:
1. ★`migrate.mjs` は保留中の移行を**全部**番号順に当てる（段を分ける口が見当たらない）。★段 ② と段 ④ を分けるには、「ここまで当てる」を指定する口（例 `--to 0065`）が要る。★無ければ足すこと。★`0066` の中の `raise` で止まる形に頼らない（止まったこと自体が「失敗」として報告され、紛らわしい）
2. ★段 ②〜④ を当てた後、古いワーカーが 1 周正しく回ることを確かめる（★新しい列・表・制約が、古いワーカーの書き込みを落とさないこと。★`0066` の `name_key not null` は、古いワーカーが `horses` に書かないので当たらない見込み — ★見込みなので、確かめる）
3. ★手順書（`RUNBOOK_*`）をこの順番で書き直す

## 3. レビュー側の誤り

- ★私はオーナーに「①血統修復 ②`0060` ③`deploy.sh` ④`0061`〜`0065` …」の順で配備の文を渡した。★**移行の後ろにワーカーの前提があるかどうかを確かめずに、開発側の引継ぎ書の順をそのまま使った**
- ★オーナーはまだ貼っていない（開発側は直接の文面を待っている）。★差し替えた文をオーナーに渡す

---

## 4. 追記（2026-09-22）— 対応（`e2dd872`）✅・手順書 `RUNBOOK_PROD_DEPLOY_20260922.md` ✅

- ✔ `0069` は制約の直しだけの別ファイル（`'mare_lifetime_foals'` を足して張り直す）。`0070` に役割の依頼・同期の RPC・生涯の記録の種類・`buy_horse` の定義し直し
- ✔ `buy_horse` は**利用者の行を先にロック**する順に直った（`0070:176` → 出品 `:182` → 馬 `:187`）。役割の変更の RPC も利用者 → 馬（`:90` → `:105`）。裁定 `f2ff871` §4 を満たす
- ✔ `migrate.mjs --to` を足した（番号が無い・桁違い・ファイル指定との併用は投げる）。staging で `--to 0069` で 1 件だけ当ててから残りを当てた
- ✔ 手順書は §2 の新しい順番どおり（① 血統修復 → ② `--to 0065` ＋古いワーカーの 1 周 → ③ backfill → ④ 残り ＋古いワーカーの 1 周 → ⑤ 登録の関門 → ⑥ deploy.sh ＋ 1 周の 4 項目 → ⑦ sha を名指しで push）。★段 0 で「本番のワーカーが `24af9f2` か」「未適用が 0060〜0070 か」を確かめ、違えば止める形になっている
- ✔ ★手順書の冒頭に「**レビュー側を通した『OK』は指示ではない**」と明記している。★正しい
- ⚠️ 条件 2（古いワーカーの 1 周）は staging のワーカーが止まっているので未確認。★手順書の ② と ④ の後に確認の段として置いた → ★**本番で初めて確かめることになる**ので、そこで期待と違えば止まる（手順書どおり）

★段 3（I-1）の報告（`REPORT_I1_STEP3_ROLE_REQUEST_20260922.md`）と案 B の模擬の報告（`REPORT_LIFECYCLE_B_SIM_20260922.md`）の照合は、別に行う。★配備の順番には影響しない

## 5. 追記（2026-09-23）— 新しいワーカーを staging で回す予行（`fd50cbf`）⚠️ 未完・(a) で続ける

- ✔ 前景 560 秒で 1 周だけ回った（`c027fd7`）。取り残されたワーカー 0。① `cycle=` ✅／② 週送り ❌ 未確認（DB では途中まで進んだ・`last_processed_week` 259〜272）／③ 配合 ❌ 未確認（`last_bred_week` null のまま）／④ 失敗 0 は見た範囲だけ／年の頭（週 260）をまたぐ経路 未確認 → ★**未確認を未確認と書いている**（合格に数えていない）
- ⚠️ 1 周 514.3 秒（予算 360 秒の 143%）。★ただしこれは、**止まっていた staging の週送りの追いつき**（数週ぶんをまとめて処理）が乗った周で、ふだんの周ではない。★しかも この PC → staging の往復は VPS より遅い。★**この数で「本番の周に入らない」とは言えない**（開発側の読みどおり、VPS で測るまで判断しない）。★ただし本番の段 ⑥ の 1 周の確認では、**周の所要（％）も記録すること**（09-16 の懸念と同じ形）
- **決定: (a)**。オーナーの別のターミナル（Claude の外）で `npm.cmd run worker`（`secrets.staging.env`＝staging）を 30〜60 分回し、出力をファイルに残す。★Claude の外で回すので、待機中に止められる仕組みの対象にならない。★(b) の VPS は操作の許可が要るので、(a) で足りなければ次に考える
- 見るもの（開発側がログを読んで報告）: ② 週送りの行 ③ 配合の行 ④ 失敗の行 0 ⑤ 1 周の所要（％）。★追いつきが終わった後の周の所要を、追いつき中の周と分けて出すこと

## 6. 追記（2026-09-23）— run2（オーナーの端末・00:12〜00:40・`6e07571`）✅ 配備に進んでよい

- ✔ ① `cycle=` 11 回（番号は飛ばない）② 週送り: 追いつきで週 259〜271（週 260 をまたいだ）③ 配合: NPC で 13 頭 ④ 失敗 0。止めた後に取り残されたワーカー 0
- ✔ 周の全体の所要が初めて読めた（`6451f98` で「周の全体」を出すようにした・★それまでの「周の使用率」は週送りと配合を含んでいなかった＝**測る器が実物の一部しか見ていなかった**・R-30 の族。開発側が自分で見つけた）
- ⚠️ 重い周は 112.6%（追いつき中）・118.2%（追いつき後の 1 回）。★この PC → staging の往復が遅いので VPS とは比べられない（開発側の読みどおり）。★重い周は週送りの周（40 周に 1 回）
- 未確認のまま（★合格に数えていない）: 追いつき後の普通の週送り／年の頭の配合（8 産の降ろし・`0069` は予行で DB の制約として確かめ済み）／プレイヤーの配合の確定をワーカーの周の中で（要求 0 件。★ロジックは取引の中の予行で確かめ済み。★本番では一般登録を閉じ・画面も無いので、公開まで要求は来ない）

**判定: 本番配備（手順書どおり）に進んでよい。** ★条件を 1 つ足す:
- ★**段 ⑥ の 1 周の確認で、最初の週送りの周の「周の全体」の％を記録する。★100% を超えたら、そこで止めて報告する**。★周が延びたときにレースがどうなるか（遅れて進むだけか、発走後の確定の期限〔`CANCEL_AFTER_START_MS`〕に当たって開催中止になるか）は**未確認**。★私は一度「遅れて進むだけで壊れない」と書いたが、確かめていなかったので取り消す。★開発側は、周が 100% を超えたときのレースの段の振る舞いを**コードで確かめて報告に書く**こと（配備の前に）
- ★手順書の段 ⑥ にこの 1 行を足すこと

## 7. 追記（2026-09-23）— 周が延びたときのレースの振る舞い（報告 §8）✅

- ✔ [CODE] 「いまの時刻」はループの最初に 1 回だけ読む（`cycle-runner.ts:284`）。期限切れの中止は発走から 60 分（`packages/scheduler/src/cycle.ts:89` `CANCEL_AFTER_START_MS = 60 * 60 * 1000`・`cycle-runner.ts:371` の `overdueRaces`）
- → 結論（開発側の読みを採る）: ①確定は遅れるだけで、遅れはループ 1 回ぶんを超えて積もらない ②中止はループが 60 分を超えない限り起きない ③組成の中止はループが 13 分を超えない限り起きない
- ⚠️ ★**実際に起きうる害は 1 つ**: 発売開始の直前に始めた組成が発売開始の後に終わると、**そのレースの発売時間（4 分）が短くなる**。★重い周（週送り・配合）の直後の周で起きうる。★本番の段 ⑥ で周の全体が 100% を超えたら、★**発売時間が何秒短くなったか**も報告に入れること
- ★§6 の私の「壊れない」の取り消しは、この確認で「中止にはならない（ループが 60 分を超えない限り）・発売時間が短くなりうる」に置き換わった
- **本番配備の準備はこれで整った。** ★残るのはオーナーの操作（一般登録を閉じる・配備の文を開発側に貼る）

## 8. 追記（2026-09-23）— §7 の条件への対応 ✅

- ✔ 発売の遅れは、そのままでは**測れなかった**（組成の終わった時刻の列もログの時刻も無い）→ 組成の直後に DB の時計を読み直し、遅れがあれば `[worker] ★発売の遅れ cycle=N +Xs` を出す（表示だけ）。試験は時計を進める／進めない対照と、1 行を外す変異で赤 → ✅。★「測れと言われて、測る器が無いことを先に言った」のは正しい
- ✔ 手順書 ⑥ の 5 行目に「100% を超えたらこの行の秒数も報告」
- ⚠️ `6e07571` で PowerShell が UTF-16 で書いたログを、門を流さずにコミットしていた（source-integrity が赤）→ UTF-8 に書き直し・門は通過・報告に明記。★**原因の半分はレビュー側**: 私がオーナーに渡した `Tee-Object` の形は、PowerShell 5 では UTF-16 でファイルを書く。★今後オーナーに渡すときは、ログを UTF-8 に直してからコミットする手順を添える
