# REPORT_P4_RACEVIEW_20260813 — ★走破タイムの差が 35秒（415m）／オーナーが画面を見て挙げた13件

## 本便で正典の改訂が要る決定をしたか

**していません。** ★ただし **§8.7 に関わる実測**があり、照会に出しています（Q-P4-23）。

---

## 0. ★結論

オーナーの求めで**開発サーバーで実際のレースを見られるように**しました。
そして**見たら13件の欠陥が出ました。** うち**最も重いものは描画ではなくエンジン**でした。

```
エンジンの走破タイム（同クラス12頭・1600m・40レース）
  勝ち時計 :  99.9 秒（16.0 m/s）   ← ★正しい
  最後の馬 : 134.9 秒（11.9 m/s）
  ★差     :  35.0 秒 ＝ ゴール時点で 415m

実際の1600m戦: 差は 2〜4秒（30〜60m）
```

★**10倍です。** そして**この差は、どんな演出でも隠せません。**

---

## 1. 🔴 走破タイムの差（Q-P4-23）

### 何が起きているか

馬群の広がりを測りました（60レース）:

| 残り | 先頭〜最後尾 | 馬身 | 画面（23m）に入るか |
|---|---|---|---|
| 1500m | 47m | 20 | ★入らない（2.0倍） |
| 900m | 217m | 90 | ★入らない（9.3倍） |
| 100m | **381m** | **159** | ★入らない（16.4倍） |

★オーナーの指摘「**色んな馬が画面から消えて〜また出て〜**」は**これ**です。
**画面に映るのは 23m ぶん**（1280px ÷ 55px/m）なので、**常に1〜2頭しか映りません。**

### ★原因の切り分け（丸投げにしないため測りました）

| 揃えたもの | 差 | 1着〜最下位のスコア差 |
|---|---|---|
| そのまま | 35.0秒 | 63.6% |
| 脚質を全部同じに | 34.2秒 | 62.2% |
| **能力を全馬同一に** | 35.2秒 | 64.1% |
| 距離適性を全馬同一に | 34.5秒 | 62.7% |
| 脚質適性を全馬同一に | 35.3秒 | 64.3% |
| **★全部同一に** | **34.7秒** | **63.2%** |

★**能力・脚質・距離適性を全部同じにしても、差は縮みません。**
**能力差から出た差ではありません。**

### ★仕組み（§8.7）

```
baseTimeSec = 距離 ÷ 速度        ← ★出走馬に依存しない（だから勝ち時計は常に 99.9秒）
timeGapSec  = (1着のスコア − その馬のスコア) / 1着のスコア × baseTime × TIME_GAP_FACTOR(0.55)
```

★**スコア差がそのまま時間差になります。** そして**スコア差が 64%** あります。

### ★どちらが過大か（判断は仰ぎます）

実際に合わせるなら **差 2〜4秒 ÷ 99.9秒 ＝ 2〜4%** です。

| 案 | 内容 |
|---|---|
| A | **`TIME_GAP_FACTOR` が過大**。スコア差 64% のままなら **0.03〜0.06**（いまの 1/10 以下）が要る |
| B | **スコア差が過大**。0.55 のままなら**スコア差を 4〜7%** に抑える必要がある |

★**私の見立ては B です。** 同クラスで**スコアが64%も開くこと自体**が、着順の意味を薄くしていると考えます
（1着と最下位の実力差が64%なら、着順はほぼ確定した順番のなぞりになります）。
★ただし **§8.7 と較正の領域**なので、決めていません。

### ★なぜ今まで出なかったか

**走破タイムの分布を見るゲートがありません。**
V-1/V-2 は遺伝、V-4/V-5 は育成、V-10/V-11 は経済、V-13 は仕掛け。
★**「レースがレースに見えるか」を見る基準が1つも無い**ので、
**画面を作って初めて出ました。**

---

## 2. オーナーが画面を見て挙げた13件

### ★直したもの（9件）

| # | 指摘 | 原因 |
|---|---|---|
| ① | 馬の顔が切れる | ★**シートを「幅÷6」で等分割**。実際は 302〜355px の**不等間隔**。頭が隣のコマに入り、**宙に浮いた頭と蹄**が出ていた。**連結成分**で切り出し、**自分の塊以外を消す**ように |
| ② | 走り方が小走り | ★**脚を「進んだ距離」で回していた**ので、道中3倍速で**脚も3倍速**。→ **表示の時計で毎秒2.0〜2.6歩** |
| ②' | 走りが跳ねる | ★**下端で揃えていた**（下端は 472〜500 で 28px ばらつく）。上端は 247 で一定、**ゼッケン布の重心**はばらつき 5px → **胴体基準**に |
| ③ | 馬の上の黄色い線 | ★中身が「余力」ではなかった（実測 −0.518 で逆を向く）。**既定で消した** |
| ④ | 芝の上（空）を走る馬 | ★**段のずれが負**になっていた。**巡回**させて走路内に |
| ⑤ | 速度が不自然 | ★**§3 に詳述**（私の欠陥3つ） |
| ⑦ | ポールがない | ハロン棒を追加 |
| ⑨ | 追いつきが不自然 | ★⑤と同根 |
| ⑩ | **1頭を中央に固定** | ★カメラを**先頭集団の重心＋先頭寄り**に。自馬が画面から出ない範囲で止める（C-6） |
| ⑪ | 馬群がまばら | 3段105px → **6段46px**、**奥→手前**で描く（手前が奥を隠す）、**手前のラチ**を追加 |

### ★直せていないもの（4件）

| # | 指摘 | 理由 |
|---|---|---|
| ⑥ | **急にアップになる切り替えが不自然** | 倍率が整数のみ（D-058）なので瞬間切り替えは避けられません。**繋ぎ方は設計判断** |
| ⑧ | **直線しかない／コーナーがない** | ★**アートバイブル §3「線遠近を描き込まない」と正面から衝突**します |
| ⑫ | **騎手が屈んでいない** | ★**素材の問題**。いまのシートは騎手がほぼ直立 |
| ⑬ | **生物的な走りでない** | ★**素材の問題**。脚の伸展・収縮が小さく、ギャロップの「飛んでいる」局面がない |

---

## 3. ★⑤ の原因は私のコードでした（3つ）

### ① 脚質バイアスが強すぎた（`watch.ts`）

区間ごとの平均速度を測ったら**非物理**でした:

```
【修正前】                       【修正後】
逃げ 17.2 → 13.0 → ★8.9 m/s    14.1 → 12.6 → 11.7  （±10%）
追込 11.5 → 17.9 → ★19.7       13.2 → 15.0 → 15.8  （±11%）
```

★**ゴール前 8.9 m/s は、駆け足ではなく速歩の速さです。**

`x(τ) = τ + b·sin(πτ)` で速度は `1 + bπ·cos(πτ)`。b が最大 0.13 だったので **±41%** 振れていました。
→ 実際の区間平均は **15〜17m/s（±10%程度）** なので、**±12% を上限**にしました。
★**`finishSec` に触れていないので、着順は変わりません。**

### ② 折れ線で繋いでいた

境界（残り800m/400m）で**速度が瞬間的に変わって**いました。
→ **単調保存の3次補間**（折れ点は必ず通る・**後戻りしない**）。

### ③ 時間配分が段で切り替わっていた

★実測: 3倍速→1倍の切り替えで **0.2秒に 31.3 m/s** の速度変化。
→ 滑らかに繋いで **2.2 m/s**（14分の1）。
さらに**道中の送りを 3倍 → 1.8倍**に（3倍では画面上の速さが**実馬の4.2倍**、1280px を **0.3秒**で横切っていました）。

---

## 4. ★私が今回した間違い

| # | 間違い | 捕まえ方 |
|---|---|---|
| ① | ★**推測で直そうとした** | 最初は指摘を読んで原因を推測しました。**やめて、自分で画面を見ました。** 見たら**全部違いました** |
| ② | ★**「最低速 0.4m/s ＝ 馬が止まる」と読んだ** | **ゴール線をまたぐ最後の1標本**を除いていなかっただけ。**存在しない不具合を追いました** |
| ③ | ★**`isDark` が2か所にあり閾値が 140 と 128 で食い違っていた** | 共通化したときに気づきました |
| ④ | ★**「4件は判断待ち」と報告した** | オーナーに「**3件はあなたの仕事では**」と指摘され、そのとおりでした |
| ⑤ | ★**スプライトが届いても再描画しない**（画面が真っ暗） | オーナーの報告 |

★**④が本便で一番重いです。** レビュー側は「演出は自由。V-16 を悪化させないこと」と
**既に明記していました。** 私はそれを読んだうえで「判断領域」に回していました。

---

## 5. 権利について（確認）

オーナーから**1996年の他社作品のスクリーンショット**を参照として受け取りました。

★**採ったのは画作りの作法だけ**です（カメラの間合い・馬群の密度・手前のラチ・実況帯の存在）。
D-060 の「業界共通の作法は採ってよい」に沿います。

★**製品名・実在馬名・実在の勝負服の意匠は、コードにもコメントにも出力にも入れていません**（憲法 §0.1）。
画像そのものもリポジトリに入れていません。

---

## 6. 🔴 照会

| # | 内容 |
|---|---|
| 🔴 **Q-P4-23（新規・最重要）** | ★**走破タイムの差 35秒（415m）**。A（`TIME_GAP_FACTOR` を 1/10 に）／B（スコア差を 4〜7% に）のどちらですか。★**見立ては B** ですが §8.7 と較正の領域です |
| 🔴 **Q-P4-24（新規）** | ★**「レースがレースに見えるか」を見るゲートがありません。** 走破タイムの分布・馬群の広がりを V-16 の隣に置くべきだと考えます |
| **Q-P4-25（新規）** | ⑧ **コーナーを描くか**。アートバイブル §3「線遠近を描き込まない」と衝突します |
| **Q-P4-20** | D-059 の「境界の位置を動かさない」を改訂するか（既出・未裁定） |
| **Q-P4-21** | `emptyAtMeter` を描画層に渡してよいか（既出・未裁定） |
| **Q-P4-22** | ゲージの定義（★逆を向いていた件・既出・未裁定） |

★**Q-P4-23 が決まらないと、②「馬が画面から消える」は直りません。**
★**Q-P4-21 が決まらないと、V-16 ①② は勝負所で落ち続けます。**

---

## 7. 現物

```
開発サーバー: npm run dev:web  → http://localhost:3100/watch
```

| ファイル | 内容 |
|---|---|
| `apps/web/src/app/watch/page.tsx` | 観戦画面（★機構は `packages/` の純粋関数） |
| `apps/web/src/lib/canvas-renderer.ts` | ★**描画コマンドを描くだけ**（§12.8） |
| `tools/diag-finish-spread.mjs` | ★走破タイムの差の切り分け |
| `tools/diag-pack.mjs` | ★馬群の広がり |
| `tools/diag-speed.mjs` | ★画面上の速さ |
| `tools/shot.mjs` | ★自分で画面を見るための静止画 |
| `tools/lib/dress.mjs` | ★勝負服・ゼッケンの塗り（**1か所化**） |

---

## 8. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
2. ★**スプライトの作り直し**（⑫⑬: 屈んだ騎手・伸展のあるギャロップ）は**私の作業**です。
   **裁定を待たずに始められます。**

---

**テスト 724件緑 / `tsc --noEmit` 通過（root と apps/web の2プロジェクト）**
