# 開発指示書 P0-fix5 — 導出の自己整合を閉じる（P0 の締め・小便）

**発行**: 2026-08-05（レビュー側 Claude）
**前提**: `REVIEW_P0FIX4_VERDICT_20260805.md`（特に §7）を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**丈夫さ sd を 66 に是正し、丸めの規約を §6.4 に明文化済み**）

---

## 0. まず結論 — P0-fix4 は PASS です

合格基準17項目すべて成立し、`npm run verify`・テスト93件・typecheck・**I-4 の実測（既定66/341 → 回帰0.05で34/177 → 創始SD200で131/341）**をレビュー側の環境で完全再現しました。

**I-3 は目的を達成しています。** 監査で前回と同じ実験を両方とも再現し、①純関数の書き換え → 4件 FAIL、②**呼び出し側への書き戻し → I-3 の3件が FAIL**（純関数テストは通過＝前回の穴が塞がれたことの裏取り）を確認しました。上限と下限の**両側**を押さえた設計が効いています。

**Q1 はあなたが正しく、正典の 65 は私の丸めミスでした。** 表の clamp 5件がすべて「丸める前の sd × 1.667」と一致していることから、**表の内部整合を使って sd 側の誤りを逆算した**指摘の仕方が的確でした。正典を 66 に是正し、丸めの規約（「表の値は導出式の出力であり、食い違ったら式が正」）も明文化しています。

自己申告3件はすべて承認。特に **I-2b で「判定不能なら `pass: false`」を選んだ判断**は正しく、**I-2c の併記が即座に「故障率の分母は表現型の 636」を可視化した**のは指示の狙いそのものでした。

**残るのは下記3件のみです。いずれも出荷既定の挙動には影響しませんが、J-1 は P1 の較正作業を確実に壊します。**

---

## 1. スコープ — 3件

### J-1【必須】切断係数を clamp比の関数にする

`CLAMP_TRUNCATION_FACTOR = 0.9156` は **k = clamp/sd の関数**ですが、リテラルで固定されているため CLI 上書きで k が変わっても追随しません:

| 経路 | k | 正しい係数 | 現在の丈夫さ sd | 導出が要求する sd |
|---|---|---|---|---|
| `--set-genetics MUTATION_SD=45` | 3.333 | 0.9992 | 66 | **60**（−10%） |
| sweep（sd 75） | 2.000 | 0.9594 | 66 | **63** |
| sweep（sd 100） | 1.500 | 0.8823 | 66 | **68** |

`balance.ts` のコメントは「比を変えたら再計算せよ」と警告しているのに、**コード自身にその警告を破る経路がある**状態です。さらに `config.test.ts` は `clamp/sd ≈ ratio` しか見ないため、**sd が誤ったままテストが緑になります**（欠陥を固定している）。

**これは I-4（CLI が導出をバイパスする）とまったく同じクラスの問題です。** そして P0-fix の誤結論を2度生んだのと同じ構造でもあります。

対処はどちらでも構いません:

- (a) 係数を k の解析式として実装する（`(2Φ(k)−1) − 2kφ(k) + 2k²(1−Φ(k))` の平方根）
- (b) `MUTATION_CLAMP_RATIO` 以外の比を**明示的に拒否**する（fail-closed）

(a) が筋ですが、正規分布のCDF/PDFを持ち込む必要があります。(b) でも「静かに間違わない」という要件は満たせます。**選んだ理由を報告書に書いてください。**

テストは「**比を変えたら係数と sd が追随する**」ことを固定してください。現在の `clamp/sd ≈ ratio` だけを見るテストは、この欠陥を通してしまいます。

### J-2【必須】`basis` 分岐をテストで固定する

I-2b で決めた「創始平均0の形質は創始SD基準へ切替、両方0なら `pass: false`」が、**何のテストにも固定されていません**（`grep -rn basis` が空）。

**I-3 であなたが受け入れた「経路を固定せよ」がそのまま当てはまります。** 3分岐（`ratio` / `sd` / `none`）それぞれを固定してください。特に `none` で確実に FAIL することを。

### J-3【必須】`sweep.ts` の r=0 構成で変異が無言で止まるのを防ぐ

`sweep.ts` の構成①②③は `regression` 未指定 → `regressionRate = 0` → `derive()` が **sd 0 / clamp 0** を返し、**非能力6形質の変異が完全停止**します（創始値で凍結）。印字列が能力形質のみなので報告数値には出ませんが、無言の縮退です。

`derive()` が `r = 0` を受けたときの挙動を明示してください（エラーにする／警告を出す／既定 r を使う のいずれか）。**静かに 0 を返さない**ことが要件です。

---

## 2. やらないこと

- V-1/V-2a〜e/V-3 の閾値変更
- 既定定数の変更（丈夫さ 66・気性10・適性13・距離341/114 は確定）
- 表現型を V-2d の判定に使うこと（下記 §3 参照）
- レースエンジン以降（P1）

---

## 3. 表現型の気性が既定設定で ±10% を超えている件 — 欠陥ではありません

監査で、既定 `npm run simulate`（200頭・100年・平均F=0.0404）では表現型の気性が +10.2〜15.2% になることが分かりました。報告書 §1 の「P0 では平均F が小さいので無害」は 800頭/300年（F=0.011）のみを根拠にしていたので、そこは訂正が要ります。

**ただしこれは欠陥ではありません。** §6.5 が定めたインブリードの代償（`temper += F×120`）がそのまま出たもので、**集団が小さければ近交が進み気性が荒くなる**のは設計どおりです。

したがって:

- **V-2d は genotype 判定のまま**（表現型を混ぜると、遺伝的な水準ずれと近交の影響が区別できなくなります）
- **P1 の監視項目に「平均F」と「表現型−genotype の乖離」を追加**してください。乖離の拡大は近交が進んだ指標であり、故障率と気性難の実効値が設計値から離れることを意味します

---

## 4. 合格基準

| # | 基準 |
|---|---|
| 18 | **clamp比を変えたら切断係数と導出 sd が追随する**（または比の変更が拒否される）ことがテストで固定されている |
| 19 | `basis` の3分岐がテストで固定され、`none` で確実に FAIL する |
| 20 | `derive()` が `r = 0` を静かに受け入れない |
| — | 既存17項目をすべて維持（`npm run verify` の数値が P0-fix4 と一致すること。**既定経路の挙動は変わらないはず**） |

---

## 5. 報告様式

`REPORT_P0FIX5_[日付].md` に:

1. 変更点（J-1 / J-2 / J-3）と J-1 で (a)/(b) どちらを選んだかの理由
2. 再実行コマンド
3. 検証結果（**`npm run verify` が P0-fix4 と同一数値であること**を示す）
4. 仕様からの逸脱・解釈（全件列挙。ゼロなら「なし」と明記）
5. 未解決の質問

---

## 6. これで P0 完了 → 次便は P1 です

J-1〜J-3 が通れば **P0（遺伝エンジン）完了**とし、次は **P1: レース判定エンジン + モンテカルロ検証**の指示書を発行します。

P1 引き継ぎ（確定）:

1. 選抜 h²=0.3 → **実レース結果に基づく選抜**へ置換
2. **選抜下での分化の維持** — V-4〜V-6 較正時に `distance_range` と V-2e の推移を確認。**基準線は P0-fix4/5 の数値**（P0-fix3 と比べると切断補正の系統差で解釈を誤る — あなたの Q2 の指摘どおりです）
3. `INJURY_BASE` の較正は**表現型**の丈夫さ（近交込みで 636 前後）を分母とする
4. **平均F と 表現型−genotype 乖離の監視**を追加（§3）
5. P0 のゲート（V-1/V-2a〜e/V-3）はレースエンジン導入後も回帰テストとして回し続ける
6. 大物覚醒の演出は能力を明示する形（P4 への申し送り）
