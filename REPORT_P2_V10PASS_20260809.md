# REPORT_P2_V10PASS — V-10 が100レースで PASS／配備完了／本番データが解析予測を裏付けました

**開発側 → レビュー側** / 2026-08-09
**コミット**: `541a016`（push 済み・**453 tests** 全PASS / typecheck 0）
※ 直前のコミットメッセージに「446 tests」と書きましたが**453 件**です。

## 本便で正典の改訂が要る決定をしたか

**なし。**

---

## 1. ★条件① — V-10 が100レースで PASS。N=100 の妥当性も測りました

```
seed=42 / 100レース / 各レース確定20,000回 / M=3,896,104 / 本番コード経路

  券種            払戻率    乖離     レース間SD    SE      売目/R
  win            81.89%  -0.11pt    1.04pt   0.104pt     13
  place          81.93%  -0.07pt    0.48pt   0.048pt     13
  quinella_place 79.92%  -0.08pt    0.67pt   0.067pt     62
  quinella       79.92%  -0.08pt    1.40pt   0.140pt     67
  exacta         79.90%  -0.10pt    1.47pt   0.147pt    132
  trio           76.73%  -0.27pt    1.59pt   0.159pt    246
  trifecta       76.52%  -0.48pt    2.09pt   0.209pt   1572

  ★上限に当たった売り目: 0 件
  ★V-10 総合: PASS
```

### ★N=100 で足りているか — 推測せず、余裕を数字で出します

**いちばん厳しいのは三連単です。**

```
三連単  乖離 -0.48pt  SE 0.209pt
  → 0 からの距離 2.3 SE ＝ ★この残差は偶然ではなく実在します
  → 判定側の余裕: |−0.48| + 2×0.209 = 0.90pt  対  ゲート 1.00pt
```

**ゲート内ですが、2 SE を見ると余裕は 0.10pt しかありません。**
他の6券種は最悪でも 0.59pt（三連複）なので十分です。

★**残差 −0.48pt 自体は設計どおり**です。λ*=30 は「打ち切りを**無視できる**」ではなく
「**十分小さくできる**」水準で、ゼロにはなりません。
余裕を厚くしたい場合の調整弁は λ* で、**M は λ* に比例**します
（λ*=60 なら M=7.8M・1レース約230秒）。**採否はそちらの判断です。**

---

## 2. ★配備しました。そして本番データが解析予測を裏付けました

`tools/deploy.sh` で `dcd2373 → 541a016`。**リリースディレクトリ＋symlink 切替**で、
検証→張り替え→ヘルスチェックまで一度も止まらずに完了しました。

### ★配備前後の本番オッズ表（同じ番組・連続するサイクル）

```
  cycle  三連単目数  capped  最大オッズ
   175      1831       71     7,700.00   ← 配備前
   176       928       38     7,700.00
   177      1263       56     7,700.00
  ─────────────────────────────────────
   178      1306        0    93,750.00   ← 配備後
   179       720        0    37,974.70
   180      1855        0    93,750.00
```

★**配備前の最大オッズ 7,700 は、解析で出した「推定の天井 (1−margin)×M」ちょうど**です
（0.77 × 10,000）。**§9.4 が三連単の上限を100,000倍と定めているのに、
実際には7,700倍が上限**でした。上限が「効いている」のではなく、
**推定が届いていなかった**だけで、しかも 71/38/56 件が黙って切り詰められていました。

**配備後は `capped` が全レースで 0 件**になり、93,750倍まで実際に出ています。
計算で言っていたことが、本番のデータで確認できました。

### 実測した所要時間

```
生成した周のティック長 ≈ 116秒（1レース・M=3,896,104）
  ※予測137秒は16頭立ての最悪値。実際の頭数はより少ない
  ★10分サイクルの 19%。超過は起きていません
```

---

## 3. D-037 / D-038 を実装しました

### D-038（確定を先に）

**ご質問への回答: 生成は確定の結果に依存していません。**
本番コードは `horses` テーブルを**一度も更新しません**（`grep -rn "update horses"` が空）。
したがって入れ替えは**速度の是正であって正しさの是正ではありません**。

★§7 の成長や §10.3 のクラス昇級で確定が馬の状態を書くようになったら、
  **この順序は正しさの問題になります**。コードにその旨を残しました。

テストは「生成より前に確定がある」ではなく**並びそのもの**（`['settle','cancel','create','create']`）を
検査します。前者だと**生成が0本の周でも通ってしまい**、順序を検査したことになりません。

### D-037（確定失敗を60分で中止）

`overdueRaces` → `cancelRace` をループに接続しました。
★**通報は省略できない引数**にしています。既定を「何もしない」にできる形だと、
  いつか誰かが省略します。中止0件の周はログに出しません
  （毎周 `中止=[]` と出ると、**実際に起きた周が埋もれます**）。

---

## 4. ★過程で本番コードの欠陥を2件見つけました

**どちらも偽ストアの単体テストでは絶対に出ず、実 DB を叩いて初めて出たものです。**

| # | 内容 |
|---|---|
| ① | `overdueRaces` の SQL が `($1 - $2)` で **`operator is not unique: unknown - unknown`** で落ちていた |
| ② | ★`cycle_index` は bigint なので `pg` が**文字列で返す**。`pendingSettlements` は `number[]` と型付けされているのに中身は文字列で、**数値比較した瞬間に静かに外れる**状態でした |

②は `horse-repo` で潰した形の**再発**です。**「型が通る」は「値が正しい」ではありません。**

- ①は引き算を SQL から出し `overdueBefore()`（純関数）に移しました。
  **副産物として、定数が SQL に閉じ込められず変異試験で防御できます。**
- ②は `toCycleIndexes()` で必ず数値に直し、直せなければ例外にします。

### ★登録した変異が本当にテストを落とすか確かめました

`CANCEL_AFTER_START_MS` を較正定数として登録し、**0 に変異させて2件のテストが落ちること**と
**復元されたこと**を確認しました。**落ちない登録は防御していないのと同じ**なので、
登録して終わりにしていません。閾値には定数自身を使わず正典の60分をリテラルで置いています（D-018）。

`tools/verify-overdue.mjs`（実 DB・59分→返らない/61分→返る）を追加しました。
★異常終了時の片付けは**実際に例外で発動**しました（R-18）。

---

## 5. A-1（判定は本日 20:53 以降）

```
生成した周 108 / 確定した周 104 / ★周の欠落 なし（cycle_index 連番）
メモリ 69MB / FD 26 / DB接続 1 / ★開催中止 0件
配備後のレースで上限に当たった売り目 0件
リリースディレクトリ 2世代（3世代を超えたら自動削除）
```

★**本日の再起動は3回**です（7分停止の復旧 / 配備方式の移行 89秒 / 本配備）。
  D-034 の範囲ですが、**M 変更前後をまたいでいるので、
  「M=3,896,104 での24時間」は別途測り直す必要があります。**
  配備は 14:16 なので、そのまま回せば明日 14:16 に24時間ぶんが揃います。

### サイクル超過の実測

**116秒 / 600秒で、超過は起きていません。**
周の欠落なし・2周先まで常に存在（`既存=2`）を確認しました。
★超過そのものは起きていないので、**「超過したとき」は実測できていません**。
  設計上の答え（二重に走らない/スキップしない/追いつく）は前便のとおりです。

---

## 6. 合格基準

| # | 状態 |
|---|---|
| A-1 | **計測中**（★M 変更後の24時間は明日 14:16 に揃う） |
| A-2 | **PASS** |
| **A-3** | **★PASS**（V-10 全7券種・100レース・本番コード経路・本番配備済み） |
| A-4〜A-9 | **PASS**（453 tests / typecheck 0） |
