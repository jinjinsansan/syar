# 常設レース表示と本編のデータ契約案

## 目的

`RaceStrip` と `/race` が、同じ実レース ID・同じ進行位置・同じ確定結果を表示する。現在の `/race` は固定プールとシードによるデモなので、実レース接続の正本としては使えない。

## 読み取りデータ

サーバーが公開する一つのレース中継スナップショットに、次を含める。

| 項目 | 用途 |
|---|---|
| `race_id`, `cycle_index`, `course_id` | 画面間で同じレースを選ぶ |
| `phase`, `server_time`, `scheduled_at`, `broadcast_started_at` | サーバー時刻を基準に進行位置を決める |
| `sequence`, `generated_at` | 古い応答や再接続後の逆戻りを防ぐ |
| `entrants`（公開可能な馬番・馬名・枠） | 表示する馬の識別 |
| `positions`（馬番・コース上の距離・横位置・進行率） | 常設表示と本編を同じ状態で描く |
| `result`（確定後のみ） | ゴール後の着順表示 |

`positions` はレースエンジンの正本からサーバー側で作る。クライアントで着順・速度・乱数を決めない。`seed_reveal` と確定結果は、既存の公開時点より前に出さない。

## 時刻と配信

- 現行は6分サイクルで、ワーカーの通常起床は60秒ごと。これだけでは短い演出の各瞬間を同期できない。
- 現行ワーカーは `scheduled_at` 以降に `settleRace` を一度呼び、その同じ処理で着順を保存する。`replayPositionModel` は確定したレースの境界時刻から画面用の位置を補間する関数で、走行中の位置を蓄積していない。この経路のままなら映像の公開は確定後の「録画」と明示する。
- 中継用の開始時刻とスナップショット更新間隔を別途決め、ワーカーが取りこぼした場合も「進行中」と偽らず、待機・遅延を表示する。
- クライアントは `server_time` と受信時刻の差で表示位置を合わせる。タブ復帰とページ移動時には最新の `sequence` を取得し直す。
- 同じレースを別ページで開いても、ローカル再生の開始時刻を 0 に戻さない。
- 走行がまだ公開できない場合は、現在の実装どおり開催時刻・受付状態・結果の文字表示だけを出す。

## 実装方式の分岐

| 方式 | サーバーで必要な変更 | 画面での表示 |
|---|---|---|
| 確定後の録画 | 確定トランザクションで、実出走馬と確定結果から再生用データを保存する。確定後だけ公開するビューを追加する | 「レース録画」と明記し、共通の開始時刻から各ページで同じ位置を再生する |
| 同時進行の中継 | 発走より前に、結果の秘匿を守りながらサーバーが位置を時刻別に公開する処理を追加する。60秒周期とは別の更新経路が必要 | 更新できた区間だけ「中継」とし、遅延時は静止・待機表示にする |

どちらの方式でも、クライアントが固定 `POOL` から独自に着順を作る現行 `/race` は、本番映像の正本にできない。導入には新しい DB 移行とワーカー変更が必要で、既存の血統修復 → 0060 → 配備の順序に組み込む。

### 暫定の録画表示（ローカル実装済み）

公開済みの `race_entries_public` が持つ確定後の馬番・馬名・脚質・走破タイム・着順を読み、`secAtMetersLeftOf` で途中位置を補間する。画面には「結果から再現」と明示し、確定済みのレースだけに限る。常設帯から同じページ内で拡大でき、フォームの React 状態は保持される。公開ビューで直近の確定レースを読み、14頭のデータが検証条件に合うことを確認した。

この方式はワーカーの位置スナップショットを使う生中継ではない。予定時刻の75秒後から45秒間だけ表示し、確定が間に合わないと途中から始まるか、その回を見送る。端末間の時計差もまだ補正していない。これらを解消して常設の本編映像にするには、上記のデータ契約と時刻配信が必要になる。

## 表示段階

1. `announced` / `scheduled`: 次のレース、登録・投票状態、発走予定時刻。走行なし。
2. `closed`: 締切済み。サーバーが中継開始を示すまで走行なし。
3. `broadcasting`: サーバーの同一 `sequence` / 位置データから「大」「極小」と本編を描く。
4. `settled`: 確定結果を表示し、短時間強調した後に次レースへ戻る。
5. `cancelled` / 遅延 / 更新失敗: 状態を明示し、架空の走行を流さない。

## 実装前の判定

- 同じレース ID の常設表示と本編の馬番・位置・着順が一致する。
- ページ移動、縦横切替、再読み込み、タブ復帰で進行位置が先頭に戻らない。
- 発走前の公開データから確定着順を読めない。
- 通信断は最後の取得内容に更新失敗を添えて表示し、復帰後に正しい `sequence` へ追いつく。
- 本番 DB・ワーカーへの変更は、血統修復 → 移行0060 → 配備の既存手順と競合しない順序にする。
