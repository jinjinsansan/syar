# REPORT_P1FIX2 — P-1 / P-2 / P-3 完了（2026-08-06）

**指示書**: `DEV_INSTRUCTIONS_P1FIX2_20260805.md` §1（P-1〜P-5）＋ Q-1〜Q-6 ＋ S-1〜S-4
**最終コミット**: `41885b9`
**位置づけ**: **P1 のクローズ報告**です。これで P1.5 / P2 の指示書起草に進めます。

---

## 1. P-1〜P-3 の対応

| # | 内容 | 状態 |
|---|---|---|
| **P-1** | `heavy_aptitude` を genotype に追加（D-015） | 完了・V-2f に**実測で**分類 |
| **P-2** | スタミナ消費のレース時間比例化（D-017）＋ 較正 | 完了・目標挙動3点を実測で確認 |
| **P-3** | `verify.ts` に V-2d / V-2f の分割を反映（D-019） | 完了・本規模で PASS |
| **P-4** | V-6 を出走頭数別に分解 | 完了（前便・下位3ランク平均へ再定義） |
| **P-5** | D-020 の設計含意 | 実装対象なし（クラス水準は `classLevel` として保持済み） |

---

## 2. 再実行コマンド

```bash
npm install && npm run typecheck && npm test      # 256 passed
npm run mutation                                   # 25件すべて防御

npm run verify -- --selection race                 # P0 ゲート（実レース選抜）
npm run verify                                     # P0 ゲート（proxy・既定）
npm run verify:race                                # V-4/V-5/V-6/V-8/V-9/V-12

npx tsx apps/cli/src/selection-compare.ts --generations 150 --population 300 --seeds 42,7
npx tsx apps/cli/src/decompose.ts
```

---

## 3. P-1 の結果

### 3-1. 導出は既存経路がそのまま正典の値を出しました

創始定義 `HEAVY_RANGE = [20, 90]`（芝/ダート適性と同型）から、D-013 の導出式がそのまま:

```
heavy_aptitude 導出値: {"sd":13,"clamp":22,"center":55}
正典 §6.4 の表  : sd 13 / clamp 22 / 中心 55   ← 完全一致
```

**新しい定数を人が手で入れる余地がありません。**

### 3-2. 馬場を4段にしました（正典との差分を是正）

実装は3段（良/稍重/重）でしたが、正典 §5.2 は「稍重・重・**不良**でのみ heavy_aptitude が効く」と4段を明記しています。3段は P0 の遺物（旧 `condition_aptitude` が「重〜不良」を1段に潰していた）で、**この形質が最も強く効くのは最悪の馬場**である以上、不良を落とすと D-015 の目的が薄まります。

あわせて `I-HEAVY-SEVERITY`（稍重0.4 / 重0.7 / 不良1.0）を新設。正典は4段を列挙しますが段ごとの強さを定めていないため、一律にすると**4段にした意味が消えます**（稍重も不良も同じ係数になる）。

### 3-3. 分類は V-2f — **実測してから決めました**

150世代・繁殖牝馬300頭・シード42,7:

| 形質 | proxy | race選抜（既定 prizePerStart） | 振れ幅 |
|---|---|---|---|
| surface.turf | −2.45% | **+6.93%** | 9.4pp |
| surface.dirt | −5.11% | **+2.02%** | 7.1pp |
| **heavy_aptitude** | **−0.22%** | **+0.55%** | **0.77pp** |

**符号が反転している**ので選抜圧はかかっています。D-019 の基準は「§8.3 の乗算補正が使う形質か」であって大きさではないので **V-2f** です。

振れ幅が他の適性の約1/10なのは、発現機会が**非良25%**に限られ、その大半が稍重（効き0.4）・重（0.7）で、不良（1.0）は3%しかないためです。
⚠️ **`TRACK_CONDITION_CDF` / `TRACK_COND_SEVERITY` を変えたら再測定が要ります**（較正定数のコメントに明記）。

### 3-4. P0 ゲートの移行前後（4シード × 100/300ゲーム内年・繁殖牝馬800頭）

| | V-1(100年) | V-1(300年) | V-2e最悪 | 総合 |
|---|---|---|---|---|
| **移行前（基準値）** | 15.42% | 15.05% | 0.91x | PASS |
| 4段化のみ | 15.31% | 15.23% | 0.95x | PASS |
| **接続後（race選抜）** | **15.09%** | **15.12%** | 0.99x | **PASS** |
| 接続後（proxy） | 15.97% | 15.91% | 0.99x | PASS |

**形質を1つ増やしても P0 ゲートはほとんど動きません**（V-1 −0.33pp）。
V-2d の余裕が薄い件（移行前 9.13%）も悪化していません。

★この切り分けができたのは、**`heavy_aptitude` を「遺伝させるが使わない」状態で一度コミットした**からです。接続前後の対照が自然に取れるので、§3-3 で踏んだ「複数の変更を同時に入れて一方に帰属させる」形を構造的に避けられました。

---

## 4. P-2 の結果

### 4-1. 何が壊れていたか

旧実装は正典どおり「毎秒消費 1.0 × 係数」の**絶対値**でした。2000m（約120秒）では消費が 120〜576 に達する一方、ゲージ上限は 100 — **どんな馬でも必ず 0 になり**、§8b.3 の −0.15 が**全馬に常時適用**されていました。つまり**介入の巧拙が結果に出ない状態**で V-8/V-9 を測っていたことになります。

D-017 の時間比例（`毎秒消費 = 基準消費量 / レース秒数 × 係数`）にすると、**総消費が距離によらず一定**になり、この構造的破綻が消えます。

### 4-2. 較正結果（`STAMINA_BASE_DRAIN = 60`）

指示書の目標挙動3点を実測で確認しました:

| 目標 | 1200m | 2000m | 3200m |
|---|---|---|---|
| ① 中団・仕掛けなしの典型馬（初期値75）が2割残す | 残 15.0（**20%**） | 残 15.0（**20%**） | 残 15.0（**20%**） |
| ② 早仕掛け（残900m超）はゴール前に尽きる | バテ | バテ | バテ |
| ③ 最適な仕掛け（残495m）はバテない | ✓（倍率1.10） | ✓（倍率1.10） | ✓（倍率1.10） |

**③の「距離が変わっても同じ関係が保たれる」は、時間比例にしたことで自動的に満たされます**（較正で合わせ込んだのではありません）。

較正条件は定数のコメントに明記しました（R-7）: 「中団・仕掛けなしの典型馬（初期値75）がゴール時に2割残す = 60」。

### 4-3. V-8 / V-9 の是正前後

6万レース × 4シード（`settings`: popularityTrials 500 / poolGenerations 40 / poolMares 400 /
raceRandomK 0.22 / tailMixP 0.03 / tailMixM 5 / longshotRanks 3 / fieldStrengthFloor 0.5）

| # | 目標 | **是正前**（全馬に −0.15 が常時適用） | **是正後**（時間比例） |
|---|---|---|---|
| V-4 | 30〜34% | 32.05% | **31.33%** |
| V-5 | 60〜65% | 62.88% | **62.18%** |
| V-6 | 0.5〜2% | 1.17% | **1.22%** |
| V-8 | 93〜97% | 94.78% | **94.57%** |
| V-9a | 全サンプル 0.90〜1.10 | 0.90〜1.10 | **0.90〜1.10** |
| V-9b（監視・目標値なし） | — | 1.0426 | **1.0401** |
| 総合 | — | PASS | **PASS** |

★**数字はほとんど動いていません**（V-8 −0.21pt / V-9b −0.0025）。これは是正が効かなかった
のではなく、**旧実装では全馬に一律 −0.15 が掛かっていた**ためです。全員に同じ定数が乗るなら、
AI 代行と手動最適の**比**（V-8）にも、キャップ後の分布の**範囲**（V-9a）にもほとんど現れません。

★**意味が変わったのは「介入の巧拙が結果に出るか」です。** 是正前は仕掛けのタイミングを
どう選んでもゲージは必ず尽き、早仕掛けの罰が**選択肢として機能していませんでした**。
是正後は §4-2 の表のとおり、中団・仕掛けなしなら2割残り、早仕掛けなら尽き、最適なら
ゴール直前に使い切ります。**V-8/V-9 の数字ではなく、この挙動の差が P-2 の成果**です。

⚠️ したがって「V-8 が是正前後で変わらなかった」ことは、V-8 が介入モデルの品質を
測る指標として**鈍い**ことも示しています。倍率の比を見る限り、ゲージが常に空でも
合格域に入ってしまいます。V-8 の定義を見直すかは正典側の判断ですが、
こちらからは「この指標は今回の破綻を検出できなかった」という事実だけ報告します。

---

## 5. P-3 の結果

- **V-2d（水準維持 ±10%）**: 丈夫さのみ（§8.3 に現れず故障率にのみ効く＝選抜圧なし）
- **V-2f（平坦化 0.5%/世代未満）**: 気性・芝/ダート適性・距離2種・道悪適性

本規模の受け入れ（4シード × 100/300年）で **proxy / race選抜とも総合 PASS**。
V-2f の最悪傾きは **0.13〜0.22 %/世代**（合格域 0.5%未満）で、`heavy_aptitude` を含め余裕があります。

### ★前便で詰まった箇所の解決

J-2 の物差し切替テストは、**V-2d の対象が丈夫さ1つに絞られた結果、形質経由では分岐を試せなくなっていました**（丈夫さは値域が0以上にクランプされるので創始平均を0にできない）。前便ではここで詰まり、赤いまま残さないよう未コミット分を戻しました。

今回は `deviationBasis()` として機構を関数に切り出し、境界の両側を含めて直接検証する形に組み替えました。**「対象が減ったせいで機構のテストが書けない」を、機構を隠したまま放置しない**という判断です。

### ★分割で新しい穴を作らないための固定

分割は「どちらからも漏れる」危険を生みます。P0 の丈夫さ 650→450 がまさに「監視から漏れた」事故だったので、**「形質を増やしたら V-2d か V-2f のどちらかに必ず入る」** ことをテストで固定しました。片方に入れ忘れれば落ちます。

---

## 6. 今回見つけた不具合・訂正（自分の分）

| # | 内容 |
|---|---|
| 1 | **登録簿の変異が「値照合のみ」と誤判定する事象**。変異は宣言を1行で置換するため、複数行にまたがる定数だと残りの行が浮いて構文エラーになり、テストが収集されず ★名が出ない。定数を1行で書いて解消（`DEFAULT_RACE_BALANCE` を免除にしたのと同じ制約）。**S-2（テスト名の出力）が無ければ気づけませんでした** |
| 2 | **申し送りの基準値に実行中の途中出力（seed42 のみ）を書いた**。4シード完走値とは別物（V-1 16.33% → 15.42%、V-2e の最悪形質も違う）。`--popularity-trials=200` の件と同じ「条件が確定する前に報告した」形の2度目。手順に「測定が完走したことを確認してから数字を書く」を追加 |
| 3 | **テストが1件落ちた状態でコミットした**（`876761c`）。`grep` の終了コードで `&&` が繋がり失敗が素通りした。次コミットで解消。落ちた原因は S-4 の走査反転が新規ファイルの定数を正しく検出したもので、**構造としては期待どおりの動作**だった |

---

## 7. 仕様からの逸脱・解釈（範囲を明記）

`npx tsx apps/cli/src/print-interpretations.ts` で再生成できます。
**範囲**: `packages/race-engine/src` / `packages/sim-engine/src` / `apps/cli/src` の `*.ts`（`tools/` とテストは対象外）。

今回の追加:

| ID | 正典 | 埋めたこと |
|---|---|---|
| `I-HEAVY-SEVERITY` | §5.2/§8.3 | 馬場の悪さごとの効き（稍重0.4 / 重0.7 / 不良1.0）。正典は4段を列挙するが段ごとの強さを定めていない |

`I-COND-APT-SOURCE` は **D-015 で解決済み**に更新しました（経緯の記録として残しています）。
`I-STAMINA-WINDOW`（勝負所以降のみ消費という暫定解釈）は **D-017 で役目を終えました**。

---

## 8. 未解決の質問

**ありません。** 正典 §5.4 の反映漏れはそちらで修正済み、`HEAVY_RANGE = [20,90]` も §6.4 の追加行で裏づけられています。

P1.5 / P2 の指示書をお待ちしています。こちらから止めている項目はありません。
