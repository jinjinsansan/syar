# 【STAR】裁定: 🔴 騎手の料金が誰からも引かれていない（D-105）／名簿をどこに置くか — 2026-09-25

- 案件: **STAR**（馬物語）／レビュー側 Claude
- きっかけ: 開発側 `star-aa` が「正典が額を決めた項目 → 渡す側の実装」の**対応表を作った初日に 1 件 出た**（★裁定 `ab1cbac` §7 の ③）

## 1. 事実（✔ レビュー側も原文で確かめた）
- ✔ 名簿は **TS に在る**（`packages/scheduler/src/jockeys.ts` の `jockeyById` / `freezeJockey`・形は `{ v, jockeyId, name, feeEP, bond, effect, calm }`）
- ✔ `enter_race` は **`p_jockey_frozen ->> 'feeEP'` をクライアントの JSON から読む**（`coalesce(…, 0)`）
- ✔ 画面が送るのは **`{ id: jockeyId }` だけ**（`entry/page.tsx:117`）→ ★**キーも違い（`id` ≠ `jockeyId`）、`feeEP` が無い**
- → ★**誰からも 1 EP も引かれていない**。★返金側も `?? 0` を読むので**帳簿は合っている**（実害は「無料」だけ）
- 🔴 ★**より重いのはこちら**: ★**料金の出どころがクライアントの JSON**。★このまま繋ぐと `feeEP: 0` を送って**高い騎手を無料で乗せられる**（憲法 3「利用者の申告を使わない」）

## 2. 決定 — ★**名簿を DB の表に転記し、`enter_race` は `jockeyId` から引く**

★**種付料・ニックスの表と同じ形**（★前例: `d85d591`「配合の相性表を DB へ転記し、ワーカーがそれを読む」）:
1. ★**正は TS の名簿**（`jockeys.ts`）。★移行で **DB の表に転記**する（`jockeys` 表・`id` / `name` / `fee_ep` / `calm` …）
2. ★**検査で結ぶ**: ★DB の表と TS の名簿が**一致**すること（★`0074` の上限と `ownership-limits-sql.test.ts` と同じ形）。★増えた騎手を片方だけに足したら落ちる
3. ★**`enter_race` が受け取るのは `jockeyId` だけ**。★料金は**表から引く**。★`jockey_frozen` も**サーバー側で組み立てる**（★クライアントの JSON を凍結の中身にしない）
4. ★**引き落としは同じ取引の中で**（D-111 ①「料金と行の作成は同じトランザクション」・§14.4）。★記帳の語は D-105 ② に合わせる
5. ★返金側（`entry-freeze.ts:174`・`entry-scratch-runner.ts:104`）は**凍結の `feeEP` を読むまま**でよい（★記帳の一貫性）。★ただし ★**`?? 0` は「黙って 0」**なので、★**凍結に `feeEP` が無ければ落とす**（★D-119 の系・黙って 0 にしない）
6. ★**検査**: ①クライアントの入力から `feeEP` を読む箇所が **0 件**（構文）②`enter_race` に `jockey_frozen` を丸ごと渡す口が無いこと ③**高い騎手を選ぶと、その額が引かれる**（staging の予行・台帳の `balance_after` と残高の一致）

★「凍結をワーカーに移す」案は**採らない**: ★料金と行の作成が同じ取引でなくなる（D-111 ① に反する）。★ワーカーが書くのは**馬の凍結**（D-111 ②）で、★騎手の料金は**受付の時点で確定してよい**（★名簿はサーバーの持ち物なので、受付の時点で引ける）

## 3. 順番と、いま急ぐ度合い
- ★**いまは実害が無い**（誰も料金を払っていない・登録そのものが今日まで 0 件）。★しかし ★**出走登録が動き、EP が入り始めると、その日から「無料で乗せ放題」になる**
- → ★**EP の日次付与を本番に出す前か、同じ便で**直す。★**順番を逆にしない**
- ★`KNOWN_RED` に `owner: 'owner'` で載せたのは正しい（★**表から行を消して緑にしない**）

## 4. 対応表そのもの — ✅
★3 列（決定 / 額は言葉で / その額を動かす実装）・★**額の数は書かない**（`ep-grant-sql.test.ts` と役割を分けた）・★**表が嘘をつけない**ようにした（SQL なら移行に関数が在るか・TS ならファイルに名前が在るか・★引いている正典の決定が実在するかも見る）→ ★良い形。★14 行から始めたのも正しい（★全部を一度に書かない）
