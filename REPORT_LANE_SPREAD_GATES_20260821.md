# 報告: 横位置の広がり（`LANE_REVEAL_FULL_RUN = 0.18`）の検証

- 発行: 開発側 Claude / 2026-08-21
- 宛先: レビュー側 Claude
- 関連: `QUESTIONS_RACE_LANE_SPREAD_20260821.md` / `REVIEW_LANE_SPREAD_VERDICT_20260821.md`
- 該当コミット: `7cff0ad`（ブランチ `p4/race-30sec-cuts`）

---

## 1. 裁定どおりに実装しました

> 動かすのは **reveal** — シード由来の swing を中盤から出し、**base / SETTLE_M には触れない**

- `REVEAL_START_RUN = 0.03` / `LANE_REVEAL_FULL_RUN = 0.18` を追加
- `base` / `SETTLE_M` / `RAIL_W` は**一切変更していません**
- 値は**先に掃引して帯を確かめてから**決めました（`tools/sweep-lane-reveal.mjs`）

| 候補 | 中盤の広がり(平均) | 枠×ロス相関 | ロスのばらつき |
|---|---|---|---|
| 1.00（変更前） | 2.13m | 0.013 | 6.19 |
| 0.50 | 5.28m | 0.012 | 10.57 |
| 0.35 | 6.63m | 0.011 | 10.78 |
| **0.18（採用）** | **8.19m** | **0.011** | 10.78 |
| 0.12 以下 | 8.49m | 0.011 | 10.78 |

★ばらつきは 0.35 以下で飽和するため、0.18 は 0.35 に対して**分散の追加コストがゼロ**で広がりだけ改善します。

---

## 2. 差分（裁定が求めた pp での報告）

**同一条件**（10 ゲーム内年 × 繁殖牝馬 150 頭 × 12,000 レース × 人気推定 200 試行）で
変更前・変更後を測りました。

| 項目 | 基準 | 変更前 | 変更後 | **差** |
|---|---|---|---|---|
| V-4 1番人気の勝率 | 30〜34% | 32.24% PASS | 32.23% PASS | **−0.01pp** |
| V-5 1番人気の複勝率 | 60〜65% | 63.03% PASS | 62.96% PASS | **−0.07pp** |
| V-6 下位3ランクの平均勝率 | 0.5〜2% | 1.24% PASS | 1.24% PASS | **±0.00pp** |

**変更後の帯の余裕（★縮小条件での参考値。全量での余裕ではありません）**

| | 下端まで | 上端まで |
|---|---|---|
| V-4 | 2.23pp | 1.77pp |
| V-5 | 2.96pp | 2.04pp |
| V-6 | 0.74pp | 0.76pp |

⚠️ ★**訂正**（レビュー側指摘 2026-08-21）。
   初版はこれを「余裕」として提示していましたが、**余裕は絶対値なので条件依存**です。
   §3 に「`pool-generations` が V-4 を 0.75pp 動かすので絶対値は全量とずれる」と自分で書きながら、
   その注意を**差分にしか掛けていませんでした。** 差分は同一条件どうしなので成立しますが、
   ★**この表は参考値です。** 全量での余裕は P4 クローズ時の全量実行で確認します。

### 読み取り

裁定の警告「**危険なのは距離ロスのばらつきが増えること。V-4 が最も危ない**」について、
**その影響は検出できませんでした。**

- 距離ロスのばらつきは **+74%** 増えている
- にもかかわらず V-4 は **0.01pp** しか動いていない（この試行数の標準誤差 約 0.43pp の 1/40）

★理由は裁定の読みどおりで、散らし方が**枠に対して単調でない一様乱数**から来ているため、
特定の馬が一方的に損をする形になっていないからだと考えます。
**ばらつきが増えても、それが誰かに偏らなければ人気馬の勝率は動かない。**

---

## 3. ⚠️ この測定の限界（隠さずに書きます）

- **母集団を縮小した条件です**（全量は 40 年 × 400 頭 × 60,000 レース × 500 試行）。
  記録では `pool-generations` が V-4 を約 0.75pp 動かすので、**絶対値は全量とずれます。**
  ★報告しているのは**同一条件どうしの差分**で、こちらは成立します。
- 全量は **1 回あたり 2.5 時間以上**かかり、往復で 5 時間を超えるため、この環境では
  現実的でないと判断しました。**必要であれば回します。**

---

## 4. V-18 について（テストで留めました）

`packages/race-engine/test/lane-reveal.test.ts`

| 見ているもの | 結果 |
|---|---|
| 枠と距離ロスの相関（多数シードをプール） | 変更前 0.013 → 変更後 0.011（**悪化なし**） |
| ★却下された形（枠を 5% 残す）を注入 | **0.132** が出て**落ちる** |

★記録されている実測値 **0.127** をほぼ再現しており、**測り方が正しいことの裏づけ**になっています。

⚠️ 掃引の途中、自作の指標が誤っていました（走路全体の平均 `w` で相関を取り 0.473）。
①発走直後の枠の広がりを丸ごと拾い ②ロスが出るのはコーナーだけ、の 2 点を外していました。
エンジンの `laneExtraM` に置き換えて 0.013 になっています。

---

## 5. 🔴 未了 — 裁定を仰ぎたい点

裁定は **V-4 / V-5 / V-6 / V-17 / V-18 / V-1** を必須としました。

| ゲート | 状態 |
|---|---|
| V-4 / V-5 / V-6 | ✅ 上表のとおり（縮小条件での差分） |
| V-18 | ✅ テストで相関の非悪化を確認 |
| **V-17（走破タイムの分布）** | ✅ 下記のとおり |
| **V-1（選抜圧）** | 測定しない（裁定 2026-08-21）。P4 クローズ時の全量実行に含める |

### V-17（`npx tsx tools/verify-v17-time.mjs --races 2000`）

⚠️ ★開発側は「全量で測るか、保留か」の二択を立てましたが、**立て方が違っていました。**
   レビュー側指摘のとおり **V-17 は 1 レースごとの統計**で、世代数も繁殖牝馬数も要りません。
   縮小条件のまま**数秒**で出ました。40 年 × 400 頭は V-1 / V-2 系のための条件です。

| | 帯 | 実測 | 判定 |
|---|---|---|---|
| ① 勝ち時計 | 93〜101 秒 | **99.90 秒** | ✅ 帯内（100.0%） |
| ② 1 着と最下位の差 | 2〜4 秒 | 平均 **2.79 秒**（中央 2.68 / 1%点 1.62 / 99%点 4.50） | ✅ 平均は帯内（83.9% が帯内） |

**変更前後の差: 走破タイムで最大 0.004 秒**（`tools/_v17probe.mjs` で 1 レースずつ照合）。
統計値（平均・中央・分位点・帯内率）は**小数 2 桁まで完全に一致**しました。

★**副産物の指摘**: `timeSec = baseTimeSec + timeGapSec` で `baseTimeSec` は距離ごとの定数のため、
**勝ち時計は常に 99.902 秒**です（シード・出走馬によらず）。
つまり **V-17 ① は定数を帯と比べているだけで、変化を検出できません。**
横位置の件とは無関係の既存の性質ですが、**①はゲートとして機能していない**ので報告します。

### 判定の結論

裁定「**V-17 を縮小条件で測り、①②が帯内かつ差分が小さければ 0.18 を確定**」に照らし、
①② とも帯内・差分は 0.004 秒。→ **`LANE_REVEAL_FULL_RUN = 0.18` を確定**として扱います。

---

## 6. 併せて報告（この件の外・すべてコミット済み）

- 🔴 **実況ナレーターの実在人物写真を撤去**（影絵に差し替え・検査つき）。裁定どおり即時対応
- 演出をオーナー判定の合格カットだけで再構成（本編 34.4 秒）。**後方・俯瞰は 5 戦 5 敗**
- ★**監査道具が画面と別のものを測っていた失敗が 3 回**ありました
  （古い素材／横視点用の背景／表示時間の固定値）。
  3 回目はオーナー指摘がなければ誤報のままでした。**いずれも検査で留めています。**
