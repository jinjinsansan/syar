# REPORT_P2_A3C — 返還を本番配備／D-013 の補正は正しい／★V-10 は §9.4 と両立しません

**開発側 → レビュー側** / 2026-08-09
**コミット**: `77cf20b` 以降（push 済み・441 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**★あり。判断を2件お願いします（§4・§5）。**
どちらも**私は決めていません**。本番へも入れていません。

---

## 1. ★最優先の返還を本番に配備しました（と、その過程で私が起こした障害）

**そちらのご指摘どおり、これが最優先でした。そして重要な事実があります —
実装は `dcd2373` で済んでいましたが、★本番の VPS には載っていませんでした。**

```
配備前の本番:  149a44a  /  cancel.ts なし  /  payout.ts の refunded 参照 0件
```

**客の EP が返らない状態が本番で生きていました。** 直ちに `dcd2373` へ更新しました。

### ★配備で7分間、本番を止めました

`npm ci --omit=dev` を使い、**実行に必要な `tsx` を消しました**（devDependency です）。

```
08:38:23  停止（status=203/EXEC）
08:38:33〜08:39:14  10秒間隔で4回再起動を試行 → systemd が諦める
08:45:22  復旧（npm ci で入れ直し）
```

★**`Restart=always` でも rate limit に当たると systemd は諦めます。**
  壊れた配備は**自力で戻りません**。「再起動するから大丈夫」は成り立ちませんでした。

**周の欠落はありませんでした**（`cycle_index` 72〜144 が連番・73周・重複なし）。
サイクル番号を時刻から決める設計が効いており、7分の空白を跨いでも周が飛びません。

---

## 2. 恒等式の予測値 — 一致しました（測定していません）

ご指示どおり、**手元の MC 出力から計算**しました。

```
(1/n) Σ (1−p̂)/(M·p̂) を A-3 と同じ10シードで計算   M=4000

  予測の平均   +1.09pt
  実測の平均   +1.36pt   (SE 0.34)
  ★残差       +0.27pt = 0.8 SE   → 一致
```

そちらの粗い見積もり「+0.8〜1.0pt」とも合っています。

★**予測はシード間でほとんど動きません**（1.03〜1.14pt・SD 0.03）。
  実測の振れ（−0.03〜+3.14pt）は**すべて勝者を引く分散**でした。
  ②「全10シードが上振れ」は、やはり恒等式の帰結です。

---

## 3. 是正を実装しました。★そして「正しい」ことを別の道具で確かめました

```
odds = (1/p̂)(1−margin) / (1 + (1−p̂)/(M·p̂))     ← そちらの式
     = (1−margin) / (p̂ + (1−p̂)/M)               ← 代数的に等価
```

後者を採りました。**補正が確率の側1か所に閉じ、券種ごとに散りません。**
等価であることは `settle.test.ts` が**両式を突き合わせて**守ります。
`trials` は必須引数にしました（既定値があると、別の M で推定した確率に
誤った補正が当たっても型でも実行時でも気づけません）。

### ★補正が正しいことの確認

**真の確率が分かれば、試行数 M で何が起きるかは二項分布から厳密に書けます。**
乱数も長時間の実測も要りません（`apps/cli/src/diag-mneed.ts`）。

```
§9.4 の配当上限を外して M を上げたときの乖離（参照MC 100万回）

  券種              M=1k     M=10k    M=100k     M=1M
  win             -0.34*   -0.00*   -0.00*   +0.00*
  place           -0.02*   -0.00*   -0.00*   -0.00*
  quinella_place  -6.30    -1.02    -0.02*   -0.00*
  quinella       -13.30    -3.83    -0.22*   -0.00*
  exacta         -17.99    -6.04    -0.80*   -0.00*
  trio           -23.83   -11.30    -3.92    -0.75*
  trifecta       -39.00   -20.90    -9.43    -2.30
```

★**全7券種が 0 に収束します。D-013 の補正は正確です。**

---

## 4. ★判断① — V-10 と §9.4 は両立しません（M では解けません）

同じ計算を**上限を入れて**やると、**M を上げても消えない乖離**が残ります。

```
§9.4 の配当上限あり（本番の設定）

  券種              M=1k     M=10k    M=100k     M=1M
  quinella_place  -7.51    -9.75   -10.20   -10.18    ← M を上げるほど悪化
  quinella       -13.30    -6.11    -7.47    -7.42
  exacta         -17.99    -6.04    -7.55    -7.69
  trio           -23.83   -11.30    -9.71   -12.65    ← 同上
  trifecta       -39.00   -20.90    -9.43    -6.84
```

**上限は払戻を減らす方向にしか働きません。**M を上げると稀な目まで売れるようになり、
**その目がことごとく上限に切り詰められる**ので、**上げるほど悪くなる券種があります。**

★つまり **§9.4（配当上限）と §13.2 V-10（券種別 margin ±1%）は、
  4〜5券種について同時には満たせません。M の選び方の問題ではありません。**

**現状は「客が受け取る還元率が、設定 margin より 7〜13pt 低い」状態です。**
運営の実効控除率が §9.3 の宣言と食い違うので、**§11 の PP 発行量の前提にも効きます。**

**私は選びません。** 考えられる方向だけ挙げます:
- **(a)** 上限を「その券種で理論上ありうる最大オッズ」まで上げる
- **(b)** 上限に当たる目を**売らない**（§9.2 の「p̂=0 は売らない」と同じ扱い）
- **(c)** 上限に当たる券種は V-10 の対象外とし、正典に明記する
- **(d)** 上限を維持し、その券種の設定 margin を実効値に合わせて書き換える

---

## 5. ★判断② — 補正だけを本番に入れると、3券種が悪化します

**★これが今回いちばん重要な発見です。本番には入れていません。**

```
M=10,000（正典 §9.2 の現行値）での乖離

  券種              補正前（現行本番）   補正後      差
  win                +0.42*          -0.00*    -0.42
  place              -0.16*          -0.26*    -0.10
  quinella_place     -9.27           -9.75     -0.48
  quinella           -3.12           -6.11     -2.99
  ★exacta           +0.21*          -6.04     -6.25   ← 合格から転落
  ★trio             +2.79          -11.30    -14.09
  ★trifecta         -1.41          -20.90    -19.49
```

**補正前に馬単が合格していたのは、★2つの誤りが偶然打ち消し合っていたからです。**

```
① 凸性（1/p̂ が上に凸）      … 払戻率を上げる  ← 今回打ち消した
② c ≧ 1 の打ち切り          … 払戻率を下げる  ← 手つかず
```

②は**そちらが予告されていたもの**です。MC で1回も出ない目は売られませんが、
**売られた目は p̂ が上に偏ります**（c=0 が落ちるので）。オッズが低く付き、払戻が不足します。
目が多い券種ほど `M·p ≲ 1` の目が大量にあるので、強く効きます。

★**「差し引きが合っている」は「正しい」ではありません。**
  頭数や M が変われば崩れます。実際 M=1,000 では馬単 +2.16pt、M=100,000 では −6.99pt と、
  同じ実装のまま符号が変わります。

### 提案（採否はそちらで）

②も**同じ D-013 の形で解析的に打ち消せます**（打ち切りは `P(c≧1) = 1−(1−p)^M` で書けます）。
ただし②の補正は**稀な目のオッズを上げる**方向なので、**④の上限と正面から衝突します。**
**①だけを入れて止めるのは、いちばん悪い状態です**（打ち消しが消えて不足だけが残る）。

→ **④と⑤は一緒に決めていただく必要があります。**
   ご指示があれば②の補正を実装し、同じ計算で確認してから配備します。

---

## 6. 券種別に測ったこと（C はご指摘どおり最初からそう測るべきものでした）

`verify-payout.ts` は元から券種別・プール判定です。実測も取りました。

```
races=8000 seed=42（補正後）
  券種              M=250   M=1000   M=4000
  win              -5.98    +1.26    +1.00
  place            -0.47    -0.01    +0.03
  trifecta        -56.64   -43.34   -30.58
```

★ただし **races=8000・1シードでは SE が約 2pt** あり、**±1pt の判定には粗すぎます。**
  §4/§5 の判定には**標本誤差の無い解析計算**を使いました。向きは実測と一致しています。

---

## 7. ★自分の道具に R-21 の穴がありました（自己申告）

最初の計算で「cap ありでも M=1M なら三連単 −0.00pt」と出ました。**誤りです。**

参照 MC が 10万回だったので、**`p < 1/10万` の目が存在しないことになっていました。**
それは M=100万 で「売られて上限に切り詰められる」目そのものです。
参照を100万回に上げると **−6.84pt** でした。

→ `目標 M > 参照 MC` を**例外で止める**前提条件を入れました。
  「測定条件が成立しない結果を報告しない」を道具側で強制します。

---

## 8. A-1（判定は本日 20:53 以降）

```
生成した周 78 / 確定した周 74 / 周の欠落なし（cycle_index 連番）
メモリ 62MB / FD 26 / DB接続 1 / systemd 再起動回数 0（配備の1回を除く）
```

★§7 のご質問への回答: **「61〜70秒」はワーカーのポーリング間隔です**（`TICK_MS=60_000`）。
  開催サイクルは10分（`CYCLE_MS=600_000`）で、**別の周期です。** 用語を分けて書きます。
  ご指摘のとおり、揺れても周は飛びません（§1 の7分停止が実証しました）。

---

## 9. 合格基準

| # | 状態 |
|---|---|
| A-1 | **計測中**（本日 20:53 判定・リーク系は確認済み） |
| A-2 | **PASS** |
| **A-3** | **★判断待ち**（win/place は補正後 PASS。他5券種は §4・§5 の決定が要る） |
| A-4〜A-9 | **PASS**（441 tests / typecheck 0） |
