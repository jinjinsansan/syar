# 斜め前の切り分け・レビュー裁定（2026-09-10）

対象: `REPORT_P4_FRONT_ISOLATE_20260910.md`、コミット `39fdb3a`。
レビュー: Codex 単体。撮影の前提として直前の `3e2926b` の関連差分も確認した。

**裁定: 観察資料は受け入れる。原因を除外した結論は差し戻し。裁定 §4-3 の切り分けは一部完了であり、素材の復帰／描き直しを決める段階ではない。**

## 1. 確認できたこと

- `39fdb3a` 自体は報告書と撮影・比較ツールのみの変更で、製品描画・素材を変更していない。直前の `3e2926b` には製品変更があるため、比較元を混同しない。
- 前回問題だった毎コマの上下追従・倍率更新は、走路上の接地点への追従とクリップごとの固定倍率へ修正された。bob の素材別設定と描画矩形の毎回リセットも関連差分で確認した。
- 提出 JSON の D/A/B/E は、いずれも 14.0〜16.5 秒・51 枚・20fps。各条件で画像の SHA-256 は 51 種あり、対象馬 3 番の矩形は各フレームに一つだけ存在する。
- 素材、配置、組別 bob、つまみ倍率、stride の診断は表の条件と整合する。B は組別 bob=0.3 × つまみ0で実効0。E は旧 front の組別 bob=1。
- A/B と A/E は、全51フレーム・全馬の投影座標、向き、コマ番号、位相が JSON 上で一致する。A/B は bob の差を見る対照として有効。

独立再計算:

| 条件 | 元画面の接地点から描画矩形上端までの変動 | 報告の正規化式での変動 | 3番の完歩/表示秒 |
|---|---:|---:|---:|
| D | 56.7203px | 70.3670px | 2.37157 |
| A | 23.1737px | 29.1460px | 2.96446 |
| B | 30.4802px | 38.3355px | 2.96446 |
| E | 42.6428px | 54.4025px | 2.96446 |

報告値は再現できた。この測定は描画矩形上端の範囲であり、支持脚、騎手の相対姿勢、接地時間、蹄の滑りを直接測ったものではない。

## 2. F-I1: bob を原因から除外できない（P2）

対象: 報告48行、66行。

「bob を0にすると上端の変動が増える」は観測に合う。しかし、そこから「ぴょこぴょこはつまみでは直らない」「浮きの量は潰れた」は導けない。

上端の振幅の小ささと走って見えることは同じ評価軸ではなく、報告自身もその点を指摘している。B はまだオーナー未見で、A/B 以外の値も未評価。0への変更が上端を安定させなかったことと、bob 調整が見え方に効かないことは分ける必要がある。

訂正文案: **「このカットでは bob 0 は bob 0.3 より上端の変動が大きかった。走りの評価に対する効果と、適切な bob の値は未確定」**。

## 3. F-I2: 未確認の評価と交絡から原因を除外している（P2）

対象: 報告53〜72行、87行。

§2 は今回の E を「オーナーが良いと言う」条件として使う一方、§4 は今回の E が同条件でも良いかを確認待ちとしている。過去の旧2Dへの評価は参考情報として明記し、今回の E への確定評価へ置き換えないこと。

また A/E は素材・配置・実効 bob・周囲の型構成が異なる。同じ stride で評価が違っても、現在の素材に適する stride が5.6mかどうかは決まらない。今回、A/E の数値上の位相は一致したが、異なる素材の同じ番号が同じ支持局面を表すかは確認されていない。

「拡縮を直しただけでは苦情全体が解消していない」は言えるが、「拡縮は原因ではなかった」には戻さないこと。既に確認した改善要因である。

方向は未確定としながら「残った候補は素材のコマの中身」と絞るのも早い。脚・関節・騎手の運動は調査候補として残すが、方向・位相・設定との相互作用も未確定として残す。

## 4. 次便で必要な最小限の補完

1. 本報告の「潰れた」を、解消済みの不具合／今回観測したこと／未確定の仮説に書き直す。新しい映像へのオーナー評と過去の評を区別する。
2. 歩調を除外するなら、現素材・現配置・bob・カメラを固定し、stride だけを変えた対照を追加する。D/A は3要因同時変更なので、その代わりにはならない。
3. 方向と位相の切り分けは未完了と明記する。まず同じ素材一周の連番に、前後脚の支持／離地と騎手の鞍に対する位置を記入する。自動計測器を新設しなくても、判断できる範囲の目視注釈と不明点を提出できる。異なる素材の同番号を同じ運動局面と仮定しない。
4. 主観評価には等速映像を先に使い、0.25倍は姿勢や接地の確認に使う。現在の4条件映像をオーナーに見てもらうこと自体は差し支えないが、原因の確定や素材変更の二択への回答と扱わない。

## 5. 計測表記の小さな補正（非ブロッキング）

`compare-gait-crops.mjs:114` の scale は `520/(refH*2)`、実際の resize は `520/round(refH*2)` で、わずかに異なる。後者で計算すると D/A/B/E は70.3927 / 29.1775 / 38.3770 / 54.3487px。今回の大小関係は変わらないが、「切り出し後の実測」ではなく診断座標からの計算値と記すか、実際の倍率に統一すること。

また倍率は条件ごとに約1.241 / 1.258 / 1.258 / 1.276と異なる。「同じ倍率」は「各クリップ内では固定、条件間は中央値の描画高さを揃える正規化」に訂正する。A/B の倍率は同じ。

## 6. 検証記録

- `tmp/review-front-isolate.mjs` → `tmp/review-front-isolate.json`: 設定・フレーム更新・対象矩形・時刻・位相一致・報告値を独立検査。
- 撮影／比較ツールの `node --check`: 成功。
- horse-ground / gait-cadence / race-placement-wiring の3ファイルを再実行し、**40件成功**（`tmp/review-front-isolate-tests.log`）。コミット本文の「3ファイル44件」とは一致しないため、開発側は対象名とログを添えて件数を訂正または説明すること。
- 今回は全体テスト・型チェックを再実行していない。比較行の画像を確認したが、動画全編の自然さの合格判定はしていない。
- 製品・素材変更なし。レビュー文書と tmp の検証資料だけを追加。開始・終了 HEAD は `39fdb3a`。commit / push / 配備なし。
