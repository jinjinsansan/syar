# Q-AUTH-01〜05 の裁定 — 認証を LINE ログインへ

**発行**: 2026-08-20（レビュー側 Claude）
**対象**: `QUESTIONS_AUTH_LINE_20260820.md` / `bde825f`
**判定**: **Q-AUTH-01 承認（条件付き・ただし論拠を差し替える）／ Q-AUTH-02 承認／ Q-AUTH-03 V-19 を新設／ Q-AUTH-04 却下（前提が事実と違う）／ Q-AUTH-05 承認（設計は変更）**

---

## 0. ★先に、前提の誤りが1件あります — しかも私が書いたものです

> **Q-AUTH-04**: 「D-042 は staging を P3 の着手条件として挙げ、まだ揃っていません」
> **私の昨日の裁定 §4-⑤**: 「オーナー手配待ちのままです」「今回また止まりました」

**[CODE] staging は存在します。8月11日から。そして8月12日に実際に動いています。**

| 証跡 | 内容 |
|---|---|
| `secrets.staging.env`（2026-08-11 00:34） | `STAR_ENV=staging` ／ 本番とも local とも**別の Supabase プロジェクト**（`pezntijqkobbzqatiuxd`。local は `hfvvxwoulrjqznperici`） |
| `REPORT_P3_DEPLOY_20260812.md:70` | **`[worker] 起動 env=staging` がログに出た**（`--schemacheck` の副作用を開発側が発見した経緯そのもの） |
| 同 `:130` | 本番の実測値を報告する文で「**staging と同じです**」— **本番と staging を突合する運用が既に成立している** |
| `db/migrations/0001_init.sql:19-24` | `app_environment` に `staging` が定義済み。`apps/worker/src/env.ts:57-63` が自己申告と DB の照合を実装（§14.6 の要求どおり） |

**★そして、その誤りは文書の内部矛盾として現れていました。** 開発側の照会 §7 と私の裁定 §4-⑤ は、**同じ表の隣り合う2行**で

```
1 | staging の用意（D-042）— まだ揃っていません
2 | staging の DB パスワード・service_role キーのローテーション（未実施）
```

と書いています。**存在しないプロジェクトの鍵は、ローテーションできません。** 片方が偽です。

**これは丈夫さ sd の 65/66 を表の内部整合から逆算したのと同じ形**で、**9日間、両側とも気づかずに繰り返していました**（D-042 は 08-10・staging 実在は 08-11・以後 08-12 と 08-20 に再掲）。

→ **R-25 を新設します**（§6）。**「オーナー手配事項」は、手配が文書の外で完了するため、文書が自動で更新されない。次に参照するとき実物の存在を確認してからでなければ再掲しない。**

### 開発側の自己訂正2件は、こちらでも当たりました

| | 判定 |
|---|---|
| アプリ化は Capacitor でなく **Expo**（§15.4） | **[CODE] 正典どおり。** §15.4 は Expo 推奨、理由も「プッシュ通知が使える」「**発走通知が §8b の介入の前提条件**」と明記されています |
| ストア審査の模擬ギャンブル・景品交換は **§15.5 が既に扱っている** | **[CODE] そのとおり。** §15.5 は "simulated gambling" 該当・景品交換ゆえの踏み込んだ審査・レーティング質問票への正直な申告まで書いています |
| **§15.6「認証を Supabase Auth に統一」** | **[CODE] 実在。** 理由は「モバイルの Deep Link 対応が楽になる」。Q-AUTH-02 の設計案はこの条文を満たします |

**自分の誤りを2件自己申告したうえで、正典の条文を自分に不利な側（§15.6＝自前 JWT を封じる）まで引いてきたのは適切です。**

---

## 1. 🔴 Q-AUTH-01 — **承認します。ただし採用の論拠を差し替えます**

### 結論

**LINE ログインへの変更を承認します**（オーナー決定・D-076）。マジックリンク裁定の3つの理由が失われないことは、こちらでも確認しました。**憲法との衝突はありません** — §0.2 の5原則・§0.3 の S-1〜S-5 のどれにも触れません（認証方式は参加ポイントの取得経路ではない）。

### ★ただし「入口で複垢を抑える」を経済の防御にしてはいけません

開発側が書いた採用理由はこうです。

> D-075 でデイリーログイン 200 EP・日次上限 10,000 EP を置いた以上、複垢による EP 乱獲が V-11 を直接押し上げる経路になる。**入口で複垢を抑えるほうが、後段で検知するより構造的に強い**

**前半は正しく、後半は採れません。** 理由は開発側が自分で申告した事実の中にあります。

- **iOS では LINE のみにできない**（Q-AUTH-01 の自己申告）→ **別経路が必ず1本開く**
- LINE も複垢コストを**上げる**が**ゼロにはしない**（アカウント削除→再作成で `sub` は変わる。番号の再取得もできる）

つまり **「入口が効いている」前提で D-075 の額を決めると、iOS 版を出した瞬間に前提が黙って崩れます。** これは R-16 の「機構は存在するが働いていない」の近縁で、**より悪い形**です — 出荷時点では本当に働いており、**後から別経路が開いたときに、どのゲートも鳴らずに崩れる**。

### → 入口は「摩擦」、担保は「出口」に置きます（D-077）

**★複垢の利得は、最終的に景品でしか実現しません。** したがって:

| 層 | 役割 | 強度の根拠 |
|---|---|---|
| **入口**（LINE ログイン） | **摩擦を上げる。** 気軽な複垢を減らす | **経済の安全をここに依存させない** |
| **上限** | 被害の総量を止める | **★user 単位の日次上限（D-075）に加えて、EP 日次総発行量の上限を置く** — 複垢は user 単位の上限を乗算で回避するが、総量は回避できない。D-075 の「バグ・エクスプロイトの被害を上限で止める」の自然な延長 |
| **観測** | 重複の兆候を測る | 同一デバイス・同一 IP・**同一の景品送付先**の重複を**観測する**（遮断しない。誤検知で正当な利用者を切るほうが損害が大きい） |
| **★出口**（景品交換） | **担保。ここが最終防衛線** | **景品交換の時点で一意性を担保する。** §11 は未執筆（D2 オーナー思案待ち）なので、**この要求を §11 の前提として先に書き込む** |

**LINE を採るべきという結論は変わりません。摩擦は上げたほうが良い。** 変えるのは**それに何を負わせるか**です。

### iOS の Apple ログイン併設について — **解は Apple 一択とは限りません**

開発側の申告「iOS 版は LINE のみにできない」は**方向として正しい**が、正確には Apple のガイドラインは「サードパーティ／ソーシャルログインを**専一に**使うアプリは、**同等の条件を満たす別のログイン手段**も提供すること」を求めています。その条件は**取得情報を氏名とメールに限る・メールを隠せる・追跡しない**であり、**Sign in with Apple はそれを満たす一例**にすぎません。

→ **追跡せずメールしか取らないメール系ログイン（＝まさに元のマジックリンク）でも、要件の文面上は満たしえます。**

**⚠️ [要確認] これは実装時点のガイドライン原文で確かめてください。** 私の知識は2026年5月までで、実務では Apple ログインを求められる例が多いという事情もあります。**アプリ化（§15.4）の着手時に、ガイドライン §4.8 の原文を引用して §15.5 に追記すること**（追記の許可は出します。ただし「Apple ログインが必要」ではなく「**専一利用が禁じられており、同等要件を満たす第2の手段が要る**」と正確に書くこと）。

**★そして、どちらの解を採っても「弱い経路が1本増える」ことは避けられません。** だから前節の入口／出口の分離が要るのです。

### ⚠️ 開発側も私も挙げていなかった不利な点 — 3件

| # | |
|---|---|
| **A** | **単一障害点になります。** LINE Login はチャネル審査と利用規約があり、**馬券要素＋景品交換が問題視されて拒否されれば、認証が丸ごと死にます**。マジックリンクは第三者の許可が要りませんでした。→ **実装前に LINE 側の規約・審査要件を確認すること**（弁護士ゲートではなく**プラットフォーム規約の確認**。§17.7 に項目を足す）。併せて Q-AUTH-05 の設計を「**後から別プロバイダを足せる形**」にします（＝緩和策。Apple 併設でも同じ設計を使う） |
| **B** | **アカウントを失うと口座を失います。** LINE アカウントを削除・喪失すると `sub` が変わり、**同じ利用者が戻れません**。**PP は景品価値を持つので、これは現実の損害です。** 一方で**復旧経路そのものが乗っ取り経路になります**（サポート手動復旧は最も狙われる）。→ **未決事項として残し、オーナー判断を仰ぎます**（§7）。**復旧経路を作るなら、その手続き自体を V-19 の対象に含めます** |
| **C** | **PC の摩擦は、主張ほど低いとは限りません。** スマホの LINE アプリ間連携はワンタップですが、**PC ブラウザの LINE ログインは QR 読み取りかメール＋パスワード**です。オーナーの「PC で登録 → 外出先はアプリ」は成立しますが、**登録時の往復はマジックリンクより増える可能性があります**。→ **[EYES] オーナーが実機で `/login` の往復を確認してください**（PC・スマホの両方） |

### 併せて — 同一チャネル／同一プロバイダーの制約を明記します

開発側の「同一チャネルに Web とネイティブを紐づければ同じアカウント」は方向として正しいですが、**LINE のユーザー ID はプロバイダー単位で共通で、チャネルを別プロバイダーに作ると別 ID になります**。

→ **Web とアプリのチャネルは同一プロバイダー配下に置くこと**を制約として正典に書きます。**staging 用のチャネルは callback URL が違うので別チャネルが必要**ですが、**同一プロバイダー配下に置けば `sub` の意味が環境間で一貫します**（[要確認] 実装時に LINE の公式ドキュメントで裏取りすること）。

---

## 2. 🔴 Q-AUTH-02 — **承認します。(a) 自前実装で進めてください**

**設計案（LINE で本人確認 → `sub` で特定 → セッションは Supabase Auth に発行させる）を採用します。**

**この形の価値は、開発側が書いたとおり「変わるのは誰であるかを確かめる方法だけ」という点にあります。** Q-WEB-04 ②〜④ の裁定（セットアップ RPC の1トランザクション・`potential` を行に含めない・`place_bet` の再利用）は**すべてそのまま生きます**。§15.6 も満たします。

### 制約として明文化します（D-076 に含める）

1. **★自前で JWT を発行しない。セッションは Supabase Auth に持たせる**（§15.6 の具体化）
2. **リフレッシュトークンの寿命管理を自前で抱えない**
3. **`auth.uid()` / RLS / 書き込み RPC は一切変更しない**（変更が要ると判明した時点で、それは設計案から外れている合図）

### 置き場所: **Supabase Edge Function**

| | |
|---|---|
| **なぜ Next.js の API Route でないか** | §15.3 の「Supabase RPC への API 集約」・§15.6 の方針から外れ、**アプリ化時に Web への依存が残ります**。Expo からも同じ入口を叩ける形にしてください |
| **なぜワーカーでないか** | ワーカーは10分サイクルの本体です。**利用者のログインを、レース確定と同じプロセスの可用性に結びつけない**（片方の障害が他方を巻き込みます） |
| **条件** | Edge Function は環境ごとに別デプロイになります。**LINE のチャネル・callback URL・シークレットを §14.6 の環境分離に載せること**（本番の callback に staging のチャネルを向けない） |

---

## 3. 🔴 Q-AUTH-03 — **V-19 を新設します。提案を採用し、6項目を足します**

**「認証にゲートが無い」という指摘が、この照会で最も重要です。** そして開発側の見立て

> 4つとも「壊れていても正常にログインできてしまう」＝ 通常の動作確認では発覚しません（R-16 の型）

は**正確**です。**PP は景品に換わるので、ここの瑕疵はゲーム内の不具合ではありません。**

### なぜ A 系（受け入れ基準）ではなく V-19 か

**A 系は一度通せば閉じるもの、V 系は回り続けるものです。** 認証は**ライブラリ更新・チャネル変更・プロバイダ追加（Apple 併設）のたびに壊れうる**ので、**回り続けるゲートでなければなりません。** → **V-19 として新設**（P4〜、以後恒久）。

### V-19 の内容 — 開発側の6項目を採用し、以下を追加します

**採用（そのまま）**: 署名改竄 → 拒否 ／ `aud` 相違 → 拒否 ／ 期限切れ → 拒否 ／ `state` 不一致 → 拒否 ／ 同一 `sub` で2回 → ユーザーは1つ ／ メール一致・`sub` 相違 → 別ユーザー

**★追加6件**:

| # | 項目 | なぜ |
|---|---|---|
| **7** | **`nonce` 不一致 → 拒否** | 開発側の4形態に**抜けています**。`state` は CSRF、`nonce` はリプレイで**別の攻撃**です。傍受された `id_token` の再送を止めるのは `nonce` のほう |
| **8** | **`iss` 相違 → 拒否** | 本文の表 #3 に `iss` が挙がっているのに、提案されたゲート項目には `aud` だけが残っています（**書いた本人が落としている**） |
| **9** | **★「LINE の検証を通らずにセッションが発行できないこと」** | **ここが最大の穴です。** Edge Function は service_role で**任意のユーザーのセッションを作れます**。そのエンドポイントの認可が緩ければ、**トークン検証がすべて正しくても全口座が抜けます**。「検証を通らない入力でセッションが返らないこと」を**直接**叩いてください |
| **10** | **`sub` の一意制約が DB 側にあること** | アプリ側の「あるか確認して無ければ作る」は**同時アクセスで二重に通ります**。#5（同一 `sub` で1ユーザー）を**DB の制約で**担保すること |
| **11** | **★本番経路で固定すること（純関数だけで固定しない）** | **この案件で3度踏んだ罠です**（I-3 / I-4 / L-1）。`verifyIdToken()` の単体テストが全部通っても、**呼び出し側が検証を飛ばす形に書き換えて全緑になるなら、防御はゼロ**です。**検証を飛ばした呼び出し側で落ちること**を実験して示してください |
| **12** | **R-14 に従い、検出器が自分自身を検出していないこと** | 「`state` を検証する」テストが `state` 検証の実装を参照して組み立てられていると同語反復になります。Q-3 で2件見つかった形 |

**★ゲートは「壊して落ちること」で書いてください**（R-14）。**「正しいトークンで入れる」ことを確かめるテストは、V-19 には数えません** — それは機能の確認であって防御の確認ではありません。

---

## 4. 🔴 Q-AUTH-04 — **却下します。staging はあります**

**§0 のとおりです。** `secrets.staging.env`（08-11）＋ `[worker] 起動 env=staging`（08-12）＋「staging と同じです」（本番突合）で、**破壊試験の器は既に存在します。**

**したがって「staging が無いから着手しない」は成立しません。** ただし**着手条件は別に2つあります**。

| # | 条件 | 理由 |
|---|---|---|
| **1** | **★露出済みクレデンシャルのローテーション（先に済ませる）** | **service_role キーが漏れている環境で「セッション発行の安全性」を試験しても、意味がありません**（守るべきものが既に開いている）。**認証を扱う今回に限り、これは順序の問題です** |
| **2** | **staging 用の LINE チャネル**（callback URL が本番と別・同一プロバイダー配下） | これが無いと OAuth 往復そのものが試せません |

### ★そして、着手を丸ごと止める必要はありません — 分割できます

**Q-AUTH-03 の検証は、全部が破壊試験ではありません。**

| 範囲 | 環境 | 先行可否 |
|---|---|---|
| **`id_token` の検証ロジック**（署名・`aud`・`iss`・`exp`・`nonce`）＋ V-19 の #1〜#3・#7・#8・#11・#12 | **ローカル**（JWKS を差し替え、偽署名トークンを自作して叩く） | **★先行できます。むしろ先にやるべきです** — ここは純粋な入力→判定で、外部の往復が要りません |
| **OAuth 往復・`state`/`nonce` の経路・セッション発行の認可**（#4・#9） | **staging 必須** | 上記2条件が揃ってから |
| `sub` の一意制約・二重作成（#5・#6・#10） | ローカル DB（Supabase CLI）で可 | 先行できます |

→ **「staging が揃うまで認証に着手しない」は却下し、「①ローカルで固められる範囲を先に固める → ②鍵のローテーションと LINE チャネルが揃ったら往復系を staging で叩く」に改めます。** 開発側の慎重さは正しいですが、**止める範囲が広すぎます。**

---

## 5. 🟡 Q-AUTH-05 — **承認。ただし `users` に LINE 専用列は作りません**

### 設計を変更します（D-078）

**`users.line_sub` を足す形は採りません。** 理由は**Apple 併設が来ると既に分かっている**ことです。専用列にすると、そのとき**同じ形の改修をもう一度やります**。

```sql
create table user_identities (
  provider text not null,               -- 'line' | 'apple' | ...
  subject  text not null,               -- プロバイダ側の sub。★メールではない
  user_id  uuid not null references users(id) on delete cascade,
  created_at timestamptz default now(),
  primary key (provider, subject)       -- ★これが Q-AUTH-03 #10 の担保
);
create unique index on user_identities(user_id, provider);   -- 1人が同一プロバイダで2つ持たない
```

1. **`sub` の置き場所**: 上記の別テーブル。**`users` は増やしません**（§4.1 の `users` は口座であって認証の器ではない）
2. **一意制約**: **そのとおり、DB 側に張ります**（`primary key (provider, subject)`）。アプリ側の確認だけにしないこと
3. **LINE の表示名・アイコンは取り込まない — 承認します。** 理由も開発側のとおりです（D-074 の入力と二重・実在の人物名が場内に出る経路）。**補強**: §17 のプライバシー観点に加え、**取り込まなければ「氏名を取得していない」と言い切れる**ので、§1 で触れた Apple §4.8 の「取得情報を氏名とメールに限る」の議論も楽になります。**LINE から取るのは `sub` のみ**を制約として書いてください

---

## 6. 正典の改訂（提案）

| 番号 | 内容 | 反映先 |
|---|---|---|
| **D-076** | **認証を LINE ログインに変更する（Q-WEB-04 ① を変更）。自前実装で進め、★自前で JWT を発行せずセッションは Supabase Auth に発行させる。実装は Supabase Edge Function に置く。Web とアプリのチャネルは同一プロバイダー配下。LINE から取得するのは `sub` のみ** | §15.6 / §14.6 / §4.1 |
| **D-077** | **★複垢抑止を経済の防御に使わない。入口は摩擦・担保は出口。** LINE は複垢コストを上げるが、iOS の第2経路とアカウント再作成で必ず抜けが残る。→ ①D-075 の user 単位上限に加え **EP 日次総発行量の上限**を置く ②同一デバイス／IP／**景品送付先**の重複を**観測**する（遮断しない）③**★景品交換（§11）の時点で一意性を担保する** — 複垢の利得は出口でしか実現しないため。**「入口が効いている前提」で額を決めると、iOS 版で前提が黙って崩れる**（R-16 の近縁） | §3.2 / §11 / §13.2 |
| **D-078** | **外部 ID は `user_identities(provider, subject)` に持つ。`users` に専用列を作らない。** Apple 併設が既に見えているため、専用列は同じ改修を二度呼ぶ。`primary key (provider, subject)` が「同一 `sub` で1ユーザー」の DB 側担保 | §4.1 |
| **V-19（新設）** | **認証が壊れたときに落ちること。** 12項目（§3）。**「正しいトークンで入れる」は数えない。壊して落ちることだけを数える。★本番経路で固定する（純関数のみの固定は防御ゼロ）** | §13.2 |
| **R-25（新設）** | **「オーナー手配事項」は、手配が文書の外で完了するため文書が自動で更新されない。次に参照するとき、実物の存在を確認してからでなければ再掲しない。** 実例: D-042 の staging は 08-11 に実在し 08-12 に稼働したのに、08-20 まで両側が「手配待ち」と書き続けた。**同じ表の隣の行が「その環境の鍵をローテーションせよ」と書いており、内部矛盾していた** | §16.3 |
| §15.5 追記 | ストア審査: **サードパーティのソーシャルログインの専一利用が禁じられており、同等要件（氏名とメールのみ・メール隠蔽可・追跡なし）を満たす第2の手段が要る。★Apple ログイン一択ではない。** [要確認] 実装時にガイドライン原文を引用して確定 | §15.5 |
| §17.7 追記 | **プラットフォーム規約の確認**（弁護士ゲートとは別）: LINE Login のチャネル審査・利用規約で馬券要素＋景品交換が問題にならないか。**拒否されれば認証が丸ごと死ぬ単一障害点** | §17.7 |

---

## 7. オーナーへ

### 決定をお願いしたいこと

| # | |
|---|---|
| **1** | **★LINE アカウントを失った利用者をどう扱うか。** `sub` が変われば別人になり、**PP（景品価値）ごと戻れません**。復旧経路を作れば、そこが乗っ取り経路になります。→ **(a) 復旧なし・規約に明記** ／ **(b) サポート手動復旧を作る（★手続き自体を V-19 の対象にする）** ／ (c) その他 |
| **2** | **EP 日次総発行量の上限（D-077）を置くこと**の承認。額は較正定数として後で決めます |

### [EYES] 実機で確かめていただきたいこと

- **PC ブラウザでの LINE ログインの往復**（QR 読み取りかメール＋パスワードになります）。**マジックリンクより楽だと感じるか** — 採用理由の2（摩擦が低い）が PC でも成立するかの判定です
- スマホでの LINE アプリ間連携（ここはワンタップになる見込み）

### 🔒 セキュリティ — 現在地を確定させました（[CODE]）

**朗報が1つ、宿題が2つあります。**

| | |
|---|---|
| **✅ 平文クレデンシャルは GitHub に漏れていません** | `git.txt` ／ `secrets.local.env` ／ `secrets.staging.env` ／ `壊す用のsupabse情報.txt` の**4件すべてが未追跡かつ .gitignore 済み**。**ローカルに存在する全 ref を走査して履歴にも一度も入っていません**。remote は `jinjinsansan/syar`。→ **引継ぎ書の「`syar` にコミット済みか」は「されていない」で閉じます**（念のため GitHub 側のシークレットスキャン結果も一度見ていただければ完全です） |
| **🔴 宿題1: ローテーション未実施** | 8/10 に露出した **staging の service_role キー**。`壊す用のsupabse情報.txt` には旧・新の DB パスワードが併記されており **DB パスワードは一度更新された形跡がありますが、service_role キーの更新形跡はありません**。**Q-AUTH-04 の着手条件1です** |
| **🔴 宿題2: 平文ファイルが4つ** | ローカルとはいえ、**service_role キー（RLS 全バイパス）が平文で作業ディレクトリにあります**。ローテーション後は `.env` 系に寄せ、`git.txt` と `壊す用のsupabse情報.txt` は削除をご検討ください |

---

## 8. 付録 — [CODE] 検証の記録

| 主張 | 検証方法 | 結果 |
|---|---|---|
| §15.4 は Expo 推奨・理由はプッシュ通知 | `STAR_SPEC_v2.0.md` §15.4 全文 | **一致** |
| §15.5 に Apple 併設要件の記述が無い | §15.5 全文（5項目の対策まで） | **無い＝追記要求は妥当** |
| §15.6 に「認証を Supabase Auth に統一」 | §15.6 の表 | **実在**（理由: Deep Link） |
| 憲法 §0.2 ／ §0.3 の S-1〜S-5 に触れるか | 両節全文と突合 | **触れない** |
| D-075 の額・「較正定数であり V-11 を満たすように調整」 | 決定ログ D-075 | **一致**（2,000 ／ 200 ／ 10,000） |
| V-11 の定義（両側・D-051） | §13.2 の V-11 行 | **一致**。①ゼロ近傍〜微減 ②PP 発行と EP 消費がともに実質ゼロでないこと |
| §4.1 `users` に外部 ID 列が無い | §4.1 の DDL | **無い**（`id` は `auth.users(id)` 参照） |
| 認証は未配線（デモ） | `apps/web/src` を `auth.uid` / `signInWith` / `createServerClient` / `@supabase/ssr` / `getUser()` で grep | **0件＝申告どおり未配線**。`@supabase/supabase-js` のみ依存・`/setup` `/login` `/signup` の `page.tsx` は実在 |
| staging の実在 | `secrets.staging.env`（`STAR_ENV=staging`・別プロジェクト）／`REPORT_P3_DEPLOY_20260812.md:70,130`／`0001_init.sql:19-24`／`apps/worker/src/env.ts:57-63` | **★実在。開発側の前提と私の前回裁定が誤り** |
| 平文クレデンシャルの git 追跡 | `git ls-files --error-unmatch` ×4 ／ `git check-ignore` ×4 ／ `git log --all --name-only` | **4件すべて untracked・ignored・履歴なし** |
