# 照会（認証方式を LINE ログインに変更したい）— 2026-08-20

**宛先**: レビュー側
**発行**: 開発側（オーナー方針の確認を受けて）
**関連**: 正典 §4（users）/ §14.6 / §15.3 / §15.4 / §15.6 / 憲法 §0.2 / D-042 / D-075
**既裁定との衝突**: **Q-WEB-04 ①（`REVIEW_WEB_ONBOARDING_VERDICT_20260820.md`）で「メールのマジックリンク」と裁定済み。本照会はその変更を求めるものです**
**未裁定**: Q-AUTH-01 〜 Q-AUTH-05

---

## 0. 何を変えたいか（1 行）

**認証を「メールのマジックリンク」から「LINE ログイン」に変え、かつ自前実装で進めたい。**

Q-WEB-04 ① の裁定を受けて `/setup`（初回セットアップ）は既に実装済みですが、**認証の接続はまだ入っていません**（デモ）。**接続前の今なら画面を書き直さずに方式を変えられます**。

---

## 1. 🔴 Q-AUTH-01 — 認証方式を LINE ログインにしてよいか

### オーナーの理由

**LINE は不正行為が少ない。** LINE アカウントは電話番号に紐づくため、**複数アカウントの作成コストが高い**。

これは経済に直接効きます。**D-075 でデイリーログイン 200 EP・日次上限 10,000 EP を置いた**以上、**複垢による EP 乱獲が V-11（ポイント純発行量）を直接押し上げる**経路になります。マジックリンクは捨てアドレスでいくらでも増やせるため、**入口で複垢を抑えるほうが、後段で検知するより構造的に強い**というのがオーナーの判断です。

### マジックリンク裁定時の3つの理由との突合

裁定文が挙げた理由と、LINE に変えた場合の当てはまりです。

| # | マジックリンクの理由 | LINE ログインでどうなるか |
|---|---|---|
| 1 | パスワードの保管・再発行の経路を持たない | **同じく持たない**（OAuth/OIDC なので当方はパスワードを扱わない）。理由は維持される |
| 2 | 外部リンクから来る利用者の摩擦が低い（ポイ活の導線と噛み合う） | **LINE のほうが低い可能性が高い**。国内のポイ活層は LINE を常用しており、メールを開いてリンクを踏むより往復が少ない。**スマホでは LINE アプリ間連携でワンタップ** |
| 3 | §14 の RLS ＋ RPC 構成と素直に繋がる | **条件付きで維持される** — セッションを Supabase Auth に発行させる形（Q-AUTH-02）なら `auth.uid()` はそのまま効き、RLS・RPC は一切変わらない |

→ **開発側の見立てでは、3つの理由はいずれも LINE ログインで失われません。** そのうえで複垢抑止という利点が乗ります。

### ★正典との整合（開発側で確認済み）

- **§15.6「認証を Supabase Auth に統一」** — 後述の実装方式（セッションは Supabase Auth が発行）ならこの要求を満たします
- **§15.4 は Expo (React Native) を推奨** — LINE は iOS/Android 向けにネイティブ SDK を提供しており、**同一チャネルに Web とネイティブを紐づければ同じアカウント**になります。「PC で登録 → 外出先はアプリ」というオーナーの想定は方式変更後も成立します

### ⚠️ 開発側から先に申告する不利な点

**iOS のストア審査で、サードパーティのソーシャルログインを使うアプリには Sign in with Apple（相当の選択肢）の併設が求められます。** つまり **iOS 版は「LINE のみ」にできません**。

- **アプリ化（§15.4）の時点で Apple ログインの実装が追加で必要**になります
- **その瞬間、複垢抑止の強度は Apple 経路の分だけ下がります**（Apple ID はメール隠蔽機能があり、LINE ほど電話番号に強く紐づきません）
- §15.5 が既に「ストア審査で必ず引っかかる点」を扱っていますが、**この併設要件は §15.5 に記述がありません**。追記の要否も併せて裁定をお願いします

**裁定をお願いしたいこと**
1. 認証方式を LINE ログインに変更してよいか（Q-WEB-04 ① の変更）
2. iOS の Apple ログイン併設を前提に置いてよいか。置く場合、**Apple 経路の複垢抑止が弱いことをどう扱うか**（日次上限で吸収する／Apple は iOS のみに限定する／その他）
3. §15.5 に併設要件を追記してよいか

---

## 2. 🔴 Q-AUTH-02 — 自前実装で進めてよいか（Supabase に LINE の標準プロバイダが無い）

**Supabase Auth に LINE の標準プロバイダはありません。** 選択肢は3つで、**オーナーは (a) 自前実装を選んでいます**。

| | 方式 | 月額 | 主なリスク |
|---|---|---|---|
| **(a)** | **Edge Function で LINE OIDC を処理し、Supabase のユーザーに接続**（オーナー選択） | **0 円** | 認証実装の瑕疵が**そのまま口座の乗っ取り**になる（後述 Q-AUTH-03） |
| (b) | LINE を標準サポートするマネージド IdP を前段に置き、Supabase の Third-Party Auth で接続 | MAU 課金（無料枠あり） | 外部依存・コストがユーザー数に比例 |
| (c) | 認証はマジックリンクのまま、LINE は後から紐づけ | 0 円 | **入口で複垢を抑える狙いが達成されない** |

### 開発側の設計案（★ここが要点）

**自前実装で最も危険なのはセッション管理**です。自前で JWT を発行するとリフレッシュトークンの寿命管理を抱え込み、§15.6「認証を Supabase Auth に統一」からも外れます。

→ **そこを回避する形を提案します**:

```
LINE で本人確認（id_token の検証）
  → LINE の sub でユーザーを特定
  → ★セッションは Supabase Auth に発行させる（自前で JWT を作らない）
  → auth.uid() / RLS / RPC は一切変更なし
```

**この形なら、変わるのは「誰であるかを確かめる方法」だけで、「確かめたあとの仕組み」は今の設計のままです。** Q-WEB-04 ②〜④ の裁定（セットアップ RPC が users 行＋初期 EP＋初期馬を1トランザクション、`potential` を行に含めない、`place_bet` の再利用）は**すべてそのまま生きます**。

**裁定をお願いしたいこと**
1. (a) 自前実装で進めてよいか
2. **「自前で JWT を発行しない／セッションは Supabase Auth に持たせる」を制約として明記してよいか**（§15.6 の具体化）
3. 実装の置き場所（Supabase Edge Function か、ワーカー側か）

---

## 3. 🔴 Q-AUTH-03 — 認証にゲートが無い。何で守るかを決めてほしい

**この案件はゲーム機構を V-1〜V-18 で縛ってきましたが、認証にはゲートがありません。** そして R-16 が繰り返し示したとおり、**この案件で事故が起きたのは決まって「機構は存在するが働いていない」形**でした。

**認証は同型の事故が最も起きやすい場所です。** しかも:

> **PP は景品に換わります。アカウント乗っ取りは、ゲーム内の不具合ではなく現実の損害です。**

### 開発側が把握している事故の形（4つ）

| # | 瑕疵 | 起きること |
|---|---|---|
| **1** | `id_token` の**署名を検証しない** | **誰でも任意のユーザーとしてログインできる**。認証実装の最も古典的な穴 |
| **2** | **メールアドレスで突合する** | LINE のメールは**任意項目かつ未検証**。★**必ず LINE の `sub`（ユーザーID）で突合する**。ここを誤ると乗っ取り経路になる |
| **3** | `aud` / `iss` / 有効期限を検証しない | 別チャネル向けのトークンや期限切れトークンが通る |
| **4** | コールバックで `state` を検証しない | CSRF でセッションを固定される |

**いずれも「知っていれば防げる」ものですが、1つ落とすだけで台帳が抜かれます。** そして **4つとも「壊れていても正常にログインできてしまう」＝ 通常の動作確認では発覚しません**（R-16 の型）。

**裁定をお願いしたいこと**
1. 認証に**ゲート（V-19 相当）を新設するか**、A 系の合格基準として置くか
2. 置く場合の内容。開発側の提案は**「壊して落ちること」を直接確かめる形**です:
   - 署名を改竄した `id_token` → **拒否されること**
   - `aud` を別チャネルにした `id_token` → 拒否
   - 期限切れ `id_token` → 拒否
   - `state` 不一致のコールバック → 拒否
   - **同一 LINE `sub` で 2 回ログイン → ユーザーが 1 つだけであること**（重複作成しない）
   - **メールだけ一致し `sub` が違う → 別ユーザーになること**（★2 の検出）

---

## 4. 🔴 Q-AUTH-04 — staging が無いと、この検証ができません（D-042・3 回目）

**Q-AUTH-03 の検証はすべて「壊して叩く」ものです。本番に向けられません。**

- D-042 は staging を **P3 の着手条件**として挙げ、まだ揃っていません
- `REVIEW_WEB_ONBOARDING_VERDICT_20260820.md` §4 ⑤ で「**今回また止まりました**」と再掲されています
- **本照会で 3 回目です。** そして今回は対象が**認証**なので、**破壊試験なしで本番に載せることは、開発側としては提案できません**

**開発側の立場**: **staging が用意されるまで、認証の実装には着手しません。** 設計と照会までを進め、着手条件が揃った時点で実装します。

**裁定をお願いしたいこと**: この順序（staging → 認証実装）でよいか。もし先行させる範囲があるなら、その線引き。

---

## 5. 🟡 Q-AUTH-05 — `users` に LINE の識別子をどう持つか

§4 の `users` は `display_name` / `stable_name` を持ちますが、外部 ID の列がありません。

**裁定をお願いしたいこと**
1. LINE の `sub` をどこに持つか（`users` に列を足すか、Supabase Auth の identities に任せるか）
2. **`sub` に一意制約を張る**という理解でよいか（Q-AUTH-03 の「重複作成しない」を DB 側でも担保する）
3. **LINE の表示名・アイコンを取り込むか** — 開発側の提案は**取り込まない**です。理由: 初回セットアップで表示名・牧場名・勝負服を入力させる設計（D-074）と二重になり、**実在の人物名が表示名に入る経路**にもなります（憲法 §0.1 は実在の競走馬・レース・競馬場を対象としていますが、利用者の実名表示は別途 §17 のプライバシー観点で確認が要ると考えます）

---

## 6. 影響範囲（開発側の見立て）

**方式が変わっても書き直しにならないことを確認済みです。**

| 対象 | 影響 |
|---|---|
| `/setup`（初回セットアップ・実装済み） | **変更なし**。認証後に来る画面で、入力項目（表示名・牧場名・勝負服）は D-074 のまま |
| `/stable` ほか Web 12 画面 | **変更なし**（`StableRepo` の差し替えのみ） |
| セットアップ RPC（Q-WEB-04 ②） | **変更なし**。users 行＋初期 EP＋初期馬を 1 トランザクション |
| RLS・`auth.uid()`・書き込み RPC（Q-WEB-04 ④） | **変更なし**（Q-AUTH-02 の設計案を採る場合） |
| `/login`・`/signup`（準備中ページ） | LINE のボタンに差し替え |

---

## 7. オーナーへ（レビュー側からの再掲）

| # | |
|---|---|
| 1 | **staging の用意（D-042）— 本照会で 3 回目。認証の破壊試験に必須で、着手条件です** |
| 2 | staging の DB パスワード・service_role キーのローテーション（未実施） |
