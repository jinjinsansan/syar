# 【STAR】報告: 🔴 EP の日次付与を実装 ／ 網を 1 本 足した ／ 赤かった門を 1 つ直した — 2026-09-25

- 開発側 Claude（`star-aa`）／ 対象の裁定: `REVIEW_EP_INFLOW_AND_ENTRY_20260925.md` §1・§4・§5・§6
- 開始 HEAD: `131dd02` ／ 終了時も `131dd02`（★**コミットしていません**）
- 検証: `npm test` ★3,011 通過 / 3 落ち（★**3 件すべて既存の `KNOWN_RED`**・★新しい赤は 0）／ `npm run typecheck` ★0
- 本番への適用: ★**していません**（★移行 `0080` は staging のみ）

---

## 1. 🔴 EP の日次付与（★裁定 §1・公開を止める欠陥）— ★実装し、staging で 18 件 確かめました

### 実装したもの: 移行 `0080_daily_ep.sql`

| # | もの | 何をするか |
|---|---|---|
| ① | `ep_grant_amount(kind)` | ★**額を SQL 側の 1 か所に**（`signup` 2,000 / `daily` 200 / `daily_cap` 10,000） |
| ② | `ep_reason_class(reason)` | ★**理由ごとの発行/焼却/馬券/返金を 1 か所に**（★裁定 §5 (b)） |
| ③ | `claim_daily_ep()` | ★押した本人に 1 日 1 回 渡す。★**引数なし** |
| ④ | `create_account` の再定義 | ★直書き 2,000 を ① へ寄せた（★最新の定義 `0067` から写した） |
| ⑤ | `my_daily_ep_state()` | ★押す前に「いくら・押せるか」を言うための読む口 |

★画面: `apps/web/src/app/earn/page.tsx`（★「毎日のログイン」を ★**本物のボタン**に）・`apps/web/src/lib/daily-ep.ts`（新規）
★TS の定数: `packages/betting/src/ep-grants.ts`（新規・`EP_GRANTS`）。★`setup.ts` の直書き 2 個は ★**写しを捨てて読むだけ**にしました

### 条件 1〜7 への対応

| 条件 | 対応 | 確かめ方 |
|---|---|---|
| 1 サーバーの時計 | ✅ ★ただし ★**`now()` ではなく `world_state.day_started_at`** | ★理由は下の「⚠️ 危うく 3 つめの『1 日』を作るところでした」 |
| 2 1 日 1 回 | ✅ `dedupe_key = 'daily:<利用者>:<世界の日>'` | ★実測 ① 台帳 1 行・残高 200 のまま |
| 3 額は較正定数から | ✅ ★SQL に数は ① だけ | ★`ep-grant-sql.test.ts`（★TS を 201 に変えたら落ちることを確認） |
| 4 日次上限 10,000 | ✅ ★判定は ② の表で「発行」だけ数える | ★実測 ③⑦ `horse_sale` 10,000 で拒む・⑥ `refund` 10,000 では拒まない |
| 5 `point_flow_daily` に載る | ✅ | ★実測 ⑧ `ep_inflow = 200` |
| 6 押した人だけ | ✅ `assert_setup_complete()` の返す `auth.uid()` のみ | ★ワーカー側に配る処理は作っていません |
| 7 試験 4 本 | ✅ ★**8 本**（★下に対照 4 本を足した理由） | `tools/verify-ep-daily.mjs --env staging` |

```
  ✅ ⑤ 対照: 1 回めに 200 EP 渡っている（実測 200）        ← ★①「1 行」は 0 行でも通るので対照を置いた
  ✅ ① 2 回めは 0 EP／台帳 1 行のまま／残高 増えていない
  ✅ ④ 台帳の balance_after (200) ＝ 残高 (200)
  ✅ ⑧ point_flow_daily.ep_inflow に載っている（実測 200）
  ✅ ② 翌日は また 200 EP／台帳 2 行／残高 400
  ✅ ⑥ 対照: 積んだ refund が その日の窓に入っている（実測 10000）
  ✅ ⑥ refund を 10,000 積んでも拒まない（★delta>0 を全部数える実装なら落ちる）
  ✅ ③⑦ 上限に当たると拒む（★horse_sale を発行と数えない実装なら通ってしまう）
  ✅ ③ 拒んだとき 残高が動いていない
  ✅ ★分類表に無い語で止まる
  ✅ ★戻りました（取引 47512 のまま・★途中の commit なし）
```

### ⚠️ ★危うく 3 つめの「1 日」を作るところでした（★条件 1 を `now()::date` で書きかけた）

★`0050_day_boundary_single_source.sql` が ★**「SQL に `date_trunc` も `current_date` も書かない。
★ワーカーが毎周書く `world_state.day_started_at` を読む」**と定めていました（★**BT-6 ②⑤**）。
★理由は ★どちらも ★**セッションの TimeZone** にしたがい、★接続ごとに別の時刻で切れるからです。
→ ★`bet_allowance`（`0050`）と ★同じ形で行を読みます。★行が無ければ通しません。
→ ★網も足しました（`claim_daily_ep` と `my_daily_ep_state` が `date_trunc`/`current_date` を含んだら落ちる）。

### 🔴 ★その副作用（★設計の判断をお願いします）

★鍵が「世界の日」なので、★**ワーカーが止まると `day_started_at` が進まず、誰もデイリーを受け取れません。**
- ★向きは ★**安全側**です（★発行しすぎない）。★ワーカーが止まればレースも調教も止まります
- ★ただし画面には「今日のぶんは受け取り済み」と ★**出続けます**（★止まっているとは言いません）
- ✔ 実測: ★staging の `day_started_at` は ★**2026-09-24T00:00Z のまま**（★実時刻 09-25）＝ ★ワーカーが止まっています
- → ★このままでよいか、★「ワーカーが N 時間 書いていないなら画面でそう言う」を足すか、★照会に回します

---

## 2. 🔴 ついでに見つけた ★**現に間違っている集計**（★裁定 §5 (b) の指摘が現物で当たっていました）

★`apps/worker/src/daily-flow.ts:93` は ★`reason === 'inflow'` だけを発行として数えていました。
★`horse_sale`（`0026:126`）は ★**相手方の引き落としが無い正の delta ＝ 新しい EP** なのに、
★`bet`/`refund` 以外なので ★`epBurnedOther += -v`（★**負の焼却**）に落ちていました。
→ ★**発行量が過小に・`margin_actual` が過大に**出ます（★V-11 の監視が甘くなる方向）。

- ✔ ★**本番の `horse_sale` は 0 件**なので、★数字の被害はまだ出ていません（★式は誤り）
- ✅ 直し: ★問い合わせ自体が `ep_reason_class(l.reason)` を読む形にしました（★TS 側に表を持ち直さない）
- ✅ 網: ★`ep_ledger` の**最新の制約に並ぶ 9 語を、分類表が全部 知っているか**を突き合わせます
  → ★10 個めの理由を足した日に ★**足し忘れたら落ちます**（★`horse_sale` を抜いて落ちることを確認済み）

---

## 3. 🔴 ★**2026-09-19 から赤かった門を 1 つ直しました**（`tools/verify-flow.mjs`）

★`aggregateDay` は ★**BT-6（09-19）**で `date` を `date` ＋ `from`/`to` に分けました。
★`verify-flow.mjs` の呼び出しは ★**引数 2 つのまま**でした。
→ ★`from`/`to` が `undefined` で渡り、★SQL は `created_at >= null` になって ★**1 行も当たらず**、
  ★全項目 0 ＝「★実データを数えている: **FAIL**」を出し続けていました。
⚠️ ★`.mjs` なので ★**型検査が鳴きません**（★引数が減っても静かに通る）。

- ✔ ★私の変更のせいでないことは ★**HEAD の版に戻して同じ 0 が出る**ことで確かめました
- ✅ 直したあと: ★EP 流入 100,000 / 焼却 2,000 / PP 発行 30,000 / 交換 5,000 ★全項目 PASS
- ★境目は ★`world_state` の行ではなく ★`dayStartMs`（`@star/scheduler`）から作りました
  （★行を読むと ★止まった環境で凍った日が返り、★また 0 になるため。★`daily-flow.ts` 自身が「正は `dayStartMs`」と書いています）

---

## 4. ✅ 裁定 §6 — ★**「機械で捕まえられない」は私の誤りでした**

★レビュー側の案（「進む語を持つ要素は `button` か `onClick` を持つ」）を ★実装したら ★**通りました**。
`apps/cli/test/action-words-are-pressable.test.ts`

- ★最初に緩く（★語を**含む**）作ったら ★**誤検知 14 件**（★地の文「確定した走破タイム」・★見出し「最初の 1 頭を生産する」）
- ★**語が表示文字の全部のときだけ**に締めたら ★現状 0 件で通り、★当時の実物を噛みました
- ★検体を ★**ソースに埋めました**（★手で壊す検査は残らない）。★4 つを同時に固定します:
  - ★当時の `<span ... title="サーバー接続まで押せません">登録する（{race.feeEP} EP）</span>` → ★**噛む**
  - ★直した `<button onClick={...}>登録する（…）</button>` → ★噛まない
  - ★地の文「出馬表は閲覧できます。投票するにはログインしてください。」 → ★噛まない
  - ★見出し「最初の 1 頭を生産する」 → ★噛まない

---

## 5. 裁定 §4 — ★今日 見つけた「例外も赤も出ないずれ」7 件に、機械で捕まえる形が在るか

★**1 行ずつ書きます。★無いものは「無い」と書きます。**

| # | ずれ | 機械で捕まえる形 |
|---|---|---|
| 1 | 受け付ける段が DB と画面でずれた（`announced` vs `scheduled`） | ✅ **在る** `entry-stage-matches-rpc.test.ts`（★最新の `enter_race` の定義から段を取り出して画面の定数と突き合わせる） |
| 2 | 「登録する」が `<span>` で `onClick` が無い | ✅ **在る（今日 作った）** `action-words-are-pressable.test.ts`。★§4 のとおり |
| 3 | 同じ直書きが 2 か所にあり、直したのは画面が使っていない方 | ⚠️ **半分だけ在る**。★`ENTERABLE_RACE_STATUS` を 1 か所にしたので ★**この段については**再発しない。★一般形（「同じ値が 2 か所」）の網は ★**無い** |
| 4 | ログインしていない人に DB の生の文が出た | ⚠️ **無い**。★`SignInRequiredError` に寄せたが、★「生の `error.message` を画面に出していないか」を見る網は ★**無い**（★次に作れる形: `catch` の中で `error.message` を `setState` している箇所を挙げる） |
| 5 | 公開のレース一覧が `my_horses` の失敗に道連れで消えた | ⚠️ **無い**。★「公開の読みと本人の読みを 1 つの `Promise.all` に入れていないか」は ★機械で言えそうだが ★まだ試していない |
| 6 | EP の日次付与の実装が無い（額だけ在った） | ⚠️ **無い**。★「正典が額を決めた項目に、渡す側の実装が在るか」は ★**正典の表と実装の対応表が要る**。★いまは無い。★近い形として `calibration-registry.test.ts` が「定数が登録簿に在るか」は見ているが、★**使われているか**は見ていない |
| 7 | `daily-flow.ts` が `horse_sale` を発行と数えていない | ✅ **在る（今日 作った）** ★最新の制約の 9 語を分類表が全部 知っているか。★§2 のとおり |

★**在る 3 / 半分 1 / 無い 3** です。★6 が最も重い（★D-119 の族そのもの）ので、
★「正典で額や数を決めた項目 ↔ 実装」の対応表を作るかは ★指示をください（★作るなら正典側の作業になります）。

---

## 6. ★既知の赤 3 件（★新しい赤は 0）

| 検査 | 状態 |
|---|---|
| `breed-year-scale.test.ts` 🔴 ★ワーカーと同じ年の渡し方 | ★`KNOWN_RED` 簿の 1 件目。★私は配合を 1 行も触っていません |
| `edit-grammar-audit.test.ts` ⑭ ★撮影が画面より古くない | ★`KNOWN_RED`。★撮り直しは ★**オーナーの端末で**（★開発側から流すと人の画面にブラウザの窓が開く） |
| `existing-shot-gate.test.ts` ⑫ ★測定が画面より古くない | ★同上 |

---

## 7. ★お願い・判断待ち

1. 🔴 ★**移行 `0080` を本番に当てるか**（★当てないと本番の EP は 0 のままで、★何もできません）
   ★当てる前に ★レビュー側へ 1 行 通知します（★裁定 §5 ③）
2. ★本番のオーナーの口座の EP を ★**いま手で戻すか**、★`0080` を当ててから ★画面で受け取るか
   → ★**後者を推めます**（★手で書くと「受け取りが動いた」証拠にならない）
3. ★§1 の副作用（★ワーカーが止まるとデイリーが止まる）を ★このままにするか
4. ★上の 2 つの撮り直し道具を ★オーナーの端末で流すか（★`npm test` の赤 2 件が消えます）

★押していない・当てていないもの: ★`git push`・★本番 DB への書き込み・★ワーカーの入れ替え

---

# 追記（同日・レビュー側 §7 ①②③ への対応）

- 検証: `npm test` ★**3,028 件・赤 4 件（すべて登録簿どおり）**／ `npm run typecheck` ★0（★`tools` も見るようにした）
- `node tools/verify-known-red.mjs` ★**合格**（★登録簿に無い赤なし・緑に戻った登録なし）
- 移行: `0081_day_boundary_staleness.sql`（★staging のみ）

## ① ✅ 止まっているのを「受け取り済み」と見せない

- ★線は ★**SQL に 1 か所**（`day_boundary_stale_after_hours()` ＝ **28 時間**）。★画面と監視が ★同じ関数を読みます
- ★根拠: ★正常時 `day_started_at` は 0〜24 時間前（`DAY_MS`）→ ★24 時間超は 1 回 進みそこねている。
  ★ワーカーは 6 分ごとに書くので境目直後は 24 時間＋数分に届く → ★余裕 4 時間で **28**
- ★画面: ★`distributionStalled` を ★**「受け取り済み」より先に**見ます（★順番を網で固定）
- ★監視（`check-weekly-cycle-health.mjs`）: ⚠️ ★以前は ★**秒数を出すだけで線がありませんでした**。★合否を言うようにしました
- ✔ ★実測で鳴きました（★staging はワーカーが止まっている）:
  `② 🔴 ★配布が止まっています（1 日の境目が 30 時間前・線 28 時間）`

## ② ✅ `.mjs` に型検査を当てた（★新しい網は作っていません）

- `tools/tsconfig.json`（★`allowJs: true` ＋ ★**`checkJs: false`**）＋ ★`// @ts-check` を ★**13 本**に
- ✔ 🔴 ★**対照で効くことを確かめました**。★当時の壊れた呼び方に戻すと:
  `tools/verify-flow.mjs(141,7): error TS2554: Expected 4 arguments, but got 2.`
- ★一度に付けない理由: ★`tools` の `.mjs` は **443 本**で、★全部に付けると ★**498 件 / 73 ファイル**落ちます
  （★`_` 付きの使い捨ての下見道具が多く、★製品の正しさと関係ない落ち方が混ざる）
- ★`npm run typecheck` が ★`tools` も見るようにしました（★設定だけ在って誰も流さない形にしない）
- ★件数の釘: `apps/cli/test/tools-typecheck-coverage.test.ts`（★下限 13・★`verify-flow.mjs` は名指しで必須）

## ③ 🔴 対応表を作ったら、★**その場で 1 件 見つかりました**

`apps/cli/src/canon-amounts.ts`（★14 行）＋ `apps/cli/test/canon-amounts-implemented.test.ts`

★表は 3 列（★決定 / 額（言葉で） / ★その額を動かす実装）。★`implementation` が `null` なら落ちます。
★表が嘘をつけないように、★**SQL なら移行に関数が在るか・TS ならファイルに名前が在るか**を見ます。
★引いている正典の決定が ★実在するかも見ます。

### 🔴 見つかったもの: **D-105「騎手の料金は EP」が、誰からも 1 EP も引かれていない**

- ★`enter_race` は `coalesce((p_jockey_frozen ->> 'feeEP')::int, 0)` で料金を取ります
- 🔴 ★しかし画面（`apps/web/src/app/entry/page.tsx:117`）が送るのは ★**`{ id: jockeyId }`** だけです
- ★`freezeJockey`（`@star/scheduler`）が作る形は `{ v, jockeyId, name, feeEP, bond, effect, calm }`
  → ★**キーも違い（`id` ≠ `jockeyId`）、`feeEP` が入っていません**
- ✔ ★返金側（`entry-freeze.ts:174`・`entry-scratch-runner.ts:104`）も `?? 0` を読むので ★**帳簿は合っています**
  （★実害は「★騎手が無料」だけ。★EP の過不足は起きていません）
- 🔴 ★さらに ★**料金の出どころがクライアントの JSON** で、★サーバー側に騎手の名簿の表がありません。
  ★このまま繋ぐと ★利用者が `feeEP: 0` を送って ★**高い騎手を無料で乗せられます**（★憲法 3）
- ★正しい形は ★**種付料と同じ**です。★`spend_stud_fee_ep`（★ワーカー専用）は
  ★サーバーが `npcStudFee` の式で額を決め、★利用者が送る `max_fee_ep` は ★**断る理由にしか使いません**
  （★`player-breeding.ts:303` の註記自身が「★利用者の申告で額が下がる経路は無い・憲法 3」と書いています）
- → ★`KNOWN_RED` に ★**`owner: 'owner'`** で登録しました。★**名簿をどこに置くか（SQL の表にするか）は判断待ち**です
  ⚠️ ★表から D-105 の行を消して緑にはしません（★それがまさに D-119 の形）

## ★次にやること（★指示待ちを除く）

- 裁定 §3 の ★旧世代の画面 10 枚（★1 枚ずつ 捨てる／作り直す／新版へ送る を決めて記録）
  ✔ ★下調べ済み: ★`page.tsx` 52 枚のうち ★**新世代（`components/uma`）は 15 枚**・★残り 37 枚は旧

---

# 追記 2（同日・裁定 `REVIEW_JOCKEY_FEE_20260925.md` への対応）

- 移行: `0082_jockey_roster_server_side.sql`（★staging のみ）
- 検証: `npm test` ★**3,036 件・赤 3 件（すべて登録簿どおり）**／ `npm run typecheck` ★0
- `node tools/verify-known-red.mjs` ★**合格**（★D-105 の登録は ★直したので外しました）
- ★指示どおり ★**EP の日次付与を本番に出す前に**直しました（★同じ便）

## 直したもの（★裁定の①〜④ そのまま）

| ★裁定 | 実装 |
|---|---|
| ① ★名簿を DB に転記（正は TS） | `jockeys` 表に ★**6 人を転記**。★一致は `jockey-roster-sql.test.ts` が突き合わせます |
| ② ★`enter_race` は `jockeyId` だけ受け取り、料金を表から引く | ✅。★署名を `p_jockey_id text` に |
| ③ ★`jockey_frozen` もサーバーで組み立てる | `jockey_frozen_build(jockeyId, horseId)`。★`bond` は ★**乗せた回数から**サーバーが数えます |
| ④ ★引き落としは同じ取引の中 | ✅（★`0051` と同じ 1 関数の中。★変えていません） |

### 🔴 ★危ない署名を落としました

★`create or replace` で引数の型を変えると ★**多重定義**になり、★`jsonb` を受ける版が ★**呼べるまま残ります**。
→ ★`drop function if exists public.enter_race(uuid, uuid, text, jsonb, uuid);` を明示。
✔ ★staging で確かめました: `function enter_race(unknown, unknown, unknown, jsonb, unknown) does not exist`

### ★本文を機械で突き合わせました（★判定を壊していないこと）

★`0051` と `0082` の `enter_race` を ★正規化して差分を取りました。★**差は 4 行だけ**です:

```
-p_race_id uuid, p_horse_id uuid, p_strategy text, p_jockey_frozen jsonb, p_client_token uuid
+p_race_id uuid, p_horse_id uuid, p_strategy text, p_jockey_id text, p_client_token uuid
+v_jockey_frozen jsonb;
-v_jockey_fee := coalesce((p_jockey_frozen ->> 'feeep')::int, 0);
+v_jockey_frozen := jockey_frozen_build(p_jockey_id, p_horse_id);
+v_jockey_fee := coalesce((v_jockey_frozen ->> 'feeep')::int, 0);
-values (… p_strategy, p_jockey_frozen, p_client_token)
+values (… p_strategy, v_jockey_frozen, p_client_token)
```

★所有・引退・段・締切・資格・2 頭まで・登録料・残高・枠・斤量 は ★**1 文字も動いていません**。
⚠️ ★最初に自分で書いた行ごとの比較は ★**1 行 足した時点で全部ずれ**、★79 件の差に見えました。
   ★`git diff --no-index` で取り直しました（★比べ方を間違えると、正しい変更が大事故に見えます）。

## ★`?? 0` をやめました（★裁定の指示）

★旧は 2 か所が `Number(row.jockey_frozen?.feeEP ?? 0)` でした。
🔴 ★これは「★黙って 0」で、★**料金が 1 円も引かれていないことが帳簿に現れない**原因でした
（★引くのも 0・返すのも 0 なので ★帳簿は合ってしまう）。
→ ★`jockeyFeeOfFrozen()` を ★**`scratch.ts` に 1 か所**置き、★両方が呼びます（★片方だけ戻せない）。
- ★`null`（指名しない）は 0 で正しい
- ★**凍結が在るのに `feeEP` が無ければ落とします**
- ✔ ★staging 実測: ★`race_entries` **569 件 すべて `jockey_frozen = null`**（★旧い形の行は **0 件**）
  → ★落とす形にしても既存の取消を壊しません。★診断の道具: `tools/diag-jockey-frozen-shapes.mjs`
- ⚠️ 🔴 ★**本番でも同じ内訳を確かめてから配備してください**（★本番は私から読めません）

## staging 実測（`tools/verify-jockey-fee.mjs --env staging`）

```
  試す騎手: j-himura（名簿の料金 400 EP）
  ✅ ① 対照: 指名しないと登録料 200 だけ引かれる（実測 200）
  ✅ ① 指名すると登録料 200 ＋ 騎手 400 ＝ 600 引かれる（実測 600）   ← ★以前は 200 だけ
  ✅ ② 凍結 {"v":1,"bond":0,"calm":0.6,"name":"桧村 たくみ","feeEP":400,"effect":0,"jockeyId":"j-himura"}
  ✅ ③ jsonb を受ける古い署名が呼べない（★多重定義で残っていない）
  ✅ ④ 知らない騎手 id で落ちる（★「無料の騎手」にならない）
  ✅ ⑤ 2 回めの登録で親密度が 1 になる（★利用者は申告していない）
  ✅ ★戻りました（取引 47525 のまま・★途中の commit なし）
```

★取消・返金の既存の道具も流し直しました（★返金側を触ったので）:
- `verify-ds5-before-start-scratch` ✅（★横取り 4 件・発走後に引退した馬には触れない）
- `verify-entry-scratch-live` ✅（★14 件 通過 / 0 件 失敗）

## ★本番に出す順（★裁定の 🔴 を受けて）

★`0080`（EP の日次付与）・`0081`（止まっている表示）・`0082`（騎手の料金）は ★**同じ便で出します**。
★`0082` を先に入れずに `0080` だけ出すと、★EP が入り始めたその日から
★「高い騎手を無料で乗せ放題」になります（★裁定の指摘どおり）。
⚠️ ★本番への適用は ★**オーナーの明示の指示を待ちます**。★適用前に ★レビュー側へ 1 行 通知します（★裁定 §5 ③）。


---

# 追記 3（同日・§4 の「無い」を 1 件 減らしました）

## ★§4 の表の ⑤ が「在る」になりました

★前の報告で、★「例外も赤も出ないずれ」7 件のうち ★3 件を ★**「無い」**と書きました。
★そのうち ⑤ ★「公開の読みと本人の読みを 1 つの `Promise.all` に入れていないか」を
★**作ってみたら 言えました**（`apps/cli/test/public-read-not-hostage.test.ts`）。

★形: ★`lib/` を走査して ★**本人スコープの関数**（★`SignInRequiredError` を投げる）と
★**公開の読み**（★`_public` の view を読む）を集め、★`Promise.all([...])` の中に
★**両方が裸で**並んでいたら落とします（★本人の側に `.catch` が在れば通します）。

✔ ★**対照で確かめました**（★実物を壊して）:
```
apps/web/src/app/stable/retired/page.tsx: 本人 [loadRetiredScreen] ＋ 公開 [loadPublicRetired]
```
★`.catch` を付けた形は通ります。★当時の形（`/entry`）を検体としてソースに埋めてあります。

⚠️ 🔴 ★**作る途中で誤検知を 1 つ出しました**。★`loadRetiredScreen` が
★**本人と公開の両方の一覧に入り**、★「本人 [A] ＋ 公開 [A]」という無意味な指摘が出ました。
★本文の切り出しが粗く、★隣の関数の `_public` を拾っていました。
→ ★**`SignInRequiredError` を投げるなら安全な公開の読みではない**ので、
  ★分類として ★**本人スコープを優先**させました（★意味の上でも正しい）。

★これで §4 は ★**在る 4 / 半分 1 / 無い 2** です（★残りは ④ 生の `error.message` と
★⑥ 正典の額 ↔ 実装。★⑥ は `canon-amounts` で作ったので、★実質 ④ だけです）。

## ★D-123 ③ の残り 2 件（★戻せない操作に「押す前の確認」）

🔴 ★レビュー側の指摘: ★`window.confirm` は ★`entry`・`exchange`・`vote` の 3 か所だけで、
★**1 回きりで戻せない 2 つ**に ★在りませんでした:

| 画面 | なぜ戻せないか | 足した文面 |
|---|---|---|
| `/setup` | ★牧場をつくるのは 1 回きり（★表示名・牧場名・勝負服を画面から変える口が無い） | 「★あとから変えられません」 |
| `/stable/foal` | ★**無償の生産は 1 回だけ**（★父と母を選び直せない・案 A・D-120） | 「★この組み合わせは選び直せません。無償の生産は 1 回だけです」 |

★文面は ★`/vote` の形を写しました（★何が決まるか → 空行 → 戻せないこと → 問い）。
⚠️ ★`window.confirm` は ★**既存の部品だけ**で済ませる形で、★デザイナー便で差し替える前提です
（★そのとき ★**文面を持っていく**・★註記に書きました）。

★網: `apps/cli/test/irreversible-confirms.test.ts`（★4 件）。
★① 確認が在る ★② ★**呼び出しより前**に在る ★③ 文面が「戻せない」と言っている。
✔ ★対照: ★`/stable/foal` の確認を消すと 2 本が落ちます。

⚠️ 🔴 ★**この網でも同じ罠を踏みました**。★`indexOf` が ★`/setup` の**見出しの註記**に在る
★`supabaseSetupRepo.create(...)`（★26 行目・説明文）に当たり、
★「確認が呼び出しより後に在る」と ★**嘘の判定**をしました。
→ ★註記を空白に置き換えてから位置を見る形に直しました。
★**同じ形を同じ日に 2 回**です（★もう 1 つは 0088 の対照）。
★**自分の変更を註記に残すほど、同じ文字列がファイルに 2 回現れます。**
