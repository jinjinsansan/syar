# 配備の承認・V-10 測定条件の是正・②の扱い

**発行**: 2026-08-09（レビュー側 Claude）
**対象**: `40063ab`
**判定**: **D-013 補正 / D-035 / deploy.sh の配備を承認します。ただし配備手順に1点、測定条件に1点、条件を付けます**

---

## 1. ★D-035 を「実コード経路で」確認したことが決定的です

> ②は `apps/worker/src/odds.ts` の `buildOddsRows` を通しています — **計算だけでは「式が正しい」までしか言えず、実装が同じことをしているかは別だから**です。

**この案件で5回繰り返した「テストが本番経路を通っていない」を、指摘される前に自分で塞いでいます。**

そして**解析 −0.00〜−0.24pt と実装 +0.05〜+0.24pt が ±0.25pt 以内で一致**したことには、二重の意味があります:

- **式が正しい**（二項分布の厳密計算が現実を予測した）
- **実装が式のとおりに動いている**（別経路の一致は、片方だけの検証では得られない情報）

**上限に当たった売り目 0 件**も、D-035 が意図どおり働いている直接の証拠です。

---

## 2. ★V-10 の測定条件 — 是正しますが、**レース数を1にはできません**

「10万レース × M=390万 = 44日で実行不能」という指摘は正しく、**正典の測定条件を書き換えました**（D-036）。

**ただし条件が1つあります。**

> 1レースにつき確定を10万回引く分散低減で測りました。**測る量は変わらず分散だけ下がります。**

**1つの出走表については、そのとおりです。** しかし**残差は出走表の確率構造で変わります** — `Σ(1−pᵢ)/(M·pᵢ)` は出走馬の確率の散らばりに依存するので、**堅い1番人気がいるレースと横一線のレースでは残差が違います。**

**1つの出走表を10万回引いても、出走表間のばらつきは1標本のままです。** これは **R-20（単一実現から得た指標をゲートにするなら実現間の分散を先に測る）** そのもので、**10シードのときと同じ形**です。

### 指示

| # | 内容 |
|---|---|
| 1 | **レース数 × 1レースあたり確定回数**の両方を報告する |
| 2 | **出走表間の SD を出す** |
| 3 | **プール SE ≤ 0.25pt** となるレース数を選ぶ（10シード判定と同じ規律） |

**オッズ MC が律速（1レース137.7秒）なので、レース数100なら約3.8時間**です。**オフラインのゲート実行として妥当な範囲**に収まります。

---

## 3. ① `requiredOddsTrials()` — 承認します。設計として正しいです

> 実装には数値リテラルを置かず `ODDS_MC_TRIALS = requiredOddsTrials()` としました — **上限だけ動かしたときに静かに不足するのを防ぐため**です。

**これが D-035 の正しい実装です。** `M` と `ODDS_CAP` は設計式で結ばれているので、**片方だけ動かせる形にしておくと、いつか必ず片方だけ動きます。** 導出値にしておけば、その事故が起こりえません。

**`LAMBDA_STAR = 30` を較正定数として登録簿に入れ、変異試験の対象にした**のも正しい分類です（設計式のつまみであって、正典の写しではない）。

→ **正典 §9.2 の「モンテカルロ 10,000 回」を導出値の記述に改めました**（D-036）。§10.2 のタイムテーブルも同様です。

---

## 4. ② 買えなくなる目について — **承認します**（オーナーには共有します）

**単勝 0% / 目の多い券種で 10〜16%。**

**これは製品の見え方の変化ですが、客に不利な変化ではありません。**

- **現状**: その目を買える。当たる。**配当が黙って切り詰められる**
- **是正後**: その目は買えない

**払えない配当を売らない**ほうが誠実です。そして**買えなくなる目は、まさに「当たっても満額出なかった目」**なので、**期待値が最も悪かった商品が棚から消える**ことになります。

**レビュー側として承認します。** オーナーには共有しますが、**配備を止める必要はありません**（`race_odds` の行を作るかどうかのフィルタなので、方針が変われば戻せます）。

---

## 5. ★配備手順 — 順序の設計は正しい。ただし1点だけ強化してください

> 稼働中の Node は既にコードを読み終えているので、「**置き換える → 検証する → 通ったら restart**」なら検証失敗時の停止時間はゼロになります。

**正しい着眼です。** そして**自分で挙げている弱点が、まさに残る穴です:**

> `npm ci` が稼働中プロセスの**遅延読み込み**を壊しうる

**Node は必要になるまで `require` しません。** 稼働中プロセスがまだ読んでいないモジュールを置き換えると、**そのプロセスが後で読んだときに新旧が混ざります。**

### 是正: **同じ場所を置き換えず、別ディレクトリに配備してシンボリックリンクを張り替える**

```
releases/2026-08-09-a/   ← 新しい木をここに丸ごと作る
releases/2026-08-09-b/
current -> releases/...  ← ここを張り替えて restart
```

- **稼働中プロセスが見ている木は最後まで変わらない**（遅延読み込みが壊れない）
- **ヘルスチェックは新しい木に対して実行できる**
- **ロールバックはリンクの張り戻しだけ**（7分停止の再発が原理的に起きない）
- **`Restart=always` が救えない「壊れた配備」も、そもそも切り替わらない**

**正典 §14 に運用要件として追記します。**

---

## 6. ★「落ちた」は「検出できた」ではない — R-21 を拡張しました

> (a) `-e` は CJS 評価で top-level await が使えず、**健全な木でも落ちていた**。しかも「壊れた木なら落ちる」検査も**同じ理由で落ちていたので効いて見えた**

**これは対照実験が空振りしていた例で、しかも空振りしていることが「成功」に見える形でした。**

**対照が通ることは、対照が正しい理由で通ったことを意味しません。** そして今回、**対照と本番が同じ誤った理由で同じ結果を出した**ので、**照合しても一致してしまいました。**

→ **新しい規則は作らず、R-21 を拡張しました**（規則表が読まれなくなる方が損失なので）:

> **R-21: 「出なかった」「落ちた」を、理由を読まずに結果として扱わない。**
> ①抽出の0件は「該当なし」と「抽出器が壊れている」の区別がつかない
> ②**検査が失敗しても「検出できた」ことにはならない**。**対照が通ることは、対照が正しい理由で通ったことを意味しない**
> 抽出器・検査器には事前条件と**終了理由の照合**を書く

**本番機で両側（健全→0 / 壊れた木→1・理由も一致）を確認した**のが正しい是正です。**終了コードだけでなく理由まで一致を見た**点が要点です。

---

## 7. ★A-1 の再測定について — 申し送りが正しいです

> 1周の所要が **1秒未満 → 275秒**に変わるので、A-1 は配備後に測り直す必要があります

**そのとおりです。** そして**1点、確認してほしいことがあります。**

**サイクル予算の 46% を占めます。** 確認してください:

| # | 内容 |
|---|---|
| 1 | **1周が10分を超えたときの挙動**（次の周をスキップするのか、遅延して追いつくのか、二重に走るのか） |
| 2 | **超過を検出したときのログ／アラート**があるか |
| 3 | P3 以降でサイクル内の仕事が増えることを見込み、**余裕が何%残っているかを継続的に出す** |

**時刻由来のサイクル番号なので、遅延しても番号は飛びません。** ただし**「間に合わなかった周」がどう扱われるかは別の設計**で、**A-1 の再測定はそこも含めて見る必要があります。**

---

## 8. 指示のまとめ

| # | 内容 |
|---|---|
| 1 | **D-013 補正・D-035・`requiredOddsTrials()` を配備**（承認） |
| 2 | 配備は**別ディレクトリ＋シンボリックリンク切替**に変更（§5） |
| 3 | **V-10 をレース数を増やして再測定**（出走表間 SD・プール SE ≤ 0.25pt・§2） |
| 4 | **A-1 を配備後に測り直す**。サイクル超過時の挙動も確認（§7） |
| 5 | ② は承認済み。オーナーへはこちらから共有 |

---

## 9. 評価

**今便は、指摘される前に自分で塞いだものが3つあります。**

1. **実コード経路での確認**（計算だけでは足りないという判断）
2. **数値リテラルを置かない**（上限だけ動いたときの静かな不足を防ぐ）
3. **A-1 の再測定が要る**という申し送り

そして**自分の検査が2回、間違った理由で成功に見えていたことを見つけた**のが本体です。

> **「落ちた」は「検出できた」ではありません。理由を読むまで同じに見えます。**

**この一文が、この案件で繰り返してきたことの最も簡潔な表現です。** 測定・ゲート・検定・抽出・対照 — すべて同じ構造で、**「期待した結果が出た」ことと「期待した理由で出た」ことの区別**でした。
