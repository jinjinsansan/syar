# 回答 — ES-4・ES-5 の報告と、次の手（`REPORT_ENGINE_LANE_SPEED_ES4_20260916.md`・`REPORT_ENGINE_LANE_SPEED_ES5_20260916.md` への回答）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側・オーナー
**対象**: コミット `0050119`（ES-4 と互換の確認）・`6a4b8a9`（ES-5）／ ブランチ `p4/race-30sec-cuts`・HEAD `6a4b8a9`（origin より 13 先行・push は未実施）
**前の文書**: `REVIEW_ENGINE_LANE_SPEED_ES3_ANSWER_20260916.md`

> 凡例（R-13）: **✔** ＝ レビュー側がコード・履歴を読んで確かめた ／ **△** ＝ 未確認（報告の記載に基づく） ／ **計算** ＝ 実測値と定数から出した値
> 本書は正典を編集しません。
>
> ⚠️ **オーナーの確認が済んでいない事項が 4 つあります**（§7）。
> - **G-1 F-1 の承認**: 開発側は「はい」で承認と受け取っていますが、**レビュー側はオーナーから直接その回答を受け取っていません**。オーナーに確かめてください（R-25: 手配が文書の外で済んだものは、実物を確かめてから書く）
> - **G-2 合格線の測り直し**（§2-2）・**G-3 F-2**・**G-4 F-3（push）**
>
> **確認が要らずに進めてよいもの**: ES-6（§2-1・結果を 1 ビットも変えない）と、その後の staging での実演（§3・オーナーのターミナル）。

---

## 0. 判定

| 範囲 | 判定 |
|---|---|
| ES-4（区間を作る回数の上限の検査） | **受理**。変異（ES-2 を戻す）で落ちる |
| ES-5（案 (f) 同じ和の分解） | **受理**。丸めの差だけ（距離ロス ≤ 3.7×10⁻¹³ m・着順は 182 組 × シード 20 で完全一致）。ループの経路が `884019c` の指紋と 1 ビット一致することを先に確かめ、期待値ファイルを作り直していない。前提が崩れる馬はループに落ちる（本番の形では大河原 1200m の 44 頭）。変異 3 つが落ちる |
| 合格線に届かず止めたこと | **正しい** |
| staging の検査 5 が照合する行 0 本だったことを、そのまま書いたこと | **正しい**（R-21: 「落ちなかった」を理由を読まずに合格と扱わない） |

△ **レビュー側は検査も速さも再実行していません。** 変更したファイルは ✔ `git show --stat` で確かめました（ES-4: 検査と報告だけ ／ ES-5: `lane.ts` と検査 3 ファイルと報告）。

---

## 1. 照合

| 事項 | 照合 |
|---|---|
| `resolveRace` が毎回下ごしらえを作る | ✔ `packages/race-engine/src/race.ts:120` `lanePlanOf(conditions.distance, conditions.course)` |
| ワーカーは試行ごとに `resolveRace` を呼ぶ | ✔ `apps/worker/src/build-race.ts:115`（`ODDS_MC_TRIALS` 回・同じ `oddsConditions`） |
| 残りの重さ | 報告 §4-1: 直線（距離ロスを通らない）は `97364c5^` と同じ水準、楕円との差 約 8〜10µs のうち `lanePlanOf` が 4〜7µs、馬ごとは約 1µs |
| 本番の実測の所要 | 報告 `REPORT_ENGINE_LANE_SPEED_20260915.md` §1 #6: 1 レースを作る周 **98.2 秒・110.8 秒**（`70d9982`・2026-09-15） |
| `0021`・`0022` と古いワーカー | 報告 ES-4 §2（△ レビュー側は staging・本番とも見ていない）: 古いワーカーが呼ぶのは `spend_training_ep` だけ ／ `0021` が剥がすのは `public・anon・authenticated` で、持ち主 `postgres` の実行権は残る（staging で確認）／ `0022` の関数を古いワーカーは呼ばない |

---

## 2. E-5 への回答

### 2-1. ES-6: 下ごしらえをオッズの試行の間で使い回す（案 (g)）— ★確認なしで着手してよい

1 レースのオッズ計算は、`ODDS_MC_TRIALS` 回すべてが**同じ条件**です。下ごしらえ（`lanePlanOf`）はシードによらないので、**1 レースで 1 回作れば足ります。結果は ES-5 と 1 ビットも同じ**です（同じ関数で同じ値を作るだけ）。

**作るもの**
- `resolveRace` の引数に、**作り済みの下ごしらえを任意で渡せる**ようにする（例 `lanePlan?`）
  - 渡されたときは、**その下ごしらえがこのレースの条件（距離・走路の形の値・`courseShape`）と一致するか**を確かめ、**一致しなければ投げる**（別のレースの下ごしらえで計算しない・R-27）
  - 省いたときは、いまと同じく中で作る
- ★**モジュールに状態を持たない**（`WeakMap` などの覚える仕組みは採らない・回答 §Q-2）
- ワーカーの `build-race.ts` は、**モンテカルロに渡すのと同じ `oddsConditions` から試行の前に 1 回作り**、全試行に渡す
- 確定（`pg-store.ts`・`settle.ts`）は 1 回しか呼ばないので変えなくてよい
- 測定器（`apps/cli/src/verify-payout.ts` など）で、同じ条件の試行を繰り返しているところがあれば同じ形にし、報告に列挙する（R-30: 測定器と製品を同じ経路に）

**検査**

| # | 内容 |
|---|---|
| 1 | 指紋（`lane-fingerprint`）・`lane-sum`・`lane-plan-count` がそのまま合格 |
| 2 | **`buildRace` の出力（オッズの行すべて）が、下ごしらえを渡す版と渡さない版で 1 ビット一致**。10 場のうち数場・大河原 1200m（ループに落ちる馬を含む）・8 頭と 18 頭・短距離と長距離 |
| 3 | **下ごしらえと条件が食い違うと投げる**（距離が違う・走路の形が違う・直線のレースに渡す）。**変異**: 一致の確かめを外すと 3 が落ちる |
| 4 | `lane-plan-count` を広げる: **オッズ計算 1 レースで `lanePlanOf` が試行の回数によらず 1 回** |
| 5 | `npx vitest run --maxWorkers=2 --minWorkers=1` の行と終了コード。既存の赤（`edit-grammar-audit` ⑨）が 1 件から増えていないこと |

### 2-2. 合格線の測り直し — ★オーナー確認待ち（G-2）

**なぜ測り直すか**
- 「D-071 の前の 1.3 倍」は、正典 D-036 の **1 レース 138 秒**を前提に、**1 レースでサイクルの 30%（1 周 2 本で 60%）**という予算から出した値でした（`REVIEW_QUESTIONS_ENGINE_LANE_SPEED_ANSWER_20260915.md` §Q-2）
- 本番の実測（`70d9982`・2026-09-15）は **1 レース 98〜111 秒**です。**同じ予算で計算し直すと、許せる比は 180 秒 ÷ 110.8 秒 ＝ 約 1.62 倍**です（計算）
- さらに、サイクルの時間を決めているのは `resolveRace` 1 回ではなく**試行 1 回の中身全体**（`resolveRace` ＋ 券種ごとの的中目の集計）で、後者は版によって変わりません（報告 ES-5 §5 の読み）

**測り方（ES-6 の後）**
- ワーカーの試行 1 回と同じ中身（`resolveRace` ＋ `build-race.ts` の的中目の集計）を、**同じ道具・同じ機械・交互に**、`97364c5^` のエンジンと ES-6 のエンジンで回す（集計のコードは HEAD のものを両方に使う）
- 条件は報告 §4 と同じ 8 条件（16・18 頭 × 1600・3000m × 既定・最長の周）
- **報告には 2 つの比を並べる**
  - ① `resolveRace` 1 回の比（**旧い線 1.3 倍**と比べる）
  - ② 試行 1 回の比（**測り直した線 1.6 倍**と比べる）
- **判定**: G-2 がオーナーに承認されたら ②（全条件の最大が 1.6 倍以下）で、承認されなければ ①で判定します。どちらでも届かなければ止めて報告（次は (d) 並列化）

### 2-3. (d) 並列化

今は進めません。レースの間隔を短くする検討（ゲーム本体の回答 §4-3 (a)）のときの手として残します。

---

## 3. E-6: staging でレースを作って確定する — ★する（10 場配線の実演と 1 回にまとめる）

ES-6 の後に、次の順で行います。**staging への書き込みはオーナーのターミナル**です（R-24: 状態を変える道具は staging 専用）。

| # | 誰 | 作業 |
|---|---|---|
| 1 | オーナー | `npx tsx tools/seed-races.mjs --env staging --races 26 --trials 10000` |
| 2 | 開発側 | 生成されたレースの `course_frozen` を読み、10 場・全件 `'oval'`・`venues.ts` と一致（10 場配線の VW-6 の 4） |
| 3 | オーナー | `npx tsx tools/settle-races.mjs --env staging` |
| 4 | 開発側 | `tools/verify-entrant-freeze.mjs --env staging --recompute-settled`: **新しく確定した行の着順が全件一致**（ES の検査 5 ＋ VW-6 の 6）・`--ignore-course-frozen` で食い違いが出る（変異の実演）・既存の走路の凍結が無い行の通報の件数（VW-6 の 7） |

- **26 本**は、10 場配線の報告 §3-2 の「10 場が出そろう連続本数の最大」
- **`--trials 10000`** で足ります。ここで確かめるのは**走路の凍結と着順の再計算**で、オッズの妥当性ではありません（staging の馬券は 0 枚）。**フルの試行回数は V-10 のとき**に回します
- 既知の食い違い（cycle 686・868・走路の凍結の無い古い行）は、報告で分けて書く

---

## 4. F-2 の材料 — `0021`・`0022` を先に本番へ当てるか（オーナー確認待ち・G-3）

**開発側の確認（ES-4 §2・△）では、古いワーカーは止まらない見込み**です。ただし次の 3 点が残っています。

1. `0021` の冒頭に「**下書き・未適用。稼働中の定義との md5 照合の前に適用しないこと**」の註記が残っている
2. **本番の DB そのものは見ていない**
3. 本番に `0020`（`spend_training_ep` の本体にセットアップの判定）が当たっているかが分からない。当たっていれば、古いワーカーの調教は**いまも毎回失敗して休養に落ちている**はずで（監査 H-3）、`0021` はそれを直す側

**推奨する進め方**
1. **オーナーの許可を得て、本番の DB を読み取り専用で 1 回確かめる**: 適用済みの移行ファイルの一覧（`schema_migrations`）／ `spend_training_ep` の稼働中の定義の md5 と `0020`・`0021` の本体の照合 ／ `spend_training_ep`・`place_bet`・`exchange_prize` の実行権（ACL）／ `app_environment = 'production'`
2. 問題がなければ、オーナーの指示で `0021` → `0022` を当て、**前後で V-20** を回す
3. これは新しいワーカーの配備とは独立に進めてよい

**急ぐ理由と急がない理由**: 「anon が他人の EP を消せる穴」（監査 H-4）は本番に残っていますが、本番の利用者は 0 人です（正典 D-095 の記載）。

---

## 5. F-3（push）の材料 — オーナー確認待ち（G-4）

- ブランチは origin より 13 先行しています
- このブランチは 2026-09-15 にもタグ付きで push しています。ワーカーの配備は `tools/deploy.sh` による手動なので、**push だけで本番のワーカーが変わることはありません**（△ Web 側の配信がこのブランチの push で動くかは確かめていません）

---

## 6. 触ってよいもの（ES-6）

| 可 | 不可 |
|---|---|
| `packages/race-engine/src/race.ts`・`packages/race-engine/src/types.ts`（引数の型）・`packages/race-engine/test/`・`apps/worker/src/build-race.ts`・`apps/worker/test/`・測定器で同じ条件の試行を繰り返しているところ（報告に列挙） | `lane.ts` の計算・指紋の期待値ファイル・較正定数・`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`・`stepM`・`LANE_MODEL`・`packages/render`・`apps/web`・正典・本番 DB・ワーカーの配備 |

同じ作業ツリーの作法はこれまでと同じです。

---

## 7. オーナーに確かめていただくこと

| # | 確認 | レビュー側の推奨 |
|---|---|---|
| **G-1** | ★**F-1（ES-5 で結果を丸めの差だけ許す）を承認したか**。開発側は「はい」と受け取っているが、レビュー側は直接受け取っていない | 承認していれば、その旨を開発側・レビュー側の両方に。**承認していなければ ES-5 は本番に出さない**（ES-3 に戻すかはその時に相談） |
| **G-2** | 合格線を「1 レースのオッズ計算でサイクルの 30%」から測り直し、**試行 1 回の比で 1.6 倍以下**とする（§2-2） | **はい** |
| **G-3** | F-2: 本番の DB の読み取り確認を許可し、問題なければ `0021` → `0022` を当てる（§4） | **確認を許可**（適用は結果を見て） |
| **G-4** | F-3: push | オーナーの判断 |

---

## 8. このあとの順番

1. **ES-6**（§2-1）→ 速さの報告（§2-2 の 2 つの比）
2. G-2 の判定
3. **staging での実演**（§3・オーナーのターミナル）
4. 10 場配線の最終裁定（`REVIEW_VENUE_WIRING_VERDICT_20260915.md` の VC-1〜VC-4。V-4〜V-6・V-18 の残りの計測もここで）
5. 本番へ `0023` と新しいワーカーの配備（オーナーの指示で）。`0021`・`0022` は §4 のとおり独立に

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
