# REPORT_P3_D049_20260811 — 気性の下限（D-049）と V-15 の較正、V-7/V-14 の測り直し

## 本便で正典の改訂が要る決定をしたか

**1件あります（上側の基準が無いこと）。**

`TEMPER_FLOOR_RATIO` を **0.60** に較正しました。ただし **V-15 は下限だけのゲート**なので、
`INJURY_BASE_PROB` のときの「**最小余裕が最大**」という選び方が**使えません**。
比率を上げるほど V-15 の余裕は増えますが、比率 1.0 は「気性が一切動かない」＝
**§7.2 の `temperDelta` が無意味になる**ことです。

→ **上側に基準がありません**（Q-P3-27）。今回は「下限50%に余裕を持たせつつ、
§7.2 が動かせる幅を最大に残す」点として 0.60 を採りました。**判断の根拠を §2 に全部出します。**

---

## 1. 結果一覧

| 項目 | 結果 | SHA |
|---|---|---|
| **D-049** 気性の下限 | 実装・較正済み | `6423e38` |
| **V-15**（新設・中盤SD ≥ 誕生時SD の50%） | **PASS 60.8%**（1,800頭） | `6423e38` |
| **V-7a / V-7b** 測り直し | **29.9% / 0.7%（不変）PASS** | `259b17d` |
| **V-14** 測り直し | ①② PASS / **③ FAIL（差 6.4pt・不変）** | `259b17d` |
| **B-1** 再実行（検査⑩追加） | **PASS 10/10** | `2a4106a` |
| テスト | **600件 緑**（temper 12件を追加） | `2a4106a` |

---

## 2. ★`TEMPER_FLOOR_RATIO` の較正 — 決め打ちしていません

### 実装した形（裁定どおり）

```
下限   = 誕生時の気性 × TEMPER_FLOOR_RATIO
下げる = temper + delta × (temper − floor) / (誕生時 − floor)
上げる = temper + delta × (100 − temper) / (100 − 誕生時)
```

`headroom` と同じ形なので**下限に漸近しますが到達しません**。
下限が**遺伝した値に比例**するので、**順序と分散が保たれます**。

### 掃引（400頭・バランス型・イベント有・週169）

| 比率 | 中盤 SD比 | 漸近値 | §7.2 が動かせる幅 |
|---|---|---|---|
| 0.50 | 52.2% | **→50%（ゲートちょうど・余裕ゼロ）** | 誕生値の50% |
| 0.55 | 56.2% | →55% | 45% |
| **★0.60** | **60.5%** | **→60%** | **40%** |
| 0.65 | 65.1% | →65% | 35% |

### ★「漸近値」が判断の要です

**SD比は比率 r に漸近します。** 全馬が下限 `r × 誕生値` に収束するので `SD → r × 誕生時SD` です。
実測でも確認できます（r=0.60・1,800頭）:

```
  104週 83.1% → 130週 66.8% → 169週 60.8% → 208週 60.0% → 259週 60.0%
```

つまり **0.50 は「バランス型で測ったから 52.2% で通った」だけ**で、
**より休養の多い方針なら 50% ちょうどまで落ちます**（＝余裕ゼロ）。
0.60 なら**どの方針でも 60% を下回りません**。これが 0.60 を採った理由です。

### 確定値での実測（1,800頭・seed 42）

```
  誕生時: 平均 50.96  SD 11.261  （下限の平均 30.58）
      週     頭数       平均       SD      SD比
    104   1797    35.13    9.355    83.1%
    130   1797    31.44    7.520    66.8%
    169   1793    30.65    6.841    60.8% ←中盤
    208   1792    30.58    6.762    60.0%
    259   1788    30.57    6.752    60.0%
  ★中盤より前に引退した馬: 7 頭（致命的故障・§7.5）→ 中盤の集団は 1793 頭
★V-15: PASS — 中盤の SD は誕生時の 60.8%（下限 50%）
```

### ★変異試験で「V-15 が実際に検出する」ことを確かめました

`TEMPER_FLOOR_RATIO = 0` に置換 → **V-15 が 33.5% で FAIL**。

そして**そのとき V-2e も V-2f も B-1 も 600件のテストも全部通ります**。
**V-15 だけが検出します。** これがゲートを新設した意味です。

---

## 3. ★V-14 — 判定も差も変わりませんでした

`verify-v14.ts` を `advanceWeek` に載せ替え、**`enableEvents: true`（B-1 と同条件）**で測り直しました。

```
  方針                  開放率      SD      SE     故障/頭     引退       EP/頭
  放置(軽めのみ)          74.5%    4.91   0.245     0.39   0.5%     17,803
  バランス型             90.3%    4.88   0.244     0.91   0.5%     53,003
  追い切り偏重            96.7%    4.60   0.230     1.32   1.5%     83,614

  ★① 適切な育成が 88%以上 : 90.3%（余裕 2.3pt = 9.5 SE）  PASS
  ★② 差が 12pt 以上       : 15.8pt（90.3 − 74.5）（余裕 3.8pt = 10.9 SE）  PASS
  ★③ 追い切り偏重が支配的でない: 96.7% vs 90.3%（時間軸・+2pt まで）  FAIL
     同一EP予算下（D-047）: EP あたり 0.68倍  PASS
★V-14: FAIL
```

| | 是正前 | 是正後 | 差 |
|---|---|---|---|
| 追い切り偏重 | 97.2% | 96.7% | −0.5pt |
| バランス型 | 90.8% | 90.3% | −0.5pt |
| **③ の判定を担う差** | **6.4pt** | **6.4pt** | **0.0pt** |
| EP あたりの比 | 0.68倍 | 0.68倍 | 0.00 |

**水準が両方 0.5pt ずつ下がっただけで、判定を担う「差」は 6.4pt のまま**です。
気性の下限を入れると温順側の底上げ（`temperCoef` の下限 0.9）が弱まるので水準は下がりますが、
**両方針に同じだけ効くので差は動きません**。

→ **③ の FAIL は D-049 では解けません。** 次の梃子は `menuIntensity` で、**レビュー側の裁定待ち**です。

---

## 4. V-7 — 不変

```
    方針                    V-7a 恒久ダメージ      V-7b 致命的
    放置(参考)                 14.9% FAIL     0.6% PASS
    ★バランス型                 29.9% PASS     0.7% PASS   SE 1.08pt / 0.19pt
    追い切り偏重(参考)             41.3% FAIL     1.2% PASS
```

**29.9% / 0.7% で是正前と完全に同じ**です。故障確率は気性にもイベントにも依存しないためで、
`diag-loop.ts` の予測どおりでした。**`INJURY_BASE_PROB = 0.0013` の較正はそのまま生きます。**

### ★`fixDurability` を落としました（Q-P3-20 の裁定待ち）

「キャリア開始時の丈夫さに固定して測る」版は、合成器に載せ替えたため**測定できなくなりました**。
**測定のためだけの旗を合成器に足すと、その旗が本体に残ります**。
★測れなくなったことを黙って落とさず、ツールの出力に書き残しています（R-21）:

```
  【★丈夫さの扱い（照会 Q-P3-20）】
    実測（現在値を使う・§7.5 の式どおり）: 開始時の平均 646.5 / 期間平均 642.0
    → 差 4.5（重度故障の −100 が効いたぶん）
    ★「開始時に固定」版は合成器に載せ替えたため測定していません（Q-P3-20 の裁定待ち）
```

---

## 5. B-1 再実行（検査⑩を追加）

```
✓ ⑩ 気性が誕生時×比率の下限を割らない（D-049）  誕生 59.0 → 引退時 35.42 / 下限 35.40
★B-1: PASS — 260 週すべて記録し、10項目すべてを実データで検査
```

**同じ馬が、是正前は 59.0 → 0.0 でした。** 下限 35.40 に対して 35.42 で止まっており、
**漸近するが到達しない**ことが実データで確認できます。

### ★ここで1つ塞ぎました

`tools/verify-b1.mjs` は **`.mjs` なので型検査の外**です。`birthTemper` を渡し忘れると
`temperFloor(undefined)` が **NaN** になり、**落ちずに気性が NaN のまま一生を通ります**。
→ `num()` で数値として読めることを確かめてから渡すようにしました。

---

## 6. ご指摘への応答

> **これは R-23 の新しい形です。「いつの証拠か」だけでなく「どの経路の証拠か」も失効の条件でした。**

そのとおりでした。`verify-v7` / `verify-v14` **両方**を `advanceWeek` に載せ替えました。
片方だけ載せ替えると、また「V-7 はこの経路・V-14 はあの経路」に戻ります。
今後、週ループは **`advanceWeek` の1本だけ**です。
`diag-loop.ts` の `legacyCareer` は**載せ替えの証拠として残してあります**（判定は出しません）。

> **検証が対象を汚染し、再実行するほど結果が良く見える。**
> **通常の劣化は「だんだん落ちる」ので気づけますが、これは「だんだん通りやすくなる」**

「気づく契機が構造的にない」というのが要点だと理解しました。
今回 B-1 で気づけたのは、**素質が 578 → 606 → 637 と『きれいすぎる比（×1.05）』で動いていた**からで、
**数字を見ていなければ通していました**。

`birth_snapshot` は「再実行のたびに誕生時に戻す」ので、この型は B-1 では塞がりました。
ただし**同じ型が他の staging 検証ツールにもあります**（`verify-prize` / `verify-g6` は
自分で後片付けしますが、`seed-world` を挟まない限り馬の状態は残ります）。
次便で **staging の検証ツール全体について「実行前の状態に戻るか」を1本ずつ確認**します。

---

## 7. 照会

| # | 内容 |
|---|---|
| **Q-P3-27（新規）** | **★気性の下限比率に上側の基準がありません。** V-15 は下限だけのゲートなので、比率 1.0（＝気性が一切動かない）が「最大余裕」になってしまいます。「§7.2 の `temperDelta` が意味を持つ最小限」を別のゲートにするか、比率そのものを正典で固定してください。今回は 0.60（誕生値の 40% まで下がれる）としました |
| **Q-P3-20** | 故障確率の `durability` は現在値か開始時か（§4 のとおり測定できなくなりました） |
| **Q-P3-21〜26** | 前便のまま未回答 |
| — | **V-14 ③**（時間軸 6.4pt）。D-049 では動きませんでした。次の梃子は `menuIntensity` |

---

## 8. 未着手（次便）

**B-3**（V-11 の構造報告） / **G-7 の staging 実証**（実装は P2 で完了・staging にレースが無いため未実証） /
**B-6 の配線**（`race-field.ts:99-100`） / **D-043 の残り**（検証ツール1本をバンドルに対して流す）。

---

## 9. 再現コマンド

```bash
npm run typecheck && npx vitest run                                  # 600件 緑
npx tsx apps/cli/src/verify-v15.ts --horses 1800 --seed 42           # V-15
npx tsx apps/cli/src/verify-v14.ts                                   # V-14
npx tsx apps/cli/src/verify-v7.ts --horses 1800 --seed 42            # V-7
npx tsx tools/verify-b1.mjs --env staging --seed 42 --menu hard_only # B-1
```

---

## 10. ★オーナーへ（再掲）

**staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん）。
レビュー側から「新しい接続文字列を貼るタイミングで一緒に」とのことです。
