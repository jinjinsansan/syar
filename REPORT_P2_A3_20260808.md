# REPORT_P2_A3 — A-3 が複数シードで FAIL ／ §8.6 を本番構成で検証

**開発側 → レビュー側** / 2026-08-08
**コミット**: `3372032`（push 済み・418 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**なし。** ただし **A-3 の判定域について判断をお願いしたい**件があります（§1）。

---

## 1. ★A-3 が複数シードで FAIL しました

```
M=4000 / races=32000（races = M × 8）

seed 42     82.62%  +0.62pt
seed 7      82.26%  +0.26pt
seed 2026   81.97%  -0.03pt
seed 31337  83.73%  +1.73pt   ← 判定域 ±1% を超える

平均 82.64%  SD 0.77  SE 0.39
★全シードが ±1% 以内: FAIL
```

### ★前便の「収束確認」は不十分でした

`seed 42` の **1点**で「+0.62pt に収束」と報告していました。4シード目で外れます。
**1シードでの収束は「そのシードで収束した」としか言えません。**

### 数字の読み方

- **平均 82.64%** は SE 0.39 に対して **+1.6 SE**。系統的なずれとは言い切れません
- ただし**判定域は個々のシードに対する ±1%** なので、31337 が 1.73pt 外れている以上 FAIL です
- **全4シードが目標より上**（+0.26〜+1.73pt）

★**原因は推測しません。** A-3 では機構を推測しては否定される順序を7回繰り返しました。
  「全部上振れだから凸性の残り」と言いたくなりますが、**それを言うのが8回目**になります。

### 判断をお願いしたいこと

| | 内容 | 懸念 |
|---|---|---|
| **A** | M を上げる（凸性を減らす） | `races ≫ M` を保つため races も比例増。M=10000 なら races=80000 で、1シード数十分 → 4シードで数時間 |
| **B** | 判定域の議論 | SD 0.77 のばらつきに対し ±1% は **1.3 SD 相当**。達成可能な水準か |

どちらも測定基準に関わるため、こちらでは決めません。

---

## 2. ★§8.6 が本番構成で検証できました

Vercel にデプロイした画面に表示された値**だけ**を使い、第三者として計算:

```
seed_reveal  2bfa640c478b3d77cc8ee442ebaa7442069cb1c4c386994c4a5b42b34f8e5baf
sha256(...)  67cdd79ff75eba068c883d1f3a3a19fc7eeff7bce7c1d769e9e66079884470e9
seed_commit  67cdd79ff75eba068c883d1f3a3a19fc7eeff7bce7c1d769e9e66079884470e9
→ 一致
```

**「公開すれば誰でも再計算できる」が、実際に公開された画面で成立しています。**
これまで DB を直接見て確認していましたが、今回は**外部から見える情報だけ**で検証できました。

---

## 3. Vercel へ初回デプロイ

```
状態      Ready（ビルド成功）
アクセス  Deployment Protection（Vercel SSO）で保護中 — オーナーのみ閲覧可
環境変数  anon キーのみ3環境。★service_role は置きません（RLS を素通りするため）
```

画面: `/` 番組表 ／ `/races/[id]` 出馬表・オッズ・人気・着順（確定後のみ）・§8.6 の検証

⚠️ モノレポの設定で1度失敗しました。**Root Directory を `apps/web` にすると
   リポジトリ全体がアップロードされず**、ワークスペース依存が解決できません。
   ルートに `vercel.json` を置く形に修正しています。

### ★公開範囲を anon で実測

```
見える:   races_public / race_entries_public / race_odds_public
拒否:     races / race_entries / race_odds / **horses**
列に無い: intervention_log / cap_violations / horse_id / probability
```

**`horses` の拒否が重要**です。`genotype` と `potential` が入っており、
§5.5 が「素質はプレイヤー非公開」と定めています。

---

## 4. V-4 は判定できません（レース数不足）

```
確定 11レース / 1番人気の勝率 2/11 = 18%（V-4 は 30〜34%）
SE = 11.6pt → 差は 1 SE 程度
人気と着順の相関 r = +0.291（前回 73行では +0.098）
```

★**この数字で主張しません。**11レースで SE 11.6pt では判定不能です。
  相関は改善しており向きは正しい。A-1 の24時間で144レース貯まれば
  SE は約 3.9pt に縮み、V-4 を判定できます。

---

## 5. 合格基準

| # | 状態 |
|---|---|
| A-1 | **計測中**（20:53 開始・約22時間後） |
| A-2 | **PASS**（★重複＋中途半端の両方） |
| **A-3** | **★FAIL**（4シード中1つが判定域外・§1） |
| A-4〜A-7 | **PASS**（実 DB） |
| A-8 / A-9 | **PASS**（418 tests / typecheck 0） |

---

## 6. 未実装

| | |
|---|---|
| 認証 | オーナー判断により後回し（現在は公開情報の閲覧のみ） |
| §9.6 購入フロー（UI） | RPC は動作済み。UI は認証が前提 |
| §7 成長・調子・疲労 | `age`=4 / `condition`=3 / `fatigue`=0 で固定 |
| §11.3 景品交換 | 未着手 |
