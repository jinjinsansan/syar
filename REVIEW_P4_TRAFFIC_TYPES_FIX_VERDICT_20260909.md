# 再レビュー回答 — 表示時間と監査経路の修正便

レビュー日: 2026-09-09。レビュー側: Codex。
依頼書: `REVIEW_REQUEST_P4_TRAFFIC_TYPES_FIX_20260909.md`。
確認HEAD: `52d659c`。実装修正: `e6a6e0a`。比較元: `2b4a3a8`。

## 1. 判定

**再修正を依頼する。F-2の実装修正、時間写像の単体検査、監査への尺の出力、Q-2/Q-3の文書反映は確認した。経路の接続検査と達成可能な目標尺の扱いは未完了。**

オーナーの映像評価を待たずに直せる技術上の残件を、下記F-3〜F-5として区別する。既存の映像に合格を出すものではない。製品コード・既存テスト・正典は変更していない。

## 2. F-3 / P2 — 画面の接続を外しても接続検査が通る

対象: `apps/cli/test/display-rate-path.test.ts:79`、`packages/render/test/audit-screen-parity.test.ts`。

共有関数を呼ぶ現在の実装自体は確認できた。しかし前回の「コメントや未使用の関数呼出を足すだけで成功する検査にしない」は満たしていない。

画面ソースのメモリ上のコピーに対し、`page.tsx:1281` を次の形に置換した。

```ts
const warp = timeWarpFor(knots, { cruise: 2, spurt: 2, straight: 2, goal: 1, start: 1 });
// ratesForPolicy(knots, targetDisplaySec(DIST), RACE_PACE_POLICY)
```

接続検査と同じ4判定（共有関数の呼出文字列、目標の文字列、旧関数名の禁止、DEFAULT_PHASE_RATESの直渡し禁止）は**すべて成功した**。元の製品ファイルは変更していない。ソース判定を複製したプローブであり、改変版を使って全テストを回したという主張ではない。

コメントだけの例は未使用importの型チェックで検出される可能性もある。そのため、コメントの代わりに `void ratesForPolicy(knots, targetDisplaySec(DIST), RACE_PACE_POLICY);` を実行し、戻り値だけを捨てるコピーも確認した。これも4判定すべて成功し、関数と方針変数への参照は残る。実際の時計へ戻り値が届くことを検査する必要がある。

`readable-rates.test.ts` は共有関数を直接呼ぶため、画面の接続を外してもその入力は変わらない。`audit-screen-parity.test.ts` も監査側のソース・実行しか見ておらず、画面と監査の出力を比較していない。従って、単体の振る舞いテストを別に追加しただけでは、この穴は閉じない。

**修正条件:** 画面で実際に採用する時計の生成経路をテスト可能な部品にし、監査側と同じ入力で呼んで照合する。画面からその部品への接続も、実際に使用される戻り値を対象に検査する。ASTによる呼出・引数・戻り値の接続確認、またはページ側の実行を使う検査など、必要な範囲でよい。単なる関数名検索は補助に留める。

少なくとも次の故障を検査対象とする。

- 画面の時計を固定倍率へ置換し、正しい関数名をコメント／未使用呼出にだけ残す。
- 画面だけをlegacy固定へ変える、またはlegacy/readableの対応を逆転する。
- 画面だけの直線長を400へ戻す。290/400/620mの両経路を比較し、400m以外で失敗させる。

新しい監査側の直線長修正は正しい。ここで再修正を求めるのは「画面との一致を検査済み」という範囲であり、F-2の実装を元へ戻す指示ではない。

## 3. F-4 / P2 — 飽和と、達成可能な目標に対する逆算のずれは別問題

対象: `packages/render/src/time-warp.ts:307` / `:337`、`packages/render/test/readable-rates.test.ts` の目標応答テスト、依頼書§4。

現実装は5.2倍／2.1倍の区間時間で道中倍率を逆算してから、区間倍率を2へ切る。切った後に増えた区間時間を道中へ配分し直していない。

依頼書の「現在の45.7秒は飽和側なので、さらに短くしても尺は変わらない」は再現した。一方、「上限2の構造上、目標を達成できない」「尺を詰めるには上限か固定値を変えるしかない」は一般化が過ぎる。

**反例:** 新設テストと同じ `knotsOf(2400)`、目標100秒。

| 計算 | cruise | spurt / straight | start / goal | 実尺 |
|---|---:|---:|---:|---:|
| 現 `ratesForPolicy(..., 'readable')` | 1.5052631579 | 2 / 2 | 1 / 1 | 107.5214029460秒 |
| 同じ写像へ別の道中倍率を入力 | 1.6998277042 | 2 / 2 | 1 / 1 | 100.0000000000秒 |

後者は既存 `timeWarpFor` をそのまま使い、道中倍率のみ二分探索した。全局面の上限・発走とゴールの等速・同じ終点を維持して100秒にできる。つまり100秒は**達成可能**。テストが確認する「100→120と目標を延ばせば実尺も延びる」だけでは、前回の「実現可能なら目標へ合わせる」の確認にならない。

**修正条件:** readableでは、上限適用後の区間時間を前提に道中倍率を求める。`timeWarpFor` の補間を含む最終の表示秒で許容誤差を確認する。上限・下限に達した場合は制約を優先し、達成不能を報告する。旧方式を変える必要はない。

現在の45.7秒のように短すぎる目標は、これでも達成できない。既定9条件が既に上限に張り付く場合は、その見た目を変えずに達成可能な領域の計算を直せる。目標100秒・120秒など達成可能なケースと、極端に短い／長いケースを別々に検査する。

既存の上限・固定値を変える作業は、この技術修正に必須ではない。実際の通常尺を短くしたいという商品判断は、引き続き別件。

## 4. F-5 / P2 — cappedPhasesを目標の成否として説明している

対象: `packages/render/src/time-warp.ts:379`、`packages/render/test/readable-rates.test.ts:204`、依頼書§4。

`cappedPhases` は「legacyで計算した倍率からreadableで下げた区間」の一覧。目標達成の有無でも、全種類の制約でもない。返す数値の計算自体は下記の事実を隠していないが、型注記とテスト名が誤った保証になっている。

| 条件（同じ2400mの合成knots） | cappedPhases | 実尺 | 実尺−目標 |
|---|---|---:|---:|
| legacy、目標1秒 | 空 | 44.9435769956秒 | +43.9435769956秒 |
| readable、目標150秒 | spurt / straight | 140.4965440142秒 | −9.5034559858秒 |

従って「空なら目標が実現」「切られていれば必ず超過が正」はどちらも偽。現在のテストが成功するのは、既定の短い目標しかその条件で走査していないため。

**修正条件:** `cappedPhases` の意味を可読性制限による倍率変更に限定する。目標達成は実尺との差と明示した許容誤差で別に判定する。必要なら道中の上限・下限による飽和も別フィールドで記録する。「制限あり」と「達成不能」を同義にしない。上記2ケースを含め、説明・テスト名・依頼書の一般化を訂正する。

## 5. 対応表への判定

| 前回の条件 | 再レビュー結果 |
|---|---|
| F-1 / Q-1a-1 | 共有関数化は確認。画面の実際の接続を守る検査はF-3で継続 |
| F-2 | 監査側の直線長修正は確認 |
| Q-1a-2 | 旧テストを維持しlegacy対象へ改名、確認 |
| Q-1a-3 | 新版の写像・終点・速度上限の単体検査を確認。ページへの接続保証とは別 |
| Q-1a-4 | 目標への応答は確認。達成可能な目標への一致と達成判定はF-4/F-5で継続 |
| Q-1a-5 | 本編・総尺・目標・差の出力を確認。再生速度つまみを測定対象外とする明記も確認 |
| Q-1a-6 | 監査への3直線長の伝達とlegacy切替は確認。画面との比較はF-3で継続 |
| Q-1b | 商品条件は未承認のまま。技術条件にも上記残件がある |
| Q-2 | `CLAUDE.md` に共有ツリーの作法が入ったことを確認 |
| Q-3 | genotype変更なし、枠番割当をデモ限定とする注記を確認 |

## 6. 検証と証拠の範囲

- 開始時のHEADは `52d659c`。追跡済み差分なし。未追跡 `.claude/` があるため、厳密には `git status --short` は空ではない。レビュー中も保持した。
- 型チェック: exit 0。
- 全体テスト: Vitestの最終集計は143ファイル・1452件すべて成功。22:25:08開始、371.21秒。実行ツールの終了コードは1で、ログにはNodeの `globSync` ExperimentalWarningがPowerShellの `NativeCommandError` として記録されている。テスト失敗は報告されていないが、コマンド全体のexit 0を確認したとは書かない。
- 9条件の交通流監査: exit 0。新版の接近判定0、最大横速度約1.30m/s、最終着順一致を再確認。本編尺は前回の実測と一致。
- 2400m新版の総尺は113.2679787771〜114.8160945857秒。これは許容帯ではなく測定値。
- 反例プローブ: `node --import tsx tmp/review-traffic-types-fix-probes.mjs`。出力は同名 `.json`。共有ファイルの改変なし。100秒の反例は既存APIで再計算した。
- 全体テストログ: `tmp/review-traffic-types-fix-tests.log`。監査ログ: `tmp/review-traffic-types-fix-audit.log`。

9条件のスカラー測定値の一致だけでは「映像が1コマも変わらない」の証明にはならない。差分を読み、画面の時計が同じ倍率を返すラッパーへ置換され、描画処理の変更がないことも併せて確認した。今回ブラウザ再撮影を省略したこと自体は差し戻し理由にしない。ただしフレーム単位の同一性を測定したとの表現は避ける。非既定走路では監査側の出力を意図して変更している。

依頼書の件数は、旧1433件から新1452件へ**19件増**（13+5+1）。「増えた20件」は成功数1432→1452との差であり、総テスト追加数とは違う。既存失敗1件の解消と新規19件を分けて記録する。直線3通りの検査と9条件の監査も別実行であり、27条件の組合せを測ったとは扱わない。

次便はF-3〜F-5の修正と反例の検出を中心に提出する。映像の美観、型C、新しい遺伝仕様、許容尺の決定は今回の技術修正へ混ぜなくてよい。
