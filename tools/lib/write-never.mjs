/**
 * ★**「読む側だけ在って、書く側が無い」列の登録簿**（★正典 **D-119** / **DB-1**・2026-09-20）
 *
 * 【🔴 ★なぜ登録簿か】
 *   ★`broad-deletes` / `printed-defaults` と同じ作法です。
 *   ★**形だけを機械が拾い、★当たりかどうかは人がここに書く。**
 *   ✔ ★精度を測りました（★8 表）: ★**6 件中 5 件 ＝ 83%**（★今日いちばん高い網）。
 *   ⚠️ ★それでも 100% ではないので、★**機械に判定させません。**
 *
 * 【★何を見るか】★`tools/diag-write-never.mjs` と同じ形を、★検査から呼びます:
 *   ★① ★移行から列を集める ★② `insert(… col …)` / `set col =` を全文検索
 *   ★③ ★読む側は **camelCase も**見る（★TS 側は `bredThisYear`）
 *   ★④ ★`default` が在る列は ★**製品が読んでいるときだけ**山に積む
 *
 * 【★新しく当たったら】
 *   ★検査が落ちます。★**繋ぐ**か、★ここに「なぜ当たりではないか」を書いてください。
 *   🔴 ★**「あとで繋ぐ」で素通りさせないこと** — ★それが `D-119` の 5 例を作りました。
 */

/**
 * ★表ごとの、★**機械が拾った列**と、★人の判定。
 *
 * ★`known` … ★拾われることが分かっている列（★増えたら落ちます）
 * ★`why`   … ★なぜそうなっているか（★直す予定なら、簿の id を書く）
 */
export const WRITE_NEVER = {
  race_entries: {
    known: ['intervention_mult', 'margin'],
    why: '✅ ★`cap_violations` は ★**2026-09-20 に繋いだので、★もう当たりません**'
      + '（`settle.ts` が通し、`pg-store.ts` が書く）。★**網がそれを見分けたことの確認にもなりました。**'
      + '⚠️ ★`intervention_log` はこの検査では拾われません（★読む側が `tools/` だけ。★製品は読まない）。'
      + '★`intervention_mult` … ⚠️ ★**介入が未実装**（★`apps/web/` に受付の口が 0 件）。'
      + '★★**族ではありません**（★正典 `D-119` が「族に入れないもの」として明記）。'
      + '★`F2-LOG-NOT-PUBLISHED` の仕掛けが、★実装した日に落ちます。'
      + '🔴 ★`margin` … ★**偽陽性**。★短くありふれた語で、★camelCase 展開（`margin` → `margin`）も効かない。'
      + '★`marginLabel` や CSS の margin に当たっています。★**追いません**（★網の限界として記録）',
  },
  horses: {
    known: ['bred_this_year', 'coverings_this_year'],
    why: '🔴 ★**当たりです。** ★`canMate` が読むのに、★書く SQL が 0 件（★簿 `COUNTER-NEVER-WRITTEN`）。'
      + '★既定値（`default false` / `default 0`）は ★**行を作るときだけ**の話で、'
      + '★**毎年 戻す**必要は消えません。→ ★`POOL-SUPPLY` の実装と同じ便で繋ぎます',
  },
};
