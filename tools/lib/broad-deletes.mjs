/**
 * ★**自分が作った行以外を消す `delete` の登録簿**（★`CLEANUP-NO-RECORD`・2026-09-19）
 *
 * 【🔴 ★なぜ在るか】
 *   ★片付けの道具が ★**何を消したかをどこにも残さない**と、
 *   ★「本当にそれだけを消したか」を ★**後から人が確かめる方法がありません**。
 *   ★戻す話ではありません（★漏れの掃除なら戻す必要は無い）。★**確かめる**話です。
 *
 * 【⚠️ ★なぜ「検出」ではなく「登録簿」なのか】
 *   🔴 ★2026-09-19、★同じ日に ★**検出の網を 3 回 書き直しました**（★CK-11 の走査）。
 *     ★1 回目は精度 20%、★2 回目は ★**探しているものを落とし**、★3 回目でやっと使えました。
 *   → ★★**「危ない `delete` かどうか」を機械に判定させると、★網のほうが壊れます。**
 *   → ★だから ★**形だけを機械が拾い、★危ないかどうかは人がここに書く**形にします。
 *     ★`known-red.mjs` / `open-findings.mjs` と同じ作法です。
 *
 * 【★機械が拾う「形」】
 *   ★`delete from …` のうち、★次のどれか:
 *     ★① `where` が無い（★全消し）
 *     ★② 述語に範囲比較がある（`>=` `<=` `>` `<`）
 *     ★③ 述語が `date` で引いている（★共有の日次集計を消しうる）
 *   ⚠️ ★この 3 つは ★**「危ない」ではなく「人が見るべき」**という意味です。
 *     ★実際 5 件のうち 2 件は「やらなくてよい」でした。
 *
 * 【★新しい `delete` を足したら】
 *   ★検査が落ちます。★**ここに 1 行 足して、★`records` に「どう残すか」を書いてください。**
 *   ★残す必要が無いなら、★`records` に ★**なぜ要らないか**を書きます。
 */

/**
 * ★道具ごとの、★**自分が作った行以外を消しうる箇所**。
 *
 * ★`sites` … ★機械が拾う形の**件数**（★増えたら検査が落ちます）
 * ★`records` … ★**何をどう残すか。★残さないなら、なぜ要らないか**
 */
export const BROAD_DELETES = {
  'cleanup-ds7-leak.mjs': {
    sites: 3,
    records: '✅ ★消す前に `races` / `public.users` / 所有馬 / `auth.users` の**識別子**を '
      + '`evidence/cleanup-ds7-leak/<時刻>.json` に書き、★端末にも件数と先頭 6 件を出す（★`9b3adea`）',
  },
  'verify-unlock-daily.mjs': {
    sites: 1,
    records: '✅ ★`unlock_daily where date = today` は ★**ワーカーが計算した今日の分布**で、'
      + '★この道具が作った行ではない。★**消す前の行を JSON で出す**（★取り直した値と違ったときに気づけるように）',
  },
  'verify-flow.mjs': {
    sites: 1,
    records: '✅ ★`point_flow_daily where date = today` は 🔴 ★**§11.2 の実経済の指標**。'
      + '★取り直せる値だが、★消したことが残らなかった。★**消す前の行を JSON で出す**',
  },
  'verify-sy1-clone.mjs': {
    sites: 4,
    records: '⚠️ ★**残しません。** ★`cycle_index >= 900000` は ★**試験専用に空けてある領域**で、'
      + '★本番のサイクル番号（★漏れの掃除が使う 100000 より下）と重ならない。'
      + '→ ★「自分が作った行だけ」と実質 同じ（★**NT-10**: ★機械が既に分けているものに規則を足さない）',
  },
  'verify-v11-synthetic.mjs': {
    sites: 4,
    records: '⚠️ ★**残しません。** ★`verify-sy1-clone` と同じ `cycle_index >= 900000`。'
      + '⚠️ ★ただしこの道具は ★**集団の育成週を消費**します — ★それは `delete` の話ではなく、'
      + '★`tool-aftermath` の `notRestored` に書いてあります',
  },
  'seed-world.mjs': {
    // 🔴 ★2026-09-20: 1 → 2（★`truncate table nicks` を足したため・`POOL-SUPPLY` ①）
    sites: 2,
    records: '⚠️ ★**残しません（残せません）。** ★`horses` の全件 削除（★条件なし）は '
      + '★**世界そのものを作り直す**操作で、★片付けではありません。'
      + '★`tool-aftermath` の `notRestored` に「世界そのもの。前の世界は戻らない」と書いてある。'
      + '--- 🔴 ★**2026-09-20: ★1 → 2 に増えました（★子の表も消すため）** ---'
      + '✔ ★**理由**: ★`horses` を止める形で参照している外部キーが在り、'
      + '★**1 文では外部キー違反で落ちます**（★実測・`tools/diag-horses-refs.mjs`）:'
      + '★本番 `race_entries` **79,746 行** ／ staging はさらに `horse_story_event` 77・'
      + '`horse_market_listing` 15。★`on delete cascade` は `horse_week_log` の 1 本だけ。'
      + '→ ★**子から順に、★1 つの取引で**消します。'
      + '🔴 ★**消す表を手で書いていません** — ★`pg_constraint` から毎回 引いて順を作ります。'
      + '★**手で書くと、★表が増えた日に黙って古くなります**（★D-052。'
      + '★実際 移行 33 件 で参照する表が 2 つ 増えました）。'
      + '→ ★**2 件目の `delete` は表名が変数**（`${t}`）です。★何を消したかは ★**端末に 1 行ずつ出します**'
      + '（★表名と行数）。⚠️ ★**それ以上は残せません** — ★作り直しなので、★消したものは戻りません。'
      + '⚠️ 🔴 ★**`cascade` を足す案は採りませんでした**: ★「消しやすくするため」に製品の制約を緩めると、'
      + '★将来 馬を 1 頭 消したときに ★**その馬のレース記録が黙って消えます**。'
      + '--- 🔴 ★**2026-09-20: ★`delete` から `truncate` に変えました（★2 件 → 1 件）** ---'
      + '✔ ★**理由は実測**（★staging の演習 1 回目・`evidence/20260920-rehearsal/run1-failed.log`）: '
      + '★子の表は速く消えた（★`race_odds` 162,568 行 が **1.6 秒**）が、'
      + '★🔴 ★**`delete from horses` が時間切れ**（★57014）。'
      + '★落ちた場所は ★**自己参照の外部キー検査**（`… WHERE $1 = sire_id FOR KEY SHARE`）。'
      + '✔ ★原因: ★`horses` の索引は 4 つだけで、★**`sire_id` にも `dam_id` にも索引が無い**。'
      + '→ ★7,370 行を消すあいだ ★**1 行ごとに 2 回の全表走査** ≒ ★**約 1 億回**。'
      + '🔴 ★**私の見立てが外れていました**: ★「自己参照は 1 文の delete で解ける」と書きましたが、'
      + '★PostgreSQL は ★**削除した行 1 つずつに RI 検査を走らせます**。'
      + '【★なぜ `truncate` でよいか】★**1 行ごとの外部キー検査をしません**（★表ごと空にするため）。'
      + '🔴 ★**`cascade` は付けていません** — ★消す集合は `pg_constraint` から閉じるまで辿ってあるので、'
      + '★**その集合を全部 並べれば足ります**。→ ★★**巻き込みが原理的に起きません。**'
      + '⚠️ ★`truncate` も ★**取引の中で巻き戻せます**（★1 回目の巻き戻りは実測済み: '
      + '★落ちた後も staging は 1 行も違いませんでした）。'
      + '⚠️ 🔴 ★**索引を足す案は採っていません**（★こちらの判断ではないため）。★別に起票しました。'
      + '--- 🔴 ★**演習 2 回目も落ちました（★機構を変えたら、効く規則も変わっていた）** ---'
      + '★`cannot truncate a table referenced in a foreign key constraint`'
      + '（★`horse_week_log` が `horses` を参照）。'
      + '★消す集合の作り方は ★**`delete` の意味**に合わせて「cascade / set null は DB が面倒を見るので'
      + '★辺を外す」と書いてありました。★**delete としては正しい**。'
      + '🔴 ★しかし ★**`truncate` は、★参照している表が在るだけで拒みます**（★削除時の動作に関係なく）。'
      + '→ ★★**同じ「消す」でも、★`delete` と `truncate` では集合が違う。**'
      + '✅ ★直し: ★`truncate` のときは ★**参照している表を 1 つ残らず**集合に入れる。'
      + '★集合は **8 表**になりました（★`horse_week_log` が加わった）。'
      + '⚠️ ★**`set null` の辺には警告を出します** — ★`delete` なら列が null になるだけですが、'
      + '★`truncate` は ★**行ごと消えます**（★いまは 0 件。★増えた日に気づけるように）。'
      + '✅ ★`truncate` のために足した辺は ★**出力に出します**（★違いが見えるように）'
      + '--- ★**2026-09-20: ★2 件目は `truncate table nicks`**（★`POOL-SUPPLY` ①） ---'
      + '★配合の相性表を ★**プリシードと同じものに入れ替えます**（★発明ではなく転記）。'
      + '⚠️ ★世界を作り直す便でしか呼ばれません。★**残しません** — ★表は種（`source_seed`）から'
      + '★いつでも作り直せるからです（★消えても失われる情報が無い）。'
      + '🔴 ★ただし ★**運営が §6.6 のとおり手で編集していた場合、★その編集は消えます**。'
      + '★★作り直しは世界ごと入れ替える操作なので、★相性表もその世界のものに戻ります',
  },
  'verify-a7.mjs': {
    sites: 2,
    records: '✅ ★`delete from app_environment`（★条件なし）。★**A-7 の門が働くことを確かめるため**に'
      + '★わざと消す。★消す前に `tmp/snapshots/verify-a7.json` へ控え、★戻す（★**SB-6**・`3bf1802`）。'
      + '🔴 ★これは「記録」ではなく「復元」が要る側（★失うと道具が 1 本も動かなくなる）',
  },
};
