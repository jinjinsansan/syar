/**
 * ★**登録簿の登録簿** — ★どの簿が「何を走査して、何と突き合わせるか」（★**CK-17**・2026-09-20）。
 *
 * 【🔴 ★なぜ在るか】
 *   ★2026-09-20、★`staleness` だけが ★**「増えたら落ちる」も「減ったら落ちる」も持って
 *   ★いませんでした**。★レビュー側は「★新しい簿だから無防備なのだ」と説明しましたが、
 *   ✔ ★**実測すると外れます**: ★同じ日に作った `printed-defaults` と `write-never` は
 *     ★**最初から両側 在りました**（★今日 作った 3 つのうち 2 つ）。
 *
 *   ✅ ★**1 つだけ違ったのは、★「外を走査する目」の有無**でした。
 *     ★両側 在る簿は、★どれも ★**走査した集合と登録簿を突き合わせて**います。
 *     ★`staleness` は ★**人が手で流す確認**を並べた簿で、★母集団が機械から見えず、
 *     ★★**誰も走査を書きませんでした。**
 *
 *   🔴 ★★**走査の無い登録簿には、★「増えた／減った」を言う根拠が そもそも無い。**
 *
 * 【★だから、ここで先に決めさせます】
 *   ★新しい簿を足すと ★`registries.test.ts` が落ちます。
 *   → ★★**`scans`（何を走査するか）と `compares`（何と突き合わせるか）を書くまで進めません。**
 *   ⚠️ ★走査できないなら、★**代わりに何と突き合わせるかを書く**
 *     （★`staleness` なら ★記録ファイルの実在 ＝ **孤児検出**。★それがここの書き方の見本です）。
 *
 * 【🔴 ★**R-29′ — ★渡された一覧を、★自分の母集団にしない**】
 *   ★2026-09-20、★この走査を ★**2 回 数え間違えました**。★どちらも同じ形です:
 *     ★① ★レビュー側が挙げた ★**8 つの簿**をそのまま母集団にした → ★実際は **9 つ**
 *        （★`exposure-registry` が抜けていた。★自分で `git ls-files tools/lib` を走査して気づいた）
 *     ★② ★こちらが「★**6 つとも同じ穴**」と渡し、★レビュー側がそれを運んだ → ★実際は **1 つ**
 *   🔴 ★★**一覧を渡した人は、★その人の目的のために作っています。★こちらの目的のためではありません。**
 *   → ★★**自分で走査して数える。** ★`R-29`（★列挙は必ず漏れる）は ★**受け取る側にも掛かります。**
 *   ⚠️ ★だからこの簿は ★**`git ls-files tools/lib` を走査して**突き合わせます（★手で並べません）。
 *
 * 【⚠️ ★部品には 2 種類あります】
 *   ★**母集団を持つ簿**（ここ `REGISTRIES`）… ★走査と突き合わせが要る
 *   ★**母集団を持たない道具**（`NOT_A_REGISTRY`）… ★要らない。★**理由を書く**
 */

/**
 * ★母集団を持つ登録簿。
 * ★`scans` … ★**外の何を走査するか**（★ここが空の簿は、落ちる根拠を持ちません）
 * ★`compares` … ★走査した集合と ★**何をどう突き合わせるか**
 */
export const REGISTRIES = {
  'broad-deletes.mjs': {
    scans: '★`tools/*.mjs` の原文（★`delete from …` と `truncate …`）',
    compares: '★道具名（★未登録は落ちる／★ゴーストも落ちる）と ★箇所の数（★増えても減っても落ちる）',
  },
  'classification.mjs': {
    scans: '★`git ls-files tools` の `*.mjs`',
    compares: '★道具名（★分類漏れ／★版管理に無いゴースト の両側）',
  },
  'exposure-registry.mjs': {
    scans: '★DB のテーブルとビューの一覧',
    compares: '★名前と区分（★登録簿に無い表／★DB から消えたのに残っている登録 の両側）',
  },
  'known-red.mjs': {
    scans: '★`vitest` の結果（★落ちている検査の一覧）',
    compares: '★赤の名前（★未登録の赤／★緑に戻ったのに残る登録 の両側）',
  },
  'open-findings.mjs': {
    scans: '★原文（★`grepCount`）と ★各指摘の `stillOpen`',
    compares: '★指摘 id（★書き漏れ・★重複・★`stillOpen` が偽になったら落ちる）',
  },
  'printed-defaults.mjs': {
    scans: '★原文（★既定値をそのまま印字している箇所）',
    compares: '★ファイル名と ★箇所の数（★`!==` なので増減の両側）',
  },
  'staleness.mjs': {
    // 🔴 ★ここだけ「走査が無い」。★だから代わりのものを書いてあります
    scans: '🔴 ★**外の走査はありません**（★人が手で流す確認の簿なので、★母集団が機械から見えない）',
    compares: '★代わりに ★**`evidence/<what>/last-check.json` の実在**（★孤児＝記録だけ残る）と'
      + '★**項目数の下限**（★黙って減らない）。★記録が 0 件なら「空回り」として落とす対照つき',
  },
  'tool-aftermath.mjs': {
    scans: '★`classification.mjs` の `STATE_CHANGING` の一覧',
    compares: '★道具名（★後始末の書き漏れ／★ゴースト の両側）',
  },
  'write-never.mjs': {
    scans: '★`db/migrations/*.sql` の列と、★それを書く原文',
    compares: '★表ごとの列名（★新しく「書く側が無い」列／★繋がったのに残る登録 の両側）',
  },
};

/**
 * ★母集団を持たない部品。★**走査も突き合わせも要りません。★理由を書きます。**
 */
export const NOT_A_REGISTRY = {
  'args.mjs': '★引数の読み取り。★登録するものがない',
  'cdp.mjs': '★ブラウザの探索。★登録するものがない',
  'counted-verdict.mjs': '★合否の数え方（★0/1/2）。★母集団を持たない',
  'name-reset.mjs': '★馬名を戻す部品（★新しい名前を選ぶ・読む・書く）。★母集団を持たない（★1 頭ずつ・★母集団は DB の `horses` の側）',
  'name-recheck.mjs': '★未検査の馬名を禁止名の判定で分ける純関数（★PLAN I-3）。★母集団を持たない（★母集団は DB の `horses` の側）',
  'name-key-survey.mjs': '★馬名の正規化キーの下見（★渡された行を数える純関数・PLAN I-3 段 2）。★母集団を持たない'
    + '（★母集団は DB の `horses` の側）',
  'clean-tree.mjs': '★`git status` の出力を読んで合否を返す純関数。★母集団を持ちません'
    + '（★母集団を持っているのは git の側です）。'
    + '⚠️ ★ただし **「空」を合格と読まない**ための対照は持っています '
    + '（★`git ls-files` の数が 0 なら判定不能・★CK-14）',
  'dress.mjs': '★毛色などの定数表。★「登録漏れ」という概念がない（★全部そこに在る）',
  'env.mjs': '★接続先の読み取り。★登録するものがない',
  'guard.mjs': '★本番かどうかの門。★登録するものがない',
  'leftovers.mjs': '★取り残しの数え上げ。★数える側であって簿ではない',
  'pixel-font.mjs': '★字形の定数。★母集団を持たない',
  'provenance.mjs': '★出どころ（ハッシュと時刻）の計算。★母集団を持たない',
  'race-audit-build.mjs': '★監査用の尺の定数。★母集団を持たない',
  'registries.mjs': '★この簿そのもの（★自分を数えない）',
  'sandbox-tx.mjs': '★取引の入れ物。★母集団を持たない',
  'snapshot-file.mjs': '★控えの読み書き。★母集団を持たない',
  'tool-restores.mjs': '★3 つの道具が戻す値の定数。★走査する母集団ではなく、'
    + '★`tool-restores-{a7,g6,prize}.test.ts` が道具ごとに突き合わせている',
  'v18.mjs': '★V-18 の定数。★母集団を持たない',
};
