/**
 * ★**道具が、★旗で変えられる値を「定数のまま印刷」していないか**の登録簿（★**CK-12**・2026-09-20）
 *
 * 【🔴 ★なぜ在るか】
 *   ★道具が実行条件を印刷するとき、★**既定値を書き写す**と、★旗で変えても表示が変わりません。
 *   → ★★**出力ファイルを後から読む人が、★条件を取り違えます。**
 *   ✔ ★実例（★2026-09-20 に 3 件 直した）:
 *     ★`apps/cli/src/verify-race.ts` … ★素質開放率を `0.55〜0.85` と直書き（★`--unlock-min/max` で変わらない）
 *     ★`tools/measure-silk-budget.mjs` … ★`32×32` と直書き（★`--size 64` でも「32×32」と出る）
 *     ★`tools/bake-oblique-sheet.mjs` … ★`8コマ` と直書き（★`--frames 6` でも「8コマ」と出る）
 *     ★`apps/cli/src/market-price-distribution.ts` … ★`「4 勝以上」`（★`--open-min 6` でも「4 勝以上」）
 *
 * 【⚠️ ★なぜ「検出」ではなく「登録簿」なのか】
 *   ★`broad-deletes.mjs` と同じ理由です。★**網の精度は上がりきりません。**
 *   ✔ ★実測（2026-09-20・★網を 2 回 書き直した）: ★精度 ★**16 件中 3 件（19%）→ 7 件中 3 件（43%）**。
 *   → ★★**「嘘かどうか」を機械に判定させると、★網のほうが壊れます。**
 *   → ★**形だけを機械が拾い、★嘘かどうかは人がここに書く。**
 *
 * 【★機械が拾う「形」】
 *   ★① 旗から取る**数の既定値**（`arg('x', 30)` / `process.argv.indexOf('--x')` の三項）
 *   ★② その数が、★**印刷する行の文字列に素で出ている**（★`${…}` の中は除く）
 *   ★除くもの: ★`使い方` / `Usage:` / `[--`（★**そこに既定値を書くのは正しい**）／
 *            ★規則の名前（`R-30`・`PO-6`）／★絶対値 2 以下（★偶然が多すぎる）
 *
 * 【🔴 ★この網が見つけられないもの（★正直に書きます）】
 *   ★① ★**桁を落とす嘘**（★CK-12 の 2 件目 — ★`206.711` と `206.684` を どちらも `206.7` と出す）。
 *      ★★**書けませんでした。** ★「その判定を支えられる桁か」は、★印刷する値と判定の関係を
 *      ★静的に辿る必要があり、★**網のほうが先に壊れます**（★上の 19% がその証拠）。
 *      → ★**規則としては残す。★機械では捕まえない。**
 *   ★② ★**註記の中の書き写し**（★印刷しないので実害が小さく、★数が多すぎる）
 *   ★③ ★**定数から来る値**（★旗で変えられないので、この網の対象外）
 *   ⚠️ ★**「0 件」を「無い」と読まないこと**（★**R-21**）。★この網は①だけを見ています。
 *
 * 【★新しく当たったら】
 *   ★検査が落ちます。★**直すか、★ここに「なぜ嘘ではないか」を書いてください。**
 */

/**
 * ★ファイルごとの、★**機械が拾った箇所**。
 *
 * ★`sites` … ★件数（★増えたら検査が落ちます）
 * ★`why`   … ★**なぜ嘘ではないか**（★嘘なら直すこと。★ここに書いて済ませない）
 */
export const PRINTED_DEFAULTS = {
  'bake-oblique-sheet.mjs': {
    sites: 1,
    why: '✅ ★**嘘ではありません。** ★当たっているのは `${FRAMES}コマ × **8枠色**` の後ろの `8` で、'
      + '★**枠色の数**です（★`ch * 8` で使う固定値。★旗にしていません）。'
      + '⚠️ ★前の `8コマ` のほうは ★**本当に嘘でした** — ★2026-09-20 に `${FRAMES}` へ直しました。'
      + '★網は「同じ数が近くに 2 つ在る」ことまでは見分けられません',
  },
  'bench-odds-cost.mjs': {
    sites: 1,
    why: '✅ ★**嘘ではありません。** ★`start /affinity 3` の `3` は ★**Windows の CPU 親和マスク**で、'
      + '★`--repeat 3` とは無関係です（★偶然 同じ数）',
  },
  'diag-gauge.mjs': {
    sites: 1,
    why: '✅ ★**嘘ではありません。** ★`残り400〜900m` の `400` は ★**C-6 の判断の窓**（★距離）で、'
      + '★`--races 400` とは無関係です（★偶然 同じ数）',
  },
  'measure-screen-direction.mjs': {
    sites: 2,
    why: '✅ ★**嘘ではありません。** ★`上位5頭` の `5` は ★**数える頭数**で、'
      + '★`--from 5`（★開始秒）とは無関係です（★偶然 同じ数）',
  },
};
