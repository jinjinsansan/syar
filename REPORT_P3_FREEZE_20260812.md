# REPORT_P3_FREEZE_20260812 — 出走馬の凍結／Q-P3-42 の実測／worktree の是正

## 本便で正典の改訂が要る決定をしたか

**していません。**

---

## 0. ★まず、この便で自分が壊したものを先に書きます

| # | やったこと | 結果 |
|---|---|---|
| 1 | worktree の `node_modules` にジャンクションを張り、`git worktree remove --force` した | ★**本体の追跡ファイル178件と `node_modules` を削除** |
| 2 | 実行中の worktree から `node_modules` リンクを外した | ★**60,000本の測定を自分で中断させた**（3/7件で停止） |

★**どちらも「隔離のために入れた道具」で起きました。**
1は保護対象を壊し、2は保護している最中の測定を壊しました。

★1が全復旧できたのは**こまめにコミットしていたから**です。隔離より先に効いた保護はそれでした。

---

## 1. ★出走馬の凍結（0016）— 構造で守る

```
現在: 生成 → horses を読む / 確定 → horses を**もう一度**読む → 2回読むから食い違う
是正: 生成 → 使った RaceEntrant を race_entries.entrant_snapshot に凍結
      確定 → 凍結を使う（読むのは1回だけ）→ ★食い違いが原理的に起きない
```

### ★指定より深い問題でした

**調子・疲労だけでは足りません。** 確定側は `horses` と結合して
**能力・適性・スキル遺伝子まで**読み直しています。しかも生成は**2周先**なので、
**間に週が進めば能力（`stats`）そのものがずれます。**

★`§10.2` の「障害時バッファとして2レース先行生成」は P2 の決定で、
**育成ループが入った瞬間に「オッズと着順で馬が別人になる経路」に変わりました。**
→ 凍結したのは `RaceEntrant` 一式（13項目）です。

### 実測（staging）— ★対照を先に置きました

```
  ✓ ① 凍結が全馬に入っている                             15/15 頭
  ✓ ② 凍結が RaceEntrant 一式を持っている                 13 項目すべて
  ✓ ★対照: 壊した値で走らせれば着順は変わる（＝③が空振りでない）
  ✓ ★③ horses を壊しても凍結どおりの着順になる            完全一致
  ✓ ④ 壊した horses を1頭ずつ元に戻せている               15/15 頭
```

★測っているのは「**食い違いが無い**」ではなく「**食い違いが起こりえない**」です。
凍結が無い馬は数えて `console.error` に出します（黙って昔の経路に落ちない）。

### 途中で踏んだもの

- 確定を2回やる設計 → `settleRace` が内部で `begin`/`commit` するので savepoint が消える（25P01）
  → **確定は1回**にして「凍結からエンジンを回した着順」と突き合わせる形に変更
- 壊し方「全馬の能力を10」→ **値域外で `marginLabel` が NaN**
  → **出走馬どうしで能力を入れ替える**形に変更（値域内のまま着順は必ず変わる）

---

## 2. ★Q-P3-42 — 実測が推論を否定しました

### 前便で私が書いた「どのゲートも捕まえないものは 0 件」は**誤り**でした

| 定数 | 統計量の動き | ★ゲートが捕まえたか |
|---|---|---|
| `DEFAULT_CLASS_BAND` | V-4 31.29% → 34.82% | ✅ **V-4 が捕まえた** |
| `TAIL_MIX_P_DEFAULT` | V-6 1.21% → 0.59%（**51%**） | ★**捕まえない**（帯の中） |
| `TAIL_MIX_M_DEFAULT` | V-6 1.21% → 0.59%（**51%**） | ★**捕まえない**（帯の中） |
| `DISTANCE_SUIT_MIN` | V-6 1.21% → 1.18%（2%） | ★**捕まえない** |
| `OFF_DISTANCE_ENTRY_RATE` | V-6 1.21% → 1.16%（4%） | ★**捕まえない** |
| `OFF_SURFACE_ENTRY_RATE` | V-4 31.29% → **31.29%**（0%） | ★**捕まえない** |
| `FIELD_STRENGTH_FLOOR` | V-4 31.29% → 32.81%（6%） | ★**捕まえない** |

生出力: `docs/MEASURE_Q42_FULL.txt`（60,000本 × 4シード・**単一実行**・`6bdfb8f`）

★**7件中6件**が、登録簿の主張どおりには捕まりませんでした。
経路で読まれていること（参照数）は確認済みでしたが、**それも十分条件ではありません**でした。
読まれていて、効いていて、なお**ゲートは落ちません**。

### 是正 — 機構の直接の性質を検査する（裁定どおり）

| ファイル | 検査している機構 |
|---|---|
| `packages/race-engine/test/tail-mixture.test.ts` | 広い成分から引かれる割合が `TAIL_MIX_P` に一致する／幅が `TAIL_MIX_M` 倍である／裾が単一正規より厚い／★分岐しても乱数の消費数が変わらない |
| `apps/cli/test/field-mechanisms.test.ts` | 床を下回る馬が出走表に残らない／距離・馬場が向かない馬の割合が設定値に見合う／距離適性の下限が足切りとして働いている |

**6件すべて「壊すと落ちる」ことを終了コードで実測**しました。テスト 622 → **630件**、全緑。

★作業中に自分の誤りを1つ: **距離適性の式を推測で書いて**落ちました。実装は §5.2 の
ガウス曲線で、私が書いたのは線形減衰でした。**推測の式で測ると、落ちても通っても意味がありません。**

### ★測り方 — 前便の記述を訂正します

前便で「縮小してよいのは**効いているか**を見るときだけ」と書きました。**それも誤りでした。**

| 定数 | 4,000本 | ★60,000本 × 4シード |
|---|---|---|
| `DISTANCE_SUIT_MIN` | 15% 動く | **2%**（ほとんど動かない） |
| `OFF_SURFACE_ENTRY_RATE` | 8% 動く | **0%**（V-4 が小数第2位まで同じ） |
| `FIELD_STRENGTH_FLOOR` | 2,000本では **V-4 が捕まえた** | **捕まえない** |

★**標本を減らすと「動いて見える」**（ばらつきが動きに化ける）。
→ **「効いているか」も「捕まえるか」も、ゲートと同じ本数でしか答えられません。**

★そして出走表側の4件は、本来の本数では**下流の統計量をほとんど動かしません**。
`OFF_SURFACE_ENTRY_RATE` に至っては **V-4 が小数第2位まで同じ**です。
**V-4 / V-6 は、これらの定数が壊れたことをまったく見ていません。**

---

## 3. worktree — ★辿られる経路そのものを無くしました

| | 前 | 後 |
|---|---|---|
| 場所 | tmpdir | **repo 直下 `.mutation-wt/`** |
| `node_modules` | ジャンクション | ★**張らない**（Node の解決が親を辿る） |
| 片付け | `remove --force` | ★**`--force` を使わない**（消せなければ残す） |

★**リンクを作らないので、辿られようがありません。**

そして早速、`--force` を外した効果が出ました — 古い作業ツリーの削除を **git が拒否**しました
（実行中のジョブが使用中だったため）。**以前なら `--force` で消していました。**

⚠️ ただし §0 の2件目のとおり、**私はその拒否を迂回してリンクだけ外し、測定を壊しました。**
道具が止めたのに、人が回り込んでいます。

### ★R-24 の拡張を、現在の構造すべてに当てました

> 「構造で守る」は、危険な操作が**不可能**になったときにだけ成立する。
> 導入したら**「これを迂回する最短経路は何か」**を問う。

| 構造 | 迂回の最短経路 | 判定 |
|---|---|---|
| worktree（repo 直下・リンク無し） | **外すリンクが存在しない** | ✅ **不可能** |
| `--schemacheck` | `deploy.sh` を使わず手で `ln -sfn` する | ★拒否（迂回可） |
| `assertNotProduction` | ツールを使わず `psql` で直接書く | ★拒否（迂回可） |
| `deploy.sh` の切り離し | `STAR_DEPLOY_DETACHED=1` を付けて前景で走らせる | ★拒否（迂回可） |
| **凍結（0016）** | `settleRace` に「一部だけ最新値で上書き」を足す | ★**拒否ですらない**（下記） |

★**私が「構造で守った」と報告してきたもののうち、不可能になっているのは1つだけ**でした。

### ★凍結について、前便の報告を訂正します

前便で「凍結は不可能に寄せられる」と書きましたが、**現在の実装は「拒否」ですらありません。**
`settleRace` は `horses` と結合したままで、凍結が無ければ**警告を出して昔の経路に落ちます**。
止めていないので、**迂回する必要すらない**状態です。

★これは移行期のための意図的な設計です（0016 より前のレースが残っているため）。
ただし**その猶予が消えた後も同じ形なら、それは「構造」ではありません。**
→ **Q-P3-43** として照会します（現在の指示の範囲外なので着手していません）。

---

## 4. 再現

```bash
npx tsx tools/verify-entrant-freeze.mjs --env staging   # ★凍結（③が本題）
node tools/mutation/gates.mjs                           # ★Q-P3-42（本来の本数・約90分）
npm test                                                # 630件
```

---

## 5. 照会・未回答

| # | 内容 |
|---|---|
| **Q-P3-43（新規）** | 移行期が終わったあと、**確定経路から `horses` を参照できなくする**か（凍結が無ければ確定しない／型で `horses` を渡せなくする）。R-24 拡張の「不可能にする」形にするかの裁定 |
| Q-P3-33 / 20〜31 / 38 / 39 | 未回答 |

---

## 6. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
2. 本便で**本体の作業ツリーを一度壊しました**（全復旧済み）。
   復旧できたのは**こまめにコミットしていたから**です
