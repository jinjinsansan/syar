# DEV INSTRUCTIONS — P4「2D 馬群描画の限界テスト」

日付: 2026-08-22  
発行: レビュー側 Codex  
対象ブランチ: `p4/race-30sec-cuts`  
優先度: 最優先（現在進行中のフルレース演出調整を一時停止する）

---

## 0. この指示の目的

4日以上にわたり、カメラ、背景、芝の占有率、HUD、カット構成などを調整しているが、参考映像に近づいたという判断に至っていない。

ここで推測によるフルレース修正を止め、現在の2Dスプライト方式が、参考映像の中心的な魅力である「密集した馬群が高速で走る画」を成立させられるかだけを、隔離された短い検証で判定する。

このタスクは完成版レースを作る作業ではない。

**唯一の目的は、現行2D方式を継続する価値があるか、3D方式へ移行すべきかを判断できる証拠を作ることである。**

---

## 1. 正典・権利・安全上の制約

1. `STAR_SPEC_v2.0.md` と `CLAUDE.md` の憲法を守ること。
2. 参考映像に映る実在の馬名、レース名、競馬場名、人物名、他社固有名称を、新規コード、変数、コメント、出力画像、動画、テストデータへ転記しないこと。
3. 参考映像は画面構成と動きの観察だけに用いる。
4. レース結果、乱数、公正性、ポイント、DB、認証、workerには触れない。
5. `.env`、`secrets.*.env`、`git.txt`を読まない。DBや外部サービスへ接続しない。
6. リモートへのpush、デプロイ、外部公開をしない。

---

## 2. 今回検証する仮説

### H1: 馬群の密度

参考映像は上位馬が短い奥行きの中で重なり、一つの動く塊として見える。現状は馬同士の前後間隔が大きく、個別の切り抜きを順に置いたように見える。

### H2: 個体ごとの運動差

参考映像は首、胴、脚、騎手の姿勢と走行位相が馬ごとに異なる。現状は少数フレームの反復により、馬群全体が同じ動きをしている印象が出る可能性がある。

### H3: 遮蔽と奥行き

参考映像は手前の馬が奥の馬を自然に隠し、前後関係が連続的に変化する。現状は各馬を読みやすく配置する処理や描画順により、馬が互いに分離して見える可能性がある。

### H4: 素材と背景の統合

現状は馬素材だけが背景より鮮明に見え、輪郭、色、影、解像感が背景と一致していない可能性がある。背景追加ではなく、馬と世界が同じカメラで撮られたように見える処理が必要かを調べる。

### H5: 方式そのものの限界

上記を改善しても、正面寄り・斜め・旋回時に側面の2D素材を貼った印象が消えないなら、現行方式では目標に届かない。その場合は失敗ではなく、3D移行を判断するための有効な結果とする。

---

## 3. 対象ショット

参考動画:

`ダービースタリオン/参考映像スターホース版.mp4`

主対象は、動画内のおおむね **50.0秒〜58.0秒**に見える、馬群を真横から追う区間とする。

ただし、実機をスマートフォンで撮影した映像であり、画面外枠、反射、撮影者由来の表示が含まれる。比較時は競馬映像が表示されている上画面だけを対象とし、撮影環境由来の黒枠、反射、外部オーバーレイを再現しない。

比較対象として最低限、次の時刻を切り出すこと。

- 50.0秒
- 52.0秒
- 54.0秒
- 56.0秒
- 58.0秒

切り出し位置は実映像を確認して最大±0.5秒の調整を許す。調整した場合は報告書へ実際の時刻を書くこと。

---

## 4. 実装スコープ

通常の `/race` ページや完成版のカット台本を直接調整してはならない。

既存の描画関数を再利用した、独立した検証用レンダリング経路を作ること。次のいずれかを選んでよい。

- `tools/` 配下の専用レンダリングスクリプト
- 既存ツールに明確に隔離された検証モード
- 開発専用ページ。ただし自動フレーム生成が困難になるため、原則としてツールを推奨する

推奨する新規入口名:

```text
tools/render-2d-pack-limit.mjs
```

既存コードを変更する場合も、通常レースの既定値、着順、進行、既存出力を変えないこと。検証専用オプションまたは純粋関数として分離する。

---

## 5. 固定条件

比較条件が動くと判断不能になるため、以下を固定する。

- 出力解像度: 1920×1080
- 出力時間: 8.0秒
- フレームレート: 30fps
- 馬数: 12頭
- 路面: 芝
- 天候と光: 1種類に固定
- 乱数シード: 固定値1つ
- カメラ: 真横追従を基本とする
- カット切替: なし
- HUD: なし
- 実況字幕: なし
- 隊列バー: なし
- 馬名プレート: なし
- 自馬マーカー: なし
- タイトルカード、ロゴワイプ、発馬機、ゴール演出: なし
- レース結果や勝敗の演出: 不要

このテストでは、背景装飾ではなく馬群の成立性を見る。新しいスタンド、掲示板、内馬場素材などを制作しない。

---

## 6. 比較するバリアント

同一条件から、最低でも以下の4バリアントを生成する。

### V0 — 現状相当の対照群

現行ロジックをできるだけそのまま使う。専用テストを作るために必要な変更以外は加えない。

目的: 改善前後を同じ生成経路で比較する基準。

### V1 — 密集だけ

V0から馬群の前後間隔だけを圧縮する。

- 上位8頭の先頭から最後尾までの距離を、おおむね5〜7mへ収める
- 横方向は全頭を一直線に重ねず、2〜4列程度の自然なばらつきを持たせる
- 馬同士が物理的に完全に同一点へ重ならないこと
- 元のレース結果や通常の位置データを書き換えず、検証用の表示座標変換として実装してよい

目的: 27.7m対5〜7mという差が、見た目の主因かを単独で調べる。

### V2 — 密集＋個別位相＋遮蔽

V1に以下を追加する。

- 各馬の走行位相を固定シードでずらす
- 位相差はゲート番号の単純な一定刻みだけにせず、同じ見た目が周期的に並ばないようにする
- 走行周波数には小さな個体差を与えてよいが、平均速度や着順は変えない
- 描画順はカメラからの奥行きで毎フレーム決める
- 手前の馬が奥の馬を自然に隠す
- 各馬を必ず見せるための横ずらし、ラベル回避、可読性補正を適用しない

目的: 「個別の馬12頭」ではなく「一つの馬群」に見えるかを調べる。

### V3 — 統合処理込み

V2に、参考の速度感へ寄せる最低限の画作りを加える。

- 馬と背景の相対運動に基づく方向性のあるブラー
- 背景の軽い被写界深度表現
- 馬の輪郭だけが過剰に鋭くならない統合処理
- 馬と地面で光の方向を一致させる
- 接地影を各馬の足元へ置き、空中に浮いて見えないようにする
- 色調を全画面で統一する

重要:

- 画面全体へ一様なぼかしを掛けて誤魔化さない
- 馬の形が判別不能になるほどブラーを強くしない
- 被写体ブラーを複数枚の残像として見せない
- 処理前後を比較できるよう、各効果をコード上で個別に無効化できること

---

## 7. 実装上の確認事項

実装前に、少なくとも次をコードから確認し、報告書に事実を書くこと。

1. 現在使われている馬素材の方向数
2. 各方向のアニメーションフレーム数
3. 真横、斜め前、正面、斜め後ろで、どの素材へ切り替わるか
4. `phaseOf` と `frameOf` が実際に何を入力としているか
5. 馬ごとの位相差が、時間経過後も維持されるか
6. 描画順が何の座標で決まるか
7. 馬の接地点と影の基準点が一致しているか
8. カメラのFOVまたは等価な拡大率
9. 馬群を追う基準が、先頭、上位群平均、全頭平均のどれか
10. 現行の被写体ブラーが、背景ではなく馬の相対運動を反映しているか

既存コメントを事実として転載せず、実際に呼ばれるコード経路を追うこと。

---

## 8. 成果物

以下をすべて `out/2d-pack-limit/` に生成する。

```text
out/2d-pack-limit/
  v0-current.mp4
  v1-dense.mp4
  v2-pack.mp4
  v3-integrated.mp4
  v0-contact-sheet.png
  v1-contact-sheet.png
  v2-contact-sheet.png
  v3-contact-sheet.png
  compare-contact-sheet.png
  reference-contact-sheet.png
  measurements.json
  README.txt
```

### 各コンタクトシート

- 0秒、2秒、4秒、6秒、8秒相当の5枚
- 同一サイズ、同一配置
- フレームにバリアント名と時刻だけを表示
- 評価を誘導する説明文を画像内に書かない

### `compare-contact-sheet.png`

列を時刻、行を `REFERENCE / V0 / V1 / V2 / V3` とする。

参考映像は実機撮影のため厳密な同一時刻対応ではない。動きの段階が近いフレームを選び、選択時刻を`measurements.json`へ記録する。

### `measurements.json`

最低限、次を機械可読に記録する。

```json
{
  "reference_times_sec": [],
  "output_times_sec": [0, 2, 4, 6, 8],
  "resolution": [1920, 1080],
  "fps": 30,
  "seed": "固定シード",
  "variants": {
    "v0": {},
    "v1": {},
    "v2": {},
    "v3": {}
  }
}
```

各バリアントには次を含める。

- 上位8頭の先頭から最後尾までのメートル差
- 横方向の最大幅
- 使用した方向別素材の種類
- 1頭あたりの使用フレーム数
- 位相オフセットの一覧
- カメラ設定
- ブラー設定
- 生成コマンド

### `README.txt`

第三者が同じ成果物を再生成できる、1本または少数のコマンドを書く。

---

## 9. テスト

検証用コードにも最低限の自動テストを置く。

必須項目:

1. 同一シードと同一時刻で座標・位相が一致する
2. V0と既存経路の座標または描画入力が意図せず変わっていない
3. V1〜V3の上位8頭スパンが設定した5〜7mに入る
4. 位相オフセットが12頭すべて同一ではない
5. 奥行き順を入れ替えると描画順も入れ替わる
6. 通常レースの既存既定値へ検証用設定が漏れない

完了時に以下を実行する。

```powershell
npm test
npm run typecheck
```

全テストが長時間かかる場合でも、まず関連テストを実行し、最後に全体を実行する。失敗を既存不具合として処理する場合は、変更前にも同じ失敗が再現する証拠を示す。

---

## 10. 合否判定

開発側は最終的なPASS/FAILを決めない。成果物と事実を提出し、レビュー側が判定する。

レビュー側は主に次を目視する。

### A. 切り抜き感

- 馬が背景に貼られた個別画像に見えないか
- 馬群全体が一つの空間に存在して見えるか

### B. 馬群感

- 前後の馬が自然に重なるか
- 密集していても、単なる画像の重なりではなく奥行きに見えるか
- 馬群内で位置関係が連続的に変化するか

### C. 運動

- 12頭が同期して機械的に動いて見えないか
- 首、脚、騎手の動きに個体差を感じられるか
- 速度感がありながら馬体を認識できるか

### D. カメラと素材の整合

- 真横カメラでは成立しているか
- 少し斜めになっただけで、側面素材を貼った印象が露呈しないか
- カメラが馬個体ではなく馬群を撮っているように見えるか

### 判定区分

#### PASS — 現行2D方式を継続

V3が、少なくとも真横ショットについて、上記A〜Cを満たす。正面・斜め方向については別途素材拡充の見積もりへ進む。

#### CONDITIONAL — 真横だけ2Dで採用

真横は成立するが斜め方向で破綻する。レース演出を真横中心へ限定する案と、方向別素材を増やす案を比較する。

#### FAIL — 3D移行を推奨

V3でも以下のいずれかが残る場合。

- 切り抜きを並べた印象が強い
- 密集させるほど画像同士が不自然に重なる
- 斜め方向で馬体の向きが明らかに合わない
- ブラーで欠点を隠さないと成立しない
- 参考へ近づけるために、方向別素材とフレームを大量追加する必要があり、3D化より安いと説明できない

---

## 11. 禁止事項

この検証中は以下を行わない。

- 通常のフルレース台本の再調整
- 俯瞰、ゴール、勝馬、発馬機など別ショットの改善
- HUDの追加・変更
- 芝占有率を目標にしたカメラ変更
- スタンド、掲示板、タイトルカードなどの素材制作
- コース曲率の変更
- `laneExtraM`、着順、能力、レースバランスの変更
- DBマイグレーション
- Web認証、worker、ポイント処理の変更
- 「良く見えるから」という理由だけの非決定的乱数
- 参考映像の固有名称をコードや成果物へ転記
- 既存の未コミット変更 `apps/web/package.json` のポート変更を戻す、上書きする、コミットへ混ぜる

問題を発見しても、このスコープ外なら実装せず、報告書の「別件」に記録する。

---

## 12. 開発側の報告書

完了時に次を作る。

```text
REPORT_P4_2D_LIMIT_TEST_20260822.md
```

報告書には以下を必ず含める。

1. 変更ファイル一覧
2. 実装した検証経路
3. §7の確認事項に対する、コード位置付きの回答
4. V0〜V3の差
5. 生成コマンド
6. テストコマンドと結果
7. 成果物一覧
8. 観察できた事実
9. 開発側の推測（事実と明確に分離）
10. 現行2D方式を続ける場合に必要な追加素材の方向数・フレーム数の概算
11. 3Dへ移行する場合に再利用できる既存部分と置換対象
12. スコープ外で見つけた問題

「改善した」「迫力が出た」だけでは報告にならない。どのバリアントの、どの時刻の、何がV0と変わったかを書くこと。

---

## 13. 作業の停止条件

次の場合は推測で先へ進まず、`QUESTIONS_P4_2D_LIMIT_TEST_20260822.md`を作って停止する。

1. 参考の50〜58秒に対応する真横区間を特定できない
2. 現行の馬素材では真横ショットすら生成できない
3. V0を作るだけで通常レースの既定動作変更が必要になる
4. 密集表示のためにレース結果や実座標の変更が避けられない
5. 外部購入素材、外部サービス、秘密情報、DB接続が必要になる
6. 既存のユーザー変更と衝突し、保持したまま作業できない

単に実装が難しい、時間がかかるという理由では停止しない。検証目的に必要な最小構成へ削ること。

---

## 14. Claude Codeへ渡す開始メッセージ

以下をこの指示書とともに渡すこと。

> `DEV_INSTRUCTIONS_P4_2D_LIMIT_TEST_20260822.md`を最初から最後まで読み、その範囲だけを実装してください。現在進行中のフルレース演出調整はいったん停止してください。目的は完成版を作ることではなく、現行2D方式を継続できるか判定する比較証拠を作ることです。既存の未コミット変更を保持し、通常レース、レース結果、DB、worker、認証には触れないでください。実装後は指定された動画、コンタクトシート、測定JSON、再生成手順、報告書、テスト結果を提出してください。報告だけで完了とせず、成果物が実際に生成されていることを確認してください。

