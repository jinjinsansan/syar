# 報告: ゲーム本体 第 4 便 — 先天個性・後天特性と騎手の介入補正（★すべて効果 0）

- 日付: 2026-09-16
- branch: `p4/race-30sec-cuts` ／ 開始時 HEAD: `df0cd4d`
- 根拠: 正典 **D-109**（先天個性・後天特性）・**D-110**（騎手は判定窓を AI 代行にも同じ幅で与え、差は「暴走の抑え」で出す）／ 裁定 `REVIEW_CONSULT_HORSE_LIFECYCLE_VERDICT_20260916.md` §2・§3 ／ 引き渡し `REVIEW_HANDOFF_20260916.md` 5
- オーナー指示: 2026-09-16「4 良いです」（＝第 4 便に着手してよい）

---

## 0. 一行でいうと

★**先天個性 4 つ・後天特性 3 つ・騎手の介入補正を「入っているが着順に効かない」状態で入れました。**
★**`resolveRace` と介入の判定を実際に回して、1 ビットも動かないことを示しています**（★定数が 0 なだけを根拠にしていません・D-109 ①）。

---

## 1. 入れたもの

### 1a. 先天個性・後天特性（`packages/sim-engine/src/traits.ts`・新規）

| 区分 | 中身 |
|---|---|
| 先天（4） | 泥巧者 `mud` ／ 負けず嫌い `competitive` ／ 繊細 `delicate` ／ 好スタート `quick-start` |
| 後天（3） | 長距離経験 `long-distance` ／ 大舞台経験 `big-stage` ／ 名コンビ `bonded` |

- **先天は「既存の発現値」から決定論で導きます** — 道悪適性 ≥ 70・根性 ≥ 700・気性 ≥ 70・賢さ ≥ 700（`INNATE_THRESHOLDS`）。
- **後天はレース結果から決定論で数えます**（D-109 ②・★新しい乱数を引きません）— 長距離（2,200m 以上）3 走・重賞 3 走・**同じ騎手** 5 騎乗（`LEARNED_STEPS`）。
- `careerInputOf(runs)` が出走の列から 3 つの回数を数え、`learnedTraitsGained(before, after)` が **新しく身についたぶんだけ**を返します（★物語の行が二重にならない・§18 LR-7）。
- `TRAIT_EFFECT = 0` ／ `traitEffectOf(t)` はどの特性でも 0。

### 1b. 騎手の介入補正（`packages/race-engine/src/intervention.ts`・`packages/scheduler/src/jockeys.ts`）

★裁定 §3-2 の「第三の形」をそのまま実装しました（★開発側の二択はどちらも採っていません）。

| # | 中身 | この便の値 |
|---|---|---|
| ① | **判定窓の広さ**は騎手で広げられる。★**AI 代行にも同じ幅**を渡す | `JOCKEY_WINDOW_EFFECT = 0` → 倍率は常に 1 |
| ② | 騎手の値打ちは ★**暴走の抑え**で出す（気性難の馬の失敗を減らす方向にだけ） | `RUNAWAY_BASE = 0`・`RUNAWAY_PENALTY = 0`・`JOCKEY_CALM_EFFECT = 0` → 期待値は常に 0 |
| ③ | `INTERVENTION_CAP`（±10%）は**動かさない** | `0.1` のまま |
| ④ | 騎手は**出走登録で凍結し、凍結から判定に渡す** | `FrozenJockey.calm` を追加。`resolveIntervention(..., jockey)` が受け取る |

- `InterventionHorse.temper` を**省略可能**で足しました（★暴走の入力。この便では渡しても渡さなくても 1 ビット同じ）。
- `race-engine` は `scheduler` に依存しないので、介入が受け取るのは `{ calm: number }` だけです（★`FrozenJockey` をそのまま渡せる形）。
- 名簿に `calm`（0.2 / 0.4 / 0.6）を置きました。★これは「**どちらが上手いか」の順序の宣言**で、効果が 0 のあいだは着順に入りません。

---

## 2. ★「効いていない」ことの示し方（D-109 ①）

★定数が 0 であることは**宣言**にすぎません。★別の経路で混ぜれば、定数が 0 のままでも結果は動きます。

| 検査 | 何を回したか | 結果 |
|---|---|---|
| `apps/cli/test/trait-no-effect.test.ts` | ★**`resolveRace` を実際に回す**。8・18 頭 × 1200・2400m × 良・**不良** × 6 シード ＝ **48 レース**。★各馬の個性を導いてから同じレースを再判定し、着順・スコア・走破タイムを**文字にして 1 ビット比較** | ✔ 全一致 |
| 同 ★空振り防止 | ★先天が一度も立たない／後天が一度も立たないなら**この検査は何も守っていない**ので、立ったことを主張（R-3） | ✔ 両方立つ |
| 同 ③ | `RaceEntrant` の鍵の一覧を固定（★個性を**受け取る口を足した日に落ちる**錨） | ✔ 14 鍵 |
| `apps/cli/test/jockey-window-no-effect.test.ts` | ★凍結した騎手 6 人 × 気性 3 種 × 入力 60 通りで `resolveIntervention` を回し、**騎手を渡さない場合と 1 ビット比較** | ✔ 全一致 |
| 同 ★V-8 | ★**AI 代行 ÷ 手動最適の比**が騎手で変わらない（★AI にも同じ騎手を渡す・D-110 ①） | ✔ どの騎手でも同値・比は 0.9〜1.0 の内側 |
| 同 ★キャップ | `INTERVENTION_CAP` を動かしていない・±10% の外に出ない | ✔ |
| `packages/sim-engine/test/traits.test.ts` | 境目の**両側**（R-2）・返す順・回数の数え方・乱数と時刻の口が無いこと | ✔ 10 件 |

⚠️ ★**「x − 0 は x」なので丸めの差も出ません** — 暴走の項は `rawMult` から引く形で入れてありますが、値がちょうど 0 なので浮動小数の差も発生しません（★上の 1 ビット比較がそれを実測で示しています）。

---

## 3. ★意図して付け替えた錨（2 件）

★どちらも「凍結に `calm` が増えた」ことで鳴りました。**検査が正しく鳴っています。**

| ファイル | 変更 | なぜ付け替えてよいか |
|---|---|---|
| `packages/scheduler/test/jockeys.test.ts` ②③ | 鍵の一覧と凍結の期待値に `calm` を追加（★0〜1 の範囲も検査に足した） | ★`calm` は**報酬率ではありません**（PP に触れない）。★着順に効かないことは `jockey-window-no-effect.test.ts` が**判定を回して**別に固定します |
| `apps/cli/test/jockey-no-effect.test.ts` ② | 同上 | 同上 |

---

## 4. 検査の結果

| 対象 | 結果 |
|---|---|
| `npm run typecheck` | ✔ エラー 0（strict・`apps/web` 込み） |
| 全体（`npx vitest run --maxWorkers=2 --minWorkers=1`） | **1,874 件中 1,871 緑**（実行時 240 秒）。★このあと赤 3 件のうち 2 件（`jockeys.test.ts` ②③）を上記のとおり付け替えて緑にしました |
| 残る赤 | **1 件のみ**: `packages/render/test/edit-grammar-audit.test.ts` ⑨「seed 474 の分類が `solo` でなく `contest`」。★**第 4 便の前から赤**で、映像の編集文法の話であり本便と無関係です |

---

## 5. ★照会（レビュー側へ・2 件）

| # | 照会 | 私の暫定と理由 |
|---|---|---|
| **Q-GB4-1** | ★**先天個性を独立した遺伝の行（§5.4）にするか。** 裁定 2-2 ① は「**遺伝させるかどうかを先に決める**」と言っています | ★**この便では作っていません。** 既存の発現値（道悪適性・気性・根性・賢さ）から導いています。→ ★**§5.4 を触らずに済み**、V-1・V-2a〜V-2e を取り直さずに済みます（★既存の形質を通して自然に遺伝します）。★独立の行にする裁定なら、**§5.2 と §5.4 の両方**を直し（★片方だけにした D-015 の再発防止）、V-1・V-2 系を取り直します |
| **Q-GB4-2** | ★**「暴走」を率で引くか、期待値で倍率に載せるか。** D-110 ② は「失敗の**率**を下げる」と書いています | ★いまは**期待値**として倍率に載せる形（0 なので不作用）。★本当に「率」で引くなら ★**乱数の注入**が要ります（憲法 4・`resolveIntervention` は現在シードを持ちません）。★どちらにするかで、効かせる便の V-13 の測り方が変わります |

---

## 6. ★この便で**していない**こと

| # | 中身 | いつ |
|---|---|---|
| 1 | ★**効かせること**（＝ 0 を 0 でない値にする） | 第 5 便。★そのとき **V-4・V-5・V-6・V-17**／★繊細を効かせるなら **V-18 ①②a②b**／★泥巧者を効かせるなら **V-2f**／★騎手なら **V-13（必ず動く）・V-9a・V-8** を取り直します |
| 2 | ★特性を**保存する DB** と**画面** | ★画面はデザイナーのカード待ち。★保存は物語（§18）の書き込み経路と同じ便に置くのが自然です |
| 3 | ★出走登録の RPC に `calm` を渡すこと | ★`0024` の `jockey_frozen` は **jsonb** なので移行は不要ですが、★註記の「中身」の行が古くなります（★次の便で直します） |
| 4 | ★**本番への適用・配備** | ★`0023` と新しいワーカーが先（最終裁定 §3）。★この便は**どの DB にも触っていません** |

---

## 7. 触ったファイル

| ファイル | |
|---|---|
| `packages/sim-engine/src/traits.ts` | 新規（先天 4・後天 3・効果 0） |
| `packages/sim-engine/src/index.ts` | 公開口に 1 行 |
| `packages/race-engine/src/intervention.ts` | 騎手の差し込み口（定数 4・入力・窓・暴走・結果の 1 項目） |
| `packages/scheduler/src/jockeys.ts` | 名簿と凍結に `calm` |
| `packages/sim-engine/test/traits.test.ts` | 新規（10） |
| `apps/cli/test/trait-no-effect.test.ts` | 新規（3・★`resolveRace` を 48 レース回す） |
| `apps/cli/test/jockey-window-no-effect.test.ts` | 新規（5・★判定を回して 1 ビット比較） |
| `packages/scheduler/test/jockeys.test.ts` / `apps/cli/test/jockey-no-effect.test.ts` | 錨の付け替え（§3） |
| `apps/cli/src/calibration.ts` | 免除 5 件（`TRAIT_EFFECT`・`TRAIT_LABEL`・`INNATE_THRESHOLDS`・`LEARNED_STEPS`・`LONG_DISTANCE_M`）と `JOCKEYS` の理由の追記 |
