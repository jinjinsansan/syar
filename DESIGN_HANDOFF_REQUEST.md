# デザイン依頼 — STAR レース観戦画面のリデザイン

**リポジトリ**: https://github.com/jinjinsansan/syar
**ブランチ**: `main`
**成果物**: ハンドオフ ZIP（下記 §6 の形）

---

## 1. 何を作っているか

**STAR** — オリジナルのオンライン競馬育成ゲーム。配合→調教→レース→引退→繁殖のループ。
今回お願いするのは **レース観戦画面（2D・ピクセルアート）の見た目** です。

現物はローカルで動きます:

```bash
git clone https://github.com/jinjinsansan/syar
cd syar && npm ci && npm run dev:web
```

| URL | 中身 |
|---|---|
| `/still` | ★**静止画1枚**。部品を出し入れでき、大きさ・詰まり具合を変えられる。**ここが今回の主戦場** |
| `/race` | 動くレース（コース幾何＋カメラ＋スプライト） |
| `/course` | コースの俯瞰（幾何の確認用） |
| `/camera` | カメラの確認用 |

**関連ファイル**

```
design/art/art-bible.md                 ★アートバイブル（必読）
design/art/assets/horse-gallop-sheet.png ★馬＋騎手の6コマ（現行素材）
apps/web/src/app/still/page.tsx          静止画の組み立て
apps/web/src/lib/canvas-renderer.ts      描画コマンド → Canvas
packages/render/src/commands.ts          ★描画コマンドの定義
tools/lib/dress.mjs                      勝負服・ゼッケンの塗り
```

---

## 2. ★絶対に守っていただきたいこと（違反は差し戻しになります）

1. **実在の競走馬・馬主・団体・競馬場の名称／ロゴ／勝負服の意匠を使わない。**
   実在の写真・スクリーンショットのトレースや流用も不可。
   ★**業界共通の作法**（枠順の色、馬番、ハロン棒、決勝線、順位表示など）は採ってよい。
2. **他社製品の固有名称を、ファイル名・変数名・コメント・出力に書かない。**
3. **ピクセルアートを非整数倍で拡大縮小しない。** 表示倍率は **1倍 / 2倍のみ**（決定事項 D-058）。
4. **馬のスプライトは 220×140 px**（決定事項 D-058）。この寸法は変更できません。
5. **色は「役割」で持つ。** 16進値をコードに直書きしない
   （`turf` `dirt` `sky` `rail` `stand` `hedge` `fence` `paper` `ink` `silk-N`）。
6. **色だけで情報を伝えない**（色覚多様性）。馬番・形など、色以外の手掛かりを必ず併記。
7. **エンジン（`packages/race-engine` `packages/sim-engine`）には触らない。**
   着順・タイム・確率に関わるものは一切変更不可です。

---

## 3. アートバイブルの方針（`design/art/art-bible.md`）

- 一行規則: **「競馬場の渋さを、ピクセルで。派手さは勝負服と『間』だけに許す。」**
- 背景は**水平の帯**で構成する。**線遠近を描き込まない**
- **奥行きは速度差（パララックス）と、手前ほど大きく描くことだけで作る**
- ゲージだけが例外で、唯一の「機械」の表現
- UI は「紙」
- カメラ: 道中は引き、勝負所で寄る

---

## 4. ★いま何が足りないか（オーナー判定）

現状は「おもちゃに見える」と判定されています。**具体的に足りないもの**:

| # | 足りないもの |
|---|---|
| 1 | **競馬場に見える景観**。空・スタンド・生垣・柵・芝しかなく、板の上を走っているように見える |
| 2 | **馬群の見え方**。団子で走るのに、重なりと厚みが表現できていない |
| 3 | **ゴール前の作り**。決勝線・ゴール板はあるが、緊張が無い |
| 4 | **手前と奥の差**。手前の柵、奥のぼけ、明度差などの層構成 |
| 5 | **配色**。全体に彩度が低く、のっぺりしている |
| 6 | **ゼッケンの番号**が小さく、2桁が欠ける |
| 7 | **順位表示**（上部の丸に馬番）の意匠 |

★**参考にすべきは「実際の競馬中継の画作り」と「80〜90年代のスポーツゲームの間」**です
（アートバイブル §7）。**特定の他社製品を模倣しないでください。**

---

## 5. ★お願いしたいこと

**1枚の静止画を、完成形として設計してください**（`/still` に相当する構図・直線でゴール前）。

そのうえで、**その1枚を再現するために必要な部品**を切り出してください:

1. **配色表**（役割名 → 色。上記 §2-5 の役割名で）
2. **背景の層構成**（各層の役割・高さ比率・スクロール速度比・模様）
3. **手前の要素**（ラチ・柵など。馬より前に描くもの）
4. **馬・騎手のスプライト**（220×140・6コマ。★現行素材を改良する形でも、新規でも可）
5. **UI**（順位表示・実況帯・ゲージ・着順表・ハロン棒・ゴール板）
6. **ゴール前の演出**の指定（何を、いつ、どれだけ）

---

## 6. ★ハンドオフ ZIP に入れてほしいもの

```
handoff/
├─ README.md                 ★何をどう使うか。1枚目に完成形の画像
├─ mockup/
│   ├─ final.png             ★完成形 1280×720（これが基準）
│   └─ layers/               各層を分けた PNG（背景・手前・UI）
├─ palette.json              ★役割名 → 16進。§2-5 の役割名で
├─ layers.json               ★層の順序・高さ比率・スクロール速度比・タイル幅
├─ sprites/
│   ├─ horse-gallop.png      ★220×140 × 6コマ（横並び・背景は透明）
│   └─ contract.md           コマの基準点・接地線・勝負服とゼッケンの位置
├─ ui/
│   ├─ spec.md               順位表示・実況帯・着順表の寸法と余白
│   └─ *.png                 必要な部品
└─ NOTES.md                  ★判断の理由（なぜこの配色・この層構成か）
```

### ★特にお願いしたいこと

- **`palette.json` と `layers.json` は数値で**ください。「もっと渋く」では実装できません
- **スプライトは 6コマとも、胴体の位置が揃っている**こと
  （現行素材はゼッケンの当て布を基準に揃えています）
- **各層の「スクロール速度比」**を必ず入れてください（奥行きはこれで作ります）
- ★**なぜそうしたか**を `NOTES.md` に書いてください。理由が分かれば、こちらで調整できます

---

## 7. こちらの状態（参考）

- テスト 767件緑 / TypeScript strict 通過
- 描画は「**描画コマンドの配列 → レンダラ**」に分離済み（`packages/render`）
  → **配色・層・寸法を差し替えれば、コードを大きく変えずに反映できます**
- コース幾何（楕円・コーナーの距離ロス）とカメラ（カット表）は実装済み

---

## 8. 質問があれば

**推測で埋めないでください。** 分からない点は列挙して返していただければ、こちらで答えます。
特に「この寸法でよいか」「この色でよいか」は、**先に聞いていただくほうが早い**です。
