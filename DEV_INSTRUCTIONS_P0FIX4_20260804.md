# 開発指示書 P0-fix4 — clamp 切断補正と積み残しの回収（P0 最終便）

**発行**: 2026-08-04（レビュー側 Claude）
**前提**: `REVIEW_P0FIX3_VERDICT_20260804.md` を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**D-013 を受けて §6.4・§13.1 を改訂済み**）

---

## 0. まず結論 — P0-fix3 は PASS でした

合格基準12項目すべて成立し、`npm run verify` と V-2e の形質別表はレビュー側の環境で**全数値一致**しました（テスト82件・typecheck も独立確認）。

**特に評価します:**

- **H-1 の対照テスト設計**。「発動率100%でも非能力形質は0件」だけでなく「同条件で通常の隔世遺伝は11形質すべてで発動する」という**反対側も固定**した点。片側だけのテストより退行検出力が高く、以後の標準にします
- **H-2 の受け止め方**。撤回の根拠（平衡式が AR(1) を前提とする以上、合計 clamp は導出根拠を壊す）を正確に理解し、`mutateAllele()` を純関数に切り出して**両形式が異なる値を返す入力**で固定した対処は的確です。「なぜ従来のテストで検出できなかったか」をコードに残した点も良い
- **`silenceMutation()` の自己申告**。「`MUTATION_SD=0` が非能力形質を止めなくなった」を**前便で指摘したトートロジーの裏返し**として自分で認識し、申告したこと
- **V-2d の乖離の向きが上方から中立へ変わった**こと（6形質すべてが上ずれ → ±混在）。D-011 の狙いが数値で確認できました

---

## 1. スコープ

### I-1【必須】非能力形質の sd に clamp 切断補正を入れる（Q1 の回答＝(b)・D-013）

**Q1 の診断を一部訂正します。原因は遺伝的浮動ではなく clamp の切断です。**

clamp を sd の 1.67倍に置くと、打ち切られたノイズの実効SDは解析的に `0.916 × sd` になります:

```
X ~ N(0,σ²), Y = clamp(X, ±1.67σ) のとき
E[Y²]/σ² = [(2Φ(1.67)−1) − 2·1.67·φ(1.67)] + 1.67²·2(1−Φ(1.67)) = 0.840
∴ 実効SD = 0.9156 × sd  →  平衡SD = 創始の 0.916倍
```

これは正典 §6.4 が既に「約0.92倍」と記載していた値で、**実測の平均0.91とも一致**します。浮動の寄与は桁違いに小さい（Ne≈738 で1世代あたり分散の 0.068% に対し、回帰は毎世代 36% を除去）。気性の 0.84 は理論値からの残差で、形質間のばらつき（±0.06）の範囲内です。

**定量できる系統誤差はゲート幅で吸収せず、導出式で打ち消します。** 正典 §6.4 の導出式を改訂しました:

```
sd = 創始アレルSD × √(2r − r²) ÷ 0.916
```

| 形質 | 現行 sd / clamp | **補正後 sd / clamp** |
|---|---|---|
| 丈夫さ | 60 / ±100 | **65 / ±109** |
| 気性 | 9 / ±15 | **10 / ±16** |
| 芝/ダート適性 | 12 / ±20 | **13 / ±22** |
| distance_center | 312 / ±520 | **341 / ±568** |
| distance_range | 104 / ±173 | **114 / ±189** |

- 補正係数は定数として外出しし（`CLAMP_TRUNCATION_FACTOR = 0.916` 等）、**clamp/sd 比を変えたら再計算が要る**旨をコメントに残すこと
- 能力5種は対象外（sd=90 のまま。V-1 から較正されているため）
- 期待される結果: V-2e が 0.84〜0.96 → **0.92〜1.05 程度**。下限0.8までの余裕が 0.04 → 0.12以上
- (c)（ゲートの下限を見直す）は採りません。**実測に合わせて基準を緩めるのは基準を持つ意味を失わせます**（正典 §13.3）

### I-2【必須】積み残しの回収（P0-fix2 のレビューで「次便」としたもの）

| # | 内容 |
|---|---|
| I-2a | `MUTATION_CLAMP_RATIO: 150/90` がリテラルの二重管理になっている。`MUTATION_CLAMP / MUTATION_SD` から導出すること（G-0 で潰したのと同じクラス） |
| I-2b | V-2d で `founderMean === 0` の形質は乖離0で**無条件 PASS** になる。将来 0 中心の形質が入ったとき静かに素通りするので、明示的に扱うこと（対象外と宣言するか、絶対差で判定するか） |
| I-2c | V-2d/V-2e は genotype 発現値で測っているが、§7.5 の故障率の分母は**表現型**。現状 meanF 0.011 で差 0.6% と無害だが、P1 で近交が進むと乖離を検出できない。**表現型平均の併記**を追加すること（判定は genotype のままでよい） |

### I-3【必須】clamp の式を「本番経路」でも固定する — H-2 の残穴

H-2 のテストは的確でしたが、**固定範囲が純関数 `mutateAllele()` の内部に限られています。** レビュー側の監査で次の実験をしました:

> `mutateAllele()` は正しいまま、**呼び出し側（`genetics.ts:238-241`）に旧「合計 clamp」形をインラインで書き戻した** → **82件すべて PASS**

つまり「旧形式に戻すと確実に落ちる」のは `mutateAllele` 本体を編集した場合だけで、**本番経路が純関数を正しく使っているかは固定されていません**。P0-fix2 で見逃した事故（テストが実質何も守っていない）と同じ構造が、もう一段外側に残っています。

`inheritGenotype` / `breed()` を経由する固定を1本追加してください。例:

- sd を非ゼロにしたスタブRNGで `breed()` を1回回し、`mutateAllele()` で別途計算した期待値と genotype を突き合わせる
- または黄金値スナップショット（決定論があるので安定します）

### I-4【必須】CLI の上書き経路が sd 導出をバイパスしている

`TRAIT_MUTATION` は `balance.ts` の読み込み時に `REGRESSION_RATE` と `FOUNDERS` から**1度だけ**構築されます。ところが:

- `apps/cli/simulate.ts` の `--set-balance REGRESSION_RATE=…` / `--set-founders …_SD=…` は `traitMutation` を**再構築しません**
- `apps/cli/src/sweep.ts` も `REGRESSION_RATE` を振りながら sd を据え置きます

`balance.ts` のコメント「定数を別々に持つと片方だけ変えたときに平衡が壊れるので、必ずここで一緒に導出する」が、この経路では守られていません。

**これは P0-fix の誤結論（「回帰0.20 では距離SDが0.4倍に収縮する」）を生んだのとまったく同じ罠です。** 既定値と `verify` は正しいので P0 の合否には影響しませんが、**P1 で回帰率や創始定義を触るときに必ず再発します。**

`REGRESSION_RATE` または `FOUNDERS` を上書きしたら `buildTraitMutation()` を再実行するようにしてください（CLI 側に1行）。`sweep-distance.ts` が距離2形質だけ再導出している点も、D-012 後は丈夫さ・気性・適性が置き去りになるので同じ扱いにしてください。

### I-5【推奨】sd が創始定義に追随することのテスト

D-009 で `center` については「`FOUNDERS.DURABILITY_MEAN` を変えたら center も変わる」というテストを入れてもらいました。**sd 側には同等のテストがありません。** 同じ形で1本足してください（`FOUNDERS.*_SD` を変えたら導出 sd が追随すること）。

---

## 2. やらないこと

- V-1/V-2a〜e/V-3 の閾値変更（オーナー承認事項）
- 能力5種の sd（=90）の変更
- 大物覚醒・回帰率・回帰中心の変更
- レースエンジン・オッズ・育成ループ・UI・DB（**P1 以降。次便が P1 になります**）

---

## 3. 合格基準

P0-fix3 の12項目をすべて維持したうえで:

| # | 基準 |
|---|---|
| 13 | **V-2e が全6形質・4シード × 100/300年で 0.9〜1.1倍**に収まること（従来のゲート 0.8〜1.4 より厳しい確認値。外れたら報告） |
| 14 | 補正係数がテストでリテラル固定され、補正後の sd/clamp が正典 §6.4 の表とリテラル一致すること |
| 15 | I-2a/b/c が反映されていること |
| 16 | **本番経路（`breed()` 経由）で clamp の式が固定されている**こと。呼び出し側に旧形式を書き戻すと落ちるテストが存在すること（I-3） |
| 17 | `--set-balance REGRESSION_RATE=…` / `--set-founders …_SD=…` の上書きで `traitMutation` が再構築されること（I-4） |

---

## 4. 報告様式

`REPORT_P0FIX4_[日付].md` に:

1. 変更点（I-1 / I-2a / I-2b / I-2c）
2. 再実行コマンド
3. 検証結果（4シード × 100/300年・**V-2e を P0-fix3 と並べて比較**）
4. 仕様からの逸脱・解釈（全件列挙。ゼロなら「なし」と明記）
5. 未解決の質問

---

## 4b. レビュー側の宿題（対応済み・報告）

監査で正典側の編集残りが3件見つかりました。**いずれもレビュー側の編集ミスで、実装への指摘ではありません。是正済みです**:

- §6.4 に**撤回済みの「合計に clamp」の式が残っていた**（直後に訂正ブロックがあるため矛盾状態だった）
- §6.4 の「他形質は値域比で縮尺する」が D-012 と矛盾していた
- **§13.1 の `TRAIT_MUTATION` で durability/temper/surface の4キーが重複定義**されていた（旧 center のみの行が残置）

正典を読み直すときは最新版を参照してください。

---

## 5. これで P0 は完了です

I-1/I-2 が通れば、**遺伝エンジン（P0）は完成**とします。次便は **P1: レース判定エンジン + モンテカルロ検証**の指示書です。

P1 への引き継ぎ事項（確定分）:

1. **選抜 h²=0.3 の置き換え** — P0 の種牡馬選抜は観測ノイズ付きの近似。P1 では**実際のレース結果に基づく選抜**へ
2. **選抜下での分化の維持** — V-4〜V-6 較正時に「`distance_range` と V-2e の推移」を必ず確認。**P0-fix4 完了後の数値が基準線**（P0-fix3 の値ではない）
3. **`INJURY_BASE` の較正** — 丈夫さの集団水準が創始650近傍にあることを前提とする
4. **表現型と genotype の乖離** — I-2c の併記を使って、近交が進んだときの故障率の分母を監視する
5. **大物覚醒の演出**（Q2）— 対象が能力5種のみなので、演出は能力を明示する形が自然（P4 の UI 実装への申し送り）
