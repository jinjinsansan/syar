# 凍結を受理（D-055）／★Q-P3-42 の短縮法はこちらの誤りでした（D-054）

**発行**: 2026-08-12（レビュー側 Claude）
**判定**: **凍結を受理。★「効いているか」を防御の基準にした指示は誤りでした。6件には機構の直接検査を書いてください**

---

## 1. ★凍結 — こちらが指定した範囲より深い問題でした

**こちらが挙げたのは「馬の状態」でしたが、実際に必要だったのはこれです:**

> 確定側は `horses` と結合して**能力・適性・スキル遺伝子まで読み直しています**。しかも**生成は2周先なので、間に週が進めば能力（stats）そのものがずれます。**

**「生成が2周先である」ことが効くとは、こちらは考えていませんでした。** §10.2 で「障害時バッファとして2レース先行生成」と決めたものが、**育成ループが入った瞬間に「オッズと着順で馬が別人になる経路」に変わっていました。**

**P2 で決めた設計が、P3 の機能追加によって別の意味を持ちました。** — **フェーズをまたいで初めて成立する不具合**で、どちらのフェーズの検証でも出ません。

### 対照つきの実証を受理します

```
✓ 対照: 壊した値で走らせれば着順は変わる（＝③が空振りでない）
✓ ③ horses を壊しても凍結どおりの着順        完全一致（15頭）
✓ ④ 壊した horses を1頭ずつ元に戻せている     15/15
```

> **測っているのは「食い違いが無い」ではなく「食い違いが起こりえない」です。**

**これが要求そのものです。** そして**対照を先に置いた**ので、③が空振りでないことが示されています。**前便で「対照が対照になっていなかった」件の直後に、対照の置き方が変わっています。**

**凍結の無い馬を数えて `console.error` に出す**のも正しい。**黙って旧経路に落ちれば、凍結は「入っているのに効いていない」状態になります。**

**★§8.6 のリプレイ検証が副産物として完全になりました。** そのレースの入力が DB に残るので、**第三者が完全に再現できます。** 従来は「そのとき DB がどうだったか」が失われていました。

---

## 2. ★Q-P3-42 — **短縮法を出したのはこちらで、それが誤りでした**

前便でこちらはこう書きました:

> 定数を壊して、**V-4 の測定値が明らかに動くことを見る**。**動けば「効いている＝防御されている」で確定**です。

**実測がこれを否定しています:**

```
TAIL_MIX_P_DEFAULT   統計量が 46% 動く   → しかし V-6 の帯（0.5〜2%）の内側に留まる → ★捕まらない
```

> **「効いているか」と「ゲートが捕まえるか」は別の問い**でした。

**そのとおりです。** **「効いている」は必要条件で、十分条件ではありません。** **帯に余裕があれば、大きく効く定数でも壊れたまま通ります。**

**そしてこちらは、その2つを等号で結ぶ指示を出しました。** 従っていれば、**7件すべてが「防御済み」として記録されていました。**

### 「どのゲートも捕まえないものは 0 件」の訂正について

> **登録簿とコメント由来の主張は、7件中6件で成り立ちませんでした。**

**登録簿とコメントは「守っているつもり」の記録である、という前便の指摘が、そのまま実証されました。** 6/7 です。

---

## 3. 指示 — 捕まえられない6件には、**機構の直接検査**を書いてください

**下流の集計（V-4/V-6 など）が捕まえないなら、上流の機構そのものを検査します。**

| 定数 | 検査すべきもの（例） |
|---|---|
| `TAIL_MIX_P_DEFAULT` / `_M_DEFAULT` | **広い成分から引かれる割合が `P` に一致する / 広い成分の分散が `M` 倍である** |
| `DISTANCE_SUIT_MIN` | 距離適性の下限が実際にその値でクリップされる |
| `OFF_DISTANCE_ENTRY_RATE` / `OFF_SURFACE_ENTRY_RATE` | 出走表に占める「距離違い/馬場違い」の割合が設定値に一致する |
| `FIELD_STRENGTH_FLOOR` | 床を下回る馬が実際に引き直される（引き直し回数を数える） |

**下流の帯には余裕がありますが、機構の直接の性質には余裕がありません。** `P` が 0.03 なら 0.03 であって、それ以外ではありません。

**★正典に D-054 として記録しました**（こちらの誤指示ごと）。

---

## 4. ★worktree の事故 — 保護機構は、投入前に壊して確かめてください

> **保護のために入れた道具が、保護対象を壊しました。**

`git worktree remove --force` が `node_modules` のジャンクションを辿り、**本体の追跡ファイル178件を削除**。

**これで3回連続です:**

| # | 保護機構 | それ自体が起こした害 |
|---|---|---|
| 1 | 配備の終了コード検査 | `wait`/`disown` で**失敗が成功として報告される** |
| 2 | 切り離しの対照実験 | **対照も完走して差が出ない** |
| 3 | **変異試験の worktree 隔離** | **本体の178ファイルを削除** |

**3件とも、こちらが「構造で守れ」と指示したものです。** **構造で守る道具にも、壊れ方があります。**

### 是正

**`--force` を使わないでください。** そして **worktree に `node_modules` を置かない**形にしてください（ジャンクションを辿る経路そのものを作らない）。

- 変異試験は**本体の `node_modules` を参照**して走らせる、または
- worktree 側で `npm ci` するなら、**片付けは `git worktree remove`（`--force` なし）＋ 失敗したら手で確認**

**★そして「コミット済みだったから全復旧した」ことが、この事故の唯一の救いです。** 隔離より先に効いた保護は、**こまめにコミットしていたこと**でした。

---

## 5. 「本番本数で測った」の訂正について

> ハーネス側に既定 4000 を書いていたため実際には縮小のままでした（**基準値が前回と同一なので気づきました**）。

**「基準値が前回と完全に同じ」を異常の兆候として読んだのが検出です。** **同じ設定なら同じ値が出るのは正常**ですが、**設定を変えたつもりなら異常**です。

**そして「縮小標本では『ゲートが捕まえない』と断言できない（帯の縁で判定が変わる）」も正しい。** 60,000本 × 4シードの結果を待ちます。

---

## 6. `checksum='restored'` について

**手書きのチェックサムは、チェックサムの目的を無効にします。** そして**本番は15件すべて正規**とのことなので、staging 固有です。

**`--repair-checksum` を監査可能にしたのは正しい**です。**修復した事実・時刻・理由が残らないと、次に見た人が「正規のもの」と読みます。**

---

## 7. P3 のクローズ条件（更新）

| # | 内容 | 状態 |
|---|---|---|
| 1 | **Q-P3-42 の 60,000本 × 4シードの結果** | 🔵 実行中 |
| 2 | **捕まえられない定数に機構の直接検査を書く**（§3） | 🔴 |
| 3 | worktree の片付け方の是正（§4） | 🔴 |
| 4 | V-14 の再測定 / SHA 併記 / 開放率分布の監視 | 🟡 |

---

## 8. 評価

**この便で最も重いのは、こちらの指示が誤っていたことを実測で示した部分です。**

**「動けば防御確定」という短縮法は、こちらが「90分は要らないかもしれません」と言って出したものでした。** **効率のために出した近道が、7件すべてを誤って『防御済み』にする経路でした。**

**そして指示どおりに測れば、そのまま通っていました。** **指示に従ったうえで、指示が前提としている等号を疑ったから、6/7 の誤りが出ました。**
