# 【STAR】指示書 — 番組表のクラス分け（出走資格の述語）CL-1〜CL-8

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **解けるもの** | ★**Q-SETUP-05**（出走資格の述語が無い）／ ★**Q-BAND-04**（どのレースに申し込むか）／ ★**Q-BAND-01**（帯の下限の線） |
| **正典** | §10.3（番組表）・§10.4（出走馬の構成）・D-018（同格帯）・D-079（初期馬）・**D-007 改訂**（480R/日）・**D-114**（素質を見せない） |
| **前の裁定** | `REVIEW_SETUP_PREDICATE_VERDICT_20260918.md`（(a) を採り、繋ぐのは別便＝**本便**）／ `REVIEW_INITIAL_BAND_GATE_BG_VERDICT_20260918.md` |

---

## 0. この便で何が起きるか

```
Q-SETUP-05  出走資格の述語が無い（＝番組表にクラス分けが無い）
     ↓
Q-BAND-04   どのレースに申し込むかが決まらない
     ↓
Q-BAND-01   帯の下限の線が引けない
```

★**本便で一番上が解ければ、下の 2 つは自動的に解けます。** ★**いま最も費用対効果の高い 1 件**です。

---

## 1. 🔴 現状 — ★**「クラス」が 3 つの別物を指しています**

✔ [CODE] で確かめました。★**これが混乱の正体です。**

| # | 語 | 実体 | 効いているか |
|---|---|---|---|
| **①** | **`RaceClass`**（新馬・1勝・2勝・3勝・OP・重賞） | ✔ `programme.ts` の `classOf(cycleIndex)` が**枠ごとに返す**。正典 §10.3 の表そのもの | 🔴 ★**番組表が持っているだけ。出走馬の選抜に一切効いていない** |
| **②** | **`races.class_rank`** | ✔ `0001_init.sql:161`「**同格帯（D-018）**」。★註記が「**無作為抽選にすると 1 番人気の勝率が 51% になり V-4 が壊れる**」 | ✅ 効いている |
| **③** | **`sortPoolByClass` / `classBand`** | 🔴 ✔ `apps/cli/src/race-field.ts:167-177` — ★**`potential`（素質）の合計で並べている**。`classBand = 0.06` の連続した窓から引く | ✅ 効いている |

### 1-1. ★ここから、これまでの発見がすべて説明できます

| 発見 | 原因 |
|---|---|
| **Q-SETUP-05**: 出走資格の述語が無い（`conditionsOf:90` が `void raceClass;` でクラスを捨てている） | ★**① が実装に無い** |
| **Q-BAND-03**: 窓ありだと勝率が ★1.0〜★3.5 で 0.063〜0.080 と平ら（＝平均頭数 12.9 の逆数） | ★**③ が「同素質だけ」を集めるので、★が勝率から消える** |
| **Q-BAND-04**: どのレースに申し込むかが決まらない（uniform 7.7% ／ matched 41.0%） | ★**① が無いので、どのレースにでも申し込める** |

### 1-2. ⚠️ ★③ は外せません

★`0001_init.sql:161` の註記が**先に警告しています** — 「**無作為抽選にすると 1 番人気の勝率が 51% になり V-4 が壊れる**」（D-018）。
→ ★**「戦績クラスだけにして能力の窓を外す」は採れません。**

---

## 2. 裁定 — ★**資格（入れるか）と選抜（その中で誰を選ぶか）を分けます**

| 層 | 何で決まるか | 役割 |
|---|---|---|
| ★**資格** | ★**① `RaceClass`（戦績）** | ★**そのレースに出られるか**（新馬戦には戦績 0 の馬だけ、1 勝クラスには 1 勝の馬だけ…） |
| ★**選抜** | ★**②③ 能力の帯**（今のまま） | ★**資格のある馬の中から、誰を出走させるか**（D-018・V-4 を守る） |

★**② と ③ は 1 行も変えません。** ★**資格の層を上に足すだけ**です。

★**これで Q-BAND-04 が自動的に解けます** — ★自馬は**自分の戦績クラスのレースにしか申し込めない**ので、★**「uniform か matched か」という自由変数がそもそも生まれません。**

---

## 3. 指示（CL-1〜CL-8）

### CL-1 🔴 ★**戦績クラスを出す述語を 1 か所に切り出す**

★**Q-SETUP-05 の裁定の作法をそのまま守ってください** — ★**「既にあるはず」で書かない。実在を確かめてから繋ぐ。**

| | |
|---|---|
| 入力 | ★**馬の戦績**（勝利数） |
| 出力 | `RaceClass`（`maiden` / `win1` / `win2` / `win3` / `open`） |
| 置き場 | ★**1 か所**（★`packages/sim-engine` か `packages/scheduler`。★`apps/cli` と `apps/worker` の両方から呼べる所） |
| ⚠️ | ★**`horses` に勝利数の列はありません**（✔ `g1_wins` はあるが**G1 勝利数のみ**） |

★**勝利数の数え方**: ★**まず `race_entries` から数えてください**（`finish_pos = 1`）。★重ければ列にする。
★**これは Q-ST-4（適性判明の試行回数）と同じ形の判断**です。★**一貫させてください。**

### CL-2 ★**`conditionsOf` の `void raceClass;` を外す**

✔ `packages/scheduler/src/conditions.ts:90`。★**クラスを捨てている 1 行**です。
★**この便で `raceClass` を実際に使うのは、①距離・馬場の寄せ方 ②出走資格の 2 つ**。★**②が本体**です。

### CL-3 🔴 ★**出走馬の選抜に資格の層を足す**（★②③ は変えない）

```
今:    プール全体 → 素質順に並べる → classBand の窓 → 出走馬
本便:  プール全体 → ★資格で絞る（CL-1）→ 素質順に並べる → classBand の窓 → 出走馬
                     ↑ ここを足すだけ
```

⚠️ ★**プールが小さくなる帯が出ます**（特に上のクラス）。★**枯渇したときの振る舞いを決めてください** — ★**黙って窓を広げない**（D-079 ⑦ と同じ形: **広げた回数と幅を記録し、監視する**）。

### CL-4 ★**`enter_race` に資格を効かせる**

★自馬の登録（`0024`）も同じ述語を通すこと。★**新しい移行で**（`0024` は書き換えない・`migrate.mjs` が拒否する）。
★**画面側にも「このレースには出られません」を出す**（★黙って弾かない・R-27）。

### CL-5 ★**`0031` の暫定註記を置き換える**

✔ `0031_setup_account.sql` の `comment on function` に「★**暫定・08-20 §3-① は撤回・クラス分けの便で置き換え・V-4〜V-6・V-18 を取り直す**」と書いてあります。
→ ★**本便がその「クラス分けの便」です。** ★**註記を「置き換え済み」に直し、`is_initial_horse_candidate` を CL-1 の述語で書き直してください**（★`maiden` ＝ 戦績 0 と同じもの）。

### CL-6 🔴 ★**測り直し**（★この便の代償・最初から織り込んであります）

| 対象 | なぜ |
|---|---|
| **V-4・V-5・V-6** | ★出走馬の顔ぶれが変わる（★Q-SETUP-05 裁定で (b) を却下した理由がこれ） |
| **V-18** | 同上 |
| **V-14** | 出走機会の分布が変わる |
| **§3.4 の収支** | 同上 |
| ★**帯の下のゲート**（Q-BAND-01） | ★**ここで初めて線が引けます** |

★**AL-11 のとおり、GB-6 と 1 回にまとめてください。** ★**D-007 改訂（480R/日）も同じ 1 回に含めます。** ★**二度測らない。**

### CL-7 ★**勝ち上がりの流量を測って報告する**（★新しい要求）

★D-007 改訂で 1 日 **480R**・内訳は **新馬 140 / 1勝 120 / 2勝 80 / 3勝 60 / OP 50 / 重賞 30** です。

★**クラス分けを入れると、初めて「勝ち上がり」が流れます。** → ★**枠の本数と流量が釣り合うかを測ってください。**

| 測ること | |
|---|---|
| 1 | ★**各クラスの「出走可能な馬の数」と「1 日の枠数 × 平均頭数」の比**（★枠が余るのか、溢れるのか） |
| 2 | ★**新馬戦から 1 勝クラスへ上がる頭数／日** と、1 勝クラスの枠が受けられる数 |
| 3 | ★**上のクラスほど馬が減るはず** — ★**減りすぎて枠が埋まらないクラスが無いか** |

★**値の良し悪しは判定しません**（★較正の話なので）。★**測って報告してください。**

### CL-8 ★**Q-BAND-04 が解けたことを実測で示す**

★資格が入れば、★**自馬は自分のクラスのレースにしか申し込めません** → ★**uniform / matched の分岐が消えます。**
→ ★**BG 便の道具（`--choice`）で「選び方によらず同じ数字が出る」ことを確かめてください。** ★**それが「自由変数が消えた」ことの証明です。**

---

## 4. ★この便で「しない」こと

| # | |
|---|---|
| 1 | ★**② `races.class_rank` と ③ `classBand` の窓には触らない**（D-018・V-4 を守る） |
| 2 | ★**較正定数の値を動かさない**（★動かす提案は照会へ） |
| 3 | ★**素質を画面・API・ビューに出さない**（**D-114**。★資格は**戦績**から出すので、★素質は入りません） |
| 4 | ★**T-10（24 段階）・T-11（購入）・T-14（場）を混ぜない**（★別便） |

---

## 5. 順序

| 順 | 中身 |
|---|---|
| 1 | **CL-1**（述語を 1 か所に）★**実在を確かめてから書く** |
| 2 | **CL-2・CL-3**（`conditionsOf` と選抜に効かせる）＋ **CL-5**（`0031` の註記） |
| 3 | **CL-4**（`enter_race`・新しい移行） |
| 4 | **CL-7**（流量の測定）→ 報告 |
| 5 | **CL-6**（V-4/5/6・V-18・V-14・§3.4・帯のゲートを **GB-6 と 1 回で**）→ 報告 |
| 6 | **CL-8**（Q-BAND-04 が消えたことの実測）→ ★**ここで Q-BAND-01 の線をオーナーに諮る** |

---

## 6. ★報告に必ず書いていただきたいこと

★**今日の 2 便で効いた形をそのまま続けてください。**

| # | |
|---|---|
| 1 | ★**変異で両方向**（赤くなること ＋ 緑に戻ること ＋ `diff` 差分なし） |
| 2 | ★**条件ごとに「どの検査のどの行が・どの経路を通ったか」**（★入口だけ確かめて出口を確かめない形を避ける） |
| 3 | ★**裁定の字面から逸れたら、先に報告する**（★今日 3 回とも、それで差し戻しが要りませんでした） |
| 4 | ★**自分の洗い出しと実際の差**（★T-13 の「7 本のつもりが 10 本」は良い形でした） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
