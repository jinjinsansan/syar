# REVIEW VERDICT — P4「3D 馬群描画 8 秒技術検証」

日付: 2026-08-23  
レビュー: Codex  
対象: 未コミットの3Dスパイク作業ツリー  
指示書: `DEV_INSTRUCTIONS_P4_3D_PACK_SPIKE_20260823.md`  
報告書: `REPORT_P4_3D_PACK_SPIKE_20260823.md`

## 判定

**CONDITIONAL PASS — 3D馬群描画を次段階へ進めてよい。ただし、現スパイクを通常レースへ直接組み込んではならない。**

3D方式は、2D方式で解消できなかった以下を解消した。

- カメラ角度に応じて馬体の見える形が連続的に変わる
- 深度バッファによる自然な遮蔽
- 左右の脚が別の奥行きを持つ
- 12頭の走行位相が位置とは独立して散る
- コーナーでモデル自体が連続回転できる
- 既存レース座標を読むだけで3D場面を構成できる

一方、性能、素材、映像統合には本実装前の課題が残るため、無条件PASSとはしない。

## 独立確認

レビュー側で以下を確認した。

- `out/3d-pack-spike/` の動画、静止画、比較シート、測定JSON、README
- `packages/render-3d` の純粋計算とテスト
- WebGLページ、GLB配信ガード、Three.js描画経路
- 購入モデルのSHA-256と元ファイルの一致
- 通常`/race`、race-engine、DB、worker、認証が未変更であること

独立再実行:

```text
npx vitest run packages/render-3d/test
  2 files / 21 tests PASS

npm run typecheck
  PASS

npm run build:web
  PASS
```

開発側報告では全体111ファイル・1107テストPASS。このレビューでは新規21テスト、型検査、本番Webビルドを再実行した。

## PASSとした理由

### 1. 2D方式の根本制約を超えた

比較シートのC2とC3では、馬体、脚、頭、首、装具が3D空間内で前後関係を持つ。C3でカメラを12度回しても、方向別素材の切替は発生せず、同じモデルが連続的に見え方を変える。

これは2D V3に残った「側面の切り抜きを重ねた印象」に対する方式上の改善である。

### 2. 馬群の密集と個別運動が両立した

上位8頭を約6.07mへ収めても、固定ハッシュ位相により走行姿勢が散る。発走直後の横並びと1m団子でも、12分割した位相区画の8区画を使用している。

2Dで起きた「密集化すると位相差も潰れる」問題は回避できた。

### 3. 既存ゲームロジックと分離できた

`sceneAt`は既存の`s / w`を入力として読み、3D座標、yaw、位相、カメラを決める。着順、速度、laneExtraM、乱数、公正性を変更していない。

したがって、描画層だけを段階的に置換できる見込みがある。

### 4. デスクトップ性能は技術基準を満たした

Intel Iris Xe、1920×1080、12頭、影ありで中央値55.4fps、p95 25.5ms、30fps未満0.4%。指示書の平均30fps以上、p95 50ms未満を満たす。

24頭・影ありでも41.6fpsで動作した。少なくともデスクトップの技術スパイクとしては成立する。

## 条件付きとした理由

### 1. 描画負荷が本実装には高い

12頭・影ありで約103万三角形、755〜764 draw calls、106 geometries、206 textures。初回ロードは3.8〜6.2秒。

動いてはいるが、完成版で世界、HUD、ポスト処理を追加する余裕として十分とは言えない。特にdraw callsとtexturesが多い。

次段階では最低限、以下を測る。

- 同一GeometryとTextureの共有状況
- 毛色・勝負服Materialのキャッシュ
- 影を落とすメッシュの限定
- 影解像度と更新頻度
- 12頭時のdraw callsを現状からどこまで削減できるか
- モバイル実機または低性能GPUでの性能

### 2. 足元に破片状の描画が残る

C2/C3では馬の足元付近に細い黒い線・破片状の要素が複数見える。手綱、たてがみ、尻尾の問題を非表示メッシュで回避したとの報告は妥当だが、映像上のアーティファクトはまだゼロではない。

本実装前に、メッシュ本体か影かを個別非表示と`castShadow`切替で特定する必要がある。強いブラーや背景で隠してはならない。

### 3. 騎手は技術用の仮形状

購入モデルに騎手がなく、現状はカプセルと球の簡易騎手である。動作検証には十分だが、完成品質の判定には使えない。

完成版には、商用利用可能で馬モデルと寸法・鞍位置が合い、前傾、追い、減速へ対応できる騎手リグが必要。

### 4. アニメーションがGallop_RMのみ

走行中の8秒には使えるが、発走、加速、追い、減速、勝馬演出、常歩には不足する。

本実装では少なくとも以下の方針が必要。

- start / acceleration
- gallop cruise
- drive / push
- deceleration
- walkまたはwinner follow

新規クリップを購入・制作するか、Gallop_RMの速度・姿勢補正・クロスフェードでどこまで作るかを別途裁定する。

### 5. 本番ビルドに検証ルートのコードが残る

`NODE_ENV=production`ではページとモデルAPIが404になるため、モデルは配信されない。しかし本番ビルドのルート一覧には`/3d-pack-lab`と`/api/spike-3d/model`が残り、3Dページの約169kBチャンクも生成される。

スパイクとしては許容するが、本実装前またはスパイク終了時に検証ルートを削除する。404ガードを完成形にしない。

### 6. モバイル性能は未確認

Intel Iris Xeの測定だけで、対象スマートフォンのGPU、発熱、メモリ、ロード時間は未測定。Webゲームとして採用を確定するにはモバイル実機測定が必要。

## 次段階の方針

次に行うのは「競馬場をすべて3Dで作る」作業ではない。

**既存の2D世界描画を維持し、その上へ3D馬群を合成するハイブリッド1カット検証**を行う。

理由:

- 2D側にはスタンド、観客、ラチ、芝、距離標、内馬場などの蓄積がある
- 3D側の価値は馬体、脚、遮蔽、向き、影にある
- 世界全体の3D化は工数と性能リスクを急増させる
- 参考へ近づける主因は、背景の作り直しではなく馬群の3D化である

検証対象はC2相当の8秒真横追従1カットとする。

最低条件:

1. 既存2D世界と3Dカメラの投影・地平線・接地点を一致させる
2. 3D馬の影を2D地面へ自然に統合する
3. ラチなど前景物が3D馬を正しく隠す
4. 既存HUDは最後に重ねる
5. 3Dだけでなく背景込みの性能を測る
6. 足元アーティファクトを原因特定して除去する
7. 12頭のdraw calls、textures、初回ロードを最適化する
8. デスクトップと最低1台のスマートフォンで測る

このハイブリッド検証が成立してから、通常レースへの段階導入を裁定する。

## 現作業ツリーの扱い

現時点ではスパイクを通常レースへ接続しない。

`apps/web/package.json`には今回の`three`追加と、着手前から存在した`dev`ポート3100→3210変更が同居している。コミットする場合は、ポート変更を今回の3Dコミットへ混ぜないよう部分ステージングするか、オーナーが同時コミットを明示承認すること。

購入モデル`assets-spike/3d/holsteiner.glb`は`.gitignore`対象であり、リポジトリへ追加しない方針を維持する。

## 最終結論

3D方式は、今回の問題に対する正しい方向である。2D調整へ戻らない。

ただし完成版への直結ではなく、性能最適化、足元アーティファクト除去、騎手素材、追加アニメーション、2D世界との合成、モバイル測定を順に通す。
