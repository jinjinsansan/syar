-- 0036: 出品から「★（素質の段）」の列を落とす（★D-114 ② / T-10・AL-2）
--       ★正典 D-114 ②「★**段を外に出す口を塞ぐ** — 画面・API・ビュー・`horse_market_listing.stars` の
--       ★どこにも出さない」
--
-- 【★なぜ落とすのか】
--   ★`0025` は ★**「見せるのは★だけ（素質の数値は持たない）」**という前提で `stars` を置きました。
--   ★**2026-09-18・D-114 でその前提が撤回されました** — ★段そのものを見せません。
--   → ★列が残っていると、★**公開ビューを 1 本足した日に静かに漏れます**（★UI-3 が市場の公開ビューを作ります）。
--     ★「出さない運用」ではなく ★**出せない形**にします（★R-29「既定を閉じて必要なものだけ開ける」）。
--
-- 【★落としても困らない理由】✔ 確かめました
--   ① ★**SQL の中で `stars` を読む所は 1 つもありません**（★`buy_horse`〔`0026`〕も `sell_horse` も
--     ★`price_ep` / `sell_back_ep` しか読みません。★grep で全移行を走査）
--   ② ★出品の入れ替え（`market-flow.ts`）は ★**価格で「帯が変わったか」を見る形**に変えました
--     — ★価格は帯の関数なので、★**帯が変われば価格が変わります**（★同じ判定です）。
--   ③ ★24 段では 1 段が目盛 4 ÷ 23 ≒ 0.174 で、★**`numeric(2,1)` に入れると丸められます。**
--     ★丸めた値は読み戻したときに一致しないので、★**この列を残すと毎サイクル全出品が入れ替わります**
--     （★静かに壊れる形・R-16）。★落とすのが正しい直し方です。
--
-- ⚠️ ★`0025` は書き換えません（★`migrate.mjs` が適用済みファイルの改変を拒みます）。

begin;

alter table horse_market_listing
  drop constraint if exists horse_market_listing_stars_range;

alter table horse_market_listing
  drop column if exists stars;

comment on table horse_market_listing is
  '★馬の購入の出品（D-102）。★価格はサーバー（ワーカー）が書く。利用者は読むだけで、価格を申告できない（憲法 3）。'
  '★2026-09-18・D-114 / T-10 で stars 列を落とした — 素質の段は画面・API・ビューのどこにも出さない。'
  '★買う人に見えるのは馬そのもの（戦績・オッズ）と価格だけ。';

commit;
