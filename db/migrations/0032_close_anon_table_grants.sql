-- 0032: anon / authenticated に開いていた実体テーブル 3 件を閉じる
--       （★裁定 `REVIEW_ANON_EXPOSURE_VERDICT_20260918.md` AE-1・V-20 ②／③・`0018` の宣言）
--
-- 【何が起きていたか】
--   `0018_lock_public_grants.sql:52` は ★**「実体テーブルには一切 grant しない」**と宣言し、
--   `revoke all on all tables` ＋ `alter default privileges ... revoke all` で閉じたうえで、
--   ★**公開するものは 4 つの `*_public` ビューにだけ** grant しています（`0018:54-57`）。
--
--   ★その宣言の後に足された 3 つの実体テーブルが、★**`grant select` で anon に開いていました**:
--     `0024:97`  grant select on horse_story_event    to anon, authenticated;
--     `0025:57`  grant select on horse_market_listing to anon, authenticated;
--     `0027:46`  grant select on stable_grade_price   to anon, authenticated;
--
--   ★3 便とも、同じ行の 1 つ上で `revoke insert, update, delete, truncate` を正しく書いています。
--   ★**書き込みは塞ぎ、読みは開ける** — ★その形を、★**検査が 3 回続けて合格として固定していました**
--   （★2 便目・3 便目は 1 便目を手本にした。★裁定 §3）。
--
-- 【なぜ「公開してよい中身だから開けたままにする」を採らないか（裁定 §2）】
--   ① ★`0018` 自身の宣言に反する
--   ② ★V-20 ② の合格条件は「**公開ビューとして明示登録されたもの以外は anon から 0 行**」で、
--      ★**中身の性質ではなく、ビューとして登録されているか**で書かれている（正典 1654 行）
--   ③ ★正典 410 行が先に答えを書いている — 「公開したくなったら**別ビューを作る**」
--   ④ ★**実体テーブルは列が増える。** ★`horse_story_event.detail` は `jsonb` なので、
--      ★**列を足さなくても中身が増える**。★その日に V-20 を読み返す人はいない
--
-- 【★閉じても壊れないことは実測で確かめてある（裁定 §1）】
--   ✔ `apps/web` が読んでいるのは `races_public` / `race_entries_public` / `race_odds_public` だけ
--   ✔ `apps/web` の `.rpc(…)` は 0 件 ／ ✔ 3 表への参照は 0 件
--   ✔ 3 表を読み書きしているのは**ワーカー**（Postgres 直結）と ★**`security definer` の RPC 本体**だけ
--   → ★どちらも anon / authenticated の権限を通らないので、剥がしても経路が壊れない
--
-- 【★`0024` / `0025` / `0027` は書き換えない（裁定 AE-1）】
--   ★`tools/migrate.mjs` は「適用済みのファイルを書き換えず、新しい番号で足してください」と
--   ★**正しく拒否**します（✔ 前便 §9-14 で実証済み）。★staging には既に当たっています。
--
-- 【★`revoke select` ではなく `revoke all` にした理由（★裁定の字面からの逸脱・報告済み）】
--   ★裁定 AE-1 の字面は `revoke select ... from anon, authenticated` です。
--   ★ここでは **`revoke all`** にしました。★理由は **R-29**:
--     「★権限は『列挙して閉じる』のではなく『既定を閉じて、必要なものだけ開ける』。**列挙は必ず漏れる**」
--   ★実例は `0002` で、`insert, update, delete` を revoke したが **`TRUNCATE` / `REFERENCES` /
--   `TRIGGER` が列挙から漏れ**、★**`TRUNCATE` は RLS の対象外**だったため公開鍵で全消しできる状態でした。
--   ★`revoke all` は `revoke select` を**含み**、★今 3 表に付いている権限は `select` だけ（✔ `0024:96`・
--   `0025:56`・`0027:45` が他を剥がし済み）なので、★**実効は同じで、列挙漏れの余地だけが消えます。**
--   ⚠️ ★**字面と違う形にしたので、レビュー側が差し戻す場合は `revoke select` に直します。**
--
-- 【★公開が要るようになったら】
--   ★この 3 表に grant を戻さず、★**`*_public` ビューを作ってください**（`races_public` と同じ形）。
--   ★今は UI が 1 つも読んでいないので、ビューも要りません（裁定 §2）。

begin;

-- ---------------------------------------------------------------------------
-- ① 生涯の記録（D-074 ／ §18）。★`detail jsonb` を持ち、★列を足さなくても中身が増える
-- ---------------------------------------------------------------------------
revoke all on horse_story_event from anon, authenticated;

-- ---------------------------------------------------------------------------
-- ② 馬の市場の出品（D-102）。★★と価格だけだが、★実体テーブルは列が増える
-- ---------------------------------------------------------------------------
revoke all on horse_market_listing from anon, authenticated;

-- ---------------------------------------------------------------------------
-- ③ 厩舎の格の価格表（D-103）。★同上
-- ---------------------------------------------------------------------------
revoke all on stable_grade_price from anon, authenticated;

commit;
