# UI 作業報告（2026-09-22）

## 実装

- `/home` `/mypage` `/train` は本人の `users` / `my_horses` / `my_runs` に接続した。未ログイン時はログイン案内を表示し、架空の残高・持ち馬・成績を出さない。
- `/vote` は開催予定の出馬表・公開オッズを表示し、ログイン済みなら投票 RPC を使う。表示中に次レースへ替わった場合は 15 秒以内に更新する。自馬の判定は認証付き `race_entries_public.is_mine` を使う。
- `/exchange` は公開景品・本人の PP・交換履歴・交換 RPC に接続した。未ログイン時は案内を表示する。
- `/earn` は未接続の報酬を架空の額で表示せず、準備中と明示した。
- 常設レース表示を通常画面に置き、公開結果からの再現であることを明記した。実走行の位置データは公開されていないため、本編と同じ生中継ではない。
- `/race` の公開画面から開発用操作を隠し、素材の読込待ちには経過秒数・進捗表示・結果一覧への導線を加えた。未追跡 WebP を要求する系列は追跡済み PNG を直接読む。
- `my_runs.prize_pp` は移行 0056 で意図的に削除済み。古い参照を除去し、馬ごとの賞金を推測して表示しない。

## 確認

- 実ブラウザで `/home` `/mypage` `/vote` `/train` `/exchange` `/earn` `/watch-race` を確認。390px のモバイル表示は横はみ出し 0。未ログイン状態で権限エラーや偽の残高を表示しない。
- 実 DB の公開レース・結果を使う読取検査は `tools/verify-race-replay-read.mjs` で確認済み。
- `npm run gate` は 5 段階すべて通過。検査 2,750 件の赤 2 件は既存の登録簿と一致。

## 未完了・配備条件

- 実レースのライブ位置データは未公開。常設表示の走行は確定結果を使った再現であり、本編 `/race` も演出デモ。
- 調教指示を適用するワーカーと、参加ポイント報酬の提供元は未配備。画面に準備中と表示し、無効な操作を受け付けない。
- ログイン済み本人による `/signup` → `/setup` → 最初の 1 頭、投票、景品交換の最終操作は未実施。本番 DB には書き込んでいない。
- 本番への反映は未実施。引継ぎ書は `git push origin HEAD:refs/heads/main` をコマンド名を挙げたオーナーの許可後に行うと定めている。
- 未追跡アートの帰属は未確認で、今回の対象から除外する。既存の `title-eye` 削除も取り込まない。
