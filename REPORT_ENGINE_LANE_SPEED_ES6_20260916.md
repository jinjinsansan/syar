# 報告 — ES-6（距離ロスの下ごしらえを、オッズの試行の間で使い回す）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ 開発側 → レビュー側・オーナー
**対象**: 回答 `REVIEW_ENGINE_LANE_SPEED_ES5_ANSWER_20260916.md` §2-1（ES-6）・§2-2（2 つの比）
**ブランチ**: `p4/race-30sec-cuts` ／ **開始 HEAD** `6fe4e28`（回答書の受領コミット）／ ES-6 は本報告と同じコミット

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **△** ＝ 未確認・推定
> ⚠️ **G-1〜G-4 のお返事は、開発側にはまだ届いていません**（オーナーから回答書が渡されただけです）。G-2 が決まっていないので、§4 は 2 つの比を並べるだけで、合否は書きません。
> ⚠️ 本番・staging には接続していません。

---

## 0. 結論

1. **ES-6 を入れました。** 結果は ES-5 と 1 ビットも同じです（`buildRace` のオッズの行すべてを、使い回す版と試行ごとに作り直す版で文字列として比較し、一致）✔
2. **下ごしらえと条件が食い違うと投げます**（距離・幅・1 周・直線の長さ・コーナーの半径の有無・直線のレース）。確かめを外す変異で落ちます ✔
3. **ワーカーのオッズ計算 1 レースで `lanePlanOf` は試行の回数によらず 1 回**です。渡し忘れる変異で落ちます ✔
4. **速さ**（`97364c5^` との比・同じ回どうしで割った 3 回の範囲）✔
   - ① `resolveRace` 1 回の比: **最大 1.47 倍**（8 条件中 2 条件で旧い線 1.3 倍を超える回がある）
   - ② 試行 1 回（`resolveRace` ＋ 的中目の集計）の比: **最大 1.28 倍**（8 条件すべて、測り直した線 1.6 倍の内側。旧い線の数値 1.3 倍の内側でもあります）
   - どちらの比で判定するかは **G-2 のお返事待ち**です

---

## 1. 実装 ✔

| ファイル | 中身 |
|---|---|
| `packages/race-engine/src/race.ts` | `ResolveRaceParams.lanePlan?`（任意）／ `lanePlanForRace(conditions)`（`resolveRace` の中と呼び出し側が通す 1 つの作り方。直線は `undefined`）／ `assertLanePlanMatches(plan, conditions)`（`courseShape` が `oval` か・距離・`lapM`・`homeStretchM`・`widthM`・`cornerRadiiM` を値で比べ、食い違えば投げる。`course` の無いレースは `DEFAULT_OVAL` と比べる）。省いたときはいままでどおり中で作ります。**モジュールに状態は持ちません** |
| `apps/worker/src/build-race.ts` | モンテカルロに渡すのと同じ `oddsConditions` から、**試行の前に 1 回** `lanePlanForRace` で作り、全試行に渡す |
| 確定（`pg-store.ts`・`settle.ts`） | 変えていません（1 回しか呼ばないため） |
| `lane.ts` | 触っていません |

### 1-1. 測定器（同じ条件で試行を繰り返しているところ・R-30）✔

| ファイル | 変えた所 |
|---|---|
| `apps/cli/src/verify-payout.ts` | オッズの試行（`ODDS_TRIALS` 回）と確定。レースごとに 1 回作る |
| `apps/cli/src/verify-pmin.ts` | オッズの試行（`MC` 回）と確定の多数引き（`FINALS` 回）。レースごとに 1 回 |
| `apps/cli/src/verify-race.ts` | 人気推定（`POPULARITY_TRIALS` 回）・本番確定・V-8 の差し替え。レースごとに 1 回 |
| `apps/cli/src/bench-mc.ts` | 暖機と測定の 3 つの回し。1 回 |
| `apps/cli/src/diag-mneed.ts` | 参照 MC（`REF` 回）。レースごとに 1 回 |

- **変えていない測定器・診断**: `tools/*.mjs`。映像の監査や撮影の道具は、1 つのシードで 1 回呼ぶだけです。`diag-uncertainty.mjs`・`diag-lane-impact.mjs`・`diag-finish-spread.mjs`・`diag-pack.mjs`・`verify-v17-time.mjs`・`verify-heavy.mjs` はシードを変えて繰り返しますが、オッズや本番の経路の測定器ではないので触っていません。結果は同じなので、製品との食い違いは生まれません（△ 速さは変わらない）

## 2. 検査 ✔

| # | 内容 | 結果 |
|---|---|---|
| 1 | 指紋（`lane-fingerprint`）・`lane-sum`・`lane-plan-count` がそのまま合格 | ✔ 合格（指紋 ① は `884019c` と 1 ビット一致のまま） |
| 2 | `buildRace` の出力（出走表・オッズの行すべて・条件）が、使い回す版と試行ごとに作り直す版で 1 ビット一致。大河原 1200m（ループに落ちる馬を含む）・大河原 3000m・大河原ダート 2400m・スターパーク 1400m × **8 頭と 18 頭**（出走頭数がそうなるサイクルを探して使用）× 試行 300 | ✔ 8 組すべて一致（`apps/worker/test/build-race-lane-plan.test.ts`）。数は `toString()` で文字にして比べています |
| 2' | `resolveRace` 単体でも、渡した場合と省いた場合で 1 ビット一致（大河原 1200・3000m・スターパーク 1600m・既定 2000m・半径 4 つ違い 1400m × 8・18 頭 × シード 10） | ✔（`packages/race-engine/test/lane-plan-param.test.ts`） |
| 3 | 食い違うと投げる: 距離・幅・1 周・直線の長さ・コーナーの半径の有無・直線のレースに渡す、の 6 通り。値が同じ別のオブジェクトなら受け取る | ✔ |
| 4 | `buildRace` 1 レースで `lanePlanOf` が試行 20 回でも 400 回でも **1 回**・全試行が下ごしらえを受け取る。対照: 外すと試行 50 回で 51 回（＝数え方が効いている） | ✔ |
| 4' | `resolveRace` に作り済みを渡して 50 回呼ぶと、中の `lanePlanOf` は 0 回（`lane-plan-count.test.ts` に追加） | ✔ |

### 2-1. 変異 ✔（どれも戻して、`git diff` に変異が残っていないことを確かめました）

| 変異 | 落ちた検査 |
|---|---|
| `resolveRace` の一致の確かめを外す | 検査 3（「距離が違う: expected [Function] to throw an error」） |
| `build-race.ts` で `lanePlan` を渡し忘れる | 検査 4（「★下ごしらえを受け取った試行: expected +0 to be 20」） |

## 3. 検査の全体 ✔

- `npm run typecheck` … 通過
- `npx vitest run --maxWorkers=2 --minWorkers=1` … `Test Files 1 failed | 176 passed (177)` ／ `Tests 1 failed | 1786 passed (1787)`・終了コード 1。落ちたのは変更前からの `edit-grammar-audit` ⑨（独走代表 expected 'contest' to be 'solo'）で、1 件から増えていません（ES-5 の時点は 1781 件で、今回 6 件増えました）
- ES-6 の検査に加えて、既存の `course-frozen-wiring.test.ts`（オッズと確定が同じ走路の形を読む）も合格しています

## 4. 速さ（回答 §2-2 の 2 つの比）✔

**道具**: スクラッチの `bench-es6-trial.mts`。報告 §4 の `bench-es0.mts` と同じ馬（`watch-pool.json` の先頭）・芝・良・シード 0〜N−1・N=2000（3000m は N/2）で、予熱は 300 回です。
- ① は `resolveRace` だけ、② は試行 1 回を回します。② の中身は `resolveRace` ＋ `build-race.ts` と同じ的中目の集計（`apps/worker/src/odds.ts` の `winningKeys`・全 7 券種・`placeDepth`）です
- **集計のコードは両方とも HEAD の作業ツリーのもの**を使いました（回答 §2-2）
- ES-6 は、ワーカーと同じく**試行の前に 1 回 `lanePlanForRace` で作り、その時間も測る区間に含めています**。`97364c5^` には下ごしらえが無いので渡していません
- 同じ機械で `97364c5^` → ES-6 の順に交互に 3 回流し、**比は同じ回の値どうし**で取りました

| 条件 | ① `97364c5^`（µs） | ① ES-6 | **① 比** | ② `97364c5^`（µs） | ② ES-6 | **② 比** |
|---|---|---|---|---|---|---|
| 16 頭 1600m 既定 | 16.5〜18.6 | 22.0〜24.3 | **1.18〜1.47** | 24.7〜30.3 | 27.3〜29.7 | **0.98〜1.14** |
| 16 頭 1600m 最長の周 | 15.7〜17.5 | 18.1〜19.7 | **1.13〜1.24** | 24.4〜28.0 | 25.1〜27.4 | **0.98〜1.05** |
| 16 頭 3000m 既定 | 18.0〜19.5 | 19.9〜21.7 | **1.11〜1.13** | 26.0〜31.5 | 30.9〜32.4 | **0.98〜1.25** |
| 16 頭 3000m 最長の周 | 12.8〜15.3 | 16.4〜17.1 | **1.12〜1.30** | 21.8〜24.8 | 25.7〜28.7 | **1.16〜1.18** |
| 18 頭 1600m 既定 | 14.5〜15.3 | 16.7〜19.6 | **1.11〜1.31** | 23.8〜26.7 | 24.9〜29.1 | **1.01〜1.09** |
| 18 頭 1600m 最長の周 | 15.3〜18.2 | 18.6〜22.9 | **1.18〜1.28** | 24.6〜27.7 | 27.9〜34.1 | **1.10〜1.23** |
| 18 頭 3000m 既定 | 16.4〜18.2 | 17.4〜18.7 | **0.99〜1.14** | 22.5〜27.5 | 24.6〜28.7 | **0.99〜1.28** |
| 18 頭 3000m 最長の周 | 15.4〜16.2 | 17.7〜19.0 | **1.14〜1.17** | 22.6〜25.0 | 25.4〜27.2 | **1.06〜1.13** |
| **8 条件の最大** | | | **1.47** | | | **1.28** |

- **①（旧い線 1.3 倍）**: 16 頭 1600m 既定（1.47・1.35）と 18 頭 1600m 既定（1.31）の回が線を超えます。16 頭 3000m 最長の周は 1.30 ちょうどの回があります。**① で判定するなら届いていません**
- **②（測り直した線 1.6 倍）**: 最大 1.28 倍で、8 条件すべてが線の内側です。**② で判定するなら合格**です（判定は G-2 のお返事の後）
- ばらつき: 同じ版でも回によって 2 割前後動きます（例 `97364c5^` の ② 16 頭 1600m 既定 24.7〜30.3）。最初の条件（16 頭 1600m 既定）の ① は、どの回も高めに出ています
- ES-5（前回の報告 §4）の ① 約 1.4〜1.6 倍から、ES-6 で ① 約 1.0〜1.47 倍に下がりました。下ごしらえの 4〜7µs が試行 1 回から消えた分です
- △ 本番機の 1 レースの秒数には換算していません（本番機のコアと速さの比を測っていないため）

## 5. 触ったもの

| ファイル | 中身 |
|---|---|
| `packages/race-engine/src/race.ts` | §1 |
| `apps/worker/src/build-race.ts` | §1 |
| `apps/cli/src/{verify-payout,verify-pmin,verify-race,bench-mc,diag-mneed}.ts` | §1-1 |
| `packages/race-engine/test/lane-plan-param.test.ts`（新規）・`lane-plan-count.test.ts`（1 件追加） | §2 |
| `apps/worker/test/build-race-lane-plan.test.ts`（新規） | §2 |
| 触っていない | `lane.ts`・指紋の期待値ファイル・較正定数・`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`・`stepM`・`LANE_MODEL`・`packages/render`・`apps/web`・正典・DB・ワーカーの配備 |
| git の対象外 | スクラッチの `bench-es6-trial.mts` |

## 6. オーナーに決めていただくこと（回答 §7 のまま・開発側には未着）

| # | 判断 |
|---|---|
| G-1 | F-1（ES-5 で結果を丸めの差だけ許す）を承認したか。開発側は 2026-09-16 の「はい」で承認と受け取っています。レビュー側にも直接お伝えください |
| G-2 | 合格線を「試行 1 回の比で 1.6 倍以下」に測り直してよいか（§4 の判定がこれで決まります） |
| G-3 | 本番の DB の読み取り確認を許可するか（問題なければ `0021` → `0022`） |
| G-4 | push |
