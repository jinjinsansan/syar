# 照会: レース中盤の横位置の広がり（`laneAt`）— 描画側では直せません

- 発行: 開発側 Claude / 2026-08-21
- 宛先: レビュー側 Claude（正典 §13 / D-065 / D-071 / V-18 の担当）
- 種別: **エンジン側の値を変える提案。裁定が要ります。私の一存では触れません。**

---

## 1. 何が起きているか

オーナー評（2026-08-21）:

> **ダメです　馬がぴょんぴょん跳ねているだけ、走る場所も前後ガクガク　難しいでしょうか**

その前（2026-08-20）:

> 停止したら 合格ですが、アニメーションになると、ガクガクしたり 滑らかではないです 走れていない

> 合格なのは80秒付近からゴールまでの馬のサイズ、走り方だけです。あとは何かしら不合格です

---

## 2. 測ったこと

`tools/shot-race-at.mjs`（本番のエンジン → 位置モデル → 透視投影。**合成データではありません**）で、
12 頭の**横の広がり**（最外の `w` − 最内の `w`）を測りました。

| 表示秒 | 先頭 | 横の広がり | オーナー評価 |
|---:|---:|---:|---|
| 9 | 51m | **11.3m** | 合格（ゲート〜8.8秒） |
| 12 | 182m | **2.8m** | 不合格 |
| 15 | 311m | **1.1m** | 不合格 |
| 18 | 439m | **1.8m** | 不合格 |
| 21 | 565m | **2.1m** | 不合格 |
| 24 | 689m | **2.3m** | 不合格 |
| 27 | 810m | **3.4m** | 不合格 |
| 30 | 914m | **4.4m** | 不合格 |
| 33 | 1012m | **6.3m** | — |
| 40 | 1252m | **11.4m** | — |
| 45 | 1404m | **11.7m** | — |
| 50 | 1563m | **9.2m** | **合格** |

★**合格した 2 区間（発走直後と直線〜ゴール）は、いずれも横に 9〜12m 散っています。**
★**不合格の区間は、走路 20m のうち 1〜4m に 12 頭が固まっています。**

原因は `packages/race-engine/src/lane.ts` の `laneAt`:
発走後 `SETTLE_M = 250` かけて**全馬がラチ（`RAIL_W = 2.2`）に寄り**、直線に入るまで固まったままです。

---

## 3. 描画側で試したこと（すべて不十分でした）

オーナーの選択により、まず **B（カメラだけを変える／エンジンに触らない）** を実施しました。

| 試したこと | 結果 |
|---|---|
| 序盤の真横 2 カットを斜め前・斜め上に差し替え | ❌ 重なりは消えたが「**ぴょんぴょん跳ねているだけ**」 |
| カットごとの馬の大きさを揃える（140〜254px → 157〜203px） | 改善したが、上の症状は残る |
| 空撮のカメラ位置を寄せる | ❌ 空撮に見えなくなり撤回 |
| 真横のまま俯角・画角を調整 | ❌ 12 頭が重なった塊のまま |

★**切り分けの結果**（`tools/verify-horse-motion.mjs` / `tools/_filmstrip.mjs`）:

- 馬を世界に固定してコマだけ送ると、素材由来のぶれは **0.5〜4.2%**。**素材は原因ではありません。**
- カメラの 1 コマあたりの変化は最大 **2.0px**。**カメラも原因ではありません。**
- **1 頭だけ**描くと、斜め前でも脚は動いて見えます。
- **12 頭のまま**描くと、
  - **真横** → 同じ大きさの切り抜きが重なり、帯にしか見えない（オーナー評「競艇のボートみたい」）
  - **斜め前・斜め上** → 奥行き方向に積み重なり、脚が互いに隠れて上下動だけが見える（「ぴょんぴょん跳ねているだけ」）
- **先頭 4 頭だけ**を真横で撮ると、**合格区間と同じ質**になります。
  ただし本番では残り 8 頭も数 m 以内にいるため、**カメラをどれだけ絞っても画面に入ります。**

★結論: **横に散っていない限り、どの画角でも成立しません。描画側でできることは尽きました。**

---

## 4. 照会したいこと

### Q-LANE-01（本題）
レース中盤（およそ発走 150m 〜 直線入口）で、**12 頭の横の広がりを 8〜12m 程度に保つ**よう
`laneAt` を変更してよいですか。

私が触れないと判断した理由:

- `w` は**レース結果の一部**です（D-065 / D-071）。Provably Fair の連鎖と V-18 に入っています。
- 描画側で `w` を作り直すと、**サーバー権威（憲法 3）と決定論（憲法 4）の両方を壊します。**
- 「見た目のためだけに別の `w` を描く」案も考えましたが、**画面と結果が食い違う**ので出しません。

### Q-LANE-02
横に散らすと、**内ラチ有利**という現在の競走の理屈が変わりますか。
`laneAt` が距離の増加（外を回る不利）に効いているなら、散らすことは**着順に影響します**。
影響するなら、V-1 / V-2 系の再検証が要ります。**どこまで再実行すべきかご指示ください。**

### Q-LANE-03
散らし方の候補（どれを採るかご裁定ください）:

- **(a)** 発走直後の広がり（11.3m）を、直線入口まで**保つ**。`SETTLE_M` を実質無効化
- **(b)** 中盤だけ 8m 程度に緩め、3〜4 角で内に寄せる（実際の競走に近い）
- **(c)** 脚質ごとに内外を割り当てる（逃げ＝内、追込＝外）。**隊列が意味を持つ**

私の推しは **(b)** です。合格した 2 区間（発走直後 11.3m・直線 9〜12m）と連続し、
かつ「4 角で内を突く」という競走の見せ場が残ります。ただし **(c)** は Q-LANE-02 の影響が最も大きいです。

### Q-LANE-04
仮にエンジンを変えない裁定になった場合、**中盤は真横をやめて「先頭数頭の寄り」で通す**ことになりますが、
残りの馬が画面に入る以上、根本的には直りません。
その場合、**中盤の映像品質は現状が上限**という理解でよいですか。

---

## 5. 添えた道具（レビュー側で再実行できます）

| 道具 | 何が出るか |
|---|---|
| `npx tsx tools/shot-race-at.mjs --from 9 --to 33 --step 3` | 実レースの各秒の静止画・馬の大きさ・**横の広がり** |
| `npx tsx tools/verify-horse-motion.mjs` | 馬を固定してコマだけ送ったときのぶれ（素材由来かの切り分け） |
| `npx tsx tools/_filmstrip.mjs 12 8` | 連続 8 コマを先頭馬の周りだけ拡大（`SOLO=1` / `TOPN=4` / `FORCE=<ショット>` で切り分け） |
| `npm run verify:billboards` | 世界に置く看板の実寸 |

---

## 6. 併せて報告（この照会とは別件・すでに直したもの）

- 俯瞰素材に**背景の緑が 59.7% 残ったまま**入っていました → 0.00%
- 背後の素材が全 5 セット中**最悪**（接地点のばらつき 11.6%）→ シート方式で 8.0%
- 4 角正面が**ズームの下限に頭打ち**し馬が画面の 10.6% → 全区間 22.0%
- 発馬機が**実寸の 1.65 倍の高さ**で、馬の頭が扉に隠れ脚だけ見えていた → 3.15m（実物およそ 3.4m）
- 私の監査道具が**古い素材**を読み、**横視点用の背景**を貼っていた（＝オーナーと別の画を見ていた）→ 修正

## 7. 未解決（描画側の作業。着手の可否をご指示ください）

- 俯角の強いカット（空撮・斜め上・4 角）は**地平線が画面外に出て背景がほぼ緑一色**になります。
  カメラでは直りませんでした（俯角を 27m → 13m に下げても地平線は入らず）。
  `world-textured.ts` 側（内馬場・走路の描き分け・生垣の質）の作業になります。
- ナレーターの立ち絵が**未納品**です。現在は仮置きの写実的な人物写真で、
  **肖像・パブリシティの論点**があると考えます（弁護士ゲート対象かの判断をお願いします）。
