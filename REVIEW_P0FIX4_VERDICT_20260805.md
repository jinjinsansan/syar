# P0-fix4 レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-05（レビュー側 Claude）
**対象**: `REVIEW_REQUEST_P0FIX4_20260805.md` / `REPORT_P0FIX4_20260805.md`（commit `aef42b0`）
**判定**: **PASS — P0（遺伝エンジン）完了**

---

## 0. 結論

| 項目 | 判定 |
|---|---|
| `npm run verify` の出力 | **CONFIRMED** — 報告書 §3 と全数値一致（所要176.8秒） |
| ユニットテスト93件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| I-1 切断補正 | **CONFIRMED** — V-2e が 0.92〜1.09倍（P0-fix3 は 0.84〜0.91倍） |
| I-2a/b/c | **CONFIRMED** |
| I-3 本番経路での clamp 固定 | **CONFIRMED** |
| I-4 CLI 上書きでの再導出 | **CONFIRMED** — レビュー側の独立実測で 既定66/341 → 回帰0.05で34/177 → 創始SD200で131/341 |
| I-5 sd の追随テスト | **CONFIRMED** |
| **Q1 丈夫さ sd = 65 vs 66** | **開発側が正しい。正典の 65 はレビュー側の丸めミス**（§2・是正済み） |
| 自己申告した逸脱3件 | **すべて承認**（§3） |

**合格基準17項目すべて成立。P0 の遺伝エンジンはこれをもって完成とする。**

---

## 1. レビュー側が実際に実行したもの

```bash
npm test              # 93件 PASS
npm run typecheck     # exit 0
npm run verify        # 報告書 §3 と全数値一致・176.8秒
# I-4 を独立実測（3条件）
npm run simulate -- --generations 5 --seed 42 --quiet --json out/i4.json [--set-balance / --set-founders]
```

加えて丈夫さ sd の導出を独立検算し（§2）、並列サブエージェントで `git diff c885ecf 07c0188` を file:line 監査した（I-3 は実際にコードを書き換えて確認）。

---

## 2. Q1 — 開発側が正しい。正典の 65 はレビュー側の丸めミス（是正済み）

独立検算:

```
丈夫さ: 100 × √(2×0.20 − 0.20²) ÷ 0.9156 = 100 × 0.6 ÷ 0.9156 = 65.531 → 四捨五入 66
```

開発側の指摘どおり **66 が正しい**。私が正典に書いた 65 は、65.531 を四捨五入せず切り捨てたもの。

**さらに開発側の逸脱2（clamp は「丸める前の sd」から算出）が、この判定の決め手になった。** 表の clamp を全数検算すると:

| 形質 | 生の sd | clamp = round(生sd × 1.667) | 正典の表 |
|---|---|---|---|
| 丈夫さ | 65.531 | 109.2 → **109** | 109 ✓ |
| 気性 | 9.830 | 16.4 → **16** | 16 ✓ |
| 芝/ダート適性 | 13.242 | 22.1 → **22** | 22 ✓ |
| distance_center | 340.508 | 567.5 → **568** | 568 ✓ |
| distance_range | 113.503 | 189.2 → **189** | 189 ✓ |

clamp は5件とも一致し、**ずれていたのは丈夫さの sd ただ1つ**だった。つまり私は clamp を生値から正しく計算しながら、sd だけ丸め方を誤っていた。開発側が「表の clamp 109 は 65.531 由来なので sd 65 のほうがおかしい」と**表の内部整合から逆算して**指摘した点は的確である。

**正典 §6.4 の表と §13.1 を 66 に是正し、あわせて丸めの規約を明文化した**:

> `sd` は導出値を四捨五入。`clamp` は**丸める前の sd** × 1.667 を四捨五入する。**表の値は導出式の出力であり、両者が食い違ったら式が正。**

---

## 3. I-1〜I-5 と自己申告への回答

| 項目 | 判定 |
|---|---|
| **I-1** 切断補正 | **承認**。解析係数 0.915604 を独立検算で確認したうえで実装している点が良い。V-2e は全形質が 1.0 の ±0.1 以内に収まり、下限0.8までの余裕が 0.04 → 0.12 に広がった。**「浮動ではなく clamp 切断」という診断の訂正が正しかったことが、補正の効果そのもので裏づけられた**（浮動が原因なら補正しても改善しないはずだった） |
| **I-2a** 二重管理の解消 | **承認**。`MUTATION_CLAMP` を §13.1 側に移して単一の出所にした整理も良い |
| **I-2b** 0中心形質の扱い | **承認**。指示書の二択に対し「**判定不能なら `pass: false`**」を選んだのは正しい。「対象外と宣言する」方式は将来の形質追加時に宣言し忘れる余地が残る。**静かな素通りより落とす**という判断は、本プロジェクトでこれまで繰り返し見つけてきた失敗（テストが実質何も守っていない）への正しい構え |
| **I-2c** 表現型併記 | **承認**。併記して即座に「**故障率の分母は表現型の 636 であって genotype の 650 ではない**」が見えたのは、この指示の狙いそのもの。P1 の `INJURY_BASE` 較正で必ず使う |
| **I-3** 本番経路での固定 | **承認**（§4） |
| **I-4** CLI の再導出 | **承認**。レビュー側の独立実測でも報告値と一致。上書きの有無にかかわらず毎回導出し直す設計にした点が良い。`sweep-distance.ts` が距離2形質だけ手で組んでいて D-012 以降は丈夫さ・気性・適性が置き去りになっていた、という自己発見も価値がある |
| **I-5** sd の追随テスト | **承認** |

### 3-1. I-3 の受け止め方について

前便で指摘した「単体を固定することと経路を固定することは別物」に対し、**観測可能な境界で式を判別する**という解き方を選んだのは的確だった:

```
正しい式  child = 900 + clamp(X, ±150) − 90   → 値域 [660, 960]
誤った式  child = 900 + clamp(X − 90, ±150)   → 値域 [750, 1050]
```

上限（誤式なら超える）と下限（誤式では到達不能）の**両側**を固定しているため、純関数を直しても呼び出し側を書き換えても落ちる。**片側だけの境界テストなら、どちらか一方の改変を見逃した**はずで、H-1 の対照テストと同じ発想が効いている。

---

## 4. P0 完了の宣言

| フェーズ | 内容 | 結果 |
|---|---|---|
| P0 | 遺伝エンジン + 100世代シミュレータ（ローカルCLI・UIなし） | **完了** |

到達した状態:

- **遺伝機構**: 2アレル継承・隔世遺伝6%・大物覚醒1.5%（能力5種のみ）・突然変異・平均回帰・Wright近交係数・インブリード補正・近交弱勢・ニックス・生産上限
- **検証**: V-1 / V-2a（平坦化）/ V-2b（天井余裕）/ V-2c（長期健全性）/ V-2d（非能力形質の水準）/ V-2e（非能力形質の分化）/ V-3 の7系統を、**4シード × 100/300ゲーム内年・実運用規模800頭**でゲート
- **決定論**: シード固定で完全再現。乱数・時刻はすべて注入
- **定数管理**: 全定数が `FOUNDERS` と平衡式から導出され、CLI 上書き時も再導出される。リテラル固定テストで正典との一致を保証

### この4便で見つかった重大な問題（記録）

| 便 | 問題 | 発見者 |
|---|---|---|
| P0 | V-2 が構造的に達成不能（選抜応答に対抗する機構が正典に無い） | 開発側 |
| P0 | 300世代で天井飽和し V-1 が 3.24% に崩壊（V-2 が測るべきものを測っていなかった） | **レビュー側** |
| P0 | 大物覚醒が単調上昇ラチェット（インフレの約14pt） | **レビュー側監査** |
| P0-fix | 丈夫さが 650→450 へ恒久移動（回帰中心を値域一律にしたため） | **レビュー側監査** |
| P0-fix2 | 距離系の回帰率 0.01 は誤り（sd を据え置いたことによる誤結論） | **開発側（自己訂正）** |
| P0-fix2 | clamp 位置の実装ずれ | **開発側（自己発見）** |
| P0-fix2 | 芝適性のSDが 0.71倍に収縮（どのゲートにも掛からなかった） | **レビュー側監査** |
| P0-fix2 | clamp の一次回答が誤り（平衡式の前提を壊す） | **レビュー側（自己訂正）** |
| P0-fix3 | clamp のテストが純関数内部しか守っていない（呼び出し側は素通り） | **レビュー側監査** |
| P0-fix3 | CLI 上書きが sd 導出をバイパス（誤結論を生んだのと同じ罠） | **レビュー側監査** |
| P0-fix4 | 正典の丈夫さ sd が丸めミス | **開発側** |

**三者のいずれか一方だけでは、この半分も見つからなかった。** 開発側が自分の結論を2度訂正し、レビュー側も2度訂正した。相互チェックが機能した記録として残す。

---

## 5. 次便 — P1（レース判定エンジン + モンテカルロ検証）

引き継ぎ事項（**基準線は本便 P0-fix4 の数値**。P0-fix3 の値と比べると切断補正の系統差で解釈を誤る — 開発側 Q2 の指摘のとおり）:

1. **選抜 h²=0.3 の置き換え** — 実際のレース結果に基づく選抜へ
2. **選抜下での分化の維持** — V-4〜V-6 較正時に `distance_range` と V-2e の推移を確認。**万能型への収束**（大物覚醒の限定で発生源は断ったが、選抜圧は新たな押し上げ要因になりうる）
3. **`INJURY_BASE` の較正** — **表現型**の丈夫さ（近交込みで 636 前後）を分母とする。genotype の 650 ではない
4. **V-2d/V-2e の維持** — レースエンジン導入後も P0 のゲートを回し続ける（回帰テストとして）
5. **大物覚醒の演出**（P4 への申し送り）— 対象が能力5種のみなので、演出は能力を明示する形

---

## 6. 教訓の記録

1. **表の内部整合は誤りの検出器になる**。丈夫さの sd が誤りだと分かったのは、clamp 5件が生値由来で一致していたから。**同じ数値を2通りの経路で書いておくと、片方の誤りが露見する**
2. **境界テストは両側を押さえる**。上限だけ／下限だけでは、改変の片側を見逃す（I-3）
3. **「判定不能」は PASS でなく FAIL に倒す**（I-2b）。静かな素通りは、このプロジェクトで最も繰り返した失敗パターンだった
4. **導出をバイパスする経路を数える**。定数を導出で守っても、上書きAPIが導出を回避すれば同じ事故が起きる（I-4）

---

## 7. 【追補】コード監査の結果 — I-3 の書き戻し実験は両方とも落ちた

前便で私が行った実験を監査側で再現した（いずれも復元済み）:

| 実験 | 結果 |
|---|---|
| ① `mutateAllele()` 本体を旧「合計 clamp」形に書き換え | **4件 FAIL**（I-3 の3件＋純関数テスト1件） |
| ② `mutateAllele()` は正しいまま、**呼び出し側**（`genetics.ts:242`）に旧形式をインライン | **I-3 の3件が FAIL**（純関数テストは通過＝**前回の穴が正しく塞がれたことの裏取り**） |

**I-3 は目的を達成している。** なお I-3 の3本目（平均795〜825）は誤式でも 826.3 とギリギリでしか落ちないため、実質効いているのは上限960と下限750の2本である（それで十分）。

I-1 の解析係数 0.915604 も監査側で独立検算し一致。**丈夫さ sd = 66 が正**（正典の 65 は自身の clamp ±109 とも矛盾していた）という判断も追認された。

### 7-1. 🔴 MEDIUM-1: 切断係数が clamp比に追随しない — I-2a が狙った二重管理が残っている

`CLAMP_TRUNCATION_FACTOR = 0.9156` は **k = clamp/sd の関数**だが、CLI 上書きで k が変わっても係数は既定のまま:

| 経路 | k | 正しい係数 | 実際の丈夫さ sd | 導出が要求する sd |
|---|---|---|---|---|
| `--set-genetics MUTATION_SD=45` | 3.333 | 0.9992 | 66 | **60**（−10%） |
| sweep（sd 75） | 2.000 | 0.9594 | 66 | **63** |
| sweep（sd 100） | 1.500 | 0.8823 | 66 | **68** |

`balance.ts` のコメントは「比を変えたら再計算せよ」と警告しているのに、**コード自身にその警告を破る経路がある**。さらに `config.test.ts` は `clamp/sd ≈ ratio` しか見ないため、**sd が誤ったままテストが緑になる**（欠陥を固定してしまっている）。

**これは I-4 とまったく同じクラスの問題であり、P1 で定数を振って較正するときに確実に踏む。** J-1 として是正を指示する。

### 7-2. 🟡 MEDIUM-2: 表現型の気性が既定設定で V-2d の窓を超えている

I-2c の併記が早速検出した。既定 `npm run simulate`（200頭・100年・平均F=0.0404）では:

| seed | genotype 乖離 | 表現型 |
|---|---|---|
| 42 | +3.10% | 50.3→56.5（**+12.3%**） |
| 7 | +4.27% | 49.9→57.5（**+15.2%**） |
| 31337 | −0.46% | 50.1→55.2（**+10.2%**） |

報告書の「P0 では平均F が小さいので無害」は、800頭/300年（F=0.011）の実行のみを根拠にしており、**200頭の高速反復モードでは既に ±10% の外**である。

**ただしレビュー側はこれを欠陥とは判定しない。** 表現型の上振れは §6.5 が定めたインブリードの代償（`temper += F×120`）がそのまま出たもので、**設計どおりの挙動**である。集団が小さければ近交が進み、気性が荒くなる — これはゲームとして正しい。

したがって:

- **V-2d は genotype 判定のまま維持**（遺伝的な水準ずれの検出が目的であり、表現型を混ぜると近交の影響と区別できなくなる）
- **P1 の判断事項として引き継ぐ**: `INJURY_BASE` の較正は表現型を分母にする（既に指示済み）ことに加え、**平均F と「表現型−genotype の乖離」を監視項目に加える**。乖離が広がることは近交が進んだことの指標であり、故障率と気性難の実効値が設計値から離れることを意味する

### 7-3. LOW 2件

| # | 内容 | 対応 |
|---|---|---|
| LOW-1 | **`basis` 分岐にテストが1件もない**。I-2b の決定（判定不能なら `pass:false`）が何にも固定されていない。**I-3 で受け入れた「経路を固定せよ」がそのまま当てはまる** | J-2 |
| LOW-2 | `sweep.ts` の r=0 構成で `derive()` が sd 0 を返し、**非能力6形質の変異が完全停止**する（創始値で凍結）。印字列は能力形質のみなので報告値には出ないが無言の縮退 | J-3 |

### 7-4. 判定への影響

**いずれも出荷既定の挙動には影響しない**（既定 k=1.667 では係数が一致し、`verify` は既定経路を通る）。したがって **P0-fix4 の PASS は維持**する。ただし MEDIUM-1 は P1 の較正作業を確実に壊すため、**P0 の締めとして小さな1便（P0-fix5）を挟む**。
