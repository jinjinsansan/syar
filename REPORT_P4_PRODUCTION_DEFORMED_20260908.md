# 記録 — ★本番のレース（桜星賞）にデフォルメ馬を当て込む（2026-09-08）

**発行**: 開発側 Claude
**状態**: ★本番 `/race` で動いています。★絵柄の合否は ★**オーナー判断**。

---

## ★0. オーナーの指示

> ★真横からのカメラで、馬がこちらを向いて、横に移動するというありえない動きがある
> ★以前の 2D で作った桜星賞の競馬場コースに当て込んでみないと判断出来ない
> ★実況中継や、真上からの位置、画面上の数字だけが動く順位など全て再現し、
> ★このデフォルメの馬で走らせたい

---

## ★1. ★「正面なのに横へ滑る」— ★検証台の限界でした

★検証台 `/rig-lab/sprite` の背景は ★**直線の視差板 1 枚**で、奥行きがありません。
★正面のカットは ★**近似**で置いていたため、こちらを向いた馬が横へ動きました。

→ ★本番 `/race` は ★**透視投影**なので、★そのまま解けます。★当て込んだら消えました。

---

## ★2. ★どう当て込んだか（★新しい工程は作っていません）

```
★① 素材を本番の「原版」の形で /art/ へ写す   tools/publish-deformed-art.mjs（★新規）
★② 既存の焼き工程に渡す                      tools/bake-race-frames.mjs（★既存・変更は 2 行）
★③ 素材名の候補の先頭に新しい版名を足す      ★外せば元の素材へ戻ります
```

⚠️ ★**毛色の焼き分け・アトラス化・anchor の算出は、すべて既存の工程がやります。**
★私たちは ★**原版を置くだけ**です。

---

## ★3. ★直した不具合

### ★① 勝負服が 12 頭とも「青」だった

```
★本番は勝負服を ★**彩度の低い（無彩色の）画素**から探します
   条件: spread（最大−最小）が ★帽子 62 以下 ／ 上着 34 以下
   ★同じ所の注記: 「素材の勝負服は ★**無彩色の灰／白**で作らせている（生成プロンプトで指定）」

★私たちの素材は勝負服が ★**青**（彩度が高い）
   → ★条件に 1 画素も当たらず、★青が焼き込まれたまま 12 頭とも同じ色
```

→ ★**素材は作り直しません。** ★本番へ写すときに、★青を ★**明るさだけ残した灰**へ戻します。
★検証台側は色相で青を選ぶ方式なので、★そちらの素材は変えていません。

### ★② 撮影道具だけが旧素材を描き続けていた

★`tools/shot-race-at.mjs` が ★**素材名を直書き**していました。
★焼く工程は `pickSet`（★先に見つかった名前）で選ぶのに、★撮影道具はそれを通りません。
→ ★差し替えても ★**その道具の絵だけ古いまま**でした。★同じ選び方に直しました。

⚠️ ★**列挙を 2 か所で持つと、片方だけが古くなります**（★R-29 と同じ形）。

---

## ★4. ★出ているもの（★実機で確認）

```
★コース        スターパーク競馬場 芝1600m 左回り（★コーナーも本番の透視描画）
★実況中継      実況文 ＋ 実況者の立ち絵
★コース図      真上からの位置・残り距離
★順位表        ORDER・上位 5 頭・馬身差
★画面上の数字   上部の枠順の並び
★馬名プレート   自馬・先頭・3 番手
★勝負服        馬ごとに違う色 ／ ★ゼッケンは枠色どおり
★毛色          7 色に焼き分け
```

---

## ★5. ★残っていること

| # | | |
|---|---|---|
| ★① | ★個体タイプは ★**型 A だけ**が本番に入っています | ★本番の焼き工程は「1 体型 × 毛色 7 色」の作りです。★3 体型を通すには `SETS` を増やす必要があります |
| ★② | ★斜め後ろ・俯瞰は旧素材のまま | ★台本 v6 では ★**1m も描かれない**ので、★急ぎません（★`broadcast-v2.ts` :1688） |
| ★③ | ★1 完歩の値 | ★本番 7m ／ この素材の実測 3.818m。★滑りが気になるかは ★**オーナーの目**で |

⚠️ ★`ASSET_VERSION` を上げないと ★**ブラウザが古い素材を出します**（★今回 58 → 60）。
