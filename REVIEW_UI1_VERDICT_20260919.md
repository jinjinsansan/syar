# 【STAR】裁定 — UI1-1〜7 を受理／`lib/` も対象に／画面は最低限を置いて進める

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-19・レビュー側 |
| **対象** | `310c6ff`（UI1・`0037`）／ `84dd67a`（TG-1/TG-2）／ `4465bee`（制御文字の道具） |

---

## 0. 判定

✅ **UI1-1〜7 を受理（条件なし）。**

| # | 判断 |
|---|---|
| **`lib/` の中身** | ★**広げてください**（§4）。★13 ファイルなので安いです |
| **`/setup`・`/entry` の見た目** | ★**最低限を置いて進めてください**（§5）。★**「あるか無いか」は機能、「どう見えるか」が [EYES]** |
| **D-115 の訂正** | ⏳ ★**オーナー承認待ち**（★まだ出ていません） |

✔ **レビュー側も独立再実行** — `initial-horse-selection` ＋ `tool-guard` ＋ `rpc-guard` の **47 件すべて緑**。

---

## 1. ✅ UI1 — 受理

| # | ✔ |
|---|---|
| **UI1-6** | `order by random()` ＋ `for update … skip locked` ／ ★**憲法 4 の対象外である理由を註記に書いた**（★指示どおり） |
| **UI1-7** | 検査 13 件。★**「固定順で並べていない」「`auth.uid()`・ハッシュから導いていない」「候補の条件を書き写していない」**を見ている。★噛むことも確認（`order by c` で赤・`skip locked` を外して赤） |
| 述語 | ★**`initial_horse_candidates()` の 1 か所**が持ち、`is_initial_horse_candidate` はその包み（**D-052**）。★**`0033` の条件を 1 文字も変えていない** |
| `0031:125` の註記 | ★**適用済みで書き換えられない**ので、`0037` の註記と `comment on` が訂正にあたる形 — ★**正しい扱い**です |

---

## 2. ★★§3 の ⚠️ — **今日いちばん価値のある自己報告です**

> ★**下見の 300 回では段 20 が +3.68 SE に見えました**（期待 1.5・実測 6）。2,000 回では +0.63 です。
> ★**小さい期待度数で正規近似を読んだだけでした** — ★**数字を先に出さずに試行を増やして正解でした**

★★**これは、私が ES-6 で踏んだ穴と同じ家族です** — ★**「測定の精度で説明がつく差を、差として読む」。**
★**違うのは、★開発側が「出す前に」気づいたこと**です。★**私は出してから撤回しました。**

★**そして私が UI1-7 に「★簡易でよい」と書いたのに、★簡易で済ませずに試行を増やしたのが正しい**です。

✔ **本測の読み方も正確です** — 最大のずれ **+1.91 SE**（2 SE 超なし）／★**向きの内訳 上 11 / 下 13**（★**同じ向きに揃って外れる形ではない**）。★**「ずれの大きさ」だけでなく「向きの偏り」を見たのが良い。**

---

## 3. ★§4 の 3 件 — すべて「測ってから」直している

| # | |
|---|---|
| **4-1** | 🔴 ★**`skip locked` は自分のトランザクションが掴んだ行を除外しない** — ★**同じ馬が 2 回引かれて気づき、「非復元抽出」の前提を復元抽出に直し、★SE の式から有限母集団修正を外した。** ★★**前提が違うと分かったら、統計の式まで直した**のが正しい。★**錠が仕事をするのは別の接続との間だけ**という切り分けも正確で、★接続 2 本で別に測っている |
| **4-2** | ★**「読むだけなら本番でもよい」は、★錠を掴む読み方には当てはまらない** — ★**良い一般化**です。★`for update` で数千行を数分掴むものを `STATE_CHANGING` に移し `assertNotProduction` を入れた判断に同意します |
| **4-3** | 🔴 ★`rpc-guard` の `auth.uid()` 検出が `comment on` の**文字列リテラル**を拾った — ★**`0031` で起きたのと全く同じ形**（★2 回目） |

### 3-1. ★4-3 について 1 つだけ — **なぜ 2 回目が起きたか**

★1 回目（`0031`）で `stripFunctionComments` を**新設した**のに、★**`rpc-guard` の `auth.uid()` 検出は `blankComments` のままでした。**
→ ★★**1 か所を直したとき、同じ形の他の箇所を洗っていなかった。**

| ★教訓（両方に効きます） |
|---|
| ★**同じ穴を塞いだら、★同じ道具・同じ判定を使っている他の箇所を洗う。** ★**「直した」は「その 1 か所を直した」でしかない** |

✅ ★**なお、構造の側で直した**（註記の語を言い換えなかった）のは正しく、★**`errcode = '…'` を見ている検査は `blankComments` のまま残した**という切り分けも正確です（★**すべてを一律に変えなかった**）。

---

## 4. ★`lib/` の中身も対象に広げてください

★開発側の「★**`lib/` などディレクトリの中身は対象外のまま。★残っている穴として註記に書いた**」は正しい認識です。

| # | 裁定 |
|---|---|
| **TG-3** | ★**`tools/` 配下を再帰で対象にする**（`lib/` の 13 ファイルを含む）。★**分類に「部品」を 1 つ足す**（★`NOT_A_TOOL` と同じ形で、理由を書かせる） |

★**理由**: ★**「部品だからディレクトリに置く」ですり抜けられる形は、★拡張子で絞るのと同じです。** ★**今日、拡張子の列挙をやめたばかりです。**
★**13 ファイルなので安く塞げます。** ★`tools/lib/_probe.mjs` を置いて赤になることを確かめてください。

---

## 5. ★`/setup`・`/entry` の見た目 — ★**最低限を置いて進めてください**

### 5-1. ★線の引き方

| | |
|---|---|
| ★**「あるか無いか」** | ★**機能です。★開発側が置いてよい**（★読み込み中・失敗の表示が**無いと、結線が正しいかを確かめられません**） |
| ★**「どう見えるか」** | ★**[EYES] です。★デザイナー便**（2026-09-15 のオーナー指示） |

★**オーナーの優先順位は「UI の本番結線が先」**です。★**デザイナー便を待つと、結線の確認ができません。**
★そして ★**失敗が黙って消える形は R-16** です — ★**表示が無いこと自体が欠陥**になります。

### 5-2. 条件

| # | |
|---|---|
| **UI1-8** | ★**仮であることをコードに明示する**（★次に読む人が「これがデザインだ」と思わないように）。★**デザイナー便への申し送りにも入れる** |
| **UI1-9** | ★**失敗は握り潰さない**（`ST001`/`ST002` は訳す・★**それ以外は原文のまま**。✔ `entry-repo.ts` が既にその形） |

✅ ★**`my_horses` が自分の馬だけを返すことと `enter_race` の資格判定の実測（△ 2 件）を同じ便で潰す**のは、★**口座が作れるようになった今が正しいタイミング**です。

---

## 6. ★★§6 の制御文字の道具 — **今日いちばん美しい出来事**

> 🔴 ★**この道具は最初に自分を赤くしました** — ★註記に書いた「例」が**実体の不可視文字になって混ざっていました**（★**まさに探している壊れ方**）

★★**探している壊れ方が、探す道具自身に入っていた。★これ以上の実証はありません。**
✅ ★**探す文字をコードポイントから組み立て、ファイルに実体を一切置かない**形に直したのも正しい（★`0034` の「検査が自分の穴を捕まえた」と同じ家族）。
✔ 全ソース **995 ファイルで 0 件**／`--changed` で未コミットの変更だけを見る／★**走査が空なら落とす**（R-21）。

★**私も使います。**

---

## 7. ⏳ D-115 の訂正 — ★**まだ承認が出ていません**

★オーナーに諮っていますが、★**承認はまだです。** ★出しだいお伝えします。
★**結論（通知は自馬だけ）は変わらず、根拠の数字（480R → 240R）だけ**なので、★**実装をここで止める必要はありません。**

---

## 8. 開発側が次にすること

| 順 | 中身 |
|---|---|
| 1 | ★**UI1 の残り**（`/setup`・`/entry` の結線 ＋ **UI1-8・UI1-9**）＋ ★**△ 2 件の実測を同じ便で** |
| 2 | **TG-3**（`lib/` も対象に） |
| 3 | **D-116 の 4 層**（★成長段階を年齢から導く純関数 → 「最近の変化」→ 調教師コメント） |
| 4 | UI-2〜UI-4 → **T-11** → **AL-11** |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
