# 報告 — ★A-10: 発走のカメラでゲートと地面が交差する（2026-08-31）

**出どころ**: `DEV_INSTRUCTIONS_P4_COURSE_MODEL_20260831.md` §1-0（★最初にこれ）
**ブランチ** `p4/race-30sec-cuts` / 起点 `425d47b`

---

## ★0-1. 一行で

★**真因は芝の縞でした。** ★縞は走路の外へ 60m まで塗られており、★**その縁がゲートの真上を横切っていました。**

## ★0-2. 提出物・コミット・変更範囲

| コミット | 中身 | 変更範囲 |
|---|---|---|
| `4e61e24` | ★レース選択のプルダウン（★オーナー指示「毎回コピペ出来ません」） | `page.tsx` / `race-setup.ts` / `graded-races.test.ts` |
| `56df6b8` | ★A-8 の起票・地形の実測・照会・道具 | `RESIDUALS` / `classification.mjs` / `shot-race-at.mjs` / `QUESTIONS_*` |
| `d25d858` | ★**受領**（レビュー側の裁定・指示書・正典の起票）★開発側は 1 文字も書いていません | `STAR_SPEC` / `REVIEW_*` / `DEV_INSTRUCTIONS_*` |
| `c22bea5` | ★**A-10 の直し** | `mow-stripes.ts` / `mow-stripes.test.ts` |

⚠️ ★`apps/web/package.json`（ポート 3210）は**混ぜていません**（★オーナーの作業）。★未コミットのまま残ります。
⚠️ ★`tools/_*.mjs` は `.gitignore` で追跡外です。★この便で **4 本**（`_cutgeom` / `_terrain` / `_gatecross` / `_railoverlay`）作り、★**全部登録簿に載せてあります**。

★**着順・タイム・着差・払戻・位置には触れていません。** ★変更は `@star/render` の描画層のみです。

---

## ★1. 再現（★指示書の手順どおり）

```powershell
cd V:\dev\Cusor\star\apps\web ; npm run dev            # ★ポート 3210
npx tsx tools/_realshot.mjs --out g1 --query "race=g2-aone"  --sec 10
npx tsx tools/_realshot.mjs --out g2 --query "race=g1-ousei" --sec 10
```

★**両鞍とも再現しました。** ★裁定 §5-4 の切り分け（★A-8 とは独立・鞍にも走路の形にも依存しない）と一致します。

---

## ★2. 真因

### ★2-1. 数が食い違っていました

| | 値 | 出どころ |
|---|---|---|
| ★**内馬場の芝を描く範囲** | ★**w = −150** | `INFIELD_LAYOUT.infieldInnerW`（`infield.ts:43`） |
| ★**芝の縞を描く範囲** | ★**w = −60 〜 幅+60** | `overhangM ?? 60`（`mow-stripes.ts`） |

★**縞の面のほうが、地面より 90m 小さい。** → ★**縞の縁が「世界の何でもない線」**になります。

★註記は「内馬場・外の芝にも縞は続いている」でした。★**外へ伸ばす意図は正しく、伸ばし方が任意の 60m だった**のが誤りです。

### ★2-2. その縁がどこを通るか（★実測・`tools/_gatecross.mjs`）

★発走のカメラ（`start-front`）・表示 10s・seed 42:

```
      s        x        y     深さ    ★ゲート(x 239〜678 / y 232〜323)の上を通るか
    -600      490      262     651m   ★★通る
    -500      522      263     553m   ★★通る
    -400      568      265     456m   ★★通る
    -300      638      267     358m   ★★通る
```

★裁定の 3 点と対応します:

| 裁定 §5-4 の記述 | ★正体 |
|---|---|
| ★「芝の境界線がゲートの足元を斜めに横切る」 | ★**縞の縁 w=−60** |
| ★「ゲートの奥にもう 1 本の柵が水平に走る」 | ★**同じ線**（y 262→267 とほぼ水平） |
| ★「右の白柵がゲートの右端と重なり、そのまま手前へ抜ける」 | ★**内ラチ w=0**。★ゲートは w=0.5〜15.3 なので ★**幾何としては正しい** |

### ★2-3. 層ごとの差分（★目で見ずに特定した経路）

★`tools/shot-race-at.mjs` に `--no-mow` / `--no-poles` / `--no-gate` を足し（★既定は画面と同じ「入り」・R-31）、★同じコマを撮って画素を比べました:

| 止めた層 | 変わった画素 | 範囲 |
|---|---|---|
| ★**刈り目** | ★**90,920（9.87%）** | ★x **0〜1279**（全幅）／ y 262〜446 |
| ★ハロン棒 | ★**0** | ★このカットでは描かれていない |
| ★ゲート | ★9,773（1.06%） | ★x 239〜678 ／ y 232〜323 |

---

## ★3. ⚠️ ★外した見立て 2 件（★記録として残します・R-25）

★**どちらも「拡大した静止画を目で見た」ところから出た仮説で、測ったら外れました。**

| ★見立て | ★実測 | ★判定 |
|---|---|---|
| ★ゲートの底辺が水平（`bottom = (a.y+c.y)/2`）なので地面と交差する | ★地面の線の傾き **Δy = 4.7px** → ★片端の浮きは **2.3px** | ★**否定**。★近似の粗さは実在するが、この大きさでは画面に出ない |
| ★白柵が垂直に断ち切られている | ★5 行の画素走査で ★**該当する段差なし** | ★**否定**（★誤読） |

⚠️ ★**台帳 B-3 と同じ形**（★拡大で判定して外す）を**2 回続けました。** → ★**層ごとの差分に切り替えてから当たりました。**
★**申し送り: 絵の破綻は、拡大して見るのではなく層を止めて差分を取ること。**

---

## ★4. 直し

★`mow-stripes.ts` の `overhangM` の既定を ★**60 → 0**。★縁が**内ラチ・外ラチと一致**します＝★世界に在る線になります。

⚠️ ★**「60 → 200 に広げて画面外へ出す」は採りません。** ★見えにくくしただけです（台帳の作法）。

⚠️ ★**内馬場・外の地面に縞を戻したいときは、その面の範囲を渡して別々に呼ぶこと。** ★1 回の呼び出しで
　★走路と内馬場をまとめて塗ると、★**内馬場のダートコースと生垣の上にも緑の縞が乗ります**（★A-5 の家族）。★註記に書いてあります。

★**縞は消えていません**（★実測: 縞を全部止めると **10.19%** の画素が変わる＝★描かれている）。★消えたのは**走路の外の縞だけ**です（★旧→新で 7.05%）。

---

## ★5. 検査

### ★5-1. 足したもの（`mow-stripes.test.ts` 6 → 9 件）

| 検査 | 何を守るか |
|---|---|
| ★縞は走路の外に出ない | ★3 カットで、★投影を頼んだ `w` の**最小 0 / 最大 `widthM`**。⚠️ ★画面座標ではなく**世界座標**で見ます（★画角に依らない） |
| ★**旧形（`overhangM: 60`）に戻すとこの検査が落ちる** | ★R-14。★min = −60 / max = 幅+60 になることを固定 |
| ★縁を閉じても帯は今までどおり出る | ★**機構ごと止めていない**ことの担保（R-16） |

### ★5-2. ⚠️ ★書き換えた既存検査 2 件（★数を合わせにいっていません）

| 検査 | ★旧が測っていたもの | ★なぜ崩れたか | ★新 |
|---|---|---|---|
| 「周期は実寸で一定」 | ★2 カットの**描かれた本数**の大小 | ★本数は間引き（外接矩形）に依り、★**画面外の帯も通ります**。★縁を絞ると顔ぶれが変わる（15 対 18） | ★**境目の `s` が周期の倍数**であること／★周期を倍にすると境目が減ること |
| 「注視点が進むと帯も進む」 | ★明暗を並べた**文字列**の比較 | ★**本数と先頭の明暗が偶然変わることに乗っていた**だけ。★本数が揃った途端、機構は無傷なのに同じ文字列になって落ちた（R-16 の家族） | ★**境目の `s` そのものが注視点とともに動く**こと |

★**両方とも変異で落ちることを確認しました**:

| 変異 | 落ちた検査 |
|---|---|
| ★窓が注視点に追随しない（世界固定を壊す） | ★**4 件**（「帯も進む」を含む） |
| ★境目を周期の格子から外す（`k*period + 3`） | ★**2 件**（「周期は実寸で一定」を含む） |

---

## ★6. 台帳

| | |
|---|---|
| ★**A-8**（新設・★未解決） | ★画面の 3 層が「直線は 400m」を前提。★50 鞍中 **34 鞍**で直線用のカットが直線でない場所に出る。★オーナー判断「B」により**模型を広げたあとに 1 回だけ**直します |
| ★**A-9**（新設・★未解決） | ★同じ競馬場でも距離によってコーナーが消える（`withRunUp`）。★段階①（引き込み線）と同じ便で |
| ★**A-10** | ★**直したので台帳に残さず、この報告書へ**（台帳の決まり） |

---

## ★7. ★次にやること（★指示書 §1 の順番）

| # | | 状態 |
|---|---|---|
| ★1-0 | ★**A-10** | ★**完了**。★レビュー側の撮り直し待ち |
| ★1-1 | ★`lane-geometry.test.ts` を 44 通りへ（★着手条件・R-33） | ★次 |
| 1-2 | ★②b を `lane-reveal.test.ts` に | |
| 1-3 | ★V-18 を `npm test` に（R-32） | |
| 1-4 | ★`OvalSpec` を広げる（★器だけ） | |

⚠️ ★**§4-2 の「半径だけを動かしたときの ①② の地図」を、1-4 の前に作ります。**

---

## ★8. 再実行（★期待値つき・★すべて読取専用）

```powershell
cd V:\dev\Cusor\star

npm test                     # ★期待: 133 ファイル / ★1352 件 PASS / EXIT=0
npm run typecheck            # ★期待: EXIT=0

npx vitest run packages/render/test/mow-stripes.test.ts   # ★期待: 9 件 PASS

# ★A-10 の合否（★レビュー側が静止画で判定）
cd apps\web ; npm run dev    # ★別の窓・ポート 3210
npx tsx tools/_realshot.mjs --out g1 --query "race=g2-aone"  --sec 10
npx tsx tools/_realshot.mjs --out g2 --query "race=g1-ousei" --sec 10
#   ★期待: ゲートの奥の地面が一様。★横切る明暗の境界が無い。
#   ★期待: 走路の上の縞は残っている（★消えていたら直しすぎ）

# ★真因の数字
npx tsx tools/_gatecross.mjs --sec 10   # ★期待: 縁 w=-60 が (490,262)→(638,267) を通る（★旧形の値）

# ★形を触る前の基準（★この便では動かしていません）
npx tsx tools/verify-v18.mjs --venues   # ★期待: 44 通り PASS / ② 7.8〜11.2 馬身 / ① 最大 +0.070
```

⚠️ ★`--sec 10` は**カットの境目ではありません**（台帳 B-2）。★同じ URL を 2 回撮って一致します。
