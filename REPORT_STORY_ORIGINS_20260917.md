# 報告：物語 15 種の「起点」を洗い出しました — 2026-09-17

> 開発側 → レビュー側・オーナー。★裁定 `REVIEW_STORY_GROWTH_VERDICT_20260916.md` Q-LR8-1
> 「**便の冒頭で 15 種すべての起点を洗い出してから**」の履行です。
> ★**調べただけで、実装には入っていません**（★製品のコードは 1 行も変えていません）。

---

## 0. 要点

| # | 分かったこと |
|---|---|
| ① | ★**起点が実装にあるのは 9 種**（うち 5 種は既に書かれている）。★**6 種は起点そのものが無い** |
| ② | 🔴 ★**`horse_week_log` に書いている所がワーカーに 1 件もありません。** ★`training-runner.ts:33` のコメントは「プレイヤーの馬だけ記録します」と**現在形**ですが、★書いているのは `tools/verify-b1.mjs`（B-1 の検証用）だけです。→ ★**LR-9 と同じ形がもう 1 つ**（表もコメントもあるのに経路が無い） |
| ③ | 🔴 ★**`seed-world.mjs` は `birth_week` を入れていません**（列の一覧に無い・0 件）。★入れているのは道具 3 本だけ。→ ★**誕生の起点が弱い** |
| ④ | 🔴 ★**配合（繁殖）がワーカーに実装されていません**（`insert into horses` はプリシードの道具と検証用のみ）。→ ★**初産駒・産駒の勝利は起点が存在しない** |
| ⑤ | 🔴 ★**発見度の「試された回数」を数えている所がどこにもありません**（`discoveryAdvanced` は純関数のみ・画面はデモ値）。→ ★**適性判明は起点を作るところから** |
| ⑥ | ★**§18 は 15 種それぞれの起点を定めていません**（LR-1〜LR-10 は「約束」で、種類は本文の**例示**）。→ ★**定義を発明せず、§4 に照会として出します** |

---

## 1. 15 種の起点（一覧）

★「起点」＝ ★**その出来事が起きた瞬間を、実装のどこが知っているか**。

| # | 種類 | 起点 | 在り処 | 判定 |
|---|---|---|---|---|
| 1 | `debut` デビュー | `runsBefore === 0` | `raceStoryEvents`／`story-flow.ts` | ✅ **書いている** |
| 2 | `first-win` 初勝利 | `winsBefore === 0 && 1着` | 同上 | ✅ **書いている** |
| 3 | `graded-win` 重賞勝ち | `grade !== null && 1着` | 同上 | ✅ **書いている**（★staging に 0 行なのは重賞が出ていないだけ） |
| 4 | `top-grade-win` 最高格勝ち | `grade === 'G1' && 1着` | 同上 | ✅ **書いている**（同上） |
| 5 | `jockey-bond` 名コンビ | `ridesWithJockeyBefore + 1 === bondMaxRides` | 同上 | ✅ **書いている**（★プレイヤーの馬だけ騎手を選ぶので、いまは 0） |
| 6 | `retirement` 引退 | `advanceWeek` の `log.retired`／`horses.retired_at_week` | `training-runner.ts:318・327-329` | 🟡 **値はある・書いていない** |
| 7 | `injury` 故障 | `advanceWeek` の `log.injury` | `week.ts:363`（★`training-runner` は**受け取って捨てている**） | 🟡 **値はある・書いていない** |
| 8 | `comeback` 復帰 | `rest_until_week` に達した週 | `horses.rest_until_week`（`0010`） | 🟡 **値はある・書いていない** |
| 9 | `first-training` 初調教 | `canTrain(week)` が偽→真に変わる週（78 週） | `week.ts` の `canTrain`／`training-runner` の `age` | 🟡 **導ける・書いていない** |
| 10 | `career-high` 自己最高 | `race_entries.finish_time` を過去の自分と比べる | `0001_init.sql:196` | ⚠️ **列はあるが定義が無い**（§4 Q-1） |
| 11 | `final-race` ラストラン | 「引退が決まった馬の最後の出走」 | ★**確定の時点では引退が決まっていない** | ⚠️ **順序の問題**（§4 Q-2） |
| 12 | `birth` 誕生 | 馬を作る所 | `tools/seed-world.mjs`（★`birth_week` を入れていない） | 🔴 **起点が弱い** |
| 13 | `trait-discovered` 適性判明 | `discoveryAdvanced(before, after)` | ★**「試された回数」を数える所が無い** | 🔴 **起点が無い** |
| 14 | `first-offspring` 初産駒 | 配合で仔が生まれた時 | ★**配合がワーカーに無い** | 🔴 **起点が無い** |
| 15 | `offspring-win` 産駒の勝利 | 確定時に親を辿る | ★**配合が無いので親子関係が育たない** | 🔴 **起点が無い** |

**内訳**: ✅ 5 種（書いている）／🟡 4 種（**値はある・繋ぐだけ**）／⚠️ 2 種（**定義が要る**）／🔴 4 種（**起点から作る**）

---

## 2. 🔴 ②の詳細 — `horse_week_log` は誰も書いていません

```
$ grep -rn "horse_week_log" --include="*.ts" --include="*.mjs" . | grep -v node_modules | grep -v "^./db/migrations"
./apps/worker/src/training-runner.ts:33: *   `horse_week_log` は B-1 の証拠として作りましたが…   ← ★コメントだけ
./tools/audit-tools.mjs:53 / ./tools/lib/*.mjs                                  ← ★登録簿の名前
./tools/verify-b1.mjs:216:    `insert into horse_week_log                       ← ★★書いているのはここだけ
```

★`0010` は表を作り、★`training-runner.ts:32-35` は「**プレイヤーの馬だけ記録します**」と**現在形**で書いています。★しかし ★**ワーカーの本体に insert がありません**。

⚠️ ★**これは LR-9 の 3 例目です**（`settle-races.mjs` の `epochMs`／`raceWeekMarkOf` の 4 値／今回）。★いずれも「★**表もコメントも関数もあるのに、経路だけが無い**」形です。

→ ★**故障・復帰を `horse_week_log` から拾う設計にはできません。** ★`advanceWeek` の戻り値から**直接**書くことになります（★そのほうが NPC 馬も拾えます）。

---

## 3. 🟡 4 種は「繋ぐだけ」— ただし 1 か所だけ判断が要ります

★引退・故障・復帰・初調教は、★**週送りが 1 頭 1 週を進める中で値を持っています**。

⚠️ ★**ただし週送りは一括更新です**（`BATCH_SIZE = 2000`・1 行ずつ書かない）。★物語を 1 頭ずつ insert すると、★**7,370 頭 × 週で行数が跳ね、週送りの所要に効きます**（★LR-5「着順にも経済にも効かない」は守れますが、★**時間には効きます**）。

→ ★**一括の insert にまとめます**（★`unnest` で 1 回・★週送りの更新と同じ作法）。★見積り: ★引退は 1 頭 1 回・故障は生涯 29.9%（V-7a）・初調教は 1 頭 1 回なので、★**1 日 612 行**（`REPORT_STORY_GROWTH_20260916.md` §4-2 の実測）。★週送りの所要に対しては小さいはずですが、★**便の中で実測して報告します**。

---

## 4. 照会（★定義が正典に無いもの）

| # | 内容 |
|---|---|
| **Q-ST-1** | ★**`career-high`（自己最高）の定義。** ★`finish_time` はありますが、★**距離も馬場も違う時計は比べられません**。★候補: ⓐ**同じ距離**での自己最高 ⓑ**上がり 3F** など別の量 ⓒ**この便では実装しない**（★定義が決まるまで起点を作らない）。★開発側の推奨は **ⓒ→ⓐ**（★決めてから作る） |
| **Q-ST-2** | ★**`final-race`（ラストラン）をいつ書くか。** ★確定の時点では**引退が決まっていません**（★引退は週送りが決める）。★候補: ⓐ**引退した週に、直前の出走を遡って書く**（★過去の行を後から作る）ⓑ**引退の行だけにして `final-race` は作らない** ⓒ**「これが最後になるかもしれない」を事前に書かない**（★D-098 の家族）。★開発側の推奨は **ⓐ**（★引退は週送りが知っているので、★そこで 1 回だけ遡る） |
| **Q-ST-3** | ★**`birth`（誕生）の起点をどこに置くか。** ★`seed-world.mjs` は `birth_week` を入れておらず、★配合も未実装です。★候補: ⓐ**プリシードの道具に足す**（★7,370 頭ぶん一度に書く＝★1 日で 1 万行超）ⓑ**これから生まれる馬だけ**に書く（★配合が入る便まで待つ）ⓒ**書かない**。★開発側の推奨は **ⓑ**（★過去に遡って作った行は「記録」ではなく「再構成」で、★Q-LR8-2 で**やらないと決めた**のと同じ形です） |
| **Q-ST-4** | ★**`trait-discovered`（適性判明）の「試された回数」を誰が数えるか。** ★いまは**どこにもありません**。★候補: ⓐ**新しい列**（`horses` に適性ごとの回数）ⓑ**`race_entries` から数える**（★距離・馬場の条件で毎回集計）ⓒ**この便では実装しない**。★開発側の推奨は **ⓑ→ⓐ**（★まず数えられるか測り、★重ければ列にする）。⚠️ ★**LR-7 のとおりレースが終わった後に書きます** |
| **Q-ST-5** | ★**`horse_week_log` を繋ぐか、捨てるか。** ★表は `0010` にあり、★コメントは「記録します」と書いていますが、★**誰も書いていません**。★候補: ⓐ**繋ぐ**（★Q-P3-21 の「1 万頭で 182 万行」の照会が未決のまま）ⓑ**コメントを事実に直す**（★「B-1 の検証用で、本番では書いていない」）ⓒ**表ごと落とす**。★開発側の推奨は **ⓑ**（★物語は別の表に書くので、★この表は B-1 の証拠のままでよい） |

---

## 5. 便の進め方（★指示書が出たらこの順で）

| 順 | 中身 | 理由 |
|---|---|---|
| 1 | **引退・故障・復帰・初調教**（🟡 4 種） | ★**値が既に手元にある**。★一括 insert で週送りに繋ぐ |
| 2 | **適性判明**（Q-ST-4 の裁定後） | ★D-108 の発見度と同じ量から出る。★LR-7 のとおりレースの後 |
| 3 | **自己最高・ラストラン**（Q-ST-1・Q-ST-2 の裁定後） | ★定義が決まってから |
| — | **誕生・初産駒・産駒の勝利** | ★**配合の便を待つ**（★起点そのものが無い） |

★**条件（裁定のとおり）**: ★LR-5 を崩さない（着順にも経済にも効かない）／★**書き込みの失敗で確定も週送りも巻き戻さない**（★`pg-store.ts` の `onStoryError` と同じ形を週送り側にも置きます）。

---

## 6. 検査

- ★**製品のコードは 1 行も変えていません**（★調べただけ）
- ★DB には接続していません（★この報告は実装の読み取りだけ）
- ★`REPORT_STORY_GROWTH_20260916.md` の実測値（1 日 612 行・1 行 154.5 バイト）を分母に使っています
