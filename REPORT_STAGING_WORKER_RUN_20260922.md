# 報告: 新しいワーカーを staging で回す予行（手順書 ⑥ の 4 項目）— 2026-09-22

> 開発側 → レビュー側。裁定 `REVIEW_NAME_RESET_TOOL_VERDICT_20260922.md` §5（`c027fd7`）の指示。
> 回したのは HEAD `c027fd7`（`5257a14` を含む）。**staging に書いた**（ワーカーの本物の周）。本番には触れていない。

## §0 一行

この PC から前景で 560 秒回し、**1 周だけ回った**。その 1 周に **514.3 秒（周の予算 360 秒の 142.9%）**かかった。
週送りは途中で時間切れになり、配合は一度も走らなかった。**4 項目のうち確かめられたのは ① だけ**で、②③ は未確認、④ は部分的。**年の頭をまたぐ経路も未確認。**

## §1 回し方

```
timeout -k 20 560 node --import tsx --env-file=secrets.staging.env apps/worker/src/main.ts
```
- `npx` を介さず `node` を直に起動した（止めの合図を子に届けるため）。
- 終了コードは 124（時間切れで止めた）。
- 終わった後に `node` の処理を探し、**取り残されたワーカーは 0 件**だった。

## §2 4 項目

| # | 見るもの | 結果 |
|---|---|---|
| 1 | `[worker] cycle=` が 1 件以上 | ✅ 1 件（`cycle=10948 phase=onSale … 周=514.3s(142.9%)`） |
| 2 | `[worker] 週送り` の行 | ❌ **未確認**。行は出なかった。ただし DB では週送りが**途中まで進んでいた**（現役の `last_processed_week` は最小 259・最大 272）。時間切れで止めた |
| 3 | 配合の行（NPC・プレイヤー） | ❌ **未確認**。`world_state.last_bred_week` は null のまま。週送りの後に呼ぶので、そこまで届かなかった |
| 4 | `失敗` の行が 0 | ⚠️ 見た範囲では 0。ただし周の後半を見ていない |

- **年の頭をまたぐ経路（週 260）: 未確認。** 週送りは 259 → 272 をまたいで進んでいた。しかし配合（生涯 8 産の降ろし）はまったく走っていない。

## §3 ログに出た警報（そのまま）

- 開放率の分布が P1 のゲートの分布からずれている（p10 55.3% → 44.8%・D-053）
- 買える NPC の現役馬が 0 頭（下限 200・D-102 ⑤）。出品 0
- 出走資格を 2 段広げた（win2・CL-7）
- `STAR_SEED_SECRET` が未設定（staging の設定。起動ごとに秘密が変わる）

いずれも staging の世界が 09-16 から止まっていたことに由来すると見ているが、**原因は突き止めていない**。

## §4 staging に残ったもの

- `world_state` に 1 行（`game_week = 273`）。
- レース 4 件（cycle 10949〜10952・未確定）。ワーカーを回せば確定か中止に進む。
- 週送りは途中まで（馬ごとの `last_processed_week` なので、次に回したときに続きから進む設計）。

## §5 🔴 1 周に 142.9% かかった件

- この PC から staging の DB への往復は 1 回 50〜220ms（記憶の実測）。VPS から回したときとは比べられない。
- それでも、周の前半（レースの生成・日次集計・出品）だけで予算を超えている。これは、本番で新しいワーカーが 1 周に入るかの問題と同じ形をしている（記憶: 新しいエンジンは本番の周に入らない恐れ・09-16）。**VPS で測るまで判断しない。**

## §6 次の案（決めてほしい）

- (a) **推し**: オーナーの別のターミナルで `npm run worker`（staging）を 30〜60 分回してもらう。① 週送りの行 ② 配合の行 ③ 失敗の行 0 ④ 1 周の所要（％）を見る。
  - 重い計算はオーナーの別のターミナルで、という以前の決め（記憶）と同じ形。
  - 前景 10 分の上限では、周の前半だけで窓を使い切る。
- (b) VPS の staging 用ワーカーで回す（VPS の操作は、オーナーの許可が要る）。
- 年の頭をまたぐ経路は、(a) か (b) で週 312 まで待つか、予行の道具（`verify-role-request-live` ⑧・取引の中で年の頭の週を流す）で代える。予行では確かめてある（`e2dd872`）。

---

## §7 run2（2026-09-23 00:12〜00:40・オーナーの端末・`6451f98` 以降）

> 裁定 `REVIEW_PROD_DEPLOY_ORDER_20260922.md` §5 の (a)。ログは `evidence/20260922-staging-worker/run2.log`、読み方と結果は同じフォルダの `analyze-worker-log.mjs`・`run2-analysis.txt`。
> ⚠️ ログは PowerShell が UTF-16 で書いた。リポジトリの検査（追跡中の文字のファイルに NUL バイトを置かない）に合わせて **UTF-8 に書き直した**（中身は同じ・分析の結果が一字一句同じことを確かめた）。
> 最初の試みは、PowerShell が stderr の警告を赤い NativeCommandError として出したため、オーナーが数十秒で止めた。ログも文字化けした。
> `[Console]::OutputEncoding = UTF8` と `cmd /c "npm run worker 2>&1"` で回し直した。止めた後、取り残されたワーカーは 0 件。

### 手順書 ⑥ の 4 項目

| # | 見るもの | 結果 |
|---|---|---|
| 1 | `cycle=` | ✅ 11 回（cycle 10952〜10956。番号は飛ばずに続いた） |
| 2 | `週送り` | ✅ 追いつきの 1 回: 週 259〜271 の 13 週・延べ 3,856 頭・引退 28 頭。**訓練の側は年の頭（週 260）をまたいだ** |
| 3 | `配合` | ✅ NPC: 週 272 に 13 頭が生まれた・相手なし 1 頭。⚠️ 配合の印が無かったため「いまの週から始めた」。**年の頭の配合（生涯 8 産の降ろし）は通っていない**。プレイヤーの配合の行は無い（要求が 0 件） |
| 4 | `失敗` の行 | ✅ 0 件 |

### 所要（％は周 360 秒に対して。「全体」は週送りと配合を含む・`6451f98` で足した行）

| ループ | 何をした回か | レースの段 | 全体 |
|---|---|---|---|
| cycle 10952（追いつき中） | レースの生成・週送り 13 週・配合 | 99.6% | **112.6%**（405.5 秒） |
| cycle 10953 | レースの生成・確定 | 117.4% | **118.2%**（425.5 秒） |
| cycle 10954 | 同 | 68.1% | 68.8% |
| cycle 10955 | 同 | 56.8% | 57.5% |
| cycle 10956（settling） | 確定 | 31.7% | 32.3% |
| そのほか 6 回 | 何もしない回 | 1.2〜1.4% | 1.9〜2.2% |

- 追いつき中のループは 1 回（112.6%）。週送り 13 週と配合で、レースの段より約 47 秒多かった。
- 追いつきの後、重い回（レースの生成と確定）は 118.2%・68.8%・57.5%・32.3%。**予算を超えたのは cycle 10953 の 1 回**。
- ⚠️ 追いつきの後の普通の週送りは**未確認**。ゲーム内の次の週は現実の 4 時間ごとで、28 分の窓には来なかった。
- ⚠️ 周の数え方: ワーカーは 60 秒おきに起きて、その時点ですべきことだけをする。「cycle=」の行はループ 1 回で、同じ cycle に複数回出る。**1 つの周（6 分）に収まるかは、重い回が次の周の頭までに終わるかで見る必要がある**。今回は重い回の後に同じ cycle の軽い回が続いており、周がずれて飛んだ跡は無い。

### 読み方の限界

- **この PC から staging の DB への往復は遅い**（1 回 50〜220ms）。レースの段の 117.4% は、VPS から回したときとは比べられない。本番で周に収まるかは、**VPS で測るまで判断しない**（§5 と同じ）。
- 年の頭の配合の経路は、実走では未確認のまま。予行（`verify-role-request-live` ⑧・`e2dd872`）では確かめてある。

### ログに出た警報（そのまま・原因は突き止めていない）

- 出走資格を 1〜4 段広げた（win1〜open・CL-7）。
- 買える NPC の現役馬 32 頭（下限 200・D-102 ⑤）。
- 相性表（nicks）が空で、全組み合わせ 1.00（§6.6 の下端）。staging の設定。
- `STAR_SEED_SECRET` 未設定（staging の設定）。
- 生涯の記録は 34 行・32 頭。

## §8 周が 100% を超えたとき、レースの段はどう振る舞うか（コードで確かめた・裁定 `REVIEW_PROD_DEPLOY_ORDER_20260922.md` §6 の確認のお願い）

**結論: 1 回のループが 6〜13 分に伸びても、確定は「遅れて進む」だけで、開催中止にはならない。** 中止に当たる条件は 2 つあり、どちらもループが 13 分か 60 分を超えて初めて起きる。
⚠️ ただし、**発売の時間が短くなることはある**（下の ③）。

ワーカーは 60 秒おきに起き、1 回のループで次の順に処理する（`apps/worker/src/cycle-runner.ts`）。**「いまの時刻」はループの最初に 1 回だけ読む**（`:284`）。

| 順 | 処理 | ループが長いとどうなるか |
|---|---|---|
| ① | 確定（`:324`・発走時刻を過ぎた `scheduled` をすべて・`pg-store.ts:494`） | **遅れて確定する**。次のループがその時点で過ぎているものを全部確定するので、遅れはループ 1 回ぶんを超えて積もらない |
| ② | 期限切れの中止（`:371`・発走から 60 分〔`CANCEL_AFTER_START_MS`・`cycle.ts:89`〕を過ぎて未確定） | ループが**60 分**を超えない限り当たらない。run1 の後に起きた cycle 10951 の中止は、ワーカーが 60 分以上止まっていたため |
| ③ | 組成（`:430〜`・登録の締切〔2 周前の周の頭・`cycle.ts:206`〕を過ぎたもの） | 発売開始（その周の頭 ＋ 60 秒・`cycle.ts:52`）までに組成できなければ**中止して登録料を返す**（DS-7・`:449`）。締切から発売開始までは **13 分**あるので、ループが 13 分を超えない限り当たらない。⚠️ ただし「いまの時刻」はループの最初に読むので、**発売開始の直前に始めた組成が発売開始の後に終わると、そのレースは遅れて発売になる**（発売の 4 分が短くなる）。組成の中身（オッズの計算）は 1 本 70〜98 秒 |

- 週送りと配合は、レースの段の**後**に同じループで走る（`main.ts:635`・`:724`）。そのため**次のループの頭が遅れる**。追いつきの量が多い回（配備直後・長い停止の後）が、いちばん長くなる。
  - 週送りは 1 回 8 週まで（`MAX_WEEKS_PER_RUN`）。配合は予算 `BREEDING_BUDGET_MS` で切る。
  - run2 の追いつき（13 週）では、レースの段 ＋ 約 47 秒だった。
- 以上はコードから言えることで、**本番の VPS の所要は測っていない**。

## §9 手順書に足したこと

段 ⑥ に 1 行（裁定 §6 の条件）: 最初の週送りの周の「周の全体」の％を記録し、**100% を超えたら止めて報告**する。

