# Claude Code 指示書：初回の馬を父母選択から生産し、命名して育成へ進める

作成日: 2026-09-22。対象: `https://github.com/jinjinsansan/syar`。ローカル作業ディレクトリは `V:\dev\Cusor\star`。この文書はオーナーが Claude Code に作業を任せるための指示書です。**まず `CLAUDE.md` を読むこと。** 次に `STAR_SPEC_v2.0.md`、`HANDOFF_AUTH_UI_20260922.md`、`PROPOSAL_FIRST_HORSE_BREEDING_20260922.md`、`DESIGN_REQUEST_FIRST_HORSE_ONBOARDING_20260922.md` を読むこと。古い `SPEC.md`、`AI_PROMPT.md`、認証情報を含む `git.txt` は読まない。

## オーナーの目的

現在の「表示名・厩舎名 → 既存の馬を１頭付与 → 調教」の初回体験を、次の実際に動く流れへ変える。

1. 表示名と厩舎名を決める。
2. 候補から父馬を選ぶ。
3. 候補から母馬を選ぶ。
4. 選んだ父母から仔馬が実際に生まれた後で、誕生アニメーションを表示する。
5. プレイヤーが仔馬を命名する。
6. 馬を育てる最初の画面へ進む。**調教画面に入った時点でレース常設表示の小窓が見える**こと。

これはアーケード競馬育成ゲームの**体験の順番**を参考にする依頼である。参考作品の固有名、実在馬、画像、文言、画面配置、アニメーションを複製しない。馬物語の独自のデザインとデータを使う。父母を選ぶだけの見せかけの画面や、既存馬を付与したのに「生まれた」と表示する演出では目的を満たさない。

## 最初に確認する事実

```powershell
git status --short --branch
git log -8 --oneline
git rev-parse origin/main
```

2026-09-22 にこの指示書を書く直前の UI 実装コミットは `3544234`、調査・提案のコミットは `f7ae738`。新しいコミットのため再開時は必ず測り直す。作業ツリーには他者由来の `apps/web/public/art/uma/title-eye.png` / `.webp` の削除状態、多数の未追跡画像等がある。**`git stash`、`git add -A`、`git commit -a`、不明なファイルの削除・復元をしない。** 対象を明示してステージする。子エージェントはオーナーが明示的に求めた場合に限る。

## 先に解く仕様の衝突

**最優先:** `packages/scheduler/src/week.ts` の `LIFECYCLE_WEEKS` は生後78ゲーム内週から調教可、104週から出走可。現在週に生まれた本当の新生馬を命名直後に `/train` へ送っても、調教は許されない。オーナーにはこの点を質問済みで、**この文書の作成時点では回答がない**。次の担当は会話の最新回答を確認すること。回答が無ければ、年齢・誕生週・調教資格を変える実装前に、次の選択肢を具体的な影響付きでオーナーに示す。

- 初回だけ育成期間を短縮して直後から調教可能にする。育成・年齢・出走・較正への影響を測る必要がある。
- 通常の78週を待ち、その間は仔馬の育成画面を見せる。オーナーの「すぐ調教」とは異なる。
- 誕生から調教可能年齢までを、世界の時系列が矛盾しない別の導入シナリオとして設計する。単に `birth_week` を過去へ書き戻して年齢を偽らない。

**勝手に既存の調教可能年齢を変えない。** 決定待ちの間も、候補公開、配合のサーバー処理、状態設計、命名、画面モック等の独立作業は進める。決定を要する箇所は明示して保留する。

現行正典 D-074 / D-079 は既存 NPC 馬を無償付与する入口を定めている。オーナーの新しい要望に従うための改訂案を作り、どの規則が置き換わり、どの規則（無償参加、再抽選対策、馬の公正性、権利保護）が残るかを明記する。正典を黙って編集して矛盾を隠さない。

## 実装方針

### 1. 配合の計算を再利用する

`packages/sim-engine/src/breeding.ts` の `canMate()` / `breed()`、血統・ニックス処理を再利用する。遺伝計算をフロントエンドや PL/pgSQL にコピーしない。`apps/worker/src/breeding-runner.ts` は NPC 世界の週次配合であり、そのままプレイヤー用 API として公開しない。サーバー権威、決定論、再現可能なシード・記録を守る。

まず「認証済みプレイヤーの要求を受け、TypeScript `breed()` を信頼できるサーバー側で実行し、DBへ安全に確定する」配置を比較・決定する。現行は Web / CLI / VPS ワーカーで、専用の公開配合サービスはない。Next.js Route Handler に業務ロジックを直書きしないという `CLAUDE.md` の制約を守る。DB トランザクション、ジョブ、失敗時の再実行、ワーカー配備順を設計する。

### 2. 父母候補を安全に見せる

父・母候補のための公開契約を新設し、`auth.uid()` の本人確認をする。現在の繁殖資格、所有・利用範囲、年齢、種付回数、母の出産回数と当年の繁殖状態をサーバーで判定する。候補を表示したあと他者が先に使った場合は、確定時に再判定して理由を画面へ返す。候補ビューから `genotype`、`potential`、`stats`、内部評価値や素質のランクを公開しない。

親の候補源、親馬の消費・利用回数、初回無料の扱いが未決なら、`PROPOSAL_FIRST_HORSE_BREEDING_20260922.md` の推奨案を具体的なDB制約・画面結果とともに提示し、オーナー判断を記録する。UIだけで「選択できた」ことにしない。

### 3. 初回の生産と命名を中断可能にする

既存 `create_account()` は `users` 行・2,000 EP 台帳・既存馬の所有権移転を１トランザクションで行う。新フローでは口座作成から命名まで複数画面になるため、永続的な導入状態が必要。ログイン直後・再読み込み・別タブ・通信断で正しい段階から再開できるようにする。無料の仔馬とEPを二重付与させない。親馬をロックして確定した結果を再送時にも返す。

`horses.name` は `NOT NULL`。名前未定の馬を通常の所有馬・出走候補として公開しない。非公開の生産結果の下書きと命名確定を分けるか、同等に安全な方法を選ぶ。命名は本人のみ。文字数、正規化した重複、禁止名、実在馬名を**サーバー側**で検査し、エラーをその入力欄に表示する。`packages/sim-engine/src/naming.ts` の `normalizeName()` だけでは禁止名判定は済まない。

既存利用者の馬、ポイント、Auth、履歴は保持する。新規アカウントから新フローを適用し、既存利用者を自動で初回配合へ戻さない。

### 4. デザインとレース小窓を接続する

新しい画面の見た目・動きは `DESIGN_REQUEST_FIRST_HORSE_ONBOARDING_20260922.md` に従い、デザイナーの成果物を受け取ってから最終実装する。既存部品を使える部分は使う。

`レースが数分おきに開催.zip` は**リポジトリ直下に存在するが Git 追跡外**。中の `design_handoff_uma_monogatari/RACE_NOTICE_HANDOFF.md` が確定仕様。ログイン・登録・初回設定中は RaceStrip 非表示。調教フォーム中は受付時の帯、走行中の 22×16px の極小表示。現在の `apps/web/src/components/uma/race-strip.tsx` は確定後データから再現する「録画」で、生中継位置ではない。小窓が存在することと本物の生中継が成立することを混同しない。実走行データの接続は `DESIGN_LIVE_RACE_DATA_CONTRACT_20260921.md` を別工程として扱い、録画には録画と表示する。

`/train` の現行ボタンは「調教指示は準備中」で保存できない。オーナーの目的は初回の育成導線まで含むため、必要なら調教指示の保存と本番ワーカーの読み込み状態も検証対象に入れる。ただし本番ワーカーは既存の移行順序があるので独断で配備しない。

## 受入条件

1. 新規アカウントが父と母を選び、サーバーで配合した**その親の ID**を持つ仔馬が１頭だけ本人に作られる。画面の表示と DB の記録が一致する。
2. 同じ確定要求を連打・再送・別タブから実行しても、仔馬・初期EP・親の利用回数が１回分しか増えない。途中失敗後の再開が可能。
3. 命名までは本人の未完了状態として扱い、他の所有馬一覧・レースへ未命名の馬が紛れ込まない。命名後に本人の馬として読める。
4. 親馬・仔馬の非公開能力値がクライアントへ出ない。他人の導入状態や馬名を変更できない。
5. 誕生、命名、最初の育成／調教の遷移がスマホ390pxとPC1280pxで成立する。調教画面には仕様どおりレース小窓が見える。録画は生中継と表示しない。
6. 年齢・調教資格・出走資格はオーナーが選んだ方式と正典改訂に一致し、関連する既存検査が通る。
7. 既存アカウント、NPC の週次配合、レース、EP・PP 台帳に回帰がない。変更前後の配合計算を固定シードで比較する。

## 検証・報告・配備の境界

- 実装ごとに `npm.cmd run typecheck`、関連テスト、`npm.cmd run gate`、staging での実際の初回完走、PC/スマホのブラウザ確認を行う。既知の赤と新規失敗を分けて報告する。通らない状態を「完成」と言わない。
- 本番 DB への書き込み・移行、本番ワーカー配備は、オーナーの明示的な許可前に行わない。前の引継ぎ書にある **本番血統修復 → 移行 `0060` → `tools/deploy.sh`** の順序を守る。Git push はワーカーを更新しない。
- 新機能に本番移行が必要なら、移行ファイル、ロールバック、staging 実測、既存版との互換性、運用手順まで完成させてからオーナーに見せる。
- 進捗報告では「画面だけ完成」「staging で生産成功」「本番データに接続済み」「実レースの生中継」を区別する。未完了を完成と呼ばない。
