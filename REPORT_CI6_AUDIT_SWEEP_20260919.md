# REPORT — **CI-6**: 2026-09-14 の監査を全数 突き合わせた

- 日付: 2026-09-19
- 発行: 開発側
- 対象: `REPORT_AUDIT_20260914.md`（★5 日前の監査）
- きっかけ: ★**2 件（Render の記述 ／ `deploy/star-worker.service`）が直っていなかった**ので、
  ★レビュー側が「★3 件目がある」と言った（**CI-6**）

---

## 0. ★結論 — ★**3 件目どころではありませんでした**

| | |
|---|---|
| ★突き合わせた項目 | **22** |
| ✅ ★直っている | **4** |
| 🔴 ★**まだ開いている** | ★**12** |
| ⚪ ★今日 直した | **2**（Render ／ unit の写し） |
| ⚠️ ★判断が要る／未確認 | **4** |

🔴 ★**指摘の半分以上が、5 日 そのまま**でした。
⚠️ ★**これは「監査が読まれなかった」ではありません** — ★**読まれて、直されなかった**ことが、
★2 件で実証されています（★unit の件は、★危険なほうを手順書が指したままでした）。

---

## 1. ✅ 直っているもの（4）

| # | 指摘 | ★いま |
|---|---|---|
| **M-3** | ★§9.5-3「自馬全頭を含む組合せのみ可」が `exists`（1 頭で通る） | ✅ ★**`not (p_selection @> to_jsonb(e.gate))`**（★移行 `0044`・全頭） |
| **M-5** | ★`--env` を省くと production を向く | ✅ ★**2026-09-14 に `--env` 必須**（`tools/lib/env.mjs:37`・既定を廃止） |
| ★記載 | ★CLAUDE.md の配備先「Render」 | ✅ ★**VPS の systemd と明記**（★ただし §2-⑫ を見てください） |
| ★unit | ★`deploy/star-worker.service:28` が tsx で原本を動かす | ⚪ ★**今日 削除**（WK-5・`589ba26`） |

⚠️ ★**M-3 と M-5 は、監査より後に別件で直った**もので、★監査を追って直したのではありません。

---

## 2. 🔴 まだ開いているもの（12）

### ★高いほう

| # | 指摘 | ✔ ★いま（★実測） | ★効き |
|---|---|---|---|
| **M-4** | ★`STAR_SEED_SECRET` が無くても**警告だけで起動**し、秘密をその場で作る（R-27） | 🔴 `apps/worker/src/main.ts:84-87` ★そのまま。★**`.env.example` にも `deploy/README.md` にも変数が無い** | ★§8.6。★行に `server_seed` を保存しているので直ちには壊れないが、★**保存前に落ちたレースは reveal を出せない**（★註記が自分でそう書いている） |
| **M-8** | ★`isPpNetHealthy` が `ppIssued <= 0` で `true`（★「経済が存在しない日」を健全と判定・R-16 ／ V-11） | 🔴 `packages/betting/src/point-flow.ts:128` ★そのまま | ★**DL-1 で `point_flow_daily` が 1 か月 0 行**だった。★**その 1 か月、この関数は「健全」と言い続ける形** |
| **M-9** | ★調子の値域の食い違い（育成 0..5 ／ race-engine `CONDITION_MIN: 1`）→ ★**調子 0 と 1 が同じ補正** | 🔴 `packages/race-engine/src/balance.ts:489` ★そのまま | ★着順に入る |
| ★低 | ★DB 接続で TLS 証明書を検証していない（`rejectUnauthorized: false`） | 🔴 `apps/worker/src/main.ts:62` ／ `tools/migrate.mjs:106` ★そのまま | ★本番の DB 接続 |

### ★記載・設定

| # | 指摘 | ✔ ★いま |
|---|---|---|
| ⑫ | ★`CLAUDE.md:10`「★**現在のフェーズ: P0**」 | 🔴 ★**そのまま**。★今日 P4 のブランチで DS-5・VT-1・GB-1 をやっています |
| ⑬ | ★`next.config.mjs` の「Route Handler を作りません」 | 🔴 ★**そのまま**。★✔ `apps/web/src/app/api/` に **`healthz` と `rig-lab`** が在ります |
| ⑭ | ★worker が使っていない `SUPABASE_SERVICE_ROLE_KEY` を**必須**にしている | 🔴 `apps/worker/src/env.ts:49` `need(...)` ★そのまま。✔ ★**`serviceRoleKey` は `env.ts` の外で 1 度も読まれません** |
| ⑮ | ★`.env.example` の `SUPABASE_ANON_KEY` と Web の `NEXT_PUBLIC_SUPABASE_ANON_KEY` が食い違う | 🔴 ★**そのまま**（`.env.example:23`） |
| ⑯ | ★ルートの zip 2 本が追跡されたまま | 🔴 ★**そのまま**（`git ls-files "*.zip"` が 2 件） |
| ⑰ | ★`.gitignore` がルートの `*.wav` を除外していない | 🔴 ★**そのまま**（★いま未追跡の wav は 0 本なので、★**即座の危険はありません**） |
| ⑱ | ★`sim-engine/src/balance.ts:78` の `RACE_RANDOM_K: 0.12`（★使われているのは race-engine 側の 0.22） | 🔴 ★**そのまま** |
| ⑲ | ★`calibration.test.ts` の試験名が「0.26」のまま | ⚠️ ★**題は直っています**（「再較正値 0.22（正典 §13.1 は 0.26・改訂依頼中）」）。★残っているのは ★**正典側の 0.26** |

---

## 3. ⚠️ 判断が要る／この 1 枚では閉じないもの（4）

| **M-1** | ★締切（§9.6 発走 30 秒前）と `place_bet` の受付時刻 | ⚠️ ★D-117 と ED-1 で**締切の定義そのものが変わりました**。★監査の時点と別の設計です。★**測り直しが要ります** |
|---|---|---|
| **M-2** | ★確定が 1 レース失敗すると周全体が抜ける | ⚠️ ★`cycle-runner.ts` は今日 大きく変わりました。★**再確認が要ります** |
| **M-6** | ★正典のゲートの大半が vitest の中で合否判定されていない（R-32） | ⚠️ ★**今日の `growth-tell-frequency.test.ts` が、その形の 1 例**です（★CAL-1）。★**全体は未着手** |
| **M-7** | ★決定論を守る自動テストがほぼ無い | ⚠️ ★**未着手**。★監査は「★現時点のロジック層に違反は無い」とも書いています |

---

## 4. 🔴 ★開発側が**やらなかった**こと、と理由

| ★`CLAUDE.md:10` の書き換え | 🔴 ★**やりません。** ★`CLAUDE.md` は**この作業ツリーの指示書**で、★開発側が伝令の求めで書き換える文書ではありません。★**オーナーの判断で直してください** |
|---|---|
| ★zip 2 本の削除 | 🔴 ★**やりません。** ★オーナーが置いた可能性があり、★**版管理から消すのは戻しにくい操作**です。★指示をください |
| ★TLS の検証 | ⚠️ ★**本番の接続に触ります。** ★証明書の出どころ（Supabase の CA）を確かめずに `true` にすると、★**ワーカーが起動しなくなります**。★staging で確かめてからにすべきです |
| ★M-4 / M-8 / M-9 / ⑭ ⑮ ⑯ ⑰ ⑱ | ⚠️ ★**指示書が出ていません。** ★この 1 枚が指示の材料です |

---

## 5. 🔴 ★**この突き合わせ自体が示したこと**

★監査は ★**5 日前に、22 項目を名指しで**出していました。
★今日 直った 2 件は、★**監査を読んで直したのではなく、別の作業でぶつかって**直りました
（★Render は CLAUDE.md を読んだとき、★unit は WK-5 で unit を探したとき）。

→ ★★**「指摘が文書にある」は「直る」ではありません。**
→ ★レビュー側が **NT-2** として立てた規則（★**註記に書いたら、その場で登録簿に載せる**）が、
  ★**監査の報告書そのものにも要ります**。

⚠️ ★**この 1 枚も、同じ運命をたどりえます。** ★登録簿に載せるか、指示書に落としてください。
