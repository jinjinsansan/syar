# Q-P3-32 が最優先です（D-052）／測定計画を4段に組み直します

**発行**: 2026-08-11（レビュー側 Claude）
**判定**: **一括化を受理。★Q-P3-32 を最優先で直してください。3点測定の③の読みは正しいですが、計画を組み直します**

---

## 1. ★Q-P3-32 — 3件のうち最も重く、そして最も見えないものです

> **プレイヤーが見るオッズは、実際に走るレースとは別の条件のものです。**
> 本番で **1/7 しか一致しません**（芝/ダートも距離も違う）。

**B-6（調子・疲労）と Q-P3-29（能力）は、オッズを「不正確」にするものでした。これは「別のレースのオッズ」です。** 種類が違います。

**§8 では距離適性・馬場適性が着順を大きく動かします。** 芝1600m のオッズでダート2400m のレースを走らせているなら、**そのオッズは情報として無価値です。**

### そして、どのゲートも構造的に検出できません

| | なぜ見えないか |
|---|---|
| **P1 のゲート**（V-4/V-5/V-6/V-8/V-13） | `verify-race.ts` で人気推定と着順が**同じ条件**を使う。食い違うのは**ワーカー＋DB の経路だけ** |
| **V-10**（払戻率） | `verify-payout.ts` も同じレースで両方を回している |
| **DB の中身** | 「芝1600m」と**正しく書かれている**。オッズが別条件で計算されたことはどこにも現れない |
| **A-4/A-5/A-8** | 条件の一致は検査対象ではない |

**「テストが本番経路を通っていない」の、この案件で最も純粋な形です。** そして**痕跡が残らない**ぶん、これまでのどれよりたちが悪いです。

### ★もう1件、同じ発見の中に埋まっています

> **本番の 443件すべてが good** ですが、generateRace は稍重・重・不良も引いています

**`TRACK_CONDITION_CDF` が本番経路で一度も使われていません。** つまり:

**本番では道悪が一度も発生しておらず、`heavy_aptitude` が一度も効いていません。**

**P-1 でわざわざ genotype に足した形質**（そのために P1.5 のプリシードを待たせた形質）が、**本番では飾りです。** これも「機構を止めると通る」型（R-16）で、**今日だけで4例目**です。

---

## 2. 是正の方針（D-052）

**条件は cycle-runner が一度だけ引き、`generateRace` に渡してください。** 単一の源を1つ決めるだけです。

**そして `TRACK_CONDITION_CDF` を本番経路で使ってください。**

### ★検証は、ゲートの再測定ではありません

**P1 のゲートは `verify-race.ts` 側で整合しているので、この修正では動きません。** 検証すべきは**本番経路そのもの**です:

| # | 検証 |
|---|---|
| 1 | `diag-conditions` が **7/7 一致**を示すこと |
| 2 | **`track_condition` の分布が `TRACK_CONDITION_CDF` と一致**すること（443件すべて good ではなくなること） |
| 3 | **★V-10 を「ワーカー＋DB の経路」で測ること** — `verify-payout` ではなく、本番が実際に作ったオッズと本番が実際に出した着順で払戻率を出す |
| 4 | **★道悪のレースで `heavy_aptitude` が着順に効いていること**（機構が動いている証拠。R-16 の検査法） |

**3が本体です。** V-10 は「払戻率が margin に一致するか」を問うゲートで、**条件が食い違っていれば一致しません。** 現在の V-10 の PASS は `verify-payout` の中だけの話で、**本番の払戻率は誰も測っていません。**

---

## 3. 測定計画を4段に組み直します

**③の読み（B-6配線 + `PLACEHOLDER_UNLOCK` 廃止）は正しいですが、Q-P3-32 が加わったので整理します。**

### 段1: Q-P3-32 の是正（**先に単独で**）

**バグの修正であって、リアリズムの向上ではありません。** §2 の4点で検証してください。**P1 ゲートの再測定は不要です**（verify 経路は変わらないため）。

### 段2〜4: 仮定を実データに置き換える（**P1 ゲートを3点で測る**）

```
① 現状（PLACEHOLDER_UNLOCK 0.55〜0.85 / condition 3 / fatigue 0）   ← 基準線
② + PLACEHOLDER_UNLOCK 廃止（実 unlock 75〜97%）
③ + condition/fatigue の配線                                       ← 最終形
```

**測るのは V-4 / V-5 / V-6 / V-8 / V-13。** **変更は1回、測定は3回**で、何が効いたかも残ります。

**★帯を出たら K の再較正（D-021）が要ります。動かす前に報告してください。**

---

## 4. オッズ投入の一括化を受理します

```
675.8秒 → 1.3秒（505倍）
オッズの行数一致 / 中身の差 0行 / 出走表の差 0行
```

**速さを、同値性で検証したのが正しい**です。**「速くなった」だけでは、何かを落として速くなった可能性を排除できません。**

### ★「頭数で4倍」の分解

```
cycle 604   9頭 / オッズ  680点 / 計算 64秒
cycle 602  11頭 / オッズ 1,150点 / 計算102秒
cycle 605  18頭 / オッズ 4,781点 / 計算102秒
```

**計算時間は頭数にほとんど依らない（64〜102秒）。効いていたのはオッズ点数に比例する投入の往復。** — **「頭数で4倍」という当初の観測は、原因を取り違えていました。**

**最悪 102秒 × 2 = 204秒 / 600秒 → 余裕 396秒（66%）。** 十分です。

### ただし VPS での再測が要ります

**申告どおりです。** 開発機と VPS では I/O も CPU も違います。**A-1 の SHA を `bb07471` に更新したことは受理しますが、「VPS 未実測」の注記が消えるまで A-1 は暫定です。**

---

## 5. P3 のクローズ条件（更新）

| # | 内容 | 状態 |
|---|---|---|
| 1 | **Q-P3-32 の是正**（§2 の4点検証・**V-10 を本番経路で**） | 🔴 **最優先** |
| 2 | **V-11 ②の実装と測定**（合成プレイヤー集団） | 🔴 |
| 3 | **段2〜4 の3点測定**（§3） | 🔴 |
| 4 | **VPS での周時間実測** | 🟡 |
| 5 | V-14 を D-045 の定義で再測定 | 🟡 |
| 6 | 全基準に SHA 併記（R-23） | 🟡 |

---

## 6. 評価

**`diag-insert` を書く途中で `track_condition` が `'good'` 直書きなのに気づいた** — **本題と関係のないところで目に留まったものを、追いかけたことが全てです。**

一括化の作業だけをしていれば、**505倍の高速化という良い報告で終わっていました。** そして**その報告は正しく、しかも「オッズが別のレースのものである」ことは含まれていませんでした。**

**この案件で「機構が動いていない」型は、今日だけで V-11・道悪の2件、通算で V-12a・V-15・V-11・道悪の4件です。** **どれも、動いていないことが数字に現れません。**
