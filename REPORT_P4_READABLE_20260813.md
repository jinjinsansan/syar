# REPORT_P4_READABLE_20260813 — ★裁定の測り方を実装／**道中1000mは「見ないほうがマシ」だった**

## 本便で正典の改訂が要る決定をしたか

**していません。**（裁定 Q-P4-12/13/14 の実装と実測です）

---

## 0. ★結論

裁定の測り方（勝負所で止めてボットに予想させる）を実装しました。**そして数字が出ました。**

```
残りm    出走表ボット   ★画面ボット    差
1500       0.747        0.540       −0.207   ★見るほうが分からなくなる
1200       0.747        0.526       −0.221   ★最悪
 800       0.747        0.605       −0.142
 600       0.747        0.864       +0.117   ← ここで初めて逆転
 400       0.747        0.765       +0.017   ★説明できていません（§5）
 200       0.747        0.928       +0.181
```

★**1600m のうち、最初の 1000m（62%）は、画面を見るより出走表を見たほうが正確です。**

「道中が退屈」ではありません。**道中は情報が負です。**

---

## 1. まず R-16 を逆向きに当てました（裁定のご指示）

> 展開の計算を全部消したら、画面は変わりますか。

```
① 展開あり: ペース high / 着順 10 12 4 6 8 7 2 5 3 11 9 1
② 展開なし: ペース slow / 着順 10 12 6 4 5 2 8 7 3 9 11 1
馬の位置が違う描画コマンド: 624 / 732（85%）
```

**変わります。** ★**ただしこれは弱い結果です。** 位置が違うことは「読める」を意味しません。
（`tools/diag-tenkai.mjs`）

---

## 2. ★測り方（裁定 Q-P4-13 のとおり）

```
N レース → 指定の残り距離で停止 → **描画コマンドだけ**を見るボットが
           「自馬は3着以内か」を予想 → 素の予想と比べる
```

### ★構造で守ったこと

ボットの入力は **`Frame.commands` と「自分の勝負服の色」だけ**です。
**エンジンの値を覗く経路が存在しません**（拒否ではなく不在・R-24）。

### ★私が3回間違えました

| # | 間違い | どうして分かったか |
|---|---|---|
| ① | **的中率で採点した** | 3着以内は25%しか起きないので、「**常に入らないと答える**」だけで75%。★**盲目ボットが最強**（74.5%）になり、画面ボット（54.5%）に「FAIL」を出しかけました → **AUC** に変更（盲目は定義上ちょうど 0.500） |
| ② | ★**ボットの係数を手で調整した** | 0.431 → 0.548 と上がりました。**これは画面ではなく読み手を鍛えただけ**で、R-16 に真正面から当たります → **前半で学習・後半で採点**に変更。私が触れるのは「画面に何を描くか」だけになりました |
| ③ | 🔴 ★**上限に漏洩したものを使った** | 「エンジンの真実」を上限に置いて **0.952** が出ました。位置モデルは**確定した結果から**作られています（`replayOf(result, …)`）。**残り1500mの「真実」に既にゴールが入っていました。** ★私はこれを「**レースはスタート時点で93%決まっている**」と読み、**重大な設計上の所見として報告しかけました** |

### ★③ をどうやって捕まえたか

「決まっているなら、**同じ出走表で乱数だけ変えても着順は動かないはず**」と考えて測りました:

```
同じ出走表・乱数200通り
  勝った馬の種類      : 12 頭 / 12 頭     ★全頭が勝つ
  最多勝ち馬の占有率  : 19.0 〜 35.0%
  毎回3着以内の馬     : 0 頭
```

★**エンジンには十分な不確定さがあります。** 私の測定が壊れていました。
（`tools/diag-uncertainty.mjs`）

→ 上限を**出走表ボット**（ゲートが開く前に分かることだけ）に置き換えました。
★**問いは「画面は、出走表を見る以上のことを教えているか」になります。**

### ★④ もう1件 — **比較相手が壊れたまま PASS が出ました**

能力の項目名を間違え（`gu` は存在しない）、出走表ボットが NaN になりました。
**AUC ちょうど 0.000**（NaN の比較は常に偽）。そして道具は
**「画面が出走表を +0.605 上回った。PASS」**と表示しました。

★**比べる相手が死んでいると、合格は自動的に出ます。**
→ 合否の前に「**出走表ボットが 0.55 を超えていること**」を確かめる自己検査を入れました。

---

## 3. 実装したこと — 展開を画面に出しました

裁定 Q-P4-14 ①「**実況は『位置』ではなく『変化』を言う**」に従い、**順位の数字は出していません**。

| 描画コマンド | 内容 | 座標系 |
|---|---|---|
| ★`effort` | **各馬の余力**（前の馬は持つのか） | 世界（馬に付く） |
| ★`gap` | **前の馬との差(m)・毎秒何m詰めているか・あと何頭抜けば足りるか** | 画面（カメラが隠せない） |

★`gap` に **`rank` はありません。** 持つのは差と詰まる速さと残り頭数だけです。
★「足りる」着順の線（既定3）は**上位から渡します**。画面で発明しません。

### 効果（残り800m）

```
位置しか無いとき      : AUC 0.431   ★何も見ないより悪い
余力と変化を足したとき: AUC 0.605
```

★**0.431 の意味**: 逃げ馬が前にいるのは強いからではないので、**位置は嘘をつきます。**

### ★カメラ検査に穴がありました

`gap` を足したとき、カメラ不変の検査は**何も言わずに通りました**。
検査が `'gauge' || 'cue'` と**列挙を書き写していた**ためです。

→ `OVERLAY_KINDS` を引く形に直しました。**足したら自動で検査対象になります。**
★P3 で繰り返した「2か所で別々に持つ」と同じ形です。

---

## 4. ★これは演出の問題ではありません

私は「レース演出のクオリティを上げる方法」を伺いました。
**測った結果、演出より前の層に問題がありました。**

```
道中1000m = 情報が負（−0.21）
        ↓
何を足しても（砂煙・実況・音・寄りの溜め）、
★**運ぶべき情報が無い時間**を飾ることにしかなりません。
```

裁定の「①自分が何を欲しているか ②いま何が起きているか ③次の予想 ④当たり外れ」で言えば、
★**道中には②が無く、しかも②のつもりで出している位置が③を誤らせています。**

---

## 5. ★説明できていないこと（隠しません）

**残り400m で 0.765 と下がります**（600m の 0.864 より低い）。

★**理由が分かっていません。** 400m は
- 直線に入る境界（`phaseOf`）
- 余力が 0 に近づく領域

のどちらかだと思いますが、**確かめていません。**
★「たぶんこう」で報告すると、次の便で私自身が信じてしまうので、**分からないと書きます。**

---

## 6. 🔴 照会

### Q-P4-15（新規・最重要）★道中に、運ぶべき情報はありますか

**画面の作り方の問題ではなく、エンジンの問題かもしれません。**

道中（残り1600〜800m）にエンジンが持っている「まだ決まっていないこと」は、
私が見た限り**ペースと隊列**だけで、どちらも**入場時点で決まっています**。

| 案 | 内容 | 懸念 |
|---|---|---|
| A | ★**道中を短くする**（映像として飛ばす・実況だけで繋ぐ） | 「レースを見た」感触が減る |
| B | ★**道中に決まることを作る**（掛かる・位置取り争い・不利） | ★エンジンの改造。**V-13 の較正に影響**しうる |
| C | 画面の作り方でまだ取れる情報がある | ★**私には見えていません**（0.540 が実測） |

★**私の見立ては B ですが、これは正典の領域**なので裁定を仰ぎます。
**C なら、何を描けば良いか教えてください。**

### Q-P4-16 ★合否の線はどこですか

いまは「盲目を有意に上回り、**かつ出走表も上回る**」にしました（私が決めました）。
★**局面ごとに違う線が要るはず**です（道中は出走表と同等でよい、勝負所は大きく上回るべき、等）。
**正典に置くべき数字だと考えます。**

### Q-P4-17 Q-P4-14 ②③ の実装

- ②**音を抜く** → ★**GIF では確かめられません**。C-1 の実測（Canvas）と一緒に出すべきですか
- ③**寄る前に一度引く** → 実装できますが、★**いまの合否では効果を測れません**（読めるかは変わらない）。
  **「間」を測る基準がありません。**

### 受領（実装済み・確認不要）

- Q-P4-14 ① 変化を出す → **実装しました**
- 「ボットに見せる」 → **測定として実装しました**

---

## 7. 現物

| ファイル | 内容 |
|---|---|
| `tools/verify-readable.mjs` | ★本体。`--at <残りm>` `--races` `--spread` |
| `tools/diag-uncertainty.mjs` | ★同じ出走表で乱数だけ変える（漏洩を捕まえた道具） |
| `tools/diag-tenkai.mjs` | ★R-16 を逆向きに |
| `packages/render/src/commands.ts` | `effort` / `gap` |
| `packages/render/test/camera.test.ts` | ★展開が画面に出ている検査（消したら落ちる） |

**再現**: `npx tsx tools/verify-readable.mjs --races 1200 --at 1200`

★**能力の幅は交絡ではありません。** 最も能力の近い12頭に絞っても結果は変わりません
（`--spread 1`: 0.928 / `--spread 20`: 0.912 — ★この2つは漏洩していた測定の値です。
差が無いことだけが読み取れる情報です）。

---

## 8. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
2. ★**「クオリティがめちゃくちゃ」の原因が1つ、数字で出ました。**
   絵や動きではなく、**道中に見るべきものが無い**ことです。
   ★**そこはレビュー側の裁定が要ります**（エンジンを触るかどうか）。

---

**テスト 694件緑 / `tsc --noEmit` 通過**
