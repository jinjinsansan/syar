# 引継ぎ書 — γ（着差の見せ方）の決定待ち（次のセッションはここから）

作成: 2026-08-27 / ブランチ `p4/race-30sec-cuts` / HEAD `2b2aa3d`（★**コミットしていません**）
経緯と数字はすべて: `REPORT_P4_V6_DEFAULT_GATE_20260827.md`
先行裁定: `REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md`
前の引継ぎ書: `HANDOVER_P4_STRAIGHT_CONTEST_20260826.md`

---

## ★0. いまオーナーの手元で止まっていること

★**γ をいくつにするか。** 比較映像は 2 seed 分できています。★**見て選ぶだけの状態です。**

| seed | 見るもの（`out/2d-finish-contest/`） |
|---|---|
| ★**253**（競り合いが**在る**側） | `finish-contest-compare-v6-253-small.mp4`（2.1MB） |
| ★**42**（競り合いが**在らない**側） | `finish-contest-compare-v6-42-small.mp4`（1.7MB） |

★どちらも **γ=1.0 / 1.3 / 1.6 を横に 3 つ並べた**もの。★**違うのは写像だけ**です。

★ブラウザで見るなら（★dev サーバーを起動してから）:

```powershell
cd V:\dev\Cusor\star\apps\web ; npm run dev
```

- γ=1.0 … `http://localhost:3210/race?seed=253&cinematography=v6&contest=1.0`
- γ=1.3 … `http://localhost:3210/race?seed=253&cinematography=v6&contest=1.3`
- γ=1.6 … `http://localhost:3210/race?seed=253&cinematography=v6&contest=1.6`

⚠️ ★**`contest=` を省くと 1.3 になります。1.0 ではありません。**
⚠️ ★**不正値も黙って 1.3 に落ちます。** URL は貼り付けで入れること。
⚠️ ★`cinematography=v6` を落とすと v5（オーナー不合格の画角）に戻ります。

---

## ★1. この便でいちばん大事なこと（★次のセッションはここを読む）

### ★1-1. 「A は照会中」ではありませんでした。**8/25 に裁定済み**です

前の引継ぎ書 §4-3 が「せめぎ合いが無いレースをどう見せるかが最大の未解決事項」と書いており、
★私は初め「レビュー側へ照会を出す」と判断しました。★**誤りでした。**

★裁定（`REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md`）は既に答えを出しています:

| 項目 | 内容 |
|---|---|
| 前提の訂正 | ★「カメラで無理 ⇒ 較正を動かすしかない」は**成り立たない**。第三の自由度が **D-064** で開かれている |
| 動かすもの | ★**`RACE_RANDOM_K` は動かさない。** ★**スコア差→タイム差の写像の形（γ）だけ** |
| なぜ安全か | 写像は r について単調 ⇒ ★**着順不変**。総差も定義から不変 ⇒ V-4/V-5/V-6・払戻・人気・V-17② に**定義上触れない** |
| 推奨値 | **γ = 1.3〜1.6** |
| ★**Q-4** | ★**「カメラ側の調整は、写像の判断が出るまで着手しないでください」** |
| 最終判定 | ★**実画面でオーナーが決める**（裁定 §9） |

★**γ の機構は既に実装済み**です（`TIME_GAP_SHAPE_GAMMA`・`packages/race-engine/src/balance.ts:447`）。
★既定 **1.0 = 現行式へ短絡**。正典 R-15 の「入っているが使われない状態で一度コミットする」形。

⚠️ ★**教訓**: ★**引継ぎ書に「照会中」と書いてあっても、裁定書の実在を先に確認すること**（正典 R-25）。
　★裁定は照会の **42 分後**に出ていました。

### ★1-2. 測定器と実画面の γ が違っていました（★R-30 の 5 件目）

```
apps/web/src/app/race/page.tsx:392    DEMO_CONTEST_GAMMA = 1.3   ← ★画面の既定
tools/lib/race-audit-build.mjs:57     DEFAULT_RACE_BALANCE       ← γ=1.0（道具の既定）
```

★**2026-08-25 以降、監査道具はオーナーが見ている画と違う写像を測っていました。**

★前の報告書・引継ぎ書の看板だった「seed 253・**4 頭以上 11.9 秒**」は γ=1.0 の数字です。
★**オーナーが実際に見たのは γ=1.3 の 10.5 秒**でした。

★是正は**対症のみ**入れました（`--gamma` を足し、実行時に必ず γ を印字）。
⚠️ ★**根治は「画面と道具が γ を 1 か所から引く」こと**（D-052 / R-30）。★**共通土台に触るので未着手・要指示。**

### ★1-3. ★★指標そのものが、オーナーの要望を減点していました

★**この便で最も重い発見です。**

「seed 42 は γ で悪化する（主役2頭以上 87% → 50%）」を実画面の位置で追ったところ、
★**悪化ではありませんでした。**

| 表示秒 | γ=1.0 の画面内 | γ=1.6 の画面内 |
|---|---|---|
| 26s | 4番・10番（4.3m 差） | ★**4番だけ**（10番は **8.5m** 後ろ＝画面外） |
| 30s | 4番・10番（1.1m 差） | ★**4番だけ**（5.9m 後ろ） |
| 37s | 10番・4番（3.9m 差） | 4番・10番（★**1.3m** 差） |

- **γ=1.0** … 2 頭が直線を通して**ずっと並走**
- **γ=1.6** … 10 番が ★**8.5m 後ろから追い込んで**、残り僅かで 1.3m まで詰める

★**「主役2頭以上の時間」という指標は、並走を高く評価し、追い込みを低く評価します。**

⚠️ ★**ところがオーナーの元々の要望はこれです**（`QUESTIONS_P4_FINISH_CONTEST_20260825.md` の発端）:

> 画面の左側から**追い込み馬が一気に差してくる**

★**指標が、要望されたものを減点していました。**
★**したがって §2 / §4-2 の数値は、この観点では信用できません。** [EYES] が要ります。

---

## 2. この便でやったこと（★製品コードは 1 行も変えていません）

| 種別 | 中身 |
|---|---|
| 測定 | seed 1〜80 の全数 × γ=1.0/1.3/1.6（台本 v6・`audit-contest-focus.mjs`） |
| 映像 | seed 253 と seed 42 の γ 3 本立て（実ブラウザ取り込み・計 6 本＋並べ 2 本） |
| 道具 | `audit-contest-focus.mjs` に `--gamma` を追加（★実行時に必ず γ を印字・R-8） |
| 道具 | `render-contest-compare.mjs` に `--dirs` / `--tag` / `--out` を追加 |
| 道具 | ★同上の footer べた書き「seed 42 / 台本 v5」を **meta 参照へ是正**（R-30） |
| 報告 | `REPORT_P4_V6_DEFAULT_GATE_20260827.md`（新規） |

★**道具の既定は変えていません**（R-27: 省略時は従来どおりへ落とす）。
★**8/25 に撮った古い証拠（seed 42・台本 v5）は消していません**（`--tag` で出力名を分離）。

### 検証

```
npm test        → 119 ファイル / 1181 件 PASS（★道具変更の前後で同数）
npm run typecheck → クリーン（exit 0）
```

### git の状態（★コミットしていません）

```
M apps/web/package.json          ← ★オーナーの既存作業（ポート 3210）。★触っていない・混ぜないこと
M tools/audit-contest-focus.mjs  ← --gamma
M tools/render-contest-compare.mjs ← --dirs/--tag/--out ＋ footer 是正
?? REPORT_P4_V6_DEFAULT_GATE_20260827.md
?? HANDOVER_P4_GAMMA_DECISION_20260827.md（この文書）
（tools/_gammaprobe.mjs は .gitignore の tools/_*.mjs に該当・追跡外）
```

★**push はしていません**（CLAUDE.md・オーナー指示待ち）。

---

## 3. 測定結果（★指標の欠陥（§1-3）を承知のうえで読むこと）

### 3-1. γ 別の分布（seed 1〜80・台本 v6）

| γ | 平均頭数（中央） | 4頭以上（中央） | 「2頭すら半分未満」 | 跳び最大 |
|---:|---:|---:|---:|---:|
| 1.0 | 2.30 | **0.0 秒** | **42%** | 1.11 |
| ★**1.3（画面既定）** | 3.00 | 0.3 秒 | ★**31%** | 1.04 |
| 1.6 | 3.30 | **3.1 秒** | ★**25%** | 0.97 |

★**γ は効くが解け切らない。** γ=1.6 でも 4 本に 1 本はほぼ単騎。
★**そして γ は底上げであって上積みではない** — seed 253 は **11.9 → 10.5 → 9.5 秒**と**減ります**。

### 3-2. 着順は動いていない（★裁定どおり）

```
seed 253  γ=1.0 / 1.3 / 1.6  すべて [7,6,12,1,9,4,10,11,5,3,2,8]
seed  42  γ=1.0 / 1.3 / 1.6  すべて [10,4,2,5,6,12,9,8,3,7,11,1]
```

### 3-3. 着差ラベル（seed 253・2〜5 着）

| γ | ラベル |
|---:|---|
| 1.0 | 1馬身 / アタマ / 1/2馬身 / ハナ |
| 1.3 | 3/4馬身 / アタマ / クビ / ハナ |
| 1.6 | ★**クビ / ハナ / クビ / ハナ** |

⚠️ γ=1.6 は 4 つ中 2 つが「ハナ」。裁定 §7 の **V-17③③**（ハナ 35% 超を禁ずる）に留意。

---

## ★4. 次にやる順序

```
① γ を決める                                    ← ★★いまここ・オーナー
      ↓
② 80 seed を決めた γ で測り直す                  ← ★ただし §1-3 の指標問題を先に片付ける
      ↓
③ 残った分だけカメラを合わせる（B の跳び・カット境界）
      ↓
④ v6 を既定にするか決める
```

---

## ★5. オーナー判断が要ること（★勝手に決めない）

| # | 論点 | 補足 |
|---|---|---|
| **1** | ★**γ をいくつにするか** | ★映像は用意済み。1.0 / 1.3 / 1.6、または「別の seed でも見たい」 |
| 2 | ★**B（注視点の跳び）を γ 決定前に直すか** | ★**原因が γ でないことが実測で判明**（seed 5/16/18 はどの γ でも跳ぶ）。★先に直す判断は妥当 |
| 3 | ★監査道具と画面の γ を **1 か所から引く**根治 | ★共通土台（`race-audit-build.mjs`）に触る。★他の全監査の数字が動きうる |
| 4 | ★**v5 の既定から `climax-choreography` を外すか** | ★γ と独立・いつでも可。★**いま「不合格の状態が既定」**（R-27） |
| 5 | v6 を既定にするか | 1 が決まってから |

---

## 6. 残件（技術）

1. ★**「主役2頭以上」に代わる指標**（§1-3）。追い込みを減点しない形。
   ⚠️ ★**自作する前に既存を探すこと**（R-30）。`tools/audit-real-overtakes.mjs` が近いはず
2. ★**「画面に入る走路 9.2m」の出どころが追えていない。** 実測は **5〜6m**。
   ★`CONTEST_MAX_LAG_M = 12`（「9.2m の窓の 1.5 倍」）の根拠がこれに依存している
3. ★`audit-contest-focus.mjs` は **0 コマのとき FAIL に倒す**（いまは 0.00 / NaN% と出て「異常なし」に見える・R-3 / R-21）
4. ★**能力順プール（D-018 準拠）で測り直していない。** `buildAuditRace` に `opts.pool` を足すのが素直
5. ★**v5 と v6 を同じ土俵で比べる道具が無い**（`straight-contest` のコマだけを見るため）
6. ★**4 角より手前は今回も見ていない**（前の引継ぎ書 §5-3 のまま）
7. ★カットの尺を実時間で測っていない（前の引継ぎ書 §5-1 のまま）

---

## ★7. 申し送り

1. ★**「照会中」と書かれていても、裁定書の実在を先に確認すること**（§1-1・R-25）。
   ★この便では**裁定が 42 分後に出ていた**のに、私は照会を出そうとしました。
2. ★**測定器が実画面と同じ入力を読んでいるか、毎回確かめること**（§1-2・R-30）。
   ★**この案件だけで通算 5 件目**、★**私自身も probe で 1 回やりました**（報告書 §4-4-4）。
3. ★**指標が「望ましい状態」を減点していないかを問うこと**（§1-3・R-16 の裏返し）。
   ★数字が良い / 悪いを言う前に、★**その指標が何を良しとしているか**を見ること。
4. ★**「テストが通る」を「見た目が合格」と読まないこと**（前の引継ぎ書から継続）。
   ★この便でも 1181 件 PASS のまま、指標のほうが間違っていました。
5. ★**push はオーナー指示があるまで行わないこと**（CLAUDE.md）。
6. ★`apps/web/package.json` のポート 3210 は**オーナーの既存作業**。コミットに混ぜないこと。

---

## 8. 再現コマンド

```powershell
# 測定（すべて読取専用・dev サーバー不要）
npx tsx tools/audit-contest-focus.mjs --seeds 253 --gamma 1.3      # ★--gamma を必ず付ける
npx tsx tools/audit-contest-focus.mjs --seeds 1,2,...,80 --gamma 1.6
npx tsx tools/_gammaprobe.mjs --seed 42                            # 画面内の頭数を位置で追う

# 映像（★dev サーバーが要る・1 本約 20 分 × 3）
$env:AUDIT_SEED=42; $env:AUDIT_SCRIPT='v6'; $env:AUDIT_SHOT='straight-contest'
$env:AUDIT_QUERY='&cinematography=v6&contest=1.6'; $env:AUDIT_FROM=18.73; $env:AUDIT_TO=43.50
npx tsx tools/capture-overhead-stride.mjs --label s42g16
npx tsx tools/render-contest-compare.mjs --dirs s42g10,s42g13,s42g16 --tag v6-42
```

★カットの時間帯は seed ごとに違います。出すには `tools/_gammaprobe.mjs` と同じ要領で
`auditSceneAt` を舐めてください（seed 253 は 19.33〜43.37s、seed 42 は 18.73〜43.50s）。
