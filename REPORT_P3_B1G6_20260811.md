# REPORT_P3_B1G6_20260811 — B-1 / G-6 と、週ループの統一で出たもの

## 本便で正典の改訂が要る決定をしたか

**要ります。2件です。**

1. **§7.2 の `temperDelta` に下限も回復も無いため、全馬の気性が 0 に潰れます。**
   実測（1,800頭・バランス型）で **51.0 → 0.1**。単頭でも 199/200 が**平均40週で 0 に到達**します。
   気性は D-044 で成長のばらつきを決める形質なので、**調教開始から1年弱で形質が1つ消えています。**
   → 正典に「気性の回復」または「下限」の規定が要ります。**こちらでは値を発明していません。**

2. **V-7 / V-14 は「§7.6 のイベントが無く、§7.2 の気性変化も無い」世界で測った数字です。**
   较正値 `INJURY_BASE_PROB = 0.0013` の根拠である V-7a 29.9% は、
   **B-1 が実際に通す経路とは別のループでの値**でした。
   → 差分を測ったところ **V-7a / V-7b は4条件すべてで不変**（後述）なので**較正は生き残ります**が、
     **「どの条件で測った数字か」を正典の測定条件に明記**する必要があります。

そのほか、正典に規定が無いため実装せず照会に上げたものが **Q-P3-20〜26** です（§6）。

---

## 1. 本便で完了したもの

| 項目 | 結果 | 証拠の SHA |
|---|---|---|
| **B-1** 1頭を誕生から引退まで（実 DB・各週の記録） | **PASS**（9項目すべて実データで検査） | `b7cff50` |
| **G-6** 調教の EP 消費を `ep_ledger` に | **PASS**（8項目） | `cb519fa` |
| 週送りの合成器 `advanceWeek`（G-1〜G-5 を1本に統合） | 単体 19件 / 全体 588件 緑 | `b7cff50` |
| 載せ替え差分の実測 `diag-loop.ts` | 判定を出さない診断 | `90b24f4` |
| `--env staging` の一元化（22本） | R-24 のガードを両側で確認 | `ff0baf0` |

**未着手（次便）**: B-3（V-11 の構造報告） / G-7 の staging 実証 / B-6 の配線 / D-043 の残り。
G-7 の**実装は P2 で既にあります**（`apps/worker/src/prize-award.ts`・§11.1 の表・二重発行は
`update races set status='settled' where status='scheduled' returning` の排他で防止済み）。
staging に**レースが1本も無い**ため実証だけが残っています。

---

## 2. ★週ループが3本になりかけていた（設計上いちばん重い発見）

B-1 のために週送りの合成器が要りました。ところが調べると
`verify-v7.ts` と `verify-v14.ts` が**それぞれ自前の週ループ**を持っており、
B-1 で**3本目**を書こうとしていました。

> **3本あると、「較正したループ」と「B-1 が通したループ」が別物になります。**
> V-7a 29.9% は V-7 のループでの値であって、B-1 の経路の値ではありません。

そこで `packages/training/src/week.ts` に `advanceWeek` を作り、**1本に統一**しました。
そして**載せ替えの差分を1つずつ測りました**（いきなり載せ替えると、
数字が動いた理由が2つ同時にあって切り分けられません）。

### 実測（`npx tsx apps/cli/src/diag-loop.ts --horses 1800 --seed 42`・バランス型）

| 条件 | V-7a 恒久 | V-7b 致命 | 故障件/頭 | 最終SP | 気性 | イベント/頭 |
|---|---|---|---|---|---|---|
| ① 旧ループ（V-7 が較正に使った手順） | 29.9% ±1.08 | 0.7% ±0.19 | 0.928 | 486.0 | 51.0 | 0.00 |
| ② 合成器（①と同条件） | **29.9% ±1.08** | 0.7% ±0.19 | 0.928 | **486.0** | 51.0 | 0.00 |
| ③ ＋気性変化（§7.2 `temperDelta`） | 29.9% ±1.08 | 0.7% ±0.19 | 0.928 | 490.1 | **0.1** | 0.00 |
| ④ ＋イベント（§7.6）＝B-1 の経路 | 29.9% ±1.08 | 0.7% ±0.19 | 0.928 | 495.3 | 0.1 | 8.75 |

- **①→② の差 0.00pt** — 載せ替えで手順は1つも変わっていません。
- **V-7a / V-7b は4条件すべてで不変。** 故障確率は気性にもイベントにも依存しないためです。
  → **`INJURY_BASE_PROB = 0.0013` の較正はそのまま生きます。**
- **成長は動きます**: 最終SP 486.0 → 495.3（**+1.9%**）。
  → **V-14（成長の較正ゲート）は測り直しが要ります。** 次便で出します。

★`enableEvents` に**既定値を置いていません**。呼ぶ側が毎回書きます。
既定値を置くと、この取りこぼしが**また呼び出し側から見えなくなる**ためです。

---

## 3. ★§7.2 の気性変化で、気性という形質が消えます（正典の改訂が要る）

§7.2 の表には `併せ馬 気性-2` / `休養（放牧） 気性-5` があります。
上げる経路は §7.6 の「強行」（+10・確率2%の選択肢）だけです。**下がる一方です。**

### 実測（200頭・イベント有・各方針）

| 方針 | 気性0に到達した馬 | 到達までの平均 |
|---|---|---|
| バランス型 | **199 / 200** | 調教開始から **40.0 週** |
| 追い切り偏重 | **199 / 200** | **24.5 週** |
| 放置（軽め調整のみ） | 32 / 200 | 88.6 週 |

現役期間は 182週（78→260）です。**適切に育てるほど早く潰れます**（疲労が溜まる＝休養する）。

### なぜこれが問題か

- D-044 で `temperCoef` を **temper 0〜100 の連続補間**にしました。
  その前提は「集団が 0〜100 に散らばっている」ことですが、**実際は1年弱で全員が 0 に集まります**。
- 気性は §6 で**遺伝する形質**であり、V-2e / V-2f のゲート対象です。
  ただしそれらは**誕生時の値**を見るので、ゲートは通ります。
  → **ゲートは通るのに、遊びの上では形質が死んでいます。**（R-16 の「壊れた状態がゲートを通る」）
- 温順（temper 0）は `rand(0.9, 1.1)` で、**ばらつきが小さく下限が高い**ので、
  ③で最終SPが上がったのはこのためです。つまり**気性難という不利が誰にも残りません**。

### こちらでは直していません

「毎週 +1 回復」「下限 20」「調教で上がる経路」——どれも**正典に無い値の発明**になります。
`temperDelta` は §7.2 の表の写しのまま置いてあります。**裁定をお願いします。**

---

## 4. B-1 の結果と、途中で見つけた3つの欠陥

**実行**: `npx tsx tools/verify-b1.mjs --env staging --seed 42 --menu hard_only`
**対象**: staging の NPC 馬 オルテナフシャロリョス（male / normal / 気性59 / 丈夫さ557）
★**専用の馬を作っていません**（§10.5「専用の簡易ロジックを作らない」）。

```
✓ ① 週の抜けが無い                    260週（0 → 259）抜け 0 箇所
✓ ② 78週未満は EP 0・成長 0            78週 / EP使用 0 / 成長 0
✓ ③ 78週・104週で段階が切り替わる      77:growing 78:trainable 103:trainable 104:racing
✓ ④ current ≤ potential                最大 99.3% 到達
✓ ⑤ 故障した週は成長していない         故障 2週 / うち成長した週 0
✓ ⑥ 故障休養中は EP を使っていない     故障休養 7週 / うち EP 使用 0
✓ ⑦ 引退と行き先が性別と整合           260週 / stallion / age
✓ ⑧ DB を読み直した値がメモリと一致    260週すべて一致
✓ ⑨ 払った EP =「選んだメニュー」の額  EP を払った 108週 / 不一致 0
★B-1: PASS — 260 週すべて記録し、9項目すべてを実データで検査
```

一生の内訳: 故障 2回（mild:2） / イベント 3回 / 休養 7週 / 調教 108週 /
**EP 消費 86,400** / 気性 59.0 → 0.0 / sp 627/662 st 770/856 pw 579/592 gt 511/515 iq 784/885

**★毎週 DB に往復させています。** メモリ上で260週回して最後に1回書くほうが速いのですが、
それでは**永続化の取りこぼしが一切出ません**（列不足・`numeric` が文字列で返る等は
「書いて読み直す」ときにしか出ません）。260週×往復ぶん遅いのは承知の上です。

### ★欠陥①: ⑤⑥ が「対象0件のまま ✓」と出ていた

最初の実行（バランス型）では**故障0回・休養0週**でした。それでも ⑤⑥ は **✓** と出ます。
**何も検査していないのに PASS に見えます。**

R-21 の「**落ちた ≠ 検出できた**」と同じ形で、ここでは「**通った ≠ 検査した**」です。
→ 対象0件の検査は **PASS にせず「未検査」として別に数え、`exit 1`** にしました。
   その上で、故障が起きる条件（追い切り偏重）で流し直し、**9項目すべてを実データで**検査しました。

### ★欠陥②: メニュー内訳が1週合わなかった

非休養 137週に対し EP 消費週が 138週。**§7.6 のイベントが休養に差し替えた週**で、
EP は払ったのに記録上は「休養」でした。**何に払ったかが記録から読めません。**
→ `menuChosen`（選んだメニュー）を `WeekLog` と `horse_week_log` に足しました（0011）。
   検査⑨がこれを見ています。

### ★欠陥③: 2回目の実行が1回目の終了状態から始まっていた

`horses.potential` / `stats` / `temper` / `durability` は一生のあいだ書き換わるので、
2回目は気性 0 のまま・素質は開花イベントで **578 → 606 → 637 と累積**していました。
**落ちも警告もせず「PASS」とだけ出ます。流すたびに証拠が静かに劣化します。**

→ `horses.birth_snapshot` に誕生時の写しを持ち、以後はそこから復元します（0012）。
   ★これは検証の都合だけではありません。§7.5 の恒久ダメージは potential を永久に削り、
   D-045 は「**何が永久に失われたかを明示する**」ことを求めます。
   1回ぶんの内訳は返していますが、**一生ぶんの累計は誕生時の値が無いと出せません。**
   再実行で**完全に同一の結果**（気性59から復元・故障2回・EP 86,400・能力すべて一致）を確認しました。

---

## 5. G-6 の結果

**実行**: `npx tsx tools/verify-g6.mjs --env staging`

```
✓ ① 引き落とし額が §7.2 の表と一致        997,400 = 1,000,000 - 2,600
✓ ② 残高が台帳の累計と一致                台帳合計 -2,600 / 7行
✓ ③ 同じ馬の同じ週は二度引かれない        残高据置・行数据置
✓ ④ 並列に5本叩いても記帳は1行だけ        成功5 失敗0 / 週200の行数1 / -800
✓ ⑤ 残高不足なら引かれず台帳にも残らない  例外あり・残高据置・行数据置
✓ ⑥ NPC 馬は課金対象でない                null が返り台帳に載らない
✓ ⑦ 休養（0 EP）は台帳に行を作らない      記帳7行 = 調教した7週
✓ ⑧ 理由は training 固定・増える行が 0    憲法②
✓ ⑧b 負の額は拒否                         調教で EP が増える経路を作らせない
★G-6: PASS — 8項目すべて成立
```

**★「記帳された」ではなく「記帳を外せない」ことを確かめています。**
引き落としと記帳は `spend_training_ep` の中で**同一トランザクション**です。
二重記帳は `ep_ledger.dedupe_key`（`training:<horse_id>:<week>`）の一意制約で
**DB が拒みます**。アプリ側の「もう処理したか確認してから書く」は同時実行ですり抜けます。

### ★④は最初 成功4/失敗1 でした

記帳も残高も正しかったのですが、1本が一意制約違反で落ちました。
「先に select して無ければ insert」は、その間に別トランザクションが入れば必ずこうなります。
**呼ぶ側（ワーカー）から見ると「課金失敗」に見え、そこで週送りを止めるのは誤りです**
（実際には既に課金済み）。
→ 関数内で `unique_violation` を捕まえ、**既に引かれている残高を返します**（0014）。
   **制約は外していません。** 外すと二度引きが本当に通ります。

---

## 6. 照会（正典に規定が無く、値を発明していないもの）

| # | 内容 |
|---|---|
| **Q-P3-20** | **故障確率の `durability` は現在値か開始時か。** REVIEW_P3_V7_VERDICT は「キャリア開始時の値を**測定条件として固定**」としました。一方 §7.5 の式 `1000/durability` は、重度故障の −100 が**その後の確率に効く**と読めます。★合成器は**ゲームの規則（現在値）**を実装しています。測定条件とゲーム規則が別なら、V-7 は合成器を通せません（実測の差は 640.0 対 634.4 と小さい） |
| **Q-P3-21** | **`horse_week_log` の保持方針。** 1頭182週なので1万頭で182万行になります。検証用に全件残すのか、期間で切るのか |
| **Q-P3-22** | **`injuryRateMult`（§6.5 インブリード由来）が `horses` に列としてありません。** B-1 では **1（倍率なし）** を置きました。近交係数から復元する式が §6.5 にあるなら、そちらが正です |
| **Q-P3-23** | **EP が足りない週の扱い。** §7.2 は費用を定めますが、払えない場合（休養に落とす／調教しない／借りられる）を書いていません。★`spend_training_ep` は**例外を投げます**（呼ぶ側が決める話を DB 側で発明しない） |
| **Q-P3-24** | **プレイヤーがメニューを選ばなかった週の既定。** B-1 は測定用の方針関数を使っています |
| **Q-P3-25** | **★気性の回復・下限**（§3 のとおり。最優先） |
| **Q-P3-26** | **★V-7 / V-14 の測定条件に「イベントの有無」「気性変化の有無」を明記してください。** 現在の較正値はどちらも「無し」で測った数字です |

未回答のまま残っているもの: **Q-P3-12〜16**（§7.6 イベント）、**Q-P3-18/19**（繁殖入りの条件）、
および **V-14 ③ の時間軸 FAIL**（97.2% 対 90.8%。次の梃子は `menuIntensity` でレビュー側の裁定待ち）。

---

## 7. 定数の3分類（本便で追加したもの）

| 定数 | 分類 | 根拠 |
|---|---|---|
| `TRAIN_STREAM = {GROWTH:61, CONDITION:62, INJURY:63, EVENT:64}` | **免除**（登録済み） | 乱数の用途ID。値に意味は無いが、**変えると較正済みの数字が再現しません** |
| `MAX_LIFE_WEEKS = 260+1` | **免除**（登録済み） | 無限ループの番人。**ここに当たったら実装が壊れています** |
| `diag-loop.ts` の `SEED` / `HORSES` | **免除**（パターン登録） | 標本の取り方。この道具は**判定を出しません** |

較正定数の登録簿テストが、これらを**未登録として落としました**（意図どおり働きました）。

---

## 8. 仕様からの逸脱（全件）

| # | 逸脱 | 理由 |
|---|---|---|
| 1 | `advanceWeek` の**順序**（故障→成長→疲労→調子→イベント→引退判定）が正典に無い | §7 は部品を定めますが**繋ぐ順序を書いていません**。イベントを調子の再判定より**後**に置いたのは、前に置くと再判定が「調子+1」を上書きするためで、`events.ts` のコメントに理由を書き `week.test.ts` が固定しています |
| 2 | 故障した週は**成長させない** | 正典に明示なし。故障週に調教の成果が出るのは不自然で、かつ `applyInjury` が potential を削った直後に `grow` を呼ぶと D-045 の不変条件検査に当たります |
| 3 | `horses.birth_snapshot` を足した | 正典に無い列。★新しいゲーム上の量ではなく**誕生時の写し**です。§7.5 の恒久ダメージがある以上、これが無いと D-045 の「失ったものの明示」を一生ぶんで出せません |
| 4 | `ep_ledger.dedupe_key` を足した | 正典に無い列。憲法③（サーバー権威）を**制約で担保する**ためのもので、ポイントの意味を一切変えません |
| 5 | `schema_migrations` を足した | 運用上の欠陥の修正（§9） |

---

## 9. 途中で直した運用上の欠陥

- **マイグレーションに追跡表が無く、毎回すべてのファイルを流していました。**
  0010 を足した時点で 0002 の `create policy` が落ちました。
  **問題は落ちたことではなく「適用済みかを DB から知る術が無い」ことです。**
  → `schema_migrations`（チェックサム付き）を追加。**適用済みファイルが書き換えられていたら実行を拒みます**。
  staging は `--baseline 0009` で記録し、0010〜0014 を適用しました。
- **22本のツールが `secrets.local.env` をべた書き**していました。1本ずつ直すと必ず漏れ、
  漏れたものは「staging のつもりで本番を叩く」形で現れます。→ `tools/lib/env.mjs` に集約。
  **既定は本番のまま**です（既定を staging にすると「本番を確かめたつもりが staging だった」という逆向きの取り違えが起きます）。
  接続先は**毎回 stdout に出します**。
- **`--env` を足したことで `process.argv[2]` を直読みしていたツールが壊れました**
  （`Number('--env')` = NaN。落ちずに NaN のまま進みます）。→ `positionals()` に集約。

---

## 10. R-24 のガードを両側で確認しました

| ツール | 向き先 | 結果 |
|---|---|---|
| `verify-a2.mjs` | **本番** | `exit 1`「"production" 宣言なので実行しません」 |
| `verify-a2.mjs` | **staging** | `exit 0` PASS（作成2件・後片付け後 残存0件） |

新規の `verify-b1.mjs` / `verify-g6.mjs` も `STATE_CHANGING` に登録済み（メタテストが緑）。

---

## 11. 再現コマンド

```bash
npm run typecheck && npx vitest run                      # 588件 緑
npx tsx apps/cli/src/diag-loop.ts --horses 1800 --seed 42  # 載せ替え差分（約25分）
npx tsx tools/migrate.mjs --env staging                    # 0010〜0014
npx tsx tools/verify-b1.mjs --env staging --seed 42 --menu hard_only
npx tsx tools/verify-g6.mjs --env staging
```

---

## 12. ★オーナーへの依頼（再掲）

**staging の DB パスワードと service_role キーのローテーションをお願いします。**
2026-08-10 に受け渡しファイルをマスクして表示しようとして失敗し、**値がそのまま出ました**。
レビュー側の規則では service_role / DB パスワードの露出は**ローテーションが必要**です。
以後、受け渡しファイルは `tools/import-staging-secrets.mjs` が**機械だけで読み**、
人が見るのは「キー名」と「文字数」だけにしてあります。
