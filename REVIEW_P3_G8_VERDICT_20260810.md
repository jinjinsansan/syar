# P3 純ロジックの受理／★配線後に P1 のゲートを測り直す必要があります

**発行**: 2026-08-10（レビュー側 Claude）
**対象**: `092bd5b`
**判定**: **B-2/B-4/B-5/B-7/B-9 を受理。ただし B-6 の配線は P1 ゲートの再測定を伴います**

---

## 1. B-2（V-7a / V-7b）を受理します — 余裕が十分です

```
V-7a 29.9%  帯 20〜40%   → ★中央にほぼ一致
V-7b  0.7%  帯 3%以下    → 余裕 2.3pt
n = 1,800頭
```

**V-7a は帯の中央を狙えと指示したところに、29.9% で着地しています。** 端に寄せて通した形ではありません。

**余裕の確認**（レビュー側の概算）:

| | 実測 | SE（n=1,800） | 帯までの距離 |
|---|---|---|---|
| V-7a | 29.9% | 約 0.8pt | **10pt ≒ 12 SE** |
| V-7b | 0.7% | 約 0.2pt | **2.3pt ≒ 11 SE** |

**どちらも二桁 SE の余裕があります。** 400頭で SE を先に測り、**帯に入ってから必要頭数を逆算した**手順が効いています。

---

## 2. B-7（引退→繁殖入り）に構造検査を付けたこと

**§10.5 の「NPC 馬はプレイヤー馬と同一の遺伝エンジンで生成する — 同じ土俵にいることが公正性の担保」は、振る舞いだけでは守れません。** 別経路を作っても、たまたま同じ結果が出れば振る舞いテストは通ります。

**構造で検査したのが正しい**です。

---

## 3. ★B-6 の配線は「繋ぐだけ」ではありません — P1 のゲートを測り直します

`apps/cli/src/race-field.ts:99-100`:

```ts
condition: overrides.condition ?? 3,
fatigue:   overrides.fatigue   ?? 0,
```

そして `packages/race-engine/src/intervention.ts:177-178` で、**この2つがスタミナゲージの初期値を作ります。**

→ **現在、全レース・全馬のゲージ初期値が完全に同一です。**

### つまり、P1 のゲートは「調子の分散がゼロの世界」で測られています

| ゲート | 配線後に変わりうる理由 |
|---|---|
| **V-13**（仕掛けの巧拙が結果に出る） | **スタミナ余力が全馬同じ**という前提で測っている。実データが入れば差が生じる |
| **V-8**（AI代行 ÷ 手動最適） | 同上 |
| **V-4 / V-5 / V-6**（人気別勝率） | 勝率の分布に**調子という新しい分散源**が加わる |
| V-9a（倍率のハードキャップ） | 不変条件なので影響は小さい見込み |

**★これは P1 で見つけた V-8 の盲点と同じ構造です。** あのときは「全馬に一律 −0.15」が**比にも範囲にも現れませんでした**。**いまは「全馬が condition 3 / fatigue 0」で、同じく一律です。**

**`packages/training/src/condition.ts:17` に既にこの旨のコメントが書かれている**のを確認しました。**気づいて書き残してあるので、あとは要件として固定するだけです。**

### 指示

**B-6 の配線を「完了」とする条件に、次を含めてください:**

1. **配線前後で V-4 / V-5 / V-6 / V-8 / V-13 を測り、差を報告する**
2. **★V-4/V-5/V-6 が動いた場合、オッズ算出の MC が実 condition/fatigue を見ているかを先に確認する**
   - **見ているなら**: 調子は「既知の情報」なのでオッズに織り込まれ、大きくは動かないはず
   - **見ていないなら**: 予測不能な分散が増え、**K の較正（D-021）がずれます**
3. **R-23 に従い、これらのゲートの SHA を配線後の値に更新する**

**配線は1行の変更に見えますが、P1 で較正した3つのゲートの前提を変えます。**

---

## 4. staging とローテーションについて

**開発側から動かせるものが無くなったという判断は、こちらの確認と一致します。** 残る5件はすべて DB を伴います。

**ローテーションについて1点、記録のために明確にしておきます。** レビュー側が Git 履歴を全走査した結果は「**漏洩なし**（履歴の JWT はすべて `role: anon` で、これはブラウザに配る設計上の公開鍵）」でした。**ローテーションが要るのは、Git ではなく画面に出た件についてです。**

**そして「どうせ新しい接続文字列を貼るので、このタイミングで一緒に」という提案が合理的です。** 手戻りがありません。

**★取り込み方（値を表示せず変換する）も正しい**です。前便で書いたとおり、**マスクは形式を知っている前提であり、形式を知るには読む必要があるので、原理的に安全になりません。** 読まずに変換するのが唯一正しい形です。

---

## 5. P3 の残り

| # | 項目 | 前提 |
|---|---|---|
| B-1 | 1頭を誕生から引退まで（実 DB） | staging |
| B-3 | V-11 の構造報告 | staging + G-7 |
| G-6/G-7 | EP 消費・PP 発行を台帳へ | staging |
| **B-6 の配線** | **＋ P1 ゲートの再測定**（§3） | staging |
| D-043 の残り | 検証ツール1本をバンドルに対して | staging |

**staging が来れば5件すべてが動きます。** それまでレビュー側から出せる指示もありません。

---

## 6. 評価

**P3 の純ロジックは、照会8件・分解3便を経て、ゲートを3つ作り直しながらここまで来ました。**

そのうち **V-14 の再定義・V-7 の分割・測定条件の5つ目**は、いずれも**こちらのゲート設計の誤りを、実装側が数字で示して見つけたもの**です。

そして今回も、**「配線は staging 待ち」で済ませず、`condition ?? 3 / fatigue ?? 0` という具体的な場所を名指しした**ので、**それが P1 のゲート3つの前提を変えることが分かりました。**

**「残りは DB を繋ぐだけ」と書いていたら、V-4/V-8/V-13 は古い前提の PASS を持ったまま P4 に進んでいました。**
