# P4 追補 — アセット制作は `image-cockpit-for-codex-workflows` を使います

**発行**: 2026-08-13（レビュー側 Claude）
**根拠**: **オーナー指示**（「元々ディレクトリにある2Dの描画スキルを必ず使うこと」）
**位置づけ**: `DEV_INSTRUCTIONS_P4_20260813.md` の U-1 への追補

---

## 0. 確認した事実

| # | 事実 |
|---|---|
| 1 | **`star/image-cockpit-for-codex-workflows/` に実在**します |
| 2 | **ピクセルアート生成 → アニメーションフレーム → スプライトシート**（`*-sheet.png`）を作るローカルツール |
| 3 | 生成は**ローカルの Codex ハンドオフ**経由（`codex-handoff/inbox` → `assets` → `outbox`） |
| 4 | **`star/.gitignore:24` で除外**されています（＝ツール本体は star のリポジトリに入りません） |
| 5 | `docs/prompt-examples/` にキャラクター系のプロンプト例、`docs/usage/manual-handoff.md` に手順 |
| 6 | **内蔵の手続き的 PNG 生成は「主たる画質の経路ではない」**と README が明記（フォールバック用） |

---

## 1. 指示

### A-1【必須】馬のスプライトはこのツールで作る

**他の方法（手描き・別ツール・内蔵フォールバック）を主たる経路にしないでください。** 内蔵の手続き的生成は、README のとおり**テスト用のフォールバック**です。

### A-2【必須】★生成ジョブをアセットと一緒にコミットする

**ツール本体は gitignore されているので、star に残るのは出力だけです。** **出力だけでは、そのスプライトがどう作られたかが失われます。**

| 何を | どこに |
|---|---|
| スプライトシート（`*-sheet.png`） | star の assets 配下（場所を決めて報告してください） |
| **★生成ジョブ（`codex-handoff/inbox/` の JSON）** | **同じ場所に、同じ名前で** |

**理由は R-23 と同じです** — **プロンプトの無いアセットは、出所の無い証拠です。** **「この馬をもう1頭、同じ絵柄で」ができなくなり、修正もできません。**

### A-3【必須】★まず1頭だけ作って、端から端まで通してください

**大量に作る前に、1頭で次を確定させてください:**

| # | 確定させるもの |
|---|---|
| 1 | **スプライトシートの契約**（フレーム寸法・列数・フレーム数・基準点） — **§12.8 の「描画コマンド」はこれに対して定義されます** |
| 2 | **1頭あたりの実バイト数** — **C-3（初回ロード）の上限を決めるのに要る数字**（前便で「先に実測してください」と書いたもの） |
| 3 | **18頭を同時に描いたときのフレーム時間** — C-1 の実測 |

**★2 は、前便の「性能の上限は実測してから決める」への答えそのものです。** **1頭のバイト数と、必要な頭数・動作パターン数を掛ければ、ロード予算が根拠つきで出せます。** **別プロジェクトの「5MB」を持ち込む必要がなくなります。**

---

## 2. アートバイブルについて

**`docs/prompt-examples/` があるということは、プロンプトの語彙をそろえないと絵柄が揃わない**ということです。

**レビュー側でアートバイブル（視覚的アイデンティティの仕様）を作り、star に納品します。** ただし**視覚の方向性はオーナーの創作判断**なので、**オーナーの合意を得てから着手します。**

**それまでは A-3 の「1頭」を、絵柄の確定ではなく**技術的な契約（シート形式・バイト数・フレーム時間）を確かめるための試作**として作ってください。** **絵柄が後で変わっても、契約は変わりません。**

---

## 3. 着手順（更新）

```
1. §12.8 の描画抽象化（承認済み・着手可）
2. ★馬1頭のスプライトシートを image-cockpit で試作
3. 1 と 2 を繋いで、シート契約・バイト数・フレーム時間を実測    ← ここで C-1/C-3/C-4 の上限が決まる
4. アートバイブル納品後、本番の絵柄で量産
5. U-2（介入UI）→ C-6 をボット3種で測る
```

**3 が終わるまで量産しないでください。** **契約が変わると、作ったシートを全部作り直します。**
