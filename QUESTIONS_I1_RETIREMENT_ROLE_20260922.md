# 照会 I-1: 引退時の役割選択（種牡馬／繁殖牝馬／功労馬）— 2026-09-22

> 開発側 → レビュー側。PLAN_BLOODLINE_INHERITANCE_20260922.md の I-1。
> **コードは書いていません。** 下の §1 の事実を確かめ、§3 の照会に答えをもらってから着手します。
> 本番には触れていません（読み取りもしていません）。

## §0 一行

**いま持ち主のいる馬が引退すると、本人の選択も所有上限も無しに種牡馬／繁殖牝馬になり、
NPC の配合に使われ、生まれた仔は持ち主のいない NPC 馬になる。** I-1 はこの穴を塞ぐ便です。

## §1 事実（原文で確かめたもの）

| # | 事実 | 箇所 |
|---|---|---|
| F-1 | 引退時の役割は `decideRetirement(sex, reason, preferHonored=false)` だけで決まる。**牡→stallion・牝→broodmare**。功労馬になるのは `preferHonored` が true のときだけ | `packages/training/src/retirement.ts:62-69`、`week.ts:331-336` |
| F-2 | ワーカーは `preferHonored` を**一度も渡していない**（`apps/` のソースで grep 0 件。当たるのはビルドのキャッシュだけ）。持ち主のいる馬も NPC と同じく自動で繁殖入りする | `apps/worker/src/*.ts` |
| F-3 | **所有上限（繁殖牝馬 10・種牡馬 5・§6.7／D-104）は引退の時点で数えていない**。TS で `canOwnMore` を呼ぶのは初回配合（`player-breeding.ts:262`・`'active'`）の 1 か所だけ。`'broodmare'`／`'stallion'` で呼ぶ箇所は 0 | `packages/scheduler/src/ownership.ts:45`、LR-2（§18）「繁殖入り・種牡馬入りした馬は所有なので数える」 |
| F-4 | NPC の配合の母の集合は `retirement_role = 'broodmare'` を **`owner_id` で絞っていない**。持ち主のいる繁殖牝馬が NPC の AI に配合される | `breeding-runner.ts:531` |
| F-5 | 種牡馬の集合も `owner_id` で絞っていない。持ち主のいる種牡馬が **NPC の牝馬に種付けされ、持ち主に種付料が入らない**（D-107 の「プレイヤー間は 20% 焼却」の経路が存在しない） | `breeding-runner.ts:582` |
| F-6 | 生まれた仔の `insert` は `owner_id` を書かない → **持ち主の繁殖牝馬の仔が NPC 馬になる** | `breeding-runner.ts:682` |
| F-7 | 年の変わり目の繁殖牝馬の補充は `honored` の牝馬から上げるが、これも `owner_id` で絞っていない → **持ち主の功労馬が勝手に NPC の繁殖牝馬に戻される** | `breeding-runner.ts:493` |
| F-8 | 初回配合の母（案 B）は `owner_id is null` の honored 牝馬に絞ってある（こちらは正しい） | `player-breeding.ts:162` |

> ⚠️ 本番で持ち主のいる引退馬が**いま何頭いるか**は測っていません（本番の読み取りは許可制）。
> 何頭いても「規則が無い」ことは変わらないので、数は §3 の答えを待ってから 1 コマンドの読み取りとして出します。

## §2 開発側の提案（たたき台）

1. **引退した時点では役割を「保留」にしない。** エンジン（`decideRetirement`）は変えず、持ち主のいる馬だけ
   **既定を honored（功労馬）** にする。繁殖入りは持ち主が選んだときだけ。
   - 理由: 上限を超える自動の繁殖入りを作らない（F-3）。選ばなかった人が不利にならない（功労馬は後から上げられる）。
   - ⚠️ `preferHonored` はエンジンの入力として既に在る（F-1）。ワーカーが `owner_id is not null` のときに true を渡すだけで済み、**遺伝エンジンに触らない**（§10.5「同じ土俵」を保つ）。
2. **RPC `request_breeding_role(horse_id, role)`**（持ち主だけ・引退済み・性別に合う役割・上限内）。
   I-2 と同じ **依頼表 ＋ ワーカーが確定** の形（`foal_requests` に `kind = 'promote'` を足すか、別表にするかは §3 Q-5）。
   上限は `canOwnMore('broodmare' | 'stallion', 現在数)` をワーカーが錠を取って数える。
3. **NPC の配合から持ち主の馬を外す**（F-4〜F-7）。母・種牡馬・補充の 3 つの query に `owner_id is null` を足す。
   - ⚠️ 持ち主の種牡馬を他人（NPC を含む）に貸す経路は D-107（種付料）と I-4（種牡馬の市場）の便で作る。**それまでは誰にも使われない。**
4. 持ち主の繁殖牝馬と種牡馬の配合は、I-2 の `kind = 'breed'`（EP の種付料つき）の便で作る。I-1 は**役割を決めるところまで**。

## §3 照会（答えが要るもの）

- **Q-1 既定の役割**: 持ち主のいる馬が引退したときの既定は honored（§2-1）でよいか。
  それとも「保留（役割 null・一定週で honored へ）」を作るか。後者は `0010` の check（retired なら role not null）を緩める移行が要る。
- **Q-2 上げ直し**: 一度 honored にした馬を後から繁殖に上げてよいか（開発側は「よい」を推す — 上限が空いたときの入れ替えに要る）。
  逆に**繁殖から降ろす**（broodmare → honored）も持ち主が選べるか。
- **Q-3 上限の数え方**: 上限に数えるのは `retirement_role in ('broodmare','stallion') and owner_id = 自分` か。
  **産み終えた（生涯 8 産）繁殖牝馬**は NPC 側では年の変わり目に honored へ降ろしている（F-7 の手前）。持ち主の馬も同じ規則で自動で降ろし、枠を空けてよいか。
- **Q-4 NPC からの除外の時期**: §2-3（F-4〜F-7 を塞ぐ）は、I-1 の役割選択より**先に単独の便**で入れるべきか。
  開発側は「先に入れる」を推す（いまのままだと、I-1 を待つ間も持ち主の仔が NPC 馬として生まれ続ける）。
  ⚠️ ただし NPC の繁殖牝馬の数（`broodmareTarget`）から持ち主の馬が抜けるので、**供給の釣り合い（`verify-pool-supply`）を測り直す**必要がある。
- **Q-5 依頼の置き場所**: `foal_requests` に `kind = 'promote'` を足すか、`role_requests` を新しく作るか。
  前者は shape の check が膨らむ。後者は冪等の作りを複製する。開発側は**別表**を推す（仔を作らない依頼を foal_ に入れない）。
- **Q-6 語**: 画面・コードの語は「種牡馬入り／繁殖入り／功労馬」とし、他社製品の機能名に当たる語を使わない（PLAN の §0 の注意と同じ）。これでよいか。

## §4 着手の順（答えをもらった後）

1. Q-4 が「先」なら: NPC の 3 つの query に `owner_id is null`（テスト: 持ち主の馬が母・種牡馬・補充に**入らない**ことと、対照として NPC の馬は入ること）→ `verify-pool-supply` を測る → 報告
2. ワーカーが持ち主の馬に `preferHonored = true` を渡す（テスト: 牡牝 × 持ち主有無の 4 通り）
3. 依頼表・RPC・ワーカーの確定（上限・錠・冪等）→ staging の予行（必ず rollback）→ 報告

本番への適用・push はオーナーの直接の指示を待ちます。
