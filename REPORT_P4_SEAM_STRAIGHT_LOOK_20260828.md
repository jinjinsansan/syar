# 報告 — `straight-contest → homestretch-front` の跳びを見た（★測るだけ）

作成: 2026-08-28 / ブランチ `p4/race-30sec-cuts`
指示: `REVIEW_P4_CUT_SEAM_REOPEN_VERDICT_20260828.md` §4-3 の宿題 4

> ⚠️ ★**測るだけです。直す判断は裁定 §4-3 の ④**（閃光後・γ 確定後・測定器の根治後）です。
> ★製品コードは 1 行も変更していません。

---

## 0. なぜ見るのか

裁定 §4-3:

> ⚠️ ★`straight-contest → homestretch-front` の**最大 2919px** は、閾値と別に一度見てください。
> ★画面幅の 2.3 倍を共通の馬が跳んでいます。**反転が無くても「同じレースに見えない」を作りうる量です。**

★**跳び px の最大は、反転していない境目で出ています。** 反転している 4 角の境目は中央 900/964・最大 1563。
★**跳び px は反転の指標ではありません**（裁定 §4-2 の [CODE] 事実）。

---

## 1. 測ったこと

道具: `tools/_seamlook.mjs`（新規・読取専用・追跡外）
条件: 台本 **v6** / γ **1.6（暫定）** / 1280×720 / 30fps

★境目の前後 1 コマで、★**先頭馬が画面のどこに・どの大きさでいるか**を出します。

| seed | 先頭と 2 着の差 | 前 `straight-contest` | 後 `homestretch-front` | 先頭は画面内か |
|---|---:|---|---|---|
| **16** | **23.6m** | (678, 516) **334px** | (308, 451) **174px** | 入っている |
| **11** | 12.2m | (630, 442) 284px | (788, 428) 164px | 入っている |
| 19 | 15.5m | (629, 441) 283px | (799, 427) 164px | 入っている |
| 42 | 7.6m | (623, 432) 277px | (860, 424) 162px | 入っている |

---

## 2. ★分かったこと

### 2-1. 跳びは 2 つの成分に分かれます

| 成分 | 大きさ | 性質 |
|---|---|---|
| ★**大きさが半分になる** | **0.52〜0.58×**（4 seed とも同じ） | ★**画角の比 2.43× そのもの**。`straight-contest` 7.6° → `homestretch-front` 18.5° |
| ★**横に動く** | **158〜370px** | ★**レースの形で向きも量も変わる** |

★**大きさの半減は毎回同じ**です。★**横の動きだけがレースごとに違います。**

### 2-2. ★横の動きの向きは、レースの形で変わります

```
seed 16（差 23.6m・逃げ切り）  x 678 → 308   ★左へ 370px
seed 42（差  7.6m）            x 623 → 860   ★右へ 237px
seed 11（差 12.2m）            x 630 → 788    右へ 158px
seed 19（差 15.5m）            x 629 → 799    右へ 170px
```

★**seed 16 だけ逆向き**です。★差が 23.6m と大きく、`homestretch-front`（`target: 'pack'`）が
★**馬群を収めようとして、離れた先頭が画面の左寄りに置かれる**ためと見ています。

⚠️ ★**これは見立てです。** 4 seed しか見ておらず、★**閾値も原因も断定していません。**

### 2-3. ★先頭は画面から出ていません

★4 seed とも `inFrame`。★**「共通の被写体が 0 頭」は起きていません。**

⚠️ ★ただし seed 16 の後の位置 **(308, 451)** は、★**コース図（ミニマップ）のすぐ隣**です
（ミニマップはおよそ x 40〜300 / y 325〜525）。★重なってはいませんが、余裕はありません。

---

## 3. ★実画面（seed 16・25.93s の前後）

```
前 25.88s  straight-contest   先頭 10 が画面中央で巨大・単騎（実況「後続を離す」）
後 26.05s  homestretch-front  先頭 10 が画面左下寄りで小さく、後方の 11 頭が奥に並ぶ
```

★**同じ馬が「画面いっぱい」から「隅の小さい馬」になります。**
★2 着まで 9.9 馬身の逃げ切りなので、★**寄りのカットには 1 頭しか入らず、
引きのカットには全部入る** — その差が最大値として出ています。

---

## 4. ★言えること / 言えないこと

★**言えること**

1. ★跳びの**半分は画角の比**（2.43×）で、これは**カットの設計そのもの**です
2. ★残りは**横の動き**で、★**レースの形（先頭が離れているか）で向きが変わります**
3. ★**先頭は画面から出ていません**（共通被写体 0 頭は起きていない）
4. ★**最大値が出るのは逃げ切り**のレースです（寄りに 1 頭・引きに 12 頭）

★**言えないこと**

1. ★**この量が「同じレースに見えない」を作るかどうか** — ★これは目で見る判断です
2. ★閾値 — ★裁定どおり**引きません**（R-16）
3. ★4 seed は分布ではありません（R-20）。★20 seed の分位は照会文 §5 にあります

---

## 5. 検証

```
npm test        → 121 ファイル / 1200 件 PASS（exit 0）
npm run typecheck → クリーン（exit 0）
```

★製品コードは変更していません。追加したのは読取専用の道具 1 本（追跡外）と本報告だけです。

## 6. 再現

```powershell
npx tsx tools/_seamlook.mjs --seeds 16,11,19,42 --gamma 1.6
# 実画面
$env:AUDIT_SEED=16; $env:AUDIT_SCRIPT='v6'; $env:AUDIT_SHOT='finish-line'
$env:AUDIT_FROM=25.75; $env:AUDIT_TO=26.15; $env:AUDIT_QUERY='&cinematography=v6&contest=1.6'
npx tsx tools/capture-overhead-stride.mjs --label seam16
```
