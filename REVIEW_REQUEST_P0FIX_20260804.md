# P0-fix レビュー依頼 — 開発側 → レビュー側

**発行**: 2026-08-04（開発側 Claude）
**対象**: `DEV_INSTRUCTIONS_P0FIX_20260804.md`（第2号指示書）
**成果物**: `https://github.com/jinjinsansan/syar`（private）/ branch `main` / commit `1321ce4`

---

## 0. 結論

合格基準7項目すべて PASS です。

| 合格基準 | 実測 | 判定 |
|---|---|---|
| 1. V-1 が 12〜18%（100年・300年とも） | 100年 15.28〜16.58% / 300年 14.63〜16.14% | PASS |
| 2. V-2a / V-2b / V-2c すべて | 傾き -0.24〜+0.01%/世代 / 天井 57〜60% / 300年V-1 14.63〜16.14% | PASS |
| 3. V-3 が設定値 ±0.5pt | 乖離 最大 0.016pt | PASS |
| 4. 距離適性の分化が保たれる | 実運用規模で創始の約1.2倍に平衡（§4） | PASS |
| 5. テスト・typecheck・`any` ゼロ | 58件 PASS / exit 0 / ゼロ | PASS |
| 6. 決定論 | 同一シードで JSON バイト一致 | PASS |
| 7. 4シード以上で安定 | 4シード × 100/300年すべて PASS | PASS |

**P0-fix 後は `npm run simulate` の exit code が 0 になります**（P0 では V-2 FAIL で 1 でした）。

---

## 1. 独立再実行の手順

```bash
git clone https://github.com/jinjinsansan/syar.git && cd syar
npm install

npm test                                                    # ① 58件
npm run typecheck                                           # ② exit 0
npm run simulate -- --generations 100 --seed 42             # ③ 100ゲーム内年
npm run simulate -- --generations 100 --seed 42 --long-horizon 300  # ④ V-2c 同時判定
npm run simulate -- --generations 300 --seed 42             # ⑤ ④のV-2cと同一結果
npm run verify                                              # ⑥ 4シード × 100/300年（合格基準7）
npm run verify:distance                                     # ⑦ F-3 の根拠（※20分程度）
```

`npm run verify` が §3 の表をそのまま出します。まずこれを見ていただくのが早いです。

---

## 2. 特に検算していただきたい2点

### 2-1. F-3 は「拡散」だけでなく「収縮」も防いでいる

レビュー側監査 A-2 が実測された「20世代で SD 369→591 と拡散」は、**回帰が無効だった P0 時点**の挙動です。F-1 で回帰0.20 を有効にすると、同じ値域比スケールのままでは今度は **SD 359→137 まで収縮**します（全馬が同じ距離型になる）。

**放置すると失敗の向きが反転するだけで、どちらでも §5.2 の分化は失われます。** F-3 は F-1 とセットで必要でした。`npm run verify:distance` の1行目で再現できます。

採用値（`distance_center: sd 73 / clamp 243 / 回帰 0.01`）は、平衡時のアレルSD = `sd / sqrt(2r - r²)` から創始水準を逆算したものです。候補5種 × 4シード × 100/300/600年の比較表は REPORT §4-2 にあります。

### 2-2. 距離SDのシード間の振れは小集団の artifact でした

既定（繁殖牝馬200頭）では距離SDが 0.67x〜1.53x と振れます。これを「実装が不安定」と読まれると困るので、原因を切り分けました。

| 規模 | seed 42 | seed 2026 | 振れ幅 |
|---|---|---|---|
| 繁殖牝馬200頭（既定・Ne≈45） | 1.41x | 0.67x | 0.74 |
| 繁殖牝馬1000頭 | 1.20x | 1.25x | **0.05** |

規模を5倍にすると振れ幅が 0.74 → 0.05 に縮み、**1.2x前後へ安定収束**します。距離適性には選抜がかからないため浮動が支配的で、小集団ほど振れるという教科書どおりの挙動です。正典 §10 の NPC 2,500頭規模では実質的に消えます。再現:

```bash
npm run simulate -- --generations 300 --seed 42 --population 1000 --stallion-pool 300
```

残る 1.2倍ぶんは**大物覚醒**です（`BIG_ATAVISM_RATE=0` にすると SD 435→361＝創始水準ちょうど）。指示書 §2 の指示どおり機構は一切触っていません。

---

## 3. 逸脱・解釈（P0-fix で新たに生じた4件）

REPORT §6 に全件あります。**判断をいただきたいのは2件目です。**

| # | 内容 |
|---|---|
| 1 | V-2a の判定を傾きの**絶対値**で行った（「平坦化」なので急落も不合格と解釈。文字どおりより厳しい側） |
| 2 | **距離系だけ回帰率を 0.01 にした**（全体は 0.20）。§6.4 は全形質共通に読め、§5.2 は距離を特別扱いしており、どちらからも「距離に回帰をかけるか」が読み取れないため → **§7 Q1 で正典への一文追記をお願いしています** |
| 3 | 距離系の clamp/sd 比を 3.33 にした（能力5種は 1.67）。同じ比だと平衡SDが創始水準を下回るため |
| 4 | 絶対値指定の形質では大突然変異SDも通常変異との比（2倍）を保つ |

---

## 4. 未解決の質問（REPORT §7）

- **Q1** §6.4 の平均回帰と §5.2 の距離適性の関係を正典に明記してほしい（上記逸脱2）
- **Q2** 大物覚醒を距離系にも適用し続けるか。いま実害はないが、**P1 でレースエンジンが入り `distance_range` の大きい万能型が有利になると、選抜圧 × 単調ラチェットで万能型に収束する**恐れがある。P1 の V-4〜V-6 較正時に併せて確認したい
- **Q3** 検証の既定集団規模を実運用（NPC 2,500頭）に寄せるべきか。P0 第1号指示書が200頭と定めているため今回は変えていない

---

## 5. 補足

- `apps/cli/src/sweep.ts`（P0 当時の Q1 判断材料）は**役目を終えた記録**としてヘッダに明記のうえ残置しました。現行の検証は `verify.ts` です
- `CLAUDE.md` は STAR 版への差し替えを確認したうえで、git 追跡対象に戻しました（禁止語ゼロを検閲済み）。次のセッションから正しい前提で起動します
- 憲法4項目は今回も独立に grep 済みです（`Date.now`/`Math.random` ゼロ・他社名称ゼロ・実在馬名ゼロ・`any` ゼロ・機密ファイル未追跡）
