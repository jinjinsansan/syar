# レース参考映像再現 作業経緯記録

記録日: 2026-08-16  
対象: STAR レース画面  
参考資料: `ダービースタリオン/ダービースタリオン2025の２D参考用.mp4`

## 1. 目的と方針

参考映像の固有素材や文言を複製するのではなく、2D競馬中継としてのショット構成、カメラ方向、馬群の見せ方、背景の奥行き、ゴールまでの演出文法を分析し、STAR独自素材で再構築する。

最初に「完全再現できない理由」を調査した結果、単一の追走カメラや横向き素材の使い回しだけでは、参考映像にある方向別ショット、距離ごとの隊列差、コーナーと直線の構図変化を表現できないことが主要因と判明した。また、目視修正だけで機械的な比較・回帰判定がなく、一箇所の修正が別区間を壊しても検出できない状態だった。

このため、以下を実装原則とした。

- レース結果・位置計算と、映像上のショット選択を分離する。
- side / diag-front / diag-rear / rear / high-diagを別素材として扱う。
- ショットを純粋関数で決定し、Web表示とオフライン監査で共有する。
- 代表静止画だけでなく、全編を一定間隔で機械監査する。
- 短距離の寄りを維持しながら、長距離の縦長隊列へ画角を適応させる。

詳細計画はルートの`RACE_REFERENCE_REPRODUCTION_PLAN.md`に記録している。

## 2. 実装した基盤

### ショットシーケンサー

`packages/render/src/shot-sequence.ts`を追加し、以下のショット族を定義した。

- start-wide
- formation
- side-pack
- close-pack
- corner-wide
- corner-chase
- straight-wide
- finish
- winner

ショットごとにview、target、transition、カメラプリセットを持たせた。終了判定、残り距離、レース局面、表示時間から同じ入力に対して必ず同じショットを返す。

### 距離適応カメラ

固定画角では1600mを直すと3000mで後続馬が切れ、3000mへ合わせると1200mで馬が小さくなりすぎた。そのため終盤のcorner-chase、straight-wide、finishだけ、1600〜3000mの間でカメラ距離・高さ・FOVを連続補間する`shotCameraForDistance()`を追加した。

### 背景と透視投影

背景全面を地表色で初期化し、透視投影外の黒抜けを解消した。内馬場、細い管理路、二段ラチ、刈り筋、樹林、距離標識、照明塔をコース座標上へ配置した。注視地点をgate / backstretch / corner / homestretchへ分類し、直線の観客席、向正面の設備棟、コーナーの樹林密度を切り替えるようにした。

初期実装では全周の建築が別区間からも投影され、浮いた灰色帯になったため、注視地点前後の範囲だけを描画する方式へ修正した。樹冠も近景で巨大化したため、配置と画面上サイズを制限した。

## 3. 方向別馬素材

次のSTAR独自素材を制作・接続した。

- `horse-diag-front-v1`: 4位相×8枠色
- `horse-high-diag-v1`: 4位相×8枠色
- `horse-diag-rear-v1`: 4位相×8枠色
- `horse-side-v3`: 既存8コマから隣接セル断片を除去した修正版
- `horse-rear`: 既存の真後ろ8コマを継続使用

生成基準画は`design/race-assets/`、実働シートとメタデータは`apps/web/public/art/`へ配置した。方向別フレームの組立ツールと、セル境界へ漏れた連結成分を除去するクリーナーも`tools/`へ追加した。

## 4. 監査で発見した問題と修正

`tools/audit-race-broadcast.mjs`を作成し、distance / field / seed / stepを指定できるようにした。各ショット族の代表PNG、コンタクトシート、数値メトリクスを`out/reference-audit/`へ生成する。

監査では表示頭数、各馬の投影矩形、画面外切れ、馬群幅、平均馬幅を測定する。0.25秒刻みの全編に加え、浮動小数点刻みでは漏れる可能性があった厳密な最終時刻も必ず検査する。これによりwinnerショットが監査対象から欠落していた問題も修正した。

実際に検出・修正した主な問題:

- 1600mのcorner-chaseで後続馬が画面外へ出た。
- 2000mのfinishとstraight-wideで端の馬が切れた。
- 2400mのcorner-chase、straight-wide、finishで縦長隊列を収容できなかった。
- 3000mでは固定画角を広げるだけだと短距離の迫力が失われた。
- 最終フレームを監査しておらずwinnerショットの検証が抜けていた。

最終的に距離適応カメラへ変更し、短距離の馬サイズを保ちながら長距離だけ必要量引く構成にした。

## 5. 現在の検証結果

12頭、seed 42:

| 距離 | 連続サンプル | 失敗 |
|---:|---:|---:|
| 1200m | 308 | 0 |
| 1600m | 366 | 0 |
| 2000m | 426 | 0 |
| 2400m | 489 | 0 |
| 3000m | 586 | 0 |

頭数端ケース:

| 条件 | 連続サンプル | 失敗 |
|---|---:|---:|
| 1600m・18頭 | 364 | 0 |
| 3000m・18頭 | 584 | 0 |
| 1200m・6頭 | 306 | 0 |
| 3000m・6頭 | 587 | 0 |

合計4,016フレームで、全頭表示・画面外切れ0を確認した。

追加検証結果:

- Vitest: 3ファイル、12テスト成功
- TypeScript: ルートおよびWebの型検査成功
- Next.js: 本番ビルド成功
- 既知の警告: 上位ディレクトリと本プロジェクトに複数のlockfileがあり、Next.jsがworkspace rootを推測している。今回の変更によるビルド失敗ではない。

## 6. 関連資料

- `RACE_REFERENCE_REPRODUCTION_PLAN.md`: 最終目標までの計画と進捗
- `docs/race-reference-shot-catalog.md`: 参考映像のショット台帳
- `docs/race-broadcast-audit-seed42.md`: 監査方法、代表値、複数距離の結果
- `tools/race-reference-shots.json`: ショット台帳の機械可読データ
- `out/reference-audit/`: 監査生成物。再生成可能なためGit管理対象外

## 7. 未完了項目

今回完了したのは、STAR独自素材によるショット基盤、方向別描画、背景基盤、距離・頭数のFraming gateである。プロジェクト全体の最終受け入れはまだ未完了。

次に必要な作業:

- 逃げ切り、大差、団子、追込など展開別seedの監査
- 右回り・左回りの組合せ監査
- 馬体へ時間蓄積する泥汚れ表現（蹄の土煙・泥・芝片は実装済み）
- 脚周期、接地点、切替時の瞬間移動を測るMotion gate
- ゴール順と画面上の通過順をフレーム単位で照合するTruth gate
- UI、実況、独自SE・環境音
- 参考フレームとの構図差を用いたReference gate
- オーナーによる最終目視受け入れ

## 8. 再開時の推奨手順

1. `RACE_REFERENCE_REPRODUCTION_PLAN.md`と本書を読む。
2. `docs/race-broadcast-audit-seed42.md`のコマンドで固定監査を再実行する。
3. 展開別seedマトリクスを追加し、Framing gateを先に閉じる。
4. Truth / Motion / Referenceの順で定量ゲートを追加する。
5. 技術ゲート通過後に映像を目視判定する。

## 9. 2026-08-17 追記

- 3ペース×5距離×16seedの240レース、318,825位置状態でTruth / Motion失敗0。
- 確定時刻差を使うゴール後ランアウトを追加し、全馬が決勝線上へ一点収束する表示を解消。
- winnerショットは確定1着馬を直接参照し、横位置込みで中央追跡する専用カットへ変更。
- 芝・ダート×4馬場状態の追加240レース、329,674位置状態でTruth / Motion失敗0。
- レース画面に走路・馬場セレクターを追加。共通背景描画も芝の荒れ色、ダートの土色とハロー跡へ切り替わる。
- 左回り・右回りセレクターを追加。右回り5距離と最大18頭を全編監査し、必須馬の画面外切れ0を確認。
- 画面座標Motion gateを追加。左右両回り×5距離で足元の瞬間移動と急拡縮を検査し、通常追走の失敗0を確認。
- 馬番と脚フレームから決定的に生成する土煙・泥・芝片を追加。馬場悪化に応じて強度が増える。
## 2026-08-17: 実況・順位 UI の局面整理

- 発馬後 0.8 秒は HUD を出さず、スタート映像の視認性を優先した。
- 道中だけスタミナ、順位、実況を表示し、全馬決着時にライブ UI を一括で消すよう統一した。
- 決着から 0.35 秒の間を置き、共有描画層の結果パネルへ切り替えるようにした。
- 実況は同一状態を再通知せず、通常変化は 1.5 秒のクールダウン、局面転換は即時通知とした。
- 条件変更および「最初から」で実況履歴を初期化し、前レースの文言残留を防止した。
- HUD タイミングと実況抑制の単体テスト 5 件、TypeScript 型検査、本番 Web ビルドに合格した。

## 2026-08-17: HUD 遮蔽の画面監査

- オフライン中継監査にも本番と同じゲージ、順位、実況、結果パネルを描画するよう統合した。
- HUD 矩形と投影後の馬スプライト矩形の交差を測り、代表カットで馬を隠した場合は監査を失敗させる gate を追加した。
- 初回監査で中央の結果パネルが勝者を隠すことを発見し、勝者カットの右セーフエリアへ移動した。
- 右回り監査で固定幅 520px の実況背景がコーナーの馬番6に触れることを発見し、文章の実測幅に追従する背景へ変更した。
- 左右両回り各371フレーム、0.25秒間隔で framing / motion failure 0、全代表カットで HUD 遮蔽 0 を確認した。
- 関連テスト15件と全体TypeScript型検査に合格した。

## 2026-08-17: 距離・頭数 HUD マトリクス

- 1200 / 1400 / 1600 / 2400 / 3000mを左右両回り、各12頭で監査し、全10条件で framing / motion / HUD遮蔽 failure 0を確認した。
- 6頭・1200mは左右とも合格した。
- 18頭・3000mの初回監査で、発馬1.25秒に馬番18が約306px飛ぶ不連続を検出した。
- 原因は馬の位置ではなく、スタートカメラが最後尾1頭を注視し、最後尾の入替時に注視レーンが切り替わることだった。
- スタートワイドの注視対象を全馬群の重心へ修正し、Webと監査で共有する`focusForRaceShot()`へ一本化した。
- 修正後の18頭・3000mは左右各588フレームでfailure 0。通常カット内最大移動は左17.7px、右17.5pxまで低下した。
- 注視対象の回帰テストを追加し、HUDを含む関連テスト12件と全体型検査に合格した。

## 2026-08-17: ユーザー目視確認と中間保存

- 開発サーバーの`/race`をユーザーが実際に再生して目視確認した。
- 評価は「まだまだ修正はあるが、Claude Codeが作った状態とは確実に変わった」。完全再現の完了判定ではなく、映像品質が明確に前進した中間到達点として記録する。
- 目視確認時、既存開発サーバーが本番ビルドで更新された古い`.next`チャンクを保持し、`Cannot find module './997.js'`が発生した。
- ポート3100の旧Nodeプロセスだけを停止し、再生成可能な`apps/web/.next`を削除して開発サーバーを再起動した。
- 再起動後は`/race`の再コンパイルとHTTP 200応答を確認し、モジュール欠落を解消した。
- この時点を中間マイルストーンとしてコミット・プッシュする。次回はユーザーの目視指摘を基準に演出を継続調整する。

## 2026-08-17: 発馬背景の品質承認

- 手描きCanvasの側面発馬機は、方向は改善したものの「玩具のよう」「昭和時代のゲームのよう」と評価され、品質不足と判断した。
- Codexの`imagegen`スキルを使用し、発馬口を右へ向けたSTAR独自の側面発馬機・芝・ラチ・スタンド背景を制作した。
- 採用画像を`apps/web/public/art/starting-gate-side-v1.png`へ保存し、馬を別レイヤーで右方向へ走らせる構成へ変更した。
- 発馬機前面を背景から再合成し、待機馬が機械の上へ貼り付かず、内部から現れるよう遮蔽した。
- ユーザーから、ゲート、芝、背景、観客席を含め「この画像のクオリティは最高」と明示的な承認を得た。
- この画面を以後のレース映像全体の品質基準とし、馬、騎手、コース、競馬場設備を同じ画像生成ワークフローと画風へ段階的に統一する。
- 詳細な維持基準と制作ルールを`docs/race-visual-quality-standard.md`へ記録した。

## 2026-08-17: 新アート制作開始

- 必要素材を馬体、騎手、ゼッケン、勝負服、馬具、方向別走行、状態差分、コース背景、競馬場設備へ分解した。
- 28項目の制作順を`docs/race-art-production-order.md`へ記録した。
- 最優先の横向き馬＋騎手8コマ走行マスターを`imagegen`スキルで生成した。
- 承認済み発馬背景を画風参照、既存横向きシートを動作順参照として使用した。
- クロマキー除去後、透過原版`horse-side-master-v4.png`と実装用`horse-side-master-v4-sheet.png`を保存した。
- 次はこのマスターのユーザー目視承認後に、馬体・騎手・ゼッケン・勝負服の分離素材を順番に制作する。
- ユーザー確認によりv4は背景と同等の高品質画風に見えないと判定し、不採用とした。
- 原因を8姿勢同時生成による解像度不足と粗い画風指定に特定し、1姿勢ずつ制作するv5方式へ変更した。
- v5 pose01では高解像度の馬・騎手を生成後、前脚の重なりを再生成で修正し、解剖学的に正しい四肢4本を確認した。
- 透過版`horse-jockey-side-v5-pose01.png`と、承認済み背景上の実寸プレビュー`out/horse-jockey-side-v5-preview.png`を作成した。
- ユーザーからv5 pose01について「このクオリティで合格」と明示的な承認を得た。
- 承認個体を参照し、次の収縮空中局面pose02を1枚ずつ生成・透過処理した。
- ユーザーからpose02について問題なしとの承認を得た。
- pose02と承認済みpose01を個体参照にして、前脚が接地へ伸び始め、後脚が腹下を通過するpose03を制作した。
- `horse-jockey-side-v5-pose03-chroma.png`を原版、`horse-jockey-side-v5-pose03.png`をゲーム用透過版として保存した。四肢4本と姿勢の連続性を確認し、透過処理では栗毛を変色させないようデスピルを使用しない設定へ修正した。
- ユーザーからpose03について次工程へ進む承認を得た。
- pose03を直前姿勢、承認済みpose01を個体・描画品質参照にして、前脚が接地し荷重を受け始めるpose04を制作した。
- `horse-jockey-side-v5-pose04-chroma.png`を原版、`horse-jockey-side-v5-pose04.png`をゲーム用透過版として保存した。四肢4本、姿勢の連続性、透過後の色と輪郭を確認済み。
- ユーザーからpose04について次工程へ進む承認を得た。
- pose04を直前姿勢、承認済みpose01を個体・描画品質参照にして、前脚への荷重が最大となり馬体が支持脚の上を通過するpose05を制作した。
- `horse-jockey-side-v5-pose05-chroma.png`を原版、`horse-jockey-side-v5-pose05.png`をゲーム用透過版として保存した。四肢4本、姿勢の連続性、透過後の色と輪郭を確認済み。
- ユーザーからpose05について次工程へ進む承認を得た。
- pose05を直前姿勢、承認済みpose01を個体・描画品質参照にして、前脚が離地し後肢が接地へ向かうpose06を制作した。
- 初回マゼンタ原版は被写体内部にもキー色が混入し、透過後に手・顔・馬体へ微小な欠損が出た。被写体と姿勢を固定したまま背景のみグリーンキーへ再生成し、内部欠損を解消した。
- 修正版`horse-jockey-side-v5-pose06-chroma.png`を原版、`horse-jockey-side-v5-pose06.png`をゲーム用透過版として保存した。四肢4本、姿勢の連続性、透過後の内部色と輪郭を確認済み。
- ユーザーからpose06について次工程へ進む承認を得た。
- pose06を直前姿勢、承認済みpose01を個体・描画品質参照にして、後肢が接地・伸展して推進力を生むpose07を制作した。
- `horse-jockey-side-v5-pose07-chroma.png`を原版、`horse-jockey-side-v5-pose07.png`をゲーム用透過版として保存した。四肢4本、姿勢の連続性、被写体内部に透過欠損がないこと、色と輪郭を確認済み。
- ユーザーからpose07について次工程へ進む承認を得た。
- pose07を直前姿勢、pose01をループ先の姿勢として参照し、その中間となる完全離地局面pose08を制作した。
- `horse-jockey-side-v5-pose08-chroma.png`を原版、`horse-jockey-side-v5-pose08.png`をゲーム用透過版として保存した。四肢4本、全蹄離地、内部欠損がないこと、pose07→pose08→pose01のループ方向を確認済み。
- 横向き馬＋騎手の高品質8コマ走行マスターが個別透過PNGとして完成した。次工程は8枚の基準点・表示寸法を統一し、開発サーバーへアニメーションとして組み込んで実動確認する。
- `apps/web/src/app/race/page.tsx`でv5 pose01〜08を読み込み、不透明領域をブラウザ上で測定して透明余白を除く処理を追加した。各フレームの切り抜き下端を接地位置へ合わせ、表示倍率は8枚共通の最大不透明高で固定した。これにより生成余白差による上下振動と、収縮姿勢だけ馬体が拡大する脈動を同時に避ける。
- `packages/render/src/perspective-draw.ts`へ個別高解像度フレームの任意入力を追加し、真横カメラで従来シートより優先して描画するようにした。既存API利用側は省略可能なため互換性を維持する。
- `packages/render/src/race-intro.ts`の高品質背景を使うゲート解放演出にも同じ個別フレームを渡し、発走から本編へ移る瞬間の画風断絶を解消した。
- `npm run typecheck`、`camera.test.ts`と`race-intro.test.ts`の計49テストが成功し、開発サーバー`http://localhost:3100/race`がHTTP 200を返すことを確認した。

## 2026-08-17: v5実装後の目視差異と是正

- ユーザー提供の13:48時点スクリーンショットを比較し、キャッシュではなく実装上の問題と確認した。発走ショットではv5が小さく密集し、斜め俯瞰ショットでは旧`horse-high-diag-v1.png`へ戻っていた。
- 高品質な視点別素材が揃う前に旧素材をフォールバック表示した判断が、承認済み画風との断絶を生んだ。以後、視点別v5素材が完成するまでは全ショットを真横へ統一し、旧馬素材を本番表示へ出さない。
- `shot-sequence.ts`の全ショット族を真横視点へ統一し、注視対象・局面・カット族は維持したままカメラ距離と高さをv5向けに寄せた。
- ゲート解放時は各馬へ12pxずつ横方向の隊列差を加え、v5の基準高を108pxから132pxへ拡大した。脚アニメーションは16fpsから12fpsへ落とし、高精細フレームが潰れて見える時間的ちらつきを抑えた。
- `npm run typecheck`と、`shot-sequence.test.ts`・`camera.test.ts`・`race-intro.test.ts`の計56テストが成功した。

## 2026-08-17: 高品質2D再構築の段階監査開始

- `docs/race-2d-quality-rebuild-plan.md`を作成し、9ステップ、100点の合格条件、不合格時の戻り先、禁止事項を固定した。
- Step 1として`/race-quality-lab`を実装し、単馬を大きく表示して8コマを手動・低速・通常速度で監査できるようにした。
- 実ブラウザ撮影では430px表示時に原画品質が保持され、レース画面の品質低下が縮小・重なり・背景合成によるものと確定した。
- Step 2の同一縮尺監査で、独立生成8姿勢の色・体格・顔・騎手比率が時間方向に揺れることを検出した。現行v5は本編用アニメーションとして不合格とし、正式採用を停止した。
- pose01を基準にpose03・pose04のv6修正候補を作成した。pose04初回案は姿勢を失ったため破棄し、姿勢を維持した二回目だけを保存した。

## 2026-08-17: 引継ぎ書 §1 — 通常 `/race` が Broadcast V2 で動いていることの実ブラウザ証明

- `@star/web` の dev サーバー（port 3100・`cmd → npm → next dev` の5プロセス）だけを停止し、`apps/web/.next` をスクラッチパッドへ退避（削除ではなく移動）してから再起動した。`image-cockpit` 側の Node プロセスには触れていない。
- 再起動後 `/race` を再コンパイル（616 modules）し、`/race` と `/race?renderer=legacy` が HTTP 200 を返すことを確認した。
- ヘッドレス Edge（DevTools Protocol）で実ブラウザ描画を撮影した。`?auditSec=12` で `/race` は Canvas 左上に `BROADCAST V2 ACTIVE [backstretch-side]`、`/race?renderer=legacy` は `LEGACY RENDERER [legacy/first-corner]` が描かれ、DOM 側バッジ・見出し文言も一致した。コンソール例外・HTTP 4xx/5xx・画像読込失敗はゼロ。
- `/race` を「演出開始」で実時間再生し 1〜101 秒を 22 フレーム撮影した。クロックは 4.8 秒でレース開始し 91.2 秒で停止、その後 5.2 秒の勝馬追従→着順表示まで到達した。V2 のショット遷移は `title → gate-hold → gate-release → start-follow → backstretch-side → third-corner-rear → fourth-corner-high → homestretch-side → finish-line → winner-follow` だった。
- 撮影から見えた未解決点（次の修正候補）:
  1. 画面左上の区間タグ（`raceCourseSectionAt` 由来: 第1コーナー／第2コーナー／向正面…）と V2 のショット（`ovalCourse` の区間ラベル由来）が別定義で、例えば「第1コーナー」表示中に `backstretch-side`、「向正面」表示中に `third-corner-rear`、「第4コーナー」表示中に `homestretch-side` が出ている。1600m では `first-corner-front` / `second-corner-high` が一度も選ばれない（引継ぎ書 §6.5）。
  2. `winner-follow`（94〜101 秒）は決勝審判塔が同じ画面位置に留まり、背景が止まったまま勝馬だけが描かれている（引継ぎ書 §3「馬だけその場で脚を動かす」に該当する恐れ）。
  3. 上記以外の各区間は、ユーザー目視での合否待ち。

## 2026-08-18: 「その場走り」の原因確定とループ多層パララックス試作（向正面）

- ユーザー指摘「背景・芝は素晴らしいのに馬の走りが全く上手くいかない」を受け、`/race?auditSec=12.00〜12.92` を 1/12 秒刻みで実ブラウザ撮影し、背景の横移動量を SAD で計測した。**生垣・ラチ・芝とも 1px/コマ ≒ 12px/秒**（馬群を追うカメラなら馬速 16m/s × 約83px/m ≒ 1,300px/秒が必要）。背景が必要量の約 1/100 しか動かず、馬の絵の良し悪しに関係なく「その場で脚だけ動く」ように見えていた。
- 構造的原因: `drawBroadcastV2Scene` の `backgroundPlate` は 1 枚絵（1672×941）を 1.14 倍に拡大した窓を `progress=(focusS%400)/400` で送るだけで、400m 走って 137px しか動けない。1枚絵である限り馬速で流せない（0.2 秒で絵が尽きる）。
- 対処: 合格済み `race-backstretch-side-v1.png` を絵に手を入れずに 9 帯（樹木／スタンド／生垣／奥ラチ／内ラチ／芝×3／手前ラチ＋生垣）へ分解し、横方向にループするタイルにする `tools/split-parallax-layers.mjs` を追加（ラチは支柱・金具の無い区間で継ぎ目、他はクロスフェード）。出力は `apps/web/public/art/parallax/backstretch-side-v1/*.png` と `manifest.json`。
- 描画: `packages/render/src/parallax-plate.ts` を新設。各層のスクロール量 = 注視点の進行距離 `focusS` × 注視点の px/m × 注視点深さ/(注視点深さ+層の奥行き差)。px/m・深さ・進行方向は馬と同じ透視カメラの `project()` から取り、速度ではなく位置から決める（決定論）。`drawBroadcastV2Scene` に `parallaxPlate` オプションを追加し、`backstretch-side` ショットだけ先行適用（他ショットは従来の 1 枚プレート）。縦の枠取りは旧プレートと同じ式（zoom 1.14・anchor 0.48）を維持し、構図は変えない。
- 脚の周期を時間（12fps 固定）から距離（1完歩 7m）へ変更。速い馬ほど脚が速く回る。
- 再計測（同じ 12.0〜12.9 秒）: 生垣 40px/コマ・手前の芝 108px/コマ（従来 1px/コマ）。継ぎ目は目視で判別不能、帯間の隙間なし、構図は従来と同一。
- `npm run typecheck` 成功、`parallax-plate.test.ts` 3件・V2 関連テスト・`tool-guard.test.ts` 9件成功。`ASSET_VERSION` を 35→36。
- 未対応: 他ショット（発走追従・3角後方・4角俯瞰・直線・ゴール・勝馬追従）のパララックス化、8 コマ間の馬体の揺れ、区間タグとショットの不一致。

## 2026-08-18: 全編監査 → 見た目速度・馬の大きさ・全横ショットのパララックス・コーナーカット

- ユーザー指摘「ゲート〜7秒は背景が動かない／7〜20秒は高速だが馬が小さく小走り／他の全カメラで背景が動かない／馬・騎手だけ絵の品質が劣る」を受け、全編を数値で監査した（`tools/audit-race-motion.mjs` 新設・読み取り専用）。
- 判明: (1) 時間圧縮 D-062（道中 1.8 倍・直線 0.7 倍）で**表示上の馬群速度が序盤 28.7m/s・直線 11〜12m/s**。距離連動にした脚は序盤 4 完歩/秒＝「小走り」、直線はスロー。(2) 横追従カメラ（backM 29・fov 30°）で馬は画面高 16%（116px）。参考映像の横追従は **30〜34%**。(3) パララックスは向正面のみで、発走追従・3角・4角・直線・ゴール・勝馬は静止プレートのまま。(4) HQ 8 コマは外接矩形の下端・中心で置かれ、脚の伸縮で胴体が上下 12px・左右 9px 跳ぶ（空中局面で沈む逆位相）。
- 対処:
  - **見た目速度の分離** `packages/render/src/visual-scroll.ts`: 表示時計での注視点増分 × (1/rate) を積分した補正 Δ(d) を作り、背景の流れと脚の周期は「真の位置＋Δ」で常に実速（16〜17m/s・2.3 完歩/秒）。順位の推移だけが圧縮される。決勝線・審判塔を映すゴール前 80m は重み 1（Δ=0）で馬と物体が一致、その手前 80m でなだらかに接続。位置・時刻・着順・時間圧縮の定義には触れない。
  - **望遠横追従** `SIDE_TELE = {backM 44, upM 6, fov 12°}` を発走追従・向正面・直線に適用（馬 ≈194px＝27%）。勝馬追従は 35%、ゴール前は少し引いて 14.5%（参考のゴール ≈10%）かつ先頭を画面 40% に置き、決勝線・塔を通過の約 1.5 秒前から見せる。
  - **注視ルール** `broadcastV2LeadFrameFocusMeters`: 馬群が画面幅（そのカメラの px/m から算出）に収まらなければ先頭を進行方向側 78% に置く（参考映像の横追従＝先頭集団を大きく、後方は画面外）。
  - **全横ショットをパララックス化**（`scene.shot.view === 'side'`）。直線も向正面と同じ層を使う（`race-corner-exit-side-v1` は斜めのラチで横タイル化不可）。縦の枠取り anchor 1.0。
  - **決勝線・審判塔を世界固定の物体**として `race-finish-side-v2.png` から差分マスクで切り出し（`split-parallax-layers.mjs` の objects）、内ラチ／芝各帯の奥行きで流す（`ParallaxObject`）。
  - **コーナー**: 3角後方・4角俯瞰の 1 枚絵カットは**コーナー冒頭 70m（約 2.5〜4.5 秒）だけ**にし、カット中はパン＋ズーム（`cutProgress`）。残りは横追従へ戻す（`CORNER_CUT_M`）。透視ワールド描画（単色）は画風が断絶するため不採用。
  - **区間タグ**をショット選択と同じ区間定義から出す `broadcastV2SectionLabel`（旧 `raceCourseSectionAt` との不一致を解消）。
  - **胴体基準の配置** `bodyAnchorSourcePx / bodyLiftSourcePx`: 各コマの胴体重心（矩形上 55% の α 重心）を接地点の真上に置き、上下動は `HORSE_BODY_BOB_SOURCE_PX`（±34px/856）だけ。外接矩形依存を廃止。
- 実測（監査ツール）: 横ショット全て馬 26〜27%、見た目速度 15.8〜17.6m/s・2.25〜2.5 完歩/秒、背景 950〜1,300px/秒。ゴール前は 0.7 倍のまま（12m/s・スロー）。実ブラウザ通し撮影（1〜101 秒）で全区間の遷移・ラベル・塔通過・勝馬追従を確認。
- 検証: `npm run typecheck` 成功、`vitest` 全 80 ファイル 871 件成功（新規 `visual-scroll.test.ts` 6 件・`parallax-plate.test.ts` 3 件）。
- 未対応・要判断: (a) コーナー冒頭以外は横追従で、旋回そのものは映らない（本格対応は芝テクスチャ付き透視ワールド）。(b) 直線後半は馬群 40m 超で画面内 2〜3 頭。(c) 8 コマ間の馬体・騎手の描き分けの揺れ（素材側）。(d) ゴール前の 0.7 倍スローを維持するか。

## 2026-08-18: ユーザー目視指摘 6 件への対処（第 2 便）

指摘: ①発走が扇型・発走→追走でカット ②真横は合格に近いが走りにおもちゃ感（影・砂・脚） ③3角後方・④4角俯瞰で品質が急落 ⑤ゴール前で馬が縮小・展開に応じたカメラが無い ⑥勝馬追従で脚が止まる。

- ⑤ `broadcastV2FinishStyleOf`（先頭が残り 80m に達した瞬間、1〜2 着差 ≤1 馬身 or 3 着以内 ≤2 馬身なら接戦）＋ `broadcastV2FinishCamera`（残り 160→80m で連続ズーム: 接戦は fov 12→22° に引き先頭を 45%、単独は望遠のまま先頭 60%）。build 時に決定論で判定（seed 42/1/12=単独、7/99=接戦）。直線→ゴール前はカット無し。
- ② 楕円影を廃止し**各コマのシルエットを地面へ潰した接地影**（起動時に黒＋ぼかしで焼く `bakeShadowSilhouette`、描画は `transform` で縦 0.26・進行方向へ 0.55 倒す）。芝片は後肢の蹴り出し局面（コマ 06→02）だけ放物線で飛ぶ小片＋発生直後の薄い土煙へ（旧実装は HQ で位相が常に 0）。
- ③④ 方向別 8 コマ（`diag-rear-v2` `high-diag-v2` `diag-front-v2`）は 271×724 の低解像度で真横 v6 と釣り合わないことを確認。`CORNER_CUT_M_WEB = 0` でコーナー専用カットを無効化し、コーナーも望遠横追従で通す（`broadcastV2ShotAt(course, s, allFinished, cornerCutM)`）。素材が承認水準に達したら再有効化。
- ① **発走の統合**: ゲート待機（2.6〜4.8 秒）〜開扉〜追走を V2 の同じ透視カメラで描く。Codex に承認済み `starting-gate-side-v1.png` から発馬機とポールだけを消した版（`design/art/assets/starting-gate-side-v1-nogate.png`）を生成させ、差分マスクで発馬機（`start-gate`・馬の手前 zOrder front）とポール（`start-pole`）を切り出し。発馬機の絵の遠近（右端が手前・左へ奥）に合わせて発走カメラ `START_CAMERA = {backM 70, upM 3, fov 7°, alongM +30}` を探索し、待機中は注視点をゲート付近 4m に固定 → 発走後 1.5 秒で馬群追従、カメラは 1.0〜3.5 秒で `SIDE_TELE` へ連続移行（`broadcastV2StartCamera` / `broadcastV2StartFocus`）。物体は (s,w) の実投影で配置（`worldW` / `anchorYRatio` / `scale`）。待機中の馬は pose04 で静止。発走の 60m は anchor weight 1（発馬機と馬の位置が一致）。
- ⑥ 暫定: 勝馬追従は 1 枚絵 `winner-v1` をやめ走行 8 コマで脚を動かす。本対応として Codex に「side-v6 の各姿勢＋ガッツポーズ騎手」の勝馬 8 コマを生成中（pose01 完了・透過済み `out/gen/horse-jockey-winner-v2-pose01.png`）。
- 検証: typecheck・vitest 871 件成功。実ブラウザ撮影で 3.5→8.0 秒の連続を確認。
- ⑥ 本対応: 勝馬 8 コマ `horse-jockey-winner-v2-pose01..08.png`（＋ `-chroma` 原版）を Codex で生成・透過し取り込み。腕を挙げるぶん外接矩形が縦長なので、基準高さは矩形幅の中央値の比で side-v6 に合わせ、勝負服・ゼッケンの領域は勝馬用レイアウト `SILKS_LAYOUT_WINNER` で着色。勝馬追従で脚が動き、騎手がガッツポーズのまま走り抜ける。
- ① 発走直後 0.7 秒の減衰カメラ揺れ（世界のみ・時刻の関数で決定論）を追加。
- 実ブラウザ撮影: 発走 2.8〜9.0 秒（`start-continuous-2.8s-9s.mp4`）、ゴール〜勝馬 91〜97 秒（`finish-winner-91s-97s.mp4`）。typecheck・vitest 871 件成功。
- 残: ③④ 高解像度の後方・俯瞰 8 コマ（Codex）→ コーナー専用カットの再有効化、直線後半の頭数、（要判断）ゴール前 0.7 倍スロー。

## 2026-08-18: 第 3 便（角度の復活・スロー廃止・接地・16 コマ・勝馬の安定）

- **A. テクスチャ付き透視ワールド** `packages/render/src/world-textured.ts` を新設。承認済みプレートの芝から 2 方向ループタイル（`world-turf.png`・90px/m）と遠景帯（`world-panorama.png`・樹木/スタンド/生垣）を `split-parallax-layers.mjs` で生成し、地面は走査線ごとに透視で貼る（mode-7）、ラチ・支柱はコース曲率から透視投影、遠景はカメラの向きでパン。横視点以外のショット（3角後方・4角俯瞰）はこれで描き、**コーナー専用ショットをコーナー全区間で復活**（`CORNER_CUT_M_WEB = 400`）。旧 1 枚絵の静止背景は不使用。
- **B.** `DEFAULT_PHASE_RATES.straight` 0.7 → 1.0（実時間）。ゴール前のスロー廃止（D-062 の「実時間かそれ以上」の範囲内）。表示は約 88 秒に短縮。
- **C.** 配置基準を胴体重心 → **鞍布中心**（`saddleReference`）に変更、縮尺は鞍布幅の中央値比（±8%）で補正。勝馬 8 コマの伸縮を抑制。
- **D.** 接地高さを「そのコマの最下点の蹄が地面」＋空中局面のみ `HORSE_FLIGHT_LIFT_SOURCE_PX` に変更（旧「全コマ平均＋bob」は支持局面で蹄が浮いていた）。影の倒しを 0.55 → 0.28、圧縮 0.22、蹄の真下に接地影（支持局面で濃く）。
- **E.** Codex で side-v6 の連続 2 コマを参照した中間姿勢 `mid01..08` を生成・透過し、**16 コマ周期**に（`pose01, mid01, pose02, …`）。コマ選択を位相ベース（`phaseOf`）にしてコマ数に依存しない形へ。
- 検証: typecheck・vitest 871 件成功。実ブラウザ撮影（発走 2.8〜9 秒、コーナー 24〜31.5 秒、勝馬）。
- 注意: 高解像度素材が増え（1536×1024 PNG ×40 枚超）ページ初期ロードが重い（ヘッドレス撮影で 1 コマ 6 秒）。圧縮／シート化は別途。
- 残: 方向別 8 コマ（後方・俯瞰）の高解像度化（Codex 16 枚）、コーナーカメラの微調整、直線後半の頭数。

## 2026-08-18: 第 4 便（走り・角度・背景・騎手・発走・繋がり）

- ⑤ 発走イージング `broadcastV2StartEase`（描画のみ・開扉 1.6 秒で係数 0.22→1・全馬同一）: ロケットスタート感を解消。
- ① 見た目の周期 7m → 9m（≈1.7 完歩/秒・合格に近いゴール後の走りに合わせる）、空中の浮きを半分、中間コマは既定オフ（8 コマ）。位相ベースのコマ選択（`phaseOf`）。
- ⑥ 全ショットで馬ごとに「進行方向とカメラの相対角」で素材集合（斜め後ろ／真横／斜め前）と左右反転を選ぶ `frameSetOf`（俯瞰・後方で向きが破綻しない）。コース図ミニマップ `minimap.ts`（走路帯・発走/決勝線・馬群・注視点）を左上に常時表示。
- ② 透視ワールドに**世界固定の立体帯**（生垣・樹林帯（空を透過した `world-trees.png`）・直線のスタンド）を追加。スライスは距離に応じて細かく、カメラ脇は省略。コーナーカメラを地平線が画面 20〜25% に来る高さへ（後方 44m/5.5m/23°、俯瞰 40m/6.5m/34°）。
- ④ **馬と騎手を分離**: Codex で「騎手なしの馬 8 コマ」（side-v6 各姿勢を参照）と「騎手単体 4 姿勢」（前傾 a/b・ガッツポーズ a/b）を生成。ブーツ（黒・剛体）の下端中心とヘルメット上端で参照コマに位置合わせし、鞍布中心の差で各コマへ移して合成（`composeHorseAndJockey`）。道中は前傾 a/b を半周期で切替、勝馬追従はガッツポーズ a/b。馬体は承認済み 8 コマそのもの＝伸縮・騎手のガクつきが消える。
- 生成中: 斜め後ろ「馬のみ」8 コマ（v3・縦長 1024×1536）。完成後、後方／斜め前も同手順。
- 検証: typecheck・vitest 871 件成功。実ブラウザ撮影（14 秒・27 秒・44 秒・84 秒、真横 13〜16 秒動画）。
- ⑥ ショット切替の**ディゾルブ**（0.45 秒）: 切替時刻は build 時に決定論で列挙（`shotChanges`）、直前ショットを同時刻に `forceShotId` でオフスクリーン描画して重ねる。
- ②③ 斜め後ろ「馬のみ」8 コマ（`horse-only-diag-rear-v3`・1024×1536）と後方視点の前傾騎手 `jockey-crouch-rear` を Codex で生成・透過。pose04 の座標で描かれた騎手を各コマの鞍布中心差で合成し、低解像度 `diag-rear-v2` を置き換え（`SILKS_LAYOUT_REAR`・ゼッケンなし）。
- 検証: typecheck・vitest 871 件成功。実ブラウザ撮影（27/33/44 秒）。
- 残: 4角俯瞰・斜め前は方向別選択で真横／斜め後ろを流用中（正面系素材は 1600m では出番が少ない）。コーナー素材の角度ばらつき（pose01/02 がやや横寄り）は必要なら再生成。ページ初期ロードの重さ（PNG 60 枚超）。

## 2026-08-18: 第 5 便（オーナー指摘 7 件・合格状態への復帰と作り直し）

指摘: ①馬の形/大きさがアンバランス（ボート）②ロケット発走③序盤が前後 2 塊④走りが上下に伸縮⑤3角の馬が最悪⑥4角は奥からこちらへ向かうカメラのはず⑦合格していた勝馬の走りが崩れた。

- **原因の自己診断**: 分離合成（馬のみ＋騎手）方式が主因。承認済みの一体素材を置き換えたことで①④⑦が発生。方向別の低解像度素材の混在で⑤⑥。
- **復帰**: 一体素材 side-v6 8 コマ・winner-v2 8 コマへ戻す（`USE_SEPARATED_COMPOSITE=false`）、周期 7m、中間コマ廃止、方向別選択は真横のみ（`directionalSets` が揃うまで）。
- ② 発走イージングを 3.0 秒（係数 0.12→1）へ。
- ③ 隊列スロットを脚質順のまま**順位に応じて等間隔**（`evenSlots`・半分だけ均す）、序盤の広がりを 12m→24m（発走〜600m）に（`packSpreadM(metersLeft, pace, metersRun)`）。境界位置・着順は不変（formation テスト 25 件成功）。
- ⑥ 4 角の**固定カメラ**ショット `fourth-corner-front`（直線入口の外側 w=27・高さ 7m、注視点を追い、距離に応じて自動ズーム `broadcastV2FixedFov`）。正面寄り一体素材が揃うまでは無効（`FOURTH_CORNER_FRONT_WEB`）。
- ⑤⑥ 方向別の**一体素材**（勝馬 8 コマと同じ方式・side-v6 の同番号を姿勢参照・前コマを角度参照）を Codex で生成: `horse-jockey-diag-front-v3`（進行中）・`horse-jockey-diag-rear-v4`（続いて）。揃った方向だけ `directionalReady` で有効化。
- 透視ワールドの樹林帯は 100m 以遠のみ（望遠の斜め視で板状に割れるため）。
- 参考映像 3 本（2D・Switch 版クリップ 2 本）を確認: 発走〜200m は横一線→緩やかな縦長の 1 塊、4 角〜直線は正面寄りで手前へ向かってくる構図。
- 方向別一体素材が完成: `horse-jockey-diag-front-v3-pose01..08`・`horse-jockey-diag-rear-v4-pose01..08`（1024×1536・角度一定・騎手込み・透過）。`directionalReady` で有効化し、4 角の固定正面カメラ `fourth-corner-front` を ON（`FOURTH_CORNER_FRONT_WEB=true`）。3 角は斜め後ろ素材、4 角は「奥からこちらへ向かってくる」正面素材で描画。望遠時は樹林帯を描かない。
- 検証: typecheck・vitest 871 件成功。実ブラウザ撮影（27/33/44/52/14/84 秒）。

## 2026-08-19: 中継台本 v3（アーケード参考映像に合わせた全面組み替え）

- 復帰点タグ `race-v2-before-broadcast-script-20260818`（1d7dcd0）。
- 参考映像（137 秒）を 1 秒刻みで分析し `docs/race-broadcast-script-reference-20260818.md` に記録。
- `SCRIPT_V3`（距離比）: 発走(現行) → 低いサイド(fov 9°・馬 ≈36%) → 寄りサイド(7.5°・≈44%) → 空撮(高さ 42m) → 3角斜め後方 → **閃光** → 勝負所サイド → 4角俯瞰ワイド → 直線入口 正面固定 → 直線サイド → **先頭争い 斜め前寄り**（`front-close`）→ ゴール板横 → 勝馬。旧台本は `script:'v2'` で残置（テスト用）。
- 実ブラウザで 10 ショットを撮影し全区間が成立することを確認。typecheck・vitest 全件成功。
- 残: 発馬機の正面プレート（発走の正面パン）、正面/後方の一体素材、勝馬の後方寄り、真横の高解像度作り直し、レース前後の画面（出走表・馬紹介・着順ボード）。
- 発走を参考映像どおりに: **正面の発馬機**（12 枠・扉閉／扉開の 2 枚を Codex で生成・透過）を世界固定ビルボード（`WorldBillboard`・幅 14.8m を実寸で貼る）として置き、待機中は扉閉を馬の手前（馬は扉に隠れる）、開扉後は扉開を馬の後ろに。新ショット `start-front`（斜め前 24m・高さ 2.4m・fov 34°）で馬群がこちらへ飛び出し、60m で低いサイドへ。側面の発馬機切り出しは不使用。
