# B-6 配線の完了条件（正典 D-050）

★**「繋ぐだけ」ではありません。** `race-field.ts:99-100` の
`condition ?? 3` / `fatigue ?? 0` が `intervention.ts:177-178` で
**スタミナゲージの初期値**を作っています。

```
現在: 全レース・全馬のゲージ初期値が完全に同一
     ＝ P1 のゲートは「調子の分散がゼロの世界」で測られている
```

**P1 で見つけた V-8 の盲点と同じ構造**です（あのときは「全馬に一律 −0.15」が
比にも範囲にも現れませんでした。いまは「全馬が condition 3 / fatigue 0」で、同じく一律）。

---

## 1. ★オッズ MC は自動的に実データを見ます（確認済み）

ご指示の「オッズ算出の MC が実 condition/fatigue を見ているか」を先に確認しました。

```ts
// apps/worker/src/build-race.ts
const numbered = race.entrants.map(...)      // ★同一の配列
for (...) resolveRace({ entrants: numbered, seed: oddsRng, ... })   // MC
// 本番確定も同じ numbered を使う（seed 系列だけが別）
```

★**MC と本番確定は同じ `entrants` を使うので、乖離は構造上起きません。**
  `condition`/`fatigue` を実データにすれば、MC も同じ値を見ます。
  → **K の較正（D-021）がずれる経路はありません。**

---

## 2. ★配線前のベースライン（`092bd5b` 時点）

`npm run verify:race` の実測。**全馬 condition=3 / fatigue=0** の世界の値です。

| ゲート | 値 | 帯 | 余裕 |
|---|---|---|---|
| **V-4** 1番人気の勝率 | **31.29%** | 30〜34% | 6.8 SE / 14.3 SE |
| **V-5** 1番人気の複勝率 | **62.4%** | 60〜65% | 12.1 SE / 13.2 SE |
| **V-6** 下位3ランクの平均勝率 | **1.21%** | 0.5〜2% | 27.8 SE / 31.2 SE |
| **V-8** AI代行 ÷ 手動最適 | **94.57%** | 93〜97% | — |
| **V-9a** ハードキャップの不変条件 | 0.90〜1.10 内 | — | PASS |
| **V-13** 仕掛けの巧拙の差 | **0.1999** | 有意差 | PASS |
| V-9b（監視値） | 1.0401 | 目標値なし（D-014） | — |

生出力: `docs/MEASURE_P1GATES_BEFORE_WIRING.txt`

---

## 3. 配線の完了条件

| # | 内容 |
|---|---|
| 1 | `race-field.ts` の `condition ?? 3` / `fatigue ?? 0` を**実データ**にする |
| 2 | **配線後に `npm run verify:race` を再実行**し、上の表との**差を報告** |
| 3 | ★**V-4/V-5/V-6 が動いたら**、動いた理由を「調子という新しい分散源が加わった」で説明できるか確認 |
| 4 | ★動いたぶんが帯を外れるなら、**較正ではなく報告**（K の較正は D-021 の管轄） |

★**差がゼロだったら、それも報告します。**「配線したのに何も変わらない」は
  **繋がっていない可能性**を意味します（P1 の V-8 の盲点そのもの）。
