# P2 合格基準 — 最後に検証したコミット（正典 R-23）

**★合格は「そのコード状態に対する主張」です。コードが変われば黙って失効します。**

`verify-a2.mjs` が**実行不能かつ本番のレースを全件削除する実装**のまま、
表には「A-2 PASS」と書かれ続けていました。3者とも気づきませんでした。
**判断力ではなく経路の問題なので、書式で解決します。**

## 読み方

- **SHA が HEAD と一致** … その状態で実際に流して通っている
- **SHA が HEAD と違う** … **なぜ古い SHA でも有効かを明文で書く**。書けないなら流し直す

---

## 現在の状態

**HEAD**: `eef7d8a`　／　**本番稼働**: `82ec229（λ*=30・内部口座・資源記録）`

| # | 内容 | 最終検証 | 手段 | 備考 |
|---|---|---|---|---|
| **A-1** | 10分サイクルが無人で24時間 | `c5df6f6` | 本番ワーカー実測 | ★レビュー側が受理済み。**§メモリの訂正**あり |
| **A-2** | 二重生成・二重払戻が起きない | `eef7d8a` | `verify-a2.mjs`（実 DB） | ★ツールを書き直して再取得 |
| **A-3** | V-10 払戻率が券種別に ±1% | **測定中** | `verify-pmin.ts` 400レース | λ*=30・M=3,896,104 |
| **A-4** | `seed_reveal` が確定前に取れない | `eef7d8a` | `verify-a4.mjs`（anon 実測） | |
| **A-5** | EP減算と発券が原子的 | `eef7d8a` | `verify-a5.mjs`（壊して確認） | |
| **A-6** | 自馬ベット制限 | `eef7d8a` | `verify-a6.mjs`（4通り） | |
| **A-7** | 環境ガード | `eef7d8a` | `verify-a7.mjs` | ★ツールを書き直して再取得 |
| **A-8** | P0/P1 の全ゲートが本番構成でも通る | **未取得** | `npm run verify` | ★`1bfebef` でエンジンが変わって以降の記録なし |
| **A-9** | テスト全PASS・typecheck・`any` ゼロ | `eef7d8a` | `npx vitest run` | 471 tests |

### 参考（合格基準ではないが本番経路の確認）

| 内容 | 最終検証 | 手段 |
|---|---|---|
| §10.2 開催中止・§9.1 返還 | `eef7d8a` | `verify-cancel.mjs` |
| §11.2 内部口座の別掲 | `eef7d8a` | `verify-flow.mjs`（混ざらない/消えない/入れ違わない） |
| D-037 の期限境界 | `541a016` | `verify-overdue.mjs`（59分/61分） |
| 経済の一周（購入→確定→払戻） | `82ec229` | `verify-economy.mjs` |

---

## ★A-1 のメモリ計測についての訂正（2026-08-10）

1回目の A-1 報告で「メモリ **95 → 100 → 62 → 69 → 67MB** に増加傾向なし」と書きました。
**この数字はワーカーのものではありませんでした。**

```
MainPID 1847973  tsx のラッパー      RSS  67 MB   ← ps で見ていたのはこれ
  └ 1847989      ★実際のワーカー     RSS 158 MB
      └ 1848001  esbuild             RSS  14 MB
```

`systemctl show -p MainPID` が **tsx のラッパー**を指しており、
**実際のワーカーは子プロセス**でした。ラッパーはほとんど何もしないので、
**あの推移はワーカーの状態を一切表していません。**

★**したがって「158MB に基準線が上がった」は M の増加ではなく、
  ようやく正しいプロセスを測ったからです。**同じ M（3,896,104）です。

**リークが無いという結論自体は変わりません**が、根拠は**プロセス内部から測った
5時間の推移**（下記）に置き換わります。外から `ps` で見た値ではありません。

```
02:02 → 06:43   mem 158〜159MB / heap 27〜39MB / fd 35 / sock 9   ★不動
```
