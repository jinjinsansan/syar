# 変異試験の未防御21件 — §16.3 の3分類（Q-P3-40）

**作成**: 2026-08-12 / 実測: `docs/MEASURE_MUTATION_P3CLOSE.txt`（24 OK / 23 NG）

> **裁定**: 21件すべてに振る舞いテストは要らない。§16.3 の3分類では、
> **較正定数だけが振る舞いテストを要し、測定条件と正典の写しは値照合が正しい防御**。
> → 21件を分類して**較正定数だけ抜き出し、その数を報告**。

★分類の根拠は**すべてソース中のコメント**です（こちらの判断で振り分けていません）。

---

## 1. 正典の写し（5件）— ★値照合が正しい防御

| 定数 | 根拠（ソースのコメント） |
|---|---|
| `PRIZE_TABLE` | 「**正典 §11.1 の写し**。1〜5着（PP）」 |
| `CANCEL_AFTER_START_MS` | 「⚠️ **較正定数ではありません（正典 D-037 の写し）**。動かすと客の金が戻る条件が変わります」 |
| `ODDS_CAP` | 「最高オッズ（**正典 §9.4**）」 |
| `FIELD_SIZE` | 「**正典 §10.4**: 1レース 8〜18頭」 |
| `CALIBRATED_RACE_RANDOM_K` | 「照会（Q3）の結果 **D-016 で正典化された**」（§13.1 の値） |

★これらは**値が正典に書いてある**ので、`toBe(...)` が「正典と食い違ったら落ちる」正しい防御です。
振る舞いテストを足しても、守っているのは同じことです。

★ただし `CALIBRATED_RACE_RANDOM_K` には**注記が要ります**（§4）。

---

## 2. 測定条件（1件）— ★値照合が正しい防御

| 定数 | 根拠 |
|---|---|
| `DEFAULT_POPULARITY_TRIALS` | 「人気推定の試行数。**測定の自由変数・R-12**」 |

★測定条件は「判定を通すために動かせる値ではない」ものです。
`measurement.ts` と同じ扱いで、値照合が正しい防御です。

---

## 3. ★較正定数（15件）— 振る舞いテストが要る

| 定数 | 根拠 | 効く先 |
|---|---|---|
| `LAMBDA_STAR` | 「⚠️ **これは較正定数です**」 | V-10 / D-035 |
| `NAME_TAIL_RATE` | 「**正典に規定が無いので較正定数として**登録簿に載せる」 | N-1 |
| `TAIL_MIX_P_DEFAULT` | 「案D: 大偏差を引く確率（**較正対象**）」 | V-6 |
| `TAIL_MIX_M_DEFAULT` | 「案D: 大偏差のときの幅の倍率（**較正対象**）」 | V-6 |
| `DEFAULT_CLASS_BAND` | 「クラス幅の既定値。**較正で決めた値**」 | V-4 |
| `CLASS_PRIZE_TOP_MULT` | 「**較正対象のプレースホルダ**で、§11 が確定したら差し替える（R-7）」 | V-1 |
| `OFF_SURFACE_ENTRY_RATE` | 0/1 の両端で壊れ方が書かれている（＝較正で決めた中間値） | V-2d / V-2f |
| `TRACK_CONDITION_CDF` | 「**どちらに分類するかは実測してから決める**」（未確定） | V-2d / V-2f |
| `DISTANCE_SUIT_MIN` | 「距離でも絞るのが実態に近い」（実態に寄せた値） | V-6 |
| `OFF_DISTANCE_ENTRY_RATE` | 同上 | V-6 |
| `FIELD_STRENGTH_FLOOR` | 「最強馬のこの割合を下回る馬は出走させない」（較正で決めた割合） | V-4 / V-6 |
| `FLOOR_FIELD_SIZE_SLOPE` | 「8頭→18頭で床がどれだけ上がるか（**P-4 の較正対象**）」 | V-6 |
| `OVERSAMPLE_RATIO` | 「多めに引いておけば頭数を維持できる」（較正で決めた倍率） | §10.4 の頭数分布 |
| `FLOOR_REDRAW_PASSES` | 「差し替えの最大回数」（較正で決めた上限） | V-4 / V-6 |
| `PLACEHOLDER_UNLOCK` | 「指示書 §3 が例示した 0.55〜0.85 を採用」 | ★**本番経路からは廃止済み**（§4） |

### ★数

```
未防御 21件 = 正典の写し 5 + 測定条件 1 + 較正定数 15
  → **振る舞いテストが要るのは 15件**
  → うち `PLACEHOLDER_UNLOCK` は本番経路から廃止済み（ハーネスの比較用にのみ残存）
     実質 **14件**
```

★`TRACK_CONDITION_CDF` は**ソースが分類を保留**しています（「実測してから決める」）。
較正定数の側に数えていますが、**正典 §10.4 の分布の写しなら写し側**です。裁定を仰ぎます。

---

## 4. ★分類の作業中に見つけたこと（報告）

### `CALIBRATED_RACE_RANDOM_K` の較正条件が、いま成り立ちません

ソースにこう書いてあります:

> ⚠️ **較正条件（正典 §13.1 に明記）**: **素質開放率プレースホルダ(0.55〜0.85)**・
> クラス分け番組・18頭立て。

★**その素質開放率プレースホルダを、本便で本番経路から廃止しました**（Q-P3-35 の裁定）。
つまり **K の記録された較正条件は、もう現在の実装を指していません。**

`TAIL_MIX_*` にも同じ注記があります:

> P3 で本物の育成ループが入ったら**必ず再較正すること**（R-7）。

### ★ただし、再較正が要るとは限りません

**本番の実分布で V-4/V-5/V-6 を測り、すべて帯の中でした**:

```
V-4 32.32%（帯 30〜34%） / V-5 63.26%（帯 60〜65%） / V-6 1.00%（帯 0.5〜2%）
```

→ **ゲートは通っているので、値を動かす理由はありません。**
★動かすべきは値ではなく、**記録された較正条件のほう**です。
「0.55〜0.85 のプレースホルダの上で較正した」ではなく
「**開放率 平均71.3% / SD12.6pt の実分布の上で確認した**」に更新が要ります。

★正典 §13.1 の記述なので、**こちらでは書き換えていません**（照会 Q-P3-41）。

---

## 5. まとめ

| 分類 | 件数 | 防御の形 | 状態 |
|---|---|---|---|
| 正典の写し | 5 | 値照合で正しい | ✅ 現状で足りている |
| 測定条件 | 1 | 値照合で正しい | ✅ 現状で足りている |
| **較正定数** | **15**（実質14） | **振る舞いテストが要る** | 🔴 未着手（P1 期からの積み残し） |

★**B-9 の判定は、この 15（実質14）件の扱いが決まってからになります。**
