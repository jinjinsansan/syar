# 直線のせめぎ合いの実装と確認

2026-09-10。変更の比較元は作業ツリーの HEAD `b9922b6`。オーナー要望は、現在のデフォルメ馬で参考映像のような数頭のせめぎ合いを見せること。

## 今回の変更

- `straight-contest` の固定画角を9度から13度、カメラ高を3.5mから6mへ変更。先頭と追走馬の馬体を同時に収める。
- 直線の中間カットを `straight-field` に置換。真横・画角16度・高さ8mの引き。前後の寄りと異なるカットを保つ。
- この2種類のカットだけコース図を上部へ移動。馬の絵・大きさ調整の倍率・完歩・浮き・位置モデル・着順・タイム・サーバー・台帳は変更していない。
- `SCRIPT_V6` の9区間の終点を維持。合格済みのゴール・勝馬・リプレイのショット定義は変更していない。
- 旧テストの「35%以上」「正面必須」は今回の構図と矛盾するため、4カット・区間の維持、馬体高20〜30%、先頭と追走集団の可視性を守るテストへ更新。

## 実画面の比較

比較ページ: `http://localhost:3210/contest-review/index.html`

同じ seed 42、表示48〜67秒、倍率1、15fps、各285コマ。seed 99でも同じ区間を撮影。

カットの切替は両方とも、標本の48.067 / 54.533 / 59.267 / 65.867秒で一致（15fpsの観測値）。中間カットのIDだけ置換した。

直線の寄り196コマにおける実描画の外接矩形:

| 指標 | 修正前 | 修正後 |
|---|---:|---:|
| 馬体全体が左右の画面内に収まる頭数の最少値 | 2 | 4 |
| いずれかの馬体が左右で切れるコマ数 | 160 | 22 |
| 馬体高／720pxの中央値 | 34.6% | 23.7% |

共有する馬の走行位相2048件は一致。画面外の尾や後続馬まで常に全頭収まることは保証していない。HUDとの外接矩形の重なりは残るため、「遮蔽ゼロ」とは判定しない。

対象外の表示10 / 16 / 22 / 28 / 34 / 38 / 46 / 68 / 74秒は、変更前後のPNGのSHA-256が一致。

通常再生も別途録画。表示48秒から19秒再生し66.95秒まで進行、ブラウザ例外0。録画18.874秒・478フレーム。初回の通常再生録画は時刻進行の検査に失敗しており採用せず、再取得した記録を使った。この実行環境の確認であり、対象端末全般の負荷試験ではない。

証拠: `out/contest-direct-review/comparison.json`、各 `frames.json`、`revised-42/live.json`。

## 撮影ツールの修正

`tools/review-deformed-race.mjs` は最初の range 入力を選んでおり、現在の画面では撮影用シークではなく馬の大きさを変更していた。`aria-label="撮影用シーク"` で選び、時刻と倍率1の読み返しを追加。今回の最初の `direct-before` 画像は無効で、比較には使っていない。従来の全録画が無効という判断ではない。

## 検証

- 型チェック成功。
- 描画関連68ファイル・627テスト成功。
- 比較ページの3本の19秒動画をブラウザで読み込み確認。通常再生録画は追加の折り畳み欄に配置。
- 2026-09-10、比較動画に対するオーナー判定は「とりあえずはOK」。直線は今後の作業の基準として保持する。発走・最初の位置取りの斜め前は今回変更しておらず、レース全体の演出完了とはしていない。
- コミット・push・配備は行っていない。
