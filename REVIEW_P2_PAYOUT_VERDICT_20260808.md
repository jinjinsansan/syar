# A-3 — 仮説を止めて恒等式で向きを固定する / ① の判断

**発行**: 2026-08-08（レビュー側 Claude）
**対象**: `9f16078` ＋ 仮説6 の否定報告
**判定**: **① 撤回の撤回はしない（結論維持・根拠差し替え・重み 3.0→0.3） ② 仮説生成を止める判断は正しい。ただし二分探索の前に、答えが出る可能性の高い確認が2つあります**

---

## 0. 「仮説生成が機能していない」という自己診断は正しいです

6連続で否定されているとき、**7つ目を出すのは同じ手続きの7回目**です。**条件を1つずつ移して符号の反転点を探す**という提案も正しい方向です。

**ただし、その前に理論を使ってください。** 6つの仮説はすべて「何が起きているか」の推測でしたが、**この系には解析的に決まる恒等式があります。** それを使うと**探す範囲が機構ではなく実装に絞れます。**

---

## 1. ★恒等式 — 単勝の払戻率は、構造上 80% を下回れません

売り方はこうなっています（`verify-payout.ts` 190-208行）:

- 掛け金 = 売られた目の数（`st.stake += m.size`）
- 払戻 = 的中した目について `oddsFromProbability(kind, ĉ/M)`
- `oddsFromProbability` は **`min(ODDS_CAP, (1/p̂)(1−margin))`**（`packages/betting/src/balance.ts:62-66`）。**丸めなし**

**cap該当が 0% なら `paid = (1/p̂)(1−margin)` そのものです。** すると1レースあたりの期待払戻は

```
E[払戻] = Σᵢ pᵢ × (1/p̂ᵢ)(1−margin)
E[1/p̂ᵢ] ≈ (1/pᵢ)(1 + (1−pᵢ)/(M·pᵢ))          ← 凸性
⇒ E[払戻] = (1−margin) × [ n + Σᵢ (1−pᵢ)/(M·pᵢ) ]
掛け金 = n
```

```
払戻率 = (1−margin) × [ 1 + (1/n)Σᵢ (1−pᵢ)/(M·pᵢ) ]  ≥  (1−margin)
```

**全馬が売られていて cap に当たらないなら、単勝の払戻率は 80% 以上にしかなりません。** 下回ることは、この式の下では起こりえません。**数値実験が +0.690 だったのは、実験が正しくこの恒等式を再現したからです。**

### したがって

| 平坦域の 75% が… | 意味 |
|---|---|
| **単勝の値なら** | **恒等式に反するので、実装のどこかが式と違うことをしている**。機構の問題ではありません |
| **馬連以上の券種の値なら** | **恒等式は成立しません**（§2 で説明します）。原因は既知で、機構の謎ではありません |

**→ ★確認1: 平坦域 75% は、どの券種の値ですか。券種別の払戻率を出してください。**

これは既に `console.log` が券種別に出力しています（214行以降）。**新しい測定は不要です。**

---

## 2. 馬連以上なら、原因は「1回以上出た目だけを売る」ことです

**単勝は全馬が売られるので効きませんが、三連単は目が 2,000通り以上あり、M=4,000 では大半が `c=0` です。** そして `c ≥ 1` で条件付けると:

```
λ = M·p とおくと、売られる確率 P(c≥1) ≈ λ    （λ が小さいとき）
その目の期待払戻 ∝ p × M × E[1/c | c≥1] ≈ λ × 1 = λ
⇒ 掛け金は λ に比例するのに、払戻は λ² に比例する
⇒ 稀な目の払戻率 ≈ λ → 0
```

**稀な目は「掛け金は取られるが、ほとんど払い戻さない」**という状態になります。**これは推定の誤差ではなく、`c ≥ 1` という打ち切りが作る構造的な下方バイアス**で、**裾に集中し、M を上げると縮みます**（λ が大きくなるため）。

**そして「裾で分布が一致する」ことと完全に両立します** — 分布は正しく、**売る目の選び方が非対称**なだけです。z=0.04 は正しい測定で、**問いが分布になかった**だけです。

### 是正（該当する場合）

- **打ち切り補正した推定量を使う**: `p̂ = (c + 0.5)/(M + 1)` など。`c=0` の目にも正の確率を与えて売る
- あるいは **`c=0` の目は売らないが、掛け金にも数えない**（現状は m.size に入っているので整合しています。ここは問題なし）
- **V-10 の目標値の定義を、実装している売り方に合わせる**

**→ ★確認2: `unseenHits` の値は券種別にいくつですか。** これも既に出力されています（「未発売的中」欄）。

---

## 3. ★2つの確認は、どちらも既存の出力に載っています

「券種別の払戻率」も「未発売的中」も **`verify-payout.ts` が最初から印字しています。** 二分探索を始める前に、**この出力を読んでください。**

**これは今回の案件で6回目の同型です**（R-21 の系）。**「測ったが読んでいない」**が繰り返し出ています。前便の「留保を本文に書いて結論では外した」も同じ場所です — **情報は既に手元にあり、読まれていない。**

**二分探索は、この2つを読んでも説明がつかなかった場合に始めてください。**

---

## 4. 優先度についての判断 — **A-3 は後回しで正しいです**

> A-3 の判定域が未確定のまま続ける作業なので、優先度としてはインフラ待ちの他タスクより低い

**同意します。** ただし §3 の2確認は**5分で終わる**ので、**後回しにする前にそれだけ済ませてください。** 判定域（Q-1）の結論も、**券種別の内訳が分かってからでないとこちらが決められません。**

**A-3 以外で進めるもの:** 40シード分布の完走 → 世界シードの §10.5 固定提案、P2 の §10.3（**D-030 で是正済みの表**を使うこと）・§11.2。

---

## 5. ① 撤回の撤回はしません。**結論を維持し、根拠を差し替えます**

| | 平均 | SD | 系統合格率 | V-12a |
|---|---|---|---|---|
| penalty=0 | 4.86 | 2.13 | 45% | **38/40** |
| penalty=0.3 | 4.42 | 1.43 | 30% | **40/40** |

**トレードオフは存在します。D-029 の「トレードオフは存在しなかった」は誤りなので訂正します**（12シードの点推定を根拠にしていました）。

### それでも有効化を維持する理由

1. **V-12a が 38/40 → 40/40。** V-12a は正典のゲートであって代理指標ではありません
2. **合格基準3 は「世界を選ぶ」に決定済み**（D-028）なので、必要なのは十分な候補が出ることで、**30%（3回に1回）で足ります**
3. **★プリシードと本番運用で規則を変えない。** 近交回避を切って世界を作り、有効にして運用すると**開始時点で世界の統計的性格が不連続に変わります**

### 重みは 3.0 ではなく **0.3**

**0.3 で既に V-12a が 40/40 です。** 系統への影響は重みとともに増えるので、**目的を達する最小の重みを採ります** → **`INBREED_PENALTY_WEIGHT = 0.3`**（D-031）。

### 整合性チェックについて

**平均 4.42・SD 1.43 なら P(X≥5) ≒ 34%、実測 30% で整合。** 前便の「平均 0.14 で合格率 18pt は説明がつかない」という指摘は、**12シードの平均差 0.14 が信用できない数字だった**ことを正しく指していました。

---

## 6. ③ 21pt の撤回について

**同じ便に矛盾する2つの主張を並べていた**という自己申告のほうが重要です。**これは測定を待たずに、自分の文書を通しで読み返すだけで検出できる型**です。前便の「留保を本文に書いて結論では外していた」と同じ場所で効きます。

→ **報告書を出す前に「この文書の中で互いに否定し合う主張はないか」を一度通しで読む。**

---

## 7. 評価

**6連続の否定を「自分の仮説生成が機能していない」と診断し、手続きそのものを変えようとした**のは正しい対応です。**回数を重ねる前に手続きを疑うのは、最も遅れがちな判断**です。

そして**二分探索という提案自体が、仮説駆動からの正しい離脱**です。今回こちらが足したのは、**その前に恒等式で向きを固定できる**という一手だけです。**恒等式が「下回りえない」と言っているなら、探すのは機構ではなく実装であり、探索範囲は桁で狭まります。**
