# 再照会 — 「真横の直線だけ」の演出で、結果を先に明かさない線（レビュー側へ）

**案件**: STAR（ポイ活型オンライン競馬育成）
**日付**: 2026-09-14
**出す側**: 開発側（オーナーの指示「聞き直しお願いします」）
**宛先**: ① レビュー側
**前の文書**
- 相談書: `CONSULT_RACE_SIDE_ONLY_20260914.md`
- ① の回答: `REVIEW_CONSULT_RACE_SIDE_ONLY_ANSWER_20260914.md`
- 3 者のまとめ: `SYNTHESIS_RACE_SIDE_ONLY_20260914.md`（§2 の O-7、§6 の Q-R7〜Q-R9）

**ブランチ・HEAD**: `p4/race-30sec-cuts` ／ `319bae0`

> 凡例: **✔** ＝ 開発側がコードで確かめた ／ **計算** ＝ コードの式から出した値で、画面での実測ではない
> 問いの番号は、正典の作業規律 R-1〜R-33 と重ならないように **Q-R7〜Q-R9** としています（① の回答の「問 1〜問 6」の続き）。

---

## 0. なぜ聞き直すか

① の回答 §2-2 ②は、次のように書いています。

> ⚠️ `race-development.ts` は「勝ち馬が直線の入口で何番手だったか」で展開の札を決めています。これを直線の入口で見出しとして出すと、勝ち馬が差し・追込だと先に明かします。
> → 見出しは「画面で起きた後に出す」か「勝ち馬を特定しない言い方にする」。

これは**見出し（文字）**についての回答でした。一方、デザイナー側の回答（D-8）は、同じ札で**第 2 幕全体の構図と音**を変える案です。さらに、**今の既定にも、既に同じ札でカメラを変えている部分があります**（§1）。

文字でなく**画と音**で出す場合も同じ扱いになるのか。回答が無いまま実装すると、どちらに倒しても差し戻しになりうるので、聞き直します。

---

## 1. 事実 ✔

### 1-1. 展開の札の決め方

| 項目 | 中身 | 出どころ |
|---|---|---|
| 勝ち馬 | **確定着順の 1 着**（`settled[0]`） | `apps/web/src/app/race/page.tsx:1685` |
| 判定の地点 | 先頭が最後の直線の入口（`DIST - homeStretchMetersOf(course)`）に達した瞬間 | `page.tsx:1701-1707` |
| 札 | 勝ち馬がその瞬間に 1 番手なら「逃げ切り」、2〜4 番手なら「差し」、5 番手以下なら「追込」 | `packages/render/src/race-development.ts:18・26-31` |

→ 札は「**結果（1 着の馬）**」と「直線の入口での位置」の組み合わせです。直線の入口の時点では、画面を見ている人はまだ知らない情報です。

### 1-2. 今の既定（台本 v8）で、札が変えているもの

| 項目 | 逃げ切り | 差し | 追込 | 出どころ |
|---|---|---|---|---|
| 画角 | 11.0° | 11.5° | 12.0° | `broadcast-v2.ts:2652-2657`（`FINISH_CAMERA_BY_DEVELOPMENT`） |
| 先頭の置き場所（画面の横の割合） | 0.58 | 0.68 | 0.78 | 同上 |
| 変え始める地点 | 残り **260m** から、ゴール板のカットまでなだらかに | | | `broadcast-v2.ts:2650`（`FINISH_DEV_RAMP_FROM_M`） |

- 先頭を右へ寄せるほど、後ろが空きます。つまり「**追込のレースでは、画面の左（後ろ）を広く空けて待つ**」構図です
- 由来はオーナー指示（2026-09-12）「追い込み馬がある時は引く必要がありますが、それは展開によってカメラワークを切り替えてください」（`broadcast-v2.ts:2756-2758`）
- ⚠️ 同じファイルの註記の表（`broadcast-v2.ts:2624-2626`: 11.5°・15°・19°／0.62・0.50・0.38）は、定数と値が合っていません。上の表は**定数の値**です

### 1-3. B で見せる区間との重なり

- B で見せる最後の直線は、**残り 250m から**です（`race-elision.ts:181` `STRAIGHT_SHOWN_M`）
- 札でカメラを変え始めるのは残り 260m からです。つまり B では、**見せる直線の最初のコマから、札に応じた構図になっています**

### 1-4. 表示位置の寄せ（Q-R8 に関係）

- 表示位置は、残り 1000m → 200m で、脚質から作った隊列を真の位置へ寄せていきます（`formation.ts:108-126`）
- 重み（1 ＝ 脚質の隊列だけ／0 ＝ 真の位置だけ）は、なめらかな 3 次の曲線（smoothstep）で下がります
- **計算**: 残り 250m で約 0.011、残り 200m で 0 です。つまり、B で見せる区間に残る寄せは、差の約 1% 以下です

### 1-5. 接戦の判定が既にある（Q-R9 に関係）

- `broadcastV2FinishStyleOf`（`broadcast-v2.ts:2723-2731`）は、先頭が**残り 80m** に達した瞬間の位置で、次のどちらかなら「接戦」、それ以外なら「単独」と判定しています
  - 1〜2 番手の差が 1 馬身以内
  - 1〜3 番手の差が 2 馬身以内
- この地点は寄せが終わった後（残り 200m より先）なので、**真の位置**です
- 使っている場所: ゴール前のカメラ（接戦なら引く）

---

## 2. 問い

| # | 問い | 開発側の読み（参考） |
|---|---|---|
| **Q-R7** | 展開の札で、**直線の入口から構図や音を変える**のは、見出しと同じく「勝ち馬の型を先に明かす」に当たるか。当たるなら、**今の既定の §1-2（残り 260m からの画角と先頭の置き場所）も直す対象になるか**。当たらないなら、どの強さまで許すか（例: 今の程度の画角 1° の差は可、デザイナー側 D-8 の「独走の孤独の構図」「後ろから低い唸り」は不可、など） | 画・音でも同じだと考えます。**文字より弱いが、毎レース見ていれば覚えられる**（「左が空いたら追込が来る」）。直すなら、札の代わりに**画面で起きていること**（§1-5 のような、その時刻の真の位置の差）から構図を決める形があります。ただしオーナー指示（2026-09-12）の由来があるので、オーナー判断も要ると考えます |
| **Q-R8** | 勢いのバーを、寄せが残っている区間（残り 250→200m）で出してよいか。**寄せが終わる残り 200m から**に限るべきか | §1-4 の計算で、寄せの影響は 1% 以下です。そのため、段階に丸めれば**250m から出しても表示は変わらない**と見込みます（未計測）。ただし「描画と同じ入力から出す」（① §2-2 ①）を守れば、寄せの分も含めて絵と矛盾はしません。**定義として「200m から」と決めるほうが検査が単純**、が開発側の意見です |
| **Q-R9** | 写真判定の止め絵を出すかどうかを、**どの値で決めてよいか**。候補は次の 2 つ<br>(a) §1-5 の「残り 80m の真の位置」での接戦判定（既にある）<br>(b) エンジンの確定着差<br>(b) は結果の側の値ですが、**ゴールの瞬間に出す**限り、結果の先出しに当たらないと読んでよいか | **(a) を推奨します。** 画面に映っている位置から決まるので、「映っていない接戦」を作りません（C-2）。(b) は、残り 80m 以降に差が開いた・詰まったレースで、画と止め絵が食い違うおそれがあります |

---

## 3. 回答をいただいた後

- 開発側: `SYNTHESIS_RACE_SIDE_ONLY_20260914.md` の O-7・#10・#13・#21 と §6 を、回答で更新します
- Q-R7 で「今の既定も直す」となった場合: 直す範囲を指示書に入れていただくまで、開発側は §1-2 のコードに触りません（指示書の範囲だけを実装）
- 註記の表と定数が合っていない件（§1-2 ⚠️）: 直すのは次の指示書の範囲に入れていただいたときにします
