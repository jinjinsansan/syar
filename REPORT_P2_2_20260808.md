# REPORT_P2_2 — P2 第2便 / P1.5 再開の反映

**開発側 → レビュー側** / 2026-08-08
**コミット**: `1997164`（push 済み・362 tests 全PASS / typecheck 0）

---

## 0. 先に自己申告

**テストが落ちたままコミットして push しました**（`c3b69f1`）。

```bash
npx vitest run --reporter basic | grep -E "Tests " && git commit ...
```

`grep` が行を見つけて成功したため `&&` が通っています。
**パイプの終了コードは最後のコマンドのもの**で、テスト結果の判定には使えません。
「grep の終了コードで失敗を素通りする」の**再発**です（R-21 が挙げた5件の1つ目と同型）。

→ 以後は `vitest` を単独実行し、**終了コードで判断**します。`1997164` で緑に戻しています。

---

## 1. 指示への対応（完了）

| 指示 | 状態 |
|---|---|
| ① 機能量への移行を撤回 → 合格基準3 は「世界を選ぶ」 | 候補を探索・**推奨1件**（§2） |
| ② D-027 撤回 → 近交回避を有効化 | **完了**（`INBREED_PENALTY_WEIGHT = 0.3`） |
| R-21 を抽出器に | **完了**（世界探索に事前条件・下回れば例外） |
| R-18 をツール共通に | **完了**（掃引スクリプトにもシグナル復元） |
| P2 を進める | §10.3 番組表・§11.2 PP監視を実装（§3） |

### ★近交ペナルティに 0.3 を選んだのは私の判断です（Q-6）

3.0（平均F 0.0094）が V-12a に最大の余裕を与えますが、
**F が小さすぎると §6.5 の近交弱勢と血の濃縮が事実上働きません**。
実在の生産でも平均F は 0.03〜0.10 程度です。
0.3（F=0.0312）は V-12a に 3.2倍の余裕を持ちつつ §6.5 の機構が生きます。
「ゲートを通す最大値」ではなく「機構が働く水準」を採りました。異論があれば従います。

---

## 2. 合格基準3 — 世界候補（30シード・近交回避 有効）

```
合格（有効≥5 かつ 平均F≤0.10）: 8/30（27%）
うち4量すべてが中央値以上: 3件

      seed   有効系統   組数  ニックス   高F%    平均F
  20260833    7.18    132     8    9.8%   0.0217   ← 推奨
  20260823    5.52    132     9   11.1%   0.0234
  20260821    5.36    140     7    8.0%   0.0192
```

**`seed 20260833` を推奨します。** 閾値5に対し 44% の余裕、4量すべてが
40シード分布の中央値以上で、barely pass ではありません。

### ⚠️ ただし合格率の根拠が古い構成のものです

**45% → 27% に下がっています。**「45%」は penalty=0 で測った値で、
近交回避を有効にした現構成の合格率ではありません。
「合格する世界が十分な割合で出る」という条件の判断材料が、現構成のものになっていません。

→ **近交回避を有効にした状態で 40シードの分布を測り直しています**（実行中）。
  結果が出てから §10.5 への固定を提案します。27% でも「十分」と見るかの判断が要ります。

---

## 3. P2 の進捗（インフラ非依存の範囲）

| | 状態 |
|---|---|
| §9 馬券・オッズ・払戻（`packages/betting`） | 実装済み |
| §10.2 開催サイクル（`packages/scheduler`） | 実装済み |
| §10.3 番組表 | 実装済み・**正典の不整合1件**（Q-4） |
| §11.2 PP発行量監視 | 実装済み |
| §4 スキーマ / §14.4 RPC（SQL） | 記述済み・**未適用** |
| A-1/A-2/A-4/A-5/A-7 | **インフラ待ち** |

### §11.2 で R-16 を適用しました

`margin_actual` を券種横断の1つの比で見ると、**券種ごとの偏りが打ち消し合います**。

```
単勝    実控除 30%（設定18%） → +12pt
三連単  実控除  0%（設定23%） → −23pt
横断    実控除 15%（設定平均20.5%） → 5.5pt しか出ない
```

**券種別に出し、券種別に判定する**形にしました。この打ち消しをテストで再現しています。

---

## 4. ★A-3 — 手を止めることを提案します

### 到達点

```
払戻率 80.53%（目標 82%・残差 1.5pt）  ※3,000R・MC4000・seed42
較正曲線は全7帯で予測と実現が一致 → **確率推定そのものは正しい**

不足 0.233 の分解（Σpᵢ/p̂ᵢ の帯別寄与）
  0〜2%    -0.176  ← 76%
  2〜5%    -0.027
  5〜8%    -0.069
  8〜12%   -0.001
  12〜18%  +0.079
  18〜30%  -0.043
  30〜100% +0.003
```

**場所は 0〜2% 帯**です。ただし較正曲線ではこの帯の**平均**が 1.03% vs 1.01% で
一致しており、それでも比で重み付けた寄与は 4% 不足します。
**帯の中の極端側**にずれがあることを示します。

### なぜ止めるべきか

1. **判定域そのものが Q-1 で照会中**です。「10万レース」の読み方が決まらないうちに
   ±1% を追うのは、動く的を撃つことになります
2. ここから先は「MC で 1/4000 前後の確率をどこまで正確に推定できるか」という
   **推定論の問題**で、機構を1つ特定しても ±1% に入る保証がありません

→ 代わりに **「現在の実装で達成できる払戻率の精度」を提示**するほうが判断材料になります。
  80.53%（残差1.5pt）が現状の到達点で、±1% が達成可能な水準かの判断に使えます。

### ★A-3 で同じ型を4回繰り返しました

| | 疑ったもの | 結果 |
|---|---|---|
| 1 | 乱数の相関 | 相関なし（そちらの直接検定で否定） |
| 2 | 配当上限 | cap該当 0.0%・cap を外しても不変 |
| 3 | 大穴の過大評価 | 300R のノイズ（3,000R で消滅） |
| 4 | 未発売の大穴 | 全馬売られていた |

いずれも**先に機構を推測し、それを測って否定される**順序です。
3 では自分で「SE ≒ 0.20%・2.5 SE・断定にはレース数が要る」と書きながら、
見出しでは「原因を特定」と書きました。**留保を本文に書いて結論では外していました。**

5回目でようやく推測をやめ、寄与の分解で**場所から特定**しています。

### R-16 の再発でもあります

較正曲線（**平均**の比較）で「較正は正しい」と結論した直後に、
比の恒等式が合わない理由を探していました。
**使った検査が、探している歪みを構造的に見られないもの**でした。
恒等式は 1/p̂ で重み付いた和なので、帯の平均が一致しても和は一致しません。

同じことが切り分けにも起きています。χ²=6.5（自由度9）で「引き方は同分布」と
結論しましたが、**あの χ² は分布の主要部に支配されており、1%以下の裾は見ていません**。
そちらの直接検定（r ≒ 0.001）も相関係数は裾をほとんど反映しないので、
両者が「異常なし」と言う一方でシステムが異常を示した食い違いは、これで説明がつきます。

→ **検定が異常を検出しなかったとき、その検定が異常のある場所を見ているかを先に確認する。**
  R-21 が抽出について言っていることの、検定版だと考えています。

---

## 5. 照会（`QUESTIONS_P2_20260807.md`・Q-4〜Q-6 を追加）

| # | 内容 |
|---|---|
| Q-1 | A-3 の「10万レース」の読み方（回答済み・§4 の提案を追加） |
| Q-4 | **★正典 §10.3 の不整合**: 重賞は週63枠だが週次頻度は G1+G2+G3 = 31 |
| Q-5 | §10.3 の解釈2件（クラスの1日均等配置 / G1 の曜日） |
| Q-6 | 近交ペナルティに 0.3 を採った理由 |

---

## 6. 次

1. **分布の測り直し完了 → 世界シードの §10.5 固定を提案**
2. Q-4〜Q-6 の回答を反映
3. インフラが立ち次第 A-1/A-2/A-4/A-5/A-7
