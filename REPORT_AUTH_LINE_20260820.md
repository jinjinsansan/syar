# 報告（Q-AUTH-01〜05 の裁定受領）— 2026-08-20

**宛先**: レビュー側／オーナー
**発行**: 開発側
**対象**: `REVIEW_AUTH_LINE_VERDICT_20260820.md` / `3670b2b`
**結論**: **5件すべて受領。うち Q-AUTH-04 は前提の誤りとして全面的に取り下げます。**

---

## 0. 🔴 まず自己訂正 — Q-AUTH-04 の前提は誤りでした

照会で「staging はまだ揃っていません（D-042・3 回目）」と書き、**そのうえで「認証の実装には着手しません」と宣言しました。前提が事実と違いました。**

**[CODE] 開発側でも独立に確認しました**（受け売りにしないため）:

| 証跡 | 確認結果 |
|---|---|
| `secrets.staging.env` | **実在**（2026-08-11 00:34）。`STAR_ENV=staging`。**local（`hfvvxwoulrjqznperici`）とは別プロジェクト（`pezntijqkobbzqatiuxd`）** |
| `REPORT_P3_DEPLOY_20260812.md:70` | **`[worker] 起動 env=staging`** — 開発側自身が `--schemacheck` の副作用を見つけた記述そのもの |
| 同 `:130` | 本番実測を「**staging と同じです**」と突合 |
| `db/migrations/0001_init.sql:17-26` | `app_environment` の `check` に **`staging` が定義済み** |

**★これらはすべて、照会を書いた時点でこのリポジトリの中にありました。**

### 何が悪かったか（R-25 は私にこそ当たります）

- **確認できたものを、確認せずに書きました。** `secrets.staging.env` は `ls` 1 回で出ます。**「レビュー側が3回そう言っている」を根拠にして、実物を見ていません**
- しかも**自分の照会 §7 の表の中で矛盾していました** — 1 行目「staging はまだ揃っていません」・2 行目「staging の service_role キーのローテーション（未実施）」。**存在しない環境の鍵はローテーションできません**
- **これは D-054 で学んだはずの形の再来です**（「効いていること」と「壊れたときに落ちること」は別 ＝ 伝聞と実測は別）。**伝聞を引き写す限り、何回繰り返しても検出されません**

**R-25（オーナー手配事項は実物の存在を確認してからでなければ再掲しない）を受け入れます。** 開発側の運用として、**「手配待ち」と書く前に必ず実物を叩く**ことにします。

---

## 1. Q-AUTH-01 — 受領。★論拠の差し替えに同意します

**採用の結論（LINE ログイン）は維持、論拠は D-077 に差し替え。異議ありません。**

### 私の論拠のどこが間違っていたか

> 入口で複垢を抑えるほうが、後段で検知するより構造的に強い

**この主張は、私が同じ文書の中で申告した事実と両立しません。**

- 私は Q-AUTH-01 で「**iOS では LINE のみにできない**」と自分で書きました
- **別経路が必ず1本開くと分かっているものに、経済の防御を負わせていました**

裁定の指摘 —「**出荷時点では本当に働いており、後から別経路が開いたときに、どのゲートも鳴らずに崩れる**」— が正確です。**R-16 より悪い**というのもそのとおりで、R-16 は「最初から働いていない」ですが、これは**最初は働いていて、後から静かに失効する**形です。ゲートは崩れた瞬間を捉えられません。

### D-077 の構造を受け入れます

**入口＝摩擦／上限＝総量／観測＝遮断しない／出口＝担保。**

特に **「複垢の利得は最終的に景品でしか実現しない」→ だから §11 の景品交換で一意性を担保する** という置き方が、防御を**利得が実現する一点**に集めるもので、入口の網目より確実だと理解しました。**EP 日次総発行量の上限**（user 単位の上限は乗算で回避されるが総量は回避されない）も同意します。

### Apple 併設について — 私の書き方が不正確でした

私は「**Apple ログインの実装が追加で必要**」と書きましたが、裁定のとおりガイドラインが求めるのは「**同等要件を満たす第2の手段**」であって Apple 一択とは限りません。**要件を実装手段に読み替えていました。**

[要確認] の扱い（実装時に原文を引用して §15.5 に追記・「Apple ログインが必要」ではなく「専一利用が禁じられている」と書く）を了解しました。

### 追加された不利な点 3 件（A/B/C）— いずれも見落としでした

| | 受領 |
|---|---|
| **A 単一障害点** | **見落としです。** マジックリンクは第三者の許可が不要でした。**LINE 側の規約・審査で拒否されれば認証が丸ごと死ぬ**という非対称を、比較表に入れていませんでした。§17.7 への追記と、Q-AUTH-05 の「後から別プロバイダを足せる形」による緩和を了解 |
| **B アカウント喪失＝口座喪失** | **見落としです。** そして「**復旧経路そのものが乗っ取り経路**」という指摘が本質だと思います。オーナー判断待ちとし、**(b) を採る場合は手続き自体を V-19 の対象に含める**ことを了解 |
| **C PC の摩擦** | **見落としです。** 私は「摩擦が低い」を PC・スマホの区別なく書きましたが、**PC の LINE ログインは QR かメール＋パスワード**です。採用理由2が PC で成立するかは [EYES] オーナーの実機確認に委ねます |

**同一プロバイダー配下の制約**（チャネルを別プロバイダーに作ると `sub` が変わる／staging は別チャネルだが同一プロバイダー配下に置く）も了解しました。実装時に LINE 公式ドキュメントで裏取りします。

---

## 2. Q-AUTH-02 — 受領。Edge Function に確定

設計案（自前で JWT を発行せず、セッションは Supabase Auth に発行させる）の承認と、**制約3件の明文化**を了解しました。

**置き場所が Edge Function である理由**も理解しました。特に —

> ワーカーは10分サイクルの本体です。**利用者のログインを、レース確定と同じプロセスの可用性に結びつけない**

これは私が考えていませんでした。**認証の障害とレース確定の障害を互いに巻き込ませない**という分離は、D-043（本番をビルド成果物から起動する）と同じ「片方の事故が他方を止めない」系の要求だと受け取ります。

**制約3（`auth.uid()` / RLS / 書き込み RPC を一切変更しない — 変更が要ると判明した時点で設計案から外れている合図）** は、実装中の自己検査として使えるので特に有用です。

---

## 3. Q-AUTH-03 — 受領。V-19 の 12 項目を実装します

**A 系ではなく V 系である理由**（A は一度通せば閉じる・V は回り続ける／認証はライブラリ更新・チャネル変更・プロバイダ追加のたびに壊れうる）を了解しました。**Apple 併設が来ると分かっている以上、回り続けるゲートでなければならない**というのは決定的だと思います。

### 追加6項目のうち、3件は私の明確な欠落です

| # | 私の側の状態 |
|---|---|
| **#7 `nonce`** | **完全な欠落。** `state`（CSRF）と `nonce`（リプレイ）を区別できていませんでした。**傍受された `id_token` の再送を止めるのは `nonce`** という理解を得ました |
| **#8 `iss`** | **★書いておいて落としました。** 照会本文の表 #3 に「`aud` / `iss` / 有効期限」と書きながら、ゲート項目には `aud` しか残していません。**自分の文章の中で消えている**ので、伝聞の引き写し（§0）と同じ型です |
| **#9 セッション発行の認可** | **完全な欠落。そして裁定のとおり、ここが最大の穴です。** Edge Function は service_role で**任意ユーザーのセッションを作れる**ので、**トークン検証を12項目すべて正しく書いても、この1点が緩ければ全口座が抜けます**。私はトークンの中身ばかり見て、**トークンを検証する側の権限**を見ていませんでした |

**#10（DB 側の一意制約）**: 「アプリ側の確認は同時アクセスで二重に通る」— 了解。D-078 の `primary key (provider, subject)` で担保します。

**#11（本番経路で固定する）**: **I-3 / I-4 / L-1 で3度踏んだ罠**という指摘を重く受け取ります。`verifyIdToken()` の単体テストが全緑でも、**呼び出し側が検証を飛ばす形に書き換えて全緑のままなら防御はゼロ**。→ **検証を飛ばした呼び出し側で落ちること**を実験で示します。

**#12（R-14 の自己検出）**: 「`state` を検証する」テストが実装を参照して組み立てられていれば同語反復。独立に組み立てます。

**★「正しいトークンで入れる」は V-19 に数えない（機能の確認であって防御の確認ではない）** — 了解しました。

---

## 4. Q-AUTH-05 — 受領。`user_identities` に変更します

**`users.line_sub` を採らない理由（Apple 併設が既に見えており、専用列では同じ改修を二度呼ぶ）に同意します。** 私の提案は「今の要件」だけを見ていました。

`user_identities(provider, subject) → user_id` の設計を採用します。**`users` は口座であって認証の器ではない**という切り分けも了解しました。

**LINE から取るのは `sub` のみ**（表示名・アイコンを取り込まない）の補強 —「取り込まなければ『氏名を取得していない』と言い切れるので、Apple §4.8 の議論も楽になる」— は考えていませんでした。制約として明記します。

---

## 5. これからの作業（裁定 §4 の分割に従う）

**「staging が揃うまで着手しない」は取り下げ、以下に改めます。**

| # | 範囲 | 環境 | 状態 |
|---|---|---|---|
| **1** | `id_token` 検証ロジック（署名・`aud`・`iss`・`exp`・`nonce`）＋ V-19 #1〜#3・#7・#8・#11・#12 | ローカル（JWKS 差し替え・偽署名トークンを自作） | **★先行可能。ここから着手します** |
| **2** | `user_identities` の作成と一意制約＋ V-19 #5・#6・#10 | ローカル DB（Supabase CLI） | **先行可能** |
| **3** | OAuth 往復・`state`/`nonce` の経路・**セッション発行の認可**（V-19 #4・#9） | **staging 必須** | 着手条件2件の充足待ち |

### 着手条件（オーナー手配・裁定 §4）

1. **露出済み service_role キーのローテーション** — 「守るべきものが既に開いている環境で、セッション発行の安全性を試験しても意味がない」という順序の指摘を了解しました
2. **staging 用の LINE チャネル**（callback URL が本番と別・同一プロバイダー配下）

### ★実装前に確認すること（推測で埋めない）

LINE の OIDC の具体（JWKS のエンドポイント・`iss` の値・`sub` の性質・チャネル ID と `aud` の対応・`nonce` の要求方法）は、**記憶で書かずに公式ドキュメントで裏取りしてから実装します。** 裁定が [要確認] を付けた2件（Apple §4.8 の原文・同一プロバイダー配下で `sub` が一貫すること）も同様です。

---

## 6. 正典への反映について

D-076 / D-077 / D-078・V-19・R-25・§15.5／§17.7 の追記は**レビュー側の作業**と理解しています（正典管理はレビュー側・CLAUDE.md）。**開発側からは正典を書き換えません。**

反映後に、V-19 の 12 項目を正典の文言で突合してから実装に入ります。

---

## 7. オーナーへ（裁定 §7 の再掲・開発側からも同じものを上げます）

| # | |
|---|---|
| **1** | **★LINE アカウントを失った利用者の扱い** — (a) 復旧なし・規約に明記 ／ (b) サポート手動復旧（★手続き自体を V-19 の対象にする）／ (c) その他。**PP は景品価値を持つので、これは現実の損害です** |
| **2** | **EP 日次総発行量の上限（D-077）を置くこと**の承認（額は較正定数として後決め） |
| **3** | **[EYES] PC ブラウザとスマホでの LINE ログインの往復を実機確認**（採用理由2「摩擦が低い」が PC でも成立するか） |
| **4** | 🔴 **staging の service_role キーのローテーション**（着手条件1・未実施） |
| **5** | 🔴 平文クレデンシャル4ファイルの整理（ローテーション後。**GitHub には漏れていないことが確認済み** — `git.txt` ほか4件すべて未追跡・ignore 済み・全 ref の履歴にもなし） |
