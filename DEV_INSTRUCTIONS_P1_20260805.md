# 開発指示書 P1 — レース判定エンジン + モンテカルロ検証

**発行**: 2026-08-05（レビュー側 Claude）
**前提**: `REVIEW_P0FIX5_VERDICT_20260805.md` を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**§8 レースシミュレーション+Provably Fair・§8b 介入・§13.2 検証基準 を読み直すこと**）
**着手条件**: P0-fix5 がレビュー PASS していること

---

## 0. P0 完了 — ここまでの到達点

遺伝エンジンは6便（P0 → P0-fix5）を経て完成しました。**確定した基盤**:

- 2アレル継承・隔世遺伝6%・大物覚醒1.5%（**能力5種のみ**）・突然変異＋平均回帰・Wright近交係数・インブリード補正・近交弱勢・ニックス・生産上限
- 全定数が `FOUNDERS` と平衡式から**導出**され、CLI 上書き時も再導出される
- V-1 / V-2a〜e / V-3 の7系統ゲートを **4シード × 100/300ゲーム内年・実運用規模800頭**で通過

**P1 はこの上に乗ります。P0 のゲートは回帰テストとして回し続けてください。**

---

## 1-0. 先に片付ける P0 の積み残し（4件・小）

P0-fix5 のコード監査で出たものです。**いずれも出荷既定の挙動には影響しない**ため6便目は立てず、P1 の冒頭作業として同梱します。**K-1 以降に着手する前にこの4件を終わらせてください。**

### L-1【必須・退行】`--set-genetics MUTATION_SD=0` で導出 sd が NaN になる

```
MUTATION_SD=0 → durability {"sd": NaN, "clamp": NaN}（非能力6形質すべて）
```

機序: `clampRatio = MUTATION_CLAMP / MUTATION_SD = 150/0 = Infinity` → ガード `!(clampRatio > 0)` は **Infinity を通す** → `clampTruncationFactor(∞)` が `2·∞·φ(∞) = ∞·0 = NaN`。

**P0-fix4 では同じ上書きが `{sd: 66, clamp: Infinity}`（有限）だったので、J-1 が新たに開けた穴です。** NaN の sd は `mutateAllele` 経由で **genotype に静かに流れ込みます**。

対処: `Number.isFinite(k)` でなければ **1 を返す**（clamp/sd → ∞ は「切断なし」なので解析的に正しい）。結果は `{sd: 60, clamp: Infinity}` となり、clamp 実質無効という意味も通ります。

> **★ここが本題です。** `regression.test.ts` には「`MUTATION_SD=0` でも NaN を返さない」という**同名のテストが既にあるのに、balance を直接組んでいて `resolveRuntimeConfig` を通らないため捕まりませんでした**。I-3・I-4 とまったく同じ構造が**3度目**です。
>
> したがって L-1 の対処は修正だけでは足りません。**既存テストの棚卸しを1度行い、「本番経路を通っていないテスト」を洗い出してください**（`resolveRuntimeConfig` / `runSimulation` / `breed` を経由しないもの）。全部を書き換える必要はありませんが、**一覧と、経路を通すべきものの判断**を報告書に書いてください。これが R-1 を実効化する唯一の方法です。

### L-2【必須】`CLAMP_TRUNCATION_FACTOR = 0.9156` の二重管理を解消

J-1 で本番が解析式を使うようになった結果、この定数の参照元は `regression.test.ts` だけになりました。**テストが古いリテラルから期待 sd を計算している**ため、将来 `MUTATION_CLAMP`/`MUTATION_SD` を動かすとコードと期待値が食い違います。`balance.ts` の「比を変えたら再計算すること」というコメントも、いまとなっては誤誘導です。

定数を削除するか、「`clampTruncationFactor(MUTATION_CLAMP_RATIO)` と一致すること」だけを固定する形にしてください（**G-0 / I-2a で二度潰したのと同じクラス**です）。

### L-3【必須】`v2d.worstDeviation` が NaN に塗り潰される

`reduce` の初期値が先頭要素で、以降は `Math.abs(t.deviation) > NaN` が常に false になります。`V2D_TRAITS` の先頭 `surface.turf` が J-2 の `none` フィクスチャそのものなので、実測で `worstKey=surface.turf / worstDeviation=NaN` となり、**同時に存在する実数の乖離が隠れます**。

合否は `every()` で別計算なのでゲートは正しいのですが、**最も診断が必要な場面で verify 表に NaN が出て真の最悪形質が見えません**。NaN を最後尾に送るだけで解消します。`--json` の型宣言（`number`）と実値（`null`）の不一致も直してください。

### L-4【必須・憲法】`AI_PROMPT.md` の実在馬名をスクラブする

廃棄済み文書ですが**実在競走馬名が10頭ぶん残っています**（憲法 §0.1）。P1 は命名・アセットに近づく段階なので、いま処理します。ファイルごと削除するか、冒頭に「**廃棄済み。実在馬名を含むため実装・辞書・アセットに一切持ち込まないこと**」の警告を入れてください。判断は任せます。

---

## 1-0c. K 便に同梱する積み残し（N-1〜N-5）— M 便のレビューより

M-1〜M-6 は **PASS**（実質的な修正はすべて成立し既定経路も不変）。特に **M-3(c) の補強は本物**でした — 監査が「`breed` が `resolveMutation` を完全迂回する」改変を試したところ、**`regression.test.ts` の定数アサーション5本はすべて素通りし、あなたが追加した経路テストだけが落ちました**。単体テストが構造的に取りこぼす穴を、あの2本が実際に埋めています。

残りは報告の正確性と小さな清掃です。

| # | 内容 |
|---|---|
| **N-1** | **`SPEC.md` に廃棄警告ヘッダを付け、禁止語を処理する**。「スターホース」4件（タイトル含む）・WBC・SWBC・メダル・ライド馬券・SH経済が全編に散在し、**`AI_PROMPT.md` と違って警告ヘッダがありません**（次に開いた人/エージェントが現行仕様と誤認しうる）。`AI_PROMPT.md` の同種の残り（WBC/SWBC/メダル/SH経済）も。**M-6 が `AI_PROMPT.md:21` しか名指ししなかったのはこちらの指示不足です** |
| **N-2** | 棚卸しの数字を訂正: 経路/単体は **52/51（49.5%）**（実行時計測による実測）。`config.test.ts` は 10本のまま（増えたのは `regression.test.ts` 34→36） |
| **N-3** | `balance.ts:387` の無名の「テストで固定している」に**テスト名を入れる**（`:58` に書いた自戒の適用漏れ。主張自体は真） |
| **N-4** | `sweep-distance.ts:37-39` の `sdFor()` が切断係数で割っておらず、**表示 sd 312 と同じループが実際に使う 341 が別物**。M-1 と同じ「記述と実装の食い違い」クラス |
| **N-5** | `config.test.ts:14-20` の同語反復（**M-3(c)② が未対応**）。再導出を丸ごと省いても当該4件は緑のままです |

### ★2つの新しい恒久ルール（今回の再発から）

| # | ルール | 由来 |
|---|---|---|
| **R-9** | **「ヒットゼロ」を根拠にするときは、検索式が拾えない形を1つ以上列挙する**。`toBe(x)` を探すなら `toBeCloseTo(x, n)` / `toEqual(x)` / 変数経由 を意識的に潰す。**ゼロという結果は、探し方が正しいことを保証しない** | M-2 の「ヒットゼロ」が誤りだった（`config.test.ts:76` の `toBeCloseTo(0.9156, 4)` を検索式が構造上マッチできなかった）。**M-1/M-2 の真因＝検証器が当の事例を見られない、と同じ形の再発** |
| **R-10** | **「数える」成果物（カバレッジ等）は静的検索でなく実行時計測で出す**。静的検索は必ず取りこぼすか過大計上し、**どちらに外れるかは事前に分からない** | 棚卸しが 46% → 実測 49.5%。但し書きの誤差方向も逆だった |

**`rg` にも同じクラスの罠があります**: **既定で `.gitignore` を尊重するため、`AI_PROMPT.md` / `SPEC.md` はリポジトリ全体 rg では黙って除外されます**（`--no-ignore` または `-uu` が必要）。N-1 の検閲はこれで行い、コマンドを報告書に書いてください。

> **レビュー側も同じ罠を踏んでいました。** L-4 の回答で「リポジトリ全体を独立検閲した」と書きましたが、私が使った `git grep` は追跡ファイルしか見ません。`AI_PROMPT.md` は検索対象外でした。**「検証した」と書くときは、何をどう検証したかを併記する** — R-9 の自分への適用です。

---

## 1-0b. 【完了】M-1〜M-6

L-1〜L-4 のコード監査で出たものです。**K-1 の着手は止めませんが、K 便の報告と同時に対応を報告してください。**

L-1（修正・経路移設）・L-3・L-4・既定経路の不変性・回帰・憲法はすべて追認されました。追加された近交係数の経路テストも、`breed()` 側を潰すと確実に落ちることを実験で確認しています（`topAncestorId` を潰したとき落ちるのは**このテストだけ**でした。良いテストです）。

### M-1【必須】`balance.ts:54` の「テストで固定している」は**偽**

コメントに「`clampTruncationFactor(MUTATION_CLAMP_RATIO)` と一致することはテストで固定している」と書かれていますが、**そのテストは存在しません**。`regression.test.ts:197-201` は本コミットで一切変更されておらず、リテラル 0.9156 を直接固定し、誤誘導コメントもテスト名もそのままです。

**コードに「テストで守られている」と書いてあれば、次に読む人（人間でも Claude でも）はそれを信じます。存在しない防御を宣言することは、防御が無いことより危険です。**

→ 実際にそのテストを1本足すか、コメントを事実に合わせてください。

### M-2【必須】L-2 の「全箇所直しました」が未達

`config.test.ts:25`（`const f = 0.9156;`）と `:40`（`/ 0.9156`）が**古いリテラルのまま**です。加えて `balance.ts:297` に「この比を変えたら `CLAMP_TRUNCATION_FACTOR` の再計算も必要」という誤誘導コメントが同文で残っています（`balance.ts:44-55` は直っています）。

現状 **0.9156 は3か所に散在**しており、比を動かすには手で同期が要ります。**二重管理は解消でなく移動しました。**

### M-3【必須】棚卸しの訂正

| # | 訂正事項 |
|---|---|
| a | 件数が誤り: `config.test.ts` 12 → **10**、`regression.test.ts` 35 → **34**（表の合計 104 が総数 101 と不一致） |
| b | 「全7ファイル」は `vitest.config.ts` のスコープ限定によるもの。ツリーには19本あり12本は走らない。**スコープを明記**すること（第三者ディレクトリなので除外自体は妥当） |
| c | **「別の経路テストが裏から押さえている」の1行目が成立しません**。`regression.test.ts` の定数アサーションが呼ぶ `resolveMutation` は `breed` が実際に呼ぶ関数ですが、`config.test.ts` は `buildTraitMutation` の出力しか見ておらず **`resolveMutation` に一度も到達しません**。しかも `config.test.ts:14-20` は `DEFAULT_BALANCE` との同語反復で、**`resolveRuntimeConfig` が再導出を丸ごと省いても通ります（＝I-4 そのもの）**。→ 根拠を撤回するか、経路側のテストを足して補強してください |
| d | **テスト単位の実態を開示**してください（`regression.test.ts` は34件中25件が経路を通らない、等）。ファイル単位の分類は間違っていませんが、実態が見えません |
| e | `helpers.ts` の `Herd` が `HorseRecord` をリテラルで直接組むため、**`createFounder` / `buildPhenotype` が構築時点で全面バイパス**されています（4ファイルの全馬がここ由来）。これをどう扱うかの判断を書いてください |

### M-4【必須】`sweep-distance.ts:90` の `clampRatio` 欠落

`buildTraitMutation(FOUNDERS, r)` を **`clampRatio` 引数なし**で呼んでいます。現状は `MUTATION_SD` を上書きしないので無害ですが、**I-4 の再発形がテストゼロのファイルに残っています**。`sweep.ts:81-85` も同型です。

### M-5【必須】`simulator.ts:275` の型と実値の不一致

`traits[].deviation: number` が依然 `number` 宣言で、実値は `NaN`（JSON で `null`）。**L-3 で `worstDeviation` だけ直り、同じ配列の要素側に同一クラスが残っています。**

あわせて、**L-1 で承認した `clamp: Infinity` の JSON 表現**もここで処理してください。レビュー側が L-1 の修正確認時に「`clamp: null` が NaN なのか Infinity なのか判別できず、本番経路を直接叩く手順が余分に必要」という実害を踏みました。**修正前（NaN）と修正後（Infinity）が JSON 上で同じ表現になるのは、直ったかを判定したい場面で見分けがつかないということです。** 非有限値を文字列化するか `isFinite` フラグを併記してください。

### M-6【推奨】`AI_PROMPT.md:21` の商標

「スターホース」が残っています（正典 §0.1 が禁止語として挙げている当のもの）。untracked かつ警告ヘッダ付きなので実害は低いですが、唯一の例外です。ついでで構いません。

---

## 1. スコープ

### K-1 レース判定エンジン（正典 §8.2〜§8.5, §8.7）

`packages/race-engine/`（新パッケージ・**純粋 TypeScript・依存ゼロ**）として実装:

| 機能 | 正典 |
|---|---|
| 距離帯別の能力重み（5段階） | §8.2 |
| 乗算補正10種（距離適性・馬場・馬場状態・脚質・調子・疲労・斤量・枠順・年齢・スキル） | §8.3 |
| 展開（逃げ宣言頭数からペース決定）＋脚質適性 | §8.4 |
| 発動型スキル6種（発動率 = 40% + IQ/2000） | §8.5 |
| 着順・着差・タイムの生成（`finalScore` 降順・`K = 0.12`） | §8.7 |

### K-2 Provably Fair（正典 §8.6・§8.8）

> **★乱数系列の独立性を検証項目に含めてください。** L 便であなたが「`deriveSeed` を実際に使っているかは固定されていないが実害が想定しにくいので記録に留める」と判断した件、**P0 の範囲では同意**しますが **P1 では前提が変わります**。レース判定・モンテカルロ・介入がそれぞれ乱数を消費するため、**サブストリームが分離されていないと系列が相関し、V-4〜V-6 の較正が静かに歪みます**（例: オッズ算出用と本番確定用が相関すると、人気と結果に人工的な相関が生まれる）。正典 §9.2 も「オッズ算出のシードは本番確定用とは**別系列**」と定めています。同一シードで系列ごとの出力が無相関であることを確認してください。

- `seed_commit = SHA256(server_seed)` → `final_seed = HMAC-SHA256(server_seed, race_id + client_entropy)` → `seed_reveal`
- **同じ seed から同じレースが完全再現できること**（リプレイ・不正調査の前提）
- **介入ログを含めた再現**（§8.8）: `seed + 介入入力ログ → 同じ着順` をサーバー側で再計算できる形にする
- ハッシュ関数は外部依存なしで実装するか、Node 標準の `crypto` を**注入**する（エンジン本体は依存ゼロを維持・P0 と同じ方針）

### K-3 介入モデル（正典 §8b・スコア側のみ）

**UI は作りません。** `interventionMult` を計算するモデルと AI 代行だけを実装します:

- スタミナゲージ（初期値は ST・調子・疲労から算出・§8b.2）
- 4局面のボーナス（スタート/道中/勝負所/直線）と早仕掛けペナルティ（§8b.3）
- **ハードキャップ ±10%**（`INTERVENTION_CAP`）
- **AI 代行**（§8b.5）: 無操作・切断時の中立既定値。目標は手動最適の 93〜97%
- ラグ猶予・連打上限（§8b.6）は**モデル側の定数として持つだけ**（実際の入力処理は P4）

### K-4 選抜を実レース結果ベースへ置換

P0 の種牡馬選抜は「真の potential ＋観測ノイズ（h²=0.3）」という**近似**でした。P1 では**実際のレース成績**（賞金・勝率など）で選抜します。

- P0 の `selectionH2` は**廃止**（または「レース非搭載時のフォールバック」として明示的に隔離）
- 選抜指標の定義（獲得賞金か勝率か複合か）は開発側の提案を報告書に書いてください。**レビュー側が指定しません** — 実測して妥当な指標を選ぶ性質のため

### K-5 モンテカルロ検証ハーネス

`npm run verify:race`（仮）で V-4/V-5/V-6/V-9 を判定できるようにしてください。**人気**は同一レースをモンテカルロした勝率順位で定義します（馬券・オッズは P2 なので、確率だけ使う）。

---

## 2. やらないこと

- **育成ループ**（P3）・馬券とオッズ（P2）・開催スケジューラ（P2）・UI（P4）・DB・サーバー
- **V-7（故障率）** — 故障は調教で発生する（§7.5）ため P3 に移しました。**正典 §13.2 を是正済み**です
- 介入の入力処理・タイミング判定の実装（P4）

---

## 3. 現在値（current stats）の扱い — ★重要な前提

レースは `stats`（調教で伸びた現在値）を使いますが、**育成ループは P3 です**。そこで P1 では:

- **素質開放率のプレースホルダ**を置く（例: 馬ごとに 0.55〜0.85 を分布からサンプルし `current = potential × 開放率`）
- **この値は仮であり P3 で実際の調教モデルに置き換わる**ことを、コードのコメントと報告書の両方に明示してください
- **`RACE_RANDOM_K = 0.12` の較正はこの仮定に依存します。** P3 で本物の育成ループが入ったら **K を再較正する必要がある**旨を、正典への追記依頼として報告書に書いてください

> **これは P0 で繰り返し踏んだ罠と同じ構造です**（「200頭・V-2d なしで校正した距離定数が、条件を変えたら FAIL した」）。**較正した条件を明記しておかないと、条件が変わったときに誤った結論が生き残ります。**

---

## 4. 合格基準

| # | 基準 | 出典 |
|---|---|---|
| **V-4** | 1番人気の勝率が **30〜34%**（10万レース） | §13.2 |
| **V-5** | 1番人気の複勝率が **60〜65%** | §13.2 |
| **V-6** | 最低人気の勝率が **0.5〜2%**（ゼロにしない＝夢を残す） | §13.2 |
| **V-8** | AI代行の期待値が手動最適の **93〜97%** | §13.2 |
| **V-9** | `interventionMult` の平均 0.99〜1.01・全サンプルが 0.90〜1.10 内 | §13.2 |
| **V-12** | 平均F が 0.10 以下・丈夫さの「表現型 − genotype」乖離が −8% 以内 | §13.2（新設） |
| 再現性 | 同じ seed（＋介入ログ）から同じ着順が完全再現できる | §8.6/§8.8 |
| **P0 ゲートの維持** | V-1 / V-2a〜e / V-3 が**実レース選抜下でも**成立する | §13.2 |
| 共通 | テスト全PASS・typecheck exit 0・`any` ゼロ・決定論・4シード以上 | — |
| **K-a** | **`Herd.add()` が作るレコードが `expressPhenotype` の出力と「値として」整合すること**を固定するテスト（M-3(e) の P1 送り分）。※「全フィールドが揃っていること」は **TypeScript が既に保証しているので追加価値がほぼありません**（監査の指摘）。実効があるのは値の整合です（`strategyAptitude` は jitter があるため厳密一致しない点に注意） | M 便 |
| **K-b** | `balanceDigest` の**シリアライズ後の型を宣言**する（内部型は `number` のままでよい。`Infinity` も `number` なので内部型は正しく、ずれているのは JSON 表現の型宣言。例: `number \| "Infinity" \| "-Infinity" \| "NaN"`）。M-5 が直したのと同じクラスが一段外側に残っている状態 | M 便 |
| **N-1〜N-5** | §1-0c の積み残し5件 | M 便 |

**特に注意してほしいのが「P0 ゲートの維持」です。** K-4 で選抜が実レース結果ベースに変わると選抜圧の形が変わります。特に:

- **`distance_range` の大きい万能型が有利になり、選抜圧で万能型に収束しないか**（V-2e）— あなたが P0-fix2 の Q2 で挙げた懸念です。大物覚醒の限定（D-011）で発生源は断ちましたが、**選抜圧は新しい押し上げ要因**になりえます
- 能力5種の平衡（V-2a/V-2b）が選抜圧の変化でどう動くか

**基準線は P0-fix4/5 の数値**です（両者は同一）。P0-fix3 以前と比べると切断補正の系統差で解釈を誤ります。

---

## 5. 恒久ルール（P0 で確立した標準・以後すべての便に適用）

P0 の6便で繰り返し見つかった失敗パターンです。**P1 では最初から守ってください**:

| # | ルール | 由来 |
|---|---|---|
| R-1 | **単体を固定するだけでなく経路を固定する**。純関数を正しくしても、呼び出し側が使わなくなれば素通りする。**この形の穴は P0 で3度出た**（I-3 / I-4 / L-1）。新しいテストを書くときは「これは本番経路を通っているか」を毎回自問する | I-3 / I-4 / L-1 |
| R-2 | **境界テストは両側を押さえる**。上限だけ／下限だけでは改変の片側を見逃す | I-3 |
| R-3 | **「判定不能」は PASS でなく FAIL に倒す**。静かな素通りを作らない | I-2b |
| R-4 | **導出をバイパスする経路を数える**。上書きAPIが導出を回避すれば同じ事故が起きる | I-4 / J-1 |
| R-5 | **平均のゲートだけでは分散の破壊を捕まえられない**。水準と分散は別々に見る | V-2e |
| R-6 | **一方向にしか働かない機構は、適用先ごとに意味が通るか確認する** | D-011 |
| R-7 | **較正した条件を明記する**。条件が変われば結論も変わる | §3 |
| R-8 | **実行時に逸脱を自己申告する出力**（既定から外れた設定を冒頭に表示） | P0 以来の標準 |

---

## 6. 報告様式

`REPORT_P1_[日付].md` に:

0. **M-1〜M-6 の対応**（L 便のレビューで出た積み残し）
1. 実装したもの・ファイル一覧
2. 再実行コマンド（コピペで動く形）
3. 検証結果（V-4/V-5/V-6/V-8/V-9/V-12 ＋ **P0 ゲートの再実行結果**・4シード以上）
4. **K-4 で選んだ選抜指標とその理由**（実測つき）
5. **素質開放率プレースホルダの具体値と、P3 で K の再較正が要る旨**
6. 定数の調整履歴
7. 仕様からの逸脱・解釈で埋めた箇所（**全件列挙**。ゼロなら「なし」と明記）
8. 未解決の質問

レビュー側が全コマンドを独立再実行して照合します。**モンテカルロ10万レースの所要時間も報告してください**（受け入れスイートに載せられる規模かの判断材料）。

---

## 7. 質問がある場合

実装を止めるほどの仕様の穴があれば、報告書を待たず `QUESTIONS_P1_[日付].md` に出してください。推測で正典を上書きしないでください。

**P0 では、あなたの照会が2度とも正典の実質的な改善につながりました**（V-2 の構造的問題・距離系の回帰率）。同じ姿勢でお願いします。
