# 【STAR】裁定: I-1 段 3 — 引退馬の役割を持ち主が変える — 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_I1_STEP3_ROLE_REQUEST_20260922.md`（開発側 `star-aa`・`350d75d`）
- 運用: オーナー指示「承認不要で自立的に進める」

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| R-1 | 上限をどこで数えるか | ✅ **(a) SQL に書き、試験で `OWNERSHIP_LIMITS` と突き合わせる**。★E-2（購入の 30）も同じ試験に入れる。条件 2 つ（§1） |
| R-2 | 確定の置き場所 | ✅ **(a) 同期の RPC**。裁定 Q-5 の「I-2 と同じ形」を**この便では改める**（§2） |
| Q-C | 生涯の記録に書く範囲 | ✅ **両方書く**（自動で降ろしたときも、持ち主が変えたときも） |
| Q-D | 年の途中の上げ下げ | ✅ 足りる（`bred_this_year` はそのまま） |
| Q-E | 配合の経路が無いまま段 3 を出す | ✅ **裏側は先に出してよい。★画面は `kind = 'breed'` と同じ便で出す**（§3） |
| 🔴 追加 | 購入の RPC が、上限を**利用者の行をロックする前に**数えている | 🔴 **E-2 と一緒に直す**（§4・照会に無かった点） |

---

## 1. R-1 — ✅ (a) SQL ＋ 突き合わせの試験

- RPC はクライアントから呼ばれるので、0067 のように「閾値を TS から引数で渡す」形は取れない（利用者が値を変えられる）。★**SQL に数を書くしかない**。★だから「ずれたら落ちる」試験で 2 か所を結ぶ（照会どおり）
- **条件**:
  1. ★**試験は「その関数の最新の定義」から数を抜き出す**。関数は後の移行で定義し直されることがある（`0061` → `0062` の `request_initial_breeding` が実例）。★最初の移行の字面を見ると、定義し直した側の数が古くても緑になる。→ 移行を番号順に読み、同じ関数名の**最後の `create or replace`** を対象にする（または staging の `pg_proc` の定義を読む）
  2. ★E-2（`buy_horse` の 30・`0025_horse_market.sql:117`）も同じ試験に入れる。★いまの試験（`horse-market-migration.test.ts:54`）が字面の 30 を固定しているだけなのは、照会の指摘どおり D-052 の族

## 2. R-2 — ✅ (a) 同期の RPC

- I-2 をワーカーにしたのは、配合が TS の遺伝エンジン（`breed()`）と乱数を要するから（§15.3「複雑な処理はワーカー」）。★役割の変更は、行の確認と数え上げだけで、エンジンも乱数も要らない。★§15.3 の主の形（RPC が確定する）に戻るのが自然
- ★裁定 Q-5 の「冪等の作りを I-2 と同じ形で複製してよい」は、**ワーカーで確定する前提**で書いたもの。★同期の RPC では、`attempts`・時間の予算・先頭の詰まりの扱いが**そもそも要らない**。→ ★**Q-5 をこの便について改める**（別表は残す。冪等の要求 ID も残し、再送には前の結果を返す）
- ロックの順（利用者の行 → 馬）は採る。★`buy_horse` は「出品 → 馬 → 利用者」の順（`0025:103`・`:108`・`:122`）で逆向きだが、★**対象の馬が重ならない**（購入は持ち主の居ない現役馬、役割の変更は持ち主の居る引退馬）ので、互いに待ち合って止まる形にはならない。★その理由を RPC の註記に書いておく

## 3. Q-C・Q-D・Q-E — ✅

- **Q-C 両方書く**: 生涯の記録は「その馬に何が起きたか」の帳面（§18）。★誰が起こしたかは `detail.reason`（`'lifetime_foals' | 'owner'`）で分かる。★`event_type` の check は新しい移行で書き換える（適用済みの移行は書き換えない・照会どおり）
- **Q-D**: 降ろして同じ年に上げ直しても `bred_this_year` は残るので、年 1 回の判定は抜けない。★上限の枠を空けて別の馬を上げることはできるが、得をするのは枠の入れ替えだけで、仔の数は増えない
- **Q-E**: ★裏側（表・RPC・記録・試験）は先に出してよい。★ただし**「繁殖入り」の画面は、自分で配合する経路（`kind = 'breed'`・D-107）と同じ便で出す**。★「上げたのに何も起きない」画面を利用者に見せない（照会の懸念どおり）。★画面が無い間も RPC は外から呼べるが、呼んでも役割が変わるだけで、資源は増えない（無償の何かが渡るわけではない）ので害は無い

## 4. 🔴 追加: 購入の RPC が、上限をロックの前に数えている

✔ [CODE] `0025_horse_market.sql`:
- `:115-119` で `select count(*) … where owner_id = v_user and retired_at_week is null` を数え、30 以上なら拒む
- ★利用者の行を `for update` でロックするのは、その**後**の `:122`
- → ★**同じ利用者が 2 つの購入を同時に送ると、どちらも「29 頭」と数えて両方が通り、31 頭になる**（別々の馬なので、馬の行のロックでは止まらない）

- ★照会の段 3 は「利用者の行を先にロックして、同時 2 頭の競合を止める」と正しく書いている。★同じ考えを `buy_horse` にも当てる
- **決定**: E-2 を直す移行で、`buy_horse` も**利用者の行を先にロックしてから数える**順に直す。試験は段 3 の予行と同じ「同時 2 本」を `buy_horse` でも流す
- ★本番の購入の画面は既に在る（利用者はほぼ 0）。★急ぎではないが、段 3 と同じ便で直す
