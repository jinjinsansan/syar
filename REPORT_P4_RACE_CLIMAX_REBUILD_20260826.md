# 報告 — P4 レース終盤の再構築（第4コーナー → 5頭の攻防 → 勝馬クローズアップ）

指示書: `DEV_INSTRUCTIONS_P4_RACE_CLIMAX_REBUILD_20260826.md`
ブランチ: `p4/race-30sec-cuts` / 着手時 HEAD: `8c7203d`（未コミットのまま作業）
日付: 2026-08-26

> ⚠️ ★**オーナーの目視判定を待っています。** 数字は全部そろっていますが、
> ★この案件では「テストが通る」は「見た目が合格」の代わりになりません（指示書 §9）。

---

## 0. 先に結論（何をして、何が残っているか）

| 指示書 | 状態 | 根拠 |
|---|---|---|
| §3 第4コーナー | ★素材切替 **0 回** / 左右反転 **0 回** / 素材の描かれた角度からのずれ **±11.2°**（旧 26.4°） | `tools/audit-corner-turn.mjs` `tools/audit-corner-cut-window.mjs` |
| §4 直線の攻防 | ★①②③⑤ **4 seed 全部で成立**。4 頭以上が同時に画面内 **11.4〜11.6 秒**（必要 2.0） | `tools/audit-climax-contest.mjs` |
| §4-4 カメラ | ★主役 5 頭が画面幅の **66.8〜74.4%**（要求 60〜75%）。画面内は **5〜7 頭**（全 12 頭を入れていない） | `tools/audit-climax-camera.mjs` |
| §5 勝馬の寄り | ★通過から **0.00 秒**で切替 / 馬体 **39.8%**（要求 35〜45%）/ 切れたコマ **0** / ハードカット | `tools/audit-winner-closeup.mjs` |
| §7 結果不変・決定論・連続性 | ★①〜⑧ **4 seed 全部で成立**（着順・タイム・着差・払戻・カット境界・fps 非依存） | `tools/audit-climax-invariance.mjs` |
| §7-4 通常検証 | ★`npm test` **1160 件 PASS** / `npm run typecheck` **クリーン** | 本文 §8 |
| §8 比較映像 | ★A / B / C すべて出力済み（本文 §9） | `out/2d-overhead-stride/` |
| §5-4「寄りを 1.5〜2.5 秒」 | ★**満たせません**（実測 4.57〜5.80 秒）。理由と選択肢は §10-1 | 同上 |

★**指示書の合格条件のうち、こちらで満たせないと分かっているのは §5-4 の 1 点だけ**です。
残りは全部数字で満たしています。★最終判定はオーナーの目視です。

---

## 1. 変更ファイルと `file:line`

### 1-1. 新規（表示専用の演出）

| ファイル | 何を置いたか |
|---|---|
| `packages/render/src/climax-choreography.ts` | ★最後の直線の攻防を**表示位置だけ**で作る純粋関数。`climaxDisplayPositions`（`:219`）・`climaxEnvelope`（`:105`）・役どころ表 `ROLES`（`:162`） |
| `packages/render/test/climax-choreography.test.ts` | 上の検査 14 件（結果不変・決定論・連続性・見かけの速さ・R-14 対照） |

主要な定数（すべて `climax-choreography.ts`）:

| 定数 | 値 | 意味 |
|---|---:|---|
| `CLIMAX_LEAD_COUNT` `:34` | 5 | 主役群の頭数 |
| `CLIMAX_ENTER_M` `:48` | 400m | 掛け始め ★指示書の 320m から前倒し（§10-2） |
| `CLIMAX_FULL_M` `:50` | 260m | 掛け終わり（★指示書どおり） |
| `CLIMAX_RELEASE_START_M` `:70` | 220m | 「寄せ」を戻し始める |
| `CLIMAX_SURGE_RELEASE_START_M` `:86` | 100m | 「役どころの波」を戻し始める |
| `CLIMAX_RELEASE_M` `:72` | 60m | ★**ここでオフセットは厳密に 0** |
| `CLIMAX_COMPRESS` `:74` | 0.55 | 先頭との差をどれだけ詰めて見せるか |
| `CLIMAX_MAX_OFFSET_M` `:76` | 12m | 「寄せ」の上限 |
| `CLIMAX_MAX_SURGE_M` `:177` | 3.68m | 「波」の上限（★`ROLES` から算出・二重管理を作らない） |

### 1-2. 変更（カメラと台本）

| ファイル:行 | 変更 |
|---|---|
| `packages/render/src/broadcast-v2.ts:231` | 4 角の固定カメラ `sFromSegmentEnd: 30→90` / `w: 27→22`（左右反転を消した） |
| `packages/render/src/broadcast-v2.ts:781-782` | 台本 v5 の 4 角の窓 `0.500/0.660 → 0.540/0.604`（1.33 秒） |
| `packages/render/src/broadcast-v2.ts:325` | 直線 `frameContenders: { withinM: 16, maxFovDeg: 22, fillFraction: 0.68 }` + `frameLeadGroup: true` |
| `packages/render/src/broadcast-v2.ts:361` | ★**決着 `finish-line` にも枠取りを追加** `{ withinM: 24, maxFovDeg: 26, fillFraction: 0.68 }` + `frameLeadGroup`（§10-3） |
| `packages/render/src/broadcast-v2.ts:435,437` | 勝馬の寄り `fovDeg: 12→10`（馬体 33.1%→39.8%）/ `hardCutIn: true` |
| `packages/render/src/broadcast-v2.ts:63,94` | ショット定義に `fillFraction` / `frameLeadGroup` を追加 |
| `packages/render/src/broadcast-v2.ts:792,806` | `BODY_DRAW_WIDTH_M`（馬の絵の横幅 4.104m）と `broadcastV2ContenderFov` の `fillFraction` 引数 |
| `packages/render/src/broadcast-v2-scene.ts:137,148,206,225` | `leadGates`（主役群の馬番）と `climaxCameraDisabled`（比較用の切替）を追加し、枠取りを主役群に絞る |
| `packages/render/src/broadcast-v2-scene.ts:534` | ★横縮小・素材切替を**描画から外した**（§10-4） |
| `packages/render/src/index.ts:33` | `climax-choreography.js` を公開 |

### 1-3. 変更（画面の接続部）

| ファイル:行 | 変更 |
|---|---|
| `apps/web/src/app/race/page.tsx:1564` | `climaxLeadGates`（確定着順の上位 5 頭の馬番） |
| `apps/web/src/app/race/page.tsx:1568,1586` | 表示位置 `easedAt0` / `easedAt` を**ショット解決の外へ引き上げ**（HUD と共有するため） |
| `apps/web/src/app/race/page.tsx:1658,1660` | `leadGates` / `climaxCameraDisabled` を場面解決へ渡す |
| `apps/web/src/app/race/page.tsx:1799` | `hardCutIn` のカットにはディゾルブを掛けない |
| `apps/web/src/app/race/page.tsx:2056` | ★**順位表 HUD を「画面に描いた位置」で並べる**（§10-5） |
| `apps/web/src/app/race/page.tsx:2093` | ★隊列バーも同じ位置で |
| `apps/web/src/app/race/page.tsx:2180` | ★実況も同じ位置で |
| `apps/web/src/app/race/page.tsx:158` | `WINNER_FOLLOW_SEC 4.2→2.4` |

### 1-4. 新規（監査の道具・すべて読取専用）

| ファイル | 測るもの |
|---|---|
| `tools/audit-climax-invariance.mjs` | §7-1 演出 ON/OFF で着順・タイム・着差・払戻・カット境界・fps 非依存 |
| `tools/audit-climax-camera.mjs` | §4-4 主役 5 頭が画面幅に占める割合／注視点／画角の連続性 |
| `tools/audit-climax-release.mjs` | 掛け・戻しで馬の「見かけの速さ」が本来から何 % ずれるか |
| `tools/render-climax-clip.mjs` | 撮ったコマを 1 本の動画にする（§8-A / §8-C） |

★4 本とも `tools/lib/classification.mjs` の `READONLY` に登録しました（`apps/cli/test/tool-guard.test.ts` が要求）。

---

## 2. 既存の未コミット実験をどう扱ったか（指示書 §2）

着手前に `git status --short` と `git diff`（855 行）を記録しました。
`git reset --hard` / `git checkout --` / 一括削除は**使っていません**。

| 実験 | 扱い |
|---|---|
| `broadcastV2TurnFacing` / `broadcastV2TurnSqueezeX` | ★**関数と検査は残置、描画からは呼ばない**（`broadcast-v2-scene.ts:534`）。指示書 §3-1 で不合格になった案なので、記録として残しつつ絵には出しません |
| 4 角の素材切替と横縮小 | 同上（描画経路から外れています） |
| `frameContenders { withinM: 16, maxFovDeg: 22 }` | ★**引き継いだうえで `fillFraction` と `frameLeadGroup` を足しました**（§4 の測定で不足が出たため） |
| `/race` の `TIME_GAP_SHAPE_GAMMA = 1.3` | ★**そのまま**。今回は触っていません |
| `apps/web/package.json` のポート 3210 | ★**触っていません**（オーナーの既存作業） |

⚠️ ★1 点だけ、**引き継いだ道具の値を直しました**。
`tools/lib/race-audit-build.mjs` の `CORNER_CUT_M_WEB` が **40**（実画面は **400**）、
`RUNOUT_SLOW` が **0.55**（実画面は **0.6**）でした。
★測る道具が実画面と違う条件で走っていたので、`page.tsx` の値に合わせました。

---

## 3. 第4コーナー（指示書 §3）

### 3-1. カットの時刻（seed 42・30fps）

```
17.37s  side-drive → fourth-corner-front     ★ハードカット
18.73s  fourth-corner-front → homestretch-side  ★ハードカット（view が diag-front → side で系統が違うためディゾルブは掛からない）
```

長さ **1.33 秒**（旧 3.27 秒）。

### 3-2. 素材切替・左右反転（`npx tsx tools/audit-corner-turn.mjs`）

```
ショット                 秒    向きの角度      1コマ最大の跳び   素材の入替  左右反転
start-front             12.1  168°〜168 °       0.00°/コマ         0 回       0 回
first-corner-front       2.2  158°〜158 °       0.00°/コマ         0 回       0 回
side-drive               3.0   87°〜89  °       0.47°/コマ         0 回       0 回
fourth-corner-front      1.4  155°〜176 °       0.63°/コマ       ★0 回     ★0 回
homestretch-side        19.8   86°〜88  °       0.23°/コマ         0 回       0 回
finish-line              5.0   87°〜87  °       0.00°/コマ         0 回       0 回
winner-follow            2.8   87°〜87  °       0.00°/コマ         0 回       0 回
```

★**指示書 §7-3「第4コーナー内の素材切替・左右反転を 0 回」を満たしています。**

### 3-3. なぜ反転が消えたか（★カットを詰めるだけでは消えませんでした）

固定カメラの据え位置は**カットの終わり**を基準にしています（`broadcast-v2-scene.ts:289`）。
★だからカットを詰めると**カメラごと動き**、同じ掃引を圧縮するだけでした
（実測: 3.27s→1.17s に縮めても反転は 1 回のまま）。

消えた決め手は**カメラの横位置**です（`npx tsx tools/audit-corner-camera-sweep.mjs`）:

```
横位置   カット内の向き    168°ずれ   ★反転
 27m(旧)  163.1°〜179.9°    11.9°    ★1 回
 24m      162.2°〜179.8°    11.8°     なし
★22m(新)  161.6°〜178.7°  ★10.7°     なし
```

★22m は外ラチ（走路幅 20m）の **2m 外**。それより内側はカメラを置ける場所ではありません。

### 3-4. 角度の窓（`npx tsx tools/audit-corner-cut-window.mjs`）

```
side-drive の until   4 角の長さ   カット内の向き     168°ずれ   反転
 0.500（旧）           3.27s      141.6°〜179.9°     26.4°    ★1 回
★0.540（新）          1.33s      156.8°〜178.7°   ★11.2°     なし
```

斜め前素材は α=12°（＝ viewDeg 168°）で描かれています。
★旧設定では入口 141.6° で**正しい見かけ幅の 1.8 分の 1**しかなく、ずっと正面を向いた絵でした
（＝オーナー評「斜め向いたまま曲がる」）。★いまは **±11.2°** の範囲だけを見せます。

---

## 4. 攻防演出の数式・入力・出力・連続性（指示書 §10-4）

### 4-1. 入力と出力

```
入力: { 馬番, 本来の位置 s(m), 確定着順 } × 全馬  +  { seed, コース距離, 演出 ON/OFF }
出力: { 馬番, 表示位置 s(m), オフセット(m) } × 全馬     ← ★これだけ
```

★エンジンを呼びません。★`Date.now()` / `Math.random()` を使いません（憲法4）。
★確定着順は `built.result`（エンジンの確定値）から取ります。**その瞬間の見た目の順位からは決めません**
（見た目から決めると演出が自分自身に反応して発振します）。

### 4-2. 数式

```
残り = コース距離 − その瞬間の先頭馬の位置          ★馬ごとではなく先頭で測る（位相を揃えるため）
差   = 先頭馬の位置 − その馬の位置

寄せ = min(12m, 差 × 0.55)
波   = 役の振幅 × ばらつき × 先頭余白 × bump(残り, 役の中心, 役の幅)
       bump(x, c, w) = cos²(π·|x−c| / 2w)          ★両端で値も傾きも 0
       先頭余白 = smoothstep(差 / 2.0m)             ★先頭にいる馬ほど波を効かせない

オフセット = 効き具合A(残り) × 寄せ  +  効き具合B(残り) × 波
```

効き具合（`climaxEnvelope`）:

```
             400m      260m      220m            100m      60m
効き具合A ：  0 ─smooth→ 1 ────── 1 ─────smooth───────────→ 0     （寄せ・戻し 160m）
効き具合B ：  0 ─smooth→ 1 ───────────────── 1 ─smooth───→ 0     （波・戻し 40m）
                                                          ★ここで厳密に 0
```

役どころ（`ROLES`・確定着順で決まる）:

| 着 | 波の中心 | 幅 | 振幅 | 役 |
|---:|---:|---:|---:|---|
| 1 | 120m | 90m | 2.4m | いちばん遅く上がる（差し切り・差し返し） |
| 2 | 175m | 100m | 2.8m | 先に進出して一度先頭に並ぶ |
| 3 | 140m | 90m | 3.2m | 遅れて差してくる |
| 4 | 235m | 85m | 2.2m | 早めに並んで脱落 |
| 5 | 265m | 80m | 1.8m | いちばん早く動いて先に沈む |

★`seed` と馬番から中心 ±15m・振幅 ±15% をずらすので、毎レース同じ振り付けにはなりません
（それでも同じ seed なら完全に同じ絵になります）。

### 4-3. 連続性をどう保証しているか

1. すべての区間の継ぎ目は `smoothstep`（両端で傾き 0）と `cos²` の山（両端で値も傾きも 0）でつないでいます
2. ★**上限は「効き具合を掛ける前」に掛けます。** 掛けた後に切ると、戻している間ずっと上限に張り付き、そこから一気に落ちます
3. ★**先頭馬は動かしません**（差 = 0 なので寄せが 0、先頭余白が 0 なので波も 0）
   → 場面解決は表示位置の最大値でカットを決めるので、★**演出の有無でカットの境目がずれません**（実測で不一致コマ **0**）
4. ★**「見かけの速さ」で測っています**（下の §5-3）。1 コマの移動量が大きいだけでは跳びではなく、
   ★競馬として在り得ない速さかどうかが基準です

---

## 5. seed 42 / 14 / 332 / 474 の測定結果

### 5-1. §4-3 の定量条件（`npx tsx tools/audit-climax-contest.mjs`）

| seed | ① 4 頭以上が同時に画面内 | ② 前後関係が変わった馬 | ③ 並んだ組 | ④ 1 コマ最大移動（演出あり/なし） | ④' 1 コマ最大「加速」 | ⑤ ゴール一致 |
|---:|---:|---:|---:|---|---|---|
| 42 | **11.43 秒** | 4 頭 | 3 組 | 5px / 2px | 1.6px / 1.4px | ○ 完全一致 |
| 14 | **11.37 秒** | 3 頭 | 7 組 | 4px / 3px | 2.2px / 1.0px | ○ 完全一致 |
| 332 | **11.50 秒** | 3 頭 | 3 組 | 4px / 2px | 1.3px / 0.3px | ○ 完全一致 |
| 474 | **11.60 秒** | 2 頭 | 4 組 | 4px / 2px | 1.6px / 0.6px | ○ 完全一致 |
| 要求 | ≥ 2.00 秒 | ≥ 2 頭 | ≥ 1 組 | 跳ばないこと | 同上 | 一致 |

★④は**演出なしと並べて**測っています。直線にはカメラ側の動きも元から在るので、
「跳びがある」だけでは演出のせいだと言えないためです。
★④'（二階差分＝加速）が演出なしと同程度なので、これは**跳びではなく滑らかな追い抜き**です。

★演出が作った見せ場の量:

| seed | 先頭が入れ替わって見える | 上位 5 頭の並びが真の順と違う |
|---:|---:|---:|
| 42 | 3.83 秒 | 10.37 秒 |
| 14 | 1.23 秒 | 8.80 秒 |
| 332 | 0.00 秒 | 7.50 秒 |
| 474 | 3.33 秒 | 10.90 秒 |

⚠️ ★seed 332 は**先頭の入れ替わりが 0 秒**です（2〜5 着の間では起きています）。
★役どころは確定着順から決めるので、勝馬が大差で逃げ切る展開では先頭は入れ替わりません。
★これは仕様どおりですが、オーナーが「毎レース先頭の入れ替わりが欲しい」と判断されるなら、
1 着の波の振幅（現在 2.4m）を上げれば作れます。★勝手には上げていません。

### 5-2. §4-4 カメラ（`npx tsx tools/audit-climax-camera.mjs`）

**攻防のカット（`homestretch-side`）**

| seed | 主役 5 頭の絵が画面幅に占める割合 | 画面内の総頭数 | 画角の 1 コマ最大差 | 注視点 − 主役 5 頭の中心 |
|---:|---|---:|---:|---:|
| 42 | **74.2%**（72.3〜80.1%） | 5〜6 頭 | 0.089° | 2.73m |
| 14 | **71.1%**（68.4〜76.0%） | 5 頭 | 0.073° | 0.59m |
| 332 | **66.8%**（64.3〜78.2%） | 5 頭 | 0.075° | 2.81m |
| 474 | **67.2%**（66.4〜75.0%） | 5〜7 頭 | 0.090° | 1.73m |
| 要求 | 60〜75% | 全頭入れない | 連続 | 先頭 1 頭ではない |

**決着のカット（`finish-line`）**

| seed | 割合 | 画面内の総頭数（直す前） | 画角の 1 コマ最大差 |
|---:|---|---:|---:|
| 42 | **74.4%** | 6 頭（★**2 頭**） | 0.047° |
| 14 | **68.2%** | 5〜6 頭（★**1 頭**） | 0.067° |
| 332 | **72.7%** | 5 頭（★**1 頭**） | 0.043° |
| 474 | **70.0%** | 7 頭（★**2 頭**） | 0.041° |

★カットの境目での画角の変化は **1.5〜2.3°**（`homestretch-side` 22.00° → `finish-line` 23.5〜24.3°）。
★ハードカットの場所なので跳んでよい箇所ですが、実際にはほとんど連続しています。

### 5-3. 見かけの速さ（`npx tsx tools/audit-climax-release.mjs`）

| seed | いちばん速く見えた | いちばん遅く見えた | 後退して見えたコマ |
|---:|---|---|---:|
| 42 | 本来の **1.133 倍**（残り 313m） | 本来の **0.852 倍**（残り 91m） | **0 回** |
| 14 | 1.131 倍（残り 280m） | 0.865 倍（残り 93m） | 0 回 |
| 332 | 1.128 倍（残り 298m） | 0.860 倍（残り 93m） | 0 回 |
| 474 | 1.128 倍（残り 302m） | 0.855 倍（残り 117m） | 0 回 |

★±13〜15% に収まっています。実際の競馬で、最後の 100m に馬が 1 割強落とすのは普通に起きる範囲です。

⚠️ ★**この検査は途中で追加しました。追加した理由は、最初の実装が不合格だったからです**（§7）。

---

## 6. 演出 ON/OFF で結果が一致する証拠（指示書 §7-1）

`npx tsx tools/audit-climax-invariance.mjs`

★**エンジンの値を読み直して比べていません。**
★演出 ON と OFF で**画面の全コマを 2 回歩き**、★**画面から読み取れること**だけを突き合わせています。
（読み直したら ON/OFF で同じなのは当たり前で、何も証明したことになりません。）

| 検査 | seed 42 | 14 | 332 | 474 |
|---|---|---|---|---|
| ① 画面の着順列 ON=OFF ／ エンジンの確定着順とも一致 | ○／○ | ○／○ | ○／○ | ○／○ |
| ② 各馬が決勝線を通る**表示秒** | ○ 完全一致 | ○ | ○ | ○ |
| ③ 着差ラベル | ○ | ○ | ○ | ○ |
| ④ 払戻（全 7 券種 × 当/外 = 14 枚） | ○ | ○ | ○ | ○ |
| ⑤ ゴール時の表示位置（全 12 頭） | ○ 完全一致 | ○ | ○ | ○ |
| ⑥ ★カットの境目（＝編集の時刻） | ○ 完全一致（不一致コマ **0**） | ○ | ○ | ○ |
| ⑦ 演出区間の外でオフセットが厳密に 0 | ○ | ○ | ○ | ○ |
| ⑧ 30fps と 60fps で同時刻の位置 | ○ 完全一致 | ○ | ○ | ○ |

seed 42 の着差ラベル（ON/OFF で完全一致）:
`2馬身 2馬身 ハナ 1/2馬身 3/4馬身 3/4馬身 1/2馬身 1/2馬身 3馬身 アタマ 1馬身`

★**払戻は `settle()` に通して比べています。** 券種の取りこぼしを作らないよう `TICKET_KINDS` から回し、
★当たり券だけだと全部 350 で差が出ないので、**外れ券（下位から組んだ買い目）も同数入れています**。

★カット表（seed 42・ON/OFF 完全一致）:
```
12.13s start-front→first-corner-front
14.37s first-corner-front→side-drive
17.37s side-drive→fourth-corner-front
18.73s fourth-corner-front→homestretch-side
38.50s homestretch-side→finish-line
43.50s finish-line→winner-follow
```

---

## 7. 勝馬クローズアップ（指示書 §5）

`npx tsx tools/audit-winner-closeup.mjs`

| seed | 勝馬 | 通過→切替 | 続く長さ | 馬体が画面高の | 画面外に切れたコマ | 入り方 |
|---:|---:|---:|---|---:|---:|---|
| 42 | 10 | **0.00 秒** | 5.23 秒（走り抜け 2.82 ＋ 保持 2.4） | **39.8%** | 0 / 157 | ハードカット |
| 14 | 6 | 0.00 秒 | 5.80 秒（3.38 ＋ 2.4） | 39.8% | 0 / 174 | ハードカット |
| 332 | 7 | 0.00 秒 | 4.57 秒（2.14 ＋ 2.4） | 39.8% | 0 / 137 | ハードカット |
| 474 | 1 | 0.00 秒 | 5.17 秒（2.75 ＋ 2.4） | 39.8% | 0 / 155 | ハードカット |
| 要求 | — | 0.0〜0.4 秒 | ★1.5〜2.5 秒 | 35〜45% | 切らない | ハードカット |

★**「定義が残っているだけ」ではありません。** 実画面から撮った動画に収録されています（§9-A）。

⚠️ ★**長さだけ要求を超えています。** 理由は §10-1。

★指示書 §5-3 の「約 35%」というコメントは**実測すると 33.1%** で、要求の下限を下回っていました。
画角 12°→10° にして **39.8%**（35〜45% のまん中）にしています。★カメラの位置は動かしていません。

---

## 8. `npm test` / `npm run typecheck`

```
> npm run typecheck
> tsc --noEmit && tsc --noEmit -p apps/web
（出力なし＝クリーン）

> npx vitest run
 Test Files  117 passed (117)
      Tests  1160 passed (1160)
   Duration  192.53s
```

★build は行っていません（指示書 §6「起動中の dev サーバーに対する無断 build」を避けるため）。

⚠️ ★作業の途中で dev サーバー（ポート 3210）が落ちていたので、
★**撮影のために `apps/web` の `npm run dev` を同じポートで起動しました**（現在も起動中）。
★止める・別ポートに移すなどのご希望があればお知らせください。

---

## 9. 出力動画のパス（指示書 §8）

すべて **seed 42 は 17.00〜49.00 秒**、**seed 違いは 18.50〜47.00 秒**、**30fps**、
★**実画面（`/race`）から取り込んだコマ**です（オフライン描画ではありません）。

### A. 全体版

```
out/2d-overhead-stride/climax-after.mp4      （961 コマ / 32.0 秒 / 34MB）
```
第4コーナーの手前 → 4 角 → 直線の攻防 → ゴール → 勝馬クローズアップの終わりまで。

### B. 直線の新旧並列比較

```
out/2d-overhead-stride/climax-on-off.mp4     （17MB）
out/2d-overhead-stride/climax-on-off.gif     （65MB）
```
左 = **修正前**（`/race?climax=off`）／ 右 = **修正後**。同一 seed・同一時刻・同一尺。

⚠️ ★`climax=off` は**演出とカメラの直しの両方**を切ります（`climaxCameraDisabled`）。
★片方だけ切ると「何と何を比べているのか言えなく」なるためです。
★**4 角の直し（カメラの据え直しとカットの窓）は台本側なので、左右どちらにも入っています。**
　4 角の新旧比較は既存の `out/2d-overhead-stride/corner4-flip-fix.mp4` をご覧ください。

⚠️ ★指示書は「4 角からゴール後 2.5 秒まで」ですが、**勝馬クローズアップの終わりまで**入れています
（尺が長い分には判定の妨げにならないと判断しました）。

### C. seed 違い

```
out/2d-overhead-stride/climax-seed14.mp4     （856 コマ / 28.5 秒 / 41MB）
out/2d-overhead-stride/climax-seed332.mp4    （29MB）
out/2d-overhead-stride/climax-seed474.mp4    （29MB）
```

★参考: 撮ったコマそのものは `out/2d-overhead-stride/{climax-after, climax-off, climax-seed14, climax-seed332, climax-seed474}/` にあります。

⚠️ ★撮影の道具は**1 コマ単位では決定論ではありません**（同条件で 2 回撮ると、カットの境目付近で
一部のコマが別のバイト列になります・平均画素差 3〜13/255）。
★**「バイト一致しない＝絵が違う」と読まないでください。** ショットや画角が同じかどうかは
`auditSceneAt` の値（決定論）で判定しています。

---

## 10. 指示から逸脱した判断（指示書 §10-10）

### 10-1. ★勝馬の寄りが「1.5〜2.5 秒」に収まりません（§5-4）

実測 **4.57〜5.80 秒**。内訳は「他馬がゴールし終えるまでの**走り抜け 2.14〜3.38 秒**」＋「寄りの保持 2.4 秒」。

★**保持を 0 にしても走り抜けの 2.14〜3.38 秒が残ります。** これは
`winner-follow` が「勝馬が決勝線を通った瞬間」から始まり（＝ §5-1 の「0.0〜0.4 秒以内」を満たすため）、
その間ずっと勝馬を寄りで追い続けるからです。

★§9 のチェックリストは「1.5 秒以上」なので**満たしています**が、§5-4 の上限は超えています。
選択肢は 3 つで、★**どれもオーナー判断が要るので勝手に選びませんでした**:

1. **今のまま**（＝レースが終わり切るまで勝馬を映す。走り抜けも見せ場と考える）
2. `WINNER_FOLLOW_SEC` を 2.4 → 0 にする（★それでも 2.1〜3.4 秒。上限は超えたまま）
3. ゴール後の流し（`RUNOUT_SLOW`）を速める（★指示書 §6 が禁じる「レース全体の時間圧縮の再設計」に近づくので触っていません）

### 10-2. ★演出の掛け始めを 320m → 400m へ前倒しました（§4-2 の表）

指示書の表は「320〜260m で主役群を集める」です。★**集め終わる地点（260m）は表のとおり**にして、
★**掛け始めだけ**を 400m へ動かしました。

理由は測定です。320m から掛けると、実測で残り 282〜290m の馬が
★**本来の 1.15〜1.33 倍**の速さに見えていました（15.6m/s が 20.8m/s に見える＝早送り）。
これは式で決まります: オフセットを長さ L で掛けるとき傾きは最大 **1.5/L**、
見かけの速さのずれ ＝ **（掛ける量）× 1.5 / L**。

```
12m を  60m（320→260）で掛ける → 0.30 ＝ +30%   ← 実測と一致
12m を 140m（400→260）で掛ける → 0.13 ＝ +13%
```

★400m はまだ `homestretch-side` のカットの中で、4 角のカットには掛かりません。

### 10-3. ★決着のカット `finish-line` にも枠取りを足しました（指示書に無い場所）

★**実画面を見て見つけました。** `homestretch-side` の構図を直しただけでは、
★**最後の 96m（約 5 秒）が直っていませんでした。** そのカットには枠取りが付いておらず、
実測で**画面内 1〜2 頭**、主役 5 頭の絵が画面幅の **40.4 / 95.3 / 95.8 / 82.9%**（★最大 103.1% ＝端が切れる）。

★つまりオーナー評「最後の直線で最後 2 頭が走って、ただ前の馬が勝つだけ」は
★**決着の 5 秒間にそのまま残っていました。**

⚠️ ★ここでカメラを引くのは「攻防を作るため」ではありません。
★残り 60m で表示オフセットは **0** なので（§1）、決着のカットでは主役 5 頭は**本来どおり 17〜21m に伸びています**。
★演出では詰められないので、★**もう起きている決着を画面外に切らないため**に画角で入れます。
（指示書 §4-1 が禁じているのは、引くことで攻防を作ったことにする代用です。）

★代償: 上限画角 26° のとき馬は画面高の **12.2%**（参考映像は 14.1%、直前の直線は 17% 前後）。
★寄りの見せ場は直後の `winner-follow`（39.8%）が受け持ちます。
★**小さすぎるとご判断されるなら、`broadcast-v2.ts:361` の `maxFovDeg` を下げれば戻せます**（頭数と引き換え）。

### 10-4. ★4 角の「素材切替 + 横縮小」は関数を残したまま描画から外しました

指示書 §3-1 で不合格になった案です。★**削除ではなく残置**にしたのは、
`packages/render/test/turn-facing.test.ts` / `turn-squeeze.test.ts` が測っている内容が
「なぜ駄目だったか」の記録として価値があるためです。★描画からは呼んでいません。

### 10-5. ★順位表 HUD・隊列バー・実況の並べ方を変えました（指示書に無い場所）

★**これも実画面を見て見つけました。**
順位表・馬名プレート・実況は `at`＝**エンジンの真の位置**で並んでいました。
演出は**画面の前後関係**を変えるので、実測（4 seed）で

- ★**「絵では 4 番が先頭なのに、順位表では 10 番が先頭」が 1.2〜3.8 秒**
- ★上位 5 頭の並びの食い違いが **7.5〜10.9 秒**

★絵と数字が食い違うのは、どちらか一方が間違っているより悪い状態です。
→ HUD・隊列バー・実況を、★**画面に描いたのと同じ位置**で並べるようにしました。

⚠️ ★**ゴールしたかどうかは今もエンジンの真の位置で判定しています**（`page.tsx:2045`）。
表示位置は発走イージングのぶん全馬が一律に後ろへずれているので、
表示位置で見ると「誰もゴールしていない」ことになるためです。
★ゴールした馬は**確定着順**で並ぶので、★**決着した瞬間から表は正しい**ままです。

⚠️ ★残した食い違いが 1 つあります。**モーションブラーの速度**（`page.tsx:1757`）は
エンジンの真の速度のままです。演出による見かけの速さのずれ（±13〜15%）は反映されません。
★ブラーは控えめな効果で、絵の前後関係には効かないので、今回は触っていません。

### 10-6. ★引き継いだ道具の値を直しました

`tools/lib/race-audit-build.mjs` が実画面と違う条件で走っていました（§2 の末尾）。
★**測る側が間違っていると、直したかどうかが判定できません。**

---

## 11. ★こちらから見た残りの弱点（隠さずに書きます）

1. ★**主役 5 頭の帯が画面の中央に来ていません。** 中心が画面中央から **−3〜−22%**（左寄り）です。
   `leadFraction`（先頭馬を画面の何割の位置に置くか）が 0.45〜0.78 で、★**先頭の前に余白を取る**
   中継の作法によります。★指示書 §4-4 の「中央 60〜75% に置く」の「中央」を厳密に取るなら
   `leadFraction` を上げれば直りますが、★**それはすでにオーナー承認済みの決着の構図を変えます**。
   ★一度に複数の要因を変えたくないので、数字を出すだけにしています。

2. ★**seed 332 は先頭の入れ替わりが起きません**（§5-1）。役どころは確定着順で決まるので、
   勝馬が大差で逃げ切る展開では作られません。

3. ★**`homestretch-side` の画角が終盤に上限 22° へ張り付きます**（seed 42 / 332 / 474）。
   隊列が伸びるほど引きたいのに止まるので、絵が少しずつ大きくなります。
   ★`finish-line` は 26° まで引けるので、カットの境目で 1.5〜2.3° の段差が出ます（ハードカットの場所）。
   ★揃えるなら `homestretch-side` も 26° にできますが、馬がさらに小さくなります。

4. ★**画面の隅の HUD が馬に重なることがあります。** 左下の `COURSE` 表示の裏に
   後方の馬が入る場面が残っています（以前から在る事象）。★今回の測定は
   「キャンバス内にいるか」で数えており、★**HUD に隠れているかは数えていません。**

---

## 12. 再現手順

```powershell
# 検証
npm test
npm run typecheck

# 測定（すべて読取専用・標準出力のみ）
npx tsx tools/audit-climax-contest.mjs        # §4-3 攻防の定量条件
npx tsx tools/audit-climax-invariance.mjs     # §7-1 結果不変・決定論
npx tsx tools/audit-climax-camera.mjs         # §4-4 攻防のカットの構図
npx tsx tools/audit-climax-camera.mjs --shot finish-line   # 決着のカットの構図
npx tsx tools/audit-climax-release.mjs        # 見かけの速さ
npx tsx tools/audit-winner-closeup.mjs        # §5 勝馬クローズアップ
npx tsx tools/audit-corner-turn.mjs           # §3 素材切替・左右反転
npx tsx tools/audit-corner-cut-window.mjs     # §3 角度の窓

# 撮影（dev サーバーが :3210 で起動していること）
$env:AUDIT_SHOT="fourth-corner-front"; $env:AUDIT_SEED="42"; $env:AUDIT_FROM="17.0"; $env:AUDIT_TO="49.0"
npx tsx tools/capture-overhead-stride.mjs --label climax-after
$env:AUDIT_QUERY="&cinematography=v5&climax=off"
npx tsx tools/capture-overhead-stride.mjs --label climax-off
npx tsx tools/render-climax-compare.mjs                       # B
npx tsx tools/render-climax-clip.mjs --label climax-after     # A

# 画面で見る
# http://localhost:3210/race                 … 現行（演出あり）
# http://localhost:3210/race?climax=off      … 修正前（演出とカメラの直しを両方切る）
```

---

★**オーナーの確認を待ちます。** 「完全解決」「採用確定」とは書きません。
★見た目で駄目な点があれば、どのカットの何秒でどう見えたかをお知らせください。
数字はすべて再現できる形で残してあります。
