# 裁定 — レース演出「真横の直線だけ」（台本 v9）と右回りの版

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-15 ／ レビュー側 → 開発側・オーナー
**対象**: `REPORT_RACE_SIDE_ONLY_20260914.md` ／ コミット `99f3774`・`57adf77`（起点 `319bae0`）／ タグ `p4-race-side-only-right-turn-20260915`
**前の文書**: `REVIEW_CONSULT_RACE_SIDE_ONLY_ANSWER_20260914.md` ／ `REVIEW_CONSULT_RACE_SIDE_ONLY_FOLLOWUP_ANSWER_20260914.md` ／ `SYNTHESIS_RACE_SIDE_ONLY_20260914.md`

> 凡例（R-13）: **✔** ＝ レビュー側が実行するかコードを読んで確かめた ／ **△** ＝ 未確認 ／ **[EYES]** ＝ オーナーの目で決めること
> ⚠️ 本便は**指示書の外**で、オーナーの直接の指示に基づいて実装されています。レビュー側はそれを前提に照合しました。

## 判定

| # | 照合の対象 | 判定 |
|---|---|---|
| R1 | ゴール前のカメラが結果を読んでいない | **PASS** |
| R2 | `leadGates` の置き換えで画角が跳ばないか | **PASS**（既定の画には効かない・§2-2） |
| R3 | 見せる区間がどの鞍でもコーナーに掛からない ／ 介入する人は残り 900m から飛ばさない | **PASS** |
| R4 | 写真判定の止め方と判定 | **CONDITIONAL**（§3-1・§3-2） |
| R5 | 勢いの入力 | **PASS**（既定では問題なし・§2-5） |
| R6 | 右回りの走路の直し | **CONDITIONAL**（直し方は正しい。検査が足りない・§3-3） |
| R7 | 服の塗り方の変更 | **CONDITIONAL**（計測が要る・§3-4） |
| R8 | 意図して書き換えた検査 | **PASS** |
| R9 | 全体のテスト | **PASS**（§1） |
| **総合** | | **CONDITIONAL PASS** |

報告 §0 の照合表は、場所と確かめ方が揃っていて照合がとても速く済みました。特に、**照合の途中で `leadGates` が確定着順から作られていたのを自分で見つけて直した**点（報告 §5-2）は、レビュー側の回答が見落としていたものです。

---

## 1. レビュー側の再実行 ✔

| # | 内容 | 結果 |
|---|---|---|
| 1 | 報告の R1・R3・R4・R6・R7・R8 の検査と V-18 のゲート（14 ファイル） | ✔ `Test Files 14 passed` ／ `Tests 100 passed` ／ EXIT=0 |
| 2 | 全体 `npx vitest run --maxWorkers=2`（HEAD `57adf77`・02:29〜02:33） | ✔ `Test Files 1 failed \| 162 passed (163)` ／ `Tests 1 failed \| 1684 passed (1685)` ／ EXIT=1。赤は変更前からの `edit-grammar-audit` ⑨ だけ |
| 3 | 変異の実演 | **未実行**。作業ツリーのファイルを書き換える実験は行っていない（別のターミナルの作業を考慮） |

→ **R9 は閉じます。** 報告の「1677 件中 3 失敗」から、直した 2 件が通り、件数は 1685 件です。

---

## 2. 照合で確かめたこと

### 2-1. R1 ✔
- 画面の `resolveBroadcastV2Scene` 2 か所は `development` を渡さず、`finishChase: built.finishChaseAt(sec)` を渡す（`page.tsx:3899`・`page.tsx:1925`）
- `finishChaseTable` は、刻み i の区間で標本 i−1 と i だけを使い、**渡したレース秒より未来の標本を読まない**（`broadcast-v2.ts` `finishChaseTable`）
- `finishChaseOf` の入力は、いまの位置と窓の前の位置だけ。先頭は各時刻の最大値で取るので、先頭が入れ替わるコマで跳ばない
- 反実仮想の検査（分かれる時刻まで同じ位置・1 着だけ違う 2 レース）は、**分かれた後に値が違うことも対照として確かめている**ので空回りしない
- 構文木の検査は、変異 3 つ（札に戻す・上位 5 頭を確定着順に戻す・流れの表だけ確定着順を渡す）を捕まえる
- 監査道具 `tools/lib/race-audit-build.mjs` も同じ関数・同じ入力に揃えた（R-30）。台本を省いたときは画面の既定で判定する（R-31）

### 2-2. R2 ✔ — 既定の画には効かない
- 枠取りは `shot.frameLeadGroup !== true || options.climaxCameraDisabled === true` のとき、`leadGates` を使わず全馬を収める（`broadcast-v2-scene.ts:313-316`）
- 既定は `?climax=on` でないので `climaxCameraDisabled` が真（`page.tsx:3759`）→ **既定の v9 では `leadGates` は枠取りに効かない**
- 競り合いの注視点（`focusContest`）を持つのは `straight-contest` だけで、v9 の台本に無い
- `climax=on` のときも、枠取りと注視点は収める相手の**位置の並び**だけで決まり、5 番手と 6 番手が入れ替わる瞬間は 2 頭が同じ位置にいるので値は連続。**計測は不要**です

### 2-3. R3 ✔
- 検査 `side-only-script.test.ts` は、画面と同じ引数（`startShownM = course.segments[0].length`・`homeStretchM`・`keepFromMetersLeft`）で組み、**全 50 鞍**を見ている（R-33）
- 介入する人の見せ方では、飛ばす区間 ∩［残り 900m, ゴール］＝ 空。**対照として観戦の見せ方では同じ区間を飛ばしている**ことも確かめている

### 2-4. R4 の判定式 ✔（止め方は §3-1）
- 判定は、勝ち馬が決勝線を通る瞬間の位置モデルで 1〜2 着の差を取り、2 番手のその瞬間の速さで秒に直し、`marginLabel` で言葉にする（`page.tsx:1849-1861`・`race-climax-hud.ts` `photoFinishOf`）。再照会の回答の (c) です
- 止める絵の位置は `withFinishRunOut`・発走の遅れ・（無効の）攻防の演出を通りますが、ゴールの瞬間では位置モデルと同じ値になります

### 2-5. R5 ✔ — 既定では問題なし
- 「いま」は描いている位置（`easedAt`）、「0.5 秒前」は位置モデル
- 2 つの差は発走の遅れ（`startShownMeters`）だけで、**発走の区間を過ぎると全馬に共通の一定値**（`broadcast-v2.ts:2556`「ramp を過ぎたら遅れは一定」）
- `momentumLevels` は**全馬の中央の速さとの差**で段を切るので、共通の一定値は打ち消されます
- ⚠️ ただし感度は高いです。段の境目 0.2 m/s は、窓 0.5 秒で **0.1m の位置のずれ**に当たります。今後、馬ごとに違うずれを描く位置に足すと、段が黙って変わります → **次に触るときは、2 つを同じ関数から取る**ことを推奨します（急がない）

### 2-6. R6 の直し方 ✔
- 直線の区間に `turn` が無く、`posOf` が直線を常に左回りとして扱っていた ／ コーナーの半径が `R + off × sgn` で、右回りだけ逆向き → 両方とも `w` が大きいほど外、に揃えた
- エンジンの距離ロス（`@star/race-engine` の `laneExtraM`）は回りを見ないので、**着順・走破タイムには効かない**
- **V-18 の測定器（`tools/lib/v18.mjs`）はエンジンの `laneExtraM` を読む**ので、今回の直しは V-18 に効かない（V-18 のゲートも §1 で通過）

### 2-7. R8 ✔
既定を v8 → v9 に変えたことで、範囲や台本を暗黙に既定から取っていた検査が別のカットを走査し始める、は D-085 の系そのものです。台本の明示と、「既定（v9）では 4 角正面が一度も選ばれない」の対照が揃っています。

---

## 3. 条件（次の便で直すもの）

### 3-1. ⚠️ R4 止め絵を放すとき、覆われていない跳びが出る

- 止め絵のあいだは表示秒を `photo.atDisplaySec` に止め、0.4 秒後に**実際の表示秒へ戻る**（`page.tsx:3617-3621`）
- 一方、v9 はゴール通過の画を保持します: `goalHeld = (cutScript || splitStraightScript) && winnerFinishedNow && winnerAfterSec < GOAL_HOLD_SEC`、**`GOAL_HOLD_SEC = 1.0`**（`page.tsx:939`・`page.tsx:3822-3824`）
- → 止め絵を放す 0.4 秒の時点は**まだ同じ `finish-line` のカット**です。放した瞬間に、**同じカットの中で表示が 0.4 秒（1600m の平均速度 約 16 m/s で約 6m）先へ跳びます**
- これは `race-elision.ts` 冒頭の規則「**跳びは覆われている所にしか置いてはいけない。裸の跳びは馬が瞬間移動したように見える**」に当たります。報告 §5-3 の「止め絵のあとに切り替わる」は、コードとは合っていません

**直し方（どちらか）**
- (a) 止めた時刻から続けて再生する（止めた 0.4 秒ぶん、以後の表示を遅らせる。倍速にはしない）
- (b) 放す瞬間をカットで覆う

**検査**: 止め絵の前後で、同じカットの中の表示秒の跳びが 0 であること（覆われているなら、その覆いがあること）。変異として、今の形に戻すと落ちること。

### 3-2. ⚠️ R4 止め絵を出す判定と、止め絵に書く言葉が別の入力

- 出すかどうか: 描いている位置の差 ÷ 2 番手の速さ → `marginLabel`（(c)）
- 書く言葉: 着順ボードの `result.order[1].marginLabel`（エンジンの秒差）
- 2 つが食い違うと「**写真判定 1/2 馬身差**」が出えます

**再照会の回答 §3-3 の検査 1〜3 がまだ入っていません。**
1. 判定関数が止める絵を描くのと同じ位置を読むこと（経路の検査）
2. 多数の seed で (c) と (b) の判定が食い違う件数を出し、報告に書く（合否の線は引かない）
3. 止め絵が出たレースでは、着順ボードの 1〜2 着の着差ラベルがクビ以内であること。食い違いがあれば件数と seed

### 3-3. ⚠️ R6 直す前の誤りを、既存の検査は原理的に捕まえられなかった

- `lane-geometry.test.ts` は「エンジン」と描画の `laneExtraMeters` を突き合わせる。ところが**描画の `laneExtraMeters` は元から回りを見ない正しい式**（`course.ts:236-252` `(w − wCentre) × dθ`）で、`laneRatioOf` を通らない
- 誤っていたのは `laneRatioOf` と、それを使う **`laneArcLengthAt`**。これは注視点を走線に沿った長さで置く処理（D-089 の (b′)・`broadcast-v2-scene.ts:597`）が使っている → **右回りの鞍では、直す前はこの注視点もずれていた**ことになる（大きさは △ 未計測）
- `lane-arc-length.test.ts` は `turn: 'left'` しか試していない
- 映像の不変条件の検査（`race-video-invariants.test.ts`）も、監査道具の既定（`turn ?? 'left'`・`race-audit-build.mjs:89`）で**左回りしか測っていない**

**足すもの**
1. **左右どちらの回りでも** `laneArcLengthAt(course, s, w) − s` ＝ `laneExtraMeters(course, 0, s, w)`（**描画の中の 2 つの関数の一致**・D-052）。**変異**: `laneRatioOf` を `R + off × sgn` に戻すと右回りで落ちること
2. `race-video-invariants.test.ts` に、**右回りの鞍を 1 つ**加える（先頭が画面の外 0% ／ 共通の被写体 0 頭の件数 0）（R-33）
3. [EYES] 右回りの 4 場（青嶺・潮風・月見丘・霧ヶ原）の既定の画が変わる。背景の板を反転していない件と合わせて、オーナーの確認

### 3-4. ⚠️ R7 塊を 1 色に揃える処理で、上着が枠色に塗られるおそれ

- 塗り分けは `region` の **1 ＝ 枠色（帽子・鞍布）／ 2 ＝ 上着**（`page.tsx:1106`・`page.tsx:1171`）
- つながった塊の中で**多い方の色に揃える**（`page.tsx:1200-1217`）
- **上着と鞍布が黒い線なしでつながる絵やコマがあると、少ない側の上着ごと枠色になり、馬を見分ける上着の色が消えます**（正典 §12.1「個体識別は勝負服の配色で行う」）。柄を無地にしたので、上着の色が唯一の見分けです
- 報告の確認は開発サーバーの 3 場面の目視

**計測（報告のみ・線は引かない）**: 使っている全ての絵とコマ（真横 `side-v8`/`v8b` の 8 コマ、発走のゲートの前から、勝ち馬の追従など）で、**1 と 2 が混ざった塊の数と、塊ごとの少ない側の割合**。少ない側が大きい塊があれば、その絵・コマ・場所。

---

## 4. レビュー側の訂正 — 再照会の回答 §1-1 の線が広すぎた

再照会の回答 §1-1 に「未来の値（確定着順・1 着の馬番・着差・**ゴール時刻**）を入力に取らない」と書きました。

- 音の「溜め」は、勝ち馬が決勝線を通る表示秒（`crossD`・`page.tsx:3641`）の 0.15 秒前に音を抜きます。字義どおりには「ゴール時刻」を読んでいます
- しかし、**誰が・どの型で・どれだけの差で勝つかは明かしません**。最初に線を越える馬は定義から勝ち馬で、その 0.15 秒前は先頭が線の約 2.4m 手前にいる、画面で見えている状態です。しかも介入の締切（ゴール 0.5 秒前・§8b.4）の後です

→ **線を次のように書き直します。** 「溜め」は許容します。

> **ゴールより前の画・音・HUD は、誰が・どの型で・どれだけの差で勝つかを読む値（確定着順・1 着の馬番・展開の札・着差）を入力に取らない。時刻 t までに描かれる状態から決める。**

同じ理由で、写真判定の止め絵を**ゴールの瞬間に**出すことも許容です（§2-4）。

---

## 5. 判断を求められた 3 件

### 5-1. 実況の着差の言葉を、着順ボードに揃える（O-9）

**揃えます。** 実況（`race-call.ts` `marginWord`・馬身のしきい値）を、`photoFinishOf` と同じ「差（m）÷ 速さ → `marginLabel`」で言葉にする形へ置き換えてください。言葉を決める場所をエンジンの `marginLabel` 1 か所にします（D-052）。
- 速さの取り方は `photoFinishOf` と同じ関数から取る（2 か所で別の速さを使わない）
- 検査: 実況と着順ボードで、同じ差に対して同じ言葉が出ること。変異として、馬身のしきい値に戻すと落ちること

### 5-2. 正典への起票（D-097・D-098）— 文面案

オーナーが明示的に決めたのは O-1 と O-7 です（`SYNTHESIS_RACE_SIDE_ONLY_20260914.md` §2 冒頭の原文）。**正典への書き込みは、オーナーに下の文面を見ていただいてから行います。**

**D-097（案）**
> **レース映像の見せ方を「観戦のみ」と「自馬で介入する人」で分ける**（オーナー判断 O-1・2026-09-14・`REVIEW_CONSULT_RACE_SIDE_ONLY_ANSWER_20260914.md` §0）。①**観戦のみ**は、発走と最後の直線を真横で見せ、その間（コーナーを含む）は時計から取り除いてよい。跳びはコース図の画面で覆う。②**自馬で介入する人**は、**残り `EARLY_SPURT_METER`（900m）→ ゴールを時計から取り除かない**。その間のコーナーはコース図の画面で覆ってよい。**理由**: 1600m で飛ばす区間（残り 1400m→250m）に §8b の「仕掛け」「追う」の局面と仕掛けの最適点（残り 495m・`intervention.ts:393`）が丸ごと入り、D-066 の「介入の瞬間を消すのは演出の選択ではなくシステムの削除」に当たるため。**§14.5 が既に 2 者で配信を分けている**のと同じ線。**検査**: `apps/cli/test/side-only-script.test.ts` ④（全鞍・飛ばす区間 ∩［残り 900m, ゴール］＝ 空）。⚠️ 介入の画面を作る便では、コース図で覆っている間も、スタミナゲージなど介入の判断に要る状態が見えていること（D-070）を確かめる

**D-098（案）**
> **ゴールより前の画・音・HUD は、誰が・どの型で・どれだけの差で勝つかを読む値（確定着順・1 着の馬番・展開の札・着差）を入力に取らない。時刻 t までに描かれる状態から決める**（`REVIEW_CONSULT_RACE_SIDE_ONLY_FOLLOWUP_ANSWER_20260914.md` §1・`REVIEW_RACE_SIDE_ONLY_VERDICT_20260915.md` §4）。**文字・画・音の区別なく同じ扱い**（見出しだけでなく、構図・音でも毎レース見れば覚えられる。旧 `FINISH_CAMERA_BY_DEVELOPMENT` は先頭の置き場所を最大 256px 変えていた）。**ゴールの後**（勝ち馬の追従・写真判定の止め絵・見出し・リプレイ・着順ボード）は使ってよい。最初の馬が線を越える時刻に合わせた音の「溜め」は、勝ち馬を明かさないので可。**2026-09-12 のオーナー指示「追い込み馬がある時は引く」は、状態（いま後ろから伸びてくる馬がいる時）として読む**（オーナー判断 O-7・2026-09-14）。**理由**: D-063「漏らす演出か、劇になる演出か」の適用。加えて、介入する人のクライアントは締切まで着順を持たない（§14.5）ので、結果から決める構図はそもそも作れない。**検査**: `packages/render/test/finish-chase.test.ts`（反実仮想）・`apps/cli/test/finish-chase-wiring.test.ts`（構文木・変異 3）

### 5-3. 計画書の「カット数は減らさない」

正典の決定ではない（計画書 `PLAN_P4_RACE_COMPLETION_20260910.md` §1・`RESIDUALS_P4_RACE_VIDEO.md:108`）ので、**D 番号にはしません**。計画書の「実施記録」に、取り消しと理由・代わりに守るものを追記しました（2026-09-15・本文の §1・§8 は書き換えていない）。

---

## 6. 開発側への事務連絡

- 報告書 `REPORT_RACE_SIDE_ONLY_20260914.md` の §0 の追記と終了 HEAD の修正が**未コミット**です。ファイルを名指ししてコミットしてください（`git add -A` を使わない）
- 計画書 `PLAN_P4_RACE_COMPLETION_20260910.md` にレビュー側が追記しました（未コミット）。コミットに含めるかは、オーナーの指示に従ってください
- 正典 `STAR_SPEC_v2.0.md` にはレビュー側の未コミットの変更があります。**コミットに混ぜないでください**

## 7. 次の便に入れるもの（予告）

1. §3-1 止め絵を放すときの跳び
2. §3-2 写真判定の検査 1〜3
3. §3-3 描画の中の 2 つの関数の一致（左右）・不変条件に右回りを 1 鞍
4. §3-4 服の塊の計測
5. §5-1 実況の着差の言葉を `marginLabel` へ
6. （急がない）§2-5 勢いの 2 つの入力を同じ関数から

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
