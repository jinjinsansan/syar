# 裁定 — 開発期間中の認証をメール＋パスワードにする（Q-AUTH-08・Q-AUTH-09）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-18 ／ レビュー側 → 開発側・オーナー
**対象**: 渡し状 `HANDOFF_AUTH_TO_REVIEW_20260918.md`（`839365e`）／ 照会 `QUESTIONS_AUTH_EMAIL_PASSWORD_20260918.md`（`e5c4b8d`）
**関連**: D-075・D-076・D-077・D-078・D-080・§4.1・§13.2 V-19・§15.6 ／ `db/migrations/0017_user_identities.sql`・`tools/verify-v19-db.mjs`

> 凡例（R-13）: **✔** ＝ レビュー側がコード・正典を読んで確かめた ／ **△** ＝ 未確認（確かめる担当を書く）
> ★**本書を書いた時点で、正典 `STAR_SPEC_v2.0.md` はまだ変えていません。** D-113 の起票・D-076 への追記・V-19 の書き換えは、案文をオーナーに見せてから行います（§7）。

---

## 0. 判定

**CONDITIONAL PASS** — ★D-113 案の骨格（**D-076 を撤回せず、開発期間中の当座の経路として並置する**）を受け入れる。**条件は §1 の C-1〜C-3。**

| # | 照会 | 裁定 |
|---|---|---|
| **Q-AUTH-08-a** | D-113 案文でよいか（並置） | ✅ **並置を採る。** ただし ★**条件 ① の「開発期間中」の終わり方を改める**（C-1）・★**開発中の登録を招待制／許可リストに限る**（C-2） |
| **Q-AUTH-08-b** | `user_identities` に `provider='email'` の行を作るか | ✅ **作らない（開発側の案 (a)）**（§2） |
| **Q-AUTH-08-c** | Edge Function に置くか（D-076 ④） | ✅ **置かない**（開発側の見解どおり。LINE を足す便で ④ は復活する） |
| **Q-AUTH-08-d** | 再設定メールの送信 | ✅ **開発中は Supabase の既定でよい。人を入れる前に SMTP を手配**（オーナー手配事項） |
| **Q-AUTH-09-a** | 空振りを「経路対象外」と明記するか | ✅ **明記する。** ★ただし **数が合っていない**・★**①③⑯ は対象外にせず読み替える**（§3-1） |
| **Q-AUTH-09-b** | ⑥ を ⑥-oidc／⑥-email に分けるか | ✅ **分ける**（§3-2・★自動結合に注意） |
| **Q-AUTH-09-c** | E-1〜E-7 でよいか | ✅ **採る。E-3 と E-6 を直す**（§3-3・C-3） |
| **Q-AUTH-09-d** | 「16項目」の数え方 | ✅ **「経路ごとに、その経路で有効な項目すべて」**に改める |
| 渡し状 §8 | 本番ワーカーも入れ替わったはず | 🔴 **[CODE] と合わない**（§4） |
| 渡し状 §9-1 | V-10 の `place` が系統的に −1.26pt | ⚠️ **直す前の記録**（§5）。★人を迎える便のブロッキング条件にすることは**受け入れる** |
| 渡し状 §9-2 | `/signup`・`/login` が旧テーマ | ✅ **見た目だけ当座で合わせてよい**（中身は「準備中」のまま・入力欄を作らない）。★`/design-check` の対象に 2 画面を足す |

**オーナー決定（2026-09-18・「推奨通り」）**: C-1・C-2 を採る ／ 本番ワーカーの版を**読み取りだけで**確かめることを許可する（§4）。

---

## 1. 条件

### C-1 🔴「開発期間中」の終わりを、出口で決める

照会 §1.2 の論点の吸収は、★**「開発期間中は出口（§11 景品交換）が動いていないので、複垢の利得は実現しない」**という 1 点だけに乗っています（✔ D-077 ③「複垢の利得は最終的に景品でしか実現しないので、出口が最終防衛線」）。

→ 案文の「**公開（P6・弁護士ゲート）より前に再判断する**」は、★**吸収の根拠と別の物差し**で期限を切っています。P6 より前に景品交換だけが先に動けば、**どのゲートも鳴らずに前提が崩れます**（D-077 が iOS について書いた形と同じ）。

**D-113 の条件 ① を次に改める**:

> ① **次のいずれか早い方より前に再判断する**: (i) 景品交換（§11）が実際の利用者に対して動く ／ (ii) 招待・許可リストの外から登録できる状態になる（C-2）／ (iii) 公開（P6）。★**「当座」を書かずに恒久の決定にしない。**

### C-2 🔴 開発中の登録は、招待制／許可リストに限る

✔ 渡し状 §8 のとおり、`https://star-two-chi.vercel.app` は**公開の URL**で、TOP の「はじめる」は `/signup` を指しています。
★メール＋パスワードの登録口を開けた瞬間、**誰でも口座を作れます**（初期 EP 2,000・初期馬 1 頭・D-075／D-079）。出口が閉じていても、**世界の NPC 在庫（D-079 ⑧）と EP 総発行量（D-077 ①）を外から削れる**形になります。

**D-113 に条件を 1 つ足す**:

> ⑦ **開発期間中の登録は、招待制または許可リストに限る。** 一般の登録口を開けない。★**許可リストの判定はサーバー側**（Supabase Auth の設定または DB）で行い、画面の出し分けだけで済ませない。★**V-19 の E 系に「許可リストの外からの登録が拒否されること」を足す**（E-8・§3-3）

△ 実現方法（Supabase の「新規登録を止めて招待で作る」設定か、DB 側の許可リストか）は**開発側が選んでよい**。★**設定で切り替わる場合は、設定の現在値を読んで判定する**（C-3 と同じ要請）。

### C-3 🔴 設定で切り替わる防御は、設定を読むか DB で重ねる

照会 §2.4 の注意（「設定を切れば検査が通る形にしない」）を、**2 か所で具体化**します。

1. ★**E-6（メール確認前に書き込みが通らない）は、Supabase の設定だけに頼らない。** セットアップ RPC（D-080 の入口）が **`auth.users.email_confirmed_at` が null の利用者を拒否する**形を DB 側にも置く。★ただし **OIDC 経路の利用者（メールを持たない LINE 利用者など）を巻き込まない**よう、**メール経路の利用者に限って**課す（経路の判定は §2 の `auth.identities` で行う）。
2. ✔ **`tools/verify-v19-db.mjs:91-92` は `admin.auth.admin.createUser({ …, email_confirm: true })` で利用者を作っています。** ★**この作り方のままでは、E-6 は「確認済みの利用者しかいない」ので必ず通ります**（R-16）。★**E-6 の検査は、未確認の利用者を本番で起こる経路（通常の登録）で作ってから行う**こと。
   ★**教訓（2026-09-16 の GE の指示で私が書き漏らしたもの）: 「変異を作れ」と指示するときは「本番で起こる経路で作ること」まで書く。**

---

## 2. Q-AUTH-08-b — `user_identities` に行を作らない

✔ `0017_user_identities.sql` の目的は「**プロバイダの `sub` → 口座**」で、註記は「`subject` に入れてよいのはプロバイダの `sub` だけ」（✔ 133-135 行）。✔ `provider` は `text` で、制約は長さ 0 を弾くだけ（✔ 169 行）。

→ 開発側の分析（案 (b) は `subject = auth.users.id` になり、★**主キーが自明に一意で何も担保しない**）に同意します。**案 (a) を採る。**

**「経路が読めない」欠点について**: △ Supabase Auth は、メール＋パスワードの登録でも **`auth.identities` に `provider = 'email'` の行を自ら作る**はずです（`auth.users` の `raw_app_meta_data` の `provider`／`providers` にも経路が載る）。★**これが本当なら、経路は `user_identities` を使わずに読めます。**
→ △ **開発側が staging で確かめる**（読み取りのみ・通常の登録で 1 人作って `auth.identities` を読む）。**無かった場合は、行を作る案に戻らず、報告してください**（別の読み方を考えます）。

★**`user_identities` は「自前で検証した外部 ID」の器**、**`auth.identities` は「Supabase が扱う ID」の器**、と役割を分けて正典に書きます（§7）。

---

## 3. Q-AUTH-09 — V-19 の書き換え

### 3-1. 棚卸しの数と、①③⑯ の扱い

✔ 正典 V-19 の 16 項目（⑮ は V-20 に統合済み）。

- 🔴 **数が合っていません。** 照会 §2.2 の表では **空振りが 7 項目（①②③④⑦⑧⑯）**ですが、同じ節の集計（「空振り 6」）と渡し状 §5 の表は **6 項目で ③ が抜けています**。§2.1 本文は「7 項目」。★**正典に写す前に 1 つに揃えてください。**
- 🔴 **①（署名改竄）③（期限切れ）⑯（`alg` で分岐しない）は「対象外」にしない。** この経路にも JWT は 1 本あります — ★**Supabase が発行するセッションのトークン**です。書き込み RPC はそれで `auth.uid()` を得ます（D-076 ③）。①③⑯ を「対象外」にすると、★**この経路で唯一の JWT を誰も検査しない**形になります。
  → **①③⑯ は「読み替え」**: 「★**改竄した／期限の切れた／`alg` を書き換えたセッショントークンで、書き込み RPC が拒否されること**」。
  △ Supabase のトークン署名方式（共有鍵か非対称鍵か）は、**プロジェクトの設定を読んで記録する**（⑯ の意味がそれで決まる）。
- **②④⑦⑧ は対象外と明記する**（リダイレクトも ID トークンの自前検証も無い）。

→ 集計は **対象外 4・読み替え 5（①③⑤⑩⑯）・反転して分割 1（⑥）・そのまま 5（⑨⑪⑫⑬⑭）** になります。

### 3-2. ⑥ の分割

✅ ⑥-oidc（現行のまま）／⑥-email（**メールの一致を根拠に口座を結合しない**）を採る。

⚠️ △ **Supabase Auth には、同じ「検証済みメール」を持つ ID を自動で 1 人に結合する働きがある**はずです（Supabase が直接扱うプロバイダを足したとき）。オーナーは当初「Google と Apple」を挙げていました。★**それらを Supabase の標準のプロバイダとして足すと、⑥-email が設定の既定で破られる恐れがあります。**
→ **⑥-email は「併設の便で効く」項目ですが、その便の冒頭で自動結合の扱いを確かめる**、と正典に書きます。△ 確かめる担当は、併設の便の開発側。

### 3-3. E 系

| # | 裁定 |
|---|---|
| E-1 | ✅ そのまま（★設定を読んで判定・C-3） |
| E-2 | ✅ そのまま |
| **E-3** | 🔴 **範囲を広げる**: 「★**旧アドレスでの確認なしに、メールアドレスの変更が成立しないこと**（再設定の経路を含む）」。★再設定リンクで得たセッションからも、通常のセッションからも、変更の口は同じ 1 つだから。△ Supabase の「メール変更時に新旧両方で確認する」設定を**読んで判定**する |
| E-4 | ✅ そのまま |
| E-5 | ✅ そのまま（★設定を読んで判定） |
| **E-6** | 🔴 **C-3 のとおり直す**（DB 側にも重ねる・未確認の利用者を通常の登録で作ってから測る） |
| E-7 | ✅ そのまま |
| **E-8（追加）** | 🔴 **許可リストの外からの登録が拒否されること**（C-2）。★**開発期間の終わり（C-1）で見直す** |

### 3-4. 数え方

✅ 「**経路ごとに、その経路で有効な項目すべて。対象外は理由とともに明記する**」。★**合格の表示に「有効な項目数／対象外の数」を必ず並べる**（「16 項目合格」とだけ出す形を作らない・R-16）。

---

## 4. 🔴 渡し状 §8「本番ワーカーも同時に入れ替わっているはず」— [CODE] と合わない

| # | 確かめたこと | |
|---|---|---|
| 1 | 本番ワーカーは **VPS 上の systemd**（`star-worker`）で、`/opt/star-current` → `/opt/star-releases/<sha>` のリンクを `tools/deploy.sh` が張り替える方式 | ✔ `tools/deploy.sh`・`deploy/README.md` |
| 2 | 2026-09-15 の実測で、稼働中は **`70d9982`** | ✔ `REPORT_ENGINE_LANE_SPEED_20260915.md:28` |
| 3 | 「Render」は star の `CLAUDE.md:31` に残った**古い記述**。★**2026-09-14 の監査で既に指摘済み**（「ワーカーの配備先『Render』（`deploy/README.md` は VPS）」） | ✔ `REPORT_AUDIT_20260914.md:142` |
| 4 | `render.yaml` も GitHub Actions も無い（開発側の実測と一致） | ✔ |
| 5 | `deploy.sh` は配備の前に `--schemacheck` を流す。本番は `0021` 以降が未適用なので、**このスクリプトで配備すれば止まる** | ✔ `tools/deploy.sh:173`・`apps/worker/src/schemacheck.ts` |

**判断**: ★**`main` の push で本番ワーカーが入れ替わった、と読む根拠はありません。** ★したがって **ES-6 裁定 §3 の 5（配備後の周の秒数）は「済み」でも「確認待ち」でもなく、未着手**です。

★**懸念は逆向きです**: もし本当に何らかの自動配備が `main` を見ているなら、★**`0023` より前に新しいワーカーが走った**ことになり、**2026-08-12 の事故（`column "condition" does not exist`）と同じ形**です（通常の起動では `--schemacheck` は走らない ✔ `main.ts:373`）。

**依頼（オーナー許可済み・読み取りのみ）**: 開発側は VPS で次を読み、出力を切らずに報告してください。**何も変えないこと。**

```
readlink -f /opt/star-current
systemctl show star-worker -p ActiveState -p ActiveEnterTimestamp -p NRestarts
journalctl -u star-worker --since "2026-09-18 00:00" | tail -50
```

★**併せて**: star の `CLAUDE.md:31` の「ゲームループは常時稼働ワーカー（Render）」を **VPS に直す**（★同じ誤りを 2 度読んでいます）。

---

## 5. 渡し状 §9-1 — V-10 の `place` −1.26pt

| # | 確かめたこと | |
|---|---|---|
| 1 | 40 本・80 本の記録は **2026-09-16** の `verify-pmin` | ✔ 渡し状 §9-1 |
| 2 | 測り方の訂正 `36d6e97` は **2026-09-17 00:05** | ✔ `git show 36d6e97` |
| 3 | その説明文は「★place は売り目 13・未発売 3.32% で最も大きく出て −1.26pt」と、**このずれを旧実装の欠陥に帰している** | ✔ |
| 4 | 渡し状は同じ記録を「**原因はまだ言えません**」として出している | ✔ |

**判断**:
- ★**40 本・80 本の記録は、直す前の道具で取ったもの**です。★**直したかどうかの証拠にはなりません**（どちらの向きにも）。
- ただし「★**未発売率が 2.20% → 3.32% と動いたのに、ずれが動かない**」という観察は、**旧実装の欠陥だけでは説明しきれない**ことを示唆します。→ ★**直した後の本測定でも −1.2pt 前後が残る前提で構える**。残ったら、**較正定数には触れず**、`REPORT_GAME_BODY_1_20260916.md` の見立て ③（「1 レース 1 確定」の手順）から切り分けます。
- ✅ **V-10 を「認証を入れて人を迎える便」のブロッキング条件にすることは受け入れます**（`REVIEW_VENUE_WIRING_FINAL_VERDICT_20260916.md` §3 の 4「実際の利用者が入る前に必ず」のとおり）。★**C-2 の許可リストで開発者だけが入る間は、利用者が入ったとは数えない**。

### 5-1. 🔴 ★レビュー側の誤りの訂正（道具の指定）

渡し状 §9-1 の指摘のとおり、`REVIEW_VENUE_WIRING_FINAL_VERDICT_20260916.md` §3 の 4 は V-10 の道具を **`npm run verify:payout -- --races 500 --seeds 42`** と書いていました。

- ✔ `verify-payout.ts:99` のオッズの試行数は、既定で **10,000**
- ✔ 本番のオッズは `ODDS_MC_TRIALS` = **3,896,104**（`apps/worker/src/odds.ts:42`・D-035 の式）
- → ★**既定の `verify:payout` は、本番と別の製品を測っています。** 正式な V-10 は ★**本番と同じ試行数で測る `verify:pmin`** です（2026-09-17 の引継ぎのとおり）。

同裁定書の §3 の 4 に、訂正の追記を入れました。★**オーナーが流すのは `npm run verify:pmin -- --races 40`（別のターミナルで 1 本）のまま**です。**開発側のセッションの裏では流さない**（2026-09-17 の撤回のとおり）。

---

## 6. 開発側の次の順番

| # | 中身 | 備考 |
|---|---|---|
| 1 | §4 の読み取り（本番ワーカーの版）・star の `CLAUDE.md:31` の訂正 | ★何も変えない |
| 2 | §2 の `auth.identities` の確認（staging・読み取り） | 無ければ報告 |
| 3 | 照会 §2.2 の数を揃える（§3-1） | 正典への写しの前に |
| 4 | ★**正典の起票を待つ**（§7） | レビュー側 |
| 5 | **V-19 の書き換えを先に**（経路ごとの表・①③⑯ の読み替え・E-1〜E-8・設定の現在値の読み取り） | ★測れないものを効かせない（D-112 ③ と同じ形） |
| 6 | 実装（登録・ログイン・再設定・許可リスト・セットアップ RPC の確認済み判定） | ★E-6 の検査は通常の登録で未確認の利用者を作る |
| 7 | `/signup`・`/login` を同じ便で。★`/design-check` に 2 画面を足す | 当座の見た目合わせは先行してよい |

★**報告では、条件ごとに「どの検査のどの行が・どの経路を通ったか」を書いてください**（2026-09-16 からの作法）。

---

## 7. 正典への起票（★本書の時点では未実施）

オーナーに案文を見せてから、次を正典に入れます。

1. **D-113**（開発期間中の認証をメール＋パスワードとする）— 照会の案文に、**C-1（条件 ① の改め）・C-2（条件 ⑦ 招待制／許可リスト）・C-3（設定を読む／DB で重ねる）**と、**§2（`user_identities` を使わない・`auth.identities` との役割分け）**を加えたもの
2. **D-076 への追記**:「★2026-09-18・D-113 が開発期間中の当座の経路として並ぶ。本決定は撤回されていない」
3. **§13.2 V-19** を経路ごとの形に（§3）
4. **§4.1 外部 ID** に一文:「メール＋パスワード経路は `user_identities` に行を作らない（D-113）」

---

## 8. オーナーへ

| # | 中身 |
|---|---|
| 1 | §7 の案文の確認（レビュー側が提示します） |
| 2 | 🔴 **V-10**: `npm run verify:pmin -- --races 40`（別のターミナルで 1 本・出力を切らない） |
| 3 | 人を迎える前に **SMTP の手配**（Q-AUTH-08-d） |
| 4 | 招待する人（許可リスト）の範囲 |
| 5 | 本番の `0021` → `0022` → V-20 → `0023` → **配備（`tools/deploy.sh`）** → `0024` → `0028`（★`0029` の位置は開発側が示す） |

---

> ✔ **追記（2026-09-18・同日）**: §7 の 1〜4 は、オーナーの承認（「推奨通り」）を得て**正典に起票しました**（未コミット・2122→2127 行・全行 CRLF・D 111→112）。

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
