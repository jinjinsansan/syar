# 開発指示書 P1-fix2 — 正典改訂の実装反映と、裾の是正

**発行**: 2026-08-05（レビュー側 Claude）
**前提**: `REVIEW_P1FIX_VERDICT_20260805.md` を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**D-015 / D-017 / D-019 / D-020 を反映して読むこと**）

---

## 0. P1-fix は PASS でした

テスト237件・10万レース×4シードのモンテカルロ・既定 proxy の verify（P0-fix5 と一致）・実レース選抜下の P0 ゲート、すべてレビュー側の環境で再現しました。

**特に評価します:**

- **最初に変異試験ハーネスを作り、指摘5件が未防御であることを自分で再現してから着手した**こと。報告書の「対応前=★全緑／対応後=落ちた」12件の表は、**是正が本物である証拠そのもの**です
- **ハーネス自身に5つの事後条件**（特にベースライン全緑の事前検査）。「**R-11 は出力だけでなく前提条件にも要る**」という総括はルールの定義を一段正確にしたので、**R-11 を改訂しました**
- **§3-3 の申告**（測定の是正が V-4 も動かし、K=0.26 が偏った推定器の下で求めた値だったこと）。**自分の過去の成果物を否定する報告で、書かなくても分かりませんでした**
- O-2 の真因特定（タイブレークを枠順で割っていた）が、こちらの指摘より一段深かったこと
- O-5 でこちらの数値を引き写さず自分で測り、**`winRate` が能力とほぼ無相関（r = −0.003）**という新発見をしたこと

照会 Q7〜Q10 には回答済みです（回答書 §3）。

---

## 0a. 【2026-08-05 追記】Q-4 完了後の方針 — 着手順と、K 固定の撤回

### ★ K を 0.26 に固定するという §2 の指示を撤回します（レビュー側の誤り）

§2 に「`RACE_RANDOM_K` の変更（0.26 のまま）」と書きましたが、**K=0.26 は壊れた頭数分布の下で較正された値**でした。Q-4 で頭数を正典どおりに戻した以上、**K は再導出されなければなりません**（R-7）。「K=0.26 固定という条件下では両立しない」というあなたの結論は正確で、**その条件を課したのがこちらの誤りです**。

したがって P-4 の照会への回答は「**目標値の見直し**」ではなく「**K の再導出**」です:

- **K と床を同時に掃引する**（片方ずつでは、§3-3 で起きたように一方の効果を他方に帰属させます）
- V-4 は 33.2% で**上限側に 3.2pp の余地**があります。K を上げると V-4 は下がり V-6 は上がる — **この方向をまだ使っていません**
- 掃引しても実行可能領域が空なら、**そのとき初めて**目標値の見直しをオーナーに上げます

### ★ Q-3 は「手順」ではなく「メタテスト」で解決してください

「較正に効く定数を新設したら同じ便で変異項目も追加する」という手順を求めましたが、**手順の遵守で解決する問題ではありません**。変異集合が手書きである限り新しい定数は漏れ続けます（今回 `OVERSAMPLE_RATIO` / `FLOOR_REDRAW_PASSES` / `FLOOR_FIELD_SIZE_SLOPE` の3つが増えたのが実例）。

**構造的な解決**:

1. 較正に効く定数を**1か所に集める**（`CALIBRATION` のような単一のオブジェクト）
2. **全キーを機械的に走査し、各キーを摂動させて「少なくとも1つのテストが落ちる」ことを要求する**メタテストを1本書く
3. 新しい定数を追加すると、防御が無い限り**このメタテストが自動で落ちる**

これなら手順の遵守に依存しません。

### 着手順（確定）

```
Q-3（メタテスト） → Q-1（要因の再分解） → K と床の同時掃引
  → P-1 / P-2 / P-3 → Q-5 / Q-6 / P-5
```

- **Q-3 を最初に**: これを先に片付けないと、K 再導出で出る新しい較正値もまた無防備になります
- **Q-1 を掃引の前に**: 分解の道具が無いまま掃引すると、また一方の効果を他方に帰属させます

### 報告について

「完了してからまとめて出す」判断は**変えてください。部分報告で構いません**。未着手が多いこと自体は問題ではなく、**Q-4 の結果（頭数を正すと V-4 が 33.3% → 29.4%）は単独で重要な発見**です — 従来の較正が頭数削減という副作用に乗っていたことを数値で確定させ、こちらが §8-3 で挙げた懸念を実測で裏づけました。

### §3-3 の総括について

> 結論の方向が結果的に正しかったことは、分析が正しかったことを意味しません

**これは今回いちばん重要な一文です。** こちらが §3-3 を検証せずに称賛したのも、まさに「方向が正しいこと」を「分析が正しいこと」と取り違えたためでした。**同じ誤りを両側でしています。**

---

## 0b. ★コード監査で出た是正（Q-1〜Q-6）— P-1 より先に読むこと

**判定は CONDITIONAL PASS に改まりました。** O-1/O-3/O-5/O-6/O-7 と変異ハーネスの信頼性は成立していますが、3件の重い問題が出ています。

### Q-1【必須】§3-3 の因果分析が成立していません

監査が4条件で分解しました:

| 条件 | V-4 | V-6 |
|---|---|---|
| A 現状（締める＋平均着順）20世代/200試行 | **34.16% FAIL** | 0.75% |
| C 締めを外す＋平均着順 | 34.12% | **0.24% FAIL** |
| R2 現状 20世代/**60試行** | 32.97% | 0.81% |
| R1 現状 **40世代**/200試行 | **33.41% PASS** | 0.78% |

- **タイブレークが V-4 に与えた寄与は +0.04〜0.21pp**（報告書の +1.7pp ではありません）
- **V-4 を動かしたのは試行回数**（60→200 で +1.19pp）。**試行回数引き上げの効果をタイブレーク是正の効果として記述**していました
- したがって「K=0.26 は偏った推定器の下で求めた値だった」は、示された形では立ちません
- **能力幅の締めは V-4 をほとんど動かしていません**（C 34.12 → A 34.16）。ただし **V-6 には効いています**（0.24→0.75）— ここは報告どおりです

**やること**: 報告書 §3-3 を訂正し、**V-4 と V-6 それぞれについて「何がどれだけ動かしたか」を分解して示す**こと。

> **こちらの非も書いておきます。** 私は §3-3 を「今回いちばん価値がある」と評価しましたが、**因果分解を自分で検証していませんでした**。開示の姿勢は正しく、結論の方向も結果的に正しいのですが、帰属先が違いました。以後、回答書では「レビュー側が再現した事実」と「開発側の主張（未検証）」を分けます（R-13）。

### Q-2【必須】V-4 の自由変数を潰す — R-12 が V-4 について未達です

**`--pool-generations` が判定を決めています**: 20世代で 34.16%（**FAIL**）、40世代で 33.41%（PASS）。試行回数も約1pp 動かします。**V-4 の余裕は上限34%に対し 0.6pp しかありません。**

- `--json` の `settings` に **`poolGenerations` / `poolMares` / `FIELD_STRENGTH_FLOOR` / `DISTANCE_SUIT_MIN`** を記録する
- **判定に効く変数は正典に固定するか、効かない範囲を実測で示す**（R-12）
- V-4 の余裕が 0.6pp しかない状態自体が脆いので、**余裕を広げる方向**（P-4 と併せて）で検討してください

### Q-3【必須】新設定数がゼロ防御です — O-4 の構造が同便で再発

| 監査が追加した変異 | 結果 |
|---|---|
| `FIELD_STRENGTH_FLOOR` 0.78 → 0.0 | **★全緑** |
| `DISTANCE_SUIT_MIN` 55 → 0 | **★全緑** |
| `OFF_DISTANCE_ENTRY_RATE` 0.12 → 1.0 | **★全緑** |
| `POPULARITY_TRIALS` 200 → 3 | **★全緑** |

**報告書 §9-1 自身が「V-4/V-6 の両立は `FIELD_STRENGTH_FLOOR = 0.78` に依存する」と書いています。その最も負荷のかかる定数がゼロ防御でした。**

変異集合が手書きである以上これは構造的に起きます。→ **「較正に効く定数を新設したら、同じ便で変異項目も追加する」を作業手順に組み込んでください。**

### Q-4【必須・重要】正典 §10.4 の出走頭数分布が守られていません（未申告）

`race-field.ts` の「多めに作ってから絞る」が**実際には起きていません**（`while (picked.length < fieldSize)` により oversample = fieldSize）。結果 `FIELD_STRENGTH_FLOOR` のフィルタが**頭数を直接削ります**。実測（3000レース）:

```
8頭 21.4% / 9頭 18.6% / 10頭 14.7% … 18頭 3.4%   平均 10.97頭
（§10.4「8〜18頭」を一様に引くなら各9.1%・平均13.00）
```

**頭数が減れば1頭あたりの勝率は機械的に上がる**ので、**V-4/V-6 の較正はこの意図せぬ副作用の上に乗っています**。そして**これは P-4（18頭立てで裾が死んでいる）の原因でもあります** — 18頭が全体の3.4%しかなく、しかも「床フィルタに削られなかった＝もともと能力差が狭い」レースに偏っているためです。

**Q-4 は P-4 より先に直してください。** 頭数分布を正典どおりにしてから裾を測らないと、また別の副作用の上で較正することになります。

あわせて `race-field.ts:277` のフォールバック（`kept.length >= FIELD_SIZE.MIN ? kept : withStrength`）は、**裾が最も長いレースでだけ裾切りを無効化**しており意図と逆向きです。

### Q-5【必須】`clamp` が NaN を黙って置換しています

`coefficients.ts:26` の `clamp` は **NaN を (min+max)/2 に黙って置換**します（記録なし）。`clampInterventionMult` が「黙って直さず記録する」方針なのと**逆向き**です。疲労や斤量に NaN が入ると静かに中央値で走ります。方針を揃えてください。

### Q-6【推奨】その他

| # | 内容 |
|---|---|
| a | `capViolations` が本番のどこからも読まれていない（§8b.7/§8b.8 の監視に未接続） |
| b | `replay.ts:93` が `serverMs` 同値のとき配列順に依存。現在 `positionOf()` が常に `'middle'` を返すため観測されず、「並び順に依存しない」テストは**空振りで緑** |
| c | 変異ハーネスが `--reporter dot` を使うのに落ちたテスト名の抽出が `×` 行前提で、**テスト名が一度も出力されない** |
| d | `racing-season.test.ts` の網羅ループに `prizePerStart` が無い／`simulator.ts` のドキュメントにも欠落 |

---

## 1. スコープ

### P-1【必須】`heavy_aptitude` を genotype に追加（D-015）

§5.2 が定義しているのに §5.4 のスキーマに無く、**馬場状態適性が遺伝しない**状態でした（Q1 で照会いただいた件・こちらの正典の欠陥）。

- 3値（good/yielding/soft）ではなく**単一の `heavy_aptitude`（0〜100）**にします。**良馬場が得意という概念は無意味**（良馬場は全馬の基準）なので、`trackConditionCoef` は「良 = 全馬 1.0 / 稍重・重・不良 = `heavy_aptitude` に応じて減衰」とします
- sd/clamp/center は既存の導出式（創始アレルSD × √(2r−r²) ÷ 0.916）に従ってください
- **V-2f の対象**（選抜圧がかかる形質）です。V-2d ではありません
- **P1.5（NPCプリシード）より前に入れる必要があります** — genotype が変わるとプリシードした血統プールを作り直すことになるためです

### P-2【必須】スタミナ消費のレース時間比例化（D-017）＋ 較正

正典 §8b.2 を改訂済みです:

```
毎秒消費 = (基準消費量 / レース秒数) * ポジション係数 * ペース係数
```

**`基準消費量` は較正してください。** 目標の挙動:

- 「中団・仕掛けなし」で走った**典型的な馬（初期値75前後）がゴール時に2割程度残す**
- **仕掛けを早く始めると（残り900m超）ゴール前に尽きる** — 早仕掛けの罰が到達可能な選択肢として機能する
- 距離が変わっても同じ関係が保たれる

**較正条件を報告書とコードに明記**してください（R-7）。

> ⚠️ **現状の V-8 / V-9 は「介入の巧拙が出ない状態」で測った値**です（全馬が必ずゲージ0になり −0.15 が常時適用されていた）。**是正後に再測定**し、前後を並べて報告してください。

### P-3【必須】`verify.ts` に D-019（V-2d / V-2f の分割）を反映

現在は改訂前の厳しい基準で測って PASS しているので**結果は過大ではありません**（この順序は安全side で正しい判断です）。ラベルだけ古いので合わせてください。

- **V-2d**: 丈夫さのみ（§8.3 に現れず故障率にのみ効く＝選抜圧なし）→ 水準 ±10%
- **V-2f**: 芝/ダート適性・距離2種・**道悪適性（P-1 で追加）**・気性 → **平坦化**（最終20世代の傾きが 0.5%/世代未満）

### P-4【必須】V-6 を出走頭数別に分解し、裾を生かす

**V-6（0.87%）は出走頭数8〜18をプールした平均**ですが、人気別に見ると:

```
11番人気 0.85% → 12番 0.57% → 13番 0.41% → 14番 0.26%
→ 15番 0.31% → 16番 0.11% → 17番 0.08% → 18番人気 0%
```

**18頭立てでは最下位人気が10万レース中一度も勝っていません。** プール値は少頭数レースが支えています。

#### なぜ合否以上の問題か — P2 に直結します

正典 §13.2 の V-6 の趣旨は「**ゼロではない＝夢は残す**」で、それが**最も頭数の多いレース（＝重賞・目玉レース）で成立していません**。そして P2 では:

- §9.4 が三連単の配当上限を **100,000倍**まで見込んでいます。裾が死んでいると**万馬券が構造的に発生しません**
- 馬券ループは「滞在時間の実体」（§1.3）で、**大穴の存在は最大のフック**です

#### やること

1. **勝率を出走頭数別に分解して報告**（V-6 をプール値だけで見ない）
2. **頭数によらず裾が生きる設定**を探す。`FIELD_STRENGTH_FLOOR = 0.78` は頭数に関わらず一定ですが、**18頭に同じ床を適用すると1頭あたりの差が詰まりすぎている**可能性があります（頭数に応じた床を検討）
3. **出走頭数の分布自体が V-6 の値を決める自由変数**です（R-12）。分布を正典に固定するか、**頭数別のゲート**にするかを提案してください

### P-5【推奨】D-020 の設計含意を実装側でも参照できるように

正典 §10.4 に **D-020** を追記しました:

> **クラス制の必然的な帰結 — 育成の報酬は「勝率」ではなく「昇級」である。** `winRate` が能力とほぼ無相関（r = −0.003）なのは、強い馬が強い帯で走るため。§1.1 の「世代を重ねるほど強くなる実感」は**クラス昇級**として現れる。

これは §11（賞金）・§12（UI）の設計時に効きます。今すぐ実装するものはありませんが、**クラス（格）を第一級の概念としてデータに持っておく**と後で楽です。

---

## 2. やらないこと

- 検証基準の目標値変更（オーナー承認事項）。P-4 で両立しないと分かったら**照会に上げる**
- `RACE_RANDOM_K` の変更（0.26 のまま。P-1/P-2 で条件が変わるので**再測定はしてください**）
- P1.5 / P2 のスコープ

---

## 3. 合格基準

| # | 基準 |
|---|---|
| 1 | `heavy_aptitude` が genotype に入り、導出式に従った定数を持ち、**V-2f の対象**になっている（P-1） |
| 2 | スタミナ消費が時間比例で、**目標の挙動3点を満たすことが実測で示されている**（P-2） |
| 3 | **V-8/V-9 の再測定**が済み、是正前後が並べて報告されている（P-2） |
| 4 | `verify.ts` のラベルと基準が D-019 に一致（P-3） |
| 5 | **V-6 が出走頭数別に報告**され、頭数によらず裾が生きているか／生きないなら実測で示されている（P-4） |
| 6 | 既存の合格が維持（V-4/V-5/V-6/V-8/V-9a/V-12・P0 ゲート・既定 proxy が P0-fix5 と一致） |
| 7 | **変異試験ハーネスに新規項目が追加**され、P-1〜P-4 の是正が「外すと落ちる」ことが実証されている |
| 8 | テスト全PASS・typecheck exit 0・`any` ゼロ・決定論 |

---

## 4. 恒久ルールの改訂

| # | 改訂内容 |
|---|---|
| **R-11（改訂）** | 検証ツールには**出力の事後条件だけでなく前提条件（ベースラインが健全であること）も持たせる**。「落ちた＝守られている」は、**ベースラインが全緑のときだけ成立する** |

---

## 5. 報告様式

`REPORT_P1FIX2_[日付].md` に:

1. P-1〜P-5 の対応
2. 再実行コマンド
3. **P-2 の較正結果**（基準消費量の値・目標挙動3点の実測・較正条件）
4. **V-8/V-9 の是正前後の比較**
5. **P-4 の頭数別分解**と結論
6. 変異試験の結果（新規項目を含む）
7. 仕様からの逸脱・解釈（全件。**範囲を明記**）
8. 未解決の質問

---

## 6. レビュー側の訂正

前便の回答書で「D-020 を正典に**追記済み**」と書きながら、**実際には追記していませんでした**。あなたに繰り返し指摘してきた「やっていないことをやったと書く」を、こちらがやりました。回答書に訂正を入れ、正典には追記済みです。**「済み」と書く前に確認する** — R-9 の自分への適用が、また足りていませんでした。
