# REPORT_P2 — P2 第1便（インフラ非依存部分）

**開発側 → レビュー側** / 2026-08-07
**コミット**: `4960c22`（push 済み）
**指示書**: `DEV_INSTRUCTIONS_P2_20260806.md`

> ⚠️ **完了報告ではありません。** 合格基準 A-1〜A-9 のうち **実測できたのは A-9 のみ**です。
> A-1/A-2/A-4/A-5/A-7 はインフラ待ち、**A-3 は測定設計が未確定**（照会中）。

---

## 1. 実装したもの

| 追加 | 内容 |
|---|---|
| `packages/betting` | §9 の7券種・オッズ・払戻（純粋TS・依存ゼロ） |
| `packages/scheduler` | §10.2 の開催サイクル（純粋TS・**時計を持たない**） |
| `db/migrations/0001_init.sql` | §4 スキーマ（**未適用**） |
| `db/migrations/0002_rls_and_place_bet.sql` | RLS と発売 RPC（**未適用**・レビュー依頼中） |
| `apps/cli/src/verify-payout.ts` | A-3 / V-10 の測定ハーネス |

### 再実行コマンド

```bash
npm test && npm run typecheck && node tools/mutation/run.mjs
npm run verify:payout -- --races 400 --odds-trials 2000 --seeds 42
```

SQL は**適用していません**。認証情報を受け取っていないため外部接続は一切行っていません。

---

## 2. 設計上の判断（レビューしていただきたい箇所）

### 2-1. 憲法をコメントではなく制約で閉じた

正典 §4.1 は「EP→PP の一方通行を **DB レベルで担保する**」と書いていますが、
「reason に変換を表す値を作らない」とコメントするだけでは担保になりません（次に書く人が足せる）。
**CHECK 制約で reason を列挙して閉じました。**

```sql
constraint pp_ledger_reason_allowed check (reason in ('prize', 'payout', 'prize_exchange'))
```

`from_ep` / `convert` / `topup` の類を足そうとすると制約に当たって落ちます（構造 S-5）。
同じ考えで次も構造で閉じています:

- `races` — 確定前に `seed_reveal` が**入ること自体**を禁止
- `race_entries` — 介入倍率を 0.90〜1.10 に制約（§8b.3 の憲法キャップを DB 側にも）
- `bets` — 未確定の馬券に払戻額が入らない（**二重払戻の足跡がここに出る**）
- `horses` — 所有者と NPC 厩舎は排他・自分を親にできない
- `app_environment` — A-7 のための環境の真実（1行しか存在できない制約つき）

### 2-2. `place_bet()` の `security definer` の扱い

- **`user_id` を引数に取らず `auth.uid()` を使う** — definer は呼び出し元の権限を無視するので、
  引数で受けると**他人の EP で買えます**
- **`search_path` を固定** — 未固定だと呼び出し元に同名関数を先に見つけさせられる（definer 関数の典型的な穴）
- 馬券の insert ポリシーを**張っていません**。発売はこの関数だけが行います
  （クライアントが insert できると、残高を減らさずに馬券を作れる）

### 2-3. スケジューラが時計を持たない

`Date.now()` を呼びません。サイクル計算をワーカーの制御フローに埋めると、
**異常系（再起動・遅延・時計のずれ）をテストできなくなります。**
A-2 は「サイクル番号が時刻だけから決まる」ことが前提なので、純関数として切り出しました。

`racesToPrepare()` は**「作るべき一覧」を返すだけ**にしています。
実際に作るかは呼び出し側が存在確認して決めます — 関数が作るところまで担うと、
**存在確認を飛ばした実装ができてしまう**ためです。

---

## 3. 合格基準の状態

| # | 状態 |
|---|---|
| A-1 24時間稼働 | **インフラ待ち** |
| A-2 冪等性 | **インフラ待ち**（前提となるサイクル番号は実装・テスト済み） |
| A-3 V-10 払戻率 | **測定設計が未確定**（§4・照会 Q-1） |
| A-4 seed_reveal 非露出 | **設計のみ**（照会 Q-3。DB CHECK とビューで二重に閉じた） |
| A-5 EP減算と発券の原子性 | **SQL 記述済み・未実行**（レビュー依頼中） |
| A-6 自馬ベット制限 | SQL 記述済み・未実行 |
| A-7 環境ガード | テーブルのみ。ワーカー側の照合は未実装 |
| A-8 P0/P1 回帰 | 未実施（本番構成が無いため） |
| A-9 テスト・変異試験 | **329 tests 全PASS / typecheck 0 / `any` ゼロ / 変異試験 34/34 防御** |

---

## 4. ★A-3 が現在の定義では実行できません（照会 Q-1）

| 実行条件 | win | place |
|---|---|---|
| races=60 | 52.43% | 74.10% |
| races=400 | **97.09%** | **83.06%** |
| 目標 | 82% | 82% |

**分散が支配的**です。一方 **place（低配当）は 83.06% で目標 82% に近い**ので、
**オッズ推定そのものは壊れていません**。振れているのは測り方です。

10万レース（§13.2）× MC 1万回（§9.2）＝ **10⁹ 回のレース解決**になり、
レース数を増やす方向では回りません。分散低減案を `QUESTIONS_P2` Q-1 に出しています。
**判定を通しやすくする変更なので自分では決めません。**

---

## 5. 自分で見つけて塞いだもの（3件・すべて過去に潰した型の再発）

| 塞いだもの | 過去の同型 |
|---|---|
| 新パッケージが `CALIBRATION_SCAN_DIRS` に無く、走査外だった | S-4 |
| `TICKET_ORDERED` が `settle` と同じ事実の2つ目の置き場だった | L-2 |
| `MARGIN`/`ODDS_CAP` を複数行で書き、行単位の変異がファイルを壊して「未防御」と報告された | `TRACK_CONDITION_CDF` |

1つ目は、追加した直後に **「この 32/32 は走査していない範囲について何も言っていない」**
と気づいて自分で塞ぎました。`packages/scheduler` は最初から走査対象に入れています。

★4件目: 変異試験が `ODDS_CAP` を **未防御** と検出しました（値照合テストしか落ちない＝R-14）。
配当上限が実際に効いていることの ★振る舞いテストを追加しています。閾値には
`ODDS_CAP` 自身ではなく**正典の数字をリテラルで**置き、自己検出を避けました。
さらに「常に cap を返す実装」でも通ってしまわないよう、**cap に達しない確率では
cap を返さない**ことも押さえています。

3つ目は**変異試験が自分で報告してくれた**ので気づけました。
R-18（異常終了時の後始末）を入れた直後に、同じハーネスが別の欠陥を教えた形です。

---

## 5-2. ★適用前レビューで指摘された欠陥3件（すべて修正済み）

**私が見つけられず、レビュー側が SQL を読んで見つけたものです。**

| # | 欠陥 | なぜ危険か |
|---|---|---|
| S-1 | `security_invoker = true` と `revoke all on races` が矛盾 | invoker はビューの基底テーブルを**呼び出し元の権限で**検査するので permission denied。**A-4 が機能しないどころかレース一覧が誰にも見えない** |
| S-2 | 台帳の `revoke all` が「自分の行を見る」ポリシーを打ち消していた | revoke が勝つので、プレイヤーが自分のポイント履歴を**永久に見られない** |
| S-3 | `place_bet` に冪等キーが無い | 再送で馬券が2枚・EP が二重に引かれる（A-2 に直結） |

**S-1 は P2 指示書 §3 が予告した「ローカルでは動くが本番構成では動かない」そのものです。**
A-4 の合格基準は「実際に叩いて確認」でしたが、**authenticated として1回引くテストがあれば
その場で分かりました**。SQL を書いて満足し、権限の経路を一度も通していませんでした。

S-3 の修正では、再送を**エラーにせず既存の馬券IDを返す**形にしています。
エラーにすると「成功したのに失敗と見えた再送」で客が二度払うためです。

---

## 6. 照会（`QUESTIONS_P2_20260807.md`）— 回答済み

| # | 内容 |
|---|---|
| Q-1 | **10⁹ は独立した2軸を掛けたために出ている。**MC回数がバイアスを支配し（1/x が凸なので p̂ の誤差でオッズは系統的に高く出る・レース数では消えない）、レース数が精度を支配する。**10万レースは下げず、削るのは MC 側。** |
| Q-2 | **認証情報はこちらに渡さない。**SQL はテキストで足りる。`git.txt` の service role キーはインフラを立てる前にオーナー回答で決着させる |
| Q-3 | **読み替えは正しく、指示書の記述が誤りだった**（撤回） |

---

## 7. 次に進められるもの（インフラ不要）

- §10.3 の番組表（1日144R のクラス配分）
- §11 の PP 発行量監視（`point_flow_daily` を埋めるロジック）

Q-1 の回答を受けて、A-3 は次の順で進めます:

1. **1,000レース固定で MC 回数を振り、払戻率が平坦になる点を探す**（総計 4×10⁷ 程度）
   — バイアスが消える最小の MC 回数を特定する
2. その MC 回数で**レース数を10万まで上げて精度を出す**

「10万」を下げる方向で考えていたのが誤りで、**バイアスと精度が別の軸である**ことを
分けていませんでした。
