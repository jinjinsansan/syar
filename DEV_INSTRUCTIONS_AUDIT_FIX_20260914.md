# 指示書 — 監査の修正 第 1 便（払戻の額 ／ 調教の EP 引き落としと関数の実行権限）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-14 ／ レビュー側 → 開発側
**根拠**: `REPORT_AUDIT_20260914.md` ／ 裁定 `REVIEW_AUDIT_VERDICT_20260914.md`
**起点 HEAD**: `620e2c1` ／ ブランチ `p4/race-30sec-cuts`

> 本便の対象は、監査の **H-1・H-2・H-3・H-4** だけです。
> H-5・H-6・M-* は次便に回します。**先走らないでください**（CLAUDE.md「指示書が出ている範囲のみ」）。

---

## 0. 範囲

### 0-1. 触ってよいもの

| 領域 | ファイル |
|---|---|
| 払戻・オッズ | `packages/betting/src/{settle.ts, balance.ts}` と、必要なら `packages/betting/src/` に新規のファイル 1 本 |
| ワーカー | `apps/worker/src/{payout.ts, training-runner.ts}` |
| DB | `db/migrations/` に**新規**のファイル（既存の移行ファイルは書き換えない・チェックサムで落ちる） |
| 検査 | `packages/betting/test/`、`apps/cli/test/rpc-guard.test.ts`、`tools/verify-anon-exposure.mjs`、`tools/lib/exposure-registry.mjs`、`apps/cli/src/verify-payout.ts` |

### 0-2. 触らないもの

- P4 のファイル（`packages/render/`・`apps/web/`・`tools/` の描画系）。**別セッションが同じ作業ツリーで作業中です**
- **本番 DB への書き込み・移行ファイルの適用**。本便は staging までです（§6）
- 正典 `STAR_SPEC_v2.0.md`（D-094・D-095 の起票はレビュー側が行います）
- `git.txt`・`secrets*.env`・`.env*`
- push

### 0-3. 同じ作業ツリーの作法（`star/CLAUDE.md`）

`git stash`・`git add -A`・`git commit -a` を使わない。コミットするときは対象ファイルを名指しする。開始 HEAD と終了 HEAD、`git diff --numstat <開始>..<終了>` を報告に書く。

---

## 1. AF-0 先に測る（読み取りのみ）

**直す前に、直す対象の実際の姿を固定します。** 手順は「**staging で道具が期待どおりの値を出すことを確かめる → 本番を 1 回だけ読む**」です（R-26・本番を探索に使わない）。

| # | 測るもの | 何のためか |
|---|---|---|
| a | `public` スキーマの**全関数** × `anon`・`authenticated` の `has_function_privilege(role, oid, 'EXECUTE')` の一覧 | H-4 の確定。**関数名を手で並べず、`pg_proc` から走査すること**（R-29） |
| b | 本番の件数: `horses where owner_id is not null` ／ `bets` ／ `ep_ledger where reason = 'training'` ／ `users` | 裁定 §3-1 の実害を閉じる（0 件と見込んでいるが推測・R-25） |
| c | staging の `race_odds`: 保存値と、`probability` から切り捨て前のオッズを再計算した値の比（券種ごとの最大・平均） | H-2 の「最大 +5.24%」の検証 |
| d | staging の `race_odds`: **オッズが 1.0 未満の目の件数**（券種別）と、**切り捨てにしたら 1.0 未満になる目の件数** | 裁定 §3-7。オーナーに諮る材料 |

**本番に向けたコマンドは全文を報告に書き、出力を途中で切らずに貼ってください**（`| head` を付けない・2026-08-20 の事故）。

---

## 2. AF-1 払戻を整数だけで計算する（監査 H-1）

### 2-1. 作るもの

1. **オッズを 0.1 単位の整数（以下 tenths。2.3 倍なら 23）へ変換する関数を 1 か所に置く。**
   - DB の文字列（`"2.3"`）から**浮動小数を経由せずに**変換する入口を用意する（裁定 §3-4）
   - 0.1 単位に乗らない値や、解釈できない値は**例外にする**（R-3「判定不能は FAIL」）。黙って丸めない
2. `settle` の払戻を **`Math.floor(stake × tenths / (10 × mult))`** にする。途中の値はすべて整数（裁定 §3-3）
3. `payout.ts:70` の `Number(b.odds_at_purchase)` をやめ、1. の入口を通す

### 2-2. 検査

| # | 内容 |
|---|---|
| 1 | **総当たり**: 購入額 100〜10,000（100 刻み）× tenths 10〜9,999 × mult 1・2・3 のすべてで、整数の正解と**完全に一致**すること（**少ない側も多い側も**・R-2）。レビュー側の実測は 1 ケースあたり 1 秒未満 |
| 2 | 境界: `100 × 2.3 = 230` ／ 同着 3 等分の切り捨て |
| 3 | 0.1 単位に乗らない値（`3.33`・`99.99`・`NaN`・`"abc"`）で**例外になる**こと |
| 4 | **既存テストの `3.33`・`99.99`**（`settle.test.ts:258`・`:263`）を 0.1 単位の値に直す。そのテストが**何を守っていたか**（同着の切り捨て・発行超過しない）は保つ |
| 5 | **経路テスト**: `payout.ts` の変換を通って払戻額が決まる形を 1 本（純関数だけを固定しても防御ゼロ・R-1） |
| 6 | **変異**: `settle` を旧式 `Math.floor(stake * odds / mult)` に戻すと、1 と 5 が**落ちる**ことを実演し、報告に書く |

---

## 3. AF-2 オッズを 0.1 単位で切り捨てて返す（監査 H-2・オーナー決定 D-094 候補）

### 3-1. 作るもの

- `oddsFromProbability` の戻り値を **0.1 単位の切り捨て値**にする。上限との順序は「上限で頭打ち → 切り捨て」（上限値は 0.1 単位に乗っているので、順序で結果は変わらない。そのことを検査で押さえる）
- **浮動小数の誤差で 1 段下がらないこと**。切り捨て前の値が本来 2.3 ちょうどなのに `2.2999999999999998` と表現されている場合は、**2.2 ではなく 2.3** を返す。許容幅は定数として置き、根拠を註記する
- ワーカーが `race_odds` に書き込む値が 0.1 単位に乗り、**`numeric(9,1)` に入るときに値が変わらない**こと

### 3-2. 1.0 未満の扱い

**今は決めません。** AF-0d の件数を報告に書いてください。1.0 未満になる目が実際にあれば、**最低値を勝手に置かずに照会**してください（オーナーの判断事項です）。

### 3-3. 検査

| # | 内容 |
|---|---|
| 1 | 戻り値が 0.1 単位に乗り、**切り捨て前の値以下**で、**切り捨て前の値 − 0.1 より大きい**（両側） |
| 2 | 格子の値ちょうど（誤差で下に表現されるもの）は下がらない ／ 格子のすぐ下は 1 段下がる（両側） |
| 3 | 既存の `toBeCloseTo(切り捨て前の値)`（`settle.test.ts:84`・`:123`・`:296-297`）を、切り捨て後の値で守る形に置き換える。**何を守っていたか（控除率・補正式・上限）は保つ** |
| 4 | **V-10（`apps/cli/src/verify-payout.ts`）を変更の前後で実行**し、券種ごとの払戻率を表にする。正典 §9.3 の控除率 ±1% に入るか |
| 5 | staging でレースを 1 本生成し、`race_odds.odds` が `probability` から再計算した切り捨て値と**全行一致**すること |

---

## 4. AF-3 `spend_training_ep` をワーカー専用として閉じる（監査 H-3＋H-4・D-095 候補）

**3 つを同じ便で入れてください。** 権限を剥がさずに検査だけ外すと、H-4 の穴が戻ります。

### 4-1. 作るもの

1. **新しい移行ファイル**
   - `revoke all on function spend_training_ep(uuid, bigint, int) from public, anon, authenticated;`
   - 関数本体から `assert_setup_complete()` を外す（本体は 0020 と同じく、稼働中の定義を `pg_get_functiondef()` で取得し、**staging と本番の md5 が一致することを確かめてから**生成する）
2. **`rpc-guard.test.ts` に「ワーカー専用関数」の登録簿を足す**
   - 登録簿に載った関数は、`assert_setup_complete()` の要求の代わりに次を満たすこと: ①最後の定義以降に `public`・`anon`・`authenticated` からの revoke がある ②それより後に、それらへの grant が無い ③本体で `auth.uid()` を使わない
   - `place_bet`・`exchange_prize` を登録簿に入れられないこと（既存の `:98-103` と同じ形）
3. **`training-runner.ts` の例外処理を分ける**
   - **EP 不足だけ**を休養に落とす。関数側で EP 不足に**専用の SQLSTATE** を付けて見分ける（メッセージの文字列一致は不可。文言を直した日に黙って外れる）
   - それ以外の例外は `epShort` に数えない。別の計数と警報に出す。その馬のその週をどう扱うか（止める／飛ばす）は、案を出して照会してよい
4. **`verify-anon-exposure.mjs` に関数の実行権限の軸を足す**（V-20 の 4 つめ）
   - 期待値は AF-0a の staging の実測から登録簿に書く。**登録簿に無い関数が現れたら落ちる**（V-20 ③ と同じ形）

### 4-2. 既定を閉じるか（先に実演）

`public` に**これから作る関数**が anon の EXECUTE を持って生まれるかを、**staging で実演**してください（作って、測って、消す）。持って生まれるなら、`alter default privileges ... on functions` で閉じる案を出してください。**ただし、`place_bet`・`exchange_prize` の既存の grant を壊さないこと**（0002・0018 の S-2 と同じ種類の事故）。

### 4-3. 実演（staging・R-26: 通る場合と通らない場合の両方）

| # | 手順 | 期待 |
|---|---|---|
| 1 | 所有馬があり EP が足りる状態で週送り | 引き落とされ、調教のメニューが実行される（**休養に落ちない**） |
| 2 | 所有馬があり EP が足りない状態で週送り | 休養になり `epShort` に数えられる |
| 3 | anon キーで `/rpc/spend_training_ep` を呼ぶ | **拒否される**（権限エラー） |
| 4 | authenticated（セットアップ済みの利用者）で同じく呼ぶ | **拒否される** |
| 5 | 変異: 新しい移行ファイルから `anon` の revoke を消す | `rpc-guard.test.ts` と V-20 の関数の軸が**落ちる** |

---

## 5. 報告 `REPORT_AUDIT_FIX_<日付>.md` の様式

- §0-2「提出物・コミット・変更範囲」と §8「期待値つきの再実行表」の形を続ける
- `npx vitest run` の **`Test Files`・`Tests` の行と終了コード**を貼る。**提出物（報告書を含む）を足したあとにもう一度回す**
- 既存の赤 `edit-grammar-audit` ⑨ はオーナー判断待ちのままでよい。**赤が 1 件から増えていないこと**を書く
- AF-0 の各数値について、**staging と本番のどちらで測ったか**を必ず添える
- 再現した事実と未検証の主張を分けて書く（R-13）

---

## 6. 本便でやらないこと

- **本番への移行ファイルの適用**。staging で §4-3 が揃ったら報告してください。本番への適用は、裁定のあと**オーナーの指示で**別に行います
- H-5（権利）・H-6（本番の開発画面）・M-1〜M-10 の修正
- 最低オッズの決定（§3-2）
- 正典の編集
