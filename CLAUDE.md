# CLAUDE.md — STAR（開発コード）

> このディレクトリで作業する Claude は、まずここを読むこと。

## このプロジェクトは何か

**STAR** — ポイ活型オンライン競馬育成ゲーム。配合→調教→レース→引退→繁殖の無限ループを持つ完全オリジナル作品。

- **正典は `STAR_SPEC_v2.0.md` のみ。** 迷ったら正典を読む。正典にない仕様を推測で埋めない（照会を出す）
- 現在のフェーズ: P0（遺伝エンジン + 100世代シミュレータ）実装済み・レビュー中

## ⚠️ 廃棄済み文書（読まない・従わない）

| ファイル | 理由 |
|---|---|
| `SPEC.md` / `AI_PROMPT.md` | 第1世代の「他社製品のブラウザ再現」仕様。**実在競走馬名・他社固有名称を含み憲法違反**。2026-08-03 廃棄決定 |
| `GORILLA_STABLE_SPEC_v1.*` / `files.zip` | 別プロジェクト（ゴリラ予想）の仕様。参考資料であり正典ではない |
| `git.txt` | **認証情報を含む。読まない・使わない・コミットしない** |

## 憲法（違反は即差し戻し・詳細は正典 §0）

1. **権利方針**: 実在競走馬名・実在レース名・実在競馬場名を使わない。他社製品の固有名称（製品名・機能名）をコード内の変数名・テーブル名・コメント・出力にも書かない
2. **ポイント5原則**: 二種ポイント制（参加ポイント `entry_points`/EP・賞金ポイント `prize_points`/PP）。**参加ポイントを金銭で購入できる経路を絶対に作らない**（賭博構造の分水嶺）。賞金P→参加Pの還流は弁護士判断まで不可
3. **サーバー権威**: ポイント台帳・レース結果・乱数はサーバー側。クライアント計算を信用しない
4. **決定論**: `Date.now()` / `Math.random()` を直接呼ばない。乱数と時刻は必ず注入し、シード固定で完全再現できること
5. **弁護士ゲートを公開前のブロッキング条件とする**

## 技術スタック（正典 §14/§15）

- ロジックは `packages/` に純粋 TypeScript で置く（`sim-engine` は依存ゼロ・Node/React に依存しない）
- Web は Next.js（Vercel）/ DB・認証は Supabase / ゲームループは常時稼働ワーカー（Render）/ 静的アセットは Cloudflare R2
- **Next.js の Route Handler にビジネスロジックを書かない**（アプリ化で書き直しになる）
- TypeScript strict・`any` 禁止

## 開発体制（三者）

```
オーナー（最終判断）
  ↕
レビュー側 Claude（別ディレクトリ・正典管理と照合）
  ↓ DEV_INSTRUCTIONS_*.md を発行
開発側 Claude（このディレクトリ）… 実装 → REPORT_*.md / QUESTIONS_*.md を返す
  ↓
レビュー側が独立に再実行して照合 → REVIEW_*_VERDICT_*.md
```

- 実装は**指示書が出ている範囲のみ**。スコープ外の先走りは差し戻し
- 仕様の穴を見つけたら推測で埋めず `QUESTIONS_*.md` に出す
- リモートへの push はオーナー指示があるまで行わない
- 報告は鵜呑みにされない前提で書く（レビュー側がテストとシミュレータを再実行して突合する）

## よく使うコマンド

```bash
npm test                                        # ユニットテスト
npm run typecheck                               # strict 型チェック
npm run simulate -- --generations 100 --seed 42 # 100世代シミュレータ（V-1/V-2/V-3 判定）
npm run simulate -- --help                      # 全オプション
```
