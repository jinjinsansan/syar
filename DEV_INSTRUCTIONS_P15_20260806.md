# 開発指示書 P1.5 — NPC 世界の50世代プリシード

**発行**: 2026-08-06（レビュー側 Claude）
**正典**: `STAR_SPEC_v2.0.md`（**全17章確定済み**。特に §10.5 NPC馬世界・§6 遺伝・§13.2 検証基準）
**前提**: P1 クローズ済み。**インフラ不要**（ローカルCLIのみ・§16）

---

## 0. P1 の評価

**P-1 / P-2 / P-3 完了、K-5 全ゲート PASS を確認しました。** 特に評価するのは **V-8/V-9 についての報告**です:

> 是正前後で数字がほとんど動かなかった。**全馬に一律 −0.15 が掛かっていたので、AI代行と手動最適の「比」にも、キャップ後の分布の「範囲」にも現れない。**
> ⚠️ **裏を返すと V-8 はこの構造的破綻を検出できなかった。**

**これは「自分の作業が成功した」より価値のある報告**です。数字が動かなかったことを「効かなかった」と解釈せず、**なぜ動かないのかを構造から説明し、その結果ゲートの盲点を発見**しています。

→ 正典に反映しました:

- **V-13 を新設**（§13.2）: **仕掛けの巧拙が結果に出ること** — 最適な仕掛けと早すぎる仕掛けで `interventionMult` に有意な差があること。**壊れた状態（ゲージが常に空）ならこの差が消える**ので検出できます
- **R-16 を新設**: **比率と範囲は、全体に一様にかかる歪みを検出できない。** ゲートを設計するときは「**壊れた状態がこのゲートを通ってしまわないか**」を必ず問う

**V-13 の実装は P1.5 のついでで構いません**（スタミナ是正は済んでいるので、測れば通るはずです）。

---

## 1. P1.5 のスコープ

**サービス開始時点で「歴史のある血統」が存在する状態**を作ります。正典 §10.5:

> プリシード: サービス開始前に NPC 世界を**50世代オフラインで回す**。開始時点で5代血統表が埋まった「歴史ある血統」が存在すること — **体験の質を大きく左右する必須工程**。

### N-1【必須】NPC 厩舎の実装（§10.5）

- **40前後**の NPC 厩舎を用意し、**個性を持たせる**（短距離特化・ダート専門・晩成型・道悪巧者 など）
- **冠名**（牧場ごとの接頭辞）を持たせる → 「あの牧場の血は買い」というプレイヤー知識＝**メタ**が生まれる
- 厩舎方針は**調教AIの選択に反映**される（能力バランス型 / SP偏重 など）
- **馬名は音節結合方式で自動生成**し、**実在競走馬名 NG リストと突合**（憲法 §0.1）。**`rg -uu` で検閲**すること

### N-2【必須】50世代のプリシード実行

- **NPC 馬はプレイヤー馬と同一の遺伝エンジンで生成**する（§10.5）。専用の簡易ロジックを作らない — **同じ土俵にいることが公正性の担保**
- プールサイズ: **現役 2,500頭 / 種牡馬 200頭 / 繁殖牝馬 800頭**
- 引退後、成績上位は自動で種牡馬・繁殖牝馬に上がる
- 出力は**再現可能な成果物**（シード固定・同じシードから同じ血統プールが出ること）

### N-3【必須】能力分布の追従（§10.5）

```
NPC平均 = プレイヤー上位30%の平均 × 0.92（週次で再較正）
```

- **固定値にするとプレイヤーが育つほど NPC が無価値**になり、**強すぎる固定だと新規が勝てない**
- **係数0.92と週次という緩やかさを守る**（追従が強すぎると成長実感が消える）
- **プリシード時点ではプレイヤーが存在しない**ので、初期値の決め方を提案してください（P0 の創始定義から出すか、別の基準か）

### N-4【必須】血統表が5代埋まっていることの検証

**プリシードの成果はここで測ります。**

- サービス開始時点の NPC 馬について、**5代血統表が埋まっている割合**を測る
- 種牡馬プールに**複数の父系ライン**が存在し、偏っていないこと
- **ニックス（§6.6）が意味を持つ程度に系統が分散している**こと

---

## 2. 合格基準

| # | 基準 |
|---|---|
| 1 | **P0 の全ゲート（V-1/V-2a〜f/V-3）がプリシード後の集団でも成立する** |
| 2 | **5代血統表が埋まっている**（割合を報告。100%でなくてよいが、埋まらない理由を説明できること） |
| 3 | 種牡馬プールの**父系ラインが分散**している（1系統に偏っていない） |
| 4 | **同じシードから同じ血統プールが再現**できる |
| 5 | **実在競走馬名がゼロ**（`rg -uu` で検閲・コマンドを報告に書く） |
| 6 | **V-13 の実装**（P1 からの持ち越し・ついでで可） |
| 7 | テスト全PASS・typecheck exit 0・`any` ゼロ・変異試験すべて防御 |

---

## 3. 注意（正典より）

- **genotype を変える変更はここより前に済ませること**（§16.2）。プリシード後に形質を増やすと**血統プールを作り直す**ことになります。`heavy_aptitude` は P-1 で入ったので済んでいます
- **較正定数を新設したら登録簿（`calibration.ts`）に、測定条件なら `measurement.ts` に**（P1 で確立した3分類・§16.3）
- **R-15**: 新しい機構は「入っているが使われない」状態で一度コミットすると、接続前後で対照が取れます

---

## 4. 報告様式

`REPORT_P15_[日付].md` に:

1. 実装したもの
2. 再実行コマンド
3. **検証結果**（合格基準1〜6・複数シード）
4. **NPC 厩舎の個性の一覧**（何種類・どう分けたか）
5. **N-3 の初期値の決め方とその根拠**
6. 定数の調整履歴（較正定数／測定条件のどちらかを明記）
7. 仕様からの逸脱・解釈（**全件・範囲を明記**）
8. 未解決の質問

**P2（開催スケジューラ＋馬券）の指示書は別途出します。** P1.5 と並行して読んでも構いませんが、**着手は P1.5 の完了後**にしてください（P2 で初めてサーバーを立てるので、ローカルで固められるものを先に固めます）。
