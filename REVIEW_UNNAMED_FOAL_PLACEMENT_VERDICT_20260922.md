# 【STAR】裁定: 命名前の仔の置き場所と命名の検査（D-120 ⑥ N-2・N-3）— 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_UNNAMED_FOAL_PLACEMENT_20260922.md`（開発側 `star-aa`・`4fd72d5`）
- 運用: オーナー指示（2026-09-22）「承認不要で自立的に進める」に基づいて発行する

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| 1 | N-2 置き場所 | ✅ **(i) 別の表（下書き）**。条件 4 つ |
| 2 | N-3 命名の検査 | ✅ **(α) 命名も要求を積み、ワーカーで検査**。★ただし**重複の最後の砦を DB に置く**（(β) の良い所だけ採る） |
| 3 | 表の分け方 | ✅ 開発側の推奨どおり。**要求の表は 1 つ（`kind` で分ける）、下書きの表は別** |
| 4 | 🔴 NG 名リストが無い間の命名（照会に無かった点） | ⚠️ 「検査していない」と明示するだけでは足りない。**リストが来たら付けた名前を検査し直せる印を、馬の行に残す** |
| 5 | ⚠️ 命名が 78 週より遅れた場合（照会に無かった点） | ⚠️ 開発側の案（追いつきで処理）を採る。★ただし**追いつきで EP がまとめて引き落とされる**ことを画面に出す |

---

## 1. N-2 — ✅ (i) 別の表

✔ [CODE] で確かめた: `horses` を読む製品コードはワーカー 8 ファイルと移行 28 本のビュー・関数（照会 §1）。(ii) の旗で隠す案は、**読む側すべてに絞り込みを足す**形で、1 か所漏れれば出走・一覧・NPC の配合相手に紛れ込む。今後 `horses` を読む処理を書くたびに同じ罠が残る。★**「足し忘れたら漏れる」形より、「構造上そこに居ない」形を採る**（D-119 の手当て「旗を立てないと通れない形」と同じ考え）。

**条件**:
1. **下書きの表にも `unique (dam_id, birth_week)` を置く。**（開発側の指摘どおり。`0054` の一意は `horses` にしか効かない）。★それに加えて、**下書き と `horses` の両方をまたいで「同じ母・同じ週に 1 頭」**でなければならない。2 つの表の一意は互いに見えないので、★**最後の判定はロックの後の `canMate()`（母の `bred_this_year`）が担う**。試験は、NPC の経路が `horses` に、プレイヤーの経路が下書きに、同じ母・同じ週で 1 頭ずつ書こうとする形で書く（前の裁定 §2 のとおり）
2. **母の `foal_count` が 1 増えるのに、`horses` に子が居ない**状態ができる。✔ いま `foal_count` と子の数を突き合わせる検査は無い（`grep` で読む側はビュー `0034`・`0040` の列だけ）。★今後そういう検査を作るときは「`horses` の子 ＋ 下書き」で数えること。**下書きの表の註記にこれを書いておく**（書かないと、未来の検査が下書きを見落として「数が合わない」を出す）
3. **下書きの表はクライアントから直接読み書きしない**（`revoke all … from anon, authenticated`・V-20 の登録簿に `closed`）。本人が読む口は RPC かビューにし、**性別・父・母・誕生週だけ**を出す。`genotype` / `potential` / `stats` を列に出さない（D-114）。★**検査は「入力の形」で書く**（出す列の一覧を構文木か `pg_views` の定義で見る・D-108 ③）
4. **所有上限（D-120 ③）に下書きを数える**（照会どおり）。数え方は `ownership.ts` の 1 か所

## 2. N-3 — ✅ (α) ＋ DB に重複の最後の砦

✔ [CODE]: 正規化は `packages/sim-engine/src/naming.ts:90` `normalizeName()`（NFKC・長音・中黒・空白・アポストロフィ・ハイフンを落として大文字化）。禁止名は `apps/cli/src/name-blocklist.ts`（ハッシュ表が無い）。**どちらも TS にしかない**。SQL に写すと出どころが 2 つになる（D-052）→ (α) が正しい。

**ただし (α) だけだと**、重複の判定はワーカーの中の「読んでから書く」だけになる。ワーカーは 1 本で順に処理するので今は競合しないが、★**ワーカーが 2 本になった日、または別の経路（道具・移行）が馬名を書いた日に、黙って重複が入る**。

**条件**:
1. **ワーカーが `normalizeName()` の結果を `horses` の列（例: `name_key`）に書き、DB が `unique` で止める。** これは正規化を SQL に写すのではなく、★**TS が計算した値を DB が比べるだけ**なので D-052 に反しない（`0054` の一意が配合の最後の砦であるのと同じ形）
2. ★**一意は NPC・引退馬を含む全頭に効かせる。** そのために既存の全行に `name_key` を埋める道具（TS で `normalizeName()` を呼ぶ）が要る。🔴 ★**一意を張る前に、既存の名前どうしの正規化後の衝突を数える**（数えずに張ると移行が落ちるか、落ちないよう衝突を黙って除外する形になる）。staging で数え、本番は読むだけで数える
3. 画面の文字数判定は即時でよい（照会どおり）。サーバーで必ず再判定する
4. 名前の可否が出るまでの待ち（最大 60 秒）は、配合の待ちと同じ扱い（前の裁定 §1）

## 3. 表の分け方 — ✅

- 要求の表を 1 つにし、`kind = 'breed_initial' | 'breed' | 'name'` で分ける。冪等キーの 2 段（前の裁定 §2）・理由の書き戻し・読む口を使い回せる
- ★業務上の一意は `kind` ごとに違うので、**部分一意を `kind` ごとに張る**（`breed_initial` は利用者ごとに 1 件、`name` は下書きごとに成功 1 件）。1 つの一意で全部を表そうとしない
- 下書きの表は別（§1）

## 4. 🔴 NG 名リストが無い間の命名 — 照会に無かった点

- 照会の案は、リストが無い間は「禁止名の検査をしていない」ことを**要求の結果に**明示して通す、というもの（前の裁定 §5-4 のとおり）。★これは正しいが、**結果の行に書くだけでは、リストが届いた後に何も起きない**
- 🔴 リストが届く前に付けられた名前は、**実在の競走馬名との突合を一度も通っていない**。これは D-119 ④（`ALLOW_ALL_NAMES` で突合が 1 度も走っていなかった）と同じ形であり、しかも今度は**利用者が付けた名前**が対象になる（権利方針 §0.1・弁護士ゲート L-7）
- **条件**:
  1. **馬の行に「どの版のリストで検査したか」を残す**（例: `name_checked_with` にリストのハッシュ、無ければ null）
  2. **リストが届いたら、null の行を全部検査し直す道具を用意する**（当たった場合の扱い＝改名を求めるか等は、そのとき決める。★今は印を残すことだけを求める）
  3. ★**null の行が 1 件でもある状態を「弁護士向けの構成」と呼ばない**（09-20 の `nameCheckSkipped: true` の世界と同じ扱い）

## 5. ⚠️ 命名が 78 週より遅れた場合 — 照会に無かった点

✔ [CODE]: 持ち主のいる馬は、指示が無ければ既定の献立で調教され、**EP が引き落とされる**（`apps/worker/src/training-runner.ts:345-358`・`spend_training_ep`）。EP が足りなければ休養に落ちる（`:361-365`・Q-P3-23）。

- 照会の案（`last_processed_week` を誕生週 ＋ 78 のまま `horses` に入れ、欠けた週を追いつきで処理）は、**NPC の仔・他の持ち馬と同じ規則**なので採る（D-120 ③「初回だけの特例を作らない」）
- ★ただしその結果、**命名した瞬間に、欠けた週ぶんの調教費がまとめて引き落とされる**（または EP が尽きて休養に落ちる）。利用者から見ると「名前を付けたら EP が減った」になる
- **条件**: ①命名の画面で、**78 週を過ぎているときは「命名すると、それまでの週の調教がまとめて行われ、調教費がかかる」ことを出す**（額は出さなくてよい。★出すならワーカーと同じ式から）②実際に 78 週を過ぎるのは実時間で 13 日放置した場合だけなので、★**急がない**。I-2 の便では画面の文言の要件として記録し、デザイナーの成果物で入れる

## [CODE] と [EYES]

全部 [CODE]。★既存の馬名の正規化後の衝突件数（§2 条件 2）は**未測定**。
