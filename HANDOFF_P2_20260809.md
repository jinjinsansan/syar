# 申し送り — P2 進行中（2026-08-09）

**開発側 Claude → オーナー / 次セッション**
**最新コミット**: `149a44a` 以降（すべて push 済み）

---

## 0. いま動いているもの（席を外している間も動きます）

| | 場所 | 状態 |
|---|---|---|
| **ゲームループ** | VPS `162.43.29.102` / systemd `star-worker` | **稼働中**。10分ごとにレース生成・確定・払戻 |
| **A-1 の24時間計測** | 同上（2026-08-08 20:53 開始） | **継続中**。約20時間後に判定 |
| **A-3 の10シード再測定** | このPC（バックグラウンド） | **実行中**。`/tmp/a3p.log` に出力。約1〜2時間 |
| **公開サイト** | Vercel | デプロイ済み・Vercel SSO で保護中 |

★**PC を閉じても VPS のワーカーは止まりません**（systemd で自動起動）。
  A-3 の測定は PC 上なので、閉じると止まります。

---

## 1. P2 の到達状況

| # | 合格基準 | 状態 |
|---|---|---|
| A-1 | 24時間サイクル | **計測中**（D-034 の基準・デプロイ再起動を含んでよい） |
| A-2 | 冪等性 | **PASS**（★重複＋中途半端の両方・SIGKILL で実証） |
| A-3 | V-10 払戻率 | **再測定中**（★判定はプール推定。シードごとは誤り） |
| A-4 | seed_reveal 非露出 | **PASS**（anon で実測） |
| A-5 | EP減算と発券の原子性 | **PASS**（壊して確認） |
| A-6 | 自馬ベット制限 | **PASS** |
| A-7 | 環境ガード | **PASS** |
| A-8 | P0/P1 回帰 | **PASS** |
| A-9 | テスト・変異試験 | **PASS**（418 tests / typecheck 0） |

**A-1 と A-3 以外はすべて実測 PASS**です。

---

## 2. 実装できているもの

```
レース生成（10分ごと）
  番組表 §10.3 → 条件 §10.4 → 出走表（P1 較正済みロジックを再利用）
  → オッズ §9.2（MC 10,000回・本番確定とは別系列）
  → すべて同一トランザクション（半端な生成が残らない）
       ↓
馬券購入 §9（place_bet RPC）
  EP 減算と発券が原子的・冪等キーつき・自馬制限・§9.4 の3種の上限
       ↓
確定 §8.6/§8.7
  final_seed = HMAC(server_seed, race_id + client_entropy)
  着順は実際の能力で決まる・seed_reveal を公開
       ↓
払戻 §9 ＋ 賞金 §11.1
  EP で買い PP で払い戻す（憲法 §0.2）
  賞金は PP の主な発行源（§9.3）・NPC には払わない
       ↓
日次集計 §4.6/§11.2 ＋ 景品交換 §11.3
```

**公開画面**（認証なし・Next.js / Vercel）
- `/` 番組表
- `/races/[id]` 出馬表・オッズ・人気・着順（確定後のみ）・**§8.6 の検証**

---

## 3. ★出荷する世界は `seed 1231` に確定（承認済み）

再現条件は **`docs/WORLD_SEED.md`** に記録。生成条件のどれかが変わると別の世界になります。

```
penalty=0.3（D-031）/ STREAM 41〜45 / 50世代 / 合格率 30%
有効系統数 8.00（分布の最大値・+2.5 SD）/ 平均F 0.0287
```

---

## 4. 再開時にまず見るもの

```bash
# A-3 の結果（完了していれば表が出ています）
cat /tmp/a3p.log

# A-1 の進捗（VPS）
ssh -i ~/.ssh/pax_vps root@162.43.29.102 \
  "journalctl -u star-worker --since '2026-08-08 20:53' --no-pager -o cat | grep -c '生成=\[[0-9]'"

# ワーカーの健全性
ssh -i ~/.ssh/pax_vps root@162.43.29.102 "systemctl is-active star-worker; journalctl -u star-worker -n 3 --no-pager -o cat"
```

### A-1 の判定（24時間後・2026-08-09 20:53 以降）

D-034 により **「24時間にわたり欠落も重複もなく前進し続ける（デプロイによる再起動を含んでよい）」**。
報告に含めるもの:

- 生成・確定・払戻の件数（★プロセス生存では見ない）
- **最長無停止区間**と、その両端の**メモリ / 接続数**（リーク検出のため）
- 欠落・重複の有無（`cycle_index` の連番と一意性）

---

## 5. 待ち・未着手

| | 状態 |
|---|---|
| **認証** | オーナー判断で後回し。これが入るまで馬券購入の UI は作れません |
| §9.6 購入フロー（UI） | RPC は動作済み。UI は認証が前提 |
| §7 成長・調子・疲労 | 未実装のため `age`=4 / `condition`=3 / `fatigue`=0 で固定（P3 のスコープ） |
| §11.4 原資会計 | 広告収益との突合。**D2 のオーナー未決事項**に依存 |
| 景品カタログの中身 | **D2 のオーナー未決事項**。構造だけ作り、品目は入れていません |

---

## 6. ★正典の自己矛盾を3回報告しました

実装して測って初めて出る種類で、**使う側からしか見えない位置**にありました。

| # | 内容 | 解決 |
|---|---|---|
| 1 | D-028（系統多様性の高い世界を採る）× D-032（F の低い世界を弾く） | D-033 |
| 2 | 指示書 §3「プロセス生存では見ない」× 合格基準「無人で24時間」 | D-034 |
| 3 | §10.3 の重賞 週63枠 × 週次頻度の合計 31 | D-030 |

---

## 7. 手順として身についたこと（次セッションも守る）

- **報告書の冒頭に「本便で正典の改訂が要る決定をしたか」を必ず書く**
  （世界シードを無報告で本番投入した件の再発防止。欄が1つあれば止まる）
- **比べる前に定義を揃える**（3度間違えた: `potential` と `stats` / 40シード分布と30シード探索 / 種牡馬プールと現役世代）
- **既知の値を投入して確かめる**（0 は「動いていない」であって「正しい」ではない）
- **原因を推測しない**（A-3 で7回失敗。推測を止めたら判定方法の誤りが見つかった）
- **`vitest` は単独実行して終了コードで判定**（`| grep` すると失敗を素通りする）
- **プロセスを止めたら残数ではなく対象が消えたことを確認**

---

## 8. 秘密の扱い

- `secrets.local.env`（このPC）/ `/etc/star/worker.env`（VPS）/ Vercel の環境変数
- **すべて `.gitignore` 済み。値をチャットにも出していません**
- Vercel には **anon キーのみ**（`service_role` は RLS を素通りするので置かない）
- `git.txt` は**読んでいません**（CLAUDE.md の指示どおり）
