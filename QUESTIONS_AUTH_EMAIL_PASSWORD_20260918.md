# 照会（認証・開発期間中をメール＋パスワードにする）— 2026-09-18

**宛先**: レビュー側
**発行**: 開発側
**関連**: 正典 **D-076**（認証を LINE ログインに変更する）/ **D-077**（複垢抑止を経済の防御に使わない）/ **D-078**（`user_identities`）/ **D-075**（初期 EP・日次上限）/ **§13.2 V-19**（16 項目）/ **§15.6**（認証を Supabase Auth に統一）/ **§14.6**（環境分離）
**未裁定**: **Q-AUTH-08** / **Q-AUTH-09**

---

## 0. 何が起きたか（オーナー決定と、それが正典と食い違う点）

2026-09-18、オーナーから次の指示がありました（逐語）:

> 「認証は googleとappleではなく 最初は開発中なのでメールアドレスとパスワード登録にします。どう思いますか？」

開発側が **D-076 と正面から食い違うこと**を申し上げたうえで、**「正典の決定として起票してよいか」**を伺い、**オーナーから承認**を得ました（同日・「B OK です」）。本書はその起票のための照会です。

### 🔴 食い違いの本体

**D-076 は「認証を LINE ログインに変更する」と決定しており、その根拠の筆頭が「パスワードを持たない」です。**

> マジックリンク裁定の3理由（**パスワードを持たない**／摩擦が低い／RLS と素直に繋がる）は LINE でも失われず、複垢コストが上がる利点が乗る

**「パスワードを持たない」は、2 度の裁定（マジックリンク → LINE）で連続して採用理由になっています。** メール＋パスワードは **その 1 本目を明示的に捨てる**ことになるため、**開発側の判断では決められません**。

---

## 1. 🔴 Q-AUTH-08 — 開発期間中の認証をメール＋パスワードにする（D-113 案）

### 1.1 起票の案文

> **D-113: 開発期間中の認証をメール＋パスワード（Supabase Auth）とする**（オーナー決定 2026-09-18・照会 Q-AUTH-08）。
> **D-076（LINE ログイン）を撤回しない。「開発期間中の当座の経路」として並置し、公開前に「LINE に戻す／併設する／メールも残す」を再判断する。**
>
> **採る理由**: ①**第三者の審査に依存しない** — D-076 自身が「★単一障害点であることを受容する（LINE のチャネル審査／規約で拒否されれば認証が丸ごと死ぬ）」と書いている。**開発中に審査で止まるのは割に合わない**。②**Supabase Auth がそのまま持っており、D-076 の制約 ①②③ を全て守れる**（自前で JWT を発行しない／セッションは Supabase Auth に発行させる／`auth.uid()`・RLS・書き込み RPC を変更しない）。③**`user_identities` の設計（D-078）を作り直さずに済む**（`0017_user_identities.sql` の `provider` は `text` で値の制限が無く、移行は不要。✔ 実測）。
>
> **★捨てるもの（明記する）**: **「パスワードを持たない」という D-076 の採用理由の 1 本**と、**「複垢コストが上がる」という LINE の利点**。
>
> **★条件**:
> ① **開発期間中に限る。** 公開（P6・弁護士ゲート）より前に再判断する。**「当座」を書かずに恒久の決定にしない。**
> ② **D-076 の制約 ①②③ をそのまま引き継ぐ**（自前 JWT を発行しない・セッションは Supabase Auth・`auth.uid()` / RLS / 書き込み RPC を変更しない）。**④（Supabase Edge Function で実装）は Q-AUTH-08-c の裁定に従う。**
> ③ **メールアドレスを `user_identities.subject` に入れない**（V-19 #6・`0017` の註記「`subject` に入れてよいのはプロバイダの `sub` だけ」）。
> ④ **メール確認（confirm）を必須にする。** 確認前は書き込み RPC を通さない（D-080 の未セットアップ状態と同じ扱い）。**確認を切れば検査が通る形にしない**（R-16）。
> ⑤ **V-19 を経路ごとに書き換える**（Q-AUTH-09）。**16 項目のうち 7 項目は OIDC 固有で、メール＋パスワード経路では空振りする。**
> ⑥ **D-075 の額（初期 EP 2,000・初期馬 1 頭）を変えない。** 下の 1.2 の理由により、**額で複垢に対抗しない**（D-077 の立場そのまま）。

### 1.2 🔴 見つけた重い論点 — 複垢の入口の摩擦が実質ゼロになる

**メール＋パスワードは、使い捨てメールで無限に口座が作れます。** LINE を選んだ利点のうち「複垢コストが上がる」は**完全に失われます**。

**★ただし、正典の立場は破れません。** D-077 が既にこう書いているためです:

> **★複垢抑止を経済の防御に使わない。** LINE ログイン（D-076）は複垢の**摩擦**を上げるが、**iOS では第2経路が必ず開き**、アカウント再作成でも `sub` は変わる。**「入口が効いている」前提で額を決めると、iOS 版を出した瞬間に、どのゲートも鳴らずに前提が崩れる。**

**D-077 は「入口を当てにするな」と先に言っており、メール＋パスワードはその前提を強めこそすれ、崩しません。** 守りは ① EP 日次総発行量の上限 ② 重複の観測 ③ **★景品交換（§11）の出口で一意性を担保** の 3 本であり、**いずれもログイン方式に依存していません**。

**★開発期間中は出口（景品交換）が動いていないので、複垢の利得は実現しません。** ①「開発期間中に限る」という条件が、この論点を丸ごと吸収します。**公開前の再判断で、ここを必ず読み直してください。**

### 1.3 裁定をお願いしたいこと

| | 問い | 開発側の見解 |
|---|---|---|
| **Q-AUTH-08-a** | **上の D-113 案文でよいか**（特に「D-076 を撤回せず並置する」形） | **並置を推します。** 撤回すると、公開時に LINE を選んだ理由（複垢コスト・審査の要否）を**もう一度ゼロから議論**することになります |
| **Q-AUTH-08-b** | 🔴 **`user_identities` に `provider='email'` の行を作るか、作らないか** | **下の 1.4 で 2 案を出します。開発側では決めません** |
| **Q-AUTH-08-c** | **実装を Supabase Edge Function に置くか**（D-076 ④） | **置かなくてよいと考えます。** メール＋パスワードは **Supabase Auth のクライアント SDK が直接扱う**ため、**間に自前の関数を挟む必要がそもそもありません**。D-076 ④ が Edge Function を求めたのは「LINE の ID トークンを検証してセッションを発行する」自前の処理が要ったからで、**その処理が存在しない以上、④ の理由が成立しません**。⚠️ ただし **LINE を足す便では ④ が復活します** |
| **Q-AUTH-08-d** | **パスワード再設定のメール送信をどうするか** | **Supabase の既定の送信は開発中の人数なら足ります**が、**人を入れるなら SMTP の手配が要ります**（オーナー手配事項。D-042 と同じ形） |

### 1.4 🔴 Q-AUTH-08-b の 2 案 — `user_identities` に行を作るか

**これは D-078 の目的に照らして決まる問題で、開発側の判断を超えています。**

D-078 が `user_identities` を置いた目的は「**外部プロバイダの `sub` → 口座**の対応を持つこと」でした。**メール＋パスワードには外部プロバイダの `sub` が存在しません** — `auth.users.id` そのものが識別子です。

| | 案 | 内容 | 利点 | 欠点 |
|---|---|---|---|---|
| **(a)** | **行を作らない** | メール経路では `user_identities` に触れない。`auth.users` の**メールの一意制約**が「同一メールで 1 ユーザー」を担保する | **担保の出どころが 1 か所**（D-052）。`subject` にメールを入れる誘惑が構造的に生じない（V-19 #6） | **`user_identities` を見ても「この口座がどの経路で入ったか」が分からない**。LINE 併設時に経路の混在が読めない |
| **(b)** | **`provider='email'`・`subject=auth.users.id` の行を作る** | 経路の記録として 1 行置く | 経路が読める。**`user_identities_user_provider_uniq`（user_id, provider）が効き、「1 人 1 メール経路」が DB 側で担保される** | 🔴 **主キー `(provider, subject)` が自明に一意**（`subject` が `auth.users.id` なので、外部キーと同じ値）。**「制約はあるが何も担保していない」形**で、D-078 が V-19 #5 について警告した「**存在するのに働かない制約**」（R-16）と**同じ姿**になる |

**開発側は (a) を推します。** (b) の制約は**見た目だけ働いている**状態で、**D-078 と `0017` の註記が繰り返し警告している形そのもの**だからです。ただし **(a) は「経路が読めない」という実務上の不便**を残すので、**裁定をお願いします**。

> ⚠️ **(a) を採る場合でも `0017` は残します**（LINE / Apple 併設のため）。**移行は不要です。**

---

## 2. 🔴 Q-AUTH-09 — V-19 の 16 項目を経路ごとに書き換える

### 2.1 なぜ書き換えが要るか

V-19 は既に「**★経路ごとに課す**（web ログイン＝HS256 / ネイティブ SDK・LIFF＝ES256。**片方の合格は他方の保証にならない**）」と書いています。**メール＋パスワードは 3 本目の経路です。**

🔴 **問題は「項目が増える」ことではなく、「16 項目のうち 7 項目が、この経路では何も測らない」ことです。**

**そのまま流すと、7 項目が「該当なし」で素通りし、★16 項目のうち 9 項目しか働いていないのに『V-19 合格』と出ます。** これは V-19 の表題「**認証が壊れたときに落ちること**」に対して、**ゲートが静かに緩む**形です（R-16）。

### 2.2 現行 16 項目の棚卸し（メール＋パスワード経路）

| # | 現行の項目 | この経路で | 扱い |
|---|---|---|---|
| ① | 署名改竄 → 拒否 | **空振り**（ID トークンを自前で検証しない） | **経路対象外と明記** |
| ② | `aud` 相違 → 拒否 | **空振り** | 同上 |
| ③ | 期限切れ → 拒否 | **空振り**（※Supabase のセッション期限は別。⑨ が見る） | 同上 |
| ④ | `state` 不一致 → 拒否 | **空振り**（リダイレクトが無い） | 同上 |
| ⑤ | 同一 `sub` で2回ログイン → ユーザーは1つ | **読み替え**: **同一メールで2回登録 → ユーザーは1つ** | **読み替えて残す** |
| ⑥ | メール一致・`sub` 相違 → 別ユーザー | 🔴 **意味が反転する**（メールが識別子そのもの） | **下の 2.3 で扱う** |
| ⑦ | `nonce` 不一致 → 拒否 | **空振り** | **経路対象外と明記** |
| ⑧ | `iss` 相違 → 拒否 | **空振り** | 同上 |
| ⑨ | **検証を通らずにセッションが発行できないこと** | ✅ **そのまま有効・最重要** | **残す** |
| ⑩ | `sub` の一意制約が DB 側にあること | **読み替え**: **メールの一意制約が DB 側にあること** | **読み替えて残す** |
| ⑪ | **本番経路で固定すること** | ✅ **そのまま有効** | **残す** |
| ⑫ | 検出器が自分自身を検出していないこと（R-14） | ✅ **そのまま有効** | **残す** |
| ⑬ | 同時ログインで auth ユーザー1・identity 1行・口座1 | ✅ **そのまま有効**（identity 行は Q-AUTH-08-b の裁定次第） | **残す** |
| ⑭ | セットアップ RPC を2回 → 口座・初期 EP・初期馬が1つだけ | ✅ **そのまま有効** | **残す** |
| ⑮ | → V-20 に統合済み | — | — |
| ⑯ | **ヘッダの `alg` で分岐しない** | **空振り** | **経路対象外と明記** |

**空振り 6・反転 1・読み替え 2・そのまま 5。**

### 2.3 🔴 ⑥ の反転をどう扱うか

現行 ⑥ は「**メール一致・`sub` 相違 → 別ユーザー**」で、その理由は「**LINE のメールは任意項目かつ未検証。突合は必ず `sub`**」でした。

**メール＋パスワード経路では、メールが識別子そのものです。** ⑥ をそのまま課すと**逆のことを要求**します。

**開発側の案**: ⑥ を**経路ごとに 2 つに分ける**。

- **⑥-oidc（LINE / Apple）**: メール一致・`sub` 相違 → **別ユーザー**（現行のまま）
- **⑥-email**: 🔴 **メール経路で作った口座に、後から同じメールの LINE アカウントで入れないこと** — **メール一致を根拠に口座を結合しない**（併設時に効く。**結合してしまうと、LINE 側のメールは未検証なので他人の口座に入れる**）

**★⑥-email は併設した瞬間に効く項目で、いま書いておかないと、併設の便で誰も思い出しません。**

### 2.4 ★足す項目（メール＋パスワード固有）

**全て「壊れたときに落ちること」の形で書きます**（V-19 の表題。「正しいパスワードで入れる」は数えない）。

| # | 項目 | なぜ要るか |
|---|---|---|
| **E-1** | **★総当たり／詰め込みに率の制限があり、超えたら拒否されること** | **パスワードを持つ経路の主たる攻撃。LINE には無かった** |
| **E-2** | **★再設定リンクが 1 回限りで、期限切れなら拒否されること** | **再設定リンクは実質パスワードそのもの。使い回せれば乗っ取り経路** |
| **E-3** | 🔴 **★再設定の経路でメールアドレスを変更できないこと** | **できると、乗っ取った後に本人を締め出せる**（口座は PP＝景品価値を持つ・§11） |
| **E-4** | **★パスワード変更・再設定の後、既存のセッションが失効すること** | **失効しないと、乗っ取り側のセッションが生き残る。「パスワードを変えたのに追い出せない」** |
| **E-5** | **★漏洩済みパスワードが拒否されること** | **使い回しが最も現実的な侵入経路**。Supabase が持つ機能で満たせる |
| **E-6** | 🔴 **★メール確認前に書き込み RPC が通らないこと** | **確認が無いと他人のメールで登録でき、後からその人が正規に登録できない**（占拠）。**D-080 の「未セットアップは明示的に拒否」と同じ形** |
| **E-7** | **★パスワードの最低要件を満たさないものが拒否されること** | **要件を緩めれば E-1 の率の制限だけが頼りになる** |

> ⚠️ **E-1・E-5・E-6 は Supabase の設定で切り替わります。** ★**「設定を切れば検査が通る」形にしないこと** — **設定の現在値を読んで判定する**（R-16。V-19 #11「本番経路で固定する」と同じ要請）。

### 2.5 裁定をお願いしたいこと

| | 問い | 開発側の見解 |
|---|---|---|
| **Q-AUTH-09-a** | **2.2 の棚卸し（空振り 6 を「経路対象外」と明記する）でよいか** | **明記を推します。** **黙って飛ばすと『16 項目合格』と出て、実は 9 項目しか働いていません** |
| **Q-AUTH-09-b** | **2.3 の ⑥ の分割（⑥-oidc / ⑥-email）でよいか** | **推します。併設の便で必ず要ります** |
| **Q-AUTH-09-c** | **2.4 の E-1〜E-7 でよいか。足りないもの・過剰なものは** | **E-3・E-6 が特に重いと考えます**（口座が PP を持つため） |
| **Q-AUTH-09-d** | **V-19 の「16項目」という数え方をどう書き換えるか** | **「経路ごとに、その経路で有効な項目すべて」と書くことを推します。** **数を固定すると、経路が増えるたびに数が嘘になります** |

---

## 3. いま実装されているもの・いないもの（事実）

**★推測ではなく、この作業ツリーを読んだ結果です。**

| | 状態 |
|---|---|
| `db/migrations/0017_user_identities.sql` | ✅ **存在する。`provider` は `text` で値の制限が無く、`'email'` がそのまま入る**（移行不要） |
| `supabase/functions/` | 🔴 **存在しない。認証の実装は 1 行も無い** |
| `apps/web/src/lib/supabase.ts` | **読み取り専用の anon クライアントのみ**（セッション管理なし） |
| `apps/web/src/lib/setup.ts` | **デモデータを返す。`SetupRepo` の差し替え口は用意済み**（画面を触らずに RPC へ繋げる） |
| `tools/verify-v19-db.mjs` | ✅ **存在する。#5・#6・#10・#15 と D-078 の参照先を実測する**。⚠️ **既に内部で email/password の Supabase ユーザーを作っている**（`PASSWORD` 定数） |
| V-19 の ①〜④・⑦〜⑨・⑪〜⑭・⑯ | 🔴 **道具が無い**（トークン検証側。Edge Function が無いため書きようがない） |

---

## 4. ★この照会が止めているもの

**開発側は、裁定が出るまで認証の実装に着手しません。**

- **D-076 が「LINE」と書いてある以上、メール＋パスワードを書くことは正典違反です**（CLAUDE.md「正典にない仕様を推測で埋めない」）
- オーナーの承認は得ていますが、**正典の改訂はレビュー側の発行物**です（本リポジトリの全 D 起票が「受領: …／★開発側の変更ではありません」の形）

**★裁定が出たら、その順で実装します。V-19 の書き換えが先で、実装が後です**（測れないものを効かせない・D-112 ③ と同じ形）。

---

## 5. 併せてオーナーにお伝えした事項（レビュー側への共有）

| | |
|---|---|
| 🔴 **push が通らない** | `~/.gitconfig` の `credential.https://github.com.helper` が **GitHub CLI に固定**され、その CLI は `sevendaysderby` で認証している。リポジトリ `jinjinsansan/syar` に対し **`push: false`**（API で実測）。**未 push 20 本** |
| **`main` へは早送りできる** | `origin/main` は現ブランチ HEAD の**祖先**（`git rev-list --left-right --count origin/main...HEAD` = `0 108`）。**マージコミット不要** |
| 🔴 **本番ワーカーの自動配備が不明** | `render.yaml` も GitHub Actions も**リポジトリに存在しない**。**Render 側の設定は読めない**。⚠️ **本番ワーカーは D-071 より前のエンジン**（D-106）で、**いまのエンジンを配備すると 10 分サイクルに入らない恐れ**がある。**`main` へ push する前に、Render の自動配備が有効かを確認する必要がある**（オーナー手配事項） |
| **移行の積み残し** | 本番へ `0021`・`0022` が未適用。`0023`→配備→`0024`→`0028` の順序制約。**`0029_story_daily` はどこにも未適用**。⚠️ **画面を見せるだけなら移行は不要**（`/odds` 以外は画面内の値で動く） |
