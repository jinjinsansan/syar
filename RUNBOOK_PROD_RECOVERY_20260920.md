# 本番を仕上げる手順（1 枚）

> 2026-09-20・開発側。**まだ 1 つも実行していません。**
> 🔴 **オーナーの直接の指示が来るまで、②以降に進みません。**

## 0. いまの本番（実測・2026-09-20）

| | 版 | 実測 |
|---|---|---|
| **画面**（Vercel） | **2026-09-18**（`02fda8a`・`main`） | `/api/healthz` ✅・3 ページ **HTTP 200**・環境変数 **あり** |
| **DB / ワーカー**（VPS） | **2026-08-20** | 移行 **20/53 適用・33 件 未適用** |

- 馬 **7,355 頭**・`birth_week` 持ち **0 頭**・`horse_week_log` **0 行**・引退 **0 頭**
- レースは **6,066 本** 走っている（★2026-08-20 のエンジンで）

## 🔴 先に要る許可（★4 つ・★いずれも戻せません）

| # | 操作 | 戻せるか |
|---|---|---|
| **A** | 移行 **33 件**を本番に当てる | ❌ 移行に下りは書かれていない |
| **B** | `seed-world.mjs` を本番に流す（**`delete from horses` から始まる**） | ❌ 前の世界は戻らない |
| **C** | ワーカーを配備する（`tools/deploy.sh`） | ⚠️ 旧リンクに戻せば戻る |
| **B′** | 先に **staging** で通しで演習する（③‴） | ❌ staging の世界は戻らない（★本番より安い） |

### 🔴 オーナーにしかできない段

| 段 | なぜ |
|---|---|
| **③′ ワーカーを止める** | **VPS に入る**（このセッションは入りません） |
| **⑥ 配備** | 同上（`tools/deploy.sh` は VPS 上で動きます） |

⚠️ **B は本番の 7,355 頭と 6,066 レースの履歴を消します。**
消える前に何か残すなら、**この手順に入る前**に決めてください。

---

## 🔴 止まる場所（★3 つ）— ★**続けないこと**

> ★レビュー側が承認したときの条件（2026-09-20）。**「たぶん大丈夫だから進む」を一度もしない。**
> **戻せない段が 3 つ あります。**

| # | これが起きたら | 止めて報告 |
|---|---|---|
| **①** | ①′（演習）で **消せない** ／ **時間が見込みの 10 倍** ／ 判定 3 つのどれかが出ない | 🔴 その場で止める |
| **②** | ③（O-7 再確認）が **②の後も不合格** | 🔴 ④に進まない |
| **③** | ⑤ で **現役が 2,400 から大きく外れる** ／ **引退が 0** ／ **予言が逆に動く** | 🔴 ⑥に進まない |
| **④** | 🔴 `verify-constitution.mjs` の **C-4 が不合格** | ⚠️ **作り直しは進めてよい／弁護士には見せない**（下記） |

### 🔴 ④ — **実在競走馬名の NG リストが繋がっていません**（2026-09-20 実測）

```
data/ng-names.hash        … ★在りません（data/ ごと無い）
tools/seed-world.mjs      … ★blocklist: ALLOW_ALL_NAMES を渡している
tools/verify-world.mjs    … ★同上
preseed-run / -verify     … ★loadNameBlocklist(undefined, false)（strict 無効）
```

- **いま本番に居る 7,355 頭の名前は、NG 判定を一度も通っていません**
- ⚠️ **過大に読まないこと**: 名前は音節を並べて作るので**実在名を参照する経路はありません**。
  危険は「**偶然の一致を拾う網が無い**」ことです
### ✅ 止める場所を 1 つ ずらしました（2026-09-20・レビュー側の裁定）

私は当初「**C-4 が緑になるまで ④ に進まない**」と書きました。**止めすぎでした。**

```
✅ 作り直しは進めてよい … ★1 分 で終わり、★誰も遊んでおらず、★何度でもやり直せる
🔴 弁護士には見せない  … ★C-4 は L-7（弁護士ゲート）の前提
🔴 印を残す           … ★その世界は「名前が未検査」と分かる形にする
🔴 届いたら作り直す    … ★リストが来たら、★もう一度 ④ を流す（★下の ④′）
```

**理由**: オーナーがリストを用意するのにどれだけかかるか分かりません（**未確認**）。
その間ずっと**本番が 1 か月 古いまま**になります。**そちらのほうが悪い。**

### ✅ 配線は済ませました（`711dbfb` の後）

```
seed-world.mjs   … ★既定で loadNameBlocklist()（★ハッシュが無ければ**投げる**）
                   ★素通しは `--allow-all-names` を**明示**しないと通らない
                   ★素通しで作った世界には印が残る（evidence/world-build/*.json の
                     nameCheckSkipped: true）
verify-world.mjs … ★同じ旗
```
⚠️ **旗が無いと素通し、ではなく、旗を立てないと止まる**（`age-horses` の `--flatten` と同じ作法）。

### ④′ リストが届いたら、もう一度 作り直す 🔴 **許可 B（再）**

```bash
npm run blocklist:build                        # ★data/ng-names.txt → data/ng-names.hash
npx tsx tools/verify-constitution.mjs          # ★C-4 が緑になること
npx tsx tools/seed-world.mjs --env production  # ★旗なし＝NG 判定あり
```
- → この世界で初めて **L-7（弁護士ゲート）に出せます**

⚠️ ③″ の**控えの時刻の空欄**は、**レビュー側が読んで確かめます。埋まっていなければ止まります。**

---

## 手順

### ① 往復を測る ✅ **済**

```
production 中央 48〜51 ms ／ staging 219 ms（read-only・select 1）
→ 追いつきの往復は約 266 回 ＝ ★本番で約 13 秒（＋ CPU 2 秒）
```

⚠️ **この数字は一度 間違えました**（3.3 時間と報告 → 往復の回数を数え直して 13 秒）。
**`update` は `unnest` で 1 文 2,000 頭**なので、1 頭 1 往復ではありません。

### ①′ 🔴🔴 **staging で通しで 1 回 演習する**（★★本番に触る前）

> レビュー側の指摘 4 件には無い段ですが、**いちばん安いのでここに置きます。**

- ②〜⑤ を **staging に対して同じ順で流す**
- 🔴🔴 **ここが ② より前にあることが要点です**（★2026-09-20・レビュー側の指摘で移しました）。
  ★もとは ③‴ に置いており、★**演習で問題が見つかったときには本番はもう触られている**形でした。
  ✔ **移せます**: staging は既に **53/53** なので、演習は ② に依存しません。**移す費用はゼロ。**
- ★staging は **53/53 適用済み**＝**移行後の本番と同じ姿**で、
  しかも **`horse_market_listing` / `horse_story_event` にも行が在る**（本番にはまだ無い）
  → ★**本番より条件が厳しい**。ここで通れば本番でも通る見込み
- ✅ ここで分かること: **本当に消せるか／所要（1 分 の見込みの答え合わせ）／判定 3 つが出るか**
- ⚠️ **staging に流すのも承認の要る操作**です（★許可 **B′**）
- ✅ ここで **自己参照（`sire_id` / `dam_id`）が 1 文の全消しで解けるか**も分かります
  （★ここだけは書かずに確かめる方法が無く、**実測していません**）

### 🔴 ①′ で**測るもの一覧**（★ここにしか書きません）

> 演習の目的は「通ること」ではなく、**本番の前に数を持つこと**です。
> 下の 6 つが出なければ、**②に進まないでください**。

| # | 測るもの | なぜ | **実測（staging・2026-09-20）** |
|---|---|---|---|
| 1 | **`race_odds` の削除に何秒か** | 1,550 万行 | ✅ **0.3 秒**（★8 表 171,344 行を `truncate` 1 文）。★**対照**: 同じ 8 表で **7,370 行 → 0.2 秒**。→ ★行数 **23 倍 で 1.5 倍**（★行に比例しない）。⚠️ ただし本番 1,550 万行 は **これの 90 倍**で、★**外挿には変わりありません** |
| 2 | **作り直し全体に何分か** | 「約 1 分」は**外挿**だった | 🔴 **6〜8 分**（生成 71〜179 秒 ＋ 投入 ＋ 追いつき 261〜276 秒）。**「1 分」は外れ。6〜8 倍**。⚠️ 生成の時間は同じ種でも 71.5〜179.3 秒 と **2.5 倍 ばらつく**（他の負荷しだい） |
| 3 | **自己参照が 1 文で解けるか** | ★書かずに確かめる方法が無い | 🔴 **落ちた**（演習 1 回目・57014 時間切れ）。`sire_id`/`dam_id` に索引が無く、7,370 行 × 全表走査 ≒ 1 億回。→ **`truncate` に変更**（行ごとの RI 検査をしない） |
| 4 | **現役 2,400 / 週の種類 156 / 週なし 0** | `POOL-CLIFF`・`PROD-NEVER-AGED` | ✅ 現役 2,388 ＋ 追いつき中の引退 12 ＝ **2,400** ／ **156 種類** ／ **0 頭** |
| 5 | **引退 4,970 / 役割なし 0** | `SEED-NOT-RETIRED` | ✅ `reason=age` **4,970** ／ 役割なし **0**。⚠️ 種牡馬・繁殖牝馬は **故障引退 12 頭の分だけ増えます**（3 回目 206/806・5 回目 **207/805**。どちらの馬が故障するかは週しだいで変わる） |
| 6 | **`mean(stats ÷ potential)`（能力値 1 つ 1 票）が 0.3149 から上がる** | 予言 3（向きだけ・**AU-7′**） | ✅ **0.3149 → 0.4367**（全馬・n=36,850）／**現役のみ 0.6902**（n=11,940）。⚠️ 最初に出した 0.6849 は**別の量**（馬ごとの和の比の平均）でした |
| 7 | 🔴 **接続が落ちずに最後まで行くか**（★2026-09-20 に追加） | ↓ | 🔴 **5 回中 1 回 落ちた**（★直した後の 1 回は落ちずに完走） |
| 8 | **追いつきが基準の週に届いたか**（★2026-09-20 に追加） | ↓ | ✅ **現役 2,388 頭 全部が `last_processed_week = 259`**（届いていない馬 0） |
| 9 | 🔴 **V-20 / O-7 を流す**（★2026-09-20 に追加・**AU-32**） | ↓ | ⚠️ **①′ では流していませんでした** — 下を読むこと |

#### 🔴 9 — **演習で流さなかった検査は、本番で初めて落ちる**（★AU-32）

- 🔴 **実例**: `0052` は staging に先に当たっていたのに、**誰も `verify-anon-exposure` を流していませんでした**。
  → `daily_run_log` が露出の登録簿に無い、が **本番へ 33 件を当てた日に初めて**落ちました（V-20 ③）。
- ✔ 露出はしていませんでした（付与の全列挙 23 件に出ない）。**未登録だっただけ**です。
- ★★**検査は在った。正しかった。流していないだけ**でした（**DP-1**）。
- ✅ 直し 2 つ:
  1. `daily_run_log: CLOSED` を登録簿へ（staging・本番とも **11/11 合格**を確認）
  2. `verify-anon-exposure.mjs --record` を足し、**`STALENESS` に載せた**（14 日で古びる）
- ⚠️ **次の演習（名前リストが来た後の作り直し）では、9 を必ず流すこと。**

⚠️ **1 と 2 は「速いはず」ではなく「何秒だったか」を書き取ること**（R-28）。
⚠️ **1 の「本番でも速いはず」は、まだ外挿です。** ②を当てた後、本番で `truncate` を流す直前に
**その場で秒数を読み、10 倍を超えたら止める**（①の停止条件）。

#### 🔴 6 — **`unlock_rate` 列は動きません**（★名前の取り違え・★2026-09-20 に直した）

- 🔴 **こちらが 2 段階で間違えていました**（★2 段目はレビュー側の指摘・`AU-21`）。
- **1 段目**: 演習 3 回目の報告で「素質開放率 0.3149 → 0.6852」と書きましたが、
  **`horses.unlock_rate` という列は育成では書き換わりません**（遺伝で決まる定数・実測 平均 **0.3148**・範囲 0.28〜0.35）。
  `training-runner.ts` が書くのは `stats` / `potential` / `fatigue` などで、**`unlock_rate` は入っていません**。
- ✅ **上がるのは「実現率」= `sum(stats) ÷ sum(potential)`** です。投入直後はこれが `unlock_rate` とほぼ同じ値になる
  （生まれた時の能力 = 素質 × 開放率）ので、**2 つの別物が同じ 0.3149 に見えていました**。
- **2 段目**: 「実現率」と言い直した後も、**「前」と「後」で別の統計量**を比べていました。
  「前」の 0.3149 は **`mean(stats÷potential)`・能力値 1 つ 1 票**（n=36,775）。
  こちらが出した 0.6849 は **馬ごとの `sum/sum` の平均**（n=2,388 頭）。**投入直後は一致するので、違いが隠れます。**
- ✅ **記録した量で測り直し**: **0.3149 → 0.4367**（全馬）／**現役のみ 0.6902**。向きは当たり。
  🔴 **全馬では 0.71 になりません**（引退 4,970 頭は意図的に追いつかせないため）。**0.713 と比べてよいのは現役のほう**。
- ⚠️ **列の名前で測ると「上がっていない」と読めます。** 予言 3 は **必ず `mean(stats ÷ potential)`（能力値ごと）** で測ること。
  → 詳細は `evidence/20260920-unlock-experiment/PREDICTION3_MEASURED_20260920.md`。`seed-world.mjs` が毎回この量を出します。

#### 🔴 8 — **進み具合の表示が、動かない数を見ていた**（★2026-09-20 に直した）

- 旧: `min(last_processed_week - birth_week)` を「いちばん遅い馬の週齢」と出していました。
  これは**いちばん若い馬の週齢**（2 歳なら 104）で、**追いつき終わっても 104 のまま**です。
- ✅ 新: **基準の週に届いていない頭数**を出します。完了の判定はこちらで見ること。


✅ 3 は**演習でしか見つからない種類の失敗**でした。①′ を ③‴ から前に移した判断が効いています。

#### 🔴 7 — **接続が落ちる**（★演習 4 回目・★本番の手順に効きます）

- **観測**: 追いつきの最中に `Connection terminated unexpectedly`。接続先は `pooler.supabase.com:5432`（Supavisor）。
- 🔴 **落ち方が問題でした**: `pg.Client` の `error` を誰も受けておらず、Node が `Unhandled error event` で**プロセスを殺します**。
  → **世界は建っているのに追いつきだけが途中で止まる**（実測: いちばん遅い馬の週齢 **78** / 基準 258）。
- ✅ **直し**: `error` を受け、**繋ぎ直して続けます**（最大 5 回・繋ぎ直しても `assertNotProduction` は毎回 通す）。
- ⚠️ **繋ぎ直したら「合格」にはしません。** 落ちた周に済んだ分は DB に残るのに戻り値には入らないので、
  **②b（追いつきの戻り値と DB の突き合わせ）は判定不能**になり、**終了コード 2** で終わります（`CK-14`）。
- ⚠️ **本番でここが落ちたら**: 世界は建っているので、**もう一度 最初から作り直す**ことになります
  （本番は `race_odds` 1,550 万行を消すところから）。**再開だけを流す口は、まだありません**
  — 作るかどうかはオーナー判断です（判定②b が独立した目でなくなるため、こちらから推しません）。

#### ★終了コードの読み方（★①′・②′ 共通）

| コード | 意味 | ②に進んでよいか |
|---|---|---|
| **0** | 全部 判定できて、全部 合格 | ✅ |
| **1** | 落ちた検査がある | ❌ |
| **2** | 🔴 **判定できなかった検査がある**（★接続断・`--no-catch-up` など） | ❌ **「合格」と読まないこと** |

### ② 移行 33 件を当てる 🔴 **許可 A**

#### 🔴 ②の前に — **稼働中の関数定義の照合**（★`0021` が自分で要求しているもの）

```bash
npx tsx tools/verify-live-function.mjs --env production \n  --function spend_training_ep --expect db/migrations/0020_rpc_setup_guard.sql
```
- `db/migrations/0021` の冒頭が **「稼働中の定義を照合してから適用すること・照合前に適用しないこと」**
  と自分で書いています。**半年 実施されていませんでした。**
- ✅ ファイル同士の照合は済み: `0021` ＝ `0020` ＋ **文書化された 2 変更だけ**
  （`assert_setup_complete()` の削除 ／ `using errcode = 'ST001'` の追加）。
- **期待する md5: `b9f344581ffdb9df47f207597ac46869`**（`0020` の再掲から計算）。
- ⚠️ 🔴 **staging の md5 と比べないこと。** staging は 53/53 で「当てた後」、本番は 20/53 で「当てる前」。
  **一致しないのが正常**で、並べると「手で書き換えられていた」と誤報します。
- 同じ出力で **接続ロール**も見えます（`0032` の `revoke ... from anon, authenticated` が
  ワーカーに効かないことの確認）。⚠️ ワーカーは間欠で繋ぐので **映るまで 2〜3 回**。
  **映らなかったことを「無害」と読まないこと**（CK-14）。

#### ✅ 33 件の走査結果 — **旧ワーカー（2026-08-20 の束）を止めるものは無い**

| 危ない形 | 判定 |
|---|---|
| `races.status` の許可リスト張り替え（0051） | ✅ 旧ワーカーが書くのは `scheduled`/`settled`/`cancelled` の 3 つだけ。新リストに在る |
| `ep_ledger.reason` の張り替え ×3（0025/26/27） | ✅ 旧ワーカーの `ep_ledger` 書き込みは 2 箇所・すべて `refund`。`prize` は **`pp_ledger` 側**だった |
| 新しい check 制約（0028/39/49/53） | ✅ すべて**新しい列**に付く。旧ワーカーは書かない（null で通る） |
| `drop column`/`view`/`function`（0036/0047） | ✅ 馬市場と利用者向け RPC。旧ワーカーは触らない |
| 旧ワーカーが呼ぶ DB 関数 | ✅ `spend_training_ep` 1 本だけ。`0021` は**同じ形** (uuid,bigint,integer) |

✅ **移行を当てると、止まるどころか直るものが在ります。** `0021` がそれで、`0020` 以降
ワーカーは `assert_setup_complete()` で毎回 例外になり、**持ち馬が全頭 休養に落ちていました**（監査 H-3）。


```bash
# ★① 下見（★何も書きません。★当たる順番と件数だけ出ます）
npx tsx tools/migrate.mjs --env production --plan
# ★② 当てる（★`--yes-production` が要ります）
npx tsx tools/migrate.mjs --env production --yes-production
```
- 🔴 **`--plan` は 2026-09-20 に足しました。** それまで**この行は存在しない機能を指していました**
  （手順書が「先に下見の出力を読む」と書いているのに、下見の口が無かった）。
- ⚠️ **1 ファイル = 1 取引です。** 途中で落ちると **そこまでは適用済みのまま残ります**（全部は戻りません）。
  → 落ちたら、**どこまで入ったかを `--plan` で数え直してから**次を決めること。

✅ **②の前提は 2026-09-20 に全部 揃いました**（本番で実測）:

| 前提 | 実測 |
|---|---|
| `0021` が要求する「稼働中の定義の照合」 | ✅ md5 `b9f344581ffdb9df47f207597ac46869` が**一致**（手で書き換えられていない） |
| `0032` の `revoke ... from anon, authenticated` がワーカーに当たらないか | ✅ 本番で `anon` / `authenticated` / `service_role` は**すべて NOLOGIN**。どのクライアントもそのロールでは繋げない |
| 33 件に旧ワーカーを止めるものが無いか | ✅ 5 観点で走査済み（上の表） |
| staging 通し演習 | ✅ 終了コード 0（①′） |
- ⚠️ `migrate.mjs` は**適用済みファイルの書き換えを拒みます**（正しい挙動）
- ⚠️ **`races.course_frozen` が入ります** — D-065 の 2 次元走路。ここから距離ロスが効き始めます

### ③ O-7 を再確認（**読むだけ**）

```bash
npx tsx tools/verify-anon-exposure.mjs --env production
```
- 🔴 **いま不合格**: `exchange_prize` / `place_bet` / `spend_training_ep` に **anon の EXECUTE**
- ②で直る**見込み**。**「当てたから直ったはず」で閉じない**
- ✅ 合格するまで **④に進まない**（弁護士に見せる前提・§17 O-7）

### ③′ ワーカーを止める 🔴 **開発側が実行**（★VPS）

```bash
ssh -i ~/.ssh/pax_vps root@162.43.29.102 \
  "systemctl stop star-worker; sleep 3; systemctl is-active star-worker"   # → inactive
```
- 🔴 **2026-09-20 に「オーナーが実行」から変えました。** オーナーの明示の許可（「VPS はあなたが
  操作しますよね？」→ 私が確認 →「はい」）と、レビュー側の同意によります。
  止める／戻すは**繰り返せる**操作で、`systemctl is-active` で確かめられるためです。
  ⚠️ **⑥（配備）はオーナーのままです** — あちらは束が入れ替わるので性質が違います。
- ⚠️ 🔴 **「SSH が通ると分かった」＝「自由に使ってよい」ではありません。** この段だけの話です。
- 🔴 **止めないと、消している最中に新しいレースと出走表が作られます**
  （★古いワーカーは 6 分ごとにレースを作り、確定させています）
- → **消し終わったつもりが終わっていない**状態になります

### ③″ 控えを取る／確かめる 🔴 **戻せない操作の前に**

🔴 **この節はオーナーにしか埋められません。** 開発側のセッションは本番に**読み取りすら**繋げないため
（権限層が `[Production Reads]` で止める）、`pg_dump` も管理画面の確認もこちらからは実行できません。

#### ★どちらか一方でよい（★両方は要りません）

**(A) Supabase の自動控えを確かめる**（★推す。★追加の操作がいらない）

1. Supabase の管理画面 → 対象プロジェクト → **Database → Backups**
2. 見るのは 3 つだけ:
   - **いちばん新しい控えの日時**（UTC か JST かも一緒に書く）
   - **方式**（Daily backups ／ Point-in-Time Recovery のどちらか）
   - **PITR なら、遡れる範囲**（例: 直近 7 日）

**(B) 自分で取る**（★A が無い・古い場合）

```bash
pg_dump "$DATABASE_URL" --data-only -t horses -t races -t race_entries -t race_odds \
  > backup_20260920.sql
wc -c backup_20260920.sql      # ★0 バイトでないことを見る（★取れたつもりを防ぐ）
```

#### ✅ 埋めました（2026-09-20・オーナーが Supabase 管理画面を確認）

| 欄 | 値 |
|---|---|
| 控えの方式 | **Physical（日次の自動バックアップ）**。PITR は見当たらず、**日次のみ** |
| **控えの時刻** | **2026-09-19 18:55:53 (+0000)** ＝ いちばん新しいもの |
| 遡れる範囲 | **7 本**（09-13 18:55 〜 09-19 18:55・いずれも +0000） |
| 確かめた人 | オーナー |

#### 🔴 この控えの意味（★読み違えないこと）

- 🔴 **いちばん新しい控えは 09-19 で、★今日（09-20）当てた移行 33 件より前です。**
  → **戻すと、移行 33 件も一緒に戻ります。**
- ✅ **それでも進めてよいと判断しました。** 理由:
  - 移行 33 件は **1 分で当て直せます**（今日 実際に当てて、手順も出力も残っている）
  - 09-19 以降に増えたのは**古いワーカーが作ったレースとオッズ**で、**④ でどのみち消す対象**
  - 馬のデータも ④ で作り直すので、**戻したい中身がほぼ在りません**
- ⚠️ 正確に言うと「**戻せる控えは在る。ただし戻すと今日の移行もやり直しになる**」です。
  「完全に戻せる」とは書きません。

### ④ 世界を作り直す 🔴 **許可 B**

```bash
# ★① 先に消す順と行数を見る（★読むだけ）
npx tsx tools/diag-horses-refs.mjs --env production
# ★② 作り直す（★約 6〜8 分・★staging で実測）
#   🔴 ★<いまの頭数> は **その場で数える**こと。★手順書から写さない
#      psql なら: select count(*) from horses;
npx tsx tools/seed-world.mjs --env production \
  --yes-production --wipe-world --expect-horses <いまの頭数> \
  --allow-all-names --wipe-races
```

#### 🔴 本番だけの関門（★2026-09-20 に足しました）

`seed-world.mjs` は `R-24` の門（`assertNotProduction`）で**本番を拒みます**。**それは正しい設計**
なので門は消さず、`migrate.mjs` と同じ**明示の二段**にしたうえで、**もう 2 つ**要求します。

| 要求 | なぜ |
|---|---|
| `--yes-production` | `migrate.mjs` と同じ二段目 |
| `--wipe-world` | **この道具だけの旗**。`migrate` と同じ指で打てないように |
| 🔴 `--expect-horses <いまの頭数>` | **言葉の旗は手順書から写せる。数は写せない。** その場で数えた人だけが通る |

- ⚠️ **緩むのは 1 か所だけ**です。`app_environment` が**読めない**ときと**宣言が無い**ときは、
  旗が在っても**止まります**（fail-closed）。
- ⚠️ 数が合わないときは **終了コード 2（判定不能）**。「間違い」ではなく「**見てから来い**」だからです。
- 🔴 **失敗の文は、正しい数を教えません。** 教えると「1 回 失敗して画面の数を写す」だけになり、
  **見る**が起きないからです。数え方（`select count(*) from horses;`）だけを出します。
- ✔ **本番で 3 通り止まることを確かめてあります**（旗なし＝1 ／ `--wipe-world` 不足＝2 ／ 数違い＝2）。
  **いずれも 1 行も書いていません。**

#### 🔴 この世界が何であるかを、先に書きます

> **これは「弁護士に見せる世界」ではありません。「配管が通ることを示す世界」です。**

- `data/ng-names.txt` がまだ無いので `--allow-all-names` が要り、**その世界の名前は未検査**です
  （`evidence/world-build/<時刻>.json` に **`nameCheckSkipped: true`** が残ります）。
- **名前リストが届いたら ④′ でもう一度 作り直します**（この文書の上のほうにある段）。
- ⚠️ **役割を混ぜないこと。** この世界を「見せられる」と読む人が出ないよう、簿にも 1 行 在ります。

#### 手順の中身（2026-09-20 に staging で 5 回 演習した後の形）

- 案 **A（転記）＋ B-3（52 週へ層化）＋ C（`advanceTrainingWeeks` で追いつき）**
- 🔴 **消し方は `truncate` です**（`delete` ではありません）。演習 1 回目で `delete from horses` が
  **時間切れ**（57014）になりました。`sire_id`/`dam_id` に索引が無く、削除行ごとに自己参照の RI 検査が
  走るためです（7,370 行 × 全表走査 ≒ 1 億回）。`truncate` は行ごとの検査をしません。
- 🔴 **`cascade` は付けません。** 消す集合を `pg_constraint` から**閉じるまで辿って全部 並べる**ので、
  巻き込みが原理的に起きません。**手で書いた順は、表が増えた日に黙って古くなります**（D-052）。
- ⚠️ **`--wipe-races` は明示したときだけ**効きます（`races` / `race_odds` も消える）。
  付けないと**出走馬の居ない確定済みレースが残り**、画面に空の行が並びます。
- 🔴 **並行で流さないこと** — 選ぶ問い合わせに `for update` が無く、同時に呼ぶと
  全員が同じ 2,000 頭を選んで **週が二重に進みます**。
- ⚠️ **接続が切れても続きます**（最大 5 回 繋ぎ直し）。ただし **繋ぎ直したら合格にしません** —
  ②b が判定不能になり **終了コード 2** で終わります。**2 を「合格」と読まないこと。**

#### ⚠️ 流す直前に読む数（★停止条件）

| 読むもの | 期待 | 超えたら |
|---|---|---|
| `truncate` の秒数 | staging で 8 表 171,344 行 = **0.3 秒** | 🔴 **10 倍（3 秒）を超えたら止める** |
| 全体の分数 | **6〜8 分** | 🔴 **10 倍（80 分）を超えたら止める** |

⚠️ 本番の `race_odds` は **1,550 万行**で、これは staging の 90 倍です。**`truncate` は行を走査しない**ので
似た値になる見込みですが、**見込みであって実測ではありません**。だから上の行をその場で読みます。

#### 道具が自分で確かめる 4 つ

```
① birth_week が無い馬が 0 頭                                （PROD-NEVER-AGED）
②a 投入時に「引退」と書いた馬が DB でもそのまま引退         （SEED-NOT-RETIRED）
②b 現役 ＋ 追いつき中の引退 ＝ プリシード世界の現役          ← ★独立した 2 つ目の目
③ 現役の birth_week が 100 種類以上                          （SEED-LOCKSTEP）
```
終了コード **0 = 全部 判定できて合格 ／ 1 = 落ちた ／ 2 = 判定できなかったものが在る**。

### ④‴ ワーカーは **止めたままにする** 🔴 **⑥ の後まで戻さない**

- 🔴 **作り直した直後に古いワーカーを戻すと、新しい世界が汚れます。**
  ✔ **現物で確認**（`git show 833212d:apps/worker/src/pg-store.ts`）:
  旧い束（2026-08-20）の `insert into races` の列に **`course_frozen` がありません**（`0023` より前のコード）。
  いまの束は書きます（`pg-store.ts:192`）。しかも**「`course_frozen` が null のレースを `DEFAULT_OVAL` で
  確定する」修復路が既に在ります**（`:74`）＝ **D-065 の 2 次元走路が効かないレースができる**、ということです。
- → **作った直後の世界に、古いエンジンの行が混ざります。**
  2026-09-20 に 9 時間かけて追った「22.58% が別エンジンの記録だった」と**同じ形**です。
- ✅ **止めたまま置くのは安全です**（誰も遊んでいない／ゲーム内の時計は実時間から決まるので、
  止めている間に進んだ週は、戻したときに追いつきます）。
- ⚠️ **戻すのは ⑥（新しい束の配備）の後**（→ この文書の ⑥ → ④″）。
- 🔴 ⚠️ **ということは、⑥ が遅れるほどワーカーは止まったままです。** `⑥` には別の未解決
  （D-071 前の本番束・いまのエンジンを入れるとサイクル 10 分に入らない）が在るので、
  **⑤ の報告と一緒に「いつ ⑥ をやるか」を決めること。**
**⑤ 作り直しの実測（読むだけ）**

**(a) 現役の側** — `POOL-CLIFF` / `PROD-NEVER-AGED`

```sql
select count(*) filter (where retired_at_week is null)                    as 現役,
       count(distinct birth_week) filter (where retired_at_week is null)  as 週の種類,
       count(*) filter (where birth_week is null)                         as 週なし
  from horses;
```
- 期待: **現役 2,400 / 週の種類 156 / 週なし 0**

**(b) 引退の側** — `SEED-NOT-RETIRED` 🔴 **ここを見ないと、今日いちばん重い発見が確かめられません**

```sql
select count(*) filter (where retired_at_week is not null)                as 引退済み,
       count(*) filter (where retirement_role = 'stallion')               as 種牡馬,
       count(*) filter (where retirement_role = 'broodmare')              as 繁殖牝馬,
       count(*) filter (where retirement_role = 'honored')                as 功労馬,
       count(*) filter (where retired_at_week is not null
                          and retirement_role is null)                    as 役割なし
  from horses;
```
- 期待: **引退済み 4,970 / 種牡馬 200 / 繁殖牝馬 800 / 功労馬 3,970 / 役割なし 0**
- 🔴 **引退済みが 0 なら、`SEED-NOT-RETIRED` は直っていません**
  （★いまの本番は 7,355 頭 **全部が現役**。★そのうち 67% は走り終えた馬です）

**(c) 予言 3 の答え合わせ** — 🔴 **直す前に記録した予言です。ここで確かめないと誰も確かめません**

```sql
select count(*) as n, avg(u) as 平均, min(u) as 最小, max(u) as 最大
  from (select (select sum((value)::numeric) from jsonb_each_text(stats))
             / nullif((select sum((value)::numeric) from jsonb_each_text(potential)), 0) as u
          from horses where retired_at_week is null) t
 where u is not null;
```

| | 素質開放率 |
|---|---|
| **修正前の本番**（2026-09-20 実測） | **0.3149**（範囲 **0.2790〜0.3509** ＝ 創始値そのもの） |
| ハーネスの既定 | 0.70 |
| D-053 が確かめた条件 | 0.713 |
| いまの staging | 0.8005 |

- **期待: 0.3149 より上がる**（★向きだけ・**AU-7′**）
- 🔴 **動かなければ、④ が効いていません**（育成が回っていない）
- ⚠️ **単一の目標値は置きません。** 作り直した世界の馬は
  **週齢 104〜259 に散っている**（案 B-3）ので、**全頭 182 週で測った 0.713 とは揃いません。**
  「0.713 に一致すること」を合否にしないこと

**(d) ⚠️ V-4 / V-5 / V-6 は、ここでは測れません**

- **レースが 1 本も走っていません**（④で `race_entries` を消しています）
- → **⑦ の後、何周か回してから**別に測ること
- 🔴 そのとき **同じ版どうしで比べる**こと（**AU-21**）。
  いまの帯は 2026-08-20 のエンジンの数字です（`SCHEMA-DRIFT-PROD`）

→ (a)(b)(c) が揃ったら **`POOL-CLIFF`・`SEED-NOT-RETIRED`・`PROD-NEVER-AGED` を閉じられます**

### ⑥ ワーカーを配備 🔴 **許可 C**

```bash
tools/deploy.sh          # ★/opt/star-current のリンクを張り替える
```
- ⚠️ **git push では入れ替わりません**（正典 §14/§15）
- ⚠️ **オーナーの実行が要ります**（このセッションから VPS に入りません）

### ④″ ワーカーを戻す 🔴 **⑥ の後・新しい束で**

```bash
ssh -i ~/.ssh/pax_vps root@162.43.29.102 \
  "systemctl start star-worker; sleep 5; systemctl is-active star-worker"
# ★1 周 回ったことを見る（★is-active だけでは「起きたが回っていない」が通る）
ssh -i ~/.ssh/pax_vps root@162.43.29.102 \
  "journalctl -u star-worker --since '-3 min' --no-pager -o cat | grep -c 'cycle='"
```

- 🔴 **この段は 2026-09-20 まで手順書に在りませんでした。** ③′ で止めたきり、
  **戻す指示がどこにも書かれていない**状態でした（止めたまま忘れると、ゲームが止まります）。
- ⚠️ **`is-active` が `active` でも足りません。** 起動しただけで周回していない場合があるので、
  **`cycle=` が 1 件以上 出ること**まで見ます（CK-14: 起きたこと ≠ 動いていること）。
- ⚠️ **⑤ の実測は、戻す前に取ります**（ワーカーが動き出すと数が変わるため）。
- 🔴 **この段は ⑥ の後です**（2026-09-20 に順を変えました）。⑥ の前に戻すと、
  **古い束が新しい世界を動かし始めます**（`course_frozen` を書かないレースが混ざる）。
  → 理由は ④‴ に書いてあります。

### ⑦ 1 周 回ることを確かめる

- `[worker] 週送り 週=… 延べ…頭` が出るか（★出なければ ④が効いていない）
- 🔴 **`[worker] 週送りに失敗:` が出ないこと** — 出たら `PROD-NEVER-AGED` の fail-closed が鳴っています

### ⑧ 版を記録する

```bash
npx tsx tools/verify-deployed-build.mjs --base https://star-two-chi.vercel.app --record
```
- **7 日を過ぎると検定が落ちます**（`DP-1` / O-6）。落ちたらまたこれを流してください

---

## 🔴 ④の後に必ず起きること（★先に言っておきます）

- **`STABLE-1-SKEW` の 66 頭は消えます。** 跡は `evidence/20260919-stable1-skew/` に残ります
- **`race_entries` 79,933 行 が消えます**（★消さないと `horses` を消せないため）

### 🔴 ④ の後に **残る**もの（2026-09-20 実測・要 判断）

```
races       6,152 行        ← ★残る（★horses を参照していないので消さない）
race_odds   15,490,715 行   ← ★残る（★1,550 万行）
bets            0 行  ／  users 0 行  ／  ep_ledger 0 行  ／  pp_ledger 0 行
```

✅ **本番にはまだ 1 人も居ません**（users 0 / bets 0 / 台帳 0）。
→ **消して困る人の資産は、1 つもありません。** ここは安心してよい点です。

🔴 **しかし、そのままだと半端な世界が残ります**:
- **出走馬が 1 頭も居ない「確定済みレース」が 6,152 本**
- そのレースに紐づく**オッズが 1,550 万行**

### ✅ **案 A を採ります — `races` / `race_odds` も消す**（裁定 2026-09-20）

```bash
npx tsx tools/seed-world.mjs --env production --wipe-races
```

**理由 4 つ**:
1. 出走馬が 1 頭も居ないレースは、**履歴ではなく壊れた行**
2. `races_public` は `race_entries` と結合する → **空のレースが画面に並ぶ**
   → 弁護士に見せる「本番さながら」に空の行が並ぶのは致命的
3. `entrant_snapshot` が 0 なので **F-3 で照合できない ＝ 何の証拠にもならない**
4. 2026-08-20 のエンジンの記録 → **今日のエンジンの記録として読めない**

✅ **決め手**: `users 0 / bets 0 / ep_ledger 0 / pp_ledger 0` — **困る人が 1 人も居ません。**

🔴 **ただし「数」は残します**（`STABLE-1-SKEW` と同じ作法 — **行は消す。記録は残す。**）
`seed-world.mjs` が `evidence/world-build/<時刻>.json` に `wipedCounts` を書きます。

⚠️ **`--wipe-races` は既定で off** です。`horses` の作り直しに**必要ではない**ので、
「ついでに消す」を既定にすると**消す範囲が黙って広がります**（`--flatten` と同じ作法）。

### 🔴 ①′ で必ず測ること — **1,550 万行の削除にかかる時間**

`race_odds` は **15,490,715 行**です。`delete` が遅い可能性があります（**未測定**）。
`seed-world.mjs` は**1 文ごとに秒数を出します**。①′ の出力を必ず読んでください。

- 遅すぎる場合は `truncate` を検討。ただし **`tools/lib/broad-deletes.mjs` に
  「なぜ truncate でよいか」を 1 件として書くこと**
- ⚠️ **本番で初めて「30 分 かかる」を知らないこと**（今日 2 度 学びました）
- **較正の取り直しが要ります**（`AL-11`）。いまの帯は 2026-08-20 のエンジンの数字です

## まだ無いもの

🔴 **定常運転の供給**（`POOL-SUPPLY`）。製品コードの `insert into horses` は **0 件**。
作り直した世界も **260 週 後から毎週 15〜16 頭ずつ減ります**。**人を迎える前に要ります。**
