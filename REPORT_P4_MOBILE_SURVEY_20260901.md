# 報告 — ★モバイル表示の調査（2026-09-01）

**出どころ**: ★オーナー指示（2026-08-31）
> ★**このアプリはモバイルアプリです。**★ブラウザ版も出しますが**基本的にはモバイルユーザー**です。
> ★開発サーバーでレースページを駆使してきましたが ★**モバイル表示のことは考慮していません。**
> ★**レースページ、TOP ページ、その他 存在する全てのページ**がレスポンシブ最適化されているか調べてください。

**ブランチ** `p4/race-30sec-cuts`

---

## ★0-1. 一行で

★**されていません。** ★調べた **23 ページ中 10 ページ**で、★内容が画面の外へ出て ★**切り落とされて**います。
★しかも ★**横スクロールが出ないように CSS で隠してある**ので、★ユーザーは**切れたことに気づけません**。

## ★0-2. 提出物・変更範囲

| | |
|---|---|
| ★道具（新） | `tools/verify-mobile-layout.mjs`（★追跡・読取専用・登録簿に記載） |
| ★出力 | `out/mobile-survey.txt`（★360px と 390px の全ページ） |
| ★変更 | ★**製品コードは 1 行も変えていません。** ★これは調査です |

---

## ★1. 測り方（★感想ではなく数）

★実ブラウザ（★`Emulation.setDeviceMetricsOverride`・`mobile: true`）で ★**360px** と ★**390px** に設定し、
★全ページを開いて **4 つの数**だけを取りました。

| ★測ったもの | ★意味 |
|---|---|
| ★**はみ出し** | ★画面の右端を越えた要素の数と、★越えた量の最大 |
| ★**44px 未満の操作要素** | ★`a / button / select / input` の短辺。★44px は Apple の指針（★Material は 48dp・★緩いほうで数えました） |
| ★**最小の文字** | ★葉ノードの `font-size` の最小値 |
| ★**キャンバスの縮み** | ★`canvas.width`（描いている大きさ）に対する**表示されている幅**の比 |

### ⚠️ ★最初の測定は間違っていました（★記録として残します・R-25）

★最初は ★`documentElement.scrollWidth − innerWidth` だけを見て、★**「19 ページすべて、はみ出し 0」**と出しました。
★メディアクエリが **139 行中 1 本**しかないのに全ページ健全というのは通らないので、★測り方を疑いました。

→ ★**真因**: ★`globals.css:138`
```css
html:has(> body[data-theme="arcade"]) { overflow-x: hidden; }
```
★これがあると、★はみ出しは**スクロールにならず切り落とされ**、★`scrollWidth` に出てきません。
→ ★`scrollWidth` とは**別に**、★**右端を越えた要素そのものを数える**ようにしたら 10 ページで出ました。

⚠️ ★**これは「横スクロールが出ないから大丈夫」ではありません。★出ないぶん、もっと悪いです。**
★ユーザーは ★**切れた内容に到達する手段がありません。**

---

## ★2. 結果（★390px ＝ iPhone 14。★360px でも同じ 10 ページ）

### ★2-1. ★**切り落とされているページ（10 / 23）**

| ページ | ★はみ出しの最大 | ★右端を越えた要素 | |
|---|---|---|---|
| ★`/stable`（厩舎） | ★**+623px** | ★**106 個** | ★中核 |
| ★`/setup`（初回セットアップ） | ★**+481px** | ★42 個 | ★**新規ユーザーが最初に見る画面** |
| ★`/records`（戦績） | ★**+462px** | ★29 個 | ★中核 |
| ★`/entry`（出走登録） | ★**+415px** | ★40 個 | ★中核 |
| ★`/stable/<id>`（馬の詳細） | ★**+382px** | ★**108 個** | ★中核 |
| ★`/races`（レース一覧） | ★+298px | ★49 個 | ★中核 |
| ★`/races/<id>/odds`（オッズ） | ★+257px | ★39 個 | ★**お金の画面** |
| ★`/training`（調教） | ★+252px | ★76 個 | ★中核 |
| ★`/prizes`（賞金） | ★+125px | ★11 個 | ★**お金の画面** |
| ★`/races/<id>`（レース詳細） | ★+65px | ★10 個 | ★中核 |

⚠️ ★**通っているのは、ほとんどが研究用のページと login / signup です。**
★**壊れているのは、ゲームの中核と、お金の画面**でした。

### ★2-2. ★**指で押せない操作要素**（★44px 未満）

| ページ | | |
|---|---|---|
| ★`/races/<id>/bet`（★**投票**） | ★**54 / 56** | ★**お金を動かす画面がいちばん悪い** |
| `/still` | 33 / 33 | 研究用 |
| `/race` | ★**20 / 20** | ★**全部**。★キャンバス上の操作列 |
| `/entry` | ★15 / 15 | ★全部 |
| `/` (TOP) | 15 / 19 | |
| `/records` `/watch` `/stable` | 13 / 13・13 / 13・13 / 20 | |
| `/camera` `/race-quality-lab` | 12 / 12 | 研究用 |
| `/prizes` `/setup` | 11 / 11・11 / 28 | |

★**23 ページ中、44px 以上の操作要素しか無いページは 1 つもありません。**

### ★2-3. ★**キャンバスが 4 分の 1 に縮みます**（★このアプリの本体）

| | 390px | 360px |
|---|---|---|
| ★`/race` | ★1280 → 362px ＝ ★**28%** | ★1280 → 332px ＝ ★**26%** |
| `/watch` | 1280 → 358px ＝ 28% | 26% |
| `/race-next` | 1280 → 362px ＝ 28% | 26% |
| `/course` | 1100 → 358px ＝ 33% | 30% |

⚠️ ★**HUD の文字はキャンバスに焼かれています。** ★1280px で設計した字が **28%** で出ます。
⚠️ ★台帳 **B-3** は「★オーナーの画面は **67%**。★塗りのぼかしは縮小で消え、残るのは線」と記録しています。
　→ ★**28% は、その 67% よりさらに 2.4 倍きつい**縮みです。★**モバイルでは別の設計が要ります。**

### ★2-4. ★小さすぎる文字

★最小 **10px** … `/stable` `/training`
★最小 **11px** … `/races` `/prizes` `/setup` `/art-lab` `/races/<id>` `/races/<id>/odds`
★（★モバイルの目安は 12px 以上。★iOS は 16px 未満の入力欄で**自動ズーム**します）

---

## ★3. 機構（★どこが原因か・★4 か所）

| # | | |
|---|---|---|
| ★① | ★`globals.css:138` ★`html:has(> body[data-theme="arcade"]) { overflow-x: hidden; }` | ★**症状を隠しています。** ★これを外すと、★10 ページで横スクロールが出ます（＝いま切れている分） |
| ★② | ★`globals.css:18` ★`main { max-width: 1180px; margin: 0 auto; padding: 0; }` | ★**左右の余白が 0**。★モバイルで内容が画面の縁に貼りつきます |
| ★③ | ★メディアクエリが ★**139 行中 1 本だけ**（`max-width: 900px` → `.hide-narrow`） | ★横並びの表・パネルが**折り返す指示を持っていません** |
| ★④ | ★`layout.tsx` のヘッダー ★`height: 56, padding: '0 26px'` 固定 | ★モバイルの安全域（ノッチ・下端）を見ていません |

⚠️ ★**キャンバス（★2-3）は ①〜④ とは別の問題**です。★CSS では直りません。
　★HUD の版面をモバイル用に組み直すか、★キャンバスの論理サイズを画面幅に合わせる必要があります。

---

## ★4. ★本番 URL でレースを見る件（★併せてのご希望）

★調べた事実だけ書きます。

| | |
|---|---|
| ★Vercel の紐付け | ★**あります**。`.vercel/project.json` … プロジェクト名 `star` |
| ★ビルド設定 | ★`vercel.json` … `buildCommand: npm run build:web` / `outputDirectory: apps/web/.next` |
| ★**本番 URL** | ⚠️ ★**文書のどこにも記録がありません**（`*.md` を全文検索・0 件） |
| ★Vercel CLI | ⚠️ ★**この環境に入っていません**（`npm i -g vercel` が要ります） |
| ★push | ⚠️ ★開発側の環境から**できません**（台帳 §4-6・`credential.helper` が GUI 待ち） |

→ ★**私の側だけでは本番に出せません。** ★必要なのは次の 3 つです:
　★① オーナーによる `git push`　★② 本番 URL の共有（★あるなら）　★③ ★**モバイルの直しを先に入れるかどうかの判断**

⚠️ ★**いま本番に出すと、上の 10 ページが切り落とされたまま出ます。**

---

## ★5. ★開発側の推奨（★決めるのはオーナー）

★直す順は ★**①→②→③** を薦めます。★理由は「★1 行の変更で被害の全体像が見える」からです。

| 順 | ★やること | ★なぜ先か |
|---|---|---|
| ★**1** | ★`overflow-x: hidden` を**一時的に外して測り直す** | ★**隠している症状を全部出す**。★どのページがどれだけ切れているかが確定します。★直しの前に**全体量を知る**ため |
| ★**2** | ★`main` に左右の余白と、★表・パネルの折り返し（★メディアクエリ）を入れる | ★10 ページの大半は「★横並びが折り返さない」形です |
| ★**3** | ★操作要素を 44px 以上にする | ★**投票画面（54/56）から**。★お金を動かす画面が最優先 |
| ★4 | ★キャンバスのモバイル版面 | ★**別の便で**。★CSS では直らず、★HUD の設計に触れます（★正典の演出に関わる） |

⚠️ ★**この報告では 1 行も直していません。** ★指示書の範囲外のためです（CLAUDE.md）。
　★どこから着手するかのご指示をお待ちします。

---

## ★6. ★再実行（★すべて読取専用）

```powershell
cd V:\dev\Cusor\star
cd apps\web ; npm run dev            # ★別の窓・ポート 3210

# ★全ページ（★動的な経路の id は /races /stable の href から拾う。★直書きしない）
npx tsx tools/verify-mobile-layout.mjs --widths 360,390 --rid <レースid> --hid h1
#   ★期待: ★はみ出しが出た組み合わせ 20 / 46（＝ 10 ページ × 2 幅）

npx tsx tools/verify-mobile-layout.mjs --widths 390    # ★動的な経路を除く 19 ページ
#   ★期待: ★7 / 19
```

⚠️ ★`--path` を Git Bash から渡すときは ★`MSYS_NO_PATHCONV=1` を付けてください。
　★付けないと `/race` が Windows のパスに変換され、★**1 ページも調べずに「0 / 0」と出ます**（★実際に出しました）。


---

# ★追記 — ★着手しました（★同日・オーナー指示「着手してください」）

## ★A-1. 一行で

★**横の崩れは 23 ページ中 21 ページで直りました。** ★残りは ★**2 ページ**（`/race` と `/course`）です。
⚠️ ★**指で押せない大きさ（44px）とキャンバスの縮み（26%）には手を付けていません**（★推奨順の 3 と 4）。

## ★A-2. ⚠️ ★**測っている途中で、道具が 3 回嘘をつきました**（★記録として残します・R-25）

| # | ★嘘の中身 | ★どう気づいたか | ★直し |
|---|---|---|---|
| ★① | ★「★19 ページすべて、はみ出し 0」 | ★メディアクエリが 139 行中 1 本なのに全ページ健全は通らない | ★`scrollWidth` ではなく ★**右端を越えた要素**を数える（`overflow-x:hidden` に隠されていた） |
| ★② | ★「★はみ出し 0」（★2 度目） | ★撮った絵が ★**465px 幅**なのに測定は 390px と言った | ★**要求した幅で測れているか**を確認。★内容が収まらないと ★**ブラウザがページ全体を縮め**、`innerWidth` が膨らむ |
| ★③ | ★「★`/race` は 669px にズームする」（★3 回とも再現） | ★別の一発道具では 390px に収まった | ★**幅を読むのは何かを触る前**。★`overflow-x` を解いた**あと**に読んでいたので、★**解いた瞬間の縮み**を測っていた<br>（⚠️ ★`/race` の 669px は**その後も再現**したので、★こちらは**本物**でした）|
| ★④ | ★「★横は直った」 | ★`/entry` の絵で ★**行が縦に重なって文字が二重**に見えた | ★**縦の溢れ**（中身が箱より高い）を検出に追加。★横しか測っていなかった |

★どれも ★**数字だけ見ていたら通っていました。** ★絵と突き合わせて見つけています。

## ★A-3. 直したもの（★`globals.css` の 1 か所＋7 ファイルの機械的な置換）

| # | ★何を | ★なぜ |
|---|---|---|
| ★1 | ★`main` に左右 12px の余白 | ★余白 0 で内容が画面の縁に貼りついていた |
| ★2 | ★`.a-row` を折り返し・高さを内容に合わせる／列の固定幅を解く | ★行が「時刻｜格｜距離｜賞金｜状態」の**表**で、★合計が画面幅を超えていた |
| ★3 | ★列見出しの帯 **7 か所**に `hide-narrow` | ★行が表でなくなるので、★見出しだけ残ると意味を失う（★実測: 3 行に散らばった）|
| ★4 | ★`.a-cards`（★新）— カードの列数を CSS 側で決める | ★`repeat(4,1fr)` が `page.tsx` に直書きで、★モバイルで ★**「参 加 ポ イ ン ト」と 1 文字ずつ縦に割れて**いた |
| ★5 | ★150px 以上の `flex: 0 0 <N>px` を解く | ★横に 2〜3 枚並ぶと画面幅を超え、★**ページ全体がズームアウト**（`/entry` は 42% に縮んでいた）|
| ★6 | ★200px 以上の `min-width` を解く | ★`min-width: 420px` の板が 390px に入らない（`/stable/<id>`）|
| ★7 | ★ヘッダーの `nav` を折り返す | ★398px で全ページ共通の原因だった |
| ★8 | ★`table` を内部で横スクロール | ★切り落とされると右側に到達できない |
| ★9 | ★`display:flex` の行の高さを内容に合わせる | ★折り返した中身が固定の高さから溢れ、★**下の行と重なっていた**（★/entry）|

⚠️ ★**`overflow-x: hidden` は外していません。** ★LP の `.lp-bleed` に要るためです。
　★外すのではなく ★**隠すものが無い状態にする**のが直しで、★合否も「右端を越えた要素の数」で見ています。

## ★A-4. ★いまの数字（★実測・360px と 390px）

| | ★着手前 | ★いま |
|---|---|---|
| ★横の崩れ | ★**20 / 46**（★10 ページ × 2 幅） | ★**8 / 46**（★2 ページ × 2 幅＋縦の残り 2 件）|
| ★残っているページ | — | ★`/race`（★ズーム 669px）／ ★`/course`（★表が切れる・研究用）|

★`npm test` **134 ファイル / 1371 件 PASS** ／ `npm run typecheck` **終了状態 0**。

## ★A-5. ★残っているもの

| # | | |
|---|---|---|
| ★① | ★**`/race` がズームアウトする**（★390px 要求 → **669px**・★58% に縮む） | ★**原因は特定できていません。**<br>★切り分けで分かったこと: ★**キャンバスではありません**（消しても 669px）。<br>★`main > div`（★操作列）を消すと **390px に収まります**。<br>★ただし ★**静止した状態では 390px を超える要素が 1 つもありません** — ★組版の途中だけ広い、と見ています（★未確認）|
| ★② | ★`/course` の表が切れる | ★研究用ページ。★優先度は低いと考えます |
| ★③ | ★**指で押せない大きさ**（★推奨順 3・★未着手） | ★**投票画面 54/56** が最悪。★お金を動かす画面 |
| ★④ | ★**キャンバスが 26%**（★推奨順 4・★未着手） | ★CSS では直りません。★HUD の版面＝演出に触れるので**別の便**で |

⚠️ ★**②③④ に手を付けるかは、ご指示をお待ちします。**


---

# ★追記 2 — ★指で押せる大きさ（★推奨順 3）を直しました

## ★B-1. 一行で

★**44px 未満の操作要素を 351 個中 88 個まで減らしました。**
★いちばん悪かった ★**投票画面は 54 / 56 → 2 / 56** です。

## ★B-2. 何が小さかったか（★「54/56」だけでは直せないので内訳を出しました）

★`tools/verify-mobile-layout.mjs --taps` を足しました。★投票画面の内訳:

| ★個数 | ★要素 | ★大きさ | |
|---|---|---|---|
| ★**36 個** | `<button>` | ★**32 x 32px** | ★**主犯**。★マークシートの ◎○▲ の丸 |
| 4 個 | `<button>` | 76 x 25px | ★式別（単勝・三連複…） |
| 3 個 | `<button class="a-chip">` | 78 x 32px | ★金額（100・500…） |
| 2 個 | `<a>` | 60 x 34px | ★ヘッダーのナビ |

## ★B-3. 直し方

```css
main button, main select, main [role="button"] { min-height: 44px; min-width: 44px; }
```

⚠️ ★`min-height` / `min-width` は inline の `height` / `width` より**強い**ので、
　★**`!important` を使わずに**下限だけ与えられます（★元の指定を消さずに済みます）。

## ★B-4. ★結果（★390px の実測）

| ページ | ★前 | ★後 |
|---|---|---|
| ★`/races/<id>/bet`（★投票） | ★**54 / 56** | ★**2 / 56** |
| `/still` | 33 / 33 | ★3 / 33 |
| `/race` | 20 / 20 | ★3 / 20 |
| `/entry` | 15 / 15 | ★2 / 15 |
| `/prizes` | 11 / 11 | ★2 / 11 |
| ★**全ページ合計** | — | ★**88 / 351** |

★残っているのは `/`（9/19）・`/stable`（9/20）・`/records`（9/13）・`/races/<id>/odds`（9/13）です。

## ★B-5. ⚠️ ★併せて起きたこと（★私の変更ではありません）

★測っている途中で ★**dev サーバー（ポート 3210）が壊れました**。
★`/` が無応答、★`/bet` と `/odds` が **HTTP 500**（`Jest worker encountered 2 child process exceptions`）。

★**私の変更が原因ではないことを確かめました**: ★別ポート（3211）で立て直すと ★**全ページ 200**。
★CSS はサーバーを落としませんし、★`bet/page.tsx` は触っていません。
⚠️ ★**3210 は触っていません**（★オーナーの窓の可能性があるため）。★不調なら立て直してください。

## ★B-6. ★残っているもの（★変わらず）

| # | | |
|---|---|---|
| ★① | ★`/race` がズームアウト（390px 要求 → **672px**） | ★原因は未特定（★§A-5 のまま）|
| ★② | ★`/course` の表が切れる | ★研究用・優先度低 |
| ★③ | ★**マークシートの列** | ★3 列（1着/2着/3着）が 2 列で折り返し、★**3 着だけ次の行**に来ます。★使えますが見出しと揃いません。★表そのものをモバイル用に組み直す話です |
| ★④ | ★**キャンバスが 26%** | ★未着手。★HUD の版面＝演出に触るので**別の便** |
