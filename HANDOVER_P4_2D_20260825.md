# 開発側の作業記録 — P4 2D 中継映像（2026-08-24 〜 25）

★**採否の判定は開発側では行いません。** 判定はレビュー側（Codex）とオーナーです。

## この文書の位置づけ

レビュー側が同日に引継ぎ書を作成しています。**次に何をするかはそちらが優先**です。

| ファイル | 誰が書いたか | 何のため |
|---|---|---|
| `HANDOFF_P4_RACE_NEXT_SESSION_20260825.md` | レビュー側 | ★**次セッションの手順・優先順位・完了条件** |
| `REVIEW_P4_RACE_PROGRESS_20260825.md` | レビュー側 | 進捗レビュー |
| **この文書** | 開発側 | **経緯・実測値・踏んだ罠**（レビュー側の引継ぎ書に載っていない数字と失敗の記録） |

次セッションの読む順:
① レビュー側の `HANDOFF_...` → ② `REVIEW_...` → ③ この文書（数字と罠が要るときだけ）

---

## 0. いまの状態（30 秒で把握する）

| | |
|---|---|
| ブランチ | `p4/race-30sec-cuts` |
| HEAD | `7d62d25`（基準 `93d270c` の 1 つ先） |
| push | **していない**（リモート追跡ブランチなし） |
| 通常 `/race` の既定 | **`v5`**（本線に入った） |
| 旧映像への切り戻し | `/race?cinematography=v4` |
| dev サーバー | ポート 3210 で稼働中。`/race`・`?cinematography=v4` とも 200 |
| `apps/web/package.json` | `next dev -p 3210` の変更を**未コミットのまま保持**（どのコミットにも入れない） |

```bash
npm run dev:web          # ポート 3210
# http://localhost:3210/race                        既定（v5）
# http://localhost:3210/race?cinematography=v4      旧映像
```

---

## 1. 何が本線に入ったか（コミット `7d62d25`）

通常 `/race` の既定ショット列のうち **2 か所だけ**を差し替えました。

| 区間（`until`） | 旧 v4 | 既定 v5 |
|---|---|---|
| 第4コーナー 0.500–0.660 | `fourth-corner-front` | **`fourth-corner-high`**（正面 → 俯瞰） |
| 直線 0.660–0.940 | `homestretch-front` | **`homestretch-side`**（正面 → 横追従） |

`start-front` / `first-corner-front` / `side-drive` / `finish-line` は旧 v4 のまま。
カット境界 `until` も動かしていません。18.6 秒の直線カットは**分割していません**。
target とカメラ定義は各ショットが元から持っているものをそのまま使い、
接戦判定・自馬追従・先頭馬追従は足していません。

あわせて**順位表の減光を 0.6 → 0.25**（最も薄いとき 40% → 75%）にしました。

### コミットに含めたファイル（6 本）

```
M  apps/web/src/app/race/page.tsx                    URL の読み方を @star/render へ委譲＋減光係数
M  packages/render/src/broadcast-v2.ts               SCRIPT_V5・既定・URL 解決
M  packages/render/src/broadcast-v2-scene.ts         script を BroadcastV2Script へ・固定カメラ終点へ伝播
A  packages/render/test/script-v5.test.ts  製品テスト 9 件
A  REPORT_P4_2D_SCRIPT_V5_DEFAULT_20260825.md        昇格の報告書
A  REPORT_P4_2D_STANDINGS_ALPHA_20260825.md          順位表の報告書
```

### 意図的にコミットへ入れていないもの

- `apps/web/package.json` のポート 3210 変更（★永久に入れない）
- `tools/lib/classification.mjs`（登録簿の追記）
- `tools/` の測定ツール群、`out/` の成果物（`.gitignore` 対象）
- 過去の監査報告書 4 本

---

## 2. これまでの経緯（2026-08-24 〜 25）

### ① 素材反復・v9・ハイブリッドの調査 → 打ち切り

側面素材の世代（v7/v9）とハイブリッド方式を検証。映像は成立したが**初期化コストが +2.6 秒 / +1.06MB**
で「通常 `/race` への導入は FAIL」。研究ブランチ `research/2d-side-hybrid-20260824` へ隔離し、
本線は基準 `93d270c` の製品コードへ戻しました。
★**この論点には戻りません。**（3D・S3・v9 も同様）

### ② 編集文法の監査（参考映像と `/race` の突合）

| | 参考映像 | 当時の `/race`（v4） |
|---|---|---|
| 争っている馬を映す割合 | 68.6% | 0%（`pack` 100%） |
| 横からの画 | 58.5% | 0%（`front` 74.3%） |
| 俯瞰 | 16.5% | **0%** |
| 後方から | — | **0%** |

- `homestretch-front` が**レース全体の 48%** を占めていた
- ショットは **19 個定義されていて 6 個しか使われていなかった**
- ゴール前カットの間、自馬は**まるごと画面外**

→ 結論は「カメラ値をどう触るか」ではなく「**誰を・どの大きさで・何秒見せるか**」。

### ③ 既存ショットの適性ゲート（実画面確認）

`?auditShot=<shot-id>` の一時パッチで実画面を確認 → **一時パッチは完全に逆パッチで除去済み**。
`fourth-corner-high` と `homestretch-side` が「今ある素材のまま成立する」ことを確認しました。

### ④ 候補台本 v5 と比較動画 8 本

seed 42 / 332 / 474 / 14 について、旧 v4 と候補を**対で**撮影（各 30fps・1280×720・音声なし・
後合わせなし）。`out/2d-script-v5/`（★約 2.8GB・`.gitignore` 対象）。

**測った動きの量**（隣り合うコマの画素差・HUD 帯を除く／seed 42）

| | 全体 | 4 角 | 直線 | ゴール前 |
|---|---|---|---|---|
| 旧 v4 | 5.3 | **3.1** | 4.5 | 6.9 |
| v5 | **7.8** | **10.8** | **8.4** | 6.9 |

★旧 v4 は**4 角がレース中いちばん動きの少ない区間**でした。

### ⑤ レビュー PASS → 既定へ昇格

「第4コーナーに視点変化と奥行き」「直線で馬が主役」「4 seed すべてで同じ方向の改善」と評価され PASS。
既定を `v5` に、旧 v4 は URL で切り戻せる形にしました。

### ⑥ 順位表が読めない → 原因は**描画順ではなく減光**

当初「馬が順位表より前に描かれている」と見立てられましたが、**誤りでした。**

- `drawStandings`（`packages/render/src/oblique-ui.ts:185`）は背景・枠・見出し・順位・枠色・
  馬名・着差を**1 つの関数の中で連続して**描いている（間に馬は入らない）
- 呼び出し順も 場面（`page.tsx` の 1760 / 1833）→ HUD（2013）で、順位表が最後

真因は `page.tsx` の意図的な減光でした（馬が画面に占めるほど薄くする規則）。
`homestretch-side` は画面比が 45% の閾値を超えるため、**いちばん馬が大きい瞬間に順位表が
いちばん薄く（40%）なり**、進行 86〜88% で 1〜5 着の馬名と着差が読めませんでした。

**証拠**（同時刻・同一画素／旧 v4 → 既定）

| 画素 | 何があるか | 旧 v4 | 既定 |
|---|---|---|---|
| (950,100) | **順位表自身**の金色の「1」 | `229,205,115` | `107,99,62`（47%） |
| (1000,200) | **順位表自身**の金色の文字 | `248,216,43` | `128,99,29`（52%） |
| (1210,48) | 馬が居ない位置 | `16,18,15` | `25,30,33`（明るい） |

→ 減光係数を **0.6 → 0.25**（最も薄いとき 75%）。閾値 `0.28 / 0.45`・位置・大きさ・色・
`HUD.glass` は変えていません。旧 v4 は `ease = 0` なので**5 点すべて画像ハッシュが一致**しました。

---

## 3. 承認済みの「既知の許容事項」（直さない・蒸し返さない）

1. **カメラ状態の引き継ぎ** — ショット定義を変えると、カメラ平滑化状態の引き継ぎで
   **後続カットの初期構図も変わります**。台本上は変えていない `finish-line` でも、旧 v4 と
   絵が **約 22.6% 変わります**（seed 42 実測）。比較動画で確認・承認済み。
   強制リセット・前カット状態の消去・smoothing 変更は**しません**。
2. **順位表に馬が重なること自体** — 減光 0.25 で 1〜5 着は読めます。位置・透明度・表示時間・
   カメラ offset・`homestretch-side` の zoom・target は変えません。
3. **直線で 2 頭しか映らないこと／自馬が一部画面外になること** — 停止条件ではありません。
   ★**これを直すために隊列を再加工してはいけません**（以前 S3 の隊列変換が汎用化に失敗しています）。

### seed 42 で絵が変わる範囲（参考）

旧 v4 と既定で絵が変わり始めるのは **f263 = 8.8 秒**（4 角の入り 8.69 秒と一致）で、
そこから**ゴールまで**続きます。4 角 43.8% / 直線 62.3% / ゴール前 22.6% の画素が違います。

---

## 4. 製品テスト（`packages/render/test/script-v5.test.ts`・9 件）

★**製品コードだけで再現できる検査に限っています。** `out/` の動画・画像や未追跡ツールには
依存しません（`.gitignore` 対象のため本線テストから外しました）。

| | 内容 |
|---|---|
| ① | 既定が `v5` |
| ② | 明示指定も同じ |
| ③ | 不正値は既定へ戻る（6 通り） |
| ④ | `?cinematography=v4` で旧台本を選べる／旧 v4 の表が残っている |
| ⑤ | 台本差は 2 ショットだけ（境界 `until` も同値） |
| ⑥ | 第4コーナーで `fourth-corner-high` |
| ⑦ | 直線で `homestretch-side`／発走・1角・道中・ゴールは両者同じ |
| ⑧ | **レース結果不変**（台本を変えても馬の (gate, s, w) は同じ・入力も書き換えない） |
| ⑨ | **HUD 係数 0.25**／古い 0.6 なし／**閾値 0.28・0.45 不変**／位置・大きさ・`HUD.glass` 不変 |

```bash
npx vitest run packages/render/test/script-v5.test.ts
npm test          # 112 ファイル / 1118 件
npm run typecheck
npm run build:web # ★下の注意を読むこと
```

---

## 5. ⚠️ 踏んだ罠（同じ穴に落ちないために）

1. **dev サーバーを動かしたまま `npm run build:web` を実行しない。**
   `next build` は `next dev` と同じ `apps/web/.next` を書き換えます。実際に 3210 の
   dev サーバーが壊れました（レースが組めない → `/race` が 500）。
   復旧は `Ctrl+C` → `npm run dev:web`。同時に必要なら別の作業ツリーを使うこと。
2. **React は同じ値のシークを無視します。** シークバーへ同値を入れ直しても再描画されません。
   → 一度 `+0.37` ずらしてから戻します。
3. **長く連続シークすると描き直しを取りこぼします。** ページを開き直しても消えません
   （34 コマ続き、その連続が開き直しの境目でちょうど終わっていました）。
   → 1 コマずつ前のコマと見比べ、同じならシークからやり直します（1 回だけ）。
4. **`gate.json` は seed 単位で差し替えること。** 丸ごと書き直していたため `--seeds` を
   分けて走らせたときに先に撮った seed の記録が消えました。`--verify-only` で撮り直さず検査だけできます。
5. **比較シートの時点は台本境界に合わせること。** 「第4コーナー」を進行 68% で撮っていましたが
   境界 0.660 を越えて**直線カット**でした（俯瞰が 1 枚も写らない）→ 60% が正しい。
6. **ファイル送付は 30MiB 上限。** 原本 30MB 級の動画は送れないので、`out/2d-script-v5/_send/`
   に crf 30 の軽量コピー（解像度・fps・コマ数は原本と同一）を置いてあります。
7. **`out/2d-script-v5/seed-474-v5.mp4` が一度消えました**（原因未特定）。
   連番が残っていたので撮り直さず再エンコードで復元しました。

---

## 6. 未追跡のまま残っているもの（判断待ち）

| 種類 | 中身 | 状態 |
|---|---|---|
| 調査報告 | `REPORT_P4_2D_STANDINGS_LAYER_20260825.md` | ★**判断待ち**。「描画順ではなく減光が原因」と特定した報告。要点は `STANDINGS_ALPHA` とコード内コメントに取り込み済み |
| 監査報告 | `REPORT_P4_2D_EDIT_GRAMMAR_AUDIT_20260824.md` / `REPORT_P4_2D_EXISTING_SHOT_GATE_20260824.md` / `REPORT_P4_2D_SCRIPT_V5_COMPARE_20260824.md` | 未追跡のまま |
| 監査テスト | `packages/render/test/edit-grammar-audit.test.ts` / `existing-shot-gate.test.ts` | ★`out/` に依存するので**本線テストには入れない** |
| 測定ツール | `tools/audit-*.mjs` / `capture-*.mjs` / `render-*.mjs` / `lib/cdp.mjs` / `lib/race-audit-build.mjs` | 未追跡のまま |
| 登録簿 | `tools/lib/classification.mjs`（追記） | 未追跡（ツールを入れないなら不要） |
| 成果物 | `out/2d-script-v5/`（約 2.8GB） | `.gitignore` 対象 |

**未決の問い**（開発側では決めません）

1. 上記の監査ツール群・報告書を本線に残すか、研究ブランチへ移すか、捨てるか
2. `REPORT_P4_2D_STANDINGS_LAYER_20260825.md` をコミットするか
3. `7d62d25` を push するか（★オーナー指示があるまで push しない）

---

## 7. 観察として残っていること（★着手は未承認）

★**次に何をするかは `HANDOFF_P4_RACE_NEXT_SESSION_20260825.md` §6〜§7 が決めます**
（最優先はオーナーの実プレイ確認、次は実画面で見つかった不具合だけを 1 件ずつ）。
ここに書くのは、測定で見えていて**まだどこにも記録していない観察**だけです。

- **4 角の俯瞰の構図** — 馬群が画面左に寄り、右に空きが出る時間帯があります
- **直線 18.6 秒の扱い** — 参考映像にも約 19 秒の長い横追従があるので、まず長さは同じまま
  向きだけを変えました。分割するかは未検討
- **`finish-line` の画角** — カメラ引き継ぎで旧 v4 と変わっています（承認済み）。
  そのままでよいか、意図した画角に置き直すかは未決
- **未使用ショット 13 個** — 19 個定義のうち 6 個しか使っていません

---

## 8. 守り続ける決まり

- 実装は**指示書が出ている範囲のみ**。スコープ外の先走りは差し戻し
- 仕様の穴は推測で埋めず `QUESTIONS_*.md` に出す
- **push はオーナー指示があるまで行わない**
- `git reset --hard` / `git checkout --` / 未追跡ファイルの一括削除は**しない**
- `apps/web/package.json` のポート 3210 変更は保持し、**どのコミットにも含めない**
- 3D / S3 / v9 / 素材反復の論点には**戻らない**
- 決定論（憲法4）: `Date.now()` / `Math.random()` を直接呼ばない。台本の切り替えも
  **URL だけ**で決める（localStorage・時刻・乱数を使わない）
- レース結果・順位はサーバー権威（憲法3）。描画側は読むだけで書き換えない
