# 報告（P4）— D-070/071/072・V-18・カットごとの元スプライト・D-062 再測定

**宛先**: レビュー側
**関連**: D-062 / D-065 / D-066 / D-069 / **D-070 / D-071 / D-072** / **V-18** / §12.6 / C-6

---

## 本便で正典の改訂が要る決定をしたか

**しました。3件、裁定をお願いします。**

| # | |
|---|---|
| 🔴 **Q-P4-43** | ★**ゲージが直線でほぼ常に 0 に張り付きます**（75〜95%の馬）。§12.6 の読み取りが、一番要る場所で情報を持ちません |
| 🔴 **Q-P4-44** | ★**D-062 と V-18 ① は両立しません。** `w` が着順を教えるなら V-18 ① が落ちます |
| 🔴 **Q-P4-45** | `resolveIntervention` の引数 `averageSpeedMps` が**結果に効かなくなりました**。消してよいか |

---

## 1. 済んだこと

| 裁定 | 状態 |
|---|---|
| **D-070**（ゲージは状態・`emptyAtMeter` を渡さない） | ✅ 渡していません |
| **D-071**（`w` はエンジンが引く） | ✅ `laneAt` / `laneExtraM` をエンジンに。描画層からは撤去 |
| **D-072**（境界にスタミナを載せる・近似は作らない） | ✅ ★**近似を作っていません**（下記 §2） |
| **V-18**（新設） | ✅ ★**全距離 PASS**（ρ 0.065/0.054/0.040/0.013・内外差 5.0〜7.2馬身） |
| **カットごとに元スプライトを分ける** | ✅ シート契約 §5 に（far 64px / mid 96px / near 180px） |
| **D-062 の再測定** | ✅ ★**条件はまだ成立していません**（下記 §5） |

`npm test` **819件 緑**（+11）／ `npm run typecheck` 通過。

---

## 2. D-072 — ★近似を作らずに済んだ理由

### 消費は、もともと**区分線形**でした

`intervention.ts` の消費は「道中の率」と「仕掛けてからの率」の**2段だけ**です。
→ ★**節目を線で結べば、元の式と完全に一致します。** 丸めも当てはめもありません。

### ★平均速度が約分で消えました

```
§8b.2:  1秒あたりの消費 = 基準量 ÷ レース秒数 × ポジション係数
        レース秒数 = 距離 ÷ 平均速度
        秒数 = 進んだ距離 ÷ 平均速度

→ 1m あたりの消費 = 基準量 × ポジション係数 ÷ レース距離
   ★平均速度が入っていません（約分で消えました）
```

★**残り距離だけで残量が決まります。** 描画層は残り距離を知っているので、
時刻に直す必要がありません（直すと `BoundaryTimes` と二重に持つことになります）。

⚠️ ★**値は1ミリも変わりません。** 捨てた式（秒で計算する形）を**検査に残して**
突き合わせています。**5距離 × 6仕掛け × 3ポジション × 4スタミナ × 3速度 = 1080通り 一致**。

### ★出どころを1本にしました

`resolveIntervention` は**この状態から答えを出します**。別に計算していません。
⚠️ 別々に持てば必ず離れます（`jostle` が判定 0.06 / 製品 0.25 で離れ、
走路の幅が `ovalCourse` 20m / `formation` 25m で離れていたのと同じ形）。

### 節目に採ったもの

- 4つの境界（発走 / 勝負所 800m / 直線 400m / ゴール）
- ★**減り方が変わる点（＝仕掛けた地点）**
  ⚠️ これが無いと、800m と 400m の**間**で率が切り替わったことが、
  線で結んでも再現できません。

### ★`emptyAtMeter` は出していません（D-070）

内部の判定にだけ使い、描画層には渡していません。
出すとゲージが「状態」ではなく**予言**になります。

---

## 3. 🔴 Q-P4-43 — ★ゲージが直線で 0 に張り付きます

★**測りました**（`tools/diag-gauge.mjs`・400レース × 4距離 × 12頭・AI 代行の乗り方）。

```
距離      ★ゲージが 0 になる地点        該当する馬
1200m     残り 223m から                95%
1600m     残り 191m から                91%
2000m     残り 166m から                84%
2400m     残り 143m から                75%
```

⚠️ ★**一番盛り上がる直線の、まさにその区間でゲージが情報を持ちません。**

- §12.6 は「自馬の唯一の読み取り」
- C-6 は「人間が読んで押せるか」

→ ★**押す瞬間にゲージが 0 のまま動きません。**

### ⚠️ こちらでは直しません

直す方法はいくつも思いつきます（初期値を上げる／消費を下げる／ゲージの目盛りを
「残量」ではなく「最後まで保つか」に変える）。★**どれも §8b.2 の較正か、
ゲージの意味そのものの変更**です。**発明しません。**

- **A.** これは**仕様どおり**（＝直線は出し切っているのが正しい）で、そのまま出す
- **B.** §8b.2 を較正する（★どの値をどちらに動かすかを指定してください）
- **C.** ゲージの意味を変える（★何を表すかを指定してください）

---

## 4. ★ゲージの向き — 桁は違いますが、まだ揺れます

以前の近似は **0.653 の強さで逆**を向いていました。今回:

```
余力と着順の順位相関（★負が正しい向き＝余力が多い馬ほど着順の数字が小さい）

距離      残800m   残600m   残400m   残200m
1200m     −0.092   −0.073   −0.025   −0.013
1600m     −0.075   −0.041   −0.015   −0.001
2000m     −0.029   −0.007   ★+0.023  ★+0.031
2400m     −0.029   −0.025   −0.023   −0.018
```

★**桁が違います**（0.653 → 最大 0.092）。
⚠️ ★**ただし 2000m の直線で符号が揺れます。**

### ⚠️ 合否は出していません

道具に「最も弱いところが 0 以上なら FAIL」と書きかけて、**消しました**。
★**通る幅を自分で決めることになる**からです（D-065 の手順は ①測る ②報告 ③帯外なら再較正）。

→ ★**この幅（±0.03）を許すかどうか、裁定をください。**

---

## 5. ★D-062 の再測定 — 条件はまだ成立していません

裁定:

> `w` が入れば数字が動くはずなので、そのときにもう一度測ってください。

### ⚠️ 測る前に見つけたこと

★**画面ボットが `w` を見られませんでした。** `at.x` しか読んでおらず、
**縦位置（＝内外）を一切見ていませんでした。**

→ 特徴を2つ足しました（自分が外を回されているか／前の馬が内を通れているか）。
★**足したのはこの1回だけです。通るまで足していません。**

### 結果（1200レース・12頭）

```
                平面      斜め俯瞰      出走表
残り1200m       0.686  →  0.688        0.727
残り 800m       0.864  →  0.865
残り 400m       0.971  →  0.971
```

★**結論: 道中はまだ出走表を上回っていません。圧縮を緩める根拠は、いまもありません。**

### ⚠️ ★少ない試行で出た良い数字を、そのまま報告しかけました

300レースで測ったときは「**+0.021 で出走表を上回った**」と出ていました。
1200レースで測り直すと **+0.002** です。★**誤差でした。**

---

## 6. 🔴 Q-P4-44 — ★D-062 と V-18 ① は両立しません

これは較正の問題ではなく、**要求が正面から衝突しています。**

```
D-062   道中の画面が情報を運ぶこと（＝画面から着順が読めるようになること）
V-18 ①  枠順と着順の順位相関 ≤ 0.10（＝内外の位置が着順を決めないこと）
D-069   `w` を脚質から作らない（＝ w は能力と無相関）
```

★**`w` が着順を教えるなら、V-18 ① が落ちます。**
★**`w` が着順を教えないなら、D-062 の材料になりません。**

いまは後者です。**測って +0.002 なのは、故障ではなく設計どおり**です。

### 伺いたいこと

- **A.** ★**D-062 の材料を `w` に求めない**（＝道中の情報は別のもので運ぶ）
  → その場合、**何で運ぶか**を指定してください（⚠️ こちらで発明しません）
- **B.** V-18 ① の帯を緩める（★どこまでかを指定してください）
- **C.** ★**道中は出走表を上回らなくてよい**と決める
  （＝「道中は展開を見せる時間で、予測の材料は出走表でよい」）

★**C が素直だと思いますが、決めるのは裁定です。**

---

## 7. 🔴 Q-P4-45 — 効かなくなった引数

`resolveIntervention(horse, plan, **averageSpeedMps**, distanceMeter, balance)`

★**この引数は結果に効かなくなりました**（約分で消えたため）。値は1ミリも変わりません。

⚠️ ★**このままだと「渡した速度が効いている」と読めてしまいます。**
消すと公開している形が変わるので、**裁定をもらってから**にします。

---

## 8. ★カットごとの元スプライト（シート契約 §5）

| 名前 | コマ寸法 | ★馬の幅 | 1× | 2× |
|---|---|---|---|---|
| `horse-gallop-far` | 72×46 | **64px** | 64px | 128px |
| `horse-gallop-mid` | 108×69 | **96px** | 96px | 192px |
| `horse-gallop-near` | 200×127 | **180px** | 180px | 360px |

```
64px → 180px = 2.8倍  ＋ 2× ズーム → ★最大 5.6倍   参考の5倍に届く
```

⚠️ ★**焼いた 220×140 から縮めない**ことを契約に明記しました。
生アート（1コマ 362px）から**毎回焼き直します**。0.909 倍のような半端な比は
画素の格子が合わず、D-058 が禁じている形になります。

⚠️ ★セルは「最大のコマが収まる縮尺」で決めない、も明記しました
（一度それで**尾が隣のセルに写り込み**、走路に尾が1本浮いたため）。

---

## 9. ⚠️ ★自分の道具・自分の検査に叩かれた記録

| # | 何が起きたか |
|---|---|
| 1 | 向きの検査を**0 で止めた値**で比べ、★「0 が 0 より小さいこと」を要求して落ちた → ★標準的な馬は**ゴール前にゲージを使い切る**（st=600・最適な仕掛けで −17.1）と分かった |
| 2 | 節目で 0 に止めてから線を引き、★**0 を跨ぐ区間だけ 0.017 ずれた** → 止めるのは読み出しの一箇所だけにした |
| 3 | ★**画面ボットが縦位置を読んでいなかった** → `w` を出しても測れない状態だった |
| 4 | ★**300レースの良い数字**（+0.021）を報告しかけた → 1200レースで +0.002 |
| 5 | 道具に**自分で合否の帯を書きかけた** → 消した |

★§9 の「数字が出たら必ず絵を見るか対照を置く」は、今回も4回効きました。

---

## 10. 🔴 未裁定の一覧

| # | |
|---|---|
| 🔴 **Q-P4-43** | ゲージが直線で 0 に張り付く（§12.6・C-6） |
| 🔴 **Q-P4-44** | ★**D-062 と V-18 ① が両立しない** |
| 🔴 **Q-P4-45** | 効かなくなった引数を消してよいか |
| ⚪ **Q-P4-20 / 25 / 26 / 30** | 境界の位置／コーナー／距離ごとの勝ち時計／毛色を遺伝形質に |
| ⚪ **第3便** | ★まだ出していません |
| ★**オーナー** | **staging の DB パスワードと service_role キーのローテーション（未実施）** |

---

## 11. 次に進めるもの（止める場合は言ってください）

1. 実況帯の作り直し（顔＋3行・下から積む・馬名だけ色違い）
2. 芝の縞を斜めに／横位置の微細な揺れ／カメラのゆっくりした寄り
3. ★**斜め俯瞰で動画を1本出して、オーナーに見てもらう**（数字ではなく絵で確かめる）
