# デザイナーへ: 第 3 便の画面写しと、抜き直した層 — 2026-09-23

デザイナーの依頼「①`/design-check` の画面 ②抜き直した層のコミット」への返しです。

| ファイル | 中身 |
|---|---|
| `design-check-coat-9.webp` | **体の色 9 種**（第 3 便 §1 の値をそのまま入れたもの）＋ **18 頭の出走表** |
| `design-check-mane.webp` | たてがみ・尾の色（★**色は開発側の仮**。仕組みを見せるためだけ） |
| `train-body-idle-mane.webp` | **抜き直した層**（ボードの馬をこれに差し替えてください） |

## 層について（依頼の 2 点は両方とも直しました）

- **明るさ 平均 140 → 221 / 255**（ご指定は 224 前後）。→ **`brightness(1.6)` は外してください**
- **尾の先まで含めました**。上 62% で切っていたのを 82% に広げ、代わりに「**下側にしか無い塊**」（蹄・脚の飾り毛）を落とす規則にしました。位置で個別に外していないので、別の絵にも使えます

## 体の色 9 種（実測）

第 3 便 §1 の値と重みをそのまま入れました。

- 12 頭立ての隣接 **3.42 → 1.90 組**（そちらの計算値 1.86 と一致）
- 18 頭立て 5.25 → 2.90 組
- 1 組も無いレース 2% → **13.6%**

## ⚠️ この写しの限界

`/design-check` は **素材全体に CSS を掛けて**います。本番のレースは「**馬体の画素だけに掛ける**」別の経路です。
**白毛・月毛の本当の見え方は、レースに毛色を繋ぐ日にその経路で撮って送ります。**
この写しで見ていただきたいのは、**白毛・月毛が浮いていないか**だけです。

## 撮り方（撮り直すとき）

開発サーバー（`/design-check`）に対し、**ヘッドレス**の Chrome で `section[aria-labelledby="coat"]` と
`"mane"` を切り出して撮りました（`out/shot-design-check.mjs`・倍率 2）。
⚠️ **窓を開かないこと**（過去にオーナーの画面へ窓を開いていました）。
