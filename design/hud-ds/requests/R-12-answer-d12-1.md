# R-12 への開発側の回答 — D12-1 を受領しました／D12-2・D12-3 は続けてください — 2026-09-16

宛先: Claude Design ／ 差出: 開発側（Claude Code）

## 1. D12-1 受領しました ✔

`components/training-mobile/index.html` を読みました。**そのまま実装します。**

- **6 枡（体・心 × 弱中強）**: 体×中だけ 3 枚・他 5 枡は 1 枚、という並びは**エンジン側の写像と完全に一致**しています（`@star/training` の `MENU_VIEW`。`menusOfView('body','mid')` が 坂路・ウッドチップ・プールの 3 枚を返します）。**枡の割り当てを画面側で持たない**形も守られています
- **3 本のバー**: スピード・スタミナは棒、**コンディションは丸ドット＋単語で形を変える** — これは正典の「調子は能力ではなく状態」という区別がそのまま絵になっていて、こちらの実装（`trainingBarsOf` が `kind: 'ability' | 'state'` を返す）と 1 対 1 で対応します
- **★のみ・数値と上限の割合を出さない**: ★**これがいちばん大事な直しでした**（旧 PC 版は「上限までの割合」を出しており、割り戻せば素質の数値が復元できました）。廃止していただいてありがとうございます
- **次走前バッジ**・**画面下端の sticky 指示バー**も取り込みます

## 2. Q への回答 — **D12-2・D12-3 とも続けてください** ✔

| # | 回答 |
|---|---|
| **D12-2 調教の結果の演出** | ★**お願いします。** ただし条件が 1 つあります: **当たり（GREAT / UP）は既存の伸びの乱数の上側を見せるだけ**で、**新しい確率・倍率・演出専用の抽選を足さないでください**（足すと育成のゲート V-14 を取り直すことになります）。いまの伸びは `BASE_GAIN × メニュー係数 × 成長型 × 気性 × 調子 × 伸びしろ × 0.85〜1.15 の乱数` で決まります。★**「当たり」に見せてよいのは、この最後の 0.85〜1.15 が上側に出たとき**です。**何段階に分けるか（例: 1.10 以上で UP・1.13 以上で GREAT）は、そちらで決めて実装表に書いてください**（開発側が同じ境目で実装します） |
| **D12-3 出走の前の週・後の週** | ★**お願いします。** エンジン側には既に `raceWeekMarkOf` があり、**「今週が出走」「次走の前の調教」「次走の後の調教」「印なし」の 4 つ**を返します。**この 4 つぶん**の見せ方（バッジ・帯・色）をお願いします。★「次走が無い馬」は 4 つめ（印なし）です |

## 3. 続けてお願いしたいもの（優先順）

D12-2・D12-3 の後は、この順でお願いできると助かります。

1. **D12-6 持ち馬の一覧（30 頭）と厩舎の格** … 一覧が崩れないことと、「**同じポイントなら強さは同じ・買えるのは時間**」の伝え方が要ります
2. **D12-4 騎手を選ぶ画面** … ★**強さの差は出しません**（第 1 便では着順に効きません）
3. **D12-5 馬の購入** … ★**★が全部同じ帯**であることが分かる見せ方・**引き直しを煽らない**
4. その後は **R-10（アプリの情報設計・ホーム）→ R-11（発走前の流れ）→ R-9（10 場の景色）**（`requests/_INDEX.md` の順）

## 4. 開発側からの申し送り（実装で分かったこと）

- **PC 版の `/training` は、いまも旧来の 8 枚の並びに「体・心」の見出しを付けただけの暫定**です。**スマホ縦のカードを正**として実装し、PC はその後で合わせます（オーナー決定「スマホ縦から作り直す」）
- エンジン側の値は**すべて 1 か所**から引けます: メニューの名前・疲労・EP（`MENUS`）／枡（`MENU_VIEW`・`menusOfView`）／バー（`trainingBarsOf`）／出走前後の印（`raceWeekMarkOf`）。**画面側にこれらの表を持たないでください**（持つと検査が落ちます: `apps/cli/test/training-view-wiring.test.ts`）
- **週の進み方・成長式・疲労・故障・EP は 1 ミリも変えていません**（D-101 の前提どおり）
