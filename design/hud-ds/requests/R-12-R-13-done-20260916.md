<!-- 開発側 → デザイナー。実装の完了報告です（依頼ではありません） -->
# 実装が終わりました（2026-09-16）— R-12 の D12-1〜6 全件、R-13 の D13-1・3・4

★いただいたカードを**全部実装しました**。★ありがとうございました。

| 依頼 | カード | 実装した場所 |
|---|---|---|
| D12-1 | `training-mobile` | `/training`（スマホ縦・既実装） |
| **D12-2** | `training-result` | `/training` の結果カード |
| **D12-3** | 同上 | `/training` の週の印（4 値） |
| **D12-4** | `jockey-market` | `components/jockey-picker.tsx` → `/entry` の確認パネル |
| **D12-5** | 同上 | `/stable/market`（新規） |
| **D12-6** | `stable-roster` | `/stable` の一覧＋`components/stable-grade-panel.tsx` |
| **D13-1・3・4** | `horse-story` | `/stable/retired`（新規） |

---

## ★カードのおかげで見つかった穴が 2 つあります

★どちらも ★**カードが「この値を出す」と書いていたので気づけました**。

| # | 中身 |
|---|---|
| 1 | ★D12-2 の「乱数 1.13 以上 → GREAT」— ★**その乱数が週の記録に載っていませんでした**。★このまま作ると、画面が伸び幅から成長式を逆算することになります。→ ★既に引いていた値を記録に載せました |
| 2 | ★D13-1 の「種類ごとに色分けしたラベル」— ★**物語を返す関数が種類を落としていました**（文字列だけ）。★このままだと画面が**文から種類を推測**することになり、★文言を直した日に色が外れます。→ ★文 ＋ 種類 ＋ 週 を返す関数を足しました |

★**3 つ目**もありました: ★D12-3 の 4 値のうち ★**2 値（今週出走・出走あと）が構造的に出せない**状態でした
（★画面が「次走の有無」から 1 週と決め打ちしていたため）。★週数を持たせて直しました。

---

## ★お返事をお待ちしているもの（3 件）

| # | 中身 |
|---|---|
| 1 | ★**「3 週続けて」を 2 週にしますか** — ★UP 以上は 16.7% なので ★**3 週連続は 0.46%（約 216 週に 1 回）**で、★現役 182 週では 1 回も出ない馬が多数です。★2 週なら 2.8%（36 週に 1 回）。★実装は**週数を引数で受け取る形**にしてあるので、★どちらでもすぐ変えられます |
| 2 | ★**帯が 0 口になったときの見せ方** — ★在庫が下限を割ると「残り 1」ではなく ★**その帯が空**になります（正典 D-102 ⑤・帯を広げないため） |
| 3 | ★`horse-detail-mobile`・`race-entry-mobile`・`records-mobile`（`group="Web"`）の**紐づけ** — ★どの依頼に対するものか `_INDEX.md` に 1 行いただけますか |

★**D13-2（履歴書型の馬詳細）は「続けてください」とお答え済み**です（`R-12-R-13-answer-20260916b.md`）。
★子孫は **2 代**（産駒と孫）で、★**個性は「強い」と読める見せ方を避けて**ください（★いまは着順に効きません）。

---

## ★守ったことの確認（検査で固定しています）

- ★**素質の数値・上限までの割合を 1 か所も出していません**（★出るのは★と段だけ）
- ★**参加ポイントと賞金ポイントを合算していません**。★「購入」「円」「換金」を使わず「**迎える**」で統一
- ★**引き直しを煽る仕掛けを置いていません**（★「もう一度探す」は画面にありません）
- ★**「強くなる」「強化」を 1 語も使っていません**（★格は「速く仕上がる」、騎手は「強さの差はありません」）
- ★**持ち主の個人情報を出していません**（★出るのは牧場名まで）
- ★**値・帯・倍率・上限を画面に直書きしていません**（★すべて 1 か所から引いています）

★次にカードをいただけるときは、★**R-11（発走前の流れ）→ R-10（アプリの情報設計）→ R-9（10 場の景色）**の順が助かります。
★D13-2 が先でも構いません — ★できた順に実装します。
