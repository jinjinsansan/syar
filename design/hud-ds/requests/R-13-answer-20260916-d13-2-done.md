# 開発側 → デザイナー：3 件の回答を受け取り、D13-2 まで実装しました — 2026-09-16

> ★**新しい依頼ではありません。** ★お返事 3 件への受領報告と、★**D13-2（履歴書型の馬詳細）の実装完了**の報告です。

## 1. お返事 3 件、すべて実装しました

| # | いただいた回答 | ★実装したこと |
|---|---|---|
| ① | **「3週続けて」→ 2週に変更**（採用） | ★週数を **`TRAINING_STREAK_WEEKS = 2`** という **1 か所**に置き、★**画面には週数を書いていません**。★カードの `training-result` と同じ文言「2週続けて良い仕上がりです」を `/training`（スマホ縦）に出しました |
| ② | **0 口の枠は点線グレー＋「今は　いません」**（「残り0」は出さない・帯は広げない） | ★そのまま実装しました。★**枡の数は変えていません**（★在庫で枡を詰めると**帯が縮む**ので、空の枠をそのまま残します）。★「残り1」の黄バッジは**欠けた帯の先頭にだけ**出します |
| ③ | **`group="Web"` 3 枚は R-8b の続き・自発のモバイル改訂** | ★了解しました。★R-12/R-13 とは別系統として扱います |

★①は ★**「2 週でも 3 週でも通る」検査にしない**ようにしました。★検査は「★**1 つ増やすと現役 182 週の間に 1 回も出なくなる**」ことを見ています（★＝ 3 週を採らなかった理由そのものを固定しています）。

## 2. ★**D13-2（履歴書型の馬詳細）を実装しました**

`components/horse-resume` のカードのとおりに作りました（★スマホ縦の幅で `/stable/<馬>` に出ます。★PC 版の馬詳細はそのまま残しています）。

| カードの指定 | ★実装 |
|---|---|
| 表紙は格チップ＋馬名＋★のみ・現在（調子・疲労）を 1 行・下帯に「生涯のピーク」 | ✔ ★**素質の数値も上限%も出していません** |
| 判明した能力は D13-3 のセグメントバーを流用 | ✔ ★**段は `discoveryStageOf` が回数から決めます**（★画面は刻みを持ちません） |
| 個性は先天＝灰・後天＝シアン、見出し直下に「いまは着順に影響しません」 | ✔ ★さらに ★**どの個性が付くかを画面に書いていません** — ★発現値と出走履歴から**導かせています**（★境目を動かした日に画面だけ古くならないように） |
| 主戦騎手は肖像・名前・コンビ経験のみ | ✔ ★**親密度の言葉は騎手の画面と同じ関数**を使っています（★組み直していません） |
| 4 タブ［競走成績］［血統］［物語］［子孫］・選択中は青グロス・下辺を白 | ✔ |
| ［物語］は D13-1 と同じ型で**直近 3 件**＋「物語帳をすべて見る」 | ✔ ★**文は `storyLinesOf` が組み立てたものをそのまま出しています**（★画面で作っていません） |
| ［子孫］は 2 代まで・いない世代は「まだいません」の 1 行 | ✔ ★空のグリッドも 0 件のしるしも置いていません |

## 3. ★お伺いしたいこと（1 件だけ）

★**［血統］タブの中身です。** ★カードの実装表に「PC 版の 5 代表を流用する想定（本カードでは省略・★**別途モバイル版血統表が必要なら次のカードで**）」とありました。

→ ★いまは ★**「5 代の血統表は横に広いので、広い画面で開くと表で見られます」＋ インブリード係数とクロス**だけを出しています。
★**スマホ縦の血統表が要るか**、お考えを 1 行いただけると助かります（★`horse-detail-mobile` には「血統表 5 代は縦スクロールカルーセル」とありますので、★そちらの型を履歴書の中でも使う、という形でも構いません）。

## 4. 次にお願いしたいもの（優先順）

1. **R-11（発走前の流れ）** … ★済ですが、★残りの依頼の中では次に効きます
2. **R-10（アプリの情報設計・ホーム）**
3. **R-9（10 場の景色）**

★**できた順に同期して実装します。** ★今日いただいた 7 枚 ＋ D13-2 は、★**すべて当日に実装が終わっています**。
