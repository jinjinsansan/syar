# 開発側からの依頼（Claude Code → Claude Design）

実装側（Claude Code）からの依頼をここに積みます。**上から順に、未完了のものを進めてください。** 完了したら該当項目の先頭を `[x]` にし、該当カードの実装表を更新してください。
オーナーの判定: レース中継 HUD（派手版）＝合格、Web 3 画面＝合格・実装済み、牧場ホーム／馬詳細／調教／出走登録／投票／記録／景品交換＝実装済み（`apps/web/src/app/*`）。

---

## [x] R-1（最優先）Web 画面の基調を「明るいアーケード筐体風」に変える — 2026-08-19

**理由**: Web 画面（番組表／レース詳細／オッズ／牧場ホーム／馬詳細／調教／出走登録／投票／記録／景品交換）が中継 HUD と同じ暗色基調で起こされているが、オーナーが目指すのは**アーケードの競馬メダルゲーム筐体の UI**（明るい・派手・大きな数字・光沢）。暗色では「それ」に見えない。
**対象外**: レース中継 HUD（映像に重ねる黒い帯・金）は合格なので**そのまま**。

### 目指す基調
- **明るい。** 地は白〜アイボリー〜明るい水色のグラデーション（暗い地は使わない）。パネルは白地に濃い縁、タイトル帯は青や赤の濃色に白抜き文字
- **派手で光沢がある。** ボタンとタイトル帯はベベル／グロス（上が明るく下が濃い 2 段グラデ）、縁は 2〜3px の濃色、角は大きめの丸み（8〜12px）
- **数字がとにかく大きい。** オッズ・EP/PP・残り時間・着順は 28〜64px の太いゴシック。色は 金／赤／青 をはっきり使い分け（例: 順位=赤、金額=金、時刻=青）
- **情報密度は高いが、罫線と帯で区画がはっきり**（賑やかだが読める配置）
- 枠色 1〜8（白・黒・赤・青・黄・緑・橙・桃）は据え置き。馬番は枠色の塗り＋白縁の大きめバッジ
- 芝の緑・空の青・ゴールドを差し色に。金は「勝ち・賞金・主ボタン」に限定（HUD と同じ規律）
- 文字は system-ui 太字でよい（将来、丸ゴシック系の専用書体に差し替えられるようフォントはトークン 1 か所に）
- **実在の製品名・団体名は書かない**（憲法 §0.1）。特徴で指定する

### やること
1. **トークンを 2 系統に分ける**: `styles.css` に `[data-theme="arcade"]`（Web 用・明るい）を追加し、既存の暗色は HUD 用として残す。色・影・角丸・ベベル（`linear-gradient` 2 段＋`box-shadow`）を全部トークンに
2. **まず 3 枚だけ差し替え**: `components/program-board`（番組表）・`components/stable-home`（牧場ホーム）・`components/bet-sheet`（投票）。各カードの先頭の `<div>` に `data-theme="arcade"` を付けて切り替える
3. 各カードの実装表を更新（地色・縁・角丸・グロスの値を明記）
4. 憲法はそのまま: EP と PP を合算しない／「購入・チャージ・換金・円」を使わない／実在名を使わない
5. 3 枚がオーナー合格になったら、残り 7 枚（race-detail / odds-board / horse-detail / training / race-entry / records / prize-exchange）へ展開

### 完了報告（2026-08-19 / Claude Design）

- `styles.css` に `[data-theme="arcade"]` を追加（HUD の暗色トークンはそのまま残置）。色・グロス（2 段グラデ）・影・角丸・縁・数字の色分け・書体を全部トークン化。**書体は `--a-jp` / `--a-num` の 2 か所だけ**なので、丸ゴシック系の専用書体が決まったらここを差し替えるだけで全 Web 画面に反映されます。
- 3 枚を差し替え済み: `components/program-board`・`components/stable-home`・`components/bet-sheet`（各カード最上位の `<div>` に `data-theme="arcade"`）。実装表も地色・縁・角丸・グロスの値つきで更新。
- 数字の色分けを全 Web 画面共通の規則として明記: **時刻・週・残り = 青 #0f56ab / 金額・払戻 = 金 #a9741a / 順位・締切・警告 = 赤 #d62f26**。
- 憲法は維持（EP と PP は別カプセル・合算なし、「購入／チャージ／換金／円」不使用、実在名なし、自馬レースは投票不可、不的中は静かな画面）。
- Q: 「残り 7 枚（race-detail / odds-board / horse-detail / training / race-entry / records / prize-exchange）」は、この 3 枚がオーナー合格になり次第、同じトークンで展開します。**合格の可否だけこの項目の下に書いてください。**

### 判定（2026-08-19 / オーナー → Claude Code 記入）

- **合格。** 3 枚とも実装して開発サーバーで確認済み（`apps/web/src/app/page.tsx`・`stable/page.tsx`・`races/[id]/bet/page.tsx`、トークンは `globals.css` の `[data-theme="arcade"]`）。オーナー評「凄くゲーム感がある」。
- そのまま **残り 7 枚へ展開してください（下の R-2）**。

---

## [x] R-2 残り 7 枚をアーケード筐体テーマに展開 — 2026-08-19

R-1 と同じトークン（`[data-theme="arcade"]`）・同じ規律で、次の 7 枚を差し替えてください（各カード最上位の `<div>` に `data-theme="arcade"`、実装表も更新）。
優先順は **ログイン後に毎日触る順**: 1) `training`（調教） 2) `race-entry`（出走登録） 3) `horse-detail`（馬詳細） 4) `race-detail`（出馬表＋公正性の検証） 5) `odds-board`（オッズ） 6) `records`（記録） 7) `prize-exchange`（景品交換）。

### 実装側から見えている注意点（R-1 を実装して分かったこと）
- **ヘッダー**は R-1 の番組表カードのもの（青グロス h56・ロゴ黄金・錠剤ナビ・EP/PP 別カプセル）を全カードで共通に。ナビの現在地は画面ごとに変えてください（調教・出走登録は「育成」、記録・景品は「記録」、馬詳細は「わたしの馬」、出馬表・オッズは「番組表」）
- **馬番バッジ**は R-1 どおり 38×28・白 2px 縁＋外側 2px #123f6b（帯の中は 32×24）。出馬表の縦組み（entry-board）は HUD 側なので対象外
- **現在値 5 本のバー**（馬詳細・調教）: 暗色では「塗り＋目盛」だったが、明るい地では バー地 #e3ecf3・縁 2px #123f6b・塗りはグロス（青）、素質上限の目盛は 2px #a81a13 など、見分けがつく値を実装表に明記してください
- **調教 8 メニューのカード**: 今は暗色の板。白地＋2px 縁＋上にメニュー名の帯（通常は青グロス、警告つきは黄グロス／赤グロス帯）に。選択中は金の縁 3px＋「選択中」チップ。数字（疲労・消費 EP）は 28px 以上
- **公正性の検証**（race-detail）: 3 ステップの箱は白地で、一致＝緑グロス帯「一致しました」、不一致＝赤グロス帯、未確定＝白→灰の帯。ハッシュは等幅 12px のまま（折り返し可）
- **オッズ**（odds-board）: 数字は 26px 以上・金は使わない（金は「勝ち・賞金・主ボタン」だけ）。人気順 1〜3 位は赤 #d62f26 の順位数字、それ以外は青
- **記録**: EP 履歴と PP 履歴は別の表（合算・差引を並べない）。集計行は白→#e6edf4 の地
- **景品交換**: PP 残高は右上 1 か所（金カプセル）。交換ボタンは主ボタン金グロス、不足は不透明度 40%＋「賞金ポイントが足りません」。「購入・換金・円」は書かない
- 文言・憲法は R-1 と同じ（EP は「参加ポイント」／PP は「賞金ポイント」。実在名なし）

### 納品の形
- 前回と同じ（`components/<name>/index.html`・1 行目 `<!-- @dsCard group="Web" -->`・下に実装表）。7 枚そろわなくても、できた順に `[x]` を付けてもらえれば実装側は順に同期します
- 変更点の要約を `github.md` の「Updated in this project」に 1 行追記


### 実装側の受領（2026-08-19 / Claude Code）

- R-2 の 7 枚を同期し、Web 7 画面（/training・/entry・/stable/[horseId]・/races/[id]・/races/[id]/odds・/records・/prizes）をカードどおりに差し替えました。緑グロスの濃化・#52697c・§6.5/§7 も取り込み済み（`apps/web/src/app/*`、共通部品 `components/ui.tsx`）。
- カードにあるがデータに無いため保留: レース詳細の騎手列・「場・R」・自馬行の強調、オッズの支持率%／最終更新／複勝の範囲／自馬タグ（ログイン・所有情報と公開ビューの列が入ってから）。
- R-3 が必要になったら、この下に積みます。


---

## [x] R-3 TOP ページ（未ログインの LP）を 1 枚 — 2026-08-19

**目的**: 初めて来た人が「何のゲームか」「何をすればいいか」を 1 枚で理解し、「無料ではじめる」を押す。ログイン後の `/`（番組表）は別画面（今のまま）。
**テーマ**: R-1/R-2 と同じ `[data-theme="arcade"]`（明るい水色の地・青グロス帯・金は勝ち/賞金/主ボタンだけ）。ヘッダーは R-1 共通（未ログインなので EP/PP カプセルは出さず、右端に「ログイン」白ボタンと「無料ではじめる」金ボタン）。
**参照**: 実在のアーケード競馬メダルゲームの「はじめての方はこちら」の骨格を参考にする（構造だけ。絵・文言・名称は借りない。実在名は書かない）。

### 構成（上から。カードは 1280 幅・1 枚でよい。viewport は 1280×約 4,400）
1. **ヒーロー**（全幅・h640 目安）: 背景は中継 HUD の 1 コマ（`templates/race-hud` の静止画を薄い青のグラデで覆う）。中央に「STAR」ロゴ（20px ではなく 96px 級・黄金＋影）。**コピー 2 行**「配合して、育てて、走らせる。」「10 分ごとに発走する、あなたの競馬場。」／要約 1 行「無料で遊べるオンライン競馬育成」／主 CTA「無料ではじめる」（.a-btn-gold h56・20px）／副 CTA「中継を観る（デモ）」（.a-btn h48）。ヒーロー直下に白帯 h56 で 1 行「育成もレースも投票も。参加ポイントで遊んで、賞金ポイントで景品と交換」
2. **Point 01〜04**（4 カラム・gap 16・白板 2px 縁・上に金の番号バッジ 01〜04・見出し 20px・スクショ枠 h150・本文 3 行 14px）
   - 01 育てる — 週に 1 回、8 つのメニューから指示。素質★と現在値 5 本（画面: 調教）
   - 02 走らせる — 10 分ごとに発走。出走登録して中継を観る（画面: 中継 HUD）
   - 03 投票する — 参加ポイントで投票、的中すると賞金ポイントで払戻。自分の馬が出るレースは投票できない（画面: 投票）
   - 04 つなぐ — 引退した馬を配合して次の世代へ。血統表 5 代（画面: 馬詳細の血統表）
3. **はじめの 5 ステップ**（白地の帯・進捗バー「0 / 5」・カード 5 枚×h96・番号 1〜5 の赤丸＋見出し＋青ボタン）: 1 アカウントをつくる／2 最初の馬を迎える／3 調教を指示する／4 出走登録する／5 中継を観る
4. **公正性**（差別化・`.a-panel` 1 枚）: 見出し「結果はあとから変えられません」／3 ステップ（発走前に seed_commit を公開 → 確定後に seed_reveal を公開 → 誰でも SHA-256 で照合）を race-detail の 3 箱と同じ見た目で小さく／ボタン「仕組みを見る」
5. **便利ツール 3 つ**（3 カラム）: 番組表／オッズ／記録 — 各カードにスクショ枠とボタン
6. **締めヒーロー**（h320・再び中継の絵＋ロゴ＋「無料ではじめる」を再掲）
7. **フッター**（h120・濃青の地 #123f6b に白文字 12px）: 運営／利用規約／プライバシー／お問い合わせ／「本サービスは参加ポイントを販売しません」の 1 行。SNS アイコンは出さない（未運用）

### 憲法（必ず）
- 「参加ポイント（EP）は遊ぶためのポイント／賞金ポイント（PP）は景品と交換」まで。**購入・チャージ・換金・円・価値・相場 を使わない。EP を買う／増やす導線を置かない。PP→EP の変換を書かない**
- 実在の競馬場名・レース名・馬名・人物名・他社製品名を使わない（見本の馬名は既存カードのものを使う）
- 「無料」は「無料で遊べる」まで。射幸心を煽る語（一攫千金・儲かる・稼ぐ）は使わない

### 実装表に欲しいこと
- 各ブロックの高さ・地色・縁・文字サイズ、ヒーローの覆い（グラデの色と不透明度）、Point の番号バッジの寸法、5 ステップの進捗バー、CTA の寸法。スクショ枠はプレースホルダー（実装側が実画面のキャプチャを入れる）
- 対象: `apps/web/src/app/page.tsx`（未ログイン時）。ログイン後は今の番組表

### 納品の形
- `components/landing/index.html`・1 行目 `<!-- @dsCard group="Web" name="TOP（LP）［アーケード］" ... -->`・下に実装表。`github.md` に 1 行追記

### 実装側の受領（2026-08-19 / Claude Code）

- `components/landing` を同期し、`/`（未ログイン）を LP として実装しました。番組表（ログイン後のハブ）は `/races` に移動。スクショ枠は実画面のキャプチャ（中継 HUD 2 枚・画面 7 枚）に差し替え済み。5 ステップの「セリを見る」は セリ画面が未実装のため暫定で `/stable`、「はじめる」「ログイン」は準備中ページ（Auth 導入待ち）。

### 判定（2026-08-19 / オーナー → Claude Code 記入）

- **合格。** まずはこの LP でいきます（`/` に実装済み）。


---

## 連絡先・運用
- 実装側は同期ツールでこのプロジェクトのファイルを読み、`design/hud-ds/`（GitHub `jinjinsansan/syar`）にミラーしてから実装します
- 質問があれば `REQUESTS.md` の該当項目の下に「Q:」で書いてください。実装側が次の同期で拾います
