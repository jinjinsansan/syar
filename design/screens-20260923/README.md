# いまの画面の写し（2026-09-23・デザイナー用）

第 1 便の確認事項 10「添付画像が届いていない」への回答です。**本番**（`86040e0`）の画面を、開発側がそのまま撮りました。

| ファイル | 画面 | 幅 |
|---|---|---|
| `setup-390.webp` / `setup-1280.webp` | はじめの設定（表示名・牧場名・勝負服 16 色） | 390 / 1280 |
| `train-390.webp` / `train-1280.webp` | 育成モード。**上に常設レースの帯**が出ています | 390 / 1280 |
| `stable-retired-390.webp` / `stable-retired-1280.webp` | 引退馬・馬物語帳。**生涯の記録の色**（誕生・初調教・デビュー・初勝利・適性判明・重賞勝ち・故障・復帰・自己最高・名コンビ・最高格勝ち・ラストラン）が見えます | 390 / 1280 |
| `race-390.webp` / `race-1280.webp` | レースの画面 | 390 / 1280 |

## 読むときの注意

- **ログインしていない状態**で撮っています。`train` は「厩舎を見るにはログインしてください」と出ており、育成の中身は写っていません。**常設レースの帯**（上部の紺色の帯）は、この状態でも出ます。
- `stable-retired` は**見本のデータ**で描かれています（実在の馬ではありません）。色の見本として使ってください。
- 画素は 2 倍で撮ってあります（390 の画像は 780px 幅）。
- 第 2 便で色を決めていただきたい「役割の変更」は、**この写しにはまだありません**（種類を足したばかりで、画面では仮に「引退」と同じ色にしています）。

## 撮り方（同じものを撮り直すとき）

本番の URL に対し、ヘッドレスの Chrome で `/setup`・`/train`・`/stable/retired`・`/race` を 390 と 1280 の幅で撮り、webp（品質 82）に変換しました。
