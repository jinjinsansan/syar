IMAGE EDIT TASK — output a TRANSPARENT OVERLAY LAYER, not a horse.

REFERENCE IMAGE (pass it as an actual image input to the image tool):
  apps/web/public/art/uma/horse-face.png

If you cannot attach that file as an image input, STOP and say so. Do not work from the text alone.

WHAT THE REFERENCE IS
A side view of a cartoon horse's head. Above its large eye there is a HEAVY, ANGULAR, DARK BROW
that makes the expression look grumpy. The artwork is FINISHED.
You must NOT redraw it, NOT restyle it, NOT output it.

WHAT TO OUTPUT
A single layer, the SAME PIXEL SIZE as the reference, that is FULLY TRANSPARENT EVERYWHERE
EXCEPT for one small shape drawn over the brow area. When this layer is placed on top of the
reference at the exact same position, the horse must look BRIGHT and PLEASED instead of grumpy.

THE SHAPE
  - First, a patch of plain coat that COVERS the existing heavy angular brow completely.
    It is filled with the SAME coat colour and the SAME cel shading as the skin around the brow
    in the reference, so that the brow simply disappears and the forehead looks unbroken.
  - On top of that patch, draw a NEW brow: a thinner, softer, slightly ARCHED line, placed
    a little HIGHER than the old one, so the eye reads as wide open and cheerful.
  - Nothing else. No eye, no ear, no muzzle, no mane, no neck, no outline of the head.
    Do NOT cover the eyeball itself — the eye must stay exactly as it is in the reference.

HARD RULES
  - At least 90% of the output pixels MUST be fully transparent (alpha 0).
  - Do NOT draw the whole head. Do NOT draw a horse. Do NOT fill the background.
  - Do NOT move or resize anything: the shape must sit exactly where the brow is in the reference.
  - Same canvas size as the reference. Same resolution.
  - No text, letters, numbers, logos or watermarks.

If transparency is impossible, STOP and say so. Do not substitute a solid background.
