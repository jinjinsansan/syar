# 引継ぎ書 — 認証をメール＋パスワードに（D-113）／ 初回セットアップ RPC（2026-09-18）

> ★このセッションで何をして、何が未決かをそのまま書きます。
> ★報告は鵜呑みにされない前提です（CLAUDE.md）。**数字はすべて実測です。**
> ★**うまくいかなかったことと、自分が作った欠陥も書いています。**

## 0. 位置（同一作業ツリーの作法・CLAUDE.md）

| | |
|---|---|
| ブランチ | `p4/race-30sec-cuts`（★`main` も同じ位置） |
| 開始 HEAD | `4eacb18` 診断の誤報を分けた（流れる背景の 2 枚目を「切落し」に数えていた） |
| 終了 HEAD | `0070004` V-19 ⑭ を実測で閉じた／★この便で私が作った欠陥 2 件を報告 |
| 差分 | `git diff --numstat 4eacb18..0070004` ＝ **20 ファイル ／ +3,062 行 ／ −28 行**（16 コミット） |
| 本番 | ✅ **`main` へ push 済み。`https://star-two-chi.vercel.app/`** に反映済み |
| staging | ✅ 移行 `0029`・`0030`・`0031` を適用済み |

### ⚠️ 意図してコミットしていないもの

| ファイル | 理由 |
|---|---|
| `STAR_SPEC_v2.0.md` | ★**レビュー側の起票**（D-113 ほか）。★このリポジトリの D 起票はすべて「受領: …／★開発側の変更ではありません」の形で、★**正典の改訂はレビュー側の発行物**です |
| `REVIEW_SETUP_PREDICATE_VERDICT_20260918.md` | ★**レビュー側の裁定書**（未追跡）。★同上 |
| `apps/web/public/art/horse-jockey-chibi-*`・`title-eye` の削除 | ★**前の便からの持ち越し**。この便のものではないので巻き込んでいません |

---

## 1. このセッションの 16 コミット

```
0070004 V-19 ⑭ を実測で閉じた／★この便で私が作った欠陥 2 件を報告
00d3475 渡し状: 手順 6 完了（0031）と、別件の露出 3 件の報告
c676c51 0031: 初回セットアップ RPC（口座・初期 EP・初期馬を 1 トランザクションで）
4199539 V-19: 登録を伴う項目のうち 4 件を確認メール無しで測り、不合格 2 件を検出
2f3399a 照会 Q-SETUP-05: 裁定 Q-SETUP-03 ① の前提（新馬戦の出走資格の述語）が存在しない
18fad7d 渡し状: 手順 6 を本番でも確認（cde2981 → 配備 Ready・入力欄が実在）
cde2981 手順 6: ログインと登録を実装（入力欄まで）／ ⑨⑪ を構文木で固定
44ec965 渡し状: 本番の状態（0021〜0030 の 10 本が未適用）とオーナー手配 5 件を追記
65f6e04 手順 6 の第一歩: E-6b を実装（移行 0030）し、DB 側の重ねが働くことを実測
82afec9 手順 5 完了: V-19 をメール＋パスワード経路の形で測る道具を作り、staging で実測
ef84544 手順 5 に着手・手順 7 の当座分: 設定を読む道具と、/signup・/login の見た目合わせ
126e4f6 裁定 §6 手順 2 完了: auth.identities に provider='email' の行ができることを実測
675f534 道具: signUp が通るメールのドメインを探す（裁定 §6 手順 2 の前提）
6d7c027 裁定 Q-AUTH-08/09 の受領: 照会と渡し状の訂正、本番ワーカーの版の読み取り、道具 2 本
839365e 引き渡し: 認証を LINE からメール＋パスワードに変える（開発側 → レビュー側）
e5c4b8d 照会: 開発期間中の認証をメール＋パスワードにする（Q-AUTH-08・Q-AUTH-09）
```

### 担当範囲（このセッションが触ったファイル）

```
   2/-1   CLAUDE.md                                   ★「Render」を VPS に訂正
 708/0    HANDOFF_AUTH_TO_REVIEW_20260918.md          ★渡し状（レビュー側へ）
 196/0    QUESTIONS_AUTH_EMAIL_PASSWORD_20260918.md   ★照会 Q-AUTH-08/09
 125/0    QUESTIONS_SETUP_PREDICATE_20260918.md       ★照会 Q-SETUP-05
 103/0    apps/cli/test/auth-wiring.test.ts           ★新規・V-19 ⑨⑪ を構文木で
 121/0    apps/cli/test/rpc-guard.test.ts             ★第三の登録簿 ＋ stripFunctionComments
   5/0    apps/web/src/app/design-check/page.tsx      ★/signup・/login を一覧に追加
 153/-11  apps/web/src/app/login/page.tsx             ★ログイン（入力欄）
 203/-16  apps/web/src/app/signup/page.tsx            ★登録（入力欄）
  41/0    apps/web/src/lib/supabase.ts                ★authClient() を新設
  96/0    db/migrations/0030_setup_requires_email_confirmed.sql   ★E-6b
 209/0    db/migrations/0031_setup_account.sql        ★セットアップ RPC ＋ 勝負服の列
  83/0    tools/cleanup-probe-user.mjs                ★新規
  17/0    tools/lib/classification.mjs                ★道具 5 本を R-24 に登録
   7/0    tools/lib/exposure-registry.mjs             ★関数 2 つを V-20 に登録
 128/0    tools/probe-auth-identities.mjs             ★新規
 103/0    tools/probe-signup-domain.mjs               ★新規
  91/0    tools/read-auth-identities.mjs              ★新規
 105/0    tools/read-auth-settings.mjs                ★新規
 566/0    tools/verify-v19-email.mjs                  ★新規・V-19 の経路別の測定
```

---

## 2. 何をしたか（裁定 §6 の手順で）

| # | 手順 | 状態 |
|---|---|---|
| 1 | 本番ワーカーの版を読み取りだけで確認・`CLAUDE.md` の訂正 | ✅ |
| 2 | `auth.identities` の確認 | ✅ |
| 3 | 照会 §2.2 の数を揃える | ✅ |
| 4 | 正典の起票を待つ | ✅（レビュー側） |
| 5 | **V-19 の書き換え** | ✅ |
| 6 | **実装**（登録・ログイン・セットアップ RPC） | ✅ |
| 7 | `/signup`・`/login`・`/design-check` | ✅ |

### V-19 の到達点（★合格の出し方が裁定 §3-4 のとおり）

```
=== メール＋パスワード経路の V-19 ===
  有効な項目: 18 件（合格 15 ／ 🔴 不合格 3）
  対象外    : 4 件（理由は上に明記）
  未実施    : 6 件（★これを「合格」に数えない）
```

★**「16 項目合格」とだけ出す形を作っていません。**

| | 中身 |
|---|---|
| **読み替え 5**（①③⑯⑤⑩） | ★①③⑯ は **Supabase のセッショントークン**に読み替え。★**この経路で唯一の JWT を誰も検査しない形にしない** |
| **対象外 4**（②④⑦⑧） | ★理由を出力に明記 |
| 🔴 **不合格 3** | **E-8**（誰でも登録できる）・**E-5**（`password123` が通る）・**E-4**（★パスワード変更で他セッションが失効しない） |
| **未実施 6** | ⑬・E-1・E-2・E-3（★確認メールや同時実行が要る）ほか |

★**不合格 3 件はすべて Supabase の管理画面の設定**で、★**オーナー判断で当面このまま**です
（★「URL を誰も知らないので実害がない」・2026-09-18）。★検査が 🔴 を出し続けるのが正しい状態です。

---

## 3. ★実測で確かめたこと（★「動くはず」で済ませていないもの）

| # | 何を | どう確かめたか |
|---|---|---|
| 1 | 本番ワーカーの版 | ✔ VPS で `readlink -f /opt/star-current` → **`70d9982`**・`NRestarts=0`・周 69.7〜113.2 秒 |
| 2 | E-6b（DB 側の重ね） | ✔ **確認済みでトークンを取り、DB で `email_confirmed_at` を null に戻す** → 同じトークンで拒否に変わる |
| 3 | ①③⑯ | ✔ 改竄／期限切れ／`alg=none` のトークンが **401**。★⑯ は `JWT is unsecured but expected 'alg' was not 'none'` |
| 4 | ⑭ | ✔ 2 回目 **409**・口座 1・EP 2000・馬 1。★**変異で両方向**（0 にすると赤・戻すと緑） |
| 5 | 本番の画面 | ✔ **配信される HTML** に `type="email"` 1・`type="password"` 1 |
| 6 | `0031` の候補判定 | ✔ 実データで **候補 6,601 頭**・存在しない馬には `false`（対照つき） |
| 7 | 復旧 | ✔ 関数の本文が**リポジトリの `0031` と一致**・署名も登録簿と一致 |
| 8 | staging の後片付け | ✔ 利用者 6・口座 0・孤児 0・候補 6,601（★基準どおり） |

---

## 4. 🔴 未決・引き継ぎ事項

### ① レビュー側の裁定待ち — **なし**（★Q-SETUP-05 は裁定済み）

### ② レビュー側が承認待ちのもの（★オーナー宛て・レビュー側の提案）

| | |
|---|---|
| 1 | ★**08-20 裁定 §3-① に撤回の註記**（★今のままだと次に読んだ人が同じ履行不能の指示を再発行します） |
| 2 | ★引継ぎ書の【最新】節に本裁定を追記 |
| 3 | ★記憶に 1 行（「あるはず」で指示しない） |
| 4 | ★**§10.3 註記 ＋ D-114 の起票**（★裁定 §5・★未起票） |

### ③ オーナー手配事項

| | | いつまで |
|---|---|---|
| 🔴 **V-10** | `npm run verify:pmin -- --races 40`（★別のターミナルで 1 本・出力を切らない） | ★人を迎える前（★ブロッキング） |
| **SMTP** | 確認メールの送信（★開発側 1 人が 8 回で `429` に当たりました） | 人を迎える前 |
| **招待制ほか** | E-8・E-5・E-4 の設定（★当面このままで合意済み） | 人を迎える前 |
| **本番への適用** | `0021`→`0022`→V-20→`0023`→**`deploy.sh` で配備**→`0024`→`0028`→`0029`→**`0030`**→**`0031`** | 本番で認証を使う前 |
| **L-9** | 「馬物語」の名称（★09-17 から持ち越し） | — |

### ④ 🔴 本便で見つけた別件（★裁定 §4 の作法で直していません）

| # | |
|---|---|
| 1 | ★**実体テーブル 3 つが anon に開いています** — `0024:97`・`0025:57`・`0027:46` が `grant select … to anon`。★`0018` が「実体テーブルには一切 grant しない」と宣言しているのに反します。✔ staging で 1 行ずつ読める（**V-20 ②③ 不合格**）。★**登録簿にも記録が無く、過去の報告にもありません**。✅ **本番は露出なし**（未適用のため）→ 🔴 ★**本番へ `0024` 以降を当てる前に扱いを決めてください**。★中身は `finishPosition`/`raceName`/`jockeyName` だけで **§12.4 には触れません** |
| 2 | ★`loadRaceablePool` の**註釈と実装の食い違い**（「現役だけ」と書いてあるのに `retired_at_week` を見ていない）。★裁定 §4 が「クラス分けの便でまとめて直す・実害は未測定」としています |
| 3 | ★**新馬戦の出走資格の述語が存在しない** — ★`0031` の「戦績 0」は**暫定**。★**クラス分けの便で共通の述語に置き換え、V-4・V-5・V-6・V-18 を取り直す**（★1 日 42 本・全 144 本の 29% が動きます） |

### ⑤ 次のセッションが続ける作業

★**裁定 §6 の 3 番目**: ★**帯の下のゲート（「適切な育成でキャリア中に 1 勝」）の測定。**

★材料は揃っています:

| | |
|---|---|
| 測る量 | ★`starsOfPotential`（`packages/sim-engine/src/stars.ts`）。★`sameStarBand()` が既にあり「★が同じ」が帯の定義 |
| 上のゲート | ★★段をひとつ選べば「★表示が全個体同一」は**構造的に満たされます** |
| 下のゲート | ★`racing-season.ts` の `CareerRecord{starts,wins,top3,prize}` と `runSeason(...)`。★**`career.wins >= 1` で測れます** |
| 方針の錨 | ★**`balanced`**（★`packages/training/src/temper.ts:67` が「代表方針」と記録。★私が方針を選ばない） |
| 書く場所 | ★`verify-v14.ts` の形（`runCareer(horse, policy, horseIndex)` ＋ 表で出す）に倣う。★★**★段別に勝利を数える口は既存に無い**と確認済み |

⚠️ ★**所要をまだ測れていません。** ★`npm run simulate -- --generations 2/4 --seed 42 --selection race` は
**0.8〜2.3 秒**でしたが、★**`隔世遺伝 0/0` で早々に打ち切られ、`runSeason` に到達していません**。
★`REPORT_P0_20260804.md:139` が「隔世遺伝は祖父母が存在しないと引けない（創始世代の直仔＝第 1〜6 世代）」と
記録しているので、★**7 世代以上でないと成立しません。★まずそこで測り直してください。**

⚠️ ★**その `FAIL` は欠陥ではありません**（★出力自身が「`V-2c` は別実行で確認すること」と書いています）。
★**「落ちた」と「測れない」を取り違えないこと。**

---

## 5. 🔴 このセッションで私が作った欠陥（★全部直しましたが、記録します）

| # | 欠陥 | 直し方 |
|---|---|---|
| 1 | ★**型検査の失敗を「合格」と報告した** — `cmd \| tail; echo $?` が **`tail` の終了コード**を読んでいた | ★**合否を出すコマンドを切らない。終了コードは道具自身から取る** |
| 2 | ★**後片付けが漏れ、staging に口座 2 件が残った** — `deleteUser` の戻り値だけを見て「✅ 消しました」と表示。★**D-078 が `NO ACTION` を意図しているので口座がある間は消せない** | ★**馬 → 台帳 → 口座 → auth の順**にし、★**消えたことを DB で確かめてから表示**・★**残骸の総量も出す** |
| 3 | ★**⑭-b が「冪等が壊れていても緑」** — 口座が既にある利用者で測っていた | ★**口座を持たない別の利用者**で測る |
| 4 | ★**E-6b が「別の理由で通った ✅」** — 未確認ではサインインもできず、★**RPC に到達していなかった** | ★**確認済みでトークンを取り、DB で null に戻す** |
| 5 | ★**変異が DB に届かないまま「噛んだ」と報告しかけた** — `migrate.mjs` が**正しく拒否**していた | ★**移行ファイルではなく DB の関数を差し替える。★変異が入ったことを先に実測** |
| 6 | ★**`comment on` の文字列リテラルを「呼んでいる」と誤判定** | ★`stripFunctionComments` を追加（★`blankComments` は**変えない** — 7 か所が**リテラルが残ることを前提**にしている） |
| 7 | ★**出力を切って 4 度誤診**（`vercel whoami`／`git push`／`vercel env ls`／型検査） | ★**判定に使う行を切らない** |
| 8 | ★**bash の入れ子引用を壊し `SPEC.md: command not found`** が出た（★実行はされていない。★`SPEC.md` は**廃棄済み文書**） | ★**スクリプトはファイルに書いて実行する** |

★**共通の形**: ★**「★ が出たとき、何がそれを通したのか」を確かめていなかった**ことです。
★対照を併記する・変異が届いたか先に見る・道具自身の終了コードを読む — ★**記憶に残しました**。

---

## 6. ★次のセッションが最初に読むもの

| 順 | |
|---|---|
| 1 | ★**この引継ぎ書** |
| 2 | ★`REVIEW_SETUP_PREDICATE_VERDICT_20260918.md`（★**未コミット**・レビュー側の裁定） |
| 3 | ★`HANDOFF_AUTH_TO_REVIEW_20260918.md`（★§9-1〜9-17 に**実測と私の誤りの訂正**が入っています） |
| 4 | ★`STAR_SPEC_v2.0.md` の **D-113**（★未コミット・レビュー側の起票） |

★**push が 5 回止まった件**は恒久解決しました（★`~/.gitconfig` の `gist.github.com` の空ヘルパ行が原因。
★オーナーが `--unset-all` で解決）。★★診断の順序は記憶 `push-hangs-on-credential-manager` に残しています。
