# 引継ぎ書 — 最後の直線のせめぎ合い（次のセッションはここから）

作成: 2026-08-26 / ブランチ `p4/race-30sec-cuts`
経緯と数字はすべて: `REPORT_P4_STRAIGHT_CONTEST_20260826.md`
★**復帰点タグ**: `p4-straight-contest-20260826`

---

## ★0. まずこれを開く

```powershell
cd V:\dev\Cusor\star\apps\web
npm run dev
```

# http://localhost:3210/race?seed=253&cinematography=v6

⚠️ ★**`?cinematography=v6` と `?seed=253` の両方が要ります。**
　`/race` だけだと**既定の v5**（旧・オーナー不合格の状態）が出ます。

---

## 1. いまどうなっているか（30 秒）

★**最後の直線のせめぎ合いが、初めて画面に映るようになりました。**（オーナー評「かなりよくなりました」）

- 馬体は寄りのカットで**画面高 46%**（v5 は 15%）
- 画面の頭数は**最大 5 頭 / 平均 4.8 頭**
- 主役が **2 頭以上 100%（15.4 秒すべて）/ 4 頭以上 11.9 秒**
- ★**馬は動かしていません。** 着順・タイム・着差・払戻に一切触れていません

やったことは 2 つだけです。

1. **直線を 4 カットに割った**（台本 v6）— 1 カットで「大きい」と「4〜5 頭」は両立しないため
2. **カメラを競り合っている場所へ向けた**（`contest-focus.ts`）— 追い抜きは**元から起きていた**が、
   カメラが先頭に固定されていたので**画面の外**だった

---

## ★2. この案件でいちばん大事な教訓

★**数日空転した原因は、確認をずっと seed 42 でやっていたことです。**

```
seed 42  上位 5 頭が直線を通してずっと 19〜21m に伸びている
         → ★この seed には「5 頭のせめぎ合い」が最初から存在しない
```

★**在らないものは、どんなカメラでも映せません。**

⚠️ ★**次のセッションへ**: 見た目の要求が満たせないとき、
★**まず「その要求を満たす場面が、そのデータに存在するか」を測ること。**
存在しないなら直すべきは表現ではなく**入力**です。
（`tools/audit-real-overtakes.mjs` がその測定器です）

---

## 3. 使う seed / 使わない seed

| seed | 4 頭以上 | 1-2 着差 | 備考 |
|---|---:|---:|---|
| ★**253** | 11.9 秒 | 2.08 馬身 | ★**これを使う** |
| 90 | 13.2 秒 | 1.72 馬身 | |
| 198 | 13.0 秒 | 2.57 馬身 | |
| 2 | 9.9 秒 | 2.95 馬身 | 差がいちばん明快 |

⚠️ **42** … せめぎ合いが存在しない ／ **99** … ハナ差で着順が読めない

★新しく探すなら `node tools/find-contest-seeds.mjs --from 1 --to 300`

---

## ★4. オーナー判断が要ること（勝手に決めない）

1. ★**v6 を既定にするか。** いまは `?cinematography=v6` の opt-in です
2. ★**`climax-choreography`（馬を動かす第 1 案）をどうするか。**
   オーナー不合格でしたが、★**v5 の既定としてまだ生きています**。v6 では使いません
3. ★**せめぎ合いが無いレース（seed 42 のような展開）をどう見せるか。**
   本番では seed を選べません。★これが最大の未解決事項です

---

## 5. 残件（技術）

1. **カットの尺を実時間で測っていない**（区間長で切っている）。調整は `SCRIPT_V6` の `until` だけ
2. **5 頭に絞ったので 6 頭目が画面端で出入りする**可能性。未評価
3. ★**4 角より手前は一切見ていません。** v6 が変えたのは直線の 4 行だけです

---

## 6. 再現コマンド

```powershell
npm test          # 1181 件
npm run typecheck

# 測定（すべて読取専用・dev サーバー不要）
npx tsx tools/audit-contest-focus.mjs --seeds 253,90,198,2   # 競り合いが映る秒数・画面の頭数
npx tsx tools/audit-real-overtakes.mjs --seeds 253           # ★追い抜きが実在するか
npx tsx tools/audit-straight-spread.mjs --seeds 253          # 伸びと画面の幅
npx tsx tools/find-contest-seeds.mjs --from 1 --to 300       # seed を探す
```

---

## 7. ★申し送り

1. ★**「テストが通る」を「見た目が合格」と読まないこと。** この案件では 1160 件 PASS の状態で
   オーナー不合格でした。
2. ★**監査道具が実画面と同じものを見ているか、毎回確かめること。**
   今回だけで**測る側の誤りが 4 件**ありました（報告書 §2-4）。
3. ★**push はオーナー指示があるまで行わないこと**（CLAUDE.md）。
4. ★`apps/web/package.json` のポート 3210 は**オーナーの既存作業**。コミットに混ぜていません。
