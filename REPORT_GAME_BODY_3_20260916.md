# 報告 — ゲーム本体 第 3 便（出走登録と投票の RPC・§9.5 の是正・厩舎の格・生涯の記録の DB）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ 開発側 → レビュー側・オーナー
**対象**: 裁定 `REVIEW_GAME_BODY_1_VERDICT_20260916.md` §5・§7 ／ `REVIEW_CONSULT_HORSE_LIFECYCLE_VERDICT_20260916.md` §8（第 3 便）／ 引き渡し `REVIEW_HANDOFF_20260916.md` §2 の 4
**測ったコミット**（R-23・R-28）: 着手時 `94635d7`（第 2 便）／ 第 3 便は本報告と同じコミット
**ブランチ**: `p4/race-30sec-cuts`

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **△** ＝ 未確認・推定
> ⚠️ ★**移行ファイルを書いただけです。** **staging にも本番にも適用していません**（適用はオーナーのご指示で）
> ⚠️ 着順に効くものは 1 つも入れていません。`packages/race-engine/src` は 1 行も触っていません

---

## 0. 結論

1. **移行 `0024` を書きました** ✔ — ①**`place_bet` を §9.5 どおりに是正**（★自馬が複数なら**全頭を含む組合せだけ**）②**出走登録の RPC `enter_race`**（同じレースに 1 人 2 頭まで）③**騎手の凍結**の列 ④**厩舎の格**の列 ⑤**生涯の記録** `horse_story_event`
2. ★**出走登録と投票を同じ移行に入れました**（裁定 §5-2 の 3。片方だけだと「2 頭出走できるのに片方だけを含む買い目が通る」穴が開くため）
3. **検査で落ちたことが 2 件あり、どちらも「検査が正しく、こちらが足りていない」形でした**（§3）。直しました ✔
4. **馬の購入の DB は次の移行に回します**（§5）。理由は所有の移転が `horses` の排他制約に触るためで、1 つの移行で 1 つのことだけにしました

---

## 1. 移行 `0024` の中身 ✔

| # | 中身 | 根拠 |
|---|---|---|
| ① | **`place_bet` の §9.5 の是正**: 「自馬のどれか 1 頭を含めばよい」→ ★**「自馬を全頭含むこと」**。★`0020` の定義を土台に**判定 1 か所だけ**を差し替え（他は 1 文字も変えず・`0021` と同じ作法） | 正典 §9.5-3・裁定 §5-2 の 1 |
| ② | **出走登録 `enter_race(race, horse, strategy, jockey_frozen, client_token)`**: 先頭で `assert_setup_complete()`（D-080）／**同じレースに 2 頭まで**（3 頭目は拒否）／自分の現役馬か／**発走 60 分前で締切**（§10.4）／再送は既存の登録を返す（EP を二度引かない） | D-104・§10.4 |
| ③ | **騎手の凍結** `race_entries.jockey_frozen`（`{v,jockeyId,name,feeEP,bond,effect}`）。★**`effect` はこの便では 0** | D-105 ④ |
| ④ | **料金**: **登録料 200 ＋ 騎手の料金**を EP で 1 回に払い、`entry_fee` で記帳。★**PP・賞金には触れない** | D-105 ②・§3.3 |
| ⑤ | **厩舎の格** `horses.stable_grade`（既定 `bronze`・CHECK つき）。★**`potential` に触れない** | D-103 |
| ⑥ | **生涯の記録** `horse_story_event`（種類・ゲーム内の週・レース・値）。★**文章そのものは保存しない**（文は `storyLineOf` が組み立てる）。★**利用者からは読み取りだけ**（`insert/update/delete/truncate` を剥がす） | 正典 §18 LR-1・LR-4・LR-6 |
| ⑦ | **権限**: `enter_race` と `place_bet` の両方で `revoke all … from public, anon` ＋ `grant execute … to authenticated`。V-20 の登録簿（`exposure-registry.mjs`）にも `enter_race` を追加 | V-20 ④・`0022` と同じ形 |

## 2. 検査 ✔

| # | 内容 | 結果 |
|---|---|---|
| 1 | ★`place_bet` の**最後の定義**が「自馬を全頭含む」になっている（`not (p_selection @> to_jsonb(e.gate))` の形）。**D-080 の 1 行と 4 つの上限（30,000／50,000／500,000／5,000）が残っている**（土台を写さずに書くと消えるため） | ✔ `apps/cli/test/entry-rpc-migration.test.ts` ① |
| 2 | 出走登録が**同じレースに 2 頭まで**・引退馬を拒否・**発走 60 分前**で締め切る | ✔ ② |
| 3 | 騎手を**凍結して保存**し、料金は `entry_fee` で記帳。★**PP に触れない** | ✔ ③ |
| 4 | 生涯の記録は**利用者から書けない**（読み取りだけ）。★**文章を保存していない** | ✔ ④ |
| 5 | 厩舎の格の既定が `bronze`・`potential` に触れていない | ✔ ⑤ |
| 6 | ★**記録だけの馬が週送りの対象に入らない**（LR-3）。週送りのクエリと頭数を数えるクエリの**両方**が `retired_at_week is null` で絞っていること・**記録が 500 頭増えても対象が増えない** | ✔ `apps/cli/test/story-weekly-advance.test.ts`（4 件） |
| 7 | `rpc-guard`（D-080 の走査・ワーカー専用の登録簿・利用者 RPC の権限）が通る | ✔ 14 件 |
| 8 | V-20 の関数の登録簿（`exposure-registry`）が通る | ✔ 28 件 |

## 3. ★検査に落ちた 2 件（どちらも「検査が正しく、こちらが足りていない」）

| # | 落ちたこと | 直し方 |
|---|---|---|
| 1 | **「`place_bet`: 最後の定義より後に public からの revoke が無い（開きすぎ）」** | ★**移行が足りていませんでした。** `create or replace` で権限は消えませんが、`rpc-guard` は「**再定義したら権限を明示し直す**」ことを求めます（黙って開いた状態を作らせないため）。`0024` に `revoke`／`grant` を足しました |
| 2 | 「**`place_bet` が `0020` から拾えていない**」 | ★**検査の側の決め打ちでした。** 見たいのは「スキーマ修飾された定義を拾えること」で、`0020` は当時の最後の定義にすぎません。★**ファイル名の決め打ちを外し**、「最後の定義が `create or replace function public.` の形で拾えている」ことを見る形に直しました（対照つき） |

★どちらも**移行を書いた側が気づけない形**でした。走査型の検査が効いた実例として記録します。

## 4. 検査の全体 ✔

- `npm run typecheck` … **通過**（exit 0・`apps/web` を含む）
- `npx vitest run --maxWorkers=2 --minWorkers=1` … `Test Files 1 failed | 189 passed (190)` ／ `Tests 1 failed | 1847 passed (1848)`・終了コード 1・**165 秒**
  - 落ちたのは変更前からの `edit-grammar-audit` ⑨（独走代表 expected 'contest' to be 'solo'）だけで、**1 件から増えていません**
  - 件数は 1839 → **1848**（+9）。増えたのは `entry-rpc-migration.test.ts` 5 件と `story-weekly-advance.test.ts` 4 件です
- ★**移行は「書いただけ」で、どの DB にも当てていません。** SQL の中身は**文字列として**検査しています（実行しての確認は staging に当ててから・§6）

## 4b. ★馬の購入（移行 `0025`）✔

### 4b-1. 作ったもの

| # | 中身 | 根拠 |
|---|---|---|
| ① | **出品** `horse_market_listing`（`horse_id`・`stars`・`price_ep`・`active`）。★**サーバー（ワーカー）が書き、利用者は読むだけ** | 憲法 3・D-102 ③ |
| ② | `ep_ledger.reason` に **`horse_purchase`** を追加（★EP のシンク。★**PP→EP を表す語は足していません**） | §0.2-3・`0001` の註記 |
| ③ | **購入の RPC `buy_horse(horse, client_token)`**: `assert_setup_complete()`／★**出品の行の価格で払う**（利用者は価格を申告できない）／NPC の馬だけ／引退馬は不可／★**現役 30 頭の上限**／冪等（台帳の `dedupe_key`） | D-102・D-104 |
| ④ | 権限: `revoke all … from public, anon` ＋ `grant execute … to authenticated`。V-20 の登録簿にも追加 | V-20 ④ |

★**価格と★の算出を SQL に写していません**（`starsOf`・`priceOfStars` は TypeScript の 1 か所のまま）。検査 ⑥ が「★の境目や ×2000 の式が SQL に無いこと」を固定しています（D-052・二重帳簿にしない）。

### 4b-2. 検査 ✔（`apps/cli/test/horse-market-migration.test.ts`・8 件）

利用者が価格を申告できない／PP に触れない／NPC の馬だけ／**30 頭の上限**／出品に素質の数値が無い／**★の算出を SQL に写していない**／D-080 の 1 行と権限の置き直し／**1 つの移行で 1 つのことだけ**（出走登録と混ぜていない）。

---

## 4c. ★staging での適用と実演 ✔

★オーナーの指示（2026-09-16「2・3 進めてください」）で、**staging にだけ**当てました。**本番には当てていません。**

| # | 中身 | 結果 |
|---|---|---|
| 1 | `npx tsx tools/migrate.mjs --env staging` | ✔ `0024` 適用（08:44Z）・`0025` 適用（08:47Z）。どちらも「未適用 1 件 → OK」 |
| 2 | 適用の確認（読み取り専用） | ✔ `horses.stable_grade` 既定 `bronze`／`race_entries.jockey_frozen`／`horse_story_event`（RLS 有効・利用者は SELECT だけ）／`horse_market_listing`（同）／`ep_ledger` の語に `horse_purchase`／`enter_race`・`place_bet`・`buy_horse` は **anon=false・authenticated=true**・セットアップ判定あり |
| 3 | **出走登録の実演**（`demo-0024.mjs`） | ✔ 1 頭目・2 頭目は通り、★**3 頭目は「同じレースに出せるのは 2 頭までです」で拒否**。料金は **`entry_fee` に −800 EP**（登録料 200 ＋ 騎手 200 の 2 頭ぶん） |
| 4 | ★**§9.5 の是正の実演**（`demo-9-5.mjs`） | ✔ 自馬 2 頭（枠 1・10）のレースで、★**片方だけを含む買い目が「自馬出走レースでは自馬を全頭含む買い目のみ購入できる（§9.5）」で拒否**／両方を含む買い目は通る／**自馬 1 頭なら従来どおり通る**（是正で壊していない） |
| 5 | **購入の実演**（`demo-0025.mjs`） | ✔ ★3 の馬を **6,000 EP** で迎え、所有が移り、台帳に `horse_purchase −6,000 EP` が **1 行**（★同じ冪等キーで二度呼んでも増えない）／出品されていない馬・すでに持ち主がいる馬は拒否／★**`pp_ledger` は 0 行** |

- ★**実演の道具は後片付けまで含みます**（合成口座・出走登録・馬券・台帳・所有の移動・出品をすべて戻す）。実行後に残っていないことも確認しています
- 実演の前提として `seed-races --env staging --races 2 --trials 10000 --ahead 14` で、**発走 60 分より先**のレースを 2 本作りました（既定の `--ahead 2` では締切に掛かります）

### 4c-1. ⚠️ 最初の実演の訂正（★自分で見つけた誤り）

★**1 回目の `demo-0024.mjs` の ④ は「拒否された」ものの、理由が §9.5 ではありませんでした。**
- 出走登録で付いた枠が **19・20** で、**オッズ表に無い買い目**だったため、**§9.5 の判定に届く前に「発売していない買い目」で弾かれていました**
- ★**「落ちたから合格」と読むと、是正が効いていなくても緑になります**（R-21 の家族）。そこで **売り目に実在する枠**で試す `demo-9-5.mjs` を作り直し、**正しい理由での拒否**を確かめました（上の 4）

## 5. 未実装（第 3 便の範囲で残したもの）

| # | 中身 | 理由・いつ |
|---|---|---|
| 1 | **出品を作る経路**（ワーカーが★の同じ帯から候補を選び、`horse_market_listing` に書く） | 次の便。★**テーブルと RPC は `0025` で用意済み**。★在庫の下限監視（D-102 ⑤）もここ |
| 2 | **手放す経路**（`sellBackEP` の RPC） | 次の便（★純関数は第 1 便で実装済み） |
| 3 | **格の解放（EP で上げる）と週送りへの配線**（`advanceWeek` に格を渡す） | 次の便（★列は `0024` で用意済み） |
| 4 | **生涯の記録を書き込む経路**（確定のときに出来事を残す）と、**行の増え方の実測**（LR-8） | 次の便（★テーブルと規則は用意済み） |
| 5 | **画面**（出走登録・騎手の選択・馬物語帳ほか） | ★デザイナーのカード待ち（R-12・R-13） |
| 6 | **本番への適用** | ★`0023` と新しいワーカーの配備が先（最終裁定 §3）。**`0024`・`0025` は本番に当てていません** |

## 6. ★オーナーにお願いすること

| # | 中身 | 状態 |
|---|---|---|
| 1 | staging に `0024`・`0025` を当てる | ✔ **済**（オーナーの指示「2・3 進めてください」・2026-09-16） |
| 2 | 実演（出走登録 → 3 頭目の拒否 → §9.5 の拒否 → 購入） | ✔ **済**（§4c。★後片付けまで確認） |
| 3 | **本番への適用** | ⏳ **していません**（`0023` と配備が先） |
| 4 | **次の便に進んでよいか**（出品を作る経路・手放す経路・格の配線・物語を書き込む経路） | ⏳ ご判断ください |
