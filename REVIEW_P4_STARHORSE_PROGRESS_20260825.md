# REVIEW — P4 スターホース版への接近：レビュー経緯と確定事項

日付: 2026-08-25  
ブランチ: `p4/race-30sec-cuts`  
現在 HEAD: `7d62d25` — `中継の台本を starhorse-v1 にする（4角を俯瞰へ・直線を横追従へ）`

## 1. 目的

通常 `/race` のレース演出を、参考映像
`ダービースタリオン/参考映像スターホース版.mp4`
へ近づける。

レビュー側は、推測による修正を止め、参考映像と通常 `/race` を同じ条件で比較し、開発側（Claude Code）へ検証・実装指示を出した。

## 2. 最終的に確定した根本原因

最大の差は馬素材や3D/2D方式ではなく、編集台本とカメラ対象だった。

| 項目 | 参考映像 | 旧通常 `/race` v4 |
|---|---:|---:|
| 編集上の主役 | 上位の競り合い集団 68.6% | 馬群 `pack` 100% |
| カメラ方向 | 横 58.5% | 正面系 74.3% |
| 画角 | 2〜4頭 31.4%、馬群 37.2% | 全体画 74.3% |
| 最長カットの占有率 | 25.3% | 約48% |
| 使用ショット数 | 確定9、曖昧込み最大13 | 6 |

旧v4では `homestretch-front` が約18.6秒、レース全体の約48%を占め、正面から12頭を小さく見せ続けていた。参考は同程度の長いカットを、横から競り合う馬を大きく見せるために使っていた。

## 3. 完了・不採用となった調査

### 3D

- 購入3D馬モデルによるスパイクは技術的には成立した。
- 騎手不足、馬具と走法の反復、背景世界不足により2Dより印象が悪かった。
- `research/3d-hybrid-20260823` へ隔離済み。
- 通常 `/race` には入れていない。

### S3隊列・遠景停止

- 特定seedの静止画では改善した。
- 500 seed監査で成立率が低く、先頭群の顔ぶれが短時間で変わるため一般化できなかった。
- 適応倍率も1100mの1時点では効くが1190mまで維持できなかった。
- `research/2d-s3-cinematic-20260824` へ隔離済み。

### 毛色・勝負服・姿勢反復

- 「全騎手が白い／全頭同じ毛色」は通常 `/race` では発生していなかった。
- オフライン検証ツールが `silksOverlays` と `bakeCoat` を通らない経路だったことが原因。
- 通常画面では8枠色、5毛色、鞍布番号が機能している。
- 隣接同フレーム率8.5%は主因ではない。

### side-v9 / v7-v9ハイブリッド

- v9は単騎画で前傾姿勢、歩幅、馬番可読性が改善。
- 密集時の重なり、接地・馬高のばらつきが悪化。
- v7＋v9同時準備は取得+1.06MB、`buildFrames` +約2.6秒。
- 費用対効果が合わず通常導入は不採用。
- `research/2d-side-hybrid-20260824` へ隔離済み。

## 4. 編集文法監査

`REPORT_P4_2D_EDIT_GRAMMAR_AUDIT_20260824.md` で、参考映像46.8〜122.0秒と通常 `/race` の4 seed（42/332/474/14）を比較した。

確定事項:

- 参考の主役は上位2〜4頭相当。
- 自馬を常時カメラ主役にはしていない。自馬はマーカー/HUDで示す。
- 通常 `/race` は全使用ショットが `target: pack`。
- 定義19ショット中、対象区間で使われるのは6ショット。
- 既存の `homestretch-side` と `fourth-corner-high` が実画面で成立した。
- 新素材ではなく台本とresolverが主な改善対象。

## 5. starhorse-v1 の比較結果

旧v4から次の2ショットだけを置換した。

```text
fourth-corner-front -> fourth-corner-high
homestretch-front   -> homestretch-side
```

比較動画はseed 42/332/474/14について、旧v4と候補版の計8本を生成した。

映像上の結果:

- 第4コーナーで視点が上がり、馬群が斜めに開く。
- 直線で横追従へ切り替わり、競り合う馬が大きくなる。
- 発走、序盤、side-drive、finish-lineの台本は維持。
- 4 seedすべてで同じ方向の改善が出た。
- HUD帯を除く隣接フレーム画素差は、第4コーナーで旧v4 3.1に対し候補10.8、直線で4.5に対し8.4。
- 旧v4で最も静かだった勝負所が、画面上も動くようになった。

レビュー判定: **PASS**。

## 6. 通常 `/race` への昇格

コミット `7d62d25` で通常 `/race` の既定を `starhorse-v1` に変更した。

URL仕様:

```text
/race                                  -> starhorse-v1
/race?cinematography=starhorse-v1      -> starhorse-v1
/race?cinematography=v4                -> 旧v4
不正な cinematography 値               -> starhorse-v1
```

レースエンジン、順位、着差、馬素材、カメラ定義値は変更していない。

カメラ平滑化状態が前カットから引き継がれるため、台本上同じ `finish-line` でも旧v4と候補版で初期構図が変わる。この挙動は比較動画で確認し、候補映像の一部として承認した。

## 7. 順位表の可読性修正

`homestretch-side` で馬が大きくなると、順位表が読めなくなった。当初は描画順を疑ったが、実際の原因は馬の画面占有率に応じた意図的なHUD減光だった。

旧処理:

```ts
ctx.globalAlpha = prevAlpha * (1 - 0.6 * ease);
```

最大寄りで順位表が40%まで薄くなっていた。

修正:

```ts
ctx.globalAlpha = prevAlpha * (1 - 0.25 * ease);
```

最大寄りでも75%を維持する。閾値、位置、サイズ、色、カメラ、台本は変更していない。

結果:

- 進行86%・88%で1〜5着の馬名と着差が判読可能。
- 旧v4は該当区間で `ease = 0` のため、比較5点すべて変更前とPNGハッシュ一致。
- 順位表の通常時、直線入口、93%、finish-lineは変更なし。

## 8. 検証結果

最終報告時点:

- `npm test`: 112ファイル / 1121件通過
- `npm run typecheck`: エラーなし
- `npm run build:web`: 成功
- `/race`: 200
- `/race?cinematography=v4`: 200
- 通常 `/race` の実画面で6ショット遷移を確認
- 黒帯、背景欠落、接地破綻なし

注意: 稼働中のdevサーバーと `build:web` が同じ `apps/web/.next` を使用すると、devサーバーが500になることがある。ビルド時はdevサーバーを停止する。

## 9. 現在のGit状態

- ブランチ: `p4/race-30sec-cuts`
- HEAD: `7d62d25`
- 製品実装はコミット済み
- `apps/web/package.json` のポート3210変更はユーザー既存の未コミット変更として保持
- `tools/lib/classification.mjs` と未追跡の監査報告・監査ツール・監査テストが残っている
- これらを製品コミットへ混ぜない
- push状態は次セッション冒頭で確認すること

## 10. レビュー側の最終判断

`starhorse-v1` は旧v4より、参考映像の編集文法とレースとしての興奮度の両方で明確に改善している。

ここまでの主成果は、通常 `/race` の既定映像を実際に変更したこと。追加の素材研究へ戻らず、次は実プレイでの確認と、残る局所的な演出改善だけを扱う。

