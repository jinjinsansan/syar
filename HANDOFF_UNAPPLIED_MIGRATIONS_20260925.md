# 【STAR】引継ぎ: 本番に出していない移行と、その束の意味 — 2026-09-25

- 開発側 Claude（`star-aa`）／ 依頼: 裁定 `REVIEW_DISCOVERY_AXES_20260925.md`（レビュー側）
- ★目的: ★**オーナーが配備を許した日に、「何が一度に入るか」を 1 枚で言える**ようにする
- ★staging は ★**88 本すべて適用済み**（✔ `npx tsx tools/list-unapplied-migrations.mjs --env staging`）

---

## 0. ⚠️ まず、この表の限界を書きます

★**本番の適用状況を、私は読めません**（★権限層が止めます・記憶 `star-production-reads-blocked`）。
★この表の「本番」列は ★**引継ぎ書 `HANDOFF_BLOODLINE_AND_DEPLOY_20260923.md` の記録（0078 まで）**に基づく
★**推定**です。★配備の前に ★**オーナーの端末で 1 コマンド**流して確かめてください:

```bash
npx tsx tools/list-unapplied-migrations.mjs --env production
```

→ ★出た一覧が ★下の表と一致することを確かめてから配備してください。
⚠️ ★一致しなければ ★**この表が古い**ので、★配備を止めて知らせてください。

---

## 1. 束の一覧（★`0079`〜`0088`・**10 本**）

| # | 移行 | 何を入れるか | 🔴 出さないと何が起きるか | 依存 |
|---|---|---|---|---|
| 0079 | `entry_scratch_requests` | ★出走登録の ★**取消の受付**（★D-123・発売の準備より前だけ） | ★登録したら ★**取り消せません**（★入口だけ在って出口が無い・D-122 ⑤） | — |
| 0080 | `daily_ep` | 🔴 ★**EP の日次付与**（★D-075・200 EP）＋★額と理由の分類を 1 か所に | 🔴🔴 ★**全員が EP 0 になり、何もできません**（★調教が毎週吸う。★本番のオーナーの口座が既にそう） | — |
| 0081 | `day_boundary_staleness` | ★「配布が止まっている」の線（28 時間）を ★画面と監視が同じ所から読む | ★ワーカーが止まったとき、★画面が ★**「受け取り済み」と嘘を出し続けます** | 0080 |
| 0082 | `jockey_roster_server_side` | 🔴 ★**騎手の名簿を DB へ**・★料金と凍結をサーバーが作る | 🔴 ★`feeEP: 0` を送れば ★**高い騎手が無料**（★憲法 3）。★D-105 が 1 EP も効いていない | — |
| 0083 | `retired_horses_public` | ★他の牧場の引退馬の公開の一覧（★正典 **LR-6**・★牧場名まで） | ★馬物語帳が ★**自分の馬しか出せません**（★正典の項目が未実装） | — |
| 0084 | `my_horse_discovery_runs` | ★発見の素（★条件別の出走回数だけ・★D-108・D-116） | ★「分かってきたこと」が出ません（★見本は消したので ★節が出ないだけ） | — |
| 0085 | `horse_market_listing_facts` | ★市場の出品に **名前と戦績**（★D-102 ③・D-114） | ★**名前の無い馬を「買いますか」と出す**ことになります（★`horses` は anon に閉じている） | — |
| 0086 | `horse_record_one_place` | ★戦績の数え方を **関数 1 つ**に（`horse_starts` / `horse_wins`）＋★3 か所を寄せ替え | ★同じ式が 4 か所に散り、★片方だけ直る（★D-052） | 0083・0085 |
| 0087 | `my_horses_record_one_place` | ★`my_horses`（★`/entry` が読む）も 同じ関数へ | 🔴 ★画面と `enter_race` の数え方が別々のまま（★**画面が「出られる」と言った馬が RPC に弾かれる**・CL-4） | 0086 |

| 0088 | `enter_race_record_one_place` | ★`enter_race`（★登録の可否）も 同じ関数へ | 🔴 ★画面と RPC が別々に数える状態（★**片方だけ寄せた状態がいちばん危ない**・CL-4） | 0086・0087 |
## 2. 🔴 ★**出す順に意味が在ります**（★裁定 `REVIEW_JOCKEY_FEE_20260925.md`）

```
0079 → 0080 → 0081 → 0082 → 0083 → 0084 → 0085 → 0086 → 0087 → 0088
```

★**`0080` を `0082` より先に出してはいけません。**
★理由: ★`0080` で EP が入り始めた ★**その日から**、★`0082` が無いと
★**「高い騎手を無料で乗せ放題」**が成立します（★いまは EP が 0 なので誰も出走登録できず、露出していません）。
→ ★**10 本を同じ便で出す**のが最も安全です。

⚠️ ★`0081` は `0080` に依存します（★`0080` が作った読む口を作り直します）。★`0080` 抜きで `0081` は通りません。

## 3. ★配備の前に確かめること（★私からは読めない・★オーナーの端末で）

| # | コマンド | 何を見るか |
|---|---|---|
| ① | `npx tsx tools/list-unapplied-migrations.mjs --env production` | ★この表と一致するか（★§0） |
| ② | `npx tsx tools/diag-jockey-frozen-shapes.mjs --env production` | 🔴 ★`race_entries.jockey_frozen` の内訳。★staging は **569 件すべて `null`**（★旧い形 0 件）。★本番も同じなら `0082` の「`feeEP` が無ければ落とす」で既存の取消が壊れません |
| ③ | `node tools/check-weekly-cycle-health.mjs --env production` | ★ワーカーが「1 日」を書き進めているか（★止まっていると `0080` のデイリーが配られません） |

★② が ★**旧い形の行を 1 件でも返したら、★配備を止めて知らせてください**
（★`jockeyFeeOfFrozen` が落ちる形にしてあるため・★裁定の指示どおり）。

## 4. ★画面（Vercel）は別に古びます

⚠️ ★移行を入れても ★**画面は自動で新しくなりません**（★記憶 `star-prod-alias-is-stale-main`）。
★確かめ方: `/api/healthz` の `sha` を見る（★日付を覚えない）。
★この束と一緒に出る画面の変更:
- ★`/earn` … ★デイリーの受け取りのボタン（★`0080`・`0081`）
- ★`/entry` … ★騎手の id だけを送る形（★`0082`。🔴 ★**画面が古いまま `0082` を入れると、
  ★古い画面が `p_jockey_frozen` を送り、★署名が違うので登録が落ちます**）
- ★`/stable/retired` … ★本物のデータ（★`0083`・`0084`）

🔴 ★**`0082` は ★画面と DB を ★同時に出してください。** ★どちらか片方だけだと出走登録が落ちます。

## 5. ★この束に入っていないもの（★別の話）

- ★`buy_horse` の**入口**（★RPC は動きます — ★staging で 20 件 確認済み（`tools/verify-buy-horse.mjs`）。
  ★読む口も在ります（`0085`）。🔴 ★**画面（`/stable/market`）の結線と入口が未着**。
  ⚠️ ★その画面の設計は **T-11 より前**のままで、「同じ値段ならどの 1 頭でも同じ」と書いています。
  ★しかし ★**実測では値段がほぼ 1 頭ごとに違う**（★式が獲得賞金を含む）ので、
  ★その前提はもう成り立ちません。→ ★**見た目の作り直しはデザイナー待ち**（★流して結線すると画面が嘘になる）
- ★発見度の画面の接続（★`/stable/[horseId]`・★進行中）
- ★旧世代の画面 15 枚の作り直し（★デザイナー待ち・★`DesignSync` の認可が必要）
- ★下見画面 18 枚を本番で塞ぐか（★オーナー判断待ち・★3 案）

---

## 6. 🔴 オーナーの操作・判断を待っているもの（★1 か所に集めました）

★2026-09-25 の時点で ★**5 件**たまっています。★どれも私からは進められません。

| # | お願い | なぜ私からできないか | 待たせているもの |
|---|---|---|---|
| ① | ★**移行 10 本を本番に当てる許可**（★`0079`〜`0088`） | ★本番への書き込みは ★オーナーの明示の指示が要る（★憲法・作法） | 🔴 ★**EP が誰にも入りません**（★残高 0 で何もできない）。★騎手の料金も 0 のまま |
| ② | ★**本番で 3 つ読む**（★§3 の表） | ★本番の読み取りも権限層が止めます | ★配備の前提（★表が推定のままでは出せません） |
| ③ | ★**`/design-login` の実行** | ★`DesignSync` が ★オーナーの claude.ai の認可を要求します | ★旧世代の画面 15 枚（★依頼は ★`design/hud-ds/requests/R-16-...md` に ★**書き終えています**） |
| ④ | ★**下見画面 18 枚を本番で塞ぐか**（★3 案） | ★どれを残すかは ★使い方の判断（★私が塞ぐと ★素材を見る手段も消えます） | ★「白い古いデザイン」に着ける経路 |
| ⑤ | ★**`/races/[id]/bet` を残すか捨てるか** | ★**遊びの手触り**（★コードからは決まりません） | ★投票の画面が 2 つ在る状態（★上限の見せ方を 2 か所で直す事故のもと） |

★③ の依頼は ★**中身が出来ています**（★落としてはいけないものの一覧つき）。
★`/design-login` が通れば ★そのまま送れます。

## 7. ★staging で流し直した予行（★2026-09-25・`0086`〜`0088` の後）

⚠️ ★`0086`〜`0088` は ★**view と `enter_race` を作り直した**ので、★全部 流し直しました。
★**8 本すべて終了コード 0**（★道具自身の値で確かめています）:

```
verify-ep-daily                 ✅  verify-retired-public           ✅
verify-discovery-runs           ✅  verify-jockey-fee               ✅
verify-buy-horse                ✅  verify-entry-scratch-live       ✅（14 件）
verify-ds5-before-start-scratch ✅  verify-flow                     ✅
```

★併せて ★**関数に寄せても数字が 1 つも変わらない**ことを確かめました
（★市場の 3 頭が同じ戦績・引退の一覧も突合 0 件の不一致）。
