# 引継ぎ書 — 次のセッションはここから（2026-08-30・第2便）

作成: 2026-08-30 / ブランチ `p4/race-30sec-cuts` / HEAD `2d7bcf2`
起点: `309f2a1` → **12 コミット**（★うち **未 push は 2 本**。★10 本は push 済み）
前の引継ぎ書: `HANDOVER_P4_TRACK_CONDITION_20260830.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**

---

## ★0. まず 3 分でやること

```powershell
cd V:\dev\Cusor\star
git log --oneline -12                                        # ★この便の 12 本
git log --oneline origin/p4/race-30sec-cuts..HEAD | wc -l    # ★未 push の数（直書きしない）
git status --short                                           # ★` M apps/web/package.json` だけのはず
npm test                                                     # ★133 ファイル / 1336 件 PASS / EXIT=0
npm run typecheck
```

⚠️ ★**`apps/web/package.json`（ポート 3210）は触らないこと。** ★オーナーの既存作業で、
　★この便でも 12 コミットとも混ぜていません。★`git add -A` には必ず `':!apps/web/package.json'` を付けます。

---

## ★1. 次の一手 — **B案から**（オーナー指示）

★**オーナーの指示は「次回 B からやります」です。**

### ★1-1. B案とは何か

★重賞 50 鞍と競馬場 10 場は**データとしては入りました**（`75d9c7c`）。
⚠️ ★しかし ★**画面には出ていません。** ★`page.tsx` の `const DIST = 1600;` は直書きのままです。

★配線しようとして、★**先にエンジンとの食い違いを見つけたので止めました**（`2d7bcf2`）:

```ts
race.ts:136   laneExtraM(entrant.gate, fieldSize, conditions.distance, seed)   // ★spec 無し
```

★`laneExtraM` の spec 既定は `DEFAULT_OVAL`（1周2000m・直線400m・幅20m）です。
→ ★**競馬場の形を描画側だけで変えると、エンジンは 2000/400 のまま距離ロスを計算します。**
⚠️ ★`w` と距離ロスは**着順に効きます**（D-065 / D-071）。★絵と結果が食い違う形です。

### ★1-2. ★なぜ「帯の内側で作り直す」なのか（実測）

★正典 **V-18 ②「最内と最外の走行距離差が 4〜12 馬身」**。
★エンジンの `laneExtraM` をそのまま呼んで測りました（`tools/_venuelane.mjs`）:

| | 半径 | 1600m の内外差 | |
|---|---|---|---|
| いまの `DEFAULT_OVAL` | 191m | **9.98 馬身** | ★既に帯の上寄り・余裕が少ない |
| 霧ヶ原 | 169m | 10.01 | ✅ |
| ⚠️ 白砂 | 178m | **14.04** | ❌ |
| ⚠️ 月見丘 | 127m | **16.17** | ❌ |

⚠️ ★**組んである 50 鞍のうち 15 鞍が帯の外**です。
★**コーナーを深くするほど内外差が増えます。** ★帯に収まるのは実測で **半径およそ 165〜215m**（1.3 倍の幅）。
★いま置いてある 127〜213m（1.68 倍）は**広すぎます**。

### ★1-3. ★B案の手順（★この順でないと測り直しになります）

| # | | |
|---|---|---|
| ① | ★**競馬場 10 場を帯の内側で作り直す** | ★半径 165〜215m。★**差は直線の長さで出す**（290〜620m・2.1 倍は保てる）。<br>★長い直線と大きい半径を両立するには **1 周を伸ばす**（例: 直線 620m・半径 200m → 1周 ≈ 2500m） |
| ② | ★**エンジンに spec を渡す** | ★`RaceConditions` に競馬場の形を持たせ、★`laneExtraM` へ通す。<br>⚠️ ★**ここは着順に効く経路**です（憲法 3・D-059）。★`lane-geometry.test.ts` が render と突き合わせています |
| ③ | ★**50 鞍全部で V-18 を測る** | ★② だけでなく ★**① 枠順と着順の順位相関 \|ρ\| ≤ 0.10** も。<br>⚠️ ★①は `_venuelane.mjs` では測っていません（★あれは②だけ）。★別に要ります |
| ④ | ★**画面に配線** | ★`const DIST = 1600;` を外し、★`?race=<id>` で 50 鞍から引く |

⚠️ ★**①②を飛ばして④をやらないこと。** ★絵だけ変えると、★**着順が別の形の走路で決まります。**

---

## ★2. この便でやったこと（12 コミット）

```
875aaaf  馬場: 濡れた走路が空を映す（照り）を入れる
eaa2e83  台帳: A-6 を外し、A-7 と B-2 を立てる
309d326  道具: 比較シート（_condsheet.mjs）を登録簿へ
2274562  報告: 馬場の調整 — 濃さはオーナー確認で確定
6722549  水たまり（残件 A-7）
70f8f7d  跳ね返り（残件 A-7）
d7b2f2f  水たまりを作り直す — オーナー評「色褪せた場所という感じ」
4410d97  水たまりに水際の明線を足す — ★私はずっと 100% で見ていた
b5d9dda  水際を二重線にする（暗線＋明線）
3c86a13  台帳: A-7 を「一旦ここまで」で止める。B-3 を立てる
75d9c7c  重賞 50 鞍と競馬場 10 場を作る（データ＋検査）   ← ★未 push
2d7bcf2  測定: 競馬場の形を変えると V-18 ② が破れる       ← ★未 push
```

### 2-1. ★確定したこと（★オーナーが実画面で確認）

| | |
|---|---|
| ★**馬場の濃さ** | ★**現行（稍重 0.22 / 重 0.42 / 不良 0.55）で確定。** ★濃い案（0.30/0.55/0.66）は入れません |
| ★実測（走路の帯・良基準） | ★ダート 稍重 −10.2% / 重 −22.0% / 不良 −29.1%　★芝 重 −19.8% |
| ★タグ | `p4-track-condition-20260830`（★較正値を本文に書いてあります・**未 push**） |

### 2-2. ★入ったもの（すべて既定 ON・戻し口つき）

| # | 何を | 戻し口 |
|---|---|---|
| ① | ★照り（`trackGlossAlpha` 0.18・フレネル） | `?gloss=0` |
| ② | ★水たまり（暗い本体＋空を映す帯＋芯＋**水際の二重線**） | `?puddles=0` |
| ③ | ★跳ね返り（蹄の拍・**2 段**: 地面が水を含む／水たまりを踏む） | `?puddles=0` と同じ |
| ④ | ★重賞 50 鞍・競馬場 10 場（★データのみ・**画面未配線**） | — |

⚠️ ★**良・稍重の絵は 1 ビットも動きません**（検査で固定）。

---

## ★3. 次のセッションが踏みやすい穴（★ここが本題です）

### ★3-1. ⚠️ ★**オーナーの画面はキャンバスを 67% に縮めて出しています**

★`apps/web/src/app/race/page.tsx:2784` … `style={{ width: '100%', maxWidth: W }}`
→ ★**1280px が約 860px** で表示されます。

⚠️ ★**私はずっと 100% の撮って出しで判定していました。** ★R-30 の家族です。
★水たまりを「暗い本体＋細い反射」で作り、★100% では水に見えていましたが、
★67% では ★**細い反射が消えて暗い楕円だけ**が残り、★**馬の影と見分けがつきません**でした。

→ ★**塗りのぼかしは縮小で消える。残るのは線。**
→ ★**比較シートは 0.67 倍に縮めてから並べること**（`out/_sheet/puddle-at-owner-size.png` の形）。

### ★3-2. ⚠️ ★**`--sec 32` では測れません**（残件 B-2）

★`homestretch-front` は **1312〜1392m の 80m しかありません**（`tools/_cuts.mjs`）。
★32 秒はその**境目**で、★**同じ URL を 2 枚撮ると帯の平均が 20% 動きます。**
★**14 秒**（`first-corner-front`）と ★**35 秒**（`straight-contest`）は ★**2 枚が 1 バイトまで一致**します。

⚠️ ★前の引継ぎ書 §6 の再実行手順が `--sec 32` です。★あの表が再現したのは**同じ側に落ちたから**です。

### ★3-3. ★**撮影中に製品コードを触らない**

★変異試験（係数を戻して検査が落ちるか）を、★**背景で撮影が走っている最中に**実行しました。
★dev サーバーは即座に再構成するので、★**撮影に混ざりえます**（R-18 の家族）。
→ ★気づいて撮り直し、★**1 バイト一致**を確認しました（混ざっていませんでした）。

### ★3-4. ★`perl -0pi -e` が識別子を食いました

★`${FONT}` を含む行を perl のワンライナーで書き換えたら、★**変数が消えた行**が入りました
（`ctx.font = 'bold 22px ';`）。★正典に壊れた行を書き込んだのと**同じ形**です。
→ ★**日本語・識別子を含む行の機械編集はエディタで行うこと。**

### ★3-5. ★和字は書体を登録しないと豆腐（□）になります

★`@napi-rs/canvas` の `sans-serif` だけでは和字が出ません。★見出しが全部 □ になりました。
→ ★環境の和字書体を探して登録し、★**無ければ英字に落とす**（黙って豆腐にしない）。

### ★3-6. ⚠️ ★**レース名が実在しないことを、開発側は断定できません**

★`name-blocklist.ts` の註記どおり、★**NG リストを平文で置くこと自体が §0.1 違反**です。
→ ★実在レース名の一覧をテストに書けません。★**構造で守りました**:
　★名前の多くを**架空の競馬場名から**作っています（「天河記念」— 天河競馬場は存在しない）。
⚠️ ★季節・天体の語を使う名（芽吹賞・紅葉賞など）は**偶然の一致がありえます**。
　★**オーナー・レビュー側の確認対象**です。★名前はデータの 1 フィールドなので差し替えは無料です。

### ★3-7. ★検査が私の設計ミスを 3 回捕まえました（★書いてよかったもの）

| | |
|---|---|
| ① | ★競馬場 10 場の**コーナー半径がほぼ同じ**だった（165〜191m・1.15 倍）＝10 場ある意味がない |
| ② | ★`(競馬場/馬場/距離)` が **2 組重複**していた＝その 2 鞍は同じ画 |
| ③ | ★較正登録簿のメタテストが `GRADED_COUNT_BY_GRADE` の**登録漏れを自動で捕まえた**（R-19） |

### ★3-8. ★オーナーの言葉が原因を名指しすることがあります

★「★**色褪せた場所という感じ**」— ★この一言で原因が分かりました。
★灰色を薄く広く重ねていたので、★**文字どおり色が褪せていました。**
→ ★**言い換えずに、その言葉のまま原因を探すこと。**

---

## 4. 残件台帳（`RESIDUALS_P4_RACE_VIDEO.md`）

| # | | 状態 |
|---|---|---|
| **A-1** | 4 角の入口の反転 | ★半分解決（入口は残る・[EYES] 待ち） |
| **A-3** | 直線の 2 本の境目に閃光がない | 当面入れない |
| **A-4** | 13 頭以上の勝負服の最小色距離 | 12 頭立てでは出ない |
| **A-5** | ★芝の縞刈りがダートの上にも引かれている | ★未解決 |
| **A-7** | ★濡れた馬場が「濡れている」に見えない | ★**一旦ここまで**（★不良は出現率 3%・オーナー判断で優先度を下げた）<br>⚠️ ★「水に見える」という評は**いただいていません** |
| **B-1** | `audit-race-broadcast.mjs` | 据え置き |
| **B-2** | ★`--sec 32` はカットの境目・測定に使えない | ★手順書が未修正 |
| **B-3** | ★100% で判定していた（画面は 67%） | ★手順は直した・**道具側の既定は未対応** |
| **C-1** | 正典 D-090 以降の起票 | ★**この便の決定が未起票**（§7） |
| **C-2** | 未 push | ★**10 本は push 済み**。★残り 2 本（数はコマンドで数える） |
| **C-3** | ダート | ★残るは汚れの濃さの [EYES] と素材の承認 |

---

## 5. この便で増えた検査

| 検査 | 何を守っているか |
|---|---|
| `packages/render/test/track-gloss.test.ts`（14 件） | ★良は 0／奥ほど強い／★**照りが濡れの暗さを打ち消さない**（★0.55 に戻すと落ちる） |
| `packages/render/test/puddles.test.ts`（30 件） | ★良・稍重は 1 つも出ない／★**決定論**（乱数を呼んでいないことを機械で見る）／★**絵に無い水で跳ねない**（全点走査）／★水は土より高く横へ飛ぶ |
| `packages/scheduler/test/graded-races.test.ts`（21 件） | ★50 鞍・格の内訳・★**（競馬場/馬場/距離）が全部違う**／年間カレンダーが途切れない |
| `apps/cli/test/venue-course.test.ts`（4 件） | ★10 場が実際に走路として成立する／★**半径と直線が散っている** |

---

## 6. 再実行（★すべて読取専用）

```powershell
# ★★映像の判定は「本物のブラウザ」から撮り、★**67% に縮めてから**見る（§3-1）
cd apps/web ; npm run dev            # ★別の窓（ポート 3210）
npx tsx tools/_realshot.mjs --out x --query "surface=dirt&condition=bad" --sec 14   # ★32 秒は不可（§3-2）
npx tsx tools/_glosscmp.mjs --off out/_real/a.png --on out/_real/b.png              # ★先に揺らぎを測る
npx tsx tools/_condsheet.mjs --prefixes now --labels 現行 --out sheet --strip

# ★★B案の出発点: 競馬場の形と V-18 ②
npx tsx tools/_venuelane.mjs         # ★いまは 50 鞍中 15 鞍が帯（4〜12 馬身）の外

npm test ; npm run typecheck         # ★133 ファイル / 1336 件 / EXIT=0
```

⚠️ ★`tools/_*.mjs` は追跡外ですが、★**登録簿には載せます**（`tools/lib/classification.mjs`）。
★この便で **3 本**足し（`_glosscmp` / `_condsheet` / `_venuelane`）、3 本とも登録済みです。

---

## 7. 発行物（この便）

| 種別 | ファイル |
|---|---|
| 報告 | `REPORT_P4_TRACK_CONDITION_20260830.md` |
| 台帳 | `RESIDUALS_P4_RACE_VIDEO.md`（★A-7 / B-2 / B-3 を新設・A-6 と C-3② を外した） |
| タグ | `p4-track-condition-20260830`（★較正値と開いている残件を本文に記載） |

⚠️ ★**50 鞍・10 場については報告書を書いていません。** ★次の便で書いてください。

---

## ★8. オーナー判断待ち

| # | | |
|---|---|---|
| ① | ★**B案の実行** | ★**次の一手はここ**（§1）。★①〜④の順を守ること |
| ② | ★**レース名 50 個の確認** | ★開発側では実在しないと断定できません（§3-6） |
| ③ | ★**正典の噛み合わせ** | ⚠️ ★50 鞍は B案（G1 9 鞍）だが、★**週次頻度は正典のまま**（G1=3/週）。★どちらに合わせるかは正典の変更 |
| ④ | ★背景素材の焼き分け | ★①(a) で「1 セット共有」を選択済み。★景色を変えるのは別便 |
| ⑤ | ★A-5（刈り目）／汚れの濃さ／ダート素材の承認 | ★持ち越し |
| ⑥ | ★push（残り 2 本）とタグ | ★タグはローカルに 5 本。★この案件はタグを push していません |
| ⑦ | ★決定ログの二重帳簿 | ★未決（前便からの持ち越し） |

★**それ以外に開いている照会はありません。**
