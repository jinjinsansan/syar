# Q-LANE-01〜04 の裁定 — 中盤の横の広がり

**発行**: 2026-08-21（レビュー側 Claude）
**対象**: `QUESTIONS_RACE_LANE_SPREAD_20260821.md`
**判定**: **(b) を採用します。★ただしレバーが違います — `SETTLE_M` を緩めてはいけません。動かすのは `reveal` です。／(a) 却下（V-18 直撃）／(c) 却下（D-069 違反）／★立ち絵の写真は即時撤去**

---

## 0. ★結論を先に — 触ろうとしている場所が、まさに触ってはいけない場所です

> 開発側: **発走後 250m かけて全馬が内ラチに寄る設計です。ここを緩めれば直ります。**

**緩めてはいけません。** `packages/race-engine/src/lane.ts` を読むと、**中盤が細いのは `SETTLE_M` のせいではありません。**

```
base  = start(gate) → RAIL_W へ SETTLE_M=250m で収束     ← ★枠に由来する
swing = max(0, drift*0.85 + wave) * reveal * (widthM*0.62) * swingScale
                ↑ シードに由来        ↑ reveal = run²(3-2run)（レース全体でのなだらかな立ち上がり）
w = clamp(base + swing)
```

**測定値を、この式に当てて読み直します。**

| 表示秒 | 先頭位置 | `settled` | `reveal` | 広がり | **その広がりの正体** |
|---|---|---|---|---|---|
| 9 | 51m | **0.11** | 0.004 | 11.3m | **★枠の広がりがまだ残っているだけ** |
| 12〜27 | 182〜810m | **≒1.0** | 0.04〜0.14 | 1.1〜3.4m | 枠の広がりは消えた・シードの広がりはまだ出ていない |
| 50 | 1563m | 1.0 | 0.93 | 9.2m | **シードの広がり（`swing`）** |

**★合格した2区間は、別々の原因で広がっています。**

- **発走直後の 11.3m は「枠の広がり」**です。**それはこの案件が意図的に消したもの**であり、**`SETTLE_M` を緩めることは、それを中盤まで引き延ばすこと**です
- **ゴール前の 9.2m は「シードの広がり」**です。**こちらは伸ばして構いません**

**そして `laneAt` のコメントが、緩めた場合に何が起きるかを既に記録しています**（`lane.ts:181-190`）:

> ⚠️ 枠の位置に居続ける形にしたら、実測で **★枠による偏り 35.5馬身＝枠順で決まるゲーム**になりました
> ⚠️ 枠の **5% を残していた**とき、外枠は**レース中ずっと 0.6m 外**を回り、1200m で **枠とロスの相関 0.127**
> （差は 2.5m ＝ 約1馬身しかないのに、**★向きが揃っているので相関は出ます**）

**5% 残しただけで相関 0.127 が出たものを、中盤いっぱいまで引き延ばせば、V-18 は確実に落ちます。**

---

## 1. 🔴 Q-LANE-01 / Q-LANE-03 — **(b) を採用。ただし動かすのは `reveal`**

### 採用する形

> **`reveal` の立ち上がりを早め、シード由来の `swing` を中盤から出す。`base` と `SETTLE_M` には触らない。**

これで4つが同時に満たされます。

| | |
|---|---|
| **見た目** | 中盤の広がりが 8〜12m になる（合格した2区間と連続する） |
| **V-18** | **★広がりが枠に由来しない**ので、枠と着順の相関は増えません。**`drift` / `wave` は `stream(seed, gate, ...)` から引く一様乱数で、枠に対して単調ではない** |
| **Provably Fair** | `w` は今までどおり**エンジンがシードから引きます**。鎖は切れません（D-071） |
| **D-069** | 脚質を受け取らない形のままです |

**★これは「新しい機構を足す」のではなく、既にある機構の出方を変えるだけ**です。D-062 が却下した B（エンジンに道中で決まることを作る）とは別物で、**再較正の範囲もずっと狭くなります。**

### (a) 却下 — `SETTLE_M` の実質無効化

**§0 のとおりです。** 発走直後の 11.3m は**枠の広がり**なので、それを保つことは**「枠順で決まるゲーム」に戻すこと**です。**V-18 が落ちます。**

### (c) 却下 — **これは D-069 が明示的に撤回した案です**

> **D-069**: 横位置 `w` は**脚質だけから作ってはならない**。V-16 ①（全局面で 画面ボット ≥ 出走表ボット）は、**道中の画面が出走表の部分集合になっていると原理的に成立しない**。**★レビュー側は Q-P4-29 で「w も脚質から作る」と裁定したが、それでは w も出走表から予測でき、① が成立しない。撤回する。**

**「逃げ＝内、追込＝外」は、脚質から `w` を作ることそのもの**です。**脚質は出走表に載っています。** 採れません。

**★「隊列が意味を持つ」という狙いは正しく、それは D-068 が既に別の形で満たしています**（道中の順位を脚質とペースから作る）。**前後の隊列は脚質から作ってよく、横位置は作ってはいけない** — この非対称が D-068 と D-069 の関係です。

---

## 2. 🔴 Q-LANE-02 — 内ラチ有利は**変わりません**。変わるのは**分散**です

**内ラチ有利は「コーナーの内側が短い」という幾何から来ます**（`laneExtraM` が `(w − centre) × Δθ` を積む）。**散らしても、この理屈は1ミリも変わりません。**

**変わるのは「誰がどれだけ外を回されるか」のばらつきです。** そして**そこが本当の危険です。**

| ゲート | 予想される向き | 危険度 |
|---|---|---|
| **V-4**（1番人気の勝率 30〜34%） | **★下がる**。距離ロスの分散が増える＝**強い馬が理由なく負ける機会が増える** | **🔴 最も危険** |
| **V-5 / V-6** | V-4 に連動して動く（V-5 は実測で V-4 の従属変数） | 🔴 |
| **V-17**（1着と最下位の差 2〜4秒） | **★上がる**。余分に走る距離が増えるほどタイム差が開く | 🔴 |
| **V-1**（実力が着順に出る度合い） | **★下がる。** K-4 で**選抜が実レース結果ベース**になっているので、雑音が増えれば選抜圧が鈍る | 🟡（**照会では挙がっていませんが、要ります**） |
| V-18（枠と着順の相関） | **★変わらないはず**（シード由来にする限り）。**だからこそ実測して示すこと** | 🟡 |
| V-13 / V-16 | **改善するはず**（内外が読めるようになる＝画面が運ぶ情報が増える） | ✅ |
| **V-2 系（血統）** | 二次的。V-1 が動かなければ動かない | ⚪ 後回しでよい |

### 再検証の範囲（ご指示のとおり指定します）

**必須**: **V-4 / V-5 / V-6 / V-17 / V-18 / V-1**
**併せて報告**: V-8 / V-9a / V-13 / V-16
**不要**: V-2 系・V-7・V-10〜V-12・V-14 / V-15（レース内の横位置に依存しない）

**★手順は D-065 が既に定めたものをそのまま使ってください**: ①**まず大きさを測る**（変更前後で `laneExtraM` の分布：内外差の馬身と、その分散）②**実装して全ゲートの差分を報告** ③**帯を出たときだけ再較正**。

---

## 3. ★「8m」は下限であって、決めてよい値ではありません

**8〜12m は視覚側から出た下限です。上限は V-4 / V-17 が決めます。**

> **両側から挟んで決めてください。** 値を先に決めて後から正当化する形は採りません（D-079 と同じ扱い）。

**掃引すべきは `reveal` の立ち上がり方1つ**です。K×床の掃引で「実行可能領域が空」を先に確かめたのと同じく、**まず帯があるかを確かめてから値を選んでください。** 空だった場合は、そのとき初めて次の案（`swing` の振幅そのもの、`swingScale`）を検討します。

**★余裕を pp で報告してください。余裕ゼロの解は採りません。**

---

## 4. Q-LANE-04 — 成立しません（エンジンを変えるため）

**ただし、この問いに含まれていた判断は正しいです** — 「**残りの馬が画面に入る以上、根本的には直らない**」。**描画側で尽きたという切り分けを、こちらでも支持します**（素材 0.5〜4.2%・カメラ最大 2.0px・1頭なら成立・先頭4頭なら合格品質、という4点の切り分けは筋が通っています）。

**★B（カメラだけ変える）を選んだのは失敗ではありません。** D-062 は「**A を試す前に機構を足す理由がない**」と定めており、**A を尽くしてから B に上げるのが、その決定が指定した順序そのもの**です。**尽くしたことを測定で示したので、上げる資格ができました。**

---

## 5. ⚠️ 「ぴょんぴょん」と「前後ガクガク」は、別の症状かもしれません

オーナー評は2つのことを言っています。

> **馬がぴょんぴょん跳ねているだけ**、**走る場所も前後ガクガク**

- **前者**（脚が互いに隠れて上下動だけ見える）は、**横の広がりで直ります**
- **後者（前後のガクつき）は、横の広がりでは直りません。** 縦方向の位置が時間に対して滑らかでない、という別の症状です

**★1つの修正が2つの症状を直したことにしないでください。** 中盤を散らしたあとで**前後のガクつきが残っているかを、オーナーに改めて確認してください**（[EYES]）。残っていれば、原因は補間（D-059 の描画層の補間）か、局面境界の繋ぎ目にあります。

**参考**: D-063 が入れた表示位置の揺らぎ（jostle）は **D-068 で撤去済み**のはずです。**もし残っていれば、それが前後のガクつきの正体です。** 先に `grep` で確認してください。

---

## 6. 🔴 §7 の2件 — 片方は止めます

### ① 背景（`world-textured.ts`）の作業 — **承認します**

**描画層は結果に触れない**ので、こちらの裁定は要りません。着手して構いません。**ただし D-066（斜め俯瞰）の判断材料になるので、作業後に V-16 を測り直してください。**

### ② 🔴 ナレーターの立ち絵に**実在人物の写真** — **即時撤去**

> 現在は仮置きの**写実的な人物写真**で、肖像・パブリシティの論点があると考えます

**弁護士ゲートに回す前に、止めます。**

| | |
|---|---|
| **憲法 §0.1 との関係** | §0.1 は**実在の競走馬・レース・競馬場**を対象としています。**実在の人物の肖像は、そこに書かれていない別の権利**です。**書かれていないことは、許されていることを意味しません** |
| **なぜ「仮置き」でも駄目か** | **スクリーンショットは外に出ます。** デザイン確認・オーナーへの提示・引継ぎ書への添付 — **どれも複製と提示**です。**「本番に出さないから」は、既に外に出た画には効きません** |
| **判断に時間をかけない理由** | **撤去のコストがほぼゼロだからです。** シルエット・単色の板・空欄で代替できます。**コストゼロで消せるリスクを、判断待ちで保持しない** |

**→ 仮置きを非写真（シルエット等）に差し替えてください。そのうえで、正式な立ち絵の権利処理（生成物か、委託か、実在人物をモデルにしないこと）を §17.6「素材と権利」の項目に足します。**

**★これは弁護士ゲート（§0.3）ではなく、§17.6 の素材の権利です。** 弁護士に諮るべきは「この形式のサービスが賭博・景表法・資金決済法に触れないか」であって、**他人の顔を無断で使ってよいかは諮るまでもありません。**

---

## 7. §6 の修正について — 特筆します

> 私の監査道具が**古い素材**を読み、**横視点用の背景**を貼っていた（＝**オーナーと別の画を見ていた**）

**★これが今回いちばん重要な発見かもしれません。** **測定者と評価者が別の画を見ていたなら、それまでの「合格／不合格」の対応関係が全部ずれていた可能性があります。**

**→ この修正より前に取った視覚評価は、いったん信用しないでください。** 特に **D-066（斜め俯瞰への転換）の根拠**が、この道具で撮った画に基づいているなら、**判断の土台が変わります。**

**再確認をお願いします**: 「合格したのは 80秒付近からゴールまで」というオーナー評は、**道具を直した後の画に対するものですか。** 直す前のものなら、取り直しが要ります。

---

## 8. まとめ

| # | |
|---|---|
| 1 | **(b) 採用。ただし `SETTLE_M` / `base` は触らない。動かすのは `reveal`**（シード由来の広がりを中盤から出す） |
| 2 | **(a) 却下**（枠の広がりを延ばす＝V-18 直撃）／**(c) 却下**（D-069 が明示的に撤回した案） |
| 3 | **内ラチ有利は変わらない。変わるのは距離ロスの分散** → **V-4 / V-5 / V-6 / V-17 / V-18 / V-1 を必須で再検証**。手順は D-065 の①測る②差分報告③帯を出たら再較正 |
| 4 | **8m は下限。上限は V-4 / V-17 が決める。掃引して帯があるかを先に確かめ、余裕を pp で報告** |
| 5 | **「前後ガクガク」は別症状の可能性。1つの修正で2つ直したことにしない**（jostle の残骸を先に grep） |
| 6 | 背景の作業は**承認**。**ナレーターの実在人物写真は即時撤去** |
| 7 | **監査道具が別の画を読んでいた件** — それ以前の視覚評価の有効性を確認すること |
