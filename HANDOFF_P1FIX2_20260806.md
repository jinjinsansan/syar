# 申し送り — P-1 着手前（2026-08-06）

**開発側 → レビュー側**
**コミット**: `926af90`（緑・push 済み）
**位置づけ**: フルのレビュー依頼ではありません。較正値は既にそちらで独立再現・一致確認済みのため、
**①そちらにしか直せないもの ②次の照合の基準値 ③進め方の確認** の3点だけをお送りします。

---

## ① 正典の不備が1件（そちらの作業）

**D-015 で `heavy_aptitude` を genotype に追加する決定はありますが、§5.4 の genotype スキーマ例（JSON）に
該当行が入っていません。** §5.2 の定義だけが更新されています。

```json
"temper":     { "a1": 30, "a2": 65 },
"durability": { "a1": 700, "a2": 640 },
// ← ここに heavy_aptitude が無い
"skill_genes": ["G_SPURT", "G_GATE", "G_MUD"]
```

実装は D-015 に従って進めますが、**次に §5.4 を読んだ人（人でも Claude でも）は
道悪適性が遺伝することに気づけません**。P0 で `condition_aptitude` が §5.2 にあるのに §5.4 に無く、
結果として遺伝しなかったのとまったく同じ形です。追記をお願いします。

---

## ② P-1 の基準値（移行前の P0 ゲート）

`heavy_aptitude` を入れると genotype が 11形質 → 12形質になり、P0 の全ゲートに波及します。
**移行後にどれが動いたかを切り分けられるよう、移行前の値を確定させました。**

| | 結果 |
|---|---|
| `npm run verify`（proxy・既定） | 総合 **PASS** / V-2c 14.88〜16.23% |
| `npm run verify -- --selection race` | 総合 **PASS** / V-1 15.42%(100年)・15.05%(300年) / V-2c 13.91〜15.87% / V-2d 最悪 surface.turf **9.13%** / V-2e 最悪 distance_range **0.91x** |

★**V-2d の余裕が薄い点は移行前から**です（9.13% / 上限10%）。

> ⚠️ **訂正**: 初版のこの表には**実行中の途中出力（seed42 のみ）**を書いていました。
> 上表は4シード完走後の確定値です。測定が終わる前の数字を基準値として出しかけたもので、
> 「どの条件で出た数字か」を確定させる前に報告した点は  の件と同じ形です。P-1 で形質が1つ増えた後に
V-2d/V-2f が動いても、それが `heavy_aptitude` の追加によるものか元からの薄さかを、
この基準値で切り分けます。

---

## ③ P-1 の導出定数（確認していただきたい）

`heavy_aptitude` は 0〜100 スケールで芝/ダート適性と同型なので、
創始定義 `HEAVY_RANGE: [20, 90]` を置けば**既存の導出経路がそのまま**次を出します:

```
sd 13 / clamp 22 / center 55   （＝ surface.turf / surface.dirt と同じ）
```

新しい式は要りません（正典 §6.4・D-013 の `創始アレルSD × √(2r−r²) ÷ 0.916` に従うだけ）。
**創始分布を芝/ダート適性と同じ [20, 90] にしてよいか**だけ、認識合わせをさせてください。
道悪適性の創始分布を別にすべき理由が正典側にあれば教えてください（無ければこのまま進めます）。

---

## 進め方について

**P-1〜P-3 は発行済み指示書のスコープ内で、正典判断も D-015 で済んでいるため、
レビューを待たずに進めます。** 手を止めて返すのは次の3つが起きたときだけにします:

1. 正典の改訂が要ると分かったとき
2. 検証基準の目標値を動かさないと通らないと分かったとき
3. 正典内の矛盾を見つけたとき

なお前セッションで P-1 に着手しかけましたが、**作業余裕が足りず途中で終わる（genotype が
半分だけ移った赤い状態になる）リスクが高かったため着手を見送りました。**
リポジトリは `926af90` の緑のまま、`git status` は空です。

---

## 直近の変更（そちらの最終照合より後のもの）

`926af90`: V-6 の SE 算出法を明示し、余裕(pp)と SE を `verify:race` 自身が出力するようにしました。

- 私が報告した **19.6 SE は根拠不明の誤り**でした（保守的だったのではなく、単に方法を示さずに出した数字）
- 実データからの3手法: 枠を独立とみなす二項 26.5 SE / **レース単位ベルヌーイ（採用）26.8 SE** / シード間分散 31.2 SE
- そちらの素朴計算 26.4 SE とほぼ一致します
- 採用理由: V-6 は1レースで勝つのが1頭なので下位3枠は排他。レース単位で `p3 = 3×rate` を用い、
  得た SE を 3 で割って1枠あたりに戻す。素朴計算とほぼ同値だが排他性を織り込んでいる分だけ根拠が明確

**手計算だと基準がぶれるので、以後はハーネスの出力を使ってください。** 算出法も出力に書いてあります。
