# REPORT_P3_LEVER — ★③は解けました／①②が両立しません

**開発側 → レビュー側** / 2026-08-10
**コミット**: `25f7e2f` 以降（push 済み・542 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**★あり。** ①と②が**2つのつまみでは両立しません**（§3）。**先回りせず報告します。**

---

## 1. ★D-046（疲労の自然回復）を入れました — 二重の罰が消えました

```
                    自然回復なし        自然回復あり（初期値5）
放置の平均疲労         77.9      →         （下記のとおり故障が激減）
放置の故障/頭          1.60      →          0.53
放置の引退率           1.3%      →          0.3%
```

★**「何もしない馬が、最も激しい調教をした馬より疲れる」が解消**しました。
  軽め +4 は実質 **−1**、追い切り +32 は実質 **+27** で、
  「**疲労は強度に対してたまる**」という関係が初めて成立しています。

---

## 2. ★③は解けました — 定義（D-047）が答えを変えました

```
★同一 EP 予算下の開放率（1万EPあたり%）
  追い切り偏重 12.39   vs   バランス型 18.55
  → ★比 0.67倍（追い切り偏重のほうが EP 効率が悪い）
  EP 実額: 80,296  vs  51,578
```

そちらの見積もりは「実効係数 2.29倍 ÷ EP 1.76倍 = **1.30倍**で支配戦略のまま」でしたが、
**D-046 を入れた後の実測は 0.67倍**です。

★理由: **自然回復で追い切り偏重の休養が増え、EP がさらに嵩みました**
  （EP 73,173 → 80,296 / バランス型 41,497 → 51,578）。
  D-046 は疲労の設計を直すために入れたもので、**③に効くとは予想していませんでした。**

**「同一 EP 予算下」の定義では、③は既に満たされています。**

---

## 3. ★①と②が両立しません（つまみが尽きました）

ご指示どおり **`BASE_GAIN` で放置を先に合わせ、その後 `MAIN_EFFECT_COEF`** の順で1次元ずつ解きました。

### 手順1: `BASE_GAIN` で放置（放置は `BASE_GAIN × 0.3` だけで決まる）

```
BASE_GAIN  12    8     6     5     4     3
放置      87.4  75.9  67.9  63.2  58.0  52.3   ← ★帯 55〜75% に入るのは 4〜7 あたり
```

### 手順2: `MAIN_EFFECT_COEF` でバランス型（`BASE_GAIN=6` 固定）

```
MAIN     0.45   0.8   1.1   1.4   1.6
バランス  78.9  80.6  82.0  83.2  83.9   ← ★正典の上限1.6でも 90% に届かない
放置      67.9  67.9  67.9  67.9  67.9   （不動・予想どおり）
```

### ★両立の境界（`MAIN_EFFECT_COEF = 1.6` に固定して `BASE_GAIN` を細かく振る）

```
BASE   放置     バランス   ①55-75   ②≧90
7.0   72.2%    88.0%     PASS     FAIL
7.4   73.7%    89.3%     PASS     FAIL
7.8   75.2%    90.5%    ★FAIL    ★PASS
8.0   76.0%    91.0%     FAIL     PASS
```

★**境界は `BASE_GAIN` 7.4〜7.8 の間にあり、どちらか一方しか通りません。**
  `MAIN_EFFECT_COEF` は既に**正典が追い切りに与えた上限 1.6** まで上げた状態です。
  **2つのつまみは尽きました。**

### なぜ届かないか（機構）

`MAIN_EFFECT_COEF` を 0.45 → 1.6（3.6倍）にしても、バランス型は **78.9% → 83.9%（+5pt）**しか動きません。

```
バランス型のメニュー内訳（182週）
  hill 36 / wood 12 / hard 42 / light 41 / rest 50
  → ★MAIN_EFFECT_COEF が効くのは 48週だけ（26%）
```

残りは**追い切り(1.6)・軽め(0.3)・休養(0)**で、**すべて正典の固定値**です。
さらに `headroom` の飽和があるため、係数を上げても後半は効きません。

---

## 4. ★次に触るものは正典の値なので、私は触りません

そちらのご指示どおり報告します。**故障は強度の利得を打ち消せていない**と実測済みです。

考えられる対象（**私は選びません**）:

| # | 対象 | 現在値 | ①②への効き方 |
|---|---|---|---|
| **A** | `menuIntensity`（追い切り 2.2） | 正典 §7.5 | ③に効く。①②には効かない |
| **B** | 軽め調整の係数 **0.3** | 正典 §7.2 | ★**放置だけを下げられる**。①②の両立が可能になる |
| **C** | `headroom` 指数 **0.7** | 正典 §7.3 | 飽和の速さが変わる。3つすべてに効く |
| **D** | V-14 の帯（放置 55〜75% / バランス 90%以上） | D-044 | ゲート側を動かす |

★**B が最も直接的です。**①は「放置＝軽めのみ」で決まり、②は「バランス型」で決まります。
  **両者を分けているのは軽め調整の係数だけ**なので、そこを下げれば1次元ずつ解けます。
  ただし **0.3 は正典の値**なので、**私が動かすものではありません。**

---

## 5. 実装したもの

- **D-046 疲労の自然回復（初期値5）**: `weeklyFatigue()` を新設。
  ★`applyFatigue` を直に呼ぶと自然回復が抜けるので、**週進行では必ずこちらを使う**旨をコメントに明記
- **D-047 ③の測定**: 「同一 EP 予算下の開放率」を V-14 の出力に追加
- **分解の計装**: メニュー別週数・実効係数・故障休養・平均疲労・平均調子・素質喪失

★較正定数の登録簿が `FATIGUE_NATURAL_RECOVERY` の未登録を捕まえました。
  **0 に変異させると二重の罰が復活する**旨を `affects` に書いて登録しています。

---

## 6. 現在値（この報告の測定条件）

```
BASE_GAIN = 12（正典の既定のまま）/ MAIN_EFFECT_COEF = 0.45 / FATIGUE_NATURAL_RECOVERY = 5

放置 87.4% FAIL / バランス 95.7% PASS / 追い切り 99.5%（同一EP比 0.67倍 PASS 相当）
```

★**①を通す値にはまだ変えていません。**②と両立しないので、
  **どちらを取るかはゲートの持ち主が決めることだからです。**
