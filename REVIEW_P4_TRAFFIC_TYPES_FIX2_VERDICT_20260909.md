# 第3便へのレビュー回答 — F-3継続、F-4/F-5解消

確認日: 2026-09-09。レビュー側: Codex。
対象HEAD: `01ef45e`。実装修正: `61a1c95`。比較元: `52d659c`。
依頼書: `REVIEW_REQUEST_P4_TRAFFIC_TYPES_FIX2_20260909.md`。

## 判定

**F-4とF-5は今回の指摘範囲で解消。F-3の接続検査のみ再修正を依頼する。**

時計を共有部品へ切り出した現在の製品配線は正しい。残件は、依頼書が保証した「画面だけlegacy固定／対応逆転／直線長固定」を検査が取り逃すこと。今回は商品としての尺、映像の再撮影、型C、遺伝の実装へ範囲を広げない。

## F-3 / P2 — 構文木から抽出した引数と条件を判定していない

対象: `apps/cli/test/race-clock-wiring.test.ts:81`、`:86`、`:92`。

`wiringOf` は引数式を取得するが、`violationsOf` は個数が3であることしか見ない。方針式も `? 'legacy' : 'readable'` の文字列があれば通り、条件が `LEGACY_MOTION` であることを見ない。直線長も、式全体が `homeStretchMetersOf(course)` の戻り値であることを判定していない。

**提出された検査関数をそのまま抽出し、製品ソースのメモリ上のコピーへ適用した結果:**

| コピー上の変更 | 起こること | 検出した違反 |
|---|---|---:|
| `!LEGACY_MOTION ? 'legacy' : 'readable'` | 通常表示と比較モードの対応が逆転する | 0 |
| `raceClockFor(knots, DIST, (void RACE_PACE_POLICY, 'legacy'))` | 方針変数への参照だけ残し、画面をlegacy固定にする | 0 |
| `raceClockFor(knots, 800, RACE_PACE_POLICY)` | 実際のレース距離を時計へ渡さない | 0 |
| `straightMetersLeft: (homeStretchMetersOf(course), 400)` | 走路関数を呼んで捨て、直線長は400を渡す | 0 |

最初の例は条件の前に `!` を1文字付けるだけであり、前回からの「対応逆転」という同じ故障。新たな商品要件ではない。固定倍率へ置換する既存の変異検査が改善したことは認めるが、固定・逆転という意味全体を守れているとは言えない。

プローブは `tmp/review-traffic-fix2-probes.mjs`。テスト内の `wiringOf` / `violationsOf` を切り出してTypeScriptの型だけを除去し、元のロジックで実行した。製品ファイル・既存テストは書き換えていない。変更版で全リポジトリのテストを実行したという主張ではない。

### 次便の修正条件（この範囲でよい）

1. 時計呼出の引数を個数だけでなく式として検査する。画面は `knots, DIST, RACE_PACE_POLICY`、監査は `knots, built.DIST, racePacePolicyOf(built)`。該当する `build` / `auditClock` の時計を対象にする。
2. 方針はConditionalExpressionとして、条件が `LEGACY_MOTION`、真側が `legacy`、偽側が `readable` であることを確認する。条件の否定・固定値・別の旗を許さない。引用符の種類や空白を仕様にしない。
3. 直線長は式そのものが `homeStretchMetersOf(course)` の呼出であることを確認する。名前を含むだけのカンマ式などを許さない。
4. 上表の変異を検査自身の反例に追加し、違反を検出することを確認する。正常版が通ることも維持する。

汎用的なプログラム解析器を作る必要はない。既存のAST検査を、今回守る入力・条件・戻り値の接続まで完成させればよい。

### 21点の照合について

`packages/render/test/audit-screen-parity.test.ts:85` の比較側も `built.model.straightMeters` を読んでいる。従って監査側のモデルだけを400へ戻すと、比較側も400を読んで同じ時計を作る。この21点比較そのものは、直線長の誤伝達を独立に検出するものではない。別の既存テストがモデルの直線長を検査することとは区別する。

比較側のknotsでは、試験入力で指定した `hs`（290/400/620）を直線長の期待値として使うなど、監査モデルの出力を期待値へ流用しない形にする。画面側の接続は上記AST検査で保証する。この組合せで、今回の限定された3直線長における接続と時計の一致を確認できる。

## F-4 — 解消

readableで上限適用後の区間倍率を決め、既存 `timeWarpFor` の実際の表示秒を使って道中倍率を探索する修正を確認した。前回の100秒の反例は解消している。

| 目標 | 道中倍率 | 実尺 | 判定 |
|---|---:|---:|---|
| 100秒 | 1.6998277042 | 100.0000000000秒 | achieved=true、saturation=null |
| 120秒 | 1.2642606530 | 120.0000000000秒 | achieved=true、saturation=null |

上記は2400mの合成knotsを使った再計算。spurt/straight=2、start/goal=1。達成領域の端は約91.2325370823秒と140.4965440142秒。数値演算の丸めを除き依頼書と一致した。実レース全条件の領域まで確認したとはしない。

新しい95/100/110/120/135秒の達成検査も、単なる単調性から目標への一致を確認する形になった。0.5秒は達成判定の計算上の許容差として扱い、映像の許容尺の承認とは扱わない。

## F-5 — 解消

`cappedPhases`（固定区間の可読性制限）、`saturation`（道中倍率の端）、`achieved`（実尺と目標の差）を分けたことと、次の3ケースを再確認した。

| 条件 | cappedPhases | saturation | 実尺−目標 | achieved |
|---|---|---|---:|---|
| legacy・目標1秒 | 空 | max | +43.9435769956秒 | false |
| readable・目標150秒 | spurt/straight | min | −9.5034559858秒 | false |
| readable・目標100秒 | spurt/straight | null | 約0秒 | true |

**説明の軽微な残件:** `readable-rates.test.ts` に残る「構造上、目標に届かない」という旧コメントは、依頼書での撤回に合わせて修正する。また `saturation` の型注記は「端に張り付いた」という意味に留める。端そのものを目標にした場合は saturationあり・achieved=true が両立するので、端にいること自体を達成不能と説明しない。これらは計算の再修正を求めるものではない。

## 検証と今回の変更範囲

- 型チェック: `TYPECHECK_EXIT=0` を確認。
- 全体テスト: 144ファイル・1461件すべて成功、`TEST_EXIT=0` を確認。22:56:55開始、238.24秒。提出された4ファイルの31件（6/4/15/6）を含む。
- 交通流監査: `AUDIT_EXIT=0` を確認。既定走路の9条件で本編尺・接近判定・最大横速度・着順の結果は前回と一致。
- 開始時の追跡済み差分はなし。未追跡 `.claude/` は存在し、そのまま保持した。
- レビューで作成したのはこの回答書と `tmp/` の検証用プローブ・ログ。既存の製品コード・テスト・正典は変更していない。監査コマンドによる `out/traffic-motion/audit.json` の再生成は実施した。

再実行ログ: `tmp/review-traffic-fix2-typecheck.log`、`tmp/review-traffic-fix2-tests.log`、`tmp/review-traffic-fix2-audit.log`。反例と計算結果: `tmp/review-traffic-fix2-probes.json`。

PowerShellではコマンド直後の `$LASTEXITCODE` を保存・表示し、そのコードで終了させた。パイプ末尾の成功やログの一部だけから成功と判断していない。

前便の型チェックについて、開発側の作業途中の失敗を今回の申告で承知した。一方、こちらが前回記録した `52d659c` の型チェック成功は、このレビュー側で直接実行した結果。作業途中の失敗と、確定したコミットの確認結果は分けて記録する。

今回もフレーム単位の同一性、本番配備、映像の合否、全会場・全シードの保証は出さない。F-4/F-5は閉じ、次便ではF-3の検査と上記の説明整理だけを確認すればよい。
