# デフォルメ演出 — ★設計レビュー依頼（開発側 → レビュー側）★**第3版・最終修正反映**

**発行**: 2026-09-03（開発側 Claude）
**対象指示書**: `DEV_INSTRUCTIONS_P4_DEFORMED_RACE_PITCH_20260903.md`
**ブランチ**: `p4/race-30sec-cuts` / HEAD `f9c0289`

| 版 | 日付 | 内容 |
|---|---|---|
| 第1版 | 2026-09-03 | ★差分 3 点・照会 4 件を出して裁定を仰いだ |
| 第2版 | 2026-09-03 | ★レビュー側裁定 13 項目を反映。★主因の断定を取り下げ（§1）／★4 層分離（§3）／★Gate 0 を 0A・0B に分割（§10）|
| ★**第3版** | ★2026-09-03 | ★**条件付き承認に伴う最終修正 5 件。** ★「1 コマずつ別々に生成した」を撤回（§1-1）／★「足りないのは接地点だけ」を撤回（§3-3）／★暫定契約 v0（§3-2）／★`.claude/` の実態を記録（§9）／★0B に 0.25 倍速を追加（§10）|

> ⚠️ ★**本書の時点では、まだ実装に着手していません。**
> ★第3版の提出をもって ★**Gate 0A・0B への着手が承認済み**という理解です。
> ★**Gate 1 以降・コミット・push には進みません。**

---

## 0. ★裁定の反映一覧

| # | ★裁定 | ★本書のどこに入れたか |
|---|---|---|
| 1 | ★Gate 0 追加は**承認** | §5-① / §10 |
| 2 | ★コード描画は**条件付き承認**（★部位名・寸法・原点・ピボットをデータ固定／★リグ計算・姿勢モデル・描画アダプタを分離）| ★**§3-2（4 層分離）**・§5-② |
| 3 | ★Gate 0〜2 を `/art-lab` で作る案は**承認**。★`/race` は Gate 3 まで変更しない | §5-③ |
| 4 | ★Gate 1〜3 は無音でよい。★Gate 4 で音を判断 | §6-① |
| 5 | ★「STARミニホース」は**内部仮称として使用可** | §6-② / §8 |
| 6 | ★18〜24% は**デフォルメ評価モード専用値**。★既存演出の定数は変更しない | §6-③ |
| 7 | ★毛色・勝負服は**既存パレットを唯一の色定義**として再利用 | §3-2 ④ / §6-④ |
| 8 | ★`git status --short` を現在値へ更新 | §9 |
| 9 | ★A-11 は「★対象経路を使わない見込み」。★解決とは書かない／★A-12 は Gate 3 で**転送量・デコード量・初期化時間**を実測 | §7 |
| 10 | ★**「真因」を断定しない。「主因候補」とする。** ★既存 8 枚の関節位置・蹄接地点・胴体寸法・騎手位置を実測してから確定 | ★**§1（全面改稿）**・§1-6 |
| 11 | ★`commands.ts` は直接原因ではなく、★**旧描画抽象がリグを表現できない補助証拠** | §1-5 |
| 12 | ★Gate 0 を **0A（単体テスト）** と **0B（人が図形動画を見る）** に分ける | §4 / §10 |
| 13 | ★修正後の依頼書だけを提示。★実装・コミット・push は行わない | ★第2版はそれで提出済み |

### ★第3版で反映した最終修正（★条件付き承認）

| # | ★指示 | ★本書のどこに入れたか |
|---|---|---|
| 14 | ★§9 の `git status` へ `?? .claude/` を追加 | ★**§9**。⚠️ ★**出力には現れません。** ★理由と実態を記録しました（★出力を実際と違う形に書けないため）|
| 15 | ★「1 コマずつ別々に生成した」は生成履歴で実証できないなら「★**ポーズ別の生成指示が記録されている**」へ | ★**§1-1**（★撤回として明記）|
| 16 | ★Gate 0A のパーツ定義 JSON は「★**暫定契約 v0**」。★0B で修正可能、★オーナー承認後に v1 固定 | ★**§3-2** / §9-6 / §10 / §11-② |
| 17 | ★「足りないのは接地点だけ」を撤回。★**接地脚・接地点・関節・パーツ姿勢の連続性**が不足 | ★**§3-3**（★4 つの表として）|
| 18 | ★Gate 0B に ★**0.25 倍速版**を追加（★接地滑り・脚の跳び・騎手の浮きの確認）| ★**§10** |

---

## 1. ★主因**候補** — ⚠️ ★**まだ断定していません**（裁定 10）

> ⚠️ ★**第1版では「真因の同定」と書きました。★取り下げます。**
> ★以下は ★**状況証拠と主因候補**であって、★**実測で確定していません。**
> ★確定は §1-6 の実測を通してからです。

★企画者評は「リアル寄りのキャラクターデザインが、動きの粗を強く見せる」。
★開発側の**候補**は ★**「絵柄ではなく、動きの作り方から出ている」**です。

### 1-1. ★状況証拠 A — 素材の生成メタが、作り方を書いています

`apps/web/public/art/horse-diag-front-v1.json`:

```json
"promptSet": [
  "…change only the legs to a compact gather phase.",
  "…change only the four legs to a powerful push-off phase.",
  "…change only the four legs to a maximum-extension flight phase.",
  "…change only the four legs to a landing/support phase."
]
```

→ ★**ポーズ別の生成指示が記録されています。**
★**蹄がどこで接地しているかを、コードの誰も持っていません。**

⚠️ ★**訂正（第3版）**: ★第2版まで「★**1 コマずつ別々に生成した**」と書いていました。★**撤回します。**
★このメタが証明するのは ★**「ポーズごとに別の生成指示が記録されている」ことまで**です。
★実際の生成が何回の実行に分かれたか、★どの版が採用されたかを示す ★**生成履歴は残っていません**
（★ツール本体は `.gitignore` されており、★リポジトリに残るのは出力とこのメタだけ・`DEV_INSTRUCTIONS_P4_ASSETS_20260813.md` §0-4）。
→ ★**実証できない部分を、実証したかのように書いていました。**

⚠️ ★これが**どれだけの量の破綻を生んでいるか**は、★**まだ数字がありません。** ★§1-6 で測ります。

### 1-2. ★状況証拠 B — これは事故ではなく、★P4 初日の決定です

`DEV_INSTRUCTIONS_P4_ASSETS_20260813.md`（レビュー側発行・オーナー指示が根拠）:

| | 記述 |
|---|---|
| A-1【必須】 | ★「馬のスプライトはこのツールで作る／**他の方法を主たる経路にしないでください**」 |
| A-3【必須】 | ★1 頭で確定させる契約 = ★**「フレーム寸法・列数・フレーム数・基準点」** |

→ ★確定させたのは ★**シート（絵の並べ方）の契約**です。★関節でもピボットでも接地でもありません。

⚠️ ★**「間違いだった」とは書きません。** ★絵柄が未確定な段階でリグは作れず、
★写真調を短期間で出すなら生成コマを並べるのが最速です。
→ ★**間違いではなく、上限に当たった**、という読みです。

### 1-3. ★状況証拠 C — 制約が見た目へ漏れた実例（台帳 C-3）

★ダートの「走り方が芝と違う」の主因は ★**砂煙の位相が 8 段に量子化されていたこと**でした（`b27144f`）。
★**この 8 はコマ数の 8 です。**

⚠️ ★あわせて記録します。★**開発側は先に地面のコントラスト（79% → 95%）を直しており、それは原因ではありませんでした。**

### 1-4. ★状況証拠 D — ★**既存の道具が、既に同種の警告を出していました**

`tools/align-pose-set.mjs` の冒頭（2026-08-20 の記録）:

> ★俯瞰の新素材は**1 コマずつは合格**だったのに、★**動かすとガクガクして走って見えません**でした。
> ★`verify-pose-set.mjs` は**接地点 22.0% / 高さ 26.9%** と出しており（★合格済みは 4.6〜9.6%）、
> ★**道具は正しく警告していました。私が「面積は 5% 以内だから」と理屈をつけて無視しました。**

→ ★**「コマ間で接地点と胴体寸法が暴れる」ことは、既に測られ、既に記録されています。**
⚠️ ★ただしこの数字は ★**俯瞰と `winner-rear` の素材**のものです。
★**いま台本 v6 が実際に使っている 2 組**（`side-v6` 53.4% / `diag-front-v2` 46.6%）の数字ではありません。
→ ★**そこを測るまで、主因とは書けません。**

### 1-5. ★補助証拠 E — ★旧描画抽象がリグを表現できないこと（裁定 11）

`packages/render/src/commands.ts` の `sprite` 命令:

```ts
{ kind: 'sprite'; sprite: { sheet, frame }; at; silk?; flip?; scale: 1 | 2 }
```

★回転がありません。★倍率も整数のみ。

⚠️ ★**これは現在の `/race` の直接原因ではありません**（裁定 11）。
★この §12.8 の純粋層を使っているのは `scene.ts` と `apps/web/src/lib/canvas-renderer.ts` で、
★**`/race` は通っていません**（`drawBroadcastV2Scene` 等が直接 ctx に描きます）。

→ ★**位置づけは「旧描画抽象がリグを表現できない」という補助証拠**にとどめます。
★「絵を選んで置く」以外の語彙が、★当時の設計に無かったことの傍証です。

⚠️ ★**訂正**: ★第1版および口頭で「既存の `commands.ts` の上に組める」と申し上げましたが、★**不正確でした。**

### 1-6. ★**確定のための実測**（裁定 10）— ★既存の道具で足ります

⚠️ ★**新しい道具を作りません**（R-30: 指標も道具も、まず既にある実装を使う）。
★調べたところ、★裁定が挙げた 4 項目のうち **3 つは既存の道具が既に出します。**

| 裁定が挙げた項目 | ★何で測るか | ★状態 |
|---|---|---|
| ★**蹄接地点** | `tools/verify-pose-set.mjs` … ★「被写体の下端が全コマで揃っているか」を**既に出力**（★合格帯 4.6〜9.6% の実績あり）| ★**そのまま使えます** |
| ★**胴体寸法** | `tools/align-pose-set.mjs` の方式 … ★**面積の平方根**（倍率）と ★**上半分の重心**（位置）<br>⚠️ ★**外接矩形は使えません** — ★脚が伸び縮みするので矩形は**正しく**変動します（★同ツールの注記）| ★**そのまま使えます** |
| ★**騎手位置** | ★上半分の重心 ＋ ★既存の `isSkinTone` / 勝負服パレット色の分布 | ★**組み合わせで出ます** |
| ★**suspension・接地脚の本数** | `tools/measure-gallop.mjs` … ★宙に浮く局面／伸縮／接地脚数を**既に測ります** | ★**そのまま使えます** |
| ⚠️ ★**関節位置** | ★**測れません。** ★写実的な生成画に関節の記録が無く、★画素から自動抽出できません | ★**申告します**（下記）|

⚠️ ★**関節位置だけは代理指標に置き換えます。** ★「関節を測った」とは書きません。
★代わりに ★**接地点・胴体倍率・重心の 3 つがコマ間でどれだけ暴れるか**を出し、
★それが ★**合格帯（4.6〜9.6%）に対してどこにあるか**で判定します。

★**測る対象**: ★台本 v6 が実際に使う 2 組のみ（★`side-v6` / `diag-front-v2`）。
★描かれない `diag-rear-v2` / `high-diag-v2` は測りません（★台帳 A-11・0% 実測済み）。

★**この実測は Gate 0A と同じ便で行い、数字が出るまで §1 の見出しは「主因候補」のままにします。**

---

## 2. ★デフォルメが効く理由（★企画者の直感と、理由の違い）

⚠️ ★**絵柄だけデフォルメにして、同じ作り方（★ポーズごとに絵を起こして並べる）を続けたら、★可愛くなった同じ違和感が出ます。**

★指示書 §2-2 が「★AI 生成した各フレームをそのまま連続再生し、骨格の変化を許容しない」を
★禁止事項として名指ししているのは、★同じ懸念からだと理解しています。

★デフォルメの意味は「★粗が目立たなくなる」ことではなく、
★**手で作れる大きさのリグに、問題を落とせる**ことだと考えています。

→ ★**分岐は「リアルか デフォルメか」ではなく、「★リグを持つか持たないか」。**
★指示書 §12-8「高品質リグ素材案との比較も必要か」も、★この軸なら同じ土俵で比べられます。

---

## 3. ★設計

### 3-1. ★横滑りを「調整」ではなく ★**式で 0 にする**

```
位相 φ = (見た目の進行距離 ÷ 7m) の小数部        ← 時間ではなく距離から
各脚に「接地している区間 [a, b)」を持たせる
  ★接地中 … 蹄の【世界座標】を固定する
             画面 x = 固定した世界座標 − カメラ位置
  ★遊脚中 … 次の着地点へ補間する
```

→ ★接地中の蹄は世界に刺さっているので、★**背景とまったく同じ速度で後ろへ流れます。**
★滑りは「見えにくくなる」のではなく、★**式の帰結として 0** です。

★胴体の上下は ★**接地している脚から**出します（★脚が押す → 胴が上がる）。
★これで指示書 §4-3 の禁止事項が、★構造的に起こせなくなります:

| §4-3 の禁止 | ★なぜ起こせないか |
|---|---|
| ★胴体を固定したまま脚だけを回す | ★胴の高さが接地脚から出ます |
| ★蹄が接地中に進行方向へ滑る | ★接地中の世界座標が定数です |
| ★騎手と馬が別々の周期で上下する | ★騎手は胴の値 × 0.4・★同位相から引きます |
| ★1 周期内で脚の長さ・関節数が変わる | ★パーツ長が**データの定数**です（★§3-2 ①）|

### 3-2. ⚠️ ★**4 層分離**（裁定 2 の条件）

★裁定の条件「★部位名・寸法・原点・ピボットをデータとして固定し、★リグ計算・パーツ姿勢モデル・描画アダプタを分離する」
★を、次の 4 層に落とします。

```
① ★パーツ定義（★データ・絵ではない）
   部位名 / 寸法 / 原点 / ピボット / 親子関係 / 描画順
   ★これが「契約」です。★2026-08-13 の A-3 が固定したのはシートの契約でしたが、
   ★今回固定するのは ★**骨組みの契約**です
        ↓ 読むだけ
② ★リグ計算（★純粋・Canvas も画像も知らない）
   進行距離 → 位相 φ → 接地判定 → ★蹄の世界座標
        ↓
③ ★パーツ姿勢モデル（★純粋）
   ①と②から、各部位の { x, y, angle, scaleX, scaleY, z } を返す
   ⚠️ ★**ここまで、絵の形式を一切知りません**
        ↓ 姿勢だけを渡す
④ ★描画アダプタ
   姿勢 → 実際の描画
   ★**ベクターか画像かの違いは、この層の中だけに閉じます**
   ★色は ★**既存パレットから役割名で引きます**（裁定 7・`palette.json` / `COAT_TRANSFORMS`）
```

★**この分離が、裁定 2 の「どちらでもよい」を実際に成立させます。**
★①②③は**ベクターでも画像でも同一**で、★差し替え点は ④ の 1 箇所だけです。
→ ★ベクターで気に入らなければ、★**同じ①②③のまま画像パーツを差し込むだけ**です。
★動きの作業は 1 行も無駄になりません。

⚠️ ★**①のデータは Gate 0A の成果物です。** ★Gate 0 を「図形で仮に作る」で終わらせません。
★**部位名・寸法・原点・ピボットは、Gate 0A の時点で JSON に書き出します**（指示書 §13-6）。

⚠️ ★**ただし「暫定契約 v0」です**（★裁定・第3版）:

| 段 | ★契約の状態 |
|---|---|
| ★Gate 0A | ★**暫定契約 v0** を書き出す（★`schemaVersion: 0` / ★ファイル名にも `v0` を含める）|
| ★Gate 0B | ★**修正可能。** ★動画を見て寸法・原点・ピボットを直してよい |
| ★オーナー承認後 | ★**v1 として固定**。★以後は変更に便を要する |

★**v0 のまま Gate 1 の絵を発注しません。** ★原点が動くと絵が全部ずれるためです。

### 3-3. ★流用する既存値（★R-30）

| 何を | 出どころ |
|---|---|
| ★1 完歩の距離 | `BROADCAST_STRIDE_M = 7`（`broadcast-v2.ts:712`）|
| ★馬体長 | `HORSE_LENGTH_M = 2.4`（`course.ts:26`）|
| ★位相の個体差 | ★`gate * 2.96`（★既存式・`page.tsx`）|
| ★見た目の進行距離 | `visualScroll.deltaAt(d)`（★時間圧縮を打ち消す既存層）|
| ★色 | ★`palette.json` / `COAT_TRANSFORMS`（★裁定 7・**唯一の色定義**）|

★**規律の半分は既にありました。** `page.tsx:2715`:

> ★脚の周期は「時間」ではなく「進んだ距離」から。
> ★旧: `raceD * 12`（全馬 1.5 完歩/秒で一定・速度と無関係）→ 1 完歩 ≈ 7m

⚠️ ★**訂正（第3版）**: ★第2版まで「★**足りないのは『1 周期の中で、蹄がどこに刺さっているか』だけ**」と書いていました。★**撤回します。**

★足りていないのは ★**1 つではなく、4 つ**です:

| # | ★不足しているもの | ★いまどうなっているか |
|---|---|---|
| ★① | ★**接地脚**（どの脚が地面に着いているか）| ★概念がありません。★絵の中にしかありません |
| ★② | ★**接地点**（着いている脚が、どこに刺さっているか）| ★同上 |
| ★③ | ★**関節**（脚の折れ方・角度）| ★同上。⚠️ ★**画素からも取り出せません**（§1-6）|
| ★④ | ★**パーツ姿勢の連続性**（コマ間で胴・頭・騎手の姿勢が滑らかに繋がること）| ★保証されていません。★`align-pose-set.mjs` が事後に**揃え直して**いるのが現状です |

→ ★リグが与えるのは ★**この 4 つを、絵ではなく数として持つこと**です。
⚠️ ★**「接地さえ直せば済む」という読みは誤りでした。**

---

## 4. ★Gate 0A の検定（★裁定 12）— ★絵を 1 枚も撮らずに数字が出ます

| # | 何を測るか | 何が守られるか |
|---|---|---|
| 1 | ★接地中の蹄の世界座標が、位相に依らず一定（許容 ε）| ★**滑り 0** |
| 2 | ★各パーツ長が位相に依らず一定 | ★**骨長不変**（指示書 §10-3）|
| 3 | ★同 seed・馬番・時刻で常に同値／★30fps と 60fps で一致 | ★**決定論**（憲法4・§10-2）|
| 4 | ★12 頭の位相が同一にならない | ★**位相分散**（§4-1）|

★すべて**純粋関数の単体テスト**です。
⚠️ ★これは ★**台帳 B-2（`--sec 32` はカットの境目）と B-3（オーナーの画面は 67%）を最初から踏まない**形でもあります。
★画素を撮って判定する段は、★Gate 0B 以降に限定されます。

---

## 5. ★差分 3 点 — ★裁定と、その満たし方

### ★差分① Gate 0 を足す → ★**承認**（裁定 1）

★さらに **0A（単体テスト）／0B（人が図形動画を見る）** に分けます（裁定 12）。★§10 の表。

### ★差分② コード描画 → ★**条件付き承認**（裁定 2）

★裁定: ★**完成画は「透過画像パーツ」でも「コードネイティブなベクターパーツ」でもよい。**
★条件: ★**部位名・寸法・原点・ピボットをデータとして固定**し、★**リグ計算・パーツ姿勢モデル・描画アダプタを分離**すること。

→ ★**§3-2 の 4 層分離がその条件そのものです。** ★①がデータ固定、★②③④が分離です。

| | |
|---|---|
| ★どちらを先に試すか | ★**ベクター**から試します（★作り直しが分単位で回るため）。★不合格なら ④ だけ差し替えて画像パーツへ |
| ★期待（⚠️ ★**見込み・未実測**）| ★端末での焼き込みが減る見込み／★引き伸ばし（A-11）が消える見込み／★色替えが実質無料 |
| ★リスク | ⚠️ ★**出来が好みに合うかは保証できません。** ★Gate 1 で実寸 2〜3 案を出します |

### ★差分③ 置き場所 → ★**承認**（裁定 3）

★Gate 0〜2 は `/art-lab`。★**`/race` は Gate 3 まで 1 行も変更しません。**
★`/art-lab` は「★実寸（レース中の大きさ）と拡大を並べて絵柄の候補を選ぶ」ために既に在ります。
★`presentation=deformed` は Gate 3 で繋ぎます（★既定は `standard` のまま）。

---

## 6. ★照会 4 件 — ★裁定

| # | ★裁定 | ★反映 |
|---|---|---|
| ★① 音 | ★**Gate 1〜3 は無音でよい。Gate 4 で判断**（裁定 4）| ★Gate 4 まで音の作業をしません。★指示書 §7 の「無音版と明記」に従い、★動画に**無音版である旨を記載**します |
| ★② 名称 | ★**「STARミニホース」を内部仮称として使用可**（裁定 5）| ★コード・変数名・画面表示に使用。★最終名称はオーナー判断待ちである旨をコメントに残します |
| ★③ 画面高 | ★**18〜24% はデフォルメ評価モード専用値。既存演出の定数は変更しない**（裁定 6）| ★新モード側に**独立した定数**を置きます。★`broadcast-v2.ts:581-586` の 35〜45%（★実装 39.7%）には**触りません** |
| ★④ 色 | ★**既存パレットを唯一の色定義として再利用**（裁定 7）| ★§3-2 ④。★ベクターの塗りも**役割名で引きます**。★16 進を新モード側に持ちません |

---

## 7. ★台帳との関係（★裁定 9・指示書 §13-4）

| 台帳 | ★この便でどうなるか |
|---|---|
| ★A-1（4 角入口の走行方向の反転）| ★**直りません。** ★転換カードで**映さないだけ**です。★報告書に「解決」とは書きません |
| ★A-9（引き込みが走路を曲げる）| ★**直りません。** ★同上 |
| ★A-11（斜め後ろ・俯瞰の引き伸ばし）| ★**「対象経路を使わない見込み」**とだけ書きます（裁定 9）。⚠️ ★**解決とは書きません。**<br>★素材そのものは引き伸ばされたまま存在し、★`?cinematography=v3`/`v2` では今後も描かれます |
| ★A-12（端末で焼く 76MB）| ★**Gate 3 で実測します**（裁定 9）。★測る 3 つ: ★**転送量／デコード量／初期化時間**<br>⚠️ ★それまで数値の見込みを報告書に書きません |
| ★A-13 / A-14 | ★**触りません** |

---

## 8. ★憲法まわりの申告

★企画者は口頭で既存作品を 1 つ挙げていますが、
★**憲法1 および指示書 §2-2 により、本書にもコードにもファイル名にも、その名称を記載しません。**
★参照するのは ★**様式**（★短脚・低頭身・丸い胴体・大きめの蹄）だけです。
★造形・輪郭・顔・配色・演出は参照しません。★比較ラベルにも使いません。

★内部仮称は ★**「STARミニホース」**（裁定 5）。

---

## 9. ★§13 事前確認の記録（★裁定 8・★現在値）

```
$ git status --short
?? DEV_INSTRUCTIONS_P4_DEFORMED_RACE_PITCH_20260903.md
?? REVIEW_REQUEST_P4_DEFORMED_RIG_20260903.md

$ git log --oneline -1
f9c0289 台帳: ★A-11 は「いまの台本では 1m も描かれない」と測れたので格下げ。★A-12 に本日の 4 手を反映
```

### ⚠️ ★`.claude/` について — ★**指示は「`?? .claude/` を足す」でしたが、出力には出ません**

★裁定に従って追記しようとしましたが、★**この機械では `git status` に `.claude/` が現れません。**
★出力を実際と違う形に書き換えることはできないので、★**理由とともに記録します。**

```
$ ls -a .claude
settings.local.json

$ git ls-files .claude          # ← 追跡されているか
（空 ＝ 追跡されていない）

$ git check-ignore -v .claude/settings.local.json
"C:\Users\USER/.config/git/ignore":3:**/.claude/settings.local.json    .claude/settings.local.json
```

| | |
|---|---|
| ★中身 | ★`settings.local.json` **1 本のみ** |
| ★なぜ出ないか | ★**この機械の利用者ごとの除外ファイル**（`~/.config/git/ignore`）が除外しているためです |
| ⚠️ ★**リポジトリの `.gitignore` は `.claude/` を守っていません** | ★`grep -n "claude" .gitignore` … ★**0 件** |
| ⚠️ ★**含意** | ★**この除外を持たない別の機械・CI では `?? .claude/` が現れます。**<br>★そこで `git add -A` を実行すると ★**リポジトリに入ります**（★引継ぎ書 §3-5 の画像の件と同型）|

★**指示書 §13-2「`.claude/` を含む未追跡物を勝手に削除・変更しない」は守っています**（★1 バイトも触っていません）。
★上の「リポジトリ側で守られていない」件は ★**本件のスコープ外**と判断し、★勝手に `.gitignore` を編集していません。
→ ★**別便での対応要否を、レビュー側でご判断ください。**

| # | 指示書 §13 | ★状態 |
|---|---|---|
| 1 | ★`git status --short` と `git diff` を記録 | ★上記。★**追跡ファイルの差分ゼロ**（★未追跡 2 本はいずれも本件の文書）|
| 2 | ★`.claude/` を含む未追跡物を勝手に削除・変更しない | ★**触っていません** |
| 3 | ★現行演出・事前ベイク・素材読込・モバイル全画面の入口を調査 | ★済（§1・`page.tsx:3407` が全画面）|
| 4 | ★台帳の未解決事項と今回の範囲を混同しない | ★§7 |
| 5 | ★既存の画像生成物を再利用する場合、生成条件と権利方針を確認 | ★ベクターを選ぶ場合は**既存の生成物を 1 枚も使いません**。★画像パーツへ倒す場合はこの項を改めて満たします |
| 6 | ★新しい画像を作る前に、パーツ・寸法・原点・命名規則を確定 | ★**Gate 0A で暫定契約 v0 を書き出し**、★0B で修正、★オーナー承認後に v1 固定（§3-2 ①）|
| 7 | ★スコープ外の判断は質問書へ | ★本書（第1版）がそれで、★裁定を受領済み |

★**実装は 1 行も書いていません。★コミットも push もしていません。**

---

## 10. ★段取り（★裁定 12 で Gate 0 を分割）

| 段 | やること | ★成果物 | ★判定 |
|---|---|---|---|
| ★**0A** | ★①パーツ定義（★**暫定契約 v0** の JSON）＋②リグ計算＋③姿勢モデル＋★§4 の検定 4 件<br>★あわせて ★**§1-6 の実測**（既存道具・`side-v6` / `diag-front-v2`）| ★テスト結果と実測値 ＋ ★v0 の JSON | ★**機械**（★人は見ません）|
| ★**0B** | ★④描画アダプタ（図形）で ★**3 秒・真横・単色背景**の動画<br>★**通常速度と 0.25 倍速の 2 本**（★裁定・第3版）| ★30fps 動画 **2 本** | ★**オーナー**（★動きだけ）|
| ★**1** | ★同じ①②③に**絵を載せる**（輪郭・面・たてがみ・目・勝負服・ゼッケン）| ★実寸と拡大の静止画 ＋ 3 秒 | ★オーナー（★絵柄）|
| ★**2** | ★4 頭・6 秒（位相分散・毛色・勝負服・重なり）| ★6 秒動画 | ★オーナー |
| ★**3** | ★`/race?presentation=deformed` に §6 の台本を接続<br>★**A-12 の 3 指標を実測**（転送量・デコード量・初期化時間）| ★20〜24 秒 ＋ 実測値 | ★オーナー |
| ★**4** | ★HUD・結果・モバイル全画面・§11 の比較資料一式・★音の判断 | ★企画者提示版 | ★オーナー → 企画者 |

★**0B で判定していただくのは 1 点だけです**:

> ★**この骨組みに完成した絵を載せれば、違和感のないデフォルメ馬になりそうか。**

★ここで不自然なら、★**ほぼ損失なしでリグを修正できます**（★絵を 1 枚も作っていないため）。

### ★0B は 2 本出します（★裁定・第3版）

| 本 | ★速度 | ★何を見るためか |
|---|---|---|
| ★① | ★**通常速度**（30fps・実時間 3 秒）| ★全体として走って見えるか |
| ★② | ★**0.25 倍速**（★同じコマを 4 倍の尺で）| ★**通常速度では見落としやすいもの**: ★接地滑り／★脚の跳び／★騎手の浮き |

⚠️ ★**0.25 倍速はコマを増やしません。** ★同じ姿勢列を引き伸ばして出します
（★補間で新しいコマを作ると、★**実際には無い滑らかさ**を見せることになるため）。

★最終報告は指示書 §14 のとおり `REPORT_P4_DEFORMED_RACE_PITCH_20260903.md` に出します。
★オーナー確認前に、★既存演出の置換・採用確定・量産開始・外部公開は行いません。

---

## 11. ★次の一手

★着手するのは ★**Gate 0A と 0B だけ**です。

| ★やること | ★やらないこと |
|---|---|
| ★①暫定契約 v0 の JSON | ⚠️ ★**Gate 1 以降**（★絵・4 頭・台本・本編）|
| ★②リグ計算・③姿勢モデル（★純粋関数）| ⚠️ ★**コミット** |
| ★§4 の検定 4 件 | ⚠️ ★**push** |
| ★§1-6 の実測（★既存道具）| ⚠️ ★**`/race` への変更**（★Gate 3 まで） |
| ★④図形の描画アダプタ ＋ ★動画 2 本（通常速度・0.25 倍速）| ⚠️ ★**12 頭分の絵／20 秒の本編** |

⚠️ ★**避けるべきこととして共有された点を、そのまま守ります**:
★「早く企画者に再提出しなければ」と焦って、★**12 頭分の完成画像や 20 秒の本編を先に作らない。**
★まず 3 秒の動きで**制作方式を確定**させる。★それが最短である、という理解です。

★**次にご覧いただくのは文章ではなく、★通常速度と 0.25 倍速の 3 秒動画 2 本**です。

★**残っている確認事項**（★着手前に潰す必要のあるものはありません。★Gate 0A / 0B の中で埋まります）:

| # | | ★いつ埋まるか |
|---|---|---|
| ★① | ★§1 の主因候補の確定（★実測値との突合）| ★**Gate 0A** |
| ★② | ★部位名・寸法・原点・ピボットの ★**v0 → v1 固定** | ★**Gate 0B 後・オーナー承認**（§3-2）|
| ★③ | ★ベクターと画像パーツのどちらで完成させるか | ★**Gate 1**（★④の差し替えのみ）|
| ★④ | ★リポジトリの `.gitignore` が `.claude/` を守っていない件 | ★**別便の要否をレビュー側判断**（§9）|
