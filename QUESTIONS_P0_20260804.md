# P0 照会事項 — 開発側 → レビュー側／オーナー

**発行**: 2026-08-04（開発側 Claude）
**対象**: 指示書 `DEV_INSTRUCTIONS_P0_20260804.md` / 正典 `STAR_SPEC_v2.0.md`
**状態**: Q1 は**合格基準 V-2 に直接関わる**ため判断が要る。Q2〜Q4 は確認のみ（実装は暫定判断で進めた）

---

## Q1【最重要】V-2（+50%以内）は現行の正典の遺伝機構では達成できない

### 何が起きたか

100世代シミュレータを正典 §6 のとおりに実装して回したところ、**V-2 が構造的に達成不能**でした。

| 設定 | V-2 実測（4シード平均 / 最悪） | V-1 | 判定 |
|---|---|---|---|
| 指示書 §3.4 の文字どおり（能力合計上位20%＝真の素質値で選抜） | **+95.03% / +99.71%** | 8.44% | FAIL |
| 選抜だけ現実的な精度にした出荷既定（h²=0.3） | **+64.71% / +73.8%** | 14.02% | FAIL |

（seed 42 単体では +64.96%。ただし**シードによって ±10pt 動く**ため、単一シードでの判定は危険です。seed 42 は比較的甘い側でした）

### なぜ起きるか（根本原因）

正典 §13.3 は血統インフレ抑制の三本柱を「アレル値上限1000 / 突然変異が負にも振れる / headroom」としていますが、**この3つはいずれも平均の上昇を止めません**。

1. **上限1000** — 創始平均450に対して +122% の位置にある「壁」です。壁は +50% を守れません
2. **平均0の対称な突然変異** — 方向性選抜と組み合わさると、毎世代その正の裾が選ばれるので、抑制どころか**選抜応答の燃料**になります（実測: 突然変異を完全に切っても +43.98%。切らないと +64.96%。つまり突然変異は約20pt ぶんインフレを**足して**います）
3. **headroom** — `current`（調教）側の頭打ちであり、`potential` には作用しません（正典 §7.3）

さらに V-1 と V-2 は**同じ量の裏表**です。両者とも σ/平均 というスケール不変量に支配されます:

- V-1（変動係数）を 12% 以上に保つには、アレルの σ/平均 ≳ 0.2 が必要
- その分散があると、14世代ぶんの方向性選抜で平均は必然的に +50% を超える

実測でも、創始平均を上げて上限との差を詰める（＝ V-2 を通す）と **V-1 が 4.9% まで崩壊**しました。**片方を通すと他方が壊れます。**

> 補足: 100年回して血統上の世代深度は **13.9世代**でした（成熟6歳・繁殖8年のため）。「100世代」を文字どおり100回の親→子と読むと選抜応答は更に約7倍になり、事態は悪化します。

### 実在サラブレッドとの対比

現実のサラブレッドは150年以上の強烈な選抜下で、走破時計がほとんど改善していません。これは**選抜停滞（selection plateau）**によるもので、加算的遺伝分散の枯渇と生理的限界への漸近が原因です。**正典にはこの「停滞」を表現する機構がありません。**

### 提案（採否の判断をお願いします）

正典 §6.4 の突然変異ノイズに「品種平均への回帰」ドリフトを加える案を実装し、**既定オフ（`REGRESSION_RATE = 0`）**で置いてあります。

```
noise = N(0, MUTATION_SD) - (アレル値 - 品種中心) × REGRESSION_RATE
```

分散（＝ V-1 のばらつき）は突然変異で維持したまま、平均だけが平衡点に落ち着きます。実測（4シード）:

| 案 | 設定 | V-2 平均/最悪 | V-1 | 親子相関 | 判定 |
|---|---|---|---|---|---|
| **A（推奨）** | 回帰0.20 + MUTATION_SD 90 + h²0.3 | **+28.81% / +30.36%** | 15.83% | 0.365 | PASS |
| B（低乖離） | 回帰0.15 + MUTATION_SD 75 + h²0.3 | +36.67% / +43.91% | 14.57% | 0.358 | PASS |
| C（h²仮定なし） | 回帰0.25 + MUTATION_SD 100 + h²1.0 | +44.24% / +49.45% | 15.57% | 0.280 | PASS |

**親子相関**（中間親と産駒の能力相関）は「血をつなぐ意味」が残っているかの健全性指標です（正典 §1.1 / §1.4-3）。案Aの 0.365 は**①指示書どおりの 0.348 より高い**（①は上限飽和で血統が効かなくなっているため）。つまり案Aは血統の意味を薄めずにインフレだけを抑えています。

### 選択肢

- **Q1-a**: 案A（またはB/C）を採用し、正典 §6.4 に平均回帰を追記する ← **開発側の推奨は案A**
- **Q1-b**: 平均回帰を採らず、V-2 の目標値をオーナー承認のうえ改訂する
  （例:「+50%以内」→「最終20世代の平均能力の傾きが 0.5%/世代 未満（＝頭打ちしている）」。
   実在の選抜停滞に照らすと、**比率よりも傾きで定義するほうが本来の意図に合う**と考えます）
- **Q1-c**: 選抜圧そのものを運営仕様で下げる（種牡馬候補のランダム化・年間種付上限の引き下げ）。
  ゲーム性（強い種牡馬を狙う楽しさ）を損なうため開発側は非推奨

**指示書 §4「目標値そのものは変更禁止」に従い、目標値には一切手を触れていません。** 現在の納品物は V-2 FAIL のままです。

---

## Q2 3×4 クロスの近交係数の期待値

指示書 §3.5-2 に「3×4クロス F=0.0703125」とありますが、Wright の式から 3×4 単独でこの値は導出できません。

- 3×4（仔から見て父の第2代・母の第3代に同一祖先）→ `F = (1/2)^(2+3+1) = 1/64 = 0.015625`
- これはサラブレッド生産で通用している「3×4 = 1.56%」と一致します

一方 0.0703125 = 9/128 は、**複数の共通祖先の寄与の和**としてなら厳密に作れます（2×3 の 0.0625 + 4×4 の 0.0078125）。テストには両方を入れてあります（`packages/sim-engine/test/inbreeding.test.ts`）。

→ **確認依頼**: 0.015625 を正、0.0703125 は多重クロスの例、という理解で問題ないでしょうか。

---

## Q3 突然変異は「形質ごと」か「アレルごと」か

正典 §6.4 は「各形質に平均0の正規分布ノイズを常時加える」とあります。P0 では **アレルごとに独立** に適用しました（遺伝子は片方だけ変異するのが自然であり、また変異が遺伝形質として次世代に伝わるため）。

形質ごと（両アレルに同じノイズ）にすると V-1 の分散が約1.4倍になります。この解釈で確定してよいか確認をお願いします。

---

## Q4 正典に数値定義がなく、P0 で暫定的に埋めた箇所

いずれも実装を止めるほどではなかったため暫定値で進めました。REPORT_P0 §5 に全件列挙しています。特に確認いただきたいのは以下2点です。

1. **素質値の発現規則**（正典 §5.4 は2アレル保持を定めるが、2つのアレルから potential を作る規則が未定義）
   → P0 は相加的（2アレルの平均・`DOMINANT_WEIGHT = 0.5`）としました。優性/劣性を入れるなら §5.4 への追記が必要です
2. **距離適性 `distance_center` / `distance_range` の値域**（未定義）
   → P0 は center 1000〜3600m / range 200〜1200 としました
