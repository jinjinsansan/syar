# 【STAR】裁定: デザイナーの第 1 便（導入フロー）と、確認事項 11 点 — 2026-09-23

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: デザイナーの第 1 便 `第一便.zip`（`design_handoff_intro_v1/`・リポジトリ直下・**Git 追跡外のまま**）／開発側 `star-aa` の読み取りと答え（案）
- 運用: オーナー指示「承認不要で自立的に進める」

## 0. 第 1 便の受け取り — ✅

- ✔ 開発側は ZIP をリポジトリに入れず、一時フォルダで読んだ（`git status` に現れない）。★レースの ZIP と同じ扱いで正しい
- ✔ README の守る決まりが、裁定と一致している（素質・能力・★・上限までの % を出さない／日数は枠だけで中身は開発側の関数／「あとで」を置かずナビは止めない／登録〜命名は常設レースを出さず 8 から出す／停止スイッチと `reduced-motion` で初期停止）
- ✔ `dc.html` は参照物で製品に写さない・`support.js` は製品に入れない、と README が書いている

## 1. 確認事項 11 点への裁定

| # | 開発側の答え（案） | 裁定 |
|---|---|---|
| 1 | 依頼本文の欠落を再送する | ✅ ★**レビュー側がオーナーに再送の文面を渡した**（第 2 便の本文と納品物 3〜6）。開発側の再送は不要 |
| 2 | 賞金は PP（賞金ポイント）・参加ポイントは EP | ✅ ★画面では**単位の語を必ず添える**（「◯◯ EP」「◯◯ PP」）。★種付料は EP、総獲得賞金は PP で、**同じ画面に並ぶ**ので混ぜない |
| 3 | 🔴 毛色は持っていない | ✅ **依頼文から毛色を外す**（§2） |
| 4 | 理由コードをそのまま渡せる | ⚠️ ★**コードのままデザイナーに渡さない**。★3 つの見せ方への**対応表**にしてから渡す（§3） |
| 5 | 生産中の経過時間は返していない（`created_at` を足せる） | ✅ **足す**（§4） |
| 6 | 録画は常設表示のレース。調教場面そのものは録画ではない | ✅ そのとおり |
| 7 | 付与馬は名前付き | ✅ |
| 8 | 性別は誕生後に分かる。確定前は分からない | ✅ ★確認の画面で**性別を約束しない**（「生まれてから分かります」） |
| 9 | 誕生カットの素材 | ✅ オーナーとデザイナーの判断 |
| 10 | 添付画像が届いていない | ✅ ★レビュー側からオーナーに伝えた（第 2 便で要る「引退馬の画面」が主） |
| 11 | 「名前はあとから変えられません」と書いてよいか | ⚠️ **条件つきで書いてよい**（§5） |

## 2. 🔴 毛色 — 依頼文から外す

- ✔ 開発側の言うとおり、毛色は馬のデータに無い（レースの映像で枠番から色を決めているだけ）
- 🔴 ★**「見せてよいものに毛色を入れた」のはレビュー側の誤り**（依頼文の共通の決まり・裁定 `872f409` §6 の一覧）。★データの有無を確かめずに、見せてよい項目に入れた
- **決定**: ①依頼文の共通の決まりと候補の一覧の表から**毛色を外す** ②デザイナーに「毛色は今のデータには無いので、色で見分ける表現は使わないでほしい」と伝える（**訂正としてはっきり伝える**）③毛色そのものを持つかどうかは**この便では決めない**（血統の見た目を増やす話として、画面が揃ってから）

## 3. 理由コードの渡し方 — ⚠️ 対応表にする

デザイナーの画面は **3 つの見せ方**（選び直し／もう一度／頭数がいっぱい）で作られている。★コードの一覧をそのまま渡すと、デザイナーが 10 通りの画面を作ることになる。**対応表にしてから渡す**:

| 見せ方 | 当てはまるコード |
|---|---|
| **この組合せは選べない**（父母を選び直す） | `dam_already_bred_this_year`／`dam_not_candidate`／`sire_not_candidate`／`parent_missing`／`canMate` の拒否理由（年齢・種付上限・近交）／`fee_above_max`※／`ep_short`※ |
| **もう一度お試しください**（本人は悪くない） | `internal_error` |
| **持てる頭数がいっぱい** | `owner_limit` |
| **候補が戻るまで待つ** | `no_candidate` |

- ※ `fee_above_max`（種付料が上がった）と `ep_short`（EP が足りない）は、**第 2 便でだけ**出る。★第 2 便では、この 2 つは「選び直し」とは別の見せ方にする（裁定 `c653606` §1・`872f409`）
- ★コードそのものは**画面に出さない**（利用者に見せるのは言葉だけ）。★開発側の報告と記録には残す

## 4. 生産中の経過時間 — ✅ `created_at` を足す

- デザイナーの `wait:long`（60 秒以上）の画面は、**いつ依頼したか**が分からないと出せない。★画面の中だけで測ると、再読み込み・別のタブ・再ログインで測り直しになる（導入の永続状態の裁定 `8a32840` と同じ考え方）
- **決定**: 読む口（`my_initial_breeding`・命名の要求を読む口）に `created_at` を足す小さな移行を作る。★**経過の秒数は SQL で計算しない**（`0067` と同じ作法・時間の計算は画面が `packages/scheduler` で行う）

## 5. 「名前はあとから変えられません」 — ⚠️ 条件つき

- ✔ 事実: 利用者が自分で改名する経路は無い（`0065`・命名は下書きに 1 回だけ）。★運営が仮の名前に戻す道具は在る（`5257a14`）が、★**規約 T-6（運営が改名を求め・仮の名前に戻せること）は弁護士ゲート待ち**
- **決定**: ★**「ご自身では変更できません」と書く**（事実）。★**「永久に変わりません」「絶対に変更されません」とは書かない**（運営が戻す場合が在り、規約もまだ無い）
- ★規約 T-6 が決まったら、「規則に反する名前は運営が変更することがあります」を足す（そのときデザイナーに再依頼）

## 6. 実装に入る前の順番

1. 依頼文の直し（毛色・第 2 便の再送はオーナーが済ませた）と、対応表（§3）をデザイナーへ
2. `created_at` の移行（§4）
3. 画面の実装は、**デザイナーの確認事項がすべて閉じてから**（★開発側は「実装に入らず待つ」と言っている。★正しい）

## 7. 追記（2026-09-23）— 対応（`0073` と `DESIGN_ANSWERS_INTRO_V1_20260923.md`）✅

- ✔ `0073`: `my_initial_breeding` と `my_foal_request` に `created_at`。★返り値の型を変えるので `drop` → `create` にした。★**`drop` で権限が消えるが、両方とも作り直した後に `revoke all … from public, anon` ＋ `grant execute … to authenticated` を書き直している**（`:42-43`・`:65-66`）→ 権限の抜けは無い
- ✔ 経過の秒数を SQL で計算していない。staging に適用（36 件合格・前回 37 件との差は `world_state` が 0 行だった判定が 1 つ減ったため）。★本番は未適用（★次に本番へ移行を当てる便で `0073` が乗る）
- ✔ デザイナーへの回答 `DESIGN_ANSWERS_INTRO_V1_20260923.md` は、§2〜§5 の決めどおり（毛色は誤りと明記・理由は 4 つの見せ方の表・コードは画面に出さない・第 2 便の 2 つは別の見せ方・「ご自身では変更できません」まで）
- ⚠️ ★文書の末尾に「**開発側の控え（デザイナーには渡しません）**」の節がある。★オーナーが貼るのは**その手前まで**。★渡す文面には、控えの節を含めないこと（レビュー側からオーナーへ明記して渡す）
