# 設計案 — 「誰も見ていない期間」を作らない仕掛け（`O-2` / `O-4` / `F-3` / 版の列）

> 2026-09-20・開発側。**案です。実装していません。**
> 🔴 **`O-2` と版の列はワーカーに手を入れる**ので、**配備と同じ便**が筋です（二度 手を入れない）。

## 0. 4 件は、同じ 1 つの形です

今日、同じ形の穴を **4 つ** 見つけました。

| # | 項目 | 何が無いか |
|---|---|---|
| `O-2` | ワーカーの死活監視 | **仕組みごと無い**（`heartbeat` / `liveness` が 0 件） |
| `O-4` | 障害時の全額返還 | 仕組みは在る。**いつ確かめたかが無い** |
| `F-3` | 結果の事後差し替え | 再計算はできる。**走らせた記録が無い**／**版の記録も無い** |
| `O-6` | 稼働中のビルド識別子 | ✅ **既に在る**（2026-09-20 に作った） |

```
★共通の形 … ① 起きたことを記録する  ② その記録が古びたら落ちる
```

🔴 **`O-6` で既に作りました。** それを**増やすのではなく、揃えます**（**D-052**）。

```
tools/verify-deployed-build.mjs --record  → evidence/prod-build/last-check.json
apps/cli/test/prod-build-freshness.test.ts → 7 日 を過ぎたら落ちる
```

**この 2 つの形を、あと 3 つに当てるだけです。**

---

## 1. 共通の部品（先にこれを作る）

```
tools/lib/staleness.mjs      ★記録を書く／読む／古さを測る（★純粋）
apps/cli/test/staleness.test.ts  ★4 件をまとめて見張る 1 つの検査
evidence/<名前>/last-check.json  ★記録の置き場（★形は揃える）
```

**記録の形**（4 件で共通）:

```json
{ "checkedAt": "ISO", "what": "prod-build|liveness|refund|recompute",
  "env": "production|staging", "ok": true, "detail": { } }
```

- 🔴 **`ok: false` でも記録する。** 「見に行った」ことが記録の中身で、「通った」ことではありません
  （`--record` で既にそうしています。**食い違いを隠すために記録を止めさせない**）
- 🔴 **`reviewBy` は持たせません。** 古さは**検査が測ります**（記録に期限を書くと、
  書いた人が伸ばせてしまいます）

---

## 2. `O-2` — ワーカーの死活

### 閾値は発明しません。**サイクルから導きます**

```
1 周 = CYCLE_MS（D-007: 6 分）
→ ★2 周 続けて記録が無ければ異常（★1 周の欠けは処理の揺れでありうる）
→ ★閾値 = CYCLE_MS × 2 … ★**数字を書かない**（D-118 の告知と同じ作法）
```

⚠️ **ここは実時間ではなくサイクル**です（`F-3` と逆）。測る対象が**ワーカーの周期**だからです。

### 3 つとも要ります

```
(a) ★ワーカーが毎周 1 行 書く          … ★worker に手を入れる（★配備と同じ便）
(b) ★VPS に入らずに読める場所へ出す    … ★/api/healthz が既にその役
(c) ★古ければ落ちる                    … ★(1) の共通部品
```

🔴 **(b) は「人が設定する環境変数を挟まない」**こと。
`NEXT_PUBLIC_BUILD_STAMP` が**一度も動かなかった**のと同じ轍を踏みます
（`healthz` の註記に既に書いてあります）。

⚠️ 🔴 **外形監視（URL を叩く）を (a)(b) の代わりにしないこと。**
**2026-09-20 に実際そうでした — 画面 ✅ ／ ワーカー 停止。**

### どこに書くか

- **案 ①**: `daily_run_log` に相乗り → ⚠️ **日次の表に毎周 書くのは意味が違う**（D-052）
- **案 ②（推す）**: `worker_heartbeat`（1 行だけの表・`singleton` 主キー・`upsert`）
  → **行が増えない。読むのも 1 行**

---

## 3. `O-4` — 全額返還の確認

`tools/verify-cancel.mjs` は既に **6 項目**で確かめます（「③ EP が全額戻った」を含む）。**良い検査です。**

🔴 足りないのは **「いつ通ったか」**だけ。

```
verify-cancel.mjs に --record を足す → evidence/refund/last-check.json
★共通の検査が、古ければ落とす
```

⚠️ **N は決めません**（材料がありません）。
⚠️ この道具は **DB の状態を変える**ので、**staging でしか流せません**。
→ **「本番で確かめた」とは永久に言えない**、と書き残すこと（`R-21`）。

---

## 4. `F-3` — 再計算と、**エンジンの版**

### 版の列（裁定: 案①）

```sql
alter table races add column if not exists engine_version text;
```

| # | 条件 | なぜ |
|---|---|---|
| ① | **確定の瞬間に書く** | 後から埋めると、埋めた人の版になる |
| ② | **`/api/healthz` と同じ値** | 人が設定する環境変数を挟まない（`O-6` の教訓・**D-052**) |
| ③ | 🔴 **版が空のレースは「版不明」として数える** | **「一致」にも「不一致」にも混ぜない** |

🔴 **③ が要点です。** 今日の `22.58%` は、**版の違うものを引き算して 9 時間 追いかけました**。

### 照合の箱は **5 つ**

```
① 一致  ② 食い違い  ③ 照合できない（入力欠け）  ④ 対象外（凍結なし）  ⑤ ★版不明
```
⚠️ `tools/verify-race-recompute.mjs` は既に ①〜④ を分けています。**⑤ を足すだけ**です。

### 頻度は **1 日 1 回**（裁定済み）

```
費用 … 6,141 本 で 約 1 秒（★10 倍 遅くても 10 秒）
露出 … 1 日 なら 240 本 が未検査
→ ★費用の側に、N を延ばす理由が 1 つもない
```
🔴 **対で: 走った記録 ＋ 2 日 走っていなければ落ちる**（走らない再計算は 0 の証明）。

---

## 5. 🔴 この設計が抱える弱点（先に書きます）

- **記録はリポジトリのファイルです。** 手で書き換えられます。
  → ただし **`F-3` の本体は再計算**で、記録は「走ったか」だけを持ちます。
  **記録を偽っても、次に走らせれば分かります。**
- **`O-2` の (a) は本番に配備されるまで動きません。** 配備までの間は、いまと同じく**無検査**です。
  → **手順書の ⑦（1 周 回ることを確かめる）が、その間の代わり**です。
- **4 件を 1 つの検査にまとめると、1 件の失敗で 4 件とも赤に見えます。**
  → **項目ごとに分けて出す**こと（`verify-open-findings` の表示と同じ作法）。

## 6. 順（★実装はまだしません）

```
1 ★共通部品（tools/lib/staleness.mjs）… ★配備が要らない。★先に作れる
2 ★O-4 の --record                    … ★同上
3 ★F-3 の ⑤「版不明」の箱              … ★同上
4 ★版の列 ＋ O-2 の心拍                … 🔴 ★**worker と移行。★配備と同じ便**
```

⚠️ **1〜3 は、許可を待たずに作れます。** 指示があれば進めます。
