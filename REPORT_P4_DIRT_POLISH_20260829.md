# 報告 — ダート演出の磨き上げ 4 件（走り方・汚れ・内側の帯・馬場状態）

作成: 2026-08-29 / ブランチ `p4/race-30sec-cuts`
起点: `0ecdd94` → HEAD `f0e1098`（**4 コミット**）
受けた指示: オーナー「早く、ダートの演出を磨き上げたい」／「あとはダートの時の走り方が芝コースと違って見えます」

> ★`Date.now()` / `Math.random()` を使っていません。着順・タイム・着差・払戻に触れていません（憲法 3/4）。
> ⚠️ ★`apps/web/package.json`（ポート 3210）はオーナーの既存作業。★混ぜていません。

---

## ⚠️ ★訂正（2026-08-29・提出後に判明）

★**§1 の「真因は地面の明るさだった」は誤りです。**

★この報告を出したあと、オーナーが芝と並べて確認し「★**まだダートのほうが遅く感じる／ぎこちない。芝は正常**」。
→ ★**地面は原因ではありませんでした。** ★真因は ★**砂煙の位相が 8 段に量子化されていたこと**でした（§10・コミット `b27144f`）。
★直したところ「★**改善されました**」を得ています。

⚠️ ★**§1 で直したもの（地面のコントラスト 79% → 95%）は無駄ではありませんが、苦情の原因ではありませんでした。**
　★私は**オーナーの確認より先に「真因」と書きました**。★この報告の §1 見出しはその誤りの記録として残します。

---

## 0. この便の要点

| # | 内容 | 種別 |
|---|---|---|
| 1 | ⚠️ ★**「走り方が芝と違う」の原因だと考えて地面を直した — 誤りでした**（§10 が真因） | 実装 |
| 2 | ★**馬体・勝負服が砂で汚れる**（報告 §10-2 の残り） | 実装 |
| 3 | ★**内側の帯を芝にする（Q-3 の B 案）を既定に** | 決定 |
| 4 | ★**馬場状態が地面に出る**（良でも不良でも同じ地面だった） | 実装 |
| 5 | ★**濃さを 2 つ決めていない**（演出の裁定事項として [EYES] へ） | 提出 |
| 6 | ★**この便で踏んだ穴 4 つ**（うち 1 つは註記が警告していた穴そのもの） | ★自己申告 |

### ★数字の一覧

| | 実測 |
|---|---|
| ① 地面の相対コントラスト | 芝の **79% → 95%** |
| ② 汚れの行き渡り | きれい 3 頭 ／ はっきり汚れる 6 頭 ／ 平均 0.392 |
| ③ 内側の帯 | 画面の **14.92%** が変化 |
| ④ 馬場状態 | 不良で良の **92%** ／ 画面の **61.87%** が変化 |

`npm test` → **129 ファイル / 1257 件 PASS / EXIT=0**（★`git add` してから実行）
`npm run typecheck` → EXIT=0

---

## 1. 「走り方が芝と違う」— ⚠️ ★**ここで挙げた原因は誤りでした**（真因は §10）

★オーナー指摘は**3 回目**で、★**原因は毎回違いました**。

| 回 | 原因 | 直した便 |
|---|---|---|
| 1 回目 | ★正面の地面タイル（整地の筋 0.385m がちらつき） | 2026-08-29 前便 |
| 2 回目 | ★横の板（輝度を 0.78 倍に圧縮していた） | 2026-08-29 前便 |
| ★3 回目 | ★**地面の明るさ**（＝相対コントラスト） | ★この便 |

### 1-1. ★先に容疑を 3 つ潰しました

★オーナーの答えは「★**場面によらず、レース全体でそう見える**」でした。
→ ★場面ごとにばらつく量は原因になりえません。

| 測ったもの | 結果 | 判定 |
|---|---|---|
| ★馬の動きが馬場で変わる経路 | ★**0 件**。コマ選びに `surface` は入らない。エンジン差は `SPEED_SURFACE_MULT` の **2.5%** だけで、歩調は 2.0〜2.6Hz に丸められて吸収される | ★シロ |
| ★接地影と地面のコントラスト | 芝 **26.3%** / ダート **27.4%** | ★シロ（一致） |
| ★砂が脚を隠す量（`tools/_gaitocclude.mjs`） | 平均 **6.7%**（砂被り前 2.8%）。★ただし場面ごとに 3.8〜12.4% と**ばらつく** | ★シロ（全体で一様ではない） |

### 1-2. ★前便の検定は、この実害を一件も見つけられませんでした（R-22）

★前便は**タイル単体の絶対 sd** を測り、芝 4.24 / ダート 4.08 で「一致」と判定しました。

⚠️ ★**しかし画面に出るのはタイル単体ではありません。**

1. ★画面に出るのは **タイル ＋ 上塗り**（`SHADE` / `TRACK_TINT`）の合成です
2. ★**見た目のコントラストは絶対値ではなく「地面の明るさに対する比」**です（Weber）

★ダートの地面は芝より一様に約 20 明るいので、★**同じ絶対 sd でも薄く見えます。**

★**これは正典 R-22 そのもの**です — 「★検定が『異常なし』と言ったら、その検定が異常のあるはずの場所を見ているかを先に確認する。」

### 1-3. ★実画面での実測（`tools/_groundcontrast.mjs`・seed 42）

★描き上がった PNG を測ります。★タイルではありません。

```
場面                    地面の明るさ      相対コントラスト
                        芝 → ダート       芝 → ダート

（直す前）
14s first-corner-front   93 → 113          3.59% →  1.91%   （53%）
20s side-drive          103 → 118         17.67% → 14.56%   （82%）
26s straight-contest    100 → 116         18.17% → 14.48%   （80%）
32s homestretch-front    86 → 113          3.80% →  1.77%   （47%）
38s straight-contest     94 → 114         17.79% → 14.76%   （83%）
44s finish-line         104 → 119         17.50% → 14.30%   （82%）
★平均                                     13.09% → 10.30%   （★芝の 79%）

（直したあと）
14s first-corner-front   93 →  98          3.59% →  2.35%   （65%）
20s side-drive          103 → 104         17.67% → 17.56%   ★一致
26s straight-contest    100 → 103         18.17% → 17.40%   ★一致
32s homestretch-front    86 → 100          3.80% →  2.32%   （61%）
38s straight-contest     94 → 102         17.79% → 17.52%   ★一致
44s finish-line         104 → 105         17.50% → 17.28%   ★一致
★平均                                     13.09% → 12.41%   （★芝の 95%）
```

### 1-4. ★なぜ「全体を暗く掛け算」ではないのか

⚠️ ★**RGB を一律 k 倍しても、平均も sd も同じだけ縮むので比は 1 ミリも動きません。**
⚠️ ★しかも上塗り（`TRACK_TINT`）は**掛け算の外にある加算項**なので、
　★掛け算だけすると**比はかえって下がります**（試算 10.4% → **8.5%**）。

→ ★**幅を保ったまま平均を下げる。** 役割を 1 段暗い側へ送り、明るい側は据え置いて**幅はむしろ広げます**。

| | 前 | 後 |
|---|---|---|
| 地面タイル | `dirt-2` → `dirt-1` → `dirt-0` | ★`dirt-3` → `dirt-2` → `dirt-0` |
| 横の板 | `dirt-3` → `dirt-1` → ★`#d2b896`（**16 進の直書き**） | ★`dirt-3` → `dirt-2` → `dirt-0` |

★役割名の幅 64 → **89**。
⚠️ ★`#d2b896` は `palette.json` から役割名で引く規則（裁定 §6-4 条件 3）の**例外**になっていました。併せて戻しました。

### 1-5. ★正面のカットは「直った」と書きません — **半分**です

★65% / 61% で止まっています。★原因は特定済みです:

⚠️ ★**`drawMowStripes`（芝の縞刈り）は馬場を一切見ていません。**
　★`mowStripes: false` を渡す製品側の呼び出しは **0 件**です（`broadcast-v2-scene.ts:606`）。
　★色は `#eaffd0` / `#0d2408` の**緑**で、★**ダートの上に芝の縞が乗っています。**

★正面のカットでは遠近圧縮でタイルの細かい模様が潰れるため、★**この緑の縞が主役になります。**
→ ★残件 **A-5** に立てました。★直すとダートの見た目が変わるので、演出の判断としてオーナーへ回します。

---

## 2. 馬体・勝負服が砂で汚れる

★砂被り（`d84841a`）で「後続が前の馬の砂を被る」ところまで来ていましたが、
★**被っても馬はきれいなまま**でした。

### 2-1. ★量は描画層で作りません（D-072 と同じ形）

★新設 `packages/render/src/dust-exposure.ts` が 1 か所で出します。★描画層は**受け取った数を塗るだけ**です。
⚠️ ★スタミナゲージを画面側で作ったとき、★**符号が逆**になった実例があります。

| 決めたこと | なぜ |
|---|---|
| ★**溜まる** | ★最後方から差してきた馬は、先頭に立った瞬間にきれいになったりしません |
| ★**前の馬は汚れない** | ★砂は後ろへ流れます |
| ★**描画の fps では積まない** | ⚠️ ★毎コマ前の値に足すと**コマ落ちで結果が変わり**、同じシードで同じ絵になりません（憲法 4） |
| ★**尾の長さの出どころは 1 か所** | ★絵に描かれていない砂で汚れたら、見ている人には理由が分かりません（R-30） |

### 2-2. ★`filter` を使わなかった理由（R-30 / R-31）

⚠️ ★`Ctx2D` の `filter` は**任意**です。★無い環境（監査道具）では効かないので、
　★**測定器が画面と違うものを見る**ことになります。★再発 9 件と同じ形です。
→ ★`ellipse` と `createRadialGradient` だけで塗ります。★どちらも既に使っている手です。
★併せて `shot-race-at.mjs` にも同じ量を渡しました（★渡さないと**道具だけ馬がきれいなまま**）。

### 2-3. ★実レースでの行き渡り（`tools/_soil.mjs`・seed 42）

★**着順ではなく「誰の砂の中を走ったか」**で決まります。

```
着  枠   平均で先頭の何m後ろ   汚れ
 1  10          15.3m        0.691   ← ★差して勝った馬は汚れている
 2   4           5.4m        0.282
 3   5          12.7m        1.000
 4  12           5.6m        0.009   ← ★近くても外のきれいな空気なら汚れない
 9   9          18.5m        0.821
12   1          23.4m        0.305

★きれいな馬（0.05 未満）3 頭 ／ はっきり汚れる馬（0.3 以上）6 頭 ／ 平均 0.392
```

### 2-4. ★刻みは測ってから決めました（`tools/_soilstep.mjs`・seed 42/253/90）

⚠️ ★最初 0.1 秒で書いたら、表を作るのに **119ms** かかりました。
　★過去に「映像は成立したが**初期化 +31%**」で本線に入れなかった案があるので、測りました。

```
刻み    費用          0.02 秒刻みとの最大の差
0.1     110〜161ms    0.0045
0.3     ★37〜54ms    ★0.0150   ← 採用（実測 26.8ms・下ごしらえ比 54% → 16%）
0.5      23〜31ms     0.0554
```

★粒の数は `round(12 × soil)` なので、誤差 0.015 で数が変わるのは**最大 0.18 個**＝★**絵は実質変わりません。**

---

## 3. 内側の帯を芝にする（Q-3 の B 案）を既定に

★裁定 §6-3 の懸念はそのとおりで、★ダートにすると**走路と内側の帯が同じ褐色に溶けて、どこを走っているか読めません。**

### 3-1. ★正典 D-085 の作法

★**既定を変えるときは戻せる口を同時に置き、戻せること自体を検査で固定する。**

| | |
|---|---|
| 戻し口 | ★`/race?infield=dirt`（`infieldReversedFromSearch`） |
| 検査 | ★`packages/render/test/infield-reversed.test.ts`（4 件） |

### 3-2. ★既定は「呼ばれる側」に置きました（R-31）

⚠️ ★これまでは**画面が `search.includes('infield=turf')` を、道具が `--infield-reversed` を、それぞれ持っていました。**
→ ★`INFIELD_REVERSED_DEFAULT` を 1 か所に置き、画面も道具もそこを読みます。
⚠️ ★R-31 の再発 9 件は、★**すべて既定が「呼ぶ側」にあった**ために起きています。

### 3-3. ★検査は「戻せること」だけでは足りません（R-16）

⚠️ ★既定と戻し口だけを見ると、★**機構が最初から効いていなくても緑**になります。
→ ★`drawInfield` を実際に 2 通り呼び、★**塗る色の列が変わること**まで見ます。空回り防止も入れました。

★実画面での確認: ★**画面の 14.92% が変化**（平均差 41.8・最大 51）。

⚠️ ★**走路の幾何には触れていません**（裁定 §6-3）。★帯の色だけです。
　★内回りコース案は却下のままです。

---

## 4. 馬場状態が地面に出る

### 4-1. ★これは発明ではなく、繋ぎ忘れの修理です

⚠️ ★`trackSurfacePaletteRole`（良→`dirt-0` … 不良→`dirt-3`）は**既に決まっていて、検査まで**ありました。
⚠️ ★しかし ★**画面が使う経路（`world-textured.ts`）は `condition` を一度も見ていません**でした。
　★あの対応表を読んでいたのは、★**使われていない旧経路**（`drawPerspectiveWorld`）だけです。
→ ★画面では**良でも不良でも地面が同じ**。変わるのは蹴り上げの量だけでした。

★馬場状態は **25% の確率で良以外**になります（`TRACK_CONDITION_CDF` の良が 0.75）。

### 4-2. ★これも R-22 の形です

★「対応表がある」「その検査もある」＝「画面に出ている」ではありません。
★検査は**役割名を返す関数**を見ていて、★**それを誰が呼ぶか**を見ていませんでした。

### 4-3. ★良は 1 ビットも動かしていません

★`trackWetnessFactor('good')` は必ず 1 を返し、`scaleHex` は `k >= 1` で**元の文字列をそのまま返します。**
★実画面で確認: ★**この変更の前後で 0 画素差**（seed 42・26s・ダート・良）。

### 4-4. ★実測（`tools/_condcmp.mjs`・seed 42・32s `homestretch-front`）

```
良       99.5（100%）
稍重     96.9（ 97%）
重       94.5（ 95%）
不良     91.9（ 92%）

★良 vs 不良で画面の 61.87% が変化（平均差 9.6・最大 37）
```

⚠️ ★併せて `shot-race-at.mjs` の `condition: 'good'` の**直書き**を直しました（`--condition`）。
　★道具が良しか撮れず、★**濡れた馬場の絵をオーナーに出せませんでした**（R-31 の家族）。

---

## ★5. 決めていないこと（[EYES]）

⚠️ ★**濃さを 2 つ、開発側で決めていません。** ★どちらも過去に外している箇所です。

| # | 何を | 実測 | なぜ決めないか |
|---|---|---|---|
| ① | ★**汚れの濃さ** | 入/切の画素差 引き 607 画素・最大 36 ／ 寄り 2304 画素・最大 17 | ★砂の色 `#cdb494` が馬体の色に近く、★**寄りのほうが薄く出ます**。★砂煙で「見えない」→「ポンポンして砂煙っぽくない」と**2 回外している**箇所です |
| ② | ★**濡れた馬場の濃さ** | 不良で良の **92%** | ★控えめです。★上塗り 1 枚だけを暗くしているため |

★見る URL:

```powershell
cd apps/web ; npm run dev
http://localhost:3210/race?seed=42&surface=dirt                # ★①②③ が全部入った絵
http://localhost:3210/race?seed=42&surface=dirt&infield=dirt   # ★③ を戻して比べる
http://localhost:3210/race?seed=42                             # ★芝（変えていません）
```

⚠️ ★**強制再読み込みしてください**（`ASSET_VERSION` は 57 → **58**）。

---

## ★6. この便で踏んだ穴 4 つ

### 6-1. ★註記が警告していた穴を、そのまま踏みました（R-27）

★`shot-race-at.mjs` に `--no-soil` を足したとき、★`VALUELESS` への登録を忘れ、
★**次の引数（秒数 44）を食われました**。★指定した秒が撮れず、既定の 6 コマが出ました。

⚠️ ★その 2 行上に、こう書いてありました:

> ★値を取らないオプション。★ここへ入れ忘れると★**次の秒数が食われます**

→ ★**註記は読まれなければ効きません。** ★フラグを足したら、その場で登録簿を見ること。

### 6-2. ★閾値を式から出さず、頭で見積もって書きました

★検査に「2m 後方 40 秒なら 0.5 以上」と書いて落ちました。★正しくは **0.395** です
（`near = (1 - 2/6)^2 = 0.44` を 40 秒積んで満量 45 秒で割る）。
→ ★**閾値は式から引く。** 近さの感覚で書かない。

### 6-3. ★検査の主張文が、筋書きと合っていませんでした

★「1 番はきれいなまま」と書いて落ちました。★筋書きは 40 秒で**前後が入れ替わる**ので、
★**1 番が汚れるのが正しい**振る舞いです。★**検査のほうが間違っていました。**

⚠️ ★このとき一瞬「実装が壊れている」と読みかけました。★実装は正しく、検査が嘘をついていました。

### 6-4. ★境界そのものを見て落としました

★入れ替わりの瞬間（t=40）で「0 のはず」と書きましたが、★積分の刻みは 0.3 秒なので
★**入れ替わったあとの 1 刻みが既に入っています。** → ★境界の**手前**を見るように直しました。

### ★6-5. 併せて: 測定器のほうが画面より平たく見ていました

⚠️ ★`_dustcover.mjs` は砂粒を**実画面より 1.5〜1.9 倍平たく**記録していました。
　★実画面の `paintDustPuff` は濃淡が使えるとき `ellipse(cx, cy, rx, **rx**)` ＝ **真円**を描きますが、
　★あの道具は `createRadialGradient` を潰して均一塗りの枝を通すので `rx × ry`（ry = rx × 0.52〜0.68）です。
★R-30 の家族です。★この便の測定器（`_gaitocclude.mjs`）は**濃淡の枝を通して**測っています。

---

## 7. 増えた検査（★次に触るとき落ちたら、これを読むこと）

| 検査 | 何を守っているか |
|---|---|
| `packages/render/test/dust-exposure.test.ts`（12 件） | ★前の馬は汚れない／溜まる／★**刻みを変えても結果が変わらない**（＝コマ落ちで絵が変わらない）／芝では 1 粒も塗らない／★**戻せる、かつ渡すと実際に画が変わる** |
| `packages/render/test/infield-reversed.test.ts`（4 件） | ★既定が芝／`?infield=dirt` で戻せる／★**戻すと実際に色の列が変わる** |
| `packages/render/test/track-wetness.test.ts`（4 件） | ★良は必ず 1（絵を動かさない）／悪くなるほど暗い／★既にある 2 つの対応表と向きが揃っている |

---

## 8. 再実行（★すべて読取専用）

```powershell
# ★地面のコントラスト（★実画面の PNG を測る）
npx tsx tools/shot-race-at.mjs --seed 42 14 20 26 32 38 44          # 芝
npx tsx tools/shot-race-at.mjs --seed 42 --surface dirt 14 20 26 32 38 44
npx tsx tools/_groundcontrast.mjs                                   # 芝の 95%

# ★汚れ
npx tsx tools/_soil.mjs                                             # きれい 3 頭 / 汚れ 6 頭
npx tsx tools/_soilstep.mjs                                         # 刻みの費用と誤差
npx tsx tools/_soilcost.mjs                                         # 26.8ms
npx tsx tools/_gaitocclude.mjs                                      # 脚の覆い 6.7%

# ★馬場状態
npx tsx tools/shot-race-at.mjs --seed 42 --surface dirt --condition bad 32
npx tsx tools/_condcmp.mjs                                          # 不良で良の 92%

# ★全体
npm test > log 2>&1 ; echo "EXIT=$?"                                # 129 ファイル / 1257 件 / EXIT=0
npm run typecheck
```

⚠️ ★`tools/_*.mjs` は `.gitignore` 対象・**追跡外**です。★ただし**登録簿には載せます**
（`tools/lib/classification.mjs`）。★この便で 6 本足し、6 本とも登録済みです。

---

## ★10. ★その後 — 真因は砂煙だった（コミット `b27144f`）

★この報告を出したあと、オーナーが**芝と並べて**確認しました。
★評「★**やはりダートのほうが違和感です。遅く感じます。ぎこちなく感じます。芝コースは正常です**」。

### 10-1. ★順に潰しました（★地面はすべてシロ）

| 測ったもの | ダート / 芝 | 判定 |
|---|---|---|
| ★地面の流れ（px/s）`_groundflow.mjs` | **97〜98%** | シロ |
| ★地面のコントラスト `_groundcontrast.mjs` | 95%（§1 で戻した） | シロ |
| ★コマ間の画素の変化 `_interframe.mjs` | **96%** | シロ |
| ★馬の動き | 完全に同一 | シロ |

### 10-2. ★犯人 — 砂煙の位相が 8 段に量子化されていた

★実測（`tools/_duststep.mjs`・seed 42・1/30 秒刻み）:

```
馬の進み        砂粒の動き
2.510 m         25.63 px
2.511 m         33.10 px
2.512 m          8.42 px
2.517 m         75.48 px
2.518 m         80.48 px
2.520 m         12.73 px
   ★馬は毎コマ 2.51m ずつ滑らか。★砂粒は 23 コマ中 16 コマが 20px 超の跳び。
```

★原因は位相を `frameIndex`（＝**脚のコマ・8 段階**）から取っていたこと:

```ts
const phase = ((frameIndex % 8) + 8) % 8 / 8;
```

★脚の位相は 1 コマで **2.87 バケット**進むので、位相が 2/8・3/8 と不揃いに跳んで巻き戻ります。

⚠️ ★**註記は嘘をついていました。**

> 位相はコマ番号（＝進んだ距離）から決まり、★**途切れずに続きます**

★8 段に量子化されているので、★**続いていませんでした。**

### 10-3. ★なぜダートだけだったのか

★砂煙は画面で**最も目立つ動く要素**で、★**芝には実質ありません**（蹴り上げ強度 0.07・5 コマだけ）。
→ ★ダートだけぎこちなく、★**馬と一緒に動く大きな塊**が流れないので遅く感じます。

### 10-4. ★直し方は導出しました（推測ではありません）

★砂は本来**空中に置き去りにされる**もの。粒の世界位置 `s - back` が一定になる条件から:

```
back = 0.55 + age × PLUME_M,  age = (i + phase) / PUFFS
d(back)/d(s) = 1  ⟹  d(phase)/d(s) = PUFFS / PLUME_M
⟹  phase = (s × PUFFS / PLUME_M) の小数部
```

★実測: 1 コマあたりの位相の動き（中央値）**12.88px → 3.95px**。★暴れが消えました。
★オーナー確認: ★**「改善されました」**。

### 10-5. ⚠️ ★測定器がずっと芝のレースを測っていた

★`tools/lib/race-audit-build.mjs` が **`surface: 'turf'` を直書き**していました。
→ ★`_groundflow` も `_soil` も `_gaitocclude` も、★**ダートについて聞いているのに芝を測っていました。**
★`SPEED_SURFACE_MULT` が turf 1.0 / dirt 0.975 なので、位置も速さも別物です。
⚠️ ★これは**渡し忘れではなく、渡せませんでした**（R-31 の家族で、より悪い形）。

### 10-6. ⚠️ ★`_groundcontrast.mjs` の軸の誤り

★行方向（画面の縦）で測っていました。★**横視点のカットでは地面は横に流れます。**
★§1-3 の数字は**正面のカットについては正しく**、★**横のカットについては流れの軸を測っていません。**

---

## ★9. 申し送り

1. ★**「一致した」と書く前に、その量が画面に出る量かを問う。**（§1-2）
   ★タイルの sd は一致していました。★画面に出るのは**合成後の、明るさに対する比**でした。★R-22 です
2. ★**フラグを足したら、その場で登録簿を見る。**（§6-1）★註記が警告していた穴をそのまま踏みました
3. ★**閾値は式から引く。**（§6-2）★近さの感覚で書くと落ちます
4. ★**検査が落ちたら、実装ではなく検査の主張文を先に疑う。**（§6-3）★3 件中 2 件は検査が嘘でした
5. ★**既定は「呼ばれる側」に置く。**（§3-2）★呼ぶ側に置くと、道具ごとに書き足しが要ります
6. ★**「対応表がある」＝「画面に出ている」ではない。**（§4-2）★誰が呼ぶかを見ること
7. ⚠️ ★**オーナーが画面で確認する前に「真因」と書かない。**（§10）
   ★私は §1 で「真因を特定して直した」と書き、★**実際には原因ではありませんでした。**
   ★映像の合否はオーナーの目だけが決めます。★測って言えるのは「★**この量は芝並みに戻した**」までです
8. ★**同じ苦情が続いたとき、前回直した所が原因とは限らない。**（§10-1）
   ★潰した容疑を**数字で並べてから**次を探すこと。★今回は 4 つ並べて 5 つ目に当たりました
