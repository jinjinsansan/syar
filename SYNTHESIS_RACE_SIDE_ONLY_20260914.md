# 3 者のまとめ — レース演出を「真横の直線だけ」にするとき

**案件**: STAR（ポイ活型オンライン競馬育成）
**日付**: 2026-09-14（夜・再照会の回答を反映）
**まとめる側**: 開発側（相談書 `CONSULT_RACE_SIDE_ONLY_20260914.md` §8-2）

**材料**
- ① レビュー側
  - `REVIEW_CONSULT_RACE_SIDE_ONLY_ANSWER_20260914.md`
  - 再照会への回答 `REVIEW_CONSULT_RACE_SIDE_ONLY_FOLLOWUP_ANSWER_20260914.md`（再照会は `CONSULT_RACE_SIDE_ONLY_FOLLOWUP_20260914.md`）
- ② デザイナー側: Claude Design「STAR レース中継 HUD」`components/race-side-only-storyboard/index.html`（D-1〜D-8）
- ③ 開発側: 相談書 §5

**ブランチ・HEAD**: `p4/race-30sec-cuts` ／ `319bae0`

> 凡例: **✔** ＝ 開発側がコードで確かめた ／ **✗** ＝ コードと食い違う ／ **△** ＝ 未確認 ／ **[EYES]** ＝ オーナーが画面で決める
> 問いの番号は、正典の作業規律 R-1〜R-33 と重ならないように付け直しています。レビュー側への問いは Q-R1〜Q-R9、デザイナー側への問いは Q-D1〜Q-D8 です。
> 秒数は、特に書いていないものは較正値からの概算で、画面での実測ではありません。
> ⚠️ オーナー判断の番号（O-1〜O-9）は、このまとめの番号です。レビュー側の引継ぎ書 §3-3 の「判断待ち 8 件」とは、番号の対応を照合していません（引継ぎ書はレビュー側のディレクトリにあるため）。

---

## 0. 結論（開発側のまとめ案）

1. **3 者とも B（コーナーの区間を時計から取り除く）です。** ただしレビュー側は条件付きです。
   - B は**観戦だけの人の見せ方**として採る
   - 自馬で介入する人には、**残り 900m からゴールまでを飛ばさない**（§8b の介入の局面が、飛ばす区間に入っているため）
   - 今の `/race` には介入の画面が無いので、**今は B で進め、この線を正典に先に書いておく**のが開発側の推奨です
2. **ゴールより前の画・音は、その時刻までの状態だけで決める**（再照会で確定）。確定着順・1 着の馬番・着差・展開の札は、ゴールの後だけ使えます。
   - **今の既定にも、この線を破っている部分が 1 つあります**: `FINISH_CAMERA_BY_DEVELOPMENT`（残り 260m から、1 着の型で先頭の置き場所を最大 256px 変える）
   - 次の指示書で、状態から決める形に置き換えます
   - 置き換えの前提は O-7（オーナーの確認）です
3. **ハードカットを増やさず、1 本のカメラが連続して寄る・引く**は、デザイナー側と開発側で一致しています。
   - ただし、枠取り（`frameContenders`）には、**過去に外した記録が 2 つあります**（§3）
   - 全区間に広げる前に、試作してオーナーの目で見る必要があります
4. **すぐ採れる小さい案**
   - カウントダウン
   - ゴール板が迫る画
   - 音の厚みと、ゴール直前の「溜め」
   - HUD を濃さで下げる
   - 跳びのつなぎを、下三分の一のテロップで見せる
   - 自馬の位置を、画面の端の矢印で示す
   - 接戦のときだけの写真判定の止め絵（判定は「止める絵そのもの」の差で行う）
5. **条件付きで採る案**
   - 対象: 勢い・順位の入れ替わり・先頭との差・展開の見出し
   - 条件
     - 描画と同じ入力から出す
     - 過去の窓から出す（未来の値を読まない）
     - 段階に丸める
6. **取り下げる案**: レース中の馬券の当たり具合（開発側の案。レビュー側が反対）。
7. **デザイナー側の回答には、コードと食い違う点が 5 つあります**（§3）。どれも、案そのものを捨てる理由にはなりません。

---

## 1. 3 者が一致していること

| 項目 | ① レビュー | ② デザイナー | ③ 開発 |
|---|---|---|---|
| A より B | B（D-062 に合う）・条件付き | B を前提に構成 | B を推奨 |
| ハードカットを増やさない | — | 勝ち馬の追従だけハードカット | 同じ |
| スローモーションを使わない | — | 止め絵 0.4 秒で代える | 同じ |
| 接戦でないレースに接戦を作らない | 守る（ただし `climax=on` と矛盾・O-4） | 独走は盛らない | 同じ |
| コース図のカットインは外す | — | 使わない | 外す |
| 勢いは「機能だけ」取り、見た目は独自 | 機能は可、意匠は不可 | 独自の見た目 | 同じ |
| 飽きるかどうかは数値で合否を決めない | [EYES]・数値は並べるだけ | — | C-6 |
| ゴールの前に、結果から画・音を決めない | 文字・画・音すべて不可（Q-R7） | D-8 は札から決める前提（→ 状態から決めれば可） | 同じ |

---

## 2. オーナーに決めていただくこと

> **2026-09-14 夜・オーナー判断（原文）**
> 「O-1 見せ方を変えます　O-7 よい　いずれにしても　コーナーを全てカットです。ただしコース表ではコーナーを曲がるのは見せます　今回の３者の意見をまとめて　真横カメラワークの直線のみだが、ワクワクするレース演出に仕上げてください。」
>
> - **O-1 → 分ける**。介入する人は残り 900m → ゴールを飛ばさない。ただし、その間のコーナーも真横の画は出さず、コース図の画面で覆う
> - **O-7 → よい**（「追い込み馬がある時は引く」を状態として読む）
> - **コーナーは全部カット**。コース図の上では、馬群がコーナーを曲がるのを見せる
> - 他の項目（O-2〜O-6b・O-8・O-9）は「3 者の意見をまとめて仕上げる」の指示により、下の表の **③ 開発側の推奨**で進めた。実装の範囲と残りは `REPORT_RACE_SIDE_ONLY_20260914.md` に記録する

| # | 決めること | ① レビュー | ② デザイナー | ③ 開発側の推奨 |
|---|---|---|---|---|
| **O-1** 急ぎ | 観戦だけの人と、自馬で介入する人とで見せ方を分けるか（正典 D-097 候補） | 分ける。介入する人は残り 900m → ゴールを飛ばさない | 触れていない（B を全員に適用する前提） | **分ける。** 今は観戦の見せ方（B）だけを作り、介入する人の見せ方は介入の画面と同じ便で作る |
| **O-2** | 計画書 §1・§8 の「カット数は減らさない」を正式に取り消すか | 取り消す。「削って隠さない」の趣旨と、理由（出来が足りない）を記録する | — | **取り消す**（レビュー側と同じ） |
| **O-3** | 最後の直線を、残り何 m から見せるか | 250m 固定はコースで変わらないので、コースの形から引く。400m（「追う」の始まり）も並べる | 構成表の攻防は「残り 400→100m」と書いてある（B の今の 250m と合わない） | **[EYES]。** 250m と 400m を並べて見ていただく。400m にすると本編は約 40 秒・全体は約 52 秒（概算・250m で 16.4 秒からの比例） |
| **O-4** | 「接戦を作らない」と `?climax=on` の攻防の演出、どちらを優先するか | オーナー判断。**勢いのバーの定義（残り 200m から）はこの決定が前提**（再照会の回答 §2・§5-2。回答書では「まとめ O-3」と書かれているが、このまとめの O-4 のこと） | 接戦は実際の位置で 3〜5 頭を並べる（作らない側） | **「作らない」を優先し、`climax=on` は既定にしない**（今も既定は無効 ✔ `page.tsx:3558`）。**勢いのバーより先に決めていただく** |
| **O-5** | レース中に馬券の当たり外れを表示しないでよいか | 出さない | — | **出さない。開発側の案を取り下げる。** 買った馬の印・買い目の一覧・確定後の祝いだけにする |
| **O-6** | 勢いを、色を主役にして見せるか（正典 §12.1 の改訂） | 形・長さ・動きを主、色は補助 | バーの長さ＋**加速中の馬だけ金色に光る** | **長さのバーを主にする。** 金色の光は、彩度を抑えた補助にする。光を主役にするなら §12.1 の改訂 |
| **O-6b** | 跳びのテロップに「先頭とは 1.6 秒」と出すか | タイム差は結果の側の値なので出さない | モックに秒差を描いている | **秒では出さない。** 馬身に丸めるか、位置バーだけにする |
| **O-7** 急ぎ | 09-12 の指示「追い込み馬がある時は引く」を、**状態**（後ろから伸びてくる馬が、いま画面の後ろにいる時）として読んでよいか | よい（再照会の回答 §1-4） | — | **よい。** そう読めば、指示の言葉どおりに、結果を先に明かさない形で作れる |
| **O-8** | 未使用の支給音源（フィナーレ・馬が来る音 など）を STAR に取り込むか | — | 「馬が来る音」「フィナーレ」を使う | **取り込むなら、オーナーから STAR へ渡していただく。** 音源は STAR のリポジトリに無い（§3） |
| **O-9** | 実況の着差の言葉を、着順ボードの言葉に揃えるか | 揃える（再照会の回答 §3-2） | — | **揃える**（§3 の最後の行） |

---

## 3. コードと突き合わせた結果

### 3-1. デザイナー側の回答

| 回答の記述 | 照合 | 出どころ | 影響 |
|---|---|---|---|
| `frameContenders` は今 `side-drive` と `finish-line` だけに付いている | **✗** 付いているのは `homestretch-side` と `finish-line`。`side-drive` からは 2026-08-28 に外した | `broadcast-v2.ts:488・524`、`contender-frame.test.ts:154-161` | 「全区間に拡張」は、**過去に外した理由と正面から当たる**。① 発走直後の団子では数頭しか映らない（`broadcast-v2.ts:1787`）② 枠取りが乗っ取って、被写体が画面の端に付いた（`broadcast-v2.ts:859`）→ 試作して目で見る |
| `closeIn`・`fovRamp` を真横に転用できる | ✔ 実在する（正面のカット用） | `broadcast-v2.ts:56・102`、`broadcast-v2-scene.ts:486-510` | 真横の枝は通っていないので、**配線が要る**（小〜中） |
| 勝ち馬の追従は `hardCutIn: true` | ✔ | `broadcast-v2.ts:841` | — |
| `drawRaceTelopBand`・`drawFormationTelop`・`drawCourseMinimap` | ✔ 実在する。`drawFormationTelop` は既に `/race` から呼ばれている | `race-cutin.ts:755・850`、`minimap.ts:33`、`page.tsx:4183` | — |
| 跳びの前後を **0.4 秒ずつ**（`RACE_CUTIN_JUMP_LEAD_SEC`）覆う | **✗** 値は **0.8 秒** | `race-cutin.ts:84` | 覆う時間は前後 0.8 秒×2 ＝ 1.6 秒。構成表の 0.8 秒の「見立て」では足りない |
| `strategyLabel` は既存のデータ | △ その名前は厩舎画面のデータ。レースの側には脚質のテロップ `drawRunningStyleTelop` がある | `apps/web/src/lib/stable.ts:81`、`page.tsx:85` | 名前が違うだけで、脚質はレースの側でも出せる |
| `focusContest` で、競り合う場所へ注視点を移す | ✔ | `broadcast-v2.ts:161` | — |
| `paintCrowd` は実装済みで未使用 → 直線だけ描画 | **✗** 起動時に 1 回、**スタンドの素材へ焼き込んでいる**（使っている）。**相談書 §1-4 の開発側の記述「呼び出しが見つからない」も誤り** | `page.tsx:2227-2243` | 「直線だけ観客を出す」は、焼き込みを別の層に分ける**新しい作業**。起動時のメモリも増える（side-v9 の +31% と同じ種類のリスク） |
| 支給済み・未使用の「馬が来る音」「フィナーレ」 | **✗（STAR には無い）** `apps/web/public/audio/` にあるのは 5 本（fanfare・gate-open・whinny・gallop・crowd）。未使用の音源は sevendays 側のフォルダにある | `race-audio.ts:38-46`、`HANDOFF_P4_RACE_SOUND_AND_TOP_20260913.md:82-83` | O-8。開発側・レビュー側とも sevendays のファイルは開かない |
| 勢い＝エンジンの位置から求めた瞬間速度（前コマとの差分） | △ 条件付き。表示位置は、残り 1000→200m で脚質の隊列から寄せていく途中（`formation.ts:108-126`） | 再照会の回答 §2 | 残り 200m から出す。過去の窓から出す。段階に丸め、数字で出さない。描画と同じ入力から出す（R-30） |
| D-8 展開ごとに第 2 幕の構図・音を変える | **札（1 着の型）から決めると不可。状態から決めれば可**（Q-R7） | 再照会の回答 §1-3 | 「独走の孤独の構図」「後ろから低い唸り」は、その時刻の差・縮み方から決める |
| モバイル縦は自動で縮むので、別のレイアウトは不要 | △ 未計測 | — | 本番のプレビューで [EYES] |

### 3-2. 再照会で見つかったこと（レビュー側の指摘・開発側も照合）

| 項目 | 中身 | 出どころ |
|---|---|---|
| 結果から構図を決めている既定 | 残り 260m から、確定の 1 着の型で、画角 11.0°/11.5°/12.0° と先頭の置き場所 0.58/0.68/0.78 を変えている。置き場所の差は最大 0.20×1280px ＝ 256px。B では見せる直線の最初のコマから効く | `broadcast-v2.ts:2650-2657`、`page.tsx:1685・1701-1707` |
| 状態から決めている手本（残す） | 実況の「伸びてきた馬」（過去の窓で、先頭との差の縮み方） ／ 残り 80m の接戦判定（その瞬間の位置の差） | `race-call.ts:104-125`、`broadcast-v2.ts:2723-2731` |
| 註記の表が定数と合っていない | 註記は 11.5°・15°・19°／0.62・0.50・0.38。定数は上の行の値 | `broadcast-v2.ts:2621-2626` |
| 着差の言葉を決める場所が 2 つあり、境目が合っていない | 着順ボードは秒差（ハナ ≤0.03・アタマ ≤0.06・クビ ≤0.10・1/2 馬身 ≤0.16）、実況は馬身（ハナ <0.15・アタマ <0.4・クビ <0.8・半馬身 <1.2）。約 16 m/s で換算すると、ボードの境目は 0.20・0.40・0.67・1.07 馬身で、**食い違う帯が 3 つ** | ✔ `balance.ts:548-558`（`MARGIN_LABELS`）、`race.ts:86-91`（`marginLabel`）、`race-call.ts:137-143`（`marginWord`）。換算は計算で、ゴール付近の実際の速さでは未確認 |
| 呼び方の訂正 | 位置モデルは、エンジンが γ 写像を掛けた結果から作られている。開発側が「真の位置」と書いたものは「**寄せが終わった表示位置（γ 写像後・着順ボードと同じ系）**」 | 再照会の回答 §0（`race.ts:267`、`page.tsx:1554-1581`） |

---

## 4. 案ごとの比較表

作業の大きさ: **小** ＝ 既存の部品の配線・定数 ／ **中** ＝ 新しい関数・検査が要る ／ **大** ＝ サーバー・DB が絡む

| # | 案 | 効く要素 | 作業 | リスク・注意 | ① レビュー | ② デザイナー | ③ 開発 | 条件 | まとめ |
|---|---|---|---|---|---|---|---|---|---|
| 1 | **B**: コーナーを時計から取り除く | W-1・W-2 | 小（仕組みはある）＋テストの書き換えは中 | 跳び 1150m のつなぎの出来で全体が決まる | 賛（観戦のみ・計画書を改訂） | 賛 | 賛 | C-8 → O-2 | **採る** |
| 2 | 直線を残り 400m から・コースの形から引く | W-2・W-3 | 小〜中 | 尺が約 10 秒延びる | 並べよ | 構成表は 400m 基準 | — | O-3 | **[EYES] で比べる** |
| 3 | 1 本の連続したカメラ（枠取りを全区間へ） | W-1 | 中 | 過去に 2 回外している（§3-1） | — | 賛 | 賛 | C-4 | **試作して目で見る** |
| 4 | 跳びの「見立て」テロップ（世界は止めない・位置バー＋脚質） | W-1・W-3 | 小 | 秒差を出さない。覆いは 0.8 秒×2 | 条件（結果の値を出さない） | 賛 | 賛 | C-2 | **採る**（O-6b） |
| 5 | 残り距離のカウントダウン | W-2 | 小 | HUD が増える → #16 と一緒に | — | 賛（120px 級） | 賛 | C-7 | **採る** |
| 6 | ゴール板が迫る | W-2 | 小 | — | — | 賛 | 賛 | — | **採る** |
| 7 | 観客を直線だけに出す | W-1・W-2 | **中** | 焼き込みを分ける作業。起動時のメモリ | — | 賛（前提に誤り） | 賛（前提に誤り） | — | **見積もってから決める** |
| 8 | 周辺の減光・薄い集中線（残り 100m から） | W-2 | 小 | 強すぎると画が暗く、弱すぎると効かない | — | 賛 | 賛 | C-4 | **[EYES]** |
| 9 | 写真判定の止め絵 0.4 秒（接戦のときだけ） | W-2・W-3 | 小〜中 | **判定は (c) 先頭が決勝線を通過する瞬間の表示位置（止める絵そのもの）の 1〜2 着の差。** 着差の言葉を出すなら `marginLabel` から | (c) を推奨（Q-R9） | 賛 | (a) を推奨していた → **(c) に合わせる** | C-2・C-4・R-30 | **採る** |
| 10 | 勢いのバー（馬名プレートの下） | W-3 | 中 | **残り 200m から・過去の窓から。** 色を主役にするなら §12.1 | 条件（Q-R8）。`climax=on` を既定にしないことが前提 | 賛（金色の光） | 賛 | C-1・C-2・O-4 | **長さを主に採る**（O-4 → O-6 の順に決める） |
| 11 | 順位が入れ替わった瞬間の合図 | W-3 | 小〜中 | HUD の順位 ＝ 描画の順位 の検査が要る | 条件（R-30） | 賛 | 賛 | C-2 | **検査付きで採る** |
| 12 | 先頭との差（馬身） | W-3 | 小 | 丸めて出す | 条件 | （秒で描いた） | 賛 | C-2 | **馬身で採る** |
| 13 | 展開の見出し | W-3・W-5 | 小 | 札を使うのはゴールの後だけ。ゴールの前は状態から（例: 実況の「伸びてきた馬」と同じ形） | 条件（Q-R7） | 「独走」だけ | 賛 | C-2 | **ゴールの前は状態から、札はゴールの後だけ** |
| 14 | 音の厚み・溜め 0.15 秒・歓声の音量カーブ | W-1・W-2 | 小 | 既定は音が切（`?sound=1`）。音の合図も状態から決める | Q-R7 の線 | 賛 | 賛 | — | **採る** |
| 15 | 未使用の音源（馬が来る音・フィナーレ） | W-2 | 小（取り込み） | STAR に無い | — | 賛 | 賛 | — | **O-8 次第** |
| 16 | HUD を濃さで下げる（`globalAlpha`） | W-3 | 小 | 先頭・自馬が HUD の下に隠れない検査 | 検査 #6 | 賛 | 賛 | C-7 | **採る** |
| 17 | 自馬の位置を画面の端の矢印で示す | W-4 | 小 | — | 検査 #4 | — | 賛 | — | **採る** |
| 18 | レース中の馬券の当たり具合 | W-4 | 中 | 演出で「惜しい」を作る | **反対** | — | 案（△） | §9.8・§12.5 | **取り下げ** |
| 19 | 確定後、当たったときだけの短い祝い | W-4 | 小 | 着順の確定後に限る | 可（§9.8） | — | 案 | — | **採る** |
| 20 | 介入の「押す場所」の画を先に入れる | W-4 | 大 | 効かないボタンを本番に置かない | 条件（`rig-lab` か「練習」の表示。O-1 が先） | — | 案 | §8b・§12.6 | **別の便** |
| 21 | 展開ごとに第 2 幕全体の見せ方を変える | W-5 | 中 | 札から決めると不可 | **状態から決めれば可**（Q-R7） | 賛 | 賛 | C-2 | **状態から決める形で採る**（#24 と同じ部品で） |
| 22 | リプレイは最後の 200m だけ | W-2 | 小 | — | — | — | 案 | — | **[EYES]** |
| 23 | `climax=on` を既定にする | W-3 | 小 | 接戦を作る。勢いのバーの定義も崩れる | O-4 | — | — | C-2 | **既定にしない** |
| 24 | **（新）** `FINISH_CAMERA_BY_DEVELOPMENT` を、状態から決める形へ置き換える | W-3・W-5 | 中 | 09-12 のオーナー指示の見え方を保てるか | 直す（Q-R7）。O-7 の後 | — | 賛 | O-7・V-16 | **次の指示書で**（見え方は [EYES]） |
| 25 | **（新）** 註記の表（`broadcast-v2.ts:2621-2626`）を定数と一致させる | — | 小 | — | 直す | — | 賛 | — | **次の指示書で** |
| 26 | **（新）** 実況の着差の言葉を、着順ボード（`marginLabel`）に揃える | — | 小 | 実況は馬身、ボードは秒差。入力の単位が違う | 揃える | — | 賛 | O-9・D-052 | **O-9 の後** |

---

## 5. 検査

### 5-1. カット本数の検査を置き換える（① の回答 §6-2）

カット本数を固定するテスト（`cut-transition` / `script-v6` / `race-cutin` / `fixed-camera` / `near-rail` / `turn-facing` ほか）を、次の性質を守るテストに置き換えます。

1. 台本や飛ばし方を変えても、着順と走破タイムが変わらない（D-059 ④）
2. 時間の跳びは、すべてカットインの下にある。飛ばす区間の一覧から引いて確かめる
3. 見せる区間で、先頭が画面の外に出た件数と、共通の被写体が 0 頭の件数が 0（D-086）
4. 自馬は常に画面の中にいるか、端の矢印で示されている
5. 見出しと実況に、見せていないコーナーの名前が出ない
6. 先頭と自馬が、長い時間 HUD の下に隠れない
7. 1〜6 が、対応する全コースで成り立つ（R-33）
8. 新しい既定の画面で V-16 を測る（D-067・R-31）
9. O-1 の決定後: 介入する人の見せ方で「飛ばす区間 ∩［残り 900m, ゴール］＝ 空」
10. **（開発側が追加）** 勢い・順位の合図・馬身差は、描画と同じ時刻・同じ入力から出す（R-30）

### 5-2. 結果を先に明かさない（再照会の回答 §1-6）

11. **反実仮想**: 時刻 t まで全馬の位置が同じで、1 着だけが違う 2 つの合成の位置モデルで、時刻 t のカメラ（画角・先頭の置き場所・注視点）と音の合図が一致する
12. ゴールより前のカメラ・音を決める関数が、`winnerGate`・`development`・確定着順を引数に取らない
13. **変異**: 構図に札を戻すと 11 が落ちることを実演する
14. 置き換えた後の既定で V-16 を測る

### 5-3. 写真判定の止め絵（再照会の回答 §3-3）

15. 止め絵の判定関数が、止める絵を描くのと同じ位置を読む（経路の検査・R-30）
16. 多数の seed で、(c) と (b)（確定着差）の判定が食い違う件数を、報告に書く（合否の線は引かない）
17. 止め絵が出たレースでは、着順ボードの 1〜2 着の着差ラベルがクビ以内。食い違いがあれば件数と seed を報告する
18. 止め絵の中の着差の言葉が、着順ボードの言葉と一致する

飽きるかどうかの数値（変化の間隔など）は、報告に並べるだけで、合否の線は引きません。

---

## 6. レビュー側への問いと回答

| # | 問い | 回答（要旨） |
|---|---|---|
| Q-R1〜Q-R6 | 相談書 §6 | `REVIEW_CONSULT_RACE_SIDE_ONLY_ANSWER_20260914.md`（§2・§4 に反映） |
| Q-R7 | 展開の札で、直線の入口から構図や音を変えるのは、「勝ち馬の型を先に明かす」に当たるか | **当たる。文字・画・音とも同じ。今の既定も直す。** 時刻 t の画・音は、時刻 t までの状態だけの関数にする。09-12 の指示は「状態」として読める（O-7） |
| Q-R8 | 勢いのバーを、寄せの途中（残り 250→200m）で出してよいか | **「残り 200m から」の定義に賛成。** ただし縛る条件は「描画と同じ入力から出す」。根拠を「影響 1% 以下」にしない（見た目の速さのずれは、重みの値でなく「重みの傾き × 隊列と真の位置の差」で決まる）。過去の窓から出す。`climax=on` を既定にしないことが前提 |
| Q-R9 | 写真判定の止め絵を出すかを、何で決めるか | **(c) 先頭が決勝線を通過する瞬間の表示位置（止める絵そのもの）の 1〜2 着の差。** (a) の残り 80m 判定は、その後に差が変わるとずれる |

- ⚠️ 開発側の再照会の書き方の訂正は 2 点です（再照会の回答による）
  - 「影響は 1% 以下」は、勢いの根拠にならない
  - 「真の位置」は「寄せが終わった表示位置（γ 写像後）」と呼ぶ

---

## 7. 決まった後の段取り（案）

| 段 | 中身 | 見ていただく場所 |
|---|---|---|
| 1 | コーナーを外す（B・観戦の見せ方）。区間名を直す。§5-1 の検査に置き換える。計画書 §1・§8 を改訂する（レビュー側が指示書で） | テスト |
| 2 | 小さい案: #4・#5・#6・#9・#14・#16・#17・#19・#25 | 本番のプレビュー（seed 42 と seed 99 を並べる） |
| 3 | 結果を読まない形への置き換え: #24（O-7 の後）・#26（O-9 の後）。§5-2 の検査と一緒に | 本番のプレビュー [EYES] |
| 4 | 条件付きの案: #10（O-4・O-6 の後）・#11・#12・#13・#21 | 本番のプレビュー |
| 5 | 試作: #3 連続カメラ、#2 直線 250m と 400m の比較、#7 観客の見積もり | 本番のプレビュー [EYES] |
| 別の便 | #20 介入の画面（O-1・§8b・サーバー側と一緒に） ／ #15 音源の取り込み（O-8） | — |

- 各段は、指示書が出た範囲だけを実装します
- `FINISH_CAMERA_BY_DEVELOPMENT` と、それを決めているコードには、指示書が出るまで触りません
- seed 42 は「せめぎ合いの無い」展開なので、1 本だけで見ると「飽きる」側に倒れます。必ず seed 99 と並べます（① の回答 §6-3）

---

## 8. このまとめで直した、開発側の誤り

- 相談書 §1-4「観客（`crowd.ts`）は描画の呼び出しが見つからない」
  - → **誤り**。起動時にスタンドへ焼き込んでいます（`page.tsx:2237`）
- 相談書 §1-4「支給済みで未使用の音源」
  - → 音源は **STAR のリポジトリには無く、sevendays 側のフォルダにあります**
  - 「支給済み」は「オーナーの手元にある」の意味で、STAR には未取り込みです
- 再照会 §1-4・§1-5「真の位置」
  - → 「**寄せが終わった表示位置（γ 写像後・着順ボードと同じ系）**」
- 再照会 Q-R8「寄せの影響は 1% 以下」
  - → 見た目の速さのずれの根拠にはなりません（重みの傾きで決まる・未計測）
- 再照会 Q-R9 で (a)（残り 80m の判定）を推奨した
  - → (a) にも、開発側が (b) について書いたのと同じ弱点があります。**(c) に改めます**
