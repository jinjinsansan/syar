# P0-fix2 レビュー依頼 — 開発側 → レビュー側

**発行**: 2026-08-04（開発側 Claude）
**対象**: `DEV_INSTRUCTIONS_P0FIX2_20260804.md`（第3号指示書）
**成果物**: `https://github.com/jinjinsansan/syar`（private）/ branch `main` / commit `73ed95c`

---

## 0. 結論

合格基準8項目すべて PASS です。**G-0 の主目的である丈夫さは 650 に戻りました**（是正前は 20ゲーム内年で 449 へ収束）。

| 合格基準 | 実測（4シード × 100/300年・800頭） | 判定 |
|---|---|---|
| 1. V-1 が 12〜18% | 100年 15.20〜16.99% / 300年 15.01〜16.61% | PASS |
| 2. V-2a / V-2b / V-2c | 傾き -0.08〜+0.13%/世代 / 天井 53〜62% / 300年V-1 15.01〜16.07% | PASS |
| 3. **V-2d**（特に丈夫さ） | 最大乖離 7.22%。**丈夫さ 649.0→651.7（+0.41%）** | PASS |
| 4. **V-2e**（距離SD） | 0.92〜1.07倍 | PASS |
| 5. V-3 | 全シード PASS | PASS |
| 6. テスト・typecheck・`any`・定数リテラル固定 | 72件 / exit 0 / ゼロ / 固定済み | PASS |
| 7. 決定論 | JSON バイト一致 | PASS |
| 8. 4シード × 100/300年で安定 | 8ランすべて PASS | PASS |

---

## 1. 独立再実行の手順

```bash
git clone https://github.com/jinjinsansan/syar.git star && cd star
npm install

npm test                                                    # ① 72件
npm run typecheck                                           # ② exit 0
npm run verify                                              # ③ 受け入れ判定（約3.5分）
npm run simulate -- --generations 300 --seed 42 --population 800 --stallion-pool 240
                                                            # ④ V-2d の形質別表
npm run verify -- --horizons 100                            # ⑤ V-2c 未評価で FAIL になること（G-3）
npm run verify -- --population 200                          # ⑥ 小集団の警告が出ること（G-4）
npm run verify:distance                                     # ⑦ 距離定数の校正根拠（約8分）
```

`cd star` の件、前回 `cd syar` と書いたのは私の誤りではなく**リポジトリ名が `syar` だから**でした。clone 先を `star` と明示して曖昧さをなくしています。

---

## 2. 報告すべき点3つ

### 2-1. V-2d は実装直後に本物の退行を検出しました

G-0〜G-4 を実装して最初に `npm run verify` を回した時点（距離系は正典 §6.4 の表どおり回帰0.01）で、**V-2d が 4シードとも FAIL**しました。`distance_range` が 300年で +12〜16% 上振れしていたためです。

このとき **V-1 / V-2a / V-2b / V-2c / V-3 はすべて PASS** でした。旧基準だけなら見逃していた退行で、丈夫さと同じクラスの問題（選抜圧のかからない形質が静かにずれる）が距離系にも起きていたことになります。**V-2d の新設は正しい判断でした。**

原因は大物覚醒のラチェットで、切り分けも取りました（seed 7・800頭・300年）。

| 設定 | distance_range | distance_center |
|---|---|---|
| 通常 | +17.4% | +5.7% |
| `BIG_ATAVISM_RATE=0` | **-1.7%** | **-3.0%** |

### 2-2.【要対応】P0-fix での私の結論が誤っていました — 正典 §6.4 の表の更新をお願いします

P0-fix で「距離系に回帰0.20 をかけると集団SDが 0.4倍に収縮するので距離だけ弱い回帰にする」と報告し、それが正典 §6.4 の表になっています。**この結論は誤りでした。** 収縮の原因は回帰率ではなく、**sd を値域比のまま小さく据え置いていたこと**です。

距離系は2条件を同時に満たす必要があり、**sd は回帰率に従属します**。

- 平均のずれ ∝ ラチェットの押し上げ量 ÷ 回帰率（回帰率が高いほど平均が固定される）
- 平衡SD = `sd / sqrt(2r - r²)`（回帰率を上げたら sd も上げないと分散が消える）

`sd = 創始アレルSD × sqrt(2r - r²)` で追随させれば **回帰率0.20 でも分散は保たれます**（実測 0.92倍）。校正の実測（4シードの最悪値・800頭・300年）:

| 回帰率 | center sd | range sd | center 最悪ずれ | SD比 | range 最悪ずれ | 判定 |
|---|---|---|---|---|---|---|
| 0.05 | 162 | 54 | 14.25% | 1.21x | 19.91% | FAIL |
| 0.10 | 226 | 75 | 8.63% | 0.89x | 12.76% | FAIL |
| **0.20** | **312** | **104** | **4.45%** | **0.92x** | **6.69%** | **PASS** |

結果として**距離系の上書きは `sd`（と比例する clamp）だけになり、回帰率は全形質共通の 0.20 に戻りました**。仕様としてはむしろ単純になっています。

| 形質 | 正典 §6.4 の表（現行） | 実装（本報告） |
|---|---|---|
| distance_center | sd 73 / clamp 243 / 回帰 0.01 | **sd 312 / clamp 520 / 回帰 0.20** |
| distance_range | sd 24 / clamp 80 / 回帰 0.01 | **sd 104 / clamp 173 / 回帰 0.20** |

正典内部の不整合（§6.4 の表 vs §13.2 の V-2d）なので、私の判断では上書きせず報告に留めています。表の更新をお願いします。

### 2-3. clamp 位置の実装ずれを自分で見つけました（是正済み）

正典 §6.4 の式は `noise = N(0,sd) - 回帰項` → `clamp(noise, ±clamp幅)` と、**合計に clamp** をかけます。私の実装は `clamp(N(0,sd)) - 回帰項` の順でした。P0-fix で回帰を追加したときの入れ違いで、レビューでも私の報告でも見落とされていたものです。

品種中心から大きく離れたアレルで、正典なら clamp 幅で頭打ちになる引き戻しが頭打ちにならず、**上位個体の裾を必要以上に削って**いました。正典どおりに直し、全数値を測り直しています。→ **この式が意図どおりかの確認をお願いします（報告書 §7 Q2）**

---

## 3. 逸脱・解釈（6件・報告書 §6 に全件）

| # | 内容 |
|---|---|
| 1 | V-2d の監視対象から能力5種を除外（方向性選抜で上がるのが正常。±10% ゲートは V-2a/V-2b と矛盾する）。ただし参考値として同じ表に併記 |
| 2 | V-2d を genotype の発現値で測定（表現型はインブリード減点を含むため） |
| 3 | `MUTATION_SD=0` でのゼロ除算防御（NaN が genotype に流れ込む経路があった） |
| 4 | clone 先ディレクトリ名の明示 |
| 5 | **距離系の定数が正典 §6.4 の表と不一致**（→ 2-2） |
| 6 | **clamp 位置の是正**（→ 2-3） |

---

## 4. 未解決の質問（報告書 §7）

- **Q1【要対応】** 正典 §6.4 の距離系パラメータ表の更新（2-2）
- **Q2** clamp 位置の是正が正典の意図どおりか（2-3）
- **Q3** 大物覚醒のラチェット。今回 `distance_range` として顕在化し回帰0.20 で相殺したが、**押し返しているだけで発生源は残っている**。P1 でレース選抜が加わると再燃しうるため、V-4〜V-6 較正時に「選抜下での distance_range の推移」を必ず確認してほしい

---

## 5. G-4 の実行時間（判断材料）

| 構成 | 実行時間 |
|---|---|
| 既定（800頭・4シード × 100/300年 = 8ラン） | **207.9 秒（約3.5分）** |
| `--population 200` | 約 20 秒 |

**妥協案は不要と判断しました。** 3.5分なら定数変更のたびに回す回帰テストとして許容範囲です。高速反復用の 200頭は残してあり、指定すると小集団の警告を出します。
