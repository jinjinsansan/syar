# 【STAR】裁定 — TOP 実装の R-14 照合（CH-1〜CH-8）

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **対象** | 実装済みの TOP（`/`）— `apps/web/src/app/page.tsx` → `apps/web/src/components/uma/uma-top.tsx` |
| **合否条件** | `REVIEW_TOP_DESIGN_INSTRUCTIONS_20260917.md` §6 の **CH-1〜CH-8** |
| **依頼カード** | `design/hud-ds/requests/R-14-top-story.md` |

---

## 0. 判定

✅ **CONDITIONAL PASS**

- **CH-1・CH-2・CH-4・CH-5・CH-6・CH-7・CH-8 — PASS**（7 件）
- **CH-3 — 未達。ただし単純な退行ではない**（§3）。★**オーナー確認待ち**
- 🔴 **CH の外で 1 件**: ★**TOP の正本が版管理の外にある**（§4。★条件のうち最も重い）

★**見た目の良否は裁定していません**（コードで確かめられないもの・09-17 の取り決め）。

---

## 1. 前提 — 照合した「現物」が 09-17 と別物です

★09-17 に読んだ TOP（`app/lp-preview/story.tsx` 131 行 ＋ `story.css` 11 行・絵 0 枚）は ★**もう TOP ではありません。**

| | |
|---|---|
| 今の TOP | `app/page.tsx`（29 行・`UmaTop` を呼ぶだけ）→ ★**`components/uma/uma-top.tsx`（258 行）** |
| 旧 TOP | ✔ `/lp-preview` に**復元点として残っている**（消されていない） |
| さらに旧い | ✔ `/lp-arcade` に退避したまま |
| 正本（と `page.tsx` が言うもの） | `design_handoff_uma_monogatari/TopE3.dc.html`（★`TopE2` は却下）→ 🔴 **§4** |

---

## 2. CH ごとの照合

| CH | 判定 | 根拠（[CODE]） |
|---|---|---|
| **CH-1** 禁止語 0 件を維持（`購入`・`チャージ`・`換金`・`円`・`課金`） | ✅ **PASS** | `page.tsx` ＋ `components/uma/*` を全走査して **0 件**。★さらに `uma-ui-wiring.test.ts:218`（①）が**恒久の検査として固定済み**（★註記の語を拾わないよう `strip()` でコメントを落としてから見ている） |
| **CH-2** 実在の馬名・騎手名・競馬場名・他社製品名を書かない | ✅ **PASS**（文字面） | TOP の可視テキストは「そだてる ・ とうひょう ・ かけぬける」「はじめる」「ログイン」「登録は無料です」と題字の `alt`「馬物語 — UMA MONOGATARI」のみ。★実在名は 1 件も無い。🔴 **ただし §5 の L-9 を参照**（★名称そのものの論点は別で、未起票のまま） |
| **CH-3** 導線 5 本を消さない | 🔴 **未達** | TOP の `href` は ★**`/signup`・`/login` の 2 本だけ**。→ ★**§3 で詳しく**（★単純な退行ではありません） |
| **CH-4** `prefers-reduced-motion:reduce` の枝も一緒に納品 | ✅ **PASS** | ✔ `uma-theme.css:208` に `@media (prefers-reduced-motion: reduce)`／✔ `uma-parts.tsx:25` が `matchMedia` を読む。★★**CH-4 が本当に求めたこと（「止めたときの見え方」も決める）まで満たしています** — `uma-top.tsx:205-207` が ★**「止めると 8 コマが `animation:none` で全部 `opacity:1` になり重なって濁る → 1 枚目以外は `opacity:0` を素の値にする」**と書き、実際にそう実装している（`:212` `opacity: n === 1 ? 1 : 0`）。★**片方だけ直して「止めた人には壊れて見える」形を回避している** |
| **CH-5** 動きを止めるボタンを消さない | ✅ **PASS** | ✔ `uma-top.tsx:252-254` の `MotionToggle`（★**全ページ常設**に格上げされている）。✔ `uma-ui-wiring.test.ts` ⑤ が恒久の検査として固定 |
| **CH-6** 値は「セレクタ＋値の表」で返す | ✅ **PASS**（発注側） | ✔ `R-14-top-story.md` §6-6 に明記（★`story.css` が 11 行に全部詰まっているという理由つき） |
| **CH-7** 2〜3 案を同時に出させる | ✅ **PASS** | ✔ `TopE2` / `TopE3` の 2 案が実在し、E2 が却下された。★さらに題字も `LOGO_01` / `LOGO_05` の 2 案が残り、★**定数 1 行（`uma-top.tsx:46`）で切り替えられる形**で実装されている（★D-085 の「戻し口」の作法と同じ形） |
| **CH-8** §2 の現在値を渡した上で描かせる | ✅ **PASS** | ✔ `R-14-top-story.md` §3「今の TOP はこうなっています（★レビュー側がコードで確認した事実）」 |

★**レビュー側でも `uma-ui-wiring.test.ts` を独立再実行し、17 件すべて緑**を確認しました。

---

## 3. CH-3 — ★単純な退行ではありません（★3 つに分けて報告します）

`page.tsx:12-14` の註記: 「⚠️ ★**TOP からは `/race`・`/lp-arcade`・`#story` の導線が外れました**（★オーナー指定「ボタンは はじめる / ログイン の 2 つのみ」）。★どちらも**ルートは生きています**」

### 3-1. ✔ ルートは 5 本とも生きている

✔ `app/signup` / `app/login` / `app/race` / `app/lp-arcade` / `app/lp-preview` の `page.tsx` が**すべて実在**します。★**消えたのは TOP からのリンクだけ**です。

### 3-2. ★`/race` を直接指さないのは、★**後から入った決定と整合します**

✔ `uma-ui-wiring.test.ts:213` が ★**恒久の検査**として次を固定しています:

> ★中継は ★**案内 1 枚を通す**（★`/race` を直接指すと出口が決まらない・**B-1**）
> `expect(code).toContain('href="/watch-race"')`

→ ★**CH-3 は R-14 を起草した 09-17 の導線設計で書かれ、その後の馬物語 UI の便で前提が変わりました。**
★**「`/race` を TOP に戻す」は、今となっては B-1 に反します。** ★CH-3 のうち `/race` の 1 本は、★**条件の側が古くなった**と見るのが正しいです。

★`/lp-arcade`・`/lp-preview` は**史料と復元点**なので、TOP から外れても実害は小さいと判断します。

### 3-3. 🔴 ★残る実質的な論点は 1 つだけ

★**未ログインの訪問者が「観戦」に着く経路が、TOP にありません。**

| 経路 | 状態 |
|---|---|
| TOP → `/watch-race`（B-1 の案内） | 🔴 **無い**（TOP の `href` は 2 本のみ） |
| `lp-arcade` / `lp-preview` から `/race` | ★**TOP から辿れない**ので、URL を直に打つ人にしか届かない |
| `stable` / `races/[id]/bet` から | ★**ログインの内側** |

★`/watch-race` 自体は認証の関門を持たない（`use client` のみ・リダイレクト無し）ので、★**着けさえすれば未ログインでも見られます。** ★問題は**着く道が無い**ことです。
★**D-097（見せ方を観戦／介入で分ける）に関わるので、黙って決めずにオーナーへ返します**（→ **TC-2**）。

### 3-4. ⚠️ ★私はこのオーナー指定を直接受けていません

★`page.tsx` の註記が根拠として挙げる「オーナー指定」を、★**レビュー側は直接受け取っていません**（R-25 の家族 — ★**開発側の文書に書かれた決定を、確かめずに既成事実として扱わない**）。→ **TC-2 で確認します。**

---

## 4. 🔴 ★CH の外で見つけたもの — TOP の正本が版管理の外にあります

```
page.tsx:7   「★正本は引き渡し資料 design_handoff_uma_monogatari/TopE3.dc.html」
実体          out/gen/top3/design_handoff_uma_monogatari/TopE3.dc.html
git ls-files  ✘ 追跡されていない
git check-ignore -v  →  .gitignore:11「out/」に該当
```

| # | |
|---|---|
| 1 | ★**`out/` を掃除した日に、TOP の正本が消えます。** ★**R-28 の家族** — 「リポジトリという名のプログラム」の外に正が置かれている形です。★実装（`uma-top.tsx`）は版管理下にあるので**画面は消えません**が、★**次に「正本と実装が食い違っていないか」を照合する手段が失われます** |
| 2 | ★**R-14 §6-1 が指定した納品先は `design/hud-ds/components/landing-story/`** でした。→ ✘ ★**そのフォルダは存在しません** |
| 3 | ★**§6-2 が要求した 1 行目の `@dsCard` も付いていません**（`TopE3.dc.html` の 1 行目は `<!DOCTYPE html>`）→ ★**デザインシステムの索引に出ません** |
| 4 | ✅ ★**`components/landing`（退避した旧 `/lp-arcade` の史料）は上書きされていません** — ★R-14 §6-1 の「上書きしないでください」は**守られました** |
| 5 | ★これは **R-15（カード 7 枚が未同期）と同じ根**に見えます（★デザイナーのチャットと版管理の間で、納品物が同期していない） |

---

## 5. 🔴 オーナーの承認・確認が要るもの

| # | |
|---|---|
| **1** | ★**CH-3 の「オーナー指定」がご記憶と合っているか**（§3-4）。合っていれば **TC-2** で導線を正式に決め直します |
| **2** | ★**未ログインの訪問者に観戦の入口を出すか**（§3-3）。出すなら TOP に `/watch-race` を 1 本。出さないなら**その旨を記録して CH-3 を正式に緩めます**（★黙って未達のまま残さない） |
| **3** | ★**L-9 の起票の可否**（09-17 から持ち越し）— ★**「馬物語」という名称と他社機種の想起**。★**TOP はこの名称を題字のロゴ画像として実装済み**（`/art/uma/logo-05.webp`・案 1/案 5 の 2 案）。★**後から変えると差し替えの範囲が広がります**（題字・`metadata.title`・`alt`・ロゴ 2 案） |

---

## 6. 条件（TC-1〜TC-3）

| # | 条件 |
|---|---|
| **TC-1** | 🔴 ★**TOP の正本を版管理下に置く。** ★`design/hud-ds/components/landing-story/index.html` に、★**1 行目の `@dsCard` を付けて**納める（R-14 §6-1・§6-2）。★併せて **`page.tsx:7` の「正本は…」の註記を新しい場所に直す**（★2 つが食い違うと、次に読む人はどちらを信じてよいか決められません） |
| **TC-2** | ★**導線を正式に決め直す**（§5 の 1・2）。★決めた結果を `page.tsx` の註記に**オーナー決定として**書く（★「オーナー指定」とだけ書かれた根拠を、確かめられる形にする） |
| **TC-3** | ★**TOP の導線を検査で固定する。** ★**今は CH-3 を守る検査がありません** — ★**だから 3 本が黙って消えました。** ★TC-2 で決まった本数を `uma-ui-wiring.test.ts` に足す（★既にある「旧ルートへ落ちない」「中継は案内を通す」と同じ場所へ） |

★**この便で着順・経済に効くものを入れないでください。**

---

## 7. ★記録しておく教訓

| # | |
|---|---|
| 1 | ★★**合否条件は、実装が進むと古くなる。** ★CH-3 の `/race` は、★**条件を書いた翌日に入った B-1 で意味が変わりました**（§3-2）。★**「条件を満たしていない」と書く前に、条件の側が生きているかを確かめる** |
| 2 | ★**「ルートは生きています」は「着けます」ではない。** ★リンクを消すことと、ページを消すことは別ですが、★**利用者にとっては同じ**です（§3-3） |
| 3 | ★**正本の置き場所は、納品のときに決める。** ★`out/` は生成物の置き場で、★**掃除される前提の場所**です（§4） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
