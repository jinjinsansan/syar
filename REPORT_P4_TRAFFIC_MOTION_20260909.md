# レースの位置取りと再生速度（2026-09-09）

## 対象

オーナー指定の3点（横滑り・馬同士の重なり、一斉に内へ寄る動き、隊列の急な伸縮・入れ替わり）を修正する。指定フォルダの「せめぎ合いを参考にできるアプリ.mp4」から抽出済みの連続コマを確認し、並走時の間隔、進路変更を追える速さ、コーナーの連続性を基準にした。

## 変更

- 前回の未完了変更を引き継ぎ、`trafficPositionModel` を画面と監査道具へ接続。希望する横位置へ直接移すのではなく、2秒先までの前後関係を調べて隣の馬との間隔を確保する。横移動はレース時間で毎秒0.65m以下。馬ごとの追従速度を変え、全馬共通の寄せ方を緩和する。
- 過去の馬ペアごとの左右関係を保持する方式を、現在の横並びから一貫した左右関係を取る方式へ変更。離れている馬が進路を交差した後も、古い左右関係が残って制約が循環することを防ぐ。
- ゴール済みの馬を線上に停止した集団として扱っていた不具合を修正。走り抜けを予測に含め、それぞれのゴール後は横の進路を保つ。seed 99では1番と3番の横位置が同じ0.8mに潰れていたが、修正後は約1.30mの間隔を保つ。
- 道中だけでなく勝負所・直線も2倍速以下に制限。勝負所の5.2倍速への加速を除き、発走とゴール前は等速を保つ。
- 4コーナーの追従カメラは、ショット自身の距離区間からズームを決める。カット終端や強制シークで次の区間の開始へズームが巻き戻る不具合を修正。既存の固定カメラは引き続き選択可能。通常画面と監査側は追従カメラを明示的に使用。

## 整合と限界

前後の走行距離、境界時刻、走破タイム、確定着順は元の位置モデル・エンジンを維持する。横位置には観戦用の間隔補正を加えるため、「画面の横位置そのものがエンジンの距離ロス積分と完全一致する」とは言えない。画面の開発用注記もこの実態に合わせた。この補正をレース結果へ逆流させてはいない。

時間制限より動きの読みやすさを優先するため本編の尺は延びる。測定した1600mでは約39秒から約65〜66秒、2400mでは約46〜48秒から約93〜95秒。導入・ゴール後の演出時間は別。

馬の素材は既存の方向別2D画像を継続使用する。3Dの馬体旋回や参考映像と同等の演出品質を達成したという判定ではない。また、透視投影による自然な前後の遮蔽と、同じ走路位置への入り込みは別に扱う。

## 検証

- `node --import tsx tools/audit-traffic-motion.mjs`: 1200/1600/2400m × seed 42/99/14、各レースを旧版と新版で30fps相当の本編全区間測定。
- 全9条件で、新版の「前後2.4m未満かつ横1m未満」の馬ペア・フレーム数は0。旧版は各条件1411〜5814。
- 新版の最大横移動速度は表示時間で毎秒1.30m以下。旧版は各条件約6.07〜9.88m。
- 全条件で最終着順がエンジンと一致。
- 数値記録: `out/traffic-motion/audit.json`。
- 回帰検査: 追従カメラのカット境界（左右回り・台本v4/v5/v6）、seed 99のゴール後の間隔維持、全局面の2倍速上限。
- `npm run typecheck`: 成功。
- `npm test -- packages/render/test`: 63ファイル・574件が成功。

## 実画面確認（Vドライブ復旧後）

`http://localhost:3210/race?dev=1&badge=0&seed=99` の実Canvasを、表示秒を固定して12fpsで300コマ撮影。各コマの表示秒が指定値と一致することを検査してから、同じ12fpsで動画化した。ヘッドレスEdgeでの確認であり、利用端末のGPU性能・実時間のフレームレートを保証する測定ではない。

- `out/traffic-motion/start.mp4`: 発走から8秒（96コマ）。
- `out/traffic-motion/corner.mp4`: 3コーナーから4コーナー、次の横カメラまで8秒（96コマ）。
- `out/traffic-motion/finish.mp4`: ゴール前から勝馬紹介まで9秒（108コマ）。
- 各動画の連続コマを `start-sheet.jpg`、`corner-sheet.jpg`、`finish-sheet.jpg` として確認。発走後の分散、コーナーの進路、ゴール後の走り抜けを確認した。横カメラでの遠近による馬体の遮蔽は残る。
- ブラウザ実行時例外0件（`browser-errors.json`）。各コマの時刻・向きの診断は各サブフォルダの `frames.json` に保存。

撮影スクリプトは `tmp/capture-traffic-motion.mjs`。開発モードで入口メニューを省略する場合も、開始可能な操作画面を待つよう修正した。前回のVドライブ読み取り障害後、依存ファイル・レポート・測定JSON・Gitの状態を再読でき、今回の撮影を完了した。
