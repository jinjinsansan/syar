# 開発指示書 P0-fix3 — 大物覚醒の適用範囲を能力5種に限定

**発行**: 2026-08-04（レビュー側 Claude）
**前提**: `REVIEW_P0FIX2_VERDICT_20260804.md` を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**D-010 / D-011 を受けて §6.3・§6.4・§13.1・§13.2 を改訂済み。必ず読み直すこと**）

---

## 0. まず結論 — P0-fix2 は PASS でした

合格基準8項目すべて成立し、`npm run verify` の出力はレビュー側の環境で**完全一致**（206.6秒）。丈夫さは 649〜681 と創始650近傍に是正され、G-0 の目的は達成されています。テスト72件・typecheck・G-3/G-4 のゲート挙動もすべて確認しました。

**特に評価します:**

- **自分の前回の結論を誤りとして申告し、正しい分析に置き換えた**こと（2-2）。レビュー側で平衡式 `平衡SD = sd/√(2r−r²)` を独立に検算し、**新しい結論が数学的に正しい**ことを確認しました。旧値73/24と新値312/104は同じ式の r 違いで平衡SDは同一（創始水準）、違うのは平均の固定力だけ — 正典 §6.4 を導出式ごと是正しました（D-010）
- **clamp 位置の実装ずれを自分で見つけた**こと（2-3）。これは P0-fix のレビューで**私が見逃したもの**です。是正は正典の意図どおりで承認します（Q2 の回答は §6.4 に明記）
- **V-2d が実装直後に本物の退行（distance_range +12〜16%）を検出した**こと。基準を新設した判断の正しさを、実際の検出で示してくれました
- `cd syar` の件は私の指摘が的外れでした（リポジトリ名でした）。失礼しました

自己申告した逸脱6件はすべて承認、Q1〜Q3 にも回答済みです（回答書 §2〜§6）。

---

## 1. スコープ — 4件

> **⚠️ 本指示書は発行後にレビュー側のコード監査結果を反映して H-2〜H-4 を追加しました。** H-2 は **Q2 への一次回答の撤回**を含みます（あなたの元の実装が正しく、私の指示が誤りでした）。

### H-1【必須】大物覚醒を能力5種のみに適用する（D-011）

大物覚醒（`genetics.ts:180-195`）が `NUMERIC_TRAITS` 全11形質に適用されているため、**非能力6形質すべてが上方向にずれています**（V-2d の実測: 芝適性 +7.22% / 気性 +3.88〜6.30% / 距離2種 +2.2% / 丈夫さ +0.41%）。

特に**気性は「高いほど気性難」**（正典 §5.3）なので、覚醒するたびに馬が扱いにくくなるという逆向きの効果になっていました。距離適性への適用は `distance_range` を押し上げて全馬を万能型に近づけ、§5.2 の分化を損ないます（あなたが Q3 で指摘した懸念の発生源）。

**正典 §6.3 を改訂しました**: 大物覚醒の適用対象は **SP/ST/PW/GT/IQ の5形質のみ**。通常の隔世遺伝（6%・ランダムな祖父母のアレル）は**全形質に適用したまま**です（「先祖の質が出る」体験自体は残す）。

- 対象形質のリストは定数として外出しし、テストでリテラル固定すること
- V-3（大物覚醒の実発生率）の分母が能力5種のみになる点に注意。**分母の定義変更を報告書に明記**すること
- これは Q3 への回答でもあります: 回帰で押し返すのでなく**発生源を断つ**方針です。ただし**能力5種のラチェットは残します**（血統インフレ抑制の均衡が D-008 でそこに合わせて組まれているため）

### H-2【必須・Q2 の一次回答を撤回】clamp は変異ノイズのみにかける — **あなたの元の実装が正しかった**

Q2 に「合計に clamp が意図」と回答しましたが、**これはレビュー側の誤りです。撤回します。** 正典 §6.4 を改訂しました:

```
通常変異: clamp(N(0, sd), ±clamp幅) − (アレル値 − 品種中心) × 回帰率
```

つまり **P0-fix2 で「是正」した内容を元に戻してください**（あなたが `clamp(N)−pull` と書いていた形が正しい）。理由:

1. **平衡式が成立するのはこの形だけ**。D-010 で正典に入れた `平衡SD = sd/√(2r−r²)` は AR(1) 過程を前提とします。合計に clamp をかけると裾で線形性が崩れ、**距離系 sd の導出根拠そのものが失われます**
2. clamp の目的は乱数の暴れを抑えることであり、決定論的な引き戻しを抑えることではない（回帰項は `r ×（中心からの距離）`で自然に有界）
3. 合計に clamp すると**最も引き戻したい極端なアレルほど引き戻しが弱くなる**。レビュー側の実測: アレル値900で −90.0 → −76.5、値1000で −110.0 → −90.6。結果として **V-2b 天井 57〜60% → 61〜64.5%、V-1 15.31% → 14.52%**（200頭では最小13.50%と下限12%に接近）。血統インフレ抑制という回帰の目的に逆行します

**あわせて、この式を固定するテストを追加してください。** 現在の回帰テストはすべて `MUTATION_SD=0` で走っており、その条件では両形式が同値になるため、**旧実装に戻しても72件全部 PASS します**（レビュー側監査で確認）。ノイズを注入できる純関数に切り出すなどして、式の形そのものを固定してください。

### H-3【必須】非能力形質の sd を創始アレルSDから導出する

距離系で起きた問題は他の形質でも起きていました（レビュー側監査）:

| 形質 | 現状の平衡SD ÷ 創始SD | 症状 |
|---|---|---|
| 芝/ダート適性 | **0.71倍（収縮）** | 馬場適性の個性が消える。**V-2d は平均しか見ないので素通りしていた** |
| 丈夫さ | **1.41倍（拡大）** | 故障率の分母のばらつきが広がる（P1 の V-7 に効く） |

原因は距離系と同じで、sd を値域比で機械的に縮尺していたためです。正典 §6.4 を改訂し、**非能力形質の sd はすべて `創始アレルSD × √(2r − r²)` から導出**することにしました:

| 形質 | 創始アレルSD | sd | clamp |
|---|---|---|---|
| 丈夫さ | 100 | **60** | ±100 |
| 気性 | 15 | **9** | ±15（現行と同値） |
| 芝/ダート適性 | 20.2 | **12** | ±20 |
| distance_center | 519.6 | 312 | ±520（現行のまま） |
| distance_range | 173.2 | 104 | ±173（現行のまま） |

**能力5種は例外**で sd=90 のまま（V-1 から較正されており、V-1/V-2a/V-2b が直接管掌するため）。可能なら `FOUNDERS` から導出し、テストで一致を固定してください（G-0 と同じ方式）。

### H-4【必須】V-2e を非能力形質すべてに一般化する

正典 §13.2 を改訂しました。V-2e の対象を `distance_center` のみから **V-2d と同じ6形質（丈夫さ・気性・芝/ダート適性・距離2種）の集団SD**へ広げ、いずれも創始水準の **0.8〜1.4倍**とします。

芝適性の収縮（0.71倍）は、**V-2d が平均しか見ず V-2e が距離しか見なかったためどのゲートにも掛かりませんでした**。距離系で起きたことの再演です。

---

## 2. やらないこと

- 距離系の定数（sd 312/104・clamp 520/173・回帰0.20）の再校正 — H-1 で非能力形質のラチェットが消えると V-2d の余裕は**増える**方向なので、現行値のままで問題ないはずです。**もし V-2e（距離SD 0.8〜1.4倍）が外れるようなら報告してください**（平衡SDはラチェットと独立なので変わらない見込み）
- 能力5種の大物覚醒の発生率変更（`BIG_ATAVISM_RATE`）
- V-1/V-2a〜e/V-3 の閾値変更（オーナー承認事項）
- レースエンジン・オッズ・育成ループ・UI・DB（P1 以降）

---

## 3. 合格基準

P0-fix2 と同じ8項目に加えて:

| # | 基準 |
|---|---|
| 9 | **大物覚醒が能力5種のみに適用されている**ことをテストで固定（非能力形質では発動しないこと） |
| 10 | **V-2d の乖離が P0-fix2 比で縮小している**こと（特に気性・距離2種）。縮小しない場合は原因を報告 |
| 11 | **clamp の式（H-2）がテストで固定されている**こと。`MUTATION_SD=0` 以外の条件で、旧形式に戻すと落ちるテストがあること |
| 12 | **V-2e（一般化版）が全6形質で 0.8〜1.4倍**に収まること。特に芝/ダート適性が 0.71倍から改善していること |

既存8項目（V-1 / V-2a〜e / V-3 / テスト・typecheck・any・定数固定 / 決定論 / 4シード以上 × 100/300年）はすべて維持してください。

H-2 で平衡が下がる方向に戻るため、V-2b・V-1 は P0-fix2 より改善する見込みです。H-3 で丈夫さ・適性の sd が変わるので、**V-2d/V-2e の数値は P0-fix2 と並べて比較**してください。

---

## 4. 報告様式

`REPORT_P0FIX3_[日付].md` に:

1. 変更点
2. 再実行コマンド
3. 検証結果（4シード × 100/300年・**V-2d の形質別の表を P0-fix2 と並べて比較**）
4. V-3 の分母定義の変更点
5. 仕様からの逸脱・解釈（全件列挙。ゼロなら「なし」と明記）
6. 未解決の質問

---

## 5. P1 への引き継ぎ（更新版）

P0 が固まったら次は P1（レース判定エンジン + モンテカルロ検証）です。以下を引き継ぎます:

- **Q3 の懸念**: D-011 で非能力形質のラチェットは消えますが、P1 でレース選抜が加わると `distance_range` の大きい万能型に選抜圧がかかりうる。**V-4〜V-6 較正時に「選抜下での distance_range と V-2e の推移」を必ず確認**すること。D-011 後の実測値を基準線とする
- **選抜 h²=0.3 の置き換え**: P0 の種牡馬選抜は観測ノイズ付きの近似。P1 では**実際のレース結果に基づく選抜**に置き換える
- **`INJURY_BASE` の較正**: 丈夫さの集団水準が創始650近傍にあることを前提とする（コード側コメント済み）
