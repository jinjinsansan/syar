# 【STAR】裁定: 導入の永続状態（初回の馬づくりを途中から再開できるようにする）— 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_ONBOARDING_STATE_20260922.md`（開発側 `star-aa`・`2c57226`）
- 運用: オーナー指示「承認不要で自立的に進める」

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| — | 段階を新しい列に書き写さず、在る行から導く | ✅ **採る**（書き写しと実物が食い違う形を作らない・D-119 の族を避ける） |
| 1 | 既存利用者の見分け方 | ✅ **(a) `users.onboarding_flow`**。条件 3 つ（§1） |
| 2 | 段階を SQL（RPC）で導く | ✅ **よい**。★ただし**時間の計算は SQL に入れない**（§2） |
| 3 | `legacy` は初回の配合を使えない | ✅ **案のまま**。★オーナーが新しい流れを試すときの注意を記録（§3） |
| 4 | 「あとで」ボタンを付けない | ✅ **案のまま**。★言葉の整理を 1 つ（§4） |
| 5 | 🔴 候補切れの段階（照会に無かった点） | ⚠️ **段階の表に足す**（§5） |

---

## 1. 既存利用者の見分け方 — ✅ (a)

- (a) の列は「段階」ではなく「**どの導入で始まったか**」という、一度だけ決まる事実。★他の行からは導けないので、書き写しではない。★(b) の日時で分ける案は、staging と本番で違う日時を書き留めることになる（D-053・NM-1 の族）→ 採らない
- **条件**:
  1. 移行で列を足し、**既存の行を `'legacy'` で埋めてから `not null`** にする。★**列の既定値に `'first_horse_v1'` を置かない** — 既定値にすると、古い `create_account`（移行の前に配備されていた版）が作った行まで新しい流れになる。★新しい流れの値は **`create_account` が明示して書く**
  2. 値は `check` で閉じる（`'legacy' | 'first_horse_v1'`。★任意の文字列を書かせない・0057 と同じ作法）
  3. ★**一度書いたら変えない**ことを、更新の口を作らないことで保つ（書き換える RPC を作らない）

## 2. 段階を SQL で導く — ✅ よい。時間の計算は入れない

- ✔ 段階は「行が在るか」と「要求の状態」だけで決まり、遺伝の計算も、較正の数も入らない。★SQL に置いても出どころは 1 つ（この RPC だけ）なので D-052 に反しない
- **条件**:
  1. ★**画面の側で段階を導き直さない**。`login`・`home`・`useStableView` は RPC の結果だけで行き先を決める。★画面のコードに `foal_requests` や `foal_drafts` を直接読む口を作らない（構文で見る試験を 1 本）
  2. ★**時間の計算（「次の年まであと何日」など）は SQL に入れない**。週・年・サイクルの定数は TS（`packages/scheduler`）が持っている。SQL で計算すると定数の写しができる。★RPC は**事実（段階・要求の状態・理由の語）だけ**を返し、日数は画面が `packages/scheduler` の関数で出す
  3. 非公開の能力値を返さない（照会どおり）。★返す列の一覧を `pg_proc` の定義か構文で見る試験を置く（D-108 ③）

## 3. `legacy` は初回の配合を使えない — ✅

- 案のまま。★既存の利用者は付与馬を持っており、D-120 の「初回 1 回」は新しい入口の話
- ⚠️ **記録しておくこと**: 本番の利用者はいまほぼ 0（09-20 の実測 `users 0`）で、その後にできた口座は**オーナー自身の確認用**の可能性が高い。★移行の後、その口座は `legacy` になり、**オーナーが自分の口座で新しい流れを試せない**。★試すときは新しい口座を作る必要がある。→ 開発側は、配備の手順書にこの 1 行を書くこと（★黙って `legacy` にしない）

## 4. 「あとで」ボタンを付けない — ✅

- 案のまま。★ただし照会の文は「進めない」と「`/home` へは行ける」が並んでいて、読み違えやすい。★整理すると:
  - 導入の画面に「あとで」ボタンは置かない
  - ★ナビゲーションで `/home` などへ移るのは止めない（付与馬ですぐ遊べる・D-120）
  - 段階は残り、導入の画面に戻れば続きから始まる
- ★この 3 行を、デザイナーへの確認事項に入れること（「あとで」が無いことと、ナビで抜けられることは、画面の設計が決める）

## 5. 🔴 候補切れの段階 — 照会に無かった点

- 裁定 `f613878` §3 で、**母の候補が尽きたら初回の配合を先送りにし、「次の年（あと○日）に候補が戻ります」と出す**と決めた。★照会の段階の表では、これが `choose_parents` の中に埋もれて、**画面が候補切れを知る方法が無い**
- **決定**: `choose_parents` の結果に **「いま選べる母の候補が在るか」**（真偽）を足す。★在るかどうかは SQL で数えてよい（行の有無）。★「あと何日」は §2 条件 2 のとおり画面が TS で出す
- 候補が 0 のまま要求が来た場合の扱い（ワーカーが `failed` にする理由の語）は、既存の失敗の語に `no_candidate` を足す形でよい。★これも画面に「もう一度お試しください」とは別の見せ方で出す（`internal_error` と区別・裁定 `322d603` §9）

## [CODE] と [EYES]

全部 [CODE] と設計の判断。画面の流れ（「あとで」の無さ・候補切れの見せ方）の手触りは [EYES] ＝ オーナーとデザイナー。

## 6. 追記（2026-09-22）— §1 条件 1 の読み方: ✅ 既定値 `'legacy'` でよい

- 開発側の走査で、`users` に行を入れる書き手は `create_account` のほかに**道具が 14 本**あった（`synthetic-bettor`・`verify-*` ほか）。既定値が無いと、移行を当てた瞬間に 14 本とも `not null` で落ちる
- §1 条件 1 が禁じたのは **`'first_horse_v1'` を既定値にすること**（古い経路が作った行まで新しい流れ＝無償の初回配合の側に倒れる）。★**`'legacy'` を既定値にするのは、その逆で、古い経路・道具が作った行を「初回の配合に戻さない」側に倒す**。★安全な側への既定なので採る
- ★私の事務連絡の要約「既定値を置かない」は、本文より粗かった（本文は「`'first_horse_v1'` を置かない」）。★要約で条件を縮めない
- **条件**: ①新しい流れの値は `create_account` が**明示して**書く（照会どおり）②★**`create_account` が `'first_horse_v1'` を書いていることを検査で固定する**。書き忘れると、新しい利用者が黙って `legacy` になり初回の配合を使えない（安全側だが、機能が静かに止まる）。★検査は「新しく作った口座の `onboarding_flow` が `'first_horse_v1'` である」を実 DB（staging の実演）で見る

## 7. 追記（2026-09-22）— 実装（`131115f`）の照合 ✅ と次の順番

### 照合 ✅
- ✔ `0067`: `onboarding_flow text not null default 'legacy'` ＋ `check ('legacy','first_horse_v1')`（§1・§6）。✔ `create_account` が `'first_horse_v1'` を書くことを、本物の `create_account` を呼んで確かめ、直接挿入した行が `legacy` になることを対照にしている（§6 ②）
- ✔ 時間の計算を SQL に入れていない（§2 条件 2）: いまの週は**ワーカーが書く `world_state.game_week` を読み**、繁殖の年齢・生涯の産数の閾値は**呼ぶ側（TS）が引数で渡す**（`p_min_breeding_age_weeks`・`p_max_lifetime_foals`）。★定数の写しを SQL に作らない形で正しい
- ✔ `0068`: いまの週が読めなければ `has_dam_candidate = null`（分からない）。★「分からない」を「無い」と言わない — staging で実際に `world_state` が 0 行だったのを見つけて直した。★**止める側で見つけた**
- ✔ 画面のコードに `foal_requests` / `foal_drafts` を直接読む口は無い（`apps/web/src` を検索して 0 件・§2 条件 1）
- ⚠️ 小さな記録（阻まない）: 読む口の候補の判定（`0067:176-182`）は母の**印**（`not h.bred_this_year`）を使い、ワーカーの確定は**2 つの表で年の仔を数える**（`damHasFoalInYearSql`）。★年の変わり目の前後で、画面が「在る」と言い、ワーカーが `no_candidate` で落とすことがありうる。★確定はワーカーが正（画面は目安）で、落ちたら理由が出るので実害は小さい。★ただし**定義が 2 つある**ことを読む口の註記に書いておくこと
- ⚠️ 引数の閾値はクライアントが渡すので、変えれば表示の真偽は変わる。★**影響は本人の画面の目安だけ**で、確定はワーカーが自分の定数で判定するので、害は無い

### 次の順番
1. **D-113 ⑦ の本番の読み取り**（開発期間中の登録が招待制・許可リストか）。★読むだけ。★本番配備のオーナーの文面を待たずにできるなら先に。★無償の初回配合（D-120 ②）の前提なので、**配備の判断材料として要る**
2. **NG 名リストが届いたときに、`name_checked_with is null` の行を全部検査し直す道具**（裁定 `ff7028c` §4 条件 2）。★リストが無くても作れ、届いた日にすぐ使える。★本番で書くのはオーナーの許可
3. **I-1 引退時の役割選択**（PLAN の本筋）
4. 案 B の模擬（全馬で調教を 0 週から・PLAN Q-1）— ★急がない

★「初回の馬づくりを staging で完走」は、画面が無い間は RPC からの完走（実演 ⑩）で足りる、を受理する。★画面ができたら、画面から完走し直す（PLAN に残しておく）。
★レビュー側は、簿 `PLAYER-NAME-OFFENSIVE-WORDS-UNCHECKED`（owner: review）の案を別に出す。
