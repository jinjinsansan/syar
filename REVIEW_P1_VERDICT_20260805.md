# P1 レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-05（レビュー側 Claude）
**対象**: `REPORT_P1_20260805.md` / `QUESTIONS_P1_20260805.md`（commit `4ab628d`）
**判定**: **CONDITIONAL PASS — O-1〜O-8 の是正が要る**（照会6件は回答済み・正典を6点改訂）

> ⚠️ **一次回答は PASS だったが、コード監査で HIGH 4件が出たため改めた。§7 を参照。**

---

## 0. 結論

| 項目 | 判定 |
|---|---|
| テスト196件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| K-5 モンテカルロ（V-4/5/6/8/9/12） | **CONFIRMED** — 10万レース×4シードを独立再実行し全数値一致（V-4 32.51% / V-8 94.69%） |
| K-4 実レース選抜下の P0 ゲート | **CONFIRMED** — 全PASS（V-1 15.87% / 15.38%） |
| 既定 proxy の verify | **CONFIRMED** — P0-fix5 と全数値一致 |
| プローブ残留（§9 の自己申告） | **CONFIRMED 復旧済み** — レビュー側の独立検索で残留ゼロ・`git status` 空 |
| 照会6件 | **全件回答**（§2）。うち**2件はレビュー側が書いた正典の欠陥** |
| ★V-2d の余裕が薄い件 | **構造的な意味を発見 → V-2f を新設**（§3） |

---

## 1. まず、照会の出し方が正しかった

**正典の定数を1つも書き換えず、暫定解釈を `INTERPRETATIONS` に登録して「ソースの参照と登録簿の一致」をテストで機械検査する**という進め方は、このプロジェクトで確立した規律の到達点です。

特に `RACE_RANDOM_K` を「正典の写しは 0.12 のまま・較正値は別定数」に分離した判断は、P0 の D-008 と同じ形で正しい。**推測で正典を上書きしないという原則が、6便かけて実装側の作法として定着した**ことが確認できました。

---

## 2. 照会6件への回答

### Q1 `condition_aptitude` が genotype に無い → **単一の `heavy_aptitude` として genotype に追加**（D-015）

**レビュー側が書いた正典の欠陥です。** §5.2 が定義しているのに §5.4 のスキーマに無く、遺伝しない状態でした。

3値（good/yielding/soft）ではなく**単一の道悪適性**に畳みます。**良馬場が得意という概念は無意味**（良馬場は全馬にとって基準）なので、`trackConditionCoef` は「良 = 全馬1.0 / 稍重・重・不良 = `heavy_aptitude` に応じて減衰」とします。

遺伝させる理由: **道悪巧者は配合で狙う古典的な目標**であり、§1.1「血をつなぐことが唯一の進行軸」に直結します。遺伝しない固定属性にすると、馬場適性は「引きの運」になって配合の深みが1本消えます。

コスト（形質が1つ増え、§6.4 の導出・V-2d/V-2e/V-2f の対象が増える）は認識のうえでの判断です。導出は既存の式（創始アレルSD × √(2r−r²) ÷ 0.916）に従ってください。

### Q2 §8b.2 のスタミナ数値が成立しない → **消費をレース時間に対する割合で定義**（D-017）

**これもレビュー側の欠陥です。** 絶対値 1.0/秒 では 2000m（約120秒）で必ずゲージが 0 になり、「ゴール前にゲージ0なら −0.15」が全馬に常時適用されて**介入の巧拙が結果に出ません**。指摘のとおりです。

正典 §8b.2 を改め、**消費率を「レース時間に対する割合」で定義**しました:

```
毎秒消費 = (基準消費量 / レース秒数) * ポジション係数 * ペース係数
```

**具体値は開発側が較正してください。** 目標の挙動を指定します:

- 「中団・仕掛けなし」で走った**典型的な馬（初期値75前後）がゴール時に2割程度残す**
- **仕掛けを早く始めると（残り900m超）ゴール前に尽きる** — つまり「早仕掛けの罰」が到達可能な選択肢として機能する
- 距離が変わっても同じ関係が保たれる（割合定義の利点）

較正条件を報告書とコードに明記してください（R-7）。

### Q3 `RACE_RANDOM_K` 0.12 → 0.26 → **①正典を 0.26 に改訂**（D-016）

較正は妥当です。**§8.7 の「0.12 は1番人気30%前後に落ち着く値」という記述が誤り**なので、そちらも訂正しました。

**このゲームでは「1番人気」がモンテカルロ自身で定義される**（＝完全な予測器）ため、V-4 は予測の精度ではなく**出走表の競争性**を縛る基準です。現実の32%は「能力差」と「予想の外れ」の両方を含みますが、こちらは後者がゼロなので、同じ32%を出すには**現実より大きな乱数か、現実より圧縮された能力差**が要ります。K=0.26 はその差を埋めています。

要因分解（素質開放率が最大寄与・幅を潰しても38%止まり）も筋が通っています。**★較正条件（素質開放率プレースホルダ・クラス分け・18頭立て）を §13.1 に明記**したので、P3 で再較正するときに前提を見失いません。

「出走表の粗さを K で埋めていないか」という自己懐疑はそのとおりで、**埋めています**。ただしそれは現時点では正しい対処です。P3 で本物の育成ループが入れば能力差の分布が変わるので、そのとき K は下がる方向に再較正されるはずです。**次便で「出走表の能力CV」を K と並べて毎回報告してください** — どちらのレバーが動いたかが分かるようにするためです。

### Q4 V-8 と V-9 が両立しない → **V-9 を分割**（D-014）

**レビュー側の誤りです。** §8b.3 の直線の追い `driveBonus` は 0〜+0.10 の**片側正**なので、介入した馬の期待倍率は必ず 1.00 を超えます。「平均 0.99〜1.01」を満たすには介入していない馬（1.00）で薄めるほかなく、**それでは指標がほぼ空洞**になります。暫定解釈は正典の字義には合っていましたが、**基準そのものが間違っていました**。

改訂:

- **V-9a（ゲート）**: 全サンプルが 0.90〜1.10 内 — **±10% のハードキャップが全経路で効いていること**。これが安全性の要（§1.5-1）
- **V-9b（監視）**: 介入した馬だけの平均は**時系列の変化**を見る。絶対値 1.00 を目標にしない。「急な上振れはバグかマクロの兆候」という §8b.8 の趣旨はこちらで果たす
- **V-8 の「期待値」は倍率の期待値**と明確化

**そして勝率比 78% についてオーナー判断を取りました。** 倍率で 5.8% の差が、勝敗は僅差で決まるため**勝率では約28%の差**になります。この数字は D-006（介入を第5の柱として採用・±10%キャップ）のトレードオフが初めて数値化されたものです。

→ **オーナー決定: 現状維持（±10%）**（2026-08-05）。判断材料として、①上記は「完璧な操作」との比較であり現実のプレイヤーとの差はより小さい ②§10.4 が「観られる時刻を選んで出走させる」設計を既に定めている、を提示しました。**勝率比はゲートではなく報告値**として毎回出してください。

### Q5 賞金表のプレースホルダ → **承認**

`1着100 / 2着40 / 3着25 / 4着15 / 5着10` は選抜指標として妥当な形（上位に厚く5着まで）です。§11 確定時に差し替えてください。

### Q6 クラス分けを正典に明記 → **採用**（D-018・§10.4）

**この指摘が今回いちばん価値がありました。** 「明記がないと P2 の開催スケジューラを別のセッションが実装したときに無作為抽選になり、V-4 が静かに壊れる」— まさにそのとおりで、**較正条件が正典に残っていないと後で壊れる**という R-7 の典型例です。

§10.4 に「出走馬は同格帯（クラス）から組む。無作為だと1番人気勝率が51%になり V-4 が壊れる」と実測値つきで明記しました。

---

## 3. ★レビュー側の発見 — V-2d の「余裕が薄い」は margin の問題ではない（D-019）

指摘2（V-2d +9.08% / 上限 +10%）を数字の並びから追ったところ、**構造的な意味**が見つかりました。

| 選抜方式 | 芝適性の乖離 |
|---|---|
| 代理選抜（P0・能力合計で選ぶ） | **−4.76%（負）** |
| **実レース選抜（P1）** | **+4.66 〜 +9.08%（正）** |

**符号が反転しています。** これは P0 で大物覚醒のラチェットを見つけたときと同じ「署名」です。

原因: §8.3 の乗算補正が使う形質（**芝/ダート適性・距離2種・道悪適性**）は**レース成績を直接押し上げる**ので、実レース選抜を入れた瞬間に**選抜圧がかかります**。代理選抜（能力5種の合計）では、これらは選抜と無関係だったので動かなかった。

**つまりこれは欠陥ではなく、ゲームが設計どおり動いている証拠です。** 走って強い形質を選べば、その形質が集団で上がる — 当たり前のことです。

そして **V-2d の前提（「非能力形質には選抜圧がかからないので水準は動かないはず」）が P1 で崩れました。** 選抜圧のかかる形質に「水準 ±10%」を課すのは、**能力5種に「+50% 以内」を課すのが誤りだったのとまったく同じ構図**です（D-008）。

→ **V-2d を分割**（D-019）:

| 基準 | 対象 | 内容 |
|---|---|---|
| **V-2d** | 丈夫さ（§8.3 に現れず故障率にのみ効く＝選抜圧なし） | 創始水準 **±10%** を維持 |
| **V-2f（新設）** | 芝/ダート適性・距離2種・道悪適性・気性 | **平坦化**（最終20世代の傾きが 0.5%/世代未満）。V-2a と同じ形 |

上がること自体でなく**上がり続けること**を禁じます。分化の維持（V-2e）と天井余裕は従来どおり効かせます。

**この整理により、指摘2の「P3 で割る可能性」は消えます** — 割るのは V-2d ではなく V-2f の傾きで見ることになり、そちらは平坦化していれば通ります。

---

## 4. 自己申告された失敗7件について

報告書 §9 に全件書かれており、うち2件は重い:

1. **変異テストのスクリプトが空振りしたのに「改変した」と出力し、「テストが検出できない」という誤った結論を一度出した** — 前提条件だけ検査して事後条件を検査していない。**M-1 とまったく同じ形の3度目**
2. **棚卸しツールが同一ファイルに2点プローブを挿すと原本を上書きし、本番コードにプローブが残った** — ツール自身の復元検査は検出したが修復できなかった

**1 について**: R-9（「ヒットゼロ」の盲点）と同じ根です。**検証ツールを書いたら、そのツールが「効いていない」ときに気づける形にする**必要があります。→ 恒久ルール **R-11** として追加します（§6）。

**2 について**: レビュー側で独立に確認し、**プローブ残留ゼロ・`git status` 空**を確認しました。復旧は完全です。ただし「ツールが本番コードを書き換える」設計自体が危険なので、**計測は本番ファイルを改変しない方法**（vitest の setup で import をラップする等）に寄せてください。

**3度目の同型ミスを自分で見つけて報告した**ことは評価します。隠されるより遥かによい。

---

## 5. 次便（P1.5 or P2）への申し送り

1. **Q1〜Q4 の正典改訂を実装に反映**（`heavy_aptitude` の追加が最も重い。P0 ゲートの再実行が要る）
2. **V-2f の実装**（V-2d から選抜圧のかかる形質を移す）
3. **K と並べて「出走表の能力CV」を毎回報告**（どちらのレバーが動いたか分かるように）
4. **スタミナ消費率の較正**（Q2 の目標挙動を満たす値を実測で出す）
5. 勝率比（AI代行÷手動最適）を報告値として毎回出す

---

## 6. 教訓の記録

1. **選抜対象が変わると「動かないはず」の前提が崩れる**。V-2d は P0 では正しく、P1 では誤りだった。**基準は前提とセットで書く**（どの選抜方式を想定しているか）
2. **検証ツールにも事後条件が要る**（→ R-11）。「改変した」と出力するツールは、**改変が実際に入ったことを確認してから**そう出力する。M-1（変更が入ったかを確認していない）の道具版
3. **完全な予測器を持つゲームでは、「1番人気の勝率」は予測精度でなく出走表の競争性を測る**。現実の数字をそのまま目標にすると、現実には存在しない補正が必要になる
4. **較正条件を正典に残さないと、別のセッションが静かに壊す**（Q6 の指摘）。これは開発側が自分から挙げた懸念であり、最も価値のある照会だった

---

## 7. 【追補】コード監査の結果 — HIGH 4件。判定を CONDITIONAL PASS に改める

監査は **K-1（式の構造まで正典どおり）・K-2 の乱数系列の独立性（実測で成立）・N-1〜N-5・K-a・自己申告7件の正確性**を追認した。特に:

- **乱数系列の独立性は end-to-end で実証**された。人気推定の系列を意図的に汚染すると1番人気勝率が +1.8pt 動く（＝検定に検出力がある）状態で、本番系列は無関係系列と誤差内
- **race-engine 本体のテストは非常に強い** — 変異試験21件のうち、§8/§8b に関わる改変は**すべて既存テストが捕捉**
- 自己申告7件は**全件正確**で、プローブ残留も完全復旧

**一方で、K-4/K-5 の「較正と是正」の層はテストが1本も守っておらず、V-6 は測定方法の自由変数で通っていた。**

### 7-1. 🔴 A: §8.8 の「介入ログ → 同じ着順」が未実装（未申告）

`InterventionInput`（記録用の型）から `InterventionPlan`（計算用の型）へ**変換する関数が存在しません**。`auditFailures` が見るのはハッシュ整合と出走馬の一致だけで、**着順を再計算しません**。

「seed だけで同じレース」は証明済みですが、**指示書 K-2 と正典 §8.8 が求める「seed + 介入入力ログ → 同じ着順」は成立していません**。介入がある以上、これが無いと**リプレイ・不正調査・サポート対応の一次資料が機能しない**（§8.8 を新設した理由そのもの）。

### 7-2. 🔴 B: V-6 の PASS は測定ノイズの産物（未申告）

`POPULARITY_TRIALS=60`（正典にない開発側の自由変数）を振ると:

| trials | V-4 | V-6 |
|---|---|---|
| **60（既定）** | 32.49% PASS | **0.79% PASS** |
| 600 | 33.61% PASS | **0.27% FAIL** |

60試行では下位馬の大半が「0勝」で並び、**タイブレークが枠順**になります。つまり「最低人気」は最低人気ではなく「**0勝で並んだ中で最も外枠の馬**」でした。正しく測ると **0.27%** で下限 0.5% を割ります。

**これは R-7（較正した条件を明記する）の対象そのもの**です。そして重要なのは、これが単なる測定バグではなく**設計上の問題を露わにしている**ことです:

- 真の最低人気が 0.27% ＝ **「ゼロではない＝夢は残す」（正典 §13.2）が成立していない**
- K を上げれば V-6 は改善しますが **V-4 が下限を割ります**（K=0.32 で 30.6%）
- **現在の出走表の作り方では V-4 と V-6 を同時に満たせない可能性がある**

比較のため: 現実の競馬は1番人気32%・大穴1〜2%（比 20〜30倍）。本実装は 33% 対 0.27%（比 **122倍**）で、**裾が現実より遥かに広い**。したがって対処は「K をさらに上げる」ではなく**出走表の能力幅を締める**（クラス帯を狭める等）方向です。

### 7-3. 🔴 C: `resolveRace` が `interventionMults` を検証・クランプしない（未申告）

実測:

```
interventionMult=5    → 着順 1/8（finalScore ×5）
interventionMult=100  → 着順 1/8（×100）
interventionMult=NaN  → 例外
```

**±10% のハードキャップ（憲法 §1.5-1）は純関数 `resolveIntervention` の中でしか守られておらず、サーバー権威のスコア確定点（§8b.4）である `resolveRace` の入口では素通し**です。

**R-1（単体でなく経路を固定する）の対象そのもので、これが5度目**。しかも今回は「テストが守っていない」ではなく「**実装が守っていない**」段階です。

### 7-4. 🔴 D: K-4 の是正がテストで無防備（未申告）

開発側が「V-1 19.7%→15.7% の是正」として入れたクラス係数を、**適用経路から外す改変を入れると V-2d が実際に FAIL するのに、`npm test` は 196/196 全緑のまま**通ります。テストは純関数 `classPrizeMultiplier` しか見ておらず、`runSeason` が実際に掛けているかを固定していません。

**I-3 / I-4 / L-1 とまったく同じ構造の4度目**です。

あわせて**較正定数（開放率の幅・クラス帯幅・K・母集団のソート）はどれ1つテストで固定されていません**（変異試験 M16/M17/M18 いずれも全緑）。

### 7-5. 🟡 E: `prize` が「在籍年数」を測っている（未申告）

実測: `r(累計賞金, 在籍年数) = 0.353` / `r(累計賞金, 能力合計) = 0.440` に対し、**`r(1走あたり賞金, 能力合計) = 0.503`**。選抜差も `prize/starts` のほうが上（+10.68% 対 +9.33%）。

報告書 §4 の3指標比較（prize / winRate / composite）に**出走数で正規化した指標が入っていません**。`winRate` は分散が大きすぎて V-1 を割ったとのことですが、`prize/starts` は正規化されていて分散も小さく、能力との相関も選抜差も上回ります。**選抜指標は「強さ」を測るべきで「長く走ったこと」を測るべきではありません。**

### 7-6. 🟡 G ほか（中〜低）

- `INTERPRETATIONS` 未登録の解釈が4件（horseId ソート・256bit→32bit 畳み込み・`Math.max(0,…)`・着差ラベルの基準）。**登録簿の走査範囲が `packages/race-engine/src` のみ**で `apps/cli` 側のプレースホルダが対象外。報告書 §7 の「全件」はこの範囲での全件
- `fatigueCoef` / `weightCoef` が非クランプ（`randomMult` には防御があるのに非対称）
- NaN が `clamp()` を素通りする
- `verify-race.ts` の `--json` が `jsonSafeNumber` を通っていない（K-b の宣言が**どこからも使われていない**）

### 7-7. 判定と是正

K-1/K-2（乱数の独立性）/K-3 の**実装本体は正典に忠実**で、テストの検出力も高い。しかし **A・B・C は「PASS の根拠が成立していない」類**であり、D は「是正が守られていない」。

→ **CONDITIONAL PASS**。指示書 `DEV_INSTRUCTIONS_P1FIX_20260805.md` で O-1〜O-8 を出す。

---

## 8. 恒久ルールの追加

| # | ルール | 由来 |
|---|---|---|
| **R-11** | **検証ツールにも事後条件を持たせる**。「改変した」と出力するツールは、**改変が実際に入ったことを確認してから**そう出力する。ツールが空振りしたときに「テストが検出できない」という誤った結論が出る | 開発側の自己申告#1（M-1 の道具版） |
| **R-12** | **測定の自由変数（試行回数・サンプル数・並べ替え）は、判定に効かないことを確認するか、正典に固定する**。効くなら較正条件として明記する | V-6 が `POPULARITY_TRIALS=60` で通っていた |