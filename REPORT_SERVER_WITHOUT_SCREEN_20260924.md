
# 報告: 本番に入っているのに、画面が無い機能 — 2026-09-24

> 裁定 `REVIEW_HORSE_IDENTITY_VERDICT_20260923.md` §15 の 2 に対する調査です。
> ★**「仕組みは在る、誰も繋がなかった」の族**（★今日だけで 2 件見つかりました — 勝負服の色・毛色の出どころ）。
> ★同じ形がほかにどれだけあるかを、**数えられる形**で出します。

## 0. 測り方（★推測していません）

1. `db/migrations/*.sql` から **利用者が呼べる関数**を取り出す（`grant execute on function … to authenticated`）
2. `apps/web/src` から **画面が呼んでいる関数**を取り出す（`.rpc('…')`）
3. **差を取る**。さらに、関数の名前が `apps/web/src` のどこにも現れないことを確かめる

## 1. 🔴 利用者が呼べるのに、画面が 1 行も呼んでいない関数

| 関数 | 何ができるはずか | 状態 |
|---|---|---|
| `buy_horse` | **馬を買う**（NPC の現役馬・`0025` / `0070`） | 🔴 画面から呼んでいない |
| `sell_horse` | **馬を売る** | 🔴 画面から呼んでいない |
| `unlock_stable_grade` | **厩舎の格を上げる**（D-103・EP を払う） | 🔴 画面から呼んでいない |
| `request_initial_breeding` | **初回の配合**（案 A の「無償の生産 1 頭」・D-120） | 🔴 画面から呼んでいない |
| `my_initial_breeding` | 上の状態を読む（`0061` / `0073`） | 🔴 画面から呼んでいない |

✔ 5 つとも、**名前が `apps/web/src` のどこにも現れません**（★註記にも出てきません）。

⚠️ ★`breeding_role_block` / `breeding_role_limit` / `mare_lifetime_foals` も差に出ますが、
★これらは **ほかの関数が呼ぶ部品**で、画面から呼ぶものではありません（★誤検出）。

## 2. これが意味すること

### `buy_horse` / `sell_horse` — 🔴 いちばん重い

★**馬を増やす・減らす経路が、利用者から使えません。**
`/stable/market` という画面はありますが、★**買う RPC を呼んでいないどころか、★出品の一覧も読んでいません**
（✔ 確かめました: `apps/web/src/app/stable/market/page.tsx:14` に「値段は見本の値（`DEMO_MARKET_PRICES_EP`）」とあり、
★`horse_market_listing_public` も `buy_horse` も出てきません ＝ ★**画面まるごと見本**）。

→ 利用者が持ち馬を増やす手段は、いまのところ **①初回の 1 頭 ②配合で生まれる仔** だけです。

### `unlock_stable_grade` — 厩舎の格

★D-103 で「上の格は速く仕上がるだけ・素質の天井は変わらない」と決めた仕組みです。
★`stable_grade_price_public`（値段のビュー）も在り、★`/train` は格を**表示**していますが、
★**上げる手段が画面にありません**。

### `request_initial_breeding` — 🔴 初回の馬の「案 A」が半分

★オーナー決定（2026-09-22）の案 A は「**付与 1 頭 ＋ 無償の生産 1 頭**」でした。
★**付与 1 頭は `create_account` が済ませています**が、★**無償の生産 1 頭の画面がありません**。

→ ★**登録した人は、いま 1 頭しか受け取れません。**

## 3. 逆側: 画面はあるが、中身が仮のもの

| 画面 | 仮のところ |
|---|---|
| `/train`（育成） | ★**調教の指示が保存できません**（画面に「適用は準備中」と出ている）。`set_training_order` は呼んでいるが、★週送りへの反映は未結線 |
| `/stable`・`/mypage` | ★一部が見本データ（`DEMO_HORSES`） |
| `/stable/retired`（馬物語帳） | ★**全部が見本データ**。本人の引退馬は `/stable/roles` 側にある |
| `/race` | ★固定プールのデモ（★実レースの ID を受け取らない） |

## 4. 開発側の見立て（★判断はオーナーとレビュー側）

**優先はこの順**だと考えます。

1. 🔴 **`request_initial_breeding` の画面** … 案 A が半分のままで、★**登録した人が受け取る馬が 1 頭少ない**
2. 🔴 **`buy_horse` の結線** … 画面は既にあり、★**呼ぶだけ**で馬を増やせるようになる
3. **`/train` の調教の適用** … 「準備中」と出したまま日が経っている
4. `unlock_stable_grade` … 上の 3 つより急がない
5. `sell_horse` … 売る必要が出るのは頭数の上限に当たってから

⚠️ ★1 と 2 は **EP を使う経路**なので、★入れる前に台帳の確かめ（予行）が要ります。
⚠️ ★この一覧は ★**RPC の有無**で測ったものです。★「画面が無い」のと「使えない」は別で、
   ★たとえば `/train` は画面が在っても保存できません。★**両方を見ないと実態は分かりません**。
