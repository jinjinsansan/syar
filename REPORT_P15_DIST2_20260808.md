# REPORT_P15_DIST2 — 近交回避 有効での分布 / A-3 の絞り込み

**開発側 → レビュー側** / 2026-08-08
**コミット**: `3987672`（push 済み）

---

## 1. ★「トレードオフは存在しない」は撤回が必要かもしれません

40シード × 50世代（近交回避 **有効**・penalty=0.3）:

| 量 | 平均 | SD | CV | 最小 | 中央 | 最大 |
|---|---|---|---|---|---|---|
| 有効系統数 | **4.42** | **1.43** | 32.3% | 1.75 | 4.37 | 8.00 |
| sire×bms 成立組数 | 95.35 | 37.55 | 39.4% | 36 | 88 | 210 |
| ニックス表ヒット | 6.15 | 2.68 | 43.7% | 1 | 6 | 14 |
| 高F配合の割合% | 13.37 | 5.23 | 39.1% | 8.2 | 12.7 | 36.6 |
| 平均F | 0.0329 | 0.007 | 22.3% | 0.0207 | 0.0329 | 0.0602 |

```
              平均     SD      合格率（有効≥5）
penalty=0     4.86    2.13         45%
penalty=0.3   4.42    1.43         30%
```

**平均が −0.44、SD が −33%**。前便で「トレードオフは存在しない・全水準が互いの SE 内」
と報告したのは **12シードの掃引**（SE 0.26〜0.72）で、**40シードではこの差が見えます**。

D-027 の撤回はこの12シード測定を根拠にしています。
**40シードの結果は「近交回避には系統多様性のコストがある」方向です。**
撤回の撤回が要るかどうかの判断をお願いします。

### 合格率の整合性チェック（ご指摘への答え）

「平均が 0.14 動いて合格率が 18pt 動くのは説明がつかない」はそのとおりでした。
**実際には平均がもっと動いていました**（0.14 は12シードの値・40シードでは 0.44）。
平均 4.42・SD 1.43 なら 5 以上は約 34% で、実測 30% と整合します。

### 副次的な収穫

**V-12a が 40/40**（penalty=0 では 38/40）。平均F は 0.0709 → 0.0329 に半減、
最大でも 0.0602 で全シードが満たします。

### 判断が必要な点

**合格率 30% を「十分な割合」と見るか。** 3〜4シード試せば1つ見つかるので探索は軽い一方、
**系統多様性を下げてまで近交回避を採るか**が上の論点と結びつきます。

推奨シード `20260833`（有効系統数 7.18）は30シードでの探索なので、
この40シード分布とは**別系列**です。固定を提案する前に同系列で取り直します。

---

## 2. A-3 — 原因を裾の分布不一致に絞り込みました

### 打ち切り条件（残差が M で縮まないこと）は満たしました

```
races=1000 固定・seed42
     M  |  単勝払戻率  | 残差
   1000 |   80.42%   | -1.58pt
   3000 |   75.27%   | -6.73pt
  10000 |   75.10%   | -6.90pt
  30000 |   74.77%   | -7.23pt
```

M=3000 以降は 0.5pt 幅で平坦。**凸性は M=3000 で消えており、残差は凸性ではありません。**

⚠️ 前便で「到達点 80.53%（残差1.5pt）」と報告したのは **M=4000 の1点**で、
**凸性の押し上げが下振れを部分的に打ち消していた領域**の値でした。
平坦域の実力は **75% 前後（残差 −7pt）**です。到達点を 5.4pt 良く見積もっていました。

### ★推定手続きは無罪です

レースエンジンも出走表も介さず、既知の真の確率から二項サンプリングで p̂ を作り、
同じ集計をした数値実験（M=4000・20万レース・13頭立て）:

```
                        0〜2%帯の寄与差    払戻率
  純粋な推定手続き          +0.690        86.41%   ← 目標より上
  実システム                −0.176        75%      ← 目標より下
```

**符号が逆**で、差は 0.87。したがって:

- 推定手続き自体は正しい（凸性は理論どおり +0.69 の押し上げを作る）
- **モンテカルロで極小確率を推定することの原理的限界でもない**（限界なら実験も同符号）

実験と実システムの違いは1つだけです。
**実験では MC と本番が同じ分布から引かれ、実システムは `resolveRace` を別シードで呼ぶ。**

→ **MC と本番確定が、裾で同じ分布になっていない。**

### ★これは私が「同分布」と結論した場所です

χ²=6.5（自由度9）で「引き方は同分布」と結論しましたが、
**あの χ² は分布の主要部に支配され、1% 以下の裾を見ていません**。
R-22 の指摘を受けて一般化しながら、**自分の過去の結論を見直していませんでした。**

---

## 3. 撤回します — 21pt は未解明ではなく私の対照設計の誤りでした

`1bfebef` は `verify-payout.ts` の `STREAM` を `{POOL:1, FIELD:2, ODDS:3, FINAL:4}`
から `{31,32,33,34}` に変えています。**`FIELD` が変わった＝出走表そのものが別物**です。

```
旧: generateRace(pool, i, deriveRng(seed, 2, i))
新: generateRace(pool, i, deriveRng(seed, 32, i))
```

「同じ M・同じ races で 21pt 動いた」と書いたものは、**同じレースを比べていませんでした**。
そちらの裾検定（400万サンプル）との食い違いも、食い違いではありませんでした。

さらに同じ便で「P0 ゲートが完全一致 → ID は結果に影響しない」とも書いており、
**自分の中で矛盾した2つの主張を並べていました。**

---

## 4. 次

1. 推奨シードを40シードと同系列で取り直す
2. 裾に絞った分布比較（0〜2% 帯の馬だけで MC と本番を突き合わせる）
3. 上記1の「トレードオフ」判断待ち
