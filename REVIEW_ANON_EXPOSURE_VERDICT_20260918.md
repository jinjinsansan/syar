# 【STAR】裁定 — anon に開いた実体テーブル 3 件（配備の前に決める）

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **出どころ** | `HANDOFF_AUTH_TO_REVIEW_20260918.md` §9-13・§9-16（開発側の報告・staging 実測） |
| **対象** | `horse_story_event`（`0024:97`）／ `horse_market_listing`（`0025:57`）／ `stable_grade_price`（`0027:46`） |
| **正典** | V-20（1654 行）／ 410 行（公開したくなったら別ビューを作る）／ `db/migrations/0018_lock_public_grants.sql` |

---

## 0. 判定

✅ **3 件とも閉じます**（`revoke select ... from anon, authenticated`）。
★**「公開してよい種類だから開けたままでよい」は採りません**（理由は §2）。

🔴 ★**ただし、閉じるだけでは終わりません。** ★**この露出を「合格」として固定していた検査が 3 本あります**（§3）。
★**そこが本体です。** ★開発側が「誰も気づいていない露出」と書いた理由の答えでもあります。

⏱ **順序**: ★**本番へ `0024` 以降を当てる配備と、閉じる移行を同じ配備に入れてください**（§5）。

---

## 1. ✔ 実測 — クライアントは 3 表を 1 つも読んでいません（★閉じても何も壊れません）

★開発側の報告（露出の事実）は全件裏付けが取れました。★その上で、**閉じてよいかを別に確かめました。**

| 確かめたこと | 結果 |
|---|---|
| `apps/web` が読んでいる表・ビュー | ✔ **`races_public`（8 か所）／ `race_entries_public`（4）／ `race_odds_public`（4）だけ**（`.from('…')` の全数） |
| `apps/web` の `.rpc(…)` | ✔ **0 件** |
| `apps/web` から 3 表への参照 | ✔ **0 件**（`horse-story-demo.ts:5` の**註記の中に名前が出るだけ**で、読んではいません） |
| 3 表を実際に読み書きしている場所 | ✔ **ワーカー**（`story-flow.ts` / `story-daily.ts` / `market-flow.ts` / `grade-flow.ts` — Postgres 直結）と ✔ **RPC の本体**（`security definer`）だけ |

→ ★**anon / authenticated の `select` を剥がしても、壊れる経路は 1 つもありません。**
★ワーカーも `security definer` の RPC も、この 2 つのロールの権限を通らないからです。

---

## 2. なぜ「公開してよい種類だから開けたまま」を採らないか

★開発側は `stable_grade_price`（格と価格）と `horse_market_listing`（★と価格）を「**公開してよい種類**」と書きました。
★**中身の判断としては同意します。** ✔ §9-16 のとおり `horse_story_event.detail` も 3 鍵だけで §12.4 には触れません。

★**それでも閉じます。理由は中身ではなく、形です。**

| # | |
|---|---|
| 1 | ★**`0018` 自身が「実体テーブルには一切 grant しない」と宣言しています**（✔ `0018_lock_public_grants.sql:52`）。★`revoke all on all tables` ＋ `alter default privileges` で閉じたうえで、★**公開するものは 4 つの `*_public` ビューにだけ grant** しています（`:54-57`）。★3 表は**その宣言の後に足された実体テーブル**です |
| 2 | ★**V-20 ② の合格条件は「公開ビューとして明示登録されたもの以外は anon から 0 行」**（✔ 正典 1654 行）。★「公開してよい中身かどうか」ではなく、★**ビューとして登録されているかどうか**で書かれています |
| 3 | ★**正典 410 行が、まさにこの誘惑への答えを先に書いています** — 「将来 馬主名を公開したくなったら、残高を持つ表にポリシーを足さず **`users_public` を別ビューとして作る**」 |
| 4 | ★**実体テーブルは列が増えます。** ★今は無害な 3 鍵でも、★**列を 1 本足した日に露出が広がり、その日は誰も V-20 を読み返しません**（★`horse_story_event.detail` は `jsonb` なので、★**列を足さなくても中身が増えます**） |

→ ★**公開が要るようになったら、その時に `*_public` ビューを作ってください**（`races_public` と同じ形）。
★**今は UI が 1 つも読んでいないので、ビューも要りません。**

---

## 3. 🔴 ★本体 — 3 本の検査が「露出していること」を合格条件にしています

★**revoke だけを入れると、この 3 本が赤くなります。** ★先に知っておいてください。

```
apps/cli/test/entry-rpc-migration.test.ts:91
  expect(sql).toMatch(/grant\s+select\s+on\s+horse_story_event\s+to\s+anon,\s*authenticated/i)
apps/cli/test/horse-market-migration.test.ts:66
  expect(body).toMatch(/grant select on horse_market_listing to anon, authenticated/i)
apps/cli/test/stable-grade-unlock-migration.test.ts:32
  expect(body).toMatch(/grant select on stable_grade_price to anon, authenticated/i)
```

★**3 便とも、同じ行の 1 つ上で `revoke insert, update, delete, truncate` を正しく要求しています。**
★**書き込みは塞ぎ、読みは開ける** — ★その形を、★**検査が 3 回続けて合格として固定しました。**

★これが **R-16 の家族で、最も静かな形**です:

| | |
|---|---|
| ★なぜ 3 便とも同じ形になったか | ★**前の便の検査が手本になった**からです（2 便目・3 便目は 1 便目の形を写しています） |
| ★なぜ V-20 ③（登録簿に無い表が現れたら落ちる）が鳴らなかったか | ★3 表は `EXPECTED_EXPOSURE` に**未登録**です（✔ `tools/lib/exposure-registry.mjs` に 3 件とも無し）。★**それでも 46 本は緑**でした — ★あの 46 本は**合成名で判定の論理を固定するだけで、実 DB を見ません**（★前夜の R-21 と同じ形） |
| ★では何が捕まえたか | ★**staging の実 DB に対して V-20 を回したこと**だけです。★**開発側の捕まえ方は正しい**です |

★**教訓としてはっきり書きます**: ★**検査は「あるべき姿」を書くものであって、「今そうなっている姿」を書くものではありません。** ★移行ファイルの文字列を写す形の検査は、★**書いた瞬間の誤りごと凍結します。**

---

## 4. 指示（AE-1〜AE-4）

| # | 指示 |
|---|---|
| **AE-1** | ★**新しい移行 `0032` で 3 表の `revoke select ... from anon, authenticated` を入れる。** 🔴 ★**`0024` / `0025` / `0027` は書き換えないこと** — ★`migrate.mjs` が「適用済みのファイルを書き換えず、新しい番号で足してください」と拒否します（✔ 開発側が §9-14 で実証済み。★staging には既に当たっています） |
| **AE-2** | ★**`EXPECTED_EXPOSURE` に 3 表を `CLOSED` で登録する**（V-20 ③）。★**1 行ずつ「なぜ閉鎖か」を書く**（既存の登録簿の作法に合わせる。例: `horse_story_event` は `detail jsonb` を持ち**列を足さなくても中身が増える**ため） |
| **AE-3** | 🔴 ★**検査 3 本の期待を反転する**（§3）。★`grant select … to anon` を**要求する**形から、★**「無いこと」を要求する**形へ。★併せて **1 行ずつ「なぜ反転したか」**（`0018` の宣言・V-20 ②・`0032`）を残すこと。★**これが無いと、次の便がまた前の便を手本にします** |
| **AE-4** | ★**staging の実 DB で測って報告する。** ★**両方向を出すこと**: ① 3 表が anon から **0 行**（V-20 ②） ② ★**公開ビュー 4 つ（`races_public` / `race_entries_public` / `race_odds_public` / `prize_catalog_public`）が anon から**引き続き**読めること**（★正典 1664 行「**塞ぎすぎていないことも測る**」。★**最も安易に満たす方法が「全部閉じる」**なので） |

★**この便で他のことをしないでください**（★着順・経済に効くものを混ぜない）。

---

## 5. ⏱ 本番への順序（★ここが「配備の前に決める」の中身）

✔ **今の本番に露出はありません** — `0024` / `0025` / `0027` が**未適用**だからです（開発側の実測と一致）。
🔴 ★**つまり、本番に `0024` 以降を当てた瞬間に、露出が現実になります。**

```
本番:  0021 → 0022 → 0023 → 0024 → 0025 → 0026 → 0027 → 0028 → 0029 → 0030 → 0031 → ★0032（閉じる）
                                ↑ ここから ★0032 までの間、anon に開いた状態になります
```

★**推奨**: ★**`0024`〜`0032` を 1 回の配備にまとめる**（間に人の手が入らない形にする）。
★本番の利用者は 0 人なので**実害は出ませんが**、★**「窓が開いたまま次の判断を待つ」形を作らないこと**が目的です。
★もし配備を分けるなら、★**`0024` 以降を当てる回に `0032` を必ず同梱**してください。

---

## 6. 🔴 オーナーの承認が要るもの

| # | |
|---|---|
| 1 | ★**§0 の判定そのもの**（3 表を閉じる／「公開してよい種類だから開けたままにする」を採らない） |
| 2 | ★**§5 の配備順**（`0024`〜`0032` を 1 回にまとめる） |
| 3 | ★**起票の可否 1 件**: 「**実体テーブルに `grant select` を足さない。公開が要るなら `*_public` ビューを作る**」を正典に 1 本立てるか。★`0018` のコメントと V-20 ② には既にありますが、★**3 便続けて破られた**ので、★**決定として引ける形にする価値があります**。★番号は承認が出てから振ります（★出していない番号を先に書かない） |

---

## 7. ★私の側の確認（作法）

- ✔ 本書のすべての主張は `star/` のコードと正典で確かめました（★開発側の報告を写した箇所はありません。★§9-13 の「§14.3 に反する」という引用だけは**確かめた結果が違いました** — ★正典 §14.3 は Vercel / Supabase / ワーカーの役割分担の節で、「読み取りはビュー経由に一本化」はそこには**ありません**。★**同じ趣旨は V-20 ②（1654 行）と 410 行と `0018:52` にあります**。★指摘の中身は正しく、参照だけが違いました）
- ✔ 本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）
