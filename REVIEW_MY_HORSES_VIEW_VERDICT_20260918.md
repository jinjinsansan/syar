# 【STAR】裁定 — `0034` 自分の馬のビューを受理

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **対象** | コミット `f79b320`（`db/migrations/0034_my_horses_view.sql`） |
| **指示** | `REVIEW_UI_WIRING_VERDICT_20260918.md` §3・§6 |

---

## 0. 判定

✅ **PASS（条件なし）。**

🔴 ★**`birth_snapshot` は私の一覧から漏れていました。★開発側が見つけて塞いでいます**（§2）。

---

## 1. ✅ 照合

| # | 条件 | ✔ |
|---|---|---|
| 出す列 | 血統（`sire_line`/`dam_sire_line`/`sire_id`/`dam_id`/`pedigree_cache`/`generation`）・調子/疲労・年齢/キャリア・繁殖・厩舎の格・`g1_wins` | ✅ 裁定 §3 の線どおり |
| 出さない列 | `genotype`/`potential`/`stats`/`unlock_rate`/適性の生値/`growth`/`temper`/`durability`/`frail`/`skill_genes`/`inbreed_coeff`/`nicks_multiplier` ＋ ★`birth_snapshot`・`npc_stable_id` | ✅ ★**2 列を自分で足している** |
| 絞り | `where owner_id = auth.uid()` ／ ★**`authenticated` にだけ grant・`anon` には revoke を明示** | ✅ |
| 登録簿 | `my_horses: OWNER_SCOPED`（V-20） | ✅ |

✔ **レビュー側も独立に再実行** — `my-horses-view` ＋ `exposure-registry` の **33 件すべて緑**。

---

## 2. ✅ 開発側が自分で足した 2 件 — どちらも正しい

### 2-1. 🔴 ★`birth_snapshot` — **私の一覧の漏れです**

✔ `0012` の註記を確認しました:

> `birth_snapshot` ＝ ★**誕生時の表現型（`potential` / `stats` / `durability` / `temper`）の写し**

→ ★**出せません。** ★**私が §3 で列を仕分けたとき、`0001_init.sql` の列しか見ておらず、★後から `alter table` で足された列を見ていませんでした。**
★**開発側が実在を確かめて足したのが正しい**です。

### 2-2. ✅ 発見度に列は要らない

✔ `packages/sim-engine/src/discovery.ts:35` — `discoveryStageOf(relevantRuns)` は ★**その条件での出走回数から決まる純関数**。
→ ★**戦績から計算できます。** ★**D-108「表示の層だけに持ち、内部値に触らない」と整合**します（★09-16 に私が確認した「回数だけを取り素質を受け取れない」形そのもの）。

---

## 3. ★★検査の形を評価します — **今日いちばん良い検査です**

```
apps/cli/test/my-horses-view.test.ts:14
  ★horses の全列を移行ファイルから組み立て、
  ★1 列ずつ「出す」か「出さない」かに分類されていることを要求する
  （R-29「既定を閉じて、必要なものだけ開ける」。★列を足した人は、どちらかに入れるまで通れません）
```

| # | なぜ良いか |
|---|---|
| 1 | ★**禁止語の一覧で書いていません**（D-108 ③・★D-098 の検査で踏んだ穴）。★**「出してはいけない列の名前」を数えるのではなく、★「分類されていない列があるか」を見ています** |
| 2 | ★★**R-29 を検査に適用した初めての形**です。★今日 `revoke all` の件で学んだ「★**列挙は必ず漏れる**」を、★**列の仕分けにそのまま持ち込んでいます** |
| 3 | ★**`create table` だけでなく後から足した `alter table` も集めている** — ★**まさに私が漏らした経路**（`birth_snapshot`）を、★**検査が構造で塞いでいます** |
| 4 | ★**検査が自分の穴を捕まえた**（列名の regex `[a-z_]+` が `g1_wins` の数字を拾えず、「出ているはず」が出ていなかった）。★**書いた本人の誤りを、書いた検査が先に見つけた**形です |

★**この形を、他の「開けてよい／いけない」の判断にも広げてください。** ★**`EXPECTED_EXPOSURE` は既にその形です**（登録簿に無い表が現れたら落ちる・V-20 ③）。★**今日 4 例あった「登録簿の外で増える」問題の、根本的な対処がこれです。**

---

## 4. ✅ UI-1 の着手 — ★**「自分の馬」の画面以外は進めてよい**

★**「育った実感」の答えを待つのは、★『自分の馬』の画面だけ**です。

| 部分 | 待つか |
|---|---|
| 登録 → セットアップ（`create_account`） | ✅ ★**待ちません。進めてよい** |
| 出走登録（`enter_race`・`ST001`/`ST002` の文言） | ✅ ★**待ちません** |
| ★**自分の馬の一覧・詳細** | 🔴 ★**待ちます**（★何をどう見せるかが決まらないと形が決まらない） |

---

## 5. ★本番へ当てる範囲が増えました

```
本番: 0021 → … → 0032 → 0033（クラス分け）→ ★0034（自分の馬のビュー）
★1 回の配備にまとめる方針は変わりません
```

★**`0033`・`0034` とも staging 未適用**です（開発側の申告）。★**配備の前に staging で当てて確かめてください。**

---

## 6. 🔴 オーナー判断待ち（★変わりません）

| # | |
|---|---|
| 1 | ★**「育った実感」をどう返すか**（★調子・疲労／発見度／物語の 3 つで足りるか。★足すなら `stats` の露出にならない形で） |
| 2 | ★**本番配備の指示**（★技術的な前提は揃っています） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
