# 【STAR】裁定 — T-13（3 分サイクル・`CYCLES_PER_WEEK` 80）の受理

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **対象** | コミット `55d1ba7` ／ `REPORT_T13_20260918.md` |
| **正典** | **D-007 改訂**（2026-09-18・`b929056`）／ **D-100** |

---

## 0. 判定

✅ **CONDITIONAL PASS** — 条件は **1 件だけ**です。

| # | |
|---|---|
| 実装・実測 | ✅ **受理**（§1）。★**不変であるべきものが不変であることを、レビュー側も独立に実測** |
| 🔴 **T13-1** | ★**`CYCLES_PER_WEEK` は直書き（80）に戻してください**（§2）。★**判断の根拠は、既にある検査です** |
| `main.ts` の発見 | ★★**本便でいちばん価値のある仕事**（§3） |

---

## 1. ✅ 実測（★レビュー側が独立に確かめたもの）

```
CYCLE_MS         3 分
CYCLES_PER_WEEK  80
WEEK_MS          4 時間 ✔
WEEKS_PER_DAY    6 ✔
RACES_PER_DAY    480 ／ クラス合計 480 ✔ 一致
G1_SLOTS         180, 260, 400 → 09:00 / 13:00 / 20:00 ✔
PHASE            settle=0s  publish=20s  salesOpen=30s  salesClose=150s  parade=150s  start=180s ✔
現役 90 週        15 日 ✔
```

→ ★**積み上げる実時間は 1 日も動いていません。動いたのはレース密度だけ（144R → 480R）。** ★D-007 改訂のとおりです。

✔ **レビュー側も検査を独立再実行 — 13 ファイル・160 件すべて緑**（`packages/scheduler` ／ `cycle-runner` ／ `training-runner` ／ `first-pass-time-of-day`）。

### 1-1. ✅ `G1_SLOTS` は導出になりました

✔ `G1_HOURS_JST` を正とし、`(hour × 60) / MINUTES_PER_SLOT` で枠を出し、★**割り切れなければ投げます**（R-27）。
✔ ★**`G1_DAYS` の枠番号の直書きも `G1_SLOTS` 参照に直っています** — ★**私が指摘したのは `G1_SLOTS` だけでした。`G1_DAYS` は開発側が自分で見つけた分です。**

### 1-2. ✅ 検査は「通すために緩める」形になっていません

| 検査 | 直し方 | 評価 |
|---|---|---|
| `prize.test.ts` | 新馬 42 本の直書き → ★**「最も本数が多い」という性質**に置き換え | ✅ ★**同じ数を 2 か所で持たない**（D-052）。★**数を追いかけるのではなく、性質を固定した**のが正しい |
| `cycle-runner.test.ts` | 固定時刻 4 分 → ★**`CYCLE_MS × 0.4`** から取る | ✅ ★10 分なら 4:00・3 分なら 1:12 で**どちらも発売中の相**。★**時刻ではなく相を固定した**のが正しい |
| `selfcheck.test.ts` | ★**配る物（`dist/worker.cjs`）が古いことを検査が捕まえた** → 再ビルド | ✅ ★**R-28（本番が別のプログラムなら、ゲートは全部通ったまま本番だけが守られていない）そのもの**を、検査が自分で捕まえています |

★**「私の洗い出しは 7 本、実際は 10 本でした」と差を明記したのも良い形**です（★自分の見積りの外れを隠していない）。

---

## 2. 🔴 T13-1 — `CYCLES_PER_WEEK` は直書き（80）に戻してください

### 2-1. 何が違うか

| | 向き |
|---|---|
| **正典 D-100** | 「**ゲーム内の 1 週は「サイクルの本数」で持つ**」「`WEEK_MS = CYCLE_MS × CYCLES_PER_WEEK`」 → ★**本数が正、実時間が導出** |
| **実装**（`week.ts:37-46`） | `CYCLES_PER_WEEK = WEEK_REAL_MS / CYCLE_MS` → `WEEK_MS = CYCLE_MS × CYCLES_PER_WEEK` → ★**実時間が正、本数が導出** |

★**向きが逆です。**

### 2-2. ★決め手は「既にある検査」です

✔ `packages/scheduler/test/week.test.ts:31` が **`expect(WEEKS_PER_DAY).toBe(6)`** を固定しています。
★**この番人が働くのは、直書きの側だけです。**

| 次に `CYCLE_MS` を変えたら | **直書き 80**（D-100 の向き） | **導出**（今の実装） |
|---|---|---|
| `CYCLES_PER_WEEK` | 80 のまま | ★**黙って変わる** |
| `WEEK_MS` | ★**変わる** | 4 時間のまま |
| `WEEKS_PER_DAY` | ★**6 でなくなる** | 6 のまま |
| `week.test.ts:31` | ✅ ★**赤くなる（気づける）** | 🔴 ★**緑のまま** |
| 1 週のレース数（＝**較正**） | 80 のまま ✔ | 🔴 ★**黙って動く** |

★★**D-100 が守ろうとしたのは、まさにこれです** — 「**本数を据え置くかぎりゲーム内の較正（1 週のレース数・調教・成長・故障）は間隔を変えても不変**で、変わるのは実時間あたりの量」。

→ ★**導出にすると、次に間隔を変えた日に、較正が黙って動きます。**

### 2-3. ★開発側の懸念（二重帳簿）は、その検査が既に解いています

> `week.ts` 自身が「数値を別々に書くと片方だけ動かしたときに静かにずれる」と書きながら直書きだった

★**もっともな指摘です。** ★**しかし「4 時間」はコードに 2 度書く必要がありません** — ★**`WEEKS_PER_DAY === 6` の検査が期待値として持っています。**

```
直書き 80  ＋  week.test.ts:31（WEEKS_PER_DAY === 6）
  = ★二重帳簿にならず（4 時間はコードに書かない）
  ＋ ★D-100 の向きに従い
  ＋ ★間隔を変えた日に「較正が動く」ことが検査で見える
```

★**これが 3 つとも満たす唯一の形**だと判断します。

| # | 条件 |
|---|---|
| **T13-1** | ★`CYCLES_PER_WEEK` を **`= 80` の直書き**に戻し、`WEEK_MS = CYCLE_MS × CYCLES_PER_WEEK` の導出は**そのまま残す**（★D-100 の式のまま）。★**註記に「4 時間は `week.test.ts` の `WEEKS_PER_DAY === 6` が守る。ここに 4 時間を書かない」**と残すこと |

★**それ以外は差し戻しません。** ★逸脱を**先に報告する形**は、今回も正しく働きました。

---

## 3. ★★`main.ts` の発見 — 本便でいちばん価値のある仕事です

```
apps/worker/src/main.ts（直す前）
  周=${(cycleMs / 1000)}s(${((cycleMs / 600000) * 100)}%)       ← ★600000（10 分）の直書き
```

★**3 分では、周の使用率が実際の 1/3.3 に見えます。**

★**なぜこれが重いか** — ★**この行の出力こそが、R-28 の最後の条件（配備後の周の実測）で読む数字そのもの**です。
→ ★**AL-6 の換算（39〜54%）を本番で突き合わせる根拠**がこの行にあります。
→ ★**気づかずに配備していたら、「使用率 12〜16%・まだ余裕がある」と読み、★AL-7（超えるなら間隔か試行数）の判断を誤っていました。**

★**これは、D-007 改訂の条件のどこにも書いていませんでした。** ★**私の指示の外で、開発側が走査して見つけたものです。**
★**R-30（測定器が実物と同じ入力を読んでいるか、毎回確かめる）の、最も価値のある形**として記録します。

---

## 4. 次

| 順 | 中身 | 持ち手 |
|---|---|---|
| 1 | **T13-1**（`CYCLES_PER_WEEK` を直書きに戻す） | 開発側 |
| 2 | ★**較正の取り直し** — V-4・V-5・V-6・V-14・§3.4 の収支・**帯の下のゲート**を **GB-6 と 1 回で**（AL-11。★二度測らない） | 開発側 ／ ★**クラス分けの便の後**（Q-BAND-04） |
| 3 | ★**配備後の周の実測** — ★**`main.ts` が直ったので、正しい数字が出ます**。★AL-6 の換算（39〜54%）と突き合わせる → ★**ここで R-28 の最後の条件が閉じます** | オーナー（配備）＋開発側（報告） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
