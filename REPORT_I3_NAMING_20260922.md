# 報告: 仔の命名（PLAN I-3・D-120 ⑥ N-2/N-3）— 2026-09-22

- 開発側 Claude（`star-aa`）
- 対象の裁定: `ff7028c`（命名前の仔の置き場所・命名の検査）／`f117984`（I-3 の実装前の論点・§5〜§9 の追記）
- コミット: `a42628b`（段 0・1）→ `a0d5f0d`（予備の名前）→ `ecd6f0d`（段 2）→ `ead29c4`（受付と確定）→ `0bad015`（段 3）→ `6668f0c`（一意違反の警報）
- **到達点: staging で完了**（本番は下見だけ・読むだけ）。画面は無い

## 1. 作ったもの

| 段 | 中身 |
|---|---|
| 規則 | `checkPlayerHorseName`（`packages/sim-engine`・画面とワーカーで共有）: カタカナ・長音・中黒・表示 2〜9 文字・正規化後 2 文字未満は弾く・半角カナは NFKC で全角に。`sim-engine` に `node:` の読み込みが無いことを検査で釘付け |
| 段 0 | `horses` に書く 4 か所（NPC の仔・`seed-world`・`verify-a6`・命名の確定）が `name_key` / `name_checked_with` を書く（列が在るときだけ）。NPC の仔は `generateHorseName`（名前の乱数は遺伝と別の流れ）。決まらなければ予備の名前（接頭辞 ＋ ID の 6 文字）、予備も使えなければ週ごと投げる |
| 段 1 | `0064`: 列を足す |
| 段 2 | `tools/backfill-name-key.mjs`（下見が既定・本番は関門 3 つ）。数えは純関数 `tools/lib/name-key-survey.mjs` |
| 受付と確定 | `0065`（`request_foal_name` / `my_foal_request`）・`apps/worker/src/player-naming.ts`（形・禁止名・重複 → 下書きから `horses` に 1 行・本人の持ち馬） |
| 段 3 | `0066`: 空の行を数えて 1 行でもあれば投げる → `not null` → `unique (name_key)` |
| 一意違反 | セーブポイントの引き直しは入れない（裁定 §9 で取り下げ）。取引ごと戻して次の周にやり直し、**戻った回数を数えて警報**（NPC の週・命名の要求の両方） |

## 2. 測ったこと

| | 結果 |
|---|---|
| 門 | 最後の `6668f0c` で 5 段合格（検査 2,814 件・赤 3 件・登録 3 件） |
| staging の埋め込み | 7,370 頭を埋めた。2 回目・3 回目は書く行 0 |
| staging の制約 | 取引の中で: 重複 → `horses_name_key_unique` で拒否／空 → not null で拒否／**対照の新しい名前は入る**。⚠️ 確かめる途中で交絡が 2 回（母・週の一意、`processed_after_birth`）。対照まで拒まれたので気づき、外して測り直した |
| staging の実演 | NPC の配合 5/5（仔 12 頭の `name_key` が正規化した名前と一致・既存とも仔どうしとも重ならない）／プレイヤーの配合と命名 29/29（命名 → 本人の `my_horses` に現れる・他人の仔を拒む・2 つ目の名前を拒む・命名後も NPC が同じ母に産ませない） |
| 本番の下見（読むだけ） | 馬名 7,370 頭・正規化で重なる組 0・正規化で空 0。初回の配合の父母候補は staging と一致（母 1,897 頭・残り 8,907 頭ぶん・種牡馬の余り 4,000 回） |

## 3. 途中で見つけたこと

1. 本番には `name_key` の列が無いので、下見の道具が判定不能で止まった。**下見だけ**名前から数えられるように直した（書くときは従来どおり止まる）
2. 予備の名前も使えずに全頭を見送ると、先に「供給が止まっています」が投げて原因が名前だと読めなかった。名前の判定を先に置いた（検査で見つけた）
3. 命名の後に NPC の判定 ⑦ が赤になったのは、**実演の数え方**がプレイヤーの仔を NPC の仔と数えていたため。二重の出産ではない（命名の後も下書きは残り、NPC 側の判定は効く）
4. `migrate-guard.test.ts` の `@ts-expect-error` は直後の 1 行にしか効かない。import を折り返すと効かなくなる

## 4. 未完了（完成と呼ばない）

| | |
|---|---|
| 本番への適用 | 未。順序: 本番の血統修復 → `0060` → 配備 → `0061`〜`0066` → `backfill-name-key --apply`（本番の関門）。すべてオーナーの許可が要る |
| 禁止名（実在馬名）の NG リスト | 無い（`NG-NAMES-UNWIRED`）。いまの命名・NPC の仔は `name_checked_with = null`（未検査）。リストが届いたら null の行を全部検査し直す道具が要る |
| 不快な語・人名・商標 | 仕組みが無い（`PLAYER-NAME-OFFENSIVE-WORDS-UNCHECKED`・owner: review） |
| 画面 | 父母選択・誕生・命名・78 週より遅い命名の説明（調教費がまとめて引き落とされる）。デザイナー待ち |
| 導入の永続状態 | PLAN の次の項目 |
