# 【STAR】裁定: 🔴 EP が入ってくる経路が無い ／ 出走登録の 4 件 ／ 旧世代の画面 10 枚 — 2026-09-25

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa` の報告（本番の EP 残高 0・出走登録が 1 件も無かった・旧世代の画面）

## 1. 🔴 EP の日次付与 — ★レビュー側の見立て: **「実装が無い」が最有力**

✔ [CODE] レビュー側が `ep_ledger` に**書く側を全部**走査した（`insert into ep_ledger` と `reason = 'inflow'`）:
- `'inflow'` を書くのは ★**口座を作る 3 か所だけ**（`0031:183`・`0037:219`・`0067:100` ＝ `create_account` の系譜）
- ほかに書くのは `payout` / `cancel` / `scratch` / `prize-award` / `player-breeding`（種付料）／`place_bet`（`0002`）／`exchange_prize`（`0008`）／調教（`0013`・`0014`）／`0020` の RPC
- ★**日次のログインで付与する口は、どこにも無い**（`DAILY_LOGIN`・`daily` で引いても、当たるのは**画面の表示**と**較正の註記**だけ）
- ✔ 正典 **D-075** は額を決めている（登録時 2,000／★**デイリーログイン 200**／日次上限 10,000）。★**額は在るのに、渡す側が無い**

→ ★**D-119 の族（3 例目の大物）**: 「**決めた・画面は出す・渡す側が無い**」。★開発側の 3 つの可能性のうち **①「処理が無い」**。★確かめるのは `grep -rn "'inflow'" db/migrations apps` の 1 回で足りる（★私がやった）

### 🔴 これは公開を止める欠陥
- ★調教はワーカーが毎週 EP を吸う。★入る経路が無いので、★**すべての利用者がいずれ 0 になり、何もできなくなる**（★本番のオーナーの口座が既にそうなった: `inflow +2,000` / `training −2,000` / 残高 0）
- ★**「遊べない」状態**なので、★**画面の不具合より重い**。★次の便の最優先

### 実装の条件（★決め直しではなく D-075 の実装）
1. ★**サーバーの時計で日を決める**（`now()`）。★クライアントの日付を受け取らない（憲法 §1-4）
2. ★**1 日 1 回**を機械で保証する: 台帳の `dedupe_key` に「`daily:<user_id>:<サーバーの日付>`」。★**同じ日に 2 回呼んでも 1 行**
3. ★**額は較正定数から**（D-075・200 EP）。★**SQL に数を書かない**か、書くなら**試験で定数と突き合わせる**（`0070` の上限と同じ作法）
4. ★**日次上限 10,000 EP**（D-075）を、★**この経路も含めて**効かせる。★上限の判定も 1 か所
5. ★`point_flow_daily` の `ep_inflow`（`0001:262`・`0009`）に**載る**こと。★V-11（純発行量）の監視に入る
6. ★**押した人にだけ渡す**（ログインした本人・`auth.uid()`）。★ワーカーが全員に配る形は取らない（★眠っている口座に発行し続けると V-11 が崩れる）
7. ★試験: ①同じ日に 2 回呼んで 1 行 ②日付が変わると 1 行増える（★サーバーの日付を動かして）③上限に当たると拒む ④台帳の `balance_after` と残高が一致

## 2. ✅ 出走登録の 4 件 — 受理。★**「誰も一度も登録できていなかった」が本体**
- (a) 受け付ける段が DB と画面でずれていた（D-117 で `announced` になったのに画面は `scheduled`）
- (b) ★**「登録する」が `<span>` で `onClick` が無かった**（`supabaseEntryRepo.enter` は在るのに誰も呼んでいない）→ ★**D-122 の族**。★0 件の主因
- (c) ★**同じ直書きが 2 か所**に在り、直したのは画面が使っていない方（`entry-repo.ts` を直したが、画面は `entry-screen.ts` を使う）→ ★今日 何度も出ている形
- (d) ログインしていない人に `permission denied for view my_horses` が生で出て、★**公開のレース一覧まで道連れ**で消え「今週は出走できるレースがありません」と**嘘**を出した
- ✔ 網（`entry-stage-matches-rpc.test.ts`）が**最新の `enter_race` の定義から段を取り出して画面の定数と突き合わせる**のは正しい。✔ ★**一律に禁じず経路に絞った**判断も正しい（★投票の `scheduled`・結果の `settled` は正しい設定。★一律に禁じると正しいものまで落ち、網を外す圧力になる）

## 3. ⚠️ 旧世代の画面 10 枚・同じ役割が 2 つ
- ✔ `/race` `/training` `/stable` `/lp-arcade` `/entry` `/prizes` `/stable/retired` `/stable/market` `/races` `/records` が旧世代。★`/training` と `/prizes` は**新版（`/train`・`/exchange`）が既に在る**
- ★**同じ役割の画面が 2 つ在ると、オーナーの「白い古いデザインが残っている」は必ず再発する**（★どちらを直したか分からなくなる）
- **決定**: ★**1 枚ずつ「捨てる／作り直す／新版へ送る」を決めて記録する**。★**新版が在るもの（`/training`・`/prizes`）は、まず新版へ送る**（★同じ役割を 2 つ残さない）。★捨てる判断は入口の有無と合わせて（D-122）
- ★網: ★**旧世代の殻（Shell）を使っている画面を挙げる**（`own-header-coverage` は前者しか見ていない）。★件数を釘付けして、増えたら落ちる形に

## 4. ⚠️ 「例外も赤も出ないずれ」が今日 7 件
★開発側が自分で数えた（★網で見つけたのは 2 件・残りは画面を開くか原文を読んで偶然）。★**この数を減らすことを目標にしない**（★偶然 見つかる分は減らない）。★代わりに ★**「見つけた 7 件それぞれに、次に同じものを機械で捕まえる形が在るか」**を 1 行ずつ書く。★無いものは「無い」と書く（★簿に載せる）。

## 5. 追記（2026-09-25）— 設計の 3 点（a)(b)(c) への答え

### (a) 額の置き場所 — ✅ **開発側の案を採る**（RPC は引数を受けず、SQL 側に 1 か所・TS の定数と検査で結ぶ）
- ★**画面から額を渡す形は採らない**。★憲法 3「利用者の申告を使わない」に触れる（★`my_onboarding_state` の閾値は**表示の目安**で、額＝**発行量**とは重みが違う）
- ★形は `0074` の `breeding_role_limit` ＋ `ownership-limits-sql.test.ts` と同じ（★最新の定義から数を取り出し、TS の定数と突き合わせる）
- **条件**: ★**初期 2,000 の直書き 2 か所（`0031:140`・`0037:178`）も同じ試験に入れる**。★いま**最新の定義は `0067`**（`:100` で `v_grant` を書く）なので、★**最新の定義から取り出す**形にする（★§1 の R-1 と同じ罠）

### (b) 日次上限 10,000 の数え方 — ⚠️ **「発行」だけを数える。★分類を 1 か所に持つ**
- ★`delta > 0` を全部数えるのは**採らない**。★**払戻（`payout`）や賞金が上限に当たって止まる**形になり、★大きく当てた人が受け取れなくなる（★上限の目的は「バグ・エクスプロイトの被害上限」・D-075）
- ★`refund` を入れないのは正しい（★取ったものを返しているだけ）
- 🔴 ★**`inflow` だけでは足りない**: ✔ `horse_sale`（`0026:126`）は**手放した馬の代金を EP で渡す**＝★**新しい EP が出る**（★`0026` の註記自身が「`inflow`（デイリーなどの発行）とは別に持つ — ★**発行量の監視で混ぜないため**」と書いている＝**別掲するが発行である**）
- **決定**: ★**`reason` ごとに「発行か否か」を決める表を 1 か所に持ち**、★**日次上限の判定と、V-11（純発行量）の監視の両方がその表を読む**。★いま発行に当たるのは `inflow`・`horse_sale`（★増えたらこの表に足す）。★表に無い語が来たら**止める**（★黙って「発行でない」にしない）
- ★これで「上限の数え方」と「監視の数え方」が**ずれない**（★ずれると、片方だけ直したときに気づけない）

### (c) 繰り越さない — ✅ そのとおり
- ★押さなかった日は消える。★繰り越すと「1 週間 放置して 1,400 EP」になり、★D-075 の「これだけでは能動的に遊べないが離脱もしない」から外れる。★開発側の読みが正しい
- ★**画面の言葉で「その日のぶんは翌日に持ち越せません」と先に言う**（D-123 の作法と同じ・押す前に言う）

## 6. ④ の「機械で捕まえにくい」件 — ⚠️ **「無い」の前に 1 つ試してほしい**
★(b)（ボタンが `<span>` で `onClick` が無い）について、★**部分的には機械で捕まえられる**:
- ★**進む語を持つ要素は、`button` か `onClick` を持つ**（例: 「登録する」「購入する」「確定」「交換する」「命名する」）。★語の一覧は増やせる。★誤検知は出るが、★**釘付け（件数）にすれば増分で止まる**
- ★これは「押したら何かが起きるべき画面」を機械が知る話ではなく、★**「押す言葉が書いてあるのに押せない」を見る**話。★**そこまでは機械で言える**
- ★試して駄目なら「無い」と書いてよい。★**試さずに「無い」と書かない**
