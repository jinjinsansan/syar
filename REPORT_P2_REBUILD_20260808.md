# REPORT_P2_REBUILD — 世界の入れ替えと A-2 の拡張試験

**開発側 → レビュー側** / 2026-08-08
**コミット**: `6d8b4c1` 以降（push 済み・413 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**なし。**

承認済みの `seed 1231` を固定し、D-033 の裁定に従って記録しました。
新たな判断は行っていません。

---

## 1. 世界を `seed 1231` に入れ替えました

### 再現条件を記録（`docs/WORLD_SEED.md`）

```
penalty = 0.3（D-031） / STREAM 41〜45 / 50世代
現役2,400 / 種牡馬200 / 繁殖牝馬800 / NPC_STABLES 40（linesPerStable=1）
合格率 30%（12/40）
```

★**合格率を併記した理由**を明記しました。1つの世界を選ぶのは
「稀な当たりを引いた」のではなく、**3〜4シード試せば見つかる水準から
余裕のあるものを選んだ**ということです。将来の読み手が
チェリーピッキングと誤解しないためです。

### 投入後の検証 — 選定時と完全に一致

```
★有効系統数: 8.00   （選定時 8.00）
★平均F:      0.0287  （選定時 0.0287）
系統数 15 / 最大シェア 22.0% / DB 7,355頭
```

### ★途中で「不一致」と誤読しました

最初 DB で測ったとき **有効系統数 7.47**（選定時 8.00）と出て、
一致しないと報告しかけました。原因は**測定対象の違い**です。

- 選定時（`world-search.mjs`）: **種牡馬プール200頭**の系統集中
- 私が測ったもの: **現役世代4,481頭**

母集団が違えば値が違うのは当然で、「一致しない」のではなく
**私が違うものを比べていた**だけでした。同じ定義で測り直して一致を確認しています。

⚠️ この形は今回で3度目です（`potential` と `stats` / 40シード分布と30シード探索 / 今回）。
   **比べる前に定義を揃える**を手順に入れます。

---

## 2. ★入れ替えを A-2 の試験に使いました（ご指示）

クリーンな再構築 → ワーカー起動 → **レース生成中に SIGKILL ×3**。

```
cycle=72  出走12頭  オッズ1,500行(7券種)  purse=94,000  §8.6:一致  完全
cycle=73  出走18頭  オッズ3,435行(7券種)  purse=22,500  §8.6:一致  完全

★重複サイクル: 0件 / 孤立した出走表: 0件
★A-2（重複＋中途半端）: PASS
```

### 前便で見つけた盲点が塞がりました

従来 A-2 で確認していたのは **「重複しないこと」だけ**でした。今回は
**中途半端な状態が残らないこと**も確認しています:

| 検査 | 結果 |
|---|---|
| レース行はあるが**出走表が無い** | 0件 |
| 出走表はあるが**オッズが無い** | 0件（7券種すべて） |
| **`purse` が 0** | 0件 |
| **`server_seed` が無く reveal を出せない** | 0件 |
| 親を失った**孤立した出走表** | 0件 |

★**同一トランザクションが効いています。**`SIGKILL` は `finally` を走らせないので、
  トランザクションが無ければ「レース行だけ作られてオッズが無い」状態が残り、
  **誰も買えないレースが番組に並ぶ**ところでした。

`purse` も正典どおりです（94,000 = G3 の1〜5着合計 / 22,500 = 1勝クラス）。

---

## 3. A-1 の24時間計測を開始しました

クリーンな再構築が完了し、承認された世界で稼働しています。

**24時間後に確認する内容**（プロセス生存では見ません）:

```bash
journalctl -u star-worker --since "24 hours ago" | grep -c "生成="
journalctl -u star-worker --since "24 hours ago" | grep -c "確定=\[[0-9]"
```

10分サイクルなので **144レース**が期待値です。

---

## 4. 合格基準

| # | 状態 |
|---|---|
| A-1 | **計測開始**（24時間後に実測） |
| A-2 | **PASS**（★重複＋中途半端の両方・本番構成） |
| A-3 | **収束確認**（M=4000/races=32000 で +0.62pt） |
| A-4〜A-7 | **PASS**（実 DB） |
| A-8 / A-9 | **PASS**（413 tests / typecheck 0） |

**A-1 以外はすべて実測 PASS**です。

---

## 5. 未実装・未解決

| | |
|---|---|
| §7 成長・調子・疲労 | 未実装のため `age`=4 / `condition`=3 / `fatigue`=0 で固定 |
| §9.6 購入フロー（UI） | 未着手（Vercel は §14.3 の「読み取り系だけ」を守る前提） |
| §11.3 景品交換 | 未着手 |
| A-3 の複数シード | seed 42 のみ |
