# 引継ぎ書 — 次のセッションはここから（2026-08-31）

作成: 2026-08-31 / ブランチ `p4/race-30sec-cuts` / HEAD `9619053`
起点: `a83b2dc` → **11 コミット**（★**未 push 0 本**）
前の引継ぎ書: `HANDOVER_P4_RACE_CATALOG_20260830.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**

---

## ★0. ★**次の一手は URL の確認です**（★オーナー指示）

★**オーナーの指示は「次回は URL チェックからです」です。**
★50 鞍が画面に出るようになりましたが、★**動きで確認いただいていません。**

```powershell
cd V:\dev\Cusor\star\apps\web ; npm run dev      # ★ポート 3210（★別の窓）
```

★開く URL（★上から順に）:

| # | URL | ★何が出るはず |
|---|---|---|
| ① | `http://localhost:3210/race` | ★**11R 桜星賞 芝1600m**（★スターパーク・左回り）<br>★**2026-08-31 まで直書きだった 1 鞍**と同じもの |
| ② | `http://localhost:3210/race?race=g1-soukai` | ★**11R 蒼海賞 ダート1800m**（★白砂・走路が茶色・左回り） |
| ③ | `http://localhost:3210/race?race=g3-hakko` | ★**11R 白光記念 芝3600m**（★大河原・1周2400m・コース図が大きい） |
| ④ | `http://localhost:3210/race?race=g2-aone` | ★**11R 青嶺記念 芝2200m**（★青嶺・★**右回り**） |
| ⑤ | `http://localhost:3210/race?race=g3-shunrai` | ★**11R 春雷カップ 芝1200m**（★月見丘・★**この便で 1周を 1650m に変えた場**） |

★見るところ:

| | |
|---|---|
| ★**走る場所**（いちばん大事） | ★中盤に ★**内ラチに重なった馬が居ないか**。★2026-08-31 に作り直した箇所です |
| ★**馬場** | ★②がダートの茶色い走路で出るか（★芝が出たら配線が効いていません） |
| ★**距離とコース図** | ★③が残 3600m から始まり、★コース図が大きい走路になっているか |
| ★**回り** | ★④が右回りで、★**コース図と走路の向きが一致**しているか |

⚠️ ★**静止画では確認済みです**（`out/_real/race_*.png`）。★**動きは未確認**です。

★旧形との比較が要るときは `?lane=old`（★2026-08-31 以前の「在り得なかった形」）。

⚠️ ★**`apps/web/package.json`（ポート 3210）は触らないこと。** ★オーナーの既存作業で、
　★この便でも 11 コミットとも混ぜていません。★`git add -A` には `':!apps/web/package.json'` を付けます。

```powershell
git log --oneline -11
git log --oneline origin/p4/race-30sec-cuts..HEAD | wc -l   # ★未 push の数（直書きしない）
git status --short                                          # ★` M apps/web/package.json` だけのはず
npm test                                                    # ★133 ファイル / 1344 件 PASS / EXIT=0
npm run typecheck                                           # ★EXIT=0
npx tsx tools/verify-v18.mjs                                # ★★PASS
npx tsx tools/verify-v18.mjs --venues                       # ★★PASS（44 通り・★時間がかかります）
```

---

## ★1. この便で何が起きたか（★1 行で）

★**オーナーの一言で、P4 の土台が 3 段ひっくり返りました。**

> ★**正しくないです　こんな競馬は在りません　以前も指摘したことです**（2026-08-31）

★調べたところ、★**2026-08-21 の直しは、この苦情の中身に一度も触れていませんでした。**

---

## ★2. ★**最重要**: ★指標が動いても苦情は残ります（★この便の核心）

### ★2-1. ★何が起きていたか

★`lane.ts` の 2 行:

```ts
const home = RAIL_W;                              // ★全馬が同じ場所を目指す
const swing = Math.max(0, drift * 0.85 + wave)…   // ★外へしか振れない
```

★`Math.max(0, …)` があるので **swing は 0 以上にしかならず**、★drift が負の馬は
★`RAIL_W = 2.2m` に ★**小数以下まで同じ位置**で積まれます。★およそ半数が必ずそうなります。

★実測（6 seed × 6 地点の平均）:

```
  ★内ラチに 6.3 頭が重なり、3.3 頭が 6m 以上外、間に 2.4 頭
  ★最も外の馬は 9〜13 頭分外（★走路 20m の外ラチのすぐ内側）
```

⚠️ ★**「中盤に 10m 開いた隊列」という絵は、どこにも存在していませんでした。**
★開発側はそう説明し、★**レビュー側の裁定もその前提の上に立っていました。**

### ★2-2. ⚠️ ★**2026-08-21 の直しは、ここに触れていませんでした**

```
  reveal   ★内ラチの重なり   最外−最内
  1.00         6.2 頭         3.95m
  0.50         6.2 頭         7.28m
  0.30         6.2 頭         8.84m
  0.18 ★本番    6.2 頭         9.78m
```

★**重なりは 1 ミリも動いていません。** ★動いたのは指標だけです。

★**なぜ気づけなかったか**: ★当時の指標「最外 − 最内」は
★**「隊列の広がり」ではなく「いちばん外を回った 1 頭までの距離」**でした。
★2〜3 頭を外へ飛ばせば上がるので、★**固まりを直さなくても数字は良くなります**（R-16 の家族）。

### ★2-3. ★**次のセッションへの申し送り**

★**苦情を直したら、その苦情の言葉そのものを数える指標を作ってください。**
★「固まっている」→ ★**同じ位置に重なっている頭数**。
★そして ★**直す前の形に戻すとその検査が落ちること**まで確かめてください。
★それが無いと、★**守っていない検査が緑のまま通ります** — ★今回まさにそれが起きました。

⚠️ ★**「最外 − 最内」のような、外れ値 1 個で動く指標を合否に使わないこと。**

---

## ★3. 直したもの

### 3-1. ★走る場所の作り方（`LANE_MODEL`）

★「★全馬がラチを取りにいき、一部が押し出される」→
★「★**各馬が自分の通り道を持ち、その分布が走路に散る**」。

⚠️ ★通り道は**シードから引き、枠に依存させません**（D-069 / D-073）。
★内ほど混むよう `u²` で偏らせ、★揺らぎは通り道の**両側**に乗せます。

| | 旧形 | ★**本番（`LANE_MODEL` = `LANE_MODELS.b`）** |
|---|---|---|
| ★内ラチに**完全に重なる**頭数 | 6.2 頭 | ★**0.0 頭** |
| ★最外（馬の幅で） | 9.0 頭分 | ★**5.2 頭分** |
| ★枠順との相関 | +0.0056 | −0.0055（★どちらも許容 0.10 の内側） |

★**選び方**: ★`tools/_lanecand.mjs` で 4 案を 200 シード × 中盤 6 地点で測り
（★候補 A が本番の `laneAt` と一致することを最初に照合・最大差 0.00・R-30）、
★`?lane=b|c|d` で **実画面のコマ**をオーナーに見ていただいて **B を選択**。
★**動きでも「動きＯＫです」を確認済み**です。

⚠️ ★**途中で 1 つ外しました**: ★通り道の幅に `swingScale` を掛け忘れ、★長距離が悪化しました
（2000m 12.4 / 2400m 15.7 馬身）。★既存の swing は距離で割り戻されているのに通り道だけ素通しで、
★コーナー数ぶん積み上がっていました。→ ★`lane.ts` が既に持つ根拠に揃えました。
★**1600m は基準距離なので、オーナーが選んだ絵は 1 ビットも変わりません。**

### 3-2. ★V-18 が**初めて** PASS しました

```
  距離   ①枠順と着順   ②内外差        判定
  1200m      +0.069  ○    7.9 馬身 ○   PASS   （★前 12.0 ★×）
  1600m      +0.049  ○    8.1 馬身 ○   PASS   （★前 12.2 ★×）
  2000m      +0.042  ○    7.9 馬身 ○   PASS
  2400m      +0.016  ○    7.9 馬身 ○   PASS
```

★帯（4〜12）の**真ん中**に 4 距離とも収まりました。★天井への張り付きが解消しています。

### 3-3. ★B案 ①（月見丘の形）

★1 周 **1500m → 1650m**（半径 127 → 151m）。★変更は**1 周だけ**です。

⚠️ ★**走路の幅では直りません**（★重要）:

```
  1周1500 / 直線350 / 幅17 → 13.1  12.9  12.5  12.5  ❌
  1周1500 / 直線350 / 幅14 → 12.8  12.9  12.5  12.5  ❌   ★3m 狭めても効かない
```

★`LANE_MODEL.homeSpreadM`（通り道 7.0m）が**絶対値**で効くので、
★**走路の幅は横位置の散らばりをほとんど決めなくなりました。** → ★効くのは**半径**だけです。
★`venues.ts` の `widthM` の註記に書いてあります。

★**結果: `verify-v18.mjs --venues` が ★PASS。** ★10 場 × 実距離 = **44 通りすべて**通りました。

### 3-4. ★B案 ④（50 鞍を画面へ）

★`?race=<id>` で 50 鞍から引けます。★`const DIST = 1600;` と `RACE_META` の直書きを外しました。

⚠️ ★**台帳 B-6 を「構造で」塞ぎました。** ★レビュー側の指摘は
「★呼び出し側が spec を渡し忘れたことは捕まえられない」でした。→ ★**渡し忘れようがない形**に:

```
@star/scheduler の raceSetupById(id)  ←★走路の形はここ 1 か所から出る
   ├→ spec        → RaceConditions.course → laneExtraM → ★着順
   ├→ spec        → laneAt（画面の laneOf）→ ★絵
   └→ spec + turn → ovalCourse           → ★絵
```

---

## ★4. 次のセッションが踏みやすい穴

### ★4-1. ⚠️ ★**push は開発側の環境からできません**

★`credential.helper = manager`（GUI で聞く種類）で、★この環境には画面も stdin も無いため
★**何分待っても返りません**（★実際に 15 分待って 1 バイトも出ませんでした）。
★`git ls-remote` は**匿名で通る**ので、★疎通の問題と誤診しやすいです。

→ ★**オーナーに `! git push origin p4/race-30sec-cuts` を実行していただくこと。**

### ★4-2. ⚠️ ★**カットの境目の直後は測定に使えません**（★台帳 B-2 が広がりました）

★危ういのは `--sec 32` だけではありません。★`_cuts.mjs` が出す境目は
★**閃光 3 箇所・重ねる 3 箇所・切り替え 2 箇所**あり、★**その全部の直後**が危ういです。
★実際、★22 秒（1216m）は「重ねる（0.28s）」の直後で ★**馬が二重に写りました**。

★**使えた秒**: ★**14 秒**（498m・向正面）と ★**18 秒**（928m・4 角）。

⚠️ ★**秒と距離の対応にも罠があります。** ★`--sec` は**画面の時計**で、
★発走前に約 10 秒の前置きがあります。★`verify-cut-timing.mjs` は**レースの時計**で
「後方・俯瞰のカットを含まない」ので、★**そのまま `--sec` に使えません。**
→ ★確実なのは ★**撮ったコマの HUD「残り ◯◯ m」を読むこと**です。

### ★4-3. ⚠️ ★**5 seed の道具で判定しないこと**（★台帳 B-4）

★`_venuelane.mjs` は 5 seed で、★**約 2 割低く出ます**（1600m 9.98 に対し真値 12.23）。
★前便の「50 鞍中 15 鞍が帯の外」は、★200 seed で取り直すと ★**43 鞍**でした。
→ ★判定には `verify-v18.mjs`（2000 レース）か `_venueverify.mjs`（200 seed）を使うこと。

### ★4-4. ⚠️ ★**相関は 1 レースごとに出して平均しないこと**（★台帳 B-5）

★12 頭ぶんの ρ を 1 レースずつ出して平均すると、★n=12 の偶然の相関
（★|ρ| の期待値およそ **0.25**）が残り、★**50/50 鞍が「違反」**と出ます。★実際そう出しました。
→ ★`verify-v18.mjs` の `spearman` と同じく ★**プールしてから 1 回だけ**取ること。

### ★4-5. ⚠️ ★**分母をコマンドで数えること**

★報告に「48 通り」と書いて ★**レビュー側に差し戻されました**（★正しくは 44）。
★自分の出力を数えれば `6 PASS + 38 FAIL = 44` で、★その場で分かる誤りでした。

### ★4-6. ★**総当りは重すぎます**

★月見丘の形を 4500 通り × 200 seed で総当りしたら、★**30 分以上かかって落ちました**。
→ ★**狙いを 2〜3 方向に絞って数個だけ測る**形にしたら数分で済みました。

### ★4-7. ★登録簿のメタテストが、この便で **5 回**登録漏れを捕まえました

★`LANE_MODELS` / `LANE_MODEL_LEGACY` / `_legacycheck.mjs` / `DEFAULT_RACE_ID` / `GRADED_RACE_NO`。
★R-19 は効いています。★**新しい `export const` を足したら、登録簿も一緒に足すこと。**

---

## ★5. この便で増えた検査

| 検査 | 何を守っているか |
|---|---|
| `lane-reveal.test.ts`（★書き換え・6 件） | ★**内ラチに同じ位置で重なる馬が居ない**（旧形は 6.2 頭）／★**最外が 8 頭分未満**（旧形は 9〜13）／★**旧形に戻すとこの検査が落ちる**（★検査が実際に効いていることの担保） |
| `apps/cli/test/venue-course.test.ts`（10 件） | ★10 場 × 実距離でエンジンと描画層の走路が一致／★**50 鞍すべてで `raceSetupById` の spec が両層で一致**／★**既定の 1 鞍は直書きされていた画面と同じ** |

★旧形は `LANE_MODEL_LEGACY` として保存し、★**変更前の式と完全一致**することを確認済みです
（`tools/_legacycheck.mjs`・最大差 0.00）。★変異試験に使えます。

---

## ★6. ★オーナー判断待ち／レビュー側へ

| # | | |
|---|---|---|
| ① | ★**URL の確認**（§0） | ★**次の一手**。★動きは未確認です |
| ② | ⚠️ ★**レビュー側の裁定の見直し** | ★`REVIEW_P4_V18_CEILING_VERDICT_20260830.md` は「中盤に 10m 開いた隊列が正しい絵」という ★**成立していなかった [EYES]** の上に立っています（§247）。<br>★併せて ② が帯の真ん中（7.9〜8.1 馬身）に戻ったので、★**上限 12 を撤去する動機自体が薄れています**（★台帳 D-2） |
| ③ | ★**②b の差し戻し 2 件**（★前便から有効） | ★実測幅 `0.24〜0.44` → ★**`0.242〜0.503`**（★裁定の 40 通りは「10 場 × 固定 4 距離」で、★実際に組まれている 44 通りではありません）<br>★「reveal で動かない」→ ★**「振っても届かない」**（★実測 1.00 で 0.200 → 0.50 で 0.319・★0.50 以下で頭打ち） |
| ④ | ★**50 鞍と番組表の接続** | ⚠️ ★いまは **URL で選べる段階**です。★`programme.ts` から自動で開催される仕組みには**繋がっていません** |
| ⑤ | ★レース名 50 個の確認 | ★前便からの持ち越し。★**開発側では実在しないと断定できません**（§0.1 の担保の限界） |
| ⑥ | ★週次頻度と 50 鞍の噛み合わせ | ★前便からの持ち越し。★正典 §10.3 は G1=3/週、★50 鞍は B案（G1 9 鞍）。★**正典の変更** |
| ⑦ | ★A-5（刈り目）／ダート素材の承認 | ★持ち越し |
| ⑧ | ★タグ | ★ローカルに 5 本あり、★**すべて origin にありません**。★この便もタグを打っていません |

---

## ★7. 発行物（この便）

| 種別 | ファイル |
|---|---|
| 報告 | `REPORT_P4_VENUE_V18_20260830.md`（★V-18 ② が本番で落ちていた件）<br>`REPORT_P4_V18_2B_VERIFY_20260830.md`（★裁定への応答・②b の独立測定）<br>`REPORT_P4_LANE_MODEL_20260831.md`（★走る場所の作り直し・B案 ①④） |
| 照会 | `QUESTIONS_P4_V18_CEILING_20260830.md` |
| 受領 | `REVIEW_P4_V18_CEILING_VERDICT_20260830.md`（★レビュー側の裁定） |
| 台帳 | `RESIDUALS_P4_RACE_VIDEO.md`（★D-1 解消／★D-2 新設／★B-4・B-5 新設／★B-6 塞いだ／★B-2 拡大） |

⚠️ ★`tools/_*.mjs` は `.gitignore` で追跡外です。★この便で **18 本**作り、★全部登録簿に載せてあります。
⚠️ ★**2026-08-31 訂正: 「レビュー側の作業ツリーには無い」は誤りでした**（裁定 §1-5）。
★**実在します** — ★本裁定でレビュー側が実際に回しています。★2 便続けて誤った記述を残していました。

---

## ★8. 再実行（★すべて読取専用）

```powershell
cd V:\dev\Cusor\star

# ★正典の検定（★これが本体）
npx tsx tools/verify-v18.mjs            # ★★PASS
npx tsx tools/verify-v18.mjs --venues   # ★★PASS（44 通り・★時間がかかります）

# ★絵の条件
npx tsx tools/_railclump.mjs            # ★内ラチの重なり 0.0 頭
npx tsx tools/_whoiswhere.mjs 498       # ★どの馬が走路のどこに居るか

# ★「前回の直しは苦情に触れていたか」（★この便の核心）
npx tsx tools/_clumpreveal.mjs          # ★reveal を振っても重なりは 6.2 頭のまま

npm test ; npm run typecheck            # ★133 ファイル / 1344 件 / EXIT=0
```
