# REPORT_P3_G8 — G-8（引退と繁殖入り）／★純ロジックが全部揃いました

**開発側 → レビュー側** / 2026-08-10
**コミット**: `262c46c` 以降（push 済み・**584 tests 全PASS** / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**★あり。** §7.1/§10.5 に**プレイヤー馬が繁殖に上がる条件がありません**（§2）。

---

## 1. B-7「同じ土俵」を、構造で守りました

正典 §10.5:「NPC 馬はプレイヤー馬と**同一の遺伝エンジン**で生成。**専用の簡易ロジックを作らない**
（**同じ土俵にいることが公正性の担保**）」

★**「同じ結果になる」の比較では守れません。** 別実装が偶然一致している状態を検出できないからです。
→ **配合の実装が `@star/training` に現れていないこと**を機械的に検査します。

```
① retirement.ts に genotype / allele / inbreed / nicks / expressNumeric が現れない
② retirement.ts に breed の再実装が無い
③ sim-engine の breeding.ts に isPlayer / playerHorse / npcOnly の分岐が無い
```

★**この検査が効くことも両側で確かめました。**

```
正しい実装                        → 通る
training に breed を書いてみる    → ★落ちる
```

### ★検査そのものが1度、正しい実装を落としました

最初 `not.toContain('function breed')` と書き、**`breedingRoleForSex` に部分一致して**
**正しい実装を FAIL にしていました**（偽陽性）。単語境界で見る形に直しています。

★**「落ちた＝検出できた」ではない**の、こちら側の例でした。

### 型を分けていません

```ts
toBreedingStock(horse: HorseRecord): HorseRecord   // ★専用型を挟まない
```

専用型を作ると**そこから「プレイヤー馬用の配合処理」が生えます**。
繁殖の年次カウンタ（§6.7）だけ初期化し、**通算の産駒数・G1勝利数は消しません**（別物なので）。

---

## 2. ★照会 — プレイヤー馬が繁殖に上がる条件がありません

§10.5 は **NPC について**「**成績上位は自動で種牡馬・繁殖入り**」と書いていますが、
**プレイヤー馬の条件が書かれていません。**

| # | 内容 | 暫定 |
|---|---|---|
| **Q-P3-18** | プレイヤー馬が種牡馬／繁殖牝馬に上がる条件 | ★**閾値を発明していません**。既定は性別に応じた繁殖入り |
| **Q-P3-19** | 「功労馬」の意味（上げない選択か／上がれなかった馬か） | ★**選ぶもの**として実装（`preferHonored`） |

★**成績の閾値を作らなかったことをテストで固定**しています
（G1 5勝の馬と0勝の馬で行き先が変わらないこと＝勝手な閾値が無いこと）。

★致命的故障で引退した馬も**繁殖には上がれます**（§7.5「繁殖入りは可能」の明示に従いました）。

---

## 3. P3 の純ロジックが全部揃いました

| # | 内容 | テスト |
|---|---|---|
| G-1 | 週進行（★時刻から決まる） | 14 |
| G-2 | 調教8種と成長式（★`current ≤ potential`） | 26 |
| G-3 | 疲労と調子（★§8b への接続） | 15 |
| G-4 | 故障（★恒久ダメージの明示） | 19 |
| G-5 | 育成イベント（★data-driven） | 13 |
| **G-8** | **引退と繁殖入り（★専用経路なし）** | **11** |

**`@star/training` = 88件 / 全体 584件 全PASS。**

### 合格基準

| # | 状態 |
|---|---|
| **B-2（V-7）** | **PASS**（V-7a 29.9% / V-7b 0.7%・1,800頭） |
| **B-4** | **PASS**（成長・故障・イベントの3経路） |
| **B-5** | **PASS** |
| **B-6** | **ロジック PASS**・配線は staging 待ち |
| **B-7** | **★PASS**（構造検査つき） |
| **B-9** | **PASS**（584 tests / typecheck 0） |
| B-1 / B-3 | **staging 待ち** |
| B-8 | P3 完了時に |
| V-14 | ①② PASS / ③は EP効率 PASS・時間軸 FAIL |

---

## 4. ★残っているのは staging が要るものだけです

```
B-1  1頭を誕生から引退まで通す（実 DB）
B-3  V-11 の構造報告
G-6  EP の消費を ep_ledger に記録
G-7  PP の発行と point_flow_daily
B-6 の配線  race-field.ts の condition ?? 3 / fatigue ?? 0 を実データに
D-043 の残り  検証ツール1本をバンドルに対して流す
```

★**staging の接続文字列（D-042）待ちです。**それまで私から動かせるものはありません。
