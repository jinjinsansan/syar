# 引き渡し（開発側 → レビュー側）— 認証を LINE からメール＋パスワードに変える（2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-18 ／ **開発側 → レビュー側**（★オーナーが渡します）
**枝・コミット**: `main` ＝ `e5c4b8d`（★push 済み。`git pull` で取れます）

> ★**この 1 枚から読んでください。** 渡す文書と、裁定をお願いしたいことだけを書いています。
> ⚠️ **正典 `STAR_SPEC_v2.0.md` は開発側で編集していません**（起票はレビュー側の作業）。

---

## 0. オーナーの決定（逐語）

2026-09-18、オーナーから:

> 「認証は googleとappleではなく **最初は開発中なのでメールアドレスとパスワード登録にします**。どう思いますか？」

開発側が **D-076 と正面から食い違うこと**を申し上げたうえで、オーナーが
**「正典の決定として起票してよい」と承認**しました。さらに同日:

> 「先にレビュー側に出しましょう。**LINEログインをメールアドレスパスワードに変えるようにレビュー側に話しましょう**」

★**開発側は実装に着手していません。** 裁定をお待ちします。

---

## 1. 渡す文書

| # | 文書 | 中身 |
|---|---|---|
| **1** | **`QUESTIONS_AUTH_EMAIL_PASSWORD_20260918.md`** | ★**本体**。Q-AUTH-08（D-113 案）と Q-AUTH-09（V-19 の書き換え案） |
| 2 | 本書 `HANDOFF_AUTH_TO_REVIEW_20260918.md` | 渡し状 |

★どちらも `origin/main` に入っています（✔ `git ls-tree origin/main` で確認）。

---

## 2. 🔴 何が食い違うか（ここだけ読めば要点が分かります）

**D-076 は「認証を LINE ログインに変更する」と決定しており、その根拠の筆頭が「パスワードを持たない」です。**

> マジックリンク裁定の3理由（**パスワードを持たない**／摩擦が低い／RLS と素直に繋がる）は LINE でも失われず…

**「パスワードを持たない」は、マジックリンク → LINE と 2 度の裁定で連続して採用理由になっています。**
メール＋パスワードは**その 1 本目を明示的に捨てます**。

**開発側の案は「撤回ではなく並置」です** — D-076 を残し、**「開発期間中の当座の経路」**として D-113 を足し、
**公開前（P6）に再判断する**。撤回すると、公開時に LINE を選んだ理由をゼロから議論し直すことになるためです。

---

## 3. 裁定をお願いしたいこと（6 件）

### Q-AUTH-08（D-113 案）

| | 問い | 開発側の見解 |
|---|---|---|
| **a** | D-113 案文でよいか（特に「撤回せず並置」） | **並置を推します** |
| **b** | 🔴 **`user_identities` に `provider='email'` の行を作るか** | **作らない案を推しますが、開発側では決めません**（下の §4） |
| **c** | 実装を Supabase Edge Function に置くか（D-076 ④） | **不要と考えます** — メール＋パスワードは Supabase Auth の SDK が直接扱い、**自前の検証処理がそもそも無い**ので、④ の理由が成立しません。⚠️ **LINE を足す便では ④ が復活します** |
| **d** | パスワード再設定のメール送信 | 開発中の人数なら既定で足りる。**人を入れるなら SMTP の手配**（オーナー手配事項・D-042 と同じ形） |

### Q-AUTH-09（V-19 の書き換え）

| | 問い | 開発側の見解 |
|---|---|---|
| **a** | **対象外の項目を明記してよいか** | ✅ **裁定済み**。🔴 ★**当初「空振り 6／7」と数が食い違い、しかも ①③⑯ を対象外にしたのが誤りでした**（★2026-09-18 に §5 を訂正）。正しくは **対象外 4・読み替え 5・分割 1・そのまま 5** |
| **b** | ⑥ を ⑥-oidc / ⑥-email に分けてよいか | **推します。併設の便で必ず要ります** |
| **c** | 足す項目でよいか | ✅ **裁定済み**: 採用。★**E-3 は範囲を広げ・E-6 は DB 側にも重ね・E-8（許可リスト）を追加**（★2026-09-18 に §5 へ反映） |
| **d** | 「16項目」という数え方をどう書き換えるか | **「経路ごとに、その経路で有効な項目すべて」**を推します（数を固定すると経路が増えるたび嘘になる） |

---

## 4. 🔴 Q-AUTH-08-b が難しい理由（レビュー側に見ていただきたい点）

D-078 が `user_identities` を置いた目的は「**外部プロバイダの `sub` → 口座**の対応」でした。
**メール＋パスワードには外部プロバイダの `sub` がありません** — `auth.users.id` そのものが識別子です。

- **行を作らない**場合: 担保は `auth.users` のメール一意制約 1 か所（D-052 に素直）。ただし
  **`user_identities` を見ても経路が読めない**
- **行を作る**場合: `subject` が `auth.users.id` になるため、**主キー `(provider, subject)` が自明に一意**。
  🔴 **「制約はあるが何も担保していない」形**で、**D-078 と `0017` の註記が繰り返し警告している姿**
  （「★このとき下の primary key は『存在するのに働かない』」）と**同じ**になります

**開発側は「作らない」を推しますが、経路が読めない不便は残ります。裁定をお願いします。**

---

## 5. 🔴 Q-AUTH-09-a が要る理由（ゲートが静かに緩む）

V-19 は既に「**経路ごとに課す**（HS256 / ES256。片方の合格は他方の保証にならない）」と書いています。
**メール＋パスワードは 3 本目の経路です。**

**問題は項目が増えることではなく、16 項目のうち 6 項目がこの経路では何も測らないことです。**

| 現行 16 項目 | この経路で |
|---|---|
| ②`aud` ④`state` ⑦`nonce` ⑧`iss` | ★**対象外 4**（リダイレクトも ID トークンの自前検証も無い） |
| ①署名改竄 ③期限切れ ⑯`alg` 分岐 ／ ⑤同一 `sub` で2回 ／ ⑩`sub` の一意制約 | 🔴 ★**読み替え 5**（★2026-09-18 訂正）。①③⑯ は **Supabase が発行するセッショントークン**に読み替える — ★**この経路にも JWT は 1 本あり、書き込み RPC はそれで `auth.uid()` を得る**（D-076 ③） |
| ⑥ メール一致・`sub` 相違 → 別ユーザー | 🔴 ★**意味が反転**（メールが識別子になる）→ ⑥-oidc ／ ⑥-email に**分割 1** |
| ⑨ 検証なしにセッション発行不可 ⑪本番経路で固定 ⑫R-14 ⑬同時ログイン ⑭RPC 2回 | ✅ **そのまま 5**（⑨ が最重要） |

🔴 **黙って流すと 6 項目が「該当なし」で素通りし、9 項目しか働いていないのに『V-19 合格』と出ます**（R-16）。

### 足す 7 項目（すべて「壊れたときに落ちる」形。「正しいパスワードで入れる」は数えない）

| # | 項目 |
|---|---|
| E-1 | ★総当たり／詰め込みに率の制限があり、超えたら拒否される |
| E-2 | ★再設定リンクが 1 回限りで、期限切れなら拒否される |
| **E-3** | 🔴 ★**旧アドレスでの確認なしに、メールアドレスの変更が成立しない**（★2026-09-18 に**範囲を広げた**。再設定の経路を含む）。乗っ取り後に本人を締め出せる。口座は PP＝景品価値を持つ。★**変更の口は経路によらず同じ 1 つ** |
| E-4 | ★パスワード変更・再設定の後、既存セッションが失効する |
| E-5 | ★漏洩済みパスワードが拒否される |
| **E-6** | 🔴 ★**メール確認前に書き込み RPC が通らない**（他人のメールで登録でき、本人が後から登録できなくなる。D-080 と同じ形）。★**設定だけに頼らず、セットアップ RPC が `email_confirmed_at` が null の利用者を拒否する形を DB 側にも重ねる**（★メール経路に限って課し、OIDC 経路を巻き込まない）。🔴 ★**検査は通常の登録で未確認の利用者を作ってから行う** — `verify-v19-db.mjs:90-91` は `email_confirm: true` で作っており、**この作り方では必ず通る**（R-16） |
| E-7 | ★パスワードの最低要件を満たさないものが拒否される |
| **E-8** | 🔴 ★**許可リストの外からの登録が拒否される**（★2026-09-18 追加・裁定 C-2）。公開 URL で誰でも口座を作れると、**出口が閉じていても NPC 在庫（D-079 ⑧）と EP 総発行量（D-077 ①）を外から削れる**。★判定はサーバー側。★開発期間の終わり（C-1）で見直す |

⚠️ **E-1・E-3・E-5・E-6・E-8 は Supabase の設定で切り替わります**（★2026-09-18 に E-3・E-8 を追加）。**「設定を切れば検査が通る」形にせず、
設定の現在値を読んで判定する**（R-16・V-19 #11 と同じ要請）。

---

## 6. 🔴 複垢について（D-077 に関わるので明記します）

**メール＋パスワードは、使い捨てメールで口座が無限に作れます。**
LINE を選んだ利点のうち「**複垢コストが上がる**」は**完全に失われます**。

**★ただし正典の立場は破れません。** D-077 が先にこう書いているためです:

> **★複垢抑止を経済の防御に使わない。**「入口が効いている」前提で額を決めると、
> iOS 版を出した瞬間に、**どのゲートも鳴らずに前提が崩れる**

守りの 3 本（EP 日次総発行量の上限／重複の観測／**★出口＝景品交換での担保**）は
**いずれもログイン方式に依存していません**。

**★開発期間中は出口（§11 景品交換）が動いていないので、複垢の利得は実現しません。**
D-113 案の条件 ①「**開発期間中に限る**」が、この論点を吸収します。
**公開前の再判断で、必ずここを読み直す**ように案文に書いてあります。

---

## 7. 実装の現況（推測ではなく、作業ツリーを読んだ結果）

| | 状態 |
|---|---|
| `db/migrations/0017_user_identities.sql` | ✅ 存在。**`provider` は `text` で値の制限が無く `'email'` がそのまま入る**（**移行は不要**）。⚠️ ★**裁定 §2 が `✔` 付きで引いた「133-135 行」「169 行」は、このファイルに存在しません** — **全 87 行**で、実際は **`provider text not null` が 29 行**・**`references auth.users (id) on delete cascade` が 43 行**・**`constraint provider_not_empty` が 52 行**です。★**文言そのものは実在するので、行番号だけの誤りであり、裁定の結論は変わりません**（R-13 の凡例に関わるため報告します） |
| `supabase/functions/` | 🔴 **存在しない。認証の実装は 1 行も無い** |
| `apps/web/src/lib/supabase.ts` | 読み取り専用の anon クライアントのみ |
| `apps/web/src/lib/setup.ts` | デモデータ。`SetupRepo` の差し替え口は用意済み |
| `apps/web/src/app/signup/` `login/` | 🔴 **「準備中」の 1 枚ずつ。旧アーケードテーマのまま**（§9） |
| `tools/verify-v19-db.mjs` | ✅ #5・#6・#10・#15 と D-078 の参照先を実測。**既に内部で email/password の Supabase ユーザーを作っている** |
| V-19 の ①〜④・⑦〜⑨・⑪〜⑭・⑯ | 🔴 **道具が無い**（トークン検証側。Edge Function が無いため書きようがない） |

---

## 8. ★この便で本番に出したもの（認証とは別件・報告）

**2026-09-18、`main` を `e5c4b8d` へ早送りして push しました**（109 コミット）。
**`https://star-two-chi.vercel.app` が更新されています。**

- 馬物語テーマの 10 画面 ＋ `/design-check`（PC 1280 / モバイル 390 を並べた確認用）が**ログイン無しで見られます**
- ✔ 12 か所すべて HTTP 200 を実測
- 🔴 **★2026-09-18 訂正 — 「本番ワーカーも同時に入れ替わっているはず」は誤りでした。★入れ替わっていません。**
  VPS を読み取りだけで確かめました（裁定 §4 のコマンド 3 本・オーナー許可済み）:
  - `readlink -f /opt/star-current` → **`/opt/star-releases/70d9982cc84090b3e658dfbd05644b400e8665a9`**（★2026-09-15 の実測と同じ版）
  - `ActiveState=active` ／ `ActiveEnterTimestamp=Thu 2026-09-10 06:35:39 JST` ／ **`NRestarts=0`**
  - `journalctl` → 本日 `cycle=5853`〜`5858` が**欠番なく前進**。**周は 69.7〜113.2 秒（11.6〜18.9%）**
  → ★**レビュー側が懸念した「`0023` より前に新しいワーカーが走る」形にもなっていません**（`--schemacheck` を経ない起動は起きていない）。
  → ★**ES-6 裁定 §3 の 5（配備後の周の秒数の確認）は未着手のまま**です。**配備は `tools/deploy.sh` で行う必要があります。**
  → ★**原因**: star の `CLAUDE.md:31` が「常時稼働ワーカー（**Render**）」と書いており、**2026-09-14 の監査で既に指摘されていたのに直っていませんでした**。★**この便で VPS に直しました。**
  → **`REVIEW_ENGINE_LANE_SPEED_ES6_VERDICT_20260916.md` §3 の手順 5**。
  **配備後の最初の数周で「1 レースを作る周」の秒数が 1 周 60% に収まることの確認**が残っています（見込み 142 秒＝24%）

---

## 9. 🔴 併せて報告（この便で見つけた欠陥 2 件）

### 9-1. V-10 の `place`（複勝）が系統的にずれている

scratchpad に **2026-09-16 の `verify-pmin` の完走記録**が 2 本残っていました（本番の M ＝ 3,896,104・seed 42）。

| 本数 | `place` の乖離 | SE | 未発売 | 他 6 券種 |
|---|---|---|---|---|
| 40 | **−1.27pt** | 0.099pt | 2.20% | すべて PASS |
| 80 | **−1.26pt** | 0.069pt | 3.32% | すべて PASS |

**★本数を 2 倍にしても乖離はほぼ動かず、SE だけ下がりました。** 乖離は SE の**約 18 倍**で、
**ばらつきでは説明できません**。`place` **だけ**が外れます。

**★交絡を 1 つ潰してあります**: 「未発売率が高いからだ」は成り立ちません —
**未発売は 2.20% → 3.32% と動いたのに乖離は動かず**、`quinella_place`（未発売 2.17%）は PASS です。
**原因はまだ言えません。**

⚠️ **これは正式な V-10 本測定ではありません。**
`REVIEW_VENUE_WIRING_FINAL_VERDICT_20260916.md` §3 の 4 が指定した道具は
**`npm run verify:payout -- --races 500 --seeds 42`** で、こちらは `verify-pmin`。**別物です。**
ただし pmin のほうが M が 390 倍で**条件は厳しい**ので、**兆候としては重いと考えます。**

**★同裁定は V-10 を「配備の後でよいが、実際の利用者が入る前に必ず」としています。**
→ **認証を入れて人を迎える便のブロッキング条件**として扱います。**裁定の確認をお願いします。**

### 9-2. `/signup` と `/login` が旧テーマのまま（オーナー指摘・2026-09-18）

**TOP の「はじめる」「ログイン」を押すと、旧アーケードテーマの「準備中」ページが出ます。**
`/design-check` が並べているのは 10 画面で、**この 2 つが入っていませんでした**
（★**利用者が TOP から最初に押す 2 か所を、確認の対象から外していた**）。

**★開発側の判断で中身を作りません** — 入力欄を作ることは D-076 に反します。
**本裁定の後に、認証の実装と同じ便で直します。** それまでの当座として、
**見た目だけ馬物語に合わせる**（中身は「準備中」のまま）ことをオーナーに提案しています。

---

## 9-3. ✅ 裁定 §2 の△に答えが出ました（2026-09-18 追記・手順 2 完了）

**問い**: メール＋パスワードの登録で `auth.identities` に `provider='email'` の行ができるか。

**答え: ✅ できます。** staging で**通常の登録（anon キーの `signUp`）**を 1 回通した実測:

```
@gmail.com  確認=未  provider=email  providers=["email"]  2026-09-17T17:34
auth.identities: provider='email' 1 行
provider_id は user.id と同じ = 1 / 別 = 0
user_identities: 0 行
```

**★3 つとも裁定の見立てどおりでした**:

| | 結果 |
|---|---|
| 経路の印が自動で残るか | ✅ **残る**（`auth.identities` に 1 行・`raw_app_meta_data` にも `provider`/`providers`）。→ ★**`user_identities` を使わずに経路が読める**（D-113 ③） |
| `provider_id` は `user.id` と同じか | ✅ **同じ**。→ ★**行を作っても主キーが何も担保しない**という D-113 ③ の根拠が、**実測で裏づけられた** |
| 通常の登録で「確認=未」ができるか | ✅ **できる**。→ ★**E-6 を本物の経路で測れる**（管理 API の `email_confirm: true` では測れない・C-3） |

### ★既存の 6 人との違いが決定的でした

`read-auth-identities.mjs`（READONLY）で先に読んだ既存 6 人（2026-08-11）は、
**全員 `provider` が null・identity 行 0・確認=未**でした。**「0 行だから Supabase は作らない」と読みかけた**が、
**管理 API（`createUser`）で作られたため**であり、**通常の登録では作られる**。
★**同じ表を見ても、作り方が違えば別の答えが出る** — C-3 が警告していた形が、そのまま観測されました。

### ⚠️ この 1 人は意図せず作ったものです（経緯を残します）

**`429` の応答の中身を見るつもりで `/auth/v1/signup` を叩いたところ、上限が明けていて `HTTP 200` が返り、
利用者が 1 人できました。** 応答本文を捨てる指定にしていたため id も残らず、DB から探して消しました
（`cleanup-probe-user.mjs`・対象 1 人・既存 6 人には触っていません）。
★**「確かめるだけ」のつもりの呼び出しが状態を変えた**、という記録として残します。

## 9-4. ⚠️ 確認メールの送信上限について（オーナーへの報告・Q-AUTH-08-d）

**言える事実だけ書きます。**

- 2026-09-18 02:07 ごろ、**6 回続けて `HTTP 429` `over_email_send_rate_limit`**
- 02:1x にも `429`、**02:34 に `200`（明けた）**
- **開発側が 1 人で 8 回ほど登録を試しただけで上限に当たりました**
- ★**プランや上限の設定値は読めません**（管理 API のトークンを持っていないため）。
  **リポジトリに SMTP 設定の痕跡はありません**が、**手配は Supabase の管理画面で行うものなので、
  リポジトリに現れないことは「未手配」の証明にはなりません**（△）

→ ★**Q-AUTH-08-d「人を入れる前に SMTP を手配」の必要性が、開発側 1 人の作業で実証された形です。**
**招待する人数が数人でも、登録・再設定・メール変更で複数通が飛ぶため、既定のままでは足りない見込みです。**

## 9-5. 🔴 手順 5 の着手で、設定の不合格を 1 件つかまえました（2026-09-18 追記）

裁定 C-3「★設定で切り替わる防御は、設定の現在値を読んで判定する」に従い、
**`read-auth-settings.mjs`（READONLY・`GET /auth/v1/settings` 1 本）**を作って staging を読みました。

| # | 項目 | いま | あるべき | |
|---|---|---|---|---|
| **E-8** | 一般の登録口が閉じている（D-113 ⑦） | **`disable_signup = false`** | `true` | 🔴 **不合格** |
| E-6 | メール確認が必須（D-113 ④） | `mailer_autoconfirm = false` | `false` | ✅ |
| 経路 | メール＋パスワードが有効（D-113） | `external.email = true` | `true` | ✅ |
| ⑥-email | google / apple はまだ足していない | ともに `false` | `false` | ✅ |

🔴 **E-8 は、いま誰でも登録できる状態**です。**設定は Supabase の管理画面にあり、開発側からは変えられません**
（★管理 API のトークンを持っていません）。**オーナー手配事項として §11 に挙げます。**

### ★どこまで設定から読めるかを、全走査して確定しました

- ✅ `GET /auth/v1/settings`（anon キーで読める）に載る: `disable_signup`・`mailer_autoconfirm`・`external.*`
- 🔴 **載らない**: E-1（率の制限）・E-3（メール変更の両側確認）・E-5（漏洩パスワード）・E-7（最低要件）
- 🔴 `auth` スキーマにも設定の表は**無い**（✔ `information_schema.tables where table_schema='auth'` を全走査。
  27 の表はすべてデータ用で `config` 相当が無い）
- 🔴 管理 API（`api.supabase.com`）のトークンは `secrets.*.env` に**無い**

→ ★**読めるものは設定で判定し、読めないものは振る舞いで測る**、と分けます。
★**「設定が読めないから検査しない」にはしません**（それは R-16 そのものです）。道具の出力に**測り方まで書いて残しました**。

## 9-6. 手順 7 の当座分を先行しました（裁定 §0「見た目だけ合わせてよい」）

- ✅ `/signup`・`/login` を**馬物語テーマに**（★**中身は「準備中」のまま・入力欄は作っていません**。手順 6 で作ります）
- ✅ **`/design-check` の一覧に 2 画面を追加**（★**漏れていたことが本件の原因**なので、見た目より先に入れました）
- ✅ `npm run typecheck` 終了コード 0

> ⚠️ ★**書いた直後に型検査が私の誤りを 5 件捕まえました** — `tone="plain"`（実在しない。正しくは `ivory`）と
> `grow={1.2}`（`grow` は `string`）。**既存画面を見よう見まねで書いた引数が、実在しない値でした。**
> ★**流さずに本番へ出していれば、また「押すと壊れる画面」を作っていました。**

## 9-7. ✅ 手順 5（V-19 の書き換え）を実施しました — 実測の結果

**`verify-v19-email.mjs`（STATE_CHANGING）**を新設し、staging で流しました。
★**`verify-v19-db.mjs` とは別の道具**にしています — あちらは `provider='line'` を前提に
`user_identities` へ直に insert するので、**行を作らないメール経路は測れない**ためです（D-113 ③・「経路ごとに課す」）。

### ★合格の出し方（裁定 §3-4 のとおり）

```
=== メール＋パスワード経路の V-19 ===
  有効な項目: 9 件（合格 7 ／ 🔴 不合格 2）
  対象外    : 4 件（理由は上に明記）
  未実施    : 11 件（★これを「合格」に数えない）
  🔴 この経路はまだ通っていない（不合格 2・未実施 11）
```

★**「16 項目合格」とだけ出す形を作っていません。** 未実施を合格に数えない行を、出力に固定しました。

### 読み替えた ①③⑯ は、実測で通りました

| # | 壊し方 | 応答 | |
|---|---|---|---|
| ① | 署名を書き換え | `401 PGRST301 None of the keys was able to decode the JWT` | ✅ |
| ③ | 期限を過去に | `401 PGRST301` | ✅ |
| **⑯** | `alg` を `none` に | `401 JWT is unsecured but expected 'alg' was not 'none'` | ✅ |

★**⑯ の応答が決定的です** — 「壊れた文字列だから落ちた」ではなく、**`alg` で分岐せずに明示的に拒んでいる**ことが
文言から読めます。**この経路で唯一の JWT を誰も検査しない形ではない**と確かめられました。

★**⑫（検出器が自分自身を検出していないこと・R-14）を対照として先に置いています** —
**正しいトークンは `400 P0001 未セットアップ` まで届き**、壊したトークンは `401`。
★**区別がついているので、検査が成立しています**（「何を渡しても拒否される」状態を合格と読まない）。

### 🔴 不合格 2 件（どちらも本物）

| # | 中身 | 誰が直すか |
|---|---|---|
| **E-8** | `disable_signup = false` ＝ **いま誰でも登録できる**（D-113 ⑦ 違反） | 🔴 **オーナー**（Supabase の管理画面。開発側からは変えられません） |
| **E-6b** | **セットアップ RPC が未確認の利用者を拒否していない**（✔ `pg_proc.prosrc` に `email_confirmed_at` が無い） | **開発側・手順 6** |

★**E-6 を 2 つに割ってあります** — `E-6a`（設定 `mailer_autoconfirm=false`）は ✅ ですが、
**設定 1 枚に頼ると、設定を戻された瞬間に素通りします**（C-3）。**DB 側の `E-6b` を別項目として立て、
いま不合格であることを可視にしました。**

### 対象外 4 件（理由を出力に明記）

②`aud`・④`state`・⑦`nonce`・⑧`iss` — **ID トークンの自前検証もリダイレクトも無い**ため。

### 未実施 11 件

⑤⑬⑭・E-1〜E-5・E-7 は**登録を伴う**ので `--with-signup` のときだけ動く形にしました
（★**確認メールの送信上限**に当たるため・§9-4）。⑨⑪ は**手順 6 で構文木で見る**項目です。
★**測り方はすべて道具の註記と出力に書いてあります。**

## 9-8. ✅ 手順 6 の第一歩: E-6b を実装し、DB 側の重ねが働くことを実測しました

**移行 `0030_setup_requires_email_confirmed.sql`**（staging 適用済み）。
`assert_setup_complete()` に、**`auth.identities.provider = 'email'` の利用者に限り**
`email_confirmed_at` が null なら弾く判定を、**`users` 行の判定より前**に足しました。

- ★**メール経路だけに課す**（C-3-1）。OIDC 経路は `auth.identities` の `provider` で除外
- ★**`user_identities` では判定しない** — D-113 ③ により行が無く、判定が常に「メール経路でない」に倒れる
- ★**引数も戻り値も変えない**ので、呼び出し側（移行 8 本・検査 3 本）は無変更。✔ `rpc-guard` `exposure-registry` 42 件緑
- ⚠️ `0030` の前に **`0029_story_daily` が staging 未適用**だったため、`migrate.mjs` が順序で止めました。
  **`0029` → `0030` の順に適用**（✔ `story_daily` 表と関数の判定の両方を実測で確認）

### 🔴 一度しくじり、測り方を直しました（R-16 の家族）

**最初の測り方**: `createUser({ email_confirm: false })` で未確認の利用者を作り、サインインを試みた。
→ **`Email not confirmed` でサインインが弾かれ**、「トークンが出ないので通れない」を **✅ と記録**した。

🔴 **これは `0030` が働いた証明になっていませんでした。** 止めていたのは **Supabase の設定**で、
**`assert_setup_complete` まで到達していない**。**設定を戻されればサインインでき、そのとき DB 側が効くかは測れていない**
— ★**裁定 C-3 が求めた「設定 1 枚に頼らない」の検証にならない**。

**直した測り方**: **確認済みで作ってトークンを取り、そのあと DB で `email_confirmed_at` を null に戻す。**
トークンは有効なまま DB 上だけが未確認になるので、**`0030` が効くかを直接測れる**。

| | 結果 |
|---|---|
| **E-6b-2**（対照） | ✅ 確認済みなら通り抜ける → `400 未セットアップ（/setup で牧場を作ってください）` |
| **E-6b**（本体） | ✅ 未確認に戻すと同じトークンでも拒否 → `400 メールの確認が済んでいません` |

★**同じトークンで、DB の 1 列を変えただけで結果が変わりました。** これが DB 側の重ねが働いている直接の証拠です。
★**対照（E-6b-2）を必ず併記します** — 無いと「何を渡しても確認で落ちる」状態を合格と読んでしまいます。

### いまの到達点

```
=== メール＋パスワード経路の V-19 ===
  有効な項目: 11 件（合格 10 ／ 🔴 不合格 1）
  対象外    : 4 件（理由は上に明記）
  未実施    : 11 件（★これを「合格」に数えない）
```

**残る不合格は E-8 のみ**（`disable_signup = false`）。**Supabase の管理画面でしか直せません**（§11）。

## 9-9. 本番の状態（★読み取りのみ・`--env production` 明示・`read only` 設定）

| | |
|---|---|
| 適用済み | **`0020` まで**（✔ `schema_migrations`） |
| **未適用** | 🔴 **`0021`〜`0030` の 10 本**（★本便の `0030` を含む） |
| `assert_setup_complete` | **確認の判定なし**（＝`0030` 未適用。staging とは別の姿） |

⚠️ ★**開発側の報告の訂正**: 先に「本番の `app_environment` が未設定」と読みかけましたが、
**`app_environment` は設定値ではなく表**でした（✔ `tools/migrate.mjs:126` は `select environment from app_environment`）。
★**私のクエリの誤りで、本番の異常ではありません。**

## 11. 🔴 オーナー手配事項（この便で増えたもの）

| # | 中身 | いつまで |
|---|---|---|
| **1** | 🔴 **Supabase の新規登録を止め、招待制／許可リストに**（`disable_signup = false` → `true`）。**D-113 ⑦**・**V-19 E-8** | **人を迎える前** |
| **6** | 🔴 **パスワード変更時に他のセッションを失効させる**（**V-19 E-4**）。★**いちばん重い** — 乗っ取られたとき**パスワードを変えても相手を締め出せません**。口座は PP（景品価値）を持ちます | **人を迎える前** |
| **7** | 🔴 **「漏洩したパスワードの拒否」を有効にする**（**V-19 E-5**）。★いま `password123` が通ります | **人を迎える前** |
| **8** | **パスワードの最低文字数を 8 に**（いまサーバーは 6・画面は 8。★実害はありませんが揃います） | 同上 |

> ★**6・7・8 は、いずれも Supabase の管理画面で切り替えるだけ**です。直していただければ、
> **`npx tsx tools/verify-v19-email.mjs --env staging` を流すだけで ✅ に変わったことが機械的に確かめられます**（人の目で見比べる必要がありません）。
| **2** | **SMTP の手配**（Q-AUTH-08-d）。★開発側が 1 人で 8 回試しただけで `429` に当たりました（§9-4） | **人を迎える前** |
| **3** | 🔴 **V-10**: `npm run verify:pmin -- --races 40`（別のターミナルで 1 本・出力を切らない）。★**人を迎える前のブロッキング条件**（§9-1） | 早めに |
| **4** | **招待する方の範囲**を決める（D-113 ⑦） | 人を迎える前 |
| **5** | **本番への適用と配備**: `0021` → `0022` → V-20 → `0023` → **`tools/deploy.sh` で配備** → `0024` → `0028` → `0029` → **`0030`**（★本便）。⚠️ **`deploy.sh` は配備前に `--schemacheck` を流すので、移行が遅れていると止まります** | 本番で認証を使う前 |

★**1 だけは、開発側の検査が機械で見つけたものです。** 直していただければ、`verify-v19-email.mjs` を流すだけで
**✅ に変わったことが機械的に確かめられます**（人の目で見比べる必要がありません）。

## 9-10. ✅ 手順 6: ログインと登録を実装しました（入力欄まで）

### 器を 2 つに分けました（`apps/web/src/lib/supabase.ts`）

| | 用途 | セッション |
|---|---|---|
| `readClient()` | 番組表・オッズ・記録（**ログイン不要**） | ★**持たない** |
| **`authClient()`**（新設） | 登録・ログイン・書き込み RPC | ★持つ |

★**分けた理由**: `readClient()` に `persistSession` を足すと、**読み取りの全経路にセッションが紛れ込み**、
「ログインの有無で読み取りの結果が変わる」形になりえます。**用途で器を呼び分けます。**

★**D-113 の制約を守っています** — ①自前で JWT を発行しない（`signUp` / `signInWithPassword` を呼ぶだけ）
②寿命管理はライブラリに任せる（`autoRefreshToken: true`）③`auth.uid()` / RLS / 書き込み RPC は無変更。
⚠️ **service_role はフロントに置いていません。**

### ⑨⑪ を構文木で固定しました（`apps/cli/test/auth-wiring.test.ts`・7 本緑）

★裁定が「構文木で見る」と定めた 2 項目です。**禁止語の一覧では書いていません**（D-108 ③）。

| 見ていること | なぜ |
|---|---|
| ★`apps/web` に **service_role を読む口が無い** | RLS を素通りする |
| ★**JWT を自前で組み立てる口が無い**（`jsonwebtoken` / `jose` 等を取り込まない） | D-113 ① |
| ★**`createClient` を直に呼ぶのは `lib/supabase.ts` だけ** | 発行口を 1 か所に保つ（D-052）。**⑨ の「発行者が 1 つ」** |
| ★`readClient` に `persistSession` が**入っていない** | 読み取りにセッションを紛れ込ませない |
| ★`authClient` が寿命管理をライブラリに任せている | D-113 ② |
| ★**両方の器が未設定を黙って通さない** | 設定漏れを「データが無い」に見せない |

### 画面

- `/signup` … メール＋パスワード → **確認メールを送り、確認待ちの画面**に切り替わる
- `/login` … メール＋パスワード → 成功で `/home` へ（**口座が無ければ RPC 側が `/setup` へ誘導**・D-080）
- ★**パスワードは 8 文字以上**を画面でも伝える（E-7。★**サーバー側でも弾かれる**）
- ★**エラーの文言に推測を足しません**。`Invalid login credentials` 等は**日本語に言い換えるだけ**で、
  **対応表は 1 か所**（`login/page.tsx` の `JA`・D-052）。**当てはまらないものは原文をそのまま出します**
- ★画面に「お誘いした方だけ」と出していますが、⚠️ **画面の文言は防御ではありません**。
  判定はサーバー側（`disable_signup`）で、**V-19 の E-8 が機械で見ます**

✔ 型検査 **合格**（`tsc` の終了コード 0）／ `build:web` **成功**／
配信される HTML に**入力欄が実在**（`/signup`・`/login` とも `type="email"` 1・`type="password"` 1）

### ✅ 本番でも確認しました（`cde2981` → 配備 `● Ready`）

| | メール | パスワード | テーマ |
|---|---|---|---|
| `https://star-two-chi.vercel.app/signup` | 1 | 1 | 馬物語 |
| `https://star-two-chi.vercel.app/login` | 1 | 1 | 馬物語 |

★**読み取り系が壊れていないことも併せて確認**（`/`・`/home`・`/odds`・`/design-check` とも HTTP 200）。
**ログインしなくても見られる**という正典の立場は保たれています。

★**独立した 2 つの経路で一致**させています — 開発側の直接の確認と、別に走らせた見張りの両方が
同じ数（メール 1・パスワード 1）を報告しました。また **配備の状態は `vercel ls` で直接読み**、
「まだ配備中」「配備に失敗」「配備済み」を**推測で分けていません**（★今夜、この取り違えを一度しています）。

### 検査の全体

**2,075 本中 2,074 本が緑。** 落ちた 1 本は `packages/render/test/edit-grammar-audit.test.ts` ⑨ で、
★**この便とは無関係な既知の赤**です（✔ `git log <便の始点>..HEAD -- packages/render` が**空**＝
この便では `packages/render` を 1 度も触っていない。落ちた検査の最終更新は `2b2aa3d`＝レース映像の便）。

### 🔴 報告: 開発側の測り方に誤りがありました（合否の偽装）

**`npm run typecheck 2>&1 | tail -12; echo "終了コード: $?"`** と書いていました。
★**`$?` は `tail` の終了コードで、ほぼ常に 0** です。

→ **型検査が 2 件のエラーを出しているのに「終了コード: 0 ✅ 合格」と表示・報告しました。**
**エラー行は目の前に出ていました。** 最後の 1 行を信じて合格と書いたのは開発側の誤りです。

★**同じ日に、出力を切って 4 度誤診しています**（`vercel whoami` / `git push` / `vercel env ls` / 本件）。
**合否を出すコマンドは切らず、終了コードは道具自身から取る**形に改めました。本件の 2 件は修正済み
（`exactOptionalPropertyTypes` により `onClick` に `undefined` を渡せない → 条件付き展開に変更）。

## 9-11. ✅ 登録を伴う項目のうち 4 件を、確認メールを飛ばさずに測りました

**当初 `--with-signup` の陰に置いていた項目のうち、4 件は既定で測れる**と実測で分かりました。

★**管理 API（`admin.auth.admin.createUser`）は、確認メールを送らないが、パスワードの検証は迂回しません。**
✔ 実測: 2 文字のパスワードが `Password should be at least 6 characters.` で弾かれた。

| 迂回する | 迂回しない |
|---|---|
| **メール確認**（`email_confirm: true` で確認済みにできる → **E-6 は signUp 経路が要る**・C-3-2 のとおり） | **パスワードの最低要件（E-7）・漏洩パスワード（E-5）・メールの重複（⑤）** |

★**この区別を取り違えると「別の理由で通った ✅」を作ります**（§9-8 で一度踏んだ形）。

### 実測の結果

| # | 結果 | |
|---|---|---|
| **⑤** 同一メールで 2 回 → 1 ユーザー | `A user with this email address has already been registered` | ✅ |
| **E-7** 最低要件 | `Password should be at least 6 characters.` | ✅ |
| **E-5** 漏洩済みパスワード | 🔴 **`password123` が通った** | **不合格** |
| **E-4** パスワード変更で他セッション失効 | 🔴 **変更後も別セッションのトークンが RPC に届く** | **不合格** |

★**E-4 は本番で起こる経路で測っています**（管理 API の `updateUserById` ではなく、**利用者自身の `updateUser`**）。
セッションを 2 つ作り、片方でパスワードを変え、**もう片方のトークンで `assert_setup_complete` を叩いて `401` になるか**を見ました。
→ `400 未セットアップ` まで届いた＝**トークンが有効なまま**。

🔴 **E-4 がこの 3 件でいちばん重いと考えます** — **乗っ取られたとき、パスワードを変えても相手を締め出せません。**
口座は PP（景品価値）を持つので、これは現実の損害になりえます。

### いまの到達点

```
有効な項目: 15 件（合格 12 ／ 🔴 不合格 3）
対象外    : 4 件（理由は上に明記）
未実施    : 7 件（★これを「合格」に数えない）
```

**不合格 3 件は、すべて Supabase の管理画面の設定**です（E-8・E-5・E-4）。**開発側からは直せません**（§11）。

### ⚠️ 小さな食い違いを 1 件報告します

**サーバーの最低文字数は 6、画面（`signup/page.tsx` の `PW_MIN`）は 8** です。
★**画面のほうが厳しいので利用者への実害はありません**。`PW_MIN` は 1 か所にあり D-052 は守られています。
**Supabase 側の最低文字数を 8 に上げていただければ揃います**（§11-8）。

### 残り 7 件が未実施である理由

| # | 理由 |
|---|---|
| ⑬⑭ | ★**セットアップ RPC が未実装**（照会 **Q-SETUP-05** の裁定待ち） |
| E-1 | 連続して叩くので、確認メールの上限とは別に**失敗ログが増える** |
| E-2・E-3 | **再設定／メール変更の確認メール**が要る（**上限に当たる**） |
| ⑨⑪ | ✅ **構文木で固定済み**（§9-10・`auth-wiring.test.ts` 7 本緑） |

## 9-12. ✅ 手順 6 完了: 初回セットアップ RPC（`0031`・staging 適用済み）

裁定 `REVIEW_SETUP_PREDICATE_VERDICT_20260918.md`（**(a) 採用・条件 4 つ**）に従いました。

| 条件 | どう満たしたか |
|---|---|
| **1** 既にある書き方を使う | 候補は `market-flow.ts` と**同一の SQL**／戦績 0 は `story-flow.ts:71` の先例どおり `finish_pos is not null`／移管は `0025`・`0026` と同形／台帳の語は **`inflow`**（`0027` の制約に**既にある語**。新設せず） |
| **2** 暫定であることを註記 | RPC の `comment on` と冒頭に、★**クラス分けの便で共通の述語に置き換え、V-4・V-5・V-6・V-18 を取り直す**ことを明記 |
| **3** 第三の登録簿 ＋ 中身の検査 | `ACCOUNT_CREATING_FUNCTIONS` を新設し `:210` から除外。★代わりに **①`auth.uid()` の null 弾き ②`email_confirmed_at` を自身でも確認（`auth.identities` で経路を判定し OIDC を巻き込まない）③`dedupe_key` の冪等** を本文に要求。★併せて **V-19 ⑭ として `on conflict` の不在**も見ます |
| **4** V-20 の登録簿 | ✔ staging 実測で 2 関数とも `anon= - / authenticated=YES`・「登録簿に無い関数が無い」緑 |

### ★検査が本当に噛むことを、変異で確かめました

`email_confirmed_at` の確認を消すと **3 条件の検査だけが赤くなり**（17 passed / 1 failed）、控えから復元しました。
★**緑であること自体を合格の根拠にしていません**（§9-8 で一度踏んだ形）。

### 🔴 途中で踏んだ罠を 1 件記録します

**`comment on function … is '…'` の文字列リテラルが、関数の本文に混ざります**
（本文の切り出しは「次の `create function` まで」なので）。
★`blankComments` は `--` しか消さないため、**説明文の中の `assert_setup_complete()` を「呼んでいる」と誤判定**しました。

→ ★**`stripFunctionComments` を足して構造の側で直しました。**
⚠️ ★**`blankComments` 自体は変えていません** — 7 か所から呼ばれ、**`errcode = '…'` のリテラルが残ることを前提にした検査**があるためです。
★**註記の語を言い換えて検出器を黙らせる形は採りませんでした**（D-108 ③）。

### 勝負服の配色の器を足しました（★オーナー承認済み）

★**D-074 が入力項目として明記しているのに、列がどこにもありませんでした。**
`users` に `silk_color` / `silk_sleeve` を `0009`（`account_type`）と同じ形で追加。
★値の一覧は**画面側（`setup.ts` の `SILK_COLORS`）が唯一の出どころ**なので、DB 側は**空でないことと袖の 3 択**だけを制約にしています（二重管理にしない・D-052）。

## 9-13. 🔴 別件の露出を 3 件見つけました（★本便では直しません）

★**`0018` は「実体テーブルには一切 grant しない」と宣言**し、`revoke all on all tables` ＋
`alter default privileges` で閉じています。★**その後の 3 つの移行が、実体テーブルを anon に開けています。**

```
0024:97  grant select on horse_story_event   to anon, authenticated;
0025:57  grant select on horse_market_listing to anon, authenticated;
0027:46  grant select on stable_grade_price  to anon, authenticated;
```

✔ staging 実測: **3 表とも anon から 1 行ずつ読めます**（V-20 ② 不合格）。
✔ **登録簿にも 1 件も載っていません**（V-20 ③ 不合格）。
✔ **過去の報告・裁定に記録が見当たりません** — ★つまり**誰も気づいていない露出**です。

★**§14.3「読み取りはビュー経由に一本化」と `0018` の方針に正面から反します。**

| | |
|---|---|
| 🔴 **本番** | ✅ **露出はありません**（②③ とも緑）。★`0024`・`0025`・`0027` が**本番未適用**のため |
| **性質** | `stable_grade_price` は格と価格（**公開してよい種類**）／`horse_market_listing` は★と価格（同様）／⚠️ **`horse_story_event` は `detail jsonb` を持ち、中身次第で §12.4 に触れます** |

★**本便では直しません**（裁定 §4 が別件の欠陥に定めた「直さず記録だけ」に倣います）。
★ただし **V-20 は恒久ゲート**で、★**本番に `0024` 以降を当てた瞬間に露出が現実になります**。
→ 🔴 ★**本番への適用より前に、この 3 件の扱いを決めてください。**

### ★本番の V-20 が 2 件赤いこと（★素性を分けて報告します）

```
🔴 登録簿にあるが DB に無い: create_account, is_initial_horse_candidate,
   enter_race, buy_horse, sell_horse, unlock_stable_grade
🔴 anon の EXECUTE: exchange_prize, place_bet, spend_training_ep が実測 true
```

| | |
|---|---|
| ★**4 件は今夜以前から**赤い | `enter_race`・`buy_horse`・`sell_horse`・`unlock_stable_grade` は `0024`〜`0027` が**本番未適用**のため |
| ★**2 件は私が増やしました** | `create_account`・`is_initial_horse_candidate` を登録簿に載せたぶん。★**新しい欠陥ではなく、登録の結果**です |
| ★**anon の EXECUTE** | **`0022` 未適用による既知**。★登録簿の註記自身が「本番で回すと④が落ちる。**それが正しい**」と予告しています |

## 9-14. ✅ V-19 ⑭ を実測で閉じました（★変異で両方向を確かめました）

`0031` が staging に入ったので、**⑭ が初めて測れるようになりました**。

| # | 結果 |
|---|---|
| **⑭-a**（対照） | ✅ 1 回目は `HTTP 200` |
| **⑭** | ✅ 2 回目は `HTTP 409` で拒まれ、★**口座 1・EP 2000・馬 1**（★増えていない） |
| **⑭-b** | ✅ 同じ冪等キーの再送は `200/200` で、★EP 2000・馬 1（★冪等の枝に入っている） |

★**⑭ の本体は「同一トランザクションだったこと」の確認**です（裁定 `REVIEW_SETUP_RPC_VERDICT_20260820.md` §5）。
「`users` の insert が落ちた」ではなく、★**EP も馬も増えていないこと**を実測しました。

### ★変異で、検査が本物であることを確かめました（★両方向）

`create_account` の台帳の記帳を `0` にしたところ、**⑭ と ⑭-b が 🔴 `EP 0`** になり、
戻したら**緑に復しました**。★**赤くなることと緑に戻ることの両方**を見ています。

> 🔴 **1 回目の変異は成立していませんでした。** 移行ファイルを書き換えたところ、
> ★`migrate.mjs` が「**適用済みのファイルを書き換えず、新しい番号で足してください**」と**正しく拒否**し、
> ★**変異が DB に届かないまま ⑭ が緑**でした。**私はそれを「噛むことを確認した」と報告しかけました。**
> → ★**移行ファイルには触らず、staging の関数だけを `create or replace` で差し替える**形に改めました。

### ★復旧を実測しました

| | |
|---|---|
| 本文 | ✅ **リポジトリの `0031` と一致**（★`migrate.mjs` が守る「DB とリポジトリが食い違わない」不変条件を回復） |
| 変異の痕跡 | ✅ なし |
| 署名 | ✅ `create_account(text,text,text,text,uuid,uuid)`・`is_initial_horse_candidate(uuid)`（★登録簿の綴りと一致） |
| `security definer` / volatility | ✅ `true` / `v` |
| 検査 | ✅ `rpc-guard` ＋ `exposure-registry` **46 本緑** |
| staging | ✅ 利用者 6・口座 0・孤児 0・候補 6,601・`setup:` の台帳 0 |

## 9-15. 🔴 この便で私が作った欠陥を 2 件報告します

**どちらも「別の理由で通った ✅」の家族**で、**自分の検査・自分の片付けの側**にありました。

### ① 後片付けが漏れ、staging に口座 2 件が残った

`admin.auth.admin.deleteUser(id)` を呼んで**戻り値だけを見て「✅ 消しました」と表示**していました。
★**実際には消えておらず**、利用者が 6 人 → 8 人に増えていました。

**理由は設計どおりの拒否**でした — ★**D-078 が `users` → `auth.users` を `NO ACTION` に保つ**
（「口座は台帳と PP を持つので勝手に消えてはならない」）。✔ 実測: FK は `REFERENCES auth.users(id)`。
★**口座がある間は auth 側を消せません。**

★`tools/lib/env.mjs` の註記が**まさにこれを警告していました** —
「後片付け漏れで検証用の口座が残り、§11.2 の実経済の指標に混ざる」。

→ **直した形**: 片付けを ★**馬を NPC に返す → `ep_ledger` → `pp_ledger` → `users` → `auth.users`** の順にし、
★**消えたことを DB で確かめてから表示**し、★**残骸の総量（口座の行数・孤児の馬）も出す**ようにしました
（★1 件ずつの成否だけ見ていると、前の実行の残りに気づきません）。✔ 再実行で 7 人すべて「消えました」・残骸 0。

### ② ⑭-b が「冪等が壊れていても緑」になる形だった

同じ利用者で同じ冪等キーを 2 回投げる形に書いていました。★**その時点で口座は ⑭-a で既にでき**ており、
★1 回目から `users` の主キーで落ちるので、★**冪等の枝に一度も入らないまま EP が 2000 のまま**になります。
→ ★**口座を持たない別の利用者で測る**形に直しました。

### ⚠️ 併せて: bash の入れ子引用を壊しました

`$$` と正規表現を bash の入れ子に置いたところ、★**`SPEC.md: command not found`** が出ました。
★`SPEC.md` は**廃棄済み文書**（読まない・従わない）で、**実行はされていません**が、
★その名が出たこと自体が私の引用の誤りです。→ ★**スクリプトはファイルに書いて実行する**形に改めました
（記憶 `star-shell-encoding-traps` のとおり）。

## 9-16. ✅ 露出の性質が確定しました（§9-13 の「中身次第」を確定に）

`horse_story_event.detail` に入るのは ★**`finishPosition` / `raceName` / `jockeyName` の 3 鍵だけ**です
（✔ `apps/worker/src/story-flow.ts:153`）。★**素質の数値も seed も入りません。**

→ ★**§12.4（本人にも数値を見せない）には触れません。** 露出の中身は「**物語の断片が読める**」程度です。
★ただし **V-20 ② ③ が赤いこと自体は変わりません**（★実体テーブルが anon に開いている・§9-13）。

## 10. 開発側が次にすること

**裁定が出るまで認証の実装に着手しません。** 裁定後は、

1. **V-19 の書き換えが先**（測れないものを効かせない・D-112 ③ と同じ形）
2. 実装（`user_identities` の扱いは Q-AUTH-08-b の裁定に従う）
3. `/signup`・`/login` を同じ便で直す
