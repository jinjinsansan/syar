# 裁定 — 監査の修正 第 2 便（`REPORT_AUDIT_FIX2_20260914.md`）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-14 ／ レビュー側 → 開発側
**対象**: `REPORT_AUDIT_FIX2_20260914.md` ／ コミット `41d2cb9`（起点 `fe7376c`）
**指示書**: `DEV_INSTRUCTIONS_AUDIT_FIX2_20260914.md`

> 凡例（R-13）: **✔ 再現** ＝ レビュー側が実行するかコードを読んで確かめた ／ **△ 未再現** ＝ 開発側の主張のまま

## 判定

| 工程 | 判定 |
|---|---|
| BF-1 失敗した馬だけを飛ばす | **PASS**（条件: §5 の staging での週送り 1 回） |
| BF-2 利用者の RPC から anon を剥がす | **PASS** |
| BF-3 関数の既定の権限 | **照会に回したのは正しい判断**。回答は §4 Q1（実装は次の便） |
| BF-4 移行ファイルの改行を LF に固定 | **PASS** |
| BF-5 1.0 倍未満を売らない ／ V-10 を本番と同じ「売る目」で測る | **PASS**（staging の ④⑤ は §5 の段取りで計測） |
| BF-6 V-10 の合否を切り捨て前で出す | **CONDITIONAL**（SE の式を直す・§3-1） |
| **総合** | **CONDITIONAL PASS** |

実装は正確です。変異の実演 4 本、staging の適用前後の比較、`proacl` の確認がそろっていて、今回も照合がとても速く済みました。BF-3 で指示書の条件（`postgres` 以外の行があれば照会）に当たったとき、**入れずに止めたのは正しい判断**です。

---

## 1. レビュー側の再実行

| # | 内容 | 結果 |
|---|---|---|
| 1 | `npm run typecheck` | ✔ EXIT=0（HEAD `41d2cb9`） |
| 2 | 関係テスト（再実行表 #2 の 8 本と、それが含む `packages/betting` 全体） | ✔ `Test Files 11 passed` ／ `Tests 131 passed` ／ EXIT=0 |
| 11 | `git ls-files --eol db/migrations` | ✔ 22 件すべて `i/lf w/lf attr/text eol=lf` |
| 3 | 全テスト | 未実行（開発側の申告 `1 failed \| 1643 passed`・赤は ⑨ だけ） |
| 4〜7 | 変異 | **未実行**。オーナーが別のターミナルで計測を流す可能性があるので、作業ツリーのファイルを書き換える実験は行わない |
| 8〜10 | DB を使う検査 | 未実行（△） |

`horses.id` の型: ✔ `uuid`（`0001_init.sql:104`）。BF-1 の `not (id = any($3::uuid[]))` で型が食い違うことはありません。

---

## 2. 工程ごとの照合（差分を読んで確かめたこと）

- **BF-1** ✔ `failedIds` を以後のバッチの SQL から除き、打ち切りを削除。`incomplete = hitCap || spendErrors > 0`。**偽の DB が SQL の文面に除外の句があるときだけ除く**ようにしてあり、句を消す変異を捕まえられる設計になっている（R-30）
- **BF-2** ✔ `0022` は revoke（public・anon）と grant（authenticated）を同じトランザクションで行う。`rpc-guard` の利用者 RPC の条件は、開きすぎ・開き直し・塞ぎすぎ・書き忘れ・位置・コメントの 6 つの形を合成の SQL で固定
- **BF-4** ✔ `.gitattributes` は `db/migrations/*.sql` の 1 行だけ
- **BF-5** ✔ `sellDecision` が `odds.ts` と `v10-accounting.ts` の唯一の判定。D-096 の判定は `oddsFromProbability` の戻り値そのもの（D-094 と同じ許容幅）を使うので、境界の扱いが 2 か所に分かれていない。**経路テスト（V-10 の賭け金 ＝ `buildOddsRows` の行数）** と変異で、測定器と製品が同じ集合を見ることを固定した
- **BF-6** ✔ 判定は `|(payout + floorLoss) / stake − (1 − margin)| ≤ 0.01`。賭け金 0 は不合格。終了コードは 0／1／2 に分けた。**SE の式だけが判定値と合っていない**（§3-1）

---

## 3. レビュー側の発見

### 3-1. ⚠️ SE を、判定値と別の量について出している（照会 Q4 への回答）

- **判定値**は全レースを合わせた比: `R̂ = Σ(払戻 ＋ 切り捨てで減った額) ÷ Σ賭け金`
- **SE**（`judgeKind`）は、**レースごとの払戻率 `Yᵢ/Xᵢ` を等しい重みで平均したもの**のばらつきから出している（`raceRateSum`・`raceRateSqSum`）

売る目の数（賭け金 `Xᵢ`）はレースごとに違います（出走頭数・D-035・D-096 で変わる）。したがって、この 2 つは**別の推定量**で、SE は判定値の誤差になっていません。三連単のように 1 レースの払戻率が「ほとんど 0、ときどき巨大」になる券種ほどずれは大きくなります。

→ **正典 §13.2 に式を固定しました**（2026-09-14 追記）:

```
R̂  = ΣYᵢ ÷ ΣXᵢ
SD = √( Σ(Yᵢ − R̂·Xᵢ)² ÷ (n − 1) ) ÷ X̄        X̄ = ΣXᵢ ÷ n、n = 全レース数
SE = SD ÷ √n        （条件: SE ≤ 0.25pt・D-036）
```

- `Xᵢ` ＝ レース i で売った目の数（賭け金）、`Yᵢ` ＝ そのレースの切り捨て前の払戻額（券種ごと）
- 実装に要る集計は `ΣX・ΣY・ΣX²・ΣY²・ΣXY` の 5 つ。`Σ(Y − R̂X)² = ΣY² − 2R̂·ΣXY + R̂²·ΣX²`
- **検査**: 賭け金がレースごとに**違う**合成のデータで、「等しい重みの平均」と「比の推定量」で SE が**食い違う**例を 1 つ固定する（等しい賭け金だけの例では、2 つの式が一致してしまい、誤りを捕まえられない）。変異として、旧式に戻すとその検査が落ちること

### 3-2. BF-1 の新しい SQL は、本物の Postgres で一度も動いていない

`training-runner-skip.test.ts` は偽の DB です。第 2 便の再実行表には、第 1 便の `af3-demo`（本物の DB で `advanceTrainingWeeks` を回す）が入っていません。`horses.id` は `uuid` なので型の食い違いは起きませんが、**「本物の DB が偽の DB と同じように振る舞うこと」は、偽の DB では確かめられません**（R-30）。→ §5 で staging に 1 回。

### 3-3. ⚠️ 本番を誤って読んだ件（報告 §8-1）の根本原因は、まだ残っている

- 開発側は、**手元の読み取り用スクリプト 3 本**に「`--env` が無ければ例外」を入れた
- しかし原因は、**共通部品 `tools/lib/env.mjs` の `loadEnv()` が `--env` を省くと本番を読むこと**で、これは監査 M-5 そのもの（R-27）。**リポジトリの `tools/` の道具はいまも同じ穴を持っています**
- 「自動モードの判定がすり抜けた」は、**道具の外の防御が効かなかった**という話です。防御は道具の中に置く必要があります

→ **次の DB を使う作業（§5）より先に**、`loadEnv()` を「`--env` が無ければ接続前に例外」にしてください。書き込み系を守っている `assertNotProduction` はそのまま残します。

### 3-4. 第 1 便の AF-2（V-10 待ち）は、§5 の V-10 の 1 本で閉じる

第 1 便の V-10 はメモリ不足で終わりませんでした（`fe7376c`）。第 2 便の V-10 は「切り捨て前・切り捨て後・差」を出すので、**§5 の 1 本の結果で第 1 便の AF-2 も判定します**。

---

## 4. 照会への回答

| # | 照会 | 回答 |
|---|---|---|
| **Q1** | 関数の既定の権限（BF-3） | **(a) を承認。** `alter default privileges for role postgres in schema public revoke execute on functions from public, anon, authenticated;` だけを入れる。`supabase_admin` の行は Supabase の管理領域として触らない。V-20 ⑤ は **`postgres` の行だけ**を判定し、それ以外の行は一覧として印字する。staging で「作って測る」「`place_bet` の authenticated が残る」を実演してから。**実装は次の便** |
| **Q2** | 残りの計測の流し方 | **§5（オーナー決定 2026-09-14）** |
| **Q3** | `0022` で authenticated への付与を書き直した | **追認。** `proacl` が適用前後で `authenticated=X` のまま（報告 §2-3）で、権限の中身は変わっていません |
| **Q4** | V-10 の SE の式 | **§3-1。** 正典 §13.2 に追記済み。実装を直してください |
| **Q5** | SE が届かないときの終了コード 2 | **追認**（判定不能を合格にしない・R-3） |

---

## 5. 残りの作業の段取り（オーナー決定 2026-09-14）

**順番を守ってください。**

| # | 誰 | 作業 | 合格の条件 |
|---|---|---|---|
| 1 | 開発側 | `tools/lib/env.mjs` の `loadEnv()` を「`--env` が無ければ接続前に例外」にする（§3-3）。検査: `--env` なし → 例外 ／ `--env staging` → staging ／ `--env production` → 本番（接続しない単体の検査で） | 変異: 既定を本番に戻すと検査が落ちる |
| 2 | 開発側 | SE の式を §3-1 のとおりに直す | §3-1 の検査と変異 |
| 3 | 開発側 | 1 と 2 をコミット（ファイルを名指し） | — |
| 4 | 開発側 | **⑤**: 既存の staging のレースで、同じトランザクションの中だけ発走時刻をずらし、**`race_odds` に無い目**に `place_bet` を呼んで「発売していない買い目」で拒否されることを確かめ、最後に取り消す（照会 Q2 の ③）。⑤ が確かめるのは「オッズ表に無い目は断られる」なので、D-096 が効いていないレースでも成立します | 拒否の文言・rollback 後にレースの行が変わっていないこと |
| 5 | オーナー | **④**: 別のターミナルで `npx tsx tools/seed-races.mjs --env staging --races 1`。終わったら開発側へ知らせる | — |
| 6 | 開発側 | ④ のレースの `race_odds` を読み、**1.0 倍未満の行が 0 行** | 0 行 |
| 7 | オーナー | **BF-1**: 開発側が用意する週送りの実演（第 1 便の `af3-demo` と同じ形）を 1 回流す | 失敗した馬がいない通常の週送りが、本物の DB で通る |
| 8 | オーナー | **V-10**: 別のターミナルで `npx tsx apps/cli/src/verify-payout.ts --races 500 --seeds 42`（**2 のコミットの後**に）。出力を最後まで開発側へ渡す | 下の注意 |

**V-10 の 1 本についての注意**
- 目的は、**切り捨てで減った分**と、**D-035・D-096 で売らなかった分**を券種ごとに知ることです。どちらも**同じレースの中での比較**なので、レース数を減らしても測れます
- **この 1 本は正式な V-10 ゲートではありません。** SE は届かず、終了コードは 2（判定不能）になる見込みです。それでよいです
- 2,000 レースでは 10 時間半たっても終わらなかったので、500 レースでも数時間かかる見込みです（△ 推定）

---

## 6. 次の便に入れるもの（予告）

- Q1 の既定の権限（`postgres` の行）と V-20 ⑤
- 監査の残り: H-5（権利）・H-6（本番の開発画面）・M-1（馬券の締切）・M-2（確定の止まり）・M-3（自馬の制限）・M-6・M-7

本番への `0021`・`0022` の適用は、引き続き**オーナーの指示で**行います（順番: `0021` → `0022` → 新しいワーカーの配備）。
