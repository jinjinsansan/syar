# 報告 — ゲーム本体 第 2 便（生涯の記録・発見度）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ 開発側 → レビュー側・オーナー
**対象**: 裁定 `REVIEW_CONSULT_HORSE_LIFECYCLE_VERDICT_20260916.md` §8（第 2 便）／ 正典 **§18 LR-1〜LR-8**・**D-108**
**測ったコミット**（R-23・R-28）: 着手時 `5c5b1e8`（GC-1・GC-2 の直しの後）／ 第 2 便は本報告と同じコミット
**ブランチ**: `p4/race-30sec-cuts`

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **△** ＝ 未確認・推定
> ⚠️ **着順にも経済にも効くものは 1 つも入れていません**（★LR-5・D-108）。`packages/race-engine/src` は 1 行も触っていません
> ⚠️ 本番 DB・ワーカーの配備・較正定数の値・正典は触っていません

---

## 0. 結論

1. **生涯イベントログ（HL-2）と馬物語帳の土台（HL-3）を入れました** ✔ — `packages/training/src/story.ts`
2. **発見度（HL-1・D-108）を入れました** ✔ — `packages/sim-engine/src/discovery.ts`。**表示の層だけ**で、`stats`・`potential`・`genotype` に触りません
3. **ゲートの取り直しは不要**です（LR-5 の条件を満たしているため）。★実際に**着順が 1 ビットも動かないこと**は §3 の対照で示しました
4. **画面はデザイナー待ち**（R-13 で依頼・§5）。**DB と RPC は第 3 便**です

---

## 1. 生涯イベントログ・馬物語帳（HL-2・HL-3）✔

### 1-1. 作ったもの

| ファイル | 中身 |
|---|---|
| `packages/training/src/story.ts`（新規） | 出来事の種類 **15 種**（誕生・初調教・デビュー・初勝利・適性の判明・故障・復帰・重賞勝ち・最高格勝ち・名コンビ・自己最高・ラストラン・引退・初産駒・産駒の勝利）／ **定型文** `storyLineOf`／ **物語** `storyOf`（週の順・同じ週は保存の順）／ **記録だけの馬** `isRecordOnly`／ **週送りの対象** `weeklyAdvanceTargets`／ **行の増え方** `storyGrowthOf` |
| `packages/training/src/index.ts` | 公開 |

### 1-2. §18 の約束をどう満たしたか

| # | 約束 | 満たし方 |
|---|---|---|
| LR-1 | 引退馬の記録は消さない | この層に削除の関数を置いていません（★読み取りと組み立てだけ） |
| LR-2 | 記録は「所有」ではない。ただし繁殖・種牡馬は所有 | `isRecordOnly(retired && !breeds)`。**繁殖入り・種牡馬入りは `false`**（＝所有として数える）。検査 ② |
| LR-3 | 週送りの対象にしない | `weeklyAdvanceTargets` が記録だけの馬を外します。★**記録が 100 頭増えても対象が増えない**ことを検査 ① で固定 |
| LR-4 | 文章は定型・実在の人名や団体名を入れない | `storyLineOf` の分岐だけ。★**値が無い項目は文から落とす**（「undefined 着」を作らない）。検査 ③ |
| LR-5 | 着順にも経済にも効かない | この層は**数を返しません**（文字列と真偽値だけ）。§3 の対照 |
| LR-6 | 他人の馬の物語も見えるが個人情報は出さない | `StoryEvent` に**持ち主の情報を持たせていません**（馬名・レース名・騎手名・週だけ） |
| LR-7 | 発見度を物語に書くのはレースの後 | `discoveryAdvanced(before, after)` を**レース後に呼ぶ**前提の形（§2） |
| LR-8 | 行の増え方を測って報告する | `storyGrowthOf`。★**実測は DB が入る第 3 便**（いまは 1 頭あたりの見積りを出す関数だけ）△ |

## 2. 発見度（HL-1・D-108）✔

### 2-1. 作ったもの

| ファイル | 中身 |
|---|---|
| `packages/sim-engine/src/discovery.ts`（新規） | 段階 4 つ（`unknown` → `hint` → `narrow` → `known`）／ `discoveryStageOf(relevantRuns)`／ 画面の文字 `discoveryLabelOf`（「？？？」「得意かも？」「B 以上？」「評価」）／ `discoveryAdvanced(before, after)`／ 刻み `DISCOVERY_STEPS = [1, 3, 6]` |
| `packages/sim-engine/src/index.ts` | 公開 |
| `apps/cli/src/calibration.ts` | `DISCOVERY_STEPS` を**見せ方の刻み**として `EXEMPT` に理由付きで登録 |

### 2-2. D-108 の条件をどう満たしたか

| # | 条件 | 満たし方 |
|---|---|---|
| ① | 決定論 | **経験の回数だけ**から決まります。`Date.now()` も乱数も読みません |
| ② | ★表示と同じ量から導く | **素質の数値を一切受け取りません**。★は `stars.ts`、発見度は「回数」— **別々に素質を読む経路を作っていません** |
| ③ | ★**検査は「入力の形」で書く** | `discoveryStageOf.length === 1`（引数は回数だけ＝**素質を渡す口が無い**）を検査で固定。★**禁止語の一覧では書いていません**（D-098 の穴を避ける） |
| ④ | レース中の画面に出すなら D-098 に照らす | ★この層は**レースの後に呼ぶ**前提です（`discoveryAdvanced`）。レース中の画面には出しません |

## 3. ★着順にも経済にも効いていないこと（対照）✔

| 根拠 | 中身 |
|---|---|
| **触っていない範囲** | `packages/race-engine/src` は **1 行も変えていません**（`git show --stat` で確認できます） |
| **型の上での対照** | 新しい 2 モジュールは **`resolveRace` からも `advanceWeek` からも呼ばれていません**（★この便では**どこからも呼ばれない**＝R-15「入っているが使われない」） |
| **返す値** | `story.ts` は文字列と真偽値、`discovery.ts` は段階の語だけ。★**能力・スコア・EP・PP を返す関数がありません** |
| **ゲート** | LR-5・D-108 の条件を満たすので**取り直し不要**（裁定 §8）。V-14・V-15・V-7 は第 1 便から同値のまま △ |

## 4. 検査 ✔

| # | 内容 | 結果 |
|---|---|---|
| 1 | ★**記録だけの馬が週送りの対象から外れる**（LR-3）。★記録が 100 頭増えても対象は 2 頭のまま | ✔ `packages/training/test/story.test.ts` ① |
| 2 | 記録は所有ではない。★**繁殖・種牡馬に上がった馬は所有として数える**（LR-2） | ✔ ② |
| 3 | ★**15 種すべての文が定型で、値が無い項目があっても壊れない**（「undefined」「null」が文に漏れない・LR-4） | ✔ ③（全数） |
| 4 | 物語は週の順。**同じ週は保存の順のまま**（安定） | ✔ ④ |
| 5 | 行の増え方を測れる（LR-8） | ✔ ⑤ |
| 6 | ★**発見度の引数は「経験の回数」だけ**（★素質を渡す口が無い＝**入力の形**で見る・D-108 ③） | ✔ `packages/sim-engine/test/discovery.test.ts` ① |
| 7 | 段階は 4 つ・**経験が増えるほど進み、下がらない**（単調） | ✔ ② |
| 8 | ★**分かる前は評価を出さない**（「？？？」に評価が混ざらない）・数値はどの段階でも出さない | ✔ ③ |
| 9 | ★**段階が上がった回だけ「判明した」と言える**（LR-7・レースの後に呼ぶ形） | ✔ ④ |

- `npm run typecheck` … **通過**（exit 0）
- `npx vitest run --maxWorkers=2 --minWorkers=1` … `Test Files 1 failed | 187 passed (188)` ／ `Tests 1 failed | 1838 passed (1839)`・終了コード 1・**154 秒**
  - 落ちたのは変更前からの `edit-grammar-audit` ⑨（独走代表 expected 'contest' to be 'solo'）だけで、**1 件から増えていません**
  - 件数は 1830 → **1839**（+9）。増えたのは `story.test.ts` 5 件と `discovery.test.ts` 4 件です
- ⚠️ **較正定数の登録簿に 1 回落ちました**（`DISCOVERY_STEPS` が未登録）。★**見せ方の刻み**（いつ分かるか）であって較正値ではないので、`EXEMPT` に**理由付きで**載せました

## 5. 未実装（第 2 便の範囲で残したもの）

| # | 中身 | いつ |
|---|---|---|
| 1 | **DB**（`horse_story_event` テーブル・記録だけの馬の保持）と**行の増え方の実測**（LR-8） | 第 3 便（RPC と同じ便） |
| 2 | **画面**（馬物語帳・履歴書型の馬詳細・発見度の見せ方） | ★**デザイナーのカード待ち**（R-13 で依頼） |
| 3 | **発見度を進める配線**（レース後にその適性が試された回数を数える） | 第 3 便（レースの記録が DB に入ってから） |
| 4 | 産駒・子孫の物語のつながり | 第 3 便以降 |

## 6. 次にやること（裁定の順番どおり）

1. **V-10 の測定**（実行中・§別報告）
2. **第 3 便**: 出走登録と投票の RPC（★**同じ便で**・§9.5 の移行を含む）・購入の DB・格の配線・**物語の DB**
3. 第 4 便: 個性と騎手を**効果 0 で**入れる
