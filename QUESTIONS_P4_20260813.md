# QUESTIONS_P4_20260813 — 着手前の照会

## 本便で正典の改訂が要る決定をしたか

**していません。**（★以下はすべて「決める前に訊く」ためのもので、実装は着手していません）

---

## ★Q-P4-01【最優先】`technical-preferences.md` が存在しません

指示書が **C-1〜C-4 と操作方式の出典**として参照している文書が、リポジトリのどこにもありません。

```
$ find . -iname "*technical*" -not -path "./node_modules/*"
（0件）
$ ls .claude/
settings.local.json      ← docs/ ディレクトリ自体が無い
$ grep -n "60fps\|ドローコール\|5MB\|256MB" STAR_SPEC_v2.0.md
（0件）★正典にも記述がありません
```

つまり**現在、次の5つに出典がありません**:

| 基準 | 値 | 状態 |
|---|---|---|
| C-1 | 60fps / フレーム予算 16.6ms | ★出典なし |
| C-2 | ドローコール 500以下 | ★出典なし |
| C-3 | 初回ロード 5MB 以下（WASM + アセット） | ★出典なし |
| C-4 | メモリ 256MB 以下 | ★出典なし |
| U-2 | 「クリック/タップ1種類で完結・ホバーに依存しない」 | ★出典なし（後述のとおり**正典と食い違います**） |

**★指示書 §7 は「描画を作る前に C-3 と C-1 の予算配分を決めよ」と書いています。**
**その予算の出典が無い状態では、配分を決められません。**

→ **文書を共有いただくか、値を正典に取り込んでいただけますか。**
   ★こちらで「妥当そうな値」を置くことはしません（正典にない仕様を推測で埋めない）。

### ★併せて: C-3 の「WASM」について

C-3 が **WASM** を含むと書かれていますが、**正典 §12 に WASM の記述はありません**。
§12.8 は「**Canvas 2D**」と書いており、§12.1 は「軽さが設計の一部」です。

→ **WASM を使う前提があるのでしょうか。** 無いなら C-3 の文言から外れると読みます。

---

## ★Q-P4-02 指示書と正典が食い違っています（操作方式）

| | 記述 |
|---|---|
| **指示書 U-2** | 全操作は**クリック/タップ1種類で完結**（マウスホバーに依存しない） |
| **正典 §12.6** | **モバイル=タップ / PC=クリック・スペースキー**の両対応 |

★**正典は「スペースキー」を明示的に要求しています。**
指示書の「1種類で完結」と両立するかは読み方によります:

- 読み方A: 「**ホバーに依存するな**」が主旨で、スペースキーは補助として可 → 両立する
- 読み方B: 「入力手段は1つだけ」→ **正典のスペースキーと矛盾する**

→ **どちらでしょうか。** 正典が優先だと理解していますが、
   ★指示書がそれを知ったうえで上書きしている可能性もあるので、確認します。

---

## ★Q-P4-03 C-6 の測り方 — 「人間」を何人集めるのですか

C-6 は P4 で最も重要な基準だと指定されています。こちらもそう考えます。
ただし**測り方が決まらないと、また「PASS だが何も検証していない」を作ります**。

```
★人間が UI 経由で操作したときの interventionMult が、
  ハーネスの「最適な仕掛け」と「早すぎる仕掛け」の範囲を再現すること
```

**測れない形になりうる点が3つあります:**

### ① 標本が「私1人 × 数回」なら、それは人間の分布ではありません

P3 の教訓（**SD を測ってから減らす**）をそのまま当てると、
**まず操作者間・試行間のばらつきを測らないと必要数が出ません**。
★そして開発者は**どこで押すべきかを知っている**ので、
**最も測りたい「押しどころが分かるか」を、いちばん測れない標本**です。

### ② 「範囲を再現する」の判定式

- 最適寄りの操作で **上端に届く**こと？
- 早すぎる操作で **下端に落ちる**こと？
- それとも**分布の重なり**を見るのですか？

★こちらの見立ては「**両側**」です（R-2）。
「上手く押せば良くなる」だけなら、**ゲージを見ずに連打しても良くなる**実装で通ります。
**下手に押したら悪くなる**ことを同時に要求しないと、C-6 は片側だけの検査になります。

### ③ ★「合格させるために最も安易な方法」（R-16）

C-6 を最も安易に満たす実装は、**介入の効きを大きくすること**です。
効きを上げれば人間でも範囲に届きますが、それは V-9a（±10%のハードキャップ）と
V-13 の較正を壊します。

→ **C-6 と V-9a/V-13 は同時に測る**という理解でよいですか。

---

## ★Q-P4-04 「面白いかの判断材料」の3点目について

指示書 §5 の3点目:

> **判断が分かれたときに何を変えられるかの選択肢**

★**ここが最も難しい**と考えています。理由は、**動かせるものの多くが較正定数**だからです。

```
例: 「仕掛けが地味で面白くない」と言われたとき
    → 介入の効きを上げる  → ★V-9a（±10%キャップ）と V-13 の較正を壊す
    → 演出を派手にする    → ★C-1（60fps）と C-3（5MB）を食う
```

つまり**「面白くない」への対処は、ほぼ必ず既存のゲートに当たります**。

→ **選択肢の一覧は「何を動かすと、どのゲートを測り直すことになるか」の対応表**として
   出す、という理解でよいですか。
   ★「自由に動かせるもの」だけを並べると、**ほとんど何も残りません**。

---

## ★Q-P4-05 U-4（第三者検証）の到達点

> **第三者が画面の情報だけで §8.6 を再計算できる**（手順つき）

**「第三者」の想定を確認させてください。**

| 想定 | 要るもの |
|---|---|
| A: 技術者が手元でスクリプトを書ける | seed_commit / seed_reveal / 出走表 / 手順の文書 |
| B: 一般の利用者がブラウザだけで確認できる | ★**検証ツールそのものを画面に載せる**（実装量が別物） |

★**A と B では作るものが違います。** どちらでしょうか。

★なお **D-056 で `entrant_snapshot` を凍結した**ので、
**「そのレースが何を入力に計算されたか」は DB に残っています**。
これは B を現実的にする材料です（P3 の副産物）。

---

## 参考: 着手できる範囲

**Q-P4-01 の予算が決まらなくても着手できるもの**があります。

| | 内容 | 依存 |
|---|---|---|
| ✅ | **§12.8 の描画抽象化**（位置データ → 描画コマンドの純粋関数） | 予算に依存しない。★正典が「**初日からやること**」と明記 |
| ✅ | **同じ seed から同じ描画コマンド列が出ること**（C-5 の核） | 同上 |
| ⛔ | スプライト・アニメーション・配信 | ★C-1/C-3 の予算が要る |

→ **ご指示がなければ、Q-P4-01 の回答を待つ間に §12.8 の抽象化から着手します。**
   ★これは「作ってから削る」の対象になりません（データ構造であって、アセットではないため）。

---

# 追補（アートバイブル受領後）

アートバイブルと D-057 を受領しました。★**§5 の実測項目に着手する前に、2件出ます。**

## ★Q-P4-06【最重要】「描画は結果を再生する」の**再生元が存在しません**

指示書 U-1:

> ★**描画はレース結果を再生するもので、結果を決めるものではない。**

**そのとおりだと考えます。ただし、再生できる形の結果がありません。**

```
RaceResultEntry が持つもの:
  finishPosition / timeSec / timeGapSec / finalScore / interventionMult / marginLabel

★持たないもの: **時刻ごとの位置**
```

エンジンはスタミナを**解析式**で解いており（`raceSec = distance / avgSpeed`、
残り800mから減る・残り400m前に尽きたらバテ）、**毎秒の位置を計算していません**。

### ★何が困るか

| # | 問題 |
|---|---|
| 1 | **道中の位置が定義されていない**。誰がどこを走っているかを、描画側が**発明する**ことになります |
| 2 | ★**介入UIの4局面（§8b）は「残り800m / 400m」という位置で決まります。** 位置が無いと、**いつ仕掛けボタンが効くのかを画面に出せません** |
| 3 | ★**単純な等速モデル（各馬が自分の走破タイムで一定速）にすると、順位が最初から最後まで変わりません。** 「勝負所で仕掛ける」が**視覚的に無意味**になります |

### ★これは「絵の話」ではありません

★**位置モデルは、介入UIがいつ何を受け付けるかを決めます。**
つまり **C-6（人間の操作が範囲を再現するか）の前提**です。
**先に決めないと、C-6 は「何を測っているのか分からない測定」になります。**

### こちらの見立て（★決めていません）

| 案 | 内容 | 懸念 |
|---|---|---|
| A | 描画側が結果から補間する（純粋関数） | ★**発明**になる。正典に無い挙動が画面の事実になる |
| B | エンジンが位置の時系列も返す | ★**確定処理が重くなる**（A-1 の余裕に効く）。既存の V-13 較正への影響も要確認 |
| C | 位置モデルを**正典に定義**し、描画とエンジンが同じ式を使う | 実装は増えるが、**両方が同じ根拠を持つ** |

★**こちらの見立ては C** です。理由は、位置が
**「絵の都合」ではなく「介入がいつ効くか」という機構の一部**だからです。
A にすると、**画面の見た目と機構の判定が別の式で動く**ことになり、
P3 で繰り返した「生成と確定で別々に組む」と同じ形になります。

→ **裁定をお願いします。** 決まるまで、位置モデルに依存する部分は作りません。

---

## ★Q-P4-07 §5 の実測（馬体は共通で足りるか）に、私が着手できる範囲

`image-cockpit` は**ブラウザUIと Codex 連携**で動くので、**1頭目の生成にはオーナーの操作**が要ります。

★そのうえで、**絵が無くても先に答えが出る部分**があります。

```
32×32 のうち、勝負服が占める画素は概ね 20〜40px と見込まれます。
  → その画素数で、何パターンを**識別可能に**作れるかは、絵柄ではなく**画素予算**の問題です。
  → これが決まれば「勝負服のパターン数」の上限が出て、**C-3 の見積もりが立ちます。**
```

→ **こちらで「勝負服の識別可能パターン数」を測る道具を作ってよいですか。**
   ★ただし**それだけでは §5 の答えになりません**。「馬体を共通にしても違和感が無いか」は
   **絵を見ないと決まらない**ので、そこはオーナーの判断です。
   **私が出せるのは「上限はこれだけある」という数字**までです。

★**なお Q-P4-01（`technical-preferences.md` が存在しない）は未回答のままです。**
C-3 の上限値そのものが無いので、**見積もりを出しても比べる相手がいません。**

---

# 追補2（1頭目の試作を受けて）★正典 §12.1 の改訂が要ります

## ★Q-P4-08【最重要】32×32 では、オーナーの求める見え方になりません

**1頭目を作り、オーナーに実物を見ていただいた結果です。**

| 試したもの | オーナーの評価 |
|---|---|
| 細密な絵を 32×32 に機械的に縮小 | ★**「ぼやけており、何かわかりません」** |
| **最初から 32×32 のドット絵として描かせた**もの（12色・輪郭立て） | ★**同じく不可** |
| 32 / 48 / 64 / 96 / 128 px の比較 | 「馬に見えるのは **96 と 128**」 |
| ★**細密版（350×222）** | ★**「これが一番求める2Dの世界観です」** |

**そして実際のレース画面（1280×720）を3つの大きさで作って選んでいただきました:**

```
  A  350×222（原寸）  → ★可
  B  220×140          → ★可
  C  140× 89          → ★**不可**
```

★**つまり許容範囲は 220〜350px** で、**正典 §12.1 の「32×32 前後」の 7〜11倍**です。

### ★私が3回作り直しても解決しなかった理由

**作り方の問題ではありませんでした。** 32×32 に馬と騎手を入れると、**輪郭以外は原理的に消えます**。
アートバイブル §3 が「32×32 では馬体で描き分けられない」と書いているのと**同じことが、絵全体に起きます**。

### ★「エンジン側の契約が動く」の中身を確認しました

D-057 は「**32×32・4〜6フレームは不変。ここを変えるとエンジン側の契約が動く**」としています。
**実際に何が動くかを機械で確かめました:**

```
$ grep -n "size|width|height" packages/render/src/*.ts
（0件）★描画層は寸法を一切持っていません
```

- **描画層**（`packages/render`）… スプライトは「シート名＋フレーム番号」で参照。**32 でも 350 でも同じコードで動きます**
- **レースエンジン**… **絵を知りません**（着順と時間だけを返す）
- **依存していたのは文書だけ**（正典 §12.1 とアートバイブル §3/§5/§7）

★**したがって「契約が動く」の実体は、こちらでは見つかりませんでした。**
見落としがあればご指摘ください。**無いのであれば、寸法は絵の都合だけで決められます。**

## ★Q-P4-09 18頭をどう見せるか（寸法とセットの設計）

大きくすると**画面に映る馬が減ります**（実測）:

| 馬の大きさ | 画面（720p）に入る段数 |
|---|---|
| 350px | **2段** |
| 220px | **3段** |
| 140px | 6段 |

**18頭立てなので、A/B いずれでも「全頭を同時に映す」ことはできません。**

| 案 | 内容 | アートバイブルとの整合 |
|---|---|---|
| 1 | **カメラが自馬を追う**（他は画面外） | 競馬中継の画作り（§7 の参照） |
| 2 | ★**道中は引き、勝負所で寄る** | ★§2「**勝負所だけが跳ねる。道中は意図的に単調に**」と素直に噛み合う |
| 3 | 常に全頭 | ★140px しか選べず、**オーナーが不可と判断済み** |

★**こちらの見立ては 2** です。**演出でしか出せない「felt impact」**（前便の裁定）を、
**カメラの寄りが担う**ことになり、V-9a の ±10% を触らずに済みます。

## ★併せて: アートバイブルの改訂が要る箇所

| 箇所 | 現在 | 問題 |
|---|---|---|
| §3 P-3 | 「32×32 で18頭が並んでも」 | 寸法が変わる |
| §5 | 「スプライト 32×32 前後」 | 同上 |
| §5 | 「**馬体は共通スプライトの色替えで足りるか**」 | ★**220px なら馬体を描き分けられます**。前提が変わります |
| §7 禁止6 | 「馬体で個体を描き分けること」 | ★同上。**32×32 だから禁止だった**もの |

★**アートバイブルはレビュー側の納品物なので、こちらでは書き換えていません。**

## 実測（記録）

```
1頭あたり（6フレーム・PNG）
   32px:  2.4 KB      96px:  7.5 KB
   48px:  3.6 KB     128px: 10.8 KB
   64px:  4.9 KB     ★350px: 640 KB（原画そのまま・未圧縮）
```

★**バイト数は判断の材料になりません。** 128px でも18頭で 194KB です。
**効くのは「画面に何頭映るか」だけ**でした。
