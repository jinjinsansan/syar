# 【STAR】裁定 — AL-9（1 日の本数をサイクルから導く）と測定器の移設を受理

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **対象** | コミット `97d3323` ／ `REPORT_AL9_AND_TOOLS_20260918.md` |
| **指示** | `REVIEW_CONSULT_ARCADE_LOOP_ANSWER_20260918.md` **AL-9** ／ `REVIEW_AL6_ODDS_COST_VERDICT_20260918.md` §6（測定器を `tools/` へ） |

---

## 0. 判定

✅ **PASS（条件なし）。**

★**特に評価する判断が 2 つあります**（§2）。★どちらも**本日の教訓を、言われる前に適用したもの**です。

---

## 1. 照合（★レビュー側が独立に確かめたもの）

### 1-1. AL-9 — ✅ 導出になっています

```
packages/scheduler/src/programme.ts:35-41
  export const RACES_PER_DAY = ((): number => {
    const n = DAY_MS / CYCLE_MS;
    if (!Number.isInteger(n) || n <= 0) throw new Error(...);   ← ★R-27
    return n;
  })();
```

✔ **いまの値は 144 のまま**（`DAY_MS / 600000 = 144`）。✔ **割り切れないサイクル長は投げます**（R-27「既定と縮退は狭い側・安全な側へ」）。

### 1-2. 🔴 ★**安全性の性質が本物であることを、算術で確かめました**

★開発側の主張「**T-13 で `RACES_BY_CLASS`（合計 144）の振り直しを忘れたら検査が止める**」を検算しました。

| `CYCLE_MS` | `RACES_PER_DAY` | 表の合計 | 結果 |
|---|---|---|---|
| **今 10 分** | 144 | 144 | ✅ 緑 |
| ★**T-13 3 分** | **480** | 144 | 🔴 ★**検査が赤**（`programme.test.ts:29-30` の `expect(sum).toBe(RACES_PER_DAY)`） |
| 割り切れない 7 分 | ★**throw** | 144 | 🔴 ★**起動時に止まる** |

→ ★**「気づかずには進めません」は事実です。** ✔ 表の合計 42+36+24+18+15+9 = **144** も確認。

★**共有ツリーで開発側が作業中のため、私は変異を再現していません**（★開発側が実施し、赤→緑・`diff` 差分なしを報告済み）。★**代わりに検査の実体（`programme.test.ts:29-30`）と算術で確かめました。**

✔ **レビュー側も `packages/scheduler` を独立再実行 — 10 ファイル・118 件すべて緑。**

### 1-3. 較正登録簿の `DAY_MS` を EXEMPT に — ✅ **正当です**

> `DAY_MS`: 実時間 1 日のミリ秒（24×60×60×1000）。**単位の換算そのもので、較正で動かす値ではない**

★**免除の理由として正しい形です** — ★`DAY_MS` は**定義**であって、測って決める値ではありません。★**「較正定数の登録簿に入れない」のではなく「免除の理由を書いて登録する」**形にしたのも正しい（★黙って外していない）。

---

## 2. ★★特に評価する判断 2 つ

### 2-1. ★`.mts` ではなく `.mjs` にしたこと — **今日の教訓を、言われる前に適用しています**

> `tool-guard` の走査は `.mjs` しか見ないので、`.mts` だと **R-24 の登録簿の外に出ます**。
> 本日「登録簿の外で増えた」事例が 3 件挙がったところなので、**4 例目を作りませんでした**。

★**これは私が指示していません。** ★**開発側が、本日の裁定 3 本（露出／検査の凍結／乱数 ID）から自分で一般化して適用したものです。**
✔ 確認: `tools/lib/classification.mjs:38-39` に `bench-odds-entrants.mjs` / `bench-odds-cost.mjs` が登録済み。

★**「登録簿の外で増やさない」を、道具を作る側が最初から守った最初の例**です。★**この形を続けてください。**

### 2-2. ★道具そのものに、測り方の警告を埋めたこと

```
tools/bench-odds-cost.mjs:17-22
  【🔴 ★測る前に読んでください — 機械のばらつき】
    ★ハイブリッド CPU（P コア＋E コア）では、載るコアで 2 倍変わります
    → ★P コアに固定して回してください（Windows: start /affinity 3）
    → ★各条件を --repeat 回まわし、最小値を採ります
    ⚠️ ★1 回の値で比を論じないこと。
       ★測定のばらつきより小さい差を「差」と読まない
```

★★**最後の 1 行は、私が ES-6 裁定を撤回した理由そのものです。**
★**裁定書は読まれないことがありますが、道具は使うときに必ず開かれます。** ★**教訓を裁定書ではなく道具に置いたのは、私の書き方より強い形です。**

★**移設後の実行でも外れ値が 1 つ出た**と報告にあります — ★**警告が要ることが実地で裏付けられています。**

---

## 3. △ 私が確かめていないこと（★正直に書きます）

| # | |
|---|---|
| 1 | ★**検査 2,079 本の全数**は回していません（★`packages/scheduler` の 118 件のみ独立再実行）。★「落ちた 1 本は既知の赤・`packages/render` は触っていません」は**開発側の申告のまま**です（△） |
| 2 | ★**変異（`CYCLE_MS` を 3 分にして赤を見る）は再現していません**（★共有ツリーで開発側が作業中のため）。★§1-2 の算術と検査の実体で代えました |

---

## 4. 次

★**開発側の止め方は正しいです** — ★**T-13 は起票 7 件の承認待ちで止める**（★正典の改訂が先、という裁定の順）。

| 順 | 中身 | 前提 |
|---|---|---|
| 1 | **T-13 の実装**（`CYCLE_MS` 3 分・`CYCLES_PER_WEEK` 80・フェーズ配分・**AL-10 の註記**・★**`RACES_BY_CLASS` の振り直しも同じ便で**） | ★**起票 7 件の承認** |
| 2 | **段 2 の配備**（`0021`〜`0032` ＋ ワーカー）★**最初の数周を実測して報告** | オーナー |
| 3 | クラス分けの便（出走資格の述語） | — |
| 4 | ★**そのあとに** 帯のゲート ＋ GB-6 ＋ 較正の取り直しを **1 回で** | 1〜3 |

★**②の「最初の数周の実測」で、R-28 の最後の条件（本番機での周）が閉じます。**

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
