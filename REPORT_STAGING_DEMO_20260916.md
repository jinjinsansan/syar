# 報告 — staging の実演（10 場のレースを作り、確定し、凍結から再計算する）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ 開発側 → レビュー側・オーナー
**対象**: 裁定 `REVIEW_ENGINE_LANE_SPEED_ES6_VERDICT_20260916.md` §3 の 2・3 ／ 回答 `REVIEW_ENGINE_LANE_SPEED_ES5_ANSWER_20260916.md` §3（検査 5 ＋ 10 場配線 VW-6 の 4・6・7）
**HEAD**: `160ccf5`（コードの変更なし。ES-6 のエンジン `ccfb684` で生成・確定しました）

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **△** ＝ 未確認・推定
> ⚠️ 流したのは **staging だけ**です。本番には接続していません（読み取りも今回はしていません）。
> ⚠️ オーナーのご指示で、**開発側のこのセッションから** staging の 2 つの道具を流しました（回答では「オーナーのターミナル」でしたが、オーナーがコマンドをこちらに渡されたため）。

---

## 0. 結論

1. **26 本を作り、26 本すべてを確定しました** ✔
2. **走路の凍結は 26 本すべてに入り、全件が楕円で、`venues.ts` の現在の値と完全に一致**しました。**10 場すべてが出ました** ✔
3. **新しく確定した 26 本は、凍結から再計算した着順が全件一致**しました ✔
4. **変異（凍結を読まない）を入れると、スターパーク以外の場で食い違いが出ました**（天河 1400m） ✔
5. 既知の食い違い（cycle 686・868）は**走路の凍結が無い古い行**で、今回の 26 本とは別です ✔

---

## 1. 流したもの ✔

| # | コマンド | 結果 |
|---|---|---|
| 1 | `npx tsx tools/seed-races.mjs --env staging --races 26 --trials 10000` | 終了コード 0。**26 本作成**（cycle 5573〜5598・1 本あたり約 2 秒・発走 17:00Z〜21:10Z） |
| 2 | `npx tsx tools/settle-races.mjs --env staging --from-cycle 5573` | 終了コード 0。**26 本確定**。`--from-cycle` を付けたので、以前から残っている未確定 2 本には触れていません |
| 3 | `npx tsx tools/verify-entrant-freeze.mjs --env staging --recompute-settled` | 終了コード 1（§3 の既知 2 件のため） |
| 4 | `npx tsx tools/verify-entrant-freeze.mjs --env staging --recompute-settled --ignore-course-frozen` | 変異の実演（§4） |
| 5 | 走路の凍結の読み取り（`out/gen/es/staging-seeded-frozen.mjs`・読み取り専用・git の対象外） | 終了コード 0（§2） |

- ⚠️ **`--trials 10000` は本番（3,896,104）と違います**。売られる買い目の数が変わるので、**この実演でオッズの妥当性は主張しません**（`seed-races.mjs` の註記どおり）。ここで確かめているのは**走路の凍結と着順の再計算**です

## 2. 走路の凍結（VW-6 の 4）✔

- 26 本すべてに `course_frozen` があり、**`parseFrozenCourse` で読めて**、`venueId` と `course_id` が一致
- **全 26 本が `oval`**
- 保存された凍結は **`frozenCourseOf(venueId)`（`venues.ts` の現在の値）と完全に一致**（キーの並びを揃えて JSON で比較）
- **場の内訳**: 陽光 3・スターパーク 3・霧ヶ原 3・天河 3・大河原 3・青音 3・白砂 2・潮風 2・月見 2・銀嶺 2 ＝ **10 場・26 本**。出ていない場はありません

## 3. 再計算（検査 5・VW-6 の 6）✔

```
確定済み 56 本 / 一致 31 / ★食い違い 2 / 出走馬の凍結が無く飛ばした 23
★走路の凍結が無い（0023 より前）ので DEFAULT_OVAL で再計算した行: 7 本
```

| 行 | 内訳 |
|---|---|
| **今回の 26 本** | **全件一致**（場ごと: 青音 3・銀嶺 2・霧ヶ原 3・大河原 3・潮風 2・白砂 2・スターパーク 3・天河 3・月見 2・陽光 3） |
| 凍結の無い古い行 7 本（VW-6 の 7 の件数） | 一致 5 ／ **食い違い 2**（cycle 686・2026-08-12 生成 ／ cycle 868・2026-08-13 生成）。どちらも **D-071（2026-08-16）より前**に確定した行で、走路の凍結もありません。**前回（ES-5 §3）と同じ 2 件で、増えていません** |
| 飛ばした 23 本 | 出走馬の凍結（`entrant_snapshot`）が無い古い行 |

- **ES 便の検査 5 は、これで照合する行ができました**。ES-5 の時点では D-071 以後に確定した行が 0 本で、照合の中身がありませんでした
- 今回の 26 本は **ES-6 のエンジン（`ccfb684`）で生成・確定し、同じエンジンで再計算**しています。別のコミットとの突き合わせではありません △

## 4. 変異の実演（凍結を読まない）✔

```
確定済み 56 本 / 一致 30 / ★食い違い 3
  …（既知の 2 件）
  cycle=5580 1400m tenga 生成 2026-09-15 走路の凍結 有り
    保存   6,16,3,1,12,17,2,13,11,14,7,10,5,15,8,9,4
    再計算 6,16,3,1,12,17,2,13,11,7,14,10,5,15,8,9,4
```

- **スターパーク以外の場（天河）で食い違いが出たので、「凍結を読まない形」は捕まります**（回答 §3 の条件）
- 食い違ったのは 26 本中 1 本です。走路の形の違いが動かすのは距離ロス（数馬身）で、**着順が入れ替わるのは着差の小さい組だけ**だからです △。捕まえる力はこの 1 本で足りています（スターパークなら 0 本になり、区別がつきません）

## 5. 残っていること

| # | 誰 | 中身 |
|---|---|---|
| 1 | レビュー側 | 10 場配線の最終裁定（`REVIEW_VENUE_WIRING_VERDICT_20260915.md` の VC-1〜VC-4・V-4〜V-6 と V-18 の残りの計測） |
| 2 | オーナー | 本番へ `0023` と新しいワーカーの配備（配備後の周の秒数で速さの最終確認） |
| 3 | オーナー | `0021`・`0022` の本番への適用（`REPORT_G3_PROD_DB_READONLY_20260916.md` §4 の手順・**まだ当てていません**） |
| 4 | オーナー | push（**していません**） |
