# 【STAR】裁定: I-2 プレイヤーの配合の確定（報告の照合）— 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: `REPORT_I2_PLAYER_BREEDING_20260922.md`（開発側 `star-aa`・`3aa5604`・`4a51eda`）
- 照合したもの: `apps/worker/src/player-breeding.ts`（全文）／`apps/worker/src/breeding-runner.ts`（変更箇所）／`apps/worker/src/main.ts:818-836`／`db/migrations/0061_foal_requests.sql`・`0062_request_initial_breeding_fix.sql`（表と RPC の頭）
- 運用: オーナー指示（2026-09-22）「承認不要で自立的に進める」に基づいて発行する

## 判定

**✅ staging の到達点として受理する。🔴 本番へ出す前に 3 件を直すこと（§1〜§3）。** ★3 件のうち §1 は**私の前の裁定の書き方が招いたもの**である。

| # | 論点 | 判定 |
|---|---|---|
| 1 | 🔴 乱数の種が、クライアントの選んだ値から作られている | **本番前に直す**。★私の裁定 `55b2fd4` §3 の書き方の誤り |
| 2 | 🔴 配合の確定に時間の予算が無い（周がレースの処理ごと延びる） | **本番前に直す** |
| 3 | 🔴 必ず落ちる要求が先頭に居座り、後ろの要求を永久に止める | **本番前に直す** |
| 4 | ⚠️ NPC の配合の「仔を入れる → 母の印を取る → 取れなければ消す」が 1 取引でない | 本番前に直すことを推奨 |
| 5 | ⚠️ `breed()` の中の `canMate` が年の尺度のずれで効いていない（NPC 経路も同じ） | 記録する。試験を 1 本足す |
| 6 | 報告 §3 の発見 1〜5 | ✅ 5 件とも妥当。とくに 2・3（母の取り合い）は私の裁定の条件に無く、開発側が見つけた |
| 7 | 報告 §4 の選択（誕生週・免除の記帳・失敗の語） | ✅ 受理 |
| 8 | 次の N-1 の測定 | ✅ 進めてよい |

---

## 1. 🔴 乱数の種がクライアントの選んだ値から作られている — ★私の裁定の誤り

✔ [CODE]:
- `foal_requests.id` は `uuid primary key` で**既定値が無い**（`0061_foal_requests.sql:28`）。値は RPC の引数 `p_request_id` をそのまま入れる（`0062:18`・`:69`）。つまり**クライアントが選ぶ**
- 仔の ID と種は `idAndSeedFromKey('player-foal|' + req.id)`（`player-breeding.ts` `confirmInitialBreeding` 内）

→ **クライアントが、自分の仔の乱数の種を選べる。**

- 今これが悪用されない理由は 1 つだけ: **父母の `genotype` を外から読めない**ので、手元で `breed()` を回して良い種を探せない。`breed()` のコードそのものは隠していない（デザイナーへの依頼文 `DESIGN_REQUEST_FIRST_HORSE_ONBOARDING_20260922.md` が GitHub の URL を案内している）
- ★守りがこの 1 枚だけだと、将来どこかで `genotype` が漏れた日（あるいは戦績から十分に推し量れるようになった日）に、**初回の配合で振り直しが成立する**（D-120 ①・D-079 の目的）
- 🔴 ★**原因は私の裁定 `55b2fd4` §3 の書き方**。「DB に記録した要求 ID から作る」と書き、**その ID を誰が作るかを書かなかった**。§2 では冪等キーを「クライアントが作って送る」と書いており、両者を同じ値にすれば今の実装になる。★開発側は裁定どおりに作った

**決定（§3 を改める）**: 仔の ID と種は、★**クライアントが選べない値**から作る。
- 例: `foal_requests` に `seed_key uuid not null default gen_random_uuid()` を足し、RPC が行を作るときに DB が決める。種は `idAndSeedFromKey('player-foal|' + seed_key)`
- ★乱数を使うのは**要求の行を作る瞬間だけ**で、その値は記録される。確定の計算は記録から何度でも再現できるので、決定論（憲法 §1-4）は保たれる
- 冪等キー（第 1 段）はクライアントの `id` のまま（そちらは再送を束ねるための値で、種ではない）
- 試験: ★**同じ `id` を 2 回送っても `seed_key` が変わらない**（2 回目は新しい行を作らないので当然だが、釘を打つ）／★**クライアントが `seed_key` を渡す口が無い**（RPC の引数に無いことを構文で見る）

## 2. 🔴 配合の確定に時間の予算が無い

✔ [CODE]: `runPlayerBreeding` は周の**末尾**で呼ばれ（`main.ts:824`）、上限は**件数** 20（`PLAYER_BREEDING_BATCH`）だけ。その後で `TICK_MS - elapsed` を待つ（`:835-836`）。

- 報告の実測は 1 件 2.8〜2.9 秒 → 20 件で **58 秒**。周は 60 秒なので、他の処理と合わせると**周が 60 秒を超える**
- ★周が延びると、**次の周のレースの処理（締切・発走・確定）がそのぶん遅れる**。レースは 6 分周期で段が 30 秒単位（D-007 ①）。★**初回の配合という脇の処理が、ゲームの本体の時刻を押す**
- 報告の §4 自身が「58 秒で周に迫る」と書いている。★**VPS からの所要を測ってから決める、では足りない** — 登録が集中した日（公開の日）に件数が溜まるのは所要と関係なく起きる
- **決定**: NPC の配合の追いつき（`b0f364d`「予算は時間で切る」）と同じく、**時間で切る**。1 件の確定の前に残り時間を見て、足りなければ次の周へ回す。件数の上限は安全柵として残してよい
- ★**溜まった件数を数えて出す**（待ちの最古の要求の年齢も）。縮まなければ警報を出す（`breedingStalled` と同じ形）

## 3. 🔴 必ず落ちる要求が先頭に居座る

✔ [CODE]: `breed()` より後の例外は取引を戻して**要求を「待ち」のまま**にする（`runPlayerBreeding` の `catch`）。拾う順は `order by created_at, id limit 20`。

- 設計の意図（「結果を見てから失敗にしない」）は正しい。★しかし**毎回同じ理由で落ちる要求**（データの不整合・想定外の値など）は、毎周やり直されて毎周落ちる
- それが 20 件あれば、★**古い順に 20 件しか拾わないので、後ろの要求が永久に処理されない**（先頭の詰まり）。1 件でも、毎周 3 秒と警報を使い続ける
- **決定**: ①要求の行に**試行回数**を持ち、例外で戻した後に**別の取引で**回数を 1 増やす ②**K 回（例: 5）続けて落ちたら `failed`・理由 `internal_error`** にして警報を出す
- ★これは D-120 ①（結果を見てから失敗させない）に**反しない**。取引を戻しているので下書きは残らず、**利用者は仔を一度も見ていない**。見ていない結果で振り直しの得はできない。★ただし `internal_error` で枠が戻る点は、「利用者が狙って `internal_error` を起こせない」ことが前提になる。★要求の中身は父・母の ID だけで、候補の判定は `breed()` の前に済んでいるので、今の形では起こせない。★**入力が増えたら（命名の要求など）この前提を見直す**
- 拾う順も、試行回数の少ない順を先にするなど、詰まった要求が他を塞がない形にする

## 4. ⚠️ NPC の配合が「入れる → 印を取る → 消す」を 1 取引でしていない

✔ [CODE]: `breeding-runner.ts:593-632`。取引を張らずに ①仔を `insert` ②母の印を条件つきで `update` ③0 行なら仔を `delete`。

- ①と②の間でワーカーが落ちると（A-1 で再起動は想定内）、**仔は `horses` に残り、母の印は立っていない**。再起動後は `on conflict (dam_id, birth_week)` で「既に居た」と数えられ、**母の `foal_count` が 1 少ないまま**になる（生涯 8 産の上限が 1 つ緩む）。★これは今回の変更の前から在った窓
- ★今回の変更で増えた窓: ①と③の間で落ちると、**プレイヤーの配合と NPC の配合が同じ母・同じ年に 1 頭ずつ**という状態が残りうる（プレイヤー側の下書き ＋ NPC 側の `horses`）
- **推奨**: 母 1 頭ぶん（①②カウンタ）を 1 取引にする。周の予算を考えると取引は短いので問題にならない。★本番の配備までに入れることを推奨する（確率は低いので阻む条件にはしない）

## 5. ⚠️ `breed()` の中の `canMate` が年の尺度のずれで効いていない

✔ [CODE]:
- `breedingRecordOf` は親の `birthYear` を**ゲームの年**（`gameYearOf(birth_week)`）で作る（`breeding-runner.ts:176`）
- 事前の判定は `canMate(sire, dam, balance, ctx.year)` で、年も**ゲームの年**。★尺度が揃っていて正しい
- ところが `breed()` は内部で `canMate(sire, dam, balance, birthYear)` を呼び（`packages/sim-engine/src/breeding.ts:102`）、渡っているのは `year + yearOffset`（**保存する尺度**。本番の実測で差は 46・`ea8434b`）
- → `breed()` の中の年齢の判定は**親が 46 歳年上に見える**ので、**「若すぎる」を弾かない**。NPC の経路も同じ（`breeding-runner.ts:587`）

- 今は事前の判定が正しいので、実害は無い。★しかし `breed()` の中の判定は**安全網のつもりで効いていない**。★事前の判定を誰かが消したとき、何も止まらない
- **決定**: 今は直さない（NPC の世界の結果に触れる可能性があるので、固定の種で前後を比べる便で扱う）。★**試験を 1 本足す**: 「事前の判定が弾く親の組（若すぎる親）を `breed()` に直接渡すと投げる」。★今は投げないはずなので、**その試験は赤で登録し、既知の赤の簿に載せる**（黙って緑にしない）

## 6. 報告 §3 の発見 1〜5 — ✅

1. `returns table` の列名と表の列の衝突 — ✔ 偽の DB では見えない種類の欠陥で、staging の実演でしか捕まらない。**静的検査の網は、同じ形がもう 1 度出たら作る**（1 回目で部品を作らない・09-20 の開発側の観測「部品はどれも 2 回目に生まれた」）。★ただし**簿に 1 行書いておく**（2 回目に気づくための簿）
2. NPC が母をロックせずに読む → 条件つきの更新 — ✔ 妥当。§4 の取引の件と合わせて閉じる
3. 年の変わり目に印が戻る → 2 つの表で年の仔を数える — ✔ 妥当。★これは私の裁定（`ff7028c` §1 条件 1）が「最後の判定はロック後の `canMate()`（母の `bred_this_year`）が担う」と書いた箇所の穴で、**印に頼るのは不十分だった**。開発側の直し方（印に頼らず数える）が正しい
4. 表が無い DB で NPC の配合を落とさない — ✔ 妥当（本番の順序を崩さない）
5. 確定の本体から取引を外す — ✔ 妥当（09-21 の「rollback は自分で commit する関数を戻さない」）

わざと壊して落ちることを確かめている点（報告 §3 の末尾）は、AU-31「止める側で試す」に適っている。

## 7. 報告 §4 の選択 — ✅

- 誕生週 ＝ いまの週: ✅（確定した時点で生まれる。NPC と違う理由が註記されている）
- 免除の記帳（`ep_ledger` の増減 0 の `stud_fee` 行 ＋ 要求の行の額）: ✅ D-120 ② に適う
- 失敗の語: ✅

## 8. オーナーへ回すもの

- **D-113 ⑦ の本番の現状**（開発期間中は招待制・許可リストか）の実測。本番を読むだけの 1 コマンドを、開発側がオーナーに渡すこと（報告 §5 のとおり）。★**公開の日より前に要る**

## [CODE] と [EYES]

全部 [CODE]。staging の実演は再実行していない（読んだのはコードと報告）。★実演の値は開発側の測定として受け取る。
