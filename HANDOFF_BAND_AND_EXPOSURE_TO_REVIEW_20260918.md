# 引き渡し（開発側 → レビュー側）— 帯の下のゲート ／ anon 露出を閉じた（2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側**（★オーナーが渡します）
**枝**: `p4/race-30sec-cuts` ／ **終了 HEAD** ＝ `11bd965`（★**push していません**。★同じ作業ツリーで読めます）

> ★**この 1 枚から読んでください。** 渡す文書と、裁定をお願いしたいことだけを書いています。
> ⚠️ **正典 `STAR_SPEC_v2.0.md` は開発側で編集していません**（起票はレビュー側の作業）。
> 凡例（R-13）: **✔** ＝ 実測で確かめた ／ **△** ＝ 未確認 ／ **🔴** ＝ 判断待ち

---

## 0. 渡す文書（2 便ぶん）

| # | 文書 | 中身 |
|---|---|---|
| **1** | **`QUESTIONS_INITIAL_BAND_GATE_20260918.md`** | ★**本体**。帯の下のゲート（D-079 ④）の照会 **Q-BAND-01〜03** |
| 2 | `REPORT_INITIAL_BAND_GATE_20260918.md` | 上の測定の実体（★数字と対照） |
| 3 | `REPORT_ANON_EXPOSURE_20260918.md` | 裁定 `REVIEW_ANON_EXPOSURE_VERDICT_20260918.md` の **AE-1〜AE-4** の履行報告 |
| 4 | 本書 | 渡し状 |

---

## 1. 位置

| | |
|---|---|
| 開始 HEAD | `02fda8a`（引継ぎ書: 認証をメール＋パスワードに） |
| ⚠️ 途中でレビュー側が動かした HEAD | `8dae6bc`（anon 露出の裁定）・`8e20139`（TOP 実装の R-14 照合） — ★**開発側の作業ではありません** |
| 終了 HEAD | ★**`11bd965`** |
| 開発側のコミット | `3e8803f` 帯の下のゲートの測定 ／ `11bd965` `0032` で 3 表を閉じた |
| staging | ✅ **`0032` 適用済み**。★**V-20 が 11 件すべて緑**になりました |
| 本番 | ★**触っていません**（`0021` 以降は未適用のまま） |
| push | ★**していません**（オーナー指示待ち・CLAUDE.md） |

---

## 2. 🔴 裁定をお願いしたいこと（5 件）

| # | 何を | 開発側の見解 |
|---|---|---|
| **Q-BAND-01** | ★**「キャリア中に 1 勝」の合格線**（帯の何 % の馬が満たせば合格か）。★正典にも裁定にも書かれていません | ★**決めません**（決めれば「通したい線」を選んだことになります） |
| **Q-BAND-02** | ★**出走機会をどう与えて測るか。** ★ハーネスは NPC 同士の番組表（能力順の窓の抽選）ですが、★正典 §10.4 は**プレイヤーは自分で出走レースを選ぶ**と定めています。★**数字が 23.1% と 76.1%（推定）に分かれます** | ★**(b) 機会を与えて測る**を推しますが、★`generateRace` に「必ず含める馬」の口を足すのは指示の範囲外です |
| **Q-BAND-03** | ★**この基準は帯をほとんど選びません**（下記 §3-2）。★残すか・変えるか | ★**(a) 残す。ただし「帯の下限はこの基準では決まらない」ことを正典に残す**を推します |
| **AE-a** | ★**`revoke select` ではなく `revoke all` にしたこと**（裁定 AE-1 の字面からの逸脱） | ★**R-29「列挙は必ず漏れる」**に従いました。★実効は同じ（付いていたのは `select` だけ）。★差し戻すなら直します |
| **AE-b** | ★**`story_daily` を登録簿に足したこと**（AE-2 の 3 件の外） | ★**露出はしておらず、未登録だっただけ**です。★これが赤のままだと AE-4 の「緑になった」が言えないため同便で足しました。★不適切ならその 1 行だけ戻せます |

---

## 3. 帯の下のゲート（D-079 ④）— ★測りましたが、合否は出していません

### 3-1. ✔ 所要（裁定 §6-3 が最初に求めたもの）

★**4 シード・1,600 頭で 9.2 秒**でした。★**オーナーの別端末は要りませんでした。**
（内訳: プール構築 1.0 秒 ／ 育成 2.2 ms/頭 ／ `runSeason` 1 年 0.1 秒）

⚠️ ★引継ぎ書が心配していた「7 世代以上でないと `runSeason` に届かない」は、★**この測定には当たりません**
（★あれは `simulate --selection race` の話で、★本測定はプールを作ってから自分で `runSeason` を呼びます）。

### 3-2. ✔ 結果（4 シード・1,600 頭・適切な育成＝`balanced`）

```
  ★         頭数     1勝以上    ±SE     平均出走     勝率/走    24戦なら   [対照A] 全頭放置   [対照B] この帯だけ放置
  ★1.0      39    23.1%   6.7pt      4.9    0.058    76.1%          20.5%     10.3%（-12.8pt）
  ★1.5     298    74.2%   2.5pt     22.5    0.080    86.4%          72.1%     55.7%（-18.5pt）
  ★2.0     564    79.1%   1.7pt     25.5    0.075    84.7%          76.8%     63.3%（-15.8pt）
  ★2.5     427    77.5%   2.0pt     26.8    0.078    85.7%          77.8%     66.0%（-11.5pt）
  ★3.0     176    74.4%   3.3pt     25.2    0.076    85.2%          76.1%     44.9%（-29.5pt）
  合計     1600    73.9%   1.1pt     23.8    0.077    85.5%          73.3%              56.8%
```

★**3 つとも対照を取っています。**

| # | 分かったこと | 対照 |
|---|---|---|
| 1 | ★**下の帯が勝てないのは弱いからではなく走っていないから** | ✔ 能力順の窓（本番と同じ `classBand=0.06`）では第 1 十分位 **12.3 走**・窓を外すと（`1.0`）**20.5 走** |
| 2 | 🔴 ★**この基準は「育成の効き」を測っていません** | ✔ **全頭放置でも 73.3%**（適切な育成 73.9%）。★勝ちは相対なので、全員弱くても勝者は出ます（R-16） |
| 3 | ★**★が勝率に出ないのはクラス分けのため** | ✔ 窓ありは **0.063〜0.080 で平ら**（平均頭数 12.9 ＝ **1/12.9**）・窓なしは ★1.0 **0.031** → ★5.0 **0.277** と単調に散る |

★ただし ★**「その帯だけ放置」では 56.8%（−17.1pt）**と落ちます — ★**プレイヤーが直面する形では基準は育成に反応します。**

### 3-3. ★合否を出していない理由（R-3）

★**合格線が無いので、道具は PASS を名乗りません。** ★「判定不能を PASS に倒さない」を、★**PASS を出さない**という形で守っています。

---

## 4. anon 露出（AE-1〜AE-4）— ✅ 履行しました

| | 適用前（staging） | 適用後（staging） |
|---|---|---|
| 3 表の grant | ★**anon・authenticated の SELECT が 6 行** | ★**0 行**（出力から名前が消えました） |
| V-20 ② 公開ビュー以外は anon から 0 行 | 🔴 3 表とも **1 行ずつ読めた** | ✅ |
| V-20 ③ 登録簿に無い表が無い | 🔴 **`story_daily` が未登録** | ✅ |
| 公開ビュー 4 つが**引き続き**読める | ✅ | ✅（★**塞ぎすぎていないことを別に測定**・AE-4 ②） |
| 総合 | 🔴 **不合格 2 件 / 11** | ✅ **合格 11 / 11** |

★**検査が噛むことを変異で確かめました**: `0032` の revoke を 1 行つぶすと `entry-rpc-migration.test.ts` が**赤**（終了コード 1）→ 控えから戻すと**緑**・✔ `diff` で差分なし。

★**反転した 3 本には「なぜ反転したか」を全部書きました**（`0018:52` の宣言・V-20 ②・`0032`）。
★裁定 §3 の「**次の便がまた前の便を手本にする**」を止めるためです。

---

## 5. ✅ 検査（★両便を入れた後の状態）

| # | |
|---|---|
| 1 | 検査一式 ✔ **2,079 本中 2,078 本が緑**（★vitest の終了コード **1**） |
| 2 | 🔴 落ちた 1 本 ＝ `packages/render/test/edit-grammar-audit.test.ts` ⑨ ＝ ★**既知の赤**。✔ ★**この便は `packages/render` を触っていません**（`git log 02fda8a..HEAD -- packages/render` が空・作業ツリーにも変更なし） |
| 3 | 型検査 ✔ `npm run typecheck` **終了コード 0**（★パイプで切らず道具自身から取得） |
| 4 | ★**V-14 が切り出しで 1 ビットも動かないこと** ✔ `diff` で**完全一致**（★切り出す**前に**対照を取りました） |

---

## 6. ★レビュー側が独立に再実行するための手順

```bash
# 帯の下のゲート（9.2 秒・DB 不要）
npm run verify:band                      # 正典 §13.3 の 4 シード
npm run verify:band -- --seeds 42        # 1 シードだけ（2.5 秒）

# V-14 が切り出しで動いていないこと
npm run verify:v14 -- --horses 400 --seed 42

# V-20（★読み取りのみ・--env は必須）
npx tsx tools/verify-anon-exposure.mjs --env staging

# 反転した検査 3 本 ＋ 登録簿
npx vitest run apps/cli/test/entry-rpc-migration.test.ts \
  apps/cli/test/horse-market-migration.test.ts \
  apps/cli/test/stable-grade-unlock-migration.test.ts \
  apps/cli/test/exposure-registry.test.ts
```

⚠️ ★**§3-2 の対照 2 本（出走機会の十分位・窓あり/なしの勝率）は、使い捨ての診断として `tmp/`（gitignore）に置いています。**
★リポジトリには入っていません。★**必要なら `tools/` に移してコミットします**（★お申し付けください）。
★測り方は報告書 §5 に書いてあります（★`classBand` を `0.06` と `1.0` で振り、同じ場・同じシードで比べただけです）。

---

## 7. 🔴 オーナー手配事項（★この便で増えたもの）

| # | | いつまで |
|---|---|---|
| **1** | 🔴 ★**本番への適用と配備**: ★本番は `0020` までで、★**`0024` を当てた瞬間に露出が現実になります**。★裁定 §5 の推奨どおり **`0024`〜`0032` を 1 回の配備にまとめて**ください。⚠️ ★**適用は開発側では行いません** | 本番で認証を使う前 |
| **2** | ★前便からの持ち越し: **V-10**（`npm run verify:pmin -- --races 40`・別端末）／**SMTP**／**E-8・E-5・E-4 の設定** | 人を迎える前 |

---

## 8. ★私が踏みかけた罠（記録します）

| # | |
|---|---|
| 1 | ★**`${PIPESTATUS[1]}` を読んだ** — ★`[1]` は `tail` です（★`[0]` が本体）。★前便の「`\| tail; echo $?`」と**同じ家族**で、★**添字を 1 つずらしただけ**。→ ★パイプを外して取り直しました |
| 2 | ★**背景タスクの枠が「exit code 0」と表示**しましたが、★**中の vitest は 1** でした。→ ★枠を合否に使わず、出力ファイルを読みました |
| 3 | ★**「勝率が平らなのはクラス分けのせい」と対照なしで書きかけました。** → ★窓を外した対照を取ってから書きました |
| 4 | ★**「下の帯は弱いから勝てない」と読みかけました。** → ★十分位ごとの出走数を測って、★**機会の差**だと分かりました |

---

## 9. 開発側が次にすること

★**Q-BAND-01〜03 と AE-a / AE-b の裁定を待ちます。**

★待つ間に進められるもの（★指示があれば）:

| | |
|---|---|
| 1 | ★**引継ぎ書の更新**（★次のセッションが読む 1 枚） |
| 2 | ★**乱数の用途 ID の登録漏れ 2 表**（`TRAIN_STREAM` 61〜64 ／ ワーカーの `STREAM` 61・62）。★**61・62 が二重**で、△ 衝突はしていません（マスターシードが別）。★**直し方の判断が要ります**（照会 §5） |
| 3 | ★`loadRaceablePool` の註釈と実装の食い違い（★**クラス分けの便へ**と裁定済み・本便では触りません） |
