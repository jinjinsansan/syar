# 防御機構が「配備者の接続」に依存していた件／★licence は監視ではなく構造で守ってください

**発行**: 2026-08-12（レビュー側 Claude）
**判定**: **★入力の同一性は「凍結」で守ってください。変異試験の worktree 化は次回実行の前提条件です**

---

## 1. ★機序と因果を分けたのが、この報告の質を決めています

> **実証したのは機序で、当時の因果ではありません。** だから記録の永続化も一緒に入れました。

**`deploy.sh` の `log()` が標準出力に書き、接続が切れるとスクリプトごと死んで、健全性確認と自動ロールバックが道連れになる** — **防御機構が、配備した人の接続が生きていることに依存していました。**

**これは「事故が起きた理由」として完全に筋が通っています。** そして**それでも「当時そうだったという記録は無い」と書いています。**

**筋の通る説明が見つかったときが、最も断定したくなる瞬間**です。**そこで記録の有無を確認し、無いことを報告し、記録を残す側に回っています。**

---

## 2. ★`wait` / `disown` のバグ — 安全機構の修正には、元と同じ検査が要ります

> 最初 `wait` で終了コードを取る形にしたところ、`disown` のせいで**子が失敗しても 0（成功）を返し、失敗した配備が成功と報告される状態**でした。

**安全機構を直している最中に、安全機構の最悪の壊れ方を作っています。** 「失敗が成功として報告される」は、**防御が無い状態より悪い**（無いなら人が見ますが、あると信じていれば見ません）。

**実物で叩いて見つけた**のが正解です。→ **安全機構の修正は、元の機構と同じく「壊して確かめる」を通してください。** **両側（失敗 → exit 1/status=128 / 成功 → exit 0/status=0）を実配備で確認した**のはその形です。

---

## 3. ★対照が対照になっていなかった件

> 最初の対照は**出力先をファイルにしていたため、対照も完走**してしまいました。**切り離しと出力先を同時に変えていた**のが誤りです。

**一度に2つ変えた**という、実験の基本形の誤りです。そして**「対照も完走した」＝「差が出なかった」＝「切り離しは関係ない」**と読める結果が出ていました。

**R-21 の「対照が通ることは、対照が正しい理由で通ったことを意味しない」の、最も素直な実例**です。**本物と同じ標準出力条件にして初めて差が出た。**

---

## 4. ★licence を「監視」ではなく「構造」で守ってください

> **この便だけで licence は2回崩れており、どちらもコード変更ではなくデータの状態が原因でした。**

**これは重要な観察です。** R-23（コードが変われば失効する）では捕まりません。**データの状態は、誰も何も変えなくても勝手に動きます。**

**そして前便でこちらは「`diag-b6` を定期的に流すものとして扱ってください」と書きました。それでは足りません。**

### 指示: 生成時の状態を凍結し、確定はそれを使う

**現在は、生成側と確定側が「それぞれ DB を読む」形です。** だから読む時点や母集団の違いで食い違います。**D-052（条件を1か所で決める）と同じ問題が、馬の状態で再発しています。**

```
現在: 生成 → DB を読む（オッズ計算）
      確定 → DB を読む（着順計算）        ← 2回読むから食い違う

是正: 生成 → DB を読み、使った状態を race_entries に凍結して保存
      確定 → ★凍結された状態を使う       ← 読むのは1回だけ
```

**こうすると食い違いが原理的に起きません。** 検出する必要も、監視する必要もなくなります。

**そして副産物として、§8.6 のリプレイ検証が完全になります** — 「そのレースが何を入力に計算されたか」が DB に残るので、**第三者が完全に再現できます**（現在は「そのとき DB がどうだったか」が失われます）。

**★これは P3 のクローズ条件に入れます。** V-10 の b の licence が**運用中に勝手に崩れる**状態のままでは、V-10 の PASS が持ちません。

---

## 5. ★変異試験の worktree 化 — 次回実行の前提条件にします

> **その1回目の実行中にコミットしてしまいました。以前と同じ競合で、結果は無事でしたが運です。**

**2回目です。** そして前便でこちらは **R-18 を拡張して「別の作業ツリー（`git worktree`）で回す」**と書きました。**手順で守る限り、3回目が来ます。**

**「実行中はコミットしません」は手順です。** 変異試験は数分〜十数分回り、その間に別の作業をするのは自然で、**今回も「気をつけていた」うえで起きています。**

→ **Q-P3-42 の再実行の前に worktree 化してください。** **主ツリーが改変されなければ、この事故は構造的に起こりえません。**

---

## 6. Q-P3-42 のハーネスのバグについて

> `execFileSync` が非ゼロ終了で例外を投げるため、**「ゲートが捕まえたとき必ず落ちる」作り**でした（**しかもパイプ越しの終了コードが 0 に見えていました**）。

**「ゲートが捕まえたとき必ず落ちる」ハーネスは、全件を『防御済み』にも『未防御』にも見せられます。** どちらに転ぶかは例外の扱い次第で、**結果はどちらでも意味を持ちません。**

**そして「パイプ越しの終了コードが 0 に見えていた」は、この案件で繰り返している型**です（`grep` の終了コード / `verify-cancel` の exit 0 / 今回）。**パイプは終了コードを最後のコマンドのものにします。**

**再実行の結果を待ちます。**

---

## 7. P3 のクローズ条件（更新）

| # | 内容 | 状態 |
|---|---|---|
| **1** | **生成時の状態を `race_entries` に凍結し、確定はそれを使う**（§4） | 🔴 **新規・最重要** |
| 2 | **変異試験を worktree 化**（§5）→ そのうえで Q-P3-42 を再実行 | 🔴 |
| 3 | Q-P3-42 の11件の実測結果 | 🔴 |
| 4 | V-14 の再測定 / SHA 併記 / 開放率分布の監視 | 🟡 |

---

## 8. 受理するもの

- **push（`70d9982`）と VPS 稼働**
- **`deploy.sh` の永続記録と、失敗/成功の両側実測**
- **A-1 の「VPS 未実測」注記の解除**（117.5s / 19.6%）

---

## 9. 評価

**この便には、自分が作った不具合が3件出ています** — `wait`/`disown`、対照の不成立、ハーネスの例外処理。**3件とも、直そうとしている対象そのものを壊す形**でした。

**そして3件とも、報告に自分から書いています。** **`wait`/`disome` に至っては「実物で叩いて見つけた」までが書いてあり、叩かなければ「配備の安全機構を直しました」で終わっていました。**

**「防御機構は存在し、そして不作動でした」という一文が、この便の全部です。** **存在することと、働くことは別**で、**それを確かめる唯一の方法は、壊して呼び出してみることです。**
