# 引継ぎ書 — 次のセッションはここから（2026-09-01）

作成: 2026-09-01 / ブランチ `p4/race-30sec-cuts`
起点: `82f1e97`（前の引継ぎ書）→ ★**12 コミット**（★この引継ぎ書のコミットを含めると 13）
⚠️ ★**未 push は 12 本**、★タグは **7 本**（★すべて origin にありません）
前の引継ぎ書: `HANDOVER_P4_COURSE_MODEL_20260831.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**

---

## ★0. ★**次の一手は「モバイル表示が正常になっているか？の点検」です**（★オーナー指示）

> ★**次回はモバイル表示が正常になっているか？の点検からです**（2026-09-01）

### ★0-1. ★立ち上げ（⚠️ ★**dev サーバーは一度落としてから**）

⚠️ ★**2026-09-01、ポート 3210 の dev サーバーが壊れました。**
　★`/` が無応答、★`/races/<id>/bet` と `/odds` が **HTTP 500**（`Jest worker encountered 2 child process exceptions`）。
　★**製品の変更が原因ではありません** — ★別ポートで立て直すと全ページ 200 でした。
　→ ★**まず落として立て直してください。**

```powershell
cd V:\dev\Cusor\star\apps\web ; npm run dev      # ★ポート 3210（★別の窓）
```

### ★0-2. ★**実機（スマホ）で見るのがいちばん確実です**

★`npm run dev` の**起動ログに `Network:` の行**が出ます（★実測: `http://100.110.117.112:3215` の形）。
★その URL を同じ Wi-Fi のスマホで開けます。★出ていなければ IP を調べてください:

```powershell
ipconfig | findstr IPv4        # ★PC の IP を調べる
# → スマホで  http://<その IP>:3210/  を開く
```

⚠️ ★開かない場合は Windows のファイアウォールです。★PC の Chrome で ★**F12 → デバイスツールバー**（Ctrl+Shift+M）
　→ ★**iPhone 14（390px）** か ★**Galaxy S20（360px）** を選んでも同じことが見られます。

### ★0-3. ★**見ていただきたいところ（★順番どおりに）**

| # | ★画面 | ★何を見るか |
|---|---|---|
| ★1 | ★`/`（TOP） | ★はみ出していないか。★ヘッダーのナビが折り返して見えるか |
| ★2 | ★`/stable`（わたしの馬） | ★**上の 4 枚のカードが縦に並ぶ**か（★以前は「参 加 ポ イ ン ト」と 1 文字ずつ縦に割れていました）<br>★馬の一覧が 2 行に折り返して読めるか |
| ★3 | ★`/races`（番組表） | ★一覧の行が 2 行に折り返しているか。★**列見出しの帯が消えている**か（★意図どおり） |
| ★4 | ★**`/races/<id>/bet`（投票）** | ★**いちばん直したところ**。★式別タブが 2 段、★マークの丸が大きい、★**1着/2着/3着 の 3 列が見出しと揃っている**か |
| ★5 | ★`/entry`（出走登録） | ★**行が縦に重なって文字が二重に見えない**か（★以前はそうでした） |
| ★6 | ★`/setup`（はじめの設定） | ★**新規の方が最初に見る画面**。★以前はページ全体が 84% に縮んでいました |
| ★7 | ★**`/race`（レース映像）** | ⚠️ ★**縦向きでは映像が 26% に縮みます**（★§0-4）。★**横向きにしてください** |

### ★0-4. ⚠️⚠️ ★**先にお伝えします — レース映像は「横向き」で見る前提です**

★映像は ★**横 1280px の画面**用に作られていて、★HUD の文字も ★**10〜15px の絶対値**です。

| | ★実測 |
|---|---|
| ★スマホを**縦**に持つ | ★1280 → 338px ＝ ★**26%**（★文字は 3px 相当・★読めません）|
| ★スマホを**横**に持つ | ★1280 → 816px ＝ ★**64%** |

★**64% は、オーナーが PC で判定してきた 67%（台帳 B-3）とほぼ同じ**大きさです。
→ ★**横向きなら、演出を 1 つも変えずに今の品質で見られます。**

⚠️ ★縦向きでも読めるようにするには ★**HUD の版面の作り直し**が要ります。★これは**演出の判断**なので
　★開発側では決めません（★正典・`RACE_PRESENTATION_BASICS.md`）。★ご指示があれば別の便で。

### ★0-5. ★数字で点検する（★目と併せて）

```powershell
cd V:\dev\Cusor\star
npx tsx tools/verify-mobile-layout.mjs --widths 360,390 --rid <レースid> --hid h1
#   ★レースid は /races の href から拾う（★直書きしない）
```

★**期待値**（★2026-09-01・本番ビルドでの実測）:

| | |
|---|---|
| ★はみ出し | ★**1 / 46**（★残るのは `/race` の 360px で **10px**＝★97% 表示・目には出ません）|
| ★縦の重なり | ★**0** |
| ★44px 未満の操作要素 | ★**27 / 350** |

⚠️ ★`--path` を Git Bash から渡すときは ★`MSYS_NO_PATHCONV=1` を付けてください。
　★付けないと `/race` が Windows のパスに変換され、★**1 ページも調べずに「0 / 0」と出ます**（★実際に出しました）。

---

## ★1. この便で何が起きたか（★2 つ）

★**前半** … ★タグを打ち、★A-9（走路が曲げられている件）を測り直して**言い直し**、★台帳 B-2 / B-5 を片付けました。
★**後半** … ★オーナー指示 ★**「このアプリはモバイルアプリです」**を受けて、★モバイル表示を調べて直しました。

---

## ★2. ★**最重要の申し送り 3 件**

### ★2-1. ⚠️⚠️ ★**この便、道具と仮説を 7 回外しました。★全部「数字だけ見ていたら通っていた」ものです**

| # | ★出した嘘 | ★どう気づいたか |
|---|---|---|
| ★① | 「19 ページ全部あふれ 0」 | ★メディアクエリが 139 行中 1 本なのに全部健全は通らない → ★`overflow-x:hidden` に隠されていた |
| ★② | 「あふれ 0」（2 度目） | ★撮った絵が **465px 幅**なのに測定は 390px → ★**内容が収まらずブラウザが全体を縮めていた** |
| ★③ | 「`/race` は 669px」（3 回再現） | ★別の道具では 390px → ★`overflow-x` を**解いたあと**に幅を読んでいた |
| ★④ | 「横は直った」 | ★`/entry` の絵で ★**行が重なって文字が二重** → ★縦の溢れを測っていなかった |
| ★⑤ | 「表の中身がはみ出している」 | ★**自分で横スクロールにした表**を、自分で「はみ出し」と数えていた |
| ★⑥ | 「dev は CSS を後から挿すからズームする」 | ★**本番ビルドでも 672px**。★仮説は外れ |
| ★⑦ | 「走路が閉じていないのが破綻の印」 | ★曲げが**最小**の鞍も同じく閉じていない → ★**距離が 1 周未満なだけ**で無関係だった |

→ ★**絵と数字を突き合わせること。** ★どれも片方だけでは見つかりませんでした。

### ★2-2. ⚠️ ★**テンプレートリテラルの中のバックスラッシュ（★本日 3 度**）

```ts
new RegExp(`minWidth:\s*${px}\b`)     // ★これは壊れています（\s は「s」になる）
new RegExp(`minWidth:\\s*${px}\\b`)   // ★正しい
```

★同じ形で ★**バッククォートの入れ子**も 2 回やりました（★`tools/*.mjs` の中の文字列に `` ` `` を書いた）。
→ ★**道具の中の日本語コメントにバッククォートを書かないこと。**

### ★2-3. ⚠️ ★**緩い検査は嘘をつきます**

★モバイル CSS の見張りを書いたとき、★**数字だけで照合**したので ★**変異試験で落ちませんでした**。
★`flex: '0 0 250px'` を 260px に変えても、★同じ行の `width: 250,` に引っかかって「生きている」と出ました。
→ ★**宣言の形**（`0 0 250px`）で照合する形に直し、★変異で落ちることを確認しました。

---

## ★3. 直したもの・作ったもの

### ★3-1. ★モバイル（★後半）

| | ★着手前 | ★いま |
|---|---|---|
| ★横の崩れ | ★**20 / 46**（★10 ページ × 2 幅）| ★**1 / 46** |
| ★44px 未満の操作 | ★**全ページで発生**（★投票 54/56）| ★**27 / 350**（★投票 **0**）|
| ★縦の重なり | ★14 ページ | ★**0** |

★直しは ★**`globals.css` の @media 2 ブロック＋7 ファイルの機械的な置換**です:

★`main` に左右余白 ／ ★`.a-row` の折り返しと固定列の解除 ／ ★列見出しの帯 7 か所を非表示 ／
★**`.a-cards`（新）でカードの列数を CSS 側に** ／ ★150px 以上の `flex: 0 0` と 200px 以上の `min-width` を解除 ／
★ヘッダー `nav` の折り返し ／ ★表の内部スクロール ／ ★行の高さを内容に合わせる ／
★**指で押せる大きさ（44px）を `pointer: coarse` で** ／ ★マークシートを 1 行に（★`56+130+70x3` → `44+84+70x3`）／
★**キャンバスの固有幅 1280px が版面を押し広げるのを止める**

### ★3-2. ★前半（★走路と道具）

| | |
|---|---|
| ★タグ | ★`p4-course-model-stage1-20260831` → `82f1e97`（★注釈つき・★直っていないものも本文に明記）|
| ★**A-9 の言い直し** | ★「コーナーが消える」は**別物が 2 つ**混ざっていました。★**① 走路は無傷・8 鞍**（★直してはいけない）／★**② 走路が曲げられている・42 鞍**（★これだけ直す）|
| ★台帳 **B-5** | ★`spearman` の写し 3 か所を畳み、★**検査で構造的に塞ぎました**（★変異で確認）|
| ★台帳 **B-2** | ★秒の安全性を「覚える」から「測る」へ（★`tools/verify-shot-stability.mjs`）。★**B-2 は閉じていません** |

---

## ★4. 次のセッションが踏みやすい穴

### ★4-1. ⚠️ ★**モバイル CSS は「inline の値」に紐付いています**

★`globals.css` は ★**20 か所**で `page.tsx` の inline の値を名指ししています:

```css
main [style*="flex:0 0 250px"] { flex: 1 1 100% !important; }
```

★`page.tsx` 側の値を変えたら、★**`globals.css` の @media の中も直してください。**
→ ★忘れても気づけるよう ★`apps/web/test/mobile-css-anchors.test.ts`（3 件）を置いてあります。

### ★4-2. ⚠️ ★**dev サーバーは長時間の測定で壊れます**（★§0-1）

★測定を何十回も回すと `Jest worker` が落ちます。★**製品の変更を疑う前に、立て直して確かめてください。**
★私は 3211〜3215 で立て直して切り分けました（★**いずれも停止済み**・★3210 は触っていません）。

### ★4-3. ⚠️ ★**モバイルの測定は「幅を決めてから開く」こと**

★開いてから縮めると、★**ブラウザの「収まらないから縮める」判断が最初の組版で決まったまま**になります。
★`verify-mobile-layout.mjs` は**先に幅**を決める形にしてあります。

### ★4-4. ⚠️ ★**`npm run typecheck` をパイプに繋がないこと**（★前便 §2-2・据え置き）

```powershell
npm run typecheck ; echo "終了状態: $?"
```

### ★4-5. ⚠️ ★**push は開発側の環境からできません**（★据え置き）

★`credential.helper = manager` が GUI を待って返りません。
→ ★**オーナーに実行していただくこと**:

```powershell
git push origin p4/race-30sec-cuts --follow-tags
```

### ★4-6. ⚠️ ★`apps/web/package.json`（ポート 3210）は**触らないこと**（★オーナーの既存作業・★12 コミットとも混ぜていません）

---

## ★5. この便で増えた検査（★+5 件）

| 検査 | 何を守っているか |
|---|---|
| `mobile-css-anchors.test.ts`（★**新** 3 件）| ★モバイル CSS が名指しした inline の値が**まだ在る**（★消えたら「黙って効かなくなる」）|
| `tool-guard.test.ts`（12 → **14** 件）| ★`spearman` の定義は `lib/v18.mjs` **1 か所だけ**（★D-052）|

★**どちらも変異で「壊すと落ちる」ことを確認済み**です。

---

## ★6. ★オーナー判断待ち／レビュー側へ

| # | | |
|---|---|---|
| ★① | ★**モバイル表示の点検** | ★**次の一手**（§0）|
| ★② | ★**A-9 の [EYES]** | ★`out/_shape/runup_shots.png`（★67%）。★「発走から輪の外へ伸びる棒」が実画面で在り得ないと見えるか（★裁定 §5-3）|
| ★③ | ★**「背景が交差している」鞍名** | ★2026-08-31 のオーナー評。★**鞍名を 1 つ**いただければ層ごとに切り分けます |
| ★④ | ★**レース映像の縦向き** | ★HUD の版面の作り直し＝**演出の判断**（★§0-4）|
| ★⑤ | ★**本番 URL** | ★**push と URL の共有**が要ります。★`npm run build:web` は通ることを確認済み |
| ★⑥ | ★A-10 の合否 ／ 上限 12 の見直し（D-090）／ 引き込み線 ／ 段階②（勾配）| ★前便から持ち越し |
| ★⑦ | ★レース名 50 個の確認 ／ ダート素材の承認 ／ A-5（刈り目）| ★持ち越し |
| ★⑧ | ★タグ | ★ローカルに **7 本**。★**すべて origin にありません** |

---

## ★7. 発行物（この便）

| 種別 | ファイル |
|---|---|
| 報告 | `REPORT_P4_RUNUP_SHAPE_20260831.md`（★A-9 の言い直し）<br>`REPORT_P4_MOBILE_SURVEY_20260901.md`（★モバイル・★調査 → 着手 → 完成まで）|
| 台帳 | `RESIDUALS_P4_RACE_VIDEO.md`（★A-8 にオーナー評／★A-9 を言い直し／★B-2・B-5 を更新）|
| 道具 | `tools/verify-mobile-layout.mjs`（★新・追跡）<br>`tools/verify-shot-stability.mjs`（★新・追跡）<br>`tools/_runupmap.mjs` / `tools/_runupshots.mjs`（★追跡外）|
| 検査 | `apps/web/test/mobile-css-anchors.test.ts`（★新 3 件）／ `tool-guard.test.ts`（+2 件）|
| 図 | `out/_shape/runup.png` / `runup_shots.png`（★A-9）<br>`out/_mobile/*.png`（★**47 枚**・全ページ 360/390px）／ `out/mobile-survey.txt` |
| タグ | `p4-course-model-stage1-20260831` |

---

## ★8. ★再実行（★期待値つき・★すべて読取専用）

```powershell
cd V:\dev\Cusor\star

npm test                                # ★期待: ★135 ファイル / ★1374 件 PASS
npm run typecheck ; echo "終了状態: $?"   # ★期待: ★0（⚠️ パイプに繋がない）

npx vitest run apps/web/test/mobile-css-anchors.test.ts   # ★3 件
npx vitest run apps/cli/test/tool-guard.test.ts           # ★14 件

# ★モバイル（★dev サーバーを立ててから・★別の窓）
cd apps\web ; npm run dev
npx tsx tools/verify-mobile-layout.mjs --widths 360,390 --rid <レースid> --hid h1
#   ★期待: ★はみ出し 1 / 46 ／ ★44px 未満 27 / 350 ／ ★縦の重なり 0

# ★走路（★前半の分）
npx tsx tools/_runupmap.mjs --all       # ★① 8 鞍 / ★② 42 鞍 / ★最大 250m が 18 鞍
npx tsx tools/_runupmap.mjs             # ★天河 5 鞍 / ★流星大賞典 140.2m / 天穹賞 72.3m
npx tsx tools/_cutgeom.mjs              # ★早すぎ 32 / 遅すぎ 16 / 破綻 48 / 合っている 2

git show p4-course-model-stage1-20260831 --stat --no-patch   # ★タグの本文
```

⚠️ ★`tools/_*.mjs` は ★**追跡外**です（`.gitignore:68`）。★新しいクローンには存在しません。
⚠️ ★`out/` も追跡外です。★図を見るには撮り直してください。
