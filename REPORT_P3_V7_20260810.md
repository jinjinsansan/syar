# REPORT_P3_V7 — V-7a / V-7b ともに PASS／★測定で2度つまずきました

**開発側 → レビュー側** / 2026-08-10
**コミット**: `4045da2` 以降（push 済み・558 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**なし**（D-049 はそちらの決定。較正結果と、測定条件の登録を返します）。

---

## 1. ★分割したら、較正なしでも帯に入りました

```
1,800頭 / 78→260週 / INJURY_BASE_PROB = 0.0018（正典の値）

方針                V-7a 恒久ダメージ   V-7b 致命的
放置(参考)              20.6%            0.6%
★バランス型             39.1% PASS       1.2% PASS
追い切り偏重(参考)        50.9%            1.8%
```

**旧ゲートの FAIL（75.8%）の実体は、軽度（3週で治る挫石・60%）を足していたこと**でした。
分割した時点で帯に入ります。

---

## 2. ★ただし余裕がほぼゼロだったので較正しました

```
0.0018 → 39.1%   上端40%まで ★0.8 SE
0.0015 → 34.0%   上端まで 5.4 SE
★0.0013 → 29.9%   下端 9.2 SE / 上端 9.4 SE   ← 帯の中央・最小余裕が最大
0.0011 → 26.6%   下端まで 6.3 SE
```

**`INJURY_BASE_PROB = 0.0013` を選びました。**「中央30%を狙う」というご指示どおりです。

### 較正後（確定値）

```
方針                V-7a          V-7b
放置(参考)            14.9%         0.6%
★バランス型           29.9% PASS    0.7% PASS
追い切り偏重(参考)      41.3%         1.2%
```

★放置・追い切り偏重が帯外ですが、**参考値なので問題ありません**（裁定どおり）。

### V-14 の回帰も確認しました

```
① 90.8% PASS（余裕 9.9 SE）  ② 15.4pt PASS（余裕 8.8 SE）
③ EP効率 0.68倍（変化なし）/ 時間軸 97.2% は FAIL のまま
```

**故障を減らしても①②の余裕はむしろ増えました。**

---

## 3. ★測定で2度つまずきました（どちらも自分で気づきました）

### ① 1,800頭を要求して、黙って400頭で測っていました

```
--horses 1800 → 実際は 400頭  SE 2.41pt のまま変わらず
```

母集団は**1シードあたり400頭**（`POOL_MARES`）しかなく、`slice(0, HORSES)` が
**黙って切り詰めていました**。★**「頭数を増やしたのに数字が1つも動かない」**のを見て気づきました。

→ **シードを積んで必要数まで集める**形に直し、**要求より少なければ例外**にしました（R-21）。

★**これは実際に判定を変えました。**

```
400頭   V-7a 36.5%   ← シード42の1集団に固有の値
1,800頭 V-7a 39.1%   ← 上端まで 0.8 SE しかない
```

**400頭のまま「PASS・余裕3.5pt」と報告するところでした。**

### ② 「丈夫さの固定」の差を測ったら 0.00pt でした

ご指示の「キャリア開始時の値で固定」を実装し、現在値を使う場合と比べました。

```
式に現在値を使う（重度の −100 が効く）  : V-7a 36.5%
式にキャリア開始時の値を固定して使う      : V-7a 36.5%
→ 差 0.00pt
```

★理由: **重度故障は 0.11件/頭しか起きず、起きた後の残り週数も短い**ので、
  `durability −100` が将来の確率に効く機会がほぼありません。

★**つまり §7.5 の「重度: durability −100」は、故障確率に対しては実質的に効いていません**
  （`potential −8%` のほうは効いています）。**判断は不要**ですが、記録に残します。

---

## 4. 測定条件を `measurement.ts` に登録しました

```ts
V7_MEASUREMENT = {
  fromWeeks: 78, toWeeks: 260,        // ★故障は調教で起きるので78週から
  policy: 'balanced',                  // ★V-14 の錨と揃える（5つ目の条件）
  rate: 'share_of_careers',            // ★件数だと130%相当になり帯と合わない
  durabilityBasis: 'career_start',     // ★差は実測0.00pt
  horses: 1800,                        // ★R-20 の逆算（下記）
}
```

### ★必要頭数の逆算（R-20）

```
V-7a: 39.1% / 上端40%まで 0.9pt（0.8 SE）→ 3 SE 確保に ★1,708頭
V-7b:  1.2% / 上端 3%まで 1.8pt（6.9 SE）→ 3 SE 確保に    993頭
```

★**V-7b のほうが多くなる可能性**をご指摘いただきましたが、**実測では V-7a のほうが厳しい**でした。
  1,800頭に確定しています。

---

## 5. 現在の合格基準

| # | 状態 |
|---|---|
| **B-2（V-7）** | **★PASS**（V-7a 29.9% / V-7b 0.7%・1,800頭） |
| B-4 / B-5 / B-6 / B-9 | **PASS** |
| B-1 / B-3 | staging 待ち |
| B-7 | ★**次に着手**（G-8） |
| V-14 | ①② PASS / ③は EP効率 PASS・時間軸 FAIL（`menuIntensity` は触っていません） |

**G-8（引退と繁殖入り）に進みます。**
