# 設計案 — 世界の供給と、生まれた週の配り方

> `POOL-DRAIN` ／ `SEED-LOCKSTEP` ／ `PROD-NEVER-AGED` の 3 つは同じ 1 つの穴です。
> 2026-09-20・開発側。**案です。実装していません。裁定を待ちます。**

## 0. この文書の立場

- **実装していません。** 配備していません。本番にも staging にも書いていません。VPS に入っていません。
- 測ったのは **プリシードをその場で回した結果だけ**（`runPreseed` は純関数・DB に繋ぎません）。
- 🔴 **`birth_week` の実装は裁定の後**（レビュー側の指示どおり）。

---

## 1. 測ったこと（★DB に繋がず・2 回 回した）

`runPreseed({ seed: 20260833, generations: 50, ...DEFAULT_PRESEED_OPTIONS })`

```
生成 40,755 頭 / 92.4 秒 / 最終年 50

現役    2,400 頭   birth_year の種類 ★3（46 / 47 / 48 = 4 歳 / 3 歳 / 2 歳・★各 800 頭ちょうど）
繁殖牝馬  800 頭   birth_year の種類 8
種牡馬    200 頭   birth_year の種類 29

seed-world が実際に投入する集合:
  現役 2,400 + 種牡馬 200 + 繁殖牝馬 800（重複を除いて 3,400）
  ＋ 5 代の祖先            → ★投入 7,370 頭
```

✔ **staging の実測 7,333〜7,370 と一致します。** この算術が、いまの DB の姿をそのまま説明します。

---

## 2. 🔴 新しく分かったこと — `seed-world.mjs` は「誰が現役か」も捨てている

`tools/seed-world.mjs:74` の `insert into horses` の列に、**`birth_week` だけでなく
`retired_at_week` も `retirement_role` もありません。**

```
投入 7,370 頭 のうち、★最終年に 5 歳以上 = 4,970 頭
  （＝ プリシード世界では現役を終えている馬。種牡馬 200・繁殖牝馬 800・純粋な祖先 3,970）
→ ★DB では 4,970 頭 すべてが「現役」（retired_at_week が null）
```

→ 🔴 **配備が読む現役プール 7,333 頭の うち 67% は、プリシード世界で走り終えた馬**です。
　　**種牡馬と繁殖牝馬も、いま出走表に載りえます。**

### これは `PO-8`（母数の帳簿が合わない）の答えです

| | 頭数 | 正体 |
|---|---|---|
| 正典 §10.5 | **2,500** | 設計値 |
| ★実際の現役 | **2,400** | ★**§10.5 とほぼ一致していた** |
| 実測（`ACTIVE_WHERE`） | 7,333 | 現役 ＋ 繁殖 ＋ 祖先 |
| 実測（`RACEABLE_WHERE`） | 4,389 | 上から「世代が古い馬」を落としたもの |

→ **正典の 2,500 は間違っていませんでした。** 帳簿が合わなかったのは
　**引退を書かずに投入していたから**です。

### ⚠️ そして `PO-4 ①` の見え方が変わります

`RACEABLE_WHERE` の `generation >= max(generation) - 2` は、
**年齢の仕事を偶然していました**（祖先は世代が古いので落ちる）。
2026-09-19 に既定を `ACTIVE_WHERE` に差し替えたとき、**その偶然の蓋が外れています**。

⚠️ **ただし蓋は完全ではありませんでした**（4,389 ＞ 2,400）。
**「PO-4 ① が原因だ」とは言いません。** 原因は投入の側です。

🔴 **私はこれを直してよいと言っているのではありません。** 測って見つけた、と報告しています。

---

## 3. 案 — 決めを 3 つに分けます

### 決め A: 初期投入は、プリシード世界を**そのまま写す**

いま捨てている 3 つを書きます。**発明ではなく、既にある値の転記です。**

| 列 | 出どころ |
|---|---|
| `birth_week` | `birthYear` ＋ 決め B の週内位置 |
| `retired_at_week` / `retirement_role` / `retirement_reason` | プリシード世界の `activeIds` / `stallionIds` / `mareIds` に居るか |
| `last_processed_week` | 決め C |

⚠️ 移行 `0010` の表明に従います（3 つとも null か、3 つとも非 null）。

### 決め B: 生まれた週は「**毎週 生まれる**」形で配る 🔴 ここが本題

| 案 | 配り方 | 現役プール中の `birth_week` の種類 | `ageWeeks % 4` の位相 |
|---|---|---|---|
| B-1 | 年 1 回・コホートごとに同じ週 | **3** | 3/4 — ★800 頭ずつ足並みが揃う |
| B-2 | 産駒期（例 10 週）に散らす | 30 | 4/4（★ただし塊は残る） |
| **B-3（推す）** | **52 週に層化して等間隔** | **156** | **4/4・ほぼ均等** |

**B-3 の式**（★層化。乱数を引きません）:

```
birth_week(i) = weekOfYear(birthYear) + floor(52 * rank(i) / n(cohort))
  rank(i) … そのコホート内での順位。★プリシードの id（NPC-48-000123）で決める
```

**なぜ B-3 か:**

1. 🔴 **`POOL-DRAIN` の「2 段の崖」が原理的に起きません。**
   いまは 800 頭が同じ週に 260 週へ達します。B-3 なら **毎週 15〜16 頭**です。
   崖は「直す」のではなく、**作らない**のが筋です。
2. 🔴 **`SEED-LOCKSTEP` が消えます。** `defaultMenu` は `ageWeeks % 4` で献立を選ぶので、
   位相が 4 つに散れば、疲労も調子も集団まるごとは動きません。
3. ✅ **較正が「1 つの位相の標本」でなくなります**（`SEED-LOCKSTEP` ④ の条件）。
4. ✅ **初日の形が、定常運転の形と同じ**になります。
   → 作り直した世界で測った較正が、そのまま運転中にも通ります。

**なぜ乱数でなく層化か:** 800 頭を 52 週へ無作為に配ると、週あたり 15.4 ± 3.9 頭
（CV 25%）で揺れます。**散らすのが目的なのに、揺れを足す理由がありません。**

🔴 **決定論（憲法 4）**: `seed-world.mjs` は `randomUUID()` で DB の id を振っています。
**そこから `birth_week` を引くと再現しません。** **プリシードの id から引きます**（seed で決まる）。

### 決め C: 能力は**本番の経路**で作る

4 歳の馬が創始のままの能力（素質開放率 0.28〜0.35）で立っているのは、世界として嘘です。

| 案 | 中身 | 判定 |
|---|---|---|
| **C-1（推す）** | `birth_week` を過去に置き、`last_processed_week = birth_week + 78` から **`advanceTrainingWeeks` を回して追いつかせる** | ★`tools/age-horses.mjs` と同じ作法。**製品の関数**を通る |
| C-2 | `stats` を計算して直書き | 🔴 **採らない**。§10.5「専用の簡易ロジックを作らない（同じ土俵にいることが公正性の担保）」に正面から反する |

✔ **EP は減りません**（`training-runner.ts:327` は `owner_id !== null` のときだけ引き落とす）。
✔ **260 週を超えた馬は、この回しの中で `retirement.ts` が自分で引退させます** — 決め A の転記と二重にならないよう、**どちらか一方に寄せる**必要があります（→ 照会 Q3）。

⚠️ **所要は未測定です。** `age-horses.mjs` が 7,370 頭 × 約 104 週を回せた前例があるだけです。
**「たぶん通る」を根拠にしません。** 裁定が出たら、まず staging で測ります。

---

## 4. 定常運転の供給（`POOL-DRAIN` の本体）

決め B を採ると、運転中の供給も同じ形になります。

```
繁殖牝馬 800 頭 に「繁殖週」を 52 週へ層化して配る
  → 毎週 15〜16 頭 が生まれる（年 800 頭）
  → 104 週 後に毎週 15〜16 頭 がデビュー
  → 260 週 後に毎週 15〜16 頭 が引退 → retirement.ts が役割を決める
  → 成績上位が種牡馬 200 / 繁殖牝馬 800 の枠へ上がる（§10.5）
現役の釣り合い ＝ 800 × 3 年 ＝ ★2,400 頭
```

🔴 **配合そのものは `breed()` をそのまま呼びます。** 専用の生成経路を作りません（§10.5）。
　現状 `insert into horses` は製品コードに **0 件** なので、**そこは新しく作ります**
　（これが `PROD-NEVER-AGED` (a) を「書けなかった」理由でもあります — 作る経路自体が無い）。

---

## 5. 🔴 数は足りるか — ★これは**算術**であって実測ではありません

現役が 7,333 → **2,400** に落ちます。D-007 は「6 分なら要る頭数 **3,500**」と書いています。
**一見 足りません。** しかし **3,500 はキャリア 24 戦のときの数**です。

```
D-007 の算術（24 戦）: 240R × 13.46 頭 ÷ 0.92 走/日 ≒ 3,511 頭
CC-1 ③ の後（40 戦）: 40 戦 ÷ 26 日 ＝ ★1.54 走/日
                       240R × 13.46 頭 ＝ 3,230 延べ出走/日 ÷ 1.54 ＝ ★2,097 頭
```

→ **現役 2,400 なら、余力 303 頭（+14%）。** 足ります。

⚠️ **D-007 の 3,500 という数字が、`CC-1 ③`（24 → 40 戦）で古くなっていました。**
　正典 D-007 の「★将来 3 分に上げる条件」の 3 つの数（6,999 / 2,333 / 583）も同じです。
　**ここでは直しません**（正典はレビュー側の管理・`PO-8` が「いま書き換えない」と裁定済み）。

⚠️ **この余力からは、まだ引かれます**:
`D-079 ⑧` の初期馬付与と `D-102` の購入は、どちらも NPC プールから抜きます。
**人数の見込みが無いと、303 頭で足りるかは言えません**（→ 照会 Q5）。

⚠️ **最終確認は配備後の周の実測です**（D-007 が自分でそう書いています・**R-28**）。

---

## 6. 🔴 照会 — 私では決められないもの

| # | 問い | なぜ私が決めないか |
|---|---|---|
| **Q1** | 現役の目標は **2,400** でよいか | §10.5 は 2,500。`PO-8` が「供給を決めてから」と保留にしている。**母数はゲームの手触りの決め** |
| **Q2** | 産駒は **毎週**（B-3）でよいか。年 1 回（B-1）を残すか | 「同期のクラスメイト」という手触りを捨てる決めになる。**遊びの話** |
| **Q3** | 既存の staging / 本番の 7,370 頭を **作り直す**か、**その場で直す**か | 作り直せば `STABLE-1-SKEW` の 66 頭の跡も消える（★レビュー側は「跡を消さない」と裁定済み）。**両立しない** |
| **Q4** | 繁殖に上がる条件 | §10.5 は NPC の「成績上位は自動」だけ。**プレイヤー馬の条件が正典に無い**（`retirement.ts` の照会が未回答のまま） |
| **Q5** | 初期馬付与・購入の想定人数 | プールの余力 303 頭が足りるかは、これ次第 |
| **Q6** | 🔴 §2 の **4,970 頭（引退が書かれていない）** を、この便で直すか | **新しく見つけた欠陥**です。`POOL-DRAIN` と同じ根ですが、**指示書の範囲外** |

---

## 7. 予言（★実装の**前**に書きます）

⚠️ 後から測ると、どんな結果にも説明が付いてしまいます。**先に書いた予言だけが外れられます。**
⚠️ **向きだけ書きます。値は書きません**（**AU-7′**）。

| # | 予言 | 外れたらどう読むか |
|---|---|---|
| 1 | 決め A（引退の転記）だけで、`ACTIVE_WHERE` の頭数が **7,333 → 2,400 付近へ落ちる** | 落ちなければ、私の 4,970 の数え方が違う |
| 2 | 決め B（52 週 層化）で、`ageWeeks % 4` の 4 位相が **ほぼ均等**になる | ならなければ、`birth_week` の配り方が届いていない |
| 3 | 決め B で、週あたりの引退頭数の **山が消える**（2 段の崖が無くなる） | 残れば、崖の出どころは年齢ではない |
| 4 | 決め C（育成の追いつき）で、素質開放率が **0.28〜0.35 から上がる** | 上がらなければ、`advanceTrainingWeeks` が回っていない |
| 5 | 🔴 決め A + B + C の後、本番の **V-4 / V-5 は上がり、V-6 は下がる** | 外れたら、**較正した模型のほうが現実と合っていない** |

⚠️ 🔴 **予言 5 は、`SCHEMA-DRIFT-PROD` が片付くまで判定できません。**
　いまの本番は **2026-08-20 のエンジン**です。**同じ版どうしで比べること**（**AU-21**）。

---

## 8. していないこと

- **実装していません。** 1 行も書いていません。
- **配備していません。本番にも staging にも書いていません。VPS に入っていません。**
- **所要を測っていません**（決め C の horse-week の量）。
- **`SCHEMA-DRIFT-PROD` を解いていません。** 予言 5 はその後です。
- §2 の 4,970 頭を **直していません**（→ Q6）。
