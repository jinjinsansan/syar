# 開発指示書 P0-fix — 平均回帰の有効化 / V-2 の新基準 / 距離適性の拡散是正

**発行**: 2026-08-04（レビュー側 Claude）
**前提**: `REVIEW_P0_VERDICT_20260804.md`（P0 レビュー回答）を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**D-008 を受けて §5.2/§5.3/§6.3/§6.4/§6.5/§13.1/§13.2/§13.3 を改訂済み。必ず読み直すこと**）

---

## 0. まず結論 — P0 は PASS でした

報告値はレビュー側の環境で完全に再現し、憲法遵守4項目も独立 grep で確認、**平均押し上げバグは無し**とコード監査で断定できました。V-2 FAIL を定数調整で取り繕わず照会に上げた判断、逸脱20件を実際に全列挙した誠実さ、実行時に逸脱を自己申告する出力設計は、いずれも高く評価します。

照会への回答は回答書 §4 に全件あります。要点:

- **Q2（3×4クロス）は開発側が正しく、指示書の 0.0703125 はレビュー側の誤り**でした。正典 §6.5 に正値 0.015625 を明記しました
- **Q1 はオーナー決定**: 案A（平均回帰）採用＋V-2 の再定義（D-008）
- 逸脱5-1（選抜 h²=0.3）・Q3（アレル単位）・Q4（相加的発現・距離値域）はすべて**承認**。他18件も承認

---

## 1. スコープ — 3件のみ

### F-1【必須】平均回帰を既定で有効化

正典 §6.4 が改訂され、平均回帰が**正典の機構になりました**（もはや正典外ではありません）。

- `REGRESSION_RATE: 0 → 0.20`
- `MUTATION_SD: 45 → 90`
- `REGRESSION_CENTER_RATIO: 0.45`（現状のまま）
- `balance.ts` のコメントを更新: 「正典外・既定無効・承認待ち」→「正典 §6.4 の機構・D-008 で採択」
- CLI 冒頭の `REGRESSION_RATE=0（正典どおり=無効）` の表示も実態に合わせて更新

### F-2【必須】V-2 を新3基準に置き換える

旧「100世代後の平均能力が +50% 以内」は**廃止**。正典 §13.2 の新基準に差し替える:

| 基準 | 内容 |
|---|---|
| **V-2a 平坦化** | 最終20世代の平均能力の傾きが **0.5%/世代 未満** |
| **V-2b 天井余裕** | 集団平均能力 ÷ アレル上限（1000）が **80% 以下** |
| **V-2c 長期健全性** | **300ゲーム内年**まで回したとき V-1 が 12〜18% に留まる |

- 3つとも PASS で V-2 系 PASS。exit code の規則は現状のまま（全PASSで0）
- V-2c は既定実行（100年）とは別に 300年を回す必要がある。`--generations 300` を別途実行する運用でよいが、**判定レポートに「V-2c は別実行で確認」と明示**すること。1コマンドで両方回せるなら尚可（`--long-horizon` 等）
- 旧 +50% の実測値は**参考値として出力に残してよい**（比較の連続性のため）。ただし合否判定には使わない
- 用語を「100世代」→「**100ゲーム内年（血統深度 N 世代・実時間約2.4年相当）**」に統一（正典 §13.3）

### F-3【必須】距離適性の拡散を止める（レビュー側監査 A-2）

現状、変異SDを値域比で機械的に縮尺しているため `distance_center` は SD=117・clamp±390 となり、**実測20世代で SD 369→591 と拡散**。正典 §5.2 が意図する「マイラー／ステイヤー／万能型の分化」が世代とともに消えます。

- 距離系形質（`distance_center` / `distance_range`）の変異幅を**値域比でなく絶対値で小さく固定**する（定数として `balance.ts` に外出し）
- 目安: 世代を経ても `distance_center` の集団SDが創始世代の水準（約370）から大きく広がらないこと。**具体的な数値は開発側が実測して提案**してください（レビュー側が指定しない — 実測で決めるべき性質のため）
- 検証: 100年・300年それぞれで `distance_center` の平均とSDの推移を出力に加え、分化が保たれていることを示すこと

---

## 2. やらないこと

- レースエンジン・オッズ・育成ループ・UI・DB（P1 以降）
- 大物覚醒（`BIG_ATAVISM_RATE`）の変更 — 単調ラチェットであることは正典 §6.3 に警告として記載済み。**回帰0.20が相殺する前提で機構は維持**します。触らないでください
- 選抜 h²=0.3 の変更 — 承認済み。P1 で実レース結果ベースの選抜に置き換えます

---

## 3. 合格基準

| # | 基準 |
|---|---|
| 1 | V-1（能力別CVの平均）が 12〜18%（100年・300年とも） |
| 2 | V-2a / V-2b / V-2c すべて PASS |
| 3 | V-3 が設定値 ±0.5pt |
| 4 | 距離適性の分化が世代を経ても保たれる（F-3・実測値で示すこと） |
| 5 | 全ユニットテスト PASS・typecheck exit 0・`any` ゼロ |
| 6 | 決定論（同一シードの出力が完全一致） |
| 7 | **複数シード（4以上）で 1〜4 が安定して成立**すること。単一シード判定は不可（正典 §13.3） |

定数調整は F-1 で指定した値を起点に、必要なら微調整してよい（履歴を報告書に記録）。**新3基準の目標値そのものの変更は不可**（オーナー承認事項）。

---

## 4. 報告様式

`REPORT_P0FIX_[日付].md` に:

1. 変更点（F-1/F-2/F-3 それぞれ）
2. 再実行コマンド（コピペで動く形）
3. 検証結果（100年・300年 × 4シード以上の表・PASS/FAIL）
4. F-3 で採用した距離系変異幅と、その根拠となる実測
5. 定数の調整履歴
6. 仕様からの逸脱・解釈で埋めた箇所（**全件列挙**。ゼロなら「なし」と明記）
7. 未解決の質問

レビュー側が全コマンドを独立再実行して照合します。

---

## 5. 申し送りへの回答

**`CLAUDE.md` の件、指摘は正しく深刻でした。** レビュー側が STAR 用の内容（正典へのポインタ・憲法要約・廃棄済み文書リスト・三者体制・コマンド）に差し替え済みです。次のセッションからは正しい前提で起動します。独断で書き換えず照会に上げた判断は正しかったです。
