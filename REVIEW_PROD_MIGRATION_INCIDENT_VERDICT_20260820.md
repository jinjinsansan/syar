# 本番への意図しないマイグレーション適用 — 事故の裁定と、「本番ビルドの乖離」の扱い

**発行**: 2026-08-20（レビュー側 Claude）
**対象**: `07294d7`（事故報告＋環境是正）／`3f9c089`／`93c76b7`
**判定**: **事故そのもの = 影響ゼロを確認・是正を承認。★ただし「本番ワーカーは 8/12 より前のビルド」という診断は成立していない（根拠が無効・こちらの独立測定は別の絵を示す）**

---

## 1. 事故の影響 — **ゼロで確定**（[CODE]）

| 確認 | 結果 |
|---|---|
| **`0017` はどの版が当たったか** | **★訂正版が当たっています。** FK は `references auth.users (id) on delete cascade`（`0017_user_identities.sql:43`）。commit 順は `3f9c089`（FK 修正・staging 適用）→ `93c76b7` → `07294d7`（事故）なので、**Q-AUTH-06 の是正後の版**。**旧版が本番に焼き付いて 0018 で直す羽目になる、という最悪は回避されています** |
| **`0016` の内容** | `alter table race_entries add column if not exists entrant_snapshot jsonb;` ＋ コメントのみ。**NULL 許容の加法的変更で、既存行にも既存コードにも影響しません** |
| **`0017` の内容** | 新規テーブル＋一意索引＋RLS 有効化＋`revoke all`。**既存テーブルに触れません** |
| **`.gitignore`** | `secrets*.env` のパターンが入り、`secrets.production.env` / `secrets.staging.env` とも **ignored ✓** |

**影響ゼロという申告は妥当です。** 加えて、**利用者0・馬券0**（D-051 で確認済みの状態）なので、仮に副作用があっても被害の受け皿がありません。

---

## 2. 自己分析は正確です。そのうえで、**3回目なので明文化します**

開発側の総括 —

> **安全策を足す変更の正しさを、その変更自体に依存する方法で確かめました**

**これは正確で、しかも一般則として正しいです。** そして**この案件で3回目**です。

| # | 何が起きたか | 出典 |
|---|---|---|
| 1 | **`--schemacheck` が、検査のついでにワーカーを起動していた** | `REPORT_P3_DEPLOY_20260812.md` §2-① |
| 2 | **`deploy.sh` の「ログに出すつもりの行」が、本番に対して migrate を実行した** | 同 §2-② |
| 3 | **`--yes-production`（＝安全策）を足したことで、引数解析が `9999` を食い、「未適用を全部当てる」に化けた** | 本件 |

→ **R-26 を新設します。**

> **R-26: 検査・安全策・ガードを足す変更は、それ自体が新しい副作用の経路になりうる。**
> **安全策を足したら、その安全策を通る場合と通らない場合の両方を、本番以外で一度実演してから本番に向ける。**
> **「安全にするための変更」を、安全になったことの根拠にしない。**

### ★併せて、もう1つ一般化できます — **壊れ方の向き**

開発側が書いた「**狭くなるなら気づくが、広くなると黙って余計に効く**」は、この案件の設計の癖を突いています。**同じ癖が2箇所で同時に噛みました**:

- `--env` を省略 → **本番**へ向く（狭い側＝staging ではない）
- 引数が消える → **未適用を全部当てる**（狭い側＝1件だけ、ではない）

→ **R-27 を新設します。**

> **R-27: 既定と縮退は、常に「狭い側・安全な側」に落ちるよう設計する。**
> 省略時・引数欠落時・解析失敗時に、**より広く効く**ほうへ落ちる形を残さない。

**`--env` 必須化・`--env local` 廃止・本番への二段目の明示要求・`package.json` の worker を staging へ — いずれも R-27 の方向で、承認します。**

### `| head -4` について

**本番に向ける操作の出力を、パイプで切らないでください。** 今回は SIGPIPE で node が途中で死に、**適用された行が目に入りませんでした**。→ **本番操作は全出力をファイルに残す**（`| tee` で手元にも出す）。これも R-27 の系です（「見る量を狭める」ことが「危険を見落とす」に落ちた）。

### ★`.gitignore` を先に直した判断 — **これは特筆に値します**

> 旧設定は秘密ファイルを**名前で個別に列挙**しており、**改名した瞬間に本番の資格情報が追跡対象になる**ところでした

**これも R-26 と同じ形です** — 「名前を変える作業」の安全性が、「名前で守る仕組み」に依存していた。**気づいて順序を入れ替えたのは、今回の一連で最も良い判断です。**

---

## 3. 🔴 「本番ワーカーは 8/12 より前のビルド」— **診断は成立していません**

### 根拠が無効です

> `entrant_snapshot` が0行であることから分かります

**`0016` を本番に当てたのは 01:50:59 です。** つまり**その列は数分前まで存在しませんでした。** **どんなビルドが動いていても、0行になります。**

**★この案件の言葉を借ります**（開発側が Q-3 の総括で書いたもの）:

> **結論の方向が結果的に正しかったことは、分析が正しかったことを意味しません**

### そして、こちらの独立測定は**別の絵**を示します（[CODE]）

**本番の公開ビュー `races_public` を読み取り専用で確認しました**（直近12レース）:

```
02:30 good/dirt   02:20 yielding/turf   02:10 good/turf    02:00 bad/dirt(settled)
01:50 good/turf   01:40 good/turf       01:30 good/dirt    01:20 good/turf ...
00:40 yielding/dirt
```

**馬場状態が `good` / `yielding` / `bad` に散っています。**

**D-052（2026-08-11）はこう記録しています** — 「**本番の443件すべてが `good` だった**ため `TRACK_CONDITION_CDF` が本番経路で使われておらず、**道悪適性が一度も効いていなかった**」。

→ **散っているということは、D-052 の修正は本番に入っています。** **本番のビルドは少なくとも 08-11 より後**です。

**つまり「8/12 より前」は、こちらの測定と噛み合いません。** D-055/D-056（08-12）だけが未配備という可能性は残りますが、**現状どちらも確定していません。**

### ★推測をやめてください。**本番のビルドは VPS が知っています**

**DB から逆算する必要がありません。** 配備は **リリースディレクトリ＋シンボリックリンク**方式です（D-037・`REPORT_P3_DEPLOY_20260812.md` §3）。**symlink の指す先を読めば、稼働中のコミットが分かります。** 本番の状態を1ビットも変えません。

**要求**:

1. **VPS の release symlink を読み、稼働ビルドのコミットを確定する**（これが一次資料）
2. **決定的な追加確認**: **01:51 以降に生成されたレース**（02:10 / 02:20 / 02:30）に `entrant_snapshot` があるか。**列が存在する状態で生成されたのに NULL なら、D-055 は未配備で確定**します。（皮肉ですが、**この確認が可能になったのは 0016 が当たったからです**）

---

## 4. 🔴 そして、**配備を今ここで決めません**

**本番のビルドが古いと確定したとしても、その場で配備しないでください。**

**理由は、たった今の事故そのものです。** —

> **未検証の前提に基づいて本番に操作を向けたことで事故が起きました。**
> **その直後に、別の未検証の前提に基づく本番操作（配備）を重ねてはいけません。**

**配備は「事故の後始末」ではなく、それ自体が独立した決定**です。順序は **①ビルドを確定 → ②差分を列挙 → ③オーナーに諮る** です。

### 諮るときの材料（先に書いておきます）

| | |
|---|---|
| **今が最も安い** | 利用者0・馬券0。**D-056 は「凍結が無いレースは中止・EP 返還」**なので、配備すると**旧ビルドが生成済みの数レースが一斉に中止**されます。**返す EP も、困る利用者も、今はゼロ**です。**この値段は今後上がる一方**です |
| **放置の害** | 本番が**リポジトリと違う программ**を走らせ続けます。**古いビルドの連続稼働は、ソークテストとしての意味を失うどころか、存在しないプログラムを検証して安心を与えます** |
| **配備の риск** | P4（レース演出）が進行中で、HEAD には D-065（走路の2次元化）など**結果に効く変更**が入っています。**本番のレース履歴が新旧で不連続になります**（利用者0なので実害は無いが、記録として） |
| **第3の選択肢** | **本番ワーカーを止める**。launch まで走らせる必要が無いなら、これが最も静かです |

**★レビュー側の推奨は「①ビルド確定 → ②オーナーに (a) HEAD へ配備 / (b) 停止 の二択で諮る」**です。**(c) このまま放置は採りません** — 乖離が見えない状態が続くこと自体が、今回の問題の本体だからです。

---

## 5. ★本体の問題 — **ゲートは「どのビルドで測ったか」とセットでなければ意味がない**

**この一件は R-16 の家族で、最も見えにくい形です。**

> **機構は実装され、テストは通り、正典にも書かれている。しかし本番には存在しない。**

**V-1〜V-19 も、`npm run verify` の全数一致も、すべて「リポジトリという名のプログラム」を測っています。** 本番が別のプログラムなら、**ゲートは全部通ったまま、本番だけが守られていません。**

→ **R-28 を新設します。**

> **R-28: 稼働中のビルド識別子を、外から確認できるようにする。**
> **ゲートの合格は「どのビルドで測ったか」とセットでのみ意味を持つ。**
> 本番の稼働コミットと、ゲートを測ったコミットが一致していることを、フェーズを閉じるときに確認する（R-23 の「全項目を HEAD に揃える」の、本番側の対）。

**★実装の参考**: 併走案件（Seven Days Derby）は **healthz にコミット sha を出しており**、「この md を push しても本番は変わっていない」を毎回 `healthz=<sha>` で確認しています。**同じ仕組みが、この案件にちょうど欠けていたもの**です。

---

## 6. まとめ — 開発側への回答

| # | |
|---|---|
| 1 | **事故: 影響ゼロで確定。是正（引数解析の切り出し・6件の固定・`--env` 必須化・`.gitignore` 先行修正）を承認します。** 特に `.gitignore` の順序判断は良い |
| 2 | **R-26 / R-27 / R-28 を新設**（§2・§5） |
| 3 | **「8/12 より前のビルド」は取り下げてください。** 根拠（0行）は 0016 の適用時刻で無効化されており、こちらの独立測定（馬場状態が散っている＝D-052 は配備済み）とも噛み合いません |
| 4 | **VPS の release symlink で稼働ビルドを確定** ＋ **01:51 以降のレースに `entrant_snapshot` があるかを確認** |
| 5 | **配備はここで決めません。** ビルド確定後、**(a) HEAD へ配備 / (b) 本番ワーカー停止** の二択でオーナーに諮ります |
| 6 | **anon 露出の全数確認は、予定どおり進めてください**（staging で期待値 → 本番を1回）。**§8.6 の `server_seed` と §12.4 の `potential` に直結するので、優先度は高いままです** |

**★4 と 6 は今すぐ進められます。5 だけが待ちです。**
