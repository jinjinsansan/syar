# 常設レース表示ハンドオフの実装前確認（2026-09-21）

対象: `レースが数分おきに開催.zip` 内の `RACE_NOTICE_HANDOFF.md` と `レース常設表示_設計案.dc.html`。

## 結論

3 サイズの UI 仕様と画面別対応表は読み取りました。ただし「本編と同じ位置データで走る」「横向きで同じレース・同じ進行位置から本編へ拡大」は、現行の本番データ経路ではまだ実現できません。表示だけ先に作ると、デモ映像を本番レースの生中継のように見せる危険があります。実装はデータ契約を決めてから進めるべきです。

常設の文字帯に必要な `races_public` の列は、実 DB への読み取り 1 件で HTTP 200 と列の存在を確認しました（`id,name,surface,distance,scheduled_at,status`）。画面での描画とページ間遷移は未確認です。

2026-09-21 のローカル実装では、新旧の通常画面に `RaceStrip` を接続し、`/watch-race` にも実際の開催情報を表示しました。`/watch-race` の全画面ボタンと説明文は「演出デモ」と明示し、実レースの結果は詳細ページへ案内します。型検査と `git diff --check` は通過しました。本番への反映・画面での目視検証は未実施です。

続いて、確定済みの `race_entries_public` の馬番・脚質・走破タイム・着順から、常設帯に45秒の「レース録画」を加えました。途中位置はエンジンの `secAtMetersLeftOf` で補間し、画面に「結果から再現」と明記します。開始位置は予定時刻から75秒後を基準にするため、ページ移動で先頭に戻りません。確定が遅ければ途中から表示するか、その回は表示しません。`npm run gate` は5段すべて通過しました（2750テスト、既知の赤2件）。本番での画面目視と実 DB の出走表読み取りはまだです。

常設帯からページ内の拡大表示も開けます。閉じれば元のフォームは同じ React ツリーに残ります。縦横切替でも開催時刻を再生時計に使うため、先頭へ戻りません。拡大表示追加後の型検査と本番向け Web ビルドは通過しました。端末での縦横切替・フォーム値保持の実操作は未検証です。

公開ビューの読み取り確認では、直近の確定レースの出走表を14頭分取得し、全頭の脚質・走破タイム・着順が録画表示の検証条件を満たしました。読み取りだけで、DB への書き込みはありません。確認コマンドは `node --env-file=apps/web/.env.local tools/verify-race-replay-read.mjs` です。

## 開発側への確認事項4点への回答

1. **`/watch-race` の扱い**: デザイン案どおり、横向き拡大の代替導線として残せます。ただし現行 `/watch-race` は `/race?return=/home` への案内ページであり、指定レース ID と進行位置を渡していません。フォールバック化にはこの受け渡しが必要です。
2. **位置データ配信**: `races_public` には開催時刻・状態・確定後の seed reveal などがありますが、発走中の各馬位置や進行率はありません。現行 `/race` の `build()` は固定 `POOL` とシードでデモ結果・位置モデルを作ります。複数ページへの配信負荷以前に、サーバー権威のレース位置スナップショット、公開範囲、更新間隔、再接続時の復元方法を決める必要があります。着順・進行率だけでも、それが本編と一致するデータから導かれることが条件です。
3. **演出時間**: 本編コード上の長さは `RACE_INTRO_RACE_START_SEC + built.warp.displaySec + POST_RACE_SEC + FINISH_REPLAY_DISPLAY_SEC` で、`warp.displaySec` はレース条件に依存します。`/race` の再生は `performance.now()` を起点とするローカル再生で、実際の開催時刻とは同期していません。したがって「発走から結果確定まで」の本番実測値として、この式だけを常設表示に使えません。
4. **自馬フィルタ**: マイページで自馬を優先するデザインを採用し、他画面では共通の開催表示を基本にするのが妥当です。どのレースを全員の常設枠に出すか、自馬出走と締切直前が競合した場合の優先順位は、サーバー側の公開データと合わせて一つの規則にする必要があります。

## 実装の順番

1. 実レースの ID、状態、発走時刻、出走馬、位置または再現可能なスナップショット、結果をつなぐ読み取り契約を決める。
2. 本編 `/race` を実レースへ接続し、同じ ID と時刻から再開できるようにする。
3. 常設 `RaceStrip` を、まず文字だけの「帯」と結果表示で接続する。接続断では最後の取得値と更新失敗を区別して表示する。
4. 同じ位置データから「大」「極小」を描き、横向き拡大・フォーム値保持を確認する。

上記を満たすまで、走行スプライトを単独でループ再生して「本物の走行」と表示しない。

## 根拠の主なコード

- `packages/scheduler/src/cycle.ts`: 開催サイクルは6分、発走はサイクル末尾。
- `apps/web/src/lib/queries.ts` と `db/migrations/0040_entry_screen_columns.sql`: 公開レースの現在のフィールド。
- `apps/web/src/app/race/page.tsx`: 固定プールの `build()`、ローカル再生時計、描画長の式。
- `apps/web/src/app/watch-race/page.tsx`: 現在の案内ページから `/race` への導線。
