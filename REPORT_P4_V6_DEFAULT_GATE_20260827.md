# 報告 — 台本 v6 を既定にしてよいか（80 seed の分布測定）

日付: 2026-08-27 / ブランチ `p4/race-30sec-cuts` / HEAD `2b2aa3d`
発端: `HANDOVER_P4_STRAIGHT_CONTEST_20260826.md` §4-1「v6 を既定にするか」（オーナー判断事項）
関連: `REPORT_P4_STRAIGHT_CONTEST_20260826.md` / `QUESTIONS_P4_FINISH_CONTEST_20260825.md`
★**先行裁定**: `REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md`（★本報告はこの裁定の**後**に位置します）

> ★**製品コードは 1 行も変更していません。** すべて読取専用の測定です。
> ★`Date.now()` / `Math.random()` を使っていません。着順・タイム・着差・払戻に触れていません。

---

## 0. 結論だけ先に

★**v6 はいま既定にできません。** 理由は 2 つあり、★**別々の問題**です。

| # | 見つかったもの | 大きさ | 担当 |
|---|---|---|---|
| **A** | ★せめぎ合いがほとんど映らないレースが**多数派** | 80 seed 中 ★**31%**（画面既定 γ=1.3）が「2 頭の競り合いすら半分未満」 | ★**描画では解けない**（エンジン側・裁定済み） |
| **B** | ★**注視点が跳ぶ seed がある**（未知の欠陥） | 80 seed 中 **8%** が 0.5m/コマ 超・最大 **1.11m/コマ** | ★**描画側の欠陥**（P4 の範囲内） |
| **C** | ★**測定器と実画面の γ が違っていた** | ★道具 γ=1.0 / 画面 γ=**1.3** | ★**測定器の欠陥**（★R-30 の 5 件目） |

★**B は γ では直りません**（§5-2 で実測）。★**A は γ で軽くなりますが、解け切りません**（42%→31%→25%）。
したがって既定にする判断は **γ の決定が付いてから**が順当です。

---

## 0-2. ★本報告を書く途中で見つけた「自分の誤り」（先に書きます）

★**A は照会中ではありません。2026-08-25 に裁定済みでした**（`REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md`）。
本報告の初稿は A を「照会済み・裁定待ち」と書いていましたが、**誤りです。** 訂正します。

裁定の内容（本報告の位置づけを決めるので、要点だけ引きます）:

| 裁定 | 内容 |
|---|---|
| 前提の訂正 | ★照会の二者択一（カメラで無理 ⇒ 較正を動かすしかない）は**成り立たない**。第三の自由度が **D-064** で既に開かれている |
| 動かすもの | ★**`RACE_RANDOM_K` は動かさない。** 動かすのは**スコア差→タイム差の写像の形（γ）だけ** |
| なぜ安全か | ★写像はスコア差について単調 ⇒ **着順は不変**。総差も定義から不変 ⇒ **V-4/V-5/V-6・払戻・人気・V-17② に定義上触れない** |
| 推奨値 | **γ = 1.3〜1.6**。目標は「4 頭以上が 5 馬身以内 = 50〜70%」「1-2 着 中央値 1 馬身前後」「独走 3 馬身以上を 20〜25% 残す」 |
| ★**Q-4** | ★**「カメラ側の調整は可。ただし写像の判断が出るまで着手しないでください」** — γ が入ると「5 馬身以内の頭数」が変わり、**カット境界の最適位置も動く**ため |
| 最終判定 | ★**実画面でオーナーが決める。** γ=1.3 と γ=1.6 を同じ seed で並べて出し、選んでもらう（裁定 §9） |

★**そして γ の機構は既に実装されています。** `TIME_GAP_SHAPE_GAMMA`（`packages/race-engine/src/balance.ts:447`）。
★既定は **1.0 = 現行の式へ短絡**。正典 R-15 の「**入っているが使われない状態で一度コミットする**」形です。

### ★この訂正が本報告の結論に与える影響

1. ★**A は「レビュー側へ出す照会」ではありません。** 出す先は**オーナー**で、必要なのは
   ★**γ=1.3 / 1.6 の比較出力を見て選ぶこと**です。新たな照会は要りません。
2. ★**§5 の「B を先に直す」という初稿の推奨は、裁定 Q-4 に反していました。** 撤回します。
   B（注視点の跳び）は**隊列が伸びたレースで出る欠陥**であり、★**γ はまさにその「伸び」を変えます。**
   ★先に B の制動を合わせると、γ 決定後に**合わせ直しになります** — 裁定 Q-4 が警告したとおりの形です。
3. ★**逆に、本報告の測定値そのものは裁定の後押しになります。** 裁定は 40 レースの中央値で
   「4 頭以上が 5 馬身以内 = 31%」を示しましたが、★本報告は **80 seed の分布**で
   「**4 頭以上が映る時間の中央値が 0.0 秒**」という、より強い形の同じ事実を出しています。

---

## 1. なぜ測ったか

★引継ぎ書 §4-3 が「**せめぎ合いが無いレースをどう見せるかが最大の未解決事項**」と書いており、
かつ **本番では seed を選べません**。

ところが v6 の合格根拠は **seed 253 / 90 / 198 / 2 の 4 本**で得られたものです。
⚠️ ★**4 本は分布ではありません。** 単一実現から得た指標をゲートにするなら実現間の分散を先に測る
（正典 R-20）— それが未了のまま既定にすると、**本番の多数派が未評価のまま既定になります。**

★これは前回の教訓の裏返しです。前回は「**在らないものは映せない**」でしたが、
今回問うのは ★「**在らないとき、何が映るのか**」です。

---

## 2. 測ったこと

道具: `tools/audit-contest-focus.mjs`（既存・読取専用・実画面と同じ `auditSceneAt` を通す）
条件: 台本 **v6** / 演出なし（`climax` 無効）/ 1280×720 / 30fps / **seed 1〜80 の全数**

```powershell
npx tsx tools/audit-contest-focus.mjs --seeds 1,2,3,...,80
```

⚠️ ★**per-seed の数値はすべて既存道具の出力そのままです。** 私が足したのは**集計だけ**で、
　指標を作り直していません（正典 R-30「指標を自作せず、既にある実装を使う」）。

### 2-0. ⚠️ ★この測定の既知の弱点（★読む前に知っておくこと）

★**出走表が D-018 非準拠のプールから引かれています。**

`buildAuditRace` は `apps/web/src/lib/watch-pool.json` の**連続 12 頭スライス**を使います
（`tools/lib/race-audit-build.mjs:25,37`）。★このプールは能力順に並んでおらず、
**12 頭スライスの能力幅は中央値 16.4%** です。★**正典のクラス幅は 6%** です
（`REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md` §2-2 が測定して指摘）。

★**裁定は「以後この道具で映像の判断材料を作るときは、能力順プールから引くべき」と明記しています。**
★**本報告はそれに従えていません。** 従っていない、と明記したうえで読んでください（R-13）。

★**ただし結論は覆らないと考えます。** 裁定 §2-2 が**まさにこの差を実測**しており:

| | 現行デモ（幅 16.4%・★本報告と同じ） | 同格帯（幅 3.1%） |
|---|---:|---:|
| 1着-2着 中央値 | 3.12 馬身 | 2.45 馬身 |
| 4 頭以上が 2 馬身以内 | 0% | **0%** |
| 4 頭以上が 5 馬身以内 | 25% | **25%** |

★**せめぎ合いの量はほとんど動きません**（レビュー側の仮説が外れた、と裁定自身が書いています）。
★したがって §2-1 の分布も、同格帯で引き直しても**大きくは動かない**と見込みます。
⚠️ ★**これは見込みであって実測ではありません。** 引き直しは §6 に残件として挙げます。

### 2-1. ★閾値に依存しない分布（80 seed）

★下の表は**私が決めた区切りを一切含みません**。生の分位数です。

| 指標 | 最小 | 25% | **中央** | 75% | 最大 |
|---|---:|---:|---:|---:|---:|
| 画面の平均頭数 | 1.00 | 1.40 | **2.30** | 3.40 | 4.80 |
| 主役 2 頭以上の時間（%） | 0 | 13 | **81** | 100 | 100 |
| 主役 4 頭以上の時間（秒） | 0.0 | 0.0 | ★**0.0** | 5.2 | 13.2 |
| 注視点が馬から離れる最大量（m/コマ） | 0.00 | 0.05 | **0.09** | 0.15 | ★**1.11** |

★**「4 頭以上」の中央値が 0.0 秒です。** つまり **半分のレースでは 4 頭のせめぎ合いが 1 秒も映りません。**
★そして跳びは**中央 0.09 に対し最大 1.11** — 平均では見えない**裾の重い**壊れ方をしています（R-22）。

### 2-2. 区分けした見え方

⚠️ ★**以下の区切り（8 秒 / 3 秒 / 50%）は私が読みやすさのために置いたもので、正典の基準ではありません。**
　判定に使うなら §2-1 の分位数を見てください（測定の自由変数・R-12）。

| 出る画 | 件数 | 割合 |
|---|---:|---:|
| 4 頭以上が 8 秒以上（★seed 253 級） | 15 | **19%** |
| 4 頭以上が 3〜8 秒 | 8 | 10% |
| 4 頭は出ないが 2 頭の競り合いは半分以上映る | 23 | 29% |
| ★**2 頭の競り合いすら半分未満**（ほぼ単騎の画） | 34 | ★**42%** |

平均頭数の平均 **2.52 頭**（seed 253 は 4.8 頭）。

★**本番で seed を選べない以上、来た人の 4 割強はほぼ単騎の画を見ます。**

### 2-3. 対照（引継ぎ書が挙げた seed）

| seed | 競り合いカット | 平均頭数 | 2 頭以上 | 3 頭以上 | 4 頭以上 | 跳び |
|---|---:|---:|---:|---:|---:|---:|
| **253**（推奨） | 15.4秒 | 4.8頭 | 15.4秒 (100%) | 13.9秒 | **11.9秒** | 0.13 |
| 99（ハナ差） | 15.2秒 | 4.6頭 | 13.2秒 (87%) | 9.9秒 | 4.6秒 | 0.08 |
| 42（せめぎ合い無し） | 15.0秒 | **2.2頭** | 13.0秒 (87%) | **0.0秒** | **0.0秒** | 0.12 |

★**seed 42 は「悪い例」の代表として引継ぎ書に載っていますが、80 seed の中では悪いほうではありません。**
　42 でも 2 頭の競り合いには 87% の時間ちゃんと向いています。★**もっと悪い seed が 34 本あります。**

---

## 2-4. ★発見 C — 測定器と実画面の γ が違っていた（★R-30 の 5 件目）

★**`/race` のデモ画面の既定は γ=1.3 です。**

```ts
const DEMO_CONTEST_GAMMA = 1.3;                        // apps/web/src/app/race/page.tsx:392
function contestGammaFromSearch(search) { ... }        // 指定なし → 1.3
```

★**ところが監査側の既定は γ=1.0 でした。**

```js
const balance = opts.balance ?? DEFAULT_RACE_BALANCE;  // tools/lib/race-audit-build.mjs:57
DEFAULT_RACE_BALANCE.TIME_GAP_SHAPE_GAMMA === 1.0      // packages/race-engine/src/balance.ts:544
```

★**つまり 2026-08-25 以降、監査道具はオーナーが見ている画とは違う写像を測っていました。**

### 2-4-1. 何がずれたか

★**引継ぎ書と `REPORT_P4_STRAIGHT_CONTEST_20260826.md` の看板の数字が、画面の数字ではありません。**

| seed 253・台本 v6 | 4 頭以上 | 平均頭数 |
|---|---:|---:|
| γ=1.0（★道具の既定・**報告書に載っている数字**） | **11.9 秒** | 4.8 頭 |
| γ=1.3（★**画面の既定・オーナーが実際に見たもの**） | **10.5 秒** | 4.7 頭 |
| γ=1.6 | 9.5 秒 | 4.6 頭 |

★差は小さいですが、★**「オーナーが見た画を測っていなかった」という事実のほうが重要です。**
正典 R-30 は「**測定器が評価者と同じ入力を読んでいることを、測定器自身の検査で固定する**」と定め、
★**3 回再発したので明文化された**と書かれています。★**これで 5 件目**です。

### 2-4-2. ★是正（どちらかを選ぶ必要があります・要指示）

⚠️ ★**「道具側の既定を 1.3 に合わせる」は安直で、危険です。** `race-audit-build.mjs` は
　**すべての監査道具の共通土台**なので、既定を変えると**他の全報告の数字が黙って動きます**。

★推奨は **②** です。

| 案 | 中身 | 懸念 |
|---|---|---|
| ① 道具の既定を画面（1.3）に合わせる | 1 行 | ★**他の全監査の数字が黙って動く。** 過去の報告と比較不能になる |
| ★**② 道具に γ を必ず表示させ、既定を持たせない** | ★**本便で `--gamma` を足し、実行時に必ず出すようにしました**（下記） | 呼ぶ側が指定を忘れる余地は残る |
| ③ 画面の既定を 1.0 に戻す | 画面が変わる | ★**オーナーが合格と言った画が変わる**。採るべきでない |

★本便では ② を実施しました。`tools/audit-contest-focus.mjs --gamma <値>` を足し、
★**実行時に必ず「γ=…」を印字**します（正典 R-8「実行時に逸脱を自己申告する出力」）。

```
台本=v6  γ=1（★本番の既定）  演出=なし（馬は動かしていません）
台本=v6  γ=1.3  演出=なし（馬は動かしていません）
```

⚠️ ★**これは対症です。** ★根治は「画面と道具が γ を **1 か所から引く**」ことです
（正典 D-052「条件は 1 か所で引く」/ R-30）。★**共通土台に触るので指示を待ちます。**

---

## 3. ★発見 B — 注視点が跳ぶ（未知の欠陥）

| seed | 跳び | 平均頭数 | 2 頭以上 |
|---|---:|---:|---:|
| **16** | ★**1.11m/コマ** | 1.2頭 | 8% |
| **18** | 1.10m/コマ | 1.4頭 | 8% |
| **72** | 1.03m/コマ | 1.4頭 | 11% |
| 78 | 0.81m/コマ | 2.5頭 | 88% |
| 74 | 0.75m/コマ | 3.2頭 | 89% |
| 5 | 0.75m/コマ | 1.4頭 | 10% |
| （**253**・対照） | 0.13m/コマ | 4.8頭 | 100% |

★**seed 253 の 8.5 倍**です。46% の寄りでは走路が **9.2m** しか映らないので、
1.11m は ★**1 コマで画面幅の 12% 動く**ことになります。

### 3-1. なぜ起きるか

★**跳ぶ seed は、ほぼ全部「競り合いが乏しい seed」と一致しています**（上表の平均頭数を見てください）。

`contest-focus.ts` は **位置に対しては連続**です。`best`（いちばん近い組の近さ）を
smoothstep で混ぜており、★**組の入れ替わりでは跳びません。** そこは設計どおりです。

⚠️ ★**しかし時間方向の制動がありません。**

隊列が伸びたレースでは `strength` がほぼ 0 で推移し、注視点は先頭に貼り付いています。
そこへ**たまたま 2 頭が近づいた一瞬**があると `best` が跳ね上がり、注視点がそちらへ振られ、
離れるとまた先頭へ戻ります。★**位置に対する連続性は、この「近づく速さ」による振れを止めません。**

★引継ぎ書と `contest-focus.ts` の注記は「加重平均だから跳ばない」と書いていますが、
★**それが防いでいるのは「組の入れ替わり」による跳びであって、「近づく速さ」による振れは別物です。**
seed 253 / 42 / 99 の 3 本では、隊列が伸びかつ一瞬だけ近づく、という組み合わせが出ませんでした。

★これは R-16 の形です — ★**「跳ばないこと」を確かめたゲートが、跳ぶ経路を見ていませんでした。**

---

## 4. ⚠️ 測定器の限界（★そのまま受け取ってはいけない点）

★**この道具では v5 と v6 を比較できません。**

```
npx tsx tools/audit-contest-focus.mjs --seeds 16 --script v5
  → 競り合いカット 計 0.0秒 / 平均 0.0 頭 / 2 頭以上 0.0秒 (NaN%) / 跳び 0.00m/コマ
```

★**この 0.00 は「v5 は跳ばない」ではありません。**
道具は `shot.id === 'straight-contest'` のコマだけを見ますが、**v5 にそのカットは存在しません**。
つまり ★**1 コマも測っていない**という意味です。

⚠️ ★**0 コマのとき 0.00 / NaN% と表示するのは危険です。**「異常なし」に見えます。
　正典 R-3（判定不能は PASS でなく FAIL に倒す）/ R-21（「出なかった」を理由を読まずに結果にしない）
　に照らし、★**0 コマは明示的に失敗として出すべき**です。これは道具側の残件として §6 に挙げます。

★**したがって本報告は「v6 が v5 より悪い」とは主張していません。** その比較は未実施です（R-13）。

---

## 4-2. ★γ を振って測った（★裁定が推した自由度は、A をどこまで解くか）

道具: `tools/audit-contest-focus.mjs --gamma <値>`（★本便で足した口・§2-4-2）
条件: **seed 1〜80 の全数** / 台本 v6 / それ以外は同一

★**前提の確認**（R-11）: γ=1.0 で seed 253・42 が**前回と 1 ビットも違わない**ことを先に確かめました
（253: 15.4/4.8/100%/13.9/11.9/0.13、42: 15.0/2.2/87%/0.0/0.0/0.12）。★差し替え機構は既定を汚していません。

| γ | 平均頭数（中央） | 主役2頭以上（中央） | 4頭以上（中央） | ★「2頭すら半分未満」 | 253 級（4頭8秒+） | 跳び最大 |
|---:|---:|---:|---:|---:|---:|---:|
| **1.0** | 2.30 | 81% | **0.0 秒** | **42%** | 18% | 1.11 |
| ★**1.3（画面の既定）** | 3.00 | 87% | 0.3 秒 | ★**31%** | 27% | 1.04 |
| 1.6 | 3.30 | 87% | **3.1 秒** | ★**25%** | 33% | 0.97 |

### 4-2-1. ★読み取れること

1. ★**γ は効きます。** 「4 頭以上」の中央値が **0.0 → 3.1 秒**、「ほぼ単騎」が **42% → 25%**。
   ★裁定の見立て（「4 頭以上が 5 馬身以内 = 31% → 54〜69%」）と**同じ向き**です。
2. ⚠️ ★**しかし解け切りません。** γ=1.6 でも ★**4 本に 1 本はほぼ単騎の画**です。
   ★**「γ を入れれば A は終わる」ではありません。**
3. ★**γ は「底上げ」であって「上積み」ではありません。** ★既に競っている seed では**逆にわずかに減ります**:

   | seed 253（4 頭以上） | γ=1.0 **11.9 秒** → γ=1.3 **10.5 秒** → γ=1.6 **9.5 秒** |
   |---|---|

   ★写像が差を詰めるので、レースが**互いに似てきます**。★悪い側は上がり、良い側は下がる。
   ⚠️ ★これは裁定 §7 の **V-17③②「独走が独走に見えること」** が守ろうとしたものの、**別の側面**です。
4. ⚠️ ★**seed 42 は γ で悪化します**（2 頭以上 87% → 65% → 50%）。★平均が良くなっても、
   **個々のレースが良くなるとは限りません**（R-22: 集団平均は個体ごとのずれを見ない）。

### 4-2-2. ★B（跳び）は γ では直りません

| γ | 0.5m/コマ 超の seed |
|---:|---|
| 1.0 | 5, 16, 18, 72, 74, 78（6 件） |
| 1.3 | 5, 16, 18, 72, 74, 78（6 件） |
| 1.6 | ★**5, 16, 18**（3 件） |

★**seed 5 / 16 / 18 はどの γ でも跳びます。** 跳びの最大値も **1.11 → 0.97** としか動きません。

★**したがって B は γ とは独立した欠陥です。** §5-B の「④まで待つ」という順序は、
★**跳びそのものの原因が γ にある可能性が消えた**ぶん、根拠が変わります（§5 を参照）。

---

## 4-3. ★出力した映像（[EYES] 用・★これを見て γ を決めてください）

★**裁定 §9 が指定した進め方**（γ を同じ seed で並べて出し、オーナーが見て選ぶ）に従いました。

★**2 seed 分あります。** seed 253（★競り合いが**在る**側）と seed 42（★**在らない**側）。

| seed | 3 つ並べ（軽量・まずこれ） | 高画質 | 単体 |
|---|---|---|---|
| **253** | `finish-contest-compare-v6-253-small.mp4`（2.1MB） | `-v6-253.mp4` / `.gif` | `finish-contest-v6g10/13/16.mp4` |
| ★**42** | `finish-contest-compare-v6-42-small.mp4`（1.7MB） | `-v6-42.mp4` / `.gif` | `finish-contest-s42g10/13/16.mp4` |

★すべて `out/2d-finish-contest/` の下です。seed 42 の窓は **18.73〜43.50s / 744 コマ**。

| 中身 | パス |
|---|---|
| ★**3 つを並べたもの（軽量・まずこれ）** | `out/2d-finish-contest/finish-contest-compare-v6-253-small.mp4`（2.1MB） |
| 3 つを並べたもの（高画質 / GIF） | `finish-contest-compare-v6-253.mp4`（17.7MB）/ `.gif`（38MB） |
| γ=1.0（現行のエンジン既定） | `finish-contest-v6g10.mp4` |
| ★γ=1.3（★**いまの画面の既定**） | `finish-contest-v6g13.mp4` |
| γ=1.6 | `finish-contest-v6g16.mp4` |

- 条件: **seed 253 / 台本 v6 / 表示秒 19.33〜43.37s（4 角出口〜ゴール〜勝馬）/ 722 コマ・30fps**
- ★**違うのは `?contest=` だけ**です。台本・カメラ・素材・表示秒・seed はすべて同一
- 撮り方: 通常 `/race` を**本物のブラウザで開いて**取り込み（`tools/capture-overhead-stride.mjs`）

⚠️ ★**2026-08-25 の `finish-contest-compare.mp4` とは別物です。** あちらは
**seed 42・台本 v5**（★せめぎ合いが存在しない seed × オーナー不合格の画角）で撮られています。
★**古い証拠は消さずに残してあります**（`--tag` で出力名を分けました）。

### 4-3-1. ★着順が動いていないことの確認

```
γ=1.0  着順=[7,6,12,1,9,4,10,11,5,3,2,8]
γ=1.3  着順=[7,6,12,1,9,4,10,11,5,3,2,8]   ← ★完全に同一
γ=1.6  着順=[7,6,12,1,9,4,10,11,5,3,2,8]   ← ★完全に同一
```

★裁定の「**写像は r について単調なので着順は動かない**」が、この seed でも成立しています。

| γ | 上位 5 頭の着差（秒） | 着差ラベル（2〜5 着） |
|---:|---|---|
| 1.0 | 0 / 0.294 / 0.338 / 0.442 / 0.445 | 1馬身 / アタマ / 1/2馬身 / ハナ |
| **1.3** | 0 / 0.164 / 0.197 / 0.279 / 0.281 | 3/4馬身 / アタマ / クビ / ハナ |
| 1.6 | 0 / 0.091 / 0.114 / 0.176 / 0.177 | ★**クビ / ハナ / クビ / ハナ** |

⚠️ ★**γ=1.6 は 4 つのうち 2 つが「ハナ」です。** 裁定 §7 の V-17③③（ハナが 35% を超えないこと）は
**レース全体の分布**に対する基準ですが、★この seed 単体では既にかなり詰まっています。

### 4-3-2. ★映像で見えたこと（★数値ではなく、見た事実）

★**γ=1.3 / 1.6 は、この seed の決着を「ゴール前の差し切り」に変えます。**

同じ表示秒 38.00s（残り 91〜94m）の順位表:

| | その瞬間の先頭 | 最終的な 1 着 |
|---|---|---|
| γ=1.0 | ★**7 ナニワスピリット**（＝勝ち馬が既に前） | 7 |
| ★**γ=1.3** | ★**12 ピンクレディ**（＝勝ち馬はまだ後ろ） | 7 |
| γ=1.6 | ★**12 ピンクレディ** | 7 |

★**つまり γ=1.0 では勝ち馬が残り 94m で既に先頭ですが、γ=1.3 / 1.6 では
残り 93m で 12 番が前にいて、そこから 7 番が差し切ります。**

⚠️ ★**これは §4-2-1 の「253 では 4 頭以上の秒数が減る」と矛盾しません。** 別の量です
（**画面に何頭入るか** と **先頭が入れ替わるか**）。★**片方の指標だけで γ を判断しないでください。**

★**そしてこれこそ [EYES] が要る理由です。** どちらが「面白い」かは数値では決まりません。

---

## 4-4. ★★指標そのものの欠陥 —「主役2頭以上」は追い込みを減点していた

★**本報告で最も重い発見です。** §4-2-1 の「seed 42 は γ で悪化する（87% → 50%）」を
実画面の位置で追ったところ、★**悪化ではありませんでした。**

道具: `tools/_gammaprobe.mjs`（★使い捨ての probe・`.gitignore` の `tools/_*.mjs` に該当）

### 4-4-1. 何が起きていたか（seed 42・同じ表示秒）

| 表示秒 | γ=1.0 の画面内 | γ=1.6 の画面内 |
|---|---|---|
| 26s | 4番・10番（10番は **4.3m** 差） | ★**4番だけ**（10番は **8.5m** 後ろ＝画面外） |
| 30s | 4番・10番（**1.1m** 差） | ★**4番だけ**（5.9m 後ろ） |
| 34s | 10番・4番（1.8m 差） | 4番・10番（3.2m 差） |
| 37s | 10番・4番（3.9m 差） | 4番・10番（★**1.3m** 差） |

- **γ=1.0** … 4番と10番が**直線を通してずっと並走**し、途中で入れ替わる
- **γ=1.6** … 10番が ★**8.5m 後ろから追い込み**、残り僅かで 1.3m まで詰める

★**γ=1.6 は seed 42 の決着を「並走」から「追い込み」に変えていました。**

### 4-4-2. ★だから指標が間違っている

★**「主役2頭以上の時間」は、ずっと並走する展開を高く評価し、後ろから差してくる展開を低く評価します。**

⚠️ ★**ところが、オーナーの元々の要望はこうでした**（`QUESTIONS_P4_FINISH_CONTEST_20260825.md` の発端）:

> 画面の左側から**追い込み馬が一気に差してくる**

★**指標が、要望されたものを減点していました。**

★これは正典 R-16 の形です（「**壊れた状態がこのゲートを通ってしまわないか**」の裏返しで、
★**望ましい状態がこのゲートに落とされないか**）。
★**§2-1 / §4-2 の数値は、この観点では信用できません。** [EYES] が要ります。

### 4-4-3. ★もう 1 つ、ついでに判明した測定条件

★**画面に入る走路は 9.2m ではなく、実測 5〜6m でした。**
`contest-focus.ts` の注記と `REPORT_P4_STRAIGHT_CONTEST_20260826.md` は **9.2m** と書いていますが、
★上表のとおり **8.5m 後ろの馬は画面外**、**4.3m 後ろは画面内**です。
⚠️ ★**9.2m という数字がどこから来たのか、追えていません。** 残件（§6-6）。

### 4-4-4. ★私自身が同じ罠に落ちた（自己申告・R-8）

★probe の初版は `r.drawn`（**12 頭全部**を返す）を数えており、
★**監査道具（`scene.visibleHorses` を投影して画面内かを見る）と違う量を測っていました。**
★1 回目の出力は「どの γ でも 12 頭」で、**矛盾の解明に一歩も近づいていませんでした。**
是正して `audit-contest-focus.mjs` と**同じ手順**に揃えたところ、上表が出ました。

★**R-30 は「3 回再発したので明文化」と書かれています。★この案件だけで、私で 2 回目です。**

---

## 5. ★次にやるべきこと（★裁定 Q-4 に従った順序）

⚠️ ★**初稿は「B を先に直す」と書いていましたが撤回します**（§0-2）。裁定 Q-4 に反していました。

### ★順序（★これが本報告の実質的な提案です）

```
① γ の比較出力を作る（γ=1.3 / 1.6 / 1.0 を同じ seed で）   ← ★開発側が実行できる
        ↓
② オーナーが実画面で見て γ を決める                        ← ★オーナー決定事項（裁定 Q-1）
        ↓
③ γ 決定後に、この 80 seed 測定を引き直す                  ← ★A がどれだけ解けたかが分かる
        ↓
④ 残った分だけカメラを合わせる（B の制動・カット境界）      ← ★裁定 Q-4 が「ここまで待て」と言った地点
        ↓
⑤ v6 を既定にするかを決める
```

### A. せめぎ合いが無いレース（42%）— ★描画では解けない／★だが裁定は既に出ている

★**新たな照会は要りません。** 裁定（§0-2）が答えを出しています:
★**`RACE_RANDOM_K` は動かさず、スコア→タイム写像の γ を動かす。着順・V-4/V-5/V-6・払戻には定義上触れない。**

★**必要なのはオーナーの目です。** 裁定 §9 が指定した進め方は
★**「γ=1.3 と γ=1.6 を同じ seed で並べて出力 → オーナーが見て選ぶ」**。

★機構は実装済み（`TIME_GAP_SHAPE_GAMMA`・既定 1.0）で、監査側も
`buildAuditRace({ balance })` で**写像だけ差し替えられる**ようになっています
（`tools/lib/race-audit-build.mjs:57`「★比較のために写像だけ差し替えられるようにする」）。
★**したがって①は既存の部品だけで実行でき、製品の既定に触れずに済みます。**

### B. 注視点の跳び（8%）— ★描画側の欠陥・★ただし着手は④まで待つ

★`contest-focus.ts` に**時間方向の制動**が要る（§3-1）。馬の位置には触れません（憲法 3）。
★`Date.now()` を使わず**表示時刻の関数**として書けば決定論を保てます（憲法 4）。

⚠️ ★**初稿は「γ が伸びを変えるから待て」と書きましたが、その理由は実測で否定されました**（§4-2-2）。
★**seed 5 / 16 / 18 はどの γ でも跳び**、最大値も 1.11 → 0.97 としか動きません。
★**跳びの原因は γ にありません。**

★**それでも④まで待つ理由は残ります。** ただし理由が変わります:

| 待つ理由 | いま有効か |
|---|---|
| ~~γ が「伸び」を変えるので、制動の効き方が変わる~~ | ★**否定された**（§4-2-2） |
| ★**カット境界の最適位置が γ で動く**（裁定 Q-4 の本文） | ★**有効**。跳びの制動もカット内で効くので、境界が動けば評価もやり直し |
| ★**跳ぶ 6 seed が γ=1.6 で 3 seed に減る** | ★**有効**。直す対象の数が γ で変わるなら、γ 決定後に測り直すのが順当 |

★**したがって「④まで待つ」は維持しますが、根拠は「原因が γ にある」ではなく
「直す対象と評価条件が γ で動く」です。** ★これは前より弱い理由です。
★**オーナーが「跳びだけ先に直せ」と判断するなら、それは妥当です**（要指示）。

⚠️ ★いずれにせよ**指示書が出るまで実装しません**（CLAUDE.md「実装は指示書が出ている範囲のみ」）。

### C. ★「不合格の状態が既定である」ことだけは、順序と切り離せる

★現状 `/race` の既定は **v5** で、v5 は ★**オーナー不合格だった `climax-choreography`（馬を動かす第 1 案）を既定として使っています。**
つまり ★**不合格の状態が既定で、合格した v6 が opt-in** です。
★正典 R-27（既定と縮退は狭い側・安全な側に落とす）に照らして、放置すべき形ではありません。

★**これは γ の決定と独立です**（`climax-choreography` は表示位置の演出で、写像とは別の機構）。
★**v5 の既定から `climax-choreography` だけを外す**中間手は、①〜⑤のどこでも実行できます。要指示。

---

## 6. 残件（測定器側）

1. ★`audit-contest-focus.mjs` は **0 コマのとき FAIL に倒す**（§4・R-3 / R-21）
2. ★v5 と v6 を**同じ土俵で比べる道具が無い**。比較するなら「直線区間の全コマ」を見る測定が要る
3. ★測ったのは **seed 1〜80 の直線区間だけ**です。**4 角より手前は今回も見ていません**（引継ぎ書 §5-3 のまま）
4. ★**能力順プール（D-018 準拠）で §2-1 を引き直していません**（§2-0）。`buildAuditRace` は
   プールの並べ替えを受け取れないので、★**`opts.pool` を足すのが素直**です（監査側だけの変更）。
   ⚠️ ★自前に測り直さず、**既存の `buildAuditRace` に口を足す**こと。同じ量を 2 か所で持つと離れます（R-30）
5. ~~★**γ を変えたときの分布を測っていません。**~~ → ★**本便で実施**（§4-2）
6. ★**「画面に入る走路 9.2m」の出どころが追えていません**（§4-4-3）。実測は **5〜6m** です。
   ★`contest-focus.ts` の注記・`CONTEST_MAX_LAG_M` の根拠（「9.2m の窓の 1.5 倍」）が
   ★**この 9.2m に依存しています。** 数字が違うなら **12m** という上限も再検討が要ります
7. ★**「主役2頭以上」に代わる指標が要ります**（§4-4-2）。追い込みを減点しない形。
   ⚠️ ★**指標を自作する前に、既にある実装を探すこと**（R-30）。
   `tools/audit-real-overtakes.mjs`（★**追い抜きの実在**を数える）が近いはずです

---

## 7. 検証

```
測定はすべて読取専用。製品コード・テストとも変更なし（git diff は apps/web/package.json のみ＝オーナーの既存作業）
再現: npx tsx tools/audit-contest-focus.mjs --seeds <1..80 の列挙>
```

★**push はしていません**（CLAUDE.md・オーナー指示待ち）。
