# 【STAR】裁定: `buy_horse` の予行 ✅ ／ 公開の出品一覧に何を出すか（`0085`）— 2026-09-25

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa`（`verify-buy-horse.mjs` 20 件・移行 `0085 horse_market_listing_public` の拡張）

## 1. ✅ 条件 ①② — 受理
- ✔ 台帳と残高の一致・理由 `horse_purchase`・再送で 1 行・出品されていない馬は買えない・EP 不足は `ST001`・★**PP の台帳に 1 行も書かない**（D-102 ①）
- ✔ 🔴 ★**別接続 2 本で「本当に同時」**に投げた（`Promise.allSettled`）。★③ 同じ馬 → 1 件だけ成功・引き落としも 1 件 ④ **別々の馬を同じ人が同時に**（29 頭 持たせて）→ ★**現役 30 頭で止まった** ＝ ★**`0070` で直した穴（29 と数えて両方通り 31 になる）が塞がっていることの実証**
- ✔ サンドボックスで包めない形（別接続）だったので、★**自分で作った馬と口座だけを触り、最後に消して 0 件を確かめた**→ ★跡の始末まで報告している

## 2. ✅ `0085`（名前＋戦績）— **そのままでよい**
- ✔ 根拠が**自分の判断ではなく正典**: ★D-102 ③（2026-09-18 T-11「★**走った実績のある馬だけ買える**」＝戦績が買える条件そのもの）／★D-114（「手がかりは**オッズと戦績**だけ」）
- ✔ ★**価格は §10.5 の式で戦績から出る**（`3,000 + G1勝利数 × 8,000 + 総獲得賞金/20`）。★**価格だけ出して戦績を隠すと、利用者は価格から逆算するだけ**（★開発側の指摘が正しい）
- ✔ 実測: 列は**名前・性・生年・戦績・価格・戻り額**だけ（★`potential`・`speed`・`owner_id`・`display_name` は無い）
- → ★**「名前だけ」にする必要は無い**。★戦績は D-114 が明示的に許した手がかり

## 3. ⚠️ 条件を 1 つ足す — ★戦績の式を**関数 1 つ**に
- ✔ [CODE] `0085:45-46` は `count(*) … finish_pos = 1` / `finish_pos is not null` を**その場に書いている**。★開発側は「**3 か所（`0075`・`0083`・`0085`）を突き合わせる網**」で守ると言っている
- 🔴 ★**突き合わせより、1 つにするほうが強い**。★前例が同じファイルに在る — `horse_total_prize_pp`（`0071:18`）は**関数 1 つ**にして 3 か所が呼ぶ形にした（★私の裁定 `c653606` §3）
- **決定**: ★**戦績（出走数・勝数・G1 勝数）を返す SQL の関数を 1 つ作り、`0075`・`0083`・`0085` がそれを呼ぶ**。★網は「`race_entries` を数えている箇所がその関数の外に無い」を見る形へ（★`sum(prize_pp)` のときと同じ）
- ★急がない（★いま 3 つは一致している）。★**`/stable/market` の結線と同じ便で**よい

## 4. 次の順番 — ✅ そのまま
★`/stable/market` の結線 →（4 つの状態）→ 動いてから入口（D-122 ④）。★`0085` は可なので進めてよい。

## 5. 追記（2026-09-25）— 関数に寄せたら **5 か所** 在った ✅ ／ `enter_race` は寄せる ／ `/stable/market` はデザイナー待ち

### ✅ 受理
- ✔ `0086` で `horse_starts()` / `horse_wins()`（前例 `horse_total_prize_pp` どおり）。`0075`・`0083`・`0085` を寄せ替え。★**本文を機械で突き合わせ、差は 2 行だけ**。★**数字が 1 つも変わらない**ことを実測で確認
- ✔ 🔴 ★**私が挙げた 3 か所の外に 2 か所 在った** — ①`view my_horses`（`0040`・`/entry` が勝利数に使う。★**その註記自身が「`enter_race` と同じ数え方でなければ、画面が『出られる』と言った馬が RPC に弾かれる（CL-4）」と書いている**）→ `0087` で寄せた ②`my_runs`（`0056`・1 走 1 行なので数を返す関数は呼べない）→ ★**「絞り方が同じか」を見る網**にした
- ✔ ★**網の当て方の誤りを自分で直した**: 移行の**全部**に当てたら歴史まで挙がり、★**本物の 1 件が紛れた**。★「**最後の定義だけ**」に絞って初めて見えた。★**移行は追記のみなので、過去のファイルを直す網は間違い**
- ✔ ★**除外の理由が誤っていたので書き直した**（「stable な関数を書き込みの経路から呼ぶと意味が変わる」は Postgres では普通のことで誤り）。★**理由を「寄せるべきだが、まだ寄せていない」に直した**のが正しい（★**誤った理由で除外を正当化しない**）

### ✅ `enter_race` も寄せる
- ★**CL-4 の当事者**（`my_horses` と同じ数え方であることが要件）。★片方だけ寄せた状態は、★**いちばん揃えるべき 2 つが別々の書き方**という形
- **条件**: ①本文を写すので、★**正規化した差分で「変えた行だけ」を示す**（`0082` と同じ作法）②★**出走登録の予行を流し直す**（`verify-entry-*`）③★寄せた後、`my_horses` と `enter_race` が**同じ関数を呼んでいる**ことを網で見る

### ✅ `/stable/market` の結線を止めた判断 — 支持
- ✔ 画面の設計が **T-11 より前**（「N つの値段・それぞれ M 頭・同じ値段ならどの 1 頭でも同じ」）。★実測は **値段がほぼ 1 頭ごとに違う**（式が獲得賞金を含むため）
- → ★**前提が崩れているので、結線すると画面が嘘をつく**。★画面自身の註記も「T-11 でこの画面ごと変わる」と書いている
- ★**デザイナー待ちで正しい**。★**この事実（値段はほぼ 1 頭ごとに違う）をデザイナーへの依頼に 1 行で入れること**（★「同じ値段でまとめる」前提が使えない、と先に言う）
