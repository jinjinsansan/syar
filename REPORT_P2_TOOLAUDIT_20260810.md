# REPORT_P2_TOOLAUDIT — 裁定②③を実装／★受け入れ証拠の一部が「動かないツール」で繰り越されていました

**開発側 → レビュー側** / 2026-08-10
**コミット**: `82ec229` 以降（本番配備済み）

## 本便で正典の改訂が要る決定をしたか

**なし。**

---

## 1. 裁定②③は完了・配備済みです

### ② λ*=30 に差し戻し

```
本番: 1レース 256秒 → 116秒（サイクル予算 43% → 19%）
```

コードに経緯を残しました。★**打ち切りは λ*=30 で説明にならないと分かっていながら、
λ⁻¹ で縮む項（＝打ち切り）で予測した**ので、根拠が最初から成り立っていませんでした。

### ③ 内部口座は「除外」ではなく「別掲」（migration 0009）

```
users.account_type（player / internal・CHECK 付き・既定 player）
point_flow_daily に別掲列5本
```

`daily-flow.ts` は区分ごとに2回集計します。★`users` を内部結合するので、
**結合できない行があると黙って両方から消えます**（「player も internal も 0 だから
経済が動いていない」と読み違える形）。→ **件数で突き合わせ、合わなければ例外**にします。

`verify-flow.mjs` を★**両方向**に拡張し、実 DB で確認しました:

```
① 内部が実利用者側に混ざらない : PASS   ← 除外実装でも別掲実装でも通る
② 内部の流量が消えていない     : PASS   ← ★除外実装なら落ちる
③ 入れ違っていない             : PASS   ← player と internal に違う値を入れて検出
```

★**片方だけだと「口座に印を付けて流量を隠す」実装を検出できません。**

---

## 2. ★資源記録が「正しく実行されて別物を測っていた」件

配備直後のログが **`conn=0`** でした。接続0本のはずがないので調べたところ:

```
select current_setting('application_name')  →  "Supavisor"
```

**Supabase のプーラが `application_name` を上書き**しており、こちらで付けた名前は
Postgres に届いていませんでした。しかも `pg_stat_activity` に見えるのは
**プーラ側の接続**で、ワーカーの接続と1対1になりません。

★**取得失敗（-1）は検出できる作りにしていましたが、成功して誤答する形は防げていませんでした。**

→ プロセス側で**実際に漏れるもの**（`/proc/self/fd` の記述子数とソケット数）に変更。

### 夜間の実測（★1回目で取れなかった証拠が取れています）

```
02:02  mem=158MB heap=34MB fd=35 sock=9
03:07  mem=158MB heap=34MB fd=35 sock=9
04:18  mem=158MB heap=38MB fd=35 sock=9
05:33  mem=159MB heap=39MB fd=35 sock=9
06:43  mem=158MB heap=28MB fd=35 sock=9
```

**rss・fd・sock が5時間まったく動いていません。**heap の揺れは GC のものです。

★あわせて分かったこと: **`ps` の 67MB とワーカー自身の 115MB がずれます**（サンプル時刻）。
  **1回目の A-1 のメモリ推移（95→100→62→69→67MB）は、その粗い道具で取ったものでした。**

---

## 3. ★本題 — 受け入れ証拠の一部が「動かないツール」で繰り越されていました

P2 を閉じる前に証拠を点検したところ、**2件見つけました。どちらも本番を壊します。**

### ① `verify-a2.mjs` — 本番のレースを全件削除する実装でした

```sql
delete from races where cycle_index is not null   -- 44行目
```

**「既存データを汚さないよう後で消す」とコメントしながら、全部消します。**

さらに `createPgStore` / `runCycle` の引数が増えたのに追随しておらず、
**そもそも実行できない状態**でした。

★**つまり A-2 の PASS は、D-037（中止）と D-038（順序入替）を入れる前の証拠のまま
  繰り越されていました。** 私は毎回「A-2 PASS」と表に書いていました。

**書き直して回し直しました:**

```
作成 2（cycle 347464,347465）→ 5回繰り返しても重複0件
★一意制約: PASS（直接ぶつけて確認）
★中止の誤発火なし: PASS
★後片付け: レース総数 210 → 212 → 210・残存0（自分が作った番号だけを消す形に）
★A-2: PASS
```

### ② `verify-a7.mjs` — 実行すると本番ワーカーが起動しなくなります

最後にこうしていました:

```js
// 実運用の宣言に戻す（この DB は development）
insert into app_environment values (true,'development');
```

★**現在の DB は `production` 宣言です。**このスクリプトを流すと、
  **次の再起動からワーカーが起動しません**（A-7 のガードが**正しく働くぶん、確実に止まります**）。
  **「この DB は development」という古い前提が、コメントごと残っていました。**

→ **元の宣言を読んでから壊し、必ず元に戻す**形に変更（異常終了時も・R-18）。

```
（元の宣言: production）
① DB に宣言なし → 起動失敗   ② DB=production / ワーカー=staging → 拒否
③ DB=production / ワーカー=production → OK
★A-7: PASS   ★後片付け: 宣言を production に戻しました
```

### 他のツールも見ました

`verify-a4` / `a5` / `a6` / `verify-cancel` / `verify-exchange` は
**自分のテスト行だけを名前で絞って消しており、安全**でした。すべて再実行して PASS です。

```
A-4（seed_reveal の RLS）: PASS —『races 実体テーブル: 拒否』も確認
A-5（EP減算と発券の原子性）: PASS — 失敗時に EP も馬券も動かない／再送で1回だけ
A-6（自馬ベット制限）      : PASS — 4通りすべて期待どおり
§10.2 開催中止・返還        : PASS — 二重中止で増えない・PP は0（§0.2）
```

★`tools/seed-world.mjs` の `delete from horses` は**意図的に破壊的な投入ツール**なので
  そのままにしましたが、検証ツールと同じ場所にあるのは危ういと考えます。

---

## 4. ★A-8 は再実行が要ります（まだ証拠がありません）

エンジンは P2 の途中で変わっています:

```
1bfebef  2026-08-07  P2: 乱数ストリーム用途IDを1か所に集約
         → packages/sim-engine/src・packages/race-engine/src・測定ハーネスに変更
```

★**その後に P0/P1 のゲートを回した記録が見つかりません。**
  「たぶん通っている」で閉じるわけにいかないので、**`npm run verify` を実行します。**

いま400レースの V-10 測定が走っており CPU を食い合うので、**測定の完了後（10:20 ごろ）**に回します。

---

## 5. 残り

| | 状態 |
|---|---|
| **A-3（400レース）** | **270/400**・完了 10:20 ごろ |
| **A-8** | 測定後に `npm run verify` |
| A-1 | ★**受理済み**（そちらの裁定） |
| A-2 / A-4〜A-7 / A-9 | **PASS**（★すべて現行コードで再実行済み） |

**この2つが出れば P2 クローズの報告を書きます。**
