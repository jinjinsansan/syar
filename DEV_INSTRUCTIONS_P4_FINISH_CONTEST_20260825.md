# 指示書 — ゴール前の接戦（P4）: 比較映像2本の出力まで

発行: 2026-08-25 / レビュー側 → 開発側 / ブランチ `p4/race-30sec-cuts`・基点 `aa52b70`
根拠: `REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md`（照会 `QUESTIONS_P4_FINISH_CONTEST_20260825.md` への回答）
オーナー決定: **γ=1.3 と 1.6 を並べて見てから採用値を決める**

---

## 0. この指示書の範囲（★ここを超えないこと）

**比較映像の出力と報告までです。採用値は決めません。**
本番既定（γ=1.0 ＝ 現行）は**この便では変えません**。

★**カメラ・カット境界・カメラ定義値・素材・台本には一切触れないこと。**
Q-4（カメラ側で「在る競り合いを逃さない」調整）は**採用値が決まるまで着手しない**。
γ が入ると「5馬身以内の頭数」が変わり、カット境界の最適位置も動くためです（二度合わせになる）。

★**`RACE_RANDOM_K` は動かしません。** V-4/V-5/V-6 の帯は維持どころか、定義上触れません。

---

## 1. 何を変えるか — スコア→タイム写像の「形」

現行（`packages/race-engine/src/race.ts:239-256`）は**線形**です。

```
現行:  timeGap_i = (S1 - Si)/S1 × baseTime × TIME_GAP_FACTOR
```

これを、**レースごとの総差を保存したまま内訳だけを変える**形にします。

```
提案:  r_i    = (S1 - Si)/S1                     ← スコア差の比（現行と同じ量）
       total  = r_last × baseTime × TIME_GAP_FACTOR   ← ★現行の「1着-最下位」と完全に同一
       timeGap_i = total × ( r_i / r_last )^γ
```

- **総差が定義から不変** ⇒ ★**V-17② は測り直しても動きません**
- **r_i について単調** ⇒ ★**着順は不変**（V-4/V-5/V-6・人気・払戻は定義上そのまま）
- **γ = 1.0 が現行**

### 実装上の必須事項

| | |
|---|---|
| **I-1** | `TIME_GAP_SHAPE_GAMMA` を `balance.ts` に新設。**既定 1.0**。値の出どころはここ1か所 |
| **I-2** | ★**γ === 1 のときは現行の式をそのまま通す**（短絡）。代数的には同値でも浮動小数の最下位ビットが動きうるため、**既定経路が1ビットも変わらないこと**を実装で保証する（P0-fix5 の核心と同じ規律） |
| **I-3** | `r_last <= 0`（全馬同スコア）のときは**全員 0 秒**（現行と同じ挙動）。ゼロ除算を作らない |
| **I-4** | ★**着差ラベルは同じ写像から出すこと**。現行 `prevGap` は前の馬のスコアから**式を再計算**しています。**全頭の `timeGap` を先に確定させ、ラベルはその隣接差から取る**形に直す。画面の差と記録のラベルが食い違ったら、それ自体が破綻です |
| **I-5** | 比較映像は**道具側で `balance` を差し替えて**出す（`resolveRace({ balance })` は既に受け取れる）。**本番既定に手を入れないこと** |
| **I-6** | 実画面でも見たい場合に限り `/race` に URL クエリを足してよい（`cinematography` と同じ作法: **既定 1.0・不正値は 1.0 へフォールバック**）。★**本番のサーバー確定経路には入れない**（憲法3）。P4 のデモ `/race` に限る |

★`Date.now()` / `Math.random()` は使わない（憲法4）。切り替えは**引数と URL だけ**。

---

## 2. 出力してほしいもの

| | |
|---|---|
| 必須 | **seed 42** で γ = **1.0 / 1.3 / 1.6** の3本（台本は既定 v5・同一条件） |
| 余力があれば | seed 332 / 474 / 14 でも同じ3本（過去の比較で使った4 seed） |

★**同じ seed・同じ台本・同じカメラ**で、**写像だけ**が違う3本にすること。
1本でも条件が混ざったら、どちらの効果か切り分けられません。

---

## 3. 実装前・実装後に確かめること

### 3-1. 既定が動かないこと（★最優先）

- γ=1.0 で `npm test` 全数パス・`npm run typecheck` エラーなし
- 変更前後で、**着順・走破タイム・着差ラベル・`auditSceneAt` のショットと画角**が一致すること
- ⚠️ ★**バイト比較で判定しないこと**。`REPORT_P4_2D_CORNER4_DIRECTION_20260825.md` §5 のとおり、
  撮り直すと同じ条件でもコマがバイト不一致になります。**決定論の数値で比べる**

### 3-2. ゲートが動かないこと

- γ=1.3 / 1.6 で、**全レースの着順列が γ=1.0 と完全一致**することを示す（N=1000 程度で可）
- ★着順が一致すれば V-4/V-5/V-6 は**定義上**不変です（`verify-race.ts` に `timeSec` 参照はゼロ・レビュー側で確認済み）。
  10万レースのゲートを回し直す必要はありません。**着順一致の証拠のほうを出してください**

### 3-3. 解析値と位置モデルの一致（★未確認事項）

回答書の馬身は解析値（`timeGap × 速度 ÷ 2.4`）です。**γ=1.0 では位置モデルと 1.03 倍で一致**することを
レビュー側が確認済みですが、**γ=1.3 / 1.6 は未確認**です。同じ比を実測して報告してください。

### 3-4. 密集の副作用（★これが今回いちばん危ない）

先頭付近が締まると、**2D 素材の重なり・馬番/鞍布の可読性**が悪化しうる（side-v9 の検証で既知）。
γ=1.6 の実画面で、**直線とゴール前の重なり**を必ず見てください。

### 3-5. 記録の整合

`timeSec` は `apps/worker/src/settle.ts` / `pg-store.ts` で保存されています。
写像を変えると**同じ着順でも走破タイムが変わります**。staging/本番に既存のレース記録があるか、
あるなら新旧混在をどう扱うかを**調べて報告**してください（変更はしないこと）。

---

## 4. 参考 — レビュー側の実測（200レース・1600m・12頭・解析値）

| γ | 1着-2着 | 4頭が5馬身内 | 独走(3馬身+) | 総差 p10/p50/p90 |
|---:|---:|---:|---:|---|
| 1.0（現行） | 2.65 馬身 | 31% | 44% | 2.15/2.58/3.73 |
| **1.3** | 1.47 | **54%** | 26% | 2.15/2.58/3.73 |
| **1.6** | 0.86 | **69%** | 19% | 2.15/2.58/3.73 |

★総差の列が全 γ で同一であることが、V-17② に触れていない証拠です。
★**この数字は必要条件であって十分条件ではありません**（俯瞰の件で実証済み）。最終判定はオーナーの目です。

---

## 5. 採用後に来る作業（★この便ではやらない）

1. 正典 §8.7 に写像の式（総差保存形）を追記・§13.2 に **V-17③** を新設（両側の基準）
2. §0.4 に D-0xx（採用 γ と「`RACE_RANDOM_K` は動かさない」旨）
3. Q-4 のカメラ調整を**1項目ずつ**

---

## 6. 報告様式

`REPORT_P4_FINISH_CONTEST_<日付>.md` に、少なくとも次を含めてください。

- I-1〜I-6 の実施状況（★I-2 の短絡を**どう保証したか**）
- 3-1 / 3-2 / 3-3 / 3-4 / 3-5 の実測結果（★数値と file:line）
- 出力した映像のパスと seed
- ★**指示から外れた判断があれば、その旨と理由**（R-8: 逸脱の自己申告）
- 採否は書かないこと（オーナー判定事項）
