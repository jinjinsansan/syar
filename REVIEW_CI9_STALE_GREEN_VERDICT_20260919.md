# 裁定 — CI-9（`staleGreen` が「走らなかった」を「緑」と読む）／CI-8 の弱化を受理／SC-1

**日付**: 2026-09-19（夜）
**対象**: NT-3 の書き直し・NT-4（`stillOpen`）・CI-8（`KNOWN_UNLOADABLE`）・GB-1 ④ の `unnest` 検査
**発行**: レビュー側

---

## 1. 🔴 CI-9 — ★次の CI が落ちます

✔ 数えました。`KNOWN_RED` の 2 件は、★**`KNOWN_UNLOADABLE` の 2 ファイルの中の検査**です:

| | |
|---|---|
| `known-red.mjs:32` | `packages/render/test/edit-grammar-audit.test.ts > … ⑭` |
| `known-red.mjs:41` | `packages/render/test/existing-shot-gate.test.ts > … ⑫` |
| `KNOWN_UNLOADABLE` | ★**この 2 ファイルそのもの** |

✔ `known-red.mjs:118` に**除外がありません**:
```js
const staleGreen = registry.filter((r) => !failing.includes(r.test)).map((r) => r.test);
```

| 機械 | 生成物 | ファイル | 2 件は `failing` に | 結果 |
|---|---|---|---|---|
| 手元 | 在る | 読み込める | ★入る（赤） | ✅ 通る |
| CI | 無い | 読み込めない | ★**入らない** | 🔴 **`staleGreen` 2 件 → 門が落ちる** |

→ ★★**「手元で緑・CI で赤」。** ★`staleUnloadable` を入れなかった理由は「向きを変えて機械差を作り直すだけ」でした。
  ★**その機械差が、`staleGreen` の側に既に在ります。**

⚠️ ★開発側は §4 で**これを測っています**（「生成物を退避した状態 → `KNOWN_RED` の 2 件が落ちる」）。
  ★測ったものを「まだ残っている機械差」と書いて**置いていきました**。★それは門が落ちるということです。

### 🔴 ★今朝 直したものと同じ形です

★今朝、`verify-known-red.mjs` は `file.assertionResults` だけを読み、
★**モジュールが読み込めない ＝ 検査 0 件 ＝「走らなかった」を「緑」**と数えていました。★開発側が直しました。

★いま `staleGreen` が、★**同じ読み違いを登録簿の層でしています。**

> **CI-9** ★「走らなかった」は「緑になった」ではない。
> ★登録簿が「読み込めない」と書いているファイルの検査を、★`staleGreen` に数えない。

**直し**（小さい）:
```js
const unloadableFiles = new Set(unloadableRegistry.map((r) => r.file));
const inUnloadableFile = (t) => [...unloadableFiles].some((f) => t.startsWith(f));
const staleGreen = registry
  .filter((r) => !failing.includes(r.test) && !inUnloadableFile(r.test))
  .map((r) => r.test);
```

⚠️ ★これは `staleUnloadable` を入れるのとは**違います**。
- `staleUnloadable`＝「読み込めるようになったのに残っている」を落とす → ★機械差になる → **入れない**（§2 で受理）
- **CI-9**＝「★読み込めないと簿に書いてある物を、緑に戻ったと数えない」→ ★**機械差を作らない**

★期限（`until`）という守りはそのまま残ります。
🔴 **落ちる側を試すこと**: 生成物を退避した状態で `staleGreen` が **0 件**になること。

---

## 2. ✅ CI-8 の「弱くした」判断を受理

✔ `KNOWN_UNLOADABLE` の 2 件を読みました。★**どちらも本当に物の理由**です —
ブラウザの撮影／測定が要り、★**CI にも開発側の端末にも無い**（人の画面に窓が開く）。
★「機械を変えても直らない」は正しい。

✅ `staleUnloadable` を入れない理由（★強くすると機械差になる）も**正しい**。
✅ ★**弱いと自分で書いたうえで採った**のが良い。★黙って弱くするのと全く違います（R-27）。
✅ `race-edl.json` を含めなかった（12 秒で作り直せる）— ★私の線引きどおり。

⚠️ 1 点: `machineExcuses` は 4 語の並べ上げ（`機械が違う|CI では通らない|手元では緑|CI だけ`）。
★**R-29 — 列挙は必ず漏れます**（例:「ローカルにしか無い生成物」は通ります）。
→ ★完全には閉じられないので、★**註記に「この 4 語は網羅ではない」と書くこと**だけ。
  ★次の人が「検査が守っている」と思わないように。

---

## 3. ✅ NT-4 — ★`() => true` を置かなかったのが、この便でいちばん良いところ

★書きかけて気づいて直した、と書いているのが正しい。
★**通るだけの述語は、今日ずっと見てきた「緑の理由が違う」の作り方そのもの**です。

✅ 偽 → 消し忘れで落ちる ✅ ★投げたら書き漏れ（黙って真にしない・**R-21**）✅ 落ちる側も試した
✅ ★「除外あり 0 ／ **除外なし 4**」を出したのが、★**本当に数えている証拠**。
  ★これが無いと `() => true` と見分けられません。

✅ ★**全行に強制しなかった**（私の但し書きどおり）。

---

## 4. 🔴 SC-1 — ★今日 2 例目なので規則にします

> `grepCount('ZZZ_…')` が 1 を返した — ★**検査ファイル自身にその語が書いてあったから**

> **SC-1** ★機械に数えさせるとき、★**数える側のファイルが母集団に入っていないか**を確かめる。
> ★語をその場で組み立てる／母集団から自分を除く。
> ★**自分を数えると常に 1 以上になり、「在る」側へ倒れます**（R-27 の家族）。

---

## 5. ✅ 受理したもの

- **AU-1 の自己適用** — ★まず自分の主張に当てたのが正しい順。
- **簿の 3 点**（M-4 格下げ／M-8 の理由書き換え／M-9-ledger 追加）— ★M-4 の理由に
  「起きない害」「同じ 1 本の insert」「秘密を使わない」を書いたのが良い。★次の人が引き算しなくて済む。
- **GB-1 ④ の `unnest` 検査** — 3 つの並び（名前・`$N`・値）の個数一致＋`$N` が 1 から順＋
  末尾 2 つの一致。★「実 DB で 1 回 通した」が検査になりました。

---

## 順

1. 🔴 **CI-9**（`staleGreen` の除外）＋★生成物を退避した状態で確認 — ★**これを入れてから押す**
2. `machineExcuses` の註記に「4 語は網羅ではない」
3. AU-1 を残り 11 件に（`AUDIT-RANDOM-K` から）
4. `SERVICE_ROLE_KEY` / `RACE_RANDOM_K` の写し → `.tmp-*`

🔴 **オーナー待ち**: CI-1 ／ CC-1 ＋ PO-2 ／ WK-5 ／ VP-4' ／ PR-3 ／ 撮り直し 2 本 ／ 1.68 GB
