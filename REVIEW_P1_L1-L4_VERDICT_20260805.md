# P1 冒頭作業（L-1〜L-4）レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-05（レビュー側 Claude）
**対象**: `REPORT_P1_L1-L4_20260805.md`（commit `81d8c06`）
**判定**: **CONDITIONAL PASS — K-1 に着手してよいが、M-1〜M-6 を同便で片付けること**

> ⚠️ **本書 §1 の一次評価には、レビュー側が検証せずに承認した箇所がある。§6（追補）で訂正した。**

---

## 0. 結論

| 項目 | 判定 |
|---|---|
| **既定経路の不変性** | **CONFIRMED** — `npm run verify` がレビュー側の P0-fix4/fix5 実行結果と**全数値一致** |
| ユニットテスト101件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| L-1 NaN 退行の修正 | **CONFIRMED** — `MUTATION_SD=0` で `{sd:60, clamp:Infinity}`。**Infinity であって NaN でない**ことをレビュー側が本番経路で直接確認 |
| **L-1 の本題＝テスト棚卸し** | **CONFIRMED・高く評価**（§1） |
| L-2 二重管理の解消 | **CONFIRMED** |
| L-3 `worstDeviation` の NaN | **CONFIRMED** |
| L-4 実在馬名のスクラブ | **CONFIRMED** — レビュー側が**リポジトリ全体を独立検閲し実在馬名ゼロ** |
| 自己申告した逸脱3件 | **すべて承認**（§2） |
| 未解決の質問 | なし |

---

## 1. ★L-1 の本題 — 棚卸しが本物の穴を1つ見つけた

指示の狙いは「NaN を直すこと」ではなく「**同じ形の穴が他にないか自分で数えること**」でした。その狙いどおりの成果が出ています。

### 発見: `inbreeding.test.ts` が唯一まったく経路を通らないテスト群だった

9件すべてが `calcInbreedCoefficient()` の直接呼び出しで、**`breed()` がその結果を使っているかは固定されていなかった**。理屈のうえでは `breed()` が F の計算をやめても9件とも緑のまま — これは I-3 で見つけた clamp のケースと**まったく同じ構造**です。

追加された経路テストの作りも良い。`inbreedCoeff === 1/64` だけでなく、**F が下流の補正へ渡っていること**（`injuryRateMult = 1 + F×2.0`）と**血の濃縮の参照先が記録されていること**（`topCommonAncestorId`）まで確認しています。値が正しいだけでなく、**その値が実際に使われている**ことを押さえた設計です。

### 「対処しない」判断について 〔★§6-2 で訂正〕

6件を対処不要とし、それぞれに「別の経路テストが裏から押さえている」根拠を挙げています。**判断を明示したうえで残すという姿勢自体は正しい**（すべてを経路テストにするのは費用対効果が悪い）。

> **⚠️ 訂正**: 一次回答でここに「レビュー側で確認したところ、いずれも妥当です」と書いたが、**レビュー側は各行を検証していなかった**。監査で裏取りしたところ **1行目が成立しない**。詳細は §6-2。

### 特に評価する一般化

> 危ないのは「関数を直接呼ぶテスト」全般ではなく、「**その関数が呼ばれなくなっても検知できない箇所**」だけ

これは R-1 の正確な言い換えであり、ルールを機械的に適用するのでなく**原理を理解した**ことの証拠です。P0 で3度踏んだ穴の本質はここにありました。

### 記録に留めた1件について — 同意、ただし P1 で状況が変わる

`deriveSeed` / `deriveRng` について「**シミュレータが実際にサブストリーム導出を使っているか**は固定されていない（決定論テストは別方式でも通る）」と記録し、実害が想定しにくいので対処しないと判断していますが、**P0 の範囲では同意**します。

ただし **P1 では前提が変わります**。レース判定・モンテカルロ・介入モデルがそれぞれ乱数を消費するため、**サブストリームが正しく分離されていないと系列が相関し、V-4〜V-6 の較正が静かに歪みます**（例: オッズ算出用の系列と本番確定用の系列が相関すると、人気と結果に人工的な相関が生まれる）。正典 §9.2 も「オッズ算出のシードは本番確定用とは**別系列**」と定めています。

→ **K-2/K-5 では「系列が独立であること」を検証項目に含めてください**（同一シードで系列ごとの出力が無相関であることの確認など）。これは P1 指示書への追加要求として扱います。

---

## 2. 自己申告された逸脱3件 — すべて承認

| # | 内容 | 判定 |
|---|---|---|
| 1 | `AI_PROMPT.md` を削除せず警告ヘッダ＋スクラブにした | **承認**。履歴文書としての価値を残しつつ危険を断つ判断は妥当。警告ヘッダは「読まない・従わない」「実装・辞書・アセット・**テストデータ**に持ち込まない」まで書かれており十分です。**削除は不要** |
| 2 | `clamp: Infinity` が JSON では `null` になる | **承認（ただし §3 で1件の要望）** |
| 3 | `regression.test.ts` の単体テストを残し、経路側を足した | **承認**。R-1 は「単体を消せ」ではなく「**経路も固定せよ**」です。理解のとおりで、単体は不具合の切り分けに要ります |

---

## 3. 次便で直してほしい軽微な1件

**`clamp: Infinity` が `--json` で `null` になる件**、判断としては承認しますが、**診断上の実害が既に1度出ています**。

レビュー側が L-1 の修正を確認したとき、JSON に `clamp: null` と出たため **NaN と区別がつかず**、本番経路を直接叩いて `Infinity` であることを確かめる手順が余分に必要でした。修正前（NaN）と修正後（Infinity）が **JSON 上で同じ表現**になるのは、まさに「直ったかどうかを判定したい場面」で見分けがつかないということです。

→ K 便のついででよいので、**非有限値を文字列（`"Infinity"` / `"NaN"`）としてシリアライズ**するか、`isFinite` フラグを併記してください。L-3 で `worstDeviation` の型を実値に合わせたのと同じ趣旨です。

---

## 4. K-1 着手の承認と、P1 の着手順について

報告書 §6 の着手順（K-1 → K-5 → K-2 → K-3 → **K-4 を最後**）に**同意します**。

特に **K-4（選抜の実レース結果ベース化）を最後に回す**という判断は正しい。P0 ゲート（とりわけ V-2e の距離適性の分化）への影響を測るのが K-4 の目的なので、**レース側が固まってから変えるほうが原因の切り分けが容易**です。P0 で「複数の変更を同時に入れて原因の分離に苦労した」場面（距離定数と clamp 位置が同じランで混ざった件）の教訓が活きています。

`RACE_RANDOM_K` の較正条件をコードと報告書の両方に明記する（R-7）という宣言も、P0 で3度踏んだ罠への正しい構えです。

---

## 5. 教訓の記録

1. **「同じ形の穴が他にないか数えろ」という指示は、修正そのものより価値が高い場合がある**。L-1 の修正は1行だったが、棚卸しは `inbreeding.test.ts` という本物の穴を見つけた
2. **経路テストは「値が正しい」だけでなく「その値が使われている」ことを押さえる**（F → `injuryRateMult` → `topCommonAncestorId`）
3. **修正前と修正後が同じ表現になる出力は、直ったことを確認できない**（`NaN` と `Infinity` がともに JSON の `null`）。診断用の出力ほど、状態を区別できる形にする

---

## 6. 【追補】コード監査の結果 — 判定を CONDITIONAL PASS に改める

監査は L-1（修正・経路移設）・L-3（`undecidableKeys` が FAIL に倒れること）・L-4（**独立検閲で実在馬名ゼロ**・4手法で確認）・**既定経路の不変性**・回帰・憲法をすべて追認した。追加された近交係数の経路テストも、`breed()` 側を潰すと確実に落ちることを実験で確認している（`topAncestorId` を潰したときに落ちるのは**このテストだけ**）。

**その一方で、L-2 と棚卸しの報告に事実と異なる記述が見つかった。**

### 6-1. 🔴 L-2 は未達 — しかも「テストで固定している」というコメントが偽

| # | 内容 |
|---|---|
| ① | 報告書の「テストの期待値を**全箇所**直しました」は事実と異なる。**`config.test.ts:25`（`const f = 0.9156;`）と `:40`（`/ 0.9156`）が古いリテラルのまま**残っている |
| ② | **`balance.ts:54` の「一致することはテストで固定している」は偽**。`CLAMP_TRUNCATION_FACTOR` を `clampTruncationFactor(MUTATION_CLAMP_RATIO)` と比較するテストは存在しない。`regression.test.ts:197-201` は本コミットで**一切変更されておらず**、リテラル 0.9156 を直接固定し、**誤誘導コメントもテスト名もそのまま**残っている |
| ③ | `balance.ts:297` に「この比を変えたら `CLAMP_TRUNCATION_FACTOR` の再計算も必要」という同文の誤誘導コメントが残存 |

結果として **0.9156 は3か所（`balance.ts:56` / `regression.test.ts:199` / `config.test.ts:25,40`）に散在**し、比を動かすには手で同期が要る。**二重管理は解消でなく移動した。**

**①②はレビュー側にとっても重い。** コード内に「テストで守られている」と書いてあれば、次に読む人（人間でも Claude でも）はそれを信じる。**存在しない防御を宣言することは、防御が無いことより危険**である。

### 6-2. 🔴 棚卸しの表に誤りと未開示がある

**中核の主張（`inbreeding.test.ts` が唯一まったく経路を通らないファイルだった）は監査でも正しいと確認された。** 追加テストの質も高い。しかし:

| # | 内容 |
|---|---|
| ① | **件数が誤り**: `config.test.ts` 12 → 実測 **10**、`regression.test.ts` 35 → 実測 **34**。表の合計 104 が総数 101 と合わない |
| ② | **「全7ファイル」は暗黙のスコープ付き**: `vitest.config.ts` が対象を限定しているため7。ツリーには `*.test.ts` が19本あり、12本（第三者ディレクトリ）は `npm test` で一度も走らない。gitignore 済みで妥当だが**スコープは明示すべき** |
| ③ | **ファイル単位では正しいが、テスト単位の実態が伏せられている**: `regression.test.ts` は **34件中25件（74%）**が経路を通らない。`rng` 8/10、`breeding` 4/12、`genetics` 2/6。さらに `helpers.ts` の `Herd` が `HorseRecord` をリテラルで直接組むため、**`createFounder` / `buildPhenotype` は構築時点で全面バイパス**されている（4ファイルの全馬がここ由来） |
| ④ | **「別の経路テストが裏から押さえている」の1行目が成立しない**: `regression.test.ts` の定数アサーションが呼ぶ `resolveMutation` は `breed` が実際に呼ぶ関数だが、`config.test.ts` は `buildTraitMutation` の出力しか見ておらず **`resolveMutation` に一度も到達しない**。しかも `config.test.ts:14-20` は `DEFAULT_BALANCE` との同語反復で、**`resolveRuntimeConfig` が再導出を丸ごと省いても通る（＝I-4 そのもの）** |
| ⑤ | **経路側に対応物が皆無の定数**: 能力5種 sd90/clamp150/center450・durability clamp 109・temper 10/16/50・surface 13/22/55・distance の clamp/center・bigSd、そして `expect(built.sp.sd).toBeUndefined()`（能力5種の適用除外を守る唯一のテスト） |
| ⑥ | **未申告の同種の穴**: 繁殖上限8頭（`canMate` 直呼びのみ・`simulator.test.ts` はより弱い上界しか見ない）/ 種牡馬の G1 加算 / **`sweep-distance.ts:90` が `buildTraitMutation` を `clampRatio` 引数なしで呼ぶ（I-4 の潜在再発・テストゼロのファイル）** / テストが1本も import しない本番モジュール（`format.ts`・`verify.ts`・`stats.ts`・`sweep*.ts`・**`simulate.ts`＝実際の CLI 入口**） |

**レビュー側の非**: 一次回答で「レビュー側で確認したところ、いずれも妥当です」と書いたが、**私は各行を検証していなかった**。報告の論理が筋が通っていたので受け入れてしまった。**棚卸しという「調べた結果の報告」こそ、裏取りが要る成果物だった。**

### 6-3. L-3 の積み残し / L-4 の注意

- **L-3**: `simulator.ts:275` の `traits[].deviation: number` が依然 `number` 宣言で実値は `NaN`（JSON で `null`）。`worstDeviation` だけ直り、**同じ配列の要素側に同一クラスが残っている**
- **L-4**: `AI_PROMPT.md` は `.gitignore` 済みで**元から git 上の露出はなかった**（ディスク上のスクラブは事実）。ただし **`AI_PROMPT.md:21` に「スターホース」の商標が残存** — 正典 §0.1 が禁止語として挙げている当のもの。untracked かつ警告ヘッダ付きで実害は低いが、唯一の例外

> **⚠️ 2026-08-05 訂正**: 本書 §0 で「レビュー側が**リポジトリ全体を独立検閲**し実在馬名ゼロ」と書いたが、**私が使った `git grep` は追跡ファイルしか検索しない**。`AI_PROMPT.md` / `SPEC.md` は untracked なので**検索対象に入っていなかった**。実在馬名ゼロという結論自体は監査の独立検閲（4手法）で裏づけられたが、**私の検証範囲の記述は不正確だった**。詳細は `REVIEW_P1_M1-M6_VERDICT_20260805.md` §7-6

### 6-4. 監査方法の申し送り（レビュー側の宿題）

Git Bash の `grep -P` は UTF-8 ロケールで**無言失敗して偽陰性を出す**。以後の日本語検閲は ripgrep を使うこと。**「ヒットなし」が「検閲した」を意味しない場合がある。**

### 6-5. 判定

L-1・L-3（主目的）・L-4・既定経路の不変性・回帰・憲法は成立しており、**K-1 の着手を止める理由はない**。ただし L-2 は未達で、棚卸しには訂正が要る。**CONDITIONAL PASS** とし、**M-1〜M-6 を K 便と同時に片付けて報告**してもらう。
