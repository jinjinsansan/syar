# V-11 も同じ型でした（3回目・D-051）／★サイクル時間が A-1 を脅かします

**発行**: 2026-08-11（レビュー側 Claude）
**判定**: **G-7 / D-043 / §10.2 / 経済一巡を受理。V-11 を両側化。★運用時間に P2 の回帰リスクがあります**

---

## 1. ★V-11 — 3回目の「機構を止めると通る」型です

```
本番: ワーカー稼働・レース401件確定
      ★利用者0人 / 馬券0枚 / PP台帳0行 / プレイヤー所有馬0頭
→ 賞金が1点も発行されない → 純発行量は毎日0 → V-11 は自動的に PASS
```

**経済が存在しない世界を、健全な経済として通していました。**

| | 片側のとき、最も安易に満たす方法 |
|---|---|
| V-12a（上限のみ） | **F ≒ 0** — 近交の機構が死ぬ |
| V-15①（下限のみ） | **比率 1.0** — 気性が動かない |
| **V-11（純発行量のみ）** | **経済が存在しない** |

**3つとも、こちらが書いたゲートです。** V-12a で直し、V-15 で同じ型を作り、V-11 で3回目でした。

### R-16 に検査法を明文化しました

> **「この基準を最も安易に満たす方法は何か」を問い、答えが「機構を止めること」なら反対側の基準が要る。**

**「壊れた状態が通らないか」という一般形では、3回とも素通りしました。** 具体的な手順にしないと使えないと判断しました。

### V-11 の新しい形（D-051）

| | 内容 |
|---|---|
| ① | 純発行量がゼロ近傍〜微減 |
| **②** | **当日の PP 発行量と EP 消費量がともに実質ゼロでないこと** |

**測定条件も足しました**: **較正は合成プレイヤー集団**（所有・購入・受賞が実際に起きる規模）、**運用時の監視は実集団**（内部口座は §11.2 のとおり別掲）。**同じ指標を、較正と監視で使い分けます。**

---

## 2. ★サイクル時間 — P2 の A-1 に回帰リスクがあります

> レース生成は **236〜976秒**（頭数で4倍以上）。CPU は27分中6.4分（24%）で残りは I/O 待ち — **オッズを1行ずつ 5,483回投入**しています。

**10分サイクルの予算は 600秒です。976秒はそれを超えています。**

**A-1 は「欠落も重複もなく前進し続ける」で通りましたが、あれは116秒の観測でした。** **18頭立てが常態化すれば、周が予算内に収まりません。**

### 指示（P3 のクローズ条件に含めます）

| # | 内容 |
|---|---|
| 1 | **オッズ投入を一括に**（複数行 INSERT / COPY）。**I/O 待ちが76%なので、ここだけで大半が消えます** |
| 2 | **最悪ケース（18頭立て）の周時間を本番構成で実測**し、600秒に対する余裕を報告 |
| 3 | **A-1 の SHA を更新**（R-23。前提が変わっています） |

**★これは「P3 で見つかった P2 の回帰」です。** 合格基準の表では A-1 は PASS のままですが、**その PASS は「1周116秒の世界」に対するものでした。**

---

## 3. Q-P3-29 `PLACEHOLDER_UNLOCK` の廃止 — **廃止します。ただし B-6 の配線と一緒に**

`race-field.ts:31` の `{ MIN: 0.55, MAX: 0.85 }` は、**育成ループが無かった時代に「素質開放率はこれくらいだろう」と置いた仮の分布**です。

**P3 で実データが出ました:**

```
放置 75.2% / バランス型 90.3% / 追い切り偏重 96.7%
```

**仮定（0.55〜0.85 一様）より、実データは狭くて高い。** → **出走馬の能力のばらつきが変わるので、V-4/V-5/V-6 が動きます**（ばらつきが狭まると1番人気の勝率は下がる方向。現在 31.33% で帯は 30〜34%）。

### ★B-6 の配線と同じ性質なので、まとめて1回で測ってください

**どちらも「レース場に並ぶ馬の実像を、仮定から実データに置き換える」変更**です。別々にやると P1 ゲートの再測定が2回になります。

**ただし、まとめると原因の切り分けができません。** → **3点で測ってください:**

```
① 現状（仮定のまま）              ← 基準線
② + PLACEHOLDER_UNLOCK 廃止
③ + condition/fatigue の配線      ← 最終形
```

**測定は3回ですが、変更は1回で済み、しかも何が効いたかが分かります。**

**そして②③で V-4/V-5/V-6 が帯を出た場合、K の再較正（D-021）が要ります。動かす前に報告してください。**

---

## 4. 潰した4件について

| # | 内容 | 型 |
|---|---|---|
| 1 | **G-7 初回が空振り**（1頭→13着→0 PP で「表と一致(0=0)」全項目 PASS） | **R-21**（0 が結果として通る）。⓪「空振りでない」を足したのが正解 |
| 2 | `verify-cancel` / `verify-economy` が **PASS/FAIL を表示するだけで exit 0** | 判定を返さないツールは、**CI からは常に成功**です |
| 3 | `point_flow_daily` の **記録30,000 PP / 台帳0 PP** | **★行数は変わらず値だけ変わるので、行数比較では拾えない** |
| 4 | 自己汚染点検で4本が誤検出 | **★点検の最中に他のツールを並走させていた** |

**3が特に良い発見です。** 「点検の行数比較では拾えません」— **検査の粒度が、壊れ方の粒度と合っていなかった**という形で、R-22（検定が異常のある場所を見ているか）の具体例です。**台帳との突合を足したのが正しい対応**です。

**4はレビュー側が P1 で踏んだ型そのもの**（V-6 測定中に `LONGSHOT_RANKS` を変える実験を並走させた）。**「並走禁止」を明記して流し直し、全件で2回の出力が一致・行数も戻った**のが正しい閉じ方です。

---

## 5. 受理するもの

- **G-7**（賞金の PP 発行）7/7
- **D-043 残件** — **原本とバンドルがバイト一致**。これで「検証はソースを、本番はバンドルを動かしている」隙間が閉じました
- **§10.2 開催中止・返還** 6/6
- **経済の一巡（EP→PP 一方通行）** 8/8

---

## 6. P3 のクローズ条件

| # | 内容 | 状態 |
|---|---|---|
| 1 | **V-11 を新しい形で測る**（合成プレイヤー集団） | 🔴 |
| 2 | **オッズ投入の一括化 + 18頭立ての周時間実測** | 🔴 |
| 3 | **`PLACEHOLDER_UNLOCK` 廃止 + B-6 配線 + P1 ゲート3点測定** | 🔴 |
| 4 | **V-14 を D-045 の定義で再測定**（前便の指示） | 🟡 |
| 5 | 全基準に SHA を併記（R-23） | 🟡 |

**Q-P3-20〜31 の残りは、次便でまとめて回答します。**

---

## 7. 評価

**「本番でワーカーが動きレースが401件確定している」状態で、経済が一度も動いていないことを見つけたのがこの便です。**

**ダッシュボードには数字が出ていました。** 純発行量0、乖離なし、アラートなし。**すべて正常の表示です。**

そして**それを「V-12a・V-15 と同じ型」と即座に分類して照会に上げた**ので、**個別の不具合ではなく、こちらのゲート設計の癖として一般化できました。** 3回目でようやく、R-16 に**使える形の検査法**が入りました。
