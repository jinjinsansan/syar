# 報告 — γ=1.6 確定 / 台本 v6 を既定化 / 先頭が画面の外に出ていた件

作成: 2026-08-28 / ブランチ `p4/race-30sec-cuts`
起点: `35dd21b` → HEAD `70bb063`（12 コミット）
前便: `REPORT_P4_RACE_POLISH_20260828.md` / `QUESTIONS_P4_CUT_SEAM_REOPEN_20260828.md`
受けた裁定: `REVIEW_P4_CUT_SEAM_REOPEN_VERDICT_20260828.md`

> ★`Date.now()` / `Math.random()` を使っていません。着順・タイム・着差・払戻に触れていません（憲法 3/4）。
> ★未 push（CLAUDE.md・オーナー指示待ち）。
> ⚠️ ★`apps/web/package.json`（ポート 3210）はオーナーの既存作業。コミットに混ぜていません。

---

## 0. この便の要点

| # | 内容 | 種別 |
|---|---|---|
| 1 | ★**γ = 1.6 確定・台本 v6 を既定化**（オーナー判断） | 決定 |
| 2 | ★**先頭（自馬）が画面の外に 6.2 秒間いた**。直した | ★不具合 |
| 3 | ★**「9.2m は取り違え」という前便の報告は誤り。取り下げます** | ★自己訂正 |
| 4 | ★**静止画の道具が画面と別の瞬間・別の位置を描いていた**（2.33 秒・8.32m） | ★不具合（道具） |
| 5 | ★**同じ壊れ方が計 38 本**。書式で止める検査を 3 件入れた | ★不具合（道具） |
| 6 | ★**測定器 20 本が「誰も見ていない v4」を測っていた** | ★不具合（道具） |
| 7 | ★**滑らかさの道具がカットの切替を「馬が飛んだ」と数えていた** | ★不具合（道具） |
| 8 | 裁定の宿題 3（γ 確定後の測り直し）を提出 | 宿題 |

---

## 1. ★自己訂正 — 「9.2m は取り違え」は誤りでした

前便で `contest-focus.ts` の **9.2m** について、
★**「画面高の 46% を、馬の高さ 1.6m ではなく体長 2.4m と取り違えた値だ」**と報告し、
コミット `2afc625` で註記を書き換えました。

★**この報告のほうが誤りです。9.2m は正しい数字でした。**

### 原因

私が書いた測定器 `tools/_screenspan.mjs` に、★**馬の高さ 1.6m を直書き**しました。
★描画は `HORSE_HEIGHT_M = 2.5m`（**馬＋騎手の絵全体**）を使います。

★**描画と同じ定数を読まなかった**（R-30）。この 1 行で結論が逆に出ました。

### 描画と同じ定数で測り直した値

```
馬高比            p50 43.8%（1.6m で測っていたときは 26.0%）
画面に入る走路    p50 10.15m
46% まで寄ると    9.63m   ← ★9.2m と一致する
机上（2.5m×46%）  9.66m
机上（1.6m×46%）  6.18m   ← ★私が出してしまった誤りの値
```

★`d89044c` で註記を戻し、**誤った経緯もファイルに残して**あります。

### ★申し送り

★**この失敗は、私が直前まで 9 件つぶしていた R-30 そのものです。**
★測る側に立った瞬間に同じ穴に落ちました。→ §5 で**書式の検査**に落としました。

---

## 2. γ = 1.6 / 台本 v6（オーナー確定）

### 判断材料（実測・seed 42 / v6 / 同じレースの決勝線・2〜5 着との差）

| γ | 2着 | 3着 | 4着 | 5着 | 所見 |
|---|---|---|---|---|---|
| 1.0 | 2.19m | 4.81m | 8.73m | 9.08m | ★勝ち馬が単騎。見せる競り合いが起きない |
| 1.3 | 0.94m | 2.61m | 5.62m | 5.86m | |
| ★**1.6** | **0.40m** | **1.42m** | **3.65m** | **3.84m** | ★上位は競り、後ろには差がつく |
| 2.0 | 0.13m | 0.63m | 2.05m | 2.18m | ★5着まで 0.9 馬身。毎レース大接戦になる |

★**着順は 4 通りとも同じ（10番）**でした。D-064 のとおり写像は単調で、
★**着順・タイム・払戻・人気には定義上触れません**。戻すときは 1 行です。

### ★実装で一番危なかったところ

既定を v6 にすると、★**明示の v5 口が無い限り `?cinematography=v5` が黙って既定（v6）へ落ちます。**
★URL を見ている側は「戻した」つもりになります。
→ `PREVIOUS_RACE_SCRIPT` を追加し、★**「戻せること」を検査で固定**しました。

### ★意図して書き換えた検査 5 件

「既定は v5」を固定していたものです。★**壊れたのを黙って通したのではありません。**

| ファイル | 前 | 後 |
|---|---|---|
| `script-v5.test.ts` ①②③ | 既定 v5 | ★既定 v6 ／ ★`?cinematography=v5` が v5 のままであること |
| `script-v5.test.ts` ⑦ | `scriptOf('')` で v5 の形を見る | ★明示で v5 を指す |
| `script-v6.test.ts` | 「v6 は opt-in」 | ★「既定は v6・v5/v4 へ戻せる」 |

---

## 3. ★先頭（自馬）が画面の外に 6.2 秒間いた

### 何が起きていたか

`straight-contest` のカメラは**競り合っている場所**を見ます。
⚠️ ★**画面の幅を見ていませんでした。**

```
画面に入る走路      p50 10.9m ＝ 注視点の前後 5.45m ずつ
注視点の画面 x      p50 629px（画面幅 1280・中央 640）＝ ほぼ中央
先頭が消える境目    ★5.58m 以上で外 / 5.61m までは写る（重なりなし）
```

★実害（seed 14 / γ1.6）: ★**32.17s〜38.37s の 6.2 秒間ずっと**、
★字幕が「**自馬・先頭 4番**」と出ているのに、★**その 4 番が画面にいません**でした
（残り 185m 付近・先頭の画面 x = 1385px）。

### ★根っこ

`CONTEST_LAG_SOFT_M = 5`（先頭から 5m 後ろまでは満額で数える）という設計値が、
★**画面の半分 5.45m とほぼ同じ**でした。
★**「ここまで譲ってよい」と決めた範囲の端が、そのまま「先頭が消える線」**になっていました。
★5m→12m の減衰区間は**全部が画面の外**です。★設計時に窓の大きさと突き合わせていません。

### ★「主役2頭以上 100%」がこれを隠していた

あの指標は**上位5頭のうち何頭映っているか**しか見ません。
★先頭が抜けていても残り 4 頭が映っていれば満点です。
→ ★裁定の残件「**主役2頭以上に代わる指標**」と**同じ根**です。

### 直し方

`contestFocusWithLeadInFrame` を新設。★下がれる上限を**定数ではなく、そのカメラの px/m から**
求めます（馬群ショットの `halfFrameM` と同じ手・手置きの m 数を使わない）。

★`Math.max` なので**連続**です。このファイルが繰り返し警告している「1 コマの跳び」は起きません。
⚠️ ★「先頭を画面に置く」処理（`broadcastV2LeadFrameFocusMeters`）は使いません。
★あれは先頭を**決めた位置に置きにいく**ので、後方の競り合いを抜けません。

### 実測

| | 前 | 後 |
|---|---|---|
| 先頭が画面の外（γ1.0） | 17.0% | ★**0%** |
| 〃（γ1.3） | 21.0% | ★**0%** |
| 〃（γ1.6） | 12.6% | ★**0%** |
| seed 14（γ1.6） | 51.0% | ★**0%** |
| 主役2頭以上（seed 42） | 100% | 100% |
| 4頭以上（seed 42） | 6.4秒 | ★6.6秒 |
| 注視点が馬から離れる最大 | 0.15m/コマ | ★**0.04m/コマ** |

★**画角は広げていません**（馬を小さくしていない）。★**カットも削っていません**。
★変えたのは注視点が下がれる上限の**決め方**だけです。

⚠️ ★**残る配分の問題**: 先頭と 2 番手の差が 5.76m ある瞬間、窓が 10.4m しかないので
★**両端を完全には収められません**。いまは「**先頭は必ず全身・追走は切れてよい**」に倒しています。
★これは見え方の判断なので、逆の配分もありえます。**オーナー確認済み**（実画面 1 枚を提示）。

---

## 4. ★静止画の道具が画面と別のものを描いていた

★**オーナーへ出す絵と、測定値を秒で照合できない状態**でした（R-30）。二重に壊れていました。

### ① 時間の伸縮を組む基準馬が違った

`knotsFor(boundaries, 1)` の **1 番固定**。
画面（`page.tsx` の `useState(3)`）と監査（`RACE_DEFAULTS.ownGate = 3`）は **3 番**。
`knotsFor` は**その馬の節目を実時間へ寄せる**ので、基準馬が違うと
★**同じ表示秒が別の瞬間を指します**。実測 **2.33 秒**ずれ。
★自馬の印だけは 4 番固定で、これも画面と違っていました。

### ② 発走の遅れ `broadcastV2StartLagM` を引いていなかった

画面も監査も引いています。★**この道具だけが馬を 8.32m 先に描いて**いました。

### 照合（seed 14 / γ1.6 / v6・先頭の位置）

```
表示 12s   道具 227m  ／ 監査 226.67m     ショットも一致
表示 20s   道具 1071m ／ 監査 1071.34m
表示 28s   道具 1322m ／ 監査 1321.69m
表示 35.8s 道具 1454m ／ 監査 1454.27m
```

---

## 5. ★同じ壊れ方の掃き出し（計 38 本）と、書式の検査

★**数え上げは必ず漏れる**（R-29）ので、3 件とも**書式で止める検査**にしました。
★どれも「わざと壊すと落ちる」ことを確かめています（R-16）。

| 壊れ方 | 本数 | 検査 |
|---|---|---|
| `knotsFor(..., 1)` の直書き（基準馬が画面と違う） | **6** | ★`tools/*.mjs` に直書きしたら落ちる |
| γ の既定が画面と違う（数値直書き／エンジン既定） | **8** | ★`DEMO_CONTEST_GAMMA` 以外を既定にしたら落ちる |
| 台本の既定が画面と違う（`'v5'` / `'v6'` 直書き） | **12** | ★`DEFAULT_RACE_SCRIPT` 以外を既定にしたら落ちる |
| 発走の遅れを引いていない | **6**（うち 3 本を修正・§7） | 未（意味の検査になるため） |
| ★`script` を渡していない（関数の既定引数 `'v4'` へ落ちる） | **20** | ★既定引数を `DEFAULT_RACE_SCRIPT` へ（§5-2） |

★特に重いもの:

- `verify-cut-timing.mjs` は**カットの時刻を検証する道具**で、★**画面に無い時刻を検証**していた
- `audit-cut-seam.mjs` は ★**「（画面既定）」と印字しながら v5 を測って**いた
- `audit-contest-focus.mjs` は ★**「γ=1.00（★エンジンの既定）」と印字して測って**いた
- `verify-horse-smoothness.mjs` は**発走の出だしの跳び**を測る道具なのに、発走の遅れを引いていなかった
  （数字が動いた: `start-front` の尖り **1.7倍 → 3.5倍**）


### 5-2. ★測定器 20 本が「誰も見ていない v4」を測っていた

`resolveBroadcastV2Scene` / `broadcastV2ShotAt` / `broadcastV2ShotEndM` などの
★**既定引数が `'v4'`** でした。`broadcast-v2.ts` に
「古い測定道具を巻き込むと範囲を超える」として**意図的に残した**註記があります。

⚠️ ★その結果、`script` を渡していない道具が **20 本**あり、
★**全部が誰も見ていない v4 を測っていました。**（`script` を渡していたのは 1 本だけ）

★**実害**: 前便で「扱いの判断が要る」とお伝えした
`verify-horse-smoothness.mjs` の赤（`side-drive` 尖り 25.5倍）は、
★**v4 の上の赤**でした。オーナーが見ている画の話ではありません。

→ ★既定引数を `DEFAULT_RACE_SCRIPT` へ倒しました（R-31）。★**1 か所で 20 本が直ります。**

### 5-3. ★滑らかさの道具が、カットの切替を「馬が飛んだ」と数えていた

`verify-horse-smoothness.mjs` はキーを `ショットID#枠番` にしていました。
★**v6 は `side-drive` も `straight-contest` も 2 回出ます。**
★すると間の別カットを**飛び越えて前のコマと比べ**ます。

| | 直す前 | ★直した後 |
|---|---|---|
| `straight-contest` | 103.6px / **尖り 1859倍** | ★**2.8px / 52.4倍** |
| `side-drive` | 200.1px / 591倍 | ★**10.9px / 46.7倍** |

★`straight-contest` の 1859倍は**まるごと嘘**でした。
→ ★**連続している区間ごとに番号を振り**、キーに入れる形にしました。

⚠️ ★**残る本物**: `side-drive` で 12番が 7.2s に **10.9px** 跳びます（尖り 46.7倍）。
★v4 でも同じ 10.9px でした。★**台本によりません。** 未着手です。

### 5-4. ★意図して書き換えた検査 4 件（既定が v4 前提だったもの）

| ファイル | 直し方 |
|---|---|
| `broadcast-v2.test.ts` ×2 | ★`script: 'v4'` を明示。検査の本体は「v4 に後方・俯瞰が無いこと」 |
| `fixed-camera.test.ts` ×2 | ★走査範囲を**台本から引く**形へ |
| `cut-transition.test.ts` | ★画角の系統が変わる切替の数を**台本ごとに**固定 |

★`fixed-camera.test.ts` は **800〜1056m（v4 の 4 角）を直書き**していました。
既定が v6 になった途端、★**カットの終わり（966m）を越えて走査**し、画角 14.3% 変化で落ちました。
★**v6 の不具合ではありません。** 実測（各台本の自分の範囲）: v4 3.1% / v5 2.8% / v6 2.8%。

### 5-5. ★v6 は画角の系統が変わる切替が 5 本（v4/v5 は 3 本）

```
 528m  first-corner-front(正面) → side-drive(横)          ★閃光あり
 864m  side-drive(横) → fourth-corner-front(正面)          ★閃光あり
 968m  fourth-corner-front(正面) → side-drive(横)          ★閃光あり
1312m  straight-contest(横) → homestretch-front(正面)      ★閃光なし
1392m  homestretch-front(正面) → straight-contest(横)      ★閃光なし
```

⚠️ ★**増えた 2 本には閃光がありません。**
★閃光を入れるかは**見え方の判断**なので、こちらでは決めません（R-16）。
★事実として検査に固定してあります。→ **Q-5**（§8）

---

## 6. ★宿題の提出（裁定 §6）

### 宿題 3 — γ 確定後に測り直して差分を出す

★**画面の既定（v6 / γ1.6）で測り直しました。**

```
先頭が画面の外            ★0 / 1485 コマ（0.0%）
主役が 2 頭以上           ★4 seed とも 100%
画面に入る走路            p50 10.15m ／ 馬高比 p50 43.8%
注視点が馬から離れる最大  0.00〜0.03m/コマ
```

### 宿題 4 — `straight-contest → homestretch-front` の跳び

| | 前報 | ★今回 |
|---|---|---|
| 最大 | 2919〜3138px | ★**608px** |
| 中央 | — | 301px |

★**先頭を枠に残す変更（§3）で、注視点が大きく振れなくなったため**です。
★別の対処を入れていません。§3 の副産物です。

### ★残る問題 — 4 角の走行方向の反転

⚠️ ★**解決していません。** 閃光で読めるようにしただけです。

```
side-drive → fourth-corner-front        4 seed 中 ★4 で反転（跳び 中央 959px）
fourth-corner-front → side-drive        4 seed 中 ★4 で反転（跳び 中央 950px）
```

★共通の馬が 0 頭の境目は **0 箇所**です（そこは解消しています）。

---

## 7. ★据え置いたもの・報告のみ

1. ★**`verify-horse-smoothness.mjs` は赤です。私の変更前から赤でした。**
   `side-drive` の 12番 / 7.1s で尖り **28.0倍 → 25.5倍**（しきい値 4 倍）。★扱いの判断が要ります
2. ★`audit-race-broadcast.mjs` は据え置き。**古い台本系**（`raceShotAt`）で、
   ショット選びが未補正の位置を使っています。★片手間で直しません
3. ★13 頭以上の勝負服の最小色距離 **27.5**（目安 30 未満）。12 頭立ての現状では出ません
4. ★`CONTEST_MAX_LAG_M = 12` の註記「9.2m の窓の 1.5 倍（=13.8m）」は
   ★**言い方だけが合っていません**（実際の窓は 10.9m とほぼ同じ幅）。値は変えていません

---

## 8. ★照会 — ダート演出の範囲

オーナーから「**次にダート演出も作れますか**」という要望が出ています。
★これは**いまの指示書（P4 のレース映像の品質）の範囲外**なので、着手前に出します。

### すでに動いているもの（実測）

| | 状態 |
|---|---|
| エンジン | `Surface = 'turf' \| 'dirt'`。`surfaceCoef` と `SPEED_SURFACE_MULT` が効き、★**着順もタイムも既にダートの理屈で出ます** |
| 正典 | `surface_aptitude: {turf, dirt}` は遺伝形質として定義済み（§6・V-2e / V-2f の対象） |
| 画面 | 馬場を選ぶ `<select>` がすでにあります。HUD も「ダート1600m」と出ます |
| 砂煙 | `kickupColor`（ダート `#796047`）と `trackKickupIntensity` が broadcast-v2 まで通っています |
| 色 | `palette.json` に `dirt-0`〜`dirt-3` があります |
| 世界 | 内馬場の描画が**すでにダートの周回帯を描いています** |

### 足りないもの（実画面で確認済み）

1. ★**地面がダートにならない** — `world-textured.ts` は `assets.turf` の**芝タイル 1 枚だけ**を敷き、
   馬場の指定を見ていません。★`surface=dirt` で撮っても**地面は芝のまま**です
2. ★**横からの画も芝のまま** — 側面は焼き込み済みの板（`parallaxPlate`）で、**芝が絵に入っています**
3. **砂煙は色だけ** — ダートの見せ場（**砂を被る**・後続がかすむ）は入っていません
4. 勝負服・馬体が汚れません

### ★照会したいこと

- **Q-1** ダート演出は P4 の範囲に入れてよいですか。別便（P4.5 など）にすべきですか
- **Q-2** 正典 §12（年間カレンダー・ダート路線）は未執筆です。
  ★**映像だけ先に作ってよいですか**。それとも路線の定義が先ですか
- **Q-3** ダートコースは**内回り**に置く前提でよいですか
  （いまの `drawInfield` は芝の内側にダート帯を描いています。
  ★ダート戦では馬が**その内側の帯を走る**ことになり、★走路の幾何に触れる可能性があります）
- **Q-5**（ダートとは別件）★v6 で増えた画角の系統が変わる切替 2 本
  （1312m / 1392m・`straight-contest(横) ↔ homestretch-front(正面)`）に
  ★閃光を入れますか。4 角の 3 本には入っています（裁定 Q-1 で 3 条件付きで許可済み）。
  ★実測の跳びは中央 301px / 最大 608px です。
- **Q-4** 素材（地面タイル・側面の板）は `tools/codex-imagegen.mjs` で起こしてよいですか。
  ★承認プレートの扱い（既存の芝タイルは「承認済み」と註記があります）はどうなりますか

---

## 9. 検証

```
npm test        → 122 ファイル / 1213 件 PASS（exit 0）
npm run typecheck → クリーン（exit 0）
```

★新規・改訂した検査:

| ファイル | 内容 |
|---|---|
| `contest-focus.test.ts`（＋6 件） | 先頭を枠に残す／★上限が空回りしていないこと（R-16） |
| `tool-guard.test.ts`（＋3 件） | ★基準馬・γ・台本の既定を書式で止める |
| `fixed-camera.test.ts` / `cut-transition.test.ts` / `broadcast-v2.test.ts`（改訂 5 件） | ★台本を名指しする形へ（§5-4） |
| `script-v5.test.ts` / `script-v6.test.ts`（改訂 5 件） | ★既定 v6 と、★v5 へ戻せること |

---

## 10. 再現コマンド

```powershell
# 見る（dev サーバー: cd apps/web ; npm run dev）
http://localhost:3210/race?seed=14        # ★パラメータなしで v6 + γ1.6
http://localhost:3210/race?seed=14&cinematography=v5   # ★戻す

# 先頭が画面の外に出ていないか（画面の既定で）
npx tsx tools/_screenspan.mjs
npx tsx tools/_leadoff.mjs --seed 14

# カットの境目（合否は出さない）
npx tsx tools/audit-cut-seam.mjs --seeds 42,14,332,474

# γ 別の着差
npx tsx tools/_crossat.mjs
```

★`tools/_*.mjs` は `.gitignore` 対象・追跡外です（登録簿には載せます）。

---

## ★11. 申し送り

1. ★**測る側に立った瞬間、自分が潰していた穴に落ちる。**（§1）
   ★定数は必ず描画側から import する。数値を書いたらその時点で誤り
2. ★**既定を変えたら、その既定を読んでいる全員を数える前に、書式で止める。**（§5）
   ★γ を確定した「直後」に、道具 12 本が古い値を測っていた
3. ★**印字は嘘をつく。**（§5）「（画面既定）」「（★エンジンの既定）」と出しながら別のものを測っていた
4. ★**片側の指標は、抜けているものを見ない。**（§3）
   「主役2頭以上 100%」は、先頭が消えていても満点だった
5. ★**「範囲を超えるので既定は据え置く」は、借金として残る。**（§5-2）
   ★`'v4'` を据え置いた判断が、★**20 本の道具を丸ごと無効にしていた**
6. ★**同じ名前が 2 回出る前提を、道具は持っていない。**（§5-3）
   ★カットの切替を「馬が飛んだ」と数え、★**尖り 1859 倍という嘘の数字**を出していた
