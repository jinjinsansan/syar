# 照会: 馬の「見た目の個体差」が成立していない — 2026-09-23（開発側 → レビュー側）

> オーナー指摘（2026-09-23）:
> 「**全員同じ馬の見た目はユーザーから指摘を受ける** その部分。また **厩舎にいる育成している自分の馬と
> レースに走る馬が同一の見た目か？**」
>
> 絵を焼く作業の途中で、**推測で埋めずに止めました**。以下は測った事実と、判断を要する 4 点です。

## 0. 要点

個体を見分ける仕組みは正典に 2 つ在ります（アートバイブル §3）。**両方とも、いま効いていません。**

| 仕組み | 正典の決め | 実装 | 状態 |
|---|---|---|---|
| 勝負服の配色 | 「★個体識別は勝負服の配色」 | 初回設定で選ばせ `users.silk_color` に保存 | 🔴 **読むコードが 0 件** |
| 毛色 5 種 | 「馬 ID から決定的に導く」 | `coatNameOf(gate) = COATS[gate % 5]` | 🔴 **枠番から導いている** |

## 1. 測った事実（すべてコードで確かめた）

### F-1 利用者が選んだ勝負服は、どこからも読まれていない

- 保存側: `db/migrations/0031_setup_account.sql:69` が `users.silk_color` を足し、
  `create_account` が `p_silk_color` / `p_silk_sleeve` を書く。`/setup` は 16 色を選ばせる。
- 読む側: **`silk_color` を読むコードは、保存の経路を除いて 1 件も無い**
  （`apps/web/src` / `apps/worker/src` / `packages` を検索して 0 件）。
- レースの勝負服は `silkRoleOf` などで **枠と頭数から**決まる（`apps/web/src/app/race/page.tsx:40`・`:196`）。

→ **利用者が選んだ色は、一度も画面に出ていない。**

### F-2 毛色は「馬」ではなく「枠番」から決まる

- `tools/lib/dress.mjs:31`: `export const coatNameOf = (gate) => COATS[gate % COATS.length]`
  （`COATS = ['kage','kurokage','kuri','ashi','ao']`）。
- アートバイブル §3: 「★ただし毛色は持たせる … 5種程度を、**馬IDから決定的に導く**」。

→ **同じ馬でも、枠が変われば毛色が変わる。** 正典との食い違い。

### F-3 `horses` に毛色の列が無い

- 2026-09-23 のデザイナー第 1 便の回答 §3 で「いま毛色のデータは無い」と伝えたとおり。
- `my_retired_horses`（`0074`/`0075`）でも毛色は返していない。

### F-4 育成の馬とレースの馬は、同一の見た目ではない

| | 育成 `/train` | レース `/race` |
|---|---|---|
| 絵柄 | デフォルメ（`train-body-idle/run`・2026-09-23 に焼いた） | 実馬寄りのスプライト |
| 騎手 | 乗っていない | 乗っている |
| 毛色 | 1 種（全頭同じ） | 枠番で 5 種 |
| 勝負服 | 無い | 枠番で決まる |

→ 持ち馬が何頭いても、育成の画面では**同じ 1 枚**。見分けは名前だけ。

## 2. 照会（推測で埋めていません）

### Q-1 🔴 毛色の出どころを「馬 ID」に直すか（正典との食い違い）

アートバイブルは馬 ID、実装は枠番です。**どちらに揃えるかはレビュー側の裁定事項**と考えます。

- 馬 ID に直す場合、`horses` に列は要りません（ID から決定的に導けます）。
  ただし **NPC も含む全頭の見た目が変わります**（本番の中継の絵が変わる）。
- ⚠️ 開発側の見立て: **同じ馬が毎レース別の毛色になるのは、個体識別として機能しません**。
  ただし「毛色は枠の見分けのため」という別の意図が在るなら、そちらが正しい可能性があります。
  **枠番にした理由が書かれた文書を見つけられませんでした**（D 番号を探しましたが当たりません）。

### Q-2 🔴 利用者が選んだ勝負服を、どこで出すか

いま保存だけして使っていません。少なくとも次のどれかが要ります。

- (a) **自分の馬が走るときは、その利用者の勝負服で描く**（枠の色は帽子・ゼッケンで担保）
- (b) 育成・厩舎の画面でだけ出す（レースは枠のまま）
- (c) 選ばせるのをやめる（初回設定から外す）

⚠️ (a) は **中継の見え方が変わる**ので、デザイナーの意見も要ると思います。
⚠️ (c) を選ぶなら、**すでに選んだ利用者の値をどうするか**も決めが要ります。

### Q-3 育成の画面で個体差をどこまで出すか

毛色を馬 ID から導けるなら、**1 枚の絵の色を差し替える**形で育成にも出せます
（アートバイブル §3「共有スプライトのパレット差し替えで作れるのでアセット量は増えない」）。

- 開発側の見立て: **育成の絵は 1 枚のまま、毛色だけ差し替える**のが安い。
- ⚠️ ただし `train-body-*` は**セル調の陰影を持つ絵**で、レースのスプライト用に作った
  `dress.mjs` の量子化（4 階調に置き換える方式）がそのまま効くかは**測っていません**。

### Q-4 育成の馬とレースの馬を、同じ絵柄にするか

いまは別の絵柄（デフォルメ／実馬寄り）です。オーナーは「デフォルメのキャラクターに揃えたい」方向ですが、
**レースの走行コマ数百枚の焼き直し**になるため、範囲の決めが要ります（別途オーナー判断待ち）。

## 3. 開発側が勝手にやらないこと

- 正典（アートバイブル）の書き換え … **レビュー側の仕事**（2026-09-23 の絵柄の決定も同じ扱いにしました）
- `coatNameOf` の変更 … **本番の中継の見た目が変わる**ため、裁定を待ちます
- 勝負服の結線 … 同上

## 4. 参考: この照会の前にやったこと（2026-09-23）

デフォルメで焼いて画面に入れた素材（**個体差は持たせていません。全頭同じです**）:
`train-face-{happy,normal,tired}` / `train-body-{idle,run}` / `intro-birth-cut`。
絵柄の決定は `DESIGN_ART_STYLE_DECISION_20260923.md`（オーナー決定）。
