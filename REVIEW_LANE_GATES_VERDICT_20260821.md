# `LANE_REVEAL_FULL_RUN = 0.18` の裁定 — 条件付きで確定

**発行**: 2026-08-21（レビュー側 Claude）
**対象**: `REPORT_LANE_SPREAD_GATES_20260821.md` / `7cff0ad`
**判定**: **★「全量で測るか、保留か」の二択が誤りです。V-17 と V-1 は性質が違います。V-17 は今すぐ安く測れ、V-1 は測る必要がありません。V-17 が通れば 0.18 を確定します**

---

## 1. ★二択の立て方が違います

> この差分をもって 0.18 を確定してよいか、それとも **V-17 / V-1 を全量で測るまで保留か**

**この2つを同じ籠に入れたことが、5時間という見積りを作っています。** 定義を見ると別物です。

| | 正典の定義 | 何の量か | 全量が要るか |
|---|---|---|---|
| **V-17** | 走破タイムの分布（§8.7）: ①勝ち時計 93〜101秒 ②**1着と最下位の差 2〜4秒** | **★1レースごとの統計** | **要りません** |
| **V-1** | **同一配合100回の potential 分布** — 能力別 CV の平均 12〜18% | **★配合（遺伝）の統計** | 要る（が、下記のとおり測る必要がない） |

### V-17 は「レース数」が要るだけで、「世代数」も「繁殖牝馬数」も要りません

**あなたは既に 12,000 レースを2回走らせています。** V-17 はそこから直接出ます。
**40年 × 400頭 × 60,000レース は V-1/V-2 系（世代をまたぐ量）のための条件**であって、**1レース内のタイム差には関係ありません。**

→ **V-17 を、今の縮小条件のまま測ってください。数分で出ます。**

---

## 2. ★V-1 は測らなくて構いません — **上流を既に測っているからです**

**私が V-1 を必須にした理由は1つでした**:

> K-4 で**選抜が実レース結果ベース**になっているので、雑音が増えれば**選抜圧が鈍る**

**その「選抜圧が鈍る」を直接測る指標が、V-4 です。**

**V-4 = 1番人気（＝強いと予想された馬）の勝率。** これが **32.24% → 32.23%（−0.01pp、標準誤差の 1/40）** ということは、**能力と着順の対応が変わっていない**ということです。**選抜圧は動いていません。**

**V-1 に至る経路は「選抜圧が変わる → 集団の遺伝的分散が変わる → 同一配合の CV が変わる」しかありません。** **上流の1段目が動いていない以上、下流は動きようがありません。** しかも V-1 は世代をまたぐ二次効果なので、**仮に微小な変化があっても、40年シミュレーションの実現間分散に埋もれます**（D-028 で「有効系統数の閾値が分布の中央にあった」と分かったのと同じ問題）。

→ **V-1 を、この変更のために単独で回すのは、同じことを桁違いに高い費用で、より大きな雑音とともに測る行為です。採りません。**

**★ただし P4 をクローズするときの全量実行には含めてください**（他の項目と同じ便で・R-23）。

---

## 3. ⚠️ 「変更後の帯の余裕」は、余裕として使えません

報告 §2 の表:

| | 下端まで | 上端まで |
|---|---|---|
| V-4 | 2.23pp | 1.77pp |

**これは縮小条件での絶対値です。** あなた自身が §3 で「`pool-generations` が V-4 を約 0.75pp 動かすので**絶対値は全量とずれます**」と書いています。**その注意は、差分だけでなく余裕にも掛かります。**

**★差分は同一条件どうしで成立します（あなたの主張は正しい）。しかし余裕は絶対値なので、条件が変われば動きます。** 1.77pp の余裕は、0.75pp 動く条件差の前では**確定した余裕ではありません**。

→ **この表は「縮小条件での参考値」と明記してください。** 出荷既定での余裕は、**P4 クローズ時の全量実行で1回確かめれば十分**です（この変更専用の実行は要りません）。

---

## 4. 実装と掃引 — 承認します

| | 評価 |
|---|---|
| `base` / `SETTLE_M` / `RAIL_W` を触っていない | **裁定どおり。ここが本件の核でした** |
| 先に掃引してから値を決めた | **正しい順序**（値を決めて後から正当化していない） |
| **0.35 以下でばらつきが飽和 → 0.18 は 0.35 に対して分散の追加コストがゼロ** | **★これが 0.18 を選ぶ最良の理由です。** 崖の上ではなく**平地の上**にいる。近傍で値が動いても挙動が変わらないので、**較正として安定** |
| 距離ロスのばらつき +74% でも V-4 が動かない | **理由の説明も正しい** — 散らし方が**枠に対して単調でない一様乱数**なので、**誰かに偏らない。ばらつきが増えても偏らなければ人気馬の勝率は動かない** |

### V-18 の対照テスト — ★特筆します

> 却下された形（枠を 5% 残す）を注入 → **0.132 が出て落ちる**（記録されている実測値 **0.127** をほぼ再現）

**これは H-1 で標準とした形そのものです**（「限定側が発動しない ＋ 非限定側は発動する」）。**「悪化していない」だけなら、指標が死んでいても同じ結果になります。** **落ちるべきものが落ちることを示したので、この 0.011 は信用できます。** そして**過去の実測値を再現したことが、測り方の正しさの裏づけ**になっています。

### 自作指標の誤り（0.473 → 0.013）の自己申告

**適切です。そして一般則としてはこうです**:

> **★指標を自作しない。既にある実装（`laneExtraM`）を使う。**

**この案件で「同じ量を2か所で持って離れた」のは5回目です**（`CLAMP_TRUNCATION_FACTOR` の二重管理／生成側と確定側で別々に引いたレース条件（D-052）／`loadTrainingStates` の絞り込み／D-079 で禁じた★の算出量／今回）。

---

## 5. 🔴 「監査道具が画面と別のものを測っていた」が3回 — 明文化します

> 古い素材／横視点用の背景／表示時間の固定値。**3回目はオーナー指摘がなければ誤報のままでした**

**この案件の慣行どおり、3回再発したので恒久ルールにします。**

> **R-30: 測定器が「評価者が見ているものと同じ入力」を読んでいることを、測定器自身の検査で固定する。**
> 測定器と製品が別々に入力を選ぶ形を残さない（R-29 の「既定を閉じる」・D-052 の「条件は1か所で引く」と同じ構造）。
> **★測定器の誤りは、結果の誤りとして現れず「正しく見える誤報」として現れる**ため、通常の確認では発覚しない（R-16 の家族）。

**私自身、同じ失敗を昨日しています** — 公開ビューに存在しない列を指定して 400 を受け取り、**「サイトが壊れます」と報告する寸前でした**。**両側で同じ形を踏んでいます。**

---

## 6. 判定

**★V-17 を縮小条件で測り、①勝ち時計 93〜101秒 ②1着と最下位の差 2〜4秒 の両方が帯内で、かつ変更前との差分が小さいことを確認できたら、`LANE_REVEAL_FULL_RUN = 0.18` を確定します。**

- **帯を出た場合**は報告してください。そのときは `TIME_GAP_FACTOR` の再較正（D-064 の手順）で吸収できるはずです — **`TIME_GAP_FACTOR` は表示と記録にしか影響せず、着順・V-4/V-5/V-6・払戻に触れません**（D-064）。**つまり V-17 が外れても、それは 0.18 を否定する理由になりません。** 写像を直せば済みます
- **V-1 は回さないでください**（§2）
- **全量実行は P4 クローズの一環で1回**（§3）

---

## 7. 併せて — 2点

### ① ナレーターの写真撤去 — 確認しました

**影絵への差し替えと検査つき、裁定どおりです。** §17.6 に正式な立ち絵の権利処理（生成物か委託か・実在人物をモデルにしない）を項目として足します。

### ② ★「後方・俯瞰は 5 戦 5 敗」

**5連敗は偶然ではありません。** 併走案件（Seven Days Derby）で確立した教訓がそのまま当てはまります —

> **品質がアート実装に依存するものを避け、情報設計に依存するものを選ぶ。3回失敗した箇所は、実装前にオーナーが承認できるレンダリング済みモックを挟む。**

→ **6回目を試す前に、その画角を落とす判断をオーナーに諮ってください。** 「合格したカットだけで本編 34.4 秒を再構成した」のは正しい方向です。**足りないぶんを、負け続けている画角で埋めようとしないこと。**

---

## 8. 次

**⑥⑨⑩の再判定と⑧の内ラチの照会がオーナー待ち、というのは妥当な区切りです。**
**⑧（内ラチ）の照会文は、こちらではまだ見ていません。** 発行済みであればファイル名を教えてください。**未発行なら、この裁定の §2（内ラチ有利は変わらない・変わるのは分散）で足りているかを確認してください。**
