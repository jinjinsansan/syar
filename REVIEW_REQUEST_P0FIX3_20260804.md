# P0-fix3 レビュー依頼 — 開発側 → レビュー側

**発行**: 2026-08-04（開発側 Claude）
**対象**: `DEV_INSTRUCTIONS_P0FIX3_20260804.md`（H-1〜H-4）
**成果物**: `https://github.com/jinjinsansan/syar`（private）/ branch `main` / commit `9f4f970`

---

## 0. 結論

合格基準12項目（既存8 + 追加4）すべて PASS です。

| 合格基準 | 実測（4シード × 100/300年・800頭） | 判定 |
|---|---|---|
| 1. V-1 が 12〜18% | 100年 15.46〜16.76% / 300年 14.88〜16.23% | PASS |
| 2. V-2a / V-2b / V-2c | 傾き -0.01〜+0.09%/世代 / 天井 52.9〜59.2% / 300年V-1 14.88〜16.23% | PASS |
| 3. V-2d | 最大乖離 **-4.26%** | PASS |
| 4. V-2e | 全6形質 **0.84〜0.96倍** | PASS |
| 5. V-3 | 全シード PASS | PASS |
| 6. テスト・typecheck・`any`・定数固定 | **82件** / exit 0 / ゼロ / 固定済み | PASS |
| 7. 決定論 | 同一設定・同一シードで JSON バイト一致 | PASS |
| 8. 4シード × 100/300年 | 8ランすべて PASS（総合 PASS・170.8秒） | PASS |
| **9. 大物覚醒が能力5種のみ** | 発動率100%でも非能力形質は0件 | PASS |
| **10. V-2d が P0-fix2 比で縮小** | 最大乖離 **7.22% → 4.26%** | PASS |
| **11. clamp の式がテストで固定** | 純関数化し、両形式が異なる入力で固定 | PASS |
| **12. V-2e が全6形質 0.8〜1.4倍** | **芝適性 0.71倍 → 0.92倍** | PASS |

---

## 1. 独立再実行の手順

```bash
git clone https://github.com/jinjinsansan/syar.git star && cd star
npm install

npm test              # ① 82件
npm run typecheck     # ② exit 0
npm run verify        # ③ 受け入れ判定（約3分）
npm run simulate -- --generations 300 --seed 42 --population 800 --stallion-pool 240
                      # ④ V-2d / V-2e の形質別表
```

---

## 2. H-2（Q2 の撤回）について — 撤回の根拠に納得しています

3つの根拠のうち、**平衡式が AR(1) を前提とする以上、合計に clamp をかけると sd の導出根拠そのものが失われる**という指摘が決定的だと理解しました。私が P0-fix2 で「正典の式どおり」に直したときに、その式が自分の使っている導出と整合するかを確認していませんでした。

**実装は元の形に戻し、あわせて式を固定するテストを追加しました。**

ご指摘のとおり、従来の回帰テストは全件 `MUTATION_SD = 0` で走っており、その条件では両形式が同値になるため**旧実装に戻しても82件全部 PASS**します。`mutateAllele(value, gaussianDraw, mutation, isBig)` という乱数を引数で受け取る純関数に切り出し、**両形式が異なる値を返す入力**で固定しました。

| 入力（値900・中心450・回帰0.2・clamp150） | 正しい式 | 誤った式 |
|---|---|---|
| ノイズ +200 | **960** | 1010 |
| ノイズ -300 | **660** | 750 |
| ノイズ 0 | **810**（引き戻し −90.0） | 823.5（−76.5） |

「ノイズが clamp 内なら両形式は同値」であることも別テストで明示し、**なぜ従来のテストでは検出できなかったか**をコードに残してあります。

---

## 3. 自己申告が1件あります

**H-3 の副作用で `MUTATION_SD = 0` が非能力形質を止めなくなりました。** 非能力形質が `traitMutation[key].sd` に絶対値を持つようになったためです。P0-fix2 までのテストはこれで変異を止めていたので、そのままだと「回帰項だけを見る」意図のテストにノイズが混ざります（実際に3件落ちました）。

**ご指摘いただいたトートロジーの裏返し**です。全形質の sd を明示的に 0 にする `silenceMutation()` ヘルパへ集約し、docstring に理由を残しました。仕様変更ではなくテスト手法の話ですが、「テストが黙って無力化される」という同じクラスの問題なので申告します。

---

## 4. 判断をお願いしたい1件（報告書 §6 Q1）

**V-2e の実測が下限 0.8 寄りです（0.84〜0.96倍）。**

正典 §6.4 は「clamp 切断で理論値の約0.92倍」と見込んでいますが、実測はそれより低く、気性は 0.84倍と下限まで 0.04 しかありません。差分は**有限集団の遺伝的浮動**によるものと見ています（clamp 切断だけなら 0.92倍のはず）。800頭でこの値なので、より小さい集団や更に長い horizon では 0.8 を割る可能性があります。

対処するなら sd を切断分だけ割り増す方法（`÷ 0.92` 相当）がありますが、**正典 §6.4 の表の値そのものが変わる**ため私の判断では行っていません。

- (a) 現状のまま（ゲート内なので許容）
- (b) sd に切断補正を入れて平衡を 1.0倍付近へ寄せる（§6.4 の表を更新）
- (c) V-2e の下限を見直す（オーナー承認事項）

---

## 5. V-3 の分母定義が変わりました

H-1 により**大物覚醒の分母が「11形質 × 産駒数」から「能力5種 × 産駒数」へ**縮みました（非能力形質ではロール自体を行わないため）。発生率の意味と目標値 1.5%±0.5pt は不変です。隔世遺伝（6%）と大突然変異（0.8%）の分母は全11形質のままです。

なお乱数の消費順が変わるため、**P0-fix2 と同じシードでも結果は一致しません**。決定論は同一設定・同一シード内で成立しています（MD5 一致を確認済み）。

---

## 6. V-2d の改善（合格基準10）

ラチェットを断ったことで、乖離の**向きが上方から中立へ変わり絶対値も縮小**しました。

| 形質 | P0-fix2 | **P0-fix3** |
|---|---|---|
| surface.turf | +7.22% | **-4.26%** |
| temper | +3.88% | **-1.53%** |
| distance_center | +2.18% | **-0.78%** |
| distance_range | +2.17% | **+1.52%** |
| surface.dirt | +1.78% | **-1.05%** |
| durability | +0.41% | **+0.93%** |
