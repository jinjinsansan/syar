# 報告: デザイナーの納品を受けて実装（D12-2・D12-3）

- 日付: 2026-09-16
- branch: `p4/race-30sec-cuts` ／ 開始時 HEAD: `8221eee`
- オーナー指示: 2026-09-16「デザイナー側は途中まで完了しています　シンクしてください」
- 根拠: 正典 **D-101**（調教は週のまま・画面は写像だけ）／デザイナーのカード `components/training-result`

---

## 0. 受け取ったもの（同期で確認）

★**R-12 は D12-1〜6 の全件完了**、★**R-13 は D13-1・3・4 完了**（残り D13-2）でした。

| カード | 依頼 | 状態 |
|---|---|---|
| `components/training-result` | D12-2・D12-3 | ✔ **この便で実装** |
| `components/horse-story` | D13-1・D13-3・D13-4 | ⏳ 次 |
| `components/stable-roster` | D12-6 | ⏳ 次 |
| `components/jockey-market` | D12-4・D12-5 | ⏳ 次 |
| `horse-detail-mobile`・`race-entry-mobile`・`records-mobile` | ★`group="Web"`・**依頼と別系統**（既存 PC 画面のモバイル改訂） | ★紐づけを照会 |

---

## 1. ★実装の前に見つけた穴 — 「乱数が画面から見えない」

★デザイナーの実装表は「**乱数 1.13 以上 → GREAT**」と段を決めています。
⚠️ ★ところが ★**その乱数は週の記録（`WeekLog`）に載っていませんでした**。

★このまま画面を作ると、伸び幅から
`BASE_GAIN × menuCoef × 成長曲線 × 気性 × 調子 × headroom` を**画面側で再計算**することになり、
★**二重帳簿**（D-052）になります。

→ ★**`grow` が既に引いていた値を、そのまま週の記録に載せました**（★新しい抽選は足していません・D-101）。

| 変更 | 中身 |
|---|---|
| `growth.ts` | `growWithOutcome()` を足し、`grow()` はそれを呼んで `next` だけ返す（★計算を 2 本持たない） |
| `week.ts` | `WeekLog.gainJitter`（★成長しなかった週は **`null`**。★0 や 1 で埋めない — 「引かなかった」と「1.0 を引いた」は違う） |
| `view.ts` | `trainingResultTierOf()`・`TRAINING_RESULT_THRESHOLDS`・`trainingStreakOf()` |

## 2. D12-2（調教の結果の演出）

- 3 段（GREAT／UP／通常）。★**境目は画面に持たせません**（`trainingResultTierOf` を呼ぶだけ）。
- ★**割合を実測して回答しました**: `rng.range` は一様・`GAIN_JITTER` は 0.85〜1.15 なので
  ★**GREAT 6.67%（15 週に 1 回）・UP 10.00%（10 週に 1 回）・通常 83.33%**。
- ⚠️ ★**「3 週続けて」はほとんど出ません** — UP 以上が 16.7% なので **0.46%（約 216 週に 1 回）**。
  ★現役 182 週では 1 回も出ない馬が多数です。★**2 週（2.8%・36 週に 1 回）にするか**をデザイナーへ照会しました。
  ★`trainingStreakOf(recent, need)` は ★**週数を引数に取り**、決め打ちしていません。

## 3. D12-3（出走の前の週・後の週）

⚠️ ★**4 値のうち 2 値が構造的に出せませんでした。**
★画面は `raceWeekMarkOf(nextRace === null ? null : 1, null)` と呼んでおり、
★**`race-week`（今週出走）と `after-race`（出走あと）が一度も出ない**状態でした。

→ ★デモ馬に **週数 2 つ**（次走まで・前走から）を持たせ、★判定は `@star/training` の 1 か所に任せました。
→ ★4 値がすべて出るよう 6 頭に割り当て、★色分け（金・青・緑・印なしはテキスト）をカードどおりにしました。

⚠️ ★**PC 版は `tone="gold"` 固定**で、どの印でも金でした（★第 1 便で私が入れたまま）。★同じ 1 か所から引く形に直しました。

## 4. ★自分で見つけた誤り 2 件（この便）

| # | 中身 |
|---|---|
| ① | ★**検査の書き方が雑**でした。`String(1.10)` は **`"1.1"`** になり、★デモ値 `1.142` の一部に部分一致して落ちました。★**実装は正しく、検査が誤り**。→ 数値リテラルを拾って**値として**比べる形に直しました |
| ② | ★デモの値を **`1.142`・`1.115`** と「境目のすぐ上」に置いていました。★これは ★**画面が境目を知っているのと同じ**で、境目を動かした日にデモだけ意味が変わります。→ ★`GAIN_JITTER` の**範囲の代表点**（上端・中央・下端）から作る形に直しました |

## 5. 検査

| 対象 | 結果 |
|---|---|
| `npm run typecheck` | ✔ エラー 0 |
| 新規 | `training-result`（6）・`training-result-wiring`（6） |
| 既存 | `packages/training` 147 ／ `training-view-wiring` 5 ／ 較正の登録簿 5 — すべて緑 |

`apps/cli/src/calibration.ts` に `TRAINING_RESULT_THRESHOLDS` の免除を追加（★較正定数ではなく「良い報せの頻度の刻み」。★伸びそのもの＝V-14 は 1 ビットも動きません）。

## 6. ★次にやること

| # | 中身 |
|---|---|
| 1 | D13-1（馬物語帳）— ★staging に実データ 69 行があります |
| 2 | D12-6（持ち馬 30 頭と厩舎の格） |
| 3 | D12-4・D12-5（騎手・馬市場） |
| 4 | ★デザイナーへの照会の返事待ち: **「2 週か 3 週か」**／在庫 0 口の見せ方／`group="Web"` 3 枚の紐づけ／D13-2 は「続けてください」と回答済み |
