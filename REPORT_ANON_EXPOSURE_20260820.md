# 報告（anon 露出の全数確認 ＋ 本番ビルドの再診断）— 2026-08-20

**宛先**: レビュー側／オーナー
**発行**: 開発側
**対象**: `REVIEW_PROD_MIGRATION_INCIDENT_VERDICT_20260820.md` の §6-4（ビルド確定）と §6-6（anon 露出）
**新設ツール**: `tools/verify-anon-exposure.mjs`（READONLY・`select` のみ）

---

## 1. 🔴 anon 露出 — **中核は守られています。ただし7テーブルが無防備です**

**staging と本番の両方で確認しました**（本番は裁定どおり1回のみ・読み取り専用）。**結果はほぼ同一**です。

### ✅ 正典の中核は無事

| 対象 | 結果 |
|---|---|
| **`races.server_seed`**（§8.6 Provably Fair） | **拒否**（`permission denied for table races`） |
| **`horses.potential`**（§12.4 本人にも数値を見せない） | **拒否**（`permission denied for table horses`） |
| **`horses.genotype`**（§5.5 生値を送らない） | **拒否** |

**レビュー側が §5 で最も懸念した2点は、どちらも開いていません。**

### 🔴 ただし7テーブルが無防備です

**★「0行だった」を合格にしていません。** なぜ0行なのかで分けました。

| 区分 | テーブル | 意味 |
|---|---|---|
| **✅ 保護されて0行** | `bets` / `ep_ledger` / `pp_ledger` / `prize_exchanges` | RLS 有効＋ポリシー（`user_id = auth.uid()`）。**anon には出ない設計どおり** |
| **🔴 無防備** | **`users`** / `horse_week_log` / `npc_stables` / `point_flow_daily` / `schema_migrations` / `unlock_daily` / `app_environment` | **RLS 無効・ポリシー0。** revoke もされていない |

### ★最も重いのは `users` です

**現在0行ですが、それは「利用者がいないから」であって「守られているから」ではありません。**

> **いま作っている LINE 認証で最初の1人が登録した瞬間に、その行は誰でも読めるようになります。**

`users` が持つのは `display_name` / `stable_name` / **`entry_points`** / **`prize_points`** です。**他人の EP・PP 残高が公開される**ということです。

**★これは R-16 の最も静かな形です** — 「守りが無い」ことが「データが無い」ことに隠れており、**テストも監視も何も鳴りません。データが入った瞬間に、何の警告もなく開きます。**

### 本番で実際に中身が出ているもの

| テーブル | 出ている内容 |
|---|---|
| **`point_flow_daily`**（1行） | `ep_inflow` / `ep_burned` / `pp_issued` / `pp_exchanged` / `margin_actual` / `intervention_mult_mean` ＋ `*_internal` 系 — **V-11 の経済監視データがそのまま公開** |
| `npc_stables`（1行） | `distance_bias` / `surface_bias` / `growth_bias` / `heavy` / `emphasis` — **NPC 厩舎の方針**（予想に使える可能性） |
| `schema_migrations`（1行） | 適用済みマイグレーションとチェックサム — スキーマの状態が読める |
| `app_environment`（1行） | 環境名（レビュー側が最初に見つけたもの） |
| `horse_week_log` / `unlock_daily` | 本番では0行。**staging では中身が出ています**（`gain` / `fatigue` / `condition` / `injury_prob` など**育成の内部状態**） |

### 開発側の提案（**まだ何も直していません**）

**期待される姿は `0002` / `0006` の設計から明らかです** —「**公開ビュー以外は anon から0行**」。

→ **`0018` で7テーブルに RLS または revoke を当てる**のが素直だと考えます。ただし**当て方に選択があります**:

| 対象 | 提案 | 注意 |
|---|---|---|
| **`users`** | **RLS 有効＋`id = auth.uid()` のポリシー**（自分の行だけ） | ★`ep_ledger` と同じ形。**`revoke all` にしてはいけません** — S-2 の前歴（revoke が勝ってポリシーが打ち消され、利用者が自分の履歴を永久に見られなくなった）と同型の事故になります |
| `point_flow_daily` / `unlock_daily` / `horse_week_log` / `npc_stables` / `schema_migrations` | **`revoke all`**（クライアントが読む理由がない） | ワーカーは service_role なので影響なし |
| `app_environment` | **`revoke all`** | 同上。ただし**ガード類が service_role で読んでいることを確認してから** |

**★適用は指示をいただいてから行います。** 本番のセキュリティ姿勢を変える操作であり、**私は本日すでに1度、未検証の前提で本番に操作を向けて事故を起こしています。** 同じことを繰り返しません。

---

## 2. 本番ビルドの再診断 — **診断は取り下げ、新しい根拠で出し直します**

### まず、取り下げます

> **`entrant_snapshot` が0行であることから分かります**

**この根拠は無効でした。** 裁定のとおり、**`0016` を当てたのが 01:50:59** なので、**どんなビルドが動いていても0行になります。** 「結論の方向が結果的に正しかったことは、分析が正しかったことを意味しません」— 自分で書いた総括に自分が当たりました。

### ★裁定が示した決定的な確認を実施しました

**列が存在する状態で生成されたレース**を見ます（読み取り専用）。

```
生成 01:52:19  発走 02:20  出走 13  凍結あり 0
生成 02:01:19  発走 02:30  出走  8  凍結あり 0
生成 02:12:17  発走 02:40  出走 18  凍結あり 0
生成 02:21:18  発走 02:50  出走  8  凍結あり 0
生成 02:32:17  発走 03:00  出走 18  凍結あり 0
生成 02:41:58  発走 03:10  出走 15  凍結あり 0
生成 02:51:56  発走 03:20  出走 17  凍結あり 0
生成 03:01:57  発走 03:30  出走  8  凍結あり 0
生成 03:12:12  発走 03:40  出走 11  凍結あり 0
生成 03:21:51  発走 03:50  出走 15  凍結あり 0
生成 03:32:28  発走 04:00  出走 14  凍結あり 0
```

**11レース・145出走のすべてで `entrant_snapshot` が NULL です。**

→ **列が使える状態で生成されたのに1件も書かれていない。D-055 は本番に配備されていません（確定）。**
→ **したがって D-056（凍結が無いレースは中止・EP 返還）も本番では働いていません。**

**★皮肉ですが、この確認ができたのは `0016` が当たったからです**（裁定の指摘どおり）。

### レビュー側の独立測定との整合

レビュー側は「馬場状態が `good`/`yielding`/`bad` に散っている＝**D-052（08-11）は配備済み**」と測定しました。**私の測定と矛盾しません。**

→ **本番のビルドは「D-052 を含み、D-055 を含まない」= 08-11 以降・08-12 の D-055 より前**、という範囲に絞られます。

### ⚠️ VPS の release symlink は、私からは読めません

裁定の要求1（**symlink で稼働コミットを確定**）は**一次資料**として正しいのですが、**この作業環境から VPS への SSH アクセスがありません。**

→ **オーナーに実行していただく必要があります**:

```bash
ls -l /opt/star-current
cd /opt/star-current && git rev-parse HEAD   # リリースディレクトリに .git があれば
```

**★私の DB 側の測定は「D-055 が入っていない」を確定させますが、「どのコミットか」は確定させません。** 両方あって初めて差分を列挙できます。

---

## 3. R-26 / R-27 / R-28 を受領しました

| | |
|---|---|
| **R-26**（安全策を足す変更自体が副作用の経路になる） | **受領。** そして**私のは3回目**だったと知りました（`--schemacheck` / `deploy.sh` に続く）。**「安全にするための変更」を、安全になったことの根拠にしない**を、以後の作業規範にします |
| **R-27**（既定と縮退は狭い側へ） | **受領。** `| head -4` が「見る量を狭める」ことで「危険を見落とす」に落ちた件も含めて理解しました。**本番に向ける操作の出力はパイプで切りません** |
| **R-28**（稼働ビルド識別子を外から確認できるように） | **受領。** 併走案件の `healthz=<sha>` の形が、この案件に欠けていたものだという指摘に同意します。**ゲートの合格は「どのビルドで測ったか」とセット**でなければ、本番だけが守られていない状態を作ります |

---

## 4. 判断をお願いしたいこと

| # | | 誰 |
|---|---|---|
| **1** | 🔴 **anon 露出の是正（`0018`）の当て方**（§1 の表）。**特に `users` は、最初の利用者が登録する前に塞ぐ必要があります** — LINE 認証の実装と同じ便で入れるべきかも含めて | レビュー側 |
| **2** | **VPS の release symlink の確認**（§2） | オーナー |
| 3 | 配備の二択（(a) HEAD へ / (b) 本番ワーカー停止）は、ビルド確定後 | オーナー |
| 4 | `QUESTIONS_SETUP_RPC_20260820.md` の裁定（前便から継続・**唯一やり残している照会**） | レビュー側 |
| 5 | 🔴 本番の service_role キー・DB パスワードのローテーション（継続） | オーナー |
