# REPORT_P3_G7B3D043_20260811 — G-7 / B-3 / D-043 の残件と、staging での実証

## 本便で正典の改訂が要る決定をしたか

**していません。** 実装は指示書の範囲内で、値を発明した箇所はありません。
ただし**正典に足していただきたい条件が1つ**あります（§2 の V-11）。

---

## 1. 結果一覧

| 項目 | 結果 | 証拠 |
|---|---|---|
| **G-7** 賞金の PP 発行（§11.1） | **PASS 7/7** | staging cycle 483 |
| **B-3** V-11 の構造報告 | 完了（★重要な発見あり） | `tools/diag-v11.mjs` |
| **D-043** 残件（配る物の検証） | 完了 | 原本とバンドルが**バイト一致** |
| §10.2 開催中止・返還 | **PASS 6/6** | staging cycle 502 |
| 経済の一巡（EP→PP 一方通行） | **PASS 8/8** | staging cycle 512 |
| テスト | **602件 緑** | `7524f35` |

---

## 2. ★B-3 — V-11 は「経済が動いていない」ことで自動的に通ります

### 構造（DB 制約で閉じています）

| 理由 | 向き | どこで | 何に比例するか |
|---|---|---|---|
| `prize` | ＋発行 | `prize-award.ts` | §11.1 の表 × レース数 × プレイヤー所有馬の割合 |
| `payout` | ＋発行 | `payout.ts` | (1 − margin) × 馬券の売上（EP） |
| `prize_exchange` | −吸収 | `0008` の RPC | ★プレイヤーの交換性向（パラメータ） |

**PP→EP の還流は存在しません**（`pp_ledger` の `reason` 制約が3つに閉じています）。**出口は景品交換だけ**です。

```
純発行量/日 = (1 − margin) × B + P − X
```

★**`P`（賞金）は EP の焼却を伴わない「対価なしの発行」**です。
`payout` は EP を焼いた見返りですが、`prize` は §11.1 の表から新規に発行されます。
→ V-11 が問うているのは「**X が P を受け止められるか**」でした。

**外部流入 / 交換性向 / レース数×プレイヤー馬の割合は、こちらで値を決めていません**（指示書の条件）。

### ★本番の実測 — これが本便で一番重い発見です

```
利用者 0 人 / 馬券 0 枚 / PP 台帳 0 行 / 確定済みレース 401 件 / プレイヤー所有馬 0 頭
point_flow_daily: 4日ぶんすべて 0 / 0 / 0 / 0
```

**ワーカーは動いていてレースも401件確定している**のに、**PP が1点も発行されていません。**
`prize-award.ts` は NPC 馬（`owner_id` が null）には払わないので、
**プレイヤー所有馬が0頭なら賞金は発行されません**。利用者が0人なので払戻もありません。

> **したがって純発行量は毎日 0 になり、V-11 は自動的に満たされます。**
> ★これは合格ではなく、**測定対象が存在しない**状態です（R-21）。

**V-12a（上限のみ → F≒0）・V-15①（下限のみ → 比率1.0）と同じ型**です。
**機構を止めるとゲートが最もよく通ります。**

→ **V-11 にも「機構が動いていること」の条件を足してください**（V-15 に②を足したのと同じ形）。
`tools/diag-v11.mjs` は毎回これを判定し、止まっていれば原因（利用者0人／プレイヤー馬0頭）まで出します。

### ★もう1つ: 日次の記録が台帳から再現できるか

V-11 は `point_flow_daily` を見て判定しますが、これは**集計結果**なので、
**元の台帳が消えても値が残ります**。

staging で実際にこうなっていました:

```
Tue Aug 11  記録 30,000 PP / 台帳から 0 PP  ★食い違い
```

`verify-flow.mjs` が台帳だけ後片付けし、集計行を残していたためです。
★**行数は変わらず値だけ変わる**ので、`audit-tools.mjs` の行数比較では拾えません。
→ 台帳との突合を `diag-v11.mjs` の⑥bに足し、`verify-flow.mjs` の後片付けを直しました。
**本番は全日一致**しています（すべて 0 なので当然ですが、検査は働いています）。

---

## 3. G-7 — 実データで PASS（ただし最初は空振りでした）

**staging cycle 483 / 2勝クラス / 出走10頭のうち9頭をプレイヤー所有**

```
✓ ⓪ 賞金の発行経路を実際に通した   2着 3,000 / 3着 1,800 / 4着 1,100 / 5着 800
✓ ① 合計が §11.1 の表と一致        期待 6,700 PP / 実際 6,700 PP
✓ ② 台帳4行・すべて reason=prize    入賞4頭と一致
✓ ③ delta の合計が賞金額と一致
✓ ④ すべての行が当該レースを指す
✓ ⑤ 二重確定で増えない             PP 6,700 → 6,700 / 台帳 4 → 4 行
✓ ⑥ NPC には払わない               ★1着だった NPC 馬は表なら 8,000 PP だが発行 0
★G-7: PASS — 7項目すべて成立
```

### ★最初の実行は空振りでした

1頭だけ所有させたところ、その馬が**13着**で賞金 0 PP。
検査は「表と一致（0 = 0）」で**全項目 PASS**と出ましたが、
**発行の経路を一度も通っていません**（B-1 の⑤⑥と同じ型）。
→ 最終枠以外を全部持つ形に変え、**⓪「空振りでない」を検査に足しました**。
最終枠を残すのは「所有していない馬には払わない」を同時に見るためです。

### ★`verify-prize` は「表示するだけ」でした

これまで数字を並べるだけで §11.1 の表と突き合わせていませんでした。
**賞金額が1桁違っても「確定後 PP=…」と出るだけ**です。7項目の判定に変えました。

---

## 4. §10.2 開催中止と、経済の一巡

### 開催中止（cycle 502）

```
✓ ① 開催中止が実行された          返還 3枚 / 3,000 EP
✓ ② 買った枚数だけ返還された      3 / 3 枚
✓ ③ EP が全額戻った               100,000 / 100,000
✓ ④ 二重中止で二度目は何もしない
✓ ⑤ 返還で PP が増えていない      PP=0（EP→PP の変換が無い・憲法②）
✓ ⑥ レースが cancelled になっている
★PASS — 6項目
```

### 経済の一巡（cycle 512・単勝を全通り購入）

```
✓ ① EP は購入額だけ減る            98,800 = 100,000 − 1,200
✓ ② EP 台帳の bet が購入額と一致    bet -1,200
✓ ③ 全通り買ったので的中は必ず1枚    won 1枚 / lost 11枚
✓ ④ PP は払戻額と一致              PP=5,930 / 払戻=5,930
✓ ⑤ PP 台帳に payout が1行だけ
✓ ⑥ EP 台帳に払戻の行が無い        （PP→EP の還流が無い・憲法②）
✓ ⑦ §8.6 sha256(seed_reveal) == seed_commit
✓ ⑧ 二重確定で PP が増えない
★PASS — 8項目（1,200 EP を焼き 5,930 PP が出た）
```

### ★両ツールとも「PASS / FAIL を表示するだけで exit 0」でした

自動化からは **FAIL が「成功した実行」に見えます**。
`audit-tools.mjs` も出力の一致と行数しか見ないので、
**FAIL のまま毎回同じ出力を出せば「一致」で通ります**。
→ 判定を集計して `exit 1` を返す形にしました。
`verify-economy` には**経済の恒等式**（EP を焼いた額と PP が出た額の突合）が
そもそも無かったので、これも足しました。

---

## 5. D-043 の残件 — 配る物そのものを検証する

**別にバンドルを作って確かめても意味がありません**（それは配る物ではない）。
→ **`node dist/worker.cjs --selfcheck`** として、**配る物自身**に自己検査を持たせました。
DB にも環境変数にも触りません（純ロジックだけ）。

```
原本（tsx）と配る物（バンドル）の出力が**バイト単位で一致**
```

- テストで機械的に守ります。★**バンドルが無い環境では飛ばさずに落とします**
  （「バンドルが無いから検査しなかった」は「検査した」ではありません）
- `deploy.sh` の preflight にも追加。既存の preflight は「起動できるか」しか見ておらず、
  **定数が畳み込まれて別の値になっていても、表が欠けていても、同じ `STAR_ENV` エラーが出ます**

### 途中で踏んだもの

- **CJS はトップレベル `await` を許さない** → 動的 import をやめて静的に
- `cycleIndexAt` の引数を逆順で書き、**−1667 という負の値を証拠に載せかけた**。
  値は式のとおり正しかったのですが、**読んだ人が誤解する形**でした

---

## 6. 運用上の実測（★報告のみ・スコープ外なので直していません）

レース生成の所要時間（staging・本番と同じ 3,896,104 試行）:

| cycle | クラス | 頭数 | 所要 |
|---|---|---|---|
| 481 | open | 18 | **976秒** |
| 482 | win3 | 15 | 571秒 |
| 483 | win2 | 10 | 236秒 |
| 484 | win1 | 12 | 397秒 |
| 512 | win3 | 12 | 315秒 |

- **頭数で4倍以上ばらつきます**
- CPU は27分中6.4分（**24%**）。**残りは I/O 待ち**で、
  オッズを**1行ずつ 5,483回**投入しています（`pg-store.ts`）
- staging はシドニー・本番は東京なので往復は違いますが、
  **本番でも18頭立ては `cycle-runner.ts` のコメントにある「1本あたり約138秒」より長くなる見込み**です
- ★サイクルは10分間隔で、生成は**2レース先まで**。18頭立てが続くと余裕が薄くなります
  （中止の閾値は60分なので、直ちに客の金が戻る事態にはなりません）

★この測定のせいで、**検証中にレースが発売時間を過ぎました**。
`seed-races.mjs` に `--ahead` を足し、先のサイクルに作れるようにしています。

### ★前提の確認にも穴がありました

3本とも `status='scheduled'` だけを見ていて、**発走時刻を見ていません**でした。
発走済みのレースには `place_bet` が「発売時間外」で落ち、
**利用者を作った後だったので行が残りました**（検出して削除・残り0人）。
→ `scheduled_at > now()` まで見る形に直しました。

---

## 6b. ★自己汚染点検で、私が点検そのものを汚しました

点検を回し直したところ **4本に問題あり**と出ました:

```
── seed-races.mjs     出力が違う / races 7→10, race_entries 100→133
── verify-ability.mjs  出力が2回で違う
── verify-flow.mjs     行数が戻らない: races 10→11, race_entries 133→146
── verify-prize.mjs    出力が2回で違う
```

**調べたところ、`verify-flow` はレースを1本も作りません。** おかしいので経緯を辿ると、
**点検を裏で回している最中に、私が同じ staging に対して
`verify-flow` / `verify-economy` / `diag-v11` / `seed-races` を並走させていました。**

★**測定中に別の作業を走らせて、測定を汚した**形です。
レビュー側が P1 で踏んだもの（V-6 の測定中に `LONGSHOT_RANKS` の実験を並走させた）と同じ型です。

- `seed-races.mjs` は**レースを作るのが仕事**なので、そもそも点検対象にすべきではありませんでした
  → 理由つきで除外一覧に入れました（1回4〜16分かかるため、点検の測定窓が長く開き、
    **その間の変化を他のツールの結果として拾ってしまう**という副作用もあります）
- 並走禁止をツールの先頭と実行時の出力に明記しました

**何も並走させずに流し直した結果:**

```
★全件: 2回の出力が一致し、行数も戻ります
★3 本は前提が揃わず**実行できていません**（点検した扱いにしない）:
  verify-cancel.mjs / verify-economy.mjs / verify-prize.mjs
```

（3本は §3〜4 のとおり**レースがある状態では実データで PASS 済み**です。
点検を回した時点でレースを使い切っていたため、点検としては「実行できていない」扱いです。）

---

## 7. 照会（未回答のもの）

| # | 内容 |
|---|---|
| **Q-P3-31（新規）** | **V-11 に「機構が動いていること」の条件を足してください。** 現状は経済が止まっていれば必ず通ります（§2） |
| **Q-P3-29** | `PLACEHOLDER_UNLOCK` を廃し、生成・確定の両方が `horses.stats` を読む形にしてよいか（★最優先） |
| **Q-P3-30** | B-6 配線後に V-4 / V-5 が帯を外れたことの扱い |
| Q-P3-27〜28 | 気性の下限比率の上側基準 / V-15 の①②で最悪方針が逆向き |
| Q-P3-20〜26 | 未回答 |
| — | 決定番号の食い違い（③の定義は D-045 か D-047 か） |

---

## 8. 再現コマンド

```bash
npm run typecheck && npx vitest run                        # 602件 緑
npm run build:worker && node dist/worker.cjs --selfcheck   # 配る物の自己検査
npx tsx tools/diag-v11.mjs                                 # B-3（本番・読むだけ）
npx tsx tools/seed-races.mjs --env staging --races 1 --ahead 20
npx tsx tools/verify-prize.mjs --env staging               # G-7
npx tsx tools/verify-cancel.mjs --env staging              # §10.2
npx tsx tools/verify-economy.mjs --env staging             # 経済の一巡
npx tsx tools/audit-tools.mjs --env staging                # 自己汚染点検
```

---

## 9. ★オーナーへ（再掲）

**staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん）。
