# 報告 — anon に開いた実体テーブル 3 件を閉じました（AE-1〜AE-4・2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**指示**: 裁定 `REVIEW_ANON_EXPOSURE_VERDICT_20260918.md`（AE-1〜AE-4）
**着手の根拠**: ★**オーナーの「お願いします」**（2026-09-18・裁定 §6 の 3 件を提示したうえで）

> 凡例（R-13）: **✔** ＝ 実測で確かめた ／ **△** ＝ 未確認 ／ **🔴** ＝ 判断待ち
> ★**数字はすべて実測です。★裁定の字面から逸れた点を §3 に 2 件書いています。**

---

## 0. 位置

| | |
|---|---|
| ブランチ | `p4/race-30sec-cuts` |
| 直前の自分のコミット | `3e8803f` 帯の下のゲート（D-079 ④）の測定 |
| ⚠️ 同じツリーで動いた HEAD | ★レビュー側が `8dae6bc`・`8e20139` を出しています（★開発側の作業ではありません） |
| 本番 | ★**触っていません**（`0021` 以降は未適用のまま） |
| staging | ✅ **`0032` を適用しました** |
| push | ★**していません**（オーナー指示待ち） |

---

## 1. AE-1〜AE-4 の履行

| # | 指示 | 状態 |
|---|---|---|
| **AE-1** | 新しい移行 `0032` で 3 表を閉じる（`0024`/`0025`/`0027` は書き換えない） | ✅ `db/migrations/0032_close_anon_table_grants.sql`。★既存 3 本は**触っていません** |
| **AE-2** | `EXPECTED_EXPOSURE` に 3 表を `CLOSED` で登録（1 行ずつ理由） | ✅ `tools/lib/exposure-registry.mjs`。★⚠️ **1 件足しています**（§3-2） |
| **AE-3** | 露出を合格条件にしていた検査 3 本の期待を反転（理由を残す） | ✅ 3 本とも。★**変異で噛むことを確認**（§4） |
| **AE-4** | staging の実 DB で**両方向**を測る | ✅ §2 |

---

## 2. ✔ 実測（staging・★両方向）

### 2-1. 適用前（★対照）— 露出は実在しました

```
=== テーブルの grant（適用前）===
     horse_market_listing   anon           SELECT
     horse_market_listing   authenticated  SELECT
     horse_story_event      anon           SELECT
     horse_story_event      authenticated  SELECT
     stable_grade_price     anon           SELECT
     stable_grade_price     authenticated  SELECT

=== V-20 の判定（適用前）===
  🔴 ★② 公開ビュー以外は anon から0行
       ★horse_market_listing(1行), horse_story_event(1行), stable_grade_price(1行)
  🔴 ★③ 登録簿に無いオブジェクトが無い
       ★未登録: story_daily
  → 🔴 V-20 不合格 2 件（11 件中）
```

### 2-2. 適用後 — ✅ 11 件すべて緑

```
=== V-20 の判定（適用後）===
  ✅ ★③ 登録簿に無いオブジェクトが無い（新しいテーブルを黙って足せない）
  ✅ ★① anon / authenticated に書き込み権限が無い（TRUNCATE を含む）
  ✅ ★④ 関数の EXECUTE が登録簿どおり
  ✅ ★② 公開ビュー以外は anon から0行            ← ★AE-4 ①
  ✅ 公開ビューは anon から読める（塞ぎすぎていない） ← ★AE-4 ②
  ✅ ★races.server_seed / horses.potential / horses.genotype が anon から読めない
  → ✅ V-20 合格（11 件中・環境 staging）
```

★**AE-4 が求めた両方向を、別々の行で出しています**:

| # | 何を | 結果 |
|---|---|---|
| ① | 3 表が anon から **0 行** | ✔ ★**出力から 3 表の名前が 1 つも消えました**（適用前は 6 行あった grant が **0 行**） |
| ② | 公開ビュー 4 つが**引き続き**読めること | ✔ `races_public` / `race_entries_public` / `race_odds_public` / `prize_catalog_public` が **anon に SELECT のまま** |

⚠️ ★**②を別に測る理由**は裁定 AE-4 のとおりです — ★**この基準を最も安易に満たす方法は「全部閉じる」**だからです（R-16）。

---

## 3. 🔴 ★裁定の字面から逸れた点 2 件（★先に報告します）

### 3-1. `revoke select` ではなく **`revoke all`** にしました

★裁定 §0・AE-1 の字面は `revoke select ... from anon, authenticated` です。

★**R-29 に従って `revoke all` にしました** — 「★権限は『列挙して閉じる』のではなく『既定を閉じて、
必要なものだけ開ける』。**列挙は必ず漏れる**」。★実例は `0002` で、`insert, update, delete` を revoke したが
★**`TRUNCATE` / `REFERENCES` / `TRIGGER` が漏れ**、★`TRUNCATE` は RLS の対象外なので止まりませんでした。

| | |
|---|---|
| ★実効の差 | ★**ありません**。✔ 3 表に付いていたのは `select` だけ（`0024:96`・`0025:56`・`0027:45` が他を剥がし済み）。★実測でも適用後に 3 表の grant は 0 行 |
| ★理由 | ★**列挙漏れの余地だけが消えます** |
| 🔴 | ★**字面と違う形にしました。差し戻す場合は `revoke select` に直します**（移行の註記にも書いています） |

### 3-2. ★**`story_daily` を登録簿に足しました**（AE-2 の 3 件には入っていません）

★適用前の V-20 で ★**③ が赤**で、★未登録の表として **`story_daily`** が出ました（前便の `0029`）。

| | |
|---|---|
| 露出 | ✅ ★**していません**（✔ `0029:43` が書き込みを剥がし、`grant select` は無い。✔ 適用前の実測でも anon から 0 行） |
| 何が問題だったか | ★**登録簿に無かっただけ**です（V-20 ③） |
| なぜ本便で足したか | ★**同じ登録簿の 1 行**で、★これが赤のままだと ★**AE-4 の「緑になった」が言えません** |
| 🔴 | ★**裁定 AE-2 の範囲外です。** ★不適切ならこの 1 行だけ戻してください（登録簿にその旨を書いています） |

⚠️ ★**裁定の「この便で他のことをしないでください」に対する私の判断**です。★着順・経済には触れていません。

---

## 4. ✔ 検査が本当に噛むことを、変異で確かめました（★両方向）

★反転した検査は「`0032` が閉じていること」を要求します。★**要求が空振りしていないか**を確かめました。

| | 操作 | 結果 |
|---|---|---|
| 変異 | `0032` の `revoke all on horse_story_event ...` を 1 行つぶす | 🔴 ★**`entry-rpc-migration.test.ts` が赤**（終了コード 1） |
| 復元 | 控えから戻す | ✅ **緑**（終了コード 0）・✔ `diff` で**差分なし** |

★**緑であること自体を根拠にしていません。** ★前便で「変異が DB に届かないまま緑」を踏んだ形（引継ぎ書 §5-5）の再発防止です。

---

## 5. ✅ 検査と型検査

| # | |
|---|---|
| 1 | 反転した 3 本 ＋ `exposure-registry.test.ts` | ✅ **4 ファイル緑**（終了コード 0） |
| 2 | 型検査 | ✅ `npm run typecheck` **終了コード 0**（★パイプで切らず、道具自身から取得） |
| 3 | 検査一式 | ✔ **2,079 本中 2,078 本が緑**（★vitest の終了コード **1**）。🔴 落ちた 1 本は `packages/render/test/edit-grammar-audit.test.ts` ⑨「seed 分類を現 HEAD で再確認している」＝ ★**既知の赤**。✔ ★**この便は `packages/render` を触っていません**（`git log 02fda8a..HEAD -- packages/render` が空・作業ツリーにも変更なし） |
| 4 | ⚠️ ★**背景タスクの枠は「exit code 0」と表示しました**が、★**中の vitest は 1** でした。★枠の値を合否に使っていません（★出力ファイルを読みました） |

---

## 6. 🔴 残り（★オーナー・レビュー側の判断が要るもの）

| # | |
|---|---|
| 1 | ★**本番への適用と配備の順序**（裁定 §5）。★推奨は **`0024`〜`0032` を 1 回の配備にまとめる**。★本番は `0020` までしか当たっておらず、★**`0024` を当てた瞬間に露出が現実になる**ため、★**`0032` を必ず同梱**してください。⚠️ ★**本番への適用は開発側では行いません** |
| 2 | ★**起票の可否**（裁定 §6-3）—「実体テーブルに `grant select` を足さない。公開が要るなら `*_public` ビューを作る」を正典に 1 本立てるか。★**正典の改訂はレビュー側の発行物**です |
| 3 | ★**`revoke all` にした判断**（§3-1）と ★**`story_daily` の登録**（§3-2）の可否 |

---

## 7. ★この便で分かったこと（記録）

★裁定 §3 が「**検査は『あるべき姿』を書くものであって、『今そうなっている姿』を書くものではない**」と
書いた形を、★**実物で 3 本見ました**。★3 本とも ★**1 つ上の行では `revoke insert, update, delete, truncate` を
正しく要求しており**、★**同じ人が同じ便で、正しい要求と誤った要求を並べて書いていました。**

★**移行ファイルの文字列を写す検査は、書いた瞬間の誤りごと凍結します。**
★今回の反転でも同じ危険があるので、★**「なぜ反転したか」を 3 本すべてに書き残しました**（次の便がまた手本にするため）。
