# P0-fix5 完了報告 — 導出の自己整合を閉じる（P0 の締め）

**発行**: 2026-08-05（開発側 Claude）
**指示書**: `DEV_INSTRUCTIONS_P0FIX5_20260805.md`（J-1〜J-3）
**正典**: `STAR_SPEC_v2.0.md`（丈夫さ sd 66 への是正・丸めの規約の明文化を確認済み）

## 結論（先に）

**J-1〜J-3 すべて完了。合格条件である「`npm run verify` が P0-fix4 と同一数値」を満たしています。**

| 合格基準 | 実測 | 判定 |
|---|---|---|
| **18. clamp比を変えたら切断係数と導出 sd が追随** | k=3.333→係数0.9992→丈夫さsd 60 / k=2.0→0.9594→63 / k=1.5→0.8823→68（指示書の表と一致） | **PASS** |
| **19. `basis` 3分岐の固定・`none` で FAIL** | ratio / sd / none を `runSimulation()` 経由で固定。none は `pass:false` かつ総合も FAIL | **PASS** |
| **20. `derive()` が r=0 を静かに受け入れない** | 例外を送出。CLI `--set-balance REGRESSION_RATE=0` も停止する | **PASS** |
| **既存17項目の維持** | `npm run verify` の出力が **P0-fix4 と完全一致**（所要時間の行を除き差分ゼロ）。テスト **98件**・typecheck exit 0・`any` ゼロ | **PASS** |

**J-2 のテストを書いた過程で、自分の I-2b 実装に到達不能な分岐があることが判明し是正しました**（§1 J-2）。既定の挙動は変わっていません。

---

## 1. 変更点

### J-1 切断係数を clamp比の関数にした — **(a) 解析式**を選びました

**理由**: (b) の fail-closed は「静かに間違わない」は満たしますが、**`sweep.ts` のように clamp比を意図的に振る用途が実際に存在する**ため、拒否すると使えなくなります。またコメントに書いた「比を変えたら再計算せよ」という要求を、コード自身が満たせる形にしておくのが筋だと判断しました。正規分布の CDF/PDF は外部依存なしで書ける範囲（誤差関数は Abramowitz & Stegun 7.1.26、絶対誤差 1.5e-7）です。

```ts
clampTruncationFactor(k) = √( (2Φ(k) − 1) − 2k·φ(k) + 2k²(1 − Φ(k)) )
```

実測が指示書 J-1 の表と一致することを確認しました。

| clamp比 k | 係数 | 導出される丈夫さ sd | 指示書の表 |
|---|---|---|---|
| 150/90 = 1.667（既定） | 0.915604 | **66** | 66（正典 §6.4） |
| 150/45 = 3.333 | 0.9992 | **60** | 60 ✓ |
| 2.000 | 0.9594 | **63** | 63 ✓ |
| 1.500 | 0.8823 | **68** | 68 ✓ |

**既定 k での係数は 0.915604 で、正典の 0.9156 と導出 sd が完全に一致します**（66/10/13/341/114 と clamp 109/16/22/568/189 のすべて）。したがって出荷既定の挙動は変わりません。

`config.test.ts` の「`clamp/sd ≈ ratio` しか見ていない」テストも是正しました。**sd の実値まで固定**し、係数がリテラルのままなら落ちるようにしています（`MUTATION_SD=45` で 60 になること、66 でないことの両方を assert）。

### J-2 `basis` の3分岐を本番経路で固定 — **書いてみたら自分の実装の欠陥が出ました**

3分岐を `runSimulation()` 経由で固定しました。そのテストを書いている最中に、**I-2b の実装では `sd` 分岐に実質到達できない**ことが判明しました。

切替条件を `Math.abs(founderMean) > 1e-9` にしていたため、創始平均が **1.01・創始SD 18.6** というほぼゼロ中心の分布でも `ratio` 判定になり、**ほぼ0の値で割った不安定な乖離**が出ます。「平均が厳密に0か」を見ていたのが誤りで、正しくは「**平均が散らばりに対して十分大きいか**」です。

```ts
// 修正前: Math.abs(founderMean) > 1e-9
// 修正後:
const usesRatio = Math.abs(founderMean) > founderSd;
```

既定の6形質はすべて平均がSDより十分大きい（丈夫さ 649 vs 71、気性 50.3 vs 10.7、適性 54.9 vs 14.3、距離 2090 vs 363 / 701 vs 122）ので **`ratio` のままで、既定の挙動は変わりません**。

固定した3分岐:

| 分岐 | 条件の作り方 | 期待 |
|---|---|---|
| `ratio` | 既定 | 6形質すべて。乖離が `final/founder − 1` と一致 |
| `sd` | `SURFACE_RANGE: [-50, 50]`（平均≈0・SD>0） | 創始SD単位の差。有限値 |
| `none` | `SURFACE_RANGE: [0, 0]`（平均もSDも0） | **`pass: false`** かつ総合判定も FAIL |

**これは I-3 でご指摘いただいた「経路を固定せよ」がそのまま効いた例です。** 単体で分岐関数を書いていたら、この到達不能に気づけませんでした。

### J-3 `derive()` の r=0 を明示 — **例外**にしました

`buildTraitMutation()` は回帰率が 0 以下なら例外を投げます。理由をメッセージに書いてあります（「r=0 では平衡式が定義されず、非能力形質の変異が無言で停止します」）。

`sweep.ts` の回帰率0構成（①②③）は **D-008 より前の状態を再現するためのもの**なので、当時の挙動＝**sd 上書きなし（値域比スケール）＋ 品種中心だけ**を `legacyCentersOnly()` として明示的に組むようにしました。「回帰なしの比較がしたい」という意図がコード上に残ります。

CLI で `--set-balance REGRESSION_RATE=0` を指定した場合も例外になります（テストで固定）。これは**望ましい挙動**で、従来は非能力6形質が無言で凍結していました。

---

## 2. 再実行コマンド

```bash
git clone https://github.com/jinjinsansan/syar.git star && cd star
npm install

npm test              # ① ユニットテスト
npm run typecheck     # ② exit 0
npm run verify        # ③ 受け入れ判定（P0-fix4 と同一数値になること）

# J-1: clamp比を変えると係数と sd が追随する
npm run simulate -- --generations 5 --seed 42 --quiet --json out/a.json --set-genetics MUTATION_SD=45
node -e "console.log(require('./out/a.json').balanceDigest.traitMutation.durability)"  # sd 60（66 ではない）

# J-3: 回帰率0は例外になる
npm run simulate -- --generations 5 --seed 42 --set-balance REGRESSION_RATE=0   # エラーで停止する
```

---

## 3. 検証結果 — `npm run verify` が P0-fix4 と同一であること

```
$ diff <(grep -v 所要時間 out/verify-fix4.txt) <(grep -v 所要時間 out/verify-fix5.txt)
（差分なし）
```

P0-fix4 の受け入れ出力と**1文字も違いません**。J-1〜J-3 はいずれも「既定以外の経路」を直したものなので、既定経路の数値は変わらないという予想どおりの結果です。参考として P0-fix5 の出力そのものを載せます。

```
繁殖牝馬=800頭 / 種牡馬プール=240頭 / シード=42,7,2026,31337 / horizon=100,300

=== 100 ゲーム内年（実時間 約2.4年相当）===
   seed |      V-1    判定 |    V-2a %/世代    判定 |   V-2b 天井    判定 |          V-2d 最大乖離    判定 |             V-2e 最悪SD比    判定 |   V-3 |      丈夫さ |     親子相関
--------------------------------------------------------------------------------------------------------------------------------------------------------
     42 |   15.46%  PASS |       0.0715  PASS |    53.77%  PASS | distance_center 1.81%  PASS |       durability 1.08x  PASS |  PASS |    645.2 |    0.348
      7 |   16.76%  PASS |       0.0402  PASS |    54.93%  PASS | distance_range -3.53%  PASS |     surface.dirt 1.07x  PASS |  PASS |    647.4 |    0.342
   2026 |   16.05%  PASS |       0.0468  PASS |    52.88%  PASS | surface.turf -2.39%  PASS |   distance_range 1.07x  PASS |  PASS |    646.0 |    0.388
  31337 |   16.44%  PASS |       0.0697  PASS |    53.92%  PASS | surface.dirt -3.67%  PASS |     surface.turf 0.93x  PASS |  PASS |    654.0 |    0.347
     平均 |   16.18%       |                    |                 |                          |                  1.04x       |       |    648.1 |

=== 300 ゲーム内年（実時間 約7.1年相当）===
   seed |      V-1    判定 |    V-2a %/世代    判定 |   V-2b 天井    判定 |          V-2d 最大乖離    判定 |             V-2e 最悪SD比    判定 |   V-3 |      丈夫さ |     親子相関
--------------------------------------------------------------------------------------------------------------------------------------------------------
     42 |   16.23%  PASS |      -0.0085  PASS |    53.81%  PASS | surface.turf -4.76%  PASS |           temper 0.92x  PASS |  PASS |    655.6 |    0.386
      7 |   15.63%  PASS |       0.0457  PASS |    59.17%  PASS | surface.dirt -2.02%  PASS |       durability 1.09x  PASS |  PASS |    645.0 |    0.357
   2026 |   14.88%  PASS |       0.0162  PASS |    55.14%  PASS | surface.turf -2.40%  PASS |       durability 1.05x  PASS |  PASS |    652.7 |    0.367
  31337 |   15.61%  PASS |       0.0852  PASS |    53.97%  PASS | surface.turf -2.70%  PASS |       durability 1.03x  PASS |  PASS |    648.9 |    0.415
     平均 |   15.59%       |                    |                 |                          |                  1.02x       |       |    650.5 |
V-2c（300年での V-1 が 12〜18%）: PASS  [最小 14.88% / 最大 16.23%]

所要時間: 207.2 秒
総合: PASS
```

### 既定以外の経路が実際に変わっていること

| 確認 | コマンド | 結果 |
|---|---|---|
| J-1 | `--set-genetics MUTATION_SD=45`（k=3.33） | 丈夫さ **sd 60 / clamp 200**（従来は係数据え置きで 66 のまま） |
| J-3 | `--set-balance REGRESSION_RATE=0` | **例外で停止**（従来は非能力6形質が無言で凍結したまま完走） |

---

## 4. 仕様からの逸脱・解釈で埋めた箇所

**正典の規定からの逸脱はありません。** J-1〜J-3 はいずれも指示書どおりで、既定の導出値（丈夫さ66/±109・気性10/±16・適性13/±22・距離341/±568・114/±189）は正典 §6.4 の表とリテラル一致します。

解釈・設計判断として申告するものが3件あります。

| # | 箇所 | 判断 | 理由 |
|---|---|---|---|
| 1 | **J-1 で (a) 解析式を選択** | 正規分布の CDF/PDF を engine 内に実装（外部依存なし・誤差関数は A&S 7.1.26） | (b) の拒否だと `sweep.ts` のように clamp比を意図的に振る用途が使えなくなるため。また「比を変えたら再計算せよ」という自分のコメントを、コード自身が満たせる形にするのが筋と判断 |
| 2 | **`basis` の切替条件を変更**（`!== 0` → `|平均| > 創始SD`） | J-2 のテストを書いて到達不能に気づいたための是正 | §1 J-2 参照。既定6形質は `ratio` のままで挙動不変 |
| 3 | **`sweep.ts` の r=0 構成に `legacyCentersOnly()` を用意** | D-008 以前の挙動（sd 上書きなし）を明示的に組む | 回帰率0は平衡式が定義されないため。歴史的構成の意図をコードに残すのが目的で、新規用途では使いません |

---

## 5. 未解決の質問

**なし。**

P0 として残る宿題は無く、P1 の指示書起草に進んでいただいて問題ありません。

引き継ぎ事項（指示書 §6 の6点）はすべて認識しています。特に:

- **基準線は P0-fix4/5 の数値**（本報告の `npm run verify` 出力が P0-fix4 と同一であることを §3 で示しています。どちらを使っても同じです）
- **§3 の「表現型の気性が既定設定で ±10% を超える件は欠陥ではない」**という整理に同意します。私の P0-fix4 報告 §1 が「平均F が小さいので無害」と書いたのは 800頭/300年（F=0.011）だけを根拠にしており、既定の 200頭/100年（F=0.0404）では成り立ちません。**訂正いただいたとおりです。** 集団が小さいほど近交が進み気性が荒くなるのは §6.5 の設計どおりで、V-2d を genotype 判定のまま据え置く判断にも同意します（表現型を混ぜると遺伝的な水準ずれと近交の影響が分離できなくなります）
- P1 では **平均F と「表現型 − genotype」の乖離**を監視項目に加えます
