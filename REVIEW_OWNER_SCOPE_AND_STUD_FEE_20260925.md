# 【STAR】裁定: (A) の網が `/records` の生きた欠陥を捕まえた ／ 見本に落ちる 2 件 ／ D-107 ③ の移転 — 2026-09-25

## 1. ✅ (A) — ★「無い」は誤りだった、を自分で覆した
- ✔ 広い形（生の `error.message` を全部）は**作らなかった**（★42 か所・前置きつきで投げるのが作法・★全部挙げる網は使われなくなる）。★**実害の形に絞った**（「本人スコープの口を、セッションを見ずに読んでいないか」）→ ★**絞り方が正しい**
- ✔ 🔴 ★**初回に生きた欠陥**: `/records` の `loadRecordsScreen` が `/entry` と**同じ形**（★`anon` に DB の文がそのまま出る・★セッション無しの `authenticated` には **0 行＝「戦績がありません」という嘘**）。★`/mypage` から来られる
- ✔ ★壊して確かめた報告に**「狙った所が本当に変わったか」**（`getSession` が 0 件になった・差分 0 行に戻した）が付いている → ★私が足した作法が効いている
- ✔ ★**画面の一覧を手で書いて 1 回 外した**（`/entry` は別の作法だった）→ ★**投げる lib から導く**形に直した。★導いたら手では出なかった 4 件が出た（★1 件は偽陽性と明記）

## 2. 🔴 見本に落ちる 2 件（`/stable`・`/training`）— ★**振る舞いだけ、いま直す**
- ✔ `catch` で**見本の馬に落ち**、★**未ログインの人に見本を「あなたの厩舎」として見せる**。★生の文より悪い（★嘘を自信ありげに出す）
- ★開発側は「見た目はデザイナー」として簿 `DEMO_FALLBACK` に入れた。★**その線引きは正しい**。★ただし ★**これは見た目ではなく振る舞い**（★誰のデータを見せるか）
- **決定**: ★**見本に落ちる経路だけを外す**。★**既存の部品で**「ログインしてください」（`/records` でいま入れた形）に寄せる。★**新しい意匠を作らない**・★**レイアウトを変えない**。★R-16 の作り直しで、そのまま置き換えられる形にする
- ★理由: ★**本番で今この瞬間、未ログインの人に他人（見本）の馬が「あなたの厩舎」として出ている**。★作り直しを待つ間、嘘を出し続けない

## 3. ✅ D-107 ③ — ★第 3 の判定（`not-yet-reachable`）を採る
- ✔ `spend_stud_fee_ep`（`0071:58`）は**引くだけ**で、★**種牡馬の持ち主に足す行が無い**。★`ep_ledger` の語彙にも受け取る側が無い
- ✔ ★**今日は壊れていない**: 配合の画面が引く口は `npc_stallion_facts()` **だけ**で、★NPC へは**全額焼却**（D-107 ③）＝ ★**いまの姿は正典どおり**
- ✔ ★**通す／落とすの 2 つでは足りない**という見立てが正しい（★赤にすると「いま壊れている」という嘘・緑にすると開いた日に黙って全額焼却）。★`kind: 'not-yet-reachable'` ＋ ★番人（「口がまだ閉じているか」を見続ける）＋ ★**番人と表の対応を両方向で見る**（★使われない番人も落とす）→ ★**この形を採る**。★他でも使える
- ★**D-105 の騎手の料金と同じ形の 7 例目**という整理も正しい

## 4. 照会 Q-STUD-1〜3 への答え
| # | 裁定 |
|---|---|
| **Q-STUD-1** 誰のどの残高・`reason`・いつ | ✅ **種牡馬の持ち主の EP に加算**／★`ep_ledger` に**受け取る側の語を足す**（例 `stud_fee_income`。★`0026` が `horse_sale` を足したのと同じ形）／★**配合が確定する同じ取引の中**（★引く側と同時。★別便にしない）／★冪等の鍵は**要求 ID**（★既存の `stud_fee_waiver:<id>` と同じ作法） |
| **Q-STUD-2** 日次上限に数えるか | ✅ **数えない**。★これは**発行ではなく移転**（★他人の EP が移るだけ・★世界の EP は増えない）。★`ep_reason_class()` に ★**第 3 の分類「移転」**を足す（★いまは「発行／焼却」の 2 つ）。★**20% は焼却**なので焼却に数える。★V-11（純発行量）にも移転を混ぜない |
| **Q-STUD-3** 公開までに開くか | ⏸ **オーナーと弁護士**。★**EP が利用者の間を動く経路**なので、★§17.2 C-3「ポイントのユーザー間譲渡機能が無い／種付料はサービス対価であり譲渡でない」の確認に触れる（★私の裁定 `c653606` で I-4 と弁護士ゲートの便に分けたのと同じ線）。★**既定は「開かない」**。★開くならその便で弁護士ゲートを通す |

## 5. 次
★Q-2 第 2 段は EP 予算の回答待ちで正しい。★手が空くなら §2（見本に落ちる 2 件の振る舞い）を先に。

## 6. 追記（2026-09-25）— §2 の直し ✅ ／ ★**網が壊れていたのを対照が捕まえた**

### ✅ §2
- ✔ `/stable`・`/training` の**見本に落ちる経路を外した**。★未ログインは「ログインしてください」・その他の失敗は理由つきの枠。★**馬を 1 頭も出さない**
- ✔ ★意匠を作らず既存の部品だけ・レイアウトも触っていない・★フックより後に早期 return（★フックの数を変えない）
- ✔ 🔴 ★**死んだ対照を 1 つ見つけた**: `/stable` の頭に「読めなかったら見本に落ちるが `view.demo` の帯が出る。★帯が出る＝本物が来ていない（CK-14）」と書いてあったが、★見本落ちを外したので**その帯は永久に出ない** → ★**註記だけ残すと「出ない目印を信じろ」と書いてあることになる**。★書き換えて、使われない import も外した

### 🔴 ★この便の中心 — 網が距離で測っていた
- ★直した後に対照を流したら **7 件 全部 緑**。★原因は ★**`catch` から 400 字**という**距離**で測っていたこと。★直しの説明を `catch` の頭に 10 行 書いたので、`DEMO_HORSES` が**400 字の外**へ出た。★`strip` が註記を**同じ長さの空白**に替えるので、★註記のぶんも字数を食う
- → ★**距離をやめ、`catch` の本体を波括弧の対応で切り出した**。★**自分を騙した形そのものを検体に埋めた**（長い註記 → `setLoaded(DEMO_HORSES)` ／ `catch` の外の `loaded ?? DEMO_HORSES` は拾わない）
- → ★**教訓**: 🔴 ★**網を直した直後・本体に註記を足した直後がいちばん危ない。対照は「直した後に」流す**
- ★**レビュー側の受理の作法に足す**: ★私は「対照あり」を受理の根拠にしてきた。★**その対照が、直しの後に流されたものか**まで見る

### ✅ 簿
- ★`DEMO_FALLBACK` は**空にして残した**（★「増えたら落ちる」を保つ）・`SCREEN_EXEMPT` は 1 件に
- ✔ 🔴 ★**使われない除外を落とす検査**を足した → ★**これが無いと、直した後も除外が効き続け、もう一度 見本に落としても緑**になる。★**除外は腐る**という発見

### ⚠️ ★未コミットのまま残さない（★レビュー側からの依頼）
✔ いま共有の作業ツリーに**未コミットの変更が多数**ある（`git status` で確認）。★**同じツリーをレビュー側も使い、私は裁定書をコミットしている**。★事故（`checkout`・`stash`・他人のコミットへの巻き込み）の余地を残さないため、★**自分の分をパスで指定してコミットしておくこと**（★push はオーナー）。

## 7. 追記（2026-09-25）— コミットの分け方 ✅ ／ `tsconfig.json` は**戻す**

### ✅ 4 コミットの分け方
- ✔ パス名指し・`add -A` も `commit -a` も使わず・★**私のコミットの上に積んだ**（`merge-base` で確認）
- ✔ ★**作者を推測で決めなかった**: 56 件のうち自分のものを**差分の中身**で確かめ（日付・`@ts-check`・裁定名・自分が作った関数名）、★0 件だったものは 1 つずつ本文を読んだ。★`next.config.mjs` の転送 3 本と**行き先が一致する**ことを根拠にした判断も筋が通る
- ✔ ★**残した 8 件を「自分のものと言えない」として触らなかった**のも正しい

### 🔴 `apps/web/tsconfig.json` — ★**コミットしない。戻す**
✔ [CODE]:
- 差分は `include` の並び替え ＋ `.next-gate/types/**` と `.next-staging/types/**` の追加
- ✔ `tools/gate.mjs:85-120` に**この問題の記録と対処が既に在る**: 「★**commit すると Vercel（`.next`）が参照できない道を指す — 門を守るために入れたものが本番を壊す形**」。★だから門は**ビルドの前に写しを取り、後で書き戻す**
- → 🔴 ★**今回 汚れたのは、`build:web` を門を通さず直に流したから**。★**門の外で同じことをすれば、同じ汚れが出る**

**決定**:
1. ★**`apps/web/tsconfig.json` は HEAD の内容に戻す**（★開発側が戻す。★私は触らない）
2. ★**`.gitignore` に `apps/web/.next-staging/` を足す**（✔ いま `.next-gate/` だけ・`:172`）
3. ★**書き戻しを門の外でも効かせる**: ★`buildWeb` の写し＋書き戻しを**小さな包み**に出し、★**直に流す用の npm スクリプト**もそれを通す。★「`next build` を直に呼ばない」を註記に
4. ★これは ★**`0dc003d`（09-21）で一度 直した形の再発**。★簿に 1 行（★2 回目なので、★次は部品にする）

### 残した 8 件の扱い（★オーナーに回す）
- ★`public/art/**` 51 件・`title-eye` の削除・`design/art/prompts/*` … ★**画像生成とデザイナーの領分**。★オーナーに「要るか／捨てるか」を聞く
- ★`evidence/**` の json 3 件（09-20・09-22 の出力）… ★**記録として残すなら足す**。★どちらでもよいが、★**放置しない**
- ★`.next-staging/` … ★上の 2（`.gitignore`）で消える
