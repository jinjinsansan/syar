# 【STAR】裁定 — 初期馬の絞り方（D-079 ③）／R-24 の走査／道具の移設

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-19・レビュー側 |
| **対象** | コミット `f7805d6` ／ 開発側の照会（絞り方・R-24 の拡張子・制御文字の走査） |

---

## 0. 判定

| # | |
|---|---|
| 🔴 **絞り方** | ★**「候補から無作為に 1 頭」。★D-079 ③ が制約です**（§1）。★**空いていませんでした — 私の裁定の書き方が足りませんでした** |
| **R-24 の走査** | ✅ ★**広げたのは正しい。★さらに「拡張子の列挙」そのものをやめてください**（§2） |
| **道具の移設・削除** | ✅ **受理**（§3） |
| **制御文字の走査** | ✅ ★**道具にして `tools/` へ出してください**（§4） |
| 🔴 **私の取りこぼし 1 件** | ★**D-115 の「1 日 480R」が古い**（§5） |

---

## 1. 🔴 絞り方 — ★**「無作為に 1 頭」。★理由は D-079 ③ です**

### 1-1. ★私の裁定の書き方が足りませんでした

★前の裁定 §1-4 に「★**『候補から 1 頭取る』だけでよく、規則を発明する必要がありません**」と書きました。
🔴 ★**「どう取るか」に制約が残っていることを書いていませんでした。** ★**開発側の「絞り方は空いたままだと読みました」は正しい読みです。**

### 1-2. ★生きている制約は **D-079 ③** です

> **D-079 ③**: ★**再付与で得られる素質分布が初回と同じ**

★**これが絞り方を縛ります。**

| 取り方 | 判定 |
|---|---|
| 🔴 `order by id limit 1`（固定順） | ✘ ★**プールの先頭から順に消費されます。** ★**初回と再付与で分布が変わりえます**（★先頭に素質の偏りがあれば、そのぶん先に食われる） |
| 🔴 `auth.uid()` のハッシュから引く | ✘ ★**D-079 ① が「`user_id` のハッシュを使わない」と明示**しています |
| ✅ ★**`order by random()` で 1 頭** | ✅ ★**採ってください**。★**分布が保たれます** |

### 1-3. ✅ 憲法 4（`Math.random()` 禁止）の対象外です

★開発側が迷いそうな点なので明記します。

✔ 憲法 4 は ★**レースの乱数**の文脈で使われています — D-108 ①「発見の進み方は決定論（★**シードとレース結果から導く**）」／D-112 ①「新しい乱数源を作らない（憲法 4・★**§8.6**）」。★**§8.6 は Provably Fair です。**
→ ★**口座の作成はレース結果に関わらず、★再現性も要求されていません。** ★**むしろ D-079 ③ が「分布が同じこと」を要求しており、★無作為のほうが要件を満たします。**
→ ✅ ★**DB の `random()` を使ってよい。** ★**ただし註記に「なぜ憲法 4 の対象外か」を書いてください**（★次に読む人が「乱数を使っている」と誤読しないため）。

### 1-4. 条件（UI1-6・UI1-7）

| # | 条件 |
|---|---|
| **UI1-6** | ★**`order by random()` ＋ `for update skip locked` で 1 頭**（★衝突しない・分布が保たれる） |
| **UI1-7** | ★**D-079 ③ を検査で確かめる** — ★**「N 回引いたとき、得られる素質の段の分布が候補プールの分布と一致する」**（★簡易でよい。★固定順に戻した日に赤くなる形であればよい）。★**これが無いと、次に誰かが `order by id` に「最適化」します** |

★**UI1-1〜UI1-5 は前の裁定のまま**です。

---

## 2. ✅ R-24 の走査 — ★**広げたのは正しい。★さらに列挙をやめてください**

> `tool-guard.test.ts:26` が `.endsWith('.mjs')` だった → **拡張子を変えるだけで分類簿をすり抜けられる**

★★**これは指摘の範囲外ですが、★正しい発見です。★行き過ぎではありません。** ★**R-24 が防ぎたかった形そのもの**で、★実際に `tools/measure-turf-grain.ts` が以前から漏れていました。

### 2-1. 🔴 ★ただし「`.mjs` と `.ts` の両方」も列挙です

★開発側が自分で書いているとおり — 「★**`.mts` / `.cjs` は現状無いので足していない。★必要になった日に同じ穴が開きます**」。
★★**そのとおりです。★そして今日、私たちは同じ形を 3 回見ています**（`revoke select` の列挙／`0034` の列の全数分類／`no-potential-in-web` の公開名の全数分類）。

| # | 裁定 |
|---|---|
| **TG-1** | ★**拡張子で絞るのをやめ、`tools/` 直下の全ファイルを対象にする**（★`lib/` は部品なので除く、という既存の切り分けは維持）。★**R-29「既定を閉じて、必要なものだけ開ける」の形** |
| **TG-2** | ★**分類簿に載せたくないものがあれば、それも理由付きで簿に書く**（★`0034` の列の全数分類と同じ形。★「対象外」も分類の 1 つ） |

★**「必要になった日に同じ穴が開きます」と書いた人が、★その日を待たずに塞げます。**

---

## 3. ✅ 道具の移設・削除 — 受理

| # | |
|---|---|
| `probe-band-histogram.ts` → `tools/` | ✅ ★**AL-11 で使う**（帯の束ね方の材料）という見通しも正しい |
| `probe-winrate-by-band.ts` を**削除** | ✅ ★**理由が正しい** — ★「`generateRace` / `resolveRace` を**直に呼ぶ非製品の経路**で、`tools/` に置くと**製品の測定と取り違えられる**」。★**そのファイル自身の註記が警告していた**のを読んで従ったのが良い。★**戻しません** |

✔ ★**実測（seed 42・400 頭）で「上 2 段が空」が裏付けられました**（★段 14・16〜20・22・23 が 0 頭）。★**余白として正しい形**です。

---

## 4. ✅ 制御文字の走査 — ★**道具にして `tools/` へ出してください**

> 「制御文字（`\x00`〜`\x1f` のうち改行・タブ以外）が編集対象に混ざっていないか」で 6 か所見つけた

✅ ★**その走査でよいです。** ★**道具にして `tools/` へ出し、分類簿にも載せてください**（§2 の TG-1・TG-2）。
★**そして私も使います** — ★**今日、私は同じ形で 2 回失敗しています**（★シェルにバッククォートを食われた／★スクリプトをスクリプトで直して `\r\n` が実体化した）。

★**「同じ日に 3 回（開発側 1・レビュー側 2）というのは偶然ではない」** — ★**同意します。** ★**機械でテキストを編集する作業が、この案件では日常になっているからです。**

---

## 5. 🔴 ★私の取りこぼし — **D-115 の「1 日 480R」が古い**

✔ 正典 D-115 ⑥ の註記:

> ★通知は自馬が出走するレースだけ（★**D-007 改訂で 1 日 480R になるため**、全レースの通知は離脱を招く）

🔴 ★**6 分に決め直したので、★正しくは 1 日 240R です。**
★**私が D-115 を起票したとき（3 分の時点）の数字が、そのまま残っていました。**
→ ★**§6 で直します**（★結論〔通知は自馬だけ〕は変わりません。★根拠の数字だけ）。

---

## 6. 🔴 オーナーの承認が要るもの

| # | |
|---|---|
| **1** | ★**D-115 ⑥ の「1 日 480R」→「1 日 240R」の訂正**（§5。★**結論は変わらず、根拠の数字だけ**） |

---

## 7. 開発側が次にすること

★**UI1-1〜UI1-7 に着手してください**（★UI1-6・UI1-7 が今回足した分）。★**新しい移行を起こす**（`0031` は書き換えない）という判断も正しいです。

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
