# 報告 — AL-9（1 日の本数をサイクルから導く）／ 測定器を `tools/` へ移しました（2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**指示**: 裁定 `REVIEW_CONSULT_ARCADE_LOOP_ANSWER_20260918.md` **AL-9**（★便の順の 1 番目）／ ★レビュー側の依頼「**測定器を `tmp/` から `tools/` へ**」
**前提**: **AL-6 は完了**（`REPORT_AL6_ODDS_COST_20260918.md`・`08aa1e4`。★レビュー側が独立再実行で確認済み）

> 凡例（R-13）: **✔** ＝ 実測 ／ **△** ＝ 未確認 ／ **🔴** ＝ 判断待ち

---

## 1. ✅ AL-9 — `RACES_PER_DAY` の直書きをやめました

| | 前 | 後 |
|---|---|---|
| `packages/scheduler/src/programme.ts:16` | `export const RACES_PER_DAY = 144;`（★註記だけが「10 分サイクル × 24 時間」と由来を述べていた） | ★**`DAY_MS / CYCLE_MS` から導出** |
| いまの値 | 144 | ✔ **144（変わりません）** |
| 3 分にしたら | ★**144 のまま**（★黙って古い値が残る） | ✔ **480 が自動で出ます** |

★**割り切れないサイクル長は投げます**（R-27「既定と縮退は狭い側・安全な側へ」）。★端数のまま進むと `classOf` の枠割りが 1 日の途中でずれます。

### ✔ 変異で確かめました（★両方向）

| 操作 | 結果 |
|---|---|
| `CYCLE_MS` を 3 分にする | ✔ `RACES_PER_DAY` が **480** に変わり、★**scheduler の検査 5 ファイルが赤**（終了コード 1） |
| 戻す | ✔ **緑**・`diff` で**差分なし** |

→ ★**T-13（3 分）に入ったとき、`RACES_BY_CLASS`（合計 144）の振り直しを忘れたら検査が止めます。** ★気づかずには進めません。

⚠️ ★**この便では `RACES_BY_CLASS` を触っていません**（★合計 144 のまま）。★T-13 で一緒に振り直す対象です。

---

## 2. ✅ 測定器を `tools/` へ移しました（★レビュー側の依頼）

| 道具 | 中身 |
|---|---|
| `tools/bench-odds-entrants.mjs` | ★出走馬を JSON に出す（★**両方の版が同じ入力を読む**ため・R-30） |
| `tools/bench-odds-cost.mjs` | ★1 レースのオッズ計算の所要を、★**距離 5 × 走路 2** で測る。★`--legacy` で D-071 前のツリーでも動く |

★**`.mts` ではなく `.mjs` で書きました。** ★`apps/cli/test/tool-guard.test.ts` の走査は **`.mjs` しか見ない**ので、
★`.mts` だと ★**分類登録簿（R-24）の外に出ます** — ★本日「登録簿の外で増えた」事例が 3 件挙がったばかりなので、★**4 例目を作りませんでした**。
✔ `tools/lib/classification.mjs` の `READONLY` に登録済み（✔ `tool-guard` 緑）。

★道具自身に**測り方の警告**を埋めました:

- ★**P コアに固定して回す**（`start /affinity 3`）— ★ハイブリッド CPU では載るコアで **2 倍**変わります（✔ 実測 25.97 対 42.22 μs）
- ★**各条件 `--repeat` 回の最小値**を採る
- ★**1 回の値で比を論じない。★測定のばらつきより小さい差を「差」と読まない**（★レビュー側が ES-6 裁定の「1.28 倍」を撤回した理由をそのまま書きました）

✔ 移した後に実行して動作を確認しました。⚠️ ★**その実行でも外れ値が 1 つ出ました**（50,000 試行 × 2 回で、最長の周 1200m だけ 54.92 μs）。★**道具の警告どおりの現象**で、★報告に使った値は 100,000 試行 × 3 回のものです。

---

## 3. ✅ 検証

| # | |
|---|---|
| 1 | 型検査 ✔ **終了コード 0** |
| 2 | 検査一式 ✔ **2,079 本中 2,078 本が緑**（★終了コード 1）。🔴 落ちた 1 本は `packages/render/test/edit-grammar-audit.test.ts` ⑨ ＝ **既知の赤**（✔ この便は `packages/render` を触っていません） |
| 3 | ★登録漏れを検査が捕まえた | ✔ `DAY_MS`（★AL-9 で置いた単位換算）を較正登録簿が検出 → ★**EXEMPT に理由付きで登録**して緑（★**赤→緑の両方を見ています**） |

---

## 4. ★次にできること・待っているもの

| | |
|---|---|
| 🔴 **待ち** | ★**起票 7 件の承認**（特に **D-007 の改訂＝3 分・80 サイクル**）。★これが出るまで **T-13 の実装に着手しません**（★正典の改訂が先・裁定 §6 の順） |
| ★待たずにできる | ★**クラス分けの便の下ごしらえ**（★ただし着順に効くので、★指示が出てから着手します） |
| ★オーナーの手元 | ★V-10 を 1 本 ／ push ／ 本番へ `0021`〜`0032` を 1 回の配備で |
| ★配備のとき | ★**配備後の最初の数周を実測**（★これで R-28 の最後の条件が閉じます・AL-6 の換算を本番で確かめる唯一の方法） |
