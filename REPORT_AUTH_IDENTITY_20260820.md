# 報告（Q-AUTH-06/07 の裁定を実施）— 2026-08-20

**宛先**: レビュー側／オーナー
**発行**: 開発側
**対象**: `REVIEW_AUTH_IDENTITY_VERDICT_20260820.md` の「次の一歩」1〜5
**テスト**: **917 件 PASS**（前便 910 ＋ #13 の 7 件）／`npm run typecheck` クリーン／**staging の DB 検証 10 件 全件合格**

---

## 0. 🔴 最優先 — 環境の照合を入れたら、初回で実物を捕まえました

裁定の条件1（**「向き先を確認する」を人間の注意力に任せない**）に従い、`tools/migrate.mjs` に §14.6 の照合を追加しました。**その1回目の実行で止まりました。**

```
$ npx tsx tools/migrate.mjs --env local 0017
接続先: secrets.local.env
接続しました
Error: 環境が一致しません: secrets.local.env は "development" のつもりですが、
       接続先の DB は "production" です。**適用しません**（§14.6・A-7）。
```

### 何が起きているか

| 事実 | 出典 |
|---|---|
| `secrets.local.env` は **`STAR_ENV=development`** と申告している | ファイル内 |
| **その接続先の DB は `app_environment = 'production'`** | 上記の実行結果 |
| `secrets.staging.env` は `staging` を申告し、**DB も `staging`**（一致） | 同じ照合で確認済み |
| **`tools/migrate.mjs` の既定は `--env local`**（`flag('env') ?? 'local'`） | `tools/migrate.mjs:43` |
| **`tools/lib/env.mjs` の注記は「★既定は本番のまま」**と書いている | `tools/lib/env.mjs:9-14` |

### ★つまり

**`secrets.local.env` は本番の資格情報を持ちながら、ファイルの中では `development` を名乗っています。**
`tools/lib/env.mjs` の注記（「既定は本番のまま」＝既定は `secrets.local.env`）が、**それを本番だと明言しています。**

**そして `migrate.mjs` は引数なしで実行すると、そこへ全マイグレーションを適用する既定でした。**

**★これは D-042 の記述とも整合します** — 08-10 時点で「Supabase の認証情報が**本番1組のみ**」であり、`secrets.local.env` の作成日時は **08-10 08:31**。**本番の資格情報から作られ、名前とラベルだけが `local` / `development` になった**と読めます。

### 開発側では直しません

**どちらが誤りかで対処が正反対になるためです。**

| 見立て | 直し方 |
|---|---|
| ファイルが本番を指しているのが正（名前が誤り） | `STAR_ENV=production` に直し、**ファイル名も `secrets.production.env` に改める**。ただし**その瞬間、既定で本番にマイグレーションが当たる経路が復活します** |
| `app_environment` の宣言が誤り | development プロジェクトの宣言を直す。ただし**本番の宣言を書き換える操作**になりうるので、実物の確認が先 |

**★私が `STAR_ENV` を書き換えれば「一致した」ことにはできますが、それは照合を黙らせるだけで、向き先は何も変わりません。**（R-16 の「機構を止めると通る」そのものです。）

**→ オーナーとレビュー側の判断を仰ぎます。** 併せて **`migrate.mjs` の既定が `local`（＝この文脈では本番）であること**の是非も見ていただきたいです。

### この件の影響で、裁定の手順を1つ入れ替えました

裁定は「**development で先に固め、staging で証拠を出す**」でしたが、**development が上記のとおり信用できない**ため、**staging だけで実施しました**（裁定 §2 の表に「staging も使えます」とあるため、範囲内と判断）。

---

## 1. `0017` の修正と適用（裁定 1・2）

| # | |
|---|---|
| 1 | **`user_id` を `references auth.users (id)` に変更**（D-078 改訂）。**当てる前に**直しました |
| 2 | **`on delete` の非対称を「意図」としてコメントに明記**（identities は cascade・`users` は NO ACTION。「揃っていないから揃えよう」で cascade を足すと、auth ユーザーの削除が台帳ごと利用者の資産を消す旨も書きました） |
| 3 | **PK の注記に「行が書けている前提の担保である」を追記** — 今回の穴を繰り返さないため |
| 4 | **staging に適用済み**（`環境を確認: staging（secrets.staging.env の申告と一致）` の後） |

---

## 2. V-19 の DB 側を staging で固定（裁定 2・#5/#6/#10/#15）

`tools/verify-v19-db.mjs` を新設し、**実際に叩いて**確かめました（`tools/lib/classification.mjs` に STATE_CHANGING として登録・`assertNotProduction` を通します）。

```
✅ D-078   user_id の参照先が auth.users であること — FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE
✅ D-078b  identity 側は on delete cascade
✅ #5-a    1行目は入る（対照）
✅ #5      ★同じ sub を別ユーザーで入れると落ちる — 一意制約違反 23505
✅ #10     ★同じユーザーに2つ目の line ID を足すと落ちる — 一意制約違反 23505
✅ #6-a    sub が違えば別ユーザーとして入る
✅ #6      ★メールを保持する列が無い — 列: created_at, provider, subject, user_id
✅ #15-anon          ★anon から拒否（permission denied）
✅ #15-auth          ★authenticated から拒否（実際にログインして確認）
✅ #15-pol           ポリシーが0件
=== 全件 合格（10 件中） ===
```

**#15 は「ポリシーが無いこと」（構造）だけでなく、実際に anon と authenticated でログインして叩いています**（裁定の「後からポリシーを足すと落ちる」要求）。

### ★このツール自身が2回、自分の誤りで嘘をつきました（R-11）

| # | 何が起きたか | 教訓 |
|---|---|---|
| 1 | **FK を「無し」と報告した。実際は正しく張られていた** — `information_schema.constraint_column_usage` は **`public` → `auth` のスキーマ跨ぎの参照を返さない**。権威のある `pg_constraint` に変えたら正しく出た | **検出器が壊れて、正しいものを 🔴 と出す**形。そのまま信じれば、直っているものを「直す」ところでした |
| 2 | 片付けが取りこぼし、2回目の実行が「既に登録済み」で落ちた — `auth.admin.listUsers()` の1ページ目で探していたが、**staging には合成集団の利用者が多数いる** | **「探して見つからなかった」と「存在しない」は別。** SQL で直接引く形に変更 |

どちらもコード中に理由つきで残しました。

---

## 3. 穴 A（#13）を塞ぎました — `resolve-identity.ts`

裁定の要求（**identity の insert が conflict したら、自分が作った auth ユーザーを破棄し、既存の `user_id` でセッションを出す**）を実装しました。

### ★不変条件の置き方について1点、裁定より踏み込みました

裁定は「**孤児を破棄する**」ことを要求していますが、実装しながら**守るべきものはその手前にある**と考えました。

> **identity の紐付けに失敗した auth ユーザーで、セッションを出さないこと。**

孤児には identity 行が無いので、**以後のログインで到達できず、セッションも出ません**。したがって**残っていても口座にはなりません。** 破棄は後片付けであって安全性の本体ではない、という整理です。

→ **削除に失敗してもログインは通し、`orphanedAuthUser` として呼び出し側に伝えます**（黙って溜まると、いつか誰かが「掃除」で触るため）。**この判断が裁定の意図と外れていればご指摘ください。**

### 併せて — 「既に紐付いていれば auth ユーザーを作らない」

毎回作って毎回消す形にすると、**失敗するたびに孤児が出ます。** 先に引く形にしました（変異 V19-13 で固定）。

### [要確認] `uuid_v5(sub)` について

裁定が挙げた「より強い解」は**採っていません**。理由は、Supabase の Admin API が `id` の指定を受けるかを**まだ確認できていない**ため（Edge Function を書く段で確認します）。**受けるなら、そのときに切り替えられる形**にしてあります（`createAuthUser` は注入された依存なので、決定的 id を返す実装に差し替えるだけで、呼び出し側は変わりません）。

---

## 4. 変異試験 — 13件すべて検出

| 変異 | 内容 | 落ちた★テスト |
|---|---|---|
| V19-1〜10 | 前便のとおり | （前便で報告済み） |
| **V19-11** | **★競合しても自分が作った auth ユーザーを返す（＝穴 A そのもの）** | **6 件** |
| **V19-12** | 競合時に孤児を消さない | **6 件** |
| **V19-13** | 既に紐付いていても auth ユーザーを作る | **2 件** |

**13件すべてで★（振る舞い）テストが落ちました。**

---

## 5. V-19 16項目の現在地

| # | 項目 | 状態 |
|---|---|---|
| 1・2・3・7・8・16 | 署名／`aud`／`exp`／`nonce`／`iss`／**`alg` 固定** | ✅ **固定済み**（単体＋変異） |
| 11・12 | 本番経路で固定／検出器の自己検出禁止 | ✅ **固定済み** |
| 5・6・10・15 | 同一 `sub` で1ユーザー／メール突合しない／DB 一意制約／RLS 0行 | ✅ **staging で固定済み** |
| **13** | 同時ログインで孤児を残さない | ✅ **固定済み**（経路。実 DB での同時実行は staging での次段） |
| **14** | セットアップ RPC の二重実行 | 🔴 **未着手**（下記） |
| 4・9 | `state` の一度きり消費／セッション発行の認可 | 🟡 **経路のみ固定**。実体は staging（#9 は鍵のローテーション後） |

### #14 について — 着手できていません

**セットアップ RPC そのものがまだ存在しないためです。** これを書くには **D-074 の初期馬付与**（NPC 世界から同格帯・低素質を取る／素質を `user_id` から決定的に導く）の設計が要り、**正典に額や手順の記述がありません。**

**→ 次便で照会するか、指示書をいただくかしたいです。** `users` を upsert しないという制約自体は明快なので、**RPC の設計が決まれば #14 の固定は短時間で済みます。**

---

## 6. オーナーへ

| # | |
|---|---|
| **1** | 🔴 **`secrets.local.env` の向き先（§0）。最優先で見ていただきたいです** |
| 2 | **LINE アカウント喪失時の扱い**（(a) 復旧なし ／ (b) サポート手動復旧） |
| 3 | **EP 日次総発行量の上限（D-077）** の承認 |
| 4 | **[EYES] PC とスマホでの LINE ログインの往復の実機確認** |
| 5 | 🔴 **staging の service_role キーのローテーション**（#9 と本番相当データの前提） |
| 6 | 🔴 平文クレデンシャル4ファイルの整理。**★併せて、チャネルシークレット（HS256 の対称鍵）は `secrets.*.env` に置かない**（裁定 §3・検証できる者は偽造もできる） |
