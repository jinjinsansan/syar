# 【STAR】裁定: 馬の「見た目の個体差」が成立していない（Q-1〜Q-4）— 2026-09-23

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_HORSE_IDENTITY_20260923.md`（開発側 `star-aa`・`ac8506d`）
- きっかけ: オーナー指摘「全員同じ馬の見た目はユーザーから指摘を受ける」「厩舎の自分の馬とレースの馬は同じ見た目か」
- 運用: オーナー指示「承認不要で自立的に進める」。★**絵の見た目そのものは [EYES]**、★**規則との食い違いは [CODE]**

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| — | 照会のしかた（絵を焼く手を止めて測った） | ✅ **正しい**。★推測で埋めずに、正典と実装の両方を引いた |
| Q-1 | 毛色の出どころを枠番 → 馬 ID に直すか | ✅ **直す**（正典 §3 が「馬 ID から決定的に導く」と書いている）。条件 5 つ（§1） |
| Q-2 | 利用者の勝負服をどこで出すか | ✅ **(a)＋(b)**: 自分の馬が走るときも、育成・厩舎でも本人の色で描く（§2） |
| Q-3 | 育成の画面の個体差 | ⚠️ **先に測る**（4 階調の量子化がセル調に効くか）。測ってから決める（§3） |
| Q-4 | 育成とレースの絵柄を揃えるか | ⏸ **いまやらない**。★オーナー判断の材料を先に出す（§4） |
| 追加 | `0074`〜`0076` を本番に当てた件 | ⚠️ **事後の照合をする**（§5） |

---

## 1. Q-1 — ✅ 毛色は馬 ID から

✔ [CODE]:
- 正典 `design/art/art-bible.md:56`: 「★**ただし毛色は持たせる**」「鹿毛・黒鹿毛・栗毛・芦毛・青毛 の 5 種程度を、**馬 ID から決定的に導く**」／`:126`・`:157`・`:202` も同じ
- 実装 `tools/lib/dress.mjs:31`: `export const coatNameOf = (gate) => COATS[gate % COATS.length]` ＝ **枠番から**
- → ★**同じ馬が、枠が変わると毛色が変わる**。★正典と食い違っている。★**見分けるための仕組みが、見分けを壊す向きに働いている**

**決定: 毛色は馬 ID から決定的に導く**。

**条件**:
1. ★**出どころを 1 か所に**。`packages/` の純粋な関数に置き、`tools/lib/dress.mjs` と画面の両方がそれを呼ぶ（D-052）。★`gate` を受け取る形をやめる
2. ★**決定論**（憲法 §1-4）。馬 ID のハッシュから引く。`Math.random()`・`Date.now()` を使わない
3. ★焼いた素材は**毛色ごとに既に在る**（`apps/web/public/art/baked/…-bay/-chestnut/-grey/…`）。★**焼き直しは要らない見込み**。★見込みなので、画面が毛色ごとの素材を選べるかを確かめてから進める
4. ★**枠番の色（ゼッケン・`POST`・D-060）は枠のまま**。★毛色と枠色は別の役割（毛色＝個体、枠色＝その日の枠）。★混ぜない
5. ★**過去のレースの再生は、見た目だけが変わる**（着順・払戻・記録は 1 ビットも変わらない）。★報告にそう書く。★検査に「同じ馬・違う枠で毛色が同じ」「違う馬で毛色が散る」を入れる

⚠️ 枠番にした理由の文書は、開発側も私も**見つけられなかった**。★**意図があった可能性は残る**ので、報告に「探した場所」を書くこと（AU-13′）。

## 2. Q-2 — ✅ (a)＋(b)

✔ [CODE]: `users.silk_color` は `create_account` が書き（`0031:69`）、`/setup` が 16 色を選ばせるが、★**読む側が 0 件**（`apps/web/src`・`apps/worker/src`・`packages` を検索。`setup.ts:132` は書く側）。★レースの勝負服は枠と頭数から決まる（`race/page.tsx` の `silkRoleOf`）
→ ★**D-119 の族**（書く側だけ在って、読む側が無い）。★**利用者が選んだ色が一度も画面に出ていない**。

**決定**: ★**自分の馬が走るときは本人の勝負服の色で描き、育成・厩舎でも同じ色を使う**（(a) と (b) の両方）。

**条件**:
1. ★**NPC 馬は今までどおり枠から**（色の出どころを 2 つにしない。★「持ち主が居る馬だけ本人の色」という 1 本の規則）
2. ★色の出どころは 1 か所（`packages/`）。画面・レースの描画・厩舎が同じ関数を呼ぶ
3. ★**勝負服の色は、強さ・素質を一切含まない**（D-114）。★16 色は利用者が選ぶだけ
4. ⚠️ ★**本人の色が出ると、他の客から「これはプレイヤーの馬だ」と分かる**。★これは情報の漏れではない（強さは分からない）が、★**オーナーに一言伝える**（§6）
5. ★(c)（選ばせるのをやめる）は採らない。★既に選ばせて保存してあり、正典 §3 が「個性は勝負服」と書いている

## 3. Q-3 — ⚠️ 先に測る

- 「1 枚の絵の色を差し替える」で足りるかは、★`dress.mjs` の 4 階調の量子化が**セル調の陰影に効くか**次第。★開発側自身が「測っていない」と書いている → ★**測ってから決める**
- 測り方: `train-body-*` の 1 枚を 5 色に変換して並べ、★**輪郭が潰れないか・色が濁らないか**を見る。★**判断は [EYES]**（オーナーとデザイナー）。レビュー側は「測ったか」を見る
- 量子化が効かないなら、★毛色ごとに焼く（5 枚）。★レースの素材と同じ作法

## 4. Q-4 — ⏸ いまやらない

- 揃えるとレースの走行コマを数百枚焼き直すことになる。★**費用が大きく、戻すのも高い**
- ★**先に Q-1・Q-2 を入れる**。毛色と勝負服が効けば、「厩舎の馬とレースの馬が同じ個体だ」と分かる手がかりは**絵柄を揃えなくても**生まれる
- その上で、★**オーナーが「まだ別物に見える」と言うかどうか**が判断の材料。★それまで焼き直さない（★安い実験を先に置く）

## 5. ⚠️ `0074`〜`0076` を本番に当てた件 — 事後の照合

- 開発側の報告: `0074`〜`0076` は**本番適用済み**、`main` は `ac8506d`、DB は `0076` まで、ワーカーは入れ替えていない
- ★オーナーの直接の指示で動くのが決まりなので、★**当てたこと自体は開発側とオーナーの間の話**。★ただし**レビュー側の照合が後になった**
- **条件**: ①当てたときの手順書の段と実測（`--plan` の件数・関門・後の 1 周）を報告に残す ②`0074` が `request_breeding_role` を**置き換えた**点は、★**判定の中身が `0070` と同じ**ことを、こちらでも読んで確かめる（次の便で）③本番に当てる移行は、**当てる前に 1 行でよいので知らせる**（止めるためではなく、**後から「何が本番に入っているか」を 1 か所で言えるようにするため**）

## 6. オーナーへ（伝えるだけ・判断は不要）

- 毛色を馬 ID から決めるようにすると、★**本番の中継に映る NPC 馬の毛色が変わります**（結果・払戻は変わりません）
- 勝負服を本人の色で描くようにすると、★**他の人から「これはプレイヤーの馬だ」と分かるようになります**（強さは分かりません）

## 7. 付記（レビュー側の記録）

- ★この便で私は、アートバイブルをオーナー決定（`DESIGN_ART_STYLE_DECISION_20260923.md`）に合わせて直した（絵柄はセル調・「ピクセルで」は失効・整数倍の決めは残す）
- ★そのとき working tree に LF を混ぜた。✔ 調べると **`core.autocrlf=true`** で、**リポジトリの中身は元から LF**（`STAR_SPEC_v2.0.md` の blob も LF）。★**「正典は CRLF」は作業ツリーの話**で、コミットされる中身は LF に揃う。★MD-4 の註記をこの事実で更新すること

## 8. 追記（2026-09-23）— Q-3 の測定（`dacd17d`）✅ 焼き直しは要らない

- ✔ ★**私の言葉の訂正を受け入れる**: 裁定 §3 で「4 階調の量子化」と書いたが、★実装（`tools/lib/dress.mjs:233`）は**4 段に潰さず連続に補間する**形に直っていた（註記に理由あり）。★**動いている式で測った**開発側が正しい（★私は正典の古い記述を引いていた）
- ✔ 測定: `train-body-idle.png` を 5 色に置き換え、①書き換え率が 5 色とも同じ 85.0%（巻き込みも取りこぼしも無い）②色数は 4 分の 1 に減るが 4 色には潰れない ③輪郭の暗い画素は 5 色とも残る（芦毛は地が明るいので相対的に減るだけ）
- → ★**1 枚の絵の色差し替えで足りる。毛色ごとに焼かない**（§3 の条件を満たした）
- ✔ 見え方は [EYES] なので、`/design-check` に**表示寸法（136×145）で 5 色**を並べ、背景を調教ステージの地の色にした（青毛が沈まないかを見るため）→ ★**測る側が「人が見るための並べ方」まで用意している**のは正しい
- ✔ 枠番にした理由の文書は、開発側も**探した場所を列挙**して「見つからない」と書いた（AU-13′）
- 次の順（Q-1 → 素材の確認 → Q-2 → §5 の事後報告）で進めてよい。★§5 ③（本番に当てる前に 1 行）も了解された

## 9. 追記（2026-09-23）— 🔴 §1 の前提が崩れた（枠番にした理由が見つかった）

### 訂正
- ✔ 開発側が**画面側**（`apps/web/src/app/race/page.tsx:661-678`）と `packages/render/src/coat.ts:107-118` に、**2026-08-28 のオーナー要望と、その経緯の註記**を見つけた
  - ①「JRA の登録馬の毛色どおりに」（鹿毛 ~48%・黒鹿毛 ~22%・栗毛 ~15%・芦毛 ~7%…）
  - ②**隣どうしが同じ毛色にならないよう散らす**（★以前 5 種のとき「12 頭中 10 頭が同じ茶色」になり、オーナー評「馬の毛も変えてください」を受けて 7 種に増やし、差を広げた）
- → ★**枠番から決めていたのは意図的**だった。★**§1 の「理由の文書は見つからない」は誤り**。★私も `dress.mjs` と正典しか見ずに「見つからない」と書いた（★**探した場所が浅かった**のは私も同じ）
- ★開発側が**実装前に止めて報告した**のは正しい（★裁定の前提が崩れたときに、そのまま実装しない）

### 決定（暫定・オーナーの目で確かめてから確定）
**(A) を採る — 毛色は馬 ID から、★ただし実在の毛色分布の重みを保って引く。** 隣接は許容する。

- ★要望 ① は**保てる**: 馬 ID のハッシュを、鹿毛 48%・黒鹿毛 22%… の重みで 7 色へ割り振れば、**世界全体の分布は実在どおり**になる（★1 レースごとではなく、★母集団で合う形）
- ★要望 ② は**崩れる**: 隣接が起きる。★ただし ② が生まれたときの問題は「**12 頭中 10 頭が同じ茶色**」であり、★いまは 7 種で茶系の差も広げてある。★**同じ状況ではない**
- (C)（隣接したら枠内で入れ替える）は採らない: ★「同じ馬なのに、その日の枠で毛色が変わる」が残り、★**個体識別という目的を半分捨てる**。★開発側が「正典にも要望にも無い思いつき」と断ったのは正しい
- (B)（いまのまま）は採らない: ★正典 §3 に反し、★オーナーの今日の指摘（「全員同じ見た目」）にも応えられない

**条件**:
1. ★**分布を測る**: 新しい規則で 7 色の出現率が実在の割合に近いか（全頭で）。★**1 レース 12〜18 頭で隣接が何組出るか**も数える（★見込みで語らない）
2. ★**オーナーの目で確かめる**: `/design-check` に、★**新しい規則で並べた 12〜18 頭の出走表**を 1 枚出す。★「同じ馬が並んでいる」に見えないかは [EYES]
3. ★語彙を 1 つに: ★焼く道具は 5 種・日本語キー（`dress.mjs:30`）、画面と焼いた素材は **7 種・英語キー**（`coat.ts:121`・`manifest.json`）。★**画面側（7 種）に寄せる**。★`dress.mjs` も同じ関数・同じキーを呼ぶ
4. ★**正典（アートバイブル §3 の「5 種程度」）は 7 種に直す**。★これはレビュー側の仕事（この裁定が確定したら直す）
5. 条件 3（素材）は確認済み: ★焼いた素材は 7 毛色ぶん在り、画面は毛色ごとに選べる（`set.coats[coat]`・`race/page.tsx:3037`）

### Q-2 — ✅ 先に進めてよい
毛色と独立なので、勝負服（§2）は**この裁定を待たずに着手してよい**。

## 10. 追記（2026-09-23）— Q-1 段 1（`6a62097`）✅ と、繋ぐ範囲

### ✅ 受理
- ✔ **測って欠陥を見つけた**: FNV-1a だけだと uuid の末尾しか効かず、12 頭立ての隣接が **9.54 組 / 11 組**（理論 3.4）。Murmur3 の仕上げを足して 3.42 組。★**分布だけ見ていたら「実在の割合と合っている」で通っていた**（欠陥があっても分布は正しかった）
  - → ★**条件 ① に「隣接の組数」を入れたことが効いた**。★**片方の指標だけで合格にしない**（CK-16 の家族・「見たものを確かめた」）
- ✔ 分布: 7 色とも実在の割合との差は最大 **0.16 点**（10 万頭）。要望 ① は母集団で保てる
- ✔ 隣接: 12 頭 **3.42 組**／18 頭 5.25 組。対照（古い規則 1 組・等確率 1.58 組）を並べた → ★**要望 ② は保てない。古い規則の 3.4 倍**。★見込みではなく数で出した
- ✔ `/design-check` に 18 頭の出走表（`coat-field-sheet.png`）。★近似（`sharp` の `modulate`）だと明記している

### 🔴 いちばん大事な事実（照会の末尾）— 繋ぐ先
- ✔ **レースの画面は馬 ID を受け取らない**（`race/page.tsx` は固定プールのデモ・出走馬は枠番でしか識別されない）
- → ★**いま `coatOfHorseId` が効くのは 育成・厩舎・引退馬だけ**。★**隣接の 3.42 組は、まだ誰の目にも起きない**（レースが実データに繋がった日に初めて起きる）

**決定（繋ぐ範囲）**:
1. ★**育成・厩舎・引退馬に繋ぐのは、いま進めてよい**（隣接の問題は起きない・個体識別はここで効く）
2. ★**レースに繋ぐのは、この便ではやらない**。★実レースの ID を渡す工事は Q-4 と同じ束（後回し）
3. ★**隣接の是非は、レースに繋ぐ便で決める**。★そのときの材料は、いま出した数（3.42 組・古い規則 1 組）と `/design-check` の 1 枚。★オーナーの目で見てから決める
4. ★**もう 1 つの案を記録しておく**（今は採らない）: 毛色は馬 ID のまま、★**同じ毛色の中で馬ごとにごく小さな色の揺らぎ**を与えると、隣接しても「同じ馬が並んでいる」には見えにくくなる。★枠順は動かせない（枠は距離ロスに効く＝結果に効く）ので、**並べ替えで解く案は採らない**
5. `dress.mjs` の呼び替え（条件 ③ の残り）は次の便で

### ⚠️ アートバイブル（条件 ④）
★**確定したら私が直す**。★いまは「(A) を採る・隣接の是非はレースに繋ぐ便でオーナーが見て決める」までなので、★**「5 種程度 → 7 種」と「馬 ID から」の 2 点だけ先に直してよい**か、レースの件と一緒に直すかは、★**レースの件が決まってからにする**（★二度書き換えない）
