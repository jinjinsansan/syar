# 【STAR】裁定: 馬の「見た目の個体差」が成立していない（Q-1〜Q-4）— 2026-09-23

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_HORSE_IDENTITY_20260923.md`（開発側 `star-aa`・`ac8506d`）
- きっかけ: オーナー指摘「全員同じ馬の見た目はユーザーから指摘を受ける」「厩舎の自分の馬とレースの馬は同じ見た目か」
- 運用: オーナー指示「承認不要で自立的に進める」。★**絵の見た目そのものは [EYES]**、★**規則との食い違いは [CODE]**

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| — | 照会のしかた（絵を焼く手を止めて測った） | ✅ **正しい**。★推測で埋めずに、正典と実装の両方を引いた |
| Q-1 | 毛色の出どころを枠番 → 馬 ID に直すか | ✅ **直す**（正典 §3 が「馬 ID から決定的に導く」と書いている）。条件 5 つ（§1） |
| Q-2 | 利用者の勝負服をどこで出すか | ✅ **(a)＋(b)**: 自分の馬が走るときも、育成・厩舎でも本人の色で描く（§2） |
| Q-3 | 育成の画面の個体差 | ⚠️ **先に測る**（4 階調の量子化がセル調に効くか）。測ってから決める（§3） |
| Q-4 | 育成とレースの絵柄を揃えるか | ⏸ **いまやらない**。★オーナー判断の材料を先に出す（§4） |
| 追加 | `0074`〜`0076` を本番に当てた件 | ⚠️ **事後の照合をする**（§5） |

---

## 1. Q-1 — ✅ 毛色は馬 ID から

✔ [CODE]:
- 正典 `design/art/art-bible.md:56`: 「★**ただし毛色は持たせる**」「鹿毛・黒鹿毛・栗毛・芦毛・青毛 の 5 種程度を、**馬 ID から決定的に導く**」／`:126`・`:157`・`:202` も同じ
- 実装 `tools/lib/dress.mjs:31`: `export const coatNameOf = (gate) => COATS[gate % COATS.length]` ＝ **枠番から**
- → ★**同じ馬が、枠が変わると毛色が変わる**。★正典と食い違っている。★**見分けるための仕組みが、見分けを壊す向きに働いている**

**決定: 毛色は馬 ID から決定的に導く**。

**条件**:
1. ★**出どころを 1 か所に**。`packages/` の純粋な関数に置き、`tools/lib/dress.mjs` と画面の両方がそれを呼ぶ（D-052）。★`gate` を受け取る形をやめる
2. ★**決定論**（憲法 §1-4）。馬 ID のハッシュから引く。`Math.random()`・`Date.now()` を使わない
3. ★焼いた素材は**毛色ごとに既に在る**（`apps/web/public/art/baked/…-bay/-chestnut/-grey/…`）。★**焼き直しは要らない見込み**。★見込みなので、画面が毛色ごとの素材を選べるかを確かめてから進める
4. ★**枠番の色（ゼッケン・`POST`・D-060）は枠のまま**。★毛色と枠色は別の役割（毛色＝個体、枠色＝その日の枠）。★混ぜない
5. ★**過去のレースの再生は、見た目だけが変わる**（着順・払戻・記録は 1 ビットも変わらない）。★報告にそう書く。★検査に「同じ馬・違う枠で毛色が同じ」「違う馬で毛色が散る」を入れる

⚠️ 枠番にした理由の文書は、開発側も私も**見つけられなかった**。★**意図があった可能性は残る**ので、報告に「探した場所」を書くこと（AU-13′）。

## 2. Q-2 — ✅ (a)＋(b)

✔ [CODE]: `users.silk_color` は `create_account` が書き（`0031:69`）、`/setup` が 16 色を選ばせるが、★**読む側が 0 件**（`apps/web/src`・`apps/worker/src`・`packages` を検索。`setup.ts:132` は書く側）。★レースの勝負服は枠と頭数から決まる（`race/page.tsx` の `silkRoleOf`）
→ ★**D-119 の族**（書く側だけ在って、読む側が無い）。★**利用者が選んだ色が一度も画面に出ていない**。

**決定**: ★**自分の馬が走るときは本人の勝負服の色で描き、育成・厩舎でも同じ色を使う**（(a) と (b) の両方）。

**条件**:
1. ★**NPC 馬は今までどおり枠から**（色の出どころを 2 つにしない。★「持ち主が居る馬だけ本人の色」という 1 本の規則）
2. ★色の出どころは 1 か所（`packages/`）。画面・レースの描画・厩舎が同じ関数を呼ぶ
3. ★**勝負服の色は、強さ・素質を一切含まない**（D-114）。★16 色は利用者が選ぶだけ
4. ⚠️ ★**本人の色が出ると、他の客から「これはプレイヤーの馬だ」と分かる**。★これは情報の漏れではない（強さは分からない）が、★**オーナーに一言伝える**（§6）
5. ★(c)（選ばせるのをやめる）は採らない。★既に選ばせて保存してあり、正典 §3 が「個性は勝負服」と書いている

## 3. Q-3 — ⚠️ 先に測る

- 「1 枚の絵の色を差し替える」で足りるかは、★`dress.mjs` の 4 階調の量子化が**セル調の陰影に効くか**次第。★開発側自身が「測っていない」と書いている → ★**測ってから決める**
- 測り方: `train-body-*` の 1 枚を 5 色に変換して並べ、★**輪郭が潰れないか・色が濁らないか**を見る。★**判断は [EYES]**（オーナーとデザイナー）。レビュー側は「測ったか」を見る
- 量子化が効かないなら、★毛色ごとに焼く（5 枚）。★レースの素材と同じ作法

## 4. Q-4 — ⏸ いまやらない

- 揃えるとレースの走行コマを数百枚焼き直すことになる。★**費用が大きく、戻すのも高い**
- ★**先に Q-1・Q-2 を入れる**。毛色と勝負服が効けば、「厩舎の馬とレースの馬が同じ個体だ」と分かる手がかりは**絵柄を揃えなくても**生まれる
- その上で、★**オーナーが「まだ別物に見える」と言うかどうか**が判断の材料。★それまで焼き直さない（★安い実験を先に置く）

## 5. ⚠️ `0074`〜`0076` を本番に当てた件 — 事後の照合

- 開発側の報告: `0074`〜`0076` は**本番適用済み**、`main` は `ac8506d`、DB は `0076` まで、ワーカーは入れ替えていない
- ★オーナーの直接の指示で動くのが決まりなので、★**当てたこと自体は開発側とオーナーの間の話**。★ただし**レビュー側の照合が後になった**
- **条件**: ①当てたときの手順書の段と実測（`--plan` の件数・関門・後の 1 周）を報告に残す ②`0074` が `request_breeding_role` を**置き換えた**点は、★**判定の中身が `0070` と同じ**ことを、こちらでも読んで確かめる（次の便で）③本番に当てる移行は、**当てる前に 1 行でよいので知らせる**（止めるためではなく、**後から「何が本番に入っているか」を 1 か所で言えるようにするため**）

## 6. オーナーへ（伝えるだけ・判断は不要）

- 毛色を馬 ID から決めるようにすると、★**本番の中継に映る NPC 馬の毛色が変わります**（結果・払戻は変わりません）
- 勝負服を本人の色で描くようにすると、★**他の人から「これはプレイヤーの馬だ」と分かるようになります**（強さは分かりません）

## 7. 付記（レビュー側の記録）

- ★この便で私は、アートバイブルをオーナー決定（`DESIGN_ART_STYLE_DECISION_20260923.md`）に合わせて直した（絵柄はセル調・「ピクセルで」は失効・整数倍の決めは残す）
- ★そのとき working tree に LF を混ぜた。✔ 調べると **`core.autocrlf=true`** で、**リポジトリの中身は元から LF**（`STAR_SPEC_v2.0.md` の blob も LF）。★**「正典は CRLF」は作業ツリーの話**で、コミットされる中身は LF に揃う。★MD-4 の註記をこの事実で更新すること

## 8. 追記（2026-09-23）— Q-3 の測定（`dacd17d`）✅ 焼き直しは要らない

- ✔ ★**私の言葉の訂正を受け入れる**: 裁定 §3 で「4 階調の量子化」と書いたが、★実装（`tools/lib/dress.mjs:233`）は**4 段に潰さず連続に補間する**形に直っていた（註記に理由あり）。★**動いている式で測った**開発側が正しい（★私は正典の古い記述を引いていた）
- ✔ 測定: `train-body-idle.png` を 5 色に置き換え、①書き換え率が 5 色とも同じ 85.0%（巻き込みも取りこぼしも無い）②色数は 4 分の 1 に減るが 4 色には潰れない ③輪郭の暗い画素は 5 色とも残る（芦毛は地が明るいので相対的に減るだけ）
- → ★**1 枚の絵の色差し替えで足りる。毛色ごとに焼かない**（§3 の条件を満たした）
- ✔ 見え方は [EYES] なので、`/design-check` に**表示寸法（136×145）で 5 色**を並べ、背景を調教ステージの地の色にした（青毛が沈まないかを見るため）→ ★**測る側が「人が見るための並べ方」まで用意している**のは正しい
- ✔ 枠番にした理由の文書は、開発側も**探した場所を列挙**して「見つからない」と書いた（AU-13′）
- 次の順（Q-1 → 素材の確認 → Q-2 → §5 の事後報告）で進めてよい。★§5 ③（本番に当てる前に 1 行）も了解された

## 9. 追記（2026-09-23）— 🔴 §1 の前提が崩れた（枠番にした理由が見つかった）

### 訂正
- ✔ 開発側が**画面側**（`apps/web/src/app/race/page.tsx:661-678`）と `packages/render/src/coat.ts:107-118` に、**2026-08-28 のオーナー要望と、その経緯の註記**を見つけた
  - ①「JRA の登録馬の毛色どおりに」（鹿毛 ~48%・黒鹿毛 ~22%・栗毛 ~15%・芦毛 ~7%…）
  - ②**隣どうしが同じ毛色にならないよう散らす**（★以前 5 種のとき「12 頭中 10 頭が同じ茶色」になり、オーナー評「馬の毛も変えてください」を受けて 7 種に増やし、差を広げた）
- → ★**枠番から決めていたのは意図的**だった。★**§1 の「理由の文書は見つからない」は誤り**。★私も `dress.mjs` と正典しか見ずに「見つからない」と書いた（★**探した場所が浅かった**のは私も同じ）
- ★開発側が**実装前に止めて報告した**のは正しい（★裁定の前提が崩れたときに、そのまま実装しない）

### 決定（暫定・オーナーの目で確かめてから確定）
**(A) を採る — 毛色は馬 ID から、★ただし実在の毛色分布の重みを保って引く。** 隣接は許容する。

- ★要望 ① は**保てる**: 馬 ID のハッシュを、鹿毛 48%・黒鹿毛 22%… の重みで 7 色へ割り振れば、**世界全体の分布は実在どおり**になる（★1 レースごとではなく、★母集団で合う形）
- ★要望 ② は**崩れる**: 隣接が起きる。★ただし ② が生まれたときの問題は「**12 頭中 10 頭が同じ茶色**」であり、★いまは 7 種で茶系の差も広げてある。★**同じ状況ではない**
- (C)（隣接したら枠内で入れ替える）は採らない: ★「同じ馬なのに、その日の枠で毛色が変わる」が残り、★**個体識別という目的を半分捨てる**。★開発側が「正典にも要望にも無い思いつき」と断ったのは正しい
- (B)（いまのまま）は採らない: ★正典 §3 に反し、★オーナーの今日の指摘（「全員同じ見た目」）にも応えられない

**条件**:
1. ★**分布を測る**: 新しい規則で 7 色の出現率が実在の割合に近いか（全頭で）。★**1 レース 12〜18 頭で隣接が何組出るか**も数える（★見込みで語らない）
2. ★**オーナーの目で確かめる**: `/design-check` に、★**新しい規則で並べた 12〜18 頭の出走表**を 1 枚出す。★「同じ馬が並んでいる」に見えないかは [EYES]
3. ★語彙を 1 つに: ★焼く道具は 5 種・日本語キー（`dress.mjs:30`）、画面と焼いた素材は **7 種・英語キー**（`coat.ts:121`・`manifest.json`）。★**画面側（7 種）に寄せる**。★`dress.mjs` も同じ関数・同じキーを呼ぶ
4. ★**正典（アートバイブル §3 の「5 種程度」）は 7 種に直す**。★これはレビュー側の仕事（この裁定が確定したら直す）
5. 条件 3（素材）は確認済み: ★焼いた素材は 7 毛色ぶん在り、画面は毛色ごとに選べる（`set.coats[coat]`・`race/page.tsx:3037`）

### Q-2 — ✅ 先に進めてよい
毛色と独立なので、勝負服（§2）は**この裁定を待たずに着手してよい**。

## 10. 追記（2026-09-23）— Q-1 段 1（`6a62097`）✅ と、繋ぐ範囲

### ✅ 受理
- ✔ **測って欠陥を見つけた**: FNV-1a だけだと uuid の末尾しか効かず、12 頭立ての隣接が **9.54 組 / 11 組**（理論 3.4）。Murmur3 の仕上げを足して 3.42 組。★**分布だけ見ていたら「実在の割合と合っている」で通っていた**（欠陥があっても分布は正しかった）
  - → ★**条件 ① に「隣接の組数」を入れたことが効いた**。★**片方の指標だけで合格にしない**（CK-16 の家族・「見たものを確かめた」）
- ✔ 分布: 7 色とも実在の割合との差は最大 **0.16 点**（10 万頭）。要望 ① は母集団で保てる
- ✔ 隣接: 12 頭 **3.42 組**／18 頭 5.25 組。対照（古い規則 1 組・等確率 1.58 組）を並べた → ★**要望 ② は保てない。古い規則の 3.4 倍**。★見込みではなく数で出した
- ✔ `/design-check` に 18 頭の出走表（`coat-field-sheet.png`）。★近似（`sharp` の `modulate`）だと明記している

### 🔴 いちばん大事な事実（照会の末尾）— 繋ぐ先
- ✔ **レースの画面は馬 ID を受け取らない**（`race/page.tsx` は固定プールのデモ・出走馬は枠番でしか識別されない）
- → ★**いま `coatOfHorseId` が効くのは 育成・厩舎・引退馬だけ**。★**隣接の 3.42 組は、まだ誰の目にも起きない**（レースが実データに繋がった日に初めて起きる）

**決定（繋ぐ範囲）**:
1. ★**育成・厩舎・引退馬に繋ぐのは、いま進めてよい**（隣接の問題は起きない・個体識別はここで効く）
2. ★**レースに繋ぐのは、この便ではやらない**。★実レースの ID を渡す工事は Q-4 と同じ束（後回し）
3. ★**隣接の是非は、レースに繋ぐ便で決める**。★そのときの材料は、いま出した数（3.42 組・古い規則 1 組）と `/design-check` の 1 枚。★オーナーの目で見てから決める
4. ★**もう 1 つの案を記録しておく**（今は採らない）: 毛色は馬 ID のまま、★**同じ毛色の中で馬ごとにごく小さな色の揺らぎ**を与えると、隣接しても「同じ馬が並んでいる」には見えにくくなる。★枠順は動かせない（枠は距離ロスに効く＝結果に効く）ので、**並べ替えで解く案は採らない**
5. `dress.mjs` の呼び替え（条件 ③ の残り）は次の便で

### ⚠️ アートバイブル（条件 ④）
★**確定したら私が直す**。★いまは「(A) を採る・隣接の是非はレースに繋ぐ便でオーナーが見て決める」までなので、★**「5 種程度 → 7 種」と「馬 ID から」の 2 点だけ先に直してよい**か、レースの件と一緒に直すかは、★**レースの件が決まってからにする**（★二度書き換えない）

## 11. 追記（2026-09-23）— Q-2（`9948d44`）✅ と、見本の作り方の訂正（`d9411ce`）

### ✅ Q-2 受理
- ✔ `packages/render/src/silks.ts` に 16 色・袖 3 択・`ownerSilksOf` / `silksForHorse`。★**出どころは 1 か所**（条件 2）
- ✔ `silksForHorse` は**持ち主が居る馬だけ本人の色**、NPC は `null`（＝枠から・条件 1）。★**枠番を受け取らない形**にし、★検査で `gate` が現れないことまで縛った（★「混ぜない」を構文で固定した）
- ✔ `setup.ts` は定義を手放し再輸出だけ。✔ `stable-repo` が `users.silk_color` / `silk_sleeve` を読む → ★**D-119 の族（書く側だけ在った）が閉じた**
- ✔ 検査 8 件（知らない鍵でも止まらない／NPC は null／画面が色の文字列を持たない／読む側が在る）
- ⚠️ `/train` の見せ方は仮（デザイナー待ち）→ 第 2 便の成果物で確定する

### ✅ 見本の作り方の訂正（★この便でいちばん価値がある）
- 🔴 `/design-check` の毛色の見本が、**色の付け方 2 通り**（`dress.mjs` のパレット置換 ／ `sharp` の `modulate` ＝ CSS の近似）で並んでいた。★オーナーの「色違いの馬の絵が違う気がします」で発覚
- ✔ 輪郭を 1 画素ずつ比べて**食い違い 0 画素**（＝絵は同じ・色の付け方だけが違う）と測ってから直した。★**推測で「同じはず」と言わずに測った**
- ✔ 直し: 焼いた写しを消し、**1 枚の絵に画面と同じ CSS filter を掛ける**形に統一
- ★**規律として残す**: 🔴 **見え方を人に判断してもらう材料は、画面と同じ経路で作る。** ★近似で作ったものを判断材料に出さない（★出すなら「近似である」と材料の上に書く）。★開発側が自分でそう書いたのを、こちらでも規律として採る
- ✔ 毛色 7 色は**オーナーが「7 色見分けられます」と判定**（`d9411ce` の見本で）→ ★[EYES] の判断が付いた

### 次の順（そのままでよい）
1. Q-1 段 2（育成・厩舎・引退馬に繋ぐ）。★CSS filter の組み立ても `packages/render` に寄せる（`/design-check` の中に在る組み立てを 1 か所に）→ ★正しい（D-052）
2. `dress.mjs` の呼び替え
3. §5 の事後報告（`0074`〜`0076`）
★18 頭の並びの判断は、§10 の 3 のまま（レースに繋ぐ便で・オーナーの目で）

## 12. 追記（2026-09-23）— §5 の事後報告 ✅ ／ 🔴 毛色 9 種はオーナーの前の決定を覆している

### ✅ §5 の事後報告（`8c242c1`）
- ✔ 下見・関門・当てた後の実測（未適用 0・関数 7/7・出る列も一致・表と行には触れていない）
- ✔ ★**当てる前に「いま本番で動いているコードが呼んでいないか」を調べた**（返り値の型を変える移行だから）。`main`（`86040e0`）の `apps/web` に参照 0 件・ワーカーは直接 SQL で呼ばない → ★これが**いちばん効く確かめ方**
- ✔ ②（`0074` が `0070` を置き換えた件）: 判定の中身は同じ・変えたのは数の出どころと置き場所。staging の予行 9/9（`0070` と同じ結果）＋読む口の理由と RPC の結果を 5 通り突き合わせ
- ✔ 反省 2 つ（知らせが後になった・`0073` が半日未適用で残っていたことに気づいていなかった）を自分で書いた
- ⚠️ **ワーカーの 1 周は未測定**（経路に触れていないという判断）。→ ★**次の週送りの周のログで「失敗 0」だけ確かめて報告**（安い。★「触っていないから見ない」を残さない）

### 🔴 毛色 9 種（`3533832`）— 受理を保留し、オーナーへ返す
✔ [CODE] `packages/render/src/coat.ts:186-196` の新しい重み: `bay 30 / dark-bay 16 / chestnut 16 / grey 10 / liver-chestnut 8 / seal-brown 8 / blue-black 6 / palomino 3 / white 3`

**なぜ保留するか — 2 つとも、オーナー自身の前の決定を覆しているため**:
1. 🔴 **実在の割合から離れる**。2026-08-28 のオーナー要望 ①「JRA の登録馬の毛色どおりに」に対し、★鹿毛は実在 ~48% → **30**、黒鹿毛 ~22% → 16。★**前の便で「差は最大 0.16 点」と報告した長所が消える**
2. 🔴 **白毛を入れる**。`coat.ts:107-118` の註記は、★**白毛を入れない理由を書いている** — 「実在の登録頭数で 0.1% 未満の珍しさで、12 頭立てに 1 頭いると『珍しい』ではなく『変』になる」。★新しい重みは **3%**（★12 頭立てなら 3 割の確率で 1 頭は白毛）

**得られるもの**: 隣接が 12 頭 3.42 → **1.90 組**（古い規則は 1 組）・1 組も無いレースが 2% → 13.6%。★数はきちんと測ってある

→ ★**「実在の割合」と「隣接の少なさ」は両立しない**。★どちらを取るかは**遊びの手触りの判断**で、★しかも**オーナーが前に決めたことを変える**ので、★レビュー側では決めない。★§13 の文面でオーナーに返す。★それまで **9 種は入れたままでよい**（育成・厩舎・引退馬にしか効かず、レースは枠番のままなので、戻すのは重みの表 1 つ）

⚠️ ★`COAT_TRANSFORMS` に足した `palomino` / `white` の値は**未確認**（型のために置いた）。★焼いた素材にも 2 種は無い。★**レースに繋ぐ日に、焼く工程で 2 種を足す**必要がある。★このことを忘れないよう、簿に 1 行起票すること

### たてがみ — ⏸ オーナー決定待ち（レビュー側の見立ては開発側と同じ）
- ✔ レースの走行コマに**たてがみの層が無い**（焼いた素材の目録は `coats` と `shadow` だけ）
- ★**育成だけ先に出さない**（デザイナーの指示・開発側の判断）で正しい。★**片方だけ足すと「別の馬に見える」が増える**
- ★作るなら「次にレースの絵を焼き直す日にまとめて」。★Q-4 の束（後回し）と同じ日になる

### ✅ 次の作業 — 命名の画面へ戻るのは可
- ★**可**。たてがみと毛色の決定を待つあいだ、★**血統の輪を閉じる（命名）ほうが先**。★画面が無いために「本番に入っているのに誰も使えない」機能が並んでいるので、そこを縮める

### 開発側が自分で挙げた 3 つの誤り
★3 つとも**自分で書いている**（見本を 2 通りの方法で並べた／たてがみの色を自分で選んだ〔2026-09-15「デザインは必ずデザイナーに」を破った〕／`/design-check` の見出しの数字を直し忘れた）。
★**見出しの数字の直し忘れ**は、★「値だけ替えて、その値を説明している文を直さない」形（★この案件で何度も出ている・D-053 の族）。★**画面に数を書いたら、その数の出どころを画面が計算するか、書いた数を検査で縛る**こと。

## 13. 追記（2026-09-24）— 週送りの周・簿・命名の画面 ✅ ／ オーナーが一周触れるようにする方法

### ✅ 受理
- ✔ 週送りの周（本番・読むだけ・`7ac05b5`）: 週 278 → 279 に進み、日次の枝 ok 10 / 失敗 0、レースの滞留なし
  - ★**道具が自分の限界を出力する**ようにした点が重要 — 「これは**DB に残った跡**であって、前の配備報告の『失敗 0』（ログの行）と**同じものではない**」。★**測ったものと測れていないものを、道具自身が言う**形
- ✔ 簿 `COAT-PALOMINO-WHITE-NOT-BAKED`（戻ってくる条件を「レースの毛色を馬 ID から引く日」にした＝日付だけで留めていない）
- ✔ 命名の画面（`b2ee89a`）: 形の判定を画面に持たずワーカーと同じ関数を呼ぶ／「使えます」と出さない／「あとから変えられません」と書かない（裁定どおり）／★**ワーカーの理由を型から読み、分類漏れ 0 件を検査**（★理由が増えた日に画面が黙って落とさない形）
- → ★**配合 → 誕生 → 命名 → 厩舎 → 育成 → レース → 引退 → 役割 → 配合 の輪が、画面として一周つながった**

### オーナーが一周触れるようにする — ★**まず staging で**（本番を書き換えない）
開発側の案（本番の NPC 引退馬を 2 頭 オーナーへ移す）は、★**本番のデータの書き換え**。★先に**書き換えずに済む道**を試す。

1. ★**第 1 案（推奨・本番に触らない）**: この PC で `npm run dev:web` を **staging に向けて**動かし、オーナーがそこで一周触る。★staging には `0060`〜`0076` が当たっており、★世界のデータも在る。★**引退馬も配合も staging のものを使えばよい**
   - 条件: ①`apps/web` が読む環境を staging に向ける（`secrets.staging.env` の値・★**本番の値と取り違えない**）②オーナーの端末で前景で動かす（★Claude の中から長く動かさない）③終わったら止める
2. ★**第 2 案（本番を書き換える）**: どうしても本番で触りたい場合のみ。★**オーナーの直接の指示が要る**。条件:
   - ★移すのは **NPC の功労馬**（`retirement_role = 'honored'`・`owner_id is null`）から。★**繁殖牝馬・種牡馬からは取らない**（NPC の供給の枠を削るため・D-079 ⑧ と同じ考え）
   - ★**初回の配合の母の候補から 2 頭減る**（案 B の候補は功労馬）。★1,897 頭に対して 2 頭なので実害は無いが、**記録に残す**
   - ★道具で行い（手で `update` しない）、★下見 → `--apply`・本番の関門・**前後の頭数を数えて残す**
   - ★**オーナーの口座が要る**（いまオーナーは本番に口座が無い）。★登録は開いているので作れるが、★**作った口座は `legacy` ではなく新しい流れになる**（`0067` の後なので `create_account` が `first_horse_v1` を書く）→ ★**初回の配合も試せる**

### 次の作業
★`dress.mjs` の呼び替え（§9 条件 ③ の残り）で構わない。★第 1 便の命名の意匠が届いたら、そちらを優先する。

## 14. 追記（2026-09-24）— 条件 ③ は「書き換えない」が正解（`af56416`）✅

- ✔ 経路を辿った結果: **本番のレースは `tools/bake-race-frames.mjs` → `public/art/baked/` で、`@star/render` の `COAT_TRANSFORMS` を直接呼んでいる**（`:37`・`:174`・`:187`）＝ ★**出どころは既に 1 か所**。`dress.mjs`（日本語 5 色・枠番）の出力を使うのは**開発用の `/watch` だけ**
- → ★**条件 ③ は、書き換えではなく「既に満たされている」ことの確認で閉じる**。★`dress.mjs` は枠ごとに焼く道具で**馬 ID を受け取れない**ので、書き換えは筋が悪い。★**事実を註記として残した**のも正しい（次に同じ調査をさせない）
- ★`/watch`（開発用の画面）を捨てるかは**オーナー判断**として残してよい。★急がない
- ✔ 併せての気づき: `COAT_TRANSFORMS` が 9 種になったので、`bake-race-frames.mjs` は既存の目録（7 種）と**混ぜると関門で止まる**（`:199-200`）。★**気づかずに壊れない形**。簿の戻ってくる条件にも含めた

### 🔴 次にやること（手が空いているあいだ）— 接続先を目で見えるようにする
開発側の懸念（★**この PC の開発サーバーが、いま本番の DB に繋がっている**／`apps/web/.env.local` が production）は、★**記憶にある「既定が本番」の族**そのもの。★オーナーに staging を触ってもらう前に、ここを先に直す。

1. ★**開発で動かしたとき、画面に接続先を出す**（`production` / `staging` の帯。★**本番の配信では出さない**形にする — 例: `next dev` のときだけ、または環境変数が production 以外のときだけ）
2. ★**起動時に、繋ぐ先のホストを標準出力に 1 行出す**（★人が見て確かめられるように）
3. ★**検査**: ①本番の配信に帯が出ないこと ②帯の文字が環境変数から来ること（★書き換えた値を印刷するのではなく、★**実際に繋いだ先**から出す）
4. ★これが入ってから、オーナーに staging の一周を渡す（★裁定 §13 の第 1 案の条件 ① を、これで機械にする）

## 15. 追記（2026-09-24）— 接続先の可視化（`4e9592e`）✅

- ✔ 帯は `NODE_ENV !== 'production'` のときだけ・起動時に 1 行・検査 7 件。★**実際にクライアントが使う `NEXT_PUBLIC_SUPABASE_URL` から host を出す**（名札を印刷しない）
- ✔ 検査が ★**別の変数を読んで「指す先がずれる」**まで縛っている（`supabase.ts` が読む変数名を取り出して同じ名前か見る）→ ★これがこの便の肝
- ✔ 対照つきの実測（本番ビルドの生成物に 0 件・dev の配信 HTML に 1 件）。★起動時の 1 行そのものが「この PC が本番に繋がっている」ことの証拠になっている
- ✔ 「どちらが本番かを言い当てない」判断も採る（★言い当てるには本番のホスト名をこの層に書くことになり、それ自体が名札）

### ⚠️ 足すこと（1 つだけ）— オーナーが見て分かる形にする
★オーナーはホスト名（`hfvvxw…`）を見ても、どちらの環境か分からない。→ ★**渡すときの文面に、確かめる 1 行を書く**:
> 「画面の下の帯に **`<staging の host>`** と出ていることを確かめてから触ってください。**`hfvvxwoulrjqznperici…` と出ていたら本番なので、触らずに知らせてください。**」
★これは**コードに名札を入れずに、人が確かめられる**形。★開発側が渡すときの手順に入れること。

### 手が空いているあいだの優先
1. ★**staging の一周の手引きを 1 枚**（オーナー用）。★どこを押すと何が起きるか・★見てほしいもの（画面の言葉・待ち・失敗の見せ分け）・★**触っても壊れないもの／壊れるもの**の区別。★オーナーが staging を選んだ日にそのまま渡せる
2. ★本番に入っているのに画面が無い機能の**一覧**（★「作ったのに誰も使えない」を数える）
3. 第 1 便の命名の意匠が届いたら、そちらを優先
