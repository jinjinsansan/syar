# Q-SETUP-01〜04 の裁定 — セットアップ RPC と初期馬の付与

**発行**: 2026-08-20（レビュー側 Claude）
**対象**: `QUESTIONS_SETUP_RPC_20260820.md` / `93c76b7`
**判定**: **01 (c) を採用（★ただし D-074 の「決定的に導く」を撤回し、要件を置き換える）／ 02 パーセンタイル＋較正定数（★測る量は★の算出量と同一）／ 03 ①戦績0の述語を再利用・②(a)＋(c)（★広げたことを数える）・③明示的に許可（★影響は見立てより小さい）／ 04 (a) 共通関数＋走査型メタテスト**

---

## 0. ★まず、D-074 の一節を撤回します

> **★素質は `user_id` から決定的に導く** — 引退させて引き直しても**同じ馬が出る**ので、振り直しが成立しない

**これは私が書いた文で、実装できません。指摘のとおりです。**

1. 再付与の発火条件が「**現役馬が0頭**」＝ **その馬は引退済み**。引退を取り消せば繁殖の系譜と矛盾する
2. プールは**有限で共有**。決定的に1頭を指すと**衝突したとき渡せず**、次点に落とせば**もう決定的でない**

**★そして、より本質的な誤りがあります。** 私は**手段（決定性）を要件として書きました**。守りたかったのは「**振り直しが成立しないこと**」です。**手段を書いたせいで、その手段が成立しない場面で、目的まで一緒に止まりました。**

→ **要件を目的の側で書き直します**（D-079-①）。

---

## 1. 🔴 Q-SETUP-01 — **(c) を採用します。ただし理由は開発側のものと違います**

### 結論

**(c)（全員共通の固定帯）を採ります。** ただし **(c) が振り直しを防ぐ、という説明は成立していません。**

> 開発側: **(c) なら決定性は自明に満たされ（誰が何回引いても同じ）**

**帯に幅がある限り、何回引いても同じにはなりません。** 引くたびに帯の中を標本抽出します。**幅があれば、繰り返せば良い個体に当たります。** (c) の防御力は「決定性」ではなく「**帯の狭さ**」に依存しています。

### ★では何が振り直しを止めるのか — **観測できる差が無いこと**

**振り直しは、良し悪しが見えるときにだけ成立します。**

**§12.4 は `potential` の数値を出さず、★1〜5 に丸めます。本人にも数値を見せません。** ということは —

> **初期馬の★が全個体で同一なら、プレイヤーには「引き直して良くなった」が観測できません。**
> **観測できない差は、戦略になりません。**

**これが要件です。決定性ではありません。**

### D-079-① として置き直します

| # | 要件 |
|---|---|
| 1 | 初期馬は**全員共通の帯**から取る（`user_id` のハッシュを持ち込まない） |
| 2 | **★帯は、全個体が §12.4 の同じ★表示になる幅に収める**（＝ 振り直しても表示が変わらない） |
| 3 | **再付与で得られる馬の素質分布が、初回と同じであること**（回数を重ねても期待値が上がらない） |
| 4 | ゲート: **★表示が全個体で同一であること**をテストで固定する（帯を広げた瞬間に落ちる） |

**★4 が要です。** 帯は較正定数なので後から動きます。**動かしたときに「振り直しが成立し始めた」ことが自動で分かる形**にしておかないと、D-074 は静かに死にます（R-16）。

### `user_id` のハッシュ（案 b）を採らない理由

**開発側の指摘が正しいです** — 「登録した瞬間に、自分では動かせない優劣がつく」。**これは振り直しへの動機そのものを作ります**（変えられないと分かるほど、アカウントを作り直したくなる）。**入口の摩擦で複垢を抑えると決めた D-077 と逆向きに働きます。**

**(c) は §10.5「同じ土俵にいることが公正性の担保」とも、憲法 §0.3 の「無償で始められる」とも噛み合います。個体差は配合と育成で作る** — この整理に同意します。

---

## 2. 🔴 Q-SETUP-02 — 「低めだが外れではない」

### ① 何で測るか — **★の算出に使っている量と、同じ量で測ってください**

**新しい指標を定義しないでください。** ★表示の算出に使っている量がそのまま帯の判定量です。

**理由**: 2つの量を持てば必ず離れます。この案件は同じ形で何度も失敗しています — `CLAMP_TRUNCATION_FACTOR` の二重管理（L-2）、生成側と確定側で別々に引いていたレース条件（D-052）、`loadTrainingStates` の絞り込み（P3 配備時）。**「帯の判定」と「プレイヤーに見える★」が別の量から出ると、帯は満たすのに★が割れる、が必ず起きます。**

### ② 帯の位置 — **パーセンタイルで指定してください**

**開発側の意見に同意します。** 絶対値で書くと、世界が成熟して集団の水準が動いたとき帯が意味を失います（**D-053「較正条件は定数の意味の一部」と同じ形**）。

**★具体的な数値は、私からは出しません。** 導出できない数値を指示書に書かない、はこの案件で3回失敗した末に自分に課したルールです。**代わりに、満たすべき結果を書きます。**

### ③ 較正定数として扱うか — **はい。値をゲートにしません**（D-075・V-14 と同型）

**帯の値ではなく、次の2つをゲートにしてください。**

| | 基準 | 意味 |
|---|---|---|
| **上** | **★表示が全個体で同一**（§1 の要件4） | 振り直しが成立しない |
| **下** | **★初期馬が、適切な育成でキャリア中に少なくとも1勝できること** | 「外れではない」の実体。**絶望的でない**とはこういうこと |

**★下限をこう置く理由**: 「下位25〜40パーセンタイル」のような位置は、**集団が動けば意味が変わります**。一方「**キャリアで1勝**」は**プレイヤーが実際に受け取る体験**であり、集団が動いても意味が変わりません。**測る手段は既にあります**（レースエンジン＋育成シミュレーション。V-14 が「引退時の素質開放率」を測っているのと同じ道具）。

**帯の値は、この2つを満たすように較正してください。**

---

## 3. 🟡 Q-SETUP-03 — 候補集合・枯渇・NPC への影響

### ① 「同格帯」＝ デビュー前 — **既にある述語を再利用してください**

**「新馬・未勝利相当（まだデビューしていない若い馬）」という理解で正しいです。**

**★ただし、ここでも新しい定義を作らないでください。** **番組表が新馬戦の出走資格を判定するのに使っている述語が、既にあるはずです**（§10.3）。**それをそのまま使ってください。** 無ければ**先にそれを1か所に切り出してから**、両方がそれを呼ぶ形にしてください。

**理由は §2-① と同じです。** 「初期馬の候補」と「新馬戦に出られる馬」が別々に定義されると、**付与された馬が新馬戦に出られない**という噛み合わせ事故が、いつか静かに起きます。

### ② 枯渇したとき — **(a) を既定に、(c) を併せて。ただし条件が1つあります**

**(b) は却下します** — 「最初の1頭が来ない」は **D-074 が避けようとした離脱そのもの**です。開発側の判断に同意します。

**★(a) には条件を付けます: 帯を広げたことを、黙って行わないでください。**

- **広げた回数と、広げ幅を記録する**
- **監視項目にする**（(c) の在庫下限と同じ場所で）

**理由**: 帯を無条件に広げてよいなら、**§1 の要件4（★が全個体で同一）は在庫が薄いときに自動的に破れます。** そして**破れたことが誰にも見えません。** これは「機構は存在するが働いていない」の典型（R-16）で、**しかも今回は「働かないことが正常系に組み込まれている」**ぶん、より静かです。

→ **広げる動作は許可しますが、それは「在庫が足りていない」という警報であって、正常な運用ではありません。**

### ③ NPC を1頭抜くこと — **明示的に許可します。ただし影響は見立てより小さいです**

**§10.5 が挙げる NPC の3役割のうち、失われるのは1つだけです。**

| 役割 | 付与後どうなるか |
|---|---|
| ① 出走頭数の充填 | **失われません。** 馬は消えず、**プレイヤーの馬として同じレースに出走します**。頭数への寄与は変わりません |
| ② ベット対象の供給 | **失われません**（同上） |
| ③ 血統プールの供給 | **失われません。** 引退後は繁殖に入れます（所有者が付いただけ） |
| **④ NPC として自動的に走ること** | **★これだけが失われます** — 誰も操作しなければ出走登録されない可能性がある |

→ **実際に減るのは「NPC 厩舎が自動で回す馬の数」**であり、**世界の母数ではありません**。7,370 頭に対して当面は軽微、という見立ては妥当です。

**(c) の在庫監視は、②の「帯に合う在庫」に対して置いてください** — 母数ではなく**帯の在庫**が枯れます。

---

## 4. 🟡 Q-SETUP-04 — **(a) 共通関数。ただしメタテストは走査型で**

**(a) に同意します。** 理由も開発側のとおりです — **(b) は書き忘れが必ず出て、書き忘れは「通ってしまう」側に倒れます**（R-27: 縮退は狭い側へ、の違反）。

**★メタテストは、手書きの一覧ではなく走査型にしてください。**

> **すべての書き込み RPC を列挙し、`assert_setup_complete()` を呼んでいないものがあれば落ちる。除外は明示の登録簿でのみ許す。**

**`tool-guard.test.ts` が `tools/*.mjs` に対してやっていることと同じ形**です。**V-20 ③ とも同じ形**です。**この案件では、手書きの対象リストは3回漏れています**（Q-3 の走査境界・classification の登録漏れ・今回の anon 露出）。**新しい RPC を黙って足せなくすることが目的です。**

---

## 5. V-19 #14 — 設計は決まりました

**要求は変わりません**: **`users` を upsert しない／2回目は失敗し、トランザクション全体がロールバックする。**

**★併せて #14 に足してください**: **2回目が失敗したとき、初期 EP も初期馬も付いていないこと**を確かめること。「`users` の insert が落ちた」だけでは不十分で、**同一トランザクションだったことの確認**がこの項目の本体です（Q-WEB-04 ② の要件そのもの）。

---

## 6. 正典の改訂（提案）

| 番号 | 内容 |
|---|---|
| **D-074（改訂）** | **★「素質は `user_id` から決定的に導く」を撤回。** 実装不能（引退馬は戻せない・共有プールで衝突する）であり、**手段を要件として書いたために目的まで止まった**。→ **D-079 の形に置き換える** |
| **D-079** | **初期馬の付与を確定**（§10.5・§12.2）。①**全員共通の帯**から取る（`user_id` のハッシュを使わない）②**★帯は全個体が §12.4 の同じ★表示になる幅**に収める — **振り直しを止めるのは決定性ではなく「観測できる差が無いこと」** ③再付与で得られる素質分布が初回と同じ ④**ゲートは帯の値ではなく、上=★表示が全個体同一・下=適切な育成でキャリア中に1勝できること**（帯は較正定数。D-075・V-14 と同型）⑤**測る量は★の算出量と同一**（二重管理の禁止・L-2/D-052 の再発防止）⑥候補集合は**新馬戦の出走資格の述語を再利用**する ⑦**枯渇時は帯を広げてよいが、広げた回数と幅を記録し監視する**（無言で広げれば ② が静かに破れる）⑧**帯の在庫**に下限監視を置く（母数ではない）⑨NPC から1頭抜くことは明示的に許可 — **失われるのは「NPC として自動で走る枠」だけ**で、出走頭数・ベット対象・血統プールへの寄与は残る |
| **D-080** | **未セットアップ状態の判定は共通関数 `assert_setup_complete()` に集約し、すべての書き込み RPC の先頭で呼ぶ。★呼んでいない RPC を走査型メタテストで検出する**（除外は明示登録簿のみ）。個別実装は書き忘れが「通る側」に倒れるため採らない | §4.1 / §14.4 |
| **V-19 #14（明確化）** | 二重実行時は **`users` の insert が落ちるだけでなく、初期 EP と初期馬も付いていないこと**（同一トランザクションの確認が本体） |

---

## 7. これで書けます

**Q-SETUP-01〜04 はすべて裁定しました。** 残る数値（帯のパーセンタイル）は**較正定数**なので、**§2-③ の上下2つの基準を満たすように測って決めてください**。値を先に決めて後から正当化する形は採りません。

**★次に私がやるのは正典への反映です**（D-076〜080／V-19 16項目／V-20／R-25〜R-28）。**このセッションで確定した内容が正典に1文字も入っていない状態が続いているので、そこを閉じます。**
