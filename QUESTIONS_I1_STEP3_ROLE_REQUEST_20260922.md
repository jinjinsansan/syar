# 照会 I-1 段 3: 引退馬の役割を持ち主が変える（繁殖入り／功労馬へ戻す）— 2026-09-22

> 開発側 → レビュー側。裁定 `REVIEW_I1_RETIREMENT_ROLE_VERDICT_20260922.md` §3（Q-2・Q-3・Q-5）・§5-3。
> コードはまだ書いていません。本番には触れていません。開始時の HEAD `22a5026`。

## §0 一行

表と RPC の形は裁定どおり（別表・冪等は I-2 と同じ）で出します。**決めてほしいのは 2 点**です。
上限 5／10 を SQL と TS のどちらで数えるか（**R-1**）と、確定を同期の RPC にするかワーカーにするか（**R-2**）。

## §1 事実

| # | 事実 | 箇所 |
|---|---|---|
| E-1 | 所有上限の出どころは TS の `OWNERSHIP_LIMITS`（active 30・broodmare 10・stallion 5） | `packages/scheduler/src/ownership.ts:23` |
| E-2 | 馬の購入の RPC は現役の上限 **30 を SQL に直に書いている**。試験は **30 という字面**を固定しているだけで、`OWNERSHIP_LIMITS.active` とは突き合わせていない → TS を変えても試験は緑のまま、SQL だけが古くなる（D-052 の族） | `0025_horse_market.sql:117`、`apps/cli/test/horse-market-migration.test.ts:54` |
| E-3 | 引退を書く経路は週送りの 1 本だけ（レビュー側も走査で確認・§7） | `training-runner.ts` |
| E-4 | 生涯 8 産で繁殖牝馬を降ろす処理は年の変わり目に 1 本の update で行い、持ち主の馬にも掛かる（段 1 で意図して残した）。**本人に知らせる経路は無い** | `breeding-runner.ts:449` |
| E-5 | 生涯の記録 `horse_story_event` があり、`event_type` は check で 15 種に固定。`'retirement'` はあるが、役割の変更を表す種類は無い。文は保存せず、種類と値だけを残す（LR-4） | `0024_entry_rpc_and_story.sql:61-77` |
| E-6 | 持ち主の繁殖牝馬・種牡馬は、段 1 以降**誰の配合にも使われない**。自分で配合する経路（I-2 の `kind = 'breed'`・D-107 の種付料）はまだ無い | 裁定 §2 ④ |
| E-7 | 手放す RPC は引退馬を拒む（記録であって所有ではない・LR-2）。**繁殖入りした馬を売る・手放す経路は無い** | `0026_horse_sale.sql:92` |

## §2 案（裁定どおりの部分）

**表 `role_requests`**（`foal_requests` と同じ作り・revoke all・RPC からだけ書く）

| 列 | 中身 |
|---|---|
| `id uuid pk` | 要求 ID（画面が作る・再送で同じ値 → 二重に確定しない） |
| `user_id` | 依頼した人 |
| `horse_id` | 対象の馬 |
| `to_role` | `'stallion' \| 'broodmare' \| 'honored'` |
| `status` | `pending \| done \| failed` |
| `failure_reason` | `not_owner \| not_retired \| sex_mismatch \| same_role \| lifetime_foals_reached \| owner_limit \| internal_error` |
| `from_role` | 確定したときの元の役割（記録用） |
| `attempts` | R-2 がワーカーの場合のみ |

- 業務上の一意: **同じ馬について `pending` は 1 本だけ**（部分一意索引）。
- 判定:
  - 持ち主本人であること・引退済みであること
  - 性別に合う役割であること（牡 → stallion、牝 → broodmare。honored はどちらも可）
  - 同じ役割への変更は拒む
  - 繁殖牝馬に上げる馬は `foal_count < MARE_LIFETIME_FOALS`
  - 上げるときは上限の中（`role = 目標 and owner_id = 自分` を数える）
- 錠の順: **利用者の行 → 馬**。同じ人が 2 頭を同時に上げて上限を 1 つ超える競合を、利用者の行の錠で止める。
- 降ろすとき（→ honored）は上限を見ない。戦績・血統・生涯の記録は残す（§18）。

**生涯 8 産で降ろしたことを知らせる**（Q-3）: `horse_story_event` に種類を 1 つ足す（例 `'breeding-ended'`、`detail` は `{"reason":"lifetime_foals"}`）。
- 同じ種類を**持ち主が自分で降ろした場合**にも書くか（`detail.reason = 'owner'`）→ Q-C
- check の書き換えは新しい移行で行う（適用済みの移行は書き換えない）。

## §3 決めてほしいこと

- **R-1 上限をどこで数えるか**
  - (a) **SQL に 10 と 5 を書き、試験で `OWNERSHIP_LIMITS` と突き合わせる**（移行の本文から数を抜き出し、TS の値と等しいことを見る）。E-2 の 30 も同じ試験に入れて直す。
  - (b) ワーカーが TS の `canOwnMore` で数える（R-2 がワーカーの場合だけ取れる）。
  - 開発側の推しは **(a)**。どちらにしても数は 2 か所に書かれる。(a) なら「ずれたら落ちる」網が張れ、しかも E-2 の既存の穴も一緒に塞げる。
- **R-2 確定の置き場所**
  - (a) **同期の RPC**: 乱数を使わず、遺伝エンジンも通らない。`buy_horse` と同じく RPC の中で錠を取って確定し、結果をその場で返す。要求 ID は冪等のために残す（再送で同じ結果を返す）。
  - (b) I-2 と同じ **依頼表 ＋ ワーカー**: 画面は待ってから結果を読む。
  - 開発側の推しは **(a)**。I-2 がワーカーだったのは、配合が TS の遺伝エンジンを要するから。役割の変更はそれを要しない。(a) なら attempts・予算・止まった先頭の扱いが要らない。
  - ⚠️ 裁定 Q-5 は「冪等の作りを I-2 と同じ形で複製してよい」でした。(a) では**ワーカー側の部品を複製しない**ことになるので、確認を求めます。
- **Q-C 通知の範囲**: 生涯 8 産で自動で降ろしたときだけ書くか、持ち主が変えたときにも書くか。開発側は**両方書く**を推す。生涯の記録は「その馬に何が起きたか」の帳面で、誰が起こしたかは `detail` で分かる。
- **Q-D 年の途中の変更**: 繁殖牝馬を年の途中で功労馬へ降ろし、同じ年にまた上げた場合、`bred_this_year` はそのまま残る（年 1 回の判定は変わらない）。これで足りるか。開発側は足りると見ている。
- **Q-E 上げても配合できない期間**: E-6 のとおり、I-2 の `kind = 'breed'` と D-107 が入るまで、繁殖入りしても配合できない。**段 3 を先に出してよいか**、それとも `kind = 'breed'` と同じ便にするか。
  - 開発側の推しは**先に出す（画面では「繁殖入り」までを見せる）**。ただし「上げたのに何も起きない」期間ができるので、画面の文言はデザイナーへの依頼に含める。

## §4 着手の順（答えをもらった後）

1. 移行（`role_requests`・RPC・`horse_story_event` の種類）→ 上限の突き合わせの試験（R-1 (a) なら E-2 の 30 も入れる）
2. 生涯 8 産で降ろしたとき、生涯の記録へ書く（ワーカー）
3. staging の予行（必ず rollback）: 上限ちょうどと 1 つ超え・性別違い・同じ役割・8 産・二重送信・同時 2 頭の競合
4. 報告。取引の境界を変える場合は、呼び出す道具と偽の DB を並べる（裁定 §6 の決め）
