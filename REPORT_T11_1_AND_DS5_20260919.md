# REPORT — T11-1 ④（店が 1 か月 開いていなかった）／DS-5 ②（G1 の告知）／FK-5 ／ VP-10

- 日付: 2026-09-19
- 発行: 開発側
- 裁定: レビュー側の指示（T11-1 ④ の掃き出し・CK-7）／`OWNER_DECISIONS_20260919.md` C-1（DS-5 ②）／`REVIEW_PO4_AND_CK4_VERDICT_20260919.md` §4（FK-5）

---

## 1. 🔴 **T11-1 ④ — 「開店初日に買えない」ではなく、1 か月 開いていませんでした**

### ★何が起きていたか

`refreshMarketListings` は日次集計の枠（`if (today !== lastAggregated)`）の**中**にあり、
`lastAggregated = today` は `aggregateDay` の**後ろ**に置かれていました。
`runDailyStep` は失敗を**投げ直します**。

→ ★★**`aggregateDay` が落ちた日は、出品の更新まで一度も到達しません。**

✔ ★実測（DL-1・2026-09-19・staging）: `point_flow_daily` は **1 か月 0 行**。
→ ★★**その 1 か月、店は一度も開いていなかった**ことになります。

> ⚠️ ★**市場の候補が痩せて見えたことの説明**にもなります。
> ⚠️ ★指示は「1 日 1 回のまま ＋ 起動後の最初の周で 1 回並べる」でしたが、
>    ★**それでは直りません**（★初日しか見ていない案でした）。

### ★直した形

| | |
|---|---|
| ★出品の更新 | ★**日次集計の try/catch の外**へ。★`lastMarketDay` を**別に**持つ |
| ★厩舎の格の値段 | ★**同じ形が残っていました**（★レビュー側が数えて発見）。★外へ出し、★`runDailyStep(…, 'grade', …)` で包んだ |
| ★日付 | ★`lastAggregated` / `lastMarketDay` / `lastGradePriceDay` の **3 本**。★共有しない |

🔴 ★**`syncStableGradePrices` には穴が 2 つ**ありました:
①「aggregate が落ちると到達しない」（★出品と同じ）
②「`runDailyStep` に包まれていない」→ ★**落ちた理由が標準出力にしか残らない**（★DL-2 が塞いだはずの穴）

### 🔴 ★自分の直しの中に在った穴（★2 つとも自分で見つけました）

| ① | ★`today` / `dayIdx` を素直に `try` の手前へ出すと、★時刻の問い合わせが ★**周のループ本体の直下に裸で**置かれます。★DB が一瞬 落ちただけで `while` を突き抜け、★**ワーカーが死にます**（★A-1 を壊す形）→ ★引くところだけ囲み、★**引けなければその周は日次を見送る** |
|---|---|
| ② | ⚠️ ★`today` は失敗時 `''` のままです。★`lastAggregated` が実日付だと `'' !== '2026-09-19'` が**真**になり、★**空の日付で集計が走ります** → ★`dayIdx !== null` が要ります（★`today` だけ見ていると素通り） |

### ⚠️ ★決めていないこと

★**棚の並べ替えの周期は 1 日 1 回のままです**（★C-3 の問いそのもの）。
★ここで直したのは「★落ちた日に届かない」ことだけで、★**周期は動かしていません**。

---

## 2. ✅ **DS-5 ②（重賞だけ告知を先に出す）**

`packages/scheduler/src/announce-window.ts`。

| | 告知 | 実時間 | ★登録できる長さ（★締切まで） |
|---|---|---|---|
| ★**G1** | ★**`CYCLES_PER_WEEK`（40）** | ★**4 時間**（1 ゲーム内週） | ★**38 サイクル ＝ 3 時間 48 分** |
| G2 / G3 | 4 | 24 分 | 12 分（従来どおり） |
| 重賞以外 | 4 | 24 分 | 12 分（従来どおり） |

- 🔴 ★**G1 の 40 は「4 時間」と書かず `CYCLES_PER_WEEK` から引きます。** ★D-007 はサイクル長を
  10 分 → 3 分 → 6 分 と動かしており、★数で書くと**意味のほうが壊れます**。
- 🔴 ★**`racesToAnnounce` は `cycle.ts` から移しました**（★写しは残していません・D-052）。
  ★`programme.ts` と `week.ts` が `cycle.ts` を読んでおり、★`gradeOf` を使うには 1 段 上に置くしかありません。

### ✔ ★DS-5 の但し書き（「画面に長く並びます」）を**測りました**

✔ ★5,000 サイクル:

| | |
|---|---|
| ★1 周に告知する本数 | ★**平均 4.065 本・最大 5 本**（★**6 本は一度も出ません**） |
| ★内訳 | ★**4 本: 93.5%** ／ 5 本: 6.5% |
| ★G1 の頻度 | ★625 サイクルに 1 本（★窓は 40 サイクル）→ ★**2 本 同時には入りません** |

→ ★★**増えるのは 6.5% の周に 1 本だけ**です。★検査は `+1` ちょうどで押さえています（★緩い上限にしない・R-16）。

### ✔ ★遠い先の G1 を、締切前に組成しない

`cycle-runner.ts:394` が `nowMs < entryDeadlineMs(idx)` の間 飛ばします。
★告知を延ばしても ★**締切は動きません**（`cycleStart(N − 2)`）。★検査で両側を押さえています。

### 🔴 ★**照会 — G2・G3 の長さが決まっていません**

★DS-5 ② の文は「重賞だけ長く（★**例: G1 は 4 時間前から**）」で、★**挙がっているのは G1 だけ**です。
★G2・G3 の長さは正典にも判断にも書かれていないので、★**狭いほうへ倒しました**（★R-27・12 分のまま）。
→ ★延ばすなら、★`ANNOUNCE_AHEAD_BY_GRADE` の **1 行**を変えるだけです。★数をください。

---

## 3. ✅ **FK-5 — 全数分類にしたら、入れた瞬間に 2 件 出ました**

### ★何を変えたか

`copied-predicates.test.ts` には `WITH_PREDICATE` という ★**手で書いた 5 個の一覧**がありました。
🔴 ★**それ自体がもう 1 つの手写し**です — ★新しいメソッドが増えても、載せなければ黙って通ります。
→ ★**`interface CycleStore` から機械に数えさせます**（★14 メソッド）。
★全メソッドが `copied` / `stub` / `noPredicate` の**どれかに入るまで通れません**。

### 🔴 ★見つかったもの

| **`announcedConditions`** | ★偽物は `announcedSet.has(i) ? {...} : null` — ★`status = 'announced'` を**写している**のに、★簿にも一覧にもありませんでした |
|---|---|
| **`raceExists`** | ★偽物は `done.has(i) \|\| announcedSet.has(i)`（★どちらの段でも真）。★これは「本物に status の条件が**無い**」ことの写しです。★★**「無い」も写しです。** |

### ✔ ★実 DB の対を足して、通しました

`tools/verify-d117-fill.mjs --env staging`（★③.5 / ③.6 / ⑦ を追加）:

```
--- ③.5 公示の条件を読み直す（announcedConditions の SQL そのまま・★FK-5）---
  ✅ 公示中は条件が読める / ✅ 公示のときの馬場をそのまま返す（good） / ✅ 凍結した走路を返す
--- ③.6 raceExists は status を見ない（★FK-5）---
  ✅ announced の段でも 1 と数える（1） / ✅ 対照: 無い番号は 0（0）
--- ⑦ 組成が済んだら条件は読めない（★述語そのもの・FK-5）---
  ✅ scheduled になったら 0 行（0） / ✅ 対照: raceExists は scheduled でも 1（1）
✅ 全部通りました / ✅ 戻りました（取引 23201 のまま・★途中の commit なし）
```

✔ ★後追いで確かめました: `cycle_index >= 100000` のレース **0 件** ／ `ACTIVE_WHERE` の頭数 **7,333**（★基準値のときと同じ）。→ ★**漏れていません。**

### ⚠️ ★自分の誤検出

★最初「引数を取るか」で写しを判定し、★`settleRace: async (i) => { settleLog.push(i); }` を写しと呼びました。
★**記録しているだけ**で、述語を写していません。→ ★**引き当て・分岐**（`.has(` `.get(` `?` `if` `===`）で見る形に直しました。
⚠️ ★**簿を水増しするところ**でした。

---

## 4. ✅ **CK-7 — 2 点間の切り出しをやめました**

`apps/cli/test/lib/ts-blocks.ts`（`stripComments` / `blockBodyAfter` / `isInOwnTry`）。

★同じ直しを **4 回**しました:

| ① | 2026-09-17 | ★始点が **import 行**に当たっていた（★数十文字しか見ていなかった） |
|---|---|---|
| ② | 2026-09-19 | ★DL-2 で `await 〜(` が消えて落ちた（★意図は満たされたまま） |
| ③ | 2026-09-19 | ★T11-1 ④ で終点（出品）が枠の外へ出た → ★**終点を差し替えた** |
| ④ | 2026-09-19 | ★**差し替え先（厩舎の格の値段）も、同じ便で枠の外へ出た** |

→ ★**波括弧で切り出します。** ★中の並びが変わっても、★隣が外へ出ても、★同じものを指し続けます。

- ★`blockBodyAfter` は ★**目印が 2 か所以上あったら投げます**（★どちらを指すか決まらない）。★見つからないときも投げます（★空を返して「該当なし」にしない・R-21）。
- ★`isInOwnTry` は ★**外側の try を拾いません**。★`try/finally` は「巻き込まない」にならないので `catch` まで見ます。
- 🔴 ⚠️ ★**罠がありました**: `indexOf('try')` は ★**`entry` / `race_entries` / `country` に当たります**。
  ★この検査は `entry` だらけのファイルを見るので、★**当たっていたら切り出しが 1 つずれたまま緑**でした。★語境界で拾います。

✔ ★対照は ★**合成文字列で関数そのものを試しています**（★実ファイルに依存しません）。

### ✔ ★機械の答えが、レビュー側の指摘と一致しました

★波括弧で切り出した日次の枠（1,806 文字）:

| ★枠の中 | `aggregateDay(` ／ `recordUnlockDistribution(` ／ `recordStoryRows(` |
|---|---|
| ★枠の外 | `refreshMarketListings(` ／ `syncStableGradePrices(` |

→ ★レビュー側は**目で数え**、★開発側は**機械に数えさせ**、★**同じ答え**になりました。
→ ★さらに `runDailyStep` の枝名を**機械に数えさせる**検査を足しました（`aggregate / unlock / story / market / grade` の 5 つ）。★簿に無い枝が増えたら落ちます。

---

## 5. ✅ **CC-1 ③'（キャリア 24 → 40 のあとの測り直し）— ★予測 的中**

★予測は `0b07608` に**先にコミット済み**:「★**D と完全一致。1 ビットも動かない**」。

### ★VP-8 の素性

| | |
|---|---|
| ★日付 | 2026-09-19 |
| ★旗 | **（既定・旗なし）** |
| ★母集団 | `out/al11/pool-D-active-3000.json`（★**sha256:df10399b5c44**・3,000 頭） |
| ★標本 | 8 シード × 15,000 ＝ **120,000 レース** ／ 1,644 秒 |

### ✔ ★結果

| seed | ★D（基準値） | ★CC-1 40 戦 | |
|---|---|---|---|
| 42 | 30.88% | 30.88% | ✓ |
| 7 | 30.75% | 30.75% | ✓ |
| 2026 | 31.07% | 31.07% | ✓ |
| 31337 | 31.29% | 31.29% | ✓ |
| 1 | 30.72% | 30.72% | ✓ |
| 99 | 30.97% | 30.97% | ✓ |
| 12345 | 31.85% | 31.85% | ✓ |
| 65537 | 30.52% | 30.52% | ✓ |
| **V-4** | 31.006% | **31.01%** | ✓ **PASS** |
| **V-5** | 61.99% | **61.99%** | ✓ **PASS** |
| **V-6** | 1.14% | **1.14%** | ✓ **PASS** |

→ ★`CAREER_RACE_LIMIT` は `verify-race` の経路に入っていない、という読みが**数で確かめられました**。
→ ★★**`REPORT_BASELINE_D_20260919.md` の AL-1（「これは CC-1 より前の値です」）は、これで解消です。**
  ★**D は CC-1 ③ の後も基準値です。**

### 🔴 ⚠️ ★**「V が動かない」は「安全」ではありません**（★**CC-1 ⑤**・レビュー側の指摘）

★一致した理由は「★壊れなかったから」ではありません。
→ ★★**`verify-race` が `CAREER_RACE_LIMIT` を見ていないから**です。
→ ★★**V は、いま変えたものを、そもそも測っていません**（★PO-5 と同じ形）。

★**CC-1 ③ が動かしたのは上級の成立率**です（★24 走 88.6% → 40 走 99.0%）。
🔴 ⚠️ ★**その 99.0% は別の集団（★3,000 from `raceable`）で取ったもの**です。
→ ★**D の集団（`active` 3,000）で取り直します**（★CC-1 ⑤・未実施）。

★予測の成績: **4 打数 1 安打**（MI-2 ✅ / A→B 🔴 / B→C 🔴 / VP-9 レンジ 🔴 / CC-1 ③' ✅）。

---

## 6. 🔴 **VP-10 — 道具の画面と報告で、同じ量が別の数になっていました**

| ★道具の出力 | 「シード間の 1番人気勝率のばらつき(SD): **0.39%**」 |
|---|---|
| ★`REPORT_BASELINE_D_20260919.md` | 「シード間の SD（**0.414pp**）」 |

🔴 ★**8 本とも 1 ビット同じなのに、SD が違います。**

✔ ★原因:

| `verify-race.ts:800` | `sd()` ＝ ★**母標準偏差（÷ n）** → **0.387** |
|---|---|
| 判定に使う `standardError()` | `sdSample()` ＝ ★**不偏（÷ n−1）** → **0.414** |

→ ★**同じ「SD」という名前で、別の量が 2 つ**ありました（★n=8 で **6% 差**）。

🔴 ⚠️ ★そして `stats.ts` 自身が、`sdSample` の註記に
★**「黙って流用すると、少ない標本で SE を過小に報告します」** と書いていました。
★★**その流用が `verify-race.ts` に在りました。**

### ★どちらを使うべきか

★前後比較で欲しいのは「★もっとシードを振ったらどれだけ散るか」なので ★**不偏**です。
★母 SD を SE の元にすると、★8 本では ★**σ を 6% 大きく出します**（＝★差を有意に見せる向きに外れます）。

⚠️ ★VP-9 で出した σ（★述語 1.94σ・大きさ 0.19σ、★A↔D 1.89σ／2.04σ）は ★**不偏から計算しており、そのままで正しい**はずです。
🔴 ★ただし ★**道具の画面の 0.39 を使って再計算すると、数が合いません**。

### ★直し

★道具が ★**不偏 SD ／ 母 SD ／ 平均の SE の 3 つを、名前付きで**出すようにしました。
→ ★画面から拾った数がどちらか分からない、という状態を無くします。

---

## 7. ✅ CK-3 の番人が、今日の切り出しを捕まえました

★`npm run gate` が落ちました（★**赤 3 件・登録 2 件**）。
★落としたのは `source-slicing-guard`（CK-3）で、★**私が今日 足した切り出しに番人が無い**という指摘でした。
→ ★位置で見る形に直して解消。★**「3 回目だから作りを変えろ」と言われた機構が、同じ日に私を止めました。**

---

## 8. 🔴 まだやっていないこと

- ★**push（CI-1 ②）** … ★**外向きで取り消せない操作**なので、★伝令で来たオーナー承認を実行根拠にしていません。
  ★オーナーから直接 指示をもらうまで動きません（★技術的にも、★このセッションからの push は資格情報の窓で止まります）。★未 push は **197 コミット超**。
- ★**VT-1 ① ／ GB-1 ④ ／ WK-5** … ★次に着手します。
- ★**PR-3** … ★保留（★本番には流しません）。
- ★**撮り直し 2 本** … ★オーナーの端末が要ります（★期限 2026-09-26・★登録簿に赤のまま）。
