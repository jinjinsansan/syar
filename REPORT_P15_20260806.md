# REPORT_P15 — P1.5（NPC プリシード）中間報告

**開発側 → レビュー側** / 2026-08-06
**コミット**: `f9582d6`（緑・未 push）
**指示書**: `DEV_INSTRUCTIONS_P15_20260806.md`

> ⚠️ **これは完了報告ではありません。** 合格基準7項目のうち **1件が未実施・1件が不合格**です。
> 先に不合格のほうを書きます。

---

## 0. 先に結論

| # | 合格基準 | 結果 |
|---|---|---|
| 1 | P0 全ゲートがプリシード後の集団でも成立 | **未実施**（§4） |
| 2 | 5代血統表が埋まっている | **PASS**（42.8〜70.9%・§2） |
| 3 | 種牡馬プールの父系ラインが分散 | **❌ 3シード中1シードで割れる**（§3） |
| 4 | 同じシードから同じ血統プールが再現 | PASS（★テスト） |
| 5 | 実在競走馬名がゼロ | PASS（§5。ただし設計上の照会あり） |
| 6 | V-13 の実装 | PASS（`d845d70`・§1） |
| 7 | テスト全PASS / typecheck 0 / `any` ゼロ / 変異試験すべて防御 | PASS（286 / 0 / 0 / 30-30） |

---

## 1. 実装したもの

| コミット | 中身 |
|---|---|
| `d845d70` | **V-13**（仕掛けの巧拙が結果に出ること） |
| `9f3b1ac` | **N-1** NPC厩舎40・馬名の音節結合生成（R-15 に従い未接続で先行） |
| `f9582d6` | **N-2/N-4** 50世代プリシード・5代血統/系統集中の計測 |

### V-13

最適な仕掛けと早すぎる仕掛け（`EARLY_SPURT_METER × 1.6` 地点）の `interventionMult` の差を測ります。

```
V-13  仕掛けの巧拙が結果に出る（最適 − 早仕掛けの倍率差）   0.20   PASS （下限 0.03）
```

変異試験 **V13-1** を追加しました。D-017 を撤回して毎秒消費を絶対値 1.0 に戻すと全馬のゲージが空になり、
★V-13 のテストが2件落ちます。**V-8（比）と V-9a（範囲）はこの改変を検出しません** — R-16 が言う
「全体に一様にかかる歪み」の実例で、それが検出できることを毎回の変異試験で確認しています。

⚠️ **R-12 の申告**: 「早すぎる」の定義（1.6倍）は判定を動かせる自由変数です。`GATES` 内に置いて
オーナー承認なしに動かせないようにしましたが、**正典 §13.2 に値が書かれていません**（→ §6 照会1）。

### 再実行コマンド

```bash
npm run preseed -- --seed 42          # 50世代プリシード（約4秒）
npm run verify:race -- --races 3000 --seeds 42   # V-13 を含む
npm test && npm run typecheck && node tools/mutation/run.mjs
```

---

## 2. 5代血統表（合格基準2）

出荷構成（現役2,400 / 種牡馬200 / 繁殖牝馬800）・50世代:

| シード | 5代完全 | 平均充填 | 5代内の系統数 | クロス保有 |
|---|---|---|---|---|
| 42 | 42.8% | 57.1 / 62 | 11.9 | 100% |
| 7 | 60.6% | 58.7 / 62 | 12.0 | 100% |
| 2026 | 70.9% | 60.4 / 62 | 12.3 | 100% |

**100% にならない理由**: 世代交代に約8年かかります（引退4歳 → 繁殖可能6歳 → 産駒がまた4歳で引退）。
50年 ≒ 6世代なので、最終年の馬の5代前が創始世代に届き、創始馬には親がいないため枠が空きます。
**充填率がシードで 42.8〜70.9% とばらつくのは、系統がどれだけ古い創始馬に遡るかの違い**です。
平均充填は 57〜60 / 62 と安定しており、「歴史のある血統」という §10.5 の目的は満たしています。

---

## 3. ❌ 父系ラインの分散（合格基準3）— 不合格

```
シード42  種牡馬: 系統15 最大36.0% 有効5.0   現役: 系統10 最大50.8% 有効3.1
シード7   種牡馬: 系統12 最大51.5% 有効3.3   現役: 系統 9 最大57.5% 有効2.7   ← 割れている
シード2026 種牡馬: 系統 9 最大30.5% 有効4.3   現役: 系統 5 最大43.3% 有効2.8
```

**種牡馬プールで 3シード中1シードが最大系統 51.5%、現役では 3シード中2シードが50%超**です。
40系統から始めて 50世代で 9〜15系統まで減り、うち1系統が3〜5割を占めます。

### 自己申告（R-8）

**★テストは seed 42・44世代でしか測っていないので、この不合格を検出していません**（そこでは 36.0%）。
1つの測定条件で判定していたことになり、**R-12 の指摘と同じ形**です。
今回は報告の直前に3シードで回して見つけました。テストを複数シードにするのは次便でやります。

### 構造的な原因（推測ではなく機構から）

配合相手の選択が **母の厩舎の方針で最も高い種牡馬を、全厩舎の中から選ぶ**ためです。
種牡馬プールの枠は厩舎ごとに切ったので**枠の持ち主**は分散していますが、
産駒が継ぐ `sireLine` は**選ばれた種牡馬のもの**なので、良い系統が全厩舎に広がります。

### ⚠️ 正典に規定がない（→ §6 照会2）

**NPC の配合相手の選び方は正典にありません。** ここを推測で埋めると分散の水準が変わるので、
今の「全厩舎から最良」は暫定です。実在の競走馬生産でも系統は数本に集約するので、
**9〜15系統・最大3〜5割が「偏っている」のかどうかは、正典側の判断**だと考えます。
判断をいただければ、必要なら「自厩舎の種牡馬を優先し、一定割合で外部と配合する」形に変えます。
（先に手を入れると、合格基準を通すために機構を変えたことになるので止めています。）

---

## 4. 未実施 — P0 ゲート（合格基準1）

**プリシード後の集団に対する V-1 / V-2a〜f / V-3 を回していません。**

気になっているのは **V-2b（天井余裕）**です。50世代で能力平均が
**2,355 → 3,136（+33%）**と上がり続けており、P0 のゲートが成立するか確認が要ります。

```
y 1 mean=2355   y10 mean=2402   y20 mean=2652   y30 mean=2807   y40 mean=2969   y50 mean=3136
```

`verify` は現在シミュレータ自身の集団を対象にしているので、**プリシード集団を入力として渡す経路**が要ります。
次便でやります。ここを飛ばして「P1.5 完了」とは書けません。

---

## 5. 憲法 §0.1（合格基準5）

```bash
rg -uu -n "NG_?LIST|BLOCKLIST_NAMES|実在馬" --glob '!*.md' .
```

コード中のヒットは**注意書きのコメントのみ**（`naming.test.ts`・`.gitignore`）で、
**実在競走馬名はゼロ**です。馬名はすべて音節表からの機械生成です（例: `ヴェルナカクマ` / `ペルシャニハ`）。

### ⚠️ 指示書との食い違い（→ §6 照会3）

指示書は「実在競走馬名 NG リストと突合」を求めますが、
**NG リストを平文でリポジトリに置くと、それ自体が §0.1 違反**です（`rg -uu` の検閲も
NG リスト自身が必ずヒットして機能しなくなります）。

そこで NG 判定を**注入された述語**（`NameBlocklist`）にし、`sim-engine` には
実在馬名もそのハッシュも置いていません。平文は未コミットの外部ファイルに置き、
CLI 側でハッシュ化して読む設計です（**その CLI 側の実装はまだです**。
現状は `ALLOW_ALL_NAMES` を注入しており、★テストで「NG 判定に当たった名前は返らない」ことだけ押さえています）。

---

## 6. 照会（QUESTIONS_P15）

1. **V-13 の測定条件**: 「早すぎる仕掛け」= `EARLY_SPURT_METER × 1.6` を正典 §13.2 に明記していただけますか
2. **NPC の配合相手の選び方**が正典にありません。上記の系統集中（9〜15系統・最大3〜5割）が
   合格基準3 の「偏っていない」に当たるかどうかの判断をお願いします
3. **NG リストを平文で保持しない**方針を §10.5 に明記していただけますか。平文リストの受け渡し方法
   （どこに置き、誰がハッシュ化するか）もあわせて決めたいです
4. **N-3 の初期値**: プレイヤー不在なので「**創始集団の上位30%平均 × 0.92**」を提案します
   （P0 の創始定義 §6.4 から出るので、別基準を持ち込まずに済みます）。実装済み・`npcTargetFrom`

---

## 7. 途中で潰した欠陥（試走で見つけたもの）

報告の価値は通った基準より落ちたもののほうにあると思うので、記録します。

| 見つけ方 | 欠陥 |
|---|---|
| 8世代の試走 | 種牡馬プールを全厩舎で共有 → 父系ラインが 40→24。50世代なら数系統に潰れる |
| 20世代の試走 | 引退4歳と繁殖6歳の2年のずれ → 入れ替えた年に**産駒が0**になった。`MIN_BREEDING_AGE_YEARS` を下げて回避せず、引退馬を待機させる形にした（下げた時点で「プレイヤー馬と同一エンジン」が崩れる） |
| 登録簿 | `PRESEED_OBSERVE_NOISE` が無防備。0 と比べたら **SD 256.8→246.4・最大系統 28.0%→27.5%** でほぼ動かず、登録簿に書いた「多様性が死ぬ」が**測定に支持されなかった**ので定数ごと削除した |
| 登録簿メタテスト | 免除理由に「同上」と書いたら落ちた（理由20字未満を弾く） |
| テスト | 系統集中を縮小スケールで測ると最大系統 75%。閾値を 0.8 に緩めれば通るが R-12 なので、**この節だけ出荷構成で回す**ようにした |

**厩舎ごとに種牡馬枠を切った結果、「父系ラインの本数」は構造的に保証されます。**
保証されたものを測っても検証にならない（R-16）ので、N-4 は
**1頭の5代血統に何系統現れるか**と**最大系統シェア・有効系統数（逆シンプソン）**で測っています。
`lineConcentration` には「40系統あるが実質2系統」を見逃さないことの★テストを付けました。
