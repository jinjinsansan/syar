# 見積り — 画面を本番データに繋ぐ便（2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**きっかけ**: ★オーナーの優先順位「**3 分が後からでもできるなら、いま重要なのはまず UI を本番結線すること**」
**前提**: ★D-007 再改訂（6 分）の実装は**完了**（`91afa23`）。★配備の前に入れる条件（D-007 ⑦）は満たしました

> ★**見積りです。★着手はしていません。** ★数字は実測で数えたものだけを使っています。

---

## 0. ✔ いまの状態（★数えました）

| # | | 数 |
|---|---|---|
| 1 | ★**デモデータのままの画面** | ✔ **13**（`/entry`・`/home`・`/mypage`・`/odds/[id]`・`/prizes`・`/race`・`/races/[id]/bet`・`/records`・`/stable/market`・`/stable/retired`・`/train`・`/training`・`/vote`） |
| 2 | ★**既に本番データを読んでいる所** | ✔ **16 か所**（`races_public` 8・`race_entries_public` 4・`race_odds_public` 4） |
| 3 | ★**`.rpc(…)` の呼び出し** | ✔ **0 件**（★書き込みは 1 本も繋がっていません） |
| 4 | ★利用者が呼べる RPC | ✔ **7 本**（`create_account`・`enter_race`・`place_bet`・`buy_horse`・`sell_horse`・`unlock_stable_grade`・`exchange_prize`） |

---

## 1. 🔴 ★先に潰さないと進めない穴が 2 つあります

### 1-1. ★**自分の馬を読む経路がありません**

✔ `horses` は登録簿で **`CLOSED`** です（★「`potential` / `genotype` を持つ」ため・§12.4）。
→ ★**厩舎・育成・出走登録の画面は、自分の馬を 1 頭も読めません。**
→ ★**`*_public` ビュー（または RPC）を新しく作る必要があります。**

⚠️ ★**D-114（素質は見せない）と同時に設計する必要があります** — ★ビューに `potential` も段も出せません。
★出せるのは ★**名前・性・年齢・戦績・調子/疲労・厩舎の格・引退の有無**など、★**戦績とオッズの側の情報だけ**です。

### 1-2. ★**AE 便で閉じた 3 表を読む画面があります**

★`horse_market_listing`（`/stable/market`）・`stable_grade_price`（厩舎の格）・`horse_story_event`（物語帳）は、
★**2026-09-18 の `0032` で anon / authenticated から閉じました**（★裁定 AE-1）。
→ ★**裁定が予告したとおり**、★**公開が要るようになった今、`*_public` ビューを作る番**です（★正典 410 行の形）。

---

## 2. ★便の分け方（★開発側の提案）

★**1 便で全部やりません。** ★**縦に 1 本通してから横に広げる**のが安全です。

| 便 | 中身 | 目安 |
|---|---|---|
| **UI-1**（★最初の 1 本） | ★**登録 → セットアップ → 自分の馬 → 出走登録**を端から端まで。★新しいビュー **1 本**（自分の馬）＋ RPC **2 本**（`create_account`・`enter_race`）＋ エラー文言（`ST001`/`ST002`） | ★**1 便** |
| **UI-2** | ★**投票（`place_bet`）と払戻の表示**。★`/vote`・`/races/[id]/bet`・`/mypage`（残高・台帳）。★`users`・`bets`・`ep_ledger`・`pp_ledger` は**既に読める**（`OWNER_SCOPED`） | 1 便 |
| **UI-3** | ★**市場・厩舎の格・物語帳**。★**公開ビュー 3 本を新設**（§1-2）＋ RPC 3 本（`buy_horse`・`sell_horse`・`unlock_stable_grade`） | 1 便 |
| **UI-4** | ★**景品交換**（`exchange_prize`）・`/prizes`・`/records` | 1 便 |

★**UI-1 が通れば、残りは同じ形の繰り返し**です（★読み・書き・エラー・検査）。

---

## 3. ★各便で必ず要るもの（★T-13 や AE 便と同じ形）

| # | |
|---|---|
| 1 | ★**V-20 の登録簿に新しいビューを載せる**（★載せないと ③ が落ちる） |
| 2 | ★**V-19 の経路の検査**（★構文木で見る・`auth-wiring.test.ts` の形） |
| 3 | ★**エラーを黙って握らない**（★`ST001` EP 不足・`ST002` 出走資格。★画面に理由を出す・R-27） |
| 4 | ★**D-114 を破らない**（★素質も段も画面・API・ビューに出さない。★検査は「入力の形」で・D-108 ③） |
| 5 | ★**staging で実測してから報告**（★デモが消えたことを、配信される HTML で確かめる） |

---

## 4. 🔴 ★判断をお願いしたいこと

| # | |
|---|---|
| 1 | ★**UI-1 から着手してよいか**（★順序は上のとおりでよいか） |
| 2 | ★**自分の馬のビューに何を出すか**（★D-114 の下で「戦績とオッズだけ」をどこまで具体化するか）。★**正典 §12 の話なので、開発側では決めません** |
| 3 | ★**本番配備との順序** — ★`0021`〜`0033` の配備が先か、UI-1 の実装が先か。★**開発側の見立てでは配備が先**です（★画面を繋ぐ先が本番に無いと、staging でしか確かめられません） |

---

## 5. ★この見積りの確からしさ

| | |
|---|---|
| ✔ 数えたもの | 画面 13・既存の読み 16 か所・`.rpc()` 0 件・RPC 7 本・`horses` が `CLOSED` であること |
| △ 数えていないもの | ★**各画面の作り直しの量**（★デモの形と本番の形がどれだけ違うか）。★UI-1 を 1 本通せば、残りの見積りが実測で出ます |
| ⚠️ | ★**「1 便」の中身は日数ではありません** — ★便＝レビュー側に出す 1 単位です |
