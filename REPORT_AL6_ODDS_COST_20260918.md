# 報告 — AL-6: ES 便の後のエンジンで 1 レースの所要を測り直しました（2026-09-18）

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**指示**: 裁定 `REVIEW_CONSULT_ARCADE_LOOP_ANSWER_20260918.md` **AL-6**（★「3 分が成立するかはこの 1 つの数字で決まります」）
**測ったコミット**（R-28・R-23）: ★**`fec322e`**（★エンジンの最終更新は **`ccfb684`＝ES-6**・✔ `git log ccfb684..HEAD -- packages/race-engine` に `race.ts`/`lane.ts` の変更なし）

> 凡例（R-13）: **✔** ＝ 実測 ／ **△** ＝ 未確認・換算 ／ **🔴** ＝ 食い違い・要照合

---

## 0. 答え（1 つの数字）

★**この機械（P コア固定）で、現エンジンの 1 レースは 94〜117 秒**（M = 3,896,104・16 頭・1200〜3200m）。

★**本番機への換算は 約 70〜98 秒**（△ §3）。→ ★**(B) 3 分・480R/日 で 1 日の 39〜54%。**

| | レビュー側の見積（AL 回答 §3-3） | ★本報告の換算 |
|---|---|---|
| 1 レース（本番機） | 98〜111 秒（D-071 前のエンジン） | ★**70〜98 秒**（現エンジン・△ 換算） |
| (B) 3 分・480R/日 | 13.1〜14.8 時間/日 ＝ **54〜62%** | ★**9.3〜13.1 時間/日 ＝ 39〜54%** |

🔴 ★**ただし §4 の食い違いを読んでから使ってください。** ★**私の測定は、ES 便が報告した比と向きが逆です。**

---

## 1. 測り方（★ばらつきを潰してから測りました）

| # | |
|---|---|
| 1 | ★測る量は ★**試行 1 回 ＝ `resolveRace` ＋ 全 7 券種の的中目の集計**（★本番のオッズ算出と同じ・`bench-mc.ts` と同じ形）。★距離ロスの下ごしらえは**試行の前に 1 回**（ES-6・`build-race.ts` と同じ） |
| 2 | ★**出走馬は両方の版で同一**（`tmp/bench-entrants.json` に出して両方が読む・R-30） |
| 3 | ★**条件が同一であることを先に確認**（✔ 両版とも 16 頭・1400m・芝・良・oval を返した） |
| 4 | 🔴 ★**この機械はハイブリッド CPU（i7-1255U・P 2 ＋ E 8）で、載るコアで 2 倍変わります** — ✔ 同じ道具の 2 回の実行が **25.97 と 42.22 μs** に割れました。→ ★**P コアに固定**（`start /affinity 3`）して測り直しました |
| 5 | ★各条件 **100,000 試行 × 3 回の最小値**（★暖機 20,000 試行）。★**1 回の値を報告しません** |

---

## 2. ✔ 実測（同じ機械・同じ P コア・同じ出走馬）

### 2-1. 現エンジン（`fec322e`／エンジンは ES-6 `ccfb684`）

| 走路 | 1200m | 1600m | 2000m | 2400m | 3200m |
|---|---|---|---|---|---|
| 既定（`DEFAULT_OVAL`） | 24.11 μs | 25.29 | 26.38 | 27.71 | 28.76 |
| 最長の周（ookawara・1 周 2400・直線 540・幅 23） | 28.62 | 26.97 | 30.06 | 28.83 | 28.70 |

→ ★**1 レース（M=3,896,104）＝ 94.0〜117.1 秒**（この機械）。★距離に対して**単調に増えます**（★機構と向きが合っています）。

### 2-2. D-071 前のエンジン（`97364c5^`・同じ機械で測定）

| | 1200m | 1600m | 2000m | 2400m | 3200m |
|---|---|---|---|---|---|
| （走路の概念が無い版） | 31.22 μs | 32.43 | 37.95 | 35.11 | 36.93 |

→ ★**1 レース ＝ 121.6〜147.9 秒**（この機械）。

### 2-3. 比（★同じ機械・同じ条件）

| 距離 | 1200 | 1600 | 2000 | 2400 | 3200 |
|---|---|---|---|---|---|
| D-071 前 ÷ 現エンジン（既定） | **1.29** | **1.28** | **1.44** | **1.27** | **1.28** |
| D-071 前 ÷ 現エンジン（最長の周） | 1.09 | 1.20 | 1.26 | 1.22 | 1.29 |

→ ★**現エンジンのほうが 1.1〜1.4 倍「速い」**（★遅いのではありません）。

---

## 3. △ 本番機への換算（★前例と同じ方法）

★`REPORT_ENGINE_LANE_SPEED_20260915.md` §2-2 と同じく、★**本番機の実測に比を掛けます。**

| # | |
|---|---|
| 1 | 本番機（VPS・D-071 前のエンジン `70d9982`）の実測 ＝ **98〜111 秒/レース**（AL 回答 §3-3 が使った値） |
| 2 | この機械（D-071 前・同じエンジン）＝ **122〜148 秒** → ★**この機械は本番機より 1.2〜1.35 倍遅い** |
| 3 | この機械（現エンジン）＝ **94〜117 秒** → ★**本番機では 約 70〜98 秒**（△ 換算） |

### 3-1. 1 日の使用率

| | レース数/日 | 1 レース 70 秒 | 1 レース 98 秒 |
|---|---|---|---|
| 今（10 分） | 144R | 2.8 時間（**12%**） | 3.9 時間（**16%**） |
| ★**(B) 3 分** | 480R | 9.3 時間（★**39%**） | 13.1 時間（★**54%**） |

→ ★**(B) は AL 回答の見積（54〜62%）より余裕があります。** ★ただし ★**54% は依然として「1.85 倍しか余裕がない」**ので、★AL-7（超えるなら間隔か試行数）の判断はそのまま生きています。

⚠️ ★**換算の弱点を 2 つ書きます**:
1. ★**本番機の 98〜111 秒の素性**。★私が 09-18 に VPS の `journalctl` で読んだ周は **69.7〜113.2 秒**でした。★「周」と「1 レースのオッズ」は同じではありません（★周には確定・払戻・DB も入ります）。→ ★**この換算の基準そのものに幅があります**
2. ★**本当に閉じる方法は 1 つだけ**です — ★**ES 便のエンジンを VPS に配備し、そこで周を測る**（★オーナーの手配・R-28「稼働中のビルド識別子とセットでのみ意味を持つ」）

---

## 4. 🔴 ★ES 便の報告と、向きが逆です（★要照合）

★`REPORT_ENGINE_LANE_SPEED_ES6_20260916.md` §4 は、★**同じ 2 つの版**についてこう報告しています。

> ① `resolveRace` 1 回の比: **最大 1.47 倍** ／ ② 試行 1 回の比: **最大 1.28 倍**（★いずれも **D-071 前より現エンジンが遅い**向き）

★**私の測定は逆で、現エンジンが 1.1〜1.4 倍「速い」**です。★**どちらかが間違っています。**

### ★私が確かめたこと

| # | |
|---|---|
| 1 | ✔ ★**エンジンは ES-6 以降変わっていません**（`git log ccfb684..HEAD -- packages/race-engine` に `race.ts`/`lane.ts` なし）。★**別物を測ってはいません** |
| 2 | ✔ ★**両版が同じ出走馬・同じ条件**（JSON で渡し、条件を印字して一致を確認） |
| 3 | ✔ ★**試行回数を変えても向きは同じ**（N=2,000 でも N=100,000 でも現エンジンが速い） |
| 4 | 🔴 ★**この機械は載るコアで 2 倍変わります**（✔ 実測 25.97 対 42.22 μs）。★**ES 便の測定がコアを固定していたかは、報告に書かれていません**（△） |
| 5 | △ ★ES 便は `watch-pool.json` の馬・N=400〜2000 で測っています。★**私とは出走馬も回数も違います** |

### ★お願い

★**レビュー側で独立に再実行してください**（★手順は §6）。★**私はこの食い違いを「自分が正しい」と決めつけません。**
⚠️ ★**もし ES 便の比が測り方の産物だったなら、★「いまのエンジンを配備すると 10 分に入らない」という前提そのものが変わります。**

---

## 5. ★止めたもの（オーナー指示・2026-09-18）

★**「(B) で行く。較正に関わる測り直しは全部止めて、まず AL-6 だけ」**を受けて:

| | |
|---|---|
| ★止めた | BG 便の測り直し（`b805694`）の続き。★**(B) で 1 週のレース数が 24 → 80 になり、出走機会が 3.3 倍になるため、今の数字は無効です**（AL-8） |
| ★止めた | Q-BAND-04 の測り直し（★同じ理由） |
| ★残っている | ★道具（`npm run verify:band`）は使えます。★**(B) の実装後にそのまま回せます** |

---

## 6. ★再現手順（レビュー側の独立再実行）

```bash
# ① 現エンジン（この機械の P コアに固定して測る）
npx tsx tmp/dump-entrants.mts                 # 出走馬を JSON に出す（両版で同じ入力・R-30）
cmd /c "start /affinity 3 /b /wait cmd /c npx tsx tmp/bench-sweep-head.mts --trials 100000 --repeat 3"

# ② D-071 前のエンジン（同じ機械・同じ入力）
#    ★git archive でスクラッチに取り出し、@star/* を junction で自分の packages に向ける
git archive '97364c5^' | tar -x -C tmp/es-before
#    （PowerShell）New-Item -ItemType Junction -Path tmp\es-before\node_modules\@star\<名> -Target tmp\es-before\packages\<名>
cmd /c "start /affinity 3 /b /wait cmd /c npx tsx tmp/es-before/tmp/bench-sweep-old.mts --trials 100000 --repeat 3"

# ③ 在庫の道具でも（★1400m 既定の 1 条件だけ）
npx tsx apps/cli/src/bench-mc.ts --trials 200000
```

⚠️ ★**測定器は `tmp/`（gitignore）に置いています。** ★**必要なら `tools/` に移してコミットします。**
★`apps/cli/src/bench-mc.ts` は **1 条件（1400m・既定）しか測りません** — ★480 レース/日に掛ける数字としては狭すぎます（★本報告が距離と走路を振った理由）。
