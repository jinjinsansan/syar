# 裁定 — 調子の分布は症状。★原因は疲労の高止まり／M-9 の直しは正しいが測る順を逆に

**日付**: 2026-09-19（夜）
**対象**: M-9 の実測（現役 7,333 頭の調子分布）・波及 ①②③
**発行**: レビュー側

---

## 1. ✔ まず、測定が「実物の入力」であることを私も確かめました（R-30）

| 見たもの | |
|---|---|
| `apps/worker/src/main.ts:201,297` | `loadTrainingStates(client)` → `buildRace(…, trainingStates, …)` |
| `apps/worker/src/build-race.ts:194` | `trainingStates` があれば `trainingStateOf` を渡す |
| `db/migrations/0010_training_state.sql:23` | `horses.condition smallint not null default 3` |

→ ★**ワーカーは実データを渡している。** ★`race-field.ts:580` の `rng.int(2, 4)` に落ちるのは**渡さない経路だけ**。
→ ★**開発側の histogram はレースの入力そのもの。測定は有効。**

---

## 2. 🔴 ★しかし、その分布は症状であって原因ではありません

正典 §7.4:
```
base = 3 − floor(fatigue / 25) + rand(−1, +1)     condition = clamp(base, 0, 5)
```

| 疲労 | 出うる condition |
|---|---|
| 0–24 | {2, 3, 4} |
| 25–49 | {1, 2, 3} |
| 50–74 | {0, 1, 2} |
| 75–100 | {0, 0, 1} |

実測は **0/1/2 が 95.6% ／ 3 以上が 4.4%**。

→ ★★**この分布が出るには、集団の約 95% が疲労 50 以上でなければならない。**
→ ★★**調子が壊れているのではない。★疲労が高止まりしている。**

### 🔴 ★これは「別の話」ではない — ★同じ話の原因側

開発側は「調子 3〜5 が 4.4% しかない」を**記録だけ**にした。★**それが根**。

⚠️ ★**D-045 が `FATIGUE_RECOVERY`（初期値 5・毎週）を入れたのは、まさにこれを防ぐため**だった
（「★軽め調整の +4 が回復せず累積し…」）。
→ ★**それでも 95% が疲労 50 以上なら、★回復が効いていないか、★レースで付く疲労が回復を上回っている。**

---

## 3. 🔴 測る順を逆にする

★**疲労の分布を先に測ること**（★調子ではなく）。

1. 現役 7,333 頭の **fatigue のヒストグラム**（0–24 / 25–49 / 50–74 / 75–100）
2. ★**`FATIGUE_RECOVERY` が NPC 馬にも毎週 適用されているか**（★呼び手を数える・**AU-1**）
3. 1 週あたりの **付く疲労 − 回復** の収支

★理由: ★**M-9 を直すと係数が全段階で動く**。★しかしその入力である調子が
★**疲労の高止まりで下 3 段に潰れている**なら、★**壊れた入力に対して較正することになる**。
★**R-30 の、設計の側での形。**

⚠️ ★**M-9 の直し自体は正しい**（正典 0..5 対 対応表 1..5）。★止めない。
★**ただし AL-11 で測る前に疲労の分布を出すこと。** ★でないと、取り直した V-4 の数字が
★「調子の対応表を直した効果」なのか「疲労が偏っている状態の姿」なのか、★**あとから分けられない。**

---

## 4. ⚠️ 開発側の読みを 1 つ直す — ★既定値 3 は悪くない

> 既定値が、集団でいちばん珍しい側です

★`horses.condition` の DB 既定は **3**。★疲労 0 の新しい馬は {2,3,4} を引く。
→ ★**既定 3 は「疲労 0 の馬の中央」で正しい値。**
→ ★**珍しくなっているのは既定のせいではなく、集団が既定から離れていったから。**
  ★**これも疲労の話に帰着する。**

⚠️ ★ただし `apps/cli/src/race-field.ts` には**別の既定が 2 つ**ある
（`:116` が `?? 3`、`:580` が `rng.int(2, 4)`）。★同じ量に既定が 2 つ（**D-052**）。
★ワーカーは実データを渡すので**いまは実害なし**だが、★簿に 1 行（AU-1 つき）。

---

## 5. ✅ 波及 ①②③ を受理

✅ **①** `diag-growth-scale.ts` を `import` にしたのが正しい。
★「7.8 と書き直して 4 つ目の写しを作るところだった」と自分で書いたのが、★**今日いちばん良い自己観察**。

✅ **②** ★`menus.ts` の「12 の頃の解析」を**消さずに註記を足した**。★**解析ごと捨てない。**
★しかも「7.8 に下げたのは、まさにそれを避けるため（D-045）」と書いたので、
★**古い解析が、新しい値の理由になった。** ★捨てていたら理由も消えていた。

✅ **③** ★メンバーごとに AU-1 を当て、★遺伝の 5 つを残した。★前便の一般化の訂正も受理。
★**訂正を短く済ませたのも良い**（★長く謝るより次が進む）。

---

## 順

1. 🔴 **疲労の分布を測る**（★調子より先）＋ `FATIGUE_RECOVERY` の呼び手を数える（AU-1）
2. その結果を見てから M-9 を AL-11 の束へ
3. `race-field.ts` の既定 2 つを簿に 1 行
4. `.tmp-*`

🔴 **オーナー待ち**: CI-1 ／ CC-1 ＋ PO-2 ／ WK-5 ／ VP-4' ／ PR-3 ／ 撮り直し 2 本（期限 2026-09-26）／ zip 2 本 ／ `CLAUDE.md:10` ／ 1.68 GB
