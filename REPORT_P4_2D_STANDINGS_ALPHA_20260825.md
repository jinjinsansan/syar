# REPORT — 順位表の減光を 0.6 → 0.25 へ（2026-08-25）

指示書: 「順位表の減光係数を 0.6 から 0.25 へ変更し、最大寄りでも不透明度 75% を維持」
★commit・push はしていません。

---

## 1. 変更

`apps/web/src/app/race/page.tsx:2019`

```diff
-ctx.globalAlpha = prevAlpha * (1 - 0.6 * ease);
+ctx.globalAlpha = prevAlpha * (1 - 0.25 * ease);
```

見出しコメントを
**「寄りでは順位表を最大 25% 減光する。starhorse-v1 の横追従でも可読性を保つ」** に更新。
経緯（0.6 では 86〜88% で 1〜5 着が読めなかったこと）も併記しました。

変更していないもの: 閾値 `0.28 / 0.45`、位置、サイズ、色、`HUD.glass`、カメラ、台本。
`packages/render/src/oblique-ui.ts` と `packages/render/src/hud-kit.ts` は無変更です。

---

## 2. 実画面確認（seed 42）

### 既定 `/race`（フラグなし）

| 点 | 変更前 | 変更後 |
|---|---|---|
| 直線入口 | 読める | 読める（減光なし・変化なし） |
| 進行 86% | **読めない**（40%） | **1〜5 着の馬名と着差がすべて読める**（75%） |
| 進行 88% | **読めない**（40%） | **1〜5 着の馬名と着差がすべて読める**（75%） |
| 進行 93% | 読める | 読める（減光なし・変化なし） |
| finish-line | 読める | 読める（減光なし・変化なし） |

86% は `ミライノツバサ / アオバハヤテ 0.2 / ピンクレディ 5.4 / グリーンアロー 5.9 / ハンシンドリーム 6.8`、
88% は `アオバハヤテ / ミライノツバサ 0.3 / ピンクレディ 6.0 / グリーンアロー 6.1 / サクラブリーズ 6.9`
がいずれも判読できます。枠色バッジも色が戻りました。

### 旧 v4（`?cinematography=v4`）— 変更前と一致

同じ 5 点を PNG で撮り、**画像のハッシュが変更前と完全に一致**しました。

| 点 | 変更前 | 変更後 |
|---|---|---|
| 直線入口 | `eb3b054fedde` | `eb3b054fedde` |
| 進行 86% | `0568f2d300dd` | `0568f2d300dd` |
| 進行 88% | `d973e1866ec7` | `d973e1866ec7` |
| 進行 93% | `124098c07ba6` | `124098c07ba6` |
| finish-line | `9ecafcec72d0` | `9ecafcec72d0` |

旧 v4 の直線は `homestretch-front`（画面比 24% 前後）で `ease = 0` のため、減光の係数を変えても
1 画素も動きません。それが実測でも確かめられました。

---

## 3. テスト

`packages/render/test/starhorse-script-v1.test.ts` — **9 件パス**

⑨ を今回の認可範囲へ更新しました。以前は「HUD をいっさい変更していない」ゲートだったので、
今回の変更で**正しく落ちました**（R-16）。開けたのは減光の 1 行だけです。

★あわせて、**`out/` の動画・画像や未追跡の測定ツールに依存する検査を製品テストから外しました。**
比較動画 8 本の存在確認は監査用で `.gitignore` の対象なので、本線のテストには置きません。
残したのは製品コードだけで再現できる次の 9 件です。

| | 内容 |
|---|---|
| ① | 既定が starhorse-v1 |
| ② | 明示指定も同じ |
| ③ | 不正値は既定へ戻る（6 通り） |
| ④ | `?cinematography=v4` で旧台本を選べる／旧 v4 の表が残っている |
| ⑤ | 台本差は 2 ショットだけ（境界 `until` も同値） |
| ⑥ | 第4コーナーで `fourth-corner-high` |
| ⑦ | 直線で `homestretch-side`／発走・1角・道中・ゴールは両者同じ |
| ⑧ | **レース結果不変**: 台本を変えても馬の (gate, s, w) は同じ、入力も書き換えない |
| ⑨ | **HUD 係数 0.25**／古い 0.6 なし／**閾値 0.28・0.45 不変**／順位表の位置・大きさ・`HUD.glass` 不変 |

全体: `npm test` **112 ファイル / 1121 件パス**、`npm run typecheck` エラーなし、`npm run build:web` 成功。

---

## 4. ⚠️ 注意事項：dev サーバー稼働中に `build:web` を実行しない

作業中に、稼働していた **3210 の dev サーバーが壊れました**
（レースが組めない → その後 `/race` が 500）。

**原因は `npm run build:web`（`next build`）です。**
`next build` は dev サーバーと同じ `apps/web/.next` を書き換えます。
`next dev` が同じディレクトリを見ているため、走らせた時点で dev サーバーの持つ内容と食い違いました。

**コードのせいではありません。** 切り分け済みです。

- 減光の行を 0.6 に戻しても 3210 は直りませんでした
- 別ポート（3211）に新しく起動した dev サーバーでは、同じコードで正常でした

### 今後の運用

> ★**dev サーバーを動かしたまま `npm run build:web` を実行しない。**
> 実行するときは dev サーバーを止めてから。壊れたら `Ctrl+C` → `npm run dev:web` で戻ります。
> どうしても同時に必要なら、ビルド用に別の作業ツリーを使うこと。

### 復旧の結果

3211 の検証用サーバーと壊れた 3210 を停止し、ポート 3210 で `npm run dev:web` を起動し直しました。

| | `/race` | `?cinematography=v4` |
|---|---|---|
| 3210（再起動後） | **200** | **200** |

★3211 の検証用サーバーは停止済みです。

---

## 5. 状態

- `apps/web/package.json` のポート 3210 保持
- commit・push なし（HEAD は `93d270c`）
- 追加動画・seed 監査は行っていません
