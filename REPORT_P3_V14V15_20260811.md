# REPORT_P3_V14V15_20260811 — V-14 ③ の定義是正、V-15 の両側化、自己汚染点検

## 本便で正典の改訂が要る決定をしたか

**決定はしていません。ただし正典の記述で確認いただきたい点が2つあります。**

1. **決定番号の食い違い**。裁定では ③ の定義を **D-045**、EP あたりの実測を **D-046** と
   参照されていますが、こちらの記録では **D-045 = 故障時の current 切り下げ**、
   **D-046 = 疲労の自然回復**、**D-047 = 同一 EP 予算での比較** です。
   コードのコメントは D-047 で書いています。**どちらが正か**をお知らせください。
   （中身は一意なので、実装は「同一 EP 予算下の開放率」で進めています）
2. **Q-P3-28（新規）**: V-15 の①と②で「**最悪の方針が逆向き**」です（§3）。

---

## 1. 結果一覧

| 項目 | 結果 | SHA |
|---|---|---|
| **V-14** ③ を D-047 の定義どおりに是正 | **全項目 PASS** | `cf32d8e` |
| **V-15** 両側化（②を追加） | **PASS**（1,800頭） | `cf32d8e` |
| 検証ツールの自己汚染点検 | **18本すべて一致・3本の漏れを是正** | `deca0ff` |
| テスト | **600件 緑** | `deca0ff` |

**`menuIntensity` は触っていません。**

---

## 2. ★V-14 ③ — ご指摘のとおり、定義と実装が食い違っていました

`apps/cli/src/verify-v14.ts` は**自分のコメントで**こう書いていました:

```ts
// ★③の定義（D-047 で確定）: 「同一 EP 予算下の開放率」。
//   EP は希少資源で、プレイヤーが直面する問いは
//   「同じ EP を注いだときどちらが強くなるか」だから。
```

**その3行下で、こう判定していました:**

```ts
const g3 = hardOnly <= balanced + DOMINANCE_MARGIN;  // ← 時間軸の水準差
```

**定義を書いた本人が、その定義で実装していませんでした。**
そして「V-14 ③ FAIL」を何便も報告し、「次の梃子は `menuIntensity`」と書いていました。
**ご指摘がなければ、正典値である `menuIntensity` を、存在しない失敗のために動かしていました。**

### 是正後（seed 42 / 400頭 / D-049 適用済み）

```
  方針                  開放率      SD      SE     故障/頭     引退       EP/頭
  放置(軽めのみ)          74.5%    4.91   0.245     0.39   0.5%     17,803
  バランス型             90.3%    4.88   0.244     0.91   0.5%     53,003
  追い切り偏重            96.7%    4.60   0.230     1.32   1.5%     83,614

  ★① 適切な育成が 88%以上 : 90.3%（余裕 2.3pt = 9.5 SE）  PASS
  ★② 差が 12pt 以上       : 15.8pt（90.3 − 74.5）（余裕 3.8pt = 10.9 SE）  PASS
  ★③ 同一EP予算下で追い切り偏重が支配的でない: 比 0.68倍（上限 1.02倍）  PASS
     EP あたり開放率: 追い切り 11.57 vs バランス 17.04（1万EPあたり%）
     【参考・判定に使わない】時間軸の水準差: 96.7% vs 90.3% = 6.4pt
       ★EP を多く注いだほうが強くなるのは設計として正しい（D-048）。ここは③の失敗ではない

★V-14: PASS
```

判定幅は `DOMINANCE_MARGIN`（+2pt）から **`DOMINANCE_MARGIN_RATIO`（1.02倍）**に置き換え、
較正定数の登録簿も更新しました（**1.02 はこちらが決めた値**なので照会のままです）。
**実測 0.68倍は上限から大きく離れている**ので、この値の選び方が判定を左右しません。

---

## 3. ★V-15 の両側化 — 窓ができ、そして①と②で最悪の方針が逆向きでした

② **キャリアを通じた平均低下 ≥ 15%** を追加しました。
①は**最悪の方針**で判定します（「安全性は『ある遊び方をした人だけ壊れる』を許さない」）。

### 1,800頭・seed 42・`TEMPER_FLOOR_RATIO = 0.60`

```
  【方針ごとの実測】
  方針             中盤SD比   中盤頭数   低下(キャリア平均)  低下(中盤)  低下(引退時)
  neglect          107.2%      1797        16.4%      17.9%      27.4%
  balanced          60.8%      1793        35.8%      39.9%      39.9%
  hard_only         60.0%      1791        37.3%      40.0%      39.9%

  ★① 中盤の集団SD ≥ 誕生時の 50%   最悪の方針 hard_only: 60.0%  PASS
  ★② キャリアを通じた平均低下 ≥ 15%  代表方針 balanced: 35.8%  PASS
     ★最悪の方針 neglect で読んだ場合: 16.4%  PASS  → 読みの違いで結論は**変わりません**
★V-15: PASS
```

### ★両側にしたことで「窓」ができました（400頭・②も最悪方針で読んだ場合）

| 比率 | ①最悪SD比 | 余裕 | ②最悪低下 | 余裕 |
|---|---|---|---|---|
| 0.50 | 50.1% | +0.1pt | 17.3% | +2.3pt |
| 0.55 | 55.0% | +5.0pt | 16.6% | **+1.6pt ← 最小余裕が最大** |
| **★0.60** | 59.9% | +9.9pt | 15.9% | +0.9pt |
| 0.65 | 64.9% | +14.9pt | 15.0% | +0.0pt |
| 0.70 | 69.9% | +19.9pt | **14.0% FAIL** | −1.0pt |

**窓は [0.50, 0.65] です。** 0.60 は**どちらの読みでも②を満たします**。

★ただし「②を最悪方針で読む」なら、**最小余裕が最大なのは 0.55**（1.6pt）です。
0.60 は①側に 9.9pt、②側に 0.9pt と偏っています。
**0.60 のままにしてあります**（裁定で承認済み・両方の読みで通る）が、
`INJURY_BASE_PROB` と同じ選び方をするなら 0.55 です。**どちらにするか裁定をお願いします。**

### ★Q-P3-28: ①と②で「最悪の方針」が逆向きです

- **①が最も厳しいのは休養の多い方針**（`hard_only` 60.0%）— 下限に速く収束するほど SD が縮む
- **②が最も厳しいのは休養の少ない方針**（`neglect` 16.4%）— そもそも `temperDelta` を引かない

放置（軽め調整のみ）は §7.2 の `temperDelta` が 0 のメニューしか使わないので、
気性は**故障休養の週しか動きません**。これは**機構が壊れているのではなく、
プレイヤーが使っていない**状態です。
②を最悪方針で測ると「**使わなかった人がいるから機構が死んでいる**」と判定してしまいます。

→ ここでは②を**代表方針**で判定しました。ただし
**②を代表方針で読むと、②が上限としてまったく効かなくなります**
（0.70 でも代表 27.4% で PASS してしまう）。
**つまり「機構を止めると通る」問題を塞いでいるのは、②を最悪方針で読んだときだけです。**

★ツールは**両方の読みで判定し、結論が変わるかどうかを毎回出力します**。
0.70 では実際に「★変わります（裁定が要る）」と出ます。

---

## 4. 検証ツールの自己汚染点検 — 3本に漏れがありました

指定どおり「**2回続けて流して出力が一致するか**」で見ました。
併せて主要9テーブルの**行数が戻るか**も見ています。

### ★見つかった問題

**`verify-prize` / `verify-cancel` / `verify-economy` の3本**が、
**利用者を作ってから**「発売中のレース」を探しており、staging にレースが無いと
`race.id` で TypeError になって落ちていました。**後片付けに到達しません。**

実際に staging に**3件の利用者が残っていました**。さらに悪いことに、
**`account_type='player'`** でした。0009 で内部口座を別掲する仕組みを作ったのに、
これらの検証ツールがそれを使っておらず、**§11.2 の実経済の指標に検証用の口座が混ざる**状態でした。

→ 前提の確認を**状態を作る前**に移し、揃わなければ**何も作らずに** `exit 2`。
   `account_type='internal'` に修正。残っていた3件を削除（**利用者 0 人**）。

### `verify-exchange` の不一致

2回の出力が違いました。差は `prize_exchanges` の **bigserial のみ**で、
PP も在庫も行数も戻っていました。
★id は**このツールが検証している対象ではありません**（見ているのは PP の減少・
在庫の減少・二重交換の拒否）。**再現しない値を証拠に混ぜない**よう出力から外しました。
隠したのではなく、「id が発行されたか」は出しています。

### 点検結果

```
  対象18本すべて: 2回の出力が一致し、行数も戻ります
  ★3本は前提（レース）が無く**実行できていません**（点検した扱いにしない）:
    verify-cancel.mjs / verify-economy.mjs / verify-prize.mjs
```

★**点検自体をツールにしました**（`tools/audit-tools.mjs`）。1回きりの手作業だと次に誰も回しません。
除外したツールは**理由つきで出力**します（「時間がかかるから」では外しません）。

---

## 5. ご指摘への応答

> **「次の梃子は menuIntensity」と決めてから測っていないので、まず測ってください。8回の仮説のときと同じ順序の問題です。**

そのとおりでした。しかも今回は**測る前に決めた**だけでなく、
**測っている対象が定義と違っていた**ぶん、より悪い形でした。
測り直した結果 ③ は 0.68倍で PASS し、`menuIntensity` を動かす理由はありません。

> **どちらも「機構を止めるとゲートが最もよく通る」形で、両方とも私が書きました。**

②を追加したことで**窓ができ**、上限が実際に効くようになりました（0.70 で FAIL）。
ただし §3 のとおり、**その上限が効くのは「②を最悪方針で読む」ときだけ**です。
代表方針で読むと②は上限として働かず、同じ型が残ります。**ここが Q-P3-28 の核心です。**

---

## 6. 未着手 — staging にレースが無いことが3件を止めています

| 項目 | 状態 |
|---|---|
| **G-7** PP 発行の staging 実証 | 実装は P2 で完了（`prize-award.ts`・二重発行は `update races ... where status='scheduled'` の排他で防止）。★**staging にレースが無いため未実証** |
| `verify-prize` / `verify-cancel` / `verify-economy` | 同上（前提が揃わず exit 2） |
| **B-6** の配線（`race-field.ts:99-100`） | レース生成の経路そのものなので、上と同じ作業で通ります |
| **B-3** V-11 の構造報告 | 未着手 |
| **D-043** の残り | 検証ツール1本をバンドル（`dist/worker.cjs`）に対して流す |

**次便でレース生成（出走表・オッズこみ）を staging に通し、上の4件をまとめて閉じます。**
`verify-a2` は `build = () => ({ entrants: [], odds: [] })`（空の出走表）で回しており、
**A-2 の PASS は出走表とオッズの中身を見ていません**。ここも併せて実データにします。

---

## 7. 再現コマンド

```bash
npm run typecheck && npx vitest run                            # 600件 緑
npx tsx apps/cli/src/verify-v14.ts                             # V-14 全項目 PASS
npx tsx apps/cli/src/verify-v15.ts --horses 1800 --seed 42     # V-15 両側 PASS
npx tsx tools/audit-tools.mjs --env staging                    # 自己汚染点検
```

---

## 8. ★オーナーへ（再掲）

**staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん）。
「新しい接続文字列を貼るタイミングで一緒に」とのことです。
