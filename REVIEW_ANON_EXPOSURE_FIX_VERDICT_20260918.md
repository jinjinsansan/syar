# 【STAR】裁定 — AE-1〜AE-4 の受理（anon 露出を閉じた便）

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **対象** | コミット `11bd965` ／ 報告 `REPORT_ANON_EXPOSURE_20260918.md` |
| **指示** | 裁定 `REVIEW_ANON_EXPOSURE_VERDICT_20260918.md`（AE-1〜AE-4） |

---

## 0. 判定

✅ **PASS（条件なし）。配備してよい。**

★**逸脱 2 件はどちらも受理します**（§2）。★**1 件は、私の指示のほうが弱かったものです**（§2-1）。

---

## 1. AE-1〜AE-4 の照合（★レビュー側が独立に確かめたもの）

| # | 指示 | ✔ 照合の結果 |
|---|---|---|
| **AE-1** | 新移行 `0032` で閉じる。`0024`/`0025`/`0027` は書き換えない | ✅ `db/migrations/0032_close_anon_table_grants.sql`。✔ ★**`git show --name-only` で確かめ、既存 3 本は 1 行も触れていない** |
| **AE-2** | `EXPECTED_EXPOSURE` に 3 表を `CLOSED` で登録（1 行ずつ理由） | ✅ 3 表とも。★理由も 1 行ずつ入っている（`detail` が `jsonb` なので列を足さなくても中身が増える／公開が要るなら `*_public`／V-20 ② は中身でなく形で書かれている）。★**＋1 件**（§2-2） |
| **AE-3** | 検査 3 本の期待を反転し、なぜ反転したかを残す | ✅ 3 本とも `grant … to anon` の**要求**を削り、★**`0032` の `revoke` を要求する形**に。★**理由が長い註記で残っている**（★「この検査が、書いた瞬間の誤りを 3 便続けて凍結していた」まで書かれている） |
| **AE-4** | staging の実 DB で**両方向**を測る | ✅ ★**適用前の対照つき**（🔴 V-20 ②③ 不合格 2 件）→ **適用後 11 件すべて緑**。★**① 3 表が 0 行 と ② 公開ビュー 4 つが読めること を別々の行で出している** |

### ★特に評価する 2 点

| # | |
|---|---|
| 1 | ★★**変異で両方向を確かめている** — `0032` の `revoke` を 1 行つぶすと `entry-rpc-migration.test.ts` が**赤**、戻すと**緑**、`diff` で差分なし。★**「反転した検査が空振りしていないか」**という、★**今回いちばん確かめるべきこと**を正面から測っている |
| 2 | ★**適用前の対照を取っている。** ★「直った」を言うには「直る前」が要ります。★staging の適用前に V-20 を回して**不合格 2 件を実測してある**ので、★**緑が何によって緑なのかが分かります** |

✔ **レビュー側も独立に再実行**: `entry-rpc-migration` / `horse-market-migration` / `stable-grade-unlock-migration` / `exposure-registry` / `rpc-guard` の **5 ファイル・67 件すべて緑**。

---

## 2. 🔴 逸脱 2 件 — ★どちらも受理します

### 2-1. `revoke select` → **`revoke all`** ✅ **受理。★私の指示のほうが弱かった**

★開発側は **R-29**（「権限は列挙して閉じるのではなく、既定を閉じて必要なものだけ開ける。**列挙は必ず漏れる**」）を引き、★実例として **`0002` が `insert, update, delete` を revoke して `TRUNCATE` / `REFERENCES` / `TRIGGER` を漏らした**こと（★`TRUNCATE` は RLS の対象外なので止まらなかった）を挙げています。

| | |
|---|---|
| 実効の差 | ✔ **無し**（3 表に付いていたのは `select` だけ。`0024:96`・`0025:56`・`0027:45` が他を剥がし済み） |
| 得るもの | ★**列挙漏れの余地が消える** |
| 判定 | ✅ ★**受理。差し戻しません** |

🔴 ★**私の側の記録**: ★**指示 AE-1 を「`revoke select`」と列挙型で書いたのは、私が R-29 を引かずに書いたからです。**
★**私はこの裁定で「登録簿の外で増える形」を 3 例挙げて論じていながら、★自分の指示が列挙型でした。** ★**同じ穴を、指示する側で踏んでいます。**

### 2-2. `story_daily` を登録簿に追加（AE-2 の 3 件の外） ✅ **受理**

★適用前の V-20 ③ が赤で、★`0029`（前便）の `story_daily` が**未登録**だったことが分かったため、同じ便で足したもの。

| | |
|---|---|
| 露出 | ✅ **していない**（✔ `0029:43` が書き込みを剥がし、`grant select` は無い。✔ 適用前の実測でも anon から 0 行）。★**未登録だっただけ** |
| 「この便で他のことをしないでください」との関係 | ✅ ★**範囲内と読みます。** ★あの一文の趣旨は「**着順・経済に効くものを混ぜない**」であって、★**同じ登録簿の 1 行**は対象外です |
| ★決め手 | ★**足さなければ AE-4 の「11 件すべて緑」が言えませんでした。** ★開発側の判断が正しいです |
| 判定 | ✅ ★**受理。戻しません** |

★**なお、これで「登録簿の外で増えた」は本日 4 例目**になりました（`EXPECTED_EXPOSURE` に **3 表 ＋ `story_daily`** ／ 検査 3 本が露出を凍結 ／ 乱数の用途 ID `TRAIN_STREAM`・`STREAM` ／ `horse_week_log` の註記）。
→ ★**一般化して R として立てる価値があります**（★起票案は `REVIEW_STORY_ORIGINS_VERDICT_20260918.md` §7-2 に既に出してあります。★**オーナー承認待ち**）。

---

## 3. ⏱ 本番への配備

✅ ★**配備してよい。** ★**`0024` 以降を当てる配備に `0032` を必ず同梱してください**（裁定 §5 のまま）。

```
本番:  0021 → 0022 → 0023 → 0024 → 0025 → 0026 → 0027 → 0028 → 0029 → 0030 → 0031 → 0032
       ★1 回の配備にまとめる（間に人の手を挟まない）
```

★✔ 本番は `0024` 以降が未適用のため、★**今この瞬間の本番に露出はありません**。

---

## 4. ★私の教訓（記録します）

| # | |
|---|---|
| 1 | ★★**指示を「列挙」で書かない。** ★`revoke select` と書いたのは、★**R-29 を引かずに書いた**から。★**閉じる指示は「既定を閉じる」形で書く** |
| 2 | ★**「この便で他のことをしないでください」は、何を指すかを書く。** ★今回は開発側が正しく「着順・経済に効くもの」と読んでくれましたが、★**字面は範囲を決めていませんでした** |
| 3 | ★**逸脱を先に報告する開発側の作法は、そのまま続けてほしい。** ★2 件とも `§3` に「先に報告します」として出ており、★**どちらも差し戻す必要がありませんでした** |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
