# 裁定 — ゲーム本体の再設計 第 1 便（GB-1〜GB-6）と M-4（V-10）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側・オーナー
**対象**: 報告 `REPORT_GAME_BODY_1_20260916.md`（GB-1〜GB-6 ＋ §1g の M-4）／ 指示書 `DEV_INSTRUCTIONS_GAME_BODY_1_20260916.md` ／ コミット `682496e`（GB-1）・`a78e1ac`（GB-2）・`cfd6648`（GB-3）・`a5bf42f`（GB-4）・`e520d4a`（GB-5）・`e947236`・`a19ca06`（GB-6）・`e80c42e`（M-4）
**測ったコミット**（R-23）: レビュー側の再実行は `f392223`（エンジンは GB 便と同じ）

> 凡例（R-13）: **✔** ＝ レビュー側がコードを読んで確かめた、または手元で実行した ／ **△** ＝ 未確認（報告の記載に基づく）
> 本書は正典を編集しません。§6 の起票はオーナーの承認後です。

---

## 0. 判定

| 範囲 | 判定 |
|---|---|
| **GB-1〜GB-6** | ✅ **受理**（条件 **GC-1・GC-2** ＝ 報告の記録の直し 2 件のみ。実装への条件はありません） |
| ★**着順に効くものを入れていないこと** | ✔ **確認**（§1-2） |
| **M-4（V-10）** | ✅ **「判定不能」という扱いを受理**（R-3 のとおり）。★**AF-2・BF-6 は閉じていません**。閉じ方は §4 |
| 見つけた食い違い（`place_bet` と §9.5） | ✅ **報告のみで直さなかった判断は正しい**。裁定は §5 |

---

## 1. 照合

### 1-1. レビュー側の再実行 ✔

| 何を | 結果 |
|---|---|
| GB の検査 9 ファイル（`jockey-no-effect`・`training-view-wiring`・`menu-view`・`grade`・`ownership`・`jockeys`・`horse-market`・`stars`・`economy-balance`） | ✔ **43 件すべて合格**（3.1 秒） |
| `packages/race-engine/src` に触っていないか | ✔ `git diff --name-only d3159f2..HEAD -- packages/race-engine/src` が**空** |
| `place_bet` の食い違い | ✔ 実在する（§5） |
| 種付料の式 | ✔ **正典にあるのは NPC 種牡馬の式だけ**（§10.5・`3,000 + G1勝利数 × 8,000 + 総獲得賞金/20`）。**プレイヤー種牡馬の種付料の決め方は正典にも実装にも無い** |

### 1-2. ★着順に効くものが入っていないこと

- **対照の実測**: 騎手 6 人 × 8・18 頭 × 1200・2400m × シード 8 ＝ 192 通りで `resolveRace` を回し、着順・スコア・走破タイムが 1 ビットも同じ（✔ レビュー側も再実行）
- ★**「効果 0 の定数」だけを根拠にせず、実際にエンジンを回して比べた**のは正しい形です（定数が 0 のまま別経路で混ざる形を捕まえる・R-16）
- 育成のゲート（V-14 ①②③・V-15 ①②・V-7a・V-7b）が **GB-1 の前と同値** △（報告の実測。レビュー側は再実行していません）

### 1-3. 設計として良かった点（記録）

| # | |
|---|---|
| 1 | ★**GB-5 で「伸びと費用に同じ倍率」を掛けた形**。上の格が買えるのは**時間**で、**強さではない**（D-103 ② を構造で満たしている。EP あたりの伸びが全格 1.0） |
| 2 | ★**★表示の算出を 1 か所に新設**した（`stars.ts`）。購入の「同じ帯」と画面の★が**同じ量**になる（D-079 ⑤ と同じ要請） |
| 3 | ★**GB-6 で額を書き写さず、実装から引いて計算**した（二重帳簿にしない・検査で固定） |
| 4 | 較正定数の登録簿に落ちたことを**隠さずに報告**し、`EXEMPT` に**理由付きで**載せた |

---

## 2. 条件（報告の直し・実装はそのままでよい）

| # | 条件 |
|---|---|
| **GC-1** | ★**§3 の表が自己矛盾しています。** 「触っていない」列に `packages/sim-engine/src` が入っていますが、**GB-4 で `stars.ts` を新設**しています（`git diff` に出ます）。表を直してください |
| **GC-2** | ★**§2 の較正定数の段落が二重です。** 「2 回落ちた」（346-349 行）と「1 回落ちた」（350 行）が並び、後者が前者と食い違います。片方を削ってください |

どちらも中身の誤りではありませんが、**報告は次の便の入力になる**ので直してから次へ進んでください。

---

## 3. 照会への回答（Q-GB-1〜3・オーナー承認済み 2026-09-16）

| # | 回答 |
|---|---|
| **Q-GB-1 種付料の式** | ★**正典の穴です**（プレイヤー種牡馬の種付料の決め方が無い）。**オーナー承認の方針**: **NPC の式を土台に、上限つきで持ち主が決める**形。★**式は起票してから実装**してください（§6）。それまで「配合より割高」（D-102 ④）は**数で示せないまま**で構いません（報告に △ で残す） |
| **Q-GB-2 初期 EP 2,000 で馬が買えない** | ★**買えないままでよい**。初期馬は無償で 1 頭（D-074・D-079）、購入は**2 頭目以降の入口**です。最初の日に買えないことは設計と矛盾しません。**額は動かさない** |
| **Q-GB-3 日次上限 10,000 EP が現役 30 頭でほぼ埋まる（1 週 9,750 EP）** | ★**実害のある噛み合わせです**。上限に当たると「調教できない日」が生まれ、§7.1 のデイリー来訪の動機と**逆に働きます**。**オーナー承認の方針**: **V-11 を回して額を決める**（D-075 は較正定数・値そのものをゲートにしない）。★**30 頭すべてを毎週調教する前提が現実的かも併せて測る**（実際には放牧・休養が混ざるので、9,750 EP は上限側の見積り） |

---

## 4. M-4（V-10）— 判定不能の扱いと、閉じ方

### 4-1. 受理した点 ✔

- **FAIL を不合格と読まなかったのは正典 R-3 のとおり**（「判定不能」は PASS でなく FAIL に倒す、の正しい適用）。★道具自身が「正式な V-10 ゲートではない」と出しているのに合否を書く報告は、これまで何度も事故の元でした
- 10,000 試行での大きな負の乖離（三連単 −27.24pt）が**打ち切りの効果**であることを、**本番の試行数の probe で姿が変わる**ことまで見せた（三連単 +0.66pt）のは良い切り分けです
- **D-035 の上限に当たった売り目 0 件**・**D-096 の「売らない目を賭け金から外す」が効いている**ことの確認も受理します

### 4-2. 閉じ方（★オーナー承認済み・段階的に）

| 順 | 中身 | 見積り |
|---|---|---|
| 1 | `verify-pmin` を **`--races 40`** 程度で流し、**`win` 以外**を閉じる | 約 1.1 時間 |
| 2 | **`win`** だけ長く回す（律速・約 314 レース） | 約 8.7 時間（夜間に） |
| — | 流す場所 | 🔴 ★**撤回しました（2026-09-17・下の追記）。オーナーの別のターミナルで 1 本**。★**開発側のセッションの裏では流せません** |

### 4-3. ★判定のときの約束

- ★**「流せば合格」ではありません。** probe では **quinella +3.27pt・exacta +2.88pt** が帯（±1%）の外でした。**測れるようになるだけ**です
- **帯の外が残った場合も、較正定数（`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`・`margin`）は動かさない。** 値と向きを報告し、照会に回す
- **AF-2（第 1 便）・BF-6（第 2 便）は、この測定が閉じるまで開いたまま**です
- ★**本番に実際の利用者が入る前に閉じること**（最終裁定 `REVIEW_VENUE_WIRING_FINAL_VERDICT_20260916.md` §3 の 4）。配備そのものは待ちません

---

## 5. `place_bet` と §9.5 の食い違い（裁定）

### 5-1. 事実 ✔

| | 中身 |
|---|---|
| 正典 §9.5-3 | 「同一レースに自馬が複数いる場合は、**それら全頭を含む組合せのみ可**」 |
| いまの DB（`0020`） | `p_selection @> to_jsonb(e.gate)` を**自馬のどれか 1 頭**について満たせば通る＝**片方だけを含む買い目が通る** |
| いつから差が出るか | ★**D-104 で 2 頭まで出せるようにした今日から**。1 頭までの間は同じ意味でした |

### 5-2. 裁定

1. ★**DB を正典に合わせます**（正典の側を緩めません）。**投票の RPC の便で移行を出してください**。理由: §9.5 は**八百長利得の遮断装置**（D-006 の前提）で、2 頭出したときに「片方だけを含む買い目」を許すと、**もう 1 頭を負けさせる利得**が残ります
2. ★**2 頭出すと単勝・複勝が買えない**のは、§9.5 の**意図した帰結**として受理します（1 頭しか選べない券種だから）。**画面に理由を明示**してください（「自馬 2 頭が出走するため、単勝・複勝は購入できません」）。黙って拒否しない
3. **移行が入るまでの間**、2 頭出走は**実運用では起きません**（出走登録の RPC が未実装）。したがって**急ぎの穴ではありません**が、**出走登録と投票の RPC は同じ便で出す**こと（片方だけ入れると穴が開きます）
4. 監視: 移行の後、**2 頭出走時の投票の拒否**が記録されること（黙って通らない・R-21）

---

## 6. 正典への起票候補（オーナーの承認後にレビュー側が起票）

| 候補 | 中身 |
|---|---|
| **種付料（プレイヤー種牡馬）** | NPC の式（§10.5）を土台に、**持ち主が上限つきで決める**。上限は較正定数。★**EP のシンクであり、プレイヤー間は 20% 焼却**（§3.3・§10.5 の既存の規則に接続）。D-102 ④「購入は配合より割高」を数で言えるようにするための前提 |
| §9.5 の註記 | **2 頭出走時は単勝・複勝が買えない**ことを明文化し、**画面に理由を出す**ことを求める（本書 §5-2） |
| D-075 の註記 | **日次上限と所有頭数の噛み合わせ**（30 頭 × 週 9,750 EP）。額は V-11 で決める |

---

## 7. 次の便（順番）

| 順 | 中身 |
|---|---|
| 1 | **GC-1・GC-2**（報告の直し） |
| 2 | **V-10**（§4-2 の段階的な測定） |
| 3 | 相談 `CONSULT_HORSE_LIFECYCLE_20260916.md` の裁定にもとづく **第 2 便**（生涯イベントログ・馬物語帳・Discovery の見せ方）＝ `REVIEW_CONSULT_HORSE_LIFECYCLE_VERDICT_20260916.md` |
| 4 | **出走登録・投票の RPC**（同じレース 2 頭の適用・騎手の保存と凍結・§9.5 の移行・購入の DB）。★**出走登録と投票は同じ便で** |
| 5 | 格の解放（EP）と週送りへの配線 ／ V-11 の流し直し（購入・騎手の料金を入れた一巡） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。

---

## 追記（2026-09-17）— 🔴 ★§4-2「流す場所」は私の誤りでした。撤回します

開発側からの訂正を受け、★**本書 §4-2 の「開発側のセッションの裏で可（ES 便でメモリ不足の形ではなくなった）」を撤回**します。

| # | 事実（✔ レビュー側がコードで確認） |
|---|---|
| 1 | ★**道具の取り違えでした。** 軽くなったのは **`verify:payout`（MC 既定 **10,000**・`apps/cli/src/verify-payout.ts:88`）**。★**`verify:pmin` は `MC = ODDS_MC_TRIALS`**（`verify-pmin.ts:72`） |
| 2 | ★その値は **3,896,104**（`packages/betting/test/sell-decision.test.ts:19` が固定）。式は **M ≧ λ* / p_min**（`balance.ts:164-174`・`LAMBDA_STAR = 30`・★律速は三連単）→ ★**約 390 倍**です |
| 3 | ★★**試行数は D-035 の設計式から決まる値なので、速度改善では 1 も減りません。** ★ES 便が軽くしたのは**レース解決**であって、**試行数ではありません** |
| 4 | 開発側の実測: `--races 40` を裏に投げ ★**10/40 レースで強制終了**（払戻率は 1 つも出ず）。利用可能メモリ 2 GB に対し Node のヒープ上限 4.1 GB で ★**最初から入りません** |

### ★さらに悪いこと — 自分の正しい指示を、測り直さずに覆していました

> `DEV_INSTRUCTIONS_VENUE_WIRING_20260915.md:209`（★私が 09-15 に書いたもの）
> | V-10 | ⚠️ **開発側は流さない**（1 本数時間・メモリ不足の前例）… **オーナーが別のターミナルで** |

★**09-15 には正しく書いていました。** ★それを 09-16 に「ES 便で軽くなった」という**測っていない理由**で覆したのが本書 §4-2 です。

★**教訓 2 つ**:

1. ★**「軽くなった」は道具ごとに確かめる。** ★同じリポジトリの似た名前の道具でも、★**律速している量が違えば恩恵は届きません**
2. ★**自分の過去の指示を覆すときは、覆す根拠を測ってから。** ★「もう大丈夫のはず」で緩めた結果、★**開発側の時間を捨てさせました**

### ★訂正後の指示

| # | |
|---|---|
| 1 | ★**V-10 の本測定は、オーナーの別のターミナルで 1 本**（`npm run verify:pmin -- --races 40`）。★**開発側は流さない** |
| 2 | ★**所要の見積り（「約 1.1 時間」「約 8.7 時間」）は伏せます。** ★入らなかったのだから、あの見積りには根拠がありません。★**流してみて実測を書いてください** |
| 3 | ★**測り方の訂正（`36d6e97`）は受理**（§下記）。★**出口の `place` の値だけが未取得**。★**AF-2・BF-6 は開いたまま**で正しい |

### ★測り方の訂正（`36d6e97`）の確認結果 — ✅ 受理

| # | 確認したこと |
|---|---|
| 1 | ✔ 集計が **`v10-accounting.ts` の `accountRaceKind` / `judgeKind`** を通り、★**`verify-payout` と同じ 1 か所**になった（D-052・R-30） |
| 2 | ✔ **`foldRaceSample` で「1 レース 1 標本」に畳む**（★畳まないと `races` が `RACES × K` になり **SE がレース内の分散で薄まる**。D-036 の趣旨どおり） |
| 3 | ✔ ★**売る目が本番のオッズ表と食い違ったら投げる**（`verify-pmin.ts:137-143`）。★`stake === 0` でも投げる（R-21） |
| 4 | ✔ ★**検査 14 件を私も実行して緑**（`apps/cli/test/v10-accounting.test.ts`）。★**それまで検査が 1 件も無かった**道具に入った点も評価します |
| 5 | ⚠️ ★ただし **R-21 のとおり、直ったのは「測り方」だけ**です。★**`place` が基準を満たすかは、まだ誰も見ていません** |
