# 報告: 本番配備（`86040e0`）— 2026-09-23

> 開発側 → レビュー側。オーナーの直接の文面「本番に配備OK」「一般登録を開いたまま本番に配備してよい」「進めてください」による。
> 手順書 `RUNBOOK_PROD_DEPLOY_20260922.md` の順で実施。★**段 ⑥ の週送りの確認だけ、まだ取れていない**（§4）。

## §0 一行

段 ①〜⑦ を通した。**本番の DB は `0072` まで・ワーカーは `86040e0`・画面も `86040e0`**。失敗の行は 0 件。
残るのは、**次にゲーム内の週が変わるとき（2026-09-23 05:00）の週送りの周**の記録。

## §1 各段の実績

| 段 | 操作 | 結果 |
|---|---|---|
| 0 | 事前の確認 | 配備する sha `86040e0`（門 2,894 件 通過）。未適用は `0060`〜`0072` の 13 件（`0059` 以前なし）。VPS のワーカーは `24af9f2` で稼働中（直近 15 分で `cycle=` 13 回） |
| ① | `repair-pedigree-cache --apply` | 壊れていた 6,981 頭（自分で数えて `--expect-broken` に渡した）。7,370 行を書き、読み直しで uuid でない鍵 0・鍵の総数 220,666 が一致。6 件とも合格 |
| ② | 移行 `0060`〜`0065`（`--to 0065`） | 6 件 OK。古いワーカーはその後 4 周・失敗 0 |
| ③ | `backfill-name-key --apply` | 空 7,370 → 0。重なり 0・正規化で空 0。5 件とも合格 |
| ④ | 移行 `0066`〜`0072` | 7 件 OK。古いワーカーはその後 4 周・失敗 0 |
| ⑤ | 記録（関門ではない・オーナー判断） | `disable_signup = false`（開いたまま）。`auth.users` 8 件 / `users` 1 件。内訳: `@test.local` 6 件（8 月の試験の残り・未確認・プロフィールなし）／オーナー 1 件（プロフィールあり）／共同開発者 1 件。★**知らない口座は無い**（オーナーの回答） |
| ⑥ | `deploy.sh 86040e0`（VPS） | 自己検査 31 行 OK・スキーマ照合 OK・検証を通過・リンク切替。★`/opt/star-current` は `86040e0`・`star-worker` は active |
| ⑦ | main への push | `913d67f..86040e0`（fast-forward）。`/api/healthz` の sha が `86040e0` と一致 |

## §2 新しいワーカーの最初の数分（手順書 ⑥ の 5 項目）

| # | 見るもの | 結果 |
|---|---|---|
| 1 | `cycle=` | ✅ 5 周（配備直後の 6 分間） |
| 2 | `週送り` | ⏳ **まだ**。★異常ではない: 馬は 273 週まで処理済みで、いまの週 274 は締まっていない（`weeksToProcess` は締まった週だけ返す）。次は **05:00**（`world_state.week_started_at` 01:00 ＋ 4 時間） |
| 3 | `配合` | ✅ 週 273 で 21 頭・相手なし 4 頭。★印が無かったので、いまの週から始めた（配備直後の想定どおり） |
| 4 | `失敗` | ✅ 0 件 |
| 5 | `周の全体` | ✅ 3.7s(1.0%)。★ただしこれは**週送りの無い周**。手順書 ⑥ の条件（最初の週送りの周で 100% 超なら止める）は §4 で取る |

- `発売の遅れ` の行は 0 件。

## §3 ついでに分かったこと（直していない）

1. **近交係数が血統と合わない馬が 2 頭**（段 ①・`--fix-inbreed` を付けなかった）。簿 `INBREED-COEFF-ABOVE-PEDIGREE`。最大差 7.81e-3。
2. 🔴 **VPS の `/opt/star` に、手で当てた跡**（レビュー側の指示で記録）:
   - `tools/deploy.sh` が **index に入った状態**（`M ` ＝ staged）で、HEAD より +56 行。★中身のハッシュは `86040e0` の版と**同一**（`0452cbbb…`）。
   - ファイルの更新日時は **2026-08-12 19:26**。同じ形のファイルは他に無い（`git status --porcelain` は `M tools/deploy.sh` と未追跡の `.npm/` だけ）。
   - HEAD が `67b2971`（2026-08-12 13:24）のままなのは、`deploy.sh` が **`git fetch` ＋ `git archive` で配る方式**で、HEAD を動かさないため。★つまり HEAD が古いのは設計どおり。
   - **いつ誰が当てたかは分からない**（日時から、08-12 の配備事故〔自分の ssh が切れて自動戻しが走らなかった件〕の直後に、その是正を手で当てたものと推測できるが、**記録は見つけていない**）。
   - ⚠️ 中身が同じでも、**VPS のファイルを直に触った跡**であることは変わらない。次に同じ形を見つけたら、中身が違うかもしれない。
3. ⚠️ `verify-deployed-build.mjs` の `--expect` は **完全一致**で比べる。短い sha を渡すと、同じコミットでも「食い違っています」と出る（今回 1 度出た）。手順書に注意を書き足した。

## §4 残り（次にすること）

- **2026-09-23 05:00 以降**に、週送りの周のログを読む:
  - `[worker] 週送り 週=…` が出たか
  - その周の `[worker] 周の全体=…s(…%)`。★**100% を超えたら止めて報告**（裁定 §6）
  - 超えていたら `[worker] ★発売の遅れ cycle=… +…s` の秒数も出す（裁定 §7）
  - `失敗` の行が 0 件か
- 手順書の段 ⑤ を「記録」に書き換える（オーナー判断・閉じる期限の 1 行を含む）。
