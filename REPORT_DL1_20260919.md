# REPORT — DL-1: 日次の流れが 1 か月以上 止まっている件

- 日付: 2026-09-19
- 発行: 開発側
- 裁定: `REVIEW_VP3_WITHDRAWN_VERDICT_20260919.md` **DL-1**（★「なぜ止まっているかを数える。推測しない」）

---

## 1. 結論を先に

| # | |
|---|---|
| ✅ **3 つの日次の関数は、★今日 動きます** | ★実 DB（staging）で ★**rollback 付き**に通しました |
| 🔴 **`point_flow_daily` は 0 行** | ★コードは **2026-08-08** からあります |
| 🔴 **なぜ書かれていないかは、★DB からは分かりません** | ★ワーカーの**標準出力**が要ります。★どこにも残っていません |

→ ★★**「壊れているから止まった」ではありませんでした。★関数は動きます。**
  ★**止まっている理由は、DB の中には書かれていません。**

---

## 2. ✅ 3 つとも動くことを確かめました

`tools/diag-dl1-daily.mjs`（★`begin` → 試す → `rollback`・★`txid` の見張り付き）:

| | 結果 |
|---|---|
| ① `aggregateDay`（`point_flow_daily`） | ✅ 通る。★**1 行 書く**ことも確かめた |
| ② `recordUnlockDistribution`（`unlock_daily`） | ✅ 通る（7,333 頭・平均 0.781・SD 0.110） |
| ③ `recordStoryRows`（`story_daily`） | ✅ 通る（77 行・68 頭） |

★どれも自分で `begin`/`commit` しません（✔ 確認済み）ので、★外側の取引に閉じ込められます。

### 🔴 ★**「落ちなかった」と「書いた」は別**でした

★最初 ① を「✅ 通った」とだけ確かめました。🔴 ★**それは「書いた」証拠ではありません。**
→ ★`point_flow_daily` の行数を ★**前後で数え直しました**（0 → **1**）。
★もし ★**黙って 0 行**だったら、★①は「✅ 通る」のに ★**表は空のまま**になります。★そこが本件の核心なので、必ず数えます。

---

## 3. 🔴 ★**私は、危うく「`aggregateDay` が壊れている」と報告するところでした**

★1 回目の実行で ① だけが落ちました:

```
🔴 ① aggregateDay: ★落ちた — Cannot read properties of undefined (reading 'slice')
```

✔ ★調べたら ★**私の診断スクリプトの中の `.slice`** でした:

```js
console.log(`✅ ${label}: 通った ${JSON.stringify(r).slice(0, 120)}`);
//   ★aggregateDay は値を返さない → JSON.stringify(undefined) は undefined → .slice で落ちる
```

→ ★★**製品ではなく、測る側が落ちていました。**
★気づけたのは、★**引数を直に書いてもう一度呼ぶ対照**を取ったからです（★そちらは落ちませんでした）。
⚠️ ★**今日 3 度目**です（★偽の DB／32.32% の引き算／これ）。★**対照が無ければ、3 度とも誤報でした。**

---

## 4. 分かっている事実（★推測と分けます）

| ✔ 事実 | |
|---|---|
| `point_flow_daily` | ★**0 行**（★`0001_init.sql` からある表） |
| `story_daily` | ★**0 行** |
| `unlock_daily` | ★**3 行**。★日付 **08-10 / 08-11 / 08-12**（★作成 08-11 / 08-12 / 08-13） |
| 日次ブロックのコード | ★**2026-08-08** から `main.ts` にある（`7cc968e`） |
| ワーカーの最後の活動 | ★**2026-09-16T13:04** |
| `lastAggregated` | ★**起動のたびに `''` に戻る**（★起動 1 周目には必ず入るはず） |

### ⚠️ ★これは推測です（★確かめていません）

- ★`unlock_daily` の 3 行は ★**`tools/verify-unlock-daily.mjs`（★単体の道具）が書いた**のかもしれません。
  ★その道具も `recordUnlockDistribution` を呼びます。→ ★**ワーカーが書いたのか道具が書いたのか、区別できません。**
- 🔴 ✔ ★**`tools/verify-flow.mjs:51` は `delete from point_flow_daily where date = $1` をします。**
  → ★**表を空にしうる道具が実在します**（★ただし 1 日ぶんずつ）。

---

## 5. ★次に要るもの（★私には取れません）

★**ワーカーの標準出力**です。★日次ブロックは失敗しても

```
[worker] 日次集計に失敗: <理由>
```

★を出して**ループは続けます**（A-1 を壊さないため）。→ ★**理由はログにしか出ません。**
✔ ★**DB にワーカーの生死や失敗を記録する表はありません**（★探しました）。

**提案**（★判断は求めません・材料として）:
- ★**日次ブロックの失敗を DB に 1 行 残す**（★`point_flow_daily` に「失敗」を書くのではなく、★別の小さな表か、★既存の警報の経路に載せる）
- ★理由: ★**いまの形は「失敗しても続ける」が正しい**のに、★**続いた結果 1 か月 誰も気づきませんでした**。
  ★R-16（静かな劣化）そのものです。★**止めないまま、見えるようにする**のが筋だと思います。
