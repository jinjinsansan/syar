# 【STAR】裁定: プレイヤー配合 `breed()` の置き場所と冪等キー（PLAN I-2）— 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: 開発側 `star-aa` からの相談（2026-09-22・経路は事務連絡用。裁定はこの文書だけ）
- 対象: `CLAUDE_CODE_INSTRUCTIONS_FIRST_HORSE_20260922.md` 受入条件 1・2 ／ `PLAN_BLOODLINE_INHERITANCE_20260922.md` I-2 ／ `QUESTIONS_FOAL_LIFECYCLE_20260922.md`（オーナー決定 案 A）
- 照合した HEAD: `df16e48`（ローカル・未 push）
- 運用: オーナー指示（2026-09-22）「承認不要で自立的に開発側と通信して進める」に基づいて発行する。**本番 DB・ワーカー・push はこの裁定の対象外で、今までどおりオーナーの直接の許可だけで動く**

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| 1 | 置き場所 | ✅ **(a) を採る**（要求を積む RPC → ワーカーが `breed()` を実行して確定）。(b)・(c) は採らない |
| 2 | 冪等キーの単位 | ✅ **2 段にする**。要求 ID だけでも、利用者ごとの初回 1 回だけでも**足りない** |
| 3 | 仔の ID と乱数の種 | 🔴 **NPC 配合の作り方（親と週）を流用しない**。要求 ID から作る（開発側の案に無かった点） |
| 4 | 本番の順序 | ⚠️ 本番に載るのは **血統修復 → `0060` → 配備** の後。staging では先に進めてよい |
| 5 | 次の順番 | ⚠️ **D-074 / D-079 の改訂案を先頭にする** |
| 6 | 持ち越し | 父母候補の出どころ（NPC の母馬の枠を使う影響）／命名前の仔の置き場所 → 別の裁定で扱う |

---

## 1. 置き場所 — ✅ (a)

### 根拠 [CODE]
- ✔ **正典 §15.3（`STAR_SPEC_v2.0.md:2026-2040`）が名指ししている。** 「複雑すぎて SQL で書きたくない処理（**配合**・レース生成）はワーカー側に置き、クライアントは『リクエストをキューに積む → 結果を Realtime で受け取る』形にする」。(a) は正典の条文そのもの。**(b) はこの条文と、同じ節の「Supabase RPC を主 API とする」（§15.3・`:2081` の「Next.js API Routes に散らすと書き直し」）の両方に反する**
- ✔ 同じ形の前例が 2 つ在る:
  - **D-111**（出走の凍結 = 受付 → ワーカーが書く）
  - **`0057_training_orders.sql`**（置き場の表 ＋ 所有をサーバーで確かめる書く口 `set_training_order` ＋ ワーカーが読む）。★今回の手本はこちら（EP をどこで動かすかまで同じ論点を踏んでいる）
- ✔ 計算の出どころが 1 か所で済む。`breed()` を呼ぶのはワーカーだけになる（D-052・R-30 と同じ作法。今の呼び手は `apps/worker/src/breeding-runner.ts:514`）
- ✔ 待ち時間は最大 60 秒（`apps/worker/src/main.ts:72` `TICK_MS = 60_000`）。デザイナーへの依頼（`DESIGN_REQUEST_FIRST_HORSE_ONBOARDING_20260922.md` §4）が既に「サーバーの確定が終わってから誕生を表示し、通信待ち・失敗・再開も設計する」と書いているので、**待ちは受入済みの前提として扱う**

### 条件
1. **要求はワーカーが毎周（60 秒ごと）処理する。** NPC の週次配合 `runBreedingCatchUp`（`main.ts:718`）の「週が進んだときだけ」の経路に相乗りさせない。相乗りさせると、誕生まで最大 4 時間（1 ゲーム週）待つことになる
2. **1 件の要求は 1 トランザクションで確定する**: 父母の行を `select … for update` でロック → `canMate()` を**ロックの後に**判定し直す → `breed()` → 仔の書き込み → 父母のカウンタ（`coverings_this_year` / `bred_this_year` / `foal_count`）→ 要求の行を「完了」にして結果（仔の ID）を書く。★どこで落ちても何も残らないこと
3. **ロックの順番を NPC の週次配合と揃える**（母 → 父など、1 通りに決めて両方の経路に書く）。同じ母馬を 2 つの経路が同じ周に取り合う場合の試験を必ず入れる（受入条件 2 の「親馬が選択中に他者に使われる競合」）
4. **失敗は理由つきで要求の行に書く**（`canMate` の `reason`・候補切れ等）。画面はそれを読んで出す。★黙って消さない（D-111 ⑤ と同じ）
5. クライアントは要求の表を直接読み書きしない（`revoke all … from anon, authenticated`・V-20 の登録簿に `closed` で載せる。0057 と同じ）。本人の要求の状態を読む口は RPC かビューで用意する
6. `Math.random()` / `Date.now()` / `randomUUID()` をワーカーの確定経路で使わない（憲法 §1-4）→ §3

### (b)・(c) を採らない理由
- (b): サーバー権威が 2 か所になる。§15.3 に反する。アプリ化（§15.4 Expo）のときに書き直しになる
- (c): 新しいインフラに見合う利点が無い。(a) で待ちの 60 秒以外の要件はすべて満たせる

---

## 2. 冪等キー — ✅ 2 段

### どちらか 1 つでは足りない理由
- **要求 ID だけの場合**: 別のタブや再ログインで画面を開き直すと、**新しい要求 ID** が作られる。すると無料の初回配合が 2 回通る → 受入条件 2 の違反
- **「利用者 1 人につき初回 1 回」だけの場合**: 初回には効くが、引退馬の繁殖（通常の配合）では「同じ要求の再送」と「新しい配合」を区別できない。PLAN §1 の「初回用に別の配合経路を作らない」とも合わない

### 決定
| 段 | 何を止めるか | 形 |
|---|---|---|
| **第 1 段: 要求 ID** | 連打・再送・通信断後の再試行 | 要求 ID はクライアントが作って送り、`unique`。★同じ ID が 2 回来たら**新しい行を作らず、前の行の状態・結果をそのまま返す**（エラーにしない） |
| **第 2 段: 業務上の一意** | 別タブ・別要求 ID による二重取り | **初回**: 「種類 = 初回」の要求は利用者ごとに 1 件（部分一意 `unique (user_id) where kind = 'initial'`）。★失敗した要求で枠を使い切らないよう、**失敗の行は枠を解放する形**にする（`where kind = 'initial' and status <> 'failed'` など）。**通常の配合**: 既存の `horses_one_foal_per_dam_per_week`（`0054_pool_supply.sql:62-73`・`unique (dam_id, birth_week)`）と、ロック後の `canMate`（母は年 1 回）で止まる |

- ✔ 初期 EP と D-079 の付与馬（案 A の「加えて 1 頭」）も、**同じ第 2 段の鍵に紐づける**。仔だけ 1 回で、EP や付与馬が 2 回、という抜け方を塞ぐ（受入条件 2 は「仔馬・初期 EP・親の利用回数」の 3 つを並べている）
- ⚠️ `0054` の一意は `dam_id` が null の行を対象外にする（`:65`）。プレイヤー配合では母が必ず在るので問題はないが、**試験は「同じ母・同じ週に NPC 経路とプレイヤー経路が 1 頭ずつ作ろうとする」形で書く**（片側の経路だけの試験で緑にしない・R-16 の家族）

---

## 3. 🔴 仔の ID と乱数の種 — NPC 配合の作り方を流用しない

- ✔ NPC の配合は、仔の ID と種を**父・母・週**のハッシュから作る（`breeding-runner.ts:129-142` `foalIdAndSeed`）。同じ親・同じ週なら同じ ID になり、再実行しても増えない、という設計
- 🔴 **プレイヤー配合がこれをそのまま使うと**:
  - NPC の週次配合が同じ週に同じ組合せを選んだ場合、**同じ ID で衝突する**
  - 「失敗した → 同じ週に同じ父母でもう一度」を送ったとき、**必ず同じ仔が出る**。これはよいが、逆に「父母を選び直して再抽選」の損得が週の境目に依存するようになる
- **決定**: プレイヤー配合の ID と種は、**DB に記録した要求 ID**（と、区別のための固定の接頭辞）のハッシュから作る。要求 ID は記録されるので、**同じ要求からは何度でも同じ仔が再現でき**、決定論は保たれる（憲法 §1-4）
- ⚠️ **再抽選の穴**: 要求 ID をクライアントが自由に作れるなら、理屈の上では「ID を変えて何度も投げ、良い仔が出るまで」ができる。★**第 2 段の鍵（初回は 1 件・通常は母の年 1 回）が確定させた時点で次が通らないので、ここでは塞がっている**。ただし、**失敗の行が枠を解放する形**（§2）にするので、**「失敗」を利用者が起こせない**ことを確かめること。仔の計算結果を見てから失敗させる経路（確定前に結果を返す等）を作らない。★**結果は確定後にだけ外へ出す**
- ✔ `canMate()` の年の引数は仔の `birthYear`（`packages/sim-engine/src/breeding.ts:102`）。NPC 経路は `year + yearOffset` を渡している（`breeding-runner.ts:520`）。**プレイヤー経路も年の出どころを同じ関数から取る**（09-20 の「年の出どころを 1 つに」`2596466`）

---

## 4. 本番の順序 — ⚠️

- ✔ 本番の `pedigree_cache` は鍵がプリシードの ID のまま（`HANDOFF_UI_POLISH_20260921.md:248`・本番で uuid 形 0 件を実測）。今の配合処理はこれを見つけると**止まって投げる**（`breeding-runner.ts:445-455`）。プレイヤー配合も同じ `breed()` を使うので、**近交係数と血統が壊れた入力で計算される**
- → **プレイヤー配合が本番に載るのは、本番の血統修復 → `0060` → `tools/deploy.sh` の後だけ**。新しい移行はこの順序の**後ろに積む**（`0061` 以降）。**順序を追い越す移行を書かない**
- **プレイヤー配合の経路にも、同じ「鍵が壊れていたら投げる」関門を通す**（NPC 経路にだけ在る状態にしない・D-119 の「読む側だけ在る」の家族）
- staging では先に進めてよい（staging は修復済み・`REPORT_PEDIGREE_REPAIR_20260921.md`）

---

## 5. 次の順番 — ⚠️ D-074 / D-079 の改訂案を先頭へ

開発側の順（命名の検査 → 導入の永続状態 → D-074 / D-079 の改訂案 → 案 B の模擬）に対して:

1. **D-074 / D-079 の改訂案を先頭にする。** 案 A はオーナーが決めたが、**正典にはまだ入っていない**（正典の最新は D-119・2026-09-20）。このまま実装を進めると、正典（「既存 NPC 馬を 1 頭付与」）と実装（「付与＋生産」）がずれる。改訂案は、**残る規則**（無償参加・振り直しの防止・公正性・権利方針）と**置き換わる規則**を並べて書くこと。番号の起票はレビュー側が裁定で行う
2. I-2 の実装（この裁定）
3. 導入の永続状態
4. 命名の検査（`NG-NAMES-UNWIRED` と接続。★リストが無い間は**「検査していない」ことを通す側で明示する**形にし、黙って通さない — D-119 ④ の再発防止）
5. 案 B の模擬（オーナーは「測ってから判断」と言っている。急がないが落とさない）

---

## 6. 持ち越し（次の裁定で扱う・今回は決めない）

- **父母候補の出どころ**: 初回配合で NPC の繁殖牝馬を使うと、その母馬の**年 1 回の枠**（正典 §6.7）を利用者が消費する。利用者 1 人につき NPC の仔が 1 頭減るので、**馬の供給（POOL-SUPPLY）と在庫の下限監視（D-079 ⑧）に効く恐れがある**。⚠️ **未測定**。案 A は加えて付与馬 1 頭も NPC から取るので、1 人の登録で NPC 側が 2 頭分の影響を受ける。★**候補の裁定の前に、「登録 N 人あたりの供給の減り」を `pool-size.ts` の式で出してほしい**（数として書き留めず、式で）
- **命名前の仔の置き場所**: `horses.name` は NOT NULL（`db/migrations/0001_init.sql:108`）。下書きの表に置くのか、`horses` に入れて公開しない旗を持つのか。どちらでも、**命名前の仔が出走・一覧・NPC の配合相手に紛れ込まない**ことを、読む側すべてで確かめる必要がある（受入条件 3）

## [CODE] と [EYES] の区別

この裁定は全部 [CODE] による（正典・移行・ワーカーのソースを読んだもの）。**画面・待ち時間の体感は見ていない**。60 秒の待ちが体験として許容できるかの最終判断はオーナー（[EYES]）。
