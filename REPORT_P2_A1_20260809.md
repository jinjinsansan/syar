# REPORT_P2_A1 — 24時間の結果（窓: 2026-08-08 20:53 → 2026-08-09 20:53）

**開発側 → レビュー側** / 2026-08-09
**コミット**: `48adbf1` 以降（本番は `cf2eeb7` が稼働中）

## 本便で正典の改訂が要る決定をしたか

**★あり（§4）。** A-1 の「払戻の件数」が **0件**で、**この窓では実証できていません。**

---

## 1. ★結論 — 欠落・重複・前進については PASS

裁定（照会3件すべて「窓を無効にしない」）に従い、結論まで書きます。

```
窓: 2026-08-08 20:53 → 2026-08-09 20:53（24時間）

  周の記録            1,338回
  平均間隔            64.4秒（TICK_MS=60秒 ＋ 処理時間）
  ★最大間隔          568秒 ＝ 9分28秒
                      → ★サイクル長 600秒 を下回る。
                        一度も「1サイクルぶん遅れる」ことがありませんでした

  生成した周          144  ← ★24時間 ÷ 10分 = 144 の理論値ちょうど
  確定した周          138  （残りは2周先まで作る先行分）
  周の処理の失敗      0
  ロック衝突          0
  開催中止            0
  日次集計            6回
```

### DB 側（`scheduled_at` が窓内のレース）

```
  cycle_index    72 〜 214    行数 143    相異なる周 143
  ★欠落 0件 / 重複 0件（連番・一意）
  status: settled 142 / cancelled 1（★cancelled は D-037 の検証で作ったもの）
  オッズ行 272,103
```

**A-1 の「欠落も重複もなく前進し続ける」は満たしています。**

---

## 2. ★ご指示の確認 — 7分51秒に発走したレースはどうなったか

**該当は1件、レース 141（発走 08:40:00）です。**

```
  生成      停止より前に完了済み（オッズ 1,161行）
  発売      ★止まっていません
  確定      08:40:00 の予定に対し 08:46:00 → ★6分の遅延
```

### ★前便までの私の記述が誤っていました

「**客は馬券を買えませんでした**」と繰り返し書きました。**誤りです。**

**発売は `place_bet`（Postgres の関数）と Web で完結し、購入経路にワーカーは入りません。**
レース141 は停止前に生成が完了しており、オッズも揃った `scheduled` 状態のまま
発売時間帯（〜08:39:30）を過ごしています。**買える状態は保たれていました。**

★止まったのは**生成と確定だけ**です。そして生成は2周先まで作る余裕（1200秒）があるので、
**停止 7分51秒では1本も間に合わなくなっていません**（欠落0件がその結果です）。

★実害は「**レース141 の確定が6分遅れた**」ことに限られます。
  ただし**当時 馬券は0枚**なので、**実際に待たされた客はいません**。

---

## 3. 窓のあいだの変動（★これも欠落0でまたいでいます）

```
  プロセスの起動      9回（うち3回は窓の起点＝A-2 の破壊試験直後）
  最長無停止区間      2026-08-08 23:58:08 → 2026-08-09 08:37:32 ＝ 8時間39分24秒
  1周の生成時間       0.4秒 → 116秒 → 256秒（M を2回変更・640倍）
  メモリ              67MB / FD 26個 / DB接続 1本（現在値）
```

---

## 4. ★判断をお願いしたいこと — 「払戻」が0件です

そちらは A-1 の報告に「**生成・確定・払戻の件数**」を求められました。

```
  生成 144  ✔
  確定 138  ✔
  ★払戻  0  ← 窓内の馬券が 0枚
```

**認証が未実装で実ユーザーがいないため、24時間のあいだ本物の馬券が1枚も存在しません。**

★払戻の経路自体は、この窓の中で `verify-economy` により通しています
（17点購入 → 確定 → **15,230 PP 払戻** → §8.6 検証 PASS → 二重確定 PASS）。
ただし**検証スクリプトが後片付けをするので、DB に痕跡が残っていません。**
現在 `users` / `bets` / `ep_ledger` / `pp_ledger` はすべて **0行**です。

```
こちらの読み（判定ではありません）
  足りる側: 払戻経路は窓内で実証済み。A-1 が問うのは「ループが止まらないこと」で、
            客がいないことは worker の性質ではない
  足りない側: 「24時間、払戻され続けた」という主張は**していない**。
              §9.3 の PP 発行が24時間動いた証拠は無い
```

**足りないという裁定であれば、次の窓で常設のテストユーザーを1つ置き、
毎周1点だけ買わせて痕跡を残します**（後片付けしません）。**ご指示ください。**

---

## 5. 申し送り

- ★**リークの記録形式**: 次の窓から**周ごとにメモリ・接続数・FD をログに出します**
  （今回、最長無停止区間の両端を記録しておらず、後から取得できませんでした）
- ★**λ*=60 の100レース実測**は、私の作業中断でプロセスが落ち **30/100 で停止**しました。
  **回し直しています**（約13時間・明朝完了見込み）
- ★**測定計画時にも「正典の改訂が要る決定をしたか」を通す**手順を `HANDOFF_P2` に追記しました。
  過剰な厳しさは緩さと違い**誰にも咎められないまま作業を溶かす**、という指摘を含めています

---

## 6. 合格基準

| # | 状態 |
|---|---|
| **A-1** | **★欠落・重複・前進は PASS。払戻0件の扱いのみ §4 で照会** |
| A-2 | **PASS** |
| A-3 | **PASS** |
| A-4〜A-9 | **PASS**（457 tests / typecheck 0） |
