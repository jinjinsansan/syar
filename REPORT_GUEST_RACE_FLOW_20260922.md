# ゲスト観戦導線（2026-09-22）

## 変更

- TOP の主ボタン「はじめる」「ログイン」はそのままにし、下の案内から `/watch-race` に進めるようにした。
- `/watch-race` はログインなしで使える。空の映像枠には既存のレース画像を表示し、「映像はデモ」と明記する。
- 演出を観ると `/race` が準備完了後に自動再生する。ゲストが演出を終えた場合は `/watch-race` に戻り、ログイン済みの人は従来どおり `/home` に戻る。
- 公開演出中の開発用切替（演出・視点・展開・回り等）を隠す。全画面・音・停止・戻る操作は使える。
- `/race` の読み込みが長い場合は経過秒数を出し、10 秒後に結果一覧へ進める。

## 確認と限界

- 390px の実ブラウザで、TOP の横はみ出し 0、44px 未満の操作 0。観戦ページの画像は読み込め、ゲスト用リンクは `/race?return=/watch-race` だった。
- タッチ端末の公開モードを実ブラウザで開くと演出が再生し、公開操作は「全画面」「音」「停止」「観戦へ戻る」だった。
- 映像は固定プールの演出デモで、常設の実レース情報・着順とは連動しない。実レースの生中継と誤認させない表記を保つ。
- 公開レース検査ツールを `tools/lib/classification.mjs` の読取専用欄へ登録した。ツール分類の検査 21 件は通過。
