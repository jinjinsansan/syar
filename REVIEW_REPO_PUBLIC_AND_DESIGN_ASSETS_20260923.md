# 【STAR】裁定: リポジトリは公開だった ／ デザイナーへ GitHub のリンクを渡す ／ 誕生カットの素材 — 2026-09-23

- 案件: **STAR**（馬物語）／レビュー側 Claude
- きっかけ: オーナー指示「これも GitHub をデザイナーは読めるので含めてください」「誕生カットの素材は開発側が焼いていくしかないはず」

## 1. 🔴 事実の訂正 — `jinjinsansan/syar` は**公開**リポジトリだった

✔ レビュー側が自分で確かめた（読むだけ）:
- `https://api.github.com/repos/jinjinsansan/syar` → **`"private": false`**
- `https://raw.githubusercontent.com/jinjinsansan/syar/main/STAR_SPEC_v2.0.md` → **200**（正典が誰でも読める）

→ ★**裁定 `REVIEW_CONSULT_HORSE_LIFECYCLE_VERDICT_20260916.md` §6 の前提「このリポジトリは非公開」は誤り**。★私は 09-23 にその前提のまま「GitHub のリンクを外す」と裁定した（`63cfecc`）。★**前提を確かめずに、文書の記述を引き写した**

### ✅ 秘密の値は入っていない（確かめた）
- `git.txt`・`secrets*.env`・`.env` は `.gitignore` に在り、**履歴にも追加された形跡が無い**（`git log --all --diff-filter=AM` で 0 件）。追跡されているのは `.env.example` だけ
- → ★**資格情報の流出は無い**

### ⚠️ ただし、公開されているもの（オーナーの判断を仰ぐ）
- **正典**（`STAR_SPEC_v2.0.md`）: 経済の較正・確率・検証の線・**弁護士ゲートの項目**・過去の欠陥とその直し方
- **報告と裁定**: いま開いている弱点（★例: 09-22〜23 の裁定に「**本番の一般登録が開いている**」「無償の配合が 1 口座 1 回取れる」と書いてある）
- ★攻める側から見ると、**どこが弱いか・いつ直るか**が読める状態

**オーナーへの問い（急ぎではないが、告知の前に）**: リポジトリを公開のままにするか、非公開にするか。★公開のままにするなら、**「いま開いている弱点」を書いた文書を公開の枝に置かない**運用（別の場所・非公開の場所に置く）を決める必要がある。

## 2. デザイナーへ GitHub のリンクを渡す — ✅（オーナー指示）

- ★公開リポジトリなので、リンクを渡しても「非公開のものを外部に渡す」ことにはならない。★09-16 の決定の前提が違っていたので、**この件はオーナーの指示のとおりにする**
- ★渡してよいもの（デザイナーが第 2 便で要る画面のソース）:
  - 引退馬の画面 `apps/web/src/app/stable/retired/page.tsx`（生涯の記録の色）
  - 初回設定 `apps/web/src/app/setup/page.tsx`／調教 `apps/web/src/app/train/page.tsx`／レースの帯 `apps/web/src/components/uma/race-strip.tsx`／既存デザイン `design/hud-ds/components/setup/index.html`
- ⚠️ ★**push していない変更はリンク先に無い**（作業の枝は push 済みだが `main` は配備の sha まで）。★リンクを渡すときは**枝と sha を指定する**（`.../blob/<sha>/...`）と、食い違いが起きない
- ⚠️ ★**スクリーンショットの代わりにはならない**: ソースを読んでも、実際の見た目（色・余白）は分からない。★第 2 便の「生涯の記録の色」は、**画面の画像**があるほうが確実

## 3. 誕生カットの素材 — ✅ 開発側が作る

- オーナーの判断: 「開発側が焼いていくしかない」。★既にその作り方が在る（`apps/web/public/art/*.prompt.txt` と対になる画像・アートバイブル）
- **条件**:
  1. ★**アートバイブルと既存の絵柄に合わせる**（新しい絵柄を作らない）
  2. ★**プロンプトの文と、出した画像を対で残す**（既存の `*.prompt.txt` と同じ作法）。★どの版を採ったかが後から分かるように
  3. ★**他社の作品名・キャラクター名・画風の固有名をプロンプトに書かない**（§0.1）
  4. ★出来たらレビュー側が見るのではなく、**オーナーとデザイナーが見る**（絵の良し悪しは [EYES]）。★レビュー側が見るのは、①プロンプトに固有名が無いか ②置き場所と命名 ③大きさ（Web の初回ロード・技術的前提）

## 4. 追記（2026-09-23）— オーナー判断: **A（公開のままにする）**

**決定（オーナー）**: リポジトリは公開のままにし、★**「いま開いている弱点」を書いた文書は公開の場所に置かない**運用にする。

### 🔴 いまの状態（先に事実を置く）
- ★**もう公開されている**: `HANDOFF_AUTH_TO_REVIEW_20260918.md`（開発側・09-18）に「E-8 `disable_signup = false` ＝ いま誰でも登録できる」と書かれており、★**これは 09-18 から公開**。今日の裁定（`REVIEW_D113_SIGNUP_OPEN_VERDICT`）も `86040e0` で push 済み
- → ★**「これから書かない」だけでは、いま開いている口は塞がらない**

### 運用（これから）
1. ★**公開の枝に置かない文書**: いま開いている弱点・その再現手順・関門の外し方・本番の設定の値（`disable_signup` などの現在値）
2. ★**置き場所**: ①当面は `star/` の**追跡しない場所**（`.gitignore` に `private/` を足す）②できればオーナーが**非公開のリポジトリを 1 つ作る**（バックアップが要るため。★オーナーの操作）
3. ★公開の側には**簿の項目名だけ**を置く（例 `SIGNUP-OPEN`）。★中身は書かない
4. ★過去の分は**消さない**（履歴から消すには書き換えが要り、公開済みなので効果も薄い）。★代わりに**弱点そのものを早く閉じる**

### ⚠️ レビュー側からオーナーへ（判断はオーナー）
- ★**一般登録が開いていることは、すでに公開の場所に書かれている**。★「URL を誰も知らない」という前提は、リポジトリが公開である以上、**もう成り立っていない**（リポジトリから本番の URL も RPC の名前も読める）
- ★塞ぐのは Supabase の管理画面で 2 分。★**いま閉じることを勧める**（招待は管理画面の「Invite user」でできる）

## 5. 追記（2026-09-23）— 対応（`4249dcd`・`e46fb74`）と、🔴 誕生カットの絵柄

### ✅ 受理
- ✔ 依頼文の GitHub のリンクは **sha 固定**（`/blob/86040e0/…`）の 6 本。★`raw` で 200 を確かめ、「枝の先頭は動くのでこの sha を見る」「ここに無い変更は未 push のことがある」も書いた（§2 の条件どおり）
- ✔ 共通の決まりから毛色を外し、訂正の理由を書いた（§2・§3）
- ✔ 誕生カットのプロンプト `design/art/prompts/intro-birth-cut.txt`: ★**固有名・文字・記号・人物・馬具なし**を明示（§0.1 を満たす）。★**特徴的な白斑や靴下を禁じた**のは正しい（全員の仔に出る絵なので、特定の個体に見えない）。★「毛色 3 種」を 1 枚に戻し、その理由を文中に書いた

### 🔴 絵柄が正典（アートバイブル）と食い違う
✔ [CODE] `design/art/art-bible.md`:
- `:4` 制作経路 = **ピクセルアート生成 → アニメフレーム → スプライトシート**
- `:15` 一行規則 = 「競馬場の渋さを、**ピクセルで**。」
- `:86` 「参考は 3D、こちらは**ピクセルアート**」／`:154` 表示倍率は**整数倍のみ**（非整数倍で拡大縮小すると絵が壊れる）／`:203` 禁止事項にも同じ

→ ★プロンプトは **`Painterly, low-saturation palette`**（絵画調）と書いている。★一行規則は引用しているのに、**その規則が指す絵柄（ピクセル）と逆の指定**になっている。

**決定**:
1. ★**絵柄をピクセルアートに直す**（アートバイブルの制作経路・`:4`）。★色数と解像度は、既存の `apps/web/public/art/` の絵に合わせる
2. ★出力の寸法は、**表示する大きさの整数倍**にする（`:154`）。★1536×1152 のまま UI で縮めると、非整数倍になって絵が壊れる。★**先に画面での表示寸法を決めてから、その整数倍で出す**
3. ★参考に渡すファイル（`horse-jockey-side-v8.png`）が、いまの本編の絵柄そのものかを確かめる。★違う絵柄の参考を渡すと、そこに引っ張られる
4. 絵の良し悪しは [EYES]（オーナーとデザイナー）。★レビュー側が見たのは、①固有名が無いこと ②絵柄が正典と合うこと ③大きさの作法
