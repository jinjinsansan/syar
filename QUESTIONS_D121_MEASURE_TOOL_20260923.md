# 照会: D-121 ③ の測る道具（利用者の繁殖牝馬の素質の分布・頭打ちに達した利用者の割合）— 2026-09-23

> 開発側 → レビュー側。正典 D-121 条件 ③、裁定 `REVIEW_I1_STEP3_ROLE_REQUEST_VERDICT_20260922.md` §5「次の順番」3。
> コードはまだ書いていません。本番には触れていません。

## §0 一行

「素質の合計の分布」は、Q-2 の模擬と同じ物差しで測れます。NPC の現役馬の素質の合計の中で何 % 点に当たるか、です。
**「頭打ちに達した」の定義が正典に無い**ので、決めてほしいです（M-2）。数は画面に出さず、道具の出力だけに出します（D-114・D-116）。

## §1 事実

| # | 事実 | 出どころ |
|---|---|---|
| F-1 | Q-2 の模擬は「仔の素質の合計が、NPC の現役馬の中で何 % 点か」を世代ごとに見た。N（手がかりで選ぶ）は 60〜70% 点、O（素質が見える上限）は 85〜90% 点で平らになった | `REPORT_Q2_LINEAGE_SIM_20260922.md` §0・§2 |
| F-2 | DB には、各馬の `potential`（素質）・`sire_id`／`dam_id`・`birth_week`・`owner_id` がある。**過去の時点の NPC の分布は残っていない**（その時の世界の写しが無い） | `horses` |
| F-3 | NPC の世界の素質の分布は、V-2a（平坦化）で世代を通じてほぼ動かない設計 | 正典 §13.2 |
| F-4 | 本番の読み取りは許可制。道具は読むだけの取引で、オーナーに 1 コマンドで渡す | 記憶・`diag-owned-breeders` と同じ形 |

## §2 案

`tools/measure-lineage-plateau.mjs`（READONLY・`begin read only`）

- **M-1 分布**（定義は Q-2 と同じ）:
  - 基準は、**いまの** NPC の現役馬の素質の合計の分布。
  - 利用者の繁殖牝馬（`owner_id` あり・`retirement_role = 'broodmare'`）1 頭ずつの素質の合計が、その中の何 % 点かを出す。
  - 出すもの: 頭数・10／50／90% 点。利用者ごとの最良の母の % 点の分布も出す。
- **M-2 頭打ち**: 下の (a)〜(c) から決めてほしい。
- 出力は数だけ。利用者の ID・馬名は出さない。
- ⚠️ 過去の NPC の分布が残っていない（F-2）ので、どの世代の仔も**いまの** NPC と比べる。F-3 により大きくはずれない見込みだが、**見込み**である。

## §3 決めてほしいこと

- **M-2 「頭打ちに達した利用者」の定義**
  - (a) **推し**: **伸びが止まった**で見る。
    - 自家の繁殖牝馬から生まれた仔を、母系の世代で並べる（母 → 娘 → 孫娘…）。
    - 各世代の最良の仔の % 点が、**直近の 2 世代続けて、それまでの最良を上回らなかった**利用者を「頭打ち」とする。
    - 世代が 3 つに満たない利用者は「まだ判定できない」として別に数える。
    - 数（2 世代・上回るの幅 0）は発明なので、決めてほしい。
  - (b) **高さ**で見る: 最良の母が N の平ら（60〜70% 点）に届いた利用者。
    - 模擬の数を線にするので、実際の利用者の遊び方が N と違えば、意味が変わる。
  - (c) 両方を出す（判定には使わず並べるだけ）。
- **M-3 近交の併記**: 頭打ちを越えるのは近交を重ねた場合だけ（F-1）なので、利用者ごとの最良の母の近交係数と、仔の虚弱の割合も並べるか。開発側は**並べる**を推す（B の判断の材料）。
- **M-4 いつ流すか**: 公開の後、オーナーが本番の読み取りとして流す。定期にするか（ワーカーの日次集計に入れるか）は、公開後のデータを見てから。→ これでよいか。
