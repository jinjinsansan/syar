# REPORT_P2_CLOSE — P2 のクローズを申請します

**開発側 → レビュー側** / 2026-08-10
**コミット**: `ca575f9`（push 済み・**本番も同一**・471 tests 全PASS / typecheck 0）

## 本便で正典の改訂が要る決定をしたか

**なし。** ただし**引き継ぎ事項が2件**あります（§5）。

---

## 1. クローズ条件が揃いました

| # | 内容 | 最終検証 | 結果 |
|---|---|---|---|
| A-1 | 10分サイクルが無人で24時間 | `c5df6f6` | **PASS**（受理済み・★メモリの根拠は差し替え） |
| A-2 | 二重生成・二重払戻が起きない | `eef7d8a` | **PASS** |
| **A-3** | V-10 払戻率が券種別に ±1% | **`ca575f9`** | **PASS** |
| A-4 | `seed_reveal` が確定前に取れない | `eef7d8a` | **PASS** |
| A-5 | EP減算と発券が原子的 | `eef7d8a` | **PASS** |
| A-6 | 自馬ベット制限 | `eef7d8a` | **PASS** |
| A-7 | 環境ガード | `eef7d8a` | **PASS** |
| **A-8** | P0/P1 の全ゲートが本番構成でも通る | **`ca575f9`** | **PASS** |
| A-9 | テスト全PASS・typecheck・`any` ゼロ | **`ca575f9`** | **PASS**（471 tests） |

`docs/ACCEPTANCE_P2.md` に SHA と、**古い SHA が有効な根拠**を書きました（R-23）。

---

## 2. ★A-3 — そちらの読みが正確でした

```
              乖離      SE      |乖離|+2SE
win          -0.00pt  0.051pt     0.10
place        +0.01    0.025       0.06
ワイド        +0.02    0.034       0.09
馬連         +0.11    0.071       0.25
馬単         +0.09    0.071       0.23
三連複       -0.06    0.079       0.22
★三連単     -0.35    0.098      ★0.55   （ゲート 1.00 → 余裕 0.45pt）
★上限に当たった売り目: 0 件      ★V-10 総合: PASS
```

```
           三連単の乖離    SE      余裕
100レース    -0.48      0.209    0.10pt
400レース    -0.35      0.098   ★0.45pt
```

★対比が明確です。

```
本番の M を2倍（λ*=60）  → 余裕 0.10 → 0.12pt   代償: ★本番の計算量が永久に2倍
測定を4倍（400レース）   → 余裕 0.10 → 0.45pt   代償: オフラインで一度きり
```

**私は本番のコストを払って 0.02pt を買っていました。**

★**三連複も −0.27 → −0.06pt に動いています。**残差そのものが100レースでは過大に見えていた
ということで、**「残差」と「測定の粗さ」を分けていなかった**のは私の側の誤りでした。

---

## 3. A-8 — `1bfebef` 以降、初めての証拠です

```
4シード × 100/300ゲーム内年
V-1 / V-2a / V-2b / V-2d / V-2e / V-2f / V-3   すべて PASS
V-2c（300年での V-1 が 12〜18%）: PASS  [最小 15.18% / 最大 16.49%]
総合: PASS
```

---

## 4. D-043 — 実行時の開発依存を外し、配備しました

```
旧  ExecStart=node_modules/.bin/tsx apps/worker/src/main.ts
新  ExecStart=/usr/bin/node /opt/star-current/dist/worker.cjs
```

**7分停止の根本原因を、構成ごと直しました。**

★調べる過程で**もう1つ**出ました。**`pg` も devDependency で、`apps/worker` はどこにも
宣言していませんでした。**`npm ci --omit=dev` は tsx だけでなく **`pg` も消していました** —
仮に tsx が残っていても **DB に繋げませんでした**。事故ではなく**必然**だったという
ご指摘のとおりです。`esbuild` も **tsx 経由の推移的依存**で、tsx を外した瞬間に消えるところでした。

### ★測定のずれが構造ごと消えました

```
配備後: MainPID = /usr/bin/node dist/worker.cjs （★1本だけ）
        ps の 132,616 kB = 129MB  ＝ 自己報告 mem=129MB   ★一致
        fd 35 → 23 / sock 9 → 3 / handles 3 → 2
```

**ラッパーが消えたので、「どちらのプロセスを測っているか」という問題自体が無くなりました。**
本番機で **node_modules 無しの単体起動**も確認済みです。

---

## 5. ★引き継ぎ（P3 の前に必要なもの）

### ① staging がありません（D-042・**オーナー作業待ち**）

プロジェクトは作っていただきましたが、**接続文字列が未取得**です
（組み立てた接続先が「テナントが見つからない」で拒否＝**別リージョン**）。
**推測でホスト名を当てにいくのはやめました。**

★私の失敗を1件申告します。**受け渡しファイルの中身を「マスクして」表示しようとして、
  DBパスワードと2つのキーをそのまま出しました。** 正規表現が `キー=値` を前提にしていたのに、
  **実際は値が独立した行**にあったためです。**形式を確かめずにマスクを当てました。**
  オーナー判断で「このまま進める」となり、**staging は合成データのみ**なので実害は限定的です。
  → 以後は**表示という手段自体をやめ**、値を読まずに変換するツールを通します
    （`tools/import-staging-secrets.mjs`）。

### ② ★検証はソースを、本番はバンドルを動かしています

D-043 の副作用として、**試験対象と本番の実行物が別**になりました。

```
検証ツール : TypeScript のソース（tsx 経由）
★本番      : esbuild のバンドル（dist/worker.cjs）
```

同じソースからの機械的変換ですが**同一ではありません**。esbuild が取り違えても、
いまの検証では捕まりません。緩和として配備の前提条件が**バンドルを実際に起動**していますが、
**検証ツールの少なくとも1本をバンドルに対して流すべき**です。

★これは **staging が要ります**（R-24 で本番には向けられないため）。
  **staging が用意でき次第、最初にやること**として `docs/ACCEPTANCE_P2.md` に記録しました。

---

## 6. P2 で作ったもの（実装の到達点）

```
10分サイクル → レース生成（番組表・条件・出走表・オッズ7券種）
             → 馬券購入（EP減算と発券が原子的・冪等・自馬制限・§9.4 の上限）
             → 確定（§8.6 の final_seed・seed_reveal 公開）
             → 払戻（EP で買い PP で払う）＋ 賞金（PP の主な発行源）
             → 日次集計（★内部口座を別掲）
             → 確定できなければ60分で開催中止・EP 全額返還（通報つき）
```

- **配備**: リリースディレクトリ＋symlink 切替・前提条件・ヘルスチェック・ロールバック
- **監視**: 周ごとに mem/heap/fd/sock/handles
- **安全**: 状態を変えるツールは本番に向けたら起動拒否（分類簿＋メタテスト）

---

**P2 のクローズをご判断ください。**
