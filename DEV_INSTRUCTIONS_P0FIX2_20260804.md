# 開発指示書 P0-fix2 — 回帰中心の形質別化 / 検証ゲートの強化

**発行**: 2026-08-04（レビュー側 Claude）
**前提**: `REVIEW_P0FIX_VERDICT_20260804.md` を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**D-009 を受けて §6.4・§13.1・§13.2 を改訂済み。必ず読み直すこと**）

---

## 0. まず結論 — P0-fix は CONDITIONAL PASS でした

F-1/F-2/F-3 は指示書どおりに実装され、`npm run verify` と `npm run verify:distance` の出力はレビュー側の環境で**一行一句まで完全再現**しました。テスト58件・typecheck・決定論も独立確認済みです。

特に評価した点:

- **`legacyRatio` に `pass` フィールドを持たせないことで旧基準を構造的に排除**した設計。テストでも `not.toHaveProperty('pass')` で固定されている
- 既定値をテストでリテラル固定した点（`REGRESSION_RATE=0.2` を 0 に戻す変更は確実に落ちる）
- **検算依頼 2-1 の指摘が正しかった**こと。レビュー側監査 A-2 の「拡散」は回帰無効時の観測であり、回帰を入れると収縮へ反転する — F-3 が F-1 とセットで必要だったという主張を実測で確認しました。正典 §6.4 に理由込みで明文化しています
- §4-4 で不利な実測（0.67x〜1.53x の振れ）を隠さず原因を切り分けて申告した姿勢

自己申告した逸脱4件はすべて承認、Q1〜Q3 にも回答済みです（回答書 §4・§6）。

**ただし未申告の副作用が1件見つかりました。** 下記 G-0 が本指示書の主目的です。

---

## 1. スコープ

### G-0【最優先】回帰中心を形質ごとの創始水準にする（D-009）

回帰中心が `bounds.min + (max-min) × 0.45` で全11形質に一律適用されているため（`genetics.ts:194-195`）、創始中心と一致するのは能力5種だけで、**丈夫さが 650 → 450 へ恒久的に移動**していました（実測20年で収束）。丈夫さは選抜対象外のため対抗する力がなく、§7.5 の故障率の分母なので `INJURY_BASE` の較正が無効化されます。気性50→45・適性55→45 も同様です。

**これはレビュー側の指示 F-1 の副作用です。** 実装は指示に忠実でした。

正典 §6.4 に形質別パラメータ表を新設し、§13.1 に `TRAIT_MUTATION` を記載しました:

| 形質 | 品種中心 | 他 |
|---|---|---|
| 能力5種 | 450 | sd 90 / clamp ±150 / r 0.20 |
| 丈夫さ | **650** | 値域比スケール / r 0.20 |
| 気性 | **50** | 同上 |
| 芝/ダート適性 | **55** | 同上 |
| distance_center | **2100** | sd 73 / clamp 243 / r 0.01 |
| distance_range | **700** | sd 24 / clamp 80 / r 0.01 |

- 既存の `TraitMutationSpec` に `center` を追加する形でよい（器は既にある）
- 創始定義（`FOUNDERS`）と回帰中心がずれないよう、**可能なら創始定義から導出**し、テストで一致を固定すること
- `REGRESSION_CENTER_RATIO` は役目を終える。残す場合もフォールバック用途に限定し、正典から参照されない旨をコメントすること

### G-1【必須】F-3 の定数をテストでリテラル固定する

`regression.test.ts` の距離テストは検証対象の定数を自分で読んで自分と比較する**トートロジー**になっており、`sd:73` が 200 に変わっても素通りします。報告書 §4-2 自身が「r を 0.01→0.02 にすると SD が 0.86倍まで縮む」と示すとおり平衡は定数に敏感なので、F-1 と同じく `toBe(73)` / `toBe(0.01)` 等のリテラル固定にしてください。**G-0 で追加する `center` も同様に固定**すること。

### G-2【必須】V-2d / V-2e を実装する（正典 §13.2）

| 基準 | 内容 |
|---|---|
| **V-2d 全形質の水準維持** | 能力5種だけでなく**丈夫さ・気性・芝/ダート適性・distance_center・distance_range** の集団平均が、それぞれ創始水準の **±10% 以内** |
| **V-2e 距離適性の分化** | `distance_center` の集団SDが創始水準の **0.8〜1.4倍** |

- `CohortStat` に非能力形質の平均（と距離のSD）を追加する必要があります
- V-2d は「V-1〜V-3 が能力5種しか見ていないせいで G-0 を見逃した」ことへの構造的な手当てです。**今後どの形質が追加されても自動で監視対象に入る**形が望ましい
- 合格基準4（距離分化）は目視でしたが、V-2e として機械ゲートに格上げします

### G-3【必須】`verify.ts` の総合判定を `simulate` と揃える

`--horizons 100` だけで回すと V-2c 未評価の警告なしに `総合: PASS` が出ます（`simulate` 側は警告を出すので非対称）。V-2c の評価有無を総合判定と出力に反映してください。

### G-4【必須】検証の既定集団規模を実運用に寄せる（Q3 への回答）

正典 §10.5 の NPC 繁殖牝馬プールは **800頭**です。検証の目的は実運用の予測なので、**`npm run verify`（受け入れ判定）の既定を繁殖牝馬800頭**にしてください（種牡馬プールは現行比率を維持）。

- 高速反復用に `--population 200` は引き続き使えるようにし、**小集団では浮動 artifact が出る**旨をレポートに注記すること
- P0 第1号指示書の「200頭」からの変更ですが、V-2 系の基準自体が刷新済みで P0 との数値比較の必要はもうありません
- **実行時間を計測して報告してください。** 受け入れスイートが極端に遅くなるなら妥協案（例: 400頭）を提案してよい

---

## 2. やらないこと

- 大物覚醒（`BIG_ATAVISM_RATE`）は**引き続き触らない**。Q2 の懸念（P1 で万能型に収束する恐れ）は正当ですが、v1 では機構を維持し、**P1 の V-4〜V-6 較正時に「距離適性の分化が選抜下でも保たれるか」を確認する**項目として引き継ぎます
- レースエンジン・オッズ・育成ループ・UI・DB（P1 以降）
- V-2a/V-2b/V-2c の閾値変更（オーナー承認事項）

---

## 3. 合格基準

| # | 基準 |
|---|---|
| 1 | V-1 が 12〜18%（100年・300年とも） |
| 2 | V-2a / V-2b / V-2c すべて PASS |
| 3 | **V-2d**: 全形質の集団平均が創始水準 ±10% 以内（**特に丈夫さが 650 近傍を保つこと**） |
| 4 | **V-2e**: `distance_center` の集団SDが創始水準の 0.8〜1.4倍 |
| 5 | V-3 が設定値 ±0.5pt |
| 6 | 全ユニットテスト PASS・typecheck exit 0・`any` ゼロ・**F-3 と G-0 の定数がリテラル固定** |
| 7 | 決定論（同一シードの出力が完全一致） |
| 8 | **4シード以上 × 100/300年**で 1〜5 が安定して成立 |

G-0 で丈夫さの水準が変われば故障率の実効値も変わります。**P0 段階では故障判定を回していない**ので影響は出ませんが、`INJURY_BASE` は創始水準 650 を前提とする旨をコード側コメントに残してください（P1 への申し送り）。

---

## 4. 報告様式

`REPORT_P0FIX2_[日付].md` に:

1. 変更点（G-0〜G-4 それぞれ）
2. 再実行コマンド（**`cd star`**。前回の報告書 §2 は `cd syar` の誤記でした）
3. 検証結果（4シード以上 × 100/300年・**V-2d の形質別の表を必ず含める**）
4. `npm run verify` の実行時間（G-4 の判断材料）
5. 定数の調整履歴
6. 仕様からの逸脱・解釈で埋めた箇所（**全件列挙**。ゼロなら「なし」と明記）
7. 未解決の質問

レビュー側が全コマンドを独立再実行して照合します。

---

## 5. 補足

- `CLAUDE.md` のコマンド節が P0-fix 前のまま（`npm run verify` / `--long-horizon` が無く、退役した「V-2」呼称が残る）でした。**レビュー側で更新します** — 開発側は触らなくて結構です
- `sweep.ts` を「役目を終えた記録」としてヘッダ明記のうえ残置した判断は妥当です
