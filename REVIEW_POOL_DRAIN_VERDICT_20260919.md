# 裁定 — 🔴 POOL-DRAIN（世界の馬が一斉に引退し、作る仕組みが無い）／TL-1／MD-1

**日付**: 2026-09-19（夜）
**対象**: `SEED-LOCKSTEP`（全頭が同じ位相）の親にあたる所見
**発行**: レビュー側
**重さ**: 🔴 **配備を止めうる**（★ただし §3 の数が出るまで確定しない）

---

## 1. ✔ 数えたこと

| 見たもの | 事実 |
|---|---|
| `birth_week` を書いている**製品コード** | ★**0 件**。書くのは `tools/age-horses.mjs` ／ `verify-b1.mjs` ／ `verify-training-week.mjs` ／ `verify-unlock-daily.mjs` の**道具だけ** |
| `insert into horses` | ★**製品コードに 0 件**（`tools/seed-world.mjs`・`tools/verify-a6.mjs`・検査 1 本のみ） |
| 配合 | `packages/sim-engine/src/breeding.ts` は**判定だけ**。★**行を作る経路が無い** |
| 引退 | ✅ `packages/training/src/retirement.ts:43` `if (ageWeeks >= LIFECYCLE_WEEKS.retireAt) return 'age'` — ★**動いている**（毎週） |
| `birth_week` の種類 | ★**1 つだけ**（全頭 −160・開発側の実測） |

---

## 2. 🔴 所見

★開発側が見つけた「`birth_week` が 1 種類」は、★**献立の位相だけの話ではない**。

→ ★★**全頭が同じ年齢。**
→ ★★**全頭が、同じゲーム内週に 260 週（強制引退）に達する。**
→ ★★**そして、新しい馬を作る製品コードが 1 行も無い。**

★**あるゲーム内週に現役 7,333 頭が一斉に引退し、プールが 0 になる。★二度と戻らない。**

⚠️ ★世代 0〜7 が在るので「繁殖が回っている」ように見えるが、
★**それは `seed-world.mjs` が最初に一度で作った系図**。★**運用中に仔は生まれていない。**

### 🔴 より重い読み替え

★これは「NPC プールの在庫」の話（**D-079 ⑧**・**D-102 ⑤** の**下限監視**）。
→ ⚠️ ★**監視は在っても、供給が無い。** ★**下限を割ったことを知らせるだけで、誰も補充しない。**
→ ★**R-27 の一族ではなく、その逆** — ★**警報だけ作って、鳴ったときにやることが無い。**

---

## 3. 🔴 確定に要る数（1 つだけ）

> ★**いまのゲーム内週は何週で、260 週まであと何週か。**

`age = current_week − (−160) = current_week + 160` → ★**`current_week = 100` で全頭が引退**。
★1 ゲーム内週 ＝ 4 時間なので、★**残り週数 × 4 時間**が寿命。

★**この 1 つの数で「配備を止めるもの」か「先の話」かが決まる。★推測しない。**

---

## 4. 🔴 ★ただし、原因が別かもしれない — 先に確かめること

`tools/verify-unlock-daily.mjs:79`:
```js
await c.query("update horses set birth_week = null where retired_at_week is null");
```
→ ★**検査の道具が、現役馬 全頭の `birth_week` を null にする。**

⚠️ ★**`birth_week` が全頭 −160 で揃っているのは、これが原因かもしれない**
（★一度 null にされ、`age-horses.mjs` で一括で入れ直された）。
→ ★**もしそうなら `SEED-LOCKSTEP` は「初期化の仕様」ではなく「★道具が壊した状態」。★意味がまったく変わる。**

> **TL-1** ★検査の道具が、共有の状態を**書き換えたまま戻さない**形を全数分類する。
> ★今日 3 件（`verify-ds7-cancel` が staging を汚した／`verify-v11-synthetic` が生きたレースを消費した／これ）。
> ★**4 件目が在る。**

---

## 5. 🔴 合成の較正も、実物と違う調子で測っている

`apps/cli/src/race-field.ts:580` は `trained?.condition ?? rng.int(2, 4)`。

| | 調子 |
|---|---|
| 合成の V-4/V-5/V-6 | **{2, 3, 4}** |
| 実物（開発側の実測） | ★**95.6% が {0, 1, 2}** |

→ ★★**V-4 は、ゲームが実際に走っている調子で一度も測られていない。** ★**R-30 そのもの。**

⚠️ ★これは「合成が間違い」ではない（★合成は定常を仮定してよい）。
★**問題は、実物が定常でないこと。** ★§3 の数が出れば、どちらを直すかが決まる。

---

## 6. ✅ 開発側の手順について — ★今日いちばん重要な一手

> 「測った数が模型と合わない」を、そこで止めずに 1 段 掘る

★**対照より強い。** ★対照は「変えたら落ちるか」を見るが、
★**予測との食い違いは「自分がまだ知らないこと」を指す。**
★開発側は「92% が 50–74」で止まれた。★止まらなかったから根が出た。

> **MD-1** ★測る前に**模型が何を出すか**を書いておく。
> ★合わなければ、★**測定を疑う前に、模型が知らない何かを探す。**

---

## 順

1. 🔴 **§3 の数**（いまのゲーム内週・260 まで残り何週）— ★これだけ先に
2. 🔴 **§4 の確認**（`verify-unlock-daily` が原因か）
3. `POOL-DRAIN` と `TL-1` を簿に
4. `.tmp-*`

🔴 **オーナー待ち**: CI-1 ／ CC-1 ＋ PO-2 ／ WK-5 ／ VP-4' ／ PR-3 ／ 撮り直し 2 本（期限 2026-09-26）／ zip 2 本 ／ `CLAUDE.md:10` ／ 1.68 GB
