# P0-fix 完了報告 — 平均回帰の有効化 / V-2 の新基準 / 距離適性の拡散是正

**発行**: 2026-08-04（開発側 Claude）
**指示書**: `DEV_INSTRUCTIONS_P0FIX_20260804.md`
**正典**: `STAR_SPEC_v2.0.md`（D-008 反映版・§5.2/§5.3/§6.3/§6.4/§6.5/§13.1/§13.2/§13.3 の改訂を読み直して実装）

## 結論（先に）

| 合格基準 | 実測 | 判定 |
|---|---|---|
| 1. V-1 が 12〜18%（100年・300年とも） | 100年 15.28〜16.58% / 300年 14.63〜16.14% | **PASS** |
| 2. V-2a / V-2b / V-2c すべて PASS | 下表 | **PASS** |
| 3. V-3 が設定値 ±0.5pt | 乖離 最大 0.016pt | **PASS** |
| 4. 距離適性の分化が保たれる | 平衡状態を実測で確認（§4） | **PASS** |
| 5. 全テスト PASS・typecheck exit 0・`any` ゼロ | 58件 PASS / exit 0 / `any` ゼロ | **PASS** |
| 6. 決定論 | 同一シードで JSON バイト一致 | **PASS** |
| 7. 複数シード（4以上）で 1〜4 が安定 | 4シード × 100/300年すべて PASS | **PASS** |

---

## 1. 変更点

### F-1 平均回帰を既定で有効化

`packages/sim-engine/src/balance.ts`:

| 定数 | 変更前 | 変更後 |
|---|---|---|
| `MUTATION_SD` | 45 | **90** |
| `REGRESSION_RATE` | 0（無効） | **0.20** |
| `REGRESSION_CENTER_RATIO` | 0.45 | 0.45（据え置き） |

- 正典 §13.1 の `BALANCE` ブロックに `REGRESSION_RATE` / `REGRESSION_CENTER_RATIO` / `DOMINANT_WEIGHT` が追加されたので、コード側の写経も追随させました（`BALANCE` から `DEFAULT_BALANCE` が参照する形）
- コメントを「正典外・既定無効・承認待ち」→「正典 §6.4 の機構・D-008 で採択」に全面改稿。あわせて **「弱める/外す場合は大物覚醒（§6.3）の発生率も併せて見直すこと」** という §6.3 の警告への相互参照を入れました
- CLI 冒頭の表示を実態に合わせ、意味を反転させました（`REGRESSION_RATE=0` のときに「★無効。血統インフレを抑える機構が働きません」と警告する形）
- 既定が変わってはいけない定数なので、`REGRESSION_RATE === 0.20` / `MUTATION_SD === 90` をユニットテストで固定しました（`packages/sim-engine/test/regression.test.ts`）。うっかり 0 に戻す変更を検知します

### F-2 V-2 を新3基準に置き換え

`apps/cli/src/simulator.ts` の `Verification` を差し替えました。

| 基準 | 実装 |
|---|---|
| **V-2a 平坦化** | 最終20世代の `meanAbilityTotal` を最小二乗回帰し、傾きをその期間の水準比 %/世代 で表示。判定は **絶対値** < 0.5 |
| **V-2b 天井余裕** | `最終世代の平均能力合計 ÷ 5 ÷ アレル上限(1000)` ≤ 0.80 |
| **V-2c 長期健全性** | 300ゲーム内年での V-1 が 12〜18%。`--long-horizon 300` で**同一実行内**に別途1本回して判定 |

- 旧「初期比 +50% 以内」は `legacyRatio` として**参考値のまま出力**しますが、**判定には一切使いません**。`legacyRatio` に `pass` フィールドを持たせないことで構造的に担保し、テストでも確認しています
- `--long-horizon` 未指定時は V-2c を「未実行」と明示し、総合判定にも含めません。人間可読レポートの末尾にも `※ V-2c は別実行で確認すること（--long-horizon 300）` を出します
- 用語を「100世代」→「**100ゲーム内年（血統深度 約N世代・実時間 約2.4年相当）**」に統一（ヘッダに毎回出力）

### F-3 距離適性の拡散を止める

`balance.ts` に形質別の変異上書き `TRAIT_MUTATION` を新設し、`resolveMutation()` で解決するようにしました。

```ts
export const TRAIT_MUTATION = {
  distance_center: { sd: 73, clamp: 243, regressionRate: 0.01 },
  distance_range:  { sd: 24, clamp:  80, regressionRate: 0.01 },
};
```

採用値の根拠は §4 に実測込みで書きました。距離系以外の形質は従来どおり値域比スケールです。

---

## 2. 再実行コマンド

```bash
cd syar && npm install

# ① ユニットテスト（58件）
npm test

# ② 型チェック
npm run typecheck

# ③ 100ゲーム内年（既定）
npm run simulate -- --generations 100 --seed 42

# ④ V-2c を同一実行内で判定する（100年 + 300年）
npm run simulate -- --generations 100 --seed 42 --long-horizon 300

# ⑤ 300ゲーム内年を直接回す（④の V-2c と同一の結果になります）
npm run simulate -- --generations 300 --seed 42

# ⑥ 合格基準7: 4シード × 100/300年の一覧（本報告 §3 の表を再現）
npm run verify

# ⑦ F-3 の採用値の根拠（候補比較・4シード × 100/300/600年）※ 20分程度かかります
npm run verify:distance

# 決定論
npm run simulate -- --generations 100 --seed 42 --json out/a.json --quiet
npm run simulate -- --generations 100 --seed 42 --json out/b.json --quiet
md5sum out/a.json out/b.json
```

`npm run simulate` は V-1/V-2a/V-2b/V-3（および `--long-horizon` 指定時は V-2c）がすべて PASS で exit 0 を返します。**P0-fix 後は exit 0 が正常です**（P0 では V-2 FAIL のため exit 1 でした）。

---

## 3. 検証結果（4シード × 100年 / 300年）

`npm run verify` の出力そのままです。

```
=== 100 ゲーム内年（実時間 約2.4年相当）===
   seed |      V-1    判定 |    V-2a %/世代    判定 |   V-2b 天井    判定 |   V-3 |      距離平均    距離SD     創始比 |     親子相関
----------------------------------------------------------------------------------------------------------------------
     42 |   15.28%  PASS |      -0.0603  PASS |    58.62%  PASS |  PASS |     2239m     397   1.11x |    0.305
      7 |   15.85%  PASS |      -0.0744  PASS |    57.89%  PASS |  PASS |     2245m     559   1.53x |    0.394
   2026 |   15.62%  PASS |      -0.2387  PASS |    58.54%  PASS |  PASS |     2283m     383   1.03x |    0.423
  31337 |   16.58%  PASS |      -0.0214  PASS |    57.23%  PASS |  PASS |     2474m     383   1.01x |    0.340
     平均 |   15.83%       |                    |                 |       |                     1.17x |

=== 300 ゲーム内年（実時間 約7.1年相当）===
   seed |      V-1    判定 |    V-2a %/世代    判定 |   V-2b 天井    判定 |   V-3 |      距離平均    距離SD     創始比 |     親子相関
----------------------------------------------------------------------------------------------------------------------
     42 |   14.91%  PASS |       0.0141  PASS |    59.78%  PASS |  PASS |     2469m     508   1.41x |    0.288
      7 |   14.63%  PASS |      -0.0900  PASS |    59.95%  PASS |  PASS |     2521m     482   1.32x |    0.335
   2026 |   16.14%  PASS |      -0.1258  PASS |    57.43%  PASS |  PASS |     2224m     250   0.67x |    0.363
  31337 |   15.57%  PASS |      -0.0763  PASS |     59.3%  PASS |  PASS |     2425m     425   1.13x |    0.303
     平均 |   15.31%       |                    |                 |       |                     1.13x |
V-2c（300年での V-1 が 12〜18%）: PASS  [最小 14.63% / 最大 16.14%]

総合: PASS
```

補足:

- **V-2a の傾きはすべて負またはほぼ 0**（-0.24〜+0.01 %/世代）。100年時点で既に平坦化しており、300年でも維持されます
- **V-2b は 57〜60%** で、目標の 80% に対して 20pt 以上の余裕があります。レビュー側が発見した「回帰なしでは 300年で上限の95%に飽和して V-1 が 3.24% に崩壊」という失敗モードから十分に離れています
- **V-3** は 4シードとも PASS（seed 42 の 100年で 隔世 6.012% / 覚醒 1.505% / 大突然変異 0.816%、乖離 最大 0.016pt）
- **親子相関**（中間親と産駒の能力相関）は 0.29〜0.42。P0 時点の出荷既定 0.43 からは下がりますが、これは D-008 が織り込み済みのトレードオフ（正典 §6.4 の「良い配合を続けている限り上位にいられるが、放置すれば平均に戻る」）です

---

## 4. F-3 で採用した距離系変異幅とその根拠

### 4-1. 何を守るべきか

正典 §5.2 が意図するのは「マイラー／ステイヤー／万能型の**分化**」です。壊れ方は2方向あります。

- **拡散**: 集団SDが広がり続ける → `distance_range` も広がって全馬が万能型になる
- **収縮**: 集団SDが縮む → 全馬が同じ距離型になり、距離適性が意思決定要素でなくなる

したがって目標は「集団SDを創始世代の水準（`distance_center` で **約359**）に**平衡**させる」ことです。

### 4-2. 是正しないとどうなるか（両方向の失敗を実測）

`npm run verify:distance` の出力（4シード平均・`distance_center` の集団SD / 創始は 359）:

| 設定 | 100年 平均/SD | 300年 平均/SD | 600年 平均/SD | 挙動 |
|---|---|---|---|---|
| 値域比スケール（旧）+ 回帰0.20 | 2193/**157** | 2190/**137** | 2216/**141** | **収縮**（0.4倍）→ 全馬が同じ距離型 |
| sd35 + 回帰0 | 2307/429 | 2483/426 | **2571**/391 | SDは保つが**平均が漂移し続ける** |
| **sd73 + 回帰0.01（採用）** | 2310/430 | 2410/416 | 2444/**377** | **創始水準へ収束** |
| sd52 + 回帰0.005 | 2309/425 | 2453/417 | 2506/387 | 採用案の劣化版 |
| sd73 + 回帰0.02 | 2297/393 | 2364/350 | 2393/**308** | **縮み続ける**（0.86倍） |

ここは指摘しておきたい点です。レビュー側監査 A-2 が実測された「20世代で SD 369→591 と**拡散**」は **回帰が無効だった P0 時点**の挙動です。F-1 で回帰0.20 を有効にすると、同じ値域比スケールのままでは今度は **SD 137 まで収縮**します（上表1行目）。**放置すると失敗の向きが反転するだけで、どちらでも分化は失われます。** F-3 は F-1 とセットで必要でした。

### 4-3. 採用値の導出

平衡時のアレルSD = `sd / sqrt(2r - r²)` から逆算しています。

| 形質 | 創始アレルSD | 回帰率 r | 必要な sd | 採用 |
|---|---|---|---|---|
| `distance_center` | 520（一様 1200〜3000） | 0.01 | 73.3 | **73** |
| `distance_range` | 173（一様 400〜1000） | 0.01 | 24.4 | **24** |

`clamp` は sd の 3.33倍としました。能力5種の比（150/90 = 1.67倍）より緩いのは、距離系は sd 自体が小さく、さらに 1.67σ で切ると平衡SDが創始水準を下回るためです。

### 4-4. 残っているばらつきについて（正直な申告）

採用値でも集団SDはシード間で **0.67x〜1.53x** と振れます。原因を2つに切り分けて、どちらも実測で確認しました。

**(1) 振れ幅そのものは小集団の遺伝的浮動です（実運用規模では消えます）。**

本シミュレータは繁殖牝馬200頭・種牡馬60頭（有効集団サイズ Ne≈45）ですが、正典 §10 の NPC は 2,500頭規模です。繁殖牝馬を1000頭に上げて 300年を回すと:

| 規模 | seed 42 | seed 2026 | 振れ幅 |
|---|---|---|---|
| 繁殖牝馬200頭（本検証の既定・Ne≈45） | 1.41x | **0.67x** | 0.74 |
| 繁殖牝馬1000頭 | 1.20x | **1.25x** | **0.05** |

規模を5倍にすると振れ幅が 0.74 → 0.05 に縮み、**1.2x前後へ安定して収束**します（V-1 も 15.39% / 15.13% で範囲内）。距離適性には選抜がかからない（P0 にレースエンジンがない）ため浮動が支配的で、小集団ほど振れるという教科書どおりの挙動です。再現コマンド:

```bash
npm run simulate -- --generations 300 --seed 42 --population 1000 --stallion-pool 300
```

**(2) 残る 1.2倍ぶんは大物覚醒（§6.3）です。**

単調上昇ラチェットが距離形質にも効いて SD を押し上げています。実測: 300年・seed42 で `BIG_ATAVISM_RATE=0` にすると SD 435 → **361**（＝創始水準ちょうど）。能力5種では回帰0.20 が相殺しますが、距離系は回帰0.01 なので相殺しきれません。指示書 §2 で「触らないこと」とされているため機構は維持しています（§7 Q2 に P1 の論点として記録）。

**まとめると「実運用規模では創始水準の約1.2倍で平衡し、その 0.2 は大物覚醒に由来する」**という状態です。時間とともに単調増加する拡散とは質的に異なり、4シード平均でも 100年 1.17x → 300年 1.13x → 600年 1.05x と平衡値の周りを動いています。

---

## 5. 定数の調整履歴

| # | 定数 | 変更 | 理由 |
|---|---|---|---|
| 1 | `MUTATION_SD` | 45 → 90 | 指示書 F-1 の指定値 |
| 2 | `REGRESSION_RATE` | 0 → 0.20 | 指示書 F-1 の指定値 |
| 3 | `TRAIT_MUTATION.distance_center` | 新設 `{sd:73, clamp:243, r:0.01}` | §4 の実測（候補5種 × 4シード × 3horizon から選定） |
| 4 | `TRAIT_MUTATION.distance_range` | 新設 `{sd:24, clamp:80, r:0.01}` | 同上（同じ平衡式で逆算） |

F-1 の指定値からの微調整は**行っていません**（指定どおりで全基準を満たしたため）。F-3 は指示書が「具体的な数値は開発側が実測して提案」としていた箇所です。

検討したが採用しなかった候補は §4-2 の表に全部載せています（回帰0 案・回帰0.005 案・回帰0.02 案）。

---

## 6. 仕様からの逸脱・解釈で埋めた箇所（全件）

P0 で列挙した20件は正典改訂とレビュー回答で承認済みのため再掲しません。**P0-fix で新たに生じたものは以下の4件です。**

| # | 箇所 | 判断 | 理由 |
|---|---|---|---|
| 1 | **V-2a の判定を傾きの「絶対値」で行った** | 指示書は「傾きが 0.5%/世代 未満」。実装は `|傾き| < 0.5%` | 基準名が「平坦化」なので、急落も平坦ではないと解釈しました。文字どおりに符号付きで判定するより**厳しい**側です。実測はすべて -0.24〜+0.01 なのでどちらでも結果は同じですが、解釈として申告します |
| 2 | **距離系の回帰率を 0.01 にした（全体は 0.20）** | 形質ごとに回帰率を変えられる構造にした | 正典 §6.4 の回帰は全形質共通の記述ですが、§5.2 が「距離適性は突然変異でスケールしないこと」と距離を特別扱いしています。共通の 0.20 のままだと §4-2 のとおり SD が 137 まで収縮するため、距離だけ弱い回帰にしました。**§6.4 と §5.2 の関係の明文化をお願いしたい箇所です（§7 Q1）** |
| 3 | **距離系の clamp/sd 比を 3.33 にした（能力5種は 1.67）** | 上記 §4-3 | 同じ 1.67 だと平衡SDが創始水準を下回るため |
| 4 | **大突然変異SDの距離系への適用** | 絶対値指定の形質では通常変異との比（180/90 = 2倍）を保つ | 正典に絶対値指定時の記述がないため。テストで固定しています |

---

## 7. 未解決の質問

### Q1 §6.4 の平均回帰と §5.2 の距離適性の関係を正典に明記してほしい

上記 §6-2 の件です。現在の正典では、§6.4 が「アレルに回帰をかける」と全形質共通で書き、§5.2 が「距離適性は突然変異でスケールしないこと」と距離のみを特別扱いしています。**距離に回帰をかけるべきか**は、どちらの節からも読み取れません。

P0-fix では「距離系のみ回帰率 0.01（実測で平衡する値）」としました。これを正典 §5.2 か §6.4 に一文追記していただけると、次のセッションが迷いません。

### Q2 大物覚醒を距離系形質にも適用し続けるか（P1 以降の論点）

指示書 §2 の指示どおり機構は一切触っていませんが、実測で「大物覚醒が距離SDを 361→435 に押し上げている」ことが分かりました（§4-4）。能力5種では回帰0.20 が相殺しますが、距離系は回帰0.01 なので相殺が効きません。

いま実害はありません（SDは平衡しています）。ただし **P1 でレースエンジンが入り、`distance_range` の大きい万能型が有利になると、選抜圧 × 単調ラチェットで万能型に収束する**恐れがあります。P1 の V-4〜V-6 較正時に併せて確認すべき論点として記録しておきます。

### Q3 検証の既定集団規模を実運用規模に寄せるべきか

本シミュレータの既定は繁殖牝馬200頭・種牡馬60頭（Ne≈45）ですが、正典 §10 の NPC は 2,500頭規模です。§4-4 のとおり、この差は距離SDの振れ幅を 0.74 → 0.05 に変えるほど効きます。

いま既定を変えていないのは、指示書 §3.4（P0 第1号）が「繁殖牝馬プール既定200頭」と定めており、勝手に変えると P0 との比較可能性が失われるためです。ただし **P1.5 の NPC プリシード（50世代・2,500頭）以降は、検証の既定規模も実運用に合わせるべき**だと考えます。判断をお願いします。
