**P4 デフォルメ馬の走行 — レビュー裁定（2026-09-06／Codex）**

対象: `REVIEW_REQUEST_P4_DEFORMED_RUN_20260906.md`。HEAD は `f9c0289`、対象実装・素材には未コミットのものがある。

**裁定: 現状は受け入れ不可。実装着手の承認は出さない。再納品の前に、検査と制作契約を整理する必要がある。**

品質を下げることは解決に含めない。「複製を除けば完成」「残りは前傾と重複コマだけ」という開発側の整理は、今回の独立検証では支持できない。素材の複製に加え、実素材に対するリグの成立性、焼き出しの設定、採点器に別々の問題がある。

本レビューで作成したのは、この裁定書と `tmp/review-20260906/` の再現画像・診断スクリプト・測定記録だけ。アプリ、リグ、焼き出しツール、納品素材は修正していない。外注への連絡、発注、コミット、デプロイも行っていない。

**1. 独立検証で確定した事実**

| 対象 | 脚の広がり | 胴〜鼻の幅変化 | 「前傾」 | 「同じ姿勢」 | 上部の変化 |
|---|---:|---:|---:|---:|---:|
| 既存合格素材・依頼書どおり再測定 | 53.3% | 9.3% | 13.7% | 0組 | 14.1% |
| §7のコマンド・出力先だけレビュー用に変更 | 49.2% | 11.6% | 2.4% | 2組 | 10.2% |
| 同条件に `--soft-body` を追加 | 50.0% | 9.8% | 3.3% | 0組 | 11.0% |
| 既存 `tmp/manus16-sheet.png` を再測定 | 22.9% | 12.7% | 6.3% | 1組 | 11.9% |
| `tmp/gallop16/frames` からシートを独立に再作成 | 22.9% | 12.7% | 6.2% | 1組 | 10.6% |

これは既存採点器が出した値の再現であり、項目名に書かれた身体動作を正しく測れたという意味ではない。後述のとおり複数項目が合否判定に使えない。

- **56.3%は、依頼書の再現コマンドでは再現しなかった。** 49.2%または50.0%だった。過去の別設定・加工済みパーツの値である可能性は残るが、対応する入力と引数が固定されていないため確定できない。
- Manusの既存シートと手元のフレームからの再作成は一致しない。どちらを最終納品と呼ぶかまで、この依頼書からは特定できない。両者を別入力として記録した。
- 指定されたリグのユニットテストは **27/27通過** を独立確認した。
- 原画のSHA-256は `e8c41a41e7092299fea6502d0c623df97421282ffabb8033c9d2ed4fe32c7617`。リグ仕様書に書かれた原画と一致した。原画のデザイン承認は継承するが、原画ファイル自体を透過済み完成素材とは扱わない。

**重大: §7の既定コマンドは、却下済みの剛体化を実行する。**

[`tools/bake-deformed-frames.mjs:74`](tools/bake-deformed-frames.mjs#L74) は `RIGID_BODY = !process.argv.includes('--soft-body')`。依頼書どおりでは首・頭を独立させない経路になる。この出力の点数を、首を動かす案の受け入れ根拠に使ってはいけない。今回の既定経路の実行は不一致を確認する対照実験であり、剛体案の承認ではない。

**重大: 27テスト通過は、今回の素材と設定の機構保証になっていない。**

テストは主に `DEFORMED_HORSE_V0` の寸法と既定歩法を使用する。一方、焼き出しは納品ピボットから脚長を取り、`stand=0.90 / stride=5.6 / overreach=0.35 / speed=16` を使っている。

その脚寸法と設定を独立に再構成し、1周期4,000点で既存の `deformedPoseAt` を調べた結果:

| 検査 | 結果 |
|---|---|
| 既存 `gaitFitsLegs(contract, gait, 0.97, 0.90)` | **ok=false**。最悪脚 `hindFar`、必要1.15128m／許容1.04986m |
| `contact=true` の蹄のY座標 | 地面0に対して最大 **+0.28010m** |
| 出力された膝〜蹄の距離と、契約上の下腿骨長との差 | 最大 **0.10291m** |
| 付け根〜蹄の距離／上下の骨長合計 | 最大 **1.0950**。届かない位置を要求している |
| 実際の16サンプルでも起こるか | 12コマ目は `hindFar` が接地扱いでY=+0.23664m。15コマ目の最大伸長比は1.09215 |

ここでのmは焼き出しが設定する400px/mの模型単位。画像を見て実馬の寸法を推定した値ではない。首・騎手の契約値は脚の計算へ入らないため、脚の再構成でこの検査を再現できる。

原因はコード上も追える。[`deformed-horse-pose.ts:265`](packages/render/src/deformed-horse-pose.ts#L265) は接地中にも `liftedToStayInReach` を適用し、その後の接地フラグは歩法の位相だけで決める。さらに逆運動学は解く距離をクランプするが、返す蹄座標は元の到達不能な目標のまま。焼き出しは下腿をその方向へ回転し、蹄を別位置へ置くので、切り分けを直しても接続が成立する保証がない。

**「脚の広がりが増えた」は観測できるが、「脚の問題が解決した」はまだ認めない。**

**素材の複製は実在する。**

24枚を読み、同じ原画座標で不透明部分を照合した。`alpha > 64` で面積合計3,320,021px／和集合777,303px = **4.27121**。比率4.271は再現した。`alpha > 128` なら4.29129になるため、面積を規約にするならアルファ閾値も明記が必要。

以下は `alpha > 128` での交差画素数。交差内のRGBは全画素で完全一致した。

| パーツと参照領域 | 交差画素 | 参照パーツ面積に対する比 |
|---|---:|---:|
| neck ∩ head | 106,715 | 64.95% |
| neck ∩ jockeyHead | 63,517 | 56.95% |
| torso ∩ jockeyHead | 63,907 | 57.30% |
| mane ∩ jockeyHead | 73,593 | 65.98% |
| jockeyArmNear ∩ jockeyHead | 73,038 | 65.49% |

交差面積だけでは人体・馬体の部位を識別できないため、これだけを新しい「複製判定器」とはしない。neck／torso原画像を開いて顔や装具が含まれることを確認し、独立焼き出しの16コマでも二重の顔、脚周辺の板状の破片、接続の破綻を確認した。画像は [首を動かす経路](tmp/review-20260906/soft-contact.png) と [剛体経路](tmp/review-20260906/default-contact.png) に保存した。どちらも左上から行順に1〜16コマ。

**2. Q1 — 制作の進め方**

**(b)を修正して主案にする。(a)の無期限・並行トリミングは採用しない。(c)は却下を維持する。**

ただし、次の外注を「面積上限1.60と複製禁止を追加して同じパーツを再送してもらう」だけにしてはいけない。必要なのは、部位ごとの所有領域を決め、隠れていた接続面を必要に応じて描き足し、指定の動作範囲で破綻しない素材へ作り直すこと。欠けている画素は削除操作では作れない。

静止時に見えている範囲は、手前の部位に隠された本来の領域を教えてくれない。脚を先に削って所有関係を取り直しても、この情報不足は残る。描画順を変えると所有者が変わる推定を、解剖上の部位の正解にはできない。

次の作業を承認する場合の順序としては、以下を推奨する。**これは着手指示ではない。**

1. 承認原画と、動作の承認資料・位相・接地順を固定する。リグの到達可能性を含め、実際に動かす範囲を契約にする。
2. 全24枚を再発注する前に、問題のある「胴・首・頭・騎手」と代表的な脚関節を、同じ完成品質で直した小さな実証サンプルを要求する。原姿勢、極端姿勢、中間、連続再生まで検品する。
3. 同じ制作方法で複製と隙間を解消できると確認してから、全体と16コマ・5層の完成出力へ広げる。

少量のサンプルで工程を確認することは品質の引下げではない。最終品の顔・首の動き・4頭以上・着色・既存背景の条件は維持する。(b)の成功回数を「1〜2往復」と保証できる根拠はない。

(a)を残すなら、正しい所有マスクが確定した領域の機械的な処理、または原因を確かめる限定実験まで。同じ納品に対して外注と開発が別々の部位定義を作り続ける運用は避ける。

**3. Q2 — 受け入れ基準案は、そのままでは不採用**

原因は「上限が無かったこと」だけではない。測りたい欠陥と指標が対応していない。

| 提案項目 | 穴 | 裁定・置き換え方 |
|---|---|---|
| ① RGB平均差2/255以内 | 大量の背景で平均が薄まる。アルファ欠損、顔の局所崩れ、静止時に隠された複製を落とせない | 原画の背景を除いた固定マスクと同一座標で比較。アルファ・輪郭・顔／ヘルメット等の局所差・着色前後を別に検査。原画ファイルの一致と、加工後画像の見た目の一致を分ける |
| ② 面積比1.15〜1.60 | 総量が小さくても顔だけ複製できる。必要な関節裏まで削れば合格できる。1.60の妥当性が実証されていない | 面積比は異常の手掛かりに留める。合否は部位の所有、許可された関節裏の領域、実際の動作中の破綻で決める |
| ③ 子面積の8%以上重なる | 重なりが関節以外にあっても通る。小さな蹄と大きな胴で意味が異なる | 重なりは指定された接続部の局所領域で検査。必要量は実際の可動域で割れないことから決める |
| ④ 他パーツ面積の2%以上へ入らない | 親子例外がなく、③の8%以上と**矛盾**する。2%未満の二重の目も通る。静止領域への侵入と「同じ顔を描いた複製」は別 | 部位本体・関節裏・装具の交差などを区別した所有／許可マスクを用意。許可領域外に別の顔・蹄等を描かない。面積だけで免除しない |
| ⑤ ピボットが不透明部内 | ピボットが画像内にあっても関節位置として正しいとは限らない。巨大な複製パーツでも通る | 骨の端点、親子座標、回転中心、原姿勢への復元を検査。論理上の親子関係と、接続部の肉の位置も区別する |
| ⑥ 全関節±40°・透け0 | 関節ごとの必要範囲が違う。両端だけでは途中の穴や複数関節の組合せを見落とす。脚間や手綱内側の正常な背景まで穴と判定し得る | 実動作の全サンプル・極値・中間・周期境界を同じ描画経路で確認。正常な隙間と、接続部で不正に開いた隙間を区別する |

**受け入れは次の四段階に分ける。どの段階も他の段階の代用にはしない。**

| 段階 | 必須の確認 |
|---|---|
| 入力と契約 | 原画・パーツ・動作・ツール・引数・出力のハッシュ、寸法、共通座標、前進距離、周期と各コマの位相、地面線、部位の所有、5層の意味、描画順を固定 |
| 機構 | **実素材の寸法と実際の設定**で到達可能性、骨長、関節接続、接地フラグと蹄XY、首・頭の親子変換、周期境界を検査。数値模型と画像上の蹄底を両方確認 |
| 素材 | 原姿勢の外観維持、動作中の二重の顔／余分な脚／穴／切断面なし。輪郭の鮮明さ、真の透過、着色領域と固定色の分離、5層再合成を確認 |
| 完成画面 | 既存背景・HUD・カット割りで4頭以上。顔、毛色、勝負服、帽子が完成し、通常速度と低速、対象画面サイズで馬らしい走りとして承認資料・オーナー基準を満たす |

画素比較の許容差は、出力寸法、アンチエイリアス、縮小、実機倍率を固定してから、承認資料と既知の不良例で校正する。現段階で新たな1.60・2%・±40°を「品質を保証する正式閾値」として採択しない。これは品質条件の撤廃ではなく、誤判定する検査を外す判断。

**4. Q3 — 騎手の前傾は品質条件として維持し、現行の6%判定は使用停止**

現在の値は、上から30%の不透明画素の重心と、馬・尾・脚を含む全体の重心との差を、全体幅で割ったもの。[`measure-gallop.mjs:136`](tools/measure-gallop.mjs#L136) に鞍、腰、肩、上体角の測定はない。

独立対照では、騎手を含む上部（0〜762行）を**バイト単位で同一に固定**したまま、脚パーツ側だけを左右へ160pxずらした。上部のハッシュは3条件とも同一で、切断帯と上下端も同一だったが、前傾スコアは次のように変化した。

| 脚側のX移動 | 現行式の「前傾」 |
|---|---:|
| 0px | 6.9007%（合格） |
| -160px | 10.3052%（合格） |
| +160px | 4.8349%（不合格） |

これは歩容を提案する実験ではなく、騎手を変えなくても合否が反転することを示す反例。**「6%未満だから騎手をもっと倒す」は正当化できない。** 焼き出し値の低下の全原因をこの実験だけで特定したわけでもない。

代わりに、承認された原画・動作の上体姿勢を基準に、腰／肩／頭と鞍の位置を、部位マスクまたは目視確認したランドマークで追う。馬体に対する相対姿勢と、身体の上下動への追従を評価する。原画と同じ局面での比較、騎手を起こした既知の不良例、脚・尾だけを変えた対照で検査の妥当性を確認する。

なお焼き出しは [`bake-deformed-frames.mjs:192`](tools/bake-deformed-frames.mjs#L192) で全jockey部位の追加角を0にしている。現行スコアに合わせて角度をいじる前に、承認された動作に対して何を固定し何を動かすか、部位ごとの仕様を確認すべき。

**5. Q4 — 現行の「同じ姿勢」は8コマにも16コマにも合否用途では不適切**

[`measure-gallop.mjs:225`](tools/measure-gallop.mjs#L225) が比較するのは、隣接コマの「外接幅」と「最下端」の2つだけ。画像や関節の姿勢を比較していない。

既定焼き出しで指摘された組を、採点器自身の前処理後のRGBAで照合した結果:

| 指摘された組（1始まり） | 異なるRGBA画素数 | 画像ハッシュ |
|---|---:|---|
| 10→11 | 14,953px | 不一致 |
| 13→14 | 14,287px | 不一致 |

全16コマのRGBAハッシュもすべて異なる。従って今回の2組は「同じ画像」の証拠にならず、姿勢が同じとも言えない。逆に、絵がほぼ固定でも尾先や最下端を1px動かせば通る。

**16コマという形式は維持してよい。閾値を緩める対応はしない。** 意図しない全く同じ画像の再送はハッシュ／復号後RGBAで検出し、単なる重複ファイル検査と動作品質の検査を分ける。動作は位相・四肢の接地・関節・首と騎手の相対運動を追い、最終→先頭を含む連続再生で停止や引っ掛かりを確認する。

滑らかな動作の隣接コマは似ていてよく、全コマに強い差を強制しない。実再生速度とfpsで、各コマが何フレーム表示されるか／飛ばされるかも評価する。今回の5.6m/完歩・16m/sでは、30fpsで約10.5表示フレーム/完歩なので、「16枚ある」だけでは16枚すべての表示は保証されない。

**6. 追加で見つかった受け入れの穴**

- **採点の前処理が、検査対象を加工している。** `loadFrames` は連結成分2,000px以上をフレームとして抽出し、小片を捨て、明るい無彩色領域の重心で各コマを合わせ、220×140へ縮小する（[`dress.mjs:139`](tools/lib/dress.mjs#L139)、156〜187行）。白い領域は常にゼッケンとは限らない。固定座標の16コマを検査する場合、コマ数・小片・上下動・地面線をこの変換に委ねてはいけない。
- **「接地の本数100%」は本数を測っていない。** 地面付近の行にある画素数の最大最小差。浮遊コマで0になれば、他に1回でも非ゼロがあれば100%になる。四肢の識別、接地順、接地時間、蹄の滑りを保証しない。採点器は模型の地面線1405も使わず、前処理後の最下端最大値から地面を推定している。
- **②-bには実際には7%の合格線がある。** 依頼書では「—」だが、コードの `TORSO_MIN=0.07` はFAILを出す。さらにこの値は明るい胴〜鼻帯の幅であり、背骨のしなりの直接測定ではない。
- **⑥は剛体化を落とせない。** 首・頭・騎手を剛体にした既定出力でも10.2%で通る。全体の回転や移動・領域の切り方で差が出るため、必要な部位の相対運動の代わりにしてはいけない。
- **⑦の緑残り判定は独立でない。** 前処理で緑優勢画素を削り、残りの緑も `max(r,b)` へ抑えてから測る（[`dress.mjs:99`](tools/lib/dress.mjs#L99)）。しかも「縁」だけではなく全不透明画素を数える。納品状態の検品と、補正後の検品を分けないと欠陥を隠す。正規の緑の勝負服にもそのまま適用できない。
- **首の親子変換にも確認が必要。** neck本体の位置は `onTorso(neckM, 0)` 相当なのに、head／mane側が使う首根元は `onTorso(neckM, neckExtra)`（[`bake-deformed-frames.mjs:220`](tools/bake-deformed-frames.mjs#L220)）。追加角があると、首根元まで胴中心の周りに移動させてしまう。素材を直すだけで首と頭の接続が保証される実装ではない。
- **模型の蹄点と画像上の蹄底は別。** 画像は下腿角に合わせて蹄も回す。蹄ピボットが地面上でも、実際の足裏が地面上とは限らない。16枚への離散化と実画面のpx/mでも接地が保たれるか、最終出力で検査が必要。
- **マスク境界と色役割が未統一。** `horse_jockey_3A 2Dリグ仕様.md` は馬の顔・口元をcoatへ含める一方、`horse_jockey_3A 引き継ぎパッケージ.md` はcoatから除く。後者のtack列にも馬の顔・口元が明示されない。次の注文で再び解釈させず、1枚の役割マスクと文書を正とするべき。
- **実機と測定対象の同一性がない。** 焼き出し先 `tmp/baked` は、Webの固定参照先 `/rig-lab-assets/sprites` と別。比較ページはさらに `/rig-lab-assets/compare` を見る。§7に更新・加工・ハッシュ照合がないため、焼いたものとブラウザで見たものが同じとは言えない。`SPAN_M=3.32` に旧512px正方形由来の説明も残るため、長方形素材の物差しを実際に再検証すべき。

本レビューはローカルでの数値再実行、原画・パーツ・再現16コマの目視、コード照合まで。ブラウザの4頭連続再生・着色・カット切替・実機fpsは今回検証していない。従って最終画面の品質合格も出していない。上記の再現済み問題だけで差し戻し判断は成立する。

**7. 見落としている選択肢**

**「原画を切って回す」こと自体を完成方法として固定する必要はない。**

手元の `horse_jockey_3A 引き継ぎパッケージ.md` は、パーツv2を「小〜中角度のリグ参考」とし、大角度のギャロップには隠れ面の補完、正式なメッシュリグまたはフレームごとの手修正が必要と明記している。この説明は「合格パーツがあるので足りないのは数値だけ」という焼き出しツールの前提と食い違う。

また `tmp/handoff/horse_jockey_3A_HANDOFF_20260906/02_APPROVED_MOTION/` に、資料上「走り方承認済み」とされた `approved_stride16.gif`、`source_motion.mp4`、16枚の動作参照が実在する。承認履歴そのものは今回独立確認していないが、確認できれば新しい「馬らしさ」を面積指標から作るより具体的な基準になる。

選択肢は、**承認動作を基準に隠れ面を補完したリグで変形し、破綻する関節や輪郭だけを手修正して、最後に16コマ・5層の2Dスプライトへ焼く**こと。関節ごとの差し替え絵や補正ポーズもこの範囲に入る。ゲーム画面は既存の2D背景・HUD・カット割りを引き続き使える。

この案は**品質を下げない方針**であり、首を止める案ではない。ただし制作費・担当者の技能・修正時間が必要で、今ある24枚の自動切り抜きより安いとは約束できない。参照GIFをそのまま背景除去して最終素材にする案は、資料自身が滲みを指摘しているため採用しない。

**8. 再現と証跡**

再現はローカルにあるtsx／Vitestを直接使い、ネットワーク経由で新しい版を取得していない。

```powershell
node node_modules/tsx/dist/cli.mjs tools/bake-deformed-frames.mjs --out tmp/review-20260906/default --frames 16
node node_modules/tsx/dist/cli.mjs tools/bake-deformed-frames.mjs --out tmp/review-20260906/soft --frames 16 --soft-body
node node_modules/tsx/dist/cli.mjs tmp/sheet.mjs tmp/review-20260906/default tmp/review-20260906/default-sheet.png 16
node node_modules/tsx/dist/cli.mjs tmp/sheet.mjs tmp/review-20260906/soft tmp/review-20260906/soft-sheet.png 16
node node_modules/tsx/dist/cli.mjs tmp/sheet.mjs tmp/gallop16/frames tmp/review-20260906/manus-rebuilt-sheet.png 16
node node_modules/tsx/dist/cli.mjs tmp/review-20260906/audit.mjs
node node_modules/vitest/vitest.mjs run packages/render/test/deformed-horse.test.ts
```

- [独立診断スクリプト](tmp/review-20260906/audit.mjs) — 部位面積／複製照合、画像重複の反例、騎手固定対照、実素材の骨長・接地・到達可能性。診断用で、ゲーム実装には接続していない。
- [測定値と素材ハッシュ](tmp/review-20260906/audit.json)、[検査対象コードのハッシュ](tmp/review-20260906/source-hashes.json)。
- 採点器の生出力: [既存合格素材](tmp/review-20260906/measure-reference.txt)、[既定経路](tmp/review-20260906/measure-default.txt)、[首を動かす経路](tmp/review-20260906/measure-soft.txt)、[既存Manusシート](tmp/review-20260906/measure-manus-existing-sheet.txt)、[Manusフレームから再作成](tmp/review-20260906/measure-manus-rebuilt.txt)。FAILの終了コード1は診断結果として保存。

今回の判断を受けて実装を開始するかはオーナーの次の指示による。本書はレビュー裁定であり、実装・外注への着手許可を兼ねない。
