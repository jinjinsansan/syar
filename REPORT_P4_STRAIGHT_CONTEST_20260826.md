# 報告 — 最後の直線の「せめぎ合い」が成立するまで（経緯と結論）

日付: 2026-08-26 / ブランチ `p4/race-30sec-cuts` / 着手時 HEAD `8c7203d`
関連: `DEV_INSTRUCTIONS_P4_RACE_CLIMAX_REBUILD_20260826.md` / `REPORT_P4_RACE_CLIMAX_REBUILD_20260826.md`

> ★**オーナー評価: 「かなりよくなりました」（2026-08-26）。**
> ★**最後の直線のせめぎ合いが、初めて画面に映るようになりました。**

---

## 0. 結論だけ先に

★**確認用 URL**: `http://localhost:3210/race?seed=253&cinematography=v6`

| | |
|---|---|
| 何を作ったか | 台本 **v6**（直線を 4 カットに割る）＋ `contest-focus.ts`（競り合いへカメラを向ける） |
| 馬の大きさ | 寄りのカットで**画面高の 46%**（v5 は 15%） |
| 画面の頭数 | **最大 5 頭 / 平均 4.8 頭** |
| 競り合いが映る時間 | 主役 2 頭以上 **100%（15.4 秒すべて）** / 3 頭以上 13.9 秒 / **4 頭以上 11.9 秒** |
| 馬を動かしたか | ★**動かしていません。** 着順・タイム・着差・払戻に一切触れていません |
| 検証 | `npm test` **1181 件 PASS**（119 ファイル）/ `npm run typecheck` クリーン |

★**既定は v5 のままです。** v6 は `?cinematography=v6` を付けたときだけです。

---

## 1. ★この案件が長引いた本当の理由

★**問題は「カメラ」でも「描画」でもなく、「どのレースを見ていたか」でした。**

数日にわたり、確認はすべて **seed 42** で行われていました。ところが実測すると:

```
seed 42  上位 5 頭は最後の直線を通してずっと 19〜21m に伸びている
         → ★この seed には「5 頭のせめぎ合い」が最初から存在しない
```

★**在らないものは、どんなカメラでも映せません。** それを「カメラの問題」「描画の問題」として
数日追いかけたのが、この案件の空転の正体です。

⚠️ ★**教訓（次のセッションへ）**: 見た目の要求が満たせないとき、
★**まず「その要求を満たす場面が、そのデータに存在するか」を測ること。**
存在しないなら、直すべきは表現ではなく**入力（見るレース）**です。

---

## 2. 経緯（何を試し、何が間違っていたか）

### 2-1. 第 1 案: 馬を動かす（`climax-choreography.ts`）— ★オーナー不合格

指示書 §1 で「表示専用の演出」が許可されたのを受け、上位 5 頭の**表示位置**を
前後に動かして密集させる純粋関数を作りました。残り 60m で完全に 0 へ戻すので、
着順・着差は変わりません。テストは 1160 件 PASS しました。

★**しかし実画面で破綻しました。**

| オーナー指摘 | 実測 |
|---|---|
| 「23 秒あたりから急にスピードが速くなる」 | **本来の 1.133 倍（+13.3%）**・残り313m |
| 「急に後ろに下がっていく」 | **本来の 0.852 倍（−14.8%）**・残り91m |
| 「どんどん巨大化していく」 | 群を画面幅 68% に保つため画角が絞られ続ける |

★**原因は式の性質そのものです。** オフセット X m を距離 L で 0 へ戻すとき、
見かけの速度のずれは **X × 1.5 / L**。12m を 160m で戻せば **11%** が原理的に出ます。
★**「馬を動かす」方針を採る限り、この歪みは消せません。**

⚠️ ★**さらに悪かったのは、監査道具が「後退 0 回」と報告していたこと**です。
道具は**地面に対する**位置しか見ておらず、オーナーが見ている**画面の中での**後退を
測っていませんでした（正典 R-22）。★**道具が見ていない症状を「無い」と報告していました。**

### 2-2. 第 2 案: 直線をカットで割る（台本 v6）— ★採用

「1 カットで『大きい』と『4〜5 頭』を両立させようとしたこと」が誤りでした。

```
馬体が画面高の 40% ＝ 馬 2.4m が 288px ＝ 画面に入る走路は前後 10〜11m だけ
```

★**割れば、馬を動かさずに両方見せられます。** 各カットが自分の得意な仕事だけをします。

| # | 区間 | ショット | 役 |
|---|---|---|---|
| ① | 残り634〜480m | `straight-contest` | せめぎ合い（46%） |
| ② | 残り480〜336m | `homestretch-front` | 差し・追い込み（正面の奥行き・12 頭） |
| ③ | 残り336〜112m | `straight-contest` | 逃げ粘りと決着争い（46%） |
| ④ | 残り112〜0m | `finish-line` | 決勝線・審判塔 |

★**4 角までは v5 と 1 行も変えていません。** v6 の違いは直線の 4 行だけです。

### 2-3. ★核心の発見: 追い抜きは実在していた

演出なしで測ると:

```
seed  42  直線での追い抜き 3 回   10-4 @残り219m / 10-5 @残り611m / 2-5 @残り18m
seed  14  9 回 / seed 332  4 回 / seed 474  4 回
```

★**エンジンは差しも追い込みもちゃんと起こしていました。**
映らなかったのは、★**カメラが先頭馬に固定されていたから**です。
seed 42 の残り241m では 4 番と 10 番が **0.9m 差で競っている**のに、
画面は先頭だけを追い、その競り合いは**画面の外**にありました。

→ `contest-focus.ts`（注視点を競り合っている場所へ寄せる純粋関数）を新設。
★**馬は 1mm も動きません。** だから見かけの速度のずれは**原理的に発生しません**。

### 2-4. ★見つけた自分の誤り（すべて「測る側」の誤り）

| # | 誤り | 実際 |
|---|---|---|
| 1 | 「注視点が 1 コマ 2.99m 跳んでいる」と読んだ | 時間圧縮区間では**馬自身が 1 コマ 2.7m 進む**。跳びではなかった（R-21） |
| 2 | 離れた 2 集団を平均し「誰もいない場所」を見ていた | `<=` という**不連続な判定**を入れたのが原因。連続な重みへ修正 |
| 3 | 「4 頭以上が画面に」と報告したが実際は 10 頭が重なっていた | 監査が**上位 5 頭しか数えず**、着外の馬が同じ画面に入ることを数えていなかった |
| 4 | seed 99 を推奨した | 点数式が「1-2 着差が小さいほど良い」＝ ★**着順が読めないレースを選んでいた** |

★**4 件すべて、製品ではなく監査の誤りです。** 正典 R-30 の実例がさらに 4 つ増えました。

---

## 3. 変更したファイル

### 新規（製品）

```
packages/render/src/contest-focus.ts        ★注視点を競り合いへ（表示専用・馬は動かさない）
packages/render/src/climax-choreography.ts  ★第 1 案。v6 では使いません（v5 の既定としては残置）
```

### 新規（テスト）

```
packages/render/test/contest-focus.test.ts  10 件
packages/render/test/script-v6.test.ts      12 件
```

### 新規（監査・すべて読取専用）

```
tools/audit-straight-spread.mjs   上位 5 頭が実際に何 m に伸びているか／その大きさで画面に入るか
tools/audit-real-overtakes.mjs    ★演出なしで直線に追い抜きが実在するか
tools/audit-contest-focus.mjs     競り合いが映る秒数／画面の頭数／注視点が馬から離れる量
tools/find-contest-seeds.mjs      ★せめぎ合いになる seed を総当たりで探す
```

### 変更

```
packages/render/src/broadcast-v2.ts        SCRIPT_V6 / ショット straight-contest / focusContest / maxVisible
packages/render/src/broadcast-v2-scene.ts  注視点の分岐 / 描画頭数の上限 / noContenderFrameShots
packages/render/src/index.ts               contest-focus を公開
apps/web/src/app/race/page.tsx             v6 の接続 / ?seed= を URL から / ゴール保持
packages/render/test/script-v5.test.ts     v6 は「不正値」ではなくなった
tools/lib/classification.mjs               新しい道具 4 本を READONLY へ登録
tools/lib/race-audit-build.mjs             noContenderFrameShots を実画面と同じに（R-30）
```

---

## 4. 確認用の seed（★開発中はこれを使うこと）

★**選定条件**: 直線は競るが、★**ゴールでは勝者がはっきり分かる**こと（1-2 着差 1.5〜3.0 馬身）。

| seed | 4 頭以上が画面 | 1-2 着差 | 追い込み | 備考 |
|---|---:|---:|---|---|
| ★**253** | 11.9 秒 | **2.08 馬身** | 9 番手→1 着 | ★**推奨**。追い抜き 8 回 |
| **90** | 13.2 秒 | 1.72 馬身 | 9 番手→1 着 | |
| **198** | 13.0 秒 | 2.57 馬身 | 9 番手→1 着 | |
| **2** | 9.9 秒 | **2.95 馬身** | 10 番手→1 着 | いちばん差が明快 |

⚠️ ★**使ってはいけない seed**

| seed | 理由 |
|---|---|
| **42** | ★上位 5 頭が 19〜21m に伸びる。**せめぎ合いが存在しない**。数日これで確認していた |
| **99** | 1-2 着差 **0.15 馬身**。★2 頭同時にゴールして**着順が読めない** |

---

## 5. 残っている課題（次のセッションへ）

1. ★**カットの尺を実時間で測っていません。** 区間の長さ（154m/144m/224m/112m）で切っており、
   実際に何秒になるかは未計測です。長短の調整は `SCRIPT_V6` の `until` だけで効きます。
2. ★**5 頭に絞ったので、6 頭目が画面の端で出入りする**可能性があります。未評価です。
3. ★**seed 42 のように「せめぎ合いが無いレース」をどう見せるか**は未解決です。
   本番では seed を選べません。★**在らない競り合いをどう扱うか**（そのまま見せる／
   別のカット構成にする／エンジン側の較正を見直す）は**オーナー判断**です。
4. ★**v6 はまだ既定ではありません。** `?cinematography=v6` の opt-in のままです。
5. ★`climax-choreography`（第 1 案）は **v5 の既定として残置**されています。
   ★v6 を既定にするなら、これをどうするか決める必要があります。

---

## 6. 検証

```
npm test        → 1181 件 PASS（119 ファイル）
npm run typecheck → クリーン
```

★**push はしていません**（CLAUDE.md・オーナー指示待ち）。
