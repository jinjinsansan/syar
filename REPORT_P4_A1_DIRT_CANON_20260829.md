# 報告 — 4 角の反転（A-1）／ダートの砂被り／正典の起票／A-2 の照会

作成: 2026-08-29 / ブランチ `p4/race-30sec-cuts`
起点: `9c8f9ed` → HEAD `f459d35`（5 コミット）
受けた指示: オーナー（4 件を並行で進める）
前の報告: `REPORT_P4_DIRT_20260829.md`
受けている裁定: `REVIEW_P4_GAMMA_V6_DIRT_VERDICT_20260828.md`

> ★`Date.now()` / `Math.random()` を使っていません。
> ★**着順・タイム・着差・払戻・位置に触れていません**（憲法 3/4）。触れたのはカメラと描く順だけです。
> ⚠️ ★`apps/web/package.json`（ポート 3210）はオーナーの既存作業。★コミットに混ぜていません。
> ★提出前に `git status` を見ました（裁定 §10-5）。

---

## 0. この便の要点

| # | 内容 | 種別 |
|---|---|---|
| 1 | ★**4 角の出口の反転を消した**（A-1・半分解決） | 実装 |
| 2 | ★**測っていない量を最適化していた**ことが原因でした | ★自己申告 |
| 3 | ★**後続が前の馬の砂を被る**（ダートの見せ場） | 実装 |
| 4 | ★**正典 D-081〜D-087 と R-31 を起票**（8 日分） | 手続き |
| 5 | ★**A-2 の (a)/(b)/(c) を照会**。★上限は 12px でなく **33.5px** だった | 照会 |
| 6 | ★**註釈に別字が 47 箇所**混ざっていた（`grep 芝` が 38 件取り逃す） | ★発見 |

---

## 1. ★4 角の反転（残件 A-1）— 出口は消えました

### 1-1. ★原因は「測っていない量を最適化していた」ことです

★2026-08-26 に固定カメラを **30m → 90m** にしました。理由は
★「★**カット内**の反転を消す」ことでした。

⚠️ ★**しかしオーナーの苦情は「★境目の反転」です**（「別のレースに見える」）。

★掃引表（`tools/audit-corner-camera-sweep.mjs`）が出していたのは
★**中央のコマの向き**と**カット内の反転回数**だけで、
★**境目で効く「入口の 1 コマ目」「出口の最終コマ」を一度も出していませんでした。**

→ ★列を足したところ、90m は**境目の反転が 2 箇所**でした。

```
  カメラ先   ★入口    ★出口    カット内   ★境目の反転   馬高比(始→終)
   90m(旧)   ←23.6   ← 4.0     なし      ★2 箇所      21% → 21%
   60m       ←17.9   → 6.2    ★1 回       1 箇所      21% → 21%
   40m       ←13.8   →18.2    ★1 回       1 箇所      21% → 23%
  ★30m(新)  ←11.5   →**33.9** ★1 回     ★1 箇所      21% → **28%**
```

★**D-082 と同じ形です**（指標が、要望されたものを見ていなかった）。

### 1-2. 実測（★製品経路・4 seed）

| | 直す前 | ★直したあと |
|---|---|---|
| 出口 `fourth-corner-front → side-drive` の反転 | 4/4 本 | ★**0/4 本** |
| 　　出口の跳び（共通馬の中央値） | 901px | ★**595〜791px** |
| 　　出口の画角比 | 2.50× | ★**1.02×**（ほぼ連続） |
| 入口 `side-drive → fourth-corner-front` の反転 | 4/4 本 | ★4/4 本（**残る**） |
| 　　入口の反転量 | ←19px/m | ★**←11px/m** |
| 馬の大きさ（カット内） | 21% → 21% | ★**21% → 28%** |

★**馬が実際に大きくなります。** 90m では自動ズームが 21% で一定に保っており、
★「迫ってくる」手掛かりを打ち消していました（これは 8/28 に一度 `approach` で作った効果が、
★カメラが遠いぶん**素の幾何としては出ていなかった**ということです）。

### 1-3. ★入口は消せません。測って確かめました

★掃引 **5〜160m × 横 8 通り**で、★**入口は全通りで ←** でした。
★カット開始時点では馬がまだコーナーの中にいるためで、★**境目の反転 1 箇所が下限**です。

⚠️ ★30m より寄せると素材の角度ずれが急に悪化します:

```
  30m 12.0°  →  25m 16.4°  →  20m 23.2°  →  5m 59.7°
```

★斜め前素材は 168° で描かれているので、ここが開くほど**絵が合わなくなります**。
→ ★**30m が最適点**です（90m の 10.2° とほぼ同等を保ったまま、出口の反転が消える）。

### 1-4. ★代わりに起きたこと（隠しません）

★カット内の**左右反転が 0 回 → 1 回**になりました。

★ただし起きるのは、★**視線と進行方向のなす角が 179.6°＝ほぼ真正面**の瞬間です:

```
   表示秒    向きの角度   180°まで   画面の向き
   17.80s    178.1°       1.9°      ←2.0px/m
  ★17.90s   ★179.6°     ★0.4°     ★→0.4px/m   ← ここで軸を跨ぐ
   18.00s    177.2°       2.8°      →3.6px/m
```

★そこでは絵が**左右対称に近く**、★**最も目立たない位置**です。

⚠️ ★**2026-08-25 にオーナーが却下したのは、素材の反転をカット開始時に固定する機構**
　（`aa52b70`「カット後半で本来と逆を向く」）であって、
　★**向きが軸で入れ替わること自体ではありません。** ★前便はこの 2 つを同じものとして扱っていました。

★映像編集で禁じられているのは**境目**で線を越えることで、
★**カットの中で軸を跨ぐのは、線を越える正当なやり方**です。

### 1-5. 守り

`apps/cli/test/race-video-invariants.test.ts` に第 3 の不変条件を追加:

> ★**4 角正面から出る境目で走行方向が反転しない**

★**旧設定（90m）に戻すと落ちること**を確認しました（R-21）。
★入口は**入れていません** — 入れるとこの検査は最初から赤になり、★守るべき 0 まで見えなくなります。

★**残る判断は [EYES]**: 入口の ←11px/m を実画面でどう感じるか。★閃光は入口側に入っています。

---

## 2. ★ダートの砂被り（報告 §10-1「ダートの見せ場」）

### 2-1. 何が起きていたか

★砂煙は「その馬を描くついでに、★**その馬の下に**」描かれていました。
★馬は**深さの降順**（遠→近）で描かれるので、
★**手前にいる後続馬は、奥にいる前の馬の砂の“上”に**描かれます。

### 2-2. どう直したか — ★砂を 2 つに分けた

| | どこへ | なぜ |
|---|---|---|
| ①**蹄元の砂**（自分の絵に重なる範囲） | ★従来どおり**自分の下** | ★ここまで上に出すと**全馬が自分の砂で灰色に曇ります**（前の馬の砂ではない） |
| ②**後方に漂う砂**（自分の絵より後ろ） | ★**全馬を描いたあと** | ★自分より後ろの空間にしか無いので、★**前を行く馬には掛かりません** |

★分かれ目は**手置きの m ではなく、絵の幅を px から m に戻した半分**です（R-31 の family）。

### 2-3. 実測（`tools/_dustcover.mjs`・seed 42・8 場面）

★他馬の砂の中にいる頭数（12 頭立て）: ★**平均 5.5 → 7.5 頭**

⚠️ ★**「直す前は 0 だった」とは書きません。**
　★旧実装でも、★前の馬がたまたま手前にいれば**偶然**被っていました（それが 5.5 の中身）。
　★**狙って出していたものではない**、というのが違いです。

### 2-4. 守り（`packages/render/test/dirt-dust-order.test.ts`・5 件）

| 検査 | 直す前なら |
|---|---|
| ① 全馬の絵のあとに砂がある（＝誰かが被れる） | ★**落ちる**（確認済み） |
| ② 蹄元の砂は自分の絵より先（＝自分の砂で曇らない） | ★**落ちる**（逆側に倒すと・確認済み） |
| ③ ★**芝では 1 粒も描かない** | — |

★**両側を押さえました**（R-2）。★道具は幾何を組み直さず、
★`drawPerspectiveHorses` 本体に描かせて記録用の ctx で拾っています（R-30）。

---

## 3. ★正典の起票（残件 C-1）

★裁定 §8 の 7 件に一対一で対応させ、★**D-081〜D-087 と R-31** を起票しました。

| | 内容 |
|---|---|
| D-081 | `LANE_REVEAL_FULL_RUN = 0.18` 確定／★後方・俯瞰は 5 戦 5 敗 |
| D-082 | 接戦は写像の形（γ）で作る。`RACE_RANDOM_K` を動かさない／★指標が要望を減点していた実例 |
| D-083 | 4 角の反転は「カットを良くする」でしか直さない。★症状をカットの削除・短縮で処理しない |
| D-084 | 閃光の認可（条件 3 つ）／`FLASH_INTO` はショット id 単位という制約 |
| D-085 | γ=1.6 確定・v6 既定化／★既定を変えるときは戻せる口を同時に置き、検査で固定する |
| D-086 | 跳び px に閾値を引かない。代わりに「良い状態を守る検査」を置く |
| D-087 | ダートは地面の描画だけ。内回り案は却下 |
| **R-31** | ★測定器の既定は「画面と同じ側」に倒す |

### 3-1. ⚠️ ★併せて分かったこと — 「決定ログ」節は 8/4 で止まっていました

★生きている記録は **§0.4 の決定表**のほうです。★決定ログ節は **25 日分**ありません。
→ ★末尾に注意書きを入れ、★**「ログがあること」を「経緯が追えること」と読ませない**ようにしました。
★二重帳簿をどうするか（埋めるか、廃止して §0.4 に一本化するか）は★**未決**として残します。

### 3-2. ★起票していないもの 1 件

★「注視点の上限を画面の実寸から決める」（`contest-focus.ts`）は裁定 §0 が [CODE] で確認済みですが、
★**§8 の 7 件に入っていません**。
★**番号の確定は正典側の作業**と裁定 §8 が明記しているので、★D-088 を振らずに回します。

---

## 4. ★A-2 の照会（`QUESTIONS_P4_SEAM_SLIP_20260829.md`）

★台帳に「★未提出・次便で出す」と書いたまま 1 便またいでいたものです。★出しました。

| # | 内容 |
|---|---|
| 1 | ★一般則: 跳び ＝ **カメラの遅れ × 走線比の差**。★既存実測 12.16px と一致（式 12.2px） |
| 2 | ★**上限は 12px ではなく 33.5px ＝ 画面幅の 2.6%**（2.7 倍）。★1 箇所しか測っていませんでした |
| 3 | ★**寄っても引いても上限は同じ**（px/m が式から消える） |
| 4 | ★候補 **(b′)** を追加。★台帳の (b) と違い**遅れが出ません**（検算で跳び 0） |
| 5 | ★(b′) の未検証 3 点を明記して出しました（R-13） |

★**選択肢に名前を付けたら、中身が 2 つ混ざっていました。** 分けたら片方は台帳が書いた欠点を持ちませんでした。

---

## 5. ★註釈に別字が 47 箇所（`QUESTIONS_P4_SOURCE_KANJI_20260829.md`）

★ダートの実装を読んでいて気づきました。★**どれも「読める字」**です。

| 別字 | 正しい字 | 箇所 |
|---|---|---|
| U+82DD | **芝** | ★**38** |
| U+697A / U+780A / U+868A / U+9451 / U+8E74 | 楕 / 砂 / 蹴 / （書き直し）/ 蹄 | 9 |

★**実害**: ★`grep 芝` が**芝についての註記を 38 件取り逃します**。
★「芝は 1 画素も変えていません」という**保証そのもの**が、検索で見つからない字で書かれていました。

⚠️ ★**列挙は即座に漏れました。** 5 種を機械で洗い出した直後に、★6 種目（蹄→蹴）を**目で**見つけました。
　★正典 R-29「列挙は必ず漏れる」の **5 回目**です。
→ ★入れた検査は**目印であって防壁ではない**と明記しました。★恒久の形は照会に出しています。

★併せて 1 つ実演: ★**検出器が自分自身を検出して落ちました**。
★除外簿で逃がすと逃がした先が温床になるので、★**コードポイントから組み立てて**除外簿を不要にしました。

---

## 6. ★この便で自分がやらかしたこと

★**バッククォートがコマンド置換になり、正典に壊れた行を書き込みました。**

```
python -c "... `script` ... `DEFAULT_RACE_SCRIPT` ..."
              ↑ シェルがコマンドとして実行し、★語が消えた行が入った
```

★正典 **R-26** が実例として挙げている `deploy.sh` の事故と**同じ形**です。
→ ★シェルを経由しない方法で入れ直し、★**終了コードではなく内容の照合で確かめました**（R-27 の系）。

---

## 7. 検証

```
npm test          → 124 ファイル / 1223 件 PASS（exit 0）
npm run typecheck → クリーン（exit 0）
```

★素材を足していないので `ASSET_VERSION` は据え置き（57）です。

---

## 8. 再現コマンド（すべて読取専用）

```powershell
# ★4 角（A-1）
npx tsx tools/audit-cut-seam.mjs --seeds 42,253,90,2 --script v6
npx tsx tools/audit-corner-camera-sweep.mjs      # ★入口・出口の列を追加した
npx tsx tools/audit-corner-turn.mjs --script v6
npx tsx tools/_camdist.mjs                       # ★軸を跨ぐ瞬間の角度

# ★ダートの砂被り
npx tsx tools/_dustcover.mjs

# ★A-2（走路の折れ目）
npx tsx tools/_seamslip.mjs

# ★実画面（dev サーバー: cd apps/web ; npm run dev）
http://localhost:3210/race?seed=42&surface=dirt
```

---

## ★9. 申し送り

1. ★**「何を最適化しているか」を、苦情の言葉と突き合わせる。**（§1-1）
   ★90m は「カット内の反転」を消しましたが、苦情は「境目の反転」でした。
   ★**掃引表に出ていない列が、答えだったところ**です
2. ★**却下されたものの中身を、名前で扱わない。**（§1-4）
   ★却下されたのは「素材の反転を固定する機構」で、「向きが軸で入れ替わること」ではありません。
   ★**前便はこの 2 つを同じ名前で呼んで、選択肢を 1 つ失っていました**
3. ★**1 箇所の実測から「小さい」と決めない。**（§4）
   ★式にして全数に当てたら、上限は 2.7 倍でした
4. ★**「読める誤り」は、目でも試験でも通る。**（§5）★NUL より見つけにくい
5. ★**シェルにバッククォートを渡さない。**（§6）★R-26 の実例をそのまま踏みました
