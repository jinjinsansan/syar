# 【STAR】計画: 実レースを `/race` に渡す — 2026-09-26

- 開発側 Claude（`star-aa`）／ 裁定 `REVIEW_OWNER_SCOPE_AND_STUD_FEE_20260925.md` §9-1・§9-2
- 正本: **`DESIGN_LIVE_RACE_DATA_CONTRACT_20260921.md`**（★この計画はその契約に従います）
- ⚠️ **まだ実装していません。** 段 1 だけ済み（`0089`）。段 2 以降に**照会 2 件**があります

---

## 0. ★いま判っている事実（★推測ではなく現物）

| 何 | 現物 |
|---|---|
| 🔴 `/race` は**実レースを読んでいません** | `race/page.tsx:1731` … `horseId: String(i + 1)`（★**枠番そのもの**）／`raceId: \`r${seed}-...\`` |
| 🔴 だから毛色が枠番由来 | `COAT_BY_GATE`（18 枠の表・★月毛と白毛を含まない） |
| ✅ 焼きは 9 色 91 枚 済み | `apps/web/public/art/baked/`（2026-09-24） |
| ✅ 焼き済みを使うのは小さい端末だけ | `race/page.tsx:2455` … `wantBaked = baked===1 || smallTouch`。★PC はブラウザで焼く |
| ✅ 馬 ID を渡す口ができた | `0089`（★この便・staging 適用済み・9 色すべて出た） |
| ✅ 「結果から再現」の部品は在る | `components/uma/race-replay.ts`（`parseReplayRunners` / `replayProgress`） |
| 🔴 契約の判定はまだ満たしていない | 契約 §実装前の判定 の 5 項目（★下の段 3） |

## 1. 段 1 ✅ ★毛色の素を公開する（★済み・`0089`）

- `race_entries_public` に `horse_id` を足した（★末尾・`owner_id` は出さない）
- 毛色は `coat.ts` の `coatOfHorseId` のまま（★SQL に重みを写さない・D-052）
- 網 `coat-single-source`（5 件）で ★**2 つ目の毛色表**を作れなくした
- ⚠️ 本番は未適用（★0079〜0088 と同じ束）

## 2. 段 2 🔴 ★`/race` が実レースを読む（★**未着手・照会あり**）

### やること（契約 §暫定の録画表示 に沿う）

1. `?race=<uuid>` を **鞍の名前ではなく実レース ID** として受ける
   - ⚠️ いまの `?race=` は**鞍（コース）の選択**です。★同じ名前を 2 つの意味で使えません
   - → **照会 Q-RACE-1**（下記）
2. `races_public` と `race_entries_public` を読み、**確定済み（`settled`）のときだけ**走行を出す
3. 毛色は `coatOfHorseId(horse_id)`。★`COAT_BY_GATE` を消し、網の除外も消す
4. 画面に **「結果から再現」** と明示する（契約 §暫定・★「中継」と偽らない）

### ⚠️ ★この段でやらないこと（★理由つき）

- **生中継（`broadcasting`）はやりません。** 契約 §実装方式の分岐が
  「★発走より前に、結果の秘匿を守りながらサーバーが位置を時刻別に公開する処理」と
  「★60 秒周期とは別の更新経路」を要求しています。→ **新しい移行とワーカー変更**が要り、
  いま **0079〜0089 が本番未適用**なので、その上に積むと配備の順序が崩れます（★契約 §実装前の判定 の 5 番目）
- **着順をクライアントで作りません**（★憲法 3）。走行は**確定した着順とタイムからの補間**だけ

## 3. 段 3 🔴 ★契約の判定を満たす（★未着手）

契約 §実装前の判定 の 5 項目。★このうち 2 つは**いまの `/race` の作りと噛み合いません**:

| 判定 | いまの状態 |
|---|---|
| 同じレース ID の常設表示と本編で馬番・位置・着順が一致 | ★常設帯は `race-replay.ts`、本編は `/race` の独自模擬。★**別物** |
| ページ移動・縦横切替・再読込・タブ復帰で位置が先頭に戻らない | 🔴 `/race` は `?race=` を**モジュール読み込み時に 1 回だけ**読み、`location.search` を書いて**再読込**させる作り（`pickRace`・:2296）。★再読込は位置を 0 に戻します |
| 発走前の公開データから確定着順を読めない | ✅ `race_entries_public` は `settled` 以外で `finish_pos` を null にしている |
| 通信断は最後の取得内容＋更新失敗を出す | ★未実装 |
| 本番 DB・ワーカーの変更が配備順序と競合しない | ★段 2 を「録画」に限れば **DB 変更なし**で済む（`0089` 以外） |

## 4. 🔴 ★照会（★推測で埋めません）

### Q-RACE-1 ★`?race=` の意味を変えますか

- ★いまは **鞍（コース）の選択**です（★`pickRace` が `params.set('race', raceId)`）。
  ★開発の見比べで使っています（★50 鞍）
- ★実レース ID を渡すなら、★**別の名前**（例 `?live=<uuid>`）にするか、★鞍の口を別名に移すか
- ⚠️ ★どちらも ★**URL の口の意味を変える**ので、開発側で勝手に決めません

### Q-RACE-2 ★「結果から再現」を、常設帯と本編で **1 つの部品**にしますか

- ★契約 §実装前の判定 は ★「常設表示と本編で馬番・位置・着順が一致」を要求しています
- ★いま位置を作る式が **2 か所**あります: ★`race-replay.ts`（常設帯）と ★`/race` の模擬
- ★一致させるには ★**位置の式を 1 か所**にするのが素直ですが、★`/race` の映像は
  ★台本 v9・カメラ・`replayPositionModel` と絡んでおり、★**大きな作り替え**になります
- ★**段 2 を「まず `/race` だけ実データに寄せる」で止めるか、★最初から 1 部品にするか**の判断を仰ぎます

## 5. ★測ると約束したこと（★裁定の条件 ③）

- 「入れた後に画面の総量を測って報告する（★1 レース ≒ 10.5MB の内訳が変わるため）」
- ⚠️ ★**`/race` の総量はローカルでは測れません**（★簿 `race-page-only-measurable-in-production`・
  ★手元は `art` が 0 枚）。★道具は `tools/measure-race-asset-bytes.mjs`
- 🔴 ★**段 2 が入るまで総量は変わりません。** ★`0089` は**ビューの定義だけ**で、
  ★画面が引く画像は 1 枚も増えていません（★毛色はブラウザ側 or 焼き済みの 91 枚から選ぶだけ）
- → ★測るのは **段 2 を入れた後**。★そのとき ★**9 色 全部が引かれる**ので、
  ★焼き済みを使う端末では ★**引く枚数が増える向き**です（★いまは 7 色しか呼んでいない）
