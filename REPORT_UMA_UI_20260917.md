
# 報告：馬物語 UI の実装（R-14）— 2026-09-17

> 開発側 → オーナー・レビュー側。★デザイナーの引き渡し資料 `design_handoff_uma_monogatari/README.md`（39.8 KiB）に沿った実装の報告です。
> ★**資料の §1 のとおり、`*.dc.html` は参照物として読み、製品コードは Next.js / React の作法で作り直しています**（★`support.js` は持ち込んでいません）。

---

## 1. 進み方（★資料 §4.4 の推奨順）

| 順 | 中身 | 状態 |
|---|---|---|
| ① | **TOP 差し替え＋ダッシュボード新設**（A-1・A-2 の器を作る） | ✅ `b142da5` |
| ② | **中継の入口・出口ラッパー**（A-3・演出は無改造） | ✅ 本便（§2 の調査つき） |
| ③ | 通知部品 | ✅ 部品は `uma-parts.tsx` に。各画面に配置済み |
| ④ | 投票モード | ✅ `/vote`（★§9.5 の無効化つき） |
| ⑤ | オッズ | ✅ `/odds` |
| ⑥ | 育成・マイページ・景品交換 | ✅ `/train`・`/mypage`・`/exchange` |
| ⑦ | ポイントを稼ぐ | ✅ `/earn`（★接続先は未定のまま画面だけ） |
| — | **使い方** | ✅ `/howto` |

★**資料 §8 の 9 画面すべてを実装しました。**

### 1-1. 🔴 ★ルート名を新設にしました（★ご判断をお願いします）

★資料 §4.3 は「作り直し」ですが、★**同じ URL を奪うと既存の arcade 版が消えます**。
→ ★**既存を残したまま**、新しい UI を別のルートに置きました。

| 今回の画面 | 新ルート | 既存（★生きています） |
|---|---|---|
| TOP | **`/`**（★差し替え済み） | 旧 LP は `/lp-preview`・さらに前は `/lp-arcade` |
| ダッシュボード | `/home` | — |
| 育成モード | **`/train`** | `/training` |
| 投票モード | **`/vote`** | `/races`・`/races/[id]/bet` |
| オッズ | **`/odds`** | `/races/[id]/odds` |
| マイページ | **`/mypage`** | `/stable`・`/records` |
| 景品交換 | **`/exchange`** | `/prizes` |
| ポイントを稼ぐ | `/earn` | — |
| 使い方 | `/howto` | — |
| 中継の入口 | `/watch-race` | `/race`（★本体・**無改造**） |

★**`/home` の 6 ボタンは新ルートを指しています。** ★切り替え（★既存を消して URL を移す）はオーナー判断です。

---

## 2. 🔴 B-2 — 中継はどの実装に繋ぐか（★根拠を報告してから接続、の履行）

**結論: ★`/race` に繋ぎます**（★資料の既定と同じ）。

| 実装 | 大きさ | 調べて分かったこと | 判断 |
|---|---|---|---|
| **`/race`** | **401 KiB** | ★現ナビが「中継（デモ）」として繋いでいる先。★`story-shell.tsx` の `OWN_HEADER` にも入っており、★**共通ヘッダーが載らない前提で作られている**。★1280×720 の Canvas 固定 | ★**本線。ここに繋ぐ** |
| `/race-next` | 45.5 KiB | ★冒頭に作った側の記録: 「★**動いている `/race` に差し込もうとして 4 回壊しました**」「★**型検査もテストも通るのに、画面を見るまで壊れが分かりません**」→ ★**第 2 便として別に立てたもの**で、★本線ではない | 繋がない |
| `/watch` | 13.6 KiB | ★冒頭に「★**レース観戦（開発用）**— 実際に動くところを見るための画面」「GIF では間も速さも判断できない」 | 繋がない |

★**`race/page.tsx` は 1 行も触っていません**（★資料 §4.1）。★作ったのは ★**入口のラッパー `/watch-race` だけ**です。

⚠️ ★**全画面 API と 90° 回転をラッパー側で掛けていません。** ★`/race` 自身が持っており（★資料 A-3 の 4955・5045 行）、★二重に掛けると**回転が二重**になります。

⚠️ ★**出口（終了後にダッシュボードへ戻す）は、まだ繋いでいません。** ★`race/page.tsx` の終了を検知する口が要りますが、★**その口はあのファイルの中**にあります。★**無改造の約束**があるので、★**どう繋ぐかをご相談してから**にします（★§5 の照会 Q-UI-1）。

---

## 3. 本便で作った画面

| 画面 | ルート | 資料 | 註記 |
|---|---|---|---|
| **使い方** | `/howto` | §8-8 | 4 ステップ＋2 種類のポイント |
| **ポイントを稼ぐ** | `/earn` | §8-7 | ★**接続先（広告 SDK・オファー壁）は未定**。★ボタンは押しても何も起きません |
| **中継の入口** | `/watch-race` | §4.2 A-3・B-1 | ★案内 1 枚 → `/race`。★16:9 の枠を先に確保 |
| **オッズ** | `/odds` | §8-9 | ★単勝は**灰色**。★最下部に常設の注記（L-8） |
| **景品交換** | `/exchange` | §8-5 | ★**EP をこの画面に出さない**。★景品 8 件・★**発送なし** |
| **マイページ** | `/mypage` | §8-6 | ★厩舎 3 頭・成績 4 列・血統表。★詳細は `/stable/[horseId]` へ送る |
| **投票モード** | `/vote` | §8-4 | ★**自馬が出走すると行が押せず・EP は「—」・主ボタンは灰・理由を併記** |
| **育成モード** | `/train` | §8-3 | ★調教ステージ・顔アップ枠（★仮）・6 メニュー・説明を常時 1〜2 行 |

★どれも ★**共通部品**（上段バー・停止スイッチ・通知帯・ボタン・EP/PP カプセル・背景・デフォルメ馬）で組んでいます。

### 3-1. ★**既存の値に繋ぎました**（★画面に名簿を持たない・D-052）

⚠️ ★**最初、私は画面に名簿を直書きしていました。** ★実装の途中で気づいて直し、★**検査で固定**しました。

| 画面 | 直書きしていたもの | ★繋いだ先 |
|---|---|---|
| `/train` | ★**6 メニュー**（追い切り／坂路／プール／軽めの調整／休養／馬房で様子見） | ★`TRAINING_MENUS` → `@star/training` の **`MENUS`**。⚠️ ★正典 §7.2 の名簿は **8 件**で、★**画面だけ別の 6 件**でした。★資料と正典が食い違うときは**正典が上**です |
| `/train` | ★スタミナ 72・調子 ◎ の固定値 | ★`DEMO_TRAINING_ABILITY`・`conditionView`（★素質は渡しません・§5.5） |
| `/vote` | ★**12 行の出馬表**（馬名・騎手名） | ★`DEMO_BET_RACE.horses` ＋ `@star/scheduler` の **`JOCKEYS`** |
| `/vote` | ★自馬判定の**真偽値** | ★**`DEMO_BET_RACE.ownGate`**（★§9.5 の判定の出どころ。★画面で真偽値を作らない） |
| `/mypage` | ★**3 頭の厩舎**・★成績 4 列の固定値 | ★`sortStable(DEMO_HORSES)`・★成績は **`DEMO_RUNS` から数える** |

### 3-2. ★**オッズを実データに繋ぎました**（★2026-09-17・読み取りのみ）

| 何を | どう |
|---|---|
| **`/odds/[id]`**（新規・★サーバー部品） | ★`races_public`・`race_entries_public`・`race_odds_public` から読みます（★実体テーブルは revoke 済み・anon では読めません） |
| **`/odds`**（作り替え） | ★**次の 1 本へ送る入口**。★`scheduled_at` の順に読み、★`scheduled` か `closed` の最初の 1 本へ `redirect`。★**以前ここに置いていたデモの出馬表 12 行は消しました**（★`/odds/[id]` と二重帳簿になるため・D-052） |
| **`uma-odds-view.tsx`**（新規・client） | ★見せ方だけ。★停止スイッチが `useState` を使うので、★**読むのはサーバー・見せるのは client** に分けました |

⚠️ ★**画面で計算していません**（★正典 §14.3）。★オッズも人気もサーバーの値をそのまま出し、★整形は `lib/format.ts` の 1 か所から引きます。★**検査で固定**しました（★「オッズを画面で計算していない」「`service_role` を持ち込まない」「読み取りの失敗を黙って空にしない」）。

⚠️ ★**複勝は範囲表記にできていません** — ★資料 §8-9 は「範囲（`1.5 - 2.1`）」ですが、★いまのオッズ表は**1 つの値**しか持ちません。→ ★**同じ値を両端に置いています**（★幅を画面で作らない）。★範囲の出どころができたら、そこから引きます。

⚠️ ★**まだ繋いでいないもの**:
- ★`/train` は ★**週送り（`advanceWeek`）を呼んでいません**（★押しても状態は動きません）
- ★**顔アップ枠は仮**（★同じ絵を拡大して切り出し）。★**表情 3 種のアセットは未作成**（★デザイナー待ち）
- ★**景品の絵 8 枚は未作成**（★いまは斜め縞の枠に「ゲーム内の品」）
- ★**ログインが無いので、どの画面もデモの口座**です（★EP 1,240・PP 380）

---

## 4. ★資料 §13 の確認リストに対する自己申告

| # | 条件 | 状態 |
|---|---|---|
| 1 | 390 幅で横スクロールが出ない | △ ★**未確認**（★実機で測っていません・§5 の Q-UI-2） |
| 2 | 390 / 1280 で文字が省略・切断されていない | △ 未確認 |
| 3 | ボタン・リンク・行の当たりが 44px 以上 | ✔ ★部品側で `minHeight` を固定 |
| 4 | 下端に 34px の余白 | ✔ `--u-safe-bottom` |
| 5 | 禁止語 5 語が 0 件／「馬券」「商品交換」も 0 件 | ✔ ★**検査で固定**（`uma-ui-wiring.test.ts`）。★既存 `entry/page.tsx` も直した |
| 6 | EP と PP を合算した数字がどこにもない | ✔ ★**検査で固定**（★足し算・合計の語・1 部品で両方受け取る形を禁止） |
| 7 | EP の副題が B-3 の文言 | ✔ 検査で固定 |
| 8 | 自馬が出走するレースで投票導線が出ない／理由が出る | ✔ ★**通知・投票モードの両方で実装**（★`/vote` は行が押せず・★使う EP は「—」・★主ボタンは灰で「投票はできません」・★理由「自分の馬が出走しているため」を併記）。★**検査で固定**（`uma-ui-wiring.test.ts`） |
| 9 | `prefers-reduced-motion` で初期停止・情報が欠けない | ✔ ★CSS でも JS でも止める。★`animation:none` で止め、★速度線は 0% から不透明 |
| 10 | 停止スイッチが全ページ常設 | ✔ ★本便の 3 画面すべてに |
| 11 | 共通ヘッダーが二重で載っていない | ✔ ★`OWN_HEADER` に `/home`・`/howto`・`/earn`・`/watch-race`。★**検査で固定**。⚠️ ★**この行を先に書いてしまい、実際は後から足しました**（★画面を 2 つ作った時点では `/home`・`/howto` だけでした）。★検査の対象にも 2 つを足し、★**画面を足したらここも足す**と註記しました |
| 12 | arcade の `a-*` / `.frame` を使っていない | ✔ ★**検査で固定**（★`--a-` トークンも禁止） |
| 13 | 「レースを見る」が案内 1 枚 → 全画面 → 終了後ダッシュボード | 🟡 ★**案内と入口は実装。★出口は未接続**（§2 の註記・Q-UI-1） |
| 14 | 中継のルートで通知が描かれない | ✔ ★`/race` は `OWN_HEADER` 側で、★通知部品を置いていません |
| 15 | `race/page.tsx` を改造していない | ✔ ★**1 行も触っていません** |

---

## 5. 照会

| # | 内容 |
|---|---|
| **Q-UI-1** | ★**中継の「出口」をどう繋ぎますか。** ★B-1 は「終了後は必ずダッシュボードへ」ですが、★終了を知るには `race/page.tsx` の中に口が要ります。★候補: ⓐ**`?return=/home` を渡し、あのファイルに 1 行だけ足す**（★無改造の約束を最小限だけ破る）ⓑ**中継の後に必ず通る別のルートを挟む**（★`/race` の完了を待たず、★時間かページ遷移で戻す）ⓒ**この便では戻さない**（★利用者が「戻る」で帰る）。★開発側の推奨は **ⓐ**（★1 行で済み、★あのファイルの描画には触れません） |
| **Q-UI-2** | ★**390 幅の実測を、どこで取りますか。** ★資料 §13 の 1・2 は ★**実機か開発サーバーでしか測れません**。★候補: ⓐオーナーが `npm run dev:web` で見る ⓑ開発側が道具で測る（★`tools/verify-mobile-layout.mjs` が既にあります） |
| **Q-UI-3** | ~~残りの画面をどの順で出しますか~~ → ✅ **9 画面すべて実装しました。** |
| **Q-UI-4** | ★**新しいルートに切り替えますか。** ★いまは既存（`/training`・`/races`・`/stable`・`/prizes`）を**残したまま**、新 UI を別ルート（`/train`・`/vote`・`/mypage`・`/exchange`）に置いています。★候補: ⓐ**このまま並走**（★比べられる・★URL が 2 系統）ⓑ**新 UI に寄せる**（★既存を消す・★§4.3 の「作り直し」の字面どおり）ⓒ**既存の URL に新 UI を載せ替える**（★リンクは変わらない・★既存の実装は消える）。★開発側の推奨は **ⓐ→ⓒ**（★オーナーが実機で見て合格してから載せ替える） |
| **Q-UI-5** | ★**データへの接続を、どの画面から繋ぎますか。** → 🟡 **表示の層は繋ぎました**（§3-1）。★**ここから先は進めずに止めています**（下の §5-1） |

---

### 5-1. 🔴 ★ここで止めました（★推測で作らないため）

★「進めてください」を受けて `/odds` の実データ接続と `/train` の週送り接続に入りましたが、★**調べた結果、開発側の判断では進められません**。

| 何が止めているか | 根拠 |
|---|---|
| ★**週送りを画面から呼べません** | ★週送りは**ワーカーの仕事**（`apps/worker/src/training-runner.ts`）。★画面から `advanceWeek` を呼ぶのは ★**サーバー権威（憲法 3）違反**。★正典 §14.3 は Vercel を「★**フロントと読み取り系だけ**」と定め、★ビジネスロジックの実装を禁じています |
| ★**書き込みの口がありません** | ★調教の指示は **RPC `spend_training_ep`**、★投票は `place_bet`、★交換は `exchange_prize`。★既存の `/training`・`/races/[id]/bet` も ★**「サーバー RPC に繋ぐまで押しても何も起きない」**と明記して止めています |
| ★**ログインがありません** | ★`lib/supabase.ts` は ★**読み取り専用の anon クライアントだけ**。★「自分の馬」「自分の EP」を出す前提が立ちません |
| ★**`/odds` はルート設計の変更が要ります** | ★既存の `/races/[id]/odds` は ★**サーバー部品**で `readClient()` から読みます。★私の `/odds` は `'use client'` で ★**レース id を持ちません**。→ ★`/odds/[id]` にするか、★`/odds` が「次のレース」を選ぶかを決める必要があり、★**資料にも指示にもありません** |

★**Q-UI-6** として伺います: ★**次に進めるべきはどれですか。**
ⓐ ★**ログイン（Supabase Auth）を先に入れる**（★これが無いと「自分の」画面が作れません）
ⓑ ★**読み取りだけ先に繋ぐ**（★`/odds/[id]` を作り、★`/vote` も id で受ける。★書き込みは後）
ⓒ ★**見た目の確認を先にする**（★オーナーが実機で見て、★直しを出してから接続に進む）
★開発側の推奨は **ⓒ→ⓑ→ⓐ** です（★見た目が固まる前に配線すると、★作り直しが二重になります）。

---

## 6. ★触っていないもの

- ~~★**`apps/web/src/app/race/page.tsx`（401 KiB）** — 1 行も触っていません~~
  🔴 ★**この行は 2026-09-17 の第 6 便で嘘になりました。** ★中継の出口のために **触りました**（★下の §7-1）。
  ★触った量は **+46 行 / −6 行**（★`git diff --numstat HEAD(2503beb) → 作業ツリー` の実測）。
  ★内訳は ★戻り先の名簿と、★出口 2 か所の分岐と、★出口のボタンの文字 2 か所と、★旧リンク 1 本です。
  ★描画・演出・較正・コース・カット割りには触れていません。
  ⚠️ ★この行を最初「**+40 / −5**」と書きました。★**測る前に書いた数字**で、★外れていました（★測って直しました）。
- ★**arcade テーマ**（`globals.css`・`[data-theme="arcade"]`）— ★既存画面が使うので消していません
- ★**較正定数・正典・本番 DB**
- ★`design/hud-ds` の 30 枚のカード（★あちらは**別系統の正本**として有効）

---

## 7. ★第 6 便 — 中継の出口と、新ルートへの切り替え（2026-09-17）

> ★オーナー指示「★中継の出口も ★ハンドオフ通りにしてください ★また新ルートに切り替えてください」

### 7-1. ★中継の出口（★ハンドオフ B-1「★終了後は必ずダッシュボードへ」）

★**何が足りなかったか**: ★`/race` は ★**どこから来たかを知りません**でした。★中継が終わると
★`/race` の中のメニューへ戻るだけで、★B-1 の「★終了後は必ずダッシュボードへ」に届いていませんでした。
★第 5 便までの `/watch-race` は「★案内 → 送り出す」だけで、★**出口が無いまま**でした。

★**直し方**（★照会 Q-UI-1 で推奨した ⓐ「★`?return=` を渡し、★あのファイルに最小限だけ足す」を採りました）:

| 場所 | したこと |
|---|---|
| `app/watch-race/page.tsx` | ★送り先を `/race` → **`/race?return=/home`** |
| `app/race/page.tsx` | ★**戻り先の名簿** `RETURN_ROUTES` を新設（`/home`・`/vote`・`/odds`・`/mypage`） |
| 〃 | ★出口 2 か所（★確定カードの「メニューへ」・★右上の「メニュー」）が、★戻り先があれば **そこへ送る** |
| 〃 | ★ボタンの文字を戻り先の名前に（★`/home` なら「**ダッシュボードへ**」） |

★**`/` 始まりの検査にしなかった理由**: ★`?return=` をそのまま `location.href` に入れると
★**外部の URL へ飛ばせます**（★開いた転送口）。★`/` 始まりだけを見る書き方では ★**`//よその所` が通ります**。
→ ★**名簿との完全一致でだけ**受けます。★名簿に無い値は ★**黙って無視**し、★これまでどおり
★`/race` の中のメニューへ戻ります（★`/race` を直接開くデモの道を壊さないため）。

### 7-2. ★新しいルートへの切り替え

| 直した場所 | 旧 | 新 |
|---|---|---|
| `components/nav.tsx` の 4 本の錠剤 | `/races`・`/stable`・`/training`・`/records` | **`/home`・`/vote`・`/mypage`・`/train`・`/exchange`**（★5 本に） |
| 〃 右端「中継（デモ）」 | `/race`（★案内を飛ばしていた） | **`/watch-race`**（★案内 1 枚を必ず通す） |
| `app/howto/page.tsx` | `/training` | **`/train`** |
| `app/earn/page.tsx` | `/races` | **`/vote`** |
| `app/home/page.tsx` の通知 | `/races` | **`/vote`** |
| `app/login/page.tsx` | `/races`「番組表を見る」 | **`/home`「ダッシュボードへ」** |
| `app/signup/page.tsx` | `/race`・`/races` | **`/watch-race`・`/home`** |
| `app/setup/page.tsx` | `/stable`・`/training` | **`/mypage`・`/train`** |
| `app/race/page.tsx`（★重い端末の案内） | `/races`「番組表へ」 | **`/home`「ダッシュボードへ」** |

🔴 ★**`/earn` の 1 本は、私の見落としを検査が見つけました。**
★先に足した検査が落ち、★その後に流した洗い出しでも同じ所が出ました。
★「★新しい画面はもう全部新ルートを指している」と ★**思い込んでいた**のを、★機械が止めました。

★**旧い画面は消していません**（`/races`・`/stable`・`/training`・`/records`・`/prizes`）。
★**旧い画面の中のリンクも、旧のままにしました。** ★理由: ★旧い画面は「★見比べと差し戻し」のために
★残しています。★中のリンクまで新へ向けると ★**旧い画面どうしを行き来できなくなり、残した意味が消えます**。
★入口（ナビ）は新へ向けたので、★旧い画面に着いた人は ★**出られます**。

🔴 ★**上の 1 行は、最初に書いたとき嘘でした**（★2026-09-17・同日中に直しました）。

★**ナビはこの作業ツリーに 2 つあります。**

| どれ | 場所 | 出る画面 |
|---|---|---|
| ① `ArcadeNav` | `components/nav.tsx` | ★`INTERIOR` にも `OWN_HEADER` にも入らない画面＝ ★**ほぼ開発用**（`/watch`・`/camera` 等） |
| ② `LINKS`（★直書き） | `components/story-shell.tsx` | ★**旧い画面のほぼ全部**（`/stable`・`/training`・`/races`・`/records`・`/prizes`・`/login`・`/signup`・`/setup`） |

★私は ★**①だけ直して**、★「旧い画面に着いた人は出られます」と書きました。★実際には
★**旧い画面のナビは 1 本も変わっていませんでした**。★`story-shell.tsx` の「▷ レースを観る」も
★`/race` 直リンクのままでした。

★**どう見つけたか**: ★オーナーが開発サーバーを開けなかった件を追う途中で、★`/records` の HTML を
★引いて ★**新しい行き先が 1 本も出ていない**ことに気づきました。★検査は通っていました
（★検査も ★**`nav.tsx` しか見ていなかった**ため）。

★**直したこと**: ★②の `LINKS` も新ルートへ。★「▷ レースを観る」も `/watch-race` へ。
★検査を ★**両方のナビを見る**形に広げました。

### 7-3. ★検査（★`apps/cli/test/uma-ui-wiring.test.ts`・15 → 17 本）

★足した 2 本:
- ★**中継の出口**: ★案内が `?return=/home` を渡している／★名簿に `/home` がある／
  ★**完全一致で受けている**（★`hasOwnProperty` を使っている）／★出口が戻り先を見ている
- ★**新ルート**: ★新しい 10 画面が `/races`・`/training`・`/prizes` を指さない／★ナビが新 5 本と `/watch-race` を指す

★**変異検査 5/5 が落ちました**（★R-16「★通るだけの検査を置かない」の確認）:

| 変異 | 検査 |
|---|---|
| ① ★案内から `?return=/home` を外す | ★落ちた |
| ② ★出口のハンドラから戻り先の分岐を消す | ★落ちた |
| ③ ★完全一致をやめて `/` 始まりで受ける（★開いた転送口） | ★落ちた |
| ④ ★ナビを `/races` へ戻す | ★落ちた |
| ⑤ ★ナビの中継を `/race` 直リンクへ戻す | ★落ちた |

★戻したあと **17/17 通過**。★型検査 通過。★`build:web` 通過。
★全テスト **2051 通過 / 1 失敗**、★失敗は ★**以前からの赤**（`packages/render/test/edit-grammar-audit.test.ts` ⑨）だけで、★本便と関係ありません。

🔴 ★**自分の変更で自分の検査を 1 本壊しました。** ★`'/race'` の完全一致で見ていた行が、
★`?return=/home` を足したことで落ちました。★前方一致に直しました（★出口そのものは新しい検査で固定）。

### 7-4. ★照会 **Q-UI-7** — ★旧いルートをどうしますか

★「★新ルートに切り替えてください」を、★開発側は ★**「リンクを新へ向ける。★旧い画面は残す」**と読みました。
★**消す**という判断はしていません。★次のどれにしますか。

- ⓐ ★**このまま**（★旧い画面を残し、★入口だけ新へ）… ★開発側の推奨。★見比べと差し戻しができます
- ⓑ ★**旧い画面を消す**（★`/races`・`/stable`・`/training`・`/records`・`/prizes`）… ★戻れなくなります
- ⓒ ★**旧い画面を新しい画面へ転送する**（★開いたら自動で新へ飛ぶ）… ★見比べができなくなります

★あわせて ★**対応画面が無い 3 つ**をどうするか伺います（★いまは旧いままにしています）:

| いま | 置き場所 | 新 UI に対応画面が |
|---|---|---|
| `/races`（★番組表） | ★`/race` のデモのメニュー足元 1 本 | ★**ありません**（★`/odds` は「次の 1 本」へ送るだけ） |
| `/records`（★記録） | ★`/exchange` の「交換の履歴」 | ★**ありません** |
| `/stable/[id]`（★馬の詳細） | ★`/mypage` の各馬 | ★**ありません**（★血統表はデザイナーの残作業） |
