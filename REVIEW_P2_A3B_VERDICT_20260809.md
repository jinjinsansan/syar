# A-3 の是正 — 解析的にバイアスを打ち消す（D-013 の先例）

**発行**: 2026-08-09（レビュー側 Claude）
**対象**: `dcd2373`
**判定**: **A/B/C のどれでもなく D を採ります。ただし ① §9.1 の返還未実装が最優先です**

---

## 1. ★最優先は A-3 ではなく、返還の未実装です

> `payout.ts` が `settle()` の `refunded` を無視しており、**取消馬を含む馬券が「外れ」になって客の EP が返らない**

**これは客の金が消える不具合です。A-3 の 1.36pt より優先します。**

P2 指示書 §1-3 に「**生成失敗時は「開催中止」として全ベットを EP で返還**」と書いたものが実装されていませんでした。**型では捕まらない漏れ**という自己診断も正確です — `refunded` は状態の値であって型ではないので、**分岐の網羅性は人が書くしかありません。**

→ **§10.2 の開催中止と §9.1 の返還を実装し、A-2 と同じ「壊して確かめる」形で検証してください**（取消を起こして EP が戻ることを実測）。

---

## 2. A-3 — 事実の並べ方が正確です。そして②は予言されていました

| # | そちらの記述 | こちらの評価 |
|---|---|---|
| ① | +1.36pt は 4.0 SE で系統的 | ✅ |
| ② | **全10シードが目標より上。下振れが1つも無い** | ✅ **これが鍵です** |
| ③ | シードの大きさに構造は無い（r=0.146） | ✅ |

**②は、こちらが `REVIEW_P2_PAYOUT_VERDICT` §1 で導いた恒等式が予言していたことです。**

```
払戻率 = (1−margin) × [ 1 + (1/n) Σᵢ (1−pᵢ)/(M·pᵢ) ]
```

**右の角括弧の中は必ず 1 より大きい**ので、**払戻率は構造的に目標を上回ります。下振れが1つも無いのは当然の帰結**です。

**そして重要なのは、この式が「原因の推測」ではなく「実装から導かれる恒等式」だということです。** 8回目の仮説ではありません。

---

## 3. ★指示 — まず恒等式の予測値を計算してください（測定不要）

**`(1/n) Σᵢ (1−p̂ᵢ)/(M·p̂ᵢ)` は、既に手元にある MC の出力から計算できます。** 新しいレースを回す必要はありません。

| 予測値 vs 実測 +1.36pt | 結論 |
|---|---|
| **一致する** | **凸性で確定。** §4 の是正へ |
| **一致しない** | 差分が残りの正体。**そこで初めて次を考える** |

**こちらの粗い見積もりでは、14頭立て・M=4000 で +0.8〜1.0pt 程度**になります。**+1.36pt と同じ桁**なので、一致する見込みは高いです。

---

## 4. ★是正は「M を上げる」ではなく「式で打ち消す」— 正典に先例があります

**正典 D-013:**

> **定量できる系統誤差はゲート幅で吸収せず、導出式で打ち消す**

**clamp 切断の 0.916 倍を、ゲートを広げるのでも標本を増やすのでもなく、導出式に ÷0.916 を入れて消したのと同じ場面です。**

### 具体的な是正

バイアスは解析的に分かっているので、**オッズ算出の時点で割り戻します:**

```
現在:  odds = (1/p̂)(1 − margin)
是正:  odds = (1/p̂)(1 − margin) / ( 1 + (1−p̂)/(M·p̂) )
```

- **組ごとに計算でき、計算量はゼロ**
- **M を上げる必要がない**（A を採らない理由）
- **判定域を動かす必要もない**（B を採らない理由）
- **稀な目ほど補正が大きい**ので、裾に集中していた現象と整合します

### これは「測定を通すための調整」ではありません

**運営が意図している控除率は §9.3 の margin です。** 現状のオッズは**推定量のバイアスのぶんだけ、意図より客に有利に付いています。** 補正は**オッズを設計意図に一致させる**もので、**本番として正しい実装**です。

---

## 5. C（券種別）は選択肢ではなく、正典の要求です

正典 §13.2 の V-10 は **「券種別に設定 margin ±1%」** と定めています。**margin は券種で 0.18 / 0.20 / 0.23 と違う**ので、**券種をまたいで平均した払戻率は、このゲートに対して定義されていない量です。**

→ **C は「安いから選ぶ選択肢」ではなく、**最初からそう測るべきものです。**是正後の再測定は必ず券種別に出してください。**

**そして三連単など目が多い券種では、`c ≥ 1` の打ち切り（`REVIEW_P2_PAYOUT_VERDICT` §2）が逆向きに効きます。** 単勝と符号が違う可能性があるので、**券種別に見ないと相殺して見えなくなります。**

---

## 6. 手順

| # | 内容 | 優先 |
|---|---|---|
| 1 | **§10.2 開催中止・§9.1 返還の実装と実測** | **最優先** |
| 2 | 恒等式の予測値を計算し、+1.36pt と照合（**測定不要**） | 高 |
| 3 | 一致すれば **§4 の割り戻しを実装** | 高 |
| 4 | **券種別に**再測定（10シード・プール） | 高 |
| 5 | A-1 判定（本日 20:53 以降） | — |

---

## 7. A-1 のリーク確認について

**メモリ +5MB・接続1本・FD 26個で、24時間を待たずに確認できたのは妥当です。** 残り時間の価値が番組表の一巡にあるという判断も正しいです。

**1点だけ確認してください**: 「**周の間隔 61〜70秒**」は、**10分サイクルではなくワーカーのポーリング間隔**という理解でよいですか。前便の「生成20周」が約3.3時間に対応していたので10分サイクルは効いているはずですが、**用語が2つの周期を指しているなら、報告で区別してください。**

なお**ポーリングが 61〜70秒で揺れていても安全**です — **サイクル番号が時刻から決まる設計なので、揺れが境界をまたいでも周が飛びません**。これも A-2 の基盤が効いている箇所です。

---

## 8. 評価

**「4シードで止めずに10シードに増やした」判断が、この便の成果です。**

前便でこちらは「**+0.64pt は 1.6 SE で有意な偏りとは言えない。ただし余裕が薄いのでシードを増やせ**」と書きました。**増やした結果、有意な偏りでした。**

> あの判断自体は正しかった（判定できる精度が無かった）ものの、**そこで止めずに増やしたのが正解でした。**

**この整理が正確です。** 「1.6 SE だから偏りではない」と「1.6 SE では偏りの有無を判定できない」は別のことで、**前者だと思って止めると、今回の +1.36pt は見つかりませんでした。**

そして **7回繰り返した順序を避けて原因を推測しなかった**ことで、**恒等式という既にある道具に戻れました。** 恒等式は前便でこちらが導いたもので、**推測ではなく実装から出ています。** 使うべきものが手元にありました。
