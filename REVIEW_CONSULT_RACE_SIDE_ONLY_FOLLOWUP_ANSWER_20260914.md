# 回答 — 再照会「真横の直線だけ」の演出で、結果を先に明かさない線（Q-R7〜Q-R9）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-14 ／ レビュー側 → 開発側・オーナー
**対象**: `CONSULT_RACE_SIDE_ONLY_FOLLOWUP_20260914.md` ／ ブランチ `p4/race-30sec-cuts` ／ HEAD `319bae0`
**前の文書**: 相談書 `CONSULT_RACE_SIDE_ONLY_20260914.md` ／ ① の回答 `REVIEW_CONSULT_RACE_SIDE_ONLY_ANSWER_20260914.md` ／ まとめ `SYNTHESIS_RACE_SIDE_ONLY_20260914.md`

> 凡例（R-13）: **✔ [CODE]** ＝ レビュー側が正典・コードを読んで確かめた ／ **計算** ＝ コードの定数・正典の実測値から出した値で、画面での実測ではない ／ **△** ＝ 未確認 ／ **[EYES]** ＝ オーナーの目で決めること
> 本書は正典を編集しません。

---

## 0. 再照会 §1 の事実の照合

| 再照会の記述 | 照合 |
|---|---|
| 展開の札の勝ち馬は確定着順の 1 着（`settled[0]`） | ✔ `apps/web/src/app/race/page.tsx:1685`・`packages/render/src/race-development.ts`（`raceDevelopmentOf(atStraight, winnerGate)`） |
| 札の判定地点は、先頭が最後の直線の入口に達した瞬間 | ✔ `page.tsx:1701-1707` |
| 札ごとの画角・先頭の置き場所 11.0°/0.58・11.5°/0.68・12.0°/0.78、残り 260m から寄せる | ✔ `broadcast-v2.ts:2650-2657` |
| 註記の表（11.5°・15°・19°／0.62・0.50・0.38）が定数と合っていない | ✔ `broadcast-v2.ts:2621-2626`。直す対象（§4） |
| 寄せの重みは残り 250m で約 0.011・200m で 0 | ✔ 計算（`formation.ts` `convergeAt`: t = 50/800 = 0.0625 → 3t² − 2t³ ≈ 0.0112） |
| 接戦の判定は先頭が残り 80m に達した瞬間の位置 | ✔ `page.tsx:1687-1692`・`broadcast-v2.ts`（`broadcastV2FinishStyleOf`） |
| 残り 80m の位置は「**真の位置**」 | **表現の訂正**（下記） |
| 音は札を使っていない | ✔ `apps/web/src/app/race/race-audio.ts` に札の参照なし |

**表現の訂正**: 位置モデル（`page.tsx:1566-1581` `replayPositionModel` → `trafficPositionModel`）は、**エンジンが γ 写像を掛けた結果**から作られています（✔ γ は `packages/race-engine/src/race.ts:267` で掛かり、デモの γ は `page.tsx:1554-1556` で渡す）。着順ボードの着差ラベルも、同じ写像後の秒差から出ています（✔ `race.ts:295` → `page.tsx:1719`）。
→ 正典が認めた写像（D-064・D-082・D-085。単調ゆえ着順不変）なので**問題ではありません**。ただし「真の位置」ではなく「**寄せが終わった表示位置（γ 写像後・着順ボードと同じ系）**」と呼んでください。

---

## 1. Q-R7 — 展開の札で、直線の入口から構図や音を変えるのは「勝ち馬の型を先に明かす」に当たるか

**回答: 当たります。文字でも画でも音でも同じ扱いです。今の既定（再照会 §1-2）も直す対象です。**

### 1-1. 守る線

> **時刻 t の画・音は、時刻 t までに描かれる状態だけの関数とする。未来の値（確定着順・1 着の馬番・着差・ゴール時刻）を入力に取らない。**

### 1-2. 理由

1. **正典 D-063**: 「問うべきは『真実か演出か』ではなく『**漏らす演出か、劇になる演出か**』」。結果から決める構図は、漏らす演出です
2. **弱くはありません（計算）**: 画角の差は 1° ですが、**先頭の置き場所の差は 0.78 − 0.58 = 0.20 × 画面幅 1280px ＝ 256px** です。再照会 §1-3 のとおり、B では見せる直線の**最初のコマから**効きます。開発側の「毎レース見ていれば覚えられる」は正しいです
3. **構造的に作れない人がいる**: 介入する人のクライアントは、締切（ゴール 0.5 秒前・正典 §8b.4）まで**着順を持っていません**（2 段階配信・§14.5）。この人たちには結果から構図を決めることが**そもそもできない**ので、いずれ状態から決める形が要ります。好みの問題ではありません
4. △ **V-16 の計測を歪めるおそれ**: V-16 のボットは描画コマンドを読みます（D-067）。構図が勝ち馬の型を運ぶと、V-16 ②（勝負所以降で画面が出走表を上回る）が**漏れのおかげで通る**おそれがあります（R-16）。計測はしていません

### 1-3. どの強さまで許すか

| 構図・音を決める入力 | 許容 |
|---|---|
| 結果（確定着順・1 着・着差・ゴール時刻・展開の札） | **強さにかかわらず不可**（ゴールの前） |
| 時刻 t までの状態（位置・差・縮み方） | **この線からの制限なし** |
| ゴールの後（勝ち馬の追従・見出し・リプレイ・着順ボード） | **札を使ってよい**（結果は既に出ている） |

→ デザイナー側 D-8 の「独走の孤独の構図」「後ろから低い唸り」は、**状態から決めるなら可**、**札から決めるなら不可**です。

### 1-4. オーナー指示（2026-09-12）との関係

- 指示の言葉は「**追い込み馬がある時は**引く必要がありますが、それは展開によってカメラワークを切り替えてください」（✔ `broadcast-v2.ts` の `broadcastV2FinishCamera` の註記）
- これは「**後ろから伸びてくる馬が、いま画面の後ろにいる時**」という**状態**として読めます。状態から決めれば、指示の言葉どおりに実装できます
- **この読み方でよいか、オーナーの確認が要ります**（まとめ O-7）

### 1-5. 状態から決める手本（既にあるもの）

| 部品 | 何から決めているか | 判定 |
|---|---|---|
| `race-call.ts:110-124`「伸びてきた馬」 | 過去の窓で、先頭との差がどれだけ縮んだか | ✔ 線を守っている。**残す** |
| `broadcastV2FinishStyleOf`（残り 80m の接戦判定） | その瞬間の位置の差 | ✔ 線を守っている。**残す** |
| `FINISH_CAMERA_BY_DEVELOPMENT`（残り 260m から） | 確定着順の 1 着 | **直す** |

### 1-6. 検査

| # | 内容 |
|---|---|
| 1 | **反実仮想の検査**: 時刻 t まで全馬の位置が同じで、**1 着だけが違う** 2 つの合成の位置モデルを作り、時刻 t の**カメラ（画角・先頭の置き場所・注視点）と音の合図が一致**すること |
| 2 | ゴールより前のカメラ・音を決める関数が、`winnerGate`・`development`・確定着順を**引数に取らない**こと |
| 3 | **変異**: 構図に札を戻すと 1 が落ちることを実演する |
| 4 | 置き換えた後の既定で V-16 を測る（① の回答 §6-2 #8） |

---

## 2. Q-R8 — 勢いのバーを、寄せの途中（残り 250→200m）で出してよいか

**回答: 「残り 200m から」と定義することに賛成します。ただし縛りになる条件は、出し始める地点ではなく「描画と同じ入力から出す」（① の回答 §2-2 ①）です。**

- ①を守れば、250m から出しても 200m から出しても**絵と矛盾しません**。「200m から」は、検査を単純にするための定義として採ってよいです
- ⚠️ **根拠を「影響は 1% 以下」にしないでください。** 見た目の速さのずれは、重みの値ではなく「**重みの傾き × 隊列と真の位置の差**」で決まります（`climax-choreography.ts` の註記が同じ式で説明しているもの）。いずれにせよ未計測です
- **条件**
  1. **`climax=on` を既定にすると、表示位置のずらしは残り 60m まで残ります**（`climax-choreography.ts` `RELEASE` 60m）。そのとき「200m から先は寄せが終わった位置」は崩れます。→ **まとめ O-3（接戦を作らない vs 攻防の演出）とセットで決めてください**
  2. **勢いは過去の窓から出す**（`race-call.ts` と同じ形）。Q-R7 の線（未来の値を読まない）を守るためです
  3. 段階に丸め、数字（m/s）では出さない（① の回答 §2-2 ③）
- [EYES] B では、見せる直線に入ってから**約 50m（約 3 秒）はバーが出ません**。それでよいかは見え方の判断です

---

## 3. Q-R9 — 写真判定の止め絵を出すかどうかを、どの値で決めてよいか

**回答: (a)・(b) のどちらでもなく、(c) を推奨します。**

> **(c) 先頭が決勝線を通過する瞬間の表示位置 — つまり「止める絵そのもの」— で、1〜2 着の差を見て決める**

### 3-1. 理由

1. **先に明かすことにはなりません**: ゴールの瞬間に出すので、(b) も (c) も結果の先出しに当たりません。締切はゴール 0.5 秒前（§8b.4）なので、**介入する人も、ゴールの時点では 2 段階目（残り区間＋着順）を受け取っています**（§14.5）
2. **(a) の弱点は、開発側が (b) について書いたのと同じ**です。残り 80m からゴールまでに差は開いたり詰まったりします。→ **止め絵が離れて見える／並んだゴールで止め絵が出ない**、が起きえます。止め絵は「その瞬間」の絵なので、**判定もその瞬間で行う**のが一致します
3. **(b)** は §0 のとおり表示位置と同じ γ 写像の系なので、開発側の心配ほどはずれないはずです（△ 未計測）。それでも、**止める絵と同じ入力から決める**ほうが確実です（R-30）
4. (c) は画面に映っている差だけで決まるので、**映っていない接戦を作りません**（相談書 C-2）

### 3-2. ⚠️ 別の発見 — 着差の言葉を決める場所が 2 つあり、境目が合っていない

| 場所 | 決め方 | 出どころ |
|---|---|---|
| **着順ボード** | 秒差: ハナ <0.03 秒・アタマ <0.06・クビ <0.10・1/2 馬身 <0.16 | ✔ `packages/race-engine/src/race.ts:86-90`・`balance.ts:550` 以下 |
| **実況** | 馬身: ハナ <0.15・アタマ <0.4・クビ <0.8・半馬身 <1.2 | ✔ `packages/render/src/race-call.ts:136-142` |

- **計算**: 1600m の平均速度を約 16 m/s（正典 D-064 の実測「1 着 99.9 秒」から）とすると、ボードの境目は馬身で **0.20・0.40・0.67・1.07**
- → **実況とボードで言葉が食い違う帯が 3 つ**あります（0.15〜0.20 馬身・0.67〜0.8 馬身・1.07〜1.2 馬身）。△ ゴール付近の実際の速さでは確かめていません
- **止め絵に着差の言葉を出すなら、着順ボードと同じ場所（`marginLabel`）から出してください**（D-052「条件は 1 か所で引く」・R-30）
- 実況の言葉をボードに揃えるかは、次の指示書の範囲に入れるのがよいです

### 3-3. 検査

| # | 内容 |
|---|---|
| 1 | 止め絵の判定関数が、**止める絵を描くのと同じ位置**を読むこと（経路の検査・R-30） |
| 2 | 多数の seed で **(c) と (b) の判定が食い違う件数**を出し、報告に書く（合否の線は引かない） |
| 3 | 止め絵が出たレースでは、着順ボードの 1〜2 着の着差ラベルがクビ以内であること。食い違いがあれば件数と seed を報告する |
| 4 | 止め絵の中の着差の言葉が、着順ボードの言葉と一致すること |

---

## 4. 次の指示書の範囲に入れるもの（予告）

レビュー側が指示書を出すまで、開発側は再照会 §3 のとおり該当コードに触らないでください。

1. `FINISH_CAMERA_BY_DEVELOPMENT` を、状態から決める形へ置き換える（§1。**オーナーの確認 §1-4 の後**）
2. `broadcast-v2.ts:2621-2626` の註記の表を、定数と一致させる
3. 勢いのバーは残り 200m から・過去の窓から（§2）
4. 写真判定の止め絵は (c)（§3-1）、着差の言葉は `marginLabel` から（§3-2）
5. 実況の着差の言葉をボードに揃えるか（§3-2・オーナー判断）

## 5. オーナーに確認していただくこと

| # | 確認 | レビュー側の推奨 |
|---|---|---|
| 1 | 09-12 の指示「追い込み馬がある時は引く」を、**状態（後ろから伸びてくる馬がいる時）**として読んでよいか（まとめ O-7） | よい |
| 2 | 勢いのバーの定義「残り 200m から」は、`climax=on` を既定にしないことが前提。O-3 とセットで決める | O-3 の決定を先に |
| 3 | 実況の着差の言葉を、着順ボードの言葉に揃えるか | 揃える |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
