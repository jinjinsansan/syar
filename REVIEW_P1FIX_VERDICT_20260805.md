# P1-fix レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-05（レビュー側 Claude）
**対象**: `REPORT_P1FIX_20260805.md`（commit `ef11b4c`）
**判定**: **CONDITIONAL PASS**（照会 Q7〜Q10 に回答・次便 P1-fix2 を発行）

> ⚠️ **一次回答は PASS だったが、コード監査で「§3-3 の因果の帰属が成立しない」「新設定数がゼロ防御」「頭数分布が正典どおりでない」が出たため改めた。§8 を参照。**

---

## 0. 結論

| 項目 | 判定 |
|---|---|
| テスト237件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| K-5 モンテカルロ | **CONFIRMED** — 10万レース×4シードで V-4 33.3% / V-8 94.79% ほか全PASS |
| 既定 proxy の verify | **CONFIRMED** — P0-fix5 と全数値一致 |
| 実レース選抜（prizePerStart 既定） | **CONFIRMED** — P0 ゲート全PASS（V-1 15.46% / 15.15%） |
| O-1〜O-7 | **全件対応**（§2） |
| 変異試験12件の防御 | **CONFIRMED**（§2-1） |
| ★レビュー側の新規発見 | **V-6 の頭数別の裾**（§4）— P2 に直結 |

---

## 1. まず、着手の順序が正しかった

指示書の閉じに「作ったものより先に、作ったものが効いているかを確認する順序で」と書きましたが、**最初に変異試験ハーネスを作り、指摘5件が未防御であることを自分の側で再現してから着手した**という進め方は、その意図をそのまま実行したものです。

報告書 §4 の表で「対応前=★全緑／対応後=落ちた」が並んでいることが、**是正が本物である証拠そのもの**になっています。これは「テストを書きました」という報告より遥かに強い。

**ハーネス自身に5つの事後条件（アンカーが1件・置換で文字列が変化・ディスク上の内容が原本と異なる・復元後 md5 一致・改変なしで全緑）を持たせた**のも R-11 の正しい実装です。特に5番目（ベースライン全緑）は、あなたが §8 で自己申告した失敗（**ベースラインが既に赤の状態で「落ちた＝守られている」と読んだ**）への直接の手当てで、

> R-11 は出力だけでなく**前提条件**にも要る

という総括は、R-11 の定義を一段正確にしました。**正典の恒久ルールを更新します。**

---

## 2. O-1〜O-7 への評価

### O-3（憲法）— 非有限値を 1 に倒す判断が正しい

> NaN を素通しすると `finalScore` が NaN になり、比較がすべて false になって「**着順が入力順のまま**」という静かな破綻を生む

この理由づけが的確です。NaN は例外で落ちるより**静かに順序を壊す**ほうが危険で、そこを見抜いています。**クランプを黙って行わず `capViolations` に記録**するのも指示どおり（不正の兆候を消さない）。

### O-1（§8.8）— 「`replayRace` が `resolveRace` を呼ぶこと自体を要件にした」が本質

> 別実装だと本番とずれても気づけない

そのとおりです。再現機構を別実装で書くと、**再現側と本番側が両方間違っている場合に一致してしまう**。判定にサーバー受信時刻のみを使う（クライアント申告は詐称で有利になる）判断も §8b.4 の趣旨どおりです。

### O-2 — 真因の特定と、その先の正直さ

**タイブレークが真因**（0勝で並んだ馬を枠順で割っていたため「最低人気」が実際には「最も外枠の馬」だった）という特定は、私の指摘より一段深い。私は「試行回数を増やすと値が動く」としか言えていませんでした。

`popularity.ts` に切り出して経路テスト8本を掛けた対処も適切です（`verify-race.ts` に埋め込んだままでは1本も掛かっていなかった）。

そして **§3-3 の申告の姿勢は評価します**（※ただし**因果の帰属は誤りでした**。§8-1 で訂正）:

> 測定の是正は V-6 だけでなく **V-4 も真の値へ押し上げていた**（32.5% → 34.2%）。つまり **D-016 で正典化された K=0.26 は、偏った推定器の下で求めた値**だった。K だけ据え置いて測定を直すと V-4 は上限に張り付く

**これは自分の過去の成果物を否定する報告で、書かなくても分かりませんでした。** 較正値が「偶然通っている」のか「根拠があって通っている」のかを区別した点も含めて、R-7 の実践として正確です。K=0.28 の感度データを添えたことで、**0.26 が崖の上に立っていないこと**も確認できました。

### O-5 — レビュー側の数値を引き写さず自分で測った

これも正しい姿勢です。そして **`winRate` が能力とほぼ無相関（r = −0.003）** という発見は、私も予期していませんでした。

理由も納得できます。**馬はクラス帯で能力別に振り分けられるので、どの帯でも「同格の中での勝率」しか出ない** — 強い馬は強い帯で走るため、絶対能力が上がっても勝率は上がらない。これは実際の競馬のクラス制度そのものです。

> **設計上の含意**（正典 §10.4 に D-020 として追記済み。※本書の初稿では「追記済み」と書きながら実際には未追記でした — レビュー側の誤りとして訂正します）: このゲームでプレイヤーの育成努力が報われる形は「**勝率が上がる**」ではなく「**より上のクラスで走れるようになる**」です。§1.1 の「世代を重ねるほど強くなる実感」は**クラス昇級**として現れます。UI（§12）と賞金設計（§11）はこれを可視化する必要があります。

`prizePerStart` を既定にした判断も実測（能力相関 0.639 / 在籍年数相関 −0.007）に基づいており妥当です。

---

## 3. 照会への回答

### Q7 `heavy_aptitude` の genotype 追加（D-015）— **次便で実装**

O-リストのスコープ外という理解は正しい（私が O に入れませんでした）。**次便 P1-fix2 で実装**してください。

**P1.5（NPC世界の50世代プリシード）より前にやる必要があります** — genotype が変わると**プリシードした血統プールを作り直す**ことになるためです。

### Q8 スタミナ消費のレース時間比例化（D-017）— **次便で実装。基準消費量は較正**

`基準消費量` の値が正典にないのはそのとおりで、**目標の挙動を指定するので較正してください**（R-7 に従い条件を明記）:

- 「中団・仕掛けなし」で走った**典型的な馬（初期値75前後）がゴール時に2割程度残す**
- **仕掛けを早く始めると（残り900m超）ゴール前に尽きる** — 早仕掛けの罰が到達可能な選択肢として機能する
- 距離が変わっても同じ関係が保たれる

現状は §8b.2 の数値では全馬が必ずゲージ0になるため、**V-8/V-9 は「介入の巧拙が出ない状態」で測った値**です。是正後に再測定が要ります。

### Q9 `verify.ts` に D-019（V-2d/V-2f 分割）が未反映 — **次便で反映**

「改訂前の厳しい基準で測って PASS しているので結果は過大ではない」という説明は正確で、**この順序（厳しいまま通す）は安全side**です。ただしラベルが古いままだと次に読む人が誤解するので、次便で合わせてください。

### Q10 V-9b の監視値 — **報告のみで正しい**

1.0427 は D-014 の設計どおりです（介入した馬の期待倍率は片側正のボーナスにより必ず 1.00 を超える）。時系列で見る値なので、**基準線として本便の値を記録**しておいてください。

---

## 4. ★レビュー側の新規発見 — V-6 は頭数別に見ると裾が死んでいる

V-6（0.87%）は各レースの最下位人気を**出走頭数8〜18でプールした平均**です。ところが人気別の表を見ると:

```
11番人気 0.85% → 12番 0.57% → 13番 0.41% → 14番 0.26%
→ 15番 0.31% → 16番 0.11% → 17番 0.08% → 18番人気 0%
```

**18頭立てでは最下位人気が一度も勝っていません**（10万レース中）。プールされた 0.87% は、**少頭数レースが支えている**値です。

### なぜ問題か — P2（馬券）に直結します

正典 §13.2 の V-6 の趣旨は「**ゼロではない＝夢は残す**」です。それが**最も頭数の多いレース（＝重賞・目玉レース）で成立していません**。

そして P2 では:

- §9.4 が三連単の配当上限を **100,000倍**まで見込んでいます。裾が死んでいると**万馬券が構造的に発生しません**
- 馬券ループは「滞在時間の実体」（§1.3）であり、**大穴の存在は最大のフック**です

つまりこれは検証基準の合否の話ではなく、**P2 の商品性に効く設計上の問題**です。

### 次便でやること

1. **勝率を出走頭数別に分解して報告**（V-6 をプール値だけで見ない）
2. 頭数によらず裾が生きる設定を探す。`FIELD_STRENGTH_FLOOR = 0.78` は**頭数に関わらず一定**ですが、18頭に同じ床を適用すると1頭あたりの差が詰まりすぎている可能性があります（**頭数に応じた床**を検討）
3. **出走頭数の分布自体が V-6 の値を決める自由変数**です（R-12）。分布を正典に固定するか、頭数別のゲートにするか

---

## 5. 恒久ルールの更新

| # | 更新内容 |
|---|---|
| **R-11（改訂）** | 検証ツールには**出力の事後条件だけでなく前提条件（ベースラインが健全であること）も持たせる**。「落ちた＝守られている」は、ベースラインが全緑のときだけ成立する | 開発側の §8 自己申告 |

---

## 6. 次便 — P1-fix2

| # | 内容 |
|---|---|
| **P-1** | `heavy_aptitude` を genotype に追加（D-015）。**P1.5 のプリシードより前**に |
| **P-2** | スタミナ消費のレース時間比例化（D-017）＋ 基準消費量の較正。**是正後に V-8/V-9 を再測定** |
| **P-3** | `verify.ts` に D-019（V-2d/V-2f 分割）を反映 |
| **P-4** | **V-6 を出走頭数別に分解**し、頭数によらず裾が生きる設定を探す（§4） |
| **P-5** | 正典 §10 に「**プレイヤーの成長はクラス昇級として現れる**」という設計上の含意を記録（レビュー側で正典に追記済み。実装側は §11/§12 の設計時に参照すること） |

---

## 7. 教訓の記録

1. **測定を直すと、その測定で較正した定数の根拠も揺らぐ**。V-6 の是正が V-4 を動かし、K=0.26 の導出根拠が事後的に無効になった。**較正値は「どの測定で出したか」とセットでないと意味を持たない**（R-7 の一段深い形）
2. **プールした統計は、層別すると別の顔を見せる**。V-6 は全体で 0.87% でも 18頭立てでは 0%。**ゲートに使う統計は、それが何を平均しているかを確認する**
3. **クラス制のゲームでは勝率が能力を測らない**（r = −0.003）。同格同士で走らせる設計の必然的な帰結であり、**選抜指標にも UI の見せ方にも効く**

---

## 8. 【追補】コード監査の結果 — 判定を CONDITIONAL PASS に改める

監査は **O-3（憲法のクランプ）を独立測定で追認**（`mult=1e9` を能力400の劣勢馬に与えても勝率2.6%＝支配されない）、**O-1 の `replayRace` が `resolveRace` を呼ぶこと・判定がサーバー受信時刻のみであること**、**O-5 の相関値の完全一致**、**既定 proxy の verify が P0-fix5 と一字一句一致**、**変異ハーネスの事前検査が実際に発火すること**（ベースラインをわざと赤にすると exit 2）を確認しました。

**一方で、3件の重い問題が出ました。**

### 8-1. 🔴 §3-3 の因果の帰属が成立しない — そして**私はそれを検証せずに称賛した**

監査が4条件の変異試験（12000〜16000レース・2シード）で分解しました:

| 条件 | V-4 | V-6 |
|---|---|---|
| A 現状（締める＋平均着順）20世代/200試行 | **34.16% FAIL** | 0.75% |
| B 現状＋タイブレークを枠順に戻す | 34.12% | 0.90% |
| C 締めを外す＋平均着順 | 34.12% | **0.24% FAIL** |
| R2 現状 20世代/**60試行** | 32.97% | 0.81% |
| R1 現状 **40世代**/200試行 | **33.41% PASS** | 0.78% |

読み取れること:

1. **タイブレークが V-4 に与えた寄与は +0.04〜0.21pp** — 報告書の **+1.7pp ではありません**
2. **V-4 を動かしたのは試行回数**（60→200 で **+1.19pp**）。報告書は**試行回数引き上げの効果をタイブレーク是正の効果として記述**しています
3. したがって「**K=0.26 は偏った推定器の下で求めた値だった**」という結論は、示された形では立ちません
4. **能力幅の締めは V-4 をほとんど動かしていません**（C 34.12 → A 34.16）。「通ったのは能力幅を締めた効果」も支持されません。ただし **V-6 には効いています**（0.24%→0.75%）— ここは報告どおり
5. **★`--pool-generations` が判定を決めています**: 20世代で 34.16%（**FAIL**）、40世代で 33.41%（PASS）

**つまり R-12 は V-6 については達成、V-4 については未達です。** V-4 は上限34%に対し余裕 0.6pp しかなく、**2つの自由変数（試行回数・プール世代数）がそれぞれ約1pp 動かします**。しかも `--json` の `settings` にどちらも記録されていません。

> **★レビュー側の非**: 私は §3-3 を「今回いちばん価値がある」「自分の過去の成果物を否定する報告」と評価しましたが、**因果分解を自分で検証していませんでした**。開示の姿勢は正しく、結論の方向（較正条件が揺らいでいる）も結果的に正しいのですが、**帰属先が違いました**。**「筋の通った説明」を検証の代わりにしたのは、L 便の棚卸しで同じ誤りをしたのと同型で、これが3度目です。**

### 8-2. 🔴 O-4 で塞いだはずの構造が、新設定数でそのまま再発

監査が追加の変異を入れました:

| 追加変異 | 結果 |
|---|---|
| `FIELD_STRENGTH_FLOOR` 0.78 → 0.0 | **★全緑** |
| `DISTANCE_SUIT_MIN` 55 → 0 | **★全緑** |
| `OFF_DISTANCE_ENTRY_RATE` 0.12 → 1.0 | **★全緑** |
| `POPULARITY_TRIALS` 200 → 3 | **★全緑** |

**報告書 §9-1 自身が「V-4/V-6 の両立は `FIELD_STRENGTH_FLOOR = 0.78` に依存する」と書いています。その最も負荷のかかる定数がゼロ防御でした。**

O-4 は「指摘された5件」を塞ぎましたが、**同じ便で新たに導入した定数には同じ防御が付いていません**。変異集合が手書きである以上、これは構造的に起きます。

### 8-3. 🔴 正典 §10.4 の出走頭数分布が実効的に守られていない（未申告）

`race-field.ts` の「多めに作ってから絞る」が**実際には起きていません**（`while (picked.length < fieldSize)` により oversample = fieldSize）。結果、`FIELD_STRENGTH_FLOOR` のフィルタが**頭数を直接削ります**。実測（3000レース）:

```
8頭 21.4% / 9頭 18.6% / 10頭 14.7% … 18頭 3.4%   平均 10.97頭
（正典 §10.4「8〜18頭」を一様に引くなら各9.1%・平均13.00）
```

**頭数が減れば1頭あたりの勝率は機械的に上がる**ため、**V-4/V-6 の較正はこの意図せぬ副作用の上に乗っています**。

そして**これは私が §4 で見つけた「18頭立てでは裾が死んでいる」の原因でもあります** — 18頭が全体の3.4%しかなく、しかも「床フィルタに削られなかった＝もともと能力差が狭い」レースに偏っているためです。

### 8-4. その他（中〜低）

| # | 内容 |
|---|---|
| A | `coefficients.ts:26` の `clamp` が **NaN を黙って (min+max)/2 に置換**（記録なし）。`clampInterventionMult` が「黙って直さず記録する」方針なのと**逆向き** |
| B | `capViolations` が本番のどこからも読まれていない（§8b.7/§8b.8 の監視に未接続） |
| C | `replay.ts:93` が `serverMs` 同値のとき**配列順に依存**。現在は `positionOf()` が常に `'middle'` を返すため観測されず、「並び順に依存しない」テストは**空振りで緑** |
| D | `--json` の `settings` に `poolGenerations` / `poolMares` / `FIELD_STRENGTH_FLOOR` / `DISTANCE_SUIT_MIN` が無い（R-12 の記録漏れ） |
| E | 変異ハーネスが `--reporter dot` を使うのに落ちたテスト名の抽出は `×` 行前提で、**テスト名が一度も出力されない**（12件すべて名前ゼロ） |
| F | `racing-season.test.ts` の網羅ループに `prizePerStart` が無い／`simulator.ts` のドキュメントにも欠落 |

### 8-5. 判定

O-1/O-3/O-5/O-6/O-7 と変異ハーネスの信頼性は**成立**。しかし **O-2 の結論の根拠が誤り**、**O-4 の構造が同便で再発**、**頭数分布が正典どおりでない**ため、**CONDITIONAL PASS** とします。是正は P1-fix2 に Q-1〜Q-6 として追加します。

---

## 9. レビュー側の恒久ルール（自分に課す）

| # | 内容 |
|---|---|
| **R-13** | **回答書では「レビュー側が再現した事実」と「開発側の主張（未検証）」をラベルで分ける。** 検証していない主張を評価の根拠にしない。§3-3 を検証せず称賛したのが3度目（L便の棚卸し・L-4 の検閲範囲に続く） |