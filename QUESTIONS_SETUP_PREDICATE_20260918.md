# 照会（初期馬の付与 — 裁定 Q-SETUP-03 ① の前提が成り立っていません）— 2026-09-18

**宛先**: レビュー側
**発行**: 開発側
**関連**: 裁定 `REVIEW_SETUP_RPC_VERDICT_20260820.md` §3-① ／ D-079 ⑥ ／ 正典 §10.3 ／ V-4・V-5・V-6・V-18
**なぜ今か**: 裁定 §6 手順 6（実装）でセットアップ RPC に着手したところ、**裁定が「既にあるはず」とした述語が存在せず、正典にも定義がありませんでした**
**未裁定**: **Q-SETUP-05**

---

## 0. 結論

裁定 §3-① はこう書いています。

> **番組表が新馬戦の出走資格を判定するのに使っている述語が、既にあるはずです**（§10.3）。**それをそのまま使ってください。**
> 無ければ**先にそれを 1 か所に切り出してから**、両方がそれを呼ぶ形にしてください。

★**述語は存在しません。そして正典 §10.3 にも定義がありません。**
したがって「切り出す」は既存の定義を移す作業ではなく、**新しい定義を作る**ことになります。
それは **1 日 42 本の新馬戦の出走馬を変え、V-4・V-5・V-6・V-18 に波及します**。

**開発側の判断では決められないため、照会します。**

---

## 1. 実測（何を見て「無い」と言っているか）

**名前で探さず、経路をたどって確かめました**（D-108 ③「禁止語の一覧で書かない」）。

| # | 場所 | 何をしているか |
|---|---|---|
| 1 | `apps/worker/src/horse-repo.ts:68` `loadRaceablePool` | `where generation >= (select max(generation) - 2 from horses)` ★**世代の新しさだけ**。引退馬も他人の所有馬も除いていません |
| 2 | `apps/cli/src/race-field.ts:167` `sortPoolByClass` | `potential` の合計で**並べ替えるだけ** |
| 3 | `apps/cli/src/race-field.ts:330` `generateRace` | 能力順の窓（`classBand`・`OVERSAMPLE_RATIO`）から `fieldSize` 頭を引く。**資格の判定なし** |
| 4 | `apps/worker/src/cycle-runner.ts:145` `runCycle` | `build: (cycleIndex) => {...}` を受け取るだけで、**`pool` を見ません** |
| 5 | `apps/worker/src/main.ts:163` | `(i) => buildRace(pool, i, ...)` — ★**全レースに同じ `pool` を渡しています** |

**`classOf(i)` の使われ方**（`main.ts:163`・`cycle-runner.ts:257`）:
- `conditionsOf(i, classOf(i), gradeOf(i))` → **距離・馬場・コースを決める**
- `purseOf(prizeTierOf(classOf(i), gradeOf(i)))` → **賞金の段を決める**
- `raceClass: classOf(idx)` → **レコードのラベルとして保存**

→ ★**クラスは「レース」に付く属性で、「どの馬が出られるか」には一切使われていません。**

### 正典 §10.3 にも条件がありません

§10.3 のクラス表は**本数だけ**です。

| クラス | R数/日 | 備考 |
|---|---|---|
| 新馬・未勝利 | 42 | デビュー直後の受け皿。常時空きを確保 |

「デビュー直後の受け皿」とありますが、**出走資格の条件は書かれていません。**

---

## 2. 🔴 裁定が恐れた失敗は、現状では起きません

裁定 §3-① の理由はこうでした。

> 「初期馬の候補」と「新馬戦に出られる馬」が別々に定義されると、**付与された馬が新馬戦に出られない**という噛み合わせ事故が、いつか静かに起きます。

★**いまは全クラスが同じ窓から引くので、戦績 0 の馬は当然そこに入ります。** 締め出される経路がありません。

→ **「切り出さないと事故が起きる」という前提も、現時点では成立していません。**
（★**将来クラス分けを実装したときには成立します。** その便で必要になります。）

---

## 3. 🔴 切り出して「繋ぐ」と何が動くか

**新馬戦を戦績 0 の馬だけに絞ると、1 日 42 本（全 144 本の 29%）の出走馬が変わります。**

| ゲート | なぜ動くか |
|---|---|
| **V-4**（1番人気勝率） | 出走馬の能力分布が変わる |
| **V-5・V-6**（万馬券率・裾） | 同上。★V-6 は少頭数で既に上限を 0.38pt 超えていると記録されています |
| **V-18**（枠順・距離ロス） | 頭数構成が変われば取り直しが要る |

★**2026-09-16 に 10 場配線で V-4〜V-6 を測り直したばかり**です（`REVIEW_VENUE_WIRING_FINAL_VERDICT_20260916.md`）。
**認証の便（D-113）でこれを動かすのは、範囲を大きく超えます。**

---

## 4. 🔴 Q-SETUP-05 — どうしますか

| | 案 | 内容 | 開発側の見立て |
|---|---|---|---|
| **(a)** | **付与の側だけで定義し、繋ぐのは別便** | 初期馬の候補を「NPC 世界・未引退・戦績 0」と RPC 内で定義する。番組表は**今のまま**（全クラス共通の窓） | ★**推します。** 事故は現状起きず（§2）、ゲートが動かない。★**ただし裁定 §3-① の字面には反します** |
| **(b)** | **先に切り出して両方を繋ぐ**（裁定の字面どおり） | 述語を 1 か所に定義し、番組表もそれを呼ぶ | ★**V-4〜V-6・V-18 の取り直しが要ります**。認証の便には収まりません |
| **(c)** | **クラス分けの便を立てて、そこで両方やる** | 新馬・1 勝・2 勝…の出走資格をまとめて実装し、ゲートを 1 回で取り直す | ★**筋は最も通ります**が、初期馬の付与＝ログインの完成が**その便を待つ**ことになります |

**★(a) を採る場合の条件**（開発側から先に申し出ます）:
- 付与の RPC に「★この定義は暫定であり、クラス分けの便で共通の述語に置き換える」と**註記を残す**
- **正典に、その置き換えが必要であることを残す**（忘れると (b) の事故が将来起きます）

---

## 5. 決まっていること（照会の対象外・確認のため列挙）

裁定 §7「これで書けます」のとおり、**残りは揃っています。**

| | 実測で確かめたこと |
|---|---|
| **帯の量** | `packages/sim-engine/src/stars.ts` の `starsOfPotential`（`potentialMean` → `STAR_THRESHOLDS = [420,470,520,570,620,670,720,800]` → 1.0〜5.0★）。★**`sameStarBand()` が既にあり、「★が同じ」が帯の定義**と註記されています |
| **上のゲート** | ★段をひとつ選べば「★表示が全個体同一」は**構造的に満たされます** |
| **下のゲート** | `npm run verify:v14 -- --horses 400 --seed 42` で測る（裁定 §2-③） |
| **★の二重管理の回避** | ★は **TypeScript で算出し、SQL では再計算しません**。`market-flow.ts:115` がワーカーで `starsOfPotential` を計算して `horse_market_listing.stars` に**数値を書いています**。付与も同じ形にします |
| **候補の SQL** | `market-flow.ts:60` に既にあります: `owner_id is null and npc_stable_id is not null and retired_at_week is null` |
| **戦績 0 の書き方** | `not exists (select 1 from race_entries where horse_id = h.id and finish_pos is not null)`。★**`finish_pos` は確定時にだけ書かれます**（`pg-store.ts:459`）。★**`race_entries` の行の有無で判定してはいけません** — 登録時に書かれ（`0024:174`）、取消もある（`0028`）ので、**登録しただけの馬・取消された馬を誤って除きます** |
| **所有権の移し方** | `0025`（購入）・`0026`（売却）の形。`perform assert_setup_complete()` → `dedupe_key` で冪等 → `update horses set owner_id = ..., npc_stable_id = null` → `insert into ep_ledger` |
| **在庫の監視** | `marketStockAlert`（`MARKET_STOCK_MIN = 200`）。★「黙って帯を広げない」も実装済み（D-102 ⑤） |
| **登録簿** | テーブルは `PUBLIC_VIEW` / `OWNER_SCOPED` / `CLOSED`、関数は `regprocedure` をキーに `{anon, authenticated}`。★**新しい関数を足すなら登録しないと V-20 ④ が落ちます** |

★**セットアップ RPC は `assert_setup_complete()` を呼べません** — あれは `users` 行の存在を要求しますが、
このRPC は**その行を作る側**です。**順序が逆**なので、D-080 の適用対象外として扱います（★裁定 §4 の「利用者が呼ぶ RPC」の例外。**明示的に確認させてください**）。

---

## 6. 開発側が次にすること

**Q-SETUP-05 の裁定を待ちます。** (a) なら即日書けます。(b)(c) は別便の立ち上げが要ります。

**待つ間に進めるもの**: 帯の下のゲート（キャリア中に 1 勝）の測定 — `verify:v14` の所要を実測して、
このセッションで流すかオーナーの別端末に回すかを決めます。
