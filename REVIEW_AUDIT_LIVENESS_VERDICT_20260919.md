# 裁定 — `AUDIT-CANON-0.26` を取り下げ／`RACE_RANDOM_K: 0.12` を消す／AU-2／CI-9 の直しを受理

**日付**: 2026-09-19（夜）
**対象**: `AUDIT-RANDOM-K`・`AUDIT-CANON-0.26`・CI-9 の直し
**発行**: レビュー側

---

## 1. 🔴 `AUDIT-CANON-0.26` は成立しません — 取り下げ

✔ 確かめました:

| | 値 |
|---|---|
| **正典 §13.1**（`STAR_SPEC_v2.0.md:1560`） | ★**0.22** |
| コード（`packages/race-engine/src/balance.ts:462`） | ★**0.22** |

→ ★**一致しています。食い違いはありません。**

★開発側が 0.26 を見たのは **D-016 の行**。★それは**経緯の記録**で、
★**D-021 が 0.26 → 0.22 に再較正**し、★**D-053 が「`RACE_RANDOM_K = 0.22` は再較正不要。
記録のほうを改める（§13.1）」と裁定済み**です。

### 🔴 AU-2 — ★AU-1 の正典側の双子

> **AU-2** ★正典の値を引くときは、★**§ の表**を読む。
> ★決定表（D-xxx）の行は**経緯**であって現行仕様ではない（**R-19**）。
> ★「D-016 が 0.26 と書いている」は「★いま 0.26 である」ではない。

⚠️ ★**今日これで 3 件目**です（M-4・M-8・`AUDIT-CANON-0.26`）。
★**2026-09-14 の監査は、生きているかを確かめずに書かれています。**
→ ★残り 10 件にも **AU-1 ＋ AU-2 を両方**当てること。

---

## 2. 🔴 `sim-engine/balance.ts:78` の `RACE_RANDOM_K: 0.12` — ★消します

開発側の推し（★註記を足して残す）は**採りません**。

| | 理由 |
|---|---|
| ① | ★**註記で閉じない**（**NT-2**）。★註記は期限も担当も持ちません。★開発側自身が同じ便で謝った形です |
| ② | ★**この写経は既に写経として失敗している** — §13.1 は **0.22**、写しは **0.12**。★**合っていない写しは、無い写しより悪い**（権威ありげに見えて値が違う） |
| ③ | ✔ **読み手 0**。★AU-1 の問い「何の挙動が変わるか」の答えが「**何も**」。★消して何も起きないなら、消すのが **D-052** |

⚠️ ★**導かせる（import）は採れません** — ✔ 依存は `race-engine → sim-engine` の向きで、
★逆向きに読むと循環します。

→ ★生きた台帳は `CALIBRATED_RACE_RANDOM_K` **1 つだけ**になり、
  ★そこは較正の登録簿（`apps/cli/src/calibration.ts:258`）と **S-1** が既に守っています。

⚠️ ★消すときに「なぜ消したか」を **1 行だけ**残すこと
（「§13.1 は 0.22。写しが 0.12 のままで、読み手が 0 だった。生きているのは `CALIBRATED_RACE_RANDOM_K`」）。
★**次の人が「P1 用の定数が抜けている」と思って戻さないように。**

---

## 3. ✅ CI-9 の直しを受理

✔ **両方の条件で 8 項目すべて ✅**:

| 生成物を退避（★CI と同じ） | 検査 2,481 件・赤 0 件・登録 2 件 → ✅ |
|---|---|
| 生成物あり（手元） | 検査 2,502 件・赤 2 件・登録 2 件 → ✅ |

→ ★★**手元と CI が、初めて同じ答えを出します。**

✅ ★開発側の自己分析が正確です — ★**「まだ残っている機械差」という書き方が、
期限も担当も持たない註記そのものだった**。★**同じ便の中で NT-2 をやっていた**という見立ても正しい。
★私はそこまでは書きませんでした。

⚠️ 1 点: ★**件数が 25 → 21 に変わっています**（検査自体が 2,479 → 2,502 に増えたため）。
→ ★**「同じ 25 件」と思わないこと。** ★名前で見る形（CI-8）にしてあるので、
  ★次に差が動いたら**名前で照らす**こと。

✅ 「★黙って消えていた 25 件が、宣言された 21 件になりました」—
★**数が減ったことではなく、名前と期限が付いたことが成果**です。

---

## 4. ✅ そのほか

- ✅ `machineExcuses` の註記に「★ここが拾うのは**うっかり**だけで、★**判断は人が下す**」と
  書いたのが正しい。★道具の守備範囲を道具自身に書かせるのは良い作法。
- ✅ `source-slicing-guard` が開発側の `growth-tell-wiring.test.ts` を止めた —
  ★`indexOf` が −1 → `slice(0, -1)` ＝ ほぼ全文、★しかも否定の検査（CK-4 の形）。
  → ★**CK-3 と CK-4 が、作られた直後に本物を 1 件 捕まえました。** ★道具が元を取っています。
- ✅ **SC-1** を受け取った整理（★自分を数えると常に 1 以上になり「在る」側へ倒れる）が正確。

---

## 順

1. 🔴 `AUDIT-CANON-0.26` を取り下げ（簿から外す・理由は §1）
2. 🔴 `sim-engine/balance.ts:78` を消す（＋消した理由を 1 行）
3. 🔴 **AU-1 ＋ AU-2 を残り 10 件に**
4. `SERVICE_ROLE_KEY` → `.tmp-*`

🔴 **オーナー待ち**: CI-1 ／ CC-1 ＋ PO-2 ／ WK-5 ／ VP-4' ／ PR-3 ／ 撮り直し 2 本（期限 2026-09-26）／ 1.68 GB
