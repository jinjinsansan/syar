# REPORT_P3_B6REVERT_20260812 — 4つ目の状態の測定・B-6 の差し戻し・Q-P3-36

## 本便で正典の改訂が要る決定をしたか

**していません。**

---

## 0. ★まず、前便の記述を訂正します

前便 §3 でこう書きました:

| | ハーネス | 本番（ワーカー） |
|---|---|---|
| 調子・疲労 | `--b6-wired` | **配線済み** |

**「配線済み」は*コード上*の話でした。** レビュー側は「**本番に入っている**」と読まれ、
判定にも「B-6 は既に本番に入っています」と書かれています。**こちらの書き方が不正確でした。**

### 実際の配備状況（実測）

```
$ ssh ... "readlink -f /opt/star-current; systemctl is-active star-worker"
/opt/star-releases/ca575f93c18da8ed2f3459f2535d88d15329e777
active
```

**VPS は `ca575f9`（P2 期）で動いています。** P3 の変更は**1つも配備されていません**。

→ **B-6 は一度も本番で動いていないので、運用上の影響はありません。**
→ ★ただし **VPS には Q-P3-32 の欠陥（オッズが別条件のレースのもの）が残っています**（§4）。

---

## 1. ★最優先の測定 — 帯を出ていました

**`--b6-wired` 単独**（＝コード上の本番の状態、4つ目の組み合わせ）:

| | ① 現状 | **★①＋B-6** | ② 能力のみ | ③ 両方 | 帯 |
|---|---|---|---|---|---|
| **V-4** | 31.29% ✓ | **34.60% ✗** | 29.20% ✗ | 32.73% ✓ | 30〜34% |
| **V-5** | 62.40% ✓ | **65.83% ✗** | 59.78% ✗ | 63.99% ✓ | 60〜65% |
| V-6 | 1.21% ✓ | 1.06% ✓ | 1.09% ✓ | 0.97% ✓ | 0.5〜2% |
| V-8 | 94.57% ✓ | 93.52% ✓ | 95.15% ✓ | 93.88% ✓ | 93〜97% |
| V-13 | 0.1999 ✓ | 0.1697 ✓ | 0.2000 ✓ | 0.1766 ✓ | 有意差 |
| **総合** | PASS | **FAIL** | FAIL | **PASS** | |

**レビュー側の予測（34.8%）とほぼ一致しました**（実測 34.60%）。
生出力: `docs/MEASURE_P1GATES_S4_B6ONLY.txt`

★**4つの組み合わせのうち、帯に入るのは①と③だけ**です。片方だけ入れるとどちらも落ちます。

---

## 2. 指示どおり戻しました

| 場所 | 戻した内容 |
|---|---|
| `main.ts` | `loadTrainingStates` を呼ぶのをやめた |
| `pg-store.ts` `settleRace` | `condition: 3 / fatigue: 0` に戻した |

★**両側を揃えて戻しました。** 生成側だけ実データにすると
「オッズを計算した馬と実際に走る馬が違う」が再発します（Q-P3-32 と同じ型）。

★**受け口は残してあります**（`loadTrainingStates` / `buildRace` の引数）。
消すと「繋ぎ直す」が作り直しになり、**そのとき別物になります**。
戻し方をコード内のコメントに書いてあります。

★**Q-P3-32 の是正（レース条件）は有効のまま**です。あちらは P1 のゲートを1つも動かしません（①が完全再現）。

### 確認

- テスト **602件 緑**
- 原本とバンドルの自己検査が**バイト単位で一致**
- 呼び出し箇所を grep で確認（`loadTrainingStates` の呼び出しは 0 / 残っているのはコメント2件）

---

## 3. Q-P3-36 — ★測る量を変えたら即座に出ました

指摘のとおり、**測っていた量が V-4 に対応していませんでした。**

`apps/cli/src/diag-topgap.ts`（600レース・seed 42）:

| | ①最強と2位の差 | ②上位3頭の SD | ③下位の裾 | 参考: 全体 CV |
|---|---|---|---|---|
| ① 仮定値（0.55〜0.85） | **3.04%** | 3.12% | 18.31% | 11.55% |
| ② 実データの能力 | **2.14%** | 2.26% | **23.42%** | 12.46% |
| **差** | **−0.90pt** | −0.86pt | **+5.11pt** | +0.91pt |

> **全体 CV が上がったのは、下位の裾が +5.11pt 伸びたからでした。**
> **上位の分離（最強と2位の差）は 3.04% → 2.14% と落ちています。**
> ★**これが V-4 の低下そのもの**です。仮説は要りませんでした。

**「分散が増えたのに V-4 が下がる」は矛盾ではなく、こちらが V-4 と無関係な量を見ていただけ**でした。

---

## 4. ★VPS に残っている欠陥（判断をお願いします・Q-P3-37）

VPS は `ca575f9` なので、**Q-P3-32 の欠陥が残ったまま動いています**:

- オッズは `generateRace` が自分で引いた条件（芝/ダート・距離・馬場）で計算
- DB には `conditionsOf` の条件が保存され、**確定はそちらで走る**
- `track_condition` は `'good'` 固定 → **道悪が一度も発生していない**

**確定済み 452 件がこの状態です。** 実プレイヤーはいないので実害は出ていませんが、
**動き続けるほど「オッズが別レースのもの」というレースが増えます。**

選択肢:

| # | 案 |
|---|---|
| A | **Q-P3-32 の是正だけを配備する**（B-6 は戻したので、①の状態＋条件の修正） |
| B | ワーカーを止めて、P3 の残り（週送りの接続）が済むまで待つ |
| C | そのまま動かし続ける（実プレイヤーがいないので実害なし） |

★こちらの見立ては **A** です。是正は P1 のゲートを動かさないことが実測で確かめてあり、
配備すれば**それ以降のレースは整合します**。ただし**配備はオーナー指示が要る**ので、判断を仰ぎます。

---

## 5. P3 のクローズ条件（更新後の状態）

| # | 内容 | 状態 |
|---|---|---|
| 1 | 本番の現状（①＋B-6）を測り、帯外なら戻す | ✅ **完了**（測定 → 帯外 → 差し戻し） |
| 2 | 週送りをワーカーに繋ぐ（P3 の本体） | 🔴 次 |
| 3 | 開放率がハーネスの前提に到達したことの確認 → ②③ 投入 | 🔴 |
| 4 | V-11 ② を**合成集団**で成立させる | 🔴 |
| 5 | Q-P3-36 の統計量を測り直す | ✅ **完了**（§3） |
| 6 | VPS での周時間実測 / V-14 の再測定 / SHA 併記 | 🟡 |

### ★4について（前便の実装は監視の形だけでした）

裁定のとおりです。**較正（P3 のゲート）は合成集団で②が成立することを示す**必要があり、
いまの実装は**監視（実集団）の形だけ**です。
2（週送りの接続）が済めば、合成プレイヤーに所有・調教・出走・受賞・馬券購入・払戻を
一巡させられるので、**2の後に4をまとめて出します**。

---

## 6. 再現コマンド

```bash
npm run verify:race -- --b6-wired                  # ★①＋B-6（帯外）
npx tsx apps/cli/src/diag-topgap.ts --races 600    # Q-P3-36
npm run typecheck && npx vitest run                # 602件
npm run build:worker && node dist/worker.cjs --selfcheck
```

---

## 7. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・未実施）
2. **§4 の判断**（VPS に Q-P3-32 の是正を配備するか）。配備はご指示があるまで行いません
