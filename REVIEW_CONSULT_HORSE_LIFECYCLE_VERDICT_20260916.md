# 裁定 — 馬の生涯を軸にした設計の提案（相談 `CONSULT_HORSE_LIFECYCLE_20260916.md` の Q-HL-1〜5）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側・オーナー
**対象**: 相談書 `CONSULT_HORSE_LIFECYCLE_20260916.md`（コミット `f392223`）／ オーナーの意向「受け入れるべきものは受け入れる」
**関連**: 正典 D-100〜D-106（2026-09-16 起票）／ 裁定 `REVIEW_GAME_BODY_1_VERDICT_20260916.md`

> 凡例（R-13）: **✔** ＝ レビュー側がコード・正典で確かめた ／ **△** ＝ 未確認 ／ **[EYES]** ＝ オーナーの目で決めること
> 本書は正典を編集しません。§7 の起票はオーナーの承認後です。
> ⚠️ 他社の製品名は書きません（§0.1）。相談書と同じく〔参考機 A〕〔目標の機種 B〕と書きます。

---

## 0. 判定

| # | 提案 | 判定 |
|---|---|---|
| **HL-1** | Discovery（能力の発見度） | ✅ **採用可**（§1。★開発側の心配は不要でした） |
| **HL-2** | 生涯イベントログ | ✅ **採用可**（§4） |
| **HL-3** | 馬物語帳 | ✅ **採用可**（§4・§5） |
| **HL-4** | 履歴書型の馬詳細・ホームの構図 | ✅ **デザイナーへの依頼に足す**（開発側が画面を独自に起こさない・O-8） |
| **HL-5** | 先天個性・後天特性 | ⚠️ **条件つきで可**（§2。効果 0 で入れて、効かせる便で V を取り直す） |
| **HL-6** | 騎手を介入の許容範囲に効かせる | ⚠️ **条件つきで可。ただし開発側の二択は採りません**（§3 に第三の形） |
| 相談書 §5（頭数無制限・正典の外部置換） | — | ✅ **採らない**に同意 |
| 相談書 §6（外部 AI に GitHub を接続） | — | ★**接続しない**（オーナー承認・§6） |

**仕分けそのものが的確でした。** とくに「すでに決着している 8 点」を新規と混ぜなかったこと、「着順に効くもの／効かないもの」で分けたことは、そのまま実装の順番に使えます。

---

## 1. Q-HL-1 — Discovery は V-16 ①・V-4 と噛み合うか

### 1-1. ★開発側の心配は不要です（コードで確認）

| 事実 | 出どころ |
|---|---|
| **人気は「モンテカルロの勝率順位」で、サーバーが決める** | ✔ `db/migrations/0001_init.sql:193-194`（`popularity int` ＝「モンテカルロ勝率順位（§9.2）」）・`apps/worker/src/build-race.ts:146` |
| **V-4 の人気も模型側の推定**（`POPULARITY_TRIALS`） | ✔ `apps/cli/src/verify-race.ts:314` |

→ ★**人気は「プレイヤーが何を知っているか」に依存しません。** 発見度を出走表に出しても出さなくても、**V-4（1 番人気の勝率 30〜34%）は動きません**。「持ち主だけに見せる」は良い設計ですが、**V を守るために必要な条件ではありません**。

### 1-2. 採用の条件

| # | 条件 |
|---|---|
| 1 | ★**内部値を変えない。** 発見度は**表示の層**に持ち、`stats`・`potential`・`genotype` に触らない（§5.5・§12.4） |
| 2 | ★**検査は「入力の形」で書く。** GB-1 で入れた形（引数に `potential` や「上限までの割合」を渡す口が無いことを構文木で見る）と同じに。★**禁止語の一覧で書かない**（D-098 の検査で踏んだ穴と同じ形にしない） |
| 3 | **発見の進み方は決定論**（シードとレース結果から導く）。`Date.now()` や端末の状態を読まない（憲法 4） |
| 4 | **★表示（§12.4 の 1〜5・半星）との関係を 1 か所で持つ。** 発見度と★を別々に計算しない（`stars.ts` に寄せる・D-079 ⑤ と同じ要請） |
| 5 | V-16（画面が情報を運ぶ）は**観戦の画面**の話なので、発見度を**レース中の画面に出さない**かぎり無関係。出すなら D-098（ゴール前に結果を読む値を使わない）に照らして設計する |

---

## 2. Q-HL-2 — 先天個性・後天特性（HL-5）

**✅ 入れてよい。手順は開発側の案（効果 0 で入れて、効かせる便で取り直す）でよい。**

### 2-1. ★効かせる便で取り直す V

| 特性の性質 | 取り直す |
|---|---|
| どの馬にも一様に効く（例: 大舞台経験） | V-4・V-5・V-6（人気別の勝率）・V-17（走破タイム） |
| ★**枠順・馬群に相関する**（例: **繊細＝馬群で力を出しにくい**） | 上に加えて ★**V-18 ①②a②b**（枠順と着順の相関・内外差）。**枠に相関する特性は V-18 ① に直に効きます** |
| 道悪・距離に効く（例: 泥巧者） | V-4〜V-6 ＋ **V-2f**（レース選抜がかかる形質の平坦化）。★**遺伝させるなら §5.4 の genotype に行を足す**こと（**§5.4 に行が無い形質は遺伝しない** — D-015 の再発防止） |
| 介入に効く | V-8・V-13（§3） |

### 2-2. 条件

1. ★**遺伝させるかどうかを先に決める。** 遺伝させるなら **§5.2 と §5.4 の両方**を更新する（片方だけにした D-015 の事故の再発防止）
2. **効果 0 で入れる便では、実際にエンジンを回して 1 ビット不変を示す**（GB-3 の `jockey-no-effect` と同じ形。定数が 0 なだけを根拠にしない）
3. **後天特性（経験）は、レース結果から決定論で導く**（新しい乱数を引かない・D-061 の制約）

---

## 3. Q-HL-3 — 騎手を介入の許容範囲に効かせる（HL-6）★裁定

### 3-1. 開発側の二択は、どちらも採りません

| 案 | なぜ採らないか |
|---|---|
| (a) AI 代行にも同じ幅を与える | V-8 の比は保たれるが、★**「上手い騎手を選ぶ意味」が消える**（開発側も同じ懸念） |
| (b) 手で介入する人にだけ効かせる | ★**V-8（AI 代行 ÷ 手動最適 93〜97%）が騎手ごとに変わる**。ゲートが「騎手の数だけ」になり、D-105 ⑥（AI 代行の質を騎手で変えない）とも衝突する |

### 3-2. ★第三の形（これを採ります）

| # | 中身 |
|---|---|
| 1 | ★**判定窓の広さ（タイミングの許容）は、AI 代行にも同じ幅を与える。** これで **V-8 の比は保たれ**、D-105 ⑥ も守られます |
| 2 | ★**「上手い騎手を選ぶ意味」は、窓ではなく「暴走の抑え」で出す。** 気性難（`temper` が高い）の馬の**失敗の率を下げる**方向にだけ効かせる。**AI 代行は暴走しない**ので、ここは自然に差が出ます（＝**人が操作する人ほど騎手の価値が高い**） |
| 3 | **効果の上限**: 介入の合計は `INTERVENTION_CAP`（±10%・§1.5-1）を**動かさない**。騎手は**キャップの中での成功率**に効くだけ |
| 4 | **取り直す**: **V-13**（仕掛けの巧拙が結果に出る）は必ず動くので取り直す。**V-9a**（全サンプルが 0.90〜1.10）は不変条件なので**破れていないことを確認**。V-8 は比が保たれることを**実測で示す** |
| 5 | **騎手の選択は出走登録で凍結**（D-105 ④）。★**凍結から介入の判定に渡す**（名簿を引き直さない） |

---

## 4. Q-HL-4 — 馬物語帳・生涯イベントログを正典に入れるか

**✅ 入れる。ただし §12（画面一覧）の一部ではなく、★新しい節として起票します。**

理由: §12 は「どの画面があるか」の章です。**これは「何を永久に残すか」の約束**で、性質が違います（削除しない・所有と切り離す・個人情報を持たない）。画面の側は §12 に行を足すだけで足ります。

### 4-1. 新しい節に書く約束（起票の文面案は §7）

| # | 約束 |
|---|---|
| 1 | **引退馬の記録は消さない**（所有が切れても残る） |
| 2 | ★**記録は「所有」ではない** — §6.7 の所有上限（現役 30・繁殖 10・種牡 5）に**数えない**（Q-HL-5） |
| 3 | ★**週送りの対象にしない**（§14.1-2 のバッチが頭数に比例して増えるのを防ぐ） |
| 4 | **イベントの文章は定型**で作る（AI 生成に依存しない）。**実在の人名・団体名を入れない**（§0.1） |
| 5 | ★**着順にも経済にも効かない**（読み取り専用の記録）。ゲートの取り直しは不要 |
| 6 | **他人の馬の物語も見える**（§1.4-4「共通世界の社会性」に沿う）。ただし**持ち主の個人情報は出さない**（表示名・牧場名まで） |
| 7 | **発見度（HL-1）を物語に書くのは、そのレースが終わった後**（D-098 の家族。ゴール前に結果を読む値を出さない） |

---

## 5. Q-HL-5 — 引退馬を永久に残すことと、引退・所有上限の噛み合わせ

**✅ 可。物語帳は所有上限の外。** 開発側の読みのとおりです。

| # | 条件 |
|---|---|
| 1 | §7.1 の引退（260 週で強制引退 → 種牡馬・繁殖牝馬・功労馬）は**変えない** |
| 2 | **記録は所有上限に数えない**（§6.7）。★ただし **繁殖入り・種牡馬入りした馬は「所有」なので上限に数える**（記録と所有を混ぜない） |
| 3 | ★**週送りの対象にしない。** 実装では「記録だけの馬」を週進行のクエリから外すことを**検査で固定**（頭数が増えてもバッチの所要が増えないこと） |
| 4 | **DB の量を監視**（行が増え続ける）。削除はしないので、**増え方を測って報告**する（D-079 ⑧ と同じ形の在庫の見張り） |

---

## 6. セキュリティ（相談書 §6）

★**外部の対話 AI に GitHub を接続しないでください**（オーナー承認・2026-09-16）。

- このリポジトリは**非公開**で、**正典と実装が丸ごと外部サービスに渡ります**
- 読ませたい文書があるときは、**その文書だけを貼る**
- ★**資格情報は貼らない**（`git.txt`・`secrets.*.env`・DB の接続文字列・LINE のチャネルシークレット）
- 原文（`ダービースタリオン/GPTとの会話.txt`）に**他社の製品名が含まれます**。開発側が〔参考機 A〕〔目標の機種 B〕に置き換えた扱いは正しい（§0.1）

---

## 7. 正典への起票候補（オーナーの承認後にレビュー側が起票）

| 候補 | 中身 |
|---|---|
| **D 候補: Discovery** | 能力の発見度は**表示の層だけ**に持ち、内部値（`stats`・`potential`・`genotype`）に触らない。★表示（§12.4）と同じ量から導く。発見の進み方は決定論。★**人気はモンテカルロの勝率順位なので、発見度の見せ方は V-4 に影響しない**（本書 §1-1 の実測の根拠つき） |
| **新しい節: 生涯の記録（馬物語帳・イベントログ）** | §4-1 の 7 つの約束 |
| **D 候補: 先天個性・後天特性** | 入れる手順（効果 0 → 効かせる便で取り直す）と、★**枠に相関する特性は V-18 ① に効く**こと。遺伝させるなら §5.2 と §5.4 の**両方**を更新する（D-015 の再発防止） |
| **D 候補: 騎手と介入** | 判定窓は AI 代行にも同じ幅（V-8 の比を保つ・D-105 ⑥ を守る）。**差は「暴走の抑え」で出す**。`INTERVENTION_CAP` は動かさない。V-13 を取り直す |

---

## 8. 実装の順（裁定つき）

| 便 | 中身 | ゲートの取り直し |
|---|---|---|
| **第 2 便** | HL-2 生涯イベントログ・HL-3 馬物語帳・HL-1 Discovery の見せ方 | **不要**（§1-2・§4-1 の条件を守るかぎり） |
| 第 3 便 | 出走登録・投票の RPC（★同じ便で・`REVIEW_GAME_BODY_1_VERDICT_20260916.md` §5）・購入の DB・格の配線 | 不要 |
| 第 4 便 | HL-5 個性・HL-6 騎手の介入補正を**効果 0 で**入れる | 不要（対照を取る） |
| 第 5 便 | 上の 2 つを**効かせる** | ★**必要**: V-4・V-5・V-6・V-17・V-18（①②a②b）・V-13・V-9a・V-8。遺伝させる形質を入れたなら V-2f も |

★**第 2 便は、`REVIEW_GAME_BODY_1_VERDICT_20260916.md` の条件（GC-1・GC-2 の報告の直し）と V-10 の測定の後**に着手してください。

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
