# 引き継ぎ書 — 走りの不具合と演出要望（2026-08-25 夕方）

★**次のセッションはここから読んでください。** これが現時点で最新です。

| ファイル | 何のため |
|---|---|
| **この文書** | ★**いまの続き**。オーナー指摘 5 件・直したもの・判断待ち |
| `HANDOVER_P4_2D_20260825.md` | 台本 v5 を既定にするまでの経緯・実測値・踏んだ罠 |
| `HANDOFF_P4_RACE_NEXT_SESSION_20260825.md` | レビュー側の手順・優先順位・完了条件 |
| `REPORT_P4_2D_MOTION_FIX_20260825.md` | 今回の測定と修正の報告書 |

---

## 0. いまの状態

| | |
|---|---|
| ブランチ | `p4/race-30sec-cuts` |
| HEAD | `9edc761` の 1 つ先（★**このコミット・未 push**）。`9edc761` までは push 済み |
| 通常 `/race` の既定 | 台本 **`v5`**（第4コーナー俯瞰＋直線横追従） |
| 旧映像 | `/race?cinematography=v4` |
| dev サーバー | ポート 3210 で**稼働中**（このセッションで再起動した） |
| `npm test` | 112 ファイル / **1121 件パス** |
| `npm run typecheck` | エラーなし |

### このコミットに入れたもの（★push はしていない）

```
M  packages/render/src/broadcast-v2-scene.ts               修正（下記 §2）
A  packages/render/test/contender-frame-continuity.test.ts 新規テスト 3 件
A  REPORT_P4_2D_MOTION_FIX_20260825.md                     報告書
A  HANDOVER_P4_2D_MOTION_20260825.md                       この文書
```

**push はオーナー指示待ちです。**

### 以前から未コミットで残しているもの（触らない）

- `apps/web/package.json` … `next dev -p 3210`。★どのコミットにも入れない
- `tools/lib/classification.mjs` … 登録簿の追記
- 監査報告書 4 本・監査テスト 2 本・測定ツール 11 本（未追跡）
- `out/2d-script-v5/`（約 2.8GB・`.gitignore` 対象）

---

## 1. オーナーが実画面を見て挙げた 5 件

seed 42 を実際に再生しての指摘です。

| | 内容 | ショット | 種別 | 状態 |
|---|---|---|---|---|
| #1 | 馬がぴょんぴょんする。絵は良いが走って見えない | `fourth-corner-high` | 不具合 | ★**原因判明・未修正（判断待ち）** |
| #2 | 走行中に一瞬後ろへ戻る。以前この真横では起きていなかった | `homestretch-side` | 不具合 | **修正済み** |
| #3-b | 寄りの途中で大きく後退し、巻き戻しのように見える | `homestretch-side` | 不具合 | **修正済み**（#2 と同因） |
| #3-a | 寄りに切り替えたら 4〜5 頭のせめぎ合いを作り、そのままゴールまで見せたい | `homestretch-side` | 演出要望 | ★**指示書待ち** |
| #4 | 真横に切り替わった後もせめぎ合いのまま決着させたい | `homestretch-side`→`finish-line` | 演出要望 | ★**指示書待ち** |

オーナー合意事項: **不具合（#1/#2/#3-b）を先に片付け、演出要望（#3-a/#4）は別立て**。
理由は、跳ね・後退が残ったまませめぎ合いの構図を作ると、どちらの効果か切り分けられないため。

---

## 2. 直したもの（#2 / #3-b）

### 原因

`packages/render/src/broadcast-v2-scene.ts` の「争っている馬を画角に収める」処理が、
**「先頭から `withinM` 以内か否か」の在/不在**で数えていた。馬が境目をまたぐと収める幅が
1 コマで変わり、画角が飛ぶ。

**実測（seed 42・`homestretch-side`）**

| 表示秒 | 画角 | 注視点の進み | 枠12 の画面上の位置 |
|---|---|---|---|
| 19.830s | 13.00° | +0.55m/コマ | −71px |
| 19.863s | **5.96°** | **+2.2m/コマ** | **−1194px** |

★同じ「在/不在で跳ぶ」問題は、以前**注視点の横位置**で直した記録がコード内に残っている
（「順位が入れ替わると画面が 237px 飛ぶ」）。今回は**画角の側に同じ形で残っていた**。

### 直し方

端の馬の効きを**連続**にした。`withinM` までは丸ごと数え、そこから同じ幅（`band = withinM`）を
かけて 0 へ落とす。カメラの位置・台本・カメラ定義値・着順・馬の位置は変えていない。

⚠️ 落とし幅を 3m にしたら**そこが新しい境目**になり、先頭差 14m で 3.22°/0.5m 跳んだ
（新テストが検出）。`withinM` と同じ幅にして解消。

### 効果（seed 42・先頭 3 頭が画面上で後ろへ動いた最大量）

| ショット | 直す前 | 直した後 |
|---|---|---|
| `homestretch-side` | **1113px**（19.83s・**カットの途中**） | 413px（12.10s・カットの境目） |
| `fourth-corner-high` | 171px（8.80s・カットの境目） | 変わらず |
| `finish-line` | 7px | 101px（30.70s・カットの境目） |

**カットの途中で跳ぶ箇所は無くなった。** 残りはすべてカットの境目（0.28 秒のディゾルブ区間）。

### 外れた仮説（同じ道をたどらないために）

最初は「脚の位相 `meters + Δ` が巻き戻っている」と考えた（`visual-scroll.ts` は
`dΔ = (k−1)·dfocusS` で Δ を積分するので、focusS が後退すれば脚も背景も巻き戻る）。
**外れ**だった。実測:

- 注視点 `focusS` は v4・v5 とも**全コマ単調**（後退 0 コマ / 1156 コマ）
- 脚の位相の後退は v5 で最大 **0.07m**（完歩 0.01 回ぶん）。旧 v4 は **1.69m** で、
  ★**v5 のほうがむしろ良い**

---

## 3. 判断待ち：#1「ぴょんぴょん」

### 測ったこと

`fourth-corner-high` では脚が 2.07 完歩/秒で回るのに、馬は画面上を 77px/秒しか進まない。
＝ **1 完歩で 37px**。俯瞰は走路方向の動きが強く圧縮されるため、脚の回転に体が追いつかない。
**カメラの見込み角の問題**であって、脚の位相の不具合ではない。

| ショット | 1 完歩で進む距離 | 画面上の速さ | 脚の周期 |
|---|---|---|---|
| `fourth-corner-front`（旧 v4） | 119px | 255px/秒 | 2.14 完歩/秒 |
| **`fourth-corner-high`（現行）** | **37px** | **77px/秒** | 2.07 完歩/秒 |
| `side-drive` | 18px | 39px/秒 | 2.22 完歩/秒 |
| `homestretch-side` | 3px | 7px/秒 | 2.42 完歩/秒 |

### 選択肢（★どれも未実施）

1. **その俯瞰だけ見た目の完歩を長くする**（脚をゆっくり回す）
   … `BROADCAST_STRIDE_M` を実馬の 7m → 9m にした前例と同じ考え方（「ウサギ」指摘への対処）。
   ⚠️ ただし**カットをまたぐと脚の速さが変わる**
2. **俯瞰の画角・位置を変えて走路方向の動きを増やす**
   … カメラ値の変更にあたるので、指示がなければ触らない
3. **そのままにする**

★開発側では決めない。

---

## 4. 判断待ち：#3-a / #4「せめぎ合いのままゴールへ」

オーナーの言葉:

> レース演出の最も重要なのは、馬の攻防をユーザーが見たいので、このドアップに切り替えてから
> 馬を 4〜5 頭がせめぎ合うドラマチックを作るべき。そして、そのまませめぎ合いを見せながら、
> ゴールする方がいい

> 先ほどのド迫力シーンから この真横カメラワークに切り替えても なお、せめぎ合いを見せたまま
> ゴールインというドラマチックが欲しい

**これは指示書が要ります。** 過去に「隊列を再加工しない」「動的な target を作らない」と決めた
領域に触れるためです（S3 の隊列変換は汎用化に失敗して不採用になっている）。

参考になる実測（seed 42・ゴール前）:
- `finish-line` の時点で 1 着と 2 着は **3.1 馬身** 離れており、画面には 2 頭しか入らない
- その次の `winner-follow` は勝馬 1 頭だけ
- 直線の `homestretch-side` は 4〜6 頭を保持できている時間帯がある（進行 70% 付近）

---

## 5. 測り方（同じ測定をやり直すとき）

測定用の使い捨てスクリプトは**残していません**。作り直す手順だけ置きます。

共通の土台は `tools/lib/race-audit-build.mjs`（未追跡）。
★今回**`auditSceneAt()` に第 5 引数 `script` を足しました**（`'v4'` / `'v5'` を渡せる）。

```js
import { cameraBasis, posOf, project, broadcastV2AnchorWeight,
         buildVisualScroll, BROADCAST_STRIDE_M } from '@star/render';
import { buildAuditRace, auditClock, auditSceneAt, RACE_DEFAULTS } from './lib/race-audit-build.mjs';

const built = buildAuditRace({ seed: 42 });
const clock = auditClock(built, RACE_DEFAULTS.ownGate);
const { scene, drawn } = auditSceneAt(built, clock, displaySec, { width: 1280, height: 720 }, 'v5');
//   画面上の x  … project(scene.camera, cameraBasis(scene.camera), posOf(course, h.s, h.w)).x
//   画角        … scene.camera.fovY * 180 / Math.PI
//   注視点      … scene.focusS
```

- **画面上の後退**: 30fps で全コマ回し、先頭 3 頭の x が前コマより減ったら記録
- **脚の位相**: `visualScroll` を `page.tsx` と同じ刻み（`STEP = 0.05`・イントロ 7.8 秒込み）で
  組み直し、`h.s + deltaAt(d)` を追う
- **1 完歩あたりの画面移動**: `Σ|Δx| ÷ Σ|Δ(位相)|`（位相 = `(h.s + Δ) / BROADCAST_STRIDE_M`）

⚠️ `auditSceneAt` に `script` を渡さないと**既定は `'v4'`** です（ライブラリ側の既定引数は
後方互換のため v4 のまま）。画面の既定は `broadcastV2ScriptFromSearch` が決めます。

---

## 6. 踏んだ罠（このセッションで新しく分かったぶん）

1. **dev サーバー稼働中に `npm run build:web` を実行しない。**
   `next build` は `next dev` と同じ `apps/web/.next` を書き換える。3210 が 500 になった。
   復旧は `Ctrl+C` → `npm run dev:web`。
2. **push は 10 分では終わらない。** このブランチは origin/main より 66 コミット先行していて
   転送量が大きい。バックグラウンドで走らせること。
3. **`git ls-tree` は非 ASCII パスをエスケープ表示する。**
   日本語のディレクトリ名を `grep` したら空振りし、追跡済みの素材を「未追跡」と誤判断した。
   `git -c core.quotepath=false ls-tree ...` を使う。
4. **連続にする直し方は「落とし幅」も見ること。** 急に 0 にすると、そこが新しい境目になる。

---

## 7. 守り続ける決まり

- 実装は**指示書が出ている範囲のみ**。スコープ外の先走りは差し戻し
- 採否・判定は**開発側では決めない**
- push はオーナー指示があるまで行わない（★今回は指示を受けて `9edc761` まで push 済み）
- `git reset --hard` / `git checkout --` / 未追跡ファイルの一括削除は**しない**
- `apps/web/package.json` のポート 3210 変更は保持し、どのコミットにも含めない
- 3D / S3 / v9 / 素材反復の論点には戻らない
- 決定論（憲法4）: `Date.now()` / `Math.random()` を直接呼ばない。台本の切り替えは URL だけ
- レース結果・順位はサーバー権威（憲法3）。描画側は読むだけで書き換えない
- 憲法1: 他社製品名をコードの変数名・コメント・出力に書かない
  （台本名は他社製品由来の語を外して **`v5`** に改名済み。`?cinematography=v5`）

---

## 8. 次のセッションの入り方

```powershell
git status --short          # 上の §0 と一致するか
git log --oneline -3        # HEAD は 9edc761 の 1 つ先（未 push）
npm run dev:web             # ポート 3210（既に動いていれば不要）
```

そのうえで、オーナー／レビュー側から次のどれかを受け取る。

1. **#1 の選択肢 1〜3 のどれにするか**
2. **今回のコミットを push してよいか**
3. **#3-a / #4（せめぎ合いのままゴールへ）の指示書**
