# 【STAR】裁定 — TG-3・`/setup` を受理／データの穴 3 つ／出走料と斤量の持ち方

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-19・レビュー側 |
| **対象** | `0b97fc8`・`5f0335e`・`5a6779a` |

---

## 0. 判定

| # | |
|---|---|
| **TG-3** | ✅ **受理**。★**分類は割ってください**（§1） |
| **§2 の「4 か所すべて洗った」** | ✅ ★★**今日いちばん良い仕事です**（§2） |
| **`/setup`（UI1-8/9）** | ✅ **受理**（§3） |
| **§4 データの穴 3 つ** | ★**毛色＝別便で持たせる／脚質＝出さない／年齢＝サーバーが今の週を返す**（§4） |
| **§5 文言の訂正** | ✅ ★**正しい。★文言でも事実の誤りは直してよい**（§5） |
| **§6 の失敗** | ✅ ★**自己申告が正しい。★そして検査が正しく噛んだ**（§6） |
| **§7 `/entry`** | ★**DB の表に持たせ、ワーカーが TS の定数から書く**（§7） |

---

## 1. ✅ TG-3 — 受理。★**分類は割ってください**

✅ 走査を `tools/` 配下の再帰に。★除外が「`.` 始まり」の **1 つだけ**になったのが正しい形です。
✔ `tools/lib/_probe.mjs` で赤・消して緑。

### 1-1. ★照会への答え — **割ってください**

> `inventory/measure.mjs` と `mutation/run.mjs` は「本番ソースを一時的に書き換えて必ず戻す」道具。
> ★**この簿の「読取専用」は「DB を変えない」の意で、★ソースを触る道具を「読むだけ」と名乗らせるのは正確ではありません**

★★**指摘のとおりです。★割ってください。**

| # | |
|---|---|
| **TG-4** | ★**新しい分類を 1 つ足す**（例: `SOURCE_MUTATING` ＝ **本番ソースを一時的に書き換え、必ず戻す**）。★2 件だけなので安い |
| ★理由 | ★**今日ずっと「分類は正確に」を積み上げてきました**（`0034` の列／公開名の `LEAKS`/`SAFE`／`NOT_A_TOOL`）。★**「だいたい読取専用」を 1 つ許すと、次に同じ理由で 2 つ目が入ります** |
| ⚠️ | ★**「必ず戻す」ことを検査で固定できるかは別問題**です。★**今は分類を正確にするだけでよい**（★固定できるなら、それは次の便） |

---

## 2. ★★§2 — 今日いちばん良い仕事です

> `blankComments` の呼び出しを **4 か所すべて**見ました

| 箇所 | ★見つけた壊れ方 |
|---|---|
| `latestFunctionBodies` | ★**註記に `create function foo(` と書くと、存在しない関数を登録する** |
| `functionPrivilegeStatements` | ★**註記に `grant … on function …` と書くと、実在しない権限の文を拾う** |
| `errcode = '…'` の 1 か所 | ★**`blankComments` のまま**（★文字列リテラルが残ることを前提にした検査。★一律に変えると静かに通る） |

★**「洗ったら 2 つ出た」だけでなく、★「洗った結果、1 つは変えてはいけないと分かった」**のが良い。★**一律に直さなかった**のが正しいです。

### 2-1. ★★そして、これがいちばん重要です

> ✔ 噛むことを確かめた: `revoke` の**後ろ**の註記に書くと、修正前は赤／修正後は緑。
> ★**最初に置いた場所（`revoke` の前）では両方緑で、そのときは「効いた」と言えませんでした** — ★**位置を変えて初めて経路を通せました**

★★**「変異を置いたが、その経路を通っていなかった」ことに自分で気づいた。**
★**これは GE-1〜GE-3（2026-09-16）で私が指摘した「入口は確かめ、出口を確かめていない」の、★逆方向の自己検出です。**
★**「変異が赤にならない ＝ 検査が悪い」ではなく「変異が経路を通っていない」を先に疑えた**のが、★**この案件で積み上げた規律が働いた形**です。

---

## 3. ✅ `/setup`（UI1-8/9）— 受理

| # | ✔ |
|---|---|
| **UI1-8** | ★画面にも註記にも「仮」と明示。★**註記に「これをデザインと思わないでください」**と書いたのが良い |
| **UI1-9** | 当てはまるものだけ言い換え、★**それ以外はサーバーの文言のまま**（`readEntryError` と同じ作法） |
| ★**冪等キー** | ★★**画面の生存中 1 つだけ作って持ち続ける** — ★**送るたびに作り直すと 2 回目も通り、V-19 ⑭ の穴になる**。★**これは指示になく、実装しないと気づけない種類のものです** |

---

## 4. ★§4 データの穴 3 つ — ★**「無い列を作って埋めなかった」のが正しい**

★**ここが本便でいちばん正しい判断です。** ★デモの「栗毛」を見て列を作っていたら、★**画面の作り物が仕様になっていました。**

| 項目 | 裁定 |
|---|---|
| **毛色** | ★**持たせてよい。★ただし別便**。✔ **D-058 ③ が「毛色 5 種を例外として許可」**と既に定めています（★アートバイブル §5）。★**今は出さない**（列が無い） |
| **脚質** | ✘ ★**出しません。★指摘のとおりです** — ★`race_entries.strategy` は**出走登録のたびに選ぶもの**で、★**固定で出すと馬の性質のように読めます**。★**出すなら「前走の脚質」**（★戦績の層・D-116） |
| **年齢** | ★**サーバーが「今の週」を返してください。★画面が時計を持たない** — ✔ **正典 §14「ゲーム内時刻の真実は Postgres の `now()` のみ。ワーカーやクライアントの時計を信用しない」**。★**画面が `Date.now()` から週を計算する形にしないこと** |

### 4-1. 条件（UI1-10）

| # | |
|---|---|
| **UI1-10** | ★**現在のゲーム内週を、公開ビューか RPC で返す**（★`cycleIndexAt` から導く）。★**画面は引き算で年齢を出すだけ**にする |

---

## 5. ✅ §5 文言の訂正 — ★**正しい。そして「文言だから」で止めなくてよい**

> 旧「引退や**引き直し**をしても**同じ馬**が迎えられます」→ ★**実装と違う**

★**事実として誤っている文言は、デザイナー便を待たずに直してよい**です。★**[EYES] は「どう見えるか」であって、「書いてあることが正しいか」ではありません。**
✔ ★**D-079 ① が `user_id` から導く形を禁じており、止めているのは決定性ではなく「観測できる差が無いこと」**という整理も正確です。
✅ ★新しい文言（「どの 1 頭が来ても、できることは変わりません」）は ★**D-114 の下で正確**です。★言い回しはデザイナー便で差し替えて構いません。

---

## 6. ✅ §6 の失敗 — ★**自己申告が正しく、★検査が正しく噛んだ**

> `5f0335e` を `npm test` が赤いまま積んだ。`typecheck` だけ見て test のサマリを読んでいなかった（★**今日 2 度目**）

★**隠さず「今日 2 度目」と書いたのが正しい。**
★そして ★**落ちたのが `mobile-css-anchors.test.ts` で、「脚質の枠を消したせいで `globals.css` の `flex:0 0 190px` が名指す相手を失った」** — ★★**「黙って効かなくなった規則」を捕まえるための検査が、まさにその形を捕まえました。**

| ★教訓（両方に効きます） |
|---|
| ★**`typecheck` と `test` は別の道具です。★片方の緑をもう片方の緑と読まない**（★R-23 の家族） |

---

## 7. ★§7 `/entry` — **DB の表に持たせ、ワーカーが TS の定数から書く**

### 7-1. ✔ 現状を確かめました

| 値 | 正 | 写し |
|---|---|---|
| **斤量 55** | ✔ ★**TS に定数がある** — `apps/cli/src/calibration.ts:671` の **`BASE_WEIGHT_KG`**（「§8.3・★直書きしていた 55 の写し」） | 🔴 `enter_race` の直書き ＝ ★**三重帳簿の 3 つ目** |
| **出走料 200** | 🔴 ★**TS に定数がありません**（★`CAREER_ASSUMPTION` の**説明文の中**に出てくるだけ・`jockeys.ts:39` が「正典の登録料（200 EP）と同じ桁」と参照） | 🔴 `enter_race` の直書き |

★**開発側の「画面が持つと三重帳簿になる」は正しく、★実は既に三重になっていました。**

### 7-2. ★裁定 — `stable_grade_price` の先例に倣ってください

✔ **D-103 ④ の註記**: 「★**値段は DB の `stable_grade_price` にサーバーが書き、RPC はその行の値で払わせる（二重帳簿にしない）**」

★**同じ形にしてください。**

```
正      TS の定数（★正典の写し・較正登録簿に載る）
  ↓ ワーカーが書く
DB の表  出走料・斤量
  ↓ 読む
RPC・公開ビュー・画面
```

| # | 条件 |
|---|---|
| **EF-1** | ★**出走料の定数を TS に作る**（例 `ENTRY_FEE_EP`）。★**較正登録簿に「正典 §3.4 の写し・較正値ではない」と書く** |
| **EF-2** | ★**出走料と斤量を DB の表に持たせ、ワーカーが TS の定数から書く。★RPC はその行を読む**（★`enter_race` の直書きをやめる・新しい移行で） |
| **EF-3** | ★**公開ビューから画面が読む**（★画面が定数を持たない） |
| **EF-4** | 🔴 ★**値は 1 つも変えないこと**（★登録料 200 は §3.4 の前提で、★**GB-6 の収支の取り直しに噛みます**）。★**器を作るだけ** |
| **EF-5** | ★**R 番号**は `slotOfDay(cycleIndex)` から導けます。★**出どころを決めて 1 か所に**（★ビューに足すか、UI1-10 の「今の週」と同じ経路で返すか） |

★**「止めて照会にした」判断が正しい**です。★**推測で埋めていたら、4 つ目の帳簿ができていました。**

---

## 8. 開発側が次にすること

| 順 | 中身 |
|---|---|
| 1 | **TG-4**（分類を割る）★小さい |
| 2 | **UI1-10**（今の週を返す）＋ ★**`/setup` の年齢** |
| 3 | **EF-1〜EF-5**（出走料・斤量・R 番号）→ ★**その後 `/entry` を繋ぐ** |
| 4 | **D-116 の 4 層** → UI-2〜UI-4 → **T-11** → **AL-11** |
| — | ★**毛色は別便**（D-058 ③ が許可済み） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
