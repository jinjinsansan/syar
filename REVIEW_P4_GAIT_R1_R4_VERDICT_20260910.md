# P4 歩様 R1〜R4 再レビュー（2026-09-10）

対象: `REPORT_P4_GAIT_R1_R4_20260910.md`、HEAD `c58ae0e`。
比較元: `a7ab03b`。レビュー: Codex 単体。

**裁定: 改善は確認。R2・R3 と R1-a は解消。R1-b の bob の扱いと R4 の比較資料・原因判定を補完して再提出。走りの完成判定は保留。**

## 1. 解消を確認したこと

- R1-a: 焼いた経路も `effectivePlacementMode` を通る。ページ内の実際の `buildFramesFromBaked` を構文木から取り出して実行し、4 組すべてで通常は measured-ground、legacy 指定は legacy-table になることを確認した。実際の referenceHeight も変わり、診断だけの修正ではない。
- R2: 前回指摘した常時 legacy 分岐、接地線の定数化、較正引数の定数化を検出する検査が追加された。対象テストは成功。AST 検査があらゆる接続事故を防ぐという意味ではないが、前回の具体的な反例への対応は受け入れる。
- R3: 「実際の検証台と完全一致」を撤回し、PNG を alpha >= 64 で読み直す独立入力と固定接地線 0.920 を使う比較になった。4 組の差が 3px 以下という検査を再実行して成功した。これは配置モデルの許容差であり、丸めを含めた実画面全体や歩様の自然さの保証ではない。
- 時計補償の説明を anchorWeight=0 に限定し、25% を設定値の比と訂正した点を受け入れる。
- 検証台の時計注入が実装され、4 頭の元画像 51 枚はすべて異なる。撮影を進めたことは確認できる。

## 2. R4 継続: 比較用の切り出しが検証台の上下動を消す（P2）

対象: `tools/compare-gait-crops.mjs:63`〜81。

各フレームで `box.h` から切り出し寸法を作り、`box.y + box.h / 2` に中心を合わせて、一定の出力寸法へ resize している。馬の描画矩形自身に追従するため、確認対象の上下動まで相殺する。

提出された元 JSON を独立に読み、同じ切り出し変換を適用した結果:

| 検証台の型 | 元画面の描画矩形中心 Y | 切り出し後の中心 Y |
|---|---:|---:|
| A（lane 0） | 523.5〜533.5px | 259.3264px 固定 |
| B（lane 1） | 498.5〜514.5px | 259.3596px 固定 |

つまり描画枠の 10px / 16px の上下動が比較画面ではゼロになる。輪郭内部の運動がすべて消えるという意味ではないが、元の配置をそのまま比べる資料になっていない。

なお、今回の本編 JSON では対象馬の box.h と中心 Y が 51 枚を通して一定だった。そのため、提出動画で本編の倍率変化まで消えたとは断定しない。ただしツールの設計上は box.h が変わる入力を毎コマ再正規化するため、今後の拡縮評価にも使えない。

再提出するもの:

1. 素材セットの共通基準高などから、クリップごとに倍率と切り出し寸法を一度だけ決める。各コマの画像・輪郭・鞍布の寸法で倍率を更新しない。
2. 縦方向は馬の画像上端ではなく走路上の投影接地点を基準にする。検証台は既存の `groundY`、本編は実描画と同じ走路上の点を使う。カメラ移動を補償する場合も馬の浮きを追従して消さない。
3. 元画面も併記し、倍率・接地基準・対象馬・型を明記する。比較行の初コマでは本編の馬が重なり、HUD も騎手付近にかかるため、対象の姿勢を見分けられる補助表示や区間を選ぶ。
4. 元画面の地面に対する上下動が、比較画像でも同じ固定倍率で残ることを記録する。等速と 0.25 倍、A/B の組合せは維持する。

## 3. R1-b 継続: stride の方針変更と bob を分ける（P2）

対象: `apps/web/src/app/race/page.tsx:2701`、同 `1513`。

報告は「揃える必要があるのは位相を共有する 1 完歩だけ」と説明する。しかし `calibration: horseCalibrationFor(sideMode)` は strideM だけでなく bob も返し、その bob が全方向に使われる。

side-v8 と旧 front の混在では、配置は measured / legacy に分かれる一方、旧 front も bob=0.3 になる。従来の較正値は bob=1 だった。真横の歩調を維持する方針自体は理解できるが、bob は位相連続性を理由に共有する必要がない。これが非真横の異常の主因だと断定はしない。

次便では stride と bob の方針を分けること。配置セットに応じた bob を実描画に渡すか、共通 bob が意図的な新しい較正であることを明示して根拠を示す。少なくとも native / baked の旧 front へのフォールバックで、実使用値を診断に出して固定する。

`?front=diag-front-v2` は素材だけでなく配置方式も切り替わるため、旧新比較は「素材単独の効果」の証明として扱わない。

## 4. R4 継続: 原因についての断定が観測を超えている（P2）

対象: 報告書 176 行、201 行付近。

- 12 頭が同じコマであることは、位相が同期している証拠。離地・支持脚・地面との関係を示さずに「全馬の同時跳躍そのもの」とは確定できない。同期を候補要因として残し、発走直後の同じ時間軸で姿勢と接地を示す。
- 真横の角度差の方が大きく、真横への評価が良いことだけでは、斜め前の方向不一致を主因から除外できない。視点・素材・遠近によって同じ角度差の見え方は変わる。「向きの共有をやめても斜め前は直らない」は未検証の予測に戻す。
- 蹄／頭頂の上下量の比は素材の特徴量であり、現素材の描き直しを選ぶ十分条件ではない。旧素材への復帰／描き直しの二択をオーナーに渡す前に、配置・bob・方向・位相を切り分けた観察を揃える。

製品の歩様を無条件に変更せよという裁定ではない。まず比較資料の誤差を直し、観測と仮説を分けたうえで、具体的な修正案とその比較を提出すること。

## 5. 独立検証と作業範囲

- 対象 3 ファイルのテスト: **37 件成功**（`tmp/review-r1-r4-tests.log`）。今回の全体 1503 件は開発側の報告値で、レビュー側では全体再実行していない。
- `npm run typecheck`: **終了コード 0**（`tmp/review-r1-r4-typecheck.log`）。
- 実 baked builder と提出撮影データの検査: `tmp/review-r1-r4-probe.mjs`、結果 `tmp/review-r1-r4-probe.json`。4 組の override 切替を確認。
- 検証台／本編修正前／本編修正後の各 51 枚は、SHA-256 がそれぞれ 51 種。診断記録も更新されている。動画全編の目視再生による自然さの合格判定はしていない。
- 開始・終了 HEAD は `c58ae0e`。製品コード・素材は変更していない。レビュー文書と tmp の検証資料のみ追加。commit / push / 配備なし。
