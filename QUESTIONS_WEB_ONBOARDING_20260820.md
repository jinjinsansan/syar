# 照会（Web・ログイン導線とオンボーディング）— 2026-08-20

**宛先**: レビュー側
**発行**: 開発側
**関連**: 正典 §3.1〜3.3 / §4（users）/ §10.4 / §11.1 / §12.2 / §14.3〜14.4 / 憲法 §0
**背景資料**: `docs/lp-reference-research-20260819.md`（参照作品 TOP/LP 構造）、`docs/lp-reference-flow-research-20260820.md`（参照作品の TOP→登録→ログイン後→レース参加の導線）、`design/hud-ds/`（Claude Design の正本カード・REQUESTS.md）
**未裁定**: Q-WEB-01 〜 Q-WEB-06

---

## 0. いま何ができていて、何で止まっているか

Codex 復帰待ちの間に、オーナー指示で **Web（DOM）画面を先に起こしました**（すべて見た目＋読み取りのみ。書き込みは無し）。

| 画面 | 状態 | データ |
|---|---|---|
| `/` TOP（未ログイン LP） | 実装済み・オーナー合格 | 静的＋`races_public`（最新確定／次の発売中レースへのリンクだけ） |
| `/races` 番組表・`/races/[id]` 出走表＋公正性・`/races/[id]/odds` | 実装済み | `races_public` / `race_entries_public` / `race_odds_public`（anon 読み取り） |
| `/stable`・`/stable/[horseId]`・`/training`・`/entry`・`/races/[id]/bet`・`/records`・`/prizes` | 実装済み（アーケード筐体テーマ） | **デモデータ**（`lib/stable.ts`・`lib/game-demo.ts`）。Auth と所有馬ビューが無いため |
| `/signup`・`/login` | 「準備中」ページ | — |

**止まっている理由**: 次の一歩が全部「正典に規定が無い／DB・Auth の指示書が要る」ものです。**推測で埋めずに照会します**（CLAUDE.md）。

---

## 1. 🔴 Q-WEB-01 — 新規ユーザーの初回セットアップ（正典に規定なし）

参照作品は「アカウント発行 → 厩舎作成（秘書の案内）→ 種牡馬×繁殖牝馬を選んで生産 → デビュー」の順です（`docs/lp-reference-flow-research-20260820.md` §2.3）。

正典側の手がかり:
- §4 `users` に `display_name` と `stable_name`（牧場名）がある → 初回に入力させる想定と読めます
- §10.5「初期プレイヤーの配合は（引退 NPC 名馬の）種牡馬市場から始まる」→ **最初の馬は配合で得る**と読めますが、**繁殖牝馬をどう持つか**（無償配布？NPC 牝馬市場？）が書かれていません。§12.2 では 配合／種牡馬市場は P5
- 「セリ」は正典にありません（LP の 5 ステップ 2「セリで 1 頭選びます」はデザイナー案。**正典確定まで「迎え方は準備中」に弱めました**）

**裁定をお願いしたいこと**
1. 初回セットアップの項目（表示名・牧場名・勝負服の配色 — D-057「個体識別は勝負服の配色」と整合）
2. **最初の馬の入手方法**: (a) 無償の初期馬を 1 頭付与（NPC 世界から同格帯・低素質を配る）(b) 繁殖牝馬を 1 頭付与して種牡馬市場で配合（P5 まで待つ）(c) その他
3. 最初の馬が出走できるまでの段階（参照作品の「デビュー前 → 入厩 → 条件戦」に当たる新馬・未勝利の扱いは §10.3 の番組表どおりでよいか）

---

## 2. 🔴 Q-WEB-02 — 登録時の初期 EP とデイリーログインの額

§3.2 は供給経路（広告視聴／オファー／**デイリーログイン（少額）**／実績／種付け料）だけで、**登録時に付与する EP と、デイリーの額が未規定**です。§11 経済表では出走登録料 200 EP・調教 100〜800 EP なので、初期 EP が 0 だと最初の週に何もできません。

**裁定をお願いしたいこと**: 登録時付与額・デイリー額・日次上限（§3.2「レート制限」）の初期値。**販売・購入の経路は作りません**（憲法 §0.2）。

---

## 3. 🟡 Q-WEB-03 — ログイン後の着地画面と Web 画面一覧の更新

§12.2 画面一覧は「牧場（ホーム）」がホームで、番組表・記録・景品交換・LP は一覧にありません。実装は `/races` を「ログイン後のハブ」として作りました（参照作品の会員ホームは「アカウント／厩舎／開催状況／ショートカットの 4 カード」。同 §4.1）。

**裁定をお願いしたいこと**
1. ログイン後の着地: (a) `/stable`（正典 §12.2 の牧場ホーム）(b) `/races` に 4 カード（EP/PP 別カプセル・わたしの牧場・開催状況・ショートカット）を足してハブに (c) `/home` を新設
2. §12.2 の画面一覧に **LP／番組表／記録（戦績・EP 台帳・PP 台帳）／景品交換／出走登録／投票** を追記してよいか（実装と正典のずれを放置しないため）

---

## 4. 🔴 Q-WEB-04 — Auth と「自分の馬」の読み取りビュー（指示書が要る）

デモデータ画面 7 枚を実データに切り替えるには、次が要ります。開発側は **Route Handler にビジネスロジックを書かず**（CLAUDE.md）、Supabase Auth ＋ RLS ＋ RPC で進める前提です。

1. **Supabase Auth** の方式（メール magic link か、メール＋パスワードか）。`users` 行の作成タイミング（初回ログイン時のトリガーか、セットアップ画面からの RPC か）
2. **所有馬ビュー**: `horses` を所有者だけが読める view（`potential`・`genotype` を出さない。出すのは current／素質★（潜在の丸め）／調子／疲労／格／戦績／血統 5 代）。現状 `races_public` などは anon の公開ビューなので、**認証付きビューの設計方針**をください
3. **書き込み RPC の契約**（画面は既に押せない形で置いてあります）: `spend_training_ep`（週の調教指示）・`register_entry`（出走登録・脚質・200 EP）・`place_bet`（投票・自馬出走レース拒否 §9.5）・`exchange_prize`（景品交換）・週送り。**ポイント増減と台帳記帳は同一トランザクション**（§14.4）
4. **staging**: D-042（staging が P3 の着手条件）。書き込み系の破壊試験はどこに向ければよいか

---

## 5. 🟡 Q-WEB-05 — 公開ビューに足りない列（デザイン正本との差）

Claude Design の正本カード（オーナー合格）にあるが公開ビューに無い列です。**画面で計算しない**（§14.3）ので、出すならビュー側でお願いします。

| 画面 | 欲しい列 | 用途 |
|---|---|---|
| `race_odds_public` | 支持率（%）／最終更新時刻 | オッズ画面の支持バー＋「最終更新」 |
| `race_entries_public` | 自馬フラグ（認証付きビューなら `owner_id = auth.uid()`） | 出走表・オッズの「わたしの馬」行 |
| `races_public` | レース番号（R）／開催場の表示名（実在名は使わない） | 番組表・出走表の「11R」「場」 |

---

## 6. 🟢 Q-WEB-06 — 参照作品から取り込むか判断が要る規則（正典と突合済み・差分だけ）

| 項目 | 参照作品 | 正典 | 照会 |
|---|---|---|---|
| 出走枠 | 優先枠＞通常枠＞抽選枠、除外あり | §10.4「上限超過は**完全抽選**（賞金上位優先にしない）」 | **正典のまま**でよいか（取り込まない提案） |
| 賞金の着順 | 5 着まで | §11.1 も 5 着まで | 一致・照会なし |
| 登録料 | 賞金額に比例 | §10.4 一律 200 EP | **正典のまま**でよいか |
| レース頻度 | 4 分おき・3 レース並行 | 10 分に 1 レース（D-007） | 正典のまま。並行開催は P2 以降の運用判断 |
| 馬券の券種 | 単複・枠連・馬連・馬単・ワイド・3連複・3連単ほか | §9 の **7 券種**（単勝・複勝・**ワイド**・馬連・馬単・三連複・三連単。枠連だけが無い）— ★初稿の「6 券種・ワイドなし」は読み違い（レビュー側指摘で訂正） | 正典のまま |

---

## 7. 開発側の提案（裁定を待つ間にできること）

- Q-WEB-01/02 が決まれば、Claude Design に **R-4（初回セットアップ画面＋ログイン後ホーム）** を依頼し、デモデータで先に画面を起こせます（Auth 接続は Q-WEB-04 の指示書後）
- Q-WEB-04 の指示書（マイグレーション・RLS・RPC 契約）が出れば、`StableRepo` を Supabase 実装に差し替えるだけで画面は変わりません（`lib/stable.ts` の設計どおり）
