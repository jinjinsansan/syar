# 【STAR】照会: 「いま売っているレース」以外に投票する画面は要るか — 2026-09-25

- 開発側 Claude（`star-aa`）／ 宛先: オーナー（★レビュー側 経由）
- きっかけ: 裁定 `REVIEW_DISCOVERY_AXES_20260925.md` §6 の手順 ⑤
  →「★この手順で決まらないもの（★**遊びの手触り**）だけ、オーナーに回してください」
- ★**これ 1 件だけ**です（★他の 4 枚は手順で決まり、簿に記録して着手しました）

---

## 1. ★手順は流しました。★ここだけ決まりませんでした

★同じ役割の画面が 2 つ在るもの 5 組に、★裁定の手順（①依存を測る → ②入口を数える →
③片方が見本なら転送 → ④両方本物なら役割の違いを 1 行で書けるか）を流しました。

| 組 | 結果 |
|---|---|
| `/races`（一覧）vs `/vote` | ✅ ★役割が二重 → ★`/vote` へ転送して消した |
| `/races/[id]/odds` vs `/odds/[id]` | ✅ ★役割が同じ → ★`/odds/[id]` へ転送して消した |
| `/odds`（一覧）vs `/odds/[id]`（1 レース） | ✅ ★役割が違う（★一覧と単体）→ ★両方 残す |
| `/races/[id]`（1 レース） | ✅ ★新版が無い（★`/vote` はレースを指定できない）→ ★残して作り直す |
| 🔴 **`/races/[id]/bet`** | ⚠️ ★**決まりませんでした** ＝ ★この照会 |

---

## 2. 🔴 決まらなかったこと

### Q-BET-1 ★「いま売っているレース」以外に投票する画面は要りますか

★いま 2 つ在ります:

| 画面 | できること |
|---|---|
| `/vote`（★新世代・`/home` から来る） | ★**いま売っているレース**に投票する |
| `/races/[id]/bet`（★旧世代） | ★**レースを指定して**投票する |

★役割の違いは 1 行で書けます（★上の表）。★しかし ★**必要かどうかは遊びの手触り**です:

- ★サイクルは **6 分**（`CYCLE_MS`）で、★**売っている（`scheduled`）レースは常に 1 本**です
- → ★「今 以外のレースに買う」は ★**そもそも起きないのではないか**と見ています
- ★ただし ★**先のレースを眺めて、発売を待って買う**という遊び方を想定しているなら、
  ★指定して入れる画面が要ります（★`/races/[id]` → 発売されたら買う、の流れ）

### ★私の推し: ★**`/vote` に寄せて `/races/[id]/bet` は捨てる**

- ★理由 ①: ★売っているレースが常に 1 本なら、★**指定する意味がありません**
- ★理由 ②: ★投票の画面が 2 つ在ると、★上限（★1 レース 5,000 EP・§9.5）や
  ★自馬の制限の見せ方を ★**2 か所で直す**ことになります（★片方だけ直す事故が起きます）
- ★理由 ③: ★`/races/[id]`（1 レースの画面）は ★**残します**。
  ★そこに「★このレースは発売中です → 投票へ」と ★`/vote` へ送るリンクを置けば、
  ★流れは切れません（★**画面は 1 つ、入口は 2 つ**）

### ⚠️ ★私が決めない理由

★これは ★**「どう遊んでほしいか」**の話で、★コードからは決まりません。
★捨てる判断は戻しにくいので（★git には残りますが、★作り直す手間が発生します）、
★オーナーの一言をいただいてから動きます。

---

## 3. ★いま在る状態（★この照会の答えを待っている間）

- ★`/races/[id]/bet` は ★**そのまま生きています**（★消していません・★転送もしていません）
- ★簿（`apps/cli/src/screen-generations.ts`）に ★`rebuild` ＋「★手順では決まらない・照会に出した」と記録
- ★釘（`screen-rebuild-pins.ts`）に ★いま呼んでいる口を記録済み
  （★`bet-screen` / `format` / `game-demo`）。★作り直しで口が消えたら検査が落ちます
