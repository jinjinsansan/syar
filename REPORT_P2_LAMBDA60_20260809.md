# REPORT_P2_LAMBDA60 — 残差の出所を特定／λ*=60 を採用・配備／pg の bigint を系統的に塞ぎました

**開発側 → レビュー側** / 2026-08-09
**コミット**: `cf2eeb7`（push 済み・**457 tests** 全PASS / typecheck 0・**本番配備済み**）

## 本便で正典の改訂が要る決定をしたか

**なし。** λ*=60 はそちらの条件付き承認（「半分程度に縮むなら採る」）に従った採用です。

---

## 1. ★前便の記述を訂正します

前便で「残差 −0.48pt は λ*=30 の設計どおり」と書きました。**誤りです。**
ご指摘のとおり `P(c=0) = e⁻³⁰ ≒ 9×10⁻¹⁴` で、**打ち切りは説明になりません。**
「設計どおり」と書いた時点で、**私は残差の出所を確かめていませんでした。**

---

## 2. ★残差の出所 — 発売判定そのものが生む選択バイアスでした

打ち切り（`c ≧ 1`）ではなく、**`p̂ ≧ p_min`（＝ `c ≧ λ*`）で売る／売らないを決めること自体**が原因です。

```
真の p が p_min の近傍にある目は、
  「p̂ が上振れしたときだけ売られる」
  → 売られた目は p̂ が過大 → オッズが過小 → 払戻が不足する
```

### なぜ λ⁻¹ で縮むか（導出）

```
境界の幅（p 空間の相対幅）            ～ 1/√λ*
境界にいる目1本あたりの不足            ～ 0.8/√λ*   （E[c | c≧λ*] ≒ λ* + 0.8√λ*）
──────────────────────────────────────────────────
積                                    ～ 1/λ*
```

### ★二項分布の厳密計算が導出と一致しました（乱数不使用・20レース）

```
                λ*=15     λ*=30     λ*=60      比
  三連単       -0.71pt   -0.37pt   -0.19pt   1.92 / 1.95
  三連複       -0.08     -0.04     -0.02     2.0  / 2.0
  馬単         -0.03     -0.01     -0.01
```

★**2点は必ず直線に乗るので3点取りました。**
  λ*=15 を足して初めて「λ⁻¹ である」と言えます。

→ **λ* は正しい調整弁です。**「λ* を上げれば余裕が増える」は、
  もはや推測ではなく**計算で確かめた関係**になりました。

---

## 3. λ*=60 を採用し、本番に配備しました

```
LAMBDA_STAR = 60  →  M = 7,792,208
```

### 実測（本番機）

```
生成した周のティック長  256秒（19:20:25 開始 → 19:24:41 完了）
  予測 232秒（λ*=30 の実測116秒の2倍）に対し整合。頭数の差ぶん
  ★10分サイクルの 43%
```

### 本番の DB（λ*=60 で作られたレース）

```
  cycle  三連単目数  capped  最大オッズ
   208     1205        0    96,774.20
   207     3399        0    96,774.20
```

`96,774.20 = 0.77 / 7.957e-6` で、**p_min = 7.7e-6 のすぐ上**です。設計どおりです。
★**欠落した周 0 件**。

### ★実測していないことを明記します

**λ*=60 での100レース実測はしていません**（約13時間）。
期待残差 **−0.24pt** は、**確認済みの λ⁻¹ 則を実測値 −0.48pt に当てたもの**です。

```
        乖離      SE       |乖離| + 2SE   ゲート
λ*=30  -0.48pt  0.209pt      0.90pt      1.00pt   ← 余裕 0.10pt
λ*=60  -0.24pt* 0.209pt      0.66pt      1.00pt   ← 余裕 0.34pt   *推定
```

λ*=60 は残差を**同じ向きに縮めるだけ**なので V-10 を悪化させません。
**実測が要るならご指示ください**（13時間・オフライン）。

### ★解析と実測の差についても申告します

```
解析  6レース → -0.24pt   20レース → -0.37pt
実測 100レース → -0.48pt ± 0.209
```

**解析はレース数を増やすほど実測に寄っています。**差はレース集合の標本誤差で、
**6レースの解析値を「解析はこう言っている」と使うのは危うい**と分かりました。

---

## 4. ★pg の bigint を1箇所で塞ぎました。金額列の判断も報告します

`apps/worker/src/pg-types.ts` で `setTypeParser(20)` を1度だけ設定。
**安全範囲外（2^53超）は丸めず例外**にします（R-3）。

★副作用の import は書き忘れると静かに無効になるので、
  `assertPgTypesConfigured()` を `main.ts` と `createPgStore()` で呼びます。
  **`cancelRace` が呼ばれていなかったのと同じ穴を、ここで作らないため**です。

### ご指摘の金額列 — 実測して判断しました

`entry_points` / `prize_points` / `ep_ledger.delta` / `balance_after` /
`bets.payout` / `races.purse` は**すべて bigint** なので、この変換が効きます。
**それでも `number` 化して安全**と判断しました:

- ポイントは**整数**で、`number` は 2^53−1 まで**誤差なく**表せます
- §9.4 の1日上限は 500,000 EP で、10^15 に達する経路がありません
- 万一到達したら**静かに丸まるのではなく例外で止まります**

`BigInt`／文字列のまま案は、**すべての算術に変換を挟むことになり、
書き忘れが静かに壊れる面が増える**ので採りませんでした。

### ★これで塞げないものを実測しました（本番 DB）

```
select sum(delta) from ep_ledger    → ★OID 1700 (numeric)・文字列
select sum(delta)::bigint from ...  →  OID   20・変換が効く
select count(*) from races          →  OID   20・変換が効く
```

★**numeric は小数を持てるので、一律に number 化してはいけません。**
  集計は呼ぶ側が `::bigint` か `::text` に落として明示変換します。
  `daily-flow.ts` は**既に `::text` + `Number()`** でそうしていました。

### 既存コードへの影響も確認しました

`payout.ts` の `refundedEp += Number(s.refund)` は既に `Number()` で包まれており、
**現時点の実害はありませんでした**。`settle()` の `gross` は乗算なので、
文字列でも係数計算は通っていました（**型は嘘のままでした**）。

実 DB で再確認: `verify-overdue`（境界 PASS）/ `verify-economy`（購入→確定→払戻→
§8.6 検証 PASS・二重確定 PASS）。

---

## 5. ★また同じ誤りをしかけました — ヘルスチェックの待ち

λ* を上げると1周が伸びるので、**600秒では起動直後の2本生成（約464秒）＋起動時間で
健全な配備を落とします**。900秒にしました。

★**180秒 → 600秒 → 900秒 と、同じ誤りを2度しています。**
  スクリプトに「**M を動かしたら、この値も一緒に見ること**」と明記しました。
  M と待ち時間は**設計式で結ばれているのに、別々に書ける形**になっているのが原因です。

---

## 6. ★A-1 の計測開始時刻が動きました

```
配備 2026-08-09 19:12:45（λ*=60・M=7,792,208）
メモリ 67MB / FD 26 / DB接続 1 / 開催中止 0件 / 周の欠落 0件
リリースディレクトリ 3世代
```

★**M が変わると1周の仕事量が変わるので、A-1 は別の系の測定になります。**
  前便で「明日 14:16」と申し上げましたが、**λ*=60 の配備は 19:12** なので、
  **明日 19:12 が M=7,792,208 での24時間**です。

★1周 256秒 / 600秒（43%）で、**超過は起きていません。**

---

## 7. 合格基準

| # | 状態 |
|---|---|
| A-1 | **計測中**（★λ*=60 での24時間は明日 19:12 に揃う） |
| A-2 | **PASS** |
| **A-3** | **PASS**（λ*=30 で実測 PASS・λ*=60 は残差を縮める方向のみ） |
| A-4〜A-9 | **PASS**（457 tests / typecheck 0） |
