# 報告 — レース映像の仕上げ（イントロの自馬・勝負服・毛色・リプレイの跳び）

作成: 2026-08-28 / ブランチ `p4/race-30sec-cuts`
起点: タグ `p4-race-quality-20260828`（`6354f42`）→ HEAD `1e6d7bd`（3 コミット）
前の記録: `HANDOVER_P4_RACE_QUALITY_20260828.md`

> ★**すべてオーナーが実画面を見ての指摘に対する対応です。**
> ★`Date.now()` / `Math.random()` を使っていません。着順・タイム・着差・払戻に触れていません（憲法 3/4）。

---

## 0. この便で直した 4 件

| # | オーナーの言葉 | 種別 |
|---|---|---|
| ① | 最初の桜星賞のイントロでデモで構わないので馬を 1 頭出してください（自分の馬） | 要望 |
| ② | 騎手の服の色を同じ色は毎レース絶対に被らないようにしたい | 要望（★機械で固定した） |
| ③ | 馬の毛も変えてください。JRA のレースに登録されている馬の毛の色通りに | 要望 |
| ④ | ★**ゲート発送の時に騎手の服は違うのに、レース中は同じ騎手の色になって破綻している** | ★**不具合** |
| ⑤ | ★**リプレイの演出の時、ゴール前で必ず 1 度がくっとします。これはなに？** | ★**不具合** |

---

## 1. ④ レース中だけ勝負服が枠色になっていた（★この便でいちばん重い）

### 何が起きていたか

2026-08-28 に「**帽子＝枠色 / 上着＝馬ごと**」を入れたとき、塗り分けをこう書きました。

```ts
const useCap = helmet || saddlecloth;      // ← ★兜が先
```

⚠️ ★**兜と上着の窓は重なっています。**

```
SILKS_LAYOUT_CROUCH（真横・レース中）
  兜   ny ≤ 0.23
  上着 ny 0.08 〜 0.39      ← ★0.08〜0.23 が重複
```

★**横から見た伏せた騎手は、服のいちばん広い部分がまさにその帯**です。
そこが全部**帽子の色（＝枠色）**で塗られていました。

★実画面（seed 42・15.7s）: ★**7 番と 8 番が同じ緑／11 番と 12 番が同じピンク**。

### ★なぜゲートでは正しかったか

★**ゲートは別の素材**（`diag-front-v2`）で、そちらは重なりが **0.06〜0.10 と狭い**ためです。

```
SILKS_LAYOUT_FRONT（正面・ゲート）
  兜   ny ≤ 0.10
  上着 ny 0.06 〜 0.24      ← 重複は 0.06〜0.10 だけ
```

★**オーナー評「ゲートでは違うのにレース中は同じ」は、この 2 つの素材の窓の差そのもの**でした。

### 直し方

```ts
const useCap = saddlecloth || (helmet && !jacket);
```

★窓は「兜は上着より上」「鞍布は上着より下」という**上下の並び**で意味を持ちます。
★**重なりでは内側（上着・鞍布）を優先**します。

---

## 2. ② 「絶対に被らない」を機械で固定

★いまは構造上そうなっていますが、★**固定しないと将来崩れます。**
実際、**2026-08-27 に「色は枠」という仕様で同じ枠の 2 頭が完全に同じ**でした。

新規 `packages/render/test/silks-distinct.test.ts`（5 件）:

| 検査 | 内容 |
|---|---|
| ★同じレースで 2 頭が同じ色にならない | **2〜18 頭立て**で役割名も 16 進も重複しない |
| 12 頭立ての最小色距離 | **30 以上**（実測 **38.8**・道具 `pick-silk-palette.mjs` の目安） |
| 13 頭以上 | ⚠️ **27.5** で目安 30 を満たせていない。**既知の不足として明記**し、下限 25 で悪化だけ止める |
| 同じ枠でも上着は違う | 枠が同じ全ペアで上着が別であること |
| 塗り分けの優先順 | `saddlecloth \|\| (helmet && !jacket)` が戻っていないこと |

---

## 3. ③ 毛色を実際の競走馬に揃える

★公式区分は **8 種**（栗毛・栃栗毛・鹿毛・黒鹿毛・青鹿毛・青毛・芦毛・白毛）。
★**7 種**を持ちます。★**白毛は入れません** — 登録頭数 0.1% 未満で、
★12 頭立てに 1 頭いると「珍しい」ではなく「変」になります。

| | 前 | 後 |
|---|---|---|
| 種類 | 5（鹿毛・栗毛・黒鹿毛・青毛・芦毛） | ★**7**（＋栃栗毛・青鹿毛） |
| 栗毛 | hue 10 / sat 1.15 / bri 1.1 | ★hue 16 / sat 1.42 / bri 1.24 |
| 青毛 | bri 0.62 / sat 0.6 | ★bri 0.36 / sat 0.38（黒鹿毛と区別がつかなかった） |
| 12 頭の内訳 | 鹿毛5 栗毛3 黒鹿毛2 芦毛1 青毛1 | ★鹿毛5 黒鹿毛3 栗毛2 芦毛1 青鹿毛1 |

★並びは実在の割合（鹿毛 ~48% / 黒鹿毛 ~22% / 栗毛 ~15% / 芦毛 ~7% / 青鹿毛 ~6%）に
近づけつつ、★**隣どうしが同じ毛色にならないよう散らして**います
（割合どおりに並べるだけだと鹿毛が固まり「同じ馬が並んでいる」に見えるため）。

### ★既存テストを 1 件是正しました

`coat.test.ts` の「他の毛色は色味を保つ」は **絶対値の色幅（`spread > 30`）**で見ており、
★**暗さと無彩色を混同**していました。青毛を黒鹿毛と区別がつくまで暗くしたら **14.4** で落ちます。

★**危険なのは「暗くする」ことではなく「彩度を落とす」こと**です
（芦毛を素材全体に掛けると騎手の肌まで灰色になる、という当の危険）。
→ ★**彩度（色幅 ÷ 最大値）**で見る形に変え、★**芦毛だけが下回るという対照**も足しました
（片側だけの基準は機構を止めると通る・R-16）。

---

## 4. ⑤ リプレイのゴール前で 1 度がくっとする

### 何が起きていたか

ゴール後の位置は `withFinishRunOut` がこう作ります。

```
位置 = 1600m + ( raceSec − その馬の finishSec + postDisplaySec ) × 速さ
```

⚠️ ★**`postDisplaySec` は「勝馬の通過」を基準にした値**です。
★本編では `raceSec` が止まっているので問題になりませんが、
★**リプレイでは `raceSec` が進み続けます。**
→ ★**2 着以降が線を越えた瞬間、既に正になっている `postDisplaySec` の分だけ前へ飛びます。**

★実測（seed 42・2 着）:

```
1 コマの進み  0.485m → ★1.901m（＝約 185px の跳び）
速さ         14.56 → ★57.04 → 14.56 m/s
```

### ★勝馬は連続していました

★**だから「勝馬を測って異常なし」で止めていたら見つかりませんでした。**
★**1 頭ずつ測って初めて出ました。**

⚠️ ★**申し送り**: ★**代表 1 頭で測って「異常なし」としない。** 全頭で測ること（R-22 の家族）。

### 直し方

`finishSpeedsOf` を新設（位置モデルから **1 レースに 1 回だけ**各馬の通過時の速さを測る）。
リプレイでは `postDisplaySec` を渡さず、★**各馬が自分の速さでそのまま線を通り抜けます。**
★線の前後で速さが変わりません。

```
seed 42 / 253 / 90 / 2 の全馬・リプレイ全区間で ★速さの跳び 0 回（前は 2 着で 1 回）
```

★テストを 2 件足しました。1 つは「飛ばないこと」、もう 1 つは★**壊れ方そのもの**
（`postDisplaySec` を渡すと飛ぶ）を固定しています。
★後者が飛ばなくなったら `withFinishRunOut` の意味が変わったということなので、対処も見直すこと。

---

## 5. ① イントロに自馬を 1 頭

右下の「あなたの馬 / MY HORSE」パネルの上に立たせました（表示 3.0〜5.6 秒）。
★暗幕は左 44〜78% なので、この位置は背景が見えたままでタイトル板と重なりません。

★**レース中と同じコマ集合**（`sideHighQuality`）を使います。★ここで別の絵を用意しません
（同じ馬に見えないため）。勝負服の色替えもそのまま。接地影を付けて浮かないようにしました。
コマ送りは `displaySec` だけで決まります（憲法 4）。

### ★2 回外しました（どちらも実画面で気づきました）

1. ★**別の馬が出ていました。** `sideHighQuality` は **`[馬番][コマ]`** なのに
   ★**`[コマ][馬番]`** として読み、★**コマを送るたびに別の馬**が出ました
   （自馬は 3 番なのにゼッケン 11・ピンクの服）。
2. ★**鼻先が画面の右端に触れていました。** パネル中心に置いたため。76px 左へ寄せました。

★どちらも**数字では気づけないもの**でした。撮って見て分かっています。

---

## 6. 検証

```
npm test        → 121 ファイル / 1199 件 PASS（exit 0）★新規 7 件
npm run typecheck → クリーン（exit 0）
```

新設・改訂したテスト:

| ファイル | 内容 |
|---|---|
| `silks-distinct.test.ts`（新規 5 件） | 勝負服が同じレースで絶対に被らない |
| `finish-replay.test.ts`（＋2 件） | リプレイで線の前後の速さが変わらない／★壊れ方の固定 |
| `coat.test.ts`（是正） | 彩度で見る形へ。芦毛だけが下回る対照を追加 |

★**未 push**（CLAUDE.md・オーナー指示待ち）。
⚠️ ★`apps/web/package.json`（ポート 3210）は**オーナーの既存作業**。コミットに混ぜていません。

---

## 7. ★残っていること

★**前の引継ぎ書（`HANDOVER_P4_RACE_QUALITY_20260828.md` §5）から変わっていません。**

1. ★**レビュー側への差し戻しが未了**（裁定の前提が 2 つ崩れている）
2. ★**4 角の境目の走行方向の反転**（20 seed / 180 境目のうち 40 箇所）。閃光トランジション未着手
3. ★**γ は主軸 1.6 のまま未確定**
4. ★**v6 は既定ではない**（既定は v5）
5. ★13 頭以上の勝負服の最小色距離 **27.5**（目安 30 未満）
6. ★リプレイに COURSE ミニマップと ORDER 表が被る
7. ★測定器が γ を 1 か所から引く根治（共通土台・別便）

---

## 8. 再現コマンド

```powershell
# 見る（dev サーバー: cd apps/web ; npm run dev）
http://localhost:3210/race?seed=42&cinematography=v6&contest=1.6
#   3.0〜5.6s イントロの自馬 ／ 9〜10s 毛色 ／ 15〜20s 勝負服 ／ 49〜53s リプレイ

# リプレイで速さが跳ぶ馬がいないか（全馬・全区間）
npx tsx tools/_replayjerk.mjs 42,253,90,2

# 勝負服の色距離（既存 18 色と、離れた 12 色の選定）
node tools/_silkdist.mjs 12
```

★`tools/_*.mjs` は `.gitignore` 対象・追跡外です（登録簿には載せます）。

---

## ★9. 申し送り

1. ★**代表 1 頭で測って「異常なし」としない**（§4）。勝馬は連続していて、2 着だけが飛んでいました
2. ★**窓や範囲が重なるものを足すときは、重なりでどちらが勝つかを決めてから書く**（§1）。
   ★片方の素材でだけ破綻し、もう片方では正しく見えます
3. ★**テストが「壊れ方」を固定していないと、直したつもりが戻る**（§4 の 2 件目）
4. ★**絶対値で測る基準は、別の軸の変化に巻き込まれる**（§3・暗さと無彩色）
