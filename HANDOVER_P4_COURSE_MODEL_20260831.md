# 引継ぎ書 — 次のセッションはここから（2026-08-31・第2便）

作成: 2026-08-31 / ブランチ `p4/race-30sec-cuts`
起点: `425d47b` → ★**12 コミット**（★**この引継ぎ書のコミットを含む**）／ ★**未 push 12 本**
⚠️ ★HEAD は**この引継ぎ書のコミット**です。★`git log --oneline -1` で確かめてください（★ハッシュを直書きしません）。
前の引継ぎ書: `HANDOVER_P4_LANE_MODEL_20260831.md`

> ⚠️ ★**この引継ぎ書は「書いた時点の真実」であって現在の真実ではありません**（正典 R-25）。
> ★参照する前に、★**実ファイルと `git log` で存在を確かめてください。**

---

## ★0. ★**次の一手は「プルダウンで各競馬場を見る」です**（★オーナー指示）

> ★**次回はプルダウンで各競馬場を開発サーバーで私が見るターンです**（2026-08-31）

```powershell
cd V:\dev\Cusor\star\apps\web ; npm run dev      # ★ポート 3210（★別の窓）
```

★**開く URL は 1 本だけ**です。★あとは**画面のプルダウン**で切り替えます:

```
http://localhost:3210/race
```

★上の操作列に ★**「レース」のプルダウン**があり、★競馬場ごとに 50 鞍が並んでいます。

### ★10 場を 1 つずつ見る（★プルダウンで選ぶ）

| 競馬場 | ★選ぶレース | 形 | ⚠️ ★A-8 の影響 |
|---|---|---|---|
| スターパーク | ★G1 桜星賞（芝1600m） | 左 / 1周2000・直線400 | ★**ほぼ無し**（1m） |
| 月見丘 | ★G2 月見丘カップ（ダート1400m） | 右 / 1周1650・直線350 | ★**無し**（★合っている 2 鞍の 1 つ） |
| 天河 | ★G2 天河記念（芝2500m） | 左 / 1周2200・**直線620** | ★軽い（6m） |
| 霧ヶ原 | ★G1 星冠賞（芝2000m） | **右** / 1周2000・直線470 | ★軽い（31m） |
| 陽光台 | ★G2 陽光賞（芝2000m） | 左 / 1周1750・直線430 | ★中（71m） |
| 白砂 | ★G1 蒼海賞（**ダート**1800m） | 左 / 1周1700・直線290 | ★**強い**（161m） |
| 青嶺 | ★G2 青嶺記念（芝2200m） | **右** / 1周1800・直線330 | ★**強い**（221m） |
| 大河原 | ★G3 白光記念（芝**3600m**） | 左 / 1周**2400**・直線540 | ★**最悪級**（361m） |
| 銀嶺 | ★G1 銀河賞（芝**3000m**） | 左 / 1周2100・直線380 | ★**最悪級**（371m） |
| 潮風 | ★G3 涼風ステークス（芝**1000m**） | **右** / 1周1900・直線310 | ★**逆側**（★直線を 60m 使い残す） |

### ★見ていただきたいところ

| | |
|---|---|
| ★**走路の形の違い**（いちばん大事） | ★コース図・回り・距離が場ごとに違って見えるか。★**「また同じレース」にならないか** |
| ★**ゲートの足元**（★A-10 の直し） | ★発走の直後（★シークバー 10 秒あたり）。★**地面を横切る明るい線が消えているか** |
| ★**馬場** | ★白砂（蒼海賞）と月見丘カップが**ダートの茶色い走路**で出るか |
| ★**回り** | ★霧ヶ原・青嶺・潮風が**右回り**で、★コース図と走路の向きが一致しているか |

### ⚠️⚠️ ★**先にお伝えします — カメラは 48/50 鞍で外れたままです**（★A-8）

★**これは既知で、直していません。** ★オーナー判断「B」により ★**走路の模型を広げたあとに 1 回だけ**直します。

★具体的に何が起きるか:

- ★**直線用のカメラが、まだコーナーの上で始まります**（★32 鞍）。★背景にスタンドが無く、ラチが斜めに走ります
- ★**逆に、直線に入っているのにコーナー用のカメラが撮り続けます**（★16 鞍）
- ★上の表の「A-8 の影響」が大きい場ほど強く出ます

→ ★**この件は新しく報告いただかなくて大丈夫です。** ★**それ以外に気づいたこと**をお聞かせください。

⚠️ ★`apps/web/package.json`（ポート 3210）は**触らないこと**。★オーナーの既存作業で、★11 コミットとも混ぜていません。

```powershell
git log --oneline 425d47b..HEAD | wc -l                     # ★12（この引継ぎ書を含む）
git log --oneline origin/p4/race-30sec-cuts..HEAD | wc -l   # ★未 push の数（直書きしない）
git status --short                                          # ★` M apps/web/package.json` だけのはず
npm test                                                    # ★134 ファイル / 1369 件 PASS
npm run typecheck ; echo "終了状態: $?"                      # ★★0（⚠️ パイプに繋がないこと・下の §4-1）
```

---

## ★1. この便で何が起きたか（★1 行で）

★**オーナーの一言で、開発側の都合を 1 つ捨てました。**

> ★**そもそも 50 レースを出すのですから、桜星賞を使い回す必要がどこにある？　なぜ開発を楽になることを選ぶ？**
> ★**開発が大変、開発が上手くいかないとかは、お客様には無関係**（2026-08-31）

★開発側は「★桜星賞の台本を 50 通りに引き伸ばす」直しを提案していました。★**取り下げました。**

---

## ★2. ★**最重要の申し送り 3 件**

### ★2-1. ⚠️ ★**絵の破綻は、拡大して目で見るのではなく、層を止めて差分を取る**

★A-10 で ★**目から出た仮説を 2 回続けて外しました**:

| ★見立て | ★実測 | |
|---|---|---|
| ★ゲートの底辺が水平だから地面と交差する | ★Δy **4.7px** → 浮きは **2.3px** | ★**否定** |
| ★白柵が垂直に断ち切られている | ★画素走査で ★**該当なし** | ★**否定**（誤読） |

★台帳 **B-3**（★100% の撮って出しで判定して外した）と**同じ形**です。
→ ★`tools/shot-race-at.mjs --no-mow / --no-poles / --no-gate` で ★**層ごとの差分**を取ったら一発で当たりました
（★刈り目だけが **90,920 画素・9.87%・全幅**を変えていた）。

### ★2-2. ⚠️ ★**自分の確認方法が壊れていた**（★3 コミット気づけず）

```
  npm run typecheck 2>&1 | tail -3 && echo TYPECHECK_OK      ← ★これは嘘をつきます
```

★パイプラインの終了状態は ★**最後の `tail`** のものなので、★**tsc が落ちても OK が出ます。**
★`ad6eb67` から 3 コミット、★typecheck が赤のまま進んでいました。★**R-21 の形**です。

→ ★**終了状態を直接見ること**: `npm run typecheck ; echo "終了状態: $?"`

### ★2-3. ⚠️ ★**片側だけ数えると被害を半分に見誤る**

★A-8 の被害を ★**「34 鞍」と報告しましたが、正しくは 48 鞍**でした。
★「早すぎ」（32 鞍）しか数えず、★**「遅すぎ」（16 鞍）を数えていませんでした。**
→ ★**合否は両側で数えること**（`tools/_cutgeom.mjs` は直しました）。

---

## ★3. 直したもの・作ったもの

| # | | |
|---|---|---|
| ★**1-0** | ★**A-10**（★ゲートの地面を横切る線） | ★**直しました。** ★真因は ★**芝の縞が走路の外 60m まで塗られ、その縁がゲートの真上を通っていた**こと。★`overhangM` の既定を **60 → 0** |
| ★**1-1** | ★`lane-geometry.test.ts` を 1 場 → **44 通り**へ | ★R-33。★旧検査は変異で**緑のまま**、新検査は落ちることを 2 通りの変異で実証 |
| ★**1-2** | ★V-18 **②b** を検査に | ★`lane-reveal.test.ts`。★seed 1000 本（★実測で決定） |
| ★**1-3** | ★V-18 を ★**`npm test` に載せた**（R-32） | ★`apps/cli/test/v18-gate.test.ts`（5 件・6.2 秒）。★**9 日間の沈黙を実際に捕まえる**ことを変異で確認 |
| ★**§4-2** | ★**半径の地図** | ★① は半径に**反応しない**（±0.0001）／★②a は **180m で頭打ち** |
| ★**1-4** | ★`OvalSpec` に**コーナーごとの半径**（★器だけ） | ★省くと**1 ビットも変わらない**。★`lapM` と食い違えば**投げる** |
| ★— | ★レース選択のプルダウン | ★オーナー指示「毎回コピペ出来ません」 |
| ★— | ★差し戻し **9 件** | ★全件 |

---

## ★4. 次のセッションが踏みやすい穴

### ★4-1. ⚠️ ★**`npm run typecheck` をパイプに繋がないこと**（★§2-2）

### ★4-2. ⚠️ ★**同じ量を 2 か所で実装しないこと**（★この便で 1 回やりました）

★`_gatebias.mjs` に ★**②b の 2 つ目の実装**を書き、★200 本で **0.567**、既存実装の 2000 本で **0.216** と食い違いました。
→ ★V-18 の測り方は ★**`tools/lib/v18.mjs` の 1 か所**に出してあります。★**書き戻さないこと**（D-052）。

### ★4-3. ⚠️ ★**②b・①は seed の本数で値が変わります**

```
  ②b（DEFAULT_OVAL 1600m）  200本 0.348 / 500本 0.365 / ★1000本 0.216 / 2000本 0.216
  ①（V-18 の相関）           50本 0.123 / 100本 0.112 ← ★上限 0.10 を超えて誤報
                            200本 0.079 / ★600本 0.071 / 1500本 0.070
```

→ ★**本数を減らすときは、値が落ち着く所を測ってから**選ぶこと。

### ★4-4. ⚠️ ★**均等半径の検査では「コーナーの並び違い」を検出できません**

★1-4 の変異で実証: ★描画層だけ 1角↔4角 を入れ替えると、★**44 通り（均等半径）の検査は 12 件緑のまま**でした。
→ ★**不均等な半径の場合を必ず 1 つ入れること**（R-33 の系）。

### ★4-5. ⚠️ ★**カットの境目の直後は測定に使えません**（★台帳 B-2・据え置き）

★`--sec 10` は**境目ではありません**（★A-10 で使いました）。★同じ URL を 2 回撮って一致します。

### ★4-6. ⚠️ ★**push は開発側の環境からできません**（★据え置き）

★`credential.helper = manager` が GUI を待って返りません。
→ ★**オーナーに `! git push origin p4/race-30sec-cuts` を実行していただくこと。**

### ★4-7. ★登録簿のメタテストは、この便でも効いています

★`tools/_*.mjs` を **7 本**作り、★全部 `classification.mjs` に載せてあります。★新しい道具を作ったら一緒に載せること。

---

## ★5. この便で増えた検査

| 検査 | 何を守っているか |
|---|---|
| `mow-stripes.test.ts`（6 → **9** 件） | ★縞が走路の外に出ない（★**世界座標**で見る）／★**旧形に戻すと落ちる**／★縁を閉じても帯は出る |
| `lane-geometry.test.ts`（4 → **13** 件） | ★**44 通り**で engine と描画層の幾何が一致／★対象そのものの見張り／★コーナーごとの半径（★器） |
| `lane-reveal.test.ts`（6 → **9** 件） | ★**②b ≤ 1 馬身**／★reveal で動かない |
| `v18-gate.test.ts`（**新** 5 件） | ★**V-18 を CI に**（R-32）。★①②a と、★旧形に戻すと落ちる対照 |

★**すべて変異で「壊すと落ちる」ことを確認済み**です。

---

## ★6. ★オーナー判断待ち／レビュー側へ

| # | | |
|---|---|---|
| ★① | ★**10 場をプルダウンで見る** | ★**次の一手**（§0） |
| ★② | ★**A-10 の合否** | ★レビュー側が同じ 2 枚を撮り直す段（`--sec 10` / 2 鞍） |
| ★③ | ★**「1 角が消えている件」の [EYES]** | ★A-10 が直ったので改めて見ていただく段（裁定 §5-3）。<br>★`out/_shape/compare.png` に ★**発走が走路から突き出している**のが写っています（★A-9 の姿） |
| ★④ | ★**上限 12 の見直し** | ★D-090。★段階①の完了時。★**10 場の数値と内回り／外回りはこの後** |
| ★⑤ | ★**引き込み線** | ★③の [EYES] のあと |
| ★⑥ | ★**段階②（勾配）** | ★独立の便（D-092）。★Q-3 は裁定 §7 で **B に条件付き同意**。★命名は「坂路」と重ねない |
| ★⑦ | ★**指示書 §1-2 の見直し** | ★「旧形に戻すと ②b が落ちる」は**成立しません**（本番 0.216 / 旧形 0.322）。★対照は `SETTLE_M` の変異にしました |
| ★⑧ | ★レース名 50 個の確認 ／ ダート素材の承認 ／ A-5（刈り目） | ★持ち越し |
| ★⑨ | ★タグ | ★ローカルに **6 本**。★**すべて origin にありません**。★この便もタグを打っていません |

---

## ★7. 発行物（この便）

| 種別 | ファイル |
|---|---|
| 報告 | `REPORT_P4_A10_MOW_EDGE_20260831.md`（★A-10）<br>`REPORT_P4_COURSE_MODEL_STAGE1_20260831.md`（★段階①「器」・§0-2 と §6 再実行表つき） |
| 照会 | `QUESTIONS_P4_COURSE_MODEL_20260831.md`（★走路の寸法は作法か／模型を広げるか／坂適性／10 場） |
| 受領 | `DEV_INSTRUCTIONS_P4_COURSE_MODEL_20260831.md` / `REVIEW_P4_COURSE_MODEL_VERDICT_20260831.md` |
| 台帳 | `RESIDUALS_P4_RACE_VIDEO.md`（★**A-8** 新設・48/50 鞍／★**A-9** 新設／★A-10 は直したので報告書へ） |
| 図 | `out/_shape/a10.png`（★A-10 の前後）／ `out/_shape/compare.png`（★コーナーごとの半径で形がどう変わるか） |

---

## ★8. 再実行（★期待値つき・★すべて読取専用）

```powershell
cd V:\dev\Cusor\star

npm test                                # ★期待: 134 ファイル / ★1369 件 PASS
npm run typecheck ; echo "終了状態: $?"   # ★期待: ★0（⚠️ パイプに繋がない）

npx vitest run apps/cli/test/v18-gate.test.ts                    # ★5 件（6.2 秒）
npx vitest run packages/race-engine/test/lane-geometry.test.ts   # ★13 件
npx vitest run packages/race-engine/test/lane-reveal.test.ts     # ★9 件
npx vitest run packages/render/test/mow-stripes.test.ts          # ★9 件

npx tsx tools/verify-v18.mjs --venues   # ★44 通り PASS / ① 最大 0.070 / ②a 7.8〜11.2（★基準値と一致）
npx tsx tools/_cutgeom.mjs              # ★早すぎ 32 / 遅すぎ 16 / どちらかで破綻 48 / 合っている 2
npx tsx tools/_terrain.mjs              # ★区間の並びが違う鞍 44/50 / コーナー 2〜6 本
npx tsx tools/_radiusmap.mjs --races 300 # ★① は半径に反応しない / ②a は 180m で頭打ち
npx tsx tools/_gatebias.mjs             # ★②b 44 通り 0.117〜0.416 / 1 馬身超え 0/44
npx tsx tools/_handovercheck.mjs        # ★この引継ぎ書の URL が実在するか

# ★A-10（★レビュー側が静止画で判定）
cd apps\web ; npm run dev               # ★別の窓・ポート 3210
npx tsx tools/_realshot.mjs --out g1 --query "race=g2-aone"  --sec 10
npx tsx tools/_realshot.mjs --out g2 --query "race=g1-ousei" --sec 10
npx tsx tools/_a10compare.mjs           # ★前後を 1 枚に並べる
```
