# オーナー用: staging で一周触る手引き — 2026-09-24

> 裁定 `REVIEW_HORSE_IDENTITY_VERDICT_20260923.md` §15 の 1。
> **本番のデータには一切さわりません。** 壊しても本番は無事です。

## 0. 🔴 いちばん大事なこと — 触る前に、繋ぎ先を確かめる

画面の **下端に赤い帯**が出ます。そこに繋ぎ先が書いてあります。

```
開発サーバー   繋ぎ先: xxxxxxxxxxxx    xxxxxxxxxxxx.supabase.co
```

- ⚠️ **開発側から「staging のホストはこれです」と、文字でお伝えします。** それと**同じ文字**が帯に出ていることを確かめてから触ってください
- 🔴 **違う文字が出ていたら、触らずに知らせてください。** それは本番です
- ⚠️ 帯が**出ていない**ときも触らないでください。本番の配信では帯が出ない作りなので、**本番を見ている**可能性があります

> なぜこの形か: 「staging」という**名札**を画面に出すこともできますが、それだと
> **名札だけ直して中身は本番のまま**という事故を見逃します。だから**実際に繋いでいる先**を出しています。

## 1. 始め方

開発側が開発サーバーを staging に向けて立てます。オーナーの端末で次を開いてください。

```
http://localhost:3210/home
```

⚠️ **ログインが要ります。** staging には別のアカウントを作ります（本番のアカウントとは別物です）。

## 2. 一周の順番（何を押すと何が起きるか）

| # | 画面 | 押すもの | 起きること |
|---|---|---|---|
| 1 | `/signup` → `/setup` | 表示名・牧場名・勝負服 16 色 | **アカウントができ、馬が 1 頭もらえます** |
| 2 | `/train` | 馬を選ぶ | **顔と全身が出ます**。毛色と勝負服がその馬のもの |
| 3 | `/stable/roles` | 「繁殖入りする」 | **その場で**役割が変わり、生涯の記録に 1 行増えます |
| 4 | `/stable/breed` | 母 → 父 → 「この組合せで依頼する」 | 参加ポイントから種付料が引かれ、**待ち**に入ります（ふつう 60 秒ほど） |
| 5 | 同上（待ちの後） | — | **仔が生まれます**。誕生の絵が出ます |
| 6 | `/stable/name` | 名前を入れて「この名前にする」 | **名前が決まり、厩舎に並びます** |

⚠️ **3 を飛ばすと 4 に進めません**（繁殖牝馬がいないため）。

### 🔴 ただし、1 の直後は 3 に進めません

**登録してもらえる馬は現役の 1 頭**で、**引退するまで実時間で約 26 時間**かかります。
つまり **`/stable/roles` は空のまま**で、そのままでは一周できません。

→ **開発側が、staging のあなたのアカウントに引退馬を 2 頭（牝 1・牡 1）移します。**
  ★**staging なので本番のデータには影響しません。**
  ✔ 素材は足ります（実測 2026-09-24: 引退した NPC の牝 **3,660 頭** / NPC の種牡馬 **299 頭**）。

⚠️ この 1 手は**開発側がやります**。オーナーは 1 → （連絡を待つ）→ 2 の順で進めてください。

## 3. 見てほしいもの

- **2**: 毛色がその馬のものになっているか。**勝負服の色**が、初回設定で選んだ色と同じか
- **3**: 「変えられない」ときの見せ方（枠がいっぱい・性別が合わない・もう産めない）が、**それぞれ違って見える**か
- **4**: 失敗したときの言葉が**責められている感じ**になっていないか
- **5**: 誕生の絵が浮いていないか
- **6**: 名前を入れるところが分かりやすいか。**「送ってから分かります」**の断りが要るか要らないか

## 4. 触っても壊れないもの / 壊れるもの

**壊れないもの**（何度でもやり直せます）

- 役割の変更（功労馬に戻せば元どおり。回数の制限もありません）
- 名前を入れる途中でやめる
- 画面を閉じる・戻る（待ちの途中でも、開き直せば続きが出ます）

🔴 **戻せないもの**

- **付けた名前**（ご自身では変えられません。運営が戻す道具はあります）
- **配合で使った参加ポイント**（種付料。確定したときに引かれます）
- **生まれた仔**（消せません。ただし staging なので実害はありません）

## 5. うまくいかないときに、開発側へ伝えてほしいこと

1. **どの画面か**（URL）
2. **何を押したか**
3. **画面に出た言葉をそのまま**（「エラーが出た」ではなく、出ている文をそのまま）
4. **帯に出ていた繋ぎ先**

⚠️ **「動かない」だけだと直せません。** 上の 4 つがあれば、たいてい原因まで辿れます。

## 6. 開発側の準備（オーナーは読まなくて構いません）

1. `apps/web/.env.local` を staging に向ける（★**元の値を控えてから**）
2. 開発サーバーを立て、★**起動時の 1 行**と★**画面の帯**の両方で繋ぎ先を確かめる
3. ✔ staging の素材は確認済み（2026-09-24: 引退した NPC の牝 3,660 / 種牡馬 299 / 初期馬の候補 2,119 / 週 273）
4. 🔴 **オーナーの staging のアカウントに、引退馬を 2 頭移す**（★牝 1・牡 1・功労馬から）。
   ★これをやらないと `/stable/roles` が空で一周できない。★**移す前後の頭数を記録に残すこと**
5. オーナーに **staging のホスト名**を文字で伝える（★この文書には書かない）
6. 終わったら `.env.local` を元に戻す（★**戻し忘れると、次の作業が staging に向いたままになります**）
