# 【STAR】裁定 — 6 分の実装を受理／★自分の馬のビューに何を出すか

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **対象** | コミット `91afa23`（6 分の実装）／ `8c4b305`（`REPORT_UI_WIRING_ESTIMATE_20260918.md`） |
| **正典** | D-007（6 分）・**D-114**（素質を見せない）・D-108（発見度）・§5.5・§12.4・§18・§1.1 |

---

## 0. 判定

| # | |
|---|---|
| **6 分の実装** | ✅ **受理**（§1）。★**配備の条件（D-007 ⑦）は満たされました** |
| ★**見積りで見つけた穴 2 つ** | ✅ ★**どちらも正しい。★見つけたことが本便の価値です**（§2） |
| **② 自分の馬に何を出すか** | ★**列を 3 つに仕分けました**（§3）。★**`stats` は出しません** |
| 🔴 **「育った実感」** | ★**[EYES] です。オーナーへ返します**（§4） |
| **③ 順序** | ✅ ★**配備が先に同意。★ただし UI-1 は staging で先行できます**（§5） |
| **① UI-1 着手** | ✅ ★**②の設計が決まってから**（§6） |

---

## 1. ✅ 6 分の実装を受理

✔ レビュー側でも確認: `CYCLE_MS = 6 * 60 * 1000` ／ `CYCLES_PER_WEEK = 40` ／ `RACES_BY_CLASS` **70/60/40/30/25/15** — ★**すべて正典 D-007 と一致。**

### ★特に評価する点

> ★**`G1_SLOTS` と `RACES_PER_DAY` は導出のままなので自動で追従 → 90/130/200 ＝ 09:00/13:00/20:00**

★★**AL-9 と D-007 ④ で「導出にする」と決めたものが、★間隔を変えた日に自分で正しい値になりました。** ★**直書きだったら、ここで 3 つとも手で直すことになり、★1 つ忘れれば §10.3 の「視聴の集まる時間帯」が黙って破れていました。**
→ ★**「導出にするもの／直書きにするもの」の線が、実地で報われた最初の例**です。

✔ 註記も 4 か所（`cycle.ts` の「10 サイクル」／`odds.ts` の「19〜27%」／`main.ts` の「40 周に 1 回」／CF-8 の分母 240 周/日）。★**註記と実体を離さない**が守られています。

---

## 2. ✅ 穴 2 つ — ★どちらも正しい指摘です

| # | |
|---|---|
| ① **自分の馬を読む経路が無い** | ★**D-114 と `horses: CLOSED` の当然の帰結**です。★**画面を作る前に見つかったのが良い** — ★繋いでから気づけば、★**急いで `horses` を開ける**という最悪の直し方に流れます |
| ② **`0032` で閉じた 3 表を読む画面がある** | ✅ ★**露出の裁定 §2 が予告したとおりです** — 「★**公開が要るようになったら `*_public` ビューを作ってください**」。★**その番が来ました** |

★**②は `races_public` と同じ形で作ってください。** ★`horse_story_event` は `detail jsonb` を持つので、★**ビューで出す鍵を明示的に選ぶこと**（★`detail` をそのまま出さない）。

---

## 3. ★自分の馬のビューに何を出すか — **列を 3 つに仕分けました**

### ✅ 出してよい

| 列 | 理由 |
|---|---|
| `id` / `name` / `sex` / `birth_year` / `generation` | 見た目と識別 |
| ★**`sire_id` / `dam_id` / `sire_line` / `dam_sire_line` / `pedigree_cache`** | ★★**血統表は出します。** ★**§1.1「血をつなぐ」が中核価値**で、★**P5 の配合 UI・血統表はそのためのもの**です。★**血統は素質ではありません**（系統と親の名前） |
| `birth_week` / `last_processed_week` / `retired_at_week` | 年齢・キャリアの残り。★**素質ではありません** |
| ★**`condition` / `fatigue` / `rest_until_week`** | ★**調子・疲労**（§7.4）。★**素質ではなく、その週の状態**です |
| `foal_count` / `coverings_this_year` / `bred_this_year` | 繁殖の回数（実績） |
| ★**戦績**（`race_entries` から） | ★**D-114 が「手がかり」として名指ししたもの** |
| ★**オッズ**（出走レースの） | ★同上。★既に `race_odds_public` で出ています |
| ★**発見度による適性** | ★**D-108**（「？？？ → 得意かも → A」）。★**表示の層だけで内部値に触らない**形が既に決まっています |
| ★**物語の行**（§18） | ★**LR-6「他人の馬の物語も見える」**。★自分の馬なら当然 |

### 🔴 出さない

| 列 | 理由 |
|---|---|
| `genotype` / `potential` | ★**§5.5・§12.4・D-114。本人にも見せない** |
| ★**`stats`** | 🔴 ★**出しません**（§3-1） |
| `unlock_rate` | ★**`potential × unlock_rate = stats`** なので、★**出すと逆算できます** |
| `surface_aptitude` / `distance_center` / `distance_range` / `strategy_aptitude` / `heavy_aptitude` | ★**D-108 の発見度で段階的に見せる対象**。★**生値は出しません** |
| `growth` / `temper` / `durability` / `frail` / `skill_genes` | ★素質・非能力形質（§6.4）。★見せない |
| `inbreed_coeff` / `nicks_multiplier` | ★配合の内部値 |

### 3-1. 🔴 ★なぜ `stats` を出さないか

★**`stats` は「能力の現在値」で、素質そのものではありません。** ★**それでも出しません。**

| # | 理由 |
|---|---|
| 1 | ★**D-114 が「手がかりはオッズと戦績だけ」と定めました**（★T-10・オーナー決定） |
| 2 | 🔴 ★★**オッズは `stats` から作られます**（✔ `apps/worker/src/build-race.ts` の `abilityOf: (h) => h.stats`）。→ ★**`stats` を数値で出せば、オッズを見る意味が消えます。** ★**D-114 の設計そのものが無効になります** |
| 3 | ★`potential × unlock_rate = stats` なので、★**`stats` と年齢から素質を推し量る経路**もできます |

---

## 4. 🔴 ★「育った実感」は [EYES] です — オーナーへ返します

★**ここが、この設計でいちばん危ういところです。**

★私は相談回答 §1-2 でこう書きました — ★**「サンクコストが効くのは、積み上がったものが見えるとき」**。
★**D-114 で素質も能力も隠した以上、★『調教した手応え』を何で返すかを決めないと、育てる動機が消えます。**

★**[CODE] として言えるのはここまでです**（★出してよい列は §3 のとおり）。★**それで足りるかは、画面を見ないと分かりません。**

### ★私の見立て（★オーナーの判断材料として）

★**返せる材料は 3 つあります。★どれも既に決まっている機構です。**

| # | 材料 | 何が見えるか |
|---|---|---|
| 1 | ★**調子・疲労**（`condition` / `fatigue`） | 「今週は調子が良い」「疲れが残っている」 |
| 2 | ★**発見度**（D-108） | 「？？？ → 得意かも → A」★**走らせるほど分かってくる** |
| 3 | ★**物語の行**（§18） | 「初勝利」「自己最高」「復帰」など、★**出来事として積み上がる** |

★**この 3 つで足りるか、★あるいは「今週は伸びた」という手応えをもう 1 つ足すかは、★オーナーの判断です。**
⚠️ ★**足すなら、それが `stats` の露出にならない形にしてください**（★例: 数値でなく「手応えがあった／なかった」の 2 値）。

---

## 5. ✅ ③ 順序 — 配備が先。★ただし UI-1 は staging で先行できます

★**開発側の見立てに同意します** — ★**繋ぐ先が本番に無いと staging でしか確かめられません。**
✔ ★**D-007 ⑦（配備の前に 6 分を入れる）は満たされました**（`91afa23`）。→ ★**配備できる状態です。**

```
1  ★本番配備（0021〜0033 ＋ ワーカー）   ← オーナー
2  UI-1（自分の馬・出走登録）
3  UI-2〜UI-4
```

★**ただし、UI-1 の設計と実装は staging で先行できます。** ★**完全に待つ必要はありません。**
⚠️ ★**「配備する」と「人を迎える」は別です。** ★人を迎える前の条件（V-10・SMTP・招待制・パスワード方針）は、★**配備を止めません。**

---

## 6. ✅ ① UI-1 の着手 — **②の設計が決まってから**

★UI-1 は「自分の馬」を含むので、★**§3 のビューが先です。**

| 順 | 中身 |
|---|---|
| 1 | ★**自分の馬のビューを設計・実装**（§3 の仕分けどおり）。★**RLS で `owner_id = auth.uid()` に絞る**（`users` と同じ形） |
| 2 | ★**検査で固定する** — ★**ビューに `potential` / `genotype` / `stats` / `unlock_rate` が出ていないことを、★列の名前ではなく構造で見る**（D-108 ③ の作法。★禁止語の一覧で書かない） |
| 3 | **UI-1**（登録 → セットアップ → 自分の馬 → 出走登録） |
| 4 | **UI-3** の `*_public` ビュー 3 本（§2 の②） |

---

## 7. 🔴 オーナーの判断・承認が要るもの

| # | |
|---|---|
| **1** | ★**§4 の「育った実感」** — ★調子・疲労／発見度／物語の 3 つで足りるか。★**足すなら `stats` の露出にならない形で** |
| **2** | ★**§3 の仕分けを正典 §12 に起票するか**（★画面に何を出すかは §12 の話です）。★**起票は私が行います** |
| **3** | ★**本番配備の指示**（★6 分が入ったので、★**技術的な前提は揃いました**） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
