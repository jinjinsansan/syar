# REPORT_P4_RACE_STAGING_20260911 — ★演出の組み直し（①〜⑤）

**日付** 2026-09-11 ／ **枝** `p4/race-30sec-cuts` ／ **着手前 HEAD** `56f35bd`

オーナー指示（5 点）:

> ①ゲートがありません ゲートを作ってください
> ②最初の位置取りを参考映像を全て調べてください
> ③発走の瞬間も参考映像を全て調べて再現している
> ④コーナーは今のところ 1 つも上手くいっていませんが、コーナーを一瞬見せる必要があります。
>   コーナーを出来るレベルで作ってください。（…）今は真横カメラワークのみで飽きます
> ⑤カットインの内容がダメです。（…）意味ないカットインにするならデザイン系のロゴを入れたような
>   カットインにしてください

★⑤ はオーナー判定 ★**A（ロゴ型）**。★追加指示: ★「スタートしました！もおかしいです。これは実況中継の枠内で出すものです」。

---

## 0. 先に、参考映像を測って分かったこと（★これが全部の土台です）

★参考映像（93 秒・480×1040）を通しで測りました。★言えることは 1 つだけです。

> ★**あちらは走行を「真横」と「後方」からしか撮りません。**
> ★正面から走ってくる馬は ★**93 秒を通して 1 カットもありません。**

★発走のくだり（実測）:

| 秒 | 何が映っているか |
|---|---|
| 〜5.6 | ★ゲートの寄り（低い正面） |
| ★5.8 | ★**ゲートは空**（★馬はもう出ていて、★踏み出しは 1 コマも映らない） |
| ★6.0 | ★**ハードカット** → ★高い後方の引き |

★馬の見かけの高さ（★暗い塊の縦の連なりを走路帯で正規化して実測）:

| 場面 | 参考映像 | ★こちら（★変更前・`tools/measure-shot-horse-size.mjs` の実測） |
|---|---|---|
| ★発走直後（後方の引き） | ★**約 5%** | ★18.6% |
| ★せめぎ合い（真横） | ★約 32% | ★28.4% |

→ ★**真横だけが元から合っていました。** ★それ以外は ★**3〜5 倍大きく**描いていました。
★オーナーが繰り返し不合格にしている「ぴょこぴょこ」「跳ねている」は、
★**大きく写しているから読める**のであって、★小さければ読めません。

⚠️ ★**これは「直した」ではありません。** ★脚さばきは 1 つも直していません。
★**小さくして見えなくしただけ**です。★§6 に未解決として残します。

---

## 1. やったこと

### ⑤ カットイン → ロゴ型（★オーナー判定 A）

★旧: ★カット 1 つ（★数秒）を ★**まるごと**隊列図／コース図に置き換えていた。
★新: ★カットの ★**境目の 0.42 秒だけ**ロゴが走る。★残りは ★**その場面の走行**。

- `packages/render/src/race-cutin.ts`
  - ★`raceCutInFor(shotId)` を削除 → ★`raceCutInFlashAt(fromId, toId)` へ。
    ⚠️ ★**「どこから来たか」で見ます。** ★台本 v6 の `side-drive` は ★**2 回**出るので、
    ★名前だけだと ★**直線の入りでない方**でも光ります。
  - ★`drawLogoCutIn` に ★字送り（`letterSpacingEm`）と ★色を追加。
    ⚠️ ★`ctx.letterSpacing` はブラウザにしかないので使わず、★**1 字ずつ**置いています（★R-30）。
  - ★字は ★発走＝レース名（`桜星賞`）／★道中＝製品ロゴ（`STAR`・★トップページと同じ 0.22em・`#ffe37a`）。
- ★光るのは ★**3 か所だけ**: ★位置取りの入り／★コーナーの入り／★コーナーから直線への出。
- ★`FLASH_INTO` の白い閃光は、★ロゴが覆う境目では ★**出しません**（★上から塗るので白く飛びます）。
- ★オーナー指摘のとおり、★**「スタートしました！」の画面中央の大書きを削除**しました。
  ★実況の帯（`drawStartCallBand`）だけが出します。
  ⚠️ ★削除前は ★ロゴと重なって ★**実際に「ス桜星賞！」と潰れて**いました（★実測）。

### ② 発走 — ★参考映像の並びへ

★`start-front`（★〜100m ＝ 表示 4.5 秒）を 2 つに割りました。★**カットは減らしていません（増えています）**。

| 表示秒 | カット | 中身 |
|---|---|---|
| 〜8.0 | `start-front` | ★ゲート（正面・閉→開） |
| 8.0〜8.4 | ★ロゴ | ★**発走の瞬間は見せない**（★参考映像と同じ） |
| 8.4〜9.5 | `start-front` | ★**空になったゲートの一拍** |
| 9.5〜13.0 | ★`start-rear-far`（★新） | ★高い後方の引き・★**馬 5.5%** |

### ③ 最初の位置取り — ★真横 → 真上寄りの引き

- `opening-formation` を ★`side` / `side-v6` → ★`high-diag` / `high-diag-v2` へ。
- ★カメラ `{ backM: 96, upM: 62, sideM: 30, fovDeg: 18.8 }`。★実測 ★**18.0% → 8.1%**。
- ★`fourth-corner-far` とは角度を変えてあります（★あちらは低め・こちらは真上寄り）。
  ★同じ絵を 2 回出すと「引き」そのものが飽きるためです。

### ④ コーナー — ★うんと引いた俯瞰、★前後をロゴで挟む

- ★新ショット `fourth-corner-far`。★`?corner=front|wide|far` で 3 通り選べます（★**既定 `far`**）。
- ⚠️ ★**最初の据え位置（backM 120 / upM 42）は失敗でした。** ★引けてはいましたが
  ★走路の奥へ向かって見るので、★画の中では ★**ほぼ直線**にしか映りませんでした（★撮って確認）。
  → ★高さを上げ、外へ回しました（`{ backM: 70, upM: 105, sideM: 55 }`）。★距離はほぼ同じです。
- ★実測 ★**19.9% → 5.9%**。★`ロゴ → コーナー(2.6 秒) → ロゴ`。

### ① ゲート

★**別セッションが 2026-09-10 に消していたのを、★着手前に戻してあります**（`56f35bd`）。
★`SHOTS['start-front']` の画角が `side` に変えられており、★ゲートが画面から外れていました。
★`diag-front` / `diag-front-v2` へ復帰。★今日はそれ以上触っていません。

### その他

- ★カットイン中に ★**自馬のピンだけが何も無い所を指して浮いて**いたのを止めました。
- `v6BoundariesM` が ★**行数（7）を 2 か所に直書き**していたのを、★中身から引くように直しました。
  ⚠️ ★これを直さずに台本の頭へ 1 行足すと、★**全部の境界が静かにずれます**。

---

## 2. 実測（★`tools/measure-shot-horse-size.mjs`・★画面が描いた矩形から）

★`/race?dev=1`（★既定）・★表示 5〜62 秒・★0.35 秒刻み。

| カット | 前 | 後 |
|---|---|---|
| `start-front`（ゲート） | 17.7% | 17.7%（★据え置き） |
| ★`start-rear-far`（新） | ― | ★**5.5%** |
| `opening-side-lead`（真横） | 20.5% | 20.2% |
| `opening-formation`（位置取り） | 18.0% | ★**8.1%** |
| `opening-side-settle`（真横） | 23.0% | 22.8% |
| `side-drive`（真横・勝負所） | 28.4% | 28.5% |
| 4 角 | 19.9%（`front`） | ★**5.9%**（`far`） |
| `straight-contest` / `straight-field` | 23.6 / 19.0% | 23.6 / 19.4% |

★カットの並び（★実測・ロゴは 3 回とも点灯を確認）:

```
 5.7 start-front → 8.2 ★ロゴ「桜星賞」 → 8.5 start-front（空ゲート）
 9.6 start-rear-far → 13.1 opening-side-lead
18.0 ★ロゴ「STAR」 → 18.3 opening-formation → 19.7 opening-side-settle
26.0 side-drive → 36.9 ★ロゴ「STAR」 → 37.2 fourth-corner-far
40.0 ★ロゴ「STAR」 → 40.7 side-drive → 48.1 straight-contest → …
```

★**カット数 11 → 12。** ★減らしていません（★台帳「カット数は減らさない」は下限）。

---

## 3. 見るところ

★開発サーバー: **`http://localhost:3210/gait-review`** の ★**⑫**（★映像・止め絵・数表）。

★そのまま見る:

| URL | 何 |
|---|---|
| `/race?dev=1` | ★既定（★今回の形） |
| `/race?dev=1&corner=front` | ★**前のコーナーへ 1 手で戻す** |
| `/race?dev=1&corner=wide` | ★中間（俯瞰ワイド） |
| `/race?dev=1&cutin=off` | ★ロゴを止める |
| `/race?dev=1&cinematography=v5` | ★台本ごと前へ戻す |

---

## 4. 変えたファイル

| ファイル | 何 |
|---|---|
| `packages/render/src/race-cutin.ts` | ★ロゴの表（`raceCutInFlashAt`）・★字送り・★旧 API 削除 |
| `packages/render/src/broadcast-v2.ts` | ★`start-rear-far` / `fourth-corner-far` 追加・★`opening-formation` を引きへ・★`cornerStyle`・★境界の導出 |
| `packages/render/src/broadcast-v2-scene.ts` | ★`cornerStyle` を通す |
| `apps/web/src/app/race/page.tsx` | ★ロゴの結線・★中央大書きの削除・★白閃光の抑止・★ピンの抑止・★`?corner=` |
| `apps/web/src/app/gait-review/page.tsx` | ★⑫ を追加 |
| `packages/render/test/race-cutin.test.ts` | ★書き直し（11 件） |
| `packages/render/test/script-v6.test.ts` | ★台本の 2 件を更新＋★引きの大きさ 2 件を追加 |
| `packages/render/test/cut-transition.test.ts` | ★画角が変わる切替 3 → 6（★意図した変更） |
| `apps/cli/test/script-geometry.test.ts` | ★§5 参照 |
| `tools/measure-shot-horse-size.mjs` ★新 | ★カットごとの馬の大きさを ★**画面の矩形**から読む |
| `tools/measure-race-heap.mjs` ★新 | ★組み終えた時点のヒープ |
| `tools/lib/classification.mjs` | ★上の 2 本を READONLY に登録（R-24） |

★`npm run typecheck` 緑。★`npm test` ★**148 ファイル / 1519 件 全緑**。

---

## 5. ★テストの赤を 2 件直しました。★出所が違います

`apps/cli/test/script-geometry.test.ts`。

### 5-1. ★私の変更ではありません（★2026-09-10 から赤のまま）

★`★②の正面カットは、どの鞍でも 80m 以上ある` が ★`homestretch-front` を探していました。
★ところが ★2026-09-10 に ★別セッションが直線の②を ★**正面固定 → 真横の引き（`straight-field`）**へ
★替えており、★この検定だけが取り残されて ★`findIndex` が ★**−1 を返し続けて**いました。

→ ★**その日から今日まで、★誰も 80m を見ていません。** ★名前を追随させました。
★守る中身（★②に 80m を先に取り置く）は 1 つも変えていません。

### 5-2. ★私の変更です

★`★代用しない設定では読みません` が ★「高所斜めの素材は既定では読まない」を留めていました。
★今日、★台本 v6 の ★**3 カット**が高所斜めを描くようになったので、★成り立ちません。

→ ★`true`（★読む）へ。★同時に、★最適化そのものが生きていることを別の形で留めました
（★`diag-rear-v2` は ★今も v6 のどのカットも選ばないので ★**読まない**）。

---

## 6. ★言っていないこと・未解決

1. **★脚さばきは 1 つも直していません。** ★引いて見えなくしただけです。
   ★寄ったときの斜め前・斜め後ろは ★**今日も不合格のまま**です。
2. **★引いたコーナーが「コーナーに見えるか」は判定していません。**
   ★弧が読めるところまでは持っていけていません。★オーナーの目で見てください。
   ★据え位置を 1 度直しています（★§1 ④）が、★まだ「走路の帯を俯瞰している」に近い絵です。
3. **★素材の組が 2 → 3 に増えました。★重さは測り切れていません。**
   - ★台帳 A-11 / A-12 は「★v6 は 2 組しか描かないので残りを読まない」で ★**102MB → 76MB**。
     ★今日 ★`high-diag-v2` を ★**3 カットで描く**ようにしたので、★その前提が崩れました。
   - ★測ったこと: ★JS ヒープは ★**51.8 MB（v6）／ 51.7 MB（v5 切り戻し）**で ★**差が出ません**。
     ⚠️ ★これは「増えていない」の証明に ★**なりません**。★`performance.memory` は
     ★canvas の実体を数えないからです。★判定に使えない指標でした。
   - ★算術の上限: ★高所斜め v4 は 518×424。★12 枠 × 8 コマを原寸で焼けば ★**約 80MB**。
     ★ただし真横 v9（1536×1024）が同じ計算で 576MB になるので、★実際は縮めて焼いています。
     → ★**正味は未測定。** ★台帳「side-v9 は初期化 +31% で本線に入れない」と同じ形です。
     ★本線へ入れる前に、★焼いた画布の実バイトを数える道具が要ります。
4. **★①「ゲートを作ってください」は、★消えていたのを戻しただけ**です。
   ★新しいゲートは作っていません。★作り直しが必要かはオーナー判断です。
5. ★シード 42・桜星賞（1600m・12 頭）1 鞍の実測です。★全会場・全シードではありません。
6. ★`?corner=far` を ★**既定にしました**。★合否はオーナーの目です。
   ★`?corner=front` で 1 手で戻せます。

---

## 7. 次に聞きたいこと

1. ★⑫ の映像を見て、★②③④ は ★**この方向で続けてよいですか**。
   ★特に ★**④ のコーナー**（★引きすぎか／★コーナーに見えないか）。
2. ★① は ★**今のゲートで足りますか**。★作り直すなら、何が足りないかを教えてください。
3. ★§6-3（★素材の組が増えた重さ）は ★**測る道具から作る**ことになります。★着手してよいですか。
