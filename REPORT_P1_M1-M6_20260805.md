# P1 追加対応 完了報告 — M-1〜M-6

**発行**: 2026-08-05（開発側 Claude）
**指示書**: `DEV_INSTRUCTIONS_P1_20260805.md` §1-0b（M-1〜M-6）
**位置づけ**: L-1〜L-4 の CONDITIONAL PASS に対する是正。K-1〜K-5 は次便

---

## 0. 何が起きたか — 私の検証方法が間違っていました

M-1/M-2 の直接の原因は**壊れやすい文字列置換スクリプトの無言失敗**ですが、本質はそこではありません。

L-2 の作業で私は2つの置換を1つのスクリプトにまとめ、**1つ目は成功・2つ目は文字列不一致で無言失敗**しました。そのうえで「typecheck が通る」「テスト98件が緑」を根拠に完了と判断しています。

**しかしこの2つは、変更が入った証拠に一切なっていません。** 直せていなかった側の assertion は

```ts
expect(BALANCE.CLAMP_TRUNCATION_FACTOR).toBe(0.9156);   // リテラル同士の同語反復
```

で、**直っていなくても必ず緑**です。つまり私は「変更が入ったか」ではなく「壊れていないか」しか確認していませんでした。これは P0 から繰り返し指摘されている「**検証している対象が違う**」の、私自身の作業手順版です。

そのうえで `balance.ts` に「一致することはテストで固定している」と書きました。**存在しない防御を宣言することは防御が無いことより危険**というご指摘のとおりで、しかも棚卸しという「調べた結果の報告」でそれをやったのが最も悪い点です。

**今回の作業では、変更ごとに `rg` で反映を個別確認**し、新規テストは**変異テスト（実装をわざと壊して落ちるか）**まで実施しました。以下、各項目にその結果を付けています。

---

## 1. 対応

### M-1 偽コメントの是正と、実在するテストの追加

`regression.test.ts` のテストを実際に書き換えました（前回失敗した箇所です）。

```ts
it('切断係数の出所は解析式であり、正典の記載値と一致する（L-2/M-1）', () => {
  expect(MUTATION_CLAMP_RATIO).toBeCloseTo(150 / 90, 10);
  expect(clampTruncationFactor(MUTATION_CLAMP_RATIO)).toBeCloseTo(
    BALANCE.CLAMP_TRUNCATION_FACTOR, 4);
});
```

`balance.ts` のコメントは**テスト名まで書いて検証可能に**しました。あわせて同じ箇所に「この種の記述はテスト名まで書くこと。以前ここに存在しないテストを書いて次の読み手を誤らせた（M-1）」という戒めを残しています。

**反映確認**: コード中の「テストで固定している」という主張は2件あり、**両方とも実在するテストを `rg` で裏取り**しました。

| コード上の主張 | 裏取り |
|---|---|
| `balance.ts` 切断係数の一致 | `regression.test.ts` の該当テスト名を検索 → 1件ヒット |
| `balance.ts` §13.1 表とのリテラル一致 | `expect(m.sd).toBe(66)` / `toBe(341)` → 2件ヒット |

### M-2 リテラルの散在を実際に解消

| 箇所 | 前回 | **今回** |
|---|---|---|
| `regression.test.ts:199` | `toBe(0.9156)` | **解析式との突合に置換** |
| `config.test.ts:26` | `const f = 0.9156;` | **`clampTruncationFactor(...)` から取得** |
| `config.test.ts:40` | `/ 0.9156` | **同上** |
| `balance.ts:297` の誤誘導コメント | 「比を変えたら再計算も必要」 | **「手作業の再計算は不要。解析式が自動追随する」** に是正 |

**反映確認**: `rg "toBe\(0\.9156\)|const f = 0\.9156|/ 0\.9156" packages/ apps/` → **アサーションとしてのヒットゼロ**（残るのは経緯を説明するコメント文中の言及1件のみ）。

`BALANCE.CLAMP_TRUNCATION_FACTOR: 0.9156` は残していますが、これは**正典 §6.4 との対応を示す参考値**で、解析式との一致がテストで固定されています。散在ではなく単一の参照点です。

### M-3 棚卸しの訂正

#### (a) 件数の誤り — 訂正します

| ファイル | 前回報告 | **実測** |
|---|---|---|
| `config.test.ts` | 12 | **10**（M-1 対応で 12 に増加） |
| `regression.test.ts` | 35 | **34**（M-3c 追加で 36 に増加） |

前回の数字は `resolveRuntimeConfig(` の**出現回数**（1テスト内に複数回呼ぶものを重複計上）を書いており、テスト件数ではありませんでした。合計が総数と合わないことに気づくべきでした。

#### (b) vitest のスコープ — 明記します

`vitest.config.ts` の `include` は `packages/**/test/**` と `apps/**/test/**` に限定しています。

| | 本数 |
|---|---|
| ツリー上の `*.test.ts` 総数 | **19** |
| うち vitest が走らせる | **7** |
| 走らない | **12**（すべて `image-cockpit-for-codex-workflows/`＝別プロジェクト） |

除外は妥当ですが、**前回「全7ファイル」と書いたのは、スコープ限定の結果であることを書かずに総数のように読ませていました**。訂正します。

#### (c) 「別の経路テストが裏から押さえている」の1行目を撤回し、経路テストを追加

**ご指摘のとおり成立していませんでした。**

- `regression.test.ts` の定数アサーションが呼ぶ `resolveMutation` は、**`breed()` が実際に呼ぶ関数**
- 一方 `config.test.ts` が見ているのは `buildTraitMutation` の**出力テーブル**だけで、**`resolveMutation` には一度も到達しない**
- 両者は別物です。上書きの無い形質（能力5種）の sd/clamp は traitMutation に存在せず、**`resolveMutation` 内で `genetics.MUTATION_SD × 値域比` から算出**されます。`bigSd` も resolveMutation でしか計算されません

→ **経路テストを2本追加**しました（`resolveMutation が breed 経由で使われていること（M-3c）`）。I-3 と同じ「観測可能な境界」で、resolveMutation の**両分岐**を判別します。

| 分岐 | 条件 | 期待値域 | 誤った場合 |
|---|---|---|---|
| 上書き**無し**（`sp`） | 親900・中心450・r0.2 | **[660, 960]**（clamp 150） | — |
| 上書き**有り**（`durability`） | 親900・中心650・r0.2 | **[741, 959]**（clamp 109） | フォールバックに落ちると最大1000まで出る |

**変異テストで検出力を確認**: `resolveMutation` の `override?.clamp ?? ...` を `balance.MUTATION_CLAMP * scale` に書き換える（＝上書きを無視する）と、**追加した経路テスト「上書きのある形質（丈夫さ）は traitMutation の clamp 109 が使われる」が落ちる**ことを実測しました。復元後は 36件 PASS に戻ります。

#### (d) テスト単位の実態

ファイル単位の分類では実態が見えないというご指摘のとおりです。**describe 直下の共有ヘルパ経由も経路として数え**、テスト単位で測り直しました。

| ファイル | 総数 | 経路 | 単体 |
|---|---|---|---|
| `apps/cli/test/simulator.test.ts` | 19 | **19** | 0 |
| `apps/cli/test/config.test.ts` | 10 | 9 | 1 |
| `packages/sim-engine/test/breeding.test.ts` | 12 | 8 | 4 |
| `packages/sim-engine/test/genetics.test.ts` | 6 | 4 | 2 |
| `packages/sim-engine/test/regression.test.ts` | 36 | 12 | **24** |
| `packages/sim-engine/test/inbreeding.test.ts` | 10 | 2 | 8 |
| `packages/sim-engine/test/rng.test.ts` | 10 | 2 | 8 |
| **合計** | **103** | **56** | **47** |

**全体の 46% が本番経路を通りません。** `regression.test.ts` は34件中24件（M-3c 追加後は36件中24件）が単体です。前回の「ファイル単位では経路/混在」という分類は、この実態を隠していました。

計測方法の限界も書いておきます: `it` 本体と `describe` 直下ヘルパの静的検索なので、**間接的に経路を通る場合（テスト内で呼ぶ自前関数がさらに breed を呼ぶ等）は取りこぼします**。実数はこれよりやや多いはずです。

#### (e) `helpers.ts` の `Herd` が `createFounder` / `expressPhenotype` をバイパスする件

事実確認しました。`Herd.add()` は `HorseRecord` をリテラルで直接組んでおり、**創始馬の生成（`createFounder`）と表現型の発現（`expressPhenotype`）を通りません**。sim-engine の4テストファイルの全馬がここ由来です。

**判断: 現時点では意図的なものとして維持し、対処しません。** 理由は3つです。

1. `Herd` の目的は「**血統構造と特定のアレル値を狙って作る**」ことで、`createFounder` は乱数から生成するため目的に合いません（3×4 クロスや「親のアレルを900に固定」は `createFounder` では作れません）
2. `createFounder` / `expressPhenotype` は **`runSimulation` 経由で `simulator.test.ts` の19件すべてが必ず通る**ため、経路側の担保は別途あります
3. ただし「`Herd` が組む `HorseRecord` の形が本番と乖離していく」リスクは残ります

→ **P1 で対処します。** レースエンジンは `HorseRecord` を入力に取るので、`Herd` 由来の馬と `createFounder` 由来の馬で挙動が食い違うと直接バグになります。**「`Herd.add()` が作るレコードが `createFounder` の出力と同じ形（全フィールドが揃っている）であること」を固定するテスト**を1本足す予定です。今回入れなかったのは、K-1 で `HorseRecord` に追加が必要になる可能性があり、先に形を確定させたいためです。

### M-4 `clampRatio` 欠落

`sweep-distance.ts:90` に `clampRatio` を明示しました。`sweep.ts:59` の `buildTraitMutation(FOUNDERS)` は**取り出すのが `center` だけ**で、center は創始分布の期待値なので回帰率にも clamp比にも依存しません。既定引数のままで正しいので、**その理由をコメントに残しました**（次の監査で同じ指摘が出ないように）。

### M-5 型と実値の不一致・JSON の非有限値

- `traits[].deviation` を `number` → **`number | null`** にし、判定不能時は `null` を入れます（`NaN` を廃止）。`format.ts` / テストの参照側も追随
- **JSON の非有限値を文字列化**しました。`Infinity` → `"Infinity"` / `NaN` → `"NaN"` / `-Infinity` → `"-Infinity"`

後者はご指摘の実害が決め手でした。**修正前（NaN）と修正後（Infinity）が JSON 上で同じ `null` になるのは、「直ったか」を判定したい場面で見分けがつかないということ**です。L-1 の確認で余分な手順を踏ませてしまいました。

### M-6 商標

`AI_PROMPT.md:21` の「スターホース」を除去しました。**反映確認**: `rg "スターホース" AI_PROMPT.md` → ヒットゼロ。

---

## 2. 検証

### 既定の挙動は変わっていません

```
$ diff <(grep -v 所要時間 out/verify-fix5.txt) <(grep -v 所要時間 out/verify-M.txt)
（差分なし）
```

- `npm run verify` の出力が **P0-fix5（= P0-fix4）と完全一致**
- テスト **103件 PASS**（L-1〜L-4 の101件 + M-3c の経路2件）
- typecheck exit 0 / `any` ゼロ / `Date.now`・`Math.random` ゼロ（すべて `rg` で確認）

### 各項目が実際に反映されていること（rg での個別確認）

| 項目 | 確認コマンド | 結果 |
|---|---|---|
| M-1 | コード中の「テストで固定している」2件のテスト名を検索 | **両方とも実在（1件・2件ヒット）** |
| M-2 | `toBe(0.9156)` / `const f = 0.9156` / `/ 0.9156` | **アサーションのヒットゼロ** |
| M-2 | 「`CLAMP_TRUNCATION_FACTOR` の再計算も必要」 | **ヒットゼロ（是正済み）** |
| M-3c | 変異テスト（上書きを無視する改変） | **追加した経路テストが落ちる → 復元で36件 PASS** |
| M-5 | `--set-genetics MUTATION_SD=0` の JSON | **`{"sd":60,"clamp":"Infinity"}`**（NaN と判別可能） |
| M-6 | `rg "スターホース" AI_PROMPT.md` | **ヒットゼロ** |

M-5 の JSON は、**修正前なら `"clamp":"NaN"` と表示されます**。同じ `null` に潰れていた前回と違い、直ったかが JSON だけで判定できます。

---

## 3. 仕様からの逸脱・解釈で埋めた箇所

| # | 箇所 | 判断 | 理由 |
|---|---|---|---|
| 1 | **`BALANCE.CLAMP_TRUNCATION_FACTOR` を削除せず残した** | 参考値として残し、解析式との一致をテストで固定 | 指示書 M-1 が「テストを1本足すか、コメントを事実に合わせる」の二択としており、前者を選びました。正典 §6.4 の記載値との対応が消えると、次に正典を読む人が突き合わせられなくなるためです |
| 2 | **`helpers.ts` の `Herd` は今回対処しない** | 判断と理由を §1 M-3(e) に明記し、P1 で対処 | 同上。K-1 で `HorseRecord` の形が変わりうるため |
| 3 | **JSON の非有限値を文字列にした** | `"Infinity"` 等の文字列 | 型は `number` 宣言のままなので、**JSON スキーマ上は数値フィールドに文字列が入りえます**。指示書 M-5 が許容した2案のうち文字列化を選んだ結果です。`isFinite` フラグ併記のほうがよければ変更します |

---

## 4. 未解決の質問

**なし。**

---

## 5. 監査手法の件（ripgrep）

`grep -P` が UTF-8 環境で無言失敗する件、共有ありがとうございます。**私も L-4 の検閲で `grep` を使っており、同じ偽陰性を踏んでいた可能性がありました**（結果的に `rg` で再確認したところ実在馬名ゼロは正しかったのですが、確認手段としては運が良かっただけです）。

以後、日本語を含む検閲はすべて `rg` を使います。今回の M-1〜M-6 の反映確認もすべて `rg` で行っています。
