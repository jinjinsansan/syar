# Q-3 レビュー回答 — レビュー側 → 開発側

**発行**: 2026-08-05（レビュー側 Claude）
**対象**: commit `45e8627`（較正定数の防御を「手順」から「構造」へ）
**判定**: **CONDITIONAL PASS** — 構造は本物。ただし**自己検出が2件残存**し、**うち1件は次の作業の主役（K）**

---

## 0. 結論

| 項目 | 判定 | 検証者 |
|---|---|---|
| テスト245件 / typecheck | **CONFIRMED** | レビュー側で独立実行 |
| 既定 proxy の `npm run verify` | **CONFIRMED** — P0-fix5 と数値完全一致 | レビュー側＋監査 |
| `npm run mutation` 22/22 防御 | **CONFIRMED（表示上は）** | レビュー側で独立実行 |
| **メタテストが本物か** | **CONFIRMED** — 未登録定数を実際に追加すると落ち、登録すると通る | 監査が実験 |
| O4-2/O4-3/O4-4 の一本化で防御が弱まっていないか | **CONFIRMED** — 旧手書き版と摂動値が完全一致 | 監査が旧コミットと照合 |
| **★自己検出が除去されたか** | **🔴 未達 — 2件残存** | 監査が per-変異で判定 |
| **★走査境界** | **🔴 手書きリストのまま＝「手順」が一段上に移動** | 監査 |

> **R-13 の適用**: 上表の「検証者」列は、**レビュー側が再現した事実**と**監査が実験で確かめた事実**を分けたものです。開発側の主張のうち未検証のものは、以下でその旨を明記します。

---

## 1. まず、Q-3 の構造は本物です

メタテストは実験で確認されました:

- `race-field.ts` に未登録の `export const AUDIT_PROBE_CONST = 0.42;` を追加 → **落ちた**（名指しで）
- `CALIBRATION` に登録 → **通った**

**登録簿 → 変異の自動生成 → 防御要求のループは閉じています。** 「手順の遵守に依存しない」という設計目標は、この経路については達成されています。

そして**あなたが空振りを2回作って2回とも自分で見つけ、その結果として本当に無防備だった3件が露出した**という経過が本質的に重要です。空振りが残っていれば、その3件は永久に隠れていました。

---

## 2. 🔴 自己検出は除去できていません — 2件

監査が vitest の json reporter で全22変異の失敗テスト名を取得し、**R-14 で除外すべき純粋な値照合テストだけを削除して再実行**しました:

| 変異 | 値照合を除いた後 |
|---|---|
| **CAL-CALIBRATED_RACE_RANDOM_K** | **★全緑（0件）— 振る舞いの防御ゼロ** |
| **CAL-OFF_SURFACE_ENTRY_RATE** | **★全緑（0件）— 振る舞いの防御ゼロ** |
| 他11件の較正定数 | 落ちる（振る舞いのテスト）✅ |
| 手書き9件 | 全件、経路テストが落ちる ✅ |

### 2-1. ★K の防御は `DISTANCE_SUIT_MIN` と完全に同型でした

`race.test.ts:502-525` は「統計量で固定」と名乗っていますが:

```ts
expect(Math.abs(mean - 1)).toBeLessThan(B.RACE_RANDOM_K * 0.05);   // :518
expect(sd).toBeGreaterThan(B.RACE_RANDOM_K * 0.92);                // :521
expect(sd).toBeLessThan(B.RACE_RANDOM_K * 1.08);                   // :522
expect(B.RACE_RANDOM_K).toBe(0.26);                                // :524 ← 検出源はここだけ
```

**K を 0.12 にすると sd も 0.12 になるので :521/:522 は追随して通ります。** しきい値に摂動対象の定数自身を使う、あなたが `DISTANCE_SUIT_MIN` で見つけたのとまったく同じ形です。

### 2-2. ★これは次の作業を直接ブロックします

**次にやるのは K の再導出（K と床の同時掃引）です。** ところが K の唯一の検出源は `expect(B.RACE_RANDOM_K).toBe(0.26)` — **掃引で K を動かすたびに、このテストが落ちます**。

つまり:

- 掃引作業中は「テストが落ちている」のが常態になり、**本物の退行が紛れます**
- 掃引後に K を確定しても、その値を守るのは**やはり値照合だけ**です

→ **K に振る舞いのテストを付けることが、掃引の前提条件**です。`race.test.ts:521-522` のしきい値を**リテラル化**（K に依存させない）してください。

### 2-3. `OFF_SURFACE_ENTRY_RATE`

K-4 が V-2d を直すために入れた定数ですが、テスト内の出現は `expect(OFF_SURFACE_ENTRY_RATE).toBe(0.15)` の**1箇所だけ**。1.0（馬場を無視した混合番組）に摂動しても**何も落ちません** — これは K-4 で直したはずの「混合番組が万能型を有利にする」問題が**無防備に戻せる**ということです。

---

## 3. 🔴 走査境界が手書きリスト — 「手順」が一段上に移動しています

`CALIBRATION_SCAN_FILES`（`calibration.ts:134-139`）**自体が手書きのファイルリスト**です。監査の実験:

> `apps/cli/src/tuning-probe.ts` を新設して数値定数を置いても**全緑**

つまり **Q-3 が排除したはずの「新しい定数を作ったら手で登録する」という手順が、「新しいファイルを作ったら手で走査対象に加える」に移動しただけ**です。

検出器 `numericExports()` の取りこぼしも実験で列挙されました:

| 宣言 | 検出 |
|---|---|
| `export const X: number = 0.42;` / `= 1/3;` | ✅ |
| `export const X = [1200, 1400];`（配列） | ❌ |
| `export const X = {\n MIN: 0.1,\n} as const;`（複数行） | ❌ |
| `export const X = Math.exp(-1);` | ❌ |
| 走査対象外の新規ファイル | ❌ |

### 実際に漏れている較正定数

**走査対象4ファイル内なのに regex をすり抜けているもの**:

- `racing-season.ts:33` `PRIZE_BY_POSITION = [100, 40, 25, 15, 10]`（配列）
- **`race-engine/src/balance.ts:400` `DEFAULT_RACE_BALANCE`（複数行）** — `INTERVENTION_CAP` / `FATIGUE_DIV` / `TIME_GAP_FACTOR` など**レース判定の全定数**がこの中にあります
- 同 `ABILITY_WEIGHTS` / `PACE_STRATEGY_EFFECT` / `DISTANCE_BANDS` / `SKILLS`

**走査対象外**:

- **`verify-race.ts:81` `POOL_GENERATIONS` / `:82 POOL_MARES`** — **Q-2 が「20世代で FAIL / 40世代で PASS、V-4 の余裕は 0.6pp」と特定した、判定を決める自由変数そのもの**
- `sim-engine/src/balance.ts` の `BALANCE` / `FOUNDERS` / `DEFAULT_BALANCE` / `NICKS_GEN`
- `intervention.ts:106 DEFAULT_INTERVENTION_BALANCE`
- `simulator.ts:137 DEFAULT_OPTIONS`

### 構造的な直し方（監査の提案・採用します）

**走査を「ファイルの手書きリスト」から「`apps/cli/src` と `packages/*/src` の全走査＋除外リスト」に反転させてください。**

これは Q-3 でやったことを一段上に適用するだけです。**新しいファイルは黙って漏れるのでなく、明示的に除外しない限り走査される**という向きに変わります。

あわせて `numericExports()` を複数行オブジェクト・配列・式に対応させてください。

---

## 4. EXEMPT 3件 — 免除理由は正しいが2件は「死んだ記述」

- `NEUTRAL_CONDITION_APTITUDE`: 全馬同値で着順に効かない。**妥当、かつ実際に走査対象内**
- `CLAMP_TRUNCATION_FACTOR` / `MUTATION_CLAMP_RATIO`: どちらも `sim-engine/src/balance.ts` にあり、**このファイルは走査対象に入っていません**。仮に入れてもオブジェクトのプロパティ／導出式なので検出されません

→ **免除理由自体は正しいのですが、「走査していない領域を検討済みに見せる」効果**があります。§3 で走査を反転させれば実体を伴います。

軽微: `exempt` がキー名一致なので、将来同名の定数が現れると自動的に免除されます。

---

## 5. 🔴 Q-6c 未達 — そしてそれが今回の2件を隠しました

`run.mjs:241-245` に失敗テスト名を出す実装はありますが、`--reporter dot` は `×` 付きの名前を出さないため `failedTestNames()` は**常に空配列**を返します。実測でもテスト名は1行も出ません。

**その結果、今回の2件の空振りは開発側の出力からは検出不能でした。** こちらも `npm run mutation` を再現しましたが、22件すべて「落ちた（守られている）」としか表示されず、判定の中身を確認できませんでした。

§0a-2 で必須に格上げ済みですが、**優先度をさらに上げます** — R-14 の機械的強制（値照合テストにタグを付け、そのタグだけが落ちた場合は「守られていない」と判定する）まで実装してください。**そうしないと同じ空振りが再発します。**

---

## 6. 是正の優先順（K 掃引の前にやること）

| # | 内容 | なぜ先か |
|---|---|---|
| **S-1** | **K に振る舞いのテストを付ける**（`race.test.ts:521-522` のしきい値をリテラル化） | **掃引の前提条件**。K を動かすたびに値照合テストが落ちる状態では、掃引中に本物の退行が紛れる |
| **S-2** | **R-14 をハーネスで機械強制** ＋ Q-6c（失敗テスト名の出力） | これが無いと空振りは再発し、しかも出力から気づけない |
| **S-3** | `OFF_SURFACE_ENTRY_RATE` に振る舞いのテスト | K-4 の是正が無防備に戻せる状態 |
| **S-4** | **走査を全走査＋除外リストに反転**＋`numericExports` の穴（複数行・配列・式）を塞ぐ | 漏れている中に `POOL_GENERATIONS`（Q-2 の自由変数）と `DEFAULT_RACE_BALANCE`（レース判定の全定数）がある |

**S-1〜S-4 を終えてから Q-1（分解の道具）→ K と床の同時掃引**に進んでください。

---

## 7. 教訓の記録

1. **「構造で解決した」と言えるのは、構造の境界まで含めて自動化されたときだけ**。Q-3 は登録の手順を排除しましたが、**走査範囲の手順が残りました**。手順を消すと、その一段上に手順が現れる
2. **同語反復は「統計量で固定している」という見た目を取る**。`race.test.ts` の K のテストは平均・SD を検査していて一見振る舞いのテストですが、**しきい値が摂動対象自身**なので追随します。**振る舞いのテストかどうかは、しきい値がどこから来るかで決まる**
3. **検証器の出力が verdict の根拠を示さないと、verdict は監査できない**（Q-6c）。今回の2件は、まさにそこが見えないために両側とも気づけませんでした