# REPORT — 順位表の読めなさ：原因報告（2026-08-25）

指示書: 最終修正指示「順位表の描画順」
★指示の §適用範囲「原因箇所を報告して停止する」に従い、**描画順の修正は実装していません。**
理由は下記のとおり、**描画順はすでに正しく、直す対象が別**だからです。

---

## 1. 結論

**描画順の問題ではありません。** 順位表は 1 つの部品として、馬より後に、
背景・枠・見出し・順位・枠色・馬名・着差まで**一体で**描かれています。

読めなくなる原因は **`ctx.globalAlpha` による意図的な減光**です。
`apps/web/src/app/race/page.tsx:2009-2012`

```ts
const closeUp = Math.max(0, Math.min(1, (v2HorseRatio - 0.28) / (0.45 - 0.28)));
const ease = closeUp * closeUp * (3 - 2 * closeUp);
const prevAlpha = ctx.globalAlpha;
ctx.globalAlpha = prevAlpha * (1 - 0.6 * ease);   // ← 画面に占める馬が大きいほど薄くする
drawStandings(...);
```

すぐ上のコメントに、この規則を入れたときの実測が残っています。

> 実測の画面比: 発走 24% ／ 4角正面 24% ／ ゴール 26% ／ 先頭争い 34%
> ／ 勝負所 41% ／ **直線の寄り 53%** → 28% までは通常、45% で最も薄く。

旧 v4 の直線は `homestretch-front`（馬が小さい・画面比 24% 前後）だったので、この減光は
ほとんど発動しませんでした。`homestretch-side` は馬が大きく、画面比が 45% を超えるため、
**いちばん馬が大きい瞬間に順位表が最も薄い状態（40%）になります。**

---

## 2. 実測（それが減光であることの証拠）

同じ時刻（seed 42 / f0687 / 22.9s）の同じ画素を、旧 v4 と既定 starhorse-v1 で比べました。

| 画面座標 | 何があるか | 旧 v4 | 既定 starhorse-v1 |
|---|---|---|---|
| (950,100) | **順位表自身の金色の「1」** | `229,205,115` | `107,99,62`（**47%**） |
| (1000,200) | **順位表自身の金色の文字** | `248,216,43` | `128,99,29`（**52%**） |
| (1210,48) | 見出し右・馬は居ない | `16,18,15` | `25,30,33`（明るい） |
| (1000,60) | 1 位行の上・馬は居ない | `19,23,22` | `36,46,38`（明るい） |

**順位表自身の金色が半分の明るさになっています。** 馬が前に出ているなら、順位表の色は
変わらず隠れるだけのはずです。また**馬が居ない位置まで明るくなっている**のは、
表全体が薄くなって背景が透けているからです。＝ 前後関係ではなく不透明度の問題です。

---

## 3. 実画面の 5 点（フラグなし `/race`・seed 42）

| 点 | 表示秒 | 順位表 1〜5 行 |
|---|---|---|
| 直線入口 | 12.8s | **すべて読める**（減光なし） |
| 進行 86% | 22.9s | **読めない**（表全体が約 40%） |
| 進行 88% | 24.7s | **読めない**（表全体が約 40%） |
| 進行 93% | 29.3s | **すべて読める**（減光なし） |
| finish-line | 32.9s | **すべて読める**（減光なし） |

読めないのは 86〜88% 付近だけで、直線入口とゴール前は正常です。

---

## 4. なぜ実装せずに止めたか

指示の「変更しないもの」に **透明度** が入っています。
上記のとおり、効く直し方は減光の規則（`1 - 0.6 * ease` の係数か、45% という閾値）を
変えることだけで、これは透明度の変更そのものです。

一方、指示された「前景 HUD レイヤーへ移す」修正は、すでにそうなっているため**何も変わりません**。

- `drawStandings`（`packages/render/src/oblique-ui.ts:185`）は
  背景・枠・見出し・順位・枠色・馬名・着差を**1 つの関数の中で連続して**描いています。
  背景と文字の間に馬が入る構造にはなっていません。
- 呼び出し順も `page.tsx` で 場面（1760 / 1833）→ HUD（2013）です。

大規模な HUD 再構成も不要です。**必要なのは 1 行の数値判断**で、それが禁止事項に当たるため、
判断をお返しします。

### 直すとしたら選べる形（いずれも透明度の変更に当たります）

1. 減光の下限を上げる … `1 - 0.6 * ease` → 例 `1 - 0.25 * ease`（薄くはするが読める濃さで止める）
2. 減光をやめる … 馬が大きいカットでも 100% のまま（馬に被る面積は増えます）
3. 減光の代わりに順位表の地だけ濃くする … 文字は減光せず、地の `HUD.glass` を上げる

★どれを採るかは決めていません。

---

## 5. 併せて行ったこと

- `packages/render/src/broadcast-v2-scene.ts` の古いコメントを実態へ訂正
  （`既定は 'starhorse-v1'。'v4' は URL による旧台本への切り戻し用`）。**動作は変えていません。**

## 6. 検証

- `packages/render/test/starhorse-script-v1.test.ts` 12 件パス
- `npm test` 112 ファイル / 1121 件パス
- `npm run typecheck` エラーなし
- `npm run build:web` 成功
- `/race` 200 ／ `/race?cinematography=v4` 200
- `apps/web/package.json` のポート 3210 保持
- commit・push なし（HEAD は `93d270c`）
