# REPORT_P3_B6_20260811 — B-6 配線と、その過程で見つかった構造上の乖離

## 本便で正典の改訂が要る決定をしたか

**していません。ただし裁定が要る事項が2つあります。**

1. **配線の結果 V-4 / V-5 が帯を外れました。** D-050 の完了条件 #4 に従い、
   **較正せず報告**します。`RACE_RANDOM_K` には一切触れていません。
2. **★オッズを計算した馬と、実際に走る馬の能力が違います**（実測 **2.28倍**）。
   B-6 の作業中に見つかったもので、**配線とは別の、より重い問題**です（§4）。

---

## 1. ★`docs/B6_WIRING_PLAN.md` の記述を2点訂正します

こちらが書いた計画書に、**事実と違う記述が2つ**ありました。

### 訂正① 「全馬が condition 3 / fatigue 0」

**condition は一律ではありませんでした。** `generateRace` が以前から
`condition: rng.int(2, 4)` を渡しています。実際に全馬で同一だったのは
**`fatigue`（`toEntrant` の `?? 0`）だけ**です。

★ただし `fatigue` は `fatigueCoef` と `initialStamina` の**両方**に入るので、
「疲労という分散源が丸ごと欠けていた」という結論自体は正しいままです。

### 訂正② 「MC と本番確定は同じ entrants を使うので、乖離は構造上起きません」

**これは誤りでした。** 出走馬は**2か所で別々に組み立てられています**:

| | 生成時（出走表・オッズ） | 確定時（着順） |
|---|---|---|
| 場所 | `apps/cli/src/race-field.ts` の `toEntrant` | `apps/worker/src/pg-store.ts` の `settleRace` |
| 能力 | `potential × rng.range(0.55, 0.85)` ★レースごとに再抽選 | `horses.stats`（DB の列） |
| condition | `rng.int(2, 4)` | **3（直書き）** |
| fatigue | **0** | **0（直書き）** |

★「同じ `entrants` を使う」は `buildRace` の**中**では正しいのですが、
**確定は別のプロセス・別のコードで組み直しています**。
片方だけ実データにすると、**オッズを計算した馬と実際に走る馬が変わります。**

---

## 2. 配線したもの

| 場所 | 内容 |
|---|---|
| `race-field.ts` | `generateRace` に `trainingStateOf` の口を追加。★渡さなければ従来どおり（既定で実データに化けさせない） |
| `pg-store.ts` | `settleRace` が `horses.condition` / `horses.fatigue` を読む（0010 の列） |
| `main.ts` | **毎周** `loadTrainingStates` を読み直す |
| `verify-race.ts` | `--b6-wired` のときだけ、`advanceWeek` を実際に回して得た (調子, 疲労) を使う |

### ★母集団と違って、調子・疲労は毎周読み直します

`pool` は「日次バッチでしか変わらない」ので起動時に1回でよいのですが、
**調子・疲労は週ごとに変わります**。ここを1回にすると、ワーカーが起動した週の値のまま
何日も走り続けます。★**「読んでいるのに古い」は、読んでいないより気づきにくい形**です。

### ★仮定値を別の仮定値に置き換えていません

ハーネスで「疲労は 0〜60 の一様分布」などと**分布を発明**すると、
配線したつもりで**別の仮定値に差し替えただけ**になります。
→ **`advanceWeek`（較正済みの週ループ）を実際に回して**、その馬が現役期間に取った値を使います。
測定条件（方針=バランス型 / デビュー104週以降 / イベント有）は `B6_SAMPLING` に固定し、
較正定数の登録簿に理由つきで登録しました。

---

## 3. ★配線後の P1 ゲート（D-050 完了条件 #2）

**3条件で測りました。** 1条件だけ測ると、動いた理由が分かりません。

| | 配線前 | 配線・同期あり | 配線・位相ずらし | 帯 | 判定 |
|---|---|---|---|---|---|
| **レース内の疲労SD** | **0** | **8.13** | **19.04** | — | — |
| **V-4** 1番人気の勝率 | 31.29% | 34.02% | **34.6%** | 30〜34% | **★FAIL**（+0.6pp = 3.1 SE） |
| **V-5** 1番人気の複勝率 | 62.4% | 64.98% | **65.83%** | 60〜65% | **★FAIL**（+0.83pp = 4.3 SE） |
| **V-6** 下位3ランクの勝率 | 1.21% | 1.16% | 1.06% | 0.5〜2% | PASS |
| **V-8** AI代行÷手動最適 | 94.57% | 93.53% | 93.52% | 93〜97% | PASS |
| **V-13** 仕掛けの巧拙 | 0.1999 | 0.1699 | 0.1697 | 有意差 | PASS |
| **総合** | PASS | FAIL | **FAIL** | | |

**配線前の値は文書の記録と完全に一致**しました（V-4 31.29% / V-5 62.4% / V-6 1.21% /
V-8 94.57% / V-13 0.1999）。**D-049 は V-4/V-5/V-6 を動かしていません**。

生出力: `docs/MEASURE_P1GATES_B6_BEFORE.txt` / `docs/MEASURE_P1GATES_B6_AFTER.txt`

### ★なぜ上がったのか（完了条件 #3）

**3点が単調に並びます。** レース内の疲労SD 0 → 8.13 → 19.04 に対し、
V-4 は 31.29% → 34.02% → 34.6%。

> **オッズ計算の MC は、`condition` も `fatigue` も見ています。**
> `fatigueCoef = 1 − fatigue/500`、`conditionCoef` は condition 1..5 → 0.88..1.1。
> **見えている量に分散を足すと、1番人気はより見分けやすくなります。**

1番人気の勝率を**下げる**のは、`RACE_RANDOM_K` のような**オッズ側から見えない**分散だけです。
★「分散源を足したのだから1番人気は弱くなるはず」という直感が、ここでは逆でした。

### ★私の最初の仮説は反証されました

同期あり版で V-4 が 34.02% に上がったとき、こう考えました:

> 全馬が同じ調教サイクルの同じ位相に並んでいる。だからレース内の差が消えて
> 1番人気が強くなった。位相をずらせば戻るはずだ。

**位相をずらしたら 34.02% → 34.6% と、さらに上がりました。** 予測と逆です。

位相をずらす版は**残しました**。馬は生まれた週もデビューの週も違うので、
**そちらが正しい模型**だからです。★**「直ったから残す」のではありません。仮説は外れました。**
レース内の疲労SD（8.13 → 19.04）だけは予測どおりに動いており、
**その量が増えるほど V-4 が上がる**という関係が、3点で確認できます。

### ★較正していません

D-050 完了条件 #4:「動いたぶんが帯を外れるなら、**較正ではなく報告**（K の較正は D-021 の管轄）」。
`RACE_RANDOM_K = 0.22` は正典 §13.1 の値で、**触っていません**。

---

## 4. ★より重い発見 — オッズを計算した馬と、実際に走る馬の能力が違います

`tools/diag-b6.mjs`（判定を出さない診断）で staging の実データを測りました。

```
【① 能力（SP）】オッズ計算時 ÷ 確定時
  出走馬 254 頭ぶん（20 レース）
  比の平均 2.2754  SD 0.3149
  比の範囲 1.584 〜 2.996
  ★ずれの平均（絶対値）: 127.5%
  ★比が 1.00 ±1% に入る馬: 0 / 254 頭
```

### 原因

- `horses.stats` は **誕生時**の能力です（`phenotype.ts`:「初期 stats = potential × 素質開放率（§6.1-8: 0.28〜0.35）」）
- `toEntrant` は **`PLACEHOLDER_UNLOCK` = 0.55〜0.85 を再抽選**します
  （そのコメント自身がこう書いています: 「⚠️ P3 で調教モデルに置き換わる仮の値。
  `INITIAL_UNLOCK_MIN/MAX`（0.28〜0.35）は**デビュー時**の開放率で、
  レースを走る現役馬の水準ではない」）

★つまり**どちらも「現役馬の能力」ではありません**。
生成側は仮置き、確定側はデビュー時の値です。**比 2.28倍はその差**です。

### ★これは「配線し忘れ」ではなく、**週送りがまだ全馬に対して回っていない**ことの現れです

`advanceWeek` を通せば `horses.stats` は訓練後の現在値になります
（B-1 の実測で開放率 99%、V-14 のバランス型で 90.3%）。そうなれば:

- 生成側の仮置き 0.55〜0.85（平均 0.70）
- 確定側の実データ 0.903（V-14 バランス型）
- → **比 0.78。向きは逆転しますが、乖離は残ります**

**`PLACEHOLDER_UNLOCK` を廃し、両方が `horses.stats` を読むのが正しい形**です。
ただしこれは **K の較正の土台を変えます**（ハーネスの `UNLOCK` も同じ定数を使っています）。
D-050 #4 の「K の較正は D-021 の管轄」に当たるので、**こちらでは実施していません。**

### 影響範囲（過大に言わないための注記）

- **P1 のゲート（V-4 等）は影響を受けていません。** ハーネスは生成・確定とも
  同じ `entrants` を使うので、内部では整合しています
- **影響するのは本番（ワーカー＋DB）の経路だけ**です。
  そこでは**プレイヤーが見るオッズが、実際に走る馬とは別の能力で計算されています**
- 現在の本番には**合成ベッターしかいません**（実プレイヤー未参加）。
  ★ただし **P2 で本番 DB を使って出した証拠は、この乖離のある経路の上のもの**です

---

## 5. 照会

| # | 内容 |
|---|---|
| **Q-P3-29（新規・最優先）** | **`PLACEHOLDER_UNLOCK` を廃し、生成・確定の両方が `horses.stats` を読む形にしてよいか。** K の較正の土台が変わるため、こちらでは実施していません。実施するなら、その前に**全馬に週送りを回す**必要があります（さもないと確定側がデビュー時の値のままです） |
| **Q-P3-30（新規）** | **V-4 / V-5 が帯を外れたことの扱い。** 較正せず報告しています。帯を動かすのか、K を動かすのか、`B6_SAMPLING` の測定条件を変えるのか |
| Q-P3-27〜28 | 前便のまま（気性の下限比率の上側基準 / V-15 の①②で最悪方針が逆向き） |
| Q-P3-20〜26 | 未回答 |

---

## 6. 未着手

| 項目 | 状態 |
|---|---|
| **G-7** の staging 実証 | staging にレースが無く未実証（実装は P2 完了） |
| `verify-prize` / `verify-cancel` / `verify-economy` | 同上（前提が揃わず exit 2） |
| **B-3** V-11 の構造報告 | 未着手 |
| **D-043** の残り | 検証ツール1本をバンドルに対して流す |

★レース生成を staging に通す作業は、**Q-P3-29 の裁定を待ってからにします。**
いま生成すると、**乖離のあるオッズを持つレース**が staging に積み上がり、
それを使って G-7 を「実証」することになります。

---

## 7. 再現コマンド

```bash
npm run typecheck && npx vitest run              # 600件 緑
npm run verify:race                              # 配線前（★比較用に残してあります）
npm run verify:race -- --b6-wired                # 配線後
npx tsx tools/diag-b6.mjs --env staging          # 能力の乖離
```
