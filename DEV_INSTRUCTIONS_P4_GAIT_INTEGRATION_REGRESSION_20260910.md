# 開発指示書 — 4頭検証台から桜星賞への組み込みで変わった走行描画を修正する

発行: Codex（調査・レビュー側）／2026-09-10  
宛先: 開発側 Claude  
調査対象 HEAD: `63f1c391e04472ab4cf1af8dc0870acc887e652e`  
今回の成果物は調査と指示書。製品コード・素材・既存テストは変更していない。

## 1. 今回の目的とユーザーの指摘

ユーザーは、4頭を真横から表示して繰り返し調整した段階では走り方に納得し、その状態を前提に桜星賞への組み込みへ進んだ。しかし現在は、真横でも騎手が不自然に揺れ、真横以外は馬の走りに見えないと指摘している。

**まず、良かった検証台の動きが組み込み先でも保たれるようにする。素材の全面的な描き直しを先に始めない。**

9月9日の技術レビュー合格は、時計の接続・直線長・計算と検査の修正に対するもの。走行映像の合格ではない。現在の指摘は別の具体的な描画問題として扱う。

## 2. 比較の基準を確定する

ユーザーから受領したスクリーンショット:

| 基準 | 読み取れた設定 | 用途 |
|---|---|---|
| 2026-09-08 09:38:59 | 4頭、16.0m/s、1完歩5.60m、2.86完歩/秒、浮き30%、馬の高さ43%、8px刻みON、見た目「納品のまま」 | 主な再現対象。A/B/Cを含む個体差のある表示 |
| 2026-09-07 14:52:07 | 4頭、16.0m/s、1完歩5.60m、上下動0%、馬の高さ50%、毛色の濃さ85%、8px刻みON | それ以前の経過。素材・着色経路が異なるため9/8版と混同しない |

保存先: `out/gait-integration-20260910/reference-20260908.png`、`reference-20260907.png`。
原本はユーザー提供の `V:/Screenshots/スクリーンショット 2026-09-08 093859.png` と同9/7ファイル。

対象画面は `apps/web/src/app/rig-lab/sprite/sprite-client.tsx` の `/rig-lab/sprite`。
12頭のレースモード導入前の `368e08d`（9/8 09:42）でも、速度16、完歩5.60、bob=0.3、および下記の接地式を確認した。**現在の検証台のデフォルト高さは36%で、スクリーンショットの43%とは異なる。再現時は43%を明示設定する。**

静止画だけで当時のフレーム間の運動を完全復元したとは主張しない。9/7版の「上下動0」と9/8版の「浮き30%」も、同じ意味のつまみとして読み替えない。

## 3. 今回、実装と実測で確認できたこと

### F-G1: コマごとの拡大縮小が検証台にはなく、組み込み先にある

`race/page.tsx` の `buildFrames` と `buildFramesFromBaked` は、検出した鞍布幅から各コマの `referenceHeight` を変更する。

```ts
scaleFix = clamp(anchor.width / medianWidth, 0.92, 1.08);
referenceHeight = setReferenceHeight * scaleFix;
// perspective-draw.ts
scale = projectedHorseHeight / referenceHeight;
```

このため、鞍布の検出幅が変わると、馬・騎手を含む画像全体が毎コマ拡大縮小される。検証台はセット内で共通の倍率を使っており、この補正をしていない。

現アトラス目録での型A真横の検出幅は `[184,166,186,200,193,169,187,195]` px。倍率の分母が変わり、基準倍率に対する実際の拡大率は **0.9350〜1.0870**、隣接コマ間で最大 **12.18%** 変わる。型A斜め前では最大 **17.39%** の隣接コマ変化になる。

ここでいう「鞍布」は白い画素の検出結果。`saddleReference` は指定窓内の明るい無彩色画素の外接矩形を使う。実際に剛体の同一部位を追えていると保証された値ではない。その幅を馬全体の拡縮に使う処理は、少なくとも今回の整形済み素材に対し動きを増幅している。

### F-G2: 接地・浮きのモデルも別物になっている

検証台の描画位置（`sprite-client.tsx`、現行の約1590行）:

```ts
gap = FEET - lowRatio[frame]; // 型・コマごとの画像をalpha>=64で実測
dy = imageHeightOnScreen * gap * (1 - bob);
y = round(groundY - imageHeightOnScreen * FEET + dy);
// FEET=.920、9/8基準 bob=.3
```

本編側（`race/page.tsx`、約1950/2050行）は各コマの不透明矩形下端を接地させ、別の固定表で浮かせる。

```ts
HORSE_FLIGHT_LIFT_SOURCE_PX = [15,20,6,0,0,0,0,22];
bodyLift = tileHeight - anchorY + flightTable[frame] * canvasHeight / 1536;
```

この表は旧来のコマ順に対する支持・空中局面を仮定しており、現在のA/B・真横/斜め前の接地データから生成していない。`scaledHorseLift` のbobを0にしても、消えるのは固定表の浮きだけ。**コマごとの拡縮と、毎コマ下端を接地させる処理は残る。**

さらに `perspective-draw.ts` の式を整理すると、縦方向の `bodyAnchorSourcePx.y` は `bodyLift` 内のanchorと打ち消し合う。名前に「bodyAnchor」があっても、胴体が一定の高さに安定する設計にはなっていない。

### F-G3: 同じ輪郭素材でも、組み込み処理だけで上下の振れが増える

今回のプローブは、実物のA/B真横・斜め前の32コマについて、検証台のPNG→公開原版PNG→アトラスの輪郭を比較した。

- 検証台PNGと公開原版PNGのアルファ差分は**全32コマで0画素**。
- 公開原版の切出し領域とアトラスのアルファ差分も**全32コマで0画素**。
- 色は勝負服の無彩色化・毛色処理で変わる。RGB一致を主張していない。
- したがって、現行のこの32コマについて、原版化やアトラス化でポーズの輪郭が崩れたという説明にはならない。

同一位相、固定カメラ・固定地面、名目基準高300pxに揃えた計測:

| 素材 | 検証台bob=.3 | 本編既定 | 本編bob=0 | 本編でコマ別拡縮のみ無効 |
|---|---:|---:|---:|---:|
| 真横 A | 17.19px | **60.00px** | 59.20px | 25.20px |
| 真横 B | 27.10px | 41.67px | 45.84px | 32.99px |
| 斜め前 A | 26.39px | **73.69px** | 71.97px | 33.42px |
| 斜め前 B | 23.21px | 50.66px | 52.47px | 31.91px |

数値は8コマ一周の**画像上端（alpha>=64の輪郭）の縦位置の最大値−最小値**。騎手の関節や重心を追跡した結果ではない。真横Aの隣接コマ最大段差も、検証台15.77px→本編56.46pxになる。

測定ではページのアトラス用フレーム組立式に現在のmanifestを与え、**実際の `drawPerspectiveHorses` の `drawImage` 座標を取得**した。別計算の閉形式とも一致を検査した。検証台側は接地式の再現で、画面の整数丸めを除いている。

これは表示処理の差を切り分ける制御実験。ブラウザ全画面の実時間動画・実機fpsの測定ではない。比較画像の各行は地面・縮尺を揃えた診断図であり、カメラ映像ではない。

**「bobを0にすれば直る」「素材だけが悪い」は撤回する。コマ別拡縮が強い寄与を持ち、接地モデルも同時に異なることまで確認できた。全ての違和感の原因を特定し終えたとはしない。**

### F-G4: 完歩・時計・位相の条件を引き継いでいない

検証台の基準は16m/s÷5.60m=2.857完歩/表示秒。位相は `(gate*.618)%1`。
本編は `BROADCAST_STRIDE_M=7` と `raceGaitPhase` を使い、待機pose04から1完歩かけて個体差を広げる。

本編の位相入力は単なる距離ではなく `easedAt.meters + gaitDelta`。`gaitDelta` には `visual-scroll.ts` の時計圧縮補正がある。**2倍のレース時計だから脚も必ず2倍、と短絡しない。** 表示秒あたりの位相増分、接地中の蹄の画面位置と地面の流れを、真横・斜め前・カット境界・発走で実測する。

過去の報告にある3.818mは、検証台冒頭コメントでは購入リグのクリップの実測値。その値を現在の生成デフォルメ素材の実測値と呼ばない。今回ユーザーが提示した画面で使っていた値は5.60m。

### F-G5: 前回の斜め素材に対する判定は再評価が必要

前回Claudeは `measure-gallop.mjs` の不通過を根拠に描き直しを提案した。しかしこの計測器の「騎手の前傾」は、画像上部30%の**横方向**の重心と全体の横重心の差で判定する。真横と正面寄りで同じ閾値が使えると確認されていない。

また「上半身の画素変化率」は運動の正しさそのものではない。閾値を超えるためだけに揺れや変形を増やさない。全4方向を測った結果を、現行の桜星賞で全4方向が使われている証拠にもしない。

v6台本の主対象は真横と斜め前。`broadcast-v2-scene.ts` はカットの代表角度から素材を選び、全馬で共有する。各馬の角度は診断に出すが素材選択には使わない。進路と画像の向きの不一致も独立した調査対象。今回の資料だけで全角度を描き直す必要があるとは確定しない。

## 4. Claudeに依頼する実装順序

### A. 比較条件を固定して、まず真横の動きを戻す

1. 現HEADと差分を記録し、他セッションの変更を保持する。ユーザー提供9/8画像の条件で `/rig-lab/sprite` を再現する。速度16、完歩5.60、浮き30%、高さ43%、4頭、8px刻みON。A/B/Cの構成も記録し、本編で未採用のCはまずA/Bと混同せず比較する。
2. 本編の真横を、まず同じ型A・同じ8コマ・同じ位相・同じ画面上の馬高・固定した地面とカメラで比較する。その後に型B、4頭、本来の12頭へ進む。
3. **整形済みデフォルメ素材に対するコマ別の鞍布幅補正を外すか、セット固定の較正値に置き換える。** 旧素材まで一律変更せず、適用する素材群を明示する。体のポーズ自体の変化と、描画が後から足す倍率変化を分ける。
4. **検証台で調整した接地・浮きの意味を本編に持ち込む。** コマごとの元画布・地面線・支持/空中局面を保持する。固定表の使い回しや、全コマを蹄の下端で揃えるだけの修正にしない。
5. 必要な配置情報を素材セットのmetadataまたは共通の純粋関数へ集約する。アトラスのクロップで捨てた元画布座標を使う場合は明示的に保存する。通常のbaked経路と原版fallback経路で同じ結果になるよう接続する。ページ側にだけ新しい補正表を置かない。
6. 「1完歩5.60m」を比較の出発点にする。16m/sでの表示上の歩調・蹄と地面の対応が再現できることを先に確認する。距離や時計の定数だけを替えて完了にしない。

### B. 真横以外と発走を、表示処理と素材に分けて直す

1. 桜星賞の発走、1角、4角、直線正面で、実際に選択したrole・prefix・個体型・コマ番号・位相・カメラ角・馬ごとの進路角を記録する。既存の `__raceDiag` を起点に不足分を開発用に補う。raw fallbackが起きた場合も識別する。
2. 真横で見つかったコマ別拡縮・接地の問題を斜め前A/Bにも修正し、その状態で一周の走りを再評価する。
3. 発走時の待機→走行でフレーム順が連続していることだけでなく、支持脚・離地・騎手の姿勢・進行方向が自然につながるかを見る。全馬の同時跳躍や急な体型変化を避ける。
4. 向きが不適合なショットがあれば、素材方向の不足なのか、代表角度の共有・カメラの選び方の問題なのかを切り分ける。横につぶすだけで回転した扱いにしない。
5. その後も素材自体に破綻が残る場合に、該当する方向・型・コマと問題箇所を示し、限定した素材修正を提案する。元の良かった真横素材を一括置換しない。

### C. 修正時に守る範囲

- 着順・走破時刻・ポイント台帳・サーバー側レース判定を変えない。表示用修正を結果へ逆流させない。
- 馬体と騎手を静止させたり、空中局面を消したりして「揺れ0」を達成しない。元の走行ポーズを保つ。
- カメラ距離を遠くして粗を隠す、再生を極端に遅くして問題を隠す、比較時に違う素材を読ませる、といった修正にしない。
- 画像加工・焼き直しが必要になっても、原版とユーザー基準を保存してから実行する。
- `git stash`・`git add -A`・共有変更を巻き込む復元をしない。コミットは対象差分を明示し、push・配備は今回の作業に含めない。

## 5. 完了条件と検証

### 数値・接続

- 現状の問題を検出する回帰検査を先に用意する。真横A/Bの実素材または独立した固定fixtureで、コマの鞍布検出幅が変わっても**描画側が余分な拡縮を足さない**ことを検査する。実装から期待値をコピーするだけの検査にしない。
- 同じ位相・素材セット・投影サイズなら、検証台で合意した配置モデルと本編配置が、明示した丸め誤差内で一致すること。native/atlas、A/B、停止・シークで確認する。
- 「本文に関数名がある」だけでなく、通常のロード経路から実際に配置関数へ届く検査を行う。`?dev=1` だけ直って既定画面は旧処理という状態を防ぐ。
- 前掲の画像上端の振れ幅・隣接段差・コマ別倍率を再計測し、どの変更がどれだけ効いたかを表にする。ただし、この数値だけを映像の合格基準にしない。
- 位相増分・蹄の接地滑りを表示秒で測る。カット境界と発走は別に示す。従来の時計・着順・交通流の検査も維持する。

### 実画面

- **基準の4頭真横／修正前の桜星賞真横／修正後の桜星賞真横**を、同じ大きさ・同じ再生時間の比較動画にする。異なる頭数による見え方と、1頭の運動の変化を分けて見せる。
- 真横、ゲート直後、1角、4角、直線正面はそれぞれ数完歩の連続映像を出す。等速版と0.25倍の確認版を明示する。1枚のスクリーンショットだけで走りの合格を報告しない。
- 撮影時の表示秒と書出しfpsを一致させる。対象URL、設定、読んだ素材、実際のフレーム番号、ロード失敗・fallback、実行時エラーを保存する。
- 可能なら30/60fpsの実時間再生も確認する。制御シークの動画だけの場合は、その限界を記す。
- 調整卓を触らなくても、通常の桜星賞で改善が見える初期値にする。ユーザーにスライダーを探してもらうことを修正の代わりにしない。

修正後は関連テスト、`npm.cmd run typecheck`、`npm.cmd test` を実行し、終了コードと件数を記録する。必要な交通流監査を追加実行する。素材の倍率・配置を触った場合、従来の重なりやフレーミング検査への影響も報告する。

## 6. 今回の証拠・再実行

```powershell
node --import tsx tmp/probe-gait-integration-20260910.mjs
```

終了コード0を確認済み。出力:

- `out/gait-integration-20260910/measurements.json` — 各型・各コマの倍率、配置、輪郭位置、アルファ差分
- `out/gait-integration-20260910/side-v6-placements.png` — 真横A、上:検証台の配置／中:本編既定／下:本編bob=0
- `out/gait-integration-20260910/side-v6-b-placements.png` — 真横B、同上
- `out/gait-integration-20260910/diag-front-v2-placements.png` — 斜め前A、同上
- `out/gait-integration-20260910/diag-front-v2-b-placements.png` — 斜め前B、同上

プローブは製品レンダラーを呼ぶが、ページ内のフレーム組立式を読み取り用に再現している。ページのローダー・勝負服overlay・背景・実時間fpsを通した検証ではない。式が変わった場合はプローブとの対応を見直す。図の色味の違いは公開原版と着色済みアトラスの違いであり、運動比較の評価対象ではない。

`tmp/` と `out/` はローカル成果物。Git経由で別環境へ渡す場合は、このスクリプトと証拠を別途同梱すること。本セッションでは全体テスト・ブラウザ動画撮影を実行しておらず、既存の1466件成功を今回の映像修正の合格としない。

## 7. 開発側から返す報告

`REPORT_P4_GAIT_INTEGRATION_REGRESSION_20260910.md` に、次をまとめる。

1. 基準画面・素材・設定の同定結果と、再現できない部分。
2. 変更した表示処理と、F-G1〜F-G5への対応。確定原因、寄与する要因、未確定を分ける。
3. 修正前後の数値・実画面動画へのリンク。どの素材・どの経路を通ったか。
4. テストの終了コード、影響範囲、残る素材修正の必要性。
5. 差分の対象、共有作業との混在の有無。映像の最終評価はユーザーに戻す。

**着手点は「馬を作り直す」ではなく、「4頭で成立していた動きを、桜星賞への組み込みで変えてしまった処理を直す」。**
