# 正典改訂案: 初回の入口を「付与 1 頭 ＋ 初回の無償配合 1 回」へ（D-074 / D-079）

2026-09-22・開発側 Claude。**案であり、正典は編集していない。** 番号の起票と採否はレビュー側の裁定で行う（`REVIEW_FIRST_HORSE_BREED_PLACEMENT_VERDICT_20260922.md` §5-1）。

- 出どころ: オーナー決定 **案 A**（2026-09-22・`QUESTIONS_FOAL_LIFECYCLE_20260922.md`）／指示書 `CLAUDE_CODE_INSTRUCTIONS_FIRST_HORSE_20260922.md`
- 照合した HEAD: `55b2fd4`
- 凡例: ✔ ＝ 正典・コードで確かめた ／ △ ＝ 未確認

## 1. 変える理由

- D-074 は、案 b（繁殖牝馬を配り、種牡馬市場で配合させる）を採らなかった。理由は「**配合 UI が P5 なので、それまで新規プレイヤーが何もできない**」✔（`STAR_SPEC_v2.0.md:128`）。
- 案 A は、**付与 1 頭を残した上で**初回の配合を加える。新規プレイヤーは付与馬ですぐ調教・出走できるので、**D-074 が案 b を退けた理由は案 A には当たらない**。
- オーナーの要望は、最初の体験を「父母を選び、本当に生まれた仔に名前を付ける」ものにすること。既存馬を付与して「生まれた」と見せる演出は不可（指示書）。

## 2. 置き換わる規則

| 現行（D-074） | 改訂案 |
|---|---|
| 「初回に馬を 1 頭無償で付与する（案 a）」 | **初回に馬を 1 頭無償で付与し（D-079 のまま）、加えて初回の配合を 1 回だけ無償で行える** |
| 「2 頭目以降は配合か種牡馬市場（P5）」 | **プレイヤーの配合の経路（要求 → ワーカーが確定・裁定 55b2fd4）を P5 より前に入れる。** 初回の配合はこの経路の「種類 = 初回」。種牡馬市場（他人の種牡馬への種付・種付料の移転）は P5 のまま |
| §12.2「配合」画面 P5 | 初回の配合（父母の選択・確認・誕生・命名）だけ前倒し。検索・相性プレビューの本体は P5 のまま |

## 3. 残る規則（変えない）

| 規則 | 初回の配合でどう守るか |
|---|---|
| **無償参加**（§0.3・D-074 の末尾） | 初回の配合は EP を取らない（種付料なし・EP の発行もなし）。EP を金銭で買う経路は作らない |
| **振り直しを成立させない**（D-079 の目的） | ① 初回の配合は利用者ごとに 1 件（第 2 段の鍵・裁定 §2）。② 仔の ID と種は DB に記録した要求 ID から作る（裁定 §3）。③ **結果は確定の後にだけ外へ出す**。確定前に結果を見て失敗させる経路を作らない。④ 素質は見せない（D-114）ので、生まれた仔どうしで観測できる差は戦績が付くまで無い |
| **付与馬の規則**（D-079 ①〜⑨） | 付与馬はいまの `create_account()`（`0037`）の選び方のまま。全員共通の帯・未出走・新馬戦の述語の再利用・在庫の下限監視・NPC から抜く許可は、すべて付与馬に対して残る |
| **同じ土俵にいることが公正性の担保**（§10.5） | 初回の仔も NPC の仔も、同じ `breed()`・同じ `canMate()`・同じ年齢規則（§7.1）で生まれる。初回だけの成長の特例は作らない（案 A） |
| **権利方針**（§0.1） | 父母候補・命名の両方で、実在馬名を出さない・付けさせない（§6.1-10「実在馬 NG リストで両方を検閲」）。参考機の名前・画面・文言を写さない |
| **サーバー権威・決定論**（§0） | 配合はワーカーだけが行う。乱数の種は要求 ID から作る |

## 4. 新しく決める必要があるもの（この改訂と一緒に・別の裁定で）

| # | 論点 | 状態 |
|---|---|---|
| N-1 | **父母候補の出どころ**と、NPC の母馬の年 1 回の枠を利用者が使う影響（供給の減り） | 裁定 §6 で持ち越し。開発側が `pool-size.ts` の式で「登録 N 人あたりの減り」を出す |
| N-2 | **命名前の仔の置き場所**（`horses.name` は NOT NULL ✔ `0001_init.sql:108`） | 裁定 §6 で持ち越し |
| N-3 | **命名の規則**（文字数・正規化した重複・禁止名・実在馬名）。`data/ng-names.txt` が無い（`NG-NAMES-UNWIRED`） | 無い間は「検査していない」ことを明示して通す（裁定 §5-4） |
| N-4 | **所有上限（現役 30）に生まれた仔を数えるか**。正典 §6.7 は「現役 30・繁殖牝馬 10・種牡馬 5」で、**未出走の若駒の扱いが書かれていない**。✔ `packages/scheduler/src/ownership.ts` の `canOwnMore()` は純関数で、**どこからも呼ばれていない**（上限はいま効いていない） | 開発側の推奨: **数える**（若駒も所有馬。数えないと上限の外で頭数を積める）。初回は 2 頭なので上限には当たらない |
| N-5 | **「現役馬が 0 頭になったら再度 1 頭付与」**（D-074）で、**若駒だけを持っている人**をどう扱うか。✔ 再付与の経路は**まだ実装されていない**（`0037` の註記にあるだけ） | 開発側の推奨: 「出走できる馬が 0 頭」で判定する（若駒しかいない人は 13〜17 日レースに出られず、D-074 が防ぎたかった行き止まりになる）。ただし再付与の実装はこの便の範囲外 |
| N-6 | 初回の配合を**使わずに**進んだ人（途中離脱）の扱い | 導入の永続状態（次の項目）で、命名まで再開できるようにする。権利は失効させない案を推奨 |

## 5. 正典のどこを書き換えるか（採用された場合）

- 決定ログに新しい D 番号を起票（レビュー側）。D-074 の行は消さず、「2026-09-22 一部改訂 → D-xxx」と追記する（D-074 → D-079 の前例と同じ形）。
- §10.5（NPC 馬世界）: 初回の配合が NPC の繁殖牝馬・種牡馬を使う場合の記述（N-1 の結果）。
- §12.2: 「配合」行に「初回の配合のみ前倒し」を追記。
- §6.1-10: 「ユーザー命名待ち」の状態と、命名前の仔を公開しないこと（N-2 の結果）。
- §6.7: 所有上限に若駒を数えるか（N-4 の結果）。
