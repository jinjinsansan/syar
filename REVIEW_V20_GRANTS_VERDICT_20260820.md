# 0018 / V-20 の裁定 — PASS

**発行**: 2026-08-20（レビュー側 Claude）
**対象**: `REPORT_V20_GRANTS_20260820.md` / `9e13261`
**判定**: **PASS。独立に効果を再現しました。★TRUNCATE の発見は、私の裁定より一段深いところに当たっています**

---

## 1. 独立検証（レビュー側が実測）

**報告を読むだけにせず、効果そのものを叩きました**（R-13）。**staging で期待値を確定 → 本番を1回**、という自分の順序どおりです。

| 対象 | staging | 本番 | 判定 |
|---|---|---|---|
| `users` GET | **401** | **401** | ✅ 閉 |
| **`users` PATCH**（実在列 `display_name`・存在しない UUID＝変化ゼロ） | **401** | **401** | ✅ **前回 204 だったものが閉じた** |
| **`npc_stables` PATCH**（実在列 `heavy`） | **401** | **401** | ✅ 同上 |
| `ep_ledger` / `pp_ledger` / `point_flow_daily` / `app_environment` GET | **401** | **401** | ✅ 閉 |
| `races_public` / `race_entries_public` / `race_odds_public` GET | **200** | **200** | ✅ **塞ぎすぎていない** |

**★私が §0 で実証した「anon キーで `users` を UPDATE できる」（HTTP 204）は、閉じました。**

### ⚠️ その過程で、私が誤報を出しかけました

**1回目の測定で、公開ビュー3つのうち2つが `400` を返し、`race_entries_public` と `race_odds_public` が壊れたように見えました。**

**原因は私の探索が雑だったことです** — `select=id` を指定しましたが、**それらのビューに `id` 列がありません**。PostgREST は列解決で 400 を返しており、**権限とは無関係**でした。PATCH の `400` も同様で、**存在しない列 `{"x":1}` を送っていた**ためです。

**★「サイトが壊れます」と報告する寸前でした。** 測り方の誤りが、そのまま結果の誤読になる — **`| head -4` で適用行を見落としたのと同じ形**です。**測定に使う道具が正しいことを、測定の前に確かめる。**

---

## 2. ★TRUNCATE の発見 — 私の裁定も、軸が1つ足りていませんでした

> **`TRUNCATE` は RLS の対象外です。ポリシーがあっても止まりません。**
> **公開鍵（anon）で、ポイント台帳を全消しできる状態でした。**

**この発見は、私の裁定より一段深いところにあります。**

私は §1 の表で `ep_ledger` / `pp_ledger` / `bets` / `prize_exchanges` を **「✅ 保護されて0行」** に分類しました。**その分類は読み取りについては正しく、破壊については何も言っていませんでした。** 開発側の自己申告のとおりです — そして**同じことが私の表にも当てはまります。**

**私は「読み／書きの2軸」と書きました。実際には3つ目がありました**:

| 軸 | 守るもの | 今回 |
|---|---|---|
| 読み | grant ＋ RLS ポリシー | ✅ 効いていた |
| 書き（行単位） | grant | 🔴 開いていた（私が発見） |
| **破壊（表単位）** | **grant のみ。★RLS は効かない** | 🔴 **開いていた（開発側が発見）** |

**★そして原因は「列挙して閉じた」ことです。** `0002` は `insert, update, delete` を revoke しましたが、**`TRUNCATE` / `REFERENCES` / `TRIGGER` は列挙に入っていませんでした。**

**この案件で、手書きの列挙が漏れたのは4回目です**（Q-3 の走査境界／`classification.mjs` の登録漏れ／anon 露出の対象／今回）。

---

## 3. `0018` の設計 — 承認します。**②が本体です**

| # | 評価 |
|---|---|
| ① `revoke all`（TRUNCATE/REFERENCES/TRIGGER 込み） | 正しい。**列挙をやめて全部落とす** |
| ② **`alter default privileges ... revoke all on tables`** | **★これが本体です。** これが無ければ**次のマイグレーションで穴が再生**します。**「新しいテーブルを黙って足せない」を、テストではなく DB の既定として持つ**形であり、V-20 ③ より一段強い（テストは走らせなければ鳴らないが、既定は常に効く） |
| ③ 公開ビューに `select` のみ戻す | 正しい |
| ④ `users` は RLS ＋ `id = auth.uid()` ＋ `grant select` のみ | 裁定どおり。**`revoke all` にしない判断も正しい** |
| ⑤ 本人スコープの台帳は `authenticated` のみ（anon には戻さない） | 正しい |
| ⑥ 実体テーブルの RLS も有効化（grant と二重） | **★正しく、しかも理由が実測です** |

### ⑥「両方要る」について — この結論は正しいです

> **RLS だけ**だと `TRUNCATE` が素通りする（今回の穴）／**grant だけ**だと1つ間違えた瞬間に開く

**多重防御を「念のため」で積むのは、たいてい思考停止です。** しかし**今回は、片方が破られる具体的な経路を実測で示したうえで積んでいます。** これは正しい多重防御です。

---

## 4. 「塞ぎすぎていない」を合格条件に入れたこと — ★特筆します

> 守りのゲートは、閉じる方向に倒れると気づきにくい（画面が壊れて初めて分かる）

**そのとおりで、これは V-12a / V-15 / V-11 を両側にしたのとまったく同じ形です**（R-16）。**「最も安易に満たす方法は何か」を問えば、守りのゲートの答えは「全部閉じる」**です。それを塞いでいます。

**私の独立検証でも、公開ビュー3つが両環境で 200 を返すことを確認しました。**

---

## 5. メタテストが自分のツールを捕まえた件 — (b) が正解です

**(a)（コメントの語を言い換える）を却下した判断が正しいです。** それは**検出器を黙らせるだけで、次に同じ検査を書く人がまた引っかかります**。

そして —

> **★検出器の精度を上げる変更は、感度が落ちていないことを同時に示さなければ、ただの目潰しです**

**これは R-14 の正しい適用であり、言い方としてはより鋭いです。** 6サンプルで感度を固定したのも適切です。**`truncate table t` と `truncate t` の両方を入れている**のが良い（文の形で見るように変えた以上、両方の書式が捕まることを示す必要があります）。

---

## 6. 環境間の差分がゼロだったこと — 読み方に同意します

> 同一だったこと自体が情報です — 穴は片方の環境の事故ではなく、**Supabase の既定をそのまま使っていたことに由来する構造的なもの**でした

**同意します。** そして**この読み方は、次に効きます** — **今後もし staging と本番で付与に差が出たら、それは「誰かが手で触った」ことの証拠**になります。**差分ゼロが平常値だと分かったので、差分そのものが警報として使えます。** V-20 の実行時に**環境間の差分も出力**しておくと、その警報が自動で鳴ります（任意）。

---

## 7. 残ボールの更新 — **1 と 2 は閉じています**

報告 §5 の一覧を更新します。**報告（13:14）の後に、私が2件片付けました。**

| # | | 状態 |
|---|---|---|
| 1 | `QUESTIONS_SETUP_RPC` の裁定 | **✅ 完了** → `REVIEW_SETUP_RPC_VERDICT_20260820.md`（13:33）。**★D-074 の「決定的に導く」は撤回**しました（実装不能・私の誤り）。振り直しを止めるのは決定性ではなく**★表示に観測できる差が無いこと** |
| 2 | 正典への反映 | **✅ 完了**（13:39）。**D-076〜D-080 / V-19（16項目・経路ごと）/ V-20 / R-25〜R-28** ＋ §4.1 の `user_identities` DDL・§14.6・§15.5・§17.7 の O-5〜O-7・§3.2 の総発行上限。**本裁定を受けて R-29 と V-20 の TRUNCATE の経緯も追記します** |
| 3 | VPS で `ls -l /opt/star-current` → 配備の二択 | オーナー |
| **🔴 4** | **本番の service_role キー・DB パスワードのローテーション** | **オーナー・最優先** |

### §5 の最後の指摘に同意します

> `0018` で anon 側は閉じましたが、**service_role キーは RLS も grant も全部迂回します**

**そのとおりです。** 今日、**anon が「既定で開いていた」ことが判明した以上、「service_role は平文でディスクにあるが、まあ大丈夫」という前提を支える根拠は何も残っていません。** ローテーションの優先度は最上位のままです。

---

## 8. 次にやること（開発側）

**止まっているものはありません。** `REVIEW_SETUP_RPC_VERDICT_20260820.md` の裁定で **Q-SETUP-01〜04 はすべて決まっています**ので、

1. **セットアップ RPC を書く**（D-079 の初期馬付与・D-080 の `assert_setup_complete()` ＋走査型メタテスト）
2. **V-19 #14 を固定**（upsert 禁止・2回目は全ロールバック・**★初期 EP も初期馬も付いていないこと**）
3. **V-19 #13**（同時ログインで孤児の auth ユーザーを残さない）を `issueSession` の実装で
4. 残る #4 / #9 は**鍵のローテーション後に staging**

**帯のパーセンタイルは、上下2つの基準（★が全個体同一 ／ 適切な育成でキャリア中1勝）を満たすように測って決めてください。** 値を先に決めて後から正当化する形は採りません。
