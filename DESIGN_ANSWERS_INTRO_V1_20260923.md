# デザイナーへの回答: 第 1 便の確認事項 11 点 — 2026-09-23

> 以下の「---」から下を、そのままデザイナーへ貼り付けられます。
> 出どころ: 裁定 `REVIEW_DESIGN_INTRO_V1_VERDICT_20260923.md`。開発側が事実を確かめ、レビュー側が §3・§4・§5 の形を決めました。

---

第 1 便をありがとうございます。確認事項 11 点に答えます。

## 1. 第 2 便の依頼文

こちらの不備でした。本文と納品物の続きは、オーナーから別途お渡ししています。

## 2. 総獲得賞金の単位

**「PP」を付けてよい**です。賞金ポイント（PP）です。
参加ポイントは EP で、種付料など「支払い」に使うのはこちらです。2 つを同じ単位に見せないでください。

## 3. 🔴 母の候補の列に「毛色」— この便では使わないでください

**いまは毛色のデータがありません。** レースの映像で枠の番号から色を決めているだけで、馬の持ち物ではありません。
アートバイブルでは将来 5 種を持たせると決めていますが、**時期は決まっていません**。
**この便の画面では毛色を使わないでください**（一覧も、誕生の場面も含みます）。依頼文に「毛色」と書いたのは、こちらの誤りでした。

母の候補に出してよいのは、次だけです。
- 名前・年齢・これまでの産駒の数・父と母の名前（血統のつながり）

## 4. 「選べない」の理由 — 3 つの見せ方に寄せてください

サーバーは細かい理由の語を返しますが、**画面は下の 4 つに寄せてください**。語そのものは画面に出さないでください。

| 見せ方 | 主ボタン | サーバーが返す理由（画面には出さない） |
|---|---|---|
| **この組合せは選べない** | 父母を選び直す | 今年もう産んだ／母が候補でなくなった／父が候補でなくなった／親が見つからない／年齢・種付枠・血が近すぎる |
| **もう一度お試しください**（利用者は悪くない） | もう一度試す | 一時的な不具合 |
| **持てる頭数がいっぱい** | 厩舎を見る | 現役と、名前を付ける前の仔の合計が上限 |
| **候補が戻るまで待つ** | すでに迎えた馬の調教へ | 母の候補が 1 頭もいない |

- 第 2 便（自分の繁殖牝馬の配合）では、これに **2 つ**が加わります。**「選び直し」とは別の見せ方**にしてください。
  - **種付料が上がった**（見積もりより高くなった）→ 新しい見積もりで出し直す
  - **参加ポイントが足りない** → 足りないことを伝える

## 5. 生産中の経過時間 — サーバーが「いつ依頼したか」を返します

読む口に **依頼の時刻**を足しました（移行 `0073`）。閉じて開き直しても、そこから経過を出せます。
**秒数の計算はサーバーではしません。** 画面が開発側の関数で出します（日数・時間の枠と同じ扱い）。

## 6. 「録画」の範囲

**常設表示のレースだけ**です。調教の場面そのものは録画ではないので、「録画」の表示は要りません。

## 7. 付与馬

**名前付きで付与されます。** 利用者が名前を付けるのは、生産した仔だけです。

## 8. 仔の性別・毛色

- **性別は誕生の後に分かります**（確定の前は分かりません）。確認の画面の「生まれてから分かります」で正しいです。
- 毛色は 3 のとおり、いまはありません（この便では使わないでください）。

## 9. 誕生の場面の素材

新規 1 件が要る、という見立てで問題ありません。**開発側で作ります**（オーナー判断）。
- **1 枚だけ**にします。毛色ごとの作り分けはしません（3 のとおり、毛色のデータがまだないため）。
- **後から色を変えられる作り**にしておきます。毛色を入れた日に、そのまま使えます。
- 大きさは **720 × 540**（表示の箱 360 × 270 の 2 倍）で作ります。★拡大縮小は整数倍だけ、という決まりのためです。**表示の箱の寸法は仮置き**なので、そちらの版に合わせて作り直します。寸法を教えてください。
- 絵柄は、いま本番に入っている馬の絵（太い輪郭のセル調）に合わせます。

## 10. 添付画像

こちらからまだお渡しできていません。オーナーから届き次第お送りします（第 2 便の「生涯の記録の色」に要ります）。

## 11. 「名前はあとから変えられません」

**「ご自身では変更できません」まで**にしてください。
「永久に変わりません」とは書かないでください。運営が名前を戻す仕組みがあり（規則に反する名前のため）、その根拠は規約に書く予定で、まだ確定していません。

---

## 開発側の控え（デザイナーには渡しません）

- 3 の毛色: `horses` に毛色の列は無い（`tools/lib/dress.mjs` の `coatNameOf(gate)` はレース映像で枠番から決めているだけ）。★ただしアートバイブル `:56`・`:126`・`:157`・`:202` に「毛色 5 種を持たせる」決めが在る（時期は未定）。★「将来入る」と約束しない。
- 4 の語（報告と記録に残す側）: `dam_already_bred_this_year`／`dam_not_candidate`／`sire_not_candidate`／`parent_missing`／`canMate` の拒否理由／`internal_error`／`owner_limit`／`no_candidate`、第 2 便で `fee_above_max`／`ep_short`。
- 5: `0073` で `my_initial_breeding` と `my_foal_request` に `created_at` を足した（落として作り直し。返り値の型を変えるため）。staging で適用・`verify-player-breeding-live` 36 件 合格。**本番は未適用**。
