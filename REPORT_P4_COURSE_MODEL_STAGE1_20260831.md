# 報告 — ★段階①「器」（2026-08-31）

**出どころ**: `DEV_INSTRUCTIONS_P4_COURSE_MODEL_20260831.md`
**ブランチ** `p4/race-30sec-cuts` / 起点 `425d47b`

---

## ★0-1. 一行で

★**指示書 §1 の 1-0 〜 1-4、§4-2、差し戻し 9 件をすべて完了しました。** ★44 通りは基準値と完全に一致します。

## ★0-2. 提出物・コミット・変更範囲

| コミット | 中身 | 変更範囲 |
|---|---|---|
| `4e61e24` | レース選択のプルダウン（オーナー指示） | `page.tsx` / `race-setup.ts` / `graded-races.test.ts` |
| `56df6b8` | A-8 の起票・地形の実測・照会・道具 | `RESIDUALS` / `classification.mjs` / `shot-race-at.mjs` / `QUESTIONS_*` |
| `d25d858` | ★**受領**（レビュー側の裁定・指示書・正典の起票）★開発側は 1 文字も書いていません | `STAR_SPEC` / `REVIEW_*` / `DEV_INSTRUCTIONS_*` |
| `c22bea5` | ★**1-0 A-10 の直し** | `mow-stripes.ts` + テスト |
| `3d30e21` | A-10 の報告書・台帳に A-9 を起票 | `REPORT_P4_A10_*` / `RESIDUALS` |
| `ad6eb67` | ★**1-1 / 1-2** | `lane-geometry.test.ts` / `lane-reveal.test.ts` / `race-engine/package.json` |
| `c5614fd` | ★**差し戻し 9 件** | `venues.ts` / `types.ts` / 文書 5 本 |
| `8a5eba6` | ★**1-3 / §4-2** | `tools/lib/v18.mjs`（新）/ `verify-v18.mjs` / `v18-gate.test.ts`（新） |
| `0a6e577` | ★**1-4 器** | `lane.ts` / `course.ts` / `race-setup.ts` / `venues.ts` + テスト |

⚠️ ★`apps/web/package.json`（ポート 3210）は**混ぜていません**（オーナーの作業）。
⚠️ ★`tools/_*.mjs` は追跡外。★この便で **7 本**作り、★全部登録簿に載せています。

★**着順に効く経路（`RACE_RANDOM_K` / `laneExtraMeters` / 較正定数）には触れていません。**
★段階①の器は `conditions.course` を通りますが、★**どの競馬場も使っていない**ので値は動きません（下の §4）。

---

## ★1-0. A-10（★別の報告書）

★`REPORT_P4_A10_MOW_EDGE_20260831.md`。★真因は ★**芝の縞が走路の外 60m まで塗られ、その縁がゲートを横切っていた**こと。

---

## ★1-1. `lane-geometry.test.ts` を 1 場 → 44 通りへ（R-33）

★**R-33 を実証しました。** ★旧検査を一時的に戻し、同じ変異を両方に掛けています:

| 変異 | ★旧検査（1 場） | ★新検査（44 通り） |
|---|---|---|
| ★`ovalSegments` が渡された `spec` を無視 | ★**4 件 PASS（緑のまま）** | ★**2 件 FAIL** |
| ★`laneExtraM` の中心を `spec.widthM` でなく既定 20 で取る | ★**4 件 PASS（緑のまま）** | ★**1 件 FAIL** |

★走路の形は `raceSetupById` から引いています（自前で組んでいません）。
★併せて **対象そのものの見張り**（10 場すべて／組まれている (競馬場,距離) をすべて／形が 10 通り）を入れました。★0 件・1 件で緑になるのがいちばん危ないためです（R-21）。

---

## ★1-2. ②b を検査に（★指示書と食い違う点が 2 つ）

### ⚠️ ★① 「旧形に戻すと落ちる」は **成立しません**

| | ②b（2000 本・`DEFAULT_OVAL` 1600m） |
|---|---|
| ★本番 | **0.216 馬身** |
| ★旧形 `LANE_MODEL_LEGACY` | **0.322 馬身** |

★どちらも 1 馬身の内側です。★理由は構造で、★**どちらの作り方も通り道をシードから引き、枠に依存させていません**（D-069 / D-073）。★旧形の欠陥は**内ラチへの重なり**であって**枠順の偏り**ではないので、★②b はこの 2 つを区別できません。

→ ★**対照は `SETTLE_M` の変異**にしました（★較正定数の登録簿にあり・変異値 2000）。★手で確かめ、★**②b を含む 4 件が落ちる**ことを確認しています。

⚠️ ★**距離を短くする対照は使えませんでした** — ★`DEFAULT_OVAL` の 400m は**全部が直線**でコーナーが 1 本も無く、★距離ロスが全馬 0 になります（★実際に書いて 0 が出ました）。

### ⚠️ ★② 指示書の実測値が古い

| | 指示書 §1-2 | ★再測（現 HEAD・2000 本） |
|---|---|---|
| ②b の幅（44 通り） | 0.242〜0.503 | ★**0.117〜0.416** |
| 1 馬身超え | 0/44 | ★**0/44** ✅ |

★8/30 の測定で、★8/31 の `LANE_MODEL` 作り直しより前のものです（R-25）。★結論は変わりません。

### ★seed の本数

★**1000 本**。★実測: 200 本 **0.348** ／ 500 本 **0.365** ／ ★**1000 本 0.216** ／ 2000 本 **0.216**。

⚠️ ★併せて自白します — ★`_gatebias.mjs` に ★**②b の 2 つ目の実装を書いてしまい**、★200 本で 0.567、既存実装の 2000 本で 0.216 と食い違わせました（★D-052 の禁じ手・台帳 B-5）。★既存関数を呼ぶ形に直しました。

---

## ★1-3. V-18 を検定（CI）へ（R-32）

★`apps/cli/test/v18-gate.test.ts`（★5 件・6.2 秒）。

★**9 日間の沈黙を実際に捕まえることを確かめました**:

```
  変異: 本番の LANE_MODEL を 2026-08-31 以前の形へ戻す
      → ★②a 1600m が 12.1 馬身で帯を割り、検査が落ちる
```

★本数は**実測で決めました**:

| レース数 | ρ の最大 | |
|---|---|---|
| 50 | **0.123** | ★上限 0.10 を超えて誤報 |
| 100 | **0.112** | ★同上 |
| 200 | 0.079 | 上限まで 0.021・際どい |
| ★**600** | ★**0.071** | ★**2000 レースの真値 0.069 と 0.003 差**・7.4 秒 ← 採用 |
| 1500 | 0.070 | これ以上増やしても動かない |

⚠️ ★**「軽い＝通りやすい」にしていません。** ★帯は `V18_BAND` から引き、★減らしたのはレース数だけです。
★重い版（44 通り × 2000 レース）は `tools/verify-v18.mjs` に残しています。

### ★測り方を 1 か所に（D-052）

★V-18 を測る場所が 3 つ（重い道具／CI／半径の地図）になったので、★`tools/lib/v18.mjs` に出しました。
★**`verify-v18.mjs` の出力が 1 文字も変わらないこと**を before/after の diff で確認済みです。

---

## ★§4-2. 半径の地図（★1-4 の着手前提）

★直線 400m 固定・300 レース（`tools/_radiusmap.mjs`）:

| 半径 | ②a（1200/1600/2000/2400m） | ① |
|---|---|---|
| 120m | **12.0** / 11.6 / 11.4 / 11.3 | 0.078 / 0.055 / 0.036 / 0.024 |
| 140m | 10.2 / 10.0 / 9.8 / 9.7 | ★**同じ** |
| 160m | 8.8 / 8.9 / 8.6 / 8.5 | ★**同じ** |
| ★**180m 以上** | ★**7.8〜8.1（頭打ち）** | ★**同じ** |

- ★**① は半径にまったく反応しません**（★半径 +10m あたり **±0.0001**）。★決めているのは**距離**です。
- ★**②a は半径 180m を境に頭打ち**（★`homeSpreadM = 7.0m` が絶対値で効くため）。

→ ★**44 通りが動いたとき、★① が動いたら幾何ではなく `swingScale` の側**と切り分けられます（裁定 §6-2）。

⚠️ ★**いまの模型では「半径だけ」を動かせません** — ★半径を変えるとコーナーの長さも一緒に動きます。★これ自体が 1-4 の設計根拠です。

⚠️ ★**10 場のうち 5 場が感度の高い側**（半径 180m 未満）: ★陽光台142 / 月見丘151 / 天河153 / 霧ヶ原169 / 白砂178。★上限に近い 2 場がまさにここです。

---

## ★1-4. `OvalSpec` を広げる（★器だけ）

### ★入れたもの — コーナーごとの半径

```ts
readonly cornerRadiiM?: readonly [number, number, number, number];   // ★[1角, 2角, 3角, 4角]
```

★省くと従来どおり。★**省いたときの値は 1 ビットも変わりません**（★44 通りで固定）。
★`ovalSpecFromCornerRadii(直線, 半径4本, 幅)` で `lapM` を導きます。
⚠️ ★**`lapM` と半径が食い違えば投げます**（★黙って辻褄を合わせない・R-27）: `lapM === 直線×2 + (π/2)×Σ半径`。

★`@star/render` は依存ゼロが憲法（§14）なので engine から引けません。★両層に同じ規則を置き、★`lane-geometry.test.ts` が突き合わせます。
★`scheduler`（`RaceCourseSpec` / `Venue`）まで運べるようにしました。

### ⚠️ ★入れていないもの（★理由つき）

| | ★なぜ |
|---|---|
| ★**引き込み線** | ★**[EYES] 待ち。**「1 角が消えているのが実画面で分かるか」は**オーナーにしか判定できません**（指示書 §2・裁定 §5-3）。★**判定を待ってから作り込みます** |
| ★**内回り／外回り** | ★同じ場に 2 つの走路を持たせる形で、★**venue の数値が要ります**。★指示書 §3 で禁じられているので、★型も入れていません |
| ★**勾配（段階②）** | ★正典 **D-092** |

### ★検査（`lane-geometry.test.ts` 7 → 13 件）

★変異で確かめました: ★**描画層だけコーナーの並びを逆に読む（1角↔4角）** → ★「コーナーごとに違う半径」が落ちる。
⚠️ ★**44 通り（均等半径）の検査は 12 件緑のまま**でした。★**均等半径では並び違いを検出できません。** ★R-33 がもう一度出ています。

⚠️ ★**この便で 10 場の数値は決めていません。** ★「まだ 0 場」であることを**検査で言って**います。★数を入れる便で、その検査と `venues.ts` の註記を一緒に直してください。

---

## ★2. 差し戻し 9 件

| # | | |
|---|---|---|
| 1 | 月見丘の註記 | ★「10.5〜11.0・天井まで 1.0」→ ★**実測どおり「10.9〜11.2・余裕 0.8〜1.1」** |
| 2 | 「月見丘だけ」 | ★**「月見丘と白砂の 8 鞍」**へ（`venues.ts` と照会 Q-4） |
| 3・4 | 受け入れに ①／44 通り化 | ★`ad6eb67` |
| 5 | `types.ts` の「11 形質」 | ★**12** へ |
| 6 | `_cutgeom.mjs` の合否を両側に | ★下記 |
| 7 | 「レビュー側の作業ツリーに `tools/_*.mjs` は無い」 | ★**訂正**（実在します）。★過去の文書は消さず注記（R-25） |
| 8 | 台帳に A-9 / A-10 | ★`3d30e21` |

### ★★#6 で自分の報告が間違っていたことが出ました

| | |
|---|---|
| ★**早すぎ**（直線用のカットがコーナーの上で始まる） | ★**32 / 50 鞍**（最悪 銀嶺 3000m **371m**） |
| ★**遅すぎ**（直線に入っているのにコーナー用のカメラが撮っている） | ★**16 / 50 鞍**（最深 天河 1400m **270m**） |
| ★**どちらかで破綻** | ★**48 / 50 鞍**（★当初「34 鞍」と報告） |
| ★合っている | ★**2 鞍だけ**（★桜星賞 400=400／月見丘カップ 350=350） |

★合っている 2 鞍は**どちらも `0.25 × 距離 == 直線` がちょうど成り立つ組**で、★機構の説明と一致します。

---

## ★3. ⚠️ ★自分の確認方法が壊れていました（★申し送り）

```
  npm run typecheck 2>&1 | tail -3 && echo TYPECHECK_OK
```

★パイプラインの終了状態は ★**最後の `tail`** のものなので、★**tsc が落ちても OK が出ます。**
→ ★`ad6eb67` から **3 コミット**、★typecheck が赤のまま気づけませんでした
（`lane-reveal.test.ts` の `sum[g-1] +=` が `noUncheckedIndexedAccess` に触れていた）。

★**R-21 の形**（「落ちた／出なかった」を理由を読まずに結果として扱わない）。★直し、以後は**終了状態を直接見ます**。

---

## ★4. 受け入れ（§4-1: ① と ② を両方取り直す）

| | ★基準値（指示書 §4-3） | ★再測（1-4 のあと） |
|---|---|---|
| 通り数（★コマンドで数える） | 44 | ★**44** |
| ① の最大 \|ρ\| | +0.070 | ★**0.070** |
| ②a の範囲 | 7.8〜11.2 馬身 | ★**7.8〜11.2** |
| 帯の外 | 0 | ★**0** |
| 上限に近い（10.5 以上） | 月見丘＋白砂 8 鞍 | ★**8 鞍**（10.9 / 11.0 / 11.2） |

★**完全に一致**します。★器を入れても、★**使っていない限り 1 ビットも動きません。**

---

## ★5. ★次にやること

| | 状態 |
|---|---|
| ★**A-10 の合否** | ★レビュー側の撮り直し待ち |
| ★**「1 角が消えている件」の [EYES]** | ★A-10 が直ったので、★**オーナーに改めて見ていただく段**です（裁定 §5-3） |
| ★引き込み線 | ★上の [EYES] のあと |
| ★内回り／外回り・10 場の数値 | ★**上限 12 の見直し**（D-090・段階①の完了時）のあと |
| ★A-8（48/50 鞍） | ★模型を広げたあとに 1 回だけ |
| ★段階②（勾配） | ★独立の便（D-092）。★Q-3（坂適性を遺伝子にするか）は裁定 §7 で **B に条件付き同意**、★命名は「坂路」と重ねないこと |

---

## ★6. 再実行（★期待値つき・★すべて読取専用）

```powershell
cd V:\dev\Cusor\star

npm test                      # ★期待: 134 ファイル / ★1369 件 PASS / EXIT=0
npm run typecheck             # ★期待: ★終了状態 0（⚠️ パイプに繋がず、終了状態を直接見ること）

npx vitest run apps/cli/test/v18-gate.test.ts                      # ★5 件 PASS（6.2 秒）
npx vitest run packages/race-engine/test/lane-geometry.test.ts     # ★13 件 PASS
npx vitest run packages/race-engine/test/lane-reveal.test.ts       # ★9 件 PASS
npx vitest run packages/render/test/mow-stripes.test.ts            # ★9 件 PASS

npx tsx tools/verify-v18.mjs --venues   # ★44 通り PASS / ① 最大 0.070 / ②a 7.8〜11.2
npx tsx tools/_cutgeom.mjs              # ★早すぎ 32 / 遅すぎ 16 / どちらかで破綻 48 / 合っている 2
npx tsx tools/_radiusmap.mjs --races 300 # ★① は半径に反応しない・②a は 180m で頭打ち
npx tsx tools/_gatebias.mjs             # ★②b 44 通り 0.117〜0.416・1 馬身超え 0/44

# ★A-10 の合否（★レビュー側が静止画で判定）
cd apps\web ; npm run dev     # ★別の窓・ポート 3210
npx tsx tools/_realshot.mjs --out g1 --query "race=g2-aone"  --sec 10
npx tsx tools/_realshot.mjs --out g2 --query "race=g1-ousei" --sec 10
```
