# P1 冒頭作業 完了報告 — L-1〜L-4（K-1 着手条件）

**発行**: 2026-08-05（開発側 Claude）
**指示書**: `DEV_INSTRUCTIONS_P1_20260805.md` §1-0（L-1〜L-4）
**位置づけ**: K-1 以降（レース判定エンジン）の**着手条件**。本便はここまでの報告で、K-1〜K-5 は次便になります

---

## 0. L-1 は私が作り込んだ退行です

`--set-genetics MUTATION_SD=0` で導出 sd が NaN になる件、**P0-fix5 の J-1 で私が開けた穴**です。再現も確認しました。

```
MUTATION_SD=0 → durability {"sd": null, "clamp": null}   ← JSON では NaN が null
```

機序はご指摘のとおりで、`clampRatio = 150/0 = Infinity` がガード `!(clampRatio > 0)` を通り抜け、`2·k·φ(k)` が `∞ × 0 = NaN` になります。

**そして本質は「同名のテストが既にあったのに捕まらなかった」ことです。** `regression.test.ts` の「`MUTATION_SD=0` でも NaN を返さない」は balance を直接組んでおり、`resolveRuntimeConfig` を通りません。**I-3・I-4 と同じ構造の3度目**を、R-1 を受け入れた直後の便で作りました。

---

## 1. 対応

### L-1 修正と、本番経路への移設

`clampTruncationFactor()` に「k が有限でなければ 1 を返す」を追加しました。clamp/sd → ∞ は「clamp をかけない」＝切断による目減りなし、なので解析的にも 1 が正です。

```
MUTATION_SD=0 → durability {"sd": 60, "clamp": Infinity}
```

テストは**本番経路側（`config.test.ts`）へ移設**し、2段階で固定しました。

1. `resolveRuntimeConfig({}, { MUTATION_SD: 0 })` の全形質で sd/clamp/center が NaN でないこと
2. さらに `runSimulation()` まで通し、**集団平均が有限であること**（NaN が genotype に流れ込んでいないこと）

`regression.test.ts` 側の単体テストも残しています（単体が壊れたときの切り分けに使えるため）。**単体を消すのではなく、経路側を足すのが正しい対処**だと理解しています。

### L-2 二重管理の解消

`CLAMP_TRUNCATION_FACTOR` は J-1 以降**実装から参照されていません**（解析式が出所）。テストが古いリテラルから期待 sd を計算していたのを、`clampTruncationFactor(MUTATION_CLAMP_RATIO)` から計算するよう全箇所直しました。

定数自体は残しますが、役割を「**正典 §6.4 の記載値との対応を示す参考値**」に限定し、テストは「**解析式の出力と一致すること**」だけを固定します（食い違ったら解析式が正）。`balance.ts` の「比を変えたら再計算すること」という誤誘導コメントも書き換えました。

### L-3 `worstDeviation` の NaN 塗り潰し

`Math.abs(x) > NaN` が常に false になるため、先頭の NaN が居座って真の最悪形質が隠れていました。**NaN を候補から外し**、判定不能だった形質は `undecidableKeys` として**別枠で報告**します（隠さない）。

実測（`SURFACE_RANGE: [0,0]` で判定不能を発生させた場合）:

| | 修正前 | **修正後** |
|---|---|---|
| worstKey / worstDeviation | `surface.turf` / **NaN** | **`temper` / 8.31%** |
| 判定不能の可視化 | なし（隠れる） | `surface.turf, surface.dirt` を別表示 |

`--json` の型宣言も `number` → `number | null` に直し、実値と一致させました。

### L-4 実在馬名のスクラブ

`AI_PROMPT.md` の該当箇所は**1行（実在馬名10頭の列挙）**でした。ファイルは削除せず、次の2点を行っています。

- 冒頭に**廃棄済みの警告ヘッダ**（「読まない・従わない」「実在馬名を実装・辞書・アセット・テストデータに持ち込まない」）
- 当該行を除去し、除去した旨と正しい方針（自動生成 or ユーザー命名＋NGリスト検閲）に置換

削除しなかったのは、**プロジェクトの経緯を示す履歴文書としての価値**があり、警告ヘッダで危険性は断てると判断したためです。ご指示どおり「判断は任せる」の範囲で選びました。削除がよければそう指示ください。

リポジトリ全体を再検閲し、**実在馬名ゼロ**を確認しています（`SPEC.md` にも無く、以前の検出は商標語でした）。

---

## 2. 【L-1 の本題】本番経路を通っていないテストの棚卸し

全7ファイル・101件を「本番経路（`resolveRuntimeConfig` / `runSimulation` / `breed` / `createFounder`）を通るか」で分類しました。

| ファイル | 件数 | 経路呼び出し | 分類 | 判断 |
|---|---|---|---|---|
| `apps/cli/test/simulator.test.ts` | 19 | `runSimulation` × 23 | **経路** | 対処不要 |
| `apps/cli/test/config.test.ts` | 12 | `resolveRuntimeConfig` × 12 / `runSimulation` × 1 | **経路** | 対処不要（L-1 の移設先） |
| `packages/sim-engine/test/breeding.test.ts` | 12 | `breed` × 8 | **経路** | `canMate` の直接呼びが4件あるが、`breed()` が交配不可で例外を投げるテストで経路も押さえている |
| `packages/sim-engine/test/genetics.test.ts` | 6 | `breed` × 4 | **経路** | 残る2件は `expressNumeric` の単体（純粋関数・下流で必ず使われる） |
| `packages/sim-engine/test/regression.test.ts` | 35 | `breed` × 8 | **混在** | 下記 |
| `packages/sim-engine/test/rng.test.ts` | 10 | `createFounder` × 2 | **混在** | 下記 |
| `packages/sim-engine/test/inbreeding.test.ts` | 10 | **0 → 1（追加）** | **単体のみだった** | **対処済み（下記）** |

### 対処したもの

**`inbreeding.test.ts` が唯一「本番経路を1度も通らない」テスト群でした。** 9件すべてが `calcInbreedCoefficient()` の直接呼び出しで、`breed()` がその結果を使っているかは固定されていません。理屈のうえでは、`breed()` が F の計算をやめても9件とも緑のままです。

→ **経路テストを1本追加**しました。3×4 クロスの血統を組んで `breed()` を通し、`foal.inbreedCoeff === 1/64` に加えて、**F が下流の補正へ渡っていること**（`injuryRateMult = 1 + F×2.0`）と**血の濃縮の参照先が記録されていること**（`topCommonAncestorId === 'X'`）まで確認します。

### 対処しないと判断したもの（理由つき）

| 対象 | 単体である理由 | 対処不要と判断した根拠 |
|---|---|---|
| `regression.test.ts` の定数アサーション（`resolveMutation(...).sd === 66` 等） | `resolveMutation` を直接呼ぶ | **同じ値を `config.test.ts` が `resolveRuntimeConfig` 経由で固定済み**。経路側の担保が既にある |
| `regression.test.ts` の `buildTraitMutation` 導出テスト | 関数を直接呼ぶ | 同上。`config.test.ts` が上書き経由で同じ導出を固定している |
| `regression.test.ts` の `mutateAllele` 式テスト | 純関数を直接呼ぶ | **I-3 で `breed()` 経由の経路テストを併設済み**。単体は切り分け用に残す |
| `rng.test.ts` の `Rng` 単体（分布・再現性） | 乱数プリミティブ | 壊れれば下流のほぼ全テストが落ちる。経路側の冗長化は費用対効果が低い |
| `rng.test.ts` の `deriveSeed`/`deriveRng` | 同上 | ただし「**シミュレータが実際にサブストリーム導出を使っているか**」は固定されていない（決定論テストは別方式でも通る）。実害が想定しにくいので**記録に留め、対処はしません** |
| `genetics.test.ts` の `expressNumeric` | 2行の純粋関数 | `breed()` 経由のテストが同ファイルに4件あり、発現規則は経路側でも押さえられている |

### 棚卸しからの一般化

今回の3件（I-3 / I-4 / L-1）に共通するのは「**単体テストの対象が正しくても、本番がそれを使う保証がない**」という一点でした。書きながら気づいたのは、**危ないのは「関数を直接呼ぶテスト」全般ではなく、「その関数が呼ばれなくなっても検知できない箇所」だけ**だということです。上表で対処不要としたものは、いずれも別の経路テストが同じ性質を裏から押さえています。

P1 では新規テストを書くたびに「**これは本番経路を通っているか／通っていないなら誰が経路を押さえているか**」を確認します。

---

## 3. 既定の挙動が変わっていないこと

L-1〜L-4 はいずれも「既定以外の経路」と「テスト・文書」への変更なので、既定経路の数値は変わらないはずです。実測でも一致しました。

```
$ diff <(grep -v 所要時間 out/verify-fix5.txt) <(grep -v 所要時間 out/verify-L.txt)
（差分なし）
```

- `npm run verify` の出力が **P0-fix5（= P0-fix4）と完全一致**
- テスト **101件 PASS**（P0-fix5 の98件 + L-1 経路2件 + 近交係数の経路1件）
- typecheck exit 0 / `any` ゼロ / `Date.now`・`Math.random` ゼロ

### 既定以外の経路が実際に直っていること

| 項目 | コマンド | 修正前 | **修正後** |
|---|---|---|---|
| L-1 | `--set-genetics MUTATION_SD=0` | `{"sd":null,"clamp":null}`（NaN） | **`{"sd":60,"clamp":Infinity}`** |
| L-3 | 判定不能な形質があるとき | `worstKey=surface.turf / worstDeviation=NaN` | **`worstKey=temper / 8.31%`** ＋ 判定不能を別枠表示 |

---

## 4. 仕様からの逸脱・解釈で埋めた箇所

| # | 箇所 | 判断 | 理由 |
|---|---|---|---|
| 1 | **`AI_PROMPT.md` を削除せず警告＋スクラブにした** | 履歴文書として残す | §1 L-4。「判断は任せる」の範囲。削除がよければ指示ください |
| 2 | **`clamp: Infinity` の JSON 表現** | そのまま（JSON では `null` になる） | 指示書 L-1 が `{sd: 60, clamp: Infinity}` を期待挙動としているため。`JSON.stringify` は非有限値を `null` にします。`MUTATION_SD=0` は診断用途のみなので型の作り替えはしていません。**`worstDeviation` は L-3 の指示どおり `number \| null` に直しました** |
| 3 | **`regression.test.ts` の単体テストを残した** | 経路側を足すだけにした | 単体を消すと不具合の切り分けができなくなるため。R-1 は「単体を消せ」ではなく「経路も固定せよ」と理解しています |

---

## 5. 未解決の質問

**なし。** K-1（レース判定エンジン）に着手できる状態です。

---

## 6. 次便の予定（K-1〜K-5）

L-1〜L-4 が承認され次第、P1 本体に入ります。着手順は次を想定しています。

1. **K-1 レース判定エンジン**（`packages/race-engine/`・依存ゼロ）— §8.2 の距離帯別重み、§8.3 の乗算補正10種、§8.4 の展開、§8.5 のスキル、§8.7 の着順・着差生成
2. **K-5 モンテカルロ検証ハーネス** — V-4/V-5/V-6 を先に測れるようにして、K-1 の較正に使う
3. **K-2 Provably Fair** — ハッシュは注入方式（エンジンは依存ゼロを維持）
4. **K-3 介入モデル** — V-8/V-9
5. **K-4 選抜の置換** — 最後に回します。**P0 ゲート（特に V-2e）への影響を測るのが目的**なので、レース側が固まってから変える方が原因の切り分けが容易なためです

§3 の「現在値プレースホルダ」と `RACE_RANDOM_K` の再較正条件については、**較正した条件をコードと報告書の両方に明記**します（R-7）。P0 で同じ構造の罠を3度踏んでいるので、ここは最初から書きます。
