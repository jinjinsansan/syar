# デザイナーへの依頼 — 全ページでレースの存在が続く UI

## 依頼の背景

オーナーの意図は「数分おきにレースが開催され、その様子がゲームのどの画面にいても見える」体験です。現状は `/watch-race` の案内から `/race` へ移動して観る構成で、`/home` の開催通知もデモ値です。ページを移動するとレース演出は見えません。ゲームの中心となる体験を、スマートフォン縦持ちを前提に再設計してください。

これは既存の `/race` を各ページへそのまま埋め込む指示ではありません。全ページで継続して見えるものの大きさと内容、観戦への遷移を設計する依頼です。

## 決めてほしいこと

1. **常設のレース表示**: 開催前・出走表・発走・レース中・結果・次レース待ちの各状態で、画面のどこに何を表示するか。レース映像が流れる時間以外も、次の開催へのつながりが途切れないようにする。
2. **ページ別の配置**: `/`、`/home`、`/vote`、`/mypage`、`/train`、`/exchange`、`/earn`、`/howto`、`/login`、`/signup`、`/setup`、`/watch-race`、`/race` を対象に、常設表示と主操作の優先順位を示す。認証・初回設定中を含め、表示を省く判断があれば理由を明記する。
3. **観戦の深さ**: ページ上の小さな表示から、レースを大きく観る画面へどう移り、どう戻るか。移動中も同じレースの同じ進行状態を保つ。自馬出走時の導線も含める。
4. **スマートフォン縦画面**: 片手操作、文字と馬の見やすさ、下部メニュー・フォーム・投票操作との重なりを示す。横向き、タブレット、PC での配置も併記する。
5. **音と動き**: 常設表示の音の既定値、ユーザー操作による再生、動きを減らす設定、画面外や別タブにいるときの扱いを示す。
6. **未取得・遅延・通信断**: レース情報がまだないとき、素材の読み込みに時間がかかるとき、結果だけ取得できたときの見え方を設計する。空の黒い画面や、終わらない「用意しています」に見えないこと。

## 前提と制約

- 開催サイクルは現コードで **6 分に 1 レース**（`packages/scheduler/src/cycle.ts`）。演出の長さと次レースまでの時間は同じではありません。待ち時間に架空の「生中継」を流しているように見せないでください。
- 現在の `/race` は 1280px 幅を基準にした専用演出です。スマートフォン縦持ちで全ページに置くには別の表示設計が必要です。
- レース状態・発走時刻・結果はサーバーのデータを正とします。デモ値を本番の開催情報のように見せないでください。
- 新しい画面群は `/home`、`/vote`、`/mypage`、`/train` などです。`/stable`、`/training`、`/races` などは旧世代ですが、既存利用者の経路として考慮してください。
- 自馬の出走レースでは投票導線を出さない、EP と PP を混ぜないなど、`STAR_SPEC_v2.0.md` の確定ルールに従ってください。
- 既存のレース演出を破棄する判断は依頼していません。観戦の大画面として活かす案を含めてください。

## 納品してほしいもの

1. 全ページに適用できる常設表示の共通部品のデザイン仕様。スマートフォン縦画面を主案とする。
2. 上記 13 画面の配置図。少なくとも `/home`、`/vote`、`/train`、`/race`、フォーム画面の詳細な完成イメージ。
3. 開催前から結果、次の開催までの時系列図と、ページ移動・再読み込み時の動き。
4. 表示状態ごとの文言、操作、音、待機中の見え方。
5. 実装へ渡す寸法・優先順位・素材要件。既存素材で足りる部分と新規制作が必要な部分を区別する。
6. 可能なら、常設表示の情報量が異なる 2 案を比較し、推奨案と理由を示す。

## 現状を確認する入口

- `apps/web/src/app/watch-race/page.tsx`: 案内から `/race` へ移動する現行導線。
- `apps/web/src/app/race/page.tsx`: 専用のレース演出。読み込みが長く、縦持ちでの表示が課題。
- `apps/web/src/app/home/page.tsx`: 開催通知は現状デモ値。
- `apps/web/src/components/story-shell.tsx`、`apps/web/src/components/nav.tsx`: 共通の帯とナビ。
- `HANDOFF_UI_POLISH_20260921.md` §2〜§4: 新旧画面の地図と、現行 `/race` の測定。

## 判断を依頼する点

常設表示を全画面で完全なレース映像にする必要があるか、それとも小さな進行表示と大画面観戦を組み合わせるべきかを、画面の可読性・性能・開催間隔を踏まえて提案してください。最終案はオーナーが選びます。
