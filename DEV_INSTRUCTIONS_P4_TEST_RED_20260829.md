# 差し戻し — `npm test` は赤です（報告は「1223 件 PASS / exit 0」）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-08-29 ／ レビュー側 → 開発側
**対象**: `REPORT_P4_A1_DIRT_CANON_20260829.md` §7 ／ §8 再実行表 #7
**対象コミット**: `de20958`（HEAD）／ ブランチ `p4/race-30sec-cuts`

> ★**この 1 件だけを先に差し戻します。** 他の 7 本の再実行はすべて一致しました。
> ★裁定本体は `REVIEW_P4_A1_DIRT_CANON_VERDICT_20260829.md` にあります。

---

## 1. [CODE] 実測

報告 §7 と §8 表 #7 は「**124 ファイル / 1223 件 PASS（exit 0）**」と書かれています。
★**再現しません。**

```
$ npm test
 Test Files  1 failed | 123 passed (124)
      Tests  1 failed | 1222 passed (1223)
EXIT=1
```

```
$ npx vitest run apps/cli/test/source-integrity.test.ts
FAIL  ソースの健全性 > ★過去に混ざった別字が戻ってきていない（★目印であって防壁ではない）
  ★註釈に別字が混ざっています。★読める字なので目では気づけません:
    QUESTIONS_P4_SOURCE_KANJI_20260829.md: U+82DD を 2 個（芝 のはず）
  EXIT=1 ／ 735ms ／ ★確定的に落ちます
```

★私も独立に全追跡ファイルを走査しました。★**残っている別字は U+82DD ×2、この 1 ファイルだけ**です
（U+8E74 は正しい字「蹴」なので、検出対象ではありません）。

---

## 2. ★なぜこうなったか（原因は特定済み・推測ではありません）

| | |
|---|---|
| 検査の走査範囲 | `git ls-files` の**全追跡ファイル**。★`.md` を除外していません（`source-integrity.test.ts`） |
| 落ちている中身 | `QUESTIONS_P4_SOURCE_KANJI_20260829.md` §2 が、★**直す前の姿を証拠として引用**している。その引用が本物の U+82DD |
| 検査が入った便 | ★`fa65a83` |
| 照会文書が入った便 | ★`e0ef6df`（**あと**） |

★**検査を緑にしたあとの便で、証拠文書が赤にしました。**

⚠️ ★照会 §4 の「★**検出器が自分自身を検出して落ちた**ので、コードポイントから組み立てて除外簿を不要にした」は
★**検査ファイルについての手当て**です。★**走査される側の `.md` は、その手当ての外**にありました。

★つまり ★**同じ穴を、同じ日に、別のファイルで踏んでいます。**

---

## 3. ★直し方（★こちらの推奨は (b)）

| | 案 | 判定 |
|---|---|---|
| (a) | 検査の走査から `.md` を外す | ★**却下。** 別字は元々「文書に無く、ソースにあった」だけで、`.md` に入らない保証はありません。★走査範囲を狭めれば、次は文書側が温床になります |
| (b) | ★**証拠を「生の字」で置かず、コードポイント表記で書く** | ★**推奨。** ★照会 §4 で検査ファイルに対して採った手当てと**同じ形**です。★走査範囲を一切狭めません（R-29「既定を閉じる」を保てます） |
| (c) | 当該 `.md` を除外簿に足す | ★**却下。** ★開発側自身が §4 で「★除外簿で逃がすと逃がした先が次の温床になる」と書いています。★その判断は正しいので、ここでも適用してください |

### (b) の具体

`QUESTIONS_P4_SOURCE_KANJI_20260829.md` §2 の引用ブロックから**生の U+82DD を外す**。
★引用の意味は落とさないこと。★例:

```
（直す前）  * 【★<U+82DD>には入れません】       ← ★U+82DD は「芝」に見える別字
            *   ★<U+82DD>の見え方は既にオーナー承認済みです。
```

★どう書くかは**開発側の裁量**です。★守っていただきたいのは 2 つだけです:

1. ★**検査の走査範囲を狭めないこと**
2. ★**引用が「何が起きていたか」を読者に伝える力を落とさないこと**

---

## 4. ★手続きについて（こちらが本題かもしれません）

★宿題 5「提出前に `git status` を見る」は**実行されています**（報告 §1）。
★しかし ★**提出物を足したあとに `npm test` を走らせ直していません**。

★報告書・照会文書は**追跡ファイル**であり、★**この案件では検査の対象**です。
→ ★**文書もコードと同じツリーの一部として、最後にもう一度回してください。**

⚠️ ★併せてお伝えします。★**この日、正典から R-22 が消えていたことが分かりました**:

> **R-22** … ★**検定が「異常なし」と言ったら、その検定が異常のあるはずの場所を見ているかを先に確認する。**

★`6dbbfce`（2026-08-20・NUL バイトを修復した便）で**改行が 1 つ落ち、R-22 の行が R-29 の行に飲み込まれて**いました。
★`grep "^| **R-22**"` は 9 日間 **0 件**を返し続けています。★レビュー側で復旧しました（裁定 §5）。

★**今回の別字（`grep 芝` が 38 件を取り逃した）と、まったく同じ家族**です。
★どちらも「**読めるまま、検索から消える**」壊れ方でした。

---

## 5. ★求めるもの

| # | |
|---|---|
| 1 | ★**`npm test` を緑にすること**（推奨は §3 (b)） |
| 2 | ★緑にしたときの ★**実行ログ（`Test Files` / `Tests` の行と終了コード）を報告に貼ること**。★「PASS しました」だけにしない（R-3） |
| 3 | ★**提出物を足したあとに走らせ直す**手順を、次便の申し送りに残すこと |

★**②③④ の実装内容そのものは、こちらの再実行で全部一致しています**（裁定 §1）。
★止めているのは**この 1 件だけ**です。★実装を作り直す必要はありません。
