# Claude Code 引継ぎ書 — R-4（初回セットアップ）と LINE 認証（V-19）、および環境の取り違え

作成日: 2026-08-20
対象リポジトリ: `V:\dev\Cusor\star`（branch `main`・**push していません**）
前回の引継ぎ: `CLAUDE_CODE_HANDOFF_RACE_20260819.md`（レース中継 HUD。本便は別系統）
このセッションのコミット: `3815daf`（開始時 HEAD）→ `07294d7`（現在）の 10 件

> 📝 **経緯のメモ**: セッション終盤に **V: ドライブが「デバイスの準備ができていません」状態**になり、
> 一時的にリポジトリへ書き込めなくなりました（読み取りはキャッシュで通るが書き込みは全滅）。
> 本書はいったん C: のスクラッチパッドに退避し、**ドライブ復帰後にここへ配置し直したもの**です。
> コミット済みの内容（`07294d7` まで）は一度も失われていません。

---

## 0. 🔴 次のセッションが最初に読むべき3件

| # | | 状態 |
|---|---|---|
| **1** | **本番に意図しないマイグレーションを2件適用した事故**（`0016` / `0017`） | **報告済み。残すか戻すかの判断待ち** → `REPORT_ENV_INCIDENT_20260820.md` |
| **2** | **本番の稼働コードが 08-12 の決定より前** — `entrant_snapshot` が本番で0行。**D-055（出走馬の凍結）も D-056（凍結が無いレースは中止・EP 返還）も本番で効いていない** | **レビュー側の判断待ち** |
| **3** | 🔴 **本番の service_role キー・DB パスワードのローテーション**（平文で `secrets.production.env` にある） | **オーナー手配・未実施** |

**★1 は「実害ゼロ」で終わっていますが、それは事後に判明したことです。** 手順としては誤っていました。詳細は §5。

---

## 1. このセッションで何をしたか（時系列）

| # | 内容 | 成果物 |
|---|---|---|
| 1 | **R-4 の納品受領**（Claude Design → `design/hud-ds/` ミラー）と実装 | `2b8187e` `6ff8fec` |
| 2 | 認証を **LINE ログイン＋自前実装**に変える照会 | `bde825f` |
| 3 | 裁定受領（D-076/077/078・V-19 新設・R-25）。**Q-AUTH-04 の前提誤りを全面撤回** | `3670b2b` `dd90c4f` |
| 4 | **V-19 の先行実装**（ID トークン検証・ログイン経路） | `d2c1708` |
| 5 | **D-078 の DDL の穴**を発見して照会 | `7f4f2d9` |
| 6 | 裁定を受けて FK 修正・staging で DB 側 V-19 を固定・穴 A（#13）を実装 | `3f9c089` |
| 7 | **D-074 の初期馬付与が実装できない**ことを照会 | `93c76b7` |
| 8 | **環境の取り違えの是正**と、**その作業中の事故の報告** | `07294d7` |

---

## 2. 完了しているもの

### R-4（初回セットアップ＋牧場ホーム 4 カード）

- **`/setup`（新規）** — 2ステップをステップ送りで。表示名・牧場名（`n / 12`）／勝負服16色パレット／袖3択／プレビュー170×180／「迎える」演出（0.6s フェード＋12px・`.welcome`）／素質は★のみ・数値なし／脚注は nowrap で1行固定／エラー行は該当1件だけ
- **`/stable`** — 「今週の予定」の上に4カード（アカウント＝EP 白/PP 金の別カプセル・お知らせピル・デイリー受取／わたしの牧場＝未指示は赤・現役0頭で再付与ボタン／開催状況＝発走34px青・締切26px赤・0件でも欄を消さない／ショートカット＝先頭「調教」だけ金）
- 共通ヘッダーのナビに `white-space:nowrap`
- **すべてデモデータ**。`SetupRepo` / `StableRepo` の差し替えだけで実データに切り替わる設計（画面は触らない）

⚠️ **`design/hud-ds/REQUESTS.md` に Q: を1件書き戻し済み** — stable-home の空状態が「**セリで** 1頭迎えると…」のままで、裁定 Q-WEB-01（セリは作らない）と矛盾。実装側は「新しい 1 頭を無償で迎えると…」に読み替え済み。次のデザイン改訂で拾われる想定。

### V-19（認証が壊れたときに落ちること）— 16項目中 11項目

| 状態 | 項目 |
|---|---|
| ✅ **固定済み**（単体＋変異） | #1 署名／#2 `aud`／#3 `exp`／#7 `nonce`／#8 `iss`／#11 本番経路／#12 自己検出禁止／#16 `alg` 固定 |
| ✅ **staging で固定済み** | #5 同一 `sub` で1ユーザー／#6 メール突合しない／#10 DB 一意制約／#15 RLS 0行 |
| ✅ **経路のみ固定** | #13 同時ログインで孤児を残さない |
| 🟡 **経路のみ** | #4 `state` の一度きり消費（実体は staging）／#9 セッション発行の認可（**鍵のローテーション後**） |
| 🔴 **未着手** | **#14 セットアップ RPC の二重実行** — RPC 自体が存在しない（§4） |

**成果物**: `packages/auth/`（`line-id-token.ts` / `login-flow.ts` / `resolve-identity.ts`・純粋 TS・依存ゼロ・WebCrypto のみ）、`db/migrations/0017_user_identities.sql`、`tools/verify-v19-db.mjs`

**変異試験 13件すべて検出・すべて★（振る舞い）テストが落ちる**ことを実証済み（`npm run mutation` に V19-1〜13 を登録）。

**★実装上の要点**（次の人が壊さないように）:

- **セッションは作らない。** `issueSession` は注入される依存で、実体は Supabase Auth（D-076 の制約1・自前で JWT を発行しない）
- **返り値の型に `sub` しか無い。** 「LINE から取るのは `sub` のみ」をコメントではなく型で担保している
- **現在時刻は注入**（憲法 4）。副産物として期限切れを再現可能に試験できる
- **`alg` はヘッダの申告で分岐しない。** LINE は web ログイン＝HS256／ネイティブ SDK＝ES256 と用途で変わるので、**経路ごとに固定**する。Expo で ES256 を足すときは**同じ関数に分岐を足さず、別の入口にする**

### 環境の取り違えの是正

`secrets.local.env`（中身は `STAR_ENV=development`）が**実際には本番を指していました**。development の環境は存在しません。

- `.gitignore` を**先に**修正（旧設定は名前で個別列挙。改名した瞬間に本番の資格情報が追跡対象になるところだった）→ `secrets*.env` のパターンを追加
- `secrets.local.env` → **`secrets.production.env`**、`STAR_ENV=production`
- **`migrate.mjs` の既定を廃止** — `--env` 必須、本番には `--yes-production` を追加要求
- `tools/lib/env.mjs` の環境名を `local` → `production`（`--env local` は廃止した旨を出して落とす）
- **`package.json` の `worker` を staging へ**（旧設定は本番の秘密ファイルを直接指しており、`STAR_ENV` を直した瞬間に `npm run worker` が本番ワーカーを起動する状態だった）

---

## 3. ★次のセッションが引き継ぐべき「考え方」

このセッションで、**同じ型の誤りが4回**出ました。次も同じ形で出ます。

| # | 形 | 実例 |
|---|---|---|
| 1 | **伝聞を実物で確かめずに書く** | 「staging が無い」を3回書いた（実際は 08-11 から存在）。`ls` 1回で分かるものだった → **R-25** |
| 2 | **検出器のほうが壊れて、正しいものを 🔴 と出す** | `information_schema` がスキーマ跨ぎの FK を返さず、正しく張られた FK を「無し」と報告 |
| 3 | **「探して見つからない」を「存在しない」と読む** | `listUsers()` の1ページ目で片付けを探し、staging の合成集団に埋もれて取りこぼした |
| 4 | **安全策を足す変更の正しさを、その変更自体に依存する方法で確かめる** | `--yes-production` を足したことで `9999` の空振りが空振りでなくなり、本番に全件適用（§5） |

**共通しているのは「確かめたつもり」です。** この案件の規律（R-16「機構は存在するが働いていない」／D-054「効いていることと壊れたときに落ちることは別」）は、まさにこれを繰り返さないためのものです。

---

## 4. 止まっているもの（裁定・判断待ち）

### レビュー側へ出したまま未回答

| ファイル | 内容 |
|---|---|
| **`QUESTIONS_SETUP_RPC_20260820.md`** | **★これが最優先。** D-074「素質は `user_id` から決定的に導く（引き直しても同じ馬）」は**文字どおりには実装できない** — ①再付与は前の馬が引退したときに起きるので戻せない ②プールは有限で共有なので特定の1頭を決定的に選ぶと衝突する。**開発側は「水準を全員共通の固定帯にする」を推奨**。ほか、素質の帯の測り方（パーセンタイル推奨）・候補集合・枯渇時・未セットアップ判定の置き場所 |
| `REPORT_ENV_INCIDENT_20260820.md` §7 | 適用してしまった `0016`/`0017` を残すか戻すか（**開発側は「残す」を推奨**）／本番の稼働コードが D-055 より前である件 |
| `REPORT_AUTH_V19_20260820.md` §0 | **V-19 を「経路ごとに」課す件は承認済み**。正典への反映はレビュー側の作業 |

### レビュー側の残作業

**正典への反映**（D-076／D-077／改訂版 D-078／V-19 16項目・経路ごと／R-25／§15.5・§17.7・§14.6 追記）。**まだ `STAR_SPEC_v2.0.md` に入っていません。** 反映後に文言を突合してから実装に入る予定でした。

### オーナー判断

| # | |
|---|---|
| 1 | 🔴 **本番の service_role キー・DB パスワードのローテーション**（#9 の前提） |
| 2 | **LINE アカウントを失った利用者の扱い** — (a) 復旧なし・規約に明記 ／ (b) サポート手動復旧（★手続き自体を V-19 の対象にする） |
| 3 | **EP 日次総発行量の上限（D-077）** の承認 |
| 4 | **[EYES] PC とスマホで LINE ログインの往復を実機確認** — 「摩擦が低い」が PC でも成立するか |
| 5 | **LINE チャネル2つ**（本番用・staging 用。**必ず同一プロバイダー配下**。別プロバイダーだと `sub` が変わる） |
| 6 | 平文クレデンシャル4ファイルの整理。**★チャネルシークレット（HS256 の対称鍵）は `secrets.*.env` に置かない**（検証できる者は偽造もできる） |

---

## 5. 事故の詳細（`REPORT_ENV_INCIDENT_20260820.md` の要約）

**やろうとしたこと**: `STAR_ENV` を直したので環境の突合が通ることの確認。適用対象を消すつもりで `9999`（存在しない番号）を渡した。

**起きたこと**: `0016`（01:50:59 UTC）と `0017`（01:51:14 UTC）が本番に適用された。

**原因**: 裁定に従って足した真偽値フラグ `--yes-production` が、`migrate.mjs` の位置引数の拾い方（「`--` で始まる語の次はその語の値」という決め打ち）を壊し、`9999` が消えて**「未適用を全部当てる」**に化けた。**壊れ方が「引数が消えて既定の広い動作に落ちる」方向だったのが最悪**（狭くなるなら気づく）。

**重ねた失敗**: 1回目を `| head -4` で切ったため SIGPIPE で node が死に、適用行が見えなかった。

**影響**: 測定できる範囲でゼロ。`0016` は NULL 許容列の追加1文、`0017` は新規テーブル。`user_identities` 0行・`entrant_snapshot` 0行・ワーカー健全（直近30分で3レース生成）。

**直したこと**: 引数解析を `tools/lib/args.mjs` に切り出し、値を取るフラグを明示する形に。`apps/cli/test/args.test.ts` で固定（6件・事故そのものの形を先頭に）。

---

## 6. 開発環境の要点（次のセッターへ）

```bash
npm test            # 923 件 PASS（このセッション終了時点）
npm run typecheck   # クリーン
npm run mutation    # 変異試験（V19-1〜13 を含む。全テストを回すので1件あたり約2分）
npm run dev:web     # http://localhost:3100 （/setup と /stable が今回の対象）
```

**DB を触るときの必須事項（★事故のあと変わりました）**:

```bash
npx tsx tools/migrate.mjs --env staging            # ← --env は必須。既定は廃止した
npx tsx tools/migrate.mjs --env staging 0017       # ← 位置引数で1件だけ
npx tsx tools/migrate.mjs --env production --yes-production   # ← 本番は二段目の明示が要る
npx tsx tools/verify-v19-db.mjs --env staging      # V-19 の DB 側 10 件
```

| 環境 | 秘密ファイル | プロジェクト | 用途 |
|---|---|---|---|
| **本番** | `secrets.production.env` | `hfvvxwoulrjqznperici` | **10分サイクルのワーカーが稼働中。触らない** |
| **staging** | `secrets.staging.env` | `pezntijqkobbzqatiuxd` | **唯一の非本番。検証はここ** |

**★`development` 環境は存在しません。** 迷ったら `app_environment` を DB に聞くこと（人の記憶と名前は当てにならない、というのが今回の教訓）。

**憲法（CLAUDE.md §0）**: 実在の競馬場・レース名・馬名・他社製品名を書かない／`Date.now()`・`Math.random()` を直接呼ばない（`packages/auth` も現在時刻は注入している）／`git.txt`・`secrets.*.env`・`壊す用のsupabse情報.txt` を読まない・コミットしない／**参加ポイントを金銭で買える経路を絶対に作らない**。

---

## 7. 次にやること（推奨順）

| # | やること | 前提 |
|---|---|---|
| 0 | **V: ドライブの復帰確認**、この引継ぎ書をリポジトリへコピー、一時ファイルの削除 | — |
| 1 | **事故の判断を仰ぐ**（`0016`/`0017` を残すか戻すか・本番コードが D-055 より前である件） | レビュー側 |
| 2 | **`QUESTIONS_SETUP_RPC_20260820.md` の裁定を受ける** → セットアップ RPC を書く → **V-19 #14 を固定**（`verify-v19-db.mjs` に足すだけ） | レビュー側 |
| 3 | **anon 露出の全数確認** — **staging で期待される姿（公開ビュー以外は0行）を確定させてから、本番を1回**。`horses.potential`・`races.server_seed` が anon で読めたら §12.4・§8.6 が丸ごと崩れる。**未着手** | すぐ着手可 |
| 4 | 本番の鍵ローテーション → **V-19 #9**（セッション発行の認可）を staging で | オーナー |
| 5 | LINE チャネル2つ → **V-19 #4** の実体・Edge Function 本体・`/login`・`/signup` の差し替え | オーナー |
| 6 | 正典への反映を受けて、V-19 16項目を正典の文言で突合 | レビュー側 |

**★3 は誰の判断も待たずに着手できます。** そして §8.6（Provably Fair）と §12.4（素質を本人にも見せない）に直結するので、**優先度は高い**です。
