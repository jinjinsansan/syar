# 報告 — T-10（AL-1〜AL-3）: 素質を 24 段にし、画面から消しました

**案件**: STAR（ポイ活型オンライン競馬育成）★ 7DAYS ではありません
**発行**: 2026-09-18 ／ **開発側 → レビュー側・オーナー**
**根拠**: 正典 **D-114**（オーナー決定 2026-09-18）／ 裁定 `REVIEW_CONSULT_ARCADE_LOOP_ANSWER_20260918.md` §1-4（AL-1〜AL-3）・§6（便の順 4）
**開始 HEAD**: `6eacff1` ／ **担当範囲**: 素質の段と、その表示・帯・出品

> ★**着順にも経済にも効かせない便**として実装しました（便の順 §6-4「T-10 は着順に効きません」）。
> ★**ただし、正典に無い決めを 2 つ置きました**（§2）。★**レビュー側の確認をお願いします。**

---

## 0. 結果（★一覧）

| 指示 | 中身 | 状態 |
|---|---|---|
| **AL-1** | `STAR_THRESHOLDS` を **24 段**に置き換える（★算出は `stars.ts` の 1 か所のまま） | ✅ |
| **AL-2** | ★**段を外に出す口を塞ぐ**（`StableHorse.stars`・`horse_market_listing.stars` を含む）。★検査は「入力の形」で | ✅ |
| **AL-3** | ★内部の 24 段は「帯」としてだけ使う | ✅ |

| 検証 | 結果 |
|---|---|
| `npm run typecheck` | ✅ **通過**（★strict・`any` 無し） |
| `npm test` | ✅ **2,121 / 2,122**（★残り 1 件は**この便と無関係**・§5） |
| `npm run verify:band -- --seeds 42` | ✅ **24.4 秒で完走**（★24 段で出ました・§4） |

---

## 1. 何を変えたか

### 1-1. ★段の算出（`packages/sim-engine/src/stars.ts`）

| | 旧 | 新 |
|---|---|---|
| 段の数 | ★**9 段**（★1〜5・0.5 刻み） | ★**24 段** |
| 返す値 | ★`starsOfPotential` → **小数**（1.0, 1.5, …） | ★`bandOfPotential` → ★**整数 0〜23** |
| 境目 | `[420, 470, 520, 570, 620, 670, 720, 800]`（★不均一） | ★**420 から 20 刻み ＝ 23 本**（420 … 860） |
| 画面 | ★**出す**（`<Stars>` の部品） | 🔴 ★**出さない**（★部品ごと削除） |

★**名前を変えました**（★`starsOfPotential` → `bandOfPotential` ／ `starsOf` → `bandOf` ／ `sameStarBand` → `sameBand`）。
★**理由**: ★「★（stars）」は ★**見せるもの**の名前で、★D-114 で見せなくなったからです。★名前が古いままだと、
★次に触る人が「画面に出してよいもの」と読みます。

🔴 ★**整数にしたのは、静かに壊れる形を潰すためです。**
★24 段では 1 段が目盛 4 ÷ 23 ≒ **0.174** です。★**小数の等値比較で帯を判定すると外れます。**
★実際、★出品の帯（`LISTED_BANDS = [2.0, 2.5, …]`）は ★**24 段の出力と 1 つも一致しなくなり**、
★**市場が黙って空になるところでした**（★§3-1）。

### 1-2. ★画面から段を消した（AL-2）

| # | 場所 | 何があったか |
|---|---|---|
| 1 | `components/ui.tsx` | ★**`Stars` の部品そのものを削除**（★置き場を残さない・R-29） |
| 2 | `lib/stable.ts` `StableHorse` | ★型から `stars` を削除（★型に列があると「埋めるために読む」が起きる） |
| 3 | `lib/setup.ts` `InitialHorse` | ★同上 |
| 4 | `stable/page.tsx` | ★一覧の「素質」列（★見出し＋行＋列幅） |
| 5 | `stable/[horseId]/page.tsx` | 🔴 ★**`★{h.stars}` と数値を直接出していました** |
| 6 | `training/page.tsx` | ★2 か所 |
| 7 | `components/horse-resume.tsx` | ★1 か所 |
| 8 | `setup/page.tsx` | ★付与直後の「素質」枠 |
| 9 | `stable/market/page.tsx` | ★★の帯ごとに並べていた画面（★§3-1） |

⚠️ ★**見た目は作っていません**（2026-09-15 オーナー指示・デザインはデザイナー経由）。
★**消した場所は空けたままにしてあります**（★代わりの見せ方を開発側で発明しない）。★註記にそう書きました。
→ ★**デザイナー便への申し送りが要ります**: ★`/stable` の一覧は **1 列（112px）減って**次走の列が広がります。
★`/setup` の付与直後の画面は ★**枠が 1 つ空きます**（★D-116 の 4 層で埋めるのが筋と考えます）。

### 1-3. ★出品から段を落とした（`0036` ＋ `horse-market.ts` ＋ `market-flow.ts`）

★D-114 ② が ★**`horse_market_listing.stars` を名指し**しているので、★**列ごと落としました**（`0036`）。

| | 旧 | 新 |
|---|---|---|
| 出品が持つ列 | `horse_id, stars, price_ep, sell_back_ep` | ★**`horse_id, price_ep, sell_back_ep`** |
| 「帯が変わった馬」の判定 | ★保存した `stars` と比べる | ★**価格で比べる**（★価格は帯の関数なので同じ判定） |
| 画面 | ★`LISTED_BANDS.map` ＋ `priceOfStars(band)` | ★**値段の並びを回す**（★見本 `DEMO_MARKET_PRICES_EP`・本番は `price_ep`） |

🔴 ★**`numeric(2,1)` の列を残していたら、毎サイクル全出品が入れ替わっていました。**
★24 段の目盛（1.174, 1.348, …）は ★**`numeric(2,1)` で丸められ**、★読み戻すと元の値と一致しません。
→ ★`planListings` が毎回「帯が変わった」と判定して ★**全部下ろして全部足し直します**（★静かな壊れ方・R-16）。

---

## 2. 🔴 ★正典に無い決めを 2 つ置きました（★ご確認ください）

### 2-1. ★**段の境目の規則**（★D-114 は「24 段に差し替える」までしか書いていません）

★**420 から 20 刻み**（420, 440, …, 860）にしました。★根拠は 3 つです:

| # | |
|---|---|
| ① | ★**下端 420 は旧 9 段の下端そのまま**（★正典 §5 の値域から引いた値を、24 段化で動かさない） |
| ② | ★**幅 20 は均等**（★旧 9 段は 50×6 ＋ 80 と不均一。★細かくするときに不均一を保つ根拠がありません） |
| ③ | ★**上端 860 は実測の分布の外側**（★`verify:band` の 40 世代 × 400 頭で、素質の平均の最大は 824） |

⚠️ ★**外からは観測できません**（★D-114 ② で段を見せないため）。★**帯の粒度だけが変わります。**
★根拠は `stars.ts` の註記と較正の登録簿の両方に書きました。

### 2-2. ★**出品する帯の書き直し**（★口数と価格の水準は変えていません）

★旧 `LISTED_BANDS = [2.0, 2.5, 3.0, 3.5, 4.0]` は ★**24 段の出力と 1 つも一致しません**（★§1-1）。
→ ★**旧の 5 つの値にいちばん近い段**に書き直しました（★四捨五入・3.0 の同値は上へ）:

| 旧（★） | 2.0 | 2.5 | 3.0 | 3.5 | 4.0 |
|---|---|---|---|---|---|
| **新（段）** | **6** | **9** | **12** | **14** | **17** |
| 目盛 | 2.043 | 2.565 | 3.087 | 3.435 | 3.957 |
| 価格 [EP] | 4,087 | 5,130 | 6,174 | 6,870 | 7,913 |
| （旧の価格） | 4,000 | 5,000 | 6,000 | 7,000 | 8,000 |

★**口数は 15 口のまま**、★**価格は最大 1.9% の差**です。

⚠️ ★**素直に「段 6〜17 を全部出す」と 12 帯 × 3 ＝ 36 口**になり、★**D-102 ③「選び直しが成立しない」に近づきます。**
→ ★**帯の数を 5 のまま保つ**ほうが正典に沿うと判断しました。★**ここは開発側の判断です。**
★なお ★**T-11 でこの仕組みごと消えます**（★候補＝走った実績のある馬・価格＝§10.5 の式）ので、★繋ぎです。

---

## 3. ★この便で見つけた噛み合わせ（★報告のみ）

### 3-1. ★`priceOfStars` が段を読んでいたら、価格が最大 4.8 倍になっていました

★`priceOfStars(x) = max(3000, round(x × 2000))`（`horse-market.ts`）が ★**1.0〜5.0 の目盛**を読みます。
★**段番号（0〜23）をそのまま渡すと 46,000 EP** になります。
→ ★**目盛（1.0〜5.0）を残し、`starScaleOfBand(band)` で渡す形**にしました。★T-11 まで価格の水準を動かしません。
★`starScaleOfBand` には ★**段の外を渡すと落ちる**判定を入れました（★黙って丸めない・小数の段も通さない）。

### 3-2. ★`/stable/market` は T-11 とデザイナー便で作り直しになります

★この画面は ★**★の帯ごとに並べる**作りでした。★D-114 で帯が見えなくなり、★T-11 で候補の選び方も価格の根拠も変わります。
→ ★**この便では「段を画面に渡さない」だけ**にし、★**値段で並べる形**に最小限で直しました。★見た目は触っていません。

---

## 4. ★検証

### 4-1. ★検査は「入力の形」で書きました（★D-114 ②・D-108 ③）

★新設: **`apps/cli/test/no-potential-in-web.test.ts`**（★`0034` の `my-horses-view.test.ts` と同じ「★**全数分類**」）

| # | 見ているもの |
|---|---|
| ① | ★`@star/sim-engine` の**公開名を実物から数え上げる**（★`index.ts` の再輸出を辿る。★手書きの一覧にしない・R-19） |
| ② | ★`LEAKS` に挙げた名前が**実在する**（★消えた名前を見張り続けない） |
| 🔴 ③ | ★**1 つ残らず `LEAKS`（素質・段を出す）／ `SAFE`（出さない）に分類**され、★**理由が書いてある**。★分類漏れがあれば落ちる |
| 🔴 ④ | ★画面の層（`apps/web/src`）が ★**`LEAKS` の名前を 1 つも import していない** |
| 🔴 ⑤ | ★`Stars` の部品が `components/ui.tsx` に**戻っていない** |

★いまの内訳（★実物を数えました）: ★**公開名 136** ＝ **`LEAKS` 42** ＋ **`SAFE` 94**。
★合計が公開名と一致することそのものが ★③ の判定です（★分類漏れが 1 つでもあれば落ちます）。

✔ ★**噛むことを確かめました**（R-14）: ★`apps/web/src/lib/stable.ts` に `import { bandOf } from '@star/sim-engine'` を
★1 行足したら ④ が**赤**になり、★戻したら**緑**に戻りました。

⚠️ 🔴 ★**この便で「禁止語の一覧」の失敗を 1 度踏みました。**
★`not.toMatch(/\bband\b/)` と書いたら ★**`className="a-band"`（見出しの帯）に当たりました。**
→ ★語ではなく ★**「どの口を引いているか」**で見る形に直しました。★その経緯を検査の註記に残しました。

⚠️ 🔴 ★**もう 1 つ踏みました（★道具の側）**: ★機械編集で正規表現に ★**制御文字（`\b` がバックスペース 1 文字）**を
★書き込んでいました。★`/\x08band\x08/` は ★**何にも一致しません**＝「検査が緑なのに何も見ていない」形です。
→ ★編集後に ★**制御文字の走査**をかけて 6 か所見つけ、★直しました。★以後、この走査を編集のたびに回します。

### 4-2. ★既存の検査で向きを反転させたもの（★2 件）

| ファイル | 旧 | 新 |
|---|---|---|
| `jockey-market-wiring.test.ts` ⑤ | ★`<Stars` が**ある**ことを要求 | ★**無い**ことを要求（★＋ 段を出す口を引いていない） |
| `horse-resume-wiring.test.ts` ④ | ★同上 | ★同上 |

★**旧の検査は「★だけ出す」を守らせるものでした。** ★D-114 でその前提が撤回されたので、★要求が反転します。

### 4-3. ★帯の下のゲート（`verify:band`）

★24 段でそのまま回りました（★seed 42・400 頭・24.4 秒）。★段 0〜21 に分布し、★上位の段は 1〜9 頭です。

⚠️ ★**この便では合否も較正も出しません。** ★**AL-11 のとおり、GB-6 の経済の取り直しと 1 回にまとめます**（★二度測らない）。
★**帯が 9 段から 24 段になったので、帯ごとの頭数が細かくなります** — ★**線を引くときは束ね方も決めることになります。**

---

## 5. ⚠️ ★緑でない 1 件（★この便と無関係であることを確かめました）

★`packages/render/test/edit-grammar-audit.test.ts` ⑨「seed 分類を現 HEAD で再確認している」
→ ★**`独走代表: expected 'contest' to be 'solo'`**

| # | 確かめたこと | 結果 |
|---|---|---|
| 1 | ★この便で `packages/render` を触ったか | ✔ **触っていません**（`git status` で未変更） |
| 2 | ★何を読んでいるか | ✔ ★**`out/2d-edit-grammar/race-edl.json`**（★git に入っていない**測定の成果物**） |
| 3 | ★その成果物の日付 | 🔴 ★**2026-09-12**（★その後のエンジンの変更を反映していない） |
| 4 | ★HEAD（`6eacff1`）の作業ツリーで走るか | ✔ ★**走りません**（★成果物が無く、★`load()` が落ちます） |

→ ★**古い測定 JSON が原因**です。★**作り直すのは `tools/audit-edit-grammar-*` を回す便**で、★この便の担当範囲外です。
⚠️ ★**作者を理由に対象外にはしません** — ★**引継ぎ先**: ★映像の便。★**再確認の条件**: ★`tools/audit-edit-grammar-*` を
★現 HEAD で回し直したうえで、★seed 474 が `solo` に分類されるか。★**分類が変わっているなら、それ自体が報告に値します。**

---

## 6. ★変更の量

| 区分 | ファイル | 行（＋/−） |
|---|---|---|
| ★エンジン | `packages/sim-engine/src/stars.ts` ＋ その検査 | +158 / −70 |
| ★出品 | `packages/scheduler/src/horse-market.ts` ＋ 検査 2 本 | +141 / −90 |
| ★ワーカー | `apps/worker/src/market-flow.ts`・`main.ts` ＋ 検査 | +76 / −46 |
| ★画面 | `apps/web/src` 9 ファイル | +76 / −84 |
| ★道具・検査 | `calibration.ts`・`verify-initial-band.ts`・既存検査 2 本 | +63 / −33 |
| ★新設 | `db/migrations/0036_drop_listing_stars.sql`（2.5 KB）／ `apps/cli/test/no-potential-in-web.test.ts`（13.6 KB） | — |

---

## 7. ★まだやっていないこと

| # | | 誰が |
|---|---|---|
| 1 | ★**`0036` の適用**（★staging も本番も未適用。★`0021`〜`0036` がまとめて未配備） | オーナー |
| 2 | ★**デザイナー便への申し送り**（★`/stable` の列が 1 つ減る・`/setup` の枠が 1 つ空く・`/stable/market` は作り直し） | 開発側 → デザイナー |
| 3 | ★**AL-11 の較正の取り直し**（★V-4・V-5・V-6・V-14・§3.4・帯の下のゲート）を **GB-6 と 1 回で** | 開発側（★次） |
| 4 | ★**T-11**（AL-4・AL-5・購入を戦績ベースへ）。★`priceOfStars`・`LISTED_BANDS`・`starScaleOfBand` はそこで消えます | 開発側 |
| 5 | ★**UI1-1〜UI1-5**（★`create_account` が中で初期馬を選ぶ・裁定 Q-UI-1 の案 A） | 開発側 |

★**ローカルにコミットしました。push はしていません**（★オーナー指示待ち・★未 push は 70 件を超えています）。
