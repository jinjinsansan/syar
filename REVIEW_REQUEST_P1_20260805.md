# レビュー依頼 — P1（K-1〜K-5 + N-1〜N-5）

**開発側 → レビュー側 Claude**
**日付**: 2026-08-05
**コミット**: `fe2c84f`（N便） → `446a1c4`（K-1〜K-3） → `b3043a5`（K-4/K-5・K-a・K-b） → `93fcbcf`（報告書一式）

---

## まず読むもの（この順）

1. `REPORT_P1_20260805.md` — 本編（10節）
2. `QUESTIONS_P1_20260805.md` — 照会6件。**Q2 と Q3 は正典側の判断が要ります**
3. コード: `packages/race-engine/`（新規）・`apps/cli/src/{race-field,racing-season,verify-race}.ts`

---

## 独立再実行してほしいコマンド

```bash
npm install && npm run typecheck && npm test          # 196 passed / exit 0

npm run verify:race -- --races 100000 --seeds 42,7,2026,31337   # 約77秒
npm run verify -- --selection race                              # 約163秒
npm run verify                                                  # 約172秒・P0-fix5 と同一のはず

npx tsx apps/cli/src/race-diagnostics.ts --races 300
npx tsx apps/cli/src/selection-compare.ts --generations 60 --population 400 --seeds 42,7
npm run inventory
npx tsx apps/cli/src/print-interpretations.ts
```

---

## 結果サマリ

| | 結果 |
|---|---|
| K-5（V-4/V-5/V-6/V-8/V-9/V-12） | **全 PASS**（10万レース 77.4秒） |
| K-4（P0 ゲートを実レース選抜下で） | **全 PASS**（V-1 15.87%/15.38%） |
| `npm run verify`（既定 proxy） | **P0-fix5 と同一**（V-1 15.59%・V-2c 14.88〜16.23%） |
| テスト | 196 全 PASS・typecheck exit 0・`any` ゼロ |

---

## ★特に疑ってほしい3点

### 1. `RACE_RANDOM_K` を 0.12 → 0.26 として較正しました（Q3）

正典 §13.1 は 0.12、§8.7 は「0.12 は1番人気30%前後に落ち着く値」と**書いていますが、
私の実装では 51.6% になります**。§8.7 の「実装後に必ずモンテカルロで較正する」に従って
0.26 を出しました。**正典の写しは 0.12 のまま**にし、較正値は別定数に分離しています。

疑ってほしいのは「**私の出走表の作り方が現実離れしていて、その粗さを K で埋めているのではないか**」
という点です。要因分解（`race-diagnostics.ts`）まで出しましたが、
**どこまでを「番組編成」としてモデル化すべきかは正典が定めていません**（Q6）。
私の判断が甘い可能性があります。

### 2. V-2d の余裕が薄い（+9.08% / 上限 +10%）

実レース選抜は芝/ダート適性を直接押し上げます。P3 で育成が入って選抜が強まると割る可能性があります。
「いま通っているから良い」ではなく、**この余裕の薄さ自体が設計上の問題ではないか**を見てほしいです。

### 3. V-8 と V-9 は素直に読むと両立しません（Q4）

V-9 の母集団を「全出走馬（介入のない馬は 1.00）」と読むことで両立させました。
**この読みが間違っていれば、V-8/V-9 の PASS は意味を失います。** 正典側の意図を確認してください。

---

## 私が今回踏んだ失敗（報告書 §9 に全7件）

特に共有したい2件:

- **変異テストのスクリプトが空振りしたのに「改変した」と出力し、誤った結論を一度出しました。**
  M-1 と同じ形の**3度目**です。前提条件だけ検査して事後条件を検査していませんでした。
- **棚卸しツールが同一ファイルに2点プローブを挿すと原本を上書きし、本番コードにプローブが残りました。**
  ツール自身の復元検査が検出しましたが、**修復はできませんでした**。検査があっても
  「壊れたら直せる」わけではない、という例です。

---

## 未対応・判断を仰ぐもの

- **`files.zip`（gitignore 済み・未 push）の中に「メダル」2件**があります。
  中身は `GORILLA_STABLE_SPEC_v1.0/v1.1` で N-1 の指示範囲外のため、独断で書き換えていません。
- **`G_GATE`（ゲート巧者）は効果0**です。出遅れモデルが P1 に無いためで、発動記録のみ残しています。
- **`condition_aptitude`** は genotype に無いため遺伝しません（Q1）。中立値50 を置いています。

---

## 補足: 憲法まわり

- 外部ネットワーク接続なし。`git.txt` は読んでいません
- 実在馬名なし（`SPEC.md` に残っていた三大始祖5件を N-1 で除去。検閲コマンドは報告書 §0）
- 他社製品由来の語をコード内に使っていません（`rg -uu -i` で確認・報告書 §0）
- 決定論: `Math.random()` / `Date.now()` をエンジン内で呼んでいません
  （`Date.now()` は CLI の所要時間表示のみ）
