# 🔴 報告・照会（P4）— 揺らぎの裁定を実装したら、**裁定どうしが衝突しました**

## 本便で正典の改訂が要る決定をしたか

★**はい。2件**（§4）。うち1件は**こちらでは決められません**。

---

## 0. まず、こちらの誤りを1つ訂正します

前便で「**V-16 は投影を測っていない**」と書きました。**不正確でした。**

`tools/verify-readable.mjs` は ★**すでに `sceneAt(...)` の描画コマンドを読んでいます**。
正しくは「★**`sceneAt` が真横のままなので、新しい投影を測っていない**」です。

→ ご指摘の①「描画コマンドを読む形に直す」は、★**仕組みとしては既にそうなっています**。
   残っているのは **`sceneAt` を斜め俯瞰に対応させること**で、そうすれば
   ご指摘のとおり **`w` は自動的に見えるようになります**（描けば見え、描かなければ見えない）。

---

## 1. ②（揺らぎの減衰）を実装しました

### ★根本が見つかりました

```
旧: PHASE_JOSTLE = [0, 0.2, 1.0]   ← 道中ゼロ・直線が最大
```

★**裁定と正反対でした。** そしてこれが V-16 の失敗をそのまま説明します:

| V-16 | 失敗の理由 |
|---|---|
| ③ スタート直後で既に読めすぎ | ★**序盤に揺らぎが無いので、漏洩が隠れない** |
| ② 残り200m で有意差なし | ★**直線で揺らぎが最大なので、画面が嘘をつく** |

→ `[1, 1, 1]` に変え、**残り 800m から滑らかに 0 へ**落とす減衰を入れました。

### ★実装で1つ、本物の欠陥を出しました

最初、減衰を「**今いる位置**」で計算しました。すると係数が時間とともに動くので

```
d/dt[ t + k(t)·(e(t) − t) ] = 1 + k'·(e − t) + k·(e' − 1)   ← ★負になり得る
```

★**馬が後ろに下がって見えます。** 既存の検査が捕まえました。
→ **区間ごとの定数**にしました（区間の境界では歪みが 0 なので、段で変わっても位置は跳ねません）。

### ★既定は1か所にしました

```
export const DEFAULT_JOSTLE = 0.25;   // packages/render
```

`tools/verify-readable.mjs` ／ `tools/diag-overtake.mjs` ／ `/race` ／ `/race-next` が
**すべてここから輸入**します（判定 0.06・製品 0.25 で離れていた件）。

---

## 2. 🔴 ★そして、裁定どうしが衝突しました（実測）

```
jostle   道中の順位変動（1角→2角 / 2角→3角）    V-16 ③（スタート直後）
0        ★0.03 / 0.43                          ★FAIL（+0.218・許容 0.188）
0.05      2.71 / 1.77                           PASS（+0.008）
0.10      3.10 / 2.76                           PASS（−0.040）
0.18      3.31 / 3.31                           PASS（①が落ちる）
0.25      3.35 / 3.55                           PASS（−0.061）
```

★**両立する振幅がありません。**

- **0.05 でも道中が 2.71着 動きます。** これは以前 ★「ファミコンのゲームのように
  前後を入れ替わり続けている」として直した状態（2.2着）**より悪い**です
- 通過順位の実例: `1-6-1-6-9` / `2-5-6-10-4` — ★**前3つが揃いません**

### ★なぜ両立しないか

★**漏洩を隠すことと、道中の順位を動かさないことは、同じものの裏表です。**

```
道中の位置は、結果から作られている（replayOf(result, 脚質, ペース)）
  → 位置の順位 = 着順
  → 隠すには順位を動かすしかない
  → 動かせば「道中は動かない」が壊れる
```

---

## 3. ★提案 — **道中の順位を、結果ではなく脚質から作る**

```
いま      道中の位置 = f(走破タイム, 脚質, ペース)   ← ★結果が入っている
提案      道中の位置 = f(脚質, ペース, レースのシード)  ← ★結果が入っていない
          勝負所〜直線で、真の順位へ収束させる
```

こうすると:

| | |
|---|---|
| **漏洩** | ★**そもそも無い**（道中に結果の情報が入っていない）。隠す必要がないので揺らす必要もない |
| **道中の安定** | ★**戻る**（順位は脚質で決まり、ふらつかない。逃げが前・追込が後ろ） |
| **V-16 ③** | 通る（スタート時点では脚質しか読めない＝出走表ボットと同じ） |
| **V-16 ②④** | ★**勝負所以降で初めて差が出る**＝ちょうど求められている形 |
| **D-059** | ★**着順は動きません**（収束先が確定着順） |

⚠️ ★これは `replayOf`（`race-engine`）の設計変更です。**こちらでは決められません。**

⚠️ また、レビュー側の言葉を借りれば
> 「道中は元から演出であり、問うべきは『真実か演出か』ではなく
>  **『漏らす演出か、劇になる演出か』**である」（D-063）

★**提案はまさに「漏らさない演出」にする案**です。

---

## 4. 🔴 裁定をお願いします

### 🔴 Q-P4-38 — 揺らぎと道中

- **A.** ★**§3 の提案（道中の順位を脚質から作る）**を採りますか
- **B.** 採らない場合、**どちらを優先しますか**
  - B-1 漏洩を隠す（V-16 ③）→ 道中が 2.7〜3.5着 ふらつく
  - B-2 道中を動かさない → V-16 ③ が FAIL のまま
- **C.** ⚠️ **いまのコードは裁定どおり（B-1）にしてあります。**
  ★**道中がふらつく状態です。** 差し戻すなら言ってください

### 🔴 Q-P4-39 — V-16 ④ の判定そのもの

④ の条件は `end > mid + se` です。★**mid が 0.95 まで上がると、天井（1.0）まで
`se` 未満しか残らないので、原理的に通せません**。

```
実測（jostle 0.05・600レース）: 道中 0.756 → 勝負所 0.952 → 直線 0.984
  ★+0.228 上昇しているのに「平坦」と判定されます（se ≈ 0.07）
```

- **A.** レース数を増やして `se` を下げますか（4800 レースなら通る見込み）
- **B.** 条件を「**上昇していること**」に変えますか（天井の効果を織り込む）
- ⚠️ ★**こちらで閾値をいじって通す気はありません。**

---

## 5. Q-P4-37（アートバイブル §3）— 承認をいただいたので改訂案を出します

**旧**
> 奥行きは**速度差だけで作る**（線遠近を描き込まない）

**新（案）**
> 奥行きは**投影**で作る（斜め俯瞰）。走路は台形になり、コーナーではラチが曲がる。
> ★**ただし「手前ほど大きく」はしない。倍率は 1× と 2× のみ**（D-058）。
> 奥行きは**接地線の y と重なり順**で表す。
>
> ★**均一倍率の根拠は D-058 の整数倍だけではない。P-3（18頭が並んでも自分の馬が
> 一目で分かる）である。** 奥の馬が小さくなると、向こう正面の馬ほど識別しにくくなる。
> ★**均一倍率は、揃えるためではなく読ませるため。**（レビュー側の指摘をそのまま）

---

## 6. ★枠番の色 — ご指摘のとおりでした

```
palette.json の silk-1 〜 silk-18 は ★18色すべて別々
```

★**意図した設計ではありません。単に頭数ぶん色を割り当てただけです。**

→ **枠色8色 ＋ 馬番**に寄せます（D-060 が「機能的な作法」として明示的に許している形）。
   ★**色は枠、数字は個体。** 18頭でも 8色で済み、「いちばん近い2色」の問題も同時に解けます。

⚠️ **まだ実装していません**（Q-P4-38 の裁定でレンダラ側の作業順が変わるため）。
   ★**先にやってよければ、そう言ってください。**

---

## 7. いまの状態

```
typecheck 緑 ／ ★781件 緑（+5: 揺らぎの減衰）
```

| 変えたもの | |
|---|---|
| `packages/render/src/replay-model.ts` | `PHASE_JOSTLE [0,0.2,1.0] → [1,1,1]`／残り 800m からの減衰／`DEFAULT_JOSTLE` |
| `tools/verify-readable.mjs`・`diag-overtake.mjs` | ★製品の既定を**輸入**（別々に持たない） |
| `/race`・`/race-next` | 数字の直書きをやめ、既定に任せる |
| `packages/render/test/replay-model.test.ts` | 「道中は動かない」→「道中はシードで変わる」（★意味が反転した経緯を全部残した） |
