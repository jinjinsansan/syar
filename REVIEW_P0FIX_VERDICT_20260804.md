# P0-fix レビュー回答 — レビュー側 → 開発側／オーナー

**発行**: 2026-08-04（レビュー側 Claude）
**対象**: `REVIEW_REQUEST_P0FIX_20260804.md` / `REPORT_P0FIX_20260804.md`（commit `40b9cee`）
**判定**: **CONDITIONAL PASS** — F-1/F-2/F-3 は指示書どおりで報告値も完全再現。**ただし未申告の副作用1件（丈夫さの恒久低下）の是正が必要**

---

## 0. 結論

| 項目 | 判定 |
|---|---|
| `npm run verify` の出力 | **CONFIRMED** — 報告書 §3 の表と**一行一句まで完全一致** |
| ユニットテスト58件 / typecheck | **CONFIRMED** — 独立実行で全PASS・exit 0 |
| 決定論 | **CONFIRMED** — 同一シードで JSON の MD5 一致 |
| F-1 回帰の既定有効化 | **CONFIRMED**（定数・テスト固定とも） |
| F-2 V-2 新3基準 | **CONFIRMED**（旧基準の構造的排除を含む） |
| F-3 距離適性 | **CONFIRMED**（`verify:distance` の表も完全再現） |
| 検算依頼 2-1（収縮への反転） | **CONFIRMED — 開発側の指摘が正しい**（§2） |
| 検算依頼 2-2（小集団の浮動） | **CONFIRMED**（seed 42・繁殖1000頭で 1.20倍・V-1 15.39%） |
| 自己申告した逸脱4件 | **すべて承認**（§4） |
| **未申告の副作用: 丈夫さ 650→450** | **🔴 要是正**（§3・オーナー決定 D-009 済み） |
| その他の回収項目4件 | 次の指示書で対応（§5） |

**合格基準7項目は成立している。** 報告に誇張・隠蔽はなく、§4-4 の「0.67x〜1.53x の振れ」「1.2倍ぶんは大物覚醒」という不利な事実の自己申告も、レビュー側の独立実測と一致した。

---

## 1. レビュー側が実際に実行したもの

```bash
npm test                                            # 58件 PASS
npm run typecheck                                   # exit 0・any ゼロ
npm run verify                                      # 報告書 §3 と完全一致
npm run verify:distance                             # 報告書 §4-2 と完全一致
npm run simulate -- --generations 300 --seed 42 --population 1000 --stallion-pool 300
npm run simulate -- --generations 100 --seed 42 --json out/rv_a.json --quiet  # ×2 → md5 一致
```

加えて並列サブエージェントで `git diff 4da5890 1321ce4` の全変更を file:line 監査した。

---

## 2. 検算依頼への回答

### 2-1. 「F-3 は拡散だけでなく収縮も防いでいる」— **開発側が正しい**

`verify:distance` を独立実行し、報告書 §4-2 の表を完全再現した:

| 設定 | 100年 SD | 300年 SD | 600年 SD | 挙動 |
|---|---|---|---|---|
| 旧（値域比 SD117）+ 回帰0.20 | 157 | **137** | 141 | **収縮**（創始359の0.4倍） |
| 採用（SD73 r0.01） | 430 | 416 | **377** | 創始水準へ収束 |

**レビュー側監査 A-2 の「拡散」は回帰が無効だった P0 時点の観測であり、F-1 で回帰を入れると失敗の向きが収縮へ反転する** — この指摘は正確で、F-3 が F-1 とセットで必要だったことも支持する。正典 §6.4 に「距離系だけ回帰率が弱い理由」として明文化した（Q1 への回答）。

### 2-2. 距離SDのシード間の振れは小集団の artifact — **確認**

seed 42・繁殖牝馬1000頭・300年で **距離SD 1.20倍・V-1 15.39%** を再現（報告値と一致）。距離適性には選抜がかからないため浮動が支配的で、有効集団サイズが小さいほど振れるという説明は妥当。

---

## 3. 🔴 未申告の副作用 — 丈夫さが 650 → 450 へ恒久移動する

### 事実（コードで確認・`genetics.ts:194-195`）

回帰中心は `bounds.min + (bounds.max - bounds.min) × 0.45` で計算され、`NUMERIC_TRAITS` の**11形質すべてに一律適用**される。ところが創始分布の中心は形質ごとに違う:

| 形質 | 創始平均 | 回帰中心 | 選抜圧 | 帰結 |
|---|---|---|---|---|
| 能力5種 | 450 | 450 | あり | 一致（意図どおり） |
| **丈夫さ** | **650** | **450** | **なし** | **-31% へ恒久移動**（実測 20年で449に収束） |
| 気性 | 50 | 45 | なし | -5pt |
| 芝/ダート適性 | 約55 | 45 | なし | -10pt |

### なぜ問題か

丈夫さは種牡馬選抜の対象外（選抜は `abilityTotal` = 能力5種のみ）なので、回帰に対抗する力がない。正典 §7.5 で**故障率の分母**であり、`INJURY_BASE = 0.0018` は創始水準を前提に置いた値。650→450 は**故障率が約1.44倍に恒久的に上がる**ことを意味し、P1 の育成バランスの基準線が丸ごとずれる。

**しかも現行の検証構造では検出できない** — V-1/V-2a〜c/V-3 はいずれも能力5種しか見ておらず、`CohortStat` に丈夫さ・気性・適性が記録されていない。

### レビュー側の非

**これはレビュー側の指示 F-1（回帰を全体で有効化）の副作用である。** 回帰中心を値域から機械的に出すと、創始分布の中心と一致するのは能力5種だけだという点を、指示を書いた時点で詰めていなかった。開発側の実装は指示に忠実だった。

### オーナー決定（D-009）

**回帰中心を形質ごとの創始水準にする**（丈夫さ650・気性50・適性55・distance_center 2100・distance_range 700）。正典 §6.4 に形質別パラメータ表を新設し、§13.1 に `TRAIT_MUTATION` を記載した。

併せて、このクラスの退行を機械的に捕まえるため検証基準を2つ新設した:

- **V-2d 全形質の水準維持**: 能力5種だけでなく丈夫さ・気性・適性・距離の集団平均が創始水準の ±10% 以内
- **V-2e 距離適性の分化**: `distance_center` の集団SDが創始水準の 0.8〜1.4倍（合格基準4 を目視から機械ゲートへ）

---

## 4. 自己申告された逸脱4件 — すべて承認

| # | 内容 | 判定 |
|---|---|---|
| 1 | V-2a を傾きの**絶対値**で判定 | **承認**。基準名が「平坦化」である以上、急落も平坦ではない。文字どおりより厳しい側であり妥当 |
| 2 | 距離系の回帰率を 0.01 に（全体は 0.20） | **承認**。正典 §6.4 に理由込みで明文化した（Q1 への回答） |
| 3 | 距離系の clamp/sd 比 3.33（能力5種は 1.67） | **承認**。1.67 では平衡SDが創始水準を下回るという実測に基づく |
| 4 | 絶対値指定形質で大突然変異SDの比（2倍）を保つ | **承認**。正典 §6.4 の記法をその形に合わせた |

---

## 5. 次の指示書で回収する項目（レビュー側監査で発見）

| # | 内容 |
|---|---|
| G-1 | **F-3 の定数がテストで固定されていない**。`regression.test.ts` の距離テストは検証対象の定数を自分で読んで自分と比較する**トートロジー**で、73 が 200 に変わっても素通りする。F-1 と同じくリテラル固定にすべき（平衡は定数に敏感） |
| G-2 | **合格基準4（距離分化）に機械判定がない**。`verify.ts` の総合判定は V-1/V-2a/V-2b/V-3 のみで、距離SDの列は情報表示にとどまる → V-2e として閾値ゲート化 |
| G-3 | **`verify.ts` の総合判定に V-2c が畳み込まれていない**。`--horizons 100` だけで回すと「V-2c 未評価」の警告なしに `総合: PASS` が出る（`simulate` 側は警告を出すので挙動が非対称） |
| G-4 | **正典 §13.1 に `TRAIT_MUTATION` が未記載だった**（レビュー側の宿題） → **対応済み**。§6.4 の表と §13.1 の定数ブロックに追記した |

軽微: `CLAUDE.md` のコマンド節が P0-fix 前のまま（`npm run verify` / `--long-horizon` が無い・退役した「V-2」呼称が残る）→ レビュー側で更新する。報告書 §2 の `cd syar` は `cd star` の誤記。

---

## 6. 未解決の質問への回答

### Q1 §6.4 の平均回帰と §5.2 の距離適性の関係 — **正典に明文化した**

§6.4 に形質別パラメータ表を新設し、「距離系だけ回帰率が弱い理由」（選抜圧がないため 0.20 では収縮する・求めるのは平均の抑制でなく創始水準への平衡）を記載した。

### Q2 大物覚醒を距離系にも適用し続けるか — **P1 の検証項目として記録・v1 では機構を維持**

指摘のとおり、P1 でレースエンジンが入り `distance_range` の大きい万能型が有利になると、選抜圧 × 単調ラチェットで万能型に収束する恐れがある。**懸念は正当**。ただし現時点で実害はなく（SDは平衡している）、機構を触るとインフレ抑制の均衡も動くため、v1 では維持する。P1 指示書に「V-4〜V-6 の較正時に距離適性の分化が選抜下でも保たれるかを確認する」項目として引き継ぐ。

### Q3 検証の既定集団規模 — **実運用規模へ寄せる（繁殖牝馬800頭）**

正典 §10.5 の NPC 繁殖牝馬プールは **800頭**である。検証の目的は実運用の予測なので、既定をそこに合わせる。既定200頭（Ne≈45）では浮動由来の artifact が大きく、V-2e のような閾値ゲートが誤検知・誤通過を起こす。

- `npm run verify`（受け入れ判定）の既定を **繁殖牝馬800頭・種牡馬プールは比率維持**にする
- 高速反復用に `--population 200` は引き続き使えるようにし、**小集団では浮動 artifact が出ることをレポートに注記**する
- P0 第1号指示書の「200頭」からの変更だが、V-2 系の基準自体が刷新済みで P0 との数値比較の必要はもうない
- **実行時間を計測して報告すること**。受け入れスイートが極端に遅くなるなら妥協案（例: 400頭）を提案してよい

---

## 7. 教訓の記録

1. **機構を全体に適用する指示を書くときは、適用先ごとの前提の違いを確認する**（F-1 の丈夫さ）。「全形質に回帰をかける」は一行で書けるが、回帰中心の定義が形質ごとに正しいかは別問題だった
2. **検証基準が見ている範囲＝退行を検出できる範囲**。V-1〜V-3 が能力5種しか見ていないことが、丈夫さの水準ずれを丸ごと見逃す構造を作っていた（V-2d で是正）
3. **「目視で確認」の基準はいずれ形骸化する**。合格基準4 は閾値のないまま置いたため、退行しても PASS が出る状態だった（V-2e で是正）
4. 開発側が不利な実測（0.67x〜1.53x の振れ）を隠さず、原因を切り分けて申告した姿勢は本レビューの前提を支えている。この様式を維持すること
