# REPORT — P4 2D 編集文法の監査（参考映像 対 通常 /race）

指示書: レビュー側 §1〜§18（2026-08-24）
日付: 2026-08-24 / ブランチ: `p4/race-30sec-cuts` / 基準 HEAD `93d270c`
成果物: `out/2d-edit-grammar/`

> **採否は書いていません。** 判定はレビュー側です（§16）。
> **製品コードを変更していません。カット・カメラ・target も変えていません。改善版動画も作っていません。**
> **commit・push もしていません。**

---

## 0. 先に、いちばん大きな差

| | 参考映像（スターホース版） | 通常 `/race`（4 seed 共通） |
|---|---|---|
| **編集上の主役** | ★**上位の競り合い集団 68.6%** ／ 馬群 21.3% ／ 不明 10.1% | ★**馬群 100.0%**（他は 0%） |
| カメラ方向 | ★**横 58.5%** ／ 俯瞰 16.5% ／ 後方 10.1% ／ 正面 4.8% | ★**正面 74.3%** ／ 横 25.8% ／ **俯瞰 0% ／ 後方 0%** |
| 画角 | 馬群 37.2% ／ ★**2〜4 頭 31.4%** ／ 全体 21.3% | ★**全体 74.3%** ／ 2〜4 頭 20.0% ／ 馬群 5.7% |
| 最長カットの占有 | 19.0s＝**25.3%** | 18.6s＝★**48.3%** |
| カット数 | 確定 9（曖昧込み最大 13） | **6**（4 seed とも同じ） |
| 平均カット長 | 8.36s（曖昧込み 5.78s） | 6.42s |
| 同じ target が続く最長 | 75.2s ※参考側は target 不明 | ★**38.52s＝全編**（`pack` のまま一度も変わらない） |
| 定義ショットの使用 | — | ★**19 個中 6 個**（32%） |

★**自馬は、ゴール前のカット（7.2〜8.4 秒）で 4 seed とも完全に画面外**です。

---

## 1. 監査範囲（§1）

レース全体の**編集構造**を比べました。素材の話には戻っていません。

確認した中心課題と、本報告書での答え:

| # | 課題 | 章 |
|---|---|---|
| 1 | 誰を主役として映しているか | §17〜§19 |
| 2 | 何頭をどの大きさで映しているか | §8・§14 |
| 3 | 何秒ごとに、何をきっかけにカットを変えているか | §6・§4・§5 |
| 4 | `homestretch-front` が約 48% を占めることの影響 | §16 |
| 5 | 接戦・独走・境界で同じ台本が成立するか | §15 |

## 2. 比較対象（§3）

| | |
|---|---|
| 参考 | `ダービースタリオン/参考映像スターホース版.mp4` **46.8〜122.0 秒（75.2 秒）** |
| 通常 `/race` | ブラウザ上の**既定 Broadcast V2**。1280×720 原寸で 24 枚撮影（`_race-shots/`） |

参考映像は**アーケード筐体を手持ちで撮った動画**です。筐体外周・下段のプレイヤー席・LED 帯・
配信ワイプは切り出しで除外しました（`crop=1400:520:100:40`）。
★ただし**ブランドワイプ自体は場面転換として使われている**ので、カット境界として記録しています（§3 の指示どおり）。

## 3. seed 分類の再確認（§4）

⚠️ ★**最初、4 seed とも `solo` と出て停止条件に当たりました。原因は測る地点の違いでした。**

`broadcastV2FinishStyleOf` は `contest | solo` の 2 値です。私は最初、
**ページ自身が finish カメラを決める地点**（先頭が残り 80m ＝ 1520m）で測りました。
過去の代表 seed は `audit-2d-contest-seeds.mjs` が**先頭 1280m（`mid`）**で選んだものでした。

同じ地点で測り直すと、指示書のラベルと**一致します**。

| seed | 用途 | entry 1100m | **mid 1280m（過去の分類地点）** | preFinish 1480m | ページ判定（1520m） |
|---|---|---|---|---|---|
| 42 | 現行既定確認 | contest 1.65m | solo 5.04m | solo 3.69m | **solo** |
| 332 | 接戦代表 | contest 1.20m | ★**contest 2.33m** ✔ | solo 9.73m | **solo** |
| 474 | 独走代表 | contest 0.13m | ★**solo 4.05m** ✔ | contest 1.71m | **solo** |
| 14 | 境界代表 | contest 2.34m | ★**contest 2.37m** ✔ | solo 10.56m | **solo** |

★境界代表 14 の `mid` での 1-2 着差は **2.37m**で、閾値 1 馬身（2.4m）との差は **0.03m**です。
過去の「境界」の選び方（`|gap2M − 2.4m|` が最小）と合っています。

★**分類は変わっていません。** 停止条件には当たりませんでした。

⚠️ ただし**重要な副産物**があります。
**ページが finish カメラを選ぶ 1520m 地点では、4 seed すべてが `solo`** です。
つまり**接戦用のゴール演出は、この 4 本のどれでも一度も起動しません**（§15）。

## 4. 参考映像の EDL（§5〜§9）

`out/2d-edit-grammar/reference-edl.json`。**人が絵を見て書いたもの**です（`method: manual-read`）。

境界候補は `tools/audit-edit-grammar-reference.mjs` が **RGB ヒストグラムのχ²距離**で出し、
**1 組ずつ前後 2 枚を並べて目視**しました。証拠は `_ref-pairs/` と `_ref-frames/` にあります。

| cut | 秒 | 長さ | 区間 | 方向 | 画角 | 追従 | 編集上の主役 | 境界 |
|---|---|---:|---|---|---|---|---|---|
| r01 | 46.8–50.4 | 3.6 | 発走 | front | whole-field | follow | pack | confirmed/high |
| r02 | 50.4–66.4 | **16.0** | 序盤〜向正面 | side | contenders-2-4 | follow | **contenders** | confirmed/medium |
| r03 | 66.4–73.6 | 7.2 | 第3コーナー | **high** | whole-field | fixed | pack | confirmed/high |
| r04 | 73.6–75.0 | 1.4 | 第4コーナー | unknown | unknown | unknown | unknown | ★**ブランドワイプ** |
| r05 | 75.0–84.0 | 9.0 | 第4コーナー | side | pack | follow | **contenders** | confirmed/high |
| r06 | 84.0–89.2 | 5.2 | 第4コーナー | **high** | whole-field | fixed | pack | confirmed/high |
| r07 | 89.2–95.4 | 6.2 | 直線入口 | **mixed** | mixed | mixed | unknown | confirmed/medium |
| r08 | 95.4–114.4 | **19.0** | 直線中盤〜ゴール前 | side | pack | follow | **contenders** | confirmed/high |
| r09 | 114.4–122.0 | 7.6 | ゴール前 | **diag-rear** | contenders-2-4 | follow | **contenders** | confirmed/high |

### 曖昧な境界（§6）

無理に確定していません。**4 か所**を `internalAmbiguities` として残しています。

| 区間 | 候補時刻 | 幅 | 理由 |
|---|---|---|---|
| r02 | 58.4s | 57.4–58.4 | 背景が「暗い内馬場」→「青い観客席」へ。カットか、同一カメラのまま馬がスタンド前へ進んだのか分離できない |
| r07 | 92.0s | 90.8–92.4 | 89.6s は遠景、92.3s は 1 頭が画面いっぱいの流し撮り。明らかに別の画だが、境界の瞬間を特定できない |
| r08 | 107.8s | 104.9–107.8 | 映っている馬の並びは変わるが、同一のサイド追従が続いているようにも見える |
| r09 | 118.0s | 116.0–118.2 | 2 頭画から単騎至近へ。カットか同一カメラの寄りか分離できない |

★**確定カット数 9 ／ 曖昧候補をすべて境界とした最大 13。**
平均カット長は **8.36 秒（確定のみ）／ 5.78 秒（曖昧込みの参考値）**。

⚠️ ★前回お伝えした「75.2 秒 ÷ 8 ＝ 約 9.4 秒」は**暫定値でした**。
その後 75.0s（ワイプ明け）も境界として確認したため、確定は 9 カットになります。

## 5. 通常 `/race` 4 本の EDL（§5〜§9）

`out/2d-edit-grammar/race-edl.json`。**実画面と同じ場面解決**（`resolveBroadcastV2Scene`）を 0.2 秒刻みで通しました。
実ブラウザの絵も各カット 1 枚ずつ撮っています（`_race-shots/` 24 枚・1280×720 原寸）。

**seed 42（他の 3 本も同じ並び）**

| cut | 秒 | 長さ | ショット | 方向 | 画角 | target | 自馬 完全可視 | 勝者 完全可視 |
|---|---|---:|---|---|---|---|---:|---:|
| c01 | 0–4.4 | 4.4 | `start-front` | diag-front | whole-field | pack | 1.00 | 1.00 |
| c02 | 4.4–6.6 | 2.2 | `first-corner-front` | diag-front | whole-field | pack | 1.00 | 1.00 |
| c03 | 6.6–8.8 | 2.2 | `side-drive` | side | pack | pack | 1.00 | 0.00 |
| c04 | 8.8–12.2 | 3.4 | `fourth-corner-front` | diag-front | whole-field | pack | 1.00 | 0.71 |
| c05 | 12.2–30.8 | ★**18.6** | `homestretch-front` | diag-front | whole-field | pack | 1.00 | 1.00 |
| c06 | 30.8–38.52 | 7.72 | `finish-line` | side | contenders-2-4 | pack | ★**0.00** | 1.00 |

★**4 seed すべてで、カット数・ショット・順序が同じ**です。違うのは長さだけ（±1 秒未満）。

## 6. カット長の比較（§12）

| | 参考 | seed 42 | seed 332 | seed 474 | seed 14 |
|---|---:|---:|---:|---:|---:|
| 対象時間 | 75.2s | 38.52s | 37.58s | 38.65s | 39.55s |
| カット数（確定） | **9** | 6 | 6 | 6 | 6 |
| 曖昧込み最大 | 13 | 6 | 6 | 6 | 6 |
| 曖昧境界数 | **4** | 0 | 0 | 0 | 0 |
| 平均 | 8.36s | 6.42s | 6.26s | 6.44s | 6.59s |
| 中央値 | 7.2s | 4.4s | 4.4s | 4.4s | 4.4s |
| 最短 | 1.4s | 2.2s | 2.4s | 2.2s | 2.2s |
| 最長 | 19.0s | 18.6s | 18.0s | 18.8s | 19.0s |
| 5 秒以上 | **7 本** | 2 本 | 2 本 | 2 本 | 2 本 |
| 10 秒以上 | 2 本 | 1 本 | 1 本 | 1 本 | 1 本 |
| **最長カットの占有** | **25.3%** | ★**48.3%** | 47.9% | 48.6% | 48.0% |

★最長カットの秒数は**ほぼ同じ（19.0s 対 18.6s）**です。違うのは**レース全体に占める割合**で、
参考は 4 分の 1、`/race` は**ほぼ半分**です。

## 7. カメラ方向の比較（§12）

| 方向 | 参考 | seed 42 | seed 332 | seed 474 | seed 14 |
|---|---:|---:|---:|---:|---:|
| 正面系（front / diag-front） | 4.8% | ★**74.3%** | 74.5% | 74.5% | 73.3% |
| 横系（side） | ★**58.5%** | 25.8% | 25.5% | 25.5% | 26.7% |
| 後方系（diag-rear / rear） | **10.1%** | ★**0%** | 0% | 0% | 0% |
| 俯瞰（high） | **16.5%** | ★**0%** | 0% | 0% | 0% |
| 不明・混在 | 10.1% | 0% | 0% | 0% | 0% |

★**向きの構成がほぼ裏返しです。** 参考は横が主・正面がほぼ無い。`/race` は正面が主・後方と俯瞰がゼロ。

## 8. 画角の比較（§12）

分類の閾値は次のとおりです（`comparison.json` にも同じ値があります）。

| 分類 | 条件 |
|---|---|
| `extreme-close` | 画面上の馬高比 ≥ 0.55 |
| `single` | 見えている馬 ≤ 1 頭 |
| `contenders-2-4` | 見えている馬 ≤ 4 頭 |
| `whole-field` | 見えている馬 ≥ ceil(頭数 × 0.8) |
| `pack` | それ以外 |

| 画角 | 参考 | seed 42 | seed 332 | seed 474 | seed 14 |
|---|---:|---:|---:|---:|---:|
| whole-field | 21.3% | ★**74.3%** | 74.5% | 74.5% | 73.3% |
| pack | ★**37.2%** | 5.7% | 6.4% | 5.7% | 5.6% |
| contenders-2-4 | ★**31.4%** | 20.0% | 0% | 19.8% | 0% |
| single | 0% | 0% | 19.1% | 0% | 21.1% |
| extreme-close | 0% | 0% | 0% | 0% | 0% |
| 不明・混在 | 10.1% | 0% | 0% | 0% | 0% |

★参考は**馬群（37%）と 2〜4 頭（31%）で 7 割**。`/race` は**全体画で 4 分の 3**。
★`extreme-close` は**どちらも 0%** です（参考側は流し撮りの一部が該当しそうですが、
そのカットを `mixed` として扱ったため計上していません — §20）。

## 9. 定義ショットと実使用ショット（§13）

定義は **19 個**（`BroadcastV2ShotId` の型定義から機械的に取得）。
4 seed のどれかで使われたのは **6 個（32%）**。

| ショット | v4 台本 | v3 台本 | 使用 seed 数 | 合計時間（seed 42） |
|---|:--:|:--:|---:|---:|
| `start-front` | ✔ | ✔ | 4 | 4.4s |
| `first-corner-front` | ✔ | ✔ | 4 | 2.2s |
| `side-drive` | ✔ | ✔ | 4 | 2.2s |
| `fourth-corner-front` | ✔ | ✔ | 4 | 3.4s |
| `homestretch-front` | ✔ | — | 4 | 18.6s |
| `finish-line` | ✔ | ✔ | 4 | 7.72s |

## 10. 未使用ショットの理由（§13）

理由は**選択の実装から**決めました（推測ではありません）。
`broadcastV2ShotAt` は既定 `script: 'v4'` で、`SCRIPT_V4` の表を上から見るだけです。

| ショット | 理由 | 詳細 |
|---|---|---|
| `second-corner-high` | 台本v4から参照されない | v3 には在るが v4 では使われない |
| `third-corner-rear` | 台本v4から参照されない | 同上 |
| `homestretch-side` | 台本v4から参照されない | 同上 |
| `aerial` | 台本v4から参照されない | 同上 |
| `front-close` | 台本v4から参照されない | 同上 |
| `start-follow` | **resolver上で到達不能** | v3 にも v4 にも無い。旧 `script:'v2'` の分岐だけ。ページは v4 固定 |
| `backstretch-side` | **resolver上で到達不能** | 同上 |
| `fourth-corner-high` | **resolver上で到達不能** | 同上 |
| `side-low` | **resolver上で到達不能** | 同上 |
| `side-close` | **resolver上で到達不能** | 同上 |
| `fourth-corner-wide` | 条件不成立 | `fourthCornerFront === false` のときの代用。ページは `true` 固定 |
| `winner-follow` | 区間外（ゴール後） | `allFinished` のときだけ。**到達は可能**だが今回の対象区間の外 |
| `winner-follow-rear` | 区間外（ゴール後） | 同上 |

★**定義されているだけのショットを、映像の多様性として数えていません。**
実際に選ばれうるのは、対象区間内では **6 個だけ**です。

⚠️ ★`side-close`（target: `contenders`）と `side-low` は、参考映像がいちばん多く使う
「横から 2〜4 頭を見せる」画に相当しますが、**既定の台本からは到達できません**。

## 11. 自馬の提示（§10）

自馬は 4 seed とも 3 番（`/race` の既定）。時間で重み付けした値です。

| | seed 42 | seed 332 | seed 474 | seed 14 |
|---|---:|---:|---:|---:|
| 完全可視 | 80.0% | 73.4% | 80.2% | 78.9% |
| 部分可視 | 0% | 1.1% | 0% | 0% |
| **画面外** | **20.0%** | **25.5%** | 19.8% | 21.1% |
| 他馬に隠れる | 46.7% | 48.4% | ★**64.2%** | 22.3% |
| 平均の馬高比 | 0.203 | 0.188 | 0.203 | 0.224 |
| 画面最大の馬になる率 | 17.1% | 0% | 3.1% | 32.3% |

★**自馬が全編で画面外になるカット**

| seed | カット |
|---|---|
| 42 | `finish-line` 7.72s |
| 332 | `side-drive` 2.4s ＋ `finish-line` 7.18s |
| 474 | `finish-line` 7.65s |
| 14 | `finish-line` 8.35s |

★**4 seed すべてで、ゴール前の 7.2〜8.4 秒のあいだ自馬が 1 コマも映りません。**
レースでいちばん盛り上がる場面です。

⚠️ ★「馬そのものが見えず HUD マーカーだけが見える時間」（§10）は**別集計できていません**。
自馬マーカーの描画位置は投影から出せますが、**マーカーが画面内にあるか**は
実画面の描画コードの中で決まるため、今回の測り方では分離できませんでした（§20）。

## 12. 先頭馬の提示（§10）

先頭馬は**カット内で入れ替わる**ので、各コマで「そのとき先頭だった馬」を見ています。

- 先頭馬は全カットで**ほぼ常に可視**（`leader.fullRatio` が各カットで 0.7〜1.0）
- ただし ★**`/race` に `target: 'leader'` のショットは 1 つも使われていません**。
  先頭が映るのは「馬群を撮ったら結果的に入っている」からです
- 参考映像では、先頭に見える馬が**画面の右寄りに置かれる**構図が繰り返し出ます（r02 / r05 / r08）

## 13. 最終勝者の提示（§10）

| | seed 42 | seed 332 | seed 474 | seed 14 |
|---|---:|---:|---:|---:|
| 完全可視 | 91.7% | 92.0% | 92.8% | 91.4% |
| 画面外 | 6.8% | 5.9% | 5.7% | 7.1% |
| 他馬に隠れる | 44.7% | 26.6% | 56.4% | 33.4% |
| 画面最大の馬になる率 | 18.2% | ★**55.3%** | 14.5% | ★**53.5%** |

★勝者は**自馬より映っています**（可視率 92% 対 74〜80%）。ただしこれは
**勝者を狙っているから**ではなく、勝者が先頭付近にいて馬群の画に入るからです。
`/race` に `target: 'winner'` のショットは、対象区間内では使われません。

★参考映像では **`winner: unknown`** です。個体番号をゴールまで追跡できませんでした（§9 の指示どおり推測していません）。

## 14. 上位集団の提示（§10）

| | 参考 | seed 42 | seed 332 | seed 474 | seed 14 |
|---|---|---:|---:|---:|---:|
| 上位 2 頭 同時可視 | ★2〜4 頭として読める区間が **68.6%** | 100% | 81.4% | 100% | 79.4% |
| 上位 3 頭 同時可視 | 同上 | 80.0% | 80.9% | 80.2% | 78.9% |
| 上位 4 頭 同時可視 | 同上 | 80.0% | 80.9% | 80.2% | 78.9% |

★**同時に「見えている」ことと、「競り合いとして読める」ことは別**です。
`/race` は上位 4 頭が 8 割の時間**画面内にはいます**が、その大半は
**全体画（74.3%）の中の小さな 4 頭**で、平均の馬高比は 0.19〜0.22（画面高の 2 割）です。
参考は同じ 4 頭を**横から大きく**見せます。

- 参考で「1 つの塊にしか見えない時間」＝ 俯瞰の 2 カット（r03 / r06）＝ **16.5%**
- `/race` で「1 つの塊にしか見えない時間」＝ 全体画 **74.3%**

## 15. 接戦・独走・境界の差（§1-5・§12）

★**台本は展開で分岐しません。4 seed の EDL は同一です。**

| | seed 42 | seed 332（接戦） | seed 474（独走） | seed 14（境界） |
|---|---|---|---|---|
| カット数 | 6 | 6 | 6 | 6 |
| ショットの並び | 同じ | 同じ | 同じ | 同じ |
| 1280m での分類 | solo | **contest** | solo | **contest** |
| **ページの finish 判定（1520m）** | solo | **solo** | solo | **solo** |

★**接戦用のゴール演出は 4 本のどれでも起動しません。**
判定地点が 1520m なので、1280m で接戦だった 332 と 14 も、そこでは差が開いています。

★展開で変わるのは**画角の出方だけ**で、しかも意図した分岐ではありません。
ゴール前カットの画角が seed 42/474 では `contenders-2-4`、seed 332/14 では `single` に出ます。
これは**その時たまたま何頭が画面に入ったか**の結果です。

## 16. `homestretch-front` 18.57 秒の影響（§1-4・§12）

| | 値 |
|---|---|
| 長さ | 18.0〜19.0 秒（seed により） |
| **レース全体に占める割合** | ★**47.9〜48.6%** |
| 方向 | diag-front（正面寄り・固定） |
| 画角 | whole-field |
| target | pack |
| この間の自馬 | 完全可視 100% だが**馬高比 0.2 前後**（画面高の 2 割） |

★**同じ構図が続く最長時間は 21.2〜22.4 秒**（`homestretch-front` と、その前後の同画角カットが連続するため）。
★**同じ target が続く最長時間は 37.6〜39.6 秒＝全編**。`pack` から一度も変わりません。

参考映像でいちばん長いカット（r08・19.0 秒）は**横から馬群を大きく見せる**画で、
`/race` の 18.6 秒は**正面から全体を小さく見せる**画です。**長さは同じでも中身が逆**です。

## 17. 参考映像が主役として扱う対象（§9）

| 主役 | 時間比 |
|---|---:|
| **contenders（上位の競り合い）** | ★**68.6%** |
| pack（馬群全体） | 21.3% |
| unknown | 10.1% |
| self | 0%（判定不能） |
| leader | 0%（個体特定不能） |
| winner | 0%（追跡不能） |

★参考映像は**レースの 3 分の 2 を「競り合っている 2〜4 頭」に使います**。
残りは俯瞰で位置関係を見せ直す時間です。

★自馬については、**緑の丸ピン「8」（自馬マーカー）**が r01 と r08 で確認できました。
ただし**自馬の馬体を主役として抜いているカットは見つかりませんでした**。

## 18. 通常 `/race` が主役として扱う対象（§9）

| 主役 | 時間比 |
|---|---:|
| **pack（馬群全体）** | ★**100.0%** |
| contenders | **0%** |
| leader | **0%** |
| winner | **0%** |
| self | **0%** |

★使われる 6 ショットの `target` は**すべて `pack`** です。
`contenders` を狙う `side-close` も、`leader` / `winner` を狙うショットも、
対象区間内では**一度も選ばれません**。

## 19. 両者の構造的な差

1. ★**主役の粒度が違う。** 参考は「競り合う 2〜4 頭」を 68.6%、`/race` は「馬群」を 100%。
   `/race` には**個体や小集団を主役にする時間が存在しません**。
2. ★**向きが裏返し。** 参考は横 58.5%・俯瞰 16.5%・後方 10.1%。`/race` は正面 74.3%・俯瞰 0%・後方 0%。
3. ★**引きの使い方が違う。** 参考の全体画は 21.3% で、**位置関係を見せ直すために短く挟む**
   （r03 7.2s / r06 5.2s）。`/race` の全体画は 74.3% で**主たる見せ方**になっています。
4. ★**最長カットの意味が違う。** 秒数はほぼ同じ（19.0 対 18.6）だが、占有率は 25.3% 対 48.3%。
   参考の最長は「横から大きく」、`/race` の最長は「正面から小さく」。
5. ★**場面転換の合図がない。** 参考は勝負どころでブランドワイプ（1.4 秒）を入れて画を切り替えます。
   `/race` は `FLASH_INTO` に `side-drive` が登録されていますが、
   ★勝負どころ（直線）には転換の合図がありません。
6. ★**展開で台本が変わらない。** 参考は 1 レースしか観測していないので比較できませんが、
   `/race` は接戦・独走・境界で**完全に同一**です。
7. ★**自馬がクライマックスで消える。** 4 seed とも `finish-line` の 7.2〜8.4 秒で画面外。

## 20. 測定不能・unknown（§20）

| 事項 | 状態 | 理由 |
|---|---|---|
| 参考の r02 / r07 / r08 / r09 の内部境界 | **ambiguous（4 か所）** | 手ぶれ＋約 5Hz 更新で、カットと追従を分離できない。無理に確定していない |
| 参考の `target` | **unknown** | ゲーム内部の指定は外から分からない |
| 参考の `self` / `winner` | **unknown** | 個体番号を全区間で追跡できない（§9 の指示どおり推測していない） |
| 参考の距離・メートル | **記録せず** | 確定できないので区間ラベルだけを使った（§11 の指示どおり） |
| 参考の r04（ワイプ）の中身 | **unknown** | 全画面がワイプに覆われる |
| 参考の r07 の方向・画角 | **mixed** | 遠景と流し撮りが混在。勝手に 1 つへ丸めていない（§8 の指示どおり） |
| 参考の完全/部分可視の頭数 | **範囲でのみ記録** | 個体を数え切れない。`"8-12"` のような幅で書いた |
| `/race` の前景ラチ遮蔽 | **unknown** | 描画時の重ね順の話で、投影からは出せない |
| `/race` の HUD 遮蔽 | **unknown** | 実画面 24 枚は撮ったが、遮蔽率を機械で測っていない |
| 自馬の「マーカーだけ見える時間」 | **未集計** | マーカーの可視判定が描画コードの中にあり、今回の測り方では分離できない |
| 参考の `extreme-close` | **0% と計上** | r07 後半・r09 後半が該当しそうだが、そのカットを `mixed` としたため計上していない。**過小評価の可能性** |

★**unknown を 0 として集計していません。** 時間比は `unknownOrMixed*` として別枠に出しています。

## 21. 次の実装候補の分類（§15・採否は書きません）

### 台本だけで直せる

1. ★**`homestretch-front` 18.6 秒を分割する。** 全体の 48% を 1 カットが占めている。
   `SCRIPT_V4` の `until` を割れば、既存のショットだけで分けられる
2. ★**到達不能な既存ショットを台本へ戻す。** `side-close`（target: `contenders`）・`side-low`・
   `backstretch-side`・`fourth-corner-high` は**定義があるのに v4 から参照されていない**
3. ★**俯瞰を入れる。** 参考は 16.5% を俯瞰に使い、位置関係を見せ直している。
   `/race` は 0%。`second-corner-high` / `fourth-corner-high` / `aerial` が既にある
4. ★**`target` を切り替える。** 全編 `pack` のまま。`side-close` は `contenders` を持っている
5. **後方系を入れる。** 参考は 10.1%。`third-corner-rear` が既にある
   ⚠️ ただし後方は過去に 5 戦 5 敗の判定があります（`broadcast-v2.ts` の注記）。**推奨ではありません**

### scene resolver の変更が必要

6. ★**接戦・独走で台本を分岐する。** 現在は `finishStyle` を**ゴール前のカメラにしか使っていません**。
   しかも判定が 1520m なので、1280m で接戦だった seed も `solo` になります。
   分岐させるなら**判定地点と使い道の両方**を決め直す必要があります
7. ★**画面上の主役数でカットを選ぶ。** 「上位 4 頭が画面高の 2 割」という状態を検出して
   寄りへ切り替えるには、resolver が**投影後の大きさ**を見る必要があります
8. **カット開始時に target を固定する。** ハイブリッド検証で確認したとおり、
   カット途中で対象が変わると馬が瞬間的に別の形になります
9. **自馬をクライマックスで画面内に保つ。** 現在 `finish-line` は自馬を考慮しません

### 新しい描画・素材が必要

★**該当なし。** 今回の測定で「新素材が要る」と確定した項目はありません。
不足しているのは**素材ではなく台本と resolver** です。

## 22. 変更ファイル

追加のみ。★**`apps/web/src` と `packages/render/src` は 1 行も変えていません。**

| ファイル | 役割 |
|---|---|
| `tools/audit-edit-grammar-reference.mjs` | 参考映像のカット候補と証拠（読取専用） |
| `tools/audit-edit-grammar-race.mjs` | `/race` 4 seed の EDL と主役測定（読取専用） |
| `tools/capture-edit-grammar-race.mjs` | 実ブラウザからカットごとに 1 枚（読取専用） |
| `tools/render-edit-grammar-comparison.mjs` | §12/§13 の集計と比較図（読取専用） |
| `tools/lib/cdp.mjs` / `tools/lib/race-audit-build.mjs` | 監査の共通部品（研究ブランチから復元） |
| `tools/lib/classification.mjs` | R-24 の分類簿へ登録（既存ファイルへの追記） |
| `packages/render/test/edit-grammar-audit.test.ts` | §17 の 13 項目 |
| `out/2d-edit-grammar/` | 成果物 |
| `REPORT_P4_2D_EDIT_GRAMMAR_AUDIT_20260824.md` | この報告書 |

## 23. テスト結果

```
npx vitest run packages/render/test/edit-grammar-audit.test.ts
  Tests  13 passed (13)
```

| # | §17 の項目 |
|---|---|
| ① | 参考と 4 seed すべてに EDL がある |
| ② | EDL の時間に欠落・重複がない（継ぎ目が一致） |
| ③ | 全カット時間の合計が対象映像時間と一致する |
| ④ | 4 seed すべてで実使用ショットを記録する |
| ⑤ | 定義済みだが未使用のショットを隠さない（理由が分類語に収まる） |
| ⑥ | 主役 4 分類を混同しない |
| ⑦ | unknown を 0 として集計しない |
| ⑧ | レース進行率が単調増加する |
| ⑨ | seed 分類が再確認されている（332=contest / 474=solo を機械で確認） |
| ⑩ | 通常 `/race` を実ブラウザ経路から撮っている |
| ⑪ | `apps/web/src` と `packages/render/src` が無変更 |
| ⑫ | 改善動画を作っていない |
| ⑬ | 測定 JSON が欠けたら skip せず失敗する |

```
npm test
  Test Files  110 passed (110)
       Tests  1099 passed (1099)

npm run typecheck
  （出力なし＝エラーなし）
```

## 24. 停止

§18 の最終確認:

```
$ git status --short
 M apps/web/package.json          ← ポート 3210 の既存変更（保持）
 M tools/lib/classification.mjs   ← R-24 の分類簿へ監査ツールを登録
?? REPORT_P4_2D_EDIT_GRAMMAR_AUDIT_20260824.md
?? packages/render/test/edit-grammar-audit.test.ts
?? tools/audit-edit-grammar-race.mjs
?? tools/audit-edit-grammar-reference.mjs
?? tools/capture-edit-grammar-race.mjs
?? tools/lib/cdp.mjs
?? tools/lib/race-audit-build.mjs
?? tools/render-edit-grammar-comparison.mjs

$ git diff --stat apps/web/src packages/render/src
（出力なし＝製品コードは無変更）

$ git log --oneline -1
93d270c 除外: 3D 検証で使ったモデルをリポジトリへ入れない
```

- ★通常 `/race` の既定出力は**未変更**（`apps/web/src` と `packages/render/src` に差分なし）
- ★`apps/web/package.json` のポート 3210 を**保持**
- ★HEAD は `93d270c`
- ★**commit なし・push なし**
- ★3D / S3 / v9 の研究ブランチに**変更を加えていません**
- ★**改善版の動画を作っていません**（`out/2d-edit-grammar/` に動画ファイルは 0 件・テスト⑫）

報告書を提出してここで停止します。**実装はしていません。採否はレビュー側へお戻しします。**
