# 【STAR】γ 確定 / v6 既定化 / 測定器の掃き出し / ダート — 裁定

**案件**: STAR（ポイ活型オンライン競馬育成ゲーム・`V:\dev\Cusor\star`）
**発行**: 2026-08-28（レビュー側 Claude）
**対象**: `REPORT_P4_GAMMA_V6_CONFIRMED_20260828.md`（405 行）/ `REPORT_P4_SEAM_STRAIGHT_LOOK_20260828.md`
**ブランチ**: `p4/race-30sec-cuts` / 起点 `35dd21b` → HEAD `d18c0fd`（13 コミット・未 push）
**前の裁定**: `REVIEW_P4_CUT_SEAM_REOPEN_VERDICT_20260828.md`（★本書が閉じます）
**先行裁定**: `REVIEW_P4_CUT_SEAM_VERDICT_20260827.md` / `REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md` / `REVIEW_LANE_GATES_VERDICT_20260821.md`

**判定**: ★**自己訂正を受理。γ=1.6 / v6 既定化を追認。宿題 1〜4 を全て閉じる。★閾値は「引かない」と決める（前裁定 ④ の結論）。★Q-5 は現機構では片方しか取れない — 両方入れるか両方入れないかの二択とし、当面は入れないことを推奨（最終は [EYES]）。★Q-1 ダートは別便 P4.5。Q-2 映像先行は可（条件付き）。★Q-3 内回り案は却下（走路の幾何に触れる）。Q-4 素材生成は可（条件 4 つ）。**

---

## 0. 照合（R-13）— 照会文の主張は全件一致。申告と 1 点だけ相違

★**すべて実ファイルで確認しました。** 推測で通したものはありません。

| 照会文の主張 | 実ファイル | 判定 |
|---|---|---|
| 起点 `35dd21b` → HEAD `d18c0fd`・13 コミット | `git rev-list --count 35dd21b..HEAD` = 13 | ✅ |
| ★9.2m は正しい / 描画は馬＋騎手 2.5m | `perspective-draw.ts:517` `HORSE_HEIGHT_M = 2.5` | ✅ |
| 註記を戻し、誤った経緯も残した | `contest-focus.ts:12-19`（`_screenspan.mjs` に 1.6m を直書きした経緯まで記載） | ✅ |
| γ=1.6 が唯一の出どころ | `broadcast-v2.ts:621` `DEMO_CONTEST_GAMMA = 1.6` | ✅ |
| v6 が既定・v5 へ戻せる | `broadcast-v2.ts:751` `DEFAULT_RACE_SCRIPT = 'v6'` / `:756` `PREVIOUS_RACE_SCRIPT = 'v5'` | ✅ |
| 先頭を枠に残す処理は px/m 由来・連続 | `contest-focus.ts:190-199`（`Math.max(rawFocusS, leaderS - maxLagM)`・`halfFrameM` から算出・手置きの m なし） | ✅ |
| 測定器 20 本の既定引数を画面側へ | `70bb063`（`broadcast-v2.ts` 3 箇所 + `broadcast-v2-scene.ts` 1 箇所） | ✅ |
| 滑らかさの道具のキーに区間番号 | `verify-horse-smoothness.mjs:98`（キーが `ショットID#区間番号#枠番` になっている） | ✅ |
| 閃光が画面まで通っている | `apps/web/src/app/race/page.tsx:1991, 2068` が `FLASH_INTO` を参照 | ✅ |
| γ の根治（前裁定 Q-4） | `audit-cut-seam.mjs:64` `SCREEN_GAMMA = DEMO_CONTEST_GAMMA` / `:66` `SCREEN_SCRIPT = DEFAULT_RACE_SCRIPT` | ✅ |
| ダートはエンジンで既に効く | `balance.ts:515` `SPEED_SURFACE_MULT: {turf: 1.0, dirt: 0.975}` / `coefficients.ts:88` `surfaceCoef` / `race.ts:79,185` | ✅ |
| 報告書は「未追跡」 | ⚠️ ★**追跡済み・コミット済み**（`6836313` で追加・`d18c0fd` で更新・作業ツリーに差分なし） | ⚠️ 相違 |

⚠️ ★最後の 1 点は無害ですが記録します。★**提出時の申告と git の状態が違うと、レビュー側は「未追跡なら消える可能性がある」と見て別の扱いをします。** 提出前に `git status` を見てください。

### ★前裁定 §2-3 の条件 3 つは全て満たされています

| 条件 | 実ファイル | 判定 |
|---|---|---|
| ① 反転を残差として残す（「解決済み」と書かない） | 報告 §6「★残る問題 — 4 角の走行方向の反転 ⚠️★解決していません」 | ✅ |
| ② `FLASH_INTO` のショット単位の波及を差分で出す | `cut-transition.test.ts:103-111`（v4/v5/v6/v3 の流入先を台本ごとに固定） | ✅ |
| ③ `cut-transition.test.ts` を意図して書き換える | `:94-100`（足した理由を書いたうえで `['fourth-corner-front','side-drive']` に更新） | ✅ |

---

## 1. §1 自己訂正 — 受理します

★**9.2m は正しい数字でした。** 測定器に馬の高さ 1.6m を直書きし、描画側の `HORSE_HEIGHT_M = 2.5m`（馬＋騎手の絵全体）と違うものを測ったことが原因、という診断に同意します。

★**註記を戻したうえで、誤った経緯をファイルに残した処理は正しい。** 消すと、同じ誤りがもう一度「発見」されます。

⚠️ ★申し送りとして共有します。**前裁定 §0 でレビュー側も同型の誤りを訂正しています**（git 履歴だけを見て「記録に無い」と結論し、作業ツリーを grep していなかった）。
★**測る側・検める側に立った瞬間に、自分が潰していた穴へ落ちる。** これは開発側だけの傾向ではありません。R-30 は双方に掛かります。

---

## 2. γ = 1.6 / 台本 v6 の既定化 — 追認します

### 2-1. 憲法適合を [CODE] で確認しました

- 着差 4 段階（γ 1.0 / 1.3 / 1.6 / 2.0）で ★**着順は 4 通りとも同一**（報告 §2）
- 写像は単調（D-064）。★**着順・タイム・払戻・人気には定義上触れません**
- `RACE_RANDOM_K` は不変。`Date.now()` / `Math.random()` の混入なし（憲法 4）

### 2-2. 既定変更の作法として正しい

★既定を v6 にすると `?cinematography=v5` が黙って既定へ落ちる、という危険を**先に見つけて塞いだ**点を評価します。
`PREVIOUS_RACE_SCRIPT` を置き、★**「戻せること」自体を検査で固定した**のは、既定変更のあるべき形です。

★意図して書き換えた検査 9 件（§2・§5-4）は、すべて**理由が書かれています**。
★**壊れたのを黙って通したものはありません**（`script-v5.test.ts` / `script-v6.test.ts` / `broadcast-v2.test.ts` / `fixed-camera.test.ts` / `cut-transition.test.ts` を確認）。

⚠️ ★`fixed-camera.test.ts` が **800〜1056m（v4 の 4 角）を直書き**していた件は、
★**「既定が変わると、範囲を直書きした検査は別のカットを走査し始める」**という一般形です。
→ ★**走査範囲を台本から引く形へ直したのは正しい。** これも R-31 の家族として扱います。

---

## 3. 宿題の消化（前裁定 §7）

| # | 宿題 | 判定 |
|---|---|---|
| 1 | 閃光を入口側（`side-drive → fourth-corner-front`）へ | ✅ **閉じる**（条件 3 つとも充足・§0） |
| 2 | 測定器の根治を**単独の便**で・前後差分 | ⚠️ ★**形式は未達**（他の作業と同じ便）。★**内容で追認して閉じる** — 指示範囲 1 件（γ）に対し、道具 38 本 + 既定引数 20 本 + 滑らかさの欠陥まで畳み、前後差分も出ている |
| 3 | γ 確定時に 1 回だけ測り直す | ✅ **閉じる**（報告 §6・画面既定 v6/γ1.6 で再測） |
| 4 | `straight-contest → homestretch-front` の 2919px を一度見る（測るだけ） | ✅ **閉じる**（`REPORT_P4_SEAM_STRAIGHT_LOOK_20260828.md`・★製品コード無変更を確認） |

★**宿題 2 の形式未達を咎めません。** ★「単独の便で」と書いた意図は**根治が他の変更に紛れて検証できなくなること**を防ぐためで、
★前後差分（1859倍 → 52.4倍 など）が出ている以上、意図は満たされています。

---

## 4. ★閾値（前裁定 §4-3 の ④）— ★線は引きません

前裁定は「①閃光 ②γ 確定 ③測定器の根治 が揃ってから ④ 判断する」としました。★**3 つとも揃いました。** 判断します。

### 4-1. 判定 — ★跳び px に閾値を設けない

★理由は 2 つです。

1. ★**最大値は「正しい演出」で出ます。** `REPORT_P4_SEAM_STRAIGHT_LOOK` の実測で、最大は `seed 16`（2 着まで 23.6m の逃げ切り）。
   ★跳びの主成分は**画角比 2.43×**（`straight-contest` 7.6° → `homestretch-front` 18.5°）＝ ★**カットの設計そのもの**です。
   ★ここに線を引けば、**寄りと引きを組み合わせる中継の基本を落とす器具**になります（R-16）。
2. ★**跳び px は反転の指標ではありません**（前裁定 §4-2）。★1 本の線で「反転」と「跳び」を同時に縛れません。

### 4-2. 代わりに固定するもの（★意味のある不変条件）

| 固定するもの | 状態 |
|---|---|
| 系統が変わる境目の**本数**（台本ごと） | ✅ `cut-transition.test.ts:70` |
| 閃光の有無（`FLASH_INTO` の中身） | ✅ `cut-transition.test.ts:100` |
| ★**共通の被写体 0 頭 = 0 件** | ★**未固定 → 宿題 1** |
| ★**先頭が画面の外 = 0%** | ★**未固定 → 宿題 2** |

★**後ろの 2 つは「良い状態を守る」検査です。** ★閾値（悪い状態を数値で切る線）とは別物で、
★**達成済みの 0 を 0 のまま保つ**ためのもの。★退行したら落ちます。

⚠️ ★とくに「先頭が画面の外 0%」は、★**報告 §3 が示したとおり「主役 2 頭以上 100%」では検出できません**。
★片側だけを見る指標が、抜けているものを見なかった実例です。★**指標を足すのではなく、抜けを直接見る検査を置いてください。**

---

## 5. ★Q-5（1312m / 1392m の閃光）— ★現機構では片方しか取れません

★**見え方の判断の前に、機構の制約があります。** これは [CODE] で決まる話なので先に置きます。

### 5-1. v6 の並びと系統（実ファイル `broadcast-v2.ts:1125-` / `view` は `:472, 343` ほか）

```
 100m  start-front(正面) → first-corner-front(正面)        同系統
 528m  first-corner-front(正面) → side-drive(側)           ★系統が変わる・閃光あり
 864m  side-drive(側) → fourth-corner-front(正面)          ★系統が変わる・閃光あり（今便で追加）
 966m  fourth-corner-front(正面) → side-drive(側)          ★系統が変わる・閃光あり
1200m  side-drive(側) → straight-contest(側)               同系統
1312m  straight-contest(側) → homestretch-front(正面)      ★系統が変わる・★閃光なし  ← Q-5
1392m  homestretch-front(正面) → straight-contest(側)      ★系統が変わる・★閃光なし  ← Q-5
1504m  straight-contest(側) → finish-line(側)              同系統
```

### 5-2. ★`FLASH_INTO` はショット id 単位なので、狙った境目だけには掛けられません

| 足すもの | 得られる境目 | ⚠️ 一緒に付く境目 |
|---|---|---|
| `homestretch-front` | ★1312m（v6・狙い） | ⚠️ **v4 の 1056m**（`fourth-corner-front → homestretch-front`＝★**同系統**） |
| `straight-contest` | ★1392m（v6・狙い） | ⚠️ ★**v6 の 1200m**（`side-drive → straight-contest`＝★**同系統**）— ★**画面既定の台本に、不要な閃光が入ります** |

★**つまり「1392m だけに閃光」は、いまの機構では表現できません。**
★1312m は取れますが、漏れ先（v4 の同系統境目）が付きます。

### 5-3. ★片方だけ入れるのは、前裁定と同じ非対称を作ります

前裁定 §2-2 でレビュー側はこう述べました。

> ★**弱いほうにだけ閃光が掛かっている、いまの状態のほうが不自然です。**

★同じ論理が Q-5 にも掛かります。★**1312 だけ閃光・1392 は素通し**は、直線の中に**新しい非対称**を作ります。
→ ★**「両方入れる」か「両方入れない」かの二択**として扱ってください。
★両方入れるなら、★**`FLASH_INTO` を境目単位（`from → to` のペア）へ変える改修が先**です（前裁定 §2-3 条件 2 が予告していた形）。

### 5-4. ★判定 — 当面は入れない。ただし最終は [EYES]

★**レビュー側の推奨は「両方入れない」**です。根拠 3 つ：

1. ★**この 2 本に走行方向の反転はありません。** 反転は 4 角の 2 本（入口 →82 → ←19 px/m）。★閃光は反転を読ませるための道具です
2. ★跳びは**中央 301px / 最大 608px**。4 角の中央 950px の約 1/3
3. ★**閃光は句読点です。** 30 秒に 3 本を 5 本にすると、★**4 角で効かせている意味が薄まります**

⚠️ ★**[EYES]**：実画面で 1312m に「画が飛ぶ」感触があるなら、この推奨は覆ります。
★その場合は**機構改修（境目単位）を単独の便で**起こしてください。★id 単位のまま押し込まないこと。

---

## 6. ダート（Q-1〜Q-4）

### 6-1. Q-1 — ★別便（P4.5）にしてください

★**「いまの指示書の範囲外なので着手していない」という開発側の判断は正しい。**
P4 は「レース映像の品質」。ダートは**新しい馬場という別の題**です。

→ ★**P4 をクローズしてから P4.5 として開けます。** P4 のクローズ条件（V-17 の縮小条件の全量実行・残件の整理）が先。

### 6-2. Q-2 — ★映像を先に作ってよい（条件付き）

[CODE]：★**ダートは既に実装済みの機構です。**

```
balance.ts:515       SPEED_SURFACE_MULT: { turf: 1.0, dirt: 0.975 }
coefficients.ts:88   surfaceCoef(entrant.surfaceAptitude, params.surface, balance)
race.ts:79, 185      速度とタイムに反映
types.ts:15          Surface = 'turf' | 'dirt'（正典 §5.2 surface_aptitude に対応）
```

★**着順もタイムも既にダートの理屈で出ています。映像だけが追いついていません。**
→ ★映像先行は「未執筆の仕様を先取りする」行為ではなく、★**既にある機構を見えるようにする**作業です。認めます。

★**条件**：★**年間カレンダー・番組表（正典 §12）には触れないこと。** 作るのは「馬場を選べるデモ」まで。
★「どのレースがダートか・年に何回か」は路線設計であり、正典側の裁定事項です。

### 6-3. ★Q-3 — 内回りに置く案は却下します（★この便で一番重い論点）

[CODE]（`packages/render/src/infield.ts`）:

```
INFIELD_LAYOUT.dirtOuterW = -5.0
INFIELD_LAYOUT.dirtInnerW = -30.0        ★内ラチ(w=0)の「内側」に描かれた帯
drawInfield(...) { void course; ... }    ★「走路の幾何を持たない」ことを型で示している
```

走路は **w = 0〜20**（`ovalCourse(1600, { widthM: 20 })`）。★**内側の褐色帯は、誰も走らない風景です。**

★**ダート戦で馬をその帯に走らせると、`w` の意味・`laneExtraM`・コーナー半径・走行距離に触れます。**
→ ★**憲法 3（着順・タイム・払戻に触れない）と D-065 / D-071 に正面から抵触します。**

★**判定**：

> ★**ダート戦でも馬は同じ走路（w = 0〜20）を走る。替えるのは、その走路の「地面の描画」だけ。**
> ★内側の帯は風景のまま。★**走路の幾何は 1mm も動かさない。**

★我々は**架空の競馬場**です（憲法 §0.1）。★実在競馬場の内回りダート配置を写す必要はなく、
★**写さないほうが権利方針に沿います。**

⚠️ ★**[EYES] 1 件**：ダート戦では走路が褐色になり、★**内側の装飾ダート帯と同色の褐色帯が 2 本並びます。**
★引きの画で「どこを走っているか」が読めなくなる恐れがあります（`infield.ts` の冒頭が述べる「引くと画面が緑一色」の裏返し）。
★対処案：★**ダート戦のときは内側の帯を芝側に反転させる**（内回り芝／外回りダートの断面）。★これも描画だけなので幾何ゼロです。
→ ★**実画面 1 枚を出してオーナーに諮ってください。** 開発側で決めないこと。

### 6-4. Q-4 — 素材生成を認めます（条件 4 つ）

1. ★**既存の承認済み芝タイルを差し替えない。** ダート素材は**新規追加**
2. ★**承認プレートは素材ごと。** ★**ダート素材は新たに承認を取る**（芝の承認は継承しません）
3. ★**実在競馬場の写真を模写しない**（憲法 §0.1）。架空の競馬場の断面として起こす
4. ★生成物を**分類簿・登録簿に載せる**（R-24）

---

## 7. 滑らかさの道具（§5-3）

### 7-1. ★前便の「赤の扱い判断」は前提が消えました — 判断不要として閉じます

★`verify-horse-smoothness.mjs` の赤（`side-drive` 尖り 25.5倍）は ★**v4 の上の赤**でした。
★オーナーが見ている画の話ではありません。★**前便でレビュー側に上げた「扱いの判断」は取り下げて構いません。**

★あわせて `straight-contest` の**尖り 1859倍**は、★**同じ名前のカットが 2 回出る台本で、間を飛び越えて比べていた**ための嘘の数字でした。
★**カットの切替を「馬が飛んだ」と数えていた**という診断に同意します。区間番号をキーへ入れた直しは正しい。

### 7-2. ★残る本物

★`side-drive` で 12番が 7.2s に **10.9px** 跳ぶ（尖り 46.7倍・★v4 でも同値＝台本によらない）。
★画面幅 1280 の **0.85%**、1 頭・1 瞬間。

→ ★**閾値を引かない方針（§4）と同じ枠で扱います。** ★**残件台帳に置いて追う。** 今すぐ直す優先度は与えません。

---

## 8. ★正典が 8 日分遅れています（★オーナー判断事項）

[CODE]：

```
STAR_SPEC_v2.0.md   最終更新 2026-08-21 17:08
記録済みの最新       D-080（2026-08-20）
```

★**2026-08-21 以降の決定が 1 件も記録されていません。**

| 未記録の決定 | 出どころ |
|---|---|
| レーン / ゲートの裁定 | `REVIEW_LANE_GATES_VERDICT_20260821.md`（8/21） |
| ★接戦は「スコア→タイム写像の形」で作る（総差保存形） | `REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md`（8/25） |
| 4 角の境目・案 A の完全閉鎖・案 B 却下 | `REVIEW_P4_CUT_SEAM_VERDICT_20260827.md`（8/27） |
| ★閃光の認可（条件 3 つ）・★**R-31 の新設** | `REVIEW_P4_CUT_SEAM_REOPEN_VERDICT_20260828.md`（8/28） |
| ★**γ = 1.6 確定・台本 v6 を既定化**（オーナー判断） | 本便（8/28） |
| ★**跳び px に閾値を引かない**（本裁定 §4） | 本書 |
| ★**ダート：走路の幾何に触れない／内回り案は却下**（本裁定 §6-3） | 本書 |

★**D-081 以降として起票が必要です。** ★番号の確定は正典側の作業であり、★**R-31 の番号も正典で確定させてください**（前裁定 §6 で予告済み）。

⚠️ ★これを放置すると、★**「決まったこと」がレビュー文書の中だけに存在する**状態になります。
★正典を読んで作業する人（将来の開発側・将来のレビュー側）には、★8/21 で時間が止まって見えます。

---

## 9. 判定のまとめ

| # | 論点 | 判定 |
|---|---|---|
| — | §1 自己訂正（9.2m） | ★**受理。** 註記の復帰と経緯の保存も正しい |
| — | γ = 1.6 / v6 既定化 | ★**追認。** 憲法 3/4 適合を [CODE] で確認 |
| — | 宿題 1・3・4 | ★**閉じる** |
| — | 宿題 2（測定器の根治） | ★**形式は未達（単独便でない）／内容で追認して閉じる** |
| ★④ | 跳び px の閾値 | ★**引かない。** 代わりに「共通被写体 0 頭 = 0 件」「先頭が画面の外 = 0%」を検査で固定する |
| **Q-5** | 1312m / 1392m の閃光 | ★**現機構では 1392m を単独で取れない**（1200m の同系統に漏れる）。★**両方入れるか両方入れないかの二択。当面は入れないことを推奨**（最終は [EYES]）。入れるなら★境目単位への機構改修を単独の便で |
| **Q-1** | ダートは P4 の範囲か | ★**別便（P4.5）。** P4 のクローズが先 |
| **Q-2** | 映像を先に作ってよいか | ★**可。** ダートは既に実装済みの機構。★条件：年間カレンダー・番組表に触れない |
| **Q-3** | ダートを内回りに置くか | ★**却下。** 走路の幾何（`w`・`laneExtraM`・コーナー半径）に触れ、憲法 3・D-065 / D-071 に抵触する。★**同じ走路の地面の描画だけを替える** |
| **Q-4** | 素材生成 | ★**可。条件 4 つ**（芝タイルを差し替えない／ダートは新規に承認を取る／実在競馬場を模写しない／登録簿に載せる） |
| — | 滑らかさの赤（25.5倍） | ★**判断不要として閉じる**（v4 の上の赤だった） |
| — | 残る本物（10.9px） | ★**残件台帳へ。** 今は直さない |

---

## 10. ★開発側の宿題

1. ★**「共通の被写体 0 頭 = 0 件」を検査で固定する**（§4-2）
2. ★**「先頭が画面の外 = 0%」を検査で固定する**（§4-2。★達成済みの 0 を守るため。★「主役 2 頭以上」では検出できない）
3. ★**4 角の走行方向の反転を、残件台帳に「未解決」として明記し続ける**（★「閃光で解決」と書かない）
4. ★**`side-drive` 12番 7.2s の 10.9px を残件台帳へ**（台本によらない・今は直さない）
5. ★**提出前に `git status` を見る**（§0・報告書の追跡状態の申告が実際と違っていた）
6. ★**ダートは P4 クローズまで着手しない**（Q-1）。着手時は Q-3 の [EYES]（内側の帯を芝に反転させるか）の実画面 1 枚を先に出す

## 11. ★オーナー判断を仰ぐこと

1. ★**Q-5** — 実画面で 1312m の切替に「画が飛ぶ」感触があるか（★飛ぶなら機構改修を別便で起こします。★片方だけは入れません）
2. ★**Q-3 の見え方** — ダート戦で内側の帯を芝に反転させるか（★実画面 1 枚を見てから）
3. ★**正典 D-081 以降の起票** — いま一括で起こすか、P4 クローズ時にまとめるか（§8・★8 日分が未記録）
4. ★**未 push 49 コミットの扱い**

---

## 12. 申し送り

1. ★**測る側に立った瞬間、自分が潰していた穴に落ちる。**（§1）
   ★開発側の §1 自己訂正と、レビュー側の前裁定 §0 は**同じ形**。★R-30 は双方に掛かる
2. ★**既定は借金を作る。**（§2-2）「範囲を超えるので `'v4'` は据え置く」という判断が、★**20 本の道具を丸ごと無効にしていた**
3. ★**範囲を直書きした検査は、既定が変わった瞬間に別のものを走査し始める**（§2-2・`fixed-camera.test.ts`）。★範囲は台本から引く
4. ★**閾値は「悪い状態を切る線」、不変条件は「良い状態を守る線」。**（§4）★前者は正しい演出を落としうる。後者は落とさない
5. ★**機構の粒度が、要求を表現できないことがある。**（§5-2）★`FLASH_INTO` はショット id 単位なので「1392m だけ」を言えない。
   ★**言えない要求を id 単位のまま押し込むと、画面既定の台本に不要な閃光が入る**
6. ★**描画で足したものを、走路だと思わない。**（§6-3）★`drawInfield` は `void course` と書いて幾何を持たないことを示している。
   ★**そこに馬を走らせた瞬間、それは風景ではなくレースの一部になる**
7. ★**決定は正典に降ろす。**（§8）★レビュー文書の中だけにある決定は、★正典を読む人には存在しない
