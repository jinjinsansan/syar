/**
 * ★**出走登録料**（★EF-1・2026-09-19・正典 §10.4・§3.4・裁定 `REVIEW_UI1_SETUP_VERDICT_20260919.md`）
 *
 * 【🔴 ★なぜ切り出したか — **どこにも定数が無かった**】
 *   ★`enter_race`（`0024` → `0033`）は ★**`v_fee := 200;` と SQL に直書き**していました。
 *   ★TypeScript の側には ★**定数がありません**でした — ★`CAREER_ASSUMPTION` の
 *   ★**説明文の中に「登録料 200 EP」と書いてあるだけ**です。
 *   → ★画面が値段を出そうとすると、★**3 つ目の写し**ができます。
 *     ★`/entry` を繋ごうとしてここで止まり、★照会に回しました（★推測で埋めない）。
 *
 * 【★どう解くか — **D-103 ④ の先例**】
 *   > 「★値段は DB に**サーバーが書き**、RPC はその行の値で払わせる」
 *   ★**正 ＝ ここ** → ★**ワーカーが `races.entry_fee_ep` に書く**（`0039`）
 *   → ★**`enter_race` はその行の値で払わせる** → ★**`races_public` が返す** → ★**画面は読むだけ**。
 *
 * ⚠️ 🔴 ★**値は 1 つも変えていません**（★EF-4）。
 *    ★登録料 200 EP は ★**正典 §3.4 の 1 キャリアの収支の前提**であり、
 *    ★**GB-6（経済の取り直し）に噛みます**。★動かすならそちらで、まとめて。
 */

/**
 * ★**1 回の出走登録に払う EP**（★騎手の料金は別・`entryCostEP` で足す）。
 *
 * ⚠️ ★較正定数だが ★**値そのものをゲートにしない**（★`STAR_PRICE_EP` などと同じ扱い）。
 *    ★GB-6 の収支の取り直しで、この額込みの 1 キャリアの収支を報告する。
 */
export const ENTRY_FEE_EP = 200;
