/**
 * ★**EP の発行の額**（★正典 **D-075** の写し・較正定数）
 *   ★裁定 `REVIEW_EP_INFLOW_AND_ENTRY_20260925.md` §5 (a)
 *
 * 【🔴 ★なぜここに 1 か所で置くか】
 *   ★2026-09-25 まで、★この 3 つの額は ★**書かれた場所がばらばら**でした:
 *     ★登録時 2,000 … `0031:140`・`0037:178`・`0067:63`（SQL に直書き 3 か所）と
 *                      `apps/web/src/lib/setup.ts`（画面の写し）
 *     ★デイリー 200 … `setup.ts` の写しだけ（★**渡す側がそもそも無かった**）
 *     ★日次上限 10,000 … ★**どこにも無し**
 *   → ★`0080_daily_ep.sql` の `ep_grant_amount()` を ★**SQL 側の 1 か所**にし、
 *     ★ここを ★**TS 側の 1 か所**にして、★`apps/cli/test/ep-grant-sql.test.ts` が突き合わせます。
 *
 * 【⚠️ ★これは「サーバーが持つ値の写し」です】
 *   ★画面はここの値で ★**額を決めません**。★渡すのはサーバー（`claim_daily_ep()`）で、
 *   ★RPC は ★**額を引数で受け取りません**（★利用者が発行量を決められる形にしない）。
 *   ★ここに在るのは ★**押す前に「いくら貰えるか」と言うため**と、★突き合わせの相手方としてです。
 *
 * 【⚠️ ★較正定数です。★値そのものをゲートにしない】（★D-075 の但し書き）
 *   ★V-11（ポイント純発行量）を満たすように調整する対象で、
 *   ★`apps/cli/test/calibration-registry.test.ts` の登録簿に載ります。
 */

/** ★EP を発行する場面の種類（★SQL の `ep_grant_amount(p_kind)` と同じ語） */
export type EpGrantKind = 'signup' | 'daily' | 'daily_cap';

/**
 * ★**D-075 の額**。
 *   ★`signup` … ★登録時 **2,000 EP**（★≒1 日ぶんの調教＋出走登録 1 回）
 *   ★`daily` … ★デイリーログイン **200 EP**（★＝登録料 1 回ぶん。
 *               ★「これだけでは能動的に遊べないが離脱もしない」額）
 *   ★`daily_cap` … ★**1 人 1 日あたりの「発行」量の上限 10,000 EP**。
 *      ⚠️ ★これは ★**残高の上限ではありません**し、★払戻や賞金は数えません
 *      ★（★数えると「大きく当てた人が受け取れない」になる ＝ 裁定 §5 (b)）。
 *      ★目的は honest play を縛ることではなく ★**バグ・エクスプロイトの被害を上限で止める**こと。
 *      ★何を「発行」と数えるかは ★**SQL の `ep_reason_class()` が 1 か所で持ちます**
 *      ★（★いまは `inflow` と `horse_sale`。★TS 側に同じ表を作らない ＝ D-052）。
 */
export const EP_GRANTS: Readonly<Record<EpGrantKind, number>> = {
  signup: 2000,
  daily: 200,
  daily_cap: 10000,
};

/**
 * ★**繰り越さない**（★裁定 §5 (c)）。
 *   ★押さなかった日のぶんは消えます。★繰り越すと「1 週間 放置して 1,400 EP」になり、
 *   ★D-075 の「これだけでは能動的に遊べないが離脱もしない」から外れます。
 *   ⚠️ ★画面は ★**押す前に**この旨を言うこと（★D-123 の作法）。
 */
export const EP_DAILY_CARRIES_OVER = false;
