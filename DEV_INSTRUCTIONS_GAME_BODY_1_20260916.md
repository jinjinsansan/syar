# 指示書 — ゲーム本体の再設計 第 1 便（決定の実装・GB-0〜GB-6）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ レビュー側 → 開発側
**根拠**: 正典 **D-100〜D-105**（2026-09-16 起票・オーナー決定 T-3〜T-8）／ 回答 `REVIEW_CONSULT_GAME_DESIGN_TWO_MODES_ANSWER_20260915.md`（§3〜§9）／ 相談書 `CONSULT_GAME_DESIGN_TWO_MODES_20260915.md` §9-1
**前提**: 正典の**本文の改訂は本便と同時にレビュー側が行い、同じ受領コミットに入ります**（§7.2・§6.7・§10.4・§3.3・§3.4・§12.2 の註記）。**開発側は正典を編集しないでください。**

> 凡例（R-13）: 報告では「**再現した事実**」と「**未検証の主張**」を分けてください。
> ⚠️ **本番 DB・ワーカーの配備・push はしません。** 本便はゲームのルールと画面の土台だけです。
> ⚠️ ★**サイクルの間隔（T-1・D-100）は未定です。** 間隔に依存する実装（`CYCLE_MS` の変更・`RACES_PER_DAY` の導出化・§10.2 の時間割）は**本便に含めません**。
> ⚠️ ★**着順に効く要素を、この便で 1 つも入れないでください**（騎手の効果は 0 のまま入れる・GB-3）。**V-4〜V-6・V-17・V-18 が動かないこと**が本便の受け入れ条件です。

---

## 0. GB-0 — 進め方と、触ってよいもの

### 0-1. 順番

| # | 工程 | 中身 |
|---|---|---|
| **GB-1** | 調教の見せ方 | 週のまま・「体・心 × 強度」への写像を 1 か所（D-101） |
| **GB-2** | 所有頭数 | 現役 30 頭・同じレースに 1 人 2 頭まで（D-104） |
| **GB-3** | 騎手 | 架空の騎手・料金は EP・出走登録で凍結・**着順への効果は 0 のまま**（D-105） |
| **GB-4** | 馬の購入 | EP・NPC 世界から・振り直しが成立しない値付け（D-102） |
| **GB-5** | 厩舎の格 | 馬ごと・天井は変えない・同じ EP で強くならない（D-103） |
| **GB-6** | 経済の取り直し | §3.4 の収支と V-11 を**まとめて最後に**（GB-1〜GB-5 の後） |

★**GB-1 から順に、1 工程 1 コミット**でお願いします。**各工程の後に V-14・V-15・V-7a/V-7b・V-11 のうち関係するものを流し、「変わらないこと」または「変わった量」を報告**してください。

### 0-2. 触ってよいもの

| 可 | 不可 |
|---|---|
| `packages/sim-engine/src`（育成の式に触る場合のみ・GB-5）・`packages/scheduler/src`（頭数・出走の規則）・`apps/cli/src`（検証ハーネス）・`apps/web/src`（画面の見せ方）・各 `test/` | ★`packages/race-engine/src`（着順の計算）・較正定数の値（`RACE_RANDOM_K`・`TAIL_MIX_*`・`BASE_GAIN` ほか）・`ODDS_MC_TRIALS`・`CYCLE_MS`・正典・本番 DB・ワーカーの配備・push |

★**較正定数を動かしたくなったら、そこで止めて照会してください**（値ではなく結果をゲートにする・V-14 の形）。

---

## 1. GB-1 — 調教の見せ方（D-101）

**単位は週のまま。** 既存の 8 メニュー（§7.2）を、画面では「**体・心 × 強度**」に見せます。

### 1-1. 作るもの

- ★**写像を 1 か所に置く**（D-052）。画面・監査道具・検査が**同じ関数**を引くこと（R-30）

| 見せ方 | 強度 | 既存メニュー |
|---|---|---|
| 体 | 弱 → 中 → 強 | 軽め調整 → 坂路／ウッドチップ／プール → 追い切り |
| 心 | 弱 → 中 → 強 | 休養 → ゲート練習 → 併せ馬 |

- 置き場所は開発側で選び、**層の向き**（エンジンが画面に依存しない）を報告に書いてください
- 画面の 3 本のバー（スピード・スタミナ・コンディション）は、**`stats` から導出**します。★**`potential` を出さない**（§5.5・星表示のみ）。コンディションは能力ではなく状態として別に見せる
- **出走の前の週・後の週を画面で強調**する（「次走の前の調教」「次走の後の調教」の導線）。★**週の進み方・成長式・疲労・故障は 1 ミリも変えない**
- 「当たり」（GREAT / UP）の演出は、**既存の伸びの乱数（`rand(0.85, 1.15)`）の上側を見せるだけ**。★新しい乱数・倍率・確率を足さない

### 1-2. 検査

| # | 内容 |
|---|---|
| 1 | 写像が **8 メニューすべてを覆い**、体・心 × 強度のどの組にも**ちょうど 1 つ以上**のメニューが対応すること（全数） |
| 2 | 画面が引く写像と、検査が引く写像が**同じ関数**であること（構文木か import の経路で固定・R-30） |
| 3 | 3 本のバーの値が `stats` だけから決まり、**`potential` を読まないこと**（★禁止語ではなく**入力**で見る。D-098 の検査で踏んだ穴と同じ形にしない） |
| 4 | **変異**: 写像を画面側に複製すると 2 が落ちる |
| 5 | **V-14・V-15・V-7a/V-7b を流し、値が変わらないこと**（写像は見せ方だけなので、式が変わっていなければ 1 ビットも動かないはず） |

---

## 2. GB-2 — 所有頭数（D-104）

- **現役の上限を 15 → 30**（§6.7）。**繁殖牝馬 10・種牡馬 5 は据え置き**
- 上限は**定数 1 か所**（正典の写しとして・§16.3 の「定数の 3 分類」）
- ★**同じレースに 1 人が出せるのは 2 頭まで**。★**出走登録の RPC はまだ無い**ので、本便では「**入っているが使われない**」形で置いてください（R-15）: 純関数（例: その人の当該レースの登録数 → 受理/拒否）＋検査。RPC に繋ぐのは出走登録の便
- §9.5 の自馬ベットの制限（自馬を含む券だけ・1 レース 5,000 EP・複数頭なら全頭を含む組合せ）と、§8b.1 の「発走前に介入対象を 1 頭選ぶ」は**既存のまま**

### 2-1. 検査

| # | 内容 |
|---|---|
| 1 | 現役 30 頭目は受理・31 頭目は拒否。繁殖 10・種牡 5 は変わっていない |
| 2 | 同じレースに 2 頭目は受理・3 頭目は拒否。**別のレースなら影響しない** |
| 3 | §9.5 の制限が 2 頭のときも成立（全頭を含む組合せだけ） |
| 4 | **変異**: 上限を 1 か所でなく 2 か所に書くと、片方だけ直したときに落ちる（値照合） |

---

## 3. GB-3 — 騎手（D-105）

★**この便では着順に効かせません。**「入っているが使われない」状態で入れてください（R-15。接続の前後で対照が取れます）。

- **架空の名前と絵**（§0.1。実在の騎手の名前・顔は使わない）
- **料金は EP**（出走登録料と同じ形・§3.3 のシンク）。★**賞金からの差し引きにしない**（D-105 ②）
- **出走登録で凍結**する（`entrant_snapshot` に騎手の id を入れる・D-055・D-071）
- **親密度**は持ってよいが、**早く頭打ちにする**（数戦で上限）
- ★**AI 代行（§8b.5）の質を騎手で変えない**
- 着順への効果は **0**（定数で明示。効かせる便で V-4・V-5・V-6・V-17・V-18・V-13 を取り直す）

### 3-1. 検査

| # | 内容 |
|---|---|
| 1 | 騎手を変えても**着順・スコア・走破タイムが 1 ビットも変わらない**（効果 0 の対照） |
| 2 | 料金が EP の台帳に記帳され、PP に触れないこと |
| 3 | 凍結に騎手が入り、確定が**凍結から読む**こと（D-055 と同じ形） |
| 4 | 親密度が上限で頭打ちになること（境界の両側・R-2） |
| 5 | AI 代行の期待値が騎手によらないこと（V-8 の測り方で 2 通り） |

---

## 4. GB-4 — 馬の購入（D-102）

★**この便では純関数と検査だけ**。DB の書き込みと RPC は次の便です（利用者向け RPC はまだ 1 つも無い）。

- **売る馬は NPC 世界から取る**（同格帯・低素質・専用の生成経路を作らない・§10.5・D-074）
- ★**振り直しが成立しないこと**（D-079 の形）: 候補は**★表示が同じ帯**から出す。手放して戻る EP は買った額より十分小さく
- **配合より割高に値付ける**（§1.1）。価格は**較正定数**として置き、値そのものをゲートにしない
- **PP では買わせない**（S-5・L-4 の近縁）
- **NPC の現役プールの在庫**に下限監視（D-079 ⑧）

### 4-1. 検査

| # | 内容 |
|---|---|
| 1 | 候補の★表示が全個体で同一（D-079 ② と同じ測り方。★の算出量と同じ量で測る） |
| 2 | 同じ人が引き直しても、**観測できる差が出ない**こと |
| 3 | 価格が配合（種付料の合計）より高いこと。同じ EP での期待する★を**報告**（線は引かない） |
| 4 | PP では買えないこと（経路が無いこと・§17.2 C-2 と同じ形の走査） |
| 5 | 在庫の下限で**警報が出る**こと（黙って帯を広げない・D-079 ⑦） |

---

## 5. GB-5 — 厩舎の格（D-103）

- **馬ごと**に持つ。ブロンズ・シルバー・ゴールド
- ★**`potential`（天井）を変えない。変えてよいのは近づき方（伸び）だけ**
- ★**同じ EP を注いだとき、上の格が強くならない**（時間の効率は上がってよい）
- **お金で格を上げない**（EP での解放は可）

### 5-1. 検査

| # | 内容 |
|---|---|
| 1 | どの格でも `current ≤ potential` が破れないこと（§7.3 の不変条件） |
| 2 | ★**同一 EP 予算で、格を上げた場合の引退時の素質開放率が、上げない場合を大きく上回らないこと**（V-14 ③ と同じ測り方・D-045） |
| 3 | 上の格でキャリア後半の伸びが 0 に張り付かないこと（V-14 の差と後半の伸びを報告・D-044） |
| 4 | **V-14・V-15・V-7a/V-7b を流し、変わった量を報告** |
| 5 | **変異**: 格が `potential` を上げる形にすると 1 が落ちる |

---

## 6. GB-6 — 経済の取り直し（最後にまとめて）

GB-1〜GB-5 が入ったあとに、**1 回でまとめて**行ってください。

- §3.4 の収支（1 頭・1 キャリア）を**再計算**: 調教費・出走登録料に加え、**購入（GB-4）・騎手の料金（GB-3）・格の消費（GB-5）**を入れる
- **V-11** を合成プレイヤー集団で流す（較正は合成集団・監視は実集団・D-051）
- D-075 の額（初期 EP 2,000・デイリー 200・日次上限 10,000）が**まだ妥当か**を報告。★値を動かす提案は照会に回す（較正定数）
- ★**帯を外れたものがあれば、値と向きだけを報告**。定数は動かさない

---

## 7. 報告

`REPORT_GAME_BODY_1_<日付>.md`。

- **工程ごと**に: 何を作ったか ／ 検査 ／ 変異の実演 ／ 流したゲートの値（変わらないなら「変わらない」と明記）
- **測ったコミットを併記**（R-23・R-28）
- `npm run typecheck` と `npx vitest run --maxWorkers=2 --minWorkers=1` の行・終了コード。既存の赤（`edit-grammar-audit` ⑨）が 1 件から増えていないこと
- ★**着順に効くものを入れていないこと**の対照（GB-3 の検査 1）を必ず載せること
- 未実装で残したもの（出走登録の RPC・購入の DB・モードの切り替えの画面）を列挙

---

## 8. この便に含めないもの

| 事項 | 理由 |
|---|---|
| サイクルの間隔（`CYCLE_MS`）・`RACES_PER_DAY` の導出化・§10.2 の時間割 | T-1 が未定（オッズ計算の所要と観戦の尺を測ってから・D-100） |
| 育成モード／馬券モードの画面の構成・ホーム・ハンバーガー | ★**デザイナー側（Claude Design）のカードを待つ**（オーナー決定 O-8。開発側が独自に起こさない） |
| 騎手を着順に効かせること | 効かせる便で V-4〜V-6・V-17・V-18・V-13 を取り直す（D-105 ③） |
| 購入の DB・RPC・画面 | 利用者向け RPC の便でまとめて |
| ジャックポット・くじ引き・期間限定の仕掛け | オーナー決定「今は不要」・弁護士ゲート事項（回答 §10） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
