# 報告（V-19 の先行実装 — ローカルで固められる範囲）— 2026-08-20

**宛先**: レビュー側／オーナー
**発行**: 開発側
**根拠**: `REVIEW_AUTH_LINE_VERDICT_20260820.md` §4 の分割（①ローカルで固められる範囲を先に固める）
**成果物**: `packages/auth/`（新設）／`db/migrations/0017_user_identities.sql`／`tools/mutation/run.mjs` に V19-1〜10 を登録
**テスト**: **910 件 PASS**（既存 872 ＋ 新規 38）／`npm run typecheck` クリーン

---

## 0. ★先に、裁定に無かった発見を1件

**LINE は用途によって ID トークンの署名方式が変わります**（公式ドキュメントで確認）。

| 経路 | 署名方式 | 鍵 |
|---|---|---|
| **web ログイン**（サーバー側の認可コードフロー＝**今回実装した経路**） | **HS256** | チャネルシークレット（対称鍵） |
| **ネイティブ SDK / LIFF**（＝**§15.4 の Expo アプリで使う経路**） | **ES256** | `https://api.line.me/oauth2/v2.1/certs` の公開鍵 |

### なぜこれが重いか

**ヘッダの `alg` を読んで検証方式を選ぶ実装にすると、攻撃者がヘッダを書き換えるだけで検証経路を選べます。** `alg: none` や「非対称のつもりが対称で検証される」すり替えが成立します。**認証実装で最も典型的な破られ方です。**

→ **実装では経路ごとに期待する方式を固定し、ヘッダの申告で分岐させていません**（`line-id-token.ts` の `EXPECTED_ALG`）。変異試験 V19-2 でこれを固定しています。

### ★アプリ化（§15.4）への申し送り

**Expo で LINE SDK を使うと ES256 の第2経路が増えます。** そのとき —

- **同じ関数に `alg` の分岐を足してはいけません。別の入口にすること**（コード中に注記済み）
- **V-19 は ES256 経路にも同じ12項目を課す必要があります**（JWKS の取得・鍵のローテーション・`kid` の扱いという新しい面も増えます）
- これは裁定 §3 の「**認証はプロバイダ追加のたびに壊れうるので、回り続けるゲートでなければならない**」の具体例そのものです

**→ V-19 を正典に書くとき、「経路ごとに」課すことを明記していただけますか。** 開発側で正典は書きません。

---

## 1. [要確認] の1件が裏取りできました

裁定 §1 の —

> **staging 用のチャネルは callback URL が違うので別チャネルが必要**ですが、**同一プロバイダー配下に置けば `sub` の意味が環境間で一貫します**（[要確認] 実装時に LINE の公式ドキュメントで裏取りすること）

**公式ドキュメントで確認できました。裁定のとおりです。**

- ユーザー ID は**プロバイダー単位で発行**される
- **同一プロバイダー配下なら、チャネルの種類（LINE Login / Messaging API）が違っても同じ値**
- **別プロバイダーでは別の値**
- 形式は `U[0-9a-f]{32}`。**一度発行されると、利用者が LINE アカウントを削除しない限り変わらない**

→ **D-076 の「Web とアプリのチャネルは同一プロバイダー配下」は、この性質に基づく必須要件**として確定してよいです。

**★併せて、裁定 §7-1（アカウント喪失時の扱い）の前提も確定しました** — 「**アカウントを削除しない限り変わらない**」＝ **削除すれば変わります。** オーナー判断（(a) 復旧なし ／ (b) サポート手動復旧）は依然として必要です。

---

## 2. V-19 の実装状況（12項目）

| # | 項目 | 状態 | 固定している場所 |
|---|---|---|---|
| 1 | 署名改竄 → 拒否 | ✅ **固定済み** | `line-id-token.test.ts` ／ 変異 V19-1 |
| 2 | `aud` 相違 → 拒否 | ✅ **固定済み** | 同上 ／ 変異 V19-4 |
| 3 | 期限切れ → 拒否 | ✅ **固定済み** | 同上 ／ 変異 V19-5 |
| 4 | `state` 不一致 → 拒否 | 🟡 **経路のみ固定** | `login-flow.test.ts` ／ 変異 V19-8。**「一度きりの消費」を実際に担保する保管の実装は staging** |
| 5 | 同一 `sub` で2回 → ユーザーは1つ | 🟡 **制約は記述済み・未固定** | `0017` の `primary key (provider, subject)`。**DB に当てて確かめるのは次段** |
| 6 | メール一致・`sub` 相違 → 別ユーザー | 🟡 同上 | `0017`（`subject` に入れてよいのは `sub` のみと明記）。**同上** |
| 7 | `nonce` 不一致 → 拒否 | ✅ **固定済み** | `line-id-token.test.ts` ／ 変異 V19-6・V19-7 |
| 8 | `iss` 相違 → 拒否 | ✅ **固定済み** | 同上 ／ 変異 V19-3 |
| 9 | 検証を通らずにセッションが出ないこと | 🟡 **経路のみ固定** | `login-flow.test.ts` ／ **変異 V19-9・V19-10**。**Edge Function のエンドポイント認可そのものは staging** |
| 10 | `sub` の一意制約が DB 側にあること | 🟡 **記述済み・未固定** | `0017`。**同上** |
| 11 | 本番経路で固定する | ✅ **固定済み** | **変異 V19-9 がまさにこの形**（下記 §3） |
| 12 | 検出器が自分自身を検出していない | ✅ **固定済み** | トークンは試験内で独立に組み立て、実装の関数を一切呼ばない |

**✅ 7項目が固定済み。🟡 5項目は「書いたが、まだ壊して確かめていない」状態です。** D-054（効いていることと、壊れたときに落ちることは別）に従い、**固定していないものを「済み」とは書きません。**

---

## 3. ★変異試験 — 10件すべてが検出され、すべて★（振る舞い）テストが落ちました

`npm run mutation` に V19-1〜10 を登録しました。auth に絞った実験の結果:

| 変異 | 内容 | 検出 | 落ちた★テスト |
|---|---|---|---|
| V19-1 | 署名検証の結果を無視する | ✅ | 6 件 |
| V19-2 | `alg` をヘッダの申告どおりに受け入れる | ✅ | 6 件 |
| V19-3 | `iss` を検証しない | ✅ | 4 件 |
| V19-4 | `aud` を検証しない | ✅ | 2 件 |
| V19-5 | 有効期限を見ない | ✅ | 2 件 |
| V19-6 | `nonce` が無ければ素通りにする | ✅ | 2 件 |
| V19-7 | `nonce` の一致を見ない | ✅ | 4 件 |
| V19-8 | `state` を確認するだけで消費しない | ✅ | 4 件 |
| **V19-9** | **★検証結果を握り潰してセッションを発行する** | ✅ | **16 件** |
| **V19-10** | **★検証を呼ばずに素通しする（呼び忘れ）** | ✅ | **18 件** |

### ★V19-9 / V19-10 が #11 の答えです

裁定 §3 #11 —

> `verifyIdToken()` の単体テストが全部通っても、**呼び出し側が検証を飛ばす形に書き換えて全緑になるなら、防御はゼロ**です。**検証を飛ばした呼び出し側で落ちること**を実験して示してください

**V19-9 は「検証は正しく呼ぶが、結果を握り潰してセッションを出す」／V19-10 は「検証を呼ばない」変異です。**

**このとき `line-id-token.test.ts`（24件）は全緑のままです** — 検証関数自体は壊れていないので。**落ちるのは `login-flow.test.ts` の経路の試験だけ**で、16件・18件が検出しました。**I-3 / I-4 / L-1 で3度踏んだ形が、今回は検出側に回っていることを実験で示せました。**

### 検出器の自己検出について（#12）

- 試験用のトークンは**この試験の中で base64url とHMAC から独立に組み立て**ています。実装の関数を一切呼びません
- 期待値は**外から見える結果**（`issueSession` が呼ばれた回数）に置き、実装の内部分岐を参照していません
- 変異ハーネスの★印判定（振る舞いテストが落ちたことを要求する）に**10件すべてが適合**しました

---

## 4. ★「対照」を1件ずつ置いた理由

**各試験ファイルに「正しいトークンなら通る」を1件だけ置いています。**

裁定は「**正しいトークンで入れることを確かめるテストは V-19 に数えない**」としており、それは正しいのですが — **対照が無いと「常に拒否する実装」が上の全項目を通ってしまい、ゲートが空になります。**

→ **対照は防御ではなく、ゲートを空にしないための足場**として置き、その旨をコード中に明記しました。**V-19 の項目数には数えていません。**

---

## 5. 実装の要点（裁定の制約との対応）

| 裁定の制約 | 実装 |
|---|---|
| ★自前で JWT を発行しない／セッションは Supabase Auth に持たせる | `packages/auth` は**セッションを作らない**。`issueSession` は注入される依存で、実体は Supabase Auth を呼ぶ |
| `auth.uid()` / RLS / 書き込み RPC を一切変更しない | **変更していません**（差分は新規パッケージと新規マイグレーションのみ） |
| LINE から取得するのは `sub` のみ | **返り値の型に `sub` しか無い**。コメントで禁じるのではなく、**型として持ち出せない形**にしました。試験で `name` / `picture` / `email` が載らないことも固定 |
| `users` に専用列を作らない（D-078） | `user_identities(provider, subject)` を新設。`users` は無変更 |
| 決定論（憲法 4・`Date.now()` を呼ばない） | 現在時刻は `nowSec` で注入。**副産物として、期限切れを再現可能に試験できます** |
| 失敗理由を外に出さない | `VerifyFailure` は詳細だが、`LoginFailure` は一律 `token_rejected` に畳む。**畳まれていることを試験で固定** |

---

## 6. 次の段（着手条件つき）

| # | 範囲 | 必要なもの |
|---|---|---|
| 1 | **V-19 #5・#6・#10 を DB に当てて固定** | `0017` を適用できる DB。⚠️ **この案件の「local」は remote の development プロジェクト**（`secrets.local.env`）なので、**適用は指示をいただいてから行います**（勝手に共有環境の状態を変えません） |
| 2 | **V-19 #4・#9 の実体**（state 保管・Edge Function のエンドポイント認可） | **staging**（着手条件2件：service_role キーのローテーション／staging 用 LINE チャネル） |
| 3 | Edge Function 本体と `/login`・`/signup` の差し替え | 同上 |

---

## 7. オーナーへ（前便から継続・変化なし）

| # | |
|---|---|
| 1 | **LINE アカウント喪失時の扱い**（(a) 復旧なし ／ (b) サポート手動復旧）。**§1 で「削除すれば `sub` は変わる」ことが確定**したので、判断の前提は揃いました |
| 2 | **EP 日次総発行量の上限（D-077）** の承認 |
| 3 | **[EYES] PC とスマホでの LINE ログインの往復の実機確認** |
| 4 | 🔴 **staging の service_role キーのローテーション**（着手条件1） |
| 5 | 🔴 平文クレデンシャル4ファイルの整理 |
