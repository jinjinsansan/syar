# 報告 — #1「俯瞰で馬がぴょんぴょんする」を直した（選択肢 1・完歩 9m）

作成日: 2026-08-25 / ブランチ `p4/race-30sec-cuts` / 基点 `ee1a884`
指示: `HANDOVER_P4_2D_MOTION_20260825.md` §3 → オーナー判断 **選択肢 1・見た目の完歩 9m**
判断材料: `REPORT_P4_2D_OVERHEAD_STRIDE_20260825.md`

---

## 1. 受けた判断

| 項目 | 判断 |
|---|---|
| #1 の対処 | ★**選択肢 1・見た目の完歩 9m**（11m ではなく 9m） |
| `ee1a884` の push | ★**可** → 実施済み（下記 §6） |
| #3-a / #4（せめぎ合いのままゴールへ） | 指示書待ち。**着手していません** |

---

## 2. 直したもの

見た目の完歩を**ショットごと**に引けるようにし、`fourth-corner-high` だけ **9m** にしました。
他の 5 ショットは実馬どおり **7m** のままです。

| ファイル | 変更 |
|---|---|
| `packages/render/src/broadcast-v2.ts` | `BROADCAST_STRIDE_M_BY_SHOT` と `broadcastStrideMFor()` を新設（★値の出どころはここ 1 か所） |
| `apps/web/src/app/race/page.tsx` | `frameOf` / `phaseOf` が `broadcastStrideMFor(sceneToDraw.shot.id)` を読む |
| `tools/shot-race-at.mjs` | 同じ関数を読む（画面と同じ絵を作る道具・R-30） |
| `tools/audit-overhead-stride.mjs` | 測定器も同じ関数を読む（★これをやらないと**画面と別の脚を測る**） |
| `packages/render/test/overhead-stride.test.ts` | 新規テスト 4 件 |

### ★`scene` ではなく `sceneToDraw` から引くこと

ディゾルブの 0.28 秒は、**1 コマの中で前のショットも描きます**（`page.tsx` の
`drawScene(offCtx, prevScene)`）。いまのショット（`scene`）から完歩を引くと、
重ねている間だけ**前の絵の脚が別の速さ**になります。テストで固定しました。

---

## 3. 実測（seed 42・台本 v5・30fps・先頭馬・中央値）

測る量は「1 完歩で足元の地面に対して馬体何個ぶん動くか」（実馬 = 7m ÷ 2.4m = 2.92 馬身）。
`npx tsx tools/audit-overhead-stride.mjs` で再現できます。

| ショット | 直す前 | 直した後 | 実馬比 |
|---|---:|---:|---:|
| `start-front` | 1.47 | 1.47 | 50%（変化なし） |
| `first-corner-front` | 2.30 | 2.30 | 79%（変化なし） |
| `side-drive` | 2.73 | 2.73 | 94%（変化なし） |
| **`fourth-corner-high`** | **1.82** | ★**2.25** | **62% → 77%** |
| `homestretch-side` | 2.88 | 2.88 | 99%（変化なし） |
| `finish-line` | 2.89 | 2.89 | 99%（変化なし） |

- ★**動いたのは指摘された俯瞰 1 ショットだけ**です。他の 5 つは判断材料の報告書の値と**完全に一致**しました。
- 脚の速さ: 2.08 → **1.62 完歩/秒**（0.78 倍）。カットの長さは 3.3 秒、真の走速 14.56 m/秒。

### 前後がハードカットであることの再確認

| 境目 | 視点の系統 | 実際の繋ぎ |
|---|---|---|
| `side-drive` → `fourth-corner-high` | side → high-diag | ★ハードカット |
| `fourth-corner-high` → `homestretch-side` | high-diag → side | ★ハードカット |

脚の速さが変わる瞬間は、画角も向きも素材も一斉に変わる切替に紛れます。
★**この前提が崩れたら新テストが鳴ります**（台本を触って前後がディゾルブになった場合）。

---

## 4. 触っていないもの

- ★**着順・レース結果・馬の位置・横位置 `w`**（憲法 3。描画側は読むだけ）
- ★**カメラ定義値**（`backM 40 / upM 6.5 / sideM 13 / fovDeg 15` のまま。選択肢 2 は採っていません）
- ★**台本・カット境界**（`SCRIPT_V5` は 1 行も変えていません）
- 素材・背景の 1 枚絵
- ★`Date.now()` / `Math.random()` は使っていません（憲法 4）

---

## 5. 検査

| | 結果 |
|---|---|
| `npm test` | ★**114 ファイル / 1125 件パス**（新規 4 件を含む） |
| `npm run typecheck` | エラーなし |
| `/race`（既定 v5） | 200 |
| `/race?cinematography=v4` | 200 |

### 新テスト `packages/render/test/overhead-stride.test.ts`（4 件）

1. `fourth-corner-high` は 9m・他は 7m。★**例外が 1 つだけ**であることも固定（増えたら鳴る）
2. 脚は「ゆっくり」側にしか動かない（7m より短い完歩を入れられない）
3. ★**採用の前提**＝この俯瞰の前後がハードカットであること
4. 画面が完歩の数字を自前で持たず、`sceneToDraw` から引いていること（R-30）

---

## 6. push

オーナー指示により `ee1a884` を push しました（承認対象を取り違えないよう SHA を明示）。

```
git push origin ee1a884:refs/heads/p4/race-30sec-cuts
   9edc761..ee1a884  ee1a884 -> p4/race-30sec-cuts
```

★**本報告のコミットはまだ push していません。** 指示があるまで手元に置きます。

---

## 7. ★残っていること

1. ★**最後は実画面で見ていただく必要があります。** 判断材料の報告書 §6 に書いたとおり、
   この指標は**必要条件であって十分条件ではありません**（`start-front` は 50% でも指摘なし）。
   77% にしたことで「ぴょんぴょん」が収まったかは、**seed 42 を再生して確かめてください**。
   - 収まらない場合の次の手は **11m（91%）**。値を 1 か所変えるだけです。
2. #3-a / #4「せめぎ合いのままゴールへ」は**指示書待ち**（着手していません）。
3. 本コミットの push 可否。
