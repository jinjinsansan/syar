# 【STAR】裁定: 公開の引退馬一覧 ✅ ／ 発見の軸（Q-DISC-1〜5）— 2026-09-25

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa`（`0083 retired_horses_public` と、照会 `QUESTIONS_DISCOVERY_AXES_20260925.md`）

## 1. ✅ (b) 公開の引退馬の一覧 — 受理
- ✔ 裁定 `51ea81d` §4 の条件 4 つを満たす: ①決定論（`retired_at_week desc → horse_id` の **2 段**・★1 段だと同じ週の順が揺れる・上限 60）②`anon` に grant ③見本を混ぜない ④★**「準備しています」を同じ便で外した**（網が残存を落とす）
- ✔ staging の実データ 18 件（引退馬 5,365 頭・★列に `display_name` / `email` / `owner_id` が**無い**・素質も無い・牧場名は出る・`anon` で読める・2 回 読んで同じ順・★**戦績の式が `my_retired_horses` と一致**〔5 頭 突合・不一致 0〕）
- ✔ ★「自分の馬の口が失敗しても公開の一覧を道連れにしない」を網にした（★`/entry` の事故と同じ形）→ ★**一度 起きた事故の形を、別の画面に先回りで当てた**

## 2. 🔴 (a) を止めたのは正しい — ★画面だけが別の模型だった
- ✔ 画面の見本は **スピード／スタミナ／パワー／賢さ**（＝能力 4 つ）、★正典 **D-116** は「発見＝**距離・馬場・脚質・気性**の判明」（`:100`）、`discovery.ts` の註記も正典側
- → ★**そのまま作れば「口も画面も間違った軸」**になっていた。★**実装の前に軸を照らした**のが正しい

## 3. 照会への答え

| # | 裁定 |
|---|---|
| **Q-DISC-1** 距離の帯 | ✅ **§8.2 の 5 段を使う**（`:1024`）。★別の区切りを作ると「距離帯」が 2 通りになる（D-052） |
| **Q-DISC-2** 馬場 | ✅ **両方**（種類＝芝／ダート、状態＝良／道悪）。★別の問いなので分ける。★ただし**画面では 2 行に分け、混ぜて 1 つの「馬場」にしない** |
| **Q-DISC-3** 気性 | ✅ **この便では行を出さない**（開発側の推しどおり）。✔ `RUNAWAY_BASE: 0`（`intervention.ts:163`）＝ ★**暴走が起きないので永久に「？？？」**。★**動かない行を出すより出さない**。★簿に「**D-112（暴走は率で引く）が入ったら気性の行を足す**」を戻ってくる条件として書く |
| **Q-DISC-4** 脚質 | ✅ **「試した回数」で出す**。★**「向く」と書かない**。★D-114 が許すのは「オッズと戦績」なので、★**その脚質での戦績（出走・勝ち）を事実として並べる**のはよい。★**適性の段・有利不利の言葉にしない** |
| **Q-DISC-5** 他人の馬 | ✅ **自分の馬だけ**。★他人に配ると「オッズと戦績以外の手がかり」になる（D-114） |

## 4. 作る順と条件
1. ★**口（回数だけ）→ 画面（段は `discoveryStageOf`）**の順（開発側の案どおり）
2. ★段の計算は **TS の 1 か所**（D-108 ②）。★SQL で段を計算しない
3. ★口が返すのは**回数と戦績の事実**だけ。★素質・現在値・段を返さない
4. ★画面の見本（能力 4 つ）は、★**この便で消す**（★消さないと「間違った軸の見本」が残り、次に誰かが写す）
5. ★口が出来たら**釘から `horse-story-demo` を外す**（前の裁定の条件）

## 5. 追記（2026-09-25）— (a) の実装（`0084`）✅ と次の順番

### ✅ 受理
- ✔ §4 の条件 5 つすべて（口 → 画面／段は TS の 1 か所／回数だけ／見本を消した／釘から `horse-story-demo` を外した）
- ✔ 距離は `DISTANCE_BANDS` を読む（★SQL に境目を書いていない）／馬場は 2 行／気性は出さない／脚質は「6 回 試しました」＝**事実だけ**／自分の馬だけ
- ✔ ★**「道悪 = good 以外」を自分で決めず、宣言から取った**（`coefficients.ts:107`・`balance.ts:72`）→ ★**線を発明しない**
- ✔ staging 13 件（列は 5 つだけ・段も素質も無い／確定した出走だけ数える／決定論／★**他人の馬は拒む**）。★`0083` で他人の引退馬が見えるようになった後なので、★この拒みは特に要る

### ✅ 網の誤検知を 3 回 直した件 — ★**直し方が正しい**
- ★①「見本だけの画面」判定がモジュール名だけを見ていた → ★**前便の「見本だけは `/stable/market` だけ」は誤りで、2 枚**（`/stable/[horseId]` も）。★自分で訂正した
- ★②③ の誤検知（`.from(` が `Array.from` に当たる等）を、★**正規表現を足して当てにいくのをやめ、理由つきの除外簿に置いた**。★この案件の作法どおり（★`printed-defaults` と同じ「機械は形だけ拾い、嘘かどうかは人が簿に書く」）
- ✔ 期待値を**実測で固定**（`['/stable/[horseId]', '/stable/market']`）

### ✅ 次は `/stable/[horseId]` を本物へ繋ぐ — ★それでよい
- ★理由: ★**利用者が開ける画面が、いま見本のデータを出している**（`demoStableRepo`）。★見た目の作り直しより**先**。★`/stable/market` は入口が無い（D-122 ④）ので急がない
- **条件**: ①本物に繋ぐと同時に ★**発見の行を渡す**（`horse-resume` は渡されなければ節を出さない形になっている）②★**4 つの状態**（読み込み中・未ログイン・他人の馬・見つからない）③★他人の馬を開いたときに**素質・能力・発見度を出さない**（D-114・`0084` の拒みと揃える）

### ⚠️ 本番に出していない移行が積み上がっている
★`0079` 以降が staging だけの状態。★**引継ぎ書に「本番に出していない移行の一覧と、その束の意味」を 1 か所**に持つこと（★オーナーが配備を許した日に、★**何が一度に入るか**を 1 枚で言えるように）。
