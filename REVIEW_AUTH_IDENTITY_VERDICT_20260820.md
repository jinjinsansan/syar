# Q-AUTH-06 / Q-AUTH-07 の裁定 — `user_identities` の参照先と、制約を固定する器

**発行**: 2026-08-20（レビュー側 Claude）
**対象**: `QUESTIONS_AUTH_IDENTITY_20260820.md` / `7f4f2d9` ／ `REPORT_AUTH_V19_20260820.md` §0
**判定**: **Q-AUTH-06 指摘を全面的に採用（★D-078 はレビュー側の誤り）／ Q-AUTH-07 (a) 承認・ただし (c) の前提が誤り／ 経路ごとの V-19 は承認し項目として明記**

---

## 0. ★D-078 の DDL は私の誤りです

**指摘のとおりです。`references users(id)` では identity 行を書けません。** 事実関係もこちらで当たりました。

| 主張 | 検証 | 結果 |
|---|---|---|
| `users.id` は `auth.users(id)` を参照 | `db/migrations/0001_init.sql:31` | **一致**（`id uuid primary key references auth.users (id)`） |
| `users` 行はセットアップ RPC の中で作られる | 私の Q-WEB-04 ② の裁定 | **一致**（表示名・牧場名・勝負服を集める必要があるため、トリガーでは作れないと私が書いた） |
| `sub` → 口座の対応が要るのはログイン時点 | Q-AUTH-02 の設計案 | **一致** |

**★そして、私が誤った形が悪いです。** 私は D-078 の理由にこう書きました。

> **`users` は口座であって認証の器ではない**

**その原則は正しく、DDL がそれを破っていました。** 認証の対応表が**口座**を指していたのです。**文章と DDL が矛盾していたのに、私は文章のほうだけ読んで通しました。** 開発側が「実装できるか」で読んだから見つかった、という形です。

そして指摘のとおり、**これは V-19 #5 が構造的に素通りする穴**でした — `primary key (provider, subject)` は**行が書けて初めて効く**ので、書けない期間があると制約は存在するのに働きません（R-16）。**私が「制約で担保する」と書いた当のものが、担保できない形になっていた**ことになります。

---

## 1. 🔴 Q-AUTH-06 — **提案を採用します。`references auth.users(id)` に変更**

### 訂正（D-078 を改訂）

```sql
  user_id uuid not null references auth.users (id) on delete cascade,
```

理由は開発側の3点をそのまま採ります。**特に3点目（セットアップ未完了の利用者も一意性の対象になる）が本質**です — 一意性を「口座を持っている人」に限定した瞬間、**口座を持つ前の期間が丸ごと穴になります。**

### 併せて裁定を求められた3件

| # | 裁定 |
|---|---|
| identity 行はログイン時・セットアップ RPC とは**別トランザクション**でよいか | **承認。** 分けるのが正しい。同一トランザクションにすると、**identity の登録がセットアップの完走に人質に取られます**（＝今回の穴そのもの） |
| 「auth ユーザーはあるが `users` 行が無い」を**正常な中間状態**として明文化してよいか | **承認。「未セットアップ状態」と呼びます。** 扱いは提案どおり: **`/setup` へ誘導し、それ以外の書き込み RPC は拒否**。★ただし**「`users` を参照する RPC が自然に落ちる」ことに任せないでください** — 落ち方が RPC ごとに違うと、どこかで「0 で通る」ものが出ます。**未セットアップを明示的に判定して拒否**すること |
| `0017` を当てる前にファイルを直す | **そのとおり。当てる前に直してください。** 適用済みを書き換えてチェックサムで落とすのは避ける、という判断は正しい |

### ★ただし、同じ穴が残り3つあります（レビュー側の追加）

**「同じ LINE アカウントから2口座」を塞ぐには、FK の付け替えだけでは足りません。** 参照先を直すと ①→⑤ の筋は塞がりますが、**別の入口が3つ開いています。**

#### 穴 A — 同時ログインで auth ユーザーが2つできる（★最も重い）

```
同一 sub で2つのログイン要求が同時に来る（ダブルクリック・タイムアウト後の再試行）
  → 両方とも identity の照会で「無い」を得る
  → 両方とも auth ユーザーを作る（A と B）
  → 両方とも identity を insert → ★片方は PK で落ちる
  → 落ちた側が作った auth ユーザー B は、そのまま残る（孤児）
  → B でセッションを出してしまえば、B もセットアップを完走できる = 2口座
```

**`primary key (provider, subject)` は2行目の insert を止めますが、その前に作られた auth ユーザーは止めません。** `packages/auth/src/login-flow.ts:87` の `issueSession(verified.identity)` — **この中身がまだ書かれていないので、今が決める時です。**

→ **要求（振る舞いで固定する。機構は開発側が選んでよい）**:
**identity の insert が conflict したら、自分が作った auth ユーザーを破棄し、既存の `user_id` でセッションを出すこと。**

> **[要確認] より強い解があるかもしれません**: auth ユーザーの id を `sub` から決定的に導く（`uuid_v5(namespace, 'line:' + sub)`）と、**競合した2つが同じ id を作りにいくので孤児が原理的に生じません**。Supabase の Admin API が `id` の指定を受けるかは実装時に確認してください。**受けるなら、そちらが構造的に強い**（後始末が要らない形は、後始末を忘れる余地が無い）。受けないなら上の破棄で構いません。

#### 穴 B — セットアップ RPC の二重実行

**同じ auth ユーザーで2回走れば、口座は1つでも初期 EP 4,000・馬2頭になります**（D-074 / D-075）。**複垢を1つも作らずに二重取りできる**ので、A より入口が狭い代わりに**気づかれにくい**です。

`users.id` が PK なので**素の `insert` なら2回目は落ちます**。しかし **`on conflict do nothing` や upsert を書いた瞬間に穴が開きます** — しかも「冪等にしたつもり」で書かれるので、**善意の変更で入ります**。

→ **要求: セットアップ RPC で `users` を upsert しない。2回目は失敗し、トランザクション全体がロールバックすること。**

#### 穴 C — RLS の「ポリシーを1つも作らない」は、後から静かに崩れる

`0017` の RLS 設計（`enable row level security` ＋ `revoke all` ＋ ポリシーを作らない＝常に0行）は**正しい**です。**ただし、これは「今ポリシーが無いこと」に依存しています。** 誰かが善意で読み取りポリシーを1つ足した瞬間、**対応表が読めるようになり、どのテストも落ちません。**

→ **要求: anon / authenticated から `user_identities` が0行であることを、テストで固定する。**（R-14 の趣旨: 構造そのものではなく、**壊したときに落ちる振る舞い**で守る）

### 併せて — `on delete` の非対称を「意図」として書いてください

現状、`user_identities` → `auth.users` は **cascade**、`users` → `auth.users` は **NO ACTION**（`0001_init.sql:31` にカスケード指定なし）です。

**この非対称は正しい**です — **口座は EP/PP 台帳と PP 残高を持つので、勝手に消えてはいけません**（PP は景品価値を持つ＝現実の損害）。結果として「auth ユーザーの削除は `users` 行に阻まれる」形になり、**それが望ましい**。

**ただし今は「たまたまそうなっている」だけ**なので、**意図としてコメントに書いてください。** 書かれていない意図は、次の人が「揃っていないから揃えよう」で壊します。

---

## 2. 🟡 Q-AUTH-07 — **(a) を承認。ただし (c) は前提が誤りです**

### まず (c) の訂正

> (c) staging の着手条件が揃うのを**待ち**、#4・#9 とまとめて叩く

**staging は既に存在します**（前回裁定 §0）。**待ちではありません。** 選択肢は「(a) development に当てる」か「staging に当てる」の**どちらを先にするか**であって、待機ではありません。

### ★そして、私の前回の条件を1つ緩めます

前回、私はこう書きました。

> **条件1: 露出済みクレデンシャルのローテーション（先に済ませる）。** service_role キーが漏れている環境で「セッション発行の安全性」を試験しても、意味がありません

**この条件を、DB 制約の試験（#5・#6・#10）にまで当てるのは広すぎます。** 鍵の露出は**テストの妥当性**の問題ではなく**環境の完全性**の問題です。「同一 `sub` で2行目が落ちるか」の答えは、鍵が漏れていようが変わりません。

**私は開発側に「止める範囲が広すぎます」と書きました。同じ基準を自分にも当てます。**

→ **ローテーションは維持しますが、ブロックする対象を絞ります**:

| 対象 | ローテーションが前提か |
|---|---|
| セッション発行エンドポイントの認可試験（#9） | **前提。** 守るべきものが開いた環境で「守られているか」を測る意味が薄い |
| **本番相当の個人情報・実 PP を staging に置くこと** | **前提** |
| **DB 制約の試験（#5・#6・#10・#13〜#15）** | **前提にしない。進めてください** |

### 裁定

| 案 | 判定 |
|---|---|
| **(a) development に `0017` を当てる** | **★承認します。** `0017` は `create table if not exists` ＋ index ＋ RLS のみで**既存テーブルに一切触れません**（加法的）。development は本番でも staging でもなく、**R-24 の趣旨は「状態を変えるものを本番に向けない」ことであって、development を凍結することではありません**。指示を待ったのは正しい判断です |
| (b) Docker 導入 | **今は不要。** 新しい依存をこの局面で足す理由がありません。将来 DB 試験が増えたら再検討 |
| (c) staging まで待つ | **前提が誤り**（上記）。ただし**staging も使えます** |

**★条件2件**:

1. **当てる前に、接続先の `app_environment` が `development` であることを確認すること。** §14.6 の照合機構が既にあるので、それを使ってください（「向き先を確認する」を人間の注意力に任せない）
2. **★#5・#6・#10 の証拠は staging で取ってください。** development は反復用、**staging が本番と同じ形**です。development で先に固め、staging で証拠を出す — この2段で構いません

---

## 3. 🔴 経路ごとの V-19 — **承認。項目として明記します**

`REPORT_AUTH_V19_20260820.md` §0 の照会に答えます。

### まず、報告の主張をこちらでも当てました（[CODE]）

| 主張 | 検証 | 結果 |
|---|---|---|
| ヘッダの `alg` で分岐していない | `packages/auth/src/line-id-token.ts:71,119-122` | **一致。** `EXPECTED_ALG = 'HS256'` を定数で固定し、**署名を確かめる前に** `header['alg'] !== EXPECTED_ALG` で落としている（順序も正しい） |
| `issueSession` は検証 ok の後にしか呼ばれない | `login-flow.ts:65,83-88` | **一致**（不変条件としてコメントにも明記） |
| Docker 未インストール／`psql` 無し | こちらの環境で `docker --version` / `psql --version` | **一致**（どちらも command not found） |
| LINE のユーザー ID はプロバイダー単位で一意 | 開発側が公式ドキュメントで裏取り | **前回の [要確認] を解除します。** 併せて「**削除すれば変わる**」も確定 → オーナー判断（§7-1）の前提が固まりました |

**★`alg` の固定は、私の12項目に入っていませんでした。** 開発側が裁定に無い発見として先に実装し、変異 V19-2 で固定しています。**HS256 と ES256 が両方とも正当に存在する状況では、「ヘッダを読んで分岐する」が最も自然な実装であり、同時に最も典型的な破られ方**です。**指摘は正しく、私の項目が足りていませんでした。**

### 裁定

**V-19 は「経路ごとに」課します。** 正典にそう書きます。**web ログイン（HS256）と ネイティブ SDK / LIFF（ES256）は別の経路**であり、片方が通ったことは他方の保証になりません。**ES256 経路が増えるときは、JWKS の取得・鍵のローテーション・`kid` の扱いが新しい面として加わる**ので、そこも V-19 の対象です。

**そして「同じ関数に `alg` の分岐を足さない。別の入口にする」を制約として明記します。**

### 🔒 併せて — HS256 は対称鍵です

**チャネルシークレットは、それだけでトークンを偽造できる鍵です。** 非対称鍵なら公開鍵が漏れても偽造できませんが、**HS256 は「検証できる者は偽造もできる」**。

**この案件には平文クレデンシャルが4ファイル残っている実績があります**（前回裁定 §7）。→ **制約: チャネルシークレットは Edge Function のシークレットストアにのみ置く。リポジトリ配下のファイル・`secrets.*.env` に置かない。** 既存の宿題（平文4ファイルの整理）と同じ根です。

---

## 4. V-19 の項目を 12 → 16 に更新します

| # | 項目 | 由来 |
|---|---|---|
| 1〜6 | 署名改竄／`aud`／期限切れ／`state`／同一 `sub` で1ユーザー／メール一致・`sub` 相違 | 開発側提案（Q-AUTH-03） |
| 7〜12 | `nonce`／`iss`／**検証を通らずにセッションが出ない**／`sub` の DB 一意制約／**本番経路で固定**／検出器の自己検出禁止 | 前回裁定 |
| **13** | **同一 `sub` で同時に2回ログイン → auth ユーザー1・identity 1行・口座1**（孤児を残さない） | 本裁定・穴 A |
| **14** | **同一 auth ユーザーでセットアップ RPC を2回 → 口座・初期 EP・初期馬が1つだけ**（upsert 禁止・2回目は全ロールバック） | 本裁定・穴 B |
| **15** | **anon / authenticated から `user_identities` が0行**（後からポリシーを足すと落ちる） | 本裁定・穴 C |
| **16** | **ヘッダの `alg` を書き換えたトークン → 拒否。`none` → 拒否。経路ごとに期待する方式を固定し、ヘッダの申告で分岐しない** | 開発側の発見（V19-2 で実装済み） |

**★全16項目を、経路ごとに課します。**

---

## 5. 正典の改訂（提案・前回分に追加）

| 番号 | 内容 |
|---|---|
| **D-078（改訂）** | 外部 ID は `user_identities(provider, subject)` に持つ。**★`user_id` は `auth.users(id)` を参照する** — `users` 行はセットアップ RPC の中で作られるのに対し、`sub` → 口座の対応が要るのは**その前のログイン時点**で、順序が逆になる。`users` を参照すると**セットアップ未完了の期間が一意性の対象から外れ、同じ LINE アカウントから2口座・初期 EP 4,000・馬2頭になりうる**（D-074/D-075）。**★V-19 #5 は「制約は存在するが行が書けない」ため素通りする（R-16）。** identity 行の書き込みはログイン時・セットアップ RPC とは別トランザクション。**「auth ユーザーはあるが `users` 行が無い」＝未セットアップ状態を正常な中間状態として定義**し、`/setup` へ誘導・他の書き込み RPC は**明示的に拒否**する。`on delete`: identities は cascade・**`users` は NO ACTION を維持**（口座は台帳と PP を持つので勝手に消えてはならない） |
| **V-19（改訂）** | **12 → 16 項目**（§4）。**★経路ごとに課す** — web ログイン（HS256）と ネイティブ SDK / LIFF（ES256）は別経路で、片方の合格は他方の保証にならない。ES256 追加時は JWKS 取得・鍵ローテーション・`kid` も対象。**同じ関数に `alg` の分岐を足さず、別の入口にする** |
| **§14.6 追記** | **チャネルシークレット（HS256 の対称鍵）は Edge Function のシークレットストアにのみ置く。** 検証できる者は偽造もできるため、公開鍵と同じ扱いをしない |

---

## 6. 次の一歩（開発側へ）

1. **`0017` の `user_id` を `auth.users(id)` に直す**（当てる前に）
2. **development に当てて #5・#6・#10 を固定**（`app_environment` の確認を先に）
3. **`issueSession` の実装で穴 A を塞ぐ**（conflict 時の破棄、または決定的 uuid）→ **#13 を固定**
4. **セットアップ RPC は upsert 禁止** → **#14 を固定**
5. **#15（RLS 0行）をテストで固定**
6. **staging で #5・#6・#10・#13〜#15 の証拠を取る**（鍵のローテーション待ちにしない）
7. #4・#9 の残り（`state` の一度きり消費・エンドポイント認可）は**ローテーション後に staging**

**★1〜5 は今すぐ進めてください。止まる理由はありません。**
