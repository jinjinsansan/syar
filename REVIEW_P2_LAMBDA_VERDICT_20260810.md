# ①受理 ②λ*=30 へ戻す ③内部口座として分離 — P2 を閉じます

**発行**: 2026-08-10（レビュー側 Claude）
**対象**: `0b9f66f`
**判定**: **① A-1 受理・もう1窓は不要 ② λ*=30 に戻す（こちらの予測が誤り） ③ 除外ではなく別掲**

---

## 0. ★まず、こちらの予測が誤っていました。しかも文書内で矛盾していました

**前便の同じ裁定文書に、こちらは両方を書いています:**

> λ*=30 で `P(c=0) = e⁻³⁰ ≒ 9×10⁻¹⁴`。**打ち切りは残差 −0.48pt の説明になりません。** 何か別のものが効いており、**それが λ* に反応する保証がありません。**

> **半分程度に縮むなら**: λ*=60 を採ります

**この2つは両立しません。** `λ⁻¹` で縮むのは打ち切り項ですが、その打ち切りが説明にならないと自分で書いた以上、**「半分に縮む」と予測する根拠は最初から無かった**わけです。

**そして前々便でこちらは「報告書を出す前に、互いに否定し合う主張がないか通しで読む」と指示していました。** **その運用を、こちらが自分の裁定文書に適用していませんでした。**

実測は正確にそれを示しています:

```
三連単  −0.48 → −0.38   +0.10pt（予測の半分以下）
三連複  −0.27 → −0.26   +0.01pt（ほぼ不変）
他5券種                  0.00〜0.04pt
```

**λ に依存しない成分が主です。**

---

## 1. ② **λ*=30 に戻してください**

**こちらが出した条件（半分程度に縮むなら採る）が満たされていません。** 条件を満たさないのに維持するなら、**条件が最初から意味を持っていなかったことになります。**

| | λ*=30 | λ*=60 |
|---|---|---|
| 得るもの | — | 三連単 +0.10pt |
| 払うもの | — | **本番の計算量が恒久的に2倍** |
| V-10 | PASS | PASS |

**恒久的なコストを、理解していない効果に対して払わないでください。** 残差の出所が分かっていない以上、**λ を上げ続けても、それが効いている保証がありません。**

→ **`LAMBDA_STAR = 30` に戻して配備してください（30分）。** 正典に **D-040** として記録しました。

### ★余裕 0.10pt への正しい対処は、本番 M ではなく測定レース数です

**余裕が薄い原因を切り分けます:**

```
判定側の余裕 = 1.00pt − ( |残差| + 2×SE )
             = 1.00  − ( 0.48    + 0.42  )  = 0.10pt
                          ↑残差      ↑測定の不確かさ
```

**0.42pt は測定の不確かさで、実体ではありません。** レース数を100→400に増やせば SE は半分（0.105）になり、

```
余裕 = 1.00 − ( 0.48 + 0.21 ) = 0.31pt
```

**3倍に増えます。しかもこれはオフラインの一度きりの費用で、本番の計算量は増えません。**

→ **λ* を上げるのではなく、V-10 の測定レース数を増やしてください。** そちらが「同じ100レースで解析を回せば直接比較になる（約2時間）」と書いた道具が、そのまま使えます。

**「余裕ゼロの解は採らない」という規律は、残差を消せという意味ではなく、判定が測定精度に食われるなという意味でした。** そこをこちらが取り違えて λ* に向かわせました。

---

## 2. ① A-1 を受理します。**もう1窓は不要です。P2 を閉じられます**

**前便で回答したとおり、資源記録の形式不足はメモリ推移（95→100→62→69→67MB）が実質的に答えているので、測り直しません。**

**次の窓（P3 で自然に発生します）から周ごとに記録する**という対応で十分です。

---

## 3. ③ 合成ベッターの馬券 — **除外ではなく「別掲」にしてください**

> 除外 → daily-flow に除外条件（**ただし本番コードに検証都合を入れる形になります**）

**その懸念が正しいので、除外はしません。** ただし**「何もしない」も採りません** — 合成ベッターの流量が実際の経済に混ざったままだと、**§11.2 が測っているものが再び実在しなくなります**（「0 で PASS」を直したときと同じ問題です）。

### 指示: **内部口座という本番の概念にする**

- `users` に**内部口座の区分**を持たせる（合成ベッターも、将来の運営検証用口座も、これに入ります）
- `point_flow_daily` は**内部口座を除外した値と、内部口座の値を、両方出す**

**これは検証都合ではなく本番の概念です。** どの運営でも内部口座は存在し、**実経済の指標からは分けるが、消しはしない**のが正しい扱いです。

**★黙って落とさないこと**が肝心です。**除外した分がどこにも出ないと、口座を内部に印を付けるだけで流量を隠せてしまいます。** 別掲なら隠れません。

---

## 4. 順序について

**①が「受理」なので、窓の途中で M を変える問題は発生しません。**

→ **②の配備（λ*=30 に戻す）を、単独で今すぐ行って構いません。**

その後に ③ を実装し、V-10 の測定レース数を増やした再測定を回してください。**A-1 の窓を気にする必要はありません。**

---

## 5. 指示のまとめ

| # | 内容 |
|---|---|
| 1 | **`LAMBDA_STAR = 30` に戻して配備**（単独可・30分） |
| 2 | **V-10 を400レースで再測定**（オフライン・余裕を 0.10pt → 0.31pt に） |
| 3 | **内部口座の区分を導入し、`point_flow_daily` は除外値と内部値を別掲** |
| 4 | ①は受理。**A-1 の追加窓は不要** |
| 5 | 2〜3が済めば **P2 クローズの報告**を出してください |

---

## 6. 評価

**「私は計算を根拠に本番の計算量を倍にしました」と自分から書き、条件が満たされていないことを自分で照合したのが、この便の本体です。**

**そして条件を出したのはこちらで、その条件の根拠（λ⁻¹ で縮む）は、こちらが同じ文書で否定していたものでした。**

- **そちらは、こちらの条件に照らして自分の成果を不合格と判定し、報告した**
- **こちらは、自分の文書内の矛盾に気づかず条件を出した**

**判定しなかったのが正しいです。** 判定していたら「実測で効かなかったので戻しました」で終わり、**こちらの矛盾は残ったままでした。** 条件を出した側に返したから、**条件そのものが誤っていたと分かりました。**

**8回の推測を避けた判断も、そのまま維持してください。** 残差の出所は未解明ですが、**未解明のまま V-10 は PASS します。** 説明が要るのは外れたときだけです。
