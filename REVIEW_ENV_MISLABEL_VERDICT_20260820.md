# 環境の取り違え — 判定と、Q-AUTH-07 の撤回

**発行**: 2026-08-20（レビュー側 Claude）
**方法**: **読み取り専用**。anon キーで公開ビュー `races_public` と `app_environment` を GET しただけ（書き込み・DDL・service_role いずれも使用せず）
**判定**: **`secrets.local.env` が指すのは本番。development プロジェクトは存在しない。★前回裁定 Q-AUTH-07 の「development に `0017` を当ててよい」を撤回する**

---

## 1. 実測（2026-08-20 01:39 UTC）

| | プロジェクト | ファイルの申告 | **DB の `app_environment`** | races 件数 | 期間 |
|---|---|---|---|---|---|
| **A** | `hfvvxwoulrjqznperici`<br>（`secrets.local.env`） | `development` | **`production`** | **1,668** | 2026-08-08 12:10 → **2026-08-20 02:00**（★実測時刻の**21分後**・`scheduled`） |
| **B** | `pezntijqkobbzqatiuxd`<br>（`secrets.staging.env`） | `staging` | `staging` | 38 | 2026-08-11 08:20 → 2026-08-14 01:00（6日前で停止） |

**A が本番です。二重に確定しています。**

1. **DB 自身が `production` と申告している**
2. **未来のレースが立っている** — 実測時刻 01:39 に対し最新が 02:00。**10分サイクルのワーカーが今この瞬間も先回りして生成している**。08-08 から連続

**B は staging** — 38件・3日で停止＝短期の試験環境。申告と一致。

**★そして `development` プロジェクトは存在しません。** 環境は**本番と staging の2つだけ**です。

---

## 2. 誤っているのは1行だけ。そして守りは効いていました

`tools/migrate.mjs:33` のコメント:

> `--env staging` で `secrets.staging.env` を使います。**既定は本番のまま**です。

**作った本人は `secrets.local.env` が本番だと知っています。** ファイル名の "local" は「**自分のローカルマシンにある秘密ファイル**」の意味で、環境名ではありません。**誤っているのは中身の `STAR_ENV=development` の1行だけ**です。

**そして、この案件が作った守りは実際に効いていました。**

| 機構 | 挙動 |
|---|---|
| `migrate.mjs:85-97` | **申告（`STAR_ENV`）と DB の `app_environment` を突合し、不一致なら throw**。→ 既定のまま流しても **`"development"` 対 `"production"` で止まります** |
| `assertNotProduction`（R-24） | **DB を見る**ので、A に対しては正しく「本番」と判定して拒否します |

**★「向き先を人間の注意力に任せず、DB を真実とする」という設計判断が、この取り違えを丸ごと吸収しています。** 設計として正しかったことが、事故で裏づけられた形です。

---

## 3. ★Q-AUTH-07 の裁定を撤回します

前回、私はこう書きました。

> **(a) development に `0017` を当てる — ★承認します。** development は本番でも staging でもなく…

**この選択肢は存在しませんでした。** 開発側が既定のまま `npx tsx tools/migrate.mjs` を叩けば、**向き先は本番**です。

**§14.6 の突合が throw して止めるので、実害は出ません。** しかし —

**★網が受け止めるところだった、というのは「手順が正しかった」ではなく「計画が誤っていた」ということです。** 網は最後の砦であって、手順の代わりではありません。**私が承認した計画は、最後の砦に着地する計画でした。**

### 差し替え

| | |
|---|---|
| **`0017` の適用先** | **staging（`--env staging`）。唯一の非本番環境です** |
| **V-19 #5・#6・#10・#13〜#15 の証拠** | **staging で取ります**（前回「development で反復・staging で証拠」と書いた2段は、前段が存在しないので消えます） |
| **鍵のローテーション待ちにするか** | **しません**（前回の絞り込みどおり。DB 制約の試験は環境の完全性ではなく制約の挙動を見るもの） |

---

## 4. 直し方 — 名前を直すだけでは足りません

### (1) ファイルの是正

```
secrets.local.env  →  secrets.production.env
STAR_ENV=development  →  STAR_ENV=production
```

**これで `migrate.mjs` の突合が通るようになります。** ただし —

### (2) ★既定を「本番」から「明示必須」に変える（こちらが本命）

**今回は名前が間違っていました。しかし名前が正しくなっても、`--env` 省略時の既定が本番であることは変わりません。**

```js
const envName = flag('env') ?? 'local';   // ← 省略すると本番へ向く
```

**「既定は本番のまま」を、コメントで注意喚起する設計として残さないでください。**

→ **`--env` を必須にし、本番だけは追加の明示を要求する**（例: `--env production --yes-production`）。**省略で本番に向く形をやめる。**

**★理由は R-16 の系です**: 今の形は「**最も手を抜いた操作が、最も危険な向き先に落ちる**」。守りが1枚（DB 突合）だけで支えられており、**その1枚は `app_environment` が正しく書かれていることに依存**しています。`verify-a7.mjs` が `app_environment` を `'development'` に固定する実装だった前歴（R-24 の背景）を踏まえると、**その1枚が書き換えられる経路は実在しました**。

### (3) `secrets.production.env` の鍵の格上げ

**★このファイルは本番の DB パスワードと（おそらく）本番の service_role キーを平文で持っています。**

前回私は「**staging の** service_role キーをローテーションせよ」と書きましたが、**優先度が逆でした**。

→ **ローテーションの対象に本番の鍵を含め、そちらを先にしてください。**（`git` には未追跡・履歴にも無いことは確認済みですが、**平文でディスク上にあること自体**が対象です）

---

## 5. 副次的な発見 — `app_environment` が anon で読めます

本番・staging とも、**anon キーで `app_environment` を GET できました**。環境名だけなので実害は小さいですが、**public スキーマに RLS の無いテーブルがある可能性**を示します。

**同じ状態のものが他にあると、重大です**:

| 対象 | 漏れると |
|---|---|
| `horses.potential` / `genotype` | **§12.4 が「本人にも数値を見せない」と定めたもの**。星の丸めが無意味になり、育成の判断が読み取りに変わります（D-070 と同じ性質の破壊） |
| `races.server_seed` | **§8.6 の Provably Fair が丸ごと無意味**になります（発走前に reveal が読める） |
| `users` / `ep_ledger` / `pp_ledger` | 他人の口座と台帳 |

→ **anon キーで何が読めるかの全数確認を要求します。** `0002_rls_and_place_bet.sql` が公開ビューを設計しているので、**「公開ビュー以外は anon から0行」**が期待される姿のはずです。

**★私はここで本番への探索を止めました。** 読み取り専用とはいえ、**本番に対して手当たり次第に叩くのは、レビュー側がやるべき形ではない**と判断しました（測定と探索を本番で混ぜない）。**開発側が staging で同じ確認を先に行い、期待される姿を確定させてから本番を1回確認する**のが順序として正しいです。

---

## 6. 優先順（レビュー側の推奨）

| # | やること | 誰 | 待てるか |
|---|---|---|---|
| **1** | **`secrets.local.env` の是正**（改名＋`STAR_ENV=production`）と **`migrate.mjs` の既定を明示必須に** | 開発 | **待てない** |
| **2** | **本番の service_role キー・DB パスワードのローテーション**（staging より先） | オーナー | **待てない** |
| **3** | **`0017` を staging に当てて #5・#6・#10・#13〜#15 を固定** | 開発 | すぐ着手可 |
| **4** | **anon 露出の全数確認**（staging で期待値を確定 → 本番を1回） | 開発 | 高（§8.6・§12.4 に直結） |
| 5 | LINE チャネル2つ（同一プロバイダー配下） | オーナー | 中 |
| 6 | オーナー判断3件（喪失時の扱い／EP 総発行上限／[EYES] 実機） | オーナー | 中 |
