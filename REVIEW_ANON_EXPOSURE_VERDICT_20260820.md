# anon 露出の裁定 — 0018 の当て方と、★報告に無かった重大な穴

**発行**: 2026-08-20（レビュー側 Claude）
**対象**: `REPORT_ANON_EXPOSURE_20260820.md` / `33e0235`
**判定**: **報告を承認。ただし★測っている軸が足りず、より重い穴が残っていた — `users` は anon から「読める」だけでなく「書ける」**

---

## 0. ★先に、報告に無い発見を1件（最優先）

**staging で実証しました**（存在しない ID を対象にした PATCH＝変化ゼロ）:

```
PATCH users        HTTP 204  🔴 書込権限あり
PATCH npc_stables  HTTP 204  🔴 書込権限あり
PATCH horses       HTTP 401  ✅ 拒否
```

**anon キーで `users` を UPDATE できます。**

**anon キーはブラウザに配られる公開の鍵です。** つまり —

> **誰でも、任意の利用者の `entry_points` と `prize_points` を、好きな値に書き換えられます。**

### なぜ読み取りより重いか

| | |
|---|---|
| **憲法 §0.2-4（サーバー権威）が破れています** | 「ポイント台帳・レース結果・乱数はすべてサーバー側。クライアント計算は信用しない」— **クライアントが台帳を直接書ける**なら、この条文は成立していません |
| **★どのゲートにも映りません** | **V-11 は台帳の流量を見ます。** 直接 UPDATE は `ep_ledger` / `pp_ledger` を1行も動かさないので、**残高だけが増えて、流量はゼロのまま**です。**ゲートは通ったままです** |
| **出口が現実の景品です** | PP を書き換えて §11 の景品交換に回せます。ゲーム内の不正ではなく**現実の損害**です |
| **`npc_stables` も書けます** | `distance_bias` / `surface_bias` / `heavy` など**レース生成の入力**です。書き換えれば結果が動きます。**シードのコミット／リビールは「乱数が後出しでない」ことしか証明せず、入力の改竄は証明の範囲外**です（§8.6 の前提が静かに崩れます） |

**★現時点の実害はゼロです**（`users` 0行）。しかし `npc_stables` は本番に1行あり、**今この瞬間から書き換え可能**です。**「最初の利用者が登録する前に」ではなく「今」です。**

### ★そして、これは測る軸が足りなかった形です

**全数確認は「読めるか」だけを見ていました。** 権限は読みと書きで別に付くので、**読みが0行でも書けることがあります**（`users` がまさにそれ）。

**この案件で何度も出た形**です — V-2d は平均だけを見て分散を見なかった／V-2e は距離だけを見て他の形質を見なかった（D-012）。**「全数」と名乗る検査が、軸の一つしか走査していない。**

---

## 1. 報告の内容 — 承認します（独立に裏取り済み）

| 主張 | レビュー側の検証 | 結果 |
|---|---|---|
| `races` / `horses` は拒否 | staging に anon で GET | **HTTP 401＝一致。中核（`server_seed` / `potential` / `genotype`）は守られています** |
| `bets` / `ep_ledger` / `pp_ledger` / `prize_exchanges` は「保護されて0行」 | 同上 | **HTTP 200＋0行＝権限はあるが RLS が絞っている。一致** |
| `users` ほか7件は無防備 | 同上 | **`users` は HTTP 200（＝権限あり）。401 ではない**。一致 |

**★「0行だった」を合格にせず、なぜ0行なのかで分けた**のは正しい判断です。**そこを分けなければ `users` は「0行だから安全」に見えました。**

---

## 2. 🔴 0018 の裁定 — **認証とは別便で、先に当てます**

### 質問への回答: **LINE 認証と同じ便に載せてはいけません**

| # | 理由 |
|---|---|
| 1 | **`users` の穴が実害になる瞬間は「最初の利用者が登録した時」＝認証が動き出す時そのもの**です。同じ便に載せると、**配備の順序が「穴が開いたまま認証が動く瞬間」の有無を決めます**。順序に依存する安全は、安全ではありません |
| 2 | **0018 は認証と独立**です（既存テーブルの権限）。束ねる理由がありません |
| 3 | **★利用者0の今が、RLS を有効化しても壊れるものが無い唯一の瞬間**です。配備判断（D-056 の一斉中止）とまったく同じ論理で、**この値段は上がる一方**です |
| 4 | **守りは機能より先に入れます。** R-15（機構は使われない状態で一度コミット）の趣旨も、守り側では「先に入れて、後から繋ぐ」になります |

### 0018 の要件

**★読み取りだけでなく、書き込みも塞ぎます。**

| 対象 | 当て方 |
|---|---|
| **`users`** | **RLS 有効 ＋ `using (id = auth.uid())` の SELECT ポリシー。`grant select` のみ。★`insert` / `update` / `delete` は anon にも authenticated にも付与しない** — 残高は `security definer` の RPC 経由でのみ動き、必ず台帳に記帳される（§4.1）。**`revoke all` にしないという判断は正しい**（S-2 と同型の事故を避ける） |
| `point_flow_daily` / `unlock_daily` / `horse_week_log` / `npc_stables` / `schema_migrations` / `app_environment` | **`revoke all`**（読みも書きも）。クライアントが読む理由がありません |
| **★全テーブル横断** | **anon / authenticated から、すべての public スキーマのテーブルに対する `insert` / `update` / `delete` を剥奪する。** 書き込みは**例外なく RPC 経由**。個別に列挙せず、**既定を「無い」にして、必要なものだけ足す** |

### ★`users` の読み取り範囲について（先回りで決めておきます）

**v1 は「自分の行だけ」で構いません。** ただし将来、出走表に**馬主名**を出したくなります。そのとき —

**同じテーブルに「自分だけ」と「みんな」のポリシーを混ぜないでください。** `users_public`（`display_name` / `stable_name` のみ）を**別の公開ビュー**として作ります。**`races_public` / `race_entries_public` と同じ形**で、この案件が既に採っているパターンです。**残高を持つテーブルに公開ポリシーを足すと、列の見落としが即座に残高の露出になります。**

### 適用の順序

1. **staging に当てて、期待される姿を実測**（「公開ビュー以外は anon から0行、かつ書き込み不可」）
2. **本番に当てる前の必須確認**: **anon 経由でこれら7テーブルを読んでいる箇所が Web / ワーカーに無いか**を grep する。ワーカーは service_role、`migrate.mjs` は `DATABASE_URL` 直結なので**どちらも RLS を迂回します**が、**確認してから当ててください**（`app_environment` はガード類が読むので特に）
3. 本番へ。**★出力を切らずに全部残す**（R-27 の系）

---

## 3. 🔴 本番の権限を、HTTP ではなく1クエリで確定してください

**私は本番に PATCH を投げていません**（staging のみ）。本番への探索は最小限に留める、という自分の方針どおりです。

**そして、HTTP で1テーブルずつ叩くのは道具として間違っています。** 読み取り専用で、全部を一度に、確実に取れる方法があります:

```sql
select table_name, grantee, privilege_type
from information_schema.role_table_grants
where table_schema = 'public' and grantee in ('anon','authenticated')
order by table_name, grantee, privilege_type;
```

→ **本番と staging の両方でこれを取り、差分も併せて報告してください。** `tools/verify-anon-exposure.mjs` は READONLY なので、ここに足すのが自然です。**「読めるか」を叩いて確かめる形から、「何が付与されているか」を列挙する形に変えてください** — Q-3 の S-4（走査を全走査＋除外リストに反転）と同じ移行です。

---

## 4. V-20 を新設します

**この穴は、ゲートが無いから開きました。** そして**新しいテーブルを足すたびに再発します**（`0017` の `user_identities` は RLS を書きましたが、それは私が §5 で要求したからで、**構造が要求したのではありません**）。

> **V-20: public スキーマの全テーブルについて、**
> **① anon / authenticated に `insert` / `update` / `delete` が付与されていないこと**
> **② 公開ビューとして明示登録されたもの以外は、anon から0行であること**
> **③ 除外は明示の登録簿でのみ許され、登録簿に無いテーブルが現れたら落ちること**

**★③ が本体です。** 手書きの対象リストは必ず漏れます（`tools/lib/classification.mjs` で既に同じ結論に達しています）。**`information_schema` を全走査し、登録簿と突き合わせる**形にしてください。**新しいテーブルを黙って足せなくなる**のが目的です。

**V-19 #15（`user_identities` が anon から0行）は V-20 に吸収されます** — 個別項目として持つより、全走査の一部であるほうが強いからです。

---

## 5. 本番ビルドの再診断 — **受理します**

**11レース・145出走のすべてで `entrant_snapshot` が NULL。列が存在する状態で生成されたものなので、これは決定的です。**

→ **D-055（出走馬の凍結）は本番未配備。したがって D-056（凍結が無いレースは中止・EP 返還）も働いていない。確定。**

私の独立測定（馬場が散る＝D-052 は配備済み）と併せて、**本番ビルドは「D-052 を含み、D-055 を含まない」範囲**に絞られました。

**診断の取り下げと出し直しは適切でした。** 自分の総括に自分が当たったことを、そのまま書いているのも良いです。

**残る「どのコミットか」は VPS の symlink でしか分かりません** → オーナーへ（§6）。

---

## 6. ボールの並べ直し

| # | やること | 誰 | いつ |
|---|---|---|---|
| **🔴 1** | **`information_schema.role_table_grants` で本番・staging の付与を全列挙**（読み取り専用・1クエリ） | 開発 | **今すぐ** |
| **🔴 2** | **`0018`: 書き込み権限の全剥奪 ＋ `users` の RLS/ポリシー ＋ 6テーブルの revoke。staging → 本番。★認証とは別便で先に** | 開発 | **今すぐ** |
| **3** | **V-20 を走査型メタテストとして実装**（登録簿方式） | 開発 | 2 と同便 |
| 4 | `QUESTIONS_SETUP_RPC_20260820.md` の裁定 | レビュー側 | 次 |
| 5 | 正典への反映（D-076〜078／V-19 16項目／**V-20**／R-25〜R-28） | レビュー側 | 次 |
| 6 | **VPS で `ls -l /opt/star-current`**（稼働コミットの確定）→ 配備の二択 | **オーナー** | 中 |
| **🔴 7** | **本番の service_role キー・DB パスワードのローテーション** | **オーナー** | **高** |

**★7 の優先度が、もう一段上がりました。** `users` が anon で書ける状態だったということは、**この案件の権限設計が「既定で開いている」側に寄っていた**ということです。**service_role キーが平文で置かれていることの意味も、それだけ重くなります。**
