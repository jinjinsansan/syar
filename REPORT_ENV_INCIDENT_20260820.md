# 🔴 事故報告 — 本番に、意図しないマイグレーションを2件適用しました

**発行**: 2026-08-20（開発側）
**対象**: `REVIEW_ENV_MISLABEL_VERDICT_20260820.md` の「1. `secrets.local.env` の是正＋`migrate.mjs` の既定変更」を実施中
**結論**: **私の誤りです。影響は測定できる範囲でゼロですが、それは運が良かっただけで、手順は誤っていました。**

---

## 1. 何をしてしまったか

| 時刻(UTC) | 適用されたもの | 意図 |
|---|---|---|
| 2026-08-20 01:50:59 | `0016_entrant_snapshot.sql` | **無し（意図していません）** |
| 2026-08-20 01:51:14 | `0017_user_identities.sql` | **無し（意図していません）** |

**私がやろうとしていたのは「`STAR_ENV` を `production` に直したので、環境の突合が通るようになったことの確認」だけ**でした。適用対象を消すつもりで `9999`（存在しない番号）を渡していました。

---

## 2. 原因 — ★私がその直前に足した安全策が、この事故を起こしました

裁定に従い `migrate.mjs` に真偽値フラグ `--yes-production` を足しました。ところが同ファイルの位置引数の拾い方はこうでした。

```js
const only = argv.filter((a, i) => !a.startsWith('--') && !argv[i - 1]?.startsWith('--'))[0] ?? null;
```

**「`--` で始まる語の次は、その語の値である」と決め打っています。**

```
migrate.mjs --env production --yes-production 9999
                                              ~~~~ ← 「--yes-production の値」と見なされて消える
```

→ **位置引数なし＝「未適用のものを全部当てる」に化けました。**

### ★壊れ方の向きが最悪でした

**引数が消えて、既定の「広い」動作に落ちる**方向です。狭くなるなら気づきますが、**広くなると黙って余計に効きます。**

### 重ねた失敗 — `head -4` で自分の目を塞ぎました

1回目の実行を `| head -4` で切っていました。**4行目を出した時点でパイプが閉じ、node が SIGPIPE で死にました。** 私が見たのは「未適用 2 件」までで、**その後に `0016` が適用された行は表示されませんでした。** 2回目（`| tail -12`）で初めて `0017 ... OK` が見え、そこで気づきました。

**「未適用 2 件」と出たのに次の実行で「未適用 1 件」だったこと**が、1件目が既に当たっていた証拠です。

### ★総括

**安全策を足す変更の正しさを、その変更自体に依存する方法で確かめました。**
`--env staging 9999` は以前は無害な空振りでしたが、**`--yes-production` を足した瞬間に無害でなくなっていました。** 私はその可能性を検討せずに、同じ手を本番に対して使いました。

**そして前便の裁定はこう書いていました** —

> **★網が受け止めるところだった、というのは「手順が正しかった」ではなく「計画が誤っていた」ということです。**

**今回は、その網（`app_environment` との突合）を私が正しく通した直後に、別の穴から入りました。**

---

## 3. 影響 — 測定できる範囲でゼロ

| 確認 | 結果 |
|---|---|
| `0016` の内容 | **`alter table race_entries add column if not exists entrant_snapshot jsonb;` の1文だけ**。NULL 許容・既存データに触れない・冪等 |
| `0017` の内容 | **新規テーブルの作成のみ**（`user_identities`・RLS 有効・ポリシーなし）。既存テーブルに触れない |
| `user_identities` の行数 | **0** |
| `entrant_snapshot` が入っている出走行 | **0**（＝**本番の稼働コードはこの列を書きません**。列は完全に不活性） |
| ワーカーの稼働 | **健全**。直近30分で3レース生成・最新レースは現在時刻の21分後（先回り生成が継続） |
| レース総数 | 1,670（レビュー側の実測 1,668 から通常どおり増加） |

**どちらも「あっても何も起きない」変更でした。しかしそれは事後に判明したことで、実行前に私が確かめたことではありません。**

---

## 4. 直したこと

| # | |
|---|---|
| 1 | **引数解析を `tools/lib/args.mjs` に切り出し、値を取るフラグを明示的に渡す形に**（`--` で始まるかだけで判断する形をやめた） |
| 2 | **`apps/cli/test/args.test.ts` で固定**（6件）。★事故そのものの形「真偽値フラグの直後の位置引数を食べない」を先頭に置いた |
| 3 | 実地確認: `--env staging 9999` が **「適用対象がありません（指定: 9999）」で止まる**ことを確認（以前は全件当たっていた） |

**923 件 PASS / typecheck クリーン。**

---

## 5. 併せて実施した是正（裁定 1）

| # | |
|---|---|
| 1 | **`.gitignore` を先に直しました** — 旧設定は `secrets.local.env` と `secrets.staging.env` を**名前で個別に列挙**しており、**改名した瞬間に本番の資格情報が追跡対象になるところでした**。`secrets*.env` のパターンを足し、`git check-ignore` で確認してから改名しています。★**名前を変える作業と、名前で守る仕組みは相性が最悪です** |
| 2 | `secrets.local.env` → **`secrets.production.env`**、`STAR_ENV=development` → **`production`** |
| 3 | **`migrate.mjs` の既定を廃止** — `--env` を必須にし、本番には `--yes-production` を追加要求。引数なし・`--env local`・二段目なしのすべてで止まることを実地確認 |
| 4 | `tools/lib/env.mjs` の環境名を `local` → `production` に。`--env local` は**廃止した旨のメッセージを出して落とします**（黙って動かない） |
| 5 | **`package.json` の `worker` を staging に向けました** — 旧設定は `--env-file=secrets.local.env`（＝本番）で、**`STAR_ENV` を直した瞬間に `npm run worker` が本番ワーカーを起動する状態**になっていました（本番は systemd の `/etc/star/worker.env` で動くので、npm スクリプトが本番を向く必要はありません） |
| 6 | `tools/import-staging-secrets.mjs` の参照を更新 |

---

## 6. ★別の発見 — 本番の稼働コードは 08-12 の決定より前のものです

`entrant_snapshot` が **0 行**であることから分かります。

- `0016` は **D-055（2026-08-12）**の出走馬凍結です。**今日まで本番に適用されていませんでした**
- 適用した今も**1行も書かれていない**＝**本番で動いているワーカーは D-055 を含まないビルド**です
- したがって **D-056（凍結が無いレースは開催中止・EP 返還）も本番では効いていません**

**★これは私の事故とは独立した、運用上の食い違いです。** リポジトリの決定が本番に届いていません。**レビュー側で扱いをご判断ください。**

---

## 7. 判断をお願いしたいこと

| # | |
|---|---|
| **1** | **適用してしまった `0016` / `0017` を残すか、戻すか。** 開発側の意見は**残す** — どちらも不活性で、`0016` は本来 08-12 に当たるべきものであり、`drop column` / `drop table` のほうが本番への変更として重いためです。ただし**決めるのはレビュー側**だと考えます |
| **2** | **§6 の食い違い**（本番の稼働コードが D-055/D-056 より前）の扱い |
| **3** | 🔴 **本番の service_role キー・DB パスワードのローテーション**（裁定 2・オーナー手配。優先度は変わらず「待てない」） |
| **4** | **anon 露出の全数確認**（裁定 4）は**まだ着手していません**。staging で期待値を確定させてから本番を1回、の順序で進めます |
