# REPORT_P3_S234_20260812 — 段2〜4 の3点測定と、V-11 の②

## 本番で正典の改訂が要る決定をしたか

**していません。K には一切触れていません。**
ただし**本番へ入れる順序について裁定が要ります**（§3）。

---

## 1. 結果

| | ① 現状 | ② ＋能力を実データに | ③ ＋調子/疲労を配線 | 帯 |
|---|---|---|---|---|
| **V-4** 1番人気の勝率 | 31.29% ✓ | **29.20% ✗** | **32.73% ✓** | 30〜34% |
| **V-5** 1番人気の複勝率 | 62.40% ✓ | **59.78% ✗** | **63.99% ✓** | 60〜65% |
| **V-6** 下位3ランクの勝率 | 1.21% ✓ | 1.09% ✓ | 0.97% ✓ | 0.5〜2% |
| **V-8** AI代行÷手動最適 | 94.57% ✓ | 95.15% ✓ | 93.88% ✓ | 93〜97% |
| **V-13** 仕掛けの巧拙 | 0.1999 ✓ | 0.2000 ✓ | 0.1766 ✓ | 有意差 |
| **総合** | **PASS** | **FAIL** | **PASS** | |

生出力: `docs/MEASURE_P1GATES_S1_CURRENT.txt` / `_S2_REALABILITY.txt` / `_S3_BOTH.txt`

### ★①が文書どおり完全に再現しました

`31.29% / 62.4% / 1.21% / 94.57% / 0.1999` — **Q-P3-32 の是正は P1 のゲートを1つも動かしていません。**
段1で「バグ修正であってリアリズムの向上ではないので P1 の再測定は不要」とされた前提が、
実測で確かめられました（`rng.pick` を飛ばさず**引いてから上書き**したためです）。

### ★2つの是正は V-4 を逆向きに動かし、合わせて帯に入ります

```
  ① 31.29%  →  ② 29.20%（−2.09pt）  →  ③ 32.73%（+3.53pt）
```

**②単独では帯を下回ります。** 「まとめて1回で」という裁定がそのまま効いています。
★片方ずつ入れると、途中で必ず落ちる状態を通ります。

---

## 2. ★②が下がる機構は説明できていません

B-6 のときは「**オッズ側から見えている分散が増えると1番人気は強くなる**」で説明がつきました
（レース内の疲労SD 0 → 8.13 → 19.04 に対し V-4 が単調に上昇）。

②はその説明と**逆向き**です。レース内の能力のばらつきを測ると:

| | レース内の能力ばらつき（変動係数） |
|---|---|
| ① 仮定値（`PLACEHOLDER_UNLOCK` 0.55〜0.85） | 11.55% |
| ② 実データの能力 | **12.82%** |

**分散は増えているのに V-4 は下がりました。** 同じ説明では通りません。

考えられる筋（未検証）:
- 出走馬は**素質**でクラス帯を切って選ばれるが、実データの能力は
  **素質と開放率の積**なので、**素質の順位と能力の順位がずれる**
- `FIELD_STRENGTH_FLOOR`（弱すぎる馬を引き直す仕組み）が能力を見るので、
  実データにすると引き直しの起き方が変わる

★**推測で説明を付けず、数字だけ出します。** 機構の特定が要るなら別途の診断を組みます。

### 開放率の実測（裁定の数字との差）

| | 平均 | SD |
|---|---|---|
| `PLACEHOLDER_UNLOCK`（一様 0.55〜0.85） | 70.0% | 8.7pt |
| **実データ（週ループが育てた値）** | **73.8%** | **11.0pt** |

★裁定にあった「実データは 75.2 / 90.3 / 96.7% と狭くて高い」は **V-14 の引退時**の値です。
**出走馬はキャリア全体に散らばる**ので、こちらの標本は**より低く、より広い**（狭くありません）。
この違いが②の方向にも効いている可能性があります。

---

## 3. ★本番へ入れる順序について裁定をお願いします（Q-P3-35）

ハーネスでは ③ が通りましたが、**本番へそのまま入れると悪化します**。

- ハーネスの「実データの能力」は **`advanceWeek` が実際に育てた値**（開放率 73.8%）
- 本番の `horses.stats` は **誕生時の値**（`potential × 0.28〜0.35`）
  — **週送りが全馬に対してまだ回っていないため**

★いま本番で `PLACEHOLDER_UNLOCK` を廃止すると、出走馬の能力が**誕生時の水準**になります。
これは仮定値（70%）より**大幅に低く**、ハーネスで測った ③ とは別の世界です。

→ **順序として、先に「週送りを全馬に対して回す」ことが要ります。**
   それは G-1〜G-8 をワーカーのサイクルに繋ぐ作業で、P3 の残りの本体です。

**いまの実装状態**（意図的にこうしてあります）:

| | ハーネス | 本番（ワーカー） |
|---|---|---|
| 調子・疲労 | `--b6-wired` | **配線済み**（DB を読む。未処理の馬は §7.4 の中央値） |
| 能力 | `--real-ability` | **未配線**（`PLACEHOLDER_UNLOCK` のまま） |

---

## 4. V-11 の②を実装しました

裁定どおり「**発行量と消費量がともに実質ゼロでない**」を追加しました。
測定条件も「較正は合成集団・監視は実集団」に従っています。

```
## ⑦ ★V-11 の② — 発行量と消費量がともに実質ゼロでないか
  利用者 0 人 / 馬券 0 枚 / PP 台帳 0 行 / 確定済みレース 452 / プレイヤー所有馬 0 頭
  実集団（internal を除く）: 発行 0 PP / 消費 0 PP / 0 行
  ★ ② 発行量と消費量がともに実質ゼロでない  ★成立しない

  → ★V-11 の②が成立しません。①（純発行量ゼロ近傍）は自動的に通りますが、
    それは「健全な経済」ではなく「経済が存在しない」ことの表れです。
```

★**合成ベッターの口座（`account_type='internal'`）は②の判定から除いています。**
除かないと「合成が動いているから経済は生きている」と読めてしまいます。

---

## 5. 実装の要点

### ★開放率も「引いてから捨てる」

`toEntrant` の `rng.range(unlockRange...)` を**飛ばしていません**。
飛ばすと乱数の並びがずれ、脚質も枠順も出走馬も変わります。
実データを渡さない経路（①）を**1ビットも動かさない**ためです。
実測でも①が文書と完全一致しました。

### 配る物（バンドル）の確認

Q-P3-32 の是正後に再ビルドし、**原本とバンドルの自己検査がバイト単位で一致**することを確認しました。

---

## 6. 照会

| # | 内容 |
|---|---|
| **Q-P3-35（新規）** | **本番へ入れる順序**（§3）。`PLACEHOLDER_UNLOCK` の廃止は、週送りが全馬に回った後でないと悪化します |
| **Q-P3-36（新規）** | **②が V-4 を下げる機構**（§2）。B-6 の説明と逆向きです。診断を組むべきか |
| Q-P3-33 / 34 | 馬場の割合は何本で判定するか / V-10 本番経路の測り方 |
| Q-P3-27〜31 | 未回答 |
| Q-P3-20〜26 | 未回答 |

---

## 7. 再現コマンド

```bash
npm run verify:race                                   # ①
npm run verify:race -- --real-ability                 # ②
npm run verify:race -- --real-ability --b6-wired      # ③
npx tsx tools/diag-v11.mjs                            # V-11 の①②
npm run build:worker && node dist/worker.cjs --selfcheck
```

---

## 8. ★オーナーへ（再掲）

**staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん）。
