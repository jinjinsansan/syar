# 【STAR】裁定: 簿を公開の枝に置く扱い ／ 絵柄の食い違い（アートバイブル・プロンプト・実物）— 2026-09-23

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa` の 2 件（`a1780f4`・`4977d47`）と照会
- 運用: オーナー指示「承認不要で自立的に進める」。★絵柄そのものは [EYES]＝オーナーとデザイナー

## 1. `private/` への切り分け — ✅

- ✔ `.gitignore` に `private/` を足し、本番の設定の現在値を `private/REPORT_PROD_DEPLOY_20260923_details.md`（追跡外）へ。公開側は「現在値は `private/` を見る」に書き換え。履歴は書き換えていない → ★§4 の運用どおり
- ⚠️ ★`private/` は**バックアップされない**（追跡外・push されない）。★PC が壊れると消える。★非公開のリポジトリを作るかどうかは、オーナーの判断（§4 の 2）

## 2. 簿（`open-findings.mjs`）の扱い — ✅ いまは動かさない。★規則だけ足す

- 開発側の判断（★独断で触らなかった）は正しい。★簿は門の検査（`why`・`owner`・`until` の有無）と結び付いており、中身を消すと**検査の意味が変わる**
- ✔ いま載っている 48 件を見たかぎり、**外から攻める手順になるものは無い**（大半は作りの負債と、直すと決めたが未着手のもの）
- **決定**:
  1. ★**簿そのものは公開のままでよい**（中身を削らない）
  2. ★**これから載せるときの規則を、簿の冒頭に 1 行足す** — 「★**外から攻められる弱点は、ここに手順を書かない**。★`why` は『どこが開いているか』までにし、★再現の手順・関門の外し方・本番の設定の現在値は `private/` に置き、★簿からはファイル名で指す」
  3. ★既に載っているものを後から見直す必要が出たら、そのときに 1 件ずつ

## 3. 🔴 絵柄 — 文書どうしが食い違っている

✔ [CODE]（開発側が実物で確かめ、レビュー側も文書を引いた）:
| 出どころ | 何と書いてあるか |
|---|---|
| `design/art/art-bible.md:4`・`:15`・`:154`・`:203` | **ピクセルアート**。整数倍のみ |
| `design/art/prompts/side-v8.txt` | **photographic realism**（写実） |
| 実物 `apps/web/public/art/horse-jockey-side-v8-pose01.png` ／ デザイナーの `art/chibi-horse.png` | **セル調のカートゥーン**（太い輪郭） |

→ ★**3 つが三様**。★正典（アートバイブル）は、いま出ている絵と合っていない。

**決定**:
1. ★**この便では、誕生カットを「実物に合わせる」でよい**（開発側の直しを採る）。★理由をプロンプトに書いてあるのも正しい。★**絵が揃って見えることが先**
2. ★**アートバイブルを、開発側が黙って書き換えない。** ★絵柄は作品の根幹で、オーナーとデザイナーの判断（[EYES]）
3. ★**オーナーへ問いを出す**（§5）。★答えが出たら、**レビュー側がアートバイブルを直す**（決定の記録として）
4. ★大きさ: 表示の箱 360×270 とその 2 倍 720×540 は、**整数倍の作法を満たす**。★ただし箱の寸法は開発側の仮置きなので、★**デザイナーの版が来たら合わせ直す**（開発側もそう書いている）

## 4. 毛色 — ⚠️ 私の訂正が半分だけ正しかった

- ✔ アートバイブル `:56`・`:126`・`:157`・`:202`: ★**毛色 5 種を持たせる**（馬 ID から決定的に導く・パレット差し替え）と**決めてある**
- ✔ いまの DB とコード: 毛色の属性は**無い**（レースの映像で枠番から色を決めているだけ）
- → ★**「毛色のデータが無い」は今の実装の話としては正しいが、「持たせると決めてある」ことを落としていた**（★レビュー側の §2 の訂正が半分だけ）
- **決定**: デザイナーへの回答を次の形に直す — 「★**いまは毛色のデータがありません。** ★アートバイブルでは将来 5 種を持たせると決めていますが、**時期は決まっていません**。★**この便の画面では毛色を使わないでください**。★誕生カットは 1 枚とし、後から色を変えられる作りにしてもらえると、毛色を入れた日に使えます」
- ★「将来入る」と約束しない（時期が決まっていないため）

## 5. オーナーへの問い（絵柄）

> 馬物語の絵柄について、3 つの文書が食い違っています。
> - アートバイブル（美術の正典）は「**ピクセルアート**」と書いています
> - 実際に本番へ入っている馬の絵と、デザイナーの第 1 便の絵は、**太い輪郭のセル調（アニメ調）**です
> - 馬の絵を作ったときのプロンプトには「**写実**」と書いてあります
>
> **いまの絵（セル調）を正とし、アートバイブルをそれに合わせて直してよいですか。** それとも、ピクセルアートに戻す方針でしょうか。
> 直す場合は、こちらでアートバイブルを書き換えて記録します。誕生カットは、答えが出るまで「いまの絵に合わせる」で進めます。
