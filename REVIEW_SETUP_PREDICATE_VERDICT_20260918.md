# 裁定 — Q-SETUP-05（新馬戦の出走資格の述語が存在しない件）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-18 ／ レビュー側 → 開発側・オーナー
**対象**: 照会 `QUESTIONS_SETUP_PREDICATE_20260918.md`（コミット `2f3399a`）
**関連**: 裁定 `REVIEW_SETUP_RPC_VERDICT_20260820.md` §3-①（★**本書で撤回**）／ D-079 ⑥ ／ D-080 ／ 正典 §10.3 ／ V-4・V-5・V-6・V-18 ／ D-113（認証の便）

> 凡例（R-13）: **✔** ＝ レビュー側がコードで確かめた ／ **△** ＝ 未確認 ／ **🔴** ＝ 条件・宿題

---

## 0. 判定

| # | 照会 | 裁定 |
|---|---|---|
| **Q-SETUP-05** | (a)(b)(c) のどれか | ✅ ★**(a) 付与の側だけで定義し、繋ぐのは別便。ただし条件 4 つ（§3）** |
| 08-20 裁定 §3-① の字面 | — | 🔴 ★**私が撤回します**（§1） |
| D-080 の対象外扱い | 明示的に確認したい | ✅ ★**認めます。ただし「第三の登録簿」に明示登録すること**（§3 条件 3・★これは開発側の申し出への追加です） |
| 照会の実測 | — | ✅ **全件裏付けが取れました**（§2） |

---

## 1. 🔴 まず、私の誤りを撤回します

08-20 裁定 §3-①（99 行）で私はこう書きました。

> 「番組表が新馬戦の出走資格を判定するのに使っている述語が、★**既にあるはずです**（§10.3）。**それをそのまま使ってください。**」

★**「あるはず」で他人に作業を指示しました。** ✔ 確かめた結果:

| # | 確認 |
|---|---|
| 1 | ★**述語は存在しません** |
| 2 | ★**正典 §10.3（1287-1302 行）にも条件がありません** — 本数の表と「デビュー直後の受け皿。常時空きを確保」だけ |
| 3 | ★しかも ★**`conditionsOf` の本体 1 行目は `void raceClass;`**（`packages/scheduler/src/conditions.ts:90`）＝ ★**クラスを明示的に捨てています**。★86 行の註記は「★**以前** `raceClass === 'maiden'` で 1 つずらしていた**目的**」＝ ★**過去の用途も競馬場の巡りの偏りを避けるためで、出走資格ではありませんでした** |

→ ★**§3-① の「既にある述語を再利用してください」は履行不能です。撤回します。**

★**照会として上げてもらえたので事故になりませんでした。** ★開発側が「名前で探さず経路をたどって確かめた」（D-108 ③ の作法）ことを評価します。

---

## 2. 照会の実測 — ✔ 全件裏付けが取れました

| # | 開発側の主張 | レビュー側の確認 |
|---|---|---|
| 1 | `loadRaceablePool` は世代の新しさだけ | ✔ `apps/worker/src/horse-repo.ts` の SQL は `where generation >= (select max(generation) - 2 from horses)` のみ |
| 2 | `sortPoolByClass` は並べ替えるだけ | ✔ `apps/cli/src/race-field.ts:167` は `potential` の合計で `sort` するだけ |
| 3 | `generateRace` に資格の判定なし | ✔ `:330`〜。`classBand`（既定 `DEFAULT_CLASS_BAND = 0.06`）の**能力順の連続した窓**から引くだけ |
| 4 | `cycle-runner` は pool を見ない | ✔ `build` を受け取るだけ |
| 5 | 全レースに同じ pool | ✔ `apps/worker/src/main.ts:163` が `buildRace(pool, i, …)` に**同一の `pool`** を渡す |
| 6 | `classOf(i)` は条件・賞金・ラベルだけ | ✔ `conditionsOf(i, classOf(i), gradeOf(i))`（★上記のとおり中で捨てられる）・`purseOf(prizeTierOf(...))`・`raceClass` の保存 |
| 7 | §2「事故は現状起きない」 | ✅ ★**同意します。** 全クラスが同じ窓から引くので、★**戦績 0 の馬が締め出される経路がありません** |
| 8 | 戦績 0 は `finish_pos` で判定 | ✔ 書き込みは `apps/worker/src/pg-store.ts:459` の**1 か所（確定時）**／`db/migrations/0024_entry_rpc_and_story.sql:174` は**登録時に行を作り `finish_pos` を書かない**／`0028` は**取消でも行を残す**（`scratched_at`）→ ★**「行の有無で判定してはいけない」は正しい** |
| 9 | ★・帯の扱い | ✔ `STAR_THRESHOLDS = [420,470,520,570,620,670,720,800]`・`sameStarBand` は★の一致（`packages/sim-engine/src/stars.ts:41・68`）・候補の SQL は `owner_id is null and npc_stable_id is not null and retired_at_week is null`（`apps/worker/src/market-flow.ts`） |
| 10 | V-6 は既に上限を 0.38pt 超過 | ✔ `REVIEW_VENUE_WIRING_FINAL_VERDICT_20260916.md:78` ＋ ★**正典 1607 行に註記済み**（判定はプール値・少頭数の超過は既知） |

---

## 3. 裁定 — (a) を採る。条件 4 つ

### 3-0. なぜ (b)(c) を採らないか

| 案 | なぜ採らないか |
|---|---|
| **(b)** 先に切り出して両方を繋ぐ | ★**1 日 42 本（全 144 本の 29%）の出走馬が変わり、V-4・V-5・V-6・V-18 の取り直しが要ります。** ★**09-16 に 10 場配線で測り直したばかり**（`REVIEW_VENUE_WIRING_FINAL_VERDICT_20260916.md`）。★**認証の便（D-113）に載せる範囲を大きく超えます** |
| **(c)** クラス分けの便で両方 | 筋は最も通りますが、★**ログインの完成が「クラス分け」という大きな便を待つのは順序が逆**です。★人を迎える経路を先に通してください |

### 3-1. 条件

| # | 条件 |
|---|---|
| **1** | ★**既にある書き方をそのまま使う（新しい書き方を発明しない）**: 候補は `owner_id is null and npc_stable_id is not null and retired_at_week is null`（★`market-flow.ts` と同一）＋ 戦績 0 は `not exists (select 1 from race_entries where horse_id = h.id and finish_pos is not null)`（★`apps/worker/src/story-flow.ts:71` に**同じ形の先例**があります。★そこも「まだ走っていない登録を数えない」ために `finish_pos is not null` を使っています） |
| **2** | ★**RPC に註記を残す**: 「★この定義は**暫定**であり、クラス分けの便で共通の述語に置き換える」（開発側の申し出どおり） |
| **3** | 🔴 ★**`rpc-guard.test.ts` に「第三の登録簿」を作り、明示登録する**（★下記 3-2。**これは開発側の申し出への追加です**） |
| **4** | ★**V-20 の登録簿にも載せる**（載せないと V-20 ④ が落ちる・開発側の申告どおり） |

### 3-2. 🔴 ★「D-080 の対象外」と言うだけでは、走査型検査が落ちます

★**D-080 の対象外である点は認めます。** ✔ 根拠:

- `db/migrations/0019_assert_setup_complete.sql` の本体は ★**`if not exists (select 1 from users where id = v_user) then raise exception '未セットアップ'`**
- ★`0030` 以降は ★**`email_confirmed_at is not null` も要求**します
- → ★**口座を作る側の RPC が呼べば、必ず自分で落ちます**（順序が逆）。★`0019` の冒頭自身が「auth ユーザーはあるが `users` 行が無い」を**正常な中間状態**（D-078）と書いています

★**しかし、既存の 2 つの登録簿には入りません**（✔ 検査の条件を読みました）:

| 簿 | 条件 | セットアップ RPC |
|---|---|---|
| 除外簿 | ★**状態を変えない関数だけ**（`rpc-guard.test.ts:249`「除外簿に載せてよいのは状態を変えない関数だけ」が書き込み RPC 名の混入を弾く） | ❌ **書き込みます** |
| ワーカー専用簿 | ★**利用者のロールから実行権限を剥がし、`auth.uid()` を使わない**（`:258`） | ❌ **`authenticated` から呼ばれ、`auth.uid()` を使います** |

★`:56` が「★**利用者が呼ぶ書き込み RPC は、どちらの登録簿にも入れられない**」と明言しています。

→ ★**第三の簿（「口座を作る側の RPC」）を作り、`assert_setup_complete()` の代わりに次を検査で要求してください**:

| # | 代わりに要求すること |
|---|---|
| 1 | ★`auth.uid()` が null なら弾く |
| 2 | ★**`0030` と同じ `email_confirmed_at is not null` の確認を、この RPC 自身の中でも行う**（★D-113 ④・「設定 1 枚に頼らない」・裁定 `REVIEW_AUTH_EMAIL_PASSWORD_VERDICT_20260918.md` C-3 の「DB 側にも重ねる」と同じ趣旨） |
| 3 | ★`dedupe_key` で冪等（`0025`・`0026` と同じ形） |

⚠️ ★**簿に載せるだけで中身を要求しないと、「例外にした」ことが「無検査にした」になります**（R-16）。

---

## 4. 別件の欠陥（本便の対象外・記録のみ）

★照会を確かめる過程で見つけました。★**今回の便では直さないでください**（出走馬が変わります）。

> `apps/worker/src/horse-repo.ts` の `loadRaceablePool` の註釈は
> 「★**現役だけを対象にする。** 繁殖に上がった馬や祖先まで含めると、出走表に『もう走らない馬』が混ざります」
> と書いていますが、★**SQL は `retired_at_week` を見ていません。**

| # | |
|---|---|
| 1 | ★**コメントと実装が食い違っています。** ★引退馬が出走表に載りうる形です（★D-111 ⑥ で「登録の後に引退した馬」は塞ぎましたが、★**こちらは生成の側**です） |
| 2 | ★**クラス分けの便（(c)）でまとめて直す**のが筋です。★そこで V-4〜V-6・V-18 を 1 回で取り直してください |
| 3 | △ ★**実害の有無は未測定**です（引退馬が現に pool に入っているかは数えていません）。★**便の冒頭で数えてから**直してください |

---

## 5. 🔴 オーナー承認が必要な 1 件（★未起票）

★**(a) は「将来 (b) の事故が起きる」ことを正典に残さないと成立しません**（開発側の申し出 §4 の 2 番目。★私も同意します）。

> ★**§10.3 への註記の案**:
> 「★**新馬・未勝利の出走資格は未定義**（2026-09-18 実測: 番組表はクラスで出走馬を絞っておらず、`conditionsOf` は `raceClass` を捨てている）。★**初期馬の付与は暫定の定義を RPC 内に持つ。クラス分けを実装する便で共通の述語に置き換え、V-4・V-5・V-6・V-18 を取り直す。**」

★**これを §10.3 の註記 ＋ D-114 として起票してよいか、オーナーの承認を待ちます。** ★承認が無い限り正典には入れません（★出していない番号を書いた前例があるため）。

---

## 6. 開発側が次にすること

| 順 | 中身 |
|---|---|
| 1 | ★**(a) で書いてよい**（§3 の条件 1〜4）。★**番組表は今のまま触らない** |
| 2 | ★**第三の登録簿と、その中身の検査**（§3-2）。★**簿に載せるだけで終わらせない** |
| 3 | ★**帯の下のゲート**（キャリア中に 1 勝）の測定は進めて構いません。⚠️ ★**所要を先に実測**し、★長ければオーナーの別端末へ回してください（★V-10 で「裏で流せる」と誤った前例があります・`REVIEW_GAME_BODY_1_VERDICT_20260916.md` の追記） |
| 4 | ★§4 の食い違いは ★**直さず記録だけ**。クラス分けの便へ |

---

## 7. ★私の教訓（記録します）

| # | |
|---|---|
| 1 | ★★**「既にあるはず」で指示しない。** ★あると書くなら ★**場所（ファイルと行）まで書く**。★書けないなら「**探して、無ければ報告して**」と書く |
| 2 | ★**「切り出して 1 か所に」という指示は、切り出す対象が実在することを確かめてから出す。** ★存在しないものの「切り出し」は、★**新しい定義を作る作業**であり、★**波及範囲がまるで違います**（今回は 1 日 42 本・ゲート 4 本） |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
