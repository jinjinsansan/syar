# 【STAR】裁定: 仔の命名 I-3 の実装前の論点 — 2026-09-22

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_I3_NAMING_20260922.md`（開発側 `star-aa`・`687c0aa`）
- 運用: オーナー指示「承認不要で自立的に進める」。★Q-4 はオーナー判断に当たるが、**戻せる**のでレビュー側の暫定決定で返す（オーナーには並行で知らせる）

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| Q-1 | NPC の仔も `generateHorseName` で名付ける | ✅ **採る**。🔴 条件: **名前の乱数を、遺伝の乱数と別の流れにする**（§1） |
| Q-2 | 禁止名の判定の置き場所 | ⚠️ **半分採る**。判定の本体（純関数）は `sim-engine` でよい。🔴 **`hashName`（`node:crypto`）は `sim-engine` に入れない**（§2） |
| Q-3 | `name_key` を埋める 3 段 | ⚠️ **採る。ただし「段 0」を足す** — ★先に**書く側を全部**揃えてから埋める（§3） |
| Q-4 | 文字数・文字の種類 | ✅ **暫定で決める**: カタカナ・長音「ー」・中黒「・」、表示の文字数で **2〜9 文字**。★**正規化後に 2 文字未満になる名前は弾く**（§4） |

---

## 1. Q-1 — ✅ NPC の仔も `generateHorseName`。名前の乱数は別の流れ

✔ [CODE]: `generateHorseName(rng, shape, taken, blocked)`（`packages/sim-engine/src/naming.ts:118`）は `rng` から音節を引く。NPC の仔の名前は今 `${stable.prefix}${foalId.slice(0, 6)}`（`breeding-runner.ts`）。

- 採る理由: 全頭の `name_key` 一意（前の裁定 `ff7028c` §2）と両立する唯一の形。世界の生成（`preseed.ts`）と**同じ関数で名付ける**ので、出どころが 1 つになる（D-052）
- 🔴 **条件 1: 名前の乱数を、`breed()` の乱数と別の流れにする。**
  - `breed()` に渡す種と同じ `Rng` から名前を引くと、**引いた数だけ後の遺伝の乱数がずれる**（もしくは逆に、遺伝の後に引けば遺伝は変わらないが、実装の順番に依存する）
  - ★**名前の種は、仔の種の鍵に別の接尾辞を付けて作る**（例: NPC は `'父|母|週' + '|name'`、プレイヤーは `seed_key + '|name'`）。★1 本の `Rng` を共有しない
  - 試験: ★**名前を付ける前と後で、`breed()` の出力（名前以外の全形質）が 1 ビットも変わらない**こと。固定の種で比べる（指示書の受入条件 7。比較の対象から外すのは**名前の列だけ**と明記する — 照会の案どおり）
- **条件 2**: `taken`（使用済みの正規化名）は、`horses` の全頭の `name_key` から作る。★命名前の下書きには名前が無いので入れなくてよい。★**同じ周に処理する要求どうし**（NPC の仔と、プレイヤーの命名の要求）も `taken` を共有すること
- **条件 3**: 一意違反の 1 頭だけを引き直す（セーブポイント）のは採る。★ただし `generateHorseName` 自体が上限回数で投げる（`:131`）。★**投げたら週ごと戻る**ので、NPC の配合が止まる。→ 投げた回数を数えて警報を出す（黙って週を止めない）。★音節表の広さからすると当面は起きない見込みだが、**未測定**
- ⚠️ ハッシュ表が無い間は、NPC の仔も `name_checked_with = null`（世界の生成の `nameCheckSkipped: true` と同じ扱い）。前の裁定 §4 の「リスト到着後に null の行を全部検査し直す」に**NPC の仔も入る**

## 2. Q-2 — ⚠️ 判定は `sim-engine`、ハッシュは入れない

✔ [CODE]:
- `packages/sim-engine/package.json` は `"dependencies": {}`。★依存を持たない純粋な計算の包みで、ワーカー・CLI・画面（将来はアプリ・§15.4）が共有する
- `hashName` は `node:crypto` の `createHash('sha256')` を使う（`apps/cli/src/name-blocklist.ts:26`・`:41-42`）。同じ式が `:87` にもう 1 か所ある（★**同じファイルの中で既に 2 か所**）

→ `hashName` を `sim-engine` に移すと、**`sim-engine` が Node に依存する**。画面やアプリから読み込んだ時点で壊れる。

**決定**:
- 判定の本体（ハッシュ集合を受け取り、正規化した名前を判定する形）は `sim-engine` の `NameBlocklist` 型のままでよい
- ★`hashName` の置き場所は**サーバー側だけが読む場所に 1 つ**（既存の Node 専用の共有の包みがあればそこ、無ければ新しく作る。`apps/cli` と `apps/worker` の両方が読む）。★**`:87` の 2 か所目も同じ関数を呼ぶ形にする**
- ★試験: `sim-engine` の中に `node:` で始まる読み込みが無いこと（構文で見る。既にあれば、それに `naming.ts` が入っていることを確かめる）
- ハッシュ表をワーカーの配備物に含める方法は、表ができた時点で決める（照会どおり）

## 3. Q-3 — ⚠️ 3 段の前に「段 0: 書く側を揃える」

✔ [CODE]: `insert into horses` を持つ製品・道具は、自分で走査して**4 か所**（試験を除く）:
- `apps/worker/src/breeding-runner.ts`（NPC の仔）
- `apps/worker/src/player-breeding.ts`（★命名の確定で入る予定）
- `tools/seed-world.mjs`（世界の作り直し）
- `tools/verify-a6.mjs`（道具）

照会の 3 段（列を足す → 埋めて衝突を数える → 一意と `not null` を張る）だけだと、**段 2 と段 3 の間に書かれた行が `name_key = null` のまま残る**。
- ワーカーが動いていれば NPC の仔が生まれる
- ★**NG 名リストが届いたら世界を作り直す（④′）予定**で、`seed-world.mjs` が `name_key` を書かなければ**全頭 null** になる
→ 段 3 の `not null` が落ちるか、落ちないように条件を緩めることになる。★**D-119（読む側だけ在って、書く側が無い）の形そのもの**

**決定: 段 0 を足す。**
- 段 0: **上の 4 か所すべてが `name_key`（と `name_checked_with`）を書く**ようにする。★一覧は私が渡したものを使わず、**自分で走査し直して数えること**（R-29′）
- その後で段 1〜3。段 2 の道具は、衝突に加えて**「埋めた後に null の行が 0 か」**も数えて止まる
- ★段 3 の移行は、実行の直前に null の行を数え、0 でなければ投げる（`--expect` の形）
- 本番は読むだけの下見まで。書くのはオーナーの許可（照会どおり）。本番の順序（血統修復 → `0060` → 配備）の後ろに積む

## 4. Q-4 — ✅ 暫定で決める

正典に規定が無いことは、私も `STAR_SPEC_v2.0.md` を「文字数」「命名」で引いて確かめた（§6.1-10 の「ユーザー命名待ち」だけ）。★遊びと権利に関わるのでオーナー判断に当たるが、**戻せる**（受付の判定を変えるだけ）ので暫定で決める。

**暫定の決定**:
1. **使える文字**: カタカナ・長音「ー」・中黒「・」だけ。★NPC の名前（`NAME_SYLLABLES`＝カタカナの音節）と同じ種類にそろえる。英字・漢字・ひらがな・数字・絵文字は使わない
2. **文字数**: NFKC で正規化した**表示の文字列で 2〜9 文字**
3. ★**`normalizeName()` の後で 2 文字未満になる名前は弾く**（「ーー」「・ア」など。正規化で消える記号だけの名前が、重複の判定をすり抜けるのを防ぐ）
4. ★**半角カナも受け付ける**が、保存するのは NFKC 後の全角（表示と `name_key` の出どころをそろえる）
5. 画面は即時に判定してよいが、**ワーカーが必ず同じ関数で判定し直す**（前の裁定 `ff7028c` §2 条件 3）。★判定の関数は 1 か所（`sim-engine`）に置き、画面とワーカーで共有する

**正典への記入**: レビュー側が D-120 に「命名の規則（暫定）」として追記する（この裁定と同じコミット）。

**持ち越し（今回は決めない）**: ★実在馬名の NG リストとは別に、**不快な語・人名・商標**を弾く仕組みが無い。カタカナに限っても作れてしまう。★公開の前に要る。★利用者が名付ける機能が本番に出るまでに、弁護士ゲート（L-7）と合わせて決める。簿（`open-findings.mjs`）に 1 行起票すること。

## [CODE] と [EYES]

§1〜§3 は [CODE]。§4 の文字数・文字の種類の手触りは [EYES] ＝ オーナー（暫定決定）。

---

## 5. 追記（2026-09-22）— 段 0・段 1（`a42628b`）の照合

| 項目 | [CODE] で確かめたこと | 判定 |
|---|---|---|
| 書き手の走査 | 開発側が自分で走査して 3 か所（`breeding-runner`・`seed-world`・`verify-a6`）。私の一覧の `player-breeding.ts` は、`insert into horses` が註記の中にあるだけで、`horses` にはまだ書いていない（下書きの表に書く）。★**開発側の数が正しい**（R-29′ が働いた） | ✅ |
| 名前の乱数 | 名前の種は `idAndSeedFromKey('父|母|週|name')` から `new Rng(nameSeed)` を作る（`breeding-runner.ts:641-643`）。遺伝の種とは別の流れ。試験は「名前を全部使用済みにして違う名前を引かせ、名前以外の全列が一致する」形 ＝ **止める側で試している**（AU-31） | ✅ |
| `hashName` の置き場所 | `apps/cli/src/name-blocklist.ts` に置いたまま。ワーカーは既に `apps/cli/src` を読んでいる（`build-race.ts:13`・`entry-freeze.ts:27`・`main.ts:28-29`）。`packages/sim-engine/src` に `node:` の読み込みは 0 件。★**§2 の意図（`sim-engine` を Node に依存させない・出どころを 1 つに）を満たす**。新しい包みを作らなかったのは正しい（前例に合わせた） | ✅ |
| 簿 | `PLAYER-NAME-OFFENSIVE-WORDS-UNCHECKED`（owner: review）。★レビュー側が持つ | ✅ |

### ⚠️ 1 点だけ直す — 名前が決まらないとき「その 1 頭を見送る」

✔ [CODE] `breeding-runner.ts:636-648`: `generateHorseName` が上限回数で投げたら `continue` して、**その仔を産ませない**（警報だけ出す）。

- 母の印を取る前に `continue` するので、母は「今年まだ産んでいない」まま残る。しかし NPC は各母を**年 1 回、番の週に**配合する（N-1 報告 §2）。★**その母はその年の仔を失う**。世界の供給が静かに 1 頭減る
- 警報だけでは足りない。★同じファイルの註記自身が「3 日・6,066 レースが『警報は読まれない』を証明しました」と書いている（`runBreedingWeek` の fail-closed の註記）
- 同じ種で引き直すので、次の週に同じ母を回しても**同じ結果**になる
- **決定**: ①`generateHorseName` が投げたら、**旧来の形（接頭辞 ＋ ID の先頭 6 文字）を予備の名前として試す**（`taken` と禁止名で判定する。重ならなければそれで産ませる）②それも重なったときだけ見送る ③★`nameGaveUp` を週の結果に載せ、**既存の「止まったら投げます」の判定と同じ場所で数える**（見送りが続くなら週を止める側に倒す）
- ★予備の名前は音の名前ではないので見栄えは落ちる。ただ、起きるのは音節表が尽きかけたときだけで、そのときはどのみち音節表を広げる必要がある。★予備が使われた回数も数えて出すこと（音節表を広げる合図）

## 6. 追記（2026-09-22）— §5 への対応（`a0d5f0d`）✅

- 予備の名前（接頭辞 ＋ ID の 6 文字）で産ませる。予備を使った回数は `nameFallback` で数えて警報を出す。予備も使えずに見送った仔が 1 頭でも居れば、週ごと投げる（`breeding-runner.ts:596-667`・`:747`）→ ✅ §5 の ①②③ を満たす。★投げるのは、既存の「止まったら投げます」と同じ fail-closed の側
- ✅ **開発側が見つけた順序の欠陥**: 全頭を見送ると「供給が止まっています」の判定が先に投げ、原因が名前だと読めなかった。名前の判定を先に置いた → ★**警報の文が原因を指すこと**まで確かめている（09-20 の「失敗の文が、正しい数を教えていた」と同じ種類の注意）

## 7. 追記（2026-09-22）— 段 2（`ecd6f0d`・`tools/backfill-name-key.mjs`）✅

- ✔ 既定は下見。書く前の数え（`blocksBackfill`）は読み取り専用の取引で行う。書くのは 1 取引で、`name_key is distinct from` の行だけ（2 回目以降は 0 行＝冪等。staging で 2 回・3 回目が 0 行）。書いた後は取引の中で数え直し、空と食い違いが 0 でなければ戻す。確定後にもう一度読む
- ✔ 本番の関門は 3 つ（`--yes-production`・`--backfill-name-key`・`--expect-null`）で、`migrate-guard` の表にそろえた
- ✔ staging に重なりが無く番人が発火しないので、数える部分を純関数に出して「重なりで止まる」を試した → **止める側で試している**（AU-31）
- ⚠️ 小さな推奨（阻まない）: 取引の中の数え直しは「空」と「食い違い」だけを見ている。★**重なり（正規化後の重複）も同じ数え直しで見る**。下見と書き込みの間に NPC の仔が生まれても、仔の `taken` は `name` から作るので今の形では重ならない。ただ、それは別のファイルの実装に頼った安全なので、この道具の側でも閉じておく
- 本番は下見も未実行。★段 3（unique・not null）の前に、本番の下見（読むだけ）をオーナーに 1 コマンドで渡すこと

## 8. 追記（2026-09-22）— 命名の受付と確定（`ead29c4`）✅

- ✔ `confirmFoalName`（`apps/worker/src/player-naming.ts`）: 要求 → 下書きの順にロックし（`for update skip locked` → `for update`）、本人の下書きか・命名済みでないか・形・禁止名・重複を判定してから、`horses` に 1 行入れる（ID は下書きの ID のまま）。★下書きの `record` の値をそのまま入れるので、遺伝の値に手が入らない
- ✔ 重複の判定（`nameTaken`・`:71-79`）は `name_key` で引いたうえで、★**段 2 の埋め込みが済んでいない DB（`name_key is null` の行が在る）では、名前を TS で正規化して比べる**。正規化を SQL に写していない（D-052）。★いまの本番のように埋め込みの前の DB で動いても、重複をすり抜けない
- ✔ 命名の判定で落ちたら `failed`（理由つき）にする判断は正しい。★命名は仔の中身を変えないので、落として要求し直させても**引き直しの得が無い**。前の裁定（`322d603` §3）で「命名の要求が増えたら `internal_error` の前提を見直す」と書いた点も、同じ理由で問題にならない
- ✔ 実演 29/29（本物の DB で、命名 → 本人の `my_horses` に現れる／命名の後も NPC が同じ母に産ませない）
- ✔ §7 の推奨（取引の中で重なりも数え直す）を入れた

★本番の下見 2 本（`name_key` の埋め込みと、N-1 の候補数）は、開発側からオーナーに渡る。段 3（unique・not null）の移行は、その数を見てから書く（照会どおり）。

## 9. 追記（2026-09-22）— 段 3（`0bad015`）✅ と、セーブポイントの件への判断

### 照合 ✅
- ✔ `0066`: 実行の直前に `name_key is null` を数え、1 行でもあれば `raise` → `not null` → `unique index horses_name_key_unique`。★§3 の「段 3 の移行は、実行の直前に null の行を数え、0 でなければ投げる」を満たす
- ✔ 本番の下見（読むだけ）: 7,370 頭・正規化で重なる組 0・空 0 → ★**本番でも段 3 を張れる**。N-1 の候補数は staging と一致（母 1,897 頭・残り 8,907 頭ぶん・種牡馬の余り 4,000 回）→ ★裁定 `f613878` §6 の条件（本番の数を測ってから）を満たした
- ✔ 書き手の側の列の検出（`loadFoalNaming`・`breeding-runner.ts:336`）は**週ごとに**呼ばれる（`:595`）。★本番で `0064` を当てた後、ワーカーを再起動しなくても次の週から `name_key` を書く。★書かなかった場合も `0066` の直前の数えが止める（黙って通らない）
- ✔ 確かめる途中の交絡を 2 回外して測り直した（報告どおり）

### 🟡 セーブポイントで 1 頭だけ引き直す件 — ✅ 入れなくてよい
開発側の判断を採る。理由:
- ワーカーは 1 本で、`taken`（使用済みの名前）を**同じ取引の中で DB から**作っている。★一意違反が起きるのは、同じ瞬間に**別の書き手**（道具・2 本目のワーカー）が同じ名前を書いたときだけ
- 違反したら取引ごと戻り、**次の周（約 60 秒後）**にやり直す。やり直しでは、衝突した名前を DB から読み直して `taken` に入るので、`generateHorseName` はそれを避けて次の名前を引く → ★**同じ衝突は繰り返さない**
- ★遅れるのは NPC の週の処理 1 回ぶん（約 60 秒）で、ゲームの 1 週（4 時間）ではない。命名の要求は要求単位で戻り、やり直しでは `name_taken` として理由つきで落ちる
- ★条件: 一意違反で戻った回数を数えて警報に出す。★続くなら、既存の「配合の遅れが縮まない」判定（`breedingStalled`）と、命名の `attempts`（5 回で `internal_error`）が拾う。★**新しい仕組みは足さない**
- ★私の裁定 §1 条件 3 の「セーブポイント」は、**違反が週ごと 4 時間遅らせる**と見込んで書いたもの。実際は 60 秒なので、★**条件を取り下げる**

### 本番の順序（記録）
本番の血統修復 → `0060` → 配備 → `0061`〜`0065` → `tools/backfill-name-key.mjs --apply`（関門 3 つ・オーナーの許可）→ `0066`。★どれもオーナーの許可で動く

## 10. 追記（2026-09-22）— §9 の条件（`6668f0c`）✅・I-3 の完了報告（`6d0857e`）✅・次の順番

- ✔ `isNameKeyConflict` は SQLSTATE `23505` **かつ**制約 `horses_name_key_unique` のときだけ真。★対照（母・週の一意違反、別の例外）では数えないことを検査している → 数える対象を取り違えない形。NPC は週を戻したときに回数を警報に出し、週の印を進めない。命名は `nameConflicts` を数えて出す → ✅
- ✔ 完了報告 `REPORT_I3_NAMING_20260922.md` §4 の未完了の一覧（本番への適用・NG 名リスト・不快な語・画面・導入の永続状態）は、裁定で持ち越したものと一致する → ✅ **I-3 は staging で完了として受理**

### 次の順番 — ⚠️ Q-2（代を重ねて伸びるか）の模擬を前に出す

`PLAN_BLOODLINE_INHERITANCE_20260922.md` §5 の順（導入の永続状態 → 初回の完走 → 案 B の模擬 → **Q-2 の模擬** → I-1 …）に対して:

1. **次は Q-2 の模擬**（固定の種・DB を使わない・本番に触れない）
2. その次に導入の永続状態 → 初回の完走 → 案 B の模擬 → I-1 …

**理由**:
- ★オーナーが「必ず仕上げる」と言ったのは「**引退馬を繁殖に上げて、代々能力を高めていく**」部分。遺伝エンジンは世界全体の能力が上がり続けないように作られている（V-2a 平坦化・V-2b 天井余裕・V-2d 水準維持・PLAN Q-2）
- ★**プレイヤーが代を重ねても自分の血統を伸ばせないなら、その目的の前提が崩れる**。それが分かるのが遅いほど、I-1・I-4〜I-7 に積んだ作業が無駄になりうる
- 模擬は安い（純粋な計算・門だけ）。★**高い作業の前に、構図だけを確かめる**（作業規律「安い実験を先に置く」）
- 導入の永続状態は画面（デザイナー待ち）と噛み合うので、1 つ後ろに回しても失うものが小さい

**Q-2 の模擬で見てほしいもの**（照会の形で出してから始めてよい）:
- 「プレイヤーが毎世代、自分の仔のうち良い牝馬を繁殖に残し、良い種牡馬を選ぶ」方針で、**何世代で、素質の合計がどれだけ伸びるか**。対照は「選ばない（無作為に残す）」
- ★伸びが**頭打ちになる所**と、その所が **NPC の上位と比べてどこか**（上位に届くのか、届かないのか）
- 実時間に直すこと（1 世代 約 60 日・`QUESTIONS_FOAL_LIFECYCLE_20260922.md` §1）。★**何か月遊べば伸びが見えるか**が、オーナーの判断の材料になる
- ★数は画面に出さない（D-114・D-116）。これは開発側とオーナーの判断の材料

★PLAN §5 の 0・1 行目の状態（「裁定待ち」「残り: N-1」）が古い。D-120・N-1・I-2 の対応はいずれも済んでいるので、次の更新で直すこと。
