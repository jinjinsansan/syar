# 回答 — レース演出を「真横の直線だけ」にするとき（相談書 `CONSULT_RACE_SIDE_ONLY_20260914.md` への ① レビュー側の回答）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-14 ／ レビュー側 → 開発側（比較表の材料）・オーナー
**対象**: `CONSULT_RACE_SIDE_ONLY_20260914.md` ／ ブランチ `p4/race-30sec-cuts` ／ HEAD `bcfed84`
**正典**: `STAR_SPEC_v2.0.md`（D-096 まで）

> 凡例（R-13）: **✔ [CODE]** ＝ レビュー側が正典・コードを読んで確かめた ／ **△** ＝ 未確認 ／ **[EYES]** ＝ オーナーの目で決めること
> ⚠️ **番号の衝突**: 相談書の問い「R-1〜R-6」は、正典の作業規律 **R-1〜R-33** と番号が重なります（例: 正典 R-2 は「検査の両側」）。本書では **問 1〜問 6** と呼びます。比較表では付け直してください（例: `Q-R1`）。
> 本書は正典を編集しません。§3 の決定をいただいた後に、レビュー側が起票します。

---

## 0. ★問いの前に — 飛ばす区間に、§8b の介入の見せ場が丸ごと入っている

### 0-1. 事実 ✔ [CODE]

| 項目 | 値 | 出どころ |
|---|---|---|
| 「仕掛け」の局面 | 残り 800m（勝負所） | 正典 §8b.1 |
| 「追う」の局面 | 残り 400m（直線） | 正典 §8b.1 |
| 早仕掛けの減点 | 残り 900m より前 | `packages/race-engine/src/intervention.ts:245`（`EARLY_SPURT_METER = 900`） |
| 仕掛けの最適点 | 残り 495m 付近（±450m で下限へ） | `intervention.ts:393-394` |
| 見せる最後の直線 | **残り 250m から** | `packages/render/src/race-elision.ts:181`（`STRAIGHT_SHOWN_M = 250`） |

→ 1600m で **B（200m→1350m を時計から取り除く）** を採ると、**残り 1400m〜250m** が画面から消えます。これは **勝負所の全体と、直線の介入局面の前半**です。
→ **今の既定（v8）でも、残り 495m 付近は既に消えています**（最後のコーナー 1140–1200m 以外は 200m→1350m を飛ばしている）。

### 0-2. 正典との関係 ✔ [CODE]

- **D-066**: 「要所を静止画で見せる」案（γ）を**採らない**。理由は「**§8b の介入が起きる『その瞬間』を消すので、C-6 が成立せず、正典の機構を 1 つ落とす（演出の選択ではなくシステムの削除）**」
- **§1.4-5**: レース中のリアルタイム介入は「**実装時に薄めてはいけない 5 点**」の 1 つ
- **D-062**: 道中は短く、**勝負所以降を実時間かそれ以上に**
- **§10.2**: 発走後の走行は **75〜105 秒**。「**レーススキップは実装しない。観戦体験そのものが商品**」

### 0-3. 評価

- **今日の時点では違反ではありません。** `/race` はブラウザ内で結果を決めるデモで、介入の画面はまだありません（✔ `apps/web/src/app/race/page.tsx` に介入ボタン・「AI 代行中」の表示は無い）
- ただし「**真横の直線だけ・約 30 秒**」を製品の形として固めると、**P4 で介入の画面を入れた時点で、D-066・§1.4-5 と正面衝突します**

### 0-4. 提案（オーナー決定 → 正典 D-097 候補）

正典 **§14.5 は、既に「介入する自馬を持つプレイヤー」と「観戦のみのユーザー」で配信を分けています**（前者だけ 2 段階配信）。見せ方も同じ線で分けます。

| 見る人 | 見せ方 |
|---|---|
| **観戦のみ** | 飛ばしてよい（B で可） |
| **自馬で介入する人** | **残り `EARLY_SPURT_METER`（900m）→ ゴールは飛ばさない** |

- 決めていない穴として: **飛ばした映像（約 42 秒）と、サーバー側のレースの時計（75〜105 秒）をどう合わせるか**は、正典に書かれていません（△ 論点として記録のみ）

---

## 1. 問 1 — A と B、どちらが正典・過去の決定と整合するか

### 1-1. 「カット数は減らさない」（2026-09-03）の位置づけ ✔ [CODE]

- **正典の D 番号ではありません。** 書かれている場所は次の 3 つです
  - `PLAN_P4_RACE_COMPLETION_20260910.md` §1「カット数は減らさない」（計画書・作成は codex）
  - `RESIDUALS_P4_RACE_VIDEO.md:108`（オーナー決定 2026-09-03「今のカット数がリリースできる限界」）
  - `packages/render/src/broadcast-v2.ts:1085-1087` の註記（v6 17 本 → v8 ＋ `pace=short` 12 本。「**上書きです**。新しい指示で置き換えています（R-7）」）
- 一方で、計画書 **§1** と、完成判定 **§8「カットを削って問題を隠していない」** は書き換えられていません。**文書どうしが食い違っています。**

**回答**: 上書きされたと読んでよいです。ただし、**コードの註記だけに置かず、計画書 §1・§8 を書き直してオーナーの承認を取ってください**（R-7「較正した条件を明記する」）。
- §8 の**趣旨（削って不具合を隠さない）は残します**。コーナーを外すのはオーナーが中身として決めたことで、**理由（出来が足りない）と一緒に記録すれば「隠した」にはなりません**

### 1-2. A か B か

- **B のほうが正典に合います。** D-062 は「情報量に比例して時間を配る」です。A は、情報の少ない道中（D-062 の実測で 1600m の最初の 1000m は出走表を見るほうが正確）を真横のカットで埋めるので、**D-062 と逆向き**です。加えて、相談書 §3 自身が書くとおり「曲がっている区間が直線に見える」
- **ただし B の直線は残り 250m からで、D-062 の「勝負所以降」より遅い。** 直線を見せ始める地点を早める案（例: 残り 400m ＝ §8b の「追う」の始まり）も、比較表に並べてください。長さとの兼ね合いなので **[EYES]**
- **`STRAIGHT_SHOWN_M = 250` はコースによらない固定値です**（✔ `race-elision.ts:181・205`）。直線の長さはコースで違うので、**コースの形から引いてください**（R-33「製品が N 通りなら検査も N 通り」）
- §0 のとおり、**B は「観戦のみ」の見せ方として採る**のが条件です

---

## 2. 問 2 — 「勢い」「順位の入れ替わり」「展開の見出し」は C-2 に反しないか

### 2-1. 事実 — 見せている位置は、全部がエンジンの真実ではない ✔ [CODE]

| 層 | 中身 | 出どころ |
|---|---|---|
| 真実 | エンジンが出す**局面境界の位置**と最終着順だけ | D-059 ① |
| 演出 | 局面の間の補間 | D-059 ② |
| 演出 | 道中の位置は**脚質とペースから生成**＋揺らぎ（結果から逆算できない形） | D-063・D-068 |
| 演出 | 残り 1000m → 200m で表示位置を真実へ寄せる（200m から先は真実） | `packages/render/src/formation.ts`（`CONVERGE_START_M = 1000`・`CONVERGE_END_M = 200`） |
| 演出 | 直線の攻防を**表示位置で作る**（残り 400m から、60m で 0 に戻す） | `packages/render/src/climax-choreography.ts`。**既定は無効**（`page.tsx:3558` `?climax=on` のときだけ） |

→ B の見せ区間（残り 250m → ゴール）では、**250m〜200m は寄せの途中**、`climax=on` なら **60m まで演出**です。

### 2-2. 「エンジンの位置から読むだけ」と言える条件

1. **描画に使った位置と同じ入力から出す**（R-30）。HUD が「2 番手」、絵は 3 番手、を作らない。検査で「HUD の順位 ＝ 同じ時刻の描画コマンドの順位」を固定する
2. **結果の側の値を読まない** — 確定着順・走破タイム・タイム差・最終スコア・`emptyAtMeter`（D-070 と同じ理屈: 答えを画面に出すと、見る・判断するが「読み取り」に変わる）
   - ⚠️ **`packages/render/src/race-development.ts` は「勝ち馬が直線の入口で何番手だったか」で展開の札を決めています**（✔ 同ファイル冒頭の註記）。これを**直線の入口で見出しとして出すと、勝ち馬が差し・追込だと先に明かします**
   - → 見出しは **「画面で起きた後に出す」** か **「勝ち馬を特定しない言い方にする」**
   - 介入する人への 2 段階配信（§14.5）では、締切前のクライアントは着順を持っていないので、この札はそもそも作れません
3. **勢いは数段階に丸め、m/s などの数字では出さない**（演出の位置から出る速さを、計測値のように見せない）。遅れ（平滑化）はあってよい
4. **新しい既定で V-16 を測り直す**（描画コマンドを読む・D-067）。勢いは画面に情報を足す変更なので、V-16 ③（スタート直後に答えが映っていない）と ②（勝負所以降で画面が出走表を上回る）の両方を見る

### 2-3. ⚠️ 相談書の C-2 と既存の演出の食い違い

- 相談書 C-2「**接戦でないレースに接戦を作らない**」（計画書 §1）
- `climax-choreography.ts` は「在るものだけを映す方針では要求を満たせない（40 本中 0 本）」として、**表示専用で攻防を作る演出**です（指示書 `DEV_INSTRUCTIONS_P4_RACE_CLIMAX_REBUILD_20260826.md` §1 で許可）
- → **この 2 つは両立しません。** どちらを優先するか、オーナーの確認が要ります（§3-3）。C-2 を優先するなら、`climax=on` は既定にしない、を記録してください

---

## 3. 問 3 — 買った馬券の「今の当たり具合」を出してよいか

**回答: 出さないことを推奨します。**

- **弁護士ゲート S-1〜S-5 の話ではありません。** D-093 は、あの表を「先行事例と同じ経済構造か」を測る表と定め、他の論点を混ぜないとしています。当たるのは**表現の正典 §9.8・§12.5・§12.7** です
- **理由** ✔ [CODE]: §2-1 のとおり、見せ区間の順位は一部が演出です（寄せが残り 200m まで・`climax=on` なら 60m まで）。したがって「今なら当たり」は、**描画側の処理で当たり・外れと揺れます**。**ポイントを賭けた馬券について、演出が「惜しい」を作る**ことになります
- **§9.5**: 自馬が出るレースでは自馬を含む馬券しか買えません。自馬を持つ人にとって、馬券表示の「自分ごと」は自馬の応援とほぼ重なります
- **§14.5**: 介入する人の 2 段階配信では、締切前に着順が無いので、当たり具合は推測になります

| 出してよい | 出さない |
|---|---|
| 買った馬への印（馬番・勝負服の強調） | レース中の当否 |
| 買い目の一覧の固定表示 | レース中の金額・払戻見込み |
| 着順確定の後のファンファーレと払戻（§9.8） | 「惜しい」「あと少し」の言葉 |
| | 外れの追い打ち（§9.8・§12.5） |

- 最終判断はオーナーです。**§17.1 の弁護士に見せる「実際の画面」に、この項目を加えてください。** 法的な評価はレビュー側からはしません

---

## 4. 問 4 — 介入の「押す場所の画」を、結果に反映しないまま先に入れてよいか

- **本番の `/race` に、効かないのに効きそうに見えるボタンを置かないでください。** 次のどちらかなら可です
  - 試作のページ（`apps/web/src/app/rig-lab/` が既にある）で作る
  - デモで、「**練習 — 結果に反映されません**」を、その局面の間ずっと表示する
- **押したときの手応えは、入力とルールだけで決まるものに限ります**: 騎手の動き・スタミナゲージの減り（仕掛け中は 3 倍・§8b.2）・音・カメラ（§12.1「仕掛けの瞬間は色ではなく**間と音とカメラ**で見せる」）
- **馬がぐっと伸びる絵は出さないでください。** 仕掛けの効果（`interventionMult`）は**サーバーが締切後に計算します**（§8.7・§8b.4）。押した瞬間には存在しない結果です
- 「**AI 代行中**」の表示（§12.6「黙って代行すると『操作していないのに勝った/負けた』が理解できない」）は、今の `/race` にありません（✔）。介入の画面と同じ便で入れてください
- **§0 の決定が先です**（飛ばす区間に介入の局面が入っているため）

---

## 5. 問 5 — 「何が伝わるか」だけ取る線引きで足りるか。色で勢いを示すこと自体は取ってよいか

- **D-060**（業界共通の作法は採ってよい・特定団体の意匠は採らない）に照らして
  - 「**速さを目に見えるようにする**」は機能 → **取ってよい**
  - 参考映像の**色の並び・形・配置・書体** → 意匠 → **取らない**
  - 機械では検査できないので、**「参考から何を機能として取ったか」をデザイン側の文書に記録**してください（D-091 の作法）
- ⚠️ **ただし正典 §12.1（D-057）と衝突します**
  - 「**勝負服だけが鮮やか**」「**仕掛けの瞬間は色ではなく『間』と音とカメラで見せる**」
  - 色だけで表すと、色覚の多様性の要求（D-060 が触れるアートバイブル §4「色＋形」）に反し、**枠番の色・勝負服による個体識別**とも重なります
  - → **形・長さ・動きを主**、色は彩度を抑えた補助、を推奨します。**色を主役にしたいなら §12.1 の改訂**としてオーナーが決めることになります（§3-5）
- 参考映像が他社の製品なら、**その名称をファイル名・コメント・コード・表示に書かないでください**（§0.1・§17.2 C-5）
- 権利の確認は弁護士ゲート S-1〜S-5 とは**別の項目**です（D-093）

---

## 6. 問 6 — 飽きないことを何で確かめるか。カット本数を固定するテストは何を守るべきか

### 6-1. 飽きの判定

- **飽きるかどうかは [EYES]。オーナーが画面で決めます**（相談書 C-6・計画書 §8）
- 変化の間隔・画面の動きの量・情報の更新頻度などの数値は、**報告に並べるだけにし、合否の線を引かないでください**
  - 前例 D-086: 「跳び px」に閾値を引くと、**正しい演出（寄りと引きの組み合わせ）を落とす器具**になる。代わりに「**良い状態を守る検査**」を置いた

### 6-2. カット本数の検査を、守りたい性質の検査に置き換える

| # | 守るもの | 根拠 |
|---|---|---|
| 1 | 台本・飛ばしの有無を変えても**着順と走破タイムが変わらない** | D-059 ④ |
| 2 | **時間の跳びはすべてカットインの下にある**。飛ばす区間の一覧そのものから引いて確かめる | `race-elision.ts` 冒頭「裸の跳びは置かない」・R-30 |
| 3 | 見せ区間で**先頭が画面の外 0%・共通の被写体 0 頭の件数 0** | D-086 |
| 4 | **自馬が常に画面内にいるか、画面の端の印で示されている** | 相談書 §5-5 |
| 5 | 見出し・実況に、**見せていないコーナーの名前が出ない** | 相談書 §1-6 |
| 6 | 先頭・自馬が**長時間 HUD の下に隠れない** | 計画書 §9 |
| 7 | 対応する**全コース**で 1〜6 が成り立つ | R-33 |
| 8 | **新しい既定の画面で V-16 を測る**（描画コマンドを読む・測定器の既定は画面と同じ側） | D-067・R-31 |
| 9 | §0 の決定後: 介入する人の見せ方で、**飛ばす区間 ∩［残り 900m, ゴール］＝ 空** | D-066・§8b |

- △ **V-16 がいま CI の中で回っているかは未確認です。** テスト（`packages/render/test/camera.test.ts`・`formation.test.ts`）には言及があり、計測は手で回す道具（`tools/render-race.mjs`）にあるように見えます。手で回す道具にしか無いなら R-32（正典のゲートは CI に置く）の対象です

### 6-3. [EYES] 見比べの注意

- **seed 42 は、上位 5 頭が 19〜21m に広がる「せめぎ合いの無い」展開です**（✔ `page.tsx:1891-1895`）。これ 1 本で見ると「飽きる」側に倒れます
- **seed 99**（上位 5 頭 4.1 馬身・10 番手から差し切り・1-2 着差 0.15 馬身）と**並べて**見てください。W-5（毎回ちがう）もこの 2 本で確かめられます

---

## 7. オーナーに決めていただくこと

| # | 決めること | レビュー側の推奨 |
|---|---|---|
| 1 | **§0**: 観戦のみと、介入する人とで見せ方を分けるか（→ 正典 D-097 候補） | 分ける。介入する人は残り 900m → ゴールを飛ばさない |
| 2 | 計画書 §1・§8 の「カット数は減らさない」を正式に取り消すか | 取り消す。趣旨（削って隠さない）は残し、理由を記録 |
| 3 | C-2「接戦を作らない」と `climax=on` の攻防の演出、どちらを優先するか | オーナー判断（§2-3） |
| 4 | レース中の馬券の当否表示を出さない、でよいか | 出さない（§3） |
| 5 | 勢いを色を主役にして見せるか（§12.1 の改訂になる） | 形・長さ・動きを主、色は補助（§5） |

---

## 8. このあと

- 開発側: 本書を相談書 §8-2 の比較表に入れる（問番号は付け直す）
- レビュー側: §7 の決定をいただいた後に、**正典へ D-097 ほかを起票**し、計画書の書き換えの指示を出す
- 本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）
