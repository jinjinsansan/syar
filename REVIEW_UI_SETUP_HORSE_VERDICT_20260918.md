# 【STAR】裁定 — Q-UI-1（初期馬の id を誰が選ぶか）／`0035` の受理

| | |
|---|---|
| **案件** | **STAR**（`V:\dev\Cusor\star`）★ 7DAYS ではありません |
| **発行** | 2026-09-18・レビュー側 |
| **対象** | `QUESTIONS_UI_SETUP_HORSE_20260918.md` ／ コミット `43c4e33`（`0035`・`entry-repo.ts`） |

---

## 0. 判定

| # | |
|---|---|
| **Q-UI-1** | ✅ ★**案 A**（`create_account` 自身が選ぶ）。★**開発側の見立てどおりです**（§1） |
| **`0035`** | ✅ **受理**（§3） |
| **`entry-repo.ts`** | ✅ **受理**（§3） |
| **画面を差し替えていない 2 つの理由** | ✅ ★**どちらも正しい。★1 つは未処理の宿題です**（§4） |

---

## 1. ✅ Q-UI-1 — ★**案 A。そして原因が `0031` の註記にあります**

### 1-1. ★**「呼ぶ側」は画面ではありませんでした**

✔ `db/migrations/0031_setup_account.sql:125`:

> ★**呼ぶ側（ワーカー）が★で選んだ馬。★SQL で★を再計算しない**

★★**`p_horse_id` の「呼ぶ側」は、★元々「ワーカー」を指していました。★画面ではありません。**
→ ★**画面が `create_account` を呼ぶ形になって初めて、この前提が噛み合わなくなりました。**

### 1-2. ★そして「★で選んだ」という前提は、今日 D-114 で消えました

★`0031` は「★**表示が同じ帯から選ぶ**」（D-079 ②・D-102 ③）を前提に書かれています。
→ 🔴 ★**D-114（素質は段も見せない）で「★」そのものが無くなりました。** ★**この註記は今日から古いものです。**

★**つまり Q-UI-1 は、D-114 の副作用のもう 1 つです**（★1 つ目は CL-5' の初期馬の候補）。

### 1-3. ✅ 案 A を採ります

| 案 | 判定 | 理由 |
|---|---|---|
| ✅ **A** `create_account` 自身が `auth.uid()` から選ぶ | ✅ **採る** | ★**元の設計（呼ぶ側＝サーバー）に戻すだけ**です |
| ✘ **B** 候補を返す RPC | ✘ ★**採りません** | 🔴 ★**候補を列挙して画面に返すと、プレイヤーが選べます。★「振り直し」が成立します**（D-079 ②） |
| ✘ **C** ワーカーが先に付与 | ✘ ★**採りません** | ★`0031` が「**口座・初期 EP・初期馬を 1 トランザクション**」と定めています。★**分けると V-19 ⑭（同一トランザクションの確認）が測れなくなります** |

### 1-4. ★「どの候補を引くか」の規則 — ★**決定的である必要はありません**

★開発側は「規則そのものは D-079 の帯の話なので決めていません」と止めました。★**正しい止め方です。★決めます。**

| # | |
|---|---|
| 1 | ★**候補は `is_initial_horse_candidate` をそのまま使う**（D-079 ⑥ の「述語を再利用する」＋ **CL-5'**〔未出走に限る〕） |
| 2 | 🔴 ★**決定的である必要はありません。** ★**D-074 の「素質は `user_id` から決定的に導く」は 2026-08-20 に撤回済み**です（★プールは有限で共有なので、決定的に 1 頭を指すと衝突時に渡せない）。★**守るべきは「観測できる差が無いこと」**（D-079 ②） |
| 3 | ★★**そして D-114 と CL-5' で、観測できる差は原理的にゼロになりました** — ★素質も段も見えず、★候補は**未出走**なので戦績も無い。→ ★**どの馬を引いても、プレイヤーには区別がつきません** |

→ ★**したがって「候補から 1 頭取る」だけでよく、規則を発明する必要がありません。**

### 1-5. 条件（UI1-1〜UI1-5）

| # | 条件 |
|---|---|
| **UI1-1** | ★`create_account` から **`p_horse_id` を落とし**、★**RPC の中で候補から 1 頭選ぶ**（★`is_initial_horse_candidate` を使う） |
| **UI1-2** | 🔴 ★**同時に 2 人が登録したとき、同じ馬を渡さないこと**（★`for update skip locked` など）。★**衝突したら次点でよい**（§1-4 の 2） |
| **UI1-3** | ★**候補が 0 頭のときの振る舞いを決める。** ★**D-079 ⑦ のとおり、広げてよいが「広げた回数と幅を記録して監視する」。★黙って広げない** |
| **UI1-4** | ★**V-19 ⑭（2 回呼んでも口座・初期 EP・初期馬が 1 つだけ）を維持する。** ★`p_horse_id` を落としても冪等が保たれることを**実測**すること |
| **UI1-5** | ★**`0031:125` の註記を直す** — 「★呼ぶ側（ワーカー）が★で選んだ馬」は ★**もう成り立ちません**（★★は無く、選ぶのは RPC 自身）。★**新しい移行で** |

---

## 2. ★これで「D-114 の波及」は 3 件になりました

| # | 波及先 | 状態 |
|---|---|---|
| 1 | **D-079 ②**（帯は★表示の幅に収める） | ✅ 訂正済み（`fef2434`） |
| 2 | **初期馬の候補**（CL-5'・未出走に限る） | ✅ 裁定済み・実装済み |
| 3 | ★**`create_account` の `p_horse_id`**（★で選ぶ前提） | ★**本裁定** |
| 4 | ★**§12.4「現在値（stats）は数値で表示する」** | ✅ 撤回済み（`c80e47b`） |

★**起票 1 本が、実装の 4 か所に波及しました。** ★**すべて開発側が見つけています**（★私が起票のときに洗い切れていませんでした）。

---

## 3. ✅ `0035` と `entry-repo.ts` — 受理

### 3-1. `0035`（`races_public` に `min_wins` / `max_wins`）

✅ ★**CL-4 が「画面にも『出られません』を出せ」と要求したのに、★資格の範囲が公開ビューに無く画面が判定できなかった** — ★**穴を正しく塞いでいます。**
✅ ★**「`class_rank` は既に公開なので新しい情報は足していない」という切り分けが正しい**です（★V-20 の観点で増えていない）。
✔ staging 実測で ★**`0033` の後始末も効いている**（`class_rank` 1 → 0/0・2 → 1/1）／ V-20 11 件すべて緑。

### 3-2. `entry-repo.ts`

| # | ✔ |
|---|---|
| `entryStateOf` | ★**段の定義を持たず、レースに保存された数と比べるだけ**（D-052）。★**二重帳簿にしていない** |
| `readEntryError` | `ST001`／`ST002` を訳し、★**それ以外は原文のまま**（R-27。★**知らないものを勝手に言い換えない**） |
| 検査 | ★**画面の判定が DB 側（`enter_race`）と食い違わないことを、段の定義から総当たり** |

✅ ★**R-15 の作法（「入っているが使われない」状態で先に置く）**も守られています。

---

## 4. ✅ 画面を差し替えていない 2 つの理由 — ★どちらも正しい

| # | 理由 | 評価 |
|---|---|---|
| 1 | ★`StableHorse` が **`stars`（素質）を持っている** → D-114 で出せない。★型から外すのは **T-10** の便 | ✅ ★**正しい。★そして T-10（AL-1〜AL-3）は未着手の宿題です**（§5） |
| 2 | 非同期にすると**読み込み中・失敗の表示**が要る ＝ **見た目** | ✅ ★**正しい。★デザイナー経由**（2026-09-15 のオーナー指示）。★**[EYES] を私が裁定しない**のと同じ線です |

✅ ★**「接続の前後で対照が取れる形にしてある」**のも良い判断です。

---

## 5. 🔴 ★残っている宿題 — **T-10 の実装（AL-1〜AL-3）**

★**D-114 は正典に入りましたが、★実装はまだです。**

| # | |
|---|---|
| **AL-1** | `STAR_THRESHOLDS` を **24 段**に置き換える（★算出は `stars.ts` の 1 か所のまま） |
| **AL-2** | ★**段を外に出す口を塞ぐ** — ★`StableHorse.stars`・`horse_market_listing.stars` を含む。★**検査は「入力の形」で** |
| **AL-3** | 内部の 24 段は「帯」としてだけ使う |

★**UI-1 の「自分の馬」は、★AL-2 が済むまで型に `stars` が残ったままです。** → ★**T-10 を UI-1 の前に入れるほうが、二度手間になりません。**

---

## 6. 開発側が次にすること（★順を 1 つ入れ替えます）

| 順 | 中身 |
|---|---|
| 1 | ★**T-10（AL-1〜AL-3）** — ★**`stars` を型と画面から外す。★UI-1 の前に** |
| 2 | **UI1-1〜UI1-5**（`create_account` が中で選ぶ） |
| 3 | **D-116 の 4 層**（★成長段階を年齢から導く純関数 → 「最近の変化」→ 調教師コメント） |
| 4 | UI-1 の画面 → UI-2〜UI-4 |

---

本書の作成にあたって、**7DAYS のファイルは開いていません**（`REVIEW_SESSION_RULES.md` §7・越境なし）。
