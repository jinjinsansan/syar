# 照会: 生まれた馬が調教・出走できるまでの時間（初回の馬と、引退馬の繁殖に共通）

2026-09-22・開発側 Claude。指示書 `CLAUDE_CODE_INSTRUCTIONS_FIRST_HORSE_20260922.md` の「先に解く仕様の衝突」への照会。
## ✅ 決定（2026-09-22・オーナー）: **案 A**

> なるほどＡでやってみましょう。そして、引退馬を継承引退させて能力値を最大化していく部分もまた引き続きＭＤに記録し、必ず仕上げてください。まずは１つずつ進めてください

- **年齢の規則（§7.1・`LIFECYCLE_WEEKS`）は変えない。** 初回は父母を選んで仔馬を本当に生産し、命名する。**加えて、いまの D-079 の規則で未出走の馬を 1 頭付与**し、すぐ調教・出走できるようにする。
- 生産した仔は通常どおり育つ（調教 13 日後・出走 約 17 日後）。
- 案 B（調教を 0 週から解禁）は**捨てていない**。固定シードの模擬で影響を測ってから、改めて判断する（A から B へは「緩める」方向なので後から移れる）。
- 引退馬を繁殖に上げて血をつなぐ部分は `PLAN_BLOODLINE_INHERITANCE_20260922.md` で追跡し、**必ず仕上げる**。
- 進め方は **1 つずつ**（オーナー指示）。

---

以下は判断前に出した照会の本文（記録として残す）。

## 0. オーナーの指摘（2026-09-22）

> これは継承引退で自分の馬を種牡馬にさせる部分にも影響しますので重大です

そのとおりでした。初回の馬だけの問題ではありません。**プレイヤーが自分の馬を繁殖に上げて産駒をとる通常の繁殖**にも、同じ時間がかかります。
初回だけ特例を作ると、「最初の 1 頭はすぐ育てられたのに、自分で配合した仔は 13 日待つ」という食い違いが生まれます。
そのため、この照会は **「初回をどうするか」ではなく「生まれた馬の時間の流れを全馬でどうするか」** として出します。

## 1. 事実（コードと正典で確かめたもの）

| 事実 | 出どころ |
|---|---|
| 1 ゲーム週 ＝ 実 4 時間（1 日 6 週） | 正典 D-007・`packages/scheduler/src/week.ts`（`CYCLES_PER_WEEK = 40` × 6 分） |
| 0〜78 週は育成不可、78 週から調教可、104 週から出走可、260 週で強制引退 | 正典 §7.1・`LIFECYCLE_WEEKS` |
| 配合した週に仔が生まれる（妊娠期間は無い）。仔は 78 週まで週送りを飛ばされる | `apps/worker/src/breeding-runner.ts:514-545`（`birth_week = week`、`last_processed_week = week + trainableFrom`） |
| 繁殖牝馬は年 1 回だけ受胎（1 年 ＝ 52 週 ＝ 実 約 8.7 日） | 正典 §6.7 |
| `raceableFrom` と `retireAt` は 52 の倍数でなければならない（`trainableFrom` は制約なし） | `packages/scheduler/src/birth-week.ts:59` `assertYearAligned()` |
| プレイヤーが自分の馬を繁殖に上げる RPC・配合の RPC はまだ無い（NPC の週次配合だけ） | `db/migrations/*.sql` を検索して 0 件。正典 §12 で種牡馬市場は P5 |
| いまの初期馬（D-079）は未出走で新馬戦の資格がある＝登録直後から調教も出走もできる | 正典 D-079 ② ⑥ |

### 実時間に直すと

```
生まれてから調教できるまで   78 週 ＝ 312 時間 ＝ 13 日
生まれてから出走できるまで  104 週 ＝ 416 時間 ＝ 約 17 日
現役期間（104〜260 週）      156 週 ＝ 26 日
生まれてから引退まで        260 週 ＝ 約 43 日
自分の馬が引退 → 配合 → その仔の初出走   最短 約 17 日（牝馬の年 1 回の枠待ちがあればさらに最大 8.7 日）
1 世代（誕生 → 産駒の初出走）            約 60 日
```

## 2. 選択肢

どの案でも **NPC とプレイヤーの馬に同じ規則を当てる**前提です（§10.5「同じ土俵にいることが公正性の担保」）。初回だけの特例は §0 の理由で推奨しません。

| 案 | 中身 | 初回の体験 | 自分の繁殖の仔 | 変わるもの（測り直しが要るもの） |
|---|---|---|---|---|
| **A. 現行のまま** | 規則を変えない。初回は仔馬を生産し、**加えて未出走の馬を 1 頭付与**（D-079 のまま）して待ち時間を埋める | 仔馬の誕生・命名 ＋ すぐ調教・出走できる馬 | 調教まで 13 日・出走まで 17 日 | 初回に 2 頭（所有上限 30 には余裕）。D-074 / D-079 の改訂は小さい。**較正は動かない** |
| **B. 調教を生まれてすぐ解禁** | `trainableFrom` 78 → 0（全馬）。出走は 104 週のまま | 命名直後から調教できる。初出走は 17 日後 | 生まれた日から育てられる。出走は 17 日後 | 調教できる期間が 182 → 260 週に伸びる → **成長の検査（V-14・D-044 のキャリア後半の伸び）**、**調教費（EP の最大の消費先）の収支（§3.4）**、NPC の調教 AI、`training-career.ts` などの模擬。**78 週の間の成長曲線をどうするか**（いまは 78 週から始まる前提）の決めが要る |
| **C. デビュー前を縮める** | `raceableFrom` 104 → 52（全馬・52 の倍数の制約のため次の候補は 52）。`trainableFrom` も合わせて前へ | 調教・出走までが半分程度（例: 調教 0〜数日・出走 約 8.7 日） | 同じく短くなる | B の全部に加え、**年齢に紐づく全て**: 成長段階（D-116 の「まだ幼い」＝104 週未満）、新馬戦の年齢、プリシードの年齢分布、`retireAt` を据え置けば**現役期間が 208 週に伸びる**（→ キャリア上限 40・頭数の算術・D-007 の要る頭数） |
| **D. 週そのものを短くする** | D-007（1 週 ＝ 4 時間）を改める。2026-09-15 のオーナー方向「週はレース間隔に合わせる」に近い | 全部が比例して速くなる | 同じく | **最大**。キャリア 26 日も縮む、1 日の来訪で進む週数、EP 経済、頭数の算術、`CYCLES_PER_WEEK` と周の関係、配備済みの週番号。D-007 は 09-18 に「4 時間を保つ」と再決定したばかり |

### 開発側の見立て（決めるのはオーナー）

- **いちばん安全に「すぐ遊べる」を満たすのは A**。規則も較正も変えず、「自分で配合した仔が生まれる」体験は本物になります。代わりに「最初の 1 頭を生産する」ではなく「最初の馬を迎え、同時に 1 頭生産する」になり、生産した仔を調教できるのは 13 日後です。
- **「生まれた馬をすぐ育てられる」を全馬で成り立たせるなら B が最小の世界改訂**です。ただし 78 週ぶん伸びた調教期間で、いまの成長・EP の較正が崩れる可能性が高く、**測ってみないと費用が分かりません**。B を選ぶ場合、実装の前に固定シードの模擬で「素質開放率・キャリア後半の伸び・EP 消費」がどれだけ動くかを測って報告します（本番には触れません）。
- C・D はこの便の範囲を超えます（正典の広い範囲とプリシードに及ぶ）。

## 3. 決めてほしいこと

1. **案 A〜D のどれか**（または別案）。
2. B・C・D の場合: **まず模擬で影響を測ってから決める**、でよいか。
3. 出走まで 17 日（A・B）が残ることを許容するか。

## 4. 決定待ちの間に進めること（指示書の「独立作業」）

年齢・誕生週・調教資格に触れない部分だけです。
- 父母候補の公開契約の設計（非公開の能力値を出さない・確定時の再判定）
- `breed()` をサーバー側で実行する配置の比較（Route Handler に業務ロジックを置かない）
- 導入の永続状態（`profile_created → parents_chosen → foal_created → named → ready`）と冪等性の設計
- 命名のサーバー側検査の設計（`data/ng-names.txt` が無い件＝`NG-NAMES-UNWIRED` と接続）
- 正典 D-074 / D-079 の改訂案の下書き（どの規則が残り、どれが置き換わるか）

**どれも staging まで。本番 DB・ワーカーには触れません。**
