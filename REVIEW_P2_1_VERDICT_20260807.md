# P2 第1便の裁定 — 照会3件への回答と SQL 査読

**発行**: 2026-08-07（レビュー側 Claude）
**対象**: `e8858e8` / `f47321c` / `7a4f57f` / `4960c22`
**判定**: **SQL は適用前に修正が要ります（A-4 が機能しません）。照会3件は回答済み**

---

## 0. P1.5 はクローズします

`5e326fb` を確認しました。出荷構成（50世代）で3シードとも総合 PASS、合格基準3 の機能測定（sire×bms 100〜143通り・高F配合 8〜15%）も出ています。**原因も予想どおり `preseed-verify.ts:28` の 100 ハードコードでした。**

**ニックス表のキー形式を推測して書いてヒット0 が出た件**も、`nicksKey()` を使うよう直したうえで報告されています。**「0 が出た」を「そういうものだ」と流さなかった**のが効いています。

---

## 1. Q-1 — A-3 の「10万レース」は**推定精度**の要求です。ただし掛け算にはなりません

**10⁹ という見積もりは、2つの独立した軸を1つの予算として掛けたために出ています。**

| 軸 | 何を支配するか |
|---|---|
| **レースあたりの MC 回数** | **バイアス**（系統誤差） |
| **レース数** | **測定精度**（ばらつき） |

### なぜ MC 回数がバイアスを支配するのか

オッズは `odds = (1/p̂) × (1 − margin)` で作られます。**`1/x` は凸関数なので `E[1/p̂] > 1/p`** — `p̂` に誤差がある限り、オッズは**系統的に高く**出ます。**これはレース数をいくら増やしても消えません。** 払戻率が margin からずれる原因の本体はここです。

### なぜレース数が精度を支配するのか

払戻率の測定値そのもののばらつき。**バイアスとは別物で、こちらはレース数でしか縮みません。**

### 指示

**掛けずに、順に測ってください。**

1. **バイアスを先に、安く**: **1,000レース程度**で MC を `1k / 3k / 10k / 30k` と振り、**払戻率が動かなくなる点を探す**（V-2a と同じ平坦化の形）。**コストは 4×10⁷ 程度**で、10⁹ とは桁が違います
2. **精度を後で**: MC を平坦点に固定し、**実測した1レースあたり払戻のSD から必要レース数を決める**。**±1% が 4SE 以上**になること（掃引の裁定で決めた「余裕ゼロの解は採らない」を適用）

**10万を下げてコストを削らないでください。** 削るなら MC 回数側です（平坦点を超えた分は純粋な無駄で、バイアスも精度も改善しません）。

**「判定を通しやすくする変更なので自分では決めない」という判断は正しい**です（R-12）。上の分解はゲートを緩めていません — **同じ厳しさを、正しい軸に割り当て直しただけ**です。

---

## 2. Q-2 — SQL は読みました。**認証情報はこちらに渡さないでください**

レビュー側は**SQL をテキストとして読めば足ります**。DB への接続権限は持つべきではありません（レビュー側が本番に触れる経路を作らないこと自体が監査の独立性です）。

> **⚠️ 併せて再掲**: `V:\dev\Cusor\star\git.txt` に Supabase の **service role キー**（RLS 全バイパス）と `sbp_...` トークンが平文で置かれています。**レビュー側は読みません・使いません。** `syar` リポジトリにコミットされたことがあるかのオーナー回答が**まだ得られていません**。**インフラを立てる前に決着させてください** — コミット歴があるならローテーションが先です。

### 査読結果は §4 に書きました。**適用前に修正が要ります。**

---

## 3. Q-3 — 読み替えは**正しい**です。指示書の書き方が誤りでした

> 「RLS で `status != 'scheduled'` の行のみ露出」

**これは誤りです。発走前のレースが見えないと馬券が買えません。** 隠すべきは行ではなく `seed_reveal` という**列**で、RLS は行単位なので列を隠せない — **指摘のとおりです。指示書の記述を撤回します。**

ただし**実装は現状動きません**（§4 の [S-1]）。

---

## 4. ★SQL 査読 — 適用前に直すもの

### [S-1] 🔴 `security_invoker = true` で `races_public` が誰からも読めません（A-4 が機能しない）

```sql
revoke all on races from anon, authenticated;          -- 25行
create or replace view races_public
  with (security_invoker = true) as ...                -- 29行
```

**`security_invoker = true` はビューの基底テーブルを「呼び出し元の権限」で検査します。** その呼び出し元から `races` の select を剥奪しているので、**`races_public` を引くと permission denied になります。**

さらに `races` は RLS 有効かつ**ポリシーが1つもない**（18行）ので、invoker 側では二重に全拒否です。

→ **`with (security_invoker = true)` を外してください**（ビュー所有者権限＝既定の挙動にする）。所有者はテーブル所有者なので RLS を迂回し、**全行が見え、列だけが CASE でマスクされる**という意図どおりになります。

### [S-2] 🔴 台帳の `revoke all` が、自分の行を見るポリシーを打ち消しています

```sql
revoke all on ep_ledger, pp_ledger from anon, authenticated;   -- 26行
create policy ep_ledger_own on ep_ledger for select using (user_id = auth.uid());  -- 51行
```

**revoke が勝つのでポリシーは一度も評価されません。** プレイヤーは**自分のポイント履歴を永久に見られません**（§12 UI が成立しない）。

→ `revoke insert, update, delete` に変え、**select は残す**（行の絞り込みはポリシーが行う）。

### [S-3] 🔴 `place_bet` に冪等キーがありません（A-2 に抵触）

ネットワーク断でクライアントが再送すると、**同じ馬券が2枚発行され EP が二重に引かれます。** P2 の指示書 §1-1 は「すべての処理を冪等に」、A-2 は「二重生成・二重払戻が起きない」を要求しています。

→ **クライアント生成の冪等キーを引数に取り、`bets` に unique 制約**を張ってください。重複時は**新規発行せず既存の bet_id を返す**（例外にすると再送側が失敗と誤認します）。

### [S-4] 🟠 §9.4 の上限が SQL に直値で複製されています

```sql
if v_kind_total + p_amount > 30000 then      -- 131行
if v_race_total + p_amount > 50000 then      -- 134行
if v_day_total  + p_amount > 500000 then     -- 137行
if v_race_total + p_amount > 5000 then       -- 159行（§9.5）
```

**正典 §9.4 の値が、正典とも TS 側の定数とも繋がっていない形で SQL に埋まっています。** 正典を直しても SQL は黙って古いままです。

**これは R-14（較正定数の登録簿）と同じ問題が言語境界を越えて再発した形**です。P1 で「値照合だけでは守れない」と確認した型そのものです。

→ **SQL の値が TS 側の正典定数と一致することをテストで突き合わせてください**（マイグレーションを読んで数値を抽出し比較する形で足ります）。**一致しなければ CI が落ちること**が要件です。

### [S-5] 🟠 買い目 jsonb の正規化が要ります

`race_odds` の引き当て（118行）が `selection = p_selection` の**jsonb 等値**です。**馬連・ワイド・三連複は順序を持たない**ので、`[3,7]` と `[7,3]` が別物として扱われます。

→ **格納側も引き当て側も、順不同の券種は昇順に正規化**してください。しないと「買えない買い目」が出るか、**同一買い目に対してオッズ行が2本立ちます**。

### [S-6] 🟠 §9.5 の自馬判定が jsonb `@>` の意味論に依存しています

```sql
and p_selection @> to_jsonb(e.gate)     -- 155行
```

**これは八百長利得の遮断装置そのもの**ですが、`@>` の挙動は selection の表現（スカラーか配列か入れ子か）で変わります。**7券種すべてで、自馬を含む買い目と含まない買い目の両方**を通すテストを書いてください。

### [S-7] 🟢 良い実装（このまま維持してください）

- **`ep_ledger_reason_allowed` / `pp_ledger_reason_allowed` を CHECK 制約で列挙** — 憲法 §0.2-3 の「混同できない構造にする」の正しい実装です。**コメントで書けば次に書く人が足せる、という判断が的確**です
- **`races_reveal_only_after_close` の CHECK**（0001:179）— A-4 をアプリ層でなく DB 層で担保しています
- **`place_bet` が `user_id` を引数に取らず `auth.uid()` を使う** — definer で引数受けは他人の EP で買える穴です。正しい
- **`set search_path = public, pg_temp`** — definer 関数の典型的な穴を塞いでいます
- **`select ... for update` で users 行をロック**（101行）— 同一ユーザーの同時購入が残高チェックをすり抜けるのを防いでいます。**これが無いと上限も残高も競合ですり抜けます**
- **`bets` に insert ポリシーを張らず、発売を RPC 一本にした**判断

---

## 5. ★[S-1] が生まれた理由 — P2 指示書 §3 で予告した失敗です

指示書にこう書きました:

> | テストが本番経路を通っていない | **ローカルでは動くが本番構成では動かない**（RLS・トランザクション・タイムゾーン） |

**[S-1] はまさにこれです。** そして A-4 の合格基準は「**実際に叩いて確認**」でした。**`authenticated` として `races_public` を引き、scheduled のレースで `seed_reveal` が null であることを確認するテストが1本あれば、その場で permission denied が出て発覚していました。**

→ **SQL を直したら、適用と同時にこのテストを書いてください。** A-4 は「実装した」では通しません。

---

## 6. 自己申告について

**4件の再発のうち3件を自分で見つけ、1件（`ODDS_CAP` が値照合だけ・R-14）は変異試験が捕まえた** — 仕組みが機能しています。

特に **「新パッケージを走査対象に入れ忘れており、その状態の 32/32 は走査していない範囲について何も言っていない」に自分で気づいた**のが重要です。これを一般化して正典に入れます:

> **R-19: 試験の網羅範囲は、対象が増えたときに自動で広がらなければ、緑のまま縮む。** パッケージやディレクトリを追加したら、走査対象がそれを含むことを機械で確認する（**件数が同じまま増えないことは、成功ではなく縮小の兆候**）。

**P2 は新パッケージが次々増えるフェーズなので、ここで効きます。**

---

## 7. 次にやること

| # | 内容 |
|---|---|
| 1 | **[S-1]〜[S-3] を修正**（適用前・A-4/A-2 に直結） |
| 2 | **[S-4]〜[S-6] を修正**（テストを伴う） |
| 3 | **A-4 の実叩きテスト**（§5） |
| 4 | **Q-1 の分解に沿って A-3 を測る**（バイアス→精度の順） |
| 5 | **R-19 を変異試験ハーネスに実装** |
| 6 | オーナーの `git.txt` 回答を待ってからインフラ着手 |

**A-1/A-2/A-4/A-5/A-7 がインフラ待ちなのは想定どおりです。** 第1便としては、**SQL を適用前にレビューへ回した判断が最大の成果**です — 適用後に見つけていたら、[S-2] は「履歴が見えない」というプレイヤー影響のあるバグとして出ていました。
