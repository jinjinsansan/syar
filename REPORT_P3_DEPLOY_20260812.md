# REPORT_P3_DEPLOY_20260812 — VPS 配備／★配備事故／本番での実測

## 本便で正典の改訂が要る決定をしたか

**していません。**（前便の Q-P3-41「§13.1 の較正条件の記述」は未回答のままです）

---

## 0. ★まず事故を報告します

**マイグレーションを本番に当てないまま新しいコードを配備し、ワーカーを止めました。**

| | |
|---|---|
| 何が起きたか | `column "condition" does not exist` で**10回連続失敗し、ワーカーが終了** |
| 壊れていた時間 | 約10分 |
| 復旧 | シンボリックリンクを `ca575f9` に張り戻して**数秒** |
| **被害** | 発走済み未確定 **0件** / 中止の閾値超え **0件** / 馬券 **0枚** / 利用者 **0人** |

確定は張り戻した直後に追いつきました。**被害が無かったのは運です。**
利用者がいる状態で同じことをすれば、10分間発売が止まります。

### ★なぜ既存の検査で防げなかったか

**配備前検査が、どれも DB を見ていませんでした。**

| 検査 | 見ているもの | この事故を検出できるか |
|---|---|---|
| `node dist/worker.cjs`（環境変数なし） | 起動できるか | ✗ DB を見ない |
| `--selfcheck`（D-043） | 束ね方が壊れていないか | ✗ **わざと DB に触らない設計** |

★`--selfcheck` は「配備物が自己完結しているか」を見る検査で、**そう作ったのは正しい**です。
足りなかったのは別の問いでした:

> **「繋ぎ先の DB は、このコードを動かせる形になっているか」**

これを誰も見ていませんでした。

---

## 1. 是正 — `--schemacheck`

配備物に足しました。リリースの `db/migrations/*.sql` と DB の `schema_migrations` を
突き合わせ、**未適用が1つでもあれば配備を中止**します。

```
node dist/worker.cjs --schemacheck
```

★**「必要な列があるか」を1つずつ書く形にはしませんでした。**
列を足すたびに検査も直す必要があり、**直し忘れたとき黙って通ります**。
ファイル名の集合どうしの比較なら、**足し忘れようがありません**。

### 両側で働くことを実測しました（R-2）

| 状態 | 結果 |
|---|---|
| 未適用あり | **exit 1** ／ ★**ワーカー起動の痕跡 0**（後述の②の確認） |
| 全適用 | exit 0 ／ 配備続行 |

---

## 2. ★是正を作っている最中に、さらに2件見つけました

正直に書きます。**どちらも「検査のつもりのものが副作用を持っていた」型**です。

### ① `--schemacheck` が、検査のついでにワーカーを起動していた

フラグ処理が非同期なので、`process.exit` が走る前に `main()` も始まっていました
（`[worker] 起動 env=staging` がログに出て気づきました）。

★**本番に向けて実行すれば、検査のつもりで周回が始まります。**
→ `else` で括りました。以後、検査時にワーカーは起動しません。

### ② `deploy.sh` のログ文が、マイグレーションを実行していた

ログに出すつもりの行にバッククォートを書いており、**コマンド置換として
`npx tsx tools/migrate.mjs` が VPS で実際に走りました。**

`secrets.local.env` が無くて失敗したので実害はありませんが、
★**「案内文を表示する」つもりの行が、本番に対してマイグレーションを走らせる状態**でした。
→ シングルクォートにしました。

---

## 3. 配備しました

| | |
|---|---|
| 配備前 | `ca575f9`（P2 期・2026-08-10 17:08 から稼働） |
| 配備後 | `67b2971` |
| 方式 | リリースディレクトリ＋シンボリックリンク（D-037） |

---

## 4. ★配備後に見つけて直したもの — 調子が生成側と確定側でずれていました

配備直後に `diag-b6` を本番に向けたところ、**能力比は 1.0000 なのに調子だけ差**がありました。

**原因**: 本番の馬はまだ週送りを一度も通っていないので、`loadTrainingStates` が
**何も返しませんでした**。その結果:

- 生成側 … 値が無いので `rng.int(2,4)` の仮定値
- 確定側 … DB の既定値 `3`

**224頭すべて**がこの状態でした。★Q-P3-32 と同じ型（オッズを計算した馬 ≠ 実際に走る馬）が、
**別の入口から再発**していました。

→ `loadTrainingStates` が**全馬を返す**ようにしました（`where` を外した）。

```
再測定: 調子 224/224頭 差 0.0000 ／ 疲労 224/224頭 差 0.0000
```

★**「週送り済みの馬だけ返す」が正しく見えたのが罠**でした。生成側と確定側は
**同じ母集団を見ていなければならない**ので、絞り込みそのものが誤りです。

---

## 5. レビュー側指定の確認（2件）

### ① 本番でも能力比 1.0000 / 254-254 か

```
★本番（VPS が実際に使う経路・254頭）
  オッズ計算時 ÷ 確定時 の能力比: 1.0000  SD 0.0000
  比が 1.00 ±1% に入る馬:        254 / 254 頭
```

**staging と同じです。**

### ★1点だけの照会 — 疲労の差 0.50 の理由

> 生成時の疲労 66.38 に対し DB 65.88（差 0.50）。能力比が SD 0.0000 で完全一致しているのに
> 疲労だけ差があるので、理由を1行ください。

**★母集団が違うものを並べていました。実装の差ではありません。**

- `66.38` … **その日の出走馬 254 頭**の平均
- `65.88` … **DB の全馬 7,370 頭**の平均

出走馬は「出走可能な馬」から選ばれるので、全馬とは疲労の分布が違います。実測:

```
全馬 7,370頭  65.88 ／ 出走可能プール 3,000頭  65.39 ／ 出走馬 254頭  66.38
```

★**同じ馬どうしで比べるべきところを、集団の平均どうしで比べていました。**
→ `diag-b6.mjs` を**1頭ずつ突き合わせる**形に直しました:

```
調子 224/224頭 差 0.0000 ／ 疲労 224/224頭 差 0.0000
```

★ご指摘のとおりで、**「平均が近いから一致している」は一致の証拠になりません。**
能力比だけ per-horse で見ていて、調子・疲労は平均で見ていたのが誤りでした。
（この per-horse 化の過程で §4 の「224頭全部で調子がずれていた」が見つかりました。）

### ② VPS での周時間実測（★最悪18頭立て）

**自然観測で最悪ケースが取れました。**

18頭立ての出現率は約9%で、生成は1周に1本しかないため狙い撃ちができません。
→ **周ごとの所要時間をワーカーのログに出す**ようにして（`周=…s(…%)`）待ちました。

| 生成した馬番 | **頭数** | オッズ行数 | **周の所要時間** | 10分周に占める割合 |
|---|---|---|---|---|
| 605 | **★18頭（最悪）** | 6,344 | **117.5s** | **19.6%** |
| 606 | 14頭 | 2,940 | 100.8s | 16.8% |
| 607 | 11頭 | 1,381 | 76.7s | 12.8% |
| 608 | 11頭 | 1,300 | 83.6s | 13.9% |
| （生成なしの周） | — | — | 0.1〜0.2s | 0.0% |

★**最悪18頭立てで 117.5s / 19.6%。A-1 に対して約8割の余裕があります。**
この周は**生成605（18頭）＋確定602 を両方含みます**（生成だけの数字ではありません）。

#### ★補強のつもりの測定を、自分で汚染しました（報告）

18頭立ての出現を待つ間に、**`buildRace` を18頭に固定して VPS で回すツール**を書きました。
結果はこうでした:

```
18頭立ての buildRace 単体: 平均 154.31s / 最大 159.03s（26.5%）
```

★**これは採用できません。周の一部が、周の全体（117.5s）を超えています。**
`buildRace` は周に含まれるので、**単体が全体を超えることは原理的にありません。**

**原因**: このツールを**稼働中のワーカーと同時に**走らせました。VPS は 4 コアで、
両者が CPU を奪い合った結果、どちらでもない数字が出ました。

★**「部分 > 全体」で気づけたのは運**です。もし 90s と出ていたら、
もっともらしいので**そのまま報告していました**。

→ **採用するのはワーカー自身のログ（117.5s）だけ**です。ツールと生ログは残していません
（リリースディレクトリに一時的に置いたので、**測定後に撤去して `67b2971` と一致する状態に戻しました**）。

⚠️ **稼働中のワーカーと同じ機械で計算量の大きい測定を回さないこと。**
P1 期に同じ型を踏んでいます（自分の監査を並走ツールで汚染した件）。

#### ★以前の報告を訂正します

> 計算は頭数によらず 64〜102s でほぼ一定

**VPS ではそうなっていません。** 上表のとおり **11頭 76.7s → 18頭 117.5s（1.53倍）**で、
**頭数に比例して増えます**（オッズの目の数が組合せで増えるため）。
以前の「ほぼ一定」は開発機での測定で、**VPS には当てはまりませんでした。**

★余裕が大きいので判定は変わりませんが、**「頭数が増えても増えない」という前提で
今後の余裕を見積もらないでください。**

---

## 6. 再現

```bash
# ★周時間はワーカー自身のログだけを採用します（並走測定は汚染されます）
ssh <vps> "journalctl -u star-worker --since '-90 min' -o cat | grep '周='"

# 生成側と確定側の一致（本番）
npx tsx tools/diag-b6.mjs
```

---

## 7. 照会・未回答

### 裁定を反映しました

| # | 裁定 | 反映先 |
|---|---|---|
| Q-P3-34 | **V-10 は a/b/c の3分解**。★①②だけでは証拠にならない | 判定書 §10 |
| Q-P3-40 | **対応ゲート表が防御の記録**。捕まえないものだけ振る舞いテスト | 判定書 §9 / `MUTATION_TRIAGE.md` §6 |
| Q-P3-41 | **D-053**（較正条件は定数の意味の一部） | 判定書 §11 |
| Q-P3-35 / 37 | ②③ 投入・VPS 配備 | §3・§4 |

★**V-10 は「鎖の全体が証拠」**と書きました。b（推定確率がレース過程に対して偏っていないか）は
本番経路では測れず、**エンジン同一・入力同一が移送を licence します**。
その licence が今回の **能力比 1.0000 / 254-254 ＋ バンドルのバイト一致**です。
→ **能力比が 1.0000 でなくなった時点で、V-10 は未証明に戻ります**（判定書に明記）。

★**Q-P3-40 の結果、どのゲートも捕まえないものは 0 件**でした。
ただし**実際に壊してゲートを流したのは 4件だけ**で、残り11件は登録簿とコメント由来です。

### まだ未回答

| # | 内容 |
|---|---|
| **Q-P3-42** | 較正定数のうち**未実測11件**をゲートで実測して裏付けるか（約90分） |
| Q-P3-33 / 20〜31 / 38 / 39 | |

---

## 8. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
2. 本便の事故について: **被害はありませんでしたが、防げた事故です。**
   配備手順に DB 側の検査が無かったことが原因で、`--schemacheck` で塞ぎました
