# QUESTIONS — P4「2D世界＋3D馬群 ハイブリッド8秒検証」

日付: 2026-08-23
発行: 開発側
対象指示書: `DEV_INSTRUCTIONS_P4_HYBRID_PACK_SPIKE_20260823.md`
停止条件: **§15-5「モバイル実機測定にユーザー操作が必要」**

---

## 照会 1（唯一の停止項目）— モバイル実機での測定

### 何が足りないか

指示書 §10 は次を求めています。

> 最低1台の実機で測る。機種、OS、ブラウザ、解像度、devicePixelRatio、発熱の有無を記録する。
> 実機へアクセスできない場合は数値を捏造せず、質問書を出す。

★**開発側からは実機のブラウザを操作できません。** したがってこの項目だけ埋まりません。
数字を作りません。

### すでに測ったこと（実機の代わりにはなりません）

`out/hybrid-pack-spike/throttled-cpu4x-844x390@2.json`

⚠️ ★**これは実機の測定ではありません。** デスクトップの Chromium で

- CPU を 1/4 に絞り
- 画面を 844×390・devicePixelRatio 2（実描画 1688×780）にした

だけのものです。**GPU は Intel Iris Xe のまま**で、**発熱・電池・メモリ制約は再現していません**。
「これでモバイルは大丈夫」という根拠にはなりません。下限の目安としてだけ見てください。

### お願いしたいこと（どれか 1 つで足ります）

**A. オーナーの端末で開いて、画面の数字を読み上げていただく**

1. PC と携帯を同じ Wi-Fi につなぐ
2. 開発サーバーを外から見えるように起動する

   ```powershell
   npm -w @star/web exec next dev -- -p 3210 -H 0.0.0.0
   ```

3. 携帯のブラウザで次を開く（`100.110.117.112` は PC の IP。変わっていれば置き換えてください）

   ```text
   http://100.110.117.112:3210/3d-hybrid-lab?v=h3&play=1
   ```

4. 画面上の 1 行（`読み込み完了（…）/ {...}`）を撮って送っていただく
5. 8 秒ほど再生したあと、**端末が熱くなったか**を一言で教えていただく

**B. 端末を USB で PC に繋いでいただく**

繋いだうえで一言いただければ、こちらから `chrome://inspect` 経由で
デスクトップと**同じ手順・同じ 3 回計測**を流し、`mobile-performance.json` を作ります。
その場合は機種・OS・ブラウザ・解像度・dPR も自動で記録します。

**C. 「今回はモバイル測定を省く」と判断していただく**

その場合、報告書には**未測定**と明記し、最終採用判定は保留のままにします。

### 知りたい最小限

| 項目 | 例 |
|---|---|
| 機種 | — |
| OS とバージョン | — |
| ブラウザとバージョン | — |
| 画面の解像度と devicePixelRatio | — |
| 8 秒再生したあとの発熱 | 変化なし / ぬるい / 熱い |

---

## 補足（照会ではありません・報告のみ）

停止条件に当たるのは上の 1 件だけです。以下は**止まらずに進めた**うえで、
判断材料として書き残しておくものです。

### ① 素の側面カットには、前景がまたぐ瞬間がありません

`foreground-proof.json`

- そのままのカットでは、手前ラチが 3D 馬を隠す画素は **48 標本すべてで 0**
- 理由: パララックスの層の縦位置は**プレートの切り取り**（`zoom` / `verticalAnchor`）で決まり、
  カメラの高さと**独立**しています。カメラを下げても重なりません（実測）
- そのため、層の前後が効いていること自体は、
  **前景を 260px 上へずらした測定専用の設定**で確かめました（馬の 20% が隠れる）

★このカットを提出物のまま進めてよいか、それとも
「手前ラチが馬をまたぐカット」を別に用意すべきかは、レビュー側の判断だと考えます。

### ② 色調統合は「変えない」を採用しました

`tone-stats.json` の実測（4.0 秒・12 頭）

| | p05 | p50 | p95 |
|---|---:|---:|---:|
| 2D 世界ぜんたい | 0 | 40 | 109 |
| 3D の馬 | 4 | 19 | **110** |

いちばん明るいところが既に一致しているため、**補正しないほうが合います**。
一度持ち上げて p95 が 158 になり、世界のどこよりも明るくなったので取り下げました。
中間調の差（19 と 40）は、茶や黒の動物と明るい芝の差なので詰めていません。

### ③ 前回の 3D スパイク報告書に訂正が 1 件あります

`REPORT_P4_3D_PACK_SPIKE_20260823.md` §6 に

> 首 … 位相を 0.12 遅らせた正弦（`neckAngleOf`）／脚と完全同期しない

と書きましたが、★**実際には効いていませんでした。**
`getObjectByName('DEF-neck.003')` が静かに `undefined` を返していたためです
（three の GLTFLoader は節名の `.` を削除するので、読み込み後は `DEF-neck003`）。
今回の修正で効くようになりました（`neckMissed: 12 → 0`）。
