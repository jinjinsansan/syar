# REPORT_P1 — レース判定エンジン + モンテカルロ検証（2026-08-05）

**指示書**: `DEV_INSTRUCTIONS_P1_20260805.md`（N-1〜N-5・K-1〜K-5・K-a・K-b）
**コミット**: `fe2c84f`（N便）→ `446a1c4`（K-1〜K-3）→ `b3043a5`（K-4/K-5・K-a・K-b）
**照会**: `QUESTIONS_P1_20260805.md`（6件）

> **この報告書は額面どおり受け取られない前提で書いています。** 数字はすべて再実行コマンド付きで、
> 私が踏んだ失敗と、いま残っている弱点も隠さず書きます。

---

## 0. M-1〜M-6 と N-1〜N-5 の対応

M 便は前回報告済み（`REPORT_P1_M1-M6_20260805.md`）。N 便の対応は以下。

### N-1 廃棄済み文書の禁止語処理

**検閲コマンド（報告書に書くよう指示のあったもの）**:

```bash
# ★既定の rg は .gitignore を尊重するため SPEC.md / AI_PROMPT.md が黙って除外される
rg -uu -n "スターホース|SEGA|WBC|SWBC|メダル|ライド|SH経済|グランドマスターズ|スターコイン" \
   -g '!node_modules' -g '!.git' .
rg -uu -i -c "wbc|swbc" -g '!node_modules' -g '!.git' .          # 大小文字を無視
rg -uu -n "ﾗｲﾄﾞ|ﾒﾀﾞﾙ|ＷＢＣ|ＳＷＢＣ|ｽﾀｰﾎｰｽ" -g '!node_modules' .   # 半角カナ・全角英字
rg -uu -n "ダーレー|バイアリー|ゴドルフィン|オルコック" -g '!node_modules' .  # 実在馬名
```

**罠の再現**（指示どおり確認しました）:

| 検索 | SPEC.md | AI_PROMPT.md |
|---|---|---|
| `rg`（既定） | **0行**（不可視） | **0行**（不可視） |
| `rg -uu` | **50行** | **3行** |

**処理内容**: `SPEC.md` に廃棄警告ヘッダを追加し、他社固有名称と実在馬名を計 **99件**
プレースホルダへ置換（内訳は下表）。`AI_PROMPT.md` の残り6件も同様。
置換は**事後条件を検査するスクリプト**で行いました（`includes` だけ見て `replace` の空振りを
見逃したのが M-1 の失敗形なので、「一致件数と実際の変化」の両方を assert しています）。

| 語 | SPEC.md | AI_PROMPT.md |
|---|---|---|
| スターホース / SEGA | 4 / 1 | 0 |
| WBC / SWBC | 24 / 14 | 2 / 2 |
| メダル / スターコイン | 5 / 2 | 1 |
| ライド系（ライド馬券・4種・識別子） | 20 | 0 |
| SH経済 / グランドマスターズ | 4 / 2 | 1 |
| **実在馬名**（三大始祖＋オルコックアラビアン） | **5** | 0 |

### ★N-1 で新たに見つけた3件

1. **私の置換ルールが大文字のみで、小文字の `wbc/`（ディレクトリ名）を取りこぼしました。**
   `rg -uu -i` を追加で回して検出。R-9（検索式が拾えない形を1つ以上列挙する）が実際に効いた例です。
2. **`SPEC.md` に実在馬名が5件ありました**（三大始祖ほか）。指示書の禁止語リストには
   無かったものですが、憲法 §0.1 が明示的に禁じているため処理しました。
3. **`files.zip`（gitignore 済み・未 push）の中に「メダル」2件**があります。
   rg はアーカイブ内を検索できないので、`unzip -l` → 展開 → スキャンで確認しました。
   中身は `GORILLA_STABLE_SPEC_v1.0/v1.1` で、N-1 の指示範囲外の別系統アーカイブのため
   **独断で書き換えていません**（→ 判断を仰ぎます）。

### ★私の文脈に入っている `CLAUDE.md` は古い版でした

セッション開始時に注入された `CLAUDE.md` は**スクラブ前の第1世代の内容**で、
ディスク上の実体（`# CLAUDE.md — STAR（開発コード）`）と食い違っています。
実体側は禁止語ゼロで問題ありません。**注入された文脈を出典として扱わない**という教訓として記録します。

### N-2 棚卸しの数字（R-10: 実行時計測）

静的検索をやめ、**本番エントリにプローブを挿してテストごとの到達を記録する**ツールを作りました
（`npm run inventory` / `tools/inventory/`）。プローブは finally で必ず撤去し、md5 一致で復元を検証します。

- **N 便時点（103テスト）: 経路 51（49.5%）／単体 52（50.5%）**
- 指示書の「経路/単体 = 52/51（49.5%）」と**ラベルが逆**でした。49.5% が付くのは**経路**側です
  （51/103 = 49.51%）。数字自体は一致しています。
- **K 便完了時点（196テスト）: 経路 89（45.4%）／単体 107（54.6%）**

### N-3 / N-4 / N-5

- **N-3**: `balance.ts` の無名の「テストで固定している」に、固定している5テストの名前を明記
- **N-4**: `sweep-distance.ts` の `sdFor()` を切断係数で割るよう修正（表示 312 と実使用 341 の食い違いを解消）
- **N-5**: `config.test.ts` の同語反復を独立構築の期待値へ置換。**変異テストで検出力を確認**
  （`resolveRuntimeConfig` から再導出を取り除くと当該テストを含む7件が落ちる。以前は緑のままでした）

> **★N-5 の変異テストで自分の検証手順の欠陥をもう1つ見つけました。**
> 1回目の改変スクリプトは前提条件（`includes`）だけ検査して**置換が起きたかを検査しておらず**、
> 空振りしたまま「改変した」と出力していました。その結果「テストが検出できない」という
> **誤った結論**を一度出しています。M-1 とまったく同じ形の3度目です。以後、改変系スクリプトは
> 必ず「文字列が実際に変化したこと」を assert しています。

---

## 1. 実装したもの・ファイル一覧

### 新パッケージ `packages/race-engine/`（純 TypeScript・`@star/sim-engine` のみに依存）

| ファイル | 内容 |
|---|---|
| `src/types.ts` | レース条件・出走馬・結果の型 |
| `src/balance.ts` | 正典 §8/§13 の定数 ＋ **`INTERPRETATIONS`（正典の空白を埋めた箇所の登録簿）** |
| `src/coefficients.ts` | §8.2 基礎スコア・§8.3 乗算補正10種・§8.4 展開 |
| `src/skills.ts` | §8.5 発動型スキル6種（発動率 40% + IQ/2000） |
| `src/race.ts` | §8.7 着順・着差・タイム。乱数サブストリームの分離 |
| `src/fairness.ts` | §8.6/§8.8 Provably Fair（ハッシュは注入） |
| `src/intervention.ts` | §8b 介入モデル・AI 代行（UI なし） |

### `apps/cli/`

| ファイル | 内容 |
|---|---|
| `src/node-hash.ts` | `HashProvider` の Node 実装。**`node:crypto` を import する唯一の場所** |
| `src/race-field.ts` | `HorseRecord` → 出走馬の変換・出走表生成（クラス分け・馬場別エントリ） |
| `src/racing-season.ts` | K-4 の1シーズン実行と選抜指標 |
| `src/verify-race.ts` | K-5 検証ハーネス（`npm run verify:race`） |
| `src/race-diagnostics.ts` | スコア分散の要因分解（較正の根拠を出すため） |
| `src/selection-compare.ts` | 選抜方式ごとの形質ドリフト比較 |
| `tools/inventory/` | N-2 の実行時計測ツール |

### テスト（196件・全 PASS）

`race.test.ts` 42 / `fairness.test.ts` 16 / `intervention.test.ts` 14 /
`racing-season.test.ts` 10 / `node-hash.test.ts` 4 / `interpretations.test.ts` 3 /
`helpers-consistency.test.ts` 4 ＋ P0 の 103件。

---

## 2. 再実行コマンド（コピペで動く形）

```bash
npm install
npm run typecheck                 # exit 0
npm test                          # 196 passed

# K-5: モンテカルロ検証（10万レース・4シード）
npm run verify:race -- --races 100000 --seeds 42,7,2026,31337

# K-4: P0 ゲートを実レース選抜下で回す
npm run verify -- --selection race
# 比較用: P0 の既定（proxy）
npm run verify

# 較正の根拠
npx tsx apps/cli/src/race-diagnostics.ts --races 300
npx tsx apps/cli/src/selection-compare.ts --generations 60 --population 400 --seeds 42,7

# 棚卸し（実行時計測）
npm run inventory

# 較正のスイープ（K を振る）
npm run verify:race -- --races 4000 --seeds 42,7 --pool-generations 20 --k 0.12
```

---

## 3. 検証結果

### 3-1. K-5 モンテカルロ（10万レース・4シード・**所要 77.4 秒**）

```
レース総数=100000（4シード × 25000） / 人気推定の試行数=60（本番確定とは別系列・§9.2）
母集団=40ゲーム内年 × 繁殖牝馬400頭の最終年産駒
RACE_RANDOM_K=0.26（★較正値。正典 §13.1 は 0.12）
```

| # | 検証項目 | 目標 | 実測 | 判定 |
|---|---|---|---|---|
| V-4 | 1番人気の勝率 | 30〜34% | **32.51%** | PASS |
| V-5 | 1番人気の複勝率 | 60〜65% | **61.31%** | PASS |
| V-6 | 最低人気の勝率 | 0.5〜2% | **0.80%** | PASS |
| V-8 | AI代行 ÷ 手動最適 | 93〜97% | **94.69%** | PASS |
| V-9a | interventionMult 平均 | 0.99〜1.01 | **1.0032** | PASS |
| V-9b | 全サンプルが範囲内 | 0.90〜1.10 | **0.90〜1.10** | PASS |
| V-12a | 平均F | ≤0.10 | **0.0184** | PASS |
| V-12b | 丈夫さの表現型−genotype 乖離 | ≥−8% | **−1.02%** | PASS |

人気別勝率（32.4 → 20.0 → 13.7 → 9.7 → 7.0 → … → 0.49%）は単調減少で、
シード間ばらつき（1番人気勝率のSD）は 0.2%。

**10万レースは受け入れスイートに載せられる規模です**（77秒）。

### 3-2. K-4 P0 ゲートの維持（4シード × 100/300ゲーム内年・繁殖牝馬800頭）

| | V-1(100年) | V-1(300年) | V-2a | V-2b | V-2d 最悪 | V-2e 最悪 | V-3 | V-2c | 総合 |
|---|---|---|---|---|---|---|---|---|---|
| **proxy（P0 既定）** | 16.18% | 15.59% | PASS | PASS | −4.76% | 0.93x | PASS | PASS | **PASS** |
| **race（K-4）** | 15.87% | 15.38% | PASS | PASS | +9.08% | 0.92x | PASS | PASS | **PASS** |

（V-2d 最悪は 8行 × 2 horizon の全 |乖離| の最大。算出:
`grep -oE "[a-z_.]+ (-?[0-9.]+)%  (PASS|FAIL)" 出力 | awk` で最大絶対値を取っています）

**実レース選抜下でも P0 ゲートは全通過しました。** ただし2点、傾向の変化を報告します:

1. **V-2d の乖離が系統的に正へ寄る**（proxy は −4.8%、race は +9.1%）。
   レースが芝/ダート適性を直接選抜するためで、**合格域には収まっていますが余裕が薄い**（+9.08% / 上限 +10%）。
   P3 で育成が入って選抜がさらに強くなれば割る可能性があります。
2. **親子相関が 0.35〜0.42 → 0.27〜0.34 に低下**。レース成績は素質にノイズが乗った指標なので、
   選抜が素質そのものを見ていた proxy より相関が下がります。これは想定どおりの挙動です。

**`npm run verify`（既定 proxy）の数値は P0-fix5 と完全に同一**です（V-1 15.59%・V-2c 14.88〜16.23%）。
K-4 を既定にしなかったのは、K-4 の影響とそれ以外を切り分けられるようにするためです。

---

## 4. K-4 で選んだ選抜指標とその理由（実測つき）

**採用: 累計獲得賞金（`prize`）**

3指標を実装して実測比較しました（60ゲーム内年・繁殖牝馬400頭・シード42,7 の平均）:

| 指標 | V-1（12〜18%） | surface.turf | surface.dirt | distance_range |
|---|---|---|---|---|
| proxy（P0 の近似） | 15.54% | −1.90% | +0.40% | +2.05% |
| **prize** | **15.70%** | +2.93% | +6.61% | −0.55% |
| winRate | **18.43% ✗** | +5.06% | +5.52% | +3.59% |
| composite | 15.77% | +6.00% | +6.81% | +3.61% |

**理由**:

1. **`winRate` は V-1 を割ります**（18.43% > 18%）。8〜18頭立てでは 0勝の馬が大量に出て
   順位付けがほぼ運になり、選抜が弱くなって分散が残ります。**実測で落ちた**のが決め手です。
2. **正典が既に賞金を繁殖価値の代理指標にしています** — §10.5 の NPC 種付料
   `3,000 + G1勝利数 × 8,000 + 総獲得賞金/20`。選抜指標を別物にすると市場価格と選抜圧が食い違います。
3. `composite` は V-1 では prize と同等ですが、**非能力形質のドリフトが一貫して大きい**
   （turf +6.00% / distance_range +3.61%）ため prize を採ります。

### ★K-4 で見つけて潰した、選抜圧の2つの歪み

どちらも**正典の欠陥ではなく私のプレースホルダ側の欠陥**です。

**(1) 混合番組が万能型を有利にする**（指示書 §4 が警告していた形）

出走馬を無作為に選ぶと、芝もダートも走れる馬が一方的に得をします。実測で
**surface.turf +5.31% / surface.dirt +5.98% と両方が揃って上昇**し、本規模では V-2d を割りました。

→ 出走馬は**自分に向いた馬場のレースに出る**ようにしました（正典 §10.4「自馬の出走レースは
自分で時刻を選んでエントリー」＝オーナーが選ぶ）。不向きな馬場にも 15% は出ます（頭数確保）。
結果 dirt は **+5.98% → +0.89%**。

**(2) クラス分けが絶対能力への選抜を消していた**

V-4 を満たすために出走表を能力帯（クラス）から引くようにしたところ、
**賞金が「そのクラスの中で強いか」しか測らなくなり**、下級条件で稼ぐ弱い馬と上級で稼ぐ強い馬が
同評価になりました。実測で **V-1 が 19.75% まで上昇**（合格域 12〜18%）。

→ 賞金に**クラス係数**（最下級1倍〜最上級40倍・指数）を導入。実際の競馬でも上級条件の賞金は
桁違いで、これが絶対能力への選抜圧を作っています。結果 **V-1 19.7% → 15.7%**。

> **この2件は、V-2d/V-1 という既存ゲートが無ければ気づけませんでした。**
> 「測っていないものは壊れても気づけない」（P0-fix2 の丈夫さ 650→450）と同じ構造です。

---

## 5. 素質開放率プレースホルダと K の再較正（R-7）

### 置いた値

```ts
export const PLACEHOLDER_UNLOCK = { MIN: 0.55, MAX: 0.85 };  // 指示書 §3 の例示に従う
// current = potential × 開放率（馬ごとに1回サンプルし、全能力に同じ率を掛ける）
```

能力ごとに独立にサンプルすると素質の形（SP型/ST型）が現在値で消えるため、馬単位で1回引いています。

### ★P3 で `RACE_RANDOM_K` の再較正が必要な理由（正典への追記依頼）

**K の較正値はこのプレースホルダに強く依存します。** 要因分解の実測:

```
中立化した要素                         レース内スコアCV   基準比
（中立化なし・基準）                        17.18%      100%
素質開放率（開放率の差を消す）                12.33%     71.8%   ← 最大の寄与
距離適性（全馬をレース距離ぴったりに）           15.51%     90.2%
馬場適性（全馬 50）                       16.43%     95.6%
脚質適性（全馬 50）                       17.16%     99.9%
調子・年齢・斤量                          16.39%     95.4%
```

開放率の幅を変えると 1番人気の勝率が直接動きます（K=0.12 固定・800レース実測）:

| 開放率 | 1番人気 勝率 |
|---|---|
| 0.55〜0.85（採用値） | 51.6% |
| 0.60〜0.80 | 46.5% |
| 0.66〜0.74 | 40.6% |
| 0.695〜0.705（ほぼ固定） | 38.0% |

**正典 §13.3 に「`RACE_RANDOM_K` の較正は素質開放率の分布に依存する。P3 で育成ループが
入ったら必ず再較正すること」を追記していただきたいです。** 条件を書き残さないと、
条件が変わったときに誤った結論（K=0.26）だけが生き残ります（P0 の距離定数でこれを踏みました）。

その他のプレースホルダ（すべて P2/P3 で差し替え予定）:

| 値 | 現在値 | 差し替え時期 |
|---|---|---|
| 賞金表 | 1着100/2着40/3着25/4着15/5着10 | 正典 §11 確定時 |
| クラス係数の傾き | 最上級 40倍 | 同上 |
| クラス幅 | 母集団の 6% | §10.3 番組表の確定時 |
| 不向きな馬場への出走率 | 15% | 同上 |
| 馬場状態適性 | 全馬 50（中立） | Q1 の回答後 |

---

## 6. 定数の調整履歴

| 定数 | 変更 | 理由 | 出典 |
|---|---|---|---|
| `RACE_RANDOM_K` | 0.12 → **0.26（較正値を別定数に分離）** | K=0.12 では 1番人気 51.6% で V-4 を大きく外す。§8.7 の「実装後に必ずモンテカルロで較正する」に従った | **正典は書き換えていません**。`CALIBRATED_RACE_RANDOM_K` として分離し、§13.1 の写しは 0.12 のまま |
| クラス幅 | （新設）6% | 出走表を無作為にすると 1番人気 51%。§10.3/§10.5 の「同格が集まる」設計に合わせた | 自前のプレースホルダ |
| 不向き馬場への出走率 | （新設）15% | 混合番組の万能型プレミアムを消すため | 自前 |
| クラス係数 | （新設）最上級40倍 | クラス分けで消えた絶対能力への選抜圧を戻すため | 自前 |
| AI 代行の精度4定数 | （新設） | V-8 を 93〜97% に収めるため | 自前（正典は結果の95%しか定めていない） |
| `STAMINA_WINDOW_METER` | （新設）800 | §8b.2 の数値では全馬が必ずゲージ0になる（→Q2） | 暫定解釈 |
| `STAMINA_EMPTY_METER` | （新設）400 | 同上 | 暫定解釈 |

**正典の定数（`RACE_RANDOM_K` / `INTERVENTION_CAP` / `STAMINA_*` / `SKILL_*` / 距離帯重み /
補正の値域 / 検証基準の目標値）は1つも書き換えていません。**

---

## 7. 仕様からの逸脱・解釈で埋めた箇所（**全件**）

**この一覧はコード中の `INTERPRETATIONS` から生成しています**（手で書いた一覧は必ず漏れるため）。
再生成: `npx tsx apps/cli/src/print-interpretations.ts --md`（**実行時計測で 17 件**）。
`apps/cli/test/interpretations.test.ts` が「ソースが参照するIDが登録簿にあること」と
「登録簿のIDがソースで使われていること」を**双方向に**機械検査します。

| ID | 正典 | 正典が定めていること | 開発側が埋めたこと |
|---|---|---|---|
| `I-DIST-MAP` | §8.3 | 距離適性を 0.75〜1.05 にマップ | 線形写像 `0.75 + a/100 × 0.30` |
| `I-SURF-MAP` | §8.3 | 芝/ダート適性を 0.70〜1.05 に | 線形写像 `0.70 + a/100 × 0.35` |
| `I-COND-APT-MAP` | §8.3/§5.2 | 良馬場は常に1.0 | 悪化時は `0.88 + a/100 × 0.17` |
| `I-COND-APT-SOURCE` | §5.2/§5.4 | `condition_aptitude` が定義されている | **genotype に無いため遺伝しない**。入力として外出し（→Q1） |
| `I-CONDITION-MAP` | §8.3 | 調子 0.88〜1.10 | 調子1〜5 の線形写像 |
| `I-GATE-MAP` | §8.3 | 枠順は距離とコース形態で内外の有利不利 | 直線は1.0／周回は内枠有利・短距離ほど強い |
| `I-AGE-CURVE` | §8.3 | 2歳0.88 → 4歳1.00 → 5歳末0.96 | 3点を線形補間、6歳以降は頭打ち |
| `I-SKILL-SEGMENT` | §8.5/§8.1 | 「直線区間スコア +12%」 | 区間を持てないので**距離シェアで按分** |
| `I-SKILL-POSITION` | §8.5 | 「6番手以下」「先頭」 | 位置取りが無いので**脚質で代理** |
| `I-SPEED-MODEL` | §8.7 | `baseTime = distance / averageSpeed(...)` | `averageSpeed` の中身が未定義。芝良 16.6 m/s の表を置いた |
| `I-MARGIN-LABELS` | §8.7 | 4段のみ明記（以降「…」） | 大差まで延長 |
| `I-STRATEGY-APT-MAP` | §8.4 | `適性/100` かつ 0.85〜1.05 | 線形写像（両立する式が書かれていない） |
| `I-STAMINA-WINDOW` | §8b.2 | ゲージ上限100・毎秒消費1.0〜4.8 | **数値が成立しない**（→Q2）。勝負所以降のみ消費と解釈 |
| `I-STAMINA-EMPTY` | §8b.3 | 「ゴール前にゲージ0なら −0.15」 | 「直線に入る前に尽きた場合」と解釈 |
| `I-START-CURVE` | §8b.3/§8b.6 | −0.04〜+0.03、判定幅は IQ 比例＋ラグ猶予 | 誤差0を頂点に窓の縁で下限へ落ちる線形 |
| `I-SPURT-CURVE` | §8b.3 | −0.08〜+0.09 | 最適点 残495m、±450m で下限 |
| `I-AI-QUALITY` | §8b.5/V-8 | AI 代行は手動の約95% | 精度4定数で表現しモンテカルロで較正 |

**17件。ゼロではありません。** 正典 §8/§8b は値域と意図を定めていますが、写像の形は
ほとんど定められていないため、埋めずに実装することはできませんでした。

---

## 8. 未解決の質問

`QUESTIONS_P1_20260805.md` に6件（要約）:

| # | 内容 |
|---|---|
| Q1 | **正典の内部矛盾**: `condition_aptitude` が §5.2 にあり §5.4 の genotype に無い |
| Q2 | **正典の数値が成立しない**: §8b.2 のスタミナ消費では全馬が必ずゲージ0になる |
| Q3 | **較正結果**: `RACE_RANDOM_K` を 0.12 → 0.26 に改訂するか（§8.7 の記述も要訂正） |
| Q4 | **基準の解釈**: V-8 と V-9 は素直に読むと両立しない。V-9 の母集団の定義 |
| Q5 | **承認依頼**: 賞金表のプレースホルダ（§11 未執筆） |
| Q6 | **提案**: 出走表のクラス分けを §10.3 に明文化したい（明記が無いと P2 で静かに壊れる） |

---

## 9. 私が今回踏んだ失敗（隠さずに）

| # | 失敗 | 検出方法 | 教訓 |
|---|---|---|---|
| 1 | 変異テストのスクリプトが**空振りしたのに「改変した」と出力**し、「テストが検出できない」という誤結論を出した | 2回目に事後条件を入れて発覚 | M-1 と同じ形の**3度目**。改変系は必ず「実際に変化したか」を assert する |
| 2 | 棚卸しツールが**同一ファイルに2点挿すと原本を上書き**し、プローブが本番コードに残った | ツール自身の復元検査（md5・TEMP-PROBE 走査）が検出 | 検査は機能したが**修復できなかった**。原本はファイル単位で1度だけ読むよう修正 |
| 3 | 純TS SHA-256 のパディングが `len ≡ 55 (mod 64)` のときだけ1ブロック余分だった | Node crypto との突き合わせテスト（境界55/56/63/64/65文字） | 境界を並べたテストが実際に効いた。**もし玩具ハッシュでテストしていたら気づけなかった** |
| 4 | テスト用ハッシュを `fnvHashProvider` と命名したが中身は本物の SHA-256 だった | 自己点検 | **名前が中身と食い違うと次に読む人が誤解する**。M-1 の裏返し。`pureHashProvider` に改名 |
| 5 | 長距離砲のテストで **base に IQ の寄与があることを忘れ**、比が 1.15 になると誤って書いた | テストが落ちた | 「比」で書くと他の寄与が混ざる。差で書くべきだった |
| 6 | AI 代行のスタート誤差を判定窓に比例させ、**IQ の効果を相殺**していた | 「IQ が高いほど安定する」テストが落ちた | 窓と誤差を同じ量に比例させると効果が消える |
| 7 | 10万レースで `Math.min(...xs)` が **32万要素でスタックを溢れさせた** | 本番規模の実行で発覚 | **スモークテストでは再現しない**クラスのバグ。規模を上げた実行が要る |

---

## 10. いま残っている弱点（レビュー側に重点的に見てほしい箇所）

1. **V-2d の余裕が薄い**（race 選抜で +9.08% / 上限 +10%）。P3 で選抜が強まると割る可能性があります。
2. **K=0.26 は素質開放率プレースホルダに依存**します。§5 の表のとおり、開放率を変えると V-4 が直接動きます。
3. **`race-diagnostics.ts` の「素質そのものを中立化」でも CV が 11.73% しか下がりません。**
   つまり距離・馬場・脚質適性の不一致だけで CV 11.7% あります。出走表の作り方（適性を考慮した
   エントリー）をさらに現実的にすれば K を下げられる可能性がありますが、
   **どこまでを「番組編成」としてモデル化すべきかは正典が定めていません**（→Q6）。
4. **介入は1レース1頭を「自馬」と仮定**しています。実運用では自馬0頭のレースが大半で、
   V-9 の母集団の形が変わります。§10.4 が「プレイヤー馬0頭でもレースは開催」と定めているので、
   実際の分布はもっと 1.00 に寄ります（＝V-9 は通りやすくなる方向）。
5. **`G_GATE`（ゲート巧者）は効果0**です。出遅れモデルが P1 に無いため、発動記録だけ残しています。
