# HANDOFF — P4 スターホース版レース演出：次セッション引継ぎ

作成日: 2026-08-25  
作業場所: `V:\dev\Cusor\star`  
ブランチ: `p4/race-30sec-cuts`  
HEAD: `7d62d25`

## 1. 次セッションで最初に読むもの

1. `REVIEW_P4_STARHORSE_PROGRESS_20260825.md`
2. `REPORT_P4_2D_STARHORSE_DEFAULT_20260825.md`
3. `REPORT_P4_2D_STANDINGS_ALPHA_20260825.md`
4. 必要時のみ `REPORT_P4_2D_EDIT_GRAMMAR_AUDIT_20260824.md`

過去の3D/S3/v9研究は、再開条件が明示されない限り読み直したり本線へ戻したりしない。

## 2. 現在の製品状態

通常 `/race` の既定は `starhorse-v1`。

ショット列:

```text
start-front
first-corner-front
side-drive
fourth-corner-high      # 旧v4は fourth-corner-front
homestretch-side        # 旧v4は homestretch-front
finish-line
```

旧v4は以下で比較・切り戻し可能:

```text
http://localhost:3210/race?cinematography=v4
```

既定確認:

```text
http://localhost:3210/race
```

## 3. 次セッション冒頭の確認

以下を順番に確認する。

```powershell
git status --short
git log --oneline -3
```

確認事項:

- ブランチが `p4/race-30sec-cuts`
- HEADが `7d62d25`
- `apps/web/package.json` のポート3210変更を保持している
- 製品実装に未コミット差分がない
- pushされていないかを確認する
- 3211などの検証用devサーバーが残っていないか確認する

## 4. 作業ツリーに残るもの

2026-08-25確認時、次が本線コミット外に残っている。

- `M apps/web/package.json` — ユーザー既存のポート3210変更。触らない・混ぜない
- `M tools/lib/classification.mjs` — 監査ツール登録
- 未追跡の編集文法・既存ショット監査報告書
- 未追跡の監査ツール
- 未追跡の監査テスト
- `out/` 配下の動画、連番、比較シート（gitignore対象）

一括削除、`git reset --hard`、`git checkout --`を使わない。整理が必要なら、対象を明示してユーザーまたはレビュー側の判断を得る。

## 5. devサーバーとbuildの注意

`npm run dev:web` と `npm run build:web` は同じ `apps/web/.next` を使用する。

devサーバー稼働中にbuildすると、稼働中の `/race` が500になることがある。

安全な順序:

1. devサーバーを停止
2. `npm run build:web`
3. build完了後に `npm run dev:web`
4. `/race` のHTTP 200を確認

ポートは3210。別ポート3211を一時利用した場合、作業後に停止する。

## 6. 次に行うこと

最優先は、オーナーが通常 `/race` を実際にプレイして `starhorse-v1` の印象を確認すること。

確認ポイント:

- 第4コーナーの俯瞰がレースの盛り上がりにつながるか
- 直線横追従で競り合いが読みやすいか
- 進行86〜88%の順位表が実プレイでも読めるか
- 直線終盤で1〜2頭が大きくなることが許容できるか
- finish-lineへのカメラ状態引継ぎが自然か

問題がなければ、現実装を維持する。新しい監査を先に増やさない。

## 7. 次に修正する場合の優先順位

### 優先1：実プレイで明確に見つかった不具合

黒帯、背景欠落、順位表不可読、先頭馬が長時間消えるなど、実画面で確認できる問題だけを扱う。

### 優先2：局所的な台本調整

必要性が明確な場合のみ、カット境界または既存ショットの選択を1項目ずつ調整する。

### 後回し

- 動的な接戦/独走台本
- 自馬常時追従
- target resolverの大規模変更
- カメラ状態の強制リセット
- 新素材作成

一度に複数の要因を変えない。

## 8. 戻らない論点

明示的な再開理由がない限り、以下には戻らない。

- 3D馬への移行
- S3隊列変換
- 遠景停止だけの再検証
- 2D素材の全頭白・全頭同毛色問題
- side-v9全面採用
- v7/v9ハイブリッド
- 位相差8.5%の追加最適化
- 前景ラチの再検証

これらは調査済みで、研究ブランチへ隔離または不採用判定済み。

## 9. 重要な設計判断

- 参考へ近づける編集上の主役は「上位の競り合い集団」。
- 自馬はカメラで常時追わず、HUD/マーカーで示す。
- 旧v4の問題は長さそのものより、長い直線を正面全体画で見せたこと。
- `starhorse-v1` は長いカットを維持し、向きを横へ変えた。
- 順位表は寄りで最大25%だけ減光し、最低75%の可読性を保つ。
- `finish-line` の構図差はカメラ状態引継ぎによる承認済み挙動。

## 10. 完了条件

次セッションの作業を完了とする前に、変更内容に応じて以下を確認する。

- `npm test`
- `npm run typecheck`
- 必要ならdevサーバー停止後に `npm run build:web`
- `/race` が200
- `/race?cinematography=v4` が200
- レース結果・順位が不変
- `apps/web/package.json` の既存変更をコミットへ混ぜない
- pushはユーザーの明示指示があるまで行わない

## 11. 次セッションへの一文要約

> 旧v4の「正面から12頭を小さく見せる直線」を、`starhorse-v1` で「第4コーナー俯瞰＋直線横追従」へ変更し、通常 `/race` の既定としてコミット済み。次は追加監査ではなく、実プレイで残る具体的な問題だけを1件ずつ直す。

