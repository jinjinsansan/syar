# REPORT_P4_REPLAY_20260813 — D-059 実装／★Q-P4-11（区間ペースが無い）

## 本便で正典の改訂が要る決定をしたか

**していません。**（D-059 / D-060 はレビュー側の裁定を実装したものです）

---

## 0. ★結論 — 動きますが、1点だけ「発明しなかったこと」があります

**D-059 のとおり実装し、ゲートも通ります。**
★**ただし「境界での順位が最終着順と一致する」**という性質が出ます。
**エンジンが区間ごとの速度を持たない**ためで、**そこを埋めるのは発明**になるので止めました（§3・Q-P4-11）。

---

## 1. D-059 の実装

```
エンジン = 真実   packages/race-engine/src/watch.ts
                  boundaryTimesOf()  各馬が各境界（残り800m/400m）を通過する時刻
                  finalOrderMatches() ★ゲート: 再計算の最終順 == 確定着順

描画層  = 演出    packages/render/src/replay-model.ts
                  replayPositionModel() 境界の「間」だけ補間
```

★**位置は保存していません。** 境界時刻から再計算します（裁定どおり）。

### 検査7件

| # | 内容 |
|---|---|
| 1 | ★**境界時刻には、必ず境界の位置にいる** |
| 2 | ★**演出の強さを 0 / 0.05 / 0.2 / 0.5 と変えても、境界は動かない** |
| 3 | ★**最終順が確定着順と完全一致**（D-059 のゲート） |
| 4 | **対照**: 局面の「間」では演出が実際に効いている（空振りでない） |
| 5 | ★**位置が後戻りしない**（単調増加） |
| 6 | ゲージは勝負所まで満タン（正典 §13） |
| 7 | 同じ入力から同じ位置（乱数を使っていない） |

---

## 2. ★間違えたこと（3件）

### ① 補間が単調増加でなかった

```
t + a·sin(πt)·sin(2πt+φ)   ← 微分が負になる領域がある
実測: 185.768 → 185.697 と後戻り ＝ 画面では馬が下がって見える
```

★**端で 0 になる（＝境界を動かさない）ことだけを見て、途中で戻らないことを見ていませんでした。**
→ **速度を正に保って積分する**形に変更（`v = 1 + a·sin(2πt+φ)`、`|a|<1` なら常に正）。
**検査5があったから出ました。**

### ② 既存の `replay.ts` を上書きした

`packages/race-engine/src/replay.ts` に **§8.8 の完全再現**が既にあり、テストも存在していました。
★**新規ファイルのつもりで書いて、上書きしていました。**
→ git から復元し、私の新規は `watch.ts` に分離。
**型検査が「既存テストの引数に型が無い」と言ったので気づきました。**

### ③ 定数を二重に持ちかけた

`PHASE_METERS` に 800/400 を書き写そうとし、**既存のメタテストが登録漏れとして検出**しました。

★書き写すと**二重定義**になり、**較正で片方だけ動いたとき、画面と機構で「仕掛けの受付位置」がずれます**。
**P3 で繰り返した「2か所で別々に持つ」と同じ形**です。
→ `DEFAULT_INTERVENTION_BALANCE` から取る形にしました（値を持たない）。

---

## 3. 🔴 ★Q-P4-11（新規）— エンジンが区間ペースを持っていません

### 事実

```
エンジンが持つもの : raceSec = 距離 ÷ 平均速度   ← ★1本だけ
                    各馬の走破タイム（timeSec）
                    仕掛け位置（spurtAtMeter）・バテた位置（emptyAtMeter）
★持たないもの     : 局面ごとの速度
```

そのため、境界時刻は**走破タイムからの按分**にしました。

### ★何が起きるか

```
境界時刻 ∝ 走破タイム  →  ★境界での順位 = 最終着順
                       →  追い抜きは「局面の間」でしか起きない
                       →  勝った馬は、最初から先頭に見える
```

**実際のレースは境界でも順位が入れ替わります。**

### ★なぜ埋めなかったか

区間ペースを描画側で作ることはできます。しかしそれは**速度の発明**で、

- 「どの馬がいつ前に出るか」が**画面の事実**になります
- ★それは D-059 が「エンジン＝真実」と定めた領域です

★**前便で「位置は絵の都合ではなく機構の一部」と申し上げたのと同じ理由**です。

### 選択肢（★決めていません）

| 案 | 内容 | 懸念 |
|---|---|---|
| A | このまま（境界での順位＝着順） | ★**レースが単調に見える**。勝者が最初から先頭 |
| B | **エンジンに区間ペースを持たせる** | 確定処理が重くなる／★**V-13 の較正に影響しうる**（仕掛けの効きは既に区間で決まっているため） |
| C | 描画側で区間ペースを作る | ★**発明**。D-059 の切り分けに反する |

★こちらの見立ては **B** ですが、**V-13 への影響を測らずには勧められません**。
**エンジンは既に `spurtAtMeter` を持っており、「遅く仕掛けた馬は前半が遅い」は
機構として存在します。** それを速度として出すだけなら発明にならない可能性がありますが、
**そこはレビュー側の判断**だと考えます。

---

## 4. Q-P4-10（業界共通の作法）— 受領しました

**枠番の色分け・馬番を採ります。** 意匠（ロゴ・特定団体の版面・書体・団体名）は採りません。

★ご指摘のとおり **枠番色＋馬番は「色＋形」の冗長化そのもの**で、
アートバイブル §4 の色覚多様性の要求を**強める方向**に働きます。

---

## 5. C-3 の内訳 — 次便で出します

**馬のアセットは制約ではなくなりました**（18頭で 75KB）。
→ **背景・UI・フォント・コード**の内訳は次便で測ります。
★ただし**背景は位置モデルが動いてから**でないと「何層要るか」が決まりません。

---

## 6. 照会

| # | 内容 |
|---|---|
| 🔴 **Q-P4-11（新規）** | ★**区間ペースをどうするか**。いまは境界での順位が着順と一致し、レースが単調に見えます |
| Q-P4-06 | ✅ **D-059 で裁定済み・実装完了** |
| Q-P4-10 | ✅ **D-060 で裁定済み・採用** |

---

## 7. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
2. ★**Q-P4-11 が決まれば、実際に動くレース画面をお見せできます。**
   いまのままでも動きますが、**追い抜きが局面の間でしか起きない**ので、
   「面白いか」の判定材料としては弱いと考えています

---

**テスト 668件緑 / `tsc --noEmit` 通過**
