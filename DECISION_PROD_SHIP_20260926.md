# 【STAR】本番へ何を出すか — 選り分け（2026-09-26）

- 開発側 Claude（`star-aa`）／ 宛先: オーナー
- きっかけ: オーナー「本番配備しましょう」→ ★私が ★`0082` の破壊的変更に気づいて止めた
- ★手順書 `RUNBOOK_PROD_DEPLOY_20260922.md` と同じ形（★移行 → 関門 → ワーカー → 画面）

---

## 0. 🔴 結論（先に）

★**選り分けは、やらないほうが安全です。** ★枝ごと出すのを勧めます。

★理由は 2 つで、どちらも ★**選り分けると危なくなる**ものでした:

- ★**① 部品が噛み合っています。** ★`0082`（DB）と ★`/entry` の画面は ★**セットでしか動きません**。
  ★`0089`（DB）と ★常設帯の画面も ★**セット**です。★片方だけ出すと ★確実に壊れます。
- ★**② 段 2 の作りかけは ★利用者から見て「何もしません」。**
  ★出しても ★**画面の振る舞いは変わりません**（★下の §3 で 1 つずつ確かめました）。
  → ★**外す理由がありません。** ★外す作業のほうがリスクです。

---

## 1. 🔴 避けられない「数分の窓」があります

★これだけは ★どう並べても残ります。

★**本番の画面は `enter_race` を ★`p_jockey_frozen`（jsonb）で呼んでいます**
（★`origin/main:apps/web/src/lib/entry-repo.ts:198`）。
★`0082` は ★その署名を ★**明示的に落として** ★`p_jockey_id` に変えます。

| 並べ方 | 何が起きるか |
|---|---|
| ★移行 → 画面 | ★移行を当てた瞬間から ★画面が配信されるまで ★**出走登録が失敗** |
| ★画面 → 移行 | ★画面が出た瞬間から ★移行が当たるまで ★**出走登録が失敗** |

⚠️ ★**シムで両方通す形にはしません。** ★`0082` は
★「★騎手の料金が ★クライアントの JSON から来ていた（★`feeEP: 0` を送れば高い騎手が無料）」
★という ★**憲法 3 の違反を塞ぐ**変更です。★古い署名を残すと ★穴を残します。

→ ★★**窓は 数分です**（★Vercel のビルドと配信）。★**出走の少ない時間に、意図してやる**のが筋です。
  ★他（★投票・オッズ・厩舎・記録）は ★窓の間も動きます。★止まるのは ★**出走登録だけ**です。

---

## 2. ★順番（この通りに）

```
① 本番の状態を読む
   node tools/list-unapplied-migrations.mjs --env production

② 関門: 一般登録が閉じているか（★手順書 ⑤ と同じ）
   ★開いていると、窓の間に新規の人が出走登録で詰まります

③ 移行を当てる（★全部）
   npx tsx tools/migrate.mjs --env production --yes-production

④ ★すぐに画面を出す（★main へ）— ★③ と ④ の間が「窓」です

⑤ ワーカーを入れ替える（★`tools/deploy.sh`）
   ⚠️ ★**git の push では入れ替わりません**（★CLAUDE.md・VPS の systemd）
   ★この便でワーカーも変わっています（★日次の集計・騎手の料金）

⑥ 後で読むだけ: /api/healthz で版を確かめる（★画面とワーカーは別々に古びます）
```

⚠️ ★**③ を `--to 0081` で止める形も在ります**（★壊れません）。★ただし ★それだけでは
★**デイリー EP は誰にも届きません** — ★受け取るボタンが ★main に無いからです。
★「地ならしだけ先に」ならこれ、★「今日から EP が入る」なら ★③ 全部 ＋ ④ です。

---

## 3. ★段 2 の作りかけが「何もしない」ことの確認（★1 つずつ）

★出しても利用者に影響が無いことを ★**現物で**確かめました。

| 作りかけ | 出したらどうなるか |
|---|---|
| ★`race-real.ts`（実レースを読む層） | ★**どこからも呼ばれていません**。★死んだコードです |
| ★`raceSetupFor`（場と距離から組む） | ★**どこからも呼ばれていません** |
| ★`Built` の `distanceM` / `spec` / `turn` | ★詰めるだけ。★**読む側がまだ無い**（★検査で固定済み） |
| ★`gauge: … | null` | ★実レースのときだけ `null`。★見本の道は ★**必ず値が入る**（★検査で固定） |
| ★`?venue=` / `?race=` の分離 | 🔴 ★**これだけは見た目が変わります**（★下記） |

🔴 ★**`?race=` の意味が変わりました。** ★いま ★`?race=` に何か付けて `/race` を開くと ★**止まります**
（★「まだ実レースの走行を出せません」）。★鞍の見比べは ★`?venue=` です。
✔ ★**張ってあるリンクは全部 書き換えました**（★網が引継ぎ書の 5 本を捕まえました）。
✔ ★利用者向けの画面から ★`?race=` を張っている所は ★**0 件**でした（★網で確認）。
→ ★影響は ★**開発の見比べだけ**です。

---

## 4. 🔴 もう 1 つの噛み合わせ（★これも選り分けを危なくします）

★`0089`（★出走表に `horse_id` を足す）と ★常設帯の画面が ★**セット**です。

✔ ★常設帯（`components/uma/race-strip.tsx`）は ★この便から ★`horse_id` を ★**select しています**。
🔴 ★`0089` が当たっていない本番に ★この画面だけ出すと、★`column "horse_id" does not exist` で
  ★**常設帯が落ちます**（★`entries.error !== null` で投げる作りです）。

→ ★★**移行を先に、全部当てる**（★§2 の ③）が ★**必須**です。
  ★`--to 0081` で止めたまま画面を出すと ★**常設帯が壊れます**。

---

## 5. ★私が確かめたこと・確かめていないこと

✔ ★確かめた:
- ★`0079` … ★関数 2 本の新規追加だけ（★破壊なし）
- ★`0080` … ★`create_account` は ★**引数が同じ**で中身だけ（★直書き 2,000 を 1 か所へ）
- ★`0081` … ★落とすのは ★`0080` で作った新規（★main は知らない）
- ★`0082` … 🔴 ★**破壊的**（★上記）
- ★`0089` … ★ビューに列を足すだけ（★ただし §4 の噛み合わせ）
- ★段 2 の作りかけが ★呼ばれていないこと（★§3）

⚠️ ★確かめていない:
- ★`0083`〜`0088` を ★**main の画面と 1 本ずつ**は突き合わせていません。
  ★理由: ★`0082` の後ろなので ★**単独では当てられず**、★どうせ全部当てる形になるためです。
  ★必要なら 1 本ずつ見ます（★30 分ほど）。
- ★ワーカーの周回時間（★簿 `prod-worker-pre-d071-deploy-breaks-cycle`）。
  🔴 ★**この便でワーカーが変わります**。★配備後に ★10 分サイクルに入るかは ★**実測が要ります**。
  ★`tools/check-weekly-cycle-health.mjs` を ★配備後に 1 回。

---

## 6. ★おすすめ（もう一度）

★**枝ごと出す。★移行は全部。★出走の少ない時間に。**
★選り分けは ★噛み合わせを壊すだけで、★得るものがありません。
