# 次にやること — ★コースの座標系で描く（最後の一片）

**状態**: 未着手。**これだけが残っています。**
**なぜ今やらないか**: `drawStill` の内部（層の描画順）を作り替える作業で、
中途半端に止めると**いま動いているものが壊れます**。まとまった時間で一気にやること。

---

## 1. いまの状態

```
✅ コース幾何（PR1）  packages/render/src/course.ts      楕円・コーナーの距離ロス
✅ カメラ（PR2）      packages/render/src/camera.ts      world→screen・カット表
✅ デザインの絵       apps/web/public/art/*              8層・霞・スプライト・UI
❌ この3つが**繋がっていない**
```

`/race` は**真横の直線**として描き、コーナーは**走路の境目を反らせる**ことで表現しています。
★**本物の楕円の上を走っているわけではありません。**

---

## 2. やること

**`drawStill` を「層を横帯で描く」から「コースの座標系で描く」に変える。**

| 層 | いま | あるべき |
|---|---|---|
| 空・スタンド・生垣 | 横帯 | ★**横帯のまま**（遠景なので曲がらない） |
| 奥の芝・走路・手前のラチ・ラチ内の芝 | 横帯 | ★**`courseToScreen` で描く**（コーナーで曲がる） |
| 馬 | 固定の3段 | ★**`courseToScreen(s, w)` の位置** |
| UI | 画面の座標系 | ★**そのまま**（変えない） |

### ★守ること

1. **描画順を崩さない**
   `空 → スタンド → 生垣 → 奥の柵 → 奥の芝 → 走路 → 馬 → 手前のラチ → ラチ内の芝 → UI`
   ⚠️ **手前のラチは馬より後**。ここを崩すと「芝に貼った絵」に戻ります。
2. **段の倍率は 1× と 2× だけ**（D-058）。`depth` から**整数で**選ぶこと。
   ⚠️ `scale = 0.62 + depth * 0.55` のような**連続値は違反**です（一度やって指摘されました）。
3. **奥から手前へ描く**（`courseToScreen` の `y` 昇順）。手前の馬が奥を隠します。
4. **着順に触れない**。位置は `replayPositionModel` から受け取るだけ。
   ★開始時の **D-059 のゲート**（映像の着順＝確定着順）を必ず残すこと。
5. **16進を書かない**。色は `palette.json` から役割名で引く。

---

## 3. 手順（この順でやること）

1. **走路の面と2本のラチ**を `courseToScreen` で描けるようにする
   （`/race` の旧実装 `git show 683c572:apps/web/src/app/race/page.tsx` に例があります）
2. **馬を `courseToScreen(s, w)` に置く**。`w` は脚質から（発走時は枠順で横一線）
3. **段の倍率**を `y` から整数で決める（画面の下 1/3 が 2×、それ以外 1×）
4. **手前のラチ**を馬の後に描く
5. **UI** は `drawStill` の該当部分をそのまま呼ぶ

★**1つ終えるごとに画面を見ること。** まとめて変えると、どれが原因か分からなくなります。

---

## 4. ★確認すること（毎回）

```bash
npm run typecheck && npm test        # 767件緑
```

- **直線で従来と同じに見えるか**（コーナーだけが変わるはず）
- **手前のラチが馬より前にあるか**
- **馬が非整数倍で縮んでいないか**
- ★**ブラウザに届いているか**（`ASSET_VERSION` を上げる。上げないと古い JS が使われます）

---

## 5. 未裁定の照会（レビュー側）

| # | 内容 |
|---|---|
| 🔴 **Q-P4-29** | **横位置 `w` の決め方**（これが決まらないと D-065 の本当の影響が測れない） |
| 🔴 **Q-P4-21** | `emptyAtMeter` を描画層に渡してよいか（★無いと余力が作れない） |
| 🔴 **Q-P4-22** | ゲージの定義（★逆を向いていた件） |
| **Q-P4-20 / 25 / 26** | 境界の位置／コーナーを描くか／距離ごとの勝ち時計 |
| **Q-P4-30** | ★**毛色を遺伝形質にするか**（いまは馬番から決めている見せかけ） |
| **アートバイブル** | 順位表示の「丸」を §3 の例外として認めるか |

---

## 6. ★オーナー判断待ち

1. **平時の彩度を上げるか**（`turf-2〜5` を1段。アートバイブル P-1 の改訂が要る）
2. **馬の大きさの差**（1× と 2× の差が大きすぎないか）
3. **毛色を入れるか**（`/race` `/still` の「毛色」チェック。既定は切）

---

## 7. ★オーナーへの未処理

- **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
