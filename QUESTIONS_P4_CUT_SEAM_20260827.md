# 照会 — 発走直後の横滑りと、カットの境目の方向反転（P4 レース演出）

作成日: 2026-08-27 / ブランチ `p4/race-30sec-cuts` / HEAD `2b2aa3d` / 開発側 → レビュー側・オーナー
発端: **オーナーが実画面（`/race?seed=42&cinematography=v6&contest=1.6`）を見ての指摘 5 件**
先行裁定: `REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md`
直前の便: `REPORT_P4_V6_DEFAULT_GATE_20260827.md` / `HANDOVER_P4_GAMMA_DECISION_20260827.md`

> ①ゲート発送時、左側の1枠の馬が白のガードにかぶっている
> ②ゲート発送後、一定の馬が内側へ・一定の馬が外側へ行くが、**インベーダーみたいな動き**であり、
> 　本来ならば走りながら隊列を作るべき。隊列も**馬群の固まりの中で**なっていくはず（JRA のレースは）
> ③画面が切り替わるとカメラワークが切り替わるが、1 つ前のカメラワークとスムーズに切り替えていないため
> 　**同じレースなのか分からない**（別のレースかもしれないレベル）
> ④騎手の色が同じのが 2 人いる。ピンクも緑も
> ⑤最後の直線で馬が小さくなるが、ゴールする数秒だけ**リプレイで馬を巨大化した状態でも映像を繋げて**ほしい

> ★**製品コードは 1 行も変更していません。** 追加したのは読取専用の道具 1 本（`tools/audit-cut-seam.mjs`）だけです。
> ★`Date.now()` / `Math.random()` を使っていません。着順・タイム・着差・払戻に触れていません。

---

## ★0. 先に、この便で私が犯した誤りを訂正します

★**②を「エンジン側の問題なので裁定が要る」と判断したのは誤りでした。** 実測で覆りました。

私は `packages/race-engine/src/lane.ts` の `laneAt` を**読んで**、
「①全馬が同じ包絡線で一斉に開く ②全馬が同じ周期で揺れる ③`Math.max(0, …)` により外にしか行かない」
と判断し、オーナーへ「描画層では直せない・裁定が要る」と報告しました。

★**測ったら、3 つとも違いました**（`tools/_laneprobe.mjs`・12 頭 /1600m/ 5 seed）:

| 読みで言ったこと | 実測 |
|---|---|
| 全馬が同じ向きへ一斉に動く | ★**全頭が同じ向きへ動く区間 = 0%** |
| 外にしか行かない | ★**内向きの移動が全体の 59%** |
| （角度は見ていなかった） | ★**進路の角度は中央 0.23° / 最大 4.6°** ＝ ほぼ真っすぐ |

★**エンジンの `w` は滑らかで小さい。②はエンジン側ではありません。**
★したがって **`RACE_RANDOM_K` にも `laneAt` にも触れる必要はなく、この照会は較正を一切開きません。**

⚠️ **申し送り**: ★**ソースを読んだ結果を「実測」と呼ばないこと。**
　この案件の R-30 は「測定器が画面と同じ入力を読んでいるか」を戒めていますが、
　★今回はその手前で、**測らずに構造だけ読んで結論を出しました。**
　オーナーへ一度誤った切り分けを渡しています（同じ便の中で訂正済み）。

---

## 1. 測ったこと

### 1-1. 新設した道具 — `tools/audit-cut-seam.mjs`（読取専用）

★**カットの「境目」を測る道具が存在しませんでした。**

既存の `tools/verify-camera-continuity.mjs` は、判定がこうなっています（同ファイル 58 行目）:

```js
if (prev !== null && prev.id === cur.id) { ... }   // ← 同じカット同士だけ
```

★**カットが変わったコマを黙って捨てています。** つまり「カットの*中*は滑らか」しか見ておらず、
★**境目は設計上一度も測られていません。** あちらは「カメラが瞬間移動していないか」の道具なので、
これは欠陥ではなく**担当の外**です。③はその空白にありました。

★**何を測るか**（既存道具が自分で書き残した教訓に従いました）:

> ⚠️ メートルだけで判定してはいけません。注視点が **2.51m** 跳んだだけで画面は **237px** 飛んだのに、
> m で見ていたので**見逃しました**。（`verify-camera-continuity.mjs:78-84`）

→ ★**画面の px** と ★**走行方向の符号**を主にしました。
★**合否の閾値は入れていません**（R-16: 測る側が線を勝手に引かない）。挙げるのは閾値の要らない事実 2 つだけです:

- **走行方向の反転**（境目の前後で、馬が画面を右へ進むか左へ進むかが入れ替わる）
- **共通の被写体が 0 頭**（前後どちらにも映っている馬が無い）

⚠️ ★**既定は「実画面の既定」に合わせ、実行時に必ず γ と台本を印字します**（R-8 / R-30）。
　この案件で通算 5 件目の「測定器と画面が別の入力を読む」事故を繰り返さないためです。
　**エンジン既定（γ=1.0）ではありません。**

### 1-2. ③ の実測 — 走行方向が 2 回反転している

```
17.3s  side-drive           → （馬は画面を右へ）
       ↓ ★反転
17.3s  fourth-corner-front  ← （馬は画面を左へ）    ← ★このカットは 1.33 秒しかない
       ↓ ★反転
18.6s  straight-contest     → （また右へ）
```

★**1.33 秒だけ、馬が画面上で逆向きに走ります。** 映像編集でいう「イマジナリーラインを越える」で、
人が「別のレースだ」と読む典型的な形です。★しかも**越えてすぐ戻る**ので、
一瞬だけ別のレースが挟まったように見えます。★オーナー評「別のレースかもしれないレベル」と一致します。

| seed | 反転した境目 |
|---|---|
| 42 / 253 / 90 / 2 | ★**全て同じ 2 箇所**（`side-drive → fourth-corner-front → straight-contest`） |

★**4 seed で 100% 再現**します。単一実現の観察ではありません（R-20）。

同じ境目（17.3s）の他の量:

| 量 | 値 |
|---|---:|
| 共通の馬が画面で跳ぶ量（中央値） | ★**1023px** ／ 画面幅 1280px ＝ **ほぼ画面 1 枚ぶん** |
| 画角の比 | ★**0.31×**（3 分の 1 に狭まる） |
| `fourth-corner-front` の尺 | ★**1.33 秒**（全カット中いちばん短い） |

★**台本 v5 でも同じ 2 箇所で反転します。v6 が持ち込んだ問題ではありません。**

★副産物として、**残件⑦「カットの尺を実時間で測っていない」も埋まりました**（全カットの秒数が出ます）。

### 1-3. ② の実測 — カメラを向けている先の問題

§0 のとおりエンジンの動きは滑らかです。ではなぜ横滑りに見えるか。

```
start-front（正面から）  0 〜 240m   ・ 12.13 秒
SETTLE_M（横の収束）      0 〜 250m              ← ★ほぼ完全に一致
```

★**12 頭が「12.7m の広がり → ラチ沿い」へ寄る全過程を、
　横移動がいちばん誇張される正面から、12.13 秒かけて見せています。**

`SETTLE_M = 250`（`lane.ts:22`）は D-071 が要求した値で、
**枠順で決まるゲームにしないために必要**なものです（枠を 5% 残しただけで枠×着順の相関 0.127）。
★**収束そのものは正しい。** 問題は**それを正面から全部見せていること**です。

⚠️ ★なお `race-intro.ts` の `startHorseVisualAt` は 3 列（`laneY = [452, 482, 512]`）へ整列させますが、
★**実画面では呼ばれません**（`page.tsx:1528`・`renderer === 'v2'` では `drawStartingGate` を通らない）。
★候補から外してあります。

### 1-4. ① の実測 — Z 順は正しく、色が溶けている

実画面と同じ経路で 6.5s を描き（`tools/shot-race-at.mjs`）、左端を 3 倍に拡大して確認しました。

- ★**前後関係は正しい**（馬はラチの手前に描かれている）
- ★向こう正面のラチの白い横木が、ちょうど **1 枠の騎手の胸から頭の高さ**を通る
- ★**白い勝負服と溶けて**「柵に乗っている」ように見える。1 枠だけなのは、左端で最もラチに近いため

`world-textured.ts:298-347` に「ラチを馬より手前か奥かで区間ごとに振り分ける」機構が既にあります
（脚が突き抜けないように、という別の実害への対処）。★**構図の問題で、描画層の中で閉じます。**

### 1-5. ④ — 仕様どおりでした（★オーナー判断済み・現状維持）

`bracket.ts`: **色は枠・個体は馬番**（D-060）。12 頭立てでは 枠5=馬5,6 / 枠6=馬7,8 / 枠7=馬9,10 / 枠8=馬11,12 の
**4 組が同色**になります。緑 = `frame-6 #148c46`（馬 7,8）／ピンク = `frame-8 #f596be`（馬 11,12）。

★オーナーへ「実際の JRA は**枠色が付くのは帽子だけ**で、勝負服は馬主ごとに全頭違う」という差もお伝えし、
★**現状維持（枠色のまま）とご判断いただきました**（2026-08-27）。★**この照会の対象外**です。

---

## 2. 照会事項

### ★Q-1（最重要）— ③ 4 角の方向反転を、どの形で直すか

★**この区間は過去に繰り返し差し戻されています。** 開発側の一存で作り替えません。

| 経緯 | コミット |
|---|---|
| 4 角を俯瞰にした（馬が走って見えるよう完歩 9m） | `3e1885d` |
| ★**俯瞰の「ぴょんぴょん」を撤回し、正面へ戻した** | `e009b34` |
| コーナー途中で馬だけ左右反転していたのを直した | `aa52b70` |
| ★**その修正を revert** | `8c7203d` |

★**いま反転を起こしているのは、`e009b34` で戻した「正面」そのもの**です。

| 案 | 中身 | 副作用 |
|---|---|---|
| **A** | 4 角正面を**反対側から**撮り、走行方向を → に揃える | 反転が消える。絵の種類も見せ場も減らない |
| **B** | 4 角正面を**外す**（`side-drive` から直線へ直結） | 反転が消え、境目が 1 つ減る。**4 角の見せ場が無くなる** |
| **C** | 4 角の**前に中立カット**（走行方向が画面上でほぼ 0 の真正面・真後ろ・俯瞰）を挟む | 映像の定石。★ただし**俯瞰は「ぴょんぴょん」で不合格**の経緯あり |
| **D** | 4 角正面を**長くする**（1.33 秒 → 3 秒程度） | 「一瞬の別レース」感は減るが、★**反転は残る＝根治ではない** |

★**開発側の推奨は A** です。反転そのものが消え、`e009b34` で得た「正面の絵」を失いません。
★ただし `8c7203d` で revert された修正と**同じ領域に触れる**ため、裁定を仰ぎます。

⚠️ ★**併せてお願い**: この道具は**閾値を持ちません**。
　「共通の馬が画面で何 px 跳んでよいか」を**基準として定めるか**、
　定めるなら**どの値か**をご指示ください（いまは中央 288px / 最大 1023px・画面幅 1280px）。
　★開発側が自分で線を引くと R-16 の形（測る側が通りやすい線を引く）になります。

### ★Q-2 — ② 「正面で収束の全過程を見せる」をどう解くか

★**エンジンには触りません**（§0）。カメラだけの話です。

| 案 | 中身 |
|---|---|
| **A** | `start-front` を短くし（240m → 100m 程度）、**収束の途中で**横か斜めへ切る |
| **B** | 収束中は**斜め後方**から撮る（横移動が奥行きに化けるので滑って見えない） |
| **C** | いまのまま（★「発走直後は団子で読めない」という別の実測もあり・`SCRIPT_V3` の注記） |

⚠️ ★**A / B は ③ の境目を 1 つ増やすか動かします。** Q-1 と独立に決められません。
　★**Q-1 と同時にご判断ください。**

⚠️ ★もう 1 つ、開発側では判断できない前提があります。
　`SCRIPT_V3` の注記が「**序盤を真横から撮らない**（12 頭の横の広がりが 0.85m しかなく、
　オーナー評『競艇のボートみたいな姿』になる）」と記録しています。
　★**その実測は `LANE_REVEAL_FULL_RUN` を 1.0 → 0.18 にする前のもの**です（`lane.ts:188`）。
　★**いまの広がりは 200m 地点で 7.6〜9.3m** あります。★**前提が変わっている可能性があります。**
　→ **序盤に横・斜めを使ってよいか**を、この数字を踏まえて再確認したいです。

### Q-3 — ⑤ ゴール前のリプレイ新設は P4 の範囲か

オーナー要望は「ゴールする数秒だけ、**リプレイで馬を巨大化した状態でも**映像を繋げてほしい」です。

★**本編の後に付けるリプレイ**なら、本編の時間・着順・着差に一切触れません。
⚠️ ★対して、既定に入っている `climax-choreography` は**馬そのものを動かす**演出で、
　見かけの速度が **+13.3% / −14.8%** ずれるため**オーナー不合格のまま既定に残っています**
　（`HANDOVER_P4_GAMMA_DECISION_20260827.md` §5-4）。★同じ轍を踏まない形にします。

→ ★**新規カットの追加が指示書の範囲内か**をご確認ください（CLAUDE.md「実装は指示書が出ている範囲のみ」）。

### Q-4 — ★裁定 Q-4（カメラ着手の凍結）は解除されたと読んでよいか

`REVIEW_P4_FINISH_CONTEST_VERDICT_20260825.md` Q-4 は
★**「カメラ側の調整は、写像（γ）の判断が出るまで着手しないでください」**としています。

2026-08-27、オーナーは実画面を見て
★**「主軸の考え方は 1.6 でいい。ただし確定ではなく、もう少し細部を調整したい」**と述べられました。

→ ★**この「主軸 1.6」で凍結は解除された**と読んでよいでしょうか。
　★開発側の読みは「解除された」です（Q-4 の理由は「γ でカット境界の最適位置が動くから」であり、
　γ の帯が 1.6 に定まればカット境界は動かないため）。★ただし確定ではないので、確認します。

### Q-5 — ① は描画層で直してよいか（★自明なら不要）

構図の調整（ラチの見え方 / 1 枠の位置関係）は描画層に閉じ、着順・位置・払戻に触れません。
★**指示書なしで着手してよければ、Q-1〜Q-3 の裁定を待たずに直します。**

---

## 3. 併せて報告（照会ではありません）

1. ★**測定器と実画面が γ を別々に引いている根治は、まだ未着手**です
   （`HANDOVER_P4_GAMMA_DECISION_20260827.md` §1-2 / D-052 / R-30）。
   ★共通土台（`tools/lib/race-audit-build.mjs`）に触るため、**他の全監査の数字が動きえます。**
   ★**新設した `audit-cut-seam.mjs` は暫定対処として「画面の既定」を自前で持っています**（`SCREEN_GAMMA = 1.3`）。
   ⚠️ ★**これは同じ値を 2 か所で持つ形**であり、R-30 が 5 回警告してきた形そのものです。
   　**根治のご指示をいただければ、この重複ごと畳みます。**
2. ★`audit-contest-focus.mjs` を **0 コマのとき FAIL に倒す**件（残件③）は未着手です。
3. ★**能力順プール（D-018 準拠）での測り直し**は、本便でも行えていません（残件④）。
   ★本便の測定（③②）は**カメラの幾何**を見ており、出走馬の能力構成に依存しないため、
   ★結論は変わらないと考えますが、★**これは見込みであって実測ではありません。**
4. ★★**前便（γ 決定の便）の「1181 件 PASS」は、その時点で既に失効していました。**（★2026-08-27 追記）

   ★**冒頭の申告「追加したのは読取専用の道具 1 本だけ」は不正確でした。** 実際の作業ツリー:

   ```
    M tools/lib/classification.mjs      ← ★申告漏れ
   ?? tools/audit-cut-seam.mjs
   ```

   ★`classification.mjs` の変更内容は R-23 が要求する登録簿への追記であり、問題はありません。
   ⚠️ ★**問題は、その中に前便への訂正が埋もれていたことです。**

   新設した `audit-cut-seam.mjs` を登録し忘れて `npm test` が落ち（R-24 のメタテストが正しく作動）、
   直すときに ★**前便で作られた `_gammaprobe.mjs` も未登録だった**ことが判明しました。
   ★`tools/_*.mjs` は `.gitignore` で追跡外ですが、★**この検査は作業ツリーの `tools/*.mjs` を見る**ので
   対象から外れません（登録簿には既に `_m4.mjs` `_v17probe.mjs` `_railprobe.mjs` が載っています）。

   → ★**前便の引継ぎ書が掲げた「119 ファイル / 1181 件 PASS」は、`_gammaprobe.mjs` を作った
     時点で失効していました**（R-23: 合格はコードが変われば黙って失効する）。
   → ★**レビュー側はその数字を受け取る立場なので、コードコメントではなくここに書くべきものでした。**

   ★是正後: `npm test` **119 ファイル / 1181 件 PASS**（exit 0）/ `npm run typecheck` クリーン（exit 0）。

   ⚠️ ★**併せて、私自身の手順の誤りを 1 件**: 最初 `npm test | tail -12` で流したため、
   ★**パイプが終了コードを飲んで `exit 0` と表示され**、危うく「通った」と読むところでした。
   ★正典 R-27 の系（**本番に向ける操作の出力をパイプで切らない**）と同じ形です。以降パイプなしで流しています。

   ★**申し送り**: ★**申告は「製品コードか否か」で選ばず、`git status` に出るもの全部を書くこと。**

---

## 4. 再現コマンド（すべて読取専用・dev サーバー不要）

```powershell
# ③ カットの境目
npx tsx tools/audit-cut-seam.mjs --seeds 42 --script v6 --gamma 1.6
npx tsx tools/audit-cut-seam.mjs --seeds 42 --script v5 --gamma 1.6     # ★v5 でも同じ 2 箇所
npx tsx tools/audit-cut-seam.mjs --seeds 253,90,2 --script v6 --gamma 1.6

# ② 横位置の動き
npx tsx tools/_laneprobe.mjs        # ★tools/_*.mjs は .gitignore 対象・追跡外

# ① 発走の画（左端を 3 倍に拡大したものが _crop-gate1.png）
npx tsx tools/shot-race-at.mjs 6.5 7.4 7.9 8.3
```

★オーナーが見た画面: `http://localhost:3210/race?seed=42&cinematography=v6&contest=1.6`
⚠️ `contest=` を省くと 1.3 に、`cinematography=v6` を落とすと v5 に**黙って**落ちます。
