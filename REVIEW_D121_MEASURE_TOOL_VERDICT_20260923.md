# 【STAR】裁定: D-121 ③ の測る道具（利用者の血統の伸び）— 2026-09-23

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 照会: `QUESTIONS_D121_MEASURE_TOOL_20260923.md`（開発側 `star-aa`）
- 運用: オーナー指示「承認不要で自立的に進める」

## 判定の一覧

| # | 論点 | 判定 |
|---|---|---|
| M-1 | 分布の物差し（いまの NPC 現役の素質の合計の中で何 % 点か・ID と馬名を出さない） | ✅ |
| M-2 | 「頭打ちに達した」の定義 | ✅ **(c) 両方を並べる**。★(a) は**参考の列**として、使った数（2 世代・幅 0）を出力に印刷して出す。★合否の判定にしない（§1） |
| M-3 | 近交係数と虚弱の割合も並べる | ✅ |
| M-4 | 流す時期（公開後・オーナーの読み取り） | ✅。★ただし**毎回の結果を日付つきでファイルに残す**（§2） |

## 1. M-2 — (c)。(a) は参考の列にとどめる

- この道具の目的は、**D-121 の B（較正の見直し）をオーナーが判断する材料**を出すこと（D-121 条件 ③）。★判断するのはオーナーで、道具ではない
- (a) の「2 世代続けて上回らない」「幅 0」は、照会自身が書くとおり**発明した数**。★その数で「頭打ちの人は ○ %」と 1 つの数にまとめると、★**発明した数が判断を決める**（CK-12 の「印刷した既定値」の族）
- **決定**:
  - 主の出力は (b) の**高さ**（利用者ごとの最良の仔の % 点の分布）と、**世代ごとの推移**（母系の世代で並べた最良の % 点）をそのまま並べる
  - ★比べる目安として、模擬の値（N0.5 の頭打ち 60〜70% 点・O の 85〜90% 点・`REPORT_Q2_LINEAGE_SIM_20260922.md`）を**出どころつきで**並べる（★模擬は EP の制約を外した上限だった、と添える）
  - (a) は**参考の列**として出してよい。★ただし使った数（世代数・幅）を**出力の先頭に印刷**し、引数で変えられるようにする。★「判定できない」（世代が 3 つに満たない）の人数も必ず出す（CK-14 — 0 人を「頭打ち 0 %」と読ませない）
- ★利用者の数が少ないうちは、分布の数字が大きく揺れる。★**人数 n を必ず並べる**

## 2. M-4 — 公開後に。結果を日付つきで残す

- 過去の NPC の分布が DB に残っていない（照会の ⚠️）。★この道具を流すたびに、**その日の NPC 現役の分布（% 点を決める物差しそのもの）を、結果と一緒に日付つきのファイルに残す**（例 `evidence/d121/<日付>.json`）。★DB には書かない（読むだけの道具のまま）
- ★そうすれば、後から「物差しが動いたのか、利用者の血統が伸びたのか」を分けられる（V-2a で大きく動かない見込みを、見込みのままにしない）
- ★ファイルには ID と馬名を書かない（照会どおり）

## 3. M-3 — ✅

- 近交係数の平均と虚弱の割合を並べる。★模擬では、90% 点を超える利用者は近交を重ねた人だけだった（D-121）。★**実際の利用者も同じ道をたどっているか**が、B を判断するときにいちばん効く材料になる

## 4. 追記（2026-09-23）— 実装 ✅

- ✔ `tools/measure-lineage-plateau.mjs`（READONLY）・部品 `tools/lib/lineage-measure.mjs`。主の出力は高さ（利用者ごとの最良の % 点の分布）と母系の世代ごとの推移。模擬の目安を出どころつきで並べた
- ✔ (a) は参考の列で、使った数（`--generations 2`・`--width 0`）を先頭に印刷・引数で変えられる。平ら／伸びている／判定できないの人数と、全段の n を出す（§1）
- ✔ その日の物差しと結果を `evidence/d121/<日付>-<接続先>.json` に残す（ID と馬名なし・DB に書かない・日付は DB の時計）（§2）
- ✔ 単体の試験 7 件。staging で流した（物差し n 2,202・利用者 0 人で全段 n 0）
- ⚠️ 記録のファイルには NPC の素質の合計の全数が入る。★利用者に見せるものではない（D-114 は画面の話）が、★**このファイルを画面や外部に出さない**ことを道具の註記に書いておくこと
