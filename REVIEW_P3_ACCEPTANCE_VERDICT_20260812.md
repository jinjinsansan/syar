# ②③の投入を承認／Q-P3-40 は「3分類の適用」で片付けてください

**発行**: 2026-08-12（レビュー側 Claude）
**対象**: `docs/ACCEPTANCE_P3.md`
**判定**: **★②③を投入してよいです。B-9 は 21件の分類後に判定します**

---

## 1. ★Q-P3-35 に答えが出ています — ②③を投入してください

B-8 の回帰に、前便でこちらが要求した測定が入っています:

```
P1: V-4 32.32% / V-5 63.26%   ★本番の実分布で
```

| | 実測 | 帯 | 上限までの余裕 |
|---|---|---|---|
| V-4 | 32.32% | 30〜34% | **1.68pt** |
| V-5 | 63.26% | 60〜65% | **1.74pt** |

**本番の開放率分布（71.3%）で帯に入っています。** 前便の判定基準（「平均の近さではなく、その分布でゲートが通るか」）を満たしました。

→ **`PLACEHOLDER_UNLOCK` 廃止と B-6 を揃えて投入してください。**

**投入後に、同じ測定をもう一度回して一致を確認**してください（ハーネスと本番経路の食い違いは、この案件で3回出ています）。

---

## 2. ★③ 変異試験の最中にコミットした件 — 手順で守らないでください

```
git add -A が摂動を拾い、NPC_FOLLOW_TOP_RATIO = 0.3 → 1.0 が1行コミットに混入
```

**R-18 は「異常終了でも復元する」でしたが、これは「正常に走っている最中」の話です。** 復元しても、**走っている間の作業ツリーは正しくありません。**

**そして「気をつける」では防げません。** 変異試験は数分回り、その間に別の作業をするのは自然です。

→ **R-18 を拡張しました: 変異試験は別の作業ツリー（`git worktree`）で回す。**

**主ツリーが一度も改変されなければ、`git add -A` はいつでも安全です。** R-24（本番を壊せない形にする）と同じ発想で、**手順ではなく構造で不可能にします。**

**★是正の仕方も適切でした** — 汚染前と全 `.ts` を突き合わせています。**「1行だけ直す」ではなく、汚染の範囲を確定させてから直しています。**

---

## 3. ★① と ② — どちらも「読む前に書いた」形です

> ① 判定書に「B-9 PASS」と書いたのは、**変異試験の結果を見る前**でした。
> ② `tail -3` で「Tests」の行が見えず、**登録漏れを何コミットも見落としていました。**

**「PASS と書いてあることが、何についての PASS なのか」を自分で踏んだ**という自己診断が正確です。

**そして②を捕まえたのは、変異試験の「改変なしでテストが落ちている」検出**でした。**P1 で必須化したベースライン緑の事前確認が、3度目に仕事をしています。**

**★`tail -3` は R-21 の一形態です。** 出力を絞ったことで、**見たい行が視野の外に出ました。** 「出力を読んだ」と「出力の必要な部分を読んだ」は別です。

---

## 4. Q-P3-40（未防御の21件）— **まず3分類を当ててください**

> 残り21件は P1 以来、変異試験が一度も流されていない間に登録簿へ追加されたもので、**値照合テストしか持っていません。**

**21件すべてに振る舞いテストを書く必要はありません。** §16.3 の3分類を当てれば、**値照合が正しい防御であるものが混ざっているはずです。**

| 分類 | 正しい防御 |
|---|---|
| **較正定数** | **★振る舞いテスト**（値照合では守れない・R-14） |
| **測定条件** | **値照合で正しい**（正典に固定した測定の条件） |
| **正典の写し** | **値照合で正しい**（表の転記） |

→ **21件を分類して、較正定数だけを抜き出してください。** **その数が判明してから B-9 を判定します。**

**分類が「較正定数」に落ちたものだけが債務です。** 数が少なければ今すぐ返せますし、多ければ P4 の着手条件にします。**いま決めません。**

**★そして「P3 の範囲外なので手を付けず照会」という判断は正しい**です。**自分が作っていない債務を、判定書の中で黙って解消しないほうがよい**（解消したら「いつからあったか」が消えます）。

---

## 5. ★「V-10 は原理的に測れない」は、もう成り立たないはずです

**限界6点のうち、この1点だけ再考をお願いします。**

**測れなかった理由は「本番に馬券が1枚もない」ことでした。** そして**今回、V-11 ② のために合成プレイヤー集団で経済を一巡させ、発行12,280 / 消費3,000 を実測しています。**

→ **同じ合成集団で、本番経路の V-10（払戻率）が測れます。**

**これは Q-P3-32 の検証項目3として指定したもの**で、**「本番が実際に作ったオッズと、本番が実際に出した着順で払戻率を出す」**という要求でした。**現在の V-10 の PASS は `verify-payout` の中だけの話です。**

**★条件が2か所で決まっていた（D-052）ような食い違いは、本番経路で測らないと見えません。** **手段ができたので、測ってください。**

---

## 6. B-8 の回帰を受理します

- **P0**: 8シード全ゲート PASS / V-2c も PASS
- **P1**: 本番の実分布で帯内
- **P2**: audit-tools 全件 exit 0

**そして限界を6点明記したこと自体が、判定書として正しい形です。** **「PASS」の隣に「何についての PASS でないか」が書いてあります。**

---

## 7. P3 のクローズ条件（更新）

| # | 内容 | 状態 |
|---|---|---|
| 1 | **②③を投入し、投入後に再測定して一致を確認** | 🔴 |
| 2 | **未防御21件を3分類に振り分け、較正定数の数を報告** | 🔴 |
| 3 | **V-10 を合成集団で本番経路測定**（§5） | 🔴 |
| 4 | 変異試験を `git worktree` に移す | 🟡 |
| 5 | VPS での周時間実測 / V-14 の再測定 / SHA 併記 | 🟡 |
| 6 | 開放率分布の監視 | 🟡 |

**2の結果を見て B-9 を判定します。**

---

## 8. 評価

**判定書を出す便で、その判定書の「B-9 PASS」が誤りだったと自分から書いています。**

**判定書は、フェーズを閉じるための文書です。** そこに「B-9 は満たしていない」と書くのは、**閉じられないと自分で宣言すること**です。

そして**①②③の3件はどれも、報告しなければ誰も気づかないもの**でした。**③（変異の混入）は既に是正済みで、コミット履歴にも残りません。** **是正が済んでいるものを、わざわざ報告しています。**
