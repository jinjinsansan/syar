# REPORT — **WK-5**: staging に常駐の仕組みを置く（★1 枚）

- 日付: 2026-09-19
- 発行: 開発側
- 判断: `OWNER_DECISIONS_20260919.md` **B-1**（★オーナー承認済み）
- 求められたもの: 「★staging の systemd unit を用意する、★または ★**要る権限と手順を 1 枚で報告する**」

---

## 0. 🔴 ⚠️ **先に — ★手順書が、事故を起こした unit を入れろと書いていました**

★unit を用意しようとして、★**まず直すべきものが見つかりました。**

| | |
|---|---|
| ★リポジトリにあった unit | ★**2 本**（`tools/star-worker.service` と `deploy/star-worker.service`） |
| 🔴 ★`deploy/` のほう | ★`ExecStart=/opt/star/node_modules/.bin/tsx apps/worker/src/main.ts` |
| 🔴 ★それは何か | ★**2026-08-09 に 7 分の停止を起こした形そのもの**です |
| 🔴 ★`deploy/README.md` §5 | ★**その壊れたほうを install しろ**と書いていました |

★仕組み: ★`npm ci --omit=dev` が `tsx` を消す → ★`ExecStart` のバイナリが無い → ★**203/EXEC で即死**
→ ★10 秒間隔で 4 回 試行 → ★`StartLimitBurst` に当たって systemd が諦める → ★**7 分 停止**（正典 **D-043**）。

### ⚠️ ★**2026-09-14 の監査が、既に指摘していました**

> `REPORT_AUDIT_20260914.md:127` — 「`deploy/star-worker.service:28` が tsx で原本を動かしており、D-043（バンドル版）と食い違う」

★**5 日 直っていませんでした。**
🔴 ★**「指摘されたのに直っていない」が 2 度目**です — ★CLAUDE.md が「Render の記述」でまったく同じことを書いています。

### ✅ ★直したこと

| ① | ★`deploy/star-worker.service` を **削除**（★unit は `tools/` の 1 本だけ・D-052） |
|---|---|
| ② | ★`deploy/README.md` §5 を ★**`tools/star-worker.service` を入れる**手順に差し替え |
| ③ | ★README 冒頭の「Render Background Worker 等」に ★**「誤り」と添えた**（★配備先は VPS の systemd） |
| ④ | ★検査 `apps/cli/test/systemd-unit-single-source.test.ts`（7 件）で ★**写しの復活を落とす** |

★検査が見るもの: ★unit が 1 本だけ ／ ★`ExecStart` が `tsx` も `node_modules` も含まない ／
★`/opt/star-current/dist/worker.cjs`（★`/opt/star` 直下ではない）／ ★`TimeoutStopSec ≥ 275`（★1 周の長さ）／
★`StartLimit*` が `[Unit]` にある ／ ★**手順書が指す `.service` が tools/ の 1 本**／ ★Render に訂正が添えてある。

> ⚠️ ★検査を書くときに ★**2 回 落ちました**。★どちらも ★**註記が検査を狂わせた**形です:
> ★① `tmp/es-before/` の写し 2 本を拾った（★`tmp/` は gitignore の作業場）
> ★② `indexOf('[Service]')` が ★**註記の中の「`[Service]` に書くと無視される」**に当たった

---

## 1. ★いまの状態（★staging）

| | |
|---|---|
| ★unit | ✅ **ありません** |
| ★どう動いているか | ★**誰かが端末でコマンドを流している間だけ** |
| ★止まった時期 | ★**2026-09-16 以降 動いていません**（★おそらく端末を閉じたため） |
| ★本番（VPS） | ★`star-worker`（systemd）。★配備は `tools/deploy.sh` が `/opt/star-current` のリンクを張り替える |

⚠️ 🔴 ★**`git push` では入れ替わりません。** ★今日 204 コミットを push しましたが、★**本番のワーカーは 1 ビットも変わっていません。**

---

## 2. ★要るもの（★開発側からは実行できません）

### ★権限

| # | 要るもの | ★誰が |
|---|---|---|
| 1 | ★staging の VPS（★または本番と同じ機械の 2 つ目の unit）への **ssh** | ★オーナー |
| 2 | ★`sudo`（★`/etc/systemd/system/` への書き込み・`systemctl`） | ★オーナー |
| 3 | ★`/etc/star/worker-staging.env` を **600・所有者 star** で置く | ★オーナー |
| 4 | ★staging の `DATABASE_URL` と鍵（★**リポジトリにも履歴にも置かない**） | ★オーナー |

🔴 ★**開発側の端末からは 1 つもできません。** ★秘密も持っていません（★持つべきでもありません）。

### ★手順（★本番と同じ機械に 2 つ目として置く場合・★最小）

```bash
# ① 秘密（★本番とは別ファイル。★★`STAR_ENV=staging` を必ず入れる）
sudo install -m 600 -o star -g star /dev/null /etc/star/worker-staging.env
sudo -e /etc/star/worker-staging.env     # DATABASE_URL / STAR_ENV=staging / …

# ② unit（★本番の写しを 1 か所だけ変える）
sudo cp /opt/star-current/tools/star-worker.service /etc/systemd/system/star-worker-staging.service
sudo sed -i 's#/etc/star/worker.env#/etc/star/worker-staging.env#' /etc/systemd/system/star-worker-staging.service
sudo sed -i 's#SyslogIdentifier=star-worker#SyslogIdentifier=star-worker-staging#' /etc/systemd/system/star-worker-staging.service

# ③ 起動
sudo systemctl daemon-reload
sudo systemctl enable --now star-worker-staging
journalctl -u star-worker-staging -f
```

> 🔴 ⚠️ ★**`app_environment` を先に `staging` にしておくこと。**
> ★ワーカーは環境が合わないと ★**起動しません**（★意図的な挙動・§14.6）。
> ★**これが、本番の DB に staging のワーカーを向ける事故を止めている唯一の仕組み**です。

> ⚠️ ★**同じ機械に 2 つ置くなら、`tools/deploy.sh` は staging を知りません。**
> ★`STAR_UNIT=star-worker-staging` を渡せば restart の相手は変わりますが、
> ★**リンク（`/opt/star-current`）は 1 本しかありません** — ★**本番と同じ木で動きます。**
> ★木を分けたいなら ★`STAR_RELEASES` / `STAR_CURRENT` も分ける必要があり、★**それは別の判断**です。

---

## 3. ★置くと測れるようになるもの（★いま測れていないもの）

| ① | ★**プールが腐るか**（★4 時間 ＝ 1 ゲーム内週で見える） |
|---|---|
| ② | ★**G1 が確定して勝利数が増える**実証（★いまは合成でしか見ていません・VP-2） |
| ③ | ★画面が「今週」を読めるか |
| ④ | 🔴 ★**CC-1 ⑤ の「配備で実測」**（★今日 出した 98.93% は ★**合成**です） |
| ⑤ | 🔴 ★**DS-5 ② の G1 の窓**（★3 時間 48 分）が、★**実時間で本当に動くか** |
| ⑥ | 🔴 ★**DS-5 ③ の直し**（★引退した登録馬の取消）が、★**週送りと本当に噛み合うか** |

⚠️ ★**④⑤⑥ は、今日 入れたものです。** ★**どれも配備では一度も動いていません。**

---

## 4. 🔴 ★これは CI-1 と同じ根です

| | ★機械が続けるべきこと | ★いま続けているもの |
|---|---|---|
| **CI-1** | ★検査を回す | ★**人**（★「赤のままコミット」が今日 3 回） |
| **WK-5** | ★ワーカーを動かす | ★**人**（★2026-09-16 に端末を閉じて止まった） |

★どちらも ★**「人が続けている」ことが、そのまま停止の原因**になっています。

---

## 5. ★開発側がやったこと／やっていないこと

| ✅ やった | ★unit の写しの削除 ／ ★手順書の訂正 ／ ★検査 7 件 ／ ★この 1 枚 |
|---|---|
| 🔴 **やっていない** | ★**staging への install・起動**（★権限が無く、★外向きの操作です） |
| ⚠️ | ★**`tools/deploy.sh` は staging 用に直していません** — ★木を分けるかが決まっていないため（★§2 の但し書き） |
