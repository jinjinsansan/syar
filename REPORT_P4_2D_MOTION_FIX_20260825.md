# REPORT — 走りの不具合（跳ね・巻き戻し）の原因と修正（2026-08-25）

オーナー指摘 #1 / #2 / #3-b（演出要望 #3-a / #4 は別件）。
★採否は書いていません。

---

## 1. 指摘と、測って分かったこと

| | 指摘 | ショット | 測定結果 |
|---|---|---|---|
| #1 | 馬がぴょんぴょんする | `fourth-corner-high` | **1 完歩で画面上を 37px しか進まない**（旧 v4 の 4 角は 119px） |
| #2 / #3-b | 走行中に後ろへ戻る／巻き戻しのよう | `homestretch-side` | 画角が **1 コマで 13.00° → 5.96°** に跳び、馬が **1113px 後退** |

最初に疑った「脚の位相（`meters + Δ`）の巻き戻り」は**外れ**でした。
実測では v5 の位相の後退は最大 0.07m（完歩 0.01 回ぶん）で、旧 v4 の 1.69m より**むしろ小さい**。
原因は位相ではなく**カメラ**でした。

---

## 2. #2 / #3-b の原因と修正（実施済み）

`packages/render/src/broadcast-v2-scene.ts` の「争っている馬を画角に収める」処理が、
**「先頭から 12m 以内か否か」の在/不在**で数えていました。

```ts
for (const h of horses) {
  if (lead - h.s <= spec.withinM) tail = Math.min(tail, h.s);   // ← 境目で跳ぶ
}
```

馬がこの境目をまたぐと、収めるべき幅が 1 コマで変わり、画角が飛びます。

**実測（seed 42・`homestretch-side`）**

| 表示秒 | 画角 | 注視点の進み | 枠12 の画面上の位置 |
|---|---|---|---|
| 19.830s | 13.00° | +0.55m/コマ | −71px |
| 19.863s | **5.96°** | **+2.2m/コマ** | **−1194px** |

★同じ「在/不在で跳ぶ」問題は、以前**注視点の横位置**で直した記録がコード内に残っていました
（「順位が入れ替わると画面が 237px 飛ぶ」）。今回は画角の側に同じ形で残っていたものです。

### 直し方

端の馬の効きを**連続**にしました。`withinM` までは丸ごと数え、そこから同じ幅をかけて 0 へ落とします。
境目をまたいでも寄与が少しずつ変わるだけになります。カメラの**位置・台本・着順・馬の位置**は変えていません。

### 効果（seed 42・先頭 3 頭が画面上で後ろへ動いた量）

| ショット | 直す前 | 直した後 |
|---|---|---|
| `homestretch-side` | 最大 **1113px**（19.83s・カットの途中） | 最大 **413px**（12.10s・**カットの境目**） |
| `fourth-corner-high` | 最大 171px（8.80s・カットの境目） | 変わらず |
| `finish-line` | 最大 7px | 最大 101px（30.70s・**カットの境目**） |

**カットの途中で跳ぶ箇所は無くなりました。** 残っているのはすべてカットの境目で、
そこは 0.28 秒のディゾルブで繋いでいる区間です。

---

## 3. #1 の原因（★直していません・判断が要ります）

`fourth-corner-high` では、脚は 2.07 完歩/秒で回るのに、馬は画面上を 77px/秒しか進みません。
＝ **1 完歩で 37px**。旧 v4 の 4 角（`fourth-corner-front`）は 119px でした。

| ショット | 1 完歩で進む距離 | 画面上の速さ | 脚の周期 |
|---|---|---|---|
| `fourth-corner-front`（旧 v4） | 119px | 255px/秒 | 2.14 完歩/秒 |
| **`fourth-corner-high`（現行）** | **37px** | **77px/秒** | 2.07 完歩/秒 |
| `side-drive` | 18px | 39px/秒 | 2.22 完歩/秒 |
| `homestretch-side` | 3px | 7px/秒 | 2.42 完歩/秒 |

俯瞰は走路方向の動きが強く圧縮されるため、脚の回転に対して体が進みません。
これは**カメラの見込み角の問題**で、脚の位相の不具合ではありません。

### 直すなら（★どれも未実施・オーナー／レビュー判断）

1. **そのショットだけ見た目の完歩を長くする**（脚をゆっくり回す）
   … `BROADCAST_STRIDE_M` を 7m → 9m にした前例と同じ考え方。ただし**カットをまたぐと脚の速さが変わります**
2. **俯瞰の画角・位置を変えて走路方向の動きを増やす**
   … カメラ値の変更にあたるため、指示がなければ触りません
3. **そのままにする**

---

## 4. 変更ファイル

| ファイル | 変更 |
|---|---|
| `packages/render/src/broadcast-v2-scene.ts` | 争っている馬の数え方を在/不在から連続へ（#2 / #3-b） |
| `packages/render/test/contender-frame-continuity.test.ts`（新規） | 画角が跳ばないことの検査 3 件 |

台本・カメラ定義値・HUD・レース結果・着順・馬の位置は変更していません。

## 5. 検証

- `packages/render/test/contender-frame-continuity.test.ts` 3 件パス
  - ★「先頭差 1m あたり画角が何度変わるか」で見ています（刻みの取り方に左右されない形）。
    直す前は境目で**不連続**、直した後は最大 2.0°/m
- `npm test` **112 ファイル / 1121 件パス**
- `npm run typecheck` エラーなし
- commit・push はしていません
