# STAR 仕様書 v2.0（正典）

> **プロジェクト**: 開発コード「STAR」— ポイ活型オンライン競馬育成ゲーム
> **状態**: スケルトン（§0 憲法のみ確定済み。§1 以降は章ごとに執筆 → オーナー承認で確定させる）
> **作成**: 2026-08-04（レビュー側 Claude 起草・オーナー承認済み骨子に基づく）
> **本書が正典**。旧文書の扱い:
> - `SPEC.md` / `CLAUDE.md` / `AI_PROMPT.md`（第1世代・スターホース再現）= **廃棄**（実在馬名・商標名・クライアント完結が憲法違反）
> - `GORILLA_STABLE_SPEC_v1.0 / v1.1 / v1.2`（ゴリラ予想連携版）= **参考資料**（ゴリラ予想は別プロジェクト。本書は各章で採用部分を明示して取り込む）

---

## §0. 憲法（確定・全開発はここに従う）

### 0.1 権利方針

| 区分 | 扱い |
|---|---|
| ゲームシステム・ルール・ループ構造（配合→調教→レース→引退→繁殖） | アイデアであり保護対象外。参考にする |
| グラフィック・UI意匠・演出・音 | **完全オリジナル**（レトロ2Dドット方向） |
| 実在競走馬名・実在血統・騎手/馬主名 | **使用しない**。全馬名は自動生成 or ユーザー命名（実在馬NGリストで両方を検閲） |
| 実在競馬場名・実在レース名 | 架空名に置換 |
| 「スターホース」「WBC」「SWBC」「ライド馬券」「SH」等の他社固有名称 | **一切使用しない**。プロダクト名・UI文言・SEO・コード内命名（変数/テーブル名）にも出さない |

### 0.2 ポイント5原則（2026-08-03 オーナー同意・不可侵）

二種ポイント制: **参加ポイント**（遊ぶ燃料）と**賞金ポイント**（勝って得る・景品交換可）。

1. **参加ポイント**: 無償取得のみ・購入不可・換金/景品交換不可・譲渡不可。外部獲得経路は「広告主負担型」（広告視聴・オファー成約等、ユーザーがお金を払わない形）に限定する。**ユーザーが金銭で買える経路を作った瞬間、賭博構造が成立する — 絶対に作らない**
2. **賞金ポイント**: 景品交換は可。ただし現金・暗号資産への交換は不可・ユーザー間譲渡不可
3. **二種の完全分離**: 参加P→賞金Pの変換は不可。賞金P→参加Pの還流は**弁護士判断まで不可**（§0.3）
4. **サーバー権威**: ポイント台帳・レース結果・乱数はすべてサーバー側。クライアント計算は信用しない
5. **弁護士レビューをローンチ前のブロッキングゲートとする**（§0.3）

### 0.3 法務の位置づけ（2026-08-06・オーナー判断で更新）

**開発のブロッカーではない。** ポイ活型の予測市場サービス（ポイポ等）が同一形式で通常営業しており、**本作もその形式に倣う**というのがオーナーの判断である。**公開前に弁護士見解を改めて取得する**が、それまで開発を止めない。

> **★番人としての記録 — 先行事例が根拠になる条件**
>
> 「他社が同じ形式で営業している」という論拠は、**こちらの構造が向こうと同じである限りにおいて**有効である。したがって以下が崩れた瞬間、**この論拠は使えなくなる**:
>
> | # | 崩れてはいけない構造 | 崩れると何が変わるか |
> |---|---|---|
> | **S-1** | **参加ポイントをユーザーが金銭で購入できない** | 「無償参加」が消え、**金を払って偶然の結果に賭ける**構造になる。先行事例と別物 |
> | **S-2** | 参加ポイントの流入が**広告主負担型**（ユーザーは払わない） | 同上。他サービスで課金して得たポイントの変換も実質購入 |
> | **S-3** | 賞金ポイントが**現金・暗号資産に交換できない** | 換金性が上がるほど賭博性の評価が変わる |
> | **S-4** | ポイントの**ユーザー間譲渡ができない** | 二次市場が立つと性質が変わる |
> | **S-5** | 賞金P→参加Pの**還流がない**（D3） | 価値あるものを再び賭ける構造の再構成になりうる |
>
> **これら5点は §0.2 のポイント5原則と同じもの**である。つまり **5原則を守っている限り先行事例との比較が成り立ち、破った瞬間に成り立たなくなる**。機能追加の提案が来たら、まずこの表と照合すること。

**公開前の弁護士確認では以下を見てもらう**（§17.1 に詳細）:

- 賭博罪（刑法185条）— 無償参加構造の成立性
- 景表法 — 賞金ポイント→景品の景品類該当性・上限額・表示義務
- 資金決済法 — 二種ポイントの前払式支払手段該当性
- 賞金P→参加P還流の可否（§0.2-3）
- ギフト券系景品の可否（現金等価性の評価）

### 0.4 確定済みの基本決定

| 決定 | 内容 | 日付 |
|---|---|---|
| D-001 | ゴリラ予想プロジェクトと完全切り離し（コード・経済・文言すべて） | 2026-08-03 |
| D-002 | ポイント5原則の採択 | 2026-08-03 |
| D-003 | プレイ形態 = **オンライン共通レース型**（全プレイヤーが同じレースに出走・ベット） | 2026-08-03 |
| D-004 | プロジェクト名 = 開発コード「STAR」（正式名称はリリース前に商標調査の上で決定） | 2026-08-03 |
| D-005 | 実在馬名・実在レース名は使用しない | 2026-08-03 |
| D-006 | レース中リアルタイム介入を v1 から採用（安全条件7点付き・§1.5/§8b） | 2026-08-04 |
| D-007 | レースサイクル = 10分（1日144R）・1ゲーム内週 = リアル4時間（§10/§7.1） | 2026-08-04 |
| D-008 | **平均回帰を採用**（REGRESSION_RATE=0.20・MUTATION_SD 45→90・§6.4）＋ **V-2 を「平坦化・天井余裕・長期V-1」へ再定義**（§13.2）。血統の優位は「築けば永続」ではなく「選び続ける腕前が保つ」形になることをオーナーが承認 | 2026-08-04 |
| D-009 | **回帰中心を形質ごとの創始水準にする**（丈夫さ650・気性50・適性55・距離2100/700）。値域一律の45%では丈夫さが650→450へ恒久移動し故障率の較正が壊れるため。併せて **V-2d（非能力形質の水準維持）・V-2e（距離分化のゲート化）**を新設 | 2026-08-04 |
| D-010 | **距離系の変異幅を平衡式から導出**（sd 312/104・clamp 520/173）し、**回帰率は全形質共通の 0.20 に統一**。P0-fix の「距離だけ回帰0.01」は sd を据え置いたことによる誤った結論だった（平衡SD = sd/√(2r−r²) より、sd を追随させれば r を上げても分散は保たれる） | 2026-08-04 |
| D-011 | **大物覚醒を能力5種のみに限定**。気性（高いほど気性難）が覚醒で悪化し、距離適性が万能型へ押される逆効果を断つ（§6.3） | 2026-08-04 |
| D-012 | **clamp は変異ノイズのみにかける**（同日の「合計に clamp」はレビュー側の誤りとして撤回）。平衡式が成立するのはこの形だけで、合計に clamp すると極端なアレルほど引き戻しが弱まり平衡が上がる。併せて**非能力形質の sd を創始アレルSDから導出**し、**V-2e を非能力形質すべての集団SDへ一般化**（§5.2/§6.4/§13.2） | 2026-08-04 |
| D-013 | **非能力形質の sd 導出に clamp 切断補正 ÷0.916 を追加**（丈夫さ66・気性10・適性13・距離341/114）。切断効果は解析的に求まる既知の系統誤差であり、ゲート幅で吸収せず導出式で打ち消す。V-2e の余裕が 0.04 → 0.12以上に増える（§6.4） | 2026-08-04 |
| D-014 | **V-9 を V-9a（不変条件・全サンプル 0.90〜1.10）と V-9b（監視・目標値なし）に分割**。旧「平均 0.99〜1.01」は §8b.3 の片側正のボーナスと両立しない（レビュー側の誤り）。**V-8 の「期待値」は倍率の期待値**と明確化し、勝率比 78% は報告値とする | 2026-08-05 |
| D-015 | **`condition_aptitude`（§5.2 にあるが genotype に無く遺伝しなかった）を単一の `heavy_aptitude`（道悪適性・0〜100）として genotype に追加**。良馬場は全馬 1.0（「良馬場が得意」は無意味）。道悪巧者を**配合で狙える**ようにする＝§1.1 の中核価値に沿う | 2026-08-05 |
| D-016 | **`RACE_RANDOM_K` を 0.12 → 0.26 に較正**（実測: 0.12 では1番人気勝率 51.6%）。§8.7 の「0.12 は30%前後」という記述は**誤りとして訂正**。★較正条件（素質開放率プレースホルダ・クラス分け・18頭立て）を明記し、P3 で再較正する | 2026-08-05 |
| D-017 | **スタミナゲージの消費をレース時間に対する割合で定義**（§8b.2）。絶対値 1.0/秒 では 2000m（約120秒）で必ず 0 になり、介入の巧拙が結果に出なかった（レビュー側の誤り） | 2026-08-05 |
| D-018 | **出走馬を同格帯（クラス）から組むことを §10.4 に明記**。無作為抽選だと1番人気勝率が51%になり V-4 が壊れる | 2026-08-05 |
| D-042 | **staging プロジェクトの用意を P3 の着手条件にする**（§14.6・R-24）。R-24 で状態を変える16ツールに本番拒否ガードを入れたが、**staging が存在しない**（Supabase の認証情報が本番1組のみ）ため、**それらのツールは現在どこにも向けられない**。A-2 のような破壊試験を HEAD で取り直すこと（R-23）が**原理的に不可能**になっている。P3 は育成ループとポイント台帳を扱うので、破壊試験なしでは進められない。**オーナー手配事項** | 2026-08-10 |
| D-056 | **凍結が無いレースは「旧経路で確定」ではなく「開催中止・EP 返還」にする**（§10.2・D-037 の適用）。現在の `settleRace` は凍結が無ければ**警告を出して `horses` 読み直しの旧経路に落ちる**が、**それは D-055 で閉じたはずの欠陥そのもの**であり、**迂回ですらなく既定の挙動**である。移行の猶予は実質1サイクル（レースは生成2周先→10分程度で確定）なので、**猶予は既に終わっている**。→ **凍結が無ければ D-037 の自動中止に載せる**（返還・冪等・アラートの機構は既存）。将来 凍結の書き込みが壊れた場合も、**静かに劣化せず、目に見える形で止まる** | 2026-08-12 |
| D-055 | **レース生成時の `RaceEntrant` 一式（13項目）を `race_entries.entrant_snapshot` に凍結し、確定は凍結を使う**（§8.6・§10.2）。**調子・疲労だけでは足りなかった** — 確定側は `horses` と結合して能力・適性・スキル遺伝子まで読み直しており、**生成は2周先なので間に週が進めば `stats` そのものがずれる**。→ **食い違いが「無い」ことを測るのではなく「起こりえない」形にする。** 副産物として §8.6 のリプレイ検証が完全になる（そのレースの入力が DB に残るので第三者が完全再現できる）。凍結の無い馬は数えて `console.error` に出す（黙って旧経路に落ちない） | 2026-08-12 |
| D-054 | **「定数が効いているか」と「ゲートが捕まえるか」は別の問い**（§16.3・R-14）。レビュー側は Q-P3-42 の短縮法として「**壊して測定値が明らかに動けば防御確定**」と指示したが、**これは誤り**。実測: `TAIL_MIX_P_DEFAULT` は統計量を **46%** 動かすのに **V-6 の帯（0.5〜2%）の内側に留まる**。**効いていることと、壊れたときに落ちることは別**である。→ **ゲートが捕まえない較正定数には、下流の集計ではなく「その定数が直接支配する機構」を検査する振る舞いテストを書く**（例: 裾混合なら、広い成分から引かれる割合が `P` に一致すること） | 2026-08-12 |
| D-053 | **`RACE_RANDOM_K = 0.22` は再較正不要。記録のほうを改める**（§13.1）。P1 の較正は素質開放率プレースホルダ（0.55〜0.85）を前提としていたが、**そのプレースホルダを廃止し本番の実分布（平均71.3%）で測り直しても V-4 32.32% / V-5 63.26% / V-6 が帯内**。→ **較正条件は定数の意味の一部**であり、条件が変わったら「値を導き直す」か「その条件でも成立することを示す」かのどちらかが要る（今回は後者）。**開発側が §13.1 を自分で書き換えず照会に回したのは正しい**（Q-P3-41） | 2026-08-12 |
| D-052 | **レース条件の決定を1か所に統一する**（§9.2・§10.2）。`cycle-runner → conditionsOf`（DB に保存され確定に使われる）と `race-field → generateRace`（オッズの MC が前提にする）が**別々に距離・馬場・馬場状態を引いており、本番で 1/7 しか一致していなかった**。→ **プレイヤーが見るオッズは、実際に走るレースとは別の条件で計算されたもの**。**DB には「芝1600m」と正しく書かれるので、どこにも痕跡が出ない。** P1 のゲートは `verify-race.ts` で人気推定と着順が同じ条件を使うため**構造的に検出できない**（食い違うのはワーカー＋DB の経路だけ）。→ **条件は cycle-runner が一度だけ引き、`generateRace` に渡す**。併せて **本番の443件すべてが `good` だった**（`track_condition` の直書き）ため **`TRACK_CONDITION_CDF` が本番経路で使われておらず、道悪適性が一度も効いていなかった** — これも「機構を止めると通る」型（R-16） | 2026-08-11 |
| D-051 | **V-11 を両側にする**（§13.2）。本番は401レース確定済みでも**利用者0・馬券0・PP台帳0行**で、**経済が存在しないまま V-11 が自動的に通っていた**。V-12a・V-15 に続く**3回目の「機構を止めると通る」型**（すべてレビュー側が書いたゲート）→ **R-16 に実務上の検査法を明文化**。併せて**較正は合成プレイヤー集団・監視は実集団**という測定条件を追加 | 2026-08-11 |
| D-050 | **V-15 を両側のゲートにする**（§13.2）。下限だけだと **`TEMPER_FLOOR_RATIO = 1.0`（機構を止める）が最大余裕**になり、**V-12a に下限が無かったときと同じ穴**（R-16）。→ **②「キャリアを通じた平均低下が 15% 以上」を追加**。併せて **性能の基準は代表方針・安全性の基準は最悪方針で測る**ことを明記（開発側が V-15 を休養の多い方針で評価し `0.50 では足りず 0.60` と判断したのは正しい） | 2026-08-11 |
| D-049 | **気性の変化に genotype 由来の下限を設ける＋V-15 を新設**（§7.2・§13.2）。`temperDelta` に下限も回復も無く、**1,800頭で平均 51.0 → 0.1・単頭では 199/200 が40週で 0 到達**。**適切に育てるほど早く形質が死ぬ**うえ、**D-044 の連続補間（集団が 0〜100 に散らばる前提）が1年弱で崩れる**。→ 下限 `genotype × TEMPER_FLOOR_RATIO`、変化量は残り幅に比例（`headroom` と同型）。**V-2e/V-2f は誕生時を見るため検出できない**ので V-15（現役集団の SD が誕生時の50%以上）を新設 | 2026-08-11 |
| D-048 | **V-7 を V-7a（恒久ダメージ 20〜40%）と V-7b（致命的 3%以下）に分割**（§13.2）。旧「故障発生率 25〜35%」は**3週で治る挫石と恒久ダメージ・引退を同じ数に足し込んでおり、割合が同じでも中身が正反対になりうる**（R-16）。実測はどの定義でも帯外（放置45.8 / バランス75.8 / 追い切り偏重86.0%）だったが、**定義の選択で届かないと先に判明したのは「測る前に候補を並べた」から**。併せて ①**測定条件に「どの育成方針で測るか」が欠けていた**（指示側の漏れ・基準はバランス型）②**「分母は表現型の丈夫さ」は式 `1000/durability` の読み**（Q-P3-17 の B が正しい）③**`INJURY_BASE_PROB` が較正定数**であり V-7 がその較正ゲート。`menuIntensity` とキャリア長は動かさない | 2026-08-10 |
| D-047 | **V-14 の①②を「絶対水準2つ」から「水準1つ＋差」に改める**（§13.2）。実測で **BASE 7.4 → 放置73.7 PASS / バランス89.3 FAIL、BASE 7.8 → 放置75.2 FAIL / バランス90.5 PASS** と両立しなかった。原因は**旧ゲートが実質1自由度で2つの絶対水準を要求していた**こと（放置＝`BASE_GAIN × 0.3` で一意に決まり、`MAIN_EFFECT_COEF` はバランス型の26%の週にしか効かない＝3.6倍にしても +5pt）。**一方で「適切な育成 − 放置」の差は BASE によらず約15pt で不変**であり、**§7.1 が求める「デイリー来訪の動機」の実体はこの差**である。→ **適切な育成 88%以上 かつ 差 12pt 以上**。K×床の「実行可能領域が空」と同型（目標の立て方が誤っていた） | 2026-08-10 |
| D-046 | **疲労の自然回復を新設**（§7.4・初期値5）。副次的に **V-14 ③ が解けた**（追い切り偏重の EP が 73,173 → 80,296 に嵩み、EP あたり効率が **1.30倍 → 0.67倍** と逆転）。**疲労の設計を直すために入れたものが、支配戦略の問題に効いた** | 2026-08-10 |
| D-045 | **V-14 の梃子は `BASE_GAIN`。`MAIN_EFFECT_COEF` 単独では届かない**（§7.3・§7.4・§13.2）。実測: `MAIN_EFFECT_COEF` を 0.45→1.20 にしても**放置 76.7%・追い切り偏重 99.1% がどちらも1ミリも動かない**（軽め0.3 と追い切り1.6 は正典の固定値のため）。しかも**追い切り偏重は 99.1%＝天井**にあり、天井に達しているものを「上回らせない」ことはできない。→ **`BASE_GAIN` を下げて全体を天井から引き剥がし、`MAIN_EFFECT_COEF` を上げてバランス型を戻す**（2つのつまみで3目標）。**`MAIN_EFFECT_COEF` を 1.6 に近づけること自体が、追い切りの相対優位を縮める**。併せて ①**週ごとの自然回復を新設**（放置の平均疲労 77.9 > 追い切り偏重 69.2 という逆転の是正）②**V-14 ③ の「期待値」を「同一 EP 予算下の開放率」と定義** | 2026-08-10 |
| D-044 | **§7 の未定義値を確定**（§7.3・§7.4・§7.5・§13.2）。①主効果/副効果は較正定数とし、**係数ではなく引退時の素質開放率をゲートにする（V-14 新設）** — 係数1だと調教104週で上限に張り付き、キャリア後半78週の伸びが 0.00 になり §7.1 の来訪動機が消える ②**temperCoef は2分類でなく連続補間**（創始水準50に境界を置くと集団の半分が境界上に乗る）③`conditionCoef` は線形 ④成長曲線は線形補間・104週未満は外挿しない（late_bloomer が負になる）⑤**故障の恒久ダメージは current も切り下げる。ただし故障処理の中で明示的に行い、プレイヤーに明示する** ⑥「疲労70以上は調子2止まり」は①式に包含され一度も発火しない → **冗長な安全網として残し、①を書き換えない** ⑦`rand(-1,+1)` は整数 ⑧疲労の値域は 0..100 | 2026-08-10 |
| D-043 | **本番はビルド成果物から起動する（未実施の積み残し）**（§14）。プロセス木の調査で、本番が **tsx のラッパー + esbuild の子プロセス**という構成で動いていることが判明した。これは ①7分停止（`npm ci --omit=dev` が実行時依存の tsx を消した）の根本原因であり、②`systemctl show -p MainPID` がラッパーを指すため**資源測定が実際のワーカーを見ていなかった**原因でもある。配備の堅牢化のうち「リリースディレクトリ＋symlink＋ヘルスチェック」は実装済みだが、**「実行時に開発依存を要求しない」が未実施** | 2026-08-10 |
| D-041 | **受け入れ証拠にコミットを併記し、フェーズを閉じるときは全項目を HEAD に揃える**（§16.3・R-23）。**検証ツールは staging 専用（状態を変えるもの）と本番可（読み取り専用）に分ける**（R-24）。発見経緯: P2 クローズ前の点検で、**A-2 の PASS が D-037/D-038 導入前の証拠のまま繰り越されており**、それを検出すべき `verify-a2` が引数変更に追随せず実行不能かつ**本番の全レースを削除する実装**だった。`verify-a7` は **`app_environment` を `development` に固定**しており、流せば次の再起動からワーカーが起動しなくなる（ガードが正しく働くぶん確実に停止する） | 2026-08-10 |
| D-040 | **λ* は V-10 残差の調整弁ではない。`LAMBDA_STAR = 30` に戻す**（§9.2）。同一 seed・同一レース・同一確定系列（着順は完全に同一・差はオッズのみ）での実測: **三連単 −0.48 → −0.38（+0.10pt）／三連複 −0.27 → −0.26（+0.01pt）／他5券種はほぼ不変**。レビュー側は「打ち切りは λ*=30 で `P(c=0)=e⁻³⁰≒9×10⁻¹⁴` だから残差の説明にならない」と書きながら、**同じ文書で「λ⁻¹ で半分に縮む」と予測しており自己矛盾していた**（打ち切り以外が主因なら λ⁻¹ は当てはまらない）。→ **残差は λ に依存しない成分が主。本番の計算量を恒久的に倍にする根拠はない。** **余裕の薄さ（0.10pt）は残差そのものではなく測定の SE に由来する**ので、対処は**測定レース数を増やすこと**（オフライン一度きり）であって本番 M ではない | 2026-08-10 |
| D-039 | **MC の分解能が、暗黙のオッズ上限になっていた**（§9.2・§9.4）。M=10,000 では観測できる最小の `p̂` が 1/10,000 なので、**オッズは `(1−margin)×M = 7,700倍` を超えられない**。§9.4 が三連単に定めた **100,000倍は一度も到達不能**で、**上限が効いていたのではなく推定が届いていなかった**。本番ログで確認（配備前 cycle 175-177: 最大7,700倍・切り詰め 71/38/56件 → 配備後 178-180: 最大93,750倍・**切り詰め0件**）。**教訓: 推定量の分解能は、宣言していない上限として振る舞う。** `ODDS_CAP` を定めるときは `(1−margin)×M` がそれを上回っていることを確認する | 2026-08-09 |
| D-038 | **サイクル内の順序を「確定 → 生成」に変える**（§10.2）。現状は生成が先で、D-035 の138秒ぶん**確定が毎周遅れていた**。**生成は1200秒の余裕を持つが、確定は客が待っている処理**（`bets` が `pending` のまま）。余裕の小さいものを先に処理する | 2026-08-09 |
| D-037 | **開催中止の定義を「生成失敗」から「確定失敗」中心に改める**（§10.2）。旧記述は**馬券が存在しない失敗**を指しており、**馬券が売れた後に確定できない**という危険なほうを扱っていなかった。→ **`scheduled_at + 60分` で自動中止・EP 全額返還・1トランザクション・冪等・アラート必須**。**発見経緯**: `cancelRace` が実装されていたが**本番ループから一度も呼ばれておらず、検証スクリプトだけが呼んでいた**（「実装した」と「経路に繋がっている」は別。VPS に載っていなかった件と同型で、今度は同じ木の中で起きた） | 2026-08-09 |
| D-036 | **オッズ MC の試行回数を導出値にし、V-10 の測定条件を書き換える**（§9.2・§13.2）。`M = requiredOddsTrials()`（D-035 の設計式から導出・数値リテラル禁止）。`LAMBDA_STAR=30` は較正定数。**V-10 の「10万レース」は M≒390万との積が約44日で実行不能**なため、「レース数 × 1レースあたり確定回数」に改める（**レース数1は不可** — 出走表間のばらつきが測れない・R-20）。本番機実測 35.4μs/試行 → 1レース137.7秒・1周275秒＝**10分サイクルの46%**で収まるため **ODDS_CAP の引き下げは不要** | 2026-08-09 |
| D-035 | **公正オッズが `ODDS_CAP` を超える目は発売しない**（§9.4）。`p_min = (1 − margin)/ODDS_CAP` 未満の目は `race_odds` に行を作らない。**払戻率を動かす3効果（凸性＋／`c≥1` 打ち切り−／上限切り詰め−）のうち後2つが同時に消える**（売る目がすべて上限の内側になり、売る下限が `p_min` に固定されて `M·p_min` を十分大きく取れるため）。凸性は D-013 型の解析補正で既済。→ **M は設計式 `M ≥ λ* × ODDS_CAP/(1−margin)` で決まる**（λ*=30 なら三連単が律速で約390万回/レース）。**発見の経緯**: 補正前の V-10 合格は凸性と打ち切りの**偶然の相殺**で、**実装を変えずに M だけで符号が変わっていた**（M=1,000 で +2.16 / M=100,000 で −6.99）。**上限値そのものは変更しない。** プレイヤーには「最長の目が買えなくなる」が、**現状は買えても配当が黙って切り詰められている**ので是正は客に有利 | 2026-08-09 |
| D-034 | **A-1 の合格条件を「無人で24時間」から「24時間にわたり欠落も重複もなく前進し続ける（再起動を含んでよい）」へ改める**（§16.1）。P2 指示書 §3 が既に「**A-1 の『24時間回る』は、レースが実際に生成・確定・払戻されたことまで確認する。プロセスが生きていることではない**」と定めており、「無人」の語はその趣旨と食い違っていた。**§14.2 が Git push デプロイを前提にしている以上、デプロイ中も止まらないことこそが要件**。再起動をまたいで周番号が飛ばず二重にも欠落にもならないのは、**連続稼働より強い証拠**である。ただし**無停止の連続区間（メモリ/接続の枯渇を捕まえる）は別途、最長区間として報告**する | 2026-08-09 |
| D-033 | **V-12a の下限は「運用中の集団」にのみ当てる**（§13.2）。D-032 の下限 0.03 をプリシード開始世界に当てると、**系統多様性が最大の候補ほど F が低く弾かれる**という衝突が起きた（実測: 有効系統数 8.00 の 1231 が F=0.0287 で下限割れ）。**V-12 は「近交の進行を監視」するゲートで F は運用とともに上がるため、開始世界の低い F は欠陥ではなく余裕**。開始世界に問うのは **§6.5 が発火しているか**で、**高F配合の割合**で直接測る（新たな閾値は設けない）。→ **世界シードは 1231 を採用** | 2026-08-08 |
| D-032 | **V-12a に下限 0.03 を追加**（§13.2）。上限だけのゲートは F≒0 の世界を通すが、それは §6.5 の近交弱勢・血の濃縮が働かない世界である（R-16 の典型例）。**近交回避の重みは「ゲートを通す最大値」ではなく「機構が働く水準」で選ぶ**という開発側の論（Q-6）を採用 | 2026-08-08 |
| D-031 | **`INBREED_PENALTY_WEIGHT = 0.3`**（§10.5）。40シード測定で **penalty=0 → 平均4.86/SD2.13/系統合格率45%/V-12a 38-40、penalty=0.3 → 平均4.42/SD1.43/30%/V-12a 40-40**。**D-029 の「トレードオフは存在しない」は誤りで訂正**（12シードの点推定が根拠だった）。それでも有効化を維持するのは ①V-12a は代理指標でなく正典のゲート ②合格基準3 は選定方式（D-028）なので 30% で足りる ③**プリシードと運用で規則を変えると開始時点で世界の統計的性格が不連続になる**。値は 3.0 でなく 0.3（F=0.0312。3.0 の F=0.0094 は §6.5 が死ぬ・D-032） | 2026-08-08 |
| D-030 | **§10.3 の重賞枠を是正**。9 R/日 = 63枠/週 に対し週次頻度の合計が 31 で 32枠が宙に浮いていた → 「**オープン特別・重賞** 9 R/日」とし、63枠のうち重賞31・残り32はオープン特別。**併せて週3 G1 = 年156 G1 が §6.7 の種付上限 `20 + G1勝利数 × 10` を広く緩め、D-026 の系統集中経路を太らせる懸念を⚠️で記録**（P3 で測定） | 2026-08-08 |
| D-029 | **D-027 を撤回し、近交回避を有効化する**（§10.5）。40シードでの再測定: **penalty=0 → 有効系統数 4.96±0.72 / 平均F 0.0700、penalty=3.0 → 4.82±0.65 / 平均F 0.0094**。**トレードオフは存在しなかった。** D-027 の根拠「6.02 → 2.09」は **CV 43.7% の量を 3シード対1シードの点推定で比べたもの**で、両端とも測れていなかった。計算コスト（約18〜25倍）はプリシードが**オフラインで1回**・以後もNPC繁殖は年1回のバッチなので許容する | 2026-08-07 |
| D-028 | **P1.5 のクローズを撤回**（§16.1）。乱数用途 ID を 1〜5 → 41〜45 に変えただけ（機構は不変）で **y50 有効系統数 6.02→2.96 / 5.03→3.00、平均F 0.0519→0.1060（V-12a FAIL）**。**レースゲートは4シード×60,000レースで最大 0.22pt しか動かず**、両者の違いは標本の性質（**平均量 vs 単一実現量**）にある。合格基準3 の「有効系統数 ≥5」はレビュー側が発明した代理指標で、**実現間の分散（SD≒1.4）に対し閾値が分布の中央にあった**。→ **機能量（sire×bms 組数・高F配合割合）へゲートを移すことを検討し、30〜50シードで分布を測る**。併せて **F-1 撤回時に penalty=0 と 3.0 の2点しか測っていなかった**ので、トレードオフ曲線（0/0.1/0.3/1.0/3.0）を引き直す（R-20） | 2026-08-07 |
| D-027 | **近交回避は系統多様性の対抗力にならない**（D-026 の対処案を撤回・§10.5）。実測: penalty=3.0 で平均F 0.052→0.0095・最大F 0.477→0.046・虚弱1.7%→0% と近交には劇的に効くが、**y50 有効系統数が 6.02 → 2.09 と悪化**。近交回避は「**自分の血からの反発**」であって「多様性への誘引」ではないため、全厩舎が「自分と血が遠い最強系統」へ収束する。**そもそも V-12a は penalty=0 で PASS**（平均F 0.052/0.088/0.051 ≤ 0.10）なので、通っているゲートを壊してまで採る理由がない。**機構は実装したまま penalty=0 で無効化**（R-15。ライブ運用でプレイヤーが意図的に近交を進めた場合の予備手段として保持） | 2026-08-06 |
| D-026 | **系統集中の原因を確定**（§10.5）: 浮動でも創始系統数でもなく、**配合の決定経路に血統を参照する項が無いこと**。対処は `mateScore` への近交回避項（`canMate` とエンジンには触らない）。**合格基準の判定線は y50**、y100 の減少はライブ運用の監視へ。併せて **V-12a がプリシード集団で未測定**であったことを発見（指示側の漏れ）＋ **R-17**（選抜されていない属性は保存されない） | 2026-08-06 |
| D-025 | **NPC の配合相手は厩舎方針に沿って選ぶ**（§10.5）。全厩舎から無差別に「良い種牡馬」を選ぶと産駒の父系ラインが優れた系統へ収束する。y50 の有効系統数が 5.02 → 8.83 に改善（ただし**単独では不十分** — D-026） | 2026-08-06 |
| D-024 | **V-13 を新設**（仕掛けの巧拙が結果に出ること）＋ **R-16**（比率と範囲は一様な歪みを検出できない）。P-2 のスタミナ是正で V-8/V-9 が動かなかったのは、旧実装が全馬に一律 −0.15 を掛けていたため。**両ゲートとも構造的破綻を素通ししていた**（§13.2） | 2026-08-06 |
| D-022 | **V-6 を「最下位1頭」から「下位3ランクの平均」へ再定義**＋**頭数別の分解を必須化**（案B）。裾を厚くした副作用で最下位人気の同定が不安定になったため。`LONGSHOT_RANKS` / `POPULARITY_TRIALS` / 頭数分布は**較正定数でなく「測定条件」**として正典に固定し値照合で守る（§13.2） | 2026-08-06 |
| D-021 | **レース乱数を裾の厚い混合分布にする**（§8.7・確率 P=0.03 で分散 M=5倍）＋ **K を 0.26 → 0.22 に再較正**。正規分布のままでは V-4/V-5/V-6 の実行可能領域が空だった（K と床はどちらも「広がり」しか変えず形を変えないため）。**余裕は全ゲート 8SE 以上**（従来の境界解は −0.7SE） | 2026-08-06 |
| D-020 | **クラス制の帰結を明記**（§10.4）: 勝率は能力を測らない（実測 r = −0.003）。育成の報酬は「昇級」として現れるので、§11 賞金・§12 UI・選抜指標をそれに合わせる | 2026-08-05 |
| D-019 | **V-2d を分割**: 選抜圧のかからない形質（丈夫さ）は水準 ±10%、**レース選抜がかかる形質（適性・距離・気性）は平坦化（V-2f）**で見る。P1 で選抜が入ると「非能力形質は動かない」という前提が崩れるため | 2026-08-05 |

---

## §1. プロダクト概要 〔確定 2026-08-04〕

### 1.1 コンセプト

**「血をつなぐ」ことが唯一の進行軸である、ポイ活型オンライン競馬育成ゲーム。**

プレイヤーは牧場オーナー兼調教師。無償で得た参加ポイントを燃料に、馬を配合で生み出し、週単位の調教で育て、全プレイヤー共通のレースに出走させ、馬券を買う。引退した馬は種牡馬・繁殖牝馬となり血が次世代に流れ込む。世代を重ねるほど自分の牧場の血統が濃く強くなる — この蓄積感が中核価値。勝てば賞金ポイントが貯まり、景品と交換できる。

### 1.2 事業ループ（ポイ活文脈）

```
広告主が出稿 → ユーザーが広告視聴等で参加ポイントを無償獲得
  → ゲームプレイ（滞在時間・再訪）→ 広告収益 → 景品原資
```

ユーザーは金を払わない。広告主が払う。ゲームの深さ（育成・血統）が滞在時間を作り、滞在時間が広告収益を作り、広告収益が景品原資を作る。**このループが成立する限り、賭博構造は発生しない**（憲法 §0.2）。

### 1.3 二重ループ構造

| ループ | 周期 | 役割 |
|---|---|---|
| 馬券ループ | 短周期（§10 で決定・D5） | 滞在時間の実体。毎分の快感 |
| 育成ループ | 数日〜数ヶ月 | 再訪の理由。長期目標 |

両者は「**自分の育てた馬に自分で賭けられる**」一点で接続される（自馬ベット制限は §9）。

### 1.4 中毒性の設計原理（実装時に薄めてはいけない5点）

1. **隔世遺伝のサプライズ** — 凡庸な親から怪物が出る体験をロジックで保証（§6）
2. **インブリードの博打性** — 能力と引き換えに故障・気性難。安全な強化にしない（§6）
3. **セッションを跨いだ蓄積** — 育成中の馬が常に「途中」。1回のログインで完結させない（§7）
4. **共通世界の社会性** — 自分の血統が他人の牧場で走って評価される（リーディングサイアー、§10）
5. **レース中のリアルタイム介入** — 観るだけにしない。仕掛けどころの判断で結果が変わる（§8b・D-006）

### 1.5 レース中リアルタイム介入の決定記録（D-006）

v1 から**採用する**（オーナー決定 2026-08-04）。設計解釈: 「全参加者が同じ条件で使える介入は、調教の延長である。あくまでゲームである」。

ただし馬券と景品が絡むため、以下を**必須要件**として §8b で仕様化する（介入を安全に成立させる条件）:

1. **効果上限は小さく保つ**（合計 ±10% 以内・§13 で管理）— 操作の上手さで馬格差は覆らない。育成が主・操作が従
2. **サーバー権威** — クライアントは入力を送るだけ。最終スコア計算は必ずサーバー（憲法 §0.2-4）
3. **切断・無操作フォールバック** — 通信切断や無操作の馬は「AI 自動進行」で走り、介入ゼロが著しい不利にならない中立既定値を持つ
4. **ラグ許容設計** — タイミング判定にネットワーク猶予幅を持たせ、端末・回線差で有利不利を作らない
5. **マクロ・自動化検知** — 景品がある以上、自動連打ツールは必ず現れる前提で入力パターン監視を初期から持つ
6. **経済監視への組み込み** — 介入による結果変動はオッズ計算（発売前のモンテカルロ）に織り込めないため、運営側の期待値ブレとなる。§11/§13 のポイント純発行量監視の必須項目とする
7. **自馬ベット制限（§9）が八百長利得の遮断装置** — 介入の有無に関わらず必須（わざと負ける操作は調教でも可能であり、儲け口を塞ぐのはベット制限側である）

## §2. 用語定義 〔確定 2026-08-04〕

| 用語 | コード名 | 定義 |
|---|---|---|
| 参加ポイント | `entry_points` (EP) | 遊ぶための燃料。無償取得のみ・購入/換金/景品交換/譲渡すべて不可（憲法 §0.2-1） |
| 賞金ポイント | `prize_points` (PP) | ゲーム内で勝って得る報酬。景品交換可・現金/暗号資産交換不可・譲渡不可（憲法 §0.2-2） |
| 素質値 | `potential` | 遺伝で決まる各能力の上限。プレイヤーには非公開（星表示で暗示・§5.5） |
| 現在値 | `stats` (current) | 調教で伸びた実際の能力。出走時に使われる値 |
| 素質開放率 | unlock rate | current ÷ potential。調教でこれを 1.0 に近づけていく |
| アレル | allele | 各形質について馬が持つ2つの遺伝子値。子は父から1つ・母から1つ受け継ぐ（§6） |
| 近交係数 F | `inbreed_coeff` | Wright の式で算出する近親交配の度合い（0〜1・§6.5） |
| ニックス | nicks | 特定の父系ライン×母父系ラインの組み合わせで生じる配合ボーナス（§6.6） |
| キャリア | career | 1頭がデビューから引退までに走るレース数（上限は §7） |
| 介入 | intervention | レース中のリアルタイム操作（§8b・D-006） |
| NPC馬 / NPC厩舎 | | システムが運営する馬・厩舎（§10）。プレイヤー馬と**同じ遺伝エンジン**で生成される |
| プリシード | preseed | サービス開始前に NPC 世界を50世代オフラインで回し、血統の歴史を作っておくこと（§10） |
| seed commit / reveal | | レース乱数の事前ハッシュ公開・事後種明かし。結果の事後改竄が不可能なことの証明（Provably Fair・§8） |
| 馬券ループ / 育成ループ | | 二重ループ構造（§1.3）。滞在時間の実体／再訪の理由 |

**命名規則**: コード内の変数名・テーブル名・コメントに他社製品由来の語を使わない（憲法 §0.1）。ポイントは EP/PP の2台帳で、旧仕様の「メダル」「GP」という語は使用しない。

## §3. 二種ポイント経済 〔確定 2026-08-06〕

> 形式はポイ活型の予測市場サービスに倣う（§0.3）。**構造は §0.2 の5原則で固定**され、**具体的な提携先や景品は運用で差し替えられる**。この章は「**何が構造で何が運用か**」を分ける。

### 3.1 二種ポイントの性質

| | 参加ポイント (EP) | 賞金ポイント (PP) |
|---|---|---|
| **役割** | 遊ぶ燃料 | 勝って得る報酬 |
| **入手** | 外部から**無償**取得（§3.2） | ゲーム内の成績・的中のみ |
| **購入** | **不可**（構造・S-1） | 不可 |
| **景品交換** | 不可 | **可**（§11） |
| **現金・暗号資産化** | 不可 | **不可**（構造・S-3） |
| **ユーザー間譲渡** | 不可（構造・S-4） | 不可 |
| **相互変換** | EP→PP は**勝つことでのみ**（馬券の払戻・賞金） | **PP→EP は不可**（構造・S-5） |

### 3.2 EP の入手経路（構造は固定・具体は運用）

**構造（変えられない）**: **広告主が費用を負担し、ユーザーは金銭を払わない**（S-2）。

**具体（運用で差し替え可）**:

| 経路 | 備考 |
|---|---|
| 広告視聴（動画リワード等） | 主要供給。1日あたりの上限を設ける |
| オファー成約（アプリインストール・会員登録等） | 単価が高い。成果報酬型 |
| デイリーログイン | 基礎供給。少額 |
| 実績（アチーブメント）報酬 | 長期目標 |
| 他ユーザーへの種付け料収入 | ゲーム内循環（§10.5） |

> **★「他サービスで課金して得たポイント」からの変換を経路にしない。** ユーザーが金銭を払っている以上、実質的な購入であり S-1/S-2 を破る（§0.3）。提携の話が来たら必ずここを確認すること。

**レート制限**: 1ユーザーあたりの日次上限を持つ。**バグやエクスプロイト時の被害上限**を兼ねる。

### 3.3 EP の消費経路

| 経路 | 参照 |
|---|---|
| 調教費（1回 100〜800 EP） | §7.2。**EP の最大の使い道**＝外部で貯めたポイントを馬に注ぐ導線の本体 |
| 出走登録料（200 EP） | §10.4 |
| 馬券の購入 | §9。**控除率18〜23% が経済の最大シンク** |
| 配合費（種付料） | §10.5。NPC 種牡馬への支払いは**焼却**。プレイヤー種牡馬へは80%移転・20%焼却 |

### 3.4 収支バランス（1頭・1キャリアあたりの想定）

| 項目 | EP |
|---|---|
| 調教費（現役約90週 × 平均350） | −31,500 |
| 出走登録料（24戦 × 200） | −4,800 |
| 配合費（自家種牡馬） | −2,000 |
| **支出計** | **約 −38,000** |
| 平凡な馬の賞金収入 | +25,000 PP 相当 |
| 重賞級の馬 | +120,000 PP 相当 |

**→ 平凡な馬は赤字、強い馬だけが黒字。** これにより「強い馬を作らねばならない」動機が経済的にも成立する。不足分は §3.2 の外部供給で補う。

### 3.5 インフレ対策（シンク）

- 調教費・登録料（**恒常シンク**）
- **馬券の控除率**（§9.3・最大のシンク）
- 種付料（NPC 種牡馬への支払いは全額焼却／プレイヤー間は20%焼却）
- 厩舎スロット拡張・馬名変更（**ただし有償拡張はしない**・§6.7）

> **★有償の便益販売をしない**（§6.7）。金を払った者が景品獲得能力で有利になる構造は、**S-1/S-2 の「ユーザーは払わない」という土台を弱める**。収益は広告のみ（§1.2）。

### 3.6 監視（§4.6 の `point_flow_daily`）

| 指標 | 基準 |
|---|---|
| **PP の純発行量/日** | ゼロ近傍〜微減（V-11） |
| 券種別の実払戻率 | 設定 margin ±1%（V-10） |
| EP の流入/焼却バランス | 流入が焼却を大きく超え続けないこと |
| `intervention_mult` の分布 | V-9a が不変条件・V-9b は時系列（§8b.8） |

**固定オッズは運営が変動リスクを負う**ため、想定外の高配当が続くと PP が急膨張する。`margin_actual` が理論値から3%以上乖離した日は**アラート**。

### 3.7 RMT の禁止と検知

- **規約で禁止**し、違反時のアカウント停止を規定（§17 T-1）
- **アカウント間の異常な資産移動を検知するログ**を持つ。譲渡機能は無いが、**種付料の異常な高額設定**や**同一IP/デバイスの複数アカウント**が抜け道になりうる
- 同一IP・同一デバイスの複数アカウントが同一レースに出走している場合はフラグ（§9.5-4）

## §4. データモデル 〔確定 2026-08-06〕

> Postgres（Supabase）。**ゲーム内時刻の真実は Postgres の `now()` のみ**とし、ワーカーやクライアントの時計を信用しない（§14）。
> **D1（参加ポイントの流入経路）と D2（景品）が未確定**なので、そこに繋がる2テーブルは §4.7 で形だけ定義し中身を空けている。それ以外は確定。

### 4.1 ユーザーと二種ポイント

```sql
create table users (
  id uuid primary key references auth.users(id),
  display_name text not null,
  stable_name text not null,                 -- 牧場名
  entry_points bigint not null default 0,    -- EP: 遊ぶ燃料（購入不可・換金不可・譲渡不可）
  prize_points bigint not null default 0,    -- PP: 賞金（景品交換可・現金化不可・譲渡不可）
  created_at timestamptz default now(),
  constraint ep_non_negative check (entry_points >= 0),
  constraint pp_non_negative check (prize_points >= 0)
);
```

**★二種ポイントは別カラム・別台帳にする**（憲法 §0.2-3）。1つのカラムに種別フラグを持たせる形にしない — **混同できない構造にすることが分離の担保**になる。

```sql
-- EP 台帳（参加ポイント）
create table ep_ledger (
  id bigserial primary key,
  user_id uuid not null references users(id),
  delta bigint not null,
  balance_after bigint not null,
  reason text not null,        -- inflow|training|entry_fee|bet|refund|stud_fee
  ref_id uuid,
  created_at timestamptz default now()
);
create index on ep_ledger(user_id, created_at desc);

-- PP 台帳（賞金ポイント）
create table pp_ledger (
  id bigserial primary key,
  user_id uuid not null references users(id),
  delta bigint not null,
  balance_after bigint not null,
  reason text not null,        -- prize|payout|prize_exchange
  ref_id uuid,
  created_at timestamptz default now()
);
create index on pp_ledger(user_id, created_at desc);
```

**★EP→PP の一方通行を DB レベルで担保する**: `pp_ledger.reason` に EP からの変換を表す値を作らない。**逆方向（PP→EP）の reason も存在させない**（憲法 §0.2-3・D3 の弁護士ゲートまで不可）。

### 4.2 馬

```sql
create table horses (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid references users(id),        -- null = NPC
  npc_stable_id int references npc_stables(id),
  name text not null,
  sex smallint not null,                     -- 0=牡 1=牝
  birth_year int not null,
  age_weeks int not null default 0,

  genotype jsonb not null,                   -- ★非公開（§5.4・heavy_aptitude を含む12形質）
  potential jsonb not null,                  -- ★非公開（星表示に丸めてのみ提示・§5.5）
  stats jsonb not null,                      -- 現在値（公開）
  aptitude jsonb not null,                   -- 適性一式（公開）
  traits jsonb not null,                     -- 成長型/気性/丈夫さ/スキル

  condition smallint not null default 3,
  fatigue smallint not null default 0,
  injury_until_week int,
  status text not null default 'training',   -- training|racing|retired|stud|broodmare

  sire_id uuid references horses(id),
  dam_id uuid references horses(id),
  sire_line text not null,
  bms_line text,
  inbreed_coeff numeric(6,5) not null default 0,
  pedigree_cache jsonb,                      -- 5代分の祖先ID（Wright の式を O(1) で）

  class_rank smallint not null default 0,    -- ★格（D-020）。育成の報酬はここに現れる
  starts int default 0, wins int default 0,
  place2 int default 0, place3 int default 0,
  earnings bigint default 0,                 -- 累計獲得 PP
  prize_per_start numeric(10,2) default 0,   -- ★選抜指標（P1 で prize より優れると実証）
  g1_wins int default 0,

  stud_fee int, covers_this_year int default 0,
  created_at timestamptz default now()
);
create index on horses(owner_id, status);
create index on horses(class_rank, status);   -- 番組編成（同格帯から引く・D-018）
```

**RLS**: `genotype` と `potential` は**本人にも直接 SELECT させない**。星評価に丸めるビュー経由でのみ返す（§5.5）。

**`class_rank` を第一級のカラムにする理由**（D-020）: クラス制では勝率が能力を測らないため、**育成の報酬は「昇級」として現れる**。UI（§12）と番組編成（§10.4）の両方がこの値を主役に使う。

### 4.3 配合・育成の記録

```sql
create table breeding_records (
  id uuid primary key default gen_random_uuid(),
  sire_id uuid not null references horses(id),
  dam_id uuid not null references horses(id),
  foal_id uuid references horses(id),
  requester_id uuid references users(id),
  rng_seed text not null,                    -- ★再現用
  inbreed_coeff numeric(6,5),
  top_common_ancestor_id uuid,               -- 血の濃縮の参照先（§6.5）
  nicks_bonus numeric(5,4),
  fee_paid int,                              -- EP
  created_at timestamptz default now()
);

create table training_records (
  id uuid primary key default gen_random_uuid(),
  horse_id uuid not null references horses(id),
  week int not null,
  menu text not null,
  gains jsonb not null,
  fatigue_delta int,
  event text,                                -- 故障/覚醒/不調
  rng_seed text not null,
  created_at timestamptz default now()
);
```

### 4.4 レースと Provably Fair

```sql
create table races (
  id uuid primary key default gen_random_uuid(),
  name text not null,                        -- ★架空名のみ（憲法 §0.1）
  grade text,                                -- G1|G2|G3|OP|条件
  class_rank smallint not null,              -- ★同格帯（D-018）
  surface text not null,                     -- turf|dirt
  distance int not null,
  track_condition text not null,             -- good|yielding|soft|bad（★4段・D-015）
  course_id text not null,
  scheduled_at timestamptz not null,
  seed_commit text not null,                 -- ★発走前に公開（§8.6）
  seed_reveal text,                          -- ★確定後に公開
  status text default 'scheduled',
  purse bigint not null                      -- PP
);
create index on races(scheduled_at, status);

create table race_entries (
  id uuid primary key default gen_random_uuid(),
  race_id uuid not null references races(id),
  horse_id uuid not null references horses(id),
  gate int not null,
  weight numeric(4,1) not null,
  strategy text not null,                    -- nige|senko|sashi|oikomi
  odds numeric(9,1),
  popularity int,                            -- モンテカルロ勝率順位（§9.2）
  finish_pos int,
  finish_time numeric(7,3),
  margin text,
  intervention_log jsonb,                    -- ★§8.8: seed + これで着順を再計算できること
  intervention_mult numeric(5,4),            -- 適用後の値（0.90〜1.10・§8b）
  cap_violations jsonb,                      -- ★範囲外入力の記録（黙って直さない・O-3）
  unique(race_id, horse_id)
);
```

**★`intervention_log` は §8.8 の一次資料**。判定は**サーバー受信時刻のみ**を使う（クライアント申告は詐称で有利になる）。`seed_reveal` + `intervention_log` → 同じ着順が再計算できることが要件。

### 4.5 馬券

```sql
create table bets (
  id bigserial primary key,
  user_id uuid not null references users(id),
  race_id uuid not null references races(id),
  bet_type text not null,                    -- win|place|wide|quinella|exacta|trio|trifecta
  selection jsonb not null,
  amount int not null,                       -- ★EP で支払う
  odds_at_purchase numeric(9,1) not null,    -- ★購入時に固定（後からオッズ式を直しても配当は変わらない）
  status text not null default 'pending',    -- pending|won|lost|refunded
  payout bigint default 0,                   -- ★PP で払い戻す
  created_at timestamptz default now()
);
create index on bets(race_id, status);
create index on bets(user_id, created_at desc);

create table race_odds (
  race_id uuid not null references races(id),
  bet_type text not null,
  selection jsonb not null,
  probability numeric(9,8) not null,         -- モンテカルロ実測（§9.2）
  odds numeric(9,1) not null,
  capped boolean default false,
  primary key (race_id, bet_type, selection)
);
```

**★EP で買い PP で受け取る**（§9）。この一方通行が二種ポイント分離のゲーム内実装である。

### 4.6 監視（運営の生命線）

```sql
create table point_flow_daily (
  date date primary key,
  ep_inflow bigint,        -- 外部からの流入（D1）
  ep_burned bigint,        -- 調教費・出走料・馬券の控除
  pp_issued bigint,        -- 賞金 + 払戻
  pp_exchanged bigint,     -- 景品交換（D2）
  margin_actual numeric(6,4),   -- 券種別の実払戻率（理論値との乖離を監視）
  intervention_mult_mean numeric(6,4),  -- ★V-9b（§8b.8）
  created_at timestamptz default now()
);
```

**PP の純発行量がゼロ近傍〜微減であることを毎日確認する**（V-11）。固定オッズは運営が変動リスクを負うため、想定外の高配当が続くと PP が急膨張する。

### 4.7 D1 / D2 に依存するため未確定 〔オーナー判断待ち〕

```sql
-- 参加ポイントの流入記録（D1: 外部獲得経路が未確定）
create table ep_inflow_events ( ... );
-- 要件のみ確定: ①購入経路を持たない ②広告主負担型の記録であること
--               ③1ユーザーあたりのレート制限を持つこと

-- 景品カタログと交換（D2: 景品の中身と原資が未確定）
create table prize_catalog ( ... );
create table prize_exchanges ( ... );
-- 要件のみ確定: ①現金・暗号資産への交換経路を持たない ②譲渡不可
--               ③交換時の本人確認の要否は弁護士ゲート（§0.3）で決まる
```

### 4.8 NPC 世界

```sql
create table npc_stables (
  id serial primary key,
  name text not null,                        -- 冠名（架空・§10.5）
  policy text not null                       -- 短距離特化/ダート専門/晩成型 など
);
```

NPC 馬は `horses.owner_id = null` + `npc_stable_id` で表す。**プレイヤー馬と同一テーブル・同一遺伝エンジン**であることが公正性の担保（§10.5）。

## §5. 馬のパラメータ 〔確定 2026-08-04〕

> 出典: GORILLA v1.0 §5 を採用。第1世代の「4能力・0〜110」は廃棄し、**5能力・0〜1000** に統一する。

### 5.1 基礎能力（5種・各 0〜1000）

| 略号 | 名称 | 意味 | 主に効く場面 |
|---|---|---|---|
| SP | スピード | 絶対速度 | 全域、特に短距離・上がり |
| ST | スタミナ | 持久力 | 長距離、ハイペース耐性、**介入時のスタミナゲージ初期値（§8b）** |
| PW | パワー | 加速力・馬場適応 | ダート、重馬場、コーナー加速 |
| GT | 根性 | 終盤の粘り | 直線の追い比べ、僅差の決着、**直線介入の上乗せ幅（§8b）** |
| IQ | 賢さ | 折り合い・出遅れ防止・指示追従 | スタート、スキル発動率、**介入の成功判定幅（§8b）** |

**5種で確定とする理由**: 配合の奥行きは §6 の遺伝モデルが担保するので能力の種類を増やす必要がない。増やすとレトロUIに収まらず育成の意思決定も薄まる。D-006（介入採用）により ST/GT/IQ は「介入との接続」という第2の役割を持つ — **賢く根性のある馬ほど指示に応える**。これは「介入は調教の延長」（D-006 の設計解釈）をパラメータ面から支える設計である。

### 5.2 適性

```
surface_aptitude:  { turf: 0..100, dirt: 0..100 }            // 芝・ダート
distance_center / distance_range                              // 距離適性（下記）
strategy_aptitude: { nige, senko, sashi, oikomi }             // 脚質: 逃げ/先行/差し/追込 各0..100
heavy_aptitude:    0..100                                     // 道悪適性（2026-08-05・D-015 で改訂）
// ★良馬場は全馬 1.0（「良馬場が得意」は無意味）。稍重・重・不良でのみ heavy_aptitude が効く
// ★★馬場状態の出現分布（良/稍重/重/不良 の割合）は §10 執筆時に確定が要る。
//    この分布が heavy_aptitude にかかる選抜圧の強さを決める。分布は較正定数として扱う
```

距離適性は「中心距離（center）」と「対応幅（range）」の2値で持ち、正規分布カーブで表現する。マイラー／ステイヤー／万能型が自然に生まれる。

```
distance_aptitude(d) = 100 * exp( -((d - center)^2) / (2 * range^2) )
値域: center 1000〜3600m / range 200〜1200
```

> **⚠️ 距離適性の変異幅は値域比で決めないこと**（2026-08-04・D-012）。§6.4 の変異SDを値域比で機械的に縮尺すると、距離系は値域が広いため平衡が創始水準からずれ、**マイラー／ステイヤーの分化が失われる**。分化は「拡散」でも「収縮」でも失われる点に注意（回帰が無効なら拡散し、回帰0.20＋値域比sdなら創始の0.4倍まで収縮する — どちらも実測済み）。**sd は §6.4 の平衡式から創始アレルSDを逆算して決める**（distance_center=341 / distance_range=114）。同じ問題は芝/ダート適性・丈夫さでも起きるため、非能力形質はすべて同じ導出に従う。ゲートは V-2e（§13.2）。

### 5.3 特性

| 項目 | 値域 | 効果 |
|---|---|---|
| 成長型 (growth) | early / normal / late / late_bloomer | 週齢ごとの成長係数カーブが変わる（§7.3） |
| 気性 (temper) | 0..100（高いほど気性難） | 調教効率のブレ、出遅れ率、介入成功率に影響 |
| 丈夫さ (durability) | 0..1000 | 故障率の分母。低いと壊れる（§7.5） |
| スキル | 最大3個 | 発動型特性（例: 末脚爆発／ゲート巧者／雨得意・§8.5） |

**スキル遺伝子の継承**: 両親の遺伝子を各50%で継承し、重複を除いて上限3個。加えて **2% で新規獲得**する（新規獲得がないと世代を経て遺伝子プールが枯渇するため必須・P0 で判明）。

### 5.4 genotype スキーマ（非公開データ）

```json
{
  "sp": { "a1": 620, "a2": 480 },
  "st": { "a1": 510, "a2": 700 },
  "pw": { "a1": 440, "a2": 455 },
  "gt": { "a1": 300, "a2": 810 },
  "iq": { "a1": 560, "a2": 520 },
  "surface": { "turf": {"a1": 80, "a2": 60}, "dirt": {"a1": 30, "a2": 55} },
  "heavy_aptitude": { "a1": 72, "a2": 41 },
  "distance_center": { "a1": 1800, "a2": 2400 },
  "distance_range":  { "a1": 600,  "a2": 900  },
  "strategy_bias":   { "a1": "senko", "a2": "sashi" },
  "growth":     { "a1": "normal", "a2": "late" },
  "temper":     { "a1": 30, "a2": 65 },
  "durability": { "a1": 700, "a2": 640 },
  "skill_genes": ["G_SPURT", "G_GATE", "G_MUD"]
}
```

各形質が2つのアレルを持つ。表面のステータスが平凡でも片方のアレルに高値が眠っている馬は「隠れた名血」として機能する（§6.2）。

> **★`heavy_aptitude` の選抜圧が弱いのは「仕様」である（2026-08-06・D-023）**: 実測で `heavy_aptitude` の選抜応答は**芝/ダート適性の約1/12**（150世代で振れ幅 0.77pp 対 9.4pp / 7.1pp）。分類は **V-2f**（符号が反転するので選抜圧は存在する）だが、**大きさが小さいこと自体に設計上の価値がある**:
> - 芝/ダート適性は選抜で集団全体が **+6.93% 上振れ**する ＝ その形質で**差がつきにくくなっていく**
> - `heavy_aptitude` はほとんど動かない ＝ **道悪巧者は長期的に希少なまま残る**
> - プレイヤー視点では「道悪適性を狙って配合した馬は非良の約25%のレースで有利になり、**他が放っておくので優位が薄まらない**」— §1.1「血をつなぐ」の報酬として質が良い
>
> **集団レベルの選抜圧が弱いことと、プレイヤーにとっての価値が低いことは別である。** 馬場分布を変えて選抜圧を強めたくなったら、この含意を踏まえて判断すること。

> **★`heavy_aptitude` の追加（2026-08-06・D-015 の反映漏れ修正）**: D-015 で「道悪適性を genotype に追加する」と決めながら **§5.2 しか更新しておらず、本スキーマに行が無かった**。**これは P0 で `condition_aptitude` が §5.2 にあって §5.4 に無く、結果として遺伝しなかったのとまったく同じ形**である（レビュー側の誤り・開発側の申し送りで判明）。
> - **創始分布は芝/ダート適性と同じ 一様 [20, 90]**（同型なので既存の導出経路がそのまま `sd 13 / clamp 22 / center 55` を出す。新しい式は不要）
> - **§5.4 に行が無い形質は遺伝しない。** 形質を追加する決定をしたら、**必ず本スキーマと §6.4 の形質別パラメータ表の両方**を更新すること

### 5.5 素質の見せ方

- `potential` と `genotype` は**プレイヤー本人にも数値を見せない**。★1〜5（半星あり）に丸めて表示
- 「★が同じでも実際は差がある」ため、育ててみないと分からない体験が残る（配合の神秘性）
- 現在値（stats）は数値で表示する — 育成の手応えの可視化
- 実装上は RLS + ビュー経由で強制する（§4）。クライアントに生値を送らない

## §6. 遺伝・配合エンジン 〔確定 2026-08-04〕

> 出典: GORILLA v1.0 §6 を採用。本作の心臓部。第1世代の「父60%+母40%±8」は廃棄（アレル方式の方が「同じ配合でも毎回違う馬」「凡庸な親から怪物」を構造的に保証できるため）。

### 6.1 配合の全体フロー

```
1. 交配可否チェック（性別・年齢・種付回数上限・同一年内重複）
2. 近交係数 F を計算              → §6.5
3. ニックス判定                    → §6.6
4. 各形質のアレル継承              → §6.2
5. 隔世遺伝ロール                  → §6.3
6. 突然変異ロール                  → §6.4
7. F とニックスの補正を適用        → §6.5, §6.6
8. potential 確定、初期 stats 算出（素質開放率 0.28〜0.35 からスタート）
9. 性別決定（牡50% / 牝50%。セン馬は将来の去勢イベント拡張とし v1 では扱わない）
10. 馬名生成 or ユーザー命名待ち（実在馬NGリストで両方を検閲・§0.1）
11. breeding_records に rng_seed とともに記録（監査・再現用）
```

### 6.2 アレル継承（基本）

各形質について、父の2アレルから1つ・母の2アレルから1つをランダムに選んで子の2アレルとする。

```ts
child[key].a1 = rng.bool(0.5) ? sire[key].a1 : sire[key].a2;
child[key].a2 = rng.bool(0.5) ? dam[key].a1  : dam[key].a2;
```

**この単純な仕組みが「同じ配合でも毎回違う馬が出る」と「凡庸な親から名馬が出る」を同時に保証する。** 表面のステータスが平凡でも、片方のアレルに900が眠っている馬は「隠れた名血」として機能する。

### 6.3 隔世遺伝

- 各形質ごとに **6%** の確率で、両親でなく**祖父母世代（4頭のいずれか）のアレルを直接継承**する。当たった祖父母がいる側のアレルを置換する
- 別枠で **1.5%** の「大物覚醒」ロール: 5代以内で最も高いアレル値を持つ祖先を参照し、**子の低い方のアレルを置換**する。発生時は専用演出（「先祖返り！」カットイン）— プレイヤーの語り草になり配合を回し続ける動機になる
- **★適用対象は能力5種（SP/ST/PW/GT/IQ）のみ**（2026-08-04・D-011）。気性・丈夫さ・適性・距離には適用しない（通常の隔世遺伝6%は全形質に適用する）

> **⚠️ 大物覚醒は単調な上昇ラチェットである**（2026-08-04 レビュー側監査で判明）。祖先の最大値は常に子のアレル対以上になるため、この機構は必ず上げ方向にしか働かない。P0 実測でこれ単独が血統インフレの約14ポイントを生んでいた。§6.4 の平均回帰がこれを相殺する前提で能力5種には残す。**回帰を弱めたり外したりする場合は、本機構の発生率も併せて見直すこと。**

> **能力5種に限定する理由（D-011）**: 「大物」= 能力の高い名馬であり、能力以外の形質に「最大値を引く」ラチェットをかけると設計意図と食い違う。特に**気性は「高いほど気性難」なので、覚醒するたびに馬が扱いにくくなる**という逆向きの効果になっていた。距離適性への適用は `distance_range` を押し上げて**全馬を万能型に近づけ**、§5.2 が求める分化を損なう。実測でも非能力6形質すべてが上方向にずれていた（芝適性 +7.2% / 気性 +3.9〜6.3% / 距離2種 +2.2%）。限定により、回帰で押し返すのでなく発生源を断つ。

### 6.4 突然変異と平均回帰 〔2026-08-04 改訂・D-008〕

**適用単位: アレルごとに独立**（形質ごとではない）。変異は個々の遺伝子に起き、片アレルだけが変異して次世代へ伝わる — §6.2 の「隠れた名血」と整合する。

```
通常変異  : clamp(N(0, sd), ±clamp幅) − (アレル値 − 品種中心) × 回帰率
大突然変異: 0.8% で N(0, sd×2)        − (アレル値 − 品種中心) × 回帰率
            （大突然変異には通常の clamp幅を適用しない。値域clampのみ）
```

**★clamp は「変異ノイズのみ」にかける**（2026-08-04・D-012 で確定。同日の一次回答「回帰項を含めた合計に clamp」は**レビュー側の誤りとして撤回済み**）。理由は3つ:

1. **平衡式が成立するのはこの形だけ**。上表の sd はすべて `平衡SD = sd/√(2r−r²)` から導出しているが、この式は AR(1) 過程 `y_{t+1} = (1−r)·y_t + ε` を前提とする。合計に clamp をかけると裾で線形性が崩れて AR(1) でなくなり、**導出の根拠そのものが失われる**
2. **clamp の目的は「乱数の暴れ」を抑えること**であり、決定論的な引き戻しを抑えることではない。回帰項は `r ×（中心からの距離）`で自然に有界である
3. **合計に clamp すると、最も引き戻したい極端なアレルほど引き戻しが弱くなる**（実測: アレル値900で −90.0 → −76.5、値1000で −110.0 → −90.6）。結果として選抜下の平衡が上がる（実測: V-2b 天井 57〜60% → 61〜64.5%、V-1 15.31% → 14.52%）。血統インフレ抑制という回帰の目的に逆行する

#### 形質ごとのパラメータ 〔2026-08-04 改訂・D-009〕

| 形質群 | sd | clamp幅 | 回帰率 | **品種中心** | 創始アレルSD |
|---|---|---|---|---|---|
| 能力5種（SP/ST/PW/GT/IQ） | **90**（V-1 から較正） | ±150 | 0.20 | 450 | 110 |
| 丈夫さ (durability) | **66** | ±109 | 0.20 | **650** | 100 |
| 気性 (temper) | **10** | ±16 | 0.20 | **50** | 15 |
| 芝/ダート適性 | **13** | ±22 | 0.20 | **55** | 20.2 |
| 道悪適性 (heavy_aptitude) | **13** | ±22 | 0.20 | **55** | 20.2 |
| distance_center | **341** | ±568 | 0.20 | **2100** | 519.6 |
| distance_range | **114** | ±189 | 0.20 | **700** | 173.2 |

- **回帰率は全形質共通の 0.20**。clamp幅は sd の 1.67倍で統一
- **★非能力形質の sd は創始アレルSDから導出する**（D-012 / D-013 で切断補正を追加）:

```
sd = 創始アレルSD × √(2r − r²) ÷ 0.916
```

- 値域比で機械的に縮尺すると形質ごとに平衡が創始水準からずれる（実測: 芝適性は 0.71倍に**収縮**＝馬場適性の個性が消え、丈夫さは 1.41倍に**拡大**＝故障率のばらつきが広がる）
- **÷0.916 は clamp による切断の補正**（D-013）。clamp を sd の 1.67倍に置くと、打ち切られたノイズの実効SDは `0.916 × sd` になる（解析値: `E[Y²]/σ² = [(2Φ(1.67)−1) − 2·1.67·φ(1.67)] + 1.67²·2(1−Φ(1.67)) = 0.840`）。補正しないと平衡が創始の 0.92倍に留まり、V-2e の下限0.8までの余裕が 0.04 しか残らない（実測）。**定量できる系統誤差はゲート幅で吸収せず、導出式で打ち消す**
- **能力5種だけは例外**で、sd は V-1（同一配合のばらつき12〜18%）から較正する。能力は方向性選抜下にあり V-1/V-2a/V-2b が直接管掌するため
- **丸めの規約**（2026-08-05 明文化）: `sd` は導出値を四捨五入。`clamp` は**丸める前の sd** × 1.667 を四捨五入する（丸めた sd から再計算すると気性が17になり表と食い違う）。**表の値は導出式の出力であり、両者が食い違ったら式が正**

**★品種中心は「値域の45%」ではなく、その形質の創始分布の期待値（§FOUNDERS の定義）を用いる。** 値域から機械的に算出すると、創始中心と一致するのは能力5種だけで、他形質は世代とともに恒久的にずれる。

> **なぜこの規定が要るか（2026-08-04 レビュー側監査で判明）**: 回帰中心を全形質一律に「値域の45%」とした実装では、**丈夫さが創始650から450へ恒久的に移動**した（実測: 20ゲーム内年で収束）。丈夫さは種牡馬選抜の対象外なので引き戻しに対抗する力がなく、§7.5 の故障率の分母であるため `INJURY_BASE` の較正が丸ごと無効化される。気性50→45・適性55→45 も同様。**選抜圧のかからない形質ほど、回帰中心の誤りがそのまま恒久的な水準ずれになる。**

> **距離系だけ sd を絶対値で持つ理由（2026-08-04 改訂・D-010）**: 距離適性は値域が広い（center 1000〜3600）ため、変異幅を値域比で機械的に縮尺すると分散が正しく制御できない。**sd は回帰率に従属する**ので、平衡式から逆算して固定する:
>
> ```
> 平衡SD = sd / √(2r − r²) × 0.916   →   sd = 創始アレルSD × √(2r − r²) ÷ 0.916
> distance_center: 519.6 × √0.36 ÷ 0.916 = 341
> distance_range : 173.2 × √0.36 ÷ 0.916 = 114
> ```
>
> **回帰率を変えても sd を追随させれば平衡SDは変わらない**（創始水準に保たれる）。変わるのは**平均を固定する強さ**で、これは回帰率に比例する（平均のずれ ∝ 大物覚醒のラチェット押し上げ量 ÷ 回帰率）。したがって距離系にも共通の 0.20 を使う。
>
> 旧版（r=0.01・sd 73/24）は分散は保てるが平均を固定できず、**大物覚醒のラチェットで `distance_range` が +12〜16% 上振れして V-2d を割った**（実測・4シードとも）。`distance_range` の上振れは「全馬が万能型に近づく」ことを意味し、§5.2 の分化を損なう。

- **値域比による縮尺は使わない**（D-012 で廃止）。非能力形質の sd/clamp は上表のとおり創始アレルSDから導出する。値域比だと形質ごとに平衡が創始水準からずれる
- **負にも振れる**（怪物も駄馬も生まれる）

#### 平均回帰の設計意図（血統インフレの唯一の実効的な抑制機構）

P0 の実測により、§13.3 が当初挙げていた「抑制の三本柱」（上限1000・対称な突然変異・headroom）は**いずれも平均の上昇を止めない**ことが判明した:

- 上限1000 は創始平均450から見て +122% の位置にある「壁」であって抑制力ではない
- 平均0の対称な突然変異は、方向性選抜と組むと毎世代その正の裾が選ばれるため**抑制どころか燃料**になる（実測: 変異ゼロで +65%→+44%）
- headroom は `current`（調教）側の機構で `potential` に作用しない（§7.3）

回帰項は実在サラブレッドの**選抜停滞（selection plateau）**を表現する。分散（＝ V-1 のばらつき）は突然変異で維持したまま、平均だけが平衡点に落ち着く。**300ゲーム内年でも +32% で横ばい・V-1 14.91% 維持**を実測で確認済み（回帰なしでは +110%・V-1 3.24% に崩壊）。

> **ゲーム体験上のトレードオフ（オーナー承認済み）**: 回帰は「血統の優位が世代を経ると平均へ引き戻される」機構であり、偏差の20%/世代を引き戻す（半減期約3世代 ≒ 実時間1ヶ月）。プレイヤー体験は「**良い配合を続けている限り上位にいられるが、放置すれば平均に戻る**」となる。§1.1/§1.4-3 の蓄積感は「一度築けば永続する優位」ではなく「**選び続ける腕前が上位を保つ**」形で実現する。新規参入者が追いつけない断絶を防ぐ効果も併せ持つ。

### 6.5 インブリード（近親配合）

近交係数は Wright の式（共通祖先の探索は5代まで・`pedigree_cache` で O(1) 参照）:

```
F = Σ_A Σ_paths (0.5)^(n1 + n2 + 1) × (1 + F_A)
n1 = 父から共通祖先Aまでの世代数 / n2 = 母からAまでの世代数
```

**経路の排他条件（必須）**: 父側経路と母側経路が、共通祖先 A 以外の individual を共有する場合、その経路対は数えない。これを省くと親子交配などで F が過大になる。

**参考値**（実装のテスト基準）: 親子交配 = 0.25 / 全兄妹 = 0.25 / **3×4クロス = (1/2)^(2+3+1) = 1/64 = 0.015625**（サラブレッド生産で言う「3×4 = 1.56%」）/ 無関係 = 0。

補正:

```ts
boost = min(F * 4.0, 0.30)          // 素質値ボーナス 最大+30%
potential[key] *= (1 + boost)
potential[key] = lerp(potential[key], 共通祖先のアレル値, min(F * 1.2, 0.35))  // 血の濃縮

// 代償
temper     += F * 120               // 気性難化
durability -= F * 350               // 脆くなる
injuryRateMult = 1 + F * 2.0        // 故障率倍増
```

**近交弱勢**: F > 0.25 のとき25%で「虚弱」フラグ（durability 追加-200・成長型が強制 late 化。素質ボーナスは維持 — ハイリスク・ハイリターンを崩さない）。

**設計意図**: 「3×4のクロスは爆発力があるが壊れやすい」という競馬ファンの実感をゲーム的な賭けにする。**安全な強化手段にしないこと**（§1.4-2）。5代以内に2回以上出現する祖先は「◯◯の3×4」として血統表で強調表示 — 配合画面の主役UI。

### 6.6 ニックス

- `nicks` テーブル（父系ライン×母父系ライン → 倍率 1.00〜1.15）を参照。ヒット時は「黄金配合」バッジ表示
- テーブルは**運営が編集可能** = 新ニックス追加でメタが動くライブオプス素材

### 6.7 生産上限（インフレ防止）と所有上限

- 繁殖牝馬: 年1回のみ受胎、生涯8産まで
- 種牡馬: 年間種付上限 = `20 + G1勝利数 × 10`（名馬の血は多く残せる）
- 所有上限: 現役15頭、繁殖牝馬10頭、種牡馬5頭
- **⚠️ 上限の有償拡張（サブスク等）は v1 では行わない**。GORILLA 案の「サブスクで拡張」は本作では**憲法抵触の疑い**がある: 金を払った者が景品獲得能力で有利になる構造は「無償参加」の法的根拠を弱める。有償要素の導入はすべて弁護士ゲート事項（§0.3）とする。v1 の収益は広告のみ（§1.2）

## §7. 育成（週ターン制） 〔確定 2026-08-04〕

> 出典: GORILLA v1.0 §7 を採用。通貨は EP（参加ポイント）に置換 — **調教費は EP の最大の消費先（シンク）であり、「外部で稼いだ参加ポイントを注ぎ込む先」の本体**。

### 7.1 タイムライン（馬の一生）

| 週齢 | 内容 |
|---|---|
| 0〜78週（〜1歳半） | 育成不可（成長待ち）。放牧場で自動経過 |
| 78〜104週 | 調教開始可能。デビュー前調整 |
| 104〜260週（2歳〜5歳末） | 現役期間。キャリア上限 24戦 |
| 260週〜 | 強制引退 → 種牡馬 / 繁殖牝馬 / 功労馬 |

- **1ゲーム内週 = リアル4時間（確定・D-007）**: 1日6週進行
- 放置していても週は進むが、調教指示を出さない週は「軽め調整」扱いで成長が鈍る — **これがデイリー来訪の動機**（ポイ活の毎日習慣と噛み合わせる）

### 7.2 調教メニュー（8種）

| メニュー | 主効果 | 副効果 | 疲労 | EP（初期値・§13で調整） |
|---|---|---|---|---|
| 坂路 | SP+, PW+ | — | +18 | 300 |
| ウッドチップ | ST+, GT+ | — | +15 | 300 |
| プール | ST+ | 疲労-5（相殺） | +6 | 400 |
| ゲート練習 | IQ+ | 出遅れ率↓ | +8 | 200 |
| 併せ馬 | GT+, IQ+ | 気性-2 | +20 | 500 |
| 追い切り | 全能力+（係数1.6） | 故障率↑↑ | +32 | 800 |
| 軽め調整 | 全能力+（係数0.3） | — | +4 | 100 |
| 休養（放牧） | — | 疲労-35, 気性-5 | — | 0 |

> **★気性の変化には genotype が定める下限がある**（2026-08-11・D-049）。`temperDelta`（休養 −5 / 併せ馬 −2）に下限も回復も無いため、**実測で 1,800頭の平均が 51.0 → 0.1、単頭では 199/200 が平均40週で 0 に到達**していた（現役期間は182週）。**適切に育てるほど早く形質が消える。** これは能力の `current ≤ potential` と同じ種類の要請であり、**下限 `TEMPER_FLOOR = genotype の気性 × TEMPER_FLOOR_RATIO`（較正定数）を下回らない**とする。**変化量は残り幅に比例させる**（`headroom` と同じ形）ので、飽和して張り付かない。
>
> **★V-2e / V-2f は誕生時の値を見るため、この崩壊を検出できない**（R-16）。→ **V-15 を新設**（§13.2）。

### 7.3 成長式

```ts
gain(key) = BASE_GAIN                     // ★較正定数。既定 12 は出発点であり固定値ではない（D-045）
          * menuCoef(menu, key)           // 0〜1.6
          * growthCoef(growthType, ageWeeks)   // 成長型カーブ（下表）
          * temperCoef(temper)            // ★temper で連続に補間（D-044）: temper 0 → rand(0.9,1.1) / temper 100 → rand(0.5,1.3)
          * conditionCoef(condition)      // 0.7〜1.3
          * headroom(key)                 // 漸近係数（下記）
          * rand(0.85, 1.15)

headroom(key) = pow( (potential[key] - current[key]) / potential[key], 0.7 )
```

`headroom` により素質上限に近づくほど伸びが鈍る。**current が potential を超えることは絶対にない**（超えたら素質値＝遺伝の意味が消える）。

- **★temperCoef は2分類ではなく連続補間**（2026-08-10・D-044）。正典の2値（温順 `rand(0.9,1.1)` / 気性難 `rand(0.5,1.3)`）を**両端**として temper 0〜100 で線形補間する。**創始水準がちょうど 50 なので、そこに境界を置くと集団の半分が境界上に乗り、変異 ±1 で成長のばらつきが不連続に切り替わる**（temper の変異SDは10）
- **★主効果 `MAIN_EFFECT_COEF` / 副効果 `SIDE_EFFECT_COEF` は較正定数**（正典は `追い切り 1.6` と `軽め調整 0.3` の2つしか与えていない）。**係数が1だと調教104週で素質上限に張り付き、キャリア後半78週の伸びが 0.00 になって §7.1 のデイリー来訪の動機が3歳以降に消える。** 値そのものではなく**引退時の素質開放率**をゲートにする（§13.2 V-14）
- **★`conditionCoef` は線形**: `0.7 + 0.6 × condition/5`
- **★成長曲線（104/156/208/260週）の点間は線形補間**。階段にすると156週で伸びが不連続に跳ぶ。**104週未満は104週の値を使い外挿しない** — 外挿すると `late_bloomer` が78週で負になり、**調教すると能力が下がる**

| 成長型 | 104週(2歳) | 156週(3歳) | 208週(4歳) | 260週(5歳) |
|---|---|---|---|---|
| early（早熟） | 1.5 | 1.2 | 0.6 | 0.3 |
| normal | 1.0 | 1.2 | 1.0 | 0.7 |
| late（晩成） | 0.5 | 0.9 | 1.3 | 1.2 |
| late_bloomer（超晩成） | 0.3 | 0.6 | 1.2 | 1.5 |

### 7.4 疲労と調子

```
condition (0..5) を毎週再判定:
  base = 3 - floor(fatigue / 25) + rand(-1, +1)
  condition = clamp(base, 0, 5)
```

- 疲労70以上: 調子は最大2止まり — **★この規定は①の式に完全に包含されており、一度も発火しない**（疲労0〜100の総当たりで確認・D-044）。**冗長な安全網として残すが、「効いていないから」と①を書き換えないこと。** 実装は「一度も発火しない」ことをテストで固定する
- **★`rand(-1, +1)` は整数**（`condition` は 0..5 の段階値であり、実数だと `conditionCoef` の対応が崩れる）
- **★疲労の値域は 0..100**（上限が無いと追い切り連投で青天井になり、§8b のゲージ初期値が下限に張り付き続ける）
- **★週ごとの自然回復 `FATIGUE_RECOVERY`（較正定数・初期値 5）を毎週引く**（2026-08-10・D-045）。これが無いと **軽め調整の +4 が回復せず累積し、放置馬が慢性疲労になる** — 実測で**放置の平均疲労 77.9 が追い切り偏重の 69.2 を上回っていた**（追い切り偏重は休養0EP を挟めるため）。**何もしない馬が、最も激しい調教をした馬より疲れているのは設計として逆**。自然回復を入れると 軽め(+4) は実質 −1、追い切り(+32) は +27 となり、**疲労が「強度に対して」たまる**
- 疲労90以上: 出走時に大幅マイナス（§8.3 fatigueCoef）
- **調子・疲労は介入のスタミナゲージ初期値にも影響**（§8b）— 仕上げの良し悪しが「操作の効き」に直結する

### 7.5 故障判定

```ts
injuryProb = 0.0018
  * (1 + fatigue / 40)
  * (1000 / durability)
  * menuIntensity          // 追い切り2.2 / 坂路1.3 / プール0.5 / 休養0
  * injuryRateMult         // インブリード由来（§6.5）
  * ageFactor              // 4歳以降1.0 → 5歳末1.6 へ線形
```

| 重篤度 | 確率 | 休養週 | 恒久ダメージ |
|---|---|---|---|
| 軽度（挫石等） | 60% | 3〜6 | なし |
| 中度（球節炎等） | 30% | 10〜20 | potential SP -3% |
| 重度（屈腱炎等） | 9% | 30〜52 | potential 全体 -8%, durability -100 |
| 致命的 | 1% | — | 競走能力喪失 → 強制引退（繁殖入りは可能） |

> **★恒久ダメージは `potential` を削り、同時に `current` を新しい `potential` まで切り下げる**（2026-08-10・D-044）。§7.3 の不変条件（`current ≤ potential`）と両立させる唯一の形であり、**成熟した馬にこそ故障の代償が生じる**という点でも正しい（据え置きにすると、既に上限に達した5歳馬は重度故障でも実質無傷になる）。**★切り下げは故障処理の中で明示的に行う** — 成長関数の副作用として起こすと、発生時点がずれ、成長を通らない週で不変条件が破れる。**★恒久ダメージはプレイヤーに明示する**（黙って8%削るほうが「理不尽」である）
>
> **馬を死なせない。** 致命的故障でも「引退」にとどめる。心理的ダメージが大きすぎて離脱要因になる。景品のかかるゲームではなおさら「理不尽な全損」を作らない。

### 7.6 育成イベント

週進行時に低確率で発生するテキストイベント（選択肢付き）。レトロUIのテキストボックスと相性が良く実装コストも低い。

- 「厩務員が言うには、調子が上向いてきたようだ」→ 調子+1
- 「気性が荒くなっている」→ 放牧 or 強行の選択（強行は IQ+ だが気性+10）
- 「素質が開花した！」（1%）→ potential のうち1形質 +5%

## §8. レースシミュレーション + Provably Fair 〔確定 2026-08-04〕

> 出典: GORILLA v1.0 §8 を採用。第1世代の8係数式は**廃棄**（二系統の式を残さない）。

### 8.1 方針

「実力スコア＋乱数」でソートして着順を決める。物理シミュレーションは行わない。ただし着順確定後に**それらしい位置取り推移を逆生成**して描画に渡す（§8.7）— 観戦の見た目はリッチに、計算は軽く。

### 8.2 基礎スコア（距離帯ごとの能力重み）

| 距離 | SP | ST | PW | GT | IQ |
|---|---|---|---|---|---|
| 〜1400m | 0.42 | 0.08 | 0.24 | 0.16 | 0.10 |
| 1401〜1800m | 0.34 | 0.18 | 0.18 | 0.20 | 0.10 |
| 1801〜2200m | 0.28 | 0.26 | 0.16 | 0.20 | 0.10 |
| 2201〜2800m | 0.22 | 0.34 | 0.14 | 0.20 | 0.10 |
| 2801m〜 | 0.18 | 0.42 | 0.12 | 0.19 | 0.09 |

```
base = Σ (stats[key] * weight[key][distanceBand])
```

### 8.3 補正係数（すべて乗算）

```ts
score = base
  * distanceAptitudeCoef   // 距離適性(§5.2)を 0.75〜1.05 にマップ
  * surfaceCoef            // 芝/ダート適性を 0.70〜1.05 にマップ
  * trackConditionCoef     // 馬場状態適性。良馬場は常に1.0
  * strategyCoef           // 展開との噛み合い(§8.4)
  * conditionCoef          // 調子 0.88〜1.10
  * fatigueCoef            // 1 - fatigue/500
  * weightCoef             // 斤量 基準55kg・±1kgごとに∓0.8%
  * gateCoef               // 枠順。距離とコース形態で内外の有利不利
  * ageCoef                // 2歳0.88 → 4歳1.00 → 5歳末0.96
  * skillCoef              // 発動スキル(§8.5)
```

### 8.4 展開（ペース）

```
逃げ宣言頭数 n から決定:
  n >= 3 → ハイペース   (差し/追込 +8%, 逃げ -10%)
  n == 2 → ミドル       (補正なし)
  n <= 1 → スローペース (逃げ/先行 +9%, 追込 -8%)
```

さらに脚質適性 `strategy_aptitude[選択脚質]/100` が 0.85〜1.05 で効く。**適性のない脚質を指定すると露骨に弱くなる** — 脚質選択を意味ある意思決定にする。

### 8.5 スキル（発動型・各馬最大3つ）

| スキル | 発動条件 | 効果 |
|---|---|---|
| 末脚爆発 | 直線・6番手以下 | 直線区間スコア +12% |
| 逃げ粘り | 直線・先頭 | 直線区間スコア +10% |
| ゲート巧者 | スタート | 出遅れ無効化 |
| 雨得意 | 馬場が良以外 | trackConditionCoef を 1.06 に上書き |
| 内枠強者 | 枠1〜4 | gateCoef +5% |
| 長距離砲 | 2400m以上 | ST 寄与 +15% |

発動率 = `40% + IQ/2000`。

### 8.6 乱数と公正性（Provably Fair・景品があるゲームの生命線）

```
1. レース作成時: server_seed を生成し、seed_commit = SHA256(server_seed) を公開
2. 締切時:      client_entropy = 全出走馬IDのハッシュ結合
3. 確定時:      final_seed = HMAC-SHA256(server_seed, race_id + client_entropy)
4. レース後:    server_seed を公開（seed_reveal）
→ 誰でも SHA256(seed_reveal) == seed_commit を検証でき、
   「結果は発走前から確定していた乱数で作られ、事後に差し替えられていない」ことを確認できる
```

- PRNG は決定的なもの（xoshiro128** 等）を使い、**同じ seed から同じレースが完全再現できる**こと（リプレイ・サポート対応・不正調査に必須）
- **結果の事後差し替えは絶対禁止**。障害時はレース不成立＋全ベット返還（§10）。整合性を一度でも崩すと信用が終わる

### 8.7 着順と着差の生成

```ts
finalScore_i = score_i * (1 + raceNoise()) * interventionMult_i;

// ★raceNoise は「裾の厚い」混合分布（2026-08-06・D-021）
//   確率 (1 − TAIL_MIX_P) で N(0, K)
//   確率 TAIL_MIX_P     で N(0, K × TAIL_MIX_M)
//   既定: K = 0.22 / P = 0.03 / M = 5
// interventionMult は §8b。介入受付締切後にサーバーが確定計算
順位 = finalScore 降順

// 着差 → タイム変換
baseTime  = distance / averageSpeed(distance, surface, condition);
timeGap_i = (finalScore_1 - finalScore_i) / finalScore_1 * baseTime * 0.55;
```

- 着差ラベル: `<0.03秒=ハナ, <0.06=アタマ, <0.10=クビ, <0.16=1/2馬身, …`
- **K の意味**: 上げるとレースが荒れる。**既定 0.22**（D-016 → D-021 で再較正。「0.12 で1番人気30%前後」という当初の記述は実測 51.6% で誤りだった）。実装後に必ずモンテカルロ10万レースで人気別勝率を較正する（§13）
- **★なぜ混合分布か（D-021）**: 正規分布は裾が指数的に減衰するため、**最下位人気が勝つのに必要な大偏差が構造的に潰れます**。K と出走表の締め（床）はどちらも分布の「広がり」しか変えず、V-5 が V-4 のほぼ従属変数（実測 V-5 ≈ 2×V-4 − 1.5）であるため、**実質1次元のつまみで V-4/V-5/V-6 の3制約を満たそうとして実行可能領域が空になりました**（実測）。裾を厚くすると**最下位人気の勝率は桁で上がる一方、1番人気は既に約69%負けているため低下は限定的**という非対称性が得られます。現実の競馬に大穴があるのは「その日だけ格上の走りをする馬がいる」からで、**正規分布の乗数では構造的に表現できません**
- 位置取り推移データは着順確定後に逆生成し描画へ渡す。配信方式（1回配信＋クライアント再生・介入レースの2段階配信）は §14

### 8.8 介入と Provably Fair の両立（D-006 対応・重要）

介入があると「seed だけでは結果を再現できない」ため、再現性の定義を拡張する:

```
完全再現 = seed_reveal + 介入入力ログ(race_entries.interventions・タイムスタンプ付き)
```

- 乱数部分は seed commit/reveal で事後改竄不可能を証明
- 介入部分は全入力をログ保存し、「seed＋介入ログ→同じ着順」をサーバーが再計算で示せること
- 検証ページでは「乱数は事前確定・介入は締切前の入力のみ・両方の記録から結果を再現可能」の3点を示す

## §8b. レース中リアルタイム介入 〔確定 2026-08-04〕

> 出典: GORILLA v1.0 §9 + D-006 の安全条件7点（§1.5）を具体化。**育成が主・操作が従**（効果上限±10%）を絶対に守る。

### 8b.1 概要 — 4つの局面

自馬が出走するレースの観戦中、オーナーは4つの局面で指示を出せる:

```
[スタート]──[道中]───[勝負所(残り800m)]───[直線(残り400m)]──[ゴール]
 タイミング   ポジション    「仕掛け」ボタン       追う(連打/リズム)
 タップ      指示(内外/前後)
```

- 介入できるのは**自馬のオーナーのみ**。観戦者は操作できない
- 同一レースに自馬が複数いる場合、**発走前に介入対象を1頭選択**（残りは AI 代行・8b.5）
- 操作はモバイル=タップ / PC=クリック・スペースキーの両対応

### 8b.2 スタミナゲージ

自馬にのみ表示される 0〜100 のゲージ。**育成の仕上がりが操作の効きに直結する**（介入は調教の延長・D-006）:

```
初期値 = clamp( 50 + ST/20 + (condition - 3) * 4 - max(0, fatigue - 50) / 2, 20, 100 )
         // ST1000・絶好調・疲労なし → 100 / 仕上げを怠ると操作の余地が減る

毎秒消費 = (基準消費量 / レース秒数) * ポジション係数 * ペース係数   // ★D-017 で改訂
           // 先頭付近1.3 / 中団1.0 / 後方0.85
仕掛け中は消費3倍
ゲージ0 → 「バテ」: 区間スコア -25%/秒 で減衰
```

### 8b.3 介入アクションと判定

| 局面 | 操作 | 効果 | 判定 |
|---|---|---|---|
| スタート | タイミングタップ | 成功で出遅れ回避＋好位確保 | 判定幅は IQ に比例＋ラグ猶予(8b.6) |
| 道中 | ポジション指示（内/外・前/後） | スタミナ消費率が変わる。囲まれると進路確保判定 | IQ が進路確保成功率に影響 |
| 勝負所 | 「仕掛け」ボタン | 押した瞬間からスコア+、スタミナ消費3倍 | タイミングの適否で -0.08〜+0.09 |
| 直線 | 連打 or リズムタップ | GT に比例した上乗せ | 最大 +0.10 × (GT/1000) |

```ts
interventionMult = clamp(
    1 + startBonus      // -0.04〜+0.03
      + spurtBonus      // -0.08〜+0.09
      + driveBonus,     // 0〜+0.10 * (GT/1000)
    0.90, 1.10 )        // ★ハードキャップ ±10%(§1.5-1・§13)

// 早仕掛けペナルティ: 残り900m超で仕掛けたらスタミナ消費1.6倍、
// ゴール前にゲージ0なら interventionMult -0.15(クランプ前に適用)
```

### 8b.4 サーバー権威と締切

- クライアントは**入力イベントのみ**送信（種別＋クライアント時刻）。サーバーが受信時刻とともに `race_entries.interventions` に記録し、**最終スコアはサーバーだけが計算**する
- 介入受付締切 = ゴール相当時刻の0.5秒前。締切後の入力は無効・記録のみ
- 完全再現 = seed + 介入ログ（§8.8）。介入ログは不正調査・サポート対応・リプレイの一次資料

### 8b.5 AI 代行（切断・無操作フォールバック）

「介入しない/できない」が罰にならない中立既定値を持つ:

- 無操作・切断・介入対象外の自馬・観戦されていない馬は **AI 代行**で走る
- AI 代行の挙動: スタート=IQ 基準の標準判定 / 仕掛け=適正区間で自動発動 / 直線=GT 比例の平均的な追い
- 設計目標: **AI 代行の期待値 ≒ 手動操作の期待値の約95%**。上手い手動は上回り、下手な手動は下回る。「操作しないと大損」にはしない（ポイ活層のながらプレイ保護・切断補償の争いを消す）
- レース中に切断→復帰した場合、復帰時点から手動に戻れる

### 8b.6 ラグ許容（公平性）

- タイミング判定には **±150ms のネットワーク猶予幅**を持たせる（§13 で調整）
- 判定基準はサーバー受信時刻。クライアント申告時刻は参考記録（乖離が大きい端末はマクロ調査の入力になる）
- 端末性能・回線差で構造的な有利不利を作らない: 連打は **秒間15回で頭打ち**（それ以上は無効 — 人間の上限を超える入力に意味を与えない）

### 8b.7 マクロ・自動化検知

- 入力間隔の分散・反応時間の分布を記録し、**人間離れした一貫性**（毎回同一msの反応・完璧なリズム）をフラグ
- フラグ付きアカウントは景品交換審査で人間確認（§11 と接続）
- 検知ロジックの詳細は公開しない（対策の対策を防ぐ）

### 8b.8 経済監視への接続

- `interventionMult` の実測分布を日次で記録（§11 のダッシュボード項目）
- 介入はオッズ（発売前モンテカルロ・§9）に織り込めないため、**運営期待値のブレ要因として常時監視**。分布が想定（平均≒1.00・±10%内）から乖離したらアラート — 乖離はバグかマクロの兆候

## §9. ベッティング 〔確定 2026-08-04〕

> 出典: GORILLA v1.1 §16 を採用。**ベットの支払いは EP（参加ポイント）のみ・的中の払戻は PP（賞金ポイント）** — この一方通行が二種ポイント分離（憲法 §0.2-3）のゲーム内実装である。

### 9.1 券種（7種）

| 券種 | 内容 | 最小単位 |
|---|---|---|
| 単勝 | 1着を的中 | 100 EP |
| 複勝 | 3着以内を的中 | 100 EP |
| ワイド | 3着以内に入る2頭の組合せ | 100 EP |
| 馬連 | 1・2着の組合せ（順不同） | 100 EP |
| 馬単 | 1・2着を着順通り | 100 EP |
| 三連複 | 1〜3着の組合せ（順不同） | 100 EP |
| 三連単 | 1〜3着を着順通り | 100 EP |

- 出走7頭以下は複勝・ワイドを2着まで（実競馬の慣行を踏襲）
- 同着は配当均等分割。取消・除外馬を含む馬券は全額返還（EP で返す）

### 9.2 オッズ方式: 固定オッズ（理論勝率ベース）

パリミュチュエルは少人数期に破綻する（1人が買うだけでオッズが数百倍動く）ため、**v1 は固定オッズ**。

- レース生成時にモンテカルロでそのレースをシミュレートし、各馬・各組合せの的中確率を実測 → `odds = (1/p̂) × (1 − margin)`
- **★試行回数は数値リテラルで書かない**（2026-08-09・D-036）。**`M = requiredOddsTrials()` として設計式 `M ≥ λ* × ODDS_CAP/(1−margin)`（D-035）から導出する。** 当初の 10,000 回では三連単が **−20.90pt** で全く足りず、しかも `ODDS_CAP` を動かしたときに静かに不足する。`LAMBDA_STAR = 30` は較正定数（登録簿・変異試験の対象）
- **★`p̂` は凸性のバイアスを打ち消してから使う**（D-013 型）: `odds = (1/p̂)(1−margin) / (1 + (1−p̂)/(M·p̂))`。`E[1/p̂] > 1/p` は解析的に分かる系統誤差なので、ゲート幅でも標本数でもなく導出式で消す
- オッズ算出のシードは本番確定用シード（§8.6）とは**別系列**を使う
- 三連単は全組合せ厳密算出をせず、上位500組合せ実測＋残りは条件付き確率の近似式
- 介入（§8b）は事前に不可知のためオッズに含めない。±10% は運営期待値のブレとして許容し **§8b.8 の監視**で管理
- 購入時のオッズを `odds_at_purchase` として馬券に固定保存 — 後からオッズ計算を修正しても既存馬券の配当は変わらない（公正性の要）
- 将来 DAU が伸びたらパリミュチュエル移行を検討（DB スキーマは両対応で設計・§4）

### 9.3 控除率（margin）— EP 経済の最大シンク

| 券種 | margin | 還元率 |
|---|---|---|
| 単勝・複勝 | 0.18 | 82% |
| ワイド・馬連・馬単 | 0.20 | 80% |
| 三連複・三連単 | 0.23 | 77% |

**馬券は長期的に必ずマイナス**であり、PP の主な稼ぎ口は「育成した馬の賞金」である — この関係を絶対に維持する（逆転すると育成が飾りになる）。ここは PP 発行量の主要な調整弁でもある（§11）。

### 9.4 配当上限（キャップ）と ベット上限

| 券種 | 最高オッズ | | 単位 | 上限 |
|---|---|---|---|---|
| 単勝 | 500.0 | | 1点あたり | 10,000 EP |
| 複勝 | 100.0 | | 1レース・1券種 | 30,000 EP |
| ワイド | 300.0 | | 1レース合計 | 50,000 EP |
| 馬連 | 2,000.0 | | **1日合計** | **500,000 EP** |
| 馬単 | 4,000.0 | | | |
| 三連複 | 10,000.0 | | | |
| 三連単 | 100,000.0 | | | |

- キャップ到達時はオッズ表示にその旨を明示
- 日次上限は射幸性の抑制とバグ・エクスプロイト時の被害上限を兼ねる。**初期から必ず入れる**

### 9.5 自馬出走レースの制限（八百長利得の遮断装置・D-006 の前提）

介入（§8b）の有無に関わらず、自馬の成績には調教・出走選択を通じて影響を与えられる。よって:

1. 自馬が出走するレースでは、**自馬を含む馬券しか購入できない**（単勝・複勝は自馬のみ。連系は自馬を軸にした組合せのみ）
2. この「応援ベット」は 1レース合計 **5,000 EP** まで
3. 同一レースに自馬が複数いる場合は、それら全頭を含む組合せのみ可
4. 同一IP・同一デバイスの複数アカウントが同一レースに出走している場合、フラグを立てて監視ログに記録（多アカウントによる制限回避の検知）

### 9.6 購入フロー

```
発売開始（§10 のタイムテーブルによる）
発走30秒前  発売締切（固定オッズなので変動なし）
発走〜走行  介入受付（§8b・出走オーナーのみ）
確定後      的中馬券の払戻を PP として自動加算・台帳記録
```

### 9.7 転がし式ベットについて（旧「ライド」相当・v1 不採用）

前レースの払戻を全額次レースに載せる連続ベットは、**v1 では実装しない**:

- 旧仕様の「控除なし・ペイアウト100%」は経済シンクを破壊するため憲法的に不可
- 控除ありの転がしは「通常ベットを手動で繰り返す」のと数学的に同一であり、専用機能の価値は演出のみ
- v1.1 以降、リテンション施策として「控除あり・連勝演出付き」での導入を再検討してよい（その際も PP でなく EP 消費・上限適用）

### 9.8 UI 方針（§12 と接続）

- マークシート風購入画面（レトロ表現と相性最良）: 券種タブ → 馬番グリッド → 金額 → 確認
- 出走表: 馬番・馬名・性齢・斤量・脚質・直近5走・オッズ・人気
- 投票シェア表示（「みんなの投票状況」）: 固定オッズでも集団心理の演出として別途表示。オッズには影響しない
- 的中時: 8bit ファンファーレ＋派手な払戻演出。**外れ時に追い打ち演出をしない**（射幸性を煽らない側の配慮）

## §10. 開催スケジューラ + NPC馬世界 〔確定 2026-08-04〕

> 出典: GORILLA v1.1 §17-18 を採用。

### 10.1 レースサイクル（D-007 決定記録）

**10分サイクルを採用**（オーナー決定 2026-08-04）。1日144R・週1,008R。判断根拠（レビュー側の当初の「30分〜1時間に緩める」提案は試算の上で撤回済み）:

- **待ち時間が短時間セッションの生命線**: ポイ活ユーザーの1セッションは5〜15分。10分サイクルなら平均待ち5分で「広告見る→EP獲得→買う→観る→結果」が1セッションに収まる。30分サイクルは平均待ち15分 — 次のレースを待たずに離脱する
- **運営費はサイクル頻度でほぼ変わらない**（ワーカーは常時稼働・モンテカルロは1レース数百ms〜数秒）
- 頭数の薄まりは NPC 充填（10.4）が吸収する
- 荒天条項: DAU が極端に少ない深夜帯もレースは止めない（ベット対象の常時確保・NPC のみで成立）

これに伴い §7.1 の「1週=リアル4時間」も確定（1日6週進行・馬の現役期間は実時間約26日 = 約1ヶ月で世代が回る）。

### 10.2 10分間のタイムテーブル

| 相対時刻 | フェーズ |
|---|---|
| T+0:00 | 前レース確定・払戻表示 |
| T+0:30 | 次レース生成完了、出走表・オッズ公開（モンテカルロ `requiredOddsTrials()` 回・D-036。本番機実測で1周275秒＝サイクルの46%） |
| T+3:00 | 発売開始 |
| T+9:30 | 発売締切 |
| T+9:30〜10:00 | パドック・返し馬演出 |
| T+10:00 | 発走（走行75〜105秒・介入受付 §8b） |

- **レーススキップは実装しない**。観戦体験そのものが商品。他人のレースは結果一覧で確認可
- 生成は**2レース先まで先行実行**（障害時バッファ）
- **★失敗は2種類あり、扱いが違う**（2026-08-09・D-037）。旧記述「生成失敗時は開催中止とし全ベットを EP で返還」は**危険なほうの失敗を指していなかった**:

| 種別 | 状況 | 扱い |
|---|---|---|
| **G: 生成失敗** | レースが作られなかった | **馬券が存在しないので返還は不要。** 次周の欠落補完で埋める（`racesToPrepare` が現在時刻から決まり存在確認で埋めるため自動） |
| **S: 確定失敗** | **馬券が売れた後に確定できない** | **★これが危険。** 放置すると `bets` が `pending` のまま残り、**客の EP も PP も永久に動かない** |

- **確定期限**: `scheduled_at + 60分`（＝6サイクル）までに `settled` に至らないレースは、**自動的に `cancelled` にし、全ベットを EP で返還する**。配備・再起動・ヘルスチェック待ち（最大600秒）を吸収してなお十分な余裕を取る
- **返還は1トランザクションで冪等に行う**（レース状態・`bets.status`・`ep_ledger` を同時に。§14.4）
- **自動中止は必ずアラートを上げる** — 正常運用では起きない事象であり、**静かに返還されると原因が調査されない**
- **確定済みの結果は事後に差し替えない**（§8.6）。自動中止の対象は `settled` に至っていないレースのみ
- **★中止経路は本番ループから呼ばれていなければ意味がない**。「実装した」と「本番の経路に繋がっている」は別（D-037 の発見経緯）

### 10.3 番組表（1日144R の内訳・10分案）

| クラス | R数/日 | 備考 |
|---|---|---|
| 新馬・未勝利 | 42 | デビュー直後の受け皿。常時空きを確保 |
| 1勝クラス | 36 | |
| 2勝クラス | 24 | |
| 3勝クラス | 18 | |
| オープン | 15 | |
| オープン特別・重賞 | 9 | 時刻固定。朝・昼・夜の目玉枠に配置 |

- **★9 R/日 = 63枠/週 のうち、重賞は 31 で残り 32 は「オープン特別」（重賞でない特別戦）**（2026-08-08・D-030）。当初「重賞 9 R/日」と書いていたが、週次頻度の合計（31）と週枠数（63）が一致していなかった。**重賞は週単位で配置し、埋まらない枠はオープン特別が受ける**
- 重賞頻度（週次）: G1=3 / G2=8 / G3=20。G1 は視聴の集まる時間帯に固定し告知を打つ
- **⚠️ 要検証**: 週3 G1 = 年156 G1 は現実（JRA 年約24）より桁で多い。**§6.7 の種付上限 `20 + G1勝利数 × 10` に効くため、G1 勝ち馬が普通になると上限が広く緩み、D-026 で特定した系統集中の経路を太らせる**。P3 で「G1 勝利頭数の分布」と「実効種付上限の分布」を測ること
- レース名・競馬場名はすべて架空名（§0.1）。年間カレンダー（クラシック三冠・古馬G1・ダート路線）は §12 執筆時に命名

### 10.4 出走馬の構成

> **★クラス制の必然的な帰結 — 育成の報酬は「勝率」ではなく「昇級」である**（2026-08-05・D-020）
>
> P1 の実測で **`winRate` が馬の能力とほぼ無相関（r = −0.003）**であることが判明した。馬はクラス帯で能力別に振り分けられるため、**強い馬は強い帯で走り、どの帯でも「同格の中での勝率」しか出ない**。実際の競馬のクラス制度と同じ構造である。
>
> したがって **§1.1「世代を重ねるほど強くなる実感」は、勝率の上昇ではなく「より上のクラスで走れるようになること」として現れる。** この含意は次に効く:
> - **§11 賞金設計**: クラスが上がるほど賞金が跳ねる形にしないと、育成の報酬が体感できない
> - **§12 UI**: 「勝率」を主指標にすると育成が報われて見えない。**クラス（格）と獲得賞金**を主役にする
> - **選抜指標**: 勝率は能力を測らないので使えない。**1走あたり賞金**（クラスの重みを含む）を使う

- **★出走馬は同格帯（クラス）から組む**（2026-08-05・D-018）。血統プール全体から無作為に引くと最強馬と最弱馬の能力差が実際の番組より遥かに広くなり、**1番人気の勝率が約51%まで上がって V-4 が壊れる**（実測）。クラス幅は母集団の6%を既定とし、§10.3 の番組表（新馬〜重賞のクラス階層）と §10.5 の「NPC はプレイヤー上位30%×0.92 に追従」＝**同格が集まる**設計に対応する。**この記述が無いと、P2 の開催スケジューラを別のセッションが実装したときに無作為抽選になり V-4 が静かに壊れる**
- 1レース **8〜18頭**。プレイヤー馬を優先し、残りを NPC 馬で充填
- **プレイヤー馬0頭でもレースは開催**（ベット対象の常時確保）
- エントリーは発走60分前まで受付。上限超過は**完全抽選**（賞金上位優先にしない — 新規が弾かれると離脱する）
- 出走登録料: 200 EP（§11 の経済表と連動）
- 介入(§8b)があるため、**自馬の出走レースは自分で時刻を選んでエントリーする**設計が重要 — 「観られる時間に走らせる」がプレイヤーの自然な行動になる。放置馬の自動出走はさせない

### 10.5 NPC 馬世界

NPC は3機能を担う本作の重要インフラ: ①出走頭数の充填 ②ベット対象の供給 ③**血統プールの供給**（引退した NPC 名馬が種牡馬市場に流入 — 初期プレイヤーの配合はここから始まる）。

- NPC 馬は**プレイヤー馬と同一の遺伝エンジン（§6）で生成**。専用の簡易ロジックを作らない（同じ土俵にいることが公正性の担保）
- プールサイズ: 現役 **2,500頭** / 種牡馬 200頭 / 繁殖牝馬 800頭
- NPC 厩舎を40前後用意し個性を持たせる（短距離特化・ダート専門・晩成型など）→ 「あの牧場の血は買い」というプレイヤー知識=メタが生まれる。冠名で牧場の人格を出す
- NPC も週進行で自動育成（厩舎方針別の調教AI）。成績上位は自動で種牡馬・繁殖入り
- **★NPC の配合相手は厩舎方針に沿って選ぶ**（2026-08-06・D-025）。全厩舎から無差別に「良い種牡馬」を選ぶと、**産駒が継ぐ父系ラインが優れた系統に収束する**（P1.5 の実測: 50世代で最大シェア51.5%）。短距離特化の厩舎は短距離向きの、ダート厩舎はダート向きの相手を選ぶ — **厩舎ごとに評価軸が違うことが、系統の多様性を保つ機構**になる。§10.5 が厩舎に個性を持たせている理由の半分はこれである（もう半分は「あの牧場の血は買い」のメタ）

> **★父系ラインの集中は「異常」ではない**（D-025）。現実のサラブレッドでもサンデーサイレンス系が日本の5割前後を占める。**最大シェアの上限は設けない。** 困るのは集中そのものではなく:
> - **ニックス（§6.6）が `sire_line × bms_line` の組で効くので、組み合わせが枯れること**
> - **アウトブリードの選択肢が消え、近交管理ができなくなること**
>
> したがって測るべきは **有効系統数**（`1 / Σ(shareᵢ²)`・逆シンプソン指数）と、**その推移**である:
> - **有効系統数 ≥ 5**（**プリシード深度 = 50世代の時点で判定**）— ニックスが 5×5 = 25通りの組で成立する下限
> - 100世代以降の減少は**ライブ運用の監視項目**であり、開始時点のゲートに混ぜない（D-026）

> **★D-026（2026-08-06）— 系統集中の原因は「決定経路に血統を参照する項が無いこと」**
>
> 実測で否定された仮説: ①遺伝的浮動が主因（創始系統を 40→200 に増やしても有効系統数は不変）、②上位K頭への分散が効く（**悪化した**: y50 8.83→2.28）。
>
> 確定した機構: **40厩舎 × 繁殖牝馬20頭で、1厩舎が1頭に集中させると種付数がちょうど年間上限20 に一致する。** そのため K=1 では上限が**偶然の集中防止装置**として働き、毎年きっかり40頭が使われる（プール200頭のうち160頭は未使用）。分散させると上限に余裕が生まれ、**全厩舎が globally 最良の種牡馬を共有できるようになって悪化する**。
>
> **`policyFit` が選抜しているのは形質であり、父系ラインは形質と独立に伝わる。** 優れたラインの産駒は短距離にもダートにも道悪にも現れるので、**厩舎ごとに評価軸を変えても全厩舎が同じラインの息子を選ぶ**。→ **系統を保存するには、配合の決定経路が血統を参照していなければならない。**
>
> 対処は **NPC 配合 AI（`mateScore`）への近交回避項の追加**とする。**`canMate`（エンジンの規則）は触らない** — 現実の生産者も近交を禁止ではなく割り引いて評価するし、AI の選好に留めれば遺伝エンジンに波及しない。**年間種付上限（§6.7）は下げない**（プレイヤーに見える正典値であり、現実の種牡馬は年100〜200頭に種付けするので20 は既に低い）。
- NPC 種牡馬の種付料は自動設定: `3,000 + G1勝利数 × 8,000 + 総獲得賞金/20`（EP建て・**支払われた EP は焼却** = 重要シンク）
- **能力分布は追従型**: `NPC平均 = プレイヤー上位30%の平均 × 0.92`（週次再較正）。固定値だとプレイヤーが育つほど NPC が無価値になり、強すぎる固定だと新規が勝てない。係数0.92と週次という緩やかさを守る（追従が強すぎると成長実感が消える）
- **プリシード**: サービス開始前に NPC 世界を**50世代オフラインで回す**（P1.5）。開始時点で5代血統表が埋まった「歴史ある血統」が存在すること — 体験の質を大きく左右する必須工程。創始世代の生成規則は DEV_INSTRUCTIONS_P0 §3.3
- 馬名は音節結合方式で自動生成し、**実在競走馬名 NG リストと突合**（生成時・ユーザー命名時の両方・§0.1）

## §11. 賞金ポイントの発行と景品交換 〔確定 2026-08-06〕

### 11.1 賞金テーブル（クラス別・PP）

**クラスが上がるほど賞金が跳ねる形にする**（D-020）。育成の報酬は「昇級」として現れるので、**格の差が賞金の差として体感できないと、育成が報われて見えない**。

| 格 | 1着 | 2着 | 3着 | 4着 | 5着 |
|---|---|---|---|---|---|
| G1 | 100,000 | 40,000 | 25,000 | 15,000 | 10,000 |
| G2 | 50,000 | 20,000 | 12,000 | 7,000 | 5,000 |
| G3 | 30,000 | 12,000 | 7,000 | 4,000 | 3,000 |
| オープン | 18,000 | 7,000 | 4,000 | 2,500 | 1,800 |
| 3勝クラス | 12,000 | 4,800 | 2,800 | 1,700 | 1,200 |
| 2勝クラス | 8,000 | 3,000 | 1,800 | 1,100 | 800 |
| 1勝クラス | 5,000 | 2,000 | 1,200 | 700 | 500 |
| 新馬・未勝利 | 3,000 | 1,200 | 700 | 400 | 300 |

**★選抜指標の `prize_per_start` はこの表の重みを含む**（§4.2）。だから勝率と違って能力を測れる（P1 の実測: 能力相関 0.639 対 勝率 −0.003）。

### 11.2 PP の発行量監視（最重要）

PP は**景品という現実の価値に繋がる出口**を持つ。したがって:

- **1日の純発行量をダッシュボードで毎日確認する**（§4.6・V-11）
- 発行の内訳を分けて記録: **賞金**（レース結果・予測可能）と **馬券の払戻**（固定オッズ・変動する）
- **固定オッズは運営が変動リスクを負う**ため、高配当の連続で PP が急膨張しうる。`margin_actual` の乖離3%でアラート（§3.6）

### 11.3 景品交換（構造は固定・カタログは運用）

**構造（変えられない）**:

| # | 規則 |
|---|---|
| **P-1** | **現金・暗号資産への交換経路を作らない**（S-3） |
| **P-2** | **ユーザー間の譲渡・売買を作らない**（S-4） |
| **P-3** | **PP→EP の還流を作らない**（S-5・D3） |
| **P-4** | 交換は**不可逆**。交換後の取り消し・払い戻しをしない（会計と在庫が壊れるため） |
| **P-5** | **交換履歴を全件記録**する（§4.7 `prize_exchanges`）。景表法上の説明責任と RMT 検知の両方に要る |

**カタログ（運用で差し替え可）**: 具体的な景品はオーナーが決める。**ギフト券系は現金等価性が高い**ので、公開前の弁護士確認（§17.1 L-5）で扱いを確認する。

**交換フロー**:

```
PP 残高の確認 → 景品選択 → 在庫確認 → 確認画面 → 交換実行
  → pp_ledger に reason='prize_exchange' で記録（同一トランザクション）
  → prize_exchanges に履歴
```

**在庫**: 景品ごとに在庫を持ち、**在庫切れの景品は交換画面に出さない**（選ばせてから断らない）。

**上限**: 1ユーザーあたりの期間内交換上限を設ける。**射幸性の抑制**と**バグ・エクスプロイト時の被害上限**を兼ねる（EP の日次上限と同じ考え方）。

### 11.4 原資会計

**景品の原資は広告収益**（§1.2）。以下を月次で突き合わせる:

| 項目 | 内容 |
|---|---|
| 広告収益 | 当月の実収 |
| 景品原価 | 当月の交換実績の原価合計 |
| **カバー率** | 景品原価 ÷ 広告収益。**恒常的に100%を超えるとサービスが成立しない** |
| PP 残高総量 | 未交換の PP。**将来の景品債務**に相当するので推移を見る |

> **★PP の総残高は「いつか景品に換わりうる量」である。** 発行量だけでなく**残高の推移**を見ること。残高が積み上がり続けると、ある時点で一斉に交換されたときに原資が足りなくなる。

### 11.5 本人確認

**要否は公開前の弁護士確認（§17.1）で決まる。** 現時点では:

- **高額景品には本人確認を求める**方向で設計しておく（後から足すより、枠だけ作っておくほうが安い）
- **マクロ検知でフラグが付いたアカウント**は、交換時に人間確認を挟む（§8b.7）
- 同一IP・同一デバイスの複数アカウントによる交換集中を監視（§3.7）

## §12. UI / UX 〔確定 2026-08-06〕

### 12.1 ビジュアル方針

- **ドット絵ベースの 2D 横スクロール**。競走馬は 32×32 前後のスプライト、4〜6フレームのギャロップアニメ
- 配色は16色程度に制限したレトロパレット。CRT スキャンライン風のオーバーレイを薄く乗せる
- フォントは日本語ビットマップフォント（**ライセンス確認必須**・§17 M-1）
- 効果音は 8bit/16bit 風のチップチューン（オリジナル制作）
- **3D は使わない**。差別化とコストの両面で 2D が正解

### 12.2 画面一覧

| 画面 | 内容 | Phase |
|---|---|---|
| 牧場（ホーム） | 所有馬一覧・今週の指示待ち馬にバッジ | P3 |
| 馬詳細 | ステータス（**星表示**）・適性チャート・戦績・血統表 | P3 |
| 調教 | メニュー選択・疲労/調子・週送り | P3 |
| **血統表** | **5代表示。クロスは色付きで強調**（配合画面の主役） | P5 |
| 配合 | 種牡馬検索 → 相性プレビュー（近交係数・ニックス・予測適性レンジ） | P5 |
| レース登録 | 出走レース選択・脚質・斤量確認 | P2 |
| **レース観戦** | 横スクロール描画＋**介入UI**＋実況テキスト | P4 |
| **馬券購入** | **マークシート風**（券種タブ → 馬番グリッド → 金額 → 確認） | P2 |
| 種牡馬市場 | 検索・並び替え・種付申込 | P5 |
| ランキング | リーディングオーナー / **リーディングサイアー** | P5 |

### 12.3 ★育成の報酬は「格」で見せる（D-020）

**クラス制では勝率が能力を測らない**（実測 r = −0.003）。強い馬は強い帯で走るので、絶対能力が上がっても勝率は上がらない。

→ **「勝率」を主指標にすると、育成が報われて見えません。**

| 見せ方 | 扱い |
|---|---|
| **クラス（格）** | **主役**。馬詳細・一覧・血統表のすべてで最も目立つ位置に置く。昇級は演出を伴うイベントにする |
| **獲得賞金** | 副主役。クラスの重みを含むので能力を反映する |
| 勝率 | **主指標にしない**。戦績の一部として控えめに |

§1.1「世代を重ねるほど強くなる実感」は、**クラス昇級として体感させる**。

### 12.4 ★素質の見せ方（§5.5）

- `potential` は数値を出さず **★1〜5（半星あり）** に丸める。**本人にも数値を見せない**
- 「★が同じでも実際は差がある」ため、育ててみないと分からない体験が残る（配合の神秘性）
- **現在値（stats）は数値で表示する** — 育成の手応えの可視化

### 12.5 馬券UI

- **マークシート風**がレトロ表現と相性最良で実装も軽い
- **出走表**: 馬番・馬名・性齢・斤量・脚質・直近5走・**オッズ・人気**
- **投票シェア表示**: 固定オッズでも「みんなの投票状況」を別途表示すると集団心理の演出が効く。**オッズには影響しない**（§9.2）
- 的中時: 8bit ファンファーレ＋派手な払戻演出
- **★外れ時に追い打ち演出をしない**（射幸性を煽らない側の配慮・§9.8）

### 12.6 介入UI（P4）

§8b の4局面（スタート/道中/勝負所/直線）。

- **自馬にのみスタミナゲージを表示**
- **モバイル=タップ / PC=クリック・スペースキー**の両対応
- 同一レースに自馬が複数いる場合、**発走前に介入対象を1頭選択**
- **AI 代行中であることを画面に明示**する（切断・無操作時）。黙って代行すると「操作していないのに勝った/負けた」が理解できない
- **連打は秒間15回で頭打ち**（§8b.6）。UI 側でも上限を示し、それ以上叩いても意味が無いことを伝える

### 12.7 ポイントの見せ方（憲法 §0.2）

- **参加ポイント（EP）と賞金ポイント（PP）を明確に区別して表示する**。同じ「ポイント」として並べない
- **EP には「購入」への導線を一切置かない**（存在しないものへの導線を作らない）
- **PP には「換金」を想起させる文言を使わない**。景品交換のみ
- 総資産的な合算表示をしない — **投資商品の見えを作らない**

### 12.8 レース描画の抽象化（§15 と接続）

**描画ロジックを「位置データ → 描画コマンド」の純粋関数として切り出す。** レンダラだけを Web（Canvas 2D）と将来のモバイル（`react-native-skia` 等）で差し替えられるようにする。**この抽象化は初日からやること** — 後からの分離は現実的に不可能に近い。

## §13. チューニングテーブル + 検証基準 〔確定 2026-08-04〕

> 出典: GORILLA v1.0 §13 + v1.1 §19/§20 を採用・介入定数を追加。**全定数は config として外出しし、コードにベタ書きしない。**

### 13.1 定数一覧

```ts
export const BALANCE = {
  // 遺伝 (§6)
  ATAVISM_RATE: 0.06,               // 隔世遺伝率
  BIG_ATAVISM_RATE: 0.015,          // 大物覚醒率（★単調上昇ラチェット・§6.3の警告参照）
  MUTATION_SD: 90,                  // 突然変異の標準偏差（D-008 で 45→90）
  BIG_MUTATION_RATE: 0.008,
  BIG_MUTATION_SD: 180,
  REGRESSION_RATE: 0.20,            // ★平均回帰（D-008・§6.4）血統インフレの唯一の実効抑制
  DOMINANT_WEIGHT: 0.5,             // 素質発現＝2アレルの相加平均。★0.5超は系統的な上げ要因になる

  // 形質別の変異・回帰パラメータ（§6.4 の表・D-009）
  // ★回帰中心は「値域の45%」ではなく創始分布の期待値。値域一律にすると
  //   選抜圧のかからない形質（丈夫さ・気性・適性）が恒久的にずれる
  TRAIT_MUTATION: {
    // ★非能力形質の sd は 創始アレルSD × √(2r−r²) ÷ 0.916 から導出（§6.4・D-012/D-013）
    durability:      { sd:  66, clamp: 109, center:  650 },
    temper:          { sd:  10, clamp:  16, center:   50 },
    'surface.turf':  { sd:  13, clamp:  22, center:   55 },
    'surface.dirt':  { sd:  13, clamp:  22, center:   55 },
    distance_center: { sd: 341, clamp: 568, center: 2100 },
    distance_range:  { sd: 114, clamp: 189, center:  700 },
  },
  INBREED_BOOST_MAX: 0.30,          // インブリード素質ボーナス上限
  INBREED_BOOST_SLOPE: 4.0,
  INBREED_DEPRESSION_THRESHOLD: 0.25,
  INITIAL_UNLOCK_MIN: 0.28,         // 初期素質開放率
  INITIAL_UNLOCK_MAX: 0.35,

  // 育成 (§7)
  BASE_GAIN: 12,
  HEADROOM_EXP: 0.7,
  INJURY_BASE: 0.0018,

  // レース (§8)
  RACE_RANDOM_K: 0.22,              // 乱数の荒れ具合（D-016 で 0.12→0.26、D-021 で 0.26→0.22 に再較正）
  TAIL_MIX_P: 0.03,                 // ★裾の厚い混合分布（D-021）। 確率 p で分散を m 倍にする
  TAIL_MIX_M: 5,                    //   → 大穴が構造的に発生する。正規分布だけでは V-6 が満たせない
                                    // ★較正条件: 素質開放率プレースホルダ(0.55〜0.85)・クラス分け番組・
                                    //   出走頭数8〜18の一様分布・人気推定の試行数
                                    //   P3 で本物の育成ループが入ったら必ず再較正すること（R-7）

  // 介入 (§8b・D-006)
  INTERVENTION_CAP: 0.10,           // interventionMult のハードキャップ幅(±)
  AI_PROXY_TARGET: 0.95,            // AI代行の期待値目標(手動比)
  LAG_WINDOW_MS: 150,               // タイミング判定のネットワーク猶予
  TAP_RATE_CAP: 15,                 // 連打の秒間上限
  STAMINA_SPURT_DRAIN: 3.0,         // 仕掛け中のスタミナ消費倍率
  EARLY_SPURT_METER: 900,           // 早仕掛けペナルティ発生の残距離
};
```

（ベッティング関連定数 = §9 確定時に `BETTING` として追加 / NPC関連 = §10 確定時に `NPC` として追加）

### 13.2 実装後に必ず行う検証（モンテカルロ）— P0/P1 の合格基準

| # | 検証項目 | 目標値 | 検証フェーズ |
|---|---|---|---|
| V-1 | 同一配合100回の potential 分布 | **能力別CVの平均**が 12〜18%（=毎回違う馬が出る） | P0 |
| **V-2a** | **平坦化**: 最終20世代の平均能力の傾き | **0.5%/世代 未満** | P0 |
| **V-2b** | **天井余裕**: 集団平均能力 ÷ アレル上限 | **80% 以下**（個体差の消滅を防ぐ早期警報） | P0 |
| **V-2c** | **長期健全性**: 300ゲーム内年でも V-1 が保たれる | V-1 が 12〜18% に留まる | P0 |
| **V-2d** | **選抜圧のかからない形質の水準維持**: 丈夫さ（genotype 発現値） | 創始水準の **±10% 以内** | P0 |
| **V-2f** | **レース選抜がかかる形質の平坦化**: 芝/ダート適性・距離2種・道悪適性・気性 | 最終20世代の傾きが **0.5%/世代未満**（V-2a と同じ形） | P1〜 |
| **V-2e** | **非能力形質の分化**: 丈夫さ・気性・芝/ダート適性・距離2種の**集団SD**（V-2d と同じ対象） | いずれも創始水準の **0.8〜1.4倍** | P0 |
| V-3 | 隔世遺伝・大物覚醒・大突然変異の実発生率 | 設定値 ±0.5pt | P0 |

> **V-2d の趣旨**: V-1/V-2a〜c/V-3 はいずれも**能力5種しか見ていない**ため、丈夫さ 650→450 のような水準ずれを検出できなかった（2026-08-04）。

> **V-2d / V-2f の分割（2026-08-05・D-019）**: P0 では非能力形質すべてに「創始水準 ±10%」を課していたが、**P1 で実レース選抜が入ると前提が崩れる**。§8.3 の乗算補正が使う形質（芝/ダート適性・距離2種・道悪適性）は**レース成績を直接押し上げるので選抜圧がかかる**。実測でその署名が出た — 代理選抜では芝適性の乖離が **−4.76%（負）** だったのに、実レース選抜では **+4.66〜+9.08%（正）** に反転した。
> - **これは欠陥ではなく、ゲームが設計どおり動いている証拠**である（走って強い形質を選べば、その形質が集団で上がる）
> - したがって選抜圧のかかる形質に「水準±10%」を課すのは誤り。**能力5種に +50% を課すのが誤りだったのと同じ構図**（D-008）。上がること自体でなく、**上がり続けること**を禁じる
> - 選抜圧のかからない形質（**丈夫さ**。故障率にのみ効き §8.3 に現れない）は V-2d の水準ゲートを維持する
> **V-2e の趣旨**: 分化は「拡散」でも「収縮」でも失われる（§5.2）。目視ではなく閾値付きのゲートにする。**平均（V-2d）だけ見ても分化の消失は捕まらない** — 実測で芝適性は平均が±10%内に収まったまま SDが 0.71倍に収縮していた。距離だけでなく非能力形質すべてを対象にするのはこのため。
| V-4 | 1番人気の勝率（10万レース） | 30〜34% | P1 |
| V-5 | 1番人気の複勝率 | 60〜65% | P1 |
| V-6 | **下位3ランク（`LONGSHOT_RANKS = 3`）の平均勝率** | 0.5〜2%（ゼロではない=夢は残す） | P1 |

> **V-6 の定義変更（2026-08-06・D-022）**: 旧定義は「**最下位1頭**の勝率」だったが、D-021 で裾を厚くした結果 **「最下位人気」の同定自体が不安定になった**（人気推定の一致率が 0.70 → 0.55）。同定が揺れると、V-6 の測定に二項分布の標準誤差を超えるばらつきが乗る。
> - **下位3ランクの平均**なら、誰が最下位でも下位3頭に入るので同定の揺れに強い
> - V-6 の趣旨は「**大穴が出るか**」であって「最下位の1頭が勝つか」ではないので、趣旨は変わらない
> - **`LONGSHOT_RANKS` は較正定数ではなく「測定条件」**である。較正定数のように振る舞いのテストで守るのではなく、**この正典の記載とコードの一致を値照合で守る**（R-14 は較正定数についての規則であり、文書化された測定条件の照合を禁じるものではない）
>
> **★同じ扱いをする測定条件**: 人気推定の試行数（`POPULARITY_TRIALS = 500`）・出走頭数分布（8〜18の一様）。いずれも**判定を動かしうる自由変数**なので、正典に固定し値照合で守る（R-12）。

> **V-6 は出走頭数別にも分解して報告する**（案B・2026-08-06）。プール平均は**大頭数レースで裾が死んでいることを隠す**（実測: 旧定義の18頭立てで10万レース中0勝）。**緩和の手段ではなく、正しく測る手段**として扱う。P2 の万馬券（§9.4 の配当上限10万倍）が成立するかは、中心的な頭数帯で裾が生きているかに依存する。
| **V-7a** | **恒久ダメージを伴う故障（中度以上）を負うキャリアの割合** | **20〜40%**（★中央 30% 付近を狙う） | **P3**（★2026-08-10・D-048 で分割） |
| **V-7b** | **致命的故障（強制引退）を負うキャリアの割合** | **3% 以下** | **P3** |

> **★V-7 を2つに分けた理由**（D-048）: 旧「1キャリアあたりの故障発生率 25〜35%」は、**3週で治る挫石と、恒久ダメージや引退を、同じ数に足し込んでいた**。**割合が同じでも中身が正反対になりうる**（全部軽度なら無害、全部重度なら破滅的）ので、**壊れた状態がこのゲートを通る**（R-16）。設計上の役割が違うものは別に測る。
>
> **★測定条件**（旧指示に欠けていた5つ目・開発側が特定）: **どの育成方針で測るかを固定する。** 故障は調教で起きる（§7.5）ため、方針で実測 45.8%〜86.0% と2倍近く動く。→ **基準は「適切な育成（バランス型）」**とし、放置・追い切り偏重は参考値として併記する（V-14 の錨と同じ基準に揃える）。
>
> **★「分母は表現型の丈夫さ」の読み**（Q-P3-17）: これは**率の分母ではなく、§7.5 の式 `1000/durability` に入れる durability を genotype ではなく表現型（current）にせよ**という意味である（率の分母に丈夫さを置くと単位が合わない）。**開始時 640.0 / 期間平均 634.4 と影響は小さい**ので、**キャリア開始時の値**を測定条件として固定する。
>
> **★`INJURY_BASE_PROB`（§7.5 の 0.0018）は較正定数**であり、V-7a/V-7b がその較正ゲートである。**`menuIntensity` と キャリア長（182週）は動かさない** — 前者は方針の差別化そのもの（V-14 ③ が依存）、後者は §7.1 のタイムライン全体に波及する。
| V-8 | **AI代行 vs 手動最適の「倍率」の期待値比** | 93〜97%（AI_PROXY_TARGET 較正） | P1 |
| **V-13** | **仕掛けの巧拙が結果に出ること** | 最適な仕掛けと**早すぎる仕掛け**（残り900m超）で `interventionMult` に**有意な差**があること | P1〜 |
| **V-9a** | `interventionMult` の**不変条件** | **全サンプルが 0.90〜1.10 内**（ハードキャップが全経路で効くこと） | P1 |
| **V-9b** | 介入した馬だけの `interventionMult` 平均 | **報告のみ・絶対目標値は置かない**（下記） | P1 |

> **V-9 の改訂（2026-08-05・D-014）**: 旧 V-9「平均 0.99〜1.01」は **§8b.3 と両立しない**（レビュー側の誤り）。直線の追い `driveBonus` は 0〜+0.10 の**片側正**なので、介入した馬の期待倍率は必ず 1.00 を超える。旧基準を満たすには「介入していない馬（1.00）」で薄めるほかなく、それでは**指標がほぼ空洞**になる。
> - **V-9a（不変条件）がゲート**: ±10% のハードキャップは安全性の要（§1.5-1）なので、全サンプルが範囲内であることを機械的に確認する
> - **V-9b（監視）**: 介入した馬の平均は**時系列の変化**を見る（§8b.8）。絶対値 1.00 を目標にしない。「急な上振れはバグかマクロの兆候」という当初の趣旨はこちらで果たす
>
> **★V-13 を新設した理由（2026-08-06・D-024）**: P-2 のスタミナ是正の前後で **V-8 が 94.78% → 94.57% とほとんど動かなかった**。是正前は §8b.2 の数値では**全馬が必ずゲージ0になり −0.15 が一律に掛かっていた**が、**全員に同じ定数が乗るなら「比」（V-8）にも「範囲」（V-9a）にも現れない**（キャップが差を吸収する）。
> - つまり **V-8 も V-9a も「介入の巧拙が結果に出ない」という構造的破綻を検出できなかった**
> - 検出できるのは「**選択によって結果が変わるか**」を直接測る指標だけ。それが V-13
> - **一般化（R-16）**: **比率と範囲は、全体に一様にかかる歪みを検出できない**。ゲートを設計するときは「**壊れた状態がこのゲートを通ってしまわないか**」を必ず問う

> **V-8 の「期待値」は倍率の期待値**である（D-014 で明確化）。**勝率で見た比は 78% 前後になる** — 倍率の 5.8% の差が、勝敗は僅差で決まるため勝率では約28%の差になるため。**この差はオーナー承認済み**（2026-08-05・D-006 の ±10% キャップを維持する判断）。勝率比は**ゲートではなく報告値**として毎回出すこと
| **V-12** | **平均F と「表現型 − genotype」の乖離** | **平均F が 0.03 〜 0.10**（★下限は2026-08-08 追加・D-032）・丈夫さの乖離が −8% 以内 | P1〜（近交の進行を監視。§7.5 の故障率較正の前提） |

> **★V-12a に下限がある理由**（D-032）: 上限だけのゲートは **F がゼロに近い世界を無条件で通す**。しかし F ≒ 0 は「**近交がまったく起きない世界**」であり、**§6.5 の近交弱勢と血の濃縮が事実上働かない**。**機構が存在するのに作動しない状態は、ゲートが「壊れた状態を通す」典型例**（R-16）。近交回避の重みは「ゲートを通す最大値」ではなく「**機構が働く水準**」で選ぶ。
>
> **★ただし下限が当たるのは「運用中の集団」であって、プリシードの開始世界ではない**（2026-08-08・D-033）。V-12 はこの表で「**近交の進行を監視**」と定めているとおり**時間発展を見るゲート**であり、F は運用とともに上がる。**開始世界の F が低いのは欠陥ではなく余裕**である（0.0364 から始まる世界は 0.0287 から始まる世界より早く 0.10 に達する）。
>
> **開始世界について問うべきは「平均F の水準」ではなく「§6.5 が実際に発火しているか」**であり、それは**高F（>1/16）配合の割合**で直接測れる。平均F は集団平均だが、近交弱勢は個々の配合で起きるからである。**新たな閾値は設けない** — 割合を報告し、ゼロから明確に離れていることを確認する（実測候補は 8.5%〜11.5%）。
| V-10 | 払戻率（券種別） | 設定 margin ±1%（§9 確定後） | P2 |

> **★V-10 の測定条件**（2026-08-09・D-036）: 旧記述「10万レース」は **M≒390万との積が 3.9×10¹¹ 回＝約44日**となり実行不能。**分散低減として「1レースあたり確定を多数回引く」形に改める。** ただし **レース数を1にしてはならない** — 残差は出走表の確率構造で変わるので、**1つの出走表を精密に測っても、出走表間のばらつきは測れない**（R-20）。**レース数 × 1レースあたり確定回数**の両方を報告し、**出走表間 SD を出したうえで、プール SE ≤ 0.25pt** となるレース数を選ぶ（10シード判定と同じ規律）。
| **V-15** | **育成で変化する形質が、現役集団で分散を保ち、かつ育成の選択が実際に効くこと**（当面は気性） | **①集団 SD が誕生時 SD の 50% 以上**（形質が死なない）**かつ ②キャリアを通じた平均低下が 15% 以上**（育成の選択が効く） | **P3**（★2026-08-11・D-049 / 2026-08-11・D-050 で両側化） |

> **★V-15 を両側にした理由**（D-050）: ①だけだと **`TEMPER_FLOOR_RATIO = 1.0`（気性が一切動かない）が最大余裕になる** — **機構を止めることでゲートが最もよく通る**という、V-12a に下限が無かったときとまったく同じ形（R-16）。②は「§7.2 の `temperDelta`（休養 −5 / 併せ馬 −2）が飾りになっていないか」を測る。
>
> **★測定方針**: **性能の基準は代表的な方針（バランス型）で、安全性の基準は最悪の方針で測る。** V-15 は「形質が死なないこと」を問う安全性の基準なので、**休養の多い方針など、最も不利な方針**で測る（V-7 が代表方針で測るのと対照的）。
| **V-14** | **引退時の素質開放率**（`current/potential`）| **★適切な育成が 88%以上** かつ **★適切な育成 − 放置 の差が 12pt 以上**（2026-08-10・D-047 で改訂。旧「放置 55〜75% かつ 適切な育成 90%以上」は**実質1自由度で2つの絶対水準を要求しており、構成上 実行可能領域が空だった** — 放置は `BASE_GAIN × 軽め0.3` で完全に決まり、`MAIN_EFFECT_COEF` はバランス型の182週中48週にしか効かない）。**§7.1 が求めているのは「指示を出さない週は成長が鈍る＝デイリー来訪の動機」であり、本質は差である**。さらに **★同一 EP 予算の下で、追い切り偏重の開放率がバランス型を大きく上回らない**こと（D-045 で定義を改訂。旧「故障込みの期待値」は曖昧だった。**EP が希少資源である以上、比べるべきは「同じ EP を注いだときにどちらが強くなるか」**） | **P3**（★係数そのものではなく結果をゲートにする。§7.1 の「デイリー来訪の動機」が成立する条件・D-044） |
| V-11 | ポイント純発行量/日 | **①ゼロ近傍〜微減 かつ ②当日の PP 発行量と EP 消費量がともに実質ゼロでないこと**（★2026-08-11・D-051 で両側化） | P2〜 |

> **★V-11 を両側にした理由**（D-051）: 本番はワーカーが動きレースも401件確定していたが、**利用者0人・馬券0枚・PP台帳0行・プレイヤー所有馬0頭**。`prize-award.ts` は NPC 馬に払わないので**賞金が1点も発行されず、純発行量は毎日ゼロ**で V-11 が自動的に通っていた。**経済が存在しない世界を、健全な経済として通すゲート**だった。
>
> **★測定条件**: **較正は合成プレイヤー集団で行う**（所有馬・馬券購入・賞金獲得が実際に起きる規模）。**運用時の監視は実集団**（内部口座は §11.2 のとおり別掲）。**同じ指標を、較正と監視で使い分ける。**

### 13.3 検証の運用ルール

- **V-2 系（血統インフレ）が最重要**。ただし守るべきは「平均が上がらないこと」ではなく「**上がり続けて天井に張り付き、個体差が消えないこと**」である（2026-08-04 改訂・D-008）:
  - このゲームでは**絶対能力値に意味がない** — レースは出走馬同士の相対比較（§8.2）、NPC はプレイヤー上位30%×0.92 に追従（§10.5）、オッズも相対スコアから算出（§9.2）。平均が +30% でも +65% でも体験は変わらない
  - 壊れるのは天井飽和で全馬が同じ能力になるとき。実測: 回帰なしでは300ゲーム内年で平均が上限の95%に達し **V-1 が 3.24% に崩壊**（レースが実力勝負でなくなる）。回帰0.20 なら上限の60%で平衡し V-1 14.91% を維持
  - 旧基準「100世代後 +50% 以内」は廃止。理由は①§1.1/§1.4-3 の「世代を重ねるほど強くなる」中核価値と直接対立する ②100年horizonでは回帰なしでも PASS してしまい長期破綻を検出できなかった ③創始世代は F=0・ニックス1.0 で生成されるため、補正済みの産駒と比べる比率には**構造的な上げ底が約14%含まれる**（指標としての精度が低い）
  - 抑制の実効手段は **§6.4 の平均回帰のみ**（アレル値上限・対称な突然変異・headroom はいずれも平均の上昇を止めないことが P0 で実証済み）
- **用語**: 「100世代」は**100ゲーム内年**を指す（1世代=1年・年1産）。成熟6歳のため血統深度は約13.9世代、実時間では約2.4年（D-007: 1週=4時間）。300ゲーム内年 ≒ 実時間7年
- シミュレータは P0 の納品物であり、以後も定数変更のたびに回す**回帰テスト**として保守する
- 検証は**シード固定で再現可能**にする（「たまたま通った」を許さない）
- 目標値から外れた場合は定数を調整して再検証。**目標値そのものを動かすときはオーナー承認＋本書の改訂**（勝手に基準を緩めない）
- 各検証はCLIコマンド1発で実行でき、結果を機械可読（JSON）+人間可読（表）で出力すること — レビュー側が独立再実行して照合する（Seven Days 方式）

## §14. インフラ 〔確定 2026-08-06〕

> **P2 で初めてサーバーを立てる**（P0〜P1.5 はローカルCLIのみ）。バランスを固めてからインフラを作る（§16）。

### 14.1 このゲームの技術的な特殊性

一般的な Web アプリと決定的に違う点が3つあり、構成はここから決まる:

1. **24時間止まらないゲームループが必要** — 10分ごとにレースを生成し確定させる（D-007）。ユーザーが1人もログインしていなくても動き続けなければならない
2. **CPU バウンドな処理がある** — オッズ算出のモンテカルロ（§9.2）× 10分ごと、加えて NPC 2,500頭の週進行バッチ
3. **状態が常に前進している** — リクエストが無くても世界が進む

**サーバーレスはこの3つすべてが苦手。** よって「**常時稼働のワーカー**」を必ず1つ持つ構成にする。

### 14.2 採用構成

| レイヤー | 採用 | 理由 |
|---|---|---|
| フロント配信 | **Vercel** | Next.js の最適解。プレビュー環境・エッジ配信 |
| DB / 認証 / Realtime | **Supabase** | Postgres + Auth + RLS + Realtime が一体 |
| ゲームループ | **常時稼働ワーカー**（Render Background Worker 等） | 常時稼働・任意の実行時間・Git push デプロイ |
| 重量シミュレーション | **VPS**（第2段階） | モンテカルロが重くなった時の逃げ場 |
| CDN / アセット / WAF | **Cloudflare (+ R2)** | ドット絵・音源の配信。**経済のあるゲームは必ず bot に狙われる** |

**ワーカーに Vercel Cron / pg_cron を使わない理由**: 実行保証が弱い・長時間処理に不適。**10分サイクルはこのゲームの生命線**であり、預けられない。

### 14.3 それぞれの役割（厳密に）

**Vercel — フロントと読み取り系だけ**

やってはいけないこと: レース生成・確定 / オッズのモンテカルロ / **ビジネスロジックの実装**（§15 の理由でアプリ化時に全部書き直しになる）。

**Supabase — 唯一の真実の源**

- **ゲーム内時刻の真実は Postgres の `now()` のみ**とする。ワーカーやクライアントの時計を信用しない
- 「次に生成すべきレース」もテーブルで管理し、ワーカーはそれを見て動く。**こうしておけばワーカーが落ちて再起動しても状態から復帰できる**
- **RLS で `genotype` と `potential` を絶対に露出させない**（§5.5）
- **★`seed_reveal` は確定前に絶対に返さない**。RLS で `status != 'scheduled'` の行のみ露出する（漏れると Provably Fair が無意味になる）

**ワーカー — ゲームループの心臓**

```ts
while (true) {
  await withAdvisoryLock('game_tick', async () => {
    await generateUpcomingRaces();   // 2レース先まで先行生成 + オッズ算出
    await settleFinishedRaces();     // 確定・払戻
    await advanceTrainingWeeks();    // 週進行（1週=リアル4時間・D-007）
    await runNpcStableAI();
    await recalcNpcStrength();       // 週次
  });
  await sleep(5_000);
}
```

- **Postgres の advisory lock で二重実行を防ぐ**
- **すべての処理を冪等にする**。「このレースはもう生成済みか」「この馬券はもう払い戻したか」を必ず確認してから動く。**再起動やデプロイ中の重複実行で二重払戻が起きたら経済が壊れる**
- 外形監視で死活確認

### 14.4 ★ポイント増減の原子性（このゲーム固有の要件）

**EP の減算と馬券の発行、PP の加算と払戻の記録は、必ず同一トランザクションに収める。** 分かれていると、ネットワーク断で「EP だけ減って馬券が無い」事故が起きる。

→ **Supabase の RPC（Postgres 関数）を主 API とする**（§15.3）。`security definer` で、発売時間内チェック・上限チェック・自馬ルール（§9.5）・残高ゲートを1つの関数の中で行い、`bets` の挿入と `ep_ledger` の更新を同一トランザクションにする。

### 14.5 リアルタイム配信の設計

レース走行中（75〜105秒）、各クライアントに位置データを流す必要がある。**方式: 送らない。**

確定時点で全馬の位置推移データ（§8.7 の逆生成）を**1回のペイロードとして配信し、クライアント側で時間軸に沿って再生する**。1レースあたり1メッセージで済む。

> ⚠️ **介入があるレースは2段階配信**（§8b）。介入受付締切（ゴール0.5秒前相当）まで結果を配信できない。①発走時に「締切までの位置データ」→ ②締切後に「残り区間＋着順」。**介入する自馬を持つプレイヤーにのみ2段階を適用**し、観戦のみのユーザーは締切後に一括配信でよい。

### 14.6 環境分離

```
production : Vercel(prod) + Supabase(prod) + ワーカー(prod)
staging    : Vercel(preview) + Supabase(staging) + ワーカー(staging)
local      : Supabase CLI (docker) + ワーカーをローカル実行
```

**★staging のワーカーを production DB に絶対に繋がないこと。** 二重にレースが生成されて経済が壊れる。接続先を環境変数任せにせず、**起動時に DB 側の `environment` フラグと照合し、不一致なら起動失敗させる**。

### 14.7 コスト見積り（初期〜小規模）

| サービス | 概算/月 |
|---|---|
| Vercel Pro | $20 |
| Supabase Pro | $25〜 |
| 常時稼働ワーカー | $25 |
| Cloudflare | $0〜20 |
| **計** | **$70〜90** |

VPS は必要になってから。**最初からすべて VPS に載せると運用負荷でスピードが落ちる。**

## §15. アプリ化設計 〔確定 2026-08-06〕

> Web版（P6）を先にリリースし、後にネイティブアプリ化する（P7）前提。**ここを間違えると後から作り直しになる**ので、Web版の時点で守っておく。

### 15.1 いつアプリ化するか

Web版で先に検証すべきなのは **①10分サイクルが無人で回り続けること** と **②血統インフレが起きないこと** の2点。**これが確認できるまでアプリ化に着手しない**（ストア審査のやり直しはコストが高い）。

②は P0 で V-2a〜f として検証済み。①は P2 の完了条件。

### 15.2 ★最重要の設計原則 — ロジックを Next.js から出す

```
packages/
  sim-engine/     ← 遺伝（P0 で完成・依存ゼロの純粋TS）
  race-engine/    ← レース判定・介入（P1 で完成・同上）
  game-core/      ← ゲームルール（ベット可否・上限・賞金計算）
  api-client/     ← API の型定義とクライアント。Web/アプリ両方から使う
apps/
  cli/            ← 検証ハーネス（P0〜P1）
  web/            ← Next.js (Vercel)
  worker/         ← 常時稼働ワーカー
  mobile/         ← 後で追加 (Expo)
```

- `sim-engine` / `race-engine` は **Node にも React Native にも依存しない純粋な TypeScript**。`crypto` や `fs` を直接使わず、必要なものは注入する（**P0/P1 で既に達成済み**）
- **Vercel の Route Handler にビジネスロジックを書かない。** Route Handler は薄い入口にとどめ、中身は `game-core` を呼ぶだけにする

**なぜか**: Next.js の API Routes にロジックを書くと、モバイルアプリから使えない。「Web だけ動くゲーム」が完成してから、アプリ化のためにサーバーを丸ごと書き直すことになる。

### 15.3 API の形 — Supabase RPC を主 API とする

```sql
create function place_bet(
  p_race_id uuid, p_bet_type text, p_selection jsonb, p_amount int
) returns jsonb
language plpgsql security definer as $$
begin
  -- 発売時間内か / 上限チェック（§9.4）/ 自馬ルール（§9.5）/ EP 残高
  -- bets 挿入 と ep_ledger 更新を同一トランザクションで（§14.4）
end $$;
```

利点: Web からもアプリからも **同じ SDK 呼び出しで済む** / 認証は Supabase Auth の JWT がそのまま効く / **ポイントの増減と馬券の発行が同一トランザクションに収まる**（§14.4）。

複雑すぎて SQL で書きたくない処理（配合・レース生成）はワーカー側に置き、クライアントは「リクエストをキューに積む → 結果を Realtime で受け取る」形にする。

### 15.4 アプリ化の方式

**Expo (React Native) を推奨。** TypeScript とロジックパッケージをそのまま共有でき、**プッシュ通知が使える**。

**プッシュ通知がアプリ化の決定的な価値**: 「あなたの馬が出走します」「調教の指示待ちです」「G1が10分後に発走」— **このゲームは通知との相性が極端に良い**。§8b の介入は「観られる時刻に出走させる」設計（§10.4）なので、**発走通知が介入の前提条件**になる。Web の Push API は iOS で制約が多く、ここはアプリの明確な価値になる。

### 15.5 🔴 ストア審査で必ず引っかかる点

本ゲームには**馬券要素**があり、**賞金ポイントが景品に交換できる**。

> **GORILLA 想定（換金一切なし）より厳しい。** あちらは「ゲーム内スコアの賭け」で済んだが、**こちらは出口に現実の価値がある**。

想定される扱い:

- **"simulated gambling"（模擬ギャンブル）に該当する**と見るべき。年齢レーティングが上がる（iOS 17+ 相当）
- **景品交換があるため、単なる模擬ギャンブルより踏み込んだ審査**になりうる。「実質的な賞金付きゲーム」と評価される可能性
- 一部の国・地域で配信制限
- 申請時のレーティング質問票で、**ギャンブル関連項目に正直に申告すること**。誤魔化すと後で削除される

**★対策として Web版の時点で用意しておくもの**（§17 と重複するが、アプリ化時に改めて必要になる）:

1. 利用規約に「**参加ポイントは購入できず、賞金ポイントは換金・譲渡できない**」と明記
2. アプリ内の目立つ位置に同趣旨の表示
3. 年齢確認フロー
4. RMT 禁止条項と違反時のアカウント停止規定
5. **§17.1 の弁護士見解（L-1〜L-6）**。ストア審査で説明を求められたときの根拠になる

### 15.6 Web版の時点で済ませておくこと

| 項目 | 理由 |
|---|---|
| ロジックのパッケージ分離（§15.2） | **後からでは不可能に近い**（P0/P1 で達成済み） |
| Supabase RPC への API 集約（§15.3） | Next.js API Routes に散らすと書き直し |
| **描画レンダラの抽象化**（§12.8） | 同上。**初日からやること** |
| 認証を Supabase Auth に統一 | モバイルの Deep Link 対応が楽になる |
| 規約・年齢確認の整備（§15.5） | 審査対策 |
| **PWA 対応** | アプリ化前でもホーム画面追加で擬似アプリ体験。**安価な保険** |

## §16. 実装フェーズ 〔確定 2026-08-06〕

> 出典: GORILLA v1.1 §21 + v1.2 §24 を採用。**P0〜P1.5 はインフラ不要**（ローカルCLIのみ）。**バランスを固めてからサーバーを立てる**。

### 16.1 フェーズ一覧

| Phase | 内容 | 完了条件 | インフラ | 状態 |
|---|---|---|---|---|
| **P0** | 遺伝エンジン + 100世代シミュレータ | V-1 / V-2a〜f / V-3 が実運用規模で通る | 不要 | **完了**（2026-08-05） |
| **P1** | レース判定エンジン + Provably Fair + 介入モデル + モンテカルロ検証 | V-4/V-5/V-6/V-8/V-9/V-12 が通り、**P0 ゲートが実レース選抜下でも成立** | 不要 | **完了**（2026-08-06） |
| **P1.5** | NPC世界の50世代プリシード | 種牡馬プールと5代血統表が成立 | 不要 | **再開**（2026-08-07・`144c2cb`）— 合格基準3 と V-12a のみ残件。**乱数用途 ID を変えただけで有効系統数 6.02→2.96・平均F 0.0519→0.1060 と判定が覆り、単一実現の指標を分散未測定でゲートにしていたことが判明**（D-028 / R-20）。他の合格基準は維持 |
| **P2** | 開催スケジューラ + 馬券 + オッズ | **10分サイクルが24時間にわたり、欠落も重複もなく前進し続ける（デプロイによる再起動を含んでよい・D-034）** / V-10 払戻率 | **Supabase + 常時稼働ワーカーを立てる** | **進行中**（第1便。合格基準9件中 A-9 のみ実測・**インフラ未着手のため A-1/A-2/A-4/A-5/A-7 が待ち**） |
| **P3** | 育成ループ + 週進行 + ポイント台帳 | 1頭を誕生から引退まで通せる / V-7 故障率 / V-11 | 同上 | **完了**（2026-08-13・`e9916ad`） |
| **P4** | レトロ描画 + 観戦 + 介入UI | 遊んで面白いと感じられる | + Cloudflare R2 | 未着手 |
| **P5** | 配合UI・血統表・種牡馬市場・ランキング | クローズドβ投入可能 | | 未着手 |
| **P6** | ポイ活連携・法務レビュー・規約整備 | **弁護士ゲート通過**（§0.3）→ Web版公開 | | 未着手 |
| **P7** | Expo アプリ + プッシュ通知 | ストア申請（simulated gambling 対応・§15） | | 将来 |

### 16.2 フェーズ間の依存で特に注意すること

- **genotype を変える変更は P1.5 より前に**。プリシードした血統プールを作り直すことになる（D-015 の `heavy_aptitude` 追加がこれに該当した）
- ~~P3 で `RACE_RANDOM_K` と スタミナ基準消費量の再較正が要る~~ → **★2026-08-12・D-053 で決着。値の変更は不要だった。**
  - P1 の較正は**素質開放率プレースホルダ（0.55〜0.85）**に依存していたが、**そのプレースホルダを廃止して本番の実分布（平均71.3%）で測り直したところ、V-4 32.32% / V-5 63.26% / V-6 いずれも帯の内側**だった
  - **★したがって動かすべきは値ではなく「較正条件の記録」のほうである。** `RACE_RANDOM_K = 0.22` の較正条件は「**プレースホルダ 0.55〜0.85 で導出 → 2026-08-12 に本番の実分布で再検証**」と読むこと
  - **較正条件は定数の意味の一部である。** 条件が変わったら、**値を導き直すか、その条件でも成立することを示すか**のどちらかが要る（今回は後者）
- **P2 の開催スケジューラは §10.4 のクラス分けを守ること**。無作為抽選にすると V-4 が静かに壊れる（D-018）
- **P0 のゲートは以後も回帰テストとして回し続ける**。フェーズが進むたびに「前フェーズのゲートが維持されているか」を確認する

### 16.3 各フェーズ共通の作法（P0〜P1 で確立）

実装の質より**検証の質**が壊れやすいことが P0〜P1 で繰り返し判明した。以下は全フェーズに適用する:

| # | ルール |
|---|---|
| R-1 | 単体を固定するだけでなく**経路**を固定する |
| R-2 | 境界テストは**両側**を押さえる |
| R-3 | 「判定不能」は PASS でなく **FAIL** に倒す |
| R-4 | **導出をバイパスする経路を数える** |
| R-5 | 平均のゲートだけでは**分散の破壊**を捕まえられない |
| R-6 | 一方向にしか働かない機構は、**適用先ごとに意味が通るか**確認する |
| R-7 | **較正した条件を明記する**（値だけでなく根拠も） |
| R-8 | 実行時に**逸脱を自己申告する出力** |
| R-9 | 「**ヒットゼロ**」を根拠にするときは、検索式が拾えない形を列挙する |
| R-10 | 「**数える**」成果物は静的検索でなく**実行時計測**で出す |
| R-11 | 検証ツールには**出力の事後条件だけでなく前提条件**（ベースラインが健全であること）も持たせる |
| R-12 | **測定の自由変数**は判定に効かないことを確認するか、正典に固定する |
| R-13 | 報告では「**再現した事実**」と「**未検証の主張**」を分ける |
| R-14 | **変異試験の対象を検査するテストは、変異によって落ちてはいけない** |
| R-15 | 新しい機構は「**入っているが使われない**」状態で一度コミットする（接続前後で対照が取れる） |
| **R-23** | **合格は「そのコード状態に対する主張」であり、コードが変われば黙って失効する。** 各合格基準には**最後に検証したコミット**を併記する。**★判定は「全項目が HEAD と一致」ではない**（2026-08-13・Q-P3-46 — SHA を書けば新しいコミットができるので、**その基準は原理的に達成できない**。実際、9件更新した直後にその9件が古くなった）。**正しい判定は「その SHA 以降、その項目が対象とするコードが変わっていないか」**であり、変わっているなら**再実行するか、「なぜ古い SHA でも有効か」を明文で書く**。実例: **A-2 の PASS は D-037/D-038 を入れる前の証拠のまま繰り越され**、しかも検証ツールが実行不能だったため誰も気づかず、報告表には毎回「PASS」と書かれていた |
| **R-24** | **検証ツールは本番を壊せない形にする。** 状態を変えるツールは **staging 専用**とし、`app_environment='production'` を見たら**起動を拒否**する。本番に向けてよいのは**読み取り専用**のツールだけ。**★そして「構造で守る」は、危険な操作が*不可能*になったときにだけ成立する** — **拒否されるだけなら迂回され、手順に戻る**。導入したら **「これを迂回する最短経路は何か」**を問う（R-16 の「最も安易に満たす方法は何か」と同じ形）。実例: `git worktree remove` から `--force` を外したので git は正しく削除を拒否したが、**人がリンクだけ外して回り込み、実行中の60,000本の測定を壊した**。是正は「拒否を強める」ではなく **リンクを必要としない構造にして、外す対象そのものを無くす**こと。**★ただし「全部を不可能にせよ」ではない。迂回経路を『うっかりやってしまう手順』と『意図してやる操作』に分け、前者だけを不可能にする** — 資格情報を持つ者の直接操作（`psql` で書く等）や、意図して置いた脱出口は拒否で足りる。**最も危険なのは迂回ですらないもの**（既定の経路が壊れた側に落ちる形。実例: 凍結が無い馬を警告つきで旧経路に流す） |
| **R-22** | **検定が「異常なし」と言ったら、その検定が異常のあるはずの場所を見ているかを先に確認する。** 平均の一致は裾の一致を意味せず、相関係数は裾の依存をほとんど反映せず、主要部に支配された χ² は1%以下の裾を見ていない。**「異常が出なかった」は「その検定の視野に異常が無かった」以上のことを言わない**（R-16 の双対。R-16 は*ゲート*の盲点、R-22 は*検定*の盲点）。**★照合は、壊れ方と同じ粒度で行う** — 行数比較は値の変化を見ず、**集団平均は個体ごとのずれを見ない**。実例: 生成側 `rng.int(2,4)`（平均3）対 確定側 DB既定値 3 で、**平均は一致したまま224頭全部がずれていた** |
| **R-21** | **「出なかった」「落ちた」を、理由を読まずに結果として扱わない。** ①パターン抽出の0件は「該当なし」と「抽出器が壊れている」の区別がつかない ②**検査が失敗しても「検出できた」ことにはならない** — 実例: `node -e` が CJS 評価で top-level await を使えず**健全な木でも落ちていた**が、対照（壊れた木）も同じ理由で落ちたので**検査が効いて見えた**。**対照が通ることは、対照が正しい理由で通ったことを意味しない。** 抽出器・検査器には事前条件と**終了理由の照合**を書く（本件で6回再発）|
| **R-20** | **単一実現から得た指標をゲートにするなら、実現間の分散を先に測る。** 平均量（多数の独立試行の平均）と実現量（1本の履歴の結果）は、同じ「数値」でも要求する標本数が桁で違う（レースゲートは用途 ID 変更で 0.22pt しか動かなかったが、有効系統数は 6.02→2.96 と覆った） |
| **R-19** | **試験の網羅範囲は、対象が増えたときに自動で広がらなければ、緑のまま縮む。** パッケージやディレクトリを追加したら、走査対象がそれを含むことを機械で確認する（**件数が同じまま増えないことは、成功ではなく縮小の兆候**） |
| **R-18** | **検証ツールが作業ツリーを改変するなら、異常終了しても復元できなければならない。** `finally` は SIGTERM では走らない → 摂動が残り、次の実行が「改変なしでテストが落ちている」と誤判定した。**後始末まで含めて検証ツールである**（この誤判定を可視化したのは P1 で必須化したベースライン緑の事前確認）。**★さらに、走っている最中の作業ツリーからコミットしてはならない** — `git add -A` が摂動を拾い、`NPC_FOLLOW_TOP_RATIO = 0.3 → 1.0` が1行コミットに混入した実例がある。**手順で守らず、別の作業ツリー（`git worktree`）で回して構造的に不可能にする** |
| **R-17** | **選抜の軸を分散させても、選抜されていない属性は保存されない。** ある属性を維持したいなら、**決定経路がその属性を参照していなければならない** — 監査が見ているだけでは何も保存されない（父系ラインは `mateScore`/`canMate` に一度も現れず、監査にしか出てこなかった）。R-16 が**測る側**の盲点なら、こちらは**決める側**の盲点 |
| **R-16** | **比率と範囲は、全体に一様にかかる歪みを検出できない。** ゲートを設計するときは「**壊れた状態がこのゲートを通ってしまわないか**」を必ず問う。**★実務上の検査法（3回再発したので明文化）: 「この基準を最も安易に満たす方法は何か」を問い、答えが「機構を止めること」なら反対側の基準が要る。** 実例はすべてレビュー側が書いたゲート — **V-12a**（上限のみ → F≒0 で近交機構が死んでも通る）/ **V-15**（下限のみ → 比率1.0 で気性が動かなくても通る）/ **V-11**（純発行量のみ → 経済が存在しなくても通る） |

**定数の3分類**（P1 で確立）:

| 分類 | 例 | 守り方 |
|---|---|---|
| **較正定数** | `RACE_RANDOM_K` / `FIELD_STRENGTH_FLOOR` / 馬場分布 | **振る舞いのテスト**（R-14） |
| **測定条件** | `LONGSHOT_RANKS` / `POPULARITY_TRIALS` / 検証のシード・レース数 | **正典に固定し値照合**で守る |
| **正典の写し** | §13.1 の定数ブロック | 導出式との一致をテストで固定 |

## §17. 公開前チェックリスト 〔確定 2026-08-06〕

> **公開＝Web版リリース（P6）の直前に全項目を確認する。** ただし **17.1 の弁護士ゲートは「最後にやる」と必ず詰まる** ので、D1/D2 が決まった時点で相談を開始する。

### 17.1 🔴 弁護士ゲート（ブロッキング・最優先）

**これが通らない限り公開しない。** 相談時に弁護士へ渡すもの: 本正典の §0（憲法）・§3（ポイント経済）・§9（馬券）・§11（賞金と景品）と、実際の画面。

| # | 論点 | 何を確認するか | 依存 |
|---|---|---|---|
| **L-1** | **賭博罪（刑法185条）** | **参加ポイントが無償取得のみ**（購入経路ゼロ）である構造で、「財物を賭ける」に当たらないと言えるか。**外部経路が広告主負担型に限定されている**ことが要件を満たすか | **D1 確定後** |
| **L-2** | **景品表示法** | 賞金ポイント→景品が**景品類**に当たるか。当たる場合の**上限額**と表示義務。「懸賞」扱いになるか | **D2 確定後** |
| **L-3** | **資金決済法** | 二種ポイントが**前払式支払手段**に当たらないか（供託・届出義務の有無）。参加ポイントが無償・賞金ポイントが換金不可であることで外れるか | D1/D2 |
| **L-4** | **賞金P→参加Pの還流**（D3） | 景品交換価値を持つポイントを再び賭けに使える構造が、賭博性の再構成にならないか | — |
| **L-5** | **ギフト券系の景品** | 現金等価性が高い景品を扱えるか。扱えるなら上限は | D2 |
| **L-6** | **未成年の利用** | 年齢制限の要否。馬券要素を持つサービスとしてのレーティング | — |

**★これらは相互に依存する。** L-1 が「無償だから賭博でない」で通っても、L-5 で景品が現金等価と判断されれば全体の評価が変わりうる。**個別に聞かず、構造全体を一度に見てもらうこと。**

### 17.2 憲法の遵守（コードレベルで確認）

| # | 項目 | 確認方法 |
|---|---|---|
| C-1 | **参加ポイントの購入経路がコード上に存在しない** | `ep_ledger.reason` に購入を表す値が無いこと。決済SDKの依存が無いこと |
| C-2 | **賞金ポイント→現金・暗号資産の経路が存在しない** | 同上（`pp_ledger.reason` に換金を表す値が無い） |
| C-3 | **ポイントのユーザー間譲渡機能が無い** | 種付料はサービス対価であり譲渡でないこと（§10.5） |
| C-4 | **実在競走馬名・実在レース名・実在競馬場名がゼロ** | **`rg -uu` で untracked も走査**（`.gitignore` 済みファイルが漏れる・M-6 の教訓）。馬名生成辞書とNGリストの両方 |
| C-5 | **他社製品の固有名称がコード内の変数名・テーブル名・コメント・出力に無い** | 同上 |
| C-6 | **RLS で `genotype` / `potential` が本人にも露出しない** | ビュー経由でのみ星評価を返すこと |

### 17.3 公正性の証明（Provably Fair）

| # | 項目 |
|---|---|
| F-1 | **検証手順をユーザー向けに公開**する。`SHA256(seed_reveal) == seed_commit` を誰でも確認できる形 |
| F-2 | **`seed + 介入ログ → 同じ着順`** が再計算で示せる（§8.8）。介入がある以上、seed だけでは再現の証明にならない |
| F-3 | **結果の事後差し替えが一度も無いこと**。障害時はレース不成立＋全額返還で、**差し替えは絶対にしない**（§10.2） |
| F-4 | 固定オッズなので **`odds_at_purchase` が購入時に確定**しており、後からオッズ式を修正しても既存馬券の配当が変わらない |

### 17.4 規約

| # | 項目 |
|---|---|
| T-1 | **RMT（現実マネー取引）の禁止**と、違反時のアカウント停止規定 |
| T-2 | **ポイントは換金・譲渡できず現金価値を持たない**旨の明記（参加P・賞金Pそれぞれについて） |
| T-3 | **サービス終了時の補償なし**の明記 |
| T-4 | **景品の交換条件・在庫・上限**（D2 確定後） |
| T-5 | 年齢確認・未成年利用方針（L-6 の回答を反映） |

### 17.5 経済の健全性

| # | 項目 | 基準 |
|---|---|---|
| E-1 | **PP の純発行量**が監視されている | ゼロ近傍〜微減（V-11・§4.6） |
| E-2 | **券種別の実払戻率**が理論値と一致 | 設定 margin ±1%（V-10） |
| E-3 | **`intervention_mult` の分布**が監視されている | V-9a が不変条件・V-9b は時系列で見る（§8b.8） |
| E-4 | 高配当が連続したときの**アラート**が動く | `margin_actual` が理論値から3%以上乖離した日 |

### 17.6 素材と権利

| # | 項目 |
|---|---|
| M-1 | フォントのライセンス確認（日本語ビットマップフォントは要注意） |
| M-2 | 音源のライセンス確認（オリジナル制作 or ライセンスフリー） |
| M-3 | ドット絵・背景がすべてオリジナルであること（§0.1） |

### 17.7 運用の備え

| # | 項目 |
|---|---|
| O-1 | **staging のワーカーが production DB に繋がらない**防御（§14.6）。二重にレースが生成されると経済が壊れる |
| O-2 | ワーカーの死活監視（10分サイクルの生命線） |
| O-3 | **P0〜P1 のゲート（V-1〜V-12）が本番構成でも通る**ことの確認。フェーズを進むたびに回してきたものを、最後にもう一度 |
| O-4 | 障害時の「レース不成立＋全額返還」が実際に動くことの確認 |

---

> **★このチェックリストの使い方**: 17.1 の弁護士ゲートだけは**先行して着手**する。他は P6 でまとめて確認してよい。**Seven Days Derby では「書面の法律意見書が最大の未解決事項」として最後まで残り続けた** — 同じ構造をここでは避ける。

---

## 未決事項一覧（オーナー判断待ち）

| # | 論点 | 現在の扱い | 決定期限 |
|---|---|---|---|
| D1 | 参加ポイントの外部獲得経路の具体 | 「広告主負担型限定」の制約のみ固定・具体は保留 | §3 執筆まで |
| D2 | 景品カタログ・原資 | 方向のみ（広告収益原資）・ギフト券は弁護士判断 | §11 執筆まで |
| D3 | 賞金P→参加P還流 | 不可がデフォルト・弁護士ゲート | 弁護士レビュー時 |

（D4 は D-006、D5 は D-007 として決定済み → §0.4）

## 決定ログ

- 2026-08-03: D-001〜D-005（§0.4 参照）
- 2026-08-04: 本スケルトン承認・執筆開始
- 2026-08-04: §1 確定。D-006 リアルタイム介入 v1 採用（オーナー決定・安全条件7点を必須要件化・§8b 新設）
- 2026-08-04: §2・§5 確定(EP/PP 命名・5能力0〜1000・ST/GT/IQ と介入の接続・素質は星表示非公開)。§6 ドラフト提示
- 2026-08-04: §6 確定(2アレル遺伝・サブスク枠拡張は v1 不採用=弁護士ゲート行き)。§7 ドラフト提示
- 2026-08-04: §7 確定(週ターン育成・EPシンク・馬を死なせない)。§8 ドラフト提示(Provably Fair+介入ログの再現性拡張 §8.8)
- 2026-08-04: §8 確定。§8b ドラフト提示(介入対象1頭選択・AI代行95%・±10%ハードキャップ・連打15/秒上限・マクロ検知)
- 2026-08-04: §8b 確定。§13 ドラフト提示(定数外出し・検証V-1〜V-11・レビュー側の独立再実行を検証要件に)
- 2026-08-04: §13 確定。DEV_INSTRUCTIONS_P0 発行(オーナーが開発側に投入済み)。§9 ドラフト提示(EP支払い→PP払戻の一方通行・転がし v1 不採用)
- 2026-08-04: §9 確定。D-007 = 10分サイクル+週=4時間 決定。§10 ドラフト提示(NPC世界・完全抽選・自馬出走は時刻を自分で選ぶ)
- 2026-08-04: §10 確定。セッション区切り。次: §4/§12/§14〜§17(技術章・レビュー側単独で進行可) / §3・§11(D1/D2待ち) / P0報告が来たら照合優先
- 2026-08-04: **P0 レビュー完了**（`REVIEW_P0_VERDICT_20260804.md`）。D-008 決定に伴い §5.2/§5.3/§6.3/§6.4/§6.5/§13.1/§13.2/§13.3 を改訂。指示書の 3×4=0.0703125 は**レビュー側の誤り**と確定し §6.5 に正値 0.015625 を明記
- 2026-08-04: **P0-fix レビュー完了**（`REVIEW_P0FIX_VERDICT_20260804.md`・CONDITIONAL PASS）。D-009 決定に伴い §6.4（形質別の変異・回帰パラメータ表）/§13.1（TRAIT_MUTATION）/§13.2（V-2d・V-2e 新設）を改訂。**回帰中心を値域一律にすると丈夫さが650→450へ恒久移動する**副作用はレビュー側の指示 F-1 に起因。指示書 `DEV_INSTRUCTIONS_P0FIX2_20260804.md` を発行
- 2026-08-04: **P0-fix3 レビュー完了**（`REVIEW_P0FIX3_VERDICT_20260804.md`・PASS）。合格基準12項目すべて成立し全数値を独立再現。**D-013**: V-2e が下限寄り（0.84〜0.96）だった原因は遺伝的浮動でなく **clamp 切断（解析値0.916倍）**とレビュー側が特定 → 導出式に ÷0.916 を追加。指示書 `DEV_INSTRUCTIONS_P0FIX4_20260804.md` を発行（P0 の最終便）
- 2026-08-04: **P0-fix2 レビュー完了**（`REVIEW_P0FIX2_VERDICT_20260804.md`・PASS）。丈夫さは650近傍に是正済み。**D-010**: 距離系の sd を平衡式から導出し回帰率を全形質共通0.20へ統一（P0-fix の「距離だけ0.01」は sd を据え置いたことによる誤り・レビュー側が独立検算して確認）。**D-011**: 大物覚醒を能力5種のみに限定（気性の悪化・万能型化という逆効果を断つ）。Q2 clamp位置は「合計に clamp」で確定。指示書 `DEV_INSTRUCTIONS_P0FIX3_20260804.md` を発行
