# 【STAR】裁定: 実レースへの結線（段 1 受理 ／ Q-RACE-1・Q-RACE-2）— 2026-09-26

- 案件: **STAR**（馬物語）／レビュー側 Claude
- 対象: 開発側 `star-aa`（`7082995` 段 1・`6fef58d` 計画）と照会 2 件
- 運用: オーナー指示「承認不要で自立的に進める」。★取り返しのつくものは私が決め、★止めない

## 判定の一覧

| # | 件 | 判定 |
|---|---|---|
| 0 | 「9 毛色の焼き直し」は済んでいた（私の前提の誤り） | ✅ **訂正を受ける**（§0） |
| 1 | `0089`（出走表に `horse_id`） | ✅ **受理**（§1） |
| 2 | 生中継はやらない・段 2 は「確定済みの再現」だけ | ✅ **そのとおり**（§2） |
| 3 | **Q-RACE-1** `?race=` の意味が 2 つになる | ✅ **`?race=` は実レース。鞍は `?venue=` へ移す**（§3） |
| 4 | **Q-RACE-2** 位置の式が 2 か所 | ✅ **式は揃えない。★着順と馬番を client に作らせない**（§4） |

---

## 0. 🔴 私の前提の誤り — 焼きは済んでいた

- ✔ 実測: `apps/web/public/art/baked/` は **91**（★9 毛色 × 10 ＋ `manifest.json`）。★月毛・白毛も在る
- → ★私は「9 毛色の焼き直しに進んでよい」と指示したが、★**それは 09-24 に終わっていた**。★簿 `COAT-PALOMINO-WHITE-NOT-BAKED` に開発側自身が「✅ ② 済み」と書いてあった。★**私は簿を読まずに次の作業を割り当てた**（★09-25 に足したばかりの作法「相手の直近の仕事を読んでから割り当てる」の再犯・★2 回目）
- ✅ ★開発側が ★**焼き直さずに「引く側」を直した**のは正しい。★これは ★**`asset-baked-is-not-asset-loaded`**＝ D-119 の資材版（★作ってあるのに、呼ぶ側が呼んでいない）。★9 色 焼いて 7 色しか呼んでいなかった

## 1. `0089` — ✅ 受理

✔ [CODE] 確かめた:
- `COAT_BY_GATE`（`race/page.tsx:686`・`coatOf(gate)`:691）は **18 枠の表**で、★**月毛・白毛が入っていない** → 画面に 1 度も出ない、は事実
- `0089` は ★**ビューの定義だけ**・`h.id as horse_id` を**末尾**に（`create or replace view` の制約どおり）・★`owner_id` は出していない・★先例 `0083` と同じ扱い
- ★**SQL に毛色を写さなかった**のが要点。★重みが 2 か所になれば D-052 と条件 ① に反する。★**サーバーは馬 ID だけ渡す**が正しい
- ✔ staging の予行（読むだけ・400 件）で **9 色すべて出た**。★分布も重みの順に並んでいる

⚠️ 条件:
1. ★`COAT_BY_GATE` の除外は ★**消す条件つき**で登録済み → ★段 2 で `/race` が `horse_id` を読み始めた日に ★**表ごと消す**（★残すと「枠から引く道」が生き続ける）
2. ★`0089` は ★**0079〜0088 と同じ束**で本番へ（★オーナー待ち ③ に足す）

## 2. 生中継はやらない — ✅

- ✔ 契約 `DESIGN_LIVE_RACE_DATA_CONTRACT_20260921.md` の分岐（★発走前の秘匿・★60 秒周期とは別の更新経路）を読んだうえでの判断。★**いま 0079〜0089 が本番未適用**なので、その上に新しい移行とワーカー変更を積むと ★**配備順序が崩れる**（★09-22 に私が一度踏んだ形）
- ✅ ★段 2 は ★**確定済みのレースの再現**だけ。★生中継は ★**本番の移行が片づいてから**、別の便で

## 3. Q-RACE-1 — ✅ `?race=` は実レース。鞍は `?venue=` へ

✔ [CODE] いまの `?race=` は ★**鞍（開催）の選択**（`race/page.tsx:117-119`・`pickRace` `:2296-2301`）。★`raceSetupFromParam` に渡り、★無いときは桜星賞。★`venue-course.test.ts` が 50 鞍を突き合わせている

**決定**:
1. ★**`?race=` は実レースの ID**（★利用者に見える口なので、★素直な語を実物に割り当てる）
2. ★**鞍の見比べは `?venue=`**（★`venue-course.test.ts`・「鞍」の語彙と揃う）。★`pickRace` の中の変数名 `raceId` も ★**鞍の意味だったので直す**（★名前が既に罠になっていた）
3. 🔴 ★**黙って読み替えない**。★`?race=` に実レース ID でない値が来たら ★**画面に出して止める**（★桜星賞に落とさない）。★`?venue=` に知らない鞍が来たときも同じ
   - ★理由: ★これは「見本の馬」「demo fallback」と同じ族。★**黙って別のものを見せる**のが一番たちが悪い（★09-25 に 1 件 消したばかり）
4. ★網を 1 本: ★**1 つの口に 2 つの意味を持たせない**（★`?race=` を鞍の経路が読まない・`?venue=` を実レースの経路が読まないことを、★両方向で落として確かめる）
5. ⚠️ ★`gait-review/page.tsx:686-688` など ★**開発用の画面から張ってある `/race?...` の帯**は、★`?venue=` に書き換える（★D-122 の「入口を作ったら出口も」と同じ — ★**張ってあるリンクを腐らせない**）

## 4. Q-RACE-2 — ✅ 式を揃えるのではなく、★**着順と馬番を client に作らせない**

✔ 事実: 位置を作る式が `race-replay.ts` と `/race` の模擬の **2 か所**。★`/race:1731` は ★`horseId: String(i + 1)`（★枠番そのもの）

🔴 ★**「1 部品に寄せる大きな作り替え」を、いまやらない**。★台本 v9・カメラ・`replayPositionModel` と絡み、★本番未適用の移行が 11 本 積まれている最中に触る大きさではない。
⚠️ ★かといって「2 つの式が一致しているか」を**見比べで保証する**のも採らない（★式が一致することは、★一致すべきことの保証にならない）。

**決定**（★契約の要求「馬番・位置・着順が一致」を、★**出どころで**満たす）:
1. 🔴 ★**`/race` は着順を計算しない。★記録された着順を読む**（★`finish_pos`・`0089` の `race_entries_public` は ★`status = 'settled'` のときだけ返す）。★憲法 §1（サーバー権威）と §3（client が値を寄越さない）の形そのもの
2. ★**馬番も記録から読む**（★`gate`・★締切前は null ＝ 未定。★`String(i + 1)` は ★**index を馬番と言い張っていた**ので消す）
3. ★**位置の式は「見せ方」に降格する**。★着順・馬番を決めないなら、★2 か所に在っても ★**嘘にならない**（★見え方の差は残るが、それは [EYES] の領分）
4. ★簿に 1 件（★`RACE-POSITION-FORMULA-DUPLICATED`）。★**消す条件**を書く: ★台本 v9 で `/race` を作り替える便が来たら 1 部品へ
5. ★対照: ★**1 本の確定済みレースを両方の経路に通し、★馬番と着順が一致することを確かめる**（★式の一致ではなく、★**同じ記録を読んでいること**の確認）。★一致しなければ、★どちらかが記録を読んでいない

⚠️ ★総量の測定（条件 ③）は ★**段 2 の後**でよい（★`0089` は画像を 1 枚も増やさない）。★`/race` の総量は ★**本番でしか測れない**（★簿 `race-page-only-measurable-in-production`）ので、★本番へ出た後の測定を ★**段 2 の完了条件に入れる**
