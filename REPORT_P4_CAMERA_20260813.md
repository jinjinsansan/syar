# REPORT_P4_CAMERA_20260813 — 描画抽象化とカメラ／1頭目の実測／寸法の確定

## 本便で正典の改訂が要る決定をしたか

**していません。**（★寸法の変更は**こちらから照会**し、レビュー側が **D-058** として改訂されました）

---

## 1. ★1頭目の試作で分かったこと — 32×32 では成立しませんでした

**オーナーに実物を見ていただいた結果です。**

| 試したもの | 評価 |
|---|---|
| 細密画を 32×32 に機械的に縮小 | ★「ぼやけており、何かわかりません」 |
| **最初から 32×32 のドット絵**として描かせた（12色・輪郭立て） | ★同じく不可 |
| 32/48/64/96/128 px の比較 | 「馬に見えるのは **96 と 128**」 |
| **実際のレース画面**（1280×720）で A=350 / B=220 / C=140 | ★**A・B 可 / C 不可** |

→ **D-058 で 220×140 に確定**（引き 1× / 寄り 2×）。

### ★私が3回作り直しても直らなかった理由

**作り方の問題ではありませんでした。** 32×32 に馬と騎手を入れると**輪郭以外は原理的に消えます**。
★アートバイブル §3 の「32×32 では馬体で描き分けられない」が、**絵全体に対して起きていました**。

### ★「契約が動く」を機械で確かめました

D-057 の「32×32 を変えるとエンジン側の契約が動く」について:

```
描画層      : 寸法を一切持たない（grep 0件）。シート名＋フレーム番号で参照
レースエンジン: 絵を知らない（着順と時間だけ）
依存        : ★文書だけ
```

→ レビュー側が「私の誤りでした」として **D-058** で撤回。

---

## 2. 実測（指示書 A-3 の3項目）

| # | 項目 | 結果 |
|---|---|---|
| 1 | **シート契約** | 6列 × 1行 / ★**接地線のばらつき 1px**（揃っている） |
| 2 | **1頭あたりのバイト数** | 32px: 2.4KB / 128px: 10.8KB / ★**350px 原画: 640KB** |
| 3 | 18頭のフレーム時間 | ★**未測定**（位置モデル待ち・§5） |

★**バイト数は判断材料になりませんでした。** 128px でも18頭で 194KB です。
**効いたのは「画面に何頭映るか」だけ**でした（350px=2段 / 220px=3段 / 140px=6段）。

### ★勝負服の画素予算

```
32×32 のとき: 20px（1枚絵から）→ 37px（シートから切り出し・余白除去後）
```

★**測り方を1度間違えました。** 「彩度が高い＝勝負服」としたところ **126px**（不透明部の48%）と出て、
**鹿毛の茶（彩度 0.57）を勝負服として数えていました**。多すぎる値で気づき、**色相**で測り直しています。

★**220×140 になったので、この問いの前提は変わりました**（馬体を描き分けられる）。

---

## 3. 実装 — §12.8 の描画抽象化とカメラ

### 3.1 描画抽象化（`packages/render`・依存ゼロ）

- Canvas も DOM も React も知りません。**数と文字だけ**を返します
- ★**位置モデルは注入**（Q-P4-06 が未裁定なので、補間式を決め打ちしない）
- ★**色値も fps も持ちません**（前便の「数値を先に置かない」に従う）
- ★**描く順序を馬番で固定** — 位置順にすると同着付近で並びが入れ替わり、C-5 が理由なく崩れます

### 3.2 ★カメラ — C-6 を構造で守りました

```
ゲージと合図 → 画面の座標系に置く
             → カメラの倍率も中心も一切使わない
             → ★寄っても1画素も動かず、消えもしない
```

★**R-24 の三分割でいう「不可能にする」側**です。隠す経路が存在しません
（`z` も `cam` もその計算に入っていない。**メタテストがコードで確認**）。

| 規律 | 内容 |
|---|---|
| 倍率 | **1 と 2 のみ**（型＋実行時）。非整数はピクセルアートを壊す |
| 局面 | ★**残り距離**で決まる（800m/400m）。**時刻ではない** — 遅い馬と速い馬で同じ時刻でも局面が違う |
| 段数 | `lanesOnScreen()`。720p/220px = **3段**。★**18頭は入らない**ことをテストで固定 |

### ★自分から足したもの: 合図は「出ていない間」も `active:false` で出す

「出るまで描かない」にすると、**反応時間制限ボットは「まだ来ていない」と「見落とした」を区別できません**。
★C-6 は**ボットが画面に描かれたものだけを見る**設計なので、そこが曖昧だと**測っているものが分からなくなります**。

---

## 4. ★仕様からの逸脱（全件）

| # | 逸脱 | 理由と補い |
|---|---|---|
| 1 | **アセットを `image-cockpit` ではなく Codex CLI で作った**（A-1 違反） | ★**オーナーのご提案**。cockpit の画面が `二足（biped）固定・セル 256×256 固定`で**馬（四足）に合わず**、かつ生成物を outbox に置けない不具合が続いたため。**生成エンジンは同一**（Codex の imagegen）。★A-2 の目的（出所の記録）は**プロンプト全文をコミット**して満たしています |

★**この逸脱で回避できた問題**もあります — cockpit のアニメーションは biped 固定でした。

---

## 5. 未解決 / 次に要るもの

| # | 内容 |
|---|---|
| **Q-P4-06** | ★**位置モデルが未裁定**。カメラは書けましたが、**C-6 のボットは動かせません**（残り距離が決まらないと局面が決まらない） |
| — | **1頭目を 220×140 で作り直す**（現在の試作は 32px 前提で作った）|
| — | 18頭のフレーム時間（C-1）は位置モデル待ち |

---

## 6. 再現

```bash
npx vitest run packages/render          # 21件（抽象化 + カメラ + メタテスト）
node tools/measure-sprite-sheet.mjs design/art/assets/horse-gallop-sheet.png --frames 6 --cell 32
node tools/codex-imagegen.mjs <プロンプト> <出力.png>   # ★生成と取り込み
```

**テスト 661件緑 / `tsc --noEmit`**

---

## 7. ★オーナーへ

1. **staging の DB パスワードと service_role キーのローテーション**（8/10 の露出ぶん・**未実施**）
2. 寸法が **220×140** に確定したので、**1頭目を正しい寸法で作り直す**のが次の作業です
