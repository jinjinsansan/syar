# 裁定 — 監査の修正 第 1 便（`REPORT_AUDIT_FIX_20260914.md`）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-14 ／ レビュー側 → 開発側
**対象**: `REPORT_AUDIT_FIX_20260914.md` ／ 実装コミット `008d822`
**指示書**: `DEV_INSTRUCTIONS_AUDIT_FIX_20260914.md`

> 凡例（R-13）: **✔ 再現** ＝ レビュー側が実行するかコードを読んで確かめた ／ **△ 未再現** ＝ 開発側の主張のまま

## 判定

| 工程 | 判定 |
|---|---|
| AF-0 先に測る | **PASS** |
| AF-1 払戻を整数で | **PASS** |
| AF-2 オッズを 0.1 単位で切り捨て | **保留**（V-10 の結果待ち・§3-1） |
| AF-3 `spend_training_ep` をワーカー専用に | **PASS**（本番への適用は §6 の条件つき） |
| **総合** | **CONDITIONAL PASS** |

実装は指示どおりで正確です。報告の書き方もよく、再実行表、変異の実演、staging での実演 5 本、`proacl` の確認がそろっていて、照合がとても速く済みました。

---

## 1. レビュー側の再実行

| # | 内容 | 結果 |
|---|---|---|
| 1 | `npm run typecheck` | ✔ EXIT=0（HEAD `008d822`） |
| 2・3 | 関係テスト（`packages/betting`・`payout`・`odds`・`rpc-guard`・`exposure-registry`・`calibration-registry`・`selfcheck`） | ✔ `Test Files 9 passed` ／ `Tests 115 passed` ／ EXIT=0 |
| 5 | H-1 の変異で食い違う件数 | ✔ 79,432。レビュー側が監査の照合で独立に数えた 31,577＋31,577＋16,278 と一致 |
| 4・6・7 | 全テスト・残りの変異 | **未実行**。開発側の V-10 が同じ作業ツリーで走っている間は、ファイルを書き換える実験をしない（測定と改変を並行させない） |
| 8〜12 | DB を使う検査 | **未実行**（△）。数値は報告 §1・§4 のまま |

**改行（報告 §0-2・§6-4）**: ✔ `git ls-files --eol` で `0016`〜`0021` はすべて `i/lf w/lf`。`git status` の `M` は中身の差ではない、という説明どおりです。

---

## 2. 工程ごとの照合

### AF-0 ✔
- 関数の実行権限を、名前を並べずに `pg_proc` から走査している（R-29）
- 本番を 1 回だけ読んで、**利用者 0・持ち馬 0・馬券 0・調教の台帳 0** を確認した。裁定 `REVIEW_AUDIT_VERDICT_20260914.md` §3-1 の「実害は推測」はこれで閉じます
- 監査の「最大 +5.24%」は staging の保存値では再現せず、最大は複勝の +4.09%。**監査の数字を鵜呑みにせず測り直したのは正しい判断です**

### AF-1 ✔
差分を読んで確かめました。
- `grossPayout` は `(numerator − numerator mod divisor) / divisor` で、途中の値もすべて整数です。安全な整数の範囲を超えたら例外にします
- `payout.ts` は `oddsTenthsFromDecimalString(b.odds_at_purchase)` で、DB の文字列から浮動小数を経ずに変換しています。`DECIMAL_ODDS` は `numeric(9,1)` の形だけを受け付けます
- 払戻の式は `settleTenths` → `grossPayout` の 1 か所だけです
- 既存テストの `3.33`・`99.99` は 0.1 単位の値に置き換え、**上側だけでなく一致（両側）で守る形**にしてあります

### AF-2（保留）
- 切り捨てそのものの実装（上限で頭打ち → 切り捨て、格子ちょうどの値を 1 段下げない許容幅、両側の検査、staging の 1,396 行の照合）は正しいです
- `odds.ts` の `capped` を「切り捨て前の値 > 上限」に変えたのも正しい判断です（範囲外の変更ですが必要なもの・Q6）
- **保留の理由は §3-1 です**

### AF-3 ✔
- `0021` は、検査の削除・3 つのロールからの権限の剥がし・`ST001` の付与を同じトランザクションで行っています
- 変更前に、稼働中の定義の md5 が本番・staging・`0020` の 3 つで一致することを確かめています
- `training-runner.ts` は、SQLSTATE が `ST001` のときだけ休養に落とし、それ以外の失敗は数えて警報を出します。✔ 打ち切ったときの `incomplete: true` は、`hitCap` の初期値が true で、対象の馬が 0 頭になったときだけ false になるので成立しています
- `rpc-guard.test.ts` のワーカー専用関数の登録簿は、壊れた移行ファイルの 5 つの形がすべて落ちることを合成の SQL で固定しています（R-14）。コメントの中の `revoke` を数えない処理も入っています

---

## 3. レビュー側の発見

### 3-1. ⚠️ 切り捨てで、複勝の払戻率が約 1pt 下がる見込み（レビュー側の見落とし）

V-10（`verify-payout.ts`）は、売っている目すべてに 1 点ずつ賭け（`st.stake += m.size`）、**目標との差が 1pt 以内**（`Math.abs(dev) <= 0.01`）なら合格とします。

切り捨てで失う額は、的中 1 本あたり平均でおよそ 0.05 倍です（端数が一様に散ると仮定）。したがって払戻率の下がり幅はおおよそ「的中数 × 0.05 ÷ 売った目の数」になります。

| 券種 | 1 レースの的中数 ／ 売る目の数（おおよそ） | 払戻率の下がり幅（推定・△） |
|---|---|---|
| 複勝 | 3 ／ 12〜13 | **約 −1.2pt** → **V-10 が不合格になる可能性が高い** |
| 単勝 | 1 ／ 12〜13 | 約 −0.4pt |
| ワイド | 3 ／ 約 70 | 約 −0.2pt |
| 馬連・馬単・三連系 | 1 ／ 70 以上 | 0.1pt 未満 |

**これは、監査の裁定で切り捨てを推奨したときにレビュー側が確かめていなかった点です。** 下がる向きは払戻が少なくなる側（お店が安全な側）ですが、**正典 §9.3 の還元率が実際には達成されなくなります**。V-10 の実測で確かめます。

**選択肢（オーナー判断・§5）**
- **(a) 受け入れる（レビュー側の推奨）**: §9.3 の控除率は「切り捨て前」の定義と明記する。V-10 は「切捨て無しなら」の列で判定し、切り捨てで減った分は券種ごとに別に報告する
- (b) 単勝・複勝の控除率を、切り捨てで減る分だけ下げて埋め合わせる（経済の数字を動かすので重い）
- (c) 四捨五入に戻す（最大 +4.09% 払い過ぎる監査 H-2 が戻るので、採らない）

### 3-2. 「切捨て無しなら」は変更前の払戻率と同じ値（報告 §3-3 の △ を閉じる）

`verify-payout.ts` の差分では、`beforeFloor = min(ODDS_CAP, raw)`・`payout += paid`・`floorLoss += beforeFloor − paid` です。「切捨て無しなら」＝ `payout + floorLoss` ＝ **`beforeFloor` の総和**になります。変更前の `oddsFromProbability` が返していた値は `min(ODDS_CAP, raw)` そのものです。売る目・的中の判定・`cappedLoss` の数え方は変わっていません。

→ **足し合わせる順序による浮動小数の丸め以外は、変更前の実行と同じ値です。** 止めた変更前の実行をやり直す必要はありません。

### 3-3. `0021` の冒頭の注釈が古いまま凍結されている

`0021` の冒頭に「⚠️【下書き・未適用】… md5 の照合は**まだ実施していません** … **照合前に適用しないこと**」と書かれています。照合は報告 §1-e で済んでおり、staging にも適用済みなので、**この注釈は事実と逆**です。

しかし、`migrate.mjs` はファイル全体の sha256 を記録し（`:102`・`:252`）、**適用済みのファイルを書き換えることを自身のルールで禁じています**（`:179`）。

→ **裁定: ファイルは書き換えない。** 本番へ適用するときの手順（§6）に「冒頭の注釈は古い。照合は報告 §1-e で済んでいる」と明記します。
→ **規律として残す: 適用すると凍結されるファイルに、状態（下書き・未適用・照合待ち）を書かない。** 状態は報告書に書く。

### 3-4. 本番では、`0021` をワーカーの配備より先に当てる

新しいワーカーは、EP 不足を `ST001` だけで見分けます。**`0021` より先に新しいワーカーを配備すると、本番の `0020` は EP 不足に `ST001` を付けず、そもそも先に「未認証」で落ちるので、持ち馬の週送りがすべて「その他の失敗」で止まります。** いまは本番に持ち馬が 0 頭なので実害はありません。順序は §6 に固定します。

### 3-5. 照会 Q1 の「1 周 1 バッチしか進まない」はコードから成立する

馬を選ぶ SQL は `last_processed_week < $1 order by id limit 2000` です（`training-runner.ts:193-196`）。失敗し続ける馬は進まないので、**毎回同じバッチに選ばれ、そのたびに打ち切りになります**。1 周（10 分）で進むのは 2,000 頭までになり、頭数が多いと週の進みに追いつけなくなります。回答は §4 の Q1。

---

## 4. 照会への回答

| # | 照会 | 回答 |
|---|---|---|
| **Q1** | EP 不足以外の失敗の扱い | **「止める」も「バッチで打ち切る」も採らない。** 失敗した馬だけを飛ばし、**同じ実行の中では選び直さない**（例: 選ぶ SQL に `and id <> all($3)` を足し、失敗した馬の ID を渡す）。他の馬はバッチの上限まで進める。警報は周ごとに出し続ける。失敗し続ける馬が 1 頭いても、世界全体の進みを落とさない |
| **Q2** | `place_bet`・`exchange_prize` の anon の実行権限 | **剥がす。** 開発側の根拠（検査を外す変更が入った日に穴が開く）は、**今回まさに `spend_training_ep` で起きた経路**であり、R-29 の「具体的な破られ方」を満たします。剥がしたら V-20 の登録簿を `anon: false` に直す |
| **Q3** | 関数の既定の権限を閉じる | **承認。** 条件は、staging で ①新しく作った関数が anon・authenticated の実行権限を持たない ②`place_bet`・`exchange_prize` の authenticated の実行権限が残っている、の 2 つを実演してから |
| **Q4** | 1.0 倍を下回る目 | **オーナー判断待ち**（§5）。レビュー側の推奨は、上限の目を売らない D-035 と同じ考え方で**売らない** |
| **Q5** | 移行ファイルの改行の再発防止 | **`.gitattributes` に `db/migrations/*.sql text eol=lf`**。`migrate.mjs` のチェックサムの取り方は変えない（安全策に手を入れる変更は R-26 の経路になる） |
| **Q6** | 範囲外に変更した 6 ファイル | **追認。** どれも指示どおりの変更に必要なものでした（§2） |
| **Q7** | 本番への `0021` の適用 | **§6 の条件で、オーナーの指示により行う** |

Q1〜Q3・Q5 の実装は**第 2 便**の指示書で出します（本裁定では着手しないでください）。

---

## 5. オーナー判断待ち

1. ~~**§3-1 の払戻率の扱い**~~ → ★**オーナー決定（2026-09-14）: (a) 受け入れる。** 正典 §9.3 の控除率・還元率は「0.1 単位の切り捨て前」の定義とする。V-10 は「切捨て無しなら」（切り捨て前）の列で合否を判定し、切り捨てで減った分は券種ごとに別に報告する（判定には使わない）
2. ~~**Q4 1.0 倍未満の目**~~ → ★**オーナー決定（2026-09-14）: 売らない**（正典 **D-096**）。切り捨て前のオッズが 1.0 倍未満、すなわち `p_eff > 1 − margin` の目は `race_odds` に載せない。V-10 の賭け金からも外す。実装は第 2 便
3. **正典への D-094・D-095 の書き込み**。1 が決まったら、D-094 に「控除率は切り捨て前の定義」まで含めて書きます
4. **本番への `0021` の適用**（§6）

---

## 6. 本番へ `0021` を適用するときの手順

| # | 手順 | 合格の条件 |
|---|---|---|
| 1 | 適用の直前に、本番の稼働中の定義の md5 を読む | `b9f344581ffdb9df47f207597ac46869`（報告 §1-e と同じ）。違ったら**止める** |
| 2 | `migrate.mjs --env production` で `0021` だけを適用（オーナーが実行・出力を切らない） | `未適用 1 件` ／ `0021_spend_training_ep_worker_only.sql ... OK` |
| 3 | 本番の稼働中の定義の md5 を読む | staging の適用後と同じ `d68a7799bf82d13c953de5040a6fdead` |
| 4 | `verify-anon-exposure.mjs --env production` | **V-20 合格 11 件中 11 件** |
| 5 | **そのあとで**新しいワーカーを配備する | §3-4。順序を逆にしない |

⚠️ `0021` の冒頭の「【下書き・未適用】照合前に適用しないこと」は**古い注釈**です。照合は報告 §1-e で済んでいます（§3-3）。

---

## 7. 開発側の次の作業

1. **報告 §3-3 に V-10 の結果を追記**（開発側が予定しているとおり）。券種ごとに次の 4 つを並べてください
   - 払戻率（切り捨て後）
   - 「切捨て無しなら」（切り捨て前）
   - その差（切り捨てで減った分）
   - 合否（切り捨て後の値での判定と、切り捨て前の値での判定を**両方**）。★§5-1 のオーナー決定により、**正式な合否は切り捨て前の値**で出す。切り捨て後の判定は参考として並べる
2. **報告 §0 の終了 HEAD を `008d822` に直す**
3. 1 と 2 だけを、ファイルを名指しした別のコミットにする（開発側の予定どおり）
4. §3-2 のとおり、**変更前の V-10 はやり直さなくてよい**
