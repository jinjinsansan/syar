# 報告 — ゲーム本体の再設計 第 1 便（GB-1〜GB-6）

**案件**: STAR（ポイ活型オンライン競馬育成）
**発行**: 2026-09-16 ／ 開発側 → レビュー側・オーナー
**対象**: 指示書 `DEV_INSTRUCTIONS_GAME_BODY_1_20260916.md`
**測ったコミット**（R-23・R-28）: 着手時 `fe5bd53`（指示書と正典の受領）／ GB-1 は本報告と同じコミット
**ブランチ**: `p4/race-30sec-cuts`

> 凡例（R-13）: **✔** ＝ 開発側がこの便で実行・実測した ／ **△** ＝ 未確認・推定
> ⚠️ **着順に効くものは 1 つも入れていません。** `packages/race-engine/src` は触っていません。
> ⚠️ 本番 DB・ワーカーの配備・較正定数・`CYCLE_MS`・正典は触っていません。

---

## 0. この報告の範囲

| 工程 | 状態 |
|---|---|
| **GB-1 調教の見せ方** | ✔ **完了**（本報告・§1） |
| **GB-2 所有頭数** | ✔ **完了**（本報告・§1b） |
| **GB-3 騎手** | ✔ **完了**（本報告・§1c） |
| **GB-4 馬の購入** | ✔ **完了**（本報告・§1d） |
| **GB-5 厩舎の格** | ✔ **完了**（本報告・§1e） |
| **GB-6 経済の取り直し** | ✔ **完了**（本報告・§1f）／ ⚠️ **V-11 はオーナーのターミナル待ち** |

★指示どおり **1 工程 1 コミット**で進めます。本報告は工程ごとに追記します。

---

## 1. GB-1 — 調教の見せ方（D-101）✔

### 1-1. 作ったもの

| ファイル | 中身 |
|---|---|
| `packages/training/src/menus.ts`（追記） | **写像を 1 か所に**: `TrainingAxis`（体・心）・`TrainingIntensity`（弱・中・強）・`MENU_VIEW`（8 メニュー → 枡）・`menuViewOf`・`menusOfView`・見出しの語（`TRAINING_AXIS_LABEL`・`TRAINING_INTENSITY_LABEL`） |
| `packages/training/src/view.ts`（新規） | **画面の 3 本のバー** `trainingBarsOf(stats, condition)`（スピード・スタミナ・コンディション）と、**出走の前後の週の印** `raceWeekMarkOf` ／ `RACE_WEEK_LABEL` |
| `packages/training/src/index.ts` | `view.js` を公開 |
| `apps/web/package.json` | `@star/training` を依存に宣言（画面がこれを引くため） |
| `apps/web/src/lib/game-demo.ts` | ★**8 メニューの表の写しを削除**し、名前・疲労・EP・枡を `@star/training` から作る形に（説明の文言だけ画面側に残す）。`trainingMenusOfView` を追加。`DEMO_TRAINING_STATS`（★上限までの割合を持っていた）を `DEMO_TRAINING_ABILITY`（`stats` だけ）に置き換え |
| `apps/web/src/app/training/page.tsx` | メニューを **体・心 × 弱中強**に並べ替え。バーは `trainingBarsOf` の戻り値から。**コンディションは能力と別の見せ方**（段階の枡＋調子の語）。**出走の前の週・後の週**を見出しに出す |

**層の向き**（報告の指示）: `@star/training` は**依存ゼロの純粋な層**（`@star/sim-engine` の型だけ）で、**画面がこちらを引きます**。逆向きの参照はありません。エンジン（`race-engine`）はこの便に関与しません。

### 1-2. 変えていないもの ✔

- **週の進み方・成長式・疲労・故障・EP コスト**は 1 ミリも変えていません（`growth.ts`・`week.ts`・`injury.ts`・`condition.ts`・`temper.ts` は無変更）
- **新しい乱数・倍率・確率を足していません**（「当たり」の演出は既存の伸びの乱数の上側を見せるだけ。この便では演出そのものは未実装で、写像と導線だけです）
- 較正定数（`MAIN_EFFECT_COEF`・`SIDE_EFFECT_COEF`・`BASE_GAIN` ほか）は触っていません

### 1-3. 検査 ✔

| # | 内容 | 結果 |
|---|---|---|
| 1 | 写像が **8 メニューすべてを覆い**、体・心 × 弱中強の **6 枡すべてに 1 つ以上**（全数・鍵の集合も一致） | ✔ `packages/training/test/menu-view.test.ts` |
| 2 | 画面・表示モデルが **`@star/training` の同じ関数**を引いている（**構文木**で見る。`grep` ではない） | ✔ `apps/cli/test/training-view-wiring.test.ts` |
| 3 | 3 本のバーが **`stats` と調子だけ**から決まる（★**入力の形**で見る。引数は 2 つ・`potential` も「上限までの割合」も渡す口が無い） | ✔ 同上＋写像の検査 ⑤ |
| 4 | **変異**: ①画面に写像の表を複製 ②`trainingBarsOf` を呼ばず自前で組む ③バーに「上限までの割合」を渡す ④表示モデルが自前の表に戻る → **4 つとも検査が落ちる** | ✔（製品ファイルは無傷。読み込んだ文字列に対して壊す形） |
| 5 | **V-14・V-15・V-7a/V-7b が変わらないこと** | ✔ §1-4 |

### 1-4. ゲート（変更前 → 変更後）✔

同じ引数（`--horses 400 --seed 42` ／ V-7 は `--horses 500 --seed 42`）で、**変更の前と後に 1 回ずつ**流しました。

| ゲート | 変更前 | 変更後 | 判定 |
|---|---|---|---|
| V-14 ① 適切な育成 88% 以上 | 90.3%（余裕 9.5 SE） | **90.3%** | **変わらない**・PASS |
| V-14 ② 差 12pt 以上 | 15.8pt（90.3 − 74.5） | **15.8pt** | **変わらない**・PASS |
| V-14 ③ 同一 EP で追い切り偏重が支配的でない | 0.68 倍（上限 1.02） | **0.68 倍** | **変わらない**・PASS |
| V-15 ① 中盤の集団 SD ≥ 誕生時の 50%（最悪 hard_only） | 59.9% | **59.9%** | **変わらない**・PASS |
| V-15 ② キャリアを通じた平均低下 ≥ 15%（balanced） | 35.8% | **35.8%** | **変わらない**・PASS |
| V-7a 恒久ダメージ 20〜40%（balanced） | 29.2% | **29.2%** | **変わらない**・PASS |
| V-7b 致命的故障 3% 以下（balanced） | 0.8% | **0.8%** | **変わらない**・PASS |

→ **写像は見せ方だけで、式に触れていない**ことが実測で確かめられました（D-101 の前提どおり）。

### 1-5. 未実装（GB-1 の範囲で残したもの）

- **「当たり」（GREAT / UP）の演出**（既存の伸びの乱数の上側を見せるだけの形）。今回は写像と導線までで、演出は画面の構成の便に回します（デザイナーのカード待ち・指示書 §8）
- **出走の前後の週**は、いまデモの `nextRace` の有無から作っています。実データ（次走の週・前走の週）に繋ぐのは出走登録の便です
- 指示の保存・週送りはサーバー RPC 待ち（従来どおり）

---

## 1b. GB-2 — 所有頭数（D-104）✔

### 1b-1. 作ったもの

| ファイル | 中身 |
|---|---|
| `packages/scheduler/src/ownership.ts`（新規） | **上限を 1 か所に**: `OWNERSHIP_LIMITS`（現役 **30**・繁殖牝馬 10・種牡馬 5）・`ENTRIES_PER_OWNER_MAX`（**2**）／ 純関数 `ownershipLimitOf`・`canOwnMore`・`canEnterRace`・`ownHorseSelectionOk` |
| `packages/scheduler/src/index.ts` | 公開 |
| `apps/cli/src/calibration.ts` | `OWNERSHIP_LIMITS`・`ENTRIES_PER_OWNER_MAX` を**正典の写し**として `EXEMPT` に理由付きで登録 |

- ★**「入っているが使われない」形**です（R-15）。**出走登録の RPC はまだ 1 つも無い**ので、繋ぐのは出走登録の便です
- ⚠️ **所有上限はこれまでコードに存在しませんでした**（正典 §6.7 にしかなかった）。今回が初出です
- 画面側に「15 頭」等の表示はありませんでした（直す箇所なし）✔

### 1b-2. 検査 ✔（`packages/scheduler/test/ownership.test.ts`・4 件）

| # | 内容 | 結果 |
|---|---|---|
| 1 | 現役 **30 頭目は受理・31 頭目は拒否**。繁殖 10・種牡 5 は変わっていない（境界の両側・R-2） | ✔ |
| 2 | 同じレースに **2 頭目は受理・3 頭目は拒否**。**別のレースなら影響しない** | ✔ |
| 3 | §9.5 の制限が 2 頭のときも成立（**全頭を含む組合せだけ**） | ✔（§1b-3 の註記つき） |
| 4 | 上限を 1 か所から引く（関数と表が同じ値。**2 か所に書いて片方だけ直せば落ちる**値照合） | ✔ |

### 1b-3. ⚠️ 見つけた食い違い（報告のみ・直していません）

**正典 §6.7・§9.5 は「自馬が複数なら全頭を含む組合せだけ」**ですが、**いまの DB の `place_bet`（`0002`・`0020`）は「自馬が 1 頭でも絡めば可」**です。

```sql
-- 0020_rpc_setup_guard.sql:151-158（現在の本番・staging の定義）
if not exists ( … and p_selection @> to_jsonb(e.gate) ) then
  raise exception '自馬出走レースでは自馬絡みの馬券のみ購入できる（§9.5）';
```

- 2 頭出したとき、**DB は「片方だけを含む買い目」を通します**。正典は通しません
- **1 頭までしか出せなかった間は同じ意味**でした。**D-104 で 2 頭まで出せるようにしたので、ここで差が生まれます**
- 開発側は **`ownHorseSelectionOk` を正典どおり**（全頭を含む）に書き、検査で固定しました。**移行ファイルは本便の触ってよいものに入っていない**ので、DB 側は直していません
- ⚠️ 正典の帰結として、**2 頭出すと単勝・複勝は買えません**（1 頭しか選べないため）。これでよいかはオーナー判断に回します（照会）
- → **次の便（出走登録・投票の RPC）で、DB 側を正典に合わせる移行が要ります**

### 1b-4. 流したゲート

- **該当なし**（GB-2 は着順・育成・経済のどれにも触れていません）。V-4〜V-6・V-17・V-18・V-14・V-15・V-7 は**この工程では動きません**
- 全体の検査で `calibration-registry` を含む 179 ファイルを流しています（§2）

---

## 1c. GB-3 — 騎手（D-105）✔

★**この便では着順に効かせていません**（効果 0 で「入っているが使われない」形・R-15）。

### 1c-1. 作ったもの

| ファイル | 中身 |
|---|---|
| `packages/scheduler/src/jockeys.ts`（新規） | **架空の騎手 6 人**（名前・料金 EP）／ `JOCKEY_EFFECT = 0`（着順への効果）／ 親密度 `JOCKEY_BOND_MAX = 5` と `jockeyBondAfterRides` ／ **凍結** `FrozenJockey`・`freezeJockey` ／ 記帳の語 `JOCKEY_FEE_LEDGER_REASON = 'entry_fee'` ／ `entryCostEP`（登録料 ＋ 騎手の料金） |
| `packages/scheduler/src/index.ts` | 公開 |
| `apps/cli/src/calibration.ts` | `JOCKEY_EFFECT`・`JOCKEY_BOND_MAX`・`JOCKEYS` を理由付きで `EXEMPT` に登録 |

- **名前は架空**です（正典 §0.1）。実在の騎手の名前・顔は使っていません
- **料金は EP**。`ep_ledger.reason` は `0001_init.sql` で `inflow / training / entry_fee / bet / refund / stud_fee` に**閉じている**ので、**出走登録と同じ `entry_fee`** で記帳します（新しい語を足すと移行が要り、本便の対象外）。**PP・賞金には触れていません**（D-105 ②）
- **凍結**は `RaceEntrant`（`@star/race-engine`）に足していません。あれは着順の計算に入る形で、**この便では騎手を着順に入れない**からです。凍結は出走登録の側（`race_entries` の `entrant_snapshot` と並ぶ列）に置く形を用意しました（繋ぐのは出走登録の便）
- **AI 代行の質は騎手で変えていません**（`resolveIntervention` の入力に騎手が入らない・D-105 ⑥）

### 1c-2. 検査 ✔

| # | 内容 | 結果 |
|---|---|---|
| 1 | **騎手を変えても着順・スコア・走破タイムが 1 ビットも変わらない**（8・18 頭 × 1200・2400m × シード 8 × 騎手 6 人＝ **192 通り**を実際に `resolveRace` で回して比較） | ✔ `apps/cli/test/jockey-no-effect.test.ts` |
| 2 | 料金が EP の台帳の語で、**PP・報酬率を持たない**（名簿の鍵が `id`・`name`・`feeEP` の 3 つだけであることも固定） | ✔ `packages/scheduler/test/jockeys.test.ts` |
| 3 | 凍結だけで確定に必要な値がそろう（名簿を引き直さない）。**名簿にいない id は投げる**（黙って既定にしない・R-27） | ✔ |
| 4 | 親密度が上限で頭打ち（境界の両側・R-2。0・1・4・5・6・1000・負) | ✔ |
| 5 | AI 代行の期待値が騎手によらない（凍結の形が騎手によらず同じ・**強さを表す項が無い**） | ✔ |

- ★**「効果 0 の定数」だけでは足りない**ので、検査 1 は**実際に `resolveRace` を回して**比べています（定数が 0 のまま別経路で混ざる形を捕まえるため・R-16）

### 1c-3. 未実装（GB-3 の範囲で残したもの）

- **騎手の絵**（架空の顔）。デザイナーのカード待ち（指示書 §8）
- **選択の画面・出走登録での保存**（RPC 待ち）。いまは純関数と凍結の形だけ
- **着順への効果**。効かせる便で上限（介入の ±10% との関係）を決め、V-4・V-5・V-6・V-17・V-18・V-13 を取り直します

---

## 1d. GB-4 — 馬の購入（D-102）✔

★**この便は純関数と検査だけ**です（DB の書き込みと RPC は次の便・指示書 §4）。

### 1d-1. 作ったもの

| ファイル | 中身 |
|---|---|
| `packages/sim-engine/src/stars.ts`（新規） | ★**素質を★で見せる算出**（`starsOfPotential`・`starsOf`・`sameStarBand`・`STAR_THRESHOLDS`）。**返すのは★だけ**で、素質の数値も開放率も返しません（§5.5・§12.4） |
| `packages/scheduler/src/horse-market.ts`（新規） | 値付け `priceOfStars`／手放して戻る `sellBackEP`／候補 `listingsFromPool`／在庫の見張り `marketStockAlert`／`canBuyWithEP` |
| `apps/cli/src/calibration.ts` | ★の刻み 4 つと市場の 4 つを理由付きで `EXEMPT` に登録 |

- ⚠️ ★**★の算出はこれまでどこにもありませんでした**（画面のデモ値 `stars: 4.5` だけ）。**購入の「同じ帯」（D-102 ③）と画面の★は同じ量でなければならない**ので、**エンジン側に 1 か所**作り、両方がそれを引く形にしました
- **層の向き**: `@star/scheduler` は **依存ゼロ**（`dependencies: {}`）なので、`horse-market.ts` は **馬の記録を知りません**。★は呼び出し側が `starsOf` で出して渡します（算出は 1 か所のまま）
- **PP で買う口はありません**（`canBuyWithEP` の引数は EP と価格の 2 つだけ・§17.2 C-2 と同じ形）
- **売る馬は NPC 世界から**。この層は**馬を作りません**（渡されたプールから帯に合う馬を取るだけ・D-074）

### 1d-2. 検査 ✔（11 件）

| # | 内容 | 結果 |
|---|---|---|
| 1 | 候補の★が**全個体で同一**（★の算出量と同じ量で測る・D-079 ②）。帯に無い★を頼んでも**別の帯で埋めない** | ✔ |
| 2 | 同じ人が引き直しても**観測できる差が出ない**（★も価格も同じ。**価格から中身を読めない**） | ✔ |
| 3 | 値付けは★から決まり、最低価格を下回らず、★に対して単調 | ✔ |
| 4 | 手放して戻る EP は買った額より十分小さい（**1 往復で必ず半分以上減る**） | ✔ |
| 5 | **PP では買えない**（引数に PP を渡す口が無い） | ✔ |
| 6 | 在庫の下限で**警報が出る**（境界の両側・R-2） | ✔ |
| 7 | ★は 1〜5・0.5 刻み・境目の両側で 1 段だけ動く・単調・**★から素質の数値は復元できない**（9 段しかない） | ✔ `packages/sim-engine/test/stars.test.ts` |

### 1d-3. ⚠️ 報告（値は GB-6 で取り直します）

- **「配合より割高」（D-102 ④）は、まだ数で示せていません。** ★**種付料（配合費）の金額の式が、正典にも実装にもありません**（`ep_ledger.reason` に `stud_fee` の語があるだけ）。いまは**最低価格 `MIN_PRICE_EP = 3,000 EP`** を下限として置き、**比較と「同じ EP での期待する★」は GB-6 の収支の取り直しで報告**します（△）
- 価格の置き方（参考・計算）: ★3.0 で **6,000 EP**（デイリー 200 EP の 30 日分・初期 EP 2,000 の 3 倍）。**値そのものはゲートにしていません**（指示書 §4）

### 1d-4. 未実装（GB-4 の範囲で残したもの）

- **DB・RPC・画面**（候補の抽選・購入の記帳・在庫の実測）。次の便
- **抽選**（毎回違う候補を見せる）。この層は乱数を持ちません。帯が同じなので、入れても**観測できる差は出ません**

---

## 1e. GB-5 — 厩舎の格（D-103）✔

### 1e-1. 作ったもの

| ファイル | 中身 |
|---|---|
| `packages/training/src/grade.ts`（新規） | `StableGrade`（ブロンズ・シルバー・ゴールド）・`STABLE_GRADE_MULT`（1.0 / 1.25 / 1.5）・`gradeEpCost`（調教費 × 倍率）・`gradeGainMult`（伸び × 倍率）・`gainPerEpRatio` |
| `packages/training/src/index.ts` | 公開 |
| `apps/cli/src/calibration.ts` | `STABLE_GRADE_MULT`・`STABLE_GRADE_LABEL` を理由付きで `EXEMPT` に登録 |

★**設計の要点**: **伸びと費用に同じ倍率を掛けます**。
- 上の格は **1 週に多く伸びるが、そのぶん高い** → **EP あたりの伸びはどの格でも 1.0**（D-103 ②）
- 得られるのは **時間**（少ない週で同じところまで行ける）で、**強さではありません**
- **`potential` には触れていません**（`grow` は格を引数に取りません＝天井に関わる式に格が入らない形）
- **既定はブロンズ 1.0** なので、格を入れる前と **1 ビットも同じ**です

### 1e-2. 検査 ✔（`packages/training/test/grade.test.ts`・5 件）

| # | 内容 | 結果 |
|---|---|---|
| 1 | 既定はブロンズ・倍率 1.0・調教費は §7.2 の表そのもの | ✔ |
| 2 | **同じ EP を注いだとき上の格が強くならない**（EP あたりの伸びが全格 1.0。費用の比と伸びの比が一致） | ✔ |
| 3 | 上の格は**時間の効率だけ**上がる（同じ予算で週数が減り、伸びの合計は同水準） | ✔ |
| 4 | `grow` が格を取らない（引数 2 つ）・`current ≤ potential` が破れない・天井に達したらどの格でも伸びない | ✔ |
| 5 | 後半（`current` が `potential` の 9 割）でも伸びしろが 0 に張り付かない（D-044） | ✔ |

### 1e-3. ゲート（GB-5 の後）✔

| ゲート | GB-1 の前 | **GB-5 の後** | 判定 |
|---|---|---|---|
| V-14 ①②③ | 90.3% / 15.8pt / 0.68 倍 | **90.3% / 15.8pt / 0.68 倍** | **変わらない**・PASS |
| V-15 ①② | 59.9% / 35.8% | **59.9% / 35.8%** | **変わらない**・PASS |
| V-7a / V-7b | 29.2% / 0.8% | **29.2% / 0.8%** | **変わらない**・PASS |

→ 既定がブロンズ（倍率 1.0）で、**格を配線していない**（`advanceWeek` は格を受け取りません）ため、**この便では 1 つも動きません**。配線は次の便（格の解放と週送りの接続）です。

### 1e-4. 未実装（GB-5 の範囲で残したもの）

- **格の解放**（EP で上げる経路）と **週送りへの配線**（`advanceWeek` に格を渡す）。次の便
- 格ごとの見た目（画面）。デザイナーのカード待ち

---

## 1f. GB-6 — 経済の取り直し（§3.4・D-075）✔

### 1f-1. 道具

`apps/cli/src/economy-balance.ts`（新規・**計算だけ**。DB にも本番にも触れず、較正定数も動かしません）。
⚠️ ★**額を書き写していません**。調教費は `@star/training` の `MENUS`・`grade.ts`、騎手の料金は `jockeys.ts`、購入価格は `horse-market.ts` から引きます（二重帳簿にしない・`economy-balance.test.ts` ① が固定）。

実行: `npx tsx apps/cli/src/economy-balance.ts --stars 3`

### 1f-2. 1 頭・1 キャリアの収支（★取り直し）✔

前提は §3.4 と同じ（現役 90 週・24 戦・登録料 200 EP・配合費 2,000 EP）。**★3 の馬を買った場合**です。

| 格 | 1 週の調教費 | 調教費 | 登録料 | **騎手** | 配合費 | **購入** | 戻り | **支出計** |
|---|---|---|---|---|---|---|---|---|
| ブロンズ | 325 | −29,250 | −4,800 | **−7,200** | −2,000 | **−6,000** | +1,200 | **−48,050** |
| シルバー | 406 | −36,563 | −4,800 | −7,200 | −2,000 | −6,000 | +1,200 | **−55,363** |
| ゴールド | 488 | −43,875 | −4,800 | −7,200 | −2,000 | −6,000 | +1,200 | **−62,675** |

- **正典 §3.4 の −38,000 EP → ブロンズで −48,050 EP**（**+10,050 EP・約 1.26 倍**）。増えた内訳は **騎手 7,200 ＋ 購入 6,000 − 戻り 1,200 − 調教費の差 2,250**（§3.4 は「平均 350」、実装の 8 メニューの平均は **325**）
- **賞金（§3.4 の写し）**: 平凡 +25,000 PP 相当 ／ 重賞級 +120,000 PP 相当 → **「平凡な馬は赤字・強い馬だけが黒字」という §3.4 の性質は変わりません**（赤字の幅は広がります）
- **格を上げても支出だけが増え、EP あたりの伸びは同じ**です（D-103 ②。増えるのは調教費だけで、登録料・騎手・購入・配合は格によらない）

### 1f-3. 同じ EP での期待する★（D-102 ④）✔

| ★ | 価格 | 1 EP あたりの★（×10⁻⁴） |
|---|---|---|
| 1 | 3,000 EP | 3.33 |
| 2 | 4,000 EP | 5.00 |
| 3 | 6,000 EP | 5.00 |
| 4 | 8,000 EP | 5.00 |
| 5 | 10,000 EP | 5.00 |

- ★2 以上は **1 EP あたりの★が一定**です（価格が★に比例）。★1 だけ最低価格 3,000 EP に張り付くので割高になります
- ⚠️ **「配合より割高」は、まだ数で示せていません**（§1d-3）。**種付料の式が正典にも実装にも無い**ためです。**照会に回します**（下の §1f-5）

### 1f-4. D-075 の額はまだ妥当か ✔（計算）

| 額 | 値 | 取り直した収支との関係 |
|---|---|---|
| 初期 EP | 2,000 | ★**購入価格の下限 3,000 EP に届きません**。D-102 の「買う」を最初の日に選べません（配合・NPC からの入手が先になります） |
| デイリー | 200 | 1 キャリアの支出 48,050 EP ＝ **240 日ぶん**。§3.4 の −38,000 なら 190 日でした |
| 日次上限 | 10,000 | 1 週の調教費（8 頭 × 325）＝ 2,600 EP。**現役 30 頭（D-104）にすると 9,750 EP** で、**上限の 98%** に届きます |

- ★**値を動かす提案はしません**（較正定数・指示書 §6）。**上の 2 点（初期 EP と日次上限）を照会に回します**

### 1f-5. ⚠️ 照会（値は動かしていません）

| # | 内容 |
|---|---|
| Q-GB-1 | **種付料（配合費）の式が正典にも実装にもありません**。「馬の購入は配合より割高」（D-102 ④）を数で示すには、配合の費用の定義が要ります |
| Q-GB-2 | **初期 EP 2,000 では馬を買えません**（最低価格 3,000 EP）。買える額にするか、買えないままでよいか |
| Q-GB-3 | **日次上限 10,000 EP は、現役 30 頭の調教費（1 週 9,750 EP）でほぼ埋まります**。D-104 の 30 頭と D-075 の上限の噛み合わせ |

### 1f-6. V-11（ポイント純発行量・合成集団）✔ **PASS**

`npx tsx tools/verify-v11-synthetic.mjs --env staging`（★staging に書き込む道具。オーナーが流したところ**前提のレースが無く**、開発側が `seed-races --races 2 --trials 10000` で 2 本作ってから流し直しました）

```
① EP を配りました: 3 人 × 500,000 EP
② 馬を持ちました: 7/8 頭（最終枠は NPC のまま）
③ 調教しました: 延べ 64,000 頭 / EP 消費 31,500
④ 馬券を買いました: 1,400 EP（7 枚・★自馬絡みのみ・§9.5）
⑤ 確定しました: payout 400 PP(1 行) / prize 30,800 PP(4 行)
⑥ 景品と交換しました: 6,000 PP

EP 台帳: bet −1,400 / inflow 1,500,000 / training −31,500
PP 発行 31,200 / PP 消費 6,000 / 純発行 25,200
★V-11 ②（合成集団）: PASS — 7 項目すべて成立・exit 0
```

| # | 見たこと | 結果 |
|---|---|---|
| 1 | 調教で EP が消費された（G-6） | ✔ |
| 2 | 馬券で EP が消費された | ✔ |
| 3 | 賞金で PP が発行された（G-7） | ✔ |
| 4 | 払戻で PP が発行された | ✔ |
| 5 | 景品交換で PP が消費された（§11.3） | ✔ |
| 6 | **発行量と消費量がともに実質ゼロでない**（D-051 の両側） | ✔ 発行 31,200 / 消費 6,000 |
| 7 | **EP に払戻の行が無い**（PP → EP の還流が無い・憲法 ②） | ✔ |

- ★**§9.5 が実際に効いています**: 合成プレイヤーは出走馬を分け合って持つので、**他人の馬の単勝は買えず**、自馬絡みだけが通りました（道具の註記のとおり）
- ⚠️ ③で「週送りが上限（8 週 × 4 バッチ）に達しました」という警告が出ています。**次の周で続く**形で、V-11 の判定には影響しません △
- 合成集団は `internal` 口座なので、§11.2 の実経済の指標とは別掲です（実集団で②が不成立なのは「利用者がいない」という正しい表示・D-051）
- ★**GB-6 で増えた支出（騎手・購入）は、この一巡には入っていません**（購入と騎手の RPC が未実装のため）。**入った後にもう一度流す**必要があります（次の便）

---

## 1g. M-4 — V-10（券種別の払戻率）⚠️ **判定不能**（★合否ではありません）

★指示書 `REVIEW_VENUE_WIRING_MEASURE_INSTRUCTIONS_20260916.md` §1 の M-4。**オーナーの別ターミナルの予定でしたが、ES 便でエンジンが速くなったので開発側で流しました**（所要 **2 分 25 秒**・15:06:34 → 15:08:59）。

```
npx tsx apps/cli/src/verify-payout.ts --races 500 --seeds 42
races=500 odds-trials=10000 seeds=42 ／ 条件: 本番（凍結した走路の形）
V-10 総合（切り捨て前で判定）: FAIL・exit 1
★SE が 0.25pt に届いていない券種があります。**この実行は正式な V-10 ゲートではありません**（D-036）
```

| 券種 | 切り捨て前 | 乖離 | 切り捨て後との差 | 出走表間 SD | **SE** | 売目/R |
|---|---|---|---|---|---|---|
| win | 78.73% | −3.27pt | −0.37pt | 135.40pt | **6.06pt** | 13.2 |
| place | 77.80% | −4.20pt | −1.12pt | 51.86pt | **2.32pt** | 12.9 |
| quinella_place | 76.14% | −3.86pt | −0.22pt | 84.94pt | **3.80pt** | 68.1 |
| quinella | 77.06% | −2.94pt | −0.07pt | 181.52pt | **8.12pt** | 73.7 |
| exacta | 73.13% | −6.87pt | −0.04pt | 161.78pt | **7.23pt** | 147.9 |
| trio | 60.82% | −16.18pt | −0.02pt | 145.85pt | **6.52pt** | 284.3 |
| trifecta | 49.76% | −27.24pt | −0.00pt | 67.78pt | **3.03pt** | 1132.6 |

### 1g-1. ★なぜ「判定不能」と書くか（R-3・D-036）

1. **SE が 0.25pt に届いていません**（実測 2.32〜8.12pt）。道具自身が「**この実行は正式な V-10 ゲートではありません**」と出しています。**FAIL の 7 券種を「不合格」と読んではいけません** — 読み取れない量で合否を出さない、が正典の規則です（R-3）
2. ★**さらに、この実行の試行数は本番の値ではありません。** `--odds-trials` の既定は **10,000** で、**D-035 が定める本番の M は 3,896,104** です。目の多い券種ほど乖離が大きい（三連単 −27.24pt・三連複 −16.18pt）のは、**M が小さいときの打ち切りの効果**として説明が付きます（`diag-mneed.ts` の解析と同じ向き）。★**M を本番の値にせずに V-10 の合否を語れません**

### 1g-2. ★どうすれば測れるか（D-036 の分散低減）

- **標本を増やす方向では回りません**: SE は 1/√N でしか減らないので、`win` の 6.06pt → 0.25pt には **約 29 万レース**（いまの 500 レースの約 588 倍 ＝ 約 24 時間）が要ります
- **正典が用意している道は `apps/cli/src/verify-pmin.ts`** です（D-036）。**1 レースにつきオッズを 1 回作り、独立系列から確定を多数引く**ので、測る量は同じまま**分散だけが下がります**。★こちらは **M を本番の 3,896,104** で回せます
- ⚠️ **所要を実測しました**（`npx tsx apps/cli/src/verify-pmin.ts --races 2 --finals 2000`・15:10:39 → 15:14:00 ＝ **3 分 21 秒**。母集団を作る時間を含む）→ **本番の M で 1 レースあたり約 100 秒** ✔

```
# D-035 を本番コード経路で確認  seed=42 races=2 M=3,896,104 確定2,000回/レース
  win            払戻率 81.29%  乖離 -0.71pt  レース間SD 4.43pt  SE 3.130pt  PASS
  place          払戻率 81.46%  乖離 -0.54pt  レース間SD 1.52pt  SE 1.076pt  PASS
  quinella_place 払戻率 80.72%  乖離 +0.72pt  レース間SD 0.62pt  SE 0.438pt  PASS
  quinella       払戻率 83.27%  乖離 +3.27pt  レース間SD 0.18pt  SE 0.129pt  FAIL
  exacta         払戻率 82.88%  乖離 +2.88pt  レース間SD 0.64pt  SE 0.452pt  FAIL
  trio           払戻率 79.27%  乖離 +2.27pt  レース間SD 1.11pt  SE 0.787pt  FAIL
  trifecta       払戻率 77.66%  乖離 +0.66pt  レース間SD 1.44pt  SE 1.016pt  PASS
  ★上限に当たった売り目: 0 件（D-035 の下では 0 のはず）★V-10 総合: FAIL
```

★**2 レースの probe なので、これも判定ではありません**（SE は win 3.130pt・trifecta 1.016pt で 0.25pt に遠い）。ただし **M を本番の値にすると乖離の姿がまるで変わる**ことは読めます:

| | M = 10,000（§1g の表） | **M = 3,896,104（probe・2 レース）** |
|---|---|---|
| win | −3.27pt | **−0.71pt** |
| trio | −16.18pt | **+2.27pt** |
| trifecta | **−27.24pt** | **+0.66pt** |

→ ★**§1g の大きな負の乖離は「打ち切り」（M が小さいこと）の効果**でした。`odds.ts` の註記「正典 §9.2 の 10,000 では足りません（三連単で −20.90pt）」と同じ向きです ✔

### 1g-3. ★V-10 を閉じるのに要る時間（実測からの外挿・計算）

SE は 1/√（レース数）で減ります。probe の SE から、**0.25pt に入れるのに要るレース数**は:

| 券種 | probe の SE（2 レース） | 0.25pt に要るレース数 | 所要（1 レース 100 秒） |
|---|---|---|---|
| win | 3.130pt | **約 314 レース** | **約 8.7 時間** |
| place | 1.076pt | 約 38 レース | 約 1.1 時間 |
| quinella_place | 0.438pt | 約 7 レース | 約 12 分 |
| quinella | 0.129pt | **2 レースで足りている** | — |
| exacta | 0.452pt | 約 7 レース | 約 12 分 |
| trio | 0.787pt | 約 20 レース | 約 33 分 |
| trifecta | 1.016pt | 約 34 レース | 約 57 分 |

- **律速は `win`（約 314 レース・約 8.7 時間）**です。★2 レースの SE から外挿した値なので、幅があります（△）
- ⚠️ ★**この見積りは「流せば必ず PASS になる」という意味ではありません。** 測れるようになるだけで、乖離が ±1pt に収まるかは**測ってみないと分かりません**（probe では quinella +3.27pt・exacta +2.88pt が帯の外でした）

### 1g-4. ★オーナー・レビュー側に決めていただくこと

| # | 判断 |
|---|---|
| Q-V10-1 | **`verify-pmin --races 320 --seeds 42`（本番の M・約 9 時間）を流すか**。流すなら、開発側のこのセッションで裏で回すか、オーナーの別ターミナルか（★ES 便の前と違い、メモリ不足で落ちる形ではなくなっています） |
| Q-V10-2 | 先に **`--races 40` 程度（約 1.1 時間）**で `win` 以外を閉じ、`win` だけ後で長く回すか（★段階的に閉じる） |

### 1g-5. ★本番の試行数・40 レースの実測（2026-09-16 16:47）✔

`npx tsx apps/cli/src/verify-pmin.ts --races 40 --seed 42`（**M = 3,896,104**・確定 20,000 回/レース・**所要 1 時間 20 分**・15:27 → 16:47）

| 券種 | 払戻率 | 乖離 | レース間 SD | **SE** | 売目/R | 未発売の的中 | 判定 |
|---|---|---|---|---|---|---|---|
| win | 81.44% | −0.56pt | 1.18pt | **0.186** ✔ | 13 | 0.00% | PASS |
| **place** | 80.73% | ★**−1.27pt** | 0.62pt | **0.099** ✔ | 13 | ★**2.20%** | ★**FAIL** |
| quinella_place | 79.70% | −0.30pt | 0.58pt | **0.092** ✔ | 63 | 2.00% | PASS |
| quinella | 79.78% | −0.22pt | 1.30pt | **0.205** ✔ | 67 | 0.23% | PASS |
| exacta | 79.75% | −0.25pt | 1.29pt | **0.204** ✔ | 133 | 0.24% | PASS |
| trio | 76.70% | −0.30pt | 1.40pt | **0.221** ✔ | 249 | 0.22% | PASS |
| trifecta | 76.50% | −0.50pt | 1.84pt | ★**0.290 ✖** | 1,588 | 0.11% | 乖離は内側／**SE 不足** |

★**上限に当たった売り目 0 件**（D-035 の設計どおり）／ 総合 FAIL・exit 1

#### 読み

1. ★**2 レースからの外挿は大きく外れていました。** §1g-3 で「`win` に約 314 レース（8.7 時間）」と見積もりましたが、**実測では 40 レースで SE 0.186pt** です。**2 レースの SE から外挿してはいけない**（レース間 SD の推定が 2 標本では暴れる）という記録として残します △→✔
2. ★**SE 0.25pt を満たしたのは 7 券種中 6 つ**。残るのは **`trifecta` の 0.290pt** だけです。**80 レースなら約 0.205pt**（1/√2）の見込みで、**追加の測定を実行中**です（約 2.4 時間）
3. ★**`place` の −1.27pt は「読み取れる外れ」です**（SE 0.099pt に対して **約 13 SE**）。**測定の誤差では説明できません**

#### ★`place` が帯を外れたことについて（★較正定数は動かしていません）

- ~~**原因の見立て（△ 未確認）**: **未発売の的中が 2.20%** あります（`quinella_place` も 2.00%）。**D-035 の売る下限**（`p_eff < p_min` の目は売らない）と **D-096**（切り捨て前 1.0 倍未満は売らない）で売られなかった目が当たると、**賭け金は払ったのに払戻が無い**ので、払戻率が下がります~~
- 🔴 **【2026-09-16 訂正】★上の見立ては誤りでした。** ★`apps/cli/src/v10-accounting.ts` の `accountRaceKind` は、★**`decision === 'sell'` のときだけ `raceStake += 1`** します。★売らなかった目は ★**賭け金（分母）に入らず、払戻（分子）にも入りません**。★`unseenHits`（MC で一度も出なかった目）も `counts` に無いので同じく入りません。
  → ★**未発売の的中は、払戻率を 1 ビットも下げません。** ★「賭け金は払ったのに払戻が無い」と書きましたが、★**賭け金を払っていません**。★私は数字（2.20%・3.32%）と乖離（−1.27pt）が同じ向きに見えたことから、★**実装を読まずに因果を書きました**。
- ★**したがって Q-V10-3 の選択肢 ①（未発売の的中を除いた払戻率を測る）は無効です** — ★**既に除かれています**。★測っても同じ数字が出るだけで、★1 本 6 時間の実行を空振りさせるところでした。
- ★**残る候補**（★いずれも未確認）: ①`diag-mneed.ts` が名指しする **`c ≧ 1` の打ち切り**（★ただし `place` は `M·p_min = 82` で λ* = 30 を満たすので、★この説明は弱い）②★**`ODDS_CAP.place = 100` が 7 券種で最小**であることに由来する何か（★`p_min` が最も大きく、★売らない目の割合も最大）③D-013 の補正が `place` の確率帯で効き方が違う
- ★**切り分けは重い実測なしにできます** — ★`diag-mneed.ts` は**乱数を使わず二項分布で厳密に計算**します。★券種を絞る口（`--kinds`）を足したので、★`place` だけなら参照 MC 10 万で足ります（★`trio` は 26 万を要求するため、従来は全券種そろわないと止まっていました）

#### ★【2026-09-16】厳密計算の結果 — ★オッズの付け方では説明できません

★`npx tsx apps/cli/src/diag-mneed.ts --seed 42 --races 6 --ref-trials 100000 --targets 10000 --pmin --kinds place,win,quinella_place`

| 条件 | `place` | `win` | `quinella_place` |
|---|---|---|---|
| ★**本番条件**（`productionRaceOf` ＋ 凍結した走路・★既定に改めた） | ★**−0.00pt** | −0.00pt | −0.38pt |
| 旧来条件（`--legacy-conditions`） | −0.05pt | −0.00pt | −0.41pt |
| D-013 の補正を外す（`--no-debias`・旧来条件） | +0.04pt | +0.36pt | — |
| ★**実測**（`verify-pmin`・80 レース・M=3,896,104） | ★**−1.26pt** | −0.50pt | −0.35pt |

★**読み**:
- ★**どの切り分けでも `place` は帯の内**（−0.00／−0.05／+0.04）。★**1.2pt の食い違いは、オッズの式（推定バイアス・`c≧1` の打ち切り・配当上限・D-013 の補正・売る下限）では説明できません**
- ★条件の違い（本番／旧来）で動いたのは **0.05pt** だけ。★これも説明になりません
- ★実測のほうが **M が 390 倍大きい**（3,896,104 対 10,000）。★推定は良くなる側なので、★「M が足りない」でも説明になりません

★**道具の穴を 1 つ直しました**: ★`diag-mneed.ts` は ★**旧来の条件**で計算する一方、★`verify-payout.ts` / `verify-pmin.ts` は ★**本番条件**で測っていました（★両者が別の出走表を見ていた・R-30 の家族）。→ ★**既定を本番条件に改め**、`--legacy-conditions` で戻せるようにしました。

#### ★【2026-09-16】見立てを 3 つ立て、★**3 つとも否定されました**

| # | 見立て | ★検証 | ★結果 |
|---|---|---|---|
| ① | **未発売の的中**が払戻率を下げる | `v10-accounting.ts` を読む | 🔴 **誤り**。★売らない目は**賭け金（分母）に入りません**。★払戻率を 1 ビットも下げません |
| ② | **オッズ用と確定用で分布が違う**（`STREAM.ODDS` と `STREAM.FINAL`） | `out/gen/es/diag-place-gap.mjs`（20 レース・オッズ 10,000・確定 2,000） | 🔴 **否定**。★差は **+0.31pt** で ★**符号が逆**（確定側のほうが高い）。★馬ごとの複勝率の食い違いも平均 0.697pt で方向が揃わない |
| ③ | **「1 レース 1 確定」の手順**が偏りを持つ（★`place` は 1 本の着順が 3 目ぶんの払戻を同時に決める） | `out/gen/es/diag-place-procedure.mjs`（20 レース・400 回くり返し） | ⚠️ **判定不能**。★`place` 乖離 **+0.50pt** に対し平均の SE **0.75pt**（0.66 倍）／★対照の `win` は **−1.49pt** に対し SE **1.41pt**（1.06 倍）。★**どちらも 3σ に遠く、あるともないとも言えません**（R-3） |

★**分かったこと**: ★`place` の −1.26pt は ★**オッズの付け方（推定バイアス・`c≧1` の打ち切り・配当上限・D-013 の補正・売る下限・条件の違い）では説明できません。** ★厳密計算はどの切り分けでも帯の内（−0.00／−0.05／+0.04pt）です。

⚠️ ★**私の測り方の誤りを 2 つ記録します**:
- ★①は ★**実装を読まずに因果を書きました**（★数字が同じ向きに見えただけ）。★`accountRaceKind` を読めば 1 分で分かることでした
- ★③の出力に「1 回あたりの SE」という欄を作り、★**SD と同じ値**を入れていました。★意味の無い欄を根拠に読もうとしました。→ ★**平均の SE ＝ SD ÷ √くり返し**に直しました

★**③を判別するのに要る精度**: ★1.26pt を 3σ で見るには **平均の SE ≦ 0.42pt**。★`place` はいま 0.75pt なので、★**くり返しを 400 → 約 1,300 回**（所要は約 3.25 倍）。⚠️ ★**重い実行になるので、指示を待ちます**（★オーナーの別端末で回す種類の作業に近づきます）。

★**較正定数は 1 つも動かしていません**（★裁定 §4-3 の約束）。★道具の穴を 1 つ直しました（`diag-mneed.ts` の条件を本番に）。
- ★**較正定数（`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`・`margin`）は動かしていません**（裁定 §4-3 の約束）。値と向きだけを報告します

#### 照会（Q-V10-3・レビュー側へ）

| # | 内容 |
|---|---|
| **Q-V10-3** | ~~**`place` の −1.27pt を、どう扱うか。** ①「未発売の的中を除いた払戻率」を測って切り分ける（開発側の推奨）②D-096・D-035 の売る下限が複勝に効きすぎていないかを見直す③このまま記録して先へ進む~~ |
| **Q-V10-3'**（★2026-09-16 改訂） | ★**選択肢 ① は無効になりました**（★未発売の的中は**既に分母から除かれています**。★測っても同じ数字が出るだけで、★1 本 6 時間を空振りさせるところでした）。★**選択肢 ② も弱まりました**（★厳密計算では売る下限を入れても `place` は −0.00pt）。→ ★**改めて伺います**: ⓐ**くり返しを 1,300 回に上げて「1 レース 1 確定」の偏りを判別する**（★平均の SE 0.42pt まで下げる・開発側の推奨。★重いのでオーナーの端末で回すか要相談）／ⓑ**V-10 の測り方そのものを見直す**（★1 レースにつき確定を多数引いて分散を下げる案は、`verify-payout.ts` の冒頭で既に提案されたまま未裁定です）／ⓒ**このまま記録して先へ進む**（★`place` の −1.26pt は未解明のまま残る） |

### 1g-6. 監査 AF-2・BF-6 について

指示書は「**この 1 本で AF-2 と BF-6 も閉じる**」としていますが、**上のとおり判定不能なので、まだ閉じていません**。閉じるには §1g-2 の道（本番の M ＋ 分散低減）で測り直す必要があります。★**値（`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`）は動かしていません。**

- ★**D-096 の「売らない目を賭け金から外す」は効いています**（§1g の表の「D-096で売らず」の列。複勝で 0.1/R・的中 23・売っていたら払戻 22.0）✔
- ★**D-035 の上限に当たった売り目は 0 件**でした（probe・本番の M）。設計どおりです ✔

---

### 1g. ★V-10 を 80 レースで取り直しました（2026-09-16 20:08 完了）

`npx tsx apps/cli/src/verify-pmin.ts --races 80 --seed 42`（M=3,896,104・確定 20,000 回/レース・3h20m）

| 券種 | 払戻率 | 乖離 | SE | 未発売 | 判定 |
|---|---|---|---|---|---|
| win | 81.50% | −0.50pt | 0.120 | 0.00% | PASS |
| **place** | 80.74% | **−1.26pt** | 0.069 | **3.32%** | ★**FAIL** |
| quinella_place | 79.65% | −0.35pt | 0.072 | 2.17% | PASS |
| quinella | 79.89% | −0.11pt | 0.149 | 0.25% | PASS |
| exacta | 79.96% | −0.04pt | 0.157 | 0.26% | PASS |
| trio | 76.59% | −0.41pt | 0.177 | 0.25% | PASS |
| **trifecta** | 76.47% | −0.53pt | **0.222** | 0.13% | ★**PASS**（★40 レースでは SE 0.290 で判定できていませんでした） |

★**7 券種すべてで SE ≤ 0.25pt（D-036）を満たしました。** ★判定できなかった `trifecta` が解け、
★**残る不合格は `place` の −1.26pt だけ**です。

⚠️ ★**40 レースの −1.27pt と 80 レースの −1.26pt はほぼ一致** — ★**外れ値ではなく再現する偏り**です。
★`place` だけ**未発売が 3.32%**（他は 0.1〜2.2%）で、★D-035／D-096 で売らない目の分だけ払戻率が下がる、
という仮説と向きが合います。★**較正定数には触れていません**（Q-V10-3 の裁定待ち）。

## 2. 検査の全体 ✔

- `npm run typecheck` … **通過**（exit 0・`apps/web` を含む）
- `npx vitest run --maxWorkers=2 --minWorkers=1`（★GB-1〜GB-6 の全部が入った状態）… `Test Files 1 failed | 185 passed (186)` ／ `Tests 1 failed | 1829 passed (1830)`・終了コード 1・**116 秒**
  - 落ちたのは変更前からの `edit-grammar-audit` ⑨（独走代表 expected 'contest' to be 'solo'）だけで、**1 件から増えていません**
  - 件数は 1787 → **1830**（+43）。増えたのは本便の 7 ファイルです: `menu-view` 6 ／ `training-view-wiring` 5 ／ `ownership` 4 ／ `jockeys` 5 ／ `jockey-no-effect` 3 ／ `stars` 5 ／ `horse-market` 6 ／ `grade` 5 ／ `economy-balance` 4
- ⚠️ **較正定数の登録簿に 2 回落ちました**（`calibration-registry.test.ts`）。新しく `export` した数が未登録だったためです。**いずれも正典の写しか、値そのものをゲートにしないもの**なので、`EXEMPT` に**理由付きで**載せました（較正値としては登録していません）:
  - 1 回目: `TRAINING_BAR_MAX`・`CONDITION_STEPS`（GB-1）
  - 2 回目: `CAREER_ASSUMPTION`（GB-6）
  - まとめて登録したもの: `OWNERSHIP_LIMITS`・`ENTRIES_PER_OWNER_MAX`（GB-2）／`JOCKEY_EFFECT`・`JOCKEY_BOND_MAX`・`JOCKEYS`（GB-3）／★の刻み 4 つ・市場の 4 つ（GB-4）／`STABLE_GRADE_MULT`・`STABLE_GRADE_LABEL`（GB-5）

## 3. 触ったもの

| 触った | 触っていない |
|---|---|
| **`packages/training/src/`**（`menus`・`view`・`grade`・`index`）・**`packages/scheduler/src/`**（`ownership`・`jockeys`・`horse-market`・`index`）・★**`packages/sim-engine/src/`**（`stars.ts` を**新設**・`index`）・`apps/cli/src/`（`calibration`・`economy-balance`）・`apps/web/`（`package.json`・`src/lib/game-demo.ts`・`src/app/training/page.tsx`）・各 `test/` 9 ファイル | `packages/race-engine/src`（**1 行も変えていない**）・較正定数の**値**・`ODDS_MC_TRIALS`・`LAMBDA_STAR`・`ODDS_CAP`・`CYCLE_MS`・`LANE_MODEL`・正典・本番 DB・ワーカーの配備 |

- ★**【2026-09-16 訂正・GC-1】** 初版はこの表の「触っていない」列に `packages/sim-engine/src` を入れていましたが、**GB-4 で `stars.ts` を新設しています**（★表示の算出）。**自己矛盾していたので直しました**（裁定 `REVIEW_GAME_BODY_1_VERDICT_20260916.md` §2）。`git diff --stat d3159f2..HEAD -- packages/sim-engine/src` に出ます

## 3b. ★着順に効くものを入れていないこと（指示書 §7 の必須項目）✔

| 根拠 | 中身 |
|---|---|
| **対照の実測** | 騎手 6 人 × 8・18 頭 × 1200・2400m × シード 8 ＝ **192 通り**で `resolveRace` を回し、**着順・スコア・走破タイムが 1 ビットも同じ**（`apps/cli/test/jockey-no-effect.test.ts`） |
| **触っていない範囲** | `packages/race-engine/src` は **1 行も変えていません**（`git show --stat` で確認できます） |
| **育成のゲート** | V-14・V-15・V-7a/V-7b が **GB-1 の前と同値**（§1e-3。写像は見せ方だけ・格の既定はブロンズ 1.0） |
| **着順のゲート** | V-4〜V-6・V-17・V-18 は**この便で動く要素がありません**（着順の計算にも走路にも触れていないため）。流していません △ |

## 3c. 未実装で残したもの（指示書 §7）

| # | 中身 | いつ |
|---|---|---|
| 1 | **出走登録の RPC**（同じレース 2 頭までの適用・騎手の保存・凍結の列） | 出走登録の便 |
| 2 | **購入の DB・RPC・画面**（候補の抽選・記帳・在庫の実測） | 利用者向け RPC の便 |
| 3 | **格の解放（EP）と週送りへの配線**（`advanceWeek` に格を渡す） | 次の便 |
| 4 | **モードの切り替えの画面・ホーム・ハンバーガー** | ★デザイナーのカード待ち（指示書 §8） |
| 5 | **騎手の絵・「当たり」の演出** | ★デザイナーのカード待ち |
| 6 | **§9.5 を正典に合わせる移行**（いまの `place_bet` は 2 頭出すと正典より緩い・§1b-3） | 投票の RPC の便 |
| 7 | **V-11 は流しました**（✔ PASS・§1f-6）。ただし**購入と騎手の料金は一巡に入っていません**（RPC が未実装）。入った後に流し直します | 次の便 |

## 4. オーナーに見ていただきたいこと [EYES]

- 調教画面の並びが「体・心 × 弱中強」になりました。**枡の分け方（何を体、何を心に置くか）は正典 §7.2 の註記どおり**ですが、画面の見た目（枡の見出し・強度の色）は開発側の暫定です。**画面の構成そのものはデザイナーのカード待ち**なので、気になる点があれば、その旨をデザイナーへの依頼に含めます
