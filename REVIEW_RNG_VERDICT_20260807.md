# 乱数導出の相関疑義への回答 — `deriveSeed` は健全。§8.6 に波及しません

**発行**: 2026-08-07（レビュー側 Claude）
**対象**: `deac878`（同一 index の別ドメイン相関の疑い）
**判定**: **仮説は否定。作業を再開してください。ただし +22pt の異常は未解明のまま残ります**

---

## 0. 止めた判断は正しいです

§8.6 の Provably Fair と race-engine の馬ごとサブストリームに波及しうる、という読みは妥当でした。**「正典の改訂が要る可能性がある」で止める**のは合意した3条件どおりです。そして **(B)（レースを挟まず乱数そのものを検定する）のほうが筋がよい**という判断も正しい — 交絡を減らす方向です。

**(B) はレビュー側で回しました。結論は否定です。**

---

## 1. 検定結果 — 相関はありません

`packages/sim-engine/src/rng.ts` の `splitmix32` / `Rng` / `deriveSeed` を逐語移植し、**20万サンプル**で検定しました。

| 検定 | 内容 | Pearson r | \|r\|/SE |
|---|---|---|---|
| 1 | index=7 固定・master を振る | 0.002983 | 1.33 |
| **2** | **master=42 固定・index を振る（★実運用の形）** | **0.000688** | **0.31** |
| 3 | 是正案（FINAL の index に +9,000,017） | 0.002368 | 1.06 |
| 4 | 先頭1個でなく**先頭8個の和**で比較 | 0.000400 | 0.18 |
| 5 | FIELD × SKILL | −0.002059 | 0.92 |
| 5 | FINAL × SKILL | 0.003567 | 1.60 |
| 6 | **導出シードの衝突**（3ドメイン×10万本） | **0件** | 誕生日期待値 10.5件 |

**すべて 1.6 SE 以内 = 相関なし。**

### ★是正案のほうが r が大きい

**検定2（現状 r=0.00069）より検定3（是正後 r=0.0024）のほうが相関が大きく出ています。** どちらもノイズの範囲ですが、**「ずらしたら直った」という因果があるなら、ずらした後のほうが相関が小さくなるはずです。逆になっています。**

### 検定6 は健全性の積極的証拠です

衝突が誕生日パラダイム期待値 10.5件に対し **0件**。`deriveSeed` は固定ドメイン内で index に対し単射（`acc ^ index` が単射、finalizer が全単射）なので、これは期待どおりの挙動です。**乱数もどきではなく、設計どおりに振る舞っています。**

### 再現用（そのまま `node` で走ります）

```js
function rotl(x,k){return((x<<k)|(x>>>(32-k)))>>>0}
function splitmix32(seed){let a=seed>>>0;return()=>{a=(a+0x9e3779b9)>>>0;let t=a;
  t=Math.imul(t^(t>>>16),0x21f0aaad)>>>0;t=Math.imul(t^(t>>>15),0x735a2d97)>>>0;return(t^(t>>>15))>>>0}}
class Rng{constructor(s){const n=splitmix32(s);this.s0=n();this.s1=n();this.s2=n();this.s3=n();
  if((this.s0|this.s1|this.s2|this.s3)===0)this.s0=0x9e3779b9}
 nextUint32(){const r=(Math.imul(rotl(Math.imul(this.s1,5),7),9)>>>0);const t=(this.s1<<9)>>>0;
  this.s2=(this.s2^this.s0)>>>0;this.s3=(this.s3^this.s1)>>>0;this.s1=(this.s1^this.s2)>>>0;
  this.s0=(this.s0^this.s3)>>>0;this.s2=(this.s2^t)>>>0;this.s3=rotl(this.s3,11);return r}
 float(){return this.nextUint32()/4294967296}}
function deriveSeed(m,...p){let a=m>>>0;for(const x of p){const n=splitmix32((a^(x>>>0))>>>0);a=n()}return a>>>0}
const deriveRng=(m,...p)=>new Rng(deriveSeed(m,...p));
function pearson(a,b){const n=a.length;let ma=0,mb=0;for(let i=0;i<n;i++){ma+=a[i];mb+=b[i]}ma/=n;mb/=n;
 let sa=0,sb=0,sab=0;for(let i=0;i<n;i++){const x=a[i]-ma,y=b[i]-mb;sa+=x*x;sb+=y*y;sab+=x*y}
 return sab/Math.sqrt(sa*sb)}
const N=200000,a=[],b=[];
for(let i=0;i<N;i++){a.push(deriveRng(42,2,i).float());b.push(deriveRng(42,4,i).float())}
console.log('r =',pearson(a,b),' |r|/SE =',Math.abs(pearson(a,b))*Math.sqrt(N));
```

---

## 2. 作業を再開してください。§8.6 は無傷です

**Provably Fair・馬ごとサブストリーム・K-2 の系列独立性はいずれも影響を受けません。** 正典の改訂は不要です。

---

## 3. ★ただし +22pt の異常は説明されていません

原因が乱数導出でないなら、**単勝の払戻率が 96〜104%（期待 ≒ 80%）という異常は未解明のまま残ります。** ここを流さないでください。

### 最有力の候補は、Q-1 で書いた凸性バイアスです

オッズは `odds = (1/p̂) × (1 − margin)` で作られます。MC 試行回数を M、真の勝率を p とすると:

```
E[1/p̂] ≈ (1/p) × (1 + (1−p)/(M·p))
```

**払戻率の押し上げ量は `(1−p)/(M·p)`** です。1番人気相当 p=0.2 で:

| M（レースあたり MC 回数） | 押し上げ |
|---|---|
| 1,000 | +0.4% |
| 100 | **+4%** |
| 20 | **+20%** |

→ **M が小さければ +22pt は凸性だけで説明がつきます。** 「凸性では説明できなかった」という判断の根拠に、この `(1−p)/(M·p)` という**大きさの見積もり**は入っていましたか。入っていなければ、まずここを確認してください。

### 指示

**Q-1 で出したバイアス測定を、そのまま診断に使ってください。** 別作業ではありません:

1. **M を `1k / 3k / 10k / 30k` と振り、払戻率が 80% に向かって収束するか**を見る
2. **収束するなら凸性で確定**。A-3 の設計は「M を平坦点に取る」で解決します
3. **収束しないなら別の原因**。そのとき初めて次の仮説に移ります

### (A) の再測定も、問い直しを変えて必要です

**300レースの払戻率の SE は概算で ±9pt 程度**（1レースあたり払戻の SD が平均の2倍前後のため）。**22pt の差は 2.4 SE で、単独では有意とは言えません。**

→ (A) は「相関があるか」ではなく **「+22pt はそもそも実在するか」** を問う形で回してください。**複数シードで 96〜104% が再現するなら実在**、ばらけるなら最初の観測がノイズだったことになります。

---

## 4. 見つかった潜在的な危険（今日のバグではありません）[CODE]

**乱数ストリームの用途 ID 表が4つあり、番号が重なっています。**

| 表 | 値 |
|---|---|
| `packages/race-engine/src/race.ts:30` `RNG_STREAM` | SKILL:**1** / FINAL:**2** |
| `apps/cli/src/verify-payout.ts:55` `STREAM` | POOL:**1** / FIELD:**2** / ODDS:3 / FINAL:4 |
| `apps/cli/src/preseed.ts:45` `STREAM` | FOUNDER:**1** / MATING:**2** / NAMING:3 / NICKS:5 |
| `apps/cli/src/verify-race.ts:100` `STREAM` | FIELD:**11** / POPULARITY:12 / DECIDE:13 / INTERVENTION:14 |

**`verify-race.ts` だけが 11〜14 という重ならない帯を取っています。** つまり**この規約は一度確立されていたのに、P2 で新設した `verify-payout.ts` が 1〜4 に戻しました。**

今日は衝突しません（`RNG_STREAM` はレースシード、`STREAM` はマスターシードに適用されるため）。**しかし「別の表の同じ番号が、同じマスターシードに当たらない」ことを保証しているものが何もありません。** 一度当たれば、まさに今回疑ったとおりの相関が本当に発生します。

→ **構造で閉じてください。** 例: 全ストリーム ID を1か所の列挙にまとめて重複を型で禁じる、または**文字列ドメインをハッシュして数値にする**（`deriveSeed(seed, hash('payout.field'), i)`）。

**これは今回の調査の副産物として最も価値のある発見です** — 疑った現象は起きていませんでしたが、**起こりうる構造は実在していました。**

---

## 5. 評価

**否定される結果に終わりましたが、この調査は正しい仕事です。**

- **切り分け1（引き方の同分布性）を先に潰した** — χ²=6.5 / df=9 は適切な検定です
- **1点観測であることを自分で明示した**（「この1回だけで断定はできません」）
- **(A) より (B) のほうが筋がよいと判断した** — レースを挟むと交絡が増えるという理由づけが正確です
- **波及範囲を見て止めた** — §8.6 に及ぶなら止めるのが正しく、**結果が否定でも判断は正しい**

**仮説が外れたことと、止めた判断が誤りだったこととは別**です。レビュー側もこの P1.5 で三度外しています。**外れた仮説を測定で潰せる形にしてから止める**のが、今回できていたことです。
