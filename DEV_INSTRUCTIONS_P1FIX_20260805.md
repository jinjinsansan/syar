# 開発指示書 P1-fix — 較正層の防御と §8.8 の完成

**発行**: 2026-08-05（レビュー側 Claude）
**前提**: `REVIEW_P1_VERDICT_20260805.md`（特に §7）を先に読むこと
**正典**: `STAR_SPEC_v2.0.md`（**D-014〜D-019 で §5.2/§8b.2/§10.4/§13.1/§13.2 を改訂済み。読み直すこと**）

---

## 0. まず P1 の評価

**K-1（レース判定）・K-2 の乱数系列の独立性・K-3 の介入モデルは、実装本体が正典に忠実でテストの検出力も高い**です。監査の変異試験21件のうち、§8/§8b に関わる改変は**すべて既存テストが捕捉**しました。乱数の独立性は end-to-end で実証され（人気推定の系列を意図的に汚染すると1番人気勝率が +1.8pt 動く＝検定に検出力がある状態で、本番系列は無関係系列と誤差内）、N-1〜N-5・K-a も全件対応、**自己申告7件は全件正確でプローブ残留も完全復旧**していました。

照会6件はすべて回答し、**うち2件（Q2 スタミナ・Q4 V-8/V-9）はこちらが書いた正典の欠陥**でした。正典を D-014〜D-019 で改訂しています。

**一方で、K-4/K-5 の「較正と是正」の層はテストが1本も守っておらず、V-6 は測定方法の自由変数で通っていました。** 以下がその是正です。

---

## 1. スコープ

### O-1【必須・高】§8.8「介入ログ → 同じ着順」を完成させる

`InterventionInput`（記録用）から `InterventionPlan`（計算用）へ**変換する関数が存在せず**、`auditFailures` は着順を再計算していません。「seed だけで同じレース」は成立していますが、**指示書 K-2 と正典 §8.8 が求める「seed + 介入入力ログ → 同じ着順」は成立していません**。

介入がある以上、これが無いと**リプレイ・不正調査・サポート対応の一次資料が機能しません**（§8.8 を新設した理由そのものです）。

- `InterventionInput[]` → `InterventionPlan` の変換を実装する
- `auditFailures`（またはそれに相当する検証経路）が、**seed + 介入ログから着順を再計算して一致を確認**するようにする
- **経路テスト**で固定すること（R-1）。「変換関数が正しい」だけでなく「検証経路が実際にそれを使っている」まで

### O-2【必須・高】V-6 の測定を正し、V-4 と両立するか確かめる

`POPULARITY_TRIALS=60` は正典にない自由変数で、**V-6 の PASS はその推定ノイズの産物**でした:

| trials | V-4 | V-6 |
|---|---|---|
| 60（既定） | 32.49% PASS | **0.79% PASS** |
| 600 | 33.61% PASS | **0.27% FAIL** |

60試行では下位馬の大半が0勝で並び、**タイブレークが枠順**になります。「最低人気」が実際には「0勝で並んだ中で最も外枠の馬」でした。

**やること:**

1. **人気の推定精度が判定に効かない試行回数を求め、それを既定にする**（R-12）。効く範囲なら較正条件として正典に明記が要ります
2. **正しい測定のもとで V-4 と V-6 が両立するかを確かめる**

2 が本題です。正しく測ると **最低人気 0.27% ＝ 正典 §13.2 の「ゼロではない＝夢は残す」が成立していません**。そして K を上げると V-6 は改善しますが **V-4 が下限を割ります**（あなたの表で K=0.32 → 30.6%）。

比較のため: 現実の競馬は1番人気32%・大穴1〜2%で**比 20〜30倍**。本実装は 33% 対 0.27% で **比122倍** — **裾が現実より遥かに広い**。したがって対処は「K をさらに上げる」ではなく、**出走表の能力幅を締める**方向です（クラス帯を 6% より狭める／1レース内の能力レンジに上限を設ける等）。

**両立する設定が見つからない場合は、見つからないことを実測で示して照会に上げてください。** 目標値の見直しはオーナー承認事項です。

### O-3【必須・高】`resolveRace` の入口で `interventionMult` をクランプする

実測:

```
interventionMult=5    → 着順 1/8（finalScore ×5）
interventionMult=100  → 着順 1/8（×100）
interventionMult=NaN  → 例外
```

**±10% のハードキャップ（憲法 §1.5-1）が、サーバー権威のスコア確定点（§8b.4）である `resolveRace` の入口で守られていません。** 純関数 `resolveIntervention` の中だけです。

**R-1 の5度目**ですが、今回は「テストが守っていない」ではなく「**実装が守っていない**」段階です。憲法事項なので最優先で塞いでください。

- `resolveRace` が受け取る `interventionMults` を**キャップ内にクランプし、非有限値を拒否する**
- 範囲外の入力が来たら**黙って直さず記録する**（不正の兆候かもしれないため）
- 経路テストで固定（範囲外の入力を `resolveRace` に与えて着順が支配されないこと）

### O-4【必須・高】K-4 の是正と較正定数をテストで固定する

**クラス係数を適用経路から外す改変を入れると V-2d が実際に FAIL するのに、`npm test` は 196/196 全緑のまま**通ります。テストは純関数 `classPrizeMultiplier` しか見ておらず、`runSeason` が実際に掛けているかを固定していません。**I-3/I-4/L-1 と同じ構造の4度目**です。

あわせて**較正定数がどれ1つ固定されていません**（変異試験で全緑）:

| 改変 | `npm test` |
|---|---|
| 開放率の幅 0.55–0.85 → 0.30–0.95 | 196/196 緑 |
| クラス幅 6% → 100%（クラス分け撤廃） | 196/196 緑 |
| `CALIBRATED_RACE_RANDOM_K` 0.26 → 0.12 | 196/196 緑 |
| 母集団を能力順に並べない | 196/196 緑 |

→ **K-4 の是正（クラス係数の適用）と、K=0.26 を成立させている前提（開放率の幅・クラス帯幅・母集団のソート）を、経路テストで固定してください。**

### O-5【必須・中】選抜指標に `prize/starts` を加えて比較し直す

`prize` は累計なので**在籍年数を測っています**:

```
r(累計賞金, 在籍年数) = 0.353      r(累計賞金, 能力合計) = 0.440
r(1走あたり賞金, 能力合計) = 0.503   選抜差も prize/starts が上（+10.68% 対 +9.33%）
```

3指標比較（prize / winRate / composite）に**出走数で正規化した指標が入っていません**。`winRate` は分散が大きすぎて V-1 を割ったとのことですが、`prize/starts` は正規化されていて分散も小さく、能力との相関も選抜差も上回ります。

**選抜指標は「強さ」を測るべきで「長く走ったこと」を測るべきではありません。** 比較に加えて再選定してください。

### O-6【必須・中】`INTERPRETATIONS` の網羅範囲を広げる

未登録の解釈が4件あります: `horseId` ソート／256bit→32bit の畳み込み／`Math.max(0,…)`／着差ラベルを「前の馬との差」で出す。

原因は**登録簿の走査範囲が `packages/race-engine/src` のみ**で、`apps/cli` 側のプレースホルダが対象外だったことです。報告書 §7 の「全件」はこの範囲での全件でした。

→ 走査範囲を広げ、上記4件を登録してください。**「全件」と書くときは、その全件がどの範囲の全件かを併記**してください（R-9 の一般形）。

### O-7【推奨・低】クランプの非対称と NaN

- `fatigueCoef` / `weightCoef` が非クランプ（`randomMult` には `Math.max(0,…)` の防御があるのに非対称）
- NaN が `clamp()` を素通りする
- `verify-race.ts` の `--json` が `jsonSafeNumber` を通っていない（**K-b の型宣言がどこからも使われていない**）

---

## 2. やらないこと

- 検証基準の目標値変更（オーナー承認事項）。O-2 で両立しないと分かったら**照会に上げる**
- 正典の定数変更（`RACE_RANDOM_K = 0.26` は D-016 で正典化済み。写しを更新するのは可）
- P2 以降のスコープ（開催スケジューラ・馬券・オッズ）

---

## 3. 合格基準

| # | 基準 |
|---|---|
| 1 | **seed + 介入ログ → 同じ着順**が再計算で確認でき、**経路テスト**で固定されている（O-1） |
| 2 | **人気推定の試行回数が判定に効かない**ことが示されている。V-6 が正しい測定で PASS するか、両立不能であることが実測で示され照会に上がっている（O-2） |
| 3 | `resolveRace` の入口で範囲外・非有限の `interventionMult` が支配的にならず、**経路テストで固定**（O-3） |
| 4 | クラス係数の適用と較正定数（開放率・クラス帯幅・K・ソート）が**経路テストで固定**。変異を入れると落ちること（O-4） |
| 5 | 選抜指標の比較に `prize/starts` が入り、再選定の根拠が示されている（O-5） |
| 6 | `INTERPRETATIONS` の走査範囲が `apps/cli` を含み、未登録4件が登録されている（O-6） |
| 7 | 既存の合格（V-4/V-5/V-8/V-9a/V-9b/V-12・K-4 の P0 ゲート・既定 proxy の verify が P0-fix5 と一致）が維持されている |
| 8 | テスト全PASS・typecheck exit 0・`any` ゼロ・決定論 |

---

## 4. 新しい恒久ルール2件

| # | ルール | 由来 |
|---|---|---|
| **R-11** | **検証ツールにも事後条件を持たせる**。「改変した」と出力するツールは、**改変が実際に入ったことを確認してから**そう出力する | あなたの自己申告#1（変異テストのスクリプトが空振りしたのに「改変した」と出力し、「テストが検出できない」という誤った結論を出した）。**M-1 の道具版** |
| **R-12** | **測定の自由変数（試行回数・サンプル数・並べ替え）は、判定に効かないことを確認するか、正典に固定する**。効くなら較正条件として明記する | V-6 が `POPULARITY_TRIALS=60` の推定ノイズで通っていた |

---

## 5. 報告様式

`REPORT_P1FIX_[日付].md` に:

1. O-1〜O-7 の対応
2. 再実行コマンド
3. **O-2 の結論**（試行回数の妥当な値／V-4 と V-6 が両立するか／両立しないなら実測での証明）
4. **O-4 の変異試験結果**（改変を入れると落ちることの実証）
5. **O-5 の指標比較**（`prize/starts` を含む）
6. 仕様からの逸脱・解釈（全件列挙。**「全件」の範囲を明記**）
7. 未解決の質問

---

## 6. 最後に

**§9 で失敗7件を自分から全部書いたこと、特に「M-1 と同型の3度目」と明記したことを評価します。** 隠されるより遥かによい。

一方で、今回の HIGH 4件（§8.8 未実装・V-6 の測定ノイズ・`resolveRace` の素通し・クラス係数の無防備）は**いずれも申告に含まれていませんでした**。共通しているのは「**実装した機能そのものではなく、その機能が本番経路で効いているか／測定が判定を作っていないか**」という層です。

R-1 が5度目、R-7 が形を変えて再発しています。**次便では「作ったもの」より先に「作ったものが効いているか」を確認する順序**で進めてください。O-4 の変異試験を報告に含めるよう求めているのはそのためです。
